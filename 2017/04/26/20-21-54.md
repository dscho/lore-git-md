# [PATCH 24/26] name-rev: avoid leaking memory in the `deref` case

| Header | Value |
|--------|-------|
| **From** | Johannes Schindelin <johannes.schindelin@gmx.de> |
| **To** | git@vger.kernel.org |
| **Date** | 2017-04-26T22:21:54+02:00 |
| **Message-ID** | [a0c807b75ea55cf944554f4c2cd4dd4126572277.1493237937.git.johannes.schindelin@gmx.de](https://lore.kernel.org/git/a0c807b75ea55cf944554f4c2cd4dd4126572277.1493237937.git.johannes.schindelin@gmx.de) |

---

**Thread**: [thread](./20-19-13.thread.md#:~:text=2017/04/26/20-21-54)

When the `name_rev()` function is asked to dereference the tip name, it
allocates memory. But when it turns out that another tip already
described the commit better than the current one, we forgot to release
the memory.

Pointed out by Coverity.

Signed-off-by: Johannes Schindelin <johannes.schindelin@gmx.de>
---
```
 builtin/name-rev.c | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

```

```diff
diff --git a/builtin/name-rev.c b/builtin/name-rev.c
index 92a5d8a5d26..a4ce73fb1e9 100644
--- a/builtin/name-rev.c
+++ b/builtin/name-rev.c
@@ -28,6 +28,7 @@ static void name_rev(struct commit *commit,
 	struct rev_name *name = (struct rev_name *)commit->util;
 	struct commit_list *parents;
 	int parent_number = 1;
+	char *p = NULL;
 
 	parse_commit(commit);
 
@@ -35,7 +36,7 @@ static void name_rev(struct commit *commit,
 		return;
 
 	if (deref) {
-		tip_name = xstrfmt("%s^0", tip_name);
+		tip_name = p = xstrfmt("%s^0", tip_name);
 
 		if (generation)
 			die("generation: %d, but deref?", generation);
@@ -53,8 +54,10 @@ static void name_rev(struct commit *commit,
 		name->taggerdate = taggerdate;
 		name->generation = generation;
 		name->distance = distance;
-	} else
+	} else {
+		free(p);
 		return;
+	}
 
 	for (parents = commit->parents;
 			parents;
-- 
```

2.12.2.windows.2.800.gede8f145e06




