# Re: [PATCH v2 1/1] sha1_file: Add support for downloading blobs on demand

| Header | Value |
|--------|-------|
| **From** | Christian Couder <christian.couder@gmail.com> |
| **To** | Ben Peart <peartben@gmail.com> |
| **Date** | 2017-07-14T17:18:27+02:00 |
| **Message-ID** | [CAP8UFD2tQQ3Dyx64LMtt_+WRoDS9V+AbMtvdCcngqiL5kCBFTg@mail.gmail.com](https://lore.kernel.org/git/CAP8UFD2tQQ3Dyx64LMtt_+WRoDS9V+AbMtvdCcngqiL5kCBFTg@mail.gmail.com) |

---

**Thread**: [thread](./13-26-50.thread.md#:~:text=2017/07/14/15-18-27)

On Fri, Jul 14, 2017 at 3:26 PM, Ben Peart <peartben@gmail.com> wrote:

> ```diff
> diff --git a/contrib/long-running-read-object/example.pl b/contrib/long-running-read-object/example.pl
> new file mode 100755
> index 0000000000..b8f37f836a
> --- /dev/null
> +++ b/contrib/long-running-read-object/example.pl
> ```
>


[...]

> ```
> +sub packet_bin_read {
> +       my $buffer;
> +       my $bytes_read = read STDIN, $buffer, 4;
> +       if ( $bytes_read == 0 ) {
> ```
>
> ```
> +
> +               # EOF - Git stopped talking to us!
> +               exit();
> +       }
> ```
>
> ```
> +       elsif ( $bytes_read != 4 ) {
> +               die "invalid packet: '$buffer'";
> +       }
> +       my $pkt_size = hex($buffer);
> ```
>
> ```
> +       if ( $pkt_size == 0 ) {
> +               return ( 1, "" );
> +       }
> +       elsif ( $pkt_size > 4 ) {
> ```
>
> ```
> +               my $content_size = $pkt_size - 4;
> +               $bytes_read = read STDIN, $buffer, $content_size;
> +               if ( $bytes_read != $content_size ) {
> +                       die "invalid packet ($content_size bytes expected; $bytes_read bytes read)";
> ```
>
> ```
> +               }
> +               return ( 0, $buffer );
> +       }
> +       else {
> ```
>
> ```
> +               die "invalid packet size: $pkt_size";
> +       }
> +}
> +
> +sub packet_txt_read {
> ```
>
> ```
> +       my ( $res, $buf ) = packet_bin_read();
> +       unless ( $buf =~ s/\n$// ) {
> +               die "A non-binary line MUST be terminated by an LF.";
> +       }
> ```
>
> ```
> +       return ( $res, $buf );
> +}
> +
> +sub packet_bin_write {
> ```
>
> ```
> +       my $buf = shift;
> +       print STDOUT sprintf( "%04x", length($buf) + 4 );
> +       print STDOUT $buf;
> +       STDOUT->flush();
> +}
> +
> +sub packet_txt_write {
> ```
>
> ```
> +       packet_bin_write( $_[0] . "\n" );
> +}
> +
> +sub packet_flush {
> ```
>
> ```
> +       print STDOUT sprintf( "%04x", 0 );
> +       STDOUT->flush();
> +}
> ```
>


The above could reuse the refactoring of t0021/rot13-filter.pl into
perl/Git/Packet.pm that is at the beginning of my patch series.

> ```diff
> diff --git a/t/t0410/read-object b/t/t0410/read-object
> new file mode 100755
> index 0000000000..85e997c930
> --- /dev/null
> +++ b/t/t0410/read-object
> ```
>


[...]

> ```
> +sub packet_bin_read {
> +       my $buffer;
> +       my $bytes_read = read STDIN, $buffer, 4;
> +       if ( $bytes_read == 0 ) {
> ```
>
> ```
> +
> +               # EOF - Git stopped talking to us!
> +               exit();
> +       }
> ```
>
> ```
> +       elsif ( $bytes_read != 4 ) {
> +               die "invalid packet: '$buffer'";
> +       }
> +       my $pkt_size = hex($buffer);
> ```
>
> ```
> +       if ( $pkt_size == 0 ) {
> +               return ( 1, "" );
> +       }
> +       elsif ( $pkt_size > 4 ) {
> ```
>
> ```
> +               my $content_size = $pkt_size - 4;
> +               $bytes_read = read STDIN, $buffer, $content_size;
> +               if ( $bytes_read != $content_size ) {
> +                       die "invalid packet ($content_size bytes expected; $bytes_read bytes read)";
> ```
>
> ```
> +               }
> +               return ( 0, $buffer );
> +       }
> +       else {
> ```
>
> ```
> +               die "invalid packet size: $pkt_size";
> +       }
> +}
> +
> +sub packet_txt_read {
> ```
>
> ```
> +       my ( $res, $buf ) = packet_bin_read();
> +       unless ( $buf =~ s/\n$// ) {
> +               die "A non-binary line MUST be terminated by an LF.";
> +       }
> ```
>
> ```
> +       return ( $res, $buf );
> +}
> +
> +sub packet_bin_write {
> ```
>
> ```
> +       my $buf = shift;
> +       print STDOUT sprintf( "%04x", length($buf) + 4 );
> +       print STDOUT $buf;
> +       STDOUT->flush();
> +}
> +
> +sub packet_txt_write {
> ```
>
> ```
> +       packet_bin_write( $_[0] . "\n" );
> +}
> +
> +sub packet_flush {
> ```
>
> ```
> +       print STDOUT sprintf( "%04x", 0 );
> +       STDOUT->flush();
> +}
> ```
>


Same thing about the above and perl/Git/Packet.pm.

Otherwise thanks for updating this!


