This is a code style cleanup patch in the ongoing `git cat-file --batch-command` remote object info series. It moves variable declarations inside for loops where they are used, improving scoping and readability. The changes are mechanical:

- Moves declarations of loop counters `i` inside their respective for loops in `builtin/cat-file.c` and `fetch-pack.c`
- Changes some `int i` declarations to `size_t i` where appropriate for array indexing
- Keeps the same loop logic while tightening variable scope
- Follows up on Christian Couder's review feedback from earlier versions

The patch is purely stylistic - no behavior changes. It's part of the final polish before merging the larger feature series, addressing minor code organization issues that were flagged during review. The changes are straightforward and uncontroversial, with the series maintainer (Eric Ju) indicating this is the final iteration before merging.