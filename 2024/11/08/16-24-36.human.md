This is a code style cleanup patch in the ongoing series adding remote object info queries to `git cat-file --batch-command`. The patch moves variable declarations inside their respective loops where they are used, improving scoping and readability. 

The changes are mechanical but important for code hygiene:
- Moves `i` declarations inside `for` loops in `builtin/cat-file.c` and `fetch-pack.c`
- Changes some loop counters from `int` to `size_t` where appropriate
- No functional changes - purely about variable scoping

This is part of the final polish for the series, addressing style feedback from Christian Couder. The patch follows the project's preference for tighter variable scoping when possible. With this change, the series appears to have addressed all outstanding review comments and is ready for merging.