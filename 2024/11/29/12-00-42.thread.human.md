This thread documents the final stages of a 7-patch series introducing a `--revision` option for `git clone`, which allows cloning a specific ref or commit hash without creating remote-tracking branches. The feature is particularly useful for CI workflows and targeted repository access. The series has undergone extensive review over six versions, with all technical concerns addressed and maintainer approval secured.

The latest patch refactors tag handling in the clone operation, moving the tags refspec addition earlier in the process to consolidate refspec handling. This change simplifies the code by eliminating duplicate logic while maintaining the same behavior. Patrick Steinhardt and Toon Claes have verified the technical implementation, including important safety checks like the `lookup_commit_or_die()` change for hardening against invalid inputs. Jean-Noël Avila has contributed documentation refinements to ensure clarity.

The series is now fully baked, with Junio C Hamano having queued it for integration. The implementation includes comprehensive test coverage in `t5621-clone-revision.sh` and carefully polished documentation explaining the feature's behavior and its interactions with options like `--tags` and `--depth`. The preparatory refactoring work has also improved the overall maintainability of `builtin/clone.c`. This represents a complete, production-ready feature that enables more efficient repository access patterns while adhering to Git's quality standards.