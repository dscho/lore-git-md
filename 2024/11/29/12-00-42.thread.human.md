Here's a human-readable summary of the thread's current state:

This thread introduces a new `--revision` option for `git clone` that allows cloning specific commits without creating remote-tracking branches, which is particularly useful for CI systems. After six iterations, the series is now complete with all technical implementation finalized and documentation polished.

The feature enables cloning at exact revisions while detaching HEAD, works with `--depth` for shallow clones, and is incompatible with `--branch`/`--mirror`. The implementation involved significant refactoring of `builtin/clone.c`, including moving globals to local scope, inverting tag handling logic, and introducing new helpers like `die_for_incompatible_opt2()`. The documentation received careful attention from Jean-Noël Avila to ensure clarity and proper formatting.

The most recent patch focuses on cleaning up the `wanted_peer_refs()` function in preparation for the new feature. It simplifies the code by eliminating duplicate paths between single-branch and regular clones, improves memory management, and reduces complexity while maintaining all existing functionality. This refactoring was reviewed as part of the overall series, which has now received sign-offs from key contributors including Patrick Steinhardt and Toon Claes, with final documentation tweaks from Junio Hamano. The series is queued for integration, representing both a useful new feature and significant maintainability improvements to the clone machinery.