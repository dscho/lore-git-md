Here's a human-readable summary of the thread's current state:

This thread introduces a new `--revision` option for `git clone` that lets users check out a specific commit or ref without creating tracking branches. The series has gone through four iterations, with significant preparatory refactoring to clean up the clone internals before adding the new functionality. The core changes restructure how tags and branches are handled during cloning, moving several global states into local scope and simplifying the codebase. The final patch implements `--revision` to support both ref names and raw commit hashes while maintaining compatibility with existing options like `--depth`.

Jean-Noël Avila has been actively reviewing the series, first catching documentation issues in the tag handling changes and now providing copyedits for the `--revision` documentation itself. His latest feedback suggests minor formatting improvements to match Git's style guide, which shouldn't require significant rework. Meanwhile, Junio Hamano has raised some technical concerns about how incompatible options are handled in the implementation, particularly around the special treatment of `--branch` versus other conflicting options. 

The series appears technically sound but awaits the author's response to Junio's questions about implementation consistency. With Jean-Noël's documentation polish being straightforward to address, the main remaining discussion point is whether the option handling needs further refinement before merging. The preparatory refactoring has already landed, leaving just the new feature and its documentation to finalize.