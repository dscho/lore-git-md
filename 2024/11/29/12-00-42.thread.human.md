Here's an updated human-readable summary of the thread:

This thread introduces a new `--revision` option for `git clone` that allows cloning a specific commit or ref without creating remote-tracking branches, instead detaching HEAD at the requested revision. The series has gone through five versions, with extensive preparatory refactoring before implementing the actual feature.

The latest version (v5) adds a new preparatory commit introducing a helper function for validating mutually exclusive options, following Junio's suggestion to separate this utility from the main feature. The series now includes significant structural improvements to `git clone`'s internals: moving global variables into local scope, inverting tag handling logic, refactoring reference handling code, and introducing a new `struct clone_opts` to consolidate clone-related state.

The most recent patch focuses on this new `struct clone_opts`, starting with a `wants_head` field that controls whether to fetch the remote HEAD (disabled when both `--single-branch` and `--branch` are used). This continues the series' careful approach of refactoring first, maintaining existing behavior while cleaning up the codebase for the new feature.

All feedback from previous rounds has been addressed, including documentation fixes from Jean-Noël Avila, technical suggestions from Junio, and code organization improvements from Patrick Steinhardt. The series now appears ready for integration with comprehensive test coverage and all preparatory work stabilized. Unless any final issues emerge in review, this feature is positioned for inclusion in an upcoming release.