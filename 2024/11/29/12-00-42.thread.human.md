Here's a human-readable summary of the thread's current state:

This thread introduces a new `--revision` option for `git clone` that lets users clone a specific commit or ref without creating tracking branches, leaving HEAD detached at the requested revision. The series has gone through four versions, with significant preparatory refactoring before implementing the actual feature.

The preparatory work has restructured clone internals, including consolidating global variables, inverting tag handling logic, and simplifying the ref mapping code. The latest patch refactors the `wanted_peer_refs()` function, reducing it by 15 lines while maintaining behavior. Patrick Steinhardt provided constructive feedback suggesting improvements to the commit message and using proper types for loop counters.

The core `--revision` implementation allows cloning by ref name or commit hash, works with shallow clones, and is incompatible with other branch-related options. Junio Hamano raised some technical concerns about option handling consistency and documentation formatting, while Jean-Noël Avila has been polishing the documentation wording throughout the series.

The series appears technically sound but awaits responses to Junio's questions about implementation details and final documentation tweaks. The extensive preparatory refactoring suggests the author is taking care to keep the codebase maintainable while adding this new capability.