Here is the dense, loss-free AI thread summary for future agent sessions:

This thread introduces a new `--revision` option for `git clone` that allows cloning a single specific revision (ref or commit hash) without creating remote-tracking branches or local branches, detaching HEAD at the requested revision. The 6-patch series (currently at v4) begins with 5 preparatory refactorings of `builtin/clone.c` to improve code structure before adding the feature. Key technical aspects: the option conflicts with `--branch` and `--mirror`, creates no tracking branches/fetch refspecs, works with both refs and raw hashes, detaches HEAD for non-branch refs, and includes comprehensive tests in new file `t5621-clone-revision.sh`. The preparatory work reduces globals, refactors `wanted_peer_refs()`, and introduces `struct clone_opts`. The implementation handles edge cases (tags, raw hashes, bare repos) and maintains compatibility with `--depth` for shallow clones. The series has evolved through 4 versions, with v4 adding significant refactoring groundwork. Documentation updates clearly the behavior and constraints of the new option. The change appears well-considered with thorough test coverage, targeting CI system use cases needing minimal clones of specific versions. No reviews or feedback have been noted yet in this initial posting of the series.