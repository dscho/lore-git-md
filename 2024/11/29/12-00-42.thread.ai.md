Here is the updated dense, loss-free AI thread summary incorporating the new email:

This 7-patch series (expanded from 6 in v4) introduces a `--revision` option for `git clone` that enables cloning a specific ref or commit hash without creating remote-tracking branches, detaching HEAD at the requested revision. The series has progressed through 5 versions with extensive preparatory refactoring before implementing the feature.

Key structural changes in v5:
1. Added new preparatory commit (now patch 6/7) introducing `die_for_incompatible_opt2()` helper function with:
   - Example usage showing how it will be used in final implementation
   - Clear documentation of its purpose for validating mutually exclusive options
   - Follows Junio C. Hamano's suggestion to separate this utility from main feature

The preparatory work now includes:
1. Major restructuring moving global variables into `cmd_clone()`'s local scope (195-line change):
   - Conversion of global variables to local scope in `cmd_clone()`
   - Proper initialization and cleanup of string lists
   - Variables affected include `option_reject_shallow`, `deepen`, `option_template`, `option_depth`, `option_since`, `option_origin`, `option_not`, `real_git_dir`, `ref_format`, `option_upload_pack`, `option_progress`, `option_sparse_checkout`, `family`, `option_config`, `option_dissociate`, `option_filter_submodules`, `server_options`, `bundle_uri`
2. Inversion of tag handling internals from `--no-tags` to `--tags` (default-enabled) with Jean-Noël Avila's documentation fixes:
   - Changed internal boolean from `option_no_tags` to `option_tags` (default true)
   - Updated all conditionals checking the flag
   - Modified option parsing to use new boolean
   - Corrected documentation syntax from `--[no-]-tags` to `--[no-]tags`
3. Refactored `wanted_peer_refs()` with Patrick Steinhardt's feedback addressed:
   - Eliminated separate code path for `option_single_branch` by modifying `refs` list pre-processing
   - Introduced `to_free` pointer for better resource management
   - Simplified tag refspec handling with added explanatory comment
   - Removed redundant warning message (now handled elsewhere)
   - Fixed type safety by changing loop counter from `int` to `size_t` to match `refspec->nr`
   - Improved commit message guiding readers through changes
4. Tag refspec handling moved earlier in clone process (updated per latest patch):
   - Now appends `TAG_REFSPEC` to `remote->fetch` before `refspec_ref_prefixes()`
   - Eliminates temporary tag handling in `wanted_peer_refs()`
   - Reduces code by 16 lines
   - Properly disables tag handling for `--mirror` clones
   - Adds clarifying comments about timing of refspec addition
5. Introduction of `struct clone_opts` consolidating clone-related state (new patch):
   - Adds `wants_head` field (default true) controlling HEAD fetching behavior
   - Makes `tail_link_ref()` non-static by moving to remote.h with documentation
   - Modifies `wanted_peer_refs()` to use opts struct
   - Sets `wants_head = 0` when both `--single-branch` and `--branch` are used
   - Maintains existing behavior while providing cleaner foundation
   - Part of larger effort to eliminate clone.c globals
6. New helper function `die_for_incompatible_opt2()` (v5 addition):
   - Prepares for consistent option validation in final implementation
   - Includes example usage demonstrating intended pattern

The final implementation (patch 7/7) now includes:
- Core `--revision` functionality accepting refs or raw hashes
- Detached HEAD for all cases (unlike `--branch`)
- No tracking branches or fetch refspecs created
- Works with `--depth` for minimal clones
- Incompatible with `--branch` and `--mirror`
- Uses new `die_for_incompatible_opt2()` for consistent validation
- Simplified error handling with `lookup_commit_or_die()`
- Removed debug artifacts from previous versions
- Added explanatory comments for complex sections

Test coverage remains comprehensive in `t5621-clone-revision.sh` verifying:
- Cloning with branch/tag/HEAD refs and raw hashes
- Behavior with `--depth` and `--bare`
- Error cases (short hashes, trees, parent refs)
- Incompatibility with other options

All v4 feedback has been addressed:
1. Jean-Noël Avila's documentation improvements:
   - Consistent `--[no-]tags` syntax
   - Imperative mood in descriptions
   - Proper formatting for `--revision <rev>`
2. Junio C. Hamano's technical points:
   - Added separate helper function for option validation
   - Removed `// TODO` comment in favor of `/* */` style
   - Made option clearing more consistent
3. Patrick Steinhardt's suggestions:
   - Type safety fix for loop counter
   - Commit lookup simplification
   - Debug artifact removal
   - Code organization improvements
   - `wanted_peer_refs()` refactoring to eliminate separate code path
   - Latest tag refspec handling simplification moving addition earlier

The series appears ready for integration with:
- All preparatory refactoring stabilized
- Core functionality thoroughly reviewed
- Documentation polished
- Test coverage comprehensive
Pending any final review comments on the v5 changes, this feature is positioned for inclusion in the next development cycle.