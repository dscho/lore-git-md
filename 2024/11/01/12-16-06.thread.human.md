This thread is discussing optimizations to `git rev-list` when used with the `--left-right` flag, particularly around bitmap traversal behavior. The core issue appears to be that bitmap traversal is currently skipped for `--left-right` operations, likely because the bitmap implementation doesn't properly support the left/right marking this option requires.

Ayush Chandekar has been leading the discussion, with other contributors showing interest in exploring implementation approaches. While the thread has established the basic optimization opportunity (avoiding unnecessary bitmap traversal when `--left-right` is present), the technical details of how to properly implement this are still being worked out. The conversation remains active, with participants preparing to dive deeper into the bitmap internals and `rev-list.c` implementation specifics.

Key open questions center around how to modify the bitmap traversal code to either properly support left/right marking or to more intelligently determine when bitmap traversal can be safely skipped. Future messages in the thread will likely present specific code changes or benchmark results comparing different approaches.