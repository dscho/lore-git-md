Thread identity: Discussion about `git add -A` behavior with submodules configured with `ignore = all`, initiated by Oliver Kopp.

Core problem: Inconsistency between `git status` (which respects `ignore = all` for submodules) and `git add -A` (which currently ignores this setting and stages all submodule changes regardless). The documentation specifies `ignore = all` only affects status/diff operations, but users expect more consistent behavior across commands.

Current status: No resolution yet. Claus Schneider referenced GitHub PR #1987 as potentially addressing this, but details of the proposed solution are not yet discussed in the thread. The behavior has been a known pain point since at least 2021 (per referenced StackOverflow question).

Key technical details: 
- Submodule configuration `ignore = all` currently only affects `git status` and diff operations
- `git add -A` explicitly states it operates on "all files in the entire working tree" without considering submodule ignore settings
- Potential solution would require modifying `git add` to respect submodule ignore configurations

Open questions:
- Should this be considered a bug (inconsistent behavior) or documented limitation?
- Would changing `git add -A` behavior break existing workflows that rely on current implementation?
- What would be the exact scope of changes needed in `git add` to respect submodule ignore settings?

Participant positions:
- Oliver Kopp: Reports the issue and argues for more consistent behavior
- Claus Schneider: Points to existing PR #1987 as potential solution

Related work:
- GitHub PR #1987 (details not yet discussed)
- 2021 StackOverflow question indicating this has been a long-standing issue