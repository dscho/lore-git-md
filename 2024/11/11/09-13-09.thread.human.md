Here's a human-readable summary of the thread so far:

The thread examines an inconsistency in how Git handles submodules marked with `ignore = all`. Oliver Kopp reports that while `git status` properly ignores these submodules (as documented), `git add -A` still stages changes in them - contrary to what users might expect. This behavior has been a known pain point since at least 2021, as referenced in a StackOverflow discussion.

Claus Schneider mentions a GitHub pull request (#1987) that may address the issue, though details aren't provided in the email. The core tension lies between Git's current documented behavior (where `git add -A` explicitly operates on all files) and user expectations for consistent handling of ignored submodules across commands. The discussion raises questions about whether this is a bug needing fixing or simply an undocumented limitation, with potential implications for existing workflows if the behavior were changed.