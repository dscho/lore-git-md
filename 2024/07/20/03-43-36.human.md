This is a small code style cleanup patch in the `git cat-file --batch-command` series, which adds remote object info support. The patch moves variable declarations inside their respective for loops where they are only used locally, improving code readability.

The changes are mechanical and straightforward:
- Moves `int i` declarations inside for loops in three functions (`dispatch_calls`, `free_cmds`, and `batch_objects_command`)
- Changes the type of loop counters from `int` to `size_t` where appropriate
- Removes unused variable declarations

This patch comes from Eric Ju with help from Christian Couder, and appears to be a final polish before the larger feature series is merged. The changes don't affect functionality but follow Git's coding style guidelines more closely by limiting variable visibility to their minimal required scope.

As part of the broader remote object info series, this cleanup helps maintain code quality in the new feature area while the main functionality is being finalized. The patch is uncontroversial and likely to be merged as part of the complete series.