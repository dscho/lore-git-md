This is a follow-up discussion in the remote object-info series, specifically addressing review feedback on the final patch that adds `remote-object-info` support to `git cat-file --batch-command`. Peijian Ju responds to Toon Claes' concerns about documentation accuracy and test organization.

The exchange clarifies that a comment in `builtin/cat-file.c` about future objecttype support is intentionally placed and relates to an open question from the cover letter about handling format strings. Peijian confirms the comment's relevance to the current implementation's limitation of only supporting objectsize, noting this breaks the default format when objecttype is requested.

For the test organization issue, Peijian acknowledges Toon's point about HTTP test placement and commits to moving file:// protocol tests before the HTTP section in v2, along with adding the standard warning comment about HTTP-specific test placement. These changes will align with Git's established test patterns.

The discussion remains focused on polish items rather than core functionality, with Peijian demonstrating responsiveness to review feedback. The exchange highlights ongoing attention to detail in this long-running series, particularly around edge cases and maintainability concerns. The v2 patch will incorporate these improvements while leaving the broader question of objecttype handling (raised in the cover letter) still open for discussion.