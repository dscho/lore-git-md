This is a response in the ongoing remote object-info series, addressing code style and implementation feedback from Toon Claes. The patch adds client-side support for querying object metadata from remote servers.

Peijian Ju (Eric) responds to two specific suggestions from Toon's review of the transport layer implementation:

1. Switching from pointer arithmetic to array subscript notation for OID access, which Eric confirms is now fixed in v2.

2. Refactoring the ref list construction to eliminate an unnecessary initial allocation. Eric agrees the original approach was obscure and has adopted Toon's suggested implementation, which simplifies the linked list construction by:
   - Initializing the list head only when needed
   - Setting OIDs directly on each ref
   - Cleaning up the pointer chaining logic

Eric also investigates Toon's observation about `fetch_object_info()` always returning non-zero, sharing that in his testing with `t1017-cat-file-remote-object-info.sh`, the function does return zero as expected. He requests Toon's test steps to further investigate any discrepancy.

The exchange shows productive collaboration on implementation details while the core architecture remains stable. The changes address code quality concerns without altering the feature's behavior. This level of refinement is typical for a patch series in its later review cycles.