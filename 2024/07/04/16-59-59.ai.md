This is a review response in the refactoring effort for remote object-info functionality. Karthik Nayak questions whether generalizing the packet-writing function is the right approach, suggesting that a custom function in `connect.c` might be more appropriate for the new "object-info" command.

The email acknowledges that the current patch (which makes the command name parameterizable) looks technically sound, but raises an architectural concern about whether this is the right layer for the abstraction. Karthik points to existing examples like `get_remote_bundle_uri()` and `get_remote_refs` as alternative patterns that handle protocol commands in `connect.c` rather than modifying fetch-specific code.

This feedback comes after reviewing the full series context, where the generalized function will be used to support the new "object-info" command in later patches. The concern suggests the implementation might be mixing concerns rather than following established patterns for new protocol commands. The response maintains the security-focused approach seen throughout the series while questioning the architectural direction at a key integration point.