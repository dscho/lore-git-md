Here's the updated human-readable summary:

The thread examines long-standing issues with `git subtree split` when working with repositories that have complex merge histories, particularly affecting projects like Tor's arti.git. The original report identified two distinct problems: a Debian-specific shell recursion limit being hit in recent Git versions, and an older issue where merge-heavy histories produce incorrectly large splits (thousands of commits instead of dozens).

Colin Stagner has proposed a patch to address the recursion issue, while Ian Jackson has put forward a more ambitious algorithm to fundamentally improve merge handling. The algorithm would make splits deterministic by carefully processing merge commits in four distinct cases, though its complexity raises questions about implementation feasibility.

In the latest response, Colin provides important context about the current implementation's behavior. While confirming splits are technically deterministic now, he notes they're inefficient due to reconstructing history repeatedly. He highlights a tension between maintaining backward compatibility (where identical splits should produce identical commit IDs) and the need to fix historical issues that have changed behavior over time. His suggestion of a config file approach could allow improvements without breaking existing workflows.

Colin also shares valuable insights from real-world usage, explaining that many users (himself included) primarily use `subtree split` for creating read-only views rather than as part of active development workflows. His technical analysis cautions that merge handling remains particularly tricky, with edge cases that might benefit from visual modeling, and that merge trailers shouldn't be relied upon due to rebasing.

The discussion remains open on several fronts: whether to proceed with Ian's algorithm given its complexity, how to balance improvements with backward compatibility, and whether the maintainers will accept Colin's more targeted recursion fix. The thread has evolved from initial problem reporting to deeper design discussion about how subtree operations should handle merges in principle, with participants bringing both technical analysis and practical workflow experience to bear.