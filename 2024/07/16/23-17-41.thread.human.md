This thread addresses two related build system maintenance items. The discussion began with Đoàn Trần Công Danh's patch adding `-D__STDC_NO_VLA__` to suppress sparse warnings from glibc 2.39's regex.h header, which uses variable-length arrays in its POSIX-compliant `regexec()` declaration. 

Johannes Schindelin (Dscho) followed up with appreciation for the fix and contributed a complementary patch updating Git's sparse CI job. His change migrates from Ubuntu 20.04 to 22.04 runners (as the older ones are being phased out) and simplifies the setup by using the system sparse package rather than custom installation. Both changes are straightforward maintenance updates - one addressing a compiler warning and the other keeping CI infrastructure current - with Johannes' work building directly on Đoàn's fix to ensure smooth operation on newer platforms.