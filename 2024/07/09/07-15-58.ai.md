This is a review response in the remote object-info series, focusing on code style and implementation details in the transport layer. Toon Claes provides two specific suggestions for improving the patch:

1. Recommends using array subscript notation (`oid[i]`) rather than pointer arithmetic (`*(oid + i)`) for accessing OIDs in the object-info list, which is more idiomatic in Git's codebase.

2. Proposes a refactoring of the ref list construction to eliminate an unnecessary initial allocation by making the list construction more straightforward. The suggested change would:
   - Initialize the list head only when needed
   - Set OIDs directly on each ref rather than through a temporary
   - Clean up the pointer chaining logic

Toon also notes a potential issue with `fetch_object_info()` always returning non-zero, questioning whether this indicates missing test coverage or an actual bug. This observation suggests the error handling path may not be properly exercised in the current implementation.

The feedback is narrowly focused on implementation quality within the established architectural approach, rather than challenging the overall design. It follows naturally from Justin Tobler's earlier review that identified optimization opportunities in the batching behavior.