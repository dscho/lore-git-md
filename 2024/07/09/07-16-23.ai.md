This is a code review follow-up in the remote object-info series, focusing on test organization and documentation accuracy. Toon Claes points out two specific issues in the final patch that implements `git cat-file --batch-command` integration for remote object queries.

First, he notes an outdated or misplaced comment in `builtin/cat-file.c` about format string handling that no longer matches the current implementation. The comment refers to supporting objecttype in the future, but this functionality remains unimplemented.

Second, he identifies a test organization issue in the new test script `t1017-cat-file-remote-object-info.sh`. The HTTP tests are placed before `start_httpd`, which means they would be skipped if HTTP setup fails. He suggests moving file:// protocol tests before the HTTP section and adding the standard warning comment about HTTP-specific test placement.

The review maintains the series' focus on robustness, with Toon catching subtle issues in test organization that could lead to incomplete test coverage. His suggestions align with the project's established patterns for HTTP test organization and documentation accuracy. These are relatively minor polish items that don't affect the core functionality but would improve maintainability.