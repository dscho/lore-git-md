This is a code review response in the ongoing remote object-info implementation series. Toon Claes provides two suggested improvements to Eric Ju's patch that adds client-side transport support for querying object metadata from remote servers.

The first suggestion is a style nit - using array subscript notation (`oid[i]`) rather than pointer arithmetic (`*(oid + i)`) for accessing elements in the object ID array. The second is a more substantive refactoring proposal that simplifies the linked list handling by eliminating an extra allocation step in the object info reference chain.

Toon also notes a potential issue with `fetch_object_info()` always returning non-zero, questioning whether this indicates missing test coverage or an actual bug. The review maintains the series' focus on security and correctness while offering concrete implementation improvements.

The exchange continues the thread's pattern of thorough technical review, with Toon (who has been involved in previous rounds) providing focused style feedback and raising questions about edge case behavior. The suggestions appear aimed at making the code more idiomatic while preserving its security properties.