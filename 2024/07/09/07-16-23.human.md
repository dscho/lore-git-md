This is a review comment on the final patch in a series implementing remote object info queries via `git cat-file --batch-command`. Toon Claes points out two specific issues in the implementation:

1. An outdated/misplaced comment in `builtin/cat-file.c` about format strings that no longer matches the current behavior (the comment suggests a format that isn't actually used).

2. A test organization issue where HTTP-specific tests should be grouped together after the `start_httpd` call, following the pattern seen in other test files, with a clear demarcation comment.

The review is focused and technical, coming from someone clearly familiar with both the test infrastructure and the codebase's conventions. The feedback doesn't challenge the core functionality (which appears settled after extensive prior review) but rather polishes the implementation details. This suggests the series is nearing completion, with only minor cleanup remaining before it can be merged.