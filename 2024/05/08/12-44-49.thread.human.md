Here's a human-readable summary of the thread so far:

The thread explores how to show download progress when fetching bundles over HTTPS while maintaining clean error handling. Toon Claes originally proposed letting git-remote-https write progress directly to stderr, but Jeff King (Peff) identified this would expose fatal errors from missing bundles that were previously suppressed.

The discussion has narrowed to three approaches: filtering stderr (seen as messy), overriding die() in child processes (considered impractical), or Jeff's preferred solution of having the child emit machine-readable progress over stdout that the parent formats. Toon is now exploring what that machine-readable protocol might look like, suggesting either structured size/total/throughput data or "log"-prefixed messages the parent could format.

Key open questions center on implementation details of the stdout-based approach, particularly how to structure the progress data exchange between parent and child processes. The thread remains technical but productive, with participants working toward a solution that balances user feedback with robust error handling.