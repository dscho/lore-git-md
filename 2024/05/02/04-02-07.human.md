This is a technical discussion in the `git url-parse` series thread, where Torsten Bögershausen clarifies the behavior of SCP-style URL parsing in response to Ghanshyam Thakkar's edge case discovery.

Torsten confirms that classifying `file:/test/test` as SSH protocol is actually correct behavior, as Git intentionally supports SCP-style shorthand syntax (host:path) alongside full URLs. He references the `scp` man page to show this is a legitimate format where the colon serves multiple purposes. This isn't a parser bug but rather a deliberate compatibility feature for Git's various URL formats.

The exchange highlights how Git's URL handling accommodates legacy formats that standard parsers might reject. Torsten's response suggests the current behavior should be preserved rather than made more rigid, though the thread leaves open whether this should be better documented or given special handling in the new `url-parse` command. This is exactly the sort of nuanced protocol behavior the series needs to get right before finalizing.