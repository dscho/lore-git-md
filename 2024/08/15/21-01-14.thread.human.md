This thread continues the deep technical discussion about MIDX bitmap optimizations, primarily between Taylor Blau (the bitmap/MIDX expert) and Patrick Steinhardt (leading the ODB abstraction work). The conversation has evolved from high-level design to focused implementation details while maintaining rigorous attention to both correctness and documentation.

The core of the thread established three key object ordering rules for MIDX bitmaps: inter-layer (earlier layers first with deduplication), intra-layer (chain-wide preferences), and intra-pack (offset order). Blau recently added documentation clarifying these architectural decisions, including deduplication guarantees and the relationship between pseudo-pack order and bitmap positions.

The latest exchange zeroes in on the `bitmap_non_extended_bits()` function and pack-revindex changes. Blau explains why `bitmap_num_objects()` remains in use for comparing commit parent bitmaps within a single layer (not across layers) and agrees this subtlety deserves an inline comment. He addresses several implementation details: fixing spacing in `midx_pack_order_cmp()`, confirming position calculation adjustments for base layer objects, and documenting the case-by-case approach to conversions rather than blanket documentation.

While most technical questions have been resolved through this thorough back-and-forth, a few open points remain regarding the practical effects of ordering rules on pack-reuse optimizations, long-term maintenance of recursive lookup patterns, and test coverage adequacy. The discussion exemplifies the project's careful approach to complex subsystems - combining deep technical review with clear documentation of subtle design choices.