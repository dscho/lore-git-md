This thread continues the technical discussion around Taylor Blau's incremental MIDX bitmap implementation, with recent focus on refining implementation details while maintaining the core architectural principles. The conversation has evolved from error handling and warning messages to now address the indexing scheme for base layers in the bitmap subsystem.

Patrick Steinhardt raised a concern about the `base_nr` field in the bitmap_index struct using 1-based indexing, which is unconventional in Git's codebase. Blau acknowledged this was done for iterator initialization convenience but agreed it could be confusing. After discussing alternatives, they converged on switching to 0-based indexing - the more conventional approach in Git - with callers adjusting as needed. This change will make the code more consistent with the rest of the codebase while maintaining the functionality.

The thread shows the careful attention being paid to both the high-level architecture (like the MIDX design ensuring object deduplication before ordering) and low-level implementation details (like indexing conventions). The discussion continues to balance immediate code quality concerns with the long-term vision for incremental MIDX bitmaps, with participants working through technical choices that affect both functionality and maintainability. The resolution of this indexing question represents another step toward robust layer tracking in the bitmap subsystem.