Taylor Blau's MIDX bitmap series continues to progress, now at version 5 (14 patches) with the core functionality for efficient bitmap operations across multi-pack index layers. The latest patch (08/14) extends `git rev-list --test-bitmap` to properly verify the recursive bitmap lookup behavior across MIDX layers, a critical validation step for the series.

The test infrastructure now mirrors the layered MIDX bitmap design, with recursive test data preparation and cleanup that tracks which bitmap layer contains each commit. New helper functions like `find_bitmap_for_commit()` and enhanced test output showing MIDX/pack origins provide thorough verification of the cross-layer behavior. This builds on the previous pack-reuse optimizations that enabled efficient object access across MIDX generations.

The series remains focused on performance-critical areas like pack-bitmap.c, maintaining backward compatibility while introducing the layered bitmap support. With comprehensive test coverage in t5334 and careful resource management, the implementation appears robust as it approaches final review. The remaining patches in the series will complete the writing infrastructure for incremental bitmaps.