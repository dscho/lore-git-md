The thread introduces a new `git-diff-pairs` plumbing command for efficient batch processing of blob diffs, particularly useful for server-side operations handling large change sets. The initial patch refactors the diff queue internals to enable post-queue modification of `diff_filepair` structs, splitting existing `diff_addremove()` and `diff_change()` functions into two layers. This preparatory change adds new public API functions that return pointers to queued pairs while maintaining all existing behavior through wrapper functions. The mechanical refactoring touches only diff.c and diff.h, with comprehensive updates to both files. The series appears well-structured, with this first patch cleanly setting up the infrastructure needed for the subsequent implementation of the actual diff-pairs command. No concerns or objections have been raised about the approach so far.