Here's the updated AI mode thread summary:

Carlos Maiolino reported a bug in `git tag -s` signing behavior with smartcard-stored GPG keys where Git silently falls back to a different key when the configured `user.signingKey` is unavailable, contradicting documented behavior. Jeff King (Peff) investigated and found CLI-specified keys work correctly while config-specified keys exhibit this fallback behavior when inaccessible. The core issue is Git's silent fallback when the primary key cannot be accessed (e.g., smartcard disconnected), undermining reliability for hardware-backed signing.

Peff's technical follow-up clarifies Git's current behavior: it passes the configured key string to GPG without key availability awareness, relying entirely on GPG's response. The discussion now focuses on diagnosing whether the issue stems from Git incorrectly passing the key (visible in GIT_TRACE) versus GPG accepting but substituting the key (a GPG-level behavior). Key diagnostic scenarios identified are comparing config-specified vs CLI-specified key behavior and examining GIT_TRACE outputs to pinpoint where key substitution occurs.

The security implication remains central - both parties agree Git should at least warn when unable to use the specifically requested signing key, particularly for hardware-backed keys where silent fallback undermines security expectations. The thread is progressing toward identifying whether the fix needs to be in Git's GPG invocation plumbing or GPG itself needs modification to properly report key substitution scenarios. Next steps involve concrete GIT_TRACE analysis to isolate the failure point in the key-handling pipeline.