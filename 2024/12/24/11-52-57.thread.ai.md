Updated Git thread summary (AI mode):

This thread introduces and finalizes `--expire-to=<dir>` support for `git gc`, completing the mirroring of `git repack --expire-to` functionality (from commit 91badeb) for cruft pack management. The feature enables specifying a directory for expired cruft packs during garbage collection, particularly valuable for server-side maintenance workflows.

Technical implementation details:
- New `repack_expire_to` field in `gc_config` structure
- Modified `add_repack_all_option()` to handle the new parameter while maintaining backward compatibility
- Special handling of `-a` flag skipping when both `--cruft` and `--expire-to` are specified
- Documentation updates in `git-gc.txt` explicitly stating the cruft pack requirement
- Comprehensive test coverage in t6500-gc.sh verifying various option combinations including `--no-cruft` scenarios

Review and evolution through v4:
- Jeff King (Peff) validated core logic and recommended series consolidation
- Junio Hamano's documentation suggestions fully implemented:
  - Clear upfront statement that `--expire-to` requires `--cruft`
  - Improved phrasing ("for more information" instead of "for more")
  - Additional test coverage for `--no-cruft` case
- Author added `git show --format=reference` for commit references
- More prominent explanation of `-a` flag skipping in both documentation and commit message

Current status and next steps:
- Junio Hamano has called for final review before merging to 'next'
- All substantive technical and documentation feedback addressed
- Expanded test coverage verifies both positive and negative cases
- No outstanding design objections remain from reviewers
- Change aligns with Taylor Blau's ongoing cruft pack work
- Implementation appears solid and ready for inclusion

The patch has completed four review iterations with all feedback incorporated, providing independent utility for repository maintenance while maintaining backward compatibility. The functionality mirrors `git repack`'s expire-to capability in the gc command, completing this aspect of cruft pack management tooling. With Junio's call for review, the patch is in final consideration for merging.