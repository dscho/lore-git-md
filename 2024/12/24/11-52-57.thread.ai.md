Here is the dense, loss-free AI mode summary capturing all key details:

This thread introduces a 2-patch series adding `--expire-to` option to `git gc`, allowing specification of where expired cruft packs should be stored. The first patch (current email) implements core functionality by plumbing the existing `git repack --expire-to` parameter (added in commit 91badeb) through to `git gc`. Key technical details: adds `repack_expire_to` field to `gc_config` struct, updates `add_repack_all_option()` to pass through the parameter, documents new option in git-gc.txt, and adds test case in t6500-gc.sh. The change provides quality-of-life improvement by eliminating need to manually chain `git repack --cruft --expire-to` with `git prune`. Implementation is minimal and focused, maintaining existing behavior while adding the passthrough capability. No open questions or objections raised yet in thread.