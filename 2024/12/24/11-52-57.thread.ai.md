Git thread summary (AI mode):

This thread introduces a single-patch addition of `--expire-to=<dir>` support to `git gc`, mirroring existing `git repack --expire-to` functionality (from commit 91badeb) for cruft pack management. The change enables specifying a directory for expired cruft packs during garbage collection, streamlining server-side maintenance workflows.

Technical implementation details:
- New `repack_expire_to` field in `gc_config` structure
- Modified `add_repack_all_option()` to handle the new parameter
- Documentation updates in `git-gc.txt` explaining the new option
- Test coverage in t6500-gc.sh verifying correct behavior
- Special case handling when both `--cruft` and `--expire-to` are specified (automatically skips `-a`)

The implementation builds on prior review feedback from Jeff King (Peff), who validated the logic but recommended consolidating the original two-patch series to avoid introducing intermediate buggy states. The current version properly safeguards against invalid option combinations while maintaining backward compatibility. The design aligns with Taylor Blau's ongoing cruft pack work, though this change provides independent utility for repository maintenance.

Key resolved technical points:
- Correct interaction between `--prune=now` and `--expire-to` options
- Validated behavior when `cruft_packs && repack_expire_to` are both set
- Confirmed preservation of existing behavior for non-cruft cases

The patch follows established patterns from the repack implementation and addresses all prior review feedback, with no outstanding technical objections. The change is now in its final form, having incorporated all suggested improvements from the discussion.