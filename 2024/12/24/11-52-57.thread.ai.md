Git thread summary (AI mode):

This thread introduces a 2-part series adding cruft pack management to `git gc`. The first patch (currently the only one sent) adds a `--expire-to` option to `git gc`, mirroring existing `git repack --expire-to` functionality (from commit 91badeb). The change allows specifying a directory for expired cruft packs during garbage collection, streamlining what currently requires separate `git repack` and `git prune` invocations.

Technical implementation includes:
- New `repack_expire_to` field in `gc_config`
- Plumbing to forward the option to `git repack`
- Documentation updates in `git-gc.txt`
- Test coverage in t6500-gc.sh

The patch has been awaiting review since December 2024. The second patch in the series (not yet sent) is expected to complete the functionality. The change is backward-compatible and follows established patterns from the repack implementation. Key technical details involve the gc/repack interface and cruft pack expiration mechanics. No reviews or objections have been raised yet in the thread.