Updated Git thread summary (AI mode):

This thread introduces `--expire-to=<dir>` support for `git gc`, mirroring existing `git repack --expire-to` functionality (from commit 91badeb) for cruft pack management. The change enables specifying a directory for expired cruft packs during garbage collection, particularly useful for server-side maintenance workflows.

Technical implementation details:
- New `repack_expire_to` field in `gc_config` structure
- Modified `add_repack_all_option()` to handle the new parameter while maintaining backward compatibility
- Special handling of `-a` flag skipping when both `--cruft` and `--expire-to` are specified
- Documentation updates in `git-gc.txt` explicitly stating the cruft pack requirement
- Comprehensive test coverage in t6500-gc.sh verifying various option combinations including `--no-cruft` scenarios

Key review feedback incorporated through v4:
- Jeff King (Peff) validated the logic and recommended series consolidation
- Junio Hamano's documentation suggestions fully implemented:
  - Clear statement that `--expire-to` requires `--cruft`
  - Improved phrasing ("for more information" instead of "for more")
  - Additional test coverage for `--no-cruft` case
- Author added `git show --format=reference` for commit references
- More prominent explanation of `-a` flag skipping in both documentation and commit message
- Documentation now clearly states the cruft` pack requirement upfront

Current status and open points:
- Core technical implementation remains sound with proper option combination handling
- Documentation clarifications regarding cruft pack requirement and `-a` behavior now addressed
- Expanded test coverage verifies both positive and negative cases
- No fundamental design objections remain from reviewers
- Change aligns with and complements Taylor Blau's ongoing cruft pack work

The patch has evolved through four review iterations with all substantive feedback addressed. The functionality provides independent utility for repository maintenance while maintaining backward compatibility, completing the mirroring of `git repack`'s expire-to capability in the gc command. The implementation appears solid and ready for inclusion.