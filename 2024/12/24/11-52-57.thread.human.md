This thread has progressed through four iterations of a patch adding `--expire-to=<dir>` support to `git gc`, mirroring existing functionality in `git repack` for managing expired cruft packs. The feature is particularly useful for server-side maintenance workflows where preserving expired packs is needed.

The core technical implementation introduces a new `repack_expire_to` field in the `gc_config` structure and modifies `add_repack_all_option()` to handle the new parameter while maintaining backward compatibility. A notable behavior change automatically skips the `-a` flag when both `--cruft` and `--expire-to` are specified.

Through the review process, Jeff King (Peff) validated the logic and recommended series improvements, while Junio Hamano provided documentation suggestions that were incorporated. The v4 patch includes more explicit explanations of the `-a` flag behavior and additional test cases for `--no-cruft` scenarios. Documentation now clearly states that `--expire-to` requires `--cruft` to be specified.

With all substantive feedback addressed and comprehensive test coverage in place, this well-tested change appears ready for inclusion. It cleanly integrates with Taylor Blau's ongoing cruft pack work while maintaining backward compatibility with existing workflows.