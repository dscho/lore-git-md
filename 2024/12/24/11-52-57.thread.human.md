This thread introduces a new `--expire-to` option for `git gc` to manage cruft packs, mirroring existing functionality in `git repack`. The discussion began with a two-patch series but has now been consolidated into a single patch based on feedback from Jeff King (Peff), who reviewed the implementation and suggested avoiding an intermediate state that could be buggy. ZheNing Hu agreed with this approach.

The change allows specifying a directory where expired cruft packs are stored during garbage collection, simplifying repository maintenance workflows. The implementation adds configuration plumbing, updates documentation, and includes test coverage while maintaining backward compatibility. Technical review confirmed the logic correctly handles edge cases, particularly around the interaction between `--cruft` and `--expire-to` options. The patch aligns with Taylor Blau's ongoing work on cruft pack management and appears ready for merging with no outstanding objections.