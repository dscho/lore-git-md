This is a security-focused feature patch in its final polishing stage, adding remote object-info support to `git cat-file --batch-command`. The discussion has shifted from technical implementation to ensuring forward compatibility of the output format.

Patrick Steinhardt raises a key concern about the current patch's choice of output format ("%(objectname) %(objectsize)") when used with remote-object-info. He argues that introducing this format now would create an inconsistency with the local case, and that changing it later to include object types would break backwards compatibility. This is a design-level issue that needs resolution before the series can be merged.

The thread shows this is the last substantive discussion point for the series, which has otherwise cleared all technical hurdles. Eric Ju had planned to defer type support to a follow-up, but Patrick's comment suggests this approach may need reconsideration to avoid painting the project into a corner format-wise. The exchange represents the final design polish for a feature that enables secure, efficient remote object metadata queries.