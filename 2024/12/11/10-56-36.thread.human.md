This thread has been working through the careful removal of long-deprecated Git features (`.git/branches/`, `.git/remotes/` directories and `pack-redundant` command) in preparation for Git 3.0, establishing infrastructure for handling breaking changes through a `WITH_BREAKING_CHANGES` compile-time flag. The series has seen active participation from key contributors - Patrick Steinhardt driving the technical implementation, Christian Couder focusing on CI and documentation details, and Junio Hamano providing historical context and user experience considerations.

The implementation has evolved through several iterations, with consensus reached around:
1) Adding explicit warning messages when deprecated features are accessed
2) Requiring `--i-still-use-this` flags for continued use
3) Comprehensive test coverage across all build systems (Makefile, CMake, Meson)

In the latest exchange, Junio has refined the warning strategy further, suggesting the warnings should only trigger when the legacy configurations are actually *used* (not just when files are read incidentally). This continues the thread's careful balance between removing technical debt and maintaining compatibility, particularly for existing automation scripts that might still rely on these features.

The series appears technically complete with robust testing and documentation. Junio's latest input represents a final polish to the warning system before potential merging. All major concerns have been addressed - build system support, test infrastructure, clear migration paths, and now refined warning behavior. The thread shows how Git approaches breaking changes: methodically, with multiple rounds of review, and special attention to real-world impact.