[PATCH] ci: fix shell syntax in test failure reporting

The thread addresses a regression in GitHub Actions CI test failure reporting caused by incorrect shell parameter expansion syntax in ci/print-test-failures.sh. The bug was introduced in commit eab5dbab ("ci: wire up Meson builds") where `${TEST_OUTPUT_DIRECTORY:t}` was used instead of the proper `${TEST_OUTPUT_DIRECTORY:-t}` default-value syntax, resulting in "Bad substitution" errors during test runs.

Junio C Hamano (the maintainer) submitted the fix, which simply corrects the parameter expansion to use `:-` for default value fallback. The change is minimal (one line) and narrowly scoped to GitHub Actions CI artifact collection. The patch version is not specified, suggesting this is the initial submission. No other participants have commented yet, but given the clear regression and maintainer authorship, this is likely to be merged promptly. The fix restores proper test failure artifact collection without affecting other CI providers or test execution paths.