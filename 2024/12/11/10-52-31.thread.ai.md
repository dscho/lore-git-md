[PATCH] ci: fix shell syntax in test failure reporting

The thread addresses a regression in GitHub Actions CI test failure reporting caused by incorrect shell parameter expansion syntax in ci/print-test-failures.sh, introduced in commit eab5dbab ("ci: wire up Meson builds"). The bug used `${TEST_OUTPUT_DIRECTORY:t}` instead of the proper `${TEST_OUTPUT_DIRECTORY:-t}` default-value syntax, resulting in "Bad substitution" errors during test runs.

Junio C Hamano (the maintainer) submitted the initial fix, correcting the parameter expansion to use `:-` for default value fallback. The change is minimal one-line fix narrowly scoped to GitHub Actions CI artifact collection. Patrick Steinhardt reviewed the patch, confirming it looks "obviously good" with no additional changes needed. The fix restores proper test failure artifact collection without affecting other CI providers or test execution paths.

With the maintainer's authorship and positive review from an active contributor, this fix is likely to be merged promptly. The thread appears complete with no outstanding questions or objections. The regression is clearly identified and the solution straightforward, making this a low-risk change targeting GitHub Actions CI test reporting specifically.