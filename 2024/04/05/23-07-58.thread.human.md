Here's a human-readable summary of the thread so far:

Aleks Todorov has proposed adding commit summary formatting to `git blame`, similar to what `git log` offers with its `--format` option. The initial prototype reuses `git log`'s formatting infrastructure, demonstrating basic functionality but raising design questions about whether this shared approach is appropriate given `blame`'s different output alignment needs versus `log`.

Junio Hamano, the Git maintainer, has now weighed in with several considerations. He suggests that alignment issues might be addressable using existing format specifiers (%</%>), though he's unsure how cleanly this would work. He also raises the possibility that GUI tools might be better served by `--incremental` output rather than custom formatting. Most importantly, Junio questions the underlying motivation for this feature - understanding the use cases better might help determine whether to reuse `log` formatting or implement a `blame`-specific solution.

The discussion remains focused on architectural tradeoffs before proceeding with implementation. Key open questions include: whether to handle alignment differences, whether GUI tools would actually use this feature, and how tightly the formatting should couple to `git log`'s existing infrastructure. The thread shows active design discussion but no clear resolution yet on the technical approach.