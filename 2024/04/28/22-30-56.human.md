This is patch 8/13 in the series introducing a new `git url-parse` builtin command. The patch defines the command-line interface for the new utility, adding the usage string and option parsing infrastructure.

The patch adds:
- A usage string showing the basic command format
- A `--component` (`-c`) option to specify which URL part to extract (like protocol, host, path)
- The option parsing boilerplate using Git's standard `parse-options.h` infrastructure

This is a straightforward plumbing patch that follows the established pattern for Git builtins. It builds on the component extraction infrastructure defined in earlier patches (particularly patch 7's enum mapping) to set up the command-line interface. The implementation looks clean and idiomatic.

The series continues its methodical progression - having established the core parsing and component extraction capabilities, this patch starts wiring them up to the command-line interface. The next logical steps would be implementing the actual command logic that uses these options to parse URLs and output the requested components.