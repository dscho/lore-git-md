This is a documentation patch completing the `git url-parse` series, which provides plumbing to expose Git's internal URL parsing logic. The patch adds a man page for the new builtin command, explaining its purpose and usage.

The documentation clearly describes how to use `git url-parse` to validate URLs and extract specific components (protocol, user, password, host, port, path). It includes practical examples showing both standard HTTPS URLs and Git's SCP-style format. The examples demonstrate component extraction and silent validation mode.

The man page follows Git's standard documentation format and appears thorough, covering all the functionality implemented in the previous patches. This completes the user-facing side of the feature, complementing the implementation work done in earlier patches of the series.

The documentation quality looks solid - it's clear, covers the key use cases, and matches the implementation details from the rest of the series. This patch appears ready for review alongside the rest of the series.