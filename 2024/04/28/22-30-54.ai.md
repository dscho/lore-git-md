This is patch 6/13 in the `git url-parse` series, introducing a component extraction helper function. The patch builds on the enum and struct definitions from previous patches to implement the core functionality of extracting specific URL components (protocol, user, password, host, port, path) from parsed URLs.

The new `extract()` function takes a URL component enum and a parsed `url_info` struct, then returns a newly allocated string containing just that component (or NULL for URL_NONE). The implementation cleanly handles all component types through a switch statement, using the offset/length fields from the url_info struct to slice the original URL string. Memory management follows Git conventions with `xstrndup()` for allocation.

This is a straightforward plumbing piece that enables the actual command-line interface (coming in later patches) to select and display individual URL parts. The implementation looks correct and matches the series' incremental approach - each patch adds one well-defined piece while maintaining the overall architecture.

The patch continues the series' careful progression toward exposing Git's URL parsing logic as reusable plumbing. Next steps would presumably involve wiring this helper into the command-line frontend and adding tests for the extraction behavior.