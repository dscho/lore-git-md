This is patch 6/13 in the series introducing a new `git url-parse` builtin command. The patch adds a key helper function that extracts specific components (protocol, user, host, etc.) from parsed URLs.

The `extract()` function takes a URL component enum (defined in patch 5) and a parsed URL structure, then returns a newly allocated string containing just that component. The implementation cleanly handles all component types by using the offset/length fields in the `url_info` struct, with clear switch cases for each possible component. Memory management follows Git conventions - the caller must free the returned string.

This is a straightforward but important building block for the command's functionality. The implementation looks solid, with proper bounds handling via `xstrndup()`. As the series progresses, this helper will be used to implement the actual command-line interface for extracting URL components.

The patch continues the series' careful, incremental approach - each patch adds one well-defined piece while maintaining overall coherence. Reviewers will likely want to check that all URL cases are properly handled (particularly edge cases like missing components), but the foundation appears sound.