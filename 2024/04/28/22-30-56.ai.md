This is patch 8/13 in the `git url-parse` series, adding command-line interface definitions for the new builtin. The patch introduces the usage string and option parsing infrastructure needed to make the command functional.

The key additions are:
- A usage string following Git's standard format
- A `--component` (`-c`) option that will specify which URL part to extract (protocol, host, path etc.)
- The option parsing boilerplate using Git's `parse-options.h` infrastructure

This continues the methodical progression of the series, building on the component extraction infrastructure from earlier patches. The implementation looks straightforward and follows Git's standard patterns for builtin commands. The next logical steps would be implementing the actual command logic that uses these options to parse URLs and output the requested components.

The patch maintains the series' clean separation of concerns - it focuses solely on the CLI interface without mixing in parsing logic. This keeps each patch focused and reviewable. The series appears to be progressing well toward its goal of exposing Git's URL parsing as a plumbing command.