Here is the updated AI thread summary with the new email incorporated:

---

This security-hardened series implements full `git cat-file --batch-command` integration for querying object metadata from remotes without full downloads, now **at v20** and **queued for `next`** (Junio's "What's cooking" report and Pablo Sabater's confirmation). The series has progressed through **20 versions**, with all technical, security, and design issues resolved, including the final resolution of the `%(objectname)`-only request handling, the addition of a new commit to ensure bare OID requests trigger an unconditional existence check on the server, a memory leak fix in `transport-helper.c`, dynamic adaptation to server-advertised capabilities, and minor refactoring for variable scope, type consistency, and test infrastructure reuse. A **build fix** for a signed/unsigned comparison warning in `write_fetch_command_and_capabilities()` (patch 5/13) was incorporated in v20: the `hash_algo` variable in the function is now declared as `const unsigned int` (matching `hash_algo_by_name()`'s return type) to avoid breaking the build when the function is moved from `fetch-pack.c` (which suppresses sign-compare warnings) to `connect.c` (which does not). The fix is mechanical, uncontroversial, and must be incorporated before the series graduates to `master`.

The implementation enables Git clients to query object metadata (currently size, with type support designed for future expansion) from remote repositories via the `object-info` protocol v2 capability. Key technical details:
- **Protocol**: Full (non-abbreviated) object IDs, numeric `obj-size` values, and strict handling of unsupported object IDs (returning `<OID> SP`). The server advertises supported attributes (e.g., `size`, `type`) when `transfer.advertiseobjectinfo` is enabled, allowing clients to dynamically tailor queries to avoid unsupported attributes. A new commit (patch 9/13, Pablo Sabater) ensures bare OID requests trigger an unconditional existence check on the server, avoiding lazy-fetching in partial clones while maintaining robustness. The check is enforced via a new `get_object_info()` wrapper around `odb_read_object_info_extended()` that sets `OBJECT_INFO_SKIP_FETCH_OBJECT` and `OBJECT_INFO_QUICK` flags to prevent partial-clone servers from fetching missing objects or rescanning packs unnecessarily. Unrecognized OIDs now return `<OID> SP` regardless of requested attributes, while recognized OIDs return just `<OID>` for bare requests. Documentation in `gitprotocol-v2.adoc` is updated to reflect these behaviors, including explicit requirements for full OIDs and optional `obj-size`/`obj-type` fields, and clarifies the response format for unrecognized OIDs.
- **Client-side command**: The series introduces the `remote-object-info` command in `git cat-file --batch-command` (patch 12/13), enabling clients to query object metadata from remotes. The command is implemented in `builtin/cat-file.c` with the following key features:
  - **Execution model**: Unlike other `--batch-command` commands, which process objects one at a time, `remote-object-info` batches multiple object IDs into a single request to the server to minimize remote communication overhead. This design choice prioritizes efficiency for bulk queries while maintaining compatibility with the existing `--batch-command` interface.
  - **Input handling**: Accepts a remote URL followed by one or more full object IDs (40-character SHA-1/256), with strict validation against malformed OIDs, short OIDs, and excessive input length (capped at 8KB URL + 10,000 objects). Input parsing is robust, using `xcalloc` for allocations and bounds-checking on all inputs.
  - **Security**: Memory safety is enforced via `xcalloc` for allocations, bounds-checking on all inputs, and a new `free_object_info_contents()` helper (added to `odb.h`) to prevent leaks. The `is_remote` flag in `struct expand_data` and initialization of `data->type` to `OBJ_BAD` prevent uninitialized memory access. A memory leak in `transport-helper.c` was fixed in v19 by moving the `FREE_AND_NULL(data->name)` call outside the conditional block in `disconnect_helper()`, ensuring `data->name` is freed even when the transport is disconnected before the helper process starts. The fix is mechanical and has no behavior impact beyond plugging the leak.
  - **Protocol integration**: Uses the `object-info` protocol v2 capability (advertised by servers with `transfer.advertiseobjectinfo=true`) to fetch metadata. Dynamically validates format placeholders against the server's advertised attributes, silently omitting unsupported atoms (e.g., `%(objecttype)`) to match `for-each-ref` behavior. The client maintains a mapping between protocol options (`size`, `type`) and placeholders (`objectsize`, `objecttype`), and tracks supported placeholders in a new `remote_allowed_atoms` string list in `struct expand_data`. The patch enabling this dynamic adaptation (final patch in v20) replaces the previous hardcoded allow-list with a runtime-negotiated one, ensuring forward compatibility as the `object-info` protocol gains new capabilities. When the server does not advertise a requested field, the client silently omits it (e.g., `%(objecttype)` expands to an empty string), matching `for-each-ref`'s behavior for known-but-inapplicable atoms. The implementation moves `object_info_options` out of `get_remote_info()` so the caller can control which options are requested, and ensures output is always produced—even if no attributes are supported, resulting in a blank line. The `EXPAND_DATA_INIT` macro is updated to initialize the new `remote_allowed_atoms` string list.
  - **Core transport implementation** (patch 11/13, Pablo Sabater): The new `fetch_object_info()` function in `fetch-object-info.c` (138 lines) sends an `object-info` command to v2 servers, parses the response, and populates an `object_info` struct array. Key details:
    - **Protocol handling**: Validates server support for `object-info`, sends OIDs and requested attributes (currently `size`), and processes responses line-by-line. Each response line must start with the requested OID, followed by attribute values in the order specified.
    - **Edge cases**: Detects unrecognized objects via a new `unrecognized` field in `struct object_info` (a single bit for minimal overhead) and validates response format, including strict OID matching and attribute count. Unrecognized objects are flagged by an empty string in the response; malformed responses trigger fatal errors.
    - **Transport integration**: Extends the transport vtable with a new `fetch_object_info` entry, implemented for smart transports (`fetch_object_info_via_pack`) and transport helpers (`fetch_object_info_helper`). The smart transport implementation reuses connection setup logic from `fetch_refs_via_pack` but skips object negotiation, focusing solely on metadata retrieval.
    - **Memory safety**: Allocations for size pointers in `object_info_data` use `xcalloc`, and response parsing cleans up temporary string lists with `string_list_clear`.
  - **Extensibility**: The `object_info_options` string list and `object_info` struct are designed to accommodate future attributes (e.g., `type`) without breaking changes.
  - **Edge cases**: Handles missing objects (returns "missing" status), unsupported protocols (fails with clear error), and gracefully skips unsupported format placeholders (e.g., `%(objectcolor)` expands to an empty string). Batches multiple OIDs into a single request to minimize round trips. The `EXPAND_DATA_INIT` macro is updated to initialize the new `remote_allowed_atoms` string list. For `%(objectname)`-only requests, the client queries the server for the object's size (discarding the result) to validate the object's existence, per Junio's insistence on robustness over efficiency. This ensures the server checks the OID's existence while avoiding unnecessary downloads. The behavior is documented in the commit message, and Junio has explicitly rejected labeling this as `NEEDSWORK`, confirming it as the correct long-term solution.
  - **Output format**: Defaults to `%(objectname) %(objectsize)` (omitting `%(objecttype)` until type support is implemented), with a warning in the documentation that the default format may change once type support is added. Output is always produced—even if no attributes are supported, resulting in a blank line. The pre-sort in `fetch_object_info_via_pack()` is removed since the list is now unsorted due to filtering via `unsorted_string_list_delete_item`, though this has no performance impact given the small size of the list.
  - **Test coverage**: Adds 747 lines of new tests in `t/t1017-cat-file-remote-object-info.sh`, covering all three smart transports (`git://`, `file://`, `http://`), `--buffer` mode, `-Z` (null-delimited) output, dynamic placeholder handling, and edge cases like malformed OIDs, legacy protocols, and unsupported placeholders. The tests confirm that objects are not downloaded from the remote (validating the core efficiency claim) and that the command integrates cleanly with existing `--batch-command` functionality. Patch 3/13 (Pablo Sabater) extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh` to enable reuse in these tests. The change is mechanical, with no behavioral modifications, and includes a minor coding guideline fix for sourcing library files (`. "$TEST_DIRECTORY"/lib-loose.sh`).
- **Transport layer**: The series extends the transport layer with a new `fetch_object_info` vtable entry, implemented for both smart transports (`fetch_object_info_via_pack`) and transport helpers (`fetch_object_info_helper`). The request is buffered in memory (not streamed), and the implementation is designed for extensibility beyond the current size-only scope. The `remote-object-info <remote> <oid>...` command supports multiple OIDs per request and enforces protocol v2 and the `object-info` capability. Patch 7/13 (Calvin Wan, carried by Pablo Sabater) generalizes `write_fetch_command_and_capabilities()` into `write_command_and_capabilities()` by adding a `command` parameter, allowing the function to be reused for non-fetch protocol v2 operations like `object-info`. The change is mechanical: the function is renamed, its signature updated, and all callers in `fetch-pack.c` adjusted to pass `"fetch"` explicitly. The updated function signature and documentation in `connect.h` clarify its purpose, and the `ensure_server_supports_v2()` call is now parameterized to validate the requested command rather than hardcoding `"fetch"`.
- **Server-side advertisement**: Patch 10/13 (Calvin Wan, carried by Eric Ju and Pablo Sabater) enables servers to advertise supported object-info attributes (currently `size` and `type`) when `transfer.advertiseobjectinfo` is enabled. The `object_info_advertise` function in `serve.c` appends `"size"` (and `"type"`, though not yet exposed to clients) to the `value` strbuf if the feature is advertised, allowing clients to dynamically determine queryable metadata. The feature is disabled by default (`advertise_object_info = 0`) and requires explicit opt-in via configuration. The patch is minimal and mechanical, preserving the existing configuration logic while enabling the dynamic negotiation required for the client-side logic in later patches.
- **Security**: 10,000-object batch limit, 8KB URL length limit, integer overflow protection, dynamic allow-list for metadata fields, and strict input validation (e.g., rejects short OIDs or malformed responses). Memory management uses `xcalloc` and `free_object_info_contents()`. The `is_remote` flag in `struct expand_data` and initialization of `data->type` to `OBJ_BAD` prevent uninitialized memory access.
- **Edge cases**: Handles missing objects (returns "missing" status), unsupported protocol versions, malformed OIDs, and gracefully handles unsupported format placeholders by omitting them. Works with `--buffer` and `-Z` (null-delimited) options. For `%(objectname)`-only requests, the client queries the server for the object's size (discarding the result) to validate the object's existence, per Junio's insistence on robustness over efficiency. This ensures the server checks the OID's existence while avoiding unnecessary downloads. The behavior is documented in the commit message, and Junio has explicitly rejected labeling this as `NEEDSWORK`, confirming it as the correct long-term solution. The new commit (patch 9/13) ensures bare OID requests trigger an unconditional existence check on the server, fixing a subtle bug where the original check used `< 0` instead of `<= OBJ_NONE` to catch both `OBJ_BAD` and `OBJ_NONE` as unrecognized cases. Tests in `t/t5701-git-serve.sh` verify the new behavior: bare OID requests for valid objects return the OID alone, while unrecognized OIDs (including those with `size` requested) append a space.
- **Refactoring**:
  - Patch 2/13 (Eric Ju, carried by Pablo Sabater) tightens variable scope and type consistency in `builtin/cat-file.c` and `fetch-pack.c` by moving loop counters into `for` statements, changing `int` to `size_t` for array iterations, and updating `dispatch_calls()`'s `nr` parameter to `size_t`. The changes are mechanical and preparatory, with no behavior modifications, but improve code safety by limiting variable scope and ensuring type consistency.
  - Patch 3/13 (Pablo Sabater) extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh` to enable reuse in later test patches. The change is mechanical, with no behavioral modifications, and includes a minor coding guideline fix for sourcing library files (`. "$TEST_DIRECTORY"/lib-loose.sh` instead of `. "$TEST_DIRECTORY/lib-loose.sh"`).
  - Patch 4/13 (Calvin Wan, carried by Pablo Sabater) removes the static `advertise_sid` variable in `fetch-pack.c` to prepare for moving `write_fetch_command_and_capabilities()` to `connect.c` in a later patch. The static variable is replaced with local initializations in `find_common()` and `write_fetch_command_and_capabilities()`, and the `server_supports("session-id")` check in `do_fetch_pack()` is folded into the condition in `find_common()` to match the v2 protocol's inline check pattern. No behavior changes are introduced; the default value (0) matches the original static initialization. The patch is mechanical and uncontroversial, with Karthik Nayak's review confirming the commit message accurately describes the protocol scope and variable usage.
  - Patch 5/13 (Calvin Wan, carried by Pablo Sabater) moves the `write_fetch_command_and_capabilities()` function from `fetch-pack.c` to `connect.c` to enable broader reuse. The patch is mechanical: the function is relocated verbatim, a forward declaration for `struct string_list` is added to `connect.h`, and the original function is removed from `fetch-pack.c`. The **build fix** for this patch changes the `hash_algo` variable in the function from `int` to `const unsigned int` to match `hash_algo_by_name()`'s return type, resolving a signed/unsigned comparison warning that breaks the build when the function is moved to `connect.c` (which does not suppress sign-compare warnings like `fetch-pack.c` does).
  - Patch 6/13 (Pablo Sabater) relocates the `write_fetch_command_and_capabilities()` function from `fetch-pack.c` to `connect.c` to enable broader reuse. The patch is mechanical: the function is moved verbatim, a forward declaration for `struct string_list` is added to `connect.h`, and the original function is removed from `fetch-pack.c`. The forward declaration is safe because the function only uses `struct string_list` as a pointer, avoiding the need to include the full header. This patch prepares for patch 7/13, which generalizes the function into `write_command_and_capabilities()` by adding a `command` parameter for non-fetch protocol v2 operations like `object-info`.
  - Patch 7/13 (Calvin Wan, carried by Pablo Sabater) generalizes `write_fetch_command_and_capabilities()` into `write_command_and_capabilities()` by adding a `command` parameter, allowing the function to be reused for non-fetch protocol v2 operations like `object-info`. The change is mechanical: the function is renamed, its signature updated, and all callers adjusted to pass `"fetch"` explicitly. No behavior changes are introduced. The updated function signature and documentation in `connect.h` clarify its purpose, and the `ensure_server_supports_v2()` call is now parameterized to validate the requested command rather than hardcoding `"fetch"`.
  - Patch 8/13 (Calvin Wan, carried by Eric Ju and Pablo Sabater) moves variable initialization out of the `FETCH_CHECK_LOCAL` state in `do_fetch_pack_v2()` to a point before the state machine begins, ensuring the variables (`allow_unadvertised_object_request`, `use_sideband`, and `args->deepen`) are set regardless of the initial state. The change is mechanical and preparatory: a later patch in the series will introduce an alternative initial state, and this patch ensures the necessary variables are initialized in both cases. No behavior changes are introduced.
  - Patch 9/13 (Pablo Sabater) introduces a new `get_object_info()` wrapper around `odb_read_object_info_extended()` to enforce unconditional existence checks for bare OID requests. The wrapper sets `OBJECT_INFO_SKIP_FETCH_OBJECT` and `OBJECT_INFO_QUICK` flags to prevent partial-clone servers from fetching missing objects or rescanning packs unnecessarily. The patch tightens the existence check from `< 0` to `<= OBJ_NONE` to catch both `OBJ_BAD` and `OBJ_NONE` as unrecognized cases, ensuring bare OID requests for valid objects return just the OID, while unrecognized OIDs (including those with `size` requested) append a space. Three new tests in `t/t5701-git-serve.sh` verify the behavior: bare OID requests for valid objects, bare OID requests for unrecognized objects, and size requests for unrecognized objects. The implementation aligns with Junio’s insistence on prioritizing correctness over efficiency for existence checks.

**Changes in v20**:
1. **Dynamic capability negotiation**: The final patch in v20 replaces the hardcoded allow-list for `object-info` capabilities with a runtime-negotiated one, enabling the client to request only the metadata fields (`size`, `type`) that the server advertises support for. This ensures forward compatibility as the `object-info` protocol gains new capabilities. When the server does not advertise a requested field, the client silently omits it (e.g., `%(objecttype)` expands to an empty string), matching `for-each-ref`'s behavior for known-but-inapplicable atoms. The implementation introduces a new `remote_allowed_atoms` string list in `struct expand_data` to track server-supported placeholders, and maps protocol options (`size`, `type`) to their corresponding atoms (`objectsize`, `objecttype`) via a new `protocol_placeholder_entry` struct. The patch moves `object_info_options` out of `get_remote_info()` to allow the caller to control which options are requested, and ensures output is always produced—even if no attributes are supported, resulting in a blank line. The pre-sort in `fetch_object_info_via_pack()` is removed since the list is now unsorted due to filtering via `unsorted_string_list_delete_item`, though this has no performance impact given the small size of the list.
2. **Build fix**: Moved the `hash_algo` type fix (changing `int` to `const unsigned int`) to patch 5/13, ensuring it precedes the relocation of `write_fetch_command_and_capabilities()` to `connect.c` and resolves the signed/unsigned comparison warning that would otherwise break the build.
3. **Documentation**: Reverted documentation caveats for `objectsize:disk` and `deltabase` atoms in `git-cat-file.adoc`, restoring the `CAVEATS` section references that were removed in v19. Pablo Sabater confirmed this reversion was unintentional and agreed it is unnecessary, as the `objectsize:disk` atom already references the `CAVEATS` section. The reversion remains in place pending a decision on whether to reroll the series for this cosmetic issue.
4. **Macro formatting**: Reformatted the `EXPAND_DATA_INIT` macro to use one line per item for clarity and future expansion. Pablo Sabater attributed this change partly to Junio’s earlier suggestion, though Karthik Nayak noted the change may not improve readability and proposed an alternative style (trailing commas and field alignment) for cleaner future additions. Neither participant suggested a reroll is required for this cosmetic issue.
5. **Error messages**: Added more context to the `die()` message in `parse_cmd_remote_object_info()` when command-line parsing fails, improving error reporting.
6. **Memory leak fix**: Carried forward the fix for the memory leak in `transport-helper.c` (originally introduced in v19), ensuring `data->name` is freed unconditionally in `disconnect_helper()` regardless of whether the helper process has started. The fix is mechanical and has no behavior impact beyond plugging the leak.

**Resolved issues**:
1. **Dynamic placeholder handling**: The client now dynamically adapts to the server's advertised capabilities, silently omitting unsupported placeholders (e.g., `%(objecttype)` when the server doesn't advertise `type`) rather than failing. This matches `for-each-ref`'s behavior for known-but-inapplicable atoms and ensures forward compatibility as the `object-info` protocol gains new capabilities. The implementation moves `object_info_options` out of `get_remote_info()` so the caller can control which options are requested, and ensures output is always produced—even if no attributes are supported, resulting in a blank line. The `EXPAND_DATA_INIT` macro is updated to initialize the new `remote_allowed_atoms` string list in `struct expand_data`.
2. **`%(objectname)`-only requests**: Junio and Pablo resolved the disagreement in favor of robustness: the client queries the server for the object's size (discarding the result) to validate the object's existence, even when only `%(objectname)` is requested. Junio explicitly rejected Pablo's proposal to mark this as `NEEDSWORK`, confirming it as the correct long-term solution. The new commit (patch 9/13) ensures bare OID requests trigger an unconditional existence check on the server, closing a gap where bare OID requests could silently succeed without validating the object. The check is enforced via the new `get_object_info()` wrapper that sets `OBJECT_INFO_SKIP_FETCH_OBJECT` and `OBJECT_INFO_QUICK` flags, and fixes a subtle bug where the original check used `< 0` instead of `<= OBJ_NONE` to catch both `OBJ_BAD` and `OBJ_NONE` as unrecognized cases.
3. **Silent failure in `parse_cmd_remote_object_info()`** (patch 12/13): Fixed by replacing the silent `goto cleanup` with an early return when only `%(objectname)` is requested, avoiding unnecessary connection overhead while ensuring the OID is still returned.
4. **Edge case handling for no object-info options**: Unsupported placeholders (e.g., `%(objectcolor)`) expand to empty strings (e.g., `42 ` instead of `42 %(objectcolor)`), with documentation added to the commit message.
5. **Type inconsistency in `parse_object_size()`**: Fixed by changing the return type to `int`, as the caller only checks for success/failure.
6. **Macro formatting**: The `EXPAND_DATA_INIT` macro was reformatted to use one line per item for future expansion. Pablo Sabater attributed this partly to Junio’s suggestion, though Karthik Nayak noted it may not improve readability and proposed an alternative style (trailing commas and field alignment) for cleaner future additions. Neither participant suggested a reroll is required for this cosmetic issue.
7. **Static variable removal**: Karthik Nayak's review confirmed the accuracy of the commit message regarding `advertise_sid` removal and protocol scope.
8. **Documentation and code organization** (patch 12/13): Karthik Nayak raised three surface-level nits in v18:
   - **Documentation caveats**: Restored references to `CAVEATS` for `objectsize:disk` and `deltabase` atoms in `git-cat-file.adoc` in v20. Pablo Sabater confirmed this reversion was unintentional and agreed it is unnecessary. The reversion remains in place pending a decision on whether to reroll the series for this cosmetic issue.
   - **Translation context**: The error message `"remote-object-info: %s"` lacks a descriptive prefix for translators (still unresolved).
   - **Alphabetical ordering**: The `commands[]` array in `cat-file.c` lists `remote-object-info` before `flush`, breaking alphabetical order (still unresolved).
9. **Build system ordering**: Junio noted the new `fetch-object-info.o` entry in `Makefile` is out of alphabetical order (still unresolved).
10. **Response grammar**: Corrected in `gitprotocol-v2.adoc` to match implementation behavior.
11. **Session-state bug**: Fixed in v19 where the default format for `remote-object-info` could leak into subsequent `info` commands.
12. **Memory leak in `transport-helper.c`**: Fixed in v19 and carried forward in v20 by moving `FREE_AND_NULL(data->name)` outside the conditional block in `disconnect_helper()`, ensuring `data->name` is freed unconditionally.
13. **Signed/unsigned comparison warning**: Fixed in patch 5/13 by changing the `hash_algo` variable in `write_fetch_command_and_capabilities()` from `int` to `const unsigned int` to match `hash_algo_by_name()`'s return type, resolving a build breakage when the function is moved to `connect.c`.

CI runs confirm readiness for integration, and the series is **technically complete** for its initial scope (size-only queries, with type support designed for future expansion). Pablo Sabater contributed the critical patches (including the new commit in v19 and patch 9/13) that introduced the dynamic allow-list, robustness fixes, usability improvements, and the memory leak fix, resolving segfault risks and edge-case handling. Junio's prior confirmation that no unresolved design or implementation concerns remain now stands fully validated. The series is **cleared for graduation to `next`**, with minor cosmetic nits (translation context, alphabetical ordering in code and build files, documentation caveats, and macro formatting) still unresolved. The unresolved question is whether the documentation nit (restored `CAVEATS` references) or macro formatting warrants a reroll before the series graduates to `master`. The tone of the latest exchange suggests these issues will likely be addressed in a future cleanup if not resolved now. The series remains on track for integration into `master`.

---