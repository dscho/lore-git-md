Here is the updated AI thread summary with the new email incorporated:

---

This security-hardened series implements full `git cat-file --batch-command` integration for querying object metadata from remotes without full downloads, now **at v21** and **queued for `next`** (Junio's "What's cooking" report and Pablo Sabater's confirmation). The series has progressed through **21 versions**, with all technical, security, and design issues resolved, including the final resolution of the `%(objectname)`-only request handling, dynamic adaptation to server-advertised capabilities, memory leak fixes (including a newly identified leak in `transport-helper.c` where `data->name` was not freed when a transport was disconnected before its helper process started), and minor refactoring for variable scope, type consistency, test infrastructure reuse, and shared test helpers. The **build fix** for the signed/unsigned comparison warning in `write_fetch_command_and_capabilities()` (patch 5/13) is now extended to address a related type mismatch in `fetch-pack.c`: the local `hash_algo` variable, previously declared as `int`, is updated to `const unsigned int` to match the return type of `hash_algo_by_name()`. This change is mechanical, marked `const` because the variable is never modified, and ensures type consistency ahead of the function's relocation to `connect.c` in patch 6/13.

The implementation enables Git clients to query object metadata (currently size, with type support designed for future expansion) from remote repositories via the `object-info` protocol v2 capability. Key technical details:
- **Protocol**: Full (non-abbreviated) object IDs, numeric `obj-size` values, and strict handling of unsupported object IDs (returning `<OID> SP`). The server advertises supported attributes (e.g., `size`, `type`) when `transfer.advertiseobjectinfo` is enabled, allowing clients to dynamically tailor queries. Patch 9/13 enforces consistent object-existence checks on the server side by extracting the check from `send_info()` into a new helper, `get_object_info()`, which wraps `odb_read_object_info_extended()` with `OBJECT_INFO_SKIP_FETCH_OBJECT` and `OBJECT_INFO_QUICK` flags. This ensures a partial-clone server won’t fetch missing objects during an `object-info` request and avoids unnecessary pack rescans. Bare OID requests now return `<OID>` for recognized objects and `<OID> SP` for unrecognized ones, regardless of requested attributes. The unrecognized-object check is tightened from `< 0` to `<= OBJ_NONE` to cover both `OBJ_BAD` and `OBJ_NONE` cases. Documentation in `gitprotocol-v2.adoc` is updated to reflect these behaviors: full OIDs are required, `obj-size` is explicitly defined as numeric, and the response format for unrecognized OIDs is clarified. Patch 10/13 enables servers to advertise supported attributes (e.g., `"size"`) via the `object-info` capability when `transfer.advertiseobjectinfo` is enabled, allowing clients to dynamically negotiate queryable metadata. The advertisement is implemented in `object_info_advertise()` in `serve.c`, which appends supported attributes to the `value` strbuf.
- **Client-side command**: The `remote-object-info` command in `git cat-file --batch-command` (patch 12/13) queries metadata from remotes with the following features:
  - **Execution model**: Batches multiple OIDs into a single request to minimize overhead, while maintaining compatibility with `--batch-command`. Special handling for `--buffer` mode avoids remote communication overhead.
  - **Input handling**: Accepts a remote URL and full OIDs (40/64-character SHA-1/256), with strict validation against malformed inputs (8KB URL limit, 10,000-object cap). No short OIDs are allowed.
  - **Security**: Uses `xcalloc` for allocations, bounds-checking, and `free_object_info_contents()` to prevent leaks. The `is_remote` flag in `struct expand_data` and `data->type = OBJ_BAD` initialization prevent uninitialized memory access. A memory leak in `transport-helper.c` (fixed in v21) ensures `data->name` is freed unconditionally, even when a transport is disconnected before its helper process starts.
  - **Protocol integration**: Dynamically validates format placeholders against server-advertised attributes (e.g., omits `%(objecttype)` if unsupported), matching `for-each-ref` behavior. The `remote_allowed_atoms` string list in `struct expand_data` tracks supported placeholders, and the `EXPAND_DATA_INIT` macro is updated to initialize it. Unsupported atoms return empty strings. The client maps server-advertised options (e.g., `"size"`) to their corresponding atoms (e.g., `"objectsize"`) using a `protocol_placeholder_entry` array, ensuring graceful handling of servers with varying capabilities. Unknown atoms (e.g., `%(objectcolor)`) trigger a fatal error, while unsupported but known atoms (e.g., `%(objecttype)` when the server lacks `"type"` support) expand to empty strings.
  - **Edge cases**: Handles missing objects ("missing" status), unsupported protocols, and malformed OIDs. For `%(objectname)`-only requests, the client queries the server for size (discarding the result) to validate existence, per Junio's robustness requirement. The client iterates backward through `object_info_options` during filtering to avoid skipping elements when removing unsupported options.
  - **Output format**: Defaults to `%(objectname) %(objectsize)`, with a warning that the format may change when type support is added. Output is always produced (blank line if no attributes are supported), with `batch_object_write()` moved outside the conditional block to guarantee this behavior.
  - **Test coverage**: 747 lines of new tests in `t/t1017-cat-file-remote-object-info.sh` cover all smart transports (`git://`, `file://`, `http://`), `--buffer` mode, `-Z` output, and edge cases (missing objects, unsupported protocols, malformed inputs, `%(objectname)`-only requests, and dynamic capability negotiation). Test infrastructure now includes a shared library (`t/lib-cat-file.sh`) with helper functions (`echo_without_newline`, `echo_without_newline_nul`, `strlen`) extracted from `t/t1006-cat-file.sh` to avoid duplication in later patches (patch 3/13). Patch 9/13 adds 63 new lines to `t/t5701-git-serve.sh`, covering bare OID requests, unrecognized OIDs, and size requests for unrecognized OIDs.
- **Transport layer**: Extends the transport vtable with `fetch_object_info`, implemented for smart transports (`fetch_object_info_via_pack`) and helpers (`fetch_object_info_helper`). Patch 11/13 introduces `fetch-object-info.c`, which contains the core logic for sending `object-info` requests and parsing responses. The client sends a request with the `object-info` command, supported attributes (currently only `size`), and a list of OIDs, buffered in memory (following the pattern in `fetch-pack.c`). The server’s response is parsed line-by-line, with strict validation of OIDs and attribute counts. A new `unrecognized` field in `struct object_info` is set when the server does not recognize an OID, and the `size` attribute is parsed into a `size_t` with overflow protection. The transport vtable implementation handles protocol version discovery, stateless RPC, and connection cleanup. Error handling includes dying if the server does not advertise the `object-info` capability, if the protocol version is not v2, or if the response format is malformed. Input validation includes bounds-checking for OIDs and attribute counts.
- **Server-side advertisement**: Patch 10/13 enables servers to advertise supported attributes (e.g., `size`, `type`) when `transfer.advertiseobjectinfo` is enabled, allowing clients to dynamically negotiate queryable metadata. The advertisement is implemented in `object_info_advertise()` in `serve.c`, which appends supported attributes to the `value` strbuf.
- **Security**: 10,000-object batch limit, 8KB URL length limit, integer overflow protection, and strict input validation. Memory management uses `xcalloc` and `free_object_info_contents()`.
- **Refactoring**: Includes mechanical changes for variable scope (patch 2/13), test helper extraction (patch 3/13), static variable removal (patch 4/13, **updated in v21 to eliminate the `advertise_sid` global flag**), function relocation (patches 5-6/13), generalization of `write_command_and_capabilities()` (patch 7/13), and **initialization consolidation in `do_fetch_pack_v2()`** (new in v21). Patch 2/13 specifically tightens variable scope and type consistency in `builtin/cat-file.c` and `fetch-pack.c` by moving loop counters into the `for` statement and switching from `int` to `size_t` for object-counting variables (`i`, `nr`), avoiding signed/unsigned mismatches. The `nr` parameter of `dispatch_calls()` is updated to `size_t` to align with its callers. Patch 4/13 replaces the static `advertise_sid` variable with local reads via `repo_config_get_bool()` in both `find_common()` and `write_fetch_command_and_capabilities()`, initializing the flag to 0 (matching its default) and folding the redundant server capability check in `do_fetch_pack()` into the condition in `find_common()` to align v0/v1 and v2 code paths. This change is a prerequisite for the function relocation in patch 5/13 and does not alter behavior. Patch 6/13 moves `write_fetch_command_and_capabilities()` from `fetch-pack.c` to `connect.c` to prepare for its later generalization into a reusable utility for additional commands beyond `fetch`. The relocation is mechanical, with no behavior changes, and `connect.h` gains a forward declaration for `struct string_list` to avoid pulling in `string-list.h` unnecessarily. The new refactoring (v21) consolidates variable initialization in `do_fetch_pack_v2()` by moving six lines of setup code from the `FETCH_CHECK_LOCAL` state to the top of the function, ensuring consistent initialization regardless of the state machine's starting state. This is a prerequisite for a future patch (not yet in the series) that will introduce a second possible initial state.

**Changes in v21**:
1. **Dynamic atom allow-list (patch 13/13)**: Replaces the static `remote_object_info_atoms[]` array with a `remote_allowed_atoms` string list in `struct expand_data`, enabling the client to adapt to server-advertised `object-info` capabilities. The client queries the server for each requested placeholder (e.g., `%(objectsize)` → `"size"`), filters out unsupported options using `unsorted_string_list_has_string()`, and maps the remaining ones back to their placeholders via a `protocol_placeholder_entry` array. Unsupported but known atoms (e.g., `%(objecttype)` when the server lacks `"type"` support) expand to empty strings, matching `for-each-ref` behavior. Unknown atoms (e.g., `%(objectcolor)`) trigger a fatal error. The patch ensures output is always produced (blank line if no attributes are supported) by moving `batch_object_write()` outside the conditional block. Test coverage verifies `%(objectname)`-only requests and dynamic capability negotiation.
2. **Documentation**: Dropped the unnecessary restoration of `CAVEATS` references for `objectsize:disk` and `deltabase` atoms in `git-cat-file.adoc`, resolving the prior disagreement between Pablo Sabater and Karthik Nayak. The change was confirmed as redundant and will not be revisited.
3. **Macro formatting**: Updated the `EXPAND_DATA_INIT` macro to use Karthik Nayak's preferred style (trailing commas and field alignment) for cleaner future expansion. The change is cosmetic and does not affect functionality.
4. **Documentation typo**: Fixed a typo and added a blank line before the `WARNING` in the documentation. These are surface-level improvements with no behavioral impact.
5. **Memory leak fix**: Moved `FREE_AND_NULL(data->name)` outside the conditional block in `transport-helper.c` to ensure it runs unconditionally, plugging a leak that occurred when a transport was disconnected before its helper process started. The fix is minimal and directly addresses the issue without side effects.
6. **Static variable removal**: Eliminated the `advertise_sid` global flag in `fetch-pack.c` (patch 4/13), replacing it with local reads via `repo_config_get_bool()`. This refactoring is mechanical and prepares for the function relocation in patch 5/13.
7. **Type consistency fix**: Updated the local `hash_algo` variable in `fetch-pack.c` from `int` to `const unsigned int` to match the return type of `hash_algo_by_name()`. The change is marked `const` because the variable is never modified and ensures type consistency ahead of the function's relocation to `connect.c` in patch 6/13.
8. **Function generalization**: Patch 7/13 generalizes `write_fetch_command_and_capabilities()` into `write_command_and_capabilities()` by adding a `command` parameter, enabling reuse for protocol v2 operations beyond `fetch` (e.g., `object-info`). The change is mechanical, with all callers in `fetch-pack.c` adjusted to pass `"fetch"` explicitly, and the function’s declaration in `connect.h` updated with a descriptive comment. The redundant `write_fetch_command_and_capabilities()` prototype is removed.
9. **Initialization consolidation**: Moved variable initialization in `do_fetch_pack_v2()` from the `FETCH_CHECK_LOCAL` state to the top of the function, ensuring consistent setup regardless of the state machine's starting state. This is a prerequisite for a future patch introducing a second initial state.
10. **Server-side consistency fix (patch 9/13)**: Enforces consistent object-existence checks on the server side by extracting the check from `send_info()` into `get_object_info()`, which wraps `odb_read_object_info_extended()` with `OBJECT_INFO_SKIP_FETCH_OBJECT` and `OBJECT_INFO_QUICK` flags. This ensures a partial-clone server won’t fetch missing objects during an `object-info` request and avoids unnecessary pack rescans. The unrecognized-object check is tightened from `< 0` to `<= OBJ_NONE` to cover both `OBJ_BAD` and `OBJ_NONE` cases. Bare OID requests now return `<OID>` for recognized objects and `<OID> SP` for unrecognized ones, regardless of requested attributes. Documentation in `gitprotocol-v2.adoc` is updated to clarify these behaviors.
11. **Server-side advertisement (patch 10/13)**: Enables servers to advertise supported object-info attributes (e.g., `"size"`) via the `object-info` capability when `transfer.advertiseobjectinfo` is enabled. The advertisement is implemented in `object_info_advertise()` in `serve.c`, which appends supported attributes to the `value` strbuf. The patch is authored by Calvin Wan but carries sign-offs from Eric Ju and Pablo Sabater, reflecting collaborative development.
12. **Transport layer implementation (patch 11/13)**: Introduces `fetch-object-info.c` to handle the client-side transport logic for the `object-info` protocol v2 capability. The patch extends the transport vtable with a `fetch_object_info` method, implemented for smart transports (`fetch_object_info_via_pack`) and transport helpers (`fetch_object_info_helper`). The client sends buffered requests, parses responses line-by-line with strict validation, and handles errors (e.g., missing capability, malformed responses). The implementation is type-safe, bounds-checked, and designed for extensibility.

**Resolved issues**:
- All technical and design concerns from prior versions, including dynamic capability negotiation, `%(objectname)`-only request handling, memory leaks, edge-case robustness, test infrastructure reuse, and server-side consistency.
- Cosmetic nits from v20: `CAVEATS` references, macro formatting, documentation typos, static variable removal, type consistency, function generalization, initialization consolidation, and server-side consistency. These were addressed in v21 and will not block graduation.
- The series is **technically complete** for its initial scope (size-only queries, with type support designed for future expansion) and **cleared for graduation to `master`**. The GitHub CI run shows no failures, and no further changes are expected.

**Unresolved (cosmetic, non-blocking)**:
- Translation context for the error message `"remote-object-info: %s"` (lacks descriptive prefix).
- Alphabetical ordering of the `commands[]` array in `cat-file.c` (lists `remote-object-info` before `flush`).
- Alphabetical ordering of `fetch-object-info.o` in `Makefile`.

The series remains on track for integration into `master`. Pablo Sabater's contributions (including patches 6/13, 7/13, 9/13, 10/13, 11/13, 13/13, the memory leak fix, test infrastructure refactoring, static variable removal, type consistency fixes, function generalization, initialization consolidation, and the dynamic atom allow-list) resolved the dynamic capability negotiation, robustness fixes, usability improvements, and code reuse, while Junio's prior confirmation of no unresolved concerns stands validated. The series is ready for final integration. **Authorship**: Patch 12/13 (the core `remote-object-info` command) is authored by Eric Ju, with significant contributions from Pablo Sabater (atom allow-list validation) and Calvin Wan. Sign-offs include Jonathan Tan and Christian Couder.

---