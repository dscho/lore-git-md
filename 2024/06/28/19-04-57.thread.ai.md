Here is the updated AI thread summary:

---

This security-hardened series implements full `git cat-file --batch-command` integration for querying object metadata from remotes without full downloads, now **complete at v18** and **queued for merging to `next`**. The series has progressed through **18 versions**, with all technical, security, and design issues resolved and confirmed ready for integration. The latest update finalizes the **client-side implementation** of the `remote-object-info` command, the core user-facing feature of the series.

The implementation enables Git clients to query object metadata (currently only size) from remote repositories via the `object-info` protocol v2 capability. Key technical details:
- **Protocol**: Full (non-abbreviated) object IDs, numeric `obj-size` values, and strict handling of unsupported object IDs (returning `<OID> SP`). The server advertises the `size` attribute when `transfer.advertiseobjectinfo` is enabled.
- **Client-side command**: `remote-object-info <remote> <oid>...` supports multiple OIDs per request, works with all smart transports (`git://`, `file://`, `http://`), and enforces protocol v2 and the `object-info` capability. Format support is currently limited to `%(objectname)` and `%(objectsize)`; other atoms (e.g., `%(objecttype)`) are explicitly rejected via a dynamic allow-list to prevent segfaults or fatal errors. Unsupported placeholders now return empty strings (aligning with `for-each-ref` behavior) rather than raising errors or crashing.
- **Security**: 10,000-object batch limit, 8KB URL length limit, integer overflow protection, dynamic allow-list for metadata fields, and strict input validation (e.g., rejects short OIDs or malformed responses). Memory management uses `xcalloc` and `free_object_info_contents()`. The `is_remote` flag in `struct expand_data` and initialization of `data->type` to `OBJ_BAD` prevent uninitialized memory access.
- **Edge cases**: Handles missing objects (returns "missing" status), unsupported protocol versions, malformed OIDs, and now gracefully handles unsupported format placeholders (e.g., `%(objectsize:disk)`, `%(deltabase)`) by returning empty strings. Works with `--buffer` and `-Z` (null-delimited output) options.
- **Integration**: New `fetch_object_info()` function with stateless RPC support, transport-agnostic design, and comprehensive test coverage (699 lines in `t/t1017-cat-file-remote-object-info.sh`). Tests verify objects are *not* downloaded, behavior matches local `info` where applicable, and format strings remain robust across local/remote contexts.

CI runs confirm readiness for integration. Future expansion (e.g., object type support) is explicitly designed for. The series is functionally complete for its initial scope (size-only queries), with the final robustness fixes addressing the last known edge cases in format placeholder handling. Pablo Sabater contributed the critical patch (12/13) that introduced the dynamic allow-list and empty-string fallback, resolving segfault risks and usability issues. No open questions or objections remain.