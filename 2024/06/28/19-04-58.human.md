This is the first patch in a 6-part series implementing remote object-info functionality in `git cat-file --batch-command`. It's a refactoring change that generalizes capability writing code in fetch-pack.c to be reusable for both fetch and the new remote-object-info command.

The patch renames `write_fetch_command_and_capabilities()` to the more generic `write_command_and_capabilities()` and makes the command name parameterizable. This prepares the codebase for the subsequent introduction of the new remote-object-info command while maintaining all existing fetch functionality.

The change is straightforward and mechanical, with no behavior modifications. It has sign-offs from Calvin Wan (the series author) and Eric Ju (the patch author), plus help-from tags from Jonathan Tan and Christian Couder indicating they provided input during development. The diff shows clean, focused changes to the fetch-pack.c file with no surprises.

This is part of a well-structured series that has already gone through multiple rounds of security hardening and review. The refactoring here is a typical preparatory step before introducing new functionality in later patches.