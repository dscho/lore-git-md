This is a small refactoring patch in the `cat-file` command series, moving loop variable declarations inside their respective `for` loops for better scoping and readability. The change affects three functions in `builtin/cat-file.c` where loop counters were previously declared at function scope but only used within the loops.

The patch is straightforward and mechanical, with no behavior changes. It follows modern C style conventions where loop variables are scoped as tightly as possible. Christian Couder provided guidance (Helped-by), suggesting this is a deliberate cleanup rather than an ad hoc change.

This appears to be a preparatory cleanup as part of the larger remote object-info functionality series, though the change itself is independent of that feature. The patch is well-contained and low-risk, touching only style rather than logic.