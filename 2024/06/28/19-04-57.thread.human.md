This thread is about a long-running security-hardened series implementing `git cat-file --batch-command` support for querying object metadata from remotes without full downloads. The series is now at version 21 and has been queued for the `next` branch, meaning it’s on track for the next Git release. The core functionality—allowing clients to fetch object sizes (and eventually types) from remote repositories—is complete, with all technical and security concerns resolved across 21 iterations.

Key participants include Pablo Sabater, who has driven much of the recent work, and Junio Hamano, the maintainer, who has confirmed the series is ready for integration. The latest update is a minor refactoring patch (part of v21) that removes a static variable to prepare for moving a function between files. This change is uncontroversial and doesn’t affect behavior, but it’s a necessary step for the broader restructuring of the transport layer.

The series has addressed all major issues, including dynamic capability negotiation, memory leak fixes, and robust handling of edge cases like missing objects or unsupported protocols. Test coverage is extensive, with over 700 lines of new tests ensuring compatibility across all smart transports. The only remaining items are minor cosmetic nits (like translation context and alphabetical ordering in code), which won’t block graduation. The series is now technically complete and awaiting final integration into `master`.