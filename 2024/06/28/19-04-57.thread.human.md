This thread introduces client-side support for the object-info protocol, allowing metadata queries without full object downloads. The series (now at v8) adds core infrastructure in `fetch-object-info.{c,h}` and integrates it into `git cat-file` for batched queries. The current implementation focuses on the "size" attribute, requiring protocol v2 with proper handling of both stateful and stateless RPC modes.

The implementation has evolved through review feedback, with Eric Ju and Junio C Hamano suggesting improvements to response parsing and error messaging. The final patch adds a `remote-object-info` command to `git cat-file --batch-command`, processing multiple OIDs in single network requests for efficiency. It includes comprehensive tests (652 lines) and documents the temporary format differences from local queries, with Calvin Wan approving this approach as stable enough for inclusion while leaving room for future attribute additions.

With architectural concerns resolved and the code polished through eight iterations, the feature appears ready for merging. The thread shows consensus forming around both the core protocol support and its `cat-file` integration, with the implementation now awaiting final maintainer approval. The solution balances immediate utility (size queries) with extensibility for future metadata types.