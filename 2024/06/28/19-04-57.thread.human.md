This thread implements a security-hardened feature allowing `git cat-file --batch-command` to query object metadata (like size) from remote repositories without downloading full objects. The series, now at version 17, is nearly complete after 17 iterations addressing memory safety, protocol design, and input validation concerns.

The core functionality is now implemented: the new `remote-object-info` command batches multiple object queries into a single network request, supports all transport protocols, and includes strict input validation (10,000-object limit, 8KB URL length) to prevent abuse. The implementation features dynamic capability-based placeholder validation that directly addressed earlier segfault issues, and includes 699 lines of new tests covering edge cases and error conditions.

All technical issues have been resolved except one remaining bounds-checking concern in the response parsing logic, which Pablo Sabater has agreed to fix. Junio Hamano identified that the code currently accesses `object_info_values.items[1 + size_index]` without verifying the `string_list` contains enough elements, creating a potential out-of-bounds read if servers return malformed responses. Pablo will add this defensive check while also making minor style improvements to loop formatting.

The series was temporarily reverted from `next` due to an attribution issue in the cover letter (still listing original authors despite Pablo taking over as primary implementer), but this is purely administrative. Once the bounds-checking fix is implemented and the cover letter corrected, the series should be ready for merging. All other aspects - memory safety, protocol design, test coverage, and security hardening - are complete and verified.