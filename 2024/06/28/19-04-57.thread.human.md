This thread implements secure remote object metadata queries via `git cat-file --batch-command`, adding a new `remote-object-info` command that lets clients request object sizes (and eventually other metadata) from servers supporting the object-info capability in Git's protocol v2. Pablo Sabater's fourteenth iteration of the series is now ready for merging, having addressed all prior review feedback—most notably removing unnecessary `static` declarations that risked thread safety and reverting an unintended protocol compatibility regression in `connect.c`.

The implementation enforces strict capability-based validation: clients validate requested format placeholders against the server's advertised capabilities, preventing information leaks. This version also fixes a memory leak in `transport-helper.c` and improves numeric conversion safety by replacing `strtoul_ul()` with `strtoul_szt()` to match the recent change of `object_info.sizep` to `size_t`. Other refinements include splitting a function-move patch for clarity, adding explanatory comments for backward iteration in capability validation, and polishing commit messages and style.

The series maintains backward compatibility while being forward-compatible for future metadata types. Currently, only object sizes are fully supported; other fields like object types return empty strings. All prior substantive reviews from Junio Hamano, Patrick Steinhardt, Karthik Nayak, and others have been addressed, and the GitHub CI run shows all tests passing. The implementation includes comprehensive test coverage in `t1017-cat-file-remote-object-info.sh` and shared test infrastructure in `t/lib-cat-file.sh`. With no outstanding objections, the series demonstrates careful attention to security, memory safety, and protocol design.