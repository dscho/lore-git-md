This thread is about a security-hardened feature that adds `remote-object-info` support to `git cat-file --batch-command`, allowing clients to query object metadata (like size) from remote repositories without downloading the full objects. The series is now at **version 19** and has been **queued for `next`**, meaning it’s in the final integration testing phase before graduating to `master`.

The latest patch, from Pablo Sabater, makes the client dynamically adapt to the server’s advertised capabilities. Instead of using a static allow-list of metadata placeholders (`objectname`, `objectsize`), the client now queries the server for supported attributes (currently `size` and `type`) and silently omits unsupported ones. This ensures forward compatibility as the `object-info` protocol evolves. The patch also ensures that bare `%(objectname)` requests still trigger an existence check on the server, maintaining robustness, and that output is always produced—even if no attributes are supported.

The series has resolved all major technical and design issues, including memory leaks, edge-case handling, and protocol robustness. Junio Hamano has confirmed no unresolved concerns remain, and the series is now technically complete for its initial scope (size-only queries). Minor cosmetic nits (like documentation tweaks and code organization) will likely be addressed in a final polish before merging to `master`. The implementation is secure, with strict input validation, memory safety, and a 10,000-object batch limit to prevent abuse. CI runs have passed, and the feature is ready for integration.