This thread implements the `git cat-file --batch-command remote-object-info` feature, allowing users to query object metadata (currently just size) from remote repositories without downloading full objects. Pablo Sabater has driven the series through 17 versions, addressing all technical feedback and resolving critical issues like memory leaks, type mismatches, and a refactoring flaw that risked undefined behavior.

The implementation is now complete, with the final `remote-object-info` command integrated into `git cat-file --batch-command`, supporting batch queries, input validation, and all three transport protocols. The series includes 699 lines of new tests and dynamic capability-based placeholder validation to prevent crashes. Junio Hamano recently identified one remaining issue in the response-parsing logic of the final patch: the code accesses `object_info_values.items[1 + size_index]` without verifying the array bounds, which could lead to out-of-bounds reads if a server returns malformed responses. Pablo has agreed to add the missing bounds-check and will either send a full v18 resend (with the cover letter attribution fixed) or a single patch on top of `next` to address this.

The series is ready for merging once this fix is applied. All other concerns—including memory safety, protocol compatibility, and error handling—have been resolved. The only administrative blocker is correcting the cover letter attribution, which still lists Eric Ju and Calvin Wan despite Pablo’s primary authorship. Reviewers Chandra Pratap and Karthik Nayak have confirmed the series is technically sound, and the implementation is now stable.