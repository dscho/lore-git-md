This thread implements a security-hardened feature allowing `git cat-file --batch-command` to query object metadata (like size) from remote repositories without downloading full objects. The series, now at version 17, is technically complete and has resolved all prior issues—including memory leaks, type mismatches, and critical refactoring flaws—except for one remaining bounds-checking concern in the final patch.

Junio Hamano recently reverted the merge of v16 to `next` due to an administrative issue: the cover letter incorrectly attributed the work to Eric Ju and Calvin Wan, despite Pablo Sabater being the primary author and implementer. This procedural error violates Git’s contribution guidelines but does not reflect any technical problems. The revert is purely clerical, and the series will be re-merged once the cover letter is corrected. All substantive feedback has been addressed, including Junio’s earlier identification of a static variable flaw (now fixed) and Chandra Pratap’s memory management concerns.

The implementation includes dynamic capability-based validation to prevent crashes from unsupported placeholders, strict protocol v2 enforcement, and comprehensive test coverage (699 lines of new tests). The only remaining issue is a bounds-checking vulnerability in the response parsing logic, where the code accesses an array without verifying its length. This could lead to an out-of-bounds read if a server returns malformed data, but the fix is straightforward. Once resolved, the series is ready for `next`. Key contributors include Pablo Sabater (implementation), Junio Hamano (review), Chandra Pratap (final review), and Eric Ju (test infrastructure).