This thread is about a security-hardened `git cat-file --batch-command` feature that lets users query object metadata from remotes without downloading full objects. The series has evolved through 13 versions, with all major technical issues now resolved, and is ready for merging.

The implementation introduces dynamic capability-based validation of format placeholders (replacing static allow-lists), server-side capability advertisement, and a complete client-side protocol implementation. Recent refinements include memory leak fixes in the transport helper, a new `strtoul_ul()` utility function with robust error handling, and improved error messages. The series has received approval from all core reviewers (Junio Hamano, Patrick Steinhardt, Jonathan Tan, Christian Couder, and Taylor Blau) after addressing concerns about type safety, protocol design, and edge cases.

The latest update is a small mechanical patch that improves code hygiene by moving loop counter declarations into `for()` statements and using `size_t` where appropriate. This change, suggested by Junio during review, has no functional impact but makes the code more readable and consistent with modern C practices. With this final polish applied, the series is now complete and awaiting integration.