This thread is about a security-hardened feature that lets Git clients query object metadata (like size) from remote repositories without downloading the full objects. The series, now at version 19, is queued for the `next` branch and nearly ready for merging. It introduces the `remote-object-info` command in `git cat-file --batch-command`, enabling efficient metadata queries via the `object-info` protocol v2 capability.

The latest update is a small but critical build fix for patch 5/13. The issue was a signed/unsigned comparison warning in `write_fetch_command_and_capabilities()`: the function’s `hash_algo` variable was declared as `int`, but `hash_algo_by_ptr()` returns an `unsigned int`. This warning was suppressed in `fetch-pack.c` but broke the build when the function was moved to `connect.c`. SZEDER Gábor spotted the problem, and Pablo Sabater agreed to fix it by changing the variable to `unsigned int`. The fix is mechanical and doesn’t change behavior, but it’s necessary for the series to compile cleanly before graduating to `master`.

The series has resolved all major technical and design issues, including dynamic placeholder handling (so clients adapt to server capabilities), robust `%(objectname)`-only requests (which now trigger existence checks), and memory safety improvements. A memory leak in `transport-helper.c` was also fixed in v19. CI runs confirm the series is ready, and Junio has confirmed no unresolved concerns remain. The only remaining tasks are minor cosmetic nits (like documentation tweaks and alphabetical ordering in code) and this build fix. Once those are addressed, the series will likely merge to `master`.