This thread implements a security-hardened `git cat-file --batch-command` feature for querying object metadata from remotes without full downloads. The series has reached its 14th version and is nearly ready for merging, with all technical and security issues resolved except for some architectural concerns and minor documentation improvements.

The feature now includes server capability advertisement, client-side implementation, dynamic format placeholder validation, and comprehensive memory safety improvements. Key changes in v14 include memory leak fixes, protocol compatibility restorations, and platform-independent numeric handling. The server advertises supported metadata (currently only "size") via a space-separated capability format (e.g., `object-info=size`), with Pablo Sabater confirming this approach will scale for future metadata types like "objecttype".

Recent discussion clarified the protocol's extensibility mechanism, but Karthik Nayak raised substantive concerns about error handling in the client implementation (patch 10/13) and documentation style in the format validation (patch 11/13). The error handling currently returns empty strings for unsupported fields, which creates ambiguity about command success and complicates future extensibility. Karthik also suggested improvements to make the documentation more scalable as new metadata types are added.

The series demonstrates strong cross-platform correctness and security awareness, with 680 lines of new tests. All prior feedback has been addressed, leaving only the newly identified architectural concerns and documentation improvements before final readiness. Pablo Sabater has been responsive to review feedback throughout the thread.