This thread is about a security-hardened feature allowing Git clients to query object metadata from remote repositories without downloading the full objects, now in its 20th iteration and queued for the `next` branch. The series has resolved all technical and design issues, including dynamic adaptation to server capabilities, robust handling of bare object ID requests, and memory safety fixes.

The latest update is a minor but necessary infrastructure change: three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) have been moved from an existing test script into a shared library file (`t/lib-cat-file.sh`). This is purely preparatory work to avoid code duplication in upcoming tests for the new `remote-object-info` command. The change is mechanical, with no functional impact, but includes a small coding style fix for how library files are sourced. Pablo Sabater contributed this patch as part of the broader effort, which is now technically complete and awaiting final polish before merging to `master`. The only remaining loose ends are minor cosmetic nits like alphabetical ordering in code and build files.