This thread implements a security-hardened `git cat-file --batch-command` extension that lets users query object metadata (currently just size) from remote repositories without downloading full objects. The feature is now complete after 18 iterations, with all technical and design issues resolved except for two final nits.

The series introduces the `remote-object-info` command, which batches multiple object requests into a single network call, supports all transport protocols, and enforces strict input validation (10,000-object limit, 8KB URL length). Key improvements include dynamic capability-based placeholder validation to prevent crashes, memory leak fixes in the transport helper, and 699 lines of new tests covering edge cases. Junio Hamano's review identified one remaining security concern: an out-of-bounds read risk in response parsing, which Pablo Sabater will fix in the upcoming v18 resend by adding bounds-checking (`object_info_values.nr > 1 + size_index`). The only other outstanding issue is a cover letter attribution correction to credit Pablo as the primary author.

Once v18 is sent, Junio will merge the series to `next`. The implementation is production-ready, with all prior feedback addressed—including protocol extensibility documentation, type safety fixes, and state machine refactoring. Pablo plans a follow-up series to add `objecttype` support after this lands.