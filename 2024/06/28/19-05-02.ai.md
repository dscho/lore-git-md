This is a small refactoring patch in the `cat-file` command implementation, part of a larger series adding remote object-info functionality. The change moves loop counter variable declarations inside their respective `for` loops rather than declaring them at function scope, improving code locality and readability.

The patch touches three functions in `builtin/cat-file.c`:
1. `dispatch_calls()` - changes `int i` declaration to be loop-local
2. `free_cmds()` - changes `size_t i` declaration to be loop-local
3. `batch_objects_command()` - moves `int i` declaration inside the command parsing loop

The changes are mechanical and straightforward, with no behavior modifications. Christian Couder provided guidance (Helped-by), suggesting this is a deliberate style improvement rather than an ad-hoc change. The patch aligns with Git's coding guidelines that prefer limiting variable scope where possible.

As part of the larger remote object-info series, this appears to be preparatory cleanup work rather than directly related to the new functionality. The patch demonstrates good attention to code hygiene in the area being modified for the feature.