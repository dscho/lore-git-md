This is a small code hygiene patch in the `git cat-file --batch-command` series, which adds remote object info querying capabilities. The patch moves variable declarations inside their respective for loops where they are only used, improving scoping and readability.

The changes are mechanical and straightforward:
- Moves `int i` declarations inside three for loops in `builtin/cat-file.c`
- Changes one loop counter from `size_t` to match the type of the variable being compared against
- Maintains all existing functionality while making the code slightly cleaner

This patch was reviewed by Christian Couder, who suggested the improvement. It's part of the final polishing for the larger feature series that appears ready for merging, with all substantive feedback addressed in earlier iterations. The change is purely stylistic and doesn't affect the security or functionality of the remote object info feature.