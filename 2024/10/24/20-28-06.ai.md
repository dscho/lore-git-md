This is the final patch in a security-hardened series implementing remote object info queries via `git cat-file --batch-command`. The patch adds the `remote-object-info` command, completing the feature with all review feedback addressed.

The implementation now includes comprehensive test coverage across different transports (git://, file://, http://) and proper fallback behavior when servers lack the capability. Security measures remain robust with protocol v2 enforcement, strict response parsing, and server-side request limits. Documentation now clearly warns about future format changes when object type support is added.

All major technical issues raised during review have been resolved, including Christian Couder's feedback on grammar, test organization, and code style. The series appears ready for maintainer consideration, having addressed:
- Transport layer implementation refinements
- Test helper consolidation into lib-cat-file.sh
- Documentation warnings about future compatibility
- Final code style fixes (comment format, variable scoping, arrow operators)

Key technical details remain unchanged from previous iterations - the command still only handles object size queries (not types) and requires protocol v2. The patch maintains the series' security focus while providing the user-facing command interface. With no outstanding review feedback beyond what's already addressed, this appears to be the final version pending maintainer pickup.