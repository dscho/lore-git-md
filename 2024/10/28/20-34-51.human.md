This is a feature patch series adding remote object info support to `git cat-file --batch-command`, building on Calvin Wan's earlier protocol v2 work. The series enables querying object metadata from remote servers without full downloads, with security hardening from prior iterations now complete.

The 6-patch series refactors fetch-pack packet writing, moves fetch initialization, advertises the object-info capability, adds transport layer support, and implements the new `remote-object-info` command. Key technical points:

- Server-side security measures remain enforced (format string validation, request limits)
- Client-side parsing is simplified while maintaining safety
- Protocol v2 is required - v1 attempts error out
- Currently only supports object size queries, with objecttype handling being discussed
- Includes comprehensive tests covering security edge cases

The cover letter raises two open design questions about behavior with protocol v1 and objecttype requests. The implementation appears well-structured with thorough test coverage, building on established security foundations. The series is now functionally complete with only minor documentation and style refinements remaining before potential merging.