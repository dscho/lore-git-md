This is a review comment on the final patch of a 6-part series implementing remote object metadata queries via `git cat-file --batch-command`. Karthik Nayak provides two specific pieces of feedback:

1. **Format unification concern**: Questions the wisdom of implementing the `remote-object-info` command with a temporary default format (`%(objectname) %(objectsize)`) that will change when object type support is added, suggesting it would be better to implement `%(objecttype)` support first rather than knowingly introduce future format breakage.

2. **Code style nit**: Notes that the patch includes an incidental whitespace change (alignment of a parameter list) in code that wasn't otherwise being modified, which goes against typical Git contribution norms.

The review is narrowly focused on these two points and doesn't raise any fundamental objections to the patch's approach. The first point touches on a design decision about how to phase in functionality, while the second is a straightforward style observation. Both comments are consistent with Karthik's role as an active reviewer in the refs/transport subsystems, where this patch series primarily operates.

This feedback comes after the series has already received extensive review from Christian Couder, Jonathan Tan, and Taylor Blau, with all major technical concerns previously addressed. The current comments appear to be final polish items rather than blockers.