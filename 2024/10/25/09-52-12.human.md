This is a review comment on the first patch of the remote object info series, which implements `git cat-file --batch-command` integration for querying object metadata from remotes. Karthik Nayak points out that the `send_object_info_request()` function appears in the diff but isn't mentioned in the commit message, suggesting it may have been added unintentionally.

The observation is valid - the function is clearly part of the new remote object info functionality but wasn't called out in the commit message that focused on the packet writing refactoring. This appears to be a minor documentation oversight in an otherwise well-structured series that has already received extensive review from Christian Couder, Jonathan Tan, and Taylor Blau.

The series as a whole looks solid, with all major feedback addressed and comprehensive test coverage. This particular note seems like a small cleanup opportunity rather than anything blocking - the function is clearly needed for the feature and its presence makes technical sense, it just wasn't properly documented in the commit message. The maintainer will likely either ask for a commit message tweak or make the adjustment themselves when applying the series.