This is a review follow-up in the remote object info series, focusing on documentation and test improvements for the final patch implementing the `remote-object-info` command in `git cat-file --batch-command`.

Christian Couder provides several small but important suggestions to strengthen the patch:

1. **Documentation warning**: He suggests making the warning about future format changes more prominent, recommending an all-caps "WARNING" notice that the default format (`%(objectname) %(objectsize)`) will change when object type support is added, advising users not to rely on the current format in scripts.

2. **Test helper unification**: Notes that some test functions (`echo_without_newline` and related) are copied verbatim from t1006-cat-file.sh and suggests adding a TODO comment about possibly factoring these out into a shared library file (e.g., `lib-cat-file.sh`).

3. **Minor wording tweak**: Suggests changing "print the info" to "printing the info" for grammatical consistency in the command description.

The review indicates the series is otherwise in good shape, with only these minor documentation and test organization nits remaining. Christian mentions finding one additional issue in patch 4/6 (not shown here) but considers these non-blocking. The feedback continues the pattern of thorough, detail-oriented review that has characterized this series' development.

Key technical context from the parent summary remains relevant: the `remote-object-info` command allows querying object sizes from remotes without full downloads, currently supports only size queries (not types), enforces protocol v2, and has comprehensive transport protocol testing. The documentation warning about format changes reflects the known limitation that object type support is planned but not yet implemented.