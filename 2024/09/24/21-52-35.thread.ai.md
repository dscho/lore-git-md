Thread summary:

Ron Ziroby Romero is implementing JSON output for `git log`, with the discussion having progressed from initial philosophical concerns about non-UTF-8 data handling to concrete implementation decisions. Junio Hamano has now provided authoritative guidance on the two key technical choices: (1) strongly recommending `--pretty=json` over `--json` since the flag fits naturally with `git log`'s existing format mechanism (contrasting with `git status --json` which lacks such infrastructure), and (2) advising against modifying `pretty.c` with JSON-specific conditionals, instead suggesting a separate `json-log.c` implementation since the pretty-printing infrastructure is designed for human-readable output with whitespace handling rather than machine-parseable JSON. Earlier concerns about encoding issues (raised by demerphq regarding Git's handling of non-UTF-8 data) have been resolved, leaving these implementation mechanics as the focus. Romero appears positioned to proceed with a formal proposal incorporating Hamano's design decisions, which carry significant weight as maintainer input. This work connects to broader JSON output efforts in Git (like Patrick Steinhardt's `git status --json`), but Hamano's response establishes that command-specific context should determine the interface approach rather than seeking consistency at the cost of natural integration with existing mechanisms. The thread shows clear technical direction from the maintainer on both user-facing design and implementation architecture.