This thread discusses adding JSON output support to Git commands, starting with `git log`. Ron Ziroby Romero is leading the implementation and has moved from initial discussions about encoding concerns to concrete technical questions about the best approach.

The key open questions are architectural: whether to modify the existing pretty-printing code in `pretty.c` or create a separate `json-log.c` implementation, and whether to use `--json` (following Patrick Steinhardt's similar work on `git status`) or `--pretty=json` as the flag name. Earlier in the thread, demerphq argued that UTF-8 encoding transformations could address concerns about Git's handling of non-UTF-8 data in refs and commit messages.

The discussion appears to be progressing toward implementation, with the reference to Patrick's parallel work on `git status` suggesting this may be part of a broader effort to add JSON output across Git commands. The next steps will likely focus on resolving the implementation approach and flag naming consistency.