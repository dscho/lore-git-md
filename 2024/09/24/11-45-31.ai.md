This is a review comment in the ongoing thread about refactoring fetch-pack's packet writing to support remote object info queries. Christian Couder provides feedback on Eric Ju's patch that generalizes the capability writing function.

Christian agrees with the refactoring approach but suggests two improvements to the patch: (1) mentioning in the commit message that further generalization (like moving the function to connect.c) could be done in a future series, and (2) following Junio Hamano's style suggestion to reorder the function parameters and adjust the pointer syntax. The suggested change would place the `command` parameter before `server_options` and attach the `*` to the type (`char *command` rather than `char* command`).

The review is focused and constructive, addressing both documentation and code style while acknowledging the patch's correctness. Christian's feedback aligns with the thread's technical direction while maintaining Git's coding conventions. This is a mid-thread review comment that helps polish the implementation without introducing new functionality or controversy.