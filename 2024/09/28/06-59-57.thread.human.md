Here's a human-readable summary of the thread's current state:

The discussion about deprecating `--committer-date-is-author-date` in `git-am` and `git-rebase` has evolved from a documentation update into a deeper examination of the option's fundamental validity. What began as Kristoffer Haugsbakk's patch to clarify warnings has now uncovered serious questions about whether the original 2009 design - particularly for `git-am` - violates Git's timestamp ordering assumptions.

Johannes Sixt's latest response crystallizes two key points: first, that the original use case of reapplying patches to generate identical commit IDs was conceptually flawed (a mailbox shouldn't serve as commit storage), and second, that documentation warnings need clearer phrasing - either a blunt "Do not use" or a standalone WARNING notice. This builds on earlier feedback from Phillip Wood and SZEDER Gábor about timestamp ordering issues.

The thread now faces a pivot point: while there's consensus to deprecate the option for `git-rebase`, the `git-am` case requires resolving whether the underlying functionality should be preserved (with stronger warnings) or removed entirely. Kristoffer's documentation updates are on hold pending input from experts familiar with Git's object model invariants and the original 2009 design decisions. Junio Hamano's perspective may prove decisive in determining how to proceed.