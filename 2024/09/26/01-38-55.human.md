This is a small refactoring patch in the `git cat-file --batch-command` remote object info series. It moves loop counter variable declarations inside their respective `for` loops where they are only used locally, improving code readability by limiting variable scope.

The changes are mechanical and straightforward:
- Converts three separate `int i` and `size_t i` declarations to be declared directly in their `for` loops
- Maintains the same loop logic while reducing variable visibility
- Follows Git's coding style preference for minimizing variable lifetimes

This patch was reviewed by Christian Couder and appears to be a clean-up of the code introduced in earlier patches of the series. It doesn't change any functionality but makes the code slightly more maintainable by following the principle of declaring variables in the smallest possible scope.

The patch is part of the larger remote object info feature but stands alone as a simple code hygiene improvement. It's a minor but welcome refinement to the series.