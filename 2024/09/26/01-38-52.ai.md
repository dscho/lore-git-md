This is a refactoring patch in the remote object info series, moving initialization code in `fetch-pack.c` to prepare for supporting multiple initial states. The change relocates three variable initializations from inside the `FETCH_CHECK_LOCAL` state to just before the state machine begins, maintaining identical behavior while making the code more flexible for future extensions.

The patch moves:
1. `allow_unadvertised_object_request` flag setting
2. `use_sideband` configuration
3. `args->deepen` conditional setting

These changes were suggested by Jonathan Tan and Christian Couder to support the upcoming `remote-object-info` command while keeping the existing fetch behavior intact. The diff shows the straightforward code motion with no functional changes, just structural preparation for the new feature. This is a typical preparatory patch in a larger series, ensuring the infrastructure is clean before adding new functionality.