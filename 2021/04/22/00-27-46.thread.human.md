Here's a human-readable summary of the thread:

This thread revives and completes Luke Shumaker's 2021 work to add signed commit support to git's fast-export/import pipeline, bringing it to parity with existing signed tag handling. Christian Couder has shepherded the series through five iterations, rebasing and adding preparatory cleanups along the way.

The final implementation introduces a new `--signed-commits` option mirroring `--signed-tags` behavior, with consistent modes (abort, verbatim, warn-verbatim, strip, warn-strip). The default changes from silently stripping signatures to aborting, though legacy behavior can be restored via an environment variable. The series includes comprehensive tests and documentation updates, plus memory safety fixes to commit parsing that address concerns raised by Junio.

Key reviewers Jeff King and Eric Sunshine provided positive feedback on the approach and test coverage. The implementation handles both GPG formats properly while maintaining header ordering, and shares code with tag handling via a unified `sign_mode` enum. While signature validation was deferred as a follow-up, the core functionality appears solid after multiple rounds of review and testing.

With all known issues addressed and CI passing, this well-polished series now provides complete signed commit support for the fast-import/export pipeline, closing a longstanding feature gap while maintaining backward compatibility.