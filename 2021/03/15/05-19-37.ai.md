Bugfix follow-up in the `git subtree` recursion depth thread. Johannes Schindelin points to an existing pull request (#493) that may address the mutual recursion issue between `process_split_commit()` and `check_parents()` in the shell script implementation. While he hasn't verified it against Arun Sharma's PostgreSQL reproduction case, he notes the PR's description mentions removing deep recursion.

The referenced patch series (v2 in October 2020) was a conversion effort from shell to C, which would inherently avoid the shell's recursion limits. This suggests the issue might already have a solution in progress, though it would need testing against the specific large-repo scenario Arun described (PostgreSQL's 52k-commit history with 1685 subtree commits in `src/interfaces/libpq`).

This moves the thread forward by connecting the reported issue to active development work, though the connection remains tentative until someone tests the C conversion against the reproduction case. The ball is now in Arun's court to try the proposed solution, or for the PR's author to confirm whether it addresses this specific recursion scenario. The thread now has both a clear problem description and a potential solution path to evaluate.