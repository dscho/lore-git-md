This thread is about a long-overlooked test suite fix for `t3420-rebase-autostash.sh`, originally proposed five years ago and recently resurfaced by SZEDER Gábor. The patch corrects a fragile test that used `! grep dirty file3` to check for the absence of dirty content after `git rebase --quit`; the test should instead verify that `file3` does not exist at all, using `test_path_is_missing file3`. The fix is straightforward, touching only one test case (executed under three rebase strategies) and aligning with Git’s test suite conventions.

Phillip Wood provided a substantive review, confirming the change is correct and closing the technical discussion. The patch applies cleanly to the current codebase and has no unresolved concerns. Todd Zullinger’s recent email merely points out a typo in the commit message ("Thighten" → "Tighten"), which is trivial to fix. With the typo corrected, the patch is ready for merging—no further changes or discussion are needed. The thread remains focused on this single, self-contained fix.