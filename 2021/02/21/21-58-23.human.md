This is a test infrastructure patch that follows up on the earlier file descriptor fix, now converting error reporting in `test_must_fail` to use Git's `BUG()` mechanism. Jeff King (Peff) reviews SZEDER Gábor's patch and suggests some test improvements while generally approving the change.

The patch makes `test_must_fail` use `BUG()` for parameter validation errors, bringing it in line with other test helpers. This requires adjusting two tests that verify the error handling, since `BUG()` writes to a different file descriptor (fd 7) than the tests were expecting. Peff proposes an alternative approach using Git's sub-test framework that would be more robust and readable, avoiding direct file descriptor manipulation in the tests themselves.

While the technical solution is sound, Peff notes the test changes reveal some awkward quoting and suggests using Git's `$SQ` helper for single quotes. He also points out similar opportunities to improve other test helpers like `test_bool_env`. The discussion shows how even small test infrastructure changes can have subtle interactions with Git's error reporting mechanisms.

This is part of an ongoing effort to make test failures more consistent and debuggable by using Git's standard error reporting paths. The change is well-contained and builds on the earlier file descriptor fix, showing careful attention to how test infrastructure pieces fit together.