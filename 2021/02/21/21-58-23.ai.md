This is a test infrastructure patch that continues the discussion about error reporting in test helper functions, specifically focusing on converting `test_must_fail` to use BUG() for parameter validation errors. The thread has already established consensus on the underlying file descriptor handling (switching from fd 7 to fd 6), and now moves to the higher-level question of error reporting style.

Jeff King (Peff) reviews SZEDER Gábor's proposal to use BUG() in `test_must_fail`, noting that while he doesn't strongly object, the change isn't critically important since test failures would be caught regardless. He suggests an improved testing approach using Git's sub-test framework (`run_sub_test_lib_test_err`) to verify the BUG() output, which would be more robust than the current direct file descriptor manipulation. The suggested approach would also make the tests more readable by avoiding complex shell quoting.

The patch touches test helper validation logic in `test-lib-functions.sh` and its test cases in `t/t0000-basic.sh`. Key technical points include the interaction between BUG()'s error reporting (which uses specific file descriptors) and test framework expectations. Peff's review indicates this is part of a broader effort to modernize error reporting in the test suite, following similar changes elsewhere (like `test_bool_env`).

While the core technical solution is sound, the discussion highlights opportunities to improve test readability and robustness through the sub-test framework. The change appears non-controversial and well-scoped, with Peff offering constructive suggestions rather than blocking objections. This continues the thread's trajectory toward more maintainable test infrastructure while preserving all existing functionality.