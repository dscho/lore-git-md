This is a test infrastructure patch from Jeff King (Peff) that builds on SZEDER Gábor's earlier work to modernize error reporting in test helpers. The patch refactors how test cases verify error conditions in `test_must_fail` and `test_bool_env` by using Git's sub-test framework rather than direct file descriptor manipulation.

The key change replaces the previous approach of redirecting stderr to fd 7 with `run_sub_test_lib_test_err`, which provides a cleaner way to capture and verify BUG() output. This makes the tests more robust and readable while maintaining the same validation behavior. The patch demonstrates this by converting four test cases - two for invalid bool values in `test_bool_env` and two for non-git command checks in `test_must_fail`.

Technically, the patch modifies `t/t0000-basic.sh` to use sub-tests for error verification, with each test case now running in an isolated context. The error messages are checked using simple grep patterns rather than complex shell quoting. While this adds some verbosity (50 lines changed, with 36 additions and 14 deletions), Peff argues the conceptual clarity is worth the tradeoff.

This continues the thread's progression toward more maintainable test infrastructure, following the earlier consensus on using fd 6 for stderr in test helpers. The changes align with the broader effort to adopt BUG() for parameter validation in test helpers while improving test robustness. The patch appears non-controversial and well-scoped, building on established patterns in the test suite.