This is a test infrastructure patch from Jeff King (Peff) that further refines the sub-test framework improvements discussed in the thread. The patch automatically adds `test_done` to all sub-test snippets in `t/t0000-basic.sh`, eliminating the need for explicit `test_done` calls in each test case.

The change simplifies test writing by making `test_done` implicit in sub-tests, while maintaining the same validation behavior. Peff notes this makes tests more robust by ensuring failures come from the intended assertions rather than missing `test_done`. The patch removes 45 explicit `test_done` calls while adding just one line to the helper function.

This builds on the thread's earlier work standardizing error handling and follows the established pattern of using sub-tests for isolated test contexts. The change is non-controversial and aligns with the broader effort to modernize Git's test infrastructure. The patch demonstrates good attention to detail - Peff considers and documents the hypothetical case where someone might want to test missing `test_done` behavior, showing this approach doesn't preclude such tests.