This is a test infrastructure patch from Jeff King (Peff) that further refactors the test framework's sub-test handling. The patch builds on the ongoing thread about modernizing test helper functions and error reporting, specifically following up on the adoption of `run_sub_test_lib_test_err` for better error verification.

The key change automatically adds `test_done` to all sub-test snippets in `t/t0000-basic.sh`, removing 45 explicit `test_done` calls from individual test cases. This makes the test code more maintainable by eliminating a common source of boilerplate while ensuring consistent test termination. The patch notes this is technically redundant since the tests should error out before reaching the end, but including it makes the test behavior more explicit and robust.

Technically, the modification happens in the `_run_sub_test_lib_test_common` helper function, which now appends `test_done` to every generated test script. The patch demonstrates this by removing all manual `test_done` calls from the test file's many sub-test examples while maintaining their exact same verification behavior. The change is purely mechanical - no test logic or output expectations are altered.

This continues the thread's progression toward cleaner test infrastructure, following earlier work on file descriptor handling and BUG() integration. The patch appears non-controversial and well-scoped, building on established patterns. It demonstrates Peff's characteristic attention to test robustness and maintainability improvements. The change is particularly valuable as it touches a foundational test file (`t0000-basic.sh`) that serves as both documentation and reference implementation for the test framework.