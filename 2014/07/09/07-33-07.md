# [PATCH v6 22/32] wrapper.c: wrapper to open a file, fprintf then close

| Header | Value |
|--------|-------|
| **From** | Nguyễn Thái Ngọc Duy <pclouds@gmail.com> |
| **To** | git@vger.kernel.org |
| **Date** | 2014-07-09T14:33:07+07:00 |
| **Message-ID** | [1404891197-18067-23-git-send-email-pclouds@gmail.com](https://lore.kernel.org/git/1404891197-18067-23-git-send-email-pclouds@gmail.com) |

---

**Thread**: [thread](./07-32-45.thread.md#:~:text=2014/07/09/07-33-07)

Signed-off-by: Nguyễn Thái Ngọc Duy <pclouds@gmail.com>
---
```
 cache.h   |  2 ++
 wrapper.c | 31 +++++++++++++++++++++++++++++++
 2 files changed, 33 insertions(+)

```

```diff
diff --git a/cache.h b/cache.h
index e7dd5ce..b363c00 100644
--- a/cache.h
+++ b/cache.h
@@ -1355,6 +1355,8 @@ static inline ssize_t write_str_in_full(int fd, const char *str)
 {
 	return write_in_full(fd, str, strlen(str));
 }
+__attribute__((format (printf,3,4)))
+extern int write_file(const char *path, int fatal, const char *fmt, ...);
 
 /* pager.c */
 extern void setup_pager(void);
diff --git a/wrapper.c b/wrapper.c
index bc1bfb8..9d7b9ac 100644
--- a/wrapper.c
+++ b/wrapper.c
@@ -493,3 +493,34 @@ struct passwd *xgetpwuid_self(void)
 		    errno ? strerror(errno) : _("no such user"));
 	return pw;
 }
+
+int write_file(const char *path, int fatal, const char *fmt, ...)
+{
+	struct strbuf sb = STRBUF_INIT;
+	va_list params;
+	int fd = open(path, O_RDWR | O_CREAT | O_TRUNC, 0666);
+	if (fd < 0) {
+		if (fatal)
+			die_errno(_("could not open %s for writing"), path);
+		return -1;
+	}
+	va_start(params, fmt);
+	strbuf_vaddf(&sb, fmt, params);
+	va_end(params);
+	if (write_in_full(fd, sb.buf, sb.len) != sb.len) {
+		int err = errno;
+		close(fd);
+		strbuf_release(&sb);
+		errno = err;
+		if (fatal)
+			die_errno(_("could not write to %s"), path);
+		return -1;
+	}
+	strbuf_release(&sb);
+	if (close(fd)) {
+		if (fatal)
+			die_errno(_("could not close %s"), path);
+		return -1;
+	}
+	return 0;
+}
-- 
```

1.9.1.346.ga2b5940


