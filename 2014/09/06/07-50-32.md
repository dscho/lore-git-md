# [PATCH v4 18/32] commit_lock_file(): if close fails, roll back

| Header | Value |
|--------|-------|
| **From** | Michael Haggerty <mhagger@alum.mit.edu> |
| **To** | Junio C Hamano <gitster@pobox.com>, Johannes Sixt <j6t@kdbg.org>, Torsten Bögershausen <tboegi@web.de> |
| **Date** | 2014-09-06T09:50:32+02:00 |
| **Message-ID** | [1409989846-22401-19-git-send-email-mhagger@alum.mit.edu](https://lore.kernel.org/git/1409989846-22401-19-git-send-email-mhagger@alum.mit.edu) |

---

**Thread**: [thread](./07-50-14.thread.md#:~:text=2014/09/06/07-50-32)

If closing an open lockfile fails, then we cannot be sure of the
contents of the lockfile, so there is nothing sensible to do but
delete it.  This change also leaves the lock_file object in a defined
state in this error path (namely, unlocked).

Signed-off-by: Michael Haggerty <mhagger@alum.mit.edu>
---
```
 lockfile.c | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

```

```diff
diff --git a/lockfile.c b/lockfile.c
index 716b674..213783a 100644
--- a/lockfile.c
+++ b/lockfile.c
@@ -310,8 +310,12 @@ int commit_lock_file(struct lock_file *lk)
 	if (!lk->filename[0])
 		die("BUG: attempt to commit unlocked object");
 
-	if (lk->fd >= 0 && close_lock_file(lk))
+	if (lk->fd >= 0 && close_lock_file(lk)) {
+		int save_errno = errno;
+		rollback_lock_file(lk);
+		errno = save_errno;
 		return -1;
+	}
 
 	strcpy(result_file, lk->filename);
 	/* remove ".lock": */
-- 
```

2.1.0


