# Re: [PATCHv4] sha1_file: fix iterating loose alternate objects

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | Jonathon Mah <me@jonathonmah.com> |
| **Date** | 2015-02-02T12:00:17-08:00 |
| **Message-ID** | [xmqqk300t772.fsf@gitster.dls.corp.google.com](https://lore.kernel.org/git/xmqqk300t772.fsf@gitster.dls.corp.google.com) |

---

**Thread**: [thread](./18-48-12.thread.md#:~:text=2015/02/02/20-00-17)

Jonathon Mah <me@jonathonmah.com> writes:

> ```diff
> +test_expect_success 'prune: handle alternate object database' '
> +	test_create_repo A &&
> +		(cd A &&
> +		echo "Hello World" >file1 &&
> +		git add file1 &&
> +		git commit -m "Initial commit" file1) &&
> +	git clone -s A B &&
> +		(cd B &&
> +		echo "foo bar" >file2 &&
> +		git add file2 &&
> +		git commit -m "next commit" file2 &&
> +		git prune)
> +'
> ```
>


The issue does not have much to do with introducing new path to the
cloned repository, or the original having any specific content for
that matter, so I am tempted to simplify the above to something like
this intead:

```
	test_create_repo A &&
	git -C A commit --allow-empty -m "initial commit" &&
	git clone --shared A B &&
	git -C B commit --allow-empty -m "next commit" &&
	git -C B prune

```

Thanks.


