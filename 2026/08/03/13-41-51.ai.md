This is a **follow-up discussion in a bugfix thread** targeting bash completion for `git diff`. Junio responds to SZEDER Gábor’s review of his patch, which introduced a regression for `git diff --no-index` by omitting untracked files from completion. Junio acknowledges the oversight but rejects SZEDER’s proposed fix (scanning the command line for `--no-index`) due to two edge cases: `--no-index` can appear anywhere in the command line, and the mode is automatically triggered outside a repository. Instead, he suggests a fallback to Bash-native path completion after the Git-aware logic yields no candidates, accepting a minor regression where untracked files sharing a prefix with tracked ones may be harder to complete.

The email is concise and technical, focusing on the trade-offs between correctness and implementation complexity. Junio’s alternative would preserve the primary goal of the patch (aligning `git diff` completion with `git status`/`git add`) while mitigating the `--no-index` regression, though it introduces a usability quirk for overlapping filenames. The discussion is now at a decision point: whether to adopt Junio’s fallback approach or refine SZEDER’s command-line scanning idea. No other reviewers have weighed in yet, and the patch remains in `seen` awaiting a reroll. Key details for follow-up: the fallback logic would live in `_git_diff()` in `contrib/completion/git-completion.bash`, and the regression scenario involves untracked files like `foo~` when `foo` is tracked.