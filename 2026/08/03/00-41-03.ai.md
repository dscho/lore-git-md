This is a **test infrastructure bugfix** targeting platform compatibility. Kenneth Lorber’s patch fixes a spurious failure in `t7528-signed-commit-ssh.sh` when the test suite runs under a `csh`-style shell (e.g., `tcsh`). The issue occurs because `ssh-agent` emits shell-specific initialization code based on the `$SHELL` environment variable, which propagates from the user’s login shell even though the test harness explicitly uses Bourne shell (`sh`). The fix is a one-line change: explicitly passing `-s` to `ssh-agent` forces Bourne shell syntax, ensuring consistent behavior regardless of the user’s login shell.

The patch touches only `t/t7528-signed-commit-ssh.sh` and is self-contained. The commit message clearly explains the problem, reproduces the failure, and rejects an alternative approach (clearing `$SHELL` in `test-lib.sh`) as potentially masking other similar issues. No new tests are added since the problem is in test infrastructure, not Git functionality. The change is uncontroversial and likely to be merged without debate, as it addresses a real but niche compatibility issue without side effects.

This is the initial submission, and no prior versions exist. The patch is under review but has not yet received feedback. Given the straightforward nature of the fix, it will probably attract a `Reviewed-by` or `Acked-by` from a test infrastructure maintainer (e.g., Eric Sunshine or SZEDER Gábor) rather than substantive discussion. The fix does not interact with other subsystems or in-flight topics.