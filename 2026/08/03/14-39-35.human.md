This is the final patch in Pablo Sabater’s eight-part GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch unifies the default output format for both local and remote queries, removing the temporary workaround that excluded `%(objecttype)` from remote queries.

The change is mechanical and uncontroversial: the default format string in `git cat-file` is now `%(objectname) %(objecttype) %(objectsize)` for all commands, including `remote-object-info`. Documentation is updated to reflect that `%(objecttype)` is now supported by the remote command, and the caveat about unsupported placeholders is adjusted to list only `%(objectname)`, `%(objectsize)`, and `%(objecttype)`. The protocol documentation (`gitprotocol-v2.adoc`) is also updated to clarify that the server transmits type information as a string (e.g., `"blob"`) and that values appear in the same order as the requested attributes.

Tests in `t1017` are updated to expect the new default format, verifying that `remote-object-info` and `info` commands can be mixed without breaking backward compatibility. The patch touches only documentation, the default format string in `builtin/cat-file.c`, and test expectations—no new logic or protocol changes are introduced. The series is now feature-complete, with all prior review feedback addressed and the architectural refactoring finalized. The GSoC deadline is approaching, and this patch appears ready for merging.