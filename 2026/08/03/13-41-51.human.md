This is a **bugfix follow-up** to Junio’s bash completion patch for `git diff`. The thread has identified a regression: the patch breaks completion for `git diff --no-index`, which should offer untracked files. Junio now proposes an alternative fix that avoids scanning the command line for `--no-index` (which he argues is error-prone due to edge cases like `-S` and outside-worktree invocations). Instead, he suggests falling back to Bash-native path completion if the Git-aware completion finds nothing.

The trade-off is clear: untracked files in a Git repository will be harder to complete (requiring a longer prefix to disambiguate from tracked files), but this aligns with the patch’s original goal of prioritizing Git’s index-based workflow. Junio acknowledges the regression but frames it as an acceptable compromise, given that `--no-index` is a secondary use case.

This reply advances the discussion by rejecting the previous fix and offering a new approach. The ball is now back in SZEDER Gábor’s court to either accept the trade-off or propose another solution. Expect further iteration before this lands in `seen`. No other reviewers have weighed in yet.