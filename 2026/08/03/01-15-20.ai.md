Summarizer brief mode:

Michael Montalbo replies to Junio C Hamano’s defense of the current binary-file check placement in `has_conflict_markers()` (patch 3/4 of the `git add --resolved` series). Montalbo acknowledges Junio’s point about false positives being preferable to false negatives in this context and agrees that the current order (conflict-marker check before binary check) is acceptable.

Montalbo then proposes an alternative approach to the binary-file issue: instead of relying on the heuristic `buffer_is_binary()` check, he suggests inspecting the *merge driver* used for the file. Since binary and union merge drivers cannot emit conflict markers, this could provide a more deterministic way to skip conflict-marker detection for files handled by those drivers. This approach would eliminate the need for the `buffer_is_binary()` check entirely, replacing it with a direct query of the merge driver’s capabilities. Montalbo notes that this might be conceptually clearer and less brittle than the current heuristic, though he acknowledges the potential fragility if new merge drivers are added in the future.

Key technical details:
- Montalbo’s proposal targets the `has_conflict_markers()` helper in `add.c`.
- The idea is to check the merge driver (e.g., `binary`, `union`) and return early if the driver cannot emit conflict markers.
- This would replace the current `buffer_is_binary()` heuristic with a more direct and deterministic check.
- The proposal is framed as a potential improvement to the current implementation, not a blocker for the series.
- Montalbo does not provide a patch or code snippet, so the feasibility and implementation details remain open.
- The proposal does not address Junio’s broader question about using Git’s attribute system to control conflict-marker detection (e.g., via `.gitattributes`). The two ideas are independent but could interact if both were implemented.