This is a bugfix patch follow-up in the ongoing trailer-parsing thread. Kristoffer Haugsbakk responds to Junio C Hamano’s review of the patch that stops Git from misinterpreting URLs as trailers. The exchange clarifies two points: the robustness of the `://` heuristic and the safety of the pointer arithmetic in the new code.

Kristoffer acknowledges that the `://` check can produce false positives (e.g., `1://` or `-://`), but argues that these are rare edge cases and that the current implementation is a strict improvement over the status quo. He also notes a minor documentation update needed to reflect that trailer keys can include hyphens. On the pointer arithmetic question, he confirms the code is safe due to C’s NUL-terminated strings and boolean short-circuiting, contrasting it with languages like Java where such checks would require explicit length validation.

The discussion remains narrowly focused on the trailer subsystem, with no new edge cases or broader implications introduced. The patch is now effectively ready for integration, pending any final adjustments to address the minor documentation nit. Files touched: `trailer.c` and `Documentation/git-interpret-trailers.txt`. Key names: `find_separator()`, `://` heuristic. The thread’s status is under review with maintainer approval.