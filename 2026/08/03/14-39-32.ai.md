This is the fifth patch in an eight-patch GSoC series (v3) extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch teaches the server-side object-info handler to accept "type" as a requested field, completing the end-to-end chain for object type metadata.

The implementation adds a `type` bit field to `struct requested_info` in `protocol-caps.c` and updates the server-side handler to parse the "type" keyword in client requests. When the client requests type data, the server now transmits the object type as a string (e.g., `"blob"`) alongside the existing size field. The patch also includes a test in `t5701-git-serve.sh` that verifies the server correctly handles requests for both "size" and "type", including duplicate OIDs.

The changes are narrowly scoped to the protocol layer and align with the hybrid design introduced in earlier patches: the server transmits raw strings, and the client maps them to internal enums. The patch wraps an over-long line and fixes the bit field style for `requested_info.size`, addressing minor style nits from prior reviews. The test coverage is thorough, including edge cases like duplicate OIDs and mixed field requests.

This patch is a key milestone in the series, as it enables the server to advertise and transmit type data, which the client will parse and display in subsequent patches. The implementation is well-motivated and follows the established pattern from the predecessor series, though reviewers familiar with the protocol subsystem may want to verify the error handling and protocol invariants. No merge status is implied.