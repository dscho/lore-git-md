This is a **bugfix patch** in Phillip Wood’s fixup series for the `git history squash` implementation. It targets a specific edge case: **rejecting attempts to squash down to a root commit** (a commit with no parents). The patch adds an explicit check in `resolve_squash_range()` to error out if such a commit is encountered, preventing silent data loss or unexpected behavior.

The change is small but important. The commit message explains that squashing to a root commit is not yet supported and would require explicit user confirmation to avoid surprises. The test case demonstrates the rejection by creating a disconnected root commit and verifying that `git history squash` fails with a clear error message. The test also clarifies why `--ancestry-path` complicates walking back to the root: the revision walk only includes commits descended from the given ancestry path, so a second `--ancestry-path` commit is needed to force the walk to include any commits at all.

This patch aligns with the stricter input validation introduced in v11 of Harald Nordgren’s series, which also rejects ranges reaching a root commit. Phillip’s fixup predates v11 but addresses the same concern, reinforcing the design decision to avoid root commits by default. The error message (`"cannot squash down to root commit"`) is clear and actionable, matching the project’s preference for user-friendly error handling.

The patch is **ready to apply** and includes both the code change and a corresponding test. It touches `builtin/history.c` (4 insertions) and `t/t3455-history-squash.sh` (12 insertions), introducing no new helper functions or configuration options. The test is well-explained and covers the edge case thoroughly. This is a **substantive review contribution** that improves the robustness of the squash implementation.