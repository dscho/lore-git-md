Thread identity: Bug report for `git rebase` regression where hardcoded `#` comment character in todo list advisory lines conflicts with `core.commentChar` configuration, causing sequencer parsing failures.

Core problem or goal: Fix a regression in `git rebase` where informational lines about checked-out refs in other worktrees use a hardcoded `#` comment character, ignoring the user-configured `core.commentChar`. This breaks parsing when the todo list is re-read by the sequencer, aborting the rebase with `error: invalid command '#'`. The issue affects users who customize `core.commentChar` (e.g., to `;`) and use worktrees, rendering `git rebase` unusable without workarounds.

Current status: Bug report submitted; no prior discussion or patch versions. The root cause is isolated to `sequencer.c`, where the literal `#` should be replaced with the resolved `comment_line_char`. Reproduction steps are provided, and the impact is high for affected users.

Key technical details:
- **Subsystems/files**: `sequencer.c` (todo list generation), `git rebase` command.
- **Behavior change**: Replace hardcoded `#` with `comment_line_char` in advisory lines about checked-out refs in other worktrees.
- **Trigger conditions**: `core.commentChar` set to a non-`#` value *and* a branch in the rebased range is checked out in another worktree.
- **Test coverage**: Minimal reproduction case provided; no existing test failure noted.

Open questions and loose ends:
- No proposed fix yet; the root cause suggests a straightforward change, but implementation details (e.g., how `comment_line_char` is resolved in this code path) are unconfirmed.
- No maintainer or reviewer feedback yet; likely to attract attention from sequencer/rebase maintainers (e.g., Phillip Wood) and worktree contributors.

Participant positions: None yet; thread root authored by Markus Geiger.

Related work: None mentioned. The issue is self-contained and not tied to larger efforts (e.g., ODB abstraction, Rustification).