This is a surface-level follow-up to the first patch in Pablo Sabater’s GSoC v3 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder. Pablo acknowledges Junio’s observation that the new version preserves the exit status of commands run inside `$(...)` within a here-document—unlike the old `wc -c | xargs` pipeline, which would have masked failures (e.g., a segfault in `git rev-parse`). The change remains a purely mechanical, uncontroversial test cleanup: replacing `wc -c | xargs` with `test_file_size()` and caching `git rev-parse` output in variables for robustness and portability. No behavior change is introduced, and the patch is ready to proceed as-is. The follow-up is a courtesy to update the commit message; no code changes are needed.