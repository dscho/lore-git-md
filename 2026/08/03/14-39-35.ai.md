This is the final patch in Pablo Sabater’s eight-part GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch unifies the default output format for all commands—local and remote—to `%(objectname) %(objecttype) %(objectsize)`, removing the temporary workaround that excluded `%(objecttype)` from remote queries. It updates the documentation to reflect the new support and adjusts the test suite to expect the unified format across all transport protocols (git://, file://, http://).

The change is mechanical and uncontroversial: the server-side and client-side logic for object type support was already implemented in earlier patches, and this patch simply removes the conditional format override in `builtin/cat-file.c`. The documentation updates clarify that `%(objecttype)` is now supported in remote-object-info and remove the warning about future format changes. Tests are updated to verify that mixing `info` and `remote-object-info` commands produces consistent output, confirming backward compatibility.

The patch touches `Documentation/git-cat-file.adoc`, `Documentation/gitprotocol-v2.adoc`, `builtin/cat-file.c`, and `t/t1017-cat-file-remote-object-info.sh`. No new symbols are introduced; the focus is on removing the temporary format exception and ensuring the default format is uniform. The series is now feature-complete, with all prior review feedback addressed and the architectural refactoring finalized. The GSoC deadline is imminent, and this patch is the last piece needed for the feature to be merge-ready.