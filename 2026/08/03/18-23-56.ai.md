This is a review reply in the GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The thread is at v3, feature-complete, and this email is Junio’s surface-level review of patch 3/8, a mechanical refactoring that removes the `struct object_info_args` wrapper and passes its three pointer members directly to `fetch_object_info()` and `send_object_info_request()`.

Junio’s feedback is brief and purely surface-level: he notes that the patch is straightforward despite its size, describing the change as losing the intermediate wrapper and updating member references to direct variable access. There are no technical concerns, no questions about behavior, and no requests for follow-up. The review does not engage with the architectural shift toward `struct fetch_object_info_results` (introduced in patch 4/8) or the broader series goals. It is a simple acknowledgment of the mechanical nature of the refactoring, consistent with the patch’s stated intent of no behavior change.

The patch itself touches `fetch-object-info.c`, `fetch-object-info.h`, and `transport.c`, updating function signatures and call sites. The change is part of the v3 architectural cleanup that replaces `struct object_info` with a dedicated results struct, and this patch specifically eliminates the now-redundant grouping struct to clarify the API surface. No further action is implied by Junio’s reply.