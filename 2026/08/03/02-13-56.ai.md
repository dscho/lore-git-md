This is a **test infrastructure bugfix** for platform compatibility, now with a **substantive review** from brian m. carlson.

The patch remains a one-line change in `t/t7528-signed-commit-ssh.sh`, replacing `ssh-agent` with `ssh-agent -s` to force Bourne shell syntax regardless of the user’s login shell (`$SHELL`). brian’s review confirms the fix is correct: POSIX defines `$SHELL` as the user’s preferred interpreter, but the test suite always expects POSIX `sh` output, so `-s` is the appropriate flag. The review also audits the codebase and finds no other instances of `ssh-agent`, meaning this is the only place the issue occurs.

No further discussion or objections are present; the patch is uncontroversial and ready for an `Acked-by` or `Reviewed-by`. The fix is self-contained, touches only test infrastructure, and has no impact on Git’s core functionality or on-disk formats.