This is a **substantive review** of Harald Nordgren’s `git history squash` patch (v10 3/5), posted by Phillip Wood as a series of seven fixup commits. The review targets the core implementation’s robustness, edge-case handling, and code structure, and arrives just as v11 of the series has been posted.

Phillip’s feedback focuses on **input validation, merge commit handling, ref safety, and message generation**—areas where v10 fell short. His seven fixups address:

1. **Option parsing cleanup** – minor hygiene, no behavior change.
2. **Range validation** – now checks for `BOTTOM` (root) commits rather than `UNINTERESTING` flags, tightening the logic.
3. **Root commit rejection** – explicitly blocks ranges that reach a root commit, preventing ambiguous base resolution.
4. **Single-tip enforcement** – rejects ranges with more than one tip, ensuring the folded history has a clear endpoint.
5. **Ref safety** – refuses to squash if any **local branches** (not just the current branch) point to commits inside the range, closing a gap in v10’s ref-handling.
6. **Message preservation** – ensures `amend!` and `squash!` commit messages are never discarded, even when `--no-edit` is used.
7. **Merge commit handling** – fixes a bug in v10’s merge squashing logic, which could produce incorrect results for interior merges.

The most significant changes are in **patches 5–7**, which improve safety and correctness. Phillip’s ref-handling fix (patch 5) aligns with Junio’s earlier guidance to protect all local branches, not just the current one. His message-preservation fix (patch 6) addresses a usability gap where `amend!` messages were silently dropped, and the merge fix (patch 7) resolves a correctness issue in v10’s implementation.

Phillip notes that **v11 already incorporates some of these fixes** (e.g., root commit rejection, single-tip enforcement), but his ref-handling and message-preservation improvements go further. His work also restructures the code to use a **single pass over the commit range**, improving efficiency and clarity compared to v10’s multi-pass approach.

The review is **well-timed but slightly stale**: v11 was posted while Phillip was preparing these fixups, so his patches are based on v10. However, the remaining gaps (especially ref safety and message preservation) are still relevant. His branch (`wip/hn/history-squash/v10-early-part`) is available for Harald to cherry-pick or rebase.

This is a **high-quality, tested review**—Phillip’s changes are small, focused, and address real edge cases. The fixes would strengthen v11 if integrated, particularly the ref-handling and message-preservation improvements. The review also underscores the series’ maturity: most of the heavy lifting is done, and the remaining work is refinement.