This is a refactoring patch in Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder. The patch removes the `struct object_info_args` wrapper and passes its three pointer members directly to `fetch_object_info()` and `send_object_info_request()`. The change is purely mechanical—no behavior change is intended—and is part of the v3 architectural shift toward a dedicated `struct fetch_object_info_results` (introduced in the next patch).

Junio’s reply is a surface-level review: he notes the patch is straightforward and correctly updates all call sites to reference the variables directly instead of the now-removed struct. There are no technical concerns or merge-blocking issues raised; the feedback is purely descriptive. The patch is ready to proceed as-is.