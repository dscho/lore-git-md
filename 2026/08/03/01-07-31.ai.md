This is a follow-up to Junio’s bash completion bugfix for `git diff`. The patch itself remains unchanged from the initial posting, but Junio is clarifying and expanding the commit message to explicitly state that the new behavior—omitting untracked paths from completion suggestions—applies even when `git -C` is not used. This is a positive change: the original commit message focused on the `git -C` case, but the fix is broader and improves consistency across all invocations of `git diff` completion.

The added paragraph makes it clear that the patch is not just a narrow fix for a corner case but a general improvement to the user experience. By aligning `git diff` completion with the behavior of `git status` and `git add`, the patch eliminates a long-standing inconsistency that has confused users for years. Junio’s decision to queue this on `seen` (rather than waiting for further review) reflects his confidence in the patch’s correctness and low risk profile.

No new code or tests are introduced in this email; it is purely a documentation update. The patch still touches only `contrib/completion/git-completion.bash` and `t/t9902-completion.sh`, and the four new tests continue to cover the key scenarios (local and `git -C` invocations, with and without `--`). The expanded commit message reinforces the patch’s motivation and makes it easier for future readers to understand why the change was made.

Given Junio’s maintainer status and the patch’s narrow scope, this is likely to graduate to `next` and then `master` without further discussion. The only remaining step is for Junio to pick it up in his next "What's cooking" report.