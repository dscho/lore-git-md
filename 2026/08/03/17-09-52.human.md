This is a contributor housekeeping patch. Christian Couder updates his `.mailmap` entry to reflect that his primary email address is now `christian.couder@gmail.com` (the one he uses to send patches) rather than the older `chriscool@tuxfamily.org`. The change swaps the two addresses in the existing entry so both still map to him, but the Gmail address becomes the canonical one.

The patch is trivial—one line changed in `.mailmap`—and the rationale is clear: the author wants the address he actually uses to be the primary one. No controversy or technical risk here; it’s a routine maintenance update. If you’ve seen Christian’s name in commit logs or mailing list traffic, this patch ensures future `git shortlog` or `git blame` output will show the Gmail address by default.