```
Thread identity: "What's cooking in git.git" report for August 2026 (#01), authored by Junio C Hamano (maintainer). Thread type: integration status report.

Core problem or goal: Provide a snapshot of all in-flight topics at the start of August 2026, detailing which topics graduated to `master`, which are cooking in `next`, which are new in `seen`, and which are stalled awaiting review or reroll.

Current status:
- 13 topics graduated to `master` (10th batch of the cycle, irreversible for the next release).
- 6 topics are new in `seen` (awaiting review), including `kh/trailers-no-urls` (blocked on `kh/doc-trailers` dependency).
- 23 topics are cooking in `next` (stable, candidate for next release).
- 22 topics are stalled (waiting for response or reroll).
- No topics are explicitly marked as "Will merge to 'next'" or "Will discard" in this report, except `hn/checkout-track-fetch` (marked "Will discard" by Junio).

Key technical details:
Graduated to `master`:
- Fixes: segfault in `git clone --revision` (protocol-v0), memory leaks in `rewrites_release()`, tempfile/lockfile `the_repository` dependency, `git cat-file --batch-command` segfault when requesting `contents` without `type`, macOS TRE memory leak on invalid UTF-8, reachability bitmap traversal for object at position zero, sparse-index crash with intent-to-add entries outside cone, `git mv` destination path validation, `git rebase` fixup/squash bugs (incorrect commit count, editor not opening), `git branch -d` bisect warning, `git merge` memory leak, `git checkout -m` autostash fallback refinement.
- Features: `url.*.insteadOf` resolution for submodule URLs, `@{push}` tracking using fetch refspec of matching remote, `--exclude-first-parent-only` correctness, `core.configLockTimeout`, `git cat-file --batch-command remote-object-info` (with `%(objecttype)` placeholder), loose-object writing logic moved to `odb/source-loose.c`, Swift userdiff patterns, `gitweb` object-ID shortening with file modes, `git stash push` sparse-index avoidance, migration of `excludes_file`, `editor_program`, `pager_program`, `askpass_program`, `push_default`, and `trust_executable_bit` into `struct repo_config_values`.
- Refactoring: loose-object writing logic moved to `odb/source-loose.c`, tempfile/lockfile APIs no longer depend on `the_repository`.

New in `seen`:
- CI: GitHub Actions PR workflows cancel stale runs.
- Documentation: ref-migration warning moved under `git refs migrate`, `linkgit` reference fix, `git interpret-trailers` overhaul (key format, comment-line treatment; `kh/doc-trailers` series, still under review), `git replay` config cross-references.
- Fixes: path completion for `git diff` with tracked files in subdirectories, `t7528` for csh-like shells, trailer recognition for lines starting with `<token>://` (`kh/trailers-no-urls` series, blocked on `kh/doc-trailers`).

Cooking in `next`:
- Features: `git add --resolved` (stage conflict-resolved paths, abort on leftover conflict markers), `git repo info` extended with path keys (absolute/relative), `git branch --delete-merged`, `git bisect --reset-when-found[=<where>]`.
- Fixes: `git cat-file --batch-command` segfault fix, `git merge` memory leak, macOS TRE memory leak workaround, `git checkout -m` autostash fallback refinement, `git mv` destination path validation, `git rebase` fixup/squash bugs, `git branch -d` bisect warning, sparse-index crash with intent-to-add entries, ODB creation pluggability, `git cat-file --batch-command remote-object-info` type support.
- Documentation: `git interpret-trailers` overhaul (dependency of `kh/trailers-no-urls`), `git replay` config cross-references, synopsis-style conversion for `git format-patch`, `git imap-send`, `git send-email`, `git request-pull`.
- Test infrastructure: `commit_body()` test helper, `t0014` dynamic deprecated commands query, static-analysis CI image bump to Ubuntu 24.04 (Coccinelle performance fix).
- Refactoring: ODB creation pluggability, loose-object map loading detanglement.

Stalled (awaiting response/reroll):
- Fixes: `t7412` modernization, `git repack --geometric --cruft` (bare `grep !` remains), `git checkout --track` fetch mode (marked "Will discard"), path completion dotfile hiding, `git rebase --continue --[no-]edit`, macOS fsmonitor cookie flush, `git rebase --update-refs` symref resolution, `git last-modified` Bloom filter optimization, Coverity unchecked returns, `git subtree` Rust config rejection, `TRACE2_ANCESTRY` prerequisite refinement, `git apply` abandoned header fix, `libgit.a` subdir move (ejected due to evil merges), `git log -L` limited ops, `git multi-pack-index write --incremental --base` fix, `git replay --linearize`, external diff hunk provider, `git commit --fixup` options (`-m`, `-F`, `-c`, `-C`), `git branch --delete-merged`, merge-base computation optimization.
- Features: `git bisect --reset-when-found[=<where>]`, `git rebase --autosquash --empty` (ejected due to conflicts), `git fetch` submodule errors config (`fetch.submoduleErrors`).

Open questions and loose ends:
- `tb/repack-geometric-cruft`: squash commit still contains a bare `grep !`; needs reroll.
- `ps/libgit-in-subdir`: ejected due to evil merges; unclear if/when it will be resurrected.
- `fz/rebase-autosquash-empty`: ejected due to conflicts; unclear if/when it will be resurrected.
- `hn/checkout-track-fetch`: marked "Will discard"; may be dropped unless rerolled with justification.
- `za/completion-hide-dotfiles`: no response to review comments; may be dropped.
- `hs/rebase-continue-edit`: no response to review comments; may be dropped.
- `tc/last-modified-bloom`: no response to review comments; may be dropped.
- `mm/lib-httpd-cgi-safe`: no response to review comments; may be dropped.
- `mm/diff-process-hunks`: no response to review comments; may be dropped.
- `ec/commit-fixup-options`: no response to review comments; may be dropped.
- `kh/trailers-no-urls`: blocked on `kh/doc-trailers` (still under review); author plans minor refinements (commit message, code consistency) but will wait for feedback on dependency before advancing.

Participant positions:
- Junio C Hamano (maintainer): authored the report, implicitly approving all topics graduated to `master` and those cooking in `next`. Explicitly marked `hn/checkout-track-fetch` as "Will discard". Suggested `kh/trailers-no-urls` is ready for `next` but deferred due to dependency.
- Kristoffer Haugsbakk (author of `kh/trailers-no-urls` and `kh/doc-trailers`): clarified that `kh/trailers-no-urls` cannot advance until `kh/doc-trailers` is ready; plans minor refinements but will wait for feedback on the dependency.
- Jeff King (Peff): reviewed earlier iterations of `kh/trailers-no-urls` (implied by prior discussion references, though no new feedback in this email).
- Reviewers/approvers for graduated topics: not explicitly named in this report (assumed via prior Reviewed-by/Acked-by trailers).
- Reviewers/approvers for cooking topics: not explicitly named in this report (assumed via prior Reviewed-by/Acked-by trailers).
- Stalled topics: awaiting responses from authors (e.g., Taylor Blau for `tb/repack-geometric-cruft`, Toon Claes for `tc/last-modified-bloom`, Han-Wen Nienhuys for `hn/bisect-reset-when-found`, Johannes Schindelin for `js/coverity-unchecked-returns-fix`, etc.).

Related work:
- ODB abstraction effort: `ps/odb-move-loose-object-writing`, `ps/odb-make-creation-pluggable`, `ps/cat-file-remote-object-info`, `ps/cat-file-remote-object-info-type`.
- `the_repository` removal: `rs/tempfile-wo-the-repository`.
- Documentation synopsis-style conversion: `ja/doc-synopsis-style-yet-more`.
- Reftable backend: `kh/doc-refs-migrate-limitations`.
- Rustification: no direct mention, but stalled topics may intersect (e.g., `ij/subtree-reject-v2-config`).
- Test modernization: `sk/test-commit-body-helper`, `bl/t7412-use-test-path-helpers`, `jk/t0014-dynamic-deprecated-cmds`.
```