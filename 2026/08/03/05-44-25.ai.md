This is a **substantive review** of Junio’s bash completion bugfix for `git diff`. SZEDER Gábor identifies a regression edge case: the patch breaks completion for `git diff --no-index`, which *should* offer untracked files (since `--no-index` explicitly operates outside Git’s index). His proposed fix is surgical: wrap both calls to `__git_complete_index_file()` in a check for `--no-index` on the command line, using the existing `__git_find_on_cmdline` helper.

The review is technically precise, naming the exact function (`__git_find_on_cmdline`), the exact flag (`--no-index`), and the exact consequence (untracked files disappearing from completion). It also explains why the regression matters: `--no-index` is a legitimate workflow for comparing arbitrary files, and users expect it to behave like a non-Git diff tool. SZEDER’s suggestion is minimal and follows the existing code style, so it should be easy to integrate.

This is the first substantive feedback on the patch, and it surfaces a real-world scenario that the original tests did not cover. The fix is likely to be uncontroversial, but it will require a reroll to add the `--no-index` guard and a new test case. Given Junio’s maintainer status and the narrow scope, expect a v2 soon.