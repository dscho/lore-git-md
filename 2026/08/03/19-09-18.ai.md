This is Junio’s "What’s cooking in git.git" report for August 2026 (#01), summarizing the state of all in-flight topics at the start of the month.

The report lists 64 topics, of which 10 have just graduated to `master` (the “10th batch” mentioned in the opening).  The rest are split between `next` (cooking), `seen` (new or stalled), and a few that are still waiting for review or a reroll.  Below are the key technical details a follow-up AI session would need to track discussion:

**Graduated to `master` (irreversible for the next release):**
- `af/clone-revision-v0-segfault-fix` – fixes a segfault in `git clone --revision` when talking to a protocol-v0 server.
- `en/submodule-insteadof-remote-match` – resolves `url.*.insteadOf` aliases before matching submodule URLs to configured remotes.
- `hn/url-push-tracking` – when pushing to a URL, `git` now uses the fetch refspec of the uniquely matching configured remote to update the remote-tracking branch (e.g., `@{push}`).
- `jc/exclude-first-parent-seen` – `--exclude-first-parent-only` now correctly stops after the first parent even if it was already marked `SEEN`.
- `jc/remote-insteadof-leakfix` – plugs memory leaks in `rewrites_release()`.
- `jt/config-lock-timeout` – adds `core.configLockTimeout` to retry acquiring `config.lock`.
- `ps/cat-file-remote-object-info` – adds `remote-object-info` to `git cat-file --batch-command`; clients can request object metadata (size) from a remote server without downloading the object.
- `ps/odb-move-loose-object-writing` – moves loose-object writing logic from `object-file.c` to `odb/source-loose.c`, making the loose backend more self-contained.
- `rs/tempfile-wo-the-repository` – tempfile and lockfile APIs no longer depend on `the_repository`; callers now use repository-aware variants.
- `sk/userdiff-swift` – adds userdiff patterns for Swift.
- `tl/gitweb-shorten-hashes-with-modes` – fixes object-ID shortening in `gitweb` when index lines carry trailing file modes.
- `tn/stash-avoid-sparse-index-expansion` – `git stash push` avoids sparse-index expansion for in-cone pathspecs.
- `ty/migrate-excludes-file` and `ty/migrate-trust-executable-bit` – migrate `excludes_file`, `editor_program`, `pager_program`, `askpass_program`, `push_default`, and `trust_executable_bit` into `struct repo_config_values`.

**New topics (in `seen`, needs review):**
- `hn/ci-cancel-stale-pr-runs` – GitHub Actions CI workflows triggered by pull requests now cancel older runs on new pushes to the same PR.
- `kh/doc-refs-migrate-limitations` – moves the ref-migration warning under the `migrate` subcommand in the `git refs` documentation and fixes a `linkgit` reference.
- `jc/complete-diff-tracked-paths` – fixes path completion for `git diff` when the path starts with a tracked file inside a subdirectory.
- `kh/trailers-no-urls` – stops recognizing lines starting with `<token>://` as trailers.
- `kl/t7528-ssh-agent-for-csh-users` – fixes `t7528` for csh-like shells by forcing Bourne syntax with `ssh-agent -s`.

**Cooking in `next` (stable, candidate for next release):**
- `kh/doc-trailers` – overhauls `git interpret-trailers` documentation: defines key format (alphanumeric + hyphens), replaces outdated terminology, and documents comment-line treatment.
- `kh/doc-replay-config` – adds cross-references to `git replay` configuration variables.
- `sk/test-commit-body-helper` – introduces `commit_body()` test helper and updates tests to use it.
- `jk/cat-file-batch-wo-type-fix` – fixes a segfault in `git cat-file --batch-command` when requesting `contents` without `type`.
- `tc/merge-default-to-upstream-leakfix` – fixes a memory leak in `git merge` when run without arguments.
- `cl/regexec-macos-leak` – works around a macOS TRE memory leak on invalid UTF-8 by segmenting the input buffer at invalid byte boundaries.
- `dl/pack-bitmap-position-zero` – fixes reachability bitmap traversal to handle the object at position zero.
- `hn/checkout-m-autostash-refine` – refines `git checkout -m` autostash fallback to retry only when there are local changes and adds a blank line to separate conflict advice.
- `jk/t0014-dynamic-deprecated-cmds` – makes `t0014` dynamically query deprecated commands to avoid failures with stale executables.
- `js/mingw-symlink-net-share-leak` – skips symlink-type auto-detection for network-share targets on Windows to prevent NTLM credential leaks.
- `ns/merge-base-is-ancestor-tests` – adds tests for `git merge-base --is-ancestor`.
- `jc/add-resolved` – adds `git add --resolved` to stage conflict-resolved paths while leaving unrelated local changes unstaged; aborts if leftover conflict markers are found.
- `lo/mv-missing-dest-dir-check` – makes `git mv` reject a destination whose leading path is missing or a symlink, and improves error messages.
- `ja/doc-synopsis-style-yet-more` – converts `git format-patch`, `git imap-send`, `git send-email`, and `git request-pull` documentation to the modern synopsis style.
- `kj/repo-info-more-path-keys` – extends `git repo info` with keys for `path.git-prefix`, `path.grafts`, `path.index`, `path.hooks`, `path.objects`, `path.superproject-working-tree`, and `path.toplevel`, supporting both absolute and relative paths.
- `pw/rebase-fixup-fixes` – fixes two bugs in `git rebase`: incorrect commit count in the template message when skipping multiple fixup/squash commands, and editor not opening when the final command in a chain containing `fixup -c` is skipped.
- `rs/branch-delete-bisect-warning` – makes `git branch -d` report when a branch cannot be deleted because it is being used in an active bisect run.
- `jk/ci-static-analysis-image-bump` – bumps the static-analysis CI image to Ubuntu 24.04, bringing in a newer Coccinelle that resolves a severe performance regression.
- `jk/diff-relative-cached-unmerged-more` – cleans up the relative-path code in diff-lib and adds a sanity check to `oneway_diff()`.
- `ds/sparse-index-ita-crash` – fixes a crash in sparse-index collapse when encountering an intent-to-add entry outside the cone.
- `ps/odb-make-creation-pluggable` – makes creation of on-disk ODB structures pluggable; defers ODB initialization and detangles loose-object map loading.
- `ps/cat-file-remote-object-info-type` – extends `remote-object-info` to support the `%(objecttype)` placeholder.

**Stalled (waiting for response or reroll):**
- `bl/t7412-use-test-path-helpers` – modernizes `t7412` to use `test_path_is_*` helpers.
- `tb/repack-geometric-cruft` – teaches `git repack` to combine `--geometric` with `--cruft`; includes a squash commit that still contains a bare `grep !`.
- `hn/checkout-track-fetch` – extends `git checkout --track` with a `fetch` mode to refresh the start-point; marked as “Will discard.”
- `za/completion-hide-dotfiles` – makes path completion hide dotfiles by default unless the path starts with a dot.
- `hs/rebase-continue-edit` – adds `--[no-]edit` to `git rebase --continue`.
- `td/fsmonitor-darwin-cookie-flush` – flushes pending FSEvents before waiting for the cookie file on macOS.
- `sn/rebase-update-refs-symrefs` – makes `git rebase --update-refs` resolve symrefs to their referents before queuing updates.
- `tc/last-modified-bloom` – optimizes `git last-modified` by reusing revision walk filtering logic and maintaining per-path Bloom filters even with wildcard pathspecs.
- `hn/bisect-reset-when-found` – adds `--reset-when-found[=<where>]` to `git bisect` to automatically run `git bisect reset` when done.
- `js/coverity-unchecked-returns-fix` – fixes unchecked returns in `bisect`, `transport-helper`, `compat/pread`, `last-modified`, `reftable` tests, `reftable/block`, `config`, and `http`.
- `ds/trace2-tolerate-failed-timestamp` – makes `trace2` tolerate failures from `gettimeofday()` and datetime formatting.
- `ij/subtree-reject-v2-config` – makes the shell `git subtree` bail out if it detects the Rust implementation’s configuration file.
- `jm/t0213-skip-emulated-ancestry-tests` – refines the `TRACE2_ANCESTRY` prerequisite to skip ancestry tests under user-mode emulation.
- `zy/apply-abandoned-header-fix` – isolates candidate `git diff` headers in a temporary structure to avoid leaking state.
- `ps/libgit-in-subdir` – moves `libgit.a` sources into a new `lib/` directory; ejected due to evil merges.
- `mm/line-log-limited-ops` – teaches `git log -L` to limit `--stat`, `--check`, and `-G` to the specified range and path.
- `tb/midx-incremental-custom-base` – fixes `git multi-pack-index write --incremental` to honor `--base`.
- `tc/replay-linearize` – adds `--linearize` to `git replay` to drop merge commits.
- `mm/diff-process-hunks` – introduces `diff.<driver>.process` to let an external process act as a hunk provider.
- `ec/commit-fixup-options` – allows `-m`, `-F`, `-c`, or `-C` for all `git commit --fixup` variations.
- `hn/branch-delete-merged` – adds `--delete-merged` to `git branch` to remove local branches already merged into their tracked remote-tracking branches.
- `kk/merge-base-exhaustion` – optimizes merge-base computation by stopping early when one side’s exclusive commits are exhausted.

**Other notable topics:**
- `tb/pack-with-duplicates` – hardens handling of packfiles with duplicate object entries: reverse index lookup, delta cycle recovery, MIDX verification, and pack reuse.
- `tb/send-pack-no-ref-delta` – teaches `git send-pack` to honor the `no-ref-delta` capability.
- `tn/packfile-uri-concurrency` – makes concurrent packfile-URI and dumb-HTTP downloads safer by avoiding concurrent appends to the staging file.
- `js/pack-objects-delta-size-t` – widens delta-related fields to `size_t` to avoid truncation on 64-bit Windows.
- `pz/fetch-submodule-errors-config` – adds `fetch.submoduleErrors` to make submodule fetch errors non-fatal.
- `gr/add-e-use-apply-api` – refactors `git add -e` to use the internal apply API directly.
- `fz/rebase-autosquash-empty` – teaches `git rebase --autosquash` to honor `--empty` when a fixup/squash empties its target; ejected due to conflicts.
- `mm/lib-httpd-cgi-safe` – makes CGI helpers use atomic filesystem operations to prevent race conditions under concurrent Apache requests.