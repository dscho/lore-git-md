This thread fixes a long-standing inconsistency in Git's bash completion for `git diff`. When you type `git diff un<TAB>`, the completion now behaves like `git status` and `git add`: it suggests tracked files first, then falls back to untracked files if nothing matches. The fix also handles edge cases like `git -C another-directory diff` and empty revision input, making the behavior predictable across all invocations.

Junio C Hamano sent a three-patch series to address this. The first patch refactored the completion script without changing behavior, the second added tracked-file completion, and the just-arrived third patch adds untracked-file fallback. The series is now complete, with 98 new test cases covering tracked and untracked paths, local and `-C` scenarios, and `--no-index` cases. SZEDER Gábor initially raised a concern about `--no-index` regressions in an earlier version, but the final series resolves this by explicitly handling untracked paths rather than relying on implicit Bash behavior.

The changes are confined to `contrib/completion/git-completion.bash` and the test suite (`t/t9902-completion.sh`). Junio has queued the series in his `seen` branch, indicating it's under review but effectively ready for merging. There are no open questions or objections; the series is uncontroversial and aligns completion behavior with user expectations.