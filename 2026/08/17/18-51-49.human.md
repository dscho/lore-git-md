This is the second patch in Kristoffer Haugsbakk’s v4 documentation series, part of the ongoing man page synopsis-style conversion effort. It applies the `[synopsis]` block markup to a code example in `Documentation/git-format-rev.adoc` to properly highlight the `<subject>` placeholder, aligning with the precedent set in commit a34d1d53 (doc: convert git-show to synopsis style, 2026-02-06).

The patch replaces a literal code block (`----`) with an open block (`--`) preceded by `[synopsis]`, resolving the AsciiDoc/AsciiDoctor rendering inconsistency that blocked v3. Kristoffer verified the fix locally with both `make doc` and `make USE_ASCIIDOCTOR=1 doc`, and the commit message now explicitly contrasts `[synopsis]` with `[verse]` blocks and documents the block syntax change. This is a purely stylistic improvement—no behavior or on-disk format changes are involved.

The series is now procedurally unblocked and ready for re-acknowledgment. Patrick Steinhardt’s prior `Acked-by` was removed due to the block syntax change, but his earlier technical agreement (favoring literal quotes for clarity) remains relevant. The patch is small, well-motivated, and directly addresses the feedback that stalled v3. For a documentation-only change, it looks like a straightforward merge once reviewers confirm the corrected syntax.