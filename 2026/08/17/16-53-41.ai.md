This is a **brief technical clarification** in the ongoing `git history squash` v13 thread. Junio C Hamano corrects a misunderstanding from his earlier reply: the `istarts_with()` call in question is **not an existing Git API usage** but rather **new code introduced by this series** (specifically, in Patch 7/8’s autosquash message handling). He **explicitly rejects case-insensitive OID matching** here, reinforcing that the test’s use of `tr a-f A-F` to generate uppercase OIDs is unnecessary and should be removed.

The exchange is **narrowly scoped** to the test’s robustness fix (already queued) and has no bearing on the series’ core functionality. Junio’s stance aligns with the broader project trend toward stricter hexadecimal validation (e.g., Brian M. Carlson’s `bc/restrict-hex-to-lowercase` topic), though the uppercase OID debate itself remains unresolved. For the `git history squash` series, this is a **final procedural confirmation** that the test’s revision (replacing `tr` with `git rev-parse --short`) is the correct path forward.