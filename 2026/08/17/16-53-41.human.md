This is a **brief technical clarification** in the ongoing `git history squash` v13 thread. Junio C Hamano corrects a misunderstanding from his earlier reply: the use of `istarts_with()` in the new `git history squash` code is **not an oversight** in Brian M. Carlson’s parallel `bc/restrict-hex-to-lowercase` topic, but rather a deliberate choice in Harald Nordgren’s patch. Junio **explicitly rejects the case-insensitive comparison**, reinforcing that Git has no reason to support uppercase object IDs in this context. The exchange is a minor but necessary step to align the series with Git’s broader direction on hexadecimal string handling, and it **removes any lingering ambiguity** about whether the test’s behavior was intentional or accidental. No action is required from Harald; the fixup Junio already queued (replacing `tr a-f A-F` with `git rev-parse --short`) remains the correct resolution.