This is the **maintainer's merge confirmation** for the `http.sslVerifyStatus` security feature patch. Junio C Hamano accepts the v4 iteration, noting that the **test integration is now more economical** (29 lines added to an existing script instead of a new 72-line test file) and that the **code and documentation remain unchanged and look good**. The patch will replace the previous version in Junio's queue.

The feature adds a boolean `http.sslVerifyStatus` config option (default `false`) that enables OCSP staple validation via libcurl, closing a security gap where OpenSSL-linked Git binaries (common in FIPS-compliant deployments) silently ignore staples and accept revoked certificates. The design is fail-closed (connection fails if no staple is provided) but defaults to `false` to avoid breaking remotes that do not support stapling. The patch touches `http.c`, `Documentation/config/http.adoc`, and `t/t5551-http-fetch-smart.sh`, with no symbol renames and no new libcurl requirements beyond Git's existing minimum.

This iteration resolves all procedural feedback from earlier rounds: the four new tests are now integrated into `t5551-http-fetch-smart.sh` (running only in the HTTPS pass via `t5559`), and the `SSL_VERIFYSTATUS` lazy prerequisite uses `test_might_fail` to handle expected failures gracefully. The commit message now includes the rationale that was previously in an in-code comment, explaining why the default cannot be left to libcurl (which intentionally ignores staples) and why the feature is fail-closed.

The patch is now **technically complete and ready for integration**, with no outstanding objections. The open question about the feature's long-term maintenance burden (given OCSP stapling's declining relevance) remains unresolved, but the author has provided concrete evidence of its value for government and FIPS-compliant deployments (e.g., US DoD PKI), where OpenSSL-linked Git binaries currently ignore staples, creating a security gap. The threat model (stolen private key impersonation enabling data exfiltration) is framed as a low-to-medium severity CVE risk. Junio's acceptance signals that the patch is now likely to graduate to `next` and eventually `master`.