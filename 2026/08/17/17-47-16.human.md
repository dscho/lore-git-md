This is a brief but substantive technical exchange in the ongoing ODB alternates refactoring series. Jeff King (Peff) acknowledges Patrick Steinhardt’s point that `repo_ignore_case()` cannot be used in the current patch because it internally relies on `repo_config_values()`, which is hardcoded to `the_repository` and would break submodule support. Peff calls this limitation "surprising" and "gross," signaling that the restriction is unintuitive and likely unintended.

The exchange clarifies a key technical constraint for patch 1/5: the inability to use `repo_ignore_case()` forces Patrick to continue using a direct `core.ignoreCase` lookup, which remains vulnerable to the timing issue where the value might be cached before the repository’s configuration is fully loaded. This reinforces the need for the reordering fix introduced in the v3 series (ensuring `core.sharedRepository` and `core.ignoreCase` are set before ODB creation) and leaves the broader design question of case-insensitive deduplication unresolved. No new solutions or workarounds are proposed here, but the thread underscores the fragility of the current approach.