This is a **design discussion follow-up** in the ODB alternates refactoring series, focusing on patch 1’s case-sensitivity handling. Patrick Steinhardt now proposes to **defer the simplification** (dropping case-insensitivity entirely) to a future patch, arguing that the current refactoring is already a net improvement and that the risk of regressions from further changes outweighs the benefits. He suggests documenting the unresolved design question as `NEEDSWORK` and proceeding with the existing implementation for now.

The email directly responds to the emerging consensus (from Jeff King and Junio Hamano) that the current patch’s complexity is unjustified, but Patrick pushes back on making the change immediately. His rationale hinges on two points: (1) the current patch is already a step forward (decoupling path comparisons from `the_repository`), and (2) the performance and correctness trade-offs of eager case-sensitive matching are not yet fully understood. He implies that the simplification could introduce subtle regressions (e.g., duplicate alternates degrading performance) and that further testing or analysis is needed before adopting it.

This reply leaves the series in a holding pattern. The v2 patches remain queued in `seen`, but patch 1’s design is now explicitly marked as incomplete. The unresolved question is whether Junio will accept the series as-is (with a `NEEDSWORK` note) or insist on a v3 that simplifies the patch first. The broader ODB abstraction effort is unaffected, but this thread’s outcome will influence how aggressively the project pursues code simplification in low-risk areas. Key technical details to watch:
- **Files touched**: `odb/source.h`, `odb.c` (patch 1).
- **Symbols**: `fspathhash()`, `fspatheq()`, `source_paths_icase` (current implementation); the proposed simplification would remove the latter.
- **Behavioral change**: The current patch retains case-insensitive deduplication; the deferred simplification would switch to case-sensitive matching.
- **Performance concern**: Duplicate alternates (e.g., `/path/to/alternate` and `/PATH/TO/ALTERNATE`) could degrade performance if not deduplicated.