This is a **clarification reply** in the `git history squash` v13 thread, where Junio C Hamano responds to Phillip Wood’s request for a security rationale behind rejecting uppercase hexadecimal object IDs. Junio’s message is brief but decisive: **there is no technical justification for supporting uppercase OIDs in Git’s codebase**, and the use of `istarts_with()` to accommodate them is unnecessary.

Junio dismisses the idea that Git needs to handle uppercase hexadecimal for compatibility or legacy reasons, noting that Git has never generated such output. This directly addresses Phillip’s earlier skepticism about the security implications—Junio frames the issue not as a security vulnerability in Git itself, but as a **potential interoperability risk** if future changes (like Brian M. Carlson’s `bc/restrict-hex-to-lowercase` topic) tighten input validation. The test case in Patch 7/8, which uses `tr a-f A-F` to mangle OIDs, is thus fragile because it assumes a behavior (uppercase OID acceptance) that the project may soon reject outright.

The takeaway for the `git history squash` series is clear: **the uppercase OID concern is orthogonal to the patch’s functionality** and does not block integration. Junio’s fixup (replacing `tr a-f A-F` with `git rev-parse --short HEAD^`) already resolves the immediate test fragility, and his latest email confirms that the broader question of Git’s stance on uppercase OIDs is a separate discussion. The series remains **technically ready for integration**, with the only remaining hurdles being Phillip’s procedural demands (explicit replies to open questions and the missing `Signed-off-by` trailer).