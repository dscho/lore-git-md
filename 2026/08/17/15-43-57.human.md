This is a **bugfix** patch that has just received Junio C Hamano's explicit approval and will be queued for integration. The patch addresses a use-after-free bug in `get_oid_with_context_1()` when resolving relative paths in the `:<stage>:<path>` syntax against a sparse index. The issue occurs because the code frees the allocated string (`new_path`) while still holding a pointer (`cp`) to it, leading to a use-after-free in `reject_tree_in_index()`.

The fix is straightforward and uncontroversial: it ensures the string is freed only after its final use, consolidating exit paths to avoid the bug. Junio's response confirms the fix aligns with expectations and matches the problem description precisely. The patch has already received substantive reviews and approvals from René Scharfe and Patrick Steinhardt, with all feedback fully addressed in earlier versions. The regression test in `t1092-sparse-checkout-compatibility.sh` reliably reproduces the bug under `SANITIZE=address` and confirms the fix.

This is a well-crafted, thoroughly tested patch that addresses a real issue with clear reproduction steps. It touches core functionality in `sha1-name.c` and sparse index handling but makes no changes to the on-disk format. The patch is now ready for `next` and eventual inclusion in `master`.