This is a **substantive technical reply** in the ODB alternates refactoring series, specifically addressing Jeff King’s correctness concern about `core.ignoreCase` timing in patch 1. Patrick acknowledges the risk of caching the wrong value during early repository initialization and proposes a targeted fix: cherry-picking a patch from a later series that ensures `core.sharedRepository` is set before `create_object_directory()` is called. However, he rejects Jeff’s suggestion to use `repo_ignore_case()` because it relies on `repo_config_values()`, which is hardcoded to `the_repository` and would break submodule support.

The exchange clarifies that the timing issue is limited to the `init_db()` code path (e.g., during `git init` or `git clone`), where alternates might be loaded before the repository’s configuration is fully initialized. Patrick’s proposed fix is narrow and does not address the broader design question of whether to retain case-insensitive deduplication. The thread remains focused on patch 1’s implementation details, with no new discussion of the series’ overall goal (removing lazy-loading of alternates) or the earlier consensus to simplify the patch by dropping case-insensitivity entirely.

Key technical details for follow-up:
- **Files touched**: The fix will likely modify `init-db.c` (where `create_object_directory()` is called) and possibly `odb.c` (to avoid caching `core.ignoreCase` prematurely).
- **Symbols involved**: `create_object_directory()`, `init_db()`, `core.sharedRepository`, `repo_ignore_case()`, `repo_config_values()`.
- **Edge case**: Alternates loaded during repository initialization (e.g., via `GIT_ALTERNATE_OBJECT_DIRECTORIES` in `git init`).
- **Submodule compatibility**: The constraint that `repo_ignore_case()` cannot be used due to its reliance on `the_repository` is now explicit.

The next step is likely a v3 revision of patch 1, incorporating the cherry-picked fix and potentially reverting to case-sensitive comparisons (as previously agreed) to avoid the complexity of case-insensitive handling. The correctness concern is now partially addressed, but the thread leaves open whether the fix is sufficient or if further changes (e.g., delaying alternate loading until after `init_db()`) will be needed.