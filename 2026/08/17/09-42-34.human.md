This is a **follow-up in the ODB alternates refactoring series**, specifically addressing Jeff King’s correctness concern about patch 1’s handling of `core.ignoreCase`. The exchange clarifies the technical constraints and outlines the next steps for the v3 revision.

Patrick acknowledges Jeff’s point about the risk of caching `core.ignoreCase` too early, but notes that the issue is already mitigated in most cases—except during `init_db()`, where `create_object_directory()` is called before `core.sharedRepository` is set. He plans to cherry-pick a fix from a later series to resolve this edge case. However, he rejects Jeff’s suggestion to use `repo_ignore_case()` (introduced in `e6a79c9eb8`) because it relies on `repo_config_values()`, which is hardcoded to `the_repository` and would break submodule support. This leaves the series in a holding pattern: the v3 revision will need to either (1) adopt a submodule-compatible alternative to `repo_ignore_case()`, (2) revert to case-sensitive comparisons (the earlier consensus), or (3) document the timing issue as a known limitation.

The discussion is **narrow but critical**: it blocks the series until the `core.ignoreCase` lookup is resolved, but does not challenge the broader goal of removing lazy-loading. The tone is collaborative, with both authors focusing on technical constraints rather than design trade-offs. The next step is Patrick’s v3, which will likely include the cherry-picked fix and either a new helper function or a simplification to case-sensitive comparisons. Until then, the series remains queued in `seen` but not yet ready for `next`.