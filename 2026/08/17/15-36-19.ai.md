This is a **substantive follow-up** in the `git history squash` v13 thread, where Junio C Hamano clarifies the **security context** behind his earlier warning about uppercase hexadecimal OIDs in the test suite. His reply directly addresses Phillip Wood’s request for concrete rationale regarding the risks of accepting uppercase object IDs.

Junio explains that the patch itself does **not introduce new uses of `istarts_with()`** to deliberately support uppercase hexadecimal. Instead, his concern stems from the test’s reliance on `tr a-f A-F` to mangle OIDs into uppercase—a fragile assumption that could break if another in-flight topic (like `bc/restrict-hex-to-lowercase`) declares uppercase letters invalid in hex strings. He emphasizes that Git has no existing machinery that generates uppercase OIDs, so there is no legacy compatibility justification for supporting them. The implication is that **uppercase OIDs are not a security issue in isolation**, but their acceptance could create **interoperability risks** if future changes tighten input validation.

This exchange **resolves the immediate test fragility issue** by confirming that the uppercase OID concern is **orthogonal to the `squash` series** and not a blocker for integration. However, it leaves the broader question of **Git’s stance on uppercase OIDs** unresolved—Phillip’s push for a security rationale remains unanswered, though Junio’s framing suggests the project may eventually reject them outright. The thread’s focus now returns to **Phillip’s procedural demands** (missing `Signed-off-by` trailers and explicit replies to open questions), which Harald Nordgren has yet to address. The series remains **technically ready for integration** but procedurally stalled.