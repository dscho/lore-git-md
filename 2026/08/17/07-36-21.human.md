This is a **substantive review reply** in the ongoing ODB alternates refactoring series, specifically about patch 1’s case-sensitivity handling. Jeff King (Peff) responds to Patrick Steinhardt’s proposal to defer simplifying the patch by dropping case-insensitive deduplication. Peff reluctantly accepts the deferral but raises a **critical technical concern**: the current implementation caches the `core.ignoreCase` setting in `o->source_paths_icase`, which could lead to incorrect behavior if alternates are loaded before the repository’s configuration is fully initialized. He suggests using the new `repo_ignore_case()` helper (introduced in commit `e6a79c9eb8`) instead, as it handles cache invalidation and is already in `master`.

The review advances the thread by identifying a concrete correctness risk in the current patch. Peff’s concern is not hypothetical—it targets a real edge case (early alternate loading) that could cause Git to misbehave in scenarios like partial clones or repository initialization. His suggestion to use `repo_ignore_case()` is actionable and aligns with recent work to centralize case-sensitivity logic. The tone is pragmatic: Peff acknowledges the deferral is acceptable but clearly signals that the current approach is fragile and should be addressed before the series graduates from `seen`.

For a busy reader, this email is worth attention if you care about ODB internals or repository initialization. The patch is part of a larger refactoring effort, and Peff’s feedback highlights a subtle but important correctness issue that could affect other subsystems. The series remains queued in `seen`, but this review may prompt a v3 revision to fix the caching problem before the patch is considered for `next`.