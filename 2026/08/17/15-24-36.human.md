This is a **brief technical exchange** in the `git history squash` v13 thread, focusing on a **test robustness issue** and its interaction with an in-flight security topic. Junio C Hamano clarifies why the test case in Patch 7/8—specifically its use of `tr a-f A-F` to generate uppercase commit OIDs—is problematic and how his fixup patch resolves it.

The discussion centers on `istarts_with()`, a helper function used in `git history squash` to match commit references by partial OID (e.g., `HEAD^` or `main`). The test pipes a lowercase OID through `tr` to uppercase it, verifying that `istarts_with()` correctly handles case-insensitive matching. However, Junio points out this conflicts with **`bc/restrict-hex-to-lowercase`**, an in-flight topic by brian m. carlson that aims to reject uppercase hexadecimal literals in Git’s codebase for security reasons. To avoid merge conflicts, Junio’s fixup replaces the `tr` pipeline with `git rev-parse --short`, which tests the "starts with" logic without relying on uppercase OIDs.

Phillip Wood pushes back, asking for a clearer explanation of the security risks posed by uppercase OIDs. Junio acknowledges the request but does not elaborate further in this email. The exchange highlights a **tradeoff between test clarity and forward compatibility**: the original test was more explicit about its intent, but Junio’s fix ensures the series integrates cleanly with parallel work. The fix is already queued in Junio’s integration branch, so no further action is needed from Harald Nordgren—this is a **resolved technical detail** rather than a blocker. The broader question of whether uppercase OIDs should be rejected remains open, but it does not affect the `squash` series’ readiness for integration.