This is a **substantive follow-up** in the ongoing discussion about fixing Git’s packfile performance regression. The thread remains focused on the broader architectural question of how to handle duplicate checks in `packfile_list` operations, even though Johannes Schindelin’s immediate fix (a fast-path optimization for `packfile_store_add_pack()`) has already been merged to `next`.

Patrick Steinhardt responds to Jeff King’s skepticism about his proposed hashmap refactoring by pointing out that while `packfile_list_append()` has only one caller, `packfile_list_prepend()` is used in multiple places—all of which could benefit from a more efficient duplicate-checking mechanism. This observation shifts the conversation slightly: the hashmap’s utility isn’t limited to the immediate regression fix but could address a recurring pattern in packfile list management. Patrick doesn’t propose a concrete patch here, but his comment suggests that the refactoring might still be worth pursuing, even if it’s not urgent.

The exchange highlights a tension between targeted fixes (like Johannes’s fast-path) and generalized improvements (like Patrick’s hashmap). Jeff’s earlier concern—that the hashmap’s current role in avoiding `add_packed_git()` overhead makes it hard to relocate—remains unresolved, but Patrick’s reply implies that the prepend operations might offer enough justification to revisit the idea. For now, this is a **design discussion** rather than an actionable patch, but it keeps the door open for future work on the packfile subsystem. If you’re tracking Git’s performance or packfile internals, this thread is worth watching for signs of a follow-up refactoring.