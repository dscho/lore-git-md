This is a **substantive review reply** in the ongoing ODB alternates refactoring series. Jeff King (Peff) responds to Patrick Steinhardt’s proposal to defer simplifying patch 1’s case-sensitivity handling, raising a **critical correctness concern** about the current implementation’s reliance on `core.ignoreCase`.

Peff’s key point is that the patch caches the case-sensitivity flag (`o->source_paths_icase`) during ODB initialization, but this value could be incorrect if the repository’s configuration isn’t fully loaded at that time. He cites a recent commit (`e6a79c9eb8`) that introduced `repo_ignore_case()` as a safer way to access `core.ignoreCase`, and suggests using it instead of inline config lookups. This would centralize any future cache invalidation logic in one place rather than scattering it across the ODB subsystem.

The review is **substantive and tested**: Peff identifies a concrete risk (timing-dependent config lookups) and references a specific commit that provides a solution. His concern is narrowly scoped to patch 1’s implementation detail, but it could block the series if unresolved. The thread’s next step will likely be a v3 revision to address this, either by adopting `repo_ignore_case()` or by reverting to case-sensitive comparisons (the earlier consensus). The broader goal of removing lazy-loading remains unchanged.