This is a **review follow-up** in the ongoing thread adding `--abbrev`, `--color`, and `--date` options to `git format-rev`. Junio C Hamano **acknowledges and endorses** Kristoffer Haugsbakk’s plan to **reorder the `BUG_ON_OPT_NEG(unset)` assertion** in the `format_nul_cb()` callback (patch 2/4) to appear **before any variable assignments**, matching the defensive programming fix already agreed for the new `date_cb()` callback (patch 4/4). The change is purely mechanical—moving two lines per callback—and reinforces a consistent "validate before use" pattern across all option callbacks in `builtin/name-rev.c`. No new technical concerns are raised; the discussion remains focused on code hygiene rather than functionality. The series is still under review, with no integration branch queuing yet.