This is the second patch in Kristoffer Haugsbakk’s v4 two-patch documentation series, part of the ongoing man-page synopsis-style conversion effort. It adds a `[synopsis]` open block to the code example in `Documentation/git-format-rev.adoc` that demonstrates `--stdin-mode=text` output formatting. The change ensures the `<subject>` placeholder is rendered with emphasis styling, matching the project-wide documentation consistency goal.

The patch resolves the procedural blocker identified in v3: an AsciiDoc/AsciiDoctor rendering inconsistency caused by using a code block (`----`) instead of an open block (`--`). Kristoffer verified the fix locally with both `make doc` and `make USE_ASCIIDOCTOR=1 doc`, confirming compatibility with both toolchains. The commit message explicitly contrasts `[synopsis]` with `[verse]` blocks and cites commit a34d1d53 (doc: convert git-show to synopsis style, 2026-02-06) as precedent for the open-block syntax.

This is a documentation-only change with no impact on on-disk formats, behavior, or test coverage. The series is now procedurally unblocked and ready for re-acknowledgment after the block-syntax correction invalidated Patrick Steinhardt’s prior `Acked-by`. The patch touches only `Documentation/git-format-rev.adoc`, introducing no new symbols or dependencies. The next step is for reviewers to verify the corrected block syntax and re-acknowledge the series, after which the topic `kh/format-rev-doc-synopsis` should be eligible for merging into `next`. The thread’s prior context suggests this will be a straightforward merge once the procedural fix is confirmed.