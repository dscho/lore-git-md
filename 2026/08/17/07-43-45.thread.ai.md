Here is the updated AI-mode thread summary, preserving all prior state and incorporating the new "What's cooking" report:

---

Thread identity: Junio C Hamano’s recurring "What's cooking in git.git" report, August 2026 edition. Originating author: Junio C Hamano. Thread type: integration status report.

Core problem or goal: Provide a snapshot of all in-flight topics across Junio’s integration branches (`master`, `next`, `seen`), including graduated, cooking, stalled, and discarded topics, with emphasis on high-impact changes and open design questions.

Current status: Since Git 2.55, 437 commits have graduated to `master`, 112 commits are cooking in `next`, and 273 commits remain in `seen`. Key graduated topics include `jc/add-resolved` (new `git add --resolved` command), `tn/packfile-uri-concurrency` (concurrency fix for packfile URI downloads), and `js/sequencer-release-odb-before-commit` (Windows file-handle leak fix). Stalled topics: `tb/repack-geometric-cruft` (unresolved feedback), `hn/checkout-track-fetch` (marked for discard). Active topics in `next`/`seen`: `ps/odb-pluggable-pack-generation` (ODB abstraction, awaiting reroll), `ps/odb-eagerly-load-alternates` (ODB simplification), `js/packfile-fast-append` (performance fix), `ss/repack-drop-filtered` (space reclamation for partial clones), `hn/bisect-reset-when-found` (new bisect option), `mm/diff-process-hunks` (external hunk providers, stalled), `hn/history-squash` (new command, design clarification requested). The `the_repository` removal effort continues with `ty/repository-fetch-if-missing`.

Key technical details:
- **`jc/add-resolved`**: New CLI option `--resolved` for `git add`; stages conflict-resolved paths while preserving unrelated local changes; aborts if conflict markers remain. No on-disk format changes.
- **`tn/packfile-uri-concurrency`**: Fixes concurrency issues in packfile URI downloads by avoiding concurrent appends to staging files; affects `fetch` and `clone` with `--packfile-uri`.
- **`js/sequencer-release-odb-before-commit`**: Releases ODB handle before spawning `git commit` in sequencer; resolves file-handle leaks on Windows.
- **`ps/odb-pluggable-pack-generation`**: Introduces ODB callbacks for packfile generation; enables alternative backends to serve packfiles directly. Files touched: `odb.h`, `pack-objects.c`, `builtin/pack-objects.c`.
- **`ps/odb-eagerly-load-alternates`**: Simplifies ODB initialization by eagerly loading alternates; reduces scattered lazy-loading calls. Part of ODB abstraction effort.
- **`js/packfile-fast-append`**: Fixes quadratic complexity in `packfile_list_append()` when adding many new packfiles.
- **`ss/repack-drop-filtered`**: New `--drop-filtered` option for `git repack`; deletes local promisor blobs exceeding a size limit; reclaims space in partial clones.
- **`hn/bisect-reset-when-found`**: New `--reset-when-found` option for `git bisect`; resets to original state or found commit after completion.
- **`mm/diff-process-hunks`**: New `diff.<driver>.process` config; allows external processes to act as hunk providers; enables tools to control which lines Git considers changed. Files touched: `diff.c`, `diff.h`, `Documentation/config/diff.txt`.
- **`hn/history-squash`**: New `git history squash` subcommand; folds a range of commits into one, with descendants replayed on top. No on-disk format changes.
- **`ty/repository-fetch-if-missing`**: Moves `fetch_if_missing` global into `struct repository`; part of `the_repository` removal effort.

Open questions and loose ends:
- **`ps/odb-pluggable-pack-generation`**: Awaiting reroll to address feedback (details not specified in report).
- **`mm/diff-process-hunks`**: Stalled pending further discussion on edge cases and integration with existing diff features.
- **`hn/history-squash`**: Junio has requested clarification on design; no specific objections recorded.
- **`tb/repack-geometric-cruft`**: Unresolved feedback; topic remains stalled.
- **`hn/checkout-track-fetch`**: Marked for discard due to design concerns; no indication of resubmission.

Participant positions:
- Junio C Hamano (maintainer): Authored `jc/add-resolved`; merged `tn/packfile-uri-concurrency`, `js/sequencer-release-odb-before-commit`, and others to `master`; marked `hn/checkout-track-fetch` for discard; requested design clarification for `hn/history-squash`; noted `mm/diff-process-hunks` as stalled.
- Patrick Steinhardt: Authored `ps/odb-pluggable-pack-generation` (awaiting reroll) and `ps/odb-eagerly-load-alternates` (cooking in `next`).
- Johannes Schindelin: Authored `js/sequencer-release-odb-before-commit` (graduated) and `js/packfile-fast-append` (cooking in `next`).
- Taylor Blau: Authored `tb/repack-geometric-cruft` (stalled).
- Han-Wen Nienhuys: Authored `hn/checkout-track-fetch` (discarded) and `hn/bisect-reset-when-found` (cooking in `next`); authored `hn/history-squash` (awaiting design clarification).
- Sohom Sengupta: Authored `ss/repack-drop-filtered` (cooking in `next`).
- Michael McClimon: Authored `mm/diff-process-hunks` (stalled).
- Teng Long: Authored `tn/packfile-uri-concurrency` (graduated).
- Toon Claes: Authored `ty/repository-fetch-if-missing` (cooking in `next`).
- Kristoffer Haugsbakk: Authored `kh/doc-trailers` (graduated documentation update).

Related work: ODB abstraction effort (Patrick Steinhardt), `the_repository` removal (René Scharfe), test modernization (community-wide). No external dependencies mentioned.

---