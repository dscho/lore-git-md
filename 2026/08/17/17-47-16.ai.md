This is a **substantive technical reply** in the ongoing ODB alternates refactoring thread. Jeff King (Peff) acknowledges Patrick Steinhardt’s explanation for why `repo_ignore_case()` cannot be used in the current patch: the function internally relies on `repo_config_values()`, which is hardcoded to `the_repository` and would break submodule support. Peff expresses surprise and mild disapproval at this limitation, calling it "gross," but accepts that the function is not yet suitable for Patrick’s use case.

The exchange confirms that the `core.ignoreCase` timing issue in patch 1 remains unresolved, as the proposed workaround (using `repo_ignore_case()`) is not viable. The thread’s focus is now on the technical constraint rather than the original design question (whether to retain case-insensitive deduplication). No alternative solutions are proposed in this email, leaving the path forward unclear—Patrick may need to either (1) implement a submodule-compatible version of `repo_ignore_case()`, (2) revert to case-sensitive comparisons (as previously agreed in the thread), or (3) proceed with the current approach and document the limitation.

Key technical details for follow-up:
- **Symbol involved**: `repo_config_values()` (hardcoded to `the_repository`).
- **Constraint**: Submodule repositories cannot use `repo_ignore_case()`.
- **Files affected**: Likely `config.c` (where `repo_config_values()` is defined) and `odb.c` (where the comparison logic lives).
- **Next steps**: The thread is now blocked on resolving this constraint, with no immediate resolution proposed. The broader series (removing lazy-loading of alternates) remains queued in `seen`, but patch 1’s correctness issue is still open.