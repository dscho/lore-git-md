This is a **substantive design discussion reply** in the ongoing ODB alternates refactoring series, specifically addressing the unresolved question of case-sensitivity handling in patch 1. Jeff King (Peff) and Patrick Steinhardt reach a clear consensus: the current implementation’s complexity is unjustified, and the code should be simplified by switching to case-sensitive path comparisons for alternate object databases.

Peff reinforces his earlier critique, dismissing the risk of duplicate alternates as a "shoot yourself in the foot" scenario unlikely to affect real-world users. He clarifies that standard Git commands (e.g., `git clone --shared`) produce linear alternate chains, not the pathological trees that would trigger the edge case Patrick described. Even in specialized setups (e.g., GitHub’s fork networks or kernel.org’s experimental tree structures), Peff argues the risk of case-induced duplicates is negligible. His examples suggest that the only plausible failure mode is manual tampering with the alternates file, which he considers an acceptable trade-off for code simplicity.

Patrick concedes the point, acknowledging that `stat()`-based deduplication (Peff’s proposed alternative) is ineffective on Windows due to unreliable `st_ino`/`st_dev` values. This leaves case-sensitive matching as the only viable simplification, which both now agree is the right path forward. The discussion does not revisit the broader performance implications of eager alternate loading (e.g., for `git fsck`) or edge cases like corrupt alternates, but the technical rationale for patch 1’s current design is now fully undermined.

The key takeaway: the series will likely require a v3 revision to strip out case-insensitive handling from patch 1. No new implementation is posted yet, but the thread has converged on a concrete direction. The unresolved procedural question (Junio’s merge strategy) remains unaddressed, but this design agreement is the critical next step for the series’ progress.