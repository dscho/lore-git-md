This is a bugfix patch targeting the config subsystem. The patch modifies `git config -e` to propagate the editor's exit code to the caller, so that if the editor fails (crashes or returns non-zero), Git exits non-zero instead of always returning 0. The change is minimal: a one-line adjustment in `builtin/config.c` to return the error code from `launch_editor()`, plus two new tests in `t/t1300-config.sh` that verify the exit code when the editor succeeds (`GIT_EDITOR=true`) or fails (`GIT_EDITOR=false`).

The patch is clearly motivated and well-scoped. The commit message acknowledges the potential for breaking scripts that rely on the current behavior (always exiting 0), which is why it is marked as RFC. The author is seeking community input on whether the current behavior is intentional or a bug, and whether the trade-off is acceptable. The test coverage is appropriate, covering both success and failure cases, and the patch itself is clean and focused.

The thread context confirms this is the first iteration of a standalone patch, with no prior versions or related work. The author has flagged the RFC status explicitly, so reviewers are likely to focus on the behavioral trade-off rather than the implementation details. The patch touches only the error propagation path in `show_editor()`, so the risk of unintended side effects is low, but the change could affect scripts that assume `git config -e` always succeeds.

This is a small but meaningful correctness improvement. Whether it lands will depend on whether the community views the current behavior as a bug or an intentional quirk. The patch is worth tracking for anyone interested in config subsystem behavior or error handling in Git.