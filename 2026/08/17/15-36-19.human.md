This is a **brief technical exchange** in the `git history squash` v13 thread, focusing on the security implications of accepting uppercase hexadecimal object IDs in Git. Junio C Hamano clarifies that the current patch does not introduce any *new* use of `istarts_with()` to deliberately support uppercase OIDs—meaning the test’s case-munging with `tr a-f A-F` is not adding a new attack surface. The discussion is **narrow and procedural**, not a blocker for the series.

Junio’s reply pushes back on Phillip Wood’s request for a detailed security rationale, signaling that the uppercase OID question is **orthogonal to this patch**. The test’s use of `tr` to generate uppercase OIDs was merely a way to exercise Git’s existing `istarts_with()` logic, not an endorsement of uppercase input. There is no existing machinery in Git that generates uppercase hexadecimal, so the test’s approach doesn’t create a new compatibility requirement or security risk. The broader debate about whether Git should reject uppercase OIDs entirely remains unresolved, but it is **not tied to this series** and does not affect its readiness for integration.

The exchange leaves the test’s design intact—Junio’s earlier fixup (replacing `tr` with `git rev-parse --short`) already resolved the immediate fragility issue. Phillip’s request for a security explanation was more about general curiosity than a demand for changes, and Junio’s response effectively closes the discussion for this thread. The series remains **technically complete and procedurally stalled** pending Harald Nordgren’s response to Phillip’s other process concerns (explicit replies to open questions and the missing `Signed-off-by` trailer).