This is a review reply in an ongoing bugfix thread targeting submodule configuration parsing. Junio C Hamano raises substantive concerns about the patch's approach, questioning whether the original hard error is actually problematic and whether the proposed warning-and-continue behavior is an improvement. He argues that the current error message is sufficient for users to diagnose and fix their configuration, and that silently skipping malformed entries could mask misconfigurations. The review also notes a minor issue with the commit message formatting (real name in Signed-off-by) and line wrapping.

The key point of contention is the handling of valueless `submodule.active` entries: the patch proposes emitting a warning and continuing, while Junio suggests the existing hard error is preferable. The thread's NEEDSWORK comment is explicitly called out as requiring validation of the suggested approach, not automatic implementation. The author's rationale for not returning an error code (due to boolean evaluation semantics in callers) is acknowledged but not endorsed. The discussion now hinges on whether the trade-off between user visibility and crash avoidance is justified.