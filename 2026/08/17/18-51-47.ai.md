This is the v4 cover letter for a two-patch documentation series targeting `Documentation/git-format-rev.adoc`. The series is part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila. Kristoffer Haugsbakk addresses a procedural blocker identified in v3: an AsciiDoc/AsciiDoctor rendering inconsistency caused by using a code block (`----`) instead of an open block (`--`) for the `[synopsis]` tag. The fix aligns the block syntax with the precedent set in commit a34d1d53 (doc: convert git-show to synopsis style, 2026-02-06), resolving the CI failure observed under AsciiDoc while maintaining compatibility with AsciiDoctor.

The series remains functionally unchanged from v3—its goal is still to improve placeholder readability and consistency in a `git format-rev` code example. Patch 1 replaces `<subject>` with `"<subject>"` to match the implied input format (a commit subject enclosed in quotes), while Patch 2 adds a `[synopsis]` block to properly highlight placeholders. The only difference in v4 is the block syntax correction, which Kristoffer verified locally with both `make doc` and `make USE_ASCIIDOCTOR=1 doc`. The commit message for Patch 2 has been updated to reflect the use of an open block rather than a code block, and Patrick Steinhardt’s `Acked-by` has been removed since the change invalidates it.

The series is now procedurally unblocked and ready for review. No technical objections remain, and the rendering issue that prompted Junio’s feedback in v3 has been resolved. The patches touch only documentation, introduce no new symbols, and have no impact on on-disk formats or behavior. The next step is for reviewers to verify the corrected block syntax and re-acknowledge the series. Once that happens, the topic `kh/format-rev-doc-synopsis` should be eligible for merging into `next`. The thread’s prior context—including Patrick’s substantive feedback on markup semantics and Kristoffer’s responsiveness—suggests this will be a straightforward merge.