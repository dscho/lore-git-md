This is a **brief technical exchange** in the `git history squash` v13 thread, focusing on a **test robustness issue** and its intersection with an unrelated in-flight topic (`bc/restrict-hex-to-lowercase`). Junio C Hamano clarifies why the test case in Patch 7/8—specifically its use of `tr a-f A-F` to generate uppercase object IDs—must be revised to avoid conflicts with Brian M. Carlson’s parallel effort to restrict hexadecimal literals to lowercase.

The core issue is the test’s reliance on `istarts_with()` to accept uppercase OIDs, which Brian’s topic aims to prohibit. Junio’s fixup patch (already queued) replaces the problematic `tr` pipeline with `git rev-parse --short`, sidestepping the conflict while preserving the test’s intent. Phillip Wood’s pushback—requesting concrete security rationale for rejecting uppercase OIDs—remains unresolved, but Junio’s response ("True") signals no further debate on the test’s immediate fix. The exchange underscores the **practical challenge of coordinating parallel topics** and the maintainer’s role in resolving such collisions preemptively.

For the `git history squash` series, this is a **minor technical adjustment** with no impact on functionality. The fix is already in Junio’s integration branch, removing the last obstacle to merging Patch 7/8. The broader question of uppercase OID security remains open but is orthogonal to this series.