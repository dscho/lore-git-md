This is the **author's reply** to Junio C Hamano's **substantive review** of patch 4/4 in Kristoffer Haugsbakk's series adding `--abbrev`, `--color`, and `--date` options to `git format-rev`. The author **accepts the review feedback** and plans to **reorder the `BUG_ON_OPT_NEG(unset)` assertion** in both the new `date_cb()` callback and the existing `format_nul_cb()` callback to ensure they appear **before any dereference of `option->value`**, aligning with defensive programming best practices. The author acknowledges that the current placement in both callbacks is a "monkey see, monkey do" mistake—copying the pattern from other callbacks without considering the order's safety implications. The fix is trivial (reordering two lines per callback) and will be included in the next revision of the series. No other changes are proposed; the rest of the patch remains uncontroversial. The series is still under review, with no objections to the core functionality. Key files affected: `builtin/name-rev.c` (callback implementations).