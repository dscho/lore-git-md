This is a documentation patch series (2 patches, v4) targeting `Documentation/git-format-rev.adoc`, part of the ongoing man page synopsis-style conversion effort. The series revises the styling of the `<subject>` placeholder in a code example to improve readability and consistency with the project's documentation standards.

The first patch replaces the bare `<subject>` placeholder with `"<subject>"` in both the explanatory text and example output, aligning with the implied input format (a commit subject enclosed in quotes). The second patch adds a `[synopsis]` block to the same code example to properly highlight placeholders, using an open block (`--`) instead of a code block (`----`) to resolve an AsciiDoc/AsciiDoctor rendering inconsistency that caused CI failures in v3. This procedural fix was the sole change in v4, prompted by Junio’s observation about the build-system mismatch.

Kristoffer Haugsbakk’s commit messages are now more precise, explicitly noting the use of an open block (not a code block) and referencing the precedent set in commit a34d1d53 (doc: convert git-show to synopsis style, 2026-02-06). The series is well-motivated, splitting changes into two logical commits to address Patrick Steinhardt’s earlier feedback about clarity and markup semantics. Patrick’s `Acked-by` from v3 was dropped in v4 due to the block syntax change, but the technical approach remains aligned with his suggestions.

No behavioral changes are introduced—this is purely a documentation improvement. The series is ready for merging once the v4 rendering is verified, as the procedural blocker (block syntax mismatch) has been resolved. The outcome reinforces flexibility in applying markup rules, particularly in non-synopsis contexts, which may guide future patches in the conversion effort. The changes are minimal and unlikely to be controversial, though reviewers may double-check the open block syntax against other synopsis-style documentation.