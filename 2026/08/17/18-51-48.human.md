This is the first patch in a two-part documentation series (v4) that refines the `<subject>` placeholder styling in `Documentation/git-format-rev.adoc`. The patch addresses an inconsistency in a code example: the text describes formatting a commit subject with `%s`, but the example output shows the subject enclosed in quotes (`"<subject>"`). To align the two, the patch updates the format string to `"%s"` and the placeholder to `"<subject>"`, making it clear that the implied input (a commit subject enclosed in quotes) is preserved in the output.

The change is part of the ongoing man page synopsis-style conversion effort, though this patch focuses on readability rather than markup structure. The commit message explains the rationale well: the implied input (`Did we not fix this in <commit object name>?`) is formatted to `"<subject>"`, so the example should reflect that. No new symbols or on-disk changes are introduced, and the patch touches only documentation. The v4 revision resolves a procedural blocker from v3 (an AsciiDoc/AsciiDoctor rendering inconsistency) by harmonizing the block syntax in the follow-up patch, but this patch itself is unchanged from earlier versions.

The patch looks like a straightforward improvement—it’s small, well-motivated, and aligns with the project’s emphasis on consistency. The series as a whole is now unblocked and ready for review, with no technical objections remaining. If the second patch (which adds the `[synopsis]` block) is also approved, this should be a quick merge into `next`.