This is a maintainer response to a v2 bugfix patch that aims to propagate the editor's exit code in `git config -e`. Junio informs the author that the change has already been implemented and merged into the `next` branch as commit 633ac346ee ("config: propagate launch_editor() failure in show_editor()", 2026-08-12). He instructs the author to test the feature by building from `next` rather than resubmitting the patch.

The patch is now effectively superseded by the merged version, resolving the thread's core question about whether the behavior should change. No further action is needed from the author on this topic. The implementation in `next` is functionally identical to the proposed v2 patch, so the discussion about trade-offs (consistency vs. script breakage) is now moot for this specific change. The feature's recent introduction (mid-2024) and the lack of reported issues with the merged version suggest the breakage risk was deemed acceptable.