This is a surface-level follow-up to the RFC patch that makes `git config -e` propagate the editor's exit code. Kenneth Lorber accepts Karthik Nayak's suggested test-script cleanups, replacing manual `GIT_EDITOR` export/unset sequences with `test_env` in both new tests (`git config --edit successful exit` and `git config --edit failure exit`). The patch's substance—code change, rationale, and test coverage—remains unchanged; this exchange only tidies the test setup. The thread's core question (whether the current behavior is intentional and whether changing it now is worth the potential script breakage) is still unresolved.