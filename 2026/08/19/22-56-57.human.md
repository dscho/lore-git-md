This is the final polish discussion for the `dk/use-nsec-runtime` series, which is cooking in `next`. The patch converts the build-time `USE_NSEC` macro into the runtime `core.useNanosec` config option, letting users enable nanosecond precision without recompiling Git. The series is nearly ready, with only minor documentation and code-cleanup items left.

D. Ben Knoble replies to Junio’s rejection of a runtime warning for `core.useNanosec=true` on `NO_NSEC` platforms. Knoble notes that users with shared `.gitconfig` files can work around the cross-platform issue by using a "site-local" config path to set the option only on supported machines. This reinforces the earlier decision to rely on documentation (not warnings) to explain platform limitations. The exchange doesn’t reopen the warning debate but clarifies how users can avoid the problem entirely.

The series remains on track for `next`. The only remaining tasks are the mechanical cleanups Patrick Steinhardt requested: unconditionally initializing `use_nanosec` to `0`, simplifying preprocessor guards, and updating a missed `USE_NSEC` reference. The conflict with `ty/repo-config-cleanups` in `seen` will need resolution if both topics advance, but that’s a merge-time concern. For now, this is a well-motivated, technically sound change that addresses a long-standing usability gap.