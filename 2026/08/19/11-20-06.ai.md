This is a **test patch** that adds coverage for documented but untested refname restrictions. The review from Patrick Steinhardt is **substantive** and **tested** (implied by his engagement with the test logic).

Patrick accepts the patch’s goal—ensuring the documented rules are enforced—but suggests a small refactoring: replacing the repetitive `invalid_ref` lines with a loop over the forbidden characters. He also notes a minor inconsistency in the test fixture (using `heads/` instead of `refs/heads/`) but does not treat it as a blocker. The `@` edge case is explicitly praised as a valuable addition.

No controversy or deeper technical questions arise; the discussion is confined to test mechanics. The patch remains purely additive, touching only `t/t1402-check-ref-format.sh`.