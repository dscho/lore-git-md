This is a brief reply in the ODB corruption-handling refactoring series. Patrick Steinhardt acknowledges Junio C Hamano’s design feedback on patch 2/7, agreeing to change the return convention for `odb_source_read_object_info()` from a hard-coded positive value (`1`) for missing objects to a more formalized enum-based approach. The reply does not specify whether the enum will use negative values for all error conditions (including missing objects) as Junio suggested, but the concession removes the immediate blocker. This design question—whether missing objects should be signaled via positive or negative return values—remains the central unresolved issue for the series, with Junio’s preference for negative error codes (`-ENOENT`) still unaddressed in the broader context. The thread is now poised for a v2 that will likely formalize the return convention across all ODB sources.