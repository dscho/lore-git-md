This is a test patch for the `git symbolic-ref` command, but the discussion has shifted from a straightforward coverage gap to a deeper design question. Patrick Steinhardt’s reply raises two points: he asks why Nikolaus chose this particular test gap (a curiosity about motivation, not a critique), and more substantively, he questions the rationale behind the differing exit codes when `--quiet` is used.

The patch itself adds two tests to `t/t1401-symbolic-ref.sh` to verify the documented behavior: `git symbolic-ref` exits with code 128 and prints an error message for non-symbolic refs, while `--quiet` suppresses the message and exits with code 1. This is a clear, well-scoped addition that follows existing test patterns. The commit message is concise and references prior art (commit 919eb8ace), making the intent obvious.

Patrick’s feedback, however, introduces a potential controversy. He finds the differing exit codes "puzzling" and suggests that `--quiet` should only affect output, not the exit code. This is not a surface-level nit; it’s a substantive design question that could lead to a behavior change if others agree. He also suggests verifying that stdout is empty in both cases, which would be a minor but useful addition to the tests.

The patch is still under review, and no other feedback has been posted yet. If the exit code discrepancy is deemed unintentional, this could evolve into a bugfix rather than just a test addition. For now, the thread is worth watching for anyone interested in ref plumbing or error-handling consistency.