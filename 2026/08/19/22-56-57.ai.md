**Summarizer brief mode**

D. Ben Knoble acknowledges Junio C Hamano’s reasoning for rejecting a runtime warning when `core.useNanosec=true` is set on a `NO_NSEC` platform. The author notes that users with shared `.gitconfig` files can work around the limitation by using a site-local configuration path to set the option only on supported platforms. This reply does not introduce new technical content or reopen the warning debate; it simply observes that the existing shared-configuration scenario Junio described can be mitigated with per-machine overrides. The series remains on track for the final polish items (unconditional initialization, preprocessor guard simplification, missed reference update, and documentation warning) without further discussion of runtime warnings. Key context for follow-up: the `core.useNanosec` config key, the `#ifdef NO_NSEC` guards that prevent silent corruption, and the shared-config use case.