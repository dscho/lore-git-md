This is Junio C Hamano's review of the second patch in Karthik Nayak's three-patch series optimizing reftable stack reloads during transactions. The patch moves the list lock from `struct reftable_addition` to `struct reftable_stack` to enable future optimizations, but Junio raises a safety concern about nested or concurrent additions on the same stack.

Junio's question centers on whether the API provides sufficient safeguards against misuse. He observes that if a caller already holds a lock (via `reftable_stack_init_addition()`) and then attempts to open a second `reftable_addition` on the same stack, the new `flock_acquire(add->stack->list_lock)` call will fail because the lock is now per-stack rather than per-addition. This would trigger an early return to the `done:` label, where `reftable_addition_close()` releases the lock—potentially disrupting other `reftable_addition` instances working on the same stack. The concern is that the shared lock could be prematurely released, leaving other operations in an inconsistent state.

The review is substantive, engaging with the patch's locking semantics and potential edge cases. Junio does not object to the optimization goal but highlights a possible regression in API safety. The question is narrowly scoped to the lock relocation's implications for concurrent access, and the answer will determine whether the patch needs additional safeguards (e.g., reference counting or explicit error handling for nested additions). No prior discussion of this issue appears in the thread context, making this a new point of contention.