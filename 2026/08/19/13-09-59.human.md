This is the final back-and-forth in the `dk/use-nsec-runtime` series, which replaces Git’s build-time `USE_NSEC` macro with the runtime `core.useNanosec` configuration option. The series is cooking in `next` and is now down to minor polish items.

D. Ben Knoble acknowledges Patrick Steinhardt’s review feedback on the last patch (v3 3/3). The changes are all straightforward: a typo fix in the commit message, a warning in the `core.useNanosec` documentation about platform/filesystem limitations, unconditional initialization of the `use_nanosec` field in `repo_config_values_init()`, and a plan to simplify the `#ifndef NO_NSEC` guards in `is_racy_stat()` to a single ternary expression. Knoble also confirms he will add a note in the documentation about the potential unsafety of enabling `core.useNanosec` on platforms lacking nanosecond support, though he defers adding a runtime warning for now.

The only unresolved question is a missed reference to `USE_NSEC` in `builtin/update-index.c`; Knoble double-checks his local branch and finds only a comment was updated, suggesting Patrick may have spotted a different instance or a stale reference. The rest of the discussion clarifies the project’s current practice for linking to HTML documentation in man pages (using `git --html-path` as the base URL), which Knoble defends as the established style despite some downstream packaging issues.

The series is now technically complete and uncontroversial. The remaining work is mechanical and should not block graduation to `master`. The only loose end is the textual conflict with `ty/repo-config-cleanups` in `seen`, which will need resolution if both topics advance.