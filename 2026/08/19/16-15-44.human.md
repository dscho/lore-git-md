This is a brief but substantive exchange in the ongoing `dk/use-nsec-runtime` series, which replaces the build-time `USE_NSEC` macro with the runtime `core.useNanosec` config option. The series is cooking in `next` and is now in the final polish phase after addressing performance and cross-platform correctness concerns.

Junio responds to Patrick Steinhardt’s question about whether to warn users when `core.useNanosec=true` is set on a platform where `NO_NSEC` is defined (i.e., where nanosecond precision is unsupported). Junio’s position is clear: **no warning**. He argues that users who share a `.gitconfig` across machines with different Git builds (some with `NO_NSEC`, some without) would find a warning obnoxious, as it would trigger constantly on the unsupported platform. This aligns with the series’ design goal of backward compatibility—silently ignoring the setting on `NO_NSEC` platforms rather than complaining.

The exchange is narrowly scoped but touches on a key usability trade-off: safety (alerting users to potentially unsafe configurations) versus convenience (avoiding noise for cross-platform workflows). Junio’s stance reinforces the series’ current approach, which relies on documentation (e.g., `core.useNanosec`’s man page) to communicate platform limitations rather than runtime warnings. No code changes are implied; this is a policy clarification for the final implementation.

For readers tracking the series, this confirms that the patch will **not** include runtime warnings for `NO_NSEC` builds, leaving documentation as the sole mechanism to inform users about filesystem/platform constraints. The decision is uncontroversial and consistent with Git’s general philosophy of avoiding noisy warnings for valid-but-unsupported configurations.