This is a **user support follow-up** in the ongoing textconv regression thread. D. Ben Knoble adds a data point confirming that he cannot reproduce Randall S. Becker’s reported issue—empty temp files for binary files on NonStop—even when testing with files containing NUL bytes. His test case uses `dd` to create a file filled with zeros (a common way to simulate binary content) and a simple `xxd`-based textconv filter to verify the content passed to the filter.

Knoble’s test aligns with Jeff King’s earlier observation: the tempfile (or working-tree file) passed to the textconv filter contains the expected binary content, not an empty file. This reinforces the thread’s emerging consensus that the regression is **platform-specific to NonStop**, likely tied to how that platform’s I/O stack handles null-byte-prefixed files during tempfile creation. The discrepancy between Knoble’s and Randall’s results is now clearly attributable to platform differences rather than file content alone.

No new technical insights or fixes are proposed here; the email serves primarily to narrow the scope of the regression to NonStop-specific behavior. Future discussion will likely focus on debugging the tempfile creation logic on NonStop or identifying platform-specific workarounds. The thread remains unresolved, with the root cause still unexplained.