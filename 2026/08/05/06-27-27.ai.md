This is a bugfix discussion targeting the notes and history-rewriting subsystems.

Patrick Steinhardt replies to D. Ben Knoble’s report that `git replay` and `git history` silently drop Git notes during history rewriting. He confirms that for `git history`—a user-facing command—this is likely an unimplemented feature rather than a deliberate design choice. While he is less certain about `git replay` (which he considers plumbing), he still frames the behavior as something that should eventually be addressed. Patrick has opened a GitLab issue to track the work for the next release cycle but invites community contributions to resolve it sooner.

The email advances the thread by clarifying intent: notes are not intentionally out of scope for `git history`, and the omission is a gap rather than a policy decision. No technical details about implementation or scope (e.g., whether all rewritten commits or only the tip should preserve notes) are provided yet. The discussion remains open-ended, with Patrick’s reply serving as a lightweight acknowledgment rather than a detailed design proposal.