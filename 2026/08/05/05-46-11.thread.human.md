This thread is about a patch from a new contributor, shrimech, that removes the `--interactive` (`-i`) mode from `git add`, leaving only `--patch` (`-p`). The patch is mechanically clean—it removes the code and documentation for `--interactive`—but provides no rationale for why the feature is being removed, whether it’s redundant, unused, or problematic. So far, the only feedback has come from Michael Montalbo, who pointed out that the commit message fails to meet Git’s documented standards for explaining the problem, justifying the solution, and documenting alternatives. His review was procedural, not technical, and didn’t address whether the removal itself is justified.

The patch touches `builtin/add.c` and `Documentation/git-add.txt`, removing all traces of `--interactive` from the command-line parser, error messages, and man page. There are no new symbols or on-disk format changes, and no prior discussion or user feedback was cited. The key open question remains: *why* is this removal being proposed? Without a clear answer, the patch is unlikely to move forward, regardless of its technical correctness. Future replies may now focus on whether the removal is justified at all, rather than on implementation details.