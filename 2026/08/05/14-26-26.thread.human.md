This thread is about a 12-patch series from Johannes Schindelin (Dscho) that upstream Git for Windows-specific build and runtime adjustments into the main Git codebase. The series addresses hard-coded assumptions in the build system, MSYS2 integration, linking behavior, deprecated artifacts, a locale-handling regression, and direct `git.exe` invocation—all without affecting non-Windows platforms.

The series is now at v2, with the only change from v1 being a fix to align Meson’s `MINGW_PREFIX` handling with the Makefile’s. Patches 10 (simplifying path logic for `ETC_GITCONFIG`/`ETC_GITATTRIBUTES`) and 11 (fixing a locale-handling regression) are uncontroversial and ready for integration. Johannes Sixt (Hannes) has been running a subset of these patches in his personal Windows builds since November 2025 without issues, and the full series passes GitHub CI on Windows, providing real-world validation.

The main remaining loose end is a cleanup suggestion from Hannes: the CMake and Meson hunks in Patch 8 introduce compiler definitions that are only used in Patch 12, making them logically misplaced. Junio C Hamano has nudged the thread after three weeks of inactivity, asking whether this discussion can be resolved soon so the series can move forward. The ball is in Dscho’s court to either accept Hannes’s suggestion (moving the hunks to Patch 12) or explain why they should stay where they are. No other objections or requests for changes are outstanding.