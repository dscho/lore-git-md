This is a follow-up in the ongoing `git-replay` and `git-history` notes-preservation thread, specifically a reply to Phillip Wood’s design proposal.

D. Ben Knoble acknowledges Phillip’s suggestion for a command-line option (`--copy-notes`) in `git replay` and agrees that config-based control would be inappropriate for a plumbing command. He then floats the idea of reusing the existing `notes.rewrite.<command>` configuration pattern (which already governs note copying for `git rebase` and `git cherry-pick`) but immediately concedes that Phillip’s point about config being unsuitable for plumbing commands still holds. No new technical direction is proposed; the email is essentially a brief nod to the design discussion and a minor brainstorm that the author himself walks back. The thread remains in the design-clarification phase with no patches or concrete implementation plan yet.