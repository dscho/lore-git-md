This is a **user support follow-up** in the ongoing thread about the Git 2.50 textconv regression on NonStop. D. Ben Knoble adds a data point confirming that the issue does *not* reproduce on his system (likely Linux) even with NUL-prefixed files, reinforcing Jeff King’s earlier observation. His test case uses `dd` to create binary files and a simple shell-based textconv filter (`xxd`) to inspect the content passed to the filter.

Knoble’s test demonstrates that Git correctly passes the working-tree file (for `git diff` with a clean file) or a populated tempfile (for `git diff` after modifying the file) to the textconv filter, even when the file contains null bytes. This aligns with Peff’s explanation of tempfile reuse behavior but does not address the core regression—empty tempfiles for binary files on NonStop. The discrepancy between Knoble’s results and Randall S. Becker’s report remains unexplained, though the thread has narrowed the scope to platform-specific behavior (NonStop’s handling of null bytes during tempfile creation).

The thread is still unresolved, with no root-cause analysis or patch proposed. Future discussion will likely focus on debugging NonStop-specific I/O behavior or gathering more evidence from other platforms. For now, the regression appears confined to NonStop and binary files, but the lack of reproduction elsewhere leaves the issue in limbo.