This is a brief follow-up in the review thread for Toon Claes’s four-patch series optimizing `git last-modified` with Bloom filters. The series is stalled on a maintainability concern raised by Jeff King (Peff) about patch 4/4, which ensures wildcard pathspecs (e.g., `*.c`) continue to benefit from Bloom-filter acceleration.

Taylor Blau responds to Junio C Hamano’s question about whether the discussion has concluded. Taylor defers to Peff and Toon, noting that he lacks strong context on this sub-thread, but reiterates that his earlier review comment—regarding the correctness of the `--show-trees` output when ancestor tree entries are not covered by the original pathspec—must be resolved before the series can proceed. That issue was previously addressed in principle (via a `match_pathspec()`-based fix), but Taylor’s reminder suggests it may still need explicit confirmation or implementation in a reroll. The unresolved maintainability concern about `bloom_filter_settings` management remains the primary blocker.