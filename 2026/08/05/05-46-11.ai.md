This is a **feature removal** patch that deletes the `--interactive` (`-i`) mode from `git add`, leaving only `--patch` (`-p`) for interactive staging. The patch removes the option from the command-line parser, updates error messages to reference only `--patch`, and strips all interactive-mode documentation from the man page, including the entire "Interactive mode" section and related examples.

The commit message is minimal: it states the removal and notes that `--patch` remains available, but does not explain why the removal is justified or whether there is any user demand for it. The patch itself is mechanically clean—it deletes code and documentation without introducing new behavior—but the rationale is entirely missing. There is no discussion of whether the feature is unused, redundant, or problematic, nor any reference to prior discussion or user feedback.

This looks like a drive-by removal rather than a considered project decision. The lack of motivation and the absence of any sign-off from a regular contributor suggest it may struggle to gain traction. If this were part of a broader effort to simplify the UI, the cover letter would likely have mentioned it; as a standalone patch, it feels under-motivated and likely to be controversial.