This is a minor follow-up in an already approved documentation patch series. The thread is about Kristoffer Haugsbakk’s improvements to the `git-refs(1)` man page, specifically the placement and styling of a warning about ref migration limitations. The series itself is uncontroversial, queued in `seen`, and expected to graduate to `next` shortly.

In this exchange, Patrick Steinhardt and Kristoffer Haugsbakk briefly discuss the choice between `CAUTION` and `WARNING` for the AsciiDoc admonition block that elevates the ref migration warning. Patrick suggests that `WARNING` might be more appropriate, given the admonition’s content: it describes a risk of inconsistent state due to concurrent writes, not just a limitation. The distinction is subtle but aligns with AsciiDoc’s documented semantics (`WARNING` for lingering danger, `CAUTION` for careful action). Kristoffer had originally chosen `CAUTION` without a strong rationale, and this reply does not dispute Patrick’s point.

The discussion is purely surface-level and does not affect the series’ integration status. No code or documentation changes are proposed or needed. The thread remains fully resolved, with the patches approved and queued as-is. This is a low-stakes, after-the-fact observation that won’t delay or alter the outcome.