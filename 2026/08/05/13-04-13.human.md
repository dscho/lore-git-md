This is a bugfix discussion targeting the notes and history-rewriting subsystems, specifically clarifying design intent for how Git commands should handle notes.

Junio C Hamano joins the thread to recall a past discussion about why `git rebase` and `git cherry-pick` behave differently with respect to notes, framing that difference as intentional and beneficial. His comment suggests that the silent dropping of notes in `git replay` and `git history` may not be an oversight but rather a deliberate choice aligned with existing plumbing commands. This introduces a new perspective: the current behavior might be correct for `git replay` (as plumbing) while still leaving open whether `git history` (as a user-facing command) should preserve notes by default. The thread now has competing interpretations—Patrick Steinhardt sees the omission as an unimplemented feature, while Junio implies it could be a principled design decision. No patches or concrete proposals have emerged yet, but the discussion is sharpening the question of what the expected behavior should be.