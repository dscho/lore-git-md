This is the first patch in a three-part refactoring series targeting Git’s repository configuration handling in `environment.c`. The patch simplifies the getter functions for repository config values by removing redundant parentheses and unnecessary NULL checks on the `repo` parameter.

The change is straightforward: the getter functions (`repo_protect_ntfs`, `repo_protect_hfs`, and `repo_ignore_case`) now assume the caller passes a non-NULL `repo` pointer, which aligns with their actual usage elsewhere in the codebase. The logic remains functionally identical, but the code is cleaner and slightly more efficient. The patch touches only `environment.c` and makes no changes to function signatures, on-disk formats, or behavior.

This is a well-scoped refactoring that improves readability without introducing risk. The commit message clearly explains the rationale, and the diff is minimal and focused. Since the series is purely about code organization and documentation, this patch is unlikely to spark controversy, though reviewers may still weigh in on whether the NULL-check removal is sufficiently justified by the call-site guarantees.

The patch is part of Tian Yuchen’s ongoing effort to clean up repository config handling, and it follows the project’s preference for small, self-contained improvements. If the series lands, this will be a minor but welcome cleanup in a core subsystem.