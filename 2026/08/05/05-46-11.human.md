This is a **feature removal patch** that deletes the `--interactive` (`-i`) mode from `git add`, leaving only the `--patch` (`-p`) mode for interactive staging.

The patch removes the `-i`/`--interactive` option from the command-line parser and strips all documentation of the interactive-mode workflow, which previously offered a menu-driven interface for staging changes, reverting hunks, and adding untracked files. The `--patch` mode, which lets users select individual hunks, is explicitly preserved. The commit message states no motivation beyond the existence of `--patch` as an alternative; it does not explain why the interactive mode is redundant, underused, or problematic.

The diff is clean and mechanical: 139 lines removed from the man page and 17 lines adjusted in `builtin/add.c`. No tests or compatibility considerations are mentioned. Given the lack of rationale and the absence of any discussion of migration paths or user impact, this patch looks under-motivated and likely to be controversial. It is a thread root with no prior context.