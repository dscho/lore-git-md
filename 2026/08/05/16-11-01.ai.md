This is a **maintainer response** to the proposed removal of `--interactive` (`-i`) mode from `git add`. Junio C Hamano rejects the patch's justification as fundamentally inadequate, comparing it to a hypothetical (and absurd) proposal to remove `git add` entirely on the grounds that `git diff` still exists. He emphasizes that breaking changes require far stronger justification than opt-in features, and dismisses common weak rationales like personal disuse or unfamiliarity with the feature.

The email does not engage with the patch's implementation (which was mechanically clean) but focuses entirely on the lack of a compelling rationale. Junio's tone suggests the removal is unlikely to proceed without evidence of widespread user harm, redundancy, or a clear migration path—none of which were provided. The thread remains stalled on procedural and motivational grounds, with no indication that the author has addressed the core critique. Future discussion would need to either (1) demonstrate that `--interactive` is actively harmful or unused at scale, or (2) abandon the removal effort.

Key technical context for follow-ups: the patch touches `builtin/add.c` and `Documentation/git-add.txt`, removing the interactive-mode parser and documentation while leaving `--patch` (`-p`) intact. No on-disk format or API changes are involved. The thread has not yet surfaced any prior discussion or user feedback about `--interactive`'s utility.