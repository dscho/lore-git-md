This is a **user support response** from Jeff King (Peff) addressing Randall S. Becker’s reported regression in Git’s textconv feature. Peff clarifies the intended behavior of tempfile reuse in textconv filters but does not reproduce the core issue (empty temp files for binary files on NonStop).

Peff explains that Git sometimes reuses the working-tree file instead of creating a tempfile as an optimization, but only when the file is clean and one diff endpoint is the index. He demonstrates this with a test case: `git diff HEAD^` or `git diff --cached HEAD^` will pass the working-tree file to the textconv filter, while `git show HEAD` will always use tempfiles. This behavior is not new and is not the root cause of Randall’s issue, though it accounts for the earlier inconsistency where Randall saw working-tree paths in some cases.

The key takeaway is that Peff’s response does *not* address the regression itself (empty temp files for binary files on NonStop). He confirms he cannot reproduce the problem even with files containing NUL bytes, leaving the root cause unresolved. The thread remains open, with the regression still unexplained and no patch or workaround proposed. Future discussion is likely to focus on platform-specific I/O behavior (e.g., NonStop’s handling of null bytes) or deeper debugging of the tempfile creation logic.