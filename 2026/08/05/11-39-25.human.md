This is a follow-up in an ongoing bugfix discussion about Git notes being silently dropped during history rewriting by `git replay` and `git history`.

D. Ben Knoble and Patrick Steinhardt briefly exchange recollections of a similar issue—possibly a recent rebase bug—but neither can pinpoint the exact thread. The conversation reaffirms the earlier consensus: for the user-facing `git history` command, notes should be preserved automatically, and the current behavior is an unimplemented feature rather than a deliberate design choice. For `git replay`, which Patrick considers plumbing, the status remains less clear, though the implication is that it too should eventually handle notes.

No new technical details or implementation plans emerge here; the email is a lightweight acknowledgment that the issue is still open and that community contributions would be welcome. The thread remains in the "under review" phase, with the GitLab issue serving as the tracking mechanism for future work.