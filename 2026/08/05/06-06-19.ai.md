This is a **brief follow-up in the ODB streaming API unification series**, specifically addressing a naming consistency question raised in Justin Tobler’s review of Patch 5/7. The email is a direct reply to Justin’s feedback about whether `struct input_zstream_data` (in `builtin/unpack-objects.c`) should be renamed to better reflect its new role as a stream after embedding `struct odb_stream`.

Patrick acknowledges the point and confirms the rename was intentionally deferred to minimize churn in Patch 5/7. He now plans to add a new commit to the series to address this, though the exact name (e.g., `input_zstream_stream`) is not yet specified. The change is purely cosmetic and does not affect functionality, but it aligns with the series’ broader goal of eliminating redundancy and clarifying abstractions. This adjustment is unlikely to attract controversy, though it may prompt minor follow-up discussion about naming conventions in the unified API.

The thread remains focused on the critical correctness issue in Patch 3/7 (the in-memory ODB backend bug), which is still the primary blocker for the series. This rename is a secondary concern and does not interact with the bug or other open questions. Key symbols to track: `struct input_zstream_data` (target of the rename), `builtin/unpack-objects.c`, and the unified `struct odb_stream` base.