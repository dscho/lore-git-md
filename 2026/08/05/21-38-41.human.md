This is a **refactoring** patch in Tian Yuchen’s three-part series to clean up repository configuration handling in `environment.c` and `environment.h`. Junio’s reply to the second patch raises two pointed questions about the documentation change.

The patch replaces three function-specific comments with a single unified block describing how repository config getters (`repo_protect_hfs`, `repo_protect_ntfs`, and `repo_ignore_case`) handle uninitialized repositories by returning default values. Junio notices that the new comment block sits above only the first two functions, leaving `repo_ignore_case` visually orphaned by a blank line, which could mislead readers into thinking it behaves differently. He also flags a potential contradiction: the new comment says the getters work in “non-Git environments,” yet the first patch in the series removed NULL checks, meaning a NULL `repo` argument would now crash instead of returning a default.

The questions are crisp and technical, not nitpicks. They highlight a real risk of confusion or regression if the documentation and code drift apart. No resolution is offered yet; the ball is back in Tian’s court to clarify whether the unified comment is meant to cover all three getters and, if so, whether the NULL-handling assumption in patch 1 needs revisiting.