Thread identity: Bug report about credential helper protocol regression during HTTP 301 redirects. Originating author: Adien Akhmad. Thread type: bug report.

Core problem or goal: When `git push` follows a 301 redirect, the `wwwauth[]` attribute (containing the WWW-Authenticate header) is stripped from credential helper input, breaking helpers like `git-credential-oauth` that rely on this header to detect authentication schemes. The expected behavior is that `wwwauth[]` is preserved across redirects, matching the behavior of direct (non-redirect) requests.

Current status: Reported as a regression; no patch or proposed fix yet. No prior discussion or versions.

Key technical details:
- Subsystems: credential helper protocol, HTTP transport (libcurl 8.15.0).
- Affected attribute: `wwwauth[]` (WWW-Authenticate header).
- Trigger: HTTP 301 redirect followed by 401 response.
- Test case: `git push` to a Forgejo instance that 301-redirects before responding with 401/WWW-Authenticate.
- On-disk format: N/A.
- New/renamed symbols: None.

Open questions and loose ends:
- Root cause: Is the issue in Git's credential helper protocol implementation or in how HTTP responses are processed during redirects?
- Regression scope: When was the behavior change introduced? Is it specific to certain libcurl versions?
- Fix approach: Should the `wwwauth[]` attribute be explicitly preserved during redirects, or is there a deeper issue in the HTTP response handling?

Participant positions: None yet (thread root).

Related work: None mentioned.