This is a reply in the ongoing Coverity unchecked-return-values series, specifically addressing the tenth patch that adds error checks to all call sites of `get_terms()` in `builtin/bisect.c`. Johannes Schindelin agrees with Patrick Steinhardt’s suggestion to centralize error handling in `get_terms()` itself rather than checking the term pointers at each call site. The change would make the code more consistent with `bisect_terms()` and deduplicate error messages.

The discussion is now settled on the design: `get_terms()` will return an error if either term is `NULL`, and callers will check only the return value. This is a clean, low-risk improvement that aligns with the series’ goal of hardening error paths without changing behavior. The patch is likely to be revised accordingly; no further controversy or open questions remain on this point.