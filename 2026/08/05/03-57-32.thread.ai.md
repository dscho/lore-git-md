Here is the updated AI thread summary:

Thread identity: bugfix series for `git maintenance` geometric repack task with promisor packs; 2-patch series; originating author Taylor Blau; thread type patch series.

Core problem or goal: fix a regression in `git maintenance`'s geometric repack task where promisor packs were not being handled correctly after they gained their own geometric progression support (dcc9c7ef47, 2026-01-05). The series ensures that the maintenance task properly evaluates both ordinary and promisor pack progressions before deciding whether to fall back to all-into-one repack, and adjusts the `--auto` trigger to consider promisor pack geometry. The discussion has expanded to address a potential edge case where the current approach might under-trigger all-into-one repacks, risking unreachable objects not being pruned.

Current status: under review; v1 posted. No prior versions. The series consists of two patches:
1. Modify the maintenance task to evaluate both ordinary and promisor pack progressions, falling back to all-into-one only when neither progression keeps a pack above its split threshold. This patch is now under deeper scrutiny for its handling of the all-into-one repack fallback.
2. Adjust the `--auto` trigger to consider `geometry.promisor_split`, ensuring the task runs when promisor packs are ready for rollup. This patch remains uncontested.

Key technical details:
- Files touched: `builtin/gc.c` (core logic for both patches), `t/t5331-pack-objects-stdin.sh` (minor test adjustment to disable auto-maintenance in filtered clone fixture), `t/t7900-maintenance.sh` (new test coverage for promisor-specific logic in both patches).
- New behavior: promisor packs are now evaluated independently for geometric progression, and the `--auto` trigger accounts for promisor pack geometry. The repack mode decision logic now checks `geometry.promisor_split` alongside `geometry.split`. The auto-condition in `geometric_repack_auto_condition()` was updated to include `geometry.promisor_split` in its evaluation.
- Test coverage: demonstrates the fix by creating three promisor packs (two small, one large) and confirming that the two smaller ones roll up while the large one remains untouched. Uses `GIT_TRACE2_EVENT` to validate the correct `git repack` command is invoked. The second patch adds explicit test coverage for the `--auto` trigger behavior with promisor packs, including a test that verifies the trigger activates when smaller promisor packs should roll up.
- Subsystems: `git maintenance`, geometric repacking, promisor packs.
- Symbols: `geometry.promisor_split` (config key), `geometry.split` (config key), `geometric_repack_auto_condition()` (function), `geometry.pack_nr` (config key).

Open questions and loose ends:
1. **All-into-one repack edge case**: Patrick Steinhardt raised a concern that the current logic in patch 1 might under-trigger all-into-one repacks when the combined set of ordinary and promisor packs is large enough to warrant a single-pack rollup, but neither progression alone crosses the threshold. This could leave unreachable objects unpruned. Taylor Blau initially responded that the current approach is intentional (geometric repacks are performed independently on promisor and non-promisor packs) and that the existing logic already handles this scenario (e.g., if non-promisor packs meet `geometry.split == geometry.pack_nr`, a single-pack rollup will still occur). Patrick later elaborated that the issue is more fundamental: the fallback to all-into-one repacks might never trigger because it’s unlikely both progressions will simultaneously meet the threshold. He suggested an alternative approach: triggering all-into-one repacks when the objects involved exceed a certain percentage of the repository size, rather than relying solely on geometric progression thresholds. The discussion remains open; no code changes have been proposed yet.
2. **Trade-off between geometric efficiency and garbage collection**: The edge case discussion highlights a tension between maintaining geometric progression efficiency and ensuring timely garbage collection. The outcome could shape whether the patch needs refinement or if the current behavior is deemed acceptable for now.

Participant positions:
- **Patrick Steinhardt**: substantive review of both patches. Acknowledges the problem and accepts the proposed fix but identifies a potential edge case in patch 1 where the current logic might under-trigger all-into-one repacks, risking unreachable objects not being pruned. Proposes a conceptual alternative (triggering all-into-one repacks based on repository size percentage) but has not yet requested specific code changes. Explicitly deemed patch 2 "obviously correct."
- **Taylor Blau**: defends the current approach in patch 1, arguing that the edge case is already handled by the existing logic (e.g., non-promisor packs meeting `geometry.split == geometry.pack_nr` will still trigger a single-pack rollup). Acknowledges Patrick’s review of patch 2 with no objections. Has not yet responded to Patrick’s elaboration on the edge case.

Related work: regression introduced by dcc9c7ef47 (2026-01-05), which added geometric progression support for promisor packs. The current series is a targeted fix for the behavioral regression caused by that change. The edge case discussion may influence future work on geometric repacking logic.