This is a follow-up in the ongoing discussion about `git-replay` and `git-history` silently dropping Git notes during history rewriting. Phillip Wood joins the thread to confirm that a similar note-handling bug was recently reported for `git rebase`, though no details or references are provided. He agrees with Patrick Steinhardt that `git-history` (a user-facing command) should handle notes automatically, and suggests that `git-replay` (a plumbing command) could benefit from a command-line option to enable note copying, rather than relying on configuration. The shared implementation would likely live in common code, making it easier to maintain consistency across commands.

The discussion remains focused on design intent rather than implementation specifics, with no patches or concrete proposals yet. The tone is collaborative, and the issue is still seen as an unimplemented feature rather than a critical bug. The GitLab issue opened by Patrick Steinhardt remains the only formal tracking mechanism, and the thread continues to invite community contributions. Key questions—such as whether all rewritten commits or only the tip should preserve notes, and how this compares to other history-rewriting commands—remain unanswered.