Thread identity
Subject: `git rebase -x` with `git commit --amend` loses notes
Series version: standalone bug report (no patch series)
Originating author: D. Ben Knoble
Thread type: bug report

Core problem or goal
`git rebase -x` (with an external command) combined with `git commit --amend` fails to rewrite notes attached to the tip commit, even when `notes.rewriteref` is configured to include the relevant notes ref (`refs/notes/benknoble/commits`). The bug is specific to the interaction between `-x` and `--amend`: plain `--amend` or plain rebase works correctly.

Current status
Root cause confirmed; three proposed fix strategies now exist. Phillip Wood diagnosed the issue: the rebase machinery records the new commit OID *before* the external command runs, so `git commit --amend` creates a new OID that the notes-rewriting step (post-rebase) does not map correctly. The notes are copied from the old OID to the pre-amend OID, not the post-amend one, making them appear lost. Three approaches are now under discussion:
1. **Post-amend update** (prior work): Update the rebase’s internal `rewritten` file after `git commit --amend` (Phillip’s stalled `wip/rebase-update-rewritten` branch). This worked for simple `exec` amend cases but stalled on interactive editing robustness.
2. **Pre-exec copy** (new proposal): Copy notes *before* stopping for an `exec` or `edit` command, allowing `git commit --amend` to handle note copying directly to the amended commit. This avoids OID-mapping issues but may complicate conflict handling during interactive editing.
3. **External OID mapping** (new proposal): Expose rebase’s internal old-to-new OID mapping to `exec` commands or post-rebase tooling, enabling external note copying without modifying rebase internals. This could avoid both the OID-mapping issue and interactive editing complications but would require new plumbing to expose the mapping safely.

Key technical details
- Subsystems: rebase, notes
- Commands: `git rebase -x`, `git commit --amend`, `git notes copy`, `git range-diff`
- Config keys: `notes.rewriteref`, `core.notesref`
- Notes ref: `refs/notes/benknoble/commits`
- Files touched: rebase state directory (`rewritten` file), potential new plumbing for OID mapping exposure
- Behavior: notes are lost only when `-x` and `--amend` are combined; plain `--amend` or plain rebase works as expected
- On-disk: notes ref is not updated during rebase because the `rewritten` file maps the old OID to the pre-amend OID, not the post-amend one
- New detail: Exposing the OID mapping could enable external workarounds without modifying rebase internals, but may require new command-line or environmental interfaces

Open questions and loose ends
- Test coverage: no minimized test case yet; reproduction sketch provided but not formalized
- Regression: unclear whether this is a long-standing edge case or a recent regression
- Fix completeness: None of the three approaches has been fully implemented or tested for edge cases (e.g., interactive editing conflicts, performance impact of pre-exec note copying, safety of exposing OID mappings)
- Implementation trade-offs:
  - Post-amend updates may reintroduce robustness issues with interactive editing
  - Pre-exec copying could complicate conflict resolution during interactive rebase
  - External OID mapping may require new plumbing and could introduce security or usability concerns
- No consensus on which approach to pursue; discussion remains conceptual

Participant positions
- D. Ben Knoble: reporter; provided configuration, reproduction sketch, and observed behavior. Proposed two new conceptual workarounds (external OID mapping exposure and post-rebase note copying) as alternatives to Phillip’s earlier strategies. Prefers lighter-weight solutions that avoid modifying rebase internals but acknowledges potential trade-offs.
- Phillip Wood: identified root cause; proposed two fix strategies:
  1. Post-amend update (prior work, stalled on interactive editing)
  2. Pre-exec note copying (new proposal, conceptual). Prefers the latter for its simplicity but acknowledges potential complications with interactive editing. Has not yet responded to the new external OID mapping proposal.

Related work
- Phillip Wood’s `wip/rebase-update-rewritten` branch (GitHub fork)