This is a **brief follow-up in the ODB streaming API refactoring series**, specifically addressing a minor documentation oversight in Patch 3/7. The email confirms Patrick Steinhardt has updated the patch to fix a stale comment in `odb/transaction.h` that Justin Tobler identified as outdated after the patch added support for streaming arbitrary object types (not just blobs). The comment previously assumed blob-only streaming and now reflects the new generic infrastructure.

The change is purely cosmetic and does not affect functionality or the critical correctness issue Junio C Hamano raised in the same patch (the in-memory ODB backend still computes object names using `OBJ_BLOB` instead of the intended `stream->type`). This fix resolves one of the loose ends noted in the thread summary but leaves the blocker bug untouched. The update is a routine housekeeping step in response to review feedback, with no broader implications for the series’ design or timeline. Files touched: `odb/transaction.h` (comment update). Symbols: `write_object_stream` callback.