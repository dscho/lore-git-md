This is the second patch in Tian Yuchen’s three-patch refactoring series that tidies up repository-configuration handling in `environment.c` and `environment.h`. It replaces three function-specific comments with a single, unified block that describes the common behavior of all repository-config getter functions: when the repository is not fully initialized, they return the appropriate default values instead of calling `repo_config_values()`. The change is purely documentary—no code or signatures are altered—so it carries no risk of behavioral regression. The patch is small (3 insertions, 8 deletions) and self-contained, making it easy to review in isolation.