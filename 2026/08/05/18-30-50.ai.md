This is the first patch in the v2 series addressing Coverity-identified unchecked return values. It fixes a potential crash in the HTTP transport layer by adding a NULL check for `curl_easy_duphandle()` in `get_active_slot()`.

The patch is small and focused: it adds a two-line guard that calls `die()` if `curl_easy_duphandle()` fails, preventing undefined behavior when `curl_easy_setopt()` is later called with a NULL handle. The fix is uncontroversial and aligns with the series' goal of hardening error paths without changing behavior. The commit message clearly explains the risk (memory allocation failure leading to a crash) and the rationale for `die()` (no reasonable recovery from out-of-memory during HTTP operations).

This patch touches only `http.c` and introduces no new symbols or on-disk format changes. The existing test suite for HTTP operations (clone, fetch, push) will implicitly exercise the new error path. No review feedback on this specific patch was noted in the thread context, but the broader series has been thoroughly vetted for correctness. The fix is ready for integration.