This is a reply in the ODB abstraction series, specifically about the third patch ("setup: defer object database creation"). Patrick Steinhardt responds to Toon Claes’s alternative implementation with a counter-proposal: instead of wrapping environment variable handling in a new helper (`setup_objects_odb_new()`), move it directly into `odb_new()`. This change would eliminate the awkward double-pointer passing in the current patch while also preparing the codebase for a planned follow-up: moving environment variable handling into the ODB backend itself.

The proposal is narrow and technical, touching only the initialization sequence. It does not alter the series’ core design (deferring ODB creation to enable pluggable backends) but refines the implementation to better align with future work. The linked thread ([1]) suggests this is part of a broader effort to consolidate ODB-related environment variables (`DB_ENVIRONMENT`, `ALTERNATE_DB_ENVIRONMENT`) into the "files" backend, which would further reduce coupling between repository setup and backend-specific logic.

Patrick’s reply is brief and pragmatic, focusing on code clarity and forward compatibility. The change is uncontroversial and does not introduce new technical questions, though it leaves open whether Toon will accept the alternative. The patch remains under review, with no merge status reported. The broader ODB abstraction effort continues to advance, and this discussion is a small but meaningful step toward cleaner backend initialization.