This is a **user support follow-up** in the ongoing thread about a **platform-specific regression in Git’s textconv feature** affecting NonStop. Randall S. Becker proposes a workaround to address the ambiguity caused by the regression: extending the textconv filter interface to support a `%f` placeholder that would expand to the original file path in the repository.

The suggestion is straightforward: if Git’s textconv configuration allowed filters to reference the original file path (e.g., via `%f`), users could bypass the empty tempfile issue entirely by having their filters locate and read the file directly. This would restore the functionality broken by the regression—where textconv filters receive empty tempfiles for binary files (those beginning with a null byte) on NonStop—without requiring a fix to the underlying I/O behavior. The proposal is pragmatic and targeted, focusing on a user-facing solution rather than a deep technical fix.

The context here is important: the regression is confirmed to be **NonStop-specific and tied to null-byte handling during tempfile creation**, and neither Peff nor D. Ben Knoble could reproduce it on other platforms. Randall’s earlier messages established that the breakage disrupts workflows relying on the original file path, so this workaround directly addresses the pain point. However, it would require changes to Git’s textconv interface, which may raise questions about backwards compatibility or whether this is the right layer to solve the problem.

The thread remains unresolved, with no maintainer response yet. Randall’s proposal is a constructive step toward a solution, but it’s unclear whether the project will prioritize a user-facing workaround over a root-cause fix (e.g., platform-specific I/O adjustments). For now, this email advances the discussion by offering a concrete, implementable idea—one that could unblock NonStop users while the deeper issue is investigated. If you’re tracking Git’s platform compatibility or textconv behavior, this is worth noting as a potential path forward.