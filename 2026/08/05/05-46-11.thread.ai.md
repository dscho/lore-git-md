Thread identity: standalone patch removing `--interactive` (`-i`) mode from `git add`, leaving only `--patch` (`-p`). Author: shrimech (new contributor). Thread type: feature removal.

Core problem or goal: delete the `--interactive` option from `git add` without providing a rationale for why the feature is being removed or whether it is redundant, unused, or problematic.

Current status: patch posted; no prior discussion or user feedback cited. Mechanically clean (removes code and documentation) but motivation entirely absent. No sign-off from a regular contributor. Under review.

Key technical details: removes `--interactive` from command-line parser, updates error messages to reference only `--patch`, strips all interactive-mode documentation from `git-add(1)` (including the "Interactive mode" section and related examples). No new symbols or on-disk format changes. Files touched: `builtin/add.c`, `Documentation/git-add.txt`.

Open questions and loose ends: why is `--interactive` being removed? Is it unused, redundant, or problematic? Is there user demand for this change? Is this part of a broader UI simplification effort? No prior discussion or user feedback referenced.

Participant positions: none yet; patch is from a new contributor without sign-off or review.

Related work: none mentioned.