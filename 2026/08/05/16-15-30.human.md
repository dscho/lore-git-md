This is a **maintainer comment** on Vincent Mailhol’s patch adding bash completion for `git history`. Junio C Hamano steps back from the technical specifics to observe a recurring pattern in the completion code: the boilerplate logic for resolving and completing subcommands is nearly identical across multiple `_git_*` functions. He suggests that, when the project is less busy, these could be refactored into a shared helper to improve maintainability. The comment is brief, constructive, and framed as a future cleanup rather than a blocker for this patch. It does not engage with the patch’s correctness or the earlier discussion about the `split` subcommand’s pathspec handling.