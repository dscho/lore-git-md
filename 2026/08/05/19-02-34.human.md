This is a documentation patch series (v2) from Kristoffer Haugsbakk, improving the `git-refs(1)` man page. The series is now approved and queued for integration, with only a minor stylistic update applied since v1.

The core goal remains the same: elevate a critical warning about ref migration limitations (concurrent writes can corrupt state) from a buried "KNOWN LIMITATIONS" section into a prominent admonition block directly under the `migrate` subcommand. This makes the warning impossible to miss for users considering a ref storage migration. The second patch adds a missing `linkgit` cross-reference to `git-maintenance(1)` in the same warning text, improving navigation in HTML and man page output.

The v2 update addresses the only remaining feedback: switching the admonition block from `CAUTION` to `WARNING` to better align with AsciiDoc’s documented semantics. This is a purely stylistic change—no substantive discussion is needed. The series touches only `Documentation/git-refs.txt` and carries Junio’s explicit approval ("Looks good. Thanks.") from v1. It is now queued in `seen` and expected to graduate to `next` in the next "What's cooking" cycle.

The patches are well-motivated and uncontroversial. The warning’s relocation is a clear usability win, and the cross-reference addition follows established documentation patterns. No code or behavior changes are involved, and the commit messages are thorough. This is a straightforward documentation improvement that looks ready for `master`.