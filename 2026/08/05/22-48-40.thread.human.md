Here’s the human-mode summary for a developer catching up on the thread:

This is Junio C Hamano’s "What's cooking" report from early August 2026, tracking the status of all in-flight topics in Git’s integration branches. Since the last report, six topics have graduated to `master`, including fixes for CI static analysis, a segfault in `git diff --relative --cached`, and a Windows symlink credential leak, along with Patrick Steinhardt’s pluggable object database housekeeping work. New topics include Steinhardt’s `ps/odb-streams` (unifying ODB streaming APIs), brian m. carlson’s Git 3.0 prep to restrict hex object IDs to lowercase, and Johannes Schindelin’s Windows build updates.

The report highlights 40+ topics cooking in `next` or stalled, with key efforts around pluggable ODBs, performance optimizations (like Bloom filters for `git last-modified`), and new features such as `git add --resolved` and `git branch --delete-merged`. Several topics are waiting for review or rerolls, including Taylor Blau’s packfile hardening and Toon Claes’s `git replay --linearize`. Pablo Sabater recently noted that `ps/cat-file-remote-object-info-type`—an extension to add `%(objecttype)` support—can now be rebased directly onto `master` after its base topic graduated, removing a logistical hurdle for review.

Stalled topics like `tb/repack-geometric-cruft` (test failures) and `hn/checkout-track-fetch` (marked for discard) remain unresolved, while `fz/rebase-autosquash-empty` was ejected due to conflicts. The report also flags ongoing work for Git 3.0, Windows improvements, and documentation modernization. Junio’s summary remains the authoritative snapshot of the project’s integration pipeline.