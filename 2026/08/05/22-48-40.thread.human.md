Here’s a concise narrative summary of the Git mailing list thread covering Junio’s "What’s cooking" report for early August 2026:

The thread is Junio C Hamano’s regular "What’s cooking" report, providing a snapshot of the Git project’s integration pipeline as of early August 2026. Since Git 2.55, 401 commits have graduated to `master` (the next release), with 55 topics currently cooking in `next` and 235 in `seen`. Key recent merges to `master` include fixes for CI static analysis, segfaults in `git diff`, Windows symlink credential leaks, and rebase `fixup`/`squash` bugs, alongside foundational work like pluggable object database (ODB) housekeeping and branch deletion warnings for active bisects.

Several new topics are under review, including Patrick Steinhardt’s `ps/odb-streams` (unifying ODB streaming APIs), brian carlson’s `bc/restrict-hex-to-lowercase` (preparing for Git 3.0’s breaking changes), and Johannes Schindelin’s Windows build updates. Some stalled topics, like geometric cruft repacking and checkout fetch tracking, remain inactive. In `next`, notable efforts include new features like `git add --resolved` (staging conflict-resolved paths), `git branch --delete-merged`, and `git history squash`, alongside performance optimizations (e.g., Bloom filters for `git last-modified`) and code hygiene fixes. The ODB abstraction work continues to dominate architectural changes, while Git 3.0 prep and Windows-specific improvements are recurring themes. Open questions remain on unresolved test failures, reviewer feedback, and rerolls for topics like `kj/repo-info-more-path-keys` and `tc/replay-linearize`.