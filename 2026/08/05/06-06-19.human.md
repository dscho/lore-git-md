This is a brief follow-up in Patrick Steinhardt’s ODB streaming API unification series, specifically addressing Justin Tobler’s review of **Patch 5/7**. The exchange clarifies a naming question raised during review: Justin suggested renaming `struct input_zstream_data` (in `builtin/unpack-objects.c`) to better reflect its new role as a stream after it was refactored to embed the unified `odb_stream` base. Patrick responds that he intentionally deferred renames to minimize churn in this patch, but agrees to add a separate commit to handle the rename. This keeps the series focused on structural unification while ensuring naming consistency in a later patch.

The discussion is purely mechanical and does not affect the series’ core correctness or design. The planned rename will likely appear in a future iteration (e.g., `v2`), aligning with the broader goal of eliminating redundancy in the ODB streaming API. The series remains blocked on the critical bug in **Patch 3/7** (incorrect object type handling in the in-memory backend), which Junio C Hamano identified as a blocker. No other technical concerns are raised here.