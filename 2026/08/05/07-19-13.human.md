This is a bugfix patch targeting the `git push` command. The author, Xavier Morel, reports that `git push --delete --porcelain` behaves inconsistently when deleting refs that don’t exist on the remote. Instead of producing machine-readable output as documented for the `--porcelain` flag, it falls back to human-readable error messages. The expected behavior would be to emit a structured line like `!	:refs/heads/<branch>	[remote failure]` to maintain consistency with the porcelain format.

The issue is straightforward: the porcelain mode is meant to provide predictable, parseable output for scripts, but this edge case breaks that contract. The commit message doesn’t include a proposed fix, but the problem is clearly described and reproducible. This is a small but meaningful fix for users who rely on `--porcelain` for automation.

No prior discussion or cover letter is referenced, so this appears to be a standalone patch. The author’s tone suggests this might have been overlooked during earlier work on porcelain mode, but the report is precise and actionable. If this gets picked up, it’s likely to be uncontroversial.