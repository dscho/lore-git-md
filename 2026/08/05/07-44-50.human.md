This is a refactoring patch, part of Patrick Steinhardt’s ongoing ODB abstraction effort to unify the streaming API. It is the sixth patch in the v2 series of eight, which consolidates `odb_write_stream` and `odb_read_stream` into a single `odb_stream` structure.

The patch renames `struct read_object_fd_data` to `struct fd_stream` in `odb/streaming.c` and updates the associated callback functions (`read_object_fd` → `fd_stream_read`, `close_object_fd` → `fd_stream_close`). The rename reflects the structure’s expanded role: it now holds both the stream state and the file descriptor, aligning with the unified API introduced in earlier patches. The change is purely mechanical—no behavior or logic is altered—and touches only one file (`odb/streaming.c`).

This is a straightforward, well-motivated cleanup that follows naturally from the preceding patches. The commit message clearly explains the rationale, and the diff is minimal and focused. There are no open questions or edge cases here; the patch simply updates names to match the new design. Reviewers are unlikely to object, though they may check for completeness (ensuring no lingering references to the old name remain elsewhere). The series as a whole is under active review and looks like a strong candidate for `next`.