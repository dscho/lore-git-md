This is a **user support proposal** in the ongoing NonStop-specific textconv regression thread. Randall S. Becker suggests a new feature to address the workflow breakage caused by Git 2.50’s regression: extending the textconv filter interface to accept a `%f` placeholder that would expand to the original file path in the working tree.

The proposal is a direct response to the confirmed regression—empty temp files for binary files (those beginning with a null byte) on NonStop—where textconv filters can no longer locate repository files by name. Randall frames `%f` as a workaround for the ambiguity introduced by tempfile paths, though it would not fix the underlying regression (the empty temp files themselves). The suggestion is narrowly scoped: it targets the symptom (path ambiguity) rather than the root cause (platform-specific I/O behavior during tempfile creation).

No prior discussion of `%f` or similar placeholders exists in the thread, and the proposal is not tied to any existing Git feature or configuration pattern. It is a novel idea introduced solely to mitigate the NonStop regression. The thread’s earlier participants (Peff, D. Ben Knoble) have not weighed in on this suggestion, so its feasibility or desirability remains untested. Future replies may explore whether the Git project would accept such a change, whether it could be implemented without breaking existing textconv filters, or whether a platform-specific fix for the regression would be preferable.