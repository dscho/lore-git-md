This is a **platform compatibility bugfix** for macOS, specifically addressing a memory leak in the system regex engine (TRE) when processing invalid UTF-8 sequences. The email is a **substantive review** from Patrick Steinhardt questioning the long-term maintainability of the current workaround and proposing an alternative approach: detecting affected macOS versions and falling back to Git’s built-in regex implementation (`NO_REGEX`) instead of adding Darwin-specific logic.

Patrick’s concern centers on the trade-off between patching an upstream bug (which may eventually be fixed) and avoiding platform-specific complexity. He notes that Git already has a compatibility `regexec` implementation that likely doesn’t suffer from this leak, suggesting it might be cleaner to use that instead of maintaining a custom workaround. The review is **not a rejection**—it’s a technical exploration of whether the current approach is the best long-term solution, given the patch’s narrow scope (macOS builds using the system regex engine) and the expectation that Apple will eventually fix the underlying issue.

The thread’s prior context shows this is the second iteration of the patch, with v2 already addressing Junio C Hamano’s structural concerns (modular platform files, expanded test coverage). Patrick’s feedback introduces a new dimension: whether the fix should be temporary (via `NO_REGEX`) or permanent (via the current Darwin-specific override). The question is **unresolved**—no counterarguments or data about the built-in regex’s performance, compatibility, or memory behavior have been presented yet.

Key technical details to track in follow-ups:
- The `NO_REGEX` flag and its implications for macOS builds (performance, feature parity).
- Whether Git’s built-in regex implementation avoids the leak (and if so, why it wasn’t chosen initially).
- How version detection would work (e.g., checking macOS SDK versions or runtime behavior).
- The maintainability burden of the current workaround versus a detection-based fallback.

This is a **worthwhile discussion** for reviewers interested in platform-specific trade-offs or regex internals, but it doesn’t invalidate the current patch—it’s a design-level question about the best path forward. The patch remains **ready for merging** unless the thread converges on the `NO_REGEX` alternative.