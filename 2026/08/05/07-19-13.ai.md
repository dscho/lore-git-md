This is a bugfix patch targeting the `git push` command's `--porcelain` output. The issue is that when using `git push --delete --porcelain` to delete a ref that does not exist on the remote (e.g., due to a concurrent deletion), the error is output in human-readable text rather than the documented machine-readable porcelain format. The author, Xavier Morel, reports that extant refs are correctly formatted, but non-existent ones fall back to unstructured error messages, breaking the expected output consistency for `--porcelain`.

The patch highlights a clear edge case in the porcelain output handling: the code path for successful deletions adheres to the documented format, but the error path for non-existent refs does not. The author suggests a fix (using `!` as a prefix for remote failures) but does not provide a patch implementation—this is an RFC-style report rather than a ready-to-merge change. The behavior change would affect scripts or tools parsing `git push --porcelain` output, as they currently cannot reliably detect or handle deletion failures for non-existent refs.

Key technical details include the `--porcelain` flag’s documented format (which uses `:` for successful deletions and `!` for proposed error markers), the specific error message (`remote ref does not exist`), and the context of concurrent deletions. The issue touches the `push.c` code (likely in `transport.c` or `remote.c` as well), though the exact location isn’t specified. No prior discussion or related commits are mentioned, so this appears to be a newly identified gap in the porcelain output coverage.

The report is well-motivated and narrowly scoped, focusing on a single inconsistency in a user-facing feature. The lack of a patch implementation means this will likely require follow-up from a contributor familiar with the push/transport code, but the problem itself is straightforward and uncontroversial. If fixed, it would improve the reliability of `--porcelain` for automation.