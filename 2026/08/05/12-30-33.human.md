This is a minor correction to the commit message of a bugfix patch for `git worktree add`. The patch itself (v2) is already well-motivated and addresses a regression introduced in Git v2.42.0, where explicit `-b` or `-B` options were ignored when a branch name could be DWIMmed to a remote-tracking branch. The fix ensures that explicit branch creation takes precedence over DWIM behavior, preventing unintended branch creation.

The correction here is purely cosmetic: the author, Yoichi Nakayama, notes that they accidentally removed the single quotes around command examples in the commit message (e.g., `'git worktree add'` became `git worktree add`). This is a surface-level tweak to align with Git’s documentation style, where command examples are typically quoted. The underlying code and test changes in v2 remain unchanged and have already incorporated substantive feedback from Junio C Hamano, including preserving warning behavior for `HEAD` and refining the test case.

The patch is part of a targeted regression fix for commit `128e5496b325640f0a09cc1d5b1e346c069b410f`, and the v2 iteration is considered ready for integration. This correction does not affect the technical content or review status—it’s a final polish before merging. If you’ve been following the thread, this is just a heads-up that the commit message will be slightly adjusted in the next version.