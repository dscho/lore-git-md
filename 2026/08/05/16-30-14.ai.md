This is a maintainer response to a bug report about inconsistent `--porcelain` output in `git push --delete`. Junio C Hamano acknowledges the issue as an oversight—no one had previously considered the case where a deletion request targets a ref that no longer exists on the remote. The response is brief and matter-of-fact: the behavior is unintended, and the project welcomes a patch to fix it.

The thread remains in the "problem identification" phase, with no implementation yet. Junio’s reply does not contest the proposed solution (using `!` as an error prefix for machine-readable output) or suggest alternatives, so the technical direction is effectively settled. The next step is for a contributor to implement the fix, likely in `push.c` or related transport code, and add test coverage for the edge case.

This is a well-scoped, uncontroversial bugfix with clear value for script authors who rely on `--porcelain` for automation. The maintainer’s explicit invitation to submit a patch signals that a well-crafted fix would likely be accepted. No prior discussion or related commits are mentioned, so this appears to be a newly identified gap in the porcelain output handling.