This is a minor correction to the commit message of a bugfix patch for `git worktree add`. The patch itself (v2) is already under review and addresses a regression where `-b` or `-B` options were ignored when a branch name could be DWIMmed to a remote-tracking branch. The fix ensures explicit branch creation takes precedence over DWIM behavior, and the v2 iteration incorporated Junio C Hamano’s feedback to preserve warning behavior and refine the test case.

The current email is a follow-up to note a cosmetic issue in the commit message: the author accidentally removed the single quotes around command invocations (e.g., `git worktree add <path> <branch>` became `git worktree add <path> <branch>`). This is purely a stylistic fix and does not affect the code or test changes. The author plans to submit a corrected version, but the substance of the patch remains unchanged.

For follow-up discussion, the key technical details remain:
- **Files touched**: `builtin/worktree.c` (logic change to suppress DWIM when `new_branch` is set), `t/t2400-worktree-add.sh` (test case for error message validation).
- **Regression commit**: 128e5496b325640f0a09cc1d5b1e346c069b410f (Git v2.42.0).
- **Behavior change**: DWIM is suppressed when `-b` or `-B` is provided, ensuring explicit branch creation is respected.

No substantive concerns remain, and this correction is unlikely to affect the patch’s readiness for integration. The thread is effectively resolved pending the trivial commit message update.