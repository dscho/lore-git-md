This is the final exchange in the long-running series to optimize `paint_down_to_common()` for one-sided histories. The patch under discussion (v7 10/10) removes the commit-date ordering fallback entirely, replacing it with a topological ceiling for v1 commit-graphs. The conversation between Kristofer Karlsson and Elijah Newren is procedural and focused on documentation alignment.

The core issue is a **documentation-code mismatch** in patch 8/10: the code treats the finite-generation region as a binary split at `GENERATION_NUMBER_INFINITY`, but v1 commit-graphs use `GENERATION_NUMBER_V1_MAX` as a topological ceiling. The current implementation is correct, but the split feels awkward. Elijah suggests moving the `topo_ceiling` logic into patch 8 to align the documentation and code, but Kristofer prefers to keep the code sequencing as-is and instead rework the documentation to better reflect the unified behavior. He plans to spend the next few days rewriting the technical document (`paint-down-to-common.adoc`) to address this and other feedback.

The tone is collaborative and forward-looking. Elijah calls the series "nearly ready to merge" and praises the removal of the date-ordering fallback as a simplification. Kristofer agrees, noting that unifying the code paths for v1 and v2 commit-graphs reduces complexity and avoids gotchas. The performance improvement (100-1000x speedups in asymmetric queries) is framed as a bonus, with a nod to users still on v1 graphs to upgrade to v2.

No technical concerns remain; the series is **fully reviewed, procedurally unblocked, and queued in Junio’s tree**. The only open work is documentation cleanup, which Kristofer is handling. This email confirms the series is in its final stages, with review bandwidth still the bottleneck. For a busy reader, the takeaway is that this is a **well-tested, high-impact optimization** that’s ready to land once the documentation is polished.