This is a process-oriented reply from Junio C Hamano to Christian Couder about the patch series introducing `uploadpack.lazyFetchTrusted`. The email does not engage with the technical content of the series but instead identifies a **formatting and tooling issue** that prevented the patches from applying cleanly.

The problem stems from how the series was threaded and versioned. Junio attempted to apply the patches to recent integration branches (`v2.55.0` and `master`) but found they did not apply. The root cause is that the cover letter for this series is a reply to an earlier, unrelated series (the `fromAccepted` proposal), but the new patches are not marked as `v2` of that earlier series. This confused `b4`, the tool Junio uses to fetch and apply patches from the mailing list, causing it to incorrectly merge patches from both series into a single, inapplicable mess.

Junio’s feedback is clear: **do not thread a new series as a reply to an unrelated older series unless it is explicitly marked as a new version (e.g., `v2`)**. The current threading structure violates Git’s patch submission conventions and breaks tooling. He requests that future submissions either avoid threading unrelated series together or use proper version numbering to signal iteration.

The email does not comment on the series’ readiness, security model, or technical merits — only on the mechanical issue that prevented it from being queued for review. This is a **blocking issue**: until the series is re-sent with correct threading or versioning, it cannot proceed in the integration pipeline. No merge status is implied; the series remains **under review but stalled due to submission format**.