This is a **substantive review follow-up** in the `paint_down_to_common()` optimization series. Kristofer Karlsson agrees to add a code comment to `paint_queue_get()` documenting why the counters must be checked *before* decrementing—an ordering invariant that prevents premature loop termination. The comment will follow Git’s multi-line formatting guidelines and clarify that the "pop first" logic ensures the just-dequeued commit remains accounted for in the counters, avoiding missed merge bases. Elijah Newren, who originally flagged the ordering as important but non-blocking, endorsed the idea as "reasonable to consider" but left the decision to Kristofer.

The exchange is narrowly technical, focusing on maintainability rather than correctness (the ordering is already correct). It reflects the series’ broader emphasis on clarity, particularly in the `paint_state` refactoring (this patch), which replaces the old `max_nonstale` pointer logic with explicit counters. The proposed comment will serve as a guardrail against future regressions, aligning with the series’ goal of hardening the commit-reach subsystem. No other changes are implied, and the series remains **queued and procedurally unblocked**, with only editorial documentation refinements outstanding.