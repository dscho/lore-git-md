This is the final technical resolution in the revived `writev(3p)` wrapper series. Patrick Steinhardt confirms the one-line change to the compatibility wrapper: it will write only the first `iovec` in the loop, restoring the non-interleaving property of `write(3p)` and avoiding interleaving even on platforms without `writev(2)`. This directly addresses Johannes Sixt’s forward-compatibility concern about future contributors assuming atomicity, while preserving the performance gains of batching. The change also simplifies the wrapper by removing most of the loop logic, though Patrick proposes retaining the overflow check to stay closer to POSIX requirements. The series is now technically complete, with all prior objections resolved and Junio C Hamano’s "clever" endorsement of the approach. The only remaining step is Junio’s final assessment of whether the demonstrated performance gains (1.37× latency improvement in `git-upload-pack`, 4 % wall-clock and 18 % system CPU time reduction in `fast-import`) justify merging the series. No new objections or open questions are introduced in this email.