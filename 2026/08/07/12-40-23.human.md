This is the eighth patch in the v8 series optimizing `paint_down_to_common()` for one-sided histories. The email is a focused discussion between Kristofer Karlsson and Elijah Newren about documentation wording for the "side-exhaustion" optimization introduced in this patch. The exchange clarifies how to describe the generation-number regions where the optimization applies, particularly the distinction between the "reliably-ordered" (finite) and "unordered" (INFINITY) regions, and how `GENERATION_NUMBER_V1_MAX` fits into this framework.

The patch itself is technically complete and well-reviewed, implementing early termination when one side of the merge-base walk exhausts its commits. The optimization delivers the advertised 100-1000x speedup for asymmetric queries (e.g., `merge-base --all commit-9-9 commit-9-1` drops from 81 steps to 9 in full commit-graph mode) and is already queued in Junio’s tree. The only remaining work is editorial: Kristofer plans to rewrite the documentation and commit messages in v8 to consistently use "ordered" and "unordered" instead of "finite" and "infinite" to better reflect the semantic boundary, and to clarify how `GENERATION_NUMBER_V1_MAX` (the topological ceiling for v1 commit-graphs) interacts with the optimization. Elijah’s feedback is purely about clarity and does not raise any technical concerns.

If you care about merge-base performance in repositories with asymmetric histories (e.g., import grafts or shallow clones), this patch is a clear win. The series is procedurally unblocked and ready for `master` pending these final documentation refinements. The discussion here is a minor but important step toward aligning the documentation with the code’s behavior, ensuring future readers understand the optimization’s scope and limitations.