This is a **substantive review follow-up** in the ongoing `git rebase --update-refs` symref bugfix thread. Phillip Wood clarifies two key points about the v3 series: (1) Erik Cervin-Edin’s earlier report about a v2 regression (incorrect current-branch skipping) is already fixed in v3, so the only remaining blocker is the latent `head_ref` buffer-clobbering bug in `master` that Junio flagged; (2) he reiterates his request for a **comment explaining the symref resolution logic** in the first patch, specifically why `resolved_ref` (not `decoration->name`) is used for HEAD comparison—this ensures symrefs pointing outside `refs/heads/` are handled correctly. He also notes he’s left additional feedback on the second patch (redundancy in `prepare_checked_out_branches()` and deduplication logic), but the tone here is procedural: the design debate is settled, and the focus is now on implementation polish for v4. The email situates the thread’s next steps clearly—address the `head_ref` bug, add the requested comment, and incorporate feedback on the second patch—without reopening settled questions.