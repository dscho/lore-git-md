This is the first patch in a two-part test fix series targeting a flaky test in `t7900-maintenance.sh`. The patch refactors several tests to use a throwaway repository instead of the default trash directory. This change improves test isolation, preventing interference between tests that exercise Git's maintenance machinery, which is sensitive to on-disk state.

The motivation is practical: the current tests are fragile because they share a single repository, and the next patch in the series will need to modify the environment in ways that could affect subsequent tests. By switching to a throwaway repository (created with `git init` and cleaned up with `test_when_finished`), the tests become self-contained and less prone to side effects.

The changes are confined to `t/t7900-maintenance.sh` and involve wrapping existing test logic in a subshell within a newly created repository. The patch is mechanical and does not alter the behavior being tested—it only changes where the tests run. This is a common test hygiene practice in Git's test suite, especially for commands like `git maintenance` that interact with repository state.

The patch looks well-crafted and uncontroversial. It addresses a real problem (test flakiness) with a targeted solution, and the commit message clearly explains the rationale. No new test helpers or user-facing changes are introduced. The next patch in the series will build on this refactoring to fix the actual flake.