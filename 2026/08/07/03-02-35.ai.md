This is the core performance patch in the `paint_down_to_common()` optimization series, now at v7. It implements early termination in the merge-base walk when one side of the history is exhausted, delivering the advertised 100-1000x speedup for asymmetric queries (e.g., `merge-base --all commit-9-9 commit-9-1` drops from 81 steps to 9 in full commit-graph mode). The patch adds the termination check to `paint_queue_get()` using the per-side counters (`parent1_count`, `parent2_count`, `mb_candidate_count`) introduced earlier in the series. The optimization only activates in the finite-generation region (where topological ordering holds) and waits for pending merge-base candidates to drain, ensuring correctness.

Elijah Newren’s review raises a technical point about `GENERATION_NUMBER_V1_MAX`: the documentation and code currently treat the finite-generation region as a binary split at `GENERATION_NUMBER_INFINITY`, but v1 commit-graphs use `GENERATION_NUMBER_V1_MAX` as a topological ceiling. The patch glosses over this distinction, though the series later addresses it in patch 9/10 (fallback removal). The split feels awkward—this patch should either defer the optimization until the ceiling is properly handled or explicitly note the limitation. The current implementation is correct but risks confusion about the region boundaries.

The documentation update clarifies the new "side-exhaustion" termination condition and explains why the optimization is skipped in the INFINITY region (where commit-date ordering can violate topological guarantees). The wording could be clearer—Elijah suggests "reliably-ordered region" instead of "finite-generation region" to better reflect the semantic boundary. The test expectations are updated to reflect the reduced step counts, which are now validated deterministically via Trace2 instrumentation added earlier in the series.

This patch is the culmination of a well-reviewed, technically complete series that has been queued in Junio’s tree. The change is minimal and directly delivers the performance win without altering behavior. The only open items are the `GENERATION_NUMBER_V1_MAX` documentation gap and the split with the fallback-removal patch, but neither blocks merging. If you care about merge-base performance in repositories with asymmetric histories (e.g., import grafts or shallow clones), this is a clear win. The series is procedurally unblocked and ready for `master` pending additional reviewer bandwidth.