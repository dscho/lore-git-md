This is a brief but substantive exchange in the ongoing thread about Pablo Sabater’s GSoC patch series extending `git cat-file --batch-command` to support `%(objecttype)` in remote-object-info queries. The series is now at v6, feature-complete, and under final review.

Jeff King (Peff) and Pablo discuss a potential future optimization: making the `remote-object-info` protocol stateful so that multiple object-info requests can reuse the same transport connection. Peff initially suggests a syntax where the remote URL is declared once (`remote <url>`) and then referenced in subsequent `remote-object-info` commands. Pablo finds the idea compelling but defers it to a follow-up series, citing time constraints and the current focus on object type support. Peff agrees, noting that the optimization can be added later without protocol changes, so there is no urgency.

The exchange is notable for its pragmatic tone. Both acknowledge the value of the optimization but prioritize landing the current series first. The discussion does not alter the immediate patch set, which remains on track for integration once the static-analysis warning in v6 is resolved. No new technical concerns or review feedback are introduced; the focus is on future work.