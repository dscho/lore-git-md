This is Junio’s administrative feedback on the patch series introducing `uploadpack.lazyFetchTrusted`. The series itself is not under technical review here; instead, Junio flags a **process issue** that prevented the patches from being applied cleanly.

The problem stems from how the series was threaded. Christian sent the new five-patch series as a reply to the earlier `GIT_NO_LAZY_FETCH=fromAccepted` thread, but did not mark it as `v2`. This caused the `b4` tool to conflate patches from both series, producing a malformed patchset that failed to apply to any recent Git base (`v2.55.0` or `master`). Junio’s message is a clear instruction: **do not thread unrelated iterations together unless they are explicitly marked with a version bump** (`v2`, `v3`, etc.). The next iteration should be sent as a fresh thread with a proper `v2` tag to avoid this issue.

The series itself remains under review, with its core goal—replacing the client-side `fromAccepted` proposal with a server-side `uploadpack.lazyFetchTrusted` configuration—intact. No technical feedback is provided in this email; the focus is solely on the mechanics of patch submission. Christian will need to resend the series with the version tagging corrected before it can proceed through the review pipeline.