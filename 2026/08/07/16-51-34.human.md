This is the final documentation patch in Michael Montalbo’s three-part series fixing race conditions in Git’s HTTP daemon test helpers. The patch adds a new section to `t/README` explaining how to write concurrency-safe test helpers for `lib-httpd`, documenting the atomic idioms (`mv`, `mkdir`) and per-request scratch file conventions (`$$` suffixes) introduced in the earlier patches.

Michael responds to Patrick Steinhardt’s surface-level review with agreement on all three editorial suggestions. He will adjust the section header to avoid implying the advice is HTTP-specific (though the examples remain HTTP-focused), restore mention of `rm` as a valid atomic alternative to `mv` for consuming one-shot markers, and clarify that `$$` expands to the process ID (PID). These changes are purely cosmetic and do not alter the technical substance of the patch.

The series as a whole remains ready for integration into `next`. This patch is uncontroversial and unlikely to see further iteration.