This is the final patch in a well-reviewed, ready-to-merge series that optimizes Git’s `paint_down_to_common()` function for one-sided histories. The patch removes the commit-date ordering fallback entirely, replacing it with a cleaner topological ceiling for v1 commit-graphs. The fallback was a long-standing workaround for performance regressions in v1 graphs, but the side-exhaustion optimization introduced earlier in the series now handles those cases more elegantly—reducing step counts from 167k to 5.7k in the Linux kernel repo while preserving correctness.

The change is straightforward: it removes the `gen_ordered` flag from `paint_state`, adds a `topo_ceiling` field to enforce the topological limit for v1 graphs, and updates the termination logic in `paint_queue_get()` to use this ceiling instead of `GENERATION_NUMBER_INFINITY`. The documentation is trimmed to remove the now-obsolete "date-ordering fallback" section, and test expectations are adjusted to reflect the reduced step counts. The patch is fully reviewed and approved, with Elijah Newren’s feedback limited to a minor inconsistency between the documentation and code in an earlier patch (8/10)—a non-blocking issue that doesn’t affect this patch’s correctness.

This is a clear improvement: it eliminates dead code, simplifies the algorithm, and hardens the commit-reach subsystem against clock-skew edge cases. The series is queued in Junio’s tree and procedurally unblocked; the only remaining hurdle is additional review bandwidth. For anyone tracking Git’s performance or merge-base machinery, this is a meaningful optimization worth watching.