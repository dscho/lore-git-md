This is a **completion infrastructure patch** in the `git history` Bash completion series. The email is a reply to Patrick Steinhardt’s substantive review of the `__git_history_has_revision` helper function, which he criticized for hardcoding options that take arguments (e.g., `--empty`, `--update-refs`). Vincent Mailhol proposes a compromise: instead of overhauling the helper now, future options that deviate from the "revision-first" pattern (e.g., a hypothetical `foo` subcommand that takes a file argument first) can be handled by dispatching them *before* the revision check, using a simple `case` statement. This keeps the current logic intact while providing a clear escape hatch for edge cases. Patrick accepts the trade-off, calling it "a huge win compared to the status quo" and leaving room for future refinement if needed.

The discussion advances the patch toward readiness but does not yet resolve the maintainability concern—it merely defers it. The proposed `case` block is not part of the current patch; it is a forward-looking suggestion for how to handle future exceptions. The immediate takeaway is that the series may proceed to v3 with the existing helper, and any new subcommands that break the "revision-first" assumption will require explicit handling. Key technical details for follow-up: the `__git_history_has_revision` function, the `__git_complete_index_file` helper, and the pattern of dispatching edge cases before the general revision check. The patch remains uncontroversial, but the thread now documents a clear path for handling future exceptions.