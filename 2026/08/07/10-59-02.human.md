This is the second patch in a two-part test fix series targeting a flaky test in `t/t7900-maintenance.sh`. The series addresses a race condition in the "maintenance.strategy" test, where the auto-detaching background maintenance logic sometimes interferes with foreground test processes, causing the test to fail intermittently in GitLab CI.

The fix here is surgical: instead of globally disabling `GIT_TEST_MAINT_AUTO_DETACH` for the entire test script (which introduced the race), the patch restores the default behavior (auto-detach disabled) and only unsets it in the three specific tests that explicitly need it. This isolates the auto-detach logic to the tests that require it, reducing the window for race conditions in the rest of the test suite. The change is minimal—just three lines moved—and preserves the original intent of exercising real-world maintenance behavior where needed.

The commit message is exemplary: it walks through the debugging process (initial hunch about disk state, then identifying the lock-file race), explains why the existing fix was insufficient, and justifies the chosen solution. The linked CI failure provides concrete evidence of the problem, and the reasoning about why other tests in the suite might also be at risk (even if not yet observed) shows thoughtful consideration of broader implications.

This looks like a well-crafted fix for a real CI pain point. The patch is narrowly scoped, well-explained, and unlikely to introduce new issues. If the earlier refactoring patch (1/2) was uncontroversial, this one should be too—it’s a natural follow-up that addresses the root cause rather than papering over it. No controversy or design questions are apparent; this is purely a test reliability improvement.