This is the second patch in Tian Yuchen’s three-patch refactoring series (`v3 2/3`) targeting repository configuration handling in `environment.h`. The patch consolidates and clarifies the documentation for the repository config getter functions (`repo_protect_hfs`, `repo_protect_ntfs`, `repo_ignore_case`, etc.), explicitly stating that these functions require a non-NULL repository pointer and handle uninitialized repositories by returning default values.

The change replaces three separate, repetitive comments with a single unified block that describes the common behavior of all getters. The new wording directly addresses Junio C Hamano’s prior feedback by making it clear that passing NULL is an error, and it removes redundant explanations about `repo->initialized` checks. The patch is purely documentary: no code or function signatures are altered, and the net line impact is negative (4 insertions, 11 deletions).

This patch is part of a technically complete series that has resolved all maintainer feedback; the only remaining open question is the author’s RFC note inviting further cleanup suggestions, which has not yet drawn new proposals. The series remains a straightforward refactoring with no functional changes, and this patch specifically improves code clarity without introducing new symbols or behavior.