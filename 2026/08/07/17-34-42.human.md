This is the third patch in the v8 series introducing the `--linearize` option to `git replay`. The email is a **substantive review** from Justin Tobler, focusing on the design and documentation of the new feature.

Justin raises several clarifying questions about the behavior of `--linearize`, particularly around its interaction with multiple branches and ref updates. He questions whether the restriction on multiple revision ranges is necessary (suggesting it might be possible to linearize each branch independently) and whether the documentation adequately explains the rationale for this limitation. His feedback is technical and probing, but he does not propose specific code changes—his concerns are about clarity and design intent rather than implementation correctness.

The review highlights a potential disconnect between the author’s intent (to enforce a predictable, all-or-nothing flattening of history) and the reviewer’s mental model (where linearizing each branch independently might be more intuitive). The discussion centers on the **multi-branch ambiguity** that was a key point of contention in earlier versions of the series. Toon’s response (implied in the quoted text) justifies the restriction as a way to avoid concatenating unrelated histories, but Justin remains unconvinced, suggesting the documentation could be more direct about the limitation.

The patch itself is technically complete and addresses all prior feedback, including the restriction to single-branch usage in v8. The review does not identify new bugs or edge cases, but it does surface lingering questions about the interface design. These questions are unlikely to block the series (given Junio’s intent to queue v8 for review), but they may prompt further discussion about whether the current design is the most user-friendly approach. The potential for future work (e.g., per-branch linearization via `--ref`) is noted, but not implemented in this version.