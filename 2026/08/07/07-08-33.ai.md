This is a **completion infrastructure patch discussion** in the ongoing `git history` Bash completion series. The email captures a technical exchange between Patrick Steinhardt and Vincent Mailhol about the maintainability of the `__git_history_has_revision` helper function, which determines whether a revision argument has already been provided on the command line.

Patrick reiterates his concern that the helper hardcodes options like `--empty` and `--update-refs` that take arguments, requiring manual updates whenever new such options are added. He cites upcoming features—commit signing, sign-offs, and notes handling—as likely sources of new parameters, making the current approach fragile. While he acknowledges Vincent’s proposed workaround (dispatching edge-case subcommands *before* the revision check via a `case` statement), he remains uneasy about the hardcoded list, calling it "error prone" and likely to diverge as the command evolves.

The discussion does not introduce new technical details or propose a revised implementation; it instead reaffirms the earlier compromise while underscoring the long-term risk. The immediate impact is minimal—the series can proceed to v3 with the existing helper—but the thread now documents a clear tension between short-term pragmatism and long-term maintainability. Key references for follow-up include the `__git_history_has_revision` function, the `__git_hash_doubledash` helper (mentioned as a potential alternative), and the pattern of pre-dispatching edge cases. The patch remains uncontroversial, but the maintainability concern is now explicitly flagged as a likely source of future churn.