This is a **test infrastructure bugfix** addressing a race condition in the HTTP daemon test helpers. Michael Montalbo clarifies the intent behind a previously unexplained change in patch 1 of the v2 series: the redirection of `stderr` to `/dev/null` when invoking `./one-time-script` is deliberate, meant to suppress expected errors when the script is removed by a concurrent request. This resolves the open question raised by Patrick Steinhardt, who had flagged the change as potentially masking legitimate errors.

The series as a whole remains a well-reviewed, targeted fix for a real flake in the test suite (`t5616.47`), observed on macOS CI runners. The atomicity fixes (using `mv` and `mkdir`) are uncontroversial and address a clear correctness issue in concurrent HTTP requests. With this clarification, the last substantive review point is now addressed, and the series is ready for integration into `next`. No further changes are expected.