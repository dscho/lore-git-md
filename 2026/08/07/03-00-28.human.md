This is a **documentation patch** and the first installment in the v7 series optimizing `paint_down_to_common()` for one-sided histories. The patch adds a detailed technical document (`Documentation/technical/paint-down-to-common.adoc`) that explains the algorithm, generation-number regions, termination conditions, and edge cases like the commit-date fallback. The document is well-structured, covering both the core paint-walk logic and subtleties such as INFINITY vs. finite generation regions, v1 commit-graph saturation, and the rationale behind the date-ordering fallback.

Elijah Newren, who previously reviewed the document, confirms that all his feedback has been addressed. The clarifications for v1 commit-graph handling (using `GENERATION_NUMBER_V1_MAX` as a topological ceiling) and the revised wording for the `min_generation` cutoff are now incorporated. The rest of the document remains unchanged from the last reviewed version and is considered technically sound.

This patch is purely informational and has no functional impact on the codebase. It serves as essential context for the subsequent performance optimizations, refactoring, and correctness fixes in the series. No further technical feedback is expected to block this patch, and it appears ready to proceed. The series as a whole is queued in Junio’s tree and awaiting additional review bandwidth.