This is the penultimate patch in Pablo Sabater’s ten-patch GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series is now feature-complete and merge-ready, with all prior review feedback addressed.

The patch itself is minimal and mechanical: it appends "type" to the server’s advertised capabilities string in `serve.c`, enabling the client to request object type metadata. The change is a single line (`strbuf_addstr(value, "size type")`), reflecting the narrow scope of the series—only "size" and "type" are supported, and other metadata (disk size, delta base, object mode) are explicitly ruled out as unreliable or context-dependent.

The test updates in `t/t1017-cat-file-remote-object-info.sh` are more substantial. The patch replaces the now-obsolete test for unsupported placeholders with two new tests: one verifying that `%(objecttype)` returns the correct type ("blob"), and another confirming that unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings. The tests are well-structured, using cached variables (`hello_type="blob"`) for clarity and portability, and they cover both positive and negative cases. The commit message could be clearer about the test restructuring—it drops one test and adds two—but the intent is obvious from the diff.

This patch is uncontroversial and ready for integration. It completes the server-side implementation for object type metadata, enabling the final patch (10/10) to unify the default format for all `cat-file` batch commands. The series as a whole is a clean, well-tested extension of the recently merged remote-object-info functionality, with no open technical concerns.