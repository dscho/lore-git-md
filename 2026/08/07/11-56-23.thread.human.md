This thread is about replacing Git’s build-time `USE_NSEC` macro with a runtime configuration option, `core.useNanosec`, to let users enable nanosecond precision without recompiling. The series, now at v4, is effectively complete and cooking in Junio’s `next` branch under the topic `dk/use-nsec-runtime`, ready to graduate to `master` once final polish is applied.

D. Ben Knoble is the author. The key technical change—introducing `core.useNanosec`—is uncontroversial and addresses earlier concerns about performance (a 41× slowdown in `git diff` benchmarks was fixed by caching the value) and cross-platform correctness (the option is safely ignored on platforms lacking nanosecond support). Junio and Patrick Steinhardt reviewed the series, identifying minor polish items now resolved: unconditional initialization of the `use_nanosec` field, simplified preprocessor guards, and clearer documentation. The only remaining open question is how to test platform-dependent behavior, but this isn’t blocking progress. A trivial conflict exists with another topic in `seen` (`ty/repo-config-cleanups`), but it’s purely textual.

The latest email is a routine acknowledgment: the author accepted Junio’s suggested tweak to the commit message of the second patch (a no-op refactor of `repo_config_values_init`), changing "struct definition" to "definition of 'struct repo_config_values'" for clarity. This patch reorders assignments to match the struct declaration and is ready to advance. No further discussion is expected for this part of the series. The rest of the patches—the Meson build fix and the core `core.useNanosec` change—are also uncontroversial and ready to merge.