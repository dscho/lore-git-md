This thread replaces Git's compile-time `USE_NSEC` setting with a runtime configuration option, `core.useNanosec`, allowing users to enable nanosecond precision on supported filesystems without recompiling. The series, now at version 6, has been **graduated to Junio's `next` branch** for integration testing after addressing all prior feedback. The only change from v5 was a trivial cosmetic tweak requested by Patrick Steinhardt—replacing "defined" with "enabled" in a comment—to better reflect the runtime nature of the new setting.

The core change is technically sound: nanosecond precision is now configurable via `core.useNanosec` (defaulting to false), and the performance regression identified in earlier versions (~41× slowdown in `git diff`) has been resolved by caching the value in the repository's config struct. The series also includes two uncontroversial patches—a Meson build-system fix and a no-op refactor of `repo_config_values_init`—that are ready to advance independently. A minor conflict with `ty/repo-config-cleanups` in `seen` will require routine resolution if both topics progress.

The discussion in this final exchange centered on an architectural constraint: the new call to `repo_config_values()` in `is_racy_stat()` hardcodes a dependency on `the_repository`, which could complicate future submodule support. While this isn’t a regression (the limitation already existed), Patrick Steinhardt suggested refactoring to `struct repo_settings` if edge cases arise. The author, D. Ben Knoble, deferred deeper changes for now, noting the complexity of the config system. All reviewers—Junio, Patrick, and SZEDER Gábor—have signed off, and the series is effectively ready for integration. The only remaining loose ends are the cosmetic comment tweak (now addressed) and the conflict resolution.