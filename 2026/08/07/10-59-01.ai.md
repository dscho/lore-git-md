This is the first patch in a two-patch test fix series targeting flaky behavior in `t/t7900-maintenance.sh`. The patch refactors three existing tests (`maintenance.auto config option`, `gc.auto config option`, and `maintenance.auto overrides gc.auto`) to use a throwaway repository instead of the default trash directory. This change isolates test state, preventing interference between tests and allowing environment modifications in the next patch without affecting subsequent tests.

The refactoring is mechanical: each test now creates a fresh repository with `git init repo`, runs its logic inside a subshell within that repository, and cleans up with `test_when_finished "rm -rf repo"`. The core test logic (checking whether `git maintenance run --auto` is triggered based on config settings) remains unchanged. This is a pure test hygiene improvement with no user-visible behavior changes.

The patch touches only `t/t7900-maintenance.sh`, adding 43 lines and removing 27. The changes are confined to test scaffolding; no new test helpers or assertions are introduced. The motivation is explicitly preparation for the next patch, which will modify the environment in these tests to fix the flaky "maintenance.strategy" test.

Key technical details for follow-up discussion:
- **Test file**: `t/t7900-maintenance.sh`
- **Refactored tests**: `maintenance.auto config option`, `gc.auto config option`, `maintenance.auto overrides gc.auto`
- **New pattern**: Throwaway repository created with `git init repo` and cleaned up with `test_when_finished`
- **Subshell usage**: Tests now run inside `(cd repo && ...)` to scope environment changes
- **Config keys**: `maintenance.auto`, `gc.auto`, `maintenance.autoDetach`, `gc.autoDetach`

The patch is uncontroversial and appears well-motivated. The use of throwaway repositories is a standard test practice in Git, and the refactoring is narrowly scoped to the tests that need environment isolation for the follow-up fix. No issues or edge cases are apparent from the diff.