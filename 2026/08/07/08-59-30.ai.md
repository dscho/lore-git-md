This is the first patch in Tian Yuchen’s v3 refactoring series targeting repository configuration handling in `environment.c`. The patch removes redundant NULL checks from the three repository config getter functions (`repo_protect_ntfs`, `repo_protect_hfs`, and `repo_ignore_case`). The commit message now explicitly states that passing NULL is a programming error, addressing Junio C Hamano’s forward-compatibility concern from the v2 review. The change is mechanical and does not alter behavior, but it makes the code’s assumptions clearer by letting invalid callers fail fast rather than silently returning defaults. The patch touches only `environment.c` and reduces line count by 9 deletions and 9 insertions. No new symbols or on-disk changes are introduced, and the series remains a pure refactoring effort. The updated commit message also notes that no new callers are expected, reinforcing the intent to keep these functions repository-bound.