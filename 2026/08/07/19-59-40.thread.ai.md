Thread identity
Subject: [PATCH v3] fix use-after-free in get_oid_with_context_1 with sparse index
Series version: standalone patch (v3)
Originating author: Shlok Kulshreshtha
Thread type: bug report and fix

Core problem or goal
A use-after-free bug in `get_oid_with_context_1()` occurs when resolving relative paths in the `:<stage>:<path>` syntax against a sparse index. The function `resolve_relative_path()` allocates a new string for the resolved path, but the code later frees this string while still holding a pointer (`cp`) to it, leading to a use-after-free in `reject_tree_in_index()`. The bug requires three specific conditions to trigger: a relative path, a sparse directory in the index, and a leading colon followed by a digit (e.g., `:0:./dir/`).

Current status
Merged to `next`. Junio C Hamano confirmed the problem description and fix are aligned, called the solution "exactly what anybody would expect," and stated he will queue it. The patch is uncontroversial, thoroughly tested, and ready for graduation to `master` in the next cycle. Explicit approvals from René Scharfe ("OK"), Patrick Steinhardt (tested review), and Junio are on record.

Key technical details
- **Subsystems touched**: `sha1-name.c` (core function `get_oid_with_context_1`), sparse index handling, and memory management.
- **New/renamed symbols**: None.
- **Behavior change**: The fix ensures the allocated string from `resolve_relative_path()` is freed only after its final use in `reject_tree_in_index()`, eliminating the use-after-free.
- **On-disk format**: No changes.
- **Test coverage**: New test in `t1092-sparse-checkout-compatibility.sh` exercises the bug under `SANITIZE=address`. The test reproduces the three necessary conditions (relative path, sparse directory, and leading colon followed by a digit) and confirms the fix.

Open questions and loose ends
None. The patch addresses the issue comprehensively, and the test provides reliable coverage. All substantive and stylistic feedback has been incorporated. Junio’s merge confirmation marks the end of the review phase.

Participant positions
- **Shlok Kulshreshtha**: Author of the patch. Provided a clear explanation of the bug, the fix, and the test coverage. Referenced prior discussion (including a suggested fix from Junio) to motivate the change. Incorporated all review feedback, including the stylistic improvement in v3.
- **Junio C Hamano**: Previously suggested a fix for this issue in prior discussion (referenced in the commit message). In this email, confirmed the problem and fix are aligned, called the solution "exactly what anybody would expect," and stated he will queue it. This is the final approval before integration.
- **René Scharfe**: Reviewed the patch substantively, confirmed the bug’s origin (dating back to 2022), and explicitly approved the fix with "OK" and "Good idea to add a test." Suggested using `Original-patch-by:` instead of `Reported-by:` to credit Johannes Schindelin. No technical objections or requests for changes.
- **Johannes Schindelin**: Credited in the `Original-patch-by:` trailer as the original reporter and patch author.
- **Patrick Steinhardt**: Provided a tested review, confirming the bug and the fix. Tested locally and observed the expected behavior (fatal error for non-existent path, instead of corrupted string due to use-after-free). Suggested a minor stylistic improvement (using the return value of `reject_tree_in_index()` directly), which was adopted in v3.

Related work
None. This is a standalone bugfix with no dependencies or follow-up work mentioned. The issue is specific to sparse index handling and relative path resolution. The bug was introduced in 2022 and has not been addressed until now.