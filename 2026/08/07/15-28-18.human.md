This is a **substantive review follow-up** in the ongoing `git rebase --update-refs` symref bugfix series. The thread is now focused on final implementation details for the v4 roll-up.

Phillip Wood confirms that Erik Cervin-Edin’s earlier report about a v2 regression (incorrect current-branch skipping) is already resolved in v3. The remaining open items are two technical points on the first patch: (1) the HEAD comparison should use `resolved_ref` instead of `decoration->name` to correctly handle symrefs pointing outside `refs/heads/`, and (2) the explanatory comment above the skip logic needs to document why symref resolution is performed and what the `flags` check ensures. Phillip also notes he has left additional feedback on the second patch (redundancy in `prepare_checked_out_branches()` and deduplication logic), but those are not discussed here.

The email is procedural and clarifies what still needs to be addressed before the series can move forward. No new design questions are raised; the focus is purely on code correctness and clarity.