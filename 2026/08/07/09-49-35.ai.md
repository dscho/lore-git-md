This is a brief follow-up in the ongoing `git-replay`/`git-history` notes-loss thread. The author, erik88, responds to Elijah Newren’s observation that `git filter-repo` (and by extension `git fast-export`/`fast-import`) silently drops notes due to architectural constraints. erik88 points to a GitHub issue comment that describes workarounds for preserving notes in `git filter-repo`, suggesting these methods are "okay" but not ideal. The comment does not propose a solution for `git-replay` or `git-history`, nor does it engage with the broader design discussion about whether notes should be preserved by default in history-rewriting tools. Instead, it provides a narrow data point: for `filter-repo`, at least partial mitigation exists, even if the underlying architectural issue remains unresolved. This may inform future implementation efforts for `git-replay` and `git-history`, but the thread’s core questions—such as whether note preservation should be opt-in or automatic, and how to handle plumbing vs. user-facing commands—remain open.