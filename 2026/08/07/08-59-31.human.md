This is the second patch in Tian Yuchen’s three-part refactoring series targeting repository configuration handling in `environment.h`. The patch consolidates and clarifies the documentation for the repository config getter functions (`repo_protect_hfs`, `repo_protect_ntfs`, `repo_ignore_case`, and others). It replaces three separate, repetitive comments with a single unified block that explicitly states the functions require a non-NULL repository pointer and handle uninitialized repositories by returning default values.

The change directly addresses Junio C Hamano’s feedback from v2, which requested clearer documentation about the behavior when the repository is not fully initialized. The new wording removes ambiguity and makes the contract for these functions more explicit. The patch is purely documentary: no code or function signatures are altered, and the behavior remains unchanged.

This is a small, uncontroversial cleanup that improves code readability without touching functionality. The series as a whole is now technically complete and ready for final review, with all prior maintainer feedback addressed. If you are following the `the_repository` removal effort or repository initialization internals, this patch may be of mild interest; otherwise, it is unlikely to require attention.