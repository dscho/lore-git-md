This is a **design discussion reply** in the ongoing thread about unifying Git’s interactive patch selection workflow. Junio C Hamano briefly acknowledges agreement with D. Ben Knoble’s clarification: the core goal is to unify the *workflow* for triaging hunks (stage/discard/defer) into a single session, regardless of whether the interface becomes more complex. This reinforces the thread’s focus on the *mechanism* (a single command set for all three actions) rather than the UI style (prompt-loop vs. TUI).

The email adds no new technical details or implementation proposals, but it signals consensus on the immediate priority: extending `git add -p` (or a similar command) to support a "discard" action alongside "stage" and "defer," eliminating the need for a separate `git checkout -p` step. The discussion remains in the conceptual phase, with no patches or sketches posted yet. The next steps will likely involve exploring how to modify the existing `add--interactive` or `apply` machinery to support this unified workflow, or whether a new command (e.g., `git triage -p`) would be a cleaner approach.

Key unresolved questions from the thread—such as how deferred hunks should persist (working tree, stash, or ignored) or how the "discard" action would interact with the index—remain unaddressed. The thread’s scope is still narrow: unifying the triage workflow, with advanced use cases (e.g., squashing hunks into older commits) deferred as longer-term concerns.