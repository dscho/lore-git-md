This is the final exchange in the thread wrapping up the `paint_down_to_common()` optimization series. The focus is on aligning the documentation with the code changes in patch 8/10 (v7), where the early-exit optimization was introduced but the documentation and implementation diverged on how v1 commit-graphs handle topological ceilings.

Kristofer Karlsson acknowledges the inconsistency and plans a full rewrite of the documentation to reflect the series' evolution. He notes that the code sequencing is mostly correct, but the documentation needs to catch up. The `topo_ceiling` logic (introduced in this patch) could technically be moved to patch 8/10, but it would have no behavioral impact since the `gen_ordered` flag is disabled for v1 graphs. Elijah Newren agrees the current split is awkward but defers to Kristofer on the final approach.

The exchange confirms the series is **technically complete and nearly ready to merge**, with only documentation refinements remaining. The fallback removal (this patch) is uncontroversial: it eliminates dead code, unifies v1/v2 commit-graph handling, and delivers measurable performance gains (167k → 5.7k steps in v1 graphs). The tone is collaborative, with both participants signaling satisfaction with the code and a shared understanding of the remaining work. No new technical concerns are raised, and the series remains queued in Junio’s tree awaiting final documentation updates.