This is a **completion infrastructure patch discussion** in the ongoing `git history` Bash completion series. The thread zooms in on a single maintainability concern: the `__git_history_has_revision` helper function, which currently hardcodes options that take arguments (e.g., `--empty`, `--update-refs`). Patrick Steinhardt flags this as error-prone, noting that future additions like commit signing, sign-offs, or notes handling will require new options, and the helper will need manual updates each time. Vincent Mailhol proposes a pragmatic workaround—dispatching edge-case subcommands *before* the revision check via a `case` statement—while acknowledging that a smarter solution may emerge once specific needs arise. Patrick accepts the trade-off, calling the current approach "a huge win compared to the status quo" despite its imperfections.

The discussion leaves the immediate patch series intact but documents a clear path for handling future exceptions. The compromise defers a full overhaul of the helper function, opting instead to keep the current logic and handle deviations explicitly. This keeps the series moving toward readiness while leaving room for refinement later. The patch itself remains uncontroversial, with no fundamental objections to its core functionality—just a forward-looking note about maintainability. A v3 iteration may follow to incorporate this feedback, but the series is now closer to being merge-ready.