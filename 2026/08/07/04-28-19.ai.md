This is a surface-level review of the fourth patch in Patrick Steinhardt’s six-patch series **v5** that makes on-disk ODB structures pluggable. The series is part of the ongoing ODB abstraction effort and is now fully approved, with only minor formatting and docstring nits remaining.

Junio’s reply is purely mechanical: he notes that the previous version of this patch (v4) had corrected an overly long line in `enter_repo()`, but this reroll (v5) no longer does so. He explicitly states this is acceptable either way, signaling no substantive concern. The rest of the diff remains unchanged from v4, which Junio describes as "truly minimum."

The patch itself defers ODB creation by lifting `odb_new()` out of `apply_repository_format()` and placing it at every call site that initializes a repository (`repo_init()`, `enter_repo()`, `setup_git_directory_gently()`, and `init_db()`). This encapsulation keeps ODB initialization and on-disk creation together, preparing for the final patch (6/6), which moves on-disk structure creation into the `struct odb_source` backends. The change is uncontroversial and aligns with Toon Claes’s alternative design, which abandoned the earlier `APPLY_REPOSITORY_FORMAT_SKIP_ODB_CREATION` flag in favor of explicit ODB creation.

No further action is needed on this patch; the review is purely cosmetic and does not affect the series’ readiness for merging. The entire series is now in `next` and expected to graduate to `master` in the current development cycle.