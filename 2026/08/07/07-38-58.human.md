This is a **design discussion** in the ongoing thread about enhancing Git’s interactive patch selection workflow. Christian Couder responds to Junio C Hamano’s proposal for a unified `git add -p`/`git checkout -p` session by raising additional use cases and suggesting broader integration with other Git features.

Christian expands the scope of the discussion by asking how the proposed feature would handle more complex workflows, such as squashing a hunk into an earlier commit (e.g., `HEAD~2`) or moving it to a new branch. He suggests that the design should account for these scenarios rather than limiting itself to the three categories (stage, discard, defer) Junio outlined. To support these cases, he proposes leveraging recent work on `git history` (the experimental command for rewriting history), which could provide the underlying machinery for more advanced hunk manipulation.

He also references GitButler’s `git rub` command (now split into more explicit subcommands) as an example of a tool that already supports many of these workflows. His point is that while the feature should be powerful, it must avoid becoming overly complex—balancing flexibility with usability. This introduces a potential tension in the design: whether to build a narrowly focused tool for the immediate problem (deferring hunks) or a more general framework that could handle a wider range of use cases.

The email does not propose a specific implementation or take a firm position on Junio’s original idea. Instead, it broadens the discussion by highlighting additional requirements and external precedents. This is likely to steer the thread toward questions about scope, integration with other commands, and whether the solution should be incremental or ambitious. For now, the thread remains in the conceptual phase, with no patches or concrete proposals on the table.