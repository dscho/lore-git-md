This is a **test infrastructure bugfix** addressing a race condition in the HTTP daemon test helpers. Michael Montalbo clarifies the previously unexplained change in error handling introduced in v2 of patch 1/3: the redirection of `stderr` to `/dev/null` is intentional and suppresses an expected error when `./one-time-script` is removed by a concurrent request. The rationale is now explicit, resolving Patrick Steinhardt’s substantive review question. The patch remains a targeted, uncontroversial fix that replaces a non-atomic `test -f`/`rm` sequence with an atomic `mv` to eliminate a race window in `apply-one-time-script.sh`, preventing HTTP 500 errors during concurrent requests. The series is otherwise complete and marked ready for integration by Junio C Hamano. Michael plans to add a code comment to document the error-handling behavior, but no further changes to the patch logic are expected. The clarification removes the last open question before the series can proceed to `next`.