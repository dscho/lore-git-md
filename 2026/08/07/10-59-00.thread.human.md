This thread is about a two-patch series from Patrick Steinhardt that fixes a flaky test in `t/t7900-maintenance.sh`, which has been a recurring CI reliability issue in GitLab. The flake occurs due to a race condition between the test process and auto-detaching background maintenance tasks in `git maintenance`. The series is now ready to merge after addressing all feedback.

The first patch refactors three tests to use throwaway repositories instead of the default trash directory, isolating environment changes and avoiding cleanup issues with `test_config` in subshells. The second patch fixes the flake by restoring the default behavior (auto-detach disabled) for the test script and only enabling it where explicitly needed, eliminating the race condition caused by a lingering `maintenance.lock` file. Both patches are test-only changes with no user-visible impact, and the fix is backed by CI evidence from GitLab.

Karthik Nayak provided substantive review, initially suggesting minor commit message improvements, which Patrick incorporated. In the final email, Karthik confirmed the v2 iteration is ready for integration, calling the changes "good." The series is uncontroversial, self-contained, and has no remaining open questions.