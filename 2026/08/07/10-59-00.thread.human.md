This thread is a straightforward fix for a flaky test in `t/t7900-maintenance.sh`, authored by Patrick Steinhardt. The issue—a race condition between the test process and a background `git-maintenance` task—has been a recurring CI reliability problem, particularly in GitLab’s pipeline. The fix comes in two patches: the first refactors test scaffolding to use throwaway repositories and subshells, and the second isolates auto-detach logic to eliminate the race. Both patches are uncontroversial, target only the test file, and leave user-visible behavior unchanged.

Karthik Nayak provided substantive review, confirming the approach and suggesting minor commit-message improvements, all of which Patrick incorporated. The series is now complete, with no open questions or loose ends, and is ready for Junio to merge. The flake’s root cause—a `maintenance.lock` file left by a detached background process—is now clearly documented in the commit message, along with the role of the `GIT_TEST_MAINT_AUTO_DETACH` environment variable. CI evidence from GitLab (job #15762975482) underscores the fix’s importance for stability.