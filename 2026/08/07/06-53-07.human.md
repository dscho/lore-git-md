This is a substantive review in the ongoing thread about `git-replay` and `git-history` silently dropping notes during history rewriting. Elijah Newren, a foremost expert on Git's merge and history-rewriting machinery, weighs in with additional context. He notes that `git filter-repo` (and by extension, `git fast-export`/`fast-import`) also drops notes, framing this as a broader issue in Git's history-rewriting tools. His comment highlights a fundamental tension: notes are implemented as commits under the hood, so any filtering operation that treats them as such will lose the association when the underlying commits are rewritten. This isn't just an oversight in `git-replay` or `git-history` but a systemic limitation in how notes are handled during export/import workflows.

The observation doesn't propose a solution or take a position on whether notes *should* be preserved, but it reframes the problem as more architectural than incidental. The thread had previously focused on whether notes were intentionally omitted or simply unimplemented; Elijah's reply suggests the issue is deeper, touching on how notes are represented and processed in Git's object model. This could complicate any fix, as it may require changes to how notes are serialized in `fast-export` or how `filter-repo` handles them.

For a reader deciding whether to follow this thread, Elijah's input signals that the discussion is now engaging with the core design challenges of notes in history rewriting. If you care about notes, export/import workflows, or the consistency of Git's history-editing tools, this is worth tracking. The thread remains open, with no patches yet, but the scope of the problem has expanded beyond the original commands.