This is a brief follow-up in the ongoing discussion about Git commands silently dropping notes during history rewriting. Elijah Newren acknowledges erik88’s reference to workarounds for preserving notes in `git filter-repo`, confirming that while these methods exist, they are not ideal and require manual intervention. He notes that built-in support for note preservation would require changes to `git fast-export` and `git fast-import`, which currently treat notes as ordinary commits—an architectural limitation that makes them prone to being detached or discarded during filtering operations. This reinforces the broader challenge of handling notes consistently across Git’s history-rewriting tools, including `git replay` and `git history`, where the issue remains unresolved. The exchange doesn’t propose new solutions but underscores the architectural constraints that complicate note preservation. For now, the thread’s core questions—such as whether note copying should be opt-in or automatic, and how to handle plumbing versus user-facing commands—remain open.