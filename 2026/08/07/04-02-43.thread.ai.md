Thread identity: Feature request for unified interactive patch selection in Git (`git add -p`/`git checkout -p` workflow enhancement). Originating author: Junio C Hamano. Thread type: design discussion (RFC).

Core problem or goal: Address the lack of a built-in workflow for handling "good but not yet ready" hunks (category 2) during interactive patch selection. Currently, `git add -p` and `git checkout -p` operate in isolation, forcing users to use workarounds (e.g., editing hunks into no-ops) to defer hunks. The goal is to unify these workflows into a single interactive session where users can stage, discard, or defer hunks, streamlining the triage of working tree changes.

Current status: Conceptual discussion only; no patches or implementation sketches have been posted. The thread is in the early design phase, with Junio outlining the pain point and proposing a high-level solution. No decisions or agreements have been reached yet.

Key technical details:
- Subsystems involved: `add--interactive` (Perl script or C rewrite in `add-interactive.c`), `apply` machinery (used by `checkout -p`), and index manipulation logic (used by `add -p`).
- New behavior: Unified interactive session supporting three actions per hunk: stage (category 1), discard (category 3), or defer (category 2).
- Open questions: How to represent deferred hunks (persist in working tree, stash temporarily, or ignore until next session), whether this belongs in `add -p` or a new command (e.g., `git triage -p`), and how to avoid complicating the existing patch-selection UI.

Open questions and loose ends:
- How should deferred hunks be handled? Persist in working tree, stash, or ignore?
- Should this functionality be added to `add -p` or introduced as a new command (e.g., `git triage -p`)?
- How to design the UI to avoid overcomplicating the existing patch-selection workflow?
- What are the implications for the underlying `apply` and index manipulation logic?

Participant positions:
- Junio C Hamano (author): Proposed the enhancement, motivated by a real workflow friction. No implementation details provided yet.

Related work: None mentioned in the thread so far. The discussion may reference prior work on `add--interactive` or `checkout -p`, but no specific dependencies or related topics have been raised.