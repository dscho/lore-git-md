This is a **minor follow-up in the `paint_down_to_common()` optimization series**, specifically addressing a maintainability nit in the `paint_queue_get()` refactoring (patch 7/8). Kristofer Karlsson agrees with Elijah Newren’s observation that the ordering of counter checks—*before* decrementing—is critical to avoid premature loop termination, and proposes adding a code comment to document this invariant. The comment will clarify that checking counters first ensures the just-popped commit remains accounted for, preventing the loop from exiting too early and missing merge bases.

The exchange is purely about code clarity and follows Git’s coding guidelines for multi-line comments. Elijah’s original feedback was framed as "thinking out loud" rather than a demand for changes, but Kristofer views the documentation as worthwhile to prevent future regressions. The patch remains **technically complete and queued**; this is a non-blocking improvement to maintainability. The series’ broader context (a 100–1000x speedup for asymmetric merge-base queries) is unaffected, and all substantive review feedback has been addressed. The only remaining task is finalizing documentation terminology ("ordered" vs. "unordered" regions).