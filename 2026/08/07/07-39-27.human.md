This is the fourth patch in the v13 series introducing `git history squash`, a new subcommand that folds a range of commits into a single commit. This patch adds the skeleton for the subcommand, including option parsing and revision range setup, but stops short of implementing the actual squash logic.

The patch defines the command-line interface for `git history squash`, which accepts a revision range (e.g., `HEAD~3..HEAD`) and three options: `--dry-run`, `--update-refs=(branches|head)`, and `--[no-]edit`. The `--edit` flag (enabled by default) will eventually control whether the user is prompted to edit the squashed commit message. The revision range is parsed using `setup_revisions()`, but the function `setup_squash_revisions()` enforces specific `rev-list` settings (`--reverse --topo-order --ancestry-path`) and restores them if the user overrides them with other options. The patch also validates that the range includes at least one excluded commit (a "bottom" revision), since the squashed commit needs a parent outside the range.

This is a well-structured foundation for the feature. The commit message and code are clear, and the patch adheres to the project’s conventions for option parsing and revision handling. The separation of concerns—parsing and validation in this patch, with the actual squash logic deferred to later patches—makes the series easier to review incrementally. The patch is uncontroversial and appears ready for integration, assuming the rest of the series holds up. If you care about history-editing tools or the `git history` command, this is worth tracking as the series progresses.