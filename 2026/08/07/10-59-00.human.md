This is a two-patch bugfix series targeting a flaky test in `t7900-maintenance.sh`. The issue is in Git’s background maintenance machinery: the auto-detaching logic can race with the foreground process, causing maintenance to be skipped unpredictably. Patrick reports seeing this flake in GitLab CI (link provided) and has adapted the test to use a throwaway repository to avoid interference from concurrent runs. The series touches only the test file, replacing 30 lines with 46 to make the test more robust. The commit messages are concise and the motivation is clear. Given Patrick’s track record and the narrow scope, this looks like a straightforward fix that should be uncontroversial.