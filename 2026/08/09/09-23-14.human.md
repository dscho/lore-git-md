This is a small but user-facing improvement to `git send-email`. The patch clarifies the error message when a patch file lacks a subject line, explaining that the first line must start with "Subject: " and ensuring the message ends with a newline to prevent Perl from appending its internal source location. The change includes a new test case in `t/t9001-send-email.sh` to verify the improved error output.

The motivation is straightforward: the current error ("No subject line in X?") is terse and doesn’t guide users toward the expected format. The new message is more actionable, which aligns with Git’s general push toward better usability. The test coverage is thorough, checking both the content and formatting of the error.

This is a low-risk, high-value tweak. It doesn’t touch core logic, just the error reporting, and the test ensures the change behaves as intended. If you use `git send-email` regularly, this will make the tool slightly more pleasant to debug. The patch is self-contained and ready for review.