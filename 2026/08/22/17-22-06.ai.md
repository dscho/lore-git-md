This is the final exchange in the v7 patch series improving error messages for ambiguous remote branch names in `git worktree add`, `git checkout`, and `git switch`. Junio C Hamano steps back from the immediate patch to analyze the historical context and identify a latent bug in `git worktree add`'s one-argument form (e.g., `git worktree add ../topic-branch`).

The current series intentionally leaves this edge case untouched, as the command succeeds even when multiple remotes match the branch name, making advice or warnings superfluous. Junio now argues that this behavior is itself a bug: the command should error out when ambiguity exists, rather than silently creating a local branch from HEAD. He traces the issue to an incomplete refactoring in 2018 (Ævar Arnfjörð Bjarmason’s `unique_tracking_name()` update), which improved `git checkout` but left `git worktree add` unable to distinguish between zero matches and multiple matches. The current series fixes one of the two affected code paths; Junio suggests the other (the one-argument `worktree add` case) should be addressed in a follow-up effort.

The email is a classic #leftoverbits note: it documents a known issue, provides historical links, and sets the stage for future work without blocking the current patch. The tone is matter-of-fact, and the analysis is thorough enough that a future contributor could pick it up without redoing the archaeology. For the summarizer brief, note the specific files (`builtin/worktree.c`), functions (`unique_tracking_name()`), and the historical commits (3c87aa946a, 8d7b558bae) that will likely be referenced in any follow-up discussion.