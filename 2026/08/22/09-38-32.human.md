This is a brief but encouraging **bugfix follow-up** in the ongoing thread about `git config --global`’s mismatch between documented and actual behavior. Delilah Ashley Wu, the author of the proposed patch series, responds to Nils Fahldieck’s offer to review the upcoming v2 by confirming she will include additional test coverage for `` `git config --get --global` ``—a key edge case that highlights the inconsistency between `` `--get` `` and `` `--global` ``. She plans to CC Nils on the v2 series once it is ready.

The exchange signals momentum toward resolving the long-standing issue. Delilah’s patch series (previewed in GitHub PR #2196) aims to make `` `--global` `` read both `~/.gitconfig` *and* the XDG config file (`~/.config/git/config`), aligning the code with the documentation and fixing related edge cases like `` `--global --no-includes` ``. Nils’s earlier analysis and real-world use cases (e.g., the `git-get` helper tool) have strengthened the case for this change, and his offer to review the v2 series suggests active community engagement. The maintainer, Junio C Hamano, previously acknowledged the documentation was incorrect but did not endorse a fix direction; this series, combined with the renewed focus on cross-command consistency, may finally tip the balance in favor of the code change.

The patch targets `config.c`, specifically the `git_global_config()` function, and preserves backward compatibility while addressing systemic inconsistencies. The v2 series is nearly complete, and this reply confirms Delilah is addressing the last remaining test gaps. For now, the thread remains under review, with no indication of integration branch status (e.g., `seen`, `next`, or `master`). The next step is the v2 submission, which will likely reignite discussion about whether to fix the code or the documentation.