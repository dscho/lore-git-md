This is a **bugfix follow-up** in the `git config --global` documentation-behavior mismatch thread. Delilah Ashley Wu confirms she is preparing a v2 patch series to make `--global` read both `~/.gitconfig` and the XDG config file (`~/.config/git/config`), aligning the implementation with the documentation. She acknowledges Nils Fahldieck’s feedback and commits to testing both `git config --get --global` and `git config list --global` to ensure cross-command consistency. The v2 series is imminent, and Nils’s offer to review it signals active community engagement that could help push the fix toward acceptance.

The patch targets the configuration subsystem (`config.c`), specifically modifying `git_global_config()` to delegate to `do_git_config_sequence()`—the same function used by `--get`—to read both files. This change preserves backward compatibility while resolving the long-standing inconsistency between `--global` and `--get` behavior, as well as edge cases like `--global --no-includes`. The series will include tests to verify the new behavior, addressing the systemic issues raised in the thread.

Junio C Hamano previously agreed the documentation was incorrect but did not endorse a fix direction. Delilah’s series, combined with renewed emphasis on cross-command consistency and real-world tooling failures (e.g., the `git-get` helper tool), strengthens the case for merging the code change. The thread is now focused on implementation rather than debate, with the v2 series expected to be the most concrete step toward resolution. The only remaining uncertainty is whether Junio will accept the behavioral change or continue to favor updating the documentation instead.

For future summarization, note the key technical details: the patch touches `config.c`, modifies `git_global_config()`, and ensures `--global` behaves identically to `--get` by reading both config files. The v2 series will include test coverage and is nearly ready for submission. Nils Fahldieck’s offer to review it suggests the thread is moving toward active collaboration.