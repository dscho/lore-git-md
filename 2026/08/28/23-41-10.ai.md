This is Junio’s "What’s cooking" report for August 2026, summarizing the current state of integration branches (`master`, `next`, `seen`) and highlighting new, cooking, stalled, and graduated topics. Below is the key technical detail a future AI summarizer would need to track follow-up discussion:

**Graduated to `master` (irreversible for next release):**
- `kh/format-rev-doc-synopsis` – documentation cleanup for `git format-rev` using `[synopsis]` blocks.
- `sk/object-name-use-after-free` – fixes a heap-use-after-free in `get_oid_with_context_1()` when reporting failures with relative paths in sparse directories.

**New topics (not yet in `next`):**
- `jc/die-for-incompatible-opts` – variadic `die_for_incompatible_opts()` to handle any number of mutually exclusive options.
- `jc/you-still-use-that` – rewords deprecation warnings to clarify finality of removal.
- `ws/squelch-svn-migrate` – adds `NO_GIT_SVN` build knob and silences v1-layout migration noise when nothing needs migration.

**Stalled (awaiting author response):**
- `ec/commit-fixup-options` – extends `--fixup` to accept `-m/-F/-c/-C` for all fixup variants.
- `ps/libgit-in-subdir` – moves `libgit.a` sources into `lib/`; ejected due to evil merges.
- `hs/rebase-continue-edit` – adds `--[no-]edit` to `rebase --continue`.
- `sn/rebase-update-refs-symrefs` – guards symref targets in `--update-refs`.
- `tb/pack-with-duplicates` – MIDX and packfile handling for duplicate objects.
- `tc/replay-linearize` – `--linearize` option for `git replay`.
- `cl/regexec-macos-leak` – macOS TRE leak workaround for invalid UTF-8.
- `cc/lazy-fetch-trusted-bit` – `uploadpack.lazyFetchTrusted` config for promisor remotes.

**Cooking in `next` (integration testing):**
- `kn/reftable-optimize-reloading` – avoids redundant stat/reload when stack is already locked.
- `ps/odb-pluggable-pack-generation` – pluggable packfile generation via ODB callbacks; used by `bundle`, `send-pack`, `upload-pack`.
- `ps/odb-eagerly-load-alternates` – alternates loaded at ODB init, removing lazy-loading calls.
- `ps/odb-generic-corrupt-objects` – ODB API distinguishes missing vs corrupt objects with strbuf errors.
- `ps/odb-pluggable-fsck` – moves fsck checks into backend-specific layers.
- `jt/receive-pack-pluggable-writes` – ODB transaction interface for writing incoming packfiles.
- `ty/repository-fetch-if-missing` – moves `fetch_if_missing` into `struct repository`.
- `vm/complete-history` – command-line completion for `git history` subcommands.
- `fr/pack-objects-trace-pack-bytes` – trace2 records total bytes written to packfiles.
- `kh/trailers-no-urls` – stops treating lines starting with URLs as trailers.
- `yn/worktree-add-no-dwim-with-b` – disables DWIM when `-b` or `-B` is given.
- `yn/worktree-ambiguous-remote-advice` – improves advice for ambiguous remote branches.

**Notable waiting-for-review topics:**
- `dw/config-read-both-global` – respects both `$HOME/.gitconfig` and `$XDG_CONFIG_HOME/git/config`.
- `vv/branch-recurse-no-start-ref` – fixes `--recurse-submodules` crash with raw object IDs.
- `en/no-amend-during-conflicts` – blocks `--amend` and partial commits during conflict resolution.
- `ps/fetch-packfile-uris-parallel` – parallelizes packfile URI fetches via `fetch.packfileURIThreads`.
- `kn/receive-report-hook` – new `report` hook for `receive-pack` to filter/modify status reports.
- `en/midx-missing-pack-fallback` – recovers when MIDX points to removed packs.
- `kj/repo-info-more-path-keys` – adds `path.cdup`, `path.git-prefix`, `path.toplevel`, etc. to `git repo info`.
- `ds/trace2-tolerate-failed-timestamp` – bans `die()` in trace2 to avoid recursion.

**Controversial / likely to see follow-up:**
- `en/no-amend-during-conflicts` – behavior change blocking `--amend` during conflicts.
- `ps/odb-pluggable-fsck` – architectural shift moving fsck into ODB backends.
- `kn/receive-report-hook` – new hook with potential security implications.