Here’s the human-mode narrative summary for a developer catching up:

This is Junio’s August 2026 “What’s cooking” report, the regular snapshot of every in-flight topic. Since the last report, two documentation cleanups and a use-after-free fix have graduated to `master`, while twelve topics—including Patrick Steinhardt’s ODB abstraction work, Karthik Nayak’s reftable optimizations, and Elijah Newren’s conflict-resolution safety net—are now cooking in `next` for integration testing. Eight topics remain stalled, and another eight are still waiting for review.

The most visible behavioral change is Elijah’s `en/no-amend-during-conflicts`, which blocks `--amend` and partial commits while conflicts are unresolved; it’s marked controversial but has no explicit objections yet. Patrick’s ODB pluggability series (`ps/odb-*`) continues to restructure the object database, exposing new callbacks for packfile generation, fsck, and transactions; the fsck backend shift is the most architecturally significant and may draw closer scrutiny. Karthik’s `kn/receive-report-hook` introduces a new `report` hook for `receive-pack` that lets servers filter or rewrite status reports; Junio has flagged it as potentially risky.

Toon Claes just un-stalled two of his topics. The `tc/replay-linearize` series (v9) adds a `--linearize` option to `git replay` that drops merge commits, matching `git rebase --no-rebase-merges` behavior. The `tc/last-modified-bloom` series (v3) speeds up `git last-modified` by reusing the Bloom-filter logic from `git log`; this version addresses Junio’s feedback on precondition checks and wildcard pathspecs. Both series are now back in play after Toon’s return from leave.