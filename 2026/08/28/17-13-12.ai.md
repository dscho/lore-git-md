This is a thread closure confirming the abandonment of the `[PATCH 0/3] format-patch: add --[no-]range-diff-notes` series. Junio C Hamano acknowledges Kristoffer Haugsbakk’s decision to drop the topic after Junio’s earlier critique of the option semantics as overly complex and conceptually flawed.

The email restates the core usability issue: the interaction between `--range-diff-notes` and the existing `--notes` could not be defined in simple terms, making the feature’s behavior unintuitive. Junio briefly sketches a potential alternative design—where `--notes` controls only the final patch output and `--range-diff-notes` controls only the range-diff material—but notes that no such redesign was proposed before the author chose to abandon the effort. The thread now ends without further action.

Key technical context for follow-up: the series touched `builtin/log.c`, `log-tree.c`, `revision.c`, `revision.h`, and `Documentation/git-format-patch.txt`, introducing a `rdiff_override_notes` flag and renaming `rdiff_log_arg` to `rdiff_notes_arg`. These changes are now moot unless someone revives the feature with a cleaner design. The goal—allowing independent control over notes in range-diffs versus patches—remains unmet. If another contributor picks this up, they will need to address the usability concern, likely by maintaining separate notes lists for the two contexts.