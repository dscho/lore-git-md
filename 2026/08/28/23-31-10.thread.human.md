Here’s a concise narrative summary of the Git mailing list thread based on Junio’s "What’s cooking" report for August 2026:

This thread is Junio C Hamano’s latest "What’s cooking" report, a high-level snapshot of the project’s integration branches and in-flight topics. Since Git 2.55, 567 non-merge commits have landed in `master`, including two small but critical fixes: a documentation tweak for `git format-rev` and a heap-use-after-free bug in object name parsing. The `next` branch currently holds 76 topics, while `seen` has 215, reflecting a busy cycle focused on backend refactoring and targeted improvements.

New topics this cycle include a plumbing refactor to generalize mutually exclusive option handling (`jc/die-for-incompatible-opts`), a clearer deprecation notice for obsolete commands (`jc/you-still-use-that`), and a build knob to silence `git-svn` migration noise (`ws/squelch-svn-migrate`). Several topics are cooking in `next` or awaiting review, such as an optimization for reftable stack reloads (`kn/reftable-optimize-reloading`), a pluggable packfile generation mechanism for ODB backends (`ps/odb-pluggable-pack-generation`), and a safety improvement to block `commit --amend` during conflicts (`en/no-amend-during-conflicts`). A small bugfix for `git worktree repair` with relative paths (`yn/worktree-repair-relative`) is also in progress.

Stalled topics highlight areas needing attention. `ec/commit-fixup-options` (extending `--fixup` to support more commit options) and `ps/libgit-in-subdir` (a large refactoring of `libgit.a`) have languished due to integration conflicts or reviewer bandwidth. `tb/pack-with-duplicates` (hardening packfile handling) and `cl/regexec-macos-leak` (a macOS-specific regex leak workaround) remain controversial or unresolved. The Git 3.0 preparation effort is visible in `bc/restrict-hex-to-lowercase`, which proposes rejecting uppercase hex object IDs, though the topic is stalled over whether the restriction should be opt-in or mandatory.

Large-scale efforts continue to advance, particularly Patrick Steinhardt’s ODB abstraction work and Karthik Nayak’s reftable optimizations. The cycle is heavy on backend improvements, with fewer user-facing changes beyond the `commit --amend` safety tweak. The stalled topics suggest opportunities for contributors to revive discussions or address lingering concerns, especially around `git-svn`, macOS compatibility, and the Git 3.0 transition.