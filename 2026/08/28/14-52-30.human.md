This is a surface-level review comment on patch 4/8 of Patrick Steinhardt’s ODB alternates refactoring series. The series is part of the ongoing ODB abstraction effort, aiming to simplify alternates handling by writing alternates during repository creation rather than allowing dynamic updates.

Toon Claes notes a minor observation about the use of `commondir` in the patch: the `strbuf` is initialized and populated unconditionally, but only used when `option_shared` is set. He points out that this will become relevant in a later patch (likely patch 5/8), where the `commondir` logic is extended to handle non-shared local clones. The comment is neutral and does not critique the patch’s correctness or design—it merely highlights a detail that might seem odd in isolation but makes sense in the context of the full series.

This is a typical surface-level review: it focuses on code structure and readability rather than technical behavior or edge cases. The observation is unlikely to prompt changes, as the follow-up patch resolves the apparent redundancy. The series remains under review, with patches 6-8 (the core design changes) expected to draw more substantive feedback from ODB-focused contributors.