This is a cleanup patch in Junio C Hamano’s eight-part refactoring series that restructures the `git checkout`, `git switch`, and `git restore` internals. The series as a whole extracts reusable logic from the built-in commands into standalone helpers, eliminating the monolithic `checkout_main()` choke point to align with the project’s architectural direction for libification.

Patch 7/8 is a post-refactoring style cleanup that wraps overly long lines and replaces manual option-compatibility checks with the existing `die_for_incompatible_optN()` helper macros. It touches only `builtin/checkout.c`, changing 54 lines (34 insertions, 20 deletions). The patch is purely cosmetic—no behavior changes, no new functionality, and no impact on the test suite. It tidies up the code after the heavy lifting of the earlier patches, making it more readable and consistent with Git’s coding style.

The patch is uncontroversial and self-contained. It could be merged independently if the rest of the series requires more time, but Junio has explicitly marked the entire series as an early draft for illustration, not yet intended for `seen`. This patch is a natural follow-up to the core refactoring in patches 1–6, which extracted utility functions and eliminated `checkout_main()`. The changes here are mechanical and low-risk, focusing on readability rather than logic.

For a busy reader, this patch is worth noting only as part of the larger series. It doesn’t introduce new ideas or raise questions, but it completes the cleanup phase of the refactoring. If you’re tracking the architectural shift toward libification, the earlier patches (especially 6/8) are the ones to watch; this one is just polish.