This is the first patch in Junio C Hamano’s 8-patch series refactoring the `git checkout`, `git switch`, and `git restore` internals to eliminate the monolithic `checkout_main()` helper. The patch removes a file-scope static variable (`cb_option`) that tracks whether a new branch is being created via `-b` (checkout) or `-c` (switch), replacing it with an explicit parameter passed to the branch name parsing functions.

The change is a small but meaningful step toward making the code more modular and easier to refactor. By removing the global mutable state, the patch reduces coupling between the branch name parsing logic and the rest of the checkout machinery, which will simplify later patches in the series. The diff is clean and mechanical: the static variable is replaced with a local variable in `checkout_main()`, and the value is passed explicitly to `parse_branchname_arg()` and `parse_remote_branch()`. The BUG() macro is updated to check the new parameter, ensuring the change is safe.

This patch could stand alone as a cleanup, but it’s clearly part of a larger effort to libify reusable logic in the checkout code. The commit message is concise and explains the motivation well, though it doesn’t explicitly tie this change to the broader architectural goals of the series (e.g., eliminating `checkout_main()` or enabling libification). That context is available in the cover letter and earlier threads, but a reader seeing only this patch might not immediately grasp its significance.

The patch applies cleanly and passes the test suite, which is unsurprising given its mechanical nature. It’s a well-crafted, low-risk change that sets the stage for the more invasive refactoring in later patches. If the series as a whole is accepted, this patch will be uncontroversial. The only open question is whether the broader architectural direction—extracting reusable logic into standalone helpers—will gain consensus, but this patch itself doesn’t touch that debate.