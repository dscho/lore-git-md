This is a cleanup patch in Junio C Hamano's 8-patch series refactoring the `git checkout`, `git switch`, and `git restore` internals. It is the seventh patch, following the core architectural changes that eliminated the `checkout_main()` choke point and extracted reusable logic into standalone helpers.

The patch tidies up `builtin/checkout.c` by wrapping overly long lines and replacing manual option-compatibility checks with the `die_for_incompatible_optN()` helper macros. It changes 54 lines (34 insertions, 20 deletions) in a single file, with no functional or behavioral impact. The motivation is readability: the earlier patches in the series moved code without reformatting to ease review, and this patch addresses the resulting long lines and inconsistent error-handling patterns.

Key changes include breaking up long `die()` calls (e.g., for `--unified` requiring `--patch`), replacing manual `if (opts->foo && opts->bar) die(...)` checks with `die_for_incompatible_opt2()` and `die_for_incompatible_opt3()`, and reformatting option strings (e.g., `-c`/`--create` vs `-C`/`--force-create`) for consistency. The patch also wraps long `OPT_BOOL()` and `parse_pathspec_file()` argument lists to stay within 80 columns.

This is a surface-level cleanup that depends on the earlier patches in the series. It is uncontroversial and aligns with Git's coding guidelines, but its value is primarily aesthetic and maintenance-oriented. The patch is part of a larger refactoring effort that has already passed the test suite, and it does not introduce new symbols or touch other files. The series remains an early draft for discussion, not yet intended for merging to `seen`.