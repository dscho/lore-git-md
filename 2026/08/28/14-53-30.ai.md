This is a surface-level review of the sixth patch in Patrick Steinhardt’s 8-patch series refactoring ODB alternates handling. The series is part of the ongoing ODB abstraction effort and aims to remove the ability to write alternates after repository creation, simplifying the ODB interface.

Toon Claes’s comment is purely about code organization: he notes that `struct strvec` is already forward-declared later in `odb/source.h` and suggests moving the new `#include "strvec.h"` after that declaration to avoid redundancy. This is a minor, mechanical observation that does not engage with the patch’s technical substance, correctness, or design. The feedback is unlikely to prompt any functional changes and reflects a preference for header hygiene rather than a concern about the patch’s behavior or implementation.

The patch itself remains uncontroversial and well-scoped. It implements the core design change by adding support for writing alternates at ODB creation time via a new `odb_create_on_disk_options` struct, paving the way for the removal of the `write_alternates()` callback in the next patch. No other feedback has been raised on this patch, and the series as a whole continues to progress through review. Reviewers are expected to focus on patches 7 and 8, which complete the refactoring by removing the old infrastructure.