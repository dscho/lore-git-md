This is the sixth patch in Junio C Hamano’s 8-patch series refactoring the `git checkout`, `git switch`, and `git restore` internals. It eliminates the monolithic `checkout_main()` choke point by rewriting the three command entry points (`cmd_switch()`, `cmd_restore()`, `cmd_checkout()`) to call the extracted utility helpers directly.

The patch removes `checkout_main()` and its supporting `enum checkout_command`, replacing them with standalone implementations for each command. `cmd_switch()` and `cmd_restore()` are rewritten from scratch to initialize their options, validate them, and dispatch to `checkout_branch()` or `checkout_paths()` directly. `cmd_checkout()` is similarly restructured but retains its dual-mode behavior (branch switching or path restoration). The diff is large (297 lines changed) but mechanical: it moves logic from the shared helper into the individual commands, eliminating the need for the `enum checkout_command` dispatch mechanism.

This is the core architectural change in the series, directly implementing the libification path clarified in earlier threads. By removing `checkout_main()`, the patch eliminates the "cheating" pattern where `git switch` and `git restore` were bolted onto `git checkout` via a shared helper. The result is a cleaner separation of concerns: each command now calls the appropriate backend (`checkout_branch()` or `checkout_paths()`) directly, using the utility helpers introduced in earlier patches. No CLI behavior or on-disk format changes are introduced.

The patch is part of a larger refactoring effort that aligns with the `the_repository` removal architectural direction. It is explicitly marked as an early draft for discussion, not for merging to `seen`, due to potential conflicts with in-flight topics. The first five patches in the series are self-contained and could be merged independently; this patch depends on them and is the linchpin of the refactoring. No objections or alternatives have been raised yet, but the scale of the change suggests it will require careful review.