This is the third patch in Junio C Hamano’s 8-patch series refactoring the `git checkout`/`switch`/`restore` internals. It moves the validation of `--ours`, `--theirs`, `--force`, and `--merge` option compatibility from the monolithic `checkout_main()` helper into `checkout_paths()`, where the logic logically belongs.

The change is a small, targeted refactoring: five lines are removed from `checkout_main()` and five are added to `checkout_paths()`. The validation now runs only when pathspecs are present, which is the only case where index stages are relevant. The patch is purely internal, does not alter CLI behavior, and passes the test suite. It is uncontroversial and self-contained, making it a candidate for independent merging if the rest of the series requires more time. This patch continues the architectural direction established in the parent thread: reusable logic is factored out of the monolithic `checkout_main()` choke point into the appropriate backend functions.