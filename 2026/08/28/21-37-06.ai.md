This is a review comment on the bugfix patch that addresses negative pathspec handling in `git ls-files` and `git add`. Junio C Hamano raises a substantive question about the patch's approach: whether the current fix (skipping prefix-stripping for negative pathspecs entirely) is the right design, or if the common prefix should instead be calculated across *both* positive and negative pathspecs.

The review centers on a concrete example: `git ls-files -- a/b/c a/b/d !a/b/`. Junio suggests that stripping `a/b/` from all three pathspecs (including the negative one) might be more intuitive, leaving the negative pathspec relative to the full tree rather than treating it as an absolute path. This implies the fix might be better implemented by modifying `common_prefix()` to include negative pathspecs in its calculation, rather than adding a conditional guard in `do_match_pathspec()`.

The comment is narrowly scoped but touches on a design choice with potential implications for pathspec behavior. No alternative implementation is proposed yet, and the discussion remains open-ended. The author (Diogo Castro) will likely need to respond to this question before the patch can proceed. This is a substantive review, not surface-level feedback.