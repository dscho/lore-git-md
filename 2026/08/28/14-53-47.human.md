This is the final patch in Patrick Steinhardt’s 8-patch series refactoring how Git handles ODB alternates during repository creation. The patch removes the now-unused `odb_source_write_alternates()` function and its supporting infrastructure across all ODB backends, deleting 118 lines of code. This cleanup completes the series’ goal of writing alternates exclusively during ODB creation, eliminating the ability to modify them afterward.

Toon Claes raises a substantive question in his review: why the new implementation (introduced in patch 6) does not use a lockfile when writing alternates, while the old code did. This is a valid technical concern about atomicity and race conditions during alternates setup. The question suggests the review is moving beyond surface-level observations to engage with the design implications of the refactoring.

The series is part of Patrick Steinhardt’s ongoing ODB abstraction effort, which aims to give backends more control over their configuration. Earlier patches consolidated alternates handling into a single code path and fixed a latent bug in worktree alternates resolution. This final patch is mechanical but important—it removes dead code and enforces the new design, but Toon’s question indicates reviewers will scrutinize whether the new approach maintains the same safety guarantees as the old one.

No other concerns have been raised yet, but this question could prompt discussion about error handling, atomicity, or whether the new design adequately protects against concurrent modifications. The patch itself is uncontroversial in its intent, but the review may reveal whether the implementation fully addresses the edge cases the old code handled.