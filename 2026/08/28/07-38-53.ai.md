This is a **bugfix patch discussion** in Elijah Newren’s three-part series that extends Git’s protection against unsafe commit operations during conflict resolution. The series blocks both `git commit --amend` and `git commit <paths>` during conflict resolution for `git am`, `git revert`, and all `git rebase` variants, except for interactive rebase directives (`squash`, `reword`, `edit`, `break`).

The email responds to Junio C Hamano’s **substantive technical question** about the necessity of blocking `git commit <paths>` during conflict resolution for commits that became empty. Junio asks why a partial commit of an empty commit would be problematic, noting that the result would still be an empty commit. Elijah acknowledges the question is valid and traces the restriction’s origin to a 2019 commit (430b75f7209c) that updated the error message for rebases without reconsidering the underlying logic. He proposes addressing the issue in v3 with a preparatory patch, but does not yet commit to removing the restriction.

The exchange leaves the series’ direction uncertain: the restriction on partial commits for empty commits may be revised or removed entirely, depending on further discussion. The rest of the series—blocking `git commit --amend` and covering additional commands—remains uncontroversial. No new implementation details or test changes are introduced in this reply. The thread remains under active review with no merge status recorded.