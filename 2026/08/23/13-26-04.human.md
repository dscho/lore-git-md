This is a policy discussion reply in the ongoing thread about replacing `utime()` with `utimensat()` for POSIX.1-2024 compatibility. The thread is currently blocked by a process objection to AI-generated content, and this email adds a new dimension by pointing out precedent.

Weijie Yuan notes that Johannes Schindelin (Dscho) has previously contributed commits to Git that include an `Assisted-by` trailer, and those commits have already been merged to `master`. The email does not specify what kind of assistance was involved (whether AI, tooling, or human collaboration), but the implication is that the project has already accepted some form of assisted contributions without explicit policy resolution. Weijie suggests this precedent should be considered in the current discussion, as it blurs the line between "we won’t accept AI-generated work" and "we don’t trust new contributors."

The email also references a 2026 Linux kernel mailing list post from Linus Torvalds, where he argues that the chain of trust matters more than whether AI was used. Weijie acknowledges that Git does not need to follow the kernel’s rules but suggests the perspective is relevant to the Git project’s own policy debate. The tone is respectful and aims to broaden the discussion rather than challenge Junio or brian m. carlson directly.

This reply does not resolve the policy question but introduces a concrete example of prior assisted contributions that were accepted, which may complicate the argument for a strict ban on AI-generated content. The next steps likely depend on whether the Project Leadership Committee (PLC) or the broader community views Dscho’s `Assisted-by` commits as a one-off exception or as evidence that the current policy needs clarification. Until then, the `utimensat()` series remains blocked.