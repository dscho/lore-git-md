This is the **eleventh patch in the 14-patch RFC series** introducing `git organize`, a tool for restructuring Git's source tree. This patch moves the **revision subsystem** (14 C sources and 15 headers) into a new `revision/` directory, updating all references—`#include` directives, build system entries (Makefile and meson.build), and tutorial examples—to reflect the new paths.

The patch is **mechanical and self-contained**: it moves 29 files (14 `.c` and 15 `.h`) from the root into `revision/`, updates 142 files to point to the new paths, and results in **net-zero line churn**, confirming that no functional changes were introduced. The tree remains buildable after the move, as the patch updates all relevant references in the build system, documentation, and codebase. The `.gitorganize` file is also updated to reflect the new paths for the revision-related files.

This is part of the **demonstration phase** of the `git organize` series, where the tool is used to carve out subsystems (`odb/`, `refs/`, `pack/`, `diff/`, and now `revision/`) into their own directories. The patch leverages the `--label=component=revision` selector introduced in the fourth patch to apply the move incrementally. The remaining backlog of 79 files labeled `component=?` (unchanged by this patch) may prompt discussion about whether this category is useful or needs refinement.

The patch touches **no new symbols or behaviors**—it is purely a structural reorganization. The revision subsystem is a core part of Git, and its move affects many other subsystems (e.g., `diff/`, `pack/`, `odb/`), as evidenced by the 142 files updated. Reviewers may focus on whether all references were caught (e.g., documentation, test fixtures, or scripts not covered by the organizer script) and whether the `revision/` directory name aligns with the project's broader goals (e.g., Patrick Steinhardt's `libgit.a` series, which might prefer `lib/revision/`). Performance and scalability are less of a concern here, as the patch is a one-time move, but the tool's ability to handle such a large-scale reorganization (347 files updated) is noteworthy.