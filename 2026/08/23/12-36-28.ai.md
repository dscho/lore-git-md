This is a surface-level review comment in the ongoing thread about Delilah Ashley Wu’s three-patch v2 series fixing the `--global` config listing inconsistency. The series has already addressed all prior feedback, including path normalization scope, test methodology, and regression concerns, and is technically complete with Johannes Schindelin’s Reviewed-by tags on all three patches.

Chris Torek’s question asks which global config file (`$HOME/.gitconfig` or `$XDG_CONFIG_HOME/git/config`) `git config --global --edit` currently edits and which it *should* edit. This is a natural follow-up to the series’ core problem—aligning `--global` operations with Git’s documented behavior of reading both files—since the edit operation is the write-side counterpart to the read-side fix. The question does not challenge the series’ implementation but instead probes a related user-facing behavior that may need similar consistency work. No technical details or code suggestions are provided; the comment is purely exploratory and likely intended to surface a potential edge case for future consideration.