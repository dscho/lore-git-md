Here is the updated AI thread summary:

---

Thread identity: Patch series improving `git push` performance from shallow clones by avoiding redundant tree transfers. Originating author: Elijah Newren. Thread type: patch series (single patch).

Core problem or goal: Eliminate the inefficiency where `git push` from shallow clones resends the entire toplevel tree (gigabytes in large repos) even for tiny changes, due to walking back to shallow grafts and assuming the server lacks them. The goal is to reduce unnecessary data transfer while maintaining backward compatibility. A key clarification emerged: `--depth=2` clones do not reliably solve this problem unless combined with `push.negotiate=true`, as they still resend the toplevel tree when the server has advanced, and workflow-specific quirks (e.g., forge configurations, branch deletion, or squash merges) can break even the accidental workaround they provide.

Current status: Under review. Two competing approaches are now on the table:
1. **Elijah’s original patch**: Introduces a new config option, `push.shallowExcludeBoundary` (default: `true`), which instructs `send-pack` to treat shallow grafts as uninteresting tips during pack generation, avoiding redundant transfers. The rare case where a server is being seeded with new shallow roots (requiring the full tree) can opt out by setting the config to `false`.
2. **Patrick’s alternative**: Proposes enabling `push.negotiate` by default (or conditionally for shallow clones) via a new `push.negotiate=shallow` mode, which would automatically enable negotiation for shallow clones. This avoids the latency overhead for full clones while addressing the redundant tree-transfer problem at the negotiation layer rather than in `send-pack`.

Key technical details:
- Files touched: `send-pack.c`, `Documentation/config/push.txt`, and test file `t5538-push-shallow.sh`.
- New function in Elijah’s patch: `append_reachable_shallow_grafts()`, which walks from pushed tips to their shallow grafts, adding those grafts to the pack boundary as negative tips. It stops early at any commit the server already has.
- New config keys:
  - `push.shallowExcludeBoundary` (boolean, default `true`) in Elijah’s patch.
  - `push.negotiate=shallow` (proposed by Patrick) as an alternative.
- Behavior:
  - Elijah’s patch: When `push.shallowExcludeBoundary` is enabled, shallow grafts are treated as uninteresting tips, reducing redundant data transfer. When disabled (`false`), the original behavior is preserved for seeding new shallow roots.
  - Patrick’s proposal: `push.negotiate=shallow` would enable negotiation only for shallow clones, avoiding the latency overhead for full clones.
- Test coverage: Added tests in `t5538-push-shallow.sh` for Elijah’s patch, including edge cases (e.g., rejected refs not over-excluding objects needed by accepted refs, interaction with `receive.shallowUpdate`).
- Implementation notes:
  - Patrick Steinhardt raised questions about the safety of the current behavior when shallow grafts are locally modified (e.g., via `git commit --amend`), noting that Git currently treats amended grafts as new root commits—a behavior Elijah considers a separate issue and plans to address in a future series.
  - Elijah accepted Patrick’s suggestions to mark input-only parameters as `const` and combine loops over `refs` for efficiency, but declined to use higher-level commit-graph interfaces like `repo_is_descendant_of()`, explaining that the explicit walk is necessary to stop at commits the peer already has and identify graft boundaries.
  - The patch does not negotiate tree objects for graft verification, as this would require protocol changes and offers limited benefit over existing commit-based negotiation.
  - Clarification: `--depth=2` clones do not reliably mitigate the redundant tree-transfer problem. They may *accidentally* help in specific workflows (e.g., forges that retain merge-request refs and advertise them, and users who never push directly to main branches or use squash merges), but GitHub’s configuration (hiding `refs/pull/` and users deleting branches on merge) breaks this, and squash merges further undermine it. The proposed config directly addresses the negotiation gap.

Open questions and loose ends:
- Which approach to adopt: Elijah’s `push.shallowExcludeBoundary` config or Patrick’s `push.negotiate=shallow` mode. The trade-off involves reduced bandwidth/CPU usage (Elijah’s patch) versus increased latency due to an extra round-trip (Patrick’s proposal).
- Whether the new config (either approach) should default to `true` outright, or if users should opt in.
- The current behavior of treating locally amended shallow grafts as new root commits is acknowledged as a foot-gun but is considered tangential to this patch. Elijah plans to address this in a future series.
- Request for additional test coverage for graft modification scenarios to clarify the patch’s impact on the current behavior.

Participant positions:
- **Elijah Newren**: Authored the original patch and addressed Patrick’s review in detail. Clarified the patch’s scope, accepted minor implementation improvements, and explained design choices (e.g., avoiding protocol changes for tree negotiation). Reinforced the necessity of the proposed config by debunking the `--depth=2` workaround myth, noting its fragility in real-world workflows. Plans to address the graft modification foot-gun separately. Has not yet responded to Patrick’s alternative proposal.
- **Patrick Steinhardt**: Provided a substantive technical review, raising questions about graft modification safety, negotiation scope, and default config behavior. Initially suggested minor implementation improvements (e.g., `const` parameters, loop efficiency). Later shifted perspective, proposing `push.negotiate=shallow` as a simpler, more targeted fix. No blocking objections to Elijah’s patch, but clearly prefers the negotiation-based approach. The discussion remains constructive.

Related work: None mentioned in the thread.

---