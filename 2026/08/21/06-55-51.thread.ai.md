Here is the updated AI thread summary:

---

Thread identity: Patch series improving `git push` performance from shallow clones by avoiding redundant tree transfers. Originating author: Elijah Newren. Thread type: patch series (now six patches at v3).

Core problem or goal: Eliminate the inefficiency where `git push` from shallow clones resends the entire toplevel tree (gigabytes in large repos) even for tiny changes, due to walking back to shallow grafts and assuming the server lacks them. The goal is to reduce unnecessary data transfer while maintaining backward compatibility and addressing usability concerns raised in review. The series now introduces a tri-state config option (`push.shallowExcludeBoundary`) to balance performance, correctness, and user control, with improved error messages and recovery advice.

Current status: Under review. The series is now at v3, expanded to six patches to address edge cases and usability concerns. The first two patches (preparatory) improve error reporting and reduce noise in `unpack-objects.c` and `receive-pack.c`, respectively. The first patch distinguishes missing objects from type mismatches, aligning diagnostics with `index-pack` and enabling clearer user guidance in later patches. The second patch silences redundant per-ref connectivity error messages during bulk checks, preserving exit status and rejection logic while reducing noise. The core optimization (patch 4) remains largely unchanged from v2, but the default behavior is now conservative ("false") to preserve backward compatibility. The series includes a fifth patch arguing for changing the default to "true" (the optimization), and a sixth patch adding advice for users to retry problematic multi-ref pushes separately. The discussion remains centered on **trade-offs between performance, correctness, and usability**, with unresolved questions about the default behavior and the acceptability of false-positive failures.

Key technical details:
- Files touched: `send-pack.c`, `receive-pack.c`, `unpack-objects.c`, `shallow.c`, `Documentation/config/push.txt`, and test files (`t5538-push-shallow.sh`, `t5504-fetch-receive-strict.sh`, `t5410-receive-pack.sh`, and others).
- New functions:
  - `append_reachable_shallow_grafts()` (carried over from v2): Walks from pushed tips to their shallow grafts, adding those grafts to the pack boundary as negative tips. Stops early at commits the server already has.
  - Preparatory patches introduce helper functions for distinguishing missing objects from type mismatches (`unpack-objects.c`) and suppressing repeated connectivity errors (`receive-pack.c`).
- New config key: `push.shallowExcludeBoundary` (tri-state: `true`/`false`/`abort`), defaulting to `false` in v3 for backward compatibility. The fifth patch argues for changing the default to `true`.
- Behavior:
  - When `push.shallowExcludeBoundary=true`: Shallow grafts are treated as uninteresting tips, reducing redundant data transfer. The optimization is applied.
  - When `push.shallowExcludeBoundary=false`: Original behavior is preserved (full tree transfer for shallow clones).
  - When `push.shallowExcludeBoundary=abort`: The push is aborted if shallow grafts are detected, forcing the user to choose explicitly.
  - The sixth patch adds advice for users to retry problematic multi-ref pushes separately, addressing the false-positive failure mode.
  - Patch 1 (v3) improves error messages in `unpack-objects.c` by using `odb_read_object_info()` to check for object existence before comparing types, reporting "did not receive expected object <oid>" for missing objects and reserving type-mismatch messages for actual type errors.
  - Patch 2 (v3) silences redundant per-ref connectivity error messages during bulk checks in `receive-pack.c` by setting `opt.quiet = 1` in `set_connectivity_errors()`, preserving exit status and rejection logic while reducing noise.
- Test coverage: Expanded tests in `t5538-push-shallow.sh` and new tests for edge cases, including over-exclusion (where a rejected ref could incorrectly omit objects needed by an accepted ref) and interaction with `receive.shallowUpdate`. The series ensures shallow boundaries are rejected without disconnecting and that error messages are more precise. Patch 1 updates `t/t5504-fetch-receive-strict.sh` to verify the new error message, and patch 2 updates `t/t5410-receive-pack.sh` to verify that error messages now appear in packetized output (not stderr) and that stderr remains empty.
- Implementation notes:
  - The tri-state config allows users to opt out entirely ("abort") if they prefer not to choose between performance and correctness.
  - The core optimization (patch 4) only excludes shallow grafts reachable from the pushed refs, stopping early at commits the server already has.
  - The preparatory patches improve error handling by distinguishing missing objects from type mismatches and suppressing redundant connectivity errors.
  - The series does not adopt Patrick’s `push.negotiate=shallow` proposal or Stolee’s suggestion to repurpose `push.negotiate` for shallow repos, instead focusing on the config-based approach with clearer documentation of trade-offs.
  - **Connectivity checks**: `receive-pack` enforces connectivity checks (commit `52fed6e1ce07`) regardless of protocol, ensuring no corruption risk from over-exclusion. The false-positive failure mode (rejecting valid refs) is the only remaining concern, now addressed with improved error messages and recovery advice.
  - **Assumption matrix**: Elijah’s 6-case matrix (from v2) remains relevant, arguing that assuming the shallow graft exists on the server is **vastly superior** in most cases (faster success or failure) and that the edge case where the assumption is wrong (case C) is rare and recoverable.

Open questions and loose ends:
- **Default behavior**: Is the project comfortable with changing the default of `push.shallowExcludeBoundary` from `false` to `true` in the future? The fifth patch argues for this change, but the trade-off between performance and false-positive failures may still be contentious.
- **Stolee’s response**: Will Stolee accept the improved error messages and recovery advice as sufficient mitigation for the false-positive failure mode, or does he still consider it a blocker? His silence on v3 leaves this unresolved.
- **Approach selection**: The series does not adopt Patrick’s `push.negotiate=shallow` proposal or Stolee’s repurposing of `push.negotiate` for shallow repos. Is the config-based approach with a tri-state option the right long-term solution, or should the project revisit the negotiation-based alternatives?
- **Graft modification behavior**: The current behavior of treating locally amended shallow grafts as new root commits remains a foot-gun, as noted by Patrick. Elijah plans to address this in a future series, but it is not part of this patch set.
- **Race condition**: The race between the client’s "have" check and server-side repacking (identified in v2) remains unaddressed. This affects `push.negotiate=true` but is not directly relevant to this series.
- **Usability**: Are the improved error messages and recovery advice sufficient to address the usability concerns raised by Stolee’s test case? The sixth patch adds advice for retrying problematic pushes, but the error messages may still require further refinement.

Participant positions:
- **Elijah Newren**: Authored the v3 series, expanding it to six patches to address edge cases and usability concerns. The core optimization remains unchanged, but the default behavior is now conservative ("false") for backward compatibility. **Key arguments**:
  - The tri-state config (`true`/`false`/`abort`) provides flexibility for users to balance performance and correctness.
  - The false-positive failure mode is non-destructive and recoverable with the new advice (patch 6).
  - The preparatory patches (e.g., patches 1 and 2) improve error handling and make the behavior more precise, enabling clearer user guidance.
  - The fifth patch argues for changing the default to `true` in the future, citing the assumption matrix and the rarity of the edge case where the server lacks the shallow graft.
  - The series does not adopt negotiation-based alternatives, as they fail in specific scenarios (e.g., rebased PRs, unrelated repos, disabled negotiation, protocol limitations) and introduce new failure modes (e.g., the repacking race).
  - Plans to address the foot-gun of locally amended grafts in a future series.
- **Patrick Steinhardt**: Provided substantive technical review in v2, raising questions about graft modification safety and suggesting minor implementation improvements (e.g., `const` parameters, loop efficiency). Proposed `push.negotiate=shallow` as an alternative but has not yet responded to v3. His preference for negotiation-based solutions remains, but the discussion with Elijah has been constructive.
- **Derrick Stolee**: Identified the false-positive failure mode in v2 and proposed repurposing `push.negotiate` for shallow repos as a simpler, safer alternative. **Key concerns**:
  - The false-positive failure mode (rejecting valid refs) is a usability issue, even if not a corruption risk.
  - The assumption that the server has the shallow graft may not hold in all cases (e.g., delayed GC, shared object databases).
  - Users need a clear recovery mechanism if the assumption is wrong.
  - **Has not yet responded** to v3 or the improved error messages and recovery advice. His silence leaves the usability concerns unresolved.

Related work: None mentioned in the thread.

---