This is a bug report about a potential interaction issue between sparse checkout and submodules. The author, Victor, reports that `git fetch` fails when `fetch.recurseSubmodules=on-demand` is set and an updated submodule is excluded by the sparse checkout configuration. They provide a self-contained reproduction case in a GitHub Gist but note they couldn’t find prior reports of this issue in the mailing list archives.

The problem appears to affect a specific workflow: users with sparse checkouts who also rely on recursive submodule fetching. The reproduction case will be key for reviewers to verify the issue and test any proposed fixes. The report is concise but provides enough detail to identify the affected subsystems (sparse-checkout, submodules, and fetch configuration) and the triggering condition (submodule updates excluded by sparse patterns).

No patch is included—this is purely a bug report, not a fix. The issue may attract attention from contributors familiar with sparse-checkout or submodule internals, particularly those who have worked on the sparse-index or recursive fetch logic. The absence of prior reports suggests this could be an edge case that hasn’t been widely encountered, but the provided reproduction case should make it straightforward to investigate.