This thread is a three-part patch series by Alexey Samsonov (via GitGitGadget) to replace Git's use of the deprecated `utime()` function with `utimensat()` for POSIX.1-2024 compatibility. The goal is to future-proof Git against libc implementations (like LLVM-libc) that may drop `utime()`, while also enabling nanosecond-precision timestamps. The series introduces a `git_utimensat()` wrapper with MinGW-specific support, replaces all `utime()` calls across the codebase, and removes legacy headers and shims.

The first two patches (already summarized) implemented the core changes, including the new wrapper and all call-site conversions. This third patch is a cleanup step, removing now-unused headers (`utime.h` from `compat/posix.h`, legacy shims in `compat/vcbuild/include/`) and the redundant `mingw_utime()` function. The series is under review in its initial version, with no prior iterations or objections raised so far. Key files touched include `commit-graph.c`, `copy.c`, and build system files, with no intended functional changes beyond timestamp precision improvements.

The cleanup patch is mechanical and uncontroversial, as it only removes dead code. Reviewers may verify no remaining `utime()` callers exist, but the series as a whole appears well-motivated and low-risk. No participant positions have been recorded yet, and the series remains in the early review phase.