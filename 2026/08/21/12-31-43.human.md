This is a two-patch feature series from Patrick Steinhardt that enables Git to fetch multiple packfile URIs in parallel during `git fetch` and `git clone`. The goal is to speed up fetches when the server advertises several packfiles, which the benchmarks in the second patch demonstrate.

The first patch lays the groundwork by refactoring `fetch-pack.c` to support threaded fetching, while the second patch implements the parallelization logic and adds a new configuration option (`fetch.parallelPackfileURIs`) to control the number of parallel fetches. The series touches the fetch-pack subsystem, updates documentation, and includes new tests in `t/t5702-protocol-v2.sh`.

This is a well-motivated performance improvement targeting a specific bottleneck—sequential fetching of packfile URIs—with clear benchmarks to justify the change. The patch series is concise, focused, and includes test coverage, which suggests it’s ready for review. Given Patrick’s track record with large-scale refactoring efforts (e.g., ODB abstraction, reftable), this is likely a carefully considered change that addresses a real-world need. If you care about fetch performance or protocol v2 optimizations, this is worth paying attention to.