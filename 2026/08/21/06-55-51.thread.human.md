This thread is about a patch from Elijah Newren that improves `git push` performance from shallow clones by avoiding redundant transfers of unchanged trees. The patch introduces a new config option, `push.shallowExcludeBoundary` (defaulting to `true`), which tells Git to treat shallow grafts as uninteresting tips during pack generation, reducing unnecessary data transfer.

Patrick Steinhardt has now posted a substantive review, engaging deeply with the technical design. He raises questions about edge cases—such as whether the patch safely handles locally modified shallow grafts (e.g., via `git commit --amend`)—and whether the current approach might send insufficient data in those cases. He also explores whether the negotiation could be extended to verify graft integrity via tree objects, though he acknowledges potential compatibility risks. On implementation, he suggests minor improvements like marking input parameters as `const` and combining loops for efficiency, while questioning whether higher-level commit-graph interfaces could simplify the reachability logic.

Patrick’s review also requests additional test coverage for graft modification scenarios and asks whether the new config should default to `true` outright rather than requiring opt-in. While no objections block the patch, his questions could lead to design refinements. The discussion is now focused on correctness and broader implications for shallow clone workflows. No other reviews have been posted yet.