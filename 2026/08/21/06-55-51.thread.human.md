Here’s the human-readable summary for a developer catching up on the thread:

This thread is about a patch from Elijah Newren to fix a performance problem in `git push` from shallow clones, where Git unnecessarily resends the entire toplevel tree (often gigabytes) even for tiny changes. The issue stems from how shallow grafts are handled during pack generation, and Elijah’s patch introduces a new config option, `push.shallowExcludeBoundary`, to treat shallow grafts as uninteresting tips and avoid redundant transfers. Patrick Steinhardt proposed an alternative: enabling `push.negotiate` by default (or conditionally for shallow clones) to address the problem at the negotiation layer instead.

The discussion has now sharpened around whether negotiation alone is sufficient. Elijah argues that `push.negotiate` doesn’t cover all cases—like rebased pull requests, unrelated repositories, or setups where negotiation fails (e.g., SSH without `GIT_PROTOCOL=version=2`). He also points out that users might disable negotiation entirely, leaving the original problem unsolved. While he’s open to making `push.negotiate` the default for large repos, he insists his patch is still needed for edge cases. Patrick hasn’t yet responded to this rebuttal, so the debate over defaults and whether a hybrid approach is possible remains open.

The series is still under review, with no blocking objections but no consensus yet on the best path forward. Elijah also plans to split his follow-up work on shallow graft handling into two separate series—one for `git commit --amend` foot-guns and another for broader fixes. For now, the key trade-off is between reducing latency (Patrick’s preference) and minimizing redundant data transfer (Elijah’s focus).