Here’s the human-mode summary for a developer catching up on the thread:

This thread is about a patch series from Elijah Newren aimed at fixing a performance issue in `git push` from shallow clones, where the entire toplevel tree is resent even for tiny changes. The series is now at v2, with minor code style fixes and a rebase onto Patrick Steinhardt’s ODB branch. The core debate revolves around two competing approaches: Elijah’s patch introduces a new config option, `push.shallowExcludeBoundary`, to treat shallow grafts as uninteresting tips during pack generation, while Patrick Steinhardt and now Derrick Stolee advocate for repurposing the existing `push.negotiate` mechanism instead.

The discussion took a critical turn with Stolee’s review, which identified a serious flaw in Elijah’s patch: it can over-exclude objects needed by other refs in a multi-ref push if one ref is rejected for shallow-update reasons, risking repository corruption. Stolee provided a test case demonstrating the issue and proposed a simpler fix using `push.negotiate` for shallow repos, which passes most of Elijah’s tests. The remaining test (for seeding new shallow roots) could be adjusted to fit the new behavior. Elijah’s rebuttal to Patrick’s earlier negotiation-based proposal—citing edge cases like rebased PRs, unrelated repos, and protocol limitations—remains relevant, but Stolee’s review shifts the focus to correctness over those trade-offs.

Key participants are Elijah (author), Patrick (reviewer and alternative approach advocate), and now Stolee (who raised the correctness concern). The thread is still under review, with the main open question being whether to refine Elijah’s patch to address the over-exclusion issue or pivot to Stolee’s negotiation-based solution. The current patch touches `send-pack.c` and adds a new config option, but its safety is now in question. No decisions have been made yet, and the series is not merged.