This is a **maintainer response** in the `git pull --hard`/`--reset` thread. Junio pushes back on Artur Bieniek’s claim that `git pull --ff-only` cannot handle force-pushed or rebased upstream branches. He argues that such events *should* be treated as exceptional and require explicit user intervention, not automated via a convenience option. The reply is brief and conceptual, focusing on design philosophy rather than technical implementation: the maintainer’s position remains that `git pull`’s primary role is history integration, not working-copy management, and that users should discard local state with separate, deliberate commands. No engagement yet with Phillip Wood’s `--reset` redesign or Kristoffer Haugsbakk’s alternative workflow suggestion. The thread’s viability hinges on whether Junio will accept the force-push/rebase use case as a legitimate gap or continue to view it as an anti-pattern.