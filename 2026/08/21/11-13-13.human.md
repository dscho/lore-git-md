This is a user question about the purpose of the `--ext-diff` option in Git.

Anselm Schüler asks why `--ext-diff` exists and why custom diff filters aren’t always executed by default. They speculate it might be a security feature but note that Git still executes diff filters when `--ext-diff` is used, so that doesn’t seem to be the case. They’re also unclear on the intended use case for diff filters—why would someone want them to run in `git diff` but not in commands like `git show`?

The question is straightforward and reflects a common point of confusion for users who encounter `--ext-diff` without prior context. It doesn’t propose a patch or suggest a change, but it could spark discussion about whether the current behavior is intuitive or if the option’s purpose needs better documentation. For a busy reader, this is a low-stakes but potentially clarifying thread to follow if they’ve ever wondered about the same thing.