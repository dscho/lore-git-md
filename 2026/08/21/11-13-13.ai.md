This is a user question about the purpose of the `--ext-diff` option in Git.

Anselm Schüler asks why `--ext-diff` exists and why custom diff filters aren’t always executed by default. They speculate whether it might be a security feature but note that Git already executes diff filters when they are configured, making that explanation unlikely. The core question is: *when would a user want a diff filter to run in some Git commands (like `git diff`) but not others (like `git show`)?*

The email does not propose a patch or technical change—it is purely a request for clarification about Git’s design intent. No prior thread context is provided, so this appears to be a new inquiry rather than a follow-up. The question is well-posed and reflects a common point of confusion about Git’s diff externalization mechanism. A clear answer would help users understand whether `--ext-diff` is a legacy quirk, a deliberate safety valve, or something else entirely.