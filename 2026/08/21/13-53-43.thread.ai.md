Thread identity
Subject: [PATCH 0/2] repo structure: use Unicode box-drawing characters in UTF-8 locale
Series version: v1 (initial submission)
Originating author: K Jayatheerth (GSoC participant)
Thread type: patch series (feature addition)

Core problem or goal
Add Unicode box-drawing characters (`│`, `├`, `─`, `┼`, `┤`) and bullets (`•`) to the output of `git repo structure` when the user’s locale is UTF-8, while preserving ASCII fallback for non-UTF-8 environments. The series also fixes a latent bug in `is_utf8_locale()` that incorrectly reported UTF-8 when `git_setup_gettext()` returned early (e.g., in uninstalled builds) and `charset` remained unset.

Current status
Under review (no maintainer decision yet). Two patches:
1. Fix `is_utf8_locale()` to use environment-derived fallbacks (`LC_ALL`, `LC_CTYPE`, `LANG`) when `charset` is unset.
2. Implement Unicode rendering in `stats_table_setup_structure()` and `stats_table_print_structure()` in `builtin/repo.c`, guarded by `is_utf8_locale()`.

Key technical details
- Files touched: `gettext.c`, `builtin/repo.c`, `t/t1901-repo-structure.sh`
- New symbols: none (internal-only changes to `is_utf8_locale()` and the `stats_table_*` functions)
- New test helper: `GIT_TEST_UTF8_LOCALE` (borrowed from `t/lib-git-svn.sh`) to define a `UTF8_LOCALE` test prerequisite
- Unicode characters introduced: `│`, `├`, `─`, `┼`, `┤`, `•`
- On-disk format: unchanged
- Test coverage: existing tests pinned to `LC_ALL=C` for ASCII fallback; new tests verify UTF-8 output

Open questions and loose ends
None raised yet; the series is self-contained and no objections or requests for changes have appeared.

Participant positions
- Author (K Jayatheerth): submitted the series with detailed commit messages and test coverage.
- No reviews recorded yet.

Related work
- `GIT_TEST_UTF8_LOCALE` test helper was previously used in `t/lib-git-svn.sh`; this series adopts the same convention.