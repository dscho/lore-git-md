Thread identity
Subject: [PATCH 0/2] repo structure: use Unicode box-drawing characters in UTF-8 locale
Series version: v1 (initial submission)
Originating author: K Jayatheerth (GSoC participant)
Thread type: patch series (feature addition)

Core problem or goal
Add Unicode box-drawing characters (`│`, `├`, `─`, `┼`, `┤`) and bullets (`•`) to the output of `git repo structure` when the user’s locale is UTF-8, while preserving ASCII fallback for non-UTF-8 environments. The series also fixes a latent bug in `is_utf8_locale()` that incorrectly reported UTF-8 when `git_setup_gettext()` returned early (e.g., in uninstalled builds) and `charset` remained unset.

Current status
Under review (no maintainer decision yet). Two patches:
1. Fix `is_utf8_locale()` in `gettext.c` to use environment-derived fallbacks (`LC_ALL`, `LC_CTYPE`, `LANG`) when `charset` is unset, ensuring robust locale detection even in uninstalled builds or when gettext support is disabled. The fix caches the fallback charset in a static buffer to avoid repeated environment lookups.
2. Implement Unicode rendering in `stats_table_setup_structure()` and `stats_table_print_structure()` in `builtin/repo.c`, guarded by `is_utf8_locale()`. The patch updates `t/t1901-repo-structure.sh` to verify Unicode output under a UTF-8 locale using the `GIT_TEST_UTF8_LOCALE` test helper (borrowed from `t/lib-git-svn.sh`), while pinning existing tests to `LC_ALL=C` to guarantee ASCII fallback. No new symbols are introduced; the changes are internal to these functions.

Key technical details
- Files touched: `gettext.c`, `builtin/repo.c`, `t/t1901-repo-structure.sh`
- New symbols: none (internal-only changes)
- New test helper: `GIT_TEST_UTF8_LOCALE` (adopted from `t/lib-git-svn.sh`) to define a `UTF8_LOCALE` test prerequisite
- Unicode characters introduced: `│`, `├`, `─`, `┼`, `┤`, `•`
- On-disk format: unchanged
- Test coverage: existing tests pinned to `LC_ALL=C` for ASCII fallback; new tests verify UTF-8 output when `GIT_TEST_UTF8_LOCALE` is set, including edge cases like missing locale directories

Open questions and loose ends
- **Maintainer requirement**: Junio C Hamano explicitly requires the feature to be opt-out via a configuration option (e.g., `repo.structure.unicode`) to address cross-terminal reliability concerns. The current implementation’s locale-only detection forces users to abandon UTF-8 entirely to disable box-drawing characters, which is unacceptable. This requirement replaces the earlier objection about rendering inconsistency and provides a clear path forward: the series must add a configuration knob to override the locale-based default.
- **Opt-out proposal**: Oswald Buddenhagen’s suggestion to make the feature opt-out via a configuration option is now the focal point of the discussion, having been endorsed by the maintainer. The author is expected to revise the series to include this option.
- **Terminal compatibility nuance**: Tuomas Ahola notes that **mutt**, cited by Buddenhagen as a successful user of box-drawing characters, actually falls back to **ACS (Alternative Character Set)** when UTF-8 is unavailable. This complicates the argument that mutt’s widespread use demonstrates universal UTF-8 support for box-drawing characters and may inform whether Git should adopt a similar fallback strategy. The observation is narrowly scoped but sharp, adding technical depth to the compatibility debate.
- **Feature scope expansion**: Buddenhagen’s earlier suggestion to extend Unicode rendering to `git log --graph` remains speculative and unrelated to the current series. No concrete implementation or maintainer feedback has been provided on this idea, and it should not influence the current patch series.

Participant positions
- **Author (K Jayatheerth)**: Submitted the series with detailed commit messages and test coverage as part of a GSoC effort mentored by Justin Tobler and Lucas Seiki Oshiro. The implementation is narrowly scoped, maintains backward compatibility, and includes thorough test coverage for both normal and edge cases. The author has not yet responded to the maintainer’s requirement for an opt-out configuration option.
- **Maintainer (Junio C Hamano)**: Initially raised a high-weight objection regarding inconsistent rendering of Unicode box-drawing characters across terminals but has since clarified that the objection is not about the feature itself but about the lack of user control. Junio now explicitly requires the feature to be opt-out via a configuration option (e.g., `repo.structure.unicode`) to override the locale-based default. This position is now the primary unresolved issue for the series.
- **Oswald Buddenhagen**: Proposed the opt-out configuration option as a compromise to address Junio’s reliability concerns. His suggestion has been endorsed by the maintainer and now defines the path forward for the series. Buddenhagen also floated the idea of extending Unicode rendering to `git log --graph`, but this remains speculative and unrelated to the current patch series.
- **Tuomas Ahola**: Provided a medium-weight technical counterpoint to Buddenhagen’s terminal-compatibility argument, noting that mutt’s resilience stems from its ACS fallback rather than universal UTF-8 support. The observation adds nuance to the discussion but does not challenge the maintainer’s requirement for an opt-out configuration.