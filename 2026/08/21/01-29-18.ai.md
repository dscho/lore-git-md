This is Junio’s "What’s cooking" report for August 2026 (#09), summarizing the current state of integration branches and in-flight topics. The report is the primary mechanism for tracking what is queued, what is cooking, and what is stalled in the Git project’s integration pipeline.

The report lists 472 non-merge commits in `master` since Git 2.55, 128 in `next`, and 245 in `seen`. It is organized into four sections: New Topics, Graduated to `master`, Stalled, and Cooking. Each topic is annotated with its current integration branch, a one-line summary, and any relevant discussion links or reroll expectations.

Key highlights for a downstream AI summarizer:

- **New Topics** (8):
  - `kn/receive-report-hook`: Introduces a new `report` hook for `git receive-pack` to filter/modify the packet-line status report sent back to the client. Marked as "Expecting a reroll".
  - `en/midx-missing-pack-fallback`: Teaches the object lookup machinery to recover when a multi-pack-index points to a removed pack during concurrent geometric repack. "Waiting for response".
  - `ll/zsh-complete-git-potty-options`: Updates zsh completion to correctly locate the Git command after global options like `-C`. "Needs review".
  - `ps/odb-generic-corrupt-objects`: Refactors the ODB API to distinguish between missing and corrupt objects, propagating error details via a generic strbuf mechanism. "Waiting for review".
  - `ap/http-preserve-wwwauth-redirect`: Preserves WWW-Authenticate headers across HTTP redirects. "Needs review".
  - `fr/pack-objects-trace-pack-bytes`: Adds trace2 output for total bytes written to pack files. "Needs review".
  - `kh/doc-datamodel`: Links the `gitdatamodel(7)` tutorial from key documentation pages. "Needs review".
  - `yn/worktree-repair-relative`: Fixes `git worktree repair` to correctly detect relative paths in `.git` files. "Needs review".

- **Graduated to `master`** (6 topics, 34 commits):
  - `hn/bisect-reset-when-found`: Adds `--reset-when-found` to `git bisect` to automatically jump back after finding the culprit.
  - `hn/branch-delete-merged`: Adds `--delete-merged` to `git branch` to remove local branches merged into their upstream.
  - `kh/doc-refs-migrate-limitations`: Moves ref migration warnings under the `migrate` subcommand.
  - `kh/doc-trailers`: Overhauls `git interpret-trailers` documentation.
  - `ps/odb-make-creation-pluggable`: Makes ODB on-disk structure creation pluggable.
  - `ps/writev`: Reintroduces `writev(3p)` wrappers for reduced syscall overhead.

- **Stalled** (1 topic):
  - `hs/rebase-continue-edit`: Adds `--[no-]edit` to `git rebase --continue`. "Waiting for response for too long".

- **Cooking** (30+ topics):
  - `sk/object-name-use-after-free`: Fixes a heap-use-after-free in object name parsing. "Will merge to `master`".
  - `kh/format-rev-doc-synopsis`: Updates `git format-rev` documentation to use `[synopsis]` blocks. "Will merge to `master`".
  - `kh/format-rev-more-options`: Teaches `git format-rev` `--abbrev`, `--color`, and `--date`. "Needs review".
  - `ps/odb-pluggable-pack-generation`: Makes packfile generation pluggable via ODB callbacks. "Expecting a reroll".
  - `ty/repository-fetch-if-missing`: Moves `fetch_if_missing` into `struct repository`. "Will merge to `next`?".
  - `ns/ref-symref-additional-tests`: Adds tests for `git symbolic-ref` exit codes. "Expecting a reroll".
  - `jt/receive-pack-pluggable-writes`: Updates `git receive-pack` to use ODB transactions for writing packfiles. "Expecting a reroll".
  - `ps/odb-eagerly-load-alternates`: Eagerly loads alternate ODBs on initialization. "Waiting for response".
  - `gg/http-ssl-verify-status`: Adds `http.sslVerifyStatus` to check stapled OCSP responses. "Needs review".
  - `yn/worktree-add-no-dwim-with-b`: Prevents DWIM when `-b` or `-B` is given to `git worktree add`. "Will merge to `next`?".
  - `yn/worktree-ambiguous-remote-advice`: Improves advice for ambiguous remote branch names. "Needs review".
  - `dk/use-nsec-runtime`: Converts `USE_NSEC` to runtime `core.useNanosec`. "Needs review".
  - `kh/trailers-no-urls`: Stops recognizing URLs as trailers. "Needs review".
  - `hn/history-squash`: Adds `git history squash` to fold a range of commits. "Waiting for response".

The report also includes stalled topics, topics cooking in `next`, and topics that graduated to `master` in this cycle. The integration branch state (`next`, `seen`, `master`) is explicitly noted for each topic, along with any reroll expectations or review status.