This is a **bugfix follow-up** in the ongoing thread about `git config --global`’s discrepancy between documented and actual behavior. Nils Fahldieck enthusiastically endorses Delilah Ashley Wu’s nearly complete v2 patch series, which would make `--global` read both `~/.gitconfig` *and* the XDG config file (`~/.config/git/config`), aligning the code with the documentation. The email is a brief but supportive reply, offering to review the v2 series once it is posted and expressing strong personal interest in seeing the change merged.

The context is clear: Delilah’s series (previewed in GitHub PR #2196) is the most concrete step toward resolving the long-standing issue, and Nils’s offer to provide feedback signals community engagement that could help push the series toward acceptance. The maintainer (Junio C Hamano) previously acknowledged the documentation was incorrect but did not endorse a fix direction; this series, combined with renewed discussion of cross-command inconsistencies (e.g., `--global --no-includes`), may finally tip the balance in favor of the code change.

For a future AI summarizer, note the key technical details: the patch targets `config.c`, specifically `git_global_config()` and `do_git_config_sequence()`, and preserves backward compatibility while fixing edge cases like `--global --no-includes`. The v2 series is nearly ready, and Nils’s offer to review suggests the thread is moving toward active collaboration rather than debate. The only remaining uncertainty is whether Junio will accept the behavioral change or continue to favor updating the documentation instead.