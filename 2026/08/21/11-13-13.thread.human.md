This thread is a user inquiry from Anselm Schüler asking about the design purpose of Git’s `--ext-diff` option. Specifically, they want to know why custom diff filters aren’t always executed by default and when a user might want a diff filter to run in some Git commands (like `git diff`) but not others (like `git show`). Anselm notes that Git already executes diff filters when configured, so security doesn’t seem like a plausible explanation, and they’re seeking clarity on the intended use case.

The question is well-framed and highlights a common point of confusion about Git’s diff externalization mechanism. So far, no responses or technical explanations have been provided—this is the first email in the thread. A clear answer would help users understand whether `--ext-diff` is a legacy quirk, a deliberate safety valve, or something else entirely.