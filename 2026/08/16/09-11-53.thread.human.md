This thread introduces documentation for Git’s experimental pseudo-merge bitmap configuration options, added via a new man-page entry for `bitmapPseudoMerge.<name>.*` settings. The patch, authored by kapicharly@icloud.com, explains how these options let users group commits into pseudo-merge bitmaps—an optimization for reachability queries in large repositories—using regex patterns, decay rates, and age-based thresholds. The documentation distinguishes between "unstable" and "stable" bitmaps, with the latter targeting older commits and requiring a one-time generation cost. While the patch is purely technical and includes no code changes, it notes that the feature is experimental and may evolve or be removed.

So far, the thread consists only of the initial patch submission. No reviews or discussions have taken place yet, so there are no decisions, objections, or changes to report. The documentation’s usefulness hinges on the status of the pseudo-merge bitmap feature itself; if the underlying implementation is still under development, this man-page entry may need future updates. For now, it provides clear guidance for users experimenting with the configuration.