This is a short, enthusiastic user request for a new feature: a built-in `git squash` command.

The email is a reply in an existing thread (started by mdujava@kocurkovo.cz) but provides no technical details, design sketch, or code. It simply endorses the idea of adding a squash operation as a first-class Git command, citing frustration with the current `git rebase` workflow. The tone is informal and supportive rather than analytical.

There is no patch, no discussion of edge cases, and no engagement with prior art (e.g., how `git merge --squash` or `git commit --amend` already cover parts of the use case). The message is best read as a +1 vote rather than a technical contribution.