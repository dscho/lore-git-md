This is a documentation patch introducing new experimental configuration options for Git’s pseudo-merge bitmap feature. It adds detailed man-page entries for `bitmapPseudoMerge.<name>.*` settings, which control how Git groups commits into pseudo-merge bitmaps—an optimization for reachability queries in large repositories.

The patch explains each option’s purpose and behavior, including how patterns (`bitmapPseudoMerge.<name>.pattern`) use regular expressions to group references (e.g., tags or remote branches), how decay rates (`bitmapPseudoMerge.<name>.decay`) adjust group sizes over time, and how thresholds (`bitmapPseudoMerge.<name>.threshold`, `bitmapPseudoMerge.<name>.stableThreshold`) determine commit eligibility based on age. It also clarifies the distinction between "unstable" and "stable" pseudo-merge bitmaps, with the latter targeting older commits and imposing a one-time generation cost.

The commit message explicitly notes that these options are **experimental** and may change or be removed, linking to `gitpacking(7)` for broader context. The documentation is technical but thorough, with examples (e.g., grouping tags by remote) to illustrate capture-group behavior. No code changes are included—this is purely a documentation update for a feature likely introduced in an earlier patch series.

The patch is well-structured for its purpose, though its relevance depends on the status of the pseudo-merge bitmap feature itself. If that feature is still in flux, this documentation may need future revisions. For now, it provides clear guidance for users experimenting with the configuration.