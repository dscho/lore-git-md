This is a feature proposal for a new `git squash` command.

The email suggests adding a built-in `git squash <n>` command that would combine the last `<n>` commits into a single new commit, equivalent to the current two-step process of `git reset --soft HEAD~<n>` followed by `git commit`. The author also proposes an optional `-s` flag to automatically add a Signed-off-by trailer to the resulting commit, citing projects that require a Developer Certificate of Origin (DCO).

The proposal is motivated by convenience and time-saving for developers who frequently squash commits. The author asks whether this idea has been suggested before but does not cite prior discussion or provide a patch implementation. There is no mention of edge cases (e.g., merge commits, conflicts, or partial staging), backward compatibility concerns, or how this would interact with existing Git workflows like interactive rebase (`git rebase -i`). The email is brief and does not engage with the broader Git command-line design principles or existing squash-like functionality (e.g., `git merge --squash` or `git commit --amend`).