Thread identity: Documentation patch introducing experimental configuration options for Git’s pseudo-merge bitmap feature (`bitmapPseudoMerge.<name>.*`). Series version: standalone patch (no version marker in subject). Originating author: kapicharly@icloud.com. Thread type: documentation update for an experimental feature.

Core problem or goal: Document the newly introduced `bitmapPseudoMerge.<name>.*` configuration options, which control how Git groups commits into pseudo-merge bitmaps for reachability query optimization in large repositories. The documentation aims to provide clear, technical guidance for users experimenting with these settings, including their purpose, behavior, and examples.

Current status: Under review. The patch is a standalone documentation update with no prior versions recorded in this thread. No merge status is indicated (not in `pu`, `next`, or `master`).

Key technical details:
- **Files touched**: Documentation (man pages only; no code changes).
- **New symbols/config keys**: `bitmapPseudoMerge.<name>.pattern`, `bitmapPseudoMerge.<name>.decay`, `bitmapPseudoMerge.<name>.threshold`, `bitmapPseudoMerge.<name>.stableThreshold`.
- **Behavior**: Options define regex patterns for grouping references (e.g., tags, remote branches), decay rates for group size adjustment, and age-based thresholds for commit eligibility. Distinction between "unstable" and "stable" pseudo-merge bitmaps is clarified, with the latter targeting older commits and imposing a one-time generation cost.
- **Examples**: Provided for grouping tags by remote using capture-group behavior.
- **Experimental status**: Explicitly noted in the commit message; options may change or be removed. Linked to `gitpacking(7)` for broader context.

Open questions and loose ends:
- The documentation’s relevance depends on the status of the pseudo-merge bitmap feature itself. If the feature is still in flux, this documentation may require future revisions.
- No review feedback or objections have been recorded yet.

Participant positions: None recorded (initial patch).

Related work: The pseudo-merge bitmap feature was likely introduced in an earlier patch series (not part of this thread). The documentation references `gitpacking(7)` for broader context.