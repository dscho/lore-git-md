This is a **documentation clarification reply** in the `report` hook feature thread. Karthik Nayak acknowledges Patrick Steinhardt’s follow-up question about the pkt-line format: whether each `ng <refname> <reason>` line must end with a newline or if the entire report is newline-delimited and terminated by a single flush packet. Karthik confirms the ambiguity and agrees to clarify the documentation in the next revision.

This exchange resolves the last open documentation nit before the patch can proceed. The thread’s primary blocker remains the exit status semantics debate (whether a non-zero hook exit should rewrite all ref status lines to `"report hook failed"` or cause `receive-pack` to die), but this reply removes a prerequisite ambiguity for finalizing the hook’s pkt-line format specification. The clarification will likely be a one-line addition to the `githooks.adoc` documentation, explicitly stating that each reference line must end with a newline.