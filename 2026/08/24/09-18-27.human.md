This is a **substantive review reply** in the ongoing `git history squash` series, specifically addressing the case-sensitivity issue in autosquash marker resolution that Junio C Hamano flagged as a blocker. Phillip Wood weighs in on whether the patch should accept uppercase or lowercase OIDs in `fixup!`/`squash!`/`amend!` markers, or enforce strict case sensitivity.

Phillip acknowledges Junio’s concern that `istarts_with()`—which allows case-insensitive prefix matching—is overly permissive, but argues that accepting *all-uppercase* or *all-lowercase* OIDs (but not mixed case) would be reasonable. He frames this as a compatibility question: the goal is to match what `git rebase --autosquash` already accepts, while still rejecting ref names like `HEAD^` or `main`. His comment suggests a middle ground—case-insensitive matching for *uniform-case* OIDs—rather than the strict case sensitivity Junio implied.

This reply keeps the discussion open on the exact behavior, but doesn’t resolve the blocker. The thread’s prior context (Junio’s "Will replace" in v7 and the series being otherwise complete) means this is the last technical hurdle before integration. The outcome will likely hinge on whether the project prioritizes strict OID matching (Junio’s position) or compatibility with `rebase --autosquash` (Phillip’s suggestion). Harald Nordgren will need to reroll the patch to address this, either by adopting strict case sensitivity or justifying a more permissive approach.