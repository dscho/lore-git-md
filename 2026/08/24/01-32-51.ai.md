This is a maintainer clarification in the thread about Delilah Ashley Wu’s three-patch series fixing the `--global` config listing inconsistency. Junio C Hamano explains the historical design intent behind Git’s global config handling, confirming that the series’ core fix (making `--global` read both `$HOME/.gitconfig` and `$XDG_CONFIG_HOME/git/config`) aligns with the original migration-friendly design for the read path. He notes that the write path (`--global --edit`/`--global set`) was deliberately implemented to write to only one file (prioritizing `$HOME/.gitconfig` unless the user has explicitly migrated), and that this behavior should remain unchanged—clarifying Chris Torek’s exploratory question without requiring follow-up work.

The email also explicitly states that the discrepancy the series addresses (where `--global` only read `$HOME/.gitconfig`) was unintentional, reinforcing the series’ motivation. No new technical concerns are raised, and Junio’s framing suggests the series is on solid ground for integration. Key files/context: `config.c`, `builtin/config.c`, and the distinction between read/write paths for global config. The thread is now effectively complete, pending final maintainer review.