This is a maintainer's reply in the `git symbolic-ref` test coverage thread. Junio C Hamano weighs in on the exit code design question, agreeing with Patrick Steinhardt that the differing exit codes (128 for the non-quiet path, 1 for the quiet path) likely reflect an oversight rather than an intentional design choice. He endorses both of Patrick's proposed alternatives: either avoid testing the exact exit codes (to prevent overspecifying behavior that may not matter) or align the exit codes by changing the quiet path to use `die(NULL)` instead of `exit(1)`. Junio's preference leans toward the former—continuing to rely on existing implicit coverage rather than codifying the current behavior—but he describes both options as "very sensible."

The email is brief and decisive, signaling that the project is unlikely to preserve the current exit code discrepancy as intentional. This shifts the thread's focus toward either dropping the exit code tests or preparing a follow-up patch to standardize the behavior. No new technical details or objections are introduced; the discussion now hinges on whether to act on the oversight or leave it untested.