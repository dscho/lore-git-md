This is Junio’s "What’s cooking" report for August 2026 (#10), summarizing the current state of integration branches and in-flight topics. The report covers 47 topics across `seen`, `next`, and `master`, with a focus on the ODB (object database) abstraction, performance improvements, and various cleanups.

Key themes in this report include:

1. **ODB abstraction and pluggable backends** – Several topics advance the ODB abstraction effort, allowing alternative backends to generate packfiles and handle object storage. Notable patches:
   - `ps/odb-pluggable-pack-generation` (6 commits) makes packfile generation pluggable via ODB callbacks, removing hardcoded references to `pack-objects`.
   - `ps/odb-eagerly-load-alternates` (6 commits) simplifies the ODB layer by eagerly loading alternate object directories, eliminating scattered lazy-loading calls.
   - `ps/odb-generic-corrupt-objects` (5 commits) refactors the ODB API to distinguish between missing and corrupt objects, improving error reporting.

2. **Performance optimizations** – Multiple topics target fetch and repack performance:
   - `ps/fetch-packfile-uris-parallel` (2 commits) allows parallelizing packfile URI fetches, speeding up clones/fetches from servers advertising multiple packfiles.
   - `ps/odb-geometric-repack-loose-threshold` (1 commit) adjusts geometric repacking thresholds to avoid over-aggressive repacking during concurrent writes.
   - `fr/pack-objects-trace-pack-bytes` (1 commit) adds trace2 output for pack bytes written, enabling performance analysis of compression settings.

3. **Cleanups and refactoring** – Many topics focus on code hygiene and API improvements:
   - `as/utimensat-utimes` (3 commits) replaces the legacy `utime()` with `utimensat()` for high-precision timestamps.
   - `ty/repository-fetch-if-missing` (1 commit) moves the `fetch_if_missing` global into `struct repository`, continuing the libification effort.
   - `js/pack-objects-delta-size-t` (13 commits) widens delta-related fields to `size_t` to avoid truncation on 64-bit Windows.

4. **New features and usability improvements** –
   - `kh/format-rev-more-options` (5 commits) extends the experimental `git format-rev` with `--abbrev`, `--color`, and `--date` options.
   - `yn/worktree-add-no-dwim-with-b` (1 commit) fixes DWIM behavior in `git worktree add` when `-b` or `-B` is explicitly given.
   - `gg/http-ssl-verify-status` (1 commit) adds `http.sslVerifyStatus` to check stapled OCSP responses during TLS handshakes.

5. **Stalled or contentious topics** –
   - `hs/rebase-continue-edit` (1 commit) adds `--[no-]edit` to `git rebase --continue` but has been stalled for over a month.
   - `cc/lazy-fetch-trusted-bit` (5 commits) introduces `uploadpack.lazyFetchTrusted` but has seen pushback over its design.
   - `tb/pack-with-duplicates` (5 commits) hardens packfile handling for duplicate objects but has been stalled since July.

6. **Notable merges to `next`** –
   - `sk/object-name-use-after-free` (1 commit) fixes a heap-use-after-free in object name parsing.
   - `ss/repack-drop-filtered` (6 commits) adds `--drop-filtered` to `git repack` to reclaim space from filtered promisor blobs.
   - `js/coverity-unchecked-returns-fix` (12 commits) addresses Coverity warnings about unchecked return values.

The report also highlights several documentation improvements, including `kh/doc-datamodel` (4 commits), which links the new `gitdatamodel(7)` tutorial from key documentation pages. Test and CI updates are scattered throughout, such as `ps/t7900-deflake-maintenance` (2 commits), which fixes flaky maintenance tests.

For a future AI summarizer, key technical details to retain include:
- The ODB abstraction topics (`ps/odb-*`) and their implications for pluggable backends.
- The `fetch.packfileURIThreads` config and parallel packfile URI fetching.
- The `core.useNanosec` runtime config replacing `USE_NSEC`.
- The `git format-rev` and `git history` experimental commands and their options.
- The `uploadpack.lazyFetchTrusted` controversy and stalled status.
- The `git worktree add` DWIM fixes and `--drop-filtered` repack option.