This is a documentation patch, the first in Karthik Nayak’s three-part series introducing the new `report` hook for `git receive-pack`. The series as a whole is ready for integration, and this patch is a straightforward preparatory step.

The patch adds a short `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc`, documenting the existing `proc-receive` hook that was previously undocumented in this file. The new section explains that the hook is invoked during push processing for refs matching the `receive.procReceiveRefs` patterns and links to `githooks(5)` for the full protocol description. The change is minimal—just six lines—and aligns the documentation with the hook’s established behavior.

This patch is uncontroversial and well-motivated: the `proc-receive` hook has been part of Git for years, and documenting it alongside the other `receive-pack` hooks (`pre-receive`, `post-receive`, `post-update`) is a clear improvement. The commit message is concise and factual, and the patch itself is clean and focused. No objections or feedback were raised in the thread for this specific patch, and it is effectively ready to merge. Expect it to land in `next` alongside the rest of the series.