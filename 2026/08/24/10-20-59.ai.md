This is the first patch in the v3 series introducing the `report` hook for `git-receive-pack`. It is a **documentation-only patch** that adds a missing section about the existing `proc-receive` hook to `git-receive-pack.adoc`.

The patch inserts a short `PROC-RECEIVE HOOK` section under the existing hook documentation, explaining that the hook is invoked during push processing to handle refs matching the `receive.procReceiveRefs` patterns and linking to `githooks(5)` for full protocol details. The change is minimal (six lines added) and purely additive, addressing a gap in the current documentation where `proc-receive` was not mentioned alongside other hooks like `pre-receive` and `post-receive`.

This patch is preparatory work for the series' main goal (the new `report` hook) and does not introduce new functionality. It was split out from the core implementation to keep the series focused and to address prior feedback about documentation completeness. The commit message is concise and factual, and the patch itself is uncontroversial. No objections or further changes are expected.