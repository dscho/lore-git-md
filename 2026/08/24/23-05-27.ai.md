This is a test-coverage patch targeting the `git symbolic-ref` plumbing command. Nikolaus Schuetz responds to Junio C Hamano’s suggestion to avoid testing the exact exit codes by proposing to update the command’s documentation instead. The patch itself remains unchanged from v2, but the author now offers to revise the man page (`git-symbolic-ref.adoc`) to accurately describe the current behavior—exit code 128 without `--quiet`, exit code 1 with `--quiet`, and empty stdout in both cases—rather than attempting to align the exit codes or drop the tests.

The thread has reached a practical compromise: the tests will stay (documenting the status quo), and the documentation will be updated to reflect reality. This sidesteps the design debate about whether the exit code discrepancy is intentional (Phillip Wood’s argument) or an oversight (Junio and Patrick Steinhardt’s view). The change is low-risk, touches only documentation and tests, and does not alter any on-disk formats or user-visible behavior. If accepted, this will close the coverage gap without disturbing scripts that might rely on the current exit codes. The next step is likely a v3 with the updated man page.