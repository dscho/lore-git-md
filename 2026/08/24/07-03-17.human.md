This is a **substantive follow-up** in the ongoing discussion about Elijah Newren’s patch 2/2, which fixes a race condition in Git’s packed-object backend where a multi-pack-index (MIDX) references a removed packfile. Jeff King (Peff) responds to Patrick Steinhardt’s clarification, refining the architectural debate about how to handle the fallback logic when the preferred pack is missing.

Peff acknowledges Patrick’s explanation that the bug affects *all* object lookups, not just those using `OBJECT_INFO_QUICK`, because the non-MIDX lookup skips *all* packs covered by the MIDX. However, he pushes back on Patrick’s proposed fix—scanning only the packs covered by the MIDX when the preferred pack is missing—arguing that this could still be inefficient in repositories with many packs. Instead, Peff suggests that the fallback should prioritize searching the *newest* packs (likely to contain the object after a repack) rather than all MIDX’d packs. This would avoid the performance penalty of scanning a large number of packs while still addressing the correctness issue.

The email narrows the discussion to a practical trade-off: correctness versus performance. Peff’s concern is that scanning all MIDX’d packs (even when gated by the `bsearch()` check) could be costly in repositories with many packs, while Patrick’s approach ensures correctness but may introduce unnecessary overhead. The thread remains focused on refining the implementation of the fallback logic in `find_pack_entry()`, with no fundamental disagreement about the need for a fix. The series is still a high-priority candidate for merging, but the outcome of this discussion may require adjustments to patch 2/2. Key technical details include the `find_pack_entry()` function in `odb/source-packed.c` and the interaction between MIDX lookups and the regular pack fallback. No new files or symbols are introduced, but the recovery logic’s efficiency is now the central point of debate.