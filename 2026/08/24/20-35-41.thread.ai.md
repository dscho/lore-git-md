Thread identity: `[PATCH 0/3] format-patch: add --[no-]range-diff-notes` series introducing options to control notes inclusion in range-diff output. Originating author: Kristoffer Haugsbakk. Thread type: patch series (feature addition).

Core problem or goal: Add `--[no-]range-diff-notes` options to `git format-patch` to allow independent control over whether Git notes are included in the range-diff section versus the patches themselves. The feature addresses workflow needs where users want to suppress iteration notes (e.g., "v3: [description]") in range-diffs while retaining them in the patch output.

Current status: Under review (v1 posted). Junio C Hamano has raised a substantive usability concern about the option semantics in the third patch. The series consists of three patches:
1. A preparatory refactoring simplifying `get_notes_args` by removing a redundant parameter and using `rev->rdiff_notes_arg` directly.
2. A rename of the `rdiff_log_arg` struct member to `rdiff_notes_arg` in `revision.h` to reflect its narrowed role after the feature addition.
3. The core implementation of `--range-diff-notes` and `--no-range-diff-notes`, introducing a new `rdiff_override_notes` flag in `struct rev_info` and modifying `get_notes_args()` to respect this override. Junio's review questions whether the current fallback behavior (where `--range-diff-notes` inherits the patch notes setting) aligns with user expectations, as `--no-notes --range-diff-notes` would suppress notes in both contexts rather than enabling them only for the range-diff.

All tests pass in CI, though the author previously noted uncertainty about the CI setup. No other review feedback has been received yet.

Key technical details:
- Files touched: `builtin/log.c`, `log-tree.c`, `revision.c`, `revision.h`, `Documentation/git-format-patch.txt`, and tests in `t/t3206-range-diff.sh`.
- New CLI options: `--range-diff-notes[=<ref>]` and `--no-range-diff-notes`.
- New struct members: `rdiff_override_notes` flag in `struct rev_info` (patch 3); renamed `rdiff_log_arg` -> `rdiff_notes_arg` (patch 2).
- Refactoring: `get_notes_args` now uses `rev->rdiff_notes_arg` directly instead of an explicit `struct strvec *arg` parameter (patch 1).
- Behavior: The options control notes inclusion in the range-diff section independently of the main patch notes, but the current implementation falls back to the patch notes setting when the override is not explicitly set. Edge cases handled include toggling the override off after it has been set and supporting specific notes refs for range-diffs (e.g., `--range-diff-notes=testing`).
- Test coverage: Comprehensive tests added to `t/t3206-range-diff.sh`, verifying scenarios like suppressing all range-diff notes, using different notes refs for patches and range-diffs, and toggling the override. The tests do not currently cover the interaction between `--no-notes` and `--range-diff-notes` that Junio highlighted.
- Prior commit dependency: 85bd88a7 (2025-09-25) introduced `rdiff_log_arg` to `struct rev_info`.

Open questions and loose ends:
- Junio's review identifies a usability issue with the option semantics: whether `--range-diff-notes` should override the patch notes setting only for the range-diff or inherit it by default. This may require redesign or clarification.
- The author previously expressed uncertainty about the CI setup, though all tests currently pass.
- No other review feedback or objections have been recorded yet.
- The implementation does not error when `--range-diff-notes` is used without `--range-diff`, which may warrant further review discussion.

Participant positions:
- Kristoffer Haugsbakk (author): Proposed the feature and implemented the three-patch series. No response yet to Junio's review.
- Junio C Hamano (maintainer): Reviewed patch 3/3 and raised a substantive usability concern about the option semantics, specifically the fallback behavior of `--range-diff-notes`. Position: the current design may confuse users and does not align with intuitive expectations for independent control.

Related work: None mentioned in the thread. The feature is self-contained and does not depend on or conflict with other in-flight efforts. The refactoring in patches 1 and 2 builds on a prior commit (85bd88a7, 2025-09-25) that introduced `rdiff_log_arg` to `struct rev_info`.