This is a review reply in the ongoing discussion about Elijah Newren’s patch to improve `git push` performance from shallow clones. Patrick Steinhardt responds to Elijah’s detailed explanations with a clear shift in perspective: he now believes the real fix lies in enabling `push.negotiate` by default, rather than introducing new logic like `push.shallowExcludeBoundary`.

Patrick’s key argument is that the current patch addresses the symptom at the wrong level. He acknowledges Elijah’s point that `--depth=2` clones don’t reliably solve the problem, and that `push.negotiate` is the true enabler of efficient negotiation. Instead of adding a new config option (`push.shallowExcludeBoundary`), he proposes a smarter default for `push.negotiate`: a new mode (`push.negotiate=shallow`) that automatically enables negotiation for shallow clones, avoiding the latency cost for full clones. This approach would address the common foot-gun scenario (shallow clones resending entire trees) without requiring users to opt into a separate config.

The discussion remains constructive, with no blocking objections, but Patrick’s suggestion could reshape the patch’s direction. His proposal is pragmatic—leveraging existing infrastructure (`push.negotiate`) rather than introducing new logic—and aligns with the project’s preference for minimal, targeted changes. If adopted, it would simplify the solution while maintaining backward compatibility. The thread is now at a crossroads: will the patch proceed as-is, or pivot toward Patrick’s more focused alternative? Either way, the core problem (redundant tree transfers from shallow clones) is now clearly framed as a negotiation gap, not a missing feature.