This is a refactoring patch targeting the reftable backend, part of the ongoing reftable effort. It is the first patch in a four-patch series (`[PATCH v2 1/4]`) titled "optimize reloading the reftable stack".

The patch removes the `REFTABLE_STACK_NEW_ADDITION_RELOAD` flag and its associated logic, making stack reloading the default behavior when a concurrent update is detected. The motivation is that all callers of `reftable_stack_init_addition()` now propagate this flag, so the flag itself has become redundant. The commit message cites two prior commits (`80e7342ea8` and `16684b6fae`) that led to this state, and notes that the change simplifies the code flow and removes redundant checks for `REFTABLE_OUTDATED_ERROR`.

The patch touches four files: `refs/reftable-backend.c`, `reftable/reftable-stack.h`, `reftable/stack.c`, and `t/unit-tests/u-reftable-stack.c`. The changes are mechanical: removing the flag definition, removing the flag parameter from function signatures, and removing conditional blocks that handled the flag. The unit tests are updated to reflect the new default behavior, and two redundant `REFTABLE_OUTDATED_ERROR` checks are removed.

The patch appears well-motivated and cleanly executed. The commit message clearly explains the rationale and the scope of the change, and the diff is focused on the stated goal. There is no indication of controversy or edge cases that might cause problems. The patch is part of a larger series, but this step stands on its own as a sensible cleanup.