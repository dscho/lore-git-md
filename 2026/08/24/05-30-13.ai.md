This is a substantive review reply in the ongoing thread about Elijah Newren’s patch to improve `git push` performance from shallow clones. Patrick Steinhardt responds to Elijah’s detailed explanations with a clear shift in perspective: he now believes the fix should operate at a different layer, advocating for enabling `push.negotiate` by default (or conditionally for shallow clones) rather than introducing new logic in `send-pack`.

Patrick’s key proposal is a new `push.negotiate=shallow` mode that would automatically enable negotiation for shallow clones, avoiding the latency overhead for full clones while addressing the redundant tree-transfer problem. He argues this is a simpler, more targeted fix than Elijah’s patch, which adds a new config option (`push.shallowExcludeBoundary`) and modifies `send-pack` to treat shallow grafts as uninteresting tips. The trade-off—latency vs. bandwidth/CPU savings—remains central, but Patrick’s suggestion reframes the solution as a negotiation-level toggle rather than a pack-generation tweak.

The discussion remains constructive, with no outright objections to Elijah’s patch but a clear preference for a different approach. Patrick’s feedback is substantive, engaging with the core problem (redundant transfers) and proposing an alternative that could simplify the design. The thread’s next steps will likely hinge on whether Elijah agrees this is a better path or defends the current patch’s merits. Key technical details—files touched (`send-pack.c`), the new config key, and the graft-modification edge case—remain relevant but may be superseded if the negotiation-based approach gains traction.