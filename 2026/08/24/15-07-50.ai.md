This is a **brief technical clarification** in the `git history squash` thread, specifically about the **autosquash marker resolution policy**. Junio C Hamano responds to Phillip Wood’s suggestion that the command should accept all-uppercase or all-lowercase OIDs in `fixup!`, `squash!`, and `amend!` markers to match `git rebase --autosquash`’s behavior.

Junio points out that Git has **never emitted uppercase hexadecimal OIDs itself** (citing brian m. carlson’s `bc/restrict-hex-to-lowercase` topic) and argues that `starts_with()`—rather than `istarts_with()`—is sufficient to match the OIDs Git would produce. This implies **strict case-sensitive matching** (rejecting all uppercase OIDs) is the correct policy, aligning with the project’s historical convention and brian’s prior work.

The exchange does not resolve the policy question but **reinforces the strict case-sensitive position** as the technically consistent choice. The thread remains open, but Junio’s input suggests the final implementation will likely **replace `istarts_with()` with `starts_with()`** and reject all uppercase OIDs, rather than adopting a permissive approach. This is the **last technical hurdle** before the series can proceed to integration.