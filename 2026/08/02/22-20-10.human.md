This is a performance patch for partial clones that delivers a 15–20% speed-up in `git index-pack` by eliminating a serialization bottleneck in promisor pack link recording. The author, Arijit Banerjee, responds to a procedural objection from brian m. carlson about AI assistance disclosure in the commit message by proposing to treat the submission as an RFC. This preserves the technical result—a well-tested, benchmarked optimization—while acknowledging the policy concern without conceding its validity.

The patch itself remains unchanged: it replaces the per-object mutex with thread-local oidmaps during normal promisor indexing, leaving strict and fsck code paths untouched. Performance gains are substantial (15% end-to-end, 19% in the index-pack phase, 46% less system CPU) and reproducible across controlled and real-world measurements. Test coverage is thorough, including new edge-case checks for malformed headers and type mismatches, and the patch passes all existing SHA-1 and SHA-256 tests. The implementation is clean, with no on-disk format changes, and the commit message is unusually detailed about methodology and results.

The procedural objection is the only open issue. brian’s review is surface-level, focusing solely on the AI disclosure rather than the patch’s technical merits, and offers no resolution path. Arijit’s reply keeps the discussion constructive by separating the policy question from the technical value, inviting maintainers to weigh the optimization’s benefits against the policy constraints. The thread is now at a crossroads: the patch is ready for technical review, but its fate hinges on whether the project is willing to accept AI-assisted contributions or whether the author will need to reimplement the solution independently. For now, the performance improvement stands on its own merits.