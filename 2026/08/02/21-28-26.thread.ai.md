Thread identity
Subject: [PATCH] read-cache: avoid index expansion in repo_index_has_changes() on unborn HEAD
Series version: standalone patch (v2 anticipated)
Originating author: Sahitya Chandra
Thread type: bug fix (performance regression in sparse-checkout/sparse-index)

Core problem or goal
When `repo_index_has_changes()` is called without an explicit tree and HEAD is unborn (e.g., on a newly created orphan branch), the function falls back to walking the index directly, which forces a full expansion of any sparse directory entries. This expansion is unnecessary because an unborn HEAD is logically equivalent to the empty tree, so every index entry is new. The patch replaces the fallback with a comparison against the empty tree, keeping the unborn-branch case on the same diff code path as normal tree comparisons and avoiding the expansion. The patch originated from a TODO comment rather than a user-reported issue; Sahitya concedes no realistic use case exists where a user with an unborn HEAD would simultaneously be using a sparse index containing millions of uncommitted files, but argues the patch still has value: it removes a TODO, unifies the code path, and eliminates an `ensure_full_index()` call that future readers would otherwise need to reason about.

Current status
Under review; v1 posted, v2 planned to address test-helper output format flaw. Elijah Newren and Sahitya Chandra have reached agreement on the patch's motivation and scope: it is a code hygiene improvement, not a performance fix. Routine ping sent 2026-08-02.

Key technical details
- File touched: `read-cache.c`
- New symbol: `test-tool read-cache --index-has-changes` (test helper)
- Test file added: `t1092-sparse-checkout-compatibility.sh` (new test case for unborn-branch scenario)
- Behavior change: unborn HEAD now uses the empty tree (`repo->hash_algo->empty_tree`) instead of walking the index directly
- On-disk format: unchanged
- Test coverage: new test verifies that paths inside sparse directories are reported without expanding the index
- Code impact: net reduction of 28 lines in `read-cache.c`
- Test helper flaw: `test-tool read-cache --index-has-changes` output format (`dirty=%s`) is ambiguous if multiple files are dirty, since it prints the entire `strbuf` as a single line

Open questions and loose ends
- **Test helper output format**: The current format (`dirty=%s`) is ambiguous for multiple dirty files; Sahitya plans to fix this in v2 by revising the output to handle multiple paths unambiguously.
- **Unittesting harness**: Elijah Newren raised whether the unittesting harness could obviate the need for test-helper changes, but noted he is not familiar with its capabilities. This remains unresolved but is not a blocker for v2.

Participant positions
- **Elijah Newren** (substantive review): Initially raised fundamental questions about the patch's motivation and identified the test-helper bug. After discussion, accepted Sahitya's argument that the patch has value as a code hygiene improvement. Engaged with the patch's design and correctness, not surface-level issues.
- **Sahitya Chandra** (author): Acknowledged the lack of a concrete use case but defended the patch's value on code hygiene grounds (removing a TODO, unifying code paths, reducing code footprint). Agreed to fix the test-helper output format in v2.

Related work
Part of the broader sparse-checkout/sparse-index performance effort (Derrick Stolee, Elijah Newren).