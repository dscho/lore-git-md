This is a **follow-up discussion** in the `git add --resolved` series, specifically addressing a design clarification and a potential refactoring opportunity in Git’s option-parsing logic. Junio C Hamano responds to his own earlier suggestion about simplifying the option incompatibility check in `die_for_incompatible_opt3()` and concludes that the correct fix is to retain `0 < addremove_explicit` (not `addremove`), as the latter would misfire when `add_resolved` is set. He then steps back to critique Git’s broader option-parsing pattern in `git add`, noting that it conflates the presence of command-line flags (e.g., `-A`) with the behaviors they trigger (e.g., `addremove`), making the logic unnecessarily confusing. Junio suggests a future refactoring: separating the detection of incompatible options (done upfront) from the determination of the program’s operating mode (decided afterward), which could reduce complexity and prevent similar bugs. This is a **substantive architectural observation**, not a blocker for the current series, but it signals a potential follow-up effort to improve maintainability in Git’s CLI parsing. The core `--resolved` feature remains unaffected; this discussion is purely about internal code structure.