This is a performance-focused bugfix for `git index-pack` when handling promisor packs (used in partial clones). The patch eliminates a bottleneck where worker threads were forced to serialize while parsing every non-blob object just to record outgoing links. Instead, it reads links directly from object buffers, using thread-local storage to avoid contention, and merges the results afterward.

The change is well-motivated: median clone time for `--filter=blob:none` drops by 15% (156s → 133s) in controlled tests, with the promisor phase itself improving by 19%. System CPU time also decreases by 46%. Full clones see no regression, as expected. The patch preserves all existing safety checks (strict/fsck paths remain unchanged) and adds new validation for malformed headers, conflicting types, and mismatched tag targets.

Code quality is high: the commit message is detailed, benchmarks are thorough, and the implementation is careful (e.g., handling commit grafts, preserving subtree-only policies). New tests cover edge cases. The patch touches only `builtin/index-pack.c` and adds tests to `t5302-pack-index.sh`, making it self-contained.

This looks like a clear win for partial clone performance and is likely to be uncontroversial. The author acknowledges AI assistance but takes responsibility for the submission, and the approach aligns with Git’s existing patterns. Worth watching for follow-up reviews, but the patch appears solid.