This is the fourth patch in Pablo Sabater’s GSoC v2 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder for remote-object-info queries. The thread is now focused on an architectural refactoring recommended by Junio C Hamano and Jeff King: the remote-object-info path should bypass `struct object_info` entirely and define its own lightweight query structure.

Pablo acknowledges the feedback and proposes a concrete design: a new `struct object_info_results` to hold batch query results, with boolean flags (`wants_size`, `wants_type`) and arrays for the returned data. He asks whether to implement this refactoring now as part of the series or defer it to a follow-up cleanup. The question is practical—GSoC ends in a few weeks—and the tone is collaborative, not defensive.

The patch itself (v2 4/6) is now superseded by the architectural discussion. The refactoring would touch `fetch-object-info.c` and `builtin/cat-file.c`, replacing the current `object_info`-based parsing with a direct mapping to `expand_data`. Pablo’s proposed structure is a reasonable starting point, though the final design may evolve as the implementation proceeds. No merge status is implied; the series remains under review.