This is a friendly ping on a well-crafted test-infrastructure bugfix series that has been under review for a month. The v2 revision (posted 2026-08-01) addresses all of Junio C Hamano’s substantive feedback from v1: commit messages and code comments now explicitly distinguish between shrinking the race window (achieved) and preventing concurrent script execution (not the goal), and the rationale for the atomic `mkdir` check in all cases—including the `"permanent"` mode—is clearly documented. The series targets a real, observed flake in `t5616.47` on macOS CI runners, where concurrent HTTP requests to the same endpoint caused HTTP 500 errors due to non-atomic state management in two CGI helper scripts (`apply-one-time-script.sh` and `http-429.sh`).

The fixes are minimal and self-contained, touching only the `t/lib-httpd/` directory and adding a new test (`t5567-one-time-script.sh`) that exercises the fixed helper deterministically using FIFOs. The documentation patch (patch 3) generalizes the atomic idioms in `t/README`, linking to Git’s lockfile machinery and `make_symlink()` as precedents. Michael Montalbo links to two recent CI failures (GitGitGadget and GitHub’s Git mirror) that appear to be caused by the same race, reinforcing the urgency of the fix. No objections or alternative proposals have been raised, and the series now looks ready for integration. The ping is a routine nudge, not a sign of controversy.