Summarizer brief mode:

This is a review reply in the `git add --resolved` series, specifically targeting the performance optimization patch (3/4) that adds an early-exit check for binary files in `has_conflict_markers()`. Michael Montalbo raises a subtle correctness concern: the current placement of the `buffer_is_binary()` check *after* the conflict-marker detection could cause the function to incorrectly return `has_markers = 1` if the first line of a binary file happens to match the pattern for a conflict marker (e.g., starts with `<<<<<<<`). The fix would require moving the binary check *before* the marker check in the loop.

The issue is narrowly scoped to the helper’s internal logic and does not affect the user-facing `--resolved` behavior or any other patches in the series. It is a classic false-positive edge case: while conflict markers are unlikely to appear in binary files, the current implementation could misclassify a binary file as conflicted if its first line coincidentally matches the marker pattern. The proposed reordering is a one-line change with no performance impact and would make the helper more robust. No review feedback has been posted yet, but this is a substantive correctness fix rather than a stylistic nit. Key technical detail: the `buffer_is_binary()` check must precede the `is_conflict_marker_line()` call in the loop.