**Summarizer brief mode**

This is a follow-up in the `git repack --drop-filtered` RFC series for partial clones, focusing on Junio’s proposed time-based heuristic for culling promisor blobs. Siddharth Shrimali responds positively to the idea of using object recency as a smarter alternative to the existing merge/rebase/cherry-pick and index validation guards, framing it as a natural extension of the "enumerate-then-filter" framework.

Key technical details: promisor objects are always stored in packs (never loose), so per-object timestamps are not natively available. The current best proxy is the mtime of the promisor pack itself, which reflects the fetch time but loses granularity after repacking consolidates multiple packs. Siddharth notes that Git already tracks per-object mtimes for cruft packs (via `.mtimes` files), suggesting this could serve as precedent for a similar mechanism for promisor objects. The proposed heuristic ("don’t cull objects younger than X minutes") would plug into the existing `enumerate_promisor_blobs()` logic as an additional filter predicate, requiring no structural changes to the series. Implementation would likely involve extending the promisor object log or introducing a separate timestamp file to preserve age data across repacks.

Siddharth’s response treats the time-based heuristic as a follow-up rather than a blocker for the current series, but the discussion signals a likely design direction for the next revision. The tone remains constructive, with both participants engaging on the trade-offs between simplicity and smarter heuristics. No code changes accompany this email, but the exchange clarifies that the current safety guards (merge/rebase/cherry-pick checks and index validation) are viewed as optional optimizations rather than correctness measures. The thread’s next steps will likely involve either removing the guards or replacing them with the time-based heuristic, with documentation updates to reflect the chosen approach.