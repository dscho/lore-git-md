This is the first patch in the v5 series adding `--reset-when-found` to `git bisect`. It prepares the plumbing for the feature by adding a `quiet` parameter to `bisect_reset()`, allowing the checkout step to suppress progress and branch-status output. The change is internal and does not alter existing behavior, as no caller sets the flag yet.

The patch touches only `builtin/bisect.c`, modifying `bisect_reset()` to conditionally pass `--quiet` to the underlying `git checkout` command. The parameter is a `bool`, and the function’s existing callers (`bisect_replay()` and `cmd_bisect__reset()`) are updated to pass `false` to preserve the current behavior. The diff is minimal and mechanical, reflecting the preparatory nature of the change.

This patch is uncontroversial and well-motivated: the quiet flag will be used in the next patch to avoid cluttering output when the auto-reset fires after a successful bisect. The commit message clearly states the purpose and scope, and the implementation is straightforward. No test changes are needed, as the behavior remains unchanged until the next patch. The patch is ready for integration and aligns with the series’ overall goal of centralizing the auto-reset logic in `cmd_bisect()`.