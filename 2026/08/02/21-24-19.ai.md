This is the first patch in the **v6 series** adding `--reset-when-found` to `git bisect`. It prepares the plumbing for the feature by adding a `quiet` parameter to `bisect_reset()`, allowing the checkout step to suppress progress and branch-status output. The change is internal and does not alter user-visible behavior yet.

The patch modifies `builtin/bisect.c` to pass `--quiet` to `git checkout` when the new `quiet` parameter is `true`. Existing callers (`bisect_replay()` and `cmd_bisect__reset()`) are updated to pass `false`, preserving current behavior. The diff is minimal and mechanical, touching only the checkout invocation and function signature.

This is a **refactoring patch** with no functional impact on its own. Its purpose is to enable the auto-reset feature in the next patch, where the quiet mode will be used to avoid noisy output during scripted or CI workflows. The change is uncontroversial and aligns with the series' goal of making auto-reset seamless for automation.

The patch is well-scoped and clearly motivated by the follow-up feature. No test changes are needed here, as the quiet behavior will be validated in the second patch's test coverage. The implementation matches Junio's earlier suggestion to centralize the quiet logic in `bisect_reset()`.