This is the latest reply in the ongoing RFC thread about restricting Git 3.0 to lowercase-only hex object IDs. Brian pushes back against Junio’s invocation of the Robustness Principle, arguing that modern security practices require strict input validation rather than leniency. He cites real-world examples (TLS, HTTP request smuggling, Git’s own CR-handling issues on Windows) where liberal parsing has led to security vulnerabilities, and notes that most of the security problems he’s observed involve object IDs passed on the command line—an area Junio’s proposed compromise (strict internal enforcement, lenient network parsing) wouldn’t fully address. Brian also rejects the idea of auditing every `oid_to_hex` call to implement a partial fix, framing the current series as a cleaner, more comprehensive solution.

The exchange sharpens the philosophical divide: Junio sees the Robustness Principle as a timeless guideline, while Brian treats it as outdated in a security-conscious world. Neither side appears ready to concede, but Brian’s latest response may force the discussion toward concrete trade-offs—whether the security benefits outweigh the ecosystem disruption, or whether a narrower compromise (e.g., strict validation only for command-line inputs) could satisfy both concerns. The thread remains in RFC limbo, with no clear path to resolution.