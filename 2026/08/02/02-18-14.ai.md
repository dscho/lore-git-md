This is Junio C Hamano following up on the `git repack --drop-filtered` RFC series for partial clones. The thread is now focused on refining the design of the safety guards introduced in patch 6/7. Junio proposes a smarter, time-based heuristic for determining which promisor blobs to cull: instead of unconditionally dropping all blobs exceeding the size threshold, the feature could avoid culling recently fetched objects (e.g., those downloaded within the last 20 minutes). This would reduce redundant network operations without requiring the merge/rebase/cherry-pick checks or index validation currently implemented as "optional optimizations."

The suggestion is technically straightforward—it would involve tracking object fetch timestamps (likely via a new metadata field in the promisor object log or a separate timestamp file) and extending the filter logic in `enumerate_promisor_blobs()` to incorporate age-based criteria. Junio frames this as an alternative to the existing safety guards, not a replacement for the core `--drop-filtered` functionality, and implies it could be implemented incrementally. The proposal aligns with the series’ goal of minimizing network overhead while preserving correctness, and it sidesteps the earlier debate about whether the guards were necessary at all.

No patches or code changes accompany this email, but the idea is likely to influence the next revision of the series. If adopted, it would require updates to the enumeration logic (patch 4/7) and possibly the documentation (patch 7/7) to describe the new behavior. The tone remains constructive, with Junio engaging directly with the trade-offs between simplicity and smarter heuristics.