This is a performance patch for partial clones, specifically targeting the `git index-pack` promisor link recording bottleneck. The author, Arijit Banerjee, responds to brian m. carlson's procedural objection about AI assistance in the patch's development by proposing to treat the submission as an RFC. The goal is to preserve the technical result—a 15–20% speedup in `--filter=blob:none` clones—while seeking maintainer guidance on whether the optimization is worth pursuing despite the policy concern.

The patch itself remains unchanged in this reply. It introduces thread-local oidmaps to avoid the per-object mutex during promisor pack indexing, leaving strict and fsck code paths untouched. The implementation includes new symbols (`struct outgoing_link`, `record_outgoing_links_from_data()`) and adds test coverage for edge cases like malformed commit headers and conflicting link types. Performance benchmarks show consistent gains (15–26% end-to-end reductions) without regressing full clones, and existing SHA-1/SHA-256 tests pass.

The key development here is the shift in framing: the author now positions the patch as a *proposal* rather than a final submission, asking maintainers to weigh the performance benefit against the policy ambiguity. This sidesteps the immediate procedural block but leaves the technical merits unresolved. The thread’s focus has pivoted from "is this correct?" to "is this worth an exception?"—a question that will likely require input from Junio or the PLC, given its policy implications.

No new technical feedback is provided in this reply, and the earlier substantive review context (performance data, test coverage, implementation details) remains valid. The patch’s quality—well-documented, benchmarked, and tested—is not in dispute; the sole obstacle is its AI-assisted origin.