This is a bugfix for `git worktree add` that restores the intended behavior when `-b` or `-B` is explicitly provided. The patch is the fourth iteration (v4) of a standalone fix for a regression introduced in Git 2.42.0, where DWIM logic incorrectly took precedence over explicit branch creation options, leading to unintended branch names.

The change is minimal and surgical: in `builtin/worktree.c`, the DWIM logic is now guarded by a check for `!new_branch`, ensuring that when `-b` or `-B` is given, the provided branch name is used as-is rather than being DWIMmed to a remote-tracking branch. The test suite (`t/t2400-worktree-add.sh`) gains a new test case that verifies the corrected behavior by confirming that `git worktree add -b branch ../wt foo` fails with the expected error message when `foo` is not a valid reference.

This v4 iteration addresses the last remaining feedback from Junio C Hamano: the commit message now cites the regression commit using the preferred `log -1 --pretty=reference` format, and the logic for handling `HEAD` with `-b` is preserved to avoid suppressing existing warning behavior. The patch is already merged to `master`, so this email serves as a final record of the change rather than an active request for review.

Key technical details for future reference: the regression was introduced by commit `128e5496b3`; the fix touches only the DWIM suppression logic in `builtin/worktree.c` and adds one test case; and the on-disk format remains unchanged. No controversy or open questions remain.