This is a brief follow-up in the reftable stack-reload optimization series, specifically about the first patch that removes the `REFTABLE_STACK_NEW_ADDITION_RELOAD` flag. Karthik Nayak acknowledges Patrick Steinhardt’s observation that libgit2 (a separate project) uses the flag but confirms all libgit2 call sites already pass it, so the change is compatible. Karthik offers to send a pull request to libgit2 once the patch lands in Git. He also accepts Patrick’s cosmetic suggestion to rename `reftable_stack_new_addition()` to `reftable_stack_addition_new()` for naming consistency, proposing to add a separate commit for the rename.

The exchange is purely about external compatibility and API aesthetics; there are no new technical concerns or objections. The patch itself remains uncontroversial and well-motivated, with the core optimization goal intact. The only remaining open question for the series is Junio C Hamano’s safety concern about the lock relocation in patch 2, which this email does not address.