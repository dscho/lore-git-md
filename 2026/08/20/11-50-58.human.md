This is the final confirmation exchange in the `dk/use-nsec-runtime` series, which replaces the build-time `USE_NSEC` macro with the runtime configuration option `core.useNanosec`. The series is cooking in `next` and is effectively complete, pending minor polish.

D. Ben Knoble and Patrick Steinhardt clarify a small oversight from an earlier review: Patrick initially thought there was an unupdated reference to `USE_NSEC` in `builtin/update-index.c`, but confirms the comment in question was already corrected by the patch. The only remaining `USE_NSEC` reference in the codebase is in a release note (`Documentation/RelNotes/2.5.0.adoc`), which is not touched by this series. Patrick also reaffirms his support for eliminating preprocessor directives in `is_racy_stat()` by unconditionally initializing the `use_nanosec` field, a change both he and Junio Hamano previously endorsed.

The series is well-motivated, thoroughly reviewed, and addresses all known correctness and performance concerns. The remaining polish items (unconditional initialization, simplified logic, and documentation updates) are uncontroversial and will be incorporated before graduation to `master`. The only coordination needed is resolving a textual conflict with `ty/repo-config-cleanups` in `seen`, but this is a routine integration matter. The patch is a clear improvement, enabling users to enable nanosecond precision without recompiling Git while preserving backward compatibility and performance.