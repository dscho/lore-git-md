This is a **follow-up from the author** in the `git pull --hard` thread, responding to Junio’s principled opposition and Phillip Wood’s proposed redesign (`git pull --reset`).

Artur Bieniek now explicitly endorses Phillip’s `--reset` redesign over his original `--hard` proposal, arguing that the new design better aligns with Git’s safety principles by making destructive behavior opt-in. He identifies a concrete use case that Junio’s preferred `git pull --ff-only` does not cover: tracking an upstream branch that has been force-pushed or rebased. In this scenario, a user with no local development (only a clean checkout of the upstream branch) cannot sync to the rewritten history with `--ff-only`, but `--reset` would allow it while still protecting uncommitted changes by default. This use case directly addresses Junio’s concern that the feature might be a niche convenience rather than a necessary addition.

The email does not include a revised patch or implementation details, but it shifts the thread’s focus from "should this exist?" to "how should it work?" by providing a clear, motivated example where the proposed behavior is genuinely useful. No other participants have yet responded to this specific argument, leaving the ball in Junio’s court to either accept the use case or further refine his objections. The thread remains under review, with the feature’s inclusion still uncertain but now grounded in a specific workflow gap.