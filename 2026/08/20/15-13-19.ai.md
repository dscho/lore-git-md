This is a test patch targeting the refs subsystem, specifically adding coverage for `git symbolic-ref`'s exit codes and output when querying a non-symbolic ref. The patch is now a follow-up to Patrick Steinhardt's review, addressing his questions and incorporating his suggestion to verify stdout emptiness.

Nikolaus clarifies the motivation: this is part of a systematic effort to ensure every Git command's documented behavior is actually tested, not an opportunistic fix. He also explains the technical reason for the differing exit codes (128 vs. 1 with `--quiet`): the non-quiet path uses `die()`, which always exits 128, while `--quiet` cannot use `die()` (as it would print output) and thus returns 1. This behavior is consistent with other Git commands like `git rev-parse --verify --quiet`. The patch now includes Patrick's proposed test to verify stdout is empty in both cases.

The discussion has settled the "why" behind the exit code behavior, but the thread remains open on whether the current design is intentional or a quirk worth revisiting. No objections to the patch itself have been raised, and the changes are confined to `t/t1401-symbolic-ref.sh`. Key technical details: the patch adds two tests, checks stdout emptiness, and touches no production code or on-disk formats. The thread may still spawn a follow-up patch to align exit codes if the current behavior is deemed unintentional.