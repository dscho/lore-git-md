This is a test patch for the `git symbolic-ref` command, now in its second version. The patch adds thorough test coverage for the exit codes and output behavior when querying a non-symbolic ref, both with and without the `--quiet` (`-q`) flag. The update incorporates feedback from Patrick Steinhardt, adding checks to verify that stdout is empty in both cases, which was not tested in the initial version.

The patch remains minimal and focused: it touches only `t/t1401-symbolic-ref.sh`, adds two new tests (plus the stdout check), and follows the existing test style. The commit message is clear and now explicitly references other tests (`t2020`, `t5621`) that rely on this behavior, reinforcing the motivation for systematic coverage. The patch documents existing behavior rather than introducing new functionality, aligning with the project’s preference for test-driven clarity.

The discussion around this patch has clarified the technical rationale for the differing exit codes (128 without `--quiet`, 1 with it), but no consensus has emerged on whether this design is intentional or a quirk worth revisiting. Patrick initially called the behavior "weird" and questioned whether `--quiet` should affect exit status at all, while Nikolaus argued it’s consistent with other Git commands like `git rev-parse --verify --quiet`. The patch itself is uncontroversial and ready for review, but the broader design question may lead to follow-up work if the current behavior is deemed unintentional.

This is a well-crafted, low-risk contribution that fills a clear gap in test coverage. The patch is likely to be merged as-is, though the thread may continue to explore the exit code design. For now, it’s a straightforward addition that busy readers can safely note as a minor but useful improvement.