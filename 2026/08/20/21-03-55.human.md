This is the sixth iteration of a usability-focused patch series that improves error messages for `git worktree add`, `git checkout`, and `git switch` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). The series is now split into three patches: the first extracts a shared helper function for displaying advice, the second applies it to `git checkout` and `git switch`, and the third extends it to `git worktree add`.

The core change replaces the terse "matched multiple remote tracking branches" warning with a detailed, actionable advice message that lists the conflicting remotes (e.g., "the branch exists on `origin`, `upstream`") and suggests using `--track` or setting `checkout.defaultRemote` to resolve the ambiguity. The advice is gated behind the `advice.checkoutAmbiguousRemoteBranchName` config key (enabled by default) and is consistent across all three commands. The series also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression fix, as the commands were already failing—just with less clarity.

The implementation is now well-structured, with the advice logic unified in a shared helper (`advise_disambiguating_remotes()`) and applied consistently to `checkout`, `switch`, and `worktree add`. The code touches `builtin/checkout.c`, `builtin/worktree.c`, `checkout.c`, `checkout.h`, and the test script `t/t2400-worktree-add.sh`. Test coverage is thorough, including updates to existing tests and a new test for the `--guess-remote` case, which is explicitly excluded from emitting warnings. The series addresses earlier feedback by renaming the helper function for clarity and splitting the first patch into mechanical refactoring and functional changes.

This looks like a straightforward usability win with no obvious downsides. The changes are localized, well-motivated, and follow Git’s conventions for advice messages. The series is functionally complete and ready for merging, with only minor nits remaining. If you care about user-facing error messages or have encountered this ambiguity yourself, this patch is worth reviewing.