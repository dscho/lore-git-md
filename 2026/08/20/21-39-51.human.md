This is a **substantive review follow-up** in the zsh Git completion bugfix thread. Junio C Hamano corrects a technical misunderstanding about POSIX shell grammar that arose during the review of Lutz Lengemann’s patch. The discussion centers on whether an empty action (just `;;`) after a `case` pattern is valid in shell scripts—specifically, in the context of the zsh completion script’s `_arguments` spec.

Junio clarifies that POSIX.1 *explicitly* allows `;;` to follow a pattern without any intervening command list, citing the formal grammar for `case_item` in the POSIX shell specification. This means constructs like `bar) ;;` are perfectly valid, contrary to D. Ben Knoble’s initial assumption that zsh’s tolerance for empty actions was a non-standard extension. The correction is precise and backed by a direct link to the POSIX standard, reinforcing the robustness of the patch’s approach.

The exchange is narrow in scope but relevant to the patch’s correctness: the `_arguments` spec in `git-completion.zsh` uses empty actions for certain global options (e.g., `-c|--git-dir|--work-tree`), and this design choice is now confirmed to be standards-compliant. No changes to the patch are needed, and the thread remains unblocked. This is a minor but useful clarification for anyone reviewing the completion script’s internals.