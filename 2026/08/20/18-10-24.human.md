This is a feature addition introducing `git history squash`, a new subcommand that folds a range of commits into their oldest ancestor while preserving descendant history. The v14 patch series from Harald Nordgren is now functionally complete and ready for integration, addressing all prior technical feedback.

The command provides a faster alternative to `git rebase --autosquash` for consolidating fixup/squash/amend commits, avoiding repeated conflict resolution by performing a single tree operation. Key behaviors include: rejecting operations that would leave local branches dangling (with advice to use `--update-refs=head`), preserving merge topology, and defaulting to an editor workflow with autosquash-style message templates. The implementation touches core Git machinery in `builtin/history.c`, `sequencer.c`, and adds thorough test coverage in `t/t3455-history-squash.sh`.

Notable improvements in v14 include proper trailer ordering (adding Phillip Wood’s `Signed-off-by` above his `Helped-by` line) and test simplifications to avoid case-munging issues. The series is now split into eight focused patches, with the core functionality decomposed into range validation, ref protection, commit creation, and message editing steps. Junio’s earlier "Will replace" signal and queued test fixup indicate strong intent to merge, though Phillip Wood’s procedural expectations (explicit replies to open questions) remain unaddressed in this iteration.

This looks like a well-crafted addition that fills a clear workflow gap, with potential to serve as a backend for future `git rebase --autosquash` optimizations. The thorough test coverage and careful handling of edge cases (merge commits, autosquash markers, ref protection) suggest it’s ready for wider review.