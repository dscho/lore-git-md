This is a **maintainer response** in the `git pull --hard` thread, addressing Phillip Wood’s proposed redesign (`git pull --reset [--discard-changes | --autostash]`). Junio C Hamano reiterates his principled opposition to adding destructive operations to `git pull`, even with the safety improvements Phillip suggested.

Junio acknowledges that Phillip’s redesign would make the feature *safer* than the existing `git fetch && git reset --hard @{u}` workflow by protecting uncommitted changes by default, but he questions whether the use case itself is compelling enough to justify inclusion. He points out that `git pull --ff-only` already serves users who want to keep a local repository in sync with upstream without integrating local changes, framing the proposed `--reset` mode as a niche convenience rather than a necessary addition.

The response does not engage with the technical details of Phillip’s proposal (e.g., error handling, `--autostash` behavior, or CLI ergonomics) but instead challenges the underlying motivation. Junio’s position remains that `git pull` should focus on history integration, not working-copy management, and that users who want to discard local state should do so explicitly with separate commands. The thread is now at an impasse: the author (Artur Bieniek) has not yet responded to Phillip’s redesign, and Junio’s latest reply suggests the feature’s inclusion would require a stronger justification of the use case itself, not just a safer implementation. No other participants have weighed in on this specific exchange.