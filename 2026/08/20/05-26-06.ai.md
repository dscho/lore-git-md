**Summarizer brief mode**

Patrick Steinhardt replies to Junio C Hamano’s rejection of a runtime warning for `core.useNanosec=true` on `NO_NSEC` platforms, explicitly agreeing that such a warning would be "too obnoxious" for users who share a `.gitconfig` across machines with different Git builds. This closes the last open design question in the `dk/use-nsec-runtime` series: **no runtime warning will be added**, and the responsibility for avoiding silent misconfiguration on unsupported platforms rests entirely on documentation and user awareness.

The exchange confirms the project’s preference for **user experience** over proactive warnings in shared-configuration scenarios, even when the risk involves platform-specific behavior (e.g., `NO_NSEC` builds ignoring the setting). The decision does not affect the other polish items from Patrick’s earlier review (unconditional initialization of `use_nanosec`, preprocessor guard simplification, or the missed `USE_NSEC` reference in `builtin/update-index.c`), which remain open pending the author’s response.

Key technical context for follow-up:
- **Config key**: `core.useNanosec` (defaults to false).
- **Platform behavior**: On `NO_NSEC` platforms, the `use_nanosec` field and related code paths are `#ifdef`-ed out, ensuring the setting is ignored without runtime warnings.
- **Documentation**: The patch will include a warning about platform/filesystem limitations (e.g., "may not be safe on all platforms"), but no runtime feedback will be provided.
- **Shared config scenario**: Users with a single `.gitconfig` across machines with different builds (e.g., one with `NO_NSEC`, one without) will not receive warnings when `core.useNanosec=true` is set on the unsupported platform. The project considers this acceptable, prioritizing silence over proactive alerts.