This is a brief reply in the ODB abstraction series, clarifying the architectural intent of patch 6/6. Patrick Steinhardt confirms Justin Tobler’s understanding: `git bundle` will work with any ODB backend (not just the files backend) while still producing packfiles as the common interchange format. The bundle format itself remains unchanged. This reply resolves the open question about long-term compatibility without introducing new technical concerns or implementation details. The series’ goal of decoupling transport operations from the files backend is now explicitly extended to `git bundle`, completing the refactoring for all pack streams leaving the repository. No further discussion is needed on this point.