This is the first patch in the v6 series improving error messages for ambiguous remote branch names in `git checkout` and `git switch`. It is a refactoring patch that extracts the advice-display logic into a new shared helper function, `advice_disambiguating_remotes()`, without changing any behavior.

The patch moves the advice message — which suggests using `--track` or setting `checkout.defaultRemote` — from the `parse_remote_branch()` function into a dedicated helper. This is a mechanical change that prepares the codebase for the functional improvements in the next two patches. The helper takes an `enum checkout_command` parameter to tailor the advice to either `git checkout` or `git switch`, and it will later be reused by `git worktree add` in patch 3/3.

The commit message is minimal, but the purpose is clear from the thread context: this is a preparatory step to unify and reuse the advice logic across multiple commands. The change is small, focused, and does not introduce any new functionality or edge cases. It looks clean and ready for merging as part of the series.