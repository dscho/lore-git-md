This is a post-merge confirmation for a bugfix that has already reached `master`. The patch restores the intended behavior of `git worktree add -b/-B`: when a user explicitly specifies a branch name with `-b` or `-B`, Git should not attempt to DWIM (Do What I Mean) it to a remote-tracking branch, even if such a branch exists. The regression was introduced in Git 2.42.0 and this fix ensures explicit branch creation takes precedence over DWIM logic.

Junio C Hamano’s reply here is brief and final: he acknowledges the v4 iteration as a clean, minimal solution that avoids the side effects seen in earlier versions, and confirms it is queued (already merged, in fact). The only change from v3 is a documentation tweak to the commit message, now citing the regression commit in the preferred `log -1 --pretty=reference` format.

The patch itself is small and surgical, touching only `builtin/worktree.c` to guard DWIM logic with a `!new_branch` check, and adding a single test case in `t/t2400-worktree-add.sh` that verifies the corrected behavior. The test uses `setup_remote_repo` to avoid redundant setup and checks for the expected error message when an invalid reference is provided with `-b`.

No further discussion or changes are expected; this email serves as the final record of the fix. The patch is well-motivated, thoroughly reviewed, and already integrated into the mainline. If you care about `git worktree` behavior, this is a done deal.