This is the second patch in the v4 series converting `USE_NSEC` to runtime configuration. It is a no-op refactoring of `repo_config_values_init()` in `environment.c` that reorders the assignments to match the struct declaration, grouping them by config section ("core", "sparse", "branch") for clarity and maintainability.

The patch makes no behavioral changes; it only reorganizes the initialization sequence to make future additions easier to validate. The diff shows 12 insertions and 7 deletions, all within the same function, with no new fields introduced. This patch is uncontroversial and was confirmed correct by Patrick Steinhardt in earlier review rounds. It remains ready to advance alongside the rest of the series, which is cooking in `next` under the topic `dk/use-nsec-runtime` and is effectively complete pending final polish.

The reordering is purely mechanical: assignments are now grouped under comments that mirror the config sections they belong to, and the order within each group matches the struct definition. This change was motivated by the upcoming addition of the `use_nanosec` field (in patch 3/3), which will make it easier to verify that all struct members are properly initialized. The patch touches only `environment.c` and is self-contained.