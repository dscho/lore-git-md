This is a test patch that adds coverage in `t1402-check-ref-format.sh` for refname characters already documented as forbidden but not explicitly tested in embedded form. The v2 iteration adopts the reviewers' suggestion to replace repetitive `invalid_ref` checks with a loop over forbidden characters (space, `~`, `^`, `:`, `*`, `[`, `".."`, and backslash), and retains the explicit test for the `@` edge case. The patch is purely additive, test-only, and has no impact on production code.

The thread context confirms this is the second version of a standalone patch, with all prior feedback incorporated. Both Patrick Steinhardt and Junio C Hamano reviewed the first version substantively, praised the `@` edge case, and suggested the loop refactoring now implemented. The fixture inconsistency (`heads/` vs `refs/heads/`) was explained as inherited and not affecting validation logic, and no further changes were requested. Discussion appears converged.

The patch touches only `t/t1402-check-ref-format.sh`, introducing no new symbols or behavior changes. It documents existing behavior in the spirit of precedent `919eb8ace` (a test-only patch for refs ending with a dot). The loop structure makes the test more maintainable and easier to extend if the forbidden character set changes in the future. No open questions or loose ends remain.