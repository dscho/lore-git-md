This is a **substantive reply** in the ODB refactoring thread, where Patrick Steinhardt clarifies the error-propagation logic in the files backend for patch 3/7. The exchange with Karthik Nayak centers on the `odb_source_files_read_object_info()` function, which now distinguishes between missing (`ODB_READ_NOT_FOUND`) and corrupt (`ODB_READ_ERROR`) objects.

Patrick pushes back on Karthik’s surface-level nit about explicit enum comparisons, reiterating his stance from patch 2/7 that returning raw integers (`0`, `-1`) is idiomatic and that converting to named constants (`ODB_READ_OK`, `ODB_READ_ERROR`) would add verbosity without clear benefit. This point remains unresolved, but it’s now clear it’s a deliberate stylistic choice rather than an oversight.

The core of the reply addresses Karthik’s deeper question about the error-propagation policy. Patrick walks through the four possible failure cases, explaining why the logic filters out `ODB_READ_NOT_FOUND` for the packed backend: when the packed backend reports "not found" but the loose backend reports corruption, the loose backend’s error is propagated to ensure corruption is surfaced. Conversely, if the packed backend reports corruption (regardless of the loose backend’s status), its error is prioritized. This design ensures that corruption errors are never masked by "not found" errors, and that the packed backend’s corruption takes precedence when both backends fail. The explanation is thorough and leaves no ambiguity about the intended behavior, though the commit message for patch 3/7 could have preempted this discussion by documenting the policy.

No new technical concerns are raised, and the latent correctness issue Junio identified (duplicate objects in corrupt packs) is untouched. The thread is now poised for either a final ack or further discussion if Karthik (or others) still find the logic counterintuitive. The series remains under review, with no merge status reported. Key files involved are `odb/source-files.c` and the `enum odb_read_status` introduced in earlier patches.