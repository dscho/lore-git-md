This is a **test patch update** in the thread adding coverage for documented but untested refname restrictions. The author, Nikolaus Schuetz, has revised the patch to address the refactoring suggestions from Patrick Steinhardt and Junio C Hamano: the new version replaces repetitive `invalid_ref` checks with a loop over forbidden characters, including multi-byte sequences like `".."` and backslash. The loop is limited to embedded forbidden tokens, as other refname rules (e.g., leading/trailing restrictions) are already well-covered elsewhere in the test file.

The update does not change the patch’s scope or goal—it remains a purely additive test-only change targeting `t/t1402-check-ref-format.sh`. The author also clarifies that the inconsistent use of `heads/` vs `refs/heads/` in test fixtures was inherited from existing tests and does not affect the validation logic, which operates on refname components regardless of prefix. This explanation addresses Junio’s observation without requiring further changes.

The patch is now more concise and maintainable, with no loss of coverage. No objections remain, and the discussion appears to be converging on a final version. The `@` edge case and the loop over forbidden characters are the key technical details likely to be referenced in future replies. The topic is still under review, with no indication of merge status.