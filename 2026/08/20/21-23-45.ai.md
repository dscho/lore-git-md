This is a brief follow-up in the reftable stack-reload optimization series, specifically about the first patch which removes the `REFTABLE_STACK_NEW_ADDITION_RELOAD` flag. Karthik Nayak replies to Patrick Steinhardt’s surface-level review comment, confirming that libgit2 (a separate project) already passes the flag at all call sites, so the change is compatible and only requires a minimal adaptation. Karthik offers to send a pull request to libgit2 once the patch lands in Git. He also agrees to Patrick’s cosmetic suggestion to rename `reftable_stack_new_addition()` to `reftable_stack_addition_new()` for naming consistency, proposing to add a separate commit for this change if desired.

The exchange does not introduce new technical concerns or block progress; the patch’s core logic and motivation remain intact. The only open item is whether to include the function rename in this series or handle it separately. No other participants have weighed in on this specific point.