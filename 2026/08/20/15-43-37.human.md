This is a **design discussion** in the ongoing thread about the new `report` hook for `git-receive-pack`. Junio C Hamano responds to Phillip Wood’s **substantive review** questioning whether the hook’s single-script design (modeled after `proc-receive`) is still justified now that Git supports config-based hooks, which allow multiple scripts per event.

Junio pushes back on Phillip’s suggestion that server-side hooks are inherently more suited to a single-script design. He argues that the server-side context does not provide tighter control over hook execution than a desktop repository, and thus does not inherently justify the limitation. His reply frames the design question as a broader architectural choice rather than a server-specific one, suggesting the discussion should focus on whether the single-script approach is the right model for this hook regardless of where it runs.

The exchange leaves the design question open: the thread has not yet settled whether the `report` hook should remain a single-script hook or integrate with the config-based system. Junio’s response implies that the justification for the current design needs to be stronger or reconsidered, but he does not propose an alternative. This is now a key point of contention that will likely require further discussion or a revised commit message to address.