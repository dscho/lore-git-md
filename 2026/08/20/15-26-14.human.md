This is a **bugfix** for Git's HTTP authentication layer, addressing a regression where `WWW-Authenticate` headers are lost during HTTP redirects. The patch is now in its second iteration (v2) and looks ready for merging.

The issue occurs when Git follows a redirect and updates the credential struct for the new URL. The `credential_from_url()` function clears all fields, including `wwwauth_headers`, which breaks authentication when the redirect target requires a credential helper (e.g., OAuth2 Bearer tokens). The fix introduces a new helper, `credential_update_url()`, which preserves the `wwwauth_headers` field across URL updates using a lightweight `SWAP()` operation. This change is minimal and focused, touching only `credential.c` and `http.c`.

The v2 update moves the preservation logic into `credential.c`, making it reusable for future credential fields that might need similar treatment. The commit message clearly explains the problem and solution, and the test coverage is thorough: a new test in `t/t5563-simple-http-auth.sh` verifies that `wwwauth_headers` are preserved after a redirect, using a custom Apache rewrite rule.

Junio's reply here is purely procedural—he notes that the v2 email lacks threading history and provides a link to the v1 discussion. There are no technical objections or open questions, and the patch addresses a real-world issue without introducing new edge cases. The fix is well-motivated, with a link to a prior report of the same bug and a reference to Junio's earlier suggestion to centralize the preservation logic. This looks like a clean, uncontroversial bugfix that should land soon.