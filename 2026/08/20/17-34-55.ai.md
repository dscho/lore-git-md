This is a post-merge confirmation email for a bugfix targeting `git worktree add`. The patch restores the intended behavior where explicit `-b` or `-B` options take precedence over DWIM (Do What I Mean) branch name inference, fixing a regression introduced in Git 2.42.0. Junio C Hamano acknowledges the final version (v4) as clean and correctly scoped, and confirms it is queued for integration.

The change is minimal: in `builtin/worktree.c`, the DWIM logic is now guarded by `!new_branch`, ensuring that when a branch name is explicitly provided via `-b` or `-B`, it is used as-is rather than being DWIMmed to a remote-tracking branch. The test suite (`t/t2400-worktree-add.sh`) adds a single test case that verifies the corrected behavior by confirming that `git worktree add -b branch ../wt foo` fails with the expected error message when `foo` is not a valid reference. The regression was introduced by commit `128e5496b3` (worktree add: extend DWIM to infer --orphan, 2023-05-17).

This v4 iteration is a documentation-only update to the commit message, now citing the regression commit using the project-preferred `log -1 --pretty=reference` format. The code changes remain identical to v3, which was already merged to `master`. The patch preserves existing warning behavior for `HEAD` with `-b` and avoids suppressing `can_use_local_refs()` warnings, addressing feedback from earlier iterations.

The fix is uncontroversial and well-motivated, with clear test coverage and a surgical change to the DWIM logic. No further discussion or revisions are expected. Key technical details for future reference: the regression commit (`128e5496b3`), the guarded DWIM logic in `builtin/worktree.c`, and the new test case in `t/t2400-worktree-add.sh`. The on-disk format remains unchanged.