This is a **substantive review** reply in the ongoing discussion about the `git pack-objects` Trace2 byte-counting patch. Jeff King (Peff) engages with Patrick Steinhardt’s correctness concern about hash size consistency, offering a nuanced analysis of the current code’s behavior and a potential refactoring.

Peff confirms that the patch’s use of `the_repository->hash_algo->rawsz` is functionally correct because the hashfile is always created with `the_repository->hash_algo`, so the two pointers (`f->algop` and `the_repository->hash_algo`) refer to the same algorithm in practice. However, he acknowledges Patrick’s point about consistency and proposes a small refactoring that would make all three conditional blocks in `write_pack_file()` use `f->algop` instead. His diff snippet shows how to do this by passing `f->algop` to `fixup_pack_header_footer()` in the third block, aligning it with the other two.

Beyond the immediate patch, Peff raises a broader performance curiosity: the current code recomputes the packfile’s hash three times (once during writing, once during `fixup_pack_header_footer()`, and once during verification on read-back), which he calls "absurdly pessimal." He speculates that switching to the unsafe hash variant (used by hashfiles) might yield measurable speedups, but notes this is a separate discussion from the Trace2 instrumentation.

The tone is collaborative and technical, with Peff treating the hash size question as a matter of style rather than correctness. His feedback does not block the patch but suggests a minor improvement for future consideration. The author will likely incorporate Patrick’s suggestion in v3, possibly with Peff’s refactoring or a similar approach. Key files remain `builtin/pack-objects.c`; the Trace2 event `"write_pack_file/wrote_bytes"` is unaffected.