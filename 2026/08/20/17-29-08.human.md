This is a **surface-level review** in the ongoing `git pull --hard`/`--reset` thread. Kristoffer Haugsbakk questions the practical need for the proposed feature by suggesting an alternative workflow: instead of maintaining a local branch that tracks a frequently force-pushed upstream, users could simply rely on the remote-tracking branch (e.g., `origin/main`) directly. This sidesteps the entire problem the patch aims to solve, as remote-tracking branches are updated automatically by `git fetch` and do not require a local branch at all.

The comment is brief and does not engage with the technical details of the patch or the redesign discussion. It focuses on workflow preference rather than code correctness, safety, or design philosophy. While the observation is valid — remote-tracking branches *are* a viable alternative for the described use case — it does not address the broader question of whether Git should provide a more convenient way to sync to rewritten upstream history when users *do* choose to maintain a local branch. The thread remains centered on whether the feature belongs in `git pull` at all, and this reply adds a minor counterpoint to the motivation without advancing the core debate. No updated patch or further discussion has emerged from this exchange yet.