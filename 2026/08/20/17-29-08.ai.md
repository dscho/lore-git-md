This is a **surface-level review** in the `git pull --hard`/`--reset` thread. Kristoffer Haugsbakk questions the necessity of the proposed feature by suggesting an alternative workflow: instead of using a local branch to track an upstream branch that may be force-pushed or rebased, users can simply work directly with the remote-tracking branch (e.g., `origin/main`). This sidesteps the need for `git pull --reset` entirely, as remote-tracking branches are updated automatically during `git fetch` and do not require local branch management. The comment is brief and does not engage with the technical details of the proposed implementation or the broader design debate; it focuses solely on whether the use case justifies the feature. Kristoffer also reminds Artur to bottom-post, a minor etiquette note. No other participants have yet responded to this specific point, leaving the thread’s core question—whether the force-push/rebase use case is compelling enough to justify the feature—still open.