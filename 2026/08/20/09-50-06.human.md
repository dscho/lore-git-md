This is a **substantive review** of Karthik Nayak’s `report` hook patch, focusing on the hook’s design rationale and its relationship to Git’s config-based hook infrastructure. Phillip Wood questions whether the hook’s single-script design (modeled after `proc-receive`) is still appropriate now that config-based hooks exist, which allow multiple scripts to run per hook event.

Phillip argues that the commit message should explicitly address why the `report` hook isn’t integrated with the config-based system. He notes that config-based hooks aren’t inherently about parallel execution—some hooks (like `pre-commit`) always run sequentially—and suggests that the real question is whether a single-script design makes sense for server-side hooks in 2026. While he concedes that a single-script approach is probably reasonable for this use case, he wants the commit message to justify it, especially since `proc-receive` predates config-based hooks and might have been designed differently today.

The review doesn’t propose code changes but targets the patch’s documentation and conceptual framing. It’s a **design-level critique** rather than a surface-level nit, and it directly engages with the trade-offs between simplicity (single script) and flexibility (config-based hooks). The thread’s earlier consensus on exit status semantics and other implementation details isn’t challenged here, but this feedback could lead to a v2 with an expanded commit message or even a design shift if the discussion broadens. For now, it’s a focused request for clearer rationale.