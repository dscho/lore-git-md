Thread identity: bug report about `git request-pull` false warning when `<ref>` is a commit hash instead of a ref name. Originating author: Gahtan Nahdi (gahtansyarif@gmail.com). Thread type: bug report.

Core problem or goal: `git request-pull` emits a spurious warning that the specified commit does not exist in the remote repository when the `<ref>` argument is provided as a commit hash (e.g., `5a12652e`) rather than a ref name (e.g., `main`). The warning is incorrect: the commit *does* exist in the remote. The issue is reproducible with Git 2.43.0; it is unknown whether this is a regression or a long-standing bug.

Current status: reported, no prior discussion or patch. The report includes a reproduction case (command: `git request-pull 5a12652e https://github.com/gahtan-syarif/fast-chess.git 1ccb9f6e`) and confirms the commit exists in the remote. The false warning may mislead users into thinking their commits are missing.

Key technical details: command: `git request-pull`; argument: `<ref>` (positional); affected version: 2.43.0; warning message: "commit X does not exist in the remote repository"; scope: appears limited to commit-hash `<ref>` usage, ref names work correctly; remote type: only GitHub explicitly tested, others unknown; no root-cause analysis provided.

Open questions and loose ends: (1) Is this a regression or long-standing? (2) Does it affect all remote types (GitHub, GitLab, local repos) or only specific ones? (3) What is the root cause—does `git request-pull` mishandle commit-hash resolution, or does it incorrectly query the remote? (4) Is there a workaround other than using ref names?

Participant positions: none yet; thread root only.

Related work: none mentioned.