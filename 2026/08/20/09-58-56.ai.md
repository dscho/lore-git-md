This is a **substantive review** in the ongoing `git pull --hard` thread. Phillip Wood engages directly with Junio C Hamano’s principled objection to the feature, proposing a modified design that could address the maintainer’s safety concerns.

Phillip suggests reworking the feature to **error out by default if uncommitted changes exist**, rather than silently discarding them. This would make the operation safer than the existing `git fetch && git reset --hard @{u}` workflow, which does not check for uncommitted changes. He also proposes an `--autostash` option to preserve uncommitted changes, mirroring the behavior of `git rebase`. The envisioned interface would look like `git pull --reset [--discard-changes | --autostash]`, replacing the original `--hard` proposal.

The review advances the thread by shifting the discussion from *whether* the feature belongs in `git pull` to *how* it could be made safe enough to belong there. Phillip’s proposal directly targets Junio’s stated concern about accidental data loss, framing the modified design as a way to protect uncommitted changes while still providing a convenient "update working copy" operation. The thread is now at a point where the author (Artur Bieniek) must respond to this alternative design or clarify whether the original `--hard` behavior is non-negotiable. No other participants have weighed in on this specific proposal yet.