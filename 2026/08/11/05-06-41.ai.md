This is a **design discussion contribution** from Patrick Steinhardt in the ongoing thread about improving Git’s interactive patch selection workflow. Patrick responds to Junio C Hamano’s clarification of Git’s philosophical boundary—avoiding competition with external tools like lazygit—by refining his earlier proposal for a TUI (text-based user interface). He concedes that a full TUI for `git history` might be too ambitious but argues that a *targeted* TUI for hunk selection (e.g., in `git add -p`) could address core workflow gaps without overlapping significantly with existing tools.

Patrick’s position now occupies a pragmatic middle ground: Git should improve its built-in workflows with *basic* UI enhancements (e.g., hierarchical, navigable hunk selection) but avoid building a "full UI" that competes with external tools. This aligns with Junio’s stance that Git’s interactive features should be minimal and reference-like, though Patrick’s proposal still risks crossing the line into UI modernization. The key distinction he draws is between *polished* interfaces (e.g., lazygit’s triage workflow) and *functional* improvements (e.g., making `git add -p` more navigable). His framing suggests that even small UI changes—like allowing users to move between hunks without restarting the prompt-loop—could meaningfully improve usability without requiring a full ncurses dependency.

The email does not propose concrete implementation details (e.g., whether to use ncurses or a lighter alternative) but instead focuses on *where to draw the line*. Patrick’s argument hinges on the idea that Git’s current interactive tools are *unnecessarily* cumbersome for basic tasks (e.g., triaging hunks into staged/unstaged/discarded states) and that modest UI improvements could bridge the gap without duplicating external tools. This leaves open questions about how such a feature would interact with Git’s existing machinery (e.g., `add--interactive`, `apply` logic) and whether the project would accept even a minimal ncurses dependency.

For readers tracking this thread, Patrick’s reply signals a shift toward incrementalism: the discussion is no longer about whether to build a TUI but about how *small* the improvement can be while still addressing the core friction. The thread remains in the design phase, with no patches or technical sketches yet proposed. The next steps will likely involve clarifying what "basic UI improvements" would entail and whether they can be implemented without violating Junio’s "do not compete" principle.