This is the fourth patch in Patrick Steinhardt’s ODB refactoring series, and Justin Tobler’s reply raises a clarifying question about the patch’s behavior. The patch removes the `alternates_db` field from `struct object_database` and updates `odb_prepare_alternates()` to accept the alternate database path as a parameter, but Justin asks why the `GIT_ALTERNATE_OBJECT_DIRECTORIES` environment variable no longer needs to be passed when flushing caches (via `odb_prepare(o, ODB_PREPARE_FLUSH_CACHES)`). His question suggests he wants to confirm that alternates are already fully initialized during ODB setup and that subsequent calls to `odb_prepare_alternates()` only re-read the alternates *file* (not the environment variable).

The question is substantive but narrow: it does not challenge the patch’s correctness or the series’ broader goal, but seeks to verify the reasoning behind the change. The answer likely hinges on the eager initialization introduced in patch 2, which ensures all alternates (including those from `GIT_ALTERNATE_OBJECT_DIRECTORIES`) are loaded during `odb_new()`. If that is the case, the patch’s logic is sound, and Justin’s question is a request for confirmation rather than a critique. No performance or edge-case concerns are raised here, and the discussion remains focused on the mechanical cleanup.