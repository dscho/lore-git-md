This is a documentation patch in the ongoing man page synopsis-style conversion effort. Kristoffer Haugsbakk replies to Patrick Steinhardt’s review of a one-line change to `Documentation/git-format-rev.adoc`, defending the use of `[synopsis]` for a code block that contains the `<subject>` placeholder.

The patch itself is trivial—adding a `[synopsis]` AsciiDoc block to highlight `<subject>` in an example demonstrating `--stdin-mode=text` output. The discussion now centers on whether `[synopsis]` is the right markup for this context. Patrick suggested backticks as a simpler alternative, questioning whether `[synopsis]` is overkill for a non-synopsis-like code block. Kristoffer clarifies that the `[synopsis]` block here is not the traditional command synopsis but rather a styling mechanism for placeholders within code examples, consistent with the pattern established in commit `a34d1d53` (doc: convert git-show to synopsis style, 2026-02-06).

The exchange is lighthearted and pragmatic. Kristoffer playfully acknowledges that the example’s wording could be clearer—suggesting `"%s"` instead of `"%s"` to avoid ambiguity about whether `<subject>` should be quoted. This doesn’t change the technical question but underscores the patch’s low stakes: it’s a cosmetic improvement, not a functional one. The outcome will likely hinge on whether the project prefers strict adherence to the synopsis-style conversion’s established pattern or a more flexible, context-appropriate approach.

No objections to the patch’s intent exist, and the discussion remains narrowly focused on markup semantics. If the project opts for backticks, the change would be even smaller, but either way, this is a minor detail in a larger documentation cleanup effort. The patch is well-motivated and uncontroversial—worth a quick skim for anyone following the synopsis-style conversion, but not urgent.