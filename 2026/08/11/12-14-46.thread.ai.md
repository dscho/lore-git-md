Thread identity: Bugfix patch series (single patch) addressing a segmentation fault in Git's command-line option parsing when `git --shallow-file` is invoked without a required argument. Originating author: Christian Couder.

Core problem or goal: Prevent a NULL dereference crash in `git.c`'s option parsing logic for the `--shallow-file` option, which currently lacks validation that a value was provided before dereferencing `argv`. The fix adds the missing validation and aligns the logic with other similar options (`--git-dir`, `--namespace`, `-C`).

Current status: **Merged to `master`**. Junio C Hamano approved the patch as-is and confirmed it will be merged without further changes. The patch is a minimal, defensive fix with no known downsides and includes a new test in `t/t0041-usage.sh` to verify the error message and usage output when `--shallow-file` is called without a value. CI tests passed. Junio resolved the earlier procedural concerns by accepting the current test coverage (a single test in `t/t0041-usage.sh`) as sufficient for now, though he noted in a `#leftoverbits` comment that future contributors might expand the test to cover all similar "potty" options (`-C`, `--git-dir`, `--namespace`, `--config-env`) for completeness. The core fix and test placement remain uncontested.

Key technical details:
- Files touched: `git.c` (option parsing logic), `t/t0041-usage.sh` (new test).
- New behavior: `--shallow-file` now validates that a value is provided and emits a usage error if not, matching the behavior of other similar options.
- On-disk format: No changes.
- Test coverage: New test case in `t/t0041-usage.sh` verifies the error message and usage output for `--shallow-file` without a value. Junio accepted the placement as appropriate for an undocumented, internal-only option but suggested future expansion for completeness.
- Error-reporting convention: The patch follows the existing two-line pattern (`fprintf` + `usage`) used by other similar options in `git.c`.

Open questions and loose ends:
- **#leftoverbits**: Junio noted that future contributors might consider expanding the test to cover all similar "potty" options (`-C`, `--git-dir`, `--namespace`, `--config-env`) for completeness, though this is not required for the current patch.

Participant positions:
- Christian Couder (author): Substantive contribution. Implemented the fix, added test coverage, and defended the patch's structure and test placement. Argues the reordering aligns with existing conventions and that the test belongs in `t/t0041-usage.sh` for an undocumented option. Open to expanding test coverage if Junio preferred but leaned toward the current approach. **Rejected Patrick's stylistic suggestion** on consistency grounds.
- Junio C Hamano (maintainer): Substantive review. Initially raised procedural concerns about scope (mixing fix and reordering) and test completeness (not all similar options tested) but ultimately approved the patch as-is. Confirmed the existing test coverage is sufficient for now and resolved the earlier concerns. Noted future expansion as a `#leftoverbits` item.
- Patrick Steinhardt: Substantive review. Approved the core fix and test but suggested a **medium-weight** stylistic tweak (single-line `usage()` call). The suggestion was **rejected by the author**; no further response from Patrick.