Thread identity: Bugfix patch series (single patch) addressing a segmentation fault in Git's command-line option parsing when `git --shallow-file` is invoked without a required argument. Originating author: Christian Couder.

Core problem or goal: Prevent a NULL dereference crash in `git.c`'s option parsing logic for the `--shallow-file` option, which currently lacks validation that a value was provided before dereferencing `argv`. The fix adds the missing validation and aligns the logic with other similar options (`--git-dir`, `--namespace`, `-C`).

Current status: Under review. The patch is a minimal, defensive fix with no known downsides and includes a new test in `t/t0041-usage.sh` to verify the error message and usage output when `--shallow-file` is called without a value. CI tests pass. The discussion has shifted to procedural concerns: (1) whether to split the patch to separate the defensive fix from the stylistic reordering (Junio's concern), and (2) whether to expand the test coverage to include all similar options (`-C`, `--git-dir`, `--namespace`, `--config-env`) or leave it as-is. Christian Couder argues that the reordering aligns with existing conventions and that the test belongs in `t/t0041-usage.sh` because `--shallow-file` is an undocumented, internal-only option. He notes that similar options also lack dedicated tests but is open to expanding coverage if Junio prefers. The core fix and test coverage remain uncontested.

Key technical details:
- Files touched: `git.c` (option parsing logic), `t/t0041-usage.sh` (new test).
- New behavior: `--shallow-file` now validates that a value is provided and emits a usage error if not, matching the behavior of other similar options.
- On-disk format: No changes.
- Test coverage: New test case in `t/t0041-usage.sh` verifies the error message and usage output for `--shallow-file` without a value. Junio suggests expanding the test to cover all similar options or moving it to a shared location; Christian argues the current placement is appropriate for an undocumented option.
- Error-reporting convention: The patch follows the existing two-line pattern (`fprintf` + `usage`) used by other similar options in `git.c`; this convention is explicitly defended by the author.

Open questions and loose ends:
- Whether to split the patch to separate the defensive fix from the stylistic reordering (Junio's preference, but not a blocker).
- Whether to expand the test to cover all similar options or leave it as-is (Junio's concern; Christian defers to Junio's judgment).
- No further discussion on Patrick Steinhardt's stylistic suggestion, which was rejected by the author on consistency grounds.

Participant positions:
- Christian Couder (author): Substantive contribution. Implemented the fix, added test coverage, and defended the patch's structure and test placement. Argues the reordering aligns with existing conventions and that the test belongs in `t/t0041-usage.sh` for an undocumented option. Open to expanding test coverage if Junio prefers but leans toward the current approach. **Rejected Patrick's stylistic suggestion** on consistency grounds.
- Junio C Hamano (maintainer): Substantive review. Acknowledges the patch is correct and low-risk but raises procedural concerns about scope (mixing fix and reordering) and test completeness (not all similar options are tested). Prefers splitting the patch or expanding the test but does not block merging.
- Patrick Steinhardt: Substantive review. Approved the core fix and test but suggested a **medium-weight** stylistic tweak (single-line `usage()` call). The suggestion was **rejected by the author**; no further response from Patrick.