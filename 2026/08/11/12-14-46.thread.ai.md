Thread identity: Bugfix patch series (single patch) addressing a segmentation fault in Git's command-line option parsing when `git --shallow-file` is invoked without a required argument. Originating author: Christian Couder.

Core problem or goal: Prevent a NULL dereference crash in `git.c`'s option parsing logic for the `--shallow-file` option, which currently lacks validation that a value was provided before dereferencing `argv`. The fix adds the missing validation and aligns the logic with other similar options (`--git-dir`, `--namespace`, `-C`).

Current status: Under review. The patch is a minimal, defensive fix with no known downsides. It includes a new test in `t/t0041-usage.sh` to verify the error message and usage output when `--shallow-file` is called without a value. CI tests pass. The commit message confirms all in-tree callers already provide a value (including empty strings), so this is purely a user-facing robustness improvement.

Key technical details:
- Files touched: `git.c` (option parsing logic), `t/t0041-usage.sh` (new test).
- New behavior: `--shallow-file` now validates that a value is provided and emits a usage error if not, matching the behavior of other similar options.
- On-disk format: No changes.
- Test coverage: New test case in `t/t0041-usage.sh` verifies the error message and usage output for the `--shallow-file` option without a value.

Open questions and loose ends: None. The patch is self-contained and addresses the issue fully. Christian Couder notes that a broader refactoring of option handling is planned but not part of this fix.

Participant positions:
- Christian Couder (author): Substantive contribution. Identified the issue, implemented the fix, added test coverage, and confirmed no downsides. The patch is ready for review.

Related work: The fix is part of ongoing work to modernize `git fast-import`, but the patch itself is standalone. No dependencies or external references.