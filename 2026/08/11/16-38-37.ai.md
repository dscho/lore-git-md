This is a **substantive review reply** in the ongoing `git worktree add` ambiguous remote branch name thread. Ben Knoble clarifies his earlier feedback, walking back any implication that the warning itself was problematic. His concern was never about the warning's content or necessity, but about the *mechanism* of signaling ambiguity: a warning is preferable to a `die()` because it avoids breaking existing workflows. He now explicitly endorses signaling ambiguity (via warning or advice) when multiple remote branches match, provided there is no documented policy for resolving it deterministically. This aligns with the thread's consensus that users should be made aware of ambiguity, even if the patch's current implementation omits the warning from `dwim_branch()` to avoid noise when there are no matches. Ben's reply removes a potential point of contention by confirming the warning's role as a non-disruptive signal, not a workflow blocker. The open question remains whether the final version will include *any* warning for multiple matches, but this reply narrows the debate to a usability trade-off (discoverability vs. noise) rather than a technical one.