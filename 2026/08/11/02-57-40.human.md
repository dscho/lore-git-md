This is a brief, positive review of a now-merged bugfix patch. Jacob Keller adds his `Reviewed-by` trailer to Éric NICOLAS’s v2 patch, confirming he is satisfied with the fix for the submodule remote-matching regression. The regression, introduced in commit ca62f524c1 (2025), caused `git submodule update --remote` to fail when URL aliases (`url.<base>.insteadOf`) were in use, because the code compared the unexpanded alias from `.gitmodules` against the resolved URL reported by the submodule’s remotes.

Jacob’s review is surface-level: he does not engage with the code’s logic or edge cases, but he does signal that he encountered the bug himself and appreciates the fix. The patch itself is already merged to `master`, so this email is essentially a final sign-off rather than an active review. No further discussion or changes are expected.