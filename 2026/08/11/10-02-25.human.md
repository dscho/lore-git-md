This is the second patch in Patrick Steinhardt’s eight-patch v2 series refactoring Git’s object database (ODB) streaming API. The patch removes the `is_finished` field from `struct odb_write_stream` and adjusts the loop logic in `builtin/unpack-objects.c` to infer stream completion from zero-byte returns, aligning the write stream’s behavior with the read stream’s design. Karthik Nayak’s review here is **surface-level**, focusing on confirming the patch’s intent and the mechanics of the loop restructuring rather than engaging with edge cases or broader API implications.

The change touches seven files, replacing checks for `stream->is_finished` with loops that break when `odb_write_stream_read()` returns zero. The most involved update is in `builtin/unpack-objects.c`, where the `feed_input_zstream()` function is rewritten to ensure at least one byte is produced before yielding, addressing an edge case in zlib decompression where output might not be immediately available. Karthik’s comment notes the use of global variables and the loop’s role in ensuring progress, but does not raise any concerns or suggest improvements.

This patch is purely preparatory, with no intended behavior changes. It eliminates redundancy by making the write stream’s completion semantics consistent with the read stream’s existing design, a key step toward the unified `odb_stream` structure introduced later in the series. The patch has already received substantive review from Justin Tobler, who confirmed the loop restructuring aligns the write stream’s behavior with the read stream’s design. Karthik’s feedback here is limited to validating the structural intent and does not address any open questions or correctness issues.