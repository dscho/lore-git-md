This is the **reply in the `git fast-import` libification series** where Christian Couder addresses Elijah Newren’s review of the behavior-changing patch (11/12). The email confirms that all three behavioral changes identified in the review have been documented in the v3 commit message and in the `git-fast-import` man page. Specifically:

- The commit message now explicitly lists the acceptance of space-separated values (`--depth 5`) and bare/trailing `--` as intentional changes.
- The `--allow-unsafe-features` inconsistency is documented in the man page with concrete examples showing which command-line forms work and which do not. A `NEEDSWORK` comment in the code flags the issue for future resolution, proposing to teach the early scan about value-taking options.

The response is **substantive**: it directly addresses the technical concern raised, adds user-facing documentation, and leaves a clear path for follow-up work. No new behavioral changes are introduced; the patch remains functionally identical to v2 except for the added documentation. The tone is collaborative, and the author’s preference to defer a general fix (rather than a targeted one) is stated plainly. The series is now ready for broader review or integration, with the `--allow-unsafe-features` inconsistency documented as a known limitation. Files touched: `Documentation/git-fast-import.adoc` and `builtin/fast-import.c` (comments only). Key symbols: `--allow-unsafe-features`, `--depth`, `parse_options()`.