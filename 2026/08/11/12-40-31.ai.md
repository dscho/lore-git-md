This is a **security-adjacent configuration option** proposed by Ron Nazarov and critically reviewed by Patrick Steinhardt. The patch introduces `http.sslVerifyHost`, a new Git config option that allows users to disable hostname verification in TLS connections *without* disabling certificate verification entirely. The stated use case is decentralized setups where self-signed certificates lack a valid Common Name (CN), but the user still wants to pin a specific CA certificate via `http.sslCAInfo`.

Patrick’s review is **substantive and skeptical**. He argues that the new option oversells its security benefits: while it may protect against passive eavesdropping on unsecured networks (where an attacker can read but not modify traffic), it remains vulnerable to active man-in-the-middle (MITM) attacks. He suggests that the commit message and documentation should explicitly acknowledge this limitation rather than implying the option is a secure middle ground between full verification and no verification at all.

The patch touches the **HTTP/HTTPS transport layer** and would affect users who rely on self-signed certificates in non-traditional deployments. The key technical detail is the interaction with `http.sslCAInfo`—the option is designed to work in tandem with certificate pinning, not as a standalone feature. Patrick’s concern centers on whether the trade-off (partial security vs. user convenience) is justified, and whether the documentation adequately warns users about the remaining risks.

This is a **thread root** with no prior discussion provided. The patch itself has not been included in the input, so the exact implementation details (e.g., default value, backward compatibility) are unknown. The review raises a **likely point of contention**: whether Git should expose configuration knobs that offer *some* security but could mislead users into believing they are fully protected. Future discussion may focus on refining the documentation, adjusting the default behavior, or rejecting the feature outright if the security model is deemed too weak.