Thread identity: security-adjacent configuration option introducing `http.sslVerifyHost`, a Git config key that allows disabling TLS hostname verification while retaining certificate verification via `http.sslCAInfo`. Originating author: Ron Nazarov; thread type: RFC patch series (version unknown, no patch text provided).

Core problem or goal: enable Git users in decentralized environments (e.g., self-signed certificates without valid Common Names) to pin a specific CA certificate without disabling TLS verification entirely. The proposed knob is positioned as a middle ground between full verification and no verification, but Patrick Steinhardt’s review challenges whether the security trade-off is justified or adequately communicated.

Current status: under initial review; no prior versions or merge status recorded. The patch touches the HTTP/HTTPS transport layer, specifically the TLS verification logic. Key technical detail: `http.sslVerifyHost` is designed to work in tandem with `http.sslCAInfo` (certificate pinning), not as a standalone feature. No new symbols, on-disk format changes, or test coverage details are available from the provided summary.

Open questions and loose ends: (1) whether the security model is sufficiently robust to justify inclusion, given that it remains vulnerable to active MITM attacks; (2) whether the documentation and commit message adequately warn users about the limitations; (3) what the default value should be; (4) whether backward compatibility is preserved (unknown due to missing patch text).

Participant positions: Patrick Steinhardt (reviewer) – skeptical, argues the option oversells its security benefits and should explicitly acknowledge its vulnerability to active MITM; no position from Ron Nazarov (author) beyond the initial proposal. No other participants recorded. No low-weight messages.