Thread identity: security-adjacent RFC patch series introducing `http.sslVerifyHost`, a Git config key that allows disabling TLS hostname verification while retaining certificate verification via `http.sslCAInfo`. Originating author: Ron Nazarov; thread type: RFC patch series (version unknown, no patch text provided).

Core problem or goal: enable Git users in decentralized environments (e.g., self-signed certificates without valid Common Names) to pin a specific CA certificate without disabling TLS verification entirely. The proposed knob is positioned as a middle ground between full verification and no verification, but reviewers challenge whether the security trade-off is justified or adequately communicated. The intended secure usage pattern is now clarified: `http.sslVerifyHost` must be paired with certificate pinning (`http.sslCAInfo` or `http.sslCAPath`) to replicate SSH’s `known_hosts` security model, preventing MITM attacks as long as the private key remains secret.

Current status: under active review; no prior versions or merge status recorded. The patch targets the HTTP/HTTPS transport layer, specifically the TLS verification logic. Key technical detail: `http.sslVerifyHost` is designed to work in tandem with `http.sslCAInfo` (certificate pinning), not as a standalone feature. No new symbols, on-disk format changes, or test coverage details are available. The thread remains in the "should this exist?" phase, but Patrick Steinhardt’s conditional acceptance suggests a path forward if documentation is strengthened.

Open questions and loose ends: (1) whether the documentation and commit message adequately warn users about the limitations and emphasize the mandatory pairing with certificate pinning (Patrick’s new concern); (2) whether the security model is sufficiently robust to justify inclusion, given that standalone use remains vulnerable to active MITM attacks (ARP spoofing, as noted by brian m. carlson); (3) what the default value should be; (4) whether backward compatibility is preserved (unknown due to missing patch text); (5) whether modern alternatives (ACME, DANE) obviate the need for this option; (6) whether the original patch’s absence from the mailing list archive will block further progress.

Participant positions:
- **Patrick Steinhardt** (reviewer) – initially skeptical, now conditionally accepting if the documentation clearly explains the secure usage pattern (certificate pinning required). Emphasizes that the option is only meaningful when paired with `http.sslCAInfo`/`http.sslCAPath` and should not be presented as a standalone security feature.
- **brian m. carlson** (reviewer) – strongly opposes the feature, asserting it provides no meaningful security, creates a false sense of security, and is unnecessary given modern alternatives (ACME, DANE). Highlights ARP spoofing as a realistic MITM vector on unsecured networks and notes that CNs are obsolete in TLS verification (replaced by subjectAltName).
- **Ron Nazarov** (author) – initial proposal; no recorded response to critiques or Patrick’s clarification.

No other participants or low-weight messages. Key technical context for follow-up: ARP spoofing, subjectAltName, ACME, DANE, and SSH’s `known_hosts` model. The thread’s focus has shifted from "how to implement" to "how to document and justify the security model." Likely next step: a v2 patch with updated documentation.