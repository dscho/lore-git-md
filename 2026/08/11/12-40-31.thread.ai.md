Thread identity: security-adjacent RFC patch series introducing `http.sslVerifyHost`, a Git config key that allows disabling TLS hostname verification while retaining certificate verification via `http.sslCAInfo`. Originating author: Ron Nazarov; thread type: RFC patch series (version unknown, no patch text provided).

Core problem or goal: enable Git users in decentralized environments (e.g., self-signed certificates without valid Common Names) to pin a specific CA certificate without disabling TLS verification entirely. The proposed knob is positioned as a middle ground between full verification and no verification, but reviewers challenge whether the security trade-off is justified or adequately communicated.

Current status: under active review; no prior versions or merge status recorded. The patch targets the HTTP/HTTPS transport layer, specifically the TLS verification logic. Key technical detail: `http.sslVerifyHost` is designed to work in tandem with `http.sslCAInfo` (certificate pinning), not as a standalone feature. No new symbols, on-disk format changes, or test coverage details are available.

Open questions and loose ends: (1) whether the security model is sufficiently robust to justify inclusion, given that it remains vulnerable to active MITM attacks (now emphasized by multiple reviewers); (2) whether the documentation and commit message adequately warn users about the limitations; (3) what the default value should be; (4) whether backward compatibility is preserved (unknown due to missing patch text); (5) whether modern alternatives (ACME, DANE) obviate the need for this option.

Participant positions:
- **Patrick Steinhardt** (reviewer) – skeptical, argues the option oversells its security benefits and should explicitly acknowledge its vulnerability to active MITM.
- **brian m. carlson** (reviewer) – strongly opposes the feature, asserting it provides no meaningful security, creates a false sense of security, and is unnecessary given modern alternatives (ACME, DANE). Highlights ARP spoofing as a realistic MITM vector on unsecured networks and notes that CNs are obsolete in TLS verification (replaced by subjectAltName).
- **Ron Nazarov** (author) – initial proposal; no recorded response to critiques.

No other participants or low-weight messages. The thread has shifted from discussing *how* to implement the option to whether it should exist at all. Key technical context for follow-up: ARP spoofing, subjectAltName, ACME, and DANE.