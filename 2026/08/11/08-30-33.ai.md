This is a brief follow-up in Christian Couder’s `git fast-import` refactoring series. The email resolves the only open point from Elijah Newren’s surface-level review of patch 7/12: the overly long function signatures introduced when adding the `struct fast_import_state *state` parameter. Christian confirms that in v3 he has reformatted all modified lines to Git’s 80-character line limit, using the multi-line style Elijah suggested (e.g., `note_change_n` split across four lines). No technical concerns remain; the patch remains a purely mechanical step toward encapsulating globals in `struct fast_import_state` as part of the libification effort. The change is confined to `builtin/fast-import.c` and introduces no new symbols or behavior.