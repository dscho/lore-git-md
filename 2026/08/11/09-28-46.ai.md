This is the fourth patch in the v8 series optimizing `paint_down_to_common()` for one-sided histories. It adds a new regression test (`t6099-merge-base-side-exhaustion.sh`) to verify that the side-exhaustion optimization correctly handles cases where multiple merge-base candidates exist and one is an ancestor of another. The test exercises the interaction between the early termination logic in `paint_down_to_common()` and the `remove_redundant()` safety net in `get_merge_bases_many_0()`.

The test constructs a diamond-shaped commit graph where two tips (`A` and `X`) share two merge-base candidates (`B` and `C`), with `B` being an ancestor of `C`. The side-exhaustion optimization may terminate the walk before `STALE` propagation removes the redundant candidate (`C`), but `remove_redundant()` ensures only the shallowest candidate (`B`) is returned. The test validates this behavior for both `--all` and single-result modes, and includes a fallback case without a commit-graph (where generation numbers are `INFINITY` and the optimization does not fire).

The patch is a targeted test addition with no code changes. It directly addresses a prior review concern about edge-case coverage in the side-exhaustion optimization, ensuring the safety net works as intended. The test topology is clearly documented in the script header, and the assertions are deterministic. This patch is procedurally ready and non-controversial, with no open review feedback. The series as a whole remains queued in Junio’s tree and is technically complete.