This is the second patch in Patrick Steinhardt’s four-patch series refactoring the object database (ODB) subsystem to remove lazy-loading of alternate object databases. The patch itself is a refactoring that centralizes alternate ODB initialization in `odb_new()` and `odb_prepare()`, eliminating 13 scattered calls to `odb_prepare_alternates()` across 10 files.

Justin Tobler’s review is surface-level, focusing on the clarity of the commit message and the mechanical correctness of the changes. He flags a minor typo in the commit message ("where read" → "where we read") and confirms that the patch’s direction—eagerly initializing alternates—is sound and simplifies the code. His comments are limited to inline observations about the diff (e.g., noting where `odb_prepare_alternates()` is now called) and do not engage with the patch’s technical rationale, edge cases, or performance implications. The review concludes with a brief "looks good" without substantive critique or testing.

The patch remains uncontroversial and mechanically clean, but the lack of deeper scrutiny—particularly around performance trade-offs for commands like `git fsck` that previously avoided loading alternates—leaves open questions for broader review. Justin’s feedback does not address these concerns, which were noted in the thread summary as potential discussion points. The series as a whole is part of Patrick’s ongoing ODB abstraction effort, and this patch is a straightforward step toward that goal.