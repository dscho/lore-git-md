This thread introduces a performance feature for Linux: using copy-on-write reflinks (via the `FICLONE` ioctl) to make local clones and file copies more space-efficient. The initial patch proposed trying reflinks before hardlinks, but Junio C Hamano questioned that order, arguing that hardlinks are simpler and universally supported for Git’s immutable object files. Vlad Petric has now revised the patch to try hardlinks first, then reflinks, and finally byte-for-byte copies—aligning with Junio’s preference for simpler mechanisms when possible.

The patch remains Linux-specific and preserves source timestamps for compatibility with Git’s prune and gc commands. Testing infrastructure is unchanged, using an LD_PRELOAD helper to simulate reflink behavior and a new test script (`t0094-reflink.sh`) to verify functionality across scenarios like local clones, `--no-hardlinks`, and fallback cases. Vlad reports the patch passes all tests on ZFS, ext4, and tmpfs, and the full test suite is clean. With the design question resolved, the patch is now ready for review focused on correctness and edge cases. No merge status has been reported yet.