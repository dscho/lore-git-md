This is a **substantive follow-up discussion** in the `git maintenance` geometric repack bugfix thread, focusing on a potential edge case in the first patch of Taylor Blau’s two-patch series. Patrick Steinhardt elaborates on his earlier concern: the current fix (which evaluates promisor and non-promisor pack progressions independently) might inadvertently prevent all-into-one repacks entirely, since it’s unlikely both progressions will simultaneously meet the threshold for a single-pack rollup. This could lead to unreachable objects never being pruned, as the fallback to all-into-one repacks (which handle pruning) would rarely trigger.

Patrick suggests a more nuanced approach: triggering an all-into-one repack when the objects involved exceed a certain percentage of the repository size, rather than relying solely on geometric progression thresholds. This would address the pruning gap while avoiding unnecessary full repacks. The discussion remains conceptual—no code changes are proposed yet—but it highlights a trade-off between geometric efficiency and garbage collection. The outcome could shape whether the patch needs refinement or if the current behavior is deemed acceptable for now. Key technical context: the edge case revolves around `geometry.split`, `geometry.pack_nr`, and the interaction between promisor and non-promisor packs in the repack decision logic.