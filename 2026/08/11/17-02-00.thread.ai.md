Thread identity
Subject: [PATCH v4] http: add http.sslVerifyStatus to check OCSP staples
Series version: standalone patch (v4)
Originating author: graysongordon-gl
Thread type: security feature patch

Core problem or goal
Git’s HTTPS certificate verification omits OCSP staple validation, leaving it vulnerable to accepting revoked certificates. The patch adds a boolean `http.sslVerifyStatus` option (default `false`) that enables staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, causing connections to fail if the server does not provide a staple. The design is fail-closed for security but defaults to `false` to avoid breaking remotes that do not support stapling. The feature is motivated by government and government-adjacent customers (e.g., US Department of Defense PKI) who mandate OCSP stapling and encounter Git servers with stapled responses; OpenSSL-linked Git binaries currently ignore staples, creating a security gap where revoked certificates are accepted.

Current status
**Accepted by the maintainer and queued for integration, but substantive review feedback remains unresolved.** Junio C Hamano confirmed the v4 iteration is technically complete, resolving all procedural feedback (test integration, Meson build updates, and prerequisite robustness). The patch will replace the prior version in Junio’s queue and is expected to graduate to `next` and eventually `master`. However, Patrick Steinhardt’s latest review raises **actionable improvements** in documentation accuracy, error message clarity, and test coverage that must be addressed before the patch can be considered fully reviewed. The substantive discussion about the feature’s real-world impact has concluded with the author providing concrete evidence (government/FIPS deployments, threat model of stolen private key impersonation) that addressed Patrick’s initial challenge. The long-term question of whether to flip the default to `true` or warn users about ignored staples remains open but does not block integration.

Key technical details
- New config key: `http.sslVerifyStatus` (boolean, default `false`)
- Per-URL override: `http.https://example.com/.sslVerifyStatus`
- Libcurl requirement: `CURLOPT_SSL_VERIFYSTATUS` (libcurl ≥ 7.41.0, already below Git’s minimum)
- Files touched: `http.c`, `Documentation/config/http.adoc`, `t/t5551-http-fetch-smart.sh`
- Test integration: four new tests moved into `t5551-http-fetch-smart.sh` (HTTPS pass via `t5559`), eliminating the need for a new test script
- Prerequisite robustness: `SSL_VERIFYSTATUS` lazy prerequisite now uses `test_might_fail` to handle expected failures when stapling is enabled but unavailable
- Behavior: fail-closed (connection fails if staple missing)
- Backend differences: **Outdated documentation**—GnuTLS and OpenSSL now behave identically in libcurl ≥ 8.10 (GnuTLS ≥ 3.8), so backend-specific caveats no longer apply. Other backends (e.g., mbedTLS) may behave differently but are not documented.
- Symbols: none renamed; new config key only
- Deployment context: GitLab’s Helm charts default to OpenSSL on UBI-based images (FIPS compliance requirement), leaving government customers exposed without this feature
- Error handling: **Proposed improvement**—include libcurl error string (`curl_easy_strerror()`) in failure messages to distinguish missing features (`CURLE_NOT_BUILT_IN`) from other errors.

Open questions and loose ends
- **Documentation accuracy**: Update `Documentation/config/http.adoc` to reflect that GnuTLS and OpenSSL now behave identically in recent libcurl versions, and consider a bulleted list for backend-specific notes to accommodate future additions (e.g., mbedTLS).
- **Error message clarity**: Improve failure messages to include the specific libcurl error string when `CURLOPT_SSL_VERIFYSTATUS` fails.
- **Test coverage gap**: Add a test for the positive case—where a server provides a valid OCSP staple and Git successfully validates it. The current tests only cover the fail-closed path and per-URL overrides.
- Whether the default (`false`) should eventually change to `true` or if users should be warned when staples are ignored.
- Whether the feature’s maintenance burden (given OCSP stapling’s declining relevance) is justified by the specific use case of government customers.

Participant positions
- **Author (graysongordon-gl)**: Introduced the feature with clear rationale, design trade-offs, and thorough testing. Addressed Junio’s v1 feedback in v2 by removing the in-code comment and preserving the rationale in the commit message. In response to Patrick Steinhardt’s initial challenge, provided concrete motivation: government customers (e.g., US DoD PKI) mandate OCSP stapling, and OpenSSL-linked Git binaries (common in FIPS-compliant deployments) currently ignore staples, creating a security gap. Framed the threat model as a low-to-medium severity CVE risk (stolen private key impersonation enabling data exfiltration). Position is now well-supported with evidence. In v4, resolved all procedural feedback (test integration, prerequisite robustness) while keeping the core change unchanged. **Expected to address Patrick’s latest review** on documentation, error messages, and test coverage.
- **Junio C Hamano**: Surface-level review on v1 (requested comment removal) and v2 (test numbering conflict). Provided procedural follow-up in v3 to update `t/meson.build` for Meson build integration and to fix the `SSL_VERIFYSTATUS` lazy prerequisite in the test script. **Accepted the v4 iteration**, confirming the patch is technically complete and ready for integration. No objections to the feature itself; feedback was purely procedural.
- **Patrick Steinhardt**: **Substantive review** questioning the patch’s purpose in v3, initially arguing the feature does not resolve backend differences (OpenSSL vs. GnuTLS), provides no security benefit by default, and may not be worth maintaining given OCSP stapling’s declining relevance. Requested a clear use case or audience to justify the change. The author’s response addressed the use case concern, but the maintenance burden question remains open. In the latest review (v4), Patrick **raises actionable improvements**:
  - **Documentation**: Update `http.adoc` to reflect that GnuTLS and OpenSSL now behave identically in recent libcurl versions, and consider a bulleted list for backend-specific notes.
  - **Error messages**: Improve failure messages to include the libcurl error string (`curl_easy_strerror()`) for better diagnostics.
  - **Test coverage**: Add a test for the positive case (valid OCSP staple validation) to verify the feature works as intended. Position is **not a rejection** but a request for continued refinement before graduation to `master`.