Thread identity
Subject: [PATCH v4] http: add http.sslVerifyStatus to check OCSP staples
Series version: standalone patch (v5 posted)
Originating author: graysongordon-gl
Thread type: security feature patch

Core problem or goal
Git’s HTTPS certificate verification omits OCSP staple validation, leaving it vulnerable to accepting revoked certificates. The patch adds a boolean `http.sslVerifyStatus` option (default `false`) that enables staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, causing connections to fail if the server does not provide a staple. The design is fail-closed for security but defaults to `false` to avoid breaking remotes that do not support stapling. The feature is motivated by government and government-adjacent customers (e.g., US Department of Defense PKI) who mandate OCSP stapling and encounter Git servers with stapled responses; OpenSSL-linked Git binaries currently ignore staples, creating a security gap where revoked certificates are accepted.

Current status
**Queued for integration and expected to graduate to `next` and eventually `master`.** Junio C Hamano previously accepted the v4 iteration as technically complete, and the v5 iteration resolves all remaining review feedback: commit message clarity, documentation accuracy, and error message improvements. Patrick Steinhardt’s requested changes—documentation updates, error message clarity, and test coverage—have been addressed, with the exception of the positive test case (valid OCSP staple validation), which the author prototyped but deferred to maintainer discretion. The patch is now **ready for `next`** and no longer has unresolved substantive feedback.

Key technical details
- New config key: `http.sslVerifyStatus` (boolean, default `false`)
- Per-URL override: `http.https://example.com/.sslVerifyStatus`
- Libcurl requirement: `CURLOPT_SSL_VERIFYSTATUS` (libcurl ≥ 7.41.0, already below Git’s minimum)
- Files touched: `http.c`, `Documentation/config/http.adoc`, `t/t5551-http-fetch-smart.sh`
- Test integration: four new tests in `t5551-http-fetch-smart.sh` verify fail-closed behavior, per-URL scoping, and edge cases; the `SSL_VERIFYSTATUS` lazy prerequisite uses `test_might_fail` to handle expected failures gracefully
- Behavior: fail-closed (connection fails if staple missing)
- Backend differences: GnuTLS and OpenSSL now behave identically in libcurl ≥ 8.10 (GnuTLS ≥ 3.8), obsoleting prior backend-specific caveats; other backends (e.g., mbedTLS) may behave differently but are not documented
- Error handling: failure messages now include the libcurl error string (`curl_easy_strerror()`) to distinguish missing features (`CURLE_NOT_BUILT_IN`) from other errors
- Symbols: none renamed; new config key only
- Deployment context: GitLab’s Helm charts default to OpenSSL on UBI-based images (FIPS compliance requirement), leaving government customers exposed without this feature

Open questions and loose ends
- Whether to add a test for the positive case (valid OCSP staple validation). The author has the infrastructure (nginx in Docker) but defers to Junio and Patrick on inclusion due to complexity.
- Whether the default (`false`) should eventually change to `true` or if users should be warned when staples are ignored.
- Whether the feature’s maintenance burden (given OCSP stapling’s declining relevance) is justified by the specific use case of government customers.

Participant positions
- **Author (graysongordon-gl)**: Introduced the feature with clear rationale, design trade-offs, and thorough testing. Addressed Junio’s procedural feedback (test integration, Meson build updates, prerequisite robustness) and Patrick Steinhardt’s substantive review (documentation accuracy, error message clarity, test coverage). Provided concrete motivation: government customers (e.g., US DoD PKI) mandate OCSP stapling, and OpenSSL-linked Git binaries (common in FIPS-compliant deployments) currently ignore staples, creating a security gap. Framed the threat model as stolen private key impersonation enabling data exfiltration. **Resolved all review feedback in v5**, including commit message clarity, documentation updates, and error message improvements. The positive test case may be deferred.
- **Junio C Hamano**: Surface-level review on v1 (requested comment removal) and v2 (test numbering conflict). Provided procedural follow-up in v3 (Meson build integration, `SSL_VERIFYSTATUS` prerequisite robustness). **Accepted the v4 iteration as technically complete**, but requested a clearer commit message to address misleading phrasing ("Leaving the default to libcurl is not an option"). The v5 iteration resolves this concern. No objections to the feature itself; feedback was purely on wording and procedural completeness.
- **Patrick Steinhardt**: **Substantive review** in v3 questioning the patch’s purpose, initially arguing the feature does not resolve backend differences, provides no security benefit by default, and may not be worth maintaining. The author’s response addressed the use case concern, but the maintenance burden question remains open. In v4, requested actionable improvements: documentation updates (backend parity in recent libcurl versions), error message clarity (libcurl error strings), and test coverage (positive case). **All requested changes except the positive test case are resolved in v5**. Position is **not a rejection** but a request for refinement before graduation to `master`.