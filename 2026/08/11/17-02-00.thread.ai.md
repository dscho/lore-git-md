Thread identity
Subject: [PATCH v4] http: add http.sslVerifyStatus to check OCSP staples
Series version: standalone patch (v4)
Originating author: graysongordon-gl
Thread type: security feature patch

Core problem or goal
Git’s HTTPS certificate verification omits OCSP staple validation, leaving it vulnerable to accepting revoked certificates. The patch adds a boolean `http.sslVerifyStatus` option (default `false`) that enables staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, causing connections to fail if the server does not provide a staple. The design is fail-closed for security but defaults to `false` to avoid breaking remotes that do not support stapling. The feature is motivated by government and government-adjacent customers (e.g., US Department of Defense PKI) who mandate OCSP stapling and encounter Git servers with stapled responses; OpenSSL-linked Git binaries currently ignore staples, creating a security gap where revoked certificates are accepted.

Current status
Under review. This is the fourth version of the patch, addressing all procedural feedback from Junio C Hamano (test numbering conflict, Meson build integration, and test prerequisite robustness). The patch is now technically complete and ready for final review. The substantive discussion has shifted from theoretical concerns to real-world impact following the author’s detailed response to Patrick Steinhardt’s challenge, which provided concrete use cases and a threat model (stolen private key impersonation enabling data exfiltration, framed as a low-to-medium severity CVE risk). The patch’s practical value is now supported by evidence, but the long-term maintenance burden (given OCSP stapling’s decline) and whether to eventually flip the default to `true` or warn users about ignored staples remain open.

Key technical details
- New config key: `http.sslVerifyStatus` (boolean, default `false`)
- Per-URL override: `http.https://example.com/.sslVerifyStatus`
- Libcurl requirement: `CURLOPT_SSL_VERIFYSTATUS` (libcurl ≥ 7.41.0, already below Git’s minimum)
- Files touched: `http.c`, `Documentation/config/http.adoc`, `t/t5551-http-fetch-smart.sh`
- Test integration: four new tests moved into `t5551-http-fetch-smart.sh` (HTTPS pass via `t5559`), eliminating the need for a new test script
- Prerequisite robustness: `SSL_VERIFYSTATUS` lazy prerequisite now uses `test_might_fail` to handle expected failures when stapling is enabled but unavailable
- Behavior: fail-closed (connection fails if staple missing)
- Backend differences: OpenSSL ignores staples unless explicitly enabled; GnuTLS checks them by default
- Symbols: none renamed; new config key only
- Deployment context: GitLab’s Helm charts default to OpenSSL on UBI-based images (FIPS compliance requirement), leaving government customers exposed without this feature

Open questions and loose ends
- Whether the default (`false`) should eventually change to `true` or if users should be warned when staples are ignored.
- Whether the feature’s maintenance burden (given OCSP stapling’s declining relevance) is justified by the specific use case of government customers.

Participant positions
- Author (graysongordon-gl): introduced the feature with clear rationale, design trade-offs, and thorough testing. Addressed Junio’s v1 feedback in v2 by removing the in-code comment and preserving the rationale in the commit message. In response to Patrick Steinhardt’s challenge, provided concrete motivation: government customers (e.g., US DoD PKI) mandate OCSP stapling, and OpenSSL-linked Git binaries (common in FIPS-compliant deployments) currently ignore staples, creating a security gap. Framed the threat model as a low-to-medium severity CVE risk (stolen private key impersonation enabling data exfiltration). Position is now well-supported with evidence. In v4, resolved all procedural feedback (test integration, prerequisite robustness) while keeping the core change unchanged.
- Junio C Hamano: surface-level review on v1 (requested comment removal) and v2 (test numbering conflict). Provided procedural follow-up in v3 to update `t/meson.build` for Meson build integration and to fix the `SSL_VERIFYSTATUS` lazy prerequisite in the test script. No objections to the feature itself; feedback is purely procedural. The patch is now technically complete from his perspective.
- Patrick Steinhardt: substantive review questioning the patch’s purpose. Initially argued the feature does not resolve backend differences (OpenSSL vs. GnuTLS), provides no security benefit by default, and may not be worth maintaining given OCSP stapling’s declining relevance. Requested a clear use case or audience to justify the change. The author’s response has addressed the use case concern, but the maintenance burden question remains open. Position is not a rejection but a request for continued justification.