This is a bugfix patch for the ongoing `git worktree add` out-of-bounds read series. René Scharfe proposes a minimal, targeted fix for the first patch in Matthias Aßhauer’s two-patch series, addressing the memory-safety flaw in `worktree_basename()` that can lead to data loss. The patch replaces the original pointer-arithmetic loop with a safer decrementing index, eliminating the risk of underflow when the input string is empty or contains only separators.

The change is confined to `builtin/worktree.c` and directly implements the stated goal of the original patch: preventing out-of-bounds reads without introducing new edge cases. It follows the style of the existing code and maintains the function’s interface, making it a drop-in replacement for the first patch in the series. The fix is narrow, well-explained, and appears to address the root cause without requiring broader refactoring.

This patch does not touch the second patch in the series (which adds explicit validation for empty strings), nor does it include test cases or reproduction steps. It is offered as an interim solution while waiting for a reroll of the full series, and René’s commit message makes clear that this is a focused fix rather than a complete redesign. The patch is likely to be uncontroversial, as it aligns with Junio’s earlier suggestion to avoid pointer arithmetic when the input length is zero.

If accepted, this would replace the first patch in the series, leaving the second patch (empty-string validation) to be adjusted or reworked separately. The patch is ready for review and could be merged independently if the second patch’s fate remains uncertain.