This is the seventh patch in Justin Tobler’s nine-patch v3 series refactoring `git receive-pack` to use ODB transactions for pluggable packfile writes. The patch updates the `unpack()` function signature to accept an explicit file descriptor for the incoming packfile, replacing the hardcoded assumption that it reads from stdin. This is a mechanical but necessary step toward the backend-agnostic `odb_transaction_write_pack()` interface introduced later in the series.

The change is straightforward: `unpack()` and its helper `parse_pack_header()` now take an `int pack_fd` parameter, and callers (including `unpack_with_sideband()`) pass the appropriate descriptor. The patch updates two call sites in `unpack()` to assign the descriptor to the child process’s stdin, ensuring the packfile is read from the correct source. No behavior change is introduced—this is purely preparatory plumbing for the ODB abstraction.

The patch is well-scoped and aligns with the series’ goal of eliminating implicit state. The commit message clearly explains the motivation, and the diff is minimal, touching only the necessary lines in `builtin/receive-pack.c`. This looks like a clean, uncontroversial step in the larger refactor. If the series as a whole is on track, this patch should merge without issue.