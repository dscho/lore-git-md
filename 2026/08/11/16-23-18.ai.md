This is a documentation patch in the ongoing man page synopsis-style conversion effort, specifically a follow-up to Patrick Steinhardt’s review of Kristoffer Haugsbakk’s resend. The patch itself is trivial—adding a `[synopsis]` AsciiDoc block to highlight the `<subject>` placeholder in a code example in `Documentation/git-format-rev.adoc`—but the discussion now centers on whether the established pattern (using `[synopsis]` for consistency) should be strictly followed or pragmatically adjusted for this case.

Kristoffer clarifies that the `[synopsis]` block here is not for a command synopsis but for styling placeholders in code blocks, matching the pattern introduced in commit `a34d1d53` (doc: convert git-show to synopsis style, 2026-02-06). However, he acknowledges Patrick’s point about the awkwardness of applying `[synopsis]` to a non-synopsis-like block and playfully suggests an alternative: rewriting the example to use `"%s"` and `"<subject>"` (with literal quotes) to avoid the need for special markup entirely. This would sidestep the markup debate but require a minor content change.

The open question remains whether to prioritize strict consistency with the conversion effort (using `[synopsis]`) or pragmatic simplicity (using backticks or adjusting the example text). The discussion is unlikely to become contentious, but it may prompt a brief clarification of AsciiDoc best practices for Git’s documentation. No objections to the patch’s intent exist, and the author’s tone suggests they are open to either approach. The outcome could influence future patches in the synopsis-style conversion, particularly for non-synopsis content.