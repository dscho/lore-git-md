This is a **substantive review reply** in the `git stash reword` thread, where Patrick Steinhardt responds to Junio C Hamano’s proposal for a new reflog API (`refs_reflog_edit_in_bulk()`). Patrick agrees that the current reflog interfaces are inadequate but suggests an alternative path: extending Git’s reference transactions to handle reflog edits natively, rather than introducing a separate bulk-editing API.

The key technical point is that Git’s reference transactions (used for atomic updates to refs and reflogs) could be generalized to support the same use cases Junio’s API would address—replacing, inserting, or deleting individual reflog entries—without introducing a parallel mechanism. Patrick implies this would be a cleaner architectural direction, as it unifies reflog operations under the existing transaction framework rather than adding a new layer. He does not detail how this would work, but the suggestion is clearly a counterproposal to Junio’s design.

This reply shifts the discussion from *whether* to extend the reflog API to *how* to do it most effectively. The thread remains blocked on resolving this design question, but Patrick’s input introduces a new technical axis: leveraging transactions instead of building a bespoke bulk-editing interface. The implications are significant—if adopted, this approach could simplify the implementation of `git stash reword` while also enabling broader refactoring of reflog-related commands (e.g., `stash drop`, `reflog expire`).

No merge status is implied; the patch is still under review, and the path forward now hinges on whether Junio or others prefer Patrick’s transaction-based approach over the standalone API. The tone is collaborative, treating the problem as a shared architectural challenge rather than a disagreement. Future discussion will likely focus on the trade-offs between the two designs.