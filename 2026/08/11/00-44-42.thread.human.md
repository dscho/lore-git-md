Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread started as a bug report about silent corruption in Git on Windows, uncovering two related usability pitfalls. The first—unquoted backslashes in textconv filter commands—was quickly dismissed as a shell misconfiguration rather than a Git flaw, though maintainers acknowledged it could use better documentation. The second, more serious issue remains unresolved: global `.gitattributes` rules like `* text diff=lfclean` can corrupt binary files (PNGs, JPEGs) during checkout by forcing line-ending normalization on them. D. Ben Knoble clarified the root cause: it’s the `text` attribute itself, not the `diff` filter, that triggers Git’s LF→CRLF conversion, corrupting binaries.

The discussion has split between technical workarounds and broader design concerns. Randall S. Becker and Knoble recommend safer configurations (`core.autocrlf=input`, `* text=auto`, explicit `*.png binary` rules), while the original reporter, Skybuck Flying, now argues the attribute system is fundamentally flawed and should be scrapped entirely. Recent emails have veered into frustration with Git’s syntax and design, with Skybuck dismissing `text=auto` as insufficiently safe and criticizing the use of `*` for pattern matching. No maintainer has weighed in on whether Git should add safeguards (like warnings for global `*` rules) or treat this as a documentation gap. For now, the practical advice is to avoid global `.gitattributes` rules, use `text=auto` instead of `text`, and explicitly mark binaries—but the thread’s tone suggests deeper dissatisfaction with Git’s handling of cross-platform text/binary distinctions.