This is a security feature patch that adds `http.sslVerifyStatus` to enable OCSP staple validation in Git’s HTTPS connections. The patch itself is well-crafted, with a clear rationale, thorough testing, and careful attention to compatibility, but Junio’s reply raises a minor stylistic concern about in-code comments.

Junio’s feedback is surface-level: he questions whether the explanatory comment about the default value and fail-closed design belongs in the code at all. His preference is to rely on the commit message for design rationale, keeping the code itself free of what he sees as distracting commentary. The patch is otherwise uncontroversial—no technical objections have been raised, and the implementation (including per-URL configuration, fail-closed behavior, and backend compatibility checks) appears sound.

The patch remains under review, with no indication of merge status. If the author addresses Junio’s comment by moving the rationale to the commit message or removing it entirely, this is likely to proceed smoothly. The broader question of whether the default should eventually change to `true` or include user warnings remains unanswered but is not blocking progress. For now, this is a straightforward security improvement that busy readers can safely track as a likely addition to Git’s HTTPS configuration options.