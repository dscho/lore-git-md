This is the first patch in a ready-to-merge performance series targeting Git’s merge-base computation. It adds a new technical document (`Documentation/technical/paint-down-to-common.adoc`) that explains the `paint_down_to_common()` algorithm, which underpins `git merge-base` and related commands.

The document is thorough and well-structured, covering the algorithm’s use cases (finding all merge bases vs. ancestry checks), the paint-walk mechanics, generation-number regions ("ordered" and "unordered"), termination conditions, and the historical commit-date fallback. It also clarifies how v1 commit-graphs fit into the region model, using `GENERATION_NUMBER_V1_MAX` as a topological ceiling. The terminology aligns with the rest of the series, replacing earlier "finite/INFINITY region" phrasing with "ordered/unordered region" to better reflect the semantic boundary.

This patch is purely documentation and build-system updates (adding the file to `Makefile` and `meson.build`). It lays the groundwork for the subsequent patches, which refactor and optimize the code. The document is already fully reviewed and non-controversial; its inclusion here is a final editorial refinement before the series advances from Junio’s queue. No code behavior changes are introduced.