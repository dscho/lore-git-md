This is a refactoring patch in the `git fast-import` libification series, specifically patch 5/12 of v3. It changes the type of the `show_stats` and `quiet` global variables from `unsigned int` to `int` to align with the expected target type of `OPT_BOOL`, which will be used in a later patch to parse these flags via the parse-options API. The change is purely mechanical and preparatory, touching only two lines in `builtin/fast-import.c` with no behavior change or new symbols introduced.

The patch is part of the broader effort to modernize `git fast-import`'s command-line parsing and reduce reliance on global variables. Earlier patches in the series introduced infrastructure (e.g., `OPT_HIDDEN_GROUP`) and documented parse-options flags, while later patches will complete the parse-options integration and encapsulate globals in a `struct fast_import_state`. This patch ensures type consistency ahead of that work, addressing a minor but necessary detail before the actual parsing logic is updated.

The change is uncontroversial and aligns with Git's coding conventions, where `OPT_BOOL` expects an `int` target. No feedback was raised on this patch in the v3 review cycle, suggesting it is a straightforward and well-understood adjustment. The patch is a small but essential step in the series' incremental refactoring approach, enabling the subsequent conversion of these flags to parse-options callbacks.

Key technical details include the file modified (`builtin/fast-import.c`), the variables affected (`show_stats` and `quiet`), and the type change (`unsigned int` -> `int`). The patch preserves the initial values of the variables (`show_stats = 1`, `quiet = 0`) and does not alter their usage elsewhere in the codebase. This is a low-risk, high-precision change that prepares the ground for the parse-options integration in patch 11/12.

For future AI summarization, note the patch's role in the series: it is a preparatory type adjustment for `OPT_BOOL` compatibility, with no behavioral or API changes. The variables `show_stats` and `quiet` remain globals for now but will be encapsulated in `struct fast_import_state` in later patches. The patch is part of the ongoing libification effort for `git fast-import`, which aims to standardize its usage string, modernize its parse-options API, and eliminate global variables.