This is a **substantive review reply** in the ongoing `git stash reword` thread. Patrick Steinhardt responds to Junio C Hamano’s proposal for a new reflog API (`refs_reflog_edit_in_bulk()`) by suggesting an alternative path: extending Git’s reference transaction system to handle reflog edits natively, rather than adding a separate bulk-editing interface.

Patrick’s position is clear: he views the current reflog interfaces as "awful" and "severely lacking," but sees potential in the recent transactional improvements (which allow batch-committing reflog entries) to serve as a foundation for a more general solution. His preference is to avoid introducing a new, specialized API like `refs_reflog_edit_in_bulk()` and instead focus on making reference transactions powerful enough to cover all reflog use cases, including `stash reword`. This approach would likely unify reflog operations under the existing transaction framework, reducing code duplication and improving consistency across backends.

The email is brief but shifts the technical discussion from *how* to implement Junio’s proposed API to *whether* it is the right architectural choice. Patrick does not reject the feature itself—his earlier review of `v1` already acknowledged the justification for `stash reword`—but he questions the long-term maintainability of adding another reflog-specific interface. His suggestion implies a broader refactoring of the refs subsystem, which could delay the feature’s merge but result in a cleaner design.

For a busy reader, this exchange signals that the `stash reword` patch is blocked not just by a correctness issue (the data-loss risk in the current implementation) but also by a deeper architectural debate. The thread is now split between two competing visions for how to fix the problem: Junio’s targeted API extension versus Patrick’s transaction-based refactoring. Neither has dismissed the other’s approach outright, but the choice between them will shape how Git handles reflog edits in the future. The patch remains under review, with no clear timeline for resolution.