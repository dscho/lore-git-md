This is Junio’s "What’s cooking" report for August 2026, summarizing the state of all in-flight topics in his integration branches. It is not a single patch but a meta-summary of the entire queue, so the usual patch classification does not apply.

The report lists 409 non-merge commits in `master` since Git 2.55, 61 in `next`, and 261 in `seen`. New topics are marked with an asterisk; stalled ones are flagged as such. The most actionable take-away is which topics are ready to graduate, which are stuck waiting for review or a reroll, and which are about to be discarded.

Notable graduations to `master` (via `next`) include `yn/worktree-add-no-dwim-with-b` (worktree add no longer DWIMs when `-b` or `-B` is given), `kl/t7528-ssh-agent-for-csh-users` (test fix for csh users), `kh/doc-replay-config` (documentation for `git replay` config), `sk/test-commit-body-helper` (new test helper), `ns/merge-base-is-ancestor-tests` (test coverage for `--is-ancestor`), `jc/add-resolved` (new `--resolved` option for `git add`), `lo/mv-missing-dest-dir-check` (better error checking in `git mv`), and `ja/doc-synopsis-style-yet-more` (four more man pages converted to the new synopsis style).

Topics that look ready to advance to `next` (or are already there and likely to graduate soon) include `hn/send-email-missing-subject-error` (clarified error message), `ps/cat-file-remote-object-info-type` (support for `%(objecttype)` in `--batch-command`), `ps/odb-make-creation-pluggable` (pluggable ODB on-disk setup), `ps/writev` (writev(3p) wrappers for sideband and fast-import), `hn/bisect-reset-when-found` (new `--reset-when-found` option for `git bisect`), and `hn/branch-delete-merged` (new `--delete-merged` option for `git branch`).

Several topics are stalled or waiting for a response: `bl/t7412-use-test-path-helpers` (test modernization), `tb/repack-geometric-cruft` (combining `--geometric` and `--cruft`), `hn/checkout-track-fetch` (proposed `--track=fetch` mode for checkout), `za/completion-hide-dotfiles` (dotfile hiding in completion), `hs/rebase-continue-edit` (new `--[no-]edit` option for `git rebase --continue`), `td/fsmonitor-darwin-cookie-flush` (macOS fsmonitor fix), and `sn/rebase-update-refs-symrefs` (symref handling in `--update-refs`).

The report also highlights a few topics that need review or are expecting a reroll: `ss/repack-drop-filtered` (new `--drop-filtered` option for `git repack`), `ty/repo-config-cleanups` (cleanup of repository config getters), `vm/complete-history` (completion for the experimental `git history` command), `cc/lazy-fetch-trusted-bit` (lazy-fetch trust control), `dk/use-nsec-runtime` (runtime control for nanosecond timestamps), `ps/t7900-deflake-maintenance` (test flake fix), `ps/odb-streams` (unified ODB streaming API), `bc/restrict-hex-to-lowercase` (lowercase-only object IDs in breaking-changes mode), `js/mingw-build-updates` (MinGW build cleanups), `tc/last-modified-bloom` (Bloom-filter optimization for `git last-modified`), `js/coverity-unchecked-returns-fix` (Coverity fixes), `ds/trace2-tolerate-failed-timestamp` (trace2 robustness), and `kk/merge-base-exhaustion` (merge-base performance optimization).

In short, the report is a snapshot of the project’s pulse: a mix of small fixes, documentation updates, and larger architectural changes, with a handful of topics that have been stuck for weeks or months. Busy readers can focus on the “Will merge to `next`” and “Will merge to `master`” sections to see what is about to land.