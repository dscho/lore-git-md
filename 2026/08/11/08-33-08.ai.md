This is patch 6/12 in Christian Couder’s v3 series refactoring `git fast-import` for parse-options integration and libification. The patch factors out the argument-parsing logic for five command-line options (`--max-pack-size`, `--big-file-threshold`, `--signed-commits`, `--signed-tags`, and `--quiet`) into dedicated helper functions (`option_max_pack_size()`, `option_big_file_threshold()`, `option_signed_commits()`, `option_signed_tags()`, and `option_quiet()`). The change is purely preparatory: it reorganizes existing code without altering behavior, except for replacing generic “unknown option” failures with specific `die()` messages when `git_parse_ulong()` fails to parse numeric arguments. The patch touches only `builtin/fast-import.c` and is a straightforward, well-motivated step toward the parse-options conversion in patch 11/12. No new symbols or on-disk behavior changes are introduced. The commit message clearly explains the purpose and the minor error-message improvement.