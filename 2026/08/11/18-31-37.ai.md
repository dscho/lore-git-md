This is a **performance feature** for Linux filesystems that support copy-on-write reflinks (e.g., Btrfs, XFS, ZFS). The author has revised the patch to address Junio C Hamano’s design concern about the fallback order: the new version now attempts a hardlink first, then a reflink, and finally falls back to a byte-for-byte copy. This change aligns with the principle that simpler mechanisms (hardlinks) should be preferred for immutable files unless there is a compelling reason to do otherwise.

The patch remains Linux-specific, using the `FICLONE` ioctl to create reflinks. It preserves source timestamps when reflinking, which is important for `prune` and `gc` decisions. The test infrastructure is unchanged: the LD_PRELOAD-based helper (`test-fake-reflink.c`) still simulates reflink success, unsupported operation, or errors, and the new test script (`t0094-reflink.sh`) continues to cover generic file copying, local clones, `--no-hardlinks`, hardlink fallback, byte-copy fallback, object integrity, and timestamp preservation.

The revised fallback order is now: hardlink → reflink → byte-for-byte copy. With `--no-hardlinks`, the order is reflink → byte-for-byte copy. The patch touches the same 12 files as before, with the core logic in `copy.c` and `clone.c`. The author reports that the focused tests still pass on ZFS, ext4, and tmpfs, and the full test suite passes on all three filesystems. The patch is now uncontroversial on its design assumptions and should proceed to review for correctness and edge cases.