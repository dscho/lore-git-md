This is a **substantive review reply** in the ongoing `gitk` custom commands thread, where the maintainer (Johannes Sixt) clarifies and doubles down on his incremental implementation stance.

Hannes acknowledges Tim Wiederhake’s goal of a generic extension system but reiterates that the feature should be introduced in **small, reviewable steps**—starting with a minimal use case (e.g., "open an editor at this file/line" using only `%f` substitution) and later adding more placeholders (`%i`, `%t`, etc.) in follow-up patches. He argues that even a single placeholder (`%f`) delivers immediate value (e.g., editor integration) and that splitting the work aligns with the project’s review process, which favors digestible increments. The reply pushes back on Tim’s claim that partial implementation would be "half-useful," emphasizing that editor invocation alone is a practical starting point.

No new technical or security concerns are raised; the focus remains on **process and scope**. The thread is now at an impasse: Tim insists the feature must be complete to be useful, while Hannes demands incremental delivery. For a future AI summarizer, note the key symbols (`%f`, `%i`, `%t`) and the maintainer’s preference for **separate commits per placeholder group**. The security flaw (unquoted shell substitution) and AI policy questions remain unresolved.