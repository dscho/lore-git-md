This is a **design-scope and process discussion** in the ongoing `gitk` custom-commands thread. Johannes Sixt (the `gitk` maintainer) clarifies his incremental-review expectation: the feature should be split into at least two separate patches—one for diff-panel commands (e.g., "open an editor at this line") and another for commit-list commands (e.g., "rebase onto this commit"). He reiterates that even a minimal implementation with only the `%f` (filename) placeholder would be immediately useful, countering Tim Wiederhake’s claim that partial functionality would be "half-useful." Hannes frames the incremental approach as a review-friendly way to land the feature without blocking on the full set of substitutions.

The disagreement remains unresolved: Tim still argues that the substitutions are parameters of a single mechanism, not independent features, while Hannes insists on digestible review units. No new technical or security concerns are raised here—this email is purely about process and scope. For a busy reader, the thread is now **stalled on a design impasse** unless Tim concedes to split the patch or Hannes relaxes his incremental-review requirement. The security flaw (unquoted shell substitution) and AI-assistance policy questions are still unaddressed.