This is a **bugfix** patch for the ongoing `git worktree add` out-of-bounds read series. René Scharfe proposes a minimal, targeted rewrite of `worktree_basename()` to eliminate the pointer underflow that causes the memory-safety issue. The patch replaces the original loop—which decremented a pointer below the start of the input string when `path` was empty—with a safer loop that stops at the string boundary. This change directly addresses the root cause identified in the thread: the function’s reliance on fragile pointer arithmetic to compute the basename length.

The patch is a **drop-in replacement** for the first patch in Matthias Aßhauer’s v1 series. It preserves the function’s interface (returning a pointer to the basename and setting `*olen`) but removes the risk of underflow by using an integer index (`name[-1]`) instead of pointer arithmetic. The implementation is simpler and more robust, avoiding the negative-length edge case that Junio C Hamano flagged in the original patch. René’s commit message is concise and aligns with the series’ goal: preventing data loss without introducing new behavior.

No new validation or error handling is added here; the patch focuses solely on fixing the memory-safety flaw. The second patch in the series (which rejects empty strings) remains necessary for the `"" HEAD` case, as René previously confirmed. This patch does not touch that logic, nor does it address the placement or wording of the second patch’s validation.

The diff is small (3 insertions, 5 deletions) and confined to `builtin/worktree.c`. It follows the existing code style and does not introduce new symbols or dependencies. The patch is **ready for review** and appears to resolve the technical objections raised against the original approach. If accepted, it would likely replace the first patch in the next revision of the series. No test cases are included, but the fix is straightforward enough that reviewers may not require them for this specific change.