Mode: ai

Thread identity
Subject: [PATCH v4 0/3] receive-pack: add new `receive-report` hook for server-side status filtering
Series version: v4 (3 patches)
Originating author: Karthik Nayak
Thread type: feature patch series

Core problem or goal
Git’s `receive-pack` currently sends a fixed status report to the client after ref updates are committed. There is no way for server administrators to intercept or modify this report before it is finalized. The patch series adds a new `receive-report` hook that acts as a bidirectional filter: it receives the pkt-line encoded status report on stdin, replaces it with its stdout, and forwards stderr to the client via the sideband channel. The hook is motivated by GitLab’s need to implement MVCC (multi-version concurrency control) on the server side, where the status report must reflect the state of the repository after all ref updates are committed but before the client receives confirmation.

Current status
**Queued in `seen`** (Junio C Hamano, date unknown). The series is **ready for integration** into `next`; all prior feedback has been addressed, including the hook naming objection from Junio C Hamano and Patrick Steinhardt. The **exit status semantics**—the sole unresolved issue in v2—were **resolved in favor of Junio C Hamano and Patrick Steinhardt’s proposal**: a non-zero exit rewrites all ref status lines to `"receive-report hook failed"` and allows `receive-pack` to exit cleanly, aligning with the behavior of the `pre-receive` hook. The series is split into three patches:
1. **Documentation-only (applied)**: adds a `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc`, explaining the existing `proc-receive` hook’s purpose and linking to `githooks(5)` for protocol details. This patch addresses prior feedback about documentation completeness for hooks in the `git-receive-pack` lifecycle.
2. **Refactoring (applied)**: extracts the status-report generation logic into a reusable `generate_response()` helper in `builtin/receive-pack.c`. The patch is mechanical, with no behavior changes, and prepares the codebase for the hook implementation by centralizing the pkt-line formatting logic. The helper takes a boolean parameter to control v2-specific reports and updates callers to pass a pre-allocated `strbuf`.
3. **Core implementation (applied)**: introduces the `receive-report` hook with the agreed-upon exit semantics. The hook runs after all ref updates are committed but before the status report is sent to the client. It receives the pkt-line encoded report on stdin and replaces it with its stdout; stderr is forwarded to the client via the sideband channel. A zero exit code means the hook’s stdout is used as the report; a non-zero exit rewrites all ref status lines to `"receive-report hook failed"` and discards the hook’s stdout, matching the behavior of the `pre-receive` hook. The implementation is self-contained and does not use the config-based hook infrastructure.

Junio has manually resolved a conflict with Justin Tobler’s concurrent "pluggable writes" series (which touches the same area of `builtin/receive-pack.c`). Karthik Nayak has confirmed the resolution is correct. **The series is now technically complete and awaits graduation to `next`**.

Key technical details
- Files touched: `builtin/receive-pack.c`, `Documentation/githooks.adoc`, `Documentation/git-receive-pack.adoc`, `t/t5412-receive-report-hook.sh` (renamed from `t5412-report-hook.sh`)
- New hook name: `receive-report` (finalized; replaces the prior `report`)
- New helper function: `generate_response()` in `builtin/receive-pack.c` (extracted from `report()` and `report_v2()`)
- Invocation point: after all ref updates are committed, before the status report is sent
- Input: pkt-line encoded status report on stdin (each `ng <refname> <reason>` line ends with a newline; entire report terminated by a flush packet)
- Output: replacement status report on stdout; stderr forwarded to client via sideband
- Exit code semantics: non-zero exit rewrites all ref status lines to `"receive-report hook failed"` and allows `receive-pack` to exit cleanly (aligned with `pre-receive` hook)
- Hook is **NOT integrated with the config-based hook infrastructure** (deliberate choice to avoid parallelization and coordination complexity)
- Test coverage: 200 lines in `t/t5412-receive-report-hook.sh` covering passthrough, failure modes, stdin/stdout handling, sideband relay, and interaction between exit status and stderr output. The test suite exercises both success and failure cases, including custom failure messages and stderr relay.
- Documentation: The hook is documented in both `githooks.adoc` and `git-receive-pack(1)`, with the latter linking to `githooks(5)`. The commit message includes a concrete use-case example (GitLab’s MVCC system) and justifies the single-script design. Pkt-line format documentation links to `gitprotocol-pack[5]`. The preparatory documentation patch adds a `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc`, explaining the existing `proc-receive` hook’s purpose and linking to `githooks(5)` for protocol details.

Open questions and loose ends
None. All prior feedback has been addressed, including the hook naming objection and exit status semantics. The series is technically complete and awaits graduation to `next`.

Participant positions
- Karthik Nayak (author): Implemented the hook, documented the use case (GitLab’s MVCC system), and provided test coverage. Addressed all prior feedback: adopted Junio and Patrick’s exit status proposal, split the series into three patches (documentation, refactoring, core implementation), resolved documentation ambiguities (linking to `gitprotocol-pack[5]`), and renamed the hook from `report` to `receive-report`. Confirmed Junio’s manual conflict resolution with Justin Tobler’s "pluggable writes" series is correct. **Acknowledged the "awkward" output restructuring** required for the new exit semantics but implemented it as agreed. Also addressed prior feedback about documentation completeness by adding a preparatory patch documenting the existing `proc-receive` hook.
- Junio C Hamano (maintainer): Initially raised concerns about exit status semantics and the single-script design. **Endorsed Patrick Steinhardt’s proposal** to align the exit status behavior with the `pre-receive` hook, calling it "the most valuable input" in Patrick’s review. This endorsement resolved the sole blocker for the patch. **Queued the series in `seen`** and manually resolved a conflict with Justin Tobler’s "pluggable writes" series. **Objected to the hook name `report` as overly generic** and proposed `receive-report` as an alternative. The naming objection has been addressed in v4.
- Patrick Steinhardt: Provided **substantive reviews** clarifying the hook’s necessity, tightening documentation, and proposing the exit status alignment with `pre-receive`. His proposal is now **fully adopted**. **Agreed with Junio’s naming objection** and proposed `receive-report` as a more specific alternative.
- Kristoffer Haugsbakk: Provided **surface-level reviews** focusing on documentation clarity and minor stylistic points. Suggested linking to `gitprotocol-v2(5)` for pkt-line format (resolved by linking to `gitprotocol-pack[5]` instead).
- Phillip Wood: Questioned the single-script design rationale but was addressed by Karthik’s justification in the commit message. No explicit agreement recorded, but no further objections raised.

Related work
- Justin Tobler’s "pluggable writes" series (concurrent with this one) touches the same area of `builtin/receive-pack.c` and required manual conflict resolution by Junio. No functional overlap; the two series are complementary. Karthik Nayak has confirmed the resolution is correct.