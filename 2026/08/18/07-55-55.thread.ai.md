Mode: ai

Thread identity
Subject: [PATCH v3 0/3] receive-pack: add new `report` hook for server-side status filtering
Series version: v3 (3 patches)
Originating author: Karthik Nayak
Thread type: feature patch series

Core problem or goal
Git’s `receive-pack` currently sends a fixed status report to the client after ref updates are committed. There is no way for server administrators to intercept or modify this report before it is finalized. The patch series adds a new `report` hook that acts as a bidirectional filter: it receives the pkt-line encoded status report on stdin, replaces it with its stdout, and forwards stderr to the client via the sideband channel. The hook is motivated by GitLab’s need to implement MVCC (multi-version concurrency control) on the server side, where the status report must reflect the state of the repository after all ref updates are committed but before the client receives confirmation.

Current status
**Ready for integration**; all prior feedback addressed. The **exit status semantics**—the sole unresolved issue in v2—were **resolved in favor of Junio C Hamano and Patrick Steinhardt’s proposal**: a non-zero exit rewrites all ref status lines to `"report hook failed"` and allows `receive-pack` to exit cleanly, aligning with the behavior of the `pre-receive` hook. The series is split into three patches:
1. Documentation-only: adds a `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc` (addresses prior feedback about documentation completeness for existing hooks).
2. **Refactoring (applied)**: extracts the status-report generation logic into a reusable `generate_response()` helper in `builtin/receive-pack.c`. The patch is mechanical, with no behavior changes, and prepares the codebase for the hook implementation by centralizing the pkt-line formatting logic. The helper takes a boolean parameter to control v2-specific reports and updates callers to pass a pre-allocated `strbuf`.
3. **Core implementation (applied)**: introduces the `report` hook with the agreed-upon exit semantics. The hook runs after all ref updates are committed but before the status report is sent to the client. It receives the pkt-line encoded report on stdin and replaces it with its stdout; stderr is forwarded to the client via the sideband channel. A zero exit code means the hook’s stdout is used as the report; a non-zero exit rewrites all ref status lines to `"report hook failed"` and discards the hook’s stdout, matching the behavior of the `pre-receive` hook. The implementation is self-contained and does not use the config-based hook infrastructure.

The pkt-line format documentation ambiguity has been resolved by linking to `gitprotocol-pack[5]`. No further changes to the core implementation are expected.

Key technical details
- Files touched: `builtin/receive-pack.c`, `Documentation/githooks.adoc`, `Documentation/git-receive-pack.adoc`, `t/t5412-report-hook.sh`
- New hook name: `report`
- New helper function: `generate_response()` in `builtin/receive-pack.c` (extracted from `report()` and `report_v2()`)
- Invocation point: after all ref updates are committed, before the status report is sent
- Input: pkt-line encoded status report on stdin (each `ng <refname> <reason>` line ends with a newline; entire report terminated by a flush packet)
- Output: replacement status report on stdout; stderr forwarded to client via sideband
- Exit code semantics: non-zero exit rewrites all ref status lines to `"report hook failed"` and allows `receive-pack` to exit cleanly (aligned with `pre-receive` hook)
- Hook is **NOT integrated with the config-based hook infrastructure** (deliberate choice to avoid parallelization and coordination complexity)
- Test coverage: passthrough, failure modes, stdin/stdout handling, sideband relay, and interaction between exit status and stderr output
- Documentation: The hook is documented in both `githooks.adoc` and `git-receive-pack(1)`, with the latter linking to `githooks(5)`. The commit message includes a concrete use-case example (GitLab’s MVCC system) and justifies the single-script design. Pkt-line format documentation links to `gitprotocol-pack[5]`. The preparatory documentation patch adds a `PROC-REIVE HOOK` section to `git-receive-pack.adoc`, explaining the existing `proc-receive` hook’s purpose and linking to `githooks(5)` for protocol details.

Open questions and loose ends
- None. The **documentation nit** regarding linking to `gitprotocol-common.adoc` or `gitprotocol-pack[5]` has been resolved by linking to `gitprotocol-pack[5]`.

Participant positions
- Karthik Nayak (author): Implemented the hook, documented the use case (GitLab’s MVCC system), and provided test coverage. Addressed all prior feedback: adopted Junio and Patrick’s exit status proposal, split the series into three patches (documentation, refactoring, core implementation), and resolved documentation ambiguities (linking to `gitprotocol-pack[5]`). **Acknowledged the "awkward" output restructuring** required for the new exit semantics but implemented it as agreed. Also addressed prior feedback about documentation completeness by adding a preparatory patch documenting the existing `proc-receive` hook.
- Junio C Hamano (maintainer): Initially raised concerns about exit status semantics and the single-script design. **Endorsed Patrick Steinhardt’s proposal** to align the exit status behavior with the `pre-receive` hook, calling it "the most valuable input" in Patrick’s review. This endorsement resolved the sole blocker for the patch.
- Patrick Steinhardt: Provided **substantive reviews** clarifying the hook’s necessity, tightening documentation, and proposing the exit status alignment with `pre-receive`. His proposal is now **fully adopted**.
- Kristoffer Haugsbakk: Provided **surface-level reviews** focusing on documentation clarity and minor stylistic points. Suggested linking to `gitprotocol-v2(5)` for pkt-line format (resolved by linking to `gitprotocol-pack[5]` instead).
- Phillip Wood: Questioned the single-script design rationale but was addressed by Karthik’s justification in the commit message. No explicit agreement recorded, but no further objections raised.

Related work
None mentioned.