This is a bugfix patch discussion, now with a clear technical stance in favor of the change. Brian M. Carlson weighs in on the ongoing debate about whether `git config -e` should propagate editor failure exit codes. He argues that the current behavior—ignoring editor failures—is counterintuitive and could lead to scripts silently ignoring errors they intended to catch, especially when using `set -e`. His position aligns with Karthik Nayak’s earlier support for the patch, framing the change as a correctness and usability improvement rather than a breaking change.

The patch itself is minimal: a one-line fix in `builtin/config.c` to return the editor’s exit code, plus two new tests in `t/t1300-config.sh` to verify the behavior. The feature (`git config -e`) was introduced only two years ago, which limits the risk of breaking existing scripts, but Junio has remained noncommittal, emphasizing the trade-off between conceptual correctness and potential disruption. Brian’s argument adds weight to the "correctness" side, noting that most editors (and programming languages) exit non-zero only on serious errors, so propagating the exit code would align with user expectations.

The thread’s core tension remains unresolved, but Brian’s input shifts the balance slightly toward accepting the patch. No new technical objections have been raised, and the discussion is now focused on whether the benefit of consistency (with commands like `git commit`) outweighs the risk of script breakage. If the patch moves forward, it will likely be with minimal changes, as the implementation and test coverage are already solid. For now, the discussion is still open, but the case for merging is strengthening.