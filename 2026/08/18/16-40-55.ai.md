This is a **maintainer reply** in the thread adding `http.sslVerifyStatus` for OCSP staple validation. Junio C Hamano responds to Patrick Steinhardt’s substantive review of the v4 patch, focusing on two points: the accuracy of the commit message’s description of libcurl’s default behavior, and the message’s readability.

Junio agrees with Patrick that the commit message is unnecessarily hard to parse, though he initially attributed the difficulty to a language barrier rather than AI generation. He echoes Patrick’s observation that the message’s phrasing—particularly the claim that "Leaving the default to libcurl is not an option"—is misleading: the patch does, in fact, leave the default to libcurl when `http.sslVerifyStatus` is unset, since it only sets `CURLOPT_SSL_VERIFYSTATUS` when the config key is explicitly enabled. The message’s wording obscures this nuance, making it sound as if the patch overrides libcurl’s default unconditionally.

The exchange does not challenge the patch’s technical substance or its readiness for integration—Junio had already accepted v4 as technically complete—but it does highlight a **documentation issue** that the author will need to address before the patch graduates to `master`. The commit message should clarify that the patch only enables `CURLOPT_SSL_VERIFYSTATUS` when `http.sslVerifyStatus` is set to `true`, and that libcurl’s default behavior remains unchanged otherwise. This aligns with Patrick’s earlier request for more precise documentation, and Junio’s feedback reinforces the need for a human-readable revision.

The thread remains on track for integration, but the author will likely send a v5 to fix the commit message wording and incorporate Patrick’s other review points (error message clarity, backend-specific documentation updates, and test coverage discussion). The tone is constructive, with no objections to the feature itself.