This is a **bugfix for zsh completion**, closing out the thread on the v2 patch that fixes command and argument completion after global options like `git -C <path> <command>`. The author, Lutz Lengemann, confirms that the patch is now available as a GitGitGadget pull request and responds to D. Ben Knoble’s final clarifications.

The patch itself is small (25 lines), well-scoped, and aligns zsh completion with the existing bash implementation by scanning past all global options (`-C`, `--git-dir`, `-p`, etc.) to correctly identify the command index. It resolves the core issue—completions now work for constructs like `git -C dir checkout <TAB>` and `git -p checkout <TAB>`—and documents two remaining gaps as follow-ups: `-c` is still missing from the `_arguments` spec, and `--git-dir` only accepts the stuck form (`--git-dir=<path>`). Both are explicitly deferred, and Ben Knoble agrees with the decisions. The unrelated `git -<TAB>` bug (redundant output) is confirmed as pre-existing and left for a separate patch.

The tone of the exchange is collaborative and low-stakes; no new objections or requests for changes are raised. The patch is effectively unblocked, with all substantive feedback from Junio C Hamano and Ben Knoble addressed in v2. Lutz expresses enthusiasm about contributing to Git, and the thread ends on a positive note. If you use zsh and rely on Git’s completion, this patch is worth your attention—it fixes a long-standing annoyance without introducing new complexity.