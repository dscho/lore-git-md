Thread identity
Subject: [PATCH] pull: introduce `--hard` mode
Series version: v1 (initial submission, redesign proposed and now endorsed by author)
Originating author: Artur Bieniek
Thread type: patch series (single patch)

Core problem or goal
Add a convenient way to discard local state and sync to upstream in a single command, specifically targeting the use case where an upstream branch has been force-pushed or rebased and the user has no local development (only a clean checkout of the upstream branch). The original proposal (`git pull --hard`) would reset the current branch, index, and working tree to the fetched branch using `git reset --hard`, discarding all local changes. The discussion has evolved to a safer redesign (`git pull --reset [--discard-changes | --autostash]`) that would error out by default if uncommitted changes exist, with an `--autostash` option to preserve them. The core question is whether such a feature belongs in `git pull` at all, and whether the force-push/rebase use case justifies its inclusion.

Current status
Under review; no prior versions. The original patch implements `--hard`, but the thread has shifted to a conceptual debate about the feature’s inclusion and a proposed redesign. Artur Bieniek now endorses Phillip Wood’s `--reset` redesign, providing a concrete use case (syncing to a force-pushed or rebased upstream branch) that `git pull --ff-only` cannot address. Junio’s latest response rejects the underlying motivation, leaving the redesign’s viability uncertain. No updated patch has been posted.

Key technical details
- Files touched: `builtin/pull.c`, `Documentation/git-pull.txt`, `t/t5520-pull.sh`
- Original CLI option: `--hard` (now deprecated in favor of redesign)
- Proposed redesign: `--reset` with sub-options `--discard-changes` (default, errors if uncommitted changes exist) and `--autostash` (stashes changes before reset, restores afterward)
- New behavior: `git pull --reset <remote> <branch>` → `git fetch <remote> <branch>` followed by `git reset --hard FETCH_HEAD` (with safety checks or autostash)
- Rejects combinations with `--ff-only`, `--squash`, `--rebase`, etc.
- Preserves `--quiet` and submodule recursion settings from the pull context
- On-disk format: no changes
- Test coverage: basic usage, error conditions, submodule recursion (original patch); proposed redesign would require additional tests for uncommitted changes and autostash behavior

Open questions and loose ends
- Whether the force-push/rebase use case is compelling enough to justify inclusion in `git pull`. Junio frames this as a design philosophy question, citing `git pull --ff-only` as a sufficient alternative for syncing without integrating local changes, but Artur Bieniek argues that `--ff-only` cannot handle force-pushed or rebased upstream branches.
- Whether Junio will accept the `--reset` redesign or further refine his objections. No response yet to Artur’s endorsement of the redesign or the force-push/rebase use case.
- No other substantive review feedback has been posted beyond Junio’s principled opposition, Phillip’s counterproposal, and Artur’s endorsement.

Participant positions
- Artur Bieniek (author): originally proposed `--hard` as a motivated, self-contained addition with thorough error handling and documentation. Now endorses Phillip’s `--reset` redesign, arguing it better aligns with Git’s safety principles and addresses a specific workflow gap (syncing to force-pushed or rebased upstream branches) that `git pull --ff-only` cannot handle. Position is now substantive and constructive, focusing on the redesign’s practical utility.
- Junio C Hamano (maintainer): firmly opposed to adding destructive operations to `git pull`, even with the proposed safety improvements. Argues the command’s primary purpose is history integration, not working-copy management, and that users should discard local state explicitly with separate commands. Position is conceptual, not technical; no engagement yet with the force-push/rebase use case or the redesign’s specifics.
- Phillip Wood: proposes the `--reset` redesign (`git pull --reset [--discard-changes | --autostash]`) that directly targets Junio’s safety concerns by erroring out by default if uncommitted changes exist, with an opt-in `--autostash` option. Frames the redesign as safer than the existing `git fetch && git reset --hard @{u}` workflow while providing convenience. Position is substantive and constructive.

Related work
The existing `git fetch && git reset --hard @{u}` workflow is cited by Junio as a safer, explicit alternative. Phillip’s redesign would make the proposed feature safer than this workflow by checking for uncommitted changes. Artur’s use case (force-pushed or rebased upstream branches) highlights a scenario where `--ff-only` is insufficient, but Junio has not yet responded to this argument.