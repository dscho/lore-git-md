This is a **substantive review** from Junio C Hamano on **patch 3/7** of Patrick Steinhardt’s ODB corruption-handling refactor series. The review engages directly with the patch’s core logic and design intent.

Junio opens by endorsing the *goal* of the patch—stopping Git from silently treating corrupt objects as missing—calling it a long-standing awkwardness. His question about the `bad_pack` logic ("if you have even one bad thing, you are marked as bad") probes whether the patch’s approach is too coarse: once an object is flagged corrupt in any pack, the entire pack is considered tainted, even if only a single object is affected. This could matter for multi-pack scenarios where a single corrupt object in one pack shouldn’t necessarily invalidate the whole pack for unrelated objects. The question is left open; no resolution or counter-argument is offered yet.

The review is narrowly scoped to this patch’s change in `fill_midx_entry()` and does not revisit the broader tri-state return convention dispute from the cover letter. It remains a focused, technical observation about the patch’s internal consistency rather than a design-level objection.