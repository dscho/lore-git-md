Here’s a concise human-readable summary of the Git mailing list thread based on Junio’s "What’s cooking" report for August 2026:

This thread is Junio’s monthly status update on the state of Git development, covering over 400 commits and 400+ in-flight topics across the `next` and `seen` branches. The most urgent item is a critical bugfix (`sk/object-name-use-after-free`) addressing a heap-use-after-free in path resolution for sparse directories, which is ready to graduate to `master`.

The biggest architectural effort is Patrick Steinhardt’s ODB pluggability work, with two major series in progress: one making packfile generation pluggable (removing hardcoded `pack-objects` references) and another simplifying the ODB layer by eagerly loading alternates. These build on his earlier merged series and touch core components like `bundle`, `send-pack`, and `upload-pack`. Other notable changes include libification work (moving `fetch_if_missing` into `struct repository`), a performance fix for quadratic complexity in packfile handling, and new features like `git repack --drop-filtered` (to reclaim space in partial clones) and expanded options for the experimental `git format-rev` command.

Preparations for Git 3.0 are underway with `bc/restrict-hex-to-lowercase`, which updates hex parsers to reject uppercase characters in breaking-changes mode. Windows builds are getting updates from Git for Windows, including dropping obsolete compatibility options. Experimental commands like `git history squash` (to fold commit ranges) and command-line completion for `git history` are also in flight.

Several topics are stalled or discarded, including a macOS fsmonitor fix and a `send-pack` delta capability change. The report also highlights smaller fixes for segfaults, memory leaks, and Coverity warnings, along with a reminder that `hs/rebase-continue-edit` has been waiting for feedback since July. The overall picture is a busy development cycle with a mix of critical fixes, architectural improvements, and new features.