Thread identity: Bugfix series addressing a race condition in Git's geometric repacking mechanism causing objects to be incorrectly reported as missing, leading to crashes in `git replay`. Two-patch series by Elijah Newren via GitGitGadget.

Core problem or goal: Fix a race condition where a multi-pack-index (MIDX) references a packfile removed by geometric repacking, causing processes using the stale MIDX to fail to locate objects. The issue manifests as a SIGSEGV in `git replay` and missing-object errors in other server-side operations (`git merge-tree`, `git diff`, `git rev-list`, `rev-parse`, `cat-file`). Patch 1/2 ensures `git replay` fails gracefully when merge inputs are unreadable, while patch 2/2 modifies the packed-object backend to recover from MIDX-packfile mismatches by scanning all packs covered by the MIDX when the normal lookup fails.

Current status: Under review. Patch 1/2 adds a missing check in `replay.c` to handle `result->clean < 0` (unreadable objects), preventing a SIGSEGV and enabling graceful failure. Patch 2/2 modifies `odb/source-packed.c` to recover from MIDX-packfile mismatches by scanning all packs covered by the MIDX when the object is not found via the normal MIDX or regular pack fallback. Both patches include tests. No prior versions of this series are recorded.

Key technical details:
- Files touched: `replay.c` (graceful failure logic), `odb/source-packed.c` (MIDX recovery), and test files (`t/t3650-replay-basics.sh` for patch 1/2, `t/t5319-multi-pack-index.sh` for patch 2/2).
- Subsystems: geometric repacking, MIDX, packed-object backend, `git replay`.
- New behavior: `git replay` now fails gracefully with an error message when objects are unreadable; MIDX-packfile mismatches are recovered transparently by scanning all packs covered by the MIDX.
- New symbols: None (logic-only changes).
- No on-disk format changes.
- Test coverage: Added for both the `git replay` crash scenario (patch 1/2) and MIDX recovery logic (patch 2/2). Tests simulate unreadable objects and MIDX-packfile mismatches, verifying error messages and recovery behavior.

Open questions and loose ends: None identified. The series remains uncontroversial, with no objections or requests for changes raised.

Participant positions: Author (Elijah Newren) is the sole participant recorded. No reviews, Acked-by, or Reviewed-by trailers observed.

Related work: Geometric repacking and MIDX are active areas of development (see recurring efforts in project context). This fix is a targeted bugfix for a race condition in those subsystems, with no dependencies on other in-flight topics.