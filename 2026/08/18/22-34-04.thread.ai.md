Thread identity: Bugfix series addressing a race condition in Git's geometric repacking mechanism causing objects to be incorrectly reported as missing, leading to crashes in `git replay`. Two-patch series by Elijah Newren via GitGitGadget.

Core problem or goal: Fix a race condition where a multi-pack-index (MIDX) references a packfile removed by geometric repacking, causing processes using the stale MIDX to fail to locate objects. The issue manifests as a SIGSEGV in `git replay` and missing-object errors in other server-side operations (`git merge-tree`, `git diff`, `git rev-list`, `rev-parse`, `cat-file`). Patch 1/2 ensures `git replay` fails gracefully when merge inputs are unreadable, while patch 2/2 modifies the packed-object backend to recover from MIDX-packfile mismatches by scanning all packs covered by the MIDX when the normal lookup fails.

Current status: **Ready for integration.** Junio C Hamano provided a **substantive** review of patch 1/2, endorsing the fix for `pick_regular_commit()`'s incorrect handling of negative `result->clean` values (which signal unreadable objects) and requesting the removal of a redundant `test_grep ! "[Ss]egmentation"` check in the test. Elijah addressed this in his response, confirming the check is unnecessary because `test_must_fail` already verifies graceful failure. He also noted the bug was introduced in an earlier commit (`e787e664da64`) and asked whether to resubmit the patch with the test adjustment or leave it to Junio to handle during integration (via an "evil merge"). Junio had previously indicated he would resolve a merge conflict with an in-flight topic for patch 2/2, so no rebase is required from Elijah. Patch 2/2 received a **substantive** review from Patrick Steinhardt, who praised the targeted recovery logic but raised a nuanced question about whether the fix is strictly necessary for callers that do not use `OBJECT_INFO_QUICK` (e.g., `cat-file`, `rev-list`). Elijah clarified that the current patch (2/2) is intentionally scoped to `OBJECT_INFO_QUICK` callers (e.g., `git replay`, `upload-pack`, `mktree --batch`), which would otherwise fail permanently, while non-`OBJECT_INFO_QUICK` callers already recover naturally. No technical objections to either patch remain. The series is a high-priority candidate for merging due to its impact on server-side stability.

Key technical details:
- Files touched: `replay.c` (graceful failure logic), `odb/source-packed.c` (MIDX recovery), and test files (`t/t3650-replay-basics.sh` for patch 1/2, `t/t5319-multi-pack-index.sh` for patch 2/2).
- Subsystems: geometric repacking, MIDX, packed-object backend, `git replay`.
- New behavior: `git replay` now fails gracefully with an error message when objects are unreadable; MIDX-packfile mismatches are recovered transparently by scanning all packs covered by the MIDX in `find_pack_entry()`. The recovery logic is gated by the presence of a stale MIDX and only triggers when the MIDX *should* have found the object but didn’t. The fix is specifically targeted at `OBJECT_INFO_QUICK` callers, which would otherwise fail permanently.
- New symbols: None (logic-only changes).
- No on-disk format changes.
- Test coverage: Added for both the `git replay` crash scenario (patch 1/2) and MIDX recovery logic (patch 2/2). Tests simulate unreadable objects and MIDX-packfile mismatches, verifying error messages and recovery behavior. The redundant `test_grep` check in patch 1/2's test was removed as part of the review process.

Open questions and loose ends:
- Elijah plans to submit three additional race-condition fixes soon, addressing broader scenarios beyond the current patch's scope (e.g., cases where the `.idx` file is still mmapped but the `.pack` file is gone). These are not blockers for the current series and will be handled in follow-up patches.

Participant positions:
- **Elijah Newren** (author): Proposed the fix and tests. Addressed Junio’s feedback on patch 1/2 by confirming the redundant `test_grep` check is unnecessary and noting the bug’s origin in an earlier commit. Clarified the scope of patch 2/2 in response to Patrick’s review, confirming it targets `OBJECT_INFO_QUICK` callers and situating it within a broader set of planned race-condition cleanups. Asked Junio whether to resubmit the adjusted patch or leave the test change to be handled during integration.
- **Junio C Hamano** (maintainer): Reviewed patch 1/2 **substantively**, endorsing the core fix and requesting the removal of the redundant test check. Acknowledged patch 2/2 **surface-level**, noting an upcoming merge conflict with an in-flight topic (no action required from Elijah).
- **Patrick Steinhardt**: Reviewed patch 2/2 **substantively**, praising the targeted recovery logic and raising the `OBJECT_INFO_QUICK` question. Feedback was constructive and reinforced the patch’s design; no objections or requests for changes.

Related work: Geometric repacking and MIDX are active areas of development. This fix is a targeted bugfix for a race condition in those subsystems, with no dependencies on other in-flight topics. The noted merge conflict arises from an unrelated topic changing the `packfile_fill_entry()` API. Elijah’s planned follow-up patches will address additional race-condition scenarios.