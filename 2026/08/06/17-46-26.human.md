This is the final confirmation from Junio C Hamano that the `git branch --delete-merged` series is ready for integration. The v25 iteration made no functional changes—only wrapping long test command lines in patch 1/7 for readability—and Junio has now explicitly asked whether the topic can be marked for `next`. With both Junio and Phillip Wood having confirmed the series is technically complete and free of blocking issues, the topic is officially **queued in `next`** for integration testing. Expect it to graduate to `master` in the current cycle unless unexpected interactions surface during cooking.

The series delivers a well-motivated, thoroughly tested feature that enables safe automated cleanup of local branches merged upstream, with precise control via repeatable upstream patterns, per-branch opt-out, and `--dry-run` preview. All safety mechanisms—checked-out branches, missing upstreams, push divergence, and order-independent stacked-branch protection—are implemented and verified. The flags-based API refactoring (patches 2–4/7) resolves the maintenance risk of local boolean caching, and the test suite covers every edge case, including the corrected push-detection logic and simplified stacked-branch protection. No further changes are expected before merging to `master`.