This is a **substantive review response** in the ongoing NonStop-specific textconv regression thread. Jeff King (Peff) engages directly with Randall S. Becker’s proposed `%f` placeholder workaround, identifying a critical technical obstacle: Git’s textconv caching system, which currently uses only the blob ID as the cache key, would produce incorrect results if the filter’s output depends on the original file path (via `%f`) rather than just the blob content.

Peff’s feedback is pragmatic and constructive. He does not dismiss the `%f` idea outright but flags the caching issue as a blocker that any implementation must address—even if the solution is simply disabling caching for filters that use `%f`. His tone suggests the proposal is technically feasible but requires careful handling of edge cases, particularly around diffing historical trees where the working-tree file may not match the blob being converted.

He also gently steers the discussion back toward fixing the root cause (the empty temp files on NonStop) rather than layering on workarounds, though he does not propose a specific fix himself. The email leaves the door open for either path—implementing `%f` with caching fixes or diagnosing the platform-specific I/O bug—but makes it clear that the caching problem is non-negotiable for any `%f`-based solution. Future replies will likely need to address this constraint to move the proposal forward.