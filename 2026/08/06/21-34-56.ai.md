This is a surface-level review from Junio on the v3 `git repack --drop-filtered` series. The email points out two test script style violations in `t/t7706-repack-drop-filtered.sh`: bare `grep` commands outside pipelines should use `test_grep` instead. The attached patch fixes these by replacing `grep -q "$BIG" present` with `test_grep ! "$BIG" present` and `grep -q "$SMALL" present` with `test_grep "$SMALL" present`.

The issue is purely mechanical—no behavior change is involved, just alignment with Git’s test script conventions. The fix is trivial and should be uncontroversial. Since this is the only feedback in the email, it does not affect the series’ core functionality or design, but the author will need to incorporate it before the series can be queued. The thread remains focused on the `--drop-filtered` feature for partial clones, with all substantive discussion already addressed in v3.