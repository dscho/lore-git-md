This is Junio’s final sign-off on the `--reset-when-found` feature for `git bisect`. The email is a short, two-line reply to Harald Nordgren’s question about whether other “dead-end” bisect states (like contradictory labels) should also trigger an auto-reset. Junio says no, but adds that the new code structure—where the main bisect logic runs unchanged and the auto-reset happens afterward—is future-proof and makes it easy to add more cleanup actions later. The series is now ready for `next`; no further changes are needed.