Mode: ai

Thread identity
Subject: [PATCH 0/6] receive-pack: use ODB transaction for pluggable packfile writes
Series version: v4 (nine-patch series; addresses all v3 feedback, including ".keep" file placement design, context-aware unpack limit resolution, and include hygiene)
Originating author: Justin Tobler
Thread type: patch series

Core problem or goal
Extend the ODB transaction system to make `git receive-pack` fully backend-agnostic by replacing its hardcoded use of `git index-pack` and `git unpack-objects` (which assume the "files" backend) with a new generic transaction interface, `odb_transaction_write_pack()`. The goal is to enable alternative ODB backends (e.g., reftable, cloud storage) to handle incoming packfiles without being tied to the traditional files backend.

Current status
Under active review; series updated to v4 (nine patches) with all critical design flaws and reviewer feedback addressed. Still cooking in `seen`; no merge status change. Key improvements since v3:
- **".keep" file design**: Fixed the regression introduced in bdee7b3013 (July 2026) where `.keep` files were orphaned after ODB transaction commit. The patch (1/9) updates `index_pack_lockfile()` to accept an explicit `struct odb_source *` parameter, ensuring the `.keep` file is written to the quarantine directory during the transaction but derived from the *final* location in the main ODB. A new test in `t/t5547-push-quarantine.sh` verifies the `.keep` file is generated in the quarantine directory and removed from the main ODB after the push completes. The design now preserves race-avoidance properties by writing the `.keep` file to the main object directory at commit time and removing it during finalization.
- **Context-aware unpack limit resolution**: Extended the `odb_transaction_write_pack()` interface to accept an `enum odb_transaction_flags` parameter (e.g., `ODB_TRANSACTION_RECEIVE`), enabling the backend to resolve the correct configuration (`transfer.unpackLimit`/`receive.unpackLimit` for `git receive-pack`, `fetch.unpackLimit` for future `git fetch-pack` users). The `get_unpack_limit()` helper now takes the flags parameter and defaults to 100. The "files" backend stores the flags in `struct odb_transaction_files` and passes them to `get_unpack_limit()`.
- **Transaction lifecycle management**: Split `odb_transaction_commit()` into `commit()` and `finalize()` phases (patch 2/9), enabling backends to perform deferred cleanup after objects are made visible. The "files" backend uses this to remove ".keep" files during finalization. Added `odb_transaction_commit_and_finalize_or_die()` helper for callers that cannot recover from failures. `git receive-pack` now explicitly calls `finalize()` after reference updates.
- **Include hygiene**: Added a forward declaration for `struct odb_source` in `pack.h` to avoid including `odb/source.h`, addressing Patrick Steinhardt’s surface-level feedback from v3.
- **Explicit ODB source tracking**: Retained the explicit `struct odb_source *` tracking introduced in v3 to avoid fragile reliance on source list ordering. `odb_set_temporary_primary_source()` returns the newly installed source, which is stored in the "files" ODB transaction struct (`quarantine` field).
- **Global state removal**: Eliminated reliance on the global `alt_shallow_file` variable (patch 3/9) and static unpack limit caching, with the unpack limit now resolved lazily via `get_unpack_limit()`.
- **Error reporting and file descriptor passing**: Retained the `strbuf`-based error reporting and explicit file descriptor parameter for the incoming packfile, enabling backend-agnostic use.

Key technical details
- Files touched: `builtin/receive-pack.c`, `odb-interface.h`, `odb.c`, `object-file.c`, `tmp-objdir.c`, `tmp-objdir.h`, `odb/transaction.c`, `odb/transaction.h`, `fetch-pack.c`, `pack-write.c`, `pack.h`, `t/t5547-push-quarantine.sh` (enhanced test), plus builtins updated for transaction lifecycle changes (`add.c`, `unpack-objects.c`, `update-index.c`, `cache-tree.c`, `read-cache.c`).
- New or renamed symbols: `enum odb_transaction_flags` (`ODB_TRANSACTION_RECEIVE`, `ODB_TRANSACTION_FETCH`), `get_unpack_limit()` (signature change: now takes `enum odb_transaction_flags`), `odb_transaction_write_pack()` (signature change: now takes `enum odb_transaction_flags`), `index_pack_lockfile()` (signature change: now takes `struct odb_source *` instead of `struct repository *`), `struct odb_transaction_files` (new field: `flags`), `odb_set_temporary_primary_source()` (unchanged), `tmp_objdir_replace_primary_odb()` (unchanged), `odb_transaction_commit_and_finalize_or_die()` (new helper), `odb_transaction_finalize()` (new function), `odb_transaction_commit()` (now split into `commit()` and `finalize()`), `struct unpack_opts` (unchanged), `strbuf`-based error reporting in `unpack()`/`unpack_with_sideband()` (unchanged), explicit `int pack_fd` parameter in `unpack()`/`parse_pack_header()` (unchanged).
- Old-vs-new behavior: No user-visible behavior change; the series is a refactor to enable pluggable ODB backends. The unpack limit is now resolved lazily in the backend via `get_unpack_limit()`, which reads `transfer.unpackLimit` and `receive.unpackLimit` (with the latter taking precedence) for `git receive-pack` and will use `fetch.unpackLimit` for `git fetch-pack` in the future. The ".keep" file placement now correctly preserves race-avoidance properties by writing the file to the main object directory at commit time and removing it during finalization. The global `alt_shallow_file` variable is removed, with the shallow file path now passed explicitly through the call chain.
- On-disk format changes: None.
- Test coverage: Enhanced test in `t/t5547-push-quarantine.sh` verifies the ".keep" file is generated in the quarantine directory and removed from the main ODB after the push completes; existing tests cover the refactored shallow file handling, unpack limit resolution, `unpack_opts` struct usage, `strbuf`-based error reporting, explicit file descriptor passing, explicit ODB source tracking, and the new `odb_transaction_write_pack()` interface.

Open questions and loose ends
- **Error reporting ergonomics**: The `strbuf`-based error reporting (patch 6/9) and explicit file descriptor passing (patch 7/9) remain part of the interface; reviewers may still evaluate their ergonomics, though the changes are uncontroversial.
- **Struct initialization and mutation patterns**: The ternary initialization of `fsck_objects` and late assignment of `err_fd` in `unpack_with_sideband()` are preserved as trade-offs for removing global state. The `unpack_opts` struct initialization (patch 5/9) follows this pattern, which remains a minor ergonomic trade-off for clarity and testability.
- **Future source-tracking design debt**: The explicit ODB source tracking (patch 8/9) addresses the immediate fragility of relying on `repo->objects->sources[0]`, but the broader design of how transactions interact with multiple ODB backends may require further refinement as the ODB abstraction effort matures.

Participant positions
- **Justin Tobler (author)**: Implemented all feedback from v3, including the agreed-upon ".keep" file design, context-aware unpack limit resolution via `odb_transaction_flags`, transaction lifecycle management (split `commit()`/`finalize()`), include hygiene fixes, and removal of the global `alt_shallow_file` variable. The v4 series fixes the `.keep` file cleanup regression introduced in bdee7b3013, ensuring the file is written to the quarantine directory during the transaction but derived from the final location in the main ODB. **Engaged with Patrick Steinhardt’s substantive design feedback** and implemented the proposed changes, signaling readiness for merging.
- **Patrick Steinhardt (ODB abstraction lead)**: Provided substantive technical feedback in v3 on the ".keep" file design, unpack limit resolution context, and transaction lifecycle. **Approved the design and implementation of patches 2/9 and 8/9 in v3**, signaling alignment with the broader ODB abstraction goals. **Engaged with Justin Tobler’s proposed fixes** (context-aware unpack limit resolution via transaction flags, ".keep" file placement design, split `commit()`/`finalize()`, and global state removal) and signaled agreement with the approach. Surface-level feedback in v3 (include hygiene) was addressed in v4.
- **Junio C Hamano (maintainer)**: Reviewed critical patches in v2, identifying the lockfile lifecycle flaw, misuse of `repo->objects->sources[0]`, and transaction lifecycle issues. All issues were addressed in v3 and retained in v4.

Related work
- Part of the ongoing ODB abstraction effort (Patrick Steinhardt).
- Depends on prior ODB transaction infrastructure (already merged, bdee7b3013). The lockfile lifecycle flaw fixed in patch 1/9 was introduced in this commit.