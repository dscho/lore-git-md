This is the ninth patch in the v7 series optimizing `paint_down_to_common()` for one-sided histories. It refactors the generation-number cutoff check by moving it into `paint_queue_get()`, consolidating all termination conditions (stale entries, side exhaustion, and `min_generation`) in one place. The change also hoists `last_gen` into the `paint_state` struct so that `commit_graph_generation()` is called exactly once per dequeued commit, eliminating redundant calls and sharing the result across termination checks and the monotonicity BUG assertion.

The patch is a mechanical cleanup with no intended behavior change. It touches only `commit-reach.c`, moving 13 lines of logic into the helper function and adding two fields (`min_generation` and `last_gen`) to `paint_state`. The diff is straightforward: the `min_generation` check and BUG assertion are lifted from the main loop into `paint_queue_get()`, and the `last_gen` variable is promoted to a struct field. All references to `commit_graph_generation(commit)` are replaced with the cached `generation` value.

This patch is part of the larger effort to optimize merge-base queries for asymmetric histories. It follows the introduction of the `paint_state` struct (patch 7/8) and precedes the final optimization (patch 10/10, which implements early termination). The series is queued in Junio’s tree and procedurally unblocked, with review bandwidth the remaining bottleneck. The change is well-motivated: it improves code clarity by centralizing termination logic and reduces redundant work by caching the generation number. No test failures or regressions are expected.