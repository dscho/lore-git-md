This is a brief follow-up to Junio’s "What’s cooking" report about the `ps/cat-file-remote-object-info-type` topic. The author, Pablo Sabater, notes that the base topic (`ps/cat-file-remote-object-info`) has now graduated to `master`, so the type-extension series no longer needs to be merged on top of the earlier version. The patch can now proceed directly on `master` without the merge requirement. This is a straightforward procedural update rather than a technical change to the patch itself.