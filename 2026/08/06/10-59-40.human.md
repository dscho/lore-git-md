This is the ninth patch in the v7 series optimizing `paint_down_to_common()` for one-sided histories. The patch consolidates the `min_generation` termination check into `paint_queue_get()`, moving it alongside the existing stale-entry and side-exhaustion checks. It also migrates `last_gen` into the `paint_state` struct, ensuring `commit_graph_generation()` is called exactly once per dequeued commit and the result is reused across all termination conditions and the monotonicity BUG assertion.

The change is a clean refactoring that eliminates redundant generation-number lookups and centralizes termination logic, improving code clarity without altering behavior. The diff is small (18 insertions, 13 deletions) and focused on `commit-reach.c`. The patch is part of a well-reviewed, ready-to-merge series that has already addressed all substantive feedback; this iteration is queued in Junio’s tree and awaiting additional reviewers to advance.

If you care about merge-base performance in repositories with asymmetric histories (e.g., import grafts or shallow clones), this series is worth tracking—it delivers 100-1000x speedups for those cases. This specific patch is a minor but logical step in the larger effort.