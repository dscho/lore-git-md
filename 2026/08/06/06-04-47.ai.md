**Summarizer brief mode**

This is a one-line follow-up to the third patch in Patrick Steinhardt’s six-patch ODB abstraction series. The patch moves handling of `GIT_OBJECT_DIRECTORY` and `GIT_ALTERNATE_OBJECT_DIRECTORIES` environment variables into `odb_new()`, making the function self-contained. In the review, Toon Claes identified a redundant `xstrdup_or_null()` call for `o->alternate_db` (and the corresponding `free()` later) because `secondary_sources` is already duplicated or NULL at this point. Patrick Steinhardt acknowledges the redundancy and will remove the unnecessary allocation in the next iteration. The change is purely mechanical and does not affect the patch’s core purpose or behavior. Key symbols: `odb_new()`, `enum odb_new_flags` (`ODB_NEW_HONOR_ENV`), `o->alternate_db`, `xstrdup_or_null()`. Files touched: `odb.c`. The patch remains uncontroversial and ready for merging, pending the revised fourth patch in the series.