This thread is about Vincent Mailhol’s patch series adding Bash completion for the new `git history` command. The series is now complete with the fourth patch, which implements pathspec completion for the `split` subcommand. This final piece ensures users get file completions after the required revision argument, matching Git’s standard behavior for other commands.

The series has evolved through two versions, with v2 addressing feedback on revision handling, `--` termination, and test coverage. The implementation is clean and idiomatic, using Git’s existing completion infrastructure without introducing new CLI options or on-disk changes. Tests in `t/t9902-completion.sh` cover all subcommands, options, and pathspecs, including edge cases like option suppression after `--`.

No substantive reviews have landed on v2 yet, but the series appears uncontroversial and ready for integration. It depends on the earlier `git history` command series, which is presumably already merged or under review elsewhere. The completion now fully supports `git history` and its subcommands, including `--empty` (with values like "drop" and "keep") and `--update-refs` (with values like "branches" and "head").