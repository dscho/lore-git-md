This is a test patch, part 3/10 in the v7 series optimizing `paint_down_to_common()` for one-sided histories. The series is queued in Junio’s tree and ready for additional review.

Elijah Newren adds four new test cases to `t6600-test-reach.sh` to exercise edge-case behavior in the side-exhaustion optimization. The tests cover scenarios that the existing 10×10 grid topology does not: a commit that is both the query subject and one of the inputs (`in_merge_bases_many:self`), duplicate entries in the input list (`duplicate-twos`), a STALE transition on an already-painted commit (`pending-stale`, using a diamond topology labeled `ps-*`), and a case where both query tips lie outside the commit-graph and have non-monotonic dates (`infinity-both-sides`, using a `pi-*` topology with deliberately skewed timestamps). Each test verifies that the merge-base calculation returns the correct commit(s) across all commit-graph modes (none, full, half, no-gdat).

The patch is purely additive, touching only the test script. It introduces no new symbols or behavioral changes, but the test topologies and assertions are essential for validating the correctness of the core optimization in later patches. The `pending-stale` and `infinity-both-sides` cases, in particular, exercise code paths that were previously untested and could have hidden regressions. The commit message and in-code comments clearly explain the purpose of each topology and the specific edge case it targets.

This is a substantive contribution to the series’ test coverage. The tests are well-motivated, clearly documented, and cover real-world edge cases that could arise in repositories with shallow histories or import grafts. They look like a necessary part of the series’ hardening effort.