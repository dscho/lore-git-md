This is a reply in the `writev(3p)` wrapper revival series, now entering its final design phase. Patrick Steinhardt responds to Junio C Hamano’s resolution of the API naming and atomicity concerns by proposing a concrete implementation tweak: restore the non-interleaving property of the emulated `writev()` by writing only the first `iovec` in the compatibility wrapper. This would match the behavior of `write(3p)` (which is already non-interleaving) and avoid introducing interleaving even on platforms where `writev(2)` is not available. The proposal is technically straightforward—just a one-line change in the compatibility wrapper—and would not reduce performance, since the wrapper already loops over `write(2)` calls. It directly addresses Johannes Sixt’s forward-compatibility concern about future contributors assuming atomicity, while preserving the performance gains of batching. No new technical objections are raised; the thread is now focused on whether this minor implementation detail should be adopted before the series is finalized.