This is a **substantive review** of the first patch in Johannes Schindelin’s v2 series widening size fields in Git’s delta/pack machinery to `size_t`. The reviewer, Patrick Steinhardt, acknowledges the patch’s correctness but flags a temporary inconsistency: the return type of `sizeof_delta_index()` is widened to `size_t` here, yet at least one caller still assigns its result to an `unsigned long`. This mismatch will be resolved by the end of the series, but the reviewer’s comment serves as a reminder that the patch is part of a coordinated twelve-patch chain and should not be evaluated in isolation. The observation is purely informational—no action is requested, and the series remains uncontroversial and ready for integration. Files touched: `delta.h` and `diff-delta.c`; no behavior or on-disk changes.