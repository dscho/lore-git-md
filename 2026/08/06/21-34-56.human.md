This is a surface-level review from Junio on the v3 `git repack --drop-filtered` series. The email points out two test script style violations in `t/t7706-repack-drop-filtered.sh`: bare `grep` commands outside pipelines should use `test_grep` instead. The attached one-line patch fixes both instances. These are minor style nits that do not affect functionality but are required for test suite consistency.

The series itself remains a well-structured feature addition for partial clones, allowing users to reclaim disk space by safely dropping large promisor blobs. The v3 iteration addressed all substantive feedback from earlier rounds, including a subtle bitmap validation edge case and clearer documentation framing the safety guards as UX optimizations rather than correctness requirements. Junio’s feedback here is purely mechanical and does not reopen any of the previously settled design questions (e.g., the time-based heuristic alternative or the promisor-object enumeration mechanism).

If Siddharth Shrimali sends a v4 to fix these test script nits, the series will likely be ready for Junio to queue in `next`. The feature’s core value—disk space reclamation in partial clones without risking data loss—is uncontroversial, and the implementation now meets the project’s coding and testing standards. Reviewers with repack or promisor expertise (e.g., Patrick Steinhardt or Christian Couder) may still weigh in on the deferred follow-up work (drop log, remote verification), but those are explicitly scoped as future improvements.