This is a brief follow-up in the ongoing `git rebase --update-refs` symref bugfix thread. Junio C Hamano acknowledges that the earlier design debate—whether Git should allow HEAD to point outside `refs/heads/`—is now settled: real-world tools like TopGit actively rely on this flexibility, so the current patch’s robust approach (handling symrefs anywhere under `refs/`) will stand. He also notes that Erik Cervin-Edin’s report of a latent bug (clobbered `head_ref` buffer during HEAD resolution) remains the last open technical issue before the series can progress. The thread is now focused on preparing v4 of the two-patch series, which will incorporate these refinements. No new objections or design changes are introduced here.