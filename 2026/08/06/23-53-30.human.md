This is a minor procedural exchange in the `ps/cat-file-remote-object-info-type` thread. Pablo Sabater notes that the base topic (`ps/cat-file-remote-object-info`) has now graduated to `master`, so the extension series no longer needs to be based on it. Junio C Hamano clarifies that he deliberately retains the same synthetic merge base across iterations to simplify diffing between versions (`git diff @{1} @{0}`), even though the base is no longer strictly necessary. He also acknowledges that the commit count in the "What's cooking" report includes synthetic merge bases, which may mislead readers about the actual number of contributor patches, and considers adjusting the tooling to report only contributor patches in the future.

The exchange is purely logistical and does not affect the technical content or review status of the series. The `ps/cat-file-remote-object-info-type` topic remains in `seen`, awaiting review, and can now be rebased directly onto `master` if desired. The key takeaway for readers is that the synthetic merge base is a tooling convenience, not a technical requirement, and the commit count in the report may overstate the number of actual patches.