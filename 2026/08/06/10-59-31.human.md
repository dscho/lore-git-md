This is the ready-to-merge v7 iteration of a performance optimization series targeting Git’s merge-base machinery. The series teaches `paint_down_to_common()` to terminate early when one side of a merge-base query exhausts its commit queue, eliminating unnecessary traversal of large one-sided histories (e.g., repositories with import grafts or shallow histories). Benchmarks show **100-1000x speedups** for asymmetric queries, with no regressions on smaller repositories like git.git.

The series is **technically complete and procedurally unblocked**, having resolved its dependency (`kk/commit-reach-find-all-fix`) and received Junio’s confirmation of the synthetic base approach. It is now queued in Junio’s tree, awaiting additional review bandwidth to advance. Kristofer Karlsson (the primary driver) has addressed all substantive feedback from prior rounds, including Elijah Newren’s documentation concerns and René Scharfe’s cosmetic test suggestion. The only remaining action is broader review to move the series forward.

Key changes since v6 include a **topological ceiling** for v1 commit-graph support, ensuring saturated generation numbers (which break ordering guarantees) are treated as unordered, and a minor test helper refinement (using `$LF` instead of a literal newline). The series also removes the commit-date ordering fallback entirely, as the side-exhaustion optimization renders it unnecessary and its removal simplifies invariants. This cleanup is a significant hardening step, though it could be deferred if scope concerns arise.

The patch structure remains logical: documentation first (describing the existing algorithm), followed by tests (demonstrating correctness before and after changes), and finally the core logic. The commit messages are thorough, with concrete benchmarks and edge-case examples. The inclusion of Trace2 instrumentation and deterministic step-count assertions in tests provides robust regression detection. If you work with large repositories or care about merge-base performance, this series is worth reviewing—it’s a rare win-win for both speed and correctness.