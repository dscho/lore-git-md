This is the documentation patch for the `git repack --drop-filtered` feature, the final part of Siddharth Shrimali’s seven-patch v3 series. It adds 37 lines to `Documentation/git-repack.adoc`, describing the new `--drop-filtered` and `--dry-run` options in detail.

The text explains what the options do: `--drop-filtered` removes locally cached promisor blobs that match the `--filter` specification (currently only `blob:limit=<n>`), reclaiming disk space while ensuring recoverability via lazy fetch. It documents the preconditions (`-a` required, promisor remote required, `--filter-to` and bitmaps incompatible), the implied `-d` behavior, and the silent disabling of config-driven bitmaps. The safety guards—refusing to run during ongoing operations (merge, rebase, etc.) or to drop index-referenced blobs—are presented as UX optimizations, with clear error messages and bare-repository exceptions. `--dry-run` is documented as a preview mode that lists candidate OIDs without making changes.

The patch is straightforward and complete: it covers all constraints, incompatibilities, and edge cases mentioned in the series, and the language aligns with the v3 reframing of the safety guards as optimizations rather than correctness measures. No loose ends remain here; the documentation is ready for integration.