This is a minor test portability patch in Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder. The patch replaces `wc -c <two.t | xargs` with Git’s `test_file_size()` helper in `t/t5701-git-serve.sh`, as previously suggested by Junio C Hamano during v1 review.

Junio’s reply here is not a rejection but a request for clarification: the commit message should explain *why* the change is beneficial, not just *what* it does. The current message states the mechanical change but omits the motivation. Junio points out that the original pipeline already discards exit status (from both `git rev-parse` and `wc -c`), so the helper’s exit-status-preserving behavior isn’t the reason. He suggests that if the goal were to preserve exit status or avoid redundant command invocations, the test could be restructured to capture values in variables first — but the patch doesn’t do that, so the benefit isn’t immediately obvious.

This is a surface-level review comment: Junio is asking for a clearer commit message, not questioning the substance of the change. The patch itself is uncontroversial and aligns with established test suite practice (the `test_file_size()` helper is used elsewhere in the codebase). The series as a whole is complete, addresses all prior feedback, and is ready for maintainer consideration; this is just a small documentation tweak needed before Junio will pick it up.