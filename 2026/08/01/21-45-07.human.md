This is a brief follow-up in the `git add --resolved` thread. Junio confirms that his earlier "blind faith" assumption—that Git's merge machinery refuses to operate on paths with local modifications—is actually backed by explicit test coverage. He cites **Testcase 11b in `t6423`**, which verifies this behavior even in newer merge code paths (like those handling directory renames). This addresses the core of Jeff King's usability concern: the risk of `--resolved` creating a false sense of completeness is mitigated by Git's existing constraints.

The exchange doesn’t reopen the design debate (e.g., Michael Montalbo’s `--unmerged` proposal) or introduce new technical issues. Instead, it reinforces that the series is technically sound and ready for integration, with the remaining loose end being whether `git-add.txt` or the command’s output should explicitly warn users about the limitation (staging only conflict-resolved files while ignoring related changes). No action is required from reviewers beyond noting the clarification.