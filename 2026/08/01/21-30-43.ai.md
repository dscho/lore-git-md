This is a brief follow-up in Pablo Sabater’s GSoC v2 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder for remote-object-info queries. The email responds to Chandra Pratap’s surface-level nit on the fifth patch, which teaches the server to advertise the "type" capability.

Pablo acknowledges Chandra’s suggestion to use double quotes (`echo "$hello_type"`) instead of unquoted variable expansion (`echo $hello_type`) in the test case, even though the variable currently contains a simple string ("blob") with no whitespace. The change is a best practice for shell scripting robustness and aligns with Git’s conventions. Pablo confirms he will incorporate the fix in the next revision.

The patch itself remains uncontroversial and mechanically correct. It updates the test suite to expect the actual object type ("blob") for `%(objecttype)` instead of an empty string, reflecting the new server capability, and adds a new test to verify that unsupported placeholders (e.g., `%(objectmode)`) still return empty strings. This is the last piece needed before the client can request and display object types from the server. The series is otherwise complete, with all prior review feedback addressed, and is ready for maintainer consideration pending Pablo’s implementation of Junio’s earlier feedback on patch 4’s memory allocation strategy and this minor test quoting fix. No substantive discussion or objections are raised in this exchange.