This is Junio C Hamano following up on his own review of the `--reset-when-found` feature in the Git bisect subsystem. The email proposes a significant refactoring of the patch’s internal implementation to simplify the state machine and eliminate the `defer_reset` parameter introduced to handle platform-specific file-descriptor constraints.

Junio’s alternative design would centralize the auto-reset logic in the top-level `cmd_bisect()` function rather than scattering it across subcommands and plumbing. The key changes would be: (1) stop calling `bisect_clean_state()` from `bisect_reset()`, (2) make all `cmd_bisect__<subcommand>` functions return their result codes instead of calling `exit()`, (3) have `bisect_next_all()` report the found commit via a file-scope static variable, and (4) handle the auto-reset in `cmd_bisect()` when it receives `BISECT_INTERNAL_SUCCESS_1ST_BAD_FOUND`. This approach would eliminate the need for the `defer_reset` parameter and the repeated filesystem checks (`is_empty_or_missing_file(git_path_bisect_reset_when_found())`), making the code easier to follow.

The proposal is framed as a question ("Hmm?") and is purely about internal code organization; it does not challenge the feature’s user-facing behavior or its readiness for `next`. Junio’s tone is collaborative, and the feedback is specific enough to be actionable. The patch remains marked for integration into `next`, but this email may prompt a follow-up patch if the author or another contributor agrees the refactoring would improve maintainability. The discussion is now focused on implementation trade-offs rather than the feature’s design or necessity.