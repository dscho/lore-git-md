This is the first patch in the **v4 series adding `--reset-when-found` to `git bisect`**, a preparatory plumbing change. It adds a `quiet` parameter to `bisect_reset()` so the upcoming `--reset-when-found` feature can suppress checkout noise when automatically resetting the working tree. The patch is small and mechanical: it modifies the function signature, updates all callers to pass `0` (false), and wires the `--quiet` flag into the underlying `` `git checkout` `` command. No behavior changes yet; this is just infrastructure for the next patch.

The change is confined to `builtin/bisect.c` and touches only the checkout logic inside `bisect_reset()`. The diff is clean and straightforward, with no new symbols or on-disk state. All existing callers are updated to preserve current behavior, so the patch is safe to merge on its own. This is a classic example of a well-scoped refactoring that enables a larger feature without breaking anything.

Given the thread context, this patch is uncontroversial and ready for integration. It directly addresses Johannes Sixt’s earlier feedback about suppressing unnecessary output during auto-reset, and the series as a whole is already marked for `next`. If you care about the `--reset-when-found` feature, this patch is worth a quick skim to confirm the plumbing is sound; otherwise, it’s safe to skip and wait for the feature patch itself.