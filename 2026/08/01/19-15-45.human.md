This is the **first patch in the v4 series** introducing the `--reset-when-found` option to `git bisect`. Junio C Hamano replies to Harald Nordgren’s patch, suggesting a minor consistency tweak: since the next patch in the series adds a `bool defer_reset` parameter to the same function, the new `quiet` parameter should also be a `bool` rather than an `int` for stylistic uniformity.

The patch itself is a straightforward refactoring that adds a `quiet` flag to `bisect_reset()`, allowing the function to suppress checkout output when the flag is set. All existing callers pass `0` (false), so the change is purely preparatory and does not alter behavior. Junio’s feedback is surface-level and does not question the patch’s correctness or design; it is a stylistic preference to align the new parameter’s type with the one introduced in the next patch. The patch remains uncontroversial and ready for integration into `next`.