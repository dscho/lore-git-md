**Summarizer brief mode**

This is a follow-up in the `git add --resolved` thread, specifically Junio C Hamano’s reply to Jeff King’s (Peff) usability concern about the feature’s potential to mislead users. Junio confirms that Git’s merge machinery (including the "ort" strategy) already enforces the constraint that merges refuse to operate on paths with local modifications, which mitigates Peff’s concern about `--resolved` staging incomplete resolutions. He initially described his confidence in this behavior as "blind faith" but later cites a test case (`t6423`, "Testcase 11b") that explicitly validates the constraint, reducing the uncertainty.

The email is narrowly technical and does not revisit the broader design debate (e.g., Michael Montalbo’s `--unmerged` proposal). It leaves the usability risk unresolved in terms of documentation or command output but frames it as a non-blocking concern. Key details for future discussion:
- The constraint is enforced by `read-tree -m -u` and newer merge code paths (e.g., directory rename handling).
- Test case `t6423` ("Testcase 11b") validates the behavior, providing concrete evidence.
- The thread remains focused on the `--resolved` implementation; no new technical issues are raised.
- The series is effectively ready for integration, pending minor documentation tweaks to clarify the feature’s limitations (e.g., staging only conflict-resolved files while ignoring related changes).

Files/subsystems referenced: `t/t6423-merge-rename-directories.sh` (test case), merge machinery (implicitly `merge-ort.c`, `read-tree.c`). No new code or options are introduced. The discussion underscores the need to document the feature’s behavior in `git-add.txt` or command output.