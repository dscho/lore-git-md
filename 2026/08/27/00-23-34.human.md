This is a **bugfix** patch update (v2) from Elijah Newren, refining the protection against `git commit --amend` during conflict resolution. The patch now consolidates logic into a new helper function, `ongoing_operation()`, in `builtin/commit.c` to improve maintainability while preserving the separation between merge and sequencer logic. This addresses Phillip Wood’s feedback about structural organization without fully adopting his proposed refactor into `sequencer_determine_whence()`.

The patch remains focused on its original goal: preventing users from accidentally amending the wrong commit during conflict resolution for `git am`, `git revert`, and all forms of `git rebase`, while allowing legitimate `--amend` usage in interactive rebase directives like `squash`, `reword`, `edit`, and `break`. Junio C Hamano’s suggestion to extend the same principle to `git commit <paths>` during conflict resolution is acknowledged but deferred to a future topic, with a `#leftoverbits` marker left in the code.

This version looks well-crafted and uncontroversial. The new helper function improves code clarity without introducing behavioral changes, and the patch maintains thorough test coverage across `t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, and `t4151-am-abort.sh`. If Junio’s minor cleanup (replacing `the_repository` with `s->repo`) is incorporated, this patch seems ready for `next`. The broader discussion about `git commit <paths>` during conflict resolution is noted but does not block this fix.