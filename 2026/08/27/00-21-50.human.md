This is a **bugfix patch update** in an ongoing thread about preventing `git commit --amend` during conflict resolution. Elijah Newren responds to Phillip Wood’s substantive review with a revised approach that addresses the structural concerns raised.

Elijah agrees with Phillip’s suggestion to centralize the conflict-resolution checks but opts for a new helper function (`ongoing_operation()`) rather than modifying `sequencer_determine_whence()`. This keeps the merge machinery separate while still avoiding hardcoded paths, a compromise that maintains modularity without duplicating logic. The new helper consolidates checks for `git am`, the apply-based rebase backend, and the sequencer (revert/rebase), which should make the code more maintainable and extensible.

The patch remains focused on its original goal: blocking `--amend` during conflict resolution for `git am`, `git revert`, and all `git rebase` variants, except when explicitly required by interactive rebase directives like `squash` or `reword`. No new CLI options or on-disk format changes are introduced. The implementation is now cleaner, though it still leaves room for future work (e.g., addressing `git commit <paths>` during conflict resolution, as Junio C Hamano noted earlier).

This update looks like a well-considered refinement of an uncontroversial bugfix. The structural improvements make the patch more robust without altering its behavior, and the discussion suggests it’s on track for eventual merging. If you care about merge machinery safety or conflict-resolution edge cases, this is worth a quick skim; otherwise, it’s safe to skip unless you’re following the thread closely.