This is a substantive architectural discussion in the ongoing `the_repository` removal thread, specifically Junio C Hamano’s reply to D. Ben Knoble’s question about libification. Junio clarifies the project’s stance on replicating built-in command behavior via libgit: rather than exposing `cmd_foo()` functions as reusable entry points, the correct approach is to factor out the reusable logic from built-in implementations (e.g., `cmd_checkout()`) into libified utility functions, then have both the original command and any new consumers (like `cmd_switch()`) call those shared routines. This reinforces the architectural direction introduced in his earlier patch, which replaces the `struct repository *` parameter in built-in commands with a `bool has_repo` flag, and redirects the `the_repository` removal effort toward utility code outside `builtin/`.

The email is short but significant: it resolves Ben’s open question by reaffirming that built-in commands are not intended for direct reuse, and it provides a concrete technical path forward (factoring out reusable logic) that aligns with the project’s broader architectural goals. This response effectively closes the libification debate in this thread, leaving the remaining open questions focused on community consensus for Junio’s `bool has_repo` proposal and coordination with other contributors (e.g., René Scharfe, Patrick Steinhardt) on the redirected `the_repository` removal effort. No new patches or technical details are introduced, but the clarification strengthens the case for Junio’s architectural shift.