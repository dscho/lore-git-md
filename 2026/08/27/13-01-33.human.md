This is a review reply in the `git checkout -m` autostash conflict-handling series. Phillip Wood, a seasoned reviewer with deep knowledge of the sequencer and rebase machinery, responds to the first patch, which adds a `conflicted` output parameter to the sequencer’s autostash plumbing.

Phillip’s feedback is concise and substantive: he suggests using the function’s return value to encode success, error, or conflict status instead of adding another parameter. The patch itself is otherwise well-structured—it threads the new parameter through the call chain, updates all existing callers to preserve backward compatibility, and sets the `conflicted` flag when stash application fails. Phillip’s alternative design would reduce API clutter but would require callers to distinguish between error and conflict cases, which may complicate error handling elsewhere.

The thread is still under review; Junio has already accepted the second patch (which uses this plumbing) but has not yet weighed in on this design choice. If Harald revises the patch to adopt Phillip’s suggestion, the series could move closer to integration.