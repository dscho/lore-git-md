Thread identity: patch series titled “git-svn: quiet migration noise and make build optional”, v1, authored by Wesley Schwengle.

Core problem or goal: eliminate two pain points in `git-svn` usage: (1) spurious “Migrating from a git-svn v1 layout…” messages and empty `.git/svn` directory creation when running `git svn info` in a plain Git repository, and (2) inability to opt out of building `git-svn` when SVN support is not needed.

Current status: under review; v1 posted 2025-06-10. No prior versions. No maintainer queue status reported.

Key technical details:
- Files touched: `perl/Git/SVN/Migration.pm`, `Makefile`, `t/t9107-git-svn-migrate.sh`.
- New Makefile knob: `NO_GIT_SVN=YesPlease` (disables build, install, and tests; introduced in patch 2/2).
- New symbol: none (behavior change only).
- On-disk format: unchanged.
- Test coverage: one new test in `t/t9107` verifies the migration message is suppressed and the `.git/svn` directory is not created when no SVN metadata exists.
- Patch 1/2 modifies `migrate_from_v1()` in `perl/Git/SVN/Migration.pm` to defer the migration message and directory creation until a legacy `refs/remotes/*` candidate is found, mirroring the existing `migrate_from_v0()` pattern. Junio’s review identified a minor typo in the error message (“git-svnexists” → “git-svn exists”), which is likely to be addressed in a follow-up.

Open questions and loose ends:
- The typo in the error message (“git-svnexists”) remains unresolved; Wesley may address it in a v2 or as a follow-up.
- No substantive technical objections have been raised, but the behavioral change has not yet received in-depth review beyond Junio’s surface-level feedback.

Participant positions:
- **Junio C Hamano**: surface-level review; noted the typo in the error message but did not engage with the behavioral change itself. Position: neutral to positive, pending the typo fix.

Related work: none mentioned.