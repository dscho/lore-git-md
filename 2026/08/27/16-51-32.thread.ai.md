Thread identity: patch series titled “git-svn: quiet migration noise and make build optional”, v2, authored by Wesley Schwengle.

Core problem or goal: eliminate two long-standing `git-svn` pain points: (1) spurious “Migrating from a git-svn v1 layout…” messages and empty `.git/svn` directory creation when running `git svn info` in a plain Git repository, and (2) inability to opt out of building `git-svn` when SVN support is not needed.

Current status: under review; v2 posted 2025-06-10. The v2 iteration addresses the only feedback received on v1: a typo in an error message (“git-svnexists” → “git-svn exists”). No maintainer queue status reported; the series remains uncontroversial and has not attracted substantive technical objections.

Key technical details:
- Files touched: `perl/Git/SVN/Migration.pm`, `Makefile`, `t/t9107-git-svn-migrate.sh`.
- New Makefile knob: `NO_GIT_SVN=YesPlease` (disables build, install, and tests; introduced in patch 2/2). When set, it also implies `NO_SVN_TESTS`, skipping SVN interoperability tests.
- New symbols: none (behavior change only).
- On-disk format: unchanged.
- Test coverage: one new test in `t/t9107` verifies the migration message is suppressed and the `.git/svn` directory is not created when no SVN metadata exists.
- Patch 1/2 modifies `migrate_from_v1()` in `perl/Git/SVN/Migration.pm` to defer the migration message and directory creation until a legacy `refs/remotes/*` candidate is found, mirroring the existing `migrate_from_v0()` pattern.
- Patch 2/2 adds the `NO_GIT_SVN` knob to the Makefile, guarding the `git-svn.perl` script inclusion and automatically setting `NO_SVN_TESTS` when enabled.

Open questions and loose ends:
- No open technical objections or unresolved requests for changes. The typo in the error message has been fixed in v2.

Participant positions:
- **Junio C Hamano**: surface-level review on v1; noted the typo in the error message but did not engage with the behavioral change itself. Position: neutral to positive, now satisfied by the v2 fix.
- **Wesley Schwengle**: author; addressed the only feedback received (typo fix) in v2. No further changes planned unless new objections arise.