This is a build system patch that adds a `NO_GIT_SVN` knob to the Makefile, allowing users to skip building or installing `git-svn` entirely. The patch is the second in a two-part series (though the first patch isn’t shown here). Setting `NO_GIT_SVN` also implies `NO_SVN_TESTS`, which skips the time-consuming SVN interoperability tests.

The change is straightforward: it modifies the Makefile to conditionally exclude `git-svn.perl` from the build when `NO_GIT_SVN` is defined and automatically sets `NO_SVN_TESTS` in that case. The commit message is minimal but clear about the intent. This is likely motivated by users who don’t need Subversion integration and want to reduce build time or avoid Perl dependencies.

The patch looks well-scoped and uncontroversial. It follows the existing pattern of other `NO_*` knobs in the Makefile, so it should integrate cleanly. If you don’t use `git-svn`, this is a small but useful optimization.