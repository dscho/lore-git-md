This is a **bugfix follow-up** in an ongoing thread about preventing `git commit --amend` during conflict resolution. Elijah Newren responds to Phillip Wood’s substantive review by proposing a structural refactor: instead of moving the conflict-resolution checks into `sequencer_determine_whence()` (as Phillip suggested), he introduces a new helper function, `ongoing_operation()`, to consolidate the logic for detecting active operations (merge, am, apply-based rebase, and sequencer-based rebase/revert). This keeps the merge machinery separate from the sequencer while avoiding hardcoded paths in `builtin/commit.c`.

The patch remains a **bugfix** targeting the merge machinery, with no new CLI options or on-disk format changes. Key files touched are still `builtin/commit.c` (now with a new helper function) and the same test scripts (`t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, `t4151-am-abort.sh`). Elijah’s approach addresses Phillip’s maintainability concern while preserving the existing separation of concerns between merge and sequencer logic. The thread is still under review, with no objections to the patch’s goal or correctness, but the structural choice may invite further discussion.

No merge status is recorded, and Junio C Hamano’s earlier feedback (about replacing `the_repository` with `s->repo` and future work on `git commit <paths>`) remains unaddressed in this reply. The patch is likely to see a v2 incorporating this refactor and possibly Junio’s cleanup suggestion.