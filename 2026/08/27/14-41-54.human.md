This is the first patch in the v10 iteration of a now-complete four-patch series improving error messages for ambiguous remote branch names in `git checkout`, `git switch`, and `git worktree add`. The series is already merged to `next` and this update addresses final refinements.

This patch extracts and refactors the advice logic for ambiguous remote branches into a new helper function, `advise_disambiguating_remotes()`. The change is purely structural: it moves the existing advice message from `parse_remote_branch()` into a dedicated function, fixes incorrect indentation, and reduces nesting. The extracted function is parameterized by the command (`checkout` or `switch`) to ensure the advice message is command-specific. This refactoring prepares the codebase for later patches in the series, which will reuse this helper in `git worktree add` and extend its functionality.

The commit message is clear and justifies the change as a preparatory step for future extensions. The diff is clean and focused, touching only `builtin/checkout.c`. No behavior changes are introduced; this is a pure refactoring. The patch aligns with Git’s naming conventions by using `advise_` as a prefix (addressing Junio’s feedback from v9) and avoids unnecessary complexity.

This is a well-crafted, uncontroversial patch that sets the stage for the rest of the series. It demonstrates good hygiene by isolating a reusable component and improving code readability. The follow-up patches will build on this foundation to deliver the series’ core usability improvements.