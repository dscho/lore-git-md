This is a **surface-level discussion** about error message phrasing in Elijah Newren’s bugfix series, which blocks unsafe commit operations (`git commit --amend` and `git commit <paths>`) during conflict resolution for `git am`, `git revert`, and all forms of `git rebase`. The series is part of a broader effort to prevent repository corruption by distinguishing between genuine conflict stops (where amending or partial commits would corrupt state) and internal `--amend` usage by interactive rebase directives (`squash`, `reword`, `edit`, `break`).

Junio’s reply asks a **clarifying question** about the logic behind rejecting partial commits during conflict resolution for commits that became empty. He wonders why a partial commit of an empty commit would be problematic, given that the result would still be an empty commit. This is not a technical objection to the series’ correctness—Junio has already confirmed the underlying logic (`whence == FROM_REBASE_PICK`) is sound—but rather a request for better justification in the error message. His question suggests the current phrasing ("while resolving a commit that became empty") may not clearly communicate *why* the restriction exists, leaving users confused about the rationale.

Phillip Wood’s earlier feedback proposed a minor rewording ("while committing a commit that became empty" instead of "while resolving a commit that became empty") for clarity, but Junio’s question indicates the issue runs deeper: the error message should explain *why* partial commits are unsafe in this context, not just *when* they are blocked. This is still a surface-level concern—no code changes are being proposed yet—but it could lead to a more informative error message in a future iteration.

The series itself remains technically uncontroversial and ready for integration. Junio’s question does not challenge the patch’s correctness or necessity, and Phillip Wood has already endorsed the series’ goal ("a useful improvement to our UI"). The open question is purely about user-facing messaging, and resolving it is unlikely to block progress. Key context for follow-up: the error messages now reference "empty commits," and Junio’s question may prompt a more explanatory phrasing in a future version.