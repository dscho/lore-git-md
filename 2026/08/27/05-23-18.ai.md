**Summarizer brief mode**

This is a **substantive review** of Derrick Stolee’s trace2 hardening series, specifically targeting the architectural approach of using `banned-die.h` to block `die()`-triggering helpers. Jeff King (Peff) expands his earlier critique of the first patch to question whether the compile-time ban mechanism is sufficient or even appropriate for the broader goal of eliminating `die()` from trace2. His core argument is that the series focuses on direct calls to banned functions (e.g., `xsnprintf()`, `xstrdup()`), but the real risk lies in *indirect* calls—functions that transitively invoke `die()` through other helpers (e.g., `strbuf`, `json-writer.c`). He points out that `strbuf` is particularly problematic because it both allocates memory and bails on `snprintf()` failures, making it unsafe for trace2 even if it isn’t explicitly banned. His conclusion is that the current approach is a "tip of the iceberg" and may require a ground-up rewrite of trace2 to avoid all dependencies that could trigger `die()`.

Key technical details for follow-up discussion:
- **Subsystems at risk**: `strbuf` (memory allocation + `snprintf()` failures), `json-writer.c` (indirect `die()` calls), and any other helper that transitively invokes banned functions.
- **Mechanism limitation**: `banned-die.h` only catches direct calls to banned functions, not transitive ones, reducing its effectiveness to a "whack-a-mole" exercise.
- **Proposed alternative**: A no-dependency rewrite of trace2 to eliminate all indirect `die()` risks, though Peff does not elaborate on feasibility or scope.
- **Files/context**: The review references `strbuf.h`/`strbuf.c` and `json-writer.c` as examples of unaddressed dependencies, but the critique applies broadly to any code that could transitively trigger `die()`.
- **Behavioral impact**: If trace2 remains dependent on helpers like `strbuf`, allocation or system call failures could still crash Git despite the series’ changes.
- **Open question**: Whether the series should expand its scope to ban or rewrite additional helpers (e.g., `strbuf`), or if the current incremental approach is sufficient despite its limitations.

Peff’s tone is measured but skeptical, framing the issue as a fundamental mismatch between the mechanism and the problem. His review does not reject the series outright but implies that the current approach may be incomplete or misaligned with the goal of making trace2 truly `die()`-free. This could prompt further discussion about whether to:
1. Accept the series as a pragmatic first step, acknowledging its limitations.
2. Expand the ban list to include more helpers (e.g., `strbuf`).
3. Pursue a more radical rewrite of trace2 to eliminate all `die()`-prone dependencies.