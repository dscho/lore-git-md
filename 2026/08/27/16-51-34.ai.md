This is a build system patch introducing a `NO_GIT_SVN` Makefile knob. It allows users to skip building and installing `git-svn` entirely, which also implies skipping the time-consuming SVN interoperability tests (by automatically setting `NO_SVN_TESTS`). The change is minimal: it adds the new knob to the Makefile's configuration documentation, wraps the `git-svn.perl` script inclusion in a conditional block, and ensures `NO_SVN_TESTS` is set when `NO_GIT_SVN` is defined.

The patch is part of a two-patch series (this is the second patch). The commit message is brief but clear about the purpose and side effect. The implementation is straightforward and follows the established pattern for optional components in the Makefile. There are no tests or documentation updates beyond the Makefile itself, which is appropriate for a build-time toggle.

This is likely motivated by environments where Subversion is not needed (e.g., pure Git workflows) and users want to reduce build time or avoid Perl dependencies. The change is uncontroversial and aligns with existing `NO_*` knobs for other optional features. The patch touches only the Makefile and does not affect runtime behavior when the knob is not set.

Key technical details for follow-up discussion:
- New Makefile knob: `NO_GIT_SVN`
- Implicitly sets: `NO_SVN_TESTS`
- Affects: `git-svn.perl` script inclusion and installation
- File modified: `Makefile` (lines 81-84, 744-746, 3408-3411)