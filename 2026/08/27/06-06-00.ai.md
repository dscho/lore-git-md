This is a documentation and usability patch for the `git format-patch` command, part of the `kh/format-patch-range-diff-notes` topic currently in `seen` (per the "What's cooking" report). The patch series introduces a new `--[no-]range-diff-notes` option to let users control whether notes are included in the range-diff output separately from the notes shown on the patches themselves.

The series consists of three commits. The first simplifies the `get_notes_arg` function parameters, the second renames a struct member in `revision.h` to clarify its role in handling notes, and the third adds the new `--range-diff-notes` option. The motivation is straightforward: range-diff output can become cluttered when notes are included by default, and users may want to suppress them for clarity while still keeping notes on the patches. The commit messages explain the changes clearly and include test updates to verify the new behavior.

This is a small but well-scoped feature addition with no obvious downsides. The implementation touches the revision machinery and format-patch plumbing, but the changes are confined to notes handling and should not affect other functionality. The series is marked as "Waiting for response" in the "What's cooking" report, and Kristoffer notes he has already replied to Junio’s earlier query, suggesting the discussion is active but not contentious.

For a future AI summarizer, key details to retain: the new `--range-diff-notes` option, the struct rename (`notes_opt` to `notes_opt_for_range_diff`), the test updates, and the topic’s current status in `seen`. The patch is unlikely to be controversial, but it may need a reroll if Junio’s feedback requires further adjustments.