This is a **substantive review** from Jeff King (Peff) on Derrick Stolee’s seven-part series to harden the trace2 subsystem against crashes caused by `die()`-triggering helpers. Peff’s reply pushes back on the series’ architectural approach, arguing that the proposed compile-time bans (`banned-die.h`) are a superficial fix that misses deeper systemic risks.

Peff acknowledges the goal—preventing trace2 from crashing Git—but calls the current approach "the tip of the iceberg." He points out that the banned functions (`xsnprintf`, `xstrdup`, `ALLOC_ARRAY`, etc.) are just the most obvious offenders; many other Git utilities (like `strbuf` and `json-writer.c`) also allocate memory or call `die()` indirectly. For example, `strbuf` uses `malloc` and bails if `snprintf` fails, while `json-writer.c` could similarly trigger crashes. His critique implies that the series’ incremental bans will devolve into a game of whack-a-mole, where new `die()`-prone helpers are discovered and added to the ban list over time.

The review suggests a more radical solution: a "ground-up no-dependency rewrite" of trace2 to eliminate all allocation risks. This would mean rewriting trace2 to avoid not just the banned functions but *any* code that could allocate memory or call `die()`, even indirectly. Peff doesn’t propose a specific alternative, but his tone suggests the current approach is insufficiently thorough. The implication is that the series, while well-intentioned, may create a false sense of security without addressing the root problem.

This is a **high-weight architectural concern** that could reshape the series’ direction. Peff’s feedback doesn’t reject the patches outright but questions whether they’re the right tool for the job. The thread’s next steps may hinge on whether Derrick (or others) can address this systemic risk or justify the pragmatic trade-offs of the current approach. For now, the series’ viability depends on whether the community views the compile-time bans as a reasonable stopgap or an incomplete solution.