This is the final acknowledgment in Elijah Newren’s **bugfix patch series** that extends Git’s protection against unsafe commit operations during conflict resolution. The series is now effectively complete, with only a **v3 resend** needed to address Phillip Wood’s **substantive maintainability feedback** about redundant hardcoded paths in the new `sequencer_ongoing_operation()` helper.

Elijah confirms he will incorporate Phillip’s suggestions, which means the series will soon be ready for Junio C Hamano to pick up. The changes are uncontroversial, well-motivated, and thoroughly reviewed, with both Junio and Phillip endorsing the approach. The only remaining work is mechanical: replacing hardcoded paths (`rebase-merge/stopped-sha` and `rebase-merge/amend`) with existing helpers (`rebase_path_stopped_sha()` and `rebase_path_amend()`) to improve maintainability and align with established patterns in the sequencer subsystem.

The series blocks both `git commit --amend` and `git commit <paths>` during conflict resolution for `git am`, `git revert`, and all forms of `git rebase`, while carefully preserving the internal use of `--amend` by interactive rebase directives (`squash`, `reword`, `edit`, `break`). It consolidates logic into a new helper function, includes thorough test coverage, and aligns with the `the_repository` removal effort. The patches touch `builtin/commit.c`, `sequencer.c`, `sequencer.h`, and test files, with no on-disk format changes or new CLI options.

This is a **clean, focused bugfix** that closes a long-standing gap in Git’s safety mechanisms. It is likely to be merged soon after v3 is sent.