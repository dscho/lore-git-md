This is a reply in the ongoing `git worktree add` ambiguous remote branch thread, addressing D. Ben Knoble’s substantive review about the unannounced `die()` behavior change.

Yoichi Nakayama clarifies that the patch does *not* introduce a new failure mode: the code path already calls `die()` later in the function when `lookup_commit_reference_by_name()` fails. The patch simply moves the `die()` call earlier, replacing the generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression, because the command was already failing — just with a less helpful message. The commit message should be updated to mention this shift in error timing and the improved diagnostic, but the change itself is safe and aligns with the patch’s stated goal of better user guidance. The thread’s open questions now narrow to whether the advice format (command-based vs. static list) is optimal and whether Junio’s surface-level nits (unnecessary newline, braces) will be addressed.