This is the second patch in a two-patch bugfix series for `git maintenance`, addressing a regression in the `--auto` trigger's handling of promisor packs. The patch updates the auto-condition logic in `builtin/gc.c` to include `geometry.promisor_split` in its evaluation, ensuring the geometric repack task runs when promisor packs are ready for rollup. The change is minimal (a single-line modification) and aligns with the fix introduced in the first patch, which adjusted the repack decision logic for promisor packs.

The test coverage is thorough: it adds a new test in `t/t7900-maintenance.sh` that creates three promisor packs (two small, one large) and verifies the `--auto` trigger activates when the smaller packs should roll up. A minor adjustment in `t/t5331-pack-objects-stdin.sh` disables auto-maintenance in a filtered clone fixture to avoid test interference. Patrick Steinhardt's review confirms the fix is "obviously correct" and approves the test adjustments as sensible.

This patch completes the series' goal of making promisor pack handling consistent across both the repack decision logic and the auto-trigger mechanism. The regression it fixes was introduced in January 2026 (dcc9c7ef47) and affects partial clone repositories. The solution is targeted and well-motivated, with clear test coverage demonstrating the fix. No open questions remain; Patrick's review signals no objections to the approach. This looks like a clean, uncontroversial fix for a real-world issue.