This is a **completion infrastructure review** in the `git history` Bash completion series. D. Ben Knoble weighs in on the ongoing discussion about the maintainability of the `__git_history_has_revision` helper, which hardcodes options like `--empty` and `--update-refs` that take arguments.

Knoble aligns with Vincent Mailhol’s preference for retaining the current helper (option 1) but acknowledges that the simpler, more maintainable approach (option 2—ignoring the `--option <value>` syntax) is consistent with the rest of the codebase. He notes that the Git documentation (`gitcli`) recommends the `--option=value` form, and most completion logic in the codebase already assumes this syntax. This makes option 2 a pragmatic choice, even if it means accepting incorrect completions in rare cases where users omit the equals sign. Knoble suggests starting with option 2 and revisiting the decision only if users complain about the limitation.

He also endorses Vincent’s proposed compromise for handling future subcommands that deviate from the "revision-first" pattern: dispatching them early via a `case` statement before checking for revisions. This approach mirrors how other Git commands (e.g., `git stash`) handle subcommand-specific logic, and Knoble sees it as a reasonable way to accommodate exceptions without overcomplicating the completion logic. The review is substantive but brief, focusing on the trade-offs between correctness, maintainability, and consistency with existing patterns. No new implementation or test changes are proposed in this email. Key references include the `__git_history_has_revision` helper, the `--option=value` vs. `--option <value>` syntax distinction, and the `__git_hash_doubledash` helper mentioned earlier in the thread.