Thread identity: Bugfix for Windows-specific resource contention in the sequencer during interactive rebase (single patch, v2). Author: Johannes Schindelin (Dscho).

Core problem or goal: Fix a deadlock on Windows where the sequencer holds the ODB lock while spawning `git commit`, causing child processes that trigger auto-maintenance (e.g., repacking) to fail when attempting to replace files still locked by the parent. The fix releases the ODB before spawning commands that might run maintenance, following an established pattern, and explicitly clarifies in the commit message that the ODB will be reacquired in a "refreshed state" after auto-maintenance completes.

Current status: Marked for inclusion in `next`. Junio C Hamano has accepted the updated commit message and signaled intent to queue the patch for integration testing. No further concerns are outstanding regarding the immediate fix. The discussion has expanded to explore broader architectural improvements inspired by the bugfix, with growing consensus that the `gc.auto=false` idea is less viable in practice due to the sequencer’s tendency to spawn `git commit` (and thus trigger auto-gc) mid-rebase, not just at the end.

Key technical details:
- **Subsystem**: Sequencer (`sequencer.c`).
- **Files touched**: `sequencer.c` (single-line change to set `cmd.odb_to_close` before spawning `git commit`).
- **Test**: Windows-specific regression test in `t/t3404-rebase-interactive.sh`, guarded by `MINGW`, using a post-commit hook to trigger repacking and `GIT_TEST_LEGACY_DELETE=1` to expose the issue. The test verifies repacking completes successfully and only one packfile remains.
- **Behavior change**: ODB is released before spawning `git commit` during interactive rebase, preventing deadlocks on Windows. The commit message now explicitly states the ODB is reacquired post-maintenance.
- **Related symbols**: `cmd.odb_to_close`, `GIT_TEST_LEGACY_DELETE`, `gc.auto`, `run_auto_maintenance()`, historical constant `6700` (auto-gc threshold).
- **On-disk format**: No changes.
- **Test coverage**: Unchanged from v1; covers the regression scenario. No new test scenarios introduced for the broader questions raised.

Open questions and loose ends:
- **Forward-looking questions**: Phillip Wood refined the analysis of when `git commit` (and thus auto-gc) is invoked during interactive rebase, noting that while simple cherry-picks avoid spawning `git commit`, rebasing merges and large rebases *do* invoke it mid-rebase. This weakens the case for disabling `gc.auto` during rebase, as auto-gc will often trigger before the rebase completes. The discussion continues to favor Junio’s suggestion of automatically closing the ODB before spawning any Git command as the most promising long-term architectural direction. No concrete follow-up patches or design proposals have been posted yet.

Participant positions:
- **Johannes Schindelin (Dscho)**: Author. Updated the commit message to address Junio’s safety concern while keeping the technical change unchanged. Position: the patch is correct, follows established patterns, and now provides sufficient rationale.
- **Junio C Hamano**: Reviewer. Raised the initial concern about resource release safety; accepted the updated commit message and signaled intent to queue the patch in `next`. In follow-up discussion, provided historical context for `gc.auto` and endorsed exploring a global ODB-release mechanism for future work.
- **Phillip Wood**: Reviewer. Acknowledges the patch as correct and sufficient for the immediate deadlock but raises forward-looking questions about broader implications. Refined the analysis of when `git commit` is spawned during rebase, demonstrating that auto-gc will often trigger mid-rebase, not just at the end. Position: exploratory, not prescriptive; no objections to the current fix, but now leans toward the global ODB-release mechanism as the more robust long-term solution.

Related work: References prior work in 28d04e1ec197 (established pattern for releasing ODB before spawning commands) and 69ed0e35a754 (`GIT_TEST_LEGACY_DELETE` trick for testing on modern Windows). The issue was introduced in 4557f1add261 (2017). No dependencies on other in-flight topics. The discussion introduces potential future work around a global ODB-release mechanism, with Junio’s endorsement of this as a long-term refactoring goal.