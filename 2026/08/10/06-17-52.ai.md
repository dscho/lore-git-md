This is a **bugfix** patch addressing a use-after-free in `get_oid_with_context_1()` when resolving relative paths in the `:<stage>:<path>` syntax against a sparse index. The patch is now in its final review stage after incorporating minor feedback.

Patrick Steinhardt provides a **tested review**, confirming the bug and the fix. He tested the patch locally and observed the expected behavior: the test now correctly reports a fatal error for a non-existent path, whereas before the fix it showed a corrupted string (`�[UU?`) due to the use-after-free. His feedback is substantive but non-blocking—he suggests a minor stylistic improvement to the control flow in the fix (using the return value of `reject_tree_in_index()` directly) but explicitly states he won’t insist on it. The patch remains functionally unchanged from v2.

The fix itself is minimal and targeted: it consolidates the exit paths in `get_oid_with_context_1()` to ensure the heap-allocated string from `resolve_relative_path()` is freed only after its final use in `reject_tree_in_index()`. The regression test in `t1092-sparse-checkout-compatibility.sh` reliably reproduces the bug under `SANITIZE=address` and covers all three necessary conditions (relative path, sparse directory, and leading colon followed by a digit).

The patch is well-motivated, with clear commit message explanations, thorough test coverage, and prior discussion aligning on the solution. René Scharfe’s earlier substantive review and explicit approval ("OK") suggest this is uncontroversial. The only remaining feedback is a non-blocking stylistic suggestion, and Patrick’s tested confirmation removes any lingering doubt about correctness. This looks ready for integration.