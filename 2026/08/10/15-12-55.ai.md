This is a **bugfix discussion** targeting the geometric repacking subsystem, specifically the concurrency failures in background maintenance.

Stefan Haller replies to Patrick Steinhardt’s proposed mitigation for the overly aggressive `geometric-repack.auto` threshold. Patrick had suggested raising the default value from 100 to 6700 (matching `gc.auto`) to reduce background repack frequency and mask the concurrency issues. Stefan declines to test this change, citing the sporadic nature of the failures in CI and the difficulty of reliably reproducing them. He also notes that he has already implemented a local workaround that guarantees stability, making the proposed default change unnecessary for his use case.

The reply leaves the proposed threshold adjustment unvalidated in practice. While Patrick’s fix is technically sound and aligns with existing `gc.auto` behavior, Stefan’s hesitation underscores the challenge of confirming whether the change sufficiently mitigates the concurrency issue. The underlying race condition—where `prune_packed_objects()` removes fanout directories while concurrent writers attempt to create temporary files—remains unaddressed, and the thread now hinges on whether the threshold adjustment alone is enough to prevent observable failures in real-world scenarios. No further technical details or alternative solutions are proposed in this email.