This is a surface-level review reply in the ongoing thread about Bash completion for the `git history` command. D. Ben Knoble acknowledges that the third patch in Vincent Mailhol’s v2 series — which adds completion for the `--update-refs` option (values: `branches` or `head`) — is well-placed in the completion script’s logic. The option applies globally to all current `git history` subcommands, so its position before subcommand-specific logic is appropriate. Knoble notes that if this assumption changes in the future (e.g., if `--update-refs` becomes subcommand-specific), the completion script can be adjusted then, but for now the placement is correct and uncontroversial. The patch itself is small, focused, and aligns with the established pattern for handling both `--option=<value>` and `--option <value>` forms. No substantive concerns are raised; this is a brief, approving comment on code organization rather than a technical critique.