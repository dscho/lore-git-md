This is a bugfix patch improving the error message for `git worktree add` when an ambiguous remote branch name is provided. The author, Yoichi Nakayama, responds to D. Ben Knoble’s review about an unannounced behavior change: the patch now calls `die()` early when ambiguity is detected, whereas the original code continued execution until a later check.

Yoichi clarifies that the original code *did* eventually fail with a generic "invalid reference" error, just later in the code path. The new behavior is therefore not a regression—it fails earlier with a more informative message. This addresses Ben’s concern about breaking existing workflows, though the commit message could still be clearer about this shift.

The patch remains focused on usability: replacing a terse warning with a detailed advice message (gated by `advice.checkoutAmbiguousRemoteBranchName`) that suggests a command (`git branch -r --list '*/<name>'`) to help users resolve ambiguity. The advice format is still under discussion (Junio C Hamano previously suggested a static list of remotes), but the core goal—better error messages—is uncontroversial.

No changes to the patch itself are included in this reply, so the outstanding questions are whether the commit message should be updated to explain the early `die()` and whether the command-based advice is preferable to a static list. The patch touches `builtin/worktree.c` and updates tests in `t/t2400-worktree-add.sh`. It looks well-motivated and unlikely to be controversial once the commit message is clarified.