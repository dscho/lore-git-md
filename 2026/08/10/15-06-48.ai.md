This is a review comment from Junio C Hamano on Yoichi Nakayama’s patch improving the error message for `git worktree add` when an ambiguous remote branch name is provided.

Junio’s feedback focuses on the commit message’s clarity regarding the behavioral change. He acknowledges the patch’s goal—replacing a generic "invalid reference" error with a more descriptive message—but notes that the current commit message does not explain *why* the `die()` call is moved earlier in the code path. He suggests rewriting the message to describe the current problematic flow: the command silently fails to resolve the ambiguous branch, leaving a `NULL` remote, which later triggers a misleading "invalid reference" error when `lookup_commit_reference_by_name()` is called. The proposed mock-up message would explicitly trace this sequence, making it clear that the early `die()` is a usability improvement, not an arbitrary change.

The review is substantive but narrowly scoped: it does not challenge the patch’s technical approach or the advice message’s content (command-based vs. static list), but instead targets the commit message’s ability to justify the error-handling refinement. Junio’s tone is constructive, and his feedback carries high weight given his role as maintainer. The patch’s next iteration will likely need to incorporate this clarification to address the open question about whether the commit message adequately explains the refined error-handling behavior. Surface-level nits (unnecessary newline, braces) remain unaddressed but are secondary to this concern.