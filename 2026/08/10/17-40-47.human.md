This is the documentation patch for the final piece of Siddharth Shrimali’s GSoC series introducing `git repack --drop-filtered` for partial clones. The patch adds 37 lines to `Documentation/git-repack.adoc`, fully documenting the new `--drop-filtered` and `--dry-run` options.

The text is clear and thorough. It explains that `--drop-filtered` removes locally cached promisor blobs matching the `--filter` specification (currently limited to `blob:limit`), reclaiming disk space while ensuring recoverability via lazy fetch. It covers all preconditions (`-a` required, promisor remote required, `--filter` limited to `blob:limit`), incompatibilities (`--filter-to` and bitmaps), and the implied `-d` (to remove redundant packs). The safety guards—refusing to run during ongoing operations (merge, rebase, etc.) or to drop blobs referenced by the current index—are presented as UX optimizations, not correctness measures, since lazy-fetching would transparently restore dropped objects. The `--dry-run` option is documented as a preview mechanism that lists candidate OIDs without making changes.

The language aligns with the series’ reframing of the safety guards and avoids overpromising: it acknowledges the current limitation to `blob:limit` filters and the silent disabling of config-driven bitmaps. The patch is well-scoped and production-ready, with no open questions or loose ends. It completes the v4 series, which has addressed all prior feedback (including Junio’s mechanical fixes) and is now awaiting maintainer integration. If the rest of the series holds up, this documentation will give users a precise understanding of how to use the feature safely.