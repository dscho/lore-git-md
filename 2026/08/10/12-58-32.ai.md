This is a surface-level review of the final patch in Vincent Mailhol’s v2 series adding Bash completion for `git history`. D. Ben Knoble’s feedback is narrowly focused on code organization: he notes that the placement of the `split` subcommand’s pathspec completion logic (after revision handling) diverges from the pattern used in other completion functions like `_git_notes` and `_git_reflog`, which dispatch on subcommand-and-cursor-position combinations (`case "$subcommand,$cur" in`) earlier in the function. Knoble acknowledges that the current placement achieves the desired behavior (completing revisions before pathspecs, respecting `--`), but flags the inconsistency as a potential stylistic wart. His comment is purely about readability and idiom, not functionality; the patch itself is correct and complete, with thorough test coverage for both pre- and post-`--` pathspec completion. No technical objections or open questions remain.