This is a **design discussion contribution** from Junio C Hamano in the ongoing thread about unifying Git’s interactive patch selection workflow. Junio responds to Stefan Haller’s suggestion that Git should avoid building interactive UIs and instead rely on external tools like lazygit. Junio’s reply reinforces his long-standing philosophy: Git should not compete with external tools by bundling functionality that duplicates what those tools already provide, unless Git’s implementation serves as a minimal, "usable but not pretty" reference.

Junio’s position aligns with Stefan’s pragmatic argument but adds a nuanced distinction: Git *can* include interactive features, but only if they are basic enough to avoid undermining the ecosystem of external tools. This implies that Junio would support incremental improvements to Git’s built-in workflows (e.g., unifying `git add -p` and `git checkout -p` into a single session) but would resist larger UI modernization efforts (e.g., a full ncurses-based TUI). His framing suggests that the *mechanism* (a single command set for stage/discard/defer) is the priority, not the *presentation* (a polished UI).

The email does not engage with the technical details of Patrick Steinhardt’s TUI proposal or Stefan’s external-tool argument but instead clarifies the project’s philosophical boundary: Git should provide core functionality without crowding out third-party innovation. This leaves the door open for small, targeted improvements (e.g., Junio’s original proposal to extend `git add -p` with a "discard" action) while ruling out larger UI efforts (e.g., Patrick’s hierarchical TUI).

For readers tracking this thread, Junio’s reply signals that the project is unlikely to adopt a full TUI or ncurses dependency but may still pursue incremental workflow fixes. The discussion now hinges on whether the proposed changes (e.g., unified hunk triage) can be implemented in a way that avoids competing with tools like lazygit. The thread remains in the design phase, with no patches or implementation sketches yet proposed.