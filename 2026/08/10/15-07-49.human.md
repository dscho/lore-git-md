This is a usability improvement for `git worktree add`. The patch refines the error message shown when a user tries to create a worktree from a branch name that exists on multiple remotes (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Instead of a terse warning, Git now provides a detailed advice message that explains the ambiguity and offers concrete solutions: listing conflicting remotes with `git branch -r --list '*/<name>'`, specifying the fully qualified branch name (e.g., `origin/foo`), or setting `checkout.defaultRemote` to prefer one remote by default.

The advice is gated behind the new `advice.checkoutAmbiguousRemoteBranchName` config key (enabled by default) and is designed to be script-friendly, avoiding a static list of remotes that might overwhelm users. The patch also improves error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability win, not a regression, as the command was already failing—just with less clarity.

The implementation touches `builtin/worktree.c` and updates the test script `t/t2400-worktree-add.sh` to verify the new behavior. The changes are localized and follow Git’s advice-message conventions. Junio C Hamano’s review has focused on the commit message’s clarity, particularly justifying the refined error-handling behavior by tracing the problematic flow (silent failure to resolve ambiguity → `NULL` remote → misleading error). The latest iteration proposes a command-based approach for listing remotes, which Junio had previously suggested as a static list, so the advice message’s content remains under discussion.

The patch is well-motivated and includes test coverage, making it a straightforward improvement for users encountering this edge case. The remaining open question is whether the command-based advice (`git branch -r --list`) is more effective than a static list for user discovery, but this is a minor detail compared to the overall usability gain. Junio’s surface-level nits (unnecessary newline, unnecessary braces) are unaddressed but secondary to the commit message clarification.