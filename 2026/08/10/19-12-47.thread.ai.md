Thread identity
Subject: GSoC 2026 – Partial Clone Disk Space Recovery (Week 11 progress)
Series version: ongoing GSoC project, no formal patch series version yet
Originating author: Siddharth Shrimali
Thread type: progress report / design discussion

Core problem or goal
Improve disk-space recovery for repositories using partial clones by designing and implementing mechanisms to reclaim space occupied by unneeded objects (e.g., stale promisor packs, unreachable objects, or objects that can be re-fetched on demand). The work is part of a Google Summer of Code project and is still in development, not yet ready for merging.

Current status
Under review (no patches merged). The Week 11 blog post linked in the email describes progress made so far, including design decisions and technical challenges encountered. The author invites feedback, implying that either an RFC or an early patch series has been posted or will be posted shortly. No prior version of this specific effort is recorded in the thread summary.

Key technical details
Subsystems: promisor-remote, packfile, garbage collection (gc), partial-clone
New or modified behaviors: not yet concretely defined in this update, but likely involve:
- A mechanism to identify reclaimable objects in partial clones
- Integration with `git gc` or a new command (e.g., `git reclaim-space`)
- Config options to control space-recovery aggressiveness
- On-disk format: no changes yet; any future changes would need to preserve compatibility with existing promisor packs
Test coverage: not yet detailed; expected to include edge cases around unreachable objects, promisor pack handling, and interaction with `git maintenance`

Open questions and loose ends
- What objects are safe to reclaim without breaking partial-clone semantics?
- How to balance runtime cost of space recovery against disk savings?
- Should space recovery be automatic (e.g., during `git gc`) or manual (e.g., a new command)?
- How to handle interaction with `git maintenance` and background maintenance tasks?
- What config options are needed to control space-recovery behavior?
- Are there edge cases around unreachable objects or objects that can be re-fetched?

Participant positions
No substantive reviews or positions recorded yet. The author has solicited feedback, so future emails may include responses from reviewers familiar with partial clones (e.g., Christian Couder, Jonathan Tan, or Paul Tarjan).

Related work
- Prior GSoC iterations on partial clones (not specified which years)
- Related patches from Paul Tarjan (fsmonitor, promisor-remote recursive fetch) and Christian Couder (promisor-remote, partial-clone)
- Git’s garbage collection (`git gc`) and maintenance infrastructure (`git maintenance`)
- Promisor pack handling and partial-clone feature (config options like `uploadpack.allowFilter`, `uploadpack.allowAnySHA1InWant`)