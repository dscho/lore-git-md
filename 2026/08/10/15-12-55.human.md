This is a **bugfix discussion** in the geometric repacking thread, focusing on the concurrency failures in background maintenance.

Stefan Haller replies to Patrick Steinhardt’s proposed fix for the overly aggressive `geometric-repack.auto` threshold. While Patrick suggested raising the default value from 100 to 6700 to align with `gc.auto` and potentially mask the concurrency issues, Stefan declines to test this change. He explains that the failures occurred sporadically in CI, making it impractical to verify the fix without extensive testing. Instead, he prefers his own solution, which he describes as "guaranteed stable," though the email does not detail what that solution entails.

The exchange highlights the difficulty of reproducing and validating fixes for race conditions in background maintenance. The underlying issue—where `prune_packed_objects()` removes fanout directories while concurrent writers attempt to create temporary files—remains unresolved, and the discussion leaves open whether Patrick’s proposed threshold adjustment would be sufficient in practice. For now, the thread continues to explore alternatives, with no clear consensus on the best path forward.