This is a documentation patch, part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila. The patch adds a `[synopsis]` AsciiDoc block to a code example in `Documentation/git-format-rev.adoc` to properly highlight the `<subject>` placeholder, ensuring consistent rendering in the man page. This is a resend of the same patch (no changes from the original submission), likely to bump visibility after no response.

The change is trivial—one line added—and follows the established pattern from commit `a34d1d53` (doc: convert git-show to synopsis style, 2026-02-06). It touches only the `git format-rev` documentation, which describes how to format revision output for use in other commands. The modified example demonstrates `--stdin-mode=text` replacing object names with formatted commit data (e.g., `<subject>`). No behavioral changes are introduced; this is purely a markup adjustment.

Kristoffer Haugsbakk, a frequent documentation contributor, is the author. The patch is standalone (not part of a series) and has no dependencies or conflicts with other in-flight topics. The commit message is clear and references prior work, though it assumes familiarity with the synopsis-style conversion effort. Reviewers may verify consistency with other synopsis blocks but are unlikely to raise substantive objections.

This patch is uncontroversial and aligns with an established project-wide documentation initiative. It could be merged without further discussion, though the resend suggests the author is seeking confirmation it hasn’t been overlooked. No prior review feedback is recorded in the thread context.