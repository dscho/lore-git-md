This is the documentation patch (7/7) for the `git repack --drop-filtered` feature, part of a seven-patch series aimed at partial clones. The patch adds 37 lines to `Documentation/git-repack.adoc`, fully documenting the new `--drop-filtered` and `--dry-run` options. The text is thorough and aligns with the series' reframing of safety guards as UX optimizations rather than correctness measures.

The documentation explains that `--drop-filtered` safely reclaims disk space by removing locally cached promisor blobs exceeding a size threshold (currently limited to `blob:limit`), provided they are recoverable from a promisor remote. It details preconditions (`-a` required, promisor remote required), incompatibilities (`--filter-to`, bitmaps), and the implied `-d` (to remove redundant packs). The safety guards—refusing to run during ongoing operations (merge, rebase, etc.) or to drop blobs referenced by the current index—are clearly described, including specific error messages and bare-repository exceptions. The `--dry-run` option is documented as a preview mechanism that lists candidate objects without making changes.

The patch is production-ready, with no unresolved technical debt. It builds on the series' existing infrastructure (e.g., `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`, `list_objects_filter__filter_oidset()`) and integrates smoothly with the rest of the implementation. The language is precise and user-focused, covering edge cases like silent bitmap disabling and the rationale for implied `-d`. Key terms for future discussion include "promisor remote," "lazy fetch," "safety guards," and "drop log" (deferred to a follow-up). The patch touches only `Documentation/git-repack.adoc` and is the final piece of the v4 series, which is now complete and awaiting maintainer review.