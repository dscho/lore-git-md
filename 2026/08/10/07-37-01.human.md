This is a follow-up in the geometric repacking series, specifically a reply from Stefan Haller that sharpens the real-world concurrency concern he raised earlier. The thread remains focused on whether Git’s background maintenance is robust enough for tools like lazygit that rely on Git commands under the hood.

Stefan clarifies that lazygit’s test suite — which does nothing more than run basic Git commands (`git add`, `git commit`) — has encountered errors when background maintenance is active. The specific error (`invalid object ... for 'file09.txt'`) suggests a race between maintenance operations (e.g., repacking) and the object database writes performed by those commands. Unlike Git’s test suite, lazygit does not inspect repository internals; it simply invokes Git commands and expects them to succeed. Stefan’s point is that if even simple workflows can fail, the auto-detach mechanism may not be sufficient to make background maintenance truly transparent in practice.

The discussion now centers on whether this is a bug in Git’s concurrency handling or an inherent limitation of the maintenance design. Patrick Steinhardt’s earlier response framed such failures as bugs worth investigating, but Stefan’s report suggests the issue may be more widespread than initially acknowledged. No code changes have been proposed yet, but the thread has shifted from a test-suite nuance to a broader question about Git’s reliability in real-world tooling. The series itself remains on track for merging, but this exchange could prompt follow-up work if the concurrency issue proves more serious than anticipated.