This is the first patch in a two-patch documentation series (v2) updating `Documentation/git-format-rev.adoc` as part of the ongoing man page synopsis-style conversion effort. The patch addresses a minor inconsistency in a code example: the placeholder `<subject>` is now consistently shown in quotes (`"<subject>"`) both in the explanatory text and in the example output. This matches the implied input format (a commit subject enclosed in quotes) and improves readability.

The change is purely cosmetic and touches only two lines in the man page. The commit message clearly explains the rationale—aligning the example with the implied input—and references the broader context (a prior discussion where the inconsistency was spotted). This patch sets the stage for the second patch, which will add a `[synopsis]` block to the same code example to properly highlight placeholders.

The series is effectively ready for merging, with no remaining objections and mutual agreement between the author (Kristoffer Haugsbakk) and reviewer (Patrick Steinhardt) on the approach. The patch itself is uncontroversial and unlikely to draw further discussion. Key technical detail: the change replaces `<subject>` with `"<subject>"` in a `--stdin-mode=text` example, reinforcing consistency in how placeholders are presented in documentation. No behavioral or on-disk format changes are involved.