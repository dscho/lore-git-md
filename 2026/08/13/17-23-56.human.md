This is a feature patch series from Kristoffer Haugsbakk adding three new formatting options to the `git format-rev` command: `--abbrev`, `--color`, and `--date`. The series also includes preparatory refactoring and documentation cleanup.

The first two patches are internal refactoring: one adjusts option description capitalization to match project conventions, and the other groups option-related variables into a struct for better organization. The third patch extracts the documentation for `--date` alternative formats into a separate file (`rev-list-option-date-alternatives.adoc`), which is then included by both `git-format-rev.adoc` and `rev-list-options.adoc`. This avoids duplicating the same text in multiple places.

The main change comes in the fourth patch, which implements the three new options. `--abbrev` controls the length of abbreviated object names (matching the behavior of `git rev-parse --abbrev`), `--color` enables colored output, and `--date` allows specifying date formats (e.g., `--date=iso`, `--date=relative`). The implementation reuses existing date-handling code from `rev-list-options.adoc` and adds tests in `t/t6120-describe.sh` to verify the new functionality.

The series looks well-structured and follows Git's conventions. The commit messages are clear, the refactoring is sensible, and the new options are documented and tested. This appears to be a straightforward quality-of-life improvement for users of `git format-rev`. The changes are contained to the formatting subsystem and shouldn’t introduce compatibility issues.