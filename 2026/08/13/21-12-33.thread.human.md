This thread is about a patch from Nikolaus Schuetz that adds test coverage for `git symbolic-ref`'s exit codes and output when querying non-symbolic refs, while also updating the command's documentation to reflect the current behavior. The patch targets `t/t1401-symbolic-ref.sh` and `Documentation/git-symbolic-ref.adoc`, documenting that `git symbolic-ref` exits with code 128 (without `--quiet`) or 1 (with `--quiet`) and produces empty stdout in both cases.

The thread has largely converged on a pragmatic compromise: preserve the current behavior—viewed by Junio C Hamano and Patrick Steinhardt as likely unintentional but low-risk—and document it rather than resolving the design debate. Nikolaus's third iteration (v3) proposes updating the man page to describe the exit codes, addressing Junio's earlier feedback to avoid testing exact exit codes by instead documenting them. However, Junio's latest email objects to "casting wrong behavior into stone" by documenting it, reinforcing his preference for aligning the exit codes. This leaves the compromise in limbo: the patch's test additions are uncontroversial, but the documentation update is now contested.

Phillip Wood remains the sole voice arguing the exit code discrepancy may be intentional and useful for script-level error differentiation, citing parallels with `git rev-parse` and `git show-ref`. No one has surveyed existing scripts to confirm this reliance, and the thread has deferred deeper design questions. The next step is unclear—Nikolaus may need to either revise the man page text to soften its framing or propose a behavioral change to unify the exit codes. Until then, the patch remains under review, with the documentation update pending.