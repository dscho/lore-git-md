This thread is about a patch from Nikolaus Schuetz that adds test coverage for `git symbolic-ref`'s behavior when querying non-symbolic refs, focusing on exit codes and output with and without the `--quiet` flag. The patch itself is uncontroversial and well-structured, touching only the test script `t/t1401-symbolic-ref.sh` and following established patterns. It replaces hardcoded exit codes with `test_must_fail` to avoid over-specifying behavior, addressing earlier feedback from Junio C Hamano.

The sticking point is whether to document the current exit code behavior (128 without `--quiet`, 1 with it) in the man page. Initially, this seemed like a pragmatic compromise to provide clarity for script authors, but Junio now objects, arguing that documenting the behavior would "cast wrong behavior into stone." This has left the documentation update in limbo, with no clear path forward. Junio and Patrick Steinhardt view the exit code discrepancy as likely unintentional, while Phillip Wood suggests it may be useful for script-level error differentiation. The patch's test coverage remains valuable regardless, but the unresolved documentation question could delay merging.

The thread is now at an impasse. Nikolaus may need to revise the man page to avoid endorsing the current behavior, propose a behavioral fix to unify the exit codes (risking script breakage), or drop the documentation update entirely. The patch itself is ready for review, but the broader question of how to handle the man page remains open. Key participants—Junio, Patrick, and Phillip—have not yet reconciled their positions on whether the exit code discrepancy is intentional or needs fixing.