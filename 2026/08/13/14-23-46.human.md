This is the second patch in a two-part documentation series (v3) that tidies up the `<subject>` placeholder styling in `Documentation/git-format-rev.adoc`. The series is part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila.

This patch adds a `[synopsis]` block immediately before the code example that uses the `<subject>` placeholder. The change ensures the placeholder is properly highlighted, aligning with the project-wide documentation style introduced in commit a34d1d53. The commit message contrasts `[synopsis]` with `[verse]` blocks to clarify the intended use case, a detail added in response to Patrick Steinhardt’s feedback on the earlier version.

The patch is uncontroversial and ready for merging. It carries Patrick Steinhardt’s `Acked-by`, confirming his agreement with the revised approach. The series as a whole resolves earlier debate about markup alternatives by splitting changes into two pragmatic commits: the first replaced `<subject>` with literal quotes (`"<subject>"`), and this one adds the `[synopsis]` block. No code behavior or test coverage changes are involved—this is purely a documentation improvement.

The outcome reinforces a pragmatic precedent for flexibility in applying synopsis-style rules to non-synopsis contexts, which may guide future patches in the conversion series. The series is effectively complete, with no open questions or loose ends.