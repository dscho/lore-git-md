This is a brief procedural update in the `kh/trailers-no-urls` thread. Junio C Hamano clarifies that Kristoffer Haugsbakk does not need to wait for `kh/doc-trailers` to reach `master` before resending the URL-fix series—it is acceptable to depend on a topic that is already in `next`, which `kh/doc-trailers` now is. The exchange confirms the hold on `kh/trailers-no-urls` is purely logistical, not technical, and the author will proceed once the base topic is stable.

For a future AI summarizer, note the key detail: the dependency (`kh/doc-trailers`) is now in `next`, so the URL-fix series can be resent without further delay. The patch itself remains a narrow, uncontroversial bugfix targeting `trailer.c`, with no changes to its scope or design. No new feedback or objections are raised in this email.