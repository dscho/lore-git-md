This is a minor style cleanup patch, the first in a four-part series from Kristoffer Haugsbakk that adds formatting options to `git format-rev`. The patch standardizes the capitalization of option descriptions in `builtin/name-rev.c` to lowercase, aligning with Git’s coding guidelines. It changes three lines—`--null`, `--null-input`, and `--null-output`—to use lowercase "use" instead of "Use" at the start of their descriptions.

The change is purely cosmetic and uncontroversial. It’s a small step toward consistency in the codebase, and the commit message clearly explains the rationale. Since this is part of a larger series introducing new features to `git format-rev`, the cleanup here is likely preparatory work to make the subsequent patches cleaner. No functional changes are introduced, and the patch is unlikely to attract debate. If the rest of the series holds up, this will be a straightforward merge.