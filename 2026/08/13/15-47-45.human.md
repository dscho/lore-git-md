This is the second patch in Christian Couder’s five-patch series introducing `uploadpack.lazyFetchTrusted`, a server-side configuration variable that lets operators mark repositories as trusted for lazy fetching. The patch extracts and generalizes the allowlist-checking logic from Git’s existing `safe.directory` machinery into a new reusable helper, `path_allowlist_apply()`.

The change is a pure refactoring: it moves the code that checks whether a repository path matches an allowlist entry from `safe_directory_cb()` into a standalone function, `path_allowlist_apply()`. The helper is made slightly more generic (e.g., it takes the target path and match flag as parameters) but introduces no new behavior or configuration. The patch is well-motivated: it prepares the codebase for the upcoming `uploadpack.lazyFetchTrusted` configuration, which will reuse the same allowlist-checking logic to determine whether a repository is trusted for lazy fetching.

The commit message is clear and concise, explaining the purpose (preparing for a future allowlist check) and the mechanical change (extracting code from `safe_directory_cb()`). The diff is clean and focused, touching only `setup.c` and introducing no new symbols or dependencies. The patch is uncontroversial and appears ready for integration once the broader series is reviewed. It is a small but necessary step toward the server-side trust mechanism that the series aims to establish.