This is a **substantive review reply** in the ongoing **packfile performance regression** thread. Jeff King (Peff) acknowledges Johannes Schindelin’s explanation for the new performance test (`abbreviate with 10,000 packs`) and concedes that the redundancy is acceptable. The reply clarifies that the regression fixed by Johannes’s patch occurs during the *initial pack list creation* (`packfile_store_add_pack()`), which affects all operations that load packfiles, not just those that open individual packs or search for object IDs. While Peff notes that the existing test suite already covers some of these scenarios (e.g., `rev-list` and abbreviation with smaller pack counts), he agrees that the new test’s focus on the 10,000-pack case—where the quadratic-time behavior is most pronounced—justifies its inclusion. The tone is pragmatic: the test is not strictly necessary but is harmless and may catch edge cases, so Peff defers to the author’s judgment. This resolves the test-coverage debate for now, leaving the broader design discussion (Junio’s maintainability concerns and Patrick Steinhardt’s hashmap counterproposal) as the remaining open question.