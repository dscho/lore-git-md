Thread identity: A four-patch series from Kristoffer Haugsbakk adding `--abbrev`, `--color`, and `--date` formatting options to the `git format-rev` command (introduced in Git 2.45). The series is preparing for v2 after v1 review feedback and a newly identified memory leak fix.

Core problem or goal: Extend `git format-rev` with three new formatting options (`--abbrev`, `--color`, `--date`) to bring it closer to parity with `git log`'s formatting flexibility. The preparatory patches (1–3) refactor option handling and documentation, while the final patch (4/4) implements the new options by reusing existing `git log` logic (`rev_info` struct and `pretty.c` date formatting).

Current status: Under review (v1 feedback addressed; v2 pending with a memory leak fix). Junio C Hamano reviewed patches 2/4 and 4/4, suggesting minor mechanical improvements (designated initializers in patch 2/4 and defensive code reordering in patch 4/4). The author has accepted both suggestions, including a fix to reorder the `BUG_ON_OPT_NEG(unset)` assertion in the `date_cb()` and `format_nul_cb()` callbacks to ensure it triggers before any dereference of `option->value`. The author has now identified a missing cleanup step in the `--date` callback: `parse_date_format()` may allocate a custom format string that requires release via `date_mode_release()`, which is currently not called, resulting in a memory leak. This fix will be incorporated into the next revision. The series remains uncontroversial and is not queued in any integration branch.

Key technical details:
- Files touched: `builtin/name-rev.c` (main implementation), `Documentation/git-format-rev.adoc`, `Documentation/rev-list-option-date-alternatives.adoc`, test coverage in `t/t6120-describe.sh`.
- New CLI options: `--abbrev`, `--color`, `--date` (with same alternatives as `git log`).
- Subsystems reused: `rev_info` struct, `pretty.c` date formatting.
- On-disk format: No changes.
- Synopsis simplification: The command synopsis now highlights the two mandatory options (`--stdin-mode` and `--format`) while moving the rest into `[<options>]`.
- Test coverage: Added in `t/t6120-describe.sh`, verifying the new options behave identically to their `git log` counterparts, including error handling for invalid values (e.g., `--color=not-valid`).
- Memory leak: The `--date` callback fails to call `date_mode_release()` after `parse_date_format()`, which may allocate a custom format string. This will be fixed in v2.
- Patch 1/4: Cosmetic documentation cleanup in `builtin/name-rev.c`, converting option descriptions from mixed case ("Use NUL...") to lowercase ("use NUL...") for style consistency. Affects only help text; no functional changes.
- Patch 2/4: Refactoring introducing `struct format_rev_data` to group option variables, inlining the existing `format_nul_data` struct, and renaming `stdin_mode_arg` to `stdin_mode` to avoid shadowing. Junio C Hamano reviewed this patch, suggesting and approving a mechanical improvement: replacing the positional initializer with a designated initializer (C99) for clarity and maintainability. The author has accepted the suggestion. The patch also includes the defensive reordering of `BUG_ON_OPT_NEG(unset)` in `format_nul_cb()` to match the agreed pattern.
- Patch 3/4: Pure documentation refactoring: extracts the 55-line block describing `--date` alternatives from `rev-list-options.adoc` into a new standalone file (`rev-list-option-date-alternatives.adoc`) so the same text can be included by both `git-rev-list` and `git-format-rev`. No user-visible behavior changes.
- Patch 4/4: Implements the three new options by reusing existing `git log` logic. Junio C Hamano identified and the author accepted a defensive programming fix: reordering the `BUG_ON_OPT_NEG(unset)` assertion in `date_cb()` to trigger before any dereference of `option->value`, preventing potential segfaults if the callback were invoked with `unset==1` (e.g., via `--no-date`). The author has now identified a missing cleanup step: `date_mode_release()` must be called after `parse_date_format()` to avoid a memory leak when a custom date format is specified.

Open questions and loose ends:
- Memory leak: The `--date` callback must call `date_mode_release()` after `parse_date_format()` to free any allocated custom format string. This will be fixed in v2.
- UI design: Whether `--date` should support the same alternatives as `git log` (no objections raised yet; may attract discussion in v2).
- Synopsis simplification: Minor UI tweak to highlight mandatory options; no objections yet.
- No prior discussion context; it is unclear if these options were explicitly requested or are filling a gap noticed by the author.

Participant positions:
- Patch 1/4: No review feedback yet; uncontroversial and likely to be accepted as-is.
- Patch 2/4: Junio C Hamano reviewed, suggesting and approving two mechanical improvements: designated initializers and defensive reordering of `BUG_ON_OPT_NEG(unset)` in `format_nul_cb()`. The author has accepted both suggestions.
- Patch 3/4: No review feedback yet; uncontroversial documentation refactoring, likely to be accepted as-is.
- Patch 4/4: Junio C Hamano reviewed, identifying and approving the defensive programming fix for `date_cb()`. The author has accepted the suggestion and has now identified and will fix the missing `date_mode_release()` cleanup.

Related work: None mentioned.