This is a **bugfix follow-up** in the `--shallow-file` segmentation fault thread. Patrick Steinhardt withdraws his earlier stylistic suggestion after Christian Couder demonstrates that the current two-line error-reporting pattern (`fprintf` + `usage`) is already the established convention for all similar options in `git.c`. The exchange confirms the patch’s consistency with the existing codebase and removes the last open question before Junio’s merge. No further changes are expected; the patch remains a minimal, defensive fix with test coverage.