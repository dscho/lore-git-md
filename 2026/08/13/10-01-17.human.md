This is a **bugfix for a packfile performance regression**, part of an ongoing design debate between a targeted fast-path optimization and a broader hashmap-based refactoring. The email is Patrick Steinhardt’s reply to Johannes Schindelin’s defense of the simpler fix, and it advances the discussion toward a potential resolution.

Patrick concedes the naming debate (`is_new` vs. `accept_duplicates`) but reaffirms his preference for the hashmap approach, now with a clearer rationale: it would speed up packfile reordering operations (used during object lookups) from O(N) to O(log N) in repositories with tens of thousands of packfiles. He proposes to implement this as a follow-up patch, either superseding Johannes’s fix or landing afterward. The tone is collaborative, leaving the door open for either path.

The key development here is Patrick’s willingness to decouple the immediate regression fix from the broader refactoring. This makes it more likely that Johannes’s patch will land first, addressing the `git rev-parse --short HEAD` slowdown in Scalar environments without blocking Patrick’s longer-term optimization. The unresolved question is whether Junio will prefer the incremental approach or ask for the hashmap solution upfront.

For readers tracking this thread: the regression is real and severe (4.5s vs. 0.4s in real-world Scalar deployments), and both solutions are technically sound. The choice hinges on whether the project prioritizes simplicity now or future-proofing later. Patrick’s follow-up plan suggests the debate may soon shift from "if" to "when" the hashmap refactoring lands.