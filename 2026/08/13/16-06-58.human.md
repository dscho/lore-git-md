This is a **procedural follow-up** to the `http.sslVerifyStatus` security feature patch. Junio C Hamano provides two small but necessary corrections to the v3 submission. The first adds the new test script `t5568-http-verify-status.sh` to `t/meson.build`, ensuring the Meson build system recognizes it. The second fixes a latent bug in the test script itself: the `ls-remote` command’s exit status was being lost, which could cause the test to silently pass even if the OCSP staple check failed. Neither change alters the patch’s core functionality, but both are required for the patch to be merge-ready. The substantive discussion about the feature’s long-term maintenance burden remains open, but this email resolves the last procedural hurdles.