This is a **bugfix proposal** targeting Git’s configuration subsystem, specifically the discrepancy between documented and actual behavior of `git config --global`. Delilah Ashley Wu resurfaces a year-old patch series that aligns the code with the documentation by making `--global` read both `~/.gitconfig` *and* the XDG config file (`~/.config/git/config`), rather than just one. The series has been dormant due to lack of community feedback but is now being revived with a nearly complete v2 update.

The patch directly addresses the core issue flagged in the thread: the current implementation reads only one file (prioritizing `~/.gitconfig` even if empty), while the documentation claims it reads both. Delilah’s approach (Option A) preserves backward compatibility—no existing configurations break—and resolves the inconsistency with `git config --get`, which already reads both files. The series also fixes related edge cases, such as unintuitive behavior with `--global --no-includes`, by ensuring include directives in either file are respected. The technical work touches `config.c`, specifically `git_global_config()` and `do_git_config_sequence()`, and includes tests to verify the new behavior.

Delilah’s email provides critical context: the maintainer (Junio C Hamano) previously acknowledged the documentation was incorrect but did not endorse a fix direction. The lack of strong opinions in the original thread left the series in limbo, but the current discussion—highlighting cross-command inconsistencies and real-world tooling failures—may tip the balance toward merging the code change. The v2 series is nearly ready, with only minor review feedback remaining, and Delilah plans to submit it soon despite limited time for side projects.

The patch’s quality appears solid: it includes a clear rationale, references prior discussion, and addresses the systemic nature of the issue. The only open question is whether the maintainer will accept the behavioral change or continue to favor updating the documentation instead. Given the renewed attention to the problem’s broader implications (e.g., `--no-includes` interactions), this series is likely to spark further debate about the right fix.

For readers tracking this thread, Delilah’s series is the most concrete step toward resolution so far. If the goal is to eliminate user confusion and align behavior with expectations, this patch is the leading candidate. The GitHub PR link ([#2196](https://github.com/gitgitgadget/git/pull/2196)) offers a preview of the changes, though the mailing list will be the official venue for review.