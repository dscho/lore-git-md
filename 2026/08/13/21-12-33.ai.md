This is a test patch targeting the `git symbolic-ref` command. It adds missing test coverage for the exit codes and output behavior when querying a non-symbolic ref, both with and without the `--quiet` (`-q`) flag.

The patch introduces two new tests in `t/t1401-symbolic-ref.sh`. The first verifies that `git symbolic-ref` exits with code 128 and prints the expected error message ("is not a symbolic ref") when given a non-symbolic ref. The second checks that `--quiet` suppresses the error message and exits with code 1 instead. These behaviors are documented in the man page but were previously untested.

The commit message is clear and references prior art (919eb8ace, which added similar edge-case testing). The patch itself is minimal: it only touches the test file, adds no new functionality, and follows the existing test style. The tests are well-scoped and directly address the gap in coverage.

This looks like a straightforward, well-motivated contribution. The absence of prior tests for this behavior is a minor oversight, and the patch corrects it cleanly. No controversy or follow-up work is expected. The patch is ready for review and likely to be merged as-is.