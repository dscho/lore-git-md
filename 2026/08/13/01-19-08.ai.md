This is a thread update in the fsmonitor macOS bugfix discussion. Tamir Duberstein withdraws the patch after further investigation failed to confirm that the proposed `FSEventStreamFlushAsync()` call reliably fixes the performance regression described in the original commit message. The thread is now closed unless Tamir resubmits with new evidence.

Key points for follow-up: the macOS fsmonitor daemon still exhibits the timeout behavior on loaded systems, but the root cause remains unproven and the proposed fix is abandoned. No code changes were merged. The discussion leaves open the question of whether the FSEvents queue drainage issue is the true culprit or if another factor is at play. If Tamir resends, the thread will need to re-establish the performance baseline, the technical rationale for the flush call, and the benchmark data requested by Koji Nakamaru and Junio C Hamano.