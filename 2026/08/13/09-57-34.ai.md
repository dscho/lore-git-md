This is a documentation patch series (two patches) targeting `Documentation/git-format-rev.adoc`, part of the ongoing man page synopsis-style conversion effort. The series revises the styling of the `<subject>` placeholder in a code example to improve readability and consistency with AsciiDoc markup conventions.

The v2 iteration splits the changes into two commits: the first replaces the `<subject>` placeholder with literal quotes (`"subject"`), and the second adds a `[synopsis]` block to the code example to properly highlight placeholders. This resolves the earlier debate about whether `[synopsis]` should apply to non-synopsis code blocks by adopting a pragmatic approach—using literal quotes for clarity while still applying the tag for consistency with the broader documentation effort.

The series touches only the `git-format-rev` man page and introduces no behavioral changes. The commit messages are detailed, referencing prior work (commit a34d1d53) and explaining the rationale for the markup choices. Kristoffer Haugsbakk (the author) and Patrick Steinhardt (reviewer) have aligned on the approach, and the thread is effectively closed with no remaining objections.

Key technical details include the use of `[synopsis]` on a code block (not just the command synopsis) and the replacement of `<subject>` with `"<subject>"` to avoid awkward markup. The changes are minimal and unlikely to be controversial, though they reinforce flexibility in the synopsis-style conversion effort by prioritizing readability over rigid rules. The series is ready for merging.