This is a **test fix follow-up** in the nearly complete series to eliminate a flaky test in `t/t7900-maintenance.sh`. The email is a brief acknowledgment from Karthik Nayak confirming Patrick Steinhardt’s explanation for the mechanical change in patch 1/2: the switch from `test_config` to `git config set` was forced by `test_config`’s reliance on `test_when_finished`, which cannot run in subshells. Karthik’s reply closes the last open discussion point in the thread.

The series itself remains ready to merge. Patch 1/2 refactors three tests to use throwaway repositories (`git init repo`) and subshells (`(cd repo && ...)`) to isolate environment changes, while patch 2/2 fixes the flake by restoring default auto-detach behavior. The fix targets a recurring CI reliability issue in GitLab and is uncontroversial, with all substantive feedback addressed. No further changes are expected.