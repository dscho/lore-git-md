This is Junio C Hamano weighing in on the design of Johannes Schindelin’s packfile performance regression fix. The patch is a bugfix targeting the packfile subsystem, addressing a severe O(N²) slowdown in `git rev-parse --short HEAD` (and other operations) when repositories accumulate tens of thousands of packfiles, as seen in Microsoft Git Scalar’s shared cache architecture.

Junio’s reply focuses on the `is_new` parameter introduced in the patch to skip duplicate checks in `packfile_list_append()`. He agrees with Patrick Steinhardt’s suggestion to rename the parameter—though Patrick proposed `accept_duplicates`, the thread later settled on `skip_dup_check`—but his deeper concern is about the safety of the fast-path optimization. The patch allows callers to bypass duplicate checks if they *guarantee* the packfile is new, but Junio questions whether this guarantee is robust enough to hold over time. He worries that future changes to code paths or new callers might violate the assumption, leading to duplicate entries in the packfile list. While he doesn’t spell out specific failure modes, he hints at potential inefficiencies or correctness issues if duplicates are allowed to accumulate.

The reply is brief but substantive, engaging with the trade-off between performance and safety. Junio’s tone is cautious, framing the fast-path as a clever optimization that might be too clever if it relies on caller discipline that could erode over time. This aligns with his earlier endorsement of the patch as "clever and clean," but now with a sharper focus on its long-term maintainability. The question he raises—what bad things happen if duplicates slip in—remains open, but his skepticism suggests the current design might need hardening before it graduates to `master`. The patch is still cooking in `next`, and this feedback could prompt a follow-up iteration.