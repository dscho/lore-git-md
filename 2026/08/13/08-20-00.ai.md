This is a reply in the ongoing Bash completion thread for `git history`. Vincent Mailhol responds to D. Ben Knoble’s stylistic feedback about the order of logic in the completion script, specifically whether to check the subcommand or the option first.

Vincent explains that checking the option first (the current approach) avoids code duplication and keeps the logic in a single `case "$cur"` block, which he argues is cleaner. He provides a side-by-side comparison showing that checking the subcommand first would require either duplicating the `--update-refs` dispatch or introducing a wildcard that could conflict with other cases. The current implementation dispatches on the option first, then narrows by subcommand only for `--empty`, which is subcommand-specific. This keeps the code compact and avoids repeating the `case "$cur"` structure.

The discussion remains purely stylistic; both approaches achieve the same behavior. Vincent’s argument centers on maintainability and avoiding redundancy, while Knoble’s preference (expressed in the parent email) is for consistency with the rest of Git’s completion codebase, where subcommand checks typically come first. No technical objections or edge cases are raised, and the patch remains correct as-is. The thread is now waiting for either Knoble’s further feedback or a decision from the author on whether to adjust the code structure in a future iteration.