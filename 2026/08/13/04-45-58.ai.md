This is Junio’s "What’s cooking" report for August 2026 (#05), summarizing the state of integration branches and in-flight topics.

The report lists 424 non-merge commits in `master` since Git 2.55, with 58 in `next` and 266 in `seen`. Key highlights for a downstream AI summarizer:

**New topics likely to see follow-up discussion:**
- `jt/receive-pack-pluggable-writes` (9 commits): Refactors `git receive-pack` to use a new ODB transaction interface for writing packfiles, making it backend-agnostic. Patrick Steinhardt raised concerns about the design (e.g., fd handling, transaction finalization), so expect revisions.
- `ps/odb-eagerly-load-alternates` (5 commits): Simplifies ODB initialization by eagerly loading alternates, eliminating lazy-loading calls. Depends on `ps/odb-make-creation-pluggable` and touches core object storage; may interact with other ODB efforts.
- `gg/http-ssl-verify-status` (1 commit): Adds `http.sslVerifyStatus` to check stapled OCSP responses during TLS handshake. Junio flagged it as potentially broken; needs testing.
- `ps/cat-file-remote-object-info-type` (11 commits): Extends `git cat-file --batch-command` to support `%(objecttype)` for remote object info. Peff and others reviewed; likely to merge soon.
- `hn/branch-delete-merged` (7 commits): Adds `git branch --delete-merged` to remove local branches merged into their upstream. Includes opt-out config (`branch.<name>.deleteMerged`) and `--dry-run`.
- `kk/merge-base-exhaustion` (11 commits): Optimizes merge-base computation by terminating early when one side’s commits are exhausted. Adds trace2 instrumentation and tests for edge cases.

**Topics needing review (high signal):**
- `ps/odb-streams` (8 commits): Unifies ODB read/write stream APIs into a single `struct odb_stream`. Touches low-level object access; may affect other ODB work.
- `js/mingw-build-updates` (12 commits): Upstreams Git for Windows build fixes (e.g., dropping `-D_USE_32BIT_TIME_T`, allowing `git.exe` as the main binary). Johannes Schindelin is the author; expect platform-specific feedback.
- `mm/diff-process-hunks` (11 commits): Introduces `diff.<driver>.process` to let external tools precompute hunks. Affects diff, blame, and line-log operations; depends on `mm/line-log-limited-ops`.

**Stalled topics (may be dropped):**
- `tb/repack-geometric-cruft` (11 commits): Combines `--geometric` and `--cruft` in `git repack`. Taylor Blau noted a "bare grep !???" squash commit; needs cleanup.
- `hn/checkout-track-fetch` (2 commits): Extends `git checkout --track` to fetch the branch. Junio marked it for discard due to design concerns.

**Graduated to `master` (notable):**
- `jc/add-resolved` (4 commits): Adds `git add --resolved` to stage conflict-resolved paths while preserving unrelated changes. Peff reviewed; now in `master`.
- `tn/packfile-uri-concurrency` (6 commits): Fixes concurrency issues in packfile URI downloads (e.g., avoiding partial pack corruption). Now in `master`.

**Key files/subsystems touched:**
- ODB/refactoring: `odb/`, `packfile.c`, `builtin/receive-pack.c`
- HTTP/transport: `http.c`, `transport.c`
- Branch/merge: `builtin/branch.c`, `commit-reach.c`
- Windows: `compat/mingw.c`, `Makefile`

**Likely points of contention:**
- ODB pluggability (`jt/receive-pack-pluggable-writes`, `ps/odb-*`): Patrick Steinhardt’s feedback suggests design debates.
- HTTP OCSP (`gg/http-ssl-verify-status`): Junio’s "Broken???" note hints at implementation issues.
- Rust-related topics: None in this report, but past discussions (e.g., `ec/rust`) have been contentious.

**Context for future threads:**
- The `the_repository` removal effort is ongoing but not directly represented here.
- `git history` (experimental) appears in multiple topics (`vm/complete-history`, `hn/history-squash`), suggesting active development.