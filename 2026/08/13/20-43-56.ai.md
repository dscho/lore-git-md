This is a test patch that improves coverage in `t1402-check-ref-format.sh`.

The patch adds test cases for characters that are already documented as forbidden in refnames but were not explicitly tested in embedded form: space, tilde (`~`), caret (`^`), colon (`:`), asterisk (`*`), and open-bracket (`[`). It also verifies that the single-character `@` refname is rejected even when `--allow-onelevel` is passed, which would otherwise permit one-level refnames like `refs/@`.

The changes are confined to the test script `t/t1402-check-ref-format.sh`, adding eight new `invalid_ref` or `valid_ref` checks. The patch is purely additive and does not alter any production code. The commit message references an earlier test-only patch (`919eb8ace`) as precedent for documenting existing behavior through tests. No thread context was provided, so this appears to be a standalone contribution.