This is a **substantive technical clarification** in the ongoing review of Johannes Schindelin’s patch to fix a quadratic-time performance regression in `packfile_store_add_pack()`.

Peff points out that Git already has a hashmap-based mechanism for tracking packfiles—introduced in commit `ec48540fe8` (2019)—which is used by `packfile_store_add_pack()` and `packfile_store_load_pack()` to determine whether a packfile is new. This mechanism is what currently allows the `is_new` flag (now renamed to `skip_dup_check`) to work correctly during "reprepare" operations. His observation suggests that the existing hashmap infrastructure could be leveraged more directly, rather than introducing a fast-path optimization that relies on caller discipline.

The comment does not propose a specific alternative or reject the current patch, but it **reopens the design discussion** by highlighting that the regression may stem from a gap in how the existing hashmap is used—or from a regression in its maintenance. This could influence whether the project pursues Johannes’s fast-path fix, Patrick Steinhardt’s hashmap counterproposal, or a third approach that builds on the existing infrastructure.

For readers tracking this thread, Peff’s note is a reminder that the packfile subsystem already has tools to handle this problem efficiently, and the current regression may be a symptom of their incomplete or incorrect application. The observation is likely to prompt further iteration on the design before the patch lands.