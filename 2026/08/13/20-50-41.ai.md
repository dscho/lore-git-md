This is the final technical clarification in the MIDX incremental writes series, focusing on the third patch which corrects pack inclusion logic for custom base layers.

Taylor Blau acknowledges Patrick Steinhardt's observation that the commit message's invariant ("at most one of m and ctx->to_include are set") no longer holds when using `--stdin-packs --incremental --base=<foo>`. The discussion confirms that while `ctx->m` remains NULL in this scenario, the local `m` variable (assigned from `ctx->base_midx`) and `ctx->to_include` can both be non-NULL simultaneously. This doesn't break the logic—packs covered by the base MIDX are correctly excluded, and remaining packs are filtered against the stdin list—but the comment needs updating to reflect the new behavior.

The exchange reaffirms the fix's correctness: the revised logic uses `ctx->base_midx` as the exclusion boundary during incremental writes, ensuring proper handling of `--base=none` (include all packs) and `--base=<hash>` (exclude packs already in the base MIDX). Both participants agree the behavior is sensible, and the previously failing test cases now pass. The only remaining action is a commit message tweak to clarify the mutual exclusivity assumption, resolving the last open question in the series.

This is a tested, substantive review interaction that advances the patch toward readiness. The series remains under review pending the trivial documentation update.