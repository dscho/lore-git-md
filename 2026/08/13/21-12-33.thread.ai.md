Thread identity
Subject: [PATCH] t1401: add tests for `git symbolic-ref` exit codes and output with non-symbolic refs
Series version: standalone patch (no series)
Originating author: Nikolaus Schuetz
Thread type: patch (test coverage addition)

Core problem or goal
Add missing test coverage for `git symbolic-ref`'s exit codes and output behavior when querying a non-symbolic ref, both with and without the `--quiet` (`-q`) flag. The behaviors (exit code 128 with error message, exit code 1 with `--quiet`) are documented in the man page but were previously untested.

Current status
Under review; no prior versions. Patch touches only `t/t1401-symbolic-ref.sh`, adds two new tests, and follows existing test style. No new functionality introduced.

Key technical details
- Files touched: `t/t1401-symbolic-ref.sh` (test script)
- New test names: (implicit in script) `git symbolic-ref exits with 128 and error on non-symbolic ref` and `git symbolic-ref --quiet exits with 1 and no error on non-symbolic ref`
- Subsystem: refs (symbolic-ref plumbing)
- New symbols: none
- On-disk format changes: none
- Test coverage: edge-case behavior of `git symbolic-ref` with non-symbolic refs

Open questions and loose ends
None identified. The patch is self-contained and addresses a clear gap in test coverage. No design objections, requests for changes, or conditional approvals have been raised.

Participant positions
None yet recorded. The patch has not yet received review feedback.

Related work
References prior art: commit 919eb8ace (added similar edge-case testing for `git symbolic-ref`). No dependencies or follow-up work mentioned.