Here’s the human-mode summary for a developer catching up after being away:

This thread is Junio Hamano’s latest "What’s cooking" report for August 2026, tracking the state of all in-flight Git topics. The big news is that Taylor Blau’s `tb/send-pack-no-ref-delta` series has been dropped from the queue after Patrick Steinhardt recommended deferring it in favor of his own `ps/odb-generate-pack` work. The eviction is now official, so anyone tracking that series can stop—it won’t be merged in its current form.

The rest of the queue is stable: four small topics (`cc/git-shallow-file-wo-value`, `en/sequencer-lose-pretty-given`, `ps/odb-streams`, and `js/coverity-unchecked-returns-fix`) were recently endorsed for the `next` branch, but nothing has graduated to `master` yet. Kristoffer Haugsbakk’s bugfix for misparsed URLs in trailers (`kh/trailer-url-fix`) is still on hold, waiting for its documentation dependency to land. Patrick is also reviewing Taylor’s `tb/midx-incremental-custom-base` series today, which fixes a bug in incremental multi-pack-index writes; if that goes well, it could be the next topic to move forward.

The ODB refactoring effort continues to dominate the technical discussion, with Patrick raising concerns about `jt/receive-pack-pluggable-writes` (file descriptor handling and transaction finalization) that will need addressing before it can progress. Junio also flagged `gg/http-ssl-verify-status` as potentially broken, so that series may need testing or revisions. Everything else—including the Windows build updates, branch deletion improvements, and diff hunk processing—remains under review with no major objections so far.