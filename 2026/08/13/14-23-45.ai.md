This is the first patch in a two-patch documentation series (v3) targeting `Documentation/git-format-rev.adoc`, part of the ongoing man page synopsis-style conversion effort. The patch addresses a minor inconsistency in a code example demonstrating `--stdin-mode=text` output formatting: the explanatory text describes the format as `%s`, but the example output shows `<subject>` enclosed in quotes. This patch aligns the two by quoting the placeholder in both places (`"%s"` and `"<subject>"`), reflecting the implied input format (a commit subject enclosed in quotes).

The change is purely cosmetic and uncontroversial, touching only two lines in the documentation. The commit message clearly explains the rationale and the implied input context, making the motivation easy to follow. This patch sets the stage for the second patch in the series, which adds a `[synopsis]` block to the same example to properly highlight placeholders. The series as a whole is ready for merging, with Patrick Steinhardt’s `Acked-by` on the second patch confirming agreement with the approach.

Files touched: `Documentation/git-format-rev.adoc`. Key terms modified: `%s` → `"%s"`, `<subject>` → `"<subject>"`. No behavior, on-disk format, or test coverage changes are involved. The patch is part of the broader documentation synopsis-style conversion effort led by Jean-Noël Avila, though it focuses on a pragmatic readability fix rather than strict synopsis markup rules. The series resolves earlier discussion about markup alternatives by splitting changes into two commits, with this patch addressing the immediate inconsistency.