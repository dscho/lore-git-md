This is the final exchange in the review of the third patch in Taylor Blau’s three-patch series fixing MIDX incremental writes with custom base layers. The discussion centers on a single technical point: whether the commit message’s claim that “at most one of `m` and `ctx->to_include` are set” remains accurate after the patch.

Taylor acknowledges Patrick Steinhardt’s observation that the comment is misleading when using `--stdin-packs --incremental --base=<foo>`, since both `ctx->base_midx` (assigned to the local `m`) and `ctx->to_include` can be non-NULL simultaneously. He confirms the filtering logic is correct—packs covered by the base MIDX are excluded, and the remaining packs are checked against the stdin list—but agrees the comment needs fixing. The rest of the exchange walks through the edge cases (`--base=none` vs `--base=<hash>`) and confirms the behavior is sensible.

This is a narrow but substantive clarification that resolves the last open question in the series. The patch itself is functionally complete and correct; only the commit message needs a minor update to reflect the actual invariants. The series is now ready to proceed once that documentation tweak is made.