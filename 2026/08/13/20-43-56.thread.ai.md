Thread identity
Subject: [PATCH] t1402: add coverage for documented but untested refname characters
Series version: standalone patch (no series)
Originating author: Nikolaus Schuetz
Thread type: test-only patch

Core problem or goal
Add test coverage in `t/t1402-check-ref-format.sh` for characters already documented as forbidden in refnames but not explicitly tested in embedded form: space, tilde (`~`), caret (`^`), colon (`:`), asterisk (`*`), and open-bracket (`[`). Also verify that the single-character `@` refname is rejected even when `--allow-onelevel` is passed.

Current status
Under review; no prior version recorded. Patrick Steinhardt provided a substantive review suggesting a minor refactoring (loop over forbidden characters) and noted a test fixture inconsistency (`heads/` vs `refs/heads/`) but did not treat either as a blocker.

Key technical details
Files touched: `t/t1402-check-ref-format.sh`
New test cases: eight new `invalid_ref` or `valid_ref` checks (proposed refactoring would reduce repetition)
Symbols introduced: none (test-only)
Behavior change: none (test-only)
On-disk format change: none
Test coverage: purely additive, no new production code

Open questions and loose ends
- Whether to adopt Patrick Steinhardt’s suggested refactoring (loop over forbidden characters) to reduce repetition.
- Minor inconsistency in test fixture (`heads/` vs `refs/heads/`), though not treated as a blocker.

Participant positions
Patrick Steinhardt: Reviewed substantively; accepts the patch’s goal and praises the `@` edge case. Suggests a refactoring for test readability and notes the fixture inconsistency. Position is supportive with minor improvements requested.

Related work
Precedent: test-only patch `919eb8ace` (referenced in commit message).