Thread identity
Subject: [PATCH] t1402: add coverage for documented but untested refname characters
Series version: standalone patch (no series)
Originating author: Nikolaus Schuetz
Thread type: test-only patch

Core problem or goal
Add test coverage in `t1402-check-ref-format.sh` for characters already documented as forbidden in refnames but not explicitly tested in embedded form: space, tilde (`~`), caret (`^`), colon (`:`), asterisk (`*`), and open-bracket (`[`). Also verify that the single-character `@` refname is rejected even when `--allow-onelevel` is passed.

Current status
Under review; no prior version recorded.

Key technical details
Files touched: `t/t1402-check-ref-format.sh`
New test cases: eight new `invalid_ref` or `valid_ref` checks
Symbols introduced: none (test-only)
Behavior change: none (test-only)
On-disk format change: none
Test coverage: purely additive, no new production code

Open questions and loose ends
None raised yet.

Participant positions
None yet.

Related work
Precedent: test-only patch `919eb8ace` (referenced in commit message).