Thread identity
Subject: [PATCH] t1402: add coverage for documented but untested refname characters
Series version: standalone patch (v2)
Originating author: Nikolaus Schuetz
Thread type: test-only patch

Core problem or goal
Add test coverage in `t/t1402-check-ref-format.sh` for characters already documented as forbidden in refnames but not explicitly tested in embedded form: space, tilde (`~`), caret (`^`), colon (`:`), asterisk (`*`), open-bracket (`[`), and multi-byte sequences like `".."`. Also verify that the single-character `@` refname is rejected even when `--allow-onelevel` is passed.

Current status
Under review; second version posted. The update adopts the refactoring suggested by Patrick Steinhardt and Junio C Hamano: repetitive `invalid_ref` checks are replaced with a loop over forbidden characters (including multi-byte sequences like `".."` and backslash). The patch remains purely additive and test-only. No objections remain; discussion appears converged.

Key technical details
Files touched: `t/t1402-check-ref-format.sh`
New test cases: loop over forbidden characters (space, `~`, `^`, `:`, `*`, `[`, `".."`, backslash) and explicit `@` edge case
Symbols introduced: none (test-only)
Behavior change: none (test-only)
On-disk format change: none
Test coverage: purely additive, no new production code

Open questions and loose ends
None. The fixture inconsistency (`heads/` vs `refs/heads/`) was explained as inherited and not affecting validation logic; no further changes requested.

Participant positions
Patrick Steinhardt: Reviewed substantively; accepts the patch’s goal, praises the `@` edge case, and suggested the refactoring now adopted. Position remains supportive.
Junio C Hamano: Endorsed Patrick’s review, expanded the refactoring suggestion to include multi-byte sequences, and accepted the fixture explanation. Position remains supportive.
Nikolaus Schuetz: Incorporated feedback, posted v2, and clarified the fixture rationale. Position is responsive and aligned with reviewers.

Related work
Precedent: test-only patch `919eb8ace` (referenced in commit message).