Thread identity
Subject: [PATCH] t1402: add coverage for documented but untested refname characters
Series version: standalone patch (v2)
Originating author: Nikolaus Schuetz
Thread type: test-only patch

Core problem or goal
Add test coverage in `t/t1402-check-ref-format.sh` for characters already documented as forbidden in refnames but not explicitly tested in embedded form (space, `~`, `^`, `:`, `*`, `[`, `".."`, backslash) and verify that the single-character `@` refname is rejected even when `--allow-onelevel` is passed. The thread has now confirmed that the test suite’s coverage of refname validation rules is complete for all documented constraints: position-independent forbidden characters (space, `~`, `^`, `:`, `*`, `[`, `".."`, backslash) are tested in embedded form, and position-specific rules (dots at start/end of components, e.g., `"refs/heads/foo."` or `"foo.lock"`) are already covered by existing tests.

Current status
Converged and ready for integration. The second version adopts the refactoring suggested by Patrick Steinhardt and Junio C Hamano: repetitive `invalid_ref` checks are replaced with a loop over forbidden characters (including multi-byte sequences like `".."` and backslash). Nikolaus’s clarification resolved the last open question about completeness of start/end character coverage. Both reviewers have endorsed v2; no objections remain.

Key technical details
Files touched: `t/t1402-check-ref-format.sh`
New test cases: loop over forbidden characters (space, `~`, `^`, `:`, `*`, `[`, `".."`, backslash) and explicit `@` edge case; existing tests already cover position-specific edge cases (e.g., `"foo."`, `"foo.lock"`)
Symbols introduced: none (test-only)
Behavior change: none (test-only)
On-disk format change: none
Test coverage: purely additive, no new production code

Open questions and loose ends
None. Nikolaus’s reply confirms that the test suite now exhaustively covers all documented forbidden characters, both position-independent and position-specific.

Participant positions
Patrick Steinhardt: Reviewed substantively; accepts the patch’s goal, praised the `@` edge case, suggested the refactoring now adopted, and endorsed v2. Position remains supportive.
Junio C Hamano: Endorsed Patrick’s review, expanded the refactoring suggestion to include multi-byte sequences, accepted the fixture explanation, and endorsed v2. Raised a follow-up question about completeness of start/end character coverage, which Nikolaus has now resolved. Position remains supportive.
Nikolaus Schuetz: Incorporated feedback, posted v2, clarified the completeness rationale, and aligned with reviewers. Position is fully responsive and converged.

Related work
Precedent: test-only patch `919eb8ace` (referenced in commit message). The loop structure makes the test more maintainable and easier to extend if the forbidden character set changes in the future. The completeness confirmation ensures no further test additions are needed for the current rules.