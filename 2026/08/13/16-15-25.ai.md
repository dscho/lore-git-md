This is a **substantive review comment** from Jeff King (Peff) on Patrick Steinhardt’s hashmap counterproposal for the packfile performance regression fix. Peff identifies a **critical oversight** in Patrick’s alternative design: Git already maintains a hashmap for tracking packfiles in `packfile_store_add_pack()` and `packfile_store_load_pack()`, introduced in commit `ec48540fe8` (2019). This existing hashmap is how the `is_new`/`skip_dup_check` flag in Johannes Schindelin’s patch is determined, and it already prevents duplicates during "reprepare" operations (e.g., when reloading the packfile list after a `git fetch`).

The observation reframes the design discussion: the fast-path optimization in Johannes’s patch is not a workaround for a missing hashmap but a targeted optimization to avoid the O(N) scan *even when the hashmap already guarantees uniqueness*. Patrick’s counterproposal would duplicate functionality (adding a second hashmap) without addressing the core issue—why the existing hashmap isn’t sufficient to eliminate the quadratic-time behavior. This may prompt Patrick to revisit his approach or clarify how his proposed hashmap differs from the existing one (e.g., whether it covers `prepend()` operations or enables O(log N) reordering as claimed).

The comment is concise but **highly technical**, directly engaging with the code’s history and the interaction between the two hashmap mechanisms. It does not resolve the debate but shifts the burden of proof to Patrick to explain how his counterproposal improves upon the status quo. No new data or test results are provided; the value lies entirely in the historical context and the implied question: *If the existing hashmap already prevents duplicates, why is the O(N) scan still happening?*