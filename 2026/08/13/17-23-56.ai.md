This is a four-patch series from Kristoffer Haugsbakk adding three new formatting options (`--abbrev`, `--color`, and `--date`) to the `git format-rev` command, along with preparatory refactoring and documentation cleanup.

The series targets the `format-rev` subsystem, which is a relatively new built-in command (introduced in Git 2.45) designed to format revision ranges in a user-configurable way. Patch 1/4 tidies up option descriptions by switching to lowercase, patch 2/4 groups option variables into a struct for better maintainability, and patch 3/4 extracts the documentation for `--date` alternatives into a separate file to avoid duplication. The main feature work is in patch 4/4, which implements the three new options by reusing logic from `git log`'s formatting machinery.

The implementation touches `builtin/name-rev.c` (where `format-rev` lives), adds a new documentation file (`rev-list-option-date-alternatives.adoc`), and updates the test suite with coverage in `t/t6120-describe.sh`. The commit messages are clear and the code changes appear mechanical, leveraging existing Git infrastructure (e.g., `rev_info` struct, `pretty.c` date formatting) rather than inventing new logic. This suggests the patch is well-scoped and unlikely to introduce subtle bugs.

No prior discussion or thread context is provided, so it’s unclear whether these options were explicitly requested or are filling a gap noticed by the author. The series looks like a natural extension of `format-rev`'s capabilities, bringing it closer to parity with `git log`'s formatting flexibility. The preparatory patches (1–3) are uncontroversial cleanup, while the feature patch (4) may attract attention for its UI design choices (e.g., whether `--date` should support the same alternatives as `git log`).