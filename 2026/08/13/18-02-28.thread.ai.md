Thread identity: Bug report about mtime handling during `--autostash` operations in `git pull`, `git merge`, and `git rebase`. Originating author: Jos van den Oever.

Core problem or goal: When using `--autostash` with `git pull`, `git merge`, or `git rebase`, the mtime (modification time) of files restored via `stash apply` is incorrectly set to the current time, even if the file content was unchanged. This breaks mtime-based build systems and disrupts automated systems that rely on mtime as a proxy for commit author dates. The goal is to track and restore the original mtime of dirty files during `--autostash` operations.

Current status: Reported as a behavioral inconsistency; no prior discussion or related work referenced. No patch or series version exists yet. Status is "under review" (no maintainer decision or merge status).

Key technical details: The issue involves the interaction between `stash apply` and filesystem mtime attributes. Affected commands: `git pull --autostash`, `git merge --autostash`, `git rebase --autostash`. No files, symbols, or subsystems explicitly identified yet; likely touches the stash, merge, rebase, and worktree subsystems. No on-disk format changes or new CLI options.

Open questions and loose ends: (1) Whether this is considered a bug or a feature request. (2) How to track original mtime without breaking existing workflows. (3) Whether this should be opt-in or opt-out. (4) Potential edge cases (e.g., files modified both before and after stash, conflicts during stash apply).

Participant positions: Only the author (Jos van den Oever) has spoken so far, framing the issue as a behavioral inconsistency with practical consequences. No reviews or objections yet.

Related work: None mentioned in the report.