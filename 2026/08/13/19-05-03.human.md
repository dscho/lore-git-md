This is the second patch in Vincent Mailhol’s four-part series adding Bash completion for `git history`. It focuses on completing values for the `--empty` option, which accepts `drop`, `keep`, or `abort` for the `drop` and `fixup` subcommands.

The patch updates `contrib/completion/git-completion.bash` to handle the documented `--empty=<value>` syntax, ignoring the undocumented `--empty <value>` split form for simplicity. The implementation is straightforward: it checks the current subcommand (`drop` or `fixup`) and offers the appropriate completions when the user types `--empty=`. Tests in `t/t9902-completion.sh` verify that the values are completed correctly and that the option is ignored for unsupported subcommands like `reword`.

This is a small but well-targeted change that fits neatly into the series. The commit message clearly explains the design trade-off (omitting the split form for simplicity), and the tests cover the expected behavior. No controversy or open questions here—just a clean, incremental step toward full `git history` completion.