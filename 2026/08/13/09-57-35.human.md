This is the first patch in a two-part documentation series updating `Documentation/git-format-rev.adoc` as part of the ongoing man page synopsis-style conversion effort. The change is minimal and cosmetic: it replaces the `<subject>` placeholder in a code example with `"<subject>"` to match the quoted output shown immediately afterward, eliminating an inconsistency in the example.

The motivation is straightforward: the example demonstrates how `git format-rev --stdin-mode=text` transforms a commit object name into a formatted string using the `%s` placeholder, but the input format (`%s`) and output (`<subject>`) were not visually aligned. By quoting both (`"%s"` and `"<subject>"`), the example becomes clearer and more consistent. The commit message explains the implied input ("Did we not fix this in <commit object name>?") and how it maps to the quoted output, making the rationale self-contained.

This patch is uncontroversial and ready for merging. It touches only the documentation, introduces no behavioral changes, and aligns with the broader goal of improving readability in Git’s man pages. The second patch in the series (not shown here) adds a `[synopsis]` block to the same example to further standardize placeholder styling, but this first patch stands alone as a simple, well-justified fix. No objections or open questions remain in the thread.