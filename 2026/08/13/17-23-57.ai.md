This is the first patch in Kristoffer Haugsbakk’s four-patch series extending `git format-rev` with `--abbrev`, `--color`, and `--date` formatting options. It is a minor documentation cleanup targeting the `format-rev` subsystem.

The patch standardizes the capitalization of option descriptions in `builtin/name-rev.c` by converting them from mixed case ("Use NUL...") to lowercase ("use NUL..."), aligning with Git’s coding style. The change is purely cosmetic, affecting only the help text displayed by `git format-rev --help`. No functional or behavioral changes are introduced.

The diff is small and mechanical, touching only three lines in the `OPT_CALLBACK_F` and `OPT_BOOL` declarations. The commit message is clear and concise, explicitly stating the motivation (style consistency) and the scope (option descriptions). This patch is uncontroversial and likely to be accepted as-is, serving as a low-risk preparatory step for the feature work in later patches.

No review feedback has been posted yet, and the patch is not yet queued in any integration branch. It is part of a larger effort to bring `git format-rev` closer to parity with `git log`'s formatting flexibility, though this specific change has no direct impact on that goal.