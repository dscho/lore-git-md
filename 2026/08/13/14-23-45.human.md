This is a documentation patch, the first of two in a v3 series that tidies up the `<subject>` placeholder styling in `Documentation/git-format-rev.adoc`. It is part of the ongoing man-page synopsis-style conversion effort led by Jean-Noël Avila.

The patch addresses a small but visible inconsistency: the text describes formatting with `%s`, but the example output shows the subject wrapped in quotes. The implied input is a commit message like `Did we not fix this in <commit object name>?`, which should produce `"<subject>"`. The change simply adds quotes around `%s` in the explanatory text so that the example and the description match.

The commit message is clear and concise, explaining the inconsistency and the implied input without over-explaining. The diff is minimal—two lines changed in one file—and the rationale is uncontroversial. This patch is ready for merging; it has already collected Patrick Steinhardt’s `Acked-by` on the second patch in the series, and the first patch is a straightforward precursor. No code behavior, on-disk format, or test coverage is affected.