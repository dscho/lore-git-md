This is the v3 cover letter for Junio C Hamano’s three-patch series fixing Bash completion for `git checkout` (with or without `-C <dir>`). The series is now technically complete and ready for integration.

The goal remains unchanged: prioritize tracked paths (those in the index) for `git checkout` path completion, with a fallback to untracked paths only when no tracked paths match. The v3 update is minimal—patch 3/3 now includes `--directory` in the `__git_complete_index_file "--others"` call, aligning with the earlier `git diff` completion series and addressing Elijah Newren’s consistency concern. This change improves performance by avoiding unnecessary stat calls on non-directory files.

The series touches `contrib/completion/git-completion.bash` and adds eight new tests in `t/t9902-completion.sh`, covering edge cases like missing files (`lostfile`) and the fallback behavior. Patch 1/3 refactors `_git_checkout()` to prepare for the functional changes, patch 2/3 implements the core fix (tracked paths only), and patch 3/3 adds the fallback to untracked paths. All patches are well-tested and uncontroversial.

No open questions remain. The series is a follow-up to the `git diff` completion fix and is rebased onto the `complete-diff` topic for better test organization. The fallback in patch 3/3 is pragmatic but of limited utility for typical `git checkout` workflows—its primary value is ensuring completion never fails silently. The refactoring in patch 1/3 improves maintainability without altering behavior.