This is a small, self-contained test improvement from Nikolaus Schuetz. The patch adds test coverage in `t1402-check-ref-format.sh` for characters that are already documented as forbidden in Git refnames—space, tilde, caret, colon, asterisk, and open-bracket—but weren’t explicitly tested when embedded in refs. It also confirms that the single-character `@` refname is rejected even with `--allow-onelevel`, which would normally permit one-level names like `refs/@`.

The change is purely additive, touching only the test script and not altering any production code. The commit message cites an earlier test-only patch as precedent for documenting existing behavior through tests. There’s no prior discussion or review activity in this thread yet, so this appears to be the initial submission.