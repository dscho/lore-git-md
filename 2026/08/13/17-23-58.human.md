This is the second patch in Kristoffer Haugsbakk’s four-patch series extending `git format-rev` with `--abbrev`, `--color`, and `--date` formatting options. The patch refactors the command’s option variables into a dedicated struct (`format_rev_data`) to reduce clutter and prepare for the upcoming feature additions.

The change is purely internal: it groups existing variables (like `format`, `stdin_mode`, and NUL-termination flags) into a single struct, renames `stdin_mode_arg` to `stdin_mode` for clarity, and inlines the `format_nul_data` helper into the new struct. The diff is mechanical and confined to `builtin/name-rev.c`, with no behavior changes or new functionality. The commit message explains the motivation clearly—reducing local variables ahead of adding more options—and the implementation is straightforward, suggesting this is a low-risk cleanup.

No review feedback has appeared yet, but the patch looks uncontroversial. It’s a typical preparatory step in a larger series, and the struct-based organization will make the subsequent feature patches (3/4 and 4/4) easier to follow. If the series continues smoothly, this patch will likely be accepted as-is.