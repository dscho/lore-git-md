This is a reply in the `core.useNanosec` series, specifically addressing the cache location decision for the runtime config value. Patrick Steinhardt responds to D. Ben Knoble’s investigation and expresses a pragmatic stance: while `repo_config_values()` is the modern target for config-driven settings, its current limitation to `the_repository` is a blocker for further expansion until the project resolves how to support other repositories (e.g., submodules). He does not oppose landing the series with `repo_config_values()` if the CI run and partial control-flow analysis suggest it is safe, but he also offers a fallback—using a global variable as a temporary measure—if the project prefers to avoid expanding the `the_repository`-only surface area. The tone is conciliatory: he does not want the series to be blocked indefinitely but acknowledges the architectural tension. Key technical detail: the decision now hinges on whether the project is willing to accept the current limitation of `repo_config_values()` or prefers a stopgap (global variable or `repo_settings`) until the broader `the_repository` issue is resolved. No new performance data or test results are introduced; the focus remains on the cache location trade-off.