This is a refactoring patch that removes an unused `name` parameter from the `chdir_notify` API. The patch observes that the `name` field in `struct chdir_notify_entry` is always set to `NULL` and never used, so it simplifies the registration and unregistration functions by dropping the parameter entirely. The change propagates through ten files in the object database (ODB), refs, and setup subsystems, updating all callers to match the new function signatures.

The patch is mechanical and well-contained: it removes the `name` argument from `chdir_notify_register()` and `chdir_notify_unregister()`, updates the callback type definition, and adjusts all implementations and call sites. The commit message is concise but sufficient, explaining the motivation (simplifying logic in `chdir_notify_unregister`) and the observation that the parameter is unused. The diff is clean and shows no behavioral changes beyond the signature updates.

This is part of Git’s internal plumbing, not user-facing functionality, so there are no compatibility concerns or new features introduced. The change is a pure code hygiene improvement, reducing complexity in a low-level notification mechanism that tracks working directory changes. The patch touches multiple subsystems (ODB sources, ref backends, setup, and tmp-objdir), but all changes are uniform and follow the same pattern. There are no tests included, but the existing test suite should suffice to verify that the notification mechanism still works as intended.