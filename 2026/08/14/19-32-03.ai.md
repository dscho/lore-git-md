This is a procedural confirmation from the author, Siddharth Shrimali, in response to Junio C Hamano’s query about marking the `repack: introduce --drop-filtered for partial clones` series for the `next` integration branch. The series, now at v5 (6 patches), extends `git repack` with `--drop-filtered` and `--dry-run` options to safely reclaim disk space in partial clones by removing locally cached promisor blobs exceeding a user-specified size threshold. All prior feedback—including the blocking type-mismatch bug, documentation squashing, error message improvements, and mechanical cleanups—has been addressed, and Christian Couder has stated "This version looks good to me." The author confirms the series is ready for integration testing. No new technical concerns are raised; this email is purely a green light for Junio to queue the topic in `next`. The series is now **proposed for `next`** and awaits Junio’s explicit merge annotation. Key details remain unchanged from the thread summary: the feature is limited to `blob:limit` filters, enforces preconditions (promisor remote, `-a` required), avoids lazy fetches during enumeration, and includes safety optimizations (merge/rebase/cherry-pick checks, index validation). The implementation is technically complete, with follow-up work (remote verification, time-based culling, drop log) explicitly deferred.