This is a **substantive review follow-up** in the MIDX incremental-write series, specifically on patch 2/3. Patrick responds to Taylor’s defense of the existing test coverage for `--base=none` by reiterating a preference for direct verification that the MIDX contains objects from *all* layers (not just the newest one), rather than relying on bitmap generation as a proxy. He acknowledges the point is minor ("a nitpick") and does not dispute the fix’s correctness, but his phrasing ("a bit roundabout", "preferred if we verified the property directly") suggests lingering unease about the test’s thoroughness. The exchange leaves the test-coverage question unresolved, though the core fix remains uncontested. No code changes are proposed; this is purely a discussion of testing philosophy. The series is still under review, with the commit message for patch 2 likely needing an update to clarify the `--stdin-packs`-only scope of the bug.