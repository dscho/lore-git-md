This is the second patch in the v2 series converting `USE_NSEC` to a runtime config (`core.useNanosec`). It is a no-op refactoring of `repo_config_values_init()` in `environment.c` that reorders the assignments to match the struct declaration, grouping them by config section (`core`, `sparse`, `branch`) for clarity. The change is purely mechanical and does not alter behavior; it is intended to make future additions easier to validate.

The patch touches only `environment.c` and is uncontroversial. It was included in the series to prepare for the third patch, which adds a new field (`use_nanosec`) to `struct repo_config_values`. The reordering is a minor cleanup that has no performance impact and does not interact with the ongoing discussion about where to cache the `core.useNanosec` value (`repo_config_values`, `repo_settings`, or a global variable).

No testing or documentation changes are needed, and the patch does not conflict with `ty/repo-config-cleanups` (the conflict is in the third patch). The commit message is clear and the diff is straightforward, with the `Best-viewed-with: --ignore-space-change` hint now moved into the message body as requested in review. This patch is ready to advance independently if the series is split.