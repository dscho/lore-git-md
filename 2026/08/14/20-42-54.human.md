This is a **refactoring patch** that removes an unused `name` parameter from the `chdir_notify` API. The patch itself is straightforward and has already received substantive review from Jeff King (Peff), who confirmed its correctness. The discussion here is purely about improving the commit message to document the historical context of why the `name` parameter became unused.

Junio C Hamano joins the thread to clarify the timeline and rationale behind the removal of `chdir_notify_reparent()`, the last function that used the `name` field. He confirms that the refs subsystem's files backend shifted to a different solution in the same series that removed `chdir_notify_reparent()`, making the parameter obsolete. This aligns with Peff's earlier explanation, which traced the deprecation of the `name` field across commits `5bf546755c` (June 2026), `1f43ff2c7e`, and `0de2467e6c`.

The patch touches ten files across the ODB, refs, setup, and tmp-objdir subsystems, but it is a pure refactoring with no behavioral changes. The existing test suite should verify that the notification mechanism remains functional. There are no open technical concerns, and the only remaining task is to update the commit message to include the historical context discussed in this thread. This will help future readers understand why the parameter was removed and avoid confusion about whether its absence is intentional or a bug. The patch looks ready to proceed once the commit message is updated.