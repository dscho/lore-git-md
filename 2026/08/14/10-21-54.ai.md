This is a minor stylistic follow-up in the ODB alternates refactoring series. Karthik Nayak suggests a one-line simplification to the `odb_source_paths_cmp()` function in patch 1/4, replacing the conditional block that initializes `o->source_paths_icase` with a direct assignment. Patrick Steinhardt explains why the original version is necessary: if the configuration key is missing, the field would remain unset (`-1`), causing repeated config lookups. He offers an alternative but prefers the original for readability. Karthik accepts the explanation, and no changes to the patch are expected.

The discussion is purely about code style and does not affect the patch’s correctness or the series’ broader goal of decoupling path comparisons from `the_repository`. The series remains queued in `seen` with all prior feedback addressed.