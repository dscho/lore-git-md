Thread identity
Subject: [GSoC microproject] submodule: warn on valueless submodule.active config
Series version: standalone patch (no series), v2 (resubmitted inline)
Originating author: Tilak Raaz (first-time contributor, GSoC 2027 microproject)
Thread type: patch (bug fix / behavior change)

Core problem or goal
Address a NEEDSWORK comment in `submodule.c` by changing how Git parses valueless `submodule.active` configuration entries. Currently, `repo_config_get_string_multi()` silently ignores such entries; the patch switches to `repo_config_get_value_multi()` and emits a warning when a valueless key is encountered, adding a test case in `t7400-submodule-basic.sh`. The goal is to align with Git's existing error-handling patterns for non-boolean configuration keys.

Current status
Under review (second iteration posted inline for `b4` compatibility). Patch applicability remains blocked by persistent whitespace corruption (likely GMail-induced), preventing substantive technical review. Maintainer feedback suggests using the existing `config_error_nonbool()` helper instead of a custom warning message, but no merge status or maintainer decision has been recorded.

Key technical details
- Files touched: `submodule.c`, `t/t7400-submodule-basic.sh`
- New behavior: valueless `submodule.active` entries now trigger a warning via `config_error_nonbool()` (per maintainer feedback) instead of being silently ignored
- Old behavior: valueless entries were silently ignored
- New symbols: none (uses existing `repo_config_get_value_multi()` and `config_error_nonbool()`)
- Test coverage: one new test (`test_expect_success 'warn on valueless submodule.active config'`)
- On-disk format: unchanged
- CLI: unchanged

Open questions and loose ends
- Whether the warning is the correct approach (alternatives: treat valueless keys as `true`, `false`, or continue silently ignoring)
- Potential backward compatibility impact: scripts or workflows that rely on the silent behavior may now emit warnings
- Whether the warning message (now standardized via `config_error_nonbool()`) is actionable or clear enough for users
- Patch cannot proceed to technical review until the formatting corruption is resolved
- No substantive technical review yet; feedback has focused on implementation consistency (use of `config_error_nonbool()`) and mechanical issues (whitespace corruption)

Participant positions
- Tilak Raaz (author): proposes the warning-based approach, cites NEEDSWORK comment as justification, provides test coverage. Resubmitted patch inline to address `b4` compatibility and prior formatting feedback. Position unchanged on technical design but may need to revise implementation to use `config_error_nonbool()`.
- Weijie Yuan: surface-level feedback on patch submission mechanics (attachment format, `b4` compatibility); no engagement with technical content.
- D. Ben Knoble: surface-level but critical feedback identifying persistent indentation corruption (likely GMail-related) that blocks patch applicability. Recommends `git send-email` for future submissions. No engagement with the patch’s technical design.
- Junio C Hamano (maintainer): substantive feedback suggesting use of `config_error_nonbool()` for consistency with Git's existing error-handling patterns. Identifies ongoing whitespace corruption as a blocker for review. No merge decision implied; patch remains under review.

Related work
- GSoC 2027 microproject (explicit framing)
- Addresses a documented TODO in the codebase (NEEDSWORK comment in `submodule.c`)
- Leverages Git's existing `config_error_nonbool()` helper for standardized error reporting