**Summarizer brief mode**

This is a **substantive review exchange** between Junio C Hamano and Jeff King (Peff) about the design of patch 1 in Patrick Steinhardt’s ODB alternates refactoring series. The discussion centers on the trade-offs of the current case-insensitivity handling for alternate path deduplication and explores alternatives.

Junio responds to Peff’s earlier critique of the patch’s reliance on `core.ignoreCase` for deduplication. He acknowledges Peff’s point that duplicates are mostly a performance issue but raises a hypothetical future scenario where a tool might enumerate objects across alternates and accidentally remove objects from the first alternate if it was registered under a different spelling (e.g., due to case differences). This reinforces Peff’s concern about correctness over complexity.

Junio endorses Peff’s proposal to simplify the code by dropping case-insensitivity entirely, prioritizing correctness in ambiguous cases. He agrees that the added complexity of the current approach is not justified. However, he pushes back on Peff’s alternative suggestion to use `stat()` to compare `st_dev` and `st_ino` for deduplication, citing distrust of these fields on platforms where case insensitivity is a concern (e.g., macOS). Junio notes that even `stat()`-based comparisons are insufficient, as filesystems may perform one-way transformations like Unicode normalization or stripping code points, which could still lead to false deduplication.

The exchange leaves the design question unresolved but shifts the discussion toward simplicity. Junio’s preference for dropping case-insensitivity entirely (despite the risk of occasional duplicates) suggests the current patch’s approach may be over-engineered. The mention of macOS hints at platform-specific edge cases that could complicate any solution. No new technical details or code paths are introduced, but the conversation clarifies the stakes: correctness in edge cases versus maintainability. Future replies will likely need to address whether to adopt Peff’s "simpler is better" stance or revisit the `stat()`-based approach with additional safeguards.