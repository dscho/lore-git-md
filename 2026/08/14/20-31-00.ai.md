This is a **refactoring patch review** with **substantive depth**. Jeff King (Peff) reviews Colin Hinton’s patch to remove the unused `name` parameter from the `chdir_notify` API. Peff confirms the patch is correct but suggests improving the commit message by documenting the historical context: the `name` field was only used by `chdir_notify_reparent()`, which was itself removed in commit `5bf546755c` (June 2026). Earlier commits (`1f43ff2c7e`, `0de2467e6c`) had already stopped passing descriptive names in favor of `NULL`, preparing for the removal of the reparent function. Peff notes the patch is safe and the unused parameter can be removed, but the commit message should explain why the field became unused to avoid confusion about whether its absence is intentional or a bug. No code changes are requested; this is purely about commit message clarity. The review is thorough, tracing the parameter’s history across multiple commits and subsystems (refs, ODB, setup).