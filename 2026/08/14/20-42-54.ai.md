This is a **refactoring patch discussion** following up on Colin Hinton’s patch to remove the unused `name` parameter from the `chdir_notify` API. The email is a brief exchange between Junio C Hamano and Jeff King (Peff) that clarifies the historical context of the parameter’s deprecation and confirms the patch’s safety.

Junio initially expresses curiosity about when the `name` parameter became unused, noting it’s unusual for an API to introduce a field that was never used. Peff responds by tracing the history: the `name` field was only used by `chdir_notify_reparent()`, which was removed in commit `5bf546755c` (June 2026). Earlier commits (`1f43ff2c7e`, `0de2467e6c`) had already stopped passing descriptive names, preparing for the reparent function’s removal. Junio confirms this explanation, noting that the refs subsystem’s files backend shifted away from using `chdir_notify_reparent()` in the same series, making the API obsolete.

The discussion reinforces Peff’s earlier review request for an improved commit message to document this history. No technical concerns or code changes are raised; the focus remains on ensuring the commit message clearly explains why the `name` parameter is safe to remove. The patch itself is still under review, with no objections to its correctness or approach. The key takeaway for future discussion is the need to update the commit message to include the historical context outlined here.