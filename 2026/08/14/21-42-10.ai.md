This is a refactoring patch that removes the unused `name` parameter from the `chdir_notify` API. The v2 iteration addresses the only remaining feedback from the review: the commit message now thoroughly documents the historical context of the `name` parameter, tracing its deprecation across three prior commits (`5bf546755c`, `1f43ff2c7e`, and `0de2467e6c`). The patch itself is unchanged from v1, retaining its clean mechanical removal of the parameter from `chdir_notify_register()`, `chdir_notify_unregister()`, and the callback type definition, along with updates to all callers in the ODB, refs, setup, and tmp-objdir subsystems.

The patch is a pure code hygiene improvement with no behavioral changes. It touches ten files, all following the same pattern of removing the `name` argument and simplifying the unregistration logic. The existing test suite should suffice to verify that the working-directory notification mechanism remains functional. Both Jeff King and Junio C Hamano have confirmed the correctness of the change, and the updated commit message now satisfies their request for historical documentation. This patch is ready for merging and carries no compatibility concerns or loose ends.