This is a procedural follow-up in the `uploadpack.lazyFetchTrusted` series thread. Christian Couder acknowledges Junio C Hamano’s feedback about the misthreaded v2 patches and explains what went wrong: he passed `--in-reply-to` to `git format-patch` instead of `git send-email`, and because `format.thread` was unset (defaulting to `--no-thread`), the option applied to every patch rather than just the cover letter. He confirms he uses plain `git format-patch` and `git send-email` without custom configuration and agrees to adopt Junio’s suggested workflow, including the `--no-chain-reply-to` flag, to ensure correct threading in future resends. No technical discussion or review of the feature itself occurs in this email. The next step is for Christian to resend the series with proper threading before substantive review can proceed.