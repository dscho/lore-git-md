**Summarizer brief mode**

This is a **substantive design discussion** continuing the review of patch 1 in Patrick Steinhardt’s ODB alternates refactoring series. Jeff King (Peff) and Junio C Hamano explore the risks and trade-offs of the current case-insensitive deduplication approach and potential alternatives, with Peff advocating for simplicity and Junio cautioning about edge cases.

Peff downplays the risk of duplicate alternates causing data loss, calling it a "shoot yourself in the foot" scenario that would require deliberate misconfiguration (e.g., an object directory pointing to itself as an alternate). He acknowledges Junio’s hypothetical future tool that might enumerate and deduplicate objects across alternates, but argues that such a tool would need to be carefully designed to avoid the very issue Junio describes. Peff’s stance reinforces his earlier preference for dropping case-insensitivity entirely, accepting occasional duplicates as a low-risk trade-off for simpler code.

Junio remains skeptical of `stat()`-based deduplication, reiterating his distrust of `st_dev` and `st_ino` on platforms like macOS, where filesystems perform one-way transformations (e.g., Unicode normalization or stripping code points). This complicates any solution that relies on filesystem metadata for deduplication, as even `stat()`-based comparisons could miss "equivalent" spellings. Junio’s mention of macOS underscores the platform-specific challenges of the problem, though he does not propose an alternative.

The discussion leaves the design question unresolved but solidifies the consensus toward simplicity. Peff’s reluctance to pursue `stat()`-based deduplication aligns with Junio’s concerns, making the current case-insensitive approach increasingly untenable. The next steps will likely involve Patrick Steinhardt revisiting the patch to either (1) drop case-insensitivity entirely or (2) propose a new approach that addresses Junio’s platform-specific edge cases without adding complexity. Key technical details to watch include:
- **Case-sensitivity handling**: Whether the patch will retain, simplify, or remove it.
- **Filesystem transformations**: How the solution accounts for macOS and other platforms with one-way transformations.
- **Performance trade-offs**: The impact of eager alternate loading and deduplication on commands like `git fsck`.
- **Symbols/functions**: Any changes to `odb_source_paths_cmp()`, `odb_source_by_path_cmp()`, or the `source_paths_icase` field in `struct object_database`.