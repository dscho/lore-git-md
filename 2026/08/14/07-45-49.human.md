This is the second patch in Kristoffer Haugsbakk’s two-part documentation series updating `Documentation/git-format-rev.adoc` as part of the ongoing man page synopsis-style conversion. The patch itself is uncontroversial—it adds a `[synopsis]` block to a code example to properly highlight the `<subject>` placeholder—and already carries Patrick Steinhardt’s `Acked-by`.

The new development here is a procedural hiccup: Junio noticed the patch fails CI under AsciiDoc (while passing locally under AsciiDoctor), and Kristoffer has now identified the root cause. The issue is a mismatch in block delimiters: the patch uses `----` (code block) where the existing `pretty-formats.adoc` uses `--` (open block). This is a surface-level build system compatibility issue, not a technical flaw in the documentation itself. Kristoffer acknowledges the oversight and plans to send a corrected version.

The patch remains ready for merging from a technical standpoint, but this small formatting inconsistency must be resolved before it can advance to `next`. The fix is straightforward and unlikely to affect the broader synopsis-style conversion effort.