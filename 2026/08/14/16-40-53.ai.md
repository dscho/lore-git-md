This is a procedural follow-up in the `uploadpack.lazyFetchTrusted` series. Junio C Hamano replies to Christian Couder’s explanation of the misthreaded v2 patches, confirming that the issue stems from passing `--in-reply-to` to `git format-patch` (which applies the header to every patch) rather than to `git send-email` (which would apply it only to the cover letter). He notes that neither plain `git send-email` nor GitGitGadget typically produce this misthreading and suggests Christian’s mentees likely avoid the issue by not using such options. The email is purely diagnostic, reinforcing the earlier request for a resend with correct threading (using `--no-chain-reply-to`) before substantive review proceeds. No technical discussion of the feature itself occurs here.