This is the first patch in Pablo Sabater’s ten-patch GSoC series (v6) that extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder for remote object queries. The patch is a test cleanup in `t/t5701-git-serve.sh`, replacing the `wc -c | xargs` idiom with Git’s `test_file_size()` helper and caching the output of `git rev-parse two:two.t` in variables. The change is purely mechanical and uncontroversial, motivated by portability and the preservation of command exit statuses within here-documents. Junio has explicitly endorsed this patch as ready to merge, noting that the new version preserves the exit status of commands run inside `$(...)` whereas the old version using `wc -c | xargs` would have masked failures. The commit message has been updated to reflect this rationale, and the patch touches only one file (`t/t5701-git-serve.sh`), making seven insertions and four deletions. No behavior changes or technical concerns are involved.