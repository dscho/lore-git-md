This update to the "What's cooking in git.git" report for August 2026 (#03) provides a minor but important correction to the integration branch statistics. Since the last report, the project has seen the 12th batch of topics graduate to `master`, bringing the total non-merge commits there to 409 (up from 401). Meanwhile, `next` has shrunk slightly to 48 commits (down from 55), reflecting both graduations and reversions of some topics, while `seen` now holds 261 commits outside of `next` (up from 235), indicating new or experimental work entering the pipeline.

The correction is purely numerical and doesn’t change the status of any individual topic—whether they’re cooking in `next`, stalled, or marked for discard. For example, `ss/repack-drop-filtered` (space reclamation in partial clones) and `cc/lazy-fetch-trusted-bit` (new `uploadpack.lazyFetchTrusted` config) remain in their prior states, with rerolls still expected for both. Similarly, `hn/checkout-track-fetch` is still marked for discard unless reworked. The update simply confirms that the integration process is active, with topics progressing as usual. If you’re tracking a specific topic, the original report’s details remain the authoritative reference.