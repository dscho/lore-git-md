This is a surface-level review from SZEDER Gábor on D. Ben Knoble’s `[PATCH 3/3]` converting `USE_NSEC` to a runtime config (`core.useNanosec`). Gábor notes that the commit message’s `Best-viewed-with: --ignore-space-change` directive should not be placed above the three-dash line (where it appears in the current patch) but could still be useful below it, either as a free-form note or as a trailer. The comment is purely about commit-message formatting and does not engage with the technical substance of the patch. Junio had already identified the performance-critical flaw in the implementation (config key parsing on every `is_racy_stat()` call), so this feedback is a minor nit rather than a blocker. The series as a whole remains under review, with a v2 expected to address Junio’s caching suggestion.