Thread identity
Subject: Inconsistent error handling for invalid `-X` options between `git rebase` and `git merge`
Series version: N/A (bug report, no patch series)
Originating author: zrzut01@poczta.fm
Thread type: bug report

Core problem or goal
A usability bug in Git where `git rebase -X <invalid-option>` silently ignores the invalid strategy option and proceeds with the rebase, while `git merge -X <invalid-option>` correctly reports an error. This inconsistency can mislead users into believing the operation succeeded with the intended option, when the option was actually ignored.

Current status
Reported; no prior discussion or related commits identified. No patch or fix proposed yet. The issue is confirmed reproducible on Git 2.55.0.

Key technical details
- Commands affected: `git rebase` and `git merge`
- Subsystem: merge strategy machinery (option parsing for `-X`/`--strategy-option`)
- Behavior: `git merge` errors out on invalid `-X`; `git rebase` silently ignores it
- Files likely involved: rebase and merge command implementations, possibly shared strategy-option parsing code
- No new symbols, CLI options, or on-disk format changes introduced by this bug
- Test coverage: none for this specific error condition (reproduction steps provided in the report)

Open questions and loose ends
- Why the error-checking logic differs between `git rebase` and `git merge`
- Whether this inconsistency affects other commands that accept `-X` (e.g., `git cherry-pick`, `git revert`)
- What the correct behavior should be: should `git rebase` error out like `git merge`, or should both silently ignore invalid options?
- No maintainer or reviewer response yet; the report is currently unacknowledged

Participant positions
- zrzut01@poczta.fm: reported the bug, provided reproduction steps, and argued for consistent error handling (no explicit position on whether to error or ignore, but implied preference for erroring to avoid silent failures)

Related work
None identified in the report. No mention of prior versions, in-flight topics, or external dependencies.