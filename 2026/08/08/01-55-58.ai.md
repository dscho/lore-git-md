This is Junio’s "What’s cooking" report for August 2026 (#03), summarizing the state of integration branches and in-flight topics. The report is the canonical snapshot of what is queued, cooking, or stalled in the Git project’s integration pipeline.

Key integration branch updates: 401 non-merge commits have graduated to `master` since Git 2.55, bringing the next release closer. The `next` branch currently holds 55 non-merge commits (some reverted), while `seen` contains 235, including everything in and out of `next`. The report explicitly notes that topics in `next` are candidates for a future release but are still subject to rewinding or dropping—nothing here is merged yet.

Notable graduated topics include a boundary case fix in pack-bitmap (dl/pack-bitmap-position-zero), a sparse-index crash fix (ds/sparse-index-ita-crash), a segfault fix in `git cat-file --batch-command` (jk/cat-file-batch-wo-type-fix), a memory leak fix in `git merge` (tc/merge-default-to-upstream-leakfix), and a refactoring of `get_commit_action()` to be a pure predicate (mm/revision-pure-get-commit-action). These are all small, targeted bugfixes or cleanups that have passed review and integration testing.

New topics of interest include ss/repack-drop-filtered, which teaches `git repack --drop-filtered` to delete local promisor blobs exceeding a size limit in partial clones, reclaiming space. The topic is marked as expecting a reroll due to a SQUASH comment and review feedback. ty/repo-config-cleanups simplifies repository configuration getters by removing redundant NULL checks and improving documentation. vm/complete-history adds command-line completion for the experimental `git history` command, including pathspec and option completion. cc/lazy-fetch-trusted-bit introduces a new `uploadpack.lazyFetchTrusted` configuration to control lazy fetching behavior on trusted repositories, with the topic also marked as expecting a reroll.

Several topics are stalled or waiting for response, including bl/t7412-use-test-path-helpers (modernizing a submodule test script), tb/repack-geometric-cruft (combining `--geometric` and `--cruft` repack options), and hn/checkout-track-fetch (a feature to fetch the start-point branch during `git checkout --track`). The latter is marked as "Will discard," indicating it is unlikely to proceed without significant rework.

Cooking topics of note include ps/odb-streams, which consolidates object database streaming APIs into a unified `struct odb_stream`, and bc/restrict-hex-to-lowercase, which prepares for Git 3.0 by rejecting uppercase hexadecimal characters in object IDs when running in breaking changes mode. js/mingw-build-updates upstream a collection of Git for Windows build improvements, including dropping obsolete compatibility options and allowing `git.exe` to be used directly. ps/cat-file-remote-object-info-type extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder for remote object info.

The report also highlights several documentation and test improvements, such as kh/doc-replay-config (updating `git replay` documentation to reference its configuration variables) and sk/test-commit-body-helper (introducing a `commit_body()` test helper to simplify commit message extraction in tests). hn/bisect-reset-when-found adds a `--reset-when-found` option to `git bisect` to automatically reset to the original state or jump to the found culprit. hn/history-squash introduces a new `git history squash` subcommand to fold a range of commits into a single commit, with descendants replayed on top.

Overall, the report reflects a mix of bugfixes, cleanups, new features, and ongoing refactoring efforts. The topics in `next` are the most stable and likely to graduate soon, while those in `seen` are still under active review or revision. The stalled topics serve as a reminder of ongoing discussions or design challenges that have not yet been resolved.