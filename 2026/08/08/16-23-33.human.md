This is a **bugfix** that resolves a use-after-free in `get_oid_with_context_1()` when resolving relative paths in `:<stage>:<path>` syntax against a sparse index. The patch, authored by Shlok Kulshreshtha, addresses a memory management flaw where a dynamically allocated string was freed prematurely while still being referenced. René Scharfe’s review confirms the fix and test are sound.

The bug is subtle but real, requiring three specific conditions to trigger: a relative path, a sparse directory in the index, and a leading colon followed by a digit (e.g., `:0:./dir/`). Without the fix, the code exhibits undefined behavior—garbled error messages or heap-use-after-free crashes under `SANITIZE=address`. The patch consolidates control flow to ensure the string is freed only after its final use in `reject_tree_in_index()`.

The fix is minimal and focused, touching only `object-name.c` and adding a regression test in `t1092-sparse-checkout-compatibility.sh`. The test reliably reproduces the issue and verifies the fix under `SANITIZE=address`. René’s review is brief but substantive, endorsing both the code change and the test coverage.

This is a well-motivated, standalone bugfix with no dependencies or follow-up work. The commit message is thorough, referencing prior discussion (including a 2022 suggestion from Junio) and providing a clear reproducer. The patch looks ready for `next` and is unlikely to be controversial. If you care about sparse index correctness or memory safety, this is worth reviewing.