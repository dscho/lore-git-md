This is patch 5/10 in Pablo Sabater’s GSoC v6 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch converts the last remaining error path in `fetch_object_info()` to `die()`, aligning with Git’s defensive programming practices and addressing a latent robustness issue.

The change is purely mechanical: the function signature is updated to `void`, the final `return 0` is removed, and the short-read error path now calls `die()` with a descriptive message instead of returning `-1`. This ensures consistent error handling across all paths in `fetch_object_info()` and eliminates a subtle bug where a short read would propagate a generic error up the call stack. The patch touches three files (`fetch-object-info.c`, `fetch-object-info.h`, and `transport.c`), updating function signatures and removing the now-unused return value check in `fetch_object_info_via_pack()`.

The motivation is clear: the original design left one error path unhandled, creating a potential for silent failures or misleading error messages. By centralizing error handling in `fetch_object_info()`, the patch improves code hygiene and makes the function’s behavior more predictable. Junio has explicitly endorsed this patch as ready to merge, and the change is uncontroversial, with no behavior changes or new functionality introduced. The patch is part of the broader effort to harden the remote-object-info protocol and prepare the codebase for object type metadata support.