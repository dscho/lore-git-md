This is a **substantive review** of the second patch in Christian Couder’s 12-patch v2 series refactoring `git fast-import`’s usage string and advancing its libification. The patch adds a new "Option flags" subsection to `Documentation/technical/api-parse-options.adoc`, documenting the per-option flags (e.g., `PARSE_OPT_HIDDEN`, `PARSE_OPT_OPTARG`) that can be set via the `flags` field of `struct option` or the `OPT_*_F()` macro variants.

Elijah Newren’s review focuses on the accuracy and precision of the documentation for `PARSE_OPT_LASTARG_DEFAULT`. He quotes the existing comment from `parse-options.h` and suggests a rewording to clarify that the no-argument form is only accepted when the option is the last token on the command line, and that it should not be combined with `PARSE_OPT_OPTARG`. His feedback is narrowly scoped but technically precise, targeting a single flag’s description rather than the patch as a whole. The review does not question the patch’s overall value or direction, nor does it address the broader libification effort. No test results or alternative implementations are proposed; the feedback is purely editorial and aimed at improving clarity. The patch remains under review, with no indication of merge status or broader community engagement beyond this exchange.