This is the first patch in Pablo Sabater’s GSoC v6 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder. The patch is a test cleanup targeting `t/t5701-git-serve.sh`, replacing the `wc -c | xargs` idiom with the `test_file_size()` helper and caching the output of `git rev-parse two:two.t` in variables (`two_oid` and `two_size`). The change is purely mechanical and uncontroversial, motivated by portability and the preservation of command exit statuses within here-documents. The original version masked the exit status of `wc -c` if it failed; the new version preserves exit statuses and is functionally identical. Junio has explicitly endorsed this patch as ready to merge, noting that the change is a straightforward improvement with no behavior changes or technical concerns. The commit message has been updated to clarify the rationale, and the patch is marked as ready for integration.