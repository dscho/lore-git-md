This is a bug report about inconsistent behavior between `git rebase` and `git merge` when given an invalid `-X` (or `--strategy-option`) argument.

The email demonstrates that `git rebase -X nonexistent-option` silently ignores the invalid option and proceeds with the rebase, while `git merge -X nonexistent-option` correctly reports an error. The author argues this inconsistency can mislead users into believing the operation succeeded with the intended option, when in fact the option was ignored. The report includes a detailed reproduction scenario using a test repository, showing the exact commands and outputs that illustrate the problem.

The issue appears to be a genuine usability bug: the lack of error reporting for invalid `-X` options in `git rebase` creates a silent failure mode that could confuse users. The report is well-structured, with clear steps to reproduce and a specific version of Git tested (2.55.0). No patch is provided, so this is purely a problem report rather than a fix.

Key technical details for follow-up discussion: the `-X` option is processed by the merge strategy machinery, and the inconsistency suggests the error-checking logic differs between the rebase and merge code paths. The report does not mention any prior discussion or related commits, so this appears to be a newly identified issue.