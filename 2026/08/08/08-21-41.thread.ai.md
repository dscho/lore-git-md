Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message that helps users resolve the ambiguity. The discussion has shifted from suggesting workarounds (fully qualified branch names or `checkout.defaultRemote`) to prioritizing *discovery* by listing the conflicting remotes (e.g., "the branch exists on `origin`, `upstream`, and `home`"). The advice remains gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default).

Current status: Under review. No prior versions. Junio C Hamano’s feedback refines the usability goal, pushing for a more actionable message that lists conflicting remotes rather than assuming the user’s issue is syntactic. The patch touches `builtin/worktree.c` (DWIM logic and advice message) and `t/t2400-worktree-add.sh` (updated tests). Implementation remains localized and follows advice-message conventions, but the advice content is now contested.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- Advice message: Static string; Junio’s feedback requests replacing the generic workaround with a list of conflicting remotes to aid discovery. No functional change to DWIM logic itself.
- Behavior change: Advice shown only when ambiguity is detected; the message’s content is the sole point of contention.
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage may need expansion to verify the new advice format.
- Style nit: Junio previously flagged an overly long line with unnecessary braces in the code (carried forward).

Open questions and loose ends:
- Whether to split the advice message into scenario-specific variants (e.g., `worktree.guessremote` set vs. unset) or focus on the more universal problem of remote discovery.
- How to format the list of conflicting remotes in the advice message (e.g., inline vs. bullet points, sorting order).
- Whether the commit message adequately explains the shift from syntactic workarounds to discovery-focused advice.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; proposed the usability improvement with a single advice message covering syntactic workarounds.
- **Junio C Hamano**: Reviewer; raised substantive concerns about the advice message’s effectiveness, arguing it should prioritize *discovery* (listing conflicting remotes) over syntactic guidance. Feedback is high weight, focusing on user experience and actionability.
- **Harald Nordgren**: Surface-level endorsement ("This is an interesting idea!") with no technical engagement or critique. Low weight; does not advance the discussion.

Related work: None mentioned. This remains a standalone usability improvement.