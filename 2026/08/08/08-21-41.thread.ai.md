Thread identity: Patch series improving user-facing error messages for `git worktree add`, `git checkout`, and `git switch` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Fifth iteration (v5) of a two-patch series originating from Yoichi NAKAYAMA. Thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed, actionable advice message to help users resolve ambiguity. The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and provides a static list of conflicting remotes (e.g., "the branch exists on `origin`, `upstream`") for immediate usability. The patch also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression, as the command was already failing — just with less clarity. The problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading "invalid reference" error) is explicitly traced in the commit message to justify the early `die()` as intentional. The implementation avoids altering `dwim_branch()`'s return behavior, addressing concerns about workflow disruption.

Current status: Functionally complete and ready for merging. The series is now fully implemented across `git checkout`, `git switch`, and `git worktree add` via the shared `be_explicit()` helper and `advise_ambiguous_remote()` logic introduced in the first patch. The second patch applies this infrastructure to `git worktree add`, completing the unification. Only minor code nits (unnecessary newline, braces) remain unaddressed; these are not blockers. No merge status is reported; the series remains under review but is technically ready for integration.

Key technical details:
- Files: `builtin/worktree.c`, `builtin/checkout.c`, `checkout.c`, `checkout.h`, `t/t2400-worktree-add.sh`
- New/updated functions: `unique_tracking_name()` (extended to optionally collect remote names via `struct string_list *dwim_remote_names`), `be_explicit()` (new shared helper for advice messages), `advise_ambiguous_remote()` (new helper for emitting advice)
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- Advice message: Proposes a static list of conflicting remotes (e.g., "the branch exists on `origin`, `upstream`") and suggests using `--track` or setting `checkout.defaultRemote` for future disambiguation. The message is tailored to the command (`checkout`/`switch` vs. `worktree add`).
- Behavior change: Moves an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. The diagnostic logic is relocated to avoid altering `dwim_branch()`'s return behavior, addressing workflow concerns. The commit message justifies this change by tracing the problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading error).
- Error message: Now includes the number of matching remotes (e.g., "matched multiple (2) remote tracking branches") for clarity.
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage verifies the new advice format, error message format, and error-handling behavior. The `--guess-remote` case is explicitly excluded from emitting warnings/advice.
- Edge cases: The ambiguous branch warning does not trigger for fully qualified branch names (e.g., `origin/bar-topic`), as these are resolved earlier in the code path. The detached HEAD scenario is dismissed as unlikely and irrelevant. A minor edge case involving typos in fully qualified branch names (e.g., `origin/bar-topik` matching multiple remotes due to partial matches) is noted but considered too rare to address.

Open questions and loose ends:
- Minor code nits (unnecessary newline, unnecessary braces) remain unaddressed but are not blockers.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; initially proposed a command-based advice message (`git branch -r --list '*/<name>'`) but accepted Junio’s refactoring to a static list of remotes, prioritizing immediate usability. Clarified that the patch moves an existing `die()` call earlier to improve diagnostics, not introduce a new failure mode, and adjusted the implementation to avoid altering `dwim_branch()`'s return behavior. Dismissed the detached HEAD edge case as unlikely and declined to extend `unique_tracking_name()` further due to scope constraints. The v5 iteration splits the series into two patches: the first introduces the shared helper and advice logic, while the second applies it to `git worktree add`. Both patches are now posted and functionally complete, with only minor nits remaining.
- **Junio C Hamano**: Reviewer; proposed and implemented a refactoring to extend `unique_tracking_name()` to optionally return the list of conflicting remotes, enabling a static list in the advice message. The refactoring unifies the advice logic across `checkout`, `switch`, and `worktree add` via the new shared helper (`be_explicit()`). Junio’s position prioritizes immediate usability and code maintainability over script-friendly workflows. His feedback is high weight and now includes a fully functional alternative implementation that has been adopted.
- **D. Ben Knoble**: Reviewer; initially raised a critical technical concern about the unannounced `die()` behavior change and potential workflow disruption. The concern was mitigated by moving the diagnostic logic out of `dwim_branch()`, preserving the function’s return behavior. Endorses signaling ambiguity (via warning or advice) when no deterministic resolution policy exists, but supports omitting it in the `--guess-remote` case where ambiguity does not block the operation.
- **Harald Nordgren**: Surface-level replies only. Initially posted a one-line endorsement ("This is an interesting idea!") without context, later clarified it referred to Junio’s suggestion to list conflicting remotes. No technical engagement or critique; low weight.

Related work: Junio’s refactoring unifies the advice logic across `git checkout`, `git switch`, and `git worktree add`, addressing a long-standing inconsistency in how Git handles ambiguous remote branch names. This is a broader usability improvement than the original series, though it remains focused on the same core problem. No external dependencies or prior versions are mentioned.