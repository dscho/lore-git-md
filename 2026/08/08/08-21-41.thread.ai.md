Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message that helps users resolve the ambiguity by listing the conflicting remotes (e.g., "the branch exists on `origin`, `upstream`, and `home`"). The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and aims to prioritize *discovery* over syntactic workarounds.

Current status: Under review. No prior versions. Junio C Hamano’s feedback refines both the usability goal (listing conflicting remotes) and surface-level formatting nits (unnecessary newline in the warning message, unnecessary braces in the code). The patch touches `builtin/worktree.c` (DWIM logic and advice message) and `t/t2400-worktree-add.sh` (updated tests). Implementation remains localized and follows advice-message conventions, but the advice content, formatting, and code style are now contested.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- Advice message: Static string; Junio’s feedback requests (1) replacing the generic workaround with a list of conflicting remotes to aid discovery, (2) removing an unnecessary terminating newline in the warning message, and (3) removing unnecessary braces in the code.
- Behavior change: Advice shown only when ambiguity is detected; the message’s content, formatting, and code style are the sole points of contention.
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage may need expansion to verify the new advice format.

Open questions and loose ends:
- Whether to split the advice message into scenario-specific variants (e.g., `worktree.guessremote` set vs. unset) or focus on the more universal problem of remote discovery.
- How to format the list of conflicting remotes in the advice message (e.g., inline vs. bullet points, sorting order).
- Whether the commit message adequately explains the shift from syntactic workarounds to discovery-focused advice.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; proposed the usability improvement with a single advice message covering syntactic workarounds. Will address Junio’s feedback in the next iteration.
- **Junio C Hamano**: Reviewer; raised substantive concerns about the advice message’s effectiveness (prioritizing *discovery* over syntactic guidance) and surface-level formatting nits (unnecessary newline, unnecessary braces). Feedback is high weight, focusing on user experience, consistency with Git’s output conventions, and code style.
- **Harald Nordgren**: Surface-level endorsement ("This is an interesting idea!") with no technical engagement or critique; Junio later noted the reply lacked context (no quoted material), making it unhelpful for readers. Low weight; does not advance the discussion.

Related work: None mentioned. This remains a standalone usability improvement.