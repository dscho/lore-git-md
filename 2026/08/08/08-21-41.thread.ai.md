Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message to help users resolve ambiguity. The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and focuses on *script-friendly discovery* (providing a command to list conflicting remotes) rather than a static list of remotes or syntactic workarounds. The patch also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression, as the command was already failing — just with less clarity.

Current status: Under review. Two iterations (v1, v2). Junio C Hamano’s feedback has refined the usability goal (listing conflicting remotes for discovery) and the commit message’s clarity regarding the error-handling change. The latest iteration (v2) adopts a command-based approach (`git branch -r --list '*/<name>'`) to dynamically list conflicting remotes, addressing Junio’s earlier suggestion of a static list. D. Ben Knoble’s review raised concerns about the unannounced `die()` behavior change, which Yoichi Nakayama clarified as a usability improvement by moving an existing `die()` call earlier. Junio’s latest feedback targets the commit message’s justification for this change, requesting it trace the problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading "invalid reference" error) to clarify that the early `die()` is intentional. The patch touches `builtin/worktree.c` (DWIM logic and advice message) and `t/t2400-worktree-add.sh` (updated tests). Implementation remains localized, but the advice message’s *content* (command vs. static list), *purpose* (flexibility vs. immediate discoverability), and *error-handling refinement* (earlier, more specific `die()`) remain under discussion.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- Advice message: Uses a command (`git branch -r --list '*/<name>'`) to dynamically list conflicting remotes, replacing Junio’s earlier suggestion of a static list. The command-based approach is intended to avoid overwhelming users and align with script-friendly workflows.
- Behavior change: The patch moves an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression, as the command was already failing. The commit message must now justify this change by tracing the problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading error), as requested by Junio.
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage verifies the new advice format and error-handling behavior. Junio’s surface-level nits (unnecessary newline, unnecessary braces) remain unaddressed but are secondary to the commit message clarification.

Open questions and loose ends:
- Whether the command-based advice (`git branch -r --list`) is more effective than a static list of remotes for user discovery. Junio’s earlier preference for a static list prioritized immediate usability, while the current approach favors flexibility.
- How to reconcile the trade-off between script-friendly flexibility (command) and immediate usability (static list).
- Whether the commit message adequately explains the refined error-handling behavior (earlier, more specific `die()`) by tracing the problematic flow, as Junio now requests. This remains the primary blocker for acceptance.
- Junio’s surface-level nits (unnecessary newline, unnecessary braces) remain unaddressed but are secondary to the commit message clarification.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; proposes a command-based advice message (`git branch -r --list '*/<name>'`) to empower users to explore conflicting remotes dynamically. Argues this avoids overwhelming users with a static list and aligns with script-friendly workflows. Clarifies that the patch moves an existing `die()` call earlier to improve diagnostics, not introduce a new failure mode. Must now address Junio’s request to rewrite the commit message to justify the error-handling change by tracing the problematic flow.
- **Junio C Hamano**: Reviewer; initially suggested a static list of conflicting remotes (e.g., "the branch exists on `origin`, `upstream`, and `home`") to prioritize *discovery*, but has since focused on the commit message’s clarity regarding the error-handling refinement. His latest feedback requests a rewrite to trace the problematic flow (silent failure -> `NULL` remote -> misleading error) and justify the early `die()`. Feedback remains high weight, focusing on user experience and maintainability.
- **D. Ben Knoble**: Reviewer; raised a critical technical concern about the unannounced `die()` behavior change, which Yoichi Nakayama clarified as a usability improvement. The review was substantive and workflow-oriented, not about the advice content or style.
- **Harald Nordgren**: Surface-level replies only. Initially posted a one-line endorsement ("This is an interesting idea!") without context, later clarified it referred to Junio’s suggestion to list conflicting remotes. No technical engagement or critique; low weight.

Related work: None mentioned. This remains a standalone usability improvement.