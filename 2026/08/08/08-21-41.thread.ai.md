Thread identity: Patch series improving user-facing error messages for `git worktree add`, `git checkout`, and `git switch` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Twelfth iteration (v12) of a four-patch series originating from Yoichi NAKAYAMA, now functionally complete with all prior feedback addressed. The series was merged to `next` as of v7, with v8 through v12 incorporating final refinements: v8 addressed the `--guess-remote` alignment and grammatical fixes, v9 improved code organization (moving the `advise_disambiguating_remotes()` helper earlier and refactoring the advice logic into a shared helper), v10 made final adjustments to function naming (renaming `advice_disambiguating_remotes()` to `advise_disambiguating_remotes()`), commit message clarity, and code structure (simplifying the `remote_names` field in `struct tracking_name_data` and reducing nesting in `parse_remote_branch()`), v11 included minor documentation and test updates, and v12 finalizes the series with a test case for the `--guess-remote` error behavior in `git worktree add`. The final patch (4/4) ensures the `--guess-remote` option in `git worktree add` errors out when multiple matches exist, aligning it with the behavior of `git checkout` and `git switch`.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed, actionable advice message to help users resolve ambiguity. The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and provides a static list of conflicting remotes (e.g., "Branch name '%s' appears in multiple remotes:") for immediate usability. The series also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity and includes the number of matching remotes. This is a usability improvement, not a regression, as the command was already failing — just with less clarity. The problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading "invalid reference" error) is explicitly traced in the commit message to justify the early `die()` as intentional. The implementation avoids altering `dwim_branch()`'s return behavior, addressing concerns about workflow disruption. The series now also fixes a latent bug in the `--guess-remote` option of `git worktree add`: when multiple remote branches match the given name, the command previously succeeded silently, which could lead to unexpected behavior. The fix treats this scenario as an error, aligning it with the behavior of `git checkout` and `git switch`. The one-argument `git worktree add` case (e.g., `git worktree add ../topic-branch`) remains slated for a follow-up fix, as agreed in the thread.

Current status: Merged to `next`. The v12 iteration is now functionally complete, covering all three commands (`checkout`, `switch`, and `worktree add`) and addressing all known edge cases, including the `--guess-remote` behavior in `git worktree add`. The second patch in v10 extends `unique_tracking_name()` to optionally collect the names of matching remotes, enabling the advice message to list conflicting remotes (e.g., "Branch name 'bar-topic' appears in multiple remotes: origin, upstream"). The patch simplifies the `remote_names` field in `struct tracking_name_data` from `struct string_list **` to `struct string_list *`, removing unnecessary indirection, and updates the `advise_disambiguating_remotes()` helper to accept the branch name and matched remote names as arguments. The `parse_remote_branch()` function is refactored to use the extended `unique_tracking_name()` and pass the matched remote names to the advice helper. The first patch in v11 updates the documentation for `advice.checkoutAmbiguousRemoteBranchName` to clarify its scope (applying to `checkout`, `switch`, and `worktree add`) and adds a cross-reference to `checkout.defaultRemote`. The fourth patch in v12 adds a test case in `t/t2400-worktree-add.sh` to verify the new error behavior for `--guess-remote` when multiple matches exist. All prior review feedback has been addressed, including Junio’s latest suggestions on commit message clarity, function naming, code structure, and documentation. The series has no remaining open questions or objections. Test coverage in `t/t2400-worktree-add.sh` verifies the new error case for `--guess-remote` and advice suppression in quiet mode, as well as the new advice and error-handling behavior for `git worktree add`. The follow-up item for the one-argument `git worktree add` case (e.g., `git worktree add ../topic-branch`) remains scoped for a separate commit, as previously agreed.

Key technical details:
- Files: `builtin/worktree.c`, `builtin/checkout.c`, `checkout.c`, `checkout.h`, `t/t2400-worktree-add.sh`, `Documentation/config/advice.txt`, and other documentation files.
- New/updated functions: `unique_tracking_name()` (extended to optionally collect remote names via `struct string_list *dwim_remote_names`), `advise_disambiguating_remotes()` (renamed from `advice_disambiguating_remotes()` in v10, new shared helper for advice messages, introduced in patch 1/4 of v8, applied to `checkout`/`switch` in patch 2/4, extended to `worktree add` in patch 3/4, and moved earlier in the third patch in v9; further refactored in patch 1/4 of v10 to reduce nesting, align with Git’s naming conventions, and prepare for reuse in later patches; updated in patch 2/4 of v10 to accept the branch name and matched remote names as arguments), `dwim_branch()` (extended to accept `add_opts` for `--quiet` support and detect multiple matches for `--guess-remote`), `parse_remote_branch()` (refactored in patch 1/4 of v9 to call the new shared helper, and in patch 2/4 of v10 to use the extended `unique_tracking_name()` and simplified `struct tracking_name_data`).
- Updated struct: `struct tracking_name_data` (simplified `remote_names` field from `struct string_list **` to `struct string_list *` in v10 to remove unnecessary indirection).
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true). Documentation updated in v11 to clarify its scope (applying to `checkout`, `switch`, and `worktree add`) and add a cross-reference to `checkout.defaultRemote`.
- Advice message: Proposes a static list of conflicting remotes (e.g., "Branch name '%s' appears in multiple remotes:") and suggests using `--track` or setting `checkout.defaultRemote` for future disambiguation. The message is tailored to the command (`checkout`/`switch` vs. `worktree add`) and includes the branch name and number of matching remotes (e.g., "matched multiple (2) remote tracking branches"). The grammatical error ("Branches ... appears") has been fixed in both patch 2/4 and 3/4.
- Behavior changes:
  - Moves an existing `die()` call earlier in the code path in `git checkout`, `git switch`, and `git worktree add`, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity and includes the number of matching remotes. The diagnostic logic is relocated to avoid altering `dwim_branch()`'s return behavior, addressing workflow concerns. The commit message justifies this change by tracing the problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading error).
  - `--guess-remote` in `git worktree add` now errors out when multiple matches exist, rather than silently succeeding. This aligns the behavior with `git checkout` and `git switch`.
  - `dwim_branch()` now respects the `--quiet` flag when emitting advice, suppressing the advice message in quiet mode.
- Error message: Now includes the number of matching remotes (e.g., "matched multiple (2) remote tracking branches") for clarity. The `--guess-remote` error message is "Branch '%s' matched multiple (%d) remote tracking branches".
- Test coverage: Updated existing tests + new tests for `--guess-remote` with ambiguous branch; coverage verifies the new advice format, error message format, and error-handling behavior across `checkout`, `switch`, and `worktree add`. The `--guess-remote` case is explicitly tested to ensure it now errors out when ambiguity exists. Test coverage for the advice message in `worktree add` is included in patch 3/4. The one-argument `git worktree add` edge case (e.g., `git worktree add ../topic-branch`) is intentionally not covered by disambiguation logic, as the command succeeds in this scenario. The scope of the `oid` variable in `builtin/worktree.c` has been narrowed.
- Edge cases: The ambiguous branch warning does not trigger for fully qualified branch names (e.g., `origin/bar-topic`), as these are resolved earlier in the code path. The detached HEAD scenario is dismissed as unlikely and irrelevant. A minor edge case involving typos in fully qualified branch names (e.g., `origin/bar-topik` matching multiple remotes due to partial matches) is noted but considered too rare to address. The one-argument `git worktree add` case (e.g., `git worktree add ../topic-branch`) is now flagged as a latent bug: the command should error out when ambiguity exists, rather than silently creating a local branch from HEAD. Historical commits 3c87aa946a and 8d7b558bae are relevant to the incomplete refactoring.

Open questions and loose ends:
- The one-argument `git worktree add` case (e.g., `git worktree add ../topic-branch`) remains slated for a follow-up fix. Junio’s analysis provides historical context (commits 3c87aa946a, 8d7b558bae) and confirms the command should error out when ambiguity exists, rather than silently creating a local branch from HEAD. Yoichi Nakayama has agreed to implement the fix in a follow-up commit. This is a well-defined #leftoverbits item with no remaining controversy.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; accepted Junio’s refactoring to a static list of remotes and agreed to all structural adjustments (renaming the helper, splitting the series into four patches, simplifying the `remote_names` field in `struct tracking_name_data`). Resolved the `--guess-remote` edge case by treating it as an error when multiple matches exist, aligning it with the behavior of `checkout` and `switch`. Fixed the grammatical error in the advice message ("Branch name '%s' appears in multiple remotes:") in both patch 2/4 and 3/4. Introduced the shared `advise_disambiguating_remotes()` helper (renamed from `advice_disambiguating_remotes()` in v10) in patch 1/4 of v8 to avoid code duplication and prepare for reuse in later patches. Extended the helper to `git worktree add` in patch 3/4, ensuring consistent advice and error handling across all three commands. In v9, moved the `advise_disambiguating_remotes()` helper earlier in the third patch to improve code organization and further refactored the advice logic into a shared helper in the first patch. In v10, renamed the helper to align with Git’s naming conventions, simplified the `remote_names` field in `struct tracking_name_data`, reduced nesting in `parse_remote_branch()`, and added a detailed commit message. The second patch in v10 extends `unique_tracking_name()` to collect remote names and applies the advice logic to `git checkout` and `git switch`. The third and fourth patches complete the command coverage and ensure `--guess-remote` errors out on ambiguity. In v11, updated the documentation for `advice.checkoutAmbiguousRemoteBranchName` to clarify its scope and add a cross-reference to `checkout.defaultRemote`. In v12, added a test case in `t/t2400-worktree-add.sh` to verify the new error behavior for `--guess-remote` when multiple matches exist. The series is now functionally complete, with the follow-up item for the one-argument `git worktree add` case clearly scoped. Addressed Junio’s latest feedback on commit message clarity, function naming, code structure, and documentation.
- **Junio C Hamano**: Reviewer and maintainer; proposed and implemented the refactoring to extend `unique_tracking_name()` for static remote lists, unifying the advice logic across commands. Applied the advice message to `git checkout` and `git switch` in patch 2/4, moving the `die()` call earlier to provide a more specific diagnostic. Identified the grammatical error, the `--guess-remote` edge case, the latent bug in the one-argument `git worktree add` form, and the unnecessary double indirection in `struct tracking_name_data`. Agreed with Yoichi’s resolution of the `--guess-remote` case (now treated as an error) and endorsed the updated advice phrasing. Queued the v7 series for `next` and acknowledged the v8, v9, v10, v11, and v12 iterations as final refinements. Confirmed the final patch in v8 is well-structured, with only minor organizational and implementation points noted (addressed in v9, v10, v11, and v12). Now collaborates with Yoichi on scoping the follow-up fix for the one-argument case. Provided surface-level review feedback on the first and second patches of v9, v10, v11, and v12, suggesting improvements to commit message clarity, function naming (e.g., renaming `advice_disambiguating_remotes()` to `advise_disambiguating_remotes()`), code structure (e.g., reducing nesting in `parse_remote_branch()` and simplifying `struct tracking_name_data`), and documentation (e.g., updating `advice.checkoutAmbiguousRemoteBranchName` to clarify its scope); these are minor, uncontroversial adjustments.
- **D. Ben Knoble**: Reviewer; raised concerns about the unannounced `die()` behavior change and workflow disruption, which were mitigated by relocating the diagnostic logic. Endorsed signaling ambiguity (via warning or advice) when no deterministic resolution policy exists, except in the `--guess-remote` case. Suggested structural improvements (renaming the helper, splitting the patch) that were adopted. No further objections.
- **Harald Nordgren**: Surface-level replies only. Initially posted a one-line endorsement without context, later clarified it referred to Junio’s suggestion. No technical engagement; low weight.