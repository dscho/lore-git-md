Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series. Four iterations (v1, v2, v3, v4).

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message to help users resolve ambiguity. The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and focuses on *script-friendly discovery* (providing a command to list conflicting remotes) rather than a static list of remotes or syntactic workarounds. The patch also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. This is a usability improvement, not a regression, as the command was already failing — just with less clarity. The problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading "invalid reference" error) is explicitly traced in the commit message to justify the early `die()` as intentional. The implementation avoids altering `dwim_branch()`'s return behavior, addressing concerns about workflow disruption.

Current status: Under review. The series is functionally complete, with all substantive feedback addressed. The latest discussion centers on whether to emit a warning or advice in the `--guess-remote` case, where ambiguity does not block the operation. Yoichi now proposes omitting warnings/advice in this scenario to avoid cluttering output with complex recovery instructions, prioritizing simplicity over discoverability. This aligns with Ben Knoble’s earlier endorsement of signaling ambiguity *only when necessary* to avoid workflow disruption. Junio’s preference for a static list of conflicting remotes (prioritizing immediate usability) remains unresolved but is not a blocker for merging. Minor code nits (unnecessary newline, braces) remain unaddressed but are secondary to the core design.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- Advice message: Uses a command (`git branch -r --list '*/<name>'`) to dynamically list conflicting remotes, replacing Junio’s earlier suggestion of a static list. The command-based approach avoids overwhelming users and aligns with script-friendly workflows.
- Behavior change: The patch moves an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity. The diagnostic logic has been relocated to avoid altering `dwim_branch()`'s return behavior, addressing workflow concerns. The commit message justifies this change by tracing the problematic flow (silent failure to resolve ambiguity -> `NULL` remote -> misleading error).
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage verifies the new advice format and error-handling behavior. The `--guess-remote` case is now explicitly excluded from emitting warnings/advice.

Open questions and loose ends:
- Whether the command-based advice (`git branch -r --list`) is more effective than a static list of remotes for user discovery. Junio’s preference for a static list prioritizes immediate usability, while the current approach favors flexibility. This trade-off remains unresolved but is not a blocker.
- Whether *any* warning should be emitted for multiple matches in non-`--guess-remote` cases, given the trade-off between discoverability and noise. The current implementation omits the warning from `dwim_branch()`, leaving this question open. Ben Knoble’s latest reply endorses signaling ambiguity but does not resolve whether the final version will include a warning.
- Junio’s surface-level nits (unnecessary newline, unnecessary braces) remain unaddressed but are minor and do not affect functionality.
- The edge case of detached HEAD checkout (vs. creating a new local branch) is noted but dismissed as unlikely and not considered a blocker.

Participant positions:
- **Yoichi NAKAYAMA**: Original author; proposes a command-based advice message (`git branch -r --list '*/<name>'`) to empower users to explore conflicting remotes dynamically. Argues this avoids overwhelming users with a static list and aligns with script-friendly workflows. Clarified that the patch moves an existing `die()` call earlier to improve diagnostics, not introduce a new failure mode, and adjusted the implementation to avoid altering `dwim_branch()`'s return behavior. Addressed Junio’s feedback by rewriting the commit message to use realistic placeholder names (`foo-dir` and `bar-topic`) and expand the explanation of the problematic code path. Dismissed the detached HEAD edge case as unlikely and declined to extend `unique_tracking_name()` due to scope constraints. Now proposes omitting warnings/advice in the `--guess-remote` case to avoid cluttering output with complex recovery instructions, prioritizing simplicity over discoverability.
- **Junio C Hamano**: Reviewer; initially suggested a static list of conflicting remotes (e.g., "the branch exists on `origin`, `upstream`") to prioritize *discovery*, but has since accepted the command-based approach. Focused on the commit message’s clarity (placeholder names and explanation of the problematic code path) and the advice message’s usability (static list vs. command-based discovery, detached HEAD edge case). Feedback remains high weight but is now limited to editorial and user-experience refinements, not technical correctness or workflow disruption.
- **D. Ben Knoble**: Reviewer; initially raised a critical technical concern about the unannounced `die()` behavior change and potential workflow disruption. The concern was mitigated by moving the diagnostic logic out of `dwim_branch()`, preserving the function’s return behavior. His latest reply clarifies that his concern was never about the warning’s content or necessity, but about the *mechanism* of signaling ambiguity: a warning is preferable to a `die()` to avoid breaking workflows. He now explicitly endorses signaling ambiguity (via warning or advice) when no deterministic resolution policy exists, but supports omitting it in the `--guess-remote` case where ambiguity does not block the operation.
- **Harald Nordgren**: Surface-level replies only. Initially posted a one-line endorsement ("This is an interesting idea!") without context, later clarified it referred to Junio’s suggestion to list conflicting remotes. No technical engagement or critique; low weight.

Related work: None mentioned. This remains a standalone usability improvement.