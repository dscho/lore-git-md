Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message that explains the ambiguity and provides actionable workarounds: (1) specify the fully qualified remote branch name (e.g., `origin/foo`), or (2) set `checkout.defaultRemote` in Git config to prefer one remote by default. The advice is gated behind the new `advice.checkoutAmbiguousRemoteBranchName` setting (enabled by default).

Current status: Under review. No prior versions. Patch touches `builtin/worktree.c` (DWIM logic for branch name guessing) and `t/t2400-worktree-add.sh` (updated tests for new warning text + new test case for `--guess-remote` with ambiguous branch name). Implementation is localized, follows advice-message conventions, and includes test coverage.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- New advice message: Static string explaining ambiguity + workarounds
- Behavior change: Advice shown only when ambiguity is detected; no functional change to DWIM logic itself
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch

Open questions and loose ends: None identified in the patch. The change is self-contained and appears uncontroversial.

Participant positions: None yet (initial patch submission).

Related work: None mentioned. This is a standalone usability improvement.