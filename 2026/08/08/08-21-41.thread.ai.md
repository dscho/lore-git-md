Thread identity: Patch series improving user-facing error message for `git worktree add` when an ambiguous remote branch name is provided (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). Single-patch series, originating author Yoichi NAKAYAMA, thread type: patch series.

Core problem or goal: Replace the terse "matched multiple remote tracking branches" warning with a detailed advice message that explains the ambiguity and provides actionable workarounds: (1) specify the fully qualified remote branch name (e.g., `origin/foo`), or (2) set `checkout.defaultRemote` in Git config to prefer one remote by default. The advice is gated behind the new `advice.checkoutAmbiguousRemoteBranchName` setting (enabled by default).

Current status: Under review. No prior versions. Junio C Hamano provided substantive feedback on the advice message's context and clarity, suggesting refinements to tailor the advice to distinct scenarios (`worktree.guessremote` set vs. unset) and improve the example command's phrasing. The patch touches `builtin/worktree.c` (DWIM logic for branch name guessing) and `t/t2400-worktree-add.sh` (updated tests for new warning text + new test case for `--guess-remote` with ambiguous branch name). Implementation remains localized and follows advice-message conventions.

Key technical details:
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- New config key: `advice.checkoutAmbiguousRemoteBranchName` (default: true)
- New advice message: Static string explaining ambiguity + workarounds; Junio’s feedback requests scenario-specific tailoring and clearer phrasing for the example command
- Behavior change: Advice shown only when ambiguity is detected; no functional change to DWIM logic itself
- Test coverage: Updated existing tests + new test for `--guess-remote` with ambiguous branch; coverage may need expansion to verify scenario-specific advice
- Style nit: Junio flagged an overly long line with unnecessary braces in the code

Open questions and loose ends:
- Whether to split the advice message into two variants (one for `worktree.guessremote` set, one for unset) to better match user intent in each scenario.
- How to reword the example command (`git worktree add <path> origin/<name>`) to avoid implying `origin/` is a literal rather than a placeholder for the actual remote name.
- Whether the commit message adequately explains the handling of the second scenario (ambiguous branch name without `guessremote` set).

Participant positions:
- **Yoichi NAKAYAMA**: Original author; proposed the usability improvement with a single advice message covering both scenarios.
- **Junio C Hamano**: Reviewer; raised concerns about the advice message’s generality and clarity, suggesting scenario-specific tailoring and clearer phrasing. Feedback is substantive (medium weight) and focuses on user experience rather than functional correctness.

Related work: None mentioned. This remains a standalone usability improvement.