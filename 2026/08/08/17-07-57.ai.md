This is the final patch in Toon Claes’s six-patch v2 series optimizing `git last-modified` by reusing Bloom filters. The patch addresses a critical edge case: wildcard pathspecs (e.g., `*.c`) cause `prepare_revision_walk()` to clear `bloom_filter_settings`, breaking the per-path Bloom filter lookups that the rest of the series relies on. The fix is minimal—restoring the settings pointer after the revision walk—but essential for ensuring the optimization works for all pathspec types.

Junio’s reply is a surface-level review, asking whether the patch should include a test to verify that the restoration of `bloom_filter_settings` works for wildcard pathspecs. The concern is valid: while the series already includes a test for `--show-trees` and wildcard pathspecs, this specific edge case (the settings being cleared and restored) is not explicitly covered. The failure mode is subtle, and without instrumentation, a test might not show behavioral differences even if the fix is missing. Junio’s suggestion is pragmatic and would strengthen the patch’s correctness guarantees.

The patch itself is well-motivated and clearly explained, but the lack of a dedicated test for this edge case leaves a small gap in coverage. The fix is uncontroversial and unlikely to introduce regressions, but Junio’s feedback highlights an opportunity to make the series more robust. No further technical blockers are known, and the series as a whole is ready for integration once this minor test gap is addressed.