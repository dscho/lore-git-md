This is a **substantive review** in the `git replay --linearize` thread, specifically Elijah Newren’s response to Justin Tobler’s feedback on patch 3/3 of v8. The email focuses on two key UX concerns: the **multi-branch restriction** for `--linearize` and the **clarity of documentation and error messages**.

Elijah endorses Toon’s decision to disallow `--linearize` with multiple branches, calling it a necessary guard against concatenating unrelated histories. He then engages with Justin’s questions about the restriction’s justification and presentation. On the documentation front, Elijah suggests simplifying the rationale for the restriction—replacing the technical explanation of emergent reachability issues with a straightforward "multiple branches are not supported" statement. He also agrees with Justin that error messages should use "branches" instead of "revision ranges" for clarity.

The email also surfaces a **subtle implementation detail**: Elijah proposes extending the multi-branch restriction to `--revert` mode, noting that reverting multiple branches would similarly concatenate commits in an unpredictable order (matching `git cherry-pick`'s existing behavior). This is a **new technical consideration** not previously discussed in the thread and could prompt a small follow-up patch to enforce the restriction for `--revert` as well.

No objections are raised to the core logic of `--linearize` (dropping merges, reparenting commits), and the tone is collaborative rather than critical. The points raised are **non-blocking** but could lead to minor documentation and error-message tweaks before the series graduates to `next`. The thread remains in active review, with Junio yet to merge v8.