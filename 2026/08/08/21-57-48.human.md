This is a follow-up review in the thread improving the error message for `git worktree add` when an ambiguous remote branch name is provided. Junio C Hamano expands on his earlier feedback, focusing on how the advice message could be more helpful by revealing information the code already knows—specifically, the names of the remotes that contain the ambiguous branch.

The patch currently suggests a generic workaround (`git worktree add <path> origin/<name>`), but Junio argues this isn’t ideal when the user’s issue isn’t command syntax but simply forgetting which remotes have the branch. Instead, he proposes the advice should list the actual remotes (e.g., `origin`, `upstream`, `home`) that contain the branch, making it easier for the user to pick the right one without guessing. This change would make the advice more actionable and context-aware, aligning better with the user’s likely intent.

The review remains substantive, targeting the advice message’s effectiveness rather than the patch’s core functionality. No new technical concerns are raised, and the discussion is still centered on refining the user experience. The patch touches `builtin/worktree.c` and tests in `t/t2400-worktree-add.sh`, and this feedback suggests a v2 may be needed to incorporate the remote-name disclosure. No other participants have weighed in yet.