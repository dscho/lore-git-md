This is a CI/build system patch thread, now in its final stages. Junio C Hamano closes the loop on the only open question from the v2 review: whether the patch’s concurrency-group change could cause duplicate workflow runs for the same commit when it is both pushed to a user’s repository and later submitted as a pull request. Harald Nordgren had confirmed that GitHub Actions’ concurrency groups are scoped per-repository, making the scenario unavoidable but also rare in practice (most contributors push to forks without CI enabled). Junio now accepts this as a non-issue, removing the last technical obstacle to merging.

The patch itself is a targeted improvement to `.github/workflows/main.yml` that cancels stale workflow runs for pull requests while preserving the existing behavior for regular pushes. It is well-motivated, narrowly scoped, and addresses all prior feedback. No further changes are expected; the topic is now ready for Junio to pick up.