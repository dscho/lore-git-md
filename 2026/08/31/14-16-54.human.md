This is the final procedural check in the thread for the `http.sslVerifyStatus` security feature. Junio C Hamano is confirming with Patrick Steinhardt that the test harness (`t5585-http-ssl-ocsp.sh`) properly exercises the "fail-closed" behavior—specifically, that Git refuses connections when the server does not provide a valid OCSP staple. Patrick clarifies that the existing test *does* cover this: the `fetch succeeds with stapled "good" OCSP response` test implicitly verifies fail-closed behavior because the connection only succeeds when a valid staple is present. Without one, the connection would fail under `http.sslVerifyStatus=true`.

No code changes or further review are needed. The patch is technically complete, all feedback has been addressed, and both reviewers (Junio and Patrick) have explicitly endorsed merging the test harness. The thread is now in Junio’s queue for graduation to `next`, with no remaining obstacles. This is a well-motivated security feature targeting government and FIPS-compliant deployments, where OCSP stapling is mandated but currently ignored by OpenSSL-linked Git binaries. The new `http.sslVerifyStatus` config key (default `false`) enables fail-closed staple validation via libcurl, closing a security gap without breaking existing setups.