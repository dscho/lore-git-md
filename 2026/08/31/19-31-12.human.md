This is a brief maintainer update in the "What's cooking" thread. Junio C Hamano acknowledges Toon Claes's new iterations of two previously stalled topics—`tc/replay-linearize` (v9) and `tc/last-modified-bloom` (v3)—and confirms both are now queued and pushed out to the integration branches.

The two topics are part of Toon's work on newer Git commands: `git replay --linearize` (which mimics `git rebase --no-rebase-merges`) and an optimization for `git last-modified` using Bloom filters. With this update, both series are back in active integration testing and should appear in the next "What's cooking" report as cooking in `next`. No new technical details or objections are raised; this is purely a procedural advancement.