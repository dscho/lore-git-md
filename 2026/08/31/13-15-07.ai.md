This is a **substantive design discussion** in the `git checkout -m` autostash conflict handling series, specifically about the API for `apply_save_autostash_oid()` and `apply_save_autostash_ref()`. Phillip Wood responds to Harald Nordgren’s question about whether to repurpose the existing `int` return value or introduce an `enum` to distinguish between success, conflicts, and errors. Phillip advocates for an `enum`, arguing it would be the clearest way to encode the three states callers care about: "stash applied without conflicts," "stash applied with conflicts," and "something went wrong." This directly addresses the API design concern he raised earlier and provides a concrete path forward for the patch.

The discussion is now resolved on the technical level: the `enum` approach is preferred, and the thread context confirms this aligns with Phillip’s prior feedback. The next step will likely be a v3 patch implementing this change, which should satisfy the remaining objections to the series. No other reviewers have weighed in on this point, and the exchange is brief but decisive. The outcome preserves backward compatibility while improving clarity, a trade-off the thread has already accepted.