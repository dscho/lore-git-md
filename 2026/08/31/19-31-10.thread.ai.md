Here is the updated AI-mode thread summary, preserving all prior state and incorporating the new email:

---

Thread identity: Junio C Hamano’s "What's cooking in git.git" report for August 2026 (integration status update). Originating author: Junio C Hamano. Thread type: integration status report.

Core problem or goal: Provide a snapshot of the current state of Junio’s integration branches (`maint`, `master`, `next`, `seen`) and the disposition of all in-flight topics, including what has graduated to `master`, what is cooking in `next`, what is new or stalled, and what has been discarded.

Current status:
- **Graduated to `master` (irreversible for Git 2.56)**:
  • fr/pack-objects-trace-pack-bytes – trace2 event now records total bytes written by `pack-objects`, enabling compression-setting comparisons.
  • jt/receive-pack-pluggable-writes – `receive-pack` uses the new ODB transaction interface to write incoming packfiles; the transaction layer now exposes `finalize`/`rollback` callbacks.
  • kh/trailers-no-urls – trailer parser no longer misinterprets lines starting with `https://...` as trailers.
  • ps/odb-eagerly-load-alternates – alternates are loaded at ODB creation time instead of lazily on first object lookup; `alternates_db` and `loaded_alternates` fields removed.
  • ps/odb-generic-corrupt-objects – ODB layer now distinguishes “missing” from “corrupt” via `strbuf` error propagation; packed and loose backends use the same error-reporting mechanism.
  • ps/odb-geometric-repack-loose-threshold – loose-object count threshold for geometric repacking now matches `gc.auto`.
  • ps/odb-pluggable-pack-generation – `pack-objects` logic moved behind an ODB interface; `upload-pack`, `send-pack`, and `bundle` now generate packfiles via `odb->generate_pack()`.
  • vm/complete-history – zsh/bash completion for the experimental `git history` command (split pathspecs, `--update-refs`, `--empty`).
  • yn/worktree-add-no-dwim-with-b – `worktree add` no longer DWIMs a remote-tracking branch when `-b`/`-B` is given.

- **New topics (in `next` or `seen`, not yet in `master`)**:
  • jc/checkout-refactor – 8-patch series restructuring `git checkout`/`switch`/`restore` front-end: pathspec and branch setup helpers extracted, `cb_option` passed explicitly, `post_checkout_hook` moved to `checkout.c`.
  • hk/typofix – single patch fixing spelling in comments and test descriptions; reroll expected.
  • ll/doc-pushcert-if-asked – documents that remote-helper `pushcert` option accepts `if-asked`.
  • ps/odb-alternates-at-creation – 8 patches deferring alternates setup to `odb_create_on_disk()`; removes the ad-hoc alternates writing API.
  • ps/odb-pluggable-fsck – 10 patches moving fsck checks into the ODB source layer; packed and loose backends now implement their own fsck callbacks.
  • rs/worktree-add-basename-fixes – 4 patches fixing out-of-bounds reads and trailing-slash handling in `worktree_basename()`.
  • en/no-amend-during-conflicts – 5 patches teaching `am`/`revert`/`rebase` to refuse `--amend` and partial commits during conflict resolution, matching existing `cherry-pick`/`merge` behavior.
  • tc/replay-linearize – 3 patches adding `--linearize` to `git replay` to drop merge commits, mimicking `rebase --no-rebase-merges`.
  • kj/repo-info-more-path-keys – 7 patches extending `git repo info` with absolute/relative paths for grafts, index, hooks, superproject-root, toplevel, and cdup.
  • tc/last-modified-bloom – 6 patches speeding up `git last-modified` by reusing revision walk Bloom-filter logic and maintaining per-path Bloom filters for wildcard pathspecs.
  • ds/trace2-tolerate-failed-timestamp – 7 patches banning `xcalloc`/`xstrfmt`/`ALLOC_GROW` from trace2 to avoid `die()` calls; trace2 now tolerates failed timestamp formatting.

- **Stalled topics (waiting for author response)**:
  • ec/commit-fixup-options – allow `-c`/`-C`/`-m`/`-F` for all `--fixup` variants.
  • ps/libgit-in-subdir – move `libgit.a` sources to `lib/` directory; topic ejected due to evil merges.
  • hs/rebase-continue-edit – add `--[no-]edit` to `rebase --continue`.
  • sn/rebase-update-refs-symrefs – `rebase --update-refs` now resolves symref aliases before queuing updates.
  • tb/pack-with-duplicates – MIDX, reverse index, and pack reuse hardened against duplicate pack entries.
  • cl/regexec-macos-leak – macOS `regexec()` leak workaround for invalid UTF-8.
  • bc/restrict-hex-to-lowercase – hex parser rejects uppercase in breaking-changes mode (prep for 3.0).
  • **cc/lazy-fetch-trusted-bit** – `uploadpack.lazyFetchTrusted` config to allow lazy-fetch on trusted repos. Marked as stalled in prior reports; author (Christian Couder) has now confirmed intent to reroll within days. No technical changes or review feedback in the update email.

- **Cooking (in `next`, likely to graduate soon)**:
  • kn/reftable-optimize-reloading – reftable stack reloads only when not already locked, reducing `newfstatat` calls.
  • en/midx-missing-pack-fallback – object lookup recovers when MIDX points to a removed pack.
  • yn/worktree-repair-relative – `worktree repair` now rewrites relative `.git` file paths when run inside the worktree.
  • yn/worktree-ambiguous-remote-advice – `worktree add` treats multiple remote matches as an error and improves advice.
  • gr/add-e-use-apply-api – `git add -e` now uses the internal apply API instead of spawning `git apply`.

- **Evicted from `next`/`seen` (awaiting reroll)**:
  • ps/fetch-packfile-uris-parallel – 2-patch series enabling parallel downloads of packfile URIs when `fetch.packfileURIThreads` is set. Evicted by Junio at Patrick Steinhardt’s request to avoid conflicts with Justin Tobler’s upcoming ODB transaction work for `git-fetch-pack`. No technical objections; will return after Justin’s series stabilizes.

- **Discarded**:
  • kh/format-patch-range-diff-notes – retracted; range-diff notes can reuse the existing `--notes` machinery.
  • jc/die-for-incompatible-opts – retracted; variadic incompatible-opts helper not needed.

Key technical details:
- **ODB abstraction**: Patrick Steinhardt’s ongoing ODB effort (`ps/odb-*`) has graduated several topics (`eagerly-load-alternates`, `generic-corrupt-objects`, `pluggable-pack-generation`) and introduced new ones (`alternates-at-creation`, `pluggable-fsck`). The ODB layer now exposes `generate_pack()`, `fsck` callbacks, and transaction finalization. Justin Tobler’s upcoming ODB transaction work for `git-fetch-pack` will further refactor the fetch path.
- **Reftable**: Karthik Nayak’s `kn/reftable-optimize-reloading` reduces filesystem calls during reftable stack reloads.
- **Worktree**: Yuchen Ning (`yn/worktree-*`) continues to improve worktree handling, including relative `.git` file path rewriting and better error advice for ambiguous remotes.
- **Trace2**: Derrick Stolee’s `ds/trace2-tolerate-failed-timestamp` makes trace2 more resilient by avoiding `die()` calls during timestamp formatting.
- **Completion**: Ville Skyttä (`vm/complete-history`) added shell completion for the experimental `git history` command.
- **Conflict resolution**: Elijah Newren (`en/no-amend-during-conflicts`) extends conflict-resolution safeguards to `am`/`revert`/`rebase`.
- **MIDX**: Elijah Newren’s `en/midx-missing-pack-fallback` improves object lookup resilience when MIDX references a removed pack.
- **Checkout refactor**: Junio’s `jc/checkout-refactor` extracts helpers and moves the `post_checkout_hook` to `checkout.c`.
- **Repo info**: Karthik Nayak (`kj/repo-info-more-path-keys`) extends `git repo info` with additional path keys.
- **Last-modified**: Toon Claes (`tc/last-modified-bloom`) speeds up `git last-modified` using Bloom filters.
- **Replay**: Taylor Blau (`tc/replay-linearize`) adds `--linearize` to `git replay` to drop merge commits.
- **Parallel packfile URIs**: Patrick Steinhardt’s `ps/fetch-packfile-uris-parallel` (now evicted) prepares for threaded fetching and implements parallel downloads of packfile URIs when `fetch.packfileURIThreads` is set. The series will return after Justin Tobler’s ODB transaction work for `git-fetch-pack` is merged.
- **Lazy-fetch trusted bit**: Christian Couder’s `cc/lazy-fetch-trusted-bit` introduces `uploadpack.lazyFetchTrusted` to allow `upload-pack` to lazily fetch missing objects from promisor remotes when serving trusted repositories. The series uses `GIT_NO_LAZY_FETCH` and `path_allowlist_apply()` to enforce trust boundaries.

Open questions and loose ends:
- **Stalled topics**: All stalled topics (e.g., `ec/commit-fixup-options`, `ps/libgit-in-subdir`, `hs/rebase-continue-edit`) remain explicitly waiting for author response. `cc/lazy-fetch-trusted-bit` is now confirmed to be rerolled by Christian Couder within days; no technical objections or review feedback have been raised in this update.
- **Breaking changes**: `bc/restrict-hex-to-lowercase` (prep for Git 3.0) remains stalled; no timeline for resolution.
- **ODB follow-ups**: No open questions about the graduated ODB topics, but the new `ps/odb-alternates-at-creation` and `ps/odb-pluggable-fsck` topics may attract review feedback. Justin Tobler’s upcoming ODB transaction work for `git-fetch-pack` will require coordination with Patrick Steinhardt’s parallel packfile URI series.
- **Worktree edge cases**: `rs/worktree-add-basename-fixes` addresses out-of-bounds reads and trailing-slash handling; no known open questions.
- **Trace2 resilience**: `ds/trace2-tolerate-failed-timestamp` is cooking; no known open questions.
- **Parallel packfile URIs**: The evicted `ps/fetch-packfile-uris-parallel` topic has no technical objections; it will return after Justin Tobler’s ODB transaction work stabilizes. The `fetch.packfileURIThreads` configuration knob is now documented as part of this topic’s scope.
- **Lazy-fetch trusted bit**: No open technical questions or review feedback on `cc/lazy-fetch-trusted-bit`; the series is purely awaiting reroll.

Participant positions:
- **Junio C Hamano**: Maintainer; authored `jc/checkout-refactor` and provided the integration report. Acknowledged Patrick Steinhardt’s request to evict `ps/fetch-packfile-uris-parallel` from `next`/`seen` to avoid conflicts with Justin Tobler’s upcoming ODB transaction work. Confirmed the eviction will be processed in the next integration cycle.
- **Patrick Steinhardt**: Author of all `ps/odb-*` topics (graduated and new) and `ps/fetch-packfile-uris-parallel` (evicted). Requested eviction of the parallel packfile URI topic to avoid conflicts with Justin Tobler’s upcoming ODB transaction work.
- **Elijah Newren**: Author of `en/no-amend-during-conflicts` and `en/midx-missing-pack-fallback`. No review feedback recorded.
- **Karthik Nayak**: Author of `kn/reftable-optimize-reloading` and `kj/repo-info-more-path-keys`. No review feedback recorded.
- **Yuchen Ning**: Author of `yn/worktree-*` topics. No review feedback recorded.
- **Derrick Stolee**: Author of `ds/trace2-tolerate-failed-timestamp`. No review feedback recorded.
- **Taylor Blau**: Author of `tc/replay-linearize`. No review feedback recorded.
- **Toon Claes**: Author of `tc/last-modified-bloom`. No review feedback recorded.
- **René Scharfe**: Author of `rs/worktree-add-basename-fixes`. No review feedback recorded.
- **Justin Tobler**: Upcoming author of ODB transaction work for `git-fetch-pack`; no patches posted yet. His series will conflict with `ps/fetch-packfile-uris-parallel`, prompting Patrick Steinhardt to request eviction of the latter.
- **Christian Couder**: Author of `cc/lazy-fetch-trusted-bit`. Confirmed intent to reroll the stalled topic within days; no technical changes or review feedback in the update email.
- **Eric Sunshine**: No authored topics in this report; no review feedback recorded.
- **Jeff King (Peff)**: No authored topics or review feedback in this report.
- **Johannes Schindelin (Dscho)**: No authored topics or review feedback in this report.
- **Ævar Arnfjörð Bjarmason**: No authored topics or review feedback in this report.
- **brian m. carlson**: No authored topics or review feedback in this report.

Related work:
- **ODB abstraction**: Patrick Steinhardt’s ODB effort spans multiple topics (`ps/odb-*`), with graduated and new topics in this report. Justin Tobler’s upcoming ODB transaction work for `git-fetch-pack` will further refactor the fetch path and conflict with `ps/fetch-packfile-uris-parallel`.
- **Reftable**: Karthik Nayak’s `kn/reftable-optimize-reloading` is part of the ongoing reftable backend work.
- **Worktree improvements**: Yuchen Ning’s `yn/worktree-*` topics build on prior worktree fixes and advice improvements.
- **Trace2 resilience**: Derrick Stolee’s `ds/trace2-tolerate-failed-timestamp` continues prior efforts to harden trace2 against failures.
- **Git 3.0 prep**: `bc/restrict-hex-to-lowercase` is part of the ongoing preparation for Git 3.0 breaking changes.
- **Parallel packfile URIs**: Patrick Steinhardt’s `ps/fetch-packfile-uris-parallel` (evicted) will return after Justin Tobler’s ODB transaction work for `git-fetch-pack` is merged. The `fetch.packfileURIThreads` configuration knob is now part of this topic’s scope.
- **Lazy-fetch trusted bit**: Christian Couder’s `cc/lazy-fetch-trusted-bit` builds on prior promisor-remote and lazy-fetch work, introducing a trust boundary for `upload-pack`.

---