This is patch 7/10 in Patrick Steinhardt’s v2 series refactoring Git’s fsck checks to make them pluggable per ODB backend. It moves reverse-index verification from `builtin/fsck.c` into the packed ODB backend (`odb/source-packed.c`), replacing the dedicated `ERROR_PACK_REV_INDEX` bit with the generic `ERROR_OBJECT` bit. The patch also changes behavior to respect the `--connectivity-only` and `--no-full` flags: reverse-index checks are now skipped when either flag is passed, aligning with their documented intent. A new test in `t/t5325-reverse-index.sh` verifies these semantics.

The diff removes 37 lines from `builtin/fsck.c` (including the `check_pack_rev_indexes()` function and the `ERROR_PACK_REV_INDEX` bit definition) and adds 39 lines to `odb/source-packed.c` (the new `verify_reverse_indices()` helper and its integration into `odb_source_packed_fsck()`). The new helper iterates over the packfile list provided by `packfile_store_get_packs()`, loads and verifies each reverse index, and reports errors via the generic `ERROR_OBJECT` bit. The progress meter is threaded through the `odb_fsck_options` structure, preserving the original behavior when `--progress` is passed.

This patch continues the pattern established in earlier patches (6/10 and 8/10): moving backend-specific verification logic into the appropriate ODB source implementation, replacing dedicated error bits with `ERROR_OBJECT`, and ensuring the new implementation respects the `--connectivity-only` and `--no-full` flags. The test added to `t/t5325-reverse-index.sh` explicitly verifies that reverse-index checks are skipped under `--connectivity-only` and performed under `--no-full`, casting the new semantics into stone.

No open technical questions or unresolved feedback remain from the v1 iteration. Karthik Nayak’s review of the series has been surface-level, focusing on editorial nits rather than substantive correctness or edge cases. This patch is a clean refactoring with no user-visible behavior changes beyond the intentional flag-based skipping of checks. It depends on the scaffolding introduced in patches 1–5 and is part of the ongoing ODB abstraction effort led by Patrick Steinhardt.