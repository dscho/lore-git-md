This is Junio C Hamano reporting that the v3 series for `git last-modified` Bloom filter optimizations failed CI testing. The series was briefly merged to the `seen` integration branch, but the failure—reproducible locally with the same CI environment variables—led Junio to eject it for now.

The failure occurs in the new test `t8020-last-modified.sh`, which was added in v3 to verify that the Bloom filter fast-path is correctly triggered for wildcard pathspecs. The test runs under a specific set of CI-like conditions (split index, commit-graph with changed-path Bloom filters, multi-pack index, etc.), suggesting the issue may be environment-dependent rather than a fundamental flaw in the logic. Junio provides the exact environment variables and command (`make T='t8020*' test`) needed to reproduce the failure locally, making it straightforward for Toon Claes to investigate.

The series itself is otherwise complete and addresses all prior technical feedback, including correctness fixes for `--show-trees`, wildcard pathspec support, and trace2 instrumentation. The ejection is procedural—no new design concerns were raised—so the fix is likely to be a small adjustment to the test or the code’s handling of the CI environment. Until the failure is resolved, the series remains on hold.