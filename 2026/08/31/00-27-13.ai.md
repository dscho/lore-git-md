This is the final reply in the `dk/use-nsec-runtime` series, which converts the build-time `USE_NSEC` macro into a runtime `core.useNanosec` configuration option. The patch itself is technically complete, well-reviewed, and ready for `master`; the discussion here focuses on a forward-looking architectural constraint rather than blocking issues.

The author, D. Ben Knoble, acknowledges Junio’s concern about the new dependency on `repo_config_values()` in `is_racy_stat()`: because `repo_config_values()` currently only works on `the_repository`, any future effort to support in-process submodule operations (e.g., `git diff --recurse-submodules`) will now require `repo_config_values()` to be generalized. This is not a regression—`repo_config_values` was already limited to `the_repository`—but the new call site makes the limitation more visible. Knoble confirms that no such `--recurse-submodules` option currently exists for `git diff`, and that existing tests (including `t4060`) pass when `core.useNanosec=true` is set. He asks for guidance on how to systematically test the new code path across the entire test suite, noting that platform-dependent behavior complicates test design.

The exchange does not introduce new technical objections or design questions; it simply clarifies the scope of the existing constraint. The series remains uncontroversial, with all prior review feedback addressed in v5. The only remaining loose end is test coverage, which is not blocking graduation. The conflict with `ty/repo-config-cleanups` in `seen` will need resolution if both topics advance, but that is a routine integration matter. Key files touched by the core patch include `read-cache.c`, `statinfo.c`, and `Documentation/config/core.adoc`; the new config key `core.useNanosec` defaults to false and is cached in `repo_config_values` for performance.