This is a contributor housekeeping patch that has hit a procedural snag. Junio C Hamano points out that the change to `git-gui/git-gui.sh` violates the project's submission rules for `git-gui` (and `gitk`), which are treated as separate subprojects. The patch must split the `git-gui` correction into a separate email thread, leaving only the `versioncmp.c` and `t/t0022-crlf-rename.sh` fixes in this one.

The issue is purely administrative: the typo fixes themselves are correct and uncontroversial, but the patch as submitted mixes two different contribution workflows. Junio’s reply is a routine reminder of the rule rather than a rejection, and the fix is straightforward. Once the `git-gui` part is resubmitted separately, both threads should be ready for integration.