This is a brief follow-up from Toon Claes in the August 2026 "What's cooking" thread. Toon responds to Junio's status notes on two of his topics that were marked as stalled or waiting for response.

The first topic, `tc/replay-linearize`, adds a `--linearize` option to `git replay` to drop merge commits and produce a linear history, similar to `git rebase --no-rebase-merges`. Toon acknowledges the delay (he was on leave) and has now sent a v9 iteration of the series, which should unblock the stalled status.

The second topic, `tc/last-modified-bloom`, optimizes the `git last-modified` command by using Bloom filters to speed up path-based queries. This series reuses revision-walk filtering logic from `git log` and extends Bloom filter support to wildcard pathspecs. Toon has sent a v3 reroll of this series as well.

Both topics are now active again after the author's response, so they should move out of the "stalled" and "waiting for response" categories in the next "What's cooking" report. The patches themselves look like feature work with clear performance motivations, and the rerolls suggest the author is addressing prior feedback. No controversy is evident from this email.