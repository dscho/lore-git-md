This is the final technical clarification in the `http.sslVerifyStatus` security feature thread. Patrick Steinhardt confirms that the "fail-closed" behavior—where Git rejects HTTPS connections when the server provides no OCSP staple—is already covered by Grayson Gordon’s existing tests in `t5551-http-fetch-smart.sh`. His earlier test harness (`t5585-http-ssl-ocsp.sh`) focused on the positive case (valid staple present), but the negative case (no staple) was implicitly verified by the original patch’s test suite. This resolves Junio C Hamano’s last lingering question about test coverage, leaving no open technical or procedural issues before the patch graduates to `next`.

The patch adds a boolean `http.sslVerifyStatus` option (default `false`) that enables libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, causing Git to fail connections if the server does not provide a valid OCSP staple. This closes a security gap for government and FIPS-compliant users (e.g., US DoD PKI) where OpenSSL-linked Git binaries currently ignore staples, accepting revoked certificates. The feature is fail-closed for security but defaults to `false` to avoid breaking remotes that do not support stapling. All review feedback—including Junio’s documentation clarity requests and Patrick’s substantive concerns about error messages and test coverage—has been addressed in the v6 iteration. The files touched are `http.c`, `Documentation/config/http.adoc`, and the test suite (`t5551` and `t5585`).

The thread now awaits Junio’s merge action, which is expected to proceed without further changes. No additional feedback or loose ends remain.