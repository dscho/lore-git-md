This is the second patch in Patrick Steinhardt’s 8-patch series refactoring ODB alternates handling during repository creation, part of the broader ODB abstraction effort. The patch is a pure refactoring with no behavior change: it moves the `setup_reference()` function in `builtin/clone.c` further down in the file, closer to `copy_alternates()`, to prepare for a subsequent commit where `setup_reference()` will call `copy_alternates()`. This avoids the need for a forward declaration.

The change is mechanical and well-motivated, aligning with the series’ goal of consolidating alternates logic into a single code path. The patch touches only `builtin/clone.c`, swapping the order of two functions (`setup_reference()` and `copy_alternates()`) without altering their implementation. This is a safe, preparatory step for patch 3, which will introduce the actual logic change. The commit message clearly states the purpose, and the diff is minimal and focused. No review feedback has been posted yet, but the patch is unlikely to be controversial.