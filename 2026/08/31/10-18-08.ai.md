This is a **follow-up in the `git checkout -m` autostash conflict handling series**, specifically addressing Phillip Wood’s API design feedback on patch 1/2. Harald Nordgren asks whether the existing `int` return value is sufficient to distinguish autostash conflicts from other errors, or if an `enum` should be introduced instead.

The question is narrow but consequential: it directly engages with Phillip’s objection to the `conflicted` output parameter by exploring whether the return value can be repurposed to encode three states (success, error, conflict) without breaking existing callers. Harald’s phrasing suggests he is open to revising the patch but wants clarity on the preferred approach before proceeding. No implementation is proposed yet, so this remains a design discussion rather than a code change.

The thread context indicates Junio has already accepted patch 2/2 (which depends on this API), so resolving this question will likely determine whether the series can proceed as-is or requires a v2. The exchange is substantive but brief, focusing on a single technical trade-off: backward compatibility of the return value versus the clarity of an explicit `enum`. No other reviewers have weighed in on this point yet.