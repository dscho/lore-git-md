This is a CI/build system patch, now effectively resolved. Junio C Hamano, the maintainer, closes the loop on the last open question from the v2 review: whether the same commit pushed to a repository and later submitted as a pull request could trigger duplicate GitHub Actions workflow runs. Harald Nordgren confirmed that GitHub Actions’ concurrency groups are scoped to individual repositories, making the scenario unavoidable but also rare in practice for the Git project’s workflow. Junio accepts this as a non-issue, removing the final obstacle to merging.

The patch itself is a targeted improvement to `.github/workflows/main.yml` that cancels stale workflow runs for pull requests while preserving the existing behavior for regular pushes. It is well-motivated, narrowly scoped, and addresses all prior feedback. No further changes are expected; the topic is now ready for Junio to pick up and queue.