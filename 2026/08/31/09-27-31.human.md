This is the final exchange in the `dk/use-nsec-runtime` series, which converts Git’s build-time `USE_NSEC` macro into the runtime `core.useNanosec` configuration option. The patch itself is complete, well-reviewed, and ready for `master`; this reply focuses on a forward-looking architectural constraint rather than any blocking issue.

Patrick Steinhardt confirms that while he has an upcoming patch series that increases reliance on sub-repositories (to eliminate submodule ODB registration), the current `core.useNanosec` change does not break it. He reiterates the long-standing limitation of `repo_config_values()`—it only works on `the_repository`—but notes that the new call site in `is_racy_stat()` does not introduce a regression. Steinhardt expresses mild discomfort with the current direction of `repo_config_values()` but acknowledges that if edge cases emerge later, the value could be moved into `struct repo_settings` (which already supports multiple repositories) without requiring a full rewrite of the series.

The only open question remains how to systematically test the new code path across the entire test suite, given its platform-dependent nature. Patrick does not offer a concrete solution, but the lack of test failures in existing scenarios (including `t4060`) suggests the change is safe to merge. The series is uncontroversial, performance-neutral, and resolves a long-standing usability pain point by allowing users to enable nanosecond precision without recompiling Git.