This is a **design discussion in the `git checkout -m` autostash conflict handling series**, focusing on the API for `apply_save_autostash_oid()` and `apply_save_autostash_ref()`. Phillip Wood responds to Harald Nordgren’s question about whether to use an `int` return value or an `enum` to distinguish between success, conflicts, and errors when applying an autostash.

Phillip recommends an `enum` for clarity, arguing that callers only need three distinct states: "stash applied without conflicts," "stash applied with conflicts," and "something went wrong." This aligns with his earlier feedback on the v1 patch, where he objected to the `conflicted` output parameter as unnecessary complexity. The discussion is now settled on the preferred approach, removing the last technical obstacle before the patch can proceed.

The thread context confirms this is the final design decision needed for the series. Junio has already conditionally accepted the advice-formatting portion, and Phillip’s earlier concerns about API design and performance (the fast-path optimization) have been addressed in v2. With this exchange, the path is clear for Harald to revise the patch using an `enum` and submit a final version.

No other reviewers have weighed in, and the scope remains narrow: this is purely about API clarity, not behavior or performance. The change will touch `sequencer.c` and its callers, but the impact is internal and won’t affect user-facing functionality beyond the already-agreed visual separation of conflict advice. The series is now effectively ready for integration once the `enum` is implemented.