This is the first patch in the v9 series introducing `--linearize` to `git replay`. It is a refactoring patch that extracts and renames helper functions to improve code clarity in the replay machinery.

The patch adds `put_mapped_commit()` to encapsulate the logic for storing rewritten commits in the `replayed_commits` map, and renames `mapped_commit()` to `get_mapped_commit()` for symmetry. These changes are purely internal and do not alter behavior. The refactoring reduces code duplication in `replay_revisions()` and prepares the ground for the `--linearize` feature introduced in patch 3/3.

The commit message is clear and concise, explaining the motivation (reducing function length) and the mechanical changes. The patch itself is small and focused, touching only `replay.c`. This refactoring was previously reviewed in earlier iterations (v2–v5) and has stabilized; the current version addresses prior feedback by reverting an enum-based approach in favor of this simpler design.

This patch is well-crafted and uncontroversial. It is a necessary preparatory step for the `--linearize` feature and should not require further discussion. The series as a whole is technically complete and ready for integration, with v9 addressing all prior feedback.