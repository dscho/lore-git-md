This is the final reply in the `dk/use-nsec-runtime` series, which converts the build-time `USE_NSEC` macro into a runtime configuration option (`core.useNanosec`). The series is well-crafted, uncontroversial, and ready to advance to `master`. This email from the author, D. Ben Knoble, responds to Junio C Hamano’s forward-looking architectural concern about the new dependency on `repo_config_values()` in `is_racy_stat()`.

Knoble clarifies that there is currently no `--recurse-submodules` option for `git diff`, and confirms that existing tests (e.g., `t4060`) pass with `core.useNanosec=true` enabled. He acknowledges the limitation Junio raised—namely, that `repo_config_values()` currently only works on `the_repository`, which could complicate future efforts to support in-process submodule operations—but notes this is not a regression and does not block the patch. He also reiterates his earlier request for guidance on testing the nanosecond-precision change, as the platform-dependent nature of the feature makes it difficult to integrate into the test suite. The email closes by thanking Junio for the review, signaling the patch is otherwise complete and ready for integration.