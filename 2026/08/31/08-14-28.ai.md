This is the final exchange in Patrick Steinhardt’s 8-patch series refactoring ODB alternates handling during repository creation. The thread context confirms this is a **substantive review reply** addressing a correctness concern: Toon Claes asks why the new implementation (introduced in patch 6) does not use a lockfile when writing the `info/alternates` file, given that concurrent repository creation could race. Patrick acknowledges the oversight—he "simply didn’t think about using a lockfile"—and agrees to adopt Git’s standard lockfile API in the next iteration, resolving Junio C Hamano’s earlier security concern about TOCTOU races.

The discussion centers on **patch 8/8**, which removes the now-unused `odb_source_write_alternates()` function and its lockfile mechanism. While the series’ core design (writing alternates exclusively during ODB creation via `odb_create_on_disk_options`) is sound, the absence of locking in the new flow risks partial writes or corruption if multiple processes (e.g., concurrent `git clone` operations) target the same alternates file. This is not a theoretical edge case: the `git clone --reference` workflow, which the series explicitly supports, can trigger such scenarios. The resolution—adding lockfile protection—will likely require a small but critical update to patch 6, where the new alternates-writing logic is implemented.

The exchange is brief but consequential. Patrick’s response is matter-of-fact and uncontentious, suggesting the fix is straightforward and the oversight was unintentional rather than a deliberate design choice. No other reviewers have weighed in on the locking issue, but the series’ ODB-focused audience (e.g., Justin Tobler, Karthik Nayak) may scrutinize the revised implementation for correctness. The thread’s earlier patches (1–7) were largely mechanical refactorings, but this final piece exposes a latent safety requirement that could have gone unnoticed until production use. The fix will likely land in `v3` of the series, with no expected delays beyond the lockfile integration.