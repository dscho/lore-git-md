This is a security feature patch targeting the HTTP transport layer. The thread is now in its final holding pattern before graduation to `next`, awaiting Patrick Steinhardt’s clarification on test coverage.

Patrick’s reply confirms that the OCSP test harness (`t5585-http-ssl-ocsp.sh`) should be merged despite its complexity, given the feature’s security focus. He explicitly endorses the harness’s coverage of rejecting servers with invalid stapled responses and suggests allocating a dedicated test number (already done in the patch). The only ambiguity Junio raised—whether the harness tests the case where the server does not provide a staple—is resolved by Patrick: the existing `fetch succeeds with stapled "good" OCSP response` test implicitly verifies this by ensuring the connection succeeds only when a valid staple is present (i.e., absence of a staple would cause failure under `http.sslVerifyStatus=true`).

No further technical changes are needed. The patch touches `http.c`, `Documentation/config/http.adoc`, and adds tests in `t5551-http-fetch-smart.sh` and `t5585-http-ssl-ocsp.sh`. The new config key `http.sslVerifyStatus` (default `false`) enables fail-closed OCSP staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, addressing a security gap for government customers (e.g., US DoD PKI) where OpenSSL-linked Git binaries ignore staples. The thread now awaits Junio’s final merge decision, which is expected to proceed once he processes Patrick’s confirmation.