This is the **merge signal** for René Scharfe’s four-patch series fixing `worktree_basename()` in `git worktree add`. Junio C Hamano has applied the previously agreed commit-message tweak to Patch 4 locally, clearing the last obstacle. The series—targeting `builtin/worktree.c` and `t/t2400-worktree-add.sh`—now advances to the `next` integration branch, where it will undergo final integration testing. No further review or resend is expected unless new issues surface during cooking.

Key technical details carried forward: the series fixes an out-of-bounds read (Patch 1), rejects malformed separator-only paths (Patch 2), trims trailing slashes from derived branch names (Patch 3), and renames `worktree_basename()` to `worktree_basename_dup()` while returning a freshly allocated string copy (Patch 4). All four patches have Junio’s explicit approval; the only change was a documentation clarification to avoid ambiguity about whether callers already free the returned string. The series supersedes an earlier stalled effort and addresses a real-world Git for Windows issue (#6346).