This is a follow-up discussion in the ongoing trace2 hardening series, specifically addressing Jeff King’s architectural critique of the `banned-die.h` mechanism introduced in the first patch. Derrick Stolee acknowledges the limitations of compile-time bans—namely, that they cannot catch transitive `die()` calls via helpers like `strbuf` or `json-writer.c`—but defends the approach as a pragmatic first step. He argues that the header provides incremental protection and a framework for gradually eliminating `die()`-prone dependencies in trace2, even if it doesn’t solve the problem entirely.

The exchange clarifies that the series is not intended as a universal solution but as a targeted hardening effort for trace2, where crashing during `atexit()` handlers is particularly disruptive. Derrick rejects the idea of a major codebase reorganization to isolate trace2 from `die()`-prone headers, calling it "very disruptive," and instead frames `banned-die.h` as a lightweight enforcement mechanism that balances protection with maintainability. His tone is collaborative, but he makes no concessions to Jeff’s suggestion that runtime solutions might be preferable; the series remains committed to the compile-time ban approach.

For readers tracking the thread, this reply underscores that the series is a deliberate trade-off: it prioritizes stability over completeness, and its limitations are acknowledged upfront. The discussion also hints at broader implications—Derrick’s mention of "other 'higher' methods" suggests `banned-die.h` could evolve into a template for enforcing other architectural constraints (e.g., banning `the_repository` in multi-repo subsystems), though this remains speculative. No technical changes to the patch are proposed here, but the framing may influence how reviewers assess the series’ long-term value.