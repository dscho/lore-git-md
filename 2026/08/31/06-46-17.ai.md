This is patch 3/10 of Patrick Steinhardt’s ODB fsck pluggability series, a refactoring that prepares `git fsck` for backend-specific consistency checks. The patch moves global fsck option variables (`check_full`, `keep_cache_objects`, `name_objects`, `check_references`) and the option-parsing array into the `cmd_fsck()` function, converting them to local variables. This reduces global state and improves code clarity ahead of later patches that will rework option handling for pluggable ODB backends.

The diff is mechanical: 23 lines of global declarations are removed, and 22 lines are added inside `cmd_fsck()`, with no behavior change. The option-parsing array (`fsck_opts`) is relocated into the function body, and the variables it references are now function-local. Karthik Nayak’s earlier review acknowledged the patch’s design rationale, noting that the variables were only used locally and did not need to be global. Feedback was purely observational and did not engage with technical implementation or correctness.

This patch is a prerequisite for the scaffolding introduced in patch 5/10, which will add the `odb_fsck_options` structure and backend-specific fsck callbacks. By de-globalizing the option state now, the series avoids mixing global and per-backend option handling later. The change is small and self-contained, touching only `builtin/fsck.c`. No new symbols are introduced; the patch merely reorganizes existing code. The commit message succinctly explains the motivation: reducing global state to make subsequent option-handling changes clearer.