This is a brief reply in the ongoing ODB alternates refactoring series. Patrick Steinhardt acknowledges Toon Claes’s surface-level observation about header organization in patch 6/8: the `#include "strvec.h"` can be removed because `struct strvec` is already forward-declared later in `odb/source.h`. He agrees to move the new `struct odb_create_on_disk_options` definition below the existing forward declaration, a purely mechanical change that does not affect the patch’s functionality or design. The core work of the patch—supporting alternates at ODB creation time via `odb_create_on_disk_options`—remains uncontroversial and on track. The series’ primary open question is still the locking strategy for writing the `info/alternates` file, raised by Junio C Hamano on the same patch.