This is a thread update from Toon Claes responding to Junio C Hamano’s "What’s cooking" report. Two stalled topics—`tc/replay-linearize` and `tc/last-modified-bloom`—are now un-stalled: Toon has sent new iterations (v9 and v3, respectively) after returning from leave.

The `tc/replay-linearize` series adds a `--linearize` option to `git replay` that drops merge commits, mimicking `git rebase --no-rebase-merges`. The previous version (v8) had been stalled since late July; the new v9 should address the open review comments referenced in the report (cf. the two linked threads).

The `tc/last-modified-bloom` series optimizes `git last-modified` by reusing revision-walk Bloom-filter logic from `git log`. The new v3 follows Junio’s feedback on v2 and should resolve the precondition-checking and wildcard-pathspec issues noted in the report.