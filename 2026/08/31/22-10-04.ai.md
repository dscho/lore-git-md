This is a contributor housekeeping patch that has been merged. Junio C Hamano applied the v3 iteration of Hardik Kumar's typo fixes, which corrects "fractionnal" to "fractional" in a comment in `versioncmp.c` and "similiarity" to "similarity" in the test description in `t/t0022-crlf-rename.sh`. The patch is part of a three-iteration series, with v3 addressing a commit-message typo identified in earlier feedback.

The changes are purely cosmetic, touching only non-executable text in two files: `versioncmp.c` (a utility for version-string comparison) and `t/t0022-crlf-rename.sh` (a test script for CRLF handling during rename detection). No functional or behavioral changes were introduced, and the patch carries no technical risk or controversy.

The thread confirms the patch is now merged, following Junio's explicit "Applied" acknowledgment. This marks the conclusion of the series, which was uncontroversial and well-scoped from the outset. The patch demonstrates the project's responsiveness to minor but corrective contributions, even when they involve only spelling fixes in comments or test descriptions.