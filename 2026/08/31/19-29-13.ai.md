This is a contributor housekeeping patch. It fixes two typos: "fractionnal" → "fractional" in a comment in `versioncmp.c`, and "similiarity" → "similarity" in the test description in `t/t0022-crlf-rename.sh`. No functional changes are introduced; the patch only corrects spelling in non-executable text.

The patch is part of a three-iteration series (v1–v3). The v3 update addresses a file name typo in the commit message itself, demonstrating responsiveness to feedback. The changes are minimal and mechanical, touching only two lines across two files.

The files involved are `versioncmp.c` (a utility for comparing version strings) and `t/t0022-crlf-rename.sh` (a test script for CRLF handling during rename detection). Neither change affects code logic or test behavior.

This is a straightforward cleanup patch with no technical risk or controversy. It is well-scoped and clearly motivated.