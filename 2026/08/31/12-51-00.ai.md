This is a **brief follow-up in the trace2 hardening series**, specifically addressing the off-by-one error in the fifth patch (`trace2: remove use of xstrfmt()`). Derrick Stolee acknowledges Junio C Hamano’s and Elijah Newren’s observation that the manual string concatenation for `:<REDACTED>` omits the trailing `>`, producing `:<REDACTED` instead of the intended `:<REDACTED>`. The error occurs because the patch copies `redact_len - 1` bytes of the placeholder string, excluding the null terminator and the final `>`.

Derrick’s reply is minimal but confirms the bug and its cause: the lack of test coverage for this specific redaction logic in trace2 output. While the pkt-line subsystem has tests for redacted values, the trace2 subsystem does not, leaving this edge case unvalidated. The fix itself is straightforward—copy `redact_len` bytes instead of `redact_len - 1`—but the admission highlights a gap in the series’ test strategy, which relies on transient allocation failures that are difficult to reproduce.

This exchange is **substantive** for the patch’s correctness but does not engage with broader architectural concerns (e.g., whether manual string manipulation is the right long-term approach for trace2). The focus remains on the immediate bug, and the tone is matter-of-fact, treating the error as a clear oversight rather than a design flaw. For future discussion, this confirms the off-by-one issue is a confirmed bug that will likely be fixed in v3, with no new objections or technical details introduced. The thread’s broader context—hardening trace2 against `die()`-triggering helpers, the `banned-die.h` mechanism, and Jeff King’s critique of indirect dependencies—remains unchanged.