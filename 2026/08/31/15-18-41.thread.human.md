This thread is about a small but targeted refactoring in Git’s revision-walking code, part of Toon Claes’s six-patch series “speed up last modified” (now at v3). The patch moves a precondition check—whether any Bloom filter key vectors are set—from the two callers into the helper function `check_maybe_different_in_bloom_filter()` itself. The change is purely mechanical and doesn’t alter behavior; it just tidies up the code by removing a duplicated guard. The only file touched is `revision.c`, and the diff is minimal (seven lines). So far there’s been no review feedback, so the patch remains uncontested and ready for consideration.