This is the final exchange in the `dk/use-nsec-runtime` series, which converts the build-time `USE_NSEC` macro into the runtime `core.useNanosec` configuration option. The series is now effectively complete and ready for `next`, pending one trivial cosmetic change requested by Patrick Steinhardt.

The discussion here focuses on the forward-looking architectural constraint introduced by the new dependency on `repo_config_values()` in `is_racy_stat()`. Both D. Ben Knoble and Patrick Steinhardt acknowledge that `repo_config_values()` is currently hard-coded to `the_repository`, which could complicate future efforts to support in-process submodule operations (e.g., `git diff --recurse-submodules`). However, they agree this is not a regression—`repo_config_values` was already limited to `the_repository`—and the constraint does not block graduation to `master`. Steinhardt notes that if future edge cases reveal problems, the series could be refactored to move the `use_nanosec` value into `struct repo_settings` instead, which already supports multiple repositories.

Knoble confirms that no `--recurse-submodules` option currently exists for `git diff`, and existing tests (including `t4060`) pass when `core.useNanosec=true` is set. He asks for guidance on systematically testing the new code path across the entire test suite, noting that platform-dependent behavior complicates test design. Steinhardt does not provide a specific solution but agrees the current state of `repo_config_values()` is messy and would benefit from refactoring.

The only remaining action before Junio can merge the topic is the trivial cosmetic change requested by Steinhardt: replacing "defined" with "enabled" in a comment in `builtin/update-index.c` to better reflect the runtime nature of `core.useNanosec`. The series is otherwise uncontroversial, with all prior review feedback addressed in v5. Key files touched by the core patch include `read-cache.c`, `statinfo.c`, and `Documentation/config/core.adoc`; the new config key `core.useNanosec` defaults to false and is cached in `repo_config_values` for performance. A routine textual conflict with `ty/repo-config-cleanups` in `seen` will need resolution if both topics advance.