This is the second patch in D. Ben Knoble’s three-patch series converting the `USE_NSEC` macro to runtime configuration (`core.useNanosec`). The series is now in `next` and technically complete, with all prior feedback addressed.

This patch is a no-op refactor of `repo_config_values_init()` in `environment.c`. It reorders the assignments to group them by config section ("core", "sparse", "branch") to match the order in the `struct repo_config_values` declaration. The change makes it easier to validate future additions to the struct and improves maintainability. There are no behavioral changes, and the patch is uncontroversial.

The refactor is part of a larger effort to make nanosecond precision configurable at runtime, but this specific patch is purely about code organization. It touches only one file (`environment.c`) and is ready to advance independently. The author describes it as "well-crafted, uncontroversial, and ready to advance to `master`." No further review comments are expected.