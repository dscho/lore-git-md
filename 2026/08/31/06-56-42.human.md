This is Patrick Steinhardt’s reply in the `http.sslVerifyStatus` security feature thread, clarifying the test coverage and endorsing the inclusion of his OCSP test harness. The patch itself is ready for `next`—all prior review feedback has been addressed, including Junio’s final documentation tweak in v6.

Patrick acknowledges that OCSP setup is complex but argues that, given the feature’s security focus, thorough testing is justified. He confirms that his test harness (`t5585-http-ssl-ocsp.sh`) already covers the case where the server does not provide a staple: the test labeled “fetch succeeds with stapled 'good' OCSP response” implicitly verifies that connections succeed when staples are present and valid, which implies they would fail if staples were missing or invalid. His endorsement removes the last procedural hurdle; Junio is now likely to merge both the feature and the test harness.

The thread is effectively resolved. The patch adds a new `http.sslVerifyStatus` config option (default `false`) that enables OCSP staple validation via libcurl, closing a security gap for government and FIPS-compliant deployments where OpenSSL-linked Git binaries currently ignore staples. The feature is fail-closed (connections fail if staples are missing or invalid) and includes per-URL overrides, four new tests in `t5551`, and Patrick’s more comprehensive test harness. No further changes are expected.