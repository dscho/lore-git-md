This is a procedural follow-up to a contributor housekeeping patch. The author, Hardik Kumar, acknowledges Junio C Hamano's earlier feedback about the submission rule violation: the original patch mixed changes to Git (`versioncmp.c` and `t/t0022-crlf-rename.sh`) with a change to Git GUI (`git-gui/git-gui.sh`), which must be submitted separately to the Git GUI project. Hardik confirms he has already sent a corrected version of the patch—now split—with a "versioncmp" prefix, addressing only the core Git files. The remaining `git-gui/` typo fix will need to be submitted independently to the Git GUI mailing list.

The thread is now unblocked for the uncontroversial portion of the original patch (the two non-GUI fixes). No technical objections remain, and the procedural issue is resolved by the split. The new patch should be ready for integration once Junio or another maintainer picks it up. The Git GUI change is still pending separate submission.