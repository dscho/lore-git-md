**Summarizer brief mode**

This is a follow-up in the trace2 hardening thread, where Derrick Stolee responds to Jeff King’s architectural critique of the `banned-die.h` mechanism. The exchange clarifies the limitations of the current approach and explores potential future directions for hardening trace2 against `die()`-triggering failures.

Derrick acknowledges Jeff’s point that the series only addresses *direct* calls to banned functions (e.g., `xsnprintf()`, `xstrdup()`) but misses *indirect* calls via helpers like `strbuf` or `json-writer.c`. He agrees that a "safe strbuf" library or a ground-up rewrite of trace2 could be necessary for full hardening but frames these as long-term efforts rather than immediate requirements. To illustrate the scope of the problem, he shares a CodeQL query that detects transitive `die()` calls in trace2 functions, demonstrating how widespread the issue is. The query highlights that even if the current series lands, trace2 would still depend on unsafe helpers, leaving the door open for crashes under memory pressure or system call failures.

Key technical details for follow-up:
- **Subsystems at risk**: `strbuf` (memory allocation + `snprintf()` failures), `json-writer.c` (indirect `die()` calls), and any other helper that transitively invokes banned functions.
- **Mechanism limitation**: `banned-die.h` only blocks direct calls, not transitive ones, making it a partial solution. Derrick’s CodeQL query confirms that trace2 functions can still reach `die()` indirectly.
- **Proposed future work**:
  - A "safe strbuf" library that avoids `die()`-triggering allocations or system calls.
  - A ground-up rewrite of trace2 to eliminate all dependencies that could transitively trigger `die()`.
  - Integration of CodeQL or similar static analysis to detect transitive `die()` calls, though this would currently fail due to the volume of existing violations.
- **Files/context**: The discussion references `strbuf.h`/`strbuf.c`, `json-writer.c`, and Derrick’s CodeQL query, which targets all trace2 `.c` files (`trace2.c` and `trace2/*.c`).
- **Behavioral impact**: If trace2 remains dependent on unsafe helpers, allocation or system call failures could still crash Git, undermining the series’ goal of making trace2 "best effort."
- **Open questions**:
  - Should the project prioritize a "safe strbuf" or a ground-up rewrite of trace2, or is the current incremental approach sufficient?
  - Can static analysis (e.g., CodeQL) be used to enforce `die()`-free code in trace2, or would it require too much upfront work?
  - Is the current series acceptable as a first step, despite its limitations, or should it be held until a more comprehensive solution is available?

Derrick’s tone is pragmatic, accepting Jeff’s critique while emphasizing the trade-offs of a disruptive rewrite. He positions the current series as a necessary first step that provides *some* protection and can serve as a template for future hardening efforts. The CodeQL query underscores the complexity of the problem and suggests that a complete solution would require significant architectural changes. No objections to Jeff’s feedback; the exchange is collaborative and forward-looking.