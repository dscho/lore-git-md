This is a **feature addition** to the `git replay` command, specifically patch 3/3 of v8 in the `--linearize` series. Toon Claes responds to feedback from Elijah Newren and Justin Tobler on the v8 implementation, addressing **UX clarity** and **terminology** in the documentation and error messages.

Toon accepts all suggested changes: he will rephrase the `--linearize` documentation to simplify the rationale for the single-branch restriction (replacing the technical explanation with "multiple branches are not supported"), update error messages to use "branches" instead of "revision ranges", and clarify the behavior when replaying shared history. These are **non-blocking** tweaks that improve user-facing clarity without altering the core logic.

On the **implementation detail** Elijah raised—whether the single-branch restriction should also apply to `--revert` mode—Toon points out that the restriction is already enforced for both `--revert` and `--advance` via the shared `set_up_branch_mode()` helper. This resolves the concern about concatenating unrelated histories in an unpredictable order, aligning `git replay`'s behavior with `git cherry-pick`.

The patch remains **technically complete** and ready for review, with no objections to the core functionality (dropping merges, reparenting commits, enforcing single-branch usage). The series is **not yet in `next`**, but Junio has indicated he will queue v8 for review. Minor documentation updates are expected before final integration.