This is a documentation patch, part of Karthik Nayak’s v4 series introducing the `receive-report` hook. It updates `git-receive-pack.adoc` to include a missing section on the `proc-receive` hook, which already exists in the codebase but was undocumented in the manual page.

The patch is a straightforward addition: it inserts a new `PROC-RECEIVE HOOK` section that briefly explains the hook’s purpose and links to `githooks(5)` for protocol details. This addresses a gap in the documentation lifecycle for `git-receive-pack`, where the `proc-receive` hook was previously omitted despite being part of the push process.

Karthik incorporates Patrick Steinhardt’s feedback verbatim, adopting a clearer commit message and replacing the custom description with the first paragraph from `githooks(5)` to avoid redundancy. The change is purely stylistic and does not alter the patch’s substance. The updated version also uses `linkgit:git-receive-pack[1]` for consistency with existing documentation conventions.

This patch is already queued in Junio’s `seen` branch alongside the rest of the series, which is technically complete and awaiting graduation to `next`. The feedback here is surface-level and non-blocking; the adjustments are minor and will likely be addressed in a follow-up or during integration. For readers tracking the `receive-report` hook effort, this patch is a small but necessary piece of the documentation puzzle.