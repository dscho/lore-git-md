This is a CI/build system patch, the third iteration of a standalone change to GitHub Actions workflow configuration. The patch modifies `.github/workflows/main.yml` to cancel stale CI runs for pull requests, reducing wasted runner capacity.

The core change remains the same as in v2: the `concurrency.group` expression now distinguishes between pull request and push events, grouping runs by pull request number for PRs and by commit hash for pushes. The `cancel-in-progress` flag is set to true only for pull request events. This ensures that only the latest push to a PR consumes CI resources, while preserving the existing `skip-if-redundant` optimization for regular pushes.

The v3 update is purely cosmetic: the commit message now describes the current behavior in present tense rather than past tense, a minor stylistic adjustment requested during review. No functional changes were made to the code or configuration. The patch continues to address the same real inefficiency in Git's CI infrastructure, and all prior feedback—including Junio C Hamano's acceptance of the unavoidable edge case where duplicate runs might occur for the same commit—has been resolved.

This version appears ready for merge, with no open technical questions or requested changes. The change is self-contained, well-explained, and unlikely to be controversial. It touches only the GitHub Actions workflow file and introduces no new symbols, CLI options, or on-disk format changes. Test coverage is not applicable, as this is a CI configuration change.