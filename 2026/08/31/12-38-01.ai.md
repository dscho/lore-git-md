This is a follow-up discussion in the ongoing trace2 hardening series, specifically addressing Jeff King’s architectural critique of the `banned-die.h` mechanism introduced in the first patch. Derrick Stolee acknowledges the limitations of compile-time bans—namely, that they cannot catch transitive `die()` calls via helpers like `strbuf` or `json-writer.c`—but defends the approach as a pragmatic first step. He argues that the header provides *some* protection and can serve as a template for gradually removing `die()`-prone dependencies from trace2, even if it doesn’t solve the problem entirely. His tone is measured, accepting Peff’s critique while emphasizing the trade-offs: a disruptive codebase reorganization (e.g., isolating trace2 from `die()`-prone headers) would be overkill, but the current mechanism at least enforces *direct* bans and documents the intent.

The exchange doesn’t resolve the fundamental tension—compile-time enforcement vs. runtime safety—but it clarifies that the series is intended as a *gradual* hardening effort, not a silver bullet. Derrick’s reply leaves the door open for future work (e.g., auditing indirect dependencies) but doesn’t commit to it. For now, the series proceeds with `banned-die.h` as a documented-but-imperfect tool, and this thread reinforces that the mechanism’s limitations are understood by the author. No immediate changes to the patch are proposed, but the discussion may influence how the header’s scope is communicated in future iterations.