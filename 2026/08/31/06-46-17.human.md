This is the third patch in Patrick Steinhardt’s 10-patch series refactoring `git fsck` to make object-database consistency checks pluggable. This specific patch reduces global state by moving fsck option variables into the `cmd_fsck()` function, converting them to local variables and relocating the option-parsing array into the function body.

The change is straightforward: four global variables (`check_full`, `keep_cache_objects`, `name_objects`, and `check_references`) are now declared locally within `cmd_fsck()`, and the `fsck_opts` array is moved inside the function. The patch removes 23 lines of global declarations and adds 22 lines of local declarations, with no behavior changes. This cleanup makes it easier to see which flags are used where and prepares the codebase for later patches that will rework option handling for pluggable ODB backends.

Karthik Nayak’s review of this patch in v1 was purely observational, noting that the variables were only used locally and did not need to be global. There were no technical concerns or edge cases raised, and the v2 iteration incorporates no changes beyond the minor editorial feedback applied across the series. The patch is a clean refactoring with no open questions or controversy.

This is a well-motivated infrastructure change that aligns with the series’ goal of making fsck checks pluggable. The reduction in global state is a clear improvement, and the patch is unlikely to attract further debate. If you’re following the ODB abstraction effort, this is a small but necessary step toward that larger goal.