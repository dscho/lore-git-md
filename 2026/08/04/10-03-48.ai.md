This is a small refactoring patch in the `git fast-import` libification series, now at v2 (patch 5/12). The patch changes the type of two boolean flags—`show_stats` and `quiet`—from `unsigned int` to `int` in `builtin/fast-import.c`. The change is preparatory: the next patch in the series will parse these flags using `OPT_BOOL`, which expects an `int` target. No behavior change is intended; the patch is purely mechanical and touches only the two variable declarations. The commit message is clear and concise, and the diff is minimal. This is a routine cleanup step in the broader effort to modernize `fast-import`'s command-line parsing and eliminate global variables. The patch looks uncontroversial and well-motivated within the series.