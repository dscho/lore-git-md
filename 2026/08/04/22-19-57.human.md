This is a brief check-in from Junio C Hamano on the fourth patch of Toon Claes’s four-part series optimizing `git last-modified` with Bloom filters. The series aims to make the experimental command as fast as `git log` when querying last-modified commits for pathspecs, including wildcards like `*.c`.

The thread context shows that Jeff King (Peff) raised a maintainability concern about patch 4/4: the current fix restores `bloom_filter_settings` after `prepare_revision_walk()` clears it, but Peff argues it would be cleaner for `last-modified` to manage its own settings pointer instead of relying on the revision machinery. His alternative design avoids potential conflicts and better isolates the command’s logic. Peff also mused about whether `bloom_filter_settings` should be a repository-level property with a helper function like `repo_bloom_filter_contains()`, but left this as an open question.

Junio’s email asks whether the discussion has reached a conclusion—are the patches in their current form acceptable, or is further deliberation needed? The question is directed at both Toon and Peff, implying the decision hinges on whether Peff’s suggested design change is worth adopting. No new technical points are introduced; this is purely a procedural nudge to clarify the path forward. The series is otherwise complete, with earlier patches addressing correctness and performance issues, so resolving this point will determine whether a reroll is needed.