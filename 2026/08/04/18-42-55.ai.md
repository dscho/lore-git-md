This is a test modernization patch (part 1/9 of Pablo Sabater’s GSoC v4 series extending `git cat-file --batch-command` to support `%(objecttype)`). The patch replaces `wc -c | xargs` with Git’s `test_file_size()` helper in `t5701-git-serve.sh`, a purely mechanical cleanup that improves portability and preserves command exit statuses. The change is uncontroversial and already acknowledged by Junio as a robustness improvement; no further discussion or code changes are expected. The commit message now explicitly notes the exit-status benefit, addressing Junio’s earlier observation. This patch is ready to merge alongside the rest of the series.