Thread identity: user support question regarding a regression in Git’s textconv feature introduced in Git 2.50; originating author Randall S. Becker (NonStop port maintainer).

Core problem or goal: since Git 2.50, external textconv filters receive an empty temporary file instead of the actual working-tree content, breaking workflows that depend on the original file path to locate repository files. The question is whether this change was deliberate or a bug, and how to restore the previous behavior.

Current status: reported as a breaking change; no patch or root-cause analysis provided yet. The issue is user-facing and affects anyone relying on external textconv filters that use the file path to identify content.

Key technical details: textconv filters now receive empty `/tmp/git-blob-*/<name>` files; prior behavior passed the actual working-tree file content. The regression impacts workflows where multiple files share the same name and the filter uses the path to disambiguate.

Open questions and loose ends: whether the change was intentional; what the root cause is; how to restore the old behavior; whether other users have encountered this.

Participant positions: Randall S. Becker reports the regression and requests clarification; no other participants or reviews yet.

Related work: none mentioned.