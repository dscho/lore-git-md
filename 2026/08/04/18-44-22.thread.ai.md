Thread identity: user support question regarding a regression in Git’s textconv feature introduced in Git 2.50; originating author Randall S. Becker (NonStop port maintainer). The regression is now confirmed to be binary-file-specific on the NonStop platform.

Core problem or goal: since Git 2.50, external textconv filters receive an empty temporary file when processing *binary* files (those beginning with a null byte), breaking workflows that depend on the original file path to locate repository files. Text files and ELF binaries (which do not begin with null bytes) work as expected. The question remains whether this change was deliberate or a bug, and how to restore the previous behavior.

Current status: reported as a breaking change with a key refinement—empty temp files occur only for binary files on NonStop, while text files and non-null-prefixed binaries (e.g., ELF) work correctly. D. Ben Knoble’s earlier observation of populated temp files on Git 2.53.0 (platform unspecified) may reflect testing with non-binary files or a platform-specific difference. No patch, root-cause analysis, or maintainer response yet. The issue remains unresolved and user-facing.

Key technical details: textconv filters now receive empty `/tmp/git-blob-*/<name>` files *only for binary files* on NonStop; prior behavior passed the actual working-tree file content. The regression appears tied to null-byte handling during temp file creation, possibly involving platform-specific I/O behavior (e.g., early truncation or failure to write). Ben’s test with `binary = true` in the diff driver showed populated temp files, but this may not have involved null-prefixed files. The discrepancy between Randall’s and Ben’s observations is now partially explained by file content type, though platform differences (e.g., NonStop’s I/O stack) may still contribute.

Open questions and loose ends:
- Whether the change was intentional (still unanswered).
- Root cause: is this a platform-specific I/O issue (e.g., NonStop’s handling of null bytes), a Git version regression, or a combination of both?
- Why Ben’s system (Git 2.53.0) behaves differently: did his test involve non-binary files, or is there a platform-specific factor?
- How to restore the old behavior for binary files on NonStop.
- Whether other users have encountered this, particularly on other platforms with binary files.
- Whether the regression affects all textconv filters or only those relying on file paths for binary content.

Participant positions:
- **Randall S. Becker**: reports the regression is binary-file-specific on NonStop, describes workflow breakage for binary files, and refines the scope to null-byte-prefixed content. Requests clarification on intent and a fix.
- **D. Ben Knoble**: earlier confirmed populated temp files on Git 2.53.0, but his test may not have involved binary files. Suggested debugging with `GIT_TRACE2=1` or a shell-based filter, but did not identify a root cause or propose a fix. His observation remains a key data point but is now partially reconciled with Randall’s findings (content type matters).

Related work: none mentioned.