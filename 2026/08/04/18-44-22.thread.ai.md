Thread identity: user support question regarding a regression in Git’s textconv feature introduced in Git 2.50; originating author Randall S. Becker (NonStop port maintainer).

Core problem or goal: since Git 2.50, external textconv filters receive an empty temporary file instead of the actual working-tree content, breaking workflows that depend on the original file path to locate repository files. The question is whether this change was deliberate or a bug, and how to restore the previous behavior.

Current status: reported as a breaking change with conflicting reports about its reproducibility. Randall S. Becker observes empty temp files on NonStop, while D. Ben Knoble reports populated temp files on Git 2.53.0 (platform unspecified). No patch, root-cause analysis, or maintainer response yet. The issue remains user-facing and unresolved.

Key technical details: textconv filters now receive empty `/tmp/git-blob-*/<name>` files on NonStop; prior behavior passed the actual working-tree file content. Ben’s test with `binary = true` in the diff driver shows populated temp files, suggesting the regression may be platform-specific or configuration-dependent. The discrepancy implies the root cause could involve platform I/O, Git version, or textconv driver setup.

Open questions and loose ends: whether the change was intentional; what the root cause is (platform-specific? version-specific?); why Ben’s system behaves differently; how to restore the old behavior; whether other users have encountered this; and whether the regression affects all textconv filters or only those relying on file paths.

Participant positions:
- Randall S. Becker: reports the regression on NonStop, describes workflow breakage, requests clarification.
- D. Ben Knoble: confirms populated temp files on Git 2.53.0, suggests debugging with `GIT_TRACE2=1` or a shell-based filter, but does not identify a root cause or propose a fix. His observation introduces a key discrepancy (populated vs. empty temp files) that narrows the regression scope to environment-specific factors.

Related work: none mentioned.