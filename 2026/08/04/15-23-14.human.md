This is a surface-level review comment on patch 3/8 of Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support `%(objecttype)` in remote-object-info queries. The series is now at v3, feature-complete, and under active review.

Karthik Nayak questions the commit message’s claim that `fetch_object_info()` does not use all three fields (`server_options`, `oids`, and `object_info_options`) bundled in `struct object_info_args`. The comment is brief and does not engage with the broader architectural refactoring (replacing `struct object_info` with a dedicated `struct fetch_object_info_results` in patch 4/8) or the mechanical nature of the change. It appears to be a minor clarification request rather than a substantive objection.

The patch itself is a straightforward refactoring that removes the intermediate struct and passes its members directly to the function. No behavior changes are intended, and the change is uncontroversial. The discussion is unlikely to derail the series, which has already addressed all prior review feedback and is considered merge-ready by the author.