This is the first patch in Patrick Steinhardt’s seven-part refactoring series to unify Git’s ODB streaming API. It moves the expected size of a write stream into the `odb_write_stream` structure itself, eliminating the need to pass it separately through the call chain. The change is mechanical but touches 14 files, including core object handling (`object-file.c`, `odb.c`), backend implementations (`odb/source-*.c`), and the unit test for the in-memory backend.

The motivation is straightforward: the size is always needed alongside the stream, so bundling it makes the API more self-contained and paves the way for merging `odb_write_stream` and `odb_read_stream` in later patches. The patch is well-scoped and clearly explained; no behavior changes are introduced, only structural cleanup. Reviewers will likely focus on whether the new field is consistently initialized and whether any edge cases (e.g., mismatched sizes) are properly handled.

This is a solid, uncontroversial step in the ODB abstraction effort. If the rest of the series maintains this quality, it should land smoothly.