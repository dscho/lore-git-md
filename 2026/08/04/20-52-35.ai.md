This is a user support discussion about a potential regression in Git’s textconv feature.

D. Ben Knoble replies to Randall S. Becker’s report that external textconv filters now receive empty temporary files (`/tmp/git-blob-*/<name>`) instead of the actual working-tree content. Ben confirms that on his system (Git 2.53.0), the temp file *does* contain the expected content, even when `binary = true` is set in the diff driver. He suggests debugging with `GIT_TRACE2=1` or a shell-based textconv filter to inspect the invocation, but does not identify a root cause or propose a fix.

The key technical detail here is the discrepancy between Randall’s experience (empty temp files) and Ben’s (populated temp files). This suggests the regression may be environment-specific (e.g., platform, Git version, or configuration) rather than universal. No other participants have weighed in yet, and the thread remains focused on diagnosing the behavior rather than addressing whether the change was intentional or how to restore the old behavior. The discussion is still open-ended, with no patch or deeper analysis provided.