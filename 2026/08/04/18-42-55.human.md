This is a test modernization patch, the first in a nine-patch GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series as a whole is now feature-complete and merge-ready, with all prior review feedback addressed.

This patch replaces the use of `wc -c | xargs` in `t5701-git-serve.sh` with Git’s `test_file_size()` helper and caches the output of `git rev-parse two:two.t` in variables. The change is purely mechanical and uncontroversial: it improves portability by avoiding platform-specific whitespace padding from `wc`, and it preserves the exit status of commands run inside the here-document, which the original pipeline would have masked. Junio’s earlier observation about exit-status preservation is now explicitly noted in the commit message, and Pablo confirms no code changes are needed beyond the test update.

The patch touches only the test script and does not affect production code. It is a small but useful cleanup that aligns with the project’s ongoing test modernization effort. No substantive review feedback remains; this is a straightforward improvement that should proceed alongside the rest of the series.