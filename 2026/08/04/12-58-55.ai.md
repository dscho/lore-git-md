This is a **substantive, tested review** of a bugfix patch for the `git maintenance` command.

Patrick Steinhardt confirms the regression and the fix: the patch correctly restores the documented `"none"` strategy handling that was broken by commit d465be2327 (October 2025). He explicitly states the code change ("looks obviously correct") and the test coverage ("test looks obviously correct to me") are both sound. The review is brief but substantive—it engages with the *behavior* (not just style) and endorses the fix as the right way to honor the documented contract. Patrick also notes that while disabling maintenance entirely achieves the same end, users expect `"none"` to work as documented, which reinforces the patch’s motivation.

The only remaining loose end is the author identity mismatch (in-body `From:` vs. sign-off address) that Junio already flagged; Patrick echoes that the patch is otherwise ready. No new technical concerns are raised, and the fix remains minimal: two lines in `builtin/gc.c` and three lines in `t/t7900-maintenance.sh`. The patch is now effectively approved, pending the trivial housekeeping fix.