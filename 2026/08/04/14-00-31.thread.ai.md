Thread identity
Subject: [PATCH] worktree add: disable DWIM when -b/-B is given
Series version: standalone patch (no series)
Originating author: Yoichi NAKAYAMA
Thread type: bugfix for a regression

Core problem or goal
Fix a regression in `git worktree add` where the `-b` or `-B` options were ignored when a branch name could be DWIMmed to a remote-tracking branch. The intended behavior is that explicit `-b` or `-B` should take precedence over DWIM, preventing unintended branch creation.

Current status
Patch posted (v1); under review with proposed adjustments. Junio C Hamano provided substantive feedback requesting changes to avoid unintended side effects (skipping `can_use_local_refs()` when `-b` is used with `HEAD` and preserving warning behavior). Yoichi Nakayama has agreed to adjust the fix to preserve the warning and improve the test case. No merge status reported.

Key technical details
- Files touched: `builtin/worktree.c` (logic change: proposed fix will skip DWIM only when `new_branch` is set *and* the warning path is not suppressed; Junio’s feedback suggests a more targeted condition to preserve `HEAD`-related warnings), `t/t2400-worktree-add.sh` (new test case `add <path> <branch> does not dwim with -b`; will be refined to remove redundant `git init` and add `test_grep` for the expected error message `fatal: invalid reference: foo`).
- Regression introduced by commit 128e5496b325640f0a09cc1d5b1e346c069b410f (Git v2.42.0).
- On-disk format: unchanged.
- Test coverage: one test case added; will be improved for precision as requested.

Open questions and loose ends
1. Finalize the adjusted condition in `builtin/worktree.c` to preserve `can_use_local_refs()` warnings when `-b` is used with `HEAD`.
2. Complete test refinements: remove redundant setup and validate error message content.

Participant positions
- **Junio C Hamano**: Substantive review; objects to the initial implementation due to unintended side effects (skipping `HEAD`-related warnings) and suggests a more targeted fix. Recommends test improvements (error message validation, redundant setup removal). Position remains actionable and requires changes.
- **Yoichi Nakayama**: Agrees with Junio’s feedback and plans to adjust the fix to preserve warning behavior and improve the test. Position is collaborative and responsive.

Related work
Regression commit 128e5496b325640f0a09cc1d5b1e346c069b410f.