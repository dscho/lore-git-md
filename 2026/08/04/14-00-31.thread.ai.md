Thread identity
Subject: [PATCH] worktree add: disable DWIM when -b/-B is given
Series version: standalone patch (v4, final; already merged)
Originating author: Yoichi NAKAYAMA
Thread type: bugfix for a regression

Core problem or goal
Fix a regression in `git worktree add` where the `-b` or `-B` options were ignored when a branch name could be DWIMmed to a remote-tracking branch. The intended behavior is that explicit `-b` or `-B` should take precedence over DWIM, preventing unintended branch creation.

Current status
Merged to `master` as of Junio C Hamano's final sign-off on v3 (2024-07-10, exact date from "What's cooking" report). The v4 iteration is a post-merge documentation tweak: the commit message now cites the regression commit using `log -1 --pretty=reference` format, matching project conventions. No code changes from v3.

Key technical details
- Files touched: `builtin/worktree.c` (logic change: suppress DWIM when `new_branch` is set, preserving `can_use_local_refs()` warnings), `t/t2400-worktree-add.sh` (test case `add <path> <branch> does not dwim with -b`; uses `setup_remote_repo` to avoid redundant `git init`, validates error message `fatal: invalid reference: foo` with `test_grep`).
- Regression introduced by commit `128e5496b3 (worktree add: extend DWIM to infer --orphan, 2023-05-17)` (Git v2.42.0).
- On-disk format: unchanged.
- Test coverage: one test case added; refined for precision and edge-case handling as requested.
- Behavior change: DWIM logic is now guarded by `!new_branch`, restoring pre-v2.42.0 behavior.
- Commit message now cites the regression commit using `log -1 --pretty=reference` (v4 change).

Open questions and loose ends
None. All feedback addressed; no further discussion expected.

Participant positions
- **Junio C Hamano**: Substantive review of v1 and v2; identified unintended side effects (skipping `HEAD`-related warnings) and recommended test improvements. Final sign-off on v3 with minor commit message tweak. Position resolved.
- **Yoichi Nakayama**: Responsive to feedback; v3 preserves warning behavior, improves test precision, and addresses stylistic commit message nits. v4 iteration is a post-merge documentation-only update. Position remains collaborative.

Related work
Regression commit `128e5496b3 (worktree add: extend DWIM to infer --orphan, 2023-05-17)`.