Thread identity
Subject: [PATCH] completion: add git history
Series version: standalone patch (no version marker)
Originating author: Vincent Mailhol
Thread type: patch series (single-patch)

Core problem or goal
Add bash completion support for the `git history` command, covering its four subcommands (`drop`, `fixup`, `reword`, `split`), their options, and positional revision arguments.

Current status
Under review; no prior version recorded in this thread.

Key technical details
- Files touched: `contrib/completion/git-completion.bash`, `t/t9902-completion.sh`
- New symbols/functions: none (uses existing `__gitcomp_builtin` and `__git_complete_refs`)
- Behavior: completion of subcommands, options (e.g., `--update-refs=` for `fixup`), and revision names (e.g., `main`)
- On-disk format: unchanged
- Test coverage: added in `t/t9902-completion.sh` for subcommands, options, and revision arguments

Open questions and loose ends
None identified in the patch or summary.

Participant positions
Vincent Mailhol (author): implemented and tested the completion script.

Related work
Depends on the `git history` command, which was introduced in an earlier (unseen) thread.