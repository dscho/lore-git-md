This is the final exchange in Patrick Steinhardt’s five-patch series making on-disk ODB structures pluggable, part of the ongoing ODB abstraction effort. The discussion centers on the fifth patch, which introduces a `create_on_disk` callback in the ODB source interface to let backends handle their own directory creation.

Junio C Hamano raises a forward-looking design question: whether the `create_on_disk` callback is the right place for backends that don’t use traditional filesystems (e.g., cloud-based object stores requiring provisioning). He suggests renaming it to something more generic, like `initialize`, to better reflect its broader purpose. Patrick acknowledges the name is somewhat misleading but argues for consistency with the existing `ref_store_create_on_disk()` function in the ref subsystem. No implementation changes are requested or promised; the feedback is purely about naming and future extensibility.

The series remains under review, with the fifth patch now receiving its first substantive design feedback. The exchange is low-stakes—no correctness issues are raised—but it highlights a tension between consistency and clarity in API design. For a busy reader, this confirms the series is technically sound but may need minor adjustments before merging. The broader ODB abstraction effort continues to advance, with this series laying groundwork for future backends.