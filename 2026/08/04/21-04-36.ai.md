This is Junio C Hamano’s confirmation that the `git branch --delete-merged` series (v24) is now **ready for merging**. The email acknowledges Phillip Wood’s final review, which verified the two key correctness fixes in v24:

1. **Push-detection logic** in `branch_pushes_to_upstream()` now correctly applies the *push* refspec followed by the *fetch* refspec, ensuring branches like `main` tracking `origin/main` are preserved regardless of `remote.pushDefault` configuration.
2. **Stacked-branch protection** has been simplified to protect only the *direct* upstream of a surviving branch, clearing stale upstream configs for kept (merged) branches whose own upstream is being deleted. This resolves the v23 test discrepancy and aligns with the design goal, with Phillip noting the implementation "pretty much takes us back to V19" but with the critical improvement of order independence.

Junio’s response—"OK. I'd mark the topic as 'expecting a (hopefully small and final) reroll'"—signals the series is effectively complete, with only a **surface-level nit** remaining: unwrapping overly long test command lines in patch 1/7. This is a trivial formatting issue and not a blocker. The series is **implementation-complete**, with all planned features, safety mechanisms, and correctness fixes in place. Junio’s phrasing ("hopefully small and final") underscores that no substantive changes are expected.

The series will now proceed to `next` for integration testing, with the agreed follow-up work (extending stacked-branch protection to `git branch -d`) to be implemented post-merge. Key files touched include `builtin/branch.c`, `ref-filter.c`, and the test suite (`t/t3200-branch.sh`), with the flags-based API (patches 2-4/7) and per-branch opt-out (patch 6/7) now finalized. The `--dry-run` functionality (patch 7/7) is also complete, with tests confirming it avoids modifying config during dry runs.