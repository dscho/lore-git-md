This is the final patch in Patrick Steinhardt’s seven-part series refactoring the object database (ODB) streaming API. It unifies the function names that create new streams—`odb_read_stream_open()` and `odb_write_stream_from_fd()`—into a consistent schema: `odb_stream_from_object()` and `odb_stream_from_fd()`. The patch also clarifies ownership of the file descriptor passed to `odb_stream_from_fd()` in the header documentation.

The change is mechanical and touches eight files, primarily updating callers to use the new function names. The commit message is concise but sufficient, and the patch completes the structural unification begun in earlier patches. No behavior changes are introduced, and the series remains purely a refactoring effort as part of the broader ODB abstraction work.

This patch is uncontroversial and should slot cleanly into the series. The only likely discussion points are edge cases around file descriptor ownership (now documented) and ensuring all callers correctly handle the stream lifecycle. Reviewers may focus on completeness—verifying no lingering references to the old function names remain—but the patch looks thorough. With this final piece, the ODB streaming API is now fully unified, simplifying future backend work.