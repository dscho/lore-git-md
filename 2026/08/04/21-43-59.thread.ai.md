Thread identity
Subject: [PATCH] gitk: add user-configurable custom commands to context menus
Series version: standalone patch (no series)
Originating author: Tim Wiederhake
Thread type: feature patch

Core problem or goal
Add user-configurable custom commands to `gitk`'s right-click context menus (commit list and diff display). Users can define up to three commands per area, with placeholder substitution (commit ID, title, author, blame origin, etc.) and shell execution. Configuration is exposed via a new "Commands" tab in the preferences dialog.

Current status
Under review; no prior versions or discussion on-list. Patch is self-contained to `gitk` (Tcl/Tk frontend), touching only the `gitk` script (217 lines added, 3 removed). No impact on Git's core C code or plumbing.

Key technical details
- Files touched: `gitk` (Tcl script)
- New global variables: command templates for commit-list and diff-display menus
- New procedures: dynamic menu population, preferences-page UI
- Execution: commands run via `sh -c`; exit code 42 displays command output
- Symbols: none in C; Tcl variables and procedures internal to `gitk`
- On-disk format: none; configuration stored in `~/.gitk` (existing file)
- Test coverage: none in this patch

Open questions and loose ends
None raised yet; patch is initial submission.

Participant positions
None yet; no reviews or replies on-list.

Related work
None mentioned.