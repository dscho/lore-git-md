This is the fourth patch in Patrick Steinhardt’s seven-patch series refactoring the object database (ODB) streaming API. It renames `struct odb_read_stream` to `struct odb_stream` in preparation for unifying the read and write stream types into a single structure. The change is purely mechanical: every occurrence of `odb_read_stream` is replaced with `odb_stream`, and the corresponding function names (`odb_read_stream_read`, `odb_read_stream_close`, `odb_read_stream_open`) are updated to match. The patch touches 18 files, including core ODB code (`odb/streaming.c`, `odb/source-*.c`), archive formats (`archive-tar.c`, `archive-zip.c`), packfile handling (`builtin/pack-objects.c`, `packfile.c`), and the unit test `t/unit-tests/u-odb-inmemory.c`.

The motivation is clear: the rename eliminates the "read" prefix, making the type name neutral and ready for the unified stream API that will handle both reading and writing. The patch is uncontroversial in itself—it’s a straightforward search-and-replace—but it sets the stage for the more substantive unification in later patches. Reviewers may focus on ensuring the rename is complete (no lingering references to the old type name) and that the updated function signatures match the new type.

This patch is part of the broader ODB abstraction effort and builds directly on the preparatory work in Patches 1–3 (embedding size in the write stream, removing `is_finished`, and adding object type support). No behavior changes are introduced, and the series remains on track for inclusion in `next` or `master` once review concludes.