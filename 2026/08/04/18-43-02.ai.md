This is the eighth patch in Pablo Sabater’s nine-patch GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series is now feature-complete and merge-ready, with all prior review feedback addressed.

This patch advertises the "type" capability in the server’s object-info advertisement, enabling the client to request object type metadata. The change is minimal: a one-line update in `serve.c` appends "type" to the advertised capabilities string (currently "size"), and the test suite is updated to reflect the new functionality. The test that previously verified unsupported placeholders return empty strings is repurposed to confirm `%(objecttype)` now returns the actual type ("blob"), while a new test ensures truly unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings. The patch also updates test helper variables (`hello_type`, `tree_type`, `commit_type`, `tag_type`) to reflect the expected object types for each test object.

The implementation is straightforward and follows the established pattern for capability advertisement. No new technical questions or loose ends are introduced; this patch completes the end-to-end chain for object type metadata, enabling the client to request and display type information from the server. Reviewers familiar with the protocol layer may want to verify the test expectations, though the changes are narrowly scoped and align with the series’ design goals. The patch is uncontroversial and ready for integration.