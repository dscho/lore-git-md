This is a **design discussion** about the user experience for `git history squash`, specifically whether the editor should open by default when squashing commits. Phillip Wood responds to Junio C Hamano’s critique of his earlier proposal for a context-sensitive default editor behavior.

Phillip acknowledges Junio’s concerns about the complexity and potential confusion of a dynamic default—where the editor opens only if the commits being squashed are not primarily `fixup!` or `amend!` messages. He now concedes that always opening the editor might be the better approach, despite its trade-offs. This simplifies the behavior, making it easier to explain and reducing the risk of user confusion. It also aligns with the principle that squashing should encourage users to review and revise the commit message, rather than silently combining commits.

The discussion remains focused on the `hn/history-squash` topic, which was ejected from `next` due to fundamental correctness flaws in its reachability logic. While this email does not address those issues (fixups are still pending from Phillip), it advances the debate on message-handling behavior. Phillip’s shift toward a simpler, more predictable default may help resolve the tension between usability and scriptability, though it does not yet address the broader question of whether `--reedit-message` should be deprecated in favor of `--edit`/`--no-edit` (as proposed by Matt Hunter and Phillip earlier).

The tone is collaborative, and Phillip’s willingness to reconsider his position suggests the design is still fluid. The next steps will likely involve further iteration on the message-handling logic, possibly incorporating this simpler default into the fixups for the reachability issues. For now, the topic remains in `seen` pending those updates.