This is Junio C Hamano's review of the third patch in Patrick Steinhardt's seven-patch series refactoring the Git object database (ODB) streaming API. The series aims to unify the read and write streaming interfaces as part of the broader ODB abstraction effort.

Junio raises a **substantive technical concern** about a potential inconsistency in the patch: while the patch updates `odb_source_inmemory_write_object()` to use the new `stream->type` field (allowing non-blob objects), the preceding `hash_object_file()` call still hardcodes `OBJ_BLOB`. This mismatch means the object name (`oid`) is computed as if the data were a blob, even when the object type is something else (e.g., a tree or commit). The implication is unclear—it could lead to incorrect object names or subtle corruption if the type affects how the data is hashed or stored.

The review is **not a rejection** but a pointed question about correctness. Junio acknowledges the goal of supporting non-blob streaming is worthwhile, though he remains unconvinced about the broader unification of read/write streams (a design decision addressed later in the series). His feedback targets a specific edge case that could undermine the patch's intent, and the answer will likely determine whether the patch needs revision or additional justification.

This is a **high-signal review**—the kind that surfaces a real issue rather than nitpicking style. If the inconsistency is confirmed, it would affect the patch's core functionality, not just its implementation details. The thread is now paused at this question; Patrick's response will clarify whether the patch is correct as-is or needs adjustment. For readers tracking the ODB refactoring, this is a key moment to watch.