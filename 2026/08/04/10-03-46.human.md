This is a documentation patch, part of Christian Couder’s 12-patch v2 series refactoring `git fast-import` and modernizing the parse-options API. It updates `Documentation/technical/api-parse-options.adoc` to document the `OPT_*_F()` macro variants (which accept a trailing `flags` argument) and the new `OPT_HIDDEN_GROUP()` and `OPT_HIDDEN_BOOL()` convenience macros introduced earlier in the series.

The patch adds a short paragraph explaining the `_F` convention—how the non-`_F` macros are simply wrappers with `flags` set to zero—and places the new hidden-group and hidden-boolean macros adjacent to their non-hidden counterparts (`OPT_GROUP()` and `OPT_BOOL()`). The placement and wording are clear and consistent with the existing documentation style. No behavior changes are introduced; this is purely internal API documentation for developers working with the parse-options API.

The patch is small, focused, and well-motivated by the series’ goal of standardizing and documenting the parse-options infrastructure. It complements the earlier patches that introduced the macros and documented per-option flags, completing the API reference. There is no controversy or open question here—just routine documentation maintenance. If you care about the parse-options API or are reviewing the `fast-import` libification work, this patch is worth noting for context; otherwise, it is safe to skip.