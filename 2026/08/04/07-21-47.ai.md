This is a short follow-up in the ODB abstraction series, specifically addressing Junio C Hamano’s review of the third patch. The patch defers object database (ODB) creation until after the ref database is set up, introducing the `APPLY_REPOSITORY_FORMAT_SKIP_ODB_CREATION` flag to allow `apply_repository_format()` to leave the repository in a partially-initialized state. Junio’s review was substantive but confirmatory, asking whether this change was a prelude to supporting non-filesystem ODB backends via a future `objectStorage` extension.

Patrick Steinhardt’s reply is brief and direct: yes, the refactoring is explicitly aimed at disentangling repository setup from backend-specific configuration, citing past pain with the `refStorage` extension and anticipating similar challenges with `objectStorage`. The response does not introduce new technical details or reopen design questions; it simply confirms the patch’s role as infrastructure for future pluggable ODB backends. This keeps the thread focused on the series’ overarching goal—making on-disk ODB structures pluggable—without revisiting the flag-based initialization sequence or the deferred creation timing that Justin Tobler previously questioned.

For a future AI summarizer, note that the reply reaffirms the patch’s purpose as a prerequisite for the `objectStorage` extension, tying it to the broader ODB abstraction effort. The key files (`setup.c`, `repository.c`) and symbols (`APPLY_REPOSITORY_FORMAT_SKIP_ODB_CREATION`, `create_object_database()`) remain unchanged, and no new test coverage or edge cases are introduced. The exchange is purely explanatory, with no unresolved questions or objections.