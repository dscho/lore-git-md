This is a refactoring patch series targeting the object database (ODB) subsystem, part of Patrick Steinhardt’s ongoing ODB abstraction effort. The series unifies the previously separate `odb_write_stream` and `odb_read_stream` into a single `odb_stream` structure, simplifying the API and enabling reuse of streams for different purposes (e.g., reading an object and writing it elsewhere). The changes are mechanical but touch 23 files, including core object handling (`object-file.c`, `odb.c`), archive formats (`archive-tar.c`, `archive-zip.c`), and packfile operations (`builtin/pack-objects.c`, `packfile.c`).

The motivation is clear: the two stream types duplicated functionality, and unifying them reduces redundancy while making the API more flexible. The commit messages are concise but sufficient, and the series is well-scoped—no behavior changes are introduced, only structural cleanup. The patch count (7) reflects incremental steps (e.g., tracking stream size, renaming fields, consolidating structures) rather than a monolithic change, which should ease review.

Key technical details for follow-up discussion:
- The new `odb_stream` structure replaces both `odb_write_stream` and `odb_read_stream`, with unified function names (e.g., `odb_stream_open()` instead of `odb_write_stream()`/`odb_read_stream()`).
- Files modified include `odb/streaming.c` (core logic), `odb/source-*.c` (backend implementations), and callers like `builtin/pack-objects.c` and `archive-zip.c`.
- The series builds on `5b2471720c` (Junio’s "10th batch" for the upcoming release), suggesting it’s targeting `next` or `master` soon.
- No tests are added, but the existing unit test `t/unit-tests/u-odb-inmemory.c` is updated to match the new API.

This looks like a solid, uncontroversial refactor that aligns with the broader ODB abstraction goals. The only likely points of discussion are edge cases in the unified API (e.g., error handling for mixed read/write operations) or potential performance regressions in stream-heavy code paths. Reviewers familiar with the ODB (e.g., Justin Tobler, Taylor Blau) may focus on those aspects.