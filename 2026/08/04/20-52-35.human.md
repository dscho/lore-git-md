This is a user support discussion about a potential regression in Git’s textconv feature. D. Ben Knoble replies to Randall S. Becker’s report that external textconv filters receive empty temporary files in Git 2.50+, breaking workflows that rely on the original file path. Ben confirms that on his system (Git 2.53.0), the temp file *does* contain the expected content, even with `binary = true` set. He suggests debugging with `GIT_TRACE2=1` or a shell-based textconv wrapper to inspect the invocation.

The key discrepancy here is that Randall sees empty temp files while Ben sees populated ones, despite both using similar configurations. This suggests the issue might be environment-specific (e.g., platform, Git version, or build flags) rather than a universal regression. No root cause or patch is proposed yet, but the exchange narrows the problem to a potential edge case in how Git writes or passes temp files to external filters. The discussion is still in the diagnostic phase, with no clear resolution or workaround offered. If you rely on textconv filters, this thread is worth monitoring for follow-up analysis or fixes.