This is the eighth patch in Pablo Sabater’s nine-patch GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series is now feature-complete and merge-ready.

This patch enables the server to advertise the "type" capability in its object-info advertisement, allowing the client to request object type metadata. The change is minimal: the server’s capability string in `serve.c` is updated from `"size"` to `"size type"`, and the accompanying test in `t1017-cat-file-remote-object-info.sh` is updated to expect the actual type ("blob") for `%(objecttype)` instead of an empty string. A new test verifies that unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings, preserving backward compatibility.

The patch is a straightforward protocol extension with no behavioral surprises. It completes the end-to-end chain for object type metadata, following the server-side parsing logic (patch 6/9) and client-side parsing logic (patch 7/9). Reviewers familiar with the protocol layer may want to confirm the capability string format, but the implementation follows established patterns. The test coverage is thorough and includes edge cases like duplicate OIDs and unsupported placeholders. No further changes are expected.