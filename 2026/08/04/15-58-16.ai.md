This is the **final review of Harald Nordgren’s `git branch --delete-merged` v24 series**, confirming it is ready for merging. Phillip Wood’s review focuses on the two key correctness fixes introduced in v24 and a minor test formatting nit.

The v24 changes address the last technical blockers:
1. **Push-detection logic**: The `branch_pushes_to_upstream()` function now correctly maps branches through the *push* and *fetch* refspecs, ensuring branches like `main` tracking `origin/main` are preserved even when `remote.pushDefault` names a fork. Phillip confirms this fix works as intended.
2. **Stacked-branch protection**: The logic now protects only the *direct* upstream of a surviving branch, deleting deeper merged bases and clearing their stale upstream configurations. This resolves the v23 test discrepancy and aligns with the design goal. Phillip notes the implementation "pretty much takes us back to V19" but with a critical improvement: pruning protected branches from the deletable set *after* visiting all branches, ensuring order independence.

The only remaining feedback is a **surface-level nit**: unwrapping overly long test command lines in patch 1/7 (e.g., `--forked "origin/*" --no-merged origin/one` → split across lines). While Phillip suggests this might not warrant a re-roll on its own, it’s a trivial change that could be addressed in a final version if Junio requests it.

**Key technical details for follow-up discussion**:
- The **flags-based API** (patches 2-4/7) remains unchanged, with local boolean caching eliminated to ensure flags are the single source of truth.
- **Per-branch opt-out** (`branch.<name>.deleteMerged=false`) and **`--dry-run`** functionality are finalized, with tests verifying config preservation during dry runs.
- The **`--forked` filter** (patch 1/7) is unaffected by v24 changes and continues to support exact refnames, remote shorthand, and shell globs.
- **Test coverage** now includes the push-detection fix and simplified stacked-branch protection, with explicit verification of cleared upstream configs for kept branches.

**Merge status**: Phillip explicitly states the series "looks ready to be merged." Junio’s prior approval (contingent on resolving Phillip’s technical concerns) is now satisfied. The series should appear in `next` shortly, with no further substantive changes expected. The agreed follow-up work—extending stacked-branch protection to `git branch -d`—will be implemented post-merge.