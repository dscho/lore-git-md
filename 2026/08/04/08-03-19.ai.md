This is a **surface-level review** of the documentation patch (3/3) in the v2 series fixing race conditions in HTTP daemon test helpers. Patrick Steinhardt provides minor wording and clarity suggestions for the new "Writing concurrency-safe helpers" section in `t/README`.

The review focuses on three nits: (1) the section header should explicitly mention `lib-httpd` since the guidance is specific to that context, (2) the note about `rm` (without `-f`) being a viable atomic alternative to `mv` for consuming one-shot markers, and (3) replacing the shell variable `$$` with an explicit mention of PIDs for clarity. None of these points challenge the technical correctness or necessity of the documented idioms; they are purely editorial.

The series as a whole remains ready for integration, pending resolution of the unrelated error-handling question in patch 1. This documentation patch itself is uncontroversial and unlikely to require further changes.