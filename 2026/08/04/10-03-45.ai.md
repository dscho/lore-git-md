This is the second patch in Christian Couder’s 12-patch v2 series refactoring `git fast-import`’s usage string and advancing its libification. It adds a new "Option flags" subsection to `Documentation/technical/api-parse-options.adoc`, documenting the per-option flags (e.g., `PARSE_OPT_HIDDEN`, `PARSE_OPT_OPTARG`) that can be set via the `flags` field of `struct option` or the `OPT_*_F()` macro variants. The patch explicitly distinguishes these per-option flags from the global `parse_options()` flags, describes their purpose, and clarifies the `-h` versus `--help-all` behavior for hidden options. The 61-line addition is purely documentation and has no runtime impact; it is a prerequisite for the subsequent patches that introduce new parse-options macros and refactor `git fast-import`’s option parsing. The patch is well-motivated, self-contained, and ready for review.