This is a user support discussion about a suspected platform-specific regression in Git’s textconv feature, specifically affecting binary files on the NonStop platform.

Randall S. Becker narrows the scope of the regression: the empty temporary files issue occurs *only* with binary files, while text files and ELF binaries (which do not begin with a null byte) work as expected. This observation suggests the root cause may involve how Git handles binary content during textconv filter invocation, possibly related to file creation, truncation, or early termination when encountering null bytes. The discrepancy between Randall’s experience (empty temp files for binaries) and D. Ben Knoble’s (populated temp files regardless of content type) now appears to be tied to file content rather than platform alone, though NonStop’s I/O behavior could still play a role.

The thread remains focused on diagnosis rather than resolution. No patch or deeper technical analysis has been provided, and the question of whether this change was intentional is still unanswered. Randall’s update reframes the problem as binary-specific, which may help future responders isolate the issue, but the discussion is still open-ended and lacks maintainer involvement. The regression’s impact is now clearer—it breaks textconv workflows for binary files on NonStop—but the underlying cause and potential fix remain unknown.