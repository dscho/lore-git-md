This is the final patch in Pablo Sabater’s nine-patch GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series is now feature-complete and merge-ready.

The patch removes the temporary workaround that used a different default format (`%(objectname) %(objectsize)`) for `remote-object-info` commands, unifying it with the standard default (`%(objectname) %(objecttype) %(objectsize)`) used by all other `cat-file` batch commands. Documentation is updated to reflect the new functionality: `%(objecttype)` is no longer listed as a caveat for `remote-object-info`, and the protocol-v2 specification is expanded to describe the "type" attribute and its string values ("blob", "tree", "commit", "tag"). Tests in `t1017` are updated to expect the unified format, ensuring consistent output across all transport protocols (git://, file://, http://) and confirming that mixing `info` and `remote-object-info` commands produces identical results.

No new technical questions or loose ends are introduced; this patch simply cleans up the temporary divergence and locks in the behavior implemented in earlier patches. The changes are mechanical and uncontroversial, touching only documentation, test expectations, and a small block of code in `builtin/cat-file.c` that enforced the temporary format. The patch is a natural capstone to the series and should integrate smoothly.