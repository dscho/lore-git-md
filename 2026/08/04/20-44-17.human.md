This is a refactoring patch in the GSoC series that extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder. This third patch of nine removes the `struct object_info_args` wrapper and passes its three members directly to the functions that use them. The change is purely mechanical—updating function signatures and call sites in `fetch-object-info.c`, `fetch-object-info.h`, and `transport.c`—and is explicitly marked as ready to merge.

Junio’s reply confirms the patch now does exactly what it claims: eliminating the misleading abstraction without introducing new functionality. The earlier version had inadvertently mixed in partial "type" support, but this iteration cleanly separates the refactoring from the feature work. The patch paves the way for the architectural redesign in patch 4/9, which replaces `struct object_info` with a more flexible results structure. No behavior changes or open questions remain.