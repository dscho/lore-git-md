This is a refactoring patch that removes the unused `name` parameter from the `chdir_notify` API. The patch is now ready for merging after addressing the only remaining feedback: the commit message now clearly documents the historical context of the parameter's deprecation, tracing its removal across three prior commits. Jeff King has reviewed the updated version and confirmed its correctness, calling the commit message description "nice."

The change is purely mechanical, removing the `name` parameter from `chdir_notify_register()`, `chdir_notify_unregister()`, and the callback type definition, along with updates to all callers in the ODB, refs, setup, and tmp-objdir subsystems. No behavioral changes are introduced, and the existing test suite should verify the working-directory notification mechanism remains functional. Junio C Hamano has also reviewed the historical context and raised no objections.

This is a straightforward cleanup with no compatibility concerns or loose ends, and it looks like a safe, well-justified refactoring.