Thread identity
Subject: [PATCH v4] submodule: handle valueless `submodule.active` config
Series version: v4
Originating author: tilak-raaz
Thread type: bugfix patch series

Core problem or goal
Git crashes when parsing a valueless `submodule.active` configuration entry (e.g., `[submodule] active` with no pathspec). The patch replaces `repo_config_get_string_multi()` with `repo_config_get_value_multi()` to safely handle such entries, emits a warning via `config_error_nonbool()`, and preserves existing caller semantics—malformed entries are skipped after warning, while valid entries continue to be processed.

Current status
Under review; v4 posted. Changes from v3: fixed commit message tense (present instead of past) and terminology ("valueless true"), corrected whitespace in the test script.

Key technical details
Files touched: `submodule.c`, `t/t7400-submodule-basic.sh`
New symbols: none
Behavior change: valueless `submodule.active` entries no longer crash Git; instead, they emit a warning and are skipped.
Test coverage: new test in `t/t7400-submodule-basic.sh` verifies the warning is emitted for valueless `submodule.active`.

Open questions and loose ends
None; the patch addresses the documented `NEEDSWORK` comment in `submodule.c` and includes test coverage.

Participant positions
Author (tilak-raaz): v4 posted, addressing feedback on commit message and test whitespace.
No reviews recorded yet.

Related work
None.