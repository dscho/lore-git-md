This is a refactoring patch that removes the now-unused `name` parameter from the `chdir_notify` API. The patch itself was already complete in v1, but the v2 commit message now thoroughly documents the historical context behind the parameter's obsolescence, tracing its deprecation across three prior commits (`5bf546755c`, `1f43ff2c7e`, and `0de2467e6c`). Jeff King's final reply confirms that the updated commit message satisfies the only remaining feedback from the review.

The patch touches ten files across the ODB, refs, setup, and tmp-objdir subsystems, mechanically removing the `name` argument from `chdir_notify_register()`, `chdir_notify_unregister()`, and the callback type definition. All callers are updated to match, and the unregistration logic is simplified accordingly. There are no behavioral changes, no new symbols, and no compatibility concerns—this is purely a code hygiene cleanup. The existing test suite should verify that the working-directory notification mechanism remains functional.

Both Jeff King and Junio C Hamano have reviewed the patch and confirmed its correctness. With the commit message now fully documenting the historical context, the patch is ready for merging. No further discussion or loose ends remain. The change is uncontroversial and aligns with the project's ongoing efforts to keep the codebase clean and well-documented.