This is the final review exchange in Johannes Schindelin’s eleven-patch series hardening Git against unchecked return values reported by Coverity. Jeff King (Peff) replies to the twelfth patch — which fixes a resource leak in `bisect_run()` when `dup()` or `dup2()` fails — with a brief, substantive review.

Peff confirms the fix is correct and notes two minor stylistic alternatives: omitting the `saved_stdout >= 0` check (since `close(-1)` is a no-op) or splitting the checks for more specific error messages. He concludes that neither refinement is worth pursuing, given the extreme unlikelihood of the error case ever being triggered. The tone is matter-of-fact: the patch is ready, the series is complete, and the discussion can close.