This is a documentation patch. Elijah Newren corrects a misleading short help message for the `-l` option in `git diff`.

The commit updates the one-line description of `-l` (which sets the `rename_limit` option) to clarify that it only affects the *exhaustive* (quadratic-time) portion of rename/copy detection, not the earlier linear-time phases like exact or basename-guided rename detection. The change aligns the short help text with a more detailed documentation update made in 2021 (commit `6623a528e00b`).

The patch is tiny—just one line changed in `diff.c`—and the motivation is clear: prevent users from misunderstanding what the limit actually controls. The commit message explains the history and rationale well, making it easy to see why this is a necessary correction rather than a new feature.

No controversy or follow-up discussion is evident from the patch itself, and the change looks uncontroversial. It’s a straightforward fix for a long-standing inconsistency in the help text.