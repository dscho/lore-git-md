This is the second patch in Justin Tobler’s nine-patch v3 series extending ODB transactions to `git receive-pack`. The patch introduces `odb_transaction_finalize()` and a convenience helper `odb_transaction_commit_and_finalize_or_die()` to split transaction cleanup from object visibility, enabling backend-specific post-commit operations like lockfile removal.

Patrick Steinhardt’s reply is a **surface-level review**: he points out a minor wording nit in the commit message (“step fail” should read “fail”) but otherwise accepts the patch as-is. The change itself is uncontroversial and directly addresses Junio’s earlier feedback about missing `finalize()` calls on non-local exits. All call sites are mechanically updated to use either the new two-step commit/finalize sequence (in `receive-pack`) or the `*_or_die()` helper (everywhere else), with no behavior changes. The patch is well-motivated, clearly explained, and ready for integration.