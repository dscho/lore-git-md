This thread is about a small cleanup patch from Elijah Newren that removes an unused variable assignment (`revs.pretty_given = 1`) in `sequencer.c`. The assignment, introduced in 2017 during a rebase-helper conversion, was never read outside `builtin/log.c` and serves no purpose in the sequencer. Junio C Hamano has already marked the patch for integration in the `next` branch, indicating it’s ready for testing.

Patrick Steinhardt reviewed the patch and confirmed the change is correct—since the sequencer doesn’t interact with `builtin/log.c`, the assignment is indeed pointless. His feedback was brief and surface-level, focusing only on validating the technical rationale without raising any new concerns. The patch remains uncontroversial, with no functional changes or open questions, and is on track to graduate to `master` in the current cycle.