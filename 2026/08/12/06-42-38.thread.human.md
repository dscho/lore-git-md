This thread is a small cleanup patch from Elijah Newren that removes an unused variable assignment in `sequencer.c`. The patch deletes a single line (`revs.pretty_given = 1;`) that was introduced in 2017 during a rebase-helper conversion but has no effect, as the variable is only read in `builtin/log.c` and never referenced in the sequencer. Elijah explains the assignment was likely copied blindly from `git log` logic during the original implementation.

The change is purely mechanical, confined to one file, and doesn’t alter behavior—it simply removes dead code. There’s no controversy or open questions here; it’s a straightforward cleanup with minimal impact. If you’ve been away from the list, this is just one of many small refactoring patches that regularly land to keep the codebase tidy.