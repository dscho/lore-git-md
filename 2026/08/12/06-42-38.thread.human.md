This thread is about a tiny but clean cleanup in Git's sequencer code. Elijah Newren noticed that a single line assigning `revs.pretty_given = 1` in `sequencer.c` has been unused since it was introduced in 2017 during the rebase--helper conversion. The assignment was likely copied blindly from `git log` logic, but since the sequencer never calls any code that reads this field, it serves no purpose.

Junio Hamano has already marked the patch for the `next` branch, meaning it's now cooking in the integration pipeline and will likely graduate to `master` in the next cycle unless issues surface. The change is purely mechanical—just deleting one line—and doesn't affect behavior, on-disk formats, or tests. There's nothing controversial here, and the patch is ready to go.