This is the first patch in a two-patch test fix series (v2) targeting a flaky test in `t/t7900-maintenance.sh`. The patch refactors three tests (`maintenance.auto config option`, `gc.auto config option`, and `maintenance.auto overrides gc.auto`) to use throwaway repositories instead of the default trash directory, making them more self-contained and isolating environment changes.

The refactoring replaces `test_config` with direct `git config set` calls inside subshells, as `test_config` relies on `test_when_finished` cleanup, which doesn’t work in subshells. The throwaway repository’s `rm -rf` cleanup (via `test_when_finished "rm -rf repo"`) ensures proper cleanup at the repository level, making per-config cleanup unnecessary. The commit message now explicitly documents this constraint and the rationale for the mechanical change, addressing feedback from Karthik Nayak on v1.

The patch touches only `t/t7900-maintenance.sh` (43 insertions, 27 deletions) and makes no functional changes to the tests themselves. It prepares the ground for the actual flake fix in patch 2/2 by ensuring environment modifications (like enabling auto-detach) won’t leak into subsequent tests. Key technical details for follow-up discussion include the use of subshells (`(cd repo && ...)`), the replacement of `test_config` with `git config set`, and the cleanup strategy via `test_when_finished "rm -rf repo"`.

This is a test-only refactoring with no user-visible behavior changes. The approach is uncontroversial and has been confirmed as correct by substantive review. The series as a whole addresses a real CI reliability issue (GitLab job #15762975482) caused by a race condition between foreground tests and background maintenance processes.