This is a **design discussion follow-up** in the ongoing thread about unified interactive patch selection in Git. Jeff King (Peff) responds to Johannes Schindelin’s revival of the `--stash` mode patch for `git add -p`, offering a nuanced perspective on the proposal’s technical and philosophical merits.

Peff acknowledges the recurring need for a unified triage workflow (stage/discard/defer) and clarifies that his earlier hesitation about the stash-mode approach—specifically, the "weirdness" of `git add -p` modifying the working tree—was not opposition but a design concern. He suggests that the feature could be implemented without this awkwardness, either by refining the stash-mode approach to avoid unintuitive side effects or by housing it under a separate command (e.g., `git triage -p`). This aligns with Junio’s preference for minimal, reference-like workflows but leaves the door open for a more elegant implementation.

The email also highlights the long history of this discussion, linking to multiple prior threads (including a 2016 proposal) and reinforcing that the stash-mode idea has been repeatedly raised—and rejected—over the years. Peff’s tone is pragmatic: he sees value in the functionality but wants to avoid design quirks that could make the feature feel bolted-on. His suggestion to explore a separate command (rather than overloading `git add -p`) may address concerns about command scope while preserving the core workflow benefit.

For the summarizer brief, note the following likely discussion points:
- **Command scope**: Whether the stash-mode functionality belongs in `git add -p` (as originally proposed) or a new command (e.g., `git triage -p`). The latter could avoid overloading `add -p` but might fragment the workflow.
- **Working tree interaction**: How to handle the "weirdness" of `git add -p` modifying the working tree (via stash). Peff’s preference for a cleaner separation suggests this will be a key technical hurdle.
- **Implementation path**: The stash-mode patch provides a concrete starting point, but Peff’s feedback implies it may need refinement to avoid unintuitive behavior.
- **Historical context**: The thread’s recursive references to prior discussions (2016, 2026) underscore that this is a long-standing friction point, not a new idea. The repeated rejections suggest the project has struggled to find a solution that balances usability with Git’s minimalist philosophy.

Peff’s reply does not take a firm stance on whether the stash-mode approach should be accepted, but his willingness to revisit the design—coupled with his technical suggestions—moves the discussion toward practical next steps. The email also subtly challenges Junio’s "usable but not pretty" framing: if the feature can be implemented cleanly (e.g., via a separate command), it may avoid the "competition with external tools" concern entirely.