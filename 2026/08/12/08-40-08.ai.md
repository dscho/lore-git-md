This is a **feature request follow-up** in the ongoing design discussion about unified interactive patch selection in Git. Johannes Schindelin (Dscho) weighs in to endorse Junio’s proposal for a single-session triage workflow (stage/discard/defer) and uses the thread to revive a previously rejected patch series by Luiz Eduardo Dias Carneiro (`[PATCH] add -p: introduce stash mode`, March 2026). That series aimed to add a `--stash` option to `git add -p`, allowing users to defer hunks by stashing them rather than discarding or staging. Dscho argues that this functionality—though rejected at the time—would directly address the core friction Junio describes (handling "good but not yet ready" hunks) and suggests the project reconsider its earlier decision.

The email links to a GitHub issue (gitgitgadget/git#1828) and a proof-of-concept branch (`dscho:git:add-p-stash-mode`) that implements the stash-mode idea. This provides a concrete starting point for the discussion: rather than designing a new solution from scratch, the thread could evaluate whether the rejected stash-mode approach (or a refined version of it) meets the current requirements. Dscho’s tone is pragmatic—he acknowledges the earlier rejection but frames the revival as a response to a demonstrated need, not a rehash of old arguments.

Key technical details for follow-up:
- The stash-mode patch targeted the `add--interactive` Perl script (or its C rewrite in `add-interactive.c`), adding a `--stash` option to defer hunks by stashing them.
- Deferred hunks would persist in the stash, not the working tree, which sidesteps the open question of how to represent them (persist vs. stash vs. ignore).
- The approach would unify triage actions (stage/stash/discard) in a single session, aligning with Junio’s core goal, but uses the stash as the mechanism for deferral rather than a new in-session state.
- The patch was rejected in March 2026, but the rejection rationale isn’t summarized here. Follow-up discussion may clarify whether the concerns were technical (e.g., stash misuse), workflow-related (e.g., stash clutter), or philosophical (e.g., overloading `add -p`).

Dscho’s proposal tests Junio’s latest stance on minimalism: the stash-mode approach is a lightweight extension of `git add -p` (no new UI, no dependencies) but introduces a new concept (deferral via stash). Whether this crosses the line into "competing with external tools" or remains a "usable but not pretty" reference implementation will likely be a point of debate. The email also implicitly challenges Patrick Steinhardt’s refined proposal for *basic* UI improvements (e.g., navigable hunk selection): if the stash-mode approach is sufficient, the need for even minimal UI enhancements may be reduced.

For the summarizer brief, note the following likely discussion points:
- **Mechanism for deferral**: Stash (Dscho’s proposal) vs. working tree persistence (Junio’s original idea) vs. temporary in-session state.
- **Backwards compatibility**: Whether stash-mode would clutter the stash or interfere with existing workflows.
- **Rejection rationale**: Why the March 2026 patch was shot down, and whether those concerns still apply.
- **Alignment with Junio’s philosophy**: Does stash-mode stay within the bounds of a "minimal, reference-like feature," or does it risk overloading `add -p`?
- **Interaction with external tools**: Would stash-mode make it easier or harder for tools like lazygit to consume Git’s interactive machinery? Stefan Haller’s API-focused proposal may weigh in here.