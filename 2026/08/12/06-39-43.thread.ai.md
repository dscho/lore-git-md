Here is the updated AI thread summary:

---

Thread identity: Bugfix for NULL-dereference in Git protocol v2 `promisor-remote` capability handling. Single-patch series (no versioning). Originating author: Elijah Newren.

Core problem or goal: Fix a NULL-dereference bug in the `promisor_remote_receive()` callback in `serve.c`, introduced in commit d460267613da (February 2025). The bug occurs when a client sends the `promisor-remote` capability without an `=` or value, causing `get_capability()` to return NULL, which is then dereferenced. The crash location shifted from `strbuf_split_str()` to `string_list_split()` due to a later refactoring (68a746e9a8), but the fix remains valid.

Current status: Patch posted as a standalone bugfix. No prior versions. Under review. Christian Couder, author of the original bug-introducing commit, has reviewed the patch and confirmed its correctness. The fix is now effectively acked by a key stakeholder (Christian), increasing its likelihood of being merged without further revision.

Key technical details:
- Files touched: `serve.c` (bugfix), `t/t5701-git-serve.sh` (new test case).
- Subsystem: Git protocol v2, promisor-remote capability.
- New behavior: Server now rejects a valueless `promisor-remote` capability with an error instead of crashing.
- Test coverage: New test verifies the server's error response to a malformed capability.
- Refactoring context: The crash location changed from `strbuf_split_str()` to `string_list_split()` due to commit 68a746e9a8, but the fix remains applicable.

Open questions and loose ends: None identified. The patch is minimal and addresses the issue directly.

Participant positions:
- **Christian Couder**: Substantive review. Confirmed the bug's current manifestation (crash in `string_list_split()` rather than `strbuf_split_str()`), verified the fix and test as correct, and effectively acked the patch. His review carries high weight due to his authorship of the original bug-introducing commit and domain knowledge of the promisor-remote subsystem.

Related work: None.

---