This is a **security-adjacent configuration patch** that introduces `http.sslVerifyHost`, a Git config option allowing users to disable TLS hostname verification while retaining certificate verification. The patch targets decentralized environments where self-signed certificates lack valid Common Names (CNs) or subjectAltNames, but users still want to pin a specific CA certificate via `http.sslCAInfo`.

Patrick Steinhardt’s review evolves from initial skepticism to cautious acceptance after Ron Nazarov clarifies the intended use case: the option is meant to be used *only* in combination with certificate pinning (`http.sslCAInfo`), not as a standalone feature. This mirrors SSH’s security model, where users verify a server’s fingerprint in `known_hosts`. The example configuration—pinning a single self-signed certificate and disabling the default CA path—demonstrates how the option can provide security guarantees comparable to SSH, provided users follow the correct setup.

The key shift in this email is Patrick’s acknowledgment that the feature can be secure *if* the documentation clearly explains the requirements. He now advocates for updating the documentation to emphasize that `sslVerifyHost` must be paired with certificate pinning to avoid creating a false sense of security. This addresses his earlier concern that users might misconfigure the option, leaving them vulnerable to man-in-the-middle (MITM) attacks. The discussion has moved from "should this exist?" to "how can we make this safe to use?"

The patch remains **controversial but viable**. The security model is now better defined, but the burden is on the documentation to prevent misuse. No implementation details (e.g., default value, test coverage) are discussed here, so the patch’s readiness is still unclear. Future discussion will likely focus on refining the documentation and ensuring the commit message adequately warns users about the risks of misconfiguration.

This is part of an **ongoing RFC thread** with no prior versions or merge status. The patch touches the HTTP/HTTPS transport layer, specifically the TLS verification logic, and could appeal to users in decentralized or private Git server setups. However, its acceptance hinges on whether the project deems the security trade-offs justified and the documentation sufficiently clear.