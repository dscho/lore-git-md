This is a surface-level review of patch 6/7 in the `git repack --drop-filtered` series. Christian Couder comments exclusively on code comment capitalization, punctuation, and grammar in both the C source (`builtin/repack.c`) and the test script (`t/t7706-repack-drop-filtered.sh`). Specific suggestions include capitalizing the first word of comments ("Refuse" instead of "refuse", "Bare" instead of "bare"), fixing a typo ("Create" instead of "creat"), and noting that the error message for ongoing operations could potentially reuse an existing helper (`die_if_some_operation_in_progress()` from `builtin/checkout.c`) to improve clarity. None of these remarks engage with the patch’s logic, safety guards, or integration with the rest of the series; they are purely stylistic and mechanical. The review does not affect the patch’s readiness or functionality.