This is a **design discussion follow-up** in the unified interactive patch selection thread. Junio C Hamano briefly replies to Jeff King (Peff) to clarify his stance on the stash-mode proposal for `git add -p`.

Junio acknowledges Peff’s point that the "weirdness" of `git add -p` modifying the working tree (via stash) is not a dealbreaker—it’s a design concern, not an outright rejection. He reaffirms his support for a unified triage workflow (stage/stash/keep/discard) but does not revisit the implementation path (e.g., separate command vs. `add -p` extension). The email is minimal, serving mostly to align Junio and Peff on the core goal: the feature is worth pursuing, and the "weirdness" is a solvable problem rather than a fundamental flaw.

For the summarizer brief, note that this reply does not advance the technical discussion—it merely confirms consensus on the *desirability* of the feature. The open questions from prior emails (command scope, stash integration, UI considerations) remain unresolved. The thread’s next steps will likely hinge on whether the stash-mode functionality is implemented as a new command (e.g., `git triage -p`) or as an extension to `git add -p`, with Peff’s suggestion of a separate command gaining traction as a way to avoid overloading existing workflows.