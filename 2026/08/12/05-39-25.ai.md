This is the fourth patch in Patrick Steinhardt’s ODB refactoring series, addressing a mechanical cleanup after the architectural shift in patch 2. The email is a reply to Justin Tobler’s clarifying question about why the `GIT_ALTERNATE_OBJECT_DIRECTORIES` environment variable no longer needs to be passed when flushing caches via `odb_prepare(o, ODB_PREPARE_FLUSH_CACHES)`. Patrick confirms that alternates are fully initialized during `odb_new()` and that subsequent calls to `odb_prepare_alternates()` only re-read the alternates *file*, not the environment variable. He plans to add a comment to clarify this behavior.

The exchange resolves the only substantive question raised about this patch, leaving no open technical concerns. The patch itself is a straightforward cleanup (removing 7 lines, adding 2) that aligns with the series’ goal of simplifying the ODB codebase by centralizing alternate initialization. The discussion confirms the patch’s correctness and reinforces the series’ design rationale: eager loading of alternates during ODB setup eliminates the need for scattered, redundant initialization calls. No performance or edge-case implications are revisited here, and the patch remains uncontroversial. The addition of a clarifying comment will address Justin’s request for documentation, making the change more maintainable.