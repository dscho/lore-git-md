This is the final clarification in the `--drop-filtered` repack series, now confirmed feature-complete. Siddharth Shrimali (a first-time contributor) explicitly states that the three deferred items—remote verification, recency-based culling, and the drop log—are follow-up work, not blocking requirements for this series. The core functionality (reclaiming space in partial clones by dropping large promisor blobs) is production-ready and has been stable since v3, with v4 incorporating only Junio’s mechanical fixes (SYNOPSIS cleanup, usage string formatting, and test script style).

The series adds two new `git repack` options: `--drop-filtered` (real runs) and `--dry-run` (preview), documented in `Documentation/git-repack.adoc` with clear preconditions (`-a` required, promisor remote required, bitmaps incompatible) and safety optimizations (merge/rebase/cherry-pick checks, index validation). The implementation is robust: promisor objects are enumerated via `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`, filtered by size using the new `list_objects_filter__filter_oidset()` helper, and dropped by rebuilding the promisor pack while excluding them. Crash safety is guaranteed by the repack machinery’s existing write-before-delete and fsync guarantees.

Junio’s latest feedback confirms the mechanical fixes match his local changes, and the author has acknowledged the roadmap for follow-ups. The series is now ready for integration; the only remaining steps are standard due diligence (full test suite runs and trial merges into `next` and `seen`), which the author intends to perform. No maintainer decision on the integration branch has been announced yet. The patches are well-structured, thoroughly tested, and address a real need for partial-clone users. Worth watching for merge readiness.