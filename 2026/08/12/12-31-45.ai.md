This is a bug report about unexpected pseudo-commits appearing in the output of a custom `git lg` alias. The user observes four spurious commits—three labeled "Notes added by 'git notes append' - rewrite-analytics" and one "chatter: initialize notes ref - chatter"—that appear unconnected at the top of the graph log. These commits are not visible in other Git GUI tools (Sublime Merge, IntelliJ IDEA) and exhibit inconsistent behavior, sometimes disappearing or reappearing after resets or new commits. The issue occurs in a tmux/mate-terminal environment with Git 2.55.0, which includes Rust support and fsmonitor--daemon.

The report suggests the pseudo-commits are generated by Git components (`rewrite-analytics` and `chatter`), likely related to notes or hooks, but the user’s enabled hooks list is empty. The custom `git lg` alias uses `--all`, which includes all refs (including notes), but the user expects only project commits. The discrepancy between CLI and GUI tools implies the issue may stem from how the alias formats or filters refs, or how notes are being appended in the background by some tooling.

This is not a patch or review, but a clear bug report with reproduction steps, system details, and observed vs. expected behavior. The issue appears to be cosmetic rather than functional, but it’s disruptive for users relying on custom log aliases. The report is well-structured and actionable for someone familiar with Git’s notes or ref-handling internals.