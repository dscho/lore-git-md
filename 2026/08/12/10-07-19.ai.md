This is a **test fix follow-up** in the ongoing series to resolve a flaky test in `t/t7900-maintenance.sh`. The email is Patrick Steinhardt’s reply to Karthik Nayak’s review of patch 1/2, clarifying the mechanical change from `test_config` to `git config set` inside throwaway repositories.

Patrick explains that the switch was forced by `test_config`’s reliance on `test_when_finished`, which cannot run in subshells. Since the refactored tests now use subshells (`(cd repo && ...)`) to scope environment changes, `git config set` is the only viable option. The throwaway repository (`git init repo`) ensures cleanup is still handled by `test_when_finished "rm -rf repo"` at the test level, so the lack of per-config cleanup is not a concern. This technical constraint resolves Karthik’s minor nit about documenting the change in the commit message.

The series remains on track: patch 1/2 is a refactoring that isolates tests in throwaway repositories, and patch 2/2 (already reviewed) fixes the flake by restoring default auto-detach behavior. No further discussion points are open; the only remaining action is Patrick’s discretionary update to the commit message for clarity. The fix targets a recurring CI reliability issue in GitLab and is uncontroversial.