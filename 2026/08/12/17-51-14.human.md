This is the documentation patch for the final piece of Siddharth Shrimali’s seven-patch series introducing `git repack --drop-filtered` for partial clones. The series is now complete and production-ready, with all feedback from earlier rounds incorporated. This specific patch adds 37 lines to `Documentation/git-repack.adoc`, fully documenting the new `--drop-filtered` and `--dry-run` options.

Christian Couder’s reply suggests a minor reorganization: most of the documentation could be squashed into patch 5/7 (the patch that implements the actual object removal), while the paragraph describing the safety guards (merge/rebase checks and index validation) could move to patch 6/7 (the patch that adds those guards). This would keep the documentation closer to the code it describes without changing the substance. The suggestion is purely mechanical and does not affect the feature’s functionality or completeness.

The documentation itself is thorough and clear, covering preconditions, incompatibilities, implied behavior (`-d`), and the rationale behind the safety guards. It aligns with the series’ reframing of those guards as UX optimizations rather than correctness measures, since lazy-fetching would transparently restore any dropped objects. The patch is ready for integration and does not introduce new technical debt. If the maintainer accepts Christian’s suggestion, the change would be trivial to implement before merging.