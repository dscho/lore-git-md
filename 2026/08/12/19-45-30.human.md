This is a reply in the ongoing `git repack --drop-filtered` series, specifically addressing feedback on patch 6/7. The author, Siddharth Shrimali, responds to Christian Couder’s suggestion about reusing an existing helper function (`die_if_some_operation_in_progress()`) for the error message when `--drop-filtered` is used during an ongoing operation (merge, rebase, etc.).

Shrimali acknowledges the conceptual fit but notes the helper is currently `static` in `builtin/checkout.c` and tied to `the_repository`, while the repack guard operates on an explicit `struct repository *`. Extracting the helper to a shared location (e.g., `wt-status.c`) and making it non-static would require a separate effort, which he proposes to defer as follow-up work. For the upcoming v5, he plans to keep the explicit repo-based check but improve the error message. This is a pragmatic compromise: it addresses the immediate feedback without derailing the series, while leaving the door open for future code deduplication.

The exchange is purely mechanical and does not affect the patch’s core logic or safety guarantees. It’s a minor UX tweak that could slightly improve error message consistency across Git commands. The author’s response is clear and reasonable, and the follow-up plan is well-scoped. No controversy or deeper technical debate here—just a small polish item for v5.