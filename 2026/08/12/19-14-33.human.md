This is the procedural wrap-up for the `paint_down_to_common()` optimization series. The thread is now closed: Junio Hamano and Kristofer Karlsson confirm the series is technically complete, fully reviewed, and ready to advance to the `next` integration branch for final testing.

The series delivers a **100–1000× speedup** for asymmetric merge-base queries (e.g., 2.1 M → 3 steps in a 2.6 M-commit repository) by terminating early when one side’s commit queue is exhausted. It touches the commit-reach subsystem (`commit-reach.c`, `commit-graph.c`), adds deterministic Trace2 instrumentation, and includes thorough test coverage for edge cases (clock skew, mixed-generation commits, diamond topologies). All prior review feedback has been incorporated, and the series is procedurally unblocked. No further discussion or patches are expected; it is now queued for integration into `next`.