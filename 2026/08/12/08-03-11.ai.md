This is the third patch in Johannes Schindelin’s v3 series fixing unchecked return values reported by Coverity. It targets the reftable backend, specifically the `reftable_writer_new()` constructor in `reftable/writer.c`.

The patch addresses a latent bug where `reftable_writer_new()` ignored the return value of `writer_reinit_block_writer()`, potentially returning a partially initialized `reftable_writer` object even if block-writer initialization failed. The fix propagates the error upward and releases the constructor’s allocations (`wp->block` and `wp` itself) if initialization fails, ensuring no unusable writer object is returned. This change is purely internal to the reftable backend and does not affect public APIs or on-disk formats.

The commit message credits GPT-5.6 Sol and Claude Opus 4.8 for identifying the issue, a detail likely to be referenced in future discussion about AI-assisted static analysis. The diff is minimal (7 lines added, 1 removed) and follows the established pattern of cleaning up resources in error paths. The patch is uncontroversial and directly resolves the memory leak identified in v2 of the series. Future discussion may reference the function name `reftable_writer_new` or the file `reftable/writer.c`.