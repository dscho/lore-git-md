This is a small, uncontroversial refactoring patch. Junio has marked it for inclusion in `next`, meaning it will soon be cooking alongside other topics for integration testing.

The patch removes a single line in `sequencer.c` that sets `revs.pretty_given = 1`. Elijah explains the assignment has been unused since it was introduced in 2017 during the rebase--helper conversion: the field is only read in `builtin/log.c`, and no code from that file is called from the sequencer. The commit message speculates the line was copied blindly from `git log` logic when the original implementation was based on `git rev-list`.

No behavior changes, no new tests, and no on-disk format impact. The patch is a clean dead-code removal that looks correct and well-motivated. It’s a minor cleanup, but precisely the kind of tidying that keeps the codebase maintainable.