This is a small refactoring patch in the sequencer subsystem. Junio C Hamano has marked the topic for `next`, indicating it will be cooking in the integration branch for further testing.

The patch removes a single line (`revs.pretty_given = 1;`) in `sequencer.c` that was introduced in 2017 during the rebase--helper conversion. The assignment is unused because `revs.pretty_given` is only read in `builtin/log.c`, and no code from that file is called from the sequencer. Elijah Newren’s commit message speculates the line was blindly copied from `git log` logic during the original implementation.

The change is purely mechanical—no behavior, on-disk format, or test coverage is affected. Junio’s reply confirms the patch is uncontroversial and ready for integration. The patch itself is minimal, touching only one file and deleting one line, and the rationale is clear and well-supported by the codebase. This is a straightforward cleanup with no loose ends or open questions.