Thread identity
Subject: Unexpected pseudo-commits in custom `git lg` alias output
Series version: N/A (bug report)
Originating author: Marcel Svitalský
Thread type: bug report

Core problem or goal
A user observed four spurious pseudo-commits—three labeled “Notes added by 'git notes append' - rewrite-analytics” and one “chatter: initialize notes ref - chatter”—appearing at the top of the graph produced by a custom `git lg` alias (`git log --graph --all --oneline --decorate`). These commits were invisible in Sublime Merge and IntelliJ IDEA, appeared inconsistently after resets or new commits, and were believed to originate from Git’s notes or hook machinery despite the user’s enabled hooks list being empty. The goal was to understand why these commits surfaced only in the CLI alias and how to suppress them while retaining the alias’s intended project-commit output.

Current status
Resolved. The root cause was identified as the `--all` flag in the alias, which includes all refs (including Git notes `refs/notes/rewrite-analytics` and `refs/notes/chatter`), forming a disconnected graph. This is not a bug but an expected consequence of `--all`. Marcel Svitalský confirmed that D. Ben Knoble’s proposed workarounds—replacing `--all` with `--branches --tags --remotes` or using `--exclude='refs/notes/*' --all`—successfully suppress the spurious notes refs. The notes’ appearance was attributed to background tooling (e.g., lazygit or IntelliJ IDEA) and deemed environment-specific rather than a Git regression. No maintainer action was requested or expected.

Key technical details
- Command: `git log --graph --all --oneline --decorate` (custom alias `git lg`)
- Spurious refs: `refs/notes/rewrite-analytics` and `refs/notes/chatter` (legitimate Git notes, likely generated by background tooling such as `rewrite-analytics`, `chatter`, or IDE integrations)
- Environment: tmux + mate-terminal, Git 2.55.0 (with Rust support and fsmonitor--daemon)
- Discrepancy: CLI showed pseudo-commits due to `--all`; GUI tools (Sublime Merge, IntelliJ IDEA) likely filter refs by default (e.g., omitting `--all` or applying implicit exclusions)
- Hooks: user’s enabled hooks list was empty; notes were appended by Git components, not user-configured hooks
- On-disk: no format changes; issue was purely cosmetic in log output
- Workarounds: `--branches --tags --remotes` (narrower scope) or `--exclude='refs/notes/*' --all` (preserves `--all` coverage while filtering notes)

Open questions and loose ends
None remaining. The user’s usability concern was addressed, and the thread is effectively closed. The source of the notes (`rewrite-analytics` or `chatter`) was not definitively identified but was attributed to environment-specific tooling (e.g., lazygit or IntelliJ IDEA). The behavioral discrepancy between CLI and GUI tools was explained by default ref filtering in the GUIs.

Participant positions
- Marcel Svitalský (reporter): Initially described the issue, provided reproduction steps, and system details. Acknowledged the root cause (`--all`) and confirmed the workarounds resolved the issue. Position evolved from observational to solution-oriented, closing the thread as resolved.
- Kristoffer Haugsbakk: Surface-level review identifying the root cause (`--all`). Noted the pseudo-commits were legitimate Git notes and not a bug. Did not propose solutions or engage with broader questions.
- D. Ben Knoble: Substantive technical review addressing the user’s questions about workarounds. Confirmed Git notes are not new and suggested the notes may have appeared due to environment-specific tooling. Proposed two practical workarounds (`--branches --tags --remotes` or `--exclude='refs/notes/*' --all`), which the user adopted.

Related work
None directly mentioned. The notes’ appearance may relate to background tooling (e.g., `rewrite-analytics`, `chatter`, or IDE integrations like lazygit/IntelliJ IDEA) or Git’s notes machinery. The thread serves as a precedent for users encountering similar cosmetic log output issues due to `--all`.