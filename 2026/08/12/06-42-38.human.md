This is a small refactoring patch from Elijah Newren. It removes a single line in `sequencer.c` that sets `revs.pretty_given = 1`, a variable that is only ever read in `builtin/log.c` and has no effect in the sequencer context.

The commit message explains that the line was likely copied from `builtin/log.c` during the 2017 rebase--helper conversion but serves no purpose in `sequencer.c`. Elijah notes he spotted this years ago while investigating revision field parsing and is now submitting it as a cleanup. The change is straightforward and uncontroversial—just removing dead code. No behavior change is expected, and the patch touches only one line in one file.