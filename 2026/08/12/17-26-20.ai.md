This is a brief follow-up in the resolved bug report thread about unexpected pseudo-commits in a custom `git lg` alias. The user, Marcel Svitalský, acknowledges Junio C Hamano’s suggestion of using `--branches`, `--tags`, and `--remotes` as narrower alternatives to `--all` but confirms he prefers the `--exclude='refs/notes/*' --all` workaround proposed earlier by D. Ben Knoble. This choice preserves the inclusion of stashes in the log output while filtering out the spurious notes refs.

The email is purely confirmatory and adds no new technical details. It closes the loop on the user’s preference for handling the cosmetic issue, reinforcing that the thread’s resolution remains intact. The workaround is now explicitly tied to the user’s specific use case (retaining stashes), which may help future readers with similar needs. No further discussion or maintainer action is expected.