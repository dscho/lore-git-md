This is a **bugfix patch follow-up** in the ongoing `--shallow-file` NULL-dereference thread. Christian Couder responds to Patrick Steinhardt’s stylistic suggestion by pointing out that the current patch deliberately mirrors the error-reporting pattern used by all other similar options in `git.c` (`--git-dir`, `--namespace`, `-C`, etc.). He argues that consistency with those options is more important than condensing the error message and `usage()` call into a single line, and suggests that any such stylistic change should either be part of this series (as an additional patch) or handled separately. The exchange leaves the stylistic question open but reaffirms the patch’s core correctness and alignment with existing code. No new technical concerns are raised; the discussion remains focused on presentation and process.