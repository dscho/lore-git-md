This is a follow-up in a bug report thread about unexpected pseudo-commits appearing in a custom `git lg` alias. The original issue was identified as an expected consequence of using `--all`, which includes Git notes refs like `refs/notes/rewrite-analytics` and `refs/notes/chatter`. Marcel Svitalský now asks two questions: whether this behavior is new (he’s never seen it before despite using the alias for years), and whether there’s an alternative to `--all` that would exclude notes while still including all project commits.

The thread remains under review, with no maintainer decision or proposed solution yet. Marcel’s questions suggest the notes may have been introduced recently by background tooling (e.g., `rewrite-analytics` or `chatter`), though the commit message doesn’t speculate on the source. His request for a `--all`-like flag that excludes notes is practical but unanswered—Git’s `log` command doesn’t offer a direct equivalent, though `--branches --tags --remotes` might approximate the desired output for most users. The discrepancy between CLI and GUI tools (which don’t show the notes) likely stems from default ref filtering in the GUIs, but this hasn’t been confirmed.

The exchange is straightforward and user-focused, with no technical debate or deeper investigation into the notes’ origin. If you’re curious about suppressing notes in your own aliases, this thread is worth watching for potential workarounds, but it’s not yet a patch or feature request—just a clarification of existing behavior.