This is a **design discussion follow-up** in the ongoing thread about unified interactive patch selection in Git. Junio C Hamano responds to Johannes Schindelin’s revival of the stash-mode patch, clarifying his vision for a minimal, reference-like solution that unifies triage actions (stage/discard/defer) into a single command session.

Junio agrees that the core functionality—classifying changes into three (or four, if stashing is included) categories—is valuable but reiterates his long-standing objection to overloading `git add -p` with working-tree modifications. His concern is architectural: `git add -p` is about staging changes, not discarding or deferring them, and shoehorning those actions into it would muddy the command’s purpose. Instead, he suggests that the solution should be a *new* command (or a new mode for an existing command) that explicitly handles all three (or four) actions without forcing users to switch between `git add -p` and `git checkout -p`.

The email introduces a subtle but important shift: Junio now explicitly considers stashing as a fourth category, expanding the original triage model (stage/keep/discard) to include "stash away." This aligns with Dscho’s stash-mode proposal but reframes it as part of a broader design rather than an extension of `git add -p`. Junio’s phrasing—"it might be beneficial to widen our horizon to also consider if it would help us to include stash into the mix"—suggests openness to the idea, provided it is implemented cleanly (e.g., not as an `add -p` flag).

The discussion remains conceptual, with no concrete implementation sketched. Junio’s focus is on the *mechanism* (a single command session for triage) rather than the *presentation* (UI or flags). His stance rules out larger UI efforts (e.g., a TUI) but leaves room for incremental workflow fixes, provided they avoid overloading existing commands. The email does not address Peff’s suggestion of a separate command (e.g., `git triage -p`), but the two ideas are compatible: a new command could serve as the clean implementation path Junio seeks.

For readers deciding whether to follow this thread, the key takeaway is that the discussion is converging on a minimalist solution—likely a new command or mode—that unifies triage actions without competing with external tools. The stash-mode idea is back in play, but its fate hinges on whether it can be implemented without violating Git’s command boundaries. Junio’s reply signals that the project is open to revisiting the rejected patch, provided the design addresses his architectural concerns. The next steps may involve sketching a concrete proposal (e.g., a new command or a refactored `add--interactive` mode) or evaluating Dscho’s proof-of-concept branch against Junio’s criteria.