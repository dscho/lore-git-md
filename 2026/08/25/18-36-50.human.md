This is the author's response to Junio's review of the third patch in the `--[no-]range-diff-notes` series. Kristoffer acknowledges Junio's usability concern: under the current implementation, `--no-notes --range-diff-notes` suppresses notes in *both* the patches and the range-diff, because `--range-diff-notes` inherits the patch notes setting rather than overriding it. He explains that this happens because the notes list is cleared by `--no-notes`, leaving nothing for `--range-diff-notes` to inherit.

Kristoffer briefly sketches a possible technical solution—maintaining a separate "shadow list" of notes for range-diffs—but also flags a conceptual issue: since `format-patch` does not show notes by default, it is unclear what `--range-diff-notes` should display when no notes are explicitly configured. The response is concise and pragmatic, but leaves the design question open for further discussion. The thread is now at a decision point: whether to adjust the option semantics to match user expectations or to document the current behavior as intentional.