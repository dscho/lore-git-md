Thread identity
Subject: [PATCH 0/4] worktree add: fix basename handling
Series version: v1 (initial)
Originating author: René Scharfe
Thread type: patch series

Core problem or goal
Fix the `worktree_basename()` helper in `git worktree add` to prevent data corruption (out-of-bounds read), reject malformed paths, handle trailing slashes consistently, and simplify the code by returning a string copy. The series supersedes an earlier stalled effort (linked in the cover letter) and addresses a real-world issue reported in Git for Windows (issue #6346).

Current status
Under review; no prior version of this series exists. Four patches posted, targeting `builtin/worktree.c` and adding tests in `t/t2400-worktree-add.sh`. The first patch (this email) is the most critical, as it fixes the out-of-bounds read that could corrupt data.

Key technical details
- Files: `builtin/worktree.c`, `t/t2400-worktree-add.sh`
- Symbols: `worktree_basename()`
- Behavior changes:
  • Patch 1 (this email): fixes out-of-bounds read by adding a bounds check before pointer decrement, preventing corruption; simplifies loop logic by removing redundant `name++` adjustment. Root cause attributed to Matthias Aßhauer (Git for Windows issue #6346).
  • Patch 2: rejects paths consisting solely of separators
  • Patch 3: normalizes trailing-slash handling
  • Patch 4: returns a string copy, simplifying memory management
- Test coverage: new tests in `t/t2400-worktree-add.sh` for edge cases (trailing slashes, separator-only paths)

Open questions and loose ends
- Whether the series fully replaces the earlier stalled effort or if further reconciliation is needed
- Whether reviewers will request additional edge-case coverage beyond the current test additions
- Whether the bounds-check fix in Patch 1 is sufficient or if reviewers will suggest alternative approaches (e.g., input validation earlier in the call chain)

Participant positions
- René Scharfe (author): proposes the series as a self-contained fix and cleanup; attributes the bounds-check fix to Matthias Aßhauer (Git for Windows)

Related work
- Earlier stalled series (linked in cover letter) addressing similar issues in `worktree_basename()`
- Git for Windows issue #6346 (real-world report motivating the fix)