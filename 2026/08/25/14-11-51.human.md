This is a refactoring patch in Patrick Steinhardt’s 8-patch series to simplify ODB alternates handling. It moves the `setup_reference()` function in `builtin/clone.c` further down in the file, closer to `copy_alternates()`, to prepare for a later patch where `setup_reference()` will call `copy_alternates()`. The change is purely mechanical—no logic is altered, just the function’s location—so it should not introduce regressions.

The patch is part of the broader ODB abstraction effort, which aims to make alternates an implementation detail of the ODB backend. By moving `setup_reference()`, Patrick ensures the code is organized for the upcoming change where alternates are written during repository creation rather than after. This is a small but necessary step in the series, and the commit message clearly explains the motivation.

Reviewers are unlikely to find much to critique here, as the patch is straightforward. The real discussion will likely focus on later patches in the series, where the `write_alternates()` callback is removed and alternates handling is consolidated. For now, this patch is a low-risk preparation step.