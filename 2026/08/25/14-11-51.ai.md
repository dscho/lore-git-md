This is the second patch in Patrick Steinhardt’s 8-patch series refactoring ODB alternates handling during repository creation. It is a preparatory refactoring in `builtin/clone.c` that moves the `setup_reference()` function later in the file to avoid adding a forward declaration when a subsequent patch makes it call `copy_alternates()`.

The change is purely mechanical: the function body is relocated without modification, and the diff shows only reordering. This patch does not alter behavior or introduce new logic; its sole purpose is to set up the codebase for the next patch in the series, which will make `setup_reference()` call `copy_alternates()`. The motivation is clear from the commit message and thread context: the series aims to consolidate alternates handling at repository creation time, simplifying the ODB interface.

No new symbols or files are introduced, and the patch does not touch any public APIs. The impact is confined to `builtin/clone.c`, and the change is unlikely to introduce regressions since it only reorders existing code. Reviewers are likely to treat this as a routine preparatory step, with substantive feedback reserved for later patches that modify behavior. The patch is well-explained and aligns with the series’ broader goal of making alternates an ODB backend implementation detail.