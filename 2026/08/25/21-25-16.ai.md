This is the first patch in a two-patch feature series that protects local branches from accidental deletion when they are upstream dependencies of other branches. This patch is a pure refactoring: it moves the `collect_stacked_branch_base` and `protect_stacked_branch_bases` helper functions (along with their supporting `stacked_branch_data` struct) earlier in `builtin/branch.c` so that `delete_branches()` can call them without requiring forward declarations.

The patch touches only `builtin/branch.c` and makes no behavioral changes. The diff is a straightforward code motion: the two functions and their struct are relocated from their original position (around line 718) to just after the `enum delete_branch_flags` definition (around line 203). The implementation remains identical; only the order of definitions changes.

This refactoring is preparation for the second patch, which will use these helpers to implement the new protection logic. The commit message is concise and accurately describes the purpose ("so delete_branches() can use them without a forward declaration"). The patch is clean, with no extraneous changes, and the diff is easy to verify as a pure move.

The patch is well-motivated: the refactoring is necessary for the behavioral change in the next patch, and the separation keeps the series logically organized. There are no open questions or loose ends in this patch itself. The only context needed to understand it is that it is part of a larger effort to protect upstream branches from accidental deletion.