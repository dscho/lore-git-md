Thread identity: Refactoring ODB alternates handling during repository creation. 8-patch series (`[PATCH 0/8]`) by Patrick Steinhardt, part of the ongoing ODB abstraction effort. Thread type: patch series.

Core problem or goal: Remove the ability to write ODB alternates after repository creation, simplifying the ODB interface. Currently, alternates can be added at any time via `odb_source_write_alternate()`, but Git only uses this during initial setup (e.g., `git clone --reference`). The series introduces a new option in `odb_source_create_on_disk()` to handle alternates at creation time, eliminating the need for a separate `write_alternates()` callback. This change prepares for alternates to become an implementation detail of the ODB backend, giving backends more control over their configuration.

Current status: Under review. No prior version recorded in this summary. The series is built on `ps/odb-eagerly-load-alternates`, indicating it follows recent ODB-related work. No merge status reported.

Key technical details:
- Files touched: `builtin/clone.c` (refactoring reference handling in patches 1-4: patches 1-3 reordered `setup_reference()` to defer ODB setup, introduced `INIT_DB_SKIP_ODB` and exposed `create_object_database()`, and renamed `setup_reference()` to `collect_alternates()`; patch 4 moves shared local clone alternates logic into `collect_alternates()` and adds `src_repo` and `is_local` parameters), `setup.c`/`setup.h` (adding `INIT_DB_SKIP_ODB` flag and exposing `create_object_database()`), and `odb/source-files.c` (later patches will add support for writing alternates during creation).
- New or renamed symbols: `INIT_DB_SKIP_ODB` flag added to `init_db()`; `create_object_database()` made public; `collect_alternates()` (renamed from `setup_reference()`, now with `src_repo` and `is_local` parameters), `add_one_alternate()` (replaces `add_one_reference()`), and `struct add_one_alternate_data` (tracks whether an alternate is required/optional and holds the `strvec` of paths). The `write_alternates()` callback will be removed in later patches.
- Subsystems: ODB (object database), alternates handling, repository creation.
- Old-vs-new behavior: No user-visible changes; the series defers ODB creation in `git clone` until after the clone source URI is resolved, ensuring alternates are written with full context. Patches 1-4 are purely mechanical and do not alter behavior.
- Test coverage: Updated to verify new behavior; no regressions expected in clone or alternates handling.

Open questions and loose ends: None raised yet. Reviewers are expected to focus on ensuring the deferred ODB creation and alternates collection don’t introduce edge-case regressions (e.g., in `git clone --reference` or `--shared`, URI rewriting via `insteadOf`, or handling of required/optional alternates). The mechanical nature of the first four patches may limit substantive feedback, but later patches (removing `write_alternates()`) are expected to draw more scrutiny.

Participant positions: None recorded yet. The series is likely to attract review from ODB-focused contributors (e.g., Justin Tobler, Karthik Nayak) and those involved in the broader ODB abstraction effort. The first four patches’ mechanical nature may limit substantive feedback, but the later patches (particularly those removing `write_alternates()`) are expected to draw more detailed review.

Related work: Part of Patrick Steinhardt’s ODB abstraction effort; built on `ps/odb-eagerly-load-alternates`. The series is foundational for moving alternates handling into the "files" backend, aligning with the goal of making alternates an ODB backend implementation detail.