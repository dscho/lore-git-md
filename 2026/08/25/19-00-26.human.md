This is the first patch in a four-part bugfix series addressing a critical race condition in Git's geometric repacking mechanism. The patch prevents a SIGSEGV in `git replay` when merge inputs are unreadable by explicitly handling the case where `result->clean < 0`. The fix is small and surgical: it adds a check in `pick_regular_commit()` to return early with an error message when the merge machinery fails to read objects, avoiding a NULL pointer dereference on `result->tree`.

The test coverage is thorough, simulating the crash scenario by removing the tree object for the `--onto` commit and verifying that `git replay` fails gracefully with an appropriate error message. The test also confirms that the merge machinery's existing error messages ("Could not read", "collecting merge info failed") are preserved, so users get clear feedback about what went wrong.

This patch is a clear bugfix with no downsides. The crash it prevents is a real issue that affects server-side operations like `git replay`, and the fix is minimal and well-motivated. The test is realistic and covers the edge case without being overly complex. Junio has already reviewed this patch substantively and endorsed the fix, so it looks ready to merge. If you care about stability in geometric repacking or use `git replay` in production, this patch is worth your attention.