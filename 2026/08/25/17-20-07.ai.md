**Summarizer brief mode**

This is a **policy clarification reply** in the `git history squash` series, specifically addressing the final unresolved technical question: the autosquash marker resolution policy for OID case sensitivity in Patch 7/8.

Junio C Hamano **reaffirms the project’s direction toward strict case-sensitive matching**, citing the `bc/restrict-hex-to-lowercase` topic (brian m. carlson’s prior work) as precedent. His position is that Git has never emitted uppercase hexadecimal OIDs itself, so `starts_with()`—which enforces case-sensitive matching—is sufficient and consistent with the broader effort to discourage uppercase OID usage. This directly rejects Phillip Wood’s suggestion to adopt permissive matching (accepting all-uppercase or all-lowercase OIDs) for consistency with `git rebase --autosquash`.

**Key technical detail**: The resolution requires a mechanical change in Patch 7/8: replacing `istarts_with()` with `starts_with()` in the autosquash marker validation logic. No new helper function or policy infrastructure is needed; the change is a one-line substitution. **Test coverage for the strict policy must be added in the reroll (v15)**.

**Files and symbols involved**:
- **File**: `builtin/history.c`
- **Function**: `squash_check_can_autosquash` (Patch 7/8)
- **Key line**: `if (target && istarts_with(oid_to_hex(&target->object.oid), s))` → `if (target && starts_with(oid_to_hex(&target->object.oid), s))`
- **Related prior work**: `bc/restrict-hex-to-lowercase` (SHA-256 interoperability topic)

**Implications for the series**:
- The series is **technically complete except for this one-line change and corresponding test cases**.
- Junio’s reply **removes the last technical blocker** for integration; the reroll (v15) is now a formality.
- The series remains **cooking in `seen`** and is likely to graduate to `next` once v15 is posted.

**Likely points of future discussion**:
- Whether the strict policy creates user-facing inconsistency with `git rebase --autosquash` (Phillip Wood’s concern).
- Whether the `bc/restrict-hex-to-lowercase` precedent is sufficient justification for rejecting uppercase OIDs in this context.

**Status**: The series is **ready for integration** pending the reroll (v15) with the `starts_with()` change and test coverage for strict case-sensitive matching. Junio’s "Will replace" sign-off (from v7) and the resolution of all other feedback indicate intent to merge.