This is Junio C Hamano's review of the second patch in Harald Nordgren's series that proposes to protect local branches from deletion via `git branch -d` when they serve as the upstream of another local branch. Junio strongly opposes the change, arguing that it alters the long-established semantics of the `-d` flag in a way that would break existing user workflows.

The review highlights a specific test case (`branch -d other@{u}`) that now requires `-D` instead of `-d` due to the new protection, illustrating how the change forces users to bypass safety checks even when they previously relied on `-d` to safely delete branches. Junio views this as a regression rather than an improvement, making the command's behavior less predictable across Git versions. He explicitly rejects the idea of introducing a configuration variable to control this behavior, as it would further complicate the command's semantics.

This is a substantive review that raises a fundamental design concern: the patch's backward-incompatible change to a core command flag. The objection is not about implementation details but about the principle of preserving user expectations. No alternative approach is suggested in this email, but the tone suggests the patch as currently proposed is unlikely to be accepted. The thread is now at a critical juncture where the author will need to either defend the change or reconsider the design.