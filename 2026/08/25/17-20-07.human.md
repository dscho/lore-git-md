This is a **policy clarification** in the ongoing `git history squash` series, specifically addressing the final unresolved question: whether autosquash marker resolution should enforce **strict case-sensitive OID matching** (rejecting all uppercase hexadecimal OIDs) or allow **permissive matching** (accepting all-uppercase or all-lowercase OIDs).

Junio C Hamano firmly reaffirms the project’s direction: **Git will not introduce new code that accepts uppercase OIDs**, even if existing commands like `git rebase --autosquash` still do. This means the `git history squash` implementation must use `starts_with()` (case-sensitive) rather than `istarts_with()` (case-insensitive) when matching autosquash markers like `fixup! ABCDEF`. The decision aligns with brian m. carlson’s earlier `bc/restrict-hex-to-lowercase` work, which established Git’s convention of emitting only lowercase hexadecimal OIDs.

For the series, this resolves the last technical blocker. Harald Nordgren will need to reroll **Patch 7/8** to replace `istarts_with()` with `starts_with()` and add test coverage for the strict case-sensitive policy. The reroll (v15) should be straightforward, as the change is mechanical and the rest of the series is already finalized. Junio’s "Will replace" sign-off from v7 still stands, so the series is now **ready for integration** once the reroll is posted.

The broader implication is that Git is moving toward a consistent policy of rejecting uppercase OIDs in new code, even if older commands remain permissive for backward compatibility. This thread confirms the project’s preference for strictness over consistency with existing behavior when introducing new functionality.