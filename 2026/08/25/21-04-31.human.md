This is the eighth iteration of a usability-focused patch series improving error messages for `git worktree add`, `git checkout`, and `git switch` when users provide an ambiguous remote branch name (e.g., `foo` when both `origin/foo` and `upstream/foo` exist). The series now consists of four patches, adding a new commit to fix the behavior of `--guess-remote` when multiple matches exist.

The core goal remains the same: replace the terse "matched multiple remote tracking branches" warning with a detailed, actionable advice message that lists the conflicting remotes (e.g., "Branch name '%s' appears in multiple remotes: origin, upstream"). The advice is gated behind `advice.checkoutAmbiguousRemoteBranchName` (enabled by default) and is now consistently applied across `git checkout`, `git switch`, and `git worktree add` via a shared helper function. The series also refines error handling by moving an existing `die()` call earlier in the code path, replacing a generic "invalid reference" message with a more specific diagnostic that explains the ambiguity.

The new patch (4/4) addresses a latent bug in the `--guess-remote` option of `git worktree add`: previously, the command would silently succeed when multiple remote branches matched, creating a local branch from HEAD. Now, it treats multiple matches as an error, aligning its behavior with the rest of the series. This change is well-motivated and closes a gap identified in earlier discussions. The patch includes updated test coverage to verify the new behavior.

The series is now functionally complete, addressing all known edge cases and review feedback. Junio C Hamano’s earlier concerns about the one-argument form of `git worktree add` (e.g., `git worktree add ../topic-branch`) are intentionally deferred to a follow-up commit, as agreed in the thread. The implementation is clean, with no remaining open questions or objections. The changes are localized to `builtin/worktree.c`, `builtin/checkout.c`, and their associated tests, and the commit messages clearly explain the motivation and behavior changes. This looks like a well-crafted usability improvement that will help users resolve ambiguity without disrupting existing workflows.