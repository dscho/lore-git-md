This is the final patch in the v8 series improving error messages for ambiguous remote branch names in `git worktree add`, `git checkout`, and `git switch`. The patch specifically addresses the `--guess-remote` option in `git worktree add`, ensuring it errors out when multiple remote branches match the given name, rather than silently succeeding. This aligns its behavior with `git checkout` and `git switch`, completing the series' goal of consistent, actionable error handling across all three commands.

The patch extends `dwim_branch()` to accept the `add_opts` struct, allowing it to respect the `--quiet` flag when emitting advice. It also updates `unique_tracking_name()` to collect remote names and count matches, enabling the new error logic. The advice helper `advise_disambiguating_remotes()`—introduced earlier in the series—is reused here to provide a clear, command-specific message listing the conflicting remotes and suggesting resolution steps (e.g., using `-b` with a fully qualified branch name or setting `checkout.defaultRemote`).

Junio’s review confirms the patch is well-structured, noting only a minor organizational point about the placement of `advise_disambiguating_remotes()` (which was already addressed in the series). The implementation is clean, with thorough test coverage in `t/t2400-worktree-add.sh` verifying the new error case. The series is now functionally complete, with all prior feedback incorporated and no remaining objections. The one-argument `git worktree add` edge case (e.g., `git worktree add ../topic-branch`) remains scoped for a follow-up, as previously agreed.

Key technical details for follow-up discussion:
- Files: `builtin/worktree.c` (primary changes), `checkout.h` (shared helper).
- Functions: `dwim_branch()` (extended to accept `add_opts`), `unique_tracking_name()` (updated to collect remote names), `advise_disambiguating_remotes()` (reused for advice).
- Behavior: `--guess-remote` now fails with a clear error when ambiguity exists, matching `checkout`/`switch`.
- Tests: New coverage for `--guess-remote` with multiple matches, verifying error message format and advice suppression in quiet mode.
- Config: `advice.checkoutAmbiguousRemoteBranchName` (default: true) gates the advice.