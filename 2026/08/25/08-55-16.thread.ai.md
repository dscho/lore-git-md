Updated AI thread summary:

This thread is a single-patch bugfix series from Nikita Leshenko addressing an edge case in `git am --3way` where a later patch in a multi-patch series fails with "sha1 information is lacking or useless" because its preimage (the postimage of an earlier patch) was never recorded in the repository. The original patch proposed recording the postimage of every cleanly applied patch when `--3way` is enabled, but Junio C Hamano objected to the performance overhead of spawning extra processes and generating loose objects for every patch in a series.

Nikita has now responded with a revised approach that avoids per-patch overhead while still fixing the bug. Instead of Junio’s suggested lazy reconstruction (which would surgically apply only the necessary prior patches to reconstruct missing blobs), Nikita proposes a coarser fallback: when `build_fake_ancestor()` fails due to missing SHA-1 information, apply *all* prior patches (1..N-1) on their fake ancestors to generate the missing blobs. To avoid repeating this expensive operation for every subsequent patch, the `am_state` (`.git/rebase-apply/`) would record a "postimage-attempted" marker indicating how far the full reconstruction has progressed. This ensures the preimage for patch N exists, allowing the 3-way fallback to proceed, while only reapplying patches (N+1)..(M-1) if patch M later fails. The trade-off is simplicity and correctness over performance, with the worst-case overhead (applying all prior patches) still being better than failing entirely. The proposal preserves clean-path performance (no overhead when patches apply cleanly or blobs already exist) and only activates when the original bug would have surfaced.

The patch remains under review with no maintainer decision or merge status reported. Key technical details: the fix is internal to `git am --3way`, with no new CLI options, config keys, or on-disk format changes. The revised approach introduces a new state variable (`postimage-attempted`) in `am_state` and modifies the fallback logic in `fall_back_threeway()`. Open questions: whether Junio will accept the performance trade-off of the coarser fallback, and whether the new state variable introduces edge cases (e.g., interrupted `git am` operations). Participant positions: Junio C Hamano (maintainer) raised a substantive objection to the original implementation and proposed an alternative design; Nikita Leshenko (author) has now countered with a simpler, less optimized approach. No other reviewers have weighed in. Related work: none mentioned.