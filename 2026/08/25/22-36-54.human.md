This is a brief but substantive reply from Junio C Hamano in the ongoing trace2 hardening thread. Junio confirms Elijah Newren’s observation about the one-byte off-by-one error in the fifth patch of Derrick Stolee’s series, which replaces `xstrfmt()` with manual string concatenation in `redact_arg()`. The error causes the redacted placeholder to be truncated to `:<REDACTED` (missing the trailing `>`), potentially corrupting trace2 logs or exposing sensitive data.

Junio’s phrasing—"if it were (redact_len + 1) it would have worked better, perhaps?"—is a gentle but clear endorsement of Elijah’s critique, suggesting the fix should copy the full placeholder length (including the null terminator) rather than stopping short. The reply is narrowly focused on this specific bug and does not revisit broader architectural concerns (e.g., the `banned-die.h` naming or include-ordering risks) raised in earlier reviews.

This exchange reinforces that the error is a real correctness issue, not a stylistic nit, and will likely be addressed in a v3 iteration. The tone is collaborative, with Junio implicitly aligning with Elijah’s technical assessment. No new objections or alternatives are introduced; the discussion remains centered on the mechanics of the fix. Readers following the thread can now treat this as a settled point—future versions of the patch will need to correct the `memcpy()` length to avoid breaking the redaction format.