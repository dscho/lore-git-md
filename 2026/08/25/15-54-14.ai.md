This is a **substantive follow-up discussion** in the ongoing thread about the Windows-specific ODB deadlock fix in the sequencer. Phillip Wood responds to Junio C Hamano’s historical context and architectural suggestions, refining the analysis of when `git commit` (and thus auto-gc) is actually invoked during interactive rebase.

Phillip clarifies that while the sequencer avoids spawning `git commit` for simple cherry-picks, it *does* invoke it when rebasing merges, and likely during large rebases even without conflicts or rewording. This means auto-gc *will* trigger mid-rebase in many real-world cases, not just at the end, weakening the argument for disabling `gc.auto` during rebase. The observation is factual and data-driven, based on the sequencer’s current logic, and it shifts the discussion away from the "single gc at the end" idea toward the more general ODB-release mechanism Junio endorsed.

No new patches or technical proposals emerge here—this is purely a refinement of the architectural discussion. The key takeaway for future summarization is that the `gc.auto=false` idea, while plausible for small projects, is less compelling in practice because the sequencer’s behavior ensures auto-gc will often run mid-rebase anyway. The focus remains on Junio’s preferred long-term direction: automatically closing the ODB before spawning any Git command, which would obviate the need for one-off fixes like the current patch. The thread continues to position this as a promising architectural study, though still without concrete next steps.