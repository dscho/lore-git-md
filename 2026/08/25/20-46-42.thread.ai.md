Thread identity: patch series introducing `git ident`, a renamed and redesigned built-in command to display Git’s identity (author/committer names/emails) and signing configuration (GPG/SSH/X.509 keys, `commit.gpgsign` status). Originating author: Andrew Pleeter (via GitGitGadget). Thread type: patch series (v2, previously `git whoami` v1).

Core problem or goal: provide a unified, user-friendly command to consolidate Git’s identity and signing configuration, which currently requires checking multiple `git config` or `git var` settings. The command is designed for both interactive use and scripting, with composable flags to query specific fields (`--author`, `--committer`, `--name`, `--email`, `--signing-key`, `--verbose`) and machine-readable output (`--porcelain`, `-z`).

Current status: v2 posted, replacing the contested `git whoami` v1. The redesign addresses all prior maintainer-level critiques: command renamed to `git ident` (aligning with Git’s naming conventions), additive/composable flags, machine-readable/NUL-terminated output, removal of `the_repository` usage, and support for X.509 signing keys. The commit message now justifies the command’s inclusion in Git core by contrasting its unified design with `git var` and `git config`. No merge status reported; still under review but likely to advance to `next` if no new concerns arise.

Key technical details:
- New built-in command: `git ident`, implemented in `builtin/ident.c`.
- Files touched: `builtin/ident.c`, `Documentation/git-ident.adoc`, `t/t0015-ident.sh`, build system files (Makefile, meson.build), `command-list.txt` (classified as `ancillaryinterrogators`), `builtin.h`.
- New symbols: `cmd_ident()` (entry point), supporting functions in `ident.c`.
- Behavior: displays author/committer names/emails and signing configuration (GPG/SSH/X.509 keys, `commit.gpgsign` status). Flags are additive (e.g., `-a -e -n` shows full author identity) and work outside of a repository. Supports `--porcelain` (machine-readable) and `-z` (NUL-terminated) output for scripting.
- Test coverage: comprehensive, including environment variable overrides, incompatible flag combinations, and signing key resolution.
- No on-disk format changes or new configuration options.
- **Resolved requirements**: machine-readable/NUL-terminated output (brian m. carlson), removal of `the_repository`, support for X.509 signing keys, and additive flag design (Junio).

Open questions and loose ends: none remaining from the v1 review. The command’s necessity is now justified in the commit message, and all technical gaps are addressed. The patch is ready for substantive review; any new concerns would likely focus on edge cases or integration (e.g., CI, platform compatibility).

Participant positions:
- **Andrew Pleeter**: addressed all prior feedback in v2, renaming the command to `git ident` and redesigning flag interactions to be additive and predictable. The implementation now aligns with Git’s conventions and includes machine-readable output, NUL-terminated output, and X.509 support.
- **brian m. carlson**: high-weight review in v1 requested machine-readable output, removal of `the_repository`, and X.509 support. All requests are now satisfied in v2.
- **Junio C Hamano**: maintainer-level skepticism in v1 focused on usability (flag interactions) and project scope. The v2 redesign resolves these concerns with additive flags and a clearer justification for inclusion in Git core.

Related work: none. The v2 patch implicitly contrasts `git ident` with `git var` and `git config`, but no direct alternatives are proposed. The discussion now centers on the command’s readiness for integration.