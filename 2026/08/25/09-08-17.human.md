This is a brief but pivotal exchange in the long-running `git history squash` series. Phillip Wood and Junio C Hamano are debating the final unresolved technical detail: whether the new command should accept uppercase hexadecimal OIDs in `fixup!`, `squash!`, and `amend!` markers.

Junio reiterates that Git has never emitted uppercase OIDs (citing brian m. carlson’s prior work) and argues that `starts_with()`—not `istarts_with()`—is sufficient, implying strict case-sensitive matching. Phillip concedes the point: if the project is moving toward rejecting uppercase hex everywhere, then `starts_with()` is the right choice here too. The exchange effectively settles the policy: the next reroll (v15) will enforce strict case-sensitive matching, rejecting all uppercase OIDs in autosquash markers. This is the last technical hurdle before the series can be queued for integration.