This is the first patch in the v9 iteration of the four-patch series improving error messages for ambiguous remote branch names in `git checkout`, `git switch`, and `git worktree add`. The patch extracts the advice message logic into a new shared helper function, now named `advise_disambiguating_remotes()` (renamed from the earlier `advise_ambiguous_remote()` in v8), to avoid code duplication and prepare for reuse in later patches. This is a purely structural refactoring with no behavior change; the advice message and error handling remain identical to the previous version.

Junio’s reply is a surface-level review comment on the commit message and function naming. He suggests expanding the commit message to explain the motivation for extracting the helper (e.g., deep nesting, incorrect indentation, or preparation for later patches) and notes that the function should use the updated name (`advise_disambiguating_remotes`) from the start, rather than introducing it in patch 2/4. These are minor, uncontroversial adjustments that do not affect the patch’s functionality or the series’ overall goal.

The patch itself moves the advice block from `parse_remote_branch()` into a standalone function that takes the `enum checkout_command` as an argument, allowing it to tailor the message for either `git checkout` or `git switch`. The advice remains gated behind the `advice.checkoutAmbiguousRemoteBranchName` config key (enabled by default) and is only shown when multiple remote branches match the given name. This refactoring sets up the shared helper for use in `git worktree add` in patch 3/4, addressing prior feedback to avoid duplicating the advice logic.

The series is now functionally complete, with all prior feedback addressed and the follow-up item for the one-argument `git worktree add` case clearly scoped for a separate commit. This patch is a straightforward, well-motivated refactoring that aligns with the series’ goal of consistent, actionable error messages across commands.