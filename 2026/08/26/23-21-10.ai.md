This is Junio’s "What’s cooking" report for August 2026 (#11), summarizing the state of 56 topics currently in flight across the integration branches.

The report lists 19 topics that graduated to `master` in the 19th batch since Git 2.55, bringing the total to 563 non-merge commits on `master`. Another 75 topics are cooking in `next`, and 209 are in `seen`. The report also includes new topics, stalled topics, and those still under review.

Key graduated topics include:
- **cc/fast-import-usage**: Standardized `git fast-import` usage and documentation.
- **kk/merge-base-exhaustion**: Optimized merge-base computation by early termination when one side’s commits are exhausted.
- **ps/odb-streams**: Unified object database streaming APIs into a single `struct odb_stream`.
- **ss/repack-drop-filtered**: Added `--drop-filtered` to `git repack` to reclaim space from local promisor blobs in partial clones.
- **js/packfile-fast-append**: Fixed a performance regression in `packfile_list_append()` when adding many packs.

Notable new topics:
- **dw/config-read-both-global**: Fixes an inconsistency where `git config --global` only read `$HOME/.gitconfig` but not `$XDG_CONFIG_HOME/git/config`.
- **kn/reftable-optimize-reloading**: Optimizes reftable stack reloads by avoiding redundant `stat()` calls when the lock is already held.
- **ps/odb-alternates-at-creation**: Defers alternates setup to ODB creation time during clone, simplifying the backend interface.
- **ps/odb-pluggable-fsck**: Moves fsck checks into backend-specific layers, making them pluggable.
- **en/no-amend-during-conflicts**: Teaches `commit --amend` to refuse during conflict resolution, aligning with existing behavior in `cherry-pick` and `merge`.

Stalled topics include:
- **ps/libgit-in-subdir**: Moves `libgit.a` sources into a `lib/` directory but was ejected due to merge conflicts.
- **tb/pack-with-duplicates**: Hardens packfile handling for duplicate objects but needs further review.
- **cl/regexec-macos-leak**: Workaround for a macOS regex memory leak but requires a reroll.

The report also highlights ongoing efforts like the ODB abstraction (`ps/odb-*` topics), reftable optimizations, and pluggable packfile generation. Several topics are marked as needing review or responses, indicating active discussion or pending updates.

For a future AI summarizer, key technical details to retain include:
- Branch states (`next`, `seen`, `master`) and graduation status.
- Subsystems touched (e.g., `odb`, `reftable`, `pack-objects`).
- New options/configs (e.g., `fetch.packfileURIThreads`, `uploadpack.lazyFetchTrusted`).
- Behavioral changes (e.g., `--drop-filtered` in `git repack`, `--linearize` in `git replay`).
- Performance improvements (e.g., merge-base optimization, reftable reloads).
- Pluggability efforts (e.g., ODB fsck, packfile generation).