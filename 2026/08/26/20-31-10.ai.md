This is a maintainer response to a bugfix patch for the `apply` subsystem, specifically rejecting the proposed fix for memory leaks and state corruption in `find_header()`.

Junio reviews the patch after it sat unreviewed for over a month (submitted in early July, revisited in late August). His core objection is conceptual: the "cross checks" that detect malformed patch headers (like stray "diff --git" lines) are *intended* to be sanity checks that force the user to inspect and fix broken patches. The current behavior—where partially parsed state from abandoned Git-style headers can corrupt the real patch—is not seen as a bug but as a deliberate design choice to surface malformed input. Junio explicitly states "I do not think we want to apply this patch."

The patch itself (as summarized in the parent email) introduced a temporary `struct patch` to isolate Git-header parsing, only committing state if the header is accepted, and added regression tests. Junio’s reply does not engage with the technical implementation or test coverage, focusing instead on the higher-level design intent. No alternative solutions or follow-up actions are proposed; the patch is effectively closed unless a compelling counterargument emerges.

This thread is now at a decision point: the maintainer has rejected the change, and further progress would require either (1) a persuasive rebuttal that the current behavior is harmful or (2) a revised approach that preserves the "sanity check" intent while addressing the memory leak. The ball is back in the original author’s court.