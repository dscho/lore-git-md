This is Junio C Hamano nudging the list for review of Kristoffer Haugsbakk’s five-patch series (v2) that adds `--abbrev`, `--color`, and `--date` formatting options to the experimental `git format-rev` command. The series is uncontroversial, well-structured, and addresses all feedback from v1—including defensive code improvements, a memory leak fix, and documentation refactoring—but has received no reviews since v2 was posted on August 26.

The patches are part of an ongoing effort to bring `git format-rev` (introduced in Git 2.45) closer to parity with `git log`’s formatting flexibility. They reuse existing `git log` logic (`rev_info` struct and `pretty.c` date formatting) and include thorough test coverage in `t/t6120-describe.sh`. Junio notes that while the patches look "more or less OKish" to him, he is not deeply invested in this experimental command and would welcome reviews from contributors who see value in its development.

The series is not yet queued in any integration branch (`seen`, `next`, or `master`). If you care about `git format-rev` or Git’s formatting infrastructure, now is the time to chime in—otherwise, this small but polished feature addition may stall for lack of feedback.