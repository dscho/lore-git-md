This is the first patch in the v9 iteration of a four-patch series improving error messages for ambiguous remote branch names in `git checkout`, `git switch`, and `git worktree add`. The series is already merged to `next` and functionally complete, with this patch serving as a structural refactoring to avoid code duplication.

Junio’s reply is a surface-level review of the commit message and function naming in Yoichi’s patch. He suggests adding motivation to the commit message—specifically, noting that the extracted function will be reused in later patches and that the original code was deeply nested or poorly indented. He also points out that the function name will be improved in the second patch (`advise_ambiguous_remote()`), so it should be given that name upfront rather than introducing it as `advice_disambiguating_remotes()` and renaming it later.

The feedback is purely mechanical and does not affect the patch’s behavior or correctness. The patch itself extracts the advice message logic into a shared helper function, `advice_disambiguating_remotes()`, to prepare for reuse in `git worktree add` in patch 3/4. This is a clean refactoring with no functional changes, and the feedback is aimed at improving clarity and consistency in the patch series. The discussion is minor and unlikely to delay the series, which is already in good shape.