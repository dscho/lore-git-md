This is the final patch in René Scharfe’s four-patch series fixing and cleaning up the `worktree_basename()` helper in `git worktree add`. The email is a **commit-message clarification** in response to Junio C Hamano’s review of Patch 4/4.

The patch itself renames `worktree_basename()` to `worktree_basename_dup()` and makes it return a freshly allocated string copy instead of a pointer into the input buffer, simplifying memory management for its three callers. Junio’s review flagged ambiguity in the commit message: the phrase “Two already do” could mislead readers into thinking two callers already free the returned string (which would crash), when in fact they make their own copies. René acknowledges the confusion and accepts Junio’s suggested rewording to clarify that two callers already copy the string, making their conversion to the new `_dup()` variant straightforward. The code change remains uncontroversial and has Junio’s “OK”; the series is now effectively ready to advance once the commit message is updated. No new technical discussion or test changes are introduced in this reply.