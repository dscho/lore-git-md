This is the first patch in Karthik Nayak’s v4 series introducing the `receive-report` hook. It is a **documentation patch** that adds a missing `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc`, explaining the purpose of the existing `proc-receive` hook and linking to `githooks(5)` for protocol details.

The patch is preparatory: it addresses prior feedback about documentation completeness for hooks in the `git-receive-pack` lifecycle. The change is minimal (six lines added) and self-contained, touching only `Documentation/git-receive-pack.adoc`. The new section follows the existing pattern of other hook documentation in the file, providing a brief description and a cross-reference to the full protocol documentation. This patch has no functional impact on the codebase and is uncontroversial. It is queued in `seen` alongside the rest of the series and awaits graduation to `next`.