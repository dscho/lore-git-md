This thread is about a bugfix from Elijah Newren that extends Git's existing protection against `git commit --amend` during conflict resolution to cover additional operations that can produce conflicts: `git am`, `git revert`, and all forms of `git rebase`. The goal is to prevent users from accidentally amending the wrong commit during conflict resolution, which can corrupt repository state.

The patch carefully distinguishes between genuine conflict stops and internal uses of `--amend` by interactive rebase (for `squash`, `reword`, `edit`, and `break` directives). It checks for the presence of `.git/rebase-merge/stopped-sha` and the absence of `.git/rebase-merge/amend` to identify conflicted stops, while allowing clean `edit` and `reword` stops to proceed. The fix includes new tests in `t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, and `t4151-am-abort.sh` to verify the behavior across all affected operations.

Elijah's commit message is thorough, explaining the motivation, technical approach, and edge cases considered. The code is clean, well-documented, and appears ready for review. Given Elijah's deep expertise in Git's merge machinery, this fix is likely to be uncontroversial and well-received. No reviews or objections have been posted yet, so the next step is for reviewers to weigh in.