This is Junio C Hamano pinging the list for additional review on Michael Montalbo’s three-patch bugfix series that makes two CGI test helpers in `t/lib-httpd/` concurrency-safe. The series replaces non-atomic shell operations (`test -f` followed by `rm` or `touch`) with atomic alternatives (`rm` without `-f` for one-shot markers, `mkdir` for first-request election) to eliminate a race condition that causes HTTP 500 errors in concurrent requests. The issue is a real, observed flake in `t5616.47` on macOS CI runners, and the series includes a new test (`t5567-one-time-script.sh`) that exercises the helper directly without a web server, making the race deterministic.

Junio previously reviewed v2 and marked the series as ready for integration, but he is now looking for a second opinion from someone with deeper HTTP test expertise. The v3 iteration incorporates all feedback from Junio and Patrick Steinhardt: patch 1 now uses `rm` (without `-f`) instead of `mv` to consume the one-shot marker, simplifying the logic while maintaining atomicity, and includes a comment explaining why the helper discards `stderr` (to suppress expected errors when the script is removed by a concurrent request). Patch 3, the documentation update, has been relocated from `t/README` to `t/lib-httpd.sh` and made specific to the HTTP daemon helpers, addressing Patrick’s preference for contextual placement.

The series is self-contained, touches only the test infrastructure (`t/` directory), and has no open questions. It looks well-crafted, addresses a real problem, and is unlikely to be controversial. If no one steps up to review it soon, Junio will likely proceed with merging it to `next` based on his earlier assessment.