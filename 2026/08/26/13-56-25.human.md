This is a **substantive review** of a **bugfix** patch targeting the merge machinery. Phillip Wood provides detailed feedback on Elijah Newren’s patch, which extends Git’s protection against `git commit --amend` during conflict resolution to cover `git am`, `git revert`, and all forms of `git rebase`. The patch is well-motivated and addresses a real foot-gun, but Phillip suggests a structural improvement.

Phillip’s review is thorough and constructive. He praises the logic and test coverage but proposes moving the conflict-detection checks into `sequencer_determine_whence()` to avoid hardcoding paths in `builtin/commit.c`. This would centralize the logic and make it easier to maintain, especially as the sequencer already handles similar state checks. He also notes a minor edge case (conflicted fixups) where the current logic correctly allows `--amend`, confirming the patch’s careful design.

The review is **substantive**—it engages with the code’s structure and suggests a refactoring to improve maintainability. Phillip’s feedback is specific, actionable, and grounded in the existing codebase, making it a high-value contribution to the thread. The patch itself remains uncontroversial, and this review is unlikely to derail it, but it may prompt a v2 to address the architectural suggestion. If you’re following merge machinery or sequencer internals, this is worth watching.