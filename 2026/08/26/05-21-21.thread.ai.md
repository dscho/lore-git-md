Thread identity: bugfix patch series (single patch) by Elijah Newren via GitGitGadget, targeting the merge machinery to prevent `git commit --amend` during conflict resolution for `git am`, `git revert`, and all forms of `git rebase`.

Core problem or goal: extend Git's existing protection against `git commit --amend` during conflict resolution to cover additional operations that can produce conflicts, preventing users from accidentally amending the wrong commit and corrupting repository state. The fix distinguishes between genuine conflict stops and internal `--amend` usage by interactive rebase (e.g., `squash`, `reword`, `edit`, `break`).

Current status: patch posted, under review. No prior versions or merge status recorded.

Key technical details:
- Files touched: merge machinery internals (specific files not named, but logic checks `.git/rebase-merge/stopped-sha` and `.git/rebase-merge/amend`).
- New behavior: blocks `git commit --amend` during conflict resolution for `git am`, `git revert`, and all `git rebase` variants unless the operation is an interactive rebase directive (`squash`, `reword`, `edit`, `break`) that explicitly requires `--amend`.
- Test coverage: new tests in `t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, and `t4151-am-abort.sh` verify behavior across all affected operations.
- No on-disk format changes or new CLI options.

Open questions and loose ends: none identified yet; patch appears uncontroversial and ready for review.

Participant positions: none recorded yet (initial patch).

Related work: builds on existing protection for `git commit --amend` during conflict resolution, extending it to additional commands. No external dependencies.