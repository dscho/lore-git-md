Thread identity: bugfix patch series (single patch) by Elijah Newren via GitGitGadget, targeting the merge machinery to prevent `git commit --amend` during conflict resolution for `git am`, `git revert`, and all forms of `git rebase`.

Core problem or goal: extend Git's existing protection against `git commit --amend` during conflict resolution to cover additional operations that can produce conflicts, preventing users from accidentally amending the wrong commit and corrupting repository state. The fix distinguishes between genuine conflict stops and internal `--amend` usage by interactive rebase (e.g., `squash`, `reword`, `edit`, `break`). Junio C Hamano notes that the same principle should eventually extend to `git commit <paths>` during conflict resolution, as conflict resolution should always record the entire tree state on top of the current HEAD, never updating individual paths.

Current status: patch posted, under review. Phillip Wood provided a substantive review suggesting a structural improvement—moving conflict-resolution checks from `builtin/commit.c` to `sequencer_determine_whence()`—to avoid hardcoded paths and improve maintainability. Junio C Hamano endorsed the patch as correct and well-reasoned but proposed leaving a `#leftoverbits` marker for future work on `git commit <paths>`. In the latest feedback, Junio suggested a minor cleanup: replacing the global `the_repository` with the repository instance already available in the `wt_status` struct (`s->repo`), aligning with the ongoing `the_repository` removal effort. The patch remains uncontroversial in its goal but may see a v2 incorporating feedback. No merge status recorded.

Key technical details:
- Files touched: `builtin/commit.c` (current implementation), with potential refactoring to `sequencer.c` (proposed by Phillip Wood). Test coverage in `t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, and `t4151-am-abort.sh`.
- New behavior: blocks `git commit --amend` during conflict resolution for `git am`, `git revert`, and all `git rebase` variants unless the operation is an interactive rebase directive (`squash`, `reword`, `edit`, `break`, `fixup`) that explicitly requires `--amend`.
- No on-disk format changes or new CLI options.
- Junio’s feedback highlights that the same protection should apply to `git commit <paths>` during conflict resolution, as partial tree updates during conflict resolution are unsafe.
- Junio’s latest suggestion: replace `the_repository` with `s->repo` in the new block to align with the `the_repository` removal effort.

Open questions and loose ends:
- Whether Elijah will adopt Phillip Wood’s suggestion to refactor the conflict-resolution checks into `sequencer_determine_whence()`.
- Whether Elijah will incorporate Junio’s minor cleanup (replacing `the_repository` with `s->repo`).
- Future work: addressing `git commit <paths>` during conflict resolution, as noted by Junio with a `#leftoverbits` marker.

Participant positions:
- **Phillip Wood**: provided a substantive review, suggesting a structural improvement to avoid hardcoded paths and improve code organization. Explicitly noted that the current logic is sound but could be cleaner. No objections to the patch’s goal or correctness.
- **Junio C Hamano**: endorsed the patch as correct and well-reasoned, agreeing with its motivation and logic. Proposed extending the principle to `git commit <paths>` in a future topic, leaving a `#leftoverbits` marker for follow-up work. In the latest feedback, suggested a minor cleanup (replacing `the_repository` with `s->repo`). No objections to the current patch.

Related work: builds on existing protection for `git commit --amend` during conflict resolution, extending it to additional commands. Junio’s feedback identifies an adjacent issue (`git commit <paths>`) worth tackling later. No external dependencies.