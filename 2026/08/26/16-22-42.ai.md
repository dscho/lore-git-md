This is a **bugfix** targeting the merge machinery, part of the ongoing thread to prevent `git commit --amend` during conflict resolution. Junio C Hamano replies to Elijah Newren’s patch, acknowledging its correctness but suggesting a broader scope for future work.

Junio agrees with the patch’s logic and motivation—blocking `git commit --amend` during conflict resolution to avoid corrupting repository state—but notes that the same principle should extend to `git commit <paths>`. He argues that conflict resolution should always record the entire tree state on top of the current HEAD, never updating individual paths in the current HEAD. While he endorses the current patch as a correct and focused fix, he proposes leaving a `#leftoverbits` marker to address the `git commit <paths>` case in a separate topic, as the guiding principle differs slightly.

The discussion remains uncontroversial and constructive. The patch itself is well-reasoned, with clear documentation of the rebase-merge directory files and their roles in distinguishing conflict stops from legitimate amend operations. Junio’s feedback does not challenge the patch’s correctness or approach but instead identifies an adjacent issue worth tackling later. No merge status is recorded, and the patch is still under review.