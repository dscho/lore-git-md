This is the first patch in Karthik Nayak’s three-part series introducing the `receive-report` hook for `git-receive-pack`. This patch is purely documentation: it adds a `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc` to document the existing `proc-receive` hook, which was previously undocumented in this file.

The change is small and self-contained, touching only one file (`Documentation/git-receive-pack.adoc`). The new section explains the hook’s purpose—handling refs that match patterns defined by `receive.procReceiveRefs`—and links to `githooks(5)` for full protocol details. This patch addresses prior feedback about documentation completeness for hooks in the `git-receive-pack` lifecycle, ensuring the new `receive-report` hook (introduced in patch 3) will not be the only hook documented here.

The patch is well-motivated and uncontroversial. It is part of a series that is now technically complete and queued in `seen`, awaiting graduation to `next`. No further changes are expected to this patch.