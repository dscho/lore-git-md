This is Junio Hamano's call for review of Erik Cervin-Edin's v2 2/2 patch, which completes the `--fixup` message specification feature by adding `-c`/`--edit` and `-C`/`--reuse-message` support. The patch has been sitting unreviewed for some time, and Junio is explicitly soliciting feedback from the list.

The email highlights one specific technical observation: the patch removes `--fixup` from the `die_for_incompatible_opt3()` check but leaves a redundant check in place. The current code blocks `-C`, `-c`, and `-F` in two separate places, and the second check (in `die_for_incompatible_opt4()`) already covers all the same combinations. This is a minor code hygiene issue rather than a functional problem, but it suggests the patch could be slightly cleaner.

The broader context (from the thread summary) is that this is the final piece of a two-patch series that now provides a complete, consistent support matrix for all message specification methods (`-m`, `-F`, `-c`, `-C`) across all `--fixup` variants (`--fixup`, `--fixup=amend:`, `--fixup=reword:`). The series has already incorporated substantive feedback from Junio and Phillip Wood, and the implementation appears robust with thorough test coverage. The only remaining open question is whether the redundant check should be tidied up before merging.