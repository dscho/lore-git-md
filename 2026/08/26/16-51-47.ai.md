This is a test patch (v3) for the `git symbolic-ref` command, part of the refs subsystem. It adds systematic test coverage for the exit codes and output behavior when querying a non-symbolic ref, both with and without the `--quiet` (`-q`) flag. The patch itself is uncontroversial and well-crafted: it touches only `t/t1401-symbolic-ref.sh`, uses `test_must_fail` instead of hardcoding exit codes (addressing Junio’s earlier feedback), and follows existing test patterns. The commit message is clear and references prior art (919eb8ace).

The key change since v2 is mechanical: replacing `test_expect_code` with `test_must_fail` to avoid freezing exact exit codes in the test. This aligns with Junio’s preference for not over-specifying behavior in tests. However, the thread’s broader compromise—documenting the current exit code behavior (128 vs. 1) in the man page—is now in question. Junio’s latest objection ("casting wrong behavior into stone") has stalled that update, leaving the patch’s documentation component unresolved. The test additions remain valid and useful regardless of the man page’s fate.

The patch documents behavior relied upon by existing tests (e.g., `t2020`, `t5621`) and provides clarity for script authors. The exit code discrepancy (128 without `--quiet`, 1 with it) is likely unintentional but may serve a purpose for error differentiation, as Phillip Wood argues. No behavioral change is proposed here, only test coverage and (contested) documentation. The next step hinges on resolving the man page update: whether to drop it, revise it to avoid normative language, or propose a behavioral fix to unify the exit codes.

This is a low-risk, high-value contribution to test coverage. The unresolved documentation question does not diminish the patch’s utility, but it may delay merging until the thread reaches consensus on how to handle the man page. The patch is ready for review as-is; the open question is whether to split the documentation update into a separate follow-up.