This is the third patch in Taylor Blau’s MIDX incremental-writes series, now updated in response to Junio C Hamano’s maintainer ping. The patch corrects the pack-inclusion logic for custom base layers during incremental MIDX writes, and the email focuses solely on the commit-message tweak requested earlier: replacing the outdated block comment in `midx-write.c` with a clearer explanation of the relaxed invariant.

Taylor shares a proposed replacement comment that explicitly states the new rule—`ctx->base_midx` and `ctx->to_include` can both be non-NULL, but `ctx->m` and `ctx->to_include` remain mutually exclusive—and walks through the two scenarios (new MIDX vs. reusing an existing layer). The diff is minimal (16 lines added, 10 removed) and targets only the explanatory comment, not the code logic, which has already been validated by tests and prior review. The message invites feedback on the clarity of the new wording before sending a formal reroll. No further technical changes are proposed; this is purely a documentation polish step to satisfy the last remaining integration requirement.