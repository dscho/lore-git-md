Here is the updated AI-mode thread summary, preserving all prior state and incorporating the new email:

---

Thread identity: Junio C Hamano’s recurring "What's cooking in git.git" report, August 2026 (#11). Originating author: Junio C Hamano. Thread type: integration status report with follow-up on individual topics.

Core problem or goal: Provide a snapshot of all in-flight topics across Git’s integration branches (`seen`, `next`, `master`), including graduated topics, new topics, stalled topics, and those under active review. The report serves as the authoritative reference for what is queued, what has merged, and what needs attention. Follow-up emails address specific topics, such as usability improvements to `git format-patch`.

Current status: As of August 2026 (#11), 56 topics graduated to `master` in the 19th batch since Git 2.55, totaling 563 non-merge commits on `master`. 75 topics are cooking in `next`, and 209 are in `seen`. The `kh/format-patch-range-diff-notes` topic, introduced in this update, is in `seen` and marked as "Waiting for response" in the report. It adds a `--[no-]range-diff-notes` option to `git format-patch` to control notes inclusion in range-diff output separately from patch notes.

Key technical details:
- **Graduated topics** (unchanged from prior summary):
  - `cc/fast-import-usage`, `kk/merge-base-exhaustion`, `ps/odb-streams`, `ss/repack-drop-filtered`, `js/packfile-fast-append`.
- **New options/configs**:
  - `git repack --drop-filtered` (from `ss/repack-drop-filtered`).
  - `git commit --amend` now refuses during conflict resolution (from `en/no-amend-during-conflicts`).
  - `git config --global` reads both `$HOME/.gitconfig` and `$XDG_CONFIG_HOME/git/config` (from `dw/config-read-both-global`).
  - **New**: `git format-patch --[no-]range-diff-notes` (from `kh/format-patch-range-diff-notes`), allowing users to suppress notes in range-diff output while retaining them on patches.
- **Behavioral changes**:
  - Reftable stack reloads avoid redundant `stat()` calls (from `kn/reftable-optimize-reloading`).
  - Alternates setup deferred to ODB creation time during clone (from `ps/odb-alternates-at-creation`).
  - Fsck checks moved into backend-specific layers (from `ps/odb-pluggable-fsck`).
  - **New**: Range-diff output no longer includes notes by default if `--range-diff-notes` is explicitly set to `false`.
- **Performance improvements**:
  - Merge-base optimization (`kk/merge-base-exhaustion`).
  - Reftable reload optimization (`kn/reftable-optimize-reloading`).
- **Pluggability efforts**:
  - ODB fsck (`ps/odb-pluggable-fsck`).
  - Packfile generation (ongoing).
- **Structural changes**:
  - `struct notes_opt` in `revision.h` renamed to `notes_opt_for_range_diff` (from `kh/format-patch-range-diff-notes`) to clarify its role.
- **Subsystems touched**: `odb`, `reftable`, `pack-objects`, `fast-import`, `config`, `merge-base`, `repack`, `commit`, `alternates`, `fsck`, `revision`, `format-patch`.
- **New symbols**:
  - `struct odb_stream` (from `ps/odb-streams`).
  - `--range-diff-notes` CLI option and `notes_opt_for_range_diff` struct member (from `kh/format-patch-range-diff-notes`).
- **Test coverage**: Updated in `kh/format-patch-range-diff-notes` to verify the new option’s behavior.

Open questions and loose ends:
- **Stalled topics** (unchanged from prior summary):
  - `ps/libgit-in-subdir`: Needs reroll due to merge conflicts.
  - `tb/pack-with-duplicates`: Needs further review.
  - `cl/regexec-macos-leak`: Requires reroll.
- **Topics needing review/response**:
  - `kh/format-patch-range-diff-notes`: Marked as "Waiting for response" in the report. Kristoffer Haugsbakk has replied to Junio’s query, but no further feedback is recorded in this email. The topic may require a reroll if additional adjustments are needed.
- **Architectural efforts**: ODB abstraction (`ps/odb-*` topics), reftable optimizations, and pluggable packfile generation remain ongoing but not yet complete.

Participant positions:
- Junio C Hamano (maintainer): Authored the report; no objections or conditional approvals noted in this email. Engaged with Kristoffer on `kh/format-patch-range-diff-notes` (query status: replied).
- Contributors of graduated topics (e.g., Christian Couder, Patrick Steinhardt, Jeff King, Elijah Newren, Taylor Blau): Implicitly approved via graduation to `master`.
- Contributors of stalled topics (e.g., Patrick Steinhardt, Taylor Blau, Carlo Marcelo Arenas Belón): Awaiting updates or review.
- Kristoffer Haugsbakk: Author of `kh/format-patch-range-diff-notes`. Position: Proposes a usability improvement to `git format-patch` with no known downsides. Review status: Awaiting further feedback from Junio or other reviewers.

Related work:
- ODB abstraction (`ps/odb-*`): Multi-series effort to make object storage pluggable.
- Reftable backend: Ongoing optimizations (`kn/reftable-optimize-reloading`).
- Pluggable packfile generation: Noted as ongoing in the report.
- Prior versions: Some topics (e.g., `ps/libgit-in-subdir`) were previously in flight but stalled due to conflicts or review feedback.

---