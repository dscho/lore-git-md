This is a user report, not a patch or review. The email comes from a Git user (Edvard) who was prompted by the Git CLI to send feedback after using the `whatchanged` command, which now requires the `--i-still-use-this` flag. Edvard notes that for their use case, alternatives like `git log --oneline --name-status` or `git diff --stat main` would suffice, and they express appreciation for Git's development and maintenance.

The context suggests this is a response to a recent change in Git where the `whatchanged` command (a legacy alias for `git log` with specific defaults) was deprecated or hidden behind the `--i-still-use-this` flag, likely to discourage its use in favor of more modern alternatives. The email does not propose a code change or engage with technical details—it is purely a user experience report. The tone is polite and constructive, but it does not advance any technical discussion or patch series.