This is a maintainer nudge on the `fetch.submoduleErrors` series (v6, 2 patches). Junio acknowledges that he previously identified and the author addressed the "obvious issues" (case-sensitivity, centralized parsing, test portability, and the recent mechanical improvements on array indexing and naming), but he has not performed a full review and is explicitly inviting others to step in. The series remains in "Needs review" status in "What's cooking" with no merge branch assigned. No new technical concerns are raised; the email is purely procedural, aimed at accelerating reviewer engagement. The patches touch `submodule.c`, `builtin/fetch.c`, documentation, and tests, and introduce a new config key (`fetch.submoduleErrors`) and CLI option (`--submodule-errors=(fail|warn)`) to control whether submodule fetch errors abort the parent fetch or merely warn. The implementation also fixes a pre-existing logic flaw in submodule fetch error handling.