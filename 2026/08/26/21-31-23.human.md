This is Junio Hamano’s call for review on the second patch in Erik Cervin-Edin’s v2 series, which completes the `--fixup` message-option support by adding `-c` and `-C`. The patch itself is ready—it removes the last artificial restriction, routes `-c/-C` through the same `prepare_to_commit()` path as the earlier `-m/-F` patch, and includes thorough tests—but it has been sitting unreviewed for some time. Junio’s only comment is a small code-cleanup suggestion: the `die_for_incompatible_opt3()` call is redundant because the subsequent `die_for_incompatible_opt4()` already catches the same combinations. The rest of the patch (behavior, tests, docs) is not questioned. If someone steps up to confirm the logic, this series looks ready to cook.