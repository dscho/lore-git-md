This is a **review feedback** email from Junio C Hamano on Elijah Newren’s bugfix patch targeting the merge machinery. The patch extends Git’s existing protection against `git commit --amend` during conflict resolution to cover `git am`, `git revert`, and all forms of `git rebase`, preventing users from accidentally amending the wrong commit.

Junio’s feedback is **surface-level** but precise: he suggests replacing the global `the_repository` with the repository instance already available in the `wt_status` struct (`s->repo`). This is a minor cleanup that aligns with the ongoing `the_repository` removal effort, though it does not affect the patch’s logic or correctness. The suggestion applies to all repository-access calls in the new block, not just the `REVERT_HEAD` check shown in the quoted diff.

The email does not revisit the patch’s core design or raise new concerns. It assumes the reader is already familiar with the thread’s context (the patch’s goal, the existing protection logic, and Phillip Wood’s earlier suggestion to refactor conflict-resolution checks into `sequencer.c`). Junio’s tone is matter-of-fact, treating the patch as correct and the feedback as a routine improvement rather than a blocker.

No merge status is implied; the patch remains under review, with a v2 likely if Elijah incorporates this and Phillip Wood’s earlier feedback. The thread’s open questions (whether to refactor the checks into `sequencer_determine_whence()` and future work on `git commit <paths>`) are unaffected by this email.