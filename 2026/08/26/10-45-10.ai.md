This is the final patch in the v9 series improving error messages for ambiguous remote branch names in `git worktree add`, `git checkout`, and `git switch`. It specifically addresses the `--guess-remote` option in `git worktree add`, which previously succeeded silently when multiple remote branches matched the given name. The patch now treats this scenario as an error, aligning the behavior with `git checkout` and `git switch`.

The change updates `dwim_branch()` to accept an `add_opts` struct (for `--quiet` support) and extends it to detect multiple matches via the `unique_tracking_name()` helper. When ambiguity is found, the function emits the same advice message introduced earlier in the series and calls `die()` with a clear diagnostic. The patch also updates the documentation for `worktree.guessRemote` and `git worktree add` to reflect the new behavior, and adds a test case in `t/t2400-worktree-add.sh` to verify the error is triggered correctly.

This completes the series’ command coverage and resolves the last known edge case. The implementation is clean, with no remaining open questions or objections. The series is merged to `next` and ready for `master`. The follow-up item for the one-argument `git worktree add` case (e.g., `git worktree add ../topic-branch`) remains scoped for a separate commit.