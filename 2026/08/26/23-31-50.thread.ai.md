Thread identity
Subject: [PATCH 0/2] Introduce die_for_incompatible_opts() helper
Series version: v2 (revised submission; parameter reordering patch dropped, va_end() fixed, EOF sentinel adopted)
Originating author: Junio C Hamano
Thread type: patch series (refactoring)

Core problem or goal
Replace the fixed-arity `die_for_incompatible_optN()` family (N = 2, 3, 4) with a single varargs helper `die_for_incompatible_opts()` that can handle an unbounded number of mutually exclusive command-line options. The change is purely internal API refactoring; no user-visible behavior is altered.

Current status
Under review (revised version). The series now consists of a single patch implementing the agreed design:
- The varargs helper accepts `<int, const char *>` pairs terminated by `EOF` (-1).
- Static inline wrappers for 2–4 options are retained for compile-time safety.
- The `va_end()` omission from v1 is fixed.
- The hard-coded error-message formatting (capped at four options) remains unchanged to avoid i18n breakage.
- The parameter-reordering patch from v1 is dropped.

Key technical details
- Files touched: `parse-options.c`, `parse-options.h`; 14 built-in commands (`add`, `clone`, `commit`, `difftool`, `gc`, `grep`, `log`, `merge-tree`, `pack-objects`, `push`, `repack`, `replay`, `rev-list`, `show-ref`) and the `revision` subsystem use the new helper through existing wrappers, so their call sites do not change.
- New symbol: `die_for_incompatible_opts()` (varargs, EOF-terminated).
- Renamed symbols: none; the fixed-arity helpers remain with their original signatures.
- Old-vs-new behavior: identical; only the internal calling convention changes.
- On-disk format: unaffected.
- Test coverage: none mentioned; the change is plumbing-only and expected to be safe.

Open questions and loose ends
- **Compile-time safety**: Jeff King (Peff) continues to question the value of the patch given no callers currently need more than four options, but now focuses on the design trade-off between compile-time safety (fixed-arity wrappers) and flexibility (varargs helper). He advocates for `LAST_ARG_MUST_BE_NULL` (a sentinel macro that enforces a `NULL` terminator at compile time) over the `EOF` sentinel, arguing that the varargs helper, unlike the fixed-arity wrappers, cannot enforce argument correctness at compile time. The static inline wrappers mitigate this risk for the common cases (2–4 options), but the varargs version itself remains less safe. Peff accepts the design as internal and low-risk, framing his feedback as a design preference rather than a correctness issue.
- **Sentinel choice**: The design uses `EOF` (-1) as the sentinel, preserving existing fixed-arity signatures. This resolves Peff’s earlier concern about compile-time protection for the common cases (2–4 options), but Peff now proposes `LAST_ARG_MUST_BE_NULL` as a safer alternative.
- **Hard-coded error-message formatting**: The error-message formatting remains hard-coded for four options, despite the varargs helper’s flexibility. Junio defers dynamic allocation to avoid breaking i18n message templates; Peff accepts this rationale ("Yeah, that makes sense").
- **Future-proofing**: The new helper is designed to handle more than four options, but no callers currently need this capability.

Participant positions
- **Jeff King (Peff)**: Substantive review engaging with two trade-offs: compile-time safety and i18n compatibility. Initially raised concerns about the loss of compile-time protection in the varargs helper but now focuses on advocating for `LAST_ARG_MUST_BE_NULL` as a safer sentinel. Accepts Junio’s rationale for retaining hard-coded error-message formatting to avoid i18n breakage. Feedback remains framed as a design preference rather than a correctness issue.
- **Elijah Newren**: Substantive review identifying the `va_end()` omission in v1. No other concerns raised; the rest of the patch (parameter reordering, error message formatting, call-site updates) is implicitly accepted.
- **Junio C Hamano**: Originating author. Acknowledged the `va_end()` omission and incorporated the fix in v2. Proposed and implemented the revised design (EOF sentinel, dropped parameter reordering). Retains the hard-coded error-message formatting to avoid i18n breakage. No objections to the core design.

Related work
None.