This is a user report, not a patch or review. A Git user named Edvard writes to the mailing list after being prompted by the `git whatchanged` command, which now requires the `--i-still-use-this` flag. He notes that for his use case, alternatives like `git log --oneline --name-status` or `git diff --stat main` would work just as well. The email is brief, polite, and expresses appreciation for Git’s development and maintenance.

The context here is that `git whatchanged`—a legacy command that predates `git log`—has been deprecated for years. Recent versions of Git now require an explicit flag (`--i-still-use-this`) to discourage its use in favor of more modern alternatives like `git log` or `git diff`. Edvard’s message doesn’t propose any changes or ask for help; it’s simply a confirmation that the deprecation notice is working as intended and that users can adapt to the recommended replacements. There’s no controversy or technical discussion here—just a routine user interaction with a long-planned deprecation.