This thread is a user feedback discussion about Git’s recent deprecation of the `whatchanged` command, which now requires the `--i-still-use-this` flag to run. The original report came from a user, Edvard, who shared that the change doesn’t disrupt their workflow and that modern alternatives like `git log --oneline --name-status` or `git diff --stat main` work well for them. The tone was appreciative, and no technical objections or requests for changes were raised.

Kristoffer Haugsbakk, a frequent documentation contributor, followed up to clarify the deprecation message’s evolution and to dig deeper into what Edvard values about `whatchanged`—whether it’s the output format or the command name itself. He suggested an alternative alias (`git log --stat --no-merges`) that might better align with Edvard’s workflow if muscle memory is the main concern. The discussion remains focused on understanding user needs rather than proposing technical changes, and no decisions or action items have emerged yet. The thread is still in the feedback-gathering phase.