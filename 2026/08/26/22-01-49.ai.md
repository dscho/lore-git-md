**Summarizer brief mode**

This is the maintainer’s final call for review on the **v6 iteration of a security feature patch** introducing `http.sslVerifyStatus`, a boolean config option (default `false`) that enables OCSP staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`. The patch is **technically complete**, with all prior feedback addressed, including Junio C Hamano’s request for clearer documentation wording in v6. No substantive objections remain, and the patch is **ready for `next`** pending final maintainer approval.

Key technical details carried forward:
- **Config key**: `http.sslVerifyStatus` (boolean, default `false`), with per-URL override support (e.g., `http.https://example.com/.sslVerifyStatus`).
- **Libcurl requirement**: Uses `CURLOPT_SSL_VERIFYSTATUS` (libcurl ≥ 7.41.0, below Git’s minimum version).
- **Files touched**: `http.c` (core logic), `Documentation/config/http.adoc` (updated in v6 to emphasize user-facing outcome), and `t/t5551-http-fetch-smart.sh` (four new tests covering fail-closed behavior, per-URL scoping, and edge cases).
- **Error handling**: Includes libcurl error strings (`curl_easy_strerror()`) to distinguish missing features (`CURLE_NOT_BUILT_IN`) from other failures.
- **Backend parity**: Recent libcurl versions (≥ 8.10) unify GnuTLS and OpenSSL behavior, obsoleting prior backend-specific caveats.
- **Test coverage**: Tests in `t5551` verify fail-closed behavior, per-URL scoping, and edge cases; Patrick Steinhardt’s proposed positive test case (valid staples) remains deferred.

The patch targets a **niche but critical security gap**: OpenSSL-linked Git binaries (common in FIPS-compliant deployments like US Department of Defense PKI) currently ignore OCSP staples, leaving revoked certificates unchecked. When enabled, `http.sslVerifyStatus` enforces fail-closed behavior, aligning with government and enterprise security mandates. The default-off design ensures backward compatibility, while the feature’s availability addresses a concrete requirement for users who mandate OCSP stapling.

Junio’s email signals **no further technical changes are expected** and the patch is poised for graduation. The only open question—whether to merge Patrick Steinhardt’s additional test harness for positive cases—has been deferred to maintainer discretion. The thread is now in a **holding pattern for final approval**, with no outstanding review feedback.