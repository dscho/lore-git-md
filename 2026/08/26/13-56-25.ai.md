This is a **substantive review** of a **bugfix** patch targeting the merge machinery. Phillip Wood provides detailed feedback on Elijah Newren’s patch, which extends Git’s protection against `git commit --amend` during conflict resolution to cover `git am`, `git revert`, and all forms of `git rebase`. The patch is well-motivated and addresses a real user-facing foot-gun, but Phillip suggests a structural improvement to the implementation.

The core of Phillip’s review is a **design-level suggestion**: move the conflict-resolution checks out of `builtin/commit.c` and into `sequencer_determine_whence()` to avoid hardcoding paths and improve maintainability. He notes that this would also allow the logic to cover `git am` and the apply-based rebase backend more cleanly. The suggestion is framed as a preference for cleaner code organization rather than a correctness issue, and Phillip explicitly states that the current logic is sound.

Phillip also clarifies an edge case: the patch correctly allows `--amend` for conflicted `fixup` operations during interactive rebase, which is the intended behavior. His feedback is **technical and constructive**, focusing on code structure and long-term maintainability rather than surface-level issues. The review is **not tested** (no mention of running the code), but it engages deeply with the patch’s logic and design.

The patch itself remains uncontroversial in its goal—preventing accidental `--amend` during conflict resolution—but Phillip’s suggestion could lead to a v2 if Elijah agrees. The thread is still in the early review phase, with no other participants yet. Key files touched include `builtin/commit.c` and three test scripts (`t3404-rebase-interactive.sh`, `t3507-cherry-pick-conflict.sh`, and `t4151-am-abort.sh`). No new CLI options or on-disk format changes are involved.