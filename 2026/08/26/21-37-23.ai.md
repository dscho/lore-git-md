This is a maintainer ping from Junio C Hamano about the third patch in Taylor Blau’s MIDX incremental-writes series. The thread context shows that the only remaining blocker is a trivial commit-message tweak to clarify that the old invariant (“at most one of m and ctx->to_include are set”) no longer holds when `--stdin-packs --incremental --base=<foo>` is used. Junio is asking whether that update has been made so the series can move forward. No technical issues remain; the patch is otherwise ready for integration.