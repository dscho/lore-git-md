This is a **bugfix** targeting the merge machinery, part of Elijah Newren’s ongoing effort to prevent users from accidentally corrupting repository state with `git commit --amend` during conflict resolution. The thread is a review exchange between Junio C Hamano and Elijah, with Junio acknowledging the patch’s sound logic but raising a broader question: whether Git should also block `git commit <paths>` during conflict resolution, not just `--amend`.

Junio’s feedback is substantive but not a blocker. He agrees the current patch is correct and well-reasoned, but suggests a follow-up topic to address the related issue of partial commits (`git commit <paths>`) during conflict resolution. He proposes leaving a `#leftoverbits` marker in the commit message to flag this as unfinished business. The distinction is clear: the current patch focuses on preventing `--amend` from corrupting history, while the follow-up would enforce that conflict resolution always commits the entire tree.

The patch itself remains uncontroversial and technically solid. It extends existing protections to `git am`, `git revert`, and all `git rebase` variants, carefully distinguishing between genuine conflict stops and interactive rebase’s internal use of `--amend` (for `squash`, `reword`, `edit`, and `break`). The logic relies on files in `.git/rebase-merge/` to determine whether the user is in a conflicted state or a clean stop, and the commit message explains this clearly. No changes to the patch are needed for this version, but a future iteration might add the `#leftoverbits` marker if Elijah agrees.

This is a well-crafted, narrowly scoped fix that addresses a real user pain point. It looks ready for `next` once the minor follow-up discussion is resolved.