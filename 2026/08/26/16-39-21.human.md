This is a **review comment** from Junio C Hamano on Elijah Newren’s bugfix patch, which extends Git’s protection against `git commit --amend` during conflict resolution. The patch itself is uncontroversial and well-motivated, but Junio’s feedback here is **surface-level**, focusing on code hygiene rather than the patch’s logic or design.

Junio points out that the patch uses `the_repository` (the global repository instance) in several places where it could instead use `s->repo` (the repository instance already passed to the function via the `wt_status` struct `s`). This is a minor stylistic nit: the change would align the code with the ongoing `the_repository` removal effort, making it more maintainable and consistent with the rest of the codebase. The suggestion is mechanical and does not affect the patch’s behavior or correctness.

The review is brief and targeted, with no objections to the patch’s substance. If Elijah incorporates this feedback, it would likely be in a follow-up iteration (v2), but the patch is otherwise ready for integration. No merge status is implied; the topic remains under review.