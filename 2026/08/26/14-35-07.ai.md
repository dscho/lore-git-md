This is a **commit-message clarification** in the final patch of René Scharfe’s four-patch series fixing and cleaning up `worktree_basename()` in `git worktree add`. The patch itself—renaming `worktree_basename()` to `worktree_basename_dup()` and making it return a freshly allocated string copy—remains unchanged and has Junio C Hamano’s approval ("OK"). The only adjustment is to the commit message: Junio flagged ambiguity in the phrase “Two already do,” which could mislead readers into thinking two callers already free the returned string (a crash), when in fact they make their own copies. René accepts Junio’s suggested rewording to clarify that two callers already copy the string, making their conversion to the new `_dup()` variant straightforward. Junio will apply the rewording locally while waiting to see if the rest of the series attracts further review. The series is now effectively ready to advance, pending this minor documentation tweak. No new technical discussion or test changes are introduced here.