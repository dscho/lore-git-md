This is a documentation patch correcting the `gitremote-helpers` man page. The patch updates the description of the `option pushcert` setting to include the `if-asked` value, which was already implemented in the codebase (commit `30261094b1f7fdcba3b7a1`) but omitted from the documentation. The change is minimal—one line in `Documentation/gitremote-helpers.adoc`—and aligns the documentation with the actual behavior of the `transport-helper.c` code.

The commit message is clear and references both the implementation commit and the commit that introduced the documentation (though the latter appears to be a typo or misreference, as `b9299a2bb1` does not correspond to a documentation addition). The patch itself is straightforward and uncontroversial, addressing a long-standing discrepancy between code and documentation.

This is part of the remote-helpers subsystem, which allows Git to interact with foreign version control systems via helper programs. The `pushcert` option controls whether GPG-signed pushes are sent, and the `if-asked` value enables conditional signing based on server requests. The fix ensures users and helper authors are aware of all available values.

No prior discussion or thread context is provided, so this appears to be a new submission. The patch is well-scoped and unlikely to generate debate, as it merely corrects an oversight in the documentation. It touches no code paths, so there are no behavioral or compatibility concerns.