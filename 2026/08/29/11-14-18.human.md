This is a refactoring patch discussion in the `die_for_incompatible_opts()` series, now in its second iteration. Jeff King (Peff) replies to Junio C Hamano’s proposed design adjustment for the varargs helper, raising a concern about compile-time safety and questioning the value of the patch if the varargs version isn’t directly exposed to callers.

Peff acknowledges Junio’s plan to retain the fixed-arity wrappers (`die_for_incompatible_opt2()` through `die_for_incompatible_opt4()`) and use `EOF` (-1) as the sentinel for the varargs helper. However, he points out that this approach loses the compile-time protection that a `NULL` sentinel would provide, making the varargs interface less safe. Since no callers currently need more than four options, he questions whether the added complexity is justified. He also accepts Junio’s reasoning for keeping the hard-coded error-message formatting to avoid breaking i18n, but the core tension remains: the patch introduces a varargs helper that isn’t strictly necessary for existing use cases, and the safety trade-offs may not be worth it.

The discussion is technical and low-stakes, with both participants aligned on the broader goal. The open question is whether the series should proceed as-is or be reconsidered for minimalism. No new behavioral concerns or edge cases are introduced; the focus is purely on internal API design. The thread remains under review, with no clear resolution yet.