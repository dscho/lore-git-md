This is a refactoring patch discussion in the ongoing `die_for_incompatible_opts()` helper series. Junio responds to Jeff King’s (Peff) feedback with a playful but pointed technical challenge: he demonstrates how the current fixed-arity `die_for_incompatible_optN()` functions can be composed to enforce mutual exclusivity among *any* number of options (not just 2–4), but notes that this approach lacks compile-time safety. The example shows that while the logic is sound, the fixed-arity design forces awkward manual composition, reinforcing the case for a varargs helper—even if no callers currently need more than four options.

Junio’s reply also acknowledges Peff’s earlier point about compile-time safety: the varargs version cannot enforce argument correctness at compile time (unlike the fixed-arity wrappers), but he implies this trade-off is acceptable for an internal API. The discussion remains focused on design trade-offs rather than correctness, with no new objections raised. The series is now effectively waiting for Peff’s reaction to Junio’s compromise (EOF sentinel + static inline wrappers for 2–4 options), but the tone suggests convergence is near. No other participants have weighed in on this iteration.