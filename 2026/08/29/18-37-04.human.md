This is a documentation patch. Lorenz Leutgeb corrects the `gitremote-helpers` man page to reflect that the `pushcert` option accepts not only `true` or `false`, but also `if-asked`. The missing value was added to the code in commit b9299a2bb1, but the documentation was never updated. The fix is a one-line change in `Documentation/gitremote-helpers.adoc`.

The patch is well-motivated: it cites the exact code location (`transport-helper.c:1009-1018`) and the commit that introduced the feature. No controversy is expected; this is a straightforward alignment of documentation with implementation.