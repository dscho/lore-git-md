This thread is a simple documentation fix for the `gitremote-helpers` man page. Lorenz Leutgeb noticed that the `option pushcert` setting in the documentation was missing the `if-asked` value, even though this option has been supported in the code since commit `30261094b1f7fdcba3b`. The patch adds the missing value to align the documentation with the actual behavior.

The change is minimal—just one line in `Documentation/gitremote-helpers.adoc`—and addresses a long-standing discrepancy. The commit message is clear, though it references a commit (`b9299a2bb1`) that doesn’t seem to exist, which might be a typo. Since this is purely a documentation update with no code changes, there are no behavioral or compatibility concerns, and the patch is unlikely to spark debate. It’s a straightforward correction that should be uncontroversial.