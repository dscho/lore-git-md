This is a review comment in an ongoing thread about optimizing partial clone behavior in `git cherry` and `git grep`. Phillip Wood notes that `git rebase` without `--reapply-cherry-picks` suffers from the same performance issue when doing cherry-pick detection, and asks whether the new `prefetch_cherry_blobs()` function could be shared with the cherry-pick detection code in `revision.c`.

The review is substantive - it identifies a related use case that could benefit from the same optimization and questions whether the implementation could be generalized. This isn't just surface-level nitpick, but a thoughtful consideration of how the change might be extended to benefit more commands.

The original patch series (now complete) implemented batch blob prefetching for `git cherry` to avoid the performance penalty of individual on-demand fetches in partial clones. Phillip's observation suggests the optimization could have broader applicability, though the current implementation is specifically tied to the `git cherry` command's internals.

No technical concerns are raised about the existing implementation - the focus is entirely on potential for reuse. The thread appears ready for merging, with this being a forward-looking consideration rather than a blocking issue.