This is a technical discussion thread about the `--reverse=before` revision walk patch, where Mirko Faina responds to Ben Knoble's detailed review feedback. The exchange focuses on implementation details and edge cases for the new revision walking behavior.

Key points in this response:

1. **Backward compatibility**: Mirko acknowledges Ben's concern about backward compatibility with multiple `--reverse` flags and agrees to address this in v2, particularly documenting any behavior changes that might affect aliases.

2. **Code clarity**: Mirko confirms he'll add `#define` constants for the reverse state values (0,1,2) to improve readability, as suggested by Ben.

3. **max_count handling**: The most substantive discussion centers on the `max_count` decrementing behavior for `--reverse=before` mode. Mirko provides a detailed explanation of why this is necessary for correctness - unlike `--reverse=after` which uses `get_revision_internal()`'s built-in counting, `--reverse=before` must manually track the count when popping commits from the reversed list.

4. **Control flow**: Mirko clarifies the two-phase operation (walk phase and output phase) and how `max_count` interacts differently with each, particularly noting that `get_revision_internal()` isn't called during the output phase for `--reverse=before`.

The discussion remains highly technical, with both parties demonstrating deep engagement with the revision walking machinery. Mirko shows particular patience in explaining the complex control flow interactions that motivated his implementation choices. The thread shows progress toward consensus on the remaining open questions, with v2 expected to address the documentation and code clarity points raised.

Key files remain `revision.c` (state handling) and `list-objects.c` (traversal logic), with the conversation now focused on finalizing these implementation details before the next version. The exchange exemplifies the Git project's culture of thorough, patient technical discussion even around complex control flow issues.