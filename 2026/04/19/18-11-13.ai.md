This is a technical discussion thread about the `--reverse=before` revision walk patch, where author Mirko Faina responds to Ben Knoble's review feedback. The exchange focuses on implementation details and edge cases in the revision walking machinery.

Mirko addresses several specific concerns raised in Ben's review:

1. **Documentation clarity**: Acknowledges the confusing "when" reference in the man page text and commits to fixing it in v2.

2. **Multiple reverse handling**: Explains why the original toggle behavior for multiple `--reverse` flags doesn't make sense with the new `before/after` options, since the ordering is now determined before the walk begins.

3. **Error state handling**: Defends the current approach of defaulting to standard reverse (value 1) when an invalid state (3) is encountered, but asks whether reviewers prefer a different approach.

4. **Enum vs bit-packing**: Open to adding macros for better readability of the state values if reviewers think it's necessary.

5. **max_count decrementing**: Provides a detailed explanation of why `max_count` needs manual decrementing during the reverse output stage, relating it to the interaction between `get_revision()` and `get_revision_internal()`.

The discussion remains technical and collaborative, with Mirko demonstrating a solid understanding of the revision walking internals while remaining open to feedback. The thread shows progress toward a v2 that will incorporate both Ben Knoble's edge case findings and Tian Yuchen's earlier suggestions about memory optimization. Key files involved remain `revision.c` (state handling) and `list-objects.c` (traversal logic).