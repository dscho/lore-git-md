This is a technical discussion about rewriting `git subtree` in Rust, emerging from an ongoing thread about recursion limits in the shell script implementation. Ian Jackson shares progress on a Rust prototype that uses libgit2, noting it's significantly faster than the shell version but raises several design questions.

Key points from Ian's email:
- The prototype shows promising performance but needs more validation of output correctness and test coverage
- The rewrite would necessarily change some metadata handling (like committer info) which the current implementation preserves in ways that may be problematic
- Packaging questions remain open - whether to integrate with git.git or keep as a standalone Rust crate
- The approach would likely expand to all `git-subtree` subcommands, not just the split operation

The email continues the thread's evolution from discussing Colin Stagner's shell script fixes to considering a more fundamental architectural change. Ian positions this as a potential long-term solution to the maintenance challenges Junio raised earlier, though it would require someone to own the Rust implementation going forward. The technical details suggest this would be a non-trivial effort with implications for commit metadata and cross-distribution compatibility.