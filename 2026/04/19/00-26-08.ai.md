This is a review comment from Elijah Newren on Taylor Blau's pseudo-merge bitmap bugfix series. The review focuses on documentation and example consistency for the `sampleRate` configuration parameter.

Elijah notes that while the code now properly rejects a sampleRate of 0 (to prevent division-by-zero), the documentation in `bitmap-pseudo-merge.adoc` still incorrectly states that 0 is an allowed value. He also points out two examples in `gitpacking.adoc` that use `sampleRate = 100`, which would be invalid since the parameter must be between 0 and 1 (and now must be greater than 0).

The review is substantive - it identifies real documentation mismatches that would mislead users about valid configuration values. Elijah is catching follow-up work needed to keep the docs in sync with the code changes, which is particularly important for configuration parameters that affect core behavior. This continues his pattern of thorough technical review throughout this series, having previously caught test initialization ordering and API type consistency issues.