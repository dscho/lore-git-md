This is a follow-up in the thread about commit signing regressions for non-UTF-8 messages. brian m. carlson responds to the bug report, agreeing that the bisect points to commit 6206089cbd as the culprit but questioning whether unconfigured non-UTF-8 commit messages are a valid use case in the first place.

The key technical point is that the problematic commit changed the order of operations in `commit_tree_extended`, moving signature generation before UTF-8 validation. brian acknowledges the function naming issue (`verify_utf8` actually mutates data) and proposes a two-patch solution: first renaming the function to `ensure_utf8` to better reflect its behavior, then fixing the underlying issue.

This moves the discussion forward by:
1. Confirming the regression analysis
2. Identifying an additional code clarity issue (misleading function name)
3. Outlining a concrete plan to address both problems

The response suggests the issue will be fixed, though with an implicit stance that non-UTF-8 messages should ideally have explicit encoding configuration. The promised patch series should resolve the immediate signing problem while improving the code's clarity.