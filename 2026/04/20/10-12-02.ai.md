This is patch 4/8 in Karthik Nayak's series refactoring Git's reference backend system. The patch moves the `print_rejected_refs()` function earlier in `builtin/update-ref.c` to avoid needing a forward declaration in a future patch that will reuse this function.

The change is purely mechanical - the function is relocated without any modifications to its implementation. The commit message explains this is needed because the function will be used by other parts of the code in upcoming changes. This aligns with the series' incremental approach of making small, focused changes that prepare for larger refactorings while keeping the code functional at each step.

The patch touches only `update-ref.c` and doesn't yet introduce the broader changes hinted at in the thread context (standardizing object validation and peeling across ref backends). This appears to be groundwork for those later changes, keeping the series' changes digestible for reviewers.