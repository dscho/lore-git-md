This is a technical follow-up in the `--reverse=before` revision walk thread, where Jeff King (Peff) provides implementation guidance for handling multiple `--reverse` flags and backward compatibility.

Peff suggests a concrete solution for the tricky case of multiple `--reverse` flags, proposing to:
1. Maintain backward compatibility for `--reverse --reverse` (which currently cancels out)
2. Add `--no-reverse` as an explicit way to cancel reversal
3. Make `--reverse=before` and `--reverse=after` override rather than combine
4. Fix the parsing to reject malformed inputs like `--reverse o-matic`

The proposed diff shows how to implement this in `revision.c` using enum values for the different reverse modes. Peff notes this would be simpler with `parse-options` but recognizes that's a larger refactor beyond the scope of this patch.

This continues the thread's pattern of thorough technical discussion, with Peff's authoritative review helping ensure the new feature fits cleanly into Git's existing revision walking machinery while maintaining backward compatibility. The exchange demonstrates how even small behavioral edge cases receive careful consideration in Git's development process.