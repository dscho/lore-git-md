This is a brief follow-up in the `--reverse=before` revision walk patch series, where Mirko Faina corrects his initial misunderstanding of Jeff King's proposed implementation.

Mirko had initially thought the `--reverse` vs `--reverse=<value>` parsing would require separate logic paths due to prefix matching concerns, but realizes upon re-reading that Peff's solution using `strcmp()` for the exact match case and `skip_prefix()` for the parameterized form already handles this cleanly. This marks the resolution of the last minor implementation question before the v2 submission.

The exchange demonstrates how even experienced contributors can initially misread subtle parsing requirements, and how clear code examples in reviews help catch these cases early. With this clarification, all technical aspects of the enum-based state machine approach are now settled, and the thread awaits the updated patch incorporating the agreed design.