This is a follow-up in the `--reverse=before` revision walk patch series, where Mirko Faina responds to Jeff King's technical review about handling multiple `--reverse` flags.

Mirko confirms he'll implement Peff's suggested enum-based state machine for the `--reverse` option, which introduces three states (no reverse, reverse after, reverse before) and adds a `--no-reverse` option for explicit negation. He notes the need to handle the parsing carefully since `--reverse` is a prefix of `--reverse=`, requiring separate logic paths.

The discussion has now resolved all major design questions about the flag's behavior:
- Multiple `--reverse` flags will follow last-one-wins semantics
- The historical `--reverse --reverse` cancellation behavior is preserved
- Invalid values for `--reverse=` will be rejected
- Documentation will clearly explain both old and new behaviors

Mirko indicates these changes will appear in v2 of the patch, which should be the final iteration before merging. The only remaining loose end is Tian Yuchen's performance optimization suggestions, which are now clearly marked as future work separate from the core functionality.

This exchange demonstrates how substantive technical review from domain experts (Peff on revision walks) can efficiently converge complex functionality while maintaining backward compatibility. The thread shows all signs of wrapping up cleanly with v2.