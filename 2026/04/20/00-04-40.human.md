This is a review of a patch adding `--reverse=before` functionality to Git's revision walk machinery. Jeff King (Peff) identifies an issue with the command-line argument parsing implementation.

The review points out that the current patch incorrectly handles the `--reverse` flag by allowing both `--reverse=after` and `--reverse after` forms, which violates Git's CLI conventions. Peff notes this creates ambiguity and could silently accept invalid input. He recommends separating the handling of `--reverse=` (with equals) from plain `--reverse` to maintain consistency with Git's established patterns.

This is a substantive review that catches a real issue in the patch's argument parsing logic. Peff demonstrates his deep knowledge of Git's CLI conventions by referencing the `gitcli` manpage and explaining the potential pitfalls of the current implementation. The feedback is specific and actionable, suggesting a clear path forward for the patch author.

The review doesn't address the core functionality of the `--reverse=before` feature itself, focusing instead on this important implementation detail that needs fixing before the patch can be merged. This level of attention to CLI consistency is characteristic of Peff's thorough review style.