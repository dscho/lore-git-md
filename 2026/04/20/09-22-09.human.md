This is a follow-up in the thread about adding `--reverse=before` functionality to Git's revision walk machinery. The patch author, Mirko Faina, is responding to Jeff King's review about command-line argument parsing.

The key development here is that Mirko acknowledges Jeff's point about Git's convention requiring equals-separated arguments (`--reverse=after`) rather than space-separated ones (`--reverse after`). He confirms understanding that supporting both forms would create ambiguity and agrees to simplify the implementation by only allowing the stuck form. This resolves the last open question about the interface design.

The exchange shows the technical consensus solidifying - Mirko is incorporating Jeff's authoritative feedback about Git's CLI conventions. This was the final substantive issue before the patch can be revised and resubmitted. The thread demonstrates how experienced reviewers like Jeff help maintain consistency in Git's command-line interface while allowing new functionality to be added.