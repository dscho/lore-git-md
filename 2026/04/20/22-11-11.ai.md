This is a follow-up in the thread about commit signing regressions for non-UTF-8 messages. brian m. carlson responds to the bug report, agreeing that the bisect points to commit 6206089cbd as the root cause but questioning whether unconfigured non-UTF-8 commit messages are a valid use case at all.

The key technical points:
- Acknowledges the bisect result but notes the patch was modified from their original version
- Points out the problematic behavior of `verify_utf8()` actually mutating the buffer (transcoding) rather than just validating
- Announces an upcoming two-patch series to:
  1. Rename the misleading `verify_utf8()` to `ensure_utf8()`
  2. Fix the underlying signing issue

The response suggests the reporter's analysis is correct but implies the solution may involve more than just reverting the operation order - the function's behavior itself may need reconsideration. The planned patches aim to both clarify the code (through renaming) and address the signing regression, though the exact technical approach isn't specified yet.

This moves the thread forward by confirming the regression while hinting at a more comprehensive fix that addresses both the immediate issue and the underlying confusing API. The tone is constructive but firm about what constitutes valid input handling.