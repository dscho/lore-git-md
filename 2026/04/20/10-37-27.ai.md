This is a follow-up in the thread about adding a `--missing-only` flag to `git rev-list` for partial clone workflows. The author Siddharth Asthana responds to Phillip Wood's review suggestions, confirming he will:

1. Adopt the `--missing-only` flag approach instead of extending the `--missing` enum, seeing it as more orthogonal and self-documenting. He's considering whether to require explicit `--missing=print` when using `--missing-only` to make behavior unambiguous.

2. Simplify the test verification by using direct OID file comparison (`test_cmp`) rather than multiple `grep` calls, as Phillip suggested. This makes the test both more efficient and provides better failure diagnostics.

The response shows the author engaging substantively with the technical feedback while maintaining the thread's constructive tone. With these changes planned for v2, the implementation appears to be converging toward a clean solution that addresses GitLab's Gitaly use case while following Git's design principles. The discussion has successfully refined both the interface design and testing approach through collaborative review.