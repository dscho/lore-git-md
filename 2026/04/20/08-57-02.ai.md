This is a review discussion about the proposed `--missing=print-only` mode for `git rev-list`, which would output only missing object IDs without prefixes for partial clone workflows.

Phillip Wood builds on Patrick Steinhardt's earlier review with two concrete suggestions to improve the implementation:

1. **Alternative design approach**: Proposes a separate `--missing-only` flag rather than extending the `--missing` enum, arguing this would be more self-documenting than `--missing=print-only` and could avoid future enum bloat (like the hypothetical `MA_PRINT_INFO_ONLY` Patrick mentioned). This suggests rethinking the interface design rather than just tweaking the current implementation.

2. **Test optimization**: Offers a simpler verification method for the test case - instead of using multiple `grep` calls to check present objects (which Patrick noted could be inefficient), Phillip suggests directly comparing the output to an expected single OID file. This would make the test both more efficient and provide better failure diagnostics.

The exchange shows the Git community's attention to both API design clarity and test quality. Phillip's suggestions aim to address Patrick's concerns about naming and test performance while keeping the discussion focused on practical improvements. Both reviewers are engaging substantively with the technical details while maintaining a constructive tone.

This continues the thread's pattern of high-quality technical discussion about a feature that multiple stakeholders (including GitLab's Gitaly service) have a use case for. The author will need to consider these design and testing suggestions when preparing a v2 submission.