This is a brief technical discussion about the proposed `--missing=print-only` mode for `git rev-list`, continuing the thread about partial clone workflows.

Patrick Steinhardt concurs with Phillip Wood's earlier suggestion to use a separate `--missing-only` flag rather than extending the `--missing` enum with a `MA_PRINT_ONLY` value. This agreement reinforces the design concern that enum-based approaches may lead to unclear semantics and future bloat (as seen with Patrick's hypothetical `MA_PRINT_INFO_ONLY` case). The exchange shows consensus forming around Phillip's proposed alternative interface design, which would make the feature's behavior more self-documenting while avoiding enum proliferation.

The discussion remains focused on implementation cleanliness rather than challenging the feature's core utility for GitLab's Gitaly service. With both Patrick and Phillip now aligned on the flag-based approach, the next expected step would be for the original author to submit a v2 patch incorporating this design change along with the previously suggested test optimizations. The thread continues to demonstrate the Git community's attention to API design quality in even small interface decisions.