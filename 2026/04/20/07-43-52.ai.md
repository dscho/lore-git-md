This is a follow-up discussion on the `--missing=print-only` feature for `git rev-list`, which aims to simplify partial clone workflows by outputting only missing object IDs without prefixes. Patrick Steinhardt provides substantive technical feedback on the initial implementation.

Key points from Patrick's review:
1. Naming concerns - suggests "print-exclusively" might better convey the behavior than "print-only", though acknowledges no perfect alternative exists
2. Implementation details - questions whether future variants like `MA_PRINT_INFO_ONLY` might be needed
3. Code quality - proposes renaming `missing_action_prints()` to `should_print_missing_object()` for clarity
4. Architectural concerns - notes the growing complexity in `rev-list.c` with special cases for the new mode
5. Test optimization - suggests using `comm(1)` instead of multiple `grep` calls to verify present objects are excluded

The review maintains support for the feature's purpose while identifying several areas for improvement in the implementation. Patrick's feedback is substantive, engaging with both the immediate changes and broader implications for the codebase. The author will need to address these points in a v2 submission, particularly around naming clarity and test efficiency.

This continues the thread's technical discussion from the initial patch, with Patrick's review focusing on implementation quality rather than challenging the feature's core premise. The exchange remains productive, with concrete suggestions for refinement.