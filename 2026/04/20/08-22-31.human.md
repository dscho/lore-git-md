This is the first patch in Patrick Steinhardt's 18-part series to remove `the_repository` usage from the setup subsystem, part of the ongoing `the_repository` removal effort.

The patch mechanically converts all static functions in setup.c to take an explicit `struct repository *` parameter instead of implicitly using the global `the_repository`. For now, it simply passes `the_repository` to these functions at their call sites - subsequent patches will propagate the repository parameter up through the call chain. The changes are wide but straightforward, touching 188 lines across setup.c with no functional changes.

This is a foundational patch that enables the rest of the series to gradually remove `the_repository` dependencies from the setup machinery. The approach follows the established pattern for these conversions - methodically working through dependencies while maintaining backward compatibility. The patch appears well-structured and focused, exactly what we'd expect for this type of mechanical refactoring in the series.