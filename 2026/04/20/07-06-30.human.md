This is a brief follow-up in Patrick Steinhardt's ongoing series to remove `the_repository` global state from `setup.c`. The email is a simple acknowledgment of Elijah Newren's earlier review comment about a boolean assignment style inconsistency in the conversion of `set_git_work_tree()`.

Patrick concisely agrees with Elijah's observation that the newly introduced `worktree_initialized` boolean field in `struct repository` should use `true` rather than `1` for initialization, matching its type. He confirms the change will be made, though the behavioral impact is purely stylistic since C treats the values equivalently.

The exchange demonstrates the high standard of code review in this architectural refactoring - even minor type consistency issues are being caught and addressed. This comes after the series has already received final approval, showing Patrick's commitment to thoroughness. The thread as a whole represents mature technical collaboration on a complex codebase change.