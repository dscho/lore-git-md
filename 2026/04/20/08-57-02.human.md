This is a technical discussion about the proposed `--missing=print-only` mode for `git rev-list`, which would output only missing object IDs without prefixes to support partial clone workflows.

Phillip Wood joins the thread with two concrete suggestions to improve the implementation:

1. **Option naming**: Proposes a `--missing-only` flag as an alternative to `--missing=print-only`, arguing it would be more self-explanatory about its behavior of limiting output to missing objects only. This addresses Patrick Steinhardt's earlier concern about the unclear naming.

2. **Test optimization**: Points out that the current test implementation using multiple `grep` calls to verify present objects are excluded could be simplified. Suggests comparing the single expected OID directly with `test_cmp` rather than checking each present object individually, which would be both simpler and more efficient.

The discussion remains focused on implementation details, with Phillip's feedback building on Patrick's earlier substantive review. Both suggestions aim to improve code clarity and test robustness without challenging the feature's core purpose. The author will need to consider these points alongside the previous feedback when preparing a v2 submission.