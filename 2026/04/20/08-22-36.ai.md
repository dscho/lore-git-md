This is part 6 of Patrick Steinhardt's 18-patch series to remove `the_repository` usage from the setup subsystem. The patch converts `verify_filename()` to take an explicit `struct repository *` parameter instead of implicitly using the global variable.

The change follows the established pattern for this series - the function is modified to accept the repository as a parameter, and all callers (in `grep.c`, `reset.c`, `rev-parse.c`, and `revision.c`) are updated to pass `the_repository` explicitly. The implementation remains functionally identical, just with the repository parameter passed through to `die_verify_filename()`.

This is a straightforward mechanical conversion touching 6 files (10 insertions, 8 deletions). Like previous patches in the series, it maintains backward compatibility while moving toward the long-term goal of eliminating `the_repository`. The changes are focused and well-contained, continuing the methodical subsystem-by-subsystem approach seen throughout this effort.