This thread addresses a regression in Git's commit signing behavior for non-UTF-8 messages introduced in v2.45.0, where signatures would become invalid due to UTF-8 transcoding happening after signing. brian m. carlson provided a two-part fix: first clarifying the function naming around UTF-8 handling, then correcting the operation order to ensure signatures match the stored content. The technical solution has been implemented and will restore pre-2.45.0 behavior where signatures remain valid for non-UTF-8 messages.

Kushal Das has now confirmed the fix works for their real-world use case, though they note that the requirement to explicitly configure encoding for non-UTF-8 messages wasn't widely known. While the core issue is resolved, Kushal suggests expanding test coverage to include non-GnuPG signing tools, moving the discussion toward broader considerations about testing and real-world usage patterns. The thread remains focused on ensuring robust commit signing behavior across different environments, with the technical fix now complete but opportunities remaining to improve test coverage and documentation around encoding requirements.