This is patch 5/8 in Karthik Nayak's series to consolidate ref backend validation logic. The patch modifies `update-ref` to handle reference update rejections more gracefully when using `--batch-updates`.

The key change is introducing a new `command_options` struct to track whether individual update failures should be tolerated (with `--batch-updates`) or treated as fatal errors. The patch updates all command parsers in `update-ref.c` to accept this options struct, though most commands don't yet use it. The main behavioral change is in the update and symref-update commands, which now check the transaction result code to distinguish between generic failures (still fatal) and backend-specific rejections (tolerated with `--batch-updates`).

This is a preparatory change for later patches that will move more rejection logic into `ref_transaction_update()`. The implementation is careful and mechanical, following the series' pattern of small, incremental steps toward the larger refactoring goal. The patch maintains existing behavior while enabling future changes to rejection handling.

As with previous patches in the series, this appears to be a focused, well-constructed change that moves the ref backend consolidation effort forward without introducing new functionality yet. Reviewers will likely want to see how these changes interact with the upcoming validation logic patches before providing substantive feedback.