This is a technical discussion in the ongoing bugfix thread about object database usage in `git diff` outside repositories, now touching on platform limitations and streaming code paths.

Jeff King responds to Luca Stefani's follow-up, maintaining that the proposed `index_core()` approach is problematic for large files. He explains the core issue isn't just performance (as Luca suggested) but that it would break cases that currently work by failing to properly stream large files. The discussion reveals the streaming path was broken by earlier ODB transaction interface changes (ce1661f9da and follow-ups), which should be fixed directly rather than worked around.

Jeff provides two test proposals: one demonstrating the large-file failure (too expensive for general use) and a smaller test case showing the segfault when hashing objects outside a repository. He declines to accept the current patch direction, noting it would make some working cases fail, and suggests waiting for Justin Tobler's input on a more comprehensive fix that properly addresses the streaming path issues.

The exchange highlights how this simple-seeming repository context bug has uncovered deeper architectural questions about Git's large-file handling, particularly around the interaction between the streaming interface and repository-less operations. The thread remains unresolved, with Jeff advocating for addressing the root cause in the streaming code rather than applying a local workaround.