Thread identity: OpenSSL build failures in Git 2.54.0-rc0, reported by Randall S. Becker (NonStop port maintainer), concerning compilation errors in `imap-send.c`.

Core problem: Initial report claimed Git fails to compile against OpenSSL 3.0 due to type mismatches around `ASN1_STRING_get0_data()` in `imap-send.c`. René Scharfe's analysis suggests the errors actually indicate pre-OpenSSL 1.1.0 usage, as the problematic functions have had stable prototypes since 1.1.0 (2016).

Current status: Initial report of build failure in rc0, now under reevaluation. Scharfe's technical analysis contradicts the OpenSSL 3.0 diagnosis, pointing instead to potential use of an older OpenSSL version.

Key technical details: The error occurs in email-sending infrastructure (`imap-send.c`) when handling SSL/TLS certificates. Scharfe notes `ASN1_STRING_get0_data()` was added in OpenSSL 1.1.0 (commit 17ebf85abd) and the const-correctness errors would only appear with pre-1.1.0 versions. This contradicts the original OpenSSL 3.0 diagnosis.

Open questions: Whether the build environment is actually using OpenSSL 3.0 as claimed. If not, whether to maintain compatibility with pre-1.1.0 OpenSSL versions or require newer versions. How to verify the actual OpenSSL version in the reported environment.

Participant positions: Randall S. Becker initially identified the issue as OpenSSL 3.0 related. René Scharfe has challenged this with technical evidence suggesting pre-1.1.0 OpenSSL usage instead. The thread awaits Becker's response to this analysis.

Related work: This continues Git's history of OpenSSL compatibility challenges, particularly in the email stack. The NonStop port has previously surfaced OpenSSL versioning issues, though this case appears potentially misdiagnosed. The discussion may impact decisions about minimum supported OpenSSL versions.