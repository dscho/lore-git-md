Thread identity: OpenSSL 3.0 build failures in Git 2.54.0-rc0, reported by Randall S. Becker (NonStop port maintainer), concerning compilation errors in `imap-send.c`.

Core problem: Git fails to compile against OpenSSL 3.0 due to type mismatches around `ASN1_STRING_get0_data()` usage at line 274 of `imap-send.c`. The issue stems from API changes in OpenSSL 3.0's stricter type checking and potential function availability differences, affecting enterprise environments still using OpenSSL 3.0.

Current status: Initial report of build failure in rc0, no fixes proposed yet. The problem is specific to OpenSSL 3.0 builds.

Key technical details: The error occurs in email-sending infrastructure (`imap-send.c`) when handling SSL/TLS certificates. The problematic code attempts to use `ASN1_STRING_get0_data()` with const-qualified parameters in a way incompatible with OpenSSL 3.0's API.

Open questions: How to maintain compatibility with OpenSSL 3.0 while supporting newer versions. Whether to modify the type handling or implement version-specific code paths.

Participant positions: Randall S. Becker identified the issue as NonStop port maintainer, noting this affects systems with extended OpenSSL 3.0 support. No other contributors have weighed in yet.

Related work: This follows historical OpenSSL compatibility challenges in Git, particularly around certificate handling in the email stack. The NonStop port has previously surfaced similar OpenSSL versioning issues.