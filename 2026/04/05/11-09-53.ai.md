This is the third version of a bugfix patch improving `git stash`'s argument parsing behavior. The patch completes the work started in v1 and v2 to make `git stash` automatically assume the "push" subcommand when unambiguous push-specific flags are present, rather than requiring explicit "push" or `--` before pathspecs.

The v3 iteration addresses the final two polish items from v2 feedback: it properly cleans up staged files in tests using `git reset --hard` and updates the documentation in `git-stash.txt` to reflect the expanded set of flags that now trigger push inference (-m, --staged, --keep-index, --include-untracked, and --pathspec-from-file). The implementation remains focused on `builtin/stash.c`'s `force_assume` logic, now covering all push-specific options in a single condition.

The test coverage in `t/t3903-stash.sh` is thorough, verifying each push-specific option works correctly with pathspec arguments. The commit message maintains clear link to the original #leftoverbits discussion that motivated the change. With these final documentation and test hygiene improvements, the series appears ready for merging as a quality-of-life improvement to a common `git stash` workflow.