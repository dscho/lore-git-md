This is a platform compatibility report about OpenSSL 3.0 build failures. Randall S. Becker, the NonStop port maintainer, reports that Git 2.54.0-rc0 fails to compile when built against OpenSSL 3.0 due to type mismatches in `imap-send.c`. The errors occur around line 274 where the code attempts to use `ASN1_STRING_get0_data()` with a `const`-qualified parameter, and the function itself appears undefined in this OpenSSL version.

The issue appears to stem from API changes in OpenSSL 3.0, which is still in use by some systems with extended support contracts. The error suggests the code needs updating to handle OpenSSL 3.0's stricter type checking and potentially different function availability. This is particularly relevant for enterprise environments where OpenSSL 3.0 remains deployed despite newer versions being available.

The report comes from a known platform expert (Becker maintains the NonStop port) and identifies a concrete compilation failure, making it lacks specific suggestions for fixes. The affected file (`imap-send.c`) is part of Git's email-sending infrastructure, though the impact is limited to builds using OpenSSL 3.0. This looks like a legitimate compatibility issue that would need addressing for users on older OpenSSL versions.