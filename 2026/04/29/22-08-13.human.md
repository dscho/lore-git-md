This is the fourth patch in a six-part refactoring series improving type safety and clarity in xdiff's record cleanup logic. The series has gone through multiple iterations with careful review from Phillip Wood, and this version incorporates his final suggestions.

The patch focuses on making the per-file limit calculations in `xdl_cleanup_records()` more explicit and easier to follow. Key changes include:
- Introducing separate limit variables (`mlim1` and `mlim2`) for each file being processed
- Removing a redundant condition (`!need_min`) that was no longer needed
- Making the minimal-case handling clearer by using `PTRDIFF_MAX` to represent "no limit" when minimal diffs are requested

The changes are purely internal - no behavior is altered. This continues the series' theme of improving code clarity while maintaining all the type safety improvements from previous versions. The patch follows Phillip Wood's suggestions precisely, showing the author's responsiveness to review feedback.

This appears to be the final form of these changes, with all parties satisfied and the regression from earlier versions fully addressed. The series as a whole demonstrates Git's attention to careful refactoring that improves maintainability without introducing new issues.