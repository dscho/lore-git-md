This is the first patch in a refactoring series targeting the xdiff subsystem, specifically cleaning up the `xdl_cleanup_records()` function. The change is a straightforward simplification that removes the local `recs` pointer variable in favor of directly indexing into `xdfile.recs[]` arrays.

The patch makes two main changes:
1. Eliminates the `recs` pointer variable that was previously used to iterate through records, instead directly accessing `xdf1->recs[i]` and `xdf2->recs[i]` in the loops
2. Simplifies the loop structure by removing the pointer increment operation that was previously part of the loop control

The changes are purely internal and don't affect behavior - this is a code clarity improvement that makes the control flow more direct. The patch maintains all existing functionality while making the code slightly easier to follow by reducing indirection.

This appears to be a well-contained, mechanical change that's part of a larger effort to improve the xdiff code's maintainability. The patch follows the series' overall goal of making the record cleanup logic more explicit and type-safe.