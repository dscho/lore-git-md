This is the fifth patch in a six-part refactoring series for the xdiff subsystem, specifically focusing on improving code clarity in `xdl_cleanup_records()`. The series has gone through multiple iterations with careful review from Phillip Wood, and this version incorporates his final suggestions.

The patch replaces nested ternary operators in the action-setting logic with explicit if/else ladders, making the decision flow more readable while maintaining identical behavior. The changes affect both `action1` and `action2` arrays, which determine how records from each file should be processed (DISCARD, KEEP, or INVESTIGATE). The new structure clearly separates the three cases and adds a clarifying comment for the INVESTIGATE condition.

This is a pure code clarity improvement with no functional changes, continuing the series' theme of making the xdiff internals more maintainable. The patch follows the same pattern for both files being compared (xdf1 and xdf2), maintaining symmetry in the logic. The changes align with Phillip Wood's review feedback and represent the final polish on this aspect of the refactoring before the series is ready for inclusion.

Technical details:
- Replaces `(nm == 0) ? DISCARD: nm >= mlim1 ? INVESTIGATE: KEEP` with explicit if/else
- Maintains identical behavior to previous versions
- Improves readability while preserving all type safety and correctness improvements from earlier patches
- Part of the broader xdiff refactoring effort that has been carefully reviewed across multiple iterations