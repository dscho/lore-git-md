This is a brief reply in the ongoing discussion about adding a `--fetch` option to `git checkout` and `git switch` for automatic remote fetching during branch creation.

Harald Nordgren responds to D. Ben Knoble's earlier comment about using `git pull --rebase` as an alternative solution for recovering from stale remote refs. While agreeing that rebasing is useful (and mentioning he configures it globally), Harald points out that it still leaves users exposed to potential merge conflicts if they've already done work on a stale branch.

The exchange highlights a tension in the proposal: while automatic fetching would prevent the stale-branch problem entirely, some reviewers see existing tools (like rebasing) as sufficient workarounds. Harald's perspective represents users who want to avoid the problem proactively rather than fixing it after the fact.

This continues the thread's exploration of whether the convenience justifies adding new behavior, with no new technical arguments introduced in this particular message. The core design questions raised by Junio remain unaddressed here.