This is a brief reply in the ongoing discussion about adding a `--fetch` option to `git checkout`/`git switch`. Harald Nordgren responds to D. Ben Knoble's earlier comment about using `git pull --rebase` as an alternative solution for stale branches.

The email reinforces the convenience argument for the proposed `--fetch` feature, noting that while rebasing can fix stale branches after the fact, it's preferable to avoid creating them in the first place. Harald shares their personal workflow of always configuring `pull.rebase=true` and advocating rebasing to colleagues, but still sees value in preventing stale branch creation upfront.

This reply doesn't advance the technical discussion but provides a user perspective supporting the feature's motivation. The exchange highlights the tension between existing solutions (rebase workflows) and the desire for more proactive branch hygiene. The thread remains in a holding pattern awaiting resolution of the fundamental design questions raised by the maintainer.