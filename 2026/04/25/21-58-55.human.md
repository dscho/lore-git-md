This is a design discussion about the `--set-head` feature for `git remote add`, with Junio Hamano weighing in on whether the functionality should be automatic rather than opt-in.

Junio suggests that the current patch's approach of adding a new `--set-head` option may be unnecessary complexity. He argues that `git remote add --fetch` should automatically respect the existing `remote.<name>.followRemoteHEAD` configuration (which defaults to creating the remote HEAD) to match `git clone`'s behavior. This would make the feature a bugfix rather than a new option.

The maintainer's position implies that:
1. The functionality is desirable and should work consistently with clone
2. The implementation should leverage existing configuration rather than introduce new options
3. This could be framed as fixing an inconsistency rather than adding a feature

This shifts the discussion from "should we add this option?" to "how should we fix this behavior gap?" The technical merits of the original patch remain valid, but Junio's feedback suggests a simpler, more integrated approach may be preferable. The thread now awaits the original author's response to this design direction.