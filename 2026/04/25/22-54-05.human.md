This is a brief technical clarification in the ongoing discussion about adding a `--fetch` option to `git checkout`/`git switch`. Harald Nordgren acknowledges D. Ben Knoble's point that `git fetch origin main` can indeed fetch just a single branch (with possible refspec expansion), which directly addresses one of the open questions in the thread about partial fetching capabilities.

The exchange doesn't advance the core philosophical debate about workflow implications that Junio Hamano raised earlier, but it provides a concrete technical answer that could help refine the implementation. The ability to fetch specific branches rather than entire remotes may help address concerns about unwanted updates to unrelated remote-tracking branches. The thread remains on hold while the maintainer is unavailable.