This is a technical clarification in the ongoing discussion about making `git remote add --fetch` automatically set the remote's HEAD reference. Jeff King (Peff) responds to Junio C Hamano's suggestion that the behavior should align with `remote.<name>.followRemoteHEAD` by pointing out that the current implementation already exhibits this behavior - `git remote add --fetch` does create the remote HEAD reference when fetching.

The key observation is that the original issue motivating the patch (GitHub `gh` tool integration problems with fork remotes) may not require any changes to Git's behavior, since the desired functionality appears to already exist. This contradicts the initial premise of the patch and suggests the problem might lie elsewhere, possibly in how the GitHub tool interacts with Git.

The email serves to ground the discussion in actual behavior rather than assumed behavior, potentially redirecting the thread toward investigating why the GitHub tool isn't seeing the expected HEAD reference rather than changing Git's remote-add functionality. This is a substantive technical contribution that could significantly alter the direction of the thread.