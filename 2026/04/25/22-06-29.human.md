Jeff King confirms that `git remote add --fetch` already respects the `remote.<name>.followRemoteHEAD` configuration, showing that it creates the remote HEAD reference automatically when fetching from the new remote. This directly addresses Junio's suggestion that the behavior should align with `git fetch`'s existing config-based approach.

The email demonstrates the current behavior with a simple test case, indicating that the proposed `--set-head` option might be redundant since the desired functionality is already available through the config mechanism. This moves the discussion toward whether the original patch is needed at all, given that the maintainer's suggested approach appears to be the current behavior.

The technical implication is that the inconsistency between `git clone` and `git remote add --fetch` may not exist in practice when `followRemoteHEAD` is properly configured, though this may depend on default settings. This shifts the focus from adding new options to potentially documenting or clarifying the existing behavior.