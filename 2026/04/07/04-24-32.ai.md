This is a bugfix discussion thread about `git fast-export`'s output ordering, specifically addressing path deletions that are prefixes of added paths (like deleting 'a' while adding 'a/b'). Jeff King (Peff) responds to Elijah Newren's technical analysis, agreeing that `fast-import` should maintain its current last-entry-wins behavior rather than trying to reorder commands on the import side.

The key technical point is that `fast-import` currently processes commands strictly in the order received, with later commands overriding earlier ones for the same path. This makes it safer to handle the ordering in `fast-export` rather than trying to make `fast-import` smarter about path prefixes. Peff's response confirms the emerging direction that the thread has been taking - that the exporter should be responsible for emitting deletions before additions when there are prefix relationships.

This moves the discussion toward resolution, with no new technical objections raised to the proposed approach of modifying `fast-export`'s sorting logic. The remaining work appears to be finalizing the patch with proper test coverage and addressing Junio C Hamano's earlier question about whether `depth_first()` is the right place for this logic. The thread shows clear technical consensus forming around the exporter-side solution.