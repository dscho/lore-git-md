This is the final version of Christian Couder's preparatory patch series for enabling auto-configuration of promisor remotes via URL allowlist. The 10-patch series has undergone review from Patrick Steinhardt and Junio Hamano, with the last patch (10/10) receiving particular attention to test script improvements.

The series systematically refactors promisor-remote handling to enable future auto-configuration features while maintaining backward compatibility. Key changes include restructuring remote lookup logic, optimizing configuration matching, and hardening security by rejecting empty names/URLs in advertised remotes. The final patch addresses Junio's feedback by improving URI handling in tests - renaming variables for clarity (`PWD_URL` -> `TRASH_DIRECTORY_URL`) and expanding URI encoding to handle more special characters while implementing a whitelist approach.

The changes touch core promisor-remote functionality in `promisor-remote.c` and its test suite in `t5710-promisor-remote-capability.sh`. The series appears well-polished after review iterations, with clean commit messages and focused changes. Patrick Steinhardt has positively acknowledged the security hardening and platform compatibility work.

This preparatory work sets the stage for future patches that will introduce the actual auto-configuration functionality via the `promisor.acceptFromServerUrl` configuration. The careful refactoring and test improvements suggest this is production-ready code that maintains stability while enabling future enhancements to promisor remote handling.