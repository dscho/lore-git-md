Bugfix for ODB transaction interface, now confirmed for backport. Justin Tobler reviews Junio C Hamano's backported version of his fix for the segfault that occurs when running `git diff` outside a repository with large files (exceeding core.bigFileThreshold). The backport applies the same solution - avoiding ODB transactions when only object hashing is needed via a new `hash_blob_stream()` helper - to the 2.52 and 2.53 maintenance branches.

Justin confirms the backported patch is correct but notes he will address Jeff King's test improvement suggestions: wrapping the working directory change in a subshell (or using the `nongit` helper) to avoid test pollution, demonstrating the bug more simply with `git hash-object`, and optional stylistic tightening of parentheses. The core fix remains unchanged - still splitting the logic between hashing-only and object-writing paths in `index_fd()` in object-file.c.

This moves the backport closer to completion, with only test refinements remaining. The thread shows effective collaboration between Justin (author), Jeff (reviewer), and Junio (maintainer) to produce a robust fix across multiple Git versions. The fix addresses a clear regression introduced by the ODB transaction interface while maintaining careful attention to test coverage and portability.