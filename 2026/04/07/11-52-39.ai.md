This is a refactoring patch in Christian Couder's series on promisor-remote handling, specifically extracting control character checking into a helper function. The patch introduces `has_control_char()` to centralize the logic for detecting control characters in strings, which was previously duplicated inline in `valid_token()`. 

The new helper function will be reused in future changes for additional control character checks. The patch maintains the same warning behavior when invalid tokens are detected for remote names, just moving the implementation to use the new helper. This is a straightforward code organization improvement that doesn't change functionality but makes the code more maintainable for upcoming work in the series.

The change touches `promisor-remote.c`, specifically the control character validation logic that helps prevent malformed or potentially malicious remote configurations. The patch follows the series' pattern of preparatory cleanups before introducing new promisor-remote features, with all existing test coverage remaining valid since behavior is preserved.