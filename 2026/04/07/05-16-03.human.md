Documentation fix for the new `git history` command's help text.

Patrick Steinhardt acknowledges René Scharfe's patch that corrects the help text for the `--update-refs` option in the `git history` command. The original patch fixed two documentation issues: removing the incorrect "print" option from the argument placeholder and making the help text consistent between the `reword` and `split` subcommands.

This is a simple but important follow-up to the new `git history` feature, ensuring users see accurate documentation about the supported options. Patrick's brief reply indicates the fix looks correct, though as the primary author of the `git history` command, his acknowledgment carries weight. The patch appears ready to be picked up by the maintainer.