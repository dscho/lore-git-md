This is a refactoring patch in Christian Couder's series on promisor-remote handling. The change modifies how accepted promisor information is managed in `filter_promisor_remote()` by keeping the original `promisor_info` structs alive rather than immediately freeing them after extraction.

The key technical change replaces three parallel data structures (an `accepted` strvec for server names, an `accepted_filters` string list, and the original `promisor_info` structs) with a single `accepted_remotes` string list that retains the full `promisor_info` structs. This simplifies the post-processing logic by allowing iteration over one consolidated structure rather than three separate ones.

The patch prepares for future addition of a 'local_name' member to `struct promisor_info` by ensuring the structs stay available throughout processing. It maintains all existing behavior while making the code more maintainable - the diff shows 25 lines removed versus 17 added, with the logic now flowing through a single consolidated processing loop.

This continues the series' pattern of structural improvements before introducing new functionality, touching the same key files (`promisor-remote.c`) while keeping all tests passing. The change is well-contained and follows the established approach of the series, with no apparent technical concerns remaining.