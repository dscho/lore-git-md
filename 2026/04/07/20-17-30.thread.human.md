This thread addresses a bugfix for a segfault in Git's object database (ODB) transaction interface, which occurred when running `git diff` outside a repository on large files. The issue was introduced by a recent change that incorrectly attempted ODB transactions in hashing-only contexts. Justin Tobler's fix cleanly separates the hashing and object-writing paths to resolve this.

The fix has been thoroughly reviewed by Jeff King (Peff), who suggested test improvements, and Junio C Hamano has backported it to maintenance branches (2.52, 2.53) while applying it to `master`. Justin has confirmed the backport's correctness and committed to implementing Peff's test refinements. 

Junio now notes that Justin's larger `jt/odb-transaction-write` topic (currently in the `seen` branch) already includes this fix. He plans to apply the standalone patch to `next` and `master` immediately while letting the full topic supersede it later, ensuring the regression test from t1517 remains in place. The test passes when applied to `seen`, confirming the topic properly addresses the issue. This demonstrates Git's careful integration workflow, where fixes flow through the branches while maintaining test coverage. With all technical aspects settled, this thread shows effective collaboration between contributor, reviewer, and maintainer to resolve a regression.