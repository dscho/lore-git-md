This is a continuation of the discussion about preserving change-id headers during commit rewriting operations, with Junio Hamano now responding to Nico Williams' latest proposal. The thread remains focused on establishing clear behavioral contracts for how Git should handle these headers during operations like rebase, cherry-pick, and commit amendment.

Junio questions whether the proposed configurable approach (with operation-specific defaults and hooks) is the right solution, suggesting instead that such metadata might be better stored in Git notes or commit trailers. His response indicates continued skepticism about baking change-id handling directly into Git's core operations, preferring mechanisms that don't require history rewriting if the metadata needs correction.

The email highlights the fundamental tension in this thread: whether Git should natively support change-id headers (which are primarily for external tools like Gerrit) or treat them as user metadata that belongs in more flexible storage like notes. Junio's preference for notes/trailers reflects Git's traditional stance of staying agnostic about workflow-specific metadata, while Nico's earlier proposal attempts to find middle ground with configurable behavior.

This exchange keeps the discussion at the design philosophy level rather than moving toward implementation. The key open question remains whether Git should treat change-ids as special-case headers deserving of core support, or whether they should be handled like other user metadata through existing extension mechanisms. Junio's response suggests the maintainer position continues to favor the latter approach.