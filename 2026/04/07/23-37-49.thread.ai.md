Thread identity: Test failure report for t5401.13 ("pre-receive hook that forgets to read its input") in Git 2.54.0-rc0, originally reported by Randall Becker (NonStop port maintainer) with technical analysis from Jeff King (Peff), now showing a regression in t5571.11 ("sigpipe does not cause pre-push hook failure") after applying the fix.

Core problem: The race condition in pre-receive hook execution (where hooks exiting successfully without reading input incorrectly rejected pushed references) was fixed by Jeff King's SIGPIPE handling change, but this fix now causes a regression in pre-push hook execution where similar conditions lead to indefinite hanging. The issue spans multiple hook types and execution contexts, requiring broader validation of signal handling changes.

Current status: While the original fix was confirmed by Randall Becker to resolve the NonStop case and parallel execution scenarios, the newly discovered pre-push regression means the fix cannot proceed to 2.54.0-rc2 in its current form. The test hangs indefinitely when pushing with a simple pre-push hook that exits successfully without reading input, indicating either a deadlock in process management or infinite loop in push/hook interaction.

Key technical details: The fix in run-command.c's `run_processes_parallel()` reordered signal handler installation to ensure SIG_IGN takes precedence over child cleanup handlers, which resolved the original pre-receive case but now shows problems in pre-push contexts. The regression manifests as a hang after the pack-reused line with no further output, suggesting the signal handling changes may need hook-type-aware adjustments or additional safeguards.

Open questions: Whether the pre-push regression represents a fundamental limitation of the approach or an additional edge case that can be handled within the same framework. The thread now requires analysis from Jeff King and Adrian Ratiu to determine next steps. The previously planned integration path (standalone patch for rc2) is on hold pending resolution of this new issue.

Participant positions:
- Randall Becker: Confirmed original fix worked for pre-receive but discovered pre-push regression
- Jeff King: Needs to analyze whether his signal handling approach can be extended to cover pre-push cases
- Adrian Ratiu: May need to adjust integration plans for both the standalone fix and parallel hooks work
- Junio C Hamano: Will likely defer inclusion in 2.54.0-rc2 until the regression is understood

Related work: This development shows the signal handling changes interact with hook execution more broadly than initially tested. The solution must now account for pre-push behavior while maintaining the pre-receive fixes, potentially affecting Adrian Ratiu's parallel hooks work. The regression demonstrates the need for comprehensive hook execution testing across all hook types when modifying this sensitive code path.