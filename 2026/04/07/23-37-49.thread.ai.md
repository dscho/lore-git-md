Thread identity: Test failure report for t5401.13 ("pre-receive hook that forgets to read its input") in Git 2.54.0-rc0, originally reported by Randall Becker (NonStop port maintainer) with technical analysis from Jeff King (Peff), now showing timing issues in t5571.11 ("sigpipe does not cause pre-push hook failure") after applying the fix.

Core problem: The race condition in pre-receive hook execution (where hooks exiting successfully without reading input incorrectly rejected pushed references) was fixed by Jeff King's SIGPIPE handling change, but this fix now causes unexpected delays in pre-push hook execution. While not a deadlock as initially suspected, the performance impact requires investigation to determine if the solution needs optimization.

Current status: Randall Becker's follow-up testing shows the pre-push hook test eventually completes successfully after a delay, indicating the fundamental behavior is correct but potentially inefficient. The fix remains unsuitable for 2.54.0-rc2 without understanding and addressing the timing implications, though the path forward may now involve performance tuning rather than architectural changes.

Key technical details: The run-command.c changes reordering signal handler installation (ensuring SIG_IGN takes precedence) correctly handle the parallel pre-receive case but introduce suboptimal behavior in pre-push contexts. The test shows delayed rather than failed completion, suggesting the signal handling may need hook-type-specific timing adjustments or additional process management safeguards.

Open questions: Whether the pre-push delay represents an acceptable tradeoff for the pre-receive fix, or if further optimization is needed. The thread now requires analysis from Jeff King on whether the timing behavior can be improved while maintaining the original fix's correctness. The NonStop platform remains a critical test case for any changes.

Participant positions:
- Randall Becker: Confirmed the test eventually passes but with concerning delay, maintaining NonStop-specific concerns
- Jeff King: Needs to evaluate if the timing impact is inherent to the solution or can be optimized
- Adrian Ratiu: May need to adjust parallel hooks integration plans based on timing characteristics
- Junio C Hamano: Still likely to defer inclusion pending resolution of performance implications

Related work: This development shows the signal handling changes have broader timing implications across hook types. The solution must now be evaluated for both correctness and performance in all hook contexts, potentially affecting Adrian Ratiu's parallel hooks work. The NonStop platform continues to provide valuable edge case testing for these sensitive code paths.