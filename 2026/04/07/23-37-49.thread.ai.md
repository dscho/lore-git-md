Thread identity: Test failure report for t5401.13 ("pre-receive hook that forgets to read its input") in Git 2.54.0-rc0, originally reported by Randall Becker (NonStop port maintainer) with technical analysis from Jeff King (Peff), now with additional input from Adrian Ratiu (parallel hooks work).

Core problem: A race condition in pre-receive hook execution where hooks that exit successfully (status 0) without reading their input incorrectly reject pushed references. The issue manifests differently across platforms - readily on NonStop and in parallel execution scenarios on Linux. The root cause is improper SIGPIPE handling in `run_processes_parallel()` where child cleanup handlers override the intended SIG_IGN signal disposition.

Current status: Jeff King's proposed fix (reordering signal handler installation to ensure SIG_IGN takes precedence) has been shown to address both the original NonStop case and parallel execution scenarios. Adrian Ratiu agrees the fix should be applied and proposes two integration paths depending on whether it resolves Randall's specific test case: either as a standalone patch (if successful) or as part of Adrian's parallel hooks series (if further work needed).

Key technical details: The bug in run-command.c's `run_processes_parallel()` creates a race condition in signal propagation to child processes. Peff's test case demonstrates this by showing how a fast hook's SIGPIPE can incorrectly terminate a slow hook without the fix. The issue affects hook execution infrastructure broadly, not just specific platforms or configurations.

Open questions: Final confirmation from Randall Becker whether the fix resolves the original NonStop test case. The integration path (standalone vs parallel hooks series) depends on this outcome. No remaining technical objections to the fix itself.

Participant positions:
- Randall Becker originally reported the issue during rc testing
- Jeff King diagnosed the root cause, proposed the fix, and extended analysis to parallel execution
- Adrian Ratiu (parallel hooks work) agrees with the technical solution and is coordinating integration
- Consensus exists that this is a general infrastructure bug needing fixing regardless of parallel hooks timeline

Related work: While touching the subsystem Adrian Ratiu is working on (configurable/parallel hooks), this fix stands independently as a correctness issue in core process handling. The solution would benefit all hook execution scenarios across platforms.