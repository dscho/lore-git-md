This is a final test infrastructure adjustment in Christian Couder's promisor-remote refactoring series, addressing a shell compatibility issue with the previous implementation. The patch fixes a problem where some shells (notably dash) couldn't handle spaces within the case statement's character class pattern matching.

The change replaces the problematic case` statement with a more portable approach using `printf` and `tr` to filter out allowed characters, then checks if any disallowed characters remain. The allowed character set remains the same: alphanumerics plus `_ . ~ / : -` for paths/URIs, with explicit percent-encoding for spaces, equals signs, commas, and semicolons. If any other characters are detected, the test suite skips all tests with a clear explanation.

This demonstrates careful attention to cross-shell compatibility while maintaining the series' security-conscious approach to path handling. The change is minimal and focused, exactly what you'd want in a final patch of a well-reviewed series. With this last shell compatibility fix, the 10-patch series appears fully ready for merging, having addressed all substantive feedback across its iterations.