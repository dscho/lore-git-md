Bugfix backport confirmation for ODB transaction issue. Junio C Hamano is finalizing the backport of Justin Tobler's fix for a segfault that occurs when running `git diff` outside a repository with large files. The original fix cleanly separates object hashing from writing by introducing `hash_blob_stream()` to avoid unnecessary ODB transactions in hashing-only contexts.

Junio confirms the fix works across versions (master and maintenance branches 2.52/2.53) and asks Justin to review the backported versions. He notes the test case in t1517 remains valid and will be kept when the broader jt/odb-transaction-write topic eventually merges. The thread shows careful coordination between author and maintainer to ensure the fix lands correctly in all affected versions while avoiding duplicate work.

This represents the final step before the backport is complete - once Justin confirms the older-branch versions look correct, the fix will flow to both stable and development branches. The collaboration demonstrates Git's thorough approach to regression fixes, with multiple eyes verifying correctness across version contexts.