Bugfix for ODB transaction interface, now being backported. Junio C Hamano confirms the segfault issue when running `git diff` outside a repository with large files (exceeding core.bigFileThreshold) and has prepared backports of Justin Tobler's fix for maintenance branches (2.52, 2.53) while applying the original patch to master. The fix avoids ODB transactions when only object hashing is needed, using a new `hash_blob_stream()` helper in those cases.

Junio verified the test case fails without the fix and succeeds with it across all branches. He requests Justin review the backported versions to ensure they correctly address the bug in the older code context. The fix remains unchanged in substance - still splitting the logic between hashing-only and object-writing paths in `index_fd()` - but needed minor adjustments to apply cleanly to the 2.52 codebase where the ODB transaction interface was first introduced.

This moves the fix toward inclusion in stable releases while maintaining the same careful approach seen in the original submission - clear problem statement, focused change, and regression test. The backport process shows the bug affects multiple versions but the solution ports cleanly with maintainer involvement.