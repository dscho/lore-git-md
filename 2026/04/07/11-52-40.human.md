This is part 7 of Christian Couder's 10-patch series refactoring promisor-remote handling, specifically focusing on the `acceptFromServer` configuration logic. The patch extracts the config parsing for `promisor.acceptFromServer` into a new `accept_from_server()` helper function, cleaning up the more complex `filter_promisor_remote()` function in preparation for future enhancements.

The change is straightforward - it moves the existing config parsing logic into a dedicated function while maintaining all current behavior. The helper now returns an `enum accept_promisor` value that `filter_promisor_remote()` uses to determine whether to proceed with remote filtering. This follows the series' pattern of breaking down complex functions into more maintainable pieces before introducing new functionality.

The patch touches only `promisor-remote.c` and keeps the same config validation and warning behavior for invalid `acceptFromServer` values. It's a clean, focused change that fits well with the series' structural improvements to promisor remote handling. The preparatory nature suggests this refactoring will enable more substantial changes to remote configuration in future patches.