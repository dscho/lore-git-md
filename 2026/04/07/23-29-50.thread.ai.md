Thread identity: Test failure report for Git 2.54.0-rc1 regarding `--git-dir` option issues in bitmap-related operations, reported by Randall S. Becker (NonStop platform maintainer).

Core problem: Unexpected "not a git repository" errors when using `git --git-dir=clone.git` in test files t5310, t5326, and t5327, which test cloning and fetching operations with bitmapped repositories. The failures suggest either environment setup problems (HOME/.gitconfig) or deeper issues in how `--git-dir` interacts with bitmap operations.

Current status: Regression in release candidate 2.54.0-rc1 affecting bitmap functionality, which is performance-critical for large repositories. The failures occur during standard test operations when using the `--git-dir` option to specify repository location.

Key technical details: Failures involve:
- `--git-dir` option not properly locating repositories during clone/fetch
- Potential network protocol issues when cloning from bitmapped repos
- Test files t5310, t5326, t5327 (bitmap-related functionality)
- Environment variable handling (HOME, .gitconfig) in test context

Open questions:
- Is this an environment setup issue in the tests or a genuine bug?
- Does the problem affect only NonStop or other platforms too?
- What changed in 2.54.0-rc1 that might have introduced this regression?

Participant positions:
- Randall S. Becker reported the failures and questioned environment setup
- No other responses yet (initial report)

Related work: Bitmap functionality is performance-critical and has seen recent work from Taylor Blau (MIDX, geometric repacking) and Derrick Stolee (pseudo-merge bitmaps). The `--git-dir` option is fundamental repository plumbing.