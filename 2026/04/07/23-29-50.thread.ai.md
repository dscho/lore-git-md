Thread identity: Test failure report for Git 2.54.0-rc1 regarding `--git-dir` option issues and `writev()` problems in bitmap-related operations, reported by Randall S. Becker (NonStop platform maintainer) with analysis from Jeff King.

Core problem: Two distinct issues causing test failures in bitmap-related operations (t5310, t5326, t5327) on NonStop platform:
1. Unexpected "not a git repository" errors when using `git --git-dir=clone.git` in test t5310
2. Network protocol errors during cloning from bitmapped repositories in tests t5326/t5327, traced to `writev()` implementation in `sideband_send()`

Current status: Regression in release candidate 2.54.0-rc1 affecting bitmap functionality. Jeff King's analysis suggests:
- The `--git-dir` issue may stem from earlier failures preventing proper test setup
- The network errors are likely specific to NonStop's `writev()` implementation
- Workaround available via `NO_WRITEV=Nope` build flag

Key technical details: Failures involve:
- `--git-dir` option not properly locating repositories during clone/fetch (t5310)
- `writev()` failures in `sideband_send()` during network operations (t5326/t5327)
- Test files t5310, t5326, t5327 (bitmap-related functionality)
- Environment variable handling (HOME, .gitconfig) in test context
- Recent introduction of `writev()` optimization in sideband code

Open questions:
- Does the `--git-dir` issue indicate a real bug or just test setup problems?
- Will the `writev()` workaround be sufficient for NonStop or is deeper change needed?
- Should the `writev()` optimization be reverted for 2.54.0 release?

Participant positions:
- Randall S. Becker reported the failures and questioned environment setup
- Jeff King analyzed the issues and proposed specific debugging steps:
  - Running tests with `-i` to check for earlier failures
  - Building with `NO_WRITEV=Nope` to test fallback implementation
- No other responses yet

Related work: Bitmap functionality is performance-critical with recent work from Taylor Blau (MIDX, geometric repacking) and Derrick Stolee (pseudo-merge bitmaps). The `--git-dir` option is fundamental repository plumbing. The `writev()` optimization was recently introduced to improve network performance.