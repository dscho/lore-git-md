This is a refactoring patch in Christian Couder's series to improve promisor-remote handling. The patch simplifies memory management by eliminating a redundant `accepted` strvec in favor of using the existing `accepted_remotes` string list throughout the promisor-remote filtering process.

The change removes 15 lines while adding 12, consolidating the data flow through a single structure. This continues the series' pattern of preparatory cleanups before introducing new URL-based auto-configuration features. The patch maintains all existing behavior while making the code more straightforward - the `accepted_remotes` list now carries both the remote names and their associated `promisor_info` structs through the entire operation.

This is a well-contained change that follows naturally from the previous refactoring in the series. It touches only `promisor-remote.c`, with no test modifications needed since the external behavior remains identical. The patch has already been reviewed in previous iterations and appears ready for merging as part of the larger preparatory work.