This is part 7 of Christian Couder's 10-patch series refactoring promisor-remote handling. The patch extracts the logic for checking the `promisor.acceptfromserver` config value into a new `accept_from_server()` helper function, cleaning up `filter_promisor_remote()` which was becoming overloaded.

The change is straightforward - it moves the config parsing and validation into a dedicated function while maintaining all existing behavior. The new helper returns an `enum accept_promisor` value (ACCEPT_NONE, ACCEPT_KNOWNURL, etc.) based on the config setting, with the same error handling for invalid values. This prepares for future patches that will add more complex filtering logic to `filter_promisor_remote()`.

The patch touches only `promisor-remote.c`, modifying how the function is structured without changing any observable behavior. It follows the series' pattern of systematic refactoring before introducing new features, in this case making the code more maintainable for upcoming URL-based promisor remote configuration work. The change is well-contained and clearly motivated, with all tests passing in CI.