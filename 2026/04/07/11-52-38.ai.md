Code refactoring in Christian Couder's promisor-remote series, specifically restructuring the `should_accept_remote()` function to prepare for future URL-based acceptance logic. This patch follows earlier security hardening that rejected empty URLs at parse time.

The changes convert a warning about empty URLs into a BUG() assertion since the condition should now be impossible, moving this check to the top of the function to cover all acceptance modes. The control flow is inverted to return early on URL mismatches rather than matches, creating a single exit path at the function's end for future extensions. The warning message for URL mismatches is preserved but now appears in the early-return case.

This is a mechanical refactoring that maintains existing behavior while making the code more maintainable for upcoming changes. The patch touches only `promisor-remote.c`, modifying the logic that validates advertised promisor remotes against configured counterparts. The changes align with the series' broader goal of cleaning up promisor-remote handling before introducing auto-configuration features.