Bugfix discussion for `git fast-export` output ordering, continuing the thread about path deletions that are prefixes of added paths. Jeff King (Peff) responds to Elijah Newren's analysis of whether `fast-import` should handle the ordering instead of `fast-export`.

Peff agrees with Newren's assessment that changing `fast-import` to reorder commands could introduce subtle bugs, since `fast-import` currently uses a last-entry-wins rule where command order matters. This reinforces the emerging consensus that the fix belongs in `fast-export`, which should emit deletions before additions when paths have prefix relationships.

The discussion has now settled the architectural question - the exporter-side solution is preferable because it maintains `fast-import`'s current behavior while fixing the invalid stream generation. This moves the thread closer to a resolution, with the remaining work being to finalize Raymond E. Pasco's patch with proper test coverage. The exchange between these experienced contributors (Peff with deep `fast-import` knowledge and Newren with merge expertise) provides strong technical validation for the proposed direction.