This is the final version of Christian Couder's preparatory patch series for enabling auto-configuration of promisor remotes via URL allowlist. The 10-patch series refactors and cleans up the promisor-remote handling code before introducing new functionality, with the last patch specifically addressing test improvements.

The series is now in its final form after addressing feedback from Patrick Steinhardt and Junio Hamano. Patch 10/10 ("t5710: use proper file:// URIs for absolute paths") has been significantly updated since v1 to improve URI handling in the test script. Key changes include renaming variables from `PWD_URL` to `TRASH_DIRECTORY_URL` for accuracy, expanding URI encoding to handle more special characters (`=`, `;`, and `,`), and adding safety checks to skip tests if the directory path contains unsupported characters.

The series systematically restructures promisor-remote handling while maintaining backward compatibility. Notable technical changes include modifications to `promisor_remote_get_direct()`, `all_fields_match()`, and `parse_one_advertised_remote()`, with security hardening (rejecting empty names/URLs) and cross-platform compatibility improvements. The test changes in patch 10 ensure proper URI formatting across platforms while making the test infrastructure more robust.

The preparatory nature of this work suggests more substantial changes to remote configuration will follow in later series. The patches are well-structured and focused, with each addressing a specific improvement to the promisor-remote subsystem. The series touches key files including `promisor-remote.c` and `t/t5710-promisor-remote-capability.sh`, and has passed all CI tests. With all substantive technical concerns addressed, this series appears ready for merging as a foundation for future promisor-remote enhancements.