This is a follow-up response in an ongoing thread about repository organization strategy. Christian Couder points Gabor Urban to a specific example in the git-filter-repo documentation that demonstrates how to combine multiple repositories while preserving history.

The reference to git-filter-repo suggests a subtree merge approach may be appropriate for Gabor's case of combining Project1 (with history) with Projects 2 and 3 under a common parent directory. The linked example shows how to rewrite paths to nest projects under new subdirectories while maintaining commit history.

Christian's response is brief but directly relevant - it provides a concrete starting point for the technical solution while avoiding making assumptions about whether the projects need to remain separable (which wasn't specified in the original question). The git-filter-repo tool is well-suited for this kind of repository restructuring operation.

This moves the thread forward by offering a specific technical approach rather than discussing the higher-level strategy of submodules vs. subtrees that the original question left open. The response assumes the asker is capable of evaluating whether the example fits their needs once they see the concrete implementation path.