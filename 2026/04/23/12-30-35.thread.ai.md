Git thread summary:

This thread introduces a new configuration option `push.showProgress` to control progress reporting during `git push`. The feature allows persistent configuration of whether to show the "Enumerating/Counting/Compressing/Writing objects" messages, addressing a gap where users wanting to suppress only progress output while keeping the final push summary had no configuration option (previously only possible via temporary `--no-progress` flags).

The implementation follows Git's standard configuration hierarchy - command-line `--progress`/`--no-progress` flags override the config setting, which in turn overrides the default TTY-based behavior. The patch includes comprehensive tests verifying the interaction between config, command-line flags, and TTY detection. Documentation covers the new config option and its relationship to existing flags.

No reviews or objections have been raised yet in this new thread. The patch appears complete with implementation, tests, and docs all included in a single well-structured commit. The change maintains backward compatibility by keeping the current default behavior when the config is unset.