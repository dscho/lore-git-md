This is the first patch in a two-part series implementing a new `--reverse=before` option for revision walks, allowing commits to be reversed before applying limiting options like `--max-count`. The patch builds on previous discussion and incorporates feedback from Jeff King and others.

The core change introduces an `enum rev_reverse` in `revision.h` to track whether reversal should happen before or after limiting, with the default remaining `--reverse=after` for backward compatibility. The implementation handles edge cases like multiple `--reverse` flags and maintains the historical behavior where `--reverse --reverse` cancels out.

The patch includes comprehensive tests in `t4202-log.sh` verifying the new behavior, including cases like `--reverse=before --max-count=2` showing the first two commits (rather than the last two in reverse order). Documentation updates clearly explain the toggle behavior and historical quirks.

This appears to be a well-considered implementation that addresses all technical feedback from previous rounds. The changes are focused in `revision.c` and `list-objects.c` (not shown in the diff), with thorough test coverage. The only remaining open question is Junio's conceptual concern about "applying count limits later," but the implementation seems functionally complete.