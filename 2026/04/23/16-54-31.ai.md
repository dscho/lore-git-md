This is part of the ongoing `the_repository` removal effort, specifically migrating the `sparse_expect_files_outside_of_patterns` configuration variable from a global to repository-specific storage. The patch is the 7th in an 8-part series (v3) that moves several configuration variables into `struct repo_config_values`.

The change moves the sparse-checkout-related configuration that controls whether Git expects files outside sparse patterns into per-repository storage, preventing cross-repository state leakage. The commit message explains this needs eager parsing (rather than lazy) because it affects fundamental sparse-checkout behavior that many commands rely on. The implementation follows the established pattern seen in previous configuration migrations in this series.

The diff shows mechanical changes across three files - removing the global declaration, adding the field to the repository config struct, and updating the single usage site in `sparse-index.c` to access it through the repository. The changes are straightforward and well-contained, continuing the incremental progress on eliminating global configuration state.

This connects to broader work on sparse-checkout optimizations (Derrick Stolee/Elijah Newren) while following the established approach for configuration migration in the `the_repository` removal effort. The patch appears uncontroversial and follows the same pattern as previous configuration migrations in the series.