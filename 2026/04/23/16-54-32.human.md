This is part of the ongoing `the_repository` removal effort, specifically migrating the `core.warnAmbiguousRefs` configuration from a global variable to repository-specific storage in `struct repo_config_values`. The patch continues the pattern seen in previous configuration migrations in this series.

The change moves the `warn_on_object_refname_ambiguity` flag that controls whether Git warns about ambiguous object references into per-repository storage. The commit message explains this needs to remain eagerly parsed since ambiguity warnings affect user interpretation of object references across many commands. The patch updates all seven locations that reference this flag, showing the typical mechanical changes required for these migrations.

The changes appear straightforward and well-contained, touching builtin commands like `cat-file` and `pack-objects` that process object references, along with the object lookup and revision walking code. The diff shows the expected pattern of many small changes across multiple files rather than deep modifications to any single component.

This represents another incremental step in the multi-year effort to eliminate global state from Git's codebase. The patch follows the established approach seen in previous configuration migrations in this series, maintaining the same behavior while making it repository-specific.