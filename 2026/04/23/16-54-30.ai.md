This is part of the ongoing `the_repository` removal effort, specifically migrating the `core.sparseCheckoutCone` configuration variable from a global to repository-specific storage. The patch moves this sparse-checkout related setting into `struct repo_config_values` to prevent cross-repository state leakage while maintaining its eager parsing behavior.

The change touches several files that interact with sparse-checkout functionality, including the sparse-checkout builtin commands, directory handling, and sparse-index integration. The migration follows the established pattern seen in previous configuration moves - converting direct global variable access to repository-specific storage via `repo_config_values()`.

The commit message explains the rationale for keeping eager parsing (needed early in repository setup) while eliminating the global state. This appears to be a mechanical change that continues the incremental progress on the larger refactoring effort. The diff shows the typical pattern of many small, straightforward changes across multiple files rather than deep modifications to any single component.

Mentored by Christian Couder and Usman Akinyemi, this patch represents another step in the multi-year effort to eliminate Git's reliance on global configuration state. It builds on previous work migrating other variables like compression settings and Unicode handling, now addressing sparse-checkout cone mode specifically.