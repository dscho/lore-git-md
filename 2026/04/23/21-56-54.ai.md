This is a reply in the ongoing discussion about per-worktree gitignore behavior. brian m. carlson responds to D. Ben Knoble's question about why secondary worktrees don't respect their own `info/exclude` files while the main worktree's exclude rules apply globally.

The response suggests this wasn't an intentional omission, but rather an unimplemented feature. brian m. carlson proposes adding this functionality as an optional extension (`extensions.worktreeConfig`), acknowledging that while some workflows would benefit from per-worktree ignore patterns, others might prefer the current global behavior.

This moves the discussion forward by offering a concrete technical direction (using the extensions mechanism) while recognizing the need to maintain backward compatibility. The reply doesn't take a strong position on whether the change should be made, but provides a viable path for implementation if the community agrees it's desirable.