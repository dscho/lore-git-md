This is a response in an ongoing discussion about worktree-specific gitignore behavior. brian m. carlson weighs in on D. Ben Knoble's question about why secondary worktrees don't respect their own `info/exclude` files while the main worktree's exclude rules apply globally.

The response suggests this wasn't an intentional omission, but rather an area that wasn't fully developed. brian m. carlson sees potential value in per-worktree ignore patterns but notes some users might prefer the current global behavior. They propose adding this as an optional feature behind a configuration flag (`extensions.worktreeConfig`) rather than changing the default behavior.

This moves the discussion forward by acknowledging the validity of the use case while suggesting a conservative implementation path that maintains backward compatibility. The response doesn't commit to any specific technical solution but opens the door for further design discussion about how such a feature might work.