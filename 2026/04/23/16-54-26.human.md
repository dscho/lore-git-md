This is part of the ongoing `the_repository` removal effort, specifically migrating the `core.checkstat` configuration from a global variable to repository-specific storage in `struct repo_config_values`. The patch continues the pattern seen in previous configuration migrations in this series.

The change moves the `check_stat` variable that controls how `match_stat_data()` determines file freshness, preserving its eager-parsing behavior while making it repository-specific. This prevents cross-repository state leakage and is another step toward eliminating global configuration state. The mechanical changes touch four files, updating all references to use the new repository-specific storage location.

The patch appears well-contained and follows the established pattern for these migrations. It maintains the existing behavior while progressing the larger architectural cleanup. The commit message clearly explains the rationale for keeping eager parsing (to avoid complications in stat-matching behavior) and how this fits into the broader effort.