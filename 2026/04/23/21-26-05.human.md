This is a question about worktree-specific gitignore behavior. D. Ben Knoble notes that while the main worktree's `.git/info/exclude` file works as expected, secondary worktrees' corresponding files don't contribute to ignore rules. He asks whether this is an intentional design choice or an oversight, pointing out that the current behavior means the main worktree's exclude file affects all worktrees, which may not always be desired.

The question touches on a subtle aspect of Git's worktree implementation - whether ignore rules should be worktree-specific or repository-wide. This could be particularly relevant for users managing multiple worktrees with different ignore needs (like build artifacts or IDE files that might vary per worktree). The thread may prompt discussion about potential changes to make exclude files worktree-aware.