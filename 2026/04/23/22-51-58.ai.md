This is the third version of a feature patch series introducing `--reverse=before` for revision walks, allowing commit reversal to happen before limiting rather than after. The series consists of two patches: the core implementation and a memory optimization.

Key changes in v3:
1. Documentation has been updated to incorporate Jeff King's suggested wording improvements, particularly clarifying the toggle behavior and historical edge cases
2. The memory optimization patch has been cleaned up with stylistic fixes to the queue handling code
3. The range-diff shows the specific doc changes and code simplifications made since v2

The implementation remains focused on enabling use cases like `git log --reverse --max-count=3` to show the first commits rather than the last, with robust handling of multiple `--reverse` specifications. The memory optimization now uses cleaner queue management while maintaining the sliding window approach for reduced memory usage when combining `--reverse=before` with commit limits.

The thread has progressed from initial implementation through optimization and documentation refinement, with all technical feedback addressed except Junio's high-level questions. The patch appears ready for maintainer consideration with documentation now finalized per Jeff King's review. Key files touched remain `revision.[ch]` for the state machine changes, `list-objects.c` for traversal, and `t4202-log.sh` for test coverage.