This is a platform compatibility patch that reverts a previous attempt to introduce a `writev()` wrapper for Windows systems. The change removes the compatibility layer that was added to emulate POSIX writev() functionality on platforms lacking native support.

The patch:
- Deletes the `compat/writev.c` implementation file
- Removes related Makefile and meson build system entries
- Strips out the `NO_WRITEV` conditional code from headers
- Updates platform-specific configuration for Windows/MinGW

This appears to be a cleanup of an unused or problematic compatibility layer, though the commit message doesn't specify why the revert is needed. The change is mechanical and focused, touching only the compatibility infrastructure without affecting core Git functionality. It's part of Olamide Caleb Bello's ongoing series (now at v3) related to repository configuration refactoring, though this particular patch seems to be a standalone platform compatibility adjustment.