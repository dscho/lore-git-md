Here's the updated human-readable summary:

The thread examines why Git's secondary worktrees don't respect their own `info/exclude` files for ignore patterns, while the main worktree's exclude rules apply globally. D. Ben Knoble initially raised this as a question about whether the current behavior was intentional design or missing functionality.

brian m. carlson has now responded, suggesting this was likely an unimplemented feature rather than a deliberate limitation. They propose implementing per-worktree ignore patterns as an optional extension (`extensions.worktreeConfig`), which would allow workflows that need worktree-specific ignores to opt in while maintaining the current global behavior for others. This provides a technical path forward that preserves backward compatibility.

The discussion has now shifted from understanding the current behavior to considering whether and how to implement the proposed extension. No other contributors have weighed in yet on the technical merits or potential impacts of this approach. The core question remains open about whether the Git project should adopt this change, but we now have a concrete implementation suggestion on the table.