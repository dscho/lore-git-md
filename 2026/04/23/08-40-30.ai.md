This is a refactoring patch from Karthik Nayak that removes an unused typedef from the refs subsystem. It's part of a larger v2 series (9 patches) standardizing reference backend behavior, but this particular change is a straightforward cleanup.

The patch removes `ref_transaction_commit_fn` from `refs/refs-internal.h` since it's not referenced anywhere in the codebase. This appears to be a leftover from previous refactoring work - the type was likely used at some point but became obsolete as the code evolved.

As a standalone change, this is a simple, uncontroversial cleanup that doesn't affect functionality. The removal aligns with the series' broader goal of tidying up the refs interface, though this particular change isn't directly related to the main thrust of the series (consolidating object validation logic).

For future thread context: this first patch in the series removes an unused typedef from refs-internal.h, cleaning up the interface before subsequent patches introduce more substantial changes to the refs backend architecture. The change is mechanical and doesn't affect behavior.