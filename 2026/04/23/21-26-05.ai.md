This is a question about worktree-specific gitignore behavior. D. Ben Knoble is asking why secondary worktrees don't respect their own `info/exclude` files, while the main worktree's exclude file affects all worktrees. He wonders if this is an intentional design choice or an unimplemented feature.

The current behavior means that per-worktree ignore patterns can't be set independently - all worktrees share the main worktree's exclude rules. This could be problematic for workflows where different worktrees need different ignore patterns. The question touches on the interaction between `$GIT_DIR`, `--git-dir`, and how Git resolves ignore paths for multiple worktrees.

The thread appears to be starting a discussion about whether this behavior should be changed, though no specific proposal is made yet. The question is framed as seeking clarification about current behavior rather than suggesting a particular solution.