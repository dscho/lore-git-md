This is the second patch in Patrick Steinhardt's v2 series introducing the `git history fixup` command, focused on refactoring internal infrastructure. The patch generalizes the `commit_tree_with_edited_message_ext()` function into a more flexible `commit_tree_ext()` that can be controlled via flags, with the first flag (`COMMIT_TREE_EDIT_MESSAGE`) determining whether to edit the commit message.

The change is a refactoring that doesn't modify behavior - it just makes the function more reusable for the upcoming `fixup` subcommand in patch 3. The implementation looks clean and maintains all existing functionality while adding the new flag parameter. The patch follows Git's style guidelines and includes updates to all call sites.

This is a typical preparatory patch in a larger series - it doesn't introduce user-facing changes but enables the functionality that will come in the next patch. The thoroughness of the refactoring (updating all callers) and the clear commit message suggest this is a well-considered change that shouldn't introduce regressions.