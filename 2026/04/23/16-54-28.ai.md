This is part of the ongoing `the_repository` removal effort, specifically migrating the `pack_compression_level` configuration from a global variable to repository-specific storage in `struct repo_config_values`. The patch follows the established pattern for configuration migrations in this series, maintaining eager parsing behavior while preventing cross-repository state leakage.

The change affects several key compression-related code paths:
- Packfile creation in `pack-objects`
- Object streaming in `fast-import`
- Blob writing in the object-file layer

The patch updates all direct references to the global `pack_compression_level` to instead access the repository-specific value through `repo_config_values()`. The compression level retains its default of `Z_DEFAULT_COMPRESSION` and continues to validate the same range (0 to `Z_BEST_COMPRESSION`). 

This is a mechanical but important change that continues the incremental progress toward eliminating global configuration state. The modifications are straightforward and follow the same template as previous configuration migrations in the series. The patch touches 5 files with mostly small, repetitive changes to access patterns rather than deep logic modifications.

The change is well-contained and appears unlikely to introduce behavior changes, though it does affect core operations like pack creation that are performance-sensitive. The patch maintains all existing validation and default handling while just moving where the value is stored.