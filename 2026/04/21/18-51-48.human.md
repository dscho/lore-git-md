This is a follow-up in the ongoing pseudo-merge bitmap bugfix series from Taylor Blau. The thread has seen active participation from multiple core contributors, with Elijah Newren now catching an API consistency issue in the test helper implementation.

Taylor responds to Elijah's observation that `bitmap_writer_push_commit()` is being called with `false` for its `pseudo_merge` parameter while the function is declared to take an `unsigned`. Rather than changing the function signature to use `bool`, Taylor opts to pass `0` for now to maintain consistency with the existing API, noting they don't want to make a habit of changing type signatures across the codebase.

This is a minor but substantive point in the larger context of adding test infrastructure for pseudo-merge bitmap fixes. The exchange demonstrates the careful attention to detail in the review process, even for what might seem like small implementation choices. The thread continues to evolve with multiple reviewers weighing in on both the test infrastructure and the actual bugfixes in subsequent patches.