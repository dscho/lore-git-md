This is a follow-up discussion about the `git-credential-libsecret` security issue where credentials are leaked to stdout during store operations. Mantas Mikulėnas questions whether the documented workflow being referenced is actually the correct one, suggesting that `git credential approve` is the intended user interface while direct helper invocation is an internal implementation detail.

The exchange highlights an important clarification about the credential helper protocol's intended usage. While the original reporter demonstrated the vulnerability using the direct helper invocation pattern, this follow-up suggests that vulnerability might only affect an undocumented/internal usage pattern rather than the official public API.

The thread is now exploring whether this changes the severity assessment - if the leak only occurs when using non-recommended invocation methods, it may be less critical than initially thought. However, the core issue of credential leakage remains concerning regardless of the invocation method.

This discussion will likely lead to either:
1) Confirmation that the direct helper invocation is indeed a supported pattern (making this a real security issue)
2) Clarification that only `git credential` commands should be used directly (making this more of a documentation/usage issue)
3) Possibly both - fixing the helper while also better documenting the proper usage patterns