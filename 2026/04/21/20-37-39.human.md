This is a refactoring patch in Taylor Blau's ongoing series to improve MIDX/bitmap-based repacking for large repositories (part 9 of 16). The patch extracts common MIDX command preparation logic into a new helper function `repack_prepare_midx_command()` that will be reused by future patches.

The change moves the shared setup for `git multi-pack-index` commands (setting git_cmd, handling progress/bitmap flags) into this helper, while leaving subcommand-specific arguments (like `--stdin-packs`) to the individual callers. This is a clean, mechanical refactor with no behavior changes - the diff shows the same functionality being executed, just a more modular organization.

The patch continues the series' pattern of careful, incremental preparation for upcoming functionality. It follows earlier patches that introduced MIDX layer compaction and geometric repacking strategies. The extracted helper will be used by subsequent patches that need to construct similar MIDX commands for different subcommands.