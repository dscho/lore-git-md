This is a bugfix series addressing issues with duplicate tree entries and D/F conflict detection, primarily affecting the merge machinery. The patches come from Elijah Newren, the recognized expert on Git's merge subsystem.

The series stems from real-world cases where corrupt trees with duplicate entries caused assertion failures in merge-ort. The first three patches are preparatory cleanups to error handling and resource management in merge-ort. The fourth patch makes merge-ort properly abort when encountering duplicate tree entries rather than hitting assertions. The fifth patch fixes verify_cache() to catch non-adjacent directory/file conflicts that could lead to tree corruption.

The changes are well-motivated by actual problems encountered in the wild, and the series includes extensive test coverage with two new test files (t0093-direct-index-write.pl and t0093-verify-cache-df-gap.sh) plus additions to existing merge tests. The patches touch sensitive areas of the codebase (merge-ort and cache-tree verification) but come from a domain expert and appear carefully constructed.

For AI summarizer context: This series hardens merge-ort against corrupt trees with duplicate entries and improves D/F conflict detection in verify_cache(). Key changes involve error propagation in traverse_trees_wrapper(), cleanup of diff pairs queues, and new abort logic for duplicate entries. The verify_cache() fix specifically addresses cases where D/F conflicts weren't caught when entries weren't adjacent in the index. Tests demonstrate both the corruption scenarios and the fixes.