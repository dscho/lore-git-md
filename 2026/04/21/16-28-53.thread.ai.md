Git thread summary (AI mode):

Subject: git-gui bugfix for bare repositories and missing worktrees. Core problem: git-gui fails with cryptic Tcl errors when launched from bare repos or locations without accessible worktrees, including special cases of separate git dir configurations. Originating author: Shroom Moo. Thread type: bugfix for git-gui.

Current status: The discussion now presents three technical solutions with a newly identified safety concern:
1. Shroom Moo's v4 patch (adopting Mark Levedahl's strict approach) blocks all gitdir launches
2. Johannes Sixt's (Hannes) middle ground preserving .git directory launches
3. Mark Levedahl's demonstration of data corruption risk in Hannes' approach when core.bare=false with no worktree

Key technical details: The critical new finding shows Hannes' proposal could allow git-gui to commit files directly into gitdir (e.g., `refs/heads/master` as a file) when core.bare=false but no valid worktree exists. Mark asserts git-gui should only operate when `--is-inside-work-tree` is true, with two possible safe fallbacks: immediate error or interactive worktree selection dialog. The safety concern centers on silent directory switching potentially masking user errors.

Open questions: The debate now balances:
1. Strict correctness (v4) preventing all gitdir launches
2. Backward compatibility (Hannes) preserving .git directory use
3. Newly demonstrated data corruption risks in the compatibility path
The technical arguments are fully articulated; resolution requires maintainer input on whether to prioritize safety over backward compatibility.

Participant positions: Shroom Moo implemented the strict approach in v4. Hannes advocates for compatibility with historical .git directory launches. Mark Levedahl now provides concrete evidence supporting strict correctness by demonstrating data corruption risks in Hannes' proposal. The thread still lacks input from maintainers (Junio or git-gui maintainer Johannes Schindelin) to make the final policy decision.

Related work: The discussion continues to surface historical context about git-gui's evolution pre-dating worktree support. Mark's edge case strengthens the argument that this may need to be a breaking change for safety, setting a potential precedent for how Git tools handle repository boundary detection. The thread exemplifies the tension between maintaining legacy behavior and preventing data corruption.