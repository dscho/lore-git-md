Git thread summary (AI mode):

Subject: git-gui bugfix for bare repositories and missing worktrees. Core problem: git-gui fails with cryptic Tcl errors when launched from bare repos or locations without accessible worktrees, including separate git dir configurations. Originating author: Shroom Moo. Thread type: completed bugfix series for git-gui.

Current status: The 11-patch v8 series has been finalized with comprehensive fixes for git-gui's repository detection and environment handling. The latest changes include:
- New `is_gitvars_error` procedure making repository discovery errors fatal when GIT_DIR/GIT_WORK_TREE are set
- Centralized environment variable management functions (set_gitdir_vars/unset_gitvars_error)
- New `pick_repo` procedure that modularizes repository selection UI
- Simplified path handling using `git rev-parse --absolute-git-dir` instead of manual path conversion
- Removed special-case handling for "." git-dir output since absolute paths are now guaranteed
- Maintained all existing functionality while improving code organization

Key technical details:
- Repository state detection occurs immediately after config loading with mode-specific requirements
- `core.bare` tri-state behavior (true/false/unset) properly handled
- Clean separation between environment-driven and automatic detection paths
- New `is_gitvars_error` makes discovery errors fatal when environment variables are set
- Error messages show invalid GIT_DIR/GIT_WORK_TREE values with specific configuration details
- Visualization menu items properly disabled in bare mode
- Path normalization in browser mode uses Git's built-in absolute path resolution
- New centralized environment variable management ensures safe handling of GIT_DIR/GIT_WORK_TREE
- Repository picker logic encapsulated in dedicated `pick_repo` proc that:
  * Clears GIT_DIR/GIT_WORK_TREE
  * Loads configuration
  * Shows repository picker dialog
  * Sets _gitdir based on selection
- Maintains identical behavior while improving code structure
- Prepares for future 'pick' subcommand addition

Open questions resolved:
1. Bare repo detection timing and mode-specific requirements finalized
2. Historical .git directory behavior preserved
3. Relative path fallback removed as unsafe
4. gitk menu item handling in bare repos implemented
5. Environment variable inheritance issues resolved
6. GIT_DIR/GIT_WORK_TREE consistency between CLI and env var forms
7. Subprocess environment cleanup implemented
8. Browser mode path normalization simplified using `--absolute-git-dir`
9. Blame/browser argument parsing simplified
10. Centralized environment variable management added
11. Repository picker logic modularized
12. Explicit environment variable cases now trigger fatal errors with detailed messages

Participant positions: Shroom Moo (author, consolidated series into final patch including this refactoring), Johannes Sixt (confirmed architectural soundness), Mark Levedahl (finalized repository state detection and provided final approval). The series reflects complete consensus with all technical points addressed and is ready for merging.

Related work: Builds on git-gui behavior changes since 2010, addressing 2014/2019 regressions. Establishes patterns for early environment validation, graceful feature degradation, and consistent environment handling. The v8 series provides comprehensive fixes for all identified edge cases with clarified detection logic, now with improved code organization through the new `pick_repo` procedure, simplified path handling via `--absolute-git-dir`, and robust error handling via `is_gitvars_error`.