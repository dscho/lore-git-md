This is a follow-up in the thread about fixing commit signing regressions for non-UTF-8 messages. brian m. carlson responds to Kushal Das's suggestion about expanding test coverage to include non-GnuPG signing tools.

The key points:
- brian m. carlson explains that the signing code is abstract at the point where the fix was applied, so it should work the same way with SSH or X.509 signatures
- They don't believe separate tests for other signing methods are necessary since the behavior should be consistent across all signing backends
- This maintains the technical focus of the thread while addressing the test coverage question raised in the previous email

The response indicates the fix is considered complete from a technical perspective, with no additional testing needed for other signing methods. The thread appears to be winding down after successfully addressing both the core issue and follow-up questions about test coverage.