This is a technical follow-up in the `--reverse=before` revision walk patch series, where Jeff King (Peff) responds to Mirko Faina's question about handling multiple `--reverse` flags. The discussion has moved from implementation mechanics to edge case behavior.

Peff proposes an alternative state machine design that would preserve the `reverse_when` setting (before/after) when toggling with bare `--reverse` flags, allowing a triple-reverse to return to the original `reverse=before` state rather than defaulting to `after`. He acknowledges this might not necessarily be clearer to users, but highlights the need to document the subtle differences in behavior between `--reverse` (which toggles) and `--reverse=<value>` (which sets).

The exchange shows the revision-walk expert considering both technical cleanliness and user expectations, with Peff's proposed solution maintaining more state than Mirko's original approach. The thread demonstrates how even after reaching consensus on core functionality, Git contributors carefully examine edge case behaviors that could surprise users or violate the principle of least surprise. Documentation of these nuances is flagged as essential before the feature can be considered complete.