This is a performance patch in Taylor Blau's ongoing series to improve Git's repacking strategy for large repositories. It introduces a `--no-write-chain-file` option for incremental MIDX writes, allowing callers to assemble MIDX chains atomically.

The patch adds a new flag that writes a new MIDX layer without immediately updating the chain file, instead printing the checksum of the new layer. This enables the upcoming incremental repacking strategy to build the complete chain before atomically updating it. The change includes thorough documentation updates, validation to ensure the flag is only used with `--incremental`, and new test coverage.

The implementation carefully handles lockfile management and maintains existing behavior while adding the new capability. This is a well-constructed patch that fits cleanly into the larger MIDX/repacking architecture improvements Taylor has been developing. The changes are focused and clearly motivated by the needs of the incremental repacking strategy mentioned in the thread context.