This is a documentation fix in Taylor Blau's ongoing series addressing pseudo-merge bitmap bugs. The patch corrects an outdated example in the gitpacking(7) man page where the sampleRate configuration was shown as 100 (from when it accepted percentage values) rather than the current valid range of 0 to 1. 

The change is straightforward - updating two example configurations to use `sampleRate = 1` instead of the incorrect `sampleRate = 100`. This matches the actual implementation's expectations that were fixed earlier in the series (patch 7/9) to properly validate the sampleRate range. 

The fix was prompted by Elijah Newren noticing the discrepancy during review. This is a minor but important documentation alignment to prevent users from copying invalid configurations from the examples. The change is part of a well-structured series that has been carefully addressing all identified issues in the pseudo-merge bitmap implementation.