This is a test fix addressing shell scripting robustness in the test suite. SZEDER Gábor follows up on his earlier patch with an updated version that maintains the same technical fix (quoting tildes in grep patterns) but improves the commit message to be more precise.

The patch modifies `t6112-rev-list-filters-objects.sh` to properly quote tilde characters in grep patterns, preventing potential shell expansion issues. While the original 2019 test worked in practice (since object hashes are unlikely to match system usernames), the unquoted tildes represented a theoretical edge case that could cause problems with certain Bash versions.

The change is minimal (3 lines modified) and well-contained, simply adding quotes around the grep patterns. It doesn't affect Git's core functionality - the test continues verifying the same object filter behavior, just with more robust pattern matching. The thread shows Git's attention to test suite reliability, with SZEDER thoroughly investigating both theoretical concerns and real-world shell behaviors before submitting this straightforward improvement.