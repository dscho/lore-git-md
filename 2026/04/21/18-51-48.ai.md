This is a follow-up in the pseudo-merge bitmap bugfix series, specifically addressing an API consistency issue raised in review. Taylor Blau responds to Elijah Newren's observation about a type mismatch in the test helper function `bitmap_writer_push_commit()`.

The issue is narrow but technically substantive: the function is declared to take an `unsigned` parameter but is being called with `false`. Taylor opts for the minimal fix - changing the call to pass `0` instead - rather than modifying the function signature to use `bool`. He notes this avoids setting a precedent for widespread type changes, though leaves the door open for future cleanup.

This exchange continues the thread's pattern of thorough technical scrutiny, with Elijah's review catching an API detail and Taylor making a deliberate choice about how to handle it. The discussion remains focused on implementation quality rather than the core bitmap functionality being tested. 

The response doesn't introduce new technical complexity but does show Taylor's maintainer perspective in balancing immediate fixes against broader codebase consistency concerns. This is a small but meaningful piece of the larger effort to solidify the pseudo-merge bitmap implementation through improved testing and careful code review.