This is a test modernization patch from Patrick Steinhardt's v6 series to make Git's test suite compatible with `set -e`. The patch updates the bisection test script to replace `expr` calls with shell arithmetic (`$(( ))`), which is more reliable under strict error checking.

The change addresses a specific issue where `expr` would fail when returning zero, which would cause problems when `set -e` is enabled. The patch also modernizes the test formatting by converting the multi-line `test_expect_success` to use a more readable quoted block style.

This is part of the final set of mechanical adaptations needed for the test suite to work with `set -e` enabled by default. The series has already gone through extensive review and testing, with this particular change being a straightforward cleanup of arithmetic operations in the test suite. The patch maintains the exact same test semantics while making the code more robust under strict error checking.