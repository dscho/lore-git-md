This is the first patch in a test modernization series for t7004-tag.sh, focusing on making tag-related tests more robust. The patch addresses a specific test that was checking tag counts in a brittle way by replacing hardcoded line counts with direct state verification.

The key improvement here is replacing the fragile `test_line_count = 1` checks with a more reliable approach that captures the tag list before and after invalid tag creation attempts, then verifies they match using `test_cmp`. This implements Junio's "belt-and-suspenders" suggestion from the v1 review, ensuring we both check the command fails as expected and confirm the repository state remains unchanged.

The change is small (7 lines modified) but meaningful - it makes the test more isolated and less likely to break due to unrelated changes in the test file. The patch follows the project's test modernization patterns by moving away from global state assumptions toward explicit verification. Given Junio's prior approval of the approach and the author's responsiveness to feedback, this appears to be on track for inclusion.