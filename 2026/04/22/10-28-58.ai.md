This is the first patch in Patrick Steinhardt's 2-part series introducing a `git history fixup` subcommand. It's a preparatory refactoring that generalizes an internal function in `builtin/history.c` to make it more flexible for the new feature.

The patch refactors `commit_tree_with_edited_message_ext()` into a more generic `commit_tree_ext()` that takes a flags parameter. This allows callers to control whether the commit message should be edited (the original behavior) or kept as-is (new capability needed for the upcoming fixup command). The change is mechanical but thorough - all existing call sites are updated to pass the new flag maintaining their current behavior.

Key technical details:
- Introduces `enum commit_tree_flags` with `COMMIT_TREE_EDIT_MESSAGE` flag
- Renames and modifies the function to conditionally edit messages
- Updates 3 existing call sites to maintain current behavior
- No functional changes yet - this is pure refactoring

The patch appears well-structured with a clear commit message explaining both the what and the why. The diff shows careful attention to maintaining existing behavior while enabling future functionality. This is typical of Patrick's approach - breaking changes into logical, reviewable steps with preparatory work coming first. The changes are localized to `builtin/history.c` and don't affect other subsystems.