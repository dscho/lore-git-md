This is patch 6/8 in Karthik Nayak's series refactoring Git's reference backend to consolidate object validation logic. The patch moves object ID validation from individual backends to the shared refs layer, eliminating duplicate logic across implementations.

Karthik confirms Patrick Steinhardt's suggested wording change ("which" instead of "this") in the commit message. The technical approach has already been approved in prior discussion - this validation now happens when updates are queued rather than during transaction preparation, which Patrick endorses as "a good step" that reduces backend entanglement with the object database.

The change means validation errors can't be ignored in batched updates, but callers get specific error codes to handle appropriately. This builds on preparatory work from earlier patches in the series. With this response, all feedback on patch 6 is addressed, leaving only minor open items in the series (primarily the typedef style discussion from patch 1). The architectural direction remains uncontested.