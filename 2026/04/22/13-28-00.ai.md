This is a brief follow-up in an ongoing thread about improving rebase todo list parsing in `git status` output. Patrick Steinhardt questions whether the project actually uses the `-Wswitch` compiler warning flag that Phillip Wood's patch relies on to catch unhandled enum cases.

The exchange is technical but brief - Patrick notes that a code search didn't find any existing use of `-Wswitch` in Git's codebase, casting doubt on whether the defensive programming approach in the patch would actually catch missing cases in practice. This raises a valid implementation concern, though it doesn't challenge the core functionality of the patch series.

The thread context shows this is a minor point in an otherwise well-reviewed series that has already addressed more substantive technical feedback from Elijah Newren and others. The core changes (better rebase command parsing and status display) remain uncontested.