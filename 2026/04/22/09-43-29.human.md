This is a supportive follow-up in the `contrib/subtree` performance discussion, where Johannes Schindelin weighs in with Windows-specific context. He expresses enthusiasm for Ian Jackson's proposed Rust/libgit2 implementation, citing the particularly poor performance of shell-based Git commands on Windows as motivation for replacing the current script.

Johannes notes that Git for Windows has long shipped `git subtree` in its installers, making this a practical concern beyond just technical interest. He highlights two specific benefits of a Rust version: better performance (already the thread's focus) and more robust error handling (a new point in this discussion). His message serves as validation that there's real-world demand for the Rust alternative, especially on Windows where shell script overhead is more pronounced.

The email reinforces the thread's emerging consensus that Colin Stagner's shell patches will handle in-tree maintenance while Ian's Rust version can proceed externally. Johannes' Windows perspective adds weight to Ian's approach without challenging Junio Hamano's policy against Rust in git.git itself. This positions the Rust version as a natural candidate for future inclusion in Git for Windows specifically, regardless of its relationship to the main Git project.