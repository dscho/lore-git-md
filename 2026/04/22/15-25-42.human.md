This is part 3/7 in Derrick Stolee's series to improve fetch/push negotiation controls, particularly for monorepo scenarios. The patch performs a mechanical rename of internal variables and functions from `negotiation_tips` to `negotiation_restrict_tips` to better reflect the option name (`--negotiation-restrict`) that was finalized in previous discussion.

The changes are straightforward and consistent across multiple files (`builtin/fetch.c`, `fetch-pack.c`, `transport.c` etc.), updating both the data structures and their usage sites. The rename maintains backward compatibility through an OPT_ALIAS while making the internal naming match the user-facing option. This is a clean refactoring that doesn't change behavior, just improves code clarity by aligning names with the now-stable external interface.

The patch fits neatly into the larger series which has already addressed substantive technical feedback and appears ready for integration. This particular change is the sort of mechanical cleanup that typically gets quick approval when it follows consensus on naming.