This is part 2 of Derrick Stolee's v3 series introducing negotiation control options for fetch/push operations. The patch renames the existing `--negotiation-tip` option to the more accurately named `--negotiation-restrict` while maintaining backward compatibility.

The change clarifies that this option restricts the negotiation set rather than specifying a minimum set, which better aligns with the planned addition of a complementary `--negotiation-include` option in later patches. The patch:

1. Adds `--negotiation-restrict` as a synonym for `--negotiation-tip`
2. Updates documentation to indicate the new name is preferred
3. Makes warning messages translatable and uses the new option name
4. Adds tests verifying the new option works identically to the old one
5. Ensures both options can be used together

The implementation touches `builtin/fetch.c`, `builtin/pull.c`, and related test files, with careful attention to maintaining backward compatibility. This is a straightforward renaming that doesn't change functionality but improves the naming scheme for the broader negotiation control feature set.

For summarizer brief mode: This patch introduces `--negotiation-restrict` as the new name for what was previously `--negotiation-tip`, marking the old name as an alias. Key technical points:
- Adds OPT_ALIAS for backward compatibility
- Updates warning messages to use new name while making them translatable
- Verifies identical behavior through new tests in t/t5510-fetch.sh
- Maintains existing protocol behavior while improving option naming clarity
- Prepares for the complementary `--negotiation-include` option coming later in the series