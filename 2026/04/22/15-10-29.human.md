Bugfix for commit signing with non-UTF-8 messages. This is a follow-up to brian m. carlson's patch that fixed a regression where signatures were being generated before UTF-8 validation, causing verification failures. Elijah Newren raises two technical questions about the implementation:

1. Points out that users might see duplicate "commit message did not conform to UTF-8" warnings since the check is now done separately for both the main buffer and compat_buffer (which likely contain the same invalid UTF-8 content).

2. Questions whether the patch also incidentally fixed a short-circuiting error in the original code where ensure_utf8() would only process the first buffer if both were invalid.

The discussion appears to be wrapping up, with Elijah's comments representing final polish on an otherwise solid fix. His tone suggests the core solution is correct, focusing now on potential edge cases in the warning behavior. The test case (using invalid UTF-8 bytes 0xfe and 0xff) remains uncontested and provides good regression protection.

This thread shows the project's attention to detail - even after the main bug is fixed, experienced reviewers like Elijah examine secondary effects like warning behavior and error handling paths. The fix is already in place, so these comments may lead to a follow-up refinement rather than blocking the current solution.