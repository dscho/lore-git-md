This is a review comment on the second patch in brian m. carlson's series fixing a commit signing regression for non-UTF-8 messages. Elijah Newren provides a substantive review, identifying two potential issues with the implementation.

First, he notes that users might see the UTF-8 warning message twice - once for the main buffer and once for the compatibility buffer - when both contain invalid UTF-8. This suggests the warning could be redundant in some cases.

Second, he observes that the original code had a logical short-circuiting issue where `ensure_utf8()` would only be called on the first buffer if both were invalid, due to the `||` operator. The patch fixes this by handling each buffer separately, though Elijah frames this as a question rather than a direct criticism.

The review is substantive, engaging with the technical details of the change and considering edge cases the original patch might have missed. Elijah approves of the test case but raises these two implementation questions that warrant consideration. His tone is constructive and technical, focused on improving the patch rather than blocking it.

This continues the thread's technical discussion about ensuring proper UTF-8 handling in commit signing, with Elijah providing experienced review feedback on the patch's specific implementation choices. The core fix remains sound, but these observations could lead to minor refinements in the warning messages and error handling paths.