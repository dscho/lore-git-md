Documentation patch fixing an inconsistency between a config option name and its corresponding command-line flag. The patch corrects a reference to `format.commitListFormat` in the docs that still mentioned the old `--cover-letter-format` option name instead of the current `--commit-list-format`. This appears to be a straightforward documentation update that was missed during a previous rename.

The change is minimal (a single line) and clearly motivated - it aligns the documentation with the actual command-line interface. The patch includes a Signed-off-by line and follows standard formatting. Since this is a simple doc fix for an already-merged change, it's unlikely to be controversial.