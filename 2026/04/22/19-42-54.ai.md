This is a technical discussion thread about the `--reverse=before` revision walk patch, where Mirko Faina responds to D. Ben Knoble's questions about the implementation details. The exchange focuses on the handling of `max_count` in the new `--reverse=before` mode and whether the counting responsibility should be refactored.

Mirko explains that in `--reverse=before` mode, the entire history is walked first (with `max_count=-1`), then commits are returned from the reversed list while manually decrementing `max_count` - unlike `--reverse=after` which relies on `get_revision_internal()`'s built-in counting. He argues against refactoring to move the counting responsibility into `get_revision_internal()` for the new mode, as it would require adding state to `rev_info` without clear benefit.

The discussion shows the patch is technically sound but reveals some complexity in `get_revision()`'s growing responsibilities. This is a substantive review that helps ensure the implementation remains maintainable while preserving the struct's memory efficiency. The thread demonstrates the careful attention to edge cases that characterizes Git's commit walking machinery.