This is a technical follow-up in the ongoing discussion about the `--reverse=before` revision walk patch. Mirko Faina responds to D. Ben Knoble's questions about the implementation details, specifically addressing concerns around `max_count` handling in the new mode.

The key clarification is that in `--reverse=before` mode, the counting responsibility shifts from `get_revision_internal()` to `get_revision()` itself. Mirko explains that maintaining state across calls would require adding fields to `rev_info`, which they want to avoid given the struct's space constraints. The current approach keeps the temporary `max_count` within `get_revision()` during the reversal phase.

This exchange shows the patch is in good technical shape, with all substantive review feedback addressed. The discussion now centers on implementation tradeoffs rather than correctness issues, suggesting the feature is nearing readiness for inclusion. The careful attention to memory usage and struct layout reflects Git's typical attention to low-level efficiency concerns.