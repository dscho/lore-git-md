This is a documentation patch fixing an inconsistency in the config documentation for `format.commitListFormat`. 

The patch corrects a reference to the old `--cover-letter-format` option name that was missed when the option was renamed to `--commit-list-format`. The change aligns the documentation with the actual command-line interface, though the commit message notes this should have been caught before the Git 2.54 release.

The change is straightforward - a single line fix in `Documentation/config/format.adoc` that updates the option name reference. The patch appears to be a simple cleanup with no functional impact, just keeping docs in sync with the code.