This is a technical discussion about the `--reverse=before` revision walk patch, where D. Ben Knoble follows up on his earlier review of Mirko Faina's implementation. The exchange focuses on understanding the subtle control flow around `max_count` handling in the new `--reverse=before` mode.

Ben acknowledges Mirko's detailed explanation from earlier in the thread about why `max_count` needs special handling in the new mode. While he initially struggled to understand the implementation, Mirko's clarification about the two-phase operation (walk phase and output phase) helps him see how `max_count` interacts differently with each phase. The key insight is that in `--reverse=before` mode, `get_revision_internal()` isn't called during the output phase, so the counting responsibility shifts to `get_revision()` itself.

Ben notes this makes `get_revision()`'s responsibilities more complex, though he doesn't push for an immediate refactor. The discussion exemplifies Git's culture of thorough technical review, where even experienced contributors like Ben take time to fully understand complex control flow changes before giving feedback. The thread shows the patch is maturing through this careful scrutiny.