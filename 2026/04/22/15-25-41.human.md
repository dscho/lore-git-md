This is part 2 of Derrick Stolee's v3 series introducing negotiation control options for fetch/push operations. The patch renames the existing `--negotiation-tip` option to the more accurately named `--negotiation-restrict`, while keeping the old name as an alias for compatibility.

The change clarifies that this option restricts the negotiation set rather than specifying a minimum set (which will be introduced in a later patch as `--negotiation-include`). The patch updates documentation, warning messages, and tests to use the new name while maintaining backward compatibility. Three new test cases verify the behavior with glob patterns and mixed old/new option names.

This is a straightforward renaming patch that follows from earlier discussion in the thread about making the option names more accurately reflect their semantics. The implementation remains unchanged - it's purely a naming improvement to set up for the complementary `--negotiation-include` functionality coming later in the series.

The patch is well-contained with thorough test coverage and maintains backward compatibility, making it a low-risk change that helps clarify the negotiation control interface.