This is a technical discussion thread about the `--reverse=before` revision walk patch, where D. Ben Knoble follows up on his earlier review with additional questions about the implementation details. The exchange focuses on understanding the `max_count` handling in the new `--reverse=before` mode.

Ben initially misunderstood the `reverse=2` (before) vs `reverse=1` (after) distinction, prompting Mirko to clarify the key difference in how `max_count` is handled between the two modes. The core insight is that in `--reverse=before` mode, the entire history must be walked first (with `max_count=-1`), then commits are returned from the reversed list while manually decrementing `max_count` - unlike `--reverse=after` which relies on `get_revision_internal()`'s built-in counting.

After some back-and-forth, Ben arrives at understanding that the counting responsibility shifts from `get_revision_internal()` to `get_revision()` itself in the new mode, though he notes this makes `get_revision()`'s responsibilities more complex. The discussion exemplifies the careful attention to edge cases that characterizes Git's commit walking machinery.

This is a substantive review that digs into the control flow implications of the new feature, showing how reviewers help ensure correctness even in subtle interactions between components. The thread demonstrates the patch is nearing readiness, with all technical questions being resolved through patient explanation.