This is patch 10/12 in Patrick Steinhardt's series to enable `set -e` (errexit) in Git's test suite for Bash 5+. The patch fixes a subtle issue in t6002-rev-list-bisect.sh where using the `expr` command for arithmetic could fail when the result is zero, which would break under strict error checking.

The change replaces `expr` with modern shell arithmetic (`$(( ))`), which doesn't have the same failure cases. The patch also modernizes the test function's style by splitting the test expectation across multiple lines for better readability. This is a mechanical, behavior-preserving change that follows the series' pattern of incrementally adapting tests to work with `set -e`.

The fix demonstrates the careful attention to detail in this series - catching edge cases where shell commands might fail in ways that weren't previously problematic. With this change, the bisection test will continue working as expected when error checking is enabled. The patch maintains the test's mathematical logic while making it more robust.