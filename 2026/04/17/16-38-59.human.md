This is a follow-up clarification about a Tig (separate project) bugfix patch that addressed a const-correctness compiler warning. The original patch fixed a `strchr()` constness mismatch in Tig's diff.c by making a variable `const char*` to match the input.

Collin Funk points out a small inaccuracy in the original commit message - the warning stems from a change in glibc 2.43 rather than being GCC-specific. This is a minor documentation nit rather than a technical issue with the patch itself. The actual code change remains correct and uncontroversial.

The exchange is brief and technical - just clarifying the root cause of the warning that motivated the fix. Since this is for Tig rather than Git core, it's unlikely to generate further discussion on the Git mailing list.