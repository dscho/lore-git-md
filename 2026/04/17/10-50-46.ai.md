This is the final version of Patrick Steinhardt's test infrastructure series to enable `set -e` (errexit) in Git's test suite for Bash 5+. The series has now incorporated all feedback from reviewers and maintainers, with the last refinement being a simplification of how multi-line variables are read in t9902-completion.sh.

The 12-patch series systematically prepares the test framework and individual tests to work correctly with strict error checking enabled. Key changes include adapting test helper functions like `test_match_signal()` and `test_must_fail()`, fixing conditional test execution patterns, and addressing specific test cases that would fail with `set -e`. The final patch enables `set -e` for Bash 5+ while maintaining compatibility with other shells.

The series has undergone thorough review from Jeff King (Peff) on architectural considerations, SZEDER Gábor on test correctness, and Junio Hamano on style and documentation. CI validation via the `linux-TEST-vars` job confirms the changes work as intended. The author has demonstrated responsiveness to feedback throughout multiple iterations, with the v4 changes focusing on a final readability improvement in t9902.

This represents a significant step forward in test robustness by ensuring errors outside test assertions are caught rather than silently ignored. The careful, incremental approach and extensive review process suggest this change is well-vetted and ready for merging. The Bash version constraint (5+) is an acknowledged tradeoff that Junio has accepted as reasonable given the benefits.