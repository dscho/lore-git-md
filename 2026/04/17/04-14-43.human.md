This is a technical discussion about improving `git subtree split`'s handling of merge-heavy histories, continuing from Ian Jackson's proposed algorithm refinement. Colin Stagner provides additional context about the current behavior and limitations of `git subtree`.

Colin notes that while the split operation is deterministic, it doesn't maintain consistency across different Git versions due to historical behavior changes. He points out that the tool's documentation promises identical commit IDs for identical histories, but this guarantee hasn't held up over time. He suggests a config file approach to track both current and historical split methods to allow improvements without breaking existing repositories.

The discussion highlights key challenges with merge handling in `git subtree`:
- The tool isn't fully aware of subtree merge operations
- Merge processing has been a source of behavior changes
- There's no robust way to track split history across rebases

Colin shares his personal workflow preferences, noting that plain merges often work better than subtree operations for his needs. He uses `format-patch` for submitting changes from subtree-merged dependencies rather than relying on `split`. This practical perspective adds useful context about real-world usage patterns.

The email advances the technical discussion by clarifying current limitations and suggesting a configuration-based approach to enable future improvements. It also provides valuable insight into how experienced users work around subtree's limitations in practice. The thread remains focused on solving the core technical challenges while acknowledging the tool's current practical constraints.