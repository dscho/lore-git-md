Thread identity: Test modernization series preparing Git's test suite for `set -e` compatibility, focusing on signal handling tests (SIGPIPE verification).

Core problem: Making Git's test suite reliably work with `set -e` enabled, requiring modifications to test cases that intentionally test failure conditions (like signal handling) to avoid false test failures from the stricter shell mode.

Current status: Patch modifies three existing tests that verify Git dies with SIGPIPE (signal 13) in specific scenarios. Changes are mechanical infrastructure updates rather than functional changes to Git itself.

Key technical details: Affects tests in t/t0005-signals.sh (two tests) and t/t3600-rm.sh (one test). Changes test patterns from `foo; echo $?` to `foo && echo 0 || echo $?` to properly capture exit codes under `set -e` while maintaining the same test coverage for signal handling behavior.

Participant positions: None yet (initial patch in series). This is part of a larger 12-patch effort to modernize test infrastructure for `set -e` compatibility.

Related work: Part of ongoing test suite modernization efforts in the Git project, which include both shell test improvements and adoption of the `clar` unit test framework for C code.