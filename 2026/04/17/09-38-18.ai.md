This is a technical discussion about implementing `--recurse-submodules` support for `git worktree add`, focusing on the object-sharing mechanism between worktrees. Phillip Wood follows up on his previous proposal to use `commondir` files for submodule worktrees, identifying a new concern with his approach.

Phillip notes that storing submodule gitdirs under the worktree's gitdir (with `commondir` pointing to the main submodule repository) would cause `git gc` in the submodule to miss per-worktree refs and index files, potentially leading to premature object pruning. This realization suggests Junio C Hamano's earlier proposal of a single bare submodule repository in `$GIT_DIR/modules/` may be more robust, as it would naturally handle GC operations correctly.

The email advances the thread's technical evaluation by surfacing a previously unconsidered tradeoff between the approaches. Phillip's observation about GC behavior adds weight to Junio's suggestion, though the discussion remains open-ended with no clear resolution yet. The key tension remains between implementation complexity, platform compatibility, and proper integration with Git's existing object lifecycle mechanisms.