This is patch 8/12 in Patrick Steinhardt's series to enable `set -e` (errexit) in Git's test suite for Bash 5+. The patch fixes a specific test case in t0008-ignores.sh where `grep -v` in a subshell would fail silently when no matches are found - a behavior that would become fatal under `set -e`.

The change is straightforward: it adds `|| :` to suppress the non-zero exit code from `grep -v` when it finds no matches, while preserving the existing behavior. This matches the pattern established earlier in the series for handling expected command failures. The diff shows the change is minimal and focused, affecting only two lines in the test helper function.

This patch continues the series' careful approach of making incremental, behavior-preserving changes to prepare the test suite for stricter error handling. The change is clearly motivated and follows the established pattern from previous patches in the series. Given the thorough review process the overall series has undergone, this patch appears ready for inclusion.