This is a response from the Git maintainer addressing a question about submodule SSH configuration during initialization. Junio C Hamano suggests a workflow separation to work around the current limitations.

The core suggestion is to split the submodule initialization process into two steps: first run `git submodule init` to set up the configuration, then manually adjust the submodule URLs in the superproject's config before running `git submodule update`. This approach would allow per-submodule SSH customization by modifying the URLs to include any necessary SSH parameters before the actual clone occurs.

The response acknowledges the current limitation (no native per-submodule SSH command support during initial setup) while providing a practical workaround using existing Git features. This maintains Git's philosophy of keeping SSH configuration separate while still enabling the desired functionality through URL manipulation.

The suggestion implies that while the project may not see this as a critical functionality gap requiring new features, there are established ways to achieve the goal within the current system. The response maintains Git's typical stance of preferring composable tools over specialized configuration options.