The thread is now closed. Junio C Hamano has queued Taylor Blau's nine-patch v3 series fixing multiple bugs in Git's pseudo-merge bitmap implementation. This follows Junio's earlier "What's cooking" signal that he considered the series "so close to the finish line" and was awaiting the final update.

The series addresses a cluster of compounding issues: a binary-search sort order bug, a swapped comparator in `pseudo_merge_at()`, extended-table offset and parsing errors, a silent classification bug in `find_pseudo_merge_group_for_ref()` that made `stableThreshold` and `sampleRate` configuration inert, a division-by-zero crash risk when `sampleRate` is exactly 0, stale documentation showing `sampleRate = 100` instead of `sampleRate = 1`, and a memory leak on duplicate `bitmapPseudoMerge.*.pattern` config keys. The series also adds a `test-tool bitmap write` subcommand and 198 lines of new tests, five of which were initially expected failures and promoted as patches fixed the corresponding bugs. Jeff King (Peff) identified the original lack of test coverage, and Elijah Newren caught the documentation error and test ordering issues in earlier review rounds.

In accepting the series, Junio offered Taylor a minor procedural note on cover-letter etiquette: rebase notes should explain *why* a rebase was done (e.g., to resolve conflicts with other merged topics), not merely state that it happened. This has no bearing on the patches themselves, which are now merged and require no further action from the list.