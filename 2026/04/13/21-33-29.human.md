This is Junio Hamano reporting test failures in Patrick Steinhardt's recently approved `set -e` test suite modernization series. The issue occurs when running git-svn tests without the required Perl modules installed - the test suite now fails immediately due to `set -e` rather than continuing past the skipped tests.

The problem demonstrates a real-world edge case of the new error handling: tests that intentionally skip when dependencies are missing now trigger hard failures. This affects the entire git-svn test suite (over 50 test scripts) when SVN::Core Perl module is unavailable. Junio's environment deliberately lacks these modules, making this a common configuration for developers not working with Subversion.

While the core `set -e` infrastructure was carefully prepared across 12 patches, this interaction with test prerequisites shows there may still be cases needing adjustment. The series remains fundamentally sound, but this fallout suggests follow-up work may be needed to handle skipped dependency checks gracefully under strict error checking.