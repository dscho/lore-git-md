CI / build system update from Johannes Schindelin, bumping the GitHub Actions cache dependency from version 4 to 5. This is a straightforward version update affecting the Coverity workflow configuration, changing two lines in `.github/workflows/coverity.yml` to use the newer cache actions. The patch appears to be a routine maintenance update with no functional changes described - it simply keeps the CI configuration current with the latest upstream GitHub Actions components. The change is mechanically simple and unlikely to be controversial, though as with any dependency update there may be subtle behavioral differences in the caching behavior that would only surface during actual CI runs. The patch originates from Git for Windows rather than mainline Git development.