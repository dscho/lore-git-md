This thread began with Shibo Xia asking how to configure different SSH commands for different submodules during initialization. The initial responses from Junio Hamano and Jeff King (Peff) proposed workarounds involving URL modifications and `includeIf` directives, with Junio maintaining that the existing URL-based approach was sufficient.

The discussion has now progressed to considering deeper architectural changes. Shibo has proposed a new mechanism that would allow passing configuration directly to `git submodule update --init`, similar to how `git clone -c` works but scoped specifically to submodule initialization. This would provide a cleaner solution than the current workarounds, which either expose implementation details or require global configuration changes.

Jeff King, while active in the discussion, has noted his limited experience with submodule internals, so the proposal may need review from submodule experts. The thread remains in a design discussion phase, weighing the tradeoffs between maintaining clean boundaries (as Junio prefers) and providing more direct configuration options (as Shibo advocates). Shibo has positioned this as an RFC, indicating he's seeking broader feedback before pursuing implementation. The core question remains whether this use case justifies changes to Git's submodule initialization model.