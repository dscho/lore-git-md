Here's the updated human-readable thread summary:

This thread began with Shibo Xia asking how to configure different SSH commands for different submodules during initialization. Junio Hamano initially suggested the standard approach of separating `submodule init` and `update` steps while modifying URLs in the superproject config, maintaining Git's traditional boundary of handling SSH through URL manipulation rather than direct configuration.

The discussion has since evolved into examining whether this design should be reconsidered. Jeff King (Peff) has now joined the conversation, offering two alternative approaches while acknowledging their limitations. One uses `includeIf` directives in global configs (though this relies on implementation details), while another proposes adding a `-c` option to `submodule update --init` similar to `git clone`. Peff notes these are exploratory ideas rather than firm recommendations, as he has limited submodule experience.

Currently, there's consensus that the existing URL-based approach works but disagreement about whether it's optimal. Junio maintains the position that current mechanisms are sufficient, while Shibo and now Peff are exploring whether more direct configuration options would be valuable. The thread remains technical and constructive, with participants carefully weighing the tradeoffs between adding new features versus working within Git's existing design boundaries.