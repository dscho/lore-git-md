Here's the updated human-readable summary:

The thread began with Shibo Xia asking how to configure different SSH commands for different submodules during initial `git submodule update --init`. Junio Hamano responded with a practical solution: separate the init and update steps, modify submodule URLs in the superproject's config between them, and let the modified URLs carry any SSH-specific information. This approach works within Git's existing design boundaries that keep SSH configuration separate from core functionality.

In a follow-up, Shibo acknowledged Junio's solution would work but raised a more fundamental question: should Git provide direct per-submodule SSH command configuration rather than relying on URL rewriting or SSH host aliases? This shifts the discussion from a specific workflow question to a broader design consideration about how Git should handle SSH configuration for submodules.

The thread now stands with two perspectives: the maintainer's view that the current URL-based approach is sufficient and follows Git's design principles, and the contributor's suggestion that more direct SSH configuration might be worth considering. While the immediate workflow question has been answered, the conversation has opened a potential longer-term discussion about whether Git's submodule SSH handling could be enhanced.