Patrick Steinhardt's v6 series to make Git's test suite compatible with `set -e` (errexit) is now ready for merging, with this final patch addressing ACL handling case in t1301-shared-repo.sh. The series establishes `set -e` as the default for Bash 5+ while maintaining compatibility with other shells via the `GIT_TEST_USE_SET_E` option, with CI already enabling this for Linux jobs.

The work has involved comprehensive mechanical adaptations across the test suite over six iterations, including converting conditional execution patterns, hardening test helpers, and updating external tool integration tests. The latest patch follows the same pattern seen throughout the series by making the `setfacl -k` call in the shared repository test gracefully handle failures (with `|| :`) when ACLs aren't supported, preserving the test's behavior while complying with strict error checking.

All technical questions have been resolved through review, with Jeff King confirming the approach and SZEDER Gábor providing input on completion tests. The series now catches silent failures across the entire test suite while maintaining original test semantics, representing a significant reliability improvement. With this final ACL-related adjustment, the work is complete and ready for Junio to merge.