Patrick Steinhardt's series to enable `set -e` in Git's test suite was approved by Junio Hamano after thorough review, but has encountered some post-merge issues that highlight the challenges of stricter error handling. The initial problem was with git-svn tests failing hard when Perl modules were missing, rather than skipping gracefully as before. Junio identified and fixed this by adjusting how exit codes are handled in the test infrastructure.

Now a new issue has surfaced in macOS CI runs, where the same changes are causing failures that weren't present before. While the exact nature of the macOS problem isn't specified yet, it appears to be platform-specific behavior triggered by the stricter error checking. This follows a pattern we've seen before - when making fundamental changes to test infrastructure, edge cases often emerge on different platforms that weren't apparent during initial testing.

The core work remains sound, but these follow-up issues show that additional platform-specific adjustments may be needed before the changes can move from the integration 'seen' branch to 'next' and eventually 'master'. Junio is monitoring the situation and will likely request further tweaks to handle macOS's particular behaviors under the new error checking regime.