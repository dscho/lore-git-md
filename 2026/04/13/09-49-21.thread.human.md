Patrick Steinhardt continues refining Git's test infrastructure to enable `set -e` (errexit) when running under Bash 5+, focusing this time on the `stop_git_daemon()` helper function. The latest changes make this helper robust under stricter error checking by explicitly capturing exit codes when waiting for the daemon to terminate and silencing expected failures from redundant kill attempts. This follows the established pattern from earlier patches in the series that modified signal handling tests and core test helpers like `test_must_fail()`.

The series has progressed through technical review with Jeff King (Peff) and shows consensus on the approach, treating this as a reversible experiment while recognizing the benefits of catching silent test failures. CI configurations already test Bash 5+ environments specifically, ensuring the changes work as intended without breaking backward compatibility. With these latest daemon-related adjustments, the series appears to have addressed the remaining edge cases, positioning it as merge-ready infrastructure work that continues Git's broader test modernization efforts.