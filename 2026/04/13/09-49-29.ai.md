Test infrastructure fix preparing for `set -e` in test suite. This patch from Patrick Steinhardt addresses a specific case in t0008-ignores.sh where `grep -v` in a subshell would fail when no matches are found, which would trigger an error under `set -e`. The change adds `|| true` to suppress this expected failure case while maintaining the test's behavior.

The modification is straightforward - it's one of many preparatory changes needed before enabling strict error checking (`set -e`) across Git's test suite. This continues the series' pattern of fixing individual test cases that would break under stricter error handling. The change is mechanical but important for test reliability, and follows the established approach of making tests work correctly with `set -e` before actually enabling it.

This patch is part of Git's ongoing test modernization effort, building on previous work to improve test patterns and robustness. Reviewers should verify the modified test continues to pass as expected, though the change is low-risk since it only affects error handling in a case where failure was already being silently ignored.