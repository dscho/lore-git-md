This is a test infrastructure patch that modifies `test_must_fail()` to work correctly when the test suite runs with `set -e` enabled. It's part of Patrick Steinhardt's series to improve test robustness by preventing silent error ignoring.

The change wraps the command execution in a conditional (`if...then...else`) so that when `set -e` is active, a failing command won't cause the test to abort early. The function still properly captures and checks the exit code, maintaining all existing behavior while becoming compatible with strict error checking. This is a mechanical but important preparation for the series' ultimate goal of enabling `set -e` across the test suite.

The patch follows the established pattern of first fixing individual components before flipping the `set -e` switch. It touches a core test helper function used throughout the suite, so reviewers will want to verify it doesn't introduce false positives in existing tests. The change is straightforward and well-motivated as part of Git's ongoing test modernization efforts.