This is a question about submodule initialization behavior, specifically regarding SSH authentication configuration during the first `git submodule update --init`.

Shibo Xia asks whether there's a Git-native way to specify different `sshCommand` settings per submodule during initial clone/update, noting that current workarounds involve SSH host aliases outside Git's configuration. The core issue is that submodules don't yet have their own `.git/config` during initial setup, making it impossible to configure them individually at that stage.

The email thoughtfully explores the current options (`core.sshCommand`, `GIT_SSH_COMMAND`, and `~/.ssh/config`) and asks whether the project would consider adding per-submodule SSH command configuration. This appears to be a genuine gap in the submodule workflow, though the author is careful to ask whether the community has already settled on SSH configuration as the preferred solution.

The question is well-framed and shows awareness of existing mechanisms, suggesting the author has done their homework before asking. The thread may attract input from submodule experts and those who've worked on Git's SSH integration, particularly around whether this use case warrants new configuration knobs or if the existing external-SSH-config approach is considered sufficient by the project.