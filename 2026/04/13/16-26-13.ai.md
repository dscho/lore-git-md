Maintainer feedback on test suite error handling changes. Junio C Hamano responds to Patrick Steinhardt's patch modifying signal test cases to work with `set -e`, raising concerns about edge cases in the proposed solution.

The discussion focuses on three test cases that verify Git dies with SIGPIPE (signal 13) when its output pipe is closed. While acknowledging the mechanical correctness of changing `foo; echo $?` to `foo || echo $?` patterns, Junio identifies potential issues if the command unexpectedly succeeds (exit code 0) - the new pattern would produce no output rather than the expected 0.

Junio explores alternative approaches that would preserve the 0 output case, including explicitly handling success cases and redirecting file descriptor 3 to avoid contamination. The examples demonstrate the subtle interactions between shell error handling, exit codes, and file descriptor manipulation in test assertions.

This is a maintainer-level technical review questioning whether the proposed solution handles all edge cases correctly, particularly around command success scenarios. The feedback suggests the change might need refinement to properly distinguish between expected failures (signal 13) and unexpected successes (exit 0) while maintaining compatibility with the upcoming `set -e` requirement.

The exchange highlights Git's attention to test reliability and the careful consideration given to even small changes in test infrastructure. Junio's response indicates this patch may need another iteration to address the edge cases he identified before being ready for inclusion.