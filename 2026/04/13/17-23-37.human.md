This is a test infrastructure patch in Patrick Steinhardt's series to make Git's test suite work with `set -e` (errexit) enabled. The patch modifies the cleanup handlers `test_when_finished()` and `test_atexit()` to properly preserve test exit codes when error checking is strict.

Junio Hamano raises a historical question about whether this change interacts correctly with a 2010 FreeBSD-specific workaround (b6b0afdc) that dealt with shell quirks around `$?` propagation. The core of the patch is sound - it simplifies the exit code handling logic while maintaining the same behavior, just in a way that's compatible with `set -e`. The truth tables in the commit message clearly show the equivalent outcomes before and after the change.

This is a small but important piece of the larger `set -e` effort, touching core test infrastructure. Junio's question suggests he's doing due diligence check on potential shell compatibility issues, but the patch itself appears well-justified and follows the series' pattern of careful, incremental preparation.