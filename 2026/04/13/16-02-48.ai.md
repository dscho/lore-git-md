This is a response from the Git maintainer addressing a question about submodule SSH configuration during initialization.

Junio C Hamano suggests separating `submodule init` from `submodule update` as a potential solution to the SSH command configuration issue. The key insight is that running these commands separately would allow modifying the submodule URLs in the superproject's configuration before the actual clone operation occurs. This approach leverages Git's existing configuration mechanisms rather than requiring new per-submodule SSH command support.

The response implies that the project sees this as a configuration sequencing issue solvable with current Git features, rather than a fundamental limitation needing new functionality. Junio's suggestion maintains the boundary between Git's configuration system and SSH tooling, treating the URL (which can embed SSH command information) as the appropriate configuration point.

This moves the discussion toward practical workflow solutions rather than API changes, suggesting the maintainer views SSH command customization during submodule init as achievable through proper use of existing submodule commands and configuration timing. The response doesn't reject the problem's validity but offers a path forward without modifying Git's core behavior.