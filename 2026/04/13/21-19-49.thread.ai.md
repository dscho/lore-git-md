New AI thread summary:

Junio C Hamano proposes exploring a gap in Git's content search capabilities through an RFC. The core problem is tracking string presence (not just changes) across file versions, particularly for "what's cooking" reports. Current tools fall short: `git log -S` only shows transitions (string appearance/disappearance) while `git grep` requires manual revision range specification. This leaves users unable to easily answer "when did this string appear in these file versions?" without guessing ranges or missing transitions.

The thread begins as an RFC from the maintainer, signaling this as a real pain point in Git's revision walking and content search tools (`git log`, `git grep`). The discussion would interest contributors working on these subsystems. No solution is proposed yet - the email frames this as a problem statement to surface the limitation. Key technical context: the use case involves tracking topic names across sequential "what's cooking" report versions where neither delta-based search (`-S`) nor static grep provides the needed historical presence tracking.