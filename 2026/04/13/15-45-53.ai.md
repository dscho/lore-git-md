This is a question about submodule initialization behavior, specifically regarding SSH authentication configuration during the first `git submodule update --init`.

Shibo Xia asks whether there's a Git-native way to specify different `sshCommand` settings for different submodules during their initial clone/update, noting that current workarounds require putting this logic in SSH config files rather than Git configuration. The core issue is that submodules don't have their own local config during initial setup, so repository-level `core.sshCommand` and environment variables like `GIT_SSH_COMMAND` can't be set per-submodule at this stage.

The email identifies three existing SSH configuration mechanisms (repository config, environment variables, and SSH host config) but finds them insufficient for this specific submodule initialization case. Xia is considering whether to propose adding per-submodule SSH command support but first wants to understand if this is recognized as a genuine gap in functionality or if the project considers SSH config files the appropriate place for such customization.

This appears to be an unexplored edge case in submodule initialization - while there's extensive prior discussion about SSH configuration in general, Xia specifically asks whether per-submodule SSH commands during initial setup has been considered before. The question touches on the boundary between Git's configuration system and external tooling (SSH), and whether Git should take more responsibility for authentication configuration during submodule operations.