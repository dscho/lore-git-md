Patrick Steinhardt's 12-patch series to enable `set -e` (errexit) in Git's test suite for Bash 5+ is now merged, having addressed all review feedback. The series systematically prepared the test framework by adapting helper functions and fixing test patterns that would break under strict error checking, catching previously silent issues like test_expect_success typos. Jeff King (Peff) had proposed extending this via a `GIT_TEST_USE_SET_E` environment variable for other shells, but this remains an open follow-up opportunity.

The thread has evolved into exploring alternative approaches to error detection, with Peff responding to Junio Hamano's request for less invasive solutions. Peff investigates two technical approaches to monitor stderr output selectively: an initial "lint" mode proved too slow/incompatible, while a second approach redirects stderr to a file during normal execution while preserving original stderr (via fd7) for intentional output. This catches typos like `test_expect_foobar` while allowing expected stderr through. The prototype reveals integration challenges around test framework diagnostics that intentionally write to stderr, leading Peff to propose a compromise where fatal errors dump accumulated stderr before exiting - though with delayed error reporting.

Key technical considerations in play:
- Preserving verbose/debug terminal output
- Maintaining test framework error message visibility
- Compatibility with existing test patterns
- Handling early exits via `die()`
- Performance impact of stderr redirection
- Timing of error visibility to users

The discussion continues the thread's core tension between strict error checking and usability, now exploring implementation tradeoffs beyond the original `set -e` approach. Peff's stderr monitoring concept offers a potential middle ground that could address Junio's maintainer friction concerns while still catching silent failures. SZEDER Gabor's prior contributions on multi-line input handling remain relevant as the error detection scope expands.