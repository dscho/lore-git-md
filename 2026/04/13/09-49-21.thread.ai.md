Patrick Steinhardt's 12-patch series to enable `set -e` in Git's test suite (Bash 5+ only) continues with part 8, systematically preparing the test infrastructure for strict error checking. The series maintains its incremental, backward-compatible approach with Jeff King (Peff) as primary reviewer.

Parts 1-7 covered signal handling (t0005, t3600), `test_must_fail()` behavior, daemon management, config cleanup, conditional test execution, command patterns across 11 test files (t4032, t7450, git-svn, HTTPD helpers, CVS, submodules), and core test infrastructure fixes to `test_when_finished()` and `test_atexit()` in `test-lib-functions.sh`.

Part 8 addresses t0008-ignores.sh, which tests Git's ignore patterns. The patch fixes a `grep -v` in a subshell where non-matches are expected behavior but would trigger an error under `set -e`, adding `|| :` to explicitly ignore these expected failures. This continues the series' pattern of methodically preparing individual test files, with the change touching only two lines in one test file.

The series remains merge-ready with successful CI validation on linux-TEST-vars (Bash 5+) via GitLab/GitHub. This test modernization effort is now 8/12 complete, having addressed signal handling, conditional execution, command patterns, cleanup handlers, and now specific test file adaptations. Remaining work likely involves completing the test suite audit and final `set -e` activation, maintaining the careful attention to detail shown in parts 6-8 where even simple command substitutions need scrutiny for their error-handling implications.