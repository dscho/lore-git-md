Patrick Steinhardt's ongoing effort to enable `set -e` (errexit) in Git's test suite has progressed to v2, refining the approach based on platform-specific shell behavior discoveries. The series now targets Bash 5+ in modern CI environments after identifying inconsistent error propagation in older shells, particularly macOS's Bash 3.2.57 which violates POSIX rules. Key technical changes include adopting a `ret=0; command || ret=$?` pattern for reliable error handling (now extended to the `test_must_fail()` helper function via `exit_code=0; "$@" 2>&7 || exit_code=$?`), fixing FreeBSD 15 issues, improving SVN module detection in prerequisites, and standardizing on `|| :` for no-op commands.

The implementation carefully prepares test infrastructure for `set -e` by addressing edge cases in signal handling, daemon management, and cleanup routines across specific test scripts (t0008, t1301, t6002, t9902), with the latest patch ensuring the core `test_must_fail()` helper remains functional under strict error checking. This version maintains the core error-checking benefits while avoiding brittle behavior on problematic platforms, exemplifying Git's pragmatic approach when universal adoption proves infeasible. The series builds on earlier analysis from Jeff King and incorporates Junio-approved solutions for test signal handling, with comprehensive documentation of shell-specific behaviors and limitations.

Open questions about the narrowed scope's value remain, but the discussion demonstrates Git's iterative design process - adapting to realities while preserving core goals. Platform constraints (especially macOS's shell limitations) have shaped the strategy, with maintainers prioritizing workable solutions over ideological purity. The changes focus on reliability in modern CI systems (now including robust `test_must_fail()` behavior under `set -e`) while maintaining compatibility elsewhere, representing a balanced approach to test suite hardening. The mechanical but critical update to `test_must_fail()` shows the series' attention to foundational test infrastructure that hundreds of tests depend upon.