Patrick Steinhardt's series enabling `set -e` in Git's test suite (restricted to Bash 5+) continues with updates addressing core test helpers. The v3 patch modified signal-handling tests (t0005-signals.sh, t3600-rm.sh) and adjusted `test_must_fail()` to explicitly capture exit codes. The latest patch focuses on `stop_git_daemon()`, making it `set -e`-safe by: 1) explicitly capturing `wait` exit codes rather than letting them trigger termination, and 2) silencing the second `kill` attempt with `|| :` when the process is already stopped.

Technical details remain consistent with the series' approach: mechanical updates to test infrastructure that preserve backward compatibility while enabling stricter error checking under Bash 5+. The changes follow established practices from earlier patches - explicit exit code handling where implicit behavior would break under `set -e`, and careful attention to edge cases in frequently used test helpers. CI validation continues via the linux-TEST-vars job testing Bash 5+ environments, with Patrick confirming successful runs in GitLab/GitHub CI. Jeff King (Peff) remains engaged in review as the series progresses from signal handling through core test infrastructure to daemon helpers.

The series demonstrates consensus on design, treating this as a reversible experiment while acknowledging the reliability benefits of catching silent test failures. Each patch follows the same careful pattern: narrowly scoped changes to specific test components, maintaining existing behavior while preparing for `set -e`. The work represents Git's ongoing test infrastructure modernization, building on previous efforts to improve test reliability through mechanical improvements. With core helpers like `test_must_fail()` and now `stop_git_daemon()` updated, the series appears increasingly merge-ready.