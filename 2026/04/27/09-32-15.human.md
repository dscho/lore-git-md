This is part 7 of Karthik Nayak's ref backend refactoring series, focusing on moving object validation logic to the generic refs layer. The patch is in active discussion with Toon Claes providing detailed implementation feedback.

Karthik responds to two technical points from Toon's review. First, he'll standardize the error variable name to `tx_err` in receive-pack.c to match update-ref.c's convention. More substantively, he explains why the `!new_target` check is necessary before `is_null_oid()` - since `new_oid` is a pointer that could be NULL, the validation must check for that case first. This differs from the reftable backend's implementation where the OID was a direct struct rather than a pointer.

The exchange shows the series moving into fine-grained implementation discussions after establishing the architectural direction. Karthik's explanation clarifies a subtle distinction between pointer vs direct struct handling that wasn't immediately obvious in the diff. This patch continues the pattern of thorough technical review from Toon Claes complementing Patrick Steinhardt's high-level oversight of the series.