This is a refactoring patch (5/9) in Karthik Nayak's series to consolidate ref backend validation logic. The change is purely mechanical - moving the `print_rejected_refs()` function earlier in `builtin/update-ref.c` to avoid needing a forward declaration in a subsequent patch.

The function itself remains unchanged - it still formats and prints error messages for rejected updates when using `git update-ref --batch-updates`. This preparatory move is part of the larger effort to standardize error handling across Git's reference backends.

The patch follows the series' pattern of small, focused changes that incrementally build toward the goal of moving validation logic into the shared refs layer. Since this is a straightforward code movement with no behavioral changes, it's unlikely to generate discussion beyond the existing review of the overall series.