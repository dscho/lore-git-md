This is a feature patch in the final review stage for adding `--reverse=before` behavior to revision walks, now addressing naming concerns from the maintainer. The patch series (currently at v4) implements the ability to reverse commit order before applying limits like `--max-count`, complementing the existing `--reverse=after` behavior.

Mirko Faina responds to Junio C Hamano's feedback about the clarity of the "before/after" terminology. While defending that the current names align with Git's documented option processing order, he acknowledges they may not be immediately intuitive to users. Junio had suggested `--reverse=oldest/newest` as potentially clearer alternatives, but Mirko notes this still requires consulting documentation to understand fully.

The discussion reveals deeper questions about whether this functionality truly belongs with `--reverse` or should instead modify `--max-count` behavior. Mirko references Junio's earlier suggestion that the core issue may be about when limiting occurs rather than reversal order, proposing `--max-count-later` as an alternative approach that better reflects the underlying operation.

This email marks a potential pivot point where the thread moves from implementation details to reconsidering the user interface design. The technical implementation (including performance optimizations and edge case handling) is complete and uncontroversial, but the naming and conceptual placement of the feature remain open questions. The exchange highlights Git's ongoing challenge of balancing technical accuracy with intuitive interfaces, particularly for features that expose internal machinery to end users.