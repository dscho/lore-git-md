This is a feature discussion about the `--reverse` and `--max-count` options in Git's revision walk machinery. The thread has progressed from technical implementation to finalizing user-facing terminology.

Junio Hamano and Johannes Sixt are debating the naming of the new `--reverse=before/after` options, with Junio suggesting `--reverse=oldest/newest` as potentially clearer alternatives. Johannes expresses skepticism that either naming scheme adequately communicates the behavior to users, proposing instead a new `--max-count-oldest` option that would handle the "show oldest commits" use case without modifying `--reverse` at all.

The key insight here is that the core functionality - showing oldest commits first - may be better served by extending `--max-count` rather than overloading `--reverse`. Johannes notes this would keep `--reverse` doing its single current job (reversing output order) while cleanly adding the new capability. The discussion suggests the original approach of modifying `--reverse` may have been solving the problem at the wrong layer.

This exchange represents the final UX polish for a feature that has already seen its technical implementation thoroughly reviewed and stabilized across four patch iterations. The remaining question is choosing the most intuitive command-line interface before merging.