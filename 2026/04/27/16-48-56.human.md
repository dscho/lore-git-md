This is a feature patch in its final review stage, addressing the interaction between `--reverse` and `--max-count` in revision walks. The patch series has gone through four iterations with all technical concerns resolved, including performance optimizations and edge case handling. The current discussion focuses solely on user-facing terminology.

Mirko Faina responds to Junio C Hamano's suggestion that `--reverse=before/after` may be too implementation-focused for users. While defending the current naming as aligned with documented option processing order, Mirko acknowledges the terminology isn't immediately intuitive. Junio had proposed `--reverse=oldest/newest` as more descriptive alternatives, but Mirko notes even this requires consulting documentation. 

The email suggests the root issue may lie with `--max-count` rather than `--reverse`, referencing Junio's earlier idea that limiting should happen later in the walk. Mirko floats `--max-count-later` as a potential alternative approach, recognizing that any solution will require users to reference documentation for clarity. 

This represents the last open question before potential merging - whether to proceed with the current implementation (technically sound but with potentially opaque naming) or reconsider the approach at the `--max-count` level. The thread shows consensus on the functionality but ongoing debate about the most intuitive user interface.