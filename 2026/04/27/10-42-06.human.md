This is a refactoring patch in Karthik Nayak's series to consolidate ref backend validation logic. It moves the `print_rejected_refs()` function earlier in `builtin/update-ref.c` to avoid needing a forward declaration in a subsequent patch. 

The change is purely mechanical - the function is moved up in the file with no modifications to its implementation. This prepares for future patches in the series that will need to use this function earlier in the file. The commit message clearly explains the motivation (avoiding a forward declaration) and the change is straightforward.

This patch is part of a well-reviewed series that has already addressed substantive feedback in prior iterations. The current version appears ready for integration, with only minor documentation fixes remaining across the series.