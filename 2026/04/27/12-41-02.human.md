This is a small refactoring patch in Christian Couder's URL-based promisor remote auto-configuration series. It changes the `allow_globs` parameter in `urlmatch.c` from a `char` to a proper `bool` type, since it's being used as a boolean value. The patch also updates the two call sites to pass `true`/`false` instead of `1`/`0`.

This is a straightforward cleanup that improves type safety and readability. The change is mechanical and low-risk, touching only internal URL matching code. It's part of the larger effort to support URL pattern matching for promisor remote configuration, but this particular patch is purely about code hygiene rather than adding new features.

The patch has been reviewed positively in the thread and appears ready to be merged as part of the series. It follows Git's coding conventions by using the project's boolean type consistently.