Thread summary:

Subject: post-merge hook behavior after conflict resolution

Core question: Why doesn't Git execute the post-merge hook after completing a merge with `git merge --continue` following conflict resolution? Current behavior only triggers post-merge after clean merges without conflicts.

Technical context: The hook subsystem (specifically post-merge) and merge machinery interaction is involved. The question implies a gap between user expectations (hook runs after any merge completion) and current implementation (hook only runs after conflict-free merges).

Open questions: Is this omission intentional design or an oversight? What technical constraints might prevent triggering post-merge after conflict resolution? Are there edge cases or downsides to changing this behavior?

Key detail: The distinction between clean merges (where post-merge runs) and conflict-resolution merges (where it doesn't) appears to be the central point of investigation. The thread may reveal whether merge state tracking or multiple possible trigger points during conflict resolution complicate hook execution.