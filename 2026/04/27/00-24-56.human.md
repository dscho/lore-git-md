This is the fourth version of a feature patch series introducing `--reverse=before` for revision walks, which changes when commit reversal happens relative to limiting operations like `--max-count`. The key change in v4 is aligning the behavior of negative numbers (less than -1) between `--reverse=before` and `--reverse=after` to treat them the same as -1, matching current behavior.

The series consists of two patches:
1. The core implementation adding the `--reverse=before` option
2. A memory optimization that reduces space complexity from O(N) to O(K) when using `--reverse=before` with `--max-count`

The v4 changes are minor but important for consistency - fixing a typo ("ammortized" to "amortized") and ensuring negative number handling matches existing behavior. The core functionality remains unchanged from v3, which had already incorporated all technical feedback including Jeff King's documentation suggestions and Tian Yuchen's performance analysis.

The patch appears ready for maintainer consideration, having progressed through four iterations with all raised issues addressed. It touches key files like `revision.[ch]` for the state machine changes and `t4202-log.sh` for test coverage. The implementation enables use cases like showing the first commits rather than the last when combining `--reverse` with limiting options.