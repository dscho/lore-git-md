This is a technical follow-up in Christian Couder's series implementing URL-based allowlisting for promisor remotes. The exchange focuses on implementation details of patch 13/16, which handles the configuration parsing for `promisor.acceptFromServerUrl`.

Christian responds to Patrick Steinhardt's earlier feedback about the use of static variables in the configuration caching mechanism. He has restructured the code to eliminate the static `accept_urls` and `initialized` variables, instead passing the string list as a parameter to a new `load_accept_from_server_url()` function. The configuration is now loaded into a stack-allocated string list in the caller (`filter_promisor_remote()`), addressing Patrick's concern about unnecessary static storage for this non-performance-critical path.

The change demonstrates Christian's responsiveness to implementation feedback while maintaining the series' core functionality. This continues the pattern of the series evolving through substantive technical discussion, with Patrick providing detailed code-level suggestions and Christian incorporating them where they align with the overall design. The security model and URL validation behavior remain unchanged by this implementation tweak.