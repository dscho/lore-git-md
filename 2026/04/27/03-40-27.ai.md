This is a question about Git hook behavior, specifically regarding why the `post-merge` hook doesn't trigger after resolving conflicts with `git merge --continue`.

The email asks why Git doesn't execute the `post-merge` hook when continuing a merge after conflict resolution, noting this as a potential missing feature. The author requests an explanation of the technical rationale behind this design choice and any potential downsides to changing it.

This touches on Git's merge machinery and hook subsystem. The question implies that `post-merge` currently only runs after a clean merge (no conflicts), not after conflict resolution. The thread may reveal whether this is an intentional design decision or an oversight, and whether there are technical constraints that make triggering the hook post-resolution problematic (e.g., merge state tracking, multiple possible hook trigger points during conflict resolution).