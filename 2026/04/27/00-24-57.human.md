This is the first patch in a two-part series implementing a new `--reverse=before` option for revision walks, allowing commits to be reversed before applying limiting options like `--max-count`. The patch has gone through four iterations with all technical feedback addressed.

The core change introduces a new `enum rev_reverse` in `revision.h` with states for `NO_REVERSE`, `REVERSE_AFTER` (current behavior), and `REVERSE_BEFORE` (new behavior). The implementation modifies `revision.c` to handle the new state machine and adds comprehensive tests in `t4202-log.sh` covering various combinations of reverse modes and limiting options.

Key improvements in v4 include:
- Fixed a typo in the commit message ("ammortized" -> "amortized")
- Clarified negative number handling in the cover letter
- Documentation now explicitly covers the historical edge case where `--reverse --reverse` cancels out
- The implementation maintains backward compatibility while adding the new `--reverse=before` behavior

The patch appears functionally complete with all substantive feedback from previous rounds incorporated. It cleanly implements the requested functionality while preserving existing behavior, and includes thorough test coverage. The only remaining open question is Junio C Hamano's conceptual framing concern from earlier in the thread.