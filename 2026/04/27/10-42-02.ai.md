This is a refactoring patch that removes an unused typedef from the refs subsystem. It's the first patch in Karthik Nayak's v3 series to consolidate ref backend logic.

The change is straightforward - it removes `ref_transaction_commit_fn` from `refs/refs-internal.h` since this function pointer type is no longer referenced anywhere in the codebase. This appears to be a simple cleanup of dead code that was left behind from previous refactoring work.

As part of the larger series standardizing ref backend behavior, this patch helps reduce unnecessary interface cruft. The removal is safe since the type isn't being used, and the series has already gone through multiple rounds of review with no objections to this particular change. The patch follows Git's coding style and includes a clear one-line commit message explaining the change.