This is a documentation patch that improves the `promisor.acceptFromServer` config entry in the promisor-remote documentation. It's the final patch (8/8) in Christian Couder's v2 series adding URL-based auto-configuration for promisor remotes.

The patch addresses several documentation issues:
- Clarifies that `acceptFromServer` doesn't create new remotes (that's handled by the new `acceptFromServerUrl` option
- Restructures the text into bullet points for better readability
- Makes the default option (`none`) more visible
- Fixes a wording error about "advertised by the client" vs server
- Adds cross-reference to the new `acceptFromServerUrl` option
- Maintains all the existing security warnings and case-sensitivity notes

The changes make the documentation more approachable while preserving all technical accuracy. This is a straightforward documentation improvement that completes the series by ensuring users understand both the existing `acceptFromServer` behavior and how it relates to the new auto-configuration feature.

The patch is well-contained, touching only the promisor.adoc file, and follows the established pattern of documentation updates in the series. It has a clear commit message explaining each change and maintains the security-conscious tone of the original text while improving its organization.