This is the fourth version of a feature patch series introducing `--reverse=before` for revision walks, which changes when commit reversal happens relative to limiting operations like `--max-count`. The key change in v4 is aligning the behavior for negative numbers (less than -1) to treat them the same as -1, matching existing behavior.

The series consists of two patches:
1. The core implementation adding `--reverse=before` functionality
2. A memory optimization that reduces space complexity from O(N) to O(K) when using `--reverse=before` with `--max-count`

Notable improvements in v4:
- Fixed a typo in the commit message ("ammortized" -> "amortized")
- Clarified the handling of negative numbers in the cover letter
- The range-diff shows the specific doc changes and code simplifications made since v3

The implementation continues to address all technical feedback from previous rounds, with Jeff King's documentation suggestions fully incorporated and the memory optimization now cleaned up. The patch appears ready for maintainer consideration, having progressed through four iterations with all substantive feedback addressed. Key files remain `revision.[ch]` for the state machine changes and `list-objects.c` for the traversal logic.