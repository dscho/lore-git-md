This is a technical discussion about the interaction between `--reverse` and `--max-count` in Git's revision walking machinery. Chris Torek weighs in with a deep analysis of the underlying priority queue mechanism and how limiting options interact with display options.

The key insight is that `--max-count` was originally implemented with an optimization that breaks when combined with `--reverse`, leading to the current debate about whether to fix this by changing `--max-count` behavior or introducing a new option. Chris suggests two potential solutions: either fix `--max-count` to work correctly with `--reverse` (which would change historical behavior) or add a new `--stop-walk-after`/`--prio-queue-size` option to preserve the old behavior while fixing the core issue.

This email provides important historical context about why the current behavior exists and the tradeoffs involved in changing it. Chris's analysis suggests the problem stems from an early implementation choice in `--max-count` that optimized for the common case at the expense of correctness with `--reverse`. The discussion now centers on whether to correct this at the cost of backward compatibility or introduce new terminology to maintain both behaviors.

The email doesn't directly address the naming debate (`before/after` vs `oldest/newest` vs `--max-count-oldest`) from earlier in the thread, but provides foundational reasoning that could inform that decision. Chris's perspective as a long-time Git contributor lends weight to the argument that the core issue lies in `--max-count`'s implementation rather than `--reverse`'s semantics.