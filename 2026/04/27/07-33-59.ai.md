This is a feature patch discussion about the `--reverse` option's behavior, now focusing on user-facing terminology. Johannes Sixt responds to Junio Hamano's suggestion about renaming the `--reverse=before/after` options to `--reverse=oldest/newest`.

Johannes agrees with Junio that the current names are too implementation-focused, but raises concerns that the proposed alternatives might be equally confusing. He questions what "revert the oldest" would actually mean to users, suggesting the core need might be better served by a new `--max-count-oldest` option instead of modifying `--reverse`. This would keep `--reverse` doing its current single task while addressing the use case of getting oldest commits.

The exchange shows the discussion has moved from technical implementation (which was settled in previous rounds) to final UX polish. Johannes' response indicates the naming question may require more thought, as neither the current nor proposed names seem ideal. His alternative suggestion of `--max-count-oldest` presents a third option that could sidestep the `--reverse` terminology debate entirely.

Key files remain `revision.[ch]` and the documentation, but the focus has shifted entirely to user experience rather than code changes. This appears to be the final discussion point before the feature can be merged.