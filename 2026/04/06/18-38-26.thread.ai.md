Thread identity: Bugfix for parse-options subsystem regarding subcommand typo reporting.

Core problem: When users mistype a subcommand with a single dash (e.g. `git stash -list`), the current error message incorrectly suggests adding two dashes, when it should indicate the subcommand requires no dashes. A related but separate issue exists for dashless subcommand typos (e.g. `git stash lost`), though this hasn't drawn user complaints.

Current status: Single patch addressing the single-dash case in parse-options.c, changing the `check_typos()` function to distinguish between OPTION_SUBCOMMAND (no dashes) and regular options (two dashes). Junio C Hamano reviewed the patch needs commit message standardization (present tense imperative) and a new test case in t0040/test-parse-options.c.

Key technical details: Changes error message generation in parse-options.c to check option type before suggesting corrections. Only 4 lines modified in check_typos() logic. The dashless typo case remains handled separately by parse_nodash_opt() and is not part of this fix.

Open questions: Whether to expand scope to handle dashless subcommand typos, though Junio notes this appears low-priority given lack of user reports. The immediate patch requires test coverage and commit message polish.

Participant positions: Original author submitted uncontroversial fix. Junio confirmed technical approach but requested standard Git commit message formatting and additional test coverage. No other reviewers yet.

Related work: Part of ongoing parse-options subsystem improvements, though not tied to any larger series. The dashless typo case relates to broader subcommand parsing but isn't blocking this fix.