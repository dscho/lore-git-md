This is a documentation fix for the new `git history` command, which is slated for the upcoming release. René Scharfe corrects the short help text for the `--update-refs` option to accurately reflect its valid arguments. 

The patch removes the incorrect "print" option and standardizes the help text between two subcommands (`reword` and `split`) to consistently show the valid choices as "(branches|head)". The change aligns the in-code help with both the documentation and actual usage string behavior.

This is a straightforward, mechanical fix from a trusted contributor. The patch is small (4 lines changed) and clearly motivated - it prevents user confusion by not advertising an invalid option. Since `git history` is new, catching these documentation issues before release is particularly valuable.