This is a bugfix discussion for `git fast-export` output ordering, specifically addressing path deletions that are prefixes of added paths (e.g. deleting 'a' while adding 'a/b'). Elijah Newren weighs in on Jeff King's earlier response, bringing merge machinery expertise to the conversation.

Newren makes two key technical observations: First, that `fast-import` currently uses a last-entry-wins rule for path operations, meaning the order of commands in the stream affects the final result. Second, he notes the `fast-import` documentation explicitly recommends `filedeleteall` come first, suggesting ordering does matter to the importer. These points strengthen the case for handling ordering in `fast-export` rather than `fast-import`.

The discussion continues to center around `builtin/fast-export.c`'s `compare_paths` function, with consensus forming that prioritizing deletions first is the right approach despite some remaining edge cases with renames. Newren agrees with King's earlier assessment that complex rename cases might simply need to be avoided rather than fully supported.

This email moves the thread forward by providing concrete reasons why reordering in `fast-import` could be problematic, reinforcing the exporter-side solution. The architectural question appears to be settling toward handling this in `fast-export`, though no final decision has been made. The next steps would likely involve Raymond E. Pasco updating the WIP patch with tests and potentially refining the sorting logic based on this discussion.