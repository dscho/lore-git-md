This is patch 3/5 in Lorenzo Pegorari's GSoC series on preserving promisor file contents during repacks, addressing a `NEEDSWORK` from commit 5374a290. The patch focuses on integrating the new `copy_promisor_content()` helper (introduced in patch 2/5) with both regular and geometric repacking scenarios.

The key change ensures that when repacking promisor packfiles, the new `.promisor` file preserves the debugging information (timestamps and ref mappings) from the original files, rather than creating an empty file. For geometric repacks, it tracks excluded packs via a `strset` to properly handle partial repacks. The documentation is updated to clarify that `.promisor` files will now contain "arbitrary contents" (the preserved metadata) rather than being empty.

The implementation shows good attention to edge cases - it handles both timestamped and non-timestamped input formats, verifies object presence in the new pack, and properly cleans up resources. The geometric repack integration demonstrates awareness of Taylor Blau's ongoing work in this area. The patch appears well-considered and responsive to prior feedback, making steady progress toward resolving the original issue while maintaining compatibility needed for debugging partial clones.