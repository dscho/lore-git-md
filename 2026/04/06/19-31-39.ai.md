This is a positive confirmation in the ongoing bugfix thread about object database usage in `git diff` outside repositories, now moving toward a concrete solution.

Luca Stefani verifies that Justin Tobler's proposed "hash-only" variant of `index_blob_packfile_transaction()` works as expected for the original use case. The approach, which avoids ODB transactions when only hashing is needed (as in repository-less `git diff`), appears to resolve the segfault while maintaining the streaming behavior Jeff King wanted to preserve. Luca offers to provide a Tested-by tag if Justin extracts this fix from his larger ODB transaction series.

The thread has progressed from problem identification to solution validation, with Luca's confirmation adding weight to Justin's technical approach. The remaining step is for him to formally extract and submit the standalone fix, which would address both the immediate segfault and the broader architectural concern about inappropriate ODB usage. This fix intersects with Justin's ongoing work on ODB transactions and the `the_repository` removal effort, though it can likely land independently as a targeted improvement.