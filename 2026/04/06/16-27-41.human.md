This is a bugfix for the `git backfill` command's sparse-checkout integration, now receiving final approval from the maintainer. The patch corrects an initialization issue where `ctx->sparse` was incorrectly set to 0 instead of -1, which prevented proper detection of sparse-checkout settings from repository configuration when no explicit flags were given.

Junio Hamano confirms the fix is correct after reviewing the explanation in the commit message, which clearly shows how the original code failed to trigger the config fallback. The change is minimal - just one line updating the initialization value - but important for correct behavior in sparse-checkout scenarios.

Derrick Stolee, the sparse-checkout expert who originally approved the patch, had already given his LGTM (looks good to me) in the previous email. With both the domain expert and maintainer satisfied, this change is clearly ready for inclusion. The thread demonstrates a clean resolution to a subtle initialization bug, with good technical explanation and review from relevant experts.