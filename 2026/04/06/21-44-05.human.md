Bugfix discussion for `git fast-export` path ordering, focusing on architectural implications. Elijah Newren weighs in on whether `fast-import` should handle reordering of path operations, building on Jeff King's historical context and Junio C Hamano's architectural questions.

Newren notes that `fast-import` currently follows a "last-entry-wins" rule for path operations, meaning the order of commands matters semantically. The documentation's recommendation about `filedeleteall` ordering suggests the importer does care about command sequence. This implies the exporter (`fast-export`) should handle the ordering properly rather than relying on the importer to fix it.

The discussion has narrowed to a practical consensus: `fast-export` should sort deletions before modifications to avoid directory/file conflicts, while complex cases like rename cycles should be avoided entirely ("don't do that"). The patch appears headed toward acceptance with this approach, though the exact implementation details remain to be settled. Newren's input reinforces that the ordering logic belongs in the exporter, as the importer's current behavior makes reordering there potentially problematic.