This is a discussion follow-up in the rejected `git switch` detach behavior thread. Kristoffer Haugsbakk weighs in with a nuanced perspective on the `git checkout` vs. `git switch` debate, acknowledging both sides of the argument.

Haugsbakk notes their personal preference for `git checkout` due to its detach behavior, while recognizing that some in the community view it as obsolete. They reference Git's official BreakingChanges document which labels `git checkout` as "superseded" by `git switch`/`git restore` but also notes it won't be deprecated due to widespread use.

The email doesn't propose any technical changes or challenge Junio's rejection directly, but provides context about why users might still prefer `git checkout` despite its superseded status. This serves more as a reflection on the broader transition between commands than as an attempt to reopen the specific rejected proposal about configurable detach behavior.

The discussion is effectively closed after Junio's authoritative rejection, with this email serving as a commentary on the underlying user experience tensions rather than a continuation of the technical debate.