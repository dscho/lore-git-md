Bugfix addressing incorrect cache-tree validation logic when tree objects are missing from the object database. This v2 patch from David Lin incorporates feedback from the previous discussion, restoring a missing negation operator in `cache_tree_fully_valid()` that was accidentally dropped during a refactor in commit 062b914c84.

The fix ensures the function correctly marks cache tree nodes as invalid when their objects are missing, rather than the opposite behavior introduced by the regression. The patch includes a test verifying that `write-tree` properly uses the cache-tree shortcut when valid, now using `test_grep !` per Derrick Stolee's suggestion for better test style.

The commit message has been updated to include the historical context requested by Junio C Hamano, tracing the regression to the specific refactor that introduced it. The change is narrowly scoped to the cache-tree validation logic but has important implications for index-to-tree consistency, particularly in partial clone scenarios where object existence checks matter more.

The thread shows clear technical consensus on both the bug's origin and solution, with Stolee and Junio confirming Lin's analysis. The v2 addresses all outstanding feedback and appears ready for merging, though Junio's earlier question about the practical impact suggests there may be room for follow-up work to better characterize the bug's effects on index-to-tree consistency beyond what the current test verifies.