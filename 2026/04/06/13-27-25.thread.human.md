Here's a human-readable summary of the thread:

Derrick Stolee has completed a three-part series optimizing `git rev-list --maximal-only` by leveraging the faster algorithm from `git merge-base --independent`. The thread began with test coverage proving the commands produce equivalent results, followed by benchmarks showing significant performance differences (0.06s vs 0.03s in git.git, and 0.70s vs instant in the Linux kernel repo).

The final patch implements the optimization by adding a new `prepare_maximal_independent()` function that checks if `--maximal-only` is the only active option. When conditions are right, it replaces the full revision walk with a direct call to the more efficient `reduce_heads()` algorithm while carefully preserving all output formatting capabilities. The change delivers the promised speedups while maintaining all existing behavior, with new tests verifying pretty-printed output still works correctly.

Stolee's thorough approach - first establishing behavioral equivalence, then quantifying the gap, and finally implementing the optimization - demonstrates his characteristic methodical style when working on performance improvements. The series shows particular benefits for large repositories like the Linux kernel where the reference topology makes the speed difference most pronounced.