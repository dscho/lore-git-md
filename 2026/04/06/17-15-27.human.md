Bugfix for `git fast-export` output ordering. Junio C Hamano responds to Raymond E. Pasco's proposed fix for a case where `git fast-export` could produce invalid output streams by emitting path additions before deletions when the deleted path is a prefix of an added path (like deleting 'a' while adding 'a/b').

Junio notes this resembles similar D/F conflict handling in `git checkout` and `git am`, and doesn't object to the approach in principle. He raises two thoughtful questions: whether the `depth_first()` function is the right place for this logic (and whether the name would still fit), and expresses surprise that this issue wasn't caught earlier - wondering if `fast-import` might have some hidden mitigation.

The response suggests Junio sees merit in the fix but wants to understand the broader context better. His questions imply the patch may need refinement before merging, particularly around function placement and naming. The discussion remains technical and constructive, with no immediate objections to the core idea. Raymond will likely need to address these architectural considerations in a v2 patch.