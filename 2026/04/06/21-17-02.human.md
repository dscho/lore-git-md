Bugfix and code quality discussion in Lorenzo Pegorari's promisor repack series. This is a follow-up to patch 2/5 that adds a helper for handling promisor files during repacks.

Junio Hamano responds to Lorenzo's acknowledgment of a memory leak fix (adding `strbuf_list_free(parts)`), but raises a broader concern about the use of `strbuf_split_max()` in the implementation. He notes this is an "overkill" API for the use case and suggests using `string-list` instead, referencing past cleanup efforts where the project systematically removed unnecessary uses of `strbuf_split*()`.

The exchange shows the maintainer's attention to both correctness (the memory leak) and long-term maintainability (API choice). While the immediate issue is resolved, Junio's comment implies the implementation could be further improved by adopting simpler string handling. This doesn't block the current patch but signals an area for future refinement. The thread continues to demonstrate careful review of the promisor repack functionality, now touching on both resource management and API design best practices.