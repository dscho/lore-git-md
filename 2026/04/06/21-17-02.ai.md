This is a maintainer review comment from Junio Hamano on Lorenzo Pegorari's patch series about promisor file handling during repacks. While acknowledging the memory leak fix identified by Tian Yuchen, Junio raises a broader architectural concern about the use of `strbuf_split_max()` in the implementation.

Junio suggests the API is unnecessarily heavyweight for this use case - splitting into a `string-list` would be more appropriate since the code doesn't need simultaneous editable access to all parts. He references two prior cleanup efforts (from 2025 and 2026) where the project systematically eliminated unnecessary uses of strbuf_split*().

This feedback indicates the patch may need more substantial reworking than just fixing the immediate memory leak and syntax error. The maintainer is pushing for alignment with the project's established patterns for string splitting, which could require restructuring how the promisor file lines are parsed. The references to prior cleanup work show this isn't just a stylistic preference but part of an ongoing effort to simplify string handling in the codebase.

The comment doesn't reject the patch outright but signals that the implementation approach should be reconsidered to match the project's evolving best practices. This may delay the patch's progression until the author can rework the line parsing logic. The feedback is constructive but implies deeper changes than the quick fixes discussed earlier in the thread.