This is a review comment on patch 2/5 of Lorenzo Pegorari's promisor file handling series. Tian Yuchen points out two specific issues in the implementation:

1. A memory leak in the `strbuf_split_max()` handling where the allocated `parts` array isn't freed when skipping lines where the OID isn't found in the destination pack.

2. A typo in the line containing `localtime_r()` where there's an errant comma at the end that should be a semicolon.

The reviewer notes the rest of patch 2/5 looks good, with particular attention to the strset-based deduplication logic for writing the final promisor file content. This continues the thread's focus on maintaining accurate promisor pack metadata during repacks, now with specific implementation feedback on resource management and syntax correctness. The issues raised are straightforward to fix and don't appear to indicate deeper architectural problems with the approach.