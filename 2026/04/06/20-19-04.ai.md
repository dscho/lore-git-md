This is a follow-up review comment from Jeff King (Peff) on patch 4/7 of Justin Tobler's ODB write operations series. Peff initially suggested moving the `odb_write_stream` initialization inside the `else` clause since it's only used for hash computation in this patch, but then realizes the stream will be used in both paths in later patches of the series.

The exchange shows the careful attention the series is receiving from experienced contributors. Peff's initial observation was technically correct for this standalone patch, but he quickly recognizes the broader context where the stream interface will be fully utilized across both code branches in subsequent changes. This demonstrates how incremental refactoring sometimes creates temporary asymmetries that resolve cleanly in later steps.

The comment doesn't require any action from the author since Peff acknowledges the current organization makes sense for the series' progression. This kind of thoughtful review feedback is typical of the project's quality control process, where even small implementation details get scrutiny from multiple perspectives. The series appears to be in good shape as it approaches finalization.