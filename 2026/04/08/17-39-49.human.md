Bugfix investigation update regarding `writev()` platform compatibility issues in Git 2.54.0-rc1.

Jeff King follows up on Randall S. Becker's report of `writev()` failures on NonStop systems, analyzing whether the issue could be related to `MAX_IO_SIZE` limitations. He notes that Git's use of `writev()` for pkt-line packets (which are capped at 64k) shouldn't exceed typical `MAX_IO_SIZE` values, suggesting this isn't the root cause of the NonStop failures. This technical clarification helps narrow down the investigation, though the fundamental platform incompatibility remains unresolved.

The exchange confirms this is a regression from recent network optimizations and affects multiple test suites. While Jeff King's proposed `NO_WRITEV=Nope` workaround remains the most promising immediate solution, this email highlights the need for careful consideration of platform constraints when introducing performance optimizations. The thread continues to develop as participants work toward a solution that balances performance gains with broad platform compatibility.