This is the final patch in a refactoring series for xdiff's record cleanup logic. The patch adds braces around else clauses in `xdl_cleanup_records()` for style consistency, completing the series' goal of improving code clarity while maintaining behavior.

The change is purely stylistic - it wraps the else blocks in braces to match Git's coding guidelines, which prefer explicit braces even for single-statement blocks. The patch follows the series' regression-safe approach by making only uncontroversial formatting changes after the earlier v4 regression was addressed by reverting the problematic INVESTIGATE handling changes.

The diff shows the mechanical transformation being applied symmetrically to both file processing loops in the function. This maintains the parallel structure between the two halves of the change while making the control flow more visually apparent. The patch has no functional impact but completes the code hygiene improvements that were the series' original motivation.

With this change, the v5 series appears ready for merging, having preserved all the type safety and clarity improvements from previous versions while avoiding the regression that required rolling back part of v4. The thread history shows this was a carefully managed refactoring that maintained backward compatibility while improving the codebase.