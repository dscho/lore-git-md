This is a platform compatibility discussion about `writev()` system call limitations on NonStop systems, continuing from test failures in Git 2.54.0-rc1. Junio C Hamano responds to Randall S. Becker's report about NonStop's specific constraints, noting that while EINVAL errors from exceeding SSIZE_MAX are expected, the mention of an "OSS I/O size limit" suggests there may be additional platform-specific restrictions beyond standard POSIX behavior.

Junio reiterates the potential workaround of building with `NO_WRITEV=Nope`, which would fall back to using `xwrite()` emulation instead of native `writev()`. He notes this approach should work since basic write operations are known to function correctly on NonStop systems. The exchange highlights the tension between Git's network performance optimizations (using vectorized writes) and the reality of platform constraints that weren't accounted for in the original implementation.

The discussion remains focused on finding a practical solution that maintains compatibility without sacrificing too much performance, with Junio steering toward the simplest workaround while acknowledging the underlying platform differences. This continues the thread that has identified multiple affected test suites and raised broader questions about how Git should handle platform-specific limits in its protocol implementations.