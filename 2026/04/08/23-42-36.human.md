This is a technical discussion about Git's signal handling architecture, specifically around SIGPIPE and child process cleanup. Jeff King (Peff) responds to Junio Hamano's observation that similar signal handler patterns exist in `upload-pack.c` and `fetch-pack.c` that could theoretically exhibit the same race condition recently fixed in `run-command.c`.

Peff agrees the issue is worth addressing but argues that simply reordering signal handler calls in those files would be an incomplete solution. He identifies deeper architectural problems with Git's signal handling model, particularly how cleanup handlers interact with temporary signal ignores (like SIGPIPE). The current push/pop model breaks down when cleanup handlers are installed permanently while other handlers are pushed/popped around them.

The email proposes two potential solutions: 1) A more fundamental rewrite of Git's signal handling to support multiple cleanup handlers and proper ignore semantics, or 2) As a stopgap, registering all cleanup handlers at program startup to avoid ordering issues. Peff acknowledges both approaches are too invasive for the current release cycle.

This continues the thread's progression from a specific bugfix to broader architectural questions about Git's process management. The discussion remains technical and exploratory, with Peff providing detailed analysis but no immediate patches. It suggests the signal handling subsystem may need more comprehensive work beyond the immediate SIGPIPE fixes.