This is a technical review follow-up in Patrick Steinhardt's 16-part series introducing an in-memory object database source (part of the ODB abstraction effort). Justin Tobler confirms the correctness of patch 3's removal of a redundant object existence check in `odb_pretend_object()`, noting that `odb_has_object()` already checks the in-memory cache through its call chain.

Justin raises a forward-looking question about potential future behavior: if temporary objects can be written to the in-memory ODB source, should `odb_has_object()` distinguish between in-memory and "real" (persistent) objects? This suggests he's thinking ahead about how the abstraction might evolve, though the current patch remains a straightforward cleanup.

The exchange shows careful technical alignment between Patrick's implementation and Justin's review, with both focused on the ODB abstraction's long-term design while handling immediate technical debt. The thread context indicates this is part of a well-structured series that's receiving thorough review from domain experts.