This is a follow-up discussion in the bundle-URI bugfix thread, where Justin Tobler responds to Toon Claes' patch addressing misconfigured servers advertising invalid bundle-URI data. The core disagreement centers on whether the fix should be client-side (Toon's approach) or server-side (Justin's preference).

Justin argues that the root issue is servers sending invalid empty bundle-URI values, and proposes a server-side fix that would prevent such malformed configurations from being advertised to clients in the first place. He provides a concrete diff showing how the server could filter out empty bundle URIs during configuration-to-packet conversion. While acknowledging the value of client-side resilience for backward compatibility, he maintains that the primary fix belongs on the server.

The discussion highlights an architectural tension around the bundle-URI feature's optional nature - whether misconfigurations should fail hard (to alert administrators) or degrade gracefully (to preserve clone functionality). Justin leans toward stricter server-side validation while Toon's patch focuses on client-side robustness. The exchange remains unresolved, with Justin's technical counterproposal now on the table for consideration.

Key technical points raised:
1. Server-side validation could prevent empty URIs via simple config filtering
2. The client's error handling could be tightened to distinguish between recoverable vs fatal errors
3. Test coverage demonstrates both missing and empty URI cases, though only empty URIs trigger the original bug

This represents a substantive technical discussion about error handling boundaries in the bundle-URI protocol, with implications for both server administrators and client behavior. The thread awaits either Toon's response to Justin's proposal or further input from other contributors.