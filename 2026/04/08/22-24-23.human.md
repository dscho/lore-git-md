This is a platform compatibility discussion about `writev()` limitations in Git's network operations, continuing from test failures in Git 2.54.0-rc1 on NonStop systems. Junio C Hamano follows up on his earlier skepticism about the `writev()` optimization, now examining the actual implementation details.

Junio notes that Git already uses `writev_in_full()` which handles short writes, but observes there's no equivalent to `xwrite()` for vectorized I/O that could transparently split large writes. He points out the fundamental tension: while `writev()` was introduced to avoid extra allocations and system calls, platforms with small `SSIZE_MAX` limits (like NonStop's 52KB in 32-bit mode) will force multiple writes anyway. The packet-line protocol's 64KB maximum directly conflicts with these platform limits.

The discussion reveals deeper architectural questions about whether the `writev()` optimization is worth maintaining given these constraints. Junio's tone suggests he's leaning toward reverting to simpler `write(2)` calls if the current approach proves too problematic, prioritizing reliability over the optimization's benefits. This would represent a significant change from the current direction, indicating the issue may have broader implications beyond just NonStop compatibility.

The thread remains unresolved, with Junio expressing uncertainty about the best path forward ("So, I dunno."), but the technical analysis suggests this may become a maintainer-level decision about Git's network I/O strategy.