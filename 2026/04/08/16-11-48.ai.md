This is a test patch addressing shell script hygiene in the test suite. Adrian Ratiu adds missing `&&` chains to commands in helper functions within `t/t1800-hook.sh` to ensure proper failure propagation between test commands. The changes are straightforward:

1. The `setup_hooks()` function now properly chains its `test_config` calls with `&&` operators
2. The `setup_hookdir()` function similarly adds `&&` between the `mkdir` and `write_script` commands, and fixes a missing `-p` flag on the `mkdir` that was discovered during this cleanup

The patch is small and focused, addressing an issue reported by SZEDER Gábor. It follows Git's test suite conventions by ensuring commands properly fail when expected. The changes are mechanical but important for test reliability, and the patch includes both a clean run and a reference to the original GitHub branch.