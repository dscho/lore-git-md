This is a platform compatibility discussion about `writev()` limitations on NonStop systems, continuing from Git 2.54.0-rc1 test failures. Randall S. Becker confirms that the `NO_WRITEV=Nope` workaround (which falls back to non-vectorized writes) successfully compiles and executes on NonStop systems, resolving the test failures caused by the platform's `writev()` limitations. He has implemented this workaround in their CI/CD pipeline and asks whether a proper fix can be expected.

The thread has established that NonStop's `writev()` implementation has stricter constraints than standard POSIX - failing with EINVAL when the sum of iov_len values exceeds either SSIZE_MAX (53248 bytes on this platform) or an additional "OSS I/O size limit". While Junio Hamano previously noted that POSIX allows partial writes, the NonStop behavior appears to be more restrictive. The `NO_WRITEV` workaround provides a viable path forward by avoiding `writev()` entirely, though it may impact performance of network operations that previously benefited from vectorized writes.

This confirmation that the workaround functions suggests the immediate release-blocking issue can be addressed, though the underlying tension between Git's network protocol assumptions (which include 64k packet-line maximums) and platform constraints remains unresolved. The discussion may now shift to whether a more permanent solution is needed, such as protocol adjustments to respect platform SSIZE_MAX limits or improved handling of partial writes across all platforms. Randall's question about planning a fix indicates this is seen as more than just a NonStop-specific issue.