This is a platform compatibility discussion continuing the investigation of `writev()` issues in Git 2.54.0-rc1, specifically regarding NonStop system limitations. Randall S. Becker provides additional technical context about the hardware constraints causing the SSIZE_MAX limitation (52KB in 32-bit environments) that conflicts with Git's 64KB packet-line protocol assumptions.

Randall notes the limitation stems from DMA/VMM hardware constraints in NonStop systems, while acknowledging the platform's otherwise excellent inter-process communication performance. He expresses personal preference for Jeff King's proposed runtime fallback approach, though recognizes this may not fully resolve the underlying architectural tension between Git's protocol design and platform constraints.

The email reinforces that this is not just a NonStop-specific issue but highlights a broader design question about how Git should handle platforms with small SSIZE_MAX values. The discussion remains unresolved between maintaining the `writev()` optimization (with its compatibility challenges) versus reverting to simpler `write()` calls (with potential performance costs). Randall's input provides useful context about why the platform has these constraints, though it doesn't immediately suggest a solution path forward.