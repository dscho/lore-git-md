This is a follow-up discussion in the bundle-URI bugfix thread, where Justin Tobler responds to Toon Claes' patch about handling misconfigured servers advertising invalid bundle-URI data. The conversation centers on whether the fix should be client-side (Toon's approach) or server-side (Justin's preference).

Justin argues that the root issue is servers sending invalid empty bundle-URI values, and suggests a server-side fix would be more appropriate - either by preventing the invalid configuration from being sent or adding server-side validation. He provides a concrete diff showing how the server could filter out empty bundle URIs before transmission. While acknowledging the value of making clients resilient, he maintains that ignoring the error doesn't address the underlying server misconfiguration.

The thread reveals a design tension around bundle-URI's optional nature: whether to fail hard on misconfigurations (making problems visible) or degrade gracefully (maintaining functionality). Justin leans toward stricter validation to prevent invalid states, while Toon's patch prioritizes uninterrupted operation. The exchange is constructive, with Justin offering specific technical suggestions while recognizing the validity of client-side hardening for backward compatibility.

This continues the thread's pattern of thoughtful discussion about error handling boundaries in the bundle-URI protocol, with both contributors bringing different but complementary perspectives to the problem. The patch itself remains unmerged while this design question is resolved.