This is a technical review discussion about the first patch in Patrick Steinhardt's 16-part series introducing an in-memory object database (ODB) source as part of the ongoing ODB abstraction effort. Justin Tobler raises several points about the implementation details:

1. He questions the wording around temporary object storage in the commit message, asking whether there are existing cases where objects are written only to cache.

2. He suggests adding a dedicated `odb_source_inmemory_release()` function for cleanup rather than handling it directly in `odb_free()`.

3. He notes that storing a path ("source") for the in-memory backend seems unnecessary since paths are primarily relevant for filesystem-based storage.

4. He confirms that the structural changes (replacing the direct `cached_objects` array with a structured `inmemory_objects` pointer) make sense.

The exchange shows careful technical review of the foundational patch in this series, with Justin identifying potential areas for improvement in both implementation and documentation. The discussion remains focused on technical merits rather than broader design questions, as the overall approach has already been vetted by the maintainer.