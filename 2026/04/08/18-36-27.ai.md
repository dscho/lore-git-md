This is a platform compatibility issue regarding `writev()` system call behavior on NonStop systems, continuing the discussion from earlier in the thread.

Randall S. Becker provides a key technical detail in response to Jeff King's analysis: the NonStop platform's `SSIZE_MAX` is 53248 bytes, which is smaller than Git's packet-line protocol maximum of 64k. This confirms that the `writev()` failures are indeed related to size limitations, contrary to the earlier hypothesis that the issue was solely about descriptor types. The platform's `SSIZE_MAX` being smaller than Git's assumed maximum packet size means the `writev()` calls are violating the platform's constraints.

This new information shifts the technical understanding of the problem - it's not just that NonStop rejects `writev()` on certain descriptors, but that the size of writes being attempted exceeds the platform's defined limits. The discussion now centers on how to reconcile Git's network protocol assumptions with platform-specific constraints, with the `NO_WRITEV=Nope` workaround still under consideration but now with clearer understanding of why it's needed.

The exchange highlights the challenge of maintaining cross-platform compatibility while optimizing performance through system calls like `writev()`, particularly when platform constraints aren't visible in the code's assumptions. This remains a release-blocking issue for 2.54.0 given its impact across multiple test suites.