This is a final administrative follow-up in the ODB transaction bugfix thread, where Junio C Hamano confirms he will incorporate Justin Tobler's suggested test improvements before integrating the fix. The patch addresses a segfault that occurs when hashing large files outside a repository due to incorrect ODB transaction handling in `index_fd()`.

Justin had proposed simplifying the regression test to use `git hash-object` directly rather than `git diff`, and wrapping the working directory change in a subshell - refinements originally suggested by Jeff King. Junio confirms he will update the test accordingly before applying the patch to `next` and `master`. The core fix remains unchanged: separating hashing-only paths from object-writing paths in `object-file.c` to avoid ODB transactions in non-repository contexts.

This concludes the technical discussion, with all parties aligned on the solution. The fix will first land as a targeted patch before being superseded by Justin's larger `jt/odb-transaction-write` topic in the `seen` branch. The test improvements maintain verification of the core issue while following Git's test hygiene best practices.