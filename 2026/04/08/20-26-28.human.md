This is the final patch in a refactoring series for xdiff's record cleanup logic. The patch adds braces around an else clause in `xdl_cleanup_records()` for style consistency, following the project's coding guidelines. 

The change is purely cosmetic and doesn't affect functionality - it's part of Ezekiel Newren's ongoing effort to improve code clarity in the xdiff subsystem. The patch comes after several iterations of the series, where earlier versions had introduced a regression that has now been resolved by reverting the problematic changes while keeping the safe improvements like this one.

This is a straightforward change that maintains the existing behavior while making the code slightly more readable. Since it's part of a series that has already been reviewed and approved (with the regression-causing parts removed), it should be ready for merging. The patch touches only xdiff/xprepare.c with minimal changes.