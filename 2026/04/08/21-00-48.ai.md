This is a technical review response in Patrick Steinhardt's ongoing ODB abstraction series, specifically addressing the first patch that introduces an in-memory object database source. Justin Tobler raises several points about the implementation details:

1. He questions the wording around temporary object storage in the commit message, asking whether there are existing cases where objects are written only to cache (not just read from it).

2. He suggests adding a dedicated `odb_source_inmemory_release()` function to clean up resources rather than handling it inline in `odb_free()`.

3. He notes that storing a path ("source") for the in-memory backend seems unnecessary since paths are primarily relevant for filesystem-based sources, and wonders if this could be removed from the generic ODB source structure.

4. He confirms that the structural change of moving cached objects into the new `odb_source_inmemory` type makes sense.

The exchange shows careful technical review of the foundational patch in this series, with Justin identifying potential areas for improvement in both documentation clarity and implementation details. Patrick will need to address these points in a future revision while also coordinating with Justin's own parallel work on ODB interface changes. The discussion remains focused on technical merits without any disagreement on the overall direction of the change.