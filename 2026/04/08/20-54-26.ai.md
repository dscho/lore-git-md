This is a follow-up discussion about signal handling in Git's process execution, prompted by Junio Hamano after merging Jeff King's SIGPIPE fix for parallel process execution. Junio notes that similar SIGPIPE handling patterns exist in `upload-pack.c` and `fetch-pack.c` where processes are spawned without cleanup marking, and raises a question about whether to proactively apply the same signal handler ordering fix to these other cases.

The core observation is that while the immediate bug was fixed in `run-command.c`, there are analogous code patterns elsewhere that could theoretically exhibit similar issues if future changes mark those processes for cleanup. Junio is weighing whether to preemptively adjust these other cases or leave them as-is since they currently work correctly and aren't marked for cleanup.

This represents a maintainer's post-merge consideration of whether a fix's scope should be expanded to similar code patterns, balancing defensive programming against unnecessary churn. The discussion touches on Git's process management internals, particularly how signal handlers interact with child process cleanup across different subsystems. No immediate action is proposed - Junio is soliciting perspective on whether broader changes would be beneficial or premature.