This is a platform compatibility discussion about `writev()` limitations on NonStop systems, continuing from test failures in Git 2.54.0-rc1. Randall S. Becker confirms that the `NO_WRITEV=Nope` workaround successfully resolves the test failures on their NonStop systems, though they note this is specifically due to the platform's 52K size limit for `writev()` in 32-bit mode (with 868K potentially available in 64-bit).

Randall indicates they can proceed with the workaround for now, but the underlying issue remains - Git's packet-line protocol assumes 64k maximums that exceed NonStop's SSIZE_MAX limit of 53248 bytes. The discussion reveals a deeper tension between performance optimizations (vectorized writes) and platform compatibility, with Junio having previously questioned whether the `writev()` optimization is worth keeping if it requires handling partial writes across platforms.

The immediate release-blocking issue appears resolved via the workaround, but the thread leaves open questions about whether to modify the packet-line protocol, keep the workaround long-term, or potentially revert the `writev()` optimization entirely if similar issues surface on other platforms. Randall's confirmation that the workaround works in their CI/CD pipeline suggests this specific regression can be addressed for 2.54.0 while broader design decisions are deferred.