This is a platform compatibility discussion continuing the investigation of `writev()` limitations in Git's network operations, specifically regarding NonStop platform constraints. Randall S. Becker provides additional technical context about the hardware-level DMA/VMM limitations that cause NonStop's SSIZE_MAX restrictions (52KB for 32-bit, 868KB for 64-bit).

The email reinforces that the `NO_WRITEV=Nope` workaround remains viable for the immediate 2.54.0-rc1 issue, while the broader architectural questions about `writev()` optimization persist. Randall notes the NonStop Message system's exceptional intra-process performance, suggesting the platform's constraints stem from physical chipset limitations rather than software design.

The discussion now appears to be converging toward Jeff King's proposed runtime approach, with Randall expressing personal preference for this solution. The email provides useful context about the hardware origins of NonStop's limitations, which helps inform the ongoing debate about whether to maintain the `writev()` optimization or revert to simpler `write()` calls. This continues the thread's central tension between performance optimizations and platform compatibility, with NonStop's unique architecture serving as a test case for how Git handles systems with non-standard constraints.