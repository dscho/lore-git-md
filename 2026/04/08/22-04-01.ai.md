This is a platform compatibility discussion about `writev()` limitations on NonStop systems, continuing from test failures in Git 2.54.0-rc1. Randall S. Becker confirms that setting `NO_WRITEV=Nope` successfully works around the `writev()` issues on NonStop systems, where the 32-bit environment enforces a strict 52K limit (returning EINVAL for larger writes) while 64-bit may support up to 868K.

Randall reports this workaround is now being used in their CI/CD pipeline for RC1 testing, indicating they can proceed with this temporary solution. This follows Junio's earlier skepticism about modifying callers to handle NonStop's `writev()` limitations and his suggestion to use the compatibility implementation in `compat/writev` instead.

The email highlights the concrete platform constraints (52K limit in 32-bit NonStop) that conflict with Git's packet-line assumptions of 64K maximums. While the immediate issue is addressed via the workaround, the underlying tension between performance optimizations (vectorized writes) and platform compatibility remains unresolved. Randall's confirmation that they can "live with this" for RC1 suggests the release can proceed while leaving the long-term solution (whether to keep `writev()` optimizations or revert to simpler `write(2)` calls) for future discussion.