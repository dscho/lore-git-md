This is a documentation patch follow-up in the `git archive --prefix` thread. Jeff King (Peff) reviews Pushkar Singh's patch that clarifies how `--prefix` handles paths without normalization, confirming the proposed documentation is accurate and well-worded.

The patch adds explicit warnings to the `git-archive` man page that `--prefix` passes paths verbatim (including absolute paths like "/" and parent references like "../"), noting some archive tools may treat these as potentially unsafe. Peff's review confirms the wording matches the technical consensus from earlier discussion, where it was agreed Git should document rather than change this behavior.

This marks the expected conclusion to the thread - a small, focused documentation improvement that makes existing behavior more clear without introducing controversy. The patch appears ready for merging unless other reviewers surface additional feedback. All key participants (Pushkar as author, Peff and brian m. carlson as technical reviewers, Junio as maintainer) have aligned on this resolution.