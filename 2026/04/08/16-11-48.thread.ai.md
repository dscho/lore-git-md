Thread identity: Test suite shell script hygiene patch by Adrian Ratiu, addressing missing `&&` chains in `t/t1800-hook.sh` helper functions.

Core problem: The test script's helper functions (`setup_hooks()` and `setup_hookdir()`) lacked proper `&&` chaining between commands, which could mask test failures by continuing execution after errors. The patch ensures proper failure propagation.

Changes made:
- Added `&&` between `test_config` calls in `setup_hooks()`
- Added `&&` between `mkdir` and `write_script` in `setup_hookdir()`
- Fixed missing `-p` flag on `mkdir` in `setup_hookdir()`

Status: Initial version addressing an issue reported by SZEDER Gábor. The changes are mechanical improvements to test reliability following Git's test suite conventions.

Technical details: Touches only `t/t1800-hook.sh`, modifying helper functions that set up test hooks. No behavioral changes to Git itself - purely test infrastructure improvements.

Related work: Originated from a GitHub branch reference, though the mailing list thread is the canonical discussion location. Follows Git's test suite hygiene standards.