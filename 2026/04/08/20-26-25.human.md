This is the third patch in a refactoring series that improves type safety in xdiff's record cleanup logic. The patch focuses on converting ambiguous `long` types to more precise `ptrdiff_t` in the `xdl_clean_mmatch()` function and related variables in `xdl_cleanup_records()`. 

The changes are straightforward and mechanical, replacing `long` with `ptrdiff_t` for the index and limit variables that operate on array bounds and pointer differences. This makes the code's intent clearer and helps prevent potential integer overflow issues. The patch maintains all existing functionality while making the type usage more explicit.

This continues the series' theme of type safety improvements while avoiding the regression that was introduced in v4. The changes are well-contained to xdiff's internal implementation and don't affect any user-facing behavior. Given the previous discussion and reviews, this appears to be a safe and uncontroversial change that aligns with the project's ongoing code quality efforts.