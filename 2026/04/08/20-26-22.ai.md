This is a refactoring series targeting the xdiff subsystem, specifically focusing on type safety and code clarity in `xdl_cleanup_records()`. The series has gone through multiple iterations and was previously merged to 'next' but then found to cause a regression in diff output behavior.

The v5 series represents a partial rollback of the changes, specifically reverting the problematic INVESTIGATE handling logic from patch 6 while keeping the other improvements. The key changes in this version are:

1. Dropping the commit that attempted to simplify INVESTIGATE handling (which caused the regression)
2. Adding braces around an else clause for style consistency
3. Maintaining all the type safety improvements from previous versions (using `ptrdiff_t` and `uint64_t` where appropriate)

The series touches xdiff/xprepare.c most heavily, with minor changes to xdiffi.c and xutils.c. Technical highlights include:
- Removal of a redundant `recs` pointer variable
- Clearer type usage throughout the record cleanup logic
- More precise limit calculations using type-appropriate constants
- Maintained improvements to code flow readability

This appears to be a conservative approach that preserves the type safety and clarity gains while avoiding the behavioral regression. The changes are purely internal refactoring with no claimed performance impact. Given that the regression was identified and addressed quickly, and that the maintainer had previously approved the type improvements, this series seems ready for final review.

The thread history shows this was a well-intentioned refactoring that uncovered a subtle edge case, demonstrating both the value of careful code review and the project's responsiveness to regressions. The regression itself affected diff output was handled for certain git log operations involving pack handling and trailer processing.