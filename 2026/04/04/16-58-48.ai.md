This is a discussion response in the ongoing thread about making `git switch`'s commit hash behavior configurable to match `git checkout`'s detach behavior. D. Ben Knoble raises concerns about the proposed `checkout.switchDetach` config option, suggesting it might cause issues for script usage where predictable behavior is important.

Knoble notes they considered a similar change but hesitated due to potential script reliability concerns - scripts using `git switch` for its current non-detach behavior would need explicit `-c switch.detach=false` to maintain that. They acknowledge this may be less critical for porcelain commands but suggest `git switch`'s script-friendliness has value.

The response doesn't propose specific changes to the implementation but surfaces an important consideration about the config approach's impact on scripting use cases. This adds a new dimension to the discussion beyond the initial review feedback about documentation and implementation details. The core tension remains between configurability (allowing user preference) and predictability (important for scripting).