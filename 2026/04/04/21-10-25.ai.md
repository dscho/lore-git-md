This is a follow-up in the thread about making `git switch`'s commit hash behavior configurable to match `git checkout`'s default detach behavior. Thibaud CANALE responds to Pablo's review feedback on their RFC patch to add a `checkout.switchDetach` config option.

Key points in this response:
- Defends the need for a separate `switch_detach` flag rather than reusing `force_detach`, explaining they tested this approach and it caused test failures
- Provides concrete examples of the current behavior differences between `git switch` and `git checkout` when dealing with commit hashes
- Acknowledges and addresses various style/formatting nits from the review (documentation wording, test descriptions, comment style)
- Indicates a v2 patch will follow shortly with these fixes

The technical justification for the new flag is notable - Thibaud explains that reusing `force_detach` would break branch switching functionality, as evidenced by test failures. This suggests the behavioral difference between the commands is more nuanced than just a simple flag setting.

The exchange continues to focus on implementation details rather than the high-level question of whether configurability is the right approach versus changing the default behavior. Thibaud appears to be incorporating the feedback while maintaining the core design of a new config option.