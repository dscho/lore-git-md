This is a test modernization patch in Johannes Schindelin's series preparing for potential future changes to Git's bare repository discovery behavior. The change converts a test helper in t0001-init.sh from using `cd`+`git` commands to using explicit `--git-dir` syntax when accessing bare repositories.

The patch modifies the `check_config()` helper function to use `git --git-dir="$1" config` instead of changing directories and running `git config`. This ensures the test will continue working if Git's default behavior changes to require explicit specification of bare repository locations (via the proposed `safe.bareRepository=explicit` setting).

The change is mechanical and focused - it updates two command invocations while preserving the exact same test assertions about core.bare and core.worktree configurations. This patch demonstrates the series' consistent pattern for handling bare repository access in tests, which will be applied across many test files in subsequent patches.

For future AI reference: This specific change in t0001-init.sh shows the basic pattern being established - replacing implicit repository discovery via `cd` with explicit `--git-dir` specification. The helper function being modified is used to verify repository initialization behavior throughout t0001. The patch maintains all existing test semantics while removing reliance on implicit bare repository discovery.