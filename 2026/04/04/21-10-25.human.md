This is a feature proposal to make `git switch`'s behavior configurable to match `git checkout` when given a commit hash. The thread continues discussion from an RFC patch that introduced a `checkout.switchDetach` config option.

Thibaud CANALE addresses review feedback from Pablo, clarifying several points:
- Defends the need for a new `switch_detach` flag rather than reusing `force_detach`, explaining they serve different purposes
- Provides concrete examples of workflow disruptions when `git switch` doesn't detach like `git checkout` does
- Accepts documentation wording suggestions but pushes back on some style nitpicks
- Plans to send a revised version (v2) addressing the feedback while maintaining the core approach

The exchange reveals the author's motivation stems from real user workflows where the current `git switch` behavior forces explicit `--detach` flags where `git checkout` would detach automatically. The discussion remains technical, focused on implementation details rather than the high-level concept, which appears uncontested.

This continues an active design discussion about how to best align `switch` and `checkout` behaviors while considering both interactive use and scripting needs. The next version will likely incorporate the documentation and test improvements while keeping the config-based approach.