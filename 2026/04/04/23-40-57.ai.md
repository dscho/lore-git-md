This is a documentation follow-up to the `git stash` push inference patch series. Mirko Faina points out two issues in Deveshi Dwivedi's v2 patch:

First, the test case leaves 'file' in the staging area after completion, which could affect subsequent tests. They suggest adding a `git reset --hard` at the end to clean up properly, matching the pattern used at the start of the test.

Second, and more substantively, the documentation in `Documentation/git-stash.txt` still describes the old behavior where only `-p` and `--` could trigger push inference. Now that the patch has expanded this to include several other push-specific options (`-m`, `--staged`, etc.), this documentation paragraph needs updating to reflect the current behavior.

The comment is narrowly focused on these two polish items - test hygiene and doc synchronization - suggesting the core functionality is otherwise sound. This level of feedback is what we'd expect at the v2 stage of a well-received patch series, where the architectural decisions are settled and reviewers are helping refine the final details before merging. The original author will need to update both the test cleanup and documentation to address these points in v3.