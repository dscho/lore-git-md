Here is the dense AI thread summary for this new email:

This is an RFC proposing to make `git switch`'s behavior configurable to match `git checkout` when given a commit hash. The thread root introduces a new config option `checkout.switchDetach` that makes `git switch` automatically detach HEAD when given a commit ID, like `git checkout` does by default. The change adds documentation in `Documentation/config/checkout.adoc`, modifies `builtin/checkout.c` to check the config, and adds tests in `t/t2060-switch.sh`. The implementation adds a `switch_detach` flag to `struct checkout_opts` set from config. Tests cover switching between branches and commits with config on/off, including advice messages suggesting the config when users might want detach behavior. The proposal addresses an inconsistency between `git switch` and `git checkout` while maintaining backward compatibility via configuration rather than changing default behavior. Key technical details: affects checkout/switch command behavior, adds new config option, modifies checkout_opts struct, tests verify both config states. Open question: whether configuration is the right approach versus making behavior consistent by default. No reviews or other participant positions yet as this is the thread root.