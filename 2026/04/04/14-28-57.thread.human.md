Here's a human-readable summary of the thread so far:

Thibaud CANALE has proposed making `git switch`'s behavior configurable to match `git checkout` when given a commit hash. Currently, `git switch` requires an explicit `-d` flag to detach HEAD at a commit, while `git checkout` does this automatically. The RFC introduces a new `checkout.switchDetach` config option that would make `git switch` behave like `git checkout` when set.

The initial patch adds the config option, modifies the checkout/switch internals to respect it, and includes tests verifying both behaviors. The implementation adds a `switch_detach` flag to the checkout options struct and updates the documentation.

Pablo has provided the first substantial review, offering detailed feedback on the patch quality and raising some design considerations. He notes issues with the commit message style, documentation clarity, and code formatting. More significantly, he questions whether introducing a new config flag is necessary versus reusing the existing `force_detach` mechanism. The review suggests improvements to the test cases and asks the author to revisit the documentation guidelines.

The core idea of making the behavior configurable remains uncontested so far, but the implementation approach is now under discussion. Thibaud will need to address Pablo's feedback in a revised version, particularly around whether the new config option is the most elegant solution. The thread is currently focused on these implementation details rather than debating the high-level concept.