This thread began with a proposal to make `git switch`'s behavior configurable to match `git checkout` when given a commit hash, via a new `checkout.switchDetach` option. Thibaud CANALE's initial patch and subsequent v2 addressed technical feedback from Pablo and D. Ben Knoble, but Junio C Hamano ultimately rejected the change on design grounds, arguing that `git switch` should remain distinct from `git checkout` as a simpler, branch-focused command.

In a follow-up discussion, Junio further clarified the project's stance on the relationship between these commands. He pushed back against documentation framing `git checkout` as "superseded," emphasizing that all three commands (`checkout`, `switch`, and `restore`) remain equally valid options with no plans to deprecate `checkout`. While acknowledging the newer commands have their place, he characterized the attempt to fully replace `checkout` as misguided, reinforcing his view that users comfortable with `checkout`'s unified model have no pressing need to switch.

The thread is now effectively closed, with Junio's responses serving as the final word. The exchange highlights an ongoing tension between documentation that suggests a migration path and the reality that `git checkout` remains widely used. For users, the practical takeaway is that all three commands remain supported options, with `git switch` intentionally maintaining its simpler branch-switching focus rather than becoming a configurable `checkout` replacement.