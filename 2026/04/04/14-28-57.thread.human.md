Here's a human-readable thread summary:

This thread proposes making `git switch`'s behavior configurable to match `git checkout` when given a commit hash. Currently, `git switch` requires `-d` to detach HEAD at a commit, while `git checkout` does this automatically. The patch introduces a `checkout.switchDetach` config option that, when enabled, makes `git switch` behave like `git checkout` by automatically detaching at commits.

The implementation touches documentation, the checkout builtin, and adds tests verifying both config states. The change is well-contained, adding a flag to `struct checkout_opts` that controls the behavior. The author proposes configuration rather than changing defaults to maintain backward compatibility, though this approach may need discussion about whether consistency by default would be preferable.

The patch is marked RFC, suggesting the author is open to feedback on the design. Key questions for discussion might include whether this subtle inconsistency warrants a config option, if the behavior should just be made consistent by default (perhaps with a transition period), and whether the config name (`checkout.switchDetach`) properly reflects its purpose given it affects `git switch` behavior.