Here's the human-readable thread summary:

This thread discusses a proposal to make `git switch`'s like `git checkout` when given a commit hash, via a new `checkout.switchDetach` config option. Thibaud CANALE initially proposed the change to help users transition from `checkout` to `switch`, with implementation adding a config check and new flag in the switch machinery.

Pablo provided thorough code review feedback on documentation clarity, commit message style, and whether the new flag was necessary. D. Ben Knoble raised concerns about script reliability with config-dependent behavior. In v2, CANALE addressed these points - improving documentation, demonstrating why the new flag is technically necessary, and clarifying the narrow scope only affects commit hash cases matching `checkout`'s behavior.

The latest exchange focuses on the balance between configurability and script stability. CANALE emphasizes this is specifically for users transitioning from `checkout`, with minimal script impact since it matches existing behavior. The core questions remain whether configurability is the right approach versus changing defaults, and how to weigh user convenience against script predictability. The technical implementation appears sound after v2's refinements, leaving the higher-level design decision open for maintainer input.