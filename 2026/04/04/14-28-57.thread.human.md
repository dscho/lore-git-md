Here's the updated human-readable thread summary:

This thread discusses a proposal to make `git switch`'s like `git checkout` when given a commit hash, via a new `checkout.switchDetach` config option. Thibaud CANALE's RFC patch adds documentation, modifies the checkout code to check this config, and includes tests showing the behavior with the option on/off.

Pablo provided thorough review feedback, questioning whether a new `switch_detach` flag was needed versus reusing `force_detach`, and suggested improvements to commit messages, documentation clarity, and test descriptions. D. Ben Knoble raised concerns about how the config approach might affect scripts that rely on the current predictable behavior.

In the latest response, Thibaud explains why reusing `force_detach` caused test failures and defends the need for a separate flag, while addressing Pablo's style and documentation suggestions. The technical discussion has revealed that the behavioral differences between the commands are more nuanced than initially apparent. Thibaud plans to send a v2 patch incorporating this feedback while keeping the config-based design.

The core questions remain open: whether configuration is preferable to changing defaults, and how to balance user preferences with scripting needs. The thread remains focused on implementation details rather than these higher-level design questions.