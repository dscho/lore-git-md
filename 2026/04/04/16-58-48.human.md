This is a discussion thread about the proposed `checkout.switchDetach` config option that would make `git switch` behave like `git checkout` when given a commit hash. D. Ben Knoble raises concerns about the impact on scripting use cases, noting that the config option could make `git switch` less predictable in scripts where the current non-detach behavior is relied upon.

Knoble suggests the option might be better named `switch.detach` to follow the `<command>.<option>` pattern, but his main hesitation is that adding configurability could undermine `git switch`'s reliability in scripts. He acknowledges the argument applies more to plumbing than porcelain, but suggests there's value in having a branch-switching command with predictable behavior for scripting.

The comment doesn't reject the proposal outright but introduces an important consideration about the tradeoffs between configurability and scriptability. This adds a new dimension to the discussion beyond the initial implementation details raised in previous reviews. The thread now has open questions about both the technical approach (config vs flag reuse) and the broader design implications (impact on scripting use cases).