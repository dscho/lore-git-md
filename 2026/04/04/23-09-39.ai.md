Bugfix addressing NULL dereference in `git diff --no-index` when comparing large files outside a Git repository.

Jeff King (Peff) weighs in on the proposed fix for the `git diff --no-index` segfault, offering an alternative approach to the problem. While acknowledging Luca Stefani's solution (which checks `INDEX_WRITE_OBJECT` to avoid the NULL dereference) would work, Peff suggests a potentially more comprehensive fix at the object database transaction layer itself. He traces the issue to commit ce1661f9da ("odb: add transaction interface"), where the transaction system became less forgiving of NULL object databases.

The key insight is that before this change, the code would safely return early when encountering a NULL transaction field, but now attempts to dereference it. Peff proposes making the transaction system handle NULL sources gracefully, which would not only fix this specific case but potentially prevent similar bugs elsewhere in the codebase. He notes this might involve establishing a "noop" transaction state or adding NULL checks at the transaction begin stage.

Peff explicitly tags Justin Tobler (author of the original transaction interface commit) for input, signaling this discussion may need deeper expertise in the object database subsystem. The email maintains a constructive tone while raising architectural considerations beyond the immediate bugfix.

This moves the thread from a straightforward bugfix to a discussion about proper handling of repository-less operations throughout Git's object database layer. The solution path now appears to hinge on whether to:
1. Accept Luca's targeted fix (quick, specific)
2. Implement Peff's more comprehensive transaction system change (broader impact but potentially more correct long-term)

Key technical details include the behavior change in ce1661f9da and the transaction system's current handling (or lack thereof) of NULL sources. The thread now awaits input from object-database experts, particularly Justin Tobler, to guide the next steps.