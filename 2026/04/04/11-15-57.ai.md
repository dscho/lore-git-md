Bugfix targeting the backfill command's sparse-checkout integration. This patch corrects an initialization issue where `ctx->sparse` was set to 0 instead of -1, preventing the command from automatically detecting sparse-checkout settings from repository configuration. The fix ensures `git backfill` properly respects `core.sparseCheckout` when no explicit `--sparse` or `--no-sparse` flags are provided.

The change is minimal - a single line edit in `builtin/backfill.c` to initialize `ctx->sparse` to -1 rather than 0. This allows the existing fallback logic (added in commit 85127bcdea) to work as intended, checking the repository config when no command-line preference is given. The patch includes a new test case that verifies the auto-detection behavior by checking both the fetch count and path walk statistics when running backfill in a sparse-checkout clone.

The author (Trieu Huynh) appears to be a GSoC participant based on the subject line. The patch is well-formed with a clear commit message explaining both the problem and solution, and includes appropriate test coverage. This looks like a straightforward fix for a clear oversight in the original implementation. The v2 revision only improves the commit message without changing the actual code change, suggesting the technical content was sound from the first submission.