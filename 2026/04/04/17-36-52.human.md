Bugfix for `git backfill`'s sparse-checkout integration. Derrick Stolee (a sparse-checkout expert) has reviewed and approved Trieu Huynh's patch that fixes an initialization issue where the command wasn't properly detecting sparse-checkout settings from repository configuration.

The patch itself is already well-formed - it changes a single initialization value from 0 to -1 in `builtin/backfill.c` to allow proper fallback to repository config when no explicit `--sparse` flag is given. Stolee's response is purely about process, noting the patch looks good but suggesting future submissions should use `--in-reply-to` to keep the thread together. He mentions GitGitGadget as an alternative tool that handles this automatically.

This appears to be the final version of the patch, with Stolee's approval indicating it's ready for merging. The technical discussion is settled, and the remaining comments are about submission workflow rather than the patch content. The fix is small but important for correct sparse-checkout behavior in the relatively new `git backfill` command.