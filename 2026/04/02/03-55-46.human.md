This is a follow-up discussion in Jeff King's C23 const-correctness patch series, specifically about patch 9/12 which makes `packet_reader.line` non-const. The patch itself has already been technically approved by Junio Hamano, but this exchange surfaces a broader conceptual question about how Git uses `const` in its codebase.

Jeff acknowledges Junio's observation that `const` is sometimes used in Git as a weak signal about memory ownership (indicating "not ours to free") rather than strictly for type safety. He agrees this is more of a hint than a hard rule, noting it appears most consistently in function return values. The discussion doesn't propose immediate changes but suggests this series might prompt future reconsideration of `const` usage patterns.

The exchange shows how a technical compliance fix (adapting to C23's stricter `strchr()` const handling) can reveal deeper design questions worth revisiting. While the patch itself is straightforward, the maintainer's response indicates it may have implications for how the project signals memory ownership going forward.