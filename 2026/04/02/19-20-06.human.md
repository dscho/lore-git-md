This is a performance optimization in Git's xdiff implementation, part of Phillip Wood's series to reduce memory usage in the Myers diff algorithm. Junio C Hamano reviews the second patch, which simplifies the `xdl_clean_mmatch()` function now that earlier changes have made some parameters redundant.

The patch removes an unused parameter (`s`) and changes another (`e`) to accept a length rather than an end index, making the calling code cleaner. Junio confirms the changes are a logical follow-on to the previous patch and makes sense technically. The modifications are small and focused, touching only `xdiff/xprepare.c` with no behavioral changes.

This continues the series' pattern of mechanical improvements to xdiff's memory handling. The changes are straightforward and well-motivated, with Junio's review indicating they're ready for integration. The patch demonstrates Phillip's careful, incremental approach to optimizing this critical diff component.