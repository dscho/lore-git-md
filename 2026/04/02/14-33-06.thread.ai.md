This is a test modernization series from Johannes Schindelin preparing for potential future changes to Git's default bare repository discovery behavior. The 17-patch series updates test scripts to explicitly handle bare repositories rather than relying on implicit discovery, which may become disabled by default in Git 3.0.

The changes are mechanical but widespread, touching 16 test files and git-p4.py across various subsystems including worktree handling, fetch/push, submodules, and the git-p4 bridge. Each commit focuses on a specific test file, using consistent techniques: replacing `git -C` with `--git-dir` for bare repos, exporting GIT_DIR explicitly, or wrapping commands in subshells with GIT_DIR set. Tests that genuinely need implicit discovery are explicitly configured with `safe.bareRepository=all`.

The series is part of a larger pull request (#2072) and represents proactive work to update tests before a backward-incompatible change in Git's behavior. The changes are straightforward and unlikely to be controversial, following Johannes Schindelin's typical focus on Windows compatibility and test infrastructure improvements. No substantive technical objections or alternative approaches have been raised in the thread so far. The series appears well-structured with each commit being self-contained and clearly motivated.