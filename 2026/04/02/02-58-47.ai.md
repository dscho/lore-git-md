Test patch adding coverage for `includeIf "worktree"` conditional configs.

This is the third and final patch in a series (v2) that adds test coverage for the `includeIf "worktree"` conditional configuration feature. The tests verify several worktree-specific behaviors that weren't covered by existing gitdir tests:

1. Confirms worktree conditions don't match in bare repositories
2. Validates correct path matching across multiple worktrees (main and linked) sharing a config file
3. Tests directory-prefix matching with trailing slashes
4. Verifies path resolution handles symlinks before matching

The tests are thorough and well-structured, exercising both positive and negative cases. Each scenario is clearly documented in the commit message and implemented with appropriate test assertions. The patch follows Git's test style conventions and includes the necessary setup/cleanup.

Key files touched:
- t/t1305-config-include.sh (66 new lines)

This completes the test coverage for worktree-specific conditional config includes, complementing the gitdir tests that were in place. The tests will help prevent regressions in this config inclusion feature.