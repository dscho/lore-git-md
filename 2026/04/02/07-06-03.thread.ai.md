Christian Couder's 10-patch series refactors promisor-remote handling to enable future auto-configuration via URL allowlist. The first seven patches restructured remote lookup logic (introducing `try_promisor_remotes()`), optimized configuration matching in `all_fields_match()` with improved warnings, hardened security by rejecting empty names/URLs in advertised promisor remotes during parsing (`parse_one_advertised_remote()`), simplified control flow in `should_accept_remote()`, extracted control character checking into `has_control_char()`, and isolated config parsing into `accept_from_server()`. Patch 8 continues this pattern by improving memory management in `filter_promisor_remote()`, replacing three parallel data structures (server names, filter strings, and `promisor_info` structs) with a unified `accepted_remotes` string list that preserves the structs until processing completes. This 17-line addition/25-line deletion change in `promisor-remote.c` maintains identical behavior while simplifying the code and preparing for an upcoming `local_name` field addition. Core promisor-remote functionality continues being optimized without new user-facing behavior - current changes focus on internal structure (configuration matching, input validation, memory management) with subsequent patches (9-10) expected to continue refactoring data structures and control flow. Key technical details: modified functions now include `promisor_remote_get_direct()`, `all_fields_match()`, `match_field_against_config()`, `parse_one_advertised_remote()`, `should_accept_remote()`, `valid_token()` with `has_control_char()`, `filter_promisor_remote()` with `accept_from_server()`, and now the memory management consolidation; warning messages remain explicit about ignored remotes, with validation continuing to catch empty URLs and control characters. The series maintains its methodical, behavior-preserving approach to restructuring the promisor-remote subsystem, with each patch building on prior improvements while keeping the `the_repository` removal effort's parameter conventions.