Christian Couder's 10-patch series refactors promisor-remote handling to enable future auto-configuration via URL allowlist, systematically restructuring remote lookup logic. The series includes structural improvements, configuration matching optimizations, and security hardening, with key changes to `all_fields_match()`, `parse_one_advertised_remote()`, `filter_promisor_remote()`, `has_control_char()`, and `accept_from_server()`. The latest patch further simplifies memory management by eliminating the redundant 'accepted' strvec in favor of using the existing 'accepted_remotes' string list throughout the promisor-remote filtering process.

This change completes the consolidation of three parallel data structures (server names strvec, filters string list, and promisor_info structs) into a single `accepted_remotes` string list that retains full `promisor_info` structs throughout processing. The patch touches `filter_promisor_remote()` and `promisor_remote_reply()`, removing 15 lines while adding 12, with logic now flowing through a single consolidated processing loop. The reply generation is updated to iterate the unified list rather than the now-removed strvec.

This refactoring prepares for future addition of a 'local_name' member to `struct promisor_info` while maintaining all existing behavior. The series continues its pattern of preparatory cleanups before introducing new features, having systematically addressed: refactoring `should_accept_remote()` for URL-based acceptance; introducing priority ordering via `try_promisor_remotes()`; security hardening by rejecting empty names/URLs in `parse_one_advertised_remote()`; decoupling config parsing via `accept_from_server()`; and now completing the memory management simplification.

Patrick Steinhardt has positively acknowledged the security hardening and platform compatibility work. The patches are well-structured, touching `promisor-remote.c` and test infrastructure, with all CI tests passing. This foundational work establishes the behavioral framework for subsequent promisor-remote enhancements while maintaining backward compatibility. Windows URI formatting per RFC 8089 remains addressed, with all substantive technical concerns resolved across iterations. The series follows a consistent methodology of extracting reusable components (`has_control_char()`, `accept_from_server()`, now consolidated memory management) to simplify future feature additions.