This is patch 8/10 in Christian Couder's series refactoring promisor-remote handling. The patch focuses on memory management improvements in the promisor-remote subsystem.

The change modifies `filter_promisor_remote()` to keep accepted `promisor_info` structs alive in a new `accepted_remotes` string list rather than immediately freeing them. This eliminates the need for three parallel data structures (server names, filter strings, and the original structs) by maintaining a single consolidated list. The patch simplifies the post-processing logic that applies advertised filters and marks remotes as accepted, while also preparing for future work that will add a `local_name` member to `promisor_info`.

Key technical details:
- Replaces separate `accepted` strvec and `accepted_filters` string list with unified `accepted_remotes` list
- Preserves `promisor_info` structs until after all processing is complete
- Maintains identical behavior while reducing memory management complexity
- Prepares for upcoming addition of `local_name` field without requiring further parallel data structures
- Touches only `promisor-remote.c` with 17 lines added and 25 removed

The change continues the series' pattern of careful, behavior-preserving refactoring to improve the promisor-remote subsystem's internal structure. The consolidation of data structures makes the code more maintainable while keeping all existing functionality intact.