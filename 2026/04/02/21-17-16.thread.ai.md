Thread identity: Feature patch series (v3) from Pablo Sabater improving `git log --graph` rendering of commits with excluded parents. The dead-code issue raised by Jeff King has been cleanly resolved.

Core problem: Current graph rendering vertically aligns commits whose parents are excluded, creating misleading visual relationships. The fix introduces an `is_placeholder` flag in graph columns to force proper indentation of subsequent commits.

Current status: Pablo has confirmed that the `if (seen_this) continue;` guard inside the `graph_output_post_merge_line()` else block is unreachable -- cargo-culted from `graph_output_commit_line()` where `seen_this` can be true for placeholders, but the post-merge-line control flow guarantees it is always false. He will remove it. No deeper logic error; the dead code is simply leftover from copying an incompatible pattern.

Key technical details: Modifies `graph.c`, `graph.h` (new `is_placeholder` flag), and `t4215-log-skewed-merges.sh`. The resolved dead-code issue is purely mechanical -- removing a redundant guard, not fixing a hidden control-flow bug.

Open questions and loose ends: The earlier unresolved issues about diagonal line artifacts and test file reorganization remain open. No new concerns raised.

Participant positions: Jeff King provided a surface-level Coverity finding; Pablo responded promptly and correctly. No substantive technical review of the placeholder mechanism itself has occurred beyond Junio's earlier confirmation that the approach sounds sound.

Related work: None.