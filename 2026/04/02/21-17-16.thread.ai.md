Here is the updated AI thread summary incorporating Kristofer Karlsson's substantive review:

---

Thread identity: Feature patch series (v5) from Pablo Sabater improving `git log --graph` rendering of commits with excluded parents, using cascading indentation of visual roots. The series remains blocked by architectural fragility in `graph_peek_next_visible()`'s direct access to `revs->commits`, now compounded by Jeff King's planned removal of `revs->commits` entirely.

Core problem: The series implements cascading indentation in `git log --graph` to clearly represent commits with excluded parents by progressively indenting visual roots (except the first) while preserving parent-child relationships. The solution must address a fundamental fragility: `graph_peek_next_visible()` directly accesses `revs->commits`, making it vulnerable to changes in revision.c's traversal model (e.g., prio-queue refactoring, `revs->commits` removal).

Current status: v5 consists of two patches - test infrastructure refactoring (1/2, reviewed by Karthik Nayak and Chandra Pratap) and main visualization changes (2/2). The second patch implements cascading indentation but contains a critical flaw: `graph_peek_next_visible()` assumes a linked-list traversal model, which is incompatible with both the existing dual-queue traversal (prio-queue + linked list) and Jeff King's planned removal of `revs->commits`. Kristofer Karlsson (author of the prio-queue refactoring) has clarified that the current prio-queue change does *not* affect `--graph` rendering (since `--graph` uses `REV_WALK_TOPO`/`REV_WALK_LIMITED`, not `REV_WALK_STREAMING`), but the fragility remains. He has proposed a lightweight abstraction in `revision.c` to insulate `graph.c` from the walker's internals.

Key technical details:
- Files touched: graph.c, graph.h, revision.c, t/lib-log-graph.sh, t4218-log-graph-indentation.sh
- New symbols: GRAPH_PRE_ROOT state, is_visual_root flag, visual_root_depth counter
- Test environment requirement: COMMIT_GRAPH file must be removed (not just disabled) to test specific visualization scenarios
- Critical interaction: `graph_peek_next_visible()` vs revision.c's traversal model (linked list vs prio-queue vs future changes)
- Dual-queue constraint: Commits may appear in both `revs->commits` and `revs->commit_queue` during iteration, but `--graph` does not use `REV_WALK_STREAMING` (so the prio-queue refactoring is not currently problematic)
- Proposed abstraction: Two new helper functions in `revision.c` (`revision_has_more_commits()` and `revision_peek_next_commit()`) to dispatch based on walk mode, allowing `graph.c` to remain agnostic to the walker's internal data structures
- Behavior change: Visual roots now appear indented while maintaining correct parent-child relationships across all column positions
- Open technical question: Whether the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) can be set correctly using the proposed abstraction, given the prio-queue's ordering constraints

Open questions and loose ends:
- **Blocker**: Whether the proposed abstraction can correctly set the three key flags without full in-order traversal; Pablo must demonstrate this before the series can proceed
- Test methodology: Junio's concern about exact output matching brittleness remains unresolved
- Documentation of visualization rules (pending since v3)
- Final review from Phillip Wood pending architectural redesign
- Taylor Blau's input pending on commit-graph interaction questions
- Jeff King's planned removal of `revs->commits` (this series must either adopt the proposed abstraction or risk being broken by the removal)
- Whether Kristofer's proposed abstraction is acceptable to maintainers (Junio, Jeff King)
- Input from Kristofer Haugsbakk on whether the abstraction addresses his original concerns about `graph_peek_next_visible()`'s fragility

Participant positions:
- Pablo Sabater: Acknowledged the fragility of `graph_peek_next_visible()` and will likely adopt Kristofer's proposed abstraction. Maintains that the core visualization logic is sound and that the lookahead is an optimization, not a correctness requirement. Must now demonstrate whether the three key flags can be set correctly using the abstraction.
- Kristofer Karlsson: Clarified that the prio-queue refactoring does not currently affect `--graph` rendering, but flagged `graph_peek_next_visible()`'s direct access to `revs->commits` as fragile. Proposed a lightweight abstraction in `revision.c` to insulate `graph.c` from the walker's internals. Suggested that multi-element peeking is unnecessary for setting the three key flags.
- Jeff King (Peff): Identified the critical fragility in `graph_peek_next_visible()` and is planning to remove `revs->commits` entirely. His review remains the most substantive technical feedback on the series, and his planned changes will break the current implementation unless the proposed abstraction is adopted.
- Junio Hamano: Accepted core visualization logic works correctly across all column positions. Remains concerned about test brittleness of exact output matching. Position on the proposed abstraction pending.
- Chandra Pratap: Reviewed and approved the series as sound despite the unresolved lookahead edge case; implicitly endorses architectural redesign and test environment fix.
- Karthik Nayak: Reviewed and approved test infrastructure refactoring in patch 1/2.
- Phillip Wood: Earlier validations may need reconfirmation given the new architectural concerns.
- Taylor Blau: CC'd for input on commit-graph interactions.

Related work:
- Jeff King's in-progress topic removing `revs->commits` entirely, which would break `graph_peek_next_visible()` unless the proposed abstraction is adopted.
- Kristofer Haugsbakk's prio-queue refactoring (commit `dd4bc01c0a`), which does not currently affect `--graph` rendering but highlights the fragility of `graph_peek_next_visible()`'s direct access to `revs->commits`.

The series is **not ready for merging** due to the critical architectural fragility in `graph_peek_next_visible()`. The discussion has advanced to a proposed abstraction in `revision.c` to insulate `graph.c` from the walker's internals. The next step is for Pablo to adopt this abstraction and demonstrate whether the three key flags can be set correctly. The core visualization logic remains sound, but the fragility of `graph_peek_next_visible()` must be addressed before the series can proceed.