Here is the updated AI thread summary incorporating Jeff King's latest substantive review:

---

Thread identity: Feature patch series (v5) from Pablo Sabater improving `git log --graph` rendering of commits with excluded parents, using cascading indentation of visual roots. The series remains blocked by a critical interaction between the lookahead logic in `graph_peek_next_visible()` and the dual-queue traversal model in revision.c (commit `dd4bc01c0a`, now merged to `master`).

Core problem: The series implements cascading indentation in `git log --graph` to clearly represent commits with excluded parents by progressively indenting visual roots (except the first) while preserving parent-child relationships. The solution must address a fundamental incompatibility between the lookahead logic and the dual-queue traversal model, which uses both `revs->commits` (linked list) and `revs->commit_queue` (prio-queue) during iteration.

Current status: v5 consists of two patches - test infrastructure refactoring (1/2, reviewed by Karthik Nayak and Chandra Pratap) and main visualization changes (2/2). The second patch implements cascading indentation but contains a critical flaw: `graph_peek_next_visible()` scans only `revs->commits`, missing commits in `revs->commit_queue`. Pablo has proposed a workaround: since the prio-queue only allows peeking at the first entry, the lookahead must scan both queues to set the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`). Jeff King has validated this approach in principle but clarified that the dual-queue iteration requires inspecting *all* pending commits, not just those in the prio-queue. Junio has clarified that the prio-queue refactoring is now in `master`, so Pablo should base his fix on `master` rather than `next`.

Key technical details:
- Files touched: graph.c, graph.h, revision.c, t/lib-log-graph.sh, t4218-log-graph-indentation.sh
- New symbols: GRAPH_PRE_ROOT state, is_visual_root flag, visual_root_depth counter
- Test environment requirement: COMMIT_GRAPH file must be removed (not just disabled) to test specific visualization scenarios
- Critical interaction: `graph_peek_next_visible()` vs dual-queue traversal in revision.c (introduced in `dd4bc01c0a`, now in `master`)
- Dual-queue constraint: Commits may appear in both `revs->commits` and `revs->commit_queue` during iteration, requiring lookahead to scan both queues
- Documented edge case: Lookahead prediction in revision.c (NEEDSWORK comment added)
- Behavior change: Visual roots now appear indented while maintaining correct parent-child relationships across all column positions
- New constraint: Prio-queues only support peeking at the first entry, making full lookahead scanning impossible in current form
- Proposed workaround: Scan both queues during lookahead, accepting potential redundant indentations as a trade-off for correctness
- Open technical question: Whether the three key flags can be set correctly with the dual-queue iteration, given the prio-queue's ordering constraints

Open questions and loose ends:
- **Blocker**: Whether the proposed workaround (dual-queue lookahead) can correctly set the three key flags without full in-order traversal; Pablo must demonstrate this before the series can proceed
- Test methodology: Junio's concern about exact output matching brittleness remains unresolved
- Documentation of visualization rules (pending since v3)
- Final review from Phillip Wood pending architectural redesign
- Taylor Blau's input pending on commit-graph interaction questions
- The NEEDSWORK lookahead edge case remains unresolved and is now compounded by the dual-queue interaction
- Whether the proposed workaround is acceptable to maintainers and reviewers (now contingent on dual-queue flag correctness)
- Input from Kristofer Haugsbakk (author of `dd4bc01c0a`) on dual-queue constraints

Participant positions:
- Pablo Sabater: Acknowledged the dual-queue interaction blocker, proposed a workaround (dual-queue lookahead), and will base his fix on `master`. Maintains that the core visualization logic is sound and that the lookahead is an optimization, not a correctness requirement. Must now demonstrate whether the three key flags can be set correctly with the dual-queue iteration.
- Jeff King (Peff): Identified the critical dual-queue interaction; validated the proposed workaround in principle but clarified that lookahead must scan both queues to set the three key flags correctly. Suggested refactoring the loop body into a helper function to avoid code duplication. His review remains the most substantive technical feedback on the series.
- Junio Hamano: Accepted core visualization logic works correctly across all column positions, clarified that the prio-queue refactoring is now in `master`, and advised basing future work on `master` (not `next`). Remains concerned about test brittleness of exact output matching.
- Kristofer Haugsbakk: Author of `dd4bc01c0a` (prio-queue refactoring); position pending on dual-queue constraints.
- Chandra Pratap: Reviewed and approved the series as sound despite the unresolved lookahead edge case; implicitly endorses architectural redesign and test environment fix.
- Karthik Nayak: Reviewed and approved test infrastructure refactoring in patch 1/2.
- Phillip Wood: Earlier validations may need reconfirmation given significant design changes in v4/v5 and new dual-queue interaction.
- Taylor Blau: CC'd for input on commit-graph interactions.

Related work: Jeff King's in-progress topic removing `revs->commits` entirely, which would expose the dual-queue interaction issue even without this series. Kristofer Haugsbakk's prio-queue refactoring (commit `dd4bc01c0a`), now in `master`.

The series is **not ready for merging** due to the critical dual-queue interaction. The discussion has advanced to a proposed workaround (dual-queue lookahead), and the procedural question about which branch to base the fix on has been resolved (use `master`). The next step is for Pablo to demonstrate whether the three key flags can be set correctly with the dual-queue iteration. The core visualization logic remains sound, but the dual-queue interaction affects its correctness in certain traversal scenarios.