Here’s the updated human-readable summary for a developer catching up:

This thread is about Pablo Sabater’s patch series (now at v5) improving `git log --graph` rendering for commits with excluded parents. The series uses cascading indentation to make these commits clearer while preserving parent-child relationships. After earlier feedback, the series was split into two patches—one for test infrastructure and one for the main visualization changes—and was generally considered sound by reviewers like Karthik Nayak and Chandra Pratap.

However, Jeff King (Peff) recently identified a critical issue: the series’ lookahead logic in `graph_peek_next_visible()` conflicts with a prio-queue refactoring in `revision.c` (commit `dd4bc01c0a`). The problem is that the lookahead scans `revs->commits`, but the prio-queue now stores commits in `revs->commit_queue`, making the logic miss commits and potentially break graph rendering. This issue is currently masked in tests but would surface if Peff’s in-progress work to remove `revs->commits` entirely lands.

Pablo has acknowledged the blocker and proposed a workaround: since the prio-queue only allows peeking at the first entry, the lookahead could still set key flags by inspecting just the next commit, even if it can’t scan further ahead. This would preserve correctness but might introduce redundant indentations. He’s now focusing on fixing this interaction, likely basing his work on the `next` branch (which includes the prio-queue changes). Junio’s earlier concern about test brittleness remains unresolved, but the prio-queue issue is the immediate priority. The series won’t merge until this is fixed.