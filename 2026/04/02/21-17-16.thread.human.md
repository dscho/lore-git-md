Here’s the human-readable summary for a developer catching up on the thread:

This thread is about Pablo Sabater’s patch series (now at v5) improving `git log --graph` rendering for commits with excluded parents by using cascading indentation. The series hit a critical snag when Jeff King identified an interaction between the lookahead logic in `graph_peek_next_visible()` and the dual-queue traversal model introduced by Kristofer Haugsbakk’s prio-queue refactoring (now in `master`). The core issue was whether the lookahead could correctly scan both queues (`revs->commits` and `revs->commit_queue`) to set key flags for indentation.

Kristofer’s latest review clarifies that the prio-queue change *doesn’t* currently affect `--graph` rendering (it only applies to `REV_WALK_STREAMING` mode), removing one blocker. However, he flags `graph_peek_next_visible()`’s direct access to `revs->commits` as fragile and proposes a cleaner fix: two helper functions in `revision.c` to abstract the walker’s internals. This would let `graph.c` work regardless of whether the walker uses a linked list or prio-queue, and it aligns with Jeff King’s longer-term goal of removing `revs->commits` entirely. Kristofer also notes that multi-element peeking isn’t needed—checking the queue size suffices for the flags.

The series is still blocked, but the path forward is clearer: Pablo will likely adopt the abstraction and revalidate the flag-setting logic. Key participants include Jeff King (who identified the dual-queue issue), Junio (who clarified the prio-queue refactoring is in `master`), and Kristofer (who proposed the abstraction). The core visualization logic remains sound, but the dual-queue interaction must be resolved before merging. Phillip Wood and Taylor Blau’s reviews are still pending.