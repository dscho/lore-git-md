Here’s the human-readable catch-up summary for a developer returning to the thread:

This thread is about Pablo Sabater’s patch series (now at v9) adding cascading indentation to `git log --graph` to clarify which commits have excluded parents. The feature is technically complete—Junio Hamano approved the lookahead buffer architecture, and all known edge cases (like `--author`/`-grep` filtering) are fixed. The series was poised for `next` until Mirko Faina raised a late-stage usability concern: the indentation, while solving a real ambiguity, clutters output for simple cases like sequential one-patch series.

Pablo has acknowledged the clutter but argues the feature should stay opt-out (`--no-graph-indent`) rather than opt-in, since it addresses a genuine problem. He’s proposed two alternatives—a zig-zag pattern or a depth limit—but both have trade-offs. The thread is now at an inflection point: the maintainers must decide whether to merge as-is (accepting the clutter), switch to opt-in (e.g., via a new flag), or add a heuristic to suppress indentation in trivial cases. No further revisions are expected until this is resolved, and the series remains ready for `next` pending a ruling on the usability question.