Here’s the human-readable summary for a developer catching up on this thread:

---

This thread is about Pablo Sabater’s patch series (now at v6) implementing cascading indentation in `git log --graph` to better visualize commits with excluded parents. The series has hit a major architectural fork in the road. Originally, Pablo proposed a peek-based abstraction layer in `revision.c` that lets `graph.c` safely inspect upcoming commits without direct access to the revision queue. This approach was conditionally approved by Junio Hamano and Phillip Wood, but Kristofer Karlsson has now proposed a fundamentally different design: a two-slot lookahead buffer inside `struct git_graph` that ensures commits are fully simplified before inspection.

The core debate centers on safety and robustness. Jeff King identified a latent bug in the peek-based approach—when Git rewrites commit parents during history simplification, the revision queue can contain commits that haven’t been processed yet, making it unsafe for `graph.c` to peek ahead. Kristofer’s lookahead buffer avoids this by having `get_revision_internal()` populate the buffer, guaranteeing that commits are simplified before `graph.c` sees them. His prototype fixes the three failing test cases in `t4218` without needing timestamp tweaks, though a documented edge case (redundant indentation when filtered parents are present) remains.

The series touches `graph.c`, `revision.c`, and test infrastructure, with new symbols like `revision_peek_next_commit()` in v6 and proposed `struct git_graph.lookahead[2]` in Kristofer’s redesign. Pablo’s v6 is technically complete, but Kristofer’s proposal directly addresses the fragility Jeff King highlighted. Junio now faces a choice: accept incremental improvements to the peek-based abstraction or switch to the lookahead buffer, which appears more robust but requires a v7 resend. Either way, follow-up work will be needed before the series can merge. Taylor Blau’s input on commit-graph interactions is still pending, and Junio has noted concerns about test brittleness in exact output matching. The next steps will likely involve Pablo prototyping the lookahead buffer for comparison.