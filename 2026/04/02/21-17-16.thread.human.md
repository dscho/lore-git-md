Here’s the updated human-readable summary for a developer catching up:

Pablo Sabater’s patch series (now at v5) improves `git log --graph` by adding cascading indentation to better visualize commits with excluded parents. The core idea—indenting visual roots while preserving parent-child relationships—has been validated by Junio and others, but the series is blocked by a critical interaction with Kristofer Haugsbakk’s prio-queue refactoring (`dd4bc01c0a`), which moved commits from `revs->commits` into a prio-queue (`revs->commit_queue`). Pablo proposed a workaround: limit lookahead to the next commit, since prio-queues only allow peeking at the first entry. Jeff King (Peff) has now clarified that the traversal machinery actually uses *both* queues during iteration, meaning Pablo’s lookahead must scan both to avoid misordering commits. This doesn’t invalidate the workaround but adds a concrete implementation hurdle.

The series consists of two patches: a test refactoring (reviewed by Karthik Nayak and Chandra Pratap) and the main visualization changes. The blocker remains whether the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) can be set correctly with limited lookahead. Junio has advised basing the fix on `master` (where the prio-queue refactoring now lives), and Phillip Wood’s earlier review may need reconfirmation given the new constraints. Taylor Blau is still CC’d for commit-graph input, and Kristofer Haugsbakk (author of the prio-queue refactoring) has been looped in for feedback. The next step is for Pablo to demonstrate whether the workaround can handle the dual-queue iteration correctly. The core visualization logic is sound, but the prio-queue interaction must be resolved before merging.