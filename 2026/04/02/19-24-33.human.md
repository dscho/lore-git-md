Performance optimization in xdiff's diff preparation. This is the third patch in Phillip Wood's series to reduce memory usage in the Myers diff algorithm implementation by shrinking dynamically allocated arrays.

Junio C Hamano reviews the patch, which simplifies error handling in `xdl_cleanup_records()` by combining two separate allocation failure checks into a single conditional. The change makes the code more concise while maintaining identical behavior - if either `action1` or `action2` array allocation fails, the function still returns -1 after cleanup. Junio notes this consolidation makes sense given the existing cleanup pattern in the function.

This is a mechanical refactoring that follows the series' pattern of focused improvements to xdiff's memory handling. The change doesn't affect the core diff algorithm but helps prepare for future optimizations. Junio's review of the earlier patches in the series was thorough, and his quick approval here suggests confidence in the approach. The series appears ready for integration after addressing minor commit message fixes noted in earlier reviews.