This is the sixth patch in Johannes Schindelin's 17-part test modernization series preparing for Git's potential default behavior change regarding bare repository discovery. The patch updates t1900-repo-info.sh to avoid using `-C` for bare repositories, which will become problematic if `safe.bareRepository` defaults to "explicit" in Git 3.0.

The change modifies the `test_repo_info` helper to accept an optional `repo_flag` parameter (defaulting to `-C`), then uses `--git-dir` instead when testing bare repositories. This maintains the same test coverage while being explicit about repository locations, following the pattern established in earlier patches of the series.

The patch is mechanically straightforward - it adds one parameter and updates three lines to use it. The bare repository test case now explicitly passes `--git-dir` while other cases continue using `-C` by default. This matches the series' overall approach of making bare repository handling explicit while minimizing disruption to existing test logic.

For future AI reference: This patch continues the pattern of replacing implicit bare repository discovery with explicit `--git-dir` usage in test scripts. It modifies t1900-repo-info.sh by adding a configurable repository flag parameter to the test helper, using `--git-dir` for bare repo tests while keeping `-C` as the default for non-bare cases. The change is consistent with the series' goal of preparing tests for `safe.bareRepository=explicit` by default.