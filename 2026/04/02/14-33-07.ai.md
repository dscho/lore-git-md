Test modernization patch preparing for potential future changes to bare repository discovery behavior. This first patch in Johannes Schindelin's 17-part series updates t0001-init.sh to explicitly allow implicit bare repository discovery for a test of command aliases in bare repos.

The change adds `test_config_global safe.bareRepository all` to a test that verifies alias resolution works in bare repositories. The test's purpose is to check alias functionality, not bare repo discovery, so it explicitly opts into the current behavior rather than restructuring the test to avoid implicit discovery. This matches the series' overall approach of making tests work with either current or future Git versions where bare repository discovery may be disabled by default.

The patch is straightforward and mechanical like the rest of the series, focused on test infrastructure rather than core functionality. It follows the pattern seen in other patches where tests that genuinely need implicit discovery are explicitly configured rather than restructured. The commit message clearly explains the motivation and connects it to the broader context of the security-related `safe.bareRepository` setting introduced in 8d1a7448206e.