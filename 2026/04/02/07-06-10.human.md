This is part 7 of Christian Couder's 10-patch series refactoring promisor-remote handling. The series is preparing the subsystem for future auto-configuration features by cleaning up and restructuring the code.

The current patch extracts the logic for checking the `promisor.acceptFromServer` config variable into a new `accept_from_server()` helper function. This simplifies `filter_promisor_remote()` by moving out the config parsing logic while maintaining the same behavior. The change is purely structural - no functionality is altered, just reorganized to make the code more maintainable.

The patch follows the series' pattern of methodical, incremental improvements to the promisor-remote subsystem. It touches only `promisor-remote.c`, focusing on internal code organization rather than introducing new features. The change looks straightforward and well-contained, continuing the series' focus on cleaning up the codebase before adding new capabilities.