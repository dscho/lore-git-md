This is part 6 of Christian Couder's 10-patch series refactoring promisor-remote handling. The series is preparing the codebase for future auto-configuration features while improving security and maintainability.

The current patch extracts control character checking into a new helper function `has_control_char()`, which will be reused in future commits. It replaces inline checks in `valid_token()` with a call to this helper, making the code more readable and consistent. The change is purely internal - no behavior changes for users.

The patch follows the series' pattern of small, focused improvements to the promisor-remote subsystem. It touches the same key file (`promisor-remote.c`) as previous patches, continuing the clean-up work before more substantial changes land. The change looks straightforward and well-contained, with the helper function following Git's typical style for such utilities.