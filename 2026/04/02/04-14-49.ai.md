This is the first patch in Jeff King's 12-part series addressing C23 const-correctness warnings in string function usage. The patch focuses on the convert.c file, specifically the `ident_to_worktree()` function which handles ident attribute expansion.

The change marks two pointer variables (`dollar` and `spc`) as `const` since they don't need to modify the strings they point to. This silences compiler warnings that would occur with C23's stricter const-correctness rules for `strchr()` and related functions, where the compiler now enforces matching constness between input and return values.

The modification is straightforward and low-risk - it simply adds const qualifiers where safe, without changing any behavior. This is characteristic of Jeff King's careful, precise approach to compatibility fixes. The patch builds on prior work from Collin Funk and Michael Forney, as noted in the thread context, and follows the series' pattern of addressing const-correctness warnings subsystem by subsystem.

As the first patch in the series, it sets the pattern for the technical approach used throughout: adding const where safe to maintain type safety while avoiding unnecessary casts or more invasive changes. The change is well-contained to convert.c and doesn't appear to have any broader architectural implications.