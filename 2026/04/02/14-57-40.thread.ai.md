Performance optimization series by Phillip Wood targeting xdiff's Myers diff algorithm implementation to reduce memory usage. The four-part series focuses on shrinking dynamically allocated arrays in `xdiff/xprepare.c` to match only the post-prefix/suffix line counts rather than full file sizes. All changes are mechanical improvements to xdiff's memory handling with no impact on diff results, continuing Phillip Wood's established pattern of xdiff optimizations.

1/4 adjusts action arrays in `xdl_cleanup_records()`: introduces `len1`/`len2` variables for reduced sizes, maintains correct indexing via offset, and updates bounds passed to `xdl_clean_mmatch()`. Junio C Hamano reviewed this with detailed technical validation while noting a minor commit message typo.

2/4 simplifies `xdl_clean_mmatch()` by the first patch's changes: removes unused `s` parameter, changes `e` parameter to accept length rather than end index, and moves end index calculation into the function. Junio approved with "Makes sense".

3/4 consolidates error handling by combining two separate allocation failure checks into a single conditional with logical OR, which Junio confirmed as safe given the shared cleanup path.

4/4 (final patch) moves allocation of `reference_index` arrays to `xdl_cleanup_records()` where the actual needed size is known, shrinking these arrays from full-file size to just the count of processed lines. Junio gave explicit "Looking good" approval, confirming the arrays were already correctly accessed from index 0 and the allocation size is appropriate.

The series demonstrates a clean, focused approach to reducing memory usage in diff preparation, particularly beneficial for large files. Junio's thorough technical reviews across all patches with no objections indicate the approach is sound and ready for integration after trivial message fixes. Maintainer confidence remains high based on consistent positive review responses. No other reviews or objections noted. Builds on prior xdiff cleanup work (en/xdiff-cleanup-3).