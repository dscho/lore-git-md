This is a follow-up in the `git backfill` sparse-checkout bugfix thread, with Derrick Stolee (the sparse backfill feature's original author) weighing in. Stolee confirms that Trieu Huynh's patch correctly implements the intended behavior that his own 2025 commit (85127bcdea) had attempted but failed to fully achieve. The key realization is that initializing `ctx.sparse` to 0 bypassed config reading entirely, while -1 properly defers to `core.sparseCheckout` when no explicit --sparse/--no-sparse flag is given.

Stolee endorses both the code change (flipping the initialization from 0 to -1) and the test case demonstrating correct sparse behavior, noting this matches his original design intent. His only suggested improvement is to reference his earlier incomplete implementation in the commit message. This resolves Junio Hamano's earlier question about whether the behavior constitutes "auto-detection" versus config-respecting defaults - Stolee's confirmation indicates the current approach is correct.

The thread now appears technically resolved, awaiting only Trieu's revision with the suggested commit message update. This is a relatively minor but important fix for sparse repository workflows, ensuring `git backfill` properly respects the repository's sparse-checkout configuration by default. The change affects builtin/backfill.c initialization and is verified by the existing t5620-backfill.sh case.