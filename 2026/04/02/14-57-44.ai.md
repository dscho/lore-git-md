Performance optimization completing Phillip Wood's series to reduce xdiff memory usage. This final patch moves allocation of the `reference_index` arrays in `xdiff/xprepare.c` to `xdl_cleanup_records()`, where the actual needed size (post prefix/suffix removal) is known. The change shrinks these arrays from full-file size to just the count of lines that will be processed by the Myers diff algorithm.

The patch follows the same pattern as earlier in the series: mechanical changes with no impact on diff results, just memory efficiency. It consolidates all array allocations in the cleanup phase where the final sizes are known, completing the transition from pre-allocating based on full file sizes. The implementation is clean - moving the allocation while maintaining the same error handling flow.

This concludes a focused, well-structured series that demonstrates Phillip Wood's characteristic attention to xdiff's memory usage patterns. The changes are small but meaningful for large-file diffs, building on his prior work in this area. With all four patches now submitted, this looks ready for straightforward integration.