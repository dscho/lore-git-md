This is the third patch in Phillip Wood's performance optimization series for xdiff's Myers diff algorithm implementation. The patch focuses on error handling simplification in `xdiff/xprepare.c`.

The change consolidates two separate allocation failure checks into a single conditional, making the code more concise and preparing for future additions. Specifically, it combines the checks for `action1` and `action2` array allocations into one `if` statement with a logical OR. The behavior remains identical - if either allocation fails, the function returns -1 after cleanup - but the implementation is now more compact.

This is a straightforward refactoring that follows the series' pattern of focused, mechanical improvements to xdiff's memory handling. The change touches only the error handling path and doesn't affect the core diff algorithm. As with previous patches in the series, this appears to be a low-risk change that maintains Phillip Wood's consistent approach to xdiff optimizations.

The patch builds on the earlier work in the series (which reduced array sizes) and helps set up for the fourth patch that will further optimize memory usage. The commit message clearly explains the motivation (code simplification to enable future changes) and the technical approach.