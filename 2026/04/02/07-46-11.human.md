This is a security hardening patch in Christian Couder's 10-part series refactoring promisor-remote handling. Patrick Steinhardt provides a brief but positive review of patch, agreeing with the change to explicitly reject empty names or URLs in advertised promisor remotes.

The patch tightens validation in `parse_one_advertised_remote()` to catch empty strings early, rather than relying on later `promisor_config_info_list()` to ignore them. While Git servers shouldn't advertise such remotes, this provides an extra layer of protection against buggy or malicious servers. Patrick's simple "Makes sense" response indicates the change is straightforward and uncontroversial, aligning with the series' goal of cleaning up and hardening the promisor-remote subsystem.

This is a small but sensible improvement that maintains backward compatibility while making the code more robust. The review suggests this patch is ready to proceed as-is in the series.