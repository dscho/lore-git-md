Documentation patch updating the reftable format description. brian m. carlson corrects two inaccuracies in `Documentation/ref-storage-format.adoc`: first, removing the outdated claim that reftable is experimental (it has since become stable), and second, clarifying that while the files format is normally the default, reftable becomes the default when `with-breaking-changes` is enabled (a mode used to prepare for Git 3.0). The patch is straightforward and well-motivated by both documentation accuracy and the 3.0 transition effort.

The changes are minimal but meaningful - replacing static text with conditional blocks that accurately reflect the current and future states. The commit message clearly explains both corrections and their rationale. This appears to be part of a broader effort to audit documentation for Git 3.0 compatibility, though this specific instance stands alone as a simple factual update.