This is the second patch in Phillip Wood's performance optimization series for xdiff, Git's diff implementation. The patch simplifies the `xdl_clean_mmatch()` function by removing a now-unused parameter and changing another to accept a length rather than an end index, making the caller code slightly more straightforward.

The change is a mechanical cleanup following the first patch's optimization. It removes the `s` parameter which is always zero after the previous changes, and replaces the `e` (end index) parameter with `len` (length) for clearer semantics. The diff shows the straightforward nature of the changes - mostly parameter adjustments with no logic changes.

This continues the series' theme of focused, low-risk optimizations to xdiff's memory usage patterns. The patch maintains the same careful approach as the first, with clear commit messages explaining each change. Given Phillip Wood's expertise in this area and the uncontroversial nature of the changes, this seems likely to proceed smoothly through review.