Performance optimization series (part 3/4) continuing Phillip Wood's work to reduce memory usage in xdiff's diff algorithm implementation. This patch simplifies error handling in `xdl_cleanup_records()` by combining two separate allocation checks into a single conditional. The change is purely about code organization - it doesn't affect functionality but makes the code more maintainable by reducing duplication and preparing for future changes (as noted in the commit message).

This follows the same focused, mechanical approach as the previous patches in the series, touching only `xdiff/xprepare.c`. The change is small (5 lines removed, 2 added) and clearly motivated by the commit message's explanation that this simplification will make it easier to add more allocations in the next patch. 

Given Phillip Wood's expertise with xdiff and the uncontroversial nature of this cleanup, this patch seems likely to be accepted as part of the overall optimization series. The change maintains the series' consistent focus on memory efficiency while keeping the code clean and maintainable.