This is a follow-up in the ongoing discussion about enabling `set -e` in Git's test suite, specifically addressing Jeff King's concerns about verifying error detection in CI environments. Patrick Steinhardt confirms that their Fedora-based CI jobs already use Bash 5.3.0, providing some test coverage for the error detection mechanism. He agrees with Peff's suggestion to explicitly configure an Ubuntu-based CI job to use Bash rather than Dash (the default `/bin/sh` on Debian/Ubuntu) to ensure broader verification.

The exchange shows the series moving from technical implementation to operational validation. While the core approach of restricting to Bash 5+ remains unchanged, the focus has shifted to ensuring the safety net works as intended across different testing environments. Patrick's pragmatic response suggests the change is ready to proceed with additional CI verification, maintaining the reversible nature of the experiment if issues emerge. This continues the thread's constructive tension between robust error checking and practical deployment considerations.