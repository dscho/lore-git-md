This is a discussion thread about the `contrib/subtree` recursion limit patches, where a user (Ian Jackson) who originally reported the issue engages with the proposed solution.

Ian Jackson, the reporter of the original recursion limit issue in `git subtree split`, responds to Colin Stagner's patch series. While offering to review the shell code changes, they first question the fundamental approach - suggesting that switching the script to run under bash (with a fallback to POSIX sh) might be a simpler solution than rewriting the recursive algorithm. They also express surprise that `git-subtree` is implemented in shell at all, noting they would have expected a Rust/libgit2 implementation.

The email introduces a new perspective to the stalled review process, proposing an alternative architectural direction rather than focusing on the specific implementation changes in the patches. This shifts the discussion from "how to fix the recursion" to "whether we should be fixing shell recursion at all". The suggestion touches on broader questions about Git's use of shell scripts versus compiled languages, though it doesn't directly address the technical merits of Colin's patches.

Notably, this is the first substantive engagement from an actual `git subtree` user in the thread, which Junio Hamano had been requesting. However, the feedback takes the discussion in a different direction than expected, questioning the implementation language rather than validating the specific changes.