This is the final patch in Patrick Steinhardt's 12-part series to enable `set -e` (errexit) in Git's test suite when running under Bash 5+. The series has systematically prepared the test infrastructure for strict error checking, and this patch completes the effort by actually activating `set -e` in `test-lib.sh`.

The change ensures that any command failures outside of test cases (like the recent undetected `test_expect_success` typo mentioned in the commit message) will cause immediate test suite failure rather than being silently ignored. The implementation is carefully scoped to Bash 5+ only, avoiding known shell compatibility issues while still providing the safety benefits for modern environments.

The patch includes a matching CI configuration update to ensure at least one CI job (`linux-TEST-vars`) runs with Bash 5+ to validate the new behavior. This completes the series' methodical approach where every command substitution was verified for its error-handling behavior before enabling strict mode.

The series has been thoroughly reviewed by Jeff King (Peff) and appears ready for merging, representing a significant improvement in test suite reliability. The changes are infrastructure-focused rather than feature work, with careful attention to backward compatibility and platform support. This continues Git's ongoing effort to modernize and harden its test infrastructure.