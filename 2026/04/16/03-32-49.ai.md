Feature addition to `git cat-file`'s batch mode, now finalized for merging.

This is the cover letter for the fifth and final version of Siddharth Asthana's patch adding dynamic mailmap toggling to `git cat-file --batch-command`. The feature allows changing mailmap behavior mid-operation without restarting the process, addressing GitLab Gitaly's need to reuse long-running processes. The implementation introduces a `mailmap <bool>` subcommand that accepts standard Git boolean values (yes/no, true/false, on/off).

The patch has gone through multiple rounds of review with all feedback now incorporated. The final change in v5 simplifies the documentation to focus on user-visible behavior (toggling mailmap application) while still noting the one-time data loading characteristic, removing implementation details about internal caching that Junio Hamano suggested were unnecessary for users. The technical implementation remains unchanged from v4, with lazy-loaded mailmap data caching and comprehensive test coverage in `t/t4203-mailmap.sh`.

The changes are well-contained to `builtin/cat-file.c` and its documentation/tests, with consensus established that the implementation is ready for merging. The thread shows clear agreement that all reviewers' concerns have been addressed while maintaining the original use case solution for GitLab's needs. The series is now fully complete with only this single documentation-polish patch remaining before merging into Git's mainline.