Thread identity: `git worktree add --recurse-submodules` feature addition for proper submodule initialization in worktrees, originating from a patch series by [author name to be filled from project context].

Core problem/goal: Currently `git worktree add` does not properly initialize submodules in new worktrees, leaving them unpopulated. This patch adds a `--recurse-submodules` flag that correctly initializes submodules while maintaining isolation from the main worktree.

Key technical details:
- New flag `--recurse-submodules` for `git worktree add`
- Submodule working trees properly populated (verified by `git diff --submodule`)
- Submodule gitdirs stored under worktree-specific location (`.git/worktrees/<id>/modules/`) rather than shared location
- Maintains backward compatibility (original behavior remains default)
- Touches worktree.c and submodule.c internals

Test coverage:
- Two new tests in t2405-worktree-submodule.sh
- Verifies both working tree population and gitdir isolation
- Original behavior marked test_expect_failure

Current status: Initial patch proposing the feature, appears carefully implemented with thorough test coverage. No reviews or discussion yet recorded.

Open questions: None raised yet - awaiting initial review feedback on the approach.

Participant positions: Only author participation so far. Key reviewers likely to include Phillip Wood (worktree expert), Jonathan Nieder (submodule expert), and possibly Elijah Newren (merge/checkout behaviors).

Related work: Builds on existing worktree and submodule infrastructure. May interact with ongoing efforts around submodule initialization behaviors and worktree isolation guarantees.