This is part 10/12 of Patrick Steinhardt's series to enable `set -e` in Git's test suite (Bash 5+ only), focusing on test modernization. The patch fixes a subtle issue in t6002-rev-list-bisect.sh where `expr` commands would fail under `set -e` when their result is zero - an expected case in the bisection math.

The change replaces three `expr` calls with `$(( ))` arithmetic expansion, which doesn't fail on zero results. It also modernizes the test formatting by splitting the multi-line `test_expect_success` into a more readable here-doc style. The changes are minimal (10 insertions, 7 deletions) and focused purely on making the math operations `set -e`-safe while preserving the exact same behavior.

This continues the series' pattern of methodical, backward-compatible test infrastructure improvements. The patch shows Patrick's attention to detail in catching edge cases where shell commands' exit codes don't match their semantic success - exactly the kind of silent issues `set -e` aims to surface. With CI already validating the changes on Bash 5+, this appears ready for merging as part of the overall series.