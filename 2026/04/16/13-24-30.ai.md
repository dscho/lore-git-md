This is a preparatory refactoring patch as part of a larger effort to reorganize Git's source tree by introducing a "lib/" directory. The patch makes a minor adjustment to a test helper file to ensure it will continue to compile after the directory structure changes.

The change modifies `t/helper/test-example-tap.c` to use a relative include path (`../unit-tests/test-lib.h`) instead of the current absolute path (`t/unit-tests/test-lib.h`). This is a mechanical change that anticipates the upcoming directory reorganization where the header file's location will change relative to the test helper.

The patch is marked as RFC (Request For Comments) and is part 1 of a 2-patch series, though the cover letter describing the full series goal is not provided here. Patrick Steinhardt is a frequent contributor who often drives large-scale architectural changes in Git, so this likely relates to one of those ongoing efforts. The change itself is straightforward and low-risk, serving as groundwork for more substantial modifications to come.