Here's the updated human-readable thread summary:

Elijah Newren has completed his series optimizing partial clone behavior for `git cherry` and `git grep`. The work addresses a significant performance issue where these commands would fetch blobs one at a time in partial clones, potentially resulting in hundreds of slow individual network operations.

The series follows Git's established pattern for batch prefetching, previously implemented for checkout/diff/merge operations. The second patch added prefetching to `git cherry`, carefully handling edge cases around merge commits and binary files while maintaining the exact same patch ID computation. The just-landed third patch completes the work by implementing similar optimizations for `git grep`, walking trees to identify all needed blobs upfront and fetching them in a single batch.

Both implementations include thorough test coverage and maintain existing behavior while eliminating the performance penalty. The `git grep` changes specifically handle submodules correctly by respecting their separate promisor configuration. With this series complete, partial clone users should see much better performance from these commands, continuing Git's ongoing improvements to make partial clones more practical for everyday use.