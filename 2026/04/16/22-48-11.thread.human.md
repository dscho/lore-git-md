This thread documents the successful optimization of partial clone behavior in `git cherry` and `git grep`, with an ongoing discussion about architectural improvements. Elijah Newren's three-patch series, now merged to `next`, significantly improves performance by batching blob fetches that previously happened one-at-a-time. The implementation handles both commands' specific needs while maintaining exact patch ID computation and respecting pathspec filtering.

Derrick Stolee's latest review looks beyond the immediate fixes, suggesting the prefetch logic might better live alongside similar functionality in `diff.c` rather than `builtin/log.c`. While praising the working solution, he notes this could provide a more maintainable foundation for future commands needing similar optimizations. Earlier discussions with Phillip Wood had already surfaced related questions about generalizing the approach, particularly for `git rebase`, though practical constraints make that a lower priority.

The thread captures a healthy pattern in Git's development: an urgent optimization lands first where it's needed most, followed by thoughtful review about how to evolve the codebase for broader benefit. With the core performance fixes merged and test coverage gaps identified, the conversation now turns to how these improvements might shape Git's architecture for future work in this area.