This is a review response from Junio Hamano on the submodule clone optimization patch. He questions whether using `git clone --local` is the right approach for reusing submodule objects across worktrees, suggesting that `git worktree add` might be more appropriate since it would maintain a single repository with multiple worktrees rather than creating separate clones.

The discussion focuses on the technical details of how submodule objects should be shared between worktrees. Junio raises an important architectural concern - whether the current approach of local clones properly maintains the relationship between worktrees sharing the same submodule repository. This could affect whether changes in one worktree's submodule are visible in another.

This email moves the discussion from implementation details of the optimization to considering its fundamental design approach. The thread now has an open question about whether the patch's strategy of using local clones is architecturally sound or if it should be reworked to use worktree add instead. This could significantly impact the direction of the patch series.