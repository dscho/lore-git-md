This is part 8 of Patrick Steinhardt's 12-patch series to enable `set -e` in Git's test suite (Bash 5+ only), focusing on test infrastructure modernization. The series has been through multiple iterations with Jeff King (Peff) as primary reviewer.

The current patch addresses t0008-ignores.sh, which tests Git's ignore patterns. The issue is that the test uses `grep -v` in a subshell where non-matches are expected behavior, but would trigger an error under `set -e`. The fix adds `|| :` to explicitly ignore these expected failures, maintaining the test's intent while making it compatible with strict error checking.

This continues the series' pattern of methodically preparing individual test files for `set -e` by auditing and fixing error-handling assumptions. The change is mechanical and low-risk, touching only two lines in one test file. The patch follows the established approach seen in parts 6 and 7, where similar patterns were addressed across multiple test files and core infrastructure.

The series remains focused on backward compatibility while incrementally improving test reliability. This particular patch shows the careful attention to detail required when enabling `set -e`, where even simple command substitutions need scrutiny for their error-handling implications. With 8 of 12 patches now in place, the series appears to be making steady progress toward its goal.