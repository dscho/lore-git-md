Thread identity
Subject: [PATCH] technical doc: add LMAP (Loose Object Map) format specification
Series version: standalone patch (no versioning)
Originating author: (not specified in summary; assume from context if later emails reveal)
Thread type: documentation patch for an experimental feature

Core problem or goal
Provide a missing technical specification for the LMAP (Loose Object Map) binary format, which is used by the experimental Rust implementation (`loose.rs`) to map between object ID formats (e.g., SHA-1 and SHA-256). The goal is to enable C developers to understand the format's logic and layout, filling a documentation gap without modifying existing code or behavior.

Current status
Patch is newly proposed; no prior versions or decisions. The patch is purely additive, introducing `Documentation/technical/loose-object-map.adoc` and updating build system files (`Makefile`, `meson.build`) to include the new documentation. No objections or reviews have been recorded yet.

Key technical details
- **New file**: `Documentation/technical/loose-object-map.adoc` (detailed bit-level specification of LMAP format).
- **Format structure**: Header, object format table, data sections (shortened indices, full OID tables, metadata tables), and trailer.
- **Encoding**: Multi-byte integers stored in big-endian; alignment requirements specified.
- **Subsystems touched**: Documentation and build system (no runtime code changes).
- **Symbols**: None (documentation-only).
- **Behavior**: No changes to existing behavior; purely additive.

Open questions and loose ends
None yet; the patch is uncontroversial and does not propose changes to the LMAP format itself. Future discussion may address whether the format is sufficient or needs adjustments, but this is out of scope for the current patch.

Participant positions
None recorded yet.

Related work
- Experimental Rust implementation (`loose.rs`) that uses the LMAP format.
- SHA-256 interoperability work (though not directly tied to this patch).
- Broader ODB abstraction efforts (Patrick Steinhardt, Justin Tobler) may intersect with this format in the future.