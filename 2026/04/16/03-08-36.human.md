This is the final documentation refinement for the mailmap toggle feature in `git cat-file --batch-command`. The patch series is otherwise complete and ready for merging.

Siddharth Asthana responds to Junio Hamano's suggestion to simplify the documentation by removing implementation-focused details about mailmap data caching. The revised text will focus on the user-visible behavior - that the `mailmap <bool>` subcommand toggles name/email rewriting, with data loaded once on first use. The internal optimization (keeping loaded data in memory even when disabled) will be implied rather than explicitly called out.

This change aligns with Git's documentation style of emphasizing what a feature does rather than how it works internally. The implementation remains unchanged from the well-reviewed previous version, with comprehensive test coverage already in place. This is the final tweak before the feature is merged.