This is a discussion about the `--recurse-submodules` support for `git worktree add` feature. Junio C Hamano is responding to Jimmy Aguilar Mena's patch series, questioning the approach of using hardlinks for submodule object sharing between worktrees.

Junio points out that Git's native worktree support was specifically designed to work well on platforms that don't handle hardlinks well, so the implementation shouldn't rely on hardlinking. He suggests an alternative approach where submodule repositories could be shared across worktrees by using `absorbgitdirs` to move the submodule repository into `$GIT_DIR/modules/`, similar to how bare repositories can support multiple worktrees.

This appears to be a technical discussion about the implementation approach rather than a rejection of the feature itself. Junio is offering guidance on how to better align with Git's existing worktree model while addressing platform compatibility concerns. The thread shows the maintainer engaging with the technical details of the proposed solution and suggesting alternative approaches that might be more robust.