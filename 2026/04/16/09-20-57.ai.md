This is a refactoring patch that is part of a series fixing an authentication bug in Git's HTTP layer. The patch extracts common credential-filling code from three HTTP retry paths into a new `http_reauth_prepare()` helper function. This is purely a code reorganization with no behavior change - the actual bugfix will come in subsequent patches that modify this new helper.

The change touches three files: `http.c` where the new helper is implemented, `http.h` where it's declared, and `remote-curl.c` where two call sites are updated. The commit message clearly states this is preparatory work for adding pre-fill logic (like empty-auth attempts) in a single place rather than duplicating it across multiple retry paths.

This is a clean, focused refactoring that follows Git's style guidelines. The commit message explains the motivation well and the diff shows straightforward mechanical changes. As the first patch in a series, it properly sets up the infrastructure needed for the actual bugfix to follow.