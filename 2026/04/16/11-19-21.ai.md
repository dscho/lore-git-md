This is part 4 of Patrick Steinhardt's 12-patch series to enable `set -e` in Git's test suite when running under Bash 5+. The patch focuses on making `git config --unset` calls resilient to failure by adding `|| :` to suppress errors when unsetting configuration values that may not exist. 

The changes are mechanical but important for the broader goal - they prevent test failures when `set -e` is enabled and a config value being unset doesn't exist. The patch modifies three test scripts that use `git config --unset` for cleanup: t4032 (diff hunk context), t7508 (status), and t9138 (svn authors). In each case, the change follows the same pattern - adding `|| :` to make the command succeed even if the config key isn't set.

This continues the series' careful approach of making test infrastructure changes that maintain backward compatibility while preparing for stricter error checking. The changes are straightforward and unlikely to be controversial, as they don't affect test behavior - they make existing cleanup more robust. With core reviewers like Jeff King already engaged on earlier patches, this incremental change appears ready to proceed as part of the larger series.