This is a test infrastructure change from Patrick Steinhardt, part of his ongoing series to enable `set -e` in Git's test suite when running under Bash 5+. The patch modifies signal-handling test cases to work correctly with `set -e` by changing how exit codes are captured from subshells.

The technical change replaces `foo; echo $?` patterns with `foo && echo 0 || echo $?` in three test cases that verify Git dies with SIGPIPE (signal 13). This modification ensures the subshell won't exit early when `set -e` is enabled, while still correctly capturing the expected non-zero exit status. The affected tests are in `t/t0005-signals.sh` (two cases) and `t/t3600-rm.sh` (one case).

This is a mechanical but necessary adjustment for the broader `set -e` effort, showing Patrick's thorough approach to edge cases. The changes are straightforward and focused - each modified line follows the same clear pattern. The patch maintains the existing test behavior while making it compatible with the stricter error handling that `set -e` will introduce.