This is a follow-up discussion on Patrick Steinhardt's completed series to enable `set -e` (errexit) in Git's test suite. Jeff King (Peff) acknowledges the technical solution but raises practical concerns about verifying the error detection works as intended.

Peff suggests adding a test case that deliberately contains an error (using a misspelled `test_expect_foobar`) to confirm the errexit behavior catches such mistakes. He notes this only triggers when explicitly using Bash, not the default `/bin/sh` (dash on Debian). His main question is about CI coverage - whether GitHub and GitLab CI environments reliably use a suitable Bash version to catch these errors, and suggests explicitly setting `TEST_SHELL_PATH` in at least one Linux CI job to ensure proper shell is used.

The email reflects ongoing attention to real-world validation of the new error checking, even after the technical implementation has been accepted. Peff's focus shifts from the core change (already settled) to practical verification and CI integration details. His tone remains constructive while probing for potential gaps in the solution's test coverage. This is characteristic of his thorough review style - ensuring not just that code works in theory, but that it will function as intended across all supported environments.