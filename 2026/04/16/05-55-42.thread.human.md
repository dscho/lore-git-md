This thread is about adding missing documentation for the LMAP (Loose Object Map) binary format, an experimental feature used by the Rust-based `loose.rs` implementation to map object IDs between different hash algorithms (e.g., SHA-1 and SHA-256). The patch introduces a new technical specification in `Documentation/technical/loose-object-map.adoc`, detailing the format's bit-level layout, including headers, object format tables, data sections, and alignment requirements. It also updates the build system to include the new documentation.

The patch is uncontroversial and purely additive, addressing a clear gap in documentation without modifying existing code or behavior. The specification itself is thorough, making it easier for C developers to understand the format's logic. While future discussions might revisit the format's design, this patch does not propose any changes—it simply documents the current state. No reviews or objections have been raised yet, so the patch is likely to be merged as-is.