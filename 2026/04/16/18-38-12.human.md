This is a technical discussion about implementing `--recurse-submodules` support for `git worktree add`, focusing on how to handle submodule repositories across worktrees. Phillip Wood responds to Junio C Hamano's concerns about the current hardlink-based approach for sharing submodule objects between worktrees.

Phillip suggests an alternative that would use Git's existing worktree infrastructure more directly. Instead of hardlinking, he proposes creating a `commondir` file in each submodule's worktree gitdir that points back to the main submodule repository in `$GIT_COMMON_DIR/modules/`. This would maintain the same isolation benefits while avoiding platform-specific hardlink issues. The approach would automatically clean up submodule worktrees when their parent worktree is removed, since all submodule gitdirs would live under the parent worktree's directory structure.

The discussion highlights the tradeoffs between different approaches to sharing submodule data across worktrees. Phillip's suggestion appears to align better with Git's cross-platform design principles while preserving the desired isolation properties. This represents progress toward resolving the implementation concerns raised earlier in the thread.