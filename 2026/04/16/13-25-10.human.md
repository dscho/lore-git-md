This is a discussion about the ongoing `contrib/subtree` recursion limit patch series, with a new participant suggesting an alternative approach.

Ian Jackson, the original reporter of the recursion limit issue, chimes in to offer a review of Colin Stagner's patches. However, they first propose a simpler solution: running the script under bash (which has higher recursion limits) rather than rewriting the algorithm. They note this could be done with a small wrapper that checks for bash availability.

The email reveals two key points: 1) there's at least one active subtree user willing to review the technical changes, addressing Junio's concern about lack of user feedback, and 2) a potentially simpler workaround exists that hasn't been considered in previous discussion. Jackson also expresses surprise that subtree is implemented in shell at all, suggesting Rust/libgit2 would have been their choice.

This shifts the conversation from "how to get the patches reviewed" to "are these patches even the right solution?" The technical discussion now has a clear stakeholder (Jackson) who can validate whether the changes work, but they're questioning the fundamental approach. The patches may need to justify why a full algorithm rewrite is better than a simpler bash workaround.