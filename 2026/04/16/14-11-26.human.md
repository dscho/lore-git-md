This is a review response from Derrick Stolee on Elijah Newren's patch to add safety checks to the new `git backfill` command. The patch (first in a 3-part series) rejects certain `rev-list` options that would be silently ignored or counterproductive when used with backfill.

Stolee generally agrees with the approach of explicitly rejecting problematic options like diff filters (-S/-G, --diff-filter), file-following (--follow), line-log (-L), and object filtering (--filter). He suggests that some formatting-related options could potentially be warnings rather than hard errors, but considers this a future refinement rather than a blocker. The only suggested change is to use a macro for the repeated error message string to avoid duplication.

The review indicates the patch is on the right track, with Stolee's comments focusing on minor implementation details rather than fundamental concerns. As an expert in Git's object model and performance optimizations, his approval carries weight regarding the technical soundness of the approach. The thread shows the patch moving forward with only minor adjustments needed.