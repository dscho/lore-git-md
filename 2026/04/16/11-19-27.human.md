This is part 10/12 of Patrick Steinhardt's series to enable `set -e` in Git's test suite (Bash 5+ only), focusing on test modernization. The patch fixes a potential issue in `t6002-rev-list-bisect.sh` where using `expr` for arithmetic could fail under `set -e` when results are zero.

The change replaces `expr` with shell arithmetic (`$(( ))`), which doesn't fail on zero results, while also modernizing the test function's style. This continues the series' pattern of methodical, focused changes - here touching just one test file with a clear explanation of the `expr` gotcha being addressed.

The patch maintains the series' high quality with careful attention to backward compatibility and platform considerations. It follows the established approach of making minimal changes to prepare individual tests for `set -e`, now covering bisection testing after previous patches addressed signal handling, ACLs, and other test patterns. With CI validation already confirmed in earlier parts, this appears ready for merging as part of the overall effort to make the suite more robust.