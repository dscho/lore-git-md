This is a technical refinement discussion in the final stages of a feature series implementing autostash behavior for `git checkout -m`. Phillip Wood raises two implementation questions about the patch that adds conflict marker label support to the autostash machinery.

First, he questions why the patch requires passing a stash message parameter when applying an existing stash, since the message is already stored with the stash. This appears to be an unnecessary parameter that could be simplified. Second, he suggests updating existing callers to pass NULL labels rather than introducing a new `apply_autostash_ref_with_labels()` function, which would make future enhancements easier.

These are minor implementation details in an otherwise complete and reviewed series. The core functionality - allowing customized conflict marker labels during autostash application - remains sound. Phillip's suggestions aim to streamline the API surface without changing behavior. The thread context shows this series has already addressed all major technical concerns through 12 iterations and has maintainer approval, with these being final polish items before merging.