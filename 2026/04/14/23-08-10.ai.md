This is a technical discussion in Patrick Steinhardt's series to enable `set -e` in Git's test suite. Jeff King continues the implications of platform-specific shell behavior, specifically around older Bash versions on macOS that incorrectly handle `set -e` with the `command` built-in.

Jeff suggests two alternative approaches to the previously identified issue: either manually toggling `set -e` around problematic invocations (which he acknowledges is "gross"), or simply not enabling `set -e` on known-buggy shells like macOS's ancient Bash 3.2.57. He notes the primary goal is catching programming errors in test scripts, which could still be achieved if even some platforms use `set -e`.

The email reveals Jeff's ongoing skepticism about whether the complexity of `set -e` adoption is justified compared to simpler approaches like checking stderr output. This reflects a philosophical difference in test robustness strategies - Patrick's series aims for strict error handling while Jeff questions whether the maintenance burden outweighs the benefits given platform inconsistencies.

The discussion highlights the tension between ideal test hygiene and practical constraints of cross-platform shell scripting, with macOS's outdated Bash version presenting a particularly stubborn obstacle. Jeff's perspective as a long-time contributor with deep shell expertise carries weight in assessing whether the edge cases make the effort worthwhile.