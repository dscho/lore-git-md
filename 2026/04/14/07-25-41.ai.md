This is a technical discussion about submodule configuration, specifically exploring ways to set per-submodule SSH commands during initialization. Shibo Xia responds to Jeff King's earlier suggestions by proposing a new approach - a general per-submodule config injection mechanism for `git submodule update --init`.

The discussion has evolved from workarounds to considering architectural changes. Shibo acknowledges Jeff's proposed solutions (using `includeIf` directives and a two-step workaround) but finds them awkward, especially when different submodules need different settings. His proposed direction would allow passing configuration directly to the underlying clone/fetch operation for individual submodules without affecting others.

Key technical points include:
- The proposed mechanism would extend `git submodule update --init` to accept configuration overrides
- This would be similar to `git clone -c` but scoped to submodule initialization
- The change would address the current limitation where workarounds either expose implementation details or require global configuration

The email positions this as an RFC (Request for Comments) approach, suggesting Shibo is seeking feedback before implementing. Jeff King's prior email had noted his limited submodule experience, so this proposal may need review from submodule experts. The thread continues to balance practical solutions with clean design boundaries, now moving toward potential core changes rather than workarounds.