This is a follow-up in Patrick Steinhardt's series to enable `set -e` in Git's test suite, specifically addressing test t0008-ignores.sh. The patch adds error suppression (`|| true`) for expected `grep -v` failures in subshells that would otherwise trigger errors under strict mode.

Junio Hamano noted a minor inconsistency in style - this patch uses `|| true` where previous ones used `|| :` (functionally equivalent but syntactically different). Patrick acknowledges this and agrees to standardize on `:` in the next iteration, maintaining the project's preference for stylistic consistency even in small details.

The exchange shows the final polishing phase of this now-merged series, where only trivial style tweaks remain. The substantive work of making the entire test suite `set -e`-safe is complete, with all edge cases addressed across platforms and subsystems. This particular patch follows the established pattern of the series: identifying potential failure points under strict mode and adding appropriate guards while preserving test semantics.

Key technical context carried forward: the test suite now runs with `set -e` enabled, requiring explicit handling of expected failures. The `test_must_fail` helper has been updated to work correctly in this environment, and platform-specific cases (FreeBSD, macOS, git-svn) have all been addressed. What remains are purely stylistic cleanups like this one.