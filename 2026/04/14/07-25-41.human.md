This is a design discussion about submodule SSH configuration, with Shibo Xia proposing a new approach after considering Jeff King's suggestions. The thread has evolved from a specific workflow question to exploring potential Git improvements.

Shibo responds to Jeff King's two proposed workarounds (using `includeIf` directives and a two-step config approach) by noting they feel awkward for cases where different submodules need different SSH settings during initial clone. He proposes creating an RFC patch for a more direct solution: a per-submodule config injection mechanism during `git submodule update --init` that would pass configuration to the underlying clone/fetch operation without affecting other submodules.

The discussion remains technical but is moving toward concrete implementation ideas. Shibo's proposal would represent a more fundamental change than the workarounds discussed so far, potentially offering a cleaner solution to the original problem of configuring SSH behavior per submodule. His email seeks validation that this direction seems reasonable before investing time in a patch.

This continues the thread's pattern of progressively deeper technical solutions, now reaching the point where a contributor is considering writing code to address the underlying limitation. The maintainer (Junio Hamano) and experienced contributor (Jeff King) have both weighed in, setting up an opportunity for Shibo to contribute a concrete proposal that could advance the discussion.