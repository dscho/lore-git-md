This is a follow-up discussion on patch 4/6 of a refactoring series for xdiff's record cleanup logic. Phillip Wood suggests an alternative approach to handling the double evaluation in XDL_MIN() that was introduced in Ezekiel Newren's patch.

Phillip's proposed changes would:
1. Move the limit calculation inside each file's processing loop
2. Eliminate the separate `mlim1` and `mlim2` variables in favor of reusing `mlim`
3. Keep the `need_min` check but apply it differently
4. Remove the redundant `!need_min` condition from the ternary operators

The suggestion comes after the series has already been merged by Junio C Hamano, indicating this is a potential post-merge improvement rather than a blocking issue. Phillip's version maintains the same logical flow but with more explicit steps and avoids the macro evaluation concern.

This continues the thread's theme of refining the xdiff cleanup code for both correctness and readability. The discussion remains focused on technical improvements to the already-accepted changes, with Phillip offering an alternative implementation that addresses a minor stylistic concern in the merged code.