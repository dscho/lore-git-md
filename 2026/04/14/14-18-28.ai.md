This is the third patch in Siddharth Shrimali's test modernization series for t7004-tag.sh, continuing the effort to replace brittle test patterns with more robust alternatives. The patch focuses on eliminating subshell usage that could mask Git command exit codes, a known issue in the test suite.

The changes replace `test $(git ...)` patterns with direct output capture to files followed by `test_cmp` or `test_grep` checks, and convert `! (git ...)` subshell negations to `test_must_fail`. This ensures that if Git crashes during a test (e.g., with a segmentation fault), the test will fail immediately rather than hiding the error behind a string comparison or inverted exit code check.

The patch modifies 26 lines across 8 test cases in t7004-tag.sh, with a net change of +10/-8 lines. Key technical changes include:
- Replacing subshell command substitution with direct output capture to files
- Using `test_grep` for pattern matching where exact output isn't critical
- Converting negative assertions from `! (git ...)` to `test_must_fail git ...`
- Adding explicit `expect` files for precise output verification where needed

This follows the same high-quality approach seen in the previous patches, systematically addressing another class of test fragility while maintaining the original test intent. The changes are focused and mechanical, with no functional impact beyond improved test reliability. The patch appears ready for review, building on the positive reception of the first two patches in the series.