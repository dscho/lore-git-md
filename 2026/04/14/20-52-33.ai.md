This is a follow-up in a test infrastructure thread about error reporting improvements, specifically addressing a missed edge case in the `test_must_fail` helper's parameter validation. The patch series, originally started by SZEDER Gábor in 2021, aims to modernize error handling by using Git's BUG() mechanism consistently across test helpers.

SZEDER Gábor notes that if this change had been merged earlier, it would have caught a specific test failure scenario that occurred in the interim (referencing a 2026 email thread where the issue was discovered). The context shows this is part of a broader effort to standardize on BUG() for parameter validation in test helpers, following similar changes like the `test_bool_env` conversion.

Jeff King (Peff) had previously reviewed the approach, expressing mild preference for the BUG() consistency but noting the practical impact would be minimal since test failures would be caught regardless. The current email serves as a) confirmation that the change would have had concrete value in catching a real issue, and b) implicit request to proceed with the long-pending patch series.

Key technical details remain unchanged from the parent summary: the change affects `test-lib-functions.sh` validation logic and its test cases in `t/t0000-basic.sh`, with the BUG() integration interacting with the test framework's file descriptor conventions (fd 6 for stderr). The thread has already established consensus on both the technical approach and the file descriptor handling, making this a low-risk continuation of agreed-upon test infrastructure improvements.