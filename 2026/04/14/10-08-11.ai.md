This is a follow-up discussion in the xdiff refactoring thread, where Phillip Wood provides an alternative implementation suggestion for the INVESTIGATE handling logic that was reverted in v5. The thread had previously seen the series merged after addressing a regression, but Phillip notes two outstanding review comments that weren't incorporated.

Phillip's proposed diff shows a different approach to handling the INVESTIGATE state that avoids modifying the action arrays directly by using a local `action` variable. The change would:
1. Make the state transitions more visible by explicitly setting `action` to KEEP or DISCARD after INVESTIGATE checks
2. Add BUG() checks for illegal states
3. Maintain the same logical flow but with clearer intermediate steps

He also references previous feedback about optimizing duplicate `xdl_bogosqrt()` calls that wasn't addressed in v5. The discussion appears technical but collegial, with Phillip offering concrete improvements rather than blocking the series.

This suggests the xdiff cleanup work may see minor follow-up patches to incorporate these remaining readability and optimization opportunities, though the core type safety improvements are now safely merged. The exchange demonstrates the project's attention to both correctness and code quality even after a series is considered "done".