This is a follow-up discussion about submodule initialization and SSH configuration, continuing a thread where the maintainer suggested a workflow solution.

Shibo Xia acknowledges Junio Hamano's proposed workaround of separating `submodule init` and `submodule update` to allow URL modification between the two steps. While agreeing this approach would work, Xia raises a deeper question about whether Git should support more direct per-submodule SSH command configuration rather than relying on URL rewriting.

The email highlights a tension points in Git's design philosophy - whether SSH configuration belongs at the URL/transport layer (current approach) or should be exposed as a first-class submodule configuration option. Xia's follow-up suggests they see value in more explicit SSH control, though they don't propose specific changes.

This appears to be moving from a concrete "how to" question toward a design discussion about Git's boundaries with SSH tooling. The thread remains constructive, with Xia demonstrating understanding of the current solution while thoughtfully questioning whether the status quo represents the ideal long-term design.