This is a technical refinement to Patrick Steinhardt's ongoing effort to enable `set -e` in Git's test suite, specifically addressing signal handling test cases. The patch responds to Junio Hamano's concerns about edge cases where commands might unexpectedly succeed.

The changes modify three test cases that verify Git dies with SIGPIPE (signal 13) when its output pipe is closed. The new version explicitly handles both success (exit code 0) and failure cases by using `&& echo 0` before the `|| echo $?` fallback, ensuring the output always contains either 0 or the actual exit code. This addresses Junio's concern about silent failures when commands unexpectedly succeed.

The patch touches:
- `t/t0005-signals.sh` (two test cases)
- `t/t3600-rm.sh` (one test case)

Each modified test now uses the pattern `command && echo 0 1>&3 || echo $? 1>&3` piped through file descriptor dance to capture output cleanly. This maintains the test's ability to verify SIGPIPE cases while properly handling successful executions, all while remaining compatible with `set -e`.

The changes demonstrate careful attention to edge cases in test infrastructure, showing how enabling stricter error handling surfaces subtle behavioral considerations. The solution follows Junio's earlier suggestions while keeping the test assertions clear and explicit. This iteration appears to resolve the maintainer's concerns about the overall approach.