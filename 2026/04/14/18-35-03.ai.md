This is a follow-up refinement to the autostash implementation in the sequencer, part of the v12 series adding unified conflict resolution to `git checkout -m`. The author Harald Nordgren responds to Phillip Wood's suggestion about optimizing the implementation by consolidating the autostash creation functions rather than adding a new one.

The change is straightforward - Harald agrees to update the existing `create_autostash_ref()` callers instead of introducing a separate silent variant. This keeps the API surface clean while maintaining the same functionality. The patch series is in its final polishing phase with all major technical decisions settled, and this exchange represents minor implementation cleanup before merging to `next`.

The thread shows the collaborative refinement process where even after 12 iterations, small optimizations are still being identified and implemented. With Junio and Phillip both engaged in the final review, this series appears ready for integration once these last details are addressed.