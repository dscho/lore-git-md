This is a technical discussion in the final polishing phase of a feature series implementing autostash behavior for `git checkout -m`. Harald Nordgren responds to Phillip Wood's review feedback about two implementation details in the autostash creation logic.

The first point addresses Phillip's observation that creating a ref (`CHECKOUT_AUTOSTASH_HEAD`) might be unnecessary since there's no need to persist the stash OID across user interactions (unlike `MERGE_AUTOSTASH` in rebase/merge). Harald acknowledges the architectural point but defers addressing it, noting implementation uncertainty.

The second point concerns the control flow around `merge_working_tree()` when autostashing. Phillip questioned whether the stash application logic properly handles cases where `opts->merge` is false. Harald similarly defers this refinement, suggesting it may be better handled in follow-up work.

These responses show the author pragmatically focusing on the immediate goal of getting the core functionality merged while flagging potential future improvements. The exchange maintains the collaborative tone characteristic of this well-reviewed series, now in its 12th iteration. Both points appear to be non-blocking refinements rather than fundamental issues, consistent with the thread's advanced state where major technical decisions are already settled.