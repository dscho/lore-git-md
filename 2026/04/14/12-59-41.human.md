This is the twelfth iteration of a feature patch series that enhances `git checkout -m` with autostash behavior during branch switching conflicts. The series has already received extensive review and maintainer approval, with this version making minor refinements to the autostash message format and function naming.

The key changes in v12 are:
1. Renaming `create_autostash_ref_silent()` to `create_autostash_ref_with_msg_silent()` to better clarity
2. Further polishing the autostash message format to include branch context ("autostash while switching to 'branch'")
3. Minor test updates to verify the new message format

The series remains technically complete with all prior feedback addressed, including Jeff King's validation of the `CHECKOUT_AUTOSTASH_HEAD` refname convention. The implementation spans multiple components (`builtin/checkout.c`, `builtin/stash.c`, `sequencer.c`) and includes comprehensive test coverage (19 new test cases in t7201-co.sh).

This represents the final polish before merging, with all discussion points resolved. The changes maintain backward compatibility while making conflict resolution more consistent with Git's existing autostash behavior in rebase/merge. The series provides several user-facing improvements:
- `git checkout -m` now automatically stashes uncommitted changes during conflicting branch switches
- New `--label-*` options for `git stash apply` to customize conflict markers
- Silent autostash creation during operations

With maintainer approval already secured, this version is ready for merging and represents a significant quality-of-life improvement for branch switching workflows with uncommitted changes.