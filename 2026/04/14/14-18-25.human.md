This is a test modernization series targeting t7004-tag.sh, following up on the author's previous work to improve test patterns in this file. The series consists of three cleanup patches that address brittle testing patterns:

1. The first patch removes a hardcoded tag count in an invalid name test, implementing a suggestion from Junio on the author's previous submission. This makes the test more maintainable by not relying on knowing the exact number of tags in the repository.

2. The second patch replaces hardcoded global state checks with dynamic verification, making the tests more robust against changes in the test environment.

3. The third patch modernizes the test by eliminating subshell patterns that could potentially mask Git exit codes, using more reliable test helper patterns instead.

The changes are focused on test hygiene rather than functionality - they improve the reliability and maintainability of the test suite without changing any actual tag behavior. The author notes this builds on their previous work to replace `wc -l` with modern test helpers in the same file. The series appears well-scoped and addresses specific, concrete issues in the test file.