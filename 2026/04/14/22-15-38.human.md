This is a post-merge discussion in the xdiff refactoring thread, where Junio C Hamano clarifies a technical point about macro evaluation in the recently merged series. The conversation follows up on Ezekiel Newren's v5 refactoring of `xdl_cleanup_records()` which improved type safety and clarity in the xdiff record cleanup logic.

Junio points out that Phillip Wood's concern about duplicate `xdl_bogosqrt()` calls was actually about the macro expansion of `XDL_MIN()` potentially evaluating its arguments multiple times, not about the function being called for both files. The `XDL_MIN` macro expands to a ternary that may evaluate `xdl_bogosqrt(xdf1->nrec)` twice when computing `mlim1`, which could be a performance concern compared to the original code that stored the result in `mlim` first.

This is a minor post-merge observation rather than a blocking issue - the series has already been merged with all parties satisfied. The discussion serves to document the macro evaluation behavior for future reference, though no immediate changes are needed. The thread shows the project's attention to detail even after code has been accepted, with maintainers noting subtle implementation characteristics that might warrant future optimization.