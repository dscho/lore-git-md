This is a technical discussion in Taylor Blau's ongoing series on incremental MIDX/bitmap-based repacking, specifically addressing patch 6/16 which introduces customizable base layers for MIDX writes. The exchange clarifies the design rationale behind the `--base=none` option, which creates a root incremental MIDX layer without a parent.

Taylor explains that while a root incremental MIDX is logically similar to a non-incremental MIDX (both being single-layer), the implementation must use the incremental format because the main multi-pack-index file cannot be directly replaced. This maintains the series' architectural approach of always treating MIDX operations incrementally, even for root layers. The discussion also confirms that `--base` requires `--checksum-only` to preserve chain invariants when not appending to the tip.

Jeff King's earlier conceptual question about whether `--base=none` could be replaced with non-incremental MIDX writes is thus resolved - the incremental approach is necessary for both technical and architectural consistency. The exchange shows the series' design holding up under expert scrutiny, with Taylor providing the missing context about filesystem constraints that make the incremental approach mandatory. This patch remains preparatory work for the later geometric repacking logic that will manage the MIDX chain structure.