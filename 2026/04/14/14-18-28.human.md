Test modernization patch completing a series to improve t7004-tag.sh reliability. This third patch focuses on eliminating subshell patterns that could mask Git command failures, replacing them with direct output capture and comparison.

The patch converts several test cases that used subshells (like `test $(git ...) = ...`) to instead write output to files and compare with `test_cmp` or `test_grep`. This ensures Git command failures (including crashes) are properly detected rather than being hidden behind string comparisons. It also replaces `! (git ...)` negation patterns with the more robust `test_must_fail`.

Changes touch 26 lines across 8 test cases, with a net addition of 10 lines (18 insertions, 8 deletions). The modifications follow established test modernization patterns seen elsewhere in the test suite. This completes the author's three-patch series to make t7004-tag.sh more robust against failures and less dependent on global state assumptions.

The patch appears well-crafted - it systematically addresses a clear issue (subshell exit code masking) using standard test suite idioms. As with the previous patches in the series, this is a mechanical improvement that doesn't affect test functionality, only reliability. The changes to test logic are minimal and focused solely on improving failure detection.