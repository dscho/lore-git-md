This is the first patch in Siddharth Shrimali's test modernization series for t7004-tag.sh, focusing on removing brittle test patterns. The patch specifically eliminates a hardcoded tag count check that made the test dependent on the exact number of tags created by previous tests.

The change removes redundant verification that exactly one tag exists before and after attempting to create invalid tags, since the test already uses `test_must_fail` to confirm the invalid tag operations fail. This makes the test more robust against changes elsewhere in the test file that might add or remove tags. The modification follows Junio Hamano's suggestion to avoid relying on global state.

The patch touches only t/t7004-tag.sh, removing 5 lines while adding 1 (a net reduction of 4 lines). This is part of the project's ongoing effort to modernize test patterns by replacing fragile constructs with more isolated, reliable alternatives. The author has clearly incorporated prior review feedback, and the change aligns with established test modernization practices.