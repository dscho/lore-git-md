This is part 2 of a 4-patch series implementing autostash behavior for `git checkout -m`, now in its 11th iteration with maintainer approval. The series as a whole enhances branch switching by automatically stashing uncommitted changes during conflicting checkouts, with this particular patch focusing on the sequencer plumbing.

The patch adds a silent mode to autostash creation, introducing a new `create_autostash_ref_silent()` function that suppresses the "Created autostash" message. This is a straightforward internal change that modifies the existing autostash machinery to support quiet operation when needed, particularly for the new `git checkout -m` behavior where automatic stashing should happen without verbose output.

The implementation is clean and minimal, just adding a `silent` parameter to the internal function and exposing it through a new public interface while maintaining the existing behavior for current callers. The change is well-tested as part of the larger series (which includes 19 new test cases), though this specific patch doesn't require additional tests since it's plumbing for functionality exercised elsewhere.

With all technical concerns resolved in previous iterations and maintainer approval secured, this series - including this silent autostash capability - appears ready to merge as a quality-of-life improvement for branch switching workflows.