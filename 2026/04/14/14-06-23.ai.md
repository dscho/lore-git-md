This is a technical refinement to the autostash implementation in the sequencer, part of an already well-reviewed series (v12) that adds silent autostash creation during branch switching conflicts. Phillip Wood suggests a minor optimization - updating the existing `create_autostash_ref()` callers rather than adding a new function - but notes the current implementation is sensible. The patch itself adds parameters to control message display and content in the autostash creation internals, with the new `create_autostash_ref_with_msg_silent()` function providing the key silent operation capability needed by the larger feature.

The changes are focused and mechanical, touching only `sequencer.c` and its header to add the new functionality while maintaining backward compatibility. This is a building block for the series' main goal of integrating autostash behavior into `git checkout -m` conflict resolution. With the series' core functionality already approved and this being a straightforward internal change, Phillip's suggestion appears to be a non-blocking optimization rather than a required fix.