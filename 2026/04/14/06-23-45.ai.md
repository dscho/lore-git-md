This is a technical follow-up to Patrick Steinhardt's test suite hardening series, specifically addressing the `test_must_fail()` helper function changes. Jeff King (Peff) points out a subtle interaction where shell functions in the test_must_fail` wrapper would have `set -e` suppressed, even if they explicitly enable it internally. While noting this is currently academic since tests don't typically use `set -e`, he flags it as a potential future gotcha.

The observation doesn't block the current series (which Junio has already approved) but serves as documentation of a behavioral edge case that might matter if test authors start using `set -e` in their helper functions. The comment preserves institutional knowledge about shell behavior that could affect future test suite evolution, particularly around error handling semantics. This fits the pattern of the series uncovering subtle error-handling interactions as it rolls out `set -e` across the test infrastructure.