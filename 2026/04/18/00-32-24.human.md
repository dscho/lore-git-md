This is the final version of Elijah Newren's performance optimization series for partial clone behavior in `git cherry` and `git grep`. The series addresses a significant performance issue where partial clone users experience slow operations due to single-blob fetches.

Key points:
- The v2 removes an accidentally included design document that shouldn't have been part of the submission
- Maintains all the technical improvements from v1:
  - Adds batch prefetching to `git cherry` to avoid hundreds of individual blob fetches
  - Extends similar optimization to `git grep`
  - Includes tests verifying the behavior
- The changes are well-motivated by real-world performance problems (3s per fetch in some cases)
- Comes from a trusted contributor with deep Git expertise
- Has already addressed the maintainer's only concern (the stray design doc)

The series is now clean and ready for merging, bringing measurable performance benefits to partial clone users of these commands. The implementation follows established patterns from similar optimizations in checkout/diff/merge commands.