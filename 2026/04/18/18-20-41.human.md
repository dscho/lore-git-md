This is a review response to a feature patch that adds a `--reverse=before` option to revision walks. Tian Yuchen provides feedback on Mirko Faina's proposed change to allow reversing commit order before applying limits like `--max-count`.

Yuchen's review is substantive, going beyond surface-level concerns to discuss both user experience and implementation details. He suggests the naming (`before`/`after`) might not be intuitive for users who simply want "the oldest commits" rather than thinking about reversal timing. He also proposes a potential optimization to reduce memory usage by avoiding loading the entire reversed commit history when only a few commits are needed.

The review maintains a constructive tone while identifying areas for improvement. Yuchen clearly understands the technical details, referencing specific parts of the implementation like the memory usage in `get_revision()`. His suggestion about using a window or max heap shows engagement with the performance implications.

This feedback will likely prompt discussion about whether the current interface is optimal or if alternative approaches (like a `--oldest` flag) might better serve users while avoiding the memory overhead. The patch author will need to consider both the UX concerns and implementation suggestions in their response.