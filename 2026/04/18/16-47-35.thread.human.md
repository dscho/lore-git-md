This thread discusses Mirko Faina's proposal to add a `--reverse=before` option to Git's revision walk, which would reverse the commit list before applying other limiting operations (unlike the current `--reverse` that reverses after). The core implementation involves changes to `revision.c`, `list-objects.c`, and related documentation.

Jeff King (Peff) provided the authoritative solution for handling multiple `--reverse` flags, introducing an enum-based state machine that maintains backward compatibility while supporting the new syntax. The implementation now cleanly handles cases like `--reverse --reverse` (which cancels out) and `--reverse=<value>` with strict parsing rules. All major design questions have been resolved, including CLI behavior, backward compatibility documentation, and internal constant definitions.

Mirko initially misunderstood some parsing edge cases but quickly corrected this after re-reading Peff's explanation, demonstrating how clear code examples in reviews help catch subtle issues early. With this last clarification, all technical aspects are settled, and the thread now awaits Mirko's v2 patch submission incorporating the finalized design. The collaboration between Mirko, Peff, and other reviewers like Ben Knoble and Tian Yuchen shows how the mailing list process helps refine complex behavior changes while preserving Git's stability.