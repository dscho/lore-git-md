The thread continues discussion of Mirko Faina's proposal to add a `--reverse=before` option to Git's revision walk machinery, which would reverse commits before applying other limiting operations (unlike the existing `--reverse=after` behavior). The v2 patch has addressed most technical concerns, including implementing Jeff King's suggested state machine approach and adding a memory optimization from Tian Yuchen's analysis.

D. Ben Knoble has been closely reviewing the implementation and raised questions about how `max_count` handling differs between the two reverse modes. After some clarification from Mirko, it became clear that in `--reverse=before` mode, the counting responsibility shifts from `get_revision_internal()` to `get_revision()` itself, requiring careful manual decrementing of the count. While this makes `get_revision()` more complex, the behavior is now well-documented and tested.

The patch appears technically sound, with all substantive feedback incorporated. The only remaining open question is Junio Hamano's earlier conceptual concern about "applying count limits later," though the current implementation handles this through its explicit state machine. With thorough test coverage and performance optimizations in place, this feature seems ready for merging pending final maintainer approval. The discussion exemplifies Git's careful approach to modifying core machinery, where even subtle control flow changes receive rigorous review.