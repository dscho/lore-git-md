This thread continues the proposal by Mirko Faina to add a `--reverse=before` option to Git's revision walk, which would reverse commits before applying other limiting operations (unlike the current `--reverse=after` behavior). The discussion has reached technical consensus on the core approach, with recent refinements focusing on edge cases and backward compatibility.

Jeff King (Peff) has just contributed a detailed review addressing how to handle multiple `--reverse` flags, proposing concrete changes to ensure combinations like `--reverse --reverse` cancel out while `--reverse=after --reverse=before` follows last-one-wins semantics. His solution introduces a `--no-reverse` option for explicit negation and uses an enum-based approach in the parsing logic. While noting that using Git's `parse-options` infrastructure would be ideal, Peff acknowledges that conversion is beyond this patch's scope.

The thread has seen productive collaboration, with Ben Knoble driving earlier refinements in edge case handling and code clarity, while Mirko has provided thorough explanations of the control flow decisions. Tian Yuchen's suggestions for memory optimizations via a windowed approach remain pending for the v2 implementation. With Peff's latest review addressing the remaining open questions about option interactions, the series appears ready to move forward once these final adjustments are incorporated.