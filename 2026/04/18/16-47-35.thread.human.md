This thread has progressed through four iterations of a patch series introducing a new `--reverse=before` option for Git's revision walk, which would reverse commits before applying other limiting operations (as opposed to the current `--reverse=after` behavior). The technical implementation is now complete, having incorporated feedback from Jeff King (Peff) on the state machine design and Tian Yuchen's analysis that led to an optimized sliding window approach for memory efficiency. The patches have comprehensive test coverage and documentation.

The discussion has now shifted to final user-facing polish, specifically around naming. Junio Hamano suggested renaming the options from `--reverse=before/after` to `--reverse=oldest/newest`, feeling the current names are too implementation-focused. Johannes Sixt agrees the current names could be clearer but questions whether "oldest/newest" would actually be more intuitive to users. He raises an alternative approach - introducing a separate `--max-count-oldest` option instead of modifying `--reverse`'s behavior - which might better serve the underlying use case of retrieving the earliest commits without overloading the `--reverse` flag.

With all technical concerns addressed, this naming discussion appears to be the final hurdle before the feature can be merged. The thread shows how even after reaching consensus on implementation, Git's maintainers carefully consider how new options will be understood by end users. Johannes' suggestion of an alternative approach at this late stage demonstrates the project's willingness to reconsider fundamental design choices when it leads to better usability.