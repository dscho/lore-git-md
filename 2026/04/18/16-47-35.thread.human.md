This thread centers on Mirko Faina's proposal to add a `--reverse=before` option to Git's revision walker, allowing commits to be reversed before applying limits like `--max-count` (The current `--reverse` operates after limiting.) The technical implementation is now complete after four iterations, with Jeff King's state machine design fully realized and Tian Yuchen's performance analysis leading to an efficient sliding-window memory optimization. All edge cases are handled, and documentation is finalized.

However, the discussion has taken a turn toward user interface design. While the implementation is solid, Junio C Hamano has raised concerns about the clarity of "before/after" terminology, suggesting "oldest/newest" might be more intuitive. Mirko acknowledges the point but notes that neither phrasing is self-explanatory—users would need to consult docs either way. More fundamentally, Chris Torek's analysis of the priority queue mechanics has opened a broader question: Should this functionality modify `--reverse` at all, or would it make more sense to adjust `--max-count` behavior instead? Junio had earlier floated `--max-count-later` as an alternative approach.

The thread now stands at a crossroads. The technical work is done, but the project must decide whether to proceed with the current design or reconsider the feature's conceptual placement. The choice hinges on whether to prioritize technical consistency (aligning with Git's documented option processing order) or user intuition (favoring terms that might be more immediately graspable, even if less precise). Mirko remains engaged, but the next steps likely depend on Junio's final judgment as maintainer.