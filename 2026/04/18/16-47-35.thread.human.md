This thread continues discussion of Mirko Faina's proposal to add a `--reverse=before` option to Git's revision walk machinery, which would reverse commits before applying other limiting options (like `--max-count`) rather than after. The second version of the patch series has addressed all technical feedback from the initial round, including implementing Jeff King's suggested state machine approach and adding a memory optimization (using a sliding window technique) in response to Tian Yuchen's performance analysis.

The implementation now cleanly handles toggling between reverse modes (no-reverse → reverse-after → no-reverse → reverse-before → no-reverse) while maintaining backward compatibility. The changes touch core revision walking code in `revision.[ch]` and `list-objects.c`, with comprehensive test coverage added. Documentation has been expanded to explain the new behavior and edge cases.

All substantive review feedback has been incorporated, leaving only Junio Hamano's high-level questions about the conceptual framing of applying count limits at different stages. The patch appears technically sound and ready for consideration, with the memory optimization particularly addressing earlier concerns about the feature's practical impact. The thread now awaits Junio's evaluation of whether the design aligns with Git's revision walking philosophy.