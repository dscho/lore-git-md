This thread continues the discussion of Mirko Faina's proposal to add a `--reverse=before` option to Git's revision walk machinery, which would reverse commits before applying other limiting operations (unlike the current `--reverse=after` behavior). The conversation has progressed to fine-tuning implementation details, with Ben Knoble providing thorough technical review and Mirko responding thoughtfully to each point.

The latest exchange resolves several open questions: Mirko will add `#define` constants to clarify the reverse state values (0,1,2) in the code, and he agrees to document backward compatibility considerations for users with multiple `--reverse` flags in their aliases. The most substantive discussion centers on the `max_count` handling - Mirko explains why `--reverse=before` needs special counting logic during its output phase, unlike `--reverse=after` which can rely on `get_revision_internal()`'s built-in counting.

With these clarifications, the technical consensus appears solid. The remaining work for v2 includes implementing Tian Yuchen's suggested memory optimizations (using a sliding window or max heap) and finalizing the documentation. The discussion exemplifies Git's culture of deep technical engagement, with both reviewer and maintainer patiently working through complex control flow interactions in `revision.c` and `list-objects.c`. Unless new concerns emerge, this feature appears on track for inclusion once these final details are addressed.