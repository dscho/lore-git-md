This is patch 3/6 in Lorenzo Pegorari's v6 series to preserve promisor file content during repacks, part of his GSoC project. The patch focuses on maintaining debug information in `.promisor` files when packfiles are repacked, addressing a `NEEDSWORK` comment from commit 5374a290.

The key change is using the new `write_promisor_file_after_repack()` helper function (introduced in earlier patches) to properly preserve the contents of `.promisor` files rather than creating empty ones. For geometric repacking, it adds logic to track excluded packs using a `strset`. The implementation shows careful attention to resource management with proper cleanup of the strset.

The patch updates both the `git-repack` documentation to clarify that `.promisor` files will contain "arbitrary contents" (preserved from original files) rather than being empty, and the implementation in `repack-promisor.c` to handle both regular and geometric repack cases. The changes demonstrate Lorenzo's growing familiarity with Git's codebase, incorporating feedback from previous reviews by Tian Yuchen and Junio Hamano.

This appears to be a well-considered technical improvement that has benefited from thorough review. The patch maintains the series' focus on defensive programming while solving the original problem of preserving debugging information in promisor files across repack operations.