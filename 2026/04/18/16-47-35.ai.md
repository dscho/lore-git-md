This is a feature patch adding a new `--reverse=before` option to revision walks, allowing the reversal to happen before commit limiting rather than after. The motivation is to enable commands like `git log --reverse --max-count=3` to return the first 3 commits (in order) rather than the last 3 commits (current behavior).

The patch modifies the revision walking machinery to accept `--reverse=before` or `--reverse=after` (defaulting to `after` for backward compatibility). When `before` is specified, the commit list is reversed before applying limits like `--max-count`. The implementation adds a new state (value 2) to the `reverse` flag in `struct rev_info` and adjusts `get_revision()` to handle the different ordering.

The change includes documentation updates and new test cases verifying both the new `before` behavior and backward compatibility with the existing `after` behavior. The tests demonstrate the difference in output between `--reverse=before --max-count=2` (shows first two commits) versus `--reverse=after --max-count=2` (shows last two commits reversed).

This appears to be a well-constructed patch with clear motivation, thorough test coverage, and careful attention to backward compatibility. The change is focused on the revision walking machinery without introducing broader architectural impacts. The commit message could be slightly more detailed about the internal implementation choices (like the new `reverse=2` state), but the technical content is sound.