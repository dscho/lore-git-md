This is a follow-up discussion on the `--reverse=before` revision walk feature, addressing memory efficiency concerns. Mirko Faina clarifies their earlier response to Tian Yuchen's review, realizing they had misunderstood the suggestion about reducing memory usage.

Mirko had initially explained that Git's linked list structure and parent-based traversal make it impossible to efficiently access commits from the tail without processing the entire history. They now recognize that Tian was proposing a sliding window or max heap approach to limit memory usage while still processing the full history, rather than trying to avoid the full traversal.

The author acknowledges this approach could work and commits to implementing it in v2 of the patch. This shows the technical discussion is progressing productively, with the reviewer's suggestion leading to a concrete improvement in the implementation strategy.

Key files involved remain `revision.c` and `list-objects.c`, with the memory optimization now being the focus before the next version is submitted. The exchange demonstrates effective collaboration between contributor and reviewer to refine both the design and implementation of the feature.