This is patch 11/16 in Patrick Steinhardt's series introducing an in-memory object database source as part of the ongoing ODB abstraction effort. The patch implements the `for_each_object()` callback for the in-memory backend, completing another piece of the ODB interface.

The implementation handles both simple iteration and filtered queries with object info requests. Key technical points:
- Extracts common object info population logic into new `populate_object_info()` helper
- Introduces `odb_source_inmemory_for_each_object_data` struct to pass iteration state
- Supports prefix filtering via oidtree's existing prefix matching
- Respects iteration flags like `PROMISOR_ONLY` and `LOCAL_ONLY`
- Reuses the oidtree infrastructure added in earlier patches for efficient O(log n) lookups

The changes are well-contained within the in-memory backend implementation and follow the pattern established by previous callbacks in the series. The patch continues the series' methodical approach of implementing one ODB interface method at a time while factoring out common functionality where appropriate.

This appears to be a straightforward implementation of a required ODB callback, building on the infrastructure established in earlier patches. The changes are focused and don't introduce any notable new concepts beyond what the series has already established. Given Patrick's track record with this kind of architectural work, the implementation is likely sound, though reviewers may want to verify the callback behavior matches expectations for how `for_each_object()` should work across all ODB backends.