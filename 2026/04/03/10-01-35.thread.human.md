This thread is about a bugfix patch that adds a configurable timeout (`core.configLockTimeout`, defaulting to 1000ms) to Git’s config file locking to prevent failures in concurrent operations like `git worktree add -b`. The patch, now in its second version, has gained strong support from Johannes Schindelin, who has committed to handling any Windows-specific adjustments and now urges the project to merge it without further delay.

The main technical debate—whether the timeout should be explicitly process-wide (Junio C Hamano’s preference) or per-repository with caching removed (Patrick Steinhardt’s counterproposal)—has been resolved in practice by Schindelin’s intervention. He considers the current implementation (with caching) acceptable and argues that any optimization concerns can be addressed in a follow-up patch. This removes the last substantive blocker, as Junio had previously indicated the patch was otherwise ready for merging.

The patch builds on precedent from `core.packedRefsTimeout` and includes comprehensive test coverage. With Schindelin’s endorsement and no remaining objections, the patch is now likely to proceed to Junio’s `next` branch for integration testing, with graduation to `master` expected in the near term. The only remaining step is Junio’s formal queueing, which should follow shortly.