This is part of the ongoing `the_repository` removal effort, specifically converting the reftable backend to avoid global state. Shreyansh Paliwal's patch updates two functions in the reftable backend to use explicit repository parameters instead of `the_repository`. The change is straightforward since the repository instance is already available through other parameters in both cases.

Patrick Steinhardt confirms in his reply that he's working on removing the last remaining dependency (on `is_bare_repository()`) in a separate patch series. The exchange shows smooth coordination between contributors on this multi-year refactoring effort. The patch appears well-scoped and uncontroversial, representing incremental progress toward eliminating global state from Git's internals.