This is a platform compatibility follow-up patch from Junio C Hamano, acknowledging Johannes Schindelin's earlier patch that added `writev()` wrapper handling to the CMake build system. The change completes the cross-build-system support for this POSIX wrapper that was already implemented for Makefile and Meson builds.

Junio notes he should have caught this omission when reviewing the original meson.build changes, taking responsibility for the oversight. The patch itself is straightforward - it adds `writev` to CMake's function detection list and includes the compatibility implementation when needed, exactly matching the pattern used for other POSIX functions like `memmem` and `strlcpy`.

The context makes clear this is a routine build system maintenance patch with no outstanding technical questions. Junio's response indicates the change is uncontroversial and ready to be picked up, though he frames it as catching up with work that ideally would have been handled in the initial series. Given Schindelin's role as Git for Windows maintainer and the focused nature of the fix, this appears to be a low-risk adjustment to keep the Windows CI builds working properly.