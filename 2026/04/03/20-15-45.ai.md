This is a discussion about Git's graph visualization improvements, specifically addressing how to represent root commits in `--graph` output. Junio C Hamano responds to Tian Yuchen's proposal to use distinct symbols (like ■) for root commits, explaining why this approach was previously considered and rejected.

Junio notes that any symbol-based solution would need to handle not just plain root commits, but also roots that appear in `--left-right` and `--boundary` outputs, requiring a set of special characters rather than just one. He points out this complexity led to rejecting symbol-based approaches in at least two prior discussions (without specifying exact dates). The email also raises the open question of whether range boundaries should visually match root commits or be distinct.

The response maintains Junio's established position favoring minimal visual changes (like spacing adjustments) over introducing new symbols, while acknowledging the historical context of these design decisions. This continues the thread's tension between visual clarity and maintaining Git's existing graph rendering conventions, with the maintainer reiterating the project's preference for consistency over new notation. The exchange shows the author (Pablo Sabater) will need to navigate these established constraints while implementing the root spacing improvements.