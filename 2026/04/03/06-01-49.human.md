This is the second patch in Patrick Steinhardt's 16-part series introducing an in-memory object database backend as part of the ongoing ODB abstraction effort. The patch implements the `free()` callback for the new in-memory source, moving cleanup logic from `odb.c` into the new backend-specific implementation.

The changes relocate the definition of `struct cached_object_entry` to the new `odb/source-inmemory.h` header since it's now shared between files, and moves the object cleanup code into a dedicated `odb_source_inmemory_free()` function. This continues the pattern of moving backend-specific logic out of the generic ODB code and into the new abstraction layer.

The patch appears straightforward and mechanical cleanup work that follows naturally from the architectural foundation laid in the first patch. It's part of the broader effort to make object storage more modular while eliminating technical debt in the process. Given Patrick's track record with this kind of structural refactoring and the clear incremental nature of the change, this seems like solid progress on the series.