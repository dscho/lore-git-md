**Thread identity:** `[PATCH v3 0/2]` from Chen Linxuan, adding `includeIf` config conditions `worktree` and `worktree/i` that match a worktree's checkout path.

**Core problem and goal:** Enable config inclusion based on which worktree checkout path the user is in -- simplifies multi-worktree setups where `gitdir`-based patterns are awkward (linked worktree gitdirs live under the main repo's `.git/worktrees/`, not alongside the checkout).

**Current status:** v3 posted. The author has now replied to Junio C Hamano and Phillip Wood with the concrete use case Junio requested. The author confirms Phillip's guessed scenario: they contribute to the Linux kernel both as an employee and as an individual, and want `includeIf worktree:` to automatically set the company email address when working in worktrees under a specific directory prefix. With `extensions.worktreeConfig`, they would have to run `git config --worktree` in every new worktree manually. With `includeIf worktree:`, a single global `.gitconfig` entry handles it automatically. This satisfies the maintainer's requirement for an articulated, real-world motivation.

**Key technical details:**

- **Patch 1/2:** Refactors `include_by_gitdir()` into a generic `include_by_path()` helper in `config.c`.
- **Patch 2/2:** Implements `worktree` and `worktree/i` conditions in `include_condition_is_true()` via `skip_prefix_mem`, calling `include_by_path()` with `repo_get_work_tree(inc->repo)` (NULL outside a repo). The matched path is the worktree checkout root (`git rev-parse --show-toplevel`), not the `.git` directory. Supports glob patterns and `~` expansion. Files: `config.c`, `Documentation/config.adoc`, `t/t1305-config-include.sh`.

**Open questions and loose ends:**

Patrick Steinhardt's three action items (uncontroversial, expected in v4): deduplicate documentation for pattern-matching rules (currently copied from `gitdir`), add test for early-config parsing, add test for nongit environment. Junio additionally requires documentation that helps users choose between `includeIf worktree:` and `extensions.worktreeConfig` based on use case. The author has provided the concrete scenario (automatic email address based on checkout prefix) but has not yet produced the documentation text or addressed the implementation items. These are expected in v4.

Patrick noted that `skip_prefix_mem` conditionals look like a future candidate for a table-driven approach, but deferred as out of scope.

**Participant positions:**

- **Chen Linxuan (author)** -- replied to the maintainer with the requested concrete use case. Has not yet addressed the three implementation action items or the documentation-guidance requirement.

- **Junio C Hamano** -- directionally positive; called the feature "worthwhile." Endorsed Phillip's motivation question and now requires documentation that helps users choose between `includeIf worktree:` and `extensions.worktreeConfig`. The author's use-case reply appears to satisfy his substantive concern, but the documentation requirement stands.

- **Patrick Steinhardt** -- full substantive review. Endorsed patch 1/2 as "good preparatory step." Called patch 2/2 "seems sensible" and "good." Three action items as above.

- **Phillip Wood** -- raised the design-level motivation question, subsequently endorsed by Junio.

**Related work:** None mentioned.