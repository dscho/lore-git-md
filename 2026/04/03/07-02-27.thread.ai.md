Thread identity: `[PATCH v3 0/2]` from Chen Linxuan, introducing new `includeIf` config conditions `worktree` and `worktree/i`. The thread root is the cover letter of this 2-patch series.

Core problem and goal: Complement existing `gitdir` and `onbranch` conditions with a `worktree` condition that matches against a worktree's checkout path, simplifying config inclusion in multi-worktree setups where `gitdir` patterns are awkward because linked worktrees store their gitdirs in the main repo's `.git/worktrees/`.

Current status: v3 has been posted. The series has now received a full substantive review from **Patrick Steinhardt**, addressing Junio's expressed need for volunteer reviewers. Patrick endorsed the series ("Seems sensible," "good patch") with three actionable requests. This clears the primary blocker for the series -- Patrick's review, combined with his earlier endorsement of patch 1/2 as a "good preparatory step," constitutes a complete review from a highly respected technical contributor, which should satisfy Junio's requirement for volunteer review before taking the series into `seen`.

Key technical details:
- **Patch 1/2**: Refactors path-matching logic from `include_by_gitdir()` into a generic `include_by_path()` helper function, decoupling the path-matching from the gitdir condition.
- **Patch 2/2**: Implements the new `worktree` condition (and `worktree/i` for case-insensitive matching) using the new helper, matching against the worktree path from `repo_get_worktree()`. Supports glob patterns (wildcards, `**/` recursive matching) and home directory expansion.
- Core files touched: `config.c`, `path.c`, and new tests in `t/t1300-config.sh`.

Participant positions:
- **Junio C Hamano** -- directionally positive, called the feature "worthwhile" and "OK-ish," but explicitly not performing a full review; solicited volunteer reviewers.
- **Patrick Steinhardt** -- provided a full substantive review of the series. Endorsed patch 1/2 as "good preparatory step." For patch 2/2, called it "Seems sensible" and "good," with three actionable items:
  1. Deduplicate documentation for pattern-matching rules (currently copied verbatim from `gitdir` condition; should share a common section to avoid drift).
  2. Noted that the chain of `skip_prefix_mem` conditionals in `include_condition_is_true()` looks like a future candidate for a table-driven approach, but explicitly deferred that refactoring as out of scope.
  3. Missing test cases: verify the new condition works during early config parsing (mimicking existing test for `gitdir:`), and test behavior outside a repository entirely ("nongit" case).

Open questions and loose ends: No technical objections raised. The three items from Patrick's review are modest and straightforward to address. The next expected step is a v4 incorporating:
- Documentation deduplication for pattern-matching rules.
- Two additional test cases (early config parsing, nongit environment).

Related work: None mentioned.