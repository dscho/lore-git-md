This is a straightforward platform compatibility patch that extends `writev()` support to CMake builds, completing the work Johannes Schindelin started when he added the wrapper to Makefile and Meson builds earlier. The change adds CMake detection for `writev()` and includes the compatibility implementation when missing, specifically addressing Git for Windows CI build failures.

Junio Hamano acknowledges the patch with a brief note, accepting it while noting he should have caught this omission when reviewing the related Meson changes. The patch follows Git's established pattern for handling missing POSIX functions and is uncontroversial given Schindelin's role as Git for Windows maintainer. The change is minimal - just adding `writev` to the list of checked functions and including the compatibility file when needed. This brings CMake builds in line with the other build systems' handling of this POSIX function.