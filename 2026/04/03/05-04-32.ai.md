This is a follow-up discussion in the graph rendering thread, where Junio C Hamano provides additional context about the multiple root commit visualization problem. The email clarifies that the core issue isn't strictly about root commits, but rather about commits whose parents are excluded from the displayed range (which includes but isn't limited to root commits).

Junio references two key scenarios from prior discussion: one with two independent root commits (A and X) merging at Z, and another where commit A isn't a root but has parents outside the displayed range (C..Z). He emphasizes that the solution needs to handle both cases consistently - the visual treatment should apply whenever a commit's parents aren't shown, regardless of whether it's technically a root commit.

The maintainer notes that Pablo Sabater may have over-attributed the problem discovery to him, clarifying he was simply aware of previous attempts to solve this issue. This suggests the thread may contain valuable historical context about why this graph rendering edge case has persisted.

This email doesn't introduce new code changes but provides important design guidance for the proposed solution. Reviewers will likely want to verify that any placeholder mechanism properly handles both true root commits and commits with excluded parents, maintaining consistent visual spacing in both scenarios. The reference to "gotchas" hints there may be subtle edge cases in the graph rendering logic that have tripped up previous attempts to fix this issue.