This is a follow-up in the thread about improving Git's graph rendering for root commits and excluded parents in `git log --graph` output. Junio C Hamano points out a test case portability issue in Pablo Sabater's RFC patch.

The problem is with the test case using filenames that differ only by case (`8_a` vs `8_A`) - these would be ambiguous on case-insensitive filesystems like Windows and macOS, as shown by CI failures. This is a common pitfall in Git's test suite that needs addressing before the patch can proceed.

The email doesn't discuss the core graph rendering changes from the RFC, focusing instead on this immediate technical blocker. Junio's comment suggests the test case needs revision to use case-unique names while still demonstrating the graph rendering behavior the patch aims to improve.

This appears to be routine patch refinement - the conceptual approach to graph rendering isn't being questioned here, just a filesystem-specific implementation detail in the test code. The thread's broader discussion about handling both root commits and commits with excluded parents remains ongoing, but this particular email is narrowly focused on making the test cases portable.