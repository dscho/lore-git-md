This is the final decision in the `git replay` root commit handling documentation discussion. Christian Couder, as the deciding maintainer, agrees with Junio C Hamano's position that the proposed documentation additions are unnecessary at this time.

The thread concludes that:
1. The technical implementation (merged to `next`) sufficiently handles root commits through the `mapped_commit()` refactoring
2. No additional documentation about root commit behavior will be added now
3. Any future error message improvements can be addressed separately if/when `git replay` gains better error reporting

This maintains Git's lean documentation philosophy where behavior that can be inferred from existing docs (like the general "Specifying Ranges" documentation) doesn't need explicit restatement. The decision shows deference to Junio's preference for minimal documentation while keeping the door open for potential future refinements in error messaging. The exchange exemplifies Git's collaborative decision-making process where maintainers reach consensus through discussion, even when initially holding different views.