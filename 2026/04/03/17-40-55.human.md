This is a review comment on patch 1/3 in Shreyansh Paliwal's series to remove `the_repository` usage from the refs subsystem. Tian Yuchen points out that while the patch correctly adds a `struct repository*` parameter to `get_files_ref_lock_timeout_ms()`, the function still uses static variables for caching the timeout value, which means it doesn't fully support multi-repo scenarios yet.

Yuchen suggests the commit message could be clearer about this limitation, noting that while the patch removes one form of global state (direct `the_repository` usage), another remains (the static cache). He proposes adding a note about the planned follow-up work to move the configuration into the `repo-settings` struct. The tone is constructive - he acknowledges this is a minor issue and leaves it to the author to decide whether to update the message.

This is a small but thoughtful review point about accurately documenting scope in commit messages. The exchange shows the careful attention to detail that characterizes the `the_repository` removal effort, where reviewers are looking not just at the immediate change but at how it fits into the larger architectural goal of proper multi-repo support.