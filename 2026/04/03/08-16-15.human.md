This is the final patch in Christian Couder's series refactoring promisor-remote handling, focusing on test improvements for cross-platform URI formatting. The patch addresses Junio Hamano's feedback by:

1. Renaming the `PWD_URL` variable to `TRASH_DIRECTORY_URL` to more accurately reflect its purpose as a base URL for the test trash directory rather than the current working directory.

2. Expanding the character encoding to handle more than just spaces, now including percent-encoding for `=`, `;`, and `,` characters in paths. The patch also adds a safety check that skips all tests if the path contains any unsupported special characters.

The changes maintain the series' focus on robustness and cross-platform compatibility, particularly for Windows file paths. Christian's response shows careful consideration of Junio's suggestions while keeping the implementation practical. The patch appears ready for merging with these refinements, completing the preparatory work for future promisor-remote auto-configuration features.