Thread subject: [PATCH v2] config: add timeout to config file locking. Thread type: bugfix patch. Originating author: Jörg Thalheim. Core problem: concurrent Git operations fail when writing to config files due to immediate lock contention failures, particularly affecting `git worktree add -b` operations writing branch tracking information.

Current status: v2 patch proposing configurable timeout (default 1000ms) is conceptually accepted and ready for merging to `next`. The patch is procedurally ready for integration; Johannes Schindelin's explicit endorsement removed the last technical obstacle, and Junio C Hamano's earlier confirmation that the patch is otherwise ready for merging remains valid. The sole remaining discussion point—whether to simplify the timeout to be explicitly process-wide (Junio's preference) or retain the current implementation with caching (Patrick Steinhardt's counterproposal)—is now framed as a minor optimization detail deferred to a follow-up patch. Junio's latest email expresses procedural discomfort about the lack of follow-through on the caching tradeoff discussion but does not reopen the design question or propose blocking the patch.

Key technical details: introduces `core.configLockTimeout` following `core.packedRefsTimeout` pattern; documents read-after-lock semantics; adds comprehensive test coverage. The caching behavior makes the timeout effectively process-wide despite the repository parameter, which Schindelin considers acceptable for now. No changes to the patch's core functionality or integration status are implied by Junio's latest comment.

Open questions: none actively blocking. The caching tradeoff is deferred to a future patch; Junio's email serves as a nudge to either submit a follow-up patch or document why the cache is not worth removing, but this is not a prerequisite for merging the current patch.

Participant positions: Jörg Thalheim driving implementation; Junio C Hamano and Patrick Steinhardt engaged in substantive technical review of implementation tradeoffs (process-wide vs per-repo); Johannes Schindelin explicitly supports merging the current implementation to `next` and then `master`, committing to handle Windows-specific adjustments and advocating for deferring cache-related changes. Junio's latest email does not alter prior positions but highlights the unresolved caching debate as a procedural note.

Related work: builds on Microsoft's Scalar-specific solution and packed-refs timeout precedent. Outcome: patch remains procedurally ready for integration.