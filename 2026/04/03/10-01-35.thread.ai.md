Thread subject: [PATCH v2] config: add timeout to config file locking. Thread type: bugfix patch. Originating author: Jörg Thalheim. Core problem: concurrent Git operations fail when writing to config files due to immediate lock contention failures, particularly affecting `git worktree add -b` operations writing branch tracking information on slower filesystems (e.g., NTFS).

Current status: **merged to `next`**. Junio C Hamano has explicitly cleared the patch for merging to `next`; all prior technical and procedural concerns are resolved. The patch is procedurally ready for integration, with no blocking issues. The deferred discussion about simplifying the timeout to be explicitly process-wide (Junio's preference) or retaining the current implementation with caching (Patrick Steinhardt's counterproposal) remains a minor optimization detail for a follow-up patch, as confirmed by Junio's latest email.

Key technical details: introduces `core.configLockTimeout` (default 1000ms) following the `core.packedRefsTimeout` pattern; documents read-after-lock semantics and last-writer-wins behavior; adds comprehensive test coverage for immediate failure and retry scenarios. The caching behavior makes the timeout effectively process-wide despite the repository parameter, which Johannes Schindelin considers acceptable for now. No changes to the patch's core functionality or integration status are implied by recent discussion.

Open questions: none actively blocking. The caching tradeoff is deferred to a future patch; Junio's latest email serves as a procedural confirmation of merge readiness rather than a reopening of design questions.

Participant positions: Jörg Thalheim (author); Junio C Hamano (maintainer, cleared for merging to `next`); Patrick Steinhardt (substantive technical review, advocated for current caching implementation); Johannes Schindelin (explicitly supports merging to `next` and `master`, committed to handling Windows-specific adjustments, deferred cache-related changes). Junio's latest email does not alter prior positions but reaffirms the patch's readiness for integration.

Related work: builds on Microsoft's Scalar-specific solution and the `core.packedRefsTimeout` precedent. Integration status: merged to `next` (date unknown).