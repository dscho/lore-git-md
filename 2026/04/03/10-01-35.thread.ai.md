Thread subject: [PATCH v2] config: add timeout to config file locking. Thread type: bugfix patch. Originating author: Jörg Thalheim. Core problem: concurrent Git operations fail when writing to config files due to immediate lock contention failures, particularly affecting `git worktree add -b` operations writing branch tracking information.

Current status: v2 patch proposing configurable timeout (default 1000ms) after previous rejection, now with explicit maintainer and reviewer support. The patch is conceptually accepted and ready for merging to `next`; the sole remaining discussion point—whether to simplify the timeout to be explicitly process-wide (Junio's preference) or remove caching while retaining the repository parameter (Patrick's counterproposal)—is now framed as a minor optimization detail rather than a blocker. Johannes Schindelin has explicitly endorsed the current implementation, urging the project to proceed without further delay and defer any cache-related refinements to a follow-up patch. Junio's earlier confirmation that the patch is otherwise ready for merging remains valid.

Key technical details: introduces `core.configLockTimeout` following `core.packedRefsTimeout` pattern; documents read-after-lock semantics; adds comprehensive test coverage. The caching behavior makes the timeout effectively process-wide despite the repository parameter, but Schindelin considers this acceptable for now. Open questions: none actively blocking; the caching tradeoff is deferred to a future patch.

Participant positions: Jörg Thalheim driving implementation; Junio C Hamano and Patrick Steinhardt engaged in substantive technical review of implementation tradeoffs (process-wide vs per-repo); Johannes Schindelin now explicitly supports merging the current implementation to `next` and then `master`, committing to handle Windows-specific adjustments and advocating for deferring cache-related changes. Johannes' earlier comment about unit-less naming was supportive, not an objection.

Related work: builds on Microsoft's Scalar-specific solution and packed-refs timeout precedent. Outcome: patch is procedurally ready for integration; Schindelin's endorsement removes the last technical obstacle.