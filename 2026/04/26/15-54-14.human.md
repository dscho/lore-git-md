Review comment on the `--fetch` option for `git checkout`/`git switch`.

Ramsay Jones provides a brief but substantive review of Harald Nordgren's v3 patch adding automatic remote fetching during branch creation. The key points:

1. **Missing test case** - Notes the absence of a test verifying that `--no-fetch` can override the `checkout.fetch` config option (a common pattern in Git commands). This would be important for completeness.

2. **Personal workflow perspective** - Shares that they personally wouldn't use this feature, drawing a parallel to avoiding `git pull` for similar reasons. This echoes the philosophical concerns raised earlier in the thread about combining fetch and checkout operations.

The review is substantive (engaging with the technical implementation) though not tested (no indication of having run the code). It surfaces a real gap in coverage while acknowledging the patch's technical soundness. The comment about workflow preferences reinforces the ongoing debate about whether this functionality belongs in core Git.

The review doesn't fundamentally challenge the implementation but does identify a test case that should be added if the feature proceeds. It comes from a contributor with long experience in Git's portability and low-level behavior, lending weight to the observation.