Thread identity: Documentation bug report for `git check-ignore -z` behavior mismatch between implementation and docs, reported by David Le.

Core problem: The `git check-ignore -z` command currently fails with "fatal: -z only makes sense with --stdin" when used with path arguments, while its documentation suggests `-z` can work independently of `--stdin`. The help text implies `-z` modifies output format generally, with an additional effect on input parsing when combined with `--stdin`.

Current status: Bug report filed with reproduction case showing the behavior mismatch on Git 2.54.0 (Arch Linux). No patches submitted yet - needs documentation or code fix to align behavior with docs.

Key technical details: The `-z` option is documented as affecting output format (NUL termination) but currently only works with `--stdin`. The command fails when `-z` is used with direct path arguments despite documentation suggesting this should work.

Open questions: Should the implementation be changed to match docs (allow `-z` without `--stdin`) or should docs be corrected to reflect current behavior (`-z` requires `--stdin`)? Needs input from documentation maintainers or `check-ignore` experts.

Participant positions: David Le reported the discrepancy through clear reproduction steps. No other participants have weighed in yet.