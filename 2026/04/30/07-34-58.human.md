This is the fourth patch in Johannes Schindelin's 6-part CI/build system update series, bumping the `actions/checkout` dependency from v5 to v6 across all GitHub Actions workflows. The change is primarily motivated by keeping the CI infrastructure on maintained versions, with v6 moving to Node.js 24 like the other updates in this series.

The key behavioral change in v6 is that credentials are now stored in `$RUNNER_TEMP` rather than `.git/config`, improving security by reducing credential leakage risks. Schindelin's analysis confirms this change doesn't affect Git's workflows since they either check out the default repository (no special credentials) or interact with the API via `GITHUB_TOKEN` directly. The patch includes thorough documentation of the change's implications and maintains compatibility with GitHub's hosted environment.

The diff shows purely mechanical version number updates across all workflow files, with no logic changes. This continues the series' pattern of careful, low-risk maintenance updates to keep CI infrastructure current. The patch appears ready for merging as part of the complete series.