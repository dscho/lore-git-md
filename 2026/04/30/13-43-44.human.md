This is a response from Phillip Wood clarifying the behavior of `git rebase -i` when rewording multiple commits. He confirms that the sequential processing of reworded commits is the intended behavior, not a regression, and explains the rationale.

Key points:
- The current behavior (processing reworded commits one at a time) is working as designed
- This allows viewing changes for each commit being edited, especially useful with `commit.verbose` enabled
- Mentions `git-revise` as an alternative tool that may support batch editing of commit messages
- The response suggests the reporter's expectations may have been shaped by third-party tools rather than Git's native behavior

Phillip Wood is a known expert on Git's sequencer and interactive rebase machinery, so his confirmation of this being expected behavior carries weight. The response doesn't address potential Windows-specific aspects raised in the original report, focusing instead on the core behavior being correct.