This is a CI/build system update series (v3) from Johannes Schindelin, expanding the GitHub Actions dependency updates to include the Windows SDK setup action. The series now consists of 6 patches, adding an update to `git-for-windows/setup-git-for-windows-sdk` (v1 to v2) while maintaining all previous updates.

The changes originate from Dependabot's automated version checks in Git for Windows' fork, but Schindelin has carefully augmented each change with detailed commit messages explaining the updates. The new v3 adds the Windows SDK setup action update while keeping all previous changes intact (as shown by the range-diff indicating identical patches 1-4 and 6).

The series systematically updates versions across all workflow files in `.github/workflows/`, now covering:
- microsoft/setup-msbuild (v2->v3)
- actions/upload-artifact and download-artifact (v7->v8)
- actions/github-script (v8->v9)
- actions/checkout (v5->v6)
- git-for-windows/setup-git-for-windows-sdk (v1->v2)
- mshick/add-pr-comment (v2->v3)

The updates remain mechanical version bumps with no workflow logic modifications, focused on keeping CI infrastructure current. The Windows SDK update in particular moves to Node.js 24, aligning with the series' pattern of updating to supported versions. Schindelin's thorough commit messages continue to provide valuable context about security implications and compatibility requirements for each change.

This appears to be routine, low-risk maintenance to keep CI system dependencies up-to-date. The series demonstrates careful attention to backward compatibility and security considerations while reducing technical debt. The addition of the Windows SDK update in v3 completes the set of GitHub Actions used by Git's CI system.