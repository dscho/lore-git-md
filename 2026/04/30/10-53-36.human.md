This is a documentation patch completing a series that fixes HTTP authentication behavior, specifically around Kerberos (SPNEGO) authentication with the `http.emptyAuth` configuration.

Matthew John Cheetham follows up on Junio C Hamano's suggestion to clarify the documentation for `http.emptyAuth`. The patch explicitly lists the three possible values (`auto`, `true`, `false`) and describes each one's behavior in detail. The default `auto` mode now clearly explains it only sends empty credentials when the server requests specific auth mechanisms like GSS-Negotiate, while `true` always sends empty credentials and `false` never does.

This documentation update complements the technical fixes in the series, which Junio has already reviewed positively. The changes are straightforward and uncontroversial, matching the implementation's actual behavior. With this addition, the series appears complete and ready for merging. The patch demonstrates good attention to user-facing documentation, ensuring the configuration's behavior is transparent to users.