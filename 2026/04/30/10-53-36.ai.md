This is a documentation patch completing the HTTP authentication bugfix series. Matthew John Cheetham follows up on Junio C Hamano's suggestion to clarify the `http.emptyAuth` configuration option's behavior. The patch explicitly documents the three valid values (`auto`, `true`, `false`) with clear descriptions of each mode's authentication behavior.

The documentation explains that `auto` (the default) only sends empty credentials when required by specific mechanisms like GSS-Negotiate, while `true` always sends them on first request and `false` never does. This matches the technical implementation from the earlier patches in the series, which Junio has already reviewed positively. The patch serves as the final piece to make the configuration's behavior transparent to users.

With this addition, the series appears complete - the technical fixes are in place, test coverage exists, and now the documentation accurately reflects the behavior. The change is straightforward and uncontroversial, building on the already-approved technical work.