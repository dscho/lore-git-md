This is a refactoring patch from Kristoffer Haugsbakk, part of the v3 series introducing the new `git format-rev` command. The patch makes a minor but correct style adjustment to `builtin/name-rev.c` to align with Git's coding guidelines by adding braces around both arms of conditional statements where one arm already required them.

The changes are straightforward:
1. Wraps the `if (!n->generation)` block in braces to match the `else` block
2. Similarly braces the `if (!ishex(*p))` condition to match its `else if` counterpart

This is purely a code style improvement with no behavior change, preparing the codebase for the subsequent patches in the series that will refactor and share this logic with the new `format-rev` command. The patch follows the project's convention of keeping conditional blocks consistently braced even when single-line, as documented in `CodingGuidelines`.

The change is small and mechanical, with the diff showing exactly what's being modified. The patch includes a proper commit message referencing the relevant coding guideline and carries forward the series notes from v2 about fixing stray formatting. This is a clean preparatory change for the larger architectural work coming in later patches of the series.