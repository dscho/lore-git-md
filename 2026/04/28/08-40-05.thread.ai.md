Here is the dense AI thread summary capturing this new information:

This thread discusses improving tarball reproducibility and verification for Debian packaging workflows. Simon Richter from Debian explains their current challenges with Git-generated tarballs (via `git archive`), which are only reproducible by accident. Debian requires verifiable original tarballs for packages. Simon has created a tool that generates tree checksums for verification, but it has two key limitations: it requires access to the original commit object (not included in tarballs) and doesn't properly handle submodules.

Two main technical solutions are proposed: 1) Adding commit objects and signed tags as ancillary files to the tarball, and 2) Extending `git archive` to include this verification information in pax headers. For submodules, the workaround of concatenating multiple archives loses metadata, so Simon suggests Git could be extended to create archives that properly include submodule information while maintaining verifiability.

The open question is whether these changes should be implemented in Git itself or handled within Debian's tooling. This intersects packaging workflows, cryptographic verification, and Git's archive functionality - specifically where Debian's needs for deterministic builds meet Git's capabilities. Key technical details involve the `git archive` command, pax headers, submodule handling in archives, and commit object inclusion. The thread currently has one participant (Simon Richter) presenting the problem and proposed solutions, awaiting feedback from Git contributors about implementation direction.