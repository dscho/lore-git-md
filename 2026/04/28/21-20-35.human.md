This is a technical discussion about improving tarball reproducibility in Git's `git archive` functionality, with brian m. carlson responding to Theodore Tso's practical concerns. The thread has evolved from Simon Richter's initial Debian packaging concerns to broader implementation challenges.

brian m. carlson outlines several key obstacles to reproducible tarballs: the inherently variable nature of `export-subst` (which embeds short object IDs), potential inconsistencies from filters (like those inserting timestamps), and external compression tools beyond Git's control. They propose a versioned approach where Git could offer explicitly reproducible formats (v1, v2 etc.) that would disable problematic features like `export-subst` and custom filters.

The response highlights deeper technical constraints - particularly around compression (Git can't guarantee identical gzip/zlib output across platforms) and zip file timezone handling (which caused issues at their workplace). brian m. carlson mentions having partial Rust-based solutions in progress but notes complexities around file ordering with `--add-file` options. The tone suggests this is a known challenge that needs dedicated implementation work rather than quick fixes.

This moves the thread forward by clarifying what would be required for stronger reproducibility guarantees while tempering expectations about absolute bit-for-bit consistency, especially when external tools are involved. The response maintains focus on technical constraints rather than revisiting earlier discussions about Debian's specific needs or Theodore Tso's workflow.