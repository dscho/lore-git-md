This is a feature discussion email in the ongoing thread about adding new revision walk behavior. Junio C Hamano weighs in on the naming debate for the `--reverse=before` functionality, strongly preferring Johannes Sixt's suggested `--max-count-oldest` approach over the current proposal.

Junio clarifies the conceptual model he prefers: `--max-count-oldest=3` would first collect all matching commits (applying filters like `--grep` or `--author`), then keep only the oldest 3 commits before displaying them (in normal or reversed order if `--reverse` is also given). This differs from the current `--reverse=before` approach which reverses first, then applies limits.

The email signals Junio's position that the functionality should be framed as a modification to `--max-count` behavior rather than as a variant of `--reverse`. His preference aligns with Johannes Sixt's earlier suggestion and suggests the maintainer may push for a different interface direction than what the patch series currently implements.

This moves the discussion beyond implementation details (which are technically complete in v4) into fundamental questions of command-line interface design. Junio's position carries significant weight as maintainer, though he hasn't yet explicitly rejected the current approach. The thread appears to either pivot toward a new interface direction or require justification for keeping the current `--reverse=before` naming.