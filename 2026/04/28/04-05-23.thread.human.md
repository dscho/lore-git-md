This thread has successfully concluded with the merge of Michael Montalbo's series integrating Git's line-log functionality (`-L`) with the standard log output pipeline. The three-patch series went through multiple iterations, with the final version now merged into the `next` branch after addressing all technical feedback.

The changes unify line-log output processing with Git's standard diff pipeline while carefully maintaining range-specific behavior. The series provides consistent support for metadata formats like `--raw`, `--name-only`, and `--name-status` while properly blocking unsupported stat formats. Key technical changes included refactoring the line-log printing logic, removing an early return in the commit processing, and adding comprehensive test coverage.

D. Ben Knoble provided positive final review, noting the code structure and rationale appear sound, though he didn't scrutinize the tests in detail. The only change in the final version was a test syntax adjustment (switching from `! test_grep` to `test_grep !`). Junio C Hamano merged the series, marking the successful conclusion of this effort to better integrate line-log functionality with Git's core output pipeline.