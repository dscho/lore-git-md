This is a style nit from Junio C Hamano on the second patch of Mirko Faina's series introducing the `--reverse=before` option for revision walking. The patch implements a memory optimization that uses a sliding window approach when both `--reverse=before` and `--max-count` are specified.

Junio points out a minor coding style issue: the `if/else` block in the patch has unnecessary curly braces around single-statement bodies. This is a straightforward cleanup that follows Git's coding guidelines, which prefer omitting braces for single-line statements when possible.

The comment is purely about code style and doesn't affect functionality - the core optimization using `retrieve_with_window()` remains technically sound. This suggests the patch series is nearing merge readiness, with only minor polish items remaining after four iterations of review and refinement. The thread shows the feature has already addressed more substantive feedback about performance characteristics and edge case handling.