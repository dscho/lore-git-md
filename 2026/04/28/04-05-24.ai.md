This is the first patch in a 3-part series refactoring `git log -L` (line-log) to integrate with Git's standard diff output path. The patch is a preparatory change that moves the line-log setup code earlier in `setup_revisions()` so that its default DIFF_FORMAT_PATCH setting will be visible when `revs->diff` is computed.

The change is mechanical - it enables the core refactoring in patch 2 by ensuring the line-log's default output format is properly registered before the diff pipeline checks take effect. The commit message clearly explains why this reordering is necessary for the upcoming changes, while noting there's no behavior change in this patch alone.

This appears to be a well-scoped preparatory change that follows Git's convention of splitting refactoring into logical steps. The patch is small (16 lines moved) and focused solely on enabling the subsequent improvements to line-log integration.