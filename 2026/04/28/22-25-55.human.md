This is a test patch (4/5) in Kristoffer Haugsbakk's series introducing the new `git format-rev` command. The patch adds a dedicated test case for the `--annotate-stdin --name-only` option combination in `git name-rev`, which was recently refactored to use a new command abstraction.

The test ensures proper handling of name-only output when processing stdin, catching potential regressions in the refactored code path. It verifies that the output matches what `git name-rev --name-only` produces when run individually on each commit. The test follows the established pattern in the test suite, using sorted output to handle non-deterministic ordering.

This is a straightforward but important addition to maintain test coverage as the code evolves. The patch is well-contained and clearly motivated by the recent architectural changes in the series. The implementation looks correct and follows Git's testing conventions.