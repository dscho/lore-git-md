This is a final review ack for a lockfile cleanup patch in the long-running autostash feature series for `git checkout -m`. Phillip Wood confirms Harald Nordgren's patch properly adds `rollback_lock_file()` calls to all early return paths in `merge_working_tree()`, addressing a minor resource cleanup issue that could have caused problems if the function were called multiple times in the same process.

The change is straightforward and uncontroversial, representing the final polish on a well-tested feature that has already been through 20 iterations of development. Phillip's sign-off indicates this completes all technical work for the series, though he had previously noted an architectural consideration about stash message handling that could be improved in future work. The patch follows Git's pattern of careful resource cleanup even in error paths, maintaining existing behavior while making it more robust.