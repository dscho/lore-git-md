This is a technical discussion about tarball reproducibility in Git's `git archive` functionality, continuing the thread about packaging workflows. brian m. carlson responds to Theodore Tso's practical concerns with a broader architectural analysis of the challenges.

The email outlines several key reproducibility obstacles: the inherently variable nature of `export-subst` (which embeds short object IDs), compression inconsistencies (since Git doesn't control external compressors like gzip), and timestamp handling in zip files. brian m. carlson proposes a versioned approach where Git could offer explicitly reproducible formats (v1, v2 etc.) that would disable non-deterministic features like `export-subst` and certain filters. They note this would be easier to implement in Rust due to better data structures for handling file insertion order.

The response connects to Theodore Tso's specific workflow by acknowledging the compression parameter issues he raised, while expanding the discussion to include zip file timestamp problems encountered in containerized environments. brian m. carlson's perspective as a Git core contributor adds weight to the assessment that true reproducibility would require deliberate design choices rather than incidental properties of the current implementation. The email maintains the thread's technical depth while shifting focus slightly toward potential solutions rather than just documenting current limitations.