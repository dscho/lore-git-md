This is a feature discussion email in the `--reverse=before` thread, where Junio C Hamano weighs in on alternative interface designs for controlling when limiting occurs in revision walks. The email marks a shift from implementation details to interface philosophy.

Junio strongly prefers Johannes Sixt's earlier suggestion of `--max-count-oldest` over the current `--reverse=before` approach, arguing it better captures the conceptual model of "apply limits late" rather than "reverse early". He provides concrete examples showing how `--max-count-oldest=3` would naturally compose with `--reverse` to produce different output orders from the same commit selection.

The email reveals a fundamental disagreement about whether the new behavior should be framed as a modification of reversal timing (Mirko's current implementation) or as a variant of limit application (Junio/Johannes' preference). Junio's position suggests he views the core innovation as being about when we count commits for limiting purposes, not about when we reverse them.

This intervention from the maintainer carries significant weight and may prompt a redesign of the feature's user interface in a future version, though the underlying technical implementation (sliding window optimization, state machine changes) would likely remain similar. The thread now faces a key decision point between maintaining the current technically-accurate-but-less-intuitive names versus adopting terminology that better matches user mental models at the cost of being less precisely aligned with implementation details.