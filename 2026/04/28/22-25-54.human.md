This is a refactoring patch (part 3/5) in Kristoffer Haugsbakk's series introducing the new `git format-rev` command. The patch factors out common code from `name-rev.c` that will be shared with the upcoming `format-rev` command.

The key changes:
- Introduces a `command` struct with a tagged union to handle command-specific data (currently just `name_rev`'s name-only flag)
- Extracts the revision parsing logic into a more generic form that can be reused
- Maintains all existing functionality while preparing for the new command
- Uses a switch statement to handle different command types, with a BUG() for unexpected cases

This is a mechanical preparation step - the actual `format-rev` command comes in later patches. The changes are cleanly separated and well-contained, showing good attention to the incremental refactoring approach common in Git development. The patch follows the project's coding style and includes tests (though they're not shown in this diff).

The refactoring looks solid and purposeful, clearly setting up for the new functionality while keeping the existing behavior intact. This continues the series' pattern of careful architectural evolution based on earlier review feedback.