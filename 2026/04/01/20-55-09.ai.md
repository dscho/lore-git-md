This is a feature patch series for the `git replay` plumbing command, introducing the `--ref` option to control which reference gets updated during replay operations. The v3 series from Toon Claes addresses a SIGABRT crash in merge-ort that was blocking the previous version, while maintaining the core functionality that allows precise control over ref updates.

The key changes in v3 focus on stabilization:
- Fixes a crash by properly initializing the merge_opt struct to zero
- Clarifies in documentation that `--ref` requires fully qualified refs (not just branch names)
- Maintains all previous functionality from v2 including:
  - Explicit non-negatable options (PARSE_OPT_NONEG)
  - Standardized "stuck form" documentation
  - Validation for single-tip requirements with `--onto` and `--ref`

The implementation continues to modify `replay.c` and `builtin/replay.c` to handle the new `--ref` option while maintaining backward compatibility. Test coverage in `t3650-replay-basics.sh` verifies both the new option and validation logic. The changes appear well-considered, with the crash fix addressing the immediate blocking issue while preserving the feature's core value of giving users precise control over ref updates during replay operations.

For future summarizers: The key technical details remain the validation rules for `--ref` usage and its interaction with other options like `--onto`. The v3 changes add proper initialization of merge_opt to prevent crashes while maintaining all existing functionality. The documentation now explicitly requires fully qualified refs with `--ref`. The series builds on Siddharth Asthana's earlier `--revert` work (ba5c0d03d3) and follows Git's standard practices for option handling.