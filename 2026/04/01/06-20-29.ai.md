This is a test modernization patch that updates the `t7004-tag.sh` test script to use Git's built-in test helpers instead of shell pipelines with `wc -l`. The changes address two issues: better error detection (since the old approach would miss Git failures) and improved test output on failures.

The patch replaces several instances of `test $(git tag | wc -l) -eq 0` with either `test_must_be_empty` (for empty output) or `test_line_count` (for counting lines). These helpers are part of Git's test infrastructure and provide more reliable failure detection by properly checking Git's exit code through the test's && chain. They also give better diagnostic output when tests fail by showing the actual file contents.

The changes are mechanical but improve the test's robustness and debuggability. The patch follows Git's test modernization conventions and appears well-scoped, touching only the intended test file with straightforward substitutions. The commit message clearly explains the motivation and benefits of the changes.