Test modernization patch updating `t7004-tag.sh` to use Git's built-in test helpers (`test_must_be_empty` and `test_line_count`) instead of shell pipelines with `wc -l`. The changes improve error detection by properly checking Git's exit code through the test's && chain and provide better diagnostic output on failures. The patch is mechanical and well-scoped, following Git's test modernization conventions by replacing `test $(git tag | wc -l) -eq 0` patterns with the more robust test infrastructure helpers. The commit message clearly explains the motivation (better failure detection and improved test output) and benefits of the changes.