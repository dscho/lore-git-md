This is a review comment on Jeff King's const-correctness patch series. Patrick Steinhardt points out a minor terminology correction in patch 3/12, noting that the commit message should say "NUL" (the null character) rather than "NULL" (the null pointer) when describing writing a zero byte into the string.

The comment is brief and focused - it doesn't challenge the technical approach of the patch (which removes const from a variable that needs to be modified via strchr()) but rather fixes an imprecise wording in the explanation. This is typical Git mailing list practice for small documentation fixes during review.

The exchange shows the series is receiving careful scrutiny, even for minor details, from experienced contributors like Patrick Steinhardt. The thread context indicates this patch is part of a larger effort to address C23 compatibility warnings about const-correct string handling across Git's codebase.