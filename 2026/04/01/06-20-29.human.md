Test modernization patch improving tag-related tests in t7004-tag.sh. Siddharth Shrimali replaces several instances of `wc -l` with Git's built-in test helpers `test_must_be_empty` and `test_line_count`. 

The changes address two issues: first, the original pattern using `wc -l` would silently ignore Git's exit code if the `git tag` command failed, and second, it created unnecessary subshell overhead. The new approach captures output to files and uses dedicated test helpers that provide better diagnostics on failure while maintaining proper error checking through the && chain.

The patch touches four test cases, converting five `wc -l` checks to use the more robust test infrastructure. This appears to be a straightforward improvement following Git's ongoing effort to modernize its test suite with better practices. The changes are focused and well-explained in the commit message.