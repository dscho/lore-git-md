This is a feature patch in the `git replay` command series, specifically addressing option handling. The patch marks several `git replay` options (`--onto`, `--advance`, `--revert`, and `--ref-action`) as non-negatable using `PARSE_OPT_NONEG`, which prevents users from trying to negate them (e.g., `--no-onto`). This is the first of three patches in v3 of the series, which primarily fixes a SIGABRT crash from v2 while maintaining the core functionality.

The change is straightforward - it converts existing `OPT_STRING` declarations to `OPT_STRING_F` with the `PARSE_OPT_NONEG` flag, ensuring consistent behavior with Git's option parsing conventions. The patch maintains all existing functionality while making the non-negatable nature of these options explicit in the code. This aligns with the series' goal of improving the robustness and clarity of the `git replay` command's option handling.

For future summarizers: This patch introduces no new functionality but hardens option parsing. The key detail is the use of `PARSE_OPT_NONEG` for these specific options, which are inherently non-negatable by design. The change is mechanical but important for maintaining consistent command behavior. The patch follows the pattern established in the parent series for precise ref updates in `git replay`.