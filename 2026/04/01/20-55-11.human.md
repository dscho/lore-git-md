This is a documentation patch in Toon Claes' ongoing series to enhance the `git replay` command with a `--ref` option. The patch focuses on standardizing the command's documentation to use Git's preferred "stuck form" for options (e.g., `--onto=<newbase>` instead of `--onto <newbase>`), as recommended in gitcli(7). 

The changes are mechanical but important for consistency:
- Updates all option documentation in both the man page and built-in help to use `--option=value` syntax
- Reorders some documentation sections to match the logical flow
- Maintains all existing functionality while making the interface more idiomatic

This is part of the v3 series that fixes a SIGABRT crash from v2 while preserving the core `--ref` functionality. The patch follows Git's documentation conventions and doesn't introduce any behavioral changes - it's purely about presentation and consistency. The author has clearly studied gitcli(7) and applied its recommendations systematically.