This is the first patch in a 3-part series improving the `git replay` plumbing command. The patch marks several command-line options (`--onto`, `--advance`, `--revert`, and `--ref-action`) as non-negatable using the `PARSE_OPT_NONEG` flag, which prevents users from trying to negate them (e.g., with `--no-onto`). 

The change is straightforward and follows Git's standard practices for option handling. The patch maintains all existing functionality while making the command's behavior more explicit and consistent. This is part of a larger effort to stabilize and document the `git replay` command, particularly around its new `--ref` option for precise reference updates.

The patch appears well-crafted - it's a focused change with a clear commit message and proper sign-off. It builds on previous work in this area while addressing specific feedback about option handling. The diff shows the mechanical conversion of the option declarations to use `PARSE_OPT_NONEG` while keeping all other parameters the same.