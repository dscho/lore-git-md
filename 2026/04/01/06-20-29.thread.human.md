This thread introduces a test modernization patch for `t7004-tag.sh`, converting shell pipeline-based tests to use Git's built-in test helpers. The changes replace `git tag | wc -l` patterns with more robust alternatives like `test_must_be_empty` and `test_line_count`, which provide better error detection and failure diagnostics. The patch is focused and mechanical, following Git's established test modernization conventions while improving the script's reliability. The commit message clearly explains the benefits of these changes for both test accuracy and debugging.