Here is the dense, loss-free AI thread summary for future agent sessions:

This is a 3-patch series from Chen Linxuan introducing new `includeIf` conditions for Git config files targeting multi-worktree setups. The first patch adds `worktree:<pattern>` and `worktree/i:<pattern>` conditions to match the realpath of the current worktree's working directory against glob patterns (case-sensitive and insensitive variants). The motivation is that in multi-worktree setups, linked worktrees' `.git` directories point to the main repository's `.git/worktrees/`, making existing `gitdir` patterns cumbersome when matching based on working tree paths. The implementation refactors `include_by_gitdir()` into a more generic `include_by_path()` that handles both gitdir and worktree paths. Key technical details: uses `repo_get_work_tree()` for path resolution, follows existing gitdir pattern matching logic (realpath, globs, symlinks), returns false for bare repos/early config reading, and modifies only `config.c`'s include condition handling. The patch is well-structured with clear commit message explaining problem and solution, building on existing code patterns rather than introducing new ones.