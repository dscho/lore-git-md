This is a follow-up in the thread about fixing a commit signing regression for non-UTF-8 messages. The regression caused signatures to become invalid when commit messages contained non-ASCII characters without explicit encoding configuration.

brian m. carlson responds to Elijah Newren's review of their patch, agreeing with two points about the implementation:

1. They acknowledge the potential for duplicate UTF-8 warning messages when both the main buffer and compatibility buffer contain invalid UTF-8, and commit to fixing this in v2 of the patch.

2. They confirm that the patch does indeed fix a logical short-circuiting issue where the original code would only process the first invalid buffer due to the `||` operator behavior.

The exchange shows the patch author engaging constructively with substantive review feedback, accepting the technical observations and planning improvements. This continues the thread's focus on ensuring robust UTF-8 handling in commit signing while maintaining signature validity across different message encodings.

The interaction demonstrates the typical Git development pattern where careful review catches edge cases even in well-motivated fixes, and authors incorporate feedback in subsequent iterations. The core solution remains sound, with these being refinements to warning behavior and error handling paths.