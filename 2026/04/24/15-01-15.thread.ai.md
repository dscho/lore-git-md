Thread identity: Test infrastructure patch series about handling `.gitconfig` in test output, authored by Johannes Scholein (Git for Windows maintainer). Current patch is 6/8 in series.

Core problem: Global `safe.bareRepository=all` setting in test-lib.sh creates `$HOME/.gitconfig` that appears in `git ls-files --others` output unexpectedly, since that command doesn't consult `.git/info/exclude` by default.

Current status: Patch adds `test_filter_gitconfig` helper to strip `.gitconfig` entries from test output, applied to several test files checking `ls-files --others`. For t7104, switches to using `--exclude-standard` which respects exclude file. Changes are mechanical but necessary for test reliability.

Key technical details: Touches test-lib.sh (new helper function) and multiple test files (t0000, t0001, t1014, t1301, t7104). Changes involve output filtering and `--exclude-standard` flag usage. Windows-relevant due to author's focus area.

Participant positions: Johannes Schindelin as author driving the change. No other participants mentioned yet in this patch.

Related work: Part of larger 8-patch series about test infrastructure improvements regarding `.gitconfig` handling.