This is a test fix patch in a series (2/8) that addresses interference from global Git config files during XDG configuration testing. The issue occurs when `$HOME/.gitconfig` exists (which test-lib.sh may create for other purposes), causing Git to prefer that location over the XDG config path being tested.

The fix modifies two test cases in t7900-maintenance.sh to override HOME to a non-existent directory within the test subshells, ensuring the XDG config location is properly isolated and tested. This prevents the global config from affecting the test results while maintaining the original HOME value outside the test scope.

The patch demonstrates good test hygiene by:
1. Clearly explaining the problem and solution in the commit message
2. Using test_when_finished to clean up
3. Maintaining the existing test structure while adding the necessary isolation

This appears to be part of a larger maintenance-related effort (likely the 8-part series mentioned in the subject), though no cover letter is provided for additional context about the series' overall goals. The change is focused and well-contained to the test isolation issue.