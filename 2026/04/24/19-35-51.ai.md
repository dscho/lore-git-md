This is a review comment from brian m. carlson on D. Ben Knoble's per-worktree ignore patterns patch. The review suggests expanding the scope beyond just ignore files to include other worktree-specific info files like `info/attributes` and `info/sparse-checkout` under a more general configuration option like `extensions.worktreeInfoFiles`.

The review is substantive, proposing an architectural alternative that would handle multiple types of worktree-specific configuration files through a single unified mechanism rather than adding separate options for each file type. This reflects a concern about future extensibility and consistency in the configuration namespace.

brian acknowledges they should have raised this idea earlier in the discussion. The tone is constructive, framing the suggestion as a potential improvement rather than a criticism of the current approach. The proposal would require significant changes to the patch's design if adopted, moving from a focused ignore-file solution to a more general worktree-info-file framework.

This feedback will likely prompt discussion about whether to:
1. Proceed with the current ignore-specific implementation
2. Expand to a more general solution now
3. Keep the current approach but design it to accommodate future generalization

The review doesn't identify any technical flaws in the current implementation, focusing instead on the higher-level design question of how to handle worktree-specific configuration going forward.