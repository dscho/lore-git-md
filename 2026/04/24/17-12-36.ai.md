Feature patch adding a `--fetch` option to `git checkout` and `git switch`, with a new review comment from D. Ben Knoble.

This is a follow-up in the thread proposing automatic remote fetching during branch creation. D. Ben Knoble provides a brief but substantive review note, pointing out that users who accidentally create branches from stale remote refs can recover using `git pull --rebase` rather than needing to start over. While not a criticism of the feature itself, this observation suggests the problem being solved may have existing workarounds.

The reviewer expresses general approval of the convenience factor ("I could certainly see this being convenient") while explicitly declining to comment on the implementation details at this time. This positions the feedback as more of a workflow consideration than a technical review, leaving the door open for others to weigh in on the code itself.

The exchange maintains the thread's focus on improving the branch-from-remote workflow, with the new comment adding perspective about alternative solutions to the stated problem. The core proposal remains unchanged - this is neither an endorsement nor rejection, but rather a contextual note about existing capabilities.