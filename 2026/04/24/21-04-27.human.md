This is a bugfix for `git grep`'s column numbering when using `--only-matching`. The patch addresses an issue where subsequent matches on the same line were showing incorrect column positions, calculating them relative to the previous match end rather than the line start.

René Scharfe provides a clear fix that makes Git's behavior match GNU grep's expected output. The change modifies `grep.c` to track absolute positions from the line start and updates the test suite to verify the corrected behavior. The patch includes a thorough demonstration showing before/after output for a test pattern, making the improvement immediately visible.

The fix is straightforward and well-contained, touching only the necessary logic in the match display code. It has already gathered positive feedback in the thread, with Phillip Wood confirming the current behavior appears unintentional. This should be a non-controversial improvement that makes `git grep --column` more reliable for editor integrations and other tools that parse its output.