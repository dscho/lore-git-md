This is a final refinement patch in a long-running series that added autostash functionality to `git checkout -m` with conflict resolution customization. The patch adds a "silent" mode for autostash creation, allowing callers to suppress the "Created autostash" message when needed.

The changes:
- Adds a `silent` parameter to `create_autostash_internal()`
- Introduces `create_autostash_ref_silent()` for silent stash creation
- Updates callers in `builtin/merge.c` to use the new interface
- Maintains backward compatibility by defaulting to non-silent behavior

This is part of the post-merge cleanup after the main functionality was already merged. The patch is small and focused, with clear mechanical changes that follow the established pattern of the series. The implementation looks solid and maintains all existing behavior while adding the requested capability.

The patch represents the final polish on a feature that went through extensive review (18 iterations total) and demonstrates Git's thorough post-merge refinement process. No further discussion is expected on this particular change.