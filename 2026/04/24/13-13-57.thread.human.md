Here's the updated human-readable summary:

The thread began with Christian König reporting what appeared to be a bug - the `sendemail.reply-to` configuration wasn't working, though the `--reply-to` command line option did. Ramsay Jones clarified this wasn't actually a bug since the config option was never implemented in the first place, and the documentation correctly reflects this.

The discussion then shifted to whether this gap should be filled by adding proper config file support. Tian Yuchen has now contributed a practical workaround: using the existing `format.headers` setting to directly inject a Reply-To header, which achieves the same end result through different means. This solution leverages functionality that's already there for handling custom headers.

While the workaround provides a viable path forward, the core question remains open - should Git add first-class config file support for reply-to addresses to match other send-email options? The thread now has both a clear explanation of the current behavior and a functional solution, leaving the project to decide whether to maintain the status quo with documented workarounds or extend the configuration interface.