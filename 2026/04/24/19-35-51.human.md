This is a review comment from brian m. carlson on D. Ben Knoble's patch introducing per-worktree ignore patterns. The review suggests expanding the scope beyond just ignore files to include other worktree-specific info files like `info/attributes` and `info/sparse-checkout` under a more general `extensions.worktreeInfoFiles` config option.

The review is substantive - it engages with the technical approach rather than just surface-level issues, proposing an alternative design that would cover more related functionality. However, it's not a tested review (no indication the reviewer ran the code) and comes late in the process (after the patch was already sent).

The suggestion would significantly broaden the patch's scope from just ignore patterns to multiple worktree configuration files. This could either be seen as a helpful expansion of the concept or as scope creep, depending on whether the additional cases share enough implementation logic with ignore patterns to justify combining them. The review doesn't provide implementation details about how the generalization would work.