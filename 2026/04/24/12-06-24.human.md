This is a substantive review from Toon Claes on patch 7/9 of Karthik Nayak's ref backend refactoring series. The patch moves object validation logic from individual backends to the generic refs layer, which Toon generally supports but has two specific technical questions about the implementation.

First, Toon suggests using `tx_err` as the variable name in receive-pack.c (matching the convention in update-ref.c) rather than `err_type` for consistency. This is a minor naming suggestion that doesn't affect functionality.

More substantively, Toon questions the addition of `!new_target` in the object validation condition, noting that `!is_null_oid(new_oid)` might already cover the same case. This gets at the heart of the validation logic - whether the condition is correctly handling all cases where validation should be skipped. The review maintains the constructive tone typical of the thread while focusing on precise technical details that could affect the patch's correctness.

This continues the pattern of thorough technical review from Toon Claes in this series, following his earlier engagement on error classification approaches. The questions are specific enough that Karthik will likely need to either adjust the code or explain the reasoning behind the current implementation.