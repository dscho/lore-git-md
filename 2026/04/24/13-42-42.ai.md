This is a reply in the `git send-email` reply-to configuration thread. Ramsay Jones points out that the `reply-to` setting under `[sendemail]` in the config file is not actually a valid configuration variable - it's not documented in the man page and appears to be treated as an internal/automatic variable in the source code.

The response clarifies that while the `--reply-to` command line option exists and works, there is no equivalent config file setting, which explains why Christian König's attempt to configure it didn't work. This appears to be a documentation/expectation mismatch rather than a bug in the functionality.

The exchange helps narrow down the issue - the behavior Christian observed is expected because the config option he tried doesn't actually exist as a supported configuration. The thread now has a clear next step: either document that this must be set via command line, or consider adding proper config file support for the reply-to address. Ramsay's response is technically accurate but doesn't propose a solution, leaving the clarification as useful information for anyone else who might expect this configuration to work.