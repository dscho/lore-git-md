This is a bug report about `git send-email` configuration behavior. Christian König reports that while the `--reply-to` command line option works as expected, setting the same value in `.gitconfig` under `[sendemail] reply-to` has no effect. The goal is to ensure replies go to his AMD email address rather than his Gmail sending address.

The issue appears to be a discrepancy between command-line and config file behavior for the reply-to functionality. This could be either a documentation issue (if the config option is named differently than expected) or an actual bug in the config parsing logic. The report is clear and provides the relevant configuration snippet, making it easy to reproduce.

No thread context was provided, so this appears to be the initial report. The next steps would likely involve someone verifying whether this is a known issue or a regression, and whether the config option name has changed or the functionality is broken. The problem touches the email-sending plumbing that many contributors rely on for patch submission workflow.