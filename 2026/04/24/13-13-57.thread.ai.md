Thread identity: Bug report and discussion about `git send-email` configuration behavior regarding reply-to addresses, now considering both direct config support and workarounds.

Core problem: Original report noted `--reply-to` command line option works while `[sendemail] reply-to` config setting has no effect. Ramsay Jones clarified this is expected since no such config option exists. Thread has evolved into evaluating whether to add config support or document workarounds.

Current status: Tian Yuchen proposed a functional workaround using `format.headers` to inject Reply-To header, providing equivalent functionality without code changes. The core question of whether to implement proper `sendemail.reply-to` config support is still open.

Key technical details: Two approaches now under consideration:
1. Native config support would require adding `sendemail.replyTo` parameter matching `--reply-to` behavior
2. Workaround uses existing `format.headers = "Reply-To: <address>"` to achieve same effect
Documentation currently only describes command-line `--reply-to` option.

Open questions: Should the project:
- Add native `sendemail.replyTo` config for consistency with other options?
- Document the `format.headers` workaround as preferred solution?
- Do nothing, maintaining current behavior where only CLI option exists?
Need to weigh implementation cost against user benefit and configuration consistency.

Participant positions: 
- Christian König (AMD) reported original issue
- Ramsay Jones provided authoritative clarification about current code behavior
- Tian Yuchen contributed the `format.headers` workaround solution
No maintainer input yet on preferred direction.

Related work: Connects to broader patterns in `git send-email` configuration:
- Some options have both CLI and config equivalents (e.g. `sendemail.from`)
- Others exist only as CLI flags
- `format.headers` is established mechanism for custom header injection
Decision here may set precedent for handling similar cases.