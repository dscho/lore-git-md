This is a response to a bug report about `git send-email` configuration. Ramsay Jones points out that the `reply-to` setting in the `[sendemail]` section of `.gitconfig` is not actually a valid configuration variable - it's not mentioned in the man page and appears to be intended only as a command-line option. 

The response clarifies that what Christian König was trying to do (setting reply-to address in config) isn't supported, explaining why his configuration wasn't having any effect. This appears to resolve the original question by identifying it as a documentation/expectation issue rather than a bug in the implementation.

The exchange highlights a common pitfall where command-line options don't always have direct config file equivalents. Ramsay Jones' note about the variable being marked in source code as automatically filled or prompted for suggests this behavior is intentional rather than a bug.