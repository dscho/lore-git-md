This is part of the parallel hook execution feature series (patch 8/13 in v7), which introduces per-event job count configuration. The patch allows users to override the global `hook.jobs` setting for specific hook events via `hook.<event>.jobs`, providing finer-grained control over parallelism.

The implementation adds a new `event_jobs` strmap to the repository struct to cache per-event job counts, with corresponding documentation explaining the precedence rules and naming requirements. The key technical change is in `get_hook_jobs()`, which now checks for event-specific overrides before falling back to the global setting. The patch maintains all existing safety mechanisms, including the requirement that hooks must explicitly opt into parallel execution via `hook.<name>.parallel=true`.

Three new test cases verify the behavior: overriding global settings per-event, forcing serial execution via per-event config, and ensuring the parallel opt-in requirement still applies even with per-event job counts. The changes integrate cleanly with the series' existing configuration hierarchy while maintaining backward compatibility.

This patch represents a logical extension of the parallel hooks system, giving users more control without compromising safety. The implementation is straightforward and well-tested, with clear documentation of the new capability. As part of the already-approved series, it's ready for merging.