This is part 13 of Patrick Steinhardt's 17-part series implementing an in-memory object database (ODB) backend, a significant piece of the ongoing ODB abstraction effort. The series is now in its third iteration and has received thorough technical review, with this patch representing one of the final callback implementations needed to complete the feature.

The patch adds the `count_objects()` callback to the in-memory ODB implementation. This is a straightforward addition that leverages the existing `for_each_object()` infrastructure - it simply iterates through all objects in the in-memory store while incrementing a counter. The implementation is clean and minimal, using a small helper callback function to perform the counting.

As noted in the thread context, this series is now technically ready for integration pending only a final terminology alignment in error messages ("in-memory" vs "in-core"). The patch demonstrates the careful attention to detail throughout the series - even for simple callbacks like this one, the implementation is complete and properly integrated into the vtable structure. This represents mature work that has been well-vetted through multiple rounds of review.