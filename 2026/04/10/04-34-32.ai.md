This is a platform compatibility clarification in the ongoing `writev()` optimization thread. Patrick Steinhardt confirms that his earlier demonstration using a reduced `MAX_IO_SIZE` value was purely for testing purposes and that he has no intention of modifying the production `MAX_IO_SIZE` definition in `git-compat-util.h`. 

The exchange reinforces the critical constraint that the existing `MAX_IO_SIZE` logic must remain unchanged to preserve NonStop platform support, as Randall S. Becker emphasized. Patrick aligns with Jeff King's earlier suggestion that testing with artificially small values should be done via `CFLAGS` rather than header modifications. This maintains the production behavior while allowing proper test coverage of edge cases.

The thread continues to balance performance optimization goals (through `writev()` usage) with cross-platform compatibility requirements, particularly for NonStop systems with strict `SSIZE_MAX` limitations. The immediate technical question appears resolved - testing will proceed via `CFLAGS` without affecting production definitions - while the broader architectural discussion about how to handle platform constraints continues in parallel.