Platform compatibility discussion about `writev()` optimizations in Git's network code, continuing the thread about test failures in Git 2.54.0-rc1. Johannes Sixt (Hannes) raises concerns about the use of `writev()` in Git's codebase, arguing that it creates false expectations about POSIX guarantees that may not hold on all platforms.

Hannes questions the actual performance benefits of using `writev()`, noting that while it reduces system call counts (from two to one in the sideband case), there haven't been concrete performance measurements provided. He points out that the code complexity actually increased with the introduction of `writev()` in commit 26986f4cbaf3 ("sideband: use writev(3p) to send pktlines"), suggesting the optimization may not be worth the cross-platform compatibility headaches.

This email adds a new perspective to the ongoing discussion by challenging the fundamental premise that `writev()` optimizations are valuable enough to justify their platform-specific complications. Hannes' position contrasts with Patrick Steinhardt's earlier proposal for a compatibility wrapper, instead suggesting the optimization may not be worthwhile at all. The thread now has three distinct approaches on the table: Jeff King's compile-time detection system, Patrick's runtime fallback wrapper, and Hannes' suggestion to reconsider the optimization entirely. The discussion continues to balance performance against portability, with no clear consensus yet emerging.