This is a follow-up in Lorenzo Pegorari's promisor file repacking patch series, addressing Junio Hamano's feedback about timestamp formatting in the `.promisor` file handling code.

Lorenzo acknowledges Junio's correction about using Git's standard `PRItime` macro and `timestamp_t` type instead of the non-portable `%lld` and `st_mtim.tv_sec` constructs. The author expresses appreciation for the guidance while noting they're still learning Git's development practices, particularly the importance of using `git grep` to find established patterns in the codebase.

The exchange represents the final polish on this technical aspect of the series, which has already addressed all major implementation concerns in previous iterations. The change doesn't affect functionality but brings the code in line with Git's portable timestamp handling conventions as seen in core components like the worktree and blame machinery.

Key technical details for future reference:
- Confirms adoption of `PRItime` macro for timestamp formatting
- Uses `timestamp_t` type casting from `st_mtime`
- References established patterns in `worktree.c`, `date.c`, and `builtin/blame.c`
- Maintains the series' focus on proper promisor file maintenance during repacks

This marks the resolution of the last outstanding style issue in the series, which is now awaiting final review from Eric Sunshine before potential integration.