This is a feature patch in the final stages of review, part of a series (v8 3/4) that enhances `git checkout -m` with autostash behavior and customizable conflict marker labels. Phillip Wood provides a positive review of the implementation, noting it could later be used to improve `git rebase --autostash` labels as well.

The patch extends the sequencer's autostash machinery to support passing custom labels (ours/theirs/base) through to `git stash apply`. It adds new parameters to internal functions and introduces `apply_autostash_ref_with_labels()` as a public API for callers needing label customization. The changes maintain backward compatibility by making all label parameters optional.

Phillip indicates the implementation looks good and suggests waiting a few days for potential additional feedback before proceeding. This represents one of the final review passes on a series that has already addressed all major technical concerns, with only minor documentation and implementation details remaining before merge. The changes focus on `sequencer.c` and `sequencer.h`, building on previous patches that implemented core autostash behavior for checkout.