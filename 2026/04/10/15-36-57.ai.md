This is a technical follow-up in Patrick Steinhardt's build system improvement series, specifically addressing the precompiled header (PCH) implementation for Meson builds. SZEDER Gábor raises a concern about the PCH being implicitly included in reftable source files that don't actually include git-compat-util.h.

The observation points out that while the current implementation works (since it doesn't cause compilation errors), it's conceptually incorrect to force-include git-compat-util.h via PCH in files that deliberately avoid including it. This is particularly relevant for the reftable subsystem, where only reftable/system.c includes git-compat-util.h while other reftable/*.c files intentionally don't.

This isn't a showstopper - the series has already addressed several similar technical constraints through careful isolation (like the compat library split in patch 7/8). However, it does suggest one more refinement may be needed to properly exclude reftable sources from PCH inclusion while maintaining the build speed improvements for the rest of the codebase. The concern is well-founded given Git's emphasis on precise build configurations and the reftable subsystem's specific design constraints.

The thread appears to be converging toward merge readiness, with this being one of the final technical points to clarify. Given Patrick's thorough approach throughout the series, we can expect either a technical justification for the current behavior or a follow-up adjustment to address this observation.