Feature patch series (v8) adding conflict marker labels to autostash in `git checkout -m`. This is patch 3/4 in the series, which is now in its final refinement stage with all core functionality approved.

Phillip Wood provides a positive review of Harald Nordgren's patch that extends the autostash machinery to support customizable conflict marker labels when applying stashes. The patch adds parameters to pass label strings (ours/theirs/base) through to `git stash apply` and introduces a new public function `apply_autostash_ref_with_labels()` in sequencer.h. Phillip notes this could also benefit `git rebase --autostash` in the future, though that would be a separate effort.

The review indicates the series is nearly ready for merging, with Phillip planning to look at the final patch next week. He suggests waiting a few days for any additional comments before sending a new version. The technical implementation appears solid, building on previous iterations that have already addressed edge cases and testing requirements. This patch completes the plumbing needed for customized conflict markers throughout the autostash workflow while maintaining backward compatibility.