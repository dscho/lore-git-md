This is a v4 patch series from Lorenzo Pegorari addressing promisor file handling during repacks, part of his GSoC work under Eric Sunshine's mentorship. The series builds on prior work (5374a290) to properly maintain ref-to-packfile mapping in `.promisor` files during repacking operations.

The key improvement in v4 is switching to Unix epoch timestamps (rather than formatted dates) for debugging information, which resolves cross-platform concerns raised in earlier reviews. The implementation now uses `string_list` instead of `strbuf_split_max()` for cleaner string handling, and the test suite has been significantly improved with more robust test hygiene and deterministic behavior via `repack -a -f`.

The series consists of 5 well-structured patches:
1. Documentation explaining `.promisor` file contents
2. New `copy_promisor_content()` helper that preserves timestamps
3. Integration of the helper into repack logic with geometric repack awareness
4-5. Expanded test coverage in both regular and geometric repack scenarios

The author has been responsive to feedback, addressing all of Junio Hamano's test hygiene concerns and confirming fixes for macOS compatibility issues. One open question remains about header inclusion policy for `strmap.h` - the author is seeking clarification on whether additional includes are needed per Git's coding guidelines.

This appears to be a mature series that has incorporated extensive review feedback. The changes are focused and well-motivated, with thorough test coverage that should help ensure the functionality works correctly with Taylor Blau's ongoing repacking work. The remaining header inclusion question seems to be the last technical hurdle before this could be ready for merging.