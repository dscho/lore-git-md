This is a platform compatibility clarification in the ongoing `writev()` optimization thread. Patrick Steinhardt confirms that his earlier test demonstrating a small `MAX_IO_SIZE` value was purely for testing purposes and not intended as a production change. He affirms that the existing `MAX_IO_SIZE` logic in `git-compat-util.h` - which properly respects platform-specific `SSIZE_MAX` values on systems like NonStop - will remain unchanged.

The exchange reinforces that any solution to the `writev()` optimization issue must maintain the current `MAX_IO_SIZE` behavior while allowing for testing edge cases through compiler flags rather than header modifications. This keeps the path clear for both the short-term `NO_WRITEV` workaround and potential longer-term solutions like Jeff King's proposed compile-time `SSIZE_MAX` detection system or Patrick's `xwritev()` implementation with proper fallback behavior.