This is part 4 of Patrick Steinhardt's 17-part series introducing an in-memory object database (ODB) backend, a significant feature in the ongoing ODB abstraction effort. The patch implements the `read_object_info()` callback for the in-memory source, moving object metadata lookup functionality from `odb.c` to the new `odb/source-inmemory.c` implementation.

The change cleanly separates concerns by relocating the object info reading logic to its proper place in the new backend architecture. The implementation handles all metadata fields (type, size, content, etc.) and maintains special handling for the empty tree object. The patch demonstrates careful attention to detail, preserving all existing behavior while restructuring the code to fit the new abstraction.

This is a well-crafted piece of the thread summary indicates has already received thorough review. The series as a whole appears ready for integration pending final terminology alignment in error messages, with this patch representing a key piece of the complete implementation.