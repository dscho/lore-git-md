This is part 12 of Patrick Steinhardt's 17-part series introducing an in-memory object database (ODB) backend, a significant piece of the ongoing ODB abstraction effort. The series is now in its third iteration and has received thorough technical review, with this patch implementing the final callback needed for complete functionality.

The patch adds the `find_abbrev_len()` callback to the in-memory ODB implementation, which determines the shortest unambiguous prefix length for a given object ID. The implementation efficiently reuses the existing `for_each_object()` infrastructure while properly handling the minimum length requirement and hex prefix matching. 

This is one of the last pieces needed to make the in-memory backend fully functional, following previous patches that implemented other core operations like read`, `write`, and iteration. The implementation looks clean and follows established patterns from the rest of the series, with proper error handling and integration with Git's existing OID abbreviation logic.

With this callback now implemented, the in-memory ODB backend has complete coverage of all required operations, bringing Patrick's substantial refactoring effort one step closer to completion. The series as a whole appears well-vetted and ready for integration after addressing the minor terminology issue mentioned in the thread context.