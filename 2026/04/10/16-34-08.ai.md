This is a feature patch in the final refinement stage of an 8-iteration series enhancing `git checkout -m` with autostash behavior and customizable conflict marker labels. The patch (v8 3/4) extends the sequencer's autostash machinery to support passing custom labels (ours/theirs/base) through to `git stash apply`, introducing `apply_autostash_ref_with_labels()` as a new public API while maintaining backward compatibility with optional label parameters.

Junio C Hamano notes a minor parameter naming inconsistency that needs updating, while Phillip Wood confirms the technical implementation looks sound and suggests this infrastructure could later improve `git rebase --autostash` labels. The changes focus on `sequencer.c` and `sequencer.h`, building on previous patches that implemented core autostash behavior for checkout. With all major technical concerns resolved, the series now awaits only trivial parameter naming fixes before final merge.

The patch demonstrates careful attention to backward compatibility and scriptability, particularly in its support for empty labels (analogous to rerere's label-agnostic resolution) which Junio has explicitly endorsed. Test coverage in t7201-co.sh provides 19-case validation of the new behavior. This represents the culmination of a well-reviewed effort to unify conflict resolution workflows across Git commands.