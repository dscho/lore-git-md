This is a refactoring patch in Derrick Stolee's series on fetch/push controls, specifically renaming internal variables to match the new `--negotiation-restrict` terminology introduced in earlier patches. The change is mechanical, updating all references from `negotiation_tips` to `negotiation_restrict_tips` across the transport layer and related components.

The patch touches several key files including `transport.[ch]`, `fetch-pack.[ch]`, and `builtin/fetch.c`, showing the rename propagating through the negotiation control system. This is part of a larger effort to improve monorepo scaling by giving users more precise control over which refs are prioritized during fetch/push negotiations.

The changes are straightforward and maintain backward compatibility while aligning the codebase with the new terminology established in the series. This patch follows naturally from the previous interface changes and doesn't introduce new functionality - it's cleaning up the implementation to match the new naming scheme. The series as a whole appears to be converging well based on earlier feedback.