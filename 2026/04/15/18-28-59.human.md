Progress reporting refinement discussion for `git backfill`.

Derrick Stolee follows up on Trieu Huynh's v2 patch adding progress reporting to the experimental `git backfill` command, requesting more concrete examples of how the progress indicators interact with `git fetch` output across multiple batches. While Huynh had demonstrated the outputs don't interleave badly in practice (fetch output completes before backfill's progress updates), Stolee wants to see actual multi-batch output before confirming the approach.

This continues the thread's focus on ensuring clean output when both backfill's cumulative counter and fetch's per-batch progress are enabled. The proposed solution - adding a `--[no-]verbose` flag in v3 to control fetch output independently - remains sound, but Stolee is being thorough in verifying the interaction works as claimed across realistic scenarios. His request suggests the patch is nearing acceptance but needs one more round of demonstration before finalizing.

The exchange shows careful attention to user experience details in this experimental command, with Stolee providing experienced guidance while Huynh responds with technical specifics. The next version will likely include sample outputs from multi-batch runs to conclusively show the progress reporting behaves as intended.