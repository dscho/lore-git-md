This is a post-merge refinement patch in the autostash feature series, adding silent operation capability to autostash creation. The patch modifies the sequencer's autostash internals to support suppressing the "Created autostash" message when requested.

The key changes are:
1. Adds a `silent` parameter to `create_autostash_internal()` and exposes it through `create_autostash_ref()`
2. Allows passing a custom message for the stash (defaulting to "autostash")
3. Updates callers in `builtin/merge.c` to use the new interface while maintaining current behavior

The implementation builds on the already-merged autostash functionality that was added to support conflicted branch switching. This appears to be a straightforward quality-of-life improvement that gives callers more control over output behavior, particularly useful for scripts or other automated contexts where the message might be noisy.

The patch follows the established pattern of this series - small, focused changes that refine the core functionality after its initial merge. It addresses a natural extension point that likely emerged during the earlier review discussions, showing the author's continued attention to the feature's post-merge evolution.