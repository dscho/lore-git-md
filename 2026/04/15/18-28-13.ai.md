Final documentation polish for the mailmap batch command feature.

Junio Hamano suggests simplifying the `git cat-file` documentation to focus on user-visible behavior rather than implementation details. The current text emphasizes the mailmap data caching strategy (loaded once and kept in memory), which he considers an internal optimization that shouldn't be the primary focus. Instead, he proposes a more concise explanation that:
- States the core functionality (toggling mailmap application to output)
- Notes the one-time data loading behavior
- Implicitly covers the caching aspect by mentioning data isn't reloaded

The implementation itself remains unchanged from the well-reviewed previous version, with Junio confirming the code looks good and the existing test coverage is sufficient. This is purely a documentation refinement at the maintainer's request, bringing the man page text more in line with Git's typical focus on user-facing behavior over implementation mechanics.

The suggested wording change maintains all the technical accuracy while being more direct about what matters to users - when mailmap processing happens, not how the data is managed internally. This follows Git's documentation conventions where implementation details are typically mentioned only when they have direct user-visible consequences (like the non-reloading behavior in this case).