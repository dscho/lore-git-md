This is a test infrastructure patch in Patrick Steinhardt's ongoing series to make Git's test suite work reliably with `set -e` enabled. The patch fixes a specific issue in t6002-rev-list-bisect.sh where the test helper `test_bisection_diff()` uses the `expr` command in a way that would fail under `set -e` when the result is zero.

The change replaces `expr` with shell arithmetic (`$(( ))`), which doesn't have the same failure modes, and modernizes the test syntax slightly by using a multi-line test expectation. This is a mechanical but important change that follows the established pattern in the series of making test infrastructure robust while maintaining the exact same behavior.

The patch is part of a well-motivated effort to improve test reliability, and like others in the series, it demonstrates careful attention to edge cases that could cause false failures. Reviewers can be confident this change is correct based on the thorough testing and iteration the series has already undergone.