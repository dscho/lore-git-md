This is a feature patch in the final polishing phase of a series that enhances `git checkout -m` with autostash behavior. The series has already been merged to the `next` branch and is now addressing technical refinements.

The patch adds silent operation capability to autostash creation, allowing callers to suppress the "Created autostash" message. It introduces a new `silent` parameter to `create_autostash_internal()` and exposes it through `create_autostash_ref()`. The change also adds support for custom stash messages, though this appears to be a secondary feature in this particular patch.

The implementation touches three files: `builtin/merge.c` (updating call sites), `sequencer.c` (core logic changes), and `sequencer.h` (interface updates). The changes are straightforward and maintain the existing behavior while adding the new functionality. This is a clean, focused patch that completes one piece of the larger autostash feature set.

The patch follows the established pattern of the series and addresses a specific reviewer request (likely from Phillip Wood or Junio Hamano) for more flexible autostash behavior. It demonstrates the project's typical attention to detail in finalizing merged features.