This is a bug report about inconsistent behavior in `git fetch` argument parsing. The author, Alix Brunet, points out that `git fetch <url>` silently fails (doing nothing) while `git fetch <url> ""` correctly fetches the default branch. The behavior difference between these two forms isn't documented in the man page.

The issue appears to be a parsing edge case where an empty string as the second argument triggers different behavior than omitting the argument entirely. The author suggests this is either a bug that should be fixed or at minimum a documentation clarification. No patch is provided - this is purely an observation of unexpected behavior that could trip up users.

The report is clear and well-formulated, though it doesn't propose a specific solution, it identifies a real inconsistency that warrants attention. The behavior difference between these two forms of invocation seems unintentional and could be considered a bug or documentation gap. The thread appears to be new with no prior discussion visible in the provided context.