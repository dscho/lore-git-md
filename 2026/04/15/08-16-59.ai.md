This is a follow-up discussion in the already-merged autostash feature series for `git checkout -m`. The technical implementation is settled (now in `next`), and this exchange focuses on refining the error handling logic in response to Phillip Wood's review.

Harald Nordgren responds to Phillip's detailed technical feedback about when exactly the autostash should be attempted during conflicted branch switches. The key points under discussion are:

1. The autostash should only be created when `unpack_trees()` fails with specific error conditions that stashing could resolve, not for all `merge_working_tree()` failures. Harald attempts to implement this but notes uncertainty about the correctness of the error codes.

2. The autostash application should be conditional on having actually created a stash, which Harald addresses by making `apply_autostash_ref` depend on the `created_autostash` flag.

This is a pattern typical of Git's review process - even after a series is merged to `next`, contributors continue polishing edge cases based on reviewer feedback. The changes discussed here are refinements to an already-working implementation, showing the project's focus on getting details right. Phillip Wood's earlier review identified several subtle aspects of the error handling that needed attention, and Harald's response demonstrates engagement with that feedback while acknowledging remaining uncertainties.

The thread illustrates how Git's review culture values precise error handling, even for cases that might seem edge-casey to outsiders. The discussion remains technical and constructive, focused on making the autostash behavior as robust as possible before the feature ships in a release.