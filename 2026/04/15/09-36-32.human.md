This is a technical refinement discussion for the already-merged autostash feature in `git checkout -m`. The patch series has been through multiple iterations and is now in the `next` branch, with this final polishing addressing edge cases in the error handling.

Phillip Wood provides follow-up review on Harald Nordgren's implementation, confirming two key improvements:

1. The autostash should only be attempted when `merge_working_tree()` fails due to specific `unpack_trees()` errors that stashing could resolve, not for all failures. Harald has implemented this but expresses uncertainty about the exact error codes.

2. The autostash application should be conditional on having actually created a stash, which Harald addresses by making `apply_autostash_ref` depend on a `created_autostash` flag. Phillip confirms this is the correct approach.

The exchange demonstrates Git's attention to detail in error handling, even for already-merged features. Phillip's review focuses on making the behavior robust while maintaining clarity in the control flow. This is typical of the project's culture of post-merge refinement, where reviewers like Phillip help ensure edge cases are properly handled before a feature ships.