This is a platform compatibility update in the ongoing discussion about `writev()` optimizations in Git's network code. Randall S. Becker (NonStop platform maintainer) responds to Patrick Steinhardt's proposed `xwritev()` implementation with an important technical constraint.

Randall strongly cautions against modifying `MAX_IO_SIZE` in `git-compat-util.h`, noting that changing this would break `xwrite()` functionality on NonStop systems. He emphasizes that the current `MAX_IO_SIZE` implementation has been working and verified for years, and altering it would make NonStop unsupportable. This is a critical platform-specific consideration that wasn't apparent in earlier discussion.

The response doesn't reject Patrick's overall approach but highlights a specific implementation detail that needs adjustment. Randall's input suggests the `xwritev()` solution may still be viable if it can work within the existing `MAX_IO_SIZE` mechanism rather than redefining it. This maintains the thread's trajectory toward a runtime fallback solution while respecting established platform constraints.

The exchange shows the careful balancing act between introducing new optimizations and preserving existing platform support. Randall's position as the NonStop maintainer gives his technical feedback particular weight in this context. The thread continues to move toward a solution that maintains the `writev()` optimization where possible while properly handling platform limitations.