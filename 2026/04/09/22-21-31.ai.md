This is a follow-up discussion about the `writev()` compatibility issue on NonStop platforms. Jeff King (Peff) responds to Junio C Hamano's complete revert of the `writev()` emulation layer with a more targeted suggestion.

Peff proposes a smaller-scope solution: instead of reverting all four commits (which removed the entire `writev()` infrastructure), he suggests only reverting the commit that introduced `writev()` usage in the sideband code (26986f4cba). This would leave the wrapper functions in place as dead code until a proper fix is implemented post-2.54, making it easier to reintroduce the functionality later.

The email highlights the technical tradeoff between a clean slate (Junio's approach) versus maintaining scaffolding for future work (Peff's suggestion). Peff acknowledges the frustration with the situation ("Yeah, I feel the same way") while keeping the door open for a better solution after the release. This represents a typical Git development pattern - when faced with platform-specific issues close to a release, the project often chooses simpler, more reliable code paths over optimized but problematic ones.

The discussion remains focused on the technical merits of each approach, with both participants clearly invested in finding the least disruptive path forward. Peff's suggestion shows his characteristic attention to minimizing collateral damage while still addressing the immediate compatibility concern.