This is a platform compatibility clarification in the ongoing `writev()` optimization thread. Jeff King responds to Randall S. Becker's concern about modifying `MAX_IO_SIZE` in `git-compat-util.h`, explaining that the small value (128 bytes) shown in Patrick Steinhardt's test diff was just a demonstration case rather than a proposed change. He suggests using `CFLAGS=-DMAX_IO_SIZE=128` for testing instead, which wouldn't modify the header file permanently.

The exchange shows the careful consideration being given to NonStop platform constraints while still allowing the `xwritev()` solution to be tested under artificial limits. Jeff's response indicates the core team understands the importance of maintaining the existing `MAX_IO_SIZE` definition that NonStop relies on, while still needing to verify the fallback behavior works at various size thresholds. This keeps the thread moving toward a solution that respects platform limitations while preserving the optimization where possible.