This is a review comment in the thread about Patrick Steinhardt's platform compatibility patch for `writev()` handling on NonStop systems. Ben Knoble raises a minor stylistic concern about the comment placement in the new `xwritev()` function.

Ben notes that the comment declaring "the first entry exceeds MAX_IO_SIZE" appears before the actual `if (!i)` check that makes this determination, which he finds slightly confusing. He questions whether this comment-before-check pattern is typical Git style, suggesting the comment might be clearer if placed after the condition it describes.

The comment in question explains the fallback to `xwrite()` when the first iovec entry exceeds the platform's `MAX_IO_SIZE` limit. Ben acknowledges that he initially misread the control flow, thinking the comment applied to cases where `i >= 1`, before realizing it specifically describes the `!i` case below it.

This is a small but thoughtful review point about code clarity from someone who admits they're "out of [their] depth" on the platform-specific I/O details. The comment doesn't challenge the technical approach (which was already discussed with Jeff King and addresses Randall Becker-reported issue), but focuses on making the implementation as clear as possible to future readers.