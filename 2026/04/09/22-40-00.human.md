This is a platform compatibility follow-up in the ongoing `writev()` optimization thread. Randall S. Becker responds to Jeff King's clarification about testing approach, emphasizing that the existing `MAX_IO_SIZE` definition in `git-compat-util.h` must remain unchanged to preserve NonStop platform support. He reiterates the current logic where `MAX_IO_SIZE` is properly set to `SSIZE_MAX` when defined (as it is on NonStop), falling back to 8MB otherwise.

The exchange shows Randall maintaining a firm position on platform constraints while acknowledging Jeff's suggestion to use `CFLAGS` for testing purposes. This keeps the discussion focused on solutions that respect NonStop's existing configuration while still allowing the `xwritev()` fallback behavior to be verified under artificial limits. The thread continues to balance optimization benefits against platform-specific requirements, with Randall ensuring the NonStop perspective remains clearly represented in the technical discussion.