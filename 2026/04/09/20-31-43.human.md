This is the final patch in a feature series implementing unified conflict resolution in `git checkout -m` with autostash behavior. The series has gone through 8 iterations and is now fully approved, addressing all feedback from Junio C Hamano and Phillip Wood.

The author, Harald Nordgren, confirms the last three minor improvements requested by Junio: using `test_grep` consistently in tests, validating base labels in conflict markers, and properly formatting empty labels by omitting trailing spaces. These were the final polish items needed before merging.

The patch series as a whole enhances branch switching by integrating autostash behavior into `git checkout -m` while maintaining backward compatibility. It adds customizable conflict marker labels (including empty label support) and implements silent autostash creation. The implementation has been validated in production environments and includes comprehensive test coverage (19 test cases in t7201-co.sh).

With these final tweaks addressed, the series is now ready for merge. The changes touch multiple components including `builtin/checkout.c`, `builtin/stash.c`, and `sequencer.c`, with new test cases and documentation updates. This represents the conclusion of a well-reviewed effort to improve Git's conflict resolution workflow.