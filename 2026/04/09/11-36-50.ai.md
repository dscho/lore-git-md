This is a minor review comment in Patrick Steinhardt's ongoing ODB abstraction series, specifically addressing documentation in the cbtree refactoring patch (v2 09/17). Karthik Nayak points out that the header file comments still reference the removed `k[FLEX_ARRAY]` field after Patrick's changes to allow arbitrary wrapper structures for nodes.

The observation is straightforward - the patch removed the flexible array member but didn't update the accompanying documentation. This is a common oversight in refactoring patches where implementation changes outpace comment updates. Karthik's review catches this inconsistency, though he doesn't propose specific replacement text. The fix would be a simple documentation update to reflect the new node structure semantics where keys are accessed via offset rather than direct array access.

This is a small but important polish item in the larger series, where attention to such details helps maintain codebase consistency. The comment update would logically be folded into the existing patch rather than requiring a new revision, as it directly relates to the same changes.