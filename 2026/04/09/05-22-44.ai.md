This is a technical discussion in Patrick Steinhardt's ODB abstraction series, specifically addressing implementation details of the in-memory object database source introduced in patch 1/16. The exchange clarifies several points about the design:

Patrick confirms that git-blame is currently the only user writing objects exclusively to the in-memory cache, and agrees to update the commit message to mention this explicitly. He acknowledges Justin Tobler's suggestion for a dedicated `odb_source_inmemory_release()` function, noting this will be addressed in subsequent commits.

The more substantive discussion centers on the `source` path field in the generic ODB_SOURCE structure. Justin questions its relevance for in-memory backends, while Patrick explains its dual role: as an identifier for source comparison (which should be an opaque payload rather than a path) and its current misuse by some callers assuming it's always a filesystem path. He outlines plans to eventually move path handling to the files backend specifically and convert the generic field to a proper payload identifier.

This exchange shows the careful attention to interface design in the ODB abstraction effort, with both contributors thinking ahead about how these structures will evolve. The discussion remains focused on technical merits, with Patrick providing clear rationale for current design choices while acknowledging areas needing future improvement.