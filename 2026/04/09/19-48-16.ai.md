This is a platform compatibility report from Randall S. Becker about a build issue on NonStop systems. The email describes a problem where `NO_WRITEV=Nope` appears to be ignored during the build, leading to `writev()` being used anyway on NonStop despite the attempt to disable it. This causes a test failure in `t5310-pack-bitmaps.sh` when cloning a repository, with the error "fatal: writev error: Invalid function argument".

The email includes the exact build command used, showing the attempt to set `NO_WRITEV=Nope` alongside other platform-specific flags and paths. The test failure occurs during a clone operation where the system unexpectedly uses `writev()`, which apparently isn't properly supported on this platform.

Randall is the maintainer of the NonStop port of Git, and this report follows his pattern of raising platform-specific compatibility issues, particularly around features that might not be available on NonStop systems. The tone suggests genuine confusion about why the `NO_WRITEV` flag isn't having the intended effect, though the closing signature ("In real life, I talk too much") adds a self-deprecating note typical of his communication style.

This appears to be a straightforward build system issue where a platform-specific configuration flag isn't being properly honored, leading to runtime failures. The problem is well-isolated to the NonStop platform and clearly demonstrated with a test case failure.