NonStop platform build issue with `writev()` compatibility, where `NO_WRITEV=Nope` flag fails to prevent `writev()` usage during clone operations, causing test failures in t5310-pack-bitmaps.sh. Randall S. Becker initially reported the problem where NonStop systems incorrectly handle `writev()` with "fatal: writev error: Invalid function argument" errors. Jeff King (Peff) analyzed the root cause: Git's fallback `writev()` wrapper is too strict in its emulation, rejecting valid operations due to NonStop's smaller `ssize_t` type and enforcing size limits that trigger `EINVAL` errors. The issue stems from Git assuming 64k chunk sizes that exceed NonStop's capabilities.

Peff proposes two potential solutions for Git 2.54: either implement proper `MAX_IO_SIZE` support to respect platform limits (requiring careful handling of partial writes to avoid overflow while maintaining progress reporting), or temporarily revert `writev()` usage in `send_sideband()` as a stopgap measure. The discussion highlights the tension between Git's performance optimizations (using `writev()` for efficient I/O) and platform compatibility on NonStop systems with unusual constraints. The technical analysis identifies both the symptom (test failures) and the deeper architectural consideration needed for a robust solution. No other participants have commented beyond Randall's initial report and Peff's detailed response.