This is the final version of a feature series implementing autostash behavior for `git checkout -m` when switching branches with conflicts. The series has undergone extensive review cycles and is now ready for merging.

Maintainer Junio C Hamano confirms the series looks good, noting the key changes in v7 are a rebase onto current master and the addition of a new test case for non-overlapping changes. The implementation now includes:

1. New stash apply labeling options (`--ours-label`, `--theirs-label`, `--base-label`) that allow customizing conflict markers
2. Silent autostash creation during checkout operations via `create_autostash_ref_silent()`
3. Unified conflict resolution behavior in `git checkout -m` through autostash
4. Comprehensive test coverage with 19 test cases in t7201-co.sh, including the new non-overlapping changes test added in v7

The changes touch multiple components including checkout.c, stash.c, and sequencer.c, with careful attention to backward compatibility. The series represents a significant quality-of-life improvement for branch switching workflows, particularly when developers need to context switch between branches with uncommitted changes.

With all edge cases addressed, maintainer approval confirmed, and the final test case added, this series is now complete and ready for integration into the main Git codebase. The thread can be considered closed with the implementation finalized.