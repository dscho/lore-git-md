This is a platform compatibility discussion continuing the thread about `writev()` optimization issues in Git 2.54.0-rc1 on NonStop systems. Randall S. Becker responds to Patrick Steinhardt's proposed `xwritev()` implementation with a caution about modifying `MAX_IO_SIZE` in `git-compat-util.h`.

Randall warns that changing the `MAX_IO_SIZE` definition as suggested in Patrick's test case would break `xwrite()` functionality on NonStop platforms. He notes this value has been working and verified for years, and changing it would make their platform unsupportable. This is an important technical constraint that needs to be considered in any solution.

The response doesn't reject Patrick's overall approach but highlights a specific implementation concern that needs addressing. The thread continues to balance the desire for `writev()` optimizations with the need to maintain platform compatibility, particularly for NonStop systems with their unique hardware constraints. Randall's input suggests the solution will need to work within the existing `MAX_IO_SIZE` framework rather than redefining it.