Thread subject: "compat: add xwritev() to handle platform-specific I/O size limits" - patch series from Patrick Steinhardt addressing NonStop platform compatibility issue with `writev()`, reviewed by Junio C Hamano.

Core problem: NonStop systems have small `MAX_IO_SIZE` limit (reported by Randall S. Becker) not respected in `writev()` path, unlike regular writes which use `xwrite()`'s size-limiting wrapper. Current implementation risks failing when large iovec entries exceed platform limits.

Solution: Introduces new `xwritev()` wrapper in `wrapper.c` that coalesces iovec entries under `MAX_IO_SIZE`, falls back to `xwrite()` for oversized first element, with careful error handling. Updates `writev_in_full()` to use new wrapper.

Technical details:
- Modifies `wrapper.c` with new function and updates callers
- Handles EINTR, partial writes like `xwrite()`
- Preserves behavior on platforms without `MAX_IO_SIZE` limits
- Must guard against `ssize_t` overflow when `ssize_t` narrower than `size_t`
- Needs signal handling consistency with `xwrite()`
- Alternative considered: disable `writev` on NonStop temporarily via `NO_WRITEV`

Current status:
- Junio approves general approach but requests specific improvements
- Patch will be deferred until after Git 2.54 release
- NonStop users should use `NO_WRITEV` workaround for now

Key technical concerns to address:
- `unsigned_add_overflows()` may not properly guard `ssize_t` overflow
- Comment clarity around first-buffer handling needs improvement
- Signal handling consistency between `xwrite()` and `xwritev()` paths

Participants:
- Patrick Steinhardt (author): proposes complete fix rather than workaround
- Randall S. Becker (reporter): identified NonStop-specific issue
- Jeff King (reviewer): provided initial input on solution approach
- D. Ben Knoble: suggested comment placement improvement
- Junio C Hamano (maintainer): approved approach with specific refinements needed

Open questions:
- Final implementation of `ssize_t` overflow protection
- Exact comment wording improvements
- Timing for inclusion post-2.54 release

Related work:
- Existing `xwrite()` implementation provides pattern to follow
- `NO_WRITEV` workaround available for NonStop users in interim