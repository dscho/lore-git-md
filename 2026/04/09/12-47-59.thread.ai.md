Thread subject: "compat: add xwritev() to handle platform-specific I/O size limits" - patch series from Patrick Steinhardt addressing NonStop platform compatibility issue with `writev()`, reviewed by Junio C Hamano and Jeff King, now deferred to post-2.54 release.

Core problem: NonStop systems have small `MAX_IO_SIZE` limit (reported by Randall S. Becker) not respected in `writev()` path, unlike regular writes which use `xwrite()`'s size-limiting wrapper. Current implementation risks failing when large iovec entries exceed platform limits.

Solution: Introduces new `xwritev()` wrapper in `wrapper.c` that coalesces iovec entries under `MAX_IO_SIZE`, falls back to `xwrite()` for oversized first element, with careful error handling. Updates `writev_in_full()` to use new wrapper.

Technical details:
- Modifies `wrapper.c` with new function and updates callers
- Handles EINTR, partial writes like `xwrite()`
- Preserves behavior on platforms without `MAX_IO_SIZE` limits
- Uses Jeff King's counting-down approach for overflow protection
- Restructured to handle oversized first entries immediately in loop
- Maintains signal handling consistency with `xwrite()`
- Alternative considered: disable `writev` on NonStop temporarily via `NO_WRITEV`

Current status:
- Implementation correctness confirmed through final review by Jeff King and Junio
- All edge cases verified: overflow protection works for all iovec entry sizes
- First entry handling properly falls back to `xwrite()` when needed
- Patch deferred until after Git 2.54 release
- NonStop users should use `NO_WRITEV` workaround for now
- Junio confirms Jeff King's analysis that all edge cases are correctly handled
- Final technical concerns resolved regarding first-entry handling and overflow protection
- Patrick Steinhardt confirms deferral decision to next development cycle

Key technical concerns addressed:
- Overflow protection now uses counting-down approach for clarity (Jeff King)
- First-buffer handling moved into main loop (Jeff King)
- Comment clarity improved per multiple reviewers' suggestions
- Both Junio and Jeff King confirm implementation correctness after final verification
- Edge cases verified where first entry is small but subsequent entries would overflow
- Edge cases verified where first entry itself exceeds `MAX_IO_SIZE`

Participants:
- Patrick Steinhardt (author): proposes complete fix rather than workaround, confirms deferral timing
- Randall S. Becker (reporter): identified NonStop-specific issue
- Jeff King (reviewer): provided overflow protection improvements, code restructuring, and final verification
- D. Ben Knoble: suggested comment placement improvement
- Junio C Hamano (maintainer): approved approach with specific refinements needed, confirmed final implementation correctness

Open questions:
- Final wording for explanatory comments (to be addressed in next cycle)
- Timing for inclusion post-2.54 release (confirmed as next development cycle)

Related work:
- Existing `xwrite()` implementation provides pattern to follow
- `NO_WRITEV` workaround available for NonStop users in interim
- Follow-up expected in next development cycle after 2.54 release