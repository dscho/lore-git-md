This is a platform compatibility discussion about `writev()` optimizations in Git's network code, continuing the thread about test failures in Git 2.54.0-rc1 on NonStop systems. Phillip Wood clarifies a technical point in response to Patrick Steinhardt's proposed solution.

Phillip confirms that on NonStop systems, the `writev()` limitation applies to the total size of the iovec array (sum of iov_len values) rather than individual entries, citing Randall S. Becker's earlier message and noting this aligns with POSIX standards. This clarification helps validate Patrick's proposed `xwritev()` implementation, which handles large writes by splitting them when they exceed platform limits.

The thread continues to explore solutions for balancing performance optimization (using `writev()` where possible) with platform compatibility (handling systems with small SSIZE_MAX limits). Phillip's input provides important context for evaluating Patrick's patch, which aims to maintain optimization benefits while working around platform constraints. The discussion remains technical and collaborative, with participants working toward a solution that serves both performance-sensitive and platform-compatibility needs.