This is a platform compatibility discussion continuing the thread about `writev()` optimization issues in Git 2.54.0-rc1 on NonStop systems. Phillip Wood clarifies a technical point in response to Patrick Steinhardt's proposed solution.

Phillip confirms that NonStop's `writev()` limitation applies to the total size of the iovec array (sum of iov_len values) rather than individual entries, citing Randall S. Becker's earlier documentation reference. This aligns with POSIX's SSIZE_MAX constraint and supports Patrick's proposed `xwritev()` implementation which handles large writes by splitting them when needed.

The email doesn't introduce new technical content but serves as an important clarification that helps validate Patrick's approach. Phillip's confirmation that the limitation is on total size rather than individual entries means Patrick's solution - which already handles both cases - is technically sound for the NonStop platform. This moves the discussion closer to resolution, with Patrick's `xwritev()` patch appearing increasingly viable as a long-term solution that maintains performance optimizations where possible while gracefully handling platform constraints.