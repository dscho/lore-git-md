This is a review comment on patch 7/17 in Patrick Steinhardt's series implementing an in-memory object database source as part of the ODB abstraction effort. Karthik Nayak points out what appears to be a rebase error - the commit message seems to be missing content, showing only the first line. The patch implements the `write_object()` callback for the in-memory backend, adding proper object ID computation via `hash_object_file()` before storing objects in the transient in-memory storage.

The review is light - Karthik doesn't raise any technical concerns about the implementation itself, which follows the pattern of the series by using the `odb_source_inmemory_downcast` helper and growing the objects array as needed. The main issue is the incomplete commit message, which appears to be a simple oversight from rebasing. This is a minor polish issue in an otherwise straightforward patch that continues building out the in-memory ODB source functionality. The series as a whole is in good shape, with just a few small issues like this to clean up before final integration.