This is a platform compatibility discussion in the ongoing `writev()` optimization thread. Jeff King clarifies Patrick Steinhardt's earlier testing approach, confirming that modifying `MAX_IO_SIZE` in `git-compat-util.h` was only for local testing purposes and not intended for the final solution. He reiterates that the proper platform-specific behavior (where `MAX_IO_SIZE` respects `SSIZE_MAX` when defined) would be preserved.

The exchange shows alignment between Jeff and Randall S. Becker on maintaining the existing `MAX_IO_SIZE` logic that properly handles NonStop's platform constraints. Jeff suggests using `CFLAGS=-DMAX_IO_SIZE=128` for testing rather than modifying headers, which addresses Randall's concern about breaking existing `xwrite()` functionality. This keeps the thread moving toward a solution that respects platform limits while allowing proper testing of the proposed `xwritev()` fallback behavior.