This is a platform compatibility clarification in the ongoing `writev()` optimization thread. Randall S. Becker responds to Jeff King's suggestion about testing with a small `MAX_IO_SIZE` value, emphasizing that the existing definition in `git-compat-util.h` must remain unchanged to preserve NonStop platform support. He reiterates the current logic where `MAX_IO_SIZE` is properly set to `SSIZE_MAX` when defined (as it is on NonStop), falling back to the 8MB default otherwise.

The exchange shows Randall defending the carefully established platform-specific behavior while acknowledging Jeff's point about testing with small values. This reinforces the constraint that any solution must work within NonStop's existing `MAX_IO_SIZE` definition rather than modifying it, which aligns with Jeff's earlier suggestion to use `CFLAGS` for testing purposes. The thread continues to balance platform constraints with the desire to maintain `writev()` optimizations where possible.