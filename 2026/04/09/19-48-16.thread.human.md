The thread discusses a build issue specific to NonStop systems where the `NO_WRITEV` flag isn't properly preventing `writev()` usage, causing test failures in `t5310-pack-bitmaps.sh`. Randall S. Becker initially reported that despite setting `NO_WRITEV=Nope`, the system still attempts to use writev() and fails with "fatal: writev error: Invalid function argument".

Jeff King (Peff) provided a detailed technical analysis, identifying that the root cause lies in Git's fallback `writev()` wrapper being too strict for NonStop's smaller `ssize_t` type. The wrapper enforces size limits that trigger `EINVAL` errors, even though the underlying issue is the platform's inability to handle Git's assumed 64k buffer chunks.

Peff suggests two potential solutions for Git 2.54: either properly implement `MAX_IO_SIZE` support to respect platform limits (which would require careful handling of partial writes), or temporarily revert `writev()` usage in `send_sideband()` as a stopgap measure. The discussion highlights the ongoing challenge of balancing Git's performance optimizations with support for platforms like NonStop that have unusual constraints. No resolution has been reached yet, but the technical analysis has clearly identified both the immediate problem and the architectural considerations needed for a proper fix.