Here's a human-readable summary of the thread so far:

The thread reports a build issue specific to NonStop systems, where the `NO_WRITEV=Nope` configuration flag appears to be ignored during compilation. Randall S. Becker, who maintains the NonStop port of Git, found that despite setting this flag to disable `writev()`, the system still attempts to use the function during a clone operation. This leads to a test failure in `t5310-pack-bitmaps.sh` with the error "fatal: writev error: Invalid function argument".

Randall included the exact build command showing the attempted configuration, along with other platform-specific flags. The problem seems isolated to NonStop's handling of the `writev()` system call, which may not be fully supported on this platform. As the maintainer responsible for keeping Git working on NonStop, Randall is seeking input on why the configuration flag isn't having its intended effect. The issue appears to be a build system quirk rather than a broader architectural problem, but it does prevent successful operation on this niche platform.