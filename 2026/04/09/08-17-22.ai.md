This is a platform compatibility discussion about `writev()` optimizations in Git's network code, continuing the thread about test failures in Git 2.54.0-rc1 on NonStop systems. Patrick Steinhardt responds to brian m. carlson's analysis with a proposed technical solution.

Patrick agrees with brian m. carlson's position that platforms with limited `writev()` support should use NO_WRITEV rather than compromising the optimization for all platforms. However, he proposes an alternative long-term solution that would handle platforms with small SSIZE_MAX values more gracefully. His patch introduces a new `xwritev()` function that splits large iovec entries that exceed MAX_IO_SIZE, falling back to `xwrite()` when needed while still using `writev()` for smaller chunks.

The patch demonstrates a middle ground between Jeff King's proposed approaches - it maintains the `writev()` optimization where possible while handling platform limitations. Patrick has tested it with an artificially small MAX_IO_SIZE (128 bytes) and asks Randall S. Becker to verify whether NonStop's limitation applies to individual iovec entries or the total size. This represents a more sophisticated approach than the current NO_WRITEV workaround while still preserving the performance benefits on capable platforms.

The discussion continues to balance performance optimization against platform compatibility, with Patrick's contribution offering a potential path forward that could satisfy both concerns. His patch maintains the key insight from brian m. carlson's analysis about reasonable size expectations while providing a more robust implementation than the current workaround. The thread remains open to either approach (NO_WRITEV or runtime fallback) with Patrick expressing willingness to pursue either direction based on consensus.