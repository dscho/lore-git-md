Thread identity
Subject: [PATCH v2] submodule: fix remote matching with url.<base>.insteadOf
Series version: standalone patch (v2)
Originating author: Éric NICOLAS
Thread type: bugfix patch

Core problem or goal
Fix a regression (introduced in ca62f524c1, 2025) where `git submodule update --remote` fails to match the submodule’s configured remote when URL aliases (`url.<base>.insteadOf`) are in use. The `.gitmodules` file stores the unexpanded alias (e.g., `local:path`), but the submodule’s remotes report the resolved URL (e.g., `/full/path`), causing the lookup to fail and fall back to the default remote.

Current status
Under review; v2 posted addressing Junio C Hamano’s feedback on the initial patch. The v2 commit message now explicitly references the original commit (ca62f524c1) and clarifies the fallback behavior’s limitations (e.g., failing when no "origin" remote exists). The implementation reuses the `url` variable instead of introducing `url_to_match`, simplifying the code. The test is streamlined but functionally unchanged. No new objections or open questions remain; the patch is ready for merging pending final nitpicks.

Key technical details
- **Files touched**: `remote.c` (core logic), `t/t7406-submodule-update.sh` (test).
- **Function modified**: `repo_remote_from_url()` resolves aliases via `alias_url()` before comparing URLs.
- **New behavior**: Submodules using aliased URLs correctly match their remotes instead of falling back to the default logic.
- **On-disk format**: unchanged.
- **Test coverage**: verifies the fix with a global `insteadOf` alias, submodule setup, and remote renaming. The test is simplified but retains full coverage.

Open questions and loose ends
None. Junio’s feedback on commit message clarity and code simplification has been addressed. The fallback behavior’s limitations (e.g., no "origin" remote) are now documented in the commit message.

Participant positions
- **Junio C Hamano**: Substantive review on v1 focused on commit message clarity and code polish. Feedback fully addressed in v2. Position: "LGTM with minor tweaks" (now resolved).

Related work
None.