This is a **bugfix patch** for `git rev-list --exclude-first-parent-only`, addressing a regression where explicitly listing commits on the command line (like `R1` in the example) caused the option to incorrectly exclude commits that should remain visible. The patch, authored by Junio C Hamano, modifies `process_parents()` in `revision.c` to break out of the parent-processing loop early when `--exclude-first-parent-only` is active and a parent commit has already been marked as `SEEN`. This prevents the exclusion logic from mistakenly marking commits as uninteresting when they were explicitly requested.

The fix is straightforward and includes a new test case in `t/t6012-rev-list-simplify.sh` that reproduces the reported issue. Jerry Zhang, who appears to have been involved in the original implementation of `--exclude-first-parent-only`, reviewed the patch and endorsed it with a `Reviewed-by`. His comment clarifies the intended use case of the option—tracking when a branch first split from a baseline—though he hasn’t weighed in on whether the fix might break other scenarios.

This patch is **well-motivated and narrowly targeted**, addressing a clear edge case without touching unrelated code. The test coverage is minimal but sufficient for the reported issue, and the review from a domain expert adds confidence. The only lingering question is whether the original author (no longer active) might have intended different behavior, but the fix aligns with the documented purpose of the option. It’s likely to be merged unless broader testing reveals regressions.