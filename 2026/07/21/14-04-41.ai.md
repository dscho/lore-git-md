This is a feature patch adding a `--[no-]edit` option to `git rebase --continue`. The patch introduces command-line flags (`--no-edit` and `--edit`) that control whether an editor is opened when continuing a rebase after conflict resolution, along with a new configuration option `rebase.noEdit` to set the default behavior. The flags apply only to the current `--continue` invocation, not to subsequent commits in the same rebase.

The motivation is straightforward: users sometimes want to accept the existing commit message without editing it, and the patch provides a way to skip the editor prompt. The implementation touches `builtin/rebase.c` and `sequencer.c`, the core files handling rebase logic, and adds tests in `t/t3436-rebase-more-options.sh` to verify the new behavior. The commit message notes that all tests were run locally, suggesting the author has validated the change.

The patch is small and focused, with clear documentation updates for both the man page (`git-rebase.adoc`) and config documentation (`rebase.adoc`). The new config option and flags follow Git’s established conventions for boolean options, making the interface intuitive for users familiar with other Git commands. There’s no indication of controversy or edge cases in the commit message, though the sequencer changes might warrant scrutiny from reviewers familiar with that subsystem.

This is a self-contained change with no dependencies on other ongoing efforts. It’s likely to appeal to users who frequently rebase and want to streamline their workflow, though the value of the feature may depend on how often users encounter the specific pain point it addresses. The patch appears well-structured and ready for review.