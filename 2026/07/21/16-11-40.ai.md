This is Junio C Hamano's review of the second patch in Harald Nordgren’s two-patch series fixing push tracking after remote rename. The review is **substantive** and identifies a **technical gap** in the patch’s logic: it only considers the `url` member of a remote when looking for a fallback, but should also consider the `pushurl` member (and ignore `url` when `pushurl` exists). Junio provides a concrete code suggestion for the upfront check and the loop condition, using a `check_url` variable to handle both cases. The review is precise, actionable, and grounded in the existing codebase’s behavior (e.g., `transport_get()` ensures `transport->remote` is never NULL, so a BUG() check is unnecessary). No other issues are raised; the rest of the patch appears uncontroversial. The author will need to revise the patch to address this oversight before it can proceed.