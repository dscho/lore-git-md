This is a **substantive review and regression confirmation** in the ongoing ODB performance regression thread. Jeff King (Peff) engages deeply with the reported issue, reproduces it with a minimal test case, and confirms the performance impact (12% slowdown even on fast filesystems). He bisects the regression to commit `a593373b09` (a nearby commit in the ODB rework series), differing slightly from Wolfgang Kritzinger’s `8384cbcb4c` but pointing to the same root cause: the ODB abstraction changed the object lookup order from "pack-first, loose-second *per backend*" to "pack-loose, pack-loose *across backends*", causing redundant loose object checks.

Peff’s review is **highly actionable**: he provides a concise reproduction recipe (using `GIT_ALTERNATE_OBJECT_DIRECTORIES` and `strace`), quantifies the regression, and identifies the problematic commit. He also frames the tension between the ODB abstraction’s design goals (independent, abstract backends) and the practical need for performance optimizations (prioritizing pack lookups). The review explicitly calls out Patrick Steinhardt (the ODB rework author) as the person to resolve this, noting that Peff hasn’t closely followed the refactoring.

Key technical details for follow-up:
- **Subsystems**: ODB backend iteration, `git rev-list`/`receive-pack` connectivity checks.
- **Files/symbols**: `find_pack_entry()` (refactored in `a593373b09`).
- **Old behavior**: Check all packs before loose objects *per backend*.
- **New behavior**: Check packs *and* loose objects *per backend* before moving to the next backend.
- **Performance impact**: 12% slowdown on fast filesystems; worse on NFS (where loose object checks are expensive).
- **Reproduction**: Use `GIT_ALTERNATE_OBJECT_DIRECTORIES` with a packed repository.
- **Open question**: How to reconcile the ODB abstraction’s design with the need for cross-backend optimizations (e.g., "fast" vs. "slow" lookups).

This email **advances the thread significantly** by confirming the regression, narrowing the problematic commit, and setting up a clear technical discussion about trade-offs in the ODB rework. Expect Patrick Steinhardt’s response to focus on whether the abstraction can accommodate this optimization or if a targeted fix is needed.