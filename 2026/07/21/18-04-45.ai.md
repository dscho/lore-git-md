This is a maintainer feedback email on the `[PATCH] rebase --continue: add --[no-]edit option` feature patch. Junio C Hamano raises substantive concerns about the design and scope of the proposed change, particularly the new `rebase.noEdit` configuration option.

Junio opens with a dismissive "Meh" and points out that the existing workaround (`GIT_SEQUENCE_EDITOR=: git rebase --continue`) already achieves the same goal without adding new flags or configuration. This suggests skepticism about the necessity of the feature. He then zeroes in on the `rebase.noEdit` config option, calling it "especially concerning" because it encourages users to default to not editing commit messages during rebase, which could lead to important changes going unrecorded.

The feedback identifies two critical edge cases that the patch does not address: (1) how `rebase.noEdit` interacts with interactive rebase steps like `edit` or `reword`, where the user’s original intent was to modify the commit message, and (2) whether the behavior differs depending on whether the rebase stopped due to a conflict or another reason. Junio notes that the current test coverage only validates the conflict-resolution path, leaving other scenarios untested. He explicitly asks whether users would need to remember to pass `--edit` in these cases, implying that the patch’s design could create confusion or require awkward workarounds.

The tone is constructive but firm, signaling that the patch as written is unlikely to be accepted without addressing these issues. The feedback does not reject the feature outright but makes it clear that the current implementation is under-motivated and incomplete. Hugo Sales will need to either expand the scope of the patch to handle the edge cases, justify why they should be ignored, or reconsider whether the feature is worth pursuing at all. The thread is now in a holding pattern until the author responds.