Thread identity
Subject: Performance regression in `git receive-pack` connectivity check after ODB rework (commit 8384cbcb4c)
Series version: N/A (regression report)
Originating author: Wolfgang Kritzinger (Atlassian)
Thread type: bug report / performance regression

Core problem or goal
A measurable performance regression in server-side Git operations (pushes) was introduced by the ODB abstraction rework, specifically commit `a593373b09` (previously bisected to `8384cbcb4c`). The regression manifests as excessive failing `open()` calls for loose objects across multiple ODB backends (quarantine and main object stores), particularly costly on NFS-backed deployments. The root cause is a change in object lookup order: the ODB rework shifted from a "pack-first, loose-second" strategy *per backend* to a "pack-loose, pack-loose" strategy *across backends*, causing redundant loose object checks even when objects exist in packs. The regression is now reframed as an architectural opportunity to optimize the ODB abstraction for specialized use cases like connectivity checks, which prioritize traversal success over general-purpose object lookups, while also addressing a broader goal of making ODB transactions self-contained.

Current status
Regression confirmed and quantified (12% slowdown on fast filesystems; worse on NFS). The problematic commit is definitively identified as `a593373b09`, with `8384cbcb4c` as an earlier precursor. The discussion has shifted from a targeted performance fix to a broader architectural redesign of ODB transactions. Patrick Steinhardt proposes bypassing the ODB source list entirely for writes during active transactions, routing them directly to the transaction object instead of the primary ODB source. This would prevent loose-object checks in the quarantine directory during connectivity traversals, as objects written to the transaction wouldn’t be visible to the ODB layer until commit. The proposal is forward-looking and tied to a planned transaction-aware ODB overhaul, with implementation delayed until late July or August due to Patrick’s absence. Junio’s earlier suggestion to distinguish "fast" and "slow" lookups within backends remains unaddressed but may be rendered moot by Patrick’s transaction-focused approach.

Key technical details
- **Subsystems touched**: ODB (object database), `git receive-pack` (connectivity check), `git rev-list` (used in reproduction), backend iteration logic, transaction handling.
- **Files/symbols**: `find_pack_entry()` (refactored in `a593373b09`), `do_oid_object_info_extended()` (in `odb.c`), `packfile_store_prepare()`, `git rev-list` connectivity check logic, transaction object routing.
- **Old behavior**: "Pack-first, loose-second" lookup *per backend* (check all packs before loose objects in a single backend), extended across backends (all packs in all backends before any loose objects).
- **New behavior**: "Pack-loose, pack-loose" lookup *across backends* (check packs *and* loose objects in one backend before moving to the next), causing redundant loose object checks in earlier backends (e.g., quarantine) before finding objects in packs in later backends (e.g., main object store).
- **Performance impact**: 12% slowdown on fast filesystems; significantly worse on NFS due to expensive loose object checks. Measured via `strace` counting failing `open()` calls.
- **On-disk format**: No changes reported.
- **Test coverage**: Reproduction requires `GIT_ALTERNATE_OBJECT_DIRECTORIES` with a packed repository; NFS-specific or large-repository testing may be needed for full validation. Peff’s reproduction recipe uses `strace` and `git rev-list`.
- **Proposed optimizations**:
  - **Transaction-aware ODB writes**: Route writes during active transactions directly to the transaction object, bypassing the ODB source list. This would prevent loose-object checks in quarantine directories during connectivity traversals.
  - **Backend-level "fast" vs. "slow" lookups** (Junio’s proposal): Distinguish between pack and loose object checks within each backend to enable cross-backend optimizations (e.g., all packs before any loose objects) without violating the abstraction. This remains unaddressed but may be superseded by Patrick’s transaction-focused approach.
  - **Pre-loaded loose object table/Bloom filter** (Junio’s proposal): Short-circuit expensive `lstat()` calls during connectivity checks by caching loose object names in memory, tolerating false positives. Also unaddressed.

Open questions and loose ends
- Will Patrick’s transaction-aware ODB write proposal fully resolve the regression, or will additional optimizations (e.g., Junio’s "fast/slow" lookups or pre-loaded loose object tables) still be needed for other use cases?
- How will the transaction-aware ODB write proposal interact with existing ODB abstraction goals, particularly the independence of backends? Will it require changes to how transactions are managed or committed?
- What are the performance trade-offs of Patrick’s proposal, and how will they be validated? Will the fix introduce new edge cases or regressions in other scenarios (e.g., transaction rollbacks or concurrent operations)?
- Is the two-phase lookup proposed by Taylor Blau still viable, or does Patrick’s proposal render it unnecessary? If the latter, how will the ODB abstraction accommodate other specialized use cases (e.g., connectivity checks) that may not align with transaction boundaries?
- What is the timeline for implementing and testing Patrick’s proposal? The regression may persist until late July or August, pending his return.
- How should the ODB abstraction balance general-purpose lookups with specialized use cases like connectivity checks, where false positives are tolerable? Should such optimizations be exposed as hints or remain internal to the ODB layer?
- Will this require a revert of `a593373b09`, a new optimization layer, or a broader redesign of the ODB lookup and transaction strategy?

Participant positions
- **Wolfgang Kritzinger (Atlassian)**: Reported the regression, provided initial bisect result (`8384cbcb4c`), and hypothesized the root cause (lookup order change). No substantive review or fix proposed yet.
- **Jeff King (Peff)**: Confirmed the regression with a minimal test case, quantified the performance impact (12% slowdown), and identified the problematic commit (`a593373b09`). Framed the tension between ODB abstraction design and performance optimizations. Explicitly called for Patrick Steinhardt’s input.
- **Taylor Blau**: Provided a **substantive technical analysis** reconciling the two bisect results and explaining the root cause at the code level. Proposed a two-phase lookup fix that restores performance but breaks the ODB abstraction. Acknowledged the tension and deferred to Patrick Steinhardt for a principled resolution. His review is high-weight (engages deeply with behavior, edge cases, and correctness).
- **Junio C Hamano**: **High-weight maintainer perspective** reframing the regression as an opportunity to optimize the ODB abstraction for specialized use cases like connectivity checks. Proposed two directions: (1) distinguishing "fast" and "slow" lookups within backends to enable cross-backend optimizations, and (2) pre-loading loose object names into an in-core table or Bloom filter to short-circuit expensive `lstat()` calls. His email shifts the focus from a narrow fix to a broader design discussion about accommodating specialized use cases within the abstraction.
- **Patrick Steinhardt**: **High-weight author of the ODB rework** proposing a transaction-aware ODB write mechanism to resolve the regression. His solution bypasses the ODB source list for writes during active transactions, routing them directly to the transaction object. This would incidentally resolve the performance issue by avoiding loose-object checks in quarantine directories during connectivity traversals. His proposal ties the regression fix to a larger refactoring effort (transaction-aware ODB writes) and delays implementation until late July or August. He does not directly address Junio’s "fast/slow" lookup idea but implies it may be unnecessary under his approach.

Related work
- **ODB abstraction rework**: Ongoing effort led by Patrick Steinhardt to enable pluggable ODB backends. This regression is a side effect of that work, specifically the refactoring of `find_pack_entry()` in `a593373b09` and its interaction with `do_oid_object_info_extended()`.
- **Transaction-aware ODB writes**: Planned refactoring effort by Patrick Steinhardt and Justin Tobler to make ODB transactions self-contained, eliminating the need to modify the ODB source list during transactions. This effort is now the proposed path to resolving the regression.
- **NFS deployments**: The regression is particularly impactful for NFS-backed Git servers (e.g., Atlassian Bitbucket), a common deployment pattern for large-scale hosting. The performance impact is exacerbated by the high cost of loose object checks on NFS.
- **Connectivity checks**: The `git rev-list` connectivity check is a non-standard use of the revision traversal machinery, optimized for a single bit of information (traversal success) rather than the full object list. This use case may justify application-specific optimizations that are not applicable to general-purpose object lookups.