This thread is a **performance regression report** from Wolfgang Kritzinger at Atlassian, detailing a slowdown in `git receive-pack` (server-side push handling) after upgrading from Git 2.50 to 2.54. The issue stems from commit `8384cbcb4c`, part of the ongoing ODB (object database) rework, which altered how Git checks for object connectivity. The regression is particularly acute on NFS-backed deployments, where excessive `open()` calls for loose objects—even when those objects exist in packs—create measurable overhead.

The report is **high-impact**: it ties a concrete performance degradation to a specific commit, affects a widely used subsystem (ODB), and has downstream consequences for platforms like Bitbucket. The author’s hypothesis—that the lookup order shifted from "pack-first, loose-second *per backend*" to "pack-loose, pack-loose *across backends*"—frames the problem as an unintended side effect of the ODB abstraction effort. No fix is proposed yet, but the thread is likely to attract attention from **Patrick Steinhardt** (ODB rework lead) and **Jeff King** (low-level performance expert), as well as other contributors familiar with the ODB or NFS-specific quirks.

So far, the thread is **diagnostic**: the regression is confirmed, the commit is identified, and the mechanism is hypothesized. Next steps will likely involve **confirming the bisect result**, debating whether the current behavior is a bug or an acceptable trade-off, and exploring optimizations (e.g., restoring the per-backend lookup order or caching pack presence). If the regression is accepted as a blocker, expect a follow-up patch series to address it. For now, the thread is **open and unresolved**, with no decisions or approvals yet.