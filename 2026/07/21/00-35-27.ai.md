This is a **substantive review** of Junio C Hamano’s bugfix patch for `git rev-list --exclude-first-parent-only`. Jerry Zhang, the original author of the `--exclude-first-parent-only` option (introduced in 2022), has reviewed the patch and provided a **Reviewed-by** trailer.

The review confirms that the fix aligns with the intended behavior of the option: `--exclude-first-parent-only` should exclude only the first-parent ancestry path from a merge commit, while still including commits reachable from explicitly specified commits (like `R1` in the bug report) even if they are not part of that excluded path. Jerry’s rationale clarifies the distinction between this option and tools like `git when-merged`, noting that `--exclude-first-parent-only` answers the question "when did a branch first split off from the baseline" rather than "when was a branch merged into the baseline."

The patch itself remains unchanged from Junio’s proposal: it modifies `process_parents()` in `revision.c` to break out of the parent-processing loop early when `--exclude-first-parent-only` is active and a parent has already been marked as `SEEN` or fails to parse. The included test case in `t/t6012-rev-list-simplify.sh` reproduces the reported edge case and verifies the fix. Jerry’s review does not raise any concerns about regressions or unintended side effects, though the original thread’s open questions—such as whether additional test coverage is needed for other edge cases (e.g., octopus merges)—remain unaddressed.

This review significantly increases the likelihood of the patch being merged, as it comes from the feature’s original author and confirms the fix’s correctness. The patch is now effectively "ready to cook" unless other reviewers identify issues.