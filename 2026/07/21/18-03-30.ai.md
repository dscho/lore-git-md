This is a reply in the `git send-email` completion thread. Yury Norov responds to Junio C Hamano’s skepticism about the patch’s necessity by clarifying the real-world scenario that motivated it: a 14-year-old tag with a purely hexadecimal name (`05c69d298c96...`) that Git itself warns is ambiguous. This tag, which cannot be deleted without disrupting history, currently prevents any patch file whose name shares its prefix (e.g., `0001-*.patch`) from appearing in completion candidates. Yury acknowledges Junio’s point that the original test case (a long hex string) was artificial but argues the underlying problem—completion logic stopping after finding a matching ref, even when matching files exist—is real and demonstrated by the existing tag.

The email also proposes expanding the test to cover more realistic cases: a patch file (`0001.patch`), a tag (`0-tag`), and a branch (`0-branch`), ensuring all are offered as completion candidates. Yury seeks confirmation on whether this broader test coverage is sufficient before posting a v2. The patch’s core implementation (the `__gitcomp_file_append()` helper) remains uncontested, but the discussion has shifted from technical correctness to whether the problem is widespread enough to justify the change. No new technical objections have been raised; the thread awaits input from completion-script experts or other contributors to break the stalemate.