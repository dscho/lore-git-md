This thread is a straightforward bugfix for a regression in `git submodule update --remote` when URL aliases (`url.<base>.insteadOf`) are used. Éric NICOLAS identified the issue—where submodules with aliased URLs fail to match their remotes because `.gitmodules` stores the unexpanded alias while remotes report the resolved URL—and posted a patch to fix it.

Junio C Hamano reviewed the initial version, requesting minor improvements to the commit message and a small code simplification. Éric addressed these in v2, which now reuses the `url` variable instead of introducing a redundant one, and clarifies the fallback behavior’s limitations in the commit message. The test coverage remains robust, verifying the fix with a global `insteadOf` alias and submodule setup.

The patch is now a well-scoped, uncontroversial fix with no remaining open questions. It touches `remote.c` and the test suite, and should be ready for merging once any final nitpicks are resolved. No further changes are expected beyond minor polish.