This is a feature patch adding a `--[no-]edit` option to `git rebase --continue`. The patch introduces command-line flags (`--no-edit` and `--edit`) that let users skip or force the editor prompt when continuing a rebase after conflict resolution, along with a new configuration option `rebase.noEdit` to set the default behavior. The flags apply only to the current `--continue` invocation, not to subsequent commits in the same rebase.

The implementation touches the expected files: `builtin/rebase.c` for CLI parsing, `sequencer.c` for the core logic, and adds tests in `t/t3436-rebase-more-options.sh` to verify the new behavior. The documentation updates (`git-rebase.adoc` and `config/rebase.adoc`) are clear and follow Git’s established conventions for boolean options. The patch is self-contained, with no dependencies on other ongoing efforts like ODB abstraction or reftable.

The change is well-motivated for users who frequently rebase and want to streamline their workflow by skipping the editor prompt. The author has included thorough test coverage, including edge cases like the interaction between `--edit` and the `rebase.noEdit` config option. The sequencer logic is careful to reset the `edit` flag after each `--continue` invocation, ensuring the option doesn’t persist unintentionally across multiple commits in the same rebase.

This looks like a clean, focused addition that addresses a real workflow pain point. The patch is ready for review, and while reviewers may scrutinize the sequencer logic for edge cases (e.g., interactions with `--autosquash` or `--signoff`), there’s no indication of major technical debt or controversy. The naming of the config option (`rebase.noEdit`) aligns with Git’s conventions, though reviewers might suggest alternatives like `rebase.continueNoEdit` for specificity. The patch appears well-crafted and worth attention from maintainers.