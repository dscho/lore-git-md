This is the first email in the thread (thread root), so there is no prior AI thread summary to update.

---

Thread identity: Junio C Hamano’s "What’s cooking in git.git" report for July 2026 (#09), summarizing the state of all in-flight topics in the Git project. Originating author: Junio C Hamano. Thread type: meta-summary (not a patch series).

Core problem or goal: Provide a comprehensive, machine-readable snapshot of every in-flight topic in the Git project as of July 2026, including integration status, dependencies, subsystems touched, new APIs, configuration keys, command-line options, and test infrastructure. The report is explicitly designed for the Summarizer-Brief audience, ensuring future AI sessions can reconstruct the state of the project without access to prior emails.

Current status:
- **Graduated to `master` this cycle**: 15 topics (listed below).
- **Cooking in `next`**: 23 topics (candidates for the next release).
- **Stalled**: 7 topics (waiting for response or reroll).
- **Ejected due to conflicts**: 2 topics.
- **On hold**: 1 topic (`tc/replay-linearize`).
- **Needing review**: 18 topics.

Key technical details:
**Subsystems touched**: ODB (object database, including pluggable backends, transactions, loose/packed object writing, geometric repack, cruft packs, MIDX, incremental repack, custom base, bitmap optimizations), refs (reftable backend, refspec, ref-filter memoization), sequencer (rebase, replay, fixup options), revision walk (topo-levels, `--no-walk` pathspec filtering), tempfile/lockfile (repo-scoped helpers), trace2 (ancestry tracking), config (transactional interface, includeIf conditions, migration of globals into `struct repo_config_values`), HTTP (transport-helper hardening), transport (protocol v2 `object-info` and `no-ref-delta` capabilities), diff (userdiff drivers, relative cached unmerged paths), sparse-index (ITA crash fix), pack-objects (delta size truncation, `size_t` portability), fast-import (usage fixes), subtree (v2 config rejection), gitweb (hash shortening), and test infrastructure (modernization, linters, new helpers).

**New APIs**:
- ODB: `odb_for_each_object()` with object filters, `odb_write_object_ext()`, `odb_optimize_required()`, `odb_optimize()`.
- Repository management: `repo_hold_lock_file_for_update()`, `repo_create_tempfile()`.
- Revision walk: `next_commit_to_show()`, `get_commit_action()` (pure predicate), `paint_state` counters.
- Reftable: `reftable_table_init_ref_iterator()`.
- Config: Transactional interface (`begin`, `prepare`, `commit`, `fsync`, `env`, `flags`).

**New data structures**:
- `struct repo_discovery`, `struct fast_import_state`, `struct paint_state`, `struct repo_config_values` (holding migrated globals like `core.ignorecase`, `core.excludesfile`).

**New files**:
- `lib/` directory for `libgit.a` sources.
- ODB backends: `odb/source-loose.c`, `odb/source-packed.c`.

**Configuration keys introduced or modified**:
- `fetch.submoduleErrors` (non-fatal submodule fetch errors).
- `log.graphIndent` (graph indentation in `git log`).
- `branch.<name>.deleteMerged` (delete merged branches).
- `diff.<driver>.process` (external diff drivers).
- Transactional config: `core.configLockTimeout`, `core.packedRefsTimeout`.
- Migrated globals: `core.ignorecase`, `core.excludesfile`, `core.editor`, `core.pager`, `core.askpass`, `core.autorebase`, `core.pushDefault`, `core.objectCreationMode`, `core.trustExecutableBit`, `core.hasSymlinks`.

**Command-line options introduced or modified**:
- `git bisect --reset-when-found[=<where>]`.
- `git branch --delete-merged`.
- `git cat-file --batch-command remote-object-info`.
- `git checkout --track=... --fetch`.
- `git fetch --submodule-errors`.
- `git gc --odb-optimize`.
- `git log --graph --graph-indent`, `git log -L --check`, `git log -L --stat`, `git log -L -G`.
- `git rebase --empty`.
- `git replay --linearize`.
- `git send-pack --no-ref-delta`.
- `git repack --geometric --cruft`.

**New commands**:
- `git repo info` (exposes path keys: `path.git-prefix`, `path.grafts`, `path.index`, `path.hooks`, `path.objects`, `path.superproject-working-tree`, `path.toplevel`).
- `git history squash` (new subcommand).

**Environment variables**:
- `GIT_CONFIG_PARAMETERS` (implied by config transaction env).

**New protocols**:
- Protocol v2: `object-info` capability (for `git cat-file --batch-command remote-object-info`).
- `no-ref-delta` capability (for `git send-pack`).

**New userdiff drivers**: Swift.

**New test infrastructure**:
- Helpers: `test_stdout_line_count`, `greplint`, `nth_line()`, `test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`, `test_grep`.
- Linters: `greplint` (detects bare `grep` assertions).
- Prerequisites: `TRACE2_ANCESTRY` (refined for user-mode emulation).
- Wrappers: `t/u-odb-inmemory` (for writing objects), `t/lib-httpd` atomic CGI helpers.
- Performance tests: `t/perf/p5334-reftable-tombstone.sh`.
- Coverage: `t6600` (clock-skew topologies), `t6099` (side-exhaustion regression).

**New hooks**: `pre-auto-gc`.

**New config includeIf conditions**: `worktree`, `worktree/i`.

**New commit message trailers**: `change-id` in `b4` cover template.

**Error handling and hardening**:
- Submodule fetch errors non-fatal via `fetch.submoduleErrors`.
- Hardening against corruption: reftable, ODB, revision walk (unmerged paths), diff (unmerged paths), pack-objects (size truncation), transport-helper (`dup()` failure), HTTP (`curl_easy_duphandle()` failure), `compat/pread` (`lseek()` failure), `last-modified` (`repo_parse_commit()` failure), `reftable/block` (`deflateInit()` failure), config (`launch_editor()` failure).
- Portability fixes: `submodule--helper` `%zu` format, `pack-objects` `size_t` vs `unsigned long`, `writev()` wrapper for NonStop, `MAX_IO_SIZE` handling, universal macOS builds via `RUST_TARGETS` and `lipo`.

**Performance fixes**:
- `unpack-trees` quadratic scan.
- `wt-status` quadratic insertion.
- `reftable` tombstone quadratic.
- `commit-graph` topo-levels slab propagation.
- `revision` `--no-walk` pathspec filtering.
- `diff` relative cached unmerged.
- `sparse-index` ITA crash.
- `pack-objects` delta size truncation.

**Test modernizations**:
- Replaced `test -f`/`test -d` with `test_path_*` helpers.
- Replaced bare `grep` with `test_grep`.
- Replaced pipelines with `test_stdout_line_count`.
- Modernized tests: `t1100`, `t1410`, `t7614`, `t9811`, `t7412`.

**Rust-related changes**:
- Cleanups: `CryptoHasher` `Clone` and `Drop` implementations.
- Universal macOS builds via `RUST_TARGETS` and `lipo`.

**Documentation style**: Synopsis-style conversion for `git format-patch`, `git imap-send`, `git send-email`, `git request-pull`, `git fast-export`.

**Dependencies**:
- `ps/odb-move-loose-object-writing` depends on `jt/receive-pack-use-odb-transactions`.

Open questions and loose ends:
- **Stalled topics**: Awaiting response or reroll for `ja/doc-synopsis-style-yet-more`, `kh/doc-trailers`, `sn/rebase-update-refs-symrefs`, `jt/config-lock-timeout`, `sk/userdiff-swift`, `tc/last-modified-bloom`, `js/coverity-unchecked-returns-fix`, `cc/fast-import-usage`, `ps/writev`, `tn/packfile-uri-concurrency`, `ps/odb-pluggable-housekeeping`, `ij/subtree-reject-v2-config`, `hn/branch-delete-merged`, `hn/checkout-track-fetch`, `za/completion-hide-dotfiles`, `ec/commit-fixup-options`, `kh/doc-replay-config`.
- **Ejected topics**: `fz/rebase-autosquash-empty`, `ps/libgit-in-subdir`, `mm/diff-process-hunks` (due to conflicts).
- **On hold**: `tc/replay-linearize` (waiting for author response).
- **Needing review**: `tl/gitweb-shorten-hashes-with-modes`, `kj/repo-info-more-path-keys`, `ps/odb-move-loose-object-writing`, `pw/rebase-fixup-fixes`, `js/pack-objects-delta-size-t`, `jk/diff-relative-cached-unmerged`, `mm/revision-pure-get-commit-action`, `pz/fetch-submodule-errors-config`, `gr/add-e-use-apply-api`, `mm/lib-httpd-cgi-safe`, `ds/sparse-index-ita-crash`, `zy/apply-abandoned-header-fix`, `ty/migrate-trust-executable-bit`, `kk/merge-base-exhaustion`, `ps/cat-file-remote-object-info`, `hn/history-squash`, `tb/midx-incremental-custom-base`, `mm/line-log-limited-ops`.

Participant positions:
- **Junio C Hamano** (maintainer): Authored the report, implicitly endorsing the integration status of all topics listed. No explicit objections or conditional approvals noted in this meta-summary.
- **Topic authors**: Positions are implicit in the submission of patches. No substantive review feedback is captured in this report (review positions are tracked in individual thread summaries).
- **Reviewers**: Not explicitly named in this report (review activity is tracked in per-topic threads).

Related work:
- **ODB abstraction**: Ongoing effort (Patrick Steinhardt), with multiple topics in flight (`ps/odb-for-each-object-filter`, `ps/odb-move-loose-object-writing`, `jt/receive-pack-use-odb-transactions`).
- **Reftable backend**: Multiple topics (`kk/reftable-tombstone-quadratic-fix`, `t/perf/p5334-reftable-tombstone.sh`).
- **Rustification**: `bc/rust-hash-cleanups` (cooking in `next`), `sn/osxkeychain-rust-universal` (graduated to `master`).
- **Test modernization**: `mm/test-grep-lint` (graduated), `sk/t1100-modernize`, `ml/t9811-replace-test-f`.
- **Documentation synopsis-style conversion**: `ja/doc-synopsis-style-yet-more` (stalled), `cc/doc-fast-export-synopsis-fix` (cooking in `next`).
- **Config transactional interface**: Multiple topics (`jt/config-lock-timeout` stalled, others cooking in `next`).
- **Submodule handling**: `dm/submodule-update-i-shorthand` (cooking in `next`), `pz/fetch-submodule-errors-config` (needing review).