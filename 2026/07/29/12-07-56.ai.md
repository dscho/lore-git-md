This is a minor follow-up to the third patch in Pablo Sabater’s GSoC series extending `` `git cat-file --batch-command` `` to support the `%(objecttype)` placeholder. The patch itself refactors `fetch-object-info.c` to dynamically request all supported object-info options (currently "size" and "type") instead of hardcoding "size" alone, preparing the client to parse the new "type" field from the server response.

The email exchanges only surface-level feedback: Chandra Pratap suggests two readability improvements—fixing a grammar nit in the code comment ("no need to check" should be a separate sentence) and replacing an explicit loop with the `for_each_string_list_item()` macro. Pablo Sabater accepts both suggestions. Neither comment affects functionality, correctness, or the patch’s role in the series; they are purely stylistic. No substantive discussion or objections remain on this patch.