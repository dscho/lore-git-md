This is a follow-up to Junio C Hamano’s `git add --resolved` series, specifically addressing a performance edge case in the `has_conflict_markers()` helper introduced in patch 3/4. The patch adds an early-exit check for binary files inside the line-scanning loop, preventing unnecessary full-file scans when conflict markers cannot exist in binary content.

The change is minimal: a single conditional in `merge-ll.c` that calls `buffer_is_binary()` on each line read. If the line is binary, the loop breaks immediately, avoiding further I/O and processing. The logic is sound and the implementation clean, with no new dependencies or behavioral changes beyond the performance optimization. This is a targeted fix for a real-world concern—scanning large binaries for conflict markers is wasteful—and the solution is appropriately scoped.

No review feedback has been posted yet, but the patch is uncontroversial and ready for integration. It does not alter the core `--resolved` functionality or any of the earlier patches in the series; it simply makes an existing helper more efficient. The commit message is concise but sufficient, and the diff is self-explanatory. This is a low-risk, high-value tweak that should be merged alongside the rest of the series. Key technical detail for follow-up: the new `buffer_is_binary()` check in `has_conflict_markers()`.