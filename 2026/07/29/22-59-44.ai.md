This is a **build system** patch that addresses a dependency-ordering issue in the Makefile, specifically for the `WITH_BREAKING_CHANGES=1` configuration. The patch moves the inclusion of `config.mak` (and related files) earlier in the Makefile so that configuration settings like `WITH_BREAKING_CHANGES` are available when determining which binaries to build. This ensures that `git-whatchanged` and `git-pack-redundant`—which are excluded when `WITH_BREAKING_CHANGES=1`—are not built, preventing stale artifacts from interfering with tests like `t1517-outside-repo.sh`.

The patch is a direct follow-up to the earlier discussion in the thread, where Jeff King (Peff) identified stale build artifacts as the root cause of test failures. While Peff’s two-patch series fixed the `t0014-alias.sh` failures by making the test resilient to the presence of deprecated commands, this patch tackles the underlying build system issue: the Makefile was reading configuration *after* generating the list of binaries to build, so `WITH_BREAKING_CHANGES` had no effect on the initial build targets. The change is minimal (moving four lines) and targeted, with no behavioral changes beyond ensuring the build system respects the configuration flag at the right time.

The motivation is clear: without this patch, users building with `WITH_BREAKING_CHANGES=1` may still encounter test failures due to stale artifacts, even after applying Peff’s fixes. The commit message explicitly ties the patch to the earlier thread, noting that it’s needed "on top" of Peff’s work to fully resolve the issue. This is a pragmatic, low-risk fix that aligns with the project’s goal of supporting the `WITH_BREAKING_CHANGES` configuration as contributors prepare for Git 3.0.

The patch touches only the Makefile and does not introduce new symbols, on-disk format changes, or user-visible behavior. It’s a build-system housekeeping change with no downstream impact beyond ensuring tests pass reliably in this configuration. The diff is straightforward, and the rationale is well-explained in the commit message. This looks like a well-crafted, necessary fix for a specific edge case in the build process.