This is Junio’s "What’s cooking in git.git" report for July 2026 (#13), summarizing the current state of integration branches and in-flight topics.

Key technical details for follow-up discussion:
- Integration branches: `maint`, `master`, `next`, `seen`.
- Topics graduated to `master` in this cycle: 9 small-to-medium patches, mostly memory-leak fixes (`bc/rust-hash-cleanups`, `jc/submodule-helper-avoid-zu`), `the_repository` removals (`ps/copy-wo-the-repository`, `ps/refs-wo-the-repository`, `ps/refspec-wo-the-repository`), graph indentation (`ps/shift-root-in-graph`), rebase notes handling (`pw/rebase-drop-notes-with-commit`), `remote-curl` simplification (`rs/remote-curl-simplify-push-specs`), `wt-status` quadratic insertion fix (`sc/wt-status-avoid-quadratic-insertion`), and `ref-filter` memoization (`td/ref-filter-memoize-contains`).
- New topics in `seen`/`next`: 12 patches, including test helpers (`sk/test-commit-body-helper`), segfault fixes (`jk/cat-file-batch-wo-type-fix`, `tc/merge-default-to-upstream-leakfix`, `af/clone-revision-v0-segfault-fix`, `dl/pack-bitmap-position-zero`), macOS regex leak workaround (`cl/regexec-macos-leak`), checkout autostash refinement (`hn/checkout-m-autostash-refine`), deprecated command test modernization (`jk/t0014-dynamic-deprecated-cmds`), Windows symlink leak fix (`js/mingw-symlink-net-share-leak`), merge-base ancestor tests (`ns/merge-base-is-ancestor-tests`), and a new `git add --resolved` feature (`jc/add-resolved`).
- Stalled topics: 5 series, notably `tb/repack-geometric-cruft` (geometric repack + cruft packs), `hn/checkout-track-fetch` (checkout --track=... fetch mode), and `za/completion-hide-dotfiles` (path completion hides dotfiles by default).
- Cooking topics: 25 series, including ODB pluggable housekeeping (`ps/odb-pluggable-housekeeping`), `cat-file` remote object info (`ps/cat-file-remote-object-info`, `ps/cat-file-remote-object-info-type`), `diff` relative path fixes (`jk/diff-relative-cached-unmerged`, `jk/diff-relative-cached-unmerged-more`), sparse-index ITA crash fix (`ds/sparse-index-ita-crash`), and `git history` squash subcommand (`hn/history-squash`).
- Notable ejected/rejected topics: `fz/rebase-autosquash-empty` (conflicted with `pw/rebase-drop-notes-with-commit`), `ps/libgit-in-subdir` (too many evil merges).
- Key subsystem touchpoints: ODB abstraction (`ps/odb-*`), refs (`ps/refs-*`), rebase/sequencer (`pw/rebase-*`, `hn/history-squash`), diff (`jk/diff-*`, `mm/diff-process-hunks`), fsmonitor (`td/fsmonitor-darwin-cookie-flush`), and sparse-index (`tn/stash-avoid-sparse-index-expansion`, `ds/sparse-index-ita-crash`).