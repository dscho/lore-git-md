This is a **bugfix and test improvement** to the first patch in Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support `%(objecttype)` in remote-object-info queries. The patch is part of a five-patch series that builds on Eric Sunshine’s recently merged `remote-object-info` functionality.

Pablo responds to Chandra Pratap’s review by addressing two issues: a **style nit** and a **test portability concern**. The error message string (`"object-info: protocol error, expected to get oid, not '%s'"`) is restored to a single line to preserve grep-ability, and the test now uses Git’s `test_file_size` helper instead of `wc -c | xargs` for better portability. Both changes are mechanical and do not affect the core functionality, but they improve code consistency and test robustness.

The patch touches `protocol-caps.c` (server-side object-info handler) and `t/t5701-git-serve.sh` (test coverage for the new "type" capability). The changes are narrow and focused: the server now correctly advertises and transmits object type metadata when requested, and the test verifies this behavior in a more portable way. No new symbols or behavior changes are introduced beyond what was already described in the thread summary.

This update is unlikely to be controversial. The review feedback was constructive and technical, and Pablo’s response addresses it directly. The series remains under review, with the remaining patches still awaiting feedback. If the other patches receive similar attention, this effort looks like a well-scoped, incremental improvement to Git’s remote-object-info protocol.