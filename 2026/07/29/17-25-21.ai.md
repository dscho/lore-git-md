This is the first patch in Junio C Hamano’s `git add --resolved` series (v2, 1/4). It is a **refactoring** that corrects the indentation of the `skip_submodule()` function in `read-cache.c`, which was inadvertently misaligned in a previous commit (a16c4a245a, February 2026). The patch is purely mechanical—no logic changes, only whitespace adjustments to align the function with the surrounding code style.

The misindented block was introduced when support for `--force` with `ignore=all` submodules was added. The patch realigns the function’s body, including its conditional blocks, trace statements, and error messages, to match the prevailing indentation style in the file. This cleanup is preparatory for the rest of the series, which touches the same file (`read-cache.c`) in later patches.

The change is uncontroversial and low-risk, as it does not alter behavior. It is a typical housekeeping patch that ensures code readability before further modifications. No test failures or behavioral regressions are expected. The patch is ready for integration and does not depend on any follow-up changes in the series.