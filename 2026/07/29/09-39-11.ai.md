This is a **substantive review** of the test patch adding coverage for `git merge-base --is-ancestor`. Phillip Wood raises a single technical point: the new tests create a dedicated repository (`is-ancestor`) instead of reusing the commits already set up by the first test in `t/t6010-merge-base.sh`. He questions whether this is necessary, noting that none of the existing tests in the file do this, and suggests the patch could be simplified by leveraging the existing test history. He otherwise calls the patch "a very useful improvement" and praises the test coverage.

The review does not object to the patch’s substance or correctness, but it does identify a potential refactoring opportunity that could reduce test boilerplate. The patch remains uncontroversial and ready for integration; the author may choose to address the feedback in a follow-up or leave it as-is, since Phillip explicitly says the current version is already an improvement. Key identifiers: test file `t/t6010-merge-base.sh`, `--is-ancestor` option, exit codes 0/1/128, and the existing test setup at the top of the file.