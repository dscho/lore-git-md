This thread is about a small but important bugfix for `git maintenance`. The command has been failing since October 2025 when users try to use the documented `"none"` strategy, either explicitly or as a fallback. David Lin’s patch restores the expected behavior with a two-line code change and a few lines of test coverage.

Junio has reviewed the patch and noted only a minor procedural issue: the author’s email in the patch header doesn’t match the sign-off address. He suggested adding an in-body `From:` line to align them, but this is purely housekeeping and doesn’t affect the patch’s technical content. No other feedback has been given yet, so the fix itself appears uncontroversial and ready for integration once the email formatting is addressed.