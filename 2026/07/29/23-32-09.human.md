This is an RFC patch series proposing a breaking change for Git 3.0: restricting hex object IDs to lowercase only. The author, brian m. carlson, argues that Git has always emitted object IDs in lowercase, but the parser currently accepts both cases, leading to compatibility and security issues in external software that assumes unique hex representation.

The series introduces new functionality in the hex-parsing code to enforce lowercase-only object IDs when a "breaking changes mode" is enabled. It touches 19 files, including core utilities like `hex.c`, `object-name.c`, and various built-in commands. The last patch fixes the only test failure caused by this change, suggesting minimal disruption to existing behavior.

The motivation is clear: uppercase hex object IDs create ambiguity and potential security risks. The author notes that only one test fails with this change, implying the impact on Git's own codebase is small. However, the RFC status indicates uncertainty about whether the community wants this change at all.

This is a foundational change that would affect nearly all Git operations involving object IDs. While the patch series appears technically complete, the decision to proceed will likely hinge on broader ecosystem considerations and whether the benefits outweigh the disruption to external tools. The timing (targeting Git 3.0) suggests the author sees this as a major version change appropriate for breaking modifications.