This is a minor follow-up in Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder. The email exchanges two surface-level review comments on the third patch of the five-patch series.

Chandra Pratap suggests two small readability improvements: rewording a code comment to be grammatically correct (“no need to check” should be a separate sentence) and replacing an explicit loop with the `for_each_string_list_item()` macro. Pablo accepts both suggestions without objection. Neither change affects functionality; the patch remains a straightforward refactoring that prepares the client to request all supported object-info options (currently “size” and “type”) dynamically rather than hardcoding “size” alone. The series as a whole is still under review, with no maintainer decision or merge status reported.