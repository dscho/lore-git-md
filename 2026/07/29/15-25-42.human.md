This is a brief follow-up in the ongoing discussion about test failures with `WITH_BREAKING_CHANGES=1`. Phillip Wood confirms Jeff King’s explanation that the failures in both `t0014-alias.sh` and `t1517-outside-repo.sh` are caused by stale build artifacts—specifically, leftover `git-whatchanged` and `git-pack-redundant` executables from prior builds. These executables are no longer built when `WITH_BREAKING_CHANGES` is enabled, but if they remain in the build directory, they interfere with test expectations.

Phillip acknowledges that Jeff’s fix for `t0014-alias.sh` (which dynamically generates the list of deprecated commands and skips the test if fewer than two are available) makes sense, though he admits he only skimmed the patches. The thread remains focused on the transitional state before Git 3.0, where `WITH_BREAKING_CHANGES` creates a limbo period for tests that assume the presence of deprecated commands. No new technical details or disagreements emerge here—this is a straightforward confirmation of the root cause and appreciation for the fix. The discussion is effectively resolved, with no further action needed beyond ensuring clean builds in CI.