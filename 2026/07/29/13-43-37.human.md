This is a **bugfix follow-up** to the `git add --resolved` feature series. Junio C Hamano acknowledges two issues raised by Michael Montalbo in the review of patch 3/4:

1. **Option incompatibility logic** – The condition `0 <= addremove_explicit` in `die_for_incompatible_opt3()` is incorrect and should be replaced with `addremove` (the original variable) to avoid misclassifying `--no-all` and `--ignore-removal` as incompatible with `--resolved`. This restores the intended behavior where only explicit `--all` usage conflicts with `--resolved`.

2. **Test infrastructure** – The new test script `t/t2207-add-resolved.sh` is missing from `t/meson.build`, which would prevent it from being discovered by the Meson build system. Junio confirms this oversight and will add the entry.

Both fixes are straightforward and address the substantive review feedback. The series remains a well-motivated feature addition, but these corrections are necessary for correctness and test coverage. The thread is now focused on finalizing these adjustments before the feature can be considered ready for integration. No new technical questions or edge cases were introduced in this exchange.