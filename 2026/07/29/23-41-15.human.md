This is a **feature review** of Junio’s `git add --resolved` series, now in its second version. Michael Montalbo’s reply raises a thoughtful naming and design question but does not challenge the technical implementation.

Michael suggests renaming the option from `--resolved` to `--unmerged`, arguing that the current implementation selects unmerged index entries and applies a conflict-marker check, rather than determining whether conflicts are truly "resolved" in a semantic sense. He proposes `--unmerged` as a more accurate name that could later accommodate escape hatches like `--allow-conflict-markers` or `--skip-conflict-markers`. His feedback is framed as exploratory rather than prescriptive, acknowledging that `--resolved` may better reflect the user’s workflow during conflict resolution.

The review is **substantive** but not blocking: it engages with the design intent and naming clarity, not with correctness or edge-case handling. Michael does not object to the current behavior or implementation, nor does he identify new technical issues. The thread remains open for discussion on whether the option name should be adjusted, but the series itself is technically complete and addresses all prior review feedback. For a busy reader, this is a well-crafted feature that looks ready for further review or integration, with the naming question as the only open design point.