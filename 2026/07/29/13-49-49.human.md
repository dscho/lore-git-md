This is a brief but important follow-up in the `git add --resolved` series. Junio acknowledges two corrections to patch 3/4, both identified by Michael Montalbo’s review. The first fixes a logic error in the option incompatibility check: the condition `0 <= addremove_explicit` was incorrect and is now corrected to `0 < addremove_explicit`. The second ensures the new test script `t/t2207-add-resolved.sh` is discoverable in the Meson build by adding it to `t/meson.build`. These are minor but necessary fixes—no changes to the core `--resolved` feature itself, just corrections to its plumbing and testability. The tone is matter-of-fact and the fixes are uncontroversial, so this should clear the way for the series to proceed.