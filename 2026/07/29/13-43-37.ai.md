This is a **bugfix follow-up** to Junio C Hamano’s `git add --resolved` feature patch (3/4 in the series). The email acknowledges and corrects the two issues Michael Montalbo identified in his substantive review:

1. **Option incompatibility logic** – Junio confirms the `0 <= addremove_explicit` condition is "absolutely bogus" and suggests simplifying it to `addremove` (the original variable) to restore correct behavior. This fixes the false incompatibility errors for `--no-all` and `--ignore-removal` when combined with `--resolved`.

2. **Test infrastructure** – Junio agrees the new test script `t/t2207-add-resolved.sh` needs a `t/meson.build` entry, noting his oversight stems from rarely using the Meson build system.

The exchange is brief but decisive: both corrections are straightforward and will likely appear in a v2. The core feature remains unchanged; these are minor but necessary fixes for correctness and testability. Key technical details for follow-up:
- The `die_for_incompatible_opt3()` call in `builtin/add.c` will use `addremove` instead of `0 <= addremove_explicit`.
- `t/meson.build` will be updated to include the new test script.