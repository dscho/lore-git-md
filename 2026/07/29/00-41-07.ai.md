This is a **performance follow-up** in the ongoing `fetch.bundleURI` slowness thread. The discussion remains an open inquiry—no patches or decisions yet—but this reply from Ryszard Knop provides a concrete resolution to the immediate bottleneck and advances the conversation toward next steps.

Ryszard confirms that Brian M. Carlson’s suggestion to enable threaded `index-pack` via `pack.threads` was effective: the checkout time was halved (from 4–5 minutes to ~2 minutes), demonstrating that the single-threaded behavior was indeed a misconfiguration rather than an inherent limitation. The remaining single-threaded sections during packing are acknowledged as a separate issue, but the immediate performance problem is now resolved. This outcome validates Brian’s technical explanation and shifts the focus from diagnosing the bottleneck to exploring further optimizations (e.g., incremental processing, bundle generation parameters).

The thread also clarifies the security trade-off: while bare repositories were a faster alternative, they are discouraged due to security risks (e.g., users working directly with untrusted repos). Ryszard’s willingness to abandon the bare-repo workaround in favor of the optimized bundle workflow reinforces the value of `fetch.bundleURI` as a safer, more maintainable solution for CI environments.

Key technical details for future discussion:
- **Configuration**: `pack.threads=0` (auto-detect cores) is now confirmed as critical for performance.
- **Subsystems**: `index-pack.c` (threading logic, `nr_threads` symbol), packfile handling.
- **Behavior**: Bundles containing multiple refs still force processing of all objects in the pack, but threading mitigates the impact.
- **Open questions**: What causes the remaining single-threaded sections during packing? Could bundle generation (e.g., single-ref bundles, compression levels) further improve performance?
- **Related work**: Potential follow-ups on `index-pack` parallelization or incremental processing for large bundles.

The thread is now poised to explore deeper optimizations or document best practices for `fetch.bundleURI` in CI workflows. No objections or alternative explanations have been raised.