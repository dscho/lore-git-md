This is a **performance discussion** about `fetch.bundleURI` in the context of cloning the Linux kernel repository. The thread remains an open inquiry, but this reply from Ryszard Knop provides a practical resolution to the immediate bottleneck.

Ryszard confirms that Brian M. Carlson’s suggestion to enable multi-threaded `index-pack` via the `pack.threads` configuration halved the clone time, reducing it from 4–5 minutes to roughly 2–2.5 minutes. This improvement addresses the most pressing performance issue: the single-threaded `index-pack` process that was consuming 100% CPU for the majority of the operation. While Ryszard notes that some single-threaded sections remain during packing, the overall workflow is now significantly faster and no longer requires falling back to bare repositories, which Brian had flagged as a security anti-pattern.

The discussion now shifts from diagnosing the bottleneck to potential follow-up optimizations. The remaining single-threaded sections in `index-pack` could be a target for further performance work, though Ryszard implies this is a lower priority for now. The thread also leaves open the question of whether the original comparison (multi-ref bundles vs. single-branch clones) was fair, but the immediate performance gain from threading makes this less urgent.

Key takeaways for readers:
- The `pack.threads` configuration is critical for performance when using `fetch.bundleURI` with large repositories. Setting it to `0` (auto-detect cores) is recommended.
- The security concerns around bare repositories are valid; bundles remain the safer alternative for distributing pre-generated data.
- The thread is effectively resolved for Ryszard’s use case, but future work could explore further optimizations to `index-pack` or bundle generation.