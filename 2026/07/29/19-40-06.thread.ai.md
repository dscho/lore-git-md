Thread identity
Subject: [PATCH] maintenance: fix regression in "none" strategy handling
Series version: standalone patch
Originating author: David Lin <davidlin@stripe.com> (patch authored as davidzylin@gmail.com; in-body From: alignment suggested)
Thread type: bugfix patch

Core problem or goal
The `git maintenance` command, since commit d465be2327 (October 2025), errors out on any unrecognized maintenance strategy instead of defaulting to the documented `"none"` strategy. The parser function `parse_maintenance_strategy()` did not accept `"none"` as valid input, breaking both the documented fallback and explicit use of `git maintenance --strategy=none`.

Current status
Under review; no prior versions. The patch is a minimal two-line code change in `builtin/gc.c` (adding case-insensitive `"none"` to the parser) plus three lines of test coverage in `t/t7900-maintenance.sh` to verify that `none` is accepted with and without a schedule.

Key technical details
- Files touched: `builtin/gc.c`, `t/t7900-maintenance.sh`
- New behavior: `parse_maintenance_strategy()` now accepts `"none"` (case-insensitive)
- Old behavior: parser rejected `"none"`, causing `git maintenance` to fail
- On-disk format: unchanged
- Test coverage: explicit tests for `--strategy=none` and `--strategy=none --schedule=<any>`
- Symbols: `parse_maintenance_strategy()` (function)

Open questions and loose ends
- Housekeeping: author identity mismatch (`davidzylin@gmail.com` vs. `davidlin@stripe.com` sign-off) noted by Junio; in-body `From:` line suggested.

Participant positions
- Junio C Hamano: surface-level review; no technical concerns raised; requests in-body `From:` line to align author and sign-off addresses.

Related work
Regression introduced by commit d465be2327 (October 2025). No external dependencies.