This is a maintainer follow-up in the ongoing trace2 recursion crash thread. Junio checks on the status of the proposed fixes—specifically, whether Taylor Blau’s alternative (disabling `libintl_vsnprintf()` redirection for `xsnprintf()` in a MinGW-specific file) has been implemented. He reiterates his preference for keeping platform-specific logic out of shared code like `wrapper.c`, suggesting it belongs in a MinGW-specific file instead.

The email also reaffirms Junio’s earlier endorsement of Derrick Stolee’s argument that trace2 should avoid `die()` where possible, calling it a "worth considering" long-term hardening measure. He leaves open the question of whether other trace2 operations (beyond `xsnprintf()` and `xstrdup()`) could trigger `die()` in non-fatal scenarios, hinting at a potential broader audit of trace2’s error handling. However, this is not framed as a blocker for the current fixes.

No new technical details or objections are introduced. The thread remains collaborative, with all participants aligned on the root cause and the complementary nature of the two fixes. The next step is likely for Taylor or Derrick to post a revised patch implementing the `xsnprintf()` fix in a MinGW-specific file, while Derrick’s trace2 patch may proceed largely unchanged. The lack of progress update suggests the discussion is still in the design phase.