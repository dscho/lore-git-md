This is a bugfix patch for the `git maintenance` command. It restores the ability to use `"none"` as a valid maintenance strategy after commit d465be2327 (2025-10-24) accidentally broke it by tightening validation without recognizing the documented `"none"` option.

The patch is small and focused: it adds a single case-insensitive string comparison in `parse_maintenance_strategy()` to accept `"none"` and returns the existing `none_strategy` struct. The test file `t/t7900-maintenance.sh` is updated to verify that `"none"` works both as a standalone strategy and with the `--schedule` option.

The commit message clearly explains the regression and the fix. The change looks correct and uncontroversial—it simply restores documented behavior that was unintentionally removed. No follow-up discussion or edge cases are apparent from the patch itself.