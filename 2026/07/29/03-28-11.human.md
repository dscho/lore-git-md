This is a **substantive review** of the `git add --resolved` feature patch (3/4 in Junio’s series). Michael Montalbo identifies two potential issues in the implementation:

First, a logic error in the option-compatibility check: the condition `0 <= addremove_explicit` incorrectly treats `--no-all` (where `addremove_explicit` is 0) as equivalent to `--all`, causing false conflicts with `--resolved`. This would break existing workflows like `git add -u --ignore-removal` and misreport the incompatible option in error messages.

Second, a build-system oversight: the new test script `t/t2207-add-resolved.sh` is missing from `t/meson.build`, which means it won’t be included in the test suite when building with Meson. This is a straightforward omission but could delay discovery of regressions.

The review is concise but technically precise, focusing on correctness and integration rather than style. Both points are actionable and likely to require changes before the patch can proceed. The author (Junio) has not yet replied, so the thread is now waiting on clarification or a v2. This is a well-targeted review that surfaces real issues in a feature that otherwise looks solid.