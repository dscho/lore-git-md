This is a **substantive discussion** about test design in the ongoing thread adding test coverage for `git merge-base --is-ancestor`. Junio C Hamano replies to Phillip Wood’s earlier review, agreeing that reusing the existing test repository (set up by the first test in `t/t6010-merge-base.sh`) would avoid creating a dedicated one, but notes the trade-off: reusing the existing history adds constraints for future maintainers who might need to modify the initial test setup, while a separate repository keeps the test sets isolated and independent.

The exchange does not introduce new objections or technical concerns; it simply weighs the maintainability implications of the two approaches. Junio’s response suggests that either choice is acceptable, leaving the decision to the patch author (Nikolaus Schuetz) without signaling a preference. The patch remains queued in `seen` and is still uncontroversial, with no blocking feedback. Key identifiers: test file `t/t6010-merge-base.sh`, `--is-ancestor` option, and the existing test setup at the top of the file.