This is a **test infrastructure / build system** follow-up in the `WITH_BREAKING_CHANGES` thread. Phillip Wood confirms Jeff King’s (Peff) explanation for the test failures: stale build artifacts (`git-whatchanged` and `git-pack-redundant` executables) left over from prior builds are responsible for both the `t0014-alias.sh` and `t1517-outside-repo.sh` failures. Phillip acknowledges that Peff’s fix for `t0014-alias.sh` (using `git --list-cmds=deprecated` and the `HAVE_DEPRECATED` prereq) makes sense, though he has only skimmed the patches.

The email adds no new technical details but reinforces the root cause and signals agreement with the proposed solution. The thread remains fully resolved: the `t0014-alias.sh` failures are fixed by Peff’s series, and the `t1517-outside-repo.sh` failures are confirmed to be resolved by a clean build. No further changes to the tests are planned, and the discussion is now closed. The key takeaway for future AI summarizers is that the failures were environment-specific (stale artifacts) and that the `HAVE_DEPRECATED` prereq provides a transitional solution for the limbo state before Git 3.0.