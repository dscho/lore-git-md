This is a **follow-up discussion** in the `git add --resolved` series, focusing on two technical corrections to patch 3/4. Junio C Hamano clarifies and resolves the issues Michael Montalbo raised in his review:

1. **Option incompatibility logic** – The original condition `0 <= addremove_explicit` was incorrect, causing false errors when `--no-all` or `--ignore-removal` were combined with `--resolved`. Junio initially considered simplifying to `addremove` but realized this would misfire if `add_resolved` was set. The fix is to use `0 < addremove_explicit`, ensuring only explicit `-A/--all` triggers the incompatibility check.

2. **Test infrastructure** – Junio acknowledges the missing `t/meson.build` entry for the new test script `t/t2207-add-resolved.sh` and confirms it will be added. This ensures the test is discoverable in Meson builds.

The exchange is brief and decisive, with no further debate. Both fixes are minor but necessary for correctness and testability. The core `--resolved` feature remains unchanged; these are targeted adjustments to address review feedback. The discussion suggests a v2 will likely incorporate these changes. Key technical details for follow-up:
- `die_for_incompatible_opt3()` in `builtin/add.c` will use `0 < addremove_explicit`.
- `t/meson.build` will include `t/t2207-add-resolved.sh`.