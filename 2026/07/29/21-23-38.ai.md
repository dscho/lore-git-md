This is a reply in the ongoing `pack.allowDuplicateObjects` RFC thread, where Taylor Blau pushes back on Junio C Hamano’s characterization of packs containing duplicate objects as "broken." Taylor argues that such packs are not inherently broken—only unsupported by current client-side logic—and points to existing Git behavior (non-strict `index-pack`, shallow clones, and filtered clones) as precedent for accepting them. His tone is measured but firm, framing the debate as a matter of perspective rather than correctness.

The exchange centers on a philosophical disagreement: Junio views duplicate objects as a violation of Git’s integrity guarantees, while Taylor sees them as a pragmatic optimization (e.g., concatenating packs to avoid deduplication overhead) that Git’s internals are already hardened to handle (via `tb/pack-with-duplicates`). Taylor’s key point is that Git already tolerates duplicates in certain contexts (non-strict `index-pack`, shallow clones), so allowing them opt-in via configuration or flag would not introduce new risks—only extend existing behavior. He also clarifies a minor technical detail: Friel’s RFC patch used a shallow depth of `UINT32_MAX` to trigger non-strict `index-pack`, a workaround he acknowledges as "gross."

No new technical proposals or design changes emerge from this reply. The core questions remain unresolved:
- Should Git treat packs with duplicate objects as "broken" (Junio’s view) or merely unsupported (Taylor’s view)?
- If the latter, is the performance benefit of allowing them sufficient to justify the risk of proliferation?
- Can the feature be redesigned to deduplicate during `index-pack` (as Junio suggests) without sacrificing the optimization’s value?

The thread is now at a conceptual impasse, with Taylor’s reply serving as a rebuttal to Junio’s philosophical objection rather than a path forward. Future discussion may need to focus on concrete trade-offs (e.g., performance measurements, deduplication mechanisms) to break the deadlock. No changes to the patch or test suite are implied by this exchange.