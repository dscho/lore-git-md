This is the first patch in Junio C Hamano’s four-part series introducing `` `git add --resolved` ``. It is a **whitespace-only refactoring** that reindents the `skip_submodule()` helper in `read-cache.c` to match the surrounding code style. The mis-indentation was introduced in commit `a16c4a245a` (February 2026) and went unnoticed until now.

The patch touches 36 lines in `read-cache.c`, adjusting indentation levels and line breaks so that the function’s control flow aligns with the rest of the file. No logic or behavior changes are made; this is purely a visual cleanup to improve readability and consistency. The reindented code now clearly shows the function’s structure, including its early returns, submodule checks, and pathspec-matching logic.

Because this is a mechanical change with no functional impact, it is unlikely to generate controversy or require further review. It serves as a preparatory step for the rest of the series, ensuring that later patches that touch the same area start from a clean, consistently formatted base. The commit message is concise and factual, explaining the origin of the issue and the nature of the fix.