This is a reply in the ongoing GSoC patch series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series is now at v1, with all patches posted and follow-up fixes addressing earlier review feedback.

Pablo Sabater accepts Chandra Pratap’s suggestion to restructure the test in `t1017` for patch 4. The current test verifies that unsupported placeholders return an empty string, but with `%(objecttype)` now supported, the test needs updating. Chandra proposed splitting it into two separate tests: one to confirm the "type" capability is now supported, and another to ensure unsupported placeholders (like `objectmode`, `deltabase`, and `objectsize:disk`) still return empty strings. Pablo agrees this improves maintainability and will implement the change, grouping all remaining unsupported placeholders into a single test for clarity. This is a minor but sensible improvement to test design and does not affect the patch’s core functionality. The series remains under review.