This is a **bugfix and test improvement follow-up** to the first patch in Pablo Sabater’s GSoC series extending `git cat-file --batch-command` to support `%(objecttype)` in remote-object-info queries. The email responds to Chandra Pratap’s review of patch 1/5, addressing two issues:

1. **Style fix**: Pablo drops the unnecessary line-wrapping of the error message string (`"object-info: protocol error, expected to get oid, not '%s'"`), restoring grep-ability and adhering to project conventions. The change is trivial but corrects a self-admitted oversight caused by misconfigured editor settings.

2. **Test portability fix**: Pablo replaces the `wc -c <file | xargs` pattern with Git’s `test_file_size` helper in the test script (`t5701-git-serve.sh`), improving portability. He also notes he will apply the same fix to a later test in the series that uses the same pattern.

The response is concise and directly addresses the feedback without introducing new concerns. The changes are mechanical and uncontroversial, likely to be incorporated in the next iteration of the series. The series remains under review, with no maintainer decision or merge status reported. This patch is part of the ongoing effort to extend Eric Sunshine’s recently merged `git cat-file --batch-command` remote-object-info functionality.