This is a **substantive discussion** in the `git replay --linearize` thread, focusing on whether the feature should allow multiple positive refs (e.g., `git replay --linearize --onto main topic1 topic2`). The exchange is between Junio C Hamano (maintainer) and Elijah Newren (merge machinery expert), and it centers on the trade-offs between usability, predictability, and flexibility.

Junio pushes back against Elijah’s proposal to disallow multiple positive refs with `--linearize`. His argument hinges on two points:
1. **Unpredictable order is inherent to linearization**: Even with a single branch containing merges, the order of commits after linearization is arbitrary (e.g., which side of a merge comes first). Forcing users to split linearization into separate invocations doesn’t eliminate this ambiguity—it just moves it to a different workflow. The current behavior, where the revision walk determines the order, is a pragmatic default for cases where the order is inconsequential (e.g., octopus merges of independent branches).
2. **The escape hatch is intentional**: The commit message’s advice to use separate invocations for independent branches is framed as a *choice*, not a limitation. Junio sees this as a feature: users who care about order can split their work, while those who don’t can let Git handle it. Disallowing multiple refs would remove this flexibility.

Junio’s position suggests he views the current behavior as **acceptable for the majority of use cases**, even if it’s not perfect. He implies that the "principle of least astonishment" might be over-indexed here—linearization is inherently lossy, and users should expect some ambiguity. His tone is conciliatory but firm: he’s not convinced the inconsistency with `--advance`/`--revert` (which reject multiple refs) is a strong enough reason to change course.

The ball remains in Toon Claes’s court to either:
- **Accept the status quo**: Document the concatenation order (e.g., "commits are linearized in revision-walk order") and move forward.
- **Restrict multiple refs**: Align with `--advance`/`--revert` by rejecting them, as Elijah suggests.
- **Propose a middle ground**: For example, track per-branch state to preserve independence (though this would complicate the implementation).

This exchange doesn’t resolve the issue but clarifies the stakes. Junio’s resistance to disallowing multiple refs suggests the current behavior might stick unless Toon or others can demonstrate a compelling alternative. The discussion is now about **design philosophy** (flexibility vs. strictness) rather than technical correctness. The series remains in `next`, but this could delay graduation to `master` if the ambiguity isn’t addressed.