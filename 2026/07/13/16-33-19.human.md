This is the sixth patch in Harald Nordgren’s **final v18 series** for `git branch --delete-merged`, which is now **implementation-complete and ready for merging** pending only mechanical pre-merge fixes. The patch introduces a **per-branch opt-out mechanism** via `branch.<name>.deleteMerged=false`, allowing users to exempt specific branches from automated cleanup while still permitting explicit deletion via `git branch -d`. The implementation is minimal, touching only the candidate-filtering logic in `builtin/branch.c`, and includes thorough tests.

Phillip Wood’s feedback here is **surface-level**: he suggests improving the test by verifying branch state with `git for-each-ref` instead of `rev-parse`, aligning it with the series’ broader testing style. Harald has already adopted this suggestion, replacing the manual `rev-parse` checks with a `check_branches` helper that uses `for-each-ref` for clearer and more consistent verification. This change is purely stylistic and does not affect the patch’s functionality or safety guarantees.

The patch remains **stable and well-tested**, with no substantive issues remaining. The opt-out mechanism complements the series’ other safety features—such as **refined stacked-branch protection (abort-and-clear)**, push-divergence checks, and `--dry-run` preview—by addressing iterative topic-branch workflows where users may want to preserve certain branches despite being merged. The series as a whole is **ready for merging** once the agreed-upon mechanical fixes (flags cleanup in patches 2–4 and `--dry-run --quiet` rejection) are applied.