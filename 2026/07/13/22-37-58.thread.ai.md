Thread identity: Bugfix series for packfile URI download race conditions in Git's HTTP transport. Series version: v4 (reroll). Originating author: Ted Nyman. Thread type: patch series (bugfix).

Core problem or goal: Fix race conditions in Git's HTTP transport when multiple processes concurrently fetch the same packfile URI or dumb HTTP pack into a single object database. The races can corrupt temporary pack files or cause resume requests to fail at EOF. The series provides a unified solution for both packfile URIs and dumb HTTP, preserving resumable downloads while preventing corruption by using deterministic staging paths opened in read-write mode without append, ensuring overlapping responses write identical bytes at identical offsets.

Current status: Rerolled (v4) with test robustness improvements; production code unchanged since v3. The v4 series consists of three patches:
- Patch 1: **Merged to baseline** -- Preliminary cleanup correcting the `--index-pack-arg` option name in `Documentation/git-http-fetch.adoc` and `http-fetch.c` error messages (plural `args` -> singular `arg`). The patch is purely textual, uncontroversial, and ready for merging. Taylor Blau's review confirms the historical context and suggests minor documentation improvements (dropping the misleading claim about URL-encoded space-separated arguments and clarifying the distinction between the command name and its arguments).
- Patch 2: Uses deterministic staging paths for both packfile URIs and dumb HTTP, preserving resumable downloads. The core change in `http.c` switches from append mode to read-write mode with per-descriptor offsets, ensuring overlapping responses write identical bytes at identical offsets. The patch handles concurrent unlink scenarios (including Windows sharing behavior) and updates `http-fetch.c`, `http-push.c`, and `http-walker.c` to accept HTTP 416 when a partial pack is already complete. Test coverage includes new regression tests in `t/t5550-http-fetch-dumb.sh` exercising resumed transfers, EOF ranges, overlapping 200/206 responses, unlinking the staging path while indexing, and a deterministic overlapping-download test using a custom Perl server with a 60-second timeout, FIFO waiter notification, and reliable server process cleanup. HTTP 416 trace matching accounts for both older and current libcurl output formats.
- Patch 3: Makes `fetch-pack` accept both `"keep<TAB><hash>"` and `"pack<TAB><hash>"` output from `index-pack`, validating the reported pack hash against the advertised hash and only registering a keep lockfile when the current process created it (`created_keep` flag). The patch adopts Peff’s suggested `strbuf` refactor for clarity and adds a regression test in `t/t5702-protocol-v2.sh` exercising packfile URI fetches with pre-existing `.keep` files.

Changes since v3:
- Test robustness improvements only: the overlapping-download test server now includes a 60-second timeout, notifies FIFO waiters on failure, and tracks the server process for reliable cleanup. The test also waits for the second downloader first, ensuring an early failure cannot leave the server waiting indefinitely. HTTP 416 trace matching now accounts for both older and current libcurl output formats, avoiding false failures. **New in v4**: Peff reports that `--stress` testing of `t5550-http-fetch-dumb.sh` fails intermittently in test 34, with curl reporting "end of response with 1048917 bytes missing" and closing the connection prematurely. The server behaves identically in success and failure cases, but the second connection never arrives in the failing case. This suggests a latent race in the test’s synchronization or timing, blocking progression to `next`.

Junio C Hamano reviewed the v2 series and suggested the preliminary cleanup split, which has been implemented. He expressed unease about the `pwrite(2)` approach but accepted the Windows/MINGW workaround. The v3 series was merged to `seen` but blocked by a test hang in `t5550-http-fetch-dumb.sh`; the v4 changes resolved this hang, but Peff’s `--stress` testing reveals a new intermittent failure. No technical objections to the core design remain, but the series is **blocked** until the test flakiness is resolved.

Key technical details:
- Files touched: `Documentation/git-http-fetch.adoc`, `http.c`, `fetch-pack.c`, `http-fetch.c`, `http-push.c`, `http-walker.c`.
- New symbols: None (behavioral changes only).
- Test coverage: New regression tests in `t/t5550-http-fetch-dumb.sh` (dumb HTTP) and `t/t5702-protocol-v2.sh` (packfile URIs), exercising resumed transfers, EOF ranges, overlapping 200/206 responses, unlinking the staging path while indexing, pre-existing `.keep` files, and concurrent `.keep` file handling. A deterministic overlapping-download test uses a custom Perl server with a 60-second timeout, FIFO waiter notification, and reliable server process cleanup. HTTP 416 trace matching accounts for both older and current libcurl output formats.
- Subsystem: HTTP transport, packfile URI feature, dumb HTTP.
- On-disk format: No changes.
- Old vs. new behavior: The v4 design preserves resumable downloads for both packfile URIs and dumb HTTP by using deterministic staging paths and read-write mode without append. Concurrent processes write identical bytes at identical offsets, preventing corruption while maintaining resumability. The `.keep` file handling is more robust, with `fetch-pack` validating the pack hash and only registering a keep lockfile when the current process created it. The patch also handles MinGW’s non-append O_RDWR open behavior, ensuring Windows compatibility.

Open questions and loose ends:
- **Test flakiness under `--stress`**: Peff reports that `t5550-http-fetch-dumb.sh` fails intermittently in test 34, with curl closing the connection prematurely ("end of response with 1048917 bytes missing"). The server behaves identically in success and failure cases, but the second connection never arrives in the failing case. This suggests a latent race in the test’s synchronization or timing, blocking progression to `next`. No fix has been proposed yet.

Participant positions:
- **Ted Nyman**: Revised the design to preserve resumable downloads for both packfile URIs and dumb HTTP, addressing feedback from Junio C Hamano, Taylor Blau, and Jeff King (Peff). The v4 series improves test robustness with a 60-second timeout, FIFO waiter notification, reliable server process cleanup, and libcurl output format flexibility. Adopted Peff’s `strbuf` refactor for Patch 3 and renamed `packname` to `packhash` for clarity. The series is rebased onto current `master` but **blocked** by the new test flakiness reported by Peff.
- **Junio C Hamano**: Skeptical of the v1 design’s abandonment of resumable downloads for packfile URIs. Reviewed the v2 reroll and requested splitting the `--index-pack-args` typo fix into Patch 1, which has been implemented. Expressed unease about the `pwrite(2)` approach but accepted the Windows/MINGW workaround. Merged the v3 series to `seen` but reported a test hang in `t5550-http-fetch-dumb.sh`; the v4 changes resolved this hang, but the series remains blocked by the new `--stress` failure.
- **Taylor Blau**: Provided substantive and surface-level reviews of Patch 1 in v1 and v4, approving the core logic, documentation, and test coverage while suggesting minor documentation improvements. Reviewed Patch 3 in v4, focusing on mechanical clarity (the `packname` → `packhash` rename) and confirming the logic for accepting both `"keep"` and `"pack"` output from `index-pack`. No substantive concerns raised; the patch is ready to proceed once the test flakiness is resolved.
- **Jeff King (Peff)**: Engaged deeply with the design trade-offs in v1, proposing an alternative approach to preserve resumability. For Patch 3, suggested a `strbuf` refactor, which has been adopted. In v4, reports a **blocking issue**: `--stress` testing of `t5550-http-fetch-dumb.sh` fails intermittently in test 34, with curl closing the connection prematurely. The failure suggests a latent race in the test’s synchronization or timing, not the core download logic. No fix proposed yet, but the observation narrows the problem to the interaction between the test’s custom Perl server and curl’s connection handling. The series remains **blocked** until this is resolved.