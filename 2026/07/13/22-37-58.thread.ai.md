Thread identity: Bugfix series for packfile URI download race conditions in Git's HTTP transport. Series version: v6 (final). Originating author: Ted Nyman. Thread type: patch series (bugfix), now **cleared for merge** after final review.

Core problem or goal: Fix race conditions in Git's HTTP transport when multiple processes concurrently fetch the same packfile URI or dumb HTTP pack into a single object database, preventing corruption while preserving resumable downloads. The series provides a unified solution for both packfile URIs and dumb HTTP using deterministic staging paths and read-write mode without append, split into six focused commits for clarity and bisectability.

Current status: Series is complete at v6, **fully approved and cleared for merge** by all key reviewers (Junio, Taylor Blau, Peff). Junio’s latest email explicitly confirms the series is "ready for merge" following Peff’s final approval, removing the last procedural hurdle. The series remains **under final review** (not yet in `next` or `master`) but Junio intends to queue it to `next` imminently for cooking. The six patches are:

1. Textual cleanup correcting `--index-pack-arg` option name in documentation and error messages.
2. Fixes latent double-close bug in `finish_http_pack_request()` (redundant `close()` removed).
3. Adds HTTP 416 handling for completed partial packs, approved by Peff with a forward-compatibility note about remote file changes mid-transfer (not a regression).
4. Implements core concurrent-download fix using deterministic staging paths and read-write mode (no append).
5. Handles Windows/MINGW sharing semantics (`O_EXCL` loop workaround) by retrying open without `O_CREAT` after exclusive creation attempt.
6. Makes `fetch-pack` accept both `"keep<TAB><hash>"` and `"pack<TAB><hash>"` output from `index-pack`, validates pack hash, and ensures `.keep` lockfiles are only registered when the current process created them.

Key technical details:
- Files touched: `Documentation/git-http-fetch.adoc`, `http.c`, `fetch-pack.c`, `http-fetch.c`, `http-push.c`, `http-walker.c`, `t/t5550-http-fetch-dumb.sh`, `t/t5702-protocol-v2.sh`.
- New symbols: None (behavioral changes only).
- Test coverage: New regression tests for completed partial packs (HTTP 416 handling), deterministic concurrent downloads, Windows-specific unlinking, and `.keep` file handling edge cases. Retained tests for resumed transfers, EOF ranges, overlapping 200/206 responses, unlinking staging paths while indexing, pre-existing `.keep` files, and concurrent `.keep` file handling. Stress-tested with 240 runs of the overlapping-download scenario.
- Subsystem: HTTP transport, packfile URI feature, dumb HTTP.
- On-disk format: No changes.
- Old vs. new behavior: Preserves resumable downloads for both packfile URIs and dumb HTTP by ensuring concurrent processes write identical bytes at identical offsets. Handles MinGW’s non-append O_RDWR open behavior, Windows sharing semantics (`FILE_SHARE_DELETE`), and `.keep` file handling edge cases. `fetch-pack` now validates pack hash and only registers `.keep` lockfiles when the current process created them.

Open questions and loose ends: None. All prior advisory points (double-close bug, HTTP 416 handling, `pwrite()`/`lseek()` logic, Windows/MINGW workaround, `.keep` file edge cases) are fully addressed in separate commits. Peff’s forward-compatibility note about remote file changes mid-transfer is not a regression and does not block approval.

Participant positions:
- **Ted Nyman**: Author of the series. Revised v5 to address Peff’s advisory feedback, splitting Patch 2 into four focused commits (Patches 2–5) and adding Patch 6 to handle the `.keep` file edge case. Confirms all patches are necessary, correct, and thoroughly tested. No reroll planned; series is complete and ready for merge.
- **Junio C Hamano**: Initially skeptical of v1’s design but accepted the Windows/MINGW workaround. Merged v3 to `seen` but reported a test hang (resolved in v4). Endorsed Peff’s proposal to drop `test -s` check and HTTP 200/206 trace greps. Explicitly signals the series is "ready for merge" in his call-for-approval email and **confirms final approval** in this email, pending queuing to `next`.
- **Taylor Blau**: Reviewed Patch 1 (v1, v4) and Patch 3 (v4), approving core logic, documentation, and test coverage. No substantive concerns raised.
- **Jeff King (Peff)**: Engaged deeply with design trade-offs in v1, proposed alternative approaches, and identified four advisory points in v5 (double-close bug, HTTP 416 handling, `pwrite()`/`lseek()` logic, Windows/MINGW workaround). Recommended splitting Patch 2 into preparatory commits (adopted in v6). Provided **final approval for the entire v6 series**, including Patch 3 (HTTP 416 handling), with a forward-compatibility note about remote file changes mid-transfer (not a regression). No further feedback expected.