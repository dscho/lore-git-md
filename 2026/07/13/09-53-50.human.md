This is a **design discussion** in the ODB abstraction series. Patrick Steinhardt responds to Jeff King’s observation that the bitmap and non-bitmap object traversal functions (`traverse_bitmap_commit_list()` and `traverse_commit_list()`) are inconsistent in how they handle callback return values. Peff noted that the non-bitmap function’s callbacks return `void`, making early termination impossible, and suggested the two functions should stay in sync—either both should allow early termination or both should ignore callback returns. Patrick acknowledges the point but argues that adapting `traverse_commit_list()` would require sweeping changes across the codebase. He proposes leaving both functions as-is, preserving their current behavior: `traverse_bitmap_commit_list()` propagates non-zero returns from its callbacks (as introduced in patch 3/8), while `traverse_commit_list()` continues to ignore them.

The response is brief and pragmatic, framing the choice as a trade-off between consistency and churn. It directly addresses the design question raised by Peff and Taylor Blau but does not reopen the broader discussion about whether early termination is desirable in the first place. The tone suggests Patrick is seeking quick agreement rather than inviting further debate, and the question is posed as a binary choice: accept the status quo or undertake a large refactoring. No new technical details or alternatives are introduced, and the patch’s core purpose—enabling early termination for bitmapped object iteration—remains uncontested. The ball is now in the reviewers’ court to either accept the inconsistency or push for a more invasive change.