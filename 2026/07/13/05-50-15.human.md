This is the first patch in Patrick Steinhardt’s RFC v4 series to reorganize Git’s source tree by moving all `libgit.a` components into a new `lib/` directory. It is a **pure refactoring** patch with no functional changes, serving as a preparatory step for the main relocation patch that follows.

The patch adjusts a single include path in `t/helper/test-example-tap.c` from `#include "t/unit-tests/test-lib.h"` to `#include "../unit-tests/test-lib.h"`. This change ensures the test helper continues to build correctly after the upcoming move of core files into `lib/`, where the compiler will no longer resolve the original include path. The adjustment is minimal and mechanical, touching only one line in one file.

This patch is uncontroversial on its own—it is a straightforward fix to maintain build integrity during the reorganization. Its significance lies entirely in its role as a prerequisite for the larger structural change in the next patch. The series’ core debate remains focused on whether the benefits of the reorganization (reducing root-directory clutter and enforcing coding conventions) justify the workflow disruptions, but this specific patch is unlikely to attract substantive discussion.

For readers tracking the series, this patch signals that the v4 iteration is proceeding as planned, with no surprises in the preparatory steps. The real discussion will center on the second patch, which performs the bulk of the file moves.