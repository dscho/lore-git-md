This is the second patch in a two-patch **test modernization** series from new contributor Shlok Kulshreshtha. It moves a here-doc that generates the `expected` output file into the existing setup test (`test_expect_success 'test preparation: write empty tree'`). The change ensures the here-doc runs under the test harness, so failures are properly reported as test failures rather than silently ignored. The patch uses a `<<-` here-doc to allow indentation within the test body, improving readability without altering behavior.

The patch is small, self-contained, and follows established patterns for test modernization (as noted in the cover letter, it aligns with Eric Sunshine’s guidance for similar updates in `t7001`). The commit message clearly explains the motivation—code outside tests isn’t protected by the harness—and the diff is straightforward. No new symbols or behaviors are introduced; the script still passes all 5 tests after the change.

This is a minor but worthwhile cleanup, part of the ongoing effort to modernize Git’s test suite. The series as a whole is uncontroversial and likely to be queued for `next` once reviewed. No feedback has been posted yet, but the patch looks solid and ready for integration. The only file touched is `t/t1100-commit-tree-options.sh`.