This is a refactoring patch in Patrick Steinhardt’s ODB abstraction series. It marks the object filter parameter in `for_each_bitmapped_object()` and its helper functions as `const` to reflect that the filter is never modified during traversal. The change is mechanical: four lines in `pack-bitmap.c` and one in `pack-bitmap.h` update function signatures to accept a `const struct list_objects_filter_options *` instead of a non-`const` pointer. No behavior changes; the patch is purely about code hygiene and API clarity.

The patch is the second in the nine-patch v3 series. It directly follows the object-lookup performance improvement (patch 1/9) and precedes Jeff King’s cleanup of the unused `int` return in `add_object_entry()` (patch 3/9). Taylor Blau reviewed the equivalent patch in v2 (then 2/8), noting only a minor stylistic preference that has since been incorporated. No other review feedback has targeted this patch, and the thread context confirms all substantive discussion has focused on other parts of the series.

This is a safe, uncontroversial change that aligns the bitmap traversal API with Git’s broader practice of marking read-only parameters as `const`. The diff is small and self-contained, touching only the bitmap subsystem. No new symbols are introduced; the patch merely tightens type safety for existing functions. The commit message is concise and accurate, though it could have noted that the change ripples through three helper functions (`filter_bitmap`, `can_filter_bitmap`, and `for_each_bitmapped_object`) to maintain consistency.

The patch is ready for integration. It has no unresolved feedback, no performance implications, and no dependencies beyond the series’ baseline (Git 2.56 with `ps/odb-drop-whence` merged). The `const` qualifier is a standard C idiom that improves maintainability without risk.