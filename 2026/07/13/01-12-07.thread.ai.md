Thread identity: Patch series introducing a new `no-ref-delta` capability to the Git wire protocol. This is the fourth and final patch in the series, authored by Taylor Blau.

Core problem or goal: Enable servers to reject `REF_DELTA` (type 7) objects in pushed packs without also requiring support for `OFS_DELTA` (type 6). The patch adds the capability, teaches `receive-pack` to advertise it, and teaches `send-pack` to honor it by passing `--no-ref-delta` to `git pack-objects` when requested.

Current status: Under review. This is the first appearance of this patch in the thread summary; no prior versions or merge status are recorded.

Key technical details:
- New capability: `no-ref-delta`.
- New boolean field `no_ref_delta` in `struct send_pack_args`.
- New command-line flag `--no-ref-delta` passed to `git pack-objects`.
- Files touched: `send-pack.c`, `Documentation/technical/protocol-v2.txt`, and test file (unnamed in summary).
- Test verifies client omits `REF_DELTA` objects when server advertises `no-ref-delta`, using `GIT_TRACE2_EVENT`.

Open questions and loose ends:
- No motivation provided in the commit message for why a server might want to reject `REF_DELTA` objects while still accepting `OFS_DELTA`.
- No prior feedback or review comments recorded; thread is at initial submission stage.

Participant positions: None recorded yet; only the author (Taylor Blau) is mentioned.

Related work: Part of a larger series (fourth patch), but the full series is not seen in this thread. The capability is kept separate from `ofs-delta`, implying a design choice to allow independent control.