This is a platform-compatibility bugfix for Cygwin, continuing the discussion from Ramsay Jones’s patch to fix IPv6-style SCP URL handling. Patrick Steinhardt replies to Ramsay’s email, endorsing the patch’s goal of reverting the 2024 test skips and addressing the regression properly rather than papering over it.

Patrick expresses support for the fix itself, calling the 2024 test skips a temporary workaround and welcoming a proper resolution. He also raises a new topic: whether Cygwin should be added to Git’s CI matrix, noting that Cygwin’s behavior differs enough from both MSYS2 and native Windows to warrant dedicated testing. His tone is neutral on the technical details of the patch, deferring to others on Windows-specific path handling, but he clearly sees value in catching Cygwin regressions earlier.

The email does not resolve the open question about the `#ifdef DUMMY` block in `connect.c`—whether to remove it entirely, replace it with `#ifdef GIT_WINDOWS_NATIVE`, or keep it as-is to preserve Git for Windows UNC path support. That debate remains with Torsten Bögershausen’s earlier objection unaddressed here. No new code changes or test results are introduced; this is purely a discussion update.