This is a documentation cleanup patch for `git fast-export`, now queued for merging.

Junio C Hamano reviewed Christian Couder’s patch that aligns the `git fast-export -h` usage string with the man-page SYNOPSIS and with Git’s standard convention for revision arguments. The patch replaces the inconsistent `[<rev-list-opts>]` in the help text and the `[<options>] | 'git fast-import'` in the man page with the uniform `[<options>] [<revision-range>] [[--] <path>...]`, matching the pattern used by commands like `git log` and `git rev-list`. It also removes `fast-export` from the test file `t/t0450/adoc-help-mismatches`, eliminating a false-positive test failure.

Junio’s feedback was positive, noting that the original man-page synopsis was unusual for including an example (`| 'git fast-import'`) rather than a formal syntax description. The updated version restores consistency with other Git commands. The patch touches only documentation and test files, with no functional changes to the command itself. Junio has queued it for inclusion.