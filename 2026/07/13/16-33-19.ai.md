This is the sixth patch in Harald Nordgren’s `git branch --delete-merged` v18 series, now updated to use `git for-each-ref` for test verification as suggested by Phillip Wood. The change is purely mechanical: the test helper introduced in earlier patches (`check_branches`) is reused here to verify branch states, replacing the manual `rev-parse` checks. This aligns the test with the series’ broader style and improves clarity without altering the patch’s functionality.

The patch itself remains unchanged in its core purpose: it adds a `branch.<name>.deleteMerged=false` config key to exempt specific branches from automated cleanup while preserving the ability to delete them explicitly via `git branch -d`. The opt-out mechanism is minimal, touching only the candidate-filtering logic in `builtin/branch.c`, and the tests now more robustly verify both opt-out behavior and explicit deletion.

This version is ready for merging alongside the rest of the series, pending the previously agreed mechanical fixes (flags cleanup in patches 2–4 and `--dry-run --quiet` rejection). The test improvement here is cosmetic but consistent with the series’ quality standards. No substantive issues remain.