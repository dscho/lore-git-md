This thread is a small but typical cleanup patch from Christian Couder to bring `git fast-export`'s help text and man-page synopsis into line with Git's standard conventions. The patch replaces an unusual phrasing (`| 'git fast-import'`) in the man page and the `[<rev-list-opts>]` placeholder in the usage string with the more conventional `[<options>] [<revision-range>] [[--] <path>...]` pattern used by commands like `git log` and `git rev-list`. It also removes `fast-export` from a test that was failing due to the mismatch between help text and documentation.

The patch has been reviewed and queued by Junio Hamano in his `seen` branch as of June 12, 2025, with no prior iterations. Patrick Steinhardt added a brief, approving review, noting the change aligns with Git's established patterns. There are no open questions or behavior changes—this is purely a documentation and consistency fix. The patch passed CI on Christian's branch, and the only remaining step is for Junio to graduate it to `next` and eventually `master`.