This is a **substantive review** of the final patch in the `includeIf worktree:<pattern>` series (v8 2/2). Patrick Steinhardt, the reviewer, signals **approval** for the current implementation while acknowledging a known limitation: the feature does not handle symlinked worktree paths (it resolves to real paths only). His feedback is narrowly scoped to the documentation and test coverage of this limitation.

The review explicitly states that the series is **"good enough"** for now, removing the last technical blocker. Patrick suggests two minor improvements: (1) adding a note in the documentation that the symlink limitation *may* be addressed later, and (2) including a `test_expect_failure` case to demonstrate the desired (but unimplemented) symlink behavior. Neither suggestion is framed as a requirement for merging—Patrick clarifies that the current version is acceptable as-is.

The patch itself remains unchanged from v8: it introduces `worktree:` and `worktree/i:` config conditions, reuses the refactored `include_by_path()` helper, and includes thorough test coverage for bare repositories, multi-worktree setups, and glob patterns. The symlink limitation is already documented in the man page, but Patrick’s suggestion would make the future-proofing intent more explicit.

This review effectively **clears the series for integration** into `next`, assuming no other objections arise. The trade-off (documented symlink limitation) is a pragmatic compromise to avoid blocking on Patrick’s larger `ps/setup-*` refactoring. The author, Chen Linxuan, has already demonstrated responsiveness to feedback by simplifying the series in v8 to resolve prior conflicts. No further rerolls are expected unless another reviewer surfaces a new issue.