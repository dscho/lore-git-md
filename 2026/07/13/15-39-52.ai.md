This is a **substantive design discussion** about the `--delete-merged` feature in Harald Nordgren’s v18 series, specifically patch 5/7. The exchange between Phillip Wood and Harald clarifies a subtle edge case in the feature’s behavior: when `remote.pushDefault` is set (as in the test environment), branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as "forked" and thus eligible for deletion unless manually exempted. This affects not only the default branch (`main`/`master`) but also other long-lived branches like `next` and `seen`.

Harald’s test refinement—replacing the artificial `mainline` branch with `main` and explicitly configuring `branch.main.pushRemote`—exposes this limitation while improving test clarity. Phillip acknowledges the pain point but agrees it’s a known trade-off, suggesting a documentation note to warn users. The discussion does not propose code changes; the core logic and safety checks (stacked-branch protection, per-branch opt-out, etc.) remain intact. This is a **final design note** rather than a blocker, and the series is still on track for merging once the mechanical flags cleanup (agreed in earlier patches) is applied.

Key takeaways for the future AI summarizer:
- **Edge case**: Branches without `branch.<name>.pushRemote` may be misclassified as forked when `remote.pushDefault` is set.
- **Test improvement**: The artificial `mainline` branch was removed in favor of `main` with explicit `pushRemote` config, making the test more realistic.
- **Documentation opportunity**: A warning about this behavior could be added to `git-branch.adoc`.
- **No functional changes**: The discussion is purely about user expectations and test clarity, not the feature’s core logic.