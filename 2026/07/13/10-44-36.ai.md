This is the first patch in the **v10 series** implementing visual root indentation for `git log --graph`. It is a **test infrastructure refactoring** that consolidates the `check_graph` helper function, previously duplicated in `t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`, into a shared library file (`t/lib-log-graph.sh`) under the new name `lib_test_check_graph`.

The patch is **mechanical and preparatory**: it moves and renames the function without altering its behavior, ensuring all existing tests continue to pass. The motivation is to avoid code duplication and to make the helper available for a new test file (`t4218-log-graph-indentation.sh`) introduced later in the series. The change is small (29 insertions, 34 deletions) and touches only test infrastructure, not production code.

This patch has **already been merged** (as noted in the thread AI summary), so it is effectively settled. The remaining patches in the series (2/7 through 7/7) build on this foundation to implement the indentation feature, the four-level wrapping behavior, and the opt-out mechanism. No controversy or open questions are associated with this specific patch.