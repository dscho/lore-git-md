**Summarizer brief mode**

This is a **substantive review objection** to the `tc/replay-linearize` topic, which was recently merged to `next` and marked for promotion to `master` in Junio’s "What’s cooking" report. The topic adds a `--linearize` option to `git replay` that mimics `git rebase --no-rebase-merges` by dropping merge commits and flattening the replayed history.

Elijah Newren raises a **principle-of-least-astonishment concern** about the third patch in the series (the one that implements the linearization logic). He reports that two alternative approaches were not considered during the original review and that one of these alternatives would be simple to implement. The email references a prior message (Message-ID `<CABPp-BGzU9KHGF1nipi2HZaa1AiikMKGGaapQzHVH06wO4V1ww@mail.gmail.com>`) that presumably details the alternatives and the specific astonishment issue, but that message is not included in the current input.

The request is to **hold the topic in `next`** (i.e., not graduate it to `master`) until the objection is addressed. No technical specifics about the alternatives or the astonishment violation are provided in this email, so the exact nature of the problem remains opaque to this session. The topic currently touches the `replay.c` file, introduces the `--linearize` CLI option, and modifies the `pick_regular_commit()` and `replayed_commits` internals.