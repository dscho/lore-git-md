Here’s a concise human-readable summary of the current state of the Git mailing list thread based on Junio’s "What’s cooking" report for July 2026:

This thread is Junio Hamano’s authoritative snapshot of all in-flight topics in Git’s integration branches, covering 58 patch series currently under review or cooking for the next release. The report highlights six new topics needing review, four stalled efforts, and 46 cooking topics—many of which are substantial architectural changes.

Key developments include Patrick Steinhardt’s ongoing work to abstract the object database (ODB) and refs subsystems, removing global state like `the_repository` and introducing transactional interfaces. Taylor Blau’s repacking improvements (geometric cruft packs) and Karthik Nayak’s merge-base optimizations are also cooking, alongside new features like `git history squash` and remote object metadata queries in `git cat-file`. Submodule error handling has been made configurable, and `git add -e` now uses the apply API directly, eliminating a subprocess.

Several topics touch foundational areas: object size fields are being widened to `size_t` for 64-bit Windows compatibility, and repository discovery is being split into two phases to unify setup. The rebase sequencer has been fixed to avoid incorrectly copying notes for dropped commits, and `git log --graph` now indents root commits for clarity. New CLI options and config keys (e.g., `fetch.submoduleErrors`, `core.configLockTimeout`) have been introduced, along with low-level functions like `odb_for_each_object()` and `prio_queue_get()`.

Open questions remain around potential regressions in geometric cruft repacking, the appropriateness of remote object metadata in the protocol, and the UI design of new `git history` subcommands. Some optimizations, like merge-base early-exit, may need scrutiny for edge cases. Stalled topics include documentation for trailers and a rebase symref fix, while one HTTP redirect patch is marked for discard. The report underscores a busy cycle with significant refactoring and performance work, but also flags areas where feedback or rerolls are still needed.