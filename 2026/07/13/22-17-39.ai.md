This is a **build system** and **platform compatibility** follow-up in the ongoing `USE_NSEC` discussion. D. Ben Knoble acknowledges Junio C Hamano’s decision to withdraw the original patch (which added a `nanosec` Meson option to mirror the Autotools `USE_NSEC` knob) from the integration tree, confirming that the patch is now obsolete in light of the broader consensus to replace the build-time knob with a runtime-configurable approach (`core.useNsec`). Knoble’s brief reply signals agreement with the shift in focus and does not introduce new technical arguments or objections.

The email serves as a procedural bookend to the original patch, marking its formal withdrawal and clearing the way for a future patch series that implements the runtime-configurable approach. No new design questions or implementation details are raised here; the thread’s next step remains the same: a new patch series that introduces `core.useNsec`, deprecates the `USE_NSEC` build knob, and documents the trade-offs and backward-compatibility story. The discussion’s earlier consensus—always compile nanosecond support into Git when the platform allows it, expose it via `core.useNsec`, and retain the current build-time default (`USE_NSEC` disabled)—still stands as the path forward.