This is a **final review comment** from Junio Hamano on **patch 7/7** of Pablo Sabater’s **v11 series** implementing visual root indentation for `git log --graph`. The series is otherwise **technically complete and ready for integration** into `next`, pending this minor mechanical adjustment.

Junio’s feedback is **surface-level**: he approves the change to pack the two boolean fields (`no_graph_indent` and `graph_indent_set`) into single bits in `struct rev_info` but suggests simplifying the conditional check in `revision.c:3209` to avoid implying that `graph_indent_set` could hold values other than 0 or 1. The fix is trivial—replace `revs->graph_indent_set > 0` with `revs->graph_indent_set`—and has no functional impact. This is the **only remaining action item** before the series can proceed.

The thread context confirms that all architectural and usability decisions (lookahead buffer, four-level wrap, opt-out mechanism) are settled, and the series has no known technical or test failures. Junio’s comment here is the last loose end; once addressed, the series is likely to be queued for `next`.