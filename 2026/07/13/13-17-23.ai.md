This is the sixth patch in Phillip Wood’s 10-patch v2 series addressing how the sequencer handles dropped commits during rebase, specifically focusing on **simplifying the control flow for fixup commits that encounter conflicts**.

The patch removes a redundant conditional check in `pick_one_commit()` that was introduced by an earlier fix (commit `e032abd5a0`). The logic now assumes that if execution reaches the `is_fixup()` branch, the only possible positive return value from `do_pick_commit()` is `1` (indicating conflicts), so it unconditionally calls `error_with_patch()` with the `intent_to_amend` flag set to `1`. This eliminates the need for the explicit `intend_to_amend()` call and reduces the branch complexity. The change is **pure refactoring**—no behavior is altered, and the commit message clearly explains the reasoning (the early return for `res == -1` ensures `res` must be positive here).

The patch touches only `sequencer.c` and is a small, targeted cleanup that aligns with the series’ broader goal of simplifying control flow before introducing the core behavioral change in patch 10. It follows the pattern established in earlier patches (e.g., patch 5/10) of removing redundant checks to make the codebase more maintainable. The diff is minimal (three lines removed, one line changed), and the commit message provides sufficient context by referencing the earlier commit that introduced the redundancy.

This is a **well-motivated refactoring** that prepares the ground for the `enum pick_result` introduction in later patches. The change is uncontroversial and unlikely to draw substantive review feedback, as it is a straightforward simplification with no functional impact. The patch is part of a **tested, queued series** and is expected to land as-is after Phillip squashes the queued typofixes.