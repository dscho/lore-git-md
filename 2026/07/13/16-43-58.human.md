This is the first patch in Pablo Sabater’s **v11 series** implementing visual root indentation for `git log --graph`. It is a **test infrastructure cleanup** that moves the `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into `t/lib-log-graph.sh`, renaming it to `lib_test_check_graph`.

The patch is **purely mechanical**: it consolidates duplicated code without changing any behavior. Both test files already source `lib-log-graph.sh`, so the move is a natural fit. The change prepares the ground for the new test file (`t4218-log-graph-indentation.sh`) introduced later in the series, which will also need the helper.

This patch was **already merged** on 2024-07-12, so it is not under active review. The diff shows a straightforward refactoring: 34 lines of duplication removed, 5 lines added to the library file. No edge cases or new functionality are introduced. The patch is well-motivated and uncontroversial.