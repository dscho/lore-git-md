This is the fifth patch in Patrick Steinhardt’s 12-patch v2 series **"Make ODB optimizations pluggable"**, part of the ongoing ODB abstraction effort. The patch refactors the assembly of `git repack` arguments in `builtin/gc.c` to eliminate global state, making the logic self-contained within `maintenance_task_odb()`.

The core change moves the `strvec` of repack arguments from global scope into `maintenance_task_odb()`, where it is now constructed locally. This requires passing two additional parameters (`keep_largest_pack` and `aggressive`) to the function, but removes the interdependence on the global `repack_args` vector. The patch is a pure refactoring with no intended behavioral changes: all existing logic (e.g., memory estimation, pack selection, incremental vs. full repack decisions) is preserved, only the code structure is adjusted. The `need_to_gc()` helper is also simplified to drop its `repack_args` parameter, as the argument assembly is now deferred to `maintenance_task_odb()`.

The diff shows the mechanical nature of the change: the repack argument logic is lifted from `cmd_gc()` and `need_to_gc()` and relocated into `maintenance_task_odb()`, with the function signature updated to accept the new parameters. The patch is uncontroversial and aligns with the series’ goal of isolating ODB optimization logic for future pluggability. No test changes are needed, as the behavior remains identical. Reviewers may wish to verify that the relocated logic handles all edge cases (e.g., memory estimation, pack selection) identically to the original code.