This is a refactoring patch in Pablo Sabater’s **v10 series** adding visual root indentation to `` `git log --graph` ``. It is the sixth of seven patches.

The patch moves the configuration reading for `log.graphcolors` out of `graph_init()` and into a new helper function `graph_read_config()`. This is a pure code reorganization: no behavior changes, no new features. The commit message calls it “preparatory” for a future patch, but the only subsequent patch in the series (7/7) adds the `--[no-]graph-indent` option and its config variable, which could have read its own config directly. The real motivation appears to be cleaning up `graph_init()` and establishing a pattern for future graph-related configuration.

The change is small (9 lines added, 4 removed) and mechanical. It does not touch any of the core indentation logic or the lookahead buffer introduced earlier in the series. The patch is uncontroversial and looks ready to merge once the rest of the series is reviewed. No test failures or review comments have surfaced on this specific patch.