This is a surface-level review of the first patch in Phillip Wood’s 10-patch series fixing how `git rebase` handles notes from dropped commits. The reviewer, Oswald Buddenhagen, suggests two minor punctuation tweaks to the commit message: adding a comma after "Now that the merge backend is the default" and another after "--apply". Neither suggestion affects the patch’s technical content or test coverage; they are purely stylistic edits to improve readability.

The patch itself is a straightforward test maintenance update, replacing the `-m` flag with `--apply` in `t3400-rebase.sh` to restore coverage for the "apply" backend’s note-copying behavior. The series as a whole is already queued in Junio’s tree and carries a `Tested-by` from Uwe Kleine-König, so these nitpicks are unlikely to delay progress. Phillip will likely incorporate them as typofixes in a reroll.