This is a brief follow-up in the `git branch --delete-merged` series, specifically addressing a maintenance concern in the second patch. The patch refactors `delete_branches()` to use a flags-based API, but Phillip Wood points out that the implementation splits the `flags` variable into local booleans (`force`, `quiet`), creating two sources of truth. He recommends replacing all local boolean uses with direct flag checks (`flags & DELETE_BRANCH_FORCE`, etc.) to avoid future regressions.

Harald Nordgren agrees with the suggestion and plans to make the change before merging. The adjustment is mechanical and does not alter functionality, but it ensures the flags remain a single source of truth throughout the function. Phillip’s advice to document the rationale in the commit message should help prevent future reviewers from mistakenly "simplifying" the code back to local variables. This is a minor but important cleanup to keep the API consistent and maintainable. The discussion is resolved, and the fix is straightforward.