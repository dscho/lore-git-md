This is a documentation cleanup patch for `git fast-export`. Junio C Hamano has reviewed and approved the change, noting that it improves consistency with other Git commands and eliminates a special case in the test suite.

The patch updates the SYNOPSIS section of the `git fast-export` man page and the command’s `-h` help text to use the standard Git convention for revision arguments: `[<options>] [<revision-range>] [[--] <path>...]`. This matches the pattern used by commands like `git log` and `git rev-list`, replacing the previous inconsistent phrasing. The change also removes `fast-export` from a test that checks for mismatches between help text and documentation, as the inconsistency no longer exists.

Junio’s feedback highlights that the original man page included an unusual example in the SYNOPSIS (showing `git fast-import` as a consumer of the output), which is better suited for the body of the documentation. The updated version aligns with Git’s established conventions. The patch is straightforward, well-motivated, and touches only documentation and usage strings—no functional changes are involved. It is now queued for merging.