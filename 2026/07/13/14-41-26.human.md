This is a small, mechanical refactoring patch in Patrick Steinhardt’s nine-patch ODB abstraction series. It marks the object filter parameter in `for_each_bitmapped_object()` and its helper functions as `const` because the filter is never modified by the code. The change is confined to `pack-bitmap.c` and `pack-bitmap.h`, touching only function signatures and internal calls.

The patch is uncontroversial and well-motivated: the `const` qualifier accurately reflects the function’s behavior and improves type safety. Taylor Blau reviewed the equivalent patch in v2 and noted only a minor stylistic preference (which Patrick did not adopt), but otherwise confirmed the change as correct. No other reviewers have raised objections.

This is the second patch in the series and a prerequisite for later patches that introduce ODB-mediated object filtering. It has no user-visible impact and does not change behavior. The diff is minimal (4 insertions, 4 deletions) and clearly shows the scope of the change. The patch looks ready to merge as part of the larger ODB effort.