This is the first patch in Pablo Sabater’s **v10 series** implementing **visual root indentation for `git log --graph`**. This patch is **test infrastructure housekeeping**: it moves the duplicated `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into the shared `lib-log-graph.sh` library, renaming it to `lib_test_check_graph`.

The change is purely mechanical: the function’s logic is unchanged, and all existing calls in the two test files are updated to use the new shared name. This cleanup is a prerequisite for the new test file (`t4218-log-graph-indentation.sh`) introduced later in the series, which will also need the helper. The patch is **small, well-scoped, and uncontroversial**; it was already merged on July 12, so this version is just for completeness in the v10 roll-up. No review feedback is expected or needed.