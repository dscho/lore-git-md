This is the ninth patch in Phillip Wood’s 10-patch v2 series that overhauls how the sequencer handles dropped commits during rebase. The patch introduces `enum pick_result` to replace the integer return values from `do_pick_commit()`, making the code’s intent clearer and paving the way for the final patch’s behavioral change (preventing notes from being copied from dropped commits).

The patch converts `do_pick_commit()`’s return type from `int` to `enum pick_result`, which has three members: `PICK_RESULT_ERROR` (-1), `PICK_RESULT_OK` (0), and `PICK_RESULT_CONFLICTS` (1). The conversion is mechanical: the function’s logic is unchanged, but the return values are now explicitly named. The patch also updates `pick_one_commit()` and `single_pick()` to handle the new enum, replacing integer checks with switch statements. The `res` variable in `pick_one_commit()` is retained in conflict-handling branches to maintain clarity during the phased introduction of the enum, as Phillip clarified in follow-up replies.

This is a **pure refactoring** with no behavior change. The patch is small (45 insertions, 16 deletions) and focused, touching only `sequencer.c`. The enum’s scope is deliberately limited to `do_pick_commit()` and its immediate callers to avoid disrupting other functions like `pick_one_commit()` and `single_pick()`. Oswald Buddenhagen’s suggestion to widen the enum’s scope further was rejected as unnecessary churn, and Phillip’s follow-up replies confirm the incremental approach was intentional.

The patch is well-motivated: the enum makes the code more readable and prepares for the final patch’s addition of `PICK_RESULT_DROPPED`. The commit message clearly explains the rationale, and the changes are minimal and targeted. This is a **substantive refactoring** that directly supports the series’ broader goal of fixing the sequencer’s handling of dropped commits. The series is queued in Junio’s tree and ready for final review, with no substantive changes expected beyond squashing typofixes.