This is the final exchange in the reftable backend performance regression fix series. The thread is now purely about post-approval cleanup and forward-looking API evolution.

Patrick Steinhardt accepts Kristofer Karlsson’s proposed field comment for the new `suppress_deletions` option in `struct reftable_stack_options` ("filters out tombstoned/deleted refs early if true") and confirms that no reroll is needed for the two cosmetic issues (commit message typo and missing comment) identified earlier.

The conversation then shifts to the future of the `suppress_deletions` flag. Patrick outlines a concrete migration path: once both the reftable hardening series (`ps/reftable-hardening`) and this tombstone fix series (`kk/reftable-tombstone-quadratic-fix`) are merged to `master`, he will update libgit2 to use the new `reftable_stack_options` field. After that, Kristofer (or anyone else) can submit a pull request to libgit2 to deactivate `suppress_deletions` entirely. Only then would Git itself drop the flag. This sequence ensures downstream compatibility is preserved throughout the transition. The tone remains matter-of-fact and actionable, with no unresolved questions or technical blockers.