This is a review comment on the `tc/replay-linearize` topic, which was recently merged to `next` and marked for promotion to `master` in Junio’s "What’s cooking" report. Elijah Newren, the project’s foremost expert on merge machinery, is asking Junio to pause the merge because the third patch in the series introduces a behavior that violates the principle of least astonishment. He notes that two alternative approaches—one of them simple to implement—were not considered in the original discussion.

The topic itself adds a `--linearize` option to `git replay` that drops merge commits and flattens the replayed history, similar to `git rebase --no-rebase-merges`. The disputed patch is the one that actually implements the linearization logic; Elijah’s concern is not with the feature itself but with how it behaves in edge cases or user expectations. The thread he links to (not provided here) presumably contains the technical details of the alternatives he proposes.

For a busy reader, this is a heads-up that a seemingly settled feature may be revisited before it ships in the next release. The topic is small (three patches) and the objection comes from a highly credible source, so the outcome is likely to be either a quick reroll or a revert in `next`. Either way, the final behavior of `git replay --linearize` is still in flux.