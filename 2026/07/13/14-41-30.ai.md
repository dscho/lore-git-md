This is a mechanical refactoring patch in the ODB abstraction series. It drops the `_1` suffix from the internal bitmap-opening functions `open_pack_bitmap_1()` and `open_midx_bitmap_1()`, renaming them to `open_pack_bitmap()` and `open_midx_bitmap()` respectively. The change follows the preceding patch (5/9), which consolidated the original `open_pack_bitmap()` and `open_midx_bitmap()` into a new `open_bitmap_for_source()` function, leaving the `_1`-suffixed variants as the only remaining callers.

The patch touches only `pack-bitmap.c` and makes no functional changes—it is purely a rename to improve clarity and consistency after the refactoring in patch 5/9. The diff shows straightforward search-and-replace updates to function definitions, declarations, and call sites. This is uncontroversial cleanup that aligns with Git’s naming conventions and has no unresolved review feedback. It is a prerequisite for the ODB filtering mechanism introduced later in the series.