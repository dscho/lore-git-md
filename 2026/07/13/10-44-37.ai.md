This is the second patch in Pablo Sabater’s **v10 series** implementing visual root indentation for `` `git log --graph` ``. It is a **refactoring patch** that introduces the helper function `next_commit_to_show()` in `revision.c`.

The patch extracts the logic that determines the next commit to show—whether from the normal revision walk (`get_revision_internal()`) or from the saved list used by `--max-count-oldest`—into a unified helper. This change is **preparatory**: the helper will be used in a later patch to pre-fetch commits into a lookahead buffer, ensuring commits are fully simplified before inspection. There is **no behavior change** in this patch; it is purely structural.

The implementation is careful to preserve the existing behavior for `--reverse`, which keeps its own commit-popping loop to avoid unintended side effects (e.g., setting `SHOWN` and `CHILD_SHOWN` flags). The patch is **small and focused**, touching only `revision.c` and adding 24 lines while removing 12.

This patch is **ready for final review** and has no unresolved technical questions. It is part of the **core infrastructure** for the series and must land before the lookahead buffer and visualization logic can be merged. The GitHub CI run for v10 shows no failures, and the range-diff confirms this patch is unchanged from v9.