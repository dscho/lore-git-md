This is a brief follow-up in the ongoing `USE_NSEC` discussion, now focused on deprecating the build-time knob in favor of runtime configuration. D. Ben Knoble acknowledges Junio C Hamano’s decision to withdraw the original patch (which added a `nanosec` Meson option to mirror the Autotools `USE_NSEC` knob) and confirms that he had intended to request its removal, having been away on vacation.

The thread has already converged on a new direction: replacing the `USE_NSEC` build knob with a runtime config option (`core.useNsec`), always compiling nanosecond support into Git when the platform allows it, and deprecating the build-time knob. This email does not introduce new technical details or debate, but it formally closes the loop on the original patch. The next step is a new patch series implementing the runtime-configurable approach, which will need to address open questions like the default value for `core.useNsec` and interoperability risks. For now, this is a procedural footnote rather than a substantive update.