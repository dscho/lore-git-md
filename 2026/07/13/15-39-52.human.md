This is the final review exchange for patch 5/7 of Harald Nordgren’s **`git branch --delete-merged`** series, now in its 18th iteration and effectively complete. The discussion centers on a minor but revealing test-suite refinement: replacing the artificial `mainline` branch with `main` and explicitly configuring `branch.main.pushRemote` to prevent `main` from being misclassified as a forked branch.

Phillip Wood’s question about the purpose of `mainline` prompted Harald to simplify the test setup, exposing a subtle edge case in the feature’s design. When `remote.pushDefault` is set (as in the test environment), branches without an explicit `branch.<name>.pushRemote`—including `main`—may be incorrectly treated as forked unless manually exempted. This isn’t a bug in the core logic but a known limitation of the current implementation, and both contributors agree it’s not worth addressing in this series. Phillip suggests adding a documentation note to warn users about this behavior, which Harald acknowledges but defers.

The exchange is purely mechanical—no functional changes are proposed or needed. The test suite now uses clearer branch names (`local-to-delete` instead of `on-local`) and removes redundant setup steps, improving readability. All substantive design questions (stacked-branch protection, per-branch opt-out, `--dry-run`) were resolved in prior iterations, and this thread confirms the series is ready for merging once the agreed-upon flags cleanup in patches 2–4 is applied. The tone is collaborative and low-stakes, reflecting the series’ maturity.