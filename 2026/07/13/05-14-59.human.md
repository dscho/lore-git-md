This is the final review exchange in the two-patch series that fixes a quadratic performance regression in Git’s reftable backend. The series is now **technically complete and ready for merging**—all substantive feedback has been addressed, and only minor cosmetic tweaks (a commit message typo and a missing field comment) remain, neither of which warrants a reroll.

Patrick Steinhardt provides the last review feedback, accepting Kristofer Karlsson’s proposed field comment (`"filters out tombstoned/deleted refs early if true"`) and confirming the API change to expose `suppress_deletions` in `struct reftable_stack_options`. The discussion then shifts to a forward-looking question: whether `suppress_deletions` should eventually be removed entirely. Patrick outlines a clear path for this: once the current series and Patrick’s `ps/reftable-hardening` topic are merged to `master`, he will update libgit2 to deactivate `suppress_deletions`, after which Git itself can drop the field. This confirms that the flag is indeed a temporary stopgap, but its removal is not blocking the current fix.

The tone is pragmatic and collaborative. The series itself remains uncontroversial and well-motivated, with real-world performance improvements (from ~14s to ~0.2s in tombstone-heavy scenarios) and no on-disk format changes. Downstream compatibility is preserved for now, though libgit2 will need to adapt to the new `reftable_stack_options` field in future versions. The patch touches only the reftable backend (`refs/reftable-backend.c`, `reftable/merged.{c,h}`, `reftable/stack.c`) and adds a perf test (`t/perf/p1401-ref-store-tombstones.sh`). Junio’s merge is the only remaining step.