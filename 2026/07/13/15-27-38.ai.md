This is a **surface-level review** of the sixth patch in Harald Nordgren’s `git branch --delete-merged` v18 series. Phillip Wood’s feedback is purely stylistic: he suggests improving the test by verifying the branch state with `git for-each-ref` instead of relying solely on `rev-parse` checks. The change is mechanical and does not affect the patch’s functionality or safety guarantees.

The patch itself remains **stable and well-tested**, introducing a per-branch opt-out mechanism via `branch.<name>.deleteMerged=false`. This config key exempts specific branches from automated cleanup while preserving the ability to delete them explicitly with `git branch -d`. The implementation is minimal, touching only the candidate-filtering logic in `builtin/branch.c`, and includes thorough tests for both opt-out behavior and explicit deletion.

Phillip’s suggestion is consistent with the test improvements applied elsewhere in the series (e.g., using `check_branches` helpers for `for-each-ref` comparisons). Adopting it would align this test with the series’ broader testing style but does not address any functional gaps. The patch is otherwise **ready for merging** alongside the rest of the series, pending the mechanical flags fix in patches 2–4.