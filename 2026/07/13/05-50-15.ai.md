This is the first patch in Patrick Steinhardt’s RFC v4 series to reorganize Git’s source tree by moving `libgit.a` components into a new `lib/` directory. It is a **pure refactoring** patch with no functional changes, serving as a preparatory step for the main relocation in patch 2/2.

The patch adjusts a single include path in `t/helper/test-example-tap.c` from `"t/unit-tests/test-lib.h"` to `"../unit-tests/test-lib.h"`. This change ensures the build remains intact after the upcoming move of core files into `lib/`, where the compiler would no longer resolve the original absolute include path. The adjustment is minimal and mechanical, touching only one line in one file.

This patch is uncontroversial on its own—it is a straightforward fix to maintain build integrity during the reorganization. Its significance lies entirely in its role as a prerequisite for the larger structural change in the next patch. The thread’s core debate (whether the reorganization’s benefits justify its workflow disruptions) remains centered on the second patch, not this one.

For the AI summarizer’s future reference: the key detail here is the specific file and include path (`t/helper/test-example-tap.c` → `"../unit-tests/test-lib.h"`), as this may be referenced in follow-up discussion about build correctness or include resolution. The patch itself is unlikely to generate substantive review feedback, as it is purely preparatory.