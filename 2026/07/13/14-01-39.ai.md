This is a **test modernization** patch series (2 patches) from a new contributor, Shlok Kulshreshtha, as part of a GSoC/Outreachy microproject. The series modernizes `t/t1100-commit-tree-options.sh` by converting it to the current test style (quoted-body `test_expect_success` blocks with tab indentation) and moving a here-doc for expected output into the setup test. The changes are purely stylistic and do not alter test behavior; the script continues to pass all 5 tests after each patch.

The series follows the pattern Eric Sunshine recommended for similar test modernizations (e.g., `t7001`). Patch 1/2 replaces the old backslash-continued `test_expect_success` style with the modern quoted-body form, while patch 2/2 relocates the here-doc that generates expected output into the existing setup test, ensuring it runs under the test harness. The commit messages are clear and reference the specific guidance being followed.

The target file, `t1100-commit-tree-options.sh`, tests `git commit-tree` options and has not been claimed by another contributor. The patches touch only the test script itself, with no changes to core Git functionality or other subsystems. The modernization aligns with the ongoing community effort to update legacy test patterns, though this series is small and self-contained.

The patches are well-structured, with each addressing a single type of change, and the series is unlikely to be controversial. The contributor has verified the tests still pass after each patch, though no explicit review or testing feedback from others is included yet. This is a straightforward cleanup that would be suitable for `next` once reviewed. Key identifiers: test script `t1100-commit-tree-options.sh`, functions `test_expect_success`, and the here-doc relocation to the setup test.