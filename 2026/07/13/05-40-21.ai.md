This is Junio C Hamano’s "What’s cooking in git.git" report for July 2026 (#05), summarizing the state of all in-flight topics in the Git project’s integration branches. The report is the authoritative snapshot of what is queued for the next release and what is still under review.

The report lists 58 topics, grouped into four sections: New Topics (6), Stalled (4), Cooking (46), and Discarded (1). Below is the key technical detail a follow-up AI session would need to track discussion:

- **New Topics needing review**
  - `js/pack-objects-delta-size-t` (12 commits): widens object-size fields from `unsigned long` to `size_t` across pack-objects, delta encoding, and zlib wrappers to avoid truncation on 64-bit Windows.
  - `pz/fetch-submodule-errors-config` (3 commits): adds `fetch.submoduleErrors` config and `--submodule-errors` CLI flag to make submodule fetch failures non-fatal; also fixes a premature failure in recursive fetches.
  - `gr/add-e-use-apply-api` (1 commit): replaces the `run_command()` call in `git add -e` with a direct `apply_all_patches()` call, removing a subprocess.
  - `ps/odb-for-each-object-filter` (9 commits): extends `odb_for_each_object()` with object filters and teaches `git cat-file --batch-all-objects` to use the generic interface, eliminating direct ODB backend access.
  - `ps/refs-wo-the-repository` (8 commits): removes remaining uses of `the_repository` from the refs subsystem and worktree API by passing a repository pointer down the call chain.
  - `jt/receive-pack-use-odb-transactions` (11 commits): refactors `git receive-pack` to use ODB transaction interfaces instead of `tmp_objdir`, moving it closer to ODB backend agnosticism.

- **Stalled topics**
  - `kh/doc-trailers` (10 commits): documentation overhaul for `git interpret-trailers`; stalled awaiting reroll.
  - `sn/rebase-update-refs-symrefs` (1 commit): teaches `git rebase --update-refs` to skip branch symref aliases; stalled awaiting response.
  - `ap/http-redirect-wwwauth-fix` (1 commit): preserves `WWW-Authenticate` headers across redirects; marked “Will discard.”
  - `jt/config-lock-timeout` (1 commit): adds `core.configLockTimeout` for retrying config.lock acquisition; stalled awaiting response.

- **Cooking topics (highlights)**
  - `ps/setup-split-discovery-and-setup` (16 commits): splits repository discovery and configuration into two phases, preparing for unified repository setup.
  - `pw/rebase-drop-notes-with-commit` (15 commits): fixes rebase post-rewrite notes-copying logic so dropped commits no longer incorrectly copy notes.
  - `tb/repack-geometric-cruft` (11 commits): teaches `git repack --geometric --cruft` to combine geometric rollup with cruft-pack creation.
  - `ps/cat-file-remote-object-info` (13 commits): adds `remote-object-info` to `git cat-file --batch-command`, allowing clients to request object metadata (e.g., size) from a remote server without downloading the object.
  - `hn/history-squash` (5 commits): adds a `squash` subcommand to the experimental `git history` command to fold a range of commits into one.
  - `kk/merge-base-exhaustion` (11 commits): optimizes merge-base computation by stopping early when one side’s exclusive commits are exhausted, yielding large speedups for one-sided histories.
  - `ps/shift-root-in-graph` (4 commits): indents root commits in `git log --graph` to visually distinguish them from unrelated commits above.

- **Key names introduced or modified**
  - Config: `fetch.submoduleErrors`, `core.configLockTimeout`, `branch.<name>.deleteMerged`, `diff.<driver>.process`
  - CLI options: `--submodule-errors`, `--linearize` (for `git replay`), `--delete-merged` (for `git branch`)
  - Commands: `git refs create|delete|update|rename`, `git history squash|drop`, `git cat-file --batch-command remote-object-info`
  - Functions: `odb_for_each_object()`, `odb_prepare()`, `prio_queue_get()`, `next_commit_to_show()`
  - Structs/fields: `struct repo_discovery`, `struct object_info_source`, `struct paint_state`

- **Subsystems touched**
  - Object database (ODB): pluggable housekeeping, transactions, filters, `odb_for_each_object()`
  - Refs: reftable hardening, `the_repository` removal, new `git refs` subcommands
  - Pack-objects & delta encoding: size field widening
  - Rebase & sequencer: autosquash empty-commit handling, notes-copying logic
  - Merge-base & commit-reach: generation-ordered early exit, side-exhaustion optimization
  - Log & graph: root-commit indentation, line-log limited operations
  - Fetch & submodules: submodule error handling
  - Apply & add: direct apply API usage
  - Test infrastructure: `test_grep` helper, `greplint`, `GIT_TEST_LONG` fixes
  - Build system: universal macOS builds with Rust, Meson hook-list buildfix

- **Likely points of contention**
  - `tb/repack-geometric-cruft`: geometric repacking with cruft packs may interact poorly with existing repack workflows.
  - `ps/cat-file-remote-object-info`: remote object metadata queries could be seen as feature creep in the protocol.
  - `hn/history-squash` and `ps/history-drop`: new subcommands in the experimental `git history` command may spark debate over command-line UI design.
  - `kk/merge-base-exhaustion`: the early-exit optimization could regress edge cases with clock skew or v1 commit graphs.