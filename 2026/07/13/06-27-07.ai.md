**Summarizer brief mode**

This is a short status update from Michael Montalbo on the stalled `mm/diff-process-hunks` topic, which introduces `diff.<driver>.process` configuration to delegate hunk detection to a long-running external process while letting Git handle all output formatting.

The topic consists of six commits that touch the diff, blame, sub-process, userdiff, and xdiff subsystems. Key changes include:
- A new `diff.<driver>.process` config key that specifies an external command to act as a hunk provider.
- Support in `xdiff` for external hunks via `xpparam_t`.
- Integration with `git blame` to consult the external process for no-hunk detection.
- Bypassing the external process when `--no-ext-diff` is used or in `git format-patch`.
- Refactoring of the sub-process API to separate process lifecycle from hashmap management.

The topic has been stalled since mid-June awaiting a reroll. Michael now states he will post a reroll within the week, which should unblock review of the external-diff-process feature. No technical objections or design changes are mentioned in this update.