This is a brief follow-up in the `git log --graph` visual root indentation series, now at **v11**. The email exchanges a minor mechanical tweak on **patch 7/7** (the opt-out mechanism) after Junio Hamano’s surface-level review.

Junio approves the change from `int`/`unsigned int` to bit fields for the two boolean flags (`no_graph_indent` and `graph_indent_set`) in `struct rev_info`, but suggests a readability improvement: replacing `revs->graph_indent_set > 0` with `revs->graph_indent_set` in one conditional check, since the field is now a single bit. Pablo acknowledges the suggestion and will incorporate it. This is a non-functional change and does not affect the rest of the series.

The series remains **technically complete and ready for final review**. Patches 1/7 (test refactoring) is already merged; patches 2/7–6/7 (unified helper, lookahead buffer, visualization logic, four-level wrap, and configuration refactoring) are unchanged and stable. Junio’s feedback here is the only open item and can be addressed with a trivial follow-up if the series is otherwise approved. No substantive concerns are raised in this exchange.