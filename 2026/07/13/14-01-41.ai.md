This is the second patch in a two-patch **test modernization** series from Shlok Kulshreshtha, targeting `t/t1100-commit-tree-options.sh`. The patch moves a here-doc that generates the `expected` output file into the existing setup test (`test_expect_success 'test preparation: write empty tree'`). This ensures the code runs under the test harness, so failures are properly reported rather than silently ignored. The change uses a `<<-` here-doc to allow indentation within the test block, improving readability without altering behavior.

The patch is a direct follow-up to the first patch, which converted the script’s `test_expect_success` blocks to the modern quoted-body style. Together, the series modernizes the test script’s structure while preserving its exact functionality—all five tests continue to pass. The motivation is purely stylistic, aligning with the project’s ongoing effort to update legacy test patterns, and follows established guidance (e.g., Eric Sunshine’s work on `t7001`).

The diff is minimal and focused, touching only the test script. The commit message clearly explains the rationale and implementation, referencing the specific issue (unprotected code outside tests) and the solution (indentation-friendly here-doc). No new symbols or behaviors are introduced, and the patch is unlikely to be controversial.

This patch completes the series, which is now ready for review. Given its adherence to established patterns and self-contained scope, it appears suitable for `next` once reviewed. Key identifiers: test script `t/t1100-commit-tree-options.sh`, the `expected` file, and the setup test `test preparation: write empty tree`.