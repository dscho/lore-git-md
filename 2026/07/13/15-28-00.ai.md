This is a brief follow-up in the `git branch --delete-merged` series, specifically addressing the flags-based API refactoring in patch 2/7. Phillip Wood confirms the maintenance risk he previously identified—splitting the `flags` variable into local booleans (`force`, `quiet`) creates two sources of truth—and reiterates his recommendation to replace all local boolean uses with direct flag checks (`flags & DELETE_BRANCH_FORCE`, etc.). Harald Nordgren agrees with the fix, which is mechanical and will not alter functionality but ensures the flags remain a single source of truth. Phillip suggests documenting the rationale in the code to prevent future reviewers from reintroducing the issue.

The discussion is narrow and resolved; the only remaining action is the agreed-upon cleanup before merging. This patch remains purely preparatory for the core `--delete-merged` logic and introduces no behavioral changes. The series as a whole is implementation-complete pending this fix.