This is the first patch in Pablo Sabater’s **v11 series** implementing visual root indentation for `git log --graph`. It is a **test infrastructure refactoring** that moves the duplicated `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into `t/lib-log-graph.sh`, renaming it to `lib_test_check_graph` to follow the library’s naming convention.

The patch is **purely mechanical** and introduces no new behavior or edge cases. It consolidates 34 lines of duplicated code into a single 5-line helper, making future graph-related tests easier to write. The change is **backward-compatible**: all existing test invocations are updated to use the new helper, and the expected output remains unchanged. This patch was **merged on 2024-07-12** and is now part of the test suite.

The motivation is clear: the next patch in the series (visual root indentation) adds a new test file that will reuse this helper, avoiding further duplication. The commit message explicitly states this dependency. The patch is small, well-scoped, and has no known issues. It received an **Acked-by from Karthik Nayak** in an earlier revision, confirming its readiness. No further review is needed.