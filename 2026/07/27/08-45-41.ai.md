This is a **substantive review comment** on the first patch of the v3 series adding path-related keys to `git repo info`. Junio C Hamano points out a **forward-compatibility issue**: the patch uses `startup_info->prefix`, a struct member that was recently removed from the tip of `master`. Since the series is not a bugfix targeting `maint`, he suggests rebasing onto a more current `master` to avoid introducing a deprecated field.

The comment is narrowly scoped to this single patch (1/7) and does not question the overall design or motivation of the series. It is a mechanical but important correction—using a removed field would break compilation. The author will need to update the implementation to use the replacement mechanism (not specified in this email) before the series can proceed. No other technical concerns are raised here, and the rest of the series remains unaffected.