Thread identity
Subject: `git cat-file --batch-command` assertion failure when omitting `%(objecttype)`
Series version: none (bug report)
Originating author: Alan Stokes
Thread type: bug report

Core problem or goal
`git cat-file --batch-command` crashes with an assertion failure in `print_object_or_die()` when the output format string does not include `%(objecttype)` and the `contents` command is issued. The crash is deterministic, affects a core plumbing command, and has existed since at least Git 2.43.0.

Current status
Under review. Jeff King (Peff) has confirmed the bug, provided a minimal fix for the immediate crash, and suggested a more targeted approach (saving/restoring `data->info.typep` around the `contents` command) to avoid performance regressions. No formal patch or test coverage yet; discussion is at the design/feedback stage.

Key technical details
- File: `builtin/cat-file.c`, line 387 (`print_object_or_die()`)
- Subsystem: `cat-file` plumbing, `--batch-command` mode
- New/renamed symbols: none
- Behavior: assertion failure (NULL dereference) when `contents` is requested without `%(objecttype)` in the format string
- Workaround: include `%(objecttype)` in the format string or use `--batch` instead of `--batch-command`
- Performance consideration: Peff’s minimal fix enables type-checking for all subsequent commands, which may slow down lightweight `info` requests
- Test coverage: none yet; reproduction steps provided in the report; target test file: `t1006-cat-file.sh`

Open questions and loose ends
- Whether to implement Peff’s suggested targeted fix (save/restore `data->info.typep`) or proceed with the broader type-checking approach
- No maintainer feedback yet; no patch proposed (Alan Stokes invited to submit one with tests)

Participant positions
- Alan Stokes (author): reports the bug, provides reproduction steps, and suggests potential fixes (always fetch type metadata or do so conditionally on `contents` command)
- Jeff King (Peff): confirms the bug, provides a minimal diff, and recommends a more precise fix to avoid performance impact on `info` commands. Encourages Alan to submit a patch with test coverage.

Related work
None mentioned.