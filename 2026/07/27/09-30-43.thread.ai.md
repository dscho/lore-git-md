Thread identity
Subject: `git cat-file --batch-command` assertion failure when omitting `%(objecttype)`
Series version: none (bug report)
Originating author: Alan Stokes
Thread type: bug report

Core problem or goal
`git cat-file --batch-command` crashes with an assertion failure in `print_object_or_die()` when the output format string does not include `%(objecttype)` and the `contents` command is issued. The crash is deterministic, affects a core plumbing command, and has existed since at least Git 2.43.0.

Current status
Under review. The technical solution is settled: Pablo Sabater’s implementation of Jeff King’s targeted fix (save/restore `data->info.typep` around the `contents` command) is confirmed correct and avoids performance regressions. A formal patch with test coverage in `t1006-cat-file.sh` is the only remaining step. Alan Stokes has declined to submit it; the patch is now awaiting a volunteer (likely Pablo Sabater or another contributor).

Key technical details
- File: `builtin/cat-file.c`, line 387 (`print_object_or_die()`)
- Subsystem: `cat-file` plumbing, `--batch-command` mode
- New/renamed symbols: none
- Behavior: assertion failure (NULL dereference) when `contents` is requested without `%(objecttype)` in the format string
- Workaround: include `%(objecttype)` in the format string or use `--batch` instead of `--batch-command`
- Performance consideration: the targeted fix avoids over-fetching type metadata for subsequent `info` commands, preserving performance for lightweight requests
- Test coverage: none yet; reproduction steps provided in the report; target test file: `t1006-cat-file.sh`

Open questions and loose ends
- No formal patch (with tests) has been submitted. The fix is ready; the thread is in a holding pattern until someone submits it.

Participant positions
- Alan Stokes (author): reports the bug, provides reproduction steps, suggests potential fixes, and confirms the targeted fix is "pretty simple and correct." Declines to submit the formal patch due to time constraints and unfamiliarity with the process.
- Jeff King (Peff): confirms the bug, provides a minimal diff, and recommends the targeted fix (save/restore `data->info.typep`) to avoid performance impact on `info` commands. Encourages submission of a patch with test coverage.
- Pablo Sabater: implements and tests Peff’s suggested fix, confirming it resolves the crash without regressing performance. Notes a minor indentation nit in the patch.

Related work
None mentioned.