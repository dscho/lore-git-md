This is a **test infrastructure** patch series (v2) that introduces and uses a `commit_body()` helper in the Git test suite. The series addresses feedback from Junio Hamano on v1, which identified two missed call sites: the `message_body()` helper in `t7509-commit-authorship.sh` and an inline pipe in `t3404-rebase-interactive.sh`. Both followed the same exit-code-masking pattern as the ~60 instances already converted and are now included in the update.

The core goal remains unchanged: replacing duplicated `git cat-file commit <rev> | sed -e '1,/^$/d'` patterns with a reusable shell helper that correctly propagates exit codes. The v2 series converts **61 call sites across 12 test scripts**, plus the removed `message_body()` helper, reducing the total footprint from 123 lines to 91. The helper is documented in `t/README` and added to `t/test-lib-functions.sh`, with no user-visible impact—this is purely internal test suite cleanup.

The changes are mechanical and well-scoped. The author verified that all touched scripts pass tests, including deliberate `git cat-file` failure cases, and the conversion handles edge cases like `t7600-merge.sh` (which greps the stripped body) and `t3900-i18n-commit.sh` (which pipes into `iconv`). The two additions in v2 are straightforward: `t7509`’s `message_body()` helper is dropped entirely, and the inline pipe in `t3404` is replaced with `commit_body()`.

No open questions remain. The series is a clean refactoring with clear motivation (fixing an exit-code hole noted in a prior bugfix, `b6b276974e`) and thorough execution. It’s ready for review, though no merge signals have appeared yet. The files touched are `t/test-lib-functions.sh`, `t/README`, and 12 test scripts (`t3404`, `t3405`, `t3408`, `t3434`, `t3900`, `t4150`, `t7500`, `t7501`, `t7502`, `t7509`, `t7600`, `t7604`, `t7614`). Key names introduced: `commit_body()` (function), `.commit` (temporary file).