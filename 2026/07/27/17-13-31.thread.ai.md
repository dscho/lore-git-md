Thread identity: patch series (single patch), originating author David Lin, thread type bugfix for the pack-bitmap subsystem.

Core problem or goal: fix an off-by-one edge case in `find_objects()` in the pack-bitmap subsystem where objects at position zero in the base bitmap were incorrectly excluded from being recognized as already represented, causing redundant work (reloading reachability bitmaps or adding to the not-mapped list).

Current status: under review; no prior versions of this patch exist. The fix is minimal and targeted, addressing only the conditional check `pos > 0` to `pos >= 0` in `find_objects()`. Taylor Blau’s review confirms the fix’s correctness and suggests a **substantive** test addition for non-pseudo-merge scenarios where a position-zero object appears after stored bitmaps.

Key technical details:
- Files touched: `pack-bitmap.c` (core bitmap traversal logic), `t/t5333-pseudo-merge-bitmaps.sh` (test for pseudo-merge bitmap behavior).
- Key symbols: `bitmap_position()`, `find_objects()`, `SEEN` flag.
- Behavior change: no change to the resulting object set, but eliminates redundant work by correctly recognizing objects at position zero as already represented in the base bitmap.
- Test coverage: adds an assertion for expected bitmap hit count in a pseudo-merge scenario; Taylor Blau suggests expanding coverage to include non-pseudo-merge cases (e.g., `git rev-list --objects --use-bitmap-index HEAD HEAD`).

Open questions and loose ends:
- **Test coverage gap**: The patch currently tests only the pseudo-merge scenario. Taylor Blau requests a test for non-pseudo-merge cases where a position-zero object appears after stored bitmaps. This is the sole remaining action item before integration.

Participant positions:
- **Taylor Blau**: Provided a **substantive review**, confirming the fix’s correctness and ruling out broader fallout from the `bitmap_position()` change. Suggested two improvements: (1) a surface-level commit message wrap (72 characters), and (2) a **substantive** test addition for non-pseudo-merge scenarios. Dismissed a potential refactoring of `bitmap_position()` as unnecessary noise.

Related work: fixes a regression introduced in commit 11d45a6e6a. No other in-flight topics or external dependencies mentioned.