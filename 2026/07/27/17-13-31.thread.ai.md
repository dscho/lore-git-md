Thread identity: patch series (single patch), originating author David Lin, thread type bugfix for the pack-bitmap subsystem.

Core problem or goal: fix an off-by-one edge case in `find_objects()` in the pack-bitmap subsystem where objects at position zero in the base bitmap were incorrectly excluded from being recognized as already represented, causing redundant work (reloading reachability bitmaps or adding to the not-mapped list).

Current status: under review; second iteration (`v2`) posted. The fix remains minimal and targeted, adjusting the conditional check `pos > 0` to `pos >= 0` in `find_objects()`. Taylor Blau’s **substantive review** of `v1` confirmed the fix’s correctness and prompted two updates in `v2`: (1) a surface-level commit message rewording to clarify the nature of the redundant work avoided, and (2) a **substantive** test addition covering non-pseudo-merge scenarios where a position-zero object appears after stored bitmaps. No further action items remain; the patch is ready for integration.

Key technical details:
- Files touched: `pack-bitmap.c` (core bitmap traversal logic), `t/t5333-pseudo-merge-bitmaps.sh` (test file).
- Key symbols: `bitmap_position()`, `find_objects()`, `SEEN` flag.
- Behavior change: internal-only; the resulting object set is unchanged, but redundant work (bitmap reloads or additions to the not-mapped list) is eliminated by correctly recognizing objects at position zero as already represented in the base bitmap.
- Test coverage: `v1` added an assertion for expected bitmap hit count in a pseudo-merge scenario; `v2` expands coverage to include non-pseudo-merge cases (e.g., `git rev-list --objects --use-bitmap-index HEAD HEAD`), addressing the sole gap identified in review. The test now verifies both pseudo-merge and non-pseudo-merge scenarios, ensuring the fix works regardless of how the position-zero object is encountered.
- Regression source: commit 11d45a6e6a.

Open questions and loose ends: none. All review feedback has been addressed.

Participant positions:
- **Taylor Blau**: Provided a **substantive review** of `v1`, confirming the fix’s correctness and ruling out broader fallout from the `bitmap_position()` change. Suggested two improvements: (1) a surface-level commit message rewording (addressed in `v2`), and (2) a **substantive** test addition for non-pseudo-merge scenarios (also addressed in `v2`). Dismissed a potential refactoring of `bitmap_position()` as unnecessary noise.

Related work: fixes a regression introduced in commit 11d45a6e6a. No other in-flight topics or external dependencies mentioned.