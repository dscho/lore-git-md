Here is the updated AI-mode thread summary, incorporating Junio’s latest response while preserving all prior state verbatim for unchanged fields:

---

Thread identity: "What's cooking in git.git" periodic integration report from Junio C Hamano, July 2026 (#12), with follow-up technical review, maintainer response, policy discussion, reviewer reflection, and evolving design proposals for `hn/history-squash`. Originating author: Junio C Hamano. Thread type: integration status report, technical review, maintainer policy proposal, design discussion, and procedural reflection.

Core problem or goal: Provide a snapshot of all in-flight topics across Junio's integration branches (`seen`, `next`, `master`), including new proposals, stalled efforts, and topics cooking in `next` or ready for `master`. Follow-up discussions challenge the correctness and design of two topics: `hn/history-squash` (ejected from `next` due to fundamental flaws in reachability logic and UX inconsistencies) and `hn/branch-delete-merged` (flagged for upstream push-check logic and merged-hierarchy behavior changes). Phillip Wood is actively developing fixups for `hn/history-squash`; `hn/branch-delete-merged` remains in `next` pending author response to Phillip’s review and Junio’s deferral to community consensus. Junio has proposed a temporary moratorium on accepting new topics into `seen` unless they receive substantive review from the community, aiming to prevent premature promotion to `next` and reduce the risk of flawed topics slipping through. The discussion now centers on systemic review quality challenges and the design of `hn/history-squash`’s message-handling behavior, with Junio engaging critically with Phillip Wood’s latest proposal for a context-sensitive default editor behavior based on commit message types.

Current status: As of July 2026 (#12), 50+ topics remain in flight. Key branch states:
- **`master`**: 3 topics graduated since the last report: `jc/exclude-first-parent-seen`, `hn/url-push-tracking`, and `ps/cat-file-remote-object-info`. No new graduations in this update.
- **`next`**: 11 topics remain (down from 12). `hn/history-squash` was ejected after Phillip Wood’s review exposed fundamental correctness flaws; it now resides in `seen` pending fixups. `hn/branch-delete-merged` remains in `next` but faces unresolved design questions from Phillip Wood and Junio’s deferral to broader consensus. Other topics unchanged.
- **`seen`**: All other topics reside here, including stalled efforts and new proposals. `hn/history-squash` now returns to `seen` pending fixups from Phillip Wood. Junio’s proposed moratorium on accepting new topics into `seen` without substantive review remains open-ended.

Key technical details:
- **Files/subsystems touched**:
  - **`hn/history-squash`**: Implementation in `builtin/history.c` and `revision.c` (reachability logic). The command is part of the `git history` suite, sharing infrastructure with `git rebase` and `git replay`.
    - **Reachability logic**: The `UNINTERESTING` flag handling in `revision.c` is confirmed as flawed. Phillip Wood’s critique demonstrates the code incorrectly assumes `UNINTERESTING` commits imply a `BOTTOM` commit or preclude root commits, violating Git’s revision traversal semantics. Fixups are in progress to address these edge cases (multi-tip histories, `UNINTERESTING` commits, root commits).
    - **Message templates**: Reuse of rebase’s squash template (`git-rebase--interactive.sh`) for multi-commit squashing remains contentious. The debate now includes four competing proposals for message handling, with Junio’s latest response introducing new edge cases and trade-offs:
      1. **Harald Nordgren’s proposal**: Remove `--reedit-message` entirely and make re-editing the default behavior (editor always opens during squash). This eliminates the need for a `--no-edit` switch but does not address Phillip’s concern about preserving `amend!`/`squash!` messages or the inconsistency with other `git history` commands.
      2. **Matt Hunter’s proposal**: Introduce conventional `--edit` and `--no-edit` options (replacing `--reedit-message`), arguing this aligns with other Git commands (e.g., `git commit`) and allows future configuration (e.g., `history.editSquashMsg`). This preserves scriptability via `--no-edit` while offering a clearer UX. Matt notes the tension with the existing `--reedit-message` precedent in `git history` but does not resolve it.
      3. **Phillip Wood’s initial proposal**: Default editor behavior should depend on the *type* of commits being squashed. If the commits are primarily `fixup!` or `amend!` messages, the editor might not open (since the user’s intent is already clear), but for other commits, the editor would open to allow review. This introduces a dynamic default that could reduce friction for common cases while still allowing user override via `--edit`/`--no-edit`. Phillip also proposes deprecating `--reedit-message` in favor of `--edit` for consistency with other Git commands.
      4. **Phillip Wood’s latest proposal**: Refines the context-sensitive default by formalizing the rule: the editor opens by default *unless* the commits being squashed are primarily `fixup!` or `amend!` messages. This aims to reduce friction for common cases (where the user’s intent is already clear) while preserving flexibility via `--edit`/`--no-edit`. The proposal also reiterates concerns about the rebase squash template creating a fragmented, hard-to-edit message when squashing into multiple targets.
    - **Junio’s critique of Phillip’s latest proposal**:
      - **Single `amend!` commit**: Junio agrees that a single `amend!` commit (which discards the base commit’s message) is a clear signal that no further editing is needed, aligning with Phillip’s intent.
      - **`fixup!` commits**: Junio questions whether `fixup!` commits should trigger the same behavior, as the need for a log message change is ambiguous. This introduces a new dimension to the debate: whether `fixup!` and `amend!` should be treated identically in the dynamic default.
      - **Multiple `amend!` commits**: Junio raises a new edge case: when squashing multiple `amend!` commits, the intent may not be as clear as with a single `amend!` commit. This complicates the dynamic default’s logic and may require additional rules or user override mechanisms.
      - **User confusion**: Junio suggests that the dynamic behavior could be confusing for users, as the editor’s behavior would depend on subtle, context-dependent rules. This aligns with his broader hesitation about introducing complex, non-deterministic defaults in Git commands.
    - **Message handling**: The debate now centers on:
      - Whether `amend!`/`squash!` messages should be preserved by default (Phillip’s concern), and whether this applies uniformly to `fixup!` commits (Junio’s question).
      - Whether the editor should open by default (Harald’s proposal), never open by default (Matt’s `--no-edit` default), open conditionally based on commit message types (Phillip’s proposals), or follow a hybrid approach.
      - Whether `--reedit-message` should be removed (Harald), replaced with `--edit`/`--no-edit` (Matt and Phillip), or deprecated (Phillip).
      - Whether the rebase squash template is appropriate for multi-commit squashing (Phillip’s critique).
      - How to handle edge cases like multiple `amend!` commits (Junio’s new concern).
    - **Ref safety**: Refuses to squash commits pointed to by tags or remote-tracking refs, unlike other `git history` commands. This inconsistency is under scrutiny but not yet addressed.
  - **`hn/branch-delete-merged`**: Implementation in `builtin/branch.c` and `remote.c` (upstream push-check logic). The series adds `--delete-merged` to `git branch`.
    - **Upstream push-check logic**: The current implementation’s method for determining whether pushing a branch would update its upstream is described as "dodgy" by Phillip Wood, raising concerns about reliability. Junio acknowledges the issue but defers to community consensus; no fixups are yet proposed.
    - **Merged hierarchies**: The series changes behavior for branches that are both merged *and* upstream of other branches. Previously, only the topmost merged branch (directly upstream of an unmerged branch) was preserved; now, the entire merged hierarchy is kept. Phillip Wood prefers the old behavior, but Junio frames this as a matter for broader discussion. No consensus yet.
- **New/renamed symbols**:
  - **`hn/history-squash`**:
    - CLI options: `--reedit-message` (proposed for removal or deprecation), `--squash` (core functionality), `--edit`/`--no-edit` (proposed by Matt Hunter and endorsed by Phillip Wood).
    - Placeholders: None added, but the command interacts with `amend!` and `squash!` commit messages.
  - **`hn/branch-delete-merged`**:
    - CLI option: `--delete-merged` (new functionality).
- **Behavior changes**:
  - **`hn/history-squash`**:
    - **Correctness flaws**: The implementation fails to handle:
      - `UNINTERESTING` commits (e.g., `git history squash ^A B` where `A` is `UNINTERESTING`).
      - Root commits (commits with no parents).
      - Multi-tip histories (e.g., `git history squash ^A C D` in `-A-B-C \ D`).
    - **Ref safety**: Refuses to squash commits pointed to by tags or remote-tracking refs, unlike other `git history` commands. This inconsistency is under scrutiny but not yet addressed.
    - **Message handling**: The debate now includes Junio’s critique of Phillip’s latest proposal, introducing new edge cases (multiple `amend!` commits, `fixup!` vs. `amend!` treatment) and reinforcing concerns about user confusion. The message template issue remains unresolved.
  - **`hn/branch-delete-merged`**:
    - **Upstream push-check logic**: The method for determining whether pushing a branch would update its upstream is unreliable, per Phillip Wood. Junio acknowledges the issue but defers to community consensus; no fixups proposed yet.
    - **Merged hierarchies**: The series changes behavior to preserve the entire merged hierarchy (previously, only the topmost merged branch was preserved). Phillip Wood prefers the old behavior, but Junio frames this as a matter for broader discussion. No consensus yet.
  - Other changes (unchanged).
- **On-disk format**: No changes reported in this cycle.
- **Test coverage**:
  - **`hn/history-squash`**: No explicit test cases for edge cases (e.g., multi-tip histories, `UNINTERESTING` commits, root commits) were cited. Phillip Wood’s fixups will likely include tests for these gaps. The message-handling proposals (Harald’s, Matt’s, Phillip’s) lack test cases or technical rationale, though Phillip’s latest proposal introduces a new dimension (commit message types) that will require validation. Junio’s critique adds further complexity (multiple `amend!` commits, `fixup!` vs. `amend!` treatment) that may necessitate additional test coverage.
  - **`hn/branch-delete-merged`**: No test failures or edge cases mentioned, but the upstream push-check logic and merged-hierarchy behavior require validation.

Open questions and loose ends:
- **Blocked topics**:
  - **`hn/history-squash`**: Blocked pending fixups from Phillip Wood and author response. Key unresolved issues:
    - **Correctness**: Fixups will address `UNINTERESTING` commits, root commits, and multi-tip histories. No timeline for submission.
    - **User experience**:
      - Should the editor open by default (Harald’s proposal), never open by default (Matt’s `--no-edit` default), open conditionally based on commit message types (Phillip’s proposals), or follow a hybrid approach?
      - Should `amend!`/`squash!` messages be preserved by default (Phillip’s concern)? Should this apply uniformly to `fixup!` commits (Junio’s question)?
      - How should multiple `amend!` commits be handled (Junio’s new edge case)? Should the dynamic default treat them differently from single `amend!` commits?
      - Should `--reedit-message` be removed (Harald), replaced with `--edit`/`--no-edit` (Matt and Phillip), or deprecated (Phillip)?
      - Should the command align with other `git history` commands in allowing squashing of tagged/remote-tracking commits? No changes proposed yet.
      - Is reusing rebase’s squash template appropriate for multi-commit squashing (Phillip’s critique)? No changes proposed yet.
      - Is the dynamic default too complex or confusing for users (Junio’s concern)? How can the trade-off between usability and predictability be resolved?
    - **Design trade-offs**: Junio’s critique introduces new complexity to Phillip’s latest proposal, raising questions about whether the dynamic default is worth the potential confusion. The trade-off between scriptability, UX consistency, and user intent remains unresolved. The appropriateness of the rebase squash template for multi-commit operations is also contested.
  - **`hn/branch-delete-merged`**: Blocked pending resolution of:
    - Upstream push-check logic: How to reliably determine whether pushing a branch would update its upstream? Junio defers to community consensus; no fixups proposed yet.
    - Merged hierarchies: Should the old behavior (preserving only the topmost merged branch) be restored, or is the new behavior (preserving the entire hierarchy) preferred? Junio frames this as a matter for broader discussion; no consensus yet.
  - **Integration pipeline policy**: Junio’s proposal for a temporary moratorium on accepting new topics into `seen` unless they receive substantive review remains open-ended. Key questions:
    - Will the moratorium be adopted, and if so, for how long?
    - How will "substantive review" be defined or enforced?
    - What impact will this have on contributor velocity and review bandwidth?
    - How can reviewers better signal the depth of their engagement (e.g., distinguishing surface-level "lgtm" from in-depth scrutiny)?
    - Junio’s latest response reinforces the expectation that reviewers demonstrate deeper engagement to catch correctness flaws before topics reach `next`, but no concrete mechanisms or incentives have been proposed.
  - Other blocked topics (unchanged).
- **Integration pipeline quality**:
  - Junio’s latest response subtly validates Matt Hunter’s concern about the difficulty of providing substantive positive reviews while reinforcing the expectation that reviewers err on the side of caution. The procedural discussion is now paused, awaiting either Phillip Wood’s fixups for `hn/history-squash` or further policy proposals. The episode underscores the need for structural adjustments (e.g., stricter review requirements) to prevent similar incidents.
  - Matt Hunter’s reflections highlight procedural gaps: reviewers may not track integration status, and surface-level approvals can inadvertently accelerate flawed patches. Junio’s response implies that the burden of proof lies with reviewers to demonstrate engagement, but no tools or incentives (e.g., reviewer checklists, CI integration) have been proposed to address this.
- **Stalled topics** (unchanged).
- **Ejected topics** (unchanged).

Participant positions:
- **Maintainer (Junio C Hamano)**:
  - Ejected `hn/history-squash` from `next` after Phillip Wood’s review exposed fundamental correctness flaws. Acknowledges reliance on community feedback to catch such issues and expresses concern about the quality of review in `next`. No other topics were affected, but the response implies heightened scrutiny for future merges.
  - On `hn/branch-delete-merged`: Acknowledges Phillip Wood’s concerns about upstream push-check logic and merged-hierarchy behavior but declines to take a firm position, deferring to broader community consensus. The topic remains in `next` pending author response or further discussion.
  - Proposed a temporary moratorium on accepting new topics into `seen` unless they receive substantive review. Frames the proposal as a pragmatic response to the tension between velocity and quality, not as a criticism of contributors. The proposal remains open-ended.
  - On `hn/history-squash` message handling:
    - Endorses the experimental nature of `git history`, signaling openness to UI tweaks but also caution about stability.
    - Agrees that the `--reedit-message` precedent is "unfortunate" but stops short of endorsing its deprecation or replacement with `--edit`, leaving the option naming debate unresolved.
    - Critiques Phillip Wood’s latest proposal for a context-sensitive default editor behavior:
      - Agrees that a single `amend!` commit is a clear signal that no further editing is needed.
      - Questions whether `fixup!` commits should trigger the same behavior, as the need for a log message change is ambiguous.
      - Raises a new edge case: multiple `amend!` commits, where the intent may not be as clear as with a single `amend!` commit. This complicates the dynamic default’s logic.
      - Suggests that the dynamic behavior could confuse users, aligning with his broader hesitation about introducing complex, non-deterministic defaults in Git commands.
    - Does not resolve the debate but frames it as a tension between usability and predictability. The next steps likely depend on further iteration from Phillip or input from other reviewers.
- **Topic authors**:
  - **Harald Nordgren** (`hn/history-squash`):
    - Proposes to remove `--reedit-message` and make re-editing the default behavior (editor always opens during squash). This does not address Phillip’s concern about preserving `amend!`/`squash!` messages or the inconsistency with other `git history` commands. The message template issue remains unaddressed.
    - Does not engage with the broader correctness concerns (e.g., `UNINTERESTING` commits, root commits, multi-tip histories) raised in Phillip’s review. Fixups are still expected from Phillip Wood.
  - **Han-Wen Nienhuys** (`hn/branch-delete-merged`): No response yet to Phillip Wood’s review or Junio’s deferral.
  - Others unchanged.
- **Reviewers**:
  - **Phillip Wood**:
    - Substantive review of `hn/history-squash` identifies critical correctness and UX issues, leading to the topic’s ejection from `next`. Now actively developing fixups to address the reachability logic flaws (e.g., `UNINTERESTING` commits, root commits, multi-tip histories). His review is high-weight due to expertise in sequencer/rebase subsystems and engagement with edge cases.
    - Substantive review of `hn/branch-delete-merged` flags two key issues: unreliable upstream push-check logic and a behavior change for merged hierarchies. Prefers the old behavior but acknowledges the new approach may have broader support.
    - On message handling:
      - Emphasizes the need to preserve `amend!`/`squash!` messages by default, regardless of editor behavior. This concern remains unaddressed in the current proposals.
      - Proposes a context-sensitive default for editor invocation: the editor opens by default *unless* the commits being squashed are primarily `fixup!` or `amend!` messages (since the user’s intent is already clear). This aims to reduce friction for common cases while preserving flexibility via `--edit`/`--no-edit`.
      - Critiques the `--reedit-message` precedent in `git history fixup` as "unfortunate" and proposes deprecating it in favor of `--edit` for consistency with other Git commands.
      - Critiques the rebase squash template as inappropriate for multi-commit squashing, arguing it creates a fragmented, hard-to-edit message when squashing into multiple targets.
      - Encourages Matt Hunter to push back on dismissed design points if the rationale remains unclear, framing this as a productive way to improve the design.
    - His latest proposal is high-weight for UX consistency, scriptability, and user intent preservation, but Junio’s critique introduces new complexity (multiple `amend!` commits, `fixup!` vs. `amend!` treatment) that may require further refinement.
  - **Matt Hunter**:
    - Reflects on his earlier surface-level review of `hn/history-squash` ("lgtm"), acknowledging it was based on limited scrutiny and expressing concern about the weight his feedback carried as a relative newcomer. Raises procedural questions about how reviewers should signal engagement depth and track integration status.
    - Proposes a middle-ground design for message handling: introduce `--edit` and `--no-edit` options (replacing `--reedit-message`), arguing this aligns with other Git commands (e.g., `git commit`) and preserves scriptability. Notes the tension with the existing `--reedit-message` precedent but does not resolve it.
    - Junio’s response to Matt’s reflection subtly validates the difficulty of providing substantive positive reviews while reinforcing the expectation that reviewers demonstrate deeper engagement. Matt’s concerns about procedural gaps remain unaddressed.
    - Engages with Phillip Wood’s latest proposal, acknowledging the value of preserving `amend!` messages and the scriptability of `--edit`/`--no-edit`. His earlier proposal is now partially superseded by Phillip’s context-sensitive default, though both share a preference for conventional option naming.
  - No other explicit Reviewed-by/Acked-by trailers cited in this update.

Related work:
- **`git history` suite**: `hn/history-squash` is part of this new command family, which shares infrastructure with `git rebase` and `git replay`. Correctness issues here may affect other commands in the suite (e.g., `git history replay`). Phillip’s fixups will likely include tests to prevent regressions.
- **Reachability logic**: The `UNINTERESTING` flag handling is shared with `git rev-list`, `git log`, and other revision-walking commands. Phillip’s concerns and fixups may have broader implications for how Git traverses commit histories.
- **Message templates**: Reuse of rebase’s squash template highlights the need for templates tailored to multi-commit operations. The debate now includes Junio’s critique of Phillip’s latest proposal, introducing new edge cases (multiple `amend!` commits, `fixup!` vs. `amend!` treatment) and reinforcing concerns about user confusion.
- **Branch deletion logic**: `hn/branch-delete-merged` interacts with `git push` and upstream tracking, which are also touched by `hn/url-push-tracking` (graduated to `master`).
- **Review bandwidth and culture**: Junio’s policy proposal and Matt Hunter’s reflections highlight systemic concerns about review quality, reviewer incentives, and the tension between velocity and correctness. Junio’s latest response reinforces the expectation that reviewers demonstrate deeper engagement but does not propose concrete mechanisms (e.g., tooling, incentives) to address the gaps. This may influence future discussions about:
  - Tooling (e.g., CI, test coverage, reviewer checklists).
  - Mentorship or onboarding for new reviewers.
  - Explicit signaling of review depth (e.g., distinguishing "lgtm" from "reviewed in depth").
  - Scriptability vs. UX trade-offs in command design (e.g., `--edit`/`--no-edit` vs. `--reedit-message`).
  - Context-sensitive defaults in Git commands (e.g., Phillip’s latest proposal for `hn/history-squash`).
  - Template design for multi-commit operations (e.g., Phillip’s critique of the rebase squash template).
  - Edge case handling in dynamic defaults (e.g., multiple `amend!` commits, `fixup!` vs. `amend!` treatment).
- Other ongoing efforts (unchanged).

---