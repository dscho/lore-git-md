Here is the updated AI-mode thread summary, preserving all prior state and incorporating Phillip Wood’s review of `hn/branch-delete-merged`:

---

Thread identity: "What's cooking in git.git" periodic integration report from Junio C Hamano, July 2026 (#12). Originating author: Junio C Hamano. Thread type: integration status report with follow-up technical review and maintainer response.

Core problem or goal: Provide a snapshot of all in-flight topics across Junio's integration branches (`seen`, `next`, `master`), including new proposals, stalled efforts, and topics cooking in `next` or ready for `master`. Follow-up discussions challenge the correctness and design of two topics: `hn/history-squash` (ejected from `next` due to fundamental flaws) and `hn/branch-delete-merged` (flagged for upstream push-check logic and merged-hierarchy behavior changes).

Current status: As of July 2026 (#12), 50+ topics remain in flight. Key branch states:
- **`master`**: 3 topics graduated since the last report: `jc/exclude-first-parent-seen`, `hn/url-push-tracking`, and `ps/cat-file-remote-object-info`. No new graduations in this update.
- **`next`**: 11 topics remain (down from 12). `hn/history-squash` was ejected after Phillip Wood’s review exposed fundamental correctness flaws. `hn/branch-delete-merged` remains in `next` but faces design questions from Phillip Wood. Other topics unchanged: performance optimizations (`tn/stash-avoid-sparse-index-expansion`, `sc/wt-status-avoid-quadratic-insertion`), bugfixes (`af/clone-revision-v0-segfault-fix`, `jk/diff-relative-cached-unmerged`), refactoring (`ps/odb-move-loose-object-writing`, `rs/tempfile-wo-the-repository`), and Rust cleanup (`bc/rust-hash-cleanups`).
- **`seen`**: All other topics reside here, including stalled efforts (`tb/repack-geometric-cruft`, `za/completion-hide-dotfiles`) and new proposals needing review (`tb/pack-with-duplicates`, `ps/cat-file-remote-object-info-type`). `hn/history-squash` now returns to `seen` pending fixups.

Key technical details:
- **Files/subsystems touched**:
  - **`hn/history-squash`**: Implementation in `builtin/history.c` and `revision.c` (reachability logic). The command is part of the `git history` suite, sharing infrastructure with `git rebase` and `git replay`.
    - **Reachability logic**: The `UNINTERESTING` flag handling in `revision.c` is confirmed as flawed. Phillip Wood’s critique demonstrates the code incorrectly assumes `UNINTERESTING` commits imply a `BOTTOM` commit or preclude root commits, violating Git’s revision traversal semantics.
    - **Message templates**: Reuse of rebase’s squash template (`git-rebase--interactive.sh`) for multi-commit squashing remains contentious.
  - **`hn/branch-delete-merged`**: Implementation in `builtin/branch.c` and `remote.c` (upstream push-check logic). The series adds `--delete-merged` to `git branch`.
    - **Upstream push-check logic**: The current implementation’s method for determining whether pushing a branch would update its upstream is described as "dodgy" by Phillip Wood, raising concerns about reliability.
    - **Merged hierarchies**: The series changes behavior for branches that are both merged *and* upstream of other branches. Previously, only the topmost merged branch (directly upstream of an unmerged branch) was preserved; now, the entire merged hierarchy is kept.
- **New/renamed symbols**:
  - **`hn/history-squash`**:
    - CLI options: `--reedit-message` (misleading name, per Phillip Wood), `--squash` (core functionality).
    - Placeholders: None added, but the command interacts with `amend!` and `squash!` commit messages.
  - **`hn/branch-delete-merged`**:
    - CLI option: `--delete-merged` (new functionality).
- **Behavior changes**:
  - **`hn/history-squash`**:
    - **Correctness flaws**: The implementation fails to handle:
      - `UNINTERESTING` commits (e.g., `git history squash ^A B` where `A` is `UNINTERESTING`).
      - Root commits (commits with no parents).
      - Multi-tip histories (e.g., `git history squash ^A C D` in `-A-B-C \ D`).
    - **Ref safety**: Refuses to squash commits pointed to by tags or remote-tracking refs, unlike other `git history` commands. This inconsistency is now under scrutiny.
    - **Message handling**: Discards `amend!`/`squash!` messages without `--reedit-message`; `--reedit-message` creates a new commit rather than editing an existing one, which Phillip Wood argues is unintuitive.
  - **`hn/branch-delete-merged`**:
    - **Upstream push-check logic**: The method for determining whether pushing a branch would update its upstream is unreliable, per Phillip Wood.
    - **Merged hierarchies**: The series changes behavior to preserve the entire merged hierarchy (previously, only the topmost merged branch was preserved).
  - Other changes (unchanged):
    - `git branch -d` now reports bisect-related deletion failures (`rs/branch-delete-bisect-warning`).
    - `git stash push` avoids sparse-index expansion for in-cone paths (`tn/stash-avoid-sparse-index-expansion`).
    - `git status` untracked path insertion reduced from O(n²) to O(n log n) (`sc/wt-status-avoid-quadratic-insertion`).
- **On-disk format**: No changes reported in this cycle.
- **Test coverage**:
  - **`hn/history-squash`**: No explicit test cases for edge cases (e.g., multi-tip histories, `UNINTERESTING` commits, root commits) were cited. Phillip Wood’s review implies these gaps contributed to the oversight.
  - **`hn/branch-delete-merged`**: No test failures or edge cases mentioned, but the upstream push-check logic and merged-hierarchy behavior require validation.

Open questions and loose ends:
- **Blocked topics**:
  - **`hn/history-squash`**: Now blocked pending fixups from Phillip Wood. Key unresolved issues:
    - Correctness: How to handle `UNINTERESTING` commits, root commits, and multi-tip histories?
    - User experience: Should `--reedit-message` be renamed or reworked? Should `amend!`/`squash!` messages be preserved by default?
    - Ref safety: Should the command align with other `git history` commands in allowing squashing of tagged/remote-tracking commits?
    - Message templates: Is reusing rebase’s squash template appropriate for multi-commit squashing?
  - **`hn/branch-delete-merged`**: Blocked pending resolution of:
    - Upstream push-check logic: How to reliably determine whether pushing a branch would update its upstream?
    - Merged hierarchies: Should the old behavior (preserving only the topmost merged branch) be restored, or is the new behavior (preserving the entire hierarchy) preferred?
  - Other blocked topics (unchanged):
    - `tb/repack-geometric-cruft`: Unresolved feedback on combining `--geometric` and `--cruft`.
    - `tb/pack-with-duplicates`: Needs review for duplicate object entry hardening.
    - `ps/odb-make-creation-pluggable`: Needs response to ODB pluggability changes.
    - Others: `pz/fetch-submodule-errors-config`, `tc/last-modified-bloom`, `kj/repo-info-more-path-keys`, `js/pack-objects-delta-size-t`, `ja/doc-synopsis-style-yet-more`.
- **Integration pipeline quality**:
  - Junio’s response to `hn/history-squash` raises broader questions about whether other topics in `next` have received adequate review. While no additional topics were ejected, the tone suggests heightened scrutiny for future merges.
- **Stalled topics** (unchanged): `hn/checkout-track-fetch` (marked for discard), `za/completion-hide-dotfiles`, `kh/doc-trailers`, `kh/doc-replay-config`.
- **Ejected topics** (unchanged): `mm/diff-process-hunks` (conflicts).

Participant positions:
- **Maintainer (Junio C Hamano)**:
  - Immediately ejected `hn/history-squash` from `next` after Phillip Wood’s review exposed fundamental correctness flaws. Acknowledges reliance on community feedback to catch such issues and expresses concern about the quality of review in `next`. No other topics were affected, but the response implies a potential reset or closer scrutiny of `next` in future reports.
  - Other markings unchanged from prior summary.
- **Topic authors**:
  - **Toon Claes** (`hn/history-squash`): No response yet to Phillip’s review or Junio’s ejection. Fixups are now expected from Phillip Wood.
  - **Han-Wen Nienhuys** (`hn/branch-delete-merged`): No response yet to Phillip Wood’s review. The series remains in `next` but may require adjustments to address the upstream push-check logic and merged-hierarchy behavior.
  - Others unchanged from prior summary.
- **Reviewers**:
  - **Phillip Wood**:
    - Substantive review of `hn/history-squash` identifies critical correctness and UX issues, leading to the topic’s ejection from `next`. Plans to submit fixups. His review is high-weight due to expertise in sequencer/rebase subsystems and engagement with edge cases (e.g., `UNINTERESTING` commits, multi-tip histories).
    - Substantive review of `hn/branch-delete-merged` flags two key issues: unreliable upstream push-check logic and a behavior change for merged hierarchies. Prefers the old behavior but acknowledges the new approach may have broader support. The review is narrowly scoped and constructive, suggesting the issues are fixable without a full rewrite.
  - No other explicit Reviewed-by/Acked-by trailers cited in this update.

Related work:
- **`git history` suite**: `hn/history-squash` is part of this new command family, which shares infrastructure with `git rebase` and `git replay`. Correctness issues here may affect other commands in the suite (e.g., `git history replay`).
- **Reachability logic**: The `UNINTERESTING` flag handling is shared with `git rev-list`, `git log`, and other revision-walking commands. Phillip’s concerns may have broader implications for how Git traverses commit histories.
- **Message templates**: Reuse of rebase’s squash template highlights the need for templates tailored to multi-commit operations. The current approach may not suit `git history squash`’s use case.
- **Branch deletion logic**: `hn/branch-delete-merged` interacts with `git push` and upstream tracking, which are also touched by `hn/url-push-tracking` (graduated to `master`).
- Other ongoing efforts (unchanged): ODB abstraction, `the_repository` removal, Rust, documentation synopsis style, performance.

---