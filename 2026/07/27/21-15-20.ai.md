This is a documentation patch that concludes the UTF-8 string-width refactoring effort.

Hardik Kumar follows through on the consensus reached in the thread: the long-standing TODO comment in `utf8.c` is replaced with a concise rationale explaining why `utf8_strwidth()` and `utf8_strnwidth()` continue to return `int` instead of `size_t`. The new comment notes that callers expect a signed result for padding and `printf()` usage, making the type change impractical without pervasive signature updates. This patch implements Junio C Hamano’s directive to document the rationale directly in `utf8.c` rather than in a separate technical document.

The patch is minimal—three lines added, two removed—and purely explanatory. It closes the loop on the withdrawn refactoring series by codifying the reasoning behind the status quo, ensuring future contributors understand the design constraints without revisiting the debate. No further technical discussion is expected.