This is a **substantive review** from Junio Hamano on the `commit_body()` test helper series. The review identifies a missed call site: the `message_body()` helper in `t7509-commit.sh` follows the same exit-code-masking pattern as the ~60 instances already converted, but was not updated in the series. Junio’s question is a direct technical observation about completeness — it does not challenge the series’ design or motivation, but points out an oversight in the mechanical conversion.

The series itself remains a well-motivated, clean refactoring of test infrastructure: it introduces a reusable shell helper to eliminate duplicated code and fix an exit-code hole, converts 11 test scripts, and has no user-visible impact. Junio’s feedback is narrow and actionable: the author will need to add `t7509` to the list of files touched in patch 2/2 and update the `message_body()` helper to use `commit_body()`. The fix is straightforward and does not affect the series’ core value.