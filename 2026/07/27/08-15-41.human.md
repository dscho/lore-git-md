This is a review comment on the test infrastructure series introducing the `commit_body()` helper. Junio Hamano points out a missed conversion: the `message_body` helper in `t7509-commit.sh` follows the same exit-code-masking pattern as the 60 instances already converted, but was not updated in this series.

The observation is brief but substantive: it identifies a concrete omission in the mechanical conversion, suggesting the series is not yet complete. The author will likely need to send a follow-up patch to address this case, either by converting `message_body` to use `commit_body()` or by removing it in favor of the new helper. No other feedback is provided, and the tone is neutral and factual. The series itself remains well-motivated and technically sound, but this oversight slightly weakens its claim to have eliminated all instances of the problematic idiom.