**Summarizer brief mode**

This is a reply in the ongoing thread about Hardik Kumar’s v3 patch changing the return type of `utf8_strwidth()` and `utf8_strnwidth()` from `int` to `size_t`. Junio C Hamano endorses Phillip Wood’s **substantive, high-weight review** opposing the patch, specifically agreeing that the TODO comment in `utf8.c` should be removed and replaced with **documentation explaining (a) why the functions return `int` and (b) why `cast_size_t_to_int()` is necessary to prevent overflows**.

The email is brief but **signals Junio’s alignment with Phillip’s position** that the patch’s approach is flawed and that the status quo (with improved documentation) is preferable. This reinforces the **blocking nature of the review feedback** and increases the likelihood that the patch will be withdrawn or abandoned unless Hardik provides a compelling counterargument. The key technical detail here is the **overflow prevention rationale** for `cast_size_t_to_int()`, which the patch removes without justification; Junio’s emphasis on this point suggests it is a critical gap in the patch’s design.

No new technical issues are introduced, but the email **solidifies the reviewer consensus against the patch’s viability**. The thread’s next steps will likely involve either (1) Hardik addressing the overflow check removal and justifying the patch’s value, or (2) the patch being dropped in favor of documenting the existing design. The focus on documentation (rather than code changes) underscores the reviewers’ skepticism about the patch’s practical benefits.