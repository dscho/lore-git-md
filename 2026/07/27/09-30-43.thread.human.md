This thread is a straightforward bug fix for a crash in `git cat-file --batch-command`. The issue, reported by Alan Stokes, caused an assertion failure when the `contents` command was used without `%(objecttype)` in the format string. Jeff King (Peff) confirmed the bug, proposed a targeted fix, and submitted a patch with test coverage, which Pablo Sabater tested and verified. Junio C Hamano has since queued the patch for `next`, meaning it’s ready for integration testing. No objections or open questions remain, and the fix resolves the crash without performance regressions.

The thread is now effectively closed, with the patch on track for inclusion. The only follow-up was a brief, off-topic exchange between Peff and Alan about an unrelated email delivery issue, which doesn’t affect the technical outcome. If you’ve been away from the list, this is a clean resolution to a core plumbing bug.