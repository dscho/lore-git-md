This is the **author's response** in the `git replay --linearize` thread, where Toon Claes engages with Elijah Newren’s critique of the multi-branch ambiguity and proposes a **syntax change** to resolve it. The email is a **design exploration**, not a patch, and focuses on clarifying the intended behavior of `--linearize` when multiple branches are involved.

Toon acknowledges Elijah’s concerns about the current implementation—particularly the **ordering unpredictability** and the **emergent reachability** (e.g., `branchC` suddenly containing commits from `branchB`)—but pushes back on the framing that the behavior is unintended. He argues that the current implementation aligns with `gitrevisions(7)`’s handling of multiple revision ranges (treating them as a single connected set), though he concedes the option name `--linearize` may be misleading in that context. His primary objection is to the **asymmetry in reachability**: commits from one branch becoming reachable from another without explicit intent.

The email proposes a **new syntax** to eliminate ambiguity: requiring `--ref` when `--linearize` is used, so users explicitly specify which ref to update (e.g., `git replay --linearize --onto master branchA branchB branchC --ref branchA`). This would ensure the output is unambiguous—all commits from the input branches are replayed linearly onto the target, but only the specified ref is updated. Toon suggests this approach could coexist with Elijah’s proposed **minimal fix** (restricting `--linearize` to a single positive ref) as a more flexible alternative, though he notes it would require tracking replayed commits per branch to avoid duplication (e.g., replaying `branchB` and `branchC` separately to preserve their independence).

Toon also raises a **new edge case**: `--contained` (a hypothetical future option) would exacerbate the same ambiguity, as it could introduce even more emergent reachability. He suggests disallowing `--contained` with `--linearize` for now, pending a clearer design.

The email does not commit to a specific solution but **narrows the design space**. Toon’s preference leans toward the `--ref` syntax, which he sees as more explicit and user-friendly than Elijah’s single-ref restriction. However, he acknowledges the implementation complexity (per-branch tracking) and invites further discussion. The ball is now back in Elijah’s and Junio’s courts to weigh the trade-offs between flexibility, predictability, and implementation effort.

Key technical details:
- **Proposed syntax**: `git replay --linearize --onto <target> <branches> --ref <ref>` (e.g., `--ref branchA` to update only `branchA`).
- **Behavior**: All commits from `<branches>` are replayed linearly onto `<target>`, but only the ref specified by `--ref` is updated.
- **Implementation challenge**: Avoiding commit duplication when replaying multiple branches independently (e.g., `branchB` and `branchC` in the example) would require tracking `last_commit` per branch.
- **Edge case**: `--contained` (hypothetical) is flagged as incompatible with `--linearize` due to similar reachability issues.
- **Compatibility**: The `--ref` syntax would not break existing workflows but would require users to adapt to a more explicit interface.

The discussion remains **centered on design philosophy**: Toon prioritizes flexibility and explicit user control, while Elijah prioritizes predictability and recoverability. Junio’s earlier defense of the status quo (concatenation as a direct result of user intent) is not directly addressed, but Toon’s proposal implicitly rejects it by seeking a more constrained interface. The thread is now at a **crossroads**: whether to adopt Elijah’s minimal fix (single-ref restriction), Toon’s `--ref` syntax, or a third alternative.