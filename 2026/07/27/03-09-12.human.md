This is Junio’s latest “What’s cooking” report for July 2026, summarizing the state of all in-flight topics in his integration branches.  It is not a patch itself, but a digest of the entire queue, so busy readers can quickly see what is moving, what is stuck, and what might need their attention.

The report is current as of 2026-07-26.  The seventh batch of topics has just graduated to `master`, so the next release cycle is underway.  Below are the highlights grouped by status.

### Graduated to `next` (cooking, likely to graduate soon)

- Memory-leak fixes (`jc/remote-insteadof-leakfix`, `rs/branch-delete-bisect-warning`).
- CI image bump (`jk/ci-static-analysis-image-bump`).
- Relative-path diff cleanups (`jk/diff-relative-cached-unmerged-more`).
- Submodule remote-matching (`en/submodule-insteadof-remote-match`).
- URL push-tracking (`hn/url-push-tracking`).
- Rust hash cleanups (`bc/rust-hash-cleanups`).
- Sparse-index stash optimization (`tn/stash-avoid-sparse-index-expansion`).
- Rebase notes-copying fix (`pw/rebase-drop-notes-with-commit`).
- Tempfile/lockfile API modernization (`rs/tempfile-wo-the-repository`).
- Refs API modernization (`ps/refs-wo-the-repository`).
- Copy/refs/refspec API modernization (`ps/copy-wo-the-repository`, `ps/refspec-wo-the-repository`).
- Swift userdiff patterns (`sk/userdiff-swift`).
- Loose-object writing refactoring (`ps/odb-move-loose-object-writing`).
- Rebase fixup fixes (`pw/rebase-fixup-fixes`).
- `git bisect --reset-when-found` (`hn/bisect-reset-when-found`).
- `git history squash` (`hn/history-squash`).
- `git branch --delete-merged` (`hn/branch-delete-merged`).
- `git log --graph` root-commit indentation (`ps/shift-root-in-graph`).

### New topics needing review

- Packfile duplicate handling (`tb/pack-with-duplicates`): five-patch series that hardens MIDX, reverse-index, and delta-cycle recovery against duplicate objects.
- `git cat-file --batch-command` object-type support (`ps/cat-file-remote-object-info-type`): extends the remote-object-info command to request object types from the server.
- `git mv` missing-destination check (`lo/mv-missing-dest-dir-check`): rejects symlink-leading destinations and improves error messages.
- ODB on-disk structure pluggability (`ps/odb-make-creation-pluggable`): prepares the object database for alternative backends by deferring initialization and detangling loose-object map loading.
- `git repo info` path keys (`kj/repo-info-more-path-keys`): adds absolute/relative path keys for hooks, grafts, index, objects, etc.
- Documentation synopsis style updates (`ja/doc-synopsis-style-yet-more`): converts `git format-patch`, `git imap-send`, `git send-email`, and `git request-pull` to the modern style.

### Stalled topics (no activity for weeks)

- `tb/repack-geometric-cruft`: `--geometric` + `--cruft` support for `git repack`.
- `hn/checkout-track-fetch`: `git checkout --track=... --fetch` to refresh the start-point.
- `za/completion-hide-dotfiles`: hide dotfiles in path completion unless explicitly requested.
- `kh/doc-trailers` and `kh/doc-replay-config`: documentation improvements for `git interpret-trailers` and `git replay`.

### Topics marked for discard

- `hn/checkout-track-fetch` is explicitly listed as “Will be discarded.”

### Notable ongoing efforts

- ODB abstraction (`ps/odb-make-creation-pluggable`, `ps/odb-move-loose-object-writing`, `ps/odb-pluggable-housekeeping`).
- `the_repository` removal (`ps/copy-wo-the-repository`, `ps/refs-wo-the-repository`, `rs/tempfile-wo-the-repository`).
- Documentation synopsis conversion (`ja/doc-synopsis-style-yet-more`).
- `git history` experimental command (`hn/history-squash`).

Readers who care about object storage, refactoring, or the new `git history` command will find the most active threads here; those interested in stalled topics may want to revive the discussion.