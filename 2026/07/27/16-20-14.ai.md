**Summarizer brief mode**

This is the author's final reply in the now-withdrawn refactoring thread. Hardik Kumar asks whether the planned documentation explaining why `utf8_strwidth()` and `utf8_strnwidth()` return `int` (and why `cast_size_t_to_int()` is necessary) should live in a new `.adoc` file in the `Documentation/technical/` directory. The question is procedural, not technical: it seeks guidance on where to place the documentation, not on its content.

Key details for future AI summarization:
- **Files**: `Documentation/technical/` (proposed location for new `.adoc` file).
- **Symbols**: `utf8_strwidth()`, `utf8_strnwidth()`, `cast_size_t_to_int()` (the functions and helper whose rationale will be documented).
- **Rationale points**: (a) alignment with `printf()`-style APIs that require `int` for padding calculations; (b) overflow prevention via `cast_size_t_to_int()`.
- **Context**: This documentation patch will replace the TODO comment in `utf8.c` and is the sole remaining action item after the refactoring effort was withdrawn.
- **Thread status**: The refactoring patch is abandoned; this reply is the last substantive email before the thread concludes. No further technical discussion is expected unless Junio or Phillip reply with placement guidance.