This is the final review feedback from Junio on the second patch in the `git mv` bugfix series. The patch itself is already approved ("Nice bugfix") and the series is ready for integration, but Junio suggests a small refactoring to avoid duplicating logic that determines whether the destination path requires an existing directory.

The core fix—rejecting moves through tracked symlinks and checking for missing or non-directory destination components—remains unchanged. Junio’s suggestion is purely about code hygiene: extract the condition that guards the directory check into a helper function so the same logic is used both in the early validation phase and in the later `rename()` call. This prevents the two checks from drifting apart in future changes. The helper’s name and exact placement are left to the author and list to bikeshed, but the change is mechanical and uncontroversial.

The patch is otherwise complete, with comprehensive test coverage and no outstanding objections. The only loose end is the TOCTOU race (acknowledged in the commit message), which remains unresolved but is not considered a blocker. This is a minor polish request on an otherwise finished series.