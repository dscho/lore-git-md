Here’s the human-mode narrative for a developer catching up on this thread:

This thread is Junio’s July 2026 “What’s cooking” report, the twelfth of the cycle, summarizing the state of all in-flight topics across his integration branches. The main event since the last report is the ejection of `hn/history-squash` from `next` after Phillip Wood’s review uncovered fundamental flaws in its reachability logic—specifically, incorrect handling of `UNINTERESTING` commits, root commits, and multi-tip histories. The command, part of the new `git history` suite, also drew criticism for its user experience: the `--reedit-message` option was deemed misleading, and its refusal to squash commits pointed to by tags or remote-tracking refs created inconsistency with other `git history` commands. Phillip has since confirmed he’s working on fixups and asked the author, Toon Claes, to hold off on a re-roll until those are ready.

The other topic under active discussion is `hn/branch-delete-merged`, which remains in `next` but faces questions from Phillip about its upstream push-check logic and a behavior change for merged hierarchies. The series adds a `--delete-merged` option to `git branch`, but Phillip flagged the method for determining whether pushing a branch would update its upstream as unreliable. He also noted that the new behavior—preserving the entire merged hierarchy rather than just the topmost merged branch—may not be universally preferred. Han-Wen Nienhuys, the author, hasn’t yet responded, so the topic’s fate in `next` is still uncertain.

Beyond these two topics, the report shows a quiet integration cycle: three topics graduated to `master` (`jc/exclude-first-parent-seen`, `hn/url-push-tracking`, and `ps/cat-file-remote-object-info`), while `next` shed only `hn/history-squash` and otherwise held steady with 11 topics. Junio’s response to the ejection hints at broader concerns about review quality in `next`, though no other topics were flagged for immediate scrutiny. The thread leaves open the question of whether more hidden issues might surface as reviewers dig deeper into the current `next` batch. For now, the focus is on Phillip’s fixups for `hn/history-squash` and Han-Wen’s response to the `branch-delete-merged` feedback.