Here’s the updated human-mode narrative for a developer catching up on the thread:

---

This thread is Junio C Hamano’s July 2026 “What’s cooking” report, a periodic snapshot of all in-flight topics across his integration branches. The main event since the last report is the ejection of `hn/history-squash` from `next` after Phillip Wood’s review uncovered fundamental flaws in its reachability logic and user-experience quirks. That topic is now back in `seen` while Phillip works on fixups. Three other topics—`jc/exclude-first-parent-seen`, `hn/url-push-tracking`, and `ps/cat-file-remote-object-info`—graduated to `master`.

The other headline is `hn/branch-delete-merged`, which adds a `--delete-merged` option to `git branch`. Phillip Wood’s review flagged two issues: the upstream push-check logic is unreliable, and the series changes how merged hierarchies are handled (preserving the entire hierarchy instead of only the topmost merged branch). Junio has now weighed in, but only to say he won’t take sides; the decision rests on whether the community prefers the new behavior or wants the old one restored. The topic is still in `next`, but its path to `master` now depends on Han-Wen Nienhuys either fixing the upstream-check logic or convincing others that the current design is the right call.

Phillip is the only reviewer whose feedback has moved the needle so far. His critiques are substantive—engaging with edge cases, correctness, and long-term maintainability—while most other topics in `next` have yet to receive comparable scrutiny. That gap is now a quiet concern: Junio’s response implies that `next` is only as robust as the reviews it attracts, and the `history-squash` episode shows what can slip through. For now, the rest of the topics in `next` remain unchanged, but the door is open for more reviews to surface similar issues before anything else graduates.