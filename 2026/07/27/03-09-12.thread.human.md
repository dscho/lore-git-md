Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is Junio’s July 2026 “What’s cooking” report, the project’s regular integration-status update. The main news is that `hn/history-squash`—a new `git history` subcommand for squashing multiple commits—was ejected from the `next` branch after Phillip Wood’s review uncovered serious correctness flaws in its reachability logic, particularly around `UNINTERESTING` commits, root commits, and multi-tip histories. The ejection has also prompted broader questions about whether other topics in `next` have received adequate scrutiny.

Separately, Phillip Wood has now reviewed `hn/branch-delete-merged`, a seven-patch series that adds a `--delete-merged` option to `git branch`. He flags two issues: the upstream push-check logic is “dodgy” and may be unreliable, and the series changes the behavior for merged hierarchies—previously only the topmost merged branch was kept, but now the entire hierarchy is preserved. Phillip prefers the old behavior but acknowledges the new approach might have wider support. The topic is still in `next`, but these concerns could delay its graduation until the upstream-check logic is fixed. No other topics were affected in this update.