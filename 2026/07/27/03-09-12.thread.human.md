Here’s the updated human-mode summary for a developer catching up on the thread:

This thread began as Junio C Hamano’s July 2026 "What’s cooking" report, summarizing the status of 50+ in-flight topics across Git’s integration branches. The focus quickly narrowed to two troubled topics: `hn/history-squash` (a new `git history` subcommand for squashing commits) and `hn/branch-delete-merged` (a `git branch` enhancement to delete merged branches). Both were flagged for serious design flaws after Phillip Wood’s technical review—`history-squash` was ejected from `next` due to fundamental reachability bugs and inconsistent UX, while `branch-delete-merged` remains in `next` pending resolution of upstream push-check logic and merged-hierarchy behavior.

The thread has since expanded into a broader discussion about review quality and integration pipeline health. Junio proposed a temporary moratorium on accepting new topics into `seen` without substantive review, citing the `history-squash` incident as evidence that premature promotion risks flawed code reaching `next`. Matt Hunter, a reviewer who initially gave `history-squash` a surface-level "LGTM," later reflected on the challenges of providing meaningful positive reviews, noting that reviewers may not track integration status or signal their engagement depth. Junio’s latest response acknowledges this difficulty but doubles down on the need for caution, implying that reviewers should prioritize thoroughness over speed.

On the technical side, `history-squash` is blocked pending fixups from Phillip Wood to address edge cases like `UNINTERESTING` commits and root commits. The command’s message-handling behavior is also contested: Harald Nordgren (the author) proposed removing `--reedit-message` entirely, Matt Hunter suggested replacing it with conventional `--edit`/`--no-edit` options, and Phillip Wood emphasized preserving `amend!`/`squash!` messages by default. Meanwhile, `branch-delete-merged` faces unresolved questions about its push-check logic and whether it should preserve entire merged hierarchies. Junio has deferred both issues to community consensus, leaving the topics in limbo until further discussion or author responses.

The thread highlights systemic tensions between contributor velocity and code quality, with Junio’s policy proposal and Matt’s reflections suggesting a potential shift toward stricter review requirements. For now, the immediate next steps are Phillip’s fixups for `history-squash` and Han-Wen Nienhuys’s response to the `branch-delete-merged` feedback. The broader procedural questions—how to signal review depth, enforce substantive feedback, or balance scriptability with UX—remain open.