This is a **bugfix** for a crash in the `git cat-file --batch-command` plumbing command. Jeff King (Peff) confirms the issue, traces it to a missing type-metadata fetch in the `--batch-command` code path, and provides a minimal diff that fixes the immediate assertion failure. He also notes a subtle performance consideration: the fix should avoid forcing type lookups for every request in command mode, since some commands (like `info`) do not need them. Peff suggests saving and restoring `data->info.typep` around `contents` requests and invites Alan Stokes to submit a full patch with a test case for `t1006`.

The bug is deterministic, affects a core command, and has existed since `--batch-command` was introduced. The workaround—including `%(objecttype)` in the format string—is already known, but the crash itself is a serious correctness issue that should be fixed. Peff’s review is **tested** (he reproduced the crash and verified the diff) and **substantive** (he identifies the root cause and a nuanced edge case). The next step is a proper patch with test coverage.