This is the ninth patch in the **v5 series optimizing `paint_down_to_common()`** for one-sided histories, now fully reviewed and ready to merge. The patch consolidates the `min_generation` termination check into `paint_queue_get()`, eliminating redundant calls to `commit_graph_generation()` and simplifying the loop logic in `paint_down_to_common()`.

The change moves `last_gen` into the `paint_state` struct, ensuring each dequeued commit’s generation is computed exactly once and reused across termination conditions. The generation-monotonicity BUG assertion is now gated by `state->min_generation`, preventing false positives when the cutoff is zero. This is a **mechanical refactoring** with no behavior change—step counts and merge-base results remain identical—but it improves code clarity and prepares for the final patch (10/10), which removes the commit-date fallback entirely.

The patch touches only `commit-reach.c` and is uncontroversial. It addresses feedback from earlier iterations by reducing code duplication and centralizing generation-number handling. The diff is small (18 insertions, 13 deletions) and self-contained, making it easy to verify. No new edge cases are introduced, and existing tests (including the new clock-skew topologies in patch 6/10) continue to pass.

This is a **cleanup patch** that complements the series’ core optimization (patch 8/10) and instrumentation (patches 5–7/10). It’s a low-risk, high-value step toward eliminating the obsolete commit-date ordering logic. The series as a whole is **ready for merging**, with all technical feedback incorporated.