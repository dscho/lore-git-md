This is the maintainer's final reply to the four-patch series that standardizes `--help`/`-h` exit codes across `git rev-parse --parseopt` and all parse-options users. Junio C Hamano acknowledges the long-standing inconsistency (dating back to 2007) and confirms the series is now approved for merging as a regular bugfix before Git 3.0. The message humorously notes the risk of breaking scripts that might depend on the old exit-129 behavior, but dismisses it as negligible given the change only affects help flags (which are unlikely to be scripted) and switches to a success exit (0), not an error. The series is now complete, with all patches merged into the integration branches. No further discussion or changes are expected.