This is a short but consequential reply in the `git history squash` thread. Phillip Wood pushes back on Harald Nordgren’s proposal to defer template improvements for consistency with `git rebase -i`. He argues that the new `git history` command is explicitly meant to experiment with better UX, so diverging from `git rebase`’s template format is not only acceptable but desirable. If the new format proves successful, it could later be adopted by `git rebase` as well.

The exchange resolves the last open usability question before Junio C Hamano’s final review. Phillip’s position is that the template should prioritize clarity and user experience over consistency with an older command, and he frames this as an opportunity to test improvements that could benefit the entire project. No code changes are required yet, but the outcome of this discussion will determine whether the final version of `git history squash` ships with the current template or adopts Phillip’s cleaner design. The series remains ready for Junio’s assessment, with this template decision now the sole remaining design question.