This is a small but practical usability fix for `git blame -b`. The thread confirms the last loose end: the original reporter (Laszlo Ersek) agrees that no new configuration knob is needed, so the patch René Scharfe posted earlier can proceed as-is. The change will remove the extra hex digit that `git blame -b` currently reserves for an unused caret marker, bringing the abbreviated commit hash length back in line with the user’s `core.abbrev` setting. This eliminates the manual truncation step Laszlo described when copying hashes from `git blame -b` output into an interactive rebase TODO list.

The patch itself is already on the list; it touches only `blame.c`, introduces no on-disk format changes, and affects only the `-b` mode. René’s implementation refactors the mark-handling logic to count and print boundary/ignored/unblamable marks only when they are actually shown, so the alignment stays correct without the extra nibble. The discussion has not surfaced any backward-compatibility concerns for scripts or tools parsing the output, and the fixed-width nature of the blame columns is preserved.

No further technical objections or edge cases have been raised since the patch was posted. The only remaining question is whether test coverage should be added, but that is a routine follow-up rather than a blocker. The patch looks ready for Junio to pick up once the thread settles.