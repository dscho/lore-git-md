This is a **CI infrastructure discussion** in the memory-leak cleanup thread. Patrick Steinhardt responds to Jeff King’s refined proposal to consolidate Git’s CI jobs, agreeing in principle but suggesting two additional refinements: extending the approach to macOS (creating a `macos-TEST-vars` job) and renaming the `linux-TEST-vars` job to something more intuitive, like `linux-changed-defaults`.

The discussion remains procedural and uncontroversial. Patrick’s suggestions are incremental and align with the broader goal of simplifying CI while improving leak detection coverage. No objections or new technical concerns are raised, and the original patch series (fixing leaks in `git format-patch` and the test harness) is unaffected. The key takeaway for readers is that the CI consolidation plan is evolving to include macOS and better naming, but the core proposal—merging redundant jobs and focusing LSan on a single `TEST-vars` variant—remains intact.