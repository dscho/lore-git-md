This is a brief follow-up in the sequencer bugfix thread. Phillip Wood acknowledges Uwe Kleine-König’s **Tested-by** for the 11-patch series that fixes how `git rebase` handles dropped commits. The series remains queued and ready for final review; no code changes are introduced here.

The only new detail is a minor tooling observation: Phillip notes that his script generates trailers in the same style as GitGitGadget, but `git format-patch` uses lowercase `base-commit:`, which may explain why `b4` did not automatically detect the base commit. This is a surface-level issue already resolved in the thread context (Uwe confirmed `b4 am --no-parent` reconstructs the full series), and it does not affect the technical content. The series itself is unaffected and continues to address all known edge cases, including external merge strategy failures and command execution failures.