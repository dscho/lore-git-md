Thread identity
Subject: [PATCH 0/3] commit-graph Bloom filter leak fixes
Series version: v1 (initial)
Originating author: Jeff King (Peff)
Thread type: patch series (bugfix)

Core problem or goal
Fix three memory leaks in Git’s commit-graph Bloom-filter code that surface when the test suite is run with `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`. The leaks are in Bloom-filter slab initialization (`bloom.c`), revision-walk keyvec handling (`revision.c`), and line-log range-copy logic (`line-log.c`). Together with two other recent fixes (one from Peff, one from Kaartic Sivaraam), this series will eliminate most leaks that would appear if the `linux-TEST-vars` CI jobs were run with leak-checking enabled; remaining OpenSSL SHA-1–related leaks are deferred to a future series.

Current status
Agreed: three patches, all new, no prior versions.
Merged: none yet.
Version: v1 (initial submission).

Key technical details
Files touched: `bloom.c`, `revision.c`, `line-log.c`.
New/renamed symbols: `bloom_filter_slab_initialized` (static flag in `bloom.c`).
Behavior change: none (pure leak fixes).
On-disk format: unchanged.
Test coverage: only visible under `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`; leak sanitizers required to detect.

Open questions and loose ends
None identified; series is self-contained and addresses the stated leaks.

Participant positions
Jeff King (Peff): author, no review yet.
No other participants have spoken in this thread.

Related work
Two recent leak fixes (one from Peff, one from Kaartic Sivaraam) that, together with this series, will eliminate most leaks in the `linux-TEST-vars` CI jobs when leak-checking is enabled. OpenSSL SHA-1–related leaks remain unaddressed and are explicitly deferred.