This is a **performance optimization for the reftable backend**, addressing the quadratic-time behavior in bulk ref deletion and re-creation that was diagnosed earlier in the thread. Patrick Steinhardt responds to Kristofer Karlsson’s proposed fix, which exposes tombstones to iterator bounds checks to enable early termination. The fix reduces runtime from **O(n²) to O(n)**—for example, 8,000 refs now take **0.145s** instead of **14.93s**—by allowing the iterator to stop early when a tombstone’s name sorts after the target ref.

The discussion centers on the trade-off between performance and code clarity. Kristofer’s approach delegates tombstone filtering to call sites (e.g., `reftable_be_reflog_exists()`), adding loops like `if (reftable_log_record_is_deletion(&iter->log)) continue` to compensate for the iterator now returning tombstones. Patrick acknowledges the fix’s effectiveness but notes it doesn’t address the broader issue of expensive reference iteration (though it solves the single-ref lookup bottleneck identified by Jeff King). He encourages Kristofer to polish and submit the patch, signaling openness to merging it.

Key technical details:
- **Files touched**: `reftable/merged.c` (iterator logic), `reftable/refs.c` (ref lookup), and `reftable/stack.c` (log iteration).
- **Root cause**: The `merged_iter`’s `suppress_deletions` flag hid tombstones from bounds checks, forcing full scans even when early termination was possible.
- **New behavior**: Tombstones are now visible to iterator bounds logic, enabling early termination in `reftable_ref_iterator_advance()` and related functions.
- **Benchmark results**: Dramatic speedups (e.g., 16,000 refs: **0.258s** vs. **7.1s** pre-fix), confirming the O(n²) bottleneck is resolved for the tested workflow (`git for-each-ref | git update-ref --stdin`).
- **Trade-offs**: The fix adds tombstone-skipping loops at multiple call sites, which could clutter the code if not abstracted. Real-world impact beyond artificial benchmarks remains untested.

This patch is well-motivated and data-driven, but its adoption may hinge on whether the added complexity is justified for an edge case. The thread’s prior consensus deprioritized this issue, but Kristofer’s results could shift that calculus. If sent as a formal patch, expect discussion about code clarity vs. performance trade-offs, especially given the reftable backend’s design emphasis on avoiding compaction overhead.