This is the second patch in a four-patch bugfix series standardizing help-flag exit codes across Git's parse-options infrastructure. It introduces a new `PARSE_OPT_HELP_ERROR` return code to distinguish between intentional help requests (`--help`/`-h`, which will soon exit 0) and help output triggered by invalid input (ambiguous options, which will continue to exit 129).

The patch modifies `parse-options.c` to return `PARSE_OPT_HELP_ERROR` when an ambiguous option triggers help output, while reserving `PARSE_OPT_HELP` for direct help requests. Three built-in commands (`git blame`, `git shortlog`, and `git update-index`) are updated to treat both codes as errors for now, preserving the current behavior until the next patch in the series switches `PARSE_OPT_HELP` to exit 0. The enum values are reordered to maintain negative return codes, with `PARSE_OPT_COMPLETE` shifted to -4 to make room.

This is a preparatory refactoring with no immediate behavior change. The technical approach is sound: it cleanly separates the two help-output cases without breaking existing code paths. The patch touches core parse-options plumbing and three representative built-in commands, demonstrating the system-wide impact of the eventual exit-code change. The commit message clearly explains the motivation and future direction, and the code changes are minimal and focused. No new options or config keys are introduced.