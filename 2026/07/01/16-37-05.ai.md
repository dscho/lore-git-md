This is the **fourth patch in the v5 series** optimizing `paint_down_to_common()` for one-sided histories. It adds **regression tests** for the side-exhaustion optimization, focusing on edge cases where multiple merge-base candidates exist and one is an ancestor of another. The tests verify that the optimization (which terminates early when one side of the history is exhausted) correctly interacts with the `remove_redundant` safety net in `get_merge_bases_many_0`, ensuring only the shallowest merge-base is returned.

The patch introduces a new test script, `t/t6099-merge-base-side-exhaustion.sh`, which constructs a **diamond-shaped topology** where two merge-base candidates (`B` and `C`) exist, but `B` is an ancestor of `C`. The test confirms that `git merge-base --all` excludes the redundant ancestor (`C`) and returns only `B`, both with and without commit-graph data. A separate test in `t/t6600-test-reach.sh` (added in patch 3/10) covers the **mixed finite/INFINITY generation case**, where one query tip is outside the commit-graph (generation = INFINITY) and the other is inside. This exercises the region transition logic, ensuring the walk starts in the INFINITY region (where side-exhaustion is disabled) and correctly transitions to the finite region (where it can fire).

The test coverage is **comprehensive and targeted**: it validates the optimization’s correctness across commit-graph modes (full, none) and edge cases (redundant candidates, region transitions). The new script is **well-documented**, with a clear ASCII-art graph and comments explaining the expected behavior. The patch is **uncontroversial** and **ready to merge**, with no open technical questions. It complements the core logic changes in earlier patches and the documentation updates in patch 1/10, completing the series’ test coverage for the side-exhaustion optimization.