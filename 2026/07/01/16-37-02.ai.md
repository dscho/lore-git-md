This is the first patch in the **v5 series** optimizing `paint_down_to_common()` for one-sided histories. It adds a **new technical document** (`Documentation/technical/paint-down-to-common.adoc`) explaining the algorithm, its use cases, generation-number regions, termination conditions, and the now-removed commit-date fallback.

The document is **comprehensive and well-structured**, covering:
- The two primary use cases (finding all merge bases vs. ancestry checks).
- The paint-walk algorithm, including priority-queue ordering and flag propagation.
- Generation-number regions (INFINITY vs. finite) and their implications for traversal order.
- Termination conditions (empty queue, stale entries, generation cutoff, single-result optimization).
- The **obsolete date-ordering fallback** (removed in a later patch), with clear warnings about its limitations (e.g., breaking side-exhaustion optimizations and single-result guarantees).

The patch also updates `commit-reach.c` to reference the new document in a comment, ensuring future maintainers can find the explanation. Build files (`Makefile`, `meson.build`) are updated to include the new document in the technical docs list.

This is **pure documentation** with no behavior changes. The writing is **clear and precise**, making it a valuable addition for developers working on merge-base logic. The document’s focus on generation-number invariants and the risks of the date-ordering fallback directly supports the series’ later patches, which remove the fallback entirely. **No controversies or open questions**—this patch is ready to merge.