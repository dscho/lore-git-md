This is a **substantive review** of the fourth patch in Johannes Schindelin’s 13-patch Coverity-driven cleanup series. Patrick Steinhardt confirms the correctness of the fix and notes that the patch’s change—replacing `if (cmd->in)` and `if (cmd->out)` with `if (cmd->in > 0)` and `if (cmd->out > 0)`—is consistent with an existing guard elsewhere in the same function. The review is brief but technical: it identifies a fourth call site (`close(cmd->out)`) that already uses the correct idiom, reinforcing that the patch aligns with established practice. No objections or concerns are raised; the feedback is purely confirmatory and adds no new requirements. The patch remains uncontroversial and ready for integration.