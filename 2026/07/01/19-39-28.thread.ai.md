Thread identity
Subject: [PATCH] meson: fix build race in generating hook-list.h
Series version: standalone patch
Originating author: D. Ben Knoble
Thread type: bug fix

Core problem or goal
Restore `hook-list.h` to the `builtin_sources` list in `meson.build` so the header is generated before `builtin/bugreport.c` is compiled, eliminating a race condition that causes build failures when the header is missing.

Current status
Patch sent; no prior iterations. No reviews yet.

Key technical details
- Files touched: `meson.build` only
- Subsystem: Meson build system
- New/renamed symbols: none
- Old-vs-new behavior: header is now generated in the correct order, preventing build failures
- On-disk format: unchanged
- Test coverage: none added; the fix is build-system internal

Open questions and loose ends
None at this point; the patch is self-contained and addresses a clear breakage.

Participant positions
None recorded yet.

Related work
The breakage was introduced by commit `2eb541e8f2a9` ("hook: move is_known_hook() to hook.c for wider use"). No other in-flight topics are affected.