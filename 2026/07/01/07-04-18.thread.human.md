This thread is a 13-patch series from Johannes Schindelin (Dscho) that addresses resource leaks and error-path bugs flagged by Coverity Scan after its recent resumption. The series is purely internal cleanup—no user-visible changes—targeting memory, file-descriptor, and process-handle leaks across core Git subsystems, including loose object handling, bundle URIs, run-command, diff, line-log, directory traversal, submodules, worktree management, IMAP sending, reftable, and the fsmonitor daemon.

Eric Sunshine has reviewed the first twelve patches, giving his Reviewed-by to all, and the final patch (13/13) plugs a Windows-specific process-handle leak by establishing consistent ownership between `exit_process()` and `terminate_process_tree()`. The fix removes a double-close hazard in `mingw_kill()` and is equally mechanical and uncontroversial. The entire series is now at v1 with no open questions or objections; it’s ready for Junio to pick up and should land without drama.