This thread is a 13-patch cleanup series from Johannes Schindelin (Dscho) addressing resource leaks and error-path bugs flagged by Coverity Scan after its recent outage. The series targets internal correctness issues—memory, file-descriptor, and process-handle leaks—without changing user-visible behavior. So far, the first seven patches have received Reviewed-by tags from Eric Sunshine, who found them straightforward and correct.

The latest update covers the eighth patch, which fixes a memory leak in `get_superproject_working_tree()` (submodule.c) by ensuring a `cwd` buffer allocated via `xgetcwd()` is freed on early-return paths. Like the earlier patches, this is a mechanical fix with no functional impact. The series remains uncontroversial, with no open questions or objections raised yet. If the remaining patches follow the same pattern, the series is likely to be merged soon after review completes.