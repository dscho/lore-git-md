This is a **brief follow-up discussion** in the memory-leak cleanup thread, focused on CI infrastructure for leak detection. Patrick Steinhardt clarifies and refines Jeff King’s proposal to consolidate CI jobs.

Patrick agrees in principle with merging the leak-checking job (`linux-reftables-leaks`) into the `linux-TEST-vars` job but suggests dropping the non-leak `linux-reftable` job entirely, since it provides no additional value over its leak-checking counterpart. This is a minor process tweak—no code changes are involved, and the original `format-patch` leak fix remains unaffected.

The discussion remains **narrow and practical**: both participants are aligned on the goal (better leak detection coverage) and the means (job consolidation). No objections or new technical concerns are raised. The thread is now effectively waiting on Junio to pick up the original series, while the CI proposal lingers as a non-blocking follow-up.