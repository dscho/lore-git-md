This is a **substantive review reply** in the ongoing `git history squash` series, part of Harald Nordgren’s effort to add a new subcommand for folding commit ranges into a single commit. Phillip Wood responds to Junio C Hamano’s feedback on input validation, refining the approach to balance flexibility and robustness.

Phillip agrees with Junio’s preference for **shape-based validation** (inspecting the resolved history for structural requirements) over a brittle blacklist of `rev-list` options. However, he argues that some **sanity checks** are still necessary to ensure the user hasn’t overridden critical `rev-list` options like `--reverse`, `--topo-order`, or `--boundary`. These checks would simplify parsing the commit list by guaranteeing a predictable order and allowing the command to detect merge parents outside the range via `BOUNDARY` commits. His suggestion is pragmatic: while shape-based validation handles most cases, a few targeted checks can prevent edge cases (e.g., misordered commits or ambiguous merges) without sacrificing usability.

The exchange is **technically focused** and collaborative, with both participants prioritizing correctness and maintainability. Phillip’s proposal bridges Junio’s high-level design (avoid over-restriction) with the practical need for predictable input handling. This feedback is likely to influence the final implementation in v6, particularly in how the command validates and processes commit ranges. The discussion remains **resolved**—no new disagreements or open questions are introduced.