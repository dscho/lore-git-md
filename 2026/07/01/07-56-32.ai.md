This is a **substantive review** of patch 5/13 in Johannes Schindelin’s Coverity-fix series. Patrick Steinhardt points out what appears to be a logical flaw in the patch: the `dpath` variable is initialized to `NULL` but never assigned a non-NULL value before the `free(dpath)` call on the error path. Since `free(NULL)` is a no-op, the patch as written does not actually plug any leak. The review is concise but technically precise, questioning the patch’s correctness rather than its style or intent. No alternative fix is proposed, but the observation is clear: the patch does not match its stated goal. This casts doubt on whether the Coverity report was correctly interpreted or whether the fix was tested. The thread’s momentum may stall until the author clarifies or revises.