Thread identity
Subject: [PATCH v8 1/11] read-cache: extract function to drop unmerged entries to stage 0
Series version: v8
Originating author: Patrick Steinhardt
Thread type: patch series (refactoring, part of the `git history` effort)

Core problem or goal
Refactor `repo_read_index_unmerged()` to extract the logic that drops unmerged index entries to stage 0 into a new reusable function `index_state_unmerged_to_stage0(struct index_state *)`. This prepares the codebase for a subsequent commit that will reuse the logic on indexes other than the repository’s default one. The patch also fixes a signedness mismatch in the loop counter (`int i` -> `unsigned int i`).

Current status
Agreed and uncontroversial; no objections or requests for changes. The patch is mechanical, behavior-preserving, and ready for integration. It is the first step in the v8 iteration of the `git history` series.

Key technical details
- Files touched: `read-cache.c`
- New symbol: `index_state_unmerged_to_stage0(struct index_state *)`
- Subsystem: index / read-cache
- On-disk format: unchanged
- Test coverage: none added in this patch; relies on existing tests for `repo_read_index_unmerged()`
- Old vs new behavior: identical; this is a pure refactoring

Open questions and loose ends
None introduced by this patch.

Participant positions
- Patrick Steinhardt (author): introduced the change as a preparatory step for the `git history` series.

Related work
- Part of the `git history` series (v8, 11 patches)
- Depends on no prior patches in the series; enables patch 2/11 and beyond
- Ongoing effort: ODB abstraction, which will eventually interact with the index refactoring