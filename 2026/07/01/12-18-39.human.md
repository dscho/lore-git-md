This is a refactoring patch in the ongoing `git cat-file --batch-command` remote object info series (v15, patch 5/13). The patch prepares for moving `write_fetch_command_and_capabilities()` to `connect.c` by eliminating its reliance on the static `advertise_sid` variable.

The change is straightforward: instead of using a static variable set in two different places (one for protocol v1, one for config), the function now directly queries the config with `repo_config_get_bool()` when needed. The patch also corrects the type of `hash_algo` to match `hash_algo_by_name()`'s return type (`unsigned int`) and makes it `const` for clarity.

This is a clean, well-motivated preparatory step that aligns with the series' broader goal of making the code more modular and secure. The commit message clearly explains the reasoning and the scope of the change. No behavior is altered, and the patch is uncontroversial. The only minor nit is that the commit message could briefly mention why protocol v1 can be ignored here (as the function is only used in v2), but the code itself is self-documenting. The patch is ready to merge as part of the larger series.