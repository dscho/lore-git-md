This is a brief procedural follow-up in the memory-leak cleanup thread. Jeff King (Peff) and Patrick Steinhardt are aligning on CI infrastructure changes to improve leak detection coverage. The original two-patch series—fixing a memory leak in `git format-patch` and a test harness LSan output redirection—is already considered correct and ready for merging; this email focuses solely on the CI job consolidation proposal.

Peff clarifies the plan: drop the redundant `linux-reftable` job (which lacks leak detection) and merge its coverage into the existing `linux-TEST-vars` job, then replace the `linux-reftables-leaks` job with a new `linux-TEST-vars-leaks` job that combines leak detection with both reftable-specific and `TEST-vars` test suites. This simplifies the CI matrix while maintaining coverage, though it may slightly increase runtime due to LeakSanitizer overhead. No technical objections remain, and the discussion is now about implementation details rather than the original patches. The thread remains low-stakes and collaborative.