This is Junio C Hamano’s reply to the **v5 series** optimizing `paint_down_to_common()` for one-sided histories. The series is **ready to merge** but has a **procedural blocker**: it was expected to be rebased on top of the **`kk/commit-reach-find-all-fix`** topic, which addresses a related regression in `!FIND_ALL` early exits with v1 commit graphs. Junio attempted to merge the two topics manually but encountered **conflicts in patch 7/10** and a **test failure in `t6600`** after resolving them.

The request is straightforward: **rebase the v5 series on top of the merged `kk/commit-reach-find-all-fix` branch** (already integrated into `v2.55.0`). This ensures the removal of the commit-date fallback (patch 10/10) cleanly supersedes the fix topic without introducing regressions. The technical work is complete—this is purely a **merge-ordering issue**. Once rebased, the series should be resubmitted as **v6** for final integration. No design changes are needed.