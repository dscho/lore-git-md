This is a **substantive follow-up discussion** in the memory-leak cleanup thread, focused on CI infrastructure for leak detection. Patrick Steinhardt responds to Jeff King’s proposal to consolidate the `linux-reftables-leaks` and `linux-TEST-vars` CI jobs into a single job.

Patrick clarifies that the `linux-reftable` job (without leak detection) is redundant and should be dropped in favor of its leak-detecting counterpart (`linux-reftables-leaks`). He agrees in principle with merging `linux-reftables-leaks` into `linux-TEST-vars`, noting that the reftable-specific tests do not interfere with the `TEST-vars` suite’s coverage. The discussion remains centered on process improvements rather than the original patch series, which is already considered correct and ready for merging.

Key technical details for future reference: the proposed CI job consolidation (`linux-reftables-leaks-TEST-vars`), the redundancy of the non-leak `linux-reftable` job, and the ~3x performance overhead of LeakSanitizer (LSan) that makes it impractical for all CI jobs. The thread also reaffirms that unrelated leak fixes (bloom filters, `git-history`) are being handled separately. No new patches or objections are introduced; this is a procedural alignment step.