This is a CI/build system patch addressing a GitHub Actions workflow issue specific to private repositories. Junio C Hamano replies to Johannes Schindelin's patch, questioning the accuracy of the subject line ("reduce the PID limit") given the patch's actual effect: raising the PID limit from an insufficient default to 16,384 to resolve consistent test failures in private repositories.

The core issue remains unchanged: Dockerized CI jobs in private GitHub repositories fail with "cannot create async thread: Resource temporarily unavailable" due to lower default PID limits on hosted runners. Schindelin's patch adds container options (`--pids-limit=16384`, `--ulimit nproc=16384`, `--ulimit nofile=32768`) conditionally applied only when `github.repository_visibility` is private. Junio's feedback focuses on the commit message's framing—it describes raising the limit to resolve failures, not reducing it—while acknowledging the technical rationale (16k is the lowest stable value, avoiding the 2-2.5x performance penalty observed at 64k).

The patch itself is a minimal YAML modification to `.github/workflows/main.yml`, with no impact on public repositories or Git's core functionality. The discussion is purely about clarity in the commit message; the technical approach (16k limit as a trade-off between stability and performance) is uncontested. Key details for follow-up: the conditional container options, the benchmarked 16k value, and the private-repository-specific scope. No substantive review or testing feedback has been provided yet.