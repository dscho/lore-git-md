This is a **bugfix patch** for `git blame -b` output formatting, finalizing the fix for the usability issue reported by Laszlo Ersek. The patch, authored by René Scharfe, has now received explicit confirmation from the original reporter that it resolves the problem: when using `git blame -b` (show boundary commits as blank), the abbreviated commit hashes no longer exceed the user’s `core.abbrev` setting by one character. This eliminates the manual truncation step previously required when pasting hashes from `git blame -b` into `git rebase -i`.

The patch refactors the mark-handling logic in `blame.c` to dynamically calculate the number of columns needed for boundary/ignored/unblamable marks (caret, `*`, `?`), reserving space only when these marks are actually displayed. This ensures the abbreviated commit hash length aligns with `core.abbrev` without over-allocation. The refactoring introduces two helper functions, `print_marks()` and `count_marks()`, and updates `find_alignment()` to use the maximum number of marks observed in the blame output. The change is minimal and targeted, affecting only the `-b` mode of `git blame`.

Laszlo Ersek’s reply provides the missing piece: a **Tested-by** trailer and explicit endorsement. He confirms the patch works as intended in his workflow, noting he didn’t need to pass `-b` explicitly to observe the fix (likely because his configuration or test case already triggered the boundary logic). While he didn’t test exhaustively, his confirmation is a strong signal that the patch addresses the core issue. No new concerns or edge cases are raised in this reply.

The patch still lacks test coverage, as René noted earlier, but the discussion has now converged on the solution. The next steps will likely involve adding tests to verify the fix and prevent regressions, particularly for edge cases like multiple marks per line (though René’s dynamic logic should handle them). No backward-compatibility risks have been identified, and the original reporter considers the issue resolved.

This email closes the loop on the usability friction, shifting the focus to final validation and testing. The patch looks ready for integration, pending test coverage.