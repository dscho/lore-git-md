This is the first patch in the **v5 series optimizing `paint_down_to_common()`**, a core function in Git’s merge-base computation. It adds a **new technical document** (`Documentation/technical/paint-down-to-common.adoc`) explaining the algorithm’s design, including the paint walk, generation-number regions, termination conditions, and the now-removed commit-date fallback.

The document is **comprehensive and well-structured**, covering use cases (merge-base discovery and ancestry checks), the algorithm’s mechanics (PARENT1/PARENT2 flags, priority queue ordering), and edge cases like INFINITY vs. finite generation regions. It explicitly notes where assumptions break down under the legacy date-ordering fallback (now obsolete in this series) and clarifies why optimizations like early termination for single-result queries are unsafe with date ordering. The doc also links to related material (`commit-graph.adoc`) for context.

This patch is **pure documentation**—no code changes beyond a one-line comment update in `commit-reach.c` to reference the new doc. It’s **low-risk** and **high-value**, providing long-term clarity for a performance-critical subsystem. The series as a whole is **ready to merge**, with all feedback incorporated, and this patch is uncontroversial. If you care about merge-base internals or performance optimizations, this doc is worth skimming; otherwise, it’s safely ignorable unless you’re debugging this area.