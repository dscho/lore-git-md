This is a **performance analysis follow-up** in the reftable backend, refining the earlier diagnosis of quadratic-time behavior during bulk ref deletion and re-creation. Jeff King confirms Patrick Steinhardt’s tombstone-based explanation but adds new detail: the bottleneck splits roughly evenly between `refs_verify_refname_available()` (scanning tombstones to confirm refname availability) and `reftable_be_transaction_prepare()` (reading each ref individually during transaction preparation, which becomes expensive due to tombstone overhead).

Peff suggests a potential optimization for the latter: merging the sorted list of transaction refs with the reftable contents via an iterator, rather than processing each ref separately. However, he defers further investigation, aligning with Patrick’s assessment that this is an edge case affecting artificial benchmarks (e.g., `git for-each-ref | git update-ref --stdin`) rather than real-world workflows. The discussion leaves the issue as a known architectural trade-off—tombstoning avoids compaction overhead but creates performance cliffs for bulk operations—with no immediate fix proposed.

Key technical details for future reference:
- **Subsystems**: reftable backend (`refs_verify_refname_available()`, `reftable_be_transaction_prepare()`, `prepare_single_update()`).
- **Root cause**: Tombstone iteration in both refname verification and transaction preparation, compounding quadratic runtime.
- **Potential optimization**: Iterator-based merging of transaction refs with reftable contents to reduce redundant reads.
- **Files touched**: None in this email, but future work would target `reftable/refs.c` and `reftable/transaction.c`.
- **Related context**: Tombstoning is a deliberate design choice (see [Z602dzQggtDdcgCX](https://lore.kernel.org/git/Z602dzQggtDdcgCX@tapette.crustytoothpaste.net)), and prior optimizations to `refs_verify_refname_available()` have already mitigated some overhead.

The tone is pragmatic: both authors acknowledge the issue but deprioritize it due to its niche impact. This email serves as a **substantive technical clarification**, adding transaction preparation to the list of tombstone-sensitive operations and hinting at iterator-based optimizations for future exploration.