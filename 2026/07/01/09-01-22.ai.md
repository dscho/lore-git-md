This is a **CI infrastructure discussion** in the memory-leak cleanup thread. Patrick Steinhardt responds to Jeff King’s refined proposal to consolidate Git’s CI jobs for leak detection and reftable testing.

Patrick agrees with Peff’s plan to merge the non-leak `linux-reftable` job into `linux-TEST-vars` and replace `linux-reftables-leaks` with a new `linux-TEST-vars-leaks` job. He then raises two new points: (1) whether to extend the same consolidation to macOS by creating a `macos-TEST-vars` job, and (2) whether to rename the `linux-TEST-vars` job to something more descriptive like `linux-changed-defaults`, criticizing the current name as unclear.

The discussion remains procedural and uncontroversial, with no objections to the technical direction. The original patch series (fixing leaks in `git format-patch` and the test harness) is unaffected and still considered ready for merging. Key technical details for future reference: the proposed job names (`linux-TEST-vars-leaks`, `macos-TEST-vars`, `linux-changed-defaults`), the redundancy of the non-leak `linux-reftable` job, and the potential cross-platform extension of the CI consolidation.