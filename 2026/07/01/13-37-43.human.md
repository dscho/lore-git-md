This is a brief, surface-level reply in the ongoing sequencer bugfix thread. Phillip Wood acknowledges Uwe Kleine-König’s **Tested-by** for the 11-patch series that fixes the issue where `git rebase` incorrectly copies notes from dropped commits. The only substantive point is a minor tooling observation: `b4` (the patch-fetching tool) did not automatically detect the base commit because Phillip’s script used the trailer `Base-Commit:` (with capital letters) instead of the lowercase `base-commit:` that `git format-patch` and `b4` expect. This is a cosmetic issue that does not affect the patch content or its correctness; the series itself remains queued and ready for final review. No new technical concerns or changes are introduced.