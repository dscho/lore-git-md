This thread is about a CI configuration fix for GitHub Actions workflows that fail in private repositories due to Dockerized jobs hitting PID limits. Johannes Schindelin (Dscho) reported the issue—where tests in the t5xxx-t6xxx range consistently fail with "cannot create async thread: Resource temporarily unavailable"—and proposed a patch that raises the PID limit to 16,384 for private repositories only. The fix is minimal, touching only `.github/workflows/main.yml` with conditional container options (`--pids-limit=16384`, `--ulimit nproc=16384`, and `--ulimit nofile=32768`), and includes benchmarking data showing 16k as the optimal trade-off between stability and performance (higher values like 64k caused 2–2.5× longer runtimes).

Junio C Hamano replied to clarify the commit message’s framing, noting that the patch *raises* the limit (to resolve failures) rather than *reducing* it, as the subject line suggested. The technical approach—applying the higher limit only to private repositories—remains uncontested. No substantive reviews or testing feedback have been provided yet, and the patch is self-contained with no open questions or dependencies. The discussion is currently focused on wording, not the fix itself.