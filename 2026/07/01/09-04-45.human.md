This is a **performance optimization proposal** for the reftable backend, targeting the quadratic-time behavior in bulk ref deletion and re-creation that was diagnosed earlier in the thread. Kristofer Karlsson jumps into the discussion with a concrete fix that dramatically improves scalability by letting tombstones pass through the merged iterator and filtering them at a higher layer, enabling early termination when bounds checks are met.

The patch idea addresses both halves of the bottleneck identified by Jeff King: `refs_verify_refname_available()` and `reftable_be_transaction_prepare()`. By exposing tombstones to the iterator’s bounds checks (e.g., prefix or refname ranges), the backend can now stop scanning early when tombstones fall outside the target range, rather than silently consuming them and forcing a full scan. The fix involves modifying `reftable_ref_iterator_advance()` and related functions to skip tombstones only after bounds checks, and adding tombstone filtering in `reftable_be_reflog_exists()` to maintain correctness.

Kristofer’s local benchmark shows a **dramatic improvement**: the runtime for 8,000 refs drops from 14.93 seconds to 0.145 seconds, and the growth becomes linear (0.258s for 16,000 refs, 0.591s for 32,000). The trade-off is minor: a few call sites (like `reftable_be_reflog_exists()`) need to explicitly skip tombstones, but the overhead is amortized and negligible compared to the quadratic cost. The approach aligns with the reftable backend’s design philosophy—avoiding compaction overhead—while mitigating its worst-case performance cliffs.

This is a **well-motivated, tested, and clearly explained** contribution. The benchmark results are compelling, and the patch idea directly addresses the architectural issue (tombstone iteration) without requiring a major redesign. If the code holds up under review, this could be a straightforward win for reftable’s scalability in edge cases. The thread’s earlier discussion had deprioritized the issue as niche, but Kristofer’s fix suggests it’s more tractable than assumed. Worth watching for follow-up patches or review feedback.