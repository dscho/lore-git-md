This is a **substantive review reply** in the `git history squash` series, continuing the discussion about input validation. Phillip Wood responds to Junio C Hamano’s proposal for **shape-based validation** (inspecting the resolved history for structural requirements) by clarifying where **sanity checks on `rev-list` options** remain necessary.

Phillip agrees with Junio’s critique of blacklisting specific `rev-list` options (e.g., `--left-only`, `--right-only`) but argues that **some option filtering is still required** to ensure the commit list is in a predictable order. Specifically, he proposes **sanity-checking `--reverse`, `--topo-order`, and `--boundary`**—options that `git replay` already enforces—to guarantee the commit list is traversed in the expected sequence. This would allow the command to reliably detect merge parents outside the squashed range (via `BOUNDARY` commits) and avoid ambiguity in range resolution. His suggestion balances Junio’s preference for shape-based validation with the practical need for predictable input processing, ensuring the command can safely analyze the commit history without misinterpreting its structure.

The reply is **technically precise** and **actionable**, focusing on a narrow but critical aspect of the implementation. It does not challenge the core design (three-way merge, ref-handling logic, or template formatting) but instead refines the input validation strategy to align with Git’s existing patterns (e.g., `git replay`). This feedback is likely to influence Harald Nordgren’s v6 implementation, particularly in how the command processes and validates commit ranges before performing the squash. The discussion remains **resolved in principle**: shape-based validation is the goal, but with targeted option checks to ensure robustness.