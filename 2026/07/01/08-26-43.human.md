This is a brief, substantive review reply in the ongoing bugfix series targeting memory leaks in Git’s commit-graph Bloom filter code. The series is authored by Jeff King (Peff) and aims to eliminate leaks that appear when running the test suite with `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`.

Patrick Steinhardt’s reply to the second patch (which fixes a leak in `revision.c` where Bloom-filter "keyvecs" are overwritten during multiple traversals of the same `rev_info` structure) endorses the chosen approach. Peff’s original patch explicitly frees old keyvecs before generating new ones, rather than attempting to reuse them, to avoid subtle bugs if the pruning pathspec changes between traversals. Patrick agrees that this is the safer solution, noting that the efficiency trade-off is negligible and that the code was not previously tracking pathspec changes to enable safe reuse. His comment is concise but confirms the technical rationale and reinforces that the fix aligns with the existing behavior. No new concerns or alternatives are raised, and the patch remains uncontroversial.