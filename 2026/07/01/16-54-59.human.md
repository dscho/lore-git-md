This is a CI/build system patch that adjusts Docker container limits for GitHub Actions workflows. Junio C Hamano replies to Johannes Schindelin's patch, questioning the accuracy of the subject line ("reduce the PID limit") since the patch actually *raises* the PID limit from its default value to 16,384 for private repositories. The discussion clarifies that the default limit is too low for the tests to pass, causing consistent failures in private repositories, while 16k is the minimum value that resolves the issue without the severe performance penalty observed at higher limits (64k caused 2-2.5x longer runtimes).

The patch itself is a targeted fix for a specific blocker: Schindelin uses private repositories to test Microsoft Git changes, and the current setup fails entirely in that context. The change modifies `.github/workflows/main.yml` to conditionally apply container options (`--pids-limit=16384`, `--ulimit=nproc=16384`, and `--ulimit=nofile=32768`) only when the repository is private. Junio's feedback is purely about the commit message wording, not the technical approach, and the patch appears well-motivated with clear benchmarking data in the commit message.

This is a straightforward, self-contained fix that won’t affect public repository workflows. The only open question is whether the subject line should be adjusted to reflect that the patch *increases* the limit rather than reduces it. The patch looks ready for merging once that minor detail is addressed.