Here is the updated dense, loss-free AI-mode summary for future invocations:

---

Thread identity: Junio C Hamano’s "What's cooking in git.git" report for July 2026 (#01), covering all in-flight topics at the start of the Git 2.56 cycle. Originating author: Junio C Hamano. Thread type: integration report.

Core problem/goal: Provide a snapshot of every topic currently in Junio’s integration branches (`maint`, `master`, `next`, `seen`), including their status, dependencies, and next steps. The report serves as the authoritative reference for what is queued for the next release and what needs review or reroll.

Current status: Git 2.55 released; `next` branch not yet rebuilt. 48 topics total: 7 new, 4 stalled, 37 cooking. Topics marked "in 'next'" remain from the previous cycle. No topics have graduated to `master` yet in this cycle. **Update**: Three ODB/ref-related topics are now explicitly ready for `next`:
- **ps/odb-drop-whence**: Ready for `next` after a minor reroll (renaming `sourcep` to `source_infop` in response to Justin Tobler’s feedback).
- **ps/odb-generalize-prepare**: Ready for `next` with no further changes.
- **ps/refs-writing-subcommands**: Ready for `next` with no further changes.

Key technical details:
- **New topics**:
  - `kk/commit-reach-find-all-fix`: Fixes `paint_down_to_common()` early-exit bug in v1 commit graphs with clock skew. 2 commits, stable in `next`.
  - `bl/t7412-use-test-path-helpers`: Modernizes `t7412` (submodule absorbgitdirs) to use `test_path_is_file`/`test_path_is_dir`. 1 commit, in `seen`.
  - `jk/format-patch-leakfix`: Plugs memory leak in `git format-patch --base`. 2 commits, in `seen`.
  - `ps/setup-split-discovery-and-setup`: Splits repository discovery/configuration into two phases (`struct repo_discovery`). 16 commits, in `seen`. Depends on `jk/repo-info-path-keys`, `ps/refs-onbranch-fixes`, `ps/setup-drop-global-state`.
  - `pw/rebase-drop-notes-with-commit`: Corrects rebase notes-copying logic for dropped commits. 15 commits, in `seen`. Expecting reroll.
  - `jk/bloom-leak-fixes`: Plugs Bloom-filter memory leaks. 3 commits, in `seen`.
  - `js/ci-dockerized-pid-limit`: Adjusts Dockerized CI process/file limits. 1 commit, in `seen`.
- **Stalled topics** (awaiting reroll or review response):
  - `ap/http-redirect-wwwauth-fix` (1 commit, stalled since June 2).
  - `jt/config-lock-timeout` (1 commit, stalled since May 17).
  - `js/parseopt-subcommand-autocorrection` (11 commits, stalled since April 27).
  - `cl/conditional-config-on-worktree-path` (2 commits, stalled since May 24).
- **Cooking topics** (selected highlights):
  - `tb/repack-geometric-cruft`: Teaches `--geometric --cruft` to roll up non-cruft packs. 11 commits, in `seen`. Expecting reroll (bare `grep !` issue).
  - **ps/odb-drop-whence**: Removes `whence` from `struct object_info` and replaces it with a backend-specific `struct object_info_source`. 7 commits, in `seen`. **Update**: Ready for `next` after minor reroll (renaming `sourcep` to `source_infop`).
  - **ps/odb-generalize-prepare**: Introduces `odb_prepare()` wrapper to standardize ODB source opening. 3 commits, in `seen`. **Update**: Ready for `next` with no further changes.
  - **ps/reftable-hardening**: Hardens reftable backend against corruption. 12 commits, in `seen` (includes fuzzer + Meson libFuzzer support).
  - **ps/cat-file-remote-object-info**: Adds `remote-object-info` to `git cat-file --batch-command` (protocol v2). 13 commits, in `seen`.
  - **ps/history-drop**: Adds `git history drop` to remove a commit and replay descendants. 11 commits, in `seen`.
  - **ps/refs-writing-subcommands**: Adds `create`, `delete`, `update`, `rename` to `git refs`. 5 commits, in `seen`. **Update**: Ready for `next` with no further changes.
  - `mm/diff-process-hunks`: Introduces `diff.<driver>.process` for external line-change control. 6 commits, in `seen`. Expecting reroll.
- **Dependencies**:
  - `ps/setup-split-discovery-and-setup` depends on `jk/repo-info-path-keys`, `ps/refs-onbranch-fixes`, `ps/setup-drop-global-state`.
  - `ps/odb-drop-whence` and `ps/odb-generalize-prepare` depend on `ps/odb-source-packed`.
  - `tb/pack-path-walk-bitmap-delta-islands` depends on `ds/path-walk-filters`.
- **Files/subsystems touched**: `setup.c`, `commit-reach.c`, `reftable/`, `odb/`, `refs/`, `builtin/rebase.c`, `builtin/cat-file.c`, `builtin/refs.c`, `t/`, `Documentation/MyFirstContribution.txt`, `Documentation/SubmittingPatches`, CI scripts, Meson build files.
- **New/renamed symbols**:
  - `struct repo_discovery`, `odb_prepare()`, `git refs create|delete|update|rename`, `git history drop|squash`, `diff.<driver>.process`, `fetch.followRemoteHEAD`, `remote-object-info` batch-command, `struct object_info_source` (replaces `whence` in `struct object_info`).
- **Behavior changes**:
  - `git merge-base` (without `--all`) now correctly handles v1 commit graphs with clock skew.
  - `git rebase` no longer copies notes for dropped commits.
  - `git cat-file --batch-command` supports remote object metadata queries.
  - `git branch --delete-merged` removes local branches merged to remote-tracking branches.
  - `git checkout --track=fetch` refreshes the start-point.

Open questions/loose ends:
- **Needs review**: `ps/setup-split-discovery-and-setup`, `ps/reftable-hardening`, `ps/history-drop`, `kk/merge-base-exhaustion`, `mm/diff-process-hunks`, `hn/branch-delete-merged`, `tb/midx-incremental-custom-base`, `mm/line-log-limited-ops`.
- **Expecting reroll**: `pw/rebase-drop-notes-with-commit` (minor fixups), `tb/repack-geometric-cruft` (bare `grep !`), `mm/diff-process-hunks`, **ps/odb-drop-whence** (minor rename: `sourcep` to `source_infop`).
- **Awaiting response to review comments**: `bl/t7412-use-test-path-helpers`, `js/ci-dockerized-pid-limit`, `ps/libgit-in-subdir`, `tc/replay-linearize`, `sn/rebase-update-refs-symrefs`, `hn/checkout-track-fetch`, `wy/doc-myfirstcontribution-trim-quotes`, `mm/test-grep-lint`, `ty/migrate-excludes-file`, `ty/migrate-trust-executable-bit`.
- **Conditional approvals**: None explicitly noted, but topics like `jk/format-patch-leakfix` and `jk/bloom-leak-fixes` are "likely to merge to `next`".
- **Stalled topics**: All 4 stalled topics are awaiting author response (reroll or clarification).

Participant positions:
- **Junio C Hamano**: Maintainer report; acknowledged Patrick Steinhardt’s clarification about the three ODB/ref topics. No explicit approvals/objections.
- **Patrick Steinhardt**: Clarified status of three ODB/ref topics:
  - **ps/odb-drop-whence**: Ready for `next` after minor reroll (addresses Justin Tobler’s feedback to rename `sourcep` to `source_infop`).
  - **ps/odb-generalize-prepare**: Ready for `next` with no further changes.
  - **ps/refs-writing-subcommands**: Ready for `next` with no further changes.
- **Justin Tobler**: Provided review feedback on `ps/odb-drop-whence` (suggested renaming `sourcep` to `source_infop`).
- **Reviewers**: No new Reviewed-by/Acked-by trailers in this update, but prior reviews are implied for topics already in `next` or marked "ready".
- **Authors** (key contributors):
  - Patrick Steinhardt (`ps/`): Driving ODB abstraction (`ps/odb-*`), `setup.c` refactoring, reftable hardening, `git refs` subcommands, `git history` features.
  - Jeff King (`jk/`): Leak fixes (`format-patch`, Bloom filters), `repo info` path keys.
  - Taylor Blau (`tb/`): Repack machinery (`geometric-cruft`, MIDX incremental).
  - Phillip Wood (`pw/`): Rebase notes logic.
  - Johannes Schindelin (`js/`): CI Docker limits, Coverity fixes.
  - Kristoffer Haugsbakk (`kk/`): Commit-reach fixes, priority queue optimizations.
  - Han-Wen Nienhuys (`hn/`): Branch/push UX improvements (`branch --delete-merged`, `checkout --track=fetch`).
  - Toon Claes (`tc/`): `git replay --linearize`.
  - Denton Liu (`dl/`): `git cat-file` remote object info.
  - Martin Michael Musiol (`mm/`): `diff.process` driver, line-log limited ops, test grep lint.
  - Others: See prior summary for full author list.

Related work:
- **Dependencies**: `ps/odb-source-packed` is a prerequisite for `ps/odb-drop-whence` and `ps/odb-generalize-prepare`.
- **In-flight efforts**: ODB abstraction (`ps/odb-*`), reftable backend (`ps/reftable-hardening`), `setup.c` refactoring (`ps/setup-*`), test modernization (`bl/t7412-*`).
- **Prior versions**: `pw/rebase-drop-notes-with-commit` is a reroll (15 commits suggest prior iterations).
- **External tools**: `b4` recommended in `ps/doc-recommend-b4` for patch submission.

---