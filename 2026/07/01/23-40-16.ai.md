This is Junio C Hamano’s "What’s cooking in git.git" report for July 2026 (#01), summarizing the state of all in-flight topics at the start of the Git 2.56 cycle. Below is the technical brief for a follow-up AI summarizer, focusing on the key details needed to track future discussion.

---

The report covers 48 topics currently cooking in Junio’s integration branches (`next`, `seen`, `master`). Git 2.55 has just been released, and the `next` branch has not been rebuilt yet, so all topics listed as "in 'next'" remain there from the previous cycle.

**New topics** (7):
- **kk/commit-reach-find-all-fix**: Fixes a bug in `paint_down_to_common()` where `git merge-base` (without `--all`) could return incorrect results on repositories with v1 commit graphs and clock skew. The early-exit optimization is now gated on the queue being generation-ordered. Two commits, marked as stable in `next`. Awaiting comments.
- **bl/t7412-use-test-path-helpers**: Modernizes `t7412` (submodule absorbgitdirs tests) to use `test_path_is_file`/`test_path_is_dir` instead of raw `test -f`/`-d`. One commit, in `seen`. Awaiting response to review comments.
- **jk/format-patch-leakfix**: Plugs a memory leak in `git format-patch --base` and improves test suite leak reporting under TAP. Two commits, in `seen`. Likely to merge to `next`.
- **ps/setup-split-discovery-and-setup**: A major refactoring of `setup.c` that splits repository discovery and configuration into two phases. Discovery now populates a `struct repo_discovery` without modifying repository state, and configuration takes this structure to initialize the repository. 16 commits, in `seen`. Depends on `jk/repo-info-path-keys`, `ps/refs-onbranch-fixes`, and `ps/setup-drop-global-state`. Needs review.
- **pw/rebase-drop-notes-with-commit**: Corrects rebase’s post-rewrite notes-copying logic. When a commit is dropped (e.g., because its changes are already upstream), it is no longer recorded as rewritten, preventing its notes from being copied to an unrelated commit. 15 commits, in `seen`. Expecting a reroll due to minor fixups and squash commits.
- **jk/bloom-leak-fixes**: Plugs memory leaks in Bloom-filter code paths exposed by `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`. Three commits, in `seen`. Likely to merge to `next`.
- **js/ci-dockerized-pid-limit**: Adjusts Dockerized CI jobs in private GitHub repositories to use explicit process/file limits, preventing resource exhaustion. One commit, in `seen`. Awaiting response to review comments.

**Stalled topics** (4):
- **ap/http-redirect-wwwauth-fix**: Preserves WWW-Authenticate headers across redirects. One commit, stalled since June 2. Expecting a reroll.
- **jt/config-lock-timeout**: Adds a configurable timeout for acquiring `config.lock`. One commit, stalled since May 17. Awaiting response to review comments.
- **js/parseopt-subcommand-autocorrection**: Adds subcommand autocorrection to `git-remote` and `git-notes`. 11 commits, stalled since April 27. Awaiting response to review comments and a squash fix.
- **cl/conditional-config-on-worktree-path**: Extends `includeIf` to support `worktree` and `worktree/i` conditions. Two commits, stalled since May 24. Awaiting response to review comments.

**Cooking topics** (37):
- **tb/repack-geometric-cruft**: Teaches `git repack --geometric --cruft` to roll up non-cruft packs while writing a separate cruft pack. 11 commits, in `seen`. Expecting a reroll due to a bare `grep !` issue.
- **jk/reftable-leakfix**: Fixes a leak in the reftable backend on API error. One commit, in `seen`. Likely to merge to `next`.
- **sg/t3420-do-not-grep-in-missing-file**: Test fix for `t3420-rebase-autostash`. One commit, already merged to `next` and ready for `master`.
- **ad/gpg-strip-cr-before-lf**: Fixes `strip_cr_before_lf` to only remove CR when it immediately precedes LF. One commit, in `seen`. Likely to merge to `next`.
- **jt/receive-pack-use-odb-transactions**: Refactors `git-receive-pack` to use ODB transactions instead of `tmp_objdir`. Six commits, in `seen`. Expecting a reroll due to review comments.
- **ps/odb-drop-whence**: Removes the `whence` field from `struct object_info` and refactors backend-specific object info into `struct object_info_source`. Seven commits, in `seen`. Depends on `ps/odb-source-packed`. Likely to merge to `next`.
- **ps/reftable-hardening**: Hardens the reftable backend against corrupted tables (OOB reads/writes, NULL pointer access). 12 commits, in `seen`. Includes a new fuzzer and Meson support for libFuzzer. Needs review.
- **hn/branch-push-slip-advice**: Updates `git push` and `git branch` to suggest `<remote>/<branch>` when a slash is mistyped. Two commits, in `seen`. Likely to merge to `next`.
- **jc/history-message-prep-fix**: Code cleanup and leak fix for `git history`. One commit, in `seen`. Likely to merge to `next`.
- **ty/migrate-excludes-file**: Moves `excludes_file` into `repo_config_values`. One commit, in `seen`. Needs review.
- **kk/merge-base-exhaustion**: Optimizes merge-base computation by stopping early when one side’s exclusive commits are exhausted. 10 commits, in `seen`. Passes standalone tests but breaks when merged to `seen`. Needs review.
- **dk/meson-enable-use-nsec-build**: Wires up `USE_NSEC` in the Meson build. One commit, in `seen`. Awaiting response to review comments.
- **ps/connected-generic-promisor-checks**: Refactors connectivity checks to search for promisor objects generically using the ODB interface. Five commits, already merged to `next` and ready for `master`. Depends on `ps/odb-source-packed`.
- **ps/libgit-in-subdir**: Moves `libgit.a` sources into a new `lib/` directory. Three commits, in `seen`. Depends on `ps/odb-source-packed`. Awaiting response to review comments.
- **ps/odb-generalize-prepare**: Introduces `odb_prepare()` and generalizes the `reprepare()` callback. Three commits, in `seen`. Depends on `ps/odb-source-packed`. Likely to merge to `next`.
- **jc/submittingpatches-design-critiques**: Updates `SubmittingPatches` to clarify how to address design critiques. One commit, already merged to `next` and ready for `master`.
- **wy/doc-myfirstcontribution-trim-quotes**: Updates `MyFirstContribution` to advise trimming quoted text in replies. One commit, in `seen`. Awaiting comments.
- **tb/midx-incremental-custom-base**: Fixes `git multi-pack-index write --incremental --base` to honor the custom base. Three commits, in `seen`. Needs review.
- **mm/test-grep-lint**: Adds `greplint` to detect bare `grep` assertions in tests. Six commits, in `seen`. Awaiting response to review comments.
- **rs/cat-file-default-format-optim**: Optimizes the default format in `git cat-file`. One commit, already merged to `next` and ready for `master`.
- **kk/prio-queue-get-put-fusion**: Folds the lazy priority queue optimization into `prio_queue` itself. Two commits, in `seen`. Likely to merge to `next`.
- **td/ref-filter-memoize-contains**: Optimizes `git branch --contains` and `git for-each-ref --contains` using memoized commit traversal. Three commits, in `seen`. Awaiting comments.
- **tc/replay-linearize**: Adds `--linearize` to `git replay` to drop merge commits. Three commits, in `seen`. Awaiting response to review comments.
- **ps/setup-drop-global-state**: Drops remaining global state in `setup.c` (`git_work_tree_cfg`, `is_bare_repository_cfg`). Eight commits, already merged to `next` and ready for `master`. Depends on `ps/setup-centralize-odb-creation`.
- **ps/refs-onbranch-fixes**: Fixes chicken-and-egg recursion in reference backend configuration by lazy-loading. 12 commits, already merged to `next` and ready for `master`. Depends on `ps/setup-centralize-odb-creation`.
- **ps/odb-source-packed**: Refactors the packed object source into a proper `struct odb_source`. 18 commits, already merged to `next` and ready for `master`.
- **td/ref-filter-restore-prefix-iteration**: Restores prefix-scoped iteration in `ref-filter`. One commit, already merged to `next` and ready for `master`.
- **ty/move-protect-hfs-ntfs**: Moves `protect_hfs` and `protect_ntfs` into `repo_config_values`. Two commits, already merged to `next` and ready for `master`.
- **ps/cat-file-remote-object-info**: Adds `remote-object-info` to `git cat-file --batch-command`, allowing clients to request object metadata (e.g., size) from a remote server via protocol v2. 13 commits, in `seen`. Awaiting response to review comments.
- **ps/doc-recommend-b4**: Updates `MyFirstContribution` to recommend `b4` for patch submission. Three commits, already merged to `next` and ready for `master`.
- **sn/rebase-update-refs-symrefs**: Teaches `git rebase --update-refs` to skip branch symref aliases. One commit, in `seen`. Awaiting response to review comments.
- **mm/diff-process-hunks**: Introduces `diff.<driver>.process` to allow external tools to control which lines Git considers changed. Six commits, in `seen`. Expecting a reroll.
- **tb/pack-path-walk-bitmap-delta-islands**: Teaches `git pack-objects --path-walk` to use reachability bitmaps and delta-islands. Five commits, already merged to `next` and ready for `master`. Depends on `ds/path-walk-filters`.
- **ty/migrate-trust-executable-bit**: Moves `trust_executable_bit` into `repo_config_values`. Three commits, in `seen`. Awaiting comments.
- **kk/prio-queue-cascade-sift**: Optimizes `prio_queue_get()` using a cascade-down approach. One commit, on hold pending `kk/prio-queue-get-put-fusion`.
- **jk/repo-info-path-keys**: Adds `path.gitdir` and `path.commondir` keys to `git repo info`, supporting absolute/relative paths. Three commits, already merged to `next` and ready for `master`.
- **ps/history-drop**: Adds a `drop` subcommand to `git history` to remove a commit and replay its descendants. 11 commits, in `seen`. Needs review.
- **jk/setup-gitfile-diag-fix**: Fixes a regression in `.git` file error diagnosis. One commit, already merged to `next` and ready for `master`.

**Other notable topics**:
- **js/coverity-fixes**: A collection of 13 fixes for resource leaks, invalid file descriptor closures, and process handle ownership issues flagged by Coverity. In `seen`, awaiting response to review comments.
- **mm/line-log-limited-ops**: Teaches `git log -L<range>:<path>` to limit `--stat`, `--check`, and `-G` to the specified range. Seven commits, in `seen`. Needs review.
- **hn/history-squash**: Adds a `squash` subcommand to `git history` to fold a range of commits into one. Four commits, in `seen`. Awaiting response to review comments.
- **mh/fetch-follow-remote-head-config**: Adds `fetch.followRemoteHEAD` configuration. Eight commits, already merged to `next` and ready for `master`.
- **ps/refs-writing-subcommands**: Adds `create`, `delete`, `update`, and `rename` subcommands to `git refs`. Five commits, in `seen`. Likely to merge to `next`.
- **hn/branch-delete-merged**: Adds `--delete-merged` to `git branch` to remove local branches already merged to their remote-tracking branches. Seven commits, in `seen`. Needs review.
- **hn/checkout-track-fetch**: Extends `git checkout --track` with a `fetch` mode to refresh the start-point. Two commits, in `seen`. Awaiting response to review comments.