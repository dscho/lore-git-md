This is a **substantive performance optimization proposal** for the reftable backend, targeting the quadratic-time behavior in bulk ref deletion and re-creation that was diagnosed earlier in the thread. Kristofer Karlsson identifies a shared root cause for the two bottlenecks (in `refs_verify_refname_available()` and `reftable_be_transaction_prepare()`): the `merged_iter`’s `suppress_deletions` flag hides tombstones from higher-level code, preventing early termination when bounds checks (e.g., prefix or refname sorting) could otherwise stop iteration. His fix exposes tombstones to the iterator’s bounds logic, allowing early termination and reducing the benchmark from **O(n²) to O(n)**.

The patch touches core reftable internals: `merged_iter_next_void()`, `reftable_ref_iterator_advance()`, `reftable_backend_read_ref()`, and log iteration paths. Benchmark results are dramatic—runtime for 8,000 refs drops from **14.93s to 0.145s**—but the change requires adding tombstone-skipping loops at multiple call sites (e.g., `reftable_be_reflog_exists()`). Kristofer has a working local branch and is offering to send a formal patch, though he acknowledges potential oversights.

Key technical details for future discussion:
- **Files touched**: `reftable/merged.c`, `reftable/refs.c`, `reftable/stack.c` (log iteration).
- **New behavior**: Tombstones are now visible to iterator bounds checks, enabling early termination when a tombstone’s name sorts after the target ref.
- **Trade-offs**: The fix adds per-call-site tombstone filtering (e.g., `if (reftable_log_record_is_deletion(&iter->log)) continue`), which could clutter the code if not carefully abstracted.
- **Related context**: This aligns with Jeff King’s earlier suggestion of iterator-based merging but implements it at a lower level, addressing both halves of the bottleneck simultaneously.
- **Performance impact**: The quadratic-time issue is now resolved for the benchmarked workflow (`git for-each-ref | git update-ref --stdin`), though real-world impact remains untested.

The proposal is well-motivated and data-driven, but its adoption may hinge on whether the added complexity (tombstone filtering at call sites) is justified for an edge case. The thread’s prior consensus deprioritized this issue, but Kristofer’s results could shift that calculus. If sent as a patch, it will likely spark discussion about code clarity vs. performance trade-offs.