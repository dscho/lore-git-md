AI mode summary:

Thread identity
Subject: [PATCH v2 00/12] Coverity fixes: plug leaks and error-path bugs
Series version: v2 (final; all patches present, reviewed, and approved for merging)
Originating author: Johannes Schindelin (Dscho)
Thread type: patch series (cleanup)

Core problem or goal
Coverity Scan recently resumed after an outage; this 12-patch series addresses the first batch of real (non-false-positive) resource leaks and error-path bugs it flagged in core Git. The goal is to plug memory, file-descriptor, process-handle, and other resource leaks, and to ensure proper cleanup when functions return early due to errors. No user-visible behavior changes; purely internal correctness.

Current status
**Series is approved and ready for merging. Junio C Hamano has explicitly stated the v2 iteration "looks good" and confirmed the series is approved for merging, upgrading his prior "will queue" statement to present-tense approval.** All patches carry Reviewed-by from Patrick Steinhardt, Jeff King (Peff), Eric Sunshine, and Junio C Hamano. **Patch 1/12 v2** replaces the unsafe `errno`-based error detection with an explicit `ferror(fp)` check, using `ret = ferror(fp) ? -1 : 0` to distinguish between EOF and I/O errors. **Patch 2/12 v2** adds a guard (`if (fd >= 0) close(fd)`) to prevent undefined behavior when `open()` fails. **Patch 3/12** ensures the `child_out` file handle is closed when `finish_command(&cp)` fails. **Patch 4/12 v2** replaces `if (cmd->in)` / `if (cmd->out)` with `if (cmd->in > 0)` / `if (cmd->out > 0)` in `start_command()`’s error-path rollback. **Patch 5/13 (v1) was withdrawn as already fixed by commit 949bb8f74f4a.** **Patch 5/12 v2** removes a redundant intermediate copy of `struct line_log_data` in `line_log_process_ranges_arbitrary_commit()`. **Patch 6/12** plugs two memory leaks in `read_one_dir()` by adding explicit `free(ud.untracked)` and `free(ud.dirs)` calls. **Patch 7/12** plugs a memory leak in `get_superproject_working_tree()` by redirecting early-return paths through a new `out:` label; Patrick Steinhardt confirmed all variables are always initialized and the zero-initialized `ret` makes the `goto out` flow correct. **Patch 8/12 v2** consolidates all cleanup in `get_superproject_working_tree()` into a single exit path. **Patch 9/12** plugs four memory leaks in `builtin/worktree.c` by redirecting an early error return through the existing `cleanup:` label. **Patch 10/12** plugs a memory leak in `curl_append_msgs_to_imap()` by adding `strbuf_release(&msgbuf.buf)`. **Patch 11/12** plugs a memory leak in `reftable_table_refs_for_unindexed()` by extending the `out:` cleanup block. **Patch 12/12** plugs a handle leak and latent use-after-free in Windows-specific process termination logic by enforcing clear ownership.

Key technical details
Files touched: loose.c (load_one_loose_object_map, write_one_object), bundle-uri.c (download_https_uri_to_file), run-command.c (start_command), diff-lib.c (run_diff_files), line-log.c (process_ranges), dir.c (read_one_dir), submodule.c (get_superproject_working_tree), builtin/worktree.c (add), imap-send.c (curl_append_msgs_to_imap), reftable/table.c (reftable_table_refs_for_unindexed), builtin/fsmonitor--daemon.c (fsmonitor_run_daemon), compat/mingw.c (exit_process, mingw_kill), compat/win32/exit-process.h (terminate_process_tree).
New or modified symbols: none user-facing; internal-only fixes (free, close, release, CloseHandle, fsmonitor_free_token_data calls).
On-disk format: unchanged.
Test coverage: none added; fixes are mechanical and covered by existing tests. Jeff King (Peff) independently reproduced the leak fixed in patch 6/12 under `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`, confirming the fix’s correctness and the leak’s real-world impact.
**Patch 1/12 v2**: ensures `FILE *fp` is closed on all error paths in `load_one_loose_object_map()` by consolidating cleanup into a single `err:` exit path. The unsafe `errno`-based error detection (`return errno ? -1 : 0`) is replaced with `ret = ferror(fp) ? -1 : 0`, reliably distinguishing between EOF and I/O errors.
**Patch 2/12 v2**: adds a guard (`if (fd >= 0) close(fd)`) in `write_one_object()` to prevent undefined behavior when `open()` fails.
**Patch 3/12**: plugs a file-descriptor leak in `download_https_uri_to_file()` by ensuring the `child_out` file handle is closed when `finish_command(&cp)` fails.
**Patch 4/12 v2**: replaces `if (cmd->in)` / `if (cmd->out)` with `if (cmd->in > 0)` / `if (cmd->out > 0)` in `start_command()`’s error-path rollback to prevent `close(-1)`.
**Patch 5/12 v2**: removes a redundant intermediate copy of `struct line_log_data` in `line_log_process_ranges_arbitrary_commit()` that was leaking whenever `bloom_filter_check()` determined a commit did not touch any tracked paths.
**Patch 6/12**: plugs two memory leaks in `read_one_dir()` by adding explicit `free(ud.untracked)` and `free(ud.dirs)` calls on early-return paths.
**Patch 7/12**: plugs a memory leak in `get_superproject_working_tree()` by redirecting two early-return paths through a new `out:` label that frees `cwd`, `sb.buf`, and `one_up.buf`; Patrick Steinhardt confirmed all variables are always initialized and the zero-initialized `ret` makes the `goto out` flow correct.
**Patch 8/12 v2**: consolidates all cleanup in `get_superproject_working_tree()` into a single exit path (`out:` label) that releases `cwd`, `sb.buf`, and `one_up.buf`, eliminating fragility and plugging a latent leak of `one_up`’s backing buffer.
**Patch 9/12**: plugs four memory leaks in `builtin/worktree.c` by redirecting an early error return through the existing `cleanup:` label so that `path`, `opt_track`, `branch_to_free`, and `new_branch_to_free` are freed.
**Patch 10/12**: plugs a memory leak in `curl_append_msgs_to_imap()` by adding `strbuf_release(&msgbuf.buf)` alongside existing libcurl cleanup calls.
**Patch 11/12**: plugs a memory leak in `reftable_table_refs_for_unindexed()` by extending the `out:` cleanup block to release `filter->oid` and free `filter`.
**Patch 12/12**: plugs a handle leak and latent use-after-free in Windows-specific process termination logic by enforcing clear ownership: `exit_process()` and `terminate_process_tree()` now unconditionally own and close the process handle passed to them, removing the redundant `CloseHandle(h)` call in `mingw_kill()` that Coverity flagged as a use-after-free (CID 1437238).

Open questions and loose ends
**Orthogonal bug in `read_one_dir()`**: uninitialized slots in `->dirs` and `->untracked` on the failure paths may cause `free_untracked()` to dereference garbage pointers, risking a crash. Johannes Schindelin plans to address this separately, keeping the current series focused on Coverity’s leak reports.

Participant positions
Johannes Schindelin (author). Eric Sunshine reviewed all 12 patches in v1, provided Reviewed-by for all, and noted the fixes as straightforward and correct. **Junio C Hamano reviewed the series as a whole, stated the v2 iteration "looks good," and confirmed the series is approved for merging. He reviewed patch 1/12 specifically, identified a critical correctness issue (unsafe `errno`-based error detection), and recommended replacing it with an explicit `ferror(fp)` check. Johannes Schindelin acknowledged the feedback and confirmed the revised patch uses `ret = ferror(fp) ? -1 : 0` to reliably detect errors.** Patrick Steinhardt reviewed patch 1/12 v1, confirmed the `fclose(fp)` call is safe, but Junio’s review now supersedes that assessment. **Patrick Steinhardt reviewed patch 2/12 v1, provided Reviewed-by, and confirmed the fix’s correctness. He reviewed patch 7/12 in prior correspondence and in the new email, confirmed correctness, noted all variables are always initialized, and that the zero-initialized `ret` makes the `goto out` flow correct. He reviewed patch 8/12 v1, confirmed correctness, but flagged a fragility risk due to two separate `free(cwd)` calls in the function. Johannes Schindelin revised the patch to v2, consolidating all cleanup into a single exit path, and Patrick Steinhardt’s concern is now resolved.** Jeff King (Peff) reviewed patch 6/12, independently reproduced the leak under `GIT_TEST_COMMIT_GRAPH_CHANGED_PATHS=1`, and confirmed the fix’s correctness. No objections raised for other patches. **Patrick Steinhardt’s reply to the v2 cover letter is a surface-level review: he notes the reflowed commit messages made the range-diff harder to read, but his feedback is purely editorial and carries no technical objections or requests for change. The series is now explicitly approved for merging by Junio C Hamano.**

Related work
None. This series is self-contained and does not depend on or conflict with other in-flight topics. The orthogonal bug in `read_one_dir()` will be addressed in a future patch.