This is a short but pointed exchange in the final stages of the `git history squash` feature. Phillip Wood pushes back on Harald Nordgren’s earlier suggestion to keep the `--reedit-message` template format consistent with `git rebase -i`. Phillip argues that the new `git history` command is explicitly meant to experiment with better user interfaces, so diverging from `rebase -i`’s template is not only acceptable but desirable. He suggests that if the new format proves successful, it could later be adopted by `git rebase` as well.

The email is a clear signal that the series is now code-complete and the only remaining questions are about polish and future direction. Phillip’s stance is pragmatic: the new command should innovate rather than replicate, and consistency can be revisited later if the experiment succeeds. No technical changes are needed for the current series, but this discussion may influence future work on `git rebase -i`.