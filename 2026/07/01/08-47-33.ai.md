This is a **CI infrastructure discussion** in the memory-leak cleanup thread. Jeff King (Peff) clarifies and refines the proposal to consolidate Git’s CI jobs for leak detection and reftable testing.

Peff now explicitly proposes two steps: (1) merge the non-leak `linux-reftable` job into the existing `linux-TEST-vars` job, and (2) replace the leak-specific `linux-reftables-leaks` job with a new `linux-TEST-vars-leaks` job that combines `TEST-vars` coverage with LeakSanitizer (LSan). This aligns with Patrick Steinhardt’s earlier suggestion to drop the redundant `linux-reftable` job and keeps the focus on targeted leak detection without duplicating effort.

The discussion remains procedural and uncontroversial; no objections or new technical concerns are raised. The original patch series (fixing leaks in `git format-patch` and the test harness) is unaffected and still considered ready for merging. Key technical details for future reference: the proposed job names (`linux-TEST-vars`, `linux-TEST-vars-leaks`), the redundancy of the non-leak `linux-reftable` job, and the continued use of LSan for leak detection in a subset of CI runs.