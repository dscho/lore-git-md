Thread identity
Subject: [PATCH] Makefile: fix sparse error for pack-revindex.c in lib/ directory
Series version: standalone patch, part of Patrick Steinhardt’s `ps/libgit-in-subdir` branch
Originating author: Ramsay Jones
Thread type: build system fix

Core problem or goal
A recent refactoring (commit 9759608622) moved several source files into a new `lib/` directory but omitted updating the Makefile rule that silences a `memset` warning for `pack-revindex.c`. This causes a sparse compilation error when building with `DEVELOPER=1`.

Current status
**Queued in `seen` as part of the next re-roll of `ps/libgit-in-subdir`**. The patch is a one-line fix (adding the `lib/` prefix to the target path in the Makefile) that directly addresses the regression. Junio queued it on top of the `ps/libgit-in-subdir` topic in `seen` (2025-06-10), noting the fix is uncontroversial and ready for the next re-roll or graduation. Johannes Schindelin previously suggested squashing the fix into commit 8da3a2c01822 (the current commit in `seen` that moved the files), and Patrick Steinhardt has now confirmed he will incorporate the change in the next iteration of the branch. Ramsay Jones provided procedural context for the delay (unrelated Cygwin build failures) and confirmed the referenced commit (9759608622) had already been rewritten in `seen`. No prior versions exist for this specific fix.

Key technical details
- Files touched: `Makefile` (one line)
- Subsystem: build system, sparse checking
- New/renamed symbols: none
- On-disk format changes: none
- Test coverage: none (build system fix)
- Platform impact: NonStop port and other environments where sparse checking is enabled

Open questions and loose ends
None. The fix is uncontroversial and now incorporated into the next re-roll of the `ps/libgit-in-subdir` branch.

Participant positions
- Ramsay Jones: authored the patch, requested squash into original patch if the branch is re-rolled, and provided procedural context for the delay. Position is substantive (build system correctness).
- Johannes Schindelin: confirmed the fix is correct and identified the correct commit (8da3a2c01822) for squashing. Review was surface-level (verified reasoning and change, but did not test build or engage with broader implications).
- Junio C Hamano: queued the patch in `seen` and noted the utility of commit reference formatting for sanity-checking. Position is procedural (integration).
- Patrick Steinhardt: confirmed incorporation of the fix into the next re-roll of the `ps/libgit-in-subdir` branch. Position is procedural (integration).

Related work
Part of Patrick Steinhardt’s `ps/libgit-in-subdir` branch, which reorganizes the codebase by consolidating library sources into a dedicated `lib/` directory. The branch is currently in `seen` and subject to rewriting. The fix is now integrated into the next iteration of the branch.