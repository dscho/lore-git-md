This is a **follow-up reply** in the ODB abstraction series, specifically addressing Justin Tobler’s review of patch 3/7. The email resolves the only open question from the review: the historical purpose of the return values in `builtin/pack-objects.c` and `builtin/rev-list.c`.

Patrick confirms that both functions (`add_object_entry_from_bitmap` and `show_object_fast`) have always returned `1` since their introduction in 2013, and the return values were never used for anything. The commit messages from their original implementations do not explain the choice, and the values were ignored by their callers. This clarification removes any lingering uncertainty about whether the change to return `0` instead of `1` could have unintended consequences.

The patch itself remains unchanged and is now uncontroversial. With this reply, patch 3/7 is effectively ready to proceed, pending any further discussion on the rest of the series. The thread’s overall status is unchanged: the series is still under review, with only minor commit message tweaks and style suggestions outstanding for other patches.