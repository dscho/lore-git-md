This is a **contributor housekeeping** discussion in the `git-subtree` safeguard thread, focusing on terminology and design intent rather than code changes.

Ian Jackson and Colin Stagner clarify the conceptual framework for the safeguard patch and the forthcoming Rust rewrite of `git-subtree`. Their exchange resolves the last ambiguity about detection scope: the safeguard will check only the top-level repository config (`${rev}:.git-subtree/config`), not internal subtree directories, aligning with the Rust rewrite’s design. Ian argues for "upstream/downstream" terminology to describe the dependency relationship between projects merged via `git-subtree`, while Colin suggests Git’s existing "root tree/sub-trees" vocabulary. The discussion confirms that the Rust rewrite will not support unmarked subtree merges (those performed manually with `git merge -X subtree`), and while this limitation will be documented and tested, it won’t be enforced at runtime.

No code changes are proposed here—this is purely a design and terminology discussion. The tone is collaborative, and Colin closes by expressing support for the next version of the patch. The thread remains under review, with all substantive feedback addressed.