This is a **brief follow-up in a review discussion** for patch 4/7 of Patrick Steinhardt’s ODB abstraction series. The patch refactors how Git opens bitmaps by iterating over object sources first, then attempting to load a bitmap for each source, reversing the previous order.

Patrick accepts Justin Tobler’s readability suggestions for `open_bitmap_for_source()`: he will rewrite the conditional to use a separate `if` for the `midx` check and assignment, matching the style used elsewhere in the patch. This is a minor, uncontroversial change that improves clarity without altering functionality. The discussion is now resolved; the patch remains on track for acceptance once Patrick incorporates this tweak. No other issues were raised in this reply.