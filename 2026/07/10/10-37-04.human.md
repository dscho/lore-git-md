This is the first patch in Pablo Sabater’s v8 series implementing cascading indentation for visual roots in `` `git log --graph` ``. It is a test infrastructure cleanup, not part of the core feature.

The patch moves the `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into `lib-log-graph.sh`, renaming it to `lib_test_check_graph`. Both test files already import this library, so the change is purely mechanical: deduplicating 34 lines of code into a single shared location. The helper formats and compares graph output in tests, and the new patch series needs it for additional test cases.

This patch was previously reviewed and approved by Karthik Nayak; it is now merged and serves as the baseline for the rest of the series. No functional changes are introduced—only code organization. The patch is uncontroversial and ready for inclusion.