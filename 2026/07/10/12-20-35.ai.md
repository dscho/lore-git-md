This is a **contributor housekeeping** discussion in the `git-subtree` safeguard thread, focusing on terminology and design intent for the forthcoming Rust rewrite (RIIR). The email is a direct reply from Ian Jackson to Colin Stagner, clarifying the conceptual framework behind the safeguard’s detection logic and the Rust rewrite’s behavior.

Ian defends his use of "upstream" and "downstream" to describe the relationship between projects merged via `git-subtree`, arguing that the dependency flow (code moving from the upstream project to downstream consumers) mirrors classic upstream/downstream dynamics. He rejects alternative terms like "root tree" and "subtree" as too file-position-centric, emphasizing the social and logical relationship between projects rather than their physical layout in the repository. This distinction is central to the safeguard’s design: the Rust rewrite will store its metadata in the top-level repository config (`subtree.rust-rewrite`), and the safeguard checks only that location—not `.git-subtree/config` inside the subtree directory—because the decision to adopt the new implementation belongs to the downstream project, not the upstream.

The exchange also touches on edge cases around unmarked subtree merges (manual `git merge -X subtree` operations). Colin suggests that `--squash` merges would likely fail with "unrelated history" errors, but Ian counters that interleaved squash and non-squash merges could produce unpredictable results. This aligns with the Rust rewrite’s documented limitation: it will not support unmarked merges, but this restriction will be enforced via documentation and tests rather than runtime checks. The tone remains collaborative, with Colin expressing support for the series and Ian acknowledging the value of the design discussion.

No code changes or new review feedback emerge from this email. The key takeaway is the confirmation that the safeguard’s scope (top-level config only) and the Rust rewrite’s design intent are now mutually aligned, resolving the last open question from earlier reviews. The thread is effectively ready for v2, pending any further feedback.