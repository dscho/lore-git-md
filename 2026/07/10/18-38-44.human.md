This is a small platform-compatibility fix for the build system. Ramsay Jones reports that commit 9759608622 ("Move libgit.a sources into separate 'lib/' directory") missed updating a sparse-checking rule in the Makefile, causing a sparse error when building `lib/pack-revindex.c`. The patch adds the missing `lib/` prefix to the `pack-revindex.sp` target, silencing the warning about a large `memset` call.

The change is trivial and clearly correct: it aligns the Makefile with the directory restructuring that already landed. Ramsay asks Patrick Steinhardt to squash this into the original patch if he re-rolls the `ps/libgit-in-subdir` branch, indicating this is a follow-up to an ongoing effort rather than a standalone fix.

No controversy or edge cases are evident; the patch is a straightforward build-system tweak. If you care about sparse-clean builds or are following the libgit.a reorganization, this is worth a quick glance. Otherwise, it’s safe to ignore.