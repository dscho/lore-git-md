This is a test infrastructure refactoring patch, part of the ongoing `remote-object-info` feature series (v16, patch 3/13). It moves three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The commit message explains this is preparatory work to avoid code duplication when these utilities are needed in a later patch in the series.

The change itself is mechanical and localized: the functions are copied verbatim into the new library file, and `t1006-cat-file.sh` is updated to source the new library. No behavior changes are introduced. The patch is well-motivated by the need for code reuse in the upcoming `remote-object-info` tests, and the implementation follows Git's established pattern for test library files.

This is a clean, uncontroversial refactoring that doesn't touch any core functionality. It's a small but necessary step in the larger series, and the patch itself looks ready to merge. The only minor nit is the typo in the commit message ("enabling" is missing a space before it), but that doesn't affect the code.