Thread identity: Patch series (single patch) introducing a bugfix for `--autosquash` behavior in `git rebase` when `fixup!`/`squash!` commits cancel out their target, resulting in empty commits. Originating author: Farid Zakaria. Thread type: bug report and fix.

Core problem or goal: Fix a long-standing edge case in the sequencer subsystem where `fixup!` or `squash!` commits that cancel out all changes in their target commit are not handled according to the `--empty` option (`drop`, `keep`, or `stop`). Currently, Git halts the rebase with an error instead of respecting the user's `--empty` configuration.

Current status: **Ready for merging** as of `v4`. The patch has evolved through four iterations, incorporating substantive feedback from Phillip Wood. The `v4` addresses all prior review concerns, including correctness issues around dropping commits (e.g., `rewritten-pending` file handling) and preserving existing behavior for empty commits. Phillip Wood's latest review confirms the patch is technically sound and suggests only minor improvements (code organization, performance, cleanup, and one additional test case). No merge event to `master` or other integration branches has been reported; the series remains under review pending these tweaks.

Key technical details:
- **Files touched**: `sequencer.c` (core logic), `Documentation/git-rebase.adoc` (user-facing documentation), `t/t3415-rebase-autosquash.sh` (new test coverage), `t/t5407-post-rewrite-hook.sh` (additional test coverage).
- **Subsystems**: Sequencer (`--autosquash` logic), rebase.
- **New/renamed symbols**: `is_amended_head_empty()` (new function to detect when applying a `fixup!`/`squash!` empties the target commit), `enum fixup_target` (tracks whether the target commit was originally picked with changes), on-disk state in `$GIT_DIR/rebase-merge/fixup-target` (persists state across rebase restarts).
- **Behavior changes**:
  - Introduces a new check (`is_amended_head_empty()`) to detect when applying a `fixup!` or `squash!` empties the target commit, ensuring `--empty` is honored for such commits.
  - Only commits that *become* empty (not those picked empty to begin with) are subject to `--empty` handling, preserving existing behavior for workflows that rely on empty commits as placeholders.
  - When `--empty=drop` applies, the patch ensures the emptied commit is dropped by updating `HEAD` to its parent and cleaning up the `rewritten-pending` file, preserving mappings for `--update-refs` and other consumers.
  - Supports conflict resolution: if a user resolves a conflicted `fixup!` by undoing its target, the patch honors `--empty` for the resulting empty commit.
  - Only the final `fixup!` in a chain may drop the target commit, as dropping it earlier would cause subsequent fixups to be squashed into the wrong commit.
- **On-disk format changes**: None, but introduces a new on-disk state file (`$GIT_DIR/rebase-merge/fixup-target`) to track whether the target commit was originally picked with changes.
- **Test coverage**: Comprehensive tests in `t/t3415-rebase-autosquash.sh` cover all three `--empty` modes (`drop`, `keep`, `stop`) for `fixup!` and `squash!` scenarios, including edge cases like mid-chain fixups, root commits, conflict resolution, and already-empty targets. Additional coverage in `t/t5407-post-rewrite-hook.sh` ensures hooks are triggered correctly. Phillip Wood suggests adding a test case for a fixup that becomes empty *after* its target has already become empty.

Open questions and loose ends:
- The follow-up discussion about a potential `revert!` (or `drop!`) autosquash directive remains unresolved. Three competing designs are proposed:
  1. **Phillip Wood (original)**: Use an empty marker commit to represent the `revert!` directive, avoiding conflicts during rebase.
  2. **Yuxuan Chen**: Use an inverse patch (like `git revert`) to represent the `revert!` directive, prioritizing semantic clarity and consistency with existing Git commands. Conflicts during rebase would signal real difficulties in undoing the change.
  3. **Phillip Wood (new)**: A middle-ground proposal: a new `git revert --drop` option (or similar) that creates a commit containing the inverse patch *and* a `drop!` message. During `git rebase --autosquash`, the sequencer would replace the target commit’s `pick` with `drop` and discard the `drop!` commit itself. This avoids conflicts during rebase while reflecting the intended removal in the repository state before the rebase runs. Cites an open GitGitGadget issue as prior art for this idea.
- The trade-off between conflict avoidance (Phillip) and semantic consistency (Yuxuan) is the central point of contention, with no consensus reached.
- Phillip Wood notes two **unrelated but worth-fixing** inconsistencies in the existing codebase:
  - The behavior of `--empty=stop` differs depending on whether the target commit was originally empty or became empty via fixup. This is not a regression (the patch preserves existing behavior) but may warrant a separate cleanup.
  - The current code drops a `pick` even if it’s followed by fixups, which is a bug. The patch fixes this by deferring the drop decision until the end of the fixup chain.

Participant positions:
- **Farid Zakaria (author)**: Proposes the patch and has iterated through four versions (`v1`–`v4`) to address feedback. Motivated by a real-world workflow at Meta (automatically dropping both a commit and its revert when `--empty=drop` is used). The `v4` incorporates all of Phillip Wood's review concerns, including correctness fixes and edge-case handling. Discloses use of LLMs to navigate the Git codebase (per project guidelines) but emphasizes hands-on review of the code and tests.
- **Phillip Wood (reviewer)**: Substantive technical review across all iterations. Initially disagreed with the `v1` implementation, arguing it broke his workflow (where empty commits are used as placeholders). Proposed an alternative: add a new check to detect when applying a `fixup!` makes the target empty, preserving existing behavior for `fixup!` commits that become empty. His feedback on correctness (e.g., `rewritten-pending` file handling) and edge cases (e.g., mid-chain fixups) is now fully addressed in `v4`. Additionally, proposes two designs for a `revert!` directive: (1) an empty marker commit, and (2) a `git revert --drop` option that creates a commit with an inverse patch and a `drop!` message, which the sequencer would discard during rebase. His latest review confirms the patch is ready for merging with minor tweaks.
- **Yuxuan Chen (reviewer)**: Substantive design-level reply. Argues that an empty marker commit for `revert!` would be semantically misleading, as the repository still contains the target commit’s changes until the rebase runs. Advocates for an inverse-patch approach (like `git revert`) to maintain consistency with existing Git commands, accepting that conflicts may arise during rebase as they do with `git revert`.

Related work:
- The patch is motivated by a real-world workflow at Meta, where internal LLVM patches are managed as single commits using `fixup!` autosquash. The fix enables automatic dropping of both an original patch and its revert when `--empty=drop` is used.
- No dependencies or prior versions mentioned. The patch is self-contained. The discussion of a `revert!` directive is a potential follow-up effort, orthogonal to this patch. Phillip Wood references an open GitGitGadget issue as prior art for the `git revert --drop` idea.