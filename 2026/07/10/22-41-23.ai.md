**Summarizer brief mode**

This is a surface-level review of patch 5/8 in Patrick Steinhardt’s ODB abstraction series. Taylor Blau confirms the patch’s intent—dropping the `_1` suffix from `open_pack_bitmap_1()` and `open_midx_bitmap_1()` after the preceding patch consolidated their logic into `open_bitmap_for_source()`—and calls the separation into its own commit sensible. No technical feedback is provided; the review is purely acknowledgment of the mechanical clean-up.

Key details preserved for future AI summarization:
- Patch 5/8 in v2 of the series, titled “pack-bitmap: drop `_1` suffix from functions that open bitmaps.”
- Renames `open_pack_bitmap_1()` → `open_pack_bitmap()` and `open_midx_bitmap_1()` → `open_midx_bitmap()`.
- Seven call sites updated in `pack-bitmap.c`.
- No behavior change; the `_1` suffix was a historical artifact.
- Taylor’s review is brief and non-substantive, noting only the commit’s logical separation.