Thread identity
Subject: [PATCH v8 1/2] config: refactor include_by_gitdir to include_by_path
Series version: v8
Originating author: Chen Linxuan
Thread type: patch series (2 patches)

Core problem or goal
Refactor the config subsystem’s path-matching logic so that the same helper can be reused for both the gitdir and the worktree path in a follow-up patch that adds a new `includeIf` condition for worktrees.

Current status
Under review; this is the first patch in the v8 series.

Key technical details
- File touched: `config.c`
- Function renamed: `include_by_gitdir()` → `include_by_path()`
- New parameter: `const char *path` replaces the hard-coded `opts->git_dir`
- Call sites updated in `include_condition_is_true()` to pass `opts->git_dir` explicitly
- No behavior change; realpath and absolute-path handling remain identical

Open questions and loose ends
None yet; the patch is purely preparatory.

Participant positions
None recorded yet.

Related work
Part of a larger series (v8) introducing `includeIf.worktree:<glob>`; the next patch (2/2) will use the refactored helper to implement the new condition.