This is the **merge signal** for Johannes Schindelin’s twelve-patch bugfix series `js/coverity-fixes-v2`. Junio C Hamano has reviewed the v2 iteration and found all patches, including the newly added twelfth patch that replaces `oid_to_hex()`’s rotating static buffer with a stack-allocated buffer and `oid_to_hex_r()`, to be correct. He explicitly states the series is ready for the `next` branch unless new issues surface in the next few days.

The series targets NULL-dereference vulnerabilities across eleven files (diffcore-break.c, diff.c, remote.c, reftable/stack.c, mailsplit.c, bisect.c, replay.c, revision.c, pack-bitmap.c, shallow.c) and addresses a latent bug in `write_one_shallow()` related to `oid_to_hex()`’s static buffer. All feedback from Junio’s v1 review has been incorporated: the `remote.c` patch now uses `BUG()` for NULL `remote`, `bisect_successful()` returns `BISECT_FAILED` instead of `-1`, the `revision.c` patch includes a regression test (`t/t0410-partial-clone.sh`), and the `bisect_start()` patch clarifies the Coverity-flagged edge case. The series is uncontroversial, well-scoped, and touches core and plumbing subsystems without altering on-disk format or introducing new user-visible behavior. Given Junio’s approval and the mechanical nature of the fixes, this series is now a straightforward merge candidate for `next`.