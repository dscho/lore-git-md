This is a brief follow-up in the now-concluded priority queue optimization thread. Kristofer Karlsson acknowledges René Scharfe’s suggestion about micro-optimizing `sift_down_root()` by reducing writes, but notes that CPU caching likely makes the swap-based approach nearly as efficient in practice. He implies that the complexity of expanding the scope—such as changing the `compare()` function signature—is not justified by the marginal performance gain, especially given the already withdrawn optimization patch. The exchange is purely speculative and does not reopen the withdrawn series; it simply closes the loop on a minor technical observation. No code changes or further action are proposed.