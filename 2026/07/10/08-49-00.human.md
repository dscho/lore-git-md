This is the final patch in Patrick Steinhardt’s eight-part series introducing object filters to `odb_for_each_object()`. It refactors `git cat-file --batch-all-objects` to use the new ODB-mediated filtering mechanism, eliminating its direct dependence on the "packed" backend’s internals.

The patch replaces 69 lines of backend-specific logic with a clean seven-line call to `odb_for_each_object_ext()`, passing the user’s object filter through the ODB layer. The old code manually managed bitmaps, iterated over packs, and reached into the "files" source internals; the new version delegates all of that to the ODB abstraction, which can now optimize filtering via bitmaps when available. The diffstat—7 insertions, 69 deletions—speaks for itself: the change is a clear simplification.

This is the payoff patch for the series. Earlier patches laid the groundwork by adding filter support to `odb_for_each_object()`, exposing bitmap-filtering capabilities to backends, and ensuring objects are correctly attributed to their source. Here, those pieces come together to remove the last vestige of backend-specific logic from `git cat-file`. The result is a command that works uniformly across all ODB backends, including future pluggable ones.

The patch is well-motivated and cleanly executed. Junio C Hamano already praised the v1 version of this patch as "very nice" and requested only a minor commit-message tweak (now incorporated). With all review feedback addressed and the series cooking in `next`, this looks like a straightforward merge candidate once the last minor adjustments are finalized. If you care about ODB abstraction or `git cat-file` internals, this is the patch to watch.