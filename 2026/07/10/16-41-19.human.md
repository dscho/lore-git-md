This is the final patch in the sixteenth iteration of a security-hardened feature series that adds `remote-object-info` support to `git cat-file --batch-command`. The patch makes the placeholder allow-list dynamic, allowing the client to adapt to servers with different capabilities rather than being hardcoded to only support "objectname" and "objectsize".

The change replaces the static `remote_object_info_atoms` array with a dynamic `remote_allowed_atoms` string list in the `expand_data` struct. The client now maps server-advertised capabilities (like "size" or "type") to their corresponding placeholders ("objectsize", "objecttype") at runtime. This happens in three steps: the client requests only the capabilities the server advertises, filters out any unsupported ones during the fetch, and then populates the allow-list with the placeholders that correspond to the successfully fetched capabilities.

The implementation is clean and well-structured, with clear separation between protocol options and placeholder names via the new `remote_atom_map` array. The backward iteration when filtering the string list is a thoughtful touch to avoid skipping elements during removal. The patch also moves `batch_object_write()` outside the conditional block to ensure output is always generated, even if all placeholders are unsupported (returning empty lines, matching established Git patterns).

This is a necessary evolution of the feature, enabling future extensibility while maintaining security through capability-based filtering. The change is minimal and focused, touching only the allow-list mechanism without altering core functionality. All prior review feedback about static allow-lists has been addressed, and the implementation follows Git's established patterns for dynamic capability handling. The series now appears complete and ready for integration.