This is a refactoring patch that generalizes the `write_fetch_command_and_capabilities()` function to support arbitrary protocol commands, not just "fetch". Part of the ongoing `remote-object-info` feature series (v16, patch 7/13), this change prepares the codebase for the upcoming `object-info` command implementation by making the function reusable.

The patch renames the function to `write_command_and_capabilities()` and adds a `command` parameter, replacing the hardcoded "fetch" string. The change is mechanical and localized: the function's core logic remains identical, but now accepts any command string passed to it. This enables the same capability-writing machinery to be reused for the `object-info` command in later patches.

The diff shows straightforward modifications: the function signature is updated, the hardcoded "fetch" is replaced with the `command` parameter, and the function name is changed throughout. The commit message helpfully explains that "command" here refers to Git's wire protocol operations, providing context for future readers.

This is a well-executed preparatory step that follows Git's established patterns for code reuse. The patch is clearly motivated by the needs of the larger series and maintains backward compatibility through the existing "fetch" usage sites. No behavior changes are introduced, and the refactoring appears clean and uncontroversial.