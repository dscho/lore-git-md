This thread is about a one-line build system fix to address a sparse compilation error introduced when source files were moved into a new `lib/` directory. The error occurs because the Makefile rule that silences a `memset` warning for `pack-revindex.c` wasn’t updated to reflect the file’s new location.

Ramsay Jones authored the fix, which adds the missing `lib/` prefix to the Makefile target. Johannes Schindelin confirmed the fix is correct and clarified that the original commit introducing the regression is no longer reachable, suggesting the fix be absorbed into the current commit (8da3a2c01822) in Patrick Steinhardt’s `ps/libgit-in-subdir` topic, which is still in `seen` and subject to rewriting. No substantive objections or open questions remain, and the fix is uncontroversial. The thread is now waiting for Patrick to either re-roll the topic or apply the fix directly.