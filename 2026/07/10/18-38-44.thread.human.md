This thread is about a one-line build system fix to address a sparse compilation error introduced when source files were moved into a new `lib/` directory. The error surfaced when building with `DEVELOPER=1` due to a missing path update in the Makefile for `pack-revindex.c`.

Ramsay Jones authored the fix, which adds the `lib/` prefix to the Makefile rule silencing the `memset` warning. Johannes Schindelin confirmed the fix was correct and suggested squashing it into the original commit that moved the files, if the `ps/libgit-in-subdir` branch is re-rolled. Junio has now queued the patch in his integration branch (`seen`), confirming it’s ready for the next steps. The fix is uncontroversial and directly addresses the regression without any open questions or objections.

The patch is part of Patrick Steinhardt’s broader `ps/libgit-in-subdir` effort, which reorganizes the codebase by consolidating library sources into a dedicated `lib/` directory. The topic remains in `seen` and may be re-rolled before graduating to `next`. No further action is needed beyond the fix itself.