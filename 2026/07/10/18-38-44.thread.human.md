This thread is about a one-line build system fix to address a sparse compilation error introduced when source files were moved into a new `lib/` directory. The error occurs because the Makefile rule silencing a `memset` warning for `pack-revindex.c` wasn’t updated to reflect the new path. Ramsay Jones authored the fix, which adds the `lib/` prefix to the target path in the Makefile.

Johannes Schindelin confirmed the fix is correct and suggested it be squashed into the original commit that moved the files (8da3a2c01822) if Patrick Steinhardt’s `ps/libgit-in-subdir` branch is re-rolled. Ramsay later clarified that the patch was written shortly after the branch was published but delayed due to unrelated Cygwin build failures, explaining why the referenced commit had already been rewritten by the time the patch was sent. The fix itself remains valid, and the thread is now waiting for either Patrick to incorporate it into a re-roll or Junio to pick it up directly if the topic graduates without further changes. No objections or open questions remain.