This is a refactoring patch in Patrick Steinhardt’s eight-part series introducing object filters to `odb_for_each_object()`. It is the fifth patch, a clean-up step that drops the `_1` suffix from two internal bitmap-opening functions (`open_midx_bitmap_1` and `open_pack_bitmap_1`) after the preceding patch consolidated their logic into a single `open_bitmap_for_source()` helper.

The change is purely mechanical: seven lines of diff rename the functions and update their call sites in `pack-bitmap.c`. No behavior or interface changes are introduced. The patch is well-motivated by the earlier refactoring and keeps the codebase tidy as the series progresses toward exposing object filters at the ODB layer. There is no review feedback yet, but the patch looks uncontroversial and ready to advance.