This is a small, targeted refactoring patch in the **GSoC v16 series** implementing `git cat-file --batch-command`'s `remote-object-info` feature. It corrects a type mismatch in `fetch-pack.c` where `hash_algo_by_name()` (which returns `unsigned int`) was being assigned to an `int` variable. The fix changes the variable to `const unsigned int` to match the function's return type and adds `const` since the value is never modified.

The change is purely mechanical and localized to a single line in `write_fetch_command_and_capabilities()`. It addresses a minor technical debt issue that was likely introduced during earlier refactoring in the series. While the patch itself is trivial, it is part of the broader effort to harden the codebase for the new remote object metadata feature, which has undergone extensive review and iteration (16 versions to date).

This patch was split out from a larger commit in v15 (where it was tangled with the `advertise_sid` static variable removal) to improve clarity and separation of concerns. The commit message is concise and accurately describes the change. No behavior modification is intended, and the fix aligns with Git's coding style by ensuring type consistency and immutability where appropriate. The patch is uncontroversial and ready for merging as part of the larger series.