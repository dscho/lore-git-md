This is a small but important cleanup patch in the ongoing `remote-object-info` feature series. It fixes a type mismatch in `fetch-pack.c` where `hash_algo_by_name()` returns an `unsigned int`, but the variable receiving its value was declared as `int`. The patch also makes the variable `const` since it is never modified after assignment.

This change is part of the preparatory refactoring leading up to the core `remote-object-info` implementation. While trivial on its own, it eliminates a signed/unsigned mismatch that could theoretically cause issues in edge cases, and it improves code clarity by making the variable's immutability explicit. The patch is well-motivated and follows Git's coding style.

The series as a whole is now in its sixteenth iteration and appears technically complete, with all substantive review feedback addressed. This particular patch was split out from a larger refactoring commit in response to Junio Hamano's feedback about keeping changes focused. It's a good example of the careful, incremental cleanup that characterizes this series.