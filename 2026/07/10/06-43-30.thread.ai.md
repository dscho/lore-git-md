Thread identity: Patch series introducing new `includeIf` conditions (`worktree` and `worktree/i`) for Git configuration, authored by Chen Linxuan. This is the second patch in a two-patch series (v1).

Core problem or goal: Enable Git configuration includes to be conditionally applied based on the realpath of the current worktree's working directory, addressing a usability gap in multi-worktree setups where `.git` directory paths (handled by existing `gitdir` conditions) are not intuitive or consistent.

Current status: Under review (v1). The series is new; no prior versions or merge status reported. The first patch (not summarized here) introduced the `include_by_path()` helper, which this patch reuses.

Key technical details:
- New conditions: `includeIf.worktree:<path>` and `includeIf.worktree/i:<path>` (case-insensitive variant).
- Worktree path obtained via `repo_get_work_tree()`.
- Files touched: `config.c` (core logic), `Documentation/git-config.txt` (documentation), and test files (`t/t1305-config-include.sh`).
- No on-disk format changes; purely runtime behavior.
- Test coverage includes bare repositories, symlinked paths, case sensitivity, early config reading, and non-repository scenarios.

Open questions and loose ends: None identified yet. The patch is well-documented, tested, and addresses a clear use case. No objections or requests for changes have been raised in this email.

Participant positions: None recorded yet (initial submission).

Related work: Depends on the first patch in the series (introducing `include_by_path()`). No external dependencies or prior versions mentioned. The feature complements existing `gitdir` and `extensions.worktreeConfig` functionality.