This is a test infrastructure refactoring patch, part of the ongoing `remote-object-info` feature series (GSoC v16, patch 3/13). It extracts three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is purely mechanical: the functions are moved verbatim, with no behavior modification, and the original test script now sources the new library.

The motivation is code reuse: a later patch in the series (likely the `remote-object-info` command implementation) will need these utilities, and centralizing them avoids duplication. The patch is small (17 insertions, 12 deletions) and self-contained, touching only test infrastructure. No substantive review feedback is apparent from the diff, and the change aligns with Git’s established practice of consolidating test helpers into library files (e.g., `lib-loose.sh`, which is already sourced by `t1006`).

This is a minor but necessary preparatory step for the larger series. The extracted functions are simple shell utilities for string manipulation, and their relocation has no impact on the core functionality being developed. The patch is unlikely to be controversial, as it follows standard Git test organization patterns.