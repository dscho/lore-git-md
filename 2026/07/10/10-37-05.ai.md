This is the second patch in Pablo Sabater’s v8 series implementing cascading indentation for visual roots in `` `git log --graph` ``. It introduces `next_commit_to_show()`, a helper function in `revision.c` that unifies the source of commits for the lookahead buffer, addressing a subtle bug in v7 where the buffer was populated inconsistently depending on whether `--max-count-oldest` was used.

The patch is a targeted refactoring with no behavior change. It extracts the logic for retrieving the next commit—whether from `get_revision_internal()` or the saved list in `revs->commits`—into a single helper, ensuring the lookahead buffer in `struct git_graph` always fetches commits from the same source. This fixes the v7 issue where `--max-count-oldest` broke tests in `t4202` by populating the buffer from a different path. The helper also sets `SHOWN` and `CHILD_SHOWN` flags, preserving the existing behavior for `--max-count-oldest` while avoiding unintended side effects for `--reverse`.

The implementation is minimal and focused: the helper is only 20 lines, and the diff shows a straightforward replacement of the conditional logic in `get_revision()` with a single call to `next_commit_to_show()`. Compatibility with other in-flight topics (Jeff King’s `revs->commits` removal and Kristofer Haugsbakk’s `kk/prio-queue-get-put-fusion`) is explicitly preserved. The patch is ready for review and is a prerequisite for the lookahead buffer implementation in patch 3/4.

This is a well-motivated, low-risk refactoring that resolves the last known architectural issue in the series. The commit message clearly explains the problem, the solution, and the edge case (`--reverse`) that required special handling. No substantive review feedback is expected beyond confirmation that the helper behaves identically to the original logic.