Thread identity
Subject: [PATCH v3 0/2] fetch: configurable submodule fetch errors
Series version: v5 (v4 plus case-sensitivity fix, centralized parsing, test portability fix, and submodule fetch logic flaw fix)
Originating author: Paulius Zaleckas
Thread type: patch series

Core problem or goal
Allow users to configure whether `git fetch --recurse-submodules` should fail hard or merely warn when a submodule’s gitlink points to an OID that is not reachable from the submodule’s remote. The default (`fail`) preserves backwards compatibility; the new `warn` mode enables workflows where upstream topic branches are still in flux without disrupting the entire fetch. The series also fixes a pre-existing logic flaw in submodule fetch error handling where phase-1 fetch failures were recorded prematurely, even when a subsequent phase-2 OID-based fetch could still succeed.

Current status
Technically complete; all prior review feedback addressed. Junio’s latest review requested mechanical improvements (case-sensitive config values, centralized parsing, test portability) and v5 implements them. Patch 1/2 fixes the pre-existing submodule fetch logic flaw, and Patch 2/2 adds the user-facing controls. No new design objections or open technical concerns remain. Under review; no merge status change yet.

Key technical details
- Files touched: `submodule.c`, `submodule.h`, `builtin/fetch.c`, `Documentation/config/fetch.txt`, `Documentation/fetch-options.txt`, `t/t5526-fetch-submodules.sh`
- New config key: `fetch.submoduleErrors` (values: `fail`, `warn`; case-sensitive, parsed with `strcmp`)
- New CLI option: `--submodule-errors=(fail|warn)`
- New behavior: when set to `warn`, submodule fetch errors are reported but do not abort the parent fetch
- Forwarding: the resolved value (whether from CLI or config) is propagated to child fetches (`--all`, `--multiple`, nested recursion) when explicitly set, preserving user intent. The setting is explicitly forwarded to per-remote child processes to override any repository config.
- Error routing: all submodule fetch errors flow through `record_fetch_error()`, which defers error recording until all fetch phases (refs and OID-based retry) have failed, fixing a logic flaw where phase-1 failures could abort the fetch prematurely. The helper also ensures errors are consistently reported with the submodule name in the final summary.
- Implementation improvements: config value parsing and option formatting are now centralized in a shared name array (`submodule_error_names`), eliminating redundant conditional branches and aligning CLI/config logic. `strcasecmp()` replaced with `strcmp` for case-sensitive matching.
- Test coverage: new tests for warn mode, error reporting, CLI override propagation, and edge cases, including tests verifying that a failed submodule fetch is still reported even when the gitlinked commits are present locally, and that the setting is forwarded correctly with `--all`. The test suite also covers the pre-existing logic flaw fix, ensuring phase-1 failures are only recorded after all retry attempts fail.
- Test hygiene: raw `grep` replaced with `test_grep` for portability.

Open questions and loose ends
None. All prior review feedback has been addressed: case-sensitivity, centralized parsing, test portability, documentation fixes, and the submodule fetch logic flaw.

Participant positions
- Paulius Zaleckas (author): implemented the series, incorporated all feedback, and posted v5. Patch 1/2 fixes a pre-existing logic flaw in submodule fetch error handling; Patch 2/2 adds the user-facing controls and wiring. Credits Jean-Noël Avila for documentation fixes in v2.
- Junio C Hamano (maintainer): reviewed prior versions and raised two substantive technical concerns:
  1. Config value case sensitivity: values for `fetch.submoduleErrors` should be case-sensitive (v5 now uses `strcmp`).
  2. Option forwarding consistency: refactor the forwarding logic to use centralized helper functions (v5 implements this via `submodule_error_names`).
  Junio also identified the test portability issue (raw `grep` → `test_grep`), now fixed. No Reviewed-by or Acked-by yet; the series is now technically complete and awaiting maintainer action.
- Ramsay Jones: reviewed v4 and noted the test portability issue (raw `grep`), now fixed in v5.
- Jean-Noël Avila: contributed documentation fixes in v2, credited in v5.