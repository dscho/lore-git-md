This is the final exchange in Kristofer Karlsson’s two-patch bugfix series that fixes a quadratic performance regression in Git’s reftable backend when re-creating deleted refs. The series is now **ready for merge**—Patrick Steinhardt’s review acked the patch with only minor cosmetic feedback (a commit message typo and a missing comment on the new `suppress_deletions` field in `struct reftable_stack_options`), which Kristofer agrees to address only if a reroll becomes necessary for other reasons.

The conversation here is brief and confirms the implementation: the `suppress_deletions` flag has been moved from the merged iterator to `reftable_stack_options`, preserving libgit2’s current behavior while future-proofing the API. Kristofer also floats the idea of eventually deprecating `suppress_deletions` entirely, noting that libgit2’s call sites could be adapted with minimal effort, but acknowledges this is a separate discussion and does not affect the current patch.

The fix is well-motivated—it resolves a real-world performance issue observed in production-scale repositories (endorsed by brian m. carlson)—and is narrowly scoped to the reftable backend with no on-disk format changes. The perf test (`t/perf/p1401-ref-store-tombstones.sh`) demonstrates the regression and fix, improving runtime from ~14s to ~0.2s in tombstone-heavy scenarios. All substantive feedback has been incorporated, and the series is finalized.