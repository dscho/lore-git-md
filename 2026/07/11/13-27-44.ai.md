This is the ninth patch in the **ready-to-merge v6 series** optimizing `paint_down_to_common()` for one-sided histories. The patch consolidates the `min_generation` termination check into `paint_queue_get()`, alongside existing stale-entry and side-exhaustion checks, and moves `last_gen` into `struct paint_state` to avoid redundant `commit_graph_generation()` calls.

The change is a **refactoring cleanup** that centralizes termination logic in `paint_queue_get()`, improving code clarity and reducing duplication. It also enforces generation-number monotonicity via a `BUG()` assertion, catching potential ordering violations early. The `min_generation` cutoff now shares the same code path as other termination conditions, and the `last_gen` field is reused across checks, eliminating redundant generation lookups.

No behavior changes are introduced; the patch is purely structural. The diff moves the `min_generation` check from the main loop into `paint_queue_get()`, updates references to `last_gen` to use the struct field, and adjusts the `GENERATION_NUMBER_INFINITY` comparison to use `state->last_gen`. The `BUG()` assertion for generation skips is preserved but now uses the shared `last_gen` value.

This patch is **fully reviewed and uncontroversial**, serving as a preparatory step for the final patch (10/10), which implements the core early-termination optimization. The series remains **regression-free** and ready for merging.