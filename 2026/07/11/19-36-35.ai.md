This is the **final test-suite refinement** for patch 5/7 of Harald Nordgren’s `--delete-merged` series, addressing Phillip Wood’s feedback on test clarity and branch naming. The patch is now **implementation-complete** and ready for merging once the mechanical flags cleanup (acknowledged in earlier patches) is applied.

The update focuses on test improvements: replacing the artificial `mainline` branch with `main` and explicitly configuring `branch.main.pushRemote` to prevent `main` from being treated as a forked branch. This change eliminates an unnecessary indirection in the test setup while preserving the intended behavior—`main` is now protected from deletion because its push remote (`origin`) diverges from its upstream (`fork/main`), matching the real-world scenario where the default branch is preserved. The adjustment also exposes a subtle edge case in the feature’s design: when `remote.pushDefault` is set (as in the test environment), branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked unless manually exempted. Harald notes this as a known limitation but defers further discussion, as the core logic and safety checks remain unchanged.

The test suite now uses clearer branch names (`local-to-delete` instead of `on-local`) and removes redundant setup steps, improving readability without altering the patch’s functionality. All substantive design questions—including stacked-branch protection, per-branch opt-out, and `--dry-run`—were resolved in prior iterations, and this update addresses only mechanical test refinements. The series remains on track for inclusion.