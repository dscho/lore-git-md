This is a follow-up cleanup patch to Patrick Steinhardt’s ODB abstraction series, addressing a dead-code inconsistency in `add_object_entry()`. Junio C Hamano replies to Jeff King’s patch, which removes the unused `int` return from `add_object_entry()` to align it with the recently modified `add_object_entry_from_bitmap()`. The change is trivial—four lines removed, five adjusted—but eliminates a long-standing inconsistency where two related functions returned `int` with different semantics.

Junio’s response is brief and supportive. He suggests referencing the earlier patch by title rather than commit ID (since the series hasn’t been merged yet) and leaves the door open for Patrick and Peff to decide whether to squash this into a future v3 or position it as preliminary cleanup. The tone implies this is a non-controversial, low-risk change that could proceed independently or alongside the ODB series.

No new technical details or objections are raised; the patch remains ready to merge. The only open question is logistical—whether it will land as part of the ODB series or as a standalone cleanup. For a future AI summarizer, note the files touched (`builtin/pack-objects.c`), the function modified (`add_object_entry()`), and the context that this is a follow-up to Patrick’s v2 3/8 patch.