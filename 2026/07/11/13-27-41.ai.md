This is the sixth patch in the **ready-to-merge v6 series** optimizing `paint_down_to_common()` for one-sided histories. It **adds deterministic test coverage for clock-skew edge cases** in `t6600-test-reach.sh`, ensuring the side-exhaustion optimization (introduced earlier in the series) does not regress when commit dates violate topological ordering.

The patch introduces two new clock-skew topologies (`se-*` and `se2-*`) where commit dates are deliberately misordered (e.g., a parent has a higher date than its child). These test that the generation-number-based queue ordering (now enforced by the series) correctly handles cases where the obsolete commit-date fallback would have caused premature side-exhaustion or missed merge bases. It also adds **step-count assertions** (`test_paint_down_steps`) to the existing edge-case tests (pending-stale, infinity-both-sides) and a new topology mixing finite and `INFINITY` generation numbers, verifying the walk transitions correctly between commit-graph and non-commit-graph regions.

**Key details:**
- **Files touched:** Only `t/t6600-test-reach.sh` (96 insertions, 2 deletions).
- **New test cases:** `merge-base --all with clock skew` and `merge-base --all with clock skew and redundant ancestor`, plus step counts for existing edge cases.
- **Symbols used:** `test_paint_down_steps` (introduced in patch 5/10), `skew_commit` (helper for creating commits with arbitrary dates).
- **Behavior validated:** The series’ early termination logic works correctly even when commit dates are out of order, and redundant merge bases are filtered by `remove_redundant()`.
- **Performance instrumentation:** Step counts (e.g., `6 4 6 6` for `se-*`) confirm the optimization’s effectiveness across all commit-graph modes (none, full, half, no-gdat).

This is a **test-only patch** with no code changes. It **complements the earlier edge-case tests** (patch 3/10) by focusing on clock-skew scenarios, which are critical for repositories with import grafts or shallow histories. The tests are **deterministic and self-verifying**, running in all commit-graph modes to ensure robustness. No open questions or objections remain; the patch is **ready to merge** as part of the series.