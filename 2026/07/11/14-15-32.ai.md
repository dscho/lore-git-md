This is a **substantive review** of Pablo Sabater’s `git log --graph` cascading indentation feature (v9). Mirko Faina raises a **user-experience concern** about the new indentation behavior: when multiple single-patch series appear in sequence, the commits are indented even though they are not visually ambiguous, cluttering the output. He suggests **making the indentation opt-in**, gating it behind `--oneline` or `--format=reference` and keeping the traditional flat layout as the default.

The objection is **not about correctness**—the series already addresses all known edge cases—but about **readability trade-offs**. Mirko’s example (multiple one-patch series) is a real-world scenario where the indentation adds noise rather than clarity. His proposal would preserve the feature’s benefits for complex histories while avoiding unnecessary visual overhead for simpler cases.

This is a **high-weight review**: Mirko is not a core contributor, but his concern is concrete, well-illustrated, and directly targets the user-facing behavior of a feature that has already undergone nine revisions. The thread has so far focused on technical robustness (lookahead buffer, edge cases, test coverage); this reply shifts the discussion toward **usability and backward compatibility**. If the maintainers agree, it could force a **design pivot**—either a new flag, a heuristic to suppress indentation in trivial cases, or a reversion to opt-in semantics.

The timing is delicate: the series is **technically complete**, with all known bugs fixed and Junio Hamano’s architectural approval secured. Mirko’s feedback arrives just as the patches are poised for `next`, raising the stakes for a late-stage change. The thread’s next steps will likely hinge on whether the maintainers view this as a **blocking UX issue** or a **post-merge refinement**. Either way, this reply ensures the usability question is now part of the record.