This is the first patch in a **ready-to-merge performance optimization series** (v6) targeting Git’s commit-reach subsystem. The patch adds a **new technical document** (`Documentation/technical/paint-down-to-common.adoc`) explaining the `paint_down_to_common()` algorithm, which computes merge bases by walking commit ancestry.

The document is **comprehensive and well-structured**, covering use cases (merge-base queries and ancestry checks), the paint-walk algorithm (color flags, priority queue ordering), generation-number regions (finite vs. INFINITY), termination conditions (stale entries, generation cutoffs, single-result optimizations), and the commit-date ordering fallback. It also clarifies edge cases, such as why the single-result optimization is unsafe under date ordering. The writing is **clear and precise**, with diagrams and cross-references to related documentation (e.g., `commit-graph.adoc`).

The patch itself is **minimal and correct**: it adds the new `.adoc` file to `Makefile` and `meson.build`, and updates a comment in `commit-reach.c` to reference the new documentation. The commit message is **concise and informative**, crediting Kristofer Karlsson (a co-signer of the series) as the author.

This patch is **pure documentation** and **zero-risk**—it introduces no code changes beyond the build system integration. It is **essential context** for the rest of the series, which optimizes `paint_down_to_common()` by adding early termination for one-sided histories. The series as a whole is **fully reviewed, regression-free, and ready to merge**, with this patch serving as the foundation for understanding the algorithmic changes that follow.