This is the first patch in Pablo Sabater’s v9 series implementing cascading indentation for visual roots in `git log --graph`. The patch itself is a test infrastructure cleanup: it moves the duplicated `check_graph` helper function from `t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh` into `t/lib-log-graph.sh` and renames it to `lib_test_check_graph`. Both test files already import this library, so the change is purely mechanical and reduces code duplication.

The patch is small (29 insertions, 34 deletions) and uncontroversial. It was already reviewed and merged on July 12, so this version is just a re-send for completeness as part of the v9 series. No further review is needed; the patch is effectively a historical note for anyone following the series.