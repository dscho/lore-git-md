This is the first patch in Pablo Sabater’s **v9 series** implementing cascading indentation for visual roots in `git log --graph`. It is a **test infrastructure refactoring** patch that moves and renames the `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into `t/lib-log-graph.sh`, where it is renamed to `lib_test_check_graph`. Both test files already import graph-related helpers from this library file, so the change consolidates duplicated code without altering any test logic or behavior.

The patch is **mechanical and uncontroversial**: it replaces 34 lines of duplicated function code with a single 5-line definition in the shared library, then updates all 23 call sites in the two test files to use the new name. No test cases are added or modified, and the expected output for every test remains identical. The motivation is purely to prepare for the next patch in the series, which introduces new graph tests in a separate file and will need the helper.

This patch was **already merged** (2024-07-12) as a standalone cleanup, so it is effectively settled. The rest of the v9 series—patches 2/4 through 4/4—implement the lookahead buffer and visualization logic and are still under final review. The series as a whole is **technically complete**, addressing all known edge cases including the `--author`/`-grep` filter issue that was the last blocker in v8. Junio Hamano and Phillip Wood’s reviews of the remaining patches are pending, but this patch is done and requires no further attention.