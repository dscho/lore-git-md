This is the first patch in Harald Nordgren’s **v18 `git branch --delete-merged` series**, which is now implementation-complete and ready for merging. The patch introduces the `--forked` filter for `git branch --list`, allowing users to list branches whose upstream matches a given pattern (e.g., `origin/main`, `origin/*`, or a remote name like `origin`). This is a foundational piece of the series, enabling the `--delete-merged` command in later patches.

Harald has addressed Phillip Wood’s **surface-level review feedback** with minor test cleanups: removing an unnecessary detached HEAD, reordering error-case tests, and deleting a redundant `test_commit`. The changes are mechanical and do not affect functionality. Phillip’s only substantive suggestion—a speculative `--forked=recurse` mode for traversing upstream chains—was deferred as a future enhancement, with no impact on this patch.

The implementation itself remains **well-crafted and uncontroversial**. The `--forked` filter supports exact refnames, remote shorthand, and shell-style globs, and composes cleanly with existing filters like `--merged` and `--no-merged`. Phillip’s review confirmed the core logic is sound, with no bugs or design flaws identified. The patch is **ready for merging** alongside the rest of the series, pending the mechanical flags fix and test improvements in later patches.

This is a **feature patch** that establishes a new, composable filtering primitive for branch management. The series as a whole addresses a long-standing user pain point—safe automated cleanup of merged local branches—with robust safety mechanisms (per-branch opt-out, stacked-branch protection, `--dry-run` preview) and thoughtful design. The `--forked` filter is a key enabler for these features, and its inclusion here reflects careful planning rather than ad-hoc functionality.