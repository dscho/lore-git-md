**Summarizer brief mode**

This is a **maintainer procedural reply** to the v6 series "commit-reach: terminate merge-base walk when one side is exhausted". Junio C Hamano **rejects the rebase on `next`** and instructs the author to **rebase instead on a stable base** (e.g., `v2.55.0` or `master`) **merged with only the required dependency** (`kk/commit-reach-find-all-fix`). The series itself remains **technically approved and ready to merge**, but the current rebase is invalid because it pulls in unrelated topics from `next`. The author must **reconstruct the base** by merging `kk/commit-reach-find-all-fix` (now in `next`) into a stable tag, then rebase the v6 patches on that. No technical changes are needed; this is purely a **procedural resubmission requirement**. The thread is now blocked on the rebase, not on design or review. Key context: the dependency `kk/commit-reach-find-all-fix` is already in `next`, so the required merge is straightforward.