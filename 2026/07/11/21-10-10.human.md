This is a procedural note from Junio C Hamano about the `repo_config_values` migration series (v9). The series, which moves nine configuration variables into `struct repo_config_values` as part of the ongoing libification effort, is already queued in Junio’s `seen` branch. Junio clarifies how to identify the base commit for the series: by inspecting the merge commit in `origin/seen` using `git log --oneline origin/master..origin/seen`. He notes that the series is currently based on `ab776a62a7` (Git 2.55-rc2, 2026-06-22), not the OID Tian Yuchen provided (`8d96f09e92`).

The email does not introduce new technical feedback or change the series’ status. The patches remain under review, with all known memory leaks resolved in v9. Key unresolved items—such as the RFC questions about migrating program paths, the circular dependency workaround for `push_default`, and the submodule limitation in `repo_config_values_clear()`—are unchanged. Junio’s note is purely informational, helping contributors apply the series locally without waiting for a reroll. No action is required from Tian or reviewers at this stage.