This is the **test cleanup reply** for the first patch in Harald Nordgren’s v18 `git branch --delete-merged` series. The patch itself (adding the `--forked` filter for `git branch --list`) remains **implementation-complete and technically sound**; this email addresses only minor test improvements suggested by Phillip Wood.

Harald acknowledges three surface-level adjustments:
1. **Detached HEAD**: Removes an unnecessary `git checkout --detach` in the test setup, as the detached state was only needed for later tests in the series.
2. **Test reordering**: Moves the two error-case tests (`--forked rejects unknown branch/pattern` and `--forked requires a value`) to the end of the test sequence for better logical flow.
3. **Speculative recursion**: Notes that Phillip’s idea for a future `--forked=recurse` mode (to traverse upstream chains) is non-blocking and can be deferred.

No functional changes are made to the `--forked` filter implementation, which continues to support exact refnames, remote shorthand, and shell-style globs. The patch is **ready for merging** pending these mechanical test tweaks, which do not affect the core functionality or API. The series as a whole remains on track for integration once the remaining test improvements (in patch 5/7) and the flags maintenance fix (in patches 2-4/7) are addressed.