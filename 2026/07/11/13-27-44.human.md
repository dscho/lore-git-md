This is the ninth patch in the **ready-to-merge v6 series** optimizing `paint_down_to_common()` for one-sided histories. The patch is a **refactoring cleanup** that consolidates the `min_generation` termination check into `paint_queue_get()`, alongside the existing stale-entry and side-exhaustion checks.

The change moves `last_gen` into `struct paint_state` so that `commit_graph_generation()` is called exactly once per dequeued commit, sharing the result across all termination conditions and the monotonicity BUG assertion. This eliminates redundant generation lookups and makes the code more maintainable. The logic remains functionally identical, but the refactoring prepares the ground for the final early-termination optimization in patch 10/10.

The patch touches only `commit-reach.c` and is **low-risk**: it removes redundant generation checks, centralizes the `min_generation` cutoff, and ensures the BUG assertion is triggered consistently. The commit message is clear, and the diff is straightforward. This is a **polishing step** that does not affect behavior but improves code hygiene. The series as a whole is **fully reviewed and ready to merge**, with this patch being a non-controversial precursor to the core optimization.