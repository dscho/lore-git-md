This is the first patch in a two-patch test modernization series targeting `t/t9811-git-p4-label-import.sh`. The patch mechanically breaks long `&&` chains into multiple lines for improved readability, as agreed in the v1 review. The changes are purely stylistic and confined to the Git-Perforce integration test script; no behavior or test logic is altered.

The patch updates five `&&` chains across three test blocks, splitting them into one command per line. This aligns with the ongoing community-wide effort to modernize Git’s test suite by replacing legacy shell idioms with more maintainable patterns. The commit message is concise but clear, crediting the co-author and framing the change as part of a broader initiative.

No new symbols or on-disk format changes are introduced. The patch is uncontroversial and ready for integration, with all feedback from the v1 review incorporated. The only technical detail likely to surface in follow-up discussion is the file path (`t/t9811-git-p4-label-import.sh`) and the specific test modernization pattern (splitting `&&` chains). The second patch in the series will address the replacement of legacy test constructs (`test -f` and `! test -f`) with modern Git test helpers.