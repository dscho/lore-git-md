This is a cleanup patch from Jeff King (Peff) that tidies up an unused return value in `builtin/pack-objects.c`. It follows up on a side discussion in Patrick Steinhardt’s ODB abstraction series, specifically patch 3/8 in v2, where Peff noted that the return value in `add_object_entry()` has been dead code since 2006 (commit `5379a5c5ee`). The patch changes the function’s return type from `int` to `void`, removing the unused return statements and aligning the function’s signature with its actual behavior.

The patch is small (9 lines changed) and mechanical, touching only one file. It does not affect functionality, as the return value was never used by callers. The change also makes the divergence between `add_object_entry()` and its sister function, `add_object_entry_from_bitmap()`, more explicit: the latter was recently modified to always return 0 to conform to a callback interface, while the former no longer returns anything at all.

Peff’s commit message is clear and concise, explaining the historical context and the rationale for the change. The patch is well-motivated and uncontroversial, addressing a minor inconsistency that surfaced during the review of Patrick’s series. It is ready to be picked up alongside the rest of the ODB abstraction work, though it is not a dependency for that series. The patch is a good example of the kind of incremental cleanup that keeps Git’s codebase tidy without introducing risk.