This is a test fixup for a recent feature patch that added `fetch.submoduleErrors` to make submodule fetch errors non-fatal. Ramsay Jones reports that the patch `df9481e6d6` (part of the `pz/fetch-submodule-errors-config` branch) introduced test failures in `t/t5526-fetch-submodules.sh` when integrated into the `seen` branch.

The fix replaces all instances of `grep` with `test_grep` in the affected test file. This is a mechanical change to align with Git’s test suite conventions, where `test_grep` is the preferred helper for pattern matching in test output. The patch touches nine test cases, all of which verify error handling behavior for submodule fetches under different configurations (`fetch.submoduleErrors=warn`, `--submodule-errors=warn`, etc.).

The commit message is minimal—just noting that the fixup was necessary for a clean test run—but the context makes it clear this is a follow-up to a larger feature. The original patch (`df9481e6d6`) is still cooking in `seen`, so this fixup is likely to be squashed before the topic graduates to `next`. No controversy or design discussion is evident; it’s a straightforward test suite adjustment.