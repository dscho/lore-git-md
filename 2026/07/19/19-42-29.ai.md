This is a substantive review comment in the ongoing `git branch --delete-merged` thread, specifically addressing the unresolved order dependency issue in the stacked-branch protection logic. Phillip Wood proposes a structural refactor to the `spare_stacked_bases()` function, suggesting that instead of mutating `data->deletable` during the branch traversal (which creates the order dependency), the function should first collect all branches to be spared in a temporary `strset`, then remove them from `data->deletable` in a single batch operation after the traversal completes. This approach would eliminate the sequential, order-dependent behavior by decoupling the identification of spared branches from their removal from the deletable set. Phillip acknowledges the complexity and plans to review the v19 iteration more thoroughly in the coming week, but his proposal offers a concrete path forward that could satisfy Junio C Hamano's correctness concerns without requiring a full multi-pass algorithm. The series remains blocked pending resolution of this issue, and this email advances the discussion by providing a specific technical alternative to Harald Nordgren's minimal fix.