This is a **tested, substantive review** from Jeff King (Peff) on brian m. carlson’s Rust hash cleanup series. Peff confirms both patches look correct and address pre-existing issues in the Rust/C boundary layer, not regressions from his own recent C-side leak fixes. He briefly considers but rejects an alternative approach for patch 1 (making `git_hash_clone()` work without prior initialization) on the grounds that it would complicate callers and risk introducing new leaks. The review is confined to `src/hash.rs` and the C/Rust FFI; no broader Rustification concerns are raised. Peff’s sign-off effectively clears the series for merging once any minor Rust-specific feedback is addressed.

Key technical details retained for follow-up: the series touches only `src/hash.rs`, declares `git_hash_discard` in the Rust FFI, and leaves on-disk formats and APIs unchanged. The review explicitly notes that the fixes are memory-hygiene only and do not alter behavior.