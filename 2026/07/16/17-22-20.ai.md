This is a **substantive review** from Junio C Hamano on the third patch of Harald Nordgren’s `--auto-reset` feature series for `git bisect`. The review raises a fundamental design question about the intended behavior of the new option.

Junio’s feedback centers on the post-bisect state: he expects `--auto-reset` to leave the working tree on the *first bad commit* (the culprit) rather than returning to the original pre-bisect commit. His workflow after identifying the bad commit typically involves inspecting that commit directly (`git reset --hard` or `git bisect reset` to it), so an auto-reset that returns to the starting point would be inconvenient. The comment is phrased as a clarification request rather than a rejection, but it signals that the current implementation may not align with the maintainer’s mental model of how the feature should work.

No other technical issues are raised in this email; the focus is entirely on the high-level behavior. The thread context shows this is the first review comment on the series, so the author will need to address this point before the patch can proceed.