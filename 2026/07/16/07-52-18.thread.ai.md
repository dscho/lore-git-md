Thread identity
Subject: [PATCH 0/6] Reintroduce writev(3p) wrapper for performance
Series version: v2 (revival with compatibility wrapper simplification)
Originating author: Patrick Steinhardt
Thread type: patch series

Core problem or goal
Reintroduce a `writev(3p)`-like wrapper to reduce system call overhead in low-level I/O operations, specifically targeting pktline transmission in `sideband.c` and cat-blob responses in `fast-import.c`. The wrapper was originally merged in Git 2.54 but reverted due to platform compatibility issues (NonStop, CMake); this series incorporates fixes for those issues, resolves all prior technical objections, and demonstrates broader applicability. The goal is performance improvement through batched writes, with the API design, naming, and architectural questions now fully resolved.

Current status
Under review; v2 posted. The series is self-contained with no unresolved external dependencies. All technical design and correctness issues from v1 have been resolved: the integer-overflow concern raised by Simon Richter was addressed by Junio C Hamano’s confirmation of existing bounds-checking safety and the compatibility wrapper’s simplification; API naming is resolved in favor of `git_write_gather()` to avoid implying POSIX semantics; the architectural question about atomicity is resolved by Johannes Sixt and Junio C Hamano’s confirmation that Git’s pktline and cat-blob use cases do not require atomicity and are inherently single-writer scenarios. Junio C Hamano accepted Patrick Steinhardt’s proposed one-line tweak to the compatibility wrapper, which writes only the first `iovec` in the loop to restore the non-interleaving property of `write(3p)` and avoid interleaving even on platforms without `writev(2)`. This change simplifies the wrapper by removing overflow checks and most loop logic, making the code both safer and cleaner. Patrick Steinhardt confirms in this email that the compatibility wrapper will write only the first `iovec` in the loop, directly addressing Johannes Sixt’s forward-compatibility concern about future contributors assuming atomicity while preserving the performance gains of batching. Patch 3/6 (new in this update) adds `xwritev()` to handle `MAX_IO_SIZE` limitations on platforms like NonStop, ensuring the wrapper remains robust across all supported systems. Patch 4/6 applies the new `git_write_gather()` infrastructure to `sideband.c`, replacing two `write()` calls per pktline with a single `writev()`-like call, reducing syscall count by roughly half and demonstrating a 1.37× latency improvement in a synthetic `git-upload-pack` benchmark (193 ms → 141 ms). Patch 5/6 (new in v2) applies the same infrastructure to `fast-import.c`’s `cat-blob` responses, replacing three `write()` calls with a single batched write, yielding a 4 % reduction in wall-clock time (1.320 s → 1.270 s) and a 18 % reduction in system CPU time (0.161 s → 0.132 s) in a synthetic benchmark with 100,000 blobs. The series is now technically complete and awaits Junio C Hamano’s final assessment of merge appetite.

Key technical details
- Files touched: `Makefile`, `compat/posix.h`, `compat/writev.c` (new), `wrapper.{c,h}`, `write-or-die.{c,h}`, `sideband.c`, `builtin/fast-import.c`, plus build system files (`config.mak.uname`, `CMakeLists.txt`, `meson.build`).
- New symbols: `git_write_gather()` and `git_write_gather_or_die()` wrappers (naming resolved); `writev_in_full()` and `writev_or_die()` plumbing functions (added in patch 2/6); `xwritev()` (added in patch 3/6) for `MAX_IO_SIZE` handling; platform-specific implementations in `compat/writev.c`. The API is a two-level abstraction: a Git-specific API that platforms can implement either via `writev(2)` or as a series of `write(2)` calls, with explicit documentation that emulation may be non-atomic.
- Behavior change: replaces multiple `write()` calls with batched `writev()`-like operations in pktline transmission and cat-blob responses; explicit `MAX_IO_SIZE` handling to avoid partial writes. The compatibility wrapper now writes only the first `iovec` in the loop, restoring the non-interleaving property of `write(3p)` and matching `write(2)` behavior to avoid interleaving even on platforms without `writev(2)`. Patch 4/6 demonstrates the performance impact by reducing syscall count in `sideband.c` from ~133,000 to ~67,000 in the benchmark scenario, yielding a 1.37× latency improvement in `git-upload-pack`. Patch 5/6 extends this to `fast-import.c`, reducing syscall count in `cat-blob` responses and yielding a 4 % wall-clock and 18 % system CPU time improvement in the synthetic benchmark.
- On-disk format: no changes.
- Test coverage: none mentioned; benchmarks referenced in patches 4/6 and 5/6 with reproduction recipes.
- Safety concerns: The integer-overflow issue in the `iov_len` accumulation logic is resolved. Junio C Hamano confirmed the existing bounds-checking logic is safe because both `sum` and `iov[i].iov_len` are individually constrained to ≤ `SSIZE_MAX`, preventing wrap-around in `size_t`. The compatibility wrapper’s simplification (writing only the first `iovec`) removes the need for overflow checks entirely. The new `writev_in_full()` function handles partial writes by adjusting the `iovec` array in-place, discarding fully written segments and shrinking the current segment for the next attempt, matching the semantics of `write_in_full()`. Patch 3/6 introduces `xwritev()` to handle `MAX_IO_SIZE` limitations, splitting large `iovec` entries into smaller chunks to avoid `EINVAL` on platforms like NonStop, while preserving the retry logic for `EINTR` and `EAGAIN`.
- Architectural resolution: Johannes Sixt and Junio C Hamano confirmed that Git’s pktline and cat-blob use cases do not require atomicity, and both are inherently single-writer scenarios. The `git_write_gather()` API is therefore implemented as a simple loop of `write(2)` calls without violating necessary semantics, and competing writes to the same destination would be a bug regardless of atomicity.

Open questions and loose ends
- Benchmark results: magnitude of performance gains now demonstrated in patches 4/6 (1.37× latency improvement in `git-upload-pack`) and 5/6 (4 % wall-clock and 18 % system CPU time improvement in `fast-import`), but broader applicability to other use cases remains unspecified.
- Maintainer appetite: Junio C Hamano has not yet commented on whether the series is welcome or whether the performance gains justify the complexity.
- Patch self-documentation: absence of a range-diff to the reverted Git 2.54 version complicates review of changes beyond the NonStop and CMake fixes; the v2 range-diff provided here shows the key simplification in the compatibility wrapper and the addition of `xwritev()`.

Participant positions
- Patrick Steinhardt (author): motivated by performance gains; acknowledges prior revert and incorporates fixes; open to abandoning if unwelcome. Conditionally accepted the rename to `git_write_gather()` and proposed the one-line tweak to the compatibility wrapper to restore the non-interleaving property of `write(3p)`, which Junio C Hamano accepted. Confirmed in this email that the compatibility wrapper will write only the first `iovec` in the loop, directly addressing Johannes Sixt’s forward-compatibility concern. Added `xwritev()` in patch 3/6 to address `MAX_IO_SIZE` limitations, ensuring platform compatibility. Patches 4/6 and 5/6 demonstrate measurable performance improvements in `sideband.c` and `fast-import.c`, reinforcing the series’ motivation. Proposes retaining the overflow check in the wrapper to stay closer to POSIX requirements, though Junio C Hamano’s prior analysis suggests it is unnecessary.
- Simon Richter (reviewer): substantive technical review identifying a correctness issue (integer overflow) in the wrapper’s bounds-checking logic. The issue is resolved by Junio C Hamano’s re-examination and the wrapper’s simplification.
- Johannes Sixt (Hannes) (reviewer): substantive technical review resolving the API design and architectural questions. Initially objected to the name `git_writev()` for implying POSIX semantics, suggesting `git_write_gather()` instead (now resolved). Noted the lack of a range-diff to the reverted version, complicating review. Resolved the architectural question by confirming that Git’s pktline and cat-blob use cases do not require atomicity. Raised a forward-compatibility concern about the API name potentially misleading future contributors into assuming atomicity; this concern is addressed by the compatibility wrapper’s tweak (writing only the first `iovec`) and Junio C Hamano’s reaffirmation of explicit documentation.
- Junio C Hamano (maintainer): substantive technical review focused on the integer-overflow issue, API naming, and architectural resolution. Initially proposed a three-case fix for the overflow, but later retracted it after confirming the existing bounds-checking logic is safe. Endorsed `git_write_gather()` to avoid implying POSIX semantics and proposed a two-level abstraction for platform compatibility. Confirmed that Git’s pktline and cat-blob use cases do not require atomicity and are inherently single-writer scenarios. Resolved the forward-compatibility concern by reaffirming that explicit documentation of non-atomic emulation is sufficient. Accepted Patrick Steinhardt’s proposed one-line tweak to the compatibility wrapper, describing it as "clever" and noting that it simplifies the wrapper by removing overflow checks and most loop logic. Position resolves all technical disputes but does not yet address maintainer appetite or the self-documentation issue.

Related work
- Prior version: merged in Git 2.54, reverted due to platform issues (NonStop, CMake).
- Build system: CMake fixes from Johannes Schindelin. The new email confirms Windows (MSVC and MinGW) compatibility via `NO_WRITEV` in `config.mak.uname`, `CMakeLists.txt`, and `meson.build`. The wrapper uses a `git_iovec` struct to avoid symbol conflicts on platforms without native `writev` support. Patch 3/6 ensures compatibility with platforms like NonStop by handling `MAX_IO_SIZE` limitations. Patches 4/6 and 5/6 demonstrate the wrapper’s performance impact in real-world scenarios.