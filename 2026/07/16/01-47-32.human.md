This is the final reply in the GSoC v18 `remote-object-info` series, now cooking in `next`. Pablo Sabater concedes Junio’s point: the `%(objectname)`-only existence check will not be marked as `NEEDSWORK`. Instead, he will add a preparatory commit to the series that forces a "size" request (discarding the result) when only `%(objectname)` is used, ensuring the server validates the OID’s existence. This aligns with Junio’s insistence on correctness over scope limitations.

The exchange is brief and decisive, resolving the last open question before the series graduates to `next`. No new technical details or edge cases are introduced; the focus is purely on project standards. The series remains technically complete, with all prior feedback incorporated, CI passing, and the dynamic allow-list for `remote-object-info` unchanged. This reply confirms the series is ready for integration.