This is the final technical clarification in the GSoC v18 `remote-object-info` series, now queued for `next`. Junio questions Pablo Sabater’s plan to mark the `%(objectname)`-only existence check as `NEEDSWORK`, arguing that correctness should not be deferred. The exchange is narrow but decisive: Pablo proposed forcing a "size" request (and discarding the result) when only `%(objectname)` is used, to ensure the server validates the OID’s existence, but labeled this as a temporary measure. Junio rejects the `NEEDSWORK` label, insisting that correctness is non-negotiable for the initial merge.

The discussion is substantive but brief, focusing on whether the current implementation is *sufficiently* correct or merely *temporarily* correct. No new technical details emerge—only a clarification of project standards. The series remains technically complete, with all prior feedback incorporated and CI passing. This reply resolves the last open question, reinforcing that the `%(objectname)`-only case will be handled robustly (via the forced "size" request) without deferring the issue. The dynamic allow-list and placeholder handling are unchanged, and the series is now cleared for graduation to `next`.