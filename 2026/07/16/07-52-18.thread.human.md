This thread revisits a performance optimization that was previously merged in Git 2.54 but later reverted due to platform compatibility issues—specifically on the NonStop platform. Patrick Steinhardt has reintroduced the `writev(3p)` wrapper, which batches low-level I/O operations to reduce system call overhead, now with fixes for CMake (contributed by Johannes Schindelin) and NonStop, along with broader applicability across multiple code paths.

The series touches core I/O infrastructure, including `sideband.c` (pktline transmission) and `builtin/fast-import.c` (cat-blob responses), replacing multiple `write()` calls with the new `git_writev()` and `git_writev_or_die()` wrappers. It also includes build system adjustments for CMake and Meson, and explicit handling of edge cases like `MAX_IO_SIZE` to avoid partial writes. Benchmarks are mentioned but not detailed in the email, leaving the performance gains unspecified. The tone suggests this is a revival of a settled-but-reverted topic rather than a new debate, though Patrick notes he’s open to dropping the effort if the approach isn’t desired. No prior discussion or objections are referenced, so the thread’s trajectory may hinge on whether the fixes address the original concerns.