Thread identity: `[PATCH]` series adding seven new path-related keys (`path.toplevel`, `path.superproject-working-tree`, `path.objects`, `path.hooks`, `path.index`, `path.grafts`, `path.git-prefix`) to the `git repo info` command. Originating author: K Jayatheerth (GSoC contributor). Thread type: feature patch series. Current version: 1 (fourth patch posted; three remaining).

Core problem or goal: Expose filesystem locations of repository components (e.g., hooks directory, index file, grafts file, superproject working tree, object database) in a user-facing, scriptable format via `git repo info`. The new keys mirror existing internal Git struct fields (e.g., `the_repository->gitdir`, `the_repository->objectdir`) but avoid requiring direct access to Git’s internals. Each key supports suffix-based formatting (e.g., absolute vs. relative paths).

Current status: Initial submission (version 1). Under review. Four of seven patches posted: the first three introduced `path.toplevel`, `path.superproject-working-tree`, and `path.objects` keys; the fourth adds `path.hooks.absolute` and `path.hooks.relative`. No merge status recorded; no substantive review feedback yet.

Key technical details:
- Files touched: `builtin/repo.c` (implementation), `Documentation/git-repo.adoc` (documentation), `t/t1900-repo-info.sh` (tests).
- New symbols: Thirteen new keys with suffix-based formatting support: `path.toplevel.absolute`, `path.toplevel.relative`, `path.superproject-working-tree.absolute`, `path.superproject-working-tree.relative`, `path.objects.absolute`, `path.objects.relative`, `path.hooks.absolute`, `path.hooks.relative`, and five more pending (`path.index`, `path.grafts`, `path.git-prefix`). The first four patches implement the first seven keys, using `repo_get_work_tree()`, `get_superproject_working_tree()`, `repo_get_object_directory()`, `repo_git_path_replace()`, and `format_path()` with `PATH_FORMAT_CANONICAL` or `PATH_FORMAT_RELATIVE`.
- Behavior: Keys return filesystem paths (absolute or relative) for repository components, matching internal Git struct fields. In bare repositories or when not inside a submodule, the respective keys return an empty string. The `path.objects` keys respect the `GIT_OBJECT_DIRECTORY` environment override, and the `path.hooks` keys respect `core.hooksPath`.
- Test coverage: Tests verify key presence, formatting behavior, edge cases (e.g., bare repositories, standalone repositories, initialized submodules), and environment/config overrides for `path.objects` and `path.hooks`.

Open questions and loose ends:
- Key naming and suffix semantics: Reviewers may suggest adjustments for consistency with existing Git terminology (e.g., `gitdir` vs. `objects`, `hooks` vs. `hookdir`).
- No substantive review feedback yet; initial submission only. The first four patches are unlikely to be controversial but await technical review.

Participant positions: None recorded yet (initial submission).

Related work: None. Self-contained series; no dependencies on other in-flight topics (e.g., ODB abstraction, `the_repository` removal). Part of a GSoC project.