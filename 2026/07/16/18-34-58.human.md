This is a two-patch bugfix series for the `wincred` credential helper on Windows, now merged to `master`. The patches address memory corruption issues that could cause process termination or silent credential loss, particularly with OAuth tokens. Both fixes were previously validated in Git for Windows v2.55.0(3) and are now being upstreamed.

The changes are minimal—just six lines modified in `contrib/credential/wincred/git-credential-wincred.c`—and target specific buffer allocation and string-handling bugs. The commit messages clearly describe the symptoms and root causes, though no test coverage is included (consistent with the minimal testing infrastructure for `contrib/` code).

Junio’s reply signals immediate acceptance, noting the fixes will be "merged down fast." This aligns with the series’ low-risk, high-impact nature: the bugs are real, the fixes are narrowly scoped, and the code has already been battle-tested in Git for Windows. No controversy or open questions were raised in the thread.

For Windows users relying on `wincred`, this resolves two critical but subtle issues. For everyone else, the changes are invisible. The series is now part of the next Git release.