This is a two-patch bugfix series for the `wincred` credential helper, targeting Windows platform compatibility. The patches address memory corruption and silent credential loss when handling credentials and OAuth tokens, issues that were already fixed in the Git for Windows v2.55.0(3) security release.

The series touches only `contrib/credential/wincred/git-credential-wincred.c`, making small but critical changes to prevent data corruption and ensure OAuth tokens are stored correctly. The commit message does not detail the exact scenarios that trigger these bugs, but the fixes are clearly motivated by real-world problems encountered in the Git for Windows release.

Johannes Schindelin, the Git for Windows maintainer, is the author, which lends credibility to the fixes—these are not theoretical issues but problems that surfaced in production. The patches are narrowly scoped and appear to be straightforward corrections rather than architectural changes.

This is not part of any larger effort; it’s a targeted fix for a specific platform helper. If you work on Windows or maintain integrations that rely on `wincred`, this series is worth reviewing. For everyone else, it’s a low-priority but uncontroversial update.