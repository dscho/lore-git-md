This is a maintainer feedback message on the `diff.<driver>.process` RFC series (v5, 9 patches). Junio Hamano reports a **merge conflict** with the `mm/line-log-limited-ops` topic, which is currently cooking in `next`. The conflict appears to be structural rather than trivial: the series touches `line-log.c` (patch 9) to wire `git log -L` to the external diff process, while `mm/line-log-limited-ops` is also modifying the same subsystem, likely altering how line-log operations are collected or processed.

The email does not include details about the nature of the conflict (e.g., whether it is a textual overlap in `collect_diff()` or a deeper semantic incompatibility), nor does it suggest a resolution path. However, the timing is notable: the `diff.<driver>.process` series is now technically complete, with all substantive review feedback addressed, and was awaiting final maintainer sign-off. This conflict introduces a new blocker that will require either a rebase of the series onto the current `next` branch or a coordinated resolution with the author of `mm/line-log-limited-ops`.

For a future AI summarizer, key technical details to track:
- **Files involved**: `line-log.c` (specifically `collect_diff()`)
- **Conflicting topic**: `mm/line-log-limited-ops` (currently in `next`)
- **Impact**: Blocks merging of the `diff.<driver>.process` series until resolved
- **Context**: The conflict arises from the series' integration of `git log -L` with the external diff process, which is a core goal of the RFC.

The message is brief and factual, reflecting Junio's typical style for reporting integration issues. No judgment is passed on the series' merit, but the conflict will need to be resolved before the topic can proceed further.