This thread is a seven-patch series from Christian Couder refactoring `git fast-import` to modernize its usage string, reduce global variables, and prepare for libification. The series introduces new parse-options infrastructure (like `OPT_HIDDEN_GROUP`) and documents existing flags, while also beginning to encapsulate globals in a `fast_import_state` struct. Version 1 is under review, with no prior iterations.

The latest email covers patch 4/7, a small mechanical cleanup that scopes loop variables locally in `cmd_fast_import()` to use the most appropriate type for each loop. This aligns with the series' broader modernization goals but doesn’t change behavior or interfaces. So far, only CI results are available—no substantive technical review has been posted yet. The series remains focused on internal refactoring, with no on-disk format or CLI changes.