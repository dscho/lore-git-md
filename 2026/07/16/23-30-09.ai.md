This is Junio’s "What’s cooking in git.git" report for July 2026 (#07), summarizing the state of all in-flight topics in the integration branches.

The report is **not a patch** but a high-level snapshot of the current integration queue. It lists topics that have graduated to `master`, new topics cooking in `next` or `seen`, stalled topics, and discarded ones. Each entry includes the topic name, author, merge status, a brief description, and links to relevant discussion threads.

Key highlights for a downstream AI summarizer:

- **Graduated to `master`**: Nine topics, including `ps/history-drop` (new `git history drop` subcommand), `ps/refs-writing-subcommands` (new `git refs create/delete/update/rename`), `rs/blame-abbrev-marks` (optimized blame output alignment), and `jk/bloom-leak-fixes` (memory leaks in Bloom-filter code paths).
- **New topics in `next`/`seen`**: 15 new topics, including `js/coverity-unchecked-returns-fix` (Coverity warning fixes), `ps/odb-for-each-object-filter` (ODB enumeration with filters), `ps/refs-wo-the-repository` (removing `the_repository` from refs API), and `hn/history-squash` (new `git history squash` subcommand).
- **Stalled topics**: Three topics (`kh/doc-trailers`, `sn/rebase-update-refs-symrefs`, `jt/config-lock-timeout`) have been waiting for responses for weeks.
- **Performance and correctness fixes**: `sc/wt-status-avoid-quadratic-insertion` (O(n log n) status enumeration), `kk/no-walk-pathspec-fix` (regression fix for `--no-walk` pathspec filtering), and `ds/sparse-index-ita-crash` (sparse-index crash fix).
- **ODB and refs refactoring**: Multiple topics (`ps/odb-drop-whence`, `ps/refs-wo-the-repository`, `ps/odb-for-each-object-filter`) continue the ODB abstraction and `the_repository` removal efforts.
- **New features**: `hn/history-squash` (squash subcommand for `git history`), `hn/branch-delete-merged` (`git branch --delete-merged`), and `pz/fetch-submodule-errors-config` (configurable submodule fetch error handling).
- **Build and CI**: `js/ci-dockerized-pid-limit` (CI resource limit fixes), `sn/osxkeychain-rust-universal` (universal macOS builds with Rust).
- **Documentation**: `jc/relnotes-2.55-rust-fix` (retroactive Rust clarification in release notes), `jc/submitting-patches-abandoning` (documenting topic retraction expectations).

The report also includes a section on **discarded topics**, such as `kk/prio-queue-cascade-sift` (retracted due to performance concerns).

This summary provides enough context for a future AI session to track follow-up discussions, such as:
- Whether `ps/odb-for-each-object-filter`’s use of reachability bitmaps is controversial.
- How `hn/history-squash`’s message template handling differs from `git rebase --squash`.
- Whether `kk/merge-base-exhaustion`’s early termination logic introduces edge cases.
- The status of `ps/cat-file-remote-object-info`’s dynamic allow-list for format placeholders.

The report is **not actionable** for human readers but is essential for tracking the project’s pulse.