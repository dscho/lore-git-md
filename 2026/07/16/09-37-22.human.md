This is a substantive review reply in the ongoing thread about adding `--abort` hints to `git rebase` failure messages. Phillip Wood pushes back against Harald Nordgren’s framing of the `--abort` hint as a universal "escape hatch," arguing that aborting a rebase after a failed `exec` command is rarely the right action. Instead, he suggests the workflow should guide users toward fixing the issue (e.g., editing the todo list or amending the commit) and continuing the rebase. His proposal to teach `git rebase --skip` to handle rescheduled `exec` failures and add hints for todo-list editing directly addresses the root problem: users often don’t know how to recover from `exec` failures without aborting.

The email shifts the discussion from interface consistency to workflow correctness. Phillip’s critique is technical and specific: he questions the assumption that `--abort` is always a valid option, cites real-world scenarios (e.g., a missing command in the todo list), and proposes concrete alternatives (`--skip` enhancements, todo-list editing hints). This challenges the thread’s prior consensus (split messages, suppress hint for conflicts) and introduces a new axis of debate: whether the hint should be suppressed not just for conflicts but for *any* recoverable `exec` failure. The tone is constructive but firm, making it clear this is a substantive disagreement, not a nit.