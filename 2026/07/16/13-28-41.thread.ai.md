Mode: ai

Thread identity
Subject: [PATCH v5 0/6] repack: introduce --drop-filtered for partial clones
Series version: v5 (posted)
Originating author: Siddharth Shrimali (first-time contributor)
Thread type: patch series

Core problem or goal
Extend `git repack` with a `--drop-filtered` option that safely reclaims disk space in partial clones by removing locally cached promisor blobs exceeding a user-specified size threshold (e.g., `--filter=blob:limit=10M`). The feature guarantees safety by (1) only targeting objects marked as promisor (ensuring they are recoverable from the promisor remote), (2) avoiding locally created objects, and (3) introducing UX optimizations (merge/rebase/cherry-pick checks and index validation) to prevent immediate re-fetching. Junio previously proposed an alternative, time-based heuristic to reduce redundant network operations, but this and two other items—remote verification (via the pending `remote-object-info` protocol command) and the drop log (with error-path improvements)—remain explicitly deferred to follow-up work. The author has reframed the safety guards as optional optimizations rather than correctness measures, since lazy-fetching would transparently restore dropped objects.

Current status
v5 (6/6 patches) is **technically complete and ready for integration**. **Christian Couder has reviewed v5 and stated "This version looks good to me,"** confirming all prior mechanical feedback has been addressed. **Junio C Hamano has queried whether the series should be marked for the `next` integration branch**, and the author has now **explicitly proposed the series for `next`**, signaling readiness for integration testing. The series is **proposed for `next`** and awaits Junio’s explicit merge annotation. Key changes since v4:
- **Fixed the blocking type-mismatch bug** in patch 2/6 (`list_objects_filter__filter_oidset()`): changed `unsigned long` to `size_t` to match `struct object_info.sizep`.
- **Squashed documentation** into relevant implementation patches (patches 5 and 6) as suggested by Christian Couder, improving patch atomicity.
- **Improved error messages** in patch 6/6 to list all blocked operations explicitly (merge, rebase, am, cherry-pick, revert, bisect).
- **Addressed mechanical feedback**: made the `opts` parameter in `list_objects_filter__filter_oidset()` `const`, fixed comment formatting, and standardized indentation.
- **Reduced patch count from 7 to 6** by merging documentation updates with implementation.
- **Patch 1/6 (updated)**: Focuses solely on CLI option parsing and validation, enforcing preconditions (e.g., `--filter=blob:limit`, `-a` required, promisor remote required) and rejecting incompatible options (`--filter-to`, explicit `-b`). The bitmap-handling logic now correctly distinguishes between explicit `-b` (error) and config-driven `repack.writeBitmaps` (silently disabled). Tests cover all validation scenarios, including the bitmap-handling edge case.
- **Patch 2/6 (updated)**: Introduces `list_objects_filter__filter_oidset()` in `list-objects-filter.{c,h}`, a helper that applies a filter (currently only `blob:limit`) to an existing oidset without performing an object walk. The implementation avoids lazy fetches by passing `OBJECT_INFO_SKIP_FETCH_OBJECT` when reading object info. The patch fixes the type-mismatch bug (changed `unsigned long` to `size_t`), makes the `opts` parameter `const`, and flags the duplicated `blob:limit` size check logic with a `NEEDSWORK` comment. The helper is called by `enumerate_promisor_blobs()` to produce the `to_drop` oidset.
- **Patch 3/6 (updated)**: Extends `repack_promisor_objects()` to accept an optional `to_drop` oidset parameter; when provided, the function rebuilds the promisor pack while excluding those objects. The change is minimal and backward-compatible: all existing callers pass `NULL`, so behavior is unchanged. The implementation skips objects in the `to_drop` set during promisor object enumeration (via `odb_for_each_object()` with `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`), ensuring the rebuilt promisor pack omits the dropped objects while preserving all others.
- **Patch 4/6 (updated)**: Implements `enumerate_promisor_blobs()` in `repack-filtered.c`, which walks all promisor objects (using `odb_for_each_object()` with `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`) and collects blobs into an oidset (`all_promisor_blobs`). It then applies the filter (currently only `blob:limit`) to that set using `list_objects_filter__filter_oidset()`, producing the final `to_drop` set. In `--dry-run` mode, the function prints the OIDs of candidate blobs to stdout. The implementation avoids lazy fetches by passing `OBJECT_INFO_SKIP_FETCH_OBJECT` to every object info query. The test script (`t/t7706-repack-drop-filtered.sh`) covers enumeration, `--dry-run`, and locally created blob exclusion, verifying that only promisor blobs exceeding the threshold are listed and that locally created objects are untouched.
- **Patch 5/6 (updated)**: Wires up the `to_drop` oidset—computed by `enumerate_promisor_blobs()`—to `repack_promisor_objects()`, enabling real object removal. On a real run (`--drop-filtered` without `--dry-run`), it sets `delete_redundant = 1` to remove old promisor packs containing the dropped blobs. The repack machinery’s existing write-before-delete and fsync guarantees ensure crash safety. The test script verifies that a large promisor blob is absent after `--drop-filtered` runs, while a smaller one remains. **Documentation**: Squashed most of the `--drop-filtered` description into this patch, covering preconditions, incompatibilities, implied `-d`, and silent bitmap disabling.
- **Patch 6/6 (updated)**: Adds safety guards to prevent `--drop-filtered` from running during ongoing operations (merge, rebase, am, cherry-pick, revert, bisect) or dropping blobs referenced by the current index. The implementation uses `wt_status_get_state()` to detect ongoing operations and `repo_read_index()` to validate the index, with clear error messages. Both checks are skipped for bare repositories. **Documentation**: Squashed the safety guards paragraph into this patch. **Error message improvement**: Now lists all blocked operations explicitly.

Key technical details
- **New CLI options**: `git repack --drop-filtered` (real runs) and `--dry-run` (reporting only), documented in `Documentation/git-repack.adoc` with constraints (e.g., `blob:limit` only, `-a` required, promisor remote required), incompatibilities (e.g., `--filter-to`, bitmaps), and safety optimizations (merge/rebase/cherry-pick checks, index validation). The documentation explains the implied `-d`, the rationale for silent bitmap disabling, and the `--dry-run` preview functionality.
- **New files**: `repack-filtered.c` (core logic, including `enumerate_promisor_blobs()`), `t/t7706-repack-drop-filtered.sh` (test script).
- **Modified files**: `builtin/repack.c` (option parsing, validation, conditional bypass of `write_filtered_pack()`, drop log wiring, real object removal via `repack_promisor_objects()` with `to_drop`, and safety guards via `wt_status_get_state()` and `repo_read_index()`), `list-objects-filter.{c,h}` (new `list_objects_filter__filter_oidset()` API), `repack-promisor.c` (promisor pack rebuilding with object exclusion via `to_drop` parameter, `append_drop_log()`), `repack.h` (function signature changes), `Documentation/git-repack.adoc` (man page updates for `--drop-filtered` and `--dry-run`).
- **New data structures**: `all_promisor_blobs` and `to_drop` oidsets.
- **New helper functions**: `list_objects_filter__filter_oidset()` (applies filter to an existing oidset without object traversal, avoids lazy fetches via `OBJECT_INFO_SKIP_FETCH_OBJECT`), `enumerate_promisor_blobs()` (walks promisor objects via `odb_for_each_object()` with `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`, returns drop set via output parameter `oidset *to_drop`).
- **Behavior**: `--drop-filtered` + `--filter=<spec>` (currently limited to `blob:limit=<n>`) removes promisor blobs exceeding the threshold; locally created objects are untouched. Bitmaps are silently disabled to avoid breaking pack closure. The `to_drop` set’s contents are passed to `repack_promisor_objects()` to rebuild promisor packs while excluding the dropped blobs, implying `-d` to remove old promisor packs containing the dropped objects. The repack machinery’s existing write-before-delete and fsync guarantees ensure crash safety.
- **Safety optimizations**: (1) merge/rebase/am/cherry-pick/revert/bisect checks (via `wt_status_get_state()`) prevent drops during ongoing operations, (2) index validation (via `repo_read_index()`) prevents drops of blobs referenced by the current index. Both checks are skipped for bare repositories. Error messages are specific: "Refuse to drop objects while another operation is in progress" (now listing all blocked operations explicitly) and "Bare repository: cannot drop '<path>' (<oid>): it is referenced by the current index".
- **Preconditions enforced**: `--filter` must be `blob:limit`, `-a` is required, a promisor remote must be configured, and `--filter-to` is rejected. Objects are only enumerated if marked as promisor (via `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`); lazy fetches are avoided during enumeration by passing `OBJECT_INFO_SKIP_FETCH_OBJECT` to every object info query.
- **Bitmap validation fix**: Patch 1/6 fixes a subtle validation bug where explicit `-b` usage was incorrectly allowed when `repack.writeBitmaps` was set in the config. The updated patch now correctly rejects explicit `-b` with `--drop-filtered`, while silently disabling config-driven bitmaps for the duration of the command.
- **Patch-specific details**:
  - **Patch 1/6**: Adds `--drop-filtered` and `--dry-run` options to `builtin/repack.c`, implements all validation logic (including the bitmap-handling fix distinguishing command-line vs config-driven settings), and wires up the options without performing object enumeration or deletion. Tests cover all validation scenarios, including the bitmap-handling fix.
  - **Patch 2/6**: Introduces `list_objects_filter__filter_oidset()` in `list-objects-filter.{c,h}`, a helper that applies a filter (currently only `blob:limit`) to an existing oidset without performing an object walk. The implementation duplicates the `blob:limit` size check logic from `list_objects_filter__filter_object()`, marked with a `NEEDSWORK` comment for future refactoring. The helper avoids lazy fetches by passing `OBJECT_INFO_SKIP_FETCH_OBJECT` when reading object info. **Fixed blocking bug**: changed `unsigned long size` to `size_t size` to match `struct object_info.sizep`. **Mechanical improvement**: made the `opts` parameter `const` as suggested by Christian Couder.
  - **Patch 3/6**: Extends `repack_promisor_objects()` to accept an optional `to_drop` oidset; when provided, the function rebuilds the promisor pack while excluding those objects. The change is minimal and backward-compatible: all existing callers pass `NULL`, so behavior is unchanged. The implementation skips objects in the `to_drop` set during promisor object enumeration (via `odb_for_each_object()` with `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`), ensuring the rebuilt promisor pack omits the dropped objects while preserving all others.
  - **Patch 4/6**: Implements `enumerate_promisor_blobs()` in `repack-filtered.c`, which walks all promisor objects (using `odb_for_each_object()` with `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`) and collects blobs into an oidset (`all_promisor_blobs`). It then applies the filter (currently only `blob:limit`) to that set using `list_objects_filter__filter_oidset()`, producing the final `to_drop` set. In `--dry-run` mode, the function prints the OIDs of candidate blobs to stdout. The implementation avoids lazy fetches by passing `OBJECT_INFO_SKIP_FETCH_OBJECT` to every object info query. The test script verifies enumeration, `--dry-run` output, and locally created blob exclusion.
  - **Patch 5/6 (updated)**: Wires up the `to_drop` oidset—computed by `enumerate_promisor_blobs()`—to `repack_promisor_objects()`, enabling real object removal. On a real run (`--drop-filtered` without `--dry-run`), it sets `delete_redundant = 1` to remove old promisor packs containing the dropped blobs. The repack machinery’s existing write-before-delete and fsync guarantees ensure crash safety. The test script verifies that a large promisor blob is absent after `--drop-filtered` runs, while a smaller one remains. **Documentation**: Squashed most of the `--drop-filtered` description into this patch, covering preconditions, incompatibilities, implied `-d`, and silent bitmap disabling. The patch removes the placeholder `die()` that previously blocked real runs, enabling the feature to work as intended.
  - **Patch 6/6 (updated)**: Adds safety guards to prevent `--drop-filtered` from running during ongoing operations (merge, rebase, am, cherry-pick, revert, bisect) or dropping blobs referenced by the current index. The implementation uses `wt_status_get_state()` to detect ongoing operations and `repo_read_index()` to validate the index, with clear error messages. Both checks are skipped for bare repositories. **Documentation**: Squashed the safety guards paragraph into this patch. **Error message improvement**: Now lists all blocked operations explicitly (merge, rebase, am, cherry-pick, revert, bisect), addressing Christian Couder’s feedback.

Open questions and loose ends
1. **Integration branch**: The series is **proposed for `next`** and awaits Junio’s explicit merge annotation. The author has confirmed readiness for integration testing.
2. **Reliability of `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`**: Junio seeks confirmation that promisor-object enumeration is robust in practice. Siddharth Shrimali and Siddharth Asthana trust the mechanism, but real-world validation from other partial-clone users would be valuable.
3. **Repack machinery behavior**: Junio confirms the rebuilt promisor pack must be constructed from locally available data only, without triggering lazy fetches. The current implementation performs two separate promisor-object walks (one for enumeration, one for repacking), but this could be optimized in the future.
4. **Filter scope**: Currently limited to `blob:limit`; future extensions may include other filter types (e.g., `tree:0`), object age, path, or reachability.
5. **Code duplication**: `list_objects_filter__filter_oidset()` duplicates the `blob:limit` size check logic from `list_objects_filter__filter_object()`. This is flagged with a `NEEDSWORK` comment and noted as technical debt.
6. **Timestamp granularity**: Promisor objects are always stored in packs (never loose), so per-object timestamps are not natively available. The current best proxy is the mtime of the promisor pack itself, which loses granularity after repacking consolidates multiple packs. A mechanism similar to cruft pack `.mtimes` files would be necessary for the time-based heuristic (deferred to follow-up).
7. **Implied `-d`**: The `--drop-filtered` option implies `-d` to remove old promisor packs containing dropped blobs. This is documented in the code comment in `builtin/repack.c` but could be made explicit in the user-facing documentation if reviewers prefer.
8. **Follow-up work confirmed**: The three deferred items—remote verification (via the pending `remote-object-info` protocol command), recency-based culling (time-based heuristic), and the drop log (with error-path improvements)—remain explicitly out of scope for this series.

Participant positions
- **Siddharth Shrimali (author)**: v5 is now posted, addressing all feedback from v4, including the blocking type-mismatch bug, documentation squashing, error message improvements, and mechanical cleanups. The series is **ready for maintainer review and integration testing**, with no known blocking issues. The author has **explicitly proposed the series for `next`**, confirming intent to perform full test suite runs and trial merges. **Latest updates**: (1) Fixed the type-mismatch bug in patch 2/6, (2) squashed documentation into patches 5 and 6, (3) improved error messages in patch 6/6 to list all blocked operations explicitly, (4) addressed all mechanical feedback from Christian Couder, (5) Patch 1/6 now focuses solely on CLI option parsing and validation, with thorough test coverage for all preconditions and the bitmap-handling edge case, (6) Patch 2/6 introduces `list_objects_filter__filter_oidset()` with `const` correctness, type safety, and a `NEEDSWORK` comment for the duplicated `blob:limit` logic, (7) Patch 4/6 implements `enumerate_promisor_blobs()` with robust promisor-object enumeration, `--dry-run` support, and test coverage for locally created blob exclusion, (8) Patch 5/6 wires up the `to_drop` oidset to `repack_promisor_objects()`, enabling real object removal with crash safety guarantees and test coverage for blob removal, (9) Patch 6/6 completes the series with safety guards for ongoing operations and index validation, including explicit error messages for all blocked operations.
- **Junio C Hamano (maintainer)**: Substantive review of v1-v3 identified blocking issues (e.g., bitmap validation flaw, type-mismatch bug) and raised technical questions about promisor-object enumeration and repack machinery behavior. The latest feedback (v3) identified the type-mismatch bug, which is now fixed in v5. **New action**: Queried whether the series should be marked for `next`; the author has now **explicitly proposed the series for `next`**. No new technical concerns raised; the query is procedural.
- **Siddharth Asthana (reviewer)**: Substantive review of v1 and v2 endorsed the core approach, the promisor-object enumeration mechanism, and the safety guards (now reframed as UX optimizations). Suggested softer documentation language to acknowledge remote availability risks. No new feedback on v3-v5.
- **Christian Couder**: Surface-level review of v1 endorsed the CLI design. **Feedback on v4 fully addressed in v5**:
  - **Patch 2/6**: Made the `opts` parameter in `list_objects_filter__filter_oidset()` `const`.
  - **Patch 4/6**: Addressed comment formatting and punctuation in `enumerate_promisor_blobs()` and the test script.
  - **Patch 6/6**: Addressed comment capitalization, punctuation, and grammar; improved error messages to list all blocked operations explicitly (merge, rebase, am, cherry-pick, revert, bisect).
  - **Documentation**: Squashed most of the `--drop-filtered` description into patch 5/6 and moved the safety guards paragraph to patch 6/6.
  - **Conclusion**: "This version looks good to me," signaling mechanical readiness for maintainer review and integration testing.

Related work
- **Partial clone**: builds on existing partial clone infrastructure (promisor packs, `--filter`).
- **Repack machinery**: leverages existing repack crash safety and promisor pack rebuilding logic.
- **Drop log**: introduces a new mechanism analogous to the existing promisor object log but for removed objects (deferred to follow-up).
- **Remote-object-info protocol command**: a pending feature that would enable client-side remote object availability checks, relevant for both the drop log and remote verification (deferred to follow-up).
- **Cruft packs**: Git’s existing `.mtimes` files for cruft packs serve as precedent for tracking per-object timestamps, which would be necessary for the time-based heuristic (deferred to follow-up).