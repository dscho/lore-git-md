Existing AI thread summary: (none -- this is the thread root)

---
**Thread identity**
Subject: [PATCH 0/5] Reintroduce `writev(3p)` support for Git's I/O layer
Series version: v1 (initial)
Originating author: Patrick Steinhardt
Thread type: patch series

**Core problem or goal**
Reintroduce portable `writev(3p)` scatter/gather I/O support to Git’s low-level I/O layer, providing generic wrapper functions that mirror the existing `write_in_full()` and `write_or_die()` helpers. The goal is to enable future patches to replace hand-rolled `writev()` loops with a uniform, robust API that handles partial writes, `EINTR`/`EAGAIN` retries, and short-write detection.

**Current status**
Under review; v1 posted. No prior versions.

**Key technical details**
- New symbols: `writev_in_full()` and `writev_or_die()` in `wrapper.[ch]` and `write-or-die.[ch]`
- Files touched: `wrapper.c`, `wrapper.h`, `write-or-die.c`, `write-or-die.h`
- Behavior: mirrors `write_in_full()`/`write_or_die()`; modifies passed `iovec` array to reflect remaining data
- No callers introduced; plumbing only

**Open questions and loose ends**
- None raised yet; awaiting review feedback

**Participant positions**
- Patrick Steinhardt: author

**Related work**
None mentioned.