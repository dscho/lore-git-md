This is a follow-up question in the already-merged `gitk` translation and build-system update thread. Harald Nordgren, the author of the Makefile tweak that silences `make -s` output, asks whether these changes eventually land in mainline Git under `gitk-git/Makefile` or how the process works.

The question is purely procedural: Harald is clarifying how `gitk` updates flow into the main Git repository. Since the thread context confirms the series was already merged to `master` by Junio C Hamano, the answer is straightforward—these changes are now part of the main Git codebase and will appear in the next release. No further action is needed; this is just a request for understanding the contribution pipeline.