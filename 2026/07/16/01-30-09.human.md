This is the final reply in the long-running GSoC series that implements `git cat-file --batch-command` support for querying remote object metadata. The series is now at v18 and queued for `next`, with all technical issues resolved except this last design question.

Junio pushes back on Pablo’s proposal to mark the `%(objectname)`-only case as `NEEDSWORK`. Pablo had suggested forcing a "size" request (discarding the result) to validate the object’s existence on the server, but deferring a cleaner solution to a future series. Junio rejects this as deliberately shipping incorrect behavior: if the server doesn’t validate the OID when only `%(objectname)` is requested, the client could return a name for a non-existent object, which is a correctness bug.

The disagreement is narrow but fundamental. Pablo’s approach prioritizes minimal change and efficiency (avoiding unnecessary server round-trips), while Junio insists on robustness (ensuring the server always validates the OID). The discussion is substantive—it affects the protocol’s reliability—but the scope is limited to this single edge case. No other parts of the series are in question.

This exchange is the last open item before the series graduates to `next`. The outcome will likely be Pablo implementing Junio’s preference (always validate the OID, even for `%(objectname)`-only requests) in a final reroll. The series is otherwise complete, with CI passing and all prior feedback incorporated.