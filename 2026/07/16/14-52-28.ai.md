This is a minor follow-up in the `git cat-file --batch-command` remote-object-info series (v18), specifically addressing surface-level feedback on patch 5/13. The patch removes a static `advertise_sid` variable in `fetch-pack.c` to enable code reuse in later patches, and the discussion here clarifies the commit message and confirms the variable's usage scope.

Karthik Nayak’s review raised two points: (1) the commit message inaccurately described `fetch_pack_config()` as a usage site (it sets, not uses, the variable), and (2) whether `find_common()` is truly v0/v1-only. Pablo Sabater acknowledges the first point and corrects the commit message to reflect that `advertise_sid` is *used* in `write_fetch_command_and_capabilities()` and `find_common()`. For the second, he confirms `find_common()` is only called in the v0/v1 code path (via `do_fetch_pack()`), aligning with the patch’s intent to restrict the session-id check to those protocols. The changes are mechanical and preserve existing behavior, resolving a refactoring blocker for later patches.

This exchange is purely cosmetic—no functional issues were identified or fixed. The series remains technically complete and queued for `next`, with this patch serving as a prerequisite for relocating `write_fetch_command_and_capabilities()` to `connect.c`. The resolution is straightforward and unlikely to impact the broader series.