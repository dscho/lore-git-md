This is the first patch in a three-part series removing `the_repository` dependency from `refspec.c`, part of the ongoing `the_repository` removal effort. It reorganizes `refspec.h` to group related structures and functions more logically, preparing the ground for the subsequent patches that will eliminate implicit repository access. The patch also fixes minor style violations (indentation, alignment) in the header file.

The changes are purely mechanical: no new functionality is introduced, and no behavior is altered. The patch moves function declarations closer to their associated structures (`refspec_item` and `refspec`) and reorders some `#define` macros to match. The only substantive change is the reordering of `refspec_clear()` to appear with the other `refspec` functions rather than after unrelated declarations.

This patch is a low-risk preparatory step. The risk of regression is minimal, as it only affects the organization of declarations in a header file. However, it is a necessary precursor to the more invasive changes in patches 2 and 3, which will modify function signatures and caller logic. Reviewers will likely focus on ensuring the reorganization is complete and that no declarations are misplaced or duplicated.

Files touched: `refspec.h` (only). No new config keys, command-line options, or test numbers are introduced. The patch is small and self-contained, making it easy to review in isolation. The commit message is clear and typical for Patrick Steinhardt’s work, framing the change as a preparatory step for the broader refactoring.