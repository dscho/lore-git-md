This is a bugfix patch targeting the wrapper subsystem, part of a five-patch series reintroducing `writev(3p)` support. It addresses a platform-specific issue on systems like NonStop where `MAX_IO_SIZE` is unusually small, causing `writev()` calls to fail with `EINVAL` when individual `iovec` entries exceed the limit.

The patch introduces a new `xwritev()` function that mirrors the behavior of `xwrite()` by splitting overly large `iovec` requests into smaller chunks that fit within `MAX_IO_SIZE`. If the first `iovec` entry itself exceeds the limit, it delegates to `xwrite()` instead. The function also handles `EINTR` and `EAGAIN` retries, just like `xwrite()`. The existing `writev_in_full()` is updated to use this new wrapper, making its retry logic consistent with `write_in_full()`.

The motivation is clear: NonStop users reported failures due to the unhandled `MAX_IO_SIZE` constraint, and this patch ensures Git behaves correctly on such platforms. The commit message is thorough, explaining the edge cases and design choices, and the code changes are minimal and focused. The patch looks well-crafted and addresses a real problem without introducing unnecessary complexity. If you care about platform compatibility or the wrapper layer, this is worth reviewing.