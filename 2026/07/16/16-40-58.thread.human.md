This thread is a user support request about a platform-specific issue in Git Bash. Moksh Goyal reports that pressing Ctrl+C in a Vagrant SSH session terminates the entire session when using Git Bash, but not when using PowerShell. The problem appears to be a shell or terminal emulator quirk rather than a Git bug, and the email lacks technical details (Git version, Vagrant version, or VM environment) that would help diagnose it.

Since this is the first message in the thread, there’s no prior discussion or proposed fix. The mailing list may redirect the user to a more appropriate forum, such as Git for Windows or Vagrant support channels, or experienced Windows contributors might suggest a known configuration tweak. For now, this remains a user-reported annoyance rather than a development topic, and it’s unlikely to generate deeper technical discussion on the list.