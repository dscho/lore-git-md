Thread identity
Subject: What's cooking in git.git (Jul 2026/#07)
Series version: N/A (integration report, not a patch series)
Originating author: Junio C Hamano
Thread type: integration status report

Core problem or goal
Junio’s monthly "What’s cooking" report provides a snapshot of all in-flight topics in the Git integration branches (`master`, `next`, `seen`) as of July 2026. It tracks which topics have graduated, which are cooking, which are stalled, and which have been discarded, along with brief descriptions and discussion links.

Current status
Graduated to `master`: nine topics, including `ps/history-drop` (new `git history drop` subcommand), `ps/refs-writing-subcommands` (new `git refs create/delete/update/rename`), `rs/blame-abbrev-marks` (optimized blame output alignment), and `jk/bloom-leak-fixes` (memory leaks in Bloom-filter code paths).
New in `next`/`seen`: 15 topics, including `js/coverity-unchecked-returns-fix`, `ps/odb-for-each-object-filter` (ODB enumeration with filters), `ps/refs-wo-the-repository` (removing `the_repository` from refs API), and `hn/history-squash` (new `git history squash` subcommand).
Stalled: three topics (`kh/doc-trailers`, `sn/rebase-update-refs-symrefs`, `jt/config-lock-timeout`) awaiting responses.
Discarded: one topic (`kk/prio-queue-cascade-sift`) retracted due to performance concerns.

Key technical details
- **New commands/subcommands**: `git history drop`, `git history squash`, `git refs create/delete/update/rename`, `git branch --delete-merged`.
- **Subsystems touched**: ODB (object database), refs API, Bloom filters, sparse-index, status enumeration, merge machinery, submodule fetch, CI infrastructure, Rust build system.
- **Behavior changes**: `sc/wt-status-avoid-quadratic-insertion` (O(n log n) status enumeration), `kk/no-walk-pathspec-fix` (regression fix for `--no-walk` pathspec filtering), `ds/sparse-index-ita-crash` (sparse-index crash fix).
- **On-disk format**: No changes reported.
- **Test coverage**: Not explicitly mentioned, but performance and correctness fixes imply regression test updates.
- **New symbols**: `git history drop`, `git history squash`, `git refs create/delete/update/rename`, `git branch --delete-merged`, `odb_for_each_object_filter()`, refs API functions without `the_repository`.

Open questions and loose ends
- **ODB abstraction**: `ps/odb-for-each-object-filter`’s use of reachability bitmaps may be controversial.
- **New features**: `hn/history-squash`’s message template handling vs. `git rebase --squash`; `hn/branch-delete-merged`’s safety checks.
- **Performance**: `kk/merge-base-exhaustion`’s early termination logic may introduce edge cases.
- **Security**: `pz/fetch-submodule-errors-config`’s dynamic allow-list for format placeholders in `ps/cat-file-remote-object-info`.
- **Stalled topics**: `kh/doc-trailers`, `sn/rebase-update-refs-symrefs`, and `jt/config-lock-timeout` remain unresolved.

Participant positions
- **Junio C Hamano**: Maintainer report; positions are implicit in branch assignments (e.g., topics in `next` are tentatively approved, stalled topics are blocked on responses).
- **Patrick Steinhardt (ps/*)**: Driving ODB abstraction (`ps/odb-for-each-object-filter`, `ps/odb-drop-whence`) and `the_repository` removal (`ps/refs-wo-the-repository`).
- **Johannes Schindelin (js/*)**: Coverity warning fixes (`js/coverity-unchecked-returns-fix`) and CI infrastructure (`js/ci-dockerized-pid-limit`).
- **René Scharfe (rs/*)**: Blame output optimization (`rs/blame-abbrev-marks`).
- **Jeff King (jk/*)**: Bloom filter memory leak fixes (`jk/bloom-leak-fixes`).
- **Hannes Näher (hn/*)**: New `git history` subcommands (`hn/history-squash`, `hn/history-drop`) and `git branch --delete-merged`.
- **Derrick Stolee (ds/*)**: Sparse-index crash fix (`ds/sparse-index-ita-crash`).
- **SZEDER Gábor (sc/*)**: Status enumeration performance fix (`sc/wt-status-avoid-quadratic-insertion`).
- **Karthik K (kk/*)**: `--no-walk` pathspec regression fix (`kk/no-walk-pathspec-fix`) and retracted `prio-queue-cascade-sift`.
- **Kristoffer Haugsbakk (kh/*)**: Stalled documentation topic (`kh/doc-trailers`).
- **Siddharth Naithani (sn/*)**: Stalled rebase topic (`sn/rebase-update-refs-symrefs`) and macOS Rust build fix (`sn/osxkeychain-rust-universal`).
- **Jonathan Tan (jt/*)**: Stalled config topic (`jt/config-lock-timeout`).
- **Paul Zusman (pz/*)**: Submodule fetch error handling (`pz/fetch-submodule-errors-config`).

Related work
- **ODB abstraction**: Follows prior work (`ps/odb-*` series) and `the_repository` removal efforts.
- **`git history` subcommands**: Extends the `git history` command introduced in earlier cycles.
- **Rust build**: `sn/osxkeychain-rust-universal` builds on ongoing Rustification efforts.
- **Documentation**: `jc/relnotes-2.55-rust-fix` retroactively clarifies Rust-related release notes.