This is a **refactoring discussion** in the `remote-curl` transport code, continuing the design-level conversation about interface improvements. Junio C Hamano weighs in on the ongoing debate between René Scharfe and Patrick Steinhardt about whether to pass a `const struct strvec *` to protocol handlers (`push_dav` and `push_git`) or stick with the current `const char **` approach.

Junio endorses Patrick’s suggestion that introducing a `strvec_pushvec()` helper could be a worthwhile independent effort, separate from René’s current patch. He notes that such a helper would make it easier to nudge contributors toward safer, length-limited vector usage (as opposed to NULL-terminated arrays, which can fail catastrophically if unterminated). Junio also points out that identifying conversion candidates for `strvec_pushvec()` would be straightforward using `git grep` or even Coccinelle, implying this could be a low-effort, high-value follow-up.

The email does not propose new code or alter the current patch’s direction. Instead, it reinforces the idea that the `strvec_pushvec()` discussion is a forward-looking API design question rather than a blocker for René’s patch. The thread remains focused on balancing simplicity (René’s preference) with interface clarity and future-proofing (Patrick’s and now Junio’s preference). No merge status is reported; the series is still under review. Key technical details from earlier in the thread—such as the patch’s confinement to `remote-curl.c` and its lack of observable behavior changes—remain unchanged.