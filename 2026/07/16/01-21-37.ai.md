This is the sixth patch in a seven-patch GSoC series adding new path-related keys to the `git repo info` command. The patch introduces `path.grafts.absolute` and `path.grafts.relative` keys, which expose the filesystem location of the repository’s grafts file in both absolute and relative formats. The implementation respects the `GIT_GRAFT_FILE` environment variable override, ensuring consistency with Git’s existing behavior for grafts file resolution.

The patch touches `builtin/repo.c` (adding the new keys and their formatting logic), `Documentation/git-repo.adoc` (documenting the keys and their behavior), and `t/t1900-repo-info.sh` (testing the new functionality). The tests verify both the standard grafts file location (`.git/info/grafts`) and the behavior under a `GIT_GRAFT_FILE` override, covering the core use cases for scripting layers that need to locate the grafts file.

This addition follows the established pattern of the series: each key mirrors an internal Git struct field (here, `repo_get_graft_file()`) and provides both absolute and relative path formatting via `format_path()`. The grafts file is a niche but important feature for history rewriting, and exposing its location via `git repo info` aligns with the series’ goal of making Git’s plumbing more accessible without requiring direct access to internals. The patch is self-contained and unlikely to be controversial, though reviewers may suggest minor adjustments to the key name (`grafts` vs. `graft-file`) for consistency with other Git terminology.

For future AI summarization, note the new keys (`path.grafts.absolute` and `path.grafts.relative`), the use of `repo_get_graft_file()` and `format_path()`, and the test coverage for environment variable overrides. The series remains on track for its GSoC goal, with only one patch (`path.git-prefix`) remaining. No substantive review feedback has been posted yet.