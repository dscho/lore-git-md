This is a `git-gui` pull request that has been merged to `master`. Junio Hamano pulled Johannes Sixt’s three small, independent changes into the main Git repository. The updates include a complete Bulgarian translation (562 strings), a wider commit-message text field in the GUI, and a quieter `make -s` build by silencing `msgfmt --statistics` output. The changes are purely cosmetic, touching only the `git-gui` subtree (`Makefile`, `lib/option.tcl`, and `po/bg.po`), and do not affect core Git functionality or tests. All three changes were already integrated into Sixt’s `git-gui` master branch before being submitted.