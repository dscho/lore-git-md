This is a **refactoring patch discussion** in the `remote-curl` transport code, continuing the thread about simplifying how push specifications are passed to HTTP transport handlers. The focus is now on a design-level question: whether to pass a `const struct strvec *` directly to the protocol handlers (`push_dav` and `push_git`) instead of a raw `const char **`.

René Scharfe’s original patch removed redundant tracking of the push specification array count (`nr_spec`), relying instead on the NULL-termination guarantee of `strvec` arrays. Patrick Steinhardt’s feedback suggests that passing the entire `strvec` struct would make the interface more self-documenting and safer, as it explicitly communicates the NULL-termination guarantee. René acknowledges the clarity benefits but notes that the simplicity of `strvec_pushv()` (which operates on NULL-terminated arrays) influenced his original choice.

The conversation also explores the idea of introducing a `strvec_pushvec()` function, which could optimize bulk appends by avoiding repeated reallocations. While neither participant expects measurable performance gains in this case, Patrick argues that having such a function could encourage safer usage patterns over time—subtly nudging contributors toward length-limited vectors instead of unterminated arrays. René agrees that this could be a useful direction, though it’s not a blocker for the current patch.

The patch itself remains a low-risk refactoring with no behavior change, confined to `remote-curl.c`. No test failures or regressions have been reported, and the discussion is now centered on design preferences rather than correctness. René hasn’t committed to a v2, but if he adopts Patrick’s suggestion, the next iteration would likely pass a `strvec *` to make the interface more explicit. For now, the thread reflects a thoughtful but unresolved debate about balancing simplicity and clarity in Git’s internal APIs.