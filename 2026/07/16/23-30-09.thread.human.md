Here’s a concise human-readable summary of the current state of the Git mailing list thread based on Junio’s "What’s cooking" report for July 2026:

This thread is Junio’s regular integration report, giving a snapshot of all active topics in Git’s development branches. Since the last report, nine topics have graduated to `master`, including new subcommands like `git history drop` and `git refs create/delete/update/rename`, as well as fixes for memory leaks in Bloom-filter code and performance improvements for `git status`. The `ps/refs-writing-subcommands` series, which adds plumbing-level ref management tools, is now part of the stable codebase.

Fifteen new topics are now cooking in `next` or `seen`, with a strong focus on refactoring and new features. Patrick Steinhardt’s ongoing ODB abstraction work continues with `ps/odb-for-each-object-filter` (adding filtered object enumeration) and `ps/refs-wo-the-repository` (removing global state from the refs API). New user-facing commands include `git history squash` (Hannes Näher) and `git branch --delete-merged` (Hannes Näher), while `pz/fetch-submodule-errors-config` adds configuration for submodule fetch error handling. Performance and correctness fixes are also prominent, such as `sc/wt-status-avoid-quadratic-insertion` (optimizing status enumeration) and `ds/sparse-index-ita-crash` (fixing a crash in sparse-index handling).

Three topics remain stalled, awaiting responses from their authors. The report also notes discarded topics, including `kk/prio-queue-cascade-sift`, which was retracted due to performance concerns. Build and CI improvements, such as `js/ci-dockerized-pid-limit` (fixing resource limits in CI), and documentation updates (e.g., clarifying Rust-related changes in release notes) round out the activity. The report sets the stage for follow-up discussions on potential controversies, like the use of reachability bitmaps in `ps/odb-for-each-object-filter` or edge cases in `kk/merge-base-exhaustion`.