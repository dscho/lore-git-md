This is a surface-level review comment on the GSoC v18 series' tenth patch, which implements client-side support for the `object-info` protocol v2 capability. Junio points out a minor but important build-system nit: the new `fetch-object-info.o` entry in the `LIB_OBJS` list in `Makefile` is inserted out of alphabetical order, breaking the established convention that the list be kept sorted. The entry appears between `fetch-pack.o` and `fmt-merge-msg.o`, violating the `o < p` ordering rule.

The comment is purely mechanical and does not engage with the patch's technical substance, which has already been thoroughly reviewed and is queued for `next`. This is a textbook example of a surface-level review: it addresses a cosmetic issue (alphabetical ordering in a build file) without questioning or discussing the code's behavior, design, or correctness. The fix is trivial and will not affect functionality, but it is necessary for consistency with the project's coding guidelines.