This is a refactoring and documentation cleanup series for `git fast-import`, consisting of seven patches from Christian Couder. The primary goal is to standardize the command's usage string and align it with its SYNOPSIS documentation, while also modernizing the codebase by reducing global variables and improving the parse-options API.

The series introduces a new `OPT_HIDDEN_GROUP` macro (patch 1/7) to better organize hidden options, updates the parse-options API documentation (patches 2/7 and 3/7), and cleans up minor code issues like loop variable scoping (patch 4/7). Patches 5/7 and 6/7 begin libifying `builtin/fast-import.c` by introducing a `struct fast_import_state` to encapsulate global variables, though many globals remain for future work. The final patch (7/7) rewrites the usage string using `struct option`, ensuring consistency with the SYNOPSIS and removing the command from the `t0450/adoc-help-mismatches` test.

Key technical details include the new `OPT_HIDDEN_GROUP` macro, the `struct fast_import_state` for state management, and the parse-options API changes. The series touches `parse-options.h`, `parse-options.c`, `builtin/fast-import.c`, and related documentation. CI tests have passed, indicating no immediate regressions. The work appears well-structured and incremental, though the libification effort is explicitly marked as incomplete. This is a solid refactoring series with clear motivation and no behavior changes.