Thread identity
Subject: [PATCH 0/3] bisect: add --reset-when-found option
Series version: v6 (two-patch series)
Originating author: Harald Nordgren (via GitGitGadget)
Thread type: patch series (feature addition)

Core problem or goal
Introduce a `--reset-when-found[=<where>]` option to `git bisect` that automatically resets the working tree to a user-specified commit after the bisect identifies the first bad commit. The parameter `<where>` accepts `original` (reset to the pre-bisect commit, the default) or `found` (leave the working tree on the first bad commit). The feature targets automated workflows (scripts, CI pipelines) and interactive debugging by eliminating the need for an explicit `git bisect reset`.

Current status
The v6 patch series is technically complete and implements all maintainer feedback, including Junio C Hamano’s final internal clean-ups and review comments. The series is now a two-patch set: (1) the preparatory plumbing change (adding a `quiet` parameter to `bisect_reset()`), and (2) the core feature implementation with the refactored state machine, documentation, and test updates. The auto-reset logic is centralized in `cmd_bisect()`, which reads `refs/bisect/bad` directly before reset to eliminate the file-scope static variable `first_bad_oid`. The `reset_when_found` variable is initialized to a sentinel value (`RESET_WHEN_FOUND_NONE`) to remove the separate boolean flag `reset_when_found_arg_seen`. Junio’s final review confirmed the patch is ready for integration into `next`; the author has addressed the actionable feedback (removing two style clean-up hunks) and left one minor stylistic choice unresolved. Junio’s latest reply reinforces that the current implementation is a sound foundation for future extensibility, but no further changes are required or expected. The series remains ready for integration into `next`.

Key technical details
- Files touched: `bisect.c`, `builtin/bisect.c`, `bisect.h`, `Documentation/git-bisect.adoc`, `t/t6030-bisect-porcelain.sh`.
- New CLI option: `--reset-when-found[=<where>]` (parameterized; values `original` or `found`; default `original`). The boolean form (`--auto-reset`) is deprecated.
- Behavior: After `git bisect` identifies the first bad commit, the working tree is automatically reset to the commit specified by `<where>`. The default (`original`) preserves the original pre-bisect state; `found` leaves the working tree on the first bad commit. For `git bisect run`, the reset is deferred until after the captured output is printed and the `BISECT_RUN` file is closed, addressing platform-compatibility concerns about unlinking open files.
- Internal changes: The preparatory patch (1/2) adds a `quiet` parameter to `bisect_reset()`, allowing the checkout step to suppress progress and branch-status output. The parameter is a `bool`, and existing callers (`bisect_replay()` and `cmd_bisect__reset()`) are updated to pass `false` to preserve current behavior. `bisect_reset()` no longer calls `bisect_clean_state()`, so callers must now explicitly invoke it. The `defer_reset` parameter has been eliminated. The `quiet` parameter in `bisect_reset()` remains an `int` (not converted to `bool`), as the suggested type change was not adopted. The auto-reset logic is centralized in `cmd_bisect()`, which reads `refs/bisect/bad` directly before reset to eliminate the file-scope static variable `first_bad_oid`. The `reset_when_found` variable is initialized to `RESET_WHEN_FOUND_NONE` to remove the separate boolean flag `reset_when_found_arg_seen`. The `BISECT_RESET_WHEN_FOUND` state file stores the target commit (`original` or `found`). If `<where>` is `found`, the reset step is omitted entirely. The option is wired into both `git bisect start` and `git bisect run`.
- Constraints: `--reset-when-found` cannot be used with `--no-checkout`, as that mode prevents checking out either target. The error message for this conflict uses Git’s standard dual-option template.
- Test coverage: Four new tests in `t/t6030-bisect-porcelain.sh` validate auto-reset behavior for both `original` and `found`, state cleanup, and edge cases (invalid targets, `--no-checkout` conflicts, interrupted bisects). Shell safety issues in test helpers have been fixed by assigning `git rev-parse` output to variables before passing them to `test_path_is_file`/`test_path_is_missing`.
- No on-disk format changes or new symbols introduced beyond the state file updates.

Open questions and loose ends
None. All maintainer feedback has been addressed, including Junio’s final review comments. The unresolved points (a debatable `else` clause and the question of whether other "dead end" bisect states should trigger auto-reset) are non-blocking and reflect Junio’s personal preferences rather than requirements. Junio’s latest reply explicitly notes that the current implementation is a good foundation for future work, but no changes are needed. The series is ready for integration into `next`.

Participant positions
- Harald Nordgren (author): Implemented all maintainer feedback, including Junio’s final internal clean-ups (reading `refs/bisect/bad` directly before reset and using a sentinel value for `reset_when_found`). Addressed the actionable feedback (removing two style clean-up hunks) and left one minor stylistic choice unresolved. The series is ready for integration into `next`.
- Junio C Hamano (maintainer): Provided final review, confirming the patch is technically sound and ready for `next`. Noted minor style distractions (e.g., reflowing function signatures) and a debatable `else` clause, but these are non-blocking and do not require changes. His latest reply gently reinforces the value of future extensibility but does not insist on any modifications. No further feedback or objections.
- Johannes Sixt (reviewer): Provided surface-level feedback on earlier versions (error message boilerplate, shell safety in tests, test cleanup chaining), all of which has been addressed. His earlier suggestion to rename the option to `--reset-when-found` (accepted in v2) remains in place.

Related work
None. The series is self-contained and does not depend on or conflict with other in-flight topics. The refactoring is internal and does not introduce new dependencies.