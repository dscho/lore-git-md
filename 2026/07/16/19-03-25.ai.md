This is a follow-up question in the already-merged `gitk` translation and build-system thread. Harald Nordgren, the author of the Makefile tweak, asks whether the changes he contributed (silencing `make -s` output) will propagate into mainline Git under `gitk-git/Makefile` or how the process works.

The thread context confirms the series is already merged to `master` and will appear in the next Git release. The question is purely about the mechanics of how `gitk` updates are integrated into the main Git repository. No code changes or further discussion are proposed; this is a clarification request from a contributor unfamiliar with the `gitk` maintenance workflow.