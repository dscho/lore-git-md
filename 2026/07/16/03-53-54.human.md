This is a **substantive discussion** in the ongoing `git replay --linearize` thread, where Elijah Newren pushes back against Junio C Hamano’s defense of the current multi-branch behavior. The email sharpens the debate by reframing the issue as a **design conflict between two intended features**—updating multiple branches at once and linearizing merges—rather than a user error.

Elijah argues that passing multiple branches to `git replay` is not a request for concatenation but for Git’s core "update many branches at once" functionality. Adding `--linearize` should only flatten merges within those branches, not weld unrelated branches together. He provides a concrete example: replaying three branches (`branchA`, `branchB`, `branchC`) onto `main` with `--linearize` should preserve their independence while flattening any merges within them, not produce a single linear chain. The current behavior, he contends, introduces an unintended third behavior (concatenation) that neither feature implies.

The email also clarifies that Elijah’s proposed solutions—either restricting `--linearize` to a single positive ref or implementing per-branch linearization—do not reintroduce the earlier regression (dropped commits). His preference remains the "minimal fix" (single-ref restriction) as an interim step to unblock the series, with per-branch linearization as a future enhancement.

Junio’s earlier syntax proposals (tracking replayed commits across invocations or limiting the command-line syntax) are not directly addressed here, but Elijah’s example underscores why those solutions might not resolve the core issue: users expect branches to remain independent unless explicitly merged. The discussion now hinges on whether the current behavior is a bug (Elijah’s view) or a feature (Junio’s view), with Elijah’s argument grounded in **usability and predictability** rather than technical correctness. The ball remains in Toon Claes’s court to respond, but this email strengthens the case for restricting `--linearize` to a single branch.