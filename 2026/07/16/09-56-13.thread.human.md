This thread is about a refactoring patch from Patrick Steinhardt that updates two utility functions in `copy.c`—`copy_file()` and `copy_file_with_time()`—to accept an explicit `struct repository *` parameter instead of implicitly using the global `the_repository`. The change is part of the ongoing effort to remove `the_repository` from Git’s codebase and touches ten call sites across various subsystems.

Phillip Wood’s review identified a correctness issue in the patch: in `sequencer.c`, the updated calls still pass `the_repository` to `copy_file()`, even though the destination path is derived from a local `struct repository *r`. This mismatch could lead to subtle bugs, such as incorrect file permissions or repository context inconsistencies, particularly in worktree or sequencer operations. Phillip’s feedback is substantive and actionable, pointing to specific lines and comparing the issue to other parts of the patch where the correct local repository instance is already used. The patch remains under review, with no prior versions or merge status recorded. No other reviews have been posted yet.