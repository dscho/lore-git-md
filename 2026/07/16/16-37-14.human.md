This is a surface-level review comment on the GSoC v18 series' 10th patch, which implements client-side support for the `object-info` protocol capability. Junio points out a minor but important build-system nit: the new `fetch-object-info.o` entry in the `LIB_OBJS` list is inserted out of alphabetical order, breaking the established convention. The correct placement should be after `fetch-pack.o` (which sorts before "o") and before `fmt-merge-msg.o` (which sorts after "o").

The comment is purely mechanical and does not engage with the patch's technical substance, which has already been thoroughly reviewed and is queued for `next`. This is a routine housekeeping observation that will likely be addressed in a final polish before the series graduates to `master`. The fix is trivial and does not affect functionality or design.