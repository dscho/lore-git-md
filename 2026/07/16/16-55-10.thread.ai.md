Mode: ai

Thread identity
Subject: [PATCH 0/7] fast-import: refactor usage string and libify
Series version: v2 (12 patches)
Originating author: Christian Couder <christian.couder@gmail.com>
Thread type: patch series

Core problem or goal
Refactor `git fast-import` to standardize its usage string with its SYNOPSIS documentation, reduce reliance on global variables, and modernize the parse-options API. The series introduces infrastructure (a new `OPT_HIDDEN_GROUP` macro and a `struct fast_import_state`) and documents existing parse-options flags to support future libification work. The ultimate goal is to fully parse command-line arguments via the parse-options API, eliminating remaining global variables and enabling libification.

Current status
Under review; v2 posted addressing Junio C Hamano's feedback on v1. The v2 series completes the parse-options integration, eliminating all placeholder local variables and redundant code. The behavior-changing patch (11/12) switches the command-line parser from a hand-written loop to `parse_options()`, removing support for five undocumented options (`--alias`, `--get-mark`, `--ls`, `--notes`, and `--cat-blob`) on the command line, as these are only meaningful inside the input stream. A new test (`t9300-fast-import.sh`) verifies that each of these options now fails when given on the command line. Other user-visible changes include support for abbreviated options (e.g., `--dep=5` for `--depth=5`), space-separated values for value-taking options (e.g., `--depth 5` in addition to `--depth=5`), and minor error message adjustments. The final patch (12/12) removes the redundant `from_stream` argument from `parse_one_feature()` and its callers, simplifying the code after the earlier patches eliminated the only non-stream call site. The codebase shrinks by 38 lines overall. CI tests pass except for a likely unrelated Windows+Meson failure. No merge status reported; series remains under review.

Elijah Newren's review of patch 11/12 identifies three behavioral changes introduced by the switch to `parse_options()`:
1. A bare `--` (or trailing `--`) is now accepted as the end-of-options marker, aligning with conventional `parse_options()` behavior.
2. Value-taking options now accept space-separated arguments in addition to the `--opt=value` form.
3. The early scan for `--allow-unsafe-features` (which predates `parse_options()` and is needed to process in-stream "feature" lines) is stricter than `parse_options()` itself: it only matches the exact spelling and stops at the first non-option token, while `parse_options()` accepts abbreviations (e.g., `--allow-unsafe`) and space-separated values. This can cause the in-stream feature to be rejected even though the command-line option is accepted, creating a subtle inconsistency. The review suggests documenting this as a known limitation or adjusting the early scan to match `parse_options()`'s flexibility.

Key technical details
- New `OPT_HIDDEN_GROUP` macro (patch 1/12) for organizing hidden options in parse-options, added to `parse-options.h`, with usage formatter updates in `parse-options.c` to skip hidden groups unless `--help-all` is requested. Test coverage added in `test-parse-options.c` and `t/t0040-parse-options.sh`.
- Patch 2/12 adds a "Option flags" subsection to `Documentation/technical/api-parse-options.adoc` documenting per-option flags (`PARSE_OPT_HIDDEN`, `PARSE_OPT_OPTARG`, `PARSE_OPT_LASTARG_DEFAULT`, etc.), their purpose, and typical use cases, distinguishing them from global `parse_options()` flags. The documentation clarifies the `-h` versus `--help-all` behavior for hidden options. Elijah Newren's review suggests rewording the `PARSE_OPT_LASTARG_DEFAULT` description to clarify that the no-argument form is only accepted when the option is the last token on the command line, and that it should not be combined with `PARSE_OPT_OPTARG`.
- Patch 3/12 adds documentation for `OPT_*_F()` macro variants (which accept a trailing `flags` argument) and the `OPT_HIDDEN_GROUP()` and `OPT_HIDDEN_BOOL()` convenience macros to `Documentation/technical/api-parse-options.adoc`, explaining the `_F` suffix convention and placing the new macros adjacent to their non-hidden counterparts (`OPT_GROUP()` and `OPT_BOOL()`).
- Patch 4/12 localizes loop counters in `cmd_fast_import()` to their respective `for` loops, using the most appropriate type (`int`, `unsigned int`, or `size_t`) for each loop's bounds.
- Patch 5/12 changes the type of `show_stats` and `quiet` from `unsigned int` to `int` in `builtin/fast-import.c` to match `OPT_BOOL`'s expected target type. No behavior change; purely mechanical.
- Patch 6/12 factors out `option_max_pack_size()`, `option_big_file_threshold()`, `option_signed_commits()`, `option_signed_tags()`, and `option_quiet()` to encapsulate the parsing logic for their respective options. These helpers replace generic "unknown option" failures with specific `die()` messages when `git_parse_ulong()` fails to parse numeric arguments. The refactoring is mechanical and preparatory, touching only `builtin/fast-import.c`.
- Patch 7/12 introduces `struct fast_import_state` to encapsulate `global_argc`, `global_argv`, and `global_prefix`, passing the struct as the first argument to nearly every function in `builtin/fast-import.c`. Many functions receive the new parameter only to forward it or for future use, so they are marked `UNUSED` to silence compiler warnings. Elijah Newren's review notes that the patch introduces overly long function signatures (exceeding Git's 80-character line limit) when adding the `struct fast_import_state *state` parameter, providing `note_change_n` as an example and suggesting reformatting across multiple lines for readability. The feedback is purely stylistic, with the reviewer explicitly stating the "substance of the patch looks good."
- Patch 8/12 moves the global variables `seen_data_command` and `allow_unsafe_features` into `struct fast_import_state` and reorders `fast_import_state_init()` to avoid clobbering the early `--allow-unsafe-features` command-line scan. The `UNUSED` marker is removed from `check_unsafe_feature()` because its `state` parameter is now used. No behavior change is intended; the patch touches only `builtin/fast-import.c`.
- Patch 9/12 replaces the hand-written, single-line usage string with a `struct option` array, grouping options into logical sections (Common, Marks, Submodule rewrite, Signing, and Advanced), marking hidden options with `PARSE_OPT_HIDDEN`, and using the `OPT_HIDDEN_GROUP` macro. The SYNOPSIS line in `Documentation/git-fast-import.adoc` is updated to match, and the command is removed from `t0450/adoc-help-mismatches`. The change is purely presentational and preparatory, with no behavior change beyond improved help output.
- Patch 10/12 converts a dozen command-line options from direct string/unsigned assignments to `OPT_CALLBACK` entries in the `fast_import_options` array, adding dedicated callback functions (e.g., `option_parse_date_format`, `option_parse_max_pack_size`) that delegate to the existing helpers (e.g., `option_date_format`, `option_max_pack_size`). The callbacks are not yet invoked by `parse_options()`, so behavior remains unchanged. The patch touches only `builtin/fast-import.c` and adds no new symbols beyond the callback functions themselves.
- Patch 11/12 (the behavior-changing patch) switches the command-line parser from a hand-written loop to `parse_options()`, completing the parse-options integration. It removes support for five undocumented options (`--alias`, `--get-mark`, `--ls`, `--notes`, and `--cat-blob`) on the command line, as these are only meaningful inside the input stream. A new test (`t9300-fast-import.sh`) verifies that each of these options now fails when given on the command line. The patch also enables abbreviated options (e.g., `--dep=5` for `--depth=5`), space-separated values for value-taking options (e.g., `--depth 5`), and may adjust error messages slightly. The code shrinks by 29 lines, replacing a bespoke loop with a single `parse_options()` call. The patch touches only `builtin/fast-import.c` and the test suite, with no new symbols or on-disk format changes.
- Patch 12/12 removes the redundant `from_stream` argument from `parse_one_feature()` and its callers, simplifying the code after patch 11/12 eliminated the only non-stream call site. The change is purely mechanical: every invocation previously passed `1` as the `from_stream` argument, so the parameter is dropped and the value hard-coded. The patch touches only `builtin/fast-import.c`, reducing its line count by 9 while preserving behavior. No new symbols are introduced.
- Files touched: `parse-options.h`, `parse-options.c`, `builtin/fast-import.c`, `Documentation/technical/api-parse-options.adoc`, `Documentation/git-fast-import.adoc`, and the test suite (`t0040`, `t0450`, `t9300`).
- No on-disk format changes; CLI behavior changes include:
  - Rejection of undocumented options (`--alias`, `--get-mark`, `--ls`, `--notes`, `--cat-blob`) on the command line (patch 11/12).
  - Improved error messages for numeric argument parsing failures (patch 6/12).
  - Support for abbreviated options (e.g., `--dep=5` for `--depth=5`) and space-separated values (e.g., `--depth 5`) (patch 11/12).
  - Acceptance of bare `--` as an end-of-options marker (patch 11/12).
  - Stricter early scan for `--allow-unsafe-features` compared to `parse_options()`, creating a potential inconsistency where the command-line option is accepted but the in-stream feature is rejected (patch 11/12).
- New symbols: `OPT_HIDDEN_GROUP`, `struct fast_import_state`, `fast_import_state_init()`, `seen_data_command`, `allow_unsafe_features` (now members of `struct fast_import_state`), `parse_allow_unsafe_features()`, `option_max_pack_size()`, `option_big_file_threshold()`, `option_signed_commits()`, `option_signed_tags()`, `option_quiet()`, `option_parse_date_format()`, `option_parse_max_pack_size()`, and other `option_parse_*()` callbacks for the dozen options converted in patch 10/12.

Open questions and loose ends
- The libification effort remains incomplete; many globals are still present for future work.
- The behavior change in patch 11/12 (rejecting undocumented options on the command line) may require documentation or release note updates.
- Elijah Newren's review of patch 2/12 suggests rewording the `PARSE_OPT_LASTARG_DEFAULT` documentation to clarify its interaction with the command-line parser and its incompatibility with `PARSE_OPT_OPTARG`. This feedback is editorial and does not block the patch but should be addressed before merging.
- Elijah Newren's review of patch 7/12 requests stylistic adjustments to function signatures exceeding Git's 80-character line limit, providing a specific example (`note_change_n`) and suggesting reformatting across multiple lines. The feedback is minor and editorial, unlikely to require significant rework.
- Elijah Newren's review of patch 11/12 identifies a subtle inconsistency between the early scan for `--allow-unsafe-features` and `parse_options()`: the early scan is stricter, only matching the exact spelling and stopping at the first non-option token, while `parse_options()` accepts abbreviations and space-separated values. This can cause the in-stream feature to be rejected even though the command-line option is accepted. The review suggests documenting this as a known limitation or adjusting the early scan to match `parse_options()`'s flexibility. Two potential fixes are proposed: a dedicated `parse_options()` pass for `--allow-unsafe-features` or requiring it to be the first argument. Elijah's follow-up email confirms these remain the only open points.
- No substantive technical review beyond Junio’s and Elijah’s feedback has occurred; the series awaits broader engagement.

Participant positions
- Author (Christian Couder): posted v2 addressing Junio’s feedback on v1 by eliminating placeholder local variables and fully integrating the parse-options API. Position is to complete the libification step and standardize the usage string, including the behavior change to reject undocumented options on the command line (patch 11/12) and the improved error messages for numeric argument parsing (patch 6/12). Patches 6/12 through 10/12 are preparatory, mechanical refactorings to enable reuse of option-handling logic as `OPT_CALLBACK` targets, encapsulate globals in `struct fast_import_state`, and align the usage string with the SYNOPSIS. The final patch (12/12) is a cleanup following the earlier work.
- Junio C Hamano: provided feedback on v1 regarding the awkwardness of unused local variables in patch 7/7; v2 resolves this concern by completing the parse-options integration. No further feedback reported in v2.
- Elijah Newren: provided substantive review of patches 2/12, 7/12, and 11/12. For patch 2/12, the feedback focuses on the accuracy of the `PARSE_OPT_LASTARG_DEFAULT` documentation, suggesting rewording for clarity. For patch 7/12, the feedback is surface-level, requesting stylistic reformatting of overly long function signatures. For patch 11/12, the review identifies three behavioral changes introduced by the switch to `parse_options()`, with the `--allow-unsafe-features` inconsistency being the most significant. The follow-up email confirms satisfaction with the overall direction and restates that the three minor issues (documentation wording, function signature style, and the `--allow-unsafe-features` inconsistency) are the only remaining open points. The tone is constructive and implies the series is otherwise ready for broader review or integration.
- D. Ben Knoble: initiated and resolved a procedural discussion about `.mailmap` update timing in v1; no further input expected.

Related work
- Part of the ongoing parse-options modernization and libification efforts. The usage string refactor aligns with broader Git-wide consistency goals for help text. The `.mailmap` discussion is resolved and unrelated to the technical content.