This is a **refactoring discussion** in the `remote-curl` transport code, continuing the design-level conversation about how to pass push specifications to protocol handlers. The thread now centers on whether to adopt Patrick Steinhardt’s suggestion to pass a `const struct strvec *` directly to `push_dav` and `push_git` instead of a raw `const char **`, making the interface more self-documenting and future-proof.

Patrick argues that passing a `strvec` would explicitly guarantee NULL-termination and align better with the caller’s context, while also enabling potential optimizations like a hypothetical `strvec_pushvec()` function. René acknowledges the safety and clarity benefits but notes that the current simplicity of `strvec_pushv()` (which operates on NULL-terminated arrays) influenced his original approach. He also observes that introducing `strvec_pushvec()` could subtly encourage safer usage by making length-limited vectors more appealing than unterminated arrays, though he doubts the performance impact would be measurable in practice.

The discussion remains unresolved but constructive, with no clear consensus yet on whether to adopt the `strvec *` interface. Patrick explicitly states he won’t insist on a reroll, so the current patch (which removes redundant `nr_spec` tracking) could proceed as-is, but a v2 incorporating his suggestion remains possible. The conversation introduces a forward-looking dimension about `strvec` API design, though no new code or behavior changes are proposed in this email. The patch itself is confined to `remote-curl.c` and has no observable impact on Git’s behavior.