This is a user support question about platform compatibility, specifically Git Bash on Windows interacting with Vagrant SSH sessions.

A user reports that pressing Ctrl+C in a Vagrant SSH session from Git Bash terminates the entire SSH session rather than just interrupting the current command. The same workflow works correctly in PowerShell. The email includes a screenshot (not described in detail) but no technical investigation or reproduction steps beyond the observed behavior.

The question is directed at the Git mailing list, but it is not about Git itself — it is about how the terminal emulator (Git Bash) handles signal forwarding over SSH. The issue may involve signal handling, terminal modes, or SSH client configuration, none of which are under Git's control. There is no patch, no code change, and no design discussion; it is purely a support request.

This is unlikely to generate technical discussion on the list unless someone recognizes it as a known issue with Git Bash's signal handling. If you are not debugging Windows terminal behavior, this thread is probably not worth following.