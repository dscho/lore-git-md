This is a pull request for `git-gui`, the graphical interface for Git. Johannes Sixt (the current `git-gui` maintainer) asks Junio to merge seven commits from the `git-gui` repository into the main Git project.

The changes are minor and user-facing. Alexander Shopov updates the Bulgarian translation (562 translated messages) and widens the commit message field in the GUI, which should make it more comfortable for users who write longer commit messages. Harald Nordgren tweaks the build system to silence `msgfmt --statistics` output, and Johannes cleans up the Makefile’s quiet-rule logic. The rest of the commits are merges of external branches.

Nothing here is controversial or risky; it’s routine maintenance and small usability improvements. If you care about `git-gui` or Bulgarian localization, this is worth a quick glance; otherwise, it’s safe to skip.