This thread introduces a new `--drop-filtered` option for `git repack` that allows partial clones to safely reclaim disk space by removing locally cached promisor blobs exceeding a size threshold. The feature is now in its third iteration (v3, 7/7 patches), with all prior feedback addressed—including a subtle bitmap validation fix, softer documentation language, and a reframing of safety guards as UX optimizations rather than correctness measures.

The core functionality is complete: promisor objects are enumerated, filtered by size, and dropped via a rebuilt promisor pack, with crash safety ensured by Git’s existing repack machinery. The documentation (patch 7/7) now fully covers the new options (`--drop-filtered` and `--dry-run`), including preconditions, incompatibilities, and safety guard behavior. Junio’s latest feedback is purely stylistic, requesting minor test script adjustments to align with Git’s conventions. No substantive objections remain, but the series hasn’t yet been queued for integration.

Key participants include first-time contributor Siddharth Shrimali (author), Junio (maintainer), and reviewers Siddharth Asthana and Christian Couder. Open questions—such as Junio’s proposed time-based heuristic for avoiding redundant network operations—are deferred to follow-ups. The thread is now focused on final polish before potential merging.