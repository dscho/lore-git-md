This is a **refactoring discussion** in the `remote-curl` transport code, continuing the thread about simplifying how push specifications are passed to protocol handlers. Junio C Hamano weighs in on the design-level conversation between René Scharfe and Patrick Steinhardt, endorsing Patrick’s forward-looking suggestion to introduce a `strvec_pushvec()` helper function.

Junio agrees with Patrick that `strvec_pushvec()` could be a worthwhile addition to the `strvec` API, independent of René’s current patch. He notes that the existing `strvec_pushv()` callers (which pass a `strvec` as the second parameter) could be easily identified and converted using `git grep` or even Coccinelle, making the transition low-effort. This aligns with the broader goal of nudging contributors toward safer, more explicit interfaces—here, favoring length-limited vectors over unterminated arrays.

The email does not propose new code or alter the current patch’s direction. Instead, it reinforces the idea that the `strvec_pushvec()` discussion is a separate, future-oriented effort. René’s patch (removing redundant `nr_spec` tracking) remains viable as-is, but the thread now has a clearer path for follow-up work. No merge status is reported; the series is still under review.