This is the third and final patch in a **feature** series introducing `--auto-reset` to `git bisect`. The patch adds the user-facing option and wires it into both `git bisect start` and `git bisect run`, completing the functionality described in the cover letter.

The implementation is straightforward: the `--auto-reset` flag is accepted by both subcommands, persisted in a new `BISECT_AUTO_RESET` state file, and triggers a quiet reset to the original commit as soon as the first bad commit is identified. The patch updates the documentation to describe the new option, adds the state-file plumbing in `bisect.c`, and extends the command-line parsing in `builtin/bisect.c` to handle the flag. Four new tests in `t/t6030-bisect-porcelain.sh` verify that the auto-reset behavior works as expected and does not leak into subsequent bisect sessions.

The change is well-scoped and addresses a clear workflow pain point for automated environments. The preparatory refactoring in patch 1 and the quiet-checkout capability added in patch 2 are both used here, so the series builds logically. The option name (`--auto-reset`) is intuitive, and the implementation avoids touching unrelated code paths. Test coverage looks thorough, including edge cases like interrupted bisects and state persistence.

No review feedback has appeared yet, but the patch is unlikely to be controversial. The only likely points of discussion are edge-case behavior (e.g., what happens if the reset fails or the bisect is interrupted) and whether the quiet checkout should be configurable separately. The series appears ready for review and is a good candidate for merging if the feedback is positive.