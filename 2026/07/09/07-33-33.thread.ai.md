Thread identity: Bug report / documentation request regarding inconsistent automatic tag fetching behavior in `git fetch`. Originating author: Ondřej Medek. Thread type: bug report.

Core problem or goal: `git fetch` inconsistently fetches tags pointing to fetched histories depending on whether any refspec in the command includes a destination (`:dst`) part. This behavior contradicts the documentation's claim that automatic tag following happens "by default" and is not explained in the manual, creating user confusion.

Current status: Reported; no resolution or agreement yet. No patch version (not a patch series).

Key technical details:
- Subsystem: `git fetch`, refspec parsing, tag-following logic.
- Behavior: Presence of *any* `:dst` in *any* refspec (even a dummy like `refs/none/*:refs/none/*`) enables automatic tag fetching for *all* refspecs in the command, not just the one containing the `:dst`.
- Files touched: None yet (report only); eventual fix may touch `builtin/fetch.c`, `Documentation/fetch-options.txt`, or `Documentation/git-fetch.txt`.
- Test coverage: Report includes clear reproduction steps (create tag, push, delete locally, fetch with/without `:dst`).

Open questions and loose ends:
- Is this behavior intentional (and thus requires documentation) or a bug (and thus requires a fix)?
- If intentional, what is the rationale for making `:dst` a global toggle rather than per-refspec?
- If a bug, what is the correct fix: make tag following consistent (always on or always off), or make it per-refspec?

Participant positions:
- Ondřej Medek (author): Reports the inconsistency as confusing and suggests either documenting or fixing it. Position is neutral on which outcome is preferable but leans toward a fix based on the phrasing ("this seems like a bug").

Related work: None mentioned.