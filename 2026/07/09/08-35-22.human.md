This is a refactoring patch, part of Patrick Steinhardt’s seven-patch series introducing object filters to `odb_for_each_object()`. It is the second patch in the series, following the cover letter’s goal of enabling `git cat-file --batch-all-objects` to work with pluggable object databases.

The patch marks the object filter parameter in `for_each_bitmapped_object()` as `const` because the function never modifies it. This is a small, mechanical change that touches only `pack-bitmap.c` and `pack-bitmap.h`, adjusting the function signature and its internal helpers (`filter_bitmap` and `can_filter_bitmap`) to match. The commit message is concise and states the motivation clearly.

This is a clean, uncontroversial cleanup that aligns with Git’s coding guidelines. It does not change behavior, introduce new features, or affect performance, but it prepares the codebase for the later patches in the series that will use the filter in a more abstracted way. The patch looks well-crafted and ready for review.