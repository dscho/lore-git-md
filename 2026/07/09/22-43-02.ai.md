This is a **contributor housekeeping** follow-up in the `git-subtree` safeguard thread. Colin Stagner’s reply to Ian Jackson’s v1 response clarifies terminology and probes the design intent of the forthcoming Rust rewrite (RIIR) without requesting code changes.

The exchange resolves the last open question about detection scope: Colin now agrees the safeguard should check only the top-level repository config (`.git-subtree/config` at the root of the revision being manipulated) and not search inside the subtree directory. He frames the decision in terms of Git’s existing vocabulary—“root tree” and “sub-trees” from `git ls-tree -r`—to avoid ambiguity around “upstream” and “downstream.” Colin also confirms that if the Rust rewrite honors only the single config location Ian’s patch tests, then the patch is correct as written.

On unmarked subtree merges (those performed manually with `git merge -X subtree`), Colin notes that `--squash` would likely fail with “unrelated history” warnings, but he does not dispute Ian’s plan to document and test the limitation rather than enforce it at runtime. The tone is supportive; Colin closes by expressing anticipation for v2, signaling no remaining objections.