Thread identity
Subject: [PATCH v7 2/3] repository: store original worktree path for includeIf matching
Series version: v7 (3-patch series)
Originating author: Chen Linxuan
Thread type: patch series (refactoring preparatory work)

Core problem or goal
Extend the `repository` struct to store the user-facing, symlink-preserving spelling of the worktree path (absolute and normalized but not realpath-resolved) so that a future patch can implement `includeIf.worktree:` pattern matching that works identically to the existing `gitdir:` logic—i.e., matching both real and symlinked spellings of the same directory.

Current status
Under review; this is the second patch in the v7 series. No prior versions of this specific patch have been posted; the series as a whole is at v7. No merge status reported; no maintainer decision recorded.

Key technical details
- New field: `worktree_original` in `struct repository`
- New accessor: `repo_get_work_tree_original()`
- Files touched: `repository.h`, `repository.c`, `setup.c`
- Behavior: `repo_set_worktree()` now computes and stores both the realpath (unchanged) and the symlink-preserving copy; discovered repositories recover the original spelling from `$PWD` via `logical_path_from_cwd()`
- No observable behavior change in this patch; the new field is not yet used
- Memory management: wired up in `repo_clear()` to prevent leaks

Open questions and loose ends
None raised in this email; the patch is presented as complete and uncontroversial.

Participant positions
None recorded yet; this is the first email in the thread summary.

Related work
Part of Chen Linxuan’s `includeIf` conditional configuration series (v7). The next patch in the series will consume the new field to implement `includeIf.worktree:` matching.