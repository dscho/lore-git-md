This is a brief, substantive review exchange in the ongoing reftable backend performance regression fix series. Patrick Steinhardt identifies a downstream compatibility issue in the v2 patch: the `suppress_deletions` flag is being removed from the merged iterator, but libgit2 still relies on it. He proposes extending `struct reftable_stack_options` to let callers control tombstone suppression at stack creation time, preserving libgit2’s ability to set the flag without depending on internal implementation details. Kristofer Karlsson agrees, noting that libgit2 will need updating regardless because it predates the `reftable_stack_options` split, so the compatibility break is already inevitable. The exchange is resolved; the v3 series will incorporate this change. No other open questions remain.