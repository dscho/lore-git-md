This is a **substantive review** from Junio C Hamano on patch 4/9 of Tian Yuchen’s v8 series migrating `pager_program` into `struct repo_config_values`. Junio identifies a **memory leak regression** in the patch’s configuration parsing logic: the `core_pager_config()` callback (used by `read_early_config()`) directly overwrites `repo_config_values(r)->pager_program` via `git_config_string()`, but unlike `check_pager_config()` — which explicitly frees the old value before re-assignment — it does not clean up the previous heap-allocated string. This reintroduces the leak Junio flagged in v7, now in a different code path.

The fix pattern established in earlier patches (3/9 for `editor_program`, and this patch’s own `check_pager_config()`) is not consistently applied: `core_pager_config()` needs to free the old value before calling `git_config_string()`. The review is **technically precise**, pinpointing the exact function (`core_pager_config`) and the missing cleanup step. No other issues are raised; the rest of the patch (field migration, lifecycle management, and caller updates) appears uncontroversial.

This is a **blocking concern** for the series, as it undermines the memory safety guarantees the patchset aims to provide. The fix is straightforward (add `free()` before `git_config_string()`), but the oversight highlights the risk of inconsistent memory management across similar code paths. The thread’s broader context — the series’ goal of eliminating globals and centralizing memory management — makes this leak particularly ironic. Expect a v9 to address this before the series can advance.