This thread is a simple administrative update from Taylor Blau. They’ve submitted a one-line patch to Git’s `.mailmap` file, mapping their new work email (`ttaylorr@openai.com`) to their canonical address (`me@ttaylorr.com`). This is purely housekeeping—it ensures tools like `git shortlog` and `git blame` display consistent author information, reflecting Taylor’s recent move from GitHub to OpenAI.

No review is required or expected for `.mailmap` updates, and this patch is ready to be merged as-is. It’s a standalone change with no functional impact on Git’s code or behavior. The commit message is minimal but clear, and the patch follows the project’s standard practice for keeping author mappings current.