This is a **substantive review** of the second patch in the `includeIf worktree:<pattern>` series (v7 2/3). Patrick Steinhardt raises two main concerns about the implementation:

1. **Accessor function overhead**: He questions the need for `repo_get_worktree_original()`, arguing the field could be accessed directly via the `repository` struct. This is a minor refactoring nit but signals a preference for simpler internal APIs.

2. **Complexity of symlink-preserving path recovery**: The core critique targets the `logical_path_from_cwd()` helper, which reconstructs the worktree path while preserving symlinks. Patrick calls the logic "awkward" and notes it will conflict with his in-flight `ps/setup-split-discovery-and-setup` series. He proposes two alternatives:
   - **Radical simplification**: Remove the `chdir()` call during repository discovery entirely (a larger architectural change he’s considering post-`ps/setup-*`).
   - **Deferred fix**: Document the current limitation and address it later, avoiding the complexity now.

The review does not reject the feature but highlights a tension between correctness (symlink preservation) and maintainability (code complexity). The conflict with Patrick’s upcoming work suggests this patch may need rework regardless of the chosen path. No test failures or functional bugs are reported—this is purely about design tradeoffs. The author will need to decide whether to:
- Refactor the symlink logic to align with Patrick’s vision,
- Document the limitation and proceed, or
- Explore the `chdir()`-removal approach (which would require coordination with Patrick’s series).