This is a **surface-level usability discussion** in the now-merged `git refs create` subcommand thread, focusing on the `--no-deref` option’s behavior and discoverability. Toon Claes raises concerns about the option’s obscurity and counterintuitive semantics, while Patrick Steinhardt clarifies its intended purpose: `--no-deref` does **not** enable symref creation but instead enforces atomicity by failing if the target reference already exists as a symbolic ref. For example, if `refs/heads/symref` points to `refs/heads/target`, `git refs create refs/heads/symref $OID` would create `refs/heads/target` (dereferencing the symref), while `--no-deref` would fail. The exchange highlights a tension between atomicity guarantees and CLI ergonomics, though no code changes are proposed or expected, as the series is already merged.

The discussion centers on two key points:
1. **Option semantics**: Toon initially misinterprets `--no-deref` as enabling symref creation, but Patrick explains it only controls failure when the target exists as a symref. The option’s name and documentation (`"Operate on <ref> itself rather than the reference it points to"`) are acknowledged as non-obvious, though Patrick defends the default behavior’s consistency with other `git refs` subcommands.
2. **Test coverage**: Toon proposes a test for symref creation with `--no-deref`, but Patrick rejects it, emphasizing that the subcommand’s "must not exist" semantics reject duplicate creations (even with the same value) and that `--no-deref` is not intended for symref creation. The existing test (`create with symref target and --no-deref refuses to create reference`) already validates the atomicity check.

No new technical details or edge cases emerge beyond those documented in the thread summary. The conversation remains **surface-level**, focusing on usability rather than correctness, and is unlikely to prompt follow-up patches given the series’ merged status. The feedback may inform future documentation improvements or CLI refinements, particularly around `--no-deref`’s discoverability. Key files/subsystems: `builtin/refs.c`, `Documentation/git-refs.adoc`, and `t1466-refs-create.sh`.