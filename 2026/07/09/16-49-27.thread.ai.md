```
Thread identity
Subject: [PATCH 0/12] Widen size fields in delta/pack machinery for 64-bit Windows
Series version: v1
Originating author: Johannes Schindelin (via GitGitGadget)
Thread type: patch series (platform-compatibility fix)

Core problem or goal
Widen size-related fields, parameters, and local variables in Git’s delta-compression and pack-objects code from `unsigned long` or `int` to `size_t` to prevent integer truncation on 64-bit Windows (where `unsigned long` is 32-bit) and other platforms where `unsigned long` does not match the pointer width. This is part of the ongoing effort to eliminate incorrect `unsigned long` usage in favor of `size_t`.

Current status
Under review; v1 posted. No prior versions. The series is mechanical and confined to type widening; no behavior or on-disk format changes.

Key technical details
- Files touched: 14, primarily in the delta (`diff-delta.c`, `delta.h`), pack-objects (`pack-objects.c`, `pack-objects.h`), and zlib (`git-zlib.c`) subsystems; also `http-push.c`, `archive-zip.c`, and test helpers.
- New/renamed symbols: `struct delta_index` size fields (`src_size`, `trg_size`, `max_size`), `create_delta_index()` and `create_delta()` parameters, delta-cache accounting variables in `pack-objects.c`, zlib stream fields in `git-zlib.c`.
- Internal preparatory work: Patch 1 widens `src_size` and `memsize` in the private `struct delta_index` (defined in `diff-delta.c`) from `unsigned long` to `size_t`; this struct is not exposed outside `diff-delta.c`, so the change is purely internal and has no runtime effect yet.
- Behavior change: none; this is a pure type-widening correctness fix.
- On-disk format change: none.
- Test coverage: unchanged; no new tests added.
- Base commit: `f85a7e662054a7b0d9070e432508831afa214b47`.

Open questions and loose ends
None identified yet.

Participant positions
- Johannes Schindelin (author): posted v1, motivated by real-world 64-bit Windows truncation issues. The series is uncontroversial and preparatory for the broader `unsigned long` → `size_t` conversion effort.

Related work
- Part of the broader `unsigned long` → `size_t` conversion effort.
- References earlier commit `606c192380` (introduced `cast_size_t_to_ulong()` shims in pack-objects code).
```