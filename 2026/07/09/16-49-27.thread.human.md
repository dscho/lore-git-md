This thread is about a 13-patch series from Johannes Schindelin that widens size-related fields in Git’s delta-compression and pack-objects code from `unsigned long` to `size_t` to prevent integer truncation on 64-bit Windows, where `unsigned long` is 32-bit. The series is now complete and queued in Junio’s `next` branch for integration testing.

The final patch in the series, just posted, widens the last remaining `unsigned long` interface—`unpack_object_header_buffer()` in `packfile.c`—to `size_t`, addressing Junio’s review feedback from the previous version. This change ensures full type consistency across all affected code paths, including zlib stream interfaces, delta-encoding APIs, and memory accounting variables. The series has been thoroughly reviewed by Patrick Steinhardt and Junio, with all feedback addressed in this iteration. No behavior or on-disk format changes are introduced; the work is purely about type safety.

The patches touch 25 files, including `diff-delta.c`, `pack-objects.c`, `git-zlib.c`, and test helpers, and introduce no new runtime behavior. The series is uncontroversial and expected to graduate to `master` without further changes. Downstream consumers should note that all size-related fields in the delta/pack machinery now use `size_t`, eliminating truncation risks on platforms where `unsigned long` is narrower than a pointer.