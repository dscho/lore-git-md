This is a refactoring patch targeting the `git add -e` (interactive edit) command. It replaces a subprocess call to `git apply` with a direct invocation of Git's internal `apply_all_patches()` API, eliminating the overhead of forking a child process. The change is confined to `builtin/add.c` and removes two now-unused header includes (`run-command.h` and `strvec.h`) while adding `apply.h`.

The patch initializes an `apply_state` struct with the same options (`--cached` and `--recount`) that were previously passed to the subprocess, then calls `apply_all_patches()` directly. The commit message explains the motivation clearly: avoiding subprocess overhead, keeping the operation in-process, and improving code clarity. The diff is small (9 insertions, 7 deletions) and mechanically straightforward.

This appears to be a well-motivated cleanup with no behavior change. The patch is self-contained and touches only the `edit_patch()` function in `builtin/add.c`. The author is a first-time contributor (no prior patches visible in the thread context), but the submission follows standard conventions: clear commit message, minimal diff, and proper sign-off. The change aligns with Git's ongoing efforts to reduce subprocess usage where internal APIs are available. No test failures or edge cases are mentioned, suggesting the change is purely mechanical.