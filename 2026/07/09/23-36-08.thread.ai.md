Here is the dense, loss-free AI-mode summary for the next invocation:

---
Thread identity: patch series introducing `post-worktree-add` and `post-worktree-remove` hooks; series version 1; originating author Domen Kožar; thread type patch series.

Core problem or goal: add reliable automation triggers for worktree lifecycle events (creation and removal) to support high-velocity tooling, particularly AI-driven development environments that create and destroy worktrees frequently.

Current status: under review; version 1 posted; no prior versions.

Key technical details: new hooks `post-worktree-add` and `post-worktree-remove`; fired after worktree addition/removal, including pruned worktrees; implemented in `builtin/worktree.c`, `worktree.c`, `worktree.h`; test coverage in `t/t2400-worktree-add.sh`, `t/t2401-worktree-prune.sh`, `t/t2403-worktree-move.sh`; documented in `Documentation/githooks.adoc`; path resolution during pruning handles missing/corrupt gitdir files. Hooks receive the worktree’s absolute directory and ID as arguments, though the process already runs inside the worktree.

Open questions and loose ends: (1) whether `post-worktree-add` should run if `post-checkout` fails (Phillip Wood endorses skipping it, as Git already removes the worktree in that case); (2) utility of passing worktree directory/ID to the hook when it is already running inside the worktree (Phillip questions this); (3) how to handle path resolution for pruned worktrees when gitdir file is missing or corrupt (hook receives empty string); (4) undocumented relative order of `post-worktree-add` and `post-checkout` (Phillip requests documentation).

Participant positions: Phillip Wood (substantive review): endorses core motivation and hook design; agrees with skipping `post-worktree-add` if `post-checkout` fails; questions utility of passing worktree directory/ID to the hook; requests documentation of hook execution order.

Related work: none mentioned.
---