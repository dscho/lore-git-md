Here is the dense, loss-free AI-mode summary for the next invocation:

---
Thread identity: patch series introducing `post-worktree-add` and `post-worktree-remove` hooks; series version 1; originating author Domen Kožar; thread type patch series.

Core problem or goal: add reliable automation triggers for worktree lifecycle events (creation and removal) to support high-velocity tooling, particularly AI-driven development environments that create and destroy worktrees frequently.

Current status: under review; version 1 posted; no prior versions; key design decisions clarified but not yet implemented in code.

Key technical details: new hooks `post-worktree-add` and `post-worktree-remove`; fired after worktree addition/removal, including pruned worktrees; implemented in `builtin/worktree.c`, `worktree.c`, `worktree.h`; test coverage in `t/t2400-worktree-add.sh`, `t/t2401-worktree-prune.sh`, `t/t2403-worktree-move.sh`; documented in `Documentation/githooks.adoc`; path resolution during pruning handles missing/corrupt gitdir files. Hooks receive the worktree’s absolute directory and ID as arguments, though the process already runs inside the worktree. **Design update**: `post-worktree-add` will now run unconditionally, even if `post-checkout` fails (previously skipped).

Open questions and loose ends: (1) utility of passing worktree directory/ID to `post-worktree-add` when the hook already runs inside the worktree (Domen argues for consistency with `post-worktree-remove` and script symmetry; Phillip concedes but notes niche utility for worktree ID); (2) how to handle path resolution for pruned worktrees when gitdir file is missing or corrupt (hook receives empty string); (3) undocumented relative order of `post-worktree-add` and `post-checkout` (Phillip requests documentation); (4) **new**: whether to add a `post-worktree-rename` hook for external tooling (Phillip raises; Domen does not address).

Participant positions: Domen Kožar (author): prefers running `post-worktree-add` unconditionally; argues for consistent hook signatures (directory/ID arguments) to simplify shared hook scripts. Phillip Wood (substantive review): initially preferred skipping `post-worktree-add` if `post-checkout` fails but now agrees to run it unconditionally; questions utility of directory/ID arguments for `post-worktree-add` but concedes for consistency; requests documentation of hook execution order; proposes `post-worktree-rename` hook (unanswered).

Related work: none mentioned.
---