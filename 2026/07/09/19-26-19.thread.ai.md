Thread identity
Subject: [PATCH] builtin/add.c: refactor `git add -e` to use internal apply API
Series version: standalone patch (v2)
Originating author: Gatla Vishweshwar Reddy
Thread type: patch submission (refactoring)

Core problem or goal
Replace the subprocess call to `git apply` in the `git add -e` (interactive edit) command with a direct invocation of Git’s internal `apply_all_patches()` API to eliminate fork overhead, keep the operation in-process, and improve code clarity.

Current status
Under review (v2 posted). The v2 update addresses Junio C Hamano’s correctness concern from v1 by adding the missing `check_apply_state()` call, which ensures the `apply_state` struct is properly configured for index updates. The author confirms all 58 tests in `t3700-add.sh` pass, indicating no regression in existing functionality.

Key technical details
- Files touched: `builtin/add.c` only
- New symbols: none (uses existing `apply_all_patches()` and `apply_state` struct)
- Removed includes: `run-command.h`, `strvec.h`
- Added include: `apply.h`
- Behavior: unchanged in intent; the v2 patch resolves the prior correctness risk by initializing `apply_state` with `check_apply_state()` after setting `state.cached = 1`
- Test coverage: existing `t3700-add.sh` (58 tests) passes; no additional test coverage added for the internal apply path

Open questions and loose ends
- Whether Junio or another maintainer will confirm the fix resolves the correctness issue
- Whether the patch is ready for merging or requires further adjustments

Participant positions
- Author (Gatla Vishweshwar Reddy): first-time contributor; submitted v1 with a correctness oversight, then posted v2 addressing Junio’s feedback. The v2 patch is well-structured, includes a clear rationale, and passes all existing tests. The response to review was substantive and timely.
- Junio C Hamano: identified a correctness risk in v1 (omission of `check_apply_state()`), which could cause the internal apply API to silently skip writing the edited patch back to the index. The review was substantive, engaging with the code’s behavior and consequences. No further review comments on v2 yet.

Related work
Aligns with Git’s ongoing effort to reduce subprocess usage where internal APIs are available. No dependencies or conflicts with other in-flight topics. The review process highlights that internal API usage requires careful attention to state initialization, even in seemingly straightforward refactorings.