Thread identity
Subject: [PATCH] builtin/add.c: refactor `git add -e` to use internal apply API
Series version: standalone patch (v1)
Originating author: Gatla Vishweshwar Reddy
Thread type: patch submission (refactoring)

Core problem or goal
Replace the subprocess call to `git apply` in the `git add -e` (interactive edit) command with a direct invocation of Git’s internal `apply_all_patches()` API. The goal is to eliminate the overhead of forking a child process, keep the operation in-process, and improve code clarity.

Current status
Under review (v1 posted). No prior versions or related series.

Key technical details
- Files touched: `builtin/add.c` only
- New symbols: none (uses existing `apply_all_patches()` and `apply_state` struct)
- Removed includes: `run-command.h`, `strvec.h`
- Added include: `apply.h`
- Behavior: unchanged; the patch is purely mechanical, passing the same `--cached` and `--recount` options to the internal API that were previously passed to the subprocess
- Test coverage: none mentioned; the change is expected to be covered by existing `git add -e` tests

Open questions and loose ends
None raised yet. The patch is self-contained and appears to be a straightforward refactoring.

Participant positions
- Author (Gatla Vishweshwar Reddy): first-time contributor; submission follows standard conventions (clear commit message, minimal diff, proper sign-off). No prior review feedback recorded.

Related work
Aligns with Git’s ongoing effort to reduce subprocess usage where internal APIs are available. No dependencies or conflicts with other in-flight topics.