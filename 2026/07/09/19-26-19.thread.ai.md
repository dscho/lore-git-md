Thread identity
Subject: [PATCH] builtin/add.c: refactor `git add -e` to use internal apply API
Series version: standalone patch (v3 posted)
Originating author: Gatla Vishweshwar Reddy
Thread type: patch submission (refactoring)

Core problem or goal
Replace the subprocess call to `git apply` in the `git add -e` (interactive edit) command with a direct invocation of Git’s internal `apply_all_patches()` API to eliminate fork overhead, keep the operation in-process, and improve code clarity.

Current status
Under review (v3 posted). The v3 update resolves the two issues Junio raised in v2: (1) variable declarations are now at the top of the function, complying with Git’s `-Wdeclaration-after-statement` rule, and (2) the subdirectory-path concern is addressed by confirming that `repo_git_path()` returns an absolute path, which `apply_all_patches()` handles correctly by skipping the prefix for absolute paths. All 58 tests in `t3700-add.sh` continue to pass. The patch remains blocked on whether a dedicated test for subdirectory behavior is required before merging.

Key technical details
- Files touched: `builtin/add.c` only
- New symbols: none (uses existing `apply_all_patches()` and `apply_state` struct)
- Removed includes: `run-command.h`, `strvec.h`
- Added include: `apply.h`
- Behavior: unchanged in intent; the internal apply path now matches the subprocess behavior exactly, including correct operation from subdirectories.
- Test coverage: existing `t3700-add.sh` (58 tests) passes; no additional test coverage yet for the internal apply path or subdirectory behavior.

Open questions and loose ends
- Whether a new test is required to verify subdirectory operation, or if the existing test suite already provides sufficient coverage.
- Whether Junio or another maintainer will confirm the v3 patch resolves all outstanding concerns and approve merging.

Participant positions
- Author (Gatla Vishweshwar Reddy): first-time contributor; posted v3 addressing Junio’s feedback on declaration order and subdirectory-path handling. The response remains substantive and timely, with clear technical explanations. The author requests guidance on test coverage but has not yet proposed a test patch.
- Junio C Hamano: identified a correctness risk in v1 and two new issues in v2 (declaration order and subdirectory path handling). The v3 patch addresses both issues, but Junio has not yet reviewed it or indicated whether the resolution is sufficient. The prior review was substantive, engaging with both code style and behavioral correctness.

Related work
Aligns with Git’s ongoing effort to reduce subprocess usage where internal APIs are available. No dependencies or conflicts with other in-flight topics. The review process underscores that internal API usage requires careful attention to both mechanical style rules and edge-case behavior, even in seemingly straightforward refactorings.