This is a contributor housekeeping patch. Taylor Blau updates the `.mailmap` file to map their work email address (`ttaylorr@openai.com`) to their canonical address (`me@ttaylorr.com`). The change is a single-line addition to the file and requires no code or test updates.

The patch is trivial and self-explanatory. `.mailmap` is used by `git shortlog` and other tools to consolidate multiple email addresses belonging to the same contributor, so this ensures future contributions from the new address will be attributed correctly. No controversy or review feedback is expected.