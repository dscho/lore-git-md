This is a brief follow-up in the HTTP authentication bugfix thread. Aaron Plattner confirms he has not revisited the architectural refactoring discussion (adding a `credential_update_url()` helper in `credential.c`) since being sidetracked by other work. He notes the immediate fix is not urgent for his users, as a workaround exists (using the server's FQDN), and agrees to treat the helper-function improvement as a low-priority follow-on. The thread now explicitly separates the already-landed bugfix (preserving WWW-Authenticate headers during redirects) from the longer-term design question, with no unresolved technical blockers.

Key context for future discussion: the `credential_update_url()` helper remains a proposed but unaddressed improvement, and Aaron’s comment about WWW-Authenticate headers potentially belonging in an HTTP-specific structure (rather than the credential struct) is still on the table. The patch itself (`http.c`, 14-line fix) is confirmed safe and correct, with test coverage in `t/t5551-http-fetch-smart.sh` and Apache redirect configurations.