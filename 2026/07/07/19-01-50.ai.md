**Summarizer brief mode**

**Classification:** bugfix (terminal compatibility) – design discussion follow-up

Mantas Mikulėnas replies to Junio C Hamano’s mild skepticism about Git’s whitelist-based approach to parsing ANSI SGR sequences in `sideband.c`. Mantas argues that the current patch (which adds support for colon-separated subparameters in SGR sequences, e.g., `\e[38:5:NNm` for 256-color codes) now covers *all* valid SGR value types, making the filter exhaustive for this sequence type. He notes that the parser already accepts any numeric value within SGR sequences, so the change is not overly permissive.

For non-SGR escape sequences, Mantas strongly favors whitelisting, citing the vast number of potentially harmful sequences (e.g., those that toggle terminal modes, display images, or interact with the clipboard) that have no legitimate place in Git’s sideband progress messages. He suggests that even cursor-movement sequences (e.g., `\e[<row>;<col>H`) could be disallowed, as they require terminal-dimension awareness to be useful—something Git’s sideband protocol does not provide. He also proposes excluding hyperlink sequences (`OSC 8;...ST`) due to past security concerns (e.g., `file://` URLs), and progress-bar sequences (`OSC 9;4;nST`), advocating instead for server-side percentage reporting to be handled via a dedicated protocol extension.

**Key details for future summarization:**
- **File/subsystem:** `sideband.c`, `handle_ansi_sequence()` (SGR parsing).
- **Design stance:** Mantas advocates for *strict whitelisting* of escape sequences in sideband output, arguing that the risk of harmful sequences (e.g., clipboard access, terminal mode toggles) outweighs any potential utility. He considers the current SGR whitelist now complete after this patch.
- **Proposed exclusions:** Cursor-movement sequences (lacking terminal-dimension context), hyperlink sequences (security risks), and progress-bar sequences (better handled via protocol-level percentage reporting).
- **Thread state:** The patch itself remains uncontroversial and approved (Acked-by from Johannes Schindelin). This reply is a *design-level discussion* about broader escape-sequence handling, not a request for changes to the patch. Junio’s earlier skepticism about whitelisting is now contextualized by Mantas’s defense of the approach.
- **Future discussion vectors:** Whether to further restrict escape sequences in sideband output (e.g., disallowing cursor movement), or to formalize a protocol extension for progress reporting (e.g., integer percentages). No immediate action is expected on these points.

The patch is still on track for merging; this exchange is a tangential but informative debate about Git’s security posture in terminal output handling.