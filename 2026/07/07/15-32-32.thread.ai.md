Thread identity: patch series titled "Make ODB optimizations pluggable", v1, 11 patches, authored by Patrick Steinhardt. Thread type: patch series introducing modular ODB optimization logic as part of the ongoing ODB abstraction effort.

Core problem or goal: restructure Git's object database (ODB) to allow different backends to implement their own optimization strategies (repacking, geometric repacking, auto-gc) without hardcoding them in `builtin/gc.c`. The immediate goal is to extract existing optimization logic into a modular system, enabling future backends (e.g., reftable, custom storage) to define their own housekeeping tasks. The first patch addresses a long-standing inconsistency in the "pre-auto-gc" hook, extending its invocation to all auto-maintenance tasks (reflog expiry, geometric repack, rerere gc) rather than just `git gc` and the "gc" maintenance task.

Current status: series v1 posted; under initial review. No prior versions or merge status recorded. Changes are preparatory and do not introduce new backends or alter user-visible behavior beyond the expanded scope of the "pre-auto-gc" hook.

Key technical details:
- Files touched: `builtin/gc.c` (refactored to remove direct hook invocation), new files `odb/source-files.c` (470 lines added), `odb.h`, `odb/source.h`, `odb/source-files.h`, `odb.c`, and test file `t/t7900-maintenance.sh` (143 lines added, including 121 lines for new hook behavior tests).
- New symbols: `odb_optimize()`, `odb_optimize_required()`, `struct odb_source_files`, `enum auto_gc_hook_result` (tracks hook state to ensure single invocation per `git maintenance run --auto`).
- On-disk format: no changes.
- Behavior: "pre-auto-gc" hook now runs once before the first auto-maintenance task that would execute, regardless of task type. Test coverage validates hook success/failure, no-op maintenance, and explicit `--auto` vs non-auto runs.
- Subsystems: hooks, maintenance tasks, ODB abstraction layer.

Open questions and loose ends: none raised yet; series is self-contained and builds on Git 2.56 cycle start (commit `f85a7e6620`).

Participant positions: none recorded yet (initial post).

Related work: part of the broader ODB abstraction effort led by Patrick Steinhardt; no external dependencies mentioned. The first patch’s hook expansion is a prerequisite for pluggable optimizations, ensuring all backends can rely on a consistent housekeeping guard.