This is a **follow-up discussion in the commit-graph regression bugfix thread**, specifically addressing Taylor Blau’s architectural suggestion to prevent similar bugs in the future. Kristofer Karlsson explores three possible approaches to refactor the `topo_levels` slab storage, weighing their trade-offs, but ultimately reaffirms the minimal one-line fix as the immediate priority.

Kristofer analyzes Taylor’s proposal to move `topo_levels` out of `struct commit_graph` and into a broader scope. He outlines three technical paths: (a) threading the slab through the call chain (invasive, noisy), (b) attaching it to `struct object_database` (minimal diff, but semantically questionable), and (c) introducing a dedicated chain-wide struct (cleanest but largest refactor). He prototyped option (b) and confirms it compiles and passes tests, but notes uncertainty about whether the object database is the right place for this state. Despite the potential long-term benefits, he concludes the current one-line fix remains the best near-term solution, deferring deeper refactoring to a follow-up effort.

The email advances the thread by clarifying the feasibility and scope of Taylor’s suggestion, but keeps the focus on the regression fix. The discussion remains constructive and non-blocking, with no objections to the immediate patch. Key technical details from prior context—such as the performance impact (233ms → 4133ms), the affected workflows (`fetch.writeCommitGraph`), and the test restructuring—remain unchanged. The architectural exploration here is purely forward-looking and does not alter the series’ readiness for merge.