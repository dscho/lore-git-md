This is the fourth patch in Jeff King's seven-patch series refactoring the `git_hash_*()` API, and this email is Junio C Hamano's review of it. The patch makes `git_hash_discard()` idempotent by adding an `active` flag to the `git_hash_ctx` struct, allowing cleanup code to safely discard a hash context without tracking whether it was already finalized.

Junio's review is substantive but narrowly focused. He asks two clarifying questions: first, whether the point of the change is to allow `git_hash_discard()` to be called even after `git_hash_final()` (answer: yes, that is exactly the goal); and second, why only `git_hash_discard()` is protected from multiple calls while `git_hash_final()` is not (answer: because `git_hash_final()` is already a no-op if called multiple times, whereas `git_hash_discard()` was not). These questions do not challenge the patch's correctness but seek to confirm the design intent and ensure the asymmetry between the two functions is intentional.

The patch itself remains unchanged in this email; Junio's feedback is purely interrogative. The review does not identify any bugs or suggest code changes, and the questions are framed as clarifications rather than objections. This suggests the patch is technically sound and likely to be accepted once the design rationale is confirmed. The thread context indicates all prior feedback has been addressed, and this is the first substantive review of this specific patch. No further changes are expected unless Jeff King's response reveals a misunderstanding or unintended behavior.