This is the latest follow-up in the `git history squash` series, now at v7. Harald Nordgren briefly responds to Phillip Wood’s concern about input validation, specifically whether the command safely handles problematic `rev-list` options like `--reverse`.

Harald confirms the code already blocks `--reverse` and other unknown options, but agrees the commit message could better document this. This addresses one of the three open design questions from v7: the command will now explicitly sanity-check `rev-list` options, following the precedent set by `git replay`. The other two unresolved issues—whether `--reedit-message` should be the default, and whether the template should reorder commits to match `git rebase -i`’s autosquash behavior—are not mentioned here, suggesting they may remain as-is for now.

The series is still marked "Will replace" by Junio, but this minor clarification may warrant a v8 before integration. The change is straightforward and low-risk, focusing on robustness rather than new functionality.