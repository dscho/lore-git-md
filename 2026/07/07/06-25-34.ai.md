This is a **reply in a refactoring series** that addresses Justin Tobler’s substantive review of patch 2/13. Patrick Steinhardt clarifies two points raised in the review:

1. **Behavioral consistency**: The patch does not introduce silent ignoring of misconfigurations (both `core.worktree` and `core.bare` set) when `GIT_WORK_TREE` is present. That behavior already existed; the patch merely relocates the logic without changing it. The commit message’s phrasing ("explicitly unset") may have been misleading—Patrick confirms the intent was to preserve existing behavior, not alter it.

2. **Ordering dependency**: `apply_repository_format()` is indeed always called eventually, as it finalizes repository setup. The patch’s relocation of the "bogus worktree" check does not introduce new ordering risks, though Justin’s question about whether it’s *always* called after `setup_explicit_git_dir()` remains unanswered. Patrick’s response implies the check’s timing is flexible, but the commit message does not document this assumption.

The reply is concise and directly addresses the review’s concerns, but it leaves one loose end: the lack of explicit documentation about the ordering dependency between `apply_repository_format()` and `setup_explicit_git_dir()`. Future refactoring could inadvertently violate this assumption, so the series may need to add safeguards or comments in later patches. No code changes are proposed yet, but this exchange could prompt Patrick to clarify the commit message or add assertions in subsequent revisions. The thread remains under active review.