This is a documentation patch updating the Git 2.55.0 release notes to clarify the Rust support timeline. Junio C Hamano fixes an incomplete sentence that previously stated Rust support "is enabled by default (but still allows opting out) in some future version of Git" — a vague phrasing that had already caused confusion. The new wording explicitly states that Git 2.55 builds with Rust by default but allows opting out, and that Rust will become mandatory starting with Git 3.0.

The change is minimal (two lines modified) and targets only the release notes file. It directly addresses a real source of ambiguity: the original text did not specify which "future version" would make Rust mandatory, leaving users and distributors uncertain about the timeline. The update provides the missing clarity without altering any code or build behavior.

This patch is part of the broader Rustification effort, though it is purely informational rather than technical. It reflects the project's current consensus that Rust support is now stable enough for default inclusion but will not be enforced until Git 3.0. The commit message notes that Junio typically avoids updating historical documents, but made an exception here due to the confusion the original wording had already caused.

No thread context was provided, so this appears to be a standalone patch rather than part of a larger discussion. The change is uncontroversial and aligns with the established roadmap for Rust integration.