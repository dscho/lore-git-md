This is a brief follow-up in the `git history squash` series, where Harald Nordgren responds to Phillip Wood’s review of v7. The focus is on input validation: specifically, how the command handles `rev-list` options like `--reverse` that could distort the resolved commit range.

Harald acknowledges Phillip’s concern and confirms the code already rejects unknown `rev-list` options (including `--reverse`), but agrees the commit message should clarify this. He does not mention adopting `git replay`’s more thorough sanity-checking approach here, though the thread summary notes he later agreed to do so. This email is narrowly scoped to the `--reverse` issue and does not address the larger unresolved questions (default behavior for `--reedit-message`, template reordering, or marker target validation).

The patch series remains in a holding state—Junio’s "Will replace" sign-off indicates intent to merge, but Phillip’s feedback exposed gaps in input validation that require another iteration. The next version will likely add explicit sanity checks for `rev-list` options to prevent edge cases where the resolved range’s parent commit is ambiguous. No new code or design changes are proposed in this email.