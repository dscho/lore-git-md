Henrique Ferreiro’s performance fix for a quadratic-time index scan in `unpack-trees.c` has been merged to `master`. The patch, which adds a `cache_bottom` field to `o->internal` to track forward-scan progress in `next_cache_entry()`, resolves a long-standing bottleneck in `git diff` when run with a pathspec matching a subtree whose entries appear early in the index. Junio’s prior review raised a critical correctness concern about the optimization’s interaction with existing scan logic, but Henrique’s argument—that `find_cache_pos()` already advances `.cache_bottom` past unpacked entries (citing commit `e53e6b4433`)—was deemed "sensible," and the perf test was updated to scale entry counts via the `EXPENSIVE` prerequisite as requested.

The fix is minimal (4 lines) and self-contained, with no open questions or loose ends. Performance gains are dramatic: an 8-minute operation on a Chromium-scale repository now completes in 0.07 seconds (a 99.7% speedup). The regression test (`t/perf/p0009-diff-pathspec.sh`) ensures the issue won’t regress, and the patch is now part of the upcoming Git release. No further review or iteration is expected unless integration testing surfaces edge cases.