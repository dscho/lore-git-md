This is the **reply from Patrick Steinhardt to Justin Tobler’s substantive review of patch 3/13** in the repository discovery refactoring series. The email clarifies the shallow file configuration precedence and addresses Justin’s design concerns about environment variables.

Patrick confirms that the `--shallow-file` command-line option has always overridden the `GIT_SHALLOW_FILE` environment variable, and this patch does not change that behavior. The override logic was previously handled by `set_alternate_shallow_file()`’s third parameter, which was set to `0` when processing the environment variable, preventing it from overwriting an already-configured shallow file. The patch removes this now-unused logic, simplifying the code. Patrick acknowledges Justin’s point about the commit message lacking this detail and agrees it should be documented.

On Justin’s broader concern about using environment variables to store global state, Patrick concedes the design is not ideal but defends it as consistent with how other global options (e.g., object directories) are handled. He notes that a more principled refactoring of argument handling in `git.c` to avoid global state entirely would be a much larger effort, implying this patch is a pragmatic step rather than a perfect solution. The response does not propose an alternative (e.g., an intermediate configuration structure) but leaves the door open for future improvements.

The exchange leaves two minor loose ends: the commit message will need updating to document the precedence behavior, and the environment variable design remains a potential point of contention for future refactoring. However, the patch itself is unlikely to face further pushback, as Patrick’s reply addresses Justin’s technical questions and the change aligns with the series’ architectural goals. The discussion reinforces the series’ focus on incremental progress toward decoupling repository discovery from setup, even if some design choices are suboptimal.