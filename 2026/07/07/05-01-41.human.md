This is the first patch in Jeff King’s seven-patch series refactoring the `git_hash_*()` API. It mechanically converts all direct calls to `algop->init_fn()` into calls to the `git_hash_init()` wrapper function, ensuring consistent usage across the codebase. The patch touches 21 files, including core object-writing code (`csum-file.c`, `object-file.c`), built-ins (`fast-import`, `index-pack`, `receive-pack`), and test helpers. A new Coccinelle semantic patch (`tools/coccinelle/hash.cocci`) enforces this conversion, preventing future regressions.

The motivation is straightforward: the series aims to make the hash API more robust, and this patch is the foundational step. By standardizing on the wrapper, Peff can later add logic (like the `active` flag introduced in patch 4) without worrying about callers bypassing it. The conversion is purely mechanical, with no behavior changes expected. The Coccinelle rule is a strong signal that this is a one-time cleanup, not an ongoing maintenance burden.

This patch is well-crafted and uncontroversial. The commit message clearly explains the goal, and the Coccinelle rule ensures the change is both thorough and enforceable. If you’re tracking the `git_hash_*()` refactoring effort, this is the baseline for the rest of the series.