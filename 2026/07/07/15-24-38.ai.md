This is a **substantive review** of the first patch in Patrick Steinhardt’s two-patch series fixing a quadratic performance regression in the reftable backend. The reviewer, Patrick Steinhardt himself (responding to a GitGitGadget submission), engages directly with the test design, identifying two specific issues that could skew performance measurements and questioning the necessity of the correctness test.

The review focuses on the performance test (`p1401-ref-store-tombstones.sh`). It notes that the current test measures both ref recreation *and* deletion, which could distort the performance data; using `test_when_finished` to queue cleanups would ensure only the intended operation is benchmarked. It also raises a concern about test isolation: the asymmetric scenario (creating new refs after deleting old ones) runs in the same repository as the previous test, so tombstones from earlier runs may affect later measurements. The reviewer suggests using separate repositories to guarantee clean state.

On the correctness test (`t0610-reftable-basics.sh`), the review questions its value, observing that Git already has extensive tests for ref creation and deletion. While the test verifies visibility after deletion and recreation, the reviewer implies it may be redundant unless it specifically targets tombstone edge cases not covered elsewhere.

The feedback is narrowly scoped to test mechanics and coverage, not the underlying bug or fix. It advances the thread by surfacing concrete, actionable concerns about measurement accuracy and test necessity, which the author will need to address before the series can proceed. No other reviewers have weighed in yet.