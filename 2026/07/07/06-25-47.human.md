This is a brief but consequential reply in Patrick Steinhardt’s ongoing refactoring series to separate repository discovery from setup. The thread is part of a 13-patch effort to introduce a new `struct repo_discovery` that holds discovery results, enabling cleaner separation of phases and eventual consolidation of repository initialization into `repo_init()`.

Patrick responds directly to Justin Tobler’s question about whether applying repository configuration more than once should be considered a bug. His answer is unequivocal: yes, it should. He frames this as a design principle for the series’ long-term goal—every repository should be created exactly once via a constructor that takes all required parameters. While he acknowledges this is not yet implemented ("future music"), the statement establishes a clear architectural invariant for the new `struct repo_discovery` API. This has immediate implications for how future patches (and external callers) must handle repository setup, particularly around edge cases like reinitialization or environment variable overrides.

The reply is short but substantive: it clarifies a key design intent that was only implicit in the original patch. No code changes are proposed here, but the clarification may influence how later patches in the series handle error conditions or state transitions. The tone is matter-of-fact, treating the question as a natural part of the design discussion rather than a critique. For readers tracking the series, this is a small but important signal about the direction of the refactoring.