This is a **brief design clarification** in response to Justin Tobler’s substantive review of patch 5/13 in Patrick Steinhardt’s repository-discovery refactoring series. The email addresses the forward-looking question about whether applying repository configuration more than once should be considered a bug.

Patrick confirms that multi-application of configuration *should* be treated as a bug, aligning with the series’ architectural goal of consolidating repository setup into a single constructor (`repo_init()`). He describes this as a future direction ("future music") rather than an immediate requirement, implying that the current patch does not yet enforce this invariant but lays the groundwork for it. The response is concise and directly answers Justin’s question without introducing new technical details or open questions.

Key context for follow-up discussion:
- **Files/subsystems**: The clarification pertains to `struct repo_discovery` (introduced in patch 5/13) and its role in `setup.c`/`repository.h`.
- **Symbols**: `repo_init()` (target constructor for future consolidation), `struct repo_discovery` (intermediate discovery results).
- **Behavioral invariant**: Single-application semantics for repository configuration, enforced by future patches.
- **Related work**: The series builds on `ps/setup-drop-global-state` and `the_repository` removal efforts, where similar constructor-style initialization is a recurring theme.
- **Implications**: Future patches (or external callers) must avoid reapplying configuration to a `struct repository` after `repo_init()` is called. This may affect edge cases like reinitialization, environment variable overrides, or worktree setup.