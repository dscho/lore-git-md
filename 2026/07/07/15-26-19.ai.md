This is a **substantive review reply** in the ongoing `includeIf.worktree` patch series, specifically addressing the symlink-handling inconsistency that is blocking v7. Patrick Steinhardt responds to Chen Linxuan’s proposed technical solution for aligning `worktree` and `gitdir` behavior.

Patrick endorses Chen’s approach—storing an additional non-realpath worktree path in `struct repository` or `struct config_options`—despite its added complexity. His rationale is user-facing consistency: the current divergence between `gitdir` (which matches both symlinked and real paths) and `worktree` (which resolves symlinks to realpath) is confusing, and he prefers a technically "ugly" solution over an inconsistent user experience. The reply is brief but decisive, signaling that the proposed plumbing changes are acceptable if they resolve the behavioral mismatch.

Key technical details for follow-up discussion:
- **Implementation path**: Chen’s proposal (derive a non-realpath worktree path in `setup_discovered_git_dir()` using `cwd`, worktree-root offset, and `$PWD`) is now the leading candidate for v7.
- **Files likely touched**: `setup.c` (for path derivation logic), `config.c` (for condition evaluation), and possibly `repository.h` (to add the new field).
- **Test impact**: Patrick’s test case (matching both symlinked and real paths) will pass, but additional edge cases (nested symlinks, Windows junctions, `$PWD` manipulation) may need coverage.
- **Risk**: The plumbing changes could affect other repository setup code paths, though Chen’s design aims to minimize disruption.

The discussion remains narrowly scoped to symlink handling; no other aspects of the series are revisited. Junio C Hamano and Phillip Wood have not yet weighed in on this specific proposal, but Patrick’s endorsement suggests v7 is likely to proceed with Chen’s approach unless objections arise. The next step is for Chen to implement the changes and submit v7 for final review.