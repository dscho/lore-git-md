This is the second patch in Kristofer Karlsson’s two-patch bugfix series for a commit-graph performance regression in split commit-graph chains. Junio C Hamano’s reply here is a brief, neutral follow-up to confirm whether the commit OID cited in the commit message (199d452758) is accurate or if it might be a mistaken reference. He notes that the same question applies to the first patch in the series and asks whether the cited commit is real or “a total hallucination.”

The patch itself remains unchanged and uncontested. It fixes the regression with a one-line change in `commit-graph.c`, replacing `g->topo_levels` with `chain->topo_levels` to ensure the `topo_levels` slab is propagated to every layer of the split commit-graph chain, not just the topmost. The test in `t/t5324-split-commit-graph.sh` is updated to expect only 1 DFS step instead of 7, reflecting the restored performance. Taylor Blau has already reviewed the patch as “obviously good,” and no other objections or alternative approaches have been raised. The series is effectively ready for merge once the commit-message clarification is resolved.