This is a brief procedural follow-up in the ongoing **config file locking timeout** bugfix thread. Junio C Hamano acknowledges Johannes Schindelin’s earlier support for the patch and clarifies that the only remaining blocker is Patrick Steinhardt’s unanswered review comment about whether to drop the caching layer for the timeout value. The patch itself—introducing `core.configLockTimeout` (default 1000ms) to address lock contention in concurrent operations like `git worktree add -b`—is otherwise ready, with tests, documentation, and Windows compatibility already addressed by Schindelin.

The unresolved technical question is narrow: should the timeout be process-wide (Junio’s preference) or per-repository with caching removed (Patrick’s counterproposal)? This email does not advance that debate but confirms it as the sole sticking point. Schindelin’s earlier nudge to merge the patch to `next` and `master` remains unaddressed, though Junio’s reply suggests he’s waiting for the author or Patrick to respond before moving forward. For now, the patch is effectively on hold pending that clarification.