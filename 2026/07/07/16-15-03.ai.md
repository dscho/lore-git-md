This is the second patch in Jeff King’s seven-patch series refactoring the `git_hash_*()` API. Junio C Hamano replies to confirm the patch is straightforward and uncontroversial.

The patch mechanically converts all remaining direct calls to the hash-algo vtable’s `update_fn()`, `final_fn()`, `final_oid_fn()`, `clone_fn()`, and `discard_fn()` into the corresponding `git_hash_*()` wrapper functions. It touches `builtin/submodule--helper.c`, `t/helper/test-synthesize.c`, and adds a Coccinelle semantic patch (`tools/coccinelle/hash.cocci`) to enforce consistent use of the wrappers. The conversion allows dropping an unused `algo` parameter from `write_uncompressed_zlib()` in `test-synthesize.c` and eliminates the need to pass around the `algo` pointer separately, simplifying the code and reducing the risk of mismatched `algo` and `ctx.algop` values.

No behavior changes are introduced; the patch is purely preparatory for later changes, particularly the idempotent `git_hash_discard()` in patch 4. The Coccinelle rule is comprehensive and will prevent future regressions by making the wrappers the only allowed interface. Junio’s review is surface-level, noting only that the patch is "very straight-forward" and does not raise any technical concerns. This aligns with the thread’s current status: all substantive feedback has been addressed, and only cosmetic issues remain in the series. The patch is ready for integration.