Thread identity: patch series, v1, 1/13, originating author Patrick Steinhardt, thread type refactoring of repository setup logic.

Core problem or goal: improve clarity and consistency of the repository setup API by renaming `check_repository_format_gently()` to `read_and_verify_repository_format()` and reordering its parameters. The old name obscured the function’s dual role of reading the repository format from disk and then verifying it. This is the first step in a larger refactoring that will split repository discovery and setup logic.

Current status: under review, v1 posted. No prior versions. The patch is mechanical, confined to `setup.c`, and changes only the function definition and its seven call sites. No behavior change; purely an API clarity improvement.

Key technical details: function renamed from `check_repository_format_gently()` to `read_and_verify_repository_format()`, parameter order changed to place `struct repository_format *` first, file `setup.c` modified, new symbol `read_and_verify_repository_format()`, old symbol `check_repository_format_gently()` removed. No on-disk format changes, no new CLI options, no new test files.

Open questions and loose ends: none yet; the patch is straightforward and unlikely to be controversial. The series as a whole may raise questions in later patches, but this first step is purely preparatory.

Participant positions: none yet; no reviews or replies recorded.

Related work: part of a 13-patch series refactoring repository discovery and setup logic. No external dependencies or related in-flight topics mentioned.