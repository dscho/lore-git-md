This is the **final patch (3/3) in v6 of the `git replay --linearize` series**, now being revised in response to Junio C Hamano’s review. The patch introduces the `--linearize` option to flatten commit topology into a single linear history, documents its behavior, and expands test coverage. This email is Toon Claes’s reply to Junio’s process and technical concerns, proposing two key changes for the next version (v7).

Toon accepts Junio’s suggestion to **take sole authorship** of the patch, removing Johannes Schindelin as a co-author. He adds a `Based-on-patches-by: Johannes Schindelin` trailer to acknowledge the original work while clarifying responsibility for the v6 changes. This addresses Junio’s process-level question about accountability for latent bugs in this round.

More substantively, Toon **removes the blanket incompatibility between `--linearize` and `--revert`**. He confirms through testing that the two options can be combined safely, contradicting the earlier documentation claim. This resolves Junio’s technical concern about the rationale for the restriction. The change is straightforward: the restriction will be dropped in v7, allowing users to revert commits while linearizing history.

The patch remains a **bugfix and documentation update** for the already-merged v5, but these adjustments simplify the interface and align it more closely with user expectations. The core design—flattening all revision ranges into a single linear sequence, dropping merge commits, and preserving both sides of divergent merges—is unchanged. The expanded test coverage in v6 (140+ lines added) already verifies edge cases like divergent merges and root commits, and this revision will not require new tests.

**Key technical details for follow-up discussion**:
- **Authorship**: Toon Claes sole author in v7, with `Based-on-patches-by: Johannes Schindelin`.
- **CLI change**: `--linearize` and `--revert` are now compatible.
- **Files touched**: `Documentation/git-replay.adoc` (removes incompatibility note), `replay.c` (no code changes needed), and commit message (updates rationale).
- **Symbols**: No new functions or data structures; the change affects only documentation and option parsing.

The series is now **ready for re-merge** once v7 is posted, with no outstanding design or correctness issues. The philosophical debate about CLI consistency (vs. `git rebase`) remains unresolved but is documented in the commit message.