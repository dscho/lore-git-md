This is the maintainer's reply in the thread about Kristofer Karlsson's two-patch bugfix series for a commit-graph performance regression in split commit-graph chains. Junio C Hamano acknowledges Kristofer's confirmation that the commit OID `199d452758` is correct but the date in the commit message was wrong (September 2025, not April 2025). He playfully suggests using `git show -s --pretty=reference` as a template for future commit references to avoid such errors, then marks the topic as "Expecting a reroll" — meaning he is ready to merge the series once Kristofer posts the corrected v2.

The email is purely procedural and resolves the last open question from the maintainer. No technical concerns remain; the series is uncontested and the fix (a one-line correction in `commit-graph.c` to propagate the `topo_levels` slab to all layers of a split commit-graph chain) is approved by both Junio and Taylor Blau. The test in `t/t5324-split-commit-graph.sh` is already updated to verify the fix. The regression, introduced by `ps/commit-graph-via-source` in September 2025, degrades incremental commit-graph write performance from 233ms to 4133ms on large repositories; the fix restores the original behavior. Key files: `commit-graph.c` and `t/t5324-split-commit-graph.sh`. No on-disk format changes. The series is ready for merge once the commit-message correction is posted.