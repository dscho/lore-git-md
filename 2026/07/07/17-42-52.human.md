This is the second patch in Kristofer Karlsson’s two-patch bugfix series for a commit-graph performance regression in split commit-graph chains. The patch itself is a one-line fix that corrects a loop variable in `commit-graph.c` to ensure the `topo_levels` slab is propagated to all layers of the chain, restoring the original performance (from 4133ms back to 233ms for incremental writes on large repositories). The accompanying test in `t/t5324-split-commit-graph.sh` flips from `test_expect_failure` to `test_expect_success` to verify the fix.

The only remaining issue is a minor commit-message nit: the cited regression commit `199d452758` exists, but its date was misstated as April 2025 instead of September 2025. Kristofer acknowledges the error and plans to correct or remove the date in the next iteration. Taylor Blau already approved the fix as “obviously good,” and Junio raised no technical objections—only the clarification request about the commit reference. The series is uncontested and ready for merge once the commit message is tidied up.