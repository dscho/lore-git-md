This thread is Junio C Hamano’s periodic "What’s cooking in git.git" report for July 2026, tracking the status of 55 in-flight topics across Git’s integration branches. The report itself remains unchanged from the prior update, with 14 topics graduated to `master`, 6 new, 2 stalled, and 33 cooking. The key development is a procedural clarification around two commit-reach topics: Kristofer Karlsson’s `kk/merge-base-exhaustion` (a 10-patch optimization series) is blocked until his `kk/commit-reach-find-all-fix` (a 2-patch bugfix) lands, due to a dependency chain involving `kk/paint-down-to-common-optim` and `kk/commit-reach-optim`.

Junio’s latest response sidesteps the specific blocker but provides general guidance for handling patch dependencies: contributors should base their work on `master` and explicitly merge prerequisite branches into their topic branch before submission, rather than relying on integration branches like `next`. This workflow keeps the dependency graph transparent and avoids rebasing integration branches. While this doesn’t resolve the immediate blocker for `kk/merge-base-exhaustion`, it establishes a standard practice for future dependency chains. The two stalled topics (HTTP redirect fix and config lock timeout) remain unchanged, awaiting author responses. No new Reviewed-by or Acked-by trailers have been added, and the procedural constraint for the commit-reach series persists until Kristofer acts on Junio’s guidance.