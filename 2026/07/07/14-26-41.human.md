This is the final review exchange in Jeff King’s seven-patch series hardening the `git_hash_*()` API. Patrick Steinhardt points out a single typo in the commit message: the function name `git_init_hash()` should be `git_hash_init()`. The rest of his reply is a brief, matter-of-fact agreement with the design choice to skip defensive checks in `git_hash_init()` itself, since the function’s contract allows the struct to be in an undefined state beforehand. No substantive discussion or objections are raised; the series appears to be converging.