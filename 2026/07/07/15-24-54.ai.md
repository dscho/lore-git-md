This is a **substantive review reply** in the reftable backend performance regression thread. Patrick Steinhardt (the original series author) responds to Kristofer Karlsson’s patch that removes the `suppress_deletions` flag from the merged iterator. The key new point is the **downstream compatibility risk**: the flag is still used by libgit2, so outright removal would break external consumers of the reftable library. Patrick otherwise confirms the patch’s logic—every call site now handles tombstones explicitly after bounds checks—but does not yet propose an alternative that preserves the flag for external users. The thread’s next step is likely a v2 that either keeps the flag (with internal callers opting out) or introduces a new API to avoid breaking libgit2. No other reviewers have weighed in yet.