This is the **reply from Patrick Steinhardt** addressing Justin Tobler’s review of the third patch in his 13-patch series to refactor Git’s repository discovery and setup logic. The patch unifies shallow file configuration by moving it into `apply_repository_format()`, and Justin’s review raised two open questions: the order of precedence for shallow file settings and the use of environment variables for global state.

Patrick clarifies that the `--shallow-file` command-line option already overrides the `GIT_SHALLOW_FILE` environment variable in the existing code, and this patch does not change that behavior. The override logic was previously handled by a parameter in `set_alternate_shallow_file()`, which is now redundant and can be removed. He acknowledges that the commit message should have mentioned this detail and agrees it warrants an update.

On the broader design question, Patrick concedes that using environment variables for global state is not ideal but defends the approach as consistent with how other global options (like object directories) are handled. He notes that a more ambitious refactoring of argument handling in `git.c` to avoid global state entirely would be a larger effort, hinting that this patch is a pragmatic step rather than a perfect solution.

The exchange is **constructive and low-stakes**: Justin’s questions were technical and forward-looking, and Patrick’s replies address them directly without defensiveness. The patch itself remains uncontroversial, but the discussion highlights minor improvements (e.g., updating the commit message) that could make the change clearer to future reviewers. This is a typical example of a **well-executed refactoring patch**—small, focused, and part of a larger architectural effort—with no red flags for reviewers. The series’ goal of separating discovery from setup continues to advance, and this patch is a straightforward step toward that goal.