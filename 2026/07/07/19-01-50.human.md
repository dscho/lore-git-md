This is a bugfix for terminal compatibility in Git’s sideband protocol. The thread is now in its final stages: Mantas Mikulėnas (the original patch author) replies to Junio C Hamano’s mild skepticism about the project’s whitelist-based design for parsing ANSI escape sequences.

Mantas clarifies that the patch exhausts all valid cases for SGR (Select Graphic Rendition) sequences—there are no other sub-parameter formats beyond the colon-separated decimals now supported. He argues that the current whitelist is already permissive about numeric values, so the change doesn’t introduce new risk. For other escape sequences (e.g., cursor movement, clipboard access, or hyperlinks), he suggests the opposite approach: a strict blacklist would be impractical because the set of potentially harmful sequences is vast, while the set of "good" sequences is nearly empty. The code already filters out most dangerous sequences by only accepting those starting with CSI ("ESC ["), and Mantas muses that even cursor-movement sequences could be disallowed entirely, leaving only SGR and EL (clear line) as safe for sideband status output.

Junio’s earlier question about whether Johannes Schindelin’s review should count as an Acked-by is now moot—Schindelin explicitly confirmed it as such in the parent email. The patch itself is uncontroversial, well-motivated, and ready to merge. The only lingering discussion is philosophical (whitelist vs. blacklist), not technical, and doesn’t block integration. Expect this to appear in Junio’s next "What's cooking" report as a candidate for `next`.