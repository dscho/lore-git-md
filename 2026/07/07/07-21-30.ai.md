This is patch 11/13 in Patrick Steinhardt’s refactoring series to separate repository discovery from setup. The patch removes a redundant assignment of `startup_info->have_repository` in `init_db()`.

The change is mechanical: `startup_info->have_repository` is set twice in `init_db()` (once before and once after reading the repository format), but the first assignment is unnecessary because the repository format configuration does not depend on it. The patch deletes the first assignment and cleans up nearby formatting. The diff shows a single line removed (`startup_info->have_repository = 1;`) and minor whitespace adjustments in `setup.c`.

This is a small, uncontroversial cleanup that aligns with the series’ broader goal of reducing scattered state. The redundant assignment was likely a leftover from earlier code evolution, and its removal has no behavioral impact. The patch touches only `setup.c` and introduces no new symbols or dependencies. No review feedback has been recorded yet, but the change is straightforward and unlikely to raise concerns. It is a logical follow-up to earlier patches (e.g., 5–10/13) that introduced `struct repo_discovery` and consolidated repository setup logic.