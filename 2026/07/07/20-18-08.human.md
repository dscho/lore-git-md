This is Jeff King’s reply to Junio C Hamano’s review of the fourth patch in his seven-patch series refactoring the `git_hash_*()` API. The patch makes `git_hash_discard()` idempotent by adding an `active` flag to the `git_hash_ctx` struct, allowing cleanup code to safely discard a hash context without tracking whether it was already finalized.

Peff clarifies the design intent behind the patch, confirming that the goal is indeed to allow `git_hash_discard()` to be called even after `git_hash_final()`. He acknowledges that the example in the commit message might have been misleading, as it could be fixed with an early return, but explains that real-world cases are more complex, often split across multiple functions where the cleanup code doesn’t know whether the hash was finalized. He provides a more realistic example involving a `foo` struct with an embedded `git_hash_ctx`, where `foo_release()` doesn’t know if the hash was finalized or not.

Peff also addresses Junio’s question about the asymmetry between `git_hash_discard()` and `git_hash_final()`, confirming that it is intentional: `git_hash_final()` must not be called multiple times, as it is a bug to do so, while `git_hash_discard()` is now safe to call redundantly. The discussion reinforces that the patch is technically sound and that the design choices are deliberate. No code changes are proposed or needed in response to this feedback, and the patch remains ready for integration.