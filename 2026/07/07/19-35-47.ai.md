This is Junio C Hamano's reply in the `git replay --linearize` thread, responding to Toon Claes's proposal to remove the mutual exclusivity between `--linearize` and `--revert` in v7. The email is a **substantive review** that engages with the technical implications of combining the two options.

Junio acknowledges Toon's point that the restriction appears unnecessary for the simple case where all relevant commits (including the merge base) are within the replay range. However, he raises a **potential edge case**: what happens when the linearized range contains the merge commit (`M`) and one side of the merge (`A`), but *not* the other side (`B`) or the merge base (`O`)? In this scenario, the replay machinery might not have enough context to correctly invert the commits, leading to undefined or confusing behavior. Junio suggests that the current implementation may not reliably distinguish this case from the "safe" scenario where all commits are included, and he cautions against lifting the restriction unless the code can consistently handle such cases in a way that is both logical and easy to explain.

The email does not reject Toon's proposal outright but instead frames the decision as a trade-off between simplicity and correctness. Junio's tone is collaborative, leaving room for Toon to either proceed with the removal (if he can demonstrate the edge case is handled) or defer the change to a future iteration. This is a **tested-level review** in spirit, as Junio is implicitly asking for verification that the edge case is covered, though he does not explicitly state he has tested it himself.

Key technical details for follow-up:
- **Edge case**: Replaying a range that includes a merge commit (`M`) and one side of the merge (`A`), but excludes the other side (`B`) and the merge base (`O`).
- **Subsystem**: `replay.c`, specifically the logic in `replay_revisions()` that handles commit mapping and parent resolution.
- **Behavioral question**: Whether the replay machinery can reliably detect and handle partial merge ranges when `--revert` and `--linearize` are combined.
- **Files touched**: No code changes yet, but the discussion affects the design of the `--linearize`/`--revert` interaction in `replay.c` and its documentation.
- **Symbols**: The `replayed_commits` map and `get_mapped_commit`/`put_mapped_commit` helpers (introduced in earlier patches) are likely involved in resolving the edge case.

The email advances the thread by shifting focus from the procedural (authorship, option naming) to the **algorithmic**: whether the replay machinery can safely handle partial merge ranges. This is a **new point of contention** that was not addressed in the v6 tests or documentation. The outcome will determine whether v7 retains the restriction (with a clearer rationale) or removes it entirely.