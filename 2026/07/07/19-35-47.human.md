This is the latest exchange in the long-running `git replay --linearize` series, now in its seventh iteration. Junio C Hamano and Toon Claes are finalizing the interface: they agree to **remove the mutual exclusivity between `--linearize` and `--revert`**, allowing users to revert commits while flattening history. The change is straightforward—no code surgery needed, just a documentation tweak—and addresses Junio’s concern that the restriction was never technically justified.

The discussion centers on a concrete example: reverting a merge commit `M` (with parents `A` and `B`) and its side branch. Toon’s testing confirms the two options can coexist safely, producing the expected linearized result `O->A->B->M->C->B'->A'`. Junio’s reply is cautious but pragmatic: if the code can’t reliably distinguish edge cases (e.g., partial ranges that omit one parent), deferring the restriction is the safe choice. The upshot is that v7 will drop the restriction entirely, simplifying the interface without introducing new complexity.

This email is a **green light** for the series. The only remaining work is mechanical: update the man page to remove the incompatibility note, adjust the commit message to reflect the new behavior, and repost as v7. The core design—flattening all replayed commits into a single linear topology, dropping merges, and preserving both sides of divergent branches—is unchanged and has survived six rounds of review. Junio’s earlier feedback on authorship (Toon taking sole credit for patch 3/3) and test coverage (already beefed up in v6) has also been addressed. The series is now **technically complete** and ready for Junio’s integration branch.