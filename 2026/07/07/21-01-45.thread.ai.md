Thread identity
Subject: [PATCH v2] unpack-trees: fix quadratic-time index scan in `next_cache_entry()`
Series version: standalone patch, v2
Originating author: Henrique Ferreiro (via GitGitGadget)
Thread type: performance bugfix

Core problem or goal
Eliminate a quadratic-time index scan in `next_cache_entry()` triggered when `git diff` is run with a pathspec matching a subtree whose entries appear early in the index. The fix introduces a `cache_bottom` field in `o->internal` to track forward-scan progress, allowing subsequent calls to skip already-unpacked entries.

Current status
Under review; v2 posted in response to Junio’s substantive review. Junio has now explicitly acknowledged that Henrique’s argument about the safety of the new `cache_bottom` optimization—specifically, that `find_cache_pos()` already advances `.cache_bottom` past unpacked entries (citing commit `e53e6b4433`)—is "sensible." This resolves the critical correctness concern raised in Junio’s earlier review. The patch remains self-contained (4 lines in `unpack-trees.c`) and includes a perf regression test (`t/perf/p0009-diff-pathspec.sh`). The only remaining action item is to scale the perf test’s entry count via the `EXPENSIVE` prerequisite, as suggested by Junio.

Key technical details
- **File modified**: `unpack-trees.c`
- **Function modified**: `next_cache_entry()`
- **New field**: `o->internal.cache_bottom` (tracks forward-scan progress)
- **Existing field**: `o->cache_bottom` (handles backward scans and ordering mismatches)
- **Test added**: `t/perf/p009-diff-pathspec.sh` (creates 100k-entry index with problematic layout; entry count should be configurable via `EXPENSIVE`)
- **Benchmark**: 100k-entry index, pathspec `aaa/file` as first entry, command `git diff HEAD -- <pathspec>`
- **Performance improvement**: 8 minutes → 0.07 seconds (99.7% speedup) on Chromium-scale repo (~500k index entries)
- **Base commit**: `e90019fcafe0040228b8631c30f97ae1adb61bcd`
- **Safety argument**: The optimization preserves ordering invariants because `find_cache_pos()` advances `.cache_bottom` past unpacked entries (commit `e53e6b4433`)

Open questions and loose ends
1. **Test improvement**: The perf test (`t/perf/p0009-diff-pathspec.sh`) should scale entry count via the `EXPENSIVE` prerequisite, as suggested by Junio.

Participant positions
- **Henrique Ferreiro**: Author; argued the optimization is safe because `find_cache_pos()` advances `.cache_bottom` past unpacked entries (citing commit `e53e6b4433`). Addressed Junio’s correctness concern and has one remaining surface-level suggestion (scaling perf test entry count via `EXPENSIVE`) to incorporate.
- **Junio C Hamano**: Substantive review identifying a **critical correctness concern** (interaction between forward/backward scan logic) and a **surface-level suggestion** (scaling perf test entry count via `EXPENSIVE`). The correctness concern is now resolved; the patch’s readiness hinges on incorporating the `EXPENSIVE` test tweak.

Related work
- Historical use of `.cache_bottom` to handle index/tree ordering mismatches (commit `e53e6b4433`). The new optimization preserves this invariant.