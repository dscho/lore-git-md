This thread is a cleanup series from Jeff King (Peff) that refactors the `git_hash_*()` API to make it more robust and consistent. The series addresses a long-standing issue where `git_hash_discard()` was not idempotent, risking inconsistent hash context states if called multiple times. Peff’s solution introduces an `active` flag to make `git_hash_discard()` safe to call repeatedly, standardizes usage through wrapper functions, and adds defensive runtime checks to catch misuse early. The series touches 30 files, including core object-writing code, built-ins, and test helpers, but makes no user-visible or on-disk format changes.

The series is now complete and ready for integration. Patrick Steinhardt reviewed the patches and, after minor typo fixes, declared himself "happy with this series." Junio C Hamano’s recent feedback on the first patch was purely stylistic—pointing out unnecessary braces in Coccinelle-generated code—rather than a substantive critique. No further changes are expected unless Junio raises additional concerns during integration. The patches are structured to build on each other, with the Coccinelle semantic patch (introduced in patch 1) enforcing the new invariants and preventing future regressions. All prior feedback has been addressed, and the series is considered mechanically sound.