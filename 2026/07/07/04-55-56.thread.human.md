This thread is a cleanup and refactoring series from Jeff King (Peff) that makes the `git_hash_*()` API more robust and consistent. The series is now at its second version (v2) with seven patches, all of which have addressed review feedback. The core goal is to fix unsafe usage patterns in the hash API, particularly the non-idempotent behavior of `git_hash_discard()`, which could leave contexts in an inconsistent state if called multiple times. The series also standardizes usage by replacing direct calls to low-level hooks like `init_fn()` with public wrapper functions, improving documentation, and adding defensive runtime checks to catch misuse early.

The key technical change in this latest patch (the fourth in the series) is making `git_hash_discard()` idempotent by adding an `active` flag to the `git_hash_ctx` struct. This ensures that redundant calls to `git_hash_discard()` return early rather than causing undefined behavior or double-frees. The change is internal and doesn’t affect user-visible behavior, but it simplifies caller logic and reduces the risk of subtle bugs. The series has touched 25 files across core subsystems like object-writing, built-ins, and test helpers, with no on-disk format changes or regressions expected.

All review feedback has been addressed, including cosmetic fixes and improved documentation. Junio C Hamano and Patrick Steinhardt provided surface-level reviews, while Brian M. Carlson contributed technical context on hash-library conventions, reinforcing the design choices. The series is now ready for integration, with no open questions or objections remaining.