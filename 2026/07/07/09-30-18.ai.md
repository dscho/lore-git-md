This is a **substantive review follow-up** in the `git history squash` series, where Phillip Wood identifies a **critical input validation gap** in how the command handles `rev-list` options passed after `--`. The series (v7) is otherwise complete and marked "Will replace" by Junio, but this issue threatens correctness: if a user runs `git history squash -- --reverse HEAD~3..HEAD`, the resolved range’s parent commit could be wrong, leading to an incorrect squash.

Phillip’s key observation is that the current implementation **does not sanitize options after `--`**, even though it blocks unknown options before `--`. This inconsistency means `--reverse` (and other `rev-list` options) can still distort the resolved range, violating the command’s core assumption that the first commit returned by `get_revision()` is the oldest in the range. He references `git replay`’s approach—passing `PARSE_OPT_KEEP_UNKNOWN_OPT` to `parse_options()` and then validating `struct rev_info` members—as the correct way to handle arbitrary `rev-list` options while ensuring structural invariants (e.g., topological order, no boundary commits).

The email also critiques the **cover letter’s lack of transparency**: it omits discussion of why certain suggestions (like `rev-list` option support) were rejected or deferred, making it hard for reviewers to track design decisions. This is a recurring pain point in long-running series, where incremental changes can obscure the bigger picture.

**What’s new here**: Phillip’s feedback exposes a **regression risk** in the input validation logic, which Harald’s earlier response (in the parent email) underestimated. The fix will require either (a) adopting `git replay`’s full validation approach or (b) explicitly rejecting all `rev-list` options (including those after `--`). The thread summary notes Harald later agreed to adopt `git replay`’s approach, but this email is the first concrete evidence of the problem.

**Impact**: Without this fix, the command could produce **silently incorrect results**—a serious flaw for a feature intended to simplify history rewriting. The series is otherwise ready for integration, but this issue must be addressed in v8. Phillip’s review is **technically precise** and **actionable**, focusing on a real edge case rather than stylistic preferences.