This is a CI/build system patch correcting a mislabeled commit. Johannes Schindelin acknowledges Junio C Hamano’s feedback that the original subject line (“reduce the PID limit”) was inaccurate: the patch actually *raises* the PID limit from the default to 16,384 to fix test failures in private GitHub repositories. The code change itself remains unchanged—it still applies the container options (`--pids-limit=16384`, `--ulimit nproc=16384`, `--ulimit nofile=32768`) only when the repository is private, and 16k is still the benchmarked sweet spot. A v2 will be sent with the corrected subject line and no other modifications.