This is a **lightweight technical clarification** in the thread about the `SVN` test prerequisite logic. Jeff King (Peff) acknowledges Junio C Hamano’s explanation of why the Perl version check (`$SVN::Core::VERSION gt '1.1.0'`) behaves unexpectedly: Perl treats dotted numbers as ASCII character sequences when compared with `gt`, not as numeric versions. This confirms the root cause of the shell-quoting bug Peff identified earlier, where the check fails to skip tests when SVN is unavailable (e.g., `1.14.5` is lexicographically *less* than `1.2.0`).

The email adds no new action items or technical proposals—it simply closes the loop on the Perl behavior question. The follow-up patch to simplify the `SVN` prerequisite (removing the version check entirely) remains the next step, as previously agreed. For a future AI summarizer, note that this exchange solidifies the rationale for the prerequisite fix and reinforces that the version check is fundamentally broken. No substantive discussion or controversy here; just a tidy resolution of a minor technical curiosity.