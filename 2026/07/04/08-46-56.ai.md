This is a CI/build system patch correcting a commit message error in Johannes Schindelin’s fix for GitHub Actions CI failures in private repositories. The patch itself remains unchanged: it conditionally raises Docker container PID, process, and file descriptor limits (`--pids-limit=16384`, `--ulimit=nproc=16384`, `--ulimit=nofile=32768`) for private repositories only, addressing the "cannot create async thread: Resource temporarily unavailable" error that consistently fails tests in the t5xxx-t6xxx range. The technical rationale—16k as the optimal trade-off between stability and performance (64k caused 2-2.5× longer runtimes)—is uncontested and unchanged.

The update is purely cosmetic: the subject line will change from "reduce the PID limit" to "raise the PID limit" to accurately reflect the patch’s effect. No code, workflow logic, or benchmarking data is modified. The patch continues to target `.github/workflows/main.yml` and applies only when `github.repository_visibility` is private, ensuring no impact on public repositories or Git’s core functionality.

Junio C Hamano’s feedback (surface-level review) identified the subject line’s inaccuracy, which Schindelin acknowledges as a remnant of an earlier, disproven hypothesis. The thread remains focused on commit message clarity; no substantive objections or testing feedback have emerged. Key technical details for follow-up: the conditional container options, the 16k limit, and the private-repository-specific scope. The patch is ready for v2 submission with the corrected title.