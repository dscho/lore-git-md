This is a procedural follow-up in the closed thread about silencing translation-catalog build output during `make -s` for gitk and git-gui. The author, Harald Nordgren, asks Junio C Hamano for an update on the status of the series, which was previously confirmed as merged into Johannes Sixt’s testing branches and slated for upstream Git proposal in the coming weeks.

The email itself adds no new technical content. It simply requests clarification on the current state of the patches, which have already been fully implemented in Sixt’s `j6t-testing` branches (gitk) and `hn/silence-make-s` branch (git-gui). The thread remains procedurally closed; no further discussion or objections are expected. The key details—files touched (`gitk-git/Makefile`, `git-gui/Makefile`), the behavior change (silent `make -s` builds), and the integration timeline—were established in prior messages. This follow-up is likely a routine check for confirmation rather than a signal of unresolved issues.