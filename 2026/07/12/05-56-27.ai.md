This is a **surface-level review reply** in the `git log --graph` cascading indentation thread. Chandra Pratap responds to Mirko Faina’s bounded zig-zag counter-proposal with two brief objections: first, that tying graph-drawing logic to specific formatting flags (like `--oneline`) would miss custom formats that are functionally single-line, and second, that a zig-zag pattern could reintroduce ambiguity by visually mimicking merge topology. He sides with Pablo Sabater’s original position that the feature should default to on, with an opt-out flag (`--no-graph-indent`) to preserve decoupling between graph logic and formatting. The reply adds no new technical detail or counter-proposal; it simply restates the existing opt-out preference and flags potential edge cases without demonstrating them. The weight of this feedback is low.