This is a procedural update in the already-closed thread about silencing translation-catalog build output during `make -s` for gitk and git-gui. Junio C Hamano confirms the current status: Johannes Sixt (J6t) intends to send a pull request for the changes after the v2.55 release is tagged, as previously communicated. The email adds no new technical details—it simply reiterates the timeline already established in the thread. The patches (targeting `gitk-git/Makefile` and `git-gui/Makefile`) are fully implemented in Sixt’s testing branches, and the goal of silent `make -s` builds is achieved. No further discussion or objections are expected; this is a routine confirmation of the integration schedule.