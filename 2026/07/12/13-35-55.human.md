This is a build system fix for a sparse compilation error in Patrick Steinhardt’s ongoing `ps/libgit-in-subdir` refactoring. The patch itself is a one-line Makefile tweak that adds the missing `lib/` prefix to the `pack-revindex.sp` target, silencing a sparse warning about a large `memset` in `lib/pack-revindex.c`.

Ramsay Jones originally sent the patch as a standalone fix, but in this reply he acknowledges that the commit it referenced (9759608622) is no longer reachable because the topic is being rewritten. He agrees with Johannes Schindelin’s suggestion to refer to the topic by name (`ps/libgit-in-subdir`) instead of by commit ID. The current commit that introduced the move is 8da3a2c01822, which is still in `seen` and subject to squashing.

The fix is uncontroversial and has been confirmed correct by Johannes. No new objections or questions have been raised; the thread is now waiting for Patrick Steinhardt to either re-roll the topic or apply the fix directly. If you are tracking the `lib/` directory reorganization, this is a minor but necessary housekeeping patch.