Thread identity: RFC proposal for a standardized debug-commit mechanism in Git. Series version: none (initial proposal). Originating author: Simon Richter. Thread type: feature request / RFC.

Core problem or goal: Provide a standardized way for developers to mark debug commits (e.g., temporary `printf` statements) so that Git's autosquash feature can automatically drop them during interactive rebase (`git rebase -i`). The goal is to streamline history cleanup before submission and prevent accidental inclusion of debug code in shared branches via push hooks or CI checks.

Current status: Initial proposal stage; no implementation, prior discussion, or related efforts identified. No agreement or decisions yet. The proposal is lightweight, targeting only interactive rebase and client-side hooks without altering core Git behavior or on-disk formats.

Key technical details: Proposed mechanism would likely involve a new commit message prefix (e.g., `discard!`) to signal debug commits for automatic removal. May include a configuration knob to enable/disable the feature. No new symbols, files, or subsystems identified yet. No changes to on-disk formats or test coverage discussed.

Open questions and loose ends: No specific technical objections or requests for changes raised yet. The proposal is conceptual and lacks concrete implementation details, leaving questions about:
- The exact commit message convention (e.g., `discard!` vs. another prefix).
- How autosquash would recognize and handle these commits (e.g., via a new `--autosquash-discard` flag or integrated into existing autosquash logic).
- Whether this would be opt-in or opt-out (e.g., via configuration).
- How push hooks or CI checks would enforce this (e.g., rejecting commits with the debug prefix).
- Potential edge cases (e.g., conflicts during rebase, interaction with other autosquash patterns like `fixup!`).
- Backwards compatibility concerns (e.g., existing workflows that might conflict with the proposed prefix).

Participant positions: None yet; this is the thread root. No reviews, objections, or endorsements recorded.

Related work: None mentioned in the email. No prior versions or external dependencies identified. The proposal is self-contained and does not reference other in-flight topics.