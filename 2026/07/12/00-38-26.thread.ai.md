Thread identity
Subject: [PATCH v3] sequencer: respect --empty for autosquash-induced empty commits
Series version: v3 (standalone patch)
Originating author: Farid Zakaria
Thread type: bugfix patch

Core problem or goal
Fix inconsistent handling of empty commits in `git rebase --autosquash`: when a `fixup!` or `squash!` operation cancels out its target commit, the resulting empty commit is currently *always* stopped at, ignoring the `--empty` option (drop/keep/stop). The patch aims to make autosquash respect `--empty` for these emptied commits, enabling workflows like reverting a commit via `fixup!` and having `--empty=drop` remove both the original and its revert. The goal includes distinguishing between commits that *become* empty during rebase (which should respect `--empty`) and commits that *were already empty* (which should not be dropped), and handling edge cases around conflict resolution, multiple fixups, and missing pick commands.

Current status
Under review; no maintainer decision. v3 is the latest posted version, but Phillip Wood’s substantive review identified edge cases and implementation refinements requiring a v4. The patch remains blocked on two factors: (1) the sequencing dependency (`pw/rebase-drop-notes-with-commit`), which Phillip Wood now expects to graduate to `next` imminently (his latest version has been posted and he anticipates no further review comments), and (2) Farid’s response to Phillip’s feedback. Junio C Hamano’s recent coordination ping and Farid’s logistical update confirm the topic is still under active consideration but stalled pending Farid’s v4 update. No technical objections to the core idea, but the patch is not yet ready for integration due to unresolved edge cases and the dependency.

Key technical details
- Files touched: `sequencer.c` (core logic), `git-rebase.adoc` (documentation), `t/t3415-rebase-autosquash.sh` (tests).
- New behavior: `do_pick_commit()` must track the original emptiness state of the target commit and distinguish it from emptiness induced by autosquash. It must handle conflict resolution paths (e.g., `git reset HEAD^` vs. `git checkout HEAD^ :/`), multiple fixups targeting the same commit, and missing pick commands (e.g., after `reset`, `exec`, or `break`).
- Implementation refinements: Phillip suggests extending the return enum from `pw/rebase-drop-notes-with-commit` to include a "dropped HEAD" state instead of adding a `dropped` output parameter. Documentation wording ("melded" → "squashed") and test cases must avoid applying `--empty` to commits that were already empty.
- On-disk format: unchanged.
- Symbols: no new CLI options or config keys; leverages existing `--empty` option.
- Test coverage: must be expanded to cover edge cases: commits that were already empty, conflict resolution paths, multiple fixups, missing pick commands, and rewritten commit tracking.

Open questions and loose ends
1. **Edge case handling**: The v3 patch does not address the scenarios raised by Phillip Wood (original commit emptiness, conflict resolution, multiple fixups, missing pick commands, rewritten commit tracking). These must be resolved in v4.
2. **Rebasing workflow**: Farid has clear guidance: watch Junio’s "What's cooking" reports for the dependency’s graduation to `next`, then rebase v4 on the tip of `pw/rebase-drop-notes-with-commit` (currently `7e70d12417d`). No further logistical questions remain.
3. **Implementation details**: The choice between a `dropped` output parameter and extending the return enum remains open, though Phillip prefers the latter. Documentation wording and test cases also require updates.
4. **Author responsiveness**: Farid’s update confirms he will resume work shortly, but no timeline or specific next steps are provided. The patch remains stalled until v4 is posted.

Participant positions
- Farid Zakaria (author): v1 → v2 → v3, incorporating feedback. Now preparing v4 to address Phillip’s edge cases and implementation refinements. Logistical questions about rebasing workflow have been resolved. Confirmed intent to post v4 shortly, but no technical updates or timeline provided.
- Phillip Wood (reviewer): substantive technical review (sequencer correctness, edge cases) completed for v3. Provided logistical guidance on tracking the dependency’s graduation and rebasing workflow. Feedback remains constructive and focused on correctness and consistency with Git’s existing behavior. No objections to the core idea, but the patch requires updates to handle edge cases. Confirmed the dependency should graduate to `next` imminently.
- Junio C Hamano (maintainer): coordination notes on sequencing and progress; no technical objections to the core idea. Recent ping and Farid’s response confirm the topic is still under active consideration but stalled pending Farid’s v4 update.

Related work
Phillip Wood’s `pw/rebase-drop-notes-with-commit` topic (in `seen`, expected to graduate to `next` imminently) is a sequencing dependency. Farid’s patch must be rebased on its tip once it lands. No other dependencies or prior versions in flight.