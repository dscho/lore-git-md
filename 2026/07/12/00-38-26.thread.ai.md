Thread identity
Subject: [PATCH v3] sequencer: respect --empty for autosquash-induced empty commits
Series version: v3 (standalone patch)
Originating author: Farid Zakaria
Thread type: bugfix patch

Core problem or goal
Fix inconsistent handling of empty commits in `git rebase --autosquash`: when a `fixup!` or `squash!` operation cancels out its target commit, the resulting empty commit is currently *always* stopped at, ignoring the `--empty` option (drop/keep/stop). The patch makes autosquash respect `--empty` for these emptied commits, enabling workflows like reverting a commit via `fixup!` and having `--empty=drop` remove both the original and its revert.

Current status
Under review. v3 posted, addressing review feedback from Phillip Wood (sequencer subsystem expert). No maintainer decision yet; no integration branch assignment.

Key technical details
- Files touched: `sequencer.c` (core logic), `git-rebase.adoc` (documentation), `t/t3415-rebase-autosquash.sh` (tests).
- New behavior: `do_pick_commit()` now detects autosquash-induced emptiness by comparing the index against the *parent* of HEAD (not HEAD itself), then applies `--empty` consistently.
- On-disk format: unchanged.
- Symbols: no new CLI options or config keys; leverages existing `--empty` option.
- Test coverage: exhaustive cases for `--empty=drop`, `--empty=keep`, and `--empty=stop` with `fixup!`/`squash!`, including edge cases (e.g., `fixup!` filling an empty commit).

Open questions and loose ends
None. Review feedback from Phillip Wood has been addressed; no outstanding objections or requests.

Participant positions
- Farid Zakaria (author): v1 → v2 → v3, incorporating feedback.
- Phillip Wood (reviewer): substantive technical review (sequencer correctness, edge cases). His feedback improved the patch’s handling of rewrite records and error paths. No objections remain; his review was thorough and constructive.

Related work
None. Standalone patch with no dependencies or prior versions in flight.