This is a build system fix for a sparse compilation error in Patrick Steinhardt’s `ps/libgit-in-subdir` topic. Johannes Schindelin replies to Ramsay Jones’s patch, confirming the fix is correct and clarifying which commit in the topic should absorb it.

The patch itself remains unchanged: it adds the missing `lib/` prefix to the `pack-revindex.sp` target in the Makefile, silencing a sparse warning about a large `memset` in `lib/pack-revindex.c`. Johannes notes that the original commit (9759608622) is no longer reachable from any official branch and suggests referring to the topic by name (`ps/libgit-in-subdir`) instead. He identifies the current commit that introduced the move as 8da3a2c01822, which is still in `seen` and subject to rewriting.

The review is surface-level: Johannes verifies the reasoning and the one-line change, but does not test the build or engage with the broader implications of the `lib/` directory reorganization. No new questions or objections are raised; the thread is now waiting for Patrick to either re-roll the topic or apply the fix directly.