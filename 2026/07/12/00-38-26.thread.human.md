This thread is about a bugfix patch (v3) by Farid Zakaria that makes `git rebase --autosquash` respect the `--empty` option when a `fixup!` or `squash!` operation cancels out its target commit, producing an empty commit. The patch has been under review and was previously considered ready for integration once its dependency—Phillip Wood’s `pw/rebase-drop-notes-with-commit` topic—lands in `next`.

Phillip Wood, a sequencer expert, has now posted a substantive review of v3, identifying several edge cases and implementation refinements needed for correctness. Key issues include: distinguishing between commits that *become* empty during rebase (which should respect `--empty`) and those that *were already empty* (which should not); handling conflict resolution scenarios where users reset or checkout without dropping commits; ensuring proper behavior with sequences of multiple fixups; and tracking rewritten commits accurately when commits are dropped. Phillip also suggests improvements to the patch’s implementation, such as using an extended return enum instead of an output parameter and revising documentation wording for consistency.

The patch is not rejected but will require a v4 to address these concerns. Farid has already indicated he’s ready to rebase and update the patch once the dependency graduates to `next`, and Phillip expects that to happen soon. No other technical objections remain, and the patch’s core goal—making autosquash respect `--empty`—is uncontested. The next steps are for Farid to incorporate Phillip’s feedback and rebase on top of the dependency once it lands.