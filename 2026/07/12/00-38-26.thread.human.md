This thread is about a bugfix patch that makes `git rebase --autosquash` respect the `--empty` option when a `fixup!` or `squash!` operation cancels out its target commit, producing an empty commit. The current behavior always stops at such emptied commits, ignoring `--empty=drop` (or `keep`/`stop`). Farid Zakaria’s v3 patch aims to fix this, but Phillip Wood’s review identified edge cases—like handling commits that were already empty, conflict resolution paths, and multiple fixups—that need addressing in a v4.

The patch is still under review and not yet ready for merging. It depends on Phillip Wood’s `pw/rebase-drop-notes-with-commit` topic, which has now graduated to `next`, so Farid can rebase v4 on its tip once posted. Junio C Hamano recently pinged Farid to check on progress, but no technical updates or timeline for v4 have been provided. The core idea has no objections, but the patch remains stalled until Farid addresses Phillip’s feedback.

Key participants: Farid (author) is preparing v4; Phillip (reviewer) provided detailed feedback and logistical guidance; Junio (maintainer) is tracking progress but hasn’t raised technical concerns. The patch touches `sequencer.c`, updates documentation, and expands test coverage for edge cases. No merge decisions have been made yet.