This is a **surface-level review reply** in the ongoing `git log --graph` cascading indentation thread. Chandra Pratap responds to Mirko Faina’s bounded zig-zag counter-proposal with two brief objections.

First, he points out that tying the indentation logic to specific formatting flags (like `--oneline`) would miss custom formats that are functionally single-line (e.g., `--format="%h %s"`), leaving the same ambiguity unaddressed. Second, he argues that a zig-zag pattern could reintroduce ambiguity by visually mimicking merge topology, making unrelated commits appear connected. His preference remains for Pablo Sabater’s original opt-out design, where indentation is the default and users disable it with `--no-graph-indent`.

The reply adds little new technical insight; it restates earlier positions rather than engaging with the bounded zig-zag’s specific mechanics (e.g., the two-level cap). While polite, the feedback is vague where precision would help—no concrete examples of ambiguity in the zig-zag pattern are provided, and the formatting-flag concern is already acknowledged in the thread. This is a low-weight contribution that does not advance the design discussion.