This is the first patch in the v6 series for the `git replay --linearize` feature, now focused on fixing critical post-merge issues from v5. It introduces a helper function `put_mapped_commit()` to encapsulate the logic for adding entries to the `replayed_commits` khash map, and renames the existing `mapped_commit()` function to `get_mapped_commit()` for consistency.

The patch is a **refactoring** that extracts a small, self-contained piece of logic from `replay_revisions()`. The change is mechanical and does not alter behavior—it simply moves code into a helper function and renames another to create a clearer API (`get_mapped_commit()`/`put_mapped_commit()`). The diff is clean, with no functional changes beyond the refactoring. This patch was already present in v5 and remains unchanged in v6, serving as a prerequisite for the regression fix in patch 2/3.

Key technical details:
- **Functions introduced**: `put_mapped_commit()`, `get_mapped_commit()` (renamed from `mapped_commit()`).
- **Data structure**: `replayed_commits` (khash OID map) used to track rewritten commits.
- **File touched**: `replay.c` only.
- **No behavior change**: This is purely a code organization improvement.

The patch is uncontroversial and well-motivated. It sets the stage for the regression fix in the next patch by making the codebase easier to modify. No review feedback is expected to focus on this patch, as it is a straightforward cleanup. The real discussion will center on patches 2/3 and 3/3, which address the critical issues in the merged v5.