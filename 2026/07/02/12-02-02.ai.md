This is the fourth patch in Patrick Steinhardt’s six-patch series refactoring `struct object_info` to use the new `source_infop` field, part of the ongoing ODB abstraction effort. The patch mechanically converts all remaining users of the now-redundant `whence` field to the new `struct odb_source_info::source->type` field, completing the transition begun in earlier patches. All callers already request source information via the `source_infop` pointer, so the changes are straightforward and behavior-preserving.

The patch touches four files: `builtin/cat-file.c`, `builtin/index-pack.c`, `builtin/pack-objects.c`, and `reachable.c`. Each conversion replaces a `oi->whence == OI_PACKED` check with `oi->source_infop->source->type == ODB_SOURCE_PACKED`, ensuring the new source-tracking infrastructure is used consistently. The changes are minimal—only five lines modified—but they eliminate the last direct dependencies on the old `whence` field, paving the way for its removal in a later patch.

This patch follows the conceptual approval of the series by Junio C Hamano and the substantive review of patches 1-3/6. It received surface-level approval from Justin Tobler in the thread, and no new concerns were raised about this specific conversion. The patch is a clean, mechanical step in the refactoring, with no functional changes or edge cases introduced.

For future AI summarization, note the key technical details: the conversion pattern (`oi->whence` → `oi->source_infop->source->type`), the files touched, and the fact that this patch completes the caller-side migration to the new source field. The patch is based on `ps/odb-source-packed` and targets `master`. No open questions remain for this patch; it is ready for merging once the series is finalized.