This is the second patch in Toon Claes’s nine-patch series aimed at making `GIT_TEST_LONG` tests reliable and efficient in CI. It addresses a specific broken test in `t0021-conversion.sh` that writes a 2 GiB file and round-trips it through clean/smudge filters. The test is broken on 64-bit systems (where `SIZE_T_IS_32BIT` is not set) because it hangs rather than failing cleanly, a behavior that only became visible after CI began running expensive tests. The patch skips the test entirely when `SIZE_T_IS_64BIT` is set, avoiding the hang without attempting to fix the underlying issue, which the commit message describes as difficult to detect reliably.

The change is minimal: it adds `SIZE_T_IS_64BIT` to the test’s prerequisites, effectively skipping it on 64-bit platforms. This is a pragmatic workaround rather than a true fix, but it resolves the immediate CI hang and aligns with the series’ goal of making `GIT_TEST_LONG` tests reliable. The patch touches only the test script and does not affect any production code or user-facing behavior. It is well-motivated by the observed CI failures and clearly explained in the commit message. No controversy or alternative approaches are evident in the thread so far.