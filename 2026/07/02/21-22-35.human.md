This is a review comment from Jeff King (Peff) on the fifth patch in Toon Claes’s nine-patch series, which aims to make `GIT_TEST_LONG` tests reliable and efficient for CI. The patch in question skips an expensive test in `t7508-status.sh` on 32-bit systems, where Git cannot track object sizes larger than 4 GiB due to `size_t` limitations.

Peff’s comment is substantive but narrow: he reports that his own GitHub Actions CI runs—including the `linux32` job—have been passing even after the commit that enabled expensive tests (`7a094d68a2`). This contradicts the patch’s claim that the test hangs on 32-bit systems, suggesting either a misunderstanding of the problem or an unaccounted-for variable in CI environments. His question is direct and technical: why does the test appear to work in his CI when the patch asserts it should fail?

The observation is worth noting because it challenges the patch’s motivation. If the test does not consistently hang on 32-bit systems, skipping it may be premature or overly conservative. Peff does not propose a specific fix or reject the patch, but his comment flags a discrepancy that the author (or series maintainer) will need to address before the patch can be considered robust. No other reviews or responses are recorded yet.