This is the first patch in the v6 series introducing `--linearize` to `git replay`. It is a refactoring patch that extracts a small piece of logic from `replay_revisions()` into a new helper function, `put_mapped_commit()`, and renames the existing `mapped_commit()` to `get_mapped_commit()` for symmetry.

The change is purely internal: no user-visible behavior is altered. The patch touches only `replay.c`, moving eleven lines of code into a helper and updating the one call site. The commit message is concise but clear, and the diff is clean. This refactoring was introduced in v6 to improve readability and maintainability, replacing an earlier enum-to-bool refactoring that had raised readability concerns in prior reviews. The current approach is uncontested and appears to be a straightforward improvement.

This patch is a prerequisite for the core feature in patch 3, but on its own it is a minor, low-risk cleanup. It is unlikely to generate significant discussion, but it sets up the more substantial changes that follow.