This is a bug report about unexpected behavior in `git rm`.

The author, Евгений Плискин, reports that `git rm -n *.json` recursively removes JSON files from subdirectories, despite the documentation stating that `git rm` should not recurse without an explicit directory specification. The issue is observed on Git for Windows 2.55.0.windows.1.

The report is concise and includes a clear reproduction case (the command `git rm -n *.json`), the expected behavior (no recursion), and the actual behavior (recursion occurs). No patch or test case is provided—this is purely a behavioral observation. The thread context does not include prior discussion or related patches, so this appears to be the first mention of the issue.

The report targets the Windows-specific build of Git, but the behavior described (recursion in `git rm`) is likely a core Git issue rather than a platform-specific quirk. The use of `*.json` as a glob pattern suggests the behavior might involve shell expansion or Git’s own path matching logic. No prior work or related efforts (e.g., `the_repository` removal, ODB abstraction) are mentioned, so this is likely an isolated bug.