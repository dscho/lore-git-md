This is a **test infrastructure** and **CI** patch discussion, specifically a **substantive review** of patch 3/9 in Toon Claes’s series to make `GIT_TEST_LONG` tests reliable and efficient. Jeff King (Peff) responds to Patrick Steinhardt’s patch that refactors `t4141-apply-too-large.sh` to eliminate a 6-minute `dd`-based bottleneck.

Peff endorses the patch’s immediate fix—replacing the byte-by-byte `dd` pipeline with `genzeros`’ built-in limit parameter—but raises a broader question about the `test_copy_bytes` helper itself. He notes that the helper was originally introduced because `head -c` (a faster alternative) was not portable, but wonders whether that limitation still holds today. He also traces the helper’s history: it started as a `dd`-based solution, was replaced with Perl for performance, and then reverted to `dd` in 2025 for portability reasons. His comment is framed as a curiosity about "Chesterton’s fence" (the principle of understanding why something exists before changing it) rather than a direct objection to the patch.

The review is **substantive** but **not blocking**: Peff does not challenge the patch’s correctness or its removal of the `EXPENSIVE` prerequisite, nor does he suggest an alternative implementation. Instead, he flags a potential follow-up opportunity to improve `test_copy_bytes` more generally, possibly by revisiting its portability assumptions. This leaves the patch’s immediate future unchanged—SZEDER Gábor’s earlier objection about memory usage remains the only open technical concern—but adds a new dimension to the thread: whether the test suite’s I/O helpers could be modernized. No other participants have weighed in on this point yet.