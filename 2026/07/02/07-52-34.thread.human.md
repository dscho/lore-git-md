This thread fixes memory leaks in Git’s non-default hash implementations (OpenSSL SHA-256, libgcrypt) by introducing a new `git_hash_discard()` function and systematically applying it across subsystems like `csum-file`, `patch-id`, and HTTP object requests. The series, now at v1, has seen partial review: Junio Hamano approved the first patch’s removal of a redundant function, while Patrick Steinhardt and Brian Carlson endorsed making `git_hash_discard()` idempotent to simplify error paths and Rust integration. Jeff King has proposed a concrete design using an `active` flag in `struct git_hash_ctx`, though this would require follow-up work to address direct backend calls in `submodule--helper.c` and `test-synthesize.c`.

The latest exchange sharpens the idempotency debate. Patrick Steinhardt advocates for strict enforcement—triggering a `BUG()` if `git_hash_update()` or `git_hash_final()` is called on an uninitialized or already-finalized context—arguing this will catch misuse early. Jeff King agrees in principle but prefers to land the current series first, deferring strictness to a follow-up. Naming discussions remain open: Patrick suggested `git_hash_release()` for consistency with Git’s cleanup conventions, and Jeff proposed renaming `free_hashfile()` to `hashfile_free()` in a later patch. The series touches 13 files, adds platform-specific discard callbacks, and eliminates leaks in 68 test scripts under `SANITIZE=leak`. No merge status yet; patches 2–9 are unreviewed.