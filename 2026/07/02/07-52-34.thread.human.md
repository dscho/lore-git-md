Here’s the human-mode summary for a developer catching up after being away:

This thread is a nine-patch series from Jeff King (Peff) fixing memory leaks in Git’s hash implementations when using non-default backends like OpenSSL or libgcrypt. The leaks only surface under leak sanitizers (`SANITIZE=leak`) with those backends enabled, but the fixes are systematic: Peff introduces a new `git_hash_discard()` function and applies it across subsystems like `csum-file`, `patch-id`, HTTP object requests, and libgcrypt-specific SHA-256 handling. The series also tidies up the API by removing a redundant function and optimizing the discard implementation in the final patch.

Junio has already reviewed the first patch and approved its direction, calling Peff’s reasoning "sounds good." The only open question is whether Eric Sunshine might suggest a tweak to the function naming, but this is minor and doesn’t block progress. The rest of the series remains unreviewed, though no objections have been raised yet. The patches touch low-level plumbing (hash contexts, file checksums, and object streaming) but don’t change user-visible behavior or on-disk formats. If you’re working with non-default hash backends or leak-checking Git, this series will matter; otherwise, it’s a quiet correctness fix.