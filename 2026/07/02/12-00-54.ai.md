This is the first patch in a nine-patch series targeting CI and test infrastructure, specifically aimed at improving the reliability and efficiency of `GIT_TEST_LONG` tests. This patch is a small documentation update that adds a GitLab CI badge to the project's README to make the GitLab CI pipeline more discoverable.

The change is straightforward: it adds a second status badge to the README, linking to the GitLab CI pipeline for the `master` branch. Currently, the README only displays a badge for GitHub Actions, which can make the GitLab CI less visible to contributors. The patch modifies only the README file, adding one line of Markdown to include the GitLab badge alongside the existing GitHub one.

This patch is part of the series' broader goal to ensure that `GIT_TEST_LONG` tests are run reliably in CI. While this specific change doesn’t directly affect test behavior or CI configuration, it supports the series' overall motivation by making GitLab CI status more visible, which is relevant since later patches in the series enable `GIT_TEST_LONG` in GitLab CI. The patch is well-motivated and uncontroversial, addressing a minor but practical discoverability issue. No objections or review feedback are expected, as the change is purely additive and aligns with the project's existing practices.