This is a follow-up to Junio C Hamano’s "What’s cooking" report, focusing on three of Patrick Steinhardt’s ODB/ref-related topics. The email clarifies the current state and next steps for each.

Patrick responds to Junio’s questions about merging three topics to `next`. For **ps/odb-drop-whence** (7 commits), he plans a small reroll to rename `sourcep` to `source_infop` based on Justin Tobler’s feedback, but the series is otherwise ready. **ps/odb-generalize-prepare** (3 commits) and **ps/refs-writing-subcommands** (5 commits) are both marked as ready for `next` with no further changes expected.

The topics are part of Patrick’s ongoing ODB abstraction effort, which aims to make Git’s object storage layer more modular. **ps/odb-drop-whence** removes the `whence` field from `struct object_info` and replaces it with a backend-specific `struct object_info_source`, while **ps/odb-generalize-prepare** introduces a new `odb_prepare()` wrapper to standardize how object database sources are opened. **ps/refs-writing-subcommands** adds `create`, `delete`, `update`, and `rename` subcommands to `git refs`, extending its functionality for programmatic ref management.

All three topics are well-scoped and appear technically sound, with prior review feedback already addressed. The reroll for **ps/odb-drop-whence** is minor and should not delay progress. The ODB changes are prerequisites for future work, so merging them to `next` will unblock dependent topics. The `git refs` subcommands are a user-facing feature but are narrowly focused and unlikely to be controversial. No objections or open questions are raised in this email.