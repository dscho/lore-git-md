Here’s the human-mode summary for a developer catching up on the thread:

This thread is a nine-patch series from Toon Claes aimed at making Git’s `GIT_TEST_LONG` tests more reliable and efficient in CI. The series fixes broken or hanging tests, reduces resource usage, and ensures these expensive tests run in GitLab CI—something that wasn’t happening before. Most patches are uncontroversial, but one (patch 3) sparked debate: it speeds up `t4141-apply-too-large.sh` by replacing a slow `dd` copy with a faster method, but SZEDER Gábor argued the test should still be labeled `EXPENSIVE` because it uses over 1 GiB of memory. Patrick Steinhardt, the patch’s original author, agreed to restore the `EXPENSIVE` prerequisite in the next version, closing the discussion. Jeff King (Peff) reviewed several patches, endorsing the fixes and efficiency improvements, while Patrick also confirmed the value of isolating expensive test data (patch 6) to prevent interference with later tests.

The series is now at v1, with no maintainer feedback yet. The main open question—whether "expensive" should refer to runtime or resource usage—remains unresolved in policy terms, but the immediate issue (patch 3) is settled. Other changes include skipping tests on platforms where they’re known to break (e.g., `t7508-status.sh` on 32-bit systems), reducing disk usage in `t5608-clone-2gb.sh`, and enabling `GIT_TEST_LONG` in GitLab CI for integration branches. The macOS RAM disk is disabled in GitLab CI to avoid out-of-memory issues, and Windows runners are excluded due to hardware limits. The series is self-contained and doesn’t depend on other in-flight work.