This is a follow-up in the now-merged series that fixed the parse-options help exit code inconsistency. Brian M. Carlson replies to Junio’s review of the first patch, which added an `SVN` test prerequisite to `t/t1517-outside-repo.sh`.

Brian acknowledges Junio’s observation that the `test -n "$NO_SVN_TESTS"` guard is backwards: it currently causes the prerequisite to fail if `NO_SVN_TESTS` is unset or empty, even when SVN is available. He agrees this is unintended and plans to simplify the prerequisite by dropping the version check entirely. He also notes that Peff already identified a separate bug in the version check (the Perl string comparison `gt '1.1.0'` is broken due to shell-quoting), making the check ineffective anyway. The fix will likely be a small follow-up patch that removes both the version check and the problematic guard, ensuring the prerequisite only fails when SVN is genuinely unavailable.

The discussion is narrow and technical, focused solely on the prerequisite’s logic. No broader questions about the series’ design or exit-code behavior are revisited. This is a clear case of post-merge cleanup: the core fix is already merged, but two minor issues in the test infrastructure remain open. Brian’s response suggests he will address both in a single follow-up.