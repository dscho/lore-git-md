This is the second patch in the v6 series fixing the `--linearize` regression in `git replay`. It **moves the base-resolution logic out of `pick_regular_commit()`** and into the caller (`replay_revisions()`), which directly addresses the root cause of the silent commit-dropping bug that plagued v5.

The change is a **clean refactoring** with no intended behavior change, but it **restores the `replayed_base` mechanism** that was broken in v5. By centralizing base selection in `replay_revisions()`, the patch makes the code easier to follow and ensures that all commits in the input range are replayed, not just the tip. The commit message explains the motivation clearly: the old split logic was confusing, and this change bundles all base-selection decisions in one place.

The diff itself is straightforward: `pick_regular_commit()` now takes a single `replayed_base` parameter instead of `replayed_commits` and `onto`, and the caller (`replay_revisions()`) handles the fallback logic. The new code path is more explicit about when to use `last_commit` (for `--revert`) versus `onto` (for `--onto`).

This patch is **well-motivated and well-executed**, addressing a real regression with minimal risk. It’s a key part of the v6 series and should be uncontroversial. If you care about `git replay` or the `--linearize` feature, this is worth reviewing to understand how the regression was fixed.