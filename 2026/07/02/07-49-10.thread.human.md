The thread began when Евгений Плискин reported that `git rm -n *.json` recursively matched JSON files in subdirectories, contrary to expectations. The behavior is now confirmed as correct: Git’s pathspec logic treats `*` as matching directory separators by default, and when no `.json` files exist in the current directory, the shell passes `*.json` literally to Git, triggering recursive matching. The discussion has shifted from treating this as a bug to addressing a documentation gap in `git-rm(1)`.

Key participants have clarified the technical details. Patrick Steinhardt explained the pathspec behavior and suggested the `:(glob)` modifier to restrict matching to the current directory. Phillip Wood added that the behavior depends on shell globbing—if `.json` files exist, the shell expands the glob, and Git receives explicit paths; if not, Git receives the literal pattern. Mikael Magnusson reinforced this by quoting `gitglossary(7)`, which documents the recursive matching rule, and noted that `git-rm(1)` does not promise non-recursive behavior for globs. Matt Hunter’s earlier attempt to reproduce the issue was confounded by having `.json` files in his working directory, masking the recursive behavior.

The thread now centers on whether `git-rm(1)` should cross-reference `gitglossary(7)` or add a brief note about pathspec modifiers like `:(glob)`. Phillip Wood’s clarification about shell-Git interaction has sharpened the focus on user expectations, and Patrick Steinhardt’s request for specific confusing wording in the man page remains unanswered. No code changes are proposed, and the behavior is not platform-specific, though it was originally reported on Git for Windows. The next step is likely a documentation patch to improve clarity in `git-rm(1)`.