This thread began when Евгений Плискин reported that `git rm -n *.json` unexpectedly recursed into subdirectories, treating it as a bug. The discussion quickly clarified that this is expected behavior: Git's pathspec logic treats `*` as matching directory separators by default, as documented in `gitglossary(7)`. The twist is that the recursion only becomes visible when no `.json` files exist in the current directory—otherwise, the shell expands the glob, passing explicit paths to Git and masking the recursive behavior.

Patrick Steinhardt recharacterized the issue as a documentation gap, noting that the `:(glob)` pathspec modifier (e.g., `git rm -n ':(glob)*.json'`) can restrict matching to the current directory, but this isn't prominently explained in `git-rm(1)`. Phillip Wood added a key technical clarification: the behavior hinges on shell globbing, which depends on whether matching files exist in the working directory. Matt Hunter, in the latest reply, confirmed this after realizing his test environment had `.json` files, causing the shell to expand the glob and hide the recursive behavior. His observation underscores the thread's focus on user expectations around shell-Git interaction, rather than a code defect.

The thread now centers on whether `git-rm(1)` should cross-reference `gitglossary(7)` or explicitly mention pathspec modifiers to prevent similar confusion. Евгений hasn't yet identified which part of the man page misled him, leaving that question open. No patches have been proposed, but the discussion has converged on the need to clarify the edge case where no matching files exist in the current directory.