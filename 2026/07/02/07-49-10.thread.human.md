This thread is a bug report from Евгений Плискин about unexpected recursion in `git rm`. When running `git rm -n *.json`, Git removes JSON files from subdirectories even though the documentation states that `git rm` should not recurse without an explicit directory path. The issue was observed on Git for Windows 2.55.0.windows.1, but it likely affects core Git behavior rather than being Windows-specific.

The report is straightforward, with a clear reproduction case, but so far no one has replied to confirm the issue, propose a fix, or discuss whether this is intended behavior. Since this is the first email in the thread, there are no prior versions or related patches to consider. The next steps will likely involve someone reproducing the issue, determining if it’s a bug or a documentation gap, and deciding whether to adjust the code or the docs. If you’re familiar with Git’s path-matching logic or the `git rm` implementation, this might be worth investigating.