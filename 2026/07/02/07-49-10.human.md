This is a bug report about unexpected behavior in `git rm`. The author, Евгений Плискин, reports that `git rm -n *.json` recursively removes JSON files even when no directory is specified, contradicting the documentation which states that `git rm` should not recurse without an explicit directory path. The issue is observed in Git for Windows 2.55.0.windows.1.

The report is concise and includes a clear reproduction case (`git rm -n *.json`), the expected behavior (no recursion), and the actual behavior (recursion occurs). No patch is provided—this is purely a bug report, not a fix. The author does not speculate about the root cause or suggest a solution, but the issue appears to be a regression or edge case in path expansion or argument handling.

Given that this is a Windows-specific build, it may involve shell globbing differences or platform-specific quirks in how arguments are processed. The report is well-structured and actionable for someone familiar with the `git rm` codepath. If confirmed, this would likely qualify as a bugfix, though the severity depends on whether the behavior is reproducible on other platforms.