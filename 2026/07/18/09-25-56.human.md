This is a substantive review follow-up in the ongoing discussion about Harald Nordgren’s patch to add `git rebase --abort` hints to failure and status messages. Phillip Wood presses for clarification on the real-world use case behind the patch’s niche workflow: using `git rebase` as a control structure to run checks on each commit, where a single exec failure should terminate the process. His question—why someone would want to check commits but not fix the failures—challenges the patch’s underlying assumption and could shift the thread’s direction toward either abandoning the exec-failure hint or rethinking its design to better align with user intent. The exchange remains focused on workflow validity rather than code details, leaving the patch’s future uncertain.