This is a surface-level review comment on the first patch of the `--auto-reset` series for `git bisect`. Johannes Sixt raises a platform-compatibility concern about the patch's claim that reopening the `BISECT_RUN` file by name might fail because the file is no longer "reachable by name". He questions whether this scenario is realistic on Windows, where open files traditionally cannot be removed, and suggests the change might be unnecessary or even risky if it introduces edge cases that don't exist in the current code.

The comment is narrowly scoped to the patch's motivation rather than its implementation. Sixt does not dispute the code changes themselves (which are straightforward and uncontroversial) but challenges the premise that the refactoring is needed. His tone is cautious, reflecting a long-standing contributor's skepticism about changes that could introduce platform-specific regressions. No alternative approach is proposed, and the comment does not engage with the broader goal of the series (enabling `--auto-reset`). The review depth is shallow, focusing on a single sentence in the commit message rather than the patch's technical merits or test coverage.