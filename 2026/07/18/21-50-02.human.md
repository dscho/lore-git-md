This is the final patch in the twentieth iteration of the security-hardened `git cat-file --batch-command` remote-object-info series, which is now technically complete and queued for `next`. The patch makes the client's allow-list for object metadata placeholders dynamic, adapting to the server's advertised capabilities rather than relying on a hardcoded list.

The change addresses a forward-compatibility problem: as the `object-info` protocol gains new capabilities (like `type`), servers with different Git versions will support different sets of attributes. The patch teaches the client to request only what the server advertises, silently omitting unsupported placeholders (e.g., `%(objecttype)` expands to an empty string if the server doesn't support `type`). This matches `for-each-ref`'s behavior for known-but-inapplicable atoms and ensures the client won't fail when querying servers with partial support.

Key implementation details include a new `remote_allowed_atoms` string list in `struct expand_data` that tracks server-supported placeholders, a mapping between protocol options (`size`, `type`) and their corresponding atoms (`objectsize`, `objecttype`), and a backward-compatible fallback that preserves the existing behavior for supported placeholders. The patch also moves `object_info_options` out of `get_remote_info()` so the caller can control which options are requested, and ensures output is always produced—even if no attributes are supported, resulting in a blank line.

The patch includes two new tests in `t/t1017-cat-file-remote-object-info.sh` that verify the dynamic allow-list behavior: one confirms that requesting only `%(objectname)` echoes back the OID, and another ensures `%(objectname)` requests still trigger an existence check on the server. The implementation is robust, with bounds-checked string list operations and memory management using `xcalloc` and `string_list_clear`.

This is a well-crafted, necessary piece of the series that resolves the last major design issue. The dynamic allow-list is a clean solution to the version-compatibility problem, and the patch integrates seamlessly with the rest of the series. The tests cover the new behavior thoroughly, and the code is clearly written with attention to edge cases. The series is now ready for final polish before merging to `master`.