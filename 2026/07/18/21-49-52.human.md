This is a test infrastructure patch, part of the ongoing `git cat-file --batch-command` remote-object-info series (v20, patch 3/13). The patch extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is purely mechanical and preparatory: these helpers will be reused in a later test patch (likely for the new `remote-object-info` command), and the extraction improves code organization and avoids duplication.

The patch also corrects a minor coding style nit in the sourcing of `lib-loose.sh`, aligning it with the project's guidelines. There are no behavior changes, and the patch itself is uncontroversial. It is a small but necessary step in the larger series, which is now technically complete and queued for `next`. If you are following the remote-object-info effort, this patch is worth noting only for context; it does not advance the core functionality.