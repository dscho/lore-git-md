This is a mechanical build fix for the ongoing `git cat-file --batch-command` remote-object-info series, now at v20 and queued for `next`. The patch corrects a signed/unsigned type mismatch in `write_fetch_command_and_capabilities()` by changing the `hash_algo` variable from `int` to `const unsigned int` to match the return type of `hash_algo_by_name()`. The mismatch is currently masked by `DISABLE_SIGN_COMPARE_WARNINGS` in `fetch-pack.c`, but would break the build when the function is relocated to `connect.c` in a later patch (which does not suppress sign-compare warnings). The fix is trivial and uncontroversial, with no behavior changes.

The patch is part of the preparatory refactoring in the series, specifically patch 5/13, which moves `write_fetch_command_and_capabilities()` from `fetch-pack.c` to `connect.c` to enable broader reuse. This type correction must land before that relocation to avoid breaking the build. The change is minimal (one line), and the `const` qualifier is added because the variable is never modified. The commit message clearly explains the motivation and context, and the patch has no dependencies beyond the series itself.

This is a required fix for the series to graduate to `master`; without it, the build would fail when the function is moved. The fix is already incorporated into the v20 roll-up and has been validated by CI. No further discussion or revision is expected.