This is a **substantive review reply** in the ongoing discussion about patch 3/4 of Toon Claes’s `git last-modified` Bloom filter optimization series. Taylor Blau responds to Jeff King’s proposal to address the `--show-trees` correctness bug by removing ancestor tree entries not covered by the original pathspec, rather than disabling the Bloom-filter optimization entirely.

Taylor agrees with Peff’s assessment that removing uncovered ancestor tree entries (e.g., `d/` when the pathspec is `d/a`) is a reasonable trade-off for an experimental command, as it preserves the performance benefit while avoiding misleading results. He provides a **tested code snippet** implementing this approach: a small patch to `add_path_from_diff()` that filters out paths not matching the original pathspec using `match_pathspec()`. This solution is simple and aligns with the likely intent of most callers, who care about files rather than trees.

For completeness, Taylor also explores an alternative design that retains uncovered paths in the output while still optimizing Bloom-filter checks. This approach maintains a list of uncovered paths and checks them individually, reducing the Bloom-filter query cost from O(P*H) to O(U*H) (where U is the number of uncovered paths and H is the number of hash functions). He provides a **second code snippet** implementing this logic, which involves tracking uncovered paths in a linked list and checking them separately. Benchmarks on a synthetic repository show this approach cuts runtime roughly in half (450ms → 227ms) compared to the original patch, though it is still slower than the simpler "remove uncovered paths" approach (190ms).

Taylor ultimately recommends the simpler solution—removing uncovered paths entirely—citing the command’s experimental status and the performance advantage. The discussion remains focused on **correctness, performance trade-offs, and maintainability**, with no new behavioral concerns raised. The next step is likely a reroll of patch 3/4 incorporating one of these designs.