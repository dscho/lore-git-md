This is a **surface-level review** of the first patch in Siddharth Shrimali’s RFC series introducing `git repack --drop-filtered` for partial clones. Christian Couder raises a minor design question about the command-line interface: whether `--dry-run` should be a standalone option (as implemented) or a modifier to `--drop-filtered` (e.g., `--drop-filtered=dry-run`). He notes that the current approach aligns with other Git commands that already use `--dry-run`, but suggests the commit message could briefly explain the reasoning. The comment is purely stylistic and does not engage with the patch’s core logic or safety mechanisms. No technical concerns are raised, and the feedback is unlikely to block progress. The author may address this in the planned v2, which is already focused on fixing a more critical validation flaw identified by Junio.