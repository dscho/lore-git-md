Thread identity
Subject: [PATCH v2] branch: clarify error message when deleting a branch used by bisect
Series version: v2 (standalone patch, follow-up work anticipated)
Originating author: René Scharfe
Thread type: user-facing error message improvement with emerging design implications for a broader branch-safety API

Core problem or goal
The error message from `git branch -d` when attempting to delete a branch currently in use by `git bisect` is generic ("used by worktree at ..."), obscuring the actual reason. The v2 patch narrows scope to address only the bisect case, replacing the generic message with a specific one ("for bisect"). Junio’s earlier feedback framed this as a foundation for a broader, consistent API to check branch usage across all commands that repoint branch tips (e.g., `git history`), but this version defers other checkout kinds (rebase, worktree) to future work.

Current status
Cooking in `next` as of Junio’s integration note. The v2 patch resolves the `int` vs. `enum` debate in favor of `enum branch_checkout_kind` for debuggability and focuses solely on the bisect case. Phillip Wood and Junio C Hamano have explicitly endorsed the bisect-specific improvement as a clear, uncontroversial step forward. The unresolved phrasing for the "in use by a rebase" state remains the primary blocker for broader coverage, but this is now framed as a known loose end rather than a flaw in the current patch.

Key technical details
- Files touched: `branch.c`, `branch.h`, `builtin/branch.c`, `t/t3200-branch.sh`
- New data structure: `checked_out_branches` array in `branch.c` tracking checkout reasons (`BRANCH_CHECKOUT_KIND_*`) and worktree paths
- New symbols: `branch_bisecting()` helper function (internal-only)
- On-disk format: unchanged
- Test coverage: extended in `t/t3200-branch.sh` to verify the bisect-specific error message; coverage for `BRANCH_CHECKOUT_KIND_WORKTREE` and `BRANCH_CHECKOUT_KIND_UPDATE_REF` is still missing
- Error message logic: The v2 patch only addresses the bisect case, leaving the `UPDATE_REF` case (rebase with merges) and `WORKTREE` case unmodified. The `BRANCH_CHECKOUT_KIND_CHECKOUT` message remains "used by worktree at '%s'", though Junio and Phillip Wood prefer "checked out at '%s'".
- Implementation detail: The `enum branch_checkout_kind` is now used for the `kind` variable, improving debuggability without affecting error messages.

Open questions and loose ends
1. **Error message design for `BRANCH_CHECKOUT_KIND_UPDATE_REF`**: Should it be merged into the "rebase" category (René’s initial proposal), kept distinct with a technical message ("for rebase with merges", Junio’s preference), or replaced with a more detailed message to address usability concerns (Phillip Wood’s proposal)?
2. **Error message wording for `BRANCH_CHECKOUT_KIND_CHECKOUT`**: Should it remain "used by worktree at '%s'" or be changed to "checked out at '%s'" (Junio and Phillip Wood’s preference)?
3. **Test coverage**: Need to extend tests to cover the remaining checkout kinds (`BRANCH_CHECKOUT_KIND_WORKTREE` and `BRANCH_CHECKOUT_KIND_UPDATE_REF`).
4. **Usability concern**: Phillip Wood raised a concrete issue about potential confusion when `git status` shows a different branch than the error message, suggesting either enhancing `git status` to show all branches being rewritten or crafting a distinct, detailed error message for the `UPDATE_REF` case. This remains unresolved.
5. **Broader API design**: Junio’s earlier feedback hinted at unifying branch-safety checks across commands (e.g., `git history`). The `checked_out_branches` array introduced in v2 is seen by Toon Claes as a foundation for this future work, but no concrete follow-up patches have been posted yet.

Participant positions
- **René Scharfe (author)**: Implemented the v2 patch, focusing on the bisect case for simplicity. Agrees to use `enum branch_checkout_kind` for debuggability but defers other checkout kinds (rebase, worktree) to future work. Open to refinements but cautious about overcomplicating the design. Has not yet addressed Junio and Phillip Wood’s proposed change to the `BRANCH_CHECKOUT_KIND_CHECKOUT` message or Phillip Wood’s usability concern.
- **stsp (reporter)**: Raised the original concern; no further feedback in this thread.
- **Junio C Hamano (maintainer)**: Reviewed v1 and v2, marking the topic ready for `next`. Suggested:
  - Refining error messages for clarity, especially for `UPDATE_REF` (prefers "for rebase with merges" over merging into "rebase").
  - Changing the `BRANCH_CHECKOUT_KIND_CHECKOUT` message to "checked out at '%s'".
  - Extending test coverage to all checkout kinds.
  - Using `enum` instead of `int` for the `kind` variable (now resolved in favor of `enum`).
  - Potential follow-up work to unify branch-safety checks across Git commands. Explicitly endorses the bisect-specific improvement as a clear step forward and frames the rebase phrasing as a known loose end.
- **Phillip Wood (reviewer)**: Substantive review focusing on usability:
  - Raises a concrete concern about confusion when `git status` shows a different branch than the error message, proposing either enhancing `git status` to show all branches being rewritten or crafting a distinct, detailed error message for the `UPDATE_REF` case.
  - Endorses Junio’s suggestion to change the `BRANCH_CHECKOUT_KIND_CHECKOUT` message to "checked out at '%s'".
  - Explicitly endorses the v2 patch as a useful incremental step, confirming the bisect-specific improvement is uncontroversial.
- **Toon Claes (reviewer)**: Provides a surface-level review of v2, endorsing it as "a step in the right direction" and confirming it "looks good to me." Agrees with Junio’s broader vision of creating a reusable API for branch-safety checks, noting that the patch’s internal `checked_out_branches` array could serve as a foundation for future work like `git history`. Does not engage with the open usability questions or unresolved error-message phrasing for rebase scenarios.

Related work
- **`git history`**: Recently missed the branch-safety check, motivating Junio’s broader API vision.
- **Branch-safety checks**: Other commands (e.g., `git reset`, `git switch`) may benefit from a unified approach. The `checked_out_branches` array introduced in v2 is seen as a template for future work.