This is a **substantive review follow-up** in the `no-ref-delta` capability thread, where Jeff King (Peff) probes the technical rationale behind Taylor Blau’s design. Peff’s questions focus on whether the proposed `no-ref-delta` capability is the right solution for the stated use case—a write-through proxy that spools incoming packs byte-for-byte and replays them upstream.

Peff’s central concern is the relative cost of `REF_DELTA` versus `OFS_DELTA` in the proxy’s indexing pipeline. He notes that even with `OFS_DELTA`, the proxy must maintain random access to the pack and in-memory structures for delta resolution, which scales with pack size. The key difference, Peff suggests, is not memory or access patterns but the risk of pipeline stalls: a single `REF_DELTA` whose base appears late in the pack forces sequential resolution of all dependent deltas, whereas `OFS_DELTA` chains can be resolved as soon as their base is seen. This worst-case scenario—where a long delta chain depends on a late base—is what `no-ref-delta` mitigates, not the general overhead of `REF_DELTA` indexing.

Peff then floats an alternative: a tighter capability that enforces “bases before deltas” ordering, which Git already generates by default. This would allow the server to reject non-standard packs without banning `REF_DELTA` outright, preserving broader compatibility while still preventing the worst-case stall. The suggestion is speculative—Peff acknowledges he’s guessing at the real issues—but it highlights a potential middle ground between the current design and a more permissive protocol.

The email leaves open whether `no-ref-delta` is the optimal solution or if a narrower capability would suffice. The thread’s next steps likely hinge on Taylor’s response to these trade-offs: whether the proxy’s design truly requires `OFS_DELTA`-only packs, or if a “bases-before-deltas” rule could achieve the same performance goals with less protocol disruption.