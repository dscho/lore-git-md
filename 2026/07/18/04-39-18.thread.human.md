Here’s the human-mode narrative summary for a developer catching up on this thread:

This thread is about improving the error message when `git branch -d` refuses to delete a branch that’s currently in use by `git bisect`. The original message was generic ("used by worktree at ..."), which didn’t clarify why the branch couldn’t be deleted. René Scharfe’s v2 patch narrows the scope to address just the bisect case, replacing the generic message with a specific one ("for bisect"). The patch is now marked ready for `next` by Junio, who sees it as a reasonable incremental step while deferring more complex cases—like branches used by rebase or worktrees—for later.

The discussion has focused on usability and future-proofing. Junio and Phillip Wood both prefer clearer phrasing, like changing "used by worktree at '%s'" to "checked out at '%s'" for the `BRANCH_CHECKOUT_KIND_CHECKOUT` case. Phillip also raised a practical concern: if `git status` shows a different branch than the error message, users might get confused, especially during rebase operations. This hasn’t been resolved yet, but the patch’s current design leaves room for follow-up work to address these edge cases.

The patch touches `branch.c`, `branch.h`, `builtin/branch.c`, and adds tests in `t/t3200-branch.sh`. It introduces an internal `checked_out_branches` array to track why a branch is in use, which could later serve as a foundation for a broader branch-safety API across Git commands. For now, the focus remains on the bisect case, with the understanding that rebase and worktree scenarios will need their own refinements down the line.