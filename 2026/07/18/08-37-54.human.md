This is a **bug report** from the original author of the notes-copying fix, Uwe Kleine-König, identifying a **regression in the approved 9-patch sequencer series** that Junio has marked ready for `next`. The issue surfaces only in **interactive rebase (`-i`)** and causes notes from dropped commits to leak into the neighboring commit, exactly the bug the series was meant to fix.

The problem is specific to **interactive mode**: when Uwe rebases with `-i`, commits that become empty are dropped (requiring manual `git rebase --skip`), and the notes from those dropped commits incorrectly appear in the commit message of the next picked commit. The same rebase without `-i` works correctly, suggesting the bug is isolated to the interactive backend’s handling of dropped commits. Uwe’s reproduction steps are clear: a real-world workflow rebasing a large topic branch onto `next/master`, with `git range-diff` and `git notes show` providing concrete evidence of the regression.

The series’ author, Phillip Wood, has not yet replied, so the scope of the regression is still unknown. The thread context confirms the series was **approved and queued for `next`**, but this new report may delay its integration until the regression is resolved. No other participants have weighed in yet.