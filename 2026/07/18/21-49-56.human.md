This is a refactoring patch in the long-running `git cat-file --batch-command` remote-object-info series, now at its twentieth iteration and queued for `next`. The patch generalizes `write_fetch_command_and_capabilities()` into `write_command_and_capabilities()` by adding a `command` parameter, allowing the function to be reused for non-fetch protocol v2 operations like `object-info`. The change is mechanical: the function is renamed, its signature updated, and all callers in `fetch-pack.c` adjusted to pass `"fetch"` explicitly. No behavior changes are introduced.

This is the seventh patch in the thirteen-patch series. It prepares the transport layer for the `object-info` protocol capability by making the command-writing logic generic, a necessary step before introducing the new `remote-object-info` command in later patches. The patch is uncontroversial and has been carried forward through multiple revisions without substantive feedback, reflecting its role as a straightforward preparatory refactor. The commit message clearly explains the motivation and scope, and the code changes are minimal and focused.