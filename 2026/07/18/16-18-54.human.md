This is the third and final patch in the v2 series adding the `--auto-reset` option to `git bisect`. The series is a feature addition that automatically resets the working tree after a bisect completes, targeting both automated workflows and interactive debugging.

Johannes Sixt (Hannes) replies with a surface-level naming suggestion: he finds `--auto-reset` too generic and proposes `--reset-when-found` instead. The review is purely stylistic and does not engage with the implementation, test coverage, or design rationale. No technical concerns are raised, and the suggestion is presented as a matter of preference rather than a correctness issue.

The series itself remains uncontroversial and ready for integration. The preparatory patches (1 and 2) are already settled, and this patch delivers the core functionality with thorough test coverage. The naming question is the only open point, and it is unlikely to block progress given the feature’s clear utility and the lack of substantive objections.