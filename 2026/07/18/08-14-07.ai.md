This is a substantive review by Jeff King (Peff) on the fourth patch of Toon Claes’s four-part series optimizing `git last-modified` with Bloom filters. The patch ensures wildcard pathspecs (e.g., `*.c`) continue to benefit from the Bloom-filter acceleration introduced earlier in the series.

Peff’s review questions whether the current fix—restoring `bloom_filter_settings` after `prepare_revision_walk()` clears it—is the right approach. He argues that `rev_info.bloom_filter_settings` is an internal implementation detail of `revision.c` and suggests it would be cleaner for `last-modified` to manage its own settings pointer instead of relying on the revision machinery. His proposed alternative (a diff included in the email) moves the settings into the `last_modified` struct, avoiding any conflict with `revision.c`’s use of the same field. He acknowledges the change is mostly academic, since both pointers would ultimately reference the same repository-level settings, but sees it as a clearer separation of concerns.

The review also touches on a broader design question: whether `bloom_filter_settings` should be a repository-level property accessible via a helper like `repo_bloom_filter_contains()`, rather than requiring callers to manage the settings pointer themselves. Peff notes this would require caching the settings to avoid repeated lookups, and leaves it as an open question whether the complexity is justified.

This feedback does not identify a correctness bug but raises a maintainability concern about the current patch’s approach. The discussion is now focused on whether the simpler fix (restoring the settings) is acceptable or if the series should adopt the more isolated design Peff proposes. The thread context confirms this is the last patch in the series, so resolving this point will determine whether a reroll is needed before the series can proceed.