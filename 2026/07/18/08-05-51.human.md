This is a brief follow-up in the ongoing discussion about adding `git rebase --abort` hints to exec-failure messages. Harald Nordgren confirms Junio C Hamano’s speculation: the workflow he’s targeting is indeed not a traditional rebase but rather a way to run checks on each commit in a range, where a single failure should terminate the process. This admission shifts the thread’s focus from the patch’s implementation (which had largely reached consensus on design) to its fundamental premise—whether `git rebase` is the right tool for this job at all.

The patch itself remains unchanged, but Harald’s confirmation strengthens Junio’s argument that the hint may be misaligned with rebase’s intended purpose. The discussion now hinges on whether this niche use case warrants built-in UI support or if users should be directed to a simpler script or dedicated command. No new code or alternatives have been proposed yet, but the thread’s tone suggests the patch may need a stronger justification to proceed.