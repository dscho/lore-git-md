This is a brief follow-up in the ongoing `git rebase --abort` advice thread. Harald Nordgren confirms Junio C Hamano’s speculation: the workflow he is targeting is indeed not a traditional rebase but rather a loop to run checks on each commit, where a single exec failure should terminate the process. This admission shifts the thread’s framing—Harald’s patch is now explicitly addressing a niche use case that treats `git rebase` as a control structure rather than a history-editing tool. The confirmation leaves the core question unresolved: whether Git should accommodate this workflow by adding `--abort` hints to exec-failure messages, or whether users should be steered toward a dedicated script or command. No code changes or new proposals are introduced; the thread now hinges on whether this use case warrants built-in support.