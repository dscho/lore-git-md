This is a substantive review follow-up in the `git rebase --abort` advice thread. Phillip Wood presses Harald Nordgren on the practical use case for aborting a rebase after a single exec failure, challenging the assumption that this workflow is desirable. Phillip’s question—why a user would want to check commits but not fix the failing ones—directly targets the patch’s core motivation. He contrasts the abort-centric design with a more typical rebase workflow where exec failures are resolved by amending commits, not abandoning the process. This reply sharpens the thread’s central tension: whether the patch addresses a real user need or optimizes for an edge case that might be better handled outside Git’s built-in tooling. No new technical details or code changes are introduced, but the exchange now forces the author to either justify the workflow’s validity or reconsider the patch’s scope.