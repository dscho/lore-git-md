This is a **substantive review comment** on patch 2/9 of Jeff King’s memory-leak bugfix series. Patrick Steinhardt, acting in his self-described role as "naming czar," questions the choice of `git_hash_discard()` and suggests `git_hash_release()` as a more idiomatic alternative for a function that frees resources held by a hash context.

The comment is narrowly scoped to naming and does not challenge the patch’s technical correctness or necessity. It follows Junio’s earlier playful deference to Eric Sunshine for naming feedback on patch 1, reinforcing that naming consistency is a recurring theme in this series. Patrick’s suggestion (`release` over `discard`) aligns with Git’s existing conventions (e.g., `strbuf_release()`, `string_list_clear()`), where `release` typically denotes resource cleanup. The choice of `discard` in the patch was likely motivated by its pairing with `git_hash_final()`, but Patrick’s point highlights that `release` may better convey the function’s intent to free resources without finalizing the hash.

No other technical feedback is provided; the comment is purely stylistic. The thread’s prior discussion (patch 1) already established that naming improvements are deferred to follow-up patches to avoid disrupting the leak fixes, so this input is unlikely to block the series but may influence a future cleanup.