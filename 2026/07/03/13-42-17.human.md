This is a reply to a feature request about programmatically editing the interactive rebase todo list. Brian M. Carlson provides a practical solution using the existing `GIT_SEQUENCE_EDITOR` environment variable, which allows users to automate modifications to the rebase sequence without requiring new Git CLI options.

The response is concise and technically grounded. It explains how `GIT_SEQUENCE_EDITOR` works—essentially as a hook for editing the rebase todo list in place—and demonstrates how to use it with a Perl one-liner to mark commits for editing based on a pathspec filter. The example is well-chosen: it directly addresses Matthias Beyer’s use case (splitting commits that modified files in a subdirectory) and shows how to integrate `git log` output into the rebase workflow. Brian also notes the importance of using abbreviated commit hashes (`%h`) for compatibility with the todo list format.

Beyond the immediate example, the reply highlights the flexibility of `GIT_SEQUENCE_EDITOR`—it can run any command that edits the todo file in place, including shell scripts, `sed`, or `ed`. This makes it a general-purpose tool for automating rebase workflows, not just for the specific use case raised. Brian’s personal alias (`srebase`) for autosquashing fixup commits further illustrates the variable’s utility.

The tone is matter-of-fact and helpful, avoiding any debate about whether a built-in `--edit-commits` option is justified. Instead, it presents the existing mechanism as a sufficient and flexible solution. For a busy reader, this reply effectively closes the loop: the feature request is already addressable with Git’s current capabilities, and the provided example is ready to use. No further discussion or patch development appears necessary unless someone wants to propose a more user-friendly wrapper.