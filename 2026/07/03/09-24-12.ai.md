This is the final patch in Patrick Steinhardt’s nine-patch v2 series making `GIT_TEST_LONG` tests reliable and efficient for CI. It enables the `GIT_TEST_LONG` environment variable in GitLab CI for pushes to integration branches (`next`, `master`, `main`, `maint`), matching the behavior already implemented for GitHub Actions in commit 7a094d68a2. The patch explicitly disables `GIT_TEST_LONG` on Windows runners due to their 7.5 GB RAM limit, which is insufficient for some of the `EXPENSIVE` tests. The change touches `.gitlab-ci.yml` and `ci/lib.sh`, updating the CI event detection logic to handle GitLab’s pipeline sources and ensuring the variable is set conditionally (`GIT_TEST_LONG=${GIT_TEST_LONG:-true}`) rather than unconditionally.

The patch is straightforward and uncontroversial, closing the test coverage gap between GitHub Actions and GitLab CI. The Windows exclusion is a pragmatic concession to hardware limitations and has not drawn objections. With this patch, the series is now complete: all `GIT_TEST_LONG` tests are either fixed, skipped on broken platforms, or optimized for CI, and both major CI systems run them on integration branch pushes. The series as a whole looks ready for integration, with no remaining substantive feedback.