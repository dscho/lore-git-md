This is the sixth patch in Patrick Steinhardt’s 12-patch v2 series hardening Git’s reftable backend against corrupted files. The patch fixes a heap-buffer-overflow in the log block parser when it encounters a maliciously small inflated log size. The series is now technically complete, with all prior feedback addressed, and this specific patch has already received substantive review from Toon Claes.

Patrick replies to Toon’s review, explaining why the proposed test improvements—adding a sanity check and using a minimally invalid size—would not actually catch the out-of-bounds write the test is designed to expose. The issue requires the block size to be smaller than the header size (1–3 bytes), so the test deliberately uses a value of 1 to ensure the offset points outside the buffer. Patrick’s response is clear and technically grounded: the test’s current design is intentional and necessary to trigger the specific vulnerability. This closes the open question on patch 6/12 without requiring changes.

The exchange is a good example of focused, productive review: Toon’s suggestions were precise and aimed at test robustness, while Patrick’s reply clarified the test’s design intent. No further discussion is needed on this patch, and the series remains on track for eventual merging. If you’re following reftable security hardening, this thread confirms the fix is solid and the test is correctly targeted.