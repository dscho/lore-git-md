This is a **substantive review** of the `includeIf.worktree` patch (v6 2/6) in the series adding worktree-based conditional config inclusion. Patrick Steinhardt identifies a **behavioral inconsistency** between the new `worktree` condition and the existing `gitdir` condition: while `gitdir` matches both the symlinked and real paths, `worktree` currently resolves symlinks to the real path only, causing it to fail when the pattern uses the symlinked location.

The review provides a **detailed test case** demonstrating the discrepancy. It sets up a symlinked repository, configures both `gitdir` and `worktree` patterns for the symlinked and real paths, and shows that `gitdir` successfully matches both locations while `worktree` only matches the real path. This inconsistency could confuse users who expect the two conditions to behave similarly when dealing with symlinks.

Patrick’s feedback is **technically precise** and **actionable**: it pinpoints the exact code path (`repo_get_work_tree()` resolves symlinks) and suggests the fix should align `worktree`’s behavior with `gitdir`’s. The review does not question the overall design or necessity of the feature, but rather focuses on a specific edge case that would likely surface in real-world usage.

This is a **tested review** in spirit—Patrick constructs a concrete reproduction scenario—though he does not explicitly state whether he ran the test locally. The feedback is **uncontroversial** and **narrow in scope**, addressing a clear deviation from established behavior rather than introducing new requirements.

The patch author will need to decide whether to adjust `worktree` to match symlinks (like `gitdir`) or document the divergence as intentional. Either way, this observation should be resolved before the series graduates to `next`.