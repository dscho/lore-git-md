This is a **surface-level review** from Christian Couder on Patrick Steinhardt’s `git history drop` series (v8, patch 6/11). The patch refactors the reset API to make HEAD updates opt-in, a preparatory step for the `drop` subcommand. Christian points out a **minor formatting nit**: a single spurious space before the closing parenthesis in a `BUG()` macro call. The issue is purely cosmetic and does not affect functionality, but it violates Git’s coding style (no trailing whitespace). The patch itself remains uncontroversial and well-motivated by the series’ goals. No substantive discussion or objections are raised.