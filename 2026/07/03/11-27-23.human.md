This is a **substantive review comment** on the second patch in Jeff King’s 9-patch bugfix series targeting memory leaks in Git’s non-default hash backends. The series introduces `git_hash_discard()` to clean up hash contexts that aren’t finalized, and this patch adds the primitive itself.

Patrick Steinhardt, who has already weighed in on naming in the cover letter thread, now suggests renaming `git_hash_discard()` to `git_hash_release()`. His comment is brief but pointed: he invokes his self-described role as "naming czar" to question the choice of "discard" over "release." The implication is that "release" better aligns with Git’s existing naming conventions (e.g., `free_hashfile()`, `release_http_object_request()`), where "release" typically signals resource cleanup. This is a **substantive** rather than surface-level critique because it engages with the API’s design intent and consistency, not just its spelling.

The comment does not challenge the technical necessity of the patch or its implementation (which calls `git_hash_final()` into a dummy buffer as a pragmatic first step). Instead, it focuses on the interface’s clarity and long-term maintainability. No alternative implementation is proposed, and Patrick does not object to the patch’s inclusion in the series. The thread context shows that naming feedback has already been partially resolved for patch 1, with Patrick opposing a rename mid-series to prioritize leak fixes, but this comment suggests he may still push for consistency across the entire series.

For a busy reader, this is a **minor but noteworthy** point: the patch is otherwise uncontroversial and well-motivated, but the naming choice could spark a brief discussion about Git’s API conventions. The outcome may affect follow-up patches (e.g., patch 9’s platform-specific discard functions) but is unlikely to derail the series.