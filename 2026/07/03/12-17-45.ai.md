This is a surface-level reply to Matthias Beyer’s feature request for programmatic commit editing during interactive rebase.

Michal Suchánek responds with a brief suggestion: use a custom script as the `EDITOR` environment variable to automate the rebase todo list. This is a lightweight workaround rather than a direct answer to the request for a built-in `--edit-commits` option. The reply is minimal and does not engage with the technical details of the sequencer or the broader design questions raised in the original proposal (e.g., pathspec filtering, interaction with `--autosquash`, or whether the feature’s complexity is justified).

The suggestion is technically valid but sidesteps the core ask, which was for a first-class Git interface. It may serve as a temporary solution for users with similar workflows, but it does not advance the discussion toward a potential implementation. No further rationale, examples, or caveats are provided.