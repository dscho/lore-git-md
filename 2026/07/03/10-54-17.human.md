This is a review of the second patch in Patrick Steinhardt’s merged five-patch series adding reference-writing subcommands to `git refs`. The review is **surface-level**, focusing on minor usability and consistency issues rather than technical correctness.

Toon Claes raises two small points about the `git refs delete` subcommand. First, he suggests allowing both `--deref` and `--no-deref` flags, with `--deref` as the default, to make the behavior more explicit and consistent with other Git commands. Second, he notes a minor inconsistency in the test script: some tests use `git -C repo` while others `cd repo`, and one test uses `git show-ref` instead of the new `git refs exists` subcommand. He also asks what happens when deleting a symbolic reference with an `<old-value>` argument, though he does not identify a specific bug or edge case.

The review is polite and constructive but does not engage with the patch’s core design or implementation. Since the series is already merged and the points raised are cosmetic, they are unlikely to prompt further changes unless other contributors weigh in. The thread can be considered closed.