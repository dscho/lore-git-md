This is a **bugfix patch** in the `greplint.pl` series, targeting the test suite. It corrects three instances where `grep` assertions were missing file arguments, causing them to read from empty stdin instead of the intended files. The result was that these assertions were effectively no-ops—always succeeding because empty input matches nothing—rather than verifying the expected test conditions.

The fixes are straightforward: in `t2402-worktree-list.sh`, `t7507-commit-verbose.sh`, and `t7700-repack.sh`, the `grep` commands now explicitly reference the files they were meant to check (`out`, `actual`, and `packlist`, respectively). The commit message explains each case clearly, including a subtle quoting error in `t7507` where the closing quote was misplaced, turning the entire `diff --git actual` string into a pattern rather than separating the pattern (`diff --git`) from the file (`actual`).

This patch is part of the larger effort to replace bare `grep` with `test_grep`, which enforces file existence and improves debuggability by printing file contents on failure. The fixes here address pre-existing bugs that would have been surfaced (and rejected) by `test_grep`, demonstrating the value of the linter’s stricter validation. All three tests pass with the corrected file arguments, confirming the intended behavior was sound but previously unchecked.

The changes are minimal—just three one-line adjustments—and the commit message provides enough context to understand the impact without needing to trace the surrounding test logic. This is a well-motivated, low-risk fix that aligns with the series’ goal of improving test reliability. It’s ready for merging as part of the broader `greplint.pl` effort.