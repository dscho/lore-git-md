This is a **substantive review reply** in Jeff King’s memory-leak bugfix series, specifically addressing **patch 7/9** (HTTP object request cleanup). Patrick Steinhardt **endorses the technical direction** of the patch but **reiterates a design preference** for making `git_hash_discard()` idempotent, citing past pain with non-idempotent APIs (e.g., reference transactions) that led to subtle bugs.

The reply is **narrow in scope**: it does not challenge the patch’s correctness or propose an alternative implementation, but it **reopens the API-design discussion** from patch 4. Patrick’s concern is forward-looking—he wants to avoid repeating mistakes where callers must meticulously track object lifetimes. His example (reference transactions) is a strong signal that this is not just a stylistic preference but a lesson learned from real-world breakage.

For the **summarizer brief**, the key detail is the **idempotency question**: Patrick’s reply keeps alive the possibility that future revisions might revisit the hash-discard API, even if the current series proceeds with the flag-based approach. The thread’s open question now includes whether idempotency is worth the platform-specific boilerplate (as noted in patch 9’s commit message). No new symbols or files are introduced, but the **design tradeoff** (flag-based tracking vs. idempotent cleanup) is now explicitly tied to historical precedent in Git’s codebase.