This is a **substantive review** of patch 7/12 in Patrick Steinhardt’s v2 security-hardening series for the reftable backend. Toon Claes engages directly with the test case for the out-of-bounds read fix in `reftable/block.c`, proposing two targeted improvements to make the test more robust and less prone to off-by-one errors.

The review focuses on the test logic in `test_reftable_block__corrupt_block_size()`. Toon suggests replacing the hardcoded corruption value (`0xffffff`) with a dynamic calculation: read the actual block size, verify it matches expectations, then corrupt it by adding 1. This approach ensures the test always targets the minimal invalid size (just one byte beyond the buffer) rather than an arbitrarily large value, reducing the risk of unintended side effects in the test itself. The proposal also includes a sanity check (`cl_assert_equal_i(block_size, 47)`) to confirm the test is operating on the expected block size before corruption.

The feedback is narrowly scoped to test mechanics and does not challenge the fix itself, which remains uncontroversial. The change is likely to be adopted, as it aligns with the series’ goal of hardening against edge cases while maintaining test reliability. No other aspects of the patch or series are addressed in this reply.