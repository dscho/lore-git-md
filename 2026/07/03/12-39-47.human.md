This is a **bugfix clarification** for the `git rm` command, focusing on pathspec behavior and user expectations.

Patrick Steinhardt acknowledges Евгений Плискин’s follow-up and confirms that the workaround—using `:(glob)*.json`—correctly limits matching to the current directory. This aligns with Git’s pathspec documentation, where `:(glob)` disables the default behavior of `*` matching directory separators. The exchange reinforces that the original behavior (`git rm -n *.json` recursing into subdirectories) is intentional, not a bug, but highlights a potential documentation gap: users may not anticipate how shell globbing interacts with Git’s pathspec rules.

The thread remains centered on whether `git-rm(1)` should explicitly cross-reference `gitglossary(7)` or add a brief note about pathspec semantics to prevent confusion. Phillip Wood’s earlier clarification (that recursion depends on whether the shell expands the glob) adds nuance, suggesting the documentation might need to address edge cases where no matching files exist in the current directory. No patch has been proposed yet, but the discussion provides a clear technical basis for future improvements.

This is a low-risk, high-clarity issue: the behavior is correct, but the documentation could be more user-friendly. The workaround (`:(glob)`) is already documented, but its relevance to `git rm` isn’t emphasized. For developers, the takeaway is that pathspec handling is consistent across Git, but command-specific man pages may need to bridge the gap between technical correctness and user expectations.