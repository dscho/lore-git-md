Matthias Beyer opened a thread proposing a new Git feature: a programmatic way to mark commits for editing during an interactive rebase, specifically to automate splitting commits that modified files in a given subdirectory. The current manual process requires cross-referencing `git log` output with the rebase todo list, which the proposal aims to streamline with a hypothetical `--edit-commits` option.

Brian m. carlson responded with a practical alternative, pointing out that Git already supports this workflow via the `GIT_SEQUENCE_EDITOR` environment variable. He provided a concrete example—a Perl one-liner that uses `git log` and `paste` to replace "pick" with "edit" for commits matching a pathspec—demonstrating how the original use case can be solved without new code. He also shared a personal alias (`srebase`) that leverages this variable for other automation tasks. The response effectively redirects the request to an existing mechanism, sidestepping the design questions raised in the original proposal.

The thread now hinges on whether the existing `GIT_SEQUENCE_EDITOR` solution is sufficient or if a built-in option (like `--edit-commits`) is still warranted. No other participants have weighed in on the technical trade-offs, and the discussion remains focused on the practicality of the workaround versus the complexity of a new feature. The ball is in Matthias’s court to clarify whether the existing tooling meets his needs or if further discussion is needed.