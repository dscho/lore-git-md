Thread identity
Subject: Replace `--cached` with `--index` in `git diff`
Series version: standalone patch (no version trailer visible in this email)
Originating author: jakesteward0916@gmail.com
Thread type: patch proposal (CLI modernization)

Core problem or goal
Replace the `--cached` option in `git diff` with `--index` to improve user-facing clarity. The change deprecates `--cached` in favor of `--index`, which is intended to be more intuitive for users unfamiliar with Git’s internal terminology.

Current status
Patch proposed; no merge or integration branch status reported. The patch includes a deprecation warning for `--cached` and introduces `--index` as a synonym. No prior versions or follow-up iterations are mentioned in this email.

Key technical details
- Command: `git diff`
- New option: `--index` (replaces `--cached`)
- Deprecated option: `--cached` (with warning)
- Subsystem: CLI argument parsing
- No new symbols, on-disk format changes, or test file additions reported in this email.

Open questions and loose ends
- Is `--index` behaviorally identical to `--cached` in all edge cases (e.g., combined diffs, submodules, sparse-checkout)?
- What is the deprecation timeline for `--cached`? Is it slated for removal in Git 3.0 or later?
- Are there backward-compatibility risks for scripts or tools relying on `--cached`?
- Has the patch been tested against the full `t/` suite, particularly `t4000-diff-format.sh` and related tests?
- No substantive review has engaged with the implementation; only surface-level endorsement exists so far.

Participant positions
- jakesteward0916@gmail.com: author; proposed the patch.
- krobchai wongkamw: surface-level review; supports the change as "a good idea" but provides no technical critique or suggestions. Position is low-weight.