This is a **test efficiency patch** in Toon Claes’s nine-patch series making `GIT_TEST_LONG` tests reliable and CI-friendly. This reply from Patrick Steinhardt clarifies the motivation for patch 6/9, which refactors `t7900-maintenance.sh` to clean up the 2 GB repository immediately after the expensive test.

Patrick acknowledges Jeff King’s observation that the patch does not reduce runtime (still ~1m40s with `--long`) but reinforces the primary goal: isolating the expensive test to prevent it from affecting later tests. The key argument is that an `EXPENSIVE`-marked test should not leave behind large repositories that could be repacked by subsequent tests, regardless of runtime impact. This aligns with the series’ broader aim of making `GIT_TEST_LONG` tests self-contained and predictable in CI.

The exchange is brief and confirms the patch’s value as a defensive measure. No objections or open questions remain; Jeff’s feedback was observational, and Patrick’s reply closes the loop on the patch’s rationale. The patch itself is uncontroversial and touches only the test script (`t7900-maintenance.sh`), with no user-facing changes. Future test additions to the suite will benefit from this isolation.