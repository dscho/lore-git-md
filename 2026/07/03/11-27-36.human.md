This is a **substantive review reply** in Jeff King’s memory-leak fix series, specifically on **patch 7/9** (HTTP object request cleanup). Patrick Steinhardt engages with the design trade-off Jeff described in the commit message: whether `git_hash_discard()` should be made idempotent to simplify error paths.

Patrick endorses the idea of idempotency, calling it "great" and citing past pain with similar interfaces (notably reference transactions, which historically required careful lifetime management and were a source of bugs). His feedback is not a request for an immediate change—Jeff already explained why the current flag-based approach is pragmatic—but it signals that the API’s design is worth revisiting in the future. The comment is brief but substantive: it connects the current patch to a broader pattern of Git interfaces that have caused maintenance headaches, and it implicitly suggests that idempotency could be a desirable property for the new `git_hash_discard()` primitive.

For a human reader, this is a low-drama exchange. The series is on track, and Patrick’s note is more of a "for the record" observation than a blocker. The takeaway: the HTTP fix is uncontroversial, but the series might eventually grow a follow-up patch to make the discard function idempotent if the benefit outweighs the platform-specific boilerplate.