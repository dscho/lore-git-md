This is a brief, surface-level reply in the ongoing thread about `git rm -n *.json` unexpectedly recursing into subdirectories. Junio C Hamano endorses Mikael Magnusson’s earlier suggestion to use `failglob` in bash as a user-side workaround for the shell-Git interaction issue. The comment is purely supportive and adds no new technical detail or direction to the discussion.

The thread remains focused on whether `git-rm(1)` should clarify pathspec behavior or cross-reference `gitglossary(7)`. Junio’s reply signals agreement that the behavior is correct but acknowledges the user-experience friction, though he does not propose or hint at a documentation patch. For a busy reader, this email is a minor footnote—it confirms the `failglob` workaround is worth considering but does not advance the core question of how (or whether) the man page should change.