This is a substantive review in the ongoing thread about `git rm -n *.json` unexpectedly recursing into subdirectories. Mikael Magnusson engages directly with the core issue: the pathspec behavior and its documentation. He quotes the relevant section from `gitglossary(7)` that explains why `*` matches directory separators by default, which is the technical root of the observed recursion. His suggestion to use `failglob` in bash is a practical workaround for users who want the shell to reject unmatched globs rather than passing them literally to Git, but he does not propose a code or documentation change.

The review clarifies that the man page for `git rm` does not actually promise non-recursive behavior for glob patterns, countering the original report’s claim. This shifts the discussion further toward documentation: the glossary already documents the recursive matching rule, but users may not find it or may misinterpret the interaction between shell globbing and Git’s pathspec logic. Mikael’s reference to `gitglossary(7)` reinforces Patrick Steinhardt’s earlier point that the behavior is correct but under-documented in the context of `git rm`.

No new technical details are introduced, but the review strengthens the case that the issue is about user expectations and discoverability, not a code defect. The thread remains focused on whether `git-rm(1)` should cross-reference the glossary or add a brief note about pathspec modifiers like `:(glob)`. Mikael’s comment is a concise, technically precise contribution that helps narrow the scope of any potential documentation patch.