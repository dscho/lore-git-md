This is the third patch in the v6 series introducing `--linearize` to `git replay`, and Junio C Hamano’s reply is a **surface-level review** of the documentation. He raises two minor points:

1. **Authorship responsibility**: Junio questions whether Johannes Schindelin should remain the primary author (and thus responsible for latent bugs) given the extensive changes in this iteration, or if Toon Claes should take over. This is a procedural nit about commit attribution, not a technical objection.

2. **Documentation clarity**: Junio suggests either justifying the incompatibility between `--linearize` and `--revert` (e.g., "because reverting a merge requires preserving the merge topology") or moving the note to a BUGS/LIMITATIONS section instead of stating it as an absolute. This is a wording suggestion, not a request to change the code.

The patch itself remains **technically complete** and ready for review. No unresolved technical objections exist; this reply only touches on documentation presentation. The interface design debate (standalone `--linearize` vs. `git rebase`-style `--rebase-merges=<mode>`) is still unresolved but no longer blocking. The series is under active review, with no merge status reported.