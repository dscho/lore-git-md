Thread identity
Subject: Feature request: programmatic commit editing during interactive rebase
Series version: N/A (standalone feature request)
Originating author: Matthias Beyer
Thread type: feature request

Core problem or goal
Provide a programmatic way to mark commits for editing during an interactive rebase, specifically to automate the workflow of splitting commits that modified files in a given subdirectory. The current manual process requires cross-referencing `git log` output with the rebase todo list.

Current status
The request has been effectively answered by an existing Git mechanism (`GIT_SEQUENCE_EDITOR`). No implementation work is needed; the original use case can be addressed with a one-liner script. The thread is now informational rather than a call for development. A minor correction to the recommended command-line usage was provided: `--autosquash` no longer requires `-i` (interactive mode) as of commit f8f87e082798, simplifying the automation pattern.

Key technical details
- Subsystem: sequencer (interactive rebase implementation)
- Existing mechanism: `GIT_SEQUENCE_EDITOR` environment variable, which allows arbitrary modification of the rebase todo list
- Example solution: `GIT_SEQUENCE_EDITOR='perl -pi -e "s/^pick/edit/ if /<pattern>/"' git rebase --autosquash <base>`
- Pattern construction: `git log --format=%h -- <pathspec> | paste -sd '|' -` generates a regex matching commits that touched the specified subdirectory
- No new CLI options, symbols, or on-disk format changes required
- Workaround previously suggested (`EDITOR` script) is superseded by `GIT_SEQUENCE_EDITOR`, which is designed for this purpose
- Redundant flags: `-i` is no longer required with `--autosquash`; the correct invocation is now `GIT_SEQUENCE_EDITOR=true git rebase --autosquash`

Open questions and loose ends
- Whether the original requester considers the `GIT_SEQUENCE_EDITOR` solution sufficient for their workflow
- Whether the project should document this pattern more prominently (e.g., in `git-rebase(1)`) to reduce future feature requests for similar automation
- No unresolved technical or design questions remain; the thread has reached a natural conclusion

Participant positions
- Matthias Beyer (author): proposed the feature, provided concrete use case (splitting commits by subdirectory), and sketched a command-line interface. Position now likely satisfied by the existing mechanism.
- Michal Suchánek: suggested a custom `EDITOR` script as a workaround; contribution remains low-weight as it did not engage with the core design questions.
- brian m. carlson: provided the definitive answer, demonstrating that the requested functionality already exists via `GIT_SEQUENCE_EDITOR`. The response was substantive (engaged with the actual use case and provided a complete, working example) and effectively closed the discussion by redirecting the request to an existing feature.
- Matt Hunter: provided a minor but useful correction to the command-line usage, noting that `--autosquash` no longer requires `-i`. The contribution is factual and directly relevant to scripting, but remains low-weight as it does not engage with the original feature request or sequencer internals.

Related work
None. The thread remains self-contained; no prior versions, external dependencies, or related in-flight topics were mentioned.