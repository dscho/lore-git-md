This is the seventh patch in Patrick Steinhardt’s nine-patch v2 series making `GIT_TEST_LONG` tests reliable and efficient for CI. It introduces consistent boolean parsing for the `GIT_TEST_LONG` environment variable by switching to the `test_bool_env` helper, allowing the variable to be explicitly disabled (e.g., `GIT_TEST_LONG=false`).

The patch touches two files: `ci/lib.sh` and `t/test-lib.sh`. In `ci/lib.sh`, it updates the CI script to export `GIT_TEST_LONG=true` instead of `YesPlease`, aligning with the boolean convention. In `t/test-lib.sh`, it replaces the direct `test -n "$GIT_TEST_LONG"` check in the `EXPENSIVE` prerequisite with `test_bool_env GIT_TEST_LONG false`, ensuring the variable is parsed as a boolean. The change is mechanical and uncontroversial, addressing a minor but persistent usability issue where setting `GIT_TEST_LONG=false` had no effect.

The motivation is clear: users and CI scripts should be able to disable `GIT_TEST_LONG` explicitly, and the current implementation does not support this. The patch resolves the issue without introducing new behavior or dependencies. It is a small but useful cleanup that complements the broader series goal of making `GIT_TEST_LONG` tests more predictable and manageable in CI. No substantive objections or feedback were raised in the thread, and the change looks ready for integration.