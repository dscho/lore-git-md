This is a **bugfix patch discussion** in Jeff King’s nine-part series plugging memory leaks in non-default hash implementations. The email is a **substantive review reply** from brian m. carlson endorsing Patrick Steinhardt’s call for `git_hash_discard()` to be made idempotent.

brian m. carlson chimes in to support Patrick’s earlier suggestion that the hash-discard API should be idempotent, meaning it can safely be called multiple times on the same context. They highlight a practical benefit: Rust code (likely referring to Git’s ongoing Rustification effort) would gain simpler `Drop` implementations if `git_hash_discard()` could be called unconditionally, avoiding the need to track whether the hash context has already been finalized. This reinforces the design preference for idempotency as a long-term maintainability win, not just a stylistic nicety.

The discussion remains focused on the **API design tradeoff** introduced in patch 4 and revisited in patch 7. No new implementation is proposed here, but brian m. carlson’s input adds weight to the idea that idempotency is worth pursuing, even if it requires platform-specific boilerplate. The thread’s open question now includes whether future revisions should prioritize idempotency over the current flag-based approach. For now, the series proceeds with the pragmatic (but non-idempotent) solution, but this reply suggests the conversation may resurface if the series is revised.