This is a **test infrastructure** follow-up in Toon Claes’s series to make `GIT_TEST_LONG` tests reliable and efficient in CI. The email is a brief but substantive review reply from Jeff King (Peff) to Patrick Steinhardt, closing the loop on a design question about the `test_copy_bytes` helper.

Peff’s earlier review had questioned whether the helper—originally added for portability—could be modernized, noting that `head -c` (non-POSIX) might now be widely supported. Patrick responded that the helper’s inefficiency is not worth fixing, since nearly all callers copy small amounts of data (the largest case, 65 KB in `t0021-conversion.sh`, completes in seconds). Peff now agrees, dropping the issue. This exchange confirms that the patch’s immediate fix (replacing `dd` with `genzeros` in `t4141-apply-too-large.sh`) stands, while the broader helper redesign is off the table.

The thread remains focused on patch 3/9’s trade-offs: runtime improvement versus resource consumption. Peff’s concession here does not reopen SZEDER Gábor’s earlier objection about retaining the `EXPENSIVE` prerequisite for memory usage (which Patrick has already agreed to restore in the next revision). No new technical concerns are introduced, and the patch’s scope is now clearly settled. The series as a whole continues to look well-motivated and uncontroversial, with only minor adjustments expected in the next iteration.