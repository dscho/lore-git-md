This thread is about a macOS-specific bugfix for a crash in `git status` when handling UTF-8 filenames longer than 255 bytes. The issue occurs in Git's Unicode normalization layer (`precompose_utf8`), where a buffer size mismatch triggers a fortified libc overflow check. The fix, from a first-time contributor to this area, restructures the `dirent` buffer as a flexible array member and adjusts memory allocation to match the actual requirements.

The patch is narrowly scoped to `compat/precompose_utf8.c` and includes a regression test that verifies the fix by creating a 261-byte non-ASCII filename. The implementation follows Git's coding conventions, using `FLEX_ARRAY` and safe size calculations. There's no impact on other platforms, and the commit message clearly explains the crash scenario and the interaction with fortified libc. So far, the patch appears uncontroversial and well-prepared for merging.