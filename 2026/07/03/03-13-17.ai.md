This is the first patch in the v6 series introducing `includeIf.worktree:<pattern>` conditions. It refactors the existing `include_by_gitdir()` function into a more generic `include_by_path()` helper, preparing the codebase for the new worktree-based config inclusion logic.

The patch mechanically generalizes the path-matching logic by replacing the hardcoded `opts->git_dir` extraction with an explicit `path` parameter. The refactoring is minimal and focused: it preserves all existing behavior while enabling reuse of the same pattern-matching infrastructure for the upcoming worktree condition. The changes are confined to `config.c`, with no functional impact on current Git behavior.

This is a clean preparatory step that avoids code duplication when adding the new worktree condition in the next patch. The commit message clearly explains the motivation (preparing for worktree-based inclusion) and the technical approach (generalizing the interface). The patch itself is straightforward, with no edge cases or behavioral changes introduced at this stage.

The refactoring touches the core config inclusion logic but maintains backward compatibility. The new `include_by_path()` function will be called with `opts->work_tree` in the next patch, completing the worktree condition implementation. This patch is unlikely to be controversial, as it is purely preparatory and does not alter any user-visible behavior.