This is a **bugfix patch discussion** in Patrick Steinhardt’s ODB abstraction series. Toon Claes chimes in on the first patch, which fixes a latent bug in loose-object map loading: the code was always using the primary ODB’s `objects/` directory instead of the source-specific path, breaking repositories that use a compatibility hash algorithm (e.g., SHA-256 with SHA-1 compatibility) and have alternates.

Toon’s reply is brief but substantive. He agrees the commit message downplays the severity—this isn’t just a logical inconsistency but a real-world breakage—and suggests a test case to verify the fix. The proposed test creates two repositories (one acting as an alternate), configures both with SHA-256 and SHA-1 compatibility, and checks that `git rev-parse` correctly resolves an object borrowed from the alternate when using the compatibility hash. The test is narrowly scoped to the bug’s edge case and looks well-crafted.

No objections or design concerns are raised; this is a straightforward endorsement of the fix with a practical improvement. The patch itself is already confirmed correct by Justin Tobler’s earlier review, so Toon’s contribution is additive—helping ensure the fix is both robust and verifiable. If Patrick incorporates the test, it will strengthen the patch’s credibility without requiring further design changes.