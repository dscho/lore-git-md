This is a **bug report** with a clear discrepancy between documentation and behavior in `git config --global`. The author, Nils Fahldieck, identifies a real and confusing issue: when both `~/.gitconfig` and the XDG config file (`~/.config/git/config`) exist, `git config --global` reads from only one of them (whichever is "preferred" by the current logic), while the documentation claims it reads from both. The report includes a detailed analysis, a reproducible test case, and a proposed fix (Option A: make the code match the documented behavior).

The problem is well-motivated and the reproducer is straightforward, making it easy for others to verify. The author also shows familiarity with the codebase by pinpointing the relevant functions and lines. This looks like a legitimate bug that could trip up users who rely on the documented behavior, especially since the asymmetry between `git config --get` (which reads both files) and `git config --global` (which does not) is unintuitive.

Nils is offering to contribute a patch, so this thread may evolve into a review discussion if others agree the behavior should be fixed. The report itself is thorough and credible—worth attention from anyone who cares about config handling or documentation accuracy.