This is a **follow-up discussion** in the ongoing thread about the `hn/history-squash` topic, specifically addressing Phillip Wood’s review of the `--reedit-message` option and message template handling.

Harald Nordgren (the author of `hn/history-squash`) responds to Phillip’s critique of the `--reedit-message` option, proposing to **remove the option entirely** and make re-editing the default behavior. This would mean the editor always opens during a squash operation, allowing users to either modify the message or exit without changes. Harald suggests this might eliminate the need for a `--no-edit` switch, though he does not explicitly rule it out. He also expresses uncertainty about changing the message template, leaving that aspect of Phillip’s feedback unaddressed.

The reply is **brief and tentative**, focusing narrowly on the `--reedit-message` UX issue without engaging with the broader correctness concerns (e.g., `UNINTERESTING` commits, root commits, or multi-tip histories) raised in Phillip’s review. It does not include technical rationale, test cases, or a revised implementation plan. The tone suggests openness to feedback but does not commit to a specific course of action.

This exchange advances the thread by clarifying the author’s stance on one of the UX issues, but leaves the more critical correctness flaws unresolved. The proposal to default to re-editing may simplify the interface, but it does not address Phillip’s underlying concern about preserving user intent (e.g., `amend!`/`squash!` messages) or the inconsistency with other `git history` commands. The message template issue remains untouched, and the broader design questions (e.g., ref safety, reachability logic) are not revisited. Junio’s earlier ejection of the topic from `next` still stands, pending Phillip’s fixups.