This is a documentation patch. Kristoffer Haugsbakk updates the `git-format-rev` man page to use the `[synopsis]` AsciiDoc block marker for a code example that contains the placeholder `<subject>`. The change follows the synopsis-style conversion effort led by Jean-Noël Avila, which aims to standardize man page formatting across the Git documentation. The patch is a single-line addition and is part of a one-patch series (topic `kh/doc-format-rev-1`).

The motivation is to improve visual consistency: the `[synopsis]` marker ensures the placeholder is rendered distinctly, matching the style introduced in an earlier commit (a34d1d53). The commit message is clear and references the relevant prior work. This is a routine documentation cleanup with no functional impact, but it contributes to the broader goal of uniform man page presentation.

The patch itself is trivial and uncontroversial. It touches only the `Documentation/git-format-rev.adoc` file and does not interact with any code or tests. Given the ongoing synopsis-style conversion effort, this kind of patch is likely to be welcomed as incremental progress. No review feedback has appeared yet, but the change looks well-targeted and in line with project conventions.