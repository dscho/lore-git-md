This is a **maintainer response** in the `git config --global` documentation/behavior discrepancy thread. Junio C Hamano engages with the two claims in Nils Fahldieck’s bug report, focusing on the reading behavior (claim 2) as the substantive issue.

Junio dismisses the first claim (about the documentation’s wording around "doesn’t exist" vs. "not readable") as pedantic and of little practical value. He notes that an unreadable file is functionally equivalent to a nonexistent one, and the distinction between a zero-byte and non-empty file is irrelevant to the behavior. This effectively rejects the proposed wording change for that part of the documentation.

On the second claim -- the core issue that `--global` reads only one file despite the documentation claiming it reads both -- Junio agrees the documentation is wrong and needs correction. He does not explicitly endorse either of Nils’s proposed fixes (Option A: align code with docs by reading both files; Option B: align docs with code by documenting the single-file behavior), but his phrasing ("The documentation needs to be corrected") suggests he leans toward Option B (fixing the docs to match the code). No technical rationale is provided, and the response does not address backward compatibility or user expectations.

The email advances the thread by settling the documentation’s accuracy as the primary issue, but leaves the choice of fix open. Future discussion will likely focus on whether to update the code (Option A) or the documentation (Option B). The maintainer’s stance on the latter is now clear, but the thread remains unresolved.