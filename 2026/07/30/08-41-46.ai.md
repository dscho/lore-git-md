This is a **feature addition**—the ongoing discussion about `git add --resolved`—and the thread is now focused on the **user-facing interface design**. Junio responds to Michael Montalbo’s critique of the `--resolved` option name, defending the current design as workflow-oriented and consistent with existing Git commands like `git am --resolved`.

The key point of contention is whether `--resolved` should be a monolithic operation (as implemented) or split into a more general `--unmerged` selector with explicit policy flags (as proposed by Montalbo). Junio argues that `--resolved` is deliberately named to reflect the user’s intent—signaling that conflicts have been dealt with and are ready to be staged—rather than merely selecting unmerged paths. He sees no practical need for policy overrides like `--allow-conflict-markers`, suggesting that users can manually stage exceptions with `git add $path` if needed. He also expresses skepticism about the usefulness of `--skip-conflict-markers`, questioning how users would recover from such an operation.

The discussion is now at a conceptual impasse: Montalbo’s proposal prioritizes flexibility and alignment with the underlying implementation, while Junio’s design prioritizes workflow clarity and consistency with existing Git conventions. No technical issues remain unresolved in the v2 series, but this naming debate could shape the final interface. The thread is likely to continue with further arguments or counter-proposals, as naming choices in Git often carry long-term implications for user expectations.