This is a documentation patch, part of the ongoing effort to convert Git man pages to a consistent synopsis style using AsciiDoc markup. The patch adds a `[synopsis]` block definition to a code example in `Documentation/git-format-rev.adoc` to properly highlight the `<subject>` placeholder. The change is minimal—one line added—and follows the pattern established in commit a34d1d53 (doc: convert git-show to synopsis style, 2026-02-06).

The patch targets the `git format-rev` command’s documentation, which describes how to format revision output for use in other commands. The modified example shows how `--stdin-mode=text` replaces object names with formatted commit data (e.g., `<subject>`). The `[synopsis]` tag ensures the placeholder is rendered with consistent styling, matching the project’s goal of uniform man page presentation.

Kristoffer Haugsbakk is the author, a frequent contributor to Git’s documentation improvements. This patch is standalone (not part of a larger series) and touches only the `git-format-rev` man page. The commit message is clear and references prior work, though it assumes familiarity with the synopsis-style conversion effort.

No behavioral changes are introduced—this is purely a markup adjustment. The patch is unlikely to be controversial, as it aligns with an established documentation initiative. Reviewers may check for consistency with other synopsis blocks but are unlikely to raise technical objections. The change is trivial enough that it could be merged without extensive discussion.