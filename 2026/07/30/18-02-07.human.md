This is the **acknowledgment and merge signal** for the second patch in Shlok Kulshreshtha’s two-patch series introducing the `commit_body()` helper to the Git test suite. The series is now marked for the `next` integration branch, meaning it is ready for broader integration testing but not yet merged to `master`.

The patch itself converts 12 test scripts to use the new `commit_body()` helper, replacing the exit-code-masking idiom `git cat-file commit <rev> | sed -e '1,/^$/d'` with a safer, reusable function. Junio Hamano spot-checked the changes and found them reasonable; Shlok confirms that 63 of the 66 hunks are mechanical replacements, with only three requiring closer attention (removing a local helper in `t7509`, consolidating a grep in `t7600`, and avoiding a pipeline in `t3900`). The series addresses a long-standing issue in the test suite, motivated by a prior bugfix in `t7614`, and is well-scoped with no open questions or objections. It looks like a clean, uncontroversial improvement to test infrastructure.