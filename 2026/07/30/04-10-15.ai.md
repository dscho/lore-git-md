This is a **build system** patch that has been **merged to `master`** by Junio C Hamano, resolving the last remaining issue in the `WITH_BREAKING_CHANGES=1` test-failure thread. The patch moves the inclusion of `config.mak`, `config.mak.autogen`, and `config.mak.uname` earlier in the Makefile—from after the list of binaries to build is generated to before it—so that configuration settings like `WITH_BREAKING_CHANGES` take effect at the correct time. This ensures that `git-whatchanged` and `git-pack-redundant` are not built when `WITH_BREAKING_CHANGES=1` is set, eliminating the stale build artifacts that caused the `t1517-outside-repo.sh` test failures.

Junio’s reply expresses caution about the change, noting that it exposes a large number of Makefile symbols (including `PROGRAM_OBJS`, `BUILT_INS`, `WITH_BREAKING_CHANGES`, and platform-specific variables like `uname_S`) to `config.mak` earlier than before. He highlights a potential inconsistency: the `RUST_LIB_NAME` logic (which depends on `uname_S`) appears *before* the new inclusion point, suggesting that the current setup might already be fragile or that the patch could introduce subtle platform-specific issues. However, he does not block the patch, and the merge confirms that the change is considered safe enough for `master`.

The patch itself is minimal—just four lines moved—and directly addresses the root cause identified in the thread: the build system was reading configuration *after* generating the list of binaries to build, so `WITH_BREAKING_CHANGES` had no effect on the initial build targets. Brian’s commit message explicitly ties the patch to the earlier discussion, framing it as a necessary follow-up to Jeff King’s test fixes to fully resolve the issue. The change is low-risk for most users, as it only affects builds with custom `config.mak` settings (like `WITH_BREAKING_CHANGES`), and it has no user-visible behavior or on-disk format impact.

With this patch merged, the thread is now **fully resolved**: the `t0014-alias.sh` failures were fixed by Peff’s dynamic command-list series, and the `t1517-outside-repo.sh` failures are now prevented by ensuring the build system respects `WITH_BREAKING_CHANGES` from the start. No further action is expected unless platform-specific regressions emerge (e.g., on Windows, where Junio noted a potential dependency on `uname_S`). The patch is a pragmatic, targeted fix that aligns with the project’s goal of supporting the `WITH_BREAKING_CHANGES` configuration as contributors prepare for Git 3.0.