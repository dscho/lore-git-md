This is a **build system** patch that has now been **merged to `master`**. Junio C Hamano’s reply confirms the patch is correct and necessary, despite initial concerns about its scope. The change moves the inclusion of `config.mak` and related files earlier in the Makefile so that configuration settings like `WITH_BREAKING_CHANGES=1` take effect before the list of binaries to build is generated. This prevents stale build artifacts (like `git-whatchanged` and `git-pack-redundant`) from being created when they shouldn’t be, which was causing test failures in `t1517-outside-repo.sh` and `t0014-alias.sh`.

The patch is small—just four lines moved—but its impact is significant for developers working with `WITH_BREAKING_CHANGES`, a configuration flag expected to see more use as Git 3.0 approaches. Junio’s initial hesitation stemmed from the patch’s potential to affect many symbols (like `PROGRAM_OBJS`, `SHELL_PATH`, and `RUST_LIB_NAME`), but he verified that the change doesn’t break existing functionality, including the Windows-specific `RUST_LIB_NAME` logic. Brian’s follow-up explanation clarified that the patch is narrowly targeted: it ensures `WITH_BREAKING_CHANGES` is respected during the initial build target generation, which is exactly what’s needed to avoid the test failures.

This is the final piece of a fully resolved thread. Earlier, Jeff King (Peff) fixed the `t0014-alias.sh` failures by making the test dynamically generate its list of deprecated commands, but those fixes alone weren’t enough to prevent stale artifacts from causing issues in other tests. Brian’s patch addresses the root cause by ensuring the build system respects the configuration flag at the right time. The combination of Peff’s and Brian’s patches means developers can now build and test with `WITH_BREAKING_CHANGES=1` without encountering false positives or requiring manual cleanup.

The patch is low-risk and well-motivated, with no user-visible changes or on-disk format implications. It’s a pragmatic fix for a specific edge case in the build process, and Junio’s merge signals that it’s ready for the next release. If you’re working with `WITH_BREAKING_CHANGES` or preparing for Git 3.0, this patch ensures your test suite will behave as expected.