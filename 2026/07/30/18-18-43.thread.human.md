This thread is about a discrepancy between the documented and actual behavior of `git config --global`. The documentation claims it reads both `~/.gitconfig` and the XDG config file (`~/.config/git/config`), but the implementation only reads one file—prioritizing `~/.gitconfig` even if it’s empty. This inconsistency also affects interactions with include directives, suggesting a broader issue in how global configuration is handled.

The discussion remains unresolved on whether to fix the code to match the documentation (Option A: make `--global` read both files) or update the documentation to reflect the current behavior (Option B: document the single-file behavior). The maintainer, Junio Hamano, and Ben Knoble agree the documentation is incorrect, but neither has endorsed a specific fix. Junio’s comments suggest a preference for Option B, while Nils Fahldieck (the original reporter) and Ben’s observations about unintuitive behavior with `--global --no-includes` strengthen the case for Option A. Nils has also highlighted a practical issue: `git config --get` reads both files, while `--global` does not, creating confusion for users and tools that rely on consistent behavior.

Nils has expressed willingness to contribute a documentation patch but is unsure about the process for updating both the Git man pages and the git-scm.com website. The thread may soon shift toward implementation, but the core question of which fix to pursue remains open. No consensus has emerged, and the maintainer’s preference for Option B has not been directly challenged. The debate now centers on user expectations and the inconsistency between `--get` and `--global`.