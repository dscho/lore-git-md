This thread is a bug report from Nils Fahldieck about a discrepancy between the documented and actual behavior of `git config --global`. The documentation claims that `--global` reads both `~/.gitconfig` and the XDG config file (`~/.config/git/config`), but the implementation only reads one file, prioritizing `~/.gitconfig` even if it is empty. Nils proposed two fixes: Option A (align the code with the docs by reading both files) or Option B (align the docs with the code by documenting the single-file behavior).

Junio C Hamano, the maintainer, has now weighed in, dismissing a minor documentation nit as irrelevant but agreeing that the core issue—the documentation’s claim that `--global` reads both files—is incorrect. He suggests the documentation needs correction, implying a preference for Option B (fixing the docs to match the code), though he does not explicitly rule out Option A. The thread remains unresolved, with the key question now being whether to update the code to match the docs (Option A) or update the docs to match the code (Option B). No other participants have yet contributed, and backward compatibility or user expectations have not been addressed.