This is the v2 patch series for the `--drop-filtered` feature in `git repack`, targeting partial clones. The series is now ready for substantive review after addressing the blocking issues from v1: the `--write-bitmap-index` validation flaw (fixed by distinguishing command-line vs config-driven bitmap settings), safety guards (merge/rebase/cherry-pick checks and index validation), and function signature reorganization. The drop log and remote verification remain deferred to follow-ups, but the core functionality is complete and documented.

The implementation adds two new options to `git repack`: `--drop-filtered` (real runs) and `--dry-run` (reporting only). It introduces `repack-filtered.c` for the core logic, extends `list-objects-filter` with `list_objects_filter__filter_oidset()`, and modifies `repack-promisor.c` to support excluding objects during promisor pack rebuilding. The series is well-structured, with tests distributed across the relevant commits and safety guards implemented to prevent drops during merges or when blobs are referenced by the current index. The commit messages are clear, and the documentation (`git-repack.adoc`) is now included.

This v2 looks like a serious, production-ready effort. The author has addressed the critical feedback from v1, and the series now includes all the non-negotiable safety mechanisms. The deferred work (drop log, remote verification) is explicitly scoped and does not block the core functionality. The patch touches the repack machinery, promisor infrastructure, and object filtering, so reviewers with expertise in those areas (e.g., Patrick Steinhardt, Christian Couder, or Taylor Blau) will likely weigh in. The series is motivated by a real pain point for partial clone users and leverages existing repack crash-safety guarantees. Worth attention from anyone interested in partial clones or repack optimizations.