This thread is about a small but important documentation cleanup: Kristoffer Haugsbakk’s two-patch series (now at v4) that revises the `<subject>` placeholder styling in a `git format-rev` code example as part of the ongoing man-page synopsis-style conversion. The goal is to apply consistent AsciiDoc markup—replacing `<subject>` with `"<subject>"` for clarity and adding a `[synopsis]` block to properly highlight placeholders—while ensuring compatibility with both AsciiDoc and AsciiDoctor.

The series has now cleared its last procedural hurdle. Junio confirmed that the v4 fix (switching from a code block to an open block) resolves the rendering inconsistency that blocked v3, and he intends to queue the patches for integration testing in the `next` branch. Patrick Steinhardt’s earlier `Acked-by` was invalidated by the block syntax change, but no technical objections remain, and the series is effectively ready to merge. The changes are documentation-only, touching only `Documentation/git-format-rev.adoc`, and introduce no new symbols or on-disk format changes. Once in `next`, the topic will cook alongside other in-flight work before graduating to `master`.