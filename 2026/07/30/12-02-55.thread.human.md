This thread is about a small documentation patch from Kristoffer Haugsbakk that adds a `[synopsis]` AsciiDoc block to a code example in `git-format-rev.adoc`. The change is part of the ongoing effort, led by Jean-Noël Avila, to standardize man page formatting using consistent AsciiDoc markup. The patch itself is trivial—just one line added to highlight the `<subject>` placeholder in an example demonstrating `--stdin-mode=text` output formatting.

The patch was originally sent without response, so this is a resend to bump visibility. It follows the established pattern from earlier conversions (like `git-show` in commit `a34d1d53`) and touches only the documentation, with no behavioral or code changes. Kristoffer is a regular documentation contributor, and this patch is uncontroversial, aligning with a well-established project initiative. No substantive objections are expected, though reviewers may check for consistency with other synopsis blocks. It’s ready to be merged if it hasn’t been overlooked.