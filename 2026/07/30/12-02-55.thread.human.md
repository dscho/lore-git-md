This thread is about a small but important documentation cleanup in `git-format-rev.adoc`, part of the ongoing effort to standardize AsciiDoc styling across Git’s man pages. Kristoffer Haugsbakk originally proposed using a `[synopsis]` block to style the `<subject>` placeholder in a code example, but after a brief discussion with Patrick Steinhardt, he revised the patch to replace the placeholder with literal quotes (`"subject"`). This change sidesteps the earlier debate about whether `[synopsis]` was appropriate for non-synopsis content while keeping the example clear and readable.

The patch is now uncontested and ready for merging. Both participants agree the adjustment is the right call—it avoids awkward markup while still advancing the broader goal of consistent documentation. The outcome also sets a pragmatic precedent for future patches in the conversion effort: when `[synopsis]` feels forced, rewriting the example to use literal values is a valid alternative. No further revisions or discussions are expected, and the change has no impact beyond the single file it touches.