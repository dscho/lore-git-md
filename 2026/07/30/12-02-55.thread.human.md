This thread is about a small but important documentation cleanup: Kristoffer Haugsbakk’s two-patch series (now preparing v4) that revises the `<subject>` placeholder styling in `Documentation/git-format-rev.adoc` to align with the project’s ongoing man page synopsis-style conversion. The goal is to make the documentation more consistent and readable by applying standard AsciiDoc markup to a code example.

The series has evolved through review: after Patrick Steinhardt’s feedback on v1, Kristoffer split the changes into two pragmatic commits—first replacing `<subject>` with literal quotes (`"<subject>"`) for clarity, then adding a `[synopsis]` block to properly highlight placeholders. Both patches now carry Patrick’s `Acked-by`, and the only remaining hurdle was a procedural one: a CI failure caused by an AsciiDoc/AsciiDoctor rendering inconsistency. Junio confirmed Kristoffer’s diagnosis that the patch used a code block (`----`) where other synopsis-style documentation uses an open block (`--`). This will be fixed in v4, which should resolve the issue and leave the series ready for merging.

The thread is effectively closed on technical grounds; the only action left is for Kristoffer to post the corrected v4. No objections remain, and the changes are uncontroversial—just a small step in the broader effort to standardize Git’s documentation.