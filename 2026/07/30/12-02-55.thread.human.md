This thread is about a small but important documentation cleanup. Kristoffer Haugsbakk sent a two-patch series (v2) to update the `<subject>` placeholder styling in a `git format-rev` code example, part of the ongoing effort to standardize Git's man pages using AsciiDoc synopsis style. The goal was to make the example clearer and more consistent with the rest of the documentation.

The first patch replaced `<subject>` with `"<subject>"` to better reflect the actual input format (a commit subject enclosed in quotes), while the second patch added a `[synopsis]` block to properly highlight placeholders in the example. Patrick Steinhardt reviewed the series, initially questioning whether `[synopsis]` was the right choice for a non-synopsis code block but ultimately agreeing with the revised approach. His final reply here simply confirms the patch now meets his expectations, closing the discussion.

The series is now ready to merge, with no objections or open questions remaining. It’s a straightforward documentation-only change that doesn’t affect behavior, tests, or on-disk formats. The outcome reinforces a pragmatic approach to markup rules, showing that flexibility is sometimes needed—especially in non-synopsis contexts—to balance consistency with readability. This may set a useful precedent for similar decisions in the broader documentation conversion effort.