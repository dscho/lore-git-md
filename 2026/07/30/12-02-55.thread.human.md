This thread is about a small but important documentation cleanup: Kristoffer Haugsbakk’s two-patch series (now at v3) that updates the `<subject>` placeholder styling in `git-format-rev.adoc` to match Git’s ongoing man page synopsis-style conversion. The patches themselves are uncontroversial—Patrick Steinhardt has already given his `Acked-by` on both—and the technical approach (using literal quotes and a `[synopsis]` block) was settled in earlier rounds.

The only remaining hurdle is procedural: Junio noticed that the patch failed CI under AsciiDoc while passing locally under AsciiDoctor, and Kristoffer has now traced the issue to a mismatch in block syntax (`----` vs `--`). This is a routine snag for documentation patches, and Kristoffer plans to send a v4 once he’s verified the fix. Once that’s resolved, the series should move forward quickly—there are no open technical objections, and the changes are a clear improvement in readability and consistency.