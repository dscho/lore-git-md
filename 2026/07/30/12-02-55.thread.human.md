This thread is a straightforward documentation cleanup as part of Git’s ongoing effort to standardize man page formatting. Kristoffer Haugsbakk sent a two-patch series (now at v2) updating `Documentation/git-format-rev.adoc` to make a code example more readable and consistent. The first patch, just posted, replaces the `<subject>` placeholder with `"<subject>"` in a `--stdin-mode=text` example, matching the implied input format and improving clarity. The second patch will follow with a `[synopsis]` block to properly highlight placeholders.

The discussion was brief and pragmatic. Patrick Steinhardt initially questioned whether `[synopsis]` was the right markup for a non-synopsis code block, suggesting literal quotes instead. Kristoffer revised the series to split the changes—first fixing the placeholder presentation, then adding the synopsis block—resolving the debate. Both agree the approach is clear and appropriate, and the thread is effectively closed with no objections remaining. The changes are purely cosmetic, with no impact on behavior or on-disk formats, and the series is ready for merging.