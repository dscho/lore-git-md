This is a **substantive reply** in the ongoing review of Patrick Steinhardt’s ODB abstraction series, specifically addressing Justin Tobler’s questions about the second patch’s loose-object-map loading refactor. Toon Claes clarifies a key design point that was left ambiguous in the original patch and Justin’s review.

Toon explains that the `should_use_loose_object_map()` guard was moved from `repo_read_loose_object_map()` into `loose_object_map_load()` to accommodate a new call site: `odb_source_loose_new()`. This ensures the guard is enforced even when the loose backend is initialized directly (e.g., during ODB source creation), not just during repository setup. The change is mechanical but important for consistency—without it, the guard would be bypassed in the new call path.

The reply does not resolve the broader architectural questions Justin raised (multi-backend consistency, downcast safety, or the potential for redundant checks), but it removes one source of confusion about the patch’s intent. For a future AI summarizer, the key detail is the new call site (`odb_source_loose_new()`) and its role in triggering lazy loading. This narrows the scope of Justin’s original question: the guard is not expected to vary per backend, but it *is* needed in more places than before. The patch’s goal—decoupling map loading from repository setup—remains uncontested, but the clarification highlights that the refactor is more than just a relocation of logic.