This is a **bug report** with a clear discrepancy between documented and actual behavior in `git config --global`. The author, Nils Fahldieck, identifies a long-standing inconsistency: when both `~/.gitconfig` and the XDG config file (`~/.config/git/config`) exist, the documentation claims `--global` reads from both, but the code only reads from one (prioritizing `~/.gitconfig` if readable, even if empty). The report includes a minimal reproducer, code references, and two proposed fixes (preferring Option A: align code with docs by reading both files).

The issue is well-scoped and affects a core Git feature (configuration handling). The analysis is thorough, showing how `git_global_config()`’s single-file logic contradicts `do_git_config_sequence()`’s multi-file behavior. The asymmetry is particularly jarring because `--global` and default `--get` behave differently when both files coexist, which could confuse users. The author’s preference for Option A (fixing the code) is reasonable, as it preserves backward compatibility while matching user expectations.

This looks like a genuine bug with a straightforward technical solution. The report’s clarity and completeness suggest it’s worth attention, especially since it touches a user-facing command with potential for silent misconfigurations. No prior discussion in the thread context is noted, so this appears to be the first public report.