Thread identity: Documentation-only patch **series** (2 patches, v3→v4) to `Documentation/git-format-rev.adoc` revising the `<subject>` placeholder styling in a code example, part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila. Author: Kristoffer Haugsbakk. Thread type: patch series (v4, now posted).

Core problem or goal: Apply consistent and readable AsciiDoc markup to the `<subject>` placeholder in a `git format-rev` code example, aligning with the project-wide documentation synopsis-style conversion. The series resolves earlier debate about markup alternatives by splitting changes into two pragmatic commits: (1) replace `<subject>` with literal quotes (`"<subject>"`) for clarity and consistency with the implied input format (a commit subject enclosed in quotes), and (2) add a `[synopsis]` block to the code example to properly highlight placeholders. The procedural blocker (AsciiDoc/AsciiDoctor rendering inconsistency) was resolved in v4 by harmonizing the block syntax with other synopsis-style documentation (replacing the code block `----` with an open block `--`).

Current status: **Queued in `next` for integration testing**. Junio C Hamano confirmed in the latest email that the procedural blocker (rendering inconsistency) has been resolved by the v4 block syntax change and intends to advance the series to `next`. The series is procedurally unblocked, has no outstanding technical objections, and awaits re-acknowledgment only as a formality. Integration status is now "cooking in `next`"; graduation to `master` will follow if no integration issues arise.

Key technical details: Files touched: `Documentation/git-format-rev.adoc`. New/renamed symbols: None. On-disk format changes: None. Test coverage: Not applicable (documentation-only). The changes target the `<subject>` placeholder in a code example demonstrating `--stdin-mode=text` output formatting:
- **Patch 1**: Replaces `<subject>` with `"<subject>"` and `%s` with `"%s"` in both explanatory text and example output, aligning with the implied input format (a commit subject enclosed in quotes) and improving readability.
- **Patch 2**: Adds a `[synopsis]` block to the same code example to properly highlight placeholders, using an open block (`--`) instead of a code block (`----`) to resolve the rendering inconsistency. The commit message cites precedent (commit a34d1d53, doc: convert git-show to synopsis style, 2026-02-06) and clarifies the block syntax change.

Open questions and loose ends: None. The procedural blocker (rendering inconsistency) has been resolved in v4, and Junio has signaled readiness to queue the series.

Participant positions:
- **Kristoffer Haugsbakk** (author): Diagnosed and resolved the procedural blocker (block syntax mismatch) in v4 by harmonizing the block syntax with precedent. His feedback throughout was substantive (engaging with markup semantics, readability, commit granularity, and build-system compatibility).
- **Patrick Steinhardt** (reviewer): Initially questioned the use of `[synopsis]` for a non-synopsis code block and suggested literal quotes as a clearer alternative. His feedback was substantive (engaging with readability and markup semantics) and aligned with Kristoffer’s revised approach. His `Acked-by` on v3 is invalidated by the block syntax change in v4; re-acknowledgment is pending but not required for integration.
- **Junio C Hamano** (maintainer): Raised the procedural question about AsciiDoc/AsciiDoctor rendering consistency, which prompted the diagnosis of the root cause (block syntax mismatch). Confirmed the fix in v4 and signaled intention to queue the series in `next`.

Related work: Part of the broader documentation synopsis-style conversion effort. The outcome reinforces the precedent for flexibility in applying markup rules, particularly in non-synopsis contexts. References prior work (commit a34d1d53) in commit messages. No dependencies or conflicts with other in-flight topics.