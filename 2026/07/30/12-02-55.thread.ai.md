Thread identity: Documentation-only patch to `git-format-rev.adoc` adding a `[synopsis]` block for consistent AsciiDoc styling, part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila. Author: Kristoffer Haugsbakk. Thread type: standalone patch (not a series).

Core problem or goal: Apply uniform AsciiDoc markup to the `<subject>` placeholder in a `git format-rev` code example to ensure consistent rendering in man pages, aligning with the project-wide documentation synopsis-style conversion. The patch’s necessity and markup choice are now contested: Patrick Steinhardt questions whether `[synopsis]` is the correct markup for a non-synopsis code example, suggesting backticks as a simpler alternative, while Kristoffer Haugsbakk proposes rewriting the example to use literal quotes (`"%s"` and `"<subject>"`) to avoid special markup entirely.

Current status: Under review. This is a resend of the original patch (no changes to the patch itself). The discussion has shifted from a simple markup choice (`[synopsis]` vs. backticks) to a broader consideration of whether the example’s content should be adjusted to eliminate the need for special markup. No behavioral changes; purely cosmetic. The open question is whether to prioritize strict consistency with the conversion effort, pragmatic simplicity, or content clarity.

Key technical details: Files touched: `Documentation/git-format-rev.adoc`. New/renamed symbols: None. On-disk format changes: None. Test coverage: Not applicable (documentation-only). The change adds a `[synopsis]` block around the `<subject>` placeholder in a code example demonstrating `--stdin-mode=text` output formatting. Alternatives discussed:
- Backticks (Patrick’s suggestion): simpler but deviates from the conversion effort’s pattern.
- Literal quotes (Kristoffer’s suggestion): avoids markup entirely but requires a minor content change.
The outcome may set a precedent for handling non-synopsis content in the conversion effort.

Open questions and loose ends:
1. Whether to use `[synopsis]` (strict consistency), backticks (simpler), or adjust the example text (content clarity) for the `<subject>` placeholder.
2. Whether the discussion will prompt a broader clarification of AsciiDoc best practices for Git’s documentation, particularly for non-synopsis content.
3. No objections to the patch’s intent exist, but the markup/content choice remains unresolved.

Participant positions:
- **Kristoffer Haugsbakk** (author): Initially proposed `[synopsis]` to align with the conversion effort (commit `a34d1d53`). Now open to alternatives, including rewriting the example to use literal quotes (`"%s"` and `"<subject>"`) to avoid special markup. His feedback is substantive (engages with markup semantics and content clarity) and pragmatic.
- **Patrick Steinhardt** (reviewer): Questions the use of `[synopsis]` for a non-synopsis code block, suggesting backticks as a simpler alternative. His feedback is substantive (engages with markup semantics) but cautious, reflecting limited AsciiDoc familiarity. No objection to the patch’s goal, only the implementation detail.

Related work: Part of the broader documentation synopsis-style conversion effort (see commit `a34d1d53`). No dependencies or conflicts with other in-flight topics. The outcome may influence future patches in the conversion effort, particularly for non-synopsis content.