Thread identity: Documentation-only patch to `git-format-rev.adoc` adding a `[synopsis]` block for consistent AsciiDoc styling, part of the ongoing man page synopsis-style conversion effort led by Jean-Noël Avila. Author: Kristoffer Haugsbakk. Thread type: standalone patch (not a series).

Core problem or goal: Apply uniform AsciiDoc markup to the `<subject>` placeholder in a `git format-rev` example to ensure consistent rendering in man pages, aligning with the project-wide documentation synopsis-style conversion. The patch’s necessity is now contested: Patrick Steinhardt questions whether `[synopsis]` is the correct markup for a non-synopsis code example, suggesting backticks as a simpler alternative.

Current status: Under review. This is a resend of the original patch (no changes). The patch remains a single-line addition to `Documentation/git-format-rev.adoc`, following the pattern established in commit `a34d1d53` (doc: convert git-show to synopsis style, 2026-02-06). No behavioral changes; purely cosmetic. The open question is whether the established pattern should be strictly followed or pragmatically adjusted for this case.

Key technical details: Files touched: `Documentation/git-format-rev.adoc`. New/renamed symbols: None. On-disk format changes: None. Test coverage: Not applicable (documentation-only). The change adds a `[synopsis]` block around the `<subject>` placeholder in a code example demonstrating `--stdin-mode=text` output formatting. Patrick’s alternative suggestion (backticks) would replace the `[synopsis]` block with inline backtick markup, reducing visual clutter but deviating from the conversion effort’s rigid pattern.

Open questions and loose ends: Whether to use `[synopsis]` (strict consistency) or backticks (simpler, more semantically appropriate) for the `<subject>` placeholder in this context. The discussion may clarify the project’s tolerance for pragmatic deviations from the synopsis-style conversion’s established pattern. No objections to the patch’s intent exist, but the markup choice is now unresolved.

Participant positions:
- **Kristoffer Haugsbakk** (author): Proposes `[synopsis]` to align with the ongoing conversion effort (commit `a34d1d53`).
- **Patrick Steinhardt** (reviewer): Questions the use of `[synopsis]` for a non-synopsis code block, suggesting backticks as a simpler alternative. His feedback is substantive (engages with markup semantics) but cautious, reflecting limited AsciiDoc familiarity. No objection to the patch’s goal, only the implementation detail.

Related work: Part of the broader documentation synopsis-style conversion effort (see commit `a34d1d53`). No dependencies or conflicts with other in-flight topics. The outcome may influence future patches in the conversion effort, particularly for non-synopsis content.