This is a **build system** follow-up in the `WITH_BREAKING_CHANGES=1` test-failure thread. Jeff King (Peff) replies to brian m. carlson’s patch that moved the inclusion of `config.mak` earlier in the Makefile to ensure the flag is respected during build target generation.

Peff confirms the underlying issue—`WITH_BREAKING_CHANGES` is not being honored correctly when set in `config.mak`—but raises a serious concern about the proposed fix: moving the `include` directives too early breaks the Makefile’s last-one-wins behavior for variables like `CFLAGS`, which are set up later. He demonstrates this with a concrete example (`CFLAGS = --break-the-build`), showing that the patch would silently ignore such settings, potentially masking build misconfigurations.

Instead, Peff suggests a more targeted solution: extract the `git-whatchanged` line from the main `BUILT_INS` setup and handle it conditionally later in the Makefile, following the existing pattern for other conditional binaries like `http-fetch`. This approach avoids disrupting the broader Makefile logic while still ensuring `WITH_BREAKING_CHANGES` is respected for the specific binaries it affects.

The email is a **substantive review** that identifies a real flaw in the patch and proposes a safer alternative. It does not reject the patch outright but clarifies the trade-offs and offers a more robust path forward. The discussion remains focused on the build system’s internal consistency, with no user-visible behavior changes or new symbols introduced. The thread is still open; brian’s patch will likely need revision to address Peff’s feedback.