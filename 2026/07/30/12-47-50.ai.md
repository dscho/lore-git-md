This is a **substantive review reply** to the first patch in Patrick Steinhardt’s ODB abstraction series. Toon Claes endorses Justin Tobler’s earlier observation that the patch fixes a real-world bug—repositories using a compatibility hash algorithm (e.g., SHA-256 with SHA-1 compatibility) and alternates are currently broken because the loose-object map loading logic ignores the source-specific path. Toon suggests the commit message should more clearly highlight the severity of the issue and proposes a concrete test case to verify the fix. The test creates two SHA-256 repositories (one acting as an alternate), configures both with SHA-1 compatibility, and confirms that `git rev-parse` correctly resolves an object borrowed from the alternate when using the compatibility hash format. This feedback elevates the patch’s importance: what initially appeared as a mechanical cleanup is now framed as a latent correctness bug affecting multi-backend setups with non-default hash algorithms. The review depth is substantive—Toon provides both a clear reproduction scenario and executable test code, making the issue immediately actionable for the author.