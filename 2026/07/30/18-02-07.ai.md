This is the **final acknowledgment** in Shlok Kulshreshtha’s two-patch test-infrastructure series. The email confirms Junio Hamano’s proposal to mark the series for the `next` integration branch, signaling readiness for broader integration testing. The patch itself converts 12 test scripts (`t3404`, `t3405`, `t3408`, `t3434`, `t3900`, `t4150`, `t7500`, `t7501`, `t7502`, `t7600`, `t7604`, `t7614`) to use the new `commit_body()` helper introduced in patch 1/2, replacing the exit-code-masking idiom `git cat-file commit <rev> | sed -e '1,/^$/d'` with a safer, reusable shell function. The conversion is mechanical in 63 of 66 hunks, with only three requiring special handling: `t7509` (removal of a local helper), `t7600` (consolidating a grep-and-strip sequence), and `t3900` (avoiding a new exit-code hole when piping into `iconv`). The series is now cooking in `next`; no further changes are expected.