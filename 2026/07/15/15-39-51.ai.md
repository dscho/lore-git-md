This is a **refactoring patch** in the `remote-curl` transport code, continuing the discussion from René Scharfe’s original patch to simplify push specification handling. The thread now focuses on a design-level question: whether to pass a `const struct strvec *` directly to the protocol handlers (`push_dav` and `push_git`) instead of a raw `const char **`.

René acknowledges Patrick Steinhardt’s suggestion that passing a `strvec` would make the interface safer and more self-documenting, as it explicitly guarantees NULL-termination. However, he notes that the current simplicity of `strvec_pushv()` (which operates on NULL-terminated arrays) influenced his original choice. The conversation also touches on a hypothetical `strvec_pushvec()` function, which could optimize bulk appends by avoiding repeated reallocations—though René doubts the performance impact would be measurable in practice.

The patch itself remains a low-risk refactoring with no behavior change, but the discussion introduces a trade-off between interface clarity and simplicity. René’s observation that `strvec_pushvec()` could subtly encourage safer usage (by making length-limited vectors more appealing than unterminated arrays) adds a forward-looking dimension to the debate. No test failures or regressions are reported, and the change is confined to `remote-curl.c`.

This is not a blocker for the current patch, but Patrick’s feedback may prompt a v2 if René opts to adopt the `strvec *` interface. The thread is now centered on design preferences rather than correctness, with no clear consensus yet on the best path forward.