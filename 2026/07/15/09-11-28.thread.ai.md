Thread identity
Subject: [PATCH 0/2] gitweb: fix object ID rendering in commitdiff index lines
Series version: v2 (v1 previously under review)
Originating author: Travor Liu
Thread type: bugfix patch series

Core problem or goal
Gitweb’s commitdiff view incorrectly renders index lines (e.g., `index <old>..<new> 100644`) by showing full, unlinked object IDs instead of the usual shortened, clickable hashes. The bug occurs because the regex that matches object IDs runs before the mode-handling code, causing the mode string to be treated as part of the ID range.

Current status
v2 submitted, incorporating all maintainer feedback. Patch 1 combines the implementation and test, and uses `test_grep`/`test_grep !` instead of raw `grep` to align with an in-flight test-script hygiene topic. The series is now procedurally ready for integration; no substantive objections or open questions remain.

Key technical details
Files touched: `gitweb/gitweb.perl` (function `format_extended_diff_header_line`), `t/t9502-gitweb-standalone-parse-output.sh`
New/renamed symbols: none
Behavior change: index lines in commitdiff view now show shortened, clickable object IDs instead of full, unlinked hashes
On-disk format: unchanged
Test coverage: regression test in `t9502` verifies the `100644` mode case by checking for the presence of linked short blob IDs and the absence of the unlinked full form

Open questions and loose ends
None; all feedback addressed.

Participant positions
Travor Liu (author): submitted v1 with clear commit messages and minimal but sufficient test coverage; explained the root cause (regex order vs. mode handling) and framed the fix as a continuation of prior mobile-overflow work. Confirmed and implemented Junio’s requested changes (test squash and `test_grep` conversion) in v2.
Junio C Hamano (maintainer): provided surface-level review focusing on test mechanics rather than the substance of the fix; requested squashing the test into the first patch and using `test_grep`/`test_grep !` to align with an in-flight test-script hygiene topic. No further feedback after v2.