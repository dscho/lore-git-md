This is a minor follow-up to the GSoC v18 series implementing `git cat-file --batch-command` support for remote object metadata queries. The series is already queued for `next` and functionally complete, with all prior design and security concerns resolved.

Junio points out a cosmetic type inconsistency in the `parse_object_size()` helper: it returns `-1` (signed) from a function declared to return `size_t` (unsigned). The caller only checks for success/failure, so the fix is straightforward—change the return type to `int`. Pablo acknowledges the oversight and will make the change.

This is purely a style nit; the function works correctly as-is. The patch remains ready for integration, and the series as a whole is on track for `next`. No further discussion or review is needed.