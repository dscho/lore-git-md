This is a brief follow-up from Patrick Steinhardt in the July 2026 "What's cooking" thread. He confirms two topics are ready for `next`:

1. **`ps/odb-for-each-object-filter`** – the 10-patch series that teaches `odb_for_each_object()` to accept object filters and updates `git cat-file --batch-all-objects` to use the new interface. Patrick sent a tiny reroll to fix function-name references in commit messages, but otherwise considers the series complete.

2. **`cl/conditional-config-on-worktree-path`** – the 2-patch series that adds `worktree` and `worktree/i` conditions to `includeIf` in Git’s config system. Patrick endorses it for `next` without further changes.

Both topics are now effectively Ack’d by their primary stakeholder and can be expected to graduate in the next integration cycle.