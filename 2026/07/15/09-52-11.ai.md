This is a test infrastructure refactoring patch, part of the `remote-object-info` series (v18, patch 3/10). It extracts three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is purely mechanical: the functions are moved verbatim with no behavior modification, and the original file now sources the new library.

The motivation is code reuse: later patches in the series (notably the new `t/t1017-cat-file-remote-object-info.sh` test script) will need these utilities, and extracting them avoids duplication. The patch itself is small (17 insertions, 12 deletions) and uncontroversial, reflecting standard Git test suite hygiene practices. Authorship is attributed to Eric Ju, with Pablo Sabater carrying it forward in the final series. No review feedback is visible in this email, but the change aligns with the series' broader goal of consolidating test infrastructure for the new `remote-object-info` command.