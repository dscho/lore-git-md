Here’s the human-readable summary of the Git mailing list thread based on Junio’s "What's cooking" report for July 2026:

This thread is Junio’s regular integration report, giving a snapshot of the current state of Git’s development branches as of mid-2026. The focus this cycle is on memory management, pluggable object database (ODB) backends, and refactoring the ref subsystem. Key participants include Patrick Steinhardt (driving ODB abstraction and refs work), Jeff King (memory leak fixes), and Taylor Blau (pack-objects and repacking optimizations).

Five topics have graduated to `master`, including fixes for GPG/SSH signature parsing, reftable leak fixes, and a performance improvement for priority queues. Another 25 topics are cooking in `next`, covering major efforts like ODB pluggability (allowing backends to optimize object traversals), de-globalizing the refs API, and new CLI features such as `git branch --delete-merged` and `git history squash`. Performance optimizations—like avoiding quadratic behavior in reftable tombstone handling and unpack-trees—are also in progress. The repository setup code has been modernized to split discovery and configuration into distinct phases, and reftable has been hardened against corruption.

Stalled topics include a documentation rewrite for `git interpret-trailers` and external hunk provider support for diffs, both awaiting updates or review. Platform-specific work continues, with macOS and Windows fixes landing, though Rustification remains a concern for NonStop. The test suite is also seeing improvements, with new linters and safer CGI helpers.

Open questions revolve around a few potentially contentious features, such as server-side suppression of REF_DELTA in `git send-pack` and a protocol v2 extension for fetching remote object metadata. The bulk branch deletion feature (`git branch --delete-merged`) may also face UX scrutiny. Overall, the report reflects steady progress on long-term architectural goals while addressing immediate correctness and performance needs.