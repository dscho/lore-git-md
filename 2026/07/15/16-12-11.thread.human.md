Here’s the updated human-mode summary for a developer catching up on the thread:

---

This thread is about hardening Git’s trace2 telemetry subsystem to prevent crashes caused by memory allocation or system call failures. The original issue—a "recursion detected in die handler" error on Git for Windows—was traced to `xsnprintf()` failures in trace2 timestamp formatting, but the fix has grown into a broader effort to eliminate all `die()`-triggering helpers (e.g., `xstrdup()`, `ALLOC_ARRAY()`, `xcalloc()`) from trace2. Derrick Stolee’s seven-patch series (v3) replaces these functions with defensive fallbacks, such as hardcoded timestamps or `NULL` returns, to ensure Git remains stable even when telemetry fails. The series also introduces `banned-die.h`, a compile-time enforcement mechanism to block banned functions in trace2 and potentially other subsystems.

The key debate centers on whether this incremental approach is sufficient or if trace2 needs a ground-up rewrite to fully eliminate unsafe dependencies. Jeff King argues that only a rewrite—stripping trace2 of helpers like `strbuf` and `json-writer.c`—can guarantee resilience, as the current series doesn’t address indirect `die()` calls. Derrick acknowledges the limitations but defends the series as a pragmatic first step, noting that a rewrite would be a major undertaking. Junio Hamano and others have endorsed the series as-is, with Junio suggesting `banned-die.h` could be reused for other architectural constraints (e.g., banning `the_repository` in specific subsystems).

The series is now complete and addresses all prior feedback, including Elijah Newren’s concerns about correctness (e.g., off-by-one errors in redaction logic) and Jeff’s architectural critique. Tests have been updated to verify the `:<REDACTED>` string format, and the patches are rebased onto `master`. The MinGW-specific fix (disabling `libintl_vsnprintf()` redirection) remains a planned follow-up. Open questions include whether to land the series now or wait for a rewrite, how to test failure cases, and the long-term impact on trace consumers (e.g., monitoring tools) when telemetry degrades under failure conditions.

Participants are aligned on the goal—trace2 should never crash Git—but differ on the solution. Derrick and Junio favor incremental progress, while Jeff prefers a rewrite. No objections to the current series have been raised, and the discussion remains collaborative.

---