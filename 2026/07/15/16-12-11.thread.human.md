Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is about a seven-part patch series from Derrick Stolee to harden Git’s trace2 subsystem against crashes caused by `die()`-triggering helpers like `xsnprintf()` or `xstrdup()` under memory pressure or system call failures. The original crash—a “recursion detected in die handler” error—was traced to `libintl_vsnprintf()` allocations on Git for Windows, but the series now aims to eliminate *all* `die()`-prone functions from trace2, replacing them with defensive fallbacks that prioritize process stability over telemetry completeness. The series introduces `banned-die.h` as a compile-time enforcement mechanism to block these functions, though its broader applicability is still under discussion.

The current version (v2) is under review, with most feedback focused on architectural concerns. Junio C Hamano has been supportive of the approach, clarifying the intent behind `banned-die.h` and confirming a one-byte off-by-one error in the redaction logic (fixed by copying the full `:<REDACTED>` placeholder). Elijah Newren’s reviews raised several technical issues: a naming inconsistency in the ban macro, an include-ordering risk in `banned-die.h`, incomplete hardening in the config parsing path (where `xstrfmt()` still triggers `die()`), and a splitting error where the sixth patch introduces a regression resolved only by the seventh. The most significant critique comes from Jeff King (Peff), who argues that the compile-time ban mechanism is fundamentally limited—it only catches *direct* calls to banned functions, not transitive ones (e.g., `strbuf` or `json-writer.c` helpers that indirectly invoke `die()`). He suggests the series may need a ground-up rewrite of trace2 to eliminate all `die()`-prone dependencies, though he hasn’t proposed a concrete alternative.

Key open questions include whether to expand the ban list (e.g., to include `strbuf`), pursue a more radical rewrite, or accept the series as a pragmatic first step despite its limitations. The MinGW-specific fix (disabling `libintl_vsnprintf()` redirection) remains a planned follow-up, and the series’ impact on trace consumers—who may see degraded telemetry under failure conditions—hasn’t been deeply explored. For now, the focus is on addressing the technical feedback before the series can graduate.