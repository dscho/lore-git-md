Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is about a bugfix and hardening series for Git’s trace2 subsystem, aimed at preventing crashes caused by `die()`-triggering helpers like `xsnprintf()` or `xstrdup()` under memory pressure. The original issue—a "recursion detected in die handler" error on Git for Windows—was traced to `xsnprintf()` failures in trace2’s timestamp formatting, but the series now takes a broader approach: eliminating *all* `die()`-prone functions from trace2 to ensure telemetry operations never crash Git, even when allocations or system calls fail. Derrick Stolee is the author, and the series has evolved from a single patch to a seven-part v2 that introduces `banned-die.h`, a new header to enforce compile-time bans on dangerous functions, and replaces banned helpers with defensive fallbacks (e.g., hardcoded timestamps, `NULL` returns, or static fallback contexts).

The series is under active review, with most feedback now resolved. Junio Hamano, the maintainer, has endorsed the approach and clarified technical details, like fixing an off-by-one error in the redaction logic (where `:<REDACTED>` was missing its trailing `>`). Elijah Newren’s reviews surfaced several correctness issues—naming inconsistencies, include-ordering risks, and a regression in the region stack handling—but these have been addressed in follow-ups. The most significant open question comes from Jeff King, who argues the hardening is incomplete: while the series blocks direct calls to banned functions, it doesn’t address indirect `die()` triggers via helpers like `strbuf` or `json-writer.c`. Peff suggests a ground-up rewrite of trace2 might be needed, but Derrick defends the current approach as a pragmatic first step that provides *some* protection and can be iterated on.

Key changes in v2 include replacing `xsnprintf()` with `snprintf()` and fallbacks, using `strdup()` instead of `xstrdup()` in config parsing (with graceful degradation on failure), and introducing a static fallback context for thread-local storage when allocations fail. The series is self-contained to trace2 but positions `banned-die.h` as a reusable mechanism for future hardening efforts. A MinGW-specific follow-up (from Taylor Blau) to disable `libintl_vsnprintf()` redirection is still planned but separate. The main loose ends are the lack of test coverage for transient failures and the broader debate over whether compile-time bans are sufficient or if a deeper rewrite is needed. For now, the series is on track for v3, with the off-by-one fix and other corrections incorporated.