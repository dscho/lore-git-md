This is a **bugfix acknowledgment and proposed fix** targeting the revision-walking subsystem. Kristofer Karlsson, the author of the problematic commit `dd4bc01c0a8f`, confirms the regression in `git rev-list --no-walk` path filtering and takes responsibility for the oversight. He reproduces the issue with a minimal test case (two commits touching distinct files) and demonstrates that the command incorrectly outputs commits even when the path filter should exclude them.

The email proposes a targeted fix: restoring the `try_to_simplify_commit()` call (which internally invokes `process_parents()`) in the `REV_WALK_NO_WALK` case of `get_revision_1()`. This aligns with the original behavior and directly addresses the root cause identified in the thread. Kristofer also raises procedural questions about whether to revert the entire patch or apply a bugfix, and whether additional test cases are needed for other code paths. His tone suggests he intends to submit a formal patch soon, likely including tests to prevent future regressions.

This reply advances the thread significantly by providing a clear path forward. The proposed fix is narrowly scoped to the regression and avoids broader changes, which should reduce controversy. The inclusion of a reproduction case and a concrete code snippet makes it easy for reviewers to verify the issue and the proposed solution. Expect follow-up discussion about test coverage and whether the fix should be backported to maintenance branches, but the technical direction appears settled.