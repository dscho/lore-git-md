Thread identity: Refactoring of `get_commit_action()` in `revision.c` to eliminate side effects, making it a pure predicate for revision-walking machinery. Series version: standalone patch (no version trailer). Originating author: Michael Montalbo.

Core problem or goal: Remove a long-standing impurity in `get_commit_action()` where the function, while nominally a predicate deciding whether a commit should be shown or ignored, also mutates tracked line ranges when used with line-level log (`git log -L`) without parent rewriting. This side effect makes the function unsafe for out-of-order evaluation (e.g., lookahead scenarios). The goal is to refactor the code so that `get_commit_action()` becomes a pure predicate, moving the mutation logic to `simplify_commit()` where it runs before the predicate check. The patch’s purpose was questioned by Junio C Hamano, who sought clarification on whether this is a code cleanup, bugfix, or new feature, noting the change’s size (over 100 lines) and expressing uncertainty about its motivation.

Current status: Under review, with Junio’s question introducing an open question about the patch’s justification. The patch remains self-contained and addresses the impurity comprehensively, but its future depends on the author’s response to Junio’s concerns.

Key technical details:
- Files touched: `revision.c`, `test-revision-walking.c`, `t4211-line-log.sh`.
- New symbols: `commit_early_ignore()` (helper function), `line-log-peek` (test subcommand).
- Subsystems: revision-walking machinery, line-level log (`git log -L`).
- Behavior: No user-visible changes; the refactoring is internal and makes the code safer for future extensions (e.g., lookahead optimizations).
- Test coverage: New test verifies that `get_commit_action()` does not mutate commit flags when called out-of-order.

Open questions and loose ends:
- Junio’s question about the patch’s purpose and scope remains unresolved. The author must clarify whether this is a cleanup, bugfix, or feature, and justify the change’s size and necessity.

Participant positions:
- **Junio C Hamano**: Substantive review questioning the patch’s motivation and scope. Expressed confusion about whether this is a cleanup, bugfix, or new feature, and noted the change’s size (over 100 lines) as unexpected. No technical objections yet, but the patch’s future depends on the author’s response.

Related work: Part of a broader effort to clean up revision-walking internals, though this patch stands alone. No dependencies or conflicts with other in-flight topics.