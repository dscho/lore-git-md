This is a **contributor housekeeping** discussion in the `git-subtree` safeguard thread, specifically a tangential but substantive exchange about documentation terminology between Colin Stagner and Ian Jackson. The email does not introduce new technical feedback or code changes for the pending patch series (v2 awaits only test style improvements), but it clarifies how the project’s documentation conventions might apply to the forthcoming Rust rewrite of `git-subtree`.

Colin argues that Git’s documentation falls into two categories: *command-level* (e.g., `git-merge(1)`), which focuses on mechanics and uses precise, file-position-centric terms like "ours/theirs" or "branch head," and *tutorial-level* (e.g., `gitworkflows(7)`), which discusses social relationships like "upstream/downstream." He suggests that the Rust rewrite’s documentation should align with command-level conventions, using terms like "(sub)tree" or "merge-base" rather than "upstream/downstream," which he sees as too broad and socially loaded for a tool that may be used to merge unrelated repositories or split history for external build systems (e.g., AUR packages). His examples—monorepo aggregation of unrelated projects, intra-repo subtree merges, and AUR’s `subtree split` workflows—highlight edge cases where the dependency relationship Ian describes (A depends on B, so B is upstream) may not hold.

Ian’s prior use of "upstream/downstream" was tied to the safeguard’s design: the Rust rewrite stores its metadata in the top-level repository config (`subtree.rust-rewrite`) because the decision to adopt the new implementation belongs to the downstream project (the one consuming the subtree). Colin’s feedback does not challenge this design, but it does question whether the terminology will confuse users who use `git-subtree` in non-dependency workflows. The exchange ends with Colin offering recommendations (focus on mechanics in command docs, reserve "upstream/downstream" for workflow tutorials) but acknowledging they are not blocking for the current series.

For the AI summarizer brief: this email adds no new technical details about the patch itself, but it surfaces a potential documentation pitfall for the Rust rewrite. The key terms to track are **"upstream/downstream"** (social relationship), **"(sub)tree"** (file-position-centric), and the distinction between *command-level* and *tutorial-level* documentation. The thread remains procedurally ready for v2, with no new objections or review points.