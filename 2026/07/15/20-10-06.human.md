This is a small, targeted bugfix from Junio C Hamano addressing a coding style violation in the submodule helper code. The patch removes a `%zu` format specifier that was introduced in Git 2.54 (January 2026) and replaces it with the project’s established workaround: casting the value to `uintmax_t` and using `PRIuMAX`. The commit message explains that while `%zu` is C99-compliant, Git’s coding guidelines still forbid it to avoid breaking builds on older toolchains, and this instance slipped through despite the documented policy.

The change is confined to a single line in `builtin/submodule--helper.c` and is purely mechanical—no behavior is altered. The motivation is clear: maintain compatibility with distributions that may still be using pre-C99 compilers or libraries, even if no actual breakage has been reported yet. Junio’s tone suggests this is a routine cleanup rather than an urgent fix, and the patch itself looks uncontroversial. If you care about strict adherence to Git’s coding conventions, this is worth a quick glance; otherwise, it’s safe to skip.