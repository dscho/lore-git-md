This is a **bugfix report** targeting the revision-walking subsystem. The email identifies a regression in Git 2.55.0 where `` `git rev-list --no-walk` `` no longer respects path filtering arguments (e.g., ``-- ':!drivers/gpu/drm/'``). The regression was introduced by commit `dd4bc01c0a8f` ("revision: use priority queue for non-limited streaming walks"), which refactored how commits are processed in non-walking modes but inadvertently skipped the `process_parents()` call that applies path filters.

The author, Peter Colberg, demonstrates the issue with a real-world example involving Linux kernel commits, showing that the second ``rev-list`` stage (with ``--no-walk``) now passes through all commits instead of filtering them. They also pinpoint the exact hunk in the patch that caused the breakage and confirm that reverting it restores the old behavior. The question of whether this change was intentional is left open, but the commit message and context suggest it was an oversight rather than a deliberate design choice.

The patch touches `revision.c`, specifically the `get_revision_1()` function, which handles commit processing logic. The regression affects workflows that rely on ``--no-walk`` for path-based filtering, such as bisecting or analyzing commits that modify specific paths. No test coverage or proposed fix is included in the email, but the reproduction steps are clear and minimal.

This is likely to attract attention from maintainers of the revision-walking code (e.g., Junio Hamano or contributors who worked on the original streaming-walk changes). The thread may evolve into a discussion about whether to revert the problematic hunk, adjust the path-filtering logic, or document the change as a breaking behavior. The key technical detail is the interaction between ``REV_WALK_NO_WALK`` and `process_parents()`, which now bypasses path filtering entirely.