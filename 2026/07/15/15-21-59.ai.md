This is patch 5/9 in Phillip Wood’s **approved nine-patch series** fixing how the sequencer handles dropped commits during rebase. The patch is a **refactoring** that simplifies control flow in `pick_one_commit()` when a fixup commit encounters conflicts.

The change removes a redundant conditional call to `intend_to_amend()` by leveraging the fact that, after earlier patches in the series, the only positive return value from `do_pick_commit()` is `1`. Instead of checking `res == 1` and calling `intend_to_amend()` directly, the patch modifies `error_failed_squash()` to pass a flag (`1`) to `error_with_patch()`, which then calls `intend_to_amend()` internally. The diff is minimal: one line changed in `error_failed_squash()` (adding a `1` flag) and three lines removed from `pick_one_commit()` (the conditional and its body).

This is a **cleanup patch** that does not alter behavior but makes the code slightly more concise. It is part of the series’ broader effort to prepare for the introduction of `enum pick_result` and the core behavioral change (skipping note copying for dropped commits) in patch 9/9. The patch is **well-motivated** by the series’ refactoring goals and carries no risk of regression. Oswald Buddenhagen’s earlier review focused on commit message phrasing ("call it error_with_patch() instead" → corrected to "call it in error_with_patch() instead"), which Phillip addressed in a follow-up reply. No substantive objections remain.

The patch touches only `sequencer.c` and is **ready for `next`** alongside the rest of the series. It is a small but necessary step in the incremental refactoring that enables the core fix.