This is a review reply in the final stretch of the GSoC v18 `remote-object-info` series, now queued for `next`. Junio and Pablo are clarifying the last behavioral edge cases before the series graduates.

The exchange focuses on two subtle points. First, Junio asks for clearer documentation of the distinction between *unknown* placeholders (which trigger a fatal error) and *known but unsupported* ones (which are silently omitted). Pablo confirms that unsupported placeholders like `%(objectcolor)` produce an empty string in the output (`42 `), not the literal placeholder, and agrees to add a concrete example to the commit message. Second, they discuss the handling of `%(objectname)`-only requests: Pablo proposes an early return to avoid unnecessary connections when no metadata is requested, while Junio suggests instead querying the server for *some* cheap metadata (e.g., size) to validate the object’s existence. This is a design tension between efficiency and robustness; no resolution is reached in this email, but the discussion makes the trade-off explicit.

The review is substantive but narrow, addressing only the final usability and documentation polish. The series itself is technically complete, with all prior feedback incorporated and CI passing. This reply is unlikely to derail graduation, but the `%(objectname)` question may require a follow-up patch if Junio’s preference for existence checks prevails. The dynamic allow-list and placeholder handling are now well-documented, closing the last open clarity issues.