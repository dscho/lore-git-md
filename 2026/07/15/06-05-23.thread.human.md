This thread began with Jeff King (Peff) fixing a long-standing segfault in `git diff --relative --cached` when used on unmerged paths outside the current directory. The bug, which dates back to 2011, was caused by a NULL pointer dereference in `do_oneway_diff()` and was resolved with a minimal NULL check. Junio C Hamano reviewed and accepted the fix, which is now under review and likely to graduate to `next` soon.

Peff followed up with a two-patch refactoring series to clean up the same code path. The first patch, removing a stale comment, was uncontroversial. The second patch, just posted, moves the prefix-checking logic earlier in `oneway_diff()` to skip paths outside the `--relative` prefix before they enter the problematic code path. This eliminates redundant work and aligns with an earlier optimization from 2021, though Peff retained the original NULL check for clarity. The change is purely internal and doesn’t alter behavior. Junio hasn’t reviewed this patch yet, but his earlier acceptance of the bugfix suggests he’ll be receptive to these cleanups. The series remains focused and well-scoped, with no open questions or objections so far.