This thread began with Jeff King (Peff) fixing a long-standing segfault in `git diff --relative --cached` when used on unmerged paths outside the current directory. That patch—a minimal NULL check in `diff-lib.c`—was reviewed and accepted by Junio C Hamano, who confirmed the fix and requested no changes. It is now under review and expected to graduate to `next` shortly.

Peff has now followed up with a two-patch cleanup series that tidies up the same area of `diff-lib.c`. The first patch removes a stale comment about `o->pos` advancement, and the second moves the prefix-checking logic earlier in `oneway_diff()` to avoid redundant work. Neither patch changes behavior; they are pure refactorings that make the original NULL-check fix technically redundant but are kept for clarity. Peff frames these as optional improvements rather than urgent fixes, and Junio has not yet responded. Given their small size (nine lines total) and tight focus on the diff plumbing, they are unlikely to spark controversy and may well be queued alongside the original fix.