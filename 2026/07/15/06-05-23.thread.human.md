This thread began with a fix for a long-standing segfault in `git diff --relative --cached` when used on unmerged paths outside the current directory. Jeff King (Peff) sent a single-patch fix, then followed up with a three-patch refactoring series that cleaned up the code and added an optional `BUG()` assertion to document an invariant. The entire four-patch set is approved and queued in Junio’s `next` branch, ready for integration.

The only remaining discussion is about the wording of the assertion message in the optional fourth patch. Peff originally proposed a neutral message (`"oneway_diff with neither idx nor tree"`) to document the invariant for human readers and static analyzers, while Junio suggested a humorous alternative (`"Hey, Coverity, this does not happen"`). Peff has since clarified that the assertion’s primary purpose is to serve as executable documentation of the invariant—ensuring `oneway_diff()` is never called with both `idx` and `tree` as NULL—rather than just silencing Coverity. The exchange remains lighthearted, with no technical objections, and the decision to include the patch (with either message or none at all) is purely stylistic. The series itself is uncontroversial and integration-ready.