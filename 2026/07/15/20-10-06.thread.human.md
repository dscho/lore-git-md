This thread is a small but illustrative example of Git's attention to coding style and platform compatibility. Junio C Hamano, the project maintainer, has posted a one-line fix to `builtin/submodule--helper.c` that replaces a C99 `%zu` format specifier with the project's long-standing workaround—casting to `(uintmax_t)` and using `PRIuMAX`. The violation was introduced six months ago in a patch that hashes submodule names for gitdir paths, but went unnoticed until now.

The fix is uncontroversial: it aligns with Git's CodingGuidelines, which explicitly forbid `%z`/`%zu` despite their technical availability, citing concerns about lagging platform support in downstream distributions. Junio briefly considered using this as a "weather balloon" to test whether `%zu` could now be safely adopted, but opted for caution, noting that failures in this code path might not be immediately visible. The change is purely stylistic and doesn't alter behavior, so no tests are included. The commit message clearly explains the rationale, making it easy for reviewers to follow the reasoning. At this point, the patch is ready for review, though given its trivial nature and alignment with existing conventions, it's likely to be merged without much discussion.