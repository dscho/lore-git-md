This is patch 7/9 in Phillip Wood’s approved 9-patch series fixing how the sequencer handles dropped commits during rebase. The patch is a **refactoring** of `pick_one_commit()` in `sequencer.c` that simplifies control flow without changing behavior.

The change eliminates redundant calls to `is_rebase_i(opts)` by returning early if the operation is not an interactive rebase. The rest of the function is restructured into a chain of `else if` blocks to make the mutually exclusive conditions clearer. The patch also moves the final `return res` into the `!res` block and adds a `BUG()` macro to catch any unhandled return values from `do_pick_commit()`, though this is purely defensive since the existing conditions are exhaustive.

This is a **cleanup patch** that prepares the codebase for the introduction of `enum pick_result` in the next patch (8/9). The diff is minimal (11 insertions, 8 deletions) and the commit message explains the rationale clearly. No substantive objections were raised; Oswald Buddenhagen’s earlier suggestion to squash this with patch 8/9 was rejected by Phillip to avoid unnecessary churn. The patch is part of a **well-tested, maintainer-approved series** that is ready for `next` after typofixes are squashed.