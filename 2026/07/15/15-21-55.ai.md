This is the first patch in Phillip Wood’s 9-patch v3 series fixing the sequencer’s handling of dropped commits during rebase. It is a **test maintenance update** targeting the `t3400-rebase.sh` test script.

The patch restores test coverage for note copying when using the "apply" backend by replacing the deprecated `-m` flag with `--apply`. Since the merge backend became the default, the existing test that uses the default backend now only covers the merge backend, leaving the apply backend untested. This change ensures both backends are verified for note-copying behavior, which is a prerequisite for the later patches that fix the core bug (notes being incorrectly copied from dropped commits).

The diff is minimal: two lines changed in `t/t3400-rebase.sh`, replacing `git rebase -m` with `git rebase --apply`. No behavior is altered; this is purely about maintaining test coverage. Oswald Buddenhagen’s earlier surface-level review suggested punctuation tweaks (commas) in the commit message, which Junio has queued as typofixes to be squashed later.

This patch is uncontroversial and part of a well-tested, maintainer-approved series. It sets the stage for the substantive fixes that follow by ensuring the test suite accurately reflects both rebase backends. No further changes are expected.