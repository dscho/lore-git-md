This is a lighthearted reply in the thread about Patrick Steinhardt’s ODB abstraction series, specifically the discussion around object filters in `odb_for_each_object()` (patch 6/7 in v2, now 8/9 in v3). Toon Claes acknowledges Patrick’s explanation for why backends are allowed to ignore filters—namely, that the only current caller (`git cat-file --batch-all-objects`) already handles manual filtering—and quips that lazy iterators (a feature in languages like Rust or Python) would solve the problem elegantly. The comment is purely conversational and adds no technical substance; it reflects a shared sentiment about the limitations of C’s iterator model but does not propose a change or critique the design.

The thread’s core discussion about filter handling is already resolved in favor of Patrick’s approach, and this reply does not reopen it. The series remains on track for integration, with no new objections or action items introduced.