This is a refactoring patch in Patrick Steinhardt’s nine-part series introducing object filters to `odb_for_each_object()`. It is patch 6/9 in v4, a cosmetic-only update from v3 that addresses trivial commit message typos in later patches but makes no technical changes here.

The patch drops the `_1` suffix from two static functions in `pack-bitmap.c`: `open_pack_bitmap_1()` and `open_midx_bitmap_1()`. These functions were left with the suffix after the preceding commit consolidated their logic into a new `open_bitmap_for_source()` helper. The rename is purely mechanical—no behavior changes, no new logic, just cleaning up the function names to match the current code structure. The diff shows seven one-line changes that replace the old names with the new ones throughout the file.