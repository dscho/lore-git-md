This is a **bugfix acknowledgment and proposed fix** from the original author of the problematic commit (`dd4bc01c0a8f`). Kristofer Karlsson confirms the regression Peter Colberg reported—`git rev-list --no-walk` now ignores path filtering—and reproduces it with a minimal test case. He accepts responsibility for the breakage and identifies the root cause: his refactoring inadvertently skipped the `process_parents()` call, which is necessary for path filtering to work.

Kristofer proposes a targeted fix (adding `try_to_simplify_commit(revs, commit)` in the `REV_WALK_NO_WALK` case) and plans to submit a patch with test coverage. He also raises the question of whether to revert the entire original patch or apply a smaller bugfix, though he leans toward the latter. The tone is constructive and collaborative, suggesting this is likely to be resolved quickly.

The regression affects the revision-walking subsystem in `revision.c`, specifically the `get_revision_1()` function. The fix will restore the old behavior where `--no-walk` respects path filters, which is critical for workflows like bisecting or analyzing commits by path. Since the original author is engaged and the issue is well-understood, this is a strong candidate for a straightforward bugfix in the next maintenance release. Watch for a follow-up patch with tests.