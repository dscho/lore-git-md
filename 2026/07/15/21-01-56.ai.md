This is the third patch in the nine-patch `diff.<driver>.process` RFC series (v5). It adds the plumbing for the new `diff.<driver>.process` configuration option by extending the `userdiff_driver` struct with `process` and `process_owned` fields and teaching `userdiff_config()` to populate them from Git configuration. The change is minimal and mechanical: seven lines in `userdiff.c` and two in `userdiff.h`, introducing no new behavior beyond storing the configured command string.

The patch is a straightforward refactoring prerequisite for the rest of the series, which implements the long-running subprocess protocol and integrates it with Git's diff and blame pipelines. The `process_owned` field follows the established pattern used by other string fields in the struct (e.g., `textconv_owned`), ensuring proper memory management. No test coverage is included here because the functionality is inert until later patches wire up the subprocess execution and protocol handling.

This is a well-scoped, uncontroversial change that directly enables the core infrastructure introduced in subsequent patches. The commit message is concise but sufficient for its role in the series. There is nothing here that would raise red flags or require deeper scrutiny.