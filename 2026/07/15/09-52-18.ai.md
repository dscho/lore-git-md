This is the core patch in the GSoC v18 series that implements client-side support for the `object-info` protocol v2 capability. It introduces the `fetch-object-info.c` and `fetch-object-info.h` files, adding 121 lines of new code that enable Git clients to query object metadata (currently only size) from remote repositories without downloading the full objects.

The patch adds a new `fetch_object_info()` function that sends an `object-info` command to a protocol v2 server, parses the response, and populates an `object_info` structure with the results. The implementation strictly validates server capabilities, enforcing protocol v2 and checking for the `object-info` capability before proceeding. It includes robust input validation: a 10,000-object batch limit, 8KB URL length limit, and strict parsing of numeric size values to prevent integer overflows or malformed responses.

Key technical details include the use of a dynamic allow-list for requested metadata fields (currently only "size" is supported), bounds-checked parsing of server responses, and careful memory management with `xcalloc` for size pointers. The code follows Git's established patterns for protocol handling, including stateless RPC support and consistent error handling. When servers do not recognize an object ID, the implementation correctly handles the protocol-mandated response format ("<OID> SP") by setting the corresponding size pointer to NULL.

The patch integrates with the transport system by adding a new `fetch_object_info` vtable entry and implementing it for both smart transports and transport helpers. This design allows the feature to work across all transport backends while maintaining the existing abstraction boundaries. The implementation is security-conscious, with explicit checks for malformed responses and strict validation of all inputs.

This is the final piece of the 18-patch series that has been cooking for months and is now queued for `next`. The patch demonstrates careful attention to edge cases and follows Git's established patterns for protocol extensions, making it a solid foundation for future expansion (such as adding object type support). The comprehensive test coverage (699 lines in `t/t1017-cat-file-remote-object-info.sh`) and successful CI runs suggest this is ready for integration.