Thread identity: bugfix patch series (single patch) by Jeff King (Peff) addressing a segfault in `git diff --relative --cached` when operating on unmerged paths outside the current directory.

Core problem or goal: fix a NULL pointer dereference in `do_oneway_diff()` when `diff_unmerge()` returns NULL for paths outside the `--relative` prefix, which occurs when using `--relative` and `--cached` together on unmerged paths. The regression dates back to 76399c0195 (2011), with a related fix for non-cached diffs in 8174627b3d (2021).

Current status: patch posted; **reviewed and accepted by Junio C Hamano**. The fix remains minimal (a NULL check before `fill_filespec()`) and includes a regression test in `t/t4045-diff-relative.sh`. Junio’s review confirms the fix is correct and endorses the minimal approach without requesting changes. No prior versions. **Integration status: under review, likely to graduate to `next` in the next "What's cooking" cycle.**

Key technical details:
- Files touched: `diff-lib.c` (fix), `t/t4045-diff-relative.sh` (test).
- Subsystem: diff plumbing.
- New behavior: `--relative --cached` now safely ignores unmerged paths outside the prefix instead of crashing.
- No on-disk format changes or new symbols.
- Test coverage: piggybacks on existing `--relative` tests, verifying the crash is resolved.

Open questions and loose ends:
- None. Junio’s review did not raise new concerns. Peff’s earlier comment about the outdated `o->pos` advancement may warrant future cleanup but is not blocking.

Participant positions:
- Jeff King (Peff): author; provided detailed analysis, regression history, and test case. Position: the fix is correct and minimal; no substantive objections expected.
- Junio C Hamano: maintainer; **reviewed and confirmed the fix is correct**. Position: **endorses the patch as-is**; no objections or changes requested.

Related work: none mentioned. The patch is standalone.