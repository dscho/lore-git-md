This is the final piece of the GSoC v18 `remote-object-info` series, now cooking in `next`. Pablo Sabater addresses Junio’s last two requests for clarity and robustness in the dynamic allow-list implementation.

The patch makes the placeholder handling explicit: known but unsupported format atoms (like `%(objectcolor)`) are omitted entirely, producing output like `42 ` instead of `42 %(objectcolor)`. This distinction—between *unknown* atoms (which trigger a fatal error) and *unsupported* ones (which are silently dropped)—will be documented in the updated commit message. The behavior aligns with `for-each-ref`’s existing rules.

On the `%(objectname)`-only case, Pablo implements Junio’s preference: when only the object name is requested, the client now forces a `size` query to the server to validate the OID’s existence, discarding the size afterward. This adds a cheap existence check without changing the output. A `NEEDSWORK` comment is added to the code, flagging the server-side limitation (bare OIDs aren’t validated) for future cleanup.

The changes are minimal and targeted—just a few lines of logic and documentation tweaks—but they close the last open questions before the series graduates. The dynamic allow-list is now fully robust, and the trade-off between efficiency and validation is resolved in favor of correctness. No further rerolls are expected.