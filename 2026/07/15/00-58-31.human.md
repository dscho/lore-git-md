This is Junio Hamano’s reply to Pablo Sabater’s question about how to proceed with the final fixes for the `remote-object-info` series. The series, now at v17, is a security-hardened feature that adds `git cat-file --batch-command remote-object-info` support, enabling users to query object metadata (like size) from remote repositories without downloading full objects.

Junio confirms that v16 was prematurely merged to `next` and then reverted due to an attribution error in the cover letter, so `next` currently does not contain the series. Since v17 is not yet in `next`, Junio asks Pablo to send a full v18 resend of the series—including both the bounds-checking fix (for the out-of-bounds read Junio identified in patch 10/10) and the corrected cover letter attribution. The bounds-checking fix is straightforward: verify that `object_info_values.nr > 1 + size_index` before accessing the array in the response-parsing loop.

Junio also asks how close the series is to being finished. The answer is very close: all technical feedback has been addressed, the implementation is complete, and the only remaining tasks are the bounds-checking fix and the cover letter correction. Once v18 is sent, the series should be ready for re-merging to `next`.