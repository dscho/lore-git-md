This is the twelfth patch in the eighteenth iteration of a security-hardened feature series that adds `remote-object-info` support to `git cat-file --batch-command`. The series is now complete and queued for merging to `next`.

This patch addresses a critical robustness issue in the format placeholder validation for remote object queries. Previously, the code used a simple `strstr()` check to validate placeholders, which could lead to two serious problems: (1) segmentation faults when `expand_atom()` recognized an atom but the remote server did not provide the corresponding data, and (2) the command dying when encountering unknown atoms, which would break local queries if the same format string was reused. The fix introduces a strict allow-list of supported atoms (`objectname` and `objectsize`) and modifies the behavior to return empty strings for unsupported placeholders, aligning with how `for-each-ref` handles known-but-inapplicable atoms.

The patch also adds defensive programming measures, such as initializing `data->type` to `OBJ_BAD` and adding a NULL check for `type_name()`, to prevent crashes from uninitialized data. The documentation is updated to clarify that unsupported placeholders will return empty strings rather than raising errors. Tests are adjusted to reflect the new behavior, including an explicit test for empty string returns on unknown placeholders.

This is a well-crafted fix that directly addresses the last major robustness concern in the series. The allow-list approach is a sensible security measure, and the behavioral change to return empty strings is consistent with Git’s established patterns. The patch is small, focused, and thoroughly tested, making it a clear improvement over the previous implementation.