This is the final patch in the GSoC 2026 `remote-object-info` series, now at v18 and queued for merging to `next`. The patch replaces the static allow-list for `git cat-file --batch-command` format placeholders with a dynamic system that adapts to what the server actually advertises.

The change is straightforward: instead of hardcoding support for only `%(objectname)` and `%(objectsize)`, the client now queries the server for its `object-info` capabilities (e.g., `size`, `type`) and populates a runtime allow-list. This means `%(objecttype)` will work automatically once servers start advertising it, without requiring a client-side code change. The patch also ensures that unsupported placeholders return empty strings rather than errors, matching the behavior of `git for-each-ref`.

The implementation is clean: it moves the `object_info_options` list out of `get_remote_info()` so the caller can control which options are requested, and it adds a mapping table (`remote_atom_map`) to translate protocol options (`size`, `type`) into format atoms (`objectsize`, `objecttype`). The patch includes a small but important safety measure: if the server advertises no supported options, the client skips the request entirely rather than sending an empty query.

This is a well-motivated, forward-looking change that resolves the last architectural limitation in the series. The commit message is clear, the code is well-structured, and the test coverage in the series (699 lines in `t/t1017`) ensures the new behavior is exercised. The series as a whole is now functionally complete and ready for integration.