This is the second patch in the v5 series refactoring `trust_executable_bit` and `has_symlinks` configuration as part of the `the_repository` removal effort. The patch moves the `ce_mode_from_stat()` helper function from `read-cache.h` (where it was a static inline) to `read-cache.c`, preparing it to access repository-specific configuration logic in later patches.

The change is mechanical and preparatory: the function’s logic remains identical, but relocating it to the `.c` file allows future patches to pass repository context without exposing global variables. The patch also removes the now-unnecessary `extern` declarations for `trust_executable_bit` and `has_symlinks` from the header. No behavior changes are introduced, and the commit message explicitly notes this is a prerequisite for repository-scoped configuration access.

This patch is uncontroversial and aligns with the series’ goal of eliminating global state. The added comment in `read-cache.c` clarifies the function’s role in handling filesystem limitations (symlinks and executable bits), which may help future reviewers. The patch touches only two files (`read-cache.c` and `read-cache.h`) and is a straightforward refactoring step. Reviewers are likely to focus on whether the relocation is clean and whether the comment adds value, but no substantive objections are expected.