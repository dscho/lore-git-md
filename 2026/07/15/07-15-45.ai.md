This is a reply in the "What's cooking in git.git" thread from Patrick Steinhardt, addressing two topics in Junio's integration report: `ps/odb-for-each-object-filter` and `cl/conditional-config-on-worktree-path`.

Patrick confirms that `ps/odb-for-each-object-filter` (the 10-patch series introducing object filters to `odb_for_each_object()`) is ready for `next` after a minor reroll to fix function name references in commit messages. The series enables backends to optimize object traversals using reachability bitmaps and simplifies `git cat-file --batch-all-objects` by leveraging the new generic interface. No substantive changes were needed, and the topic is now effectively ack'd for integration.

He also endorses `cl/conditional-config-on-worktree-path` (the 2-patch series adding `worktree` and `worktree/i` conditions to `includeIf`), calling it ready for `next`. This feature allows Git config files to conditionally include other files based on the worktree's location, complementing the existing `gitdir` and `onbranch` conditions. The implementation refactors `include_by_gitdir()` into a more general `include_by_path()` helper, making the codebase more maintainable.

Both topics are part of larger ongoing efforts: the ODB abstraction work (Patrick's domain) and config system improvements. Neither appears controversial, and Patrick's explicit "ready to go" signals confidence in their stability. The next "What's cooking" report will likely show both topics cooking in `next`.