This is a refactoring patch in Patrick Steinhardt’s ODB abstraction series. It marks the object filter parameter in `for_each_bitmapped_object()` and its helper functions as `const` to reflect that the filter is never modified. The change is purely mechanical: four lines of function signatures updated in `pack-bitmap.c` and `pack-bitmap.h`.

The patch is part of the v4 series that is otherwise ready for integration. Taylor Blau’s review of v2 noted this as a minor stylistic improvement, and it is now incorporated. No behavior changes or new functionality are introduced; the patch simply tightens the type system to prevent accidental modification of the filter. The commit message is clear and the diff is minimal and focused. There is no controversy or open question here.