This thread is a small but typical refactoring from René Scharfe, aimed at cleaning up the `remote-curl` transport code. The patch removes a redundant parameter (`nr_spec`) that tracked the number of push specifications, since the array is already NULL-terminated (as all `strvec` arrays are). Instead, the code now relies on the NULL terminator to determine the end of the array, simplifying the logic in `push_dav`, `push_git`, and `push`.

The change is confined to `remote-curl.c` and is purely mechanical—no behavior changes are expected. René’s patches are known for being tidy and focused, and this one follows that pattern. There’s been no discussion yet, so the patch is still under review, but given its nature, it’s likely to be uncontroversial. If you’re tracking transport or HTTP-related changes, this is a minor cleanup worth noting.