This is the ninth patch in the eighteenth iteration of a security-hardened feature series that adds `remote-object-info` support to `git cat-file --batch-command`. The series is now complete and queued for merging to `next`.

This patch updates the Git protocol v2 documentation and the server-side code to advertise the `object-info` capability. The documentation changes clarify that object IDs must be full (not abbreviated), define `obj-size` explicitly as a numeric value, and specify that unsupported object IDs return just the object ID followed by a space (regardless of requested attributes). The server-side code in `serve.c` now advertises `size` as a supported attribute when `transfer.advertiseobjectinfo` is enabled, allowing clients to discover whether the server can provide object size information without downloading the full object.

The patch is a straightforward documentation and capability advertisement update, reflecting the design decisions already implemented in earlier patches of the series. It ensures protocol-level consistency between client and server expectations, particularly around handling unsupported object IDs and optional attributes. The changes align with Git's established patterns for capability advertisement and protocol documentation.