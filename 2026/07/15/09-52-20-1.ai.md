This is patch 12/13 in the eighteenth iteration of a security-hardened feature series that adds `remote-object-info` support to `git cat-file --batch-command`. The series is now complete and queued for merging to `next`.

This patch addresses a critical robustness issue in the format placeholder validation for remote object queries. Previously, the code used a simple `strstr()` check to validate placeholders, which could lead to two failure modes: (1) segfaults when `expand_atom()` recognized an atom but the remote server did not provide the corresponding data, leaving `data->type` uninitialized, and (2) fatal errors when unknown atoms were encountered, blocking local queries if the same format string was reused. The fix introduces a dynamic allow-list of supported atoms (`objectname` and `objectsize`) and modifies the behavior to return empty strings for unsupported placeholders, aligning with how `for-each-ref` handles known-but-inapplicable atoms.

The implementation adds an `is_remote` flag to `struct expand_data` and initializes `data->type` to `OBJ_BAD` as a safety measure. It also includes a NULL check for `type_name()` to prevent segfaults from uninitialized data. The patch updates the documentation to clarify that unsupported placeholders will return empty strings rather than raising errors, and modifies the tests to reflect this new behavior. Notably, it removes the previous `die()` calls for unsupported filter options (e.g., `%(objectsize:disk)` and `%(deltabase)`) and replaces them with empty string returns, which is a more graceful failure mode for remote queries.

The changes are minimal but important for security and usability. The allow-list approach ensures clients cannot request metadata that servers do not advertise support for, while the empty-string fallback prevents format strings from breaking local operations. The patch maintains the series' focus on robustness and security, addressing the last known edge cases in the format placeholder handling. All tests pass, and the series remains on track for integration.