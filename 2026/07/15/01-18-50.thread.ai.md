Thread identity
Subject: [PATCH v5 0/2] repository: move `fetch_if_missing` into `struct repository`
Series version: v5 (2-patch series, part of libification and `the_repository` removal efforts)
Originating author: Tian Yuchen <cat@malon.dev>
Thread type: patch submission

Core problem or goal
Move the global `fetch_if_missing` variable into `struct repository` to make it a per-repository flag controlling whether missing objects should trigger lazy fetches from promisor remotes. This continues the project-wide effort to eliminate global state in favor of repository-specific configuration, preserving existing behavior (default value remains `1`, enabled).

Current status
**Technically complete for the immediate goal**; v5 posted addressing all remaining feedback from Junio’s v4 review by eliminating the last five direct uses of `the_repository` for `fetch_if_missing` in `builtin/fetch-pack.c`, `builtin/pack-objects.c`, `builtin/rev-list.c`, `common-init.c`, and `git.c`. The preparatory patch (1/2) ensures the `repo` parameter is propagated in `pack-objects.c` (e.g., `read_stdin_packs()`), resolving the last mechanical blocker. The series is **ready for maintainer review** but **not yet queued**; the broader architectural question of whether `fetch_if_missing` belongs in `struct repository` remains unresolved. The series is coordinated with other topics in `seen` and `next` to avoid conflicts.

Key technical details
- **Files touched**: 17 files, including `odb.c`, `repository.c`, `fetch-pack.c`, `fsck.c`, `index-pack.c`, `revision.c`, `setup.c`, `pack-objects.c`, `fetch-pack.c`, `rev-list.c`, `common-init.c`, `git.c`, and partial clone documentation. All five lingering `the_repository` references from v4 are now resolved.
- **New symbols**: `struct repository->fetch_if_missing` (replaces global `fetch_if_missing`).
- **Behavior**: Default value remains `1` (enabled); initialization occurs in `initialize_repository()`. Special handling in `fsck.c` and `index-pack.c` defers operations until after parameter parsing to avoid null pointer dereferences. `revision.c` uses `revs->repo` instead of `the_repository`, ensuring consistency with the function's parameters. The ternary fallback in `index-pack.c` (`(repo ? repo : the_repository)->fetch_if_missing`) remains unchanged to handle repository-less invocations.
- **Edge cases**:
  - The `setup.c` fix (`repo->fetch_if_missing = 0` when `NO_LAZY_FETCH_ENVIRONMENT` is set) now uses the passed-in `repo` parameter, resolving the inconsistency noted in v3.
  - The v5 series resolves the conflict with `cf1687a41c` (split of `setup_git_env_internal()`) by coordinating with other topics in `seen` and `next`.
  - Junio’s v2 review outlined three possible approaches to resolve repository-less invocation issues:
    1. Require `index-pack` to always run inside a repository (adopted in v3 for the immediate fix).
    2. Use `the_repository` as a fallback when `repo` is NULL (partially adopted in v3 and v4 via the ternary operator).
    3. Introduce a new global "basic settings" structure to hold flags like `fetch_if_missing` that apply even outside a repository, with per-repository settings overriding them.
  - The broader design question of whether `fetch_if_missing` belongs in `struct repository` remains open. The author has deferred a more comprehensive solution (e.g., Junio’s third approach) until subsystems are fully prepared to handle `repo` instead of `the_repository`.
  - **Preparatory patch (1/2)**: Drops the `UNUSED` annotation from `cmd_pack_objects()`'s `repo` parameter and propagates it into `read_stdin_packs()`, which previously hard-coded `the_repository`. The `option_parse_missing_action()` callback, constrained by its fixed signature, records the desired missing-action mode (`arg_missing_action`) and defers the actual `fetch_if_missing = 0` side effect to `cmd_pack_objects()` after `parse_options()` returns, where `repo` is available. This ensures the repository parameter is explicit throughout the call graph without altering behavior.
- **On-disk format**: No changes.
- **Test coverage**: No new tests; existing behavior is preserved. The series addresses the mechanical correctness issues, but explicit testing of per-repository behavior (especially in `index-pack` and `revision`) and repository-less invocations remains unconfirmed. The author has coordinated with other topics to avoid conflicts, but verification of multi-repository behavior is still pending.

Open questions and loose ends
- **Repository-less invocations**: The broader architectural question of whether `fetch_if_missing` should be tied to `struct repository` remains unresolved. Junio’s v2 review outlined three approaches, and the author has adopted the first (restricting `index-pack` to repository-bound operation) as a pragmatic solution. The other two approaches—a fallback to `the_repository` or a new global "basic settings" structure—remain on the table for future discussion.
- **Testing**: The author has not provided explicit confirmation that the series was tested with repositories other than `the_repository` or in repository-less contexts. While the fixes address the raised concerns, the absence of new test coverage or multi-repository verification leaves a risk of undetected regressions. The next iteration may need to include tests for the new repository-bound behavior of `index-pack`, per-repository `fetch_if_missing` consistency, and the resolved `the_repository` references.
- **Design preference**: Patrick Steinhardt previously suggested moving the flag into the object database rather than `struct repository`, but acknowledged the current approach aligns with libification goals. Junio’s review now challenges whether the flag belongs in `struct repository` at all, making this a more fundamental point of discussion. The v5 series resolves the immediate technical blockers, but the long-term architectural direction remains open.
- **Architectural clarification**: Junio may still request further clarification on whether `fetch_if_missing` belongs in `struct repository` before queuing the series. The author has deferred a more comprehensive solution (e.g., Junio’s third approach) until subsystems are fully prepared to handle `repo` instead of `the_repository`.

Participant positions
- **Tian Yuchen (author)**: Addressed all feedback in v5 by resolving the five remaining `the_repository` references, ensuring the `repo` parameter is passed through the call graph in `pack-objects.c`, and coordinating with other topics to avoid conflicts. Position: **series is mechanically ready for review**; architectural clarification may still be needed before queuing.
- **Junio C Hamano (maintainer)**: Identified the critical correctness issues in v1 (`setup.c`) and surfaced the repository-less invocation edge case in v2. Outlined three possible approaches to resolve the design question, with the first (restricting `index-pack` to repository-bound operation) adopted in v3 and v4. Raised concerns in v3 about the `setup.c` inconsistency and conflict with `cf1687a41c`, both resolved in v4. **Latest review (v4)** identified five remaining `the_repository` references and provided specific guidance for fixing them, now fully addressed in v5. Position: **series is technically complete for the immediate goal**; architectural clarification may still be required before queuing.
- **Patrick Steinhardt**: Raised substantive concerns about potential behavior changes in `index-pack.c` and `revision.c` in v1, which were addressed in v2. Identified the null pointer dereference risk in `builtin/index-pack.c` in v2, which is resolved in v3 and later. Position: **series looks good for the immediate issues**; no new concerns raised in v5.

Related work
- **Libification effort**: Ongoing project to make Git's internals more modular and reusable as a library.
- **`the_repository` removal**: Broader initiative to eliminate the `the_repository` global variable, with René Scharfe as a primary driver. This series follows the established pattern of moving globals into `struct repository`.
- **Partial clone documentation**: Updated to reflect the new per-repository nature of the `fetch_if_missing` flag.
- **`cf1687a41c`**: Recent topic in `seen` that splits `setup_git_env_internal()`, now resolved in v5 by coordinating with other topics.