Thread identity
Subject: [PATCH] repository: move `fetch_if_missing` into `struct repository`
Series version: standalone patch (part of libification and `the_repository` removal efforts)
Originating author: Tian Yuchen <cat@malon.dev>
Thread type: patch submission

Core problem or goal
Move the global `fetch_if_missing` variable into `struct repository` to make it a per-repository flag controlling whether missing objects should trigger lazy fetches from promisor remotes. This continues the project-wide effort to eliminate global state in favor of repository-specific configuration, preserving existing behavior (default value remains `1`, enabled).

Current status
Under review; no prior versions of this specific patch have been posted. Junio C Hamano identified a critical correctness issue in `setup.c` where the patch hardcodes `the_repository->fetch_if_missing` instead of using the passed `repo` parameter, breaking per-repository behavior. The thread remains open pending a v2 update addressing this issue.

Key technical details
- **Files touched**: 15 files, including `odb.c`, `repository.c`, `fetch-pack.c`, `fsck.c`, `index-pack.c`, `revision.c`, `setup.c`, and partial clone documentation.
- **New symbols**: `struct repository->fetch_if_missing` (replaces global `fetch_if_missing`).
- **Behavior**: Default value remains `1` (enabled); initialization occurs in `initialize_repository()`. Special handling in `fsck` and `index-pack` avoids null pointer dereferences during early option parsing. `revision.c` adds a `BUG()` check to enforce that `--exclude-promisor-objects` can only be used when `fetch_if_missing` is disabled.
- **On-disk format**: No changes.
- **Test coverage**: No new tests; existing behavior is preserved, but the correctness issue in `setup.c` suggests multi-repository scenarios may not have been tested.

Open questions and loose ends
- **Critical correctness issue**: The patch hardcodes `the_repository` in `setup_git_env_internal()`, undermining its per-repository goal. This must be fixed in v2 (`repo->fetch_if_missing = 0`).
- **Testing**: The oversight raises questions about whether the patch was tested with repositories other than `the_repository`. Future revisions should verify multi-repository behavior.

Participant positions
- **Tian Yuchen (author)**: Submitted the patch as part of the libification and `the_repository` removal efforts. No prior review feedback recorded before Junio's critique.
- **Junio C Hamano (maintainer)**: Identified the critical correctness issue in `setup.c`. The review is narrowly scoped but blocks merging until addressed. Position: **request for changes** (v2 required).

Related work
- **Libification effort**: Ongoing project to make Git's internals more modular and reusable as a library.
- **`the_repository` removal**: Broader initiative to eliminate the `the_repository` global variable, with René Scharfe as a primary driver. This patch follows the established pattern of moving globals into `struct repository`.
- **Partial clone documentation**: Updated to reflect the new per-repository nature of the `fetch_if_missing` flag.