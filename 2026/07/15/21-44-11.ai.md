This is the final technical exchange in the GSoC v18 `remote-object-info` series, now queued for `next`. Pablo Sabater addresses the last two open questions from Junio: how unsupported placeholders are handled in the output, and whether `%(objectname)`-only requests should trigger a server-side existence check.

The patch in question (13/13) makes the allow-list for `git cat-file --batch-command` dynamic, so the client only requests metadata fields the server actually supports. Pablo clarifies that unsupported placeholders (like `%(objectcolor)`) produce an empty string in the output (`42 `), not the literal placeholder or a fatal error. This distinction—between *unknown* placeholders (which die) and *known but unsupported* ones (which are omitted)—will be documented in the next reroll.

On `%(objectname)`-only requests, Pablo initially proposed an early return to avoid unnecessary connections, but Junio prefers querying the server for *some* cheap metadata (e.g., size) to validate the object’s existence. Pablo now agrees to force a "size" request when only `%(objectname)` is used, discarding the size on the client side. This ensures the server checks the OID’s existence while keeping the change minimal. A `NEEDSWORK` comment will be added to revisit this on the server side in a future series.

The discussion is substantive but narrow, focusing only on edge-case behavior and documentation. The series is technically complete, with all prior feedback incorporated and CI passing. This reply resolves the last design tension, clearing the way for graduation to `next`. The dynamic allow-list and placeholder handling are now well-documented, and the `%(objectname)` question is settled in favor of robustness over efficiency.