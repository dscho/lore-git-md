This thread is about a refactoring patch by Michael Montalbo that makes `get_commit_action()` in `revision.c` a pure predicate, eliminating a long-standing side effect where it mutates tracked line ranges during line-level log operations (`git log -L`). The patch is internal, with no user-visible changes, but it aims to make the code safer for future optimizations like lookahead evaluation. It introduces a helper function, `commit_early_ignore()`, and a test subcommand, `line-log-peek`, to verify the function’s purity.

Junio C Hamano, the maintainer, has raised a substantive question about the patch’s purpose and scope. While the patch is well-documented and includes tests, Junio’s reply suggests he is unclear whether this is a cleanup, bugfix, or new feature, and he does not recall suggesting a change of this size. The thread is now at a standstill: the patch remains under review, but its future depends on Michael clarifying the motivation and ensuring the scope aligns with project priorities. No other reviews have been posted yet.