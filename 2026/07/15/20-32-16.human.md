This is the final review exchange in the long-running GSoC v18 series that implements `git cat-file --batch-command`'s `remote-object-info` feature. The series is now queued for `next` and technically complete, with this email resolving the last clarification Junio requested before graduation.

Junio asks for two clarifications in the commit message: first, the distinction between "unknown" and "known but unsupported" format placeholders, and second, whether the client should contact the server when only `%(objectname)` is requested. Pablo confirms that unsupported placeholders (like `%(objectcolor)`) are omitted entirely from the output, producing `42 ` rather than `42 %(objectcolor)` when only `%(objectsize)` is supported. For `%(objectname)`-only requests, Pablo initially proposes an early return to avoid unnecessary connection overhead, but Junio pushes back, suggesting the client should still contact the server to validate the object's existence, even if only to fetch a cheap attribute like size.

The exchange is brief but decisive: it clarifies the final edge-case behavior and ensures the commit message will document it clearly. No code changes are needed beyond the early-return tweak Pablo already planned, and the dynamic allow-list design remains intact. The series is now ready to graduate to `next` with all prior feedback addressed.