This is a refactoring patch that changes the return type of UTF-8 string-width calculation functions (`utf8_strwidth()` and `utf8_strnwidth()`) from `int` to `size_t` to better match their inherently non-negative semantics. The v2 revision addresses feedback by updating all direct callers to use `size_t` for local variables holding the return value, removing redundant parentheses, and improving the commit message. However, it still retains explicit casts to `int` at call sites where the value is used in contexts requiring `int` (e.g., arithmetic with `int` variables or format strings), rather than converting those contexts to `size_t`.

The patch touches 11 files, including `utf8.c`/`utf8.h` (the core change), `gettext.c`/`gettext.h` (a wrapper function), and several built-in commands (`blame`, `branch`, `repo`) and formatting utilities (`column`, `diff`, `pretty`, `wt-status`). The header-level change means *all* callers now see the new `size_t` return type, even if they are not updated in this patch. The author has audited and updated the most obvious callers, but some implicit conversions remain where the value is immediately cast back to `int`.

The discussion in the thread has centered on two unresolved concerns: whether the patch should update *all* callers to use `size_t` consistently (rather than relying on implicit conversion), and whether removing the overflow check in the now-deleted `cast_size_t_to_int()` helper could introduce subtle bugs. The v2 revision does not fully resolve these issues, but it demonstrates progress by reducing the number of implicit conversions and clarifying the commit message. The patch is well-motivated (addressing a `//TODO` comment) and appears technically sound for the callers it updates, but the remaining implicit conversions may still draw scrutiny.