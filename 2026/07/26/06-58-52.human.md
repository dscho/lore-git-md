This is a **substantive review** of the first patch in the **ready-to-merge v6 series** optimizing `paint_down_to_common()` for one-sided histories. The series is **queued in Junio’s tree** and delivers **100-1000x speedups** for asymmetric merge-base queries, but remains **blocked on review bandwidth**.

Elijah Newren’s review focuses on the **new technical document** (`Documentation/technical/paint-down-to-common.adoc`) added in this patch. He **praises the document** as "really nice" and highlights the **critical invariant** (topological traversal in the finite generation region) but **identifies a potential gap**: the document does not explicitly address how **v1 commit-graph generation numbers** (which saturate at `GENERATION_NUMBER_V1_MAX`) interact with the algorithm. He suggests treating `GENERATION_NUMBER_V1_MAX` the same as `INFINITY` for consistency, since both can violate the desired ordering.

He also **questions the wording** around the `min_generation` cutoff, noting that the current explanation might mislead future callers into expecting `MERGE_BASE_FIND_ALL` results when passing a nonzero `min_generation`. The concern is **forward-compatibility**: the optimization relies on callers *not* needing deeper merge bases, but the document does not make this constraint clear.

The review is **technical and constructive**, engaging with the algorithm’s edge cases rather than surface-level details. It does not block the series (which is procedurally unblocked and fully reviewed), but the points raised could **improve clarity for future maintainers**. Kristofer Karlsson’s proposed simplifications (e.g., deferring the commit-date fallback cleanup) might also help address this feedback by reducing complexity.