This is a brief follow-up in the ongoing review of Son Luong Ngoc’s two-patch series fixing edge cases in `git rebase --update-refs` when dealing with symbolic references. Junio C Hamano joins the thread to weigh in on a specific technical point raised by Phillip Wood: whether the code should compare the resolved target (`resolved_ref`) or the symbolic name (`decoration->name`) when checking if a branch matches the current HEAD.

Junio agrees with Phillip’s suggestion that using `resolved_ref` would be more robust, as it avoids ambiguity when HEAD points outside `refs/heads/` (e.g., to a tag) and a symref under `refs/heads/` points to the same target. However, he also questions whether such a configuration—HEAD as a symref pointing outside `refs/heads/`—is even valid or desirable. His comment implies that the series might be over-engineering for a corner case that could be treated as user error rather than supported behavior.

The thread remains in active review, with no merge status reported. The next step is likely a v4 from Son Luong Ngoc addressing Phillip’s refinements, including this HEAD comparison logic. Junio’s input adds a layer of architectural scrutiny: whether the patch should handle this edge case at all, or if the project should instead enforce stricter invariants around HEAD’s location. This could spark further discussion about Git’s expectations for symbolic references.