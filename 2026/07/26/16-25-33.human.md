This is a brief technical exchange in the "Make ODB optimizations pluggable" series, specifically about patch 11/12. Junio C Hamano follows up on his earlier review of the signedness fix in `find_base_packs()`, questioning whether the unilateral cast to `uintmax_t` is the right approach when `off_t` might be wider than `unsigned long`. Patrick Steinhardt had already clarified that the cast is technically safe because `pack_size` is never negative and the right-hand side (`limit`, an `unsigned long`) would be promoted to `uintmax_t` under C’s usual arithmetic conversion rules.

Junio’s reply acknowledges the explanation but suggests the cast feels arbitrary rather than a true signedness fix. The exchange does not propose any code change or block the patch; it simply reflects a stylistic preference for how signedness warnings should be addressed. The series remains ready for integration, with no unresolved technical objections. Reviewers concerned about type-width edge cases may still want to verify the behavior on platforms where `off_t` is wider than `unsigned long`, but the discussion itself is now closed.