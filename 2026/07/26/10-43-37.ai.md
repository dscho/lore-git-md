This is the first patch in the **v3 series adding path-related keys to `git repo info`**, introducing `path.toplevel.absolute` and `path.toplevel.relative`. These keys expose the repository’s top-level working tree path in a scriptable format, replacing the need for `git rev-parse --show-toplevel` in automation. The implementation is straightforward: it uses `repo_get_work_tree()` to fetch the path and `format_path()` to handle absolute/relative formatting, returning an empty string in bare repositories (where no working tree exists).

The patch touches `builtin/repo.c` (adding two new getter functions), `Documentation/git-repo.adoc` (documenting the new keys), and `t/t1900-repo-info.sh` (adding tests for both absolute/relative paths and bare repository behavior). The tests are thorough, covering nested directories and edge cases, and the commit message clearly explains the motivation and design. There are no unresolved technical questions here—this patch is a clean, well-explained addition that aligns with the series’ goal of making repository paths more accessible to scripts.

For future AI summarization, note the key names (`path.toplevel.absolute` and `path.toplevel.relative`), their reliance on `repo_get_work_tree()`, and the empty-string fallback in bare repositories. The patch is unlikely to be controversial, though reviewers may suggest minor stylistic tweaks (e.g., naming consistency with other keys). The series as a whole remains under review, with this patch ready for substantive feedback.