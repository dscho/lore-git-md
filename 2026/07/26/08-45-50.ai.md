This is a follow-up cleanup series from Jeff King (Peff) that builds on his recently accepted bugfix for the `git diff --relative --cached` segfault. The series consists of two small refactoring patches targeting `diff-lib.c`, both motivated by historical context uncovered while investigating the original bug.

The first patch removes a stale comment about advancing `o->pos` that no longer reflects the current code flow. The second patch moves the prefix-checking logic earlier in `oneway_diff()`, eliminating redundant work for paths outside the `--relative` prefix. While the second patch makes the original NULL-check fix technically redundant, Peff explicitly proposes keeping both for clarity and safety. Neither patch changes behavior; they are pure refactorings.

The patches are well-scoped and directly tied to the earlier bugfix. Peff’s commit messages reference the same 2011 regression history (76399c0195) and 2021 follow-up (8174627b3d), grounding the cleanups in the same diff plumbing context. The series is positioned as a "might be worth doing" improvement rather than a critical fix, and Peff’s tone suggests he expects no controversy.

Junio’s earlier review of the bugfix did not raise concerns about these specific cleanups, but he has not yet responded to this series. The patches are small (9 lines changed total) and touch only the diff plumbing subsystem, so they are unlikely to introduce new issues. Integration status remains under review, with no indication yet of whether Junio will queue them alongside the original fix.