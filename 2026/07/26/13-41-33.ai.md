This is a refactoring patch targeting the UTF-8 string-width calculation functions, with a substantive review raising a compatibility concern.

René Scharfe's reply questions whether all callers of `utf8_strwidth()` and `utf8_strnwidth()` are safe with the new `size_t` return type, now that the `cast_size_t_to_int()` helper has been removed. This is a critical point: the patch changes the function signatures in `utf8.h`, so any caller outside the provided diff (which only shows `strbuf_utf8_align()`) could now receive a `size_t` where it previously expected an `int`. If those callers perform arithmetic or comparisons assuming a signed type, this could introduce subtle bugs or warnings.

The patch itself is a clean, mechanical type-system cleanup that addresses a long-standing `//TODO` comment. It removes redundant type conversion and updates the internal caller (`strbuf_utf8_align()`) without changing behavior. However, the review highlights that the patch's scope is narrower than its impact: the header change affects all callers, not just the ones shown in the diff. This makes the change potentially riskier than it first appears.

René also points out a minor style nit (unnecessary parentheses around `string` in the ternary), but this is secondary to the compatibility question. The thread is now waiting for Hardik to either audit all callers or explain why the change is safe as-is. Until that happens, the patch remains under review with an open technical question.