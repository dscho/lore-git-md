This is a maintainer's note on René Scharfe’s v2 patch that improves the error message when `git branch -d` refuses to delete a branch currently used by `git bisect`. The patch is now scoped to address only the bisect case, leaving the more complex "in use by rebase" scenarios for future work. Junio considers this a reasonable incremental step and plans to mark the topic for the `next` branch unless objections arise.

The key open question remains how to phrase error messages for branches used by rebase operations, particularly when merges are involved (`BRANCH_CHECKOUT_KIND_UPDATE_REF`). Junio does not propose a solution here but acknowledges the phrasing challenge as the main blocker for broader coverage. The patch itself is technically sound and well-motivated, focusing on a clear usability improvement for the bisect case while deferring more ambiguous scenarios.

This email signals that the patch is likely to advance to `next` soon, assuming no further pushback. The unresolved rebase messaging issue suggests follow-up work will be needed, but the current patch stands on its own as a worthwhile improvement. If you care about branch-safety error messages or the broader API design, this thread is worth watching for future iterations.