This is the second patch in Jeff King’s two-patch refactoring series that follows up on his recently accepted bugfix for the `git diff --relative --cached` segfault. It targets the diff plumbing subsystem in `diff-lib.c`.

The patch moves the prefix-checking logic earlier in `oneway_diff()`, so paths outside the `--relative` prefix are skipped before any further processing. This eliminates redundant work in the diff-index code path, mirroring a similar optimization added to `run_diff_files()` in 2021. While the change makes the original NULL-check fix technically redundant, Peff explicitly keeps both for clarity and safety. The patch is a pure refactoring with no behavior change.

The commit message ties the change directly to the same historical context as the original bugfix (the 2011 regression and 2021 follow-up), grounding it in the same diff plumbing code path. The patch is small (five lines added) and uncontroversial in intent, though Junio has not yet reviewed it. Given his earlier acceptance of the bugfix and the minimal scope of this cleanup, it looks likely to be queued alongside the original fix once reviewed.