This is a follow-up discussion in the already-merged packfile URI race-condition bugfix series. The thread centers on Jeff King’s post-acceptance feedback about three subtle points in the second patch (`http.c`), which Ted Nyman addresses in this exchange.

Peff’s main concern is code clarity: the Windows/MINGW `O_EXCL` loop workaround, the latent `tmpfile_fd` leak fix, and the HTTP 416 handling could each be split into separate preparatory commits to make the core change easier to follow. Ted agrees in principle but notes the series is already in Junio’s `next` branch, so no reroll is planned. The tone is collegial and low-stakes; both participants treat the feedback as advisory rather than blocking.

On the technical side, Ted confirms that the HTTP 416 handling is still necessary: a concurrent downloader can complete the staging file before another process issues its Range request, triggering a 416 response that must be handled gracefully. Peff concedes that real-world servers behave this way, though he muses that a more intuitive 0-byte read would be preferable. The Windows/MINGW workaround—opening a file with `O_EXCL`, closing it, and retrying with `O_RDWR` to ensure `FILE_SHARE_DELETE`—is acknowledged as subtle but unavoidable given MinGW’s quirks. Neither participant proposes an alternative.

The exchange closes with lighthearted banter about the Perl test server, which Peff praises as the right tool for the job despite its complexity. No new technical objections or test failures are introduced, and the series remains ready for graduation to `master`.