This is a **substantive follow-up in the `paint_down_to_common()` optimization thread**, addressing Elijah Newren’s review of the technical documentation. The email focuses on two precision gaps in `Documentation/technical/paint-down-to-common.adoc`: handling of `GENERATION_NUMBER_V1_MAX` (v1 commit-graph saturation) and the wording around `min_generation` cutoffs.

Kristofer Karlsson acknowledges the v1 commit-graph issue—where generation numbers saturate at `GENERATION_NUMBER_V1_MAX` and can violate topological ordering—and proposes two approaches to address it. **Option A** (remapping `V1_MAX` to `INFINITY` at load time) is simpler but risks conflating "not in the graph" with "saturated," potentially causing unnecessary bloom-filter skips. **Option B** (introducing a `commit_graph_generation_topo_ceiling()` helper) preserves the semantic distinction but adds a new concept for callers. He leans toward Option B for its clarity and forward compatibility, though neither approach affects the core optimization’s correctness.

On the `min_generation` wording, Kristofer agrees the current phrasing could mislead future callers into expecting complete `MERGE_BASE_FIND_ALL` results. He drafts a revised note clarifying that a non-zero `min_generation` is only safe for reachability checks among input commits, not for finding all merge bases. This is a **forward-compatibility concern** rather than a bug, but it’s important for long-term maintainability.

The discussion is **non-blocking**—the series is queued in Junio’s tree and procedurally ready for `seen`—but these refinements aim to ensure the documentation remains accurate as the algorithm evolves. The v1 commit-graph issue, while hard to trigger in practice, highlights the need for precise invariants in the technical document. No code changes are required yet, but the proposed adjustments would improve clarity for future maintainers.