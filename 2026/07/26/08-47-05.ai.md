This is the second patch in Jeff King's two-patch refactoring series that follows up on his recently accepted bugfix for the `git diff --relative --cached` segfault. It targets the diff plumbing subsystem in `diff-lib.c`.

The patch moves the prefix-checking logic earlier in `oneway_diff()`, skipping paths outside the `--relative` prefix before they enter the `do_oneway_diff()` code path. This eliminates redundant work for such paths, aligning the behavior with an earlier optimization in `run_diff_files()` (introduced in 8174627b3d, 2021). While this change makes the original NULL-check fix technically redundant, Peff explicitly chose to retain both for clarity and safety. The patch is a pure refactoring with no behavior change.

The commit message grounds the change in the same historical context as the original bugfix (the 2011 regression and 2021 follow-up), making the motivation clear. The patch is small (5 lines added) and touches only the diff plumbing code, so it is unlikely to introduce new issues. Junio has not yet reviewed this patch, but his earlier acceptance of the bugfix suggests openness to cleanups in this area. The first patch in the series (stale comment removal) was uncontroversial, and this one follows the same minimal, well-scoped approach.

For future summarization, note the key technical details: the patch adds an early return in `oneway_diff()` for paths outside the `--relative` prefix, using the same `strncmp` check as the earlier `run_diff_files()` optimization. The change is tied to the `revs->diffopt.prefix` and `revs->diffopt.prefix_length` fields, and the function signature (`oneway_diff`) and file (`diff-lib.c`) are critical identifiers for follow-up discussion. No new symbols or on-disk formats are introduced.