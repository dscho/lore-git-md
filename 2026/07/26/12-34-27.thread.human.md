This thread wraps up a small but important documentation cleanup for Git’s UTF-8 string-width functions. Hardik Kumar’s patch (now at v5) adds a clear explanation in `utf8.c` for why `utf8_strwidth()` and `utf8_strnwidth()` return `int` rather than `size_t`: it aligns with `printf()`-style APIs, which expect `int` for padding calculations (e.g., `printf("%*s", width, string)`), and the explicit `cast_size_t_to_int()` wrapper prevents overflow when converting from `size_t`. The patch itself changes no code—it simply documents the rationale that emerged from earlier debate.

The discussion is now in its final stages. Phillip Wood and Junio C Hamano have both reviewed the proposed comment and suggested minor refinements to ensure it fully captures the design constraints and safety considerations. The only remaining task is for Hardik to update the commit message and in-code comment to explicitly reference commit 937b7c8b, which introduced `cast_size_t_to_int()` to fix a crash. Once that’s done, the patch should be ready to merge. There are no open objections or technical disagreements—just a bit of polish left before the thread can close.