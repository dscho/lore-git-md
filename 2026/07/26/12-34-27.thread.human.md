This thread is wrapping up the documentation phase of a long-running discussion about Git’s UTF-8 string-width functions. Hardik Kumar’s latest patch (v5) updates the comments in `utf8.c` to explain why `utf8_strwidth()` and `utf8_strnwidth()` return `int`—to stay consistent with `printf()`-style APIs—and why `cast_size_t_to_int()` is necessary to prevent overflow crashes. The patch itself is uncontroversial and aligns with the earlier consensus to document the existing design rather than change it.

The only remaining task is to reference the original commit (937b7c8b) that introduced `cast_size_t_to_int()` to fix a crash, as requested by Phillip Wood. Hardik has confirmed he’ll make this update, after which the patch should be ready for Junio to pick up. No further technical debate is expected; this is purely a documentation clarification to close out the thread. The functions’ behavior remains unchanged.