This is a documentation and error-message patch, the first of three in Ted Nyman’s v5 series fixing packfile URI download race conditions. It corrects the `--index-pack-arg` option name in both the `git-http-fetch` man page and the error messages in `http-fetch.c`, changing the plural `--index-pack-args` to the singular `--index-pack-arg` to match the actual command-line interface. The man page now also clarifies that the first instance specifies the command and subsequent instances specify its arguments, resolving an ambiguity noted in earlier reviews.

The patch is purely textual, touching only `Documentation/git-http-fetch.adoc` and `http-fetch.c`. It is uncontroversial and has been stable since v3 of the series; the thread AI summary notes it as “merged to baseline” and ready for `next`. No code behavior changes are introduced, and the patch carries no risk of regression. It is a prerequisite for the subsequent patches, which rely on the corrected option name.