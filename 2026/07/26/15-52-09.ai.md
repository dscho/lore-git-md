This is a follow-up in the ongoing refactoring thread that changes the return type of `utf8_strwidth()` and `utf8_strnwidth()` from `int` to `size_t`. The author, Hardik Kumar, acknowledges the substantive review feedback from Pablo Sabater and René Scharfe and signals intent to revise the patch for a v2 submission.

The key unresolved question remains whether the patch should rely on implicit conversion at call sites or explicitly update all callers to use `size_t`. Hardik’s reply does not resolve this—he merely notes he will address the feedback in v2, leaving the core compatibility concern (raised by Pablo’s audit of callers in `builtin/repo.c` and elsewhere) still open. The removal of the overflow check in `cast_size_t_to_int()` also remains unaddressed; no rationale or verification of caller reliance has been provided.

The only concrete change mentioned is a surface-level style fix (removing unnecessary parentheses around `string` in a ternary), which Hardik plans to include in the next version. The commit message rewrite Pablo suggested (to be more direct and explanatory) is also acknowledged but not yet implemented.

This email advances the thread only by signaling the author’s responsiveness to feedback; the substantive technical questions about caller safety and patch scope remain unresolved. The next version will likely clarify the author’s chosen approach to the type-system change, but for now, the patch is still under active discussion and not ready for merging.