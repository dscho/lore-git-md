This is a maintainer's integration note on René Scharfe’s v2 patch that improves the error message when `git branch -d` refuses to delete a branch currently used by `git bisect`. Junio marks the topic ready for `next`, noting that the patch now focuses solely on the bisect case and defers the more complex "in use by a rebase" scenarios due to unresolved phrasing challenges. The patch is acknowledged as a reasonable incremental step, with the door left open for follow-up work to address the remaining checkout kinds (rebase, worktree) and to refine error messages further. No objections or new technical concerns are raised; the tone is matter-of-fact and forward-looking.