This is a minor test-infrastructure patch in the ongoing `paint_down_to_common()` optimization series. Kristofer Karlsson acknowledges René Scharfe’s surface-level review suggesting a cosmetic improvement: replace the hard-coded two-line string literal (`*"
"`) with the existing `$LF` variable (which holds a newline) in the `test_trace2_data_singular()` helper. The change is trivial—purely stylistic—and will be incorporated in the next reroll (v7). The patch itself remains non-blocking; the series as a whole is already queued in Junio’s tree and procedurally ready for merging, with review bandwidth the only remaining bottleneck.