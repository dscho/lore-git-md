Thread identity: standalone refactoring patch series by Hardik Kumar changing the return type of UTF-8 string-width calculation functions (`utf8_strwidth()` and `utf8_strnwidth()`) from `int` to `size_t` in `utf8.c` and `utf8.h`. The series was **withdrawn after three iterations (v1–v3)** following sustained reviewer opposition from Junio C Hamano, Pablo Sabater, and Phillip Wood. The final action is a **documentation update** to explain the existing design's rationale, now confirmed to be placed **directly in `utf8.c`** as a replacement for the existing TODO comment.

Core problem or goal: eliminate a type mismatch where functions computing inherently non-negative display widths (UTF-8 string width) return `int` instead of `size_t`. The effort was intended as a purely type-system cleanup but **reviewers unanimously concluded the practical benefits were minimal** for functions measuring display widths of strings that fit on a single line, while risks (regressions, complexity, and pervasive explicit casts) outweighed benefits. The withdrawal resolves the debate in favor of the status quo, with **improved documentation** to clarify the existing design’s rationale.

Current status: **withdrawn and concluded**. Hardik Kumar’s final email acknowledges the v3 approach—prioritizing minimal disruption by reintroducing explicit casts to `int` at call sites—failed to deliver meaningful type-system improvements. The patch’s regressions (e.g., underflow risks in `diff.c`, mixed-sign comparisons in `pretty.c`, struct field mismatches in `builtin/repo.c`) and incomplete conversions (e.g., overlooked callers in `builtin/worktree.c`) will **not be addressed**. The **documentation update** will:
- Replace the TODO comment in `utf8.c` with a rationale explaining (a) why the functions return `int` (to align with APIs like `printf()` that require `int` for padding calculations) and (b) the necessity of `cast_size_t_to_int()` to prevent overflows.
- The `cast_size_t_to_int()` helper will **remain in place** with its rationale documented in the same comment.
Junio C Hamano’s latest email decisively resolves the documentation placement question: the rationale should be **colocated with the code in `utf8.c`**, not in a separate file in `Documentation/technical/`. No further technical discussion is expected unless Phillip Wood or others reply with additional placement guidance.

Key technical details:
- Files: `utf8.c`, `utf8.h`, and 9 caller files (`builtin/blame.c`, `builtin/fetch.c`, `builtin/repo.c`, `column.c`, `diff.c`, `gettext.c`, `gettext.h`, `pretty.c`, `wt-status.c`). Phillip Wood’s review suggested additional callers (e.g., `builtin/worktree.c`) may have been affected or overlooked.
- Functions: `utf8_strwidth()`, `utf8_strnwidth()`, `gettext_width()` (updated signature in v3), `strbuf_utf8_align()` (updated in v1), `item_length()` (in `column.c`), `maxwidth()` (in `wt-status.c`).
- Symbols: `cast_size_t_to_int()` (removed in v3, but will remain with documented rationale in `utf8.c`).
- Behavior: unchanged in practice, as v3 reintroduced explicit casts to `int` at call sites, preserving original behavior while failing to deliver type-system improvements. Key regressions persisted:
  - `diff.c`: underflow in `len -= 3` (now `size_t`) and unsafe `padding = len - cast_size_t_to_int(...)`.
  - `pretty.c`: mixed-sign comparisons between `size_t occupied` and `int padding`.
  - `builtin/blame.c`: `-Wsign-compare` errors due to casts back to `int` (highlighted by Phillip Wood as impractical for `printf()` usage).
  - `builtin/repo.c`: struct fields (`name_col_width`, `value_col_width`) remained `int`, forcing casts.
  - `gettext.h`: conditional compilation fixes were correct but untested.
- Variables: `len` (now `size_t` in `diff.c` but cast back to `int`), `occupied` (now `size_t` in `pretty.c`), `num` (now `size_t` in `blame.c` but cast back to `int`), `name_len` (mixed-sign comparisons in `diff.c`).
- Test coverage: none mentioned; affected code paths are used in `git diff --stat`, commit message formatting, and `git blame` output.
- **Documentation**: Will be placed **in `utf8.c`**, replacing the existing TODO comment. Content will cover:
  - Why `utf8_strwidth()`/`utf8_strnwidth()` return `int` (alignment with `printf()`-style APIs).
  - Why `cast_size_t_to_int()` is necessary (overflow prevention).

Open questions and loose ends: **fully resolved**. The follow-up documentation patch will address:
- **Overflow check rationale**: `cast_size_t_to_int()` will remain, with its overflow prevention rationale documented in `utf8.c`.
- **Mixed-sign comparisons**: No longer a concern, as functions retain `int` return type.
- **Underflow risks**: The `diff.c` underflow (`len -= 3`) and unsafe `padding` calculations will persist but are now documented as part of the existing design.
- **Struct field mismatches**: The `stats_table` struct fields in `builtin/repo.c` will remain `int`, with casts documented as necessary.
- **Documentation location**: **Resolved by Junio C Hamano**: the rationale will be placed in `utf8.c`, replacing the TODO comment.

Participant positions:
- **René Scharfe**: substantive review in v1 identifying compatibility risks (high weight). His feedback is resolved by withdrawal.
- **Pablo Sabater**: **substantive, high-weight review in v2** identifying regressions (underflow, mixed-sign comparisons, conditional compilation issues) and questioning the commit message’s accuracy. His concerns are resolved by withdrawal.
- **Junio C Hamano**: **substantive, high-weight review in v2** identifying regressions in `diff.c` and `pretty.c` and **strongly questioning the patch’s overall value**. His latest stance endorses documenting the overflow prevention rationale for `cast_size_t_to_int()` and **resolves the documentation placement** by directing it to `utf8.c`. His feedback is resolved.
- **Phillip Wood**: **substantive, high-weight review in v3** **strongly opposing the patch** on technical and practical grounds. He argued the patch forced callers to explicitly cast `size_t` results back to `int` for use with `printf()`, creating unnecessary complexity. His proposed alternative—**abandoning the patch and documenting the existing design**—is now the consensus outcome. His feedback is resolved by withdrawal.
- **Hardik Kumar**: author. The v3 patch addressed compilation errors but reintroduced explicit casts to `int` at call sites. His final email **withdraws the patch** and asks where to place the documentation. Junio’s reply resolves this by directing the documentation to `utf8.c`.

Related work: none. The withdrawal concludes this standalone refactoring effort. The only remaining action is the documentation update in `utf8.c`.