Thread identity: standalone documentation patch by Hardik Kumar concluding the UTF-8 string-width type-refactoring thread. The patch (v5) updates `utf8.c` to document the rationale for `utf8_strwidth()` and `utf8_strnwidth()` returning `int` (alignment with `printf()`-style APIs) and the necessity of `cast_size_t_to_int()` for overflow prevention. The series is now at **v5**, replacing the withdrawn v1–v3 refactoring attempts.

Core problem or goal: document the design rationale for the existing `int` return type of UTF-8 string-width functions, resolving the earlier debate about type-system cleanup. The patch clarifies why the functions return `int` (consistency with `printf()`-style APIs for padding calculations) and why `cast_size_t_to_int()` is required (overflow prevention when converting `size_t` to `int`). The goal is purely explanatory, with no code behavior changes.

Current status: **under review, ready for final revision**. Hardik Kumar’s v5 patch addresses Phillip Wood’s feedback from v4 by explicitly stating the overflow prevention rationale in both the commit message and in-code comment. The sole remaining action item is now **resolved in principle**: Hardik confirms he will add a reference to the original commit (937b7c8b) that introduced `cast_size_t_to_int()` to fix a crash, as requested by Phillip Wood. The patch is otherwise uncontroversial and aligns with the thread’s consensus to document the status quo.

Key technical details:
- Files: `utf8.c` (updated). The patch removes the TODO comment and adds a three-line rationale explaining the `int` return type and `cast_size_t_to_int()`’s role.
- Functions: `utf8_strwidth()`, `utf8_strnwidth()`, `cast_size_t_to_int()` (unchanged, with documented rationale).
- Behavior: unchanged. The patch is purely documentation.
- Documentation: The new comment in `utf8.c` explains:
  - Why the functions return `int` (alignment with `printf()`-style APIs for padding calculations).
  - Why `cast_size_t_to_int()` is necessary (overflow prevention). The comment will explicitly cite the original commit (937b7c8b) and its crash-fix rationale, per Phillip Wood’s request.

Open questions and loose ends: none. The last remaining action item (referencing commit 937b7c8b) is acknowledged by the author and will be addressed in the next revision. No further discussion or objections are expected.

Participant positions:
- **Phillip Wood**: **substantive, high-weight review** of v4 and v5. Requested the commit message and in-code comment explicitly cite the original commit (937b7c8b) and its crash-fix rationale. His feedback is now acknowledged and will be implemented.
- **Hardik Kumar**: author. The v5 patch implements the consensus to document the rationale and will incorporate the reference to 937b7c8b in the next revision. The patch is otherwise complete.
- **Junio C Hamano**: **substantive, high-weight review in v2** endorsing the documentation approach. His feedback is resolved by the v5 patch.
- **Pablo Sabater**: **substantive, high-weight review in v2** resolved by withdrawal and documentation.
- **René Scharfe**: substantive review in v1 resolved by withdrawal and documentation.

Related work: none. This documentation patch concludes the thread. No further technical discussion is expected after the next revision.