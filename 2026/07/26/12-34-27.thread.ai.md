Thread identity: standalone documentation patch by Hardik Kumar concluding the UTF-8 string-width type-refactoring thread. The patch (v5) updates `utf8.c` to document the rationale for `utf8_strwidth()` and `utf8_strnwidth()` returning `int` (alignment with `printf()`-style APIs) and the necessity of `cast_size_t_to_int()` for overflow prevention. The series is now at **v5**, replacing the withdrawn v1–v3 refactoring attempts.

Core problem or goal: document the design rationale for the existing `int` return type of UTF-8 string-width functions, resolving the earlier debate about type-system cleanup. The patch clarifies why the functions return `int` (consistency with `printf()`-style APIs for padding calculations, e.g., `printf("%*s", width, string)` requiring `int`) and why `cast_size_t_to_int()` is required (overflow prevention when converting `size_t` to `int`). The goal is purely explanatory, with no code behavior changes.

Current status: **under review, ready for final revision**. Hardik Kumar’s v5 patch addresses Phillip Wood’s and Junio C Hamano’s feedback from v4 by explicitly stating the overflow prevention rationale, the `printf()`-style API alignment, and the crash-fix context from commit 937b7c8b in both the commit message and in-code comment. The sole remaining action item is now **resolved in principle**: Hardik confirms he will add the reference to 937b7c8b and refine the comment to match Junio’s suggested template. The patch is otherwise uncontroversial and aligns with the thread’s consensus to document the status quo.

Key technical details:
- Files: `utf8.c` (updated). The patch removes the TODO comment and adds a detailed rationale explaining:
  - Why the functions return `int` (alignment with `printf()`-style APIs for padding calculations, e.g., `printf("%*s", width, string)`).
  - Why `cast_size_t_to_int()` is necessary (overflow prevention, not just signature alignment).
  - The original crash-fix rationale from commit 937b7c8b, which introduced `cast_size_t_to_int()`.
- Functions: `utf8_strwidth()`, `utf8_strnwidth()`, `cast_size_t_to_int()` (unchanged, with documented rationale).
- Behavior: unchanged. The patch is purely documentation.
- Documentation: The new comment in `utf8.c` will explicitly cite commit 937b7c8b and its crash-fix context, per Phillip Wood’s and Junio’s requests.

Open questions and loose ends: none. The last remaining action item (referencing commit 937b7c8b and refining the comment) is acknowledged by the author and will be addressed in the next revision. No further discussion or objections are expected.

Participant positions:
- **Junio C Hamano**: **substantive, high-weight review** of v5. Provided a detailed template for the in-code comment, emphasizing the `printf()`-style API alignment and the safety guarantees of `cast_size_t_to_int()`. His feedback is now the basis for the final revision.
- **Phillip Wood**: **substantive, high-weight review** of v4 and v5. Requested the commit message and in-code comment explicitly cite commit 937b7c8b and its crash-fix rationale. His feedback is now acknowledged and will be implemented.
- **Hardik Kumar**: author. The v5 patch implements the consensus to document the rationale and will incorporate the reference to 937b7c8b and Junio’s comment template in the next revision. The patch is otherwise complete.
- **Pablo Sabater**: **substantive, high-weight review in v2** resolved by withdrawal and documentation.
- **René Scharfe**: substantive review in v1 resolved by withdrawal and documentation.

Related work: none. This documentation patch concludes the thread. No further technical discussion is expected after the next revision.