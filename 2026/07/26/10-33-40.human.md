This is a **documentation and correctness follow-up** in the **ready-to-merge v6 series** optimizing `paint_down_to_common()` for one-sided histories. The series is already queued in Junio’s tree and procedurally unblocked, but this exchange addresses **two precision gaps** in the technical document (`Documentation/technical/paint-down-to-common.adoc`) that Elijah Newren flagged in his review.

Kristofer Karlsson acknowledges the issues and proposes fixes. First, he agrees that **v1 commit-graphs** (where generation numbers saturate at `GENERATION_NUMBER_V1_MAX`) can violate topological ordering, just like the `INFINITY` region. He sketches two approaches to handle this: remapping `V1_MAX` to `INFINITY` at load time (simpler but conflates two distinct concepts) or introducing a new helper function (`commit_graph_generation_topo_ceiling()`) to keep the semantics separate (cleaner but slightly more intrusive). He leans toward the latter for long-term clarity, though either would work.

Second, he rewrites the `min_generation` documentation to clarify that a non-zero cutoff **only applies to reachability checks among input commits** and cannot guarantee a complete `MERGE_BASE_FIND_ALL` result. This prevents future callers from misusing the cutoff in contexts where deeper merge bases are needed.

The discussion is **non-blocking**—the series is technically complete and queued—but these tweaks will improve the document’s accuracy and forward compatibility. No code changes are required, only updates to the technical document. The exchange reflects the series’ high quality: even after being marked ready-to-merge, the author and reviewers are still refining edge cases to ensure long-term maintainability.