This is the second patch in a well-crafted test infrastructure series introducing the `commit_body()` helper to the Git test suite. The patch mechanically converts 60 existing call sites across 11 test scripts from the error-prone `git cat-file commit | sed -e '1,/^$/d'` idiom to the new helper, eliminating the exit-code masking bug that motivated the series.

The conversion is thorough and careful: the author collapses four equivalent spellings of the same idiom into a single function call, handles two special cases (one where a grep can now run against the stripped body, another where piping into `iconv` would reintroduce the exit-code hole), and leaves unrelated sed invocations untouched. The commit message clearly explains the reasoning behind each adjustment, and the diff is clean and focused.

All converted tests pass, and the series as a whole is already well-reviewed in principle (Junio Hamano identified the exit-code hole that this series fixes). This patch is the payoff: it delivers the promised cleanup and correctness improvement across the test suite. The changes are purely internal and have no user-visible impact. If the first patch is accepted, this one should follow without controversy.