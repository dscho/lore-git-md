This is a brief follow-up in the ongoing thread about test failures under `WITH_BREAKING_CHANGES=1`. Jeff King (Peff) replies to Junio C Hamano’s acknowledgment of his two-patch series fixing the `t0014-alias.sh` failures. Peff clarifies the intent behind the `HAVE_DEPRECATED` prereq: it ensures the alias-loop tests remain meaningful only when at least two deprecated commands are available, avoiding false positives when deprecated commands are removed or stale build artifacts interfere.

He suggests the two patches could be squashed for simplicity, using the second patch’s commit message, but leaves the decision to Junio. Peff also notes that while the alias-loop test *could* be rewritten to work with a single deprecated command, the effort isn’t justified given the current state (two commands now, zero planned for Git 3.0). The exchange is procedural and confirms the fix’s direction, with no new technical concerns raised. The remaining `t1517-outside-repo.sh` failures—still tied to stale build artifacts—are not addressed here.