This is a **bugfix** for `git cat-file --batch-command`. Jeff King (Peff) submits the formal patch that resolves the assertion failure Alan Stokes reported when the `--batch-command` format string omits `%(objecttype)` and a `contents` command is issued. The patch implements the targeted fix discussed earlier: it saves and restores `data->info.typep` around the `contents` command to avoid over-fetching type metadata for subsequent `info` commands, preserving performance.

The patch touches `builtin/cat-file.c` and adds a test in `t1006-cat-file.sh` that verifies the crash no longer occurs. The test sends a `contents` command with a format string lacking `%(objecttype)` and confirms the output matches the expected behavior from `--batch`. The fix is minimal, well-motivated, and avoids regressing performance for lightweight `info` requests.

This patch should be uncontroversial and ready for `next` once reviewed. It addresses a deterministic crash in a core plumbing command and includes test coverage, making it a clear candidate for merging. The thread has been in a holding pattern awaiting this submission, so expect quick convergence.