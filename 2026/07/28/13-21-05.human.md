This is a bugfix discussion targeting the sequencer subsystem, specifically the cleanup logic for `REBASE_HEAD` after a rebase completes. Phillip Wood, a well-regarded contributor with deep sequencer expertise, weighs in to confirm that the leftover `REBASE_HEAD` file is indeed a bug—though not a critical one. His analysis clarifies the root cause: the sequencer deletes `REBASE_HEAD` before processing each rebase command but fails to clean it up after the final command, leaving it behind in scenarios like `squash` or `edit` as the last action.

The thread now has a clear technical consensus. Phillip’s explanation resolves the earlier ambiguity about whether this behavior was intentional or problematic. He also provides authoritative guidance on how to reliably detect a rebase in progress: scripts should check for the `rebase-merge` or `rebase-apply` directories rather than relying on `REBASE_HEAD`, which is only present during conflicts or when the editor is open for rewording. This aligns with Matt Hunter’s workaround and underscores that `REBASE_HEAD` was never meant to be a general-purpose indicator of rebase state.

The discussion remains in the diagnostic phase, with no patch yet proposed. However, the issue is now well-scoped: the fix will likely involve ensuring `REBASE_HEAD` is deleted after the final rebase command, regardless of the action type. The sequencer’s cleanup logic is the obvious place to look, and Phillip’s familiarity with the codebase suggests he may drive the fix forward. For now, the thread is a useful reference for anyone encountering spurious `REBASE_HEAD` files or writing scripts that need to detect rebase state.