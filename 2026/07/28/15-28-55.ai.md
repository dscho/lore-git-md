This is a follow-up in the ongoing bug report about leftover `REBASE_HEAD` state after a successful rebase. Long 76 reiterates his position that the behavior is a bug, not an intentional design choice. He points to Git’s consistent cleanup of other temporary HEAD files (like those for `merge` or `cherry-pick`) as precedent, arguing that `REBASE_HEAD` should be removed after rebase completion just like those files are. He also cites the `--show-current-patch` documentation, which implies `REBASE_HEAD` should exist only when a rebase is actively in progress (e.g., during conflicts or editor pauses), not after it finishes.

The email does not introduce new technical details or propose a fix, but it strengthens the case for treating this as a bug by aligning with Phillip Wood’s earlier assessment. Long 76 acknowledges Matt Hunter’s workaround (checking for `rebase-merge` or `rebase-apply` directories instead of `REBASE_HEAD`) but does not engage with the inconsistency Hunter noted around the `break` command or the specific rebase actions that trigger the issue. The thread remains focused on confirming the bug’s existence and expected behavior, with no patch yet proposed. The key question—whether this is a regression or a long-standing oversight—remains unresolved.