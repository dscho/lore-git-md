This is a **sequencer bugfix discussion** targeting the cleanup logic for `REBASE_HEAD` after rebase operations. Phillip Wood, a domain expert on the sequencer and rebase internals, weighs in with a clear technical assessment: the leftover `REBASE_HEAD` file after a successful rebase is indeed a bug, though not a critical one. His analysis pinpoints the root cause: the sequencer deletes `REBASE_HEAD` *before* processing each rebase command but fails to clean it up after the final command completes.

The email advances the thread significantly by clarifying the expected behavior of `REBASE_HEAD`. Phillip explains that `REBASE_HEAD` is only meant to exist during *active* rebase operations—specifically when there are conflicts or when the editor is open for rewording a commit. It should *not* persist after the rebase finishes, regardless of the final action (squash, edit, reword, etc.). He also resolves the inconsistency Matt Hunter observed with the `break` command: `REBASE_HEAD` is intentionally absent when the rebase stops on a `break` or failed `exec` command, as those states are not considered "conflict" scenarios.

Phillip’s response implicitly rejects Hunter’s suggestion that the behavior might be intentional, and he endorses Hunter’s workaround (checking for `rebase-merge` or `rebase-apply` directories) as the correct way to detect an in-progress rebase. The discussion now has a clear technical direction: the sequencer’s cleanup logic needs to be adjusted to remove `REBASE_HEAD` after the final rebase command, regardless of the command type. No patch has been proposed yet, but the root cause and expected fix are now well-defined. The subsystem involved is the sequencer’s state management, specifically the cleanup phase in `sequencer.c`.