This is a **bugfix** patch for the `git cat-file --batch-command` assertion failure. Jeff King (Peff) submits the formal patch that implements the targeted fix previously agreed upon in the thread: saving and restoring `data->info.typep` around the `contents` command to avoid a NULL dereference when `%(objecttype)` is omitted from the format string. The patch includes a new test in `t1006-cat-file.sh` that verifies the crash no longer occurs and that the output matches the `--batch` mode.

The fix is minimal and surgical, touching only `builtin/cat-file.c` and the test suite. It preserves performance for `info` commands by restoring the original `typep` value after processing `contents`, ensuring no unnecessary type metadata is fetched. The test case is straightforward and covers the reported edge case. This patch should be uncontroversial and ready for `next` once reviewed. The thread has now moved from discussion to implementation, with the solution fully aligned with earlier feedback from Alan Stokes and Pablo Sabater.