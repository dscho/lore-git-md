This thread introduces `git add --resolved`, a new option designed to help users stage only those files from which conflict markers have been removed after a merge. The feature is now in its second iteration, with all prior technical feedback addressed—including fixes for option incompatibility logic, test script discoverability, and portability. The series also consolidates duplicate conflict-marker detection code and adds a performance tweak for binary files.

The current discussion centers on the command’s interface. Junio, the author, defends the monolithic `--resolved` design as workflow-oriented, arguing it clearly signals the user’s intent to stage resolved conflicts. Michael Montalbo, the reviewer, proposes an alternative: a more general `--unmerged` selector with explicit policy flags like `--allow-conflict-markers`, which he believes would better reflect the underlying implementation and offer greater flexibility. Junio sees little practical need for such flags, suggesting users can manually stage exceptions if needed, and remains unconvinced of the value of `--skip-conflict-markers`.

The series itself is technically ready, but this naming debate could shape the final form of the feature. No other objections or open questions remain, and the discussion is now focused on balancing clarity with flexibility in the user interface.