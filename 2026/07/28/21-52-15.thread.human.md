The `git add --resolved` feature is shaping up as a practical tool for conflict resolution, now in its third iteration. Junio Hamano’s latest update tidies up a misindented block in `read-cache.c`—a minor but necessary cleanup that paves the way for the rest of the series. The core debate remains unchanged: Junio’s workflow-oriented `--resolved` option, which stages only files with resolved conflicts, versus Michael Montalbo’s proposal for a more flexible `--unmerged` selector with explicit policy flags. All technical feedback from Montalbo’s earlier review has been addressed, including a logic fix for option incompatibility and a portability tweak in the test script.

The implementation itself is technically sound, with helpers like `is_conflict_marker_line()` and `remove_file_from_index_with_flags()` consolidating duplicate logic and adding support for dry-run and verbose modes. Binary files and deletions are handled efficiently, and the stricter conflict-marker rule (requiring a space after `<<<<<<<` and `>>>>>>>`) is now consistent across all callers. The series is under review, but the conceptual divide over the user interface lingers—Junio sees `--resolved` as a natural extension of Git’s existing conventions, while Montalbo argues for a design that better reflects the underlying mechanics. For now, the ball is in the reviewers’ court to weigh in on that design choice.