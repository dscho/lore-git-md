This is a test patch for the `git merge-base` command. Junio C Hamano has queued it for integration, signaling that the contribution is uncontroversial and ready for inclusion.

The patch adds comprehensive test coverage for the `--is-ancestor` option in `t/t6010-merge-base.sh`, which previously had none. It verifies the command's exit codes (0 for ancestor, 1 for non-ancestor, 128 for invalid arguments), ensures `--is-ancestor` and `--all` cannot be combined, and checks the error message explicitly names both options. The tests cover edge cases like self-ancestry, diverged commits, and non-existent commits using a small three-commit repository.

Key technical details include the test file `t/t6010-merge-base.sh`, the `--is-ancestor` option, and the exit codes 0/1/128. The patch is self-contained, touching only the test suite without modifying production code. The setup and test cases follow the existing style in the file, making it a straightforward addition.

This addresses a clear gap in test coverage for a widely used script-facing option. The patch is well-scoped, well-explained, and unlikely to generate discussion. Future follow-ups might reference the test file, the `--is-ancestor` option, or the specific error message about mutually exclusive options.