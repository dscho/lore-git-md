This thread is about test failures that appear when building Git with `WITH_BREAKING_CHANGES=1`, a configuration flag expected to see more use as the project prepares for Git 3.0. Brian m. carlson reported that two test scripts—`t0014-alias.sh` (tests 4 and 8) and `t1517-outside-repo.sh` (tests 248 and 249)—fail under this configuration, but only when combined with `DEVELOPER=1`, `WITH_RUST=1`, and `CC=clang`. The failures don’t occur without the flag, suggesting they’re directly tied to the breaking changes it introduces.

Junio C Hamano followed up to say he can’t reproduce the failures in his local builds across all integration branches (`maint`, `master`, `next`, and `seen`). This suggests the issue might be specific to Brian’s environment or build setup, though no root cause has been identified yet. The thread is still in the diagnostic phase, with no proposed fixes or deeper explanations. While Brian mentioned an upcoming patch series that depends on `WITH_BREAKING_CHANGES`, the failures aren’t currently blocking any integration work, and the CI job for this configuration hasn’t been flagged as problematic. For now, the focus is on understanding why the failures appear in some setups but not others.