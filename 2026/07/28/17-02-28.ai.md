This is a documentation patch that concludes the UTF-8 string-width type-refactoring thread. Hardik Kumar’s v5 patch replaces the TODO comment in `utf8.c` with a concise rationale explaining why `utf8_strwidth()` and `utf8_strnwidth()` return `int` (alignment with `printf()`-style APIs for padding calculations) and why `cast_size_t_to_int()` is necessary (overflow prevention). The patch addresses Phillip Wood’s review feedback from v4 by explicitly stating the overflow prevention rationale in both the commit message and in-code comment, though it does not yet reference the original commit (937b7c8b) that introduced `cast_size_t_to_int()` to fix a crash.

The patch is uncontroversial and aligns with the thread’s consensus to document the status quo rather than change the return type. It touches only `utf8.c`, removing the TODO and adding a three-line comment that clarifies the design constraints. The commit message now provides sufficient context about the patch’s purpose, though it could still benefit from the requested reference to 937b7c8b for completeness. No code behavior is altered—this is purely a documentation update to explain the existing design’s rationale after the earlier refactoring attempt was withdrawn.