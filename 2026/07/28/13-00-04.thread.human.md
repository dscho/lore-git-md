This thread is about a small but long-standing memory leak in `git merge` when the `merge.defaultToUpstream` configuration is enabled. Toon Claes sent a patch that fixes the leak by ensuring an allocated argument array is properly freed. Jeff King reviewed the patch, confirmed the leak and the fix, and noted the surrounding code is awkward but not worth refactoring for this change.

Junio C Hamano has now queued the patch for integration, so it is cooking in the `seen` branch and will likely move to `next` in the next cycle. The fix is minimal, well-tested, and uncontroversial—no further discussion or objections have emerged. The thread is effectively closed with Junio’s approval. The only loose end is the code’s messiness, but as Peff observed, that doesn’t block this fix.