This is a documentation patch that wraps up the long-running discussion about the return type of UTF-8 string-width functions. Hardik Kumar sends a brief reply to Phillip Wood’s review of v4, acknowledging that the commit message and in-code comment need to explicitly cite the original crash-fix commit (937b7c8b) that introduced `cast_size_t_to_int()`. The patch itself is otherwise ready; the only remaining work is to add the requested reference to the commit that motivated the current design. The thread is now a single action item away from completion.