This is the final exchange in Jeff King’s two-patch series that fixes the `t0014-alias.sh` test failures under `WITH_BREAKING_CHANGES=1`. Peff acknowledges Junio’s explanation of the `HAVE_DEPRECATED` prereq—it skips tests that require at least two deprecated commands to exercise alias-loop detection—and agrees that the two patches could be squashed for clarity, though he leaves the decision to Junio. He briefly considers whether the looping-alias test could be rewritten to work with a single deprecated command but dismisses it as unnecessary given the current two-command state and the eventual removal of all deprecated commands in Git 3.0. The discussion is now settled: the fix is complete, well-motivated, and unlikely to need further revision. The remaining `t1517-outside-repo.sh` failures (also caused by stale build artifacts) are acknowledged as a separate issue not addressed by this series.