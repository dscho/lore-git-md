This is the first patch in Junio C Hamano’s four-patch series introducing `git add --resolved`. It consolidates duplicate conflict-marker detection logic from `diff.c` and `rerere.c` into a single helper, `is_conflict_marker_line()`, in `merge-ll.c`. The new helper enforces a stricter rule than the original `diff.c` version: it requires a space character after `<<<<<<<` and `>>>>>>>` markers, matching the behavior of Git’s conflict output and the existing `rerere.c` implementation.

The patch removes 25 lines of duplicate code from `diff.c` and 33 from `rerere.c`, replacing them with a single 31-line implementation in `merge-ll.c`. The new helper returns the marker character (`<`, `=`, `>`, or `|`) on success, which allows callers to distinguish between different marker types without additional parsing. This change touches four files: `merge-ll.c` (new helper), `merge-ll.h` (declaration), `diff.c` (replacement), and `rerere.c` (replacement).

The commit message clearly explains the motivation: eliminating redundant logic and adopting a consistent, stricter rule for conflict-marker detection. The patch is a refactoring with no user-visible behavior change, but it lays the groundwork for the `--resolved` option in the third patch by centralizing the logic that identifies resolved conflicts. The stricter rule is intentional and aligns with Git’s conflict output format, so reviewers should focus on whether the consolidation is complete and whether the new helper’s behavior is correct for all callers. The test script introduced in the third patch (`t/t2207-add-resolved.sh`) will likely validate this logic indirectly.