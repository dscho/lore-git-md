This is a **substantive review** of Toon Claes’s memory-leak fix in `git merge`. Jeff King (Peff) confirms the leak’s existence and the correctness of the fix, but notes the surrounding code’s awkwardness—specifically the way `argv` is manipulated after `setup_with_upstream()` allocates it. He observes that the current implementation overwrites `argv[0]`, which complicates memory management, and suggests that a cleaner design would use a separate `strvec` for the heads. However, he stops short of requesting a refactor, calling it “micro-polishing” that isn’t worth the effort.

The review is brief but technically precise, focusing on the code’s behavior rather than surface details. Peff’s feedback carries weight due to his deep familiarity with Git’s internals, and his acknowledgment of the fix’s correctness signals that the patch is on solid ground. The only open question is whether the project will tolerate the existing code’s messiness or eventually revisit it, but for now the fix stands as a minimal, well-motivated correction. No further discussion or objections are evident in this reply.