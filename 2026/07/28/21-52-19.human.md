This is the final patch in Junio C Hamano’s four-part series introducing `git add --resolved`. It is a pure refactoring patch that reindents code in `read-cache.c` to correct inconsistent whitespace introduced by an earlier commit (a16c4a245a, February 2026).

The patch targets the `skip_submodule()` helper function, which determines whether a submodule with `ignore=all` configuration should be skipped during `git add`. The original indentation was irregular, mixing tabs and spaces in a way that made the code harder to read. This patch realigns the function’s body to match the surrounding style, using hard tabs for indentation and ensuring consistent alignment of parameters and nested blocks. No logic changes are made—only whitespace is adjusted.

The commit message is concise and factual, explaining the origin of the misindentation and the purpose of the fix. The patch itself is small (36 insertions, 34 deletions) and touches only one file. Since it is purely mechanical, it is unlikely to be controversial, though reviewers may verify that the reindentation is correct and that no unintended changes were introduced.

This patch completes the series, which is now fully described and ready for review. The earlier patches laid the groundwork for `git add --resolved` by consolidating conflict-marker detection logic and introducing helper functions; this one tidies up a minor but visible inconsistency in the codebase. The series as a whole appears well-structured and motivated, with clear commit messages and thorough test coverage.