Thread identity: patch series (single patch), subject "merge: plug memory leak in default-to-upstream path", originating author Toon Claes.

Core problem or goal: fix a long-standing memory leak in `git merge` when `merge.defaultToUpstream` is enabled and the command is invoked without arguments. The leak occurs in `builtin/merge.c` when `setup_with_upstream()` allocates an argument array that is never freed.

Current status: patch posted (v1), under review. Jeff King (Peff) has reviewed, confirming the leak and the fix's correctness, but notes the surrounding code's awkwardness in handling `argv` manipulation. No refactor requested; the fix is considered sufficient for now.

Key technical details:
- Files touched: `builtin/merge.c`, `t/t7600-merge.sh`.
- New variable: `argv_to_free` in `cmd_merge()` to track the allocated array.
- Behavior change: none; the fix is purely about memory hygiene.
- Test coverage: new test in `t/t7600-merge.sh` exercises the default-to-upstream path, confirming the leak and the fix under leak sanitizer.
- Subsystem: merge.
- Code design note: `setup_with_upstream()` allocates an `argv` array that is later overwritten at `argv[0]`, complicating memory management. Peff suggests a cleaner design would use a separate `strvec` for heads but deems this "micro-polishing" not worth the effort at this stage.

Open questions and loose ends:
- Whether the project will tolerate the existing code's messiness long-term (no action required for this patch).

Participant positions:
- **Jeff King (Peff)**: substantive review; confirms leak and fix correctness, notes code awkwardness but does not request changes. Position: supportive of the fix as-is. Review was substantive (engaged with behavior and memory management).