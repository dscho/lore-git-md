This is a **test infrastructure refactoring patch** in response to the ongoing discussion about test failures with `WITH_BREAKING_CHANGES=1`. Junio C Hamano replies to Jeff King’s first patch in a two-part series, which factors out the hardcoded names of deprecated commands (`whatchanged` and `pack-redundant`) into variables (`deprecated1` and `deprecated2`) in `t/t0014-alias.sh`.

Junio’s feedback is mild but perceptive: he initially questions the value of parameterizing the command names while still hardcoding the fact that there are exactly two deprecated commands. His concern is that the commit message might mislead readers into thinking the test is now more flexible than it actually is. However, he acknowledges that the real purpose—preparing for dynamic command list generation in the next patch—will become clear once he sees the follow-up. The exchange is collegial and forward-looking, with no disagreement or controversy.

The patch itself is mechanical and uncontroversial, replacing direct references to `whatchanged` and `pack-redundant` with variables throughout the test script. While the diff is noisy due to repeated substitutions, the logic remains unchanged. This refactoring sets the stage for the second patch, which will generate the list of deprecated commands dynamically using `git --list-cmds=deprecated`, making the test resilient to future changes in the set of deprecated commands.

For a busy reader, this patch is worth noting only as part of the broader effort to stabilize tests in the `WITH_BREAKING_CHANGES` configuration. The real fix comes in the next patch, but this one is a necessary and well-motivated preparatory step. No action is needed beyond reviewing the follow-up, which directly addresses the test failures reported earlier in the thread.