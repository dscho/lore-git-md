This is the first patch in Junio C Hamano’s four-part series introducing `git add --resolved`. It consolidates duplicate conflict-marker detection logic into a single helper function, `is_conflict_marker_line()`, in `merge-ll.c`.

The patch replaces two near-identical implementations—one in `diff.c` (used by `git diff --check`) and another in `rerere.c`—with a shared helper. The new version adopts the stricter rule from `rerere.c`, requiring a space after `<<<<<<<` and `>>>>>>>` markers, which matches Git’s own conflict output. The change is purely internal and does not alter user-visible behavior, but it paves the way for the `--resolved` option in later patches by ensuring consistent conflict-marker detection across subsystems.

The patch is clean and well-motivated: the commit message clearly explains the duplication and the rationale for unifying the logic. The diff is straightforward, removing redundant code while preserving functionality. No new edge cases are introduced, and the stricter rule is justified by Git’s own conflict-marker format. This refactoring looks like a solid foundation for the rest of the series.