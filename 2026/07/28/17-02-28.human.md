This is a documentation patch that concludes the UTF-8 string-width refactoring thread. Hardik Kumar’s v5 patch replaces the TODO comment in `utf8.c` with a clear explanation of why `utf8_strwidth()` and `utf8_strnwidth()` return `int` instead of `size_t`: the functions are used in contexts (padding, `printf()` calls) that expect signed integers, and the explicit cast via `cast_size_t_to_int()` prevents integer overflow crashes. The commit message now explicitly states the overflow rationale, addressing Phillip Wood’s review feedback from v4.

The patch is uncontroversial and aligns with the thread’s consensus to document the status quo rather than change the return type. It touches only `utf8.c`, removing the outdated TODO and adding a concise rationale. No code behavior is altered, and the change is purely explanatory. This version appears ready for merging, pending any final nitpicks on wording. The thread’s earlier debate—whether to refactor the return type—is now resolved in favor of documentation.