This is a test patch for the `git merge-base` command. Junio C Hamano has queued it for integration, signaling that it is uncontroversial and ready to land.

The patch adds comprehensive test coverage for the `--is-ancestor` option in `t/t6010-merge-base.sh`, which previously had none. It verifies the expected exit codes (0 when the first commit is an ancestor of the second, 1 when it is not, and 128 for invalid arguments) and confirms that `--is-ancestor` and `--all` cannot be used together. The tests also cover edge cases like self-ancestry, diverged commits, and non-existent commits, using a small repository with three commits to exercise these scenarios.

The contribution is well-scoped and self-contained: it only adds tests, does not modify production code, and follows the existing test style. The commit message clearly explains the motivation and scope, and the patch itself is clean and focused. Since `--is-ancestor` is widely used in scripts, having proper test coverage is a clear improvement. There are no open questions or loose ends, and the patch appears to be a straightforward, low-risk addition.