This is a brief acknowledgment from brian m. carlson confirming that Jeff King’s two-patch series fixing the `t0014-alias.sh` test failures under `WITH_BREAKING_CHANGES=1` looks sensible. Brian had intended to investigate the issue further but appreciates Peff’s proactive submission of patches. The message adds no new technical details or objections; it simply signals agreement with the approach and gratitude for the quick resolution.

The thread remains settled: the root cause (stale build artifacts triggering alias-loop detection) is fully addressed for `t0014-alias.sh` by Peff’s series, which dynamically generates the list of deprecated commands and skips the tests if none are available. The `t1517-outside-repo.sh` failures, also caused by stale artifacts, are acknowledged as a separate issue requiring only a clean build to resolve. No further discussion or changes are expected.