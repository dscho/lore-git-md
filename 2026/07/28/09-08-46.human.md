This is a **bugfix** for the `git cat-file --batch-command` assertion failure that was reported earlier. Alan Stokes, the original reporter, acknowledges Pablo Sabater’s tested fix (which implements Jeff King’s suggestion to save and restore `data->info.typep` around the `contents` command) as "pretty simple and correct." The fix avoids the crash without regressing performance for lightweight `info` requests.

Alan also signals his intent to eventually submit a formal patch with test coverage in `t1006-cat-file.sh`, but notes he is new to the email-based patch workflow and may not have time for a month or two. This leaves the door open for Pablo (or anyone else) to pick up the patch submission if they want to move it forward sooner. The only remaining loose end is the missing test case; the code fix itself is settled and uncontroversial. Expect this to land in `next` once tests are added.