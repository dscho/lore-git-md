Existing AI thread summary (absent, this is the thread root):

Thread identity: Performance inquiry regarding `fetch.bundleURI` slowness when cloning the Linux kernel repository. Originating author: Ryszard Knop. Thread type: bug report / performance investigation (no patch series).

Core problem or goal: Determine why `fetch.bundleURI`-based clones of the Linux kernel repository are unexpectedly slow—comparable to or slower than standard full clones—despite the intended performance benefit of using pre-generated bundles. The inquiry seeks to establish whether this is expected behavior, a known limitation, or a potential bug in the bundle-fetching implementation.

Current status: Open inquiry; no decisions, resolutions, or patch versions yet. The author has observed the issue in a CI workflow and provided profiling data (`GIT_TRACE` output) showing `git index-pack` as the bottleneck, consuming 100% CPU on a single thread for most of the operation.

Key technical details:
- Feature in question: `fetch.bundleURI`, a Git configuration option enabling the use of pre-generated bundles as a source for clone/fetch operations.
- Repository: Linux kernel (large, monolithic repository).
- Workflow: Initialize a new repository, configure `fetch.bundleURI` to point to a bundle, then fetch a specific reference (branch, tag, or commit).
- Bottleneck: `git index-pack` running single-threaded, consuming the majority of the 4–5 minute clone time.
- Alternative approach mentioned: Packaging and working with a full bare repository (currently more performant but less elegant).
- No files, subsystems, or symbols explicitly identified as problematic; the issue appears to be in the bundle-fetching or index-pack pipeline.

Open questions and loose ends:
- Is the observed slowness expected behavior for `fetch.bundleURI` with large repositories, or is it a bug?
- If expected, what are the known limitations of the feature that explain the performance characteristics?
- If a bug, what part of the bundle-fetching or index-pack logic is responsible, and what optimizations (e.g., parallelization, incremental processing) might address it?
- Are there alternative approaches or configurations (e.g., bundle generation parameters, repository preparation) that could improve performance for this use case?

Participant positions: None yet; only the originating author (Ryszard Knop) has contributed. No reviews, acks, or objections recorded.

Related work: None mentioned in the email. The inquiry may later reference prior work on bundle performance, `index-pack` optimizations, or CI workflows for large repositories.