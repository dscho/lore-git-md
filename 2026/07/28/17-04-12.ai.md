This is a **documentation patch** that updates the comment in `utf8.c` to explain why `utf8_strwidth()` and `utf8_strnwidth()` return `int` and why `cast_size_t_to_int()` is necessary. The patch is part of the final polish for the UTF-8 string-width refactoring thread, now at **v5**.

Hardik Kumar acknowledges Phillip Wood’s feedback from the v4 review, which pointed out that the commit message and in-code comment did not adequately explain the *core* rationale: preventing overflow crashes caused by implicit `size_t`→`int` conversion. The original commit (937b7c8b) introduced `cast_size_t_to_int()` to fix such a crash, and Phillip requested that this be explicitly cited in both the commit message and the comment. Hardik confirms he has read the commit and will revise the patch to address this.

The patch itself is uncontroversial and aligns with the thread’s consensus to document the existing design rather than refactor it. The only remaining action is to incorporate the reference to 937b7c8b, after which the patch should be ready for merging. No further technical debate is expected; this is a straightforward documentation update to clarify a low-level design decision. The functions’ behavior remains unchanged.