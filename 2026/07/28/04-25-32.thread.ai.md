Thread identity: RFC patch introducing `pack.allowDuplicateObjects` configuration and `--[no-]allow-duplicate-objects` flag for `git index-pack`, targeting the packfile handling subsystem. Series version: RFC (single patch). Originating author: friel@openai.com. Thread type: RFC patch series.

Core problem or goal: Allow Git to accept packfiles containing duplicate objects during `git index-pack` when explicitly configured to do so, addressing a performance bottleneck for servers that stream objects from overlapping existing packs. The feature is opt-in (default `false`) and preserves all other validation checks, including strict mode rejection of duplicates.

Current status: Under initial review as an RFC. No prior versions or integration status (not in `pu`, `seen`, `next`, or `master`). The patch is self-contained and ready for substantive technical review.

Key technical details:
- **Files touched**: `builtin/index-pack.c`, `Documentation/git-index-pack.txt`, `Documentation/config/pack.txt`, `t/t5308-pack-detect-duplicates.sh` (new test file).
- **New symbols**: Configuration knob `pack.allowDuplicateObjects` (boolean, default `false`), command-line flag `--[no-]allow-duplicate-objects` for `git index-pack`.
- **Behavior**: When enabled, `git index-pack` accepts packs with duplicate objects during validation, but `--strict`/`--verify` flags override this and continue to reject duplicates. Connectivity checks and object validation (e.g., `fsck-objects`) remain unchanged.
- **On-disk format**: No changes; duplicates are only allowed during pack validation and do not affect delta resolution or storage.
- **Test coverage**: Extensive, including configuration overrides, integration with `git clone` (full, shallow, and partial clones), and edge cases like connectivity checks.

Open questions and loose ends:
- The RFC tag indicates the author is seeking feedback on the approach, though the implementation is conservative and unlikely to be controversial.
- No objections or requests for changes have been raised yet (initial post).

Participant positions: None recorded yet (initial post).

Related work: Builds on the existing topic `tb/pack-with-duplicates`, though no direct dependency is stated. The feature is motivated by real-world use cases where streaming packs from overlapping sources is more efficient than deduplicating at request time.