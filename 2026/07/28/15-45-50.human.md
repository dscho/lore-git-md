This is the v8 iteration of the feature addition that teaches `git replay` a `--linearize` option to flatten merge commits into a linear history. The series is now **technically complete** and addresses the last major design concern: **disallowing multiple revision ranges with `--linearize`** to prevent unintended concatenation of unrelated histories. This resolves the "multi-branch ambiguity" that has been the primary blocker since v7, adopting Elijah Newren’s "minimal fix" approach (restricting `--linearize` to a single positive ref) while leaving the door open for future per-branch linearization.

The series consists of three patches. The first two are internal refactorings that introduce helper functions (`put_mapped_commit`, `get_mapped_commit`) and move replay-base resolution logic out of `pick_regular_commit()` for clarity. The third patch implements `--linearize`, which skips merge commits and reparents subsequent commits onto the previous non-merge commit, ensuring a single linear history. The commit message and documentation now explicitly forbid combining `--linearize` with multiple positive revisions or `--contained`, with clear error messages and tests to enforce these restrictions. The behavioral difference from `git rebase --no-rebase-merges` (which replays both sides of a merge) is documented and tested.

Key changes in v8:
- **Disallowed multiple revision ranges with `--linearize`**, addressing the core ambiguity raised by Elijah and Junio. The implementation now errors out if `--linearize` is used with more than one positive ref, aligning with `--advance`/`--revert` behavior.
- **Disallowed `--contained` with `--linearize`**, as both options imply emergent reachability concerns. This is a proactive restriction pending a clearer design for future `--contained` support.
- **Updated documentation** to reflect the new restrictions and clarify the intended use case (linearizing a single branch at a time).
- **Added tests** to verify the new error conditions, including cases for multiple revision ranges and `--contained` incompatibility.

The series is now **unblocked for merging** and appears ready for `master`. The remaining design questions (per-branch linearization, `--contained` compatibility) are deferred to future work, and the current implementation is narrowly scoped to avoid ambiguity. The code is well-tested, with thorough coverage of edge cases (root commits, merge handling, `--revert` compatibility) and clear documentation. Junio’s earlier concerns about partial merge ranges are addressed by the mutual exclusivity with `--revert` (removed in v7) and the new restrictions in v8. The series is based on `13c7afec212f` and has a `Change-Id` of `20260604-toon-git-replay-drop-merges-807fa008d395`. Potential trivial conflicts with Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series are noted but expected to be minor.