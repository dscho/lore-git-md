This is a **documentation patch** that concludes the UTF-8 string-width refactoring discussion. Phillip Wood reviews Hardik Kumar’s v4 patch, which replaces the TODO comment in `utf8.c` with a rationale for why `utf8_strwidth()` and `utf8_strnwidth()` return `int` instead of `size_t`. The review is **substantive**: Phillip critiques the commit message and the new comment for omitting the *core* reason for the existing design—the overflow prevention provided by `cast_size_t_to_int()`. He directs Hardik to cite the original commit (937b7c8b) that introduced the helper, which fixed a crash caused by implicit `size_t`→`int` conversion. The feedback is actionable: Hardik should revise the commit message to explicitly state that the cast is not merely about signature alignment but about preventing overflow, and adjust the in-code comment accordingly. The patch itself is otherwise uncontroversial and aligns with the thread’s consensus to document the status quo rather than refactor. No further technical debate is expected; this is a final polish step before the patch can be merged.