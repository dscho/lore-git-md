This is a **performance inquiry** about `fetch.bundleURI`, specifically why clones using this feature are unexpectedly slow—comparable to or even slower than a standard full clone—when fetching the Linux kernel repository.

The author, Ryszard Knop, describes a CI setup where Git bundles are used to avoid repeatedly cloning the large kernel repository from a remote host. The workflow involves initializing a new repository, configuring `fetch.bundleURI` to point to a pre-generated bundle, and then fetching a specific reference (branch, tag, or commit). Despite the expectation that bundle-based clones would be faster, the observed performance is poor: 4–5 minutes for the bundle-based approach versus 3–4 minutes for a standard clone. Profiling with `GIT_TRACE` reveals that most of the time is spent in `git index-pack`, which consumes 100% CPU on a single thread for the majority of the operation.

The email does not include a patch or propose a fix, but it raises a question about whether this slowness is expected behavior. The author notes that the alternative—packaging and working with a full bare repository—is less elegant but may be more performant. The context suggests this is a real-world pain point for CI systems dealing with large repositories, and the inquiry is likely aimed at understanding whether the observed behavior is a known limitation or a potential bug in the bundle-fetching logic. Future discussion may focus on profiling, optimizations, or alternative approaches to the use case.