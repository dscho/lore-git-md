This is a follow-up in the `WITH_BREAKING_CHANGES` test-failure thread. Phillip Wood suggests a plausible explanation for the failures in `t1517-outside-repo.sh`: stale executables left over from a previous build (on a different branch) that are no longer built when `WITH_BREAKING_CHANGES=1` is enabled. He speculates that these stale binaries might be interfering with the test, which would explain why the failures are environment-specific and do not reproduce in Junio’s clean builds. For the alias-related failures in `t0014-alias.sh`, Phillip does not offer a hypothesis, noting only that the cause is unclear.

The observation about stale build artifacts is substantive: it identifies a concrete mechanism (build pollution) that could account for the discrepancy between brian’s and Junio’s environments. This shifts the focus from the breaking-changes code itself to the build and test infrastructure, suggesting the issue may not be a regression in Git’s logic but rather a side effect of how the build system handles conditional compilation. If confirmed, this would be a low-risk, high-impact fix (e.g., adding a `make clean` step to the CI job or improving the build system’s dependency tracking).

The thread remains in the diagnostic phase; no patch or test modification has been proposed yet. The next likely step is for someone to check whether a `make clean` or `git clean -xdf` resolves the `t1517` failures in brian’s environment. If it does, the discussion may pivot to ensuring the CI job for `WITH_BREAKING_CHANGES` includes a clean build step. The alias failures remain unexplained and may require separate investigation.