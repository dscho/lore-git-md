Thread identity: Build configuration report regarding test failures with `WITH_BREAKING_CHANGES=1` in `config.mak`. Originating author: brian m. carlson. Thread type: bug report / regression notice.

Core problem or goal: Identify and document test suite failures that occur when `WITH_BREAKING_CHANGES=1` is enabled, a build configuration flag expected to see increased use as contributors prepare for Git 3.0. The failures affect `t0014-alias.sh` (tests 4 and 8) and `t1517-outside-repo.sh` (tests 248 and 249), and are caused by stale build artifacts (`git-whatchanged` and `git-pack-redundant` executables) left from prior builds. These executables are no longer built when `WITH_BREAKING_CHANGES=1` is enabled, but if they remain in the build directory, the test suite treats them as external commands, triggering alias-loop detection logic that causes the failures.

Current status: Root cause identified and partially addressed. Jeff King (Peff) has submitted a two-patch series to fix the `t0014-alias.sh` failures by dynamically generating the list of deprecated commands used in the test, rather than hardcoding them. This ensures the test remains robust as deprecated commands are removed in future Git versions, including Git 3.0. The `t1517-outside-repo.sh` failures remain unaddressed but are acknowledged as a separate issue caused by stale build artifacts. The discussion has shifted to ensuring CI and local testing environments avoid false positives by using clean builds. No prereq-based skipping of tests has been implemented, and the focus is on making tests resilient to build environment changes.

Key technical details:
- **Build configuration**: `WITH_BREAKING_CHANGES=1` in `config.mak`, with `DEVELOPER=1`, `WITH_RUST=1`, and `CC=clang`.
- **Failing tests**:
  - `t0014-alias.sh`: tests 4 and 8 (alias handling, triggered by stale `git-whatchanged` and `git-pack-redundant` binaries being treated as external commands). Fixed by Peff's series to dynamically generate the list of deprecated commands from the Makefile.
  - `t1517-outside-repo.sh`: tests 248 and 249 (operations outside the repository, expecting `git-whatchanged` and `git-pack-redundant` to be unavailable). Root cause identified as stale build artifacts, but no fix has been implemented yet.
- **Root cause**: Stale build artifacts (`git-whatchanged` and `git-pack-redundant` executables) left from prior builds. These executables are no longer built when `WITH_BREAKING_CHANGES=1` is enabled, but if they remain in the build directory, they interfere with test expectations.
- **Behavioral change**: `WITH_BREAKING_CHANGES` removes special handling for deprecated builtins, making them subject to normal alias rules. This triggers alias-loop detection logic in `t0014-alias.sh`.
- **Subsystems touched**: Alias handling, repository boundary checks, and build system dependency tracking. Peff's series modifies `t/t0014-alias.sh` to use a dynamic list of deprecated commands derived from the Makefile. No on-disk format changes or new symbols are involved.
- **Test coverage**: The failures are isolated to environments with stale build artifacts. A clean build (`make clean` or `git clean -xdf`) is expected to resolve the issue. Peff's series makes `t0014-alias.sh` resilient to future changes in the set of deprecated commands.

Open questions and loose ends:
- Confirm whether a clean build resolves the `t1517-outside-repo.sh` failures in brian m. carlson's environment (actionable next step).
- Decide whether to improve `t1517-outside-repo.sh` by generating its command list from the Makefile (as suggested by Peff) or rely on clean builds to avoid the issue.
- Ensure the CI job for this configuration includes a clean build step to prevent false positives.
- Whether the `t0014` test should be removed or modified once the breaking changes land in Git 3.0.
- No impact on brian's upcoming patch series is expected, as the failures are not regressions in Git's logic but artifacts of the build system.

Participant positions:
- **brian m. carlson**: Reported the failures, noting they are preparing a patch series that depends on `WITH_BREAKING_CHANGES`. Position remains factual and diagnostic. Medium weight (substantive technical observation about a configuration likely to see increased use).
- **Junio C Hamano**: Confirmed the failures do not reproduce in his environment, implying the issue may be specific to brian's build configuration. Position is diagnostic and observational. Medium weight (substantive technical observation about reproducibility).
- **Phillip Wood**: Proposed that stale build artifacts may explain the `t1517-outside-repo.sh` failures, identifying a concrete mechanism (build pollution) that could account for the environment-specific failures. High weight (substantive technical hypothesis with actionable implications).
- **Jeff King (Peff)**: Provided a thorough and actionable explanation for both sets of failures, identifying the exact mechanism (stale `git-whatchanged` and `git-pack-redundant` binaries) and the behavioral change (`WITH_BREAKING_CHANGES` removing special handling for deprecated builtins). Submitted a two-patch series to fix the `t0014-alias.sh` failures by dynamically generating the list of deprecated commands. Position is diagnostic, solution-oriented, and forward-looking, shifting the discussion to how to handle the limbo state before Git 3.0. High weight (substantive technical analysis and implementation with clear next steps).

Related work:
- The `WITH_BREAKING_CHANGES` flag is expected to see increased use as contributors prepare for Git 3.0.
- The report mentions an upcoming patch series from brian that depends on this configuration, but no details about the series are provided. The failures are not expected to block or delay this series.
- Peff's series is motivated by the "limbo state" of `WITH_BREAKING_CHANGES` and aims to prevent false positives in CI and local testing environments. Follow-up work may be needed to address the `t1517-outside-repo.sh` issue.