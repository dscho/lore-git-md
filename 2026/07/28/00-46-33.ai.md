This is a build configuration report from brian m. carlson identifying test failures when `WITH_BREAKING_CHANGES=1` is enabled in `config.mak`. The failures occur in two test scripts: `t0014-alias.sh` (tests 4 and 8) and `t1517-outside-repo.sh` (tests 248 and 249). The failures disappear when `WITH_BREAKING_CHANGES` is removed, suggesting they are directly caused by the breaking changes introduced by that build flag.

The context is that brian is preparing a patch series that depends on `WITH_BREAKING_CHANGES` and wants to ensure the test suite passes before sending it. They note that a CI job exists for this configuration, but the failures suggest the CI job may not be testing the intended scope or may have gaps. The report is factual and does not speculate on the root cause beyond the correlation with the build flag.

Key technical details: the build configuration includes `DEVELOPER=1`, `WITH_RUST=1`, and `CC=clang`, but the failures are isolated to `WITH_BREAKING_CHANGES`. The failing tests involve alias handling (`t0014`) and operations outside the repository (`t1517`), which are common areas for edge cases when breaking changes are introduced. No specific commit or patch is referenced as the cause, and the report does not include reproduction steps beyond the `config.mak` snippet.

This is not a patch or review, but a heads-up about potential regressions in a configuration that is likely to see increased use as contributors prepare for Git 3.0. The mention of an upcoming series suggests these failures may become more visible soon.