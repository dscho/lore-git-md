This is a **documentation patch** that updates the comment in `utf8.c` to explain why `utf8_strwidth()` and `utf8_strnwidth()` return `int` instead of `size_t`. The patch is part of the final polish for the UTF-8 string-width type-refactoring thread, which has now shifted from proposing code changes to documenting the existing design rationale.

Junio C Hamano and Phillip Wood engage in a **substantive review** of the proposed comment, refining its clarity and completeness. The discussion centers on ensuring the comment explicitly addresses three key points: (1) why the functions return `int` (alignment with `printf()`-style APIs for padding calculations), (2) why `cast_size_t_to_int()` is necessary (overflow prevention, not just signature alignment), and (3) the original crash-fix rationale from commit 937b7c8b. Junio suggests a more detailed template for the comment, emphasizing the practical constraints (e.g., `printf("%*s", width, string)` requiring `int`) and the safety guarantees provided by the explicit cast.

The patch is otherwise uncontroversial and aligns with the thread’s consensus to document the status quo. The only remaining action is for Hardik Kumar to revise the commit message and in-code comment to incorporate the feedback, particularly the reference to 937b7c8b. Once this is done, the patch is expected to be ready for merging. No further technical debate is anticipated.