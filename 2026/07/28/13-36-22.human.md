This is a small but user-facing improvement to Git's branch deletion error messages. The patch, now in `next`, makes `git branch -d` report a clearer reason when refusing to delete a branch currently used by `git bisect`—instead of the generic "used by worktree at ..." message, it now says the branch is "used for bisect." The change is narrowly scoped to the bisect case, leaving more complex scenarios (like rebases) for future work.

Toon Claes's reply is a brief, positive review. He agrees the patch is a step in the right direction and sees it as a foundation for a broader API to check branch usage consistently across Git commands, like the recently discussed `git history` safety gap. His feedback is surface-level—he endorses the direction without digging into technical details—but it reinforces the patch's role as a building block for future improvements.

The patch itself is well-crafted: it touches only the necessary files (`branch.c`, `branch.h`, `builtin/branch.c`, and a test), introduces a clean internal helper (`branch_bisecting()`), and extends test coverage for the bisect case. The commit message clearly explains the motivation and scope, and the implementation avoids over-engineering by deferring the harder "rebase" scenarios.

For a busy reader, this is worth noting if you care about Git's usability or the ongoing effort to unify branch-safety checks. The patch is uncontroversial and likely to graduate to `master` soon, but the broader API discussion—hinted at by Junio and echoed by Toon—could lead to follow-up work. If you're tracking `git history` or branch-safety features, keep an eye on this thread.