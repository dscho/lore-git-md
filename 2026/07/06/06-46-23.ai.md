This is a **performance optimization discussion** for the reftable backend, specifically addressing the quadratic-time behavior in bulk ref deletion and re-creation. Patrick Steinhardt responds to Kristofer Karlsson’s uncertainty about whether to formally submit a patch that exposes tombstones to iterator bounds checks, which previously showed dramatic speedups in artificial benchmarks (e.g., 8,000 refs: 14.93s → 0.145s) but limited real-world impact.

Patrick clarifies a **reproducible scenario** where the patch’s benefits should be observable without disabling compaction: creating and then deleting a large number of refs. This workflow avoids compaction overhead and directly exercises the tombstone iteration bottleneck in `refs_verify_refname_available()` and `reftable_be_transaction_prepare()`. He suggests that if the patch delivers a **significant improvement** in this case, it would justify submission despite its code complexity (tombstone-skipping loops at call sites).

The email advances the thread by narrowing the scope of the patch’s value to a **specific, demonstrable use case**—bulk ref deletion—rather than relying on artificial benchmarks. This shifts the discussion toward **practical validation**: if Kristofer’s patch can show measurable gains in Peff’s test script (or similar), it may still be worth merging as a safeguard against worst-case performance. If not, the patch could be abandoned or deferred. Patrick’s input here is pivotal, as his earlier encouragement to submit the patch was based on broader benchmark results; his current stance leaves the door open for further testing but does not commit to merging. The thread remains in a **diagnostic phase**, with the next step likely being Kristofer’s attempt to quantify the improvement in the suggested scenario.