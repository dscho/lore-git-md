Thread identity: standalone patch improving error reporting in `git rm` by switching from `die()` to `die_errno()` in `builtin/rm.c`. Originating author: first-time contributor.

Core problem or goal: enhance user-facing error messages when `git rm` fails due to system-level issues (e.g., permission errors, missing files) by including the system error string from `errno`.

Current status: patch posted, under initial review. No prior versions.

Key technical details: single-line change in `builtin/rm.c`; replaces `die()` with `die_errno()` after a failed `remove_file_from_index()` call. No new symbols, no on-disk format changes, no test coverage added.

Open questions and loose ends: none identified yet; patch is uncontroversial and mechanical.

Participant positions: none recorded yet beyond the author.

Related work: none.