This is a **clarification reply** in the merged `git refs` subcommand series, addressing a usability question about symbolic reference (symref) support in the new `git refs rename` subcommand. Patrick Steinhardt confirms that the subcommand **does not support renaming symrefs**, attributing this to backend limitations rather than a deliberate design choice. The response is matter-of-fact and does not propose a fix or future work, suggesting this is a known constraint rather than an oversight.

The exchange highlights a gap between user expectations (symref renaming, analogous to `git branch -m`) and Git’s current capabilities. While the series is already merged, this clarification may inform future discussions about reference backend improvements or CLI ergonomics. The limitation is not documented in the patch’s man page, which could lead to confusion for users unfamiliar with Git’s internals. No code changes or follow-ups are implied by this reply.