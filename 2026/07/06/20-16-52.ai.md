This is a **contributor housekeeping** follow-up in the `git-subtree` safeguard series. Junio C Hamano responds to Ian Jackson’s note about the error message formatting in the `reject_if_v2_config` helper function. Junio confirms that the current long, single-line `die` message is acceptable, given the script’s eventual deprecation in favor of the Rust rewrite. He emphasizes that the primary goal is to halt execution and prevent repository corruption, not to optimize error readability.

The exchange resolves the last open question from the v1 review: no further adjustments to the error message are needed. The series remains on track for acceptance once Ian posts the scoping fix (using `$1` instead of the global `rev` variable) in the next revision. The patch continues to target `contrib/subtree/git-subtree.sh` and `t/t7900-subtree.sh`, introducing the `subtree.rust-rewrite` config key as a safeguard against mixing the shell and Rust implementations.