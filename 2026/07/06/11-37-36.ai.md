This is a **performance optimization patch submission** for the reftable backend, addressing a **quadratic-time scalability issue** in bulk ref deletion and re-creation workflows. Kristofer Karlsson confirms that his previously discussed fix—exposing tombstones to iterator bounds checks—delivers **measurable improvements in realistic scenarios**, not just artificial benchmarks. The patch reduces runtime from **O(n²) to O(n)** for workflows like deleting old refs and creating new ones (e.g., 27s → 0.09s for 8,000 refs), a **300x speedup** that justifies its code complexity.

The email advances the thread by resolving the open question about the patch’s real-world value. Kristofer’s benchmarks demonstrate that the fix is **not just a niche optimization** but a meaningful safeguard against worst-case performance in workflows involving lexicographically ordered ref deletions/re-creations (e.g., `old-*` → `new-*`). This aligns with Patrick Steinhardt’s earlier suggestion to validate the patch in a reproducible scenario, and the results likely ensure its acceptance. The patch touches `reftable/merged.c` and related call sites, adding tombstone-skipping loops to compensate for the iterator now returning tombstones. While the fix is narrowly scoped to the reftable backend’s tombstone design, its impact on bulk operations is substantial enough to warrant merging.

Key details:
- **Files touched**: `reftable/merged.c`, `reftable/refs.c`, and call sites like `reftable_be_reflog_exists()`.
- **Behavior change**: Tombstones are now exposed to iterator bounds checks, enabling early termination in `merged_iter_next_void()` and eliminating the O(n²) bottleneck.
- **Benchmark results**: 8,000 refs (delete `old-*`, create `new-*`) improved from 27s to 0.09s; recreate-same-refs improved ~100x.
- **Subsystems**: Reftable backend, ref transactions, iterator logic.
- **Test coverage**: Peff’s ad-hoc script for bulk ref deletion/re-creation, now validated for realistic workflows.

The patch is now ready for formal submission, with no remaining technical objections. The thread’s focus shifts to final review and integration.