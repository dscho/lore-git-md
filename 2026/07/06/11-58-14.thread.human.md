This thread is about a small but important safeguard for `git-subtree` in `contrib/`: detecting repositories that have been modified by a forthcoming Rust rewrite of the same command, which won’t be backward-compatible. The patch series (v1) is now under review and nearly ready.

Ian Jackson, the author, has addressed all substantive feedback. Junio C Hamano, the maintainer, spotted and fixed a scoping bug in the detection helper function, and confirmed the error message format is acceptable. Colin Stagner suggested minor test style improvements, which Ian has accepted. The only remaining discussion is about test mechanics—specifically, how to assert that a command fails for the expected reason. Phillip Wood stepped in to provide the canonical Git test framework idiom for checking error messages, which should help Ian finalize the test case in the next revision.

The series touches `contrib/subtree/git-subtree.sh` and adds a test case to `t/t7900-subtree.sh`. It introduces a new config key (`subtree.rust-rewrite`) that triggers the safeguard, and the test will verify the error is raised when this key exists. No open questions remain on the patch’s design or purpose; the next version should be ready for merging.