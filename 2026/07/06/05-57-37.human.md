This is a brief follow-up in the memory-leak cleanup thread, specifically about the role of compiler diversity in CI. Patrick Steinhardt agrees with Jeff King’s point that undefined behavior (UB) can occasionally surface differently under different compilers or optimization levels, but he reinforces that tools like UBSan are a more systematic and effective way to catch such issues. He notes that the existing CI setup already runs the full test suite with both clang and gcc, so even if UBSan misses something, compiler diversity provides a fallback safety net.

The exchange is procedural and clarifying, not a review or patch. It reaffirms the earlier consensus to streamline the CI matrix (e.g., merging `linux-TEST-vars-leaks`, dropping redundant jobs) while acknowledging that compiler diversity still has incidental value. No new objections or proposals are introduced, and the original patch series (fixing `format-patch` and test harness leaks) remains unaffected. The discussion is now fully converged.