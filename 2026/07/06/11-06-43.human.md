This is a **surface-level review** of patch 8/11 in Phillip Wood’s systemic overhaul of how `git rebase` handles dropped commits. The reviewer, Oswald Buddenhagen, points out two redundant checks in the refactored `pick_one_commit()` function in `sequencer.c`.

The patch is part of a larger series that audits and repairs how the rebase machinery treats commits that are dropped due to empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, or command execution failures. This specific patch is a **pure refactoring**—it simplifies control flow without changing behavior, preparing the codebase for the core fix in patch 11/11.

Oswald’s feedback is mechanical and correct: the early return for non-interactive rebases (`!is_rebase_i(opts)`) makes the `res` checks in the subsequent `else if` blocks redundant, since `res` can only be true if the rebase is interactive. The observation is minor but improves code clarity. No functional issues are raised, and the feedback does not challenge the series’ direction or scope.

The review is **surface-level**—it focuses on code style and redundancy rather than deeper technical concerns. It does not engage with the broader problem (notes and post-rewrite hooks incorrectly receiving information about dropped commits) or the systemic fixes in later patches. The feedback is unlikely to require a reroll of the series, but Phillip may incorporate it as a small cleanup in the next iteration.