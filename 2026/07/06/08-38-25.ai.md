This is a targeted bugfix for `git blame` output formatting, specifically addressing the usability friction reported by Laszlo Ersek where `git blame -b` (blank boundary commits) unnecessarily reserved an extra hex digit for a caret marker that was never displayed. The patch, authored by René Scharfe, refactors the mark-handling logic in `blame.c` to dynamically allocate space for boundary/ignored/unblamable marks only when they are actually shown, ensuring abbreviated commit hashes align with the user’s `core.abbrev` setting.

The change is minimal and surgical: it introduces helper functions `print_marks()` and `count_marks()` to centralize mark-handling logic, updates `find_alignment()` to calculate mark columns dynamically based on the maximum number of marks observed in the output, and removes the hardcoded `+1` abbreviation adjustment. The documentation is updated to clarify that the abbreviation length now extends only when marks are visible. The patch preserves the fixed-width nature of the output, minimizing backward-compatibility risks for scripts parsing `git blame` output.

Laszlo Ersek has confirmed the fix resolves the issue with a **Tested-by** trailer, and the discussion has converged on the solution. The patch is well-motivated, directly addressing the reported workflow friction (e.g., copy-pasting hashes into `git rebase -i`), and René’s implementation is clean and focused. However, the patch currently lacks test coverage, which René acknowledges; no tests have been proposed yet to verify edge cases like multiple marks per line or regression prevention. The thread does not identify any downstream tools that might rely on the previous hash length, but the risk appears low given the output’s fixed-width structure.

Key technical details for follow-up discussion:
- **Files touched**: `blame.c` (core logic), `Documentation/git-blame.adoc` (updated description of `--abbrev`), and `t/t8002-blame.sh` (existing test expectations adjusted).
- **Symbols introduced**: `print_marks()`, `count_marks()`, `maybe_putc()`.
- **Behavior change**: Non-boundary commit hashes now respect `core.abbrev` exactly when `-b` is used; previously, they were abbreviated to `core.abbrev + 1` hex chars.
- **Config keys**: Only `core.abbrev` is relevant; no new config keys (e.g., `blame.abbrev`) are introduced.
- **Edge cases**: The patch’s logic should handle multiple marks per line if they exist, but René did not verify their feasibility.

The patch is ready for integration pending test coverage, with no unresolved technical concerns or backward-compatibility objections raised in the thread.