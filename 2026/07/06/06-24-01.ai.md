This is the sixth patch in the nine-patch v3 series making `GIT_TEST_LONG` tests reliable and efficient for CI. It targets the `t7900-maintenance.sh` test script, specifically the `incremental-repack 2g limit` test marked as `EXPENSIVE` due to its 2 GB repository.

The patch isolates the large repository into a dedicated sub-repository (`expensive-repo`) and cleans it up immediately after the test using `test_when_finished rm -rf`. This reduces peak disk usage by 2 GB and prevents the repository from persisting until the end of the test suite, which could otherwise affect subsequent tests' heuristics. The change also disables auto-maintenance in the sub-repository to avoid races with manual maintenance tasks.

The approach is straightforward and well-motivated: the test now creates its data in a standalone directory, ensuring cleanup and avoiding side effects on other tests. The diff is mechanical but clear, wrapping the test logic in a subshell and adding the cleanup hook. No new test infrastructure or user-facing changes are introduced.

This patch is uncontroversial and aligns with the series' broader goal of making `GIT_TEST_LONG` tests more efficient and reliable. It complements earlier patches in the series that reduce disk usage (e.g., patch 4) and improve test correctness (e.g., patches 2 and 5). The change is ready for integration, with no open questions or feedback from prior revisions.