This is a small but well-motivated bugfix patch from a new contributor. The change replaces `die()` with `die_errno()` in two places in `setup.c` where `chdir()` can fail, ensuring that the system error message (from `errno`) is included in the output. This makes failures easier to diagnose without changing behavior.

The patch is self-contained, touches only one file, and includes a clear commit message explaining why the change matters. The code change itself is mechanical and correct: `die_errno()` is the right tool when a system call fails and you want to preserve the error context. No tests are needed because this is purely about error reporting, not logic.

This is not part of a larger series; it’s a standalone fix. The contributor appears to be new to the project, but the patch follows Git’s conventions and addresses a real (if minor) usability gap. Worth a quick look from a reviewer, but nothing controversial here.