Thread identity: patch series, v1, "fix issues with GIT_TEST_LONG and expensive tests", originating author Patrick Steinhardt.

Core problem or goal: address test failures and hangs in expensive tests (those guarded by `GIT_TEST_LONG`) on 32-bit platforms, where large file operations (e.g., 2 GB files) exceed `SSIZE_MAX` and cause `ENOMEM` or indefinite hangs.

Current status: under review; this is the second patch in a nine-patch series. No prior versions of this series have been posted. No merge status recorded.

Key technical details:
- Files touched: `t/t0021-conversion.sh`.
- New test prerequisite: `SIZE_T_IS_64BIT`.
- Behavior change: the 2 GB clean/smudge roundtrip test is now skipped on 32-bit platforms.
- No new symbols, CLI options, or on-disk format changes.
- Test coverage: the skipped test is part of the existing `GIT_TEST_LONG` suite; no new test files.

Open questions and loose ends: none introduced by this patch; the commit message notes that reliably detecting the failure is difficult but does not request follow-up.

Participant positions: none recorded yet beyond the author.

Related work: this series is part of the broader effort to stabilize and enable expensive tests in CI, which began in May 2026. No external dependencies.