This is a **substantive follow-up in the memory-leak cleanup thread**, clarifying the role of compiler diversity in Git’s CI infrastructure. Patrick Steinhardt responds to Jeff King’s (Peff) concern about undefined behavior (UB) surfacing differently under clang and gcc, reinforcing that UBSan (Undefined Behavior Sanitizer) is the more reliable tool for detecting such issues. He agrees with Peff’s broader point that compiler-specific test failures are rare and that the existing CI matrix—where at least one job runs the full test suite with each compiler—already provides sufficient coverage for latent UB. The discussion remains procedural and converged; no new objections or technical changes are introduced.

The email touches on two key technical details: **UBSan’s effectiveness** in systematically catching UB (as opposed to the scattershot nature of compiler-specific test runs) and the **existing CI structure**, which ensures that all tests are executed under both clang and gcc, albeit not in a full matrix. Patrick’s position aligns with the earlier consensus to streamline the CI jobs (e.g., merging `linux-reftables-leaks` and `linux-TEST-vars` into `linux-TEST-vars-leaks`), while acknowledging that compiler diversity still serves a limited but useful purpose. The original patch series (fixing `format-patch` and test harness leaks) and the CI consolidation plan remain unaffected. For future discussion, the thread establishes that UBSan is the preferred tool for UB detection, and compiler-specific jobs are not a priority for expansion.