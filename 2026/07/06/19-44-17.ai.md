This is a **substantive review** of the `git rm` error reporting patch, focusing on correctness. Junio C Hamano questions the technical foundation of the change: he doubts that `remove_file_from_index()` (or any function in its call chain) actually sets `errno` in a meaningful way when it fails. Without that guarantee, switching from `die()` to `die_errno()` would risk displaying irrelevant or misleading system error messages.

The review requests a call graph analysis to identify which system or library call in the `remove_file_from_index()` path actually sets `errno` on failure. Until that is demonstrated, Junio suggests the patch would introduce noise rather than useful diagnostic information. This is a fundamental challenge to the patch’s premise, not a surface-level nit.

The thread remains open; no revised patch has been posted yet. The author will need to either (a) trace the call chain and confirm that `errno` is reliably set, or (b) withdraw or rethink the approach. For now, the patch looks unlikely to proceed without further justification.