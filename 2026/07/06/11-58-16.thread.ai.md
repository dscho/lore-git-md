Thread identity: patch series (2 patches), subject "contrib/subtree: add test to reject RIIR output", originating author D. Ben Knoble, thread type patch series.

Core problem or goal: add a defensive test to `contrib/subtree` that detects and rejects output that might be produced by a hypothetical future Rust rewrite of `git subtree`. The test is a precautionary safeguard against a potential RIIR (Rewrite It In Rust) effort, ensuring that `git subtree split` fails if it encounters a marker file (`.git-subtree/config`) that a Rust implementation might introduce.

Current status: under review; this is the first version of the series (v1). The patch is small, self-contained, and touches only the test script `t7900-subtree.sh`. No prior versions exist, and there is no indication that a Rust rewrite of `git subtree` is actively in progress.

Key technical details:
- Files touched: `t/t7900-subtree.sh` (test script only).
- New test: creates a repository, adds a subtree, sabotages it by creating `.git-subtree/config`, and verifies that `git subtree split` fails.
- Scope: narrow; checks only for the presence of a specific marker file and does not validate Rust-generated data or correctness.
- No new symbols, CLI options, or on-disk format changes introduced.
- Test follows existing test style and conventions in the subtree test suite.

Open questions and loose ends:
- No explicit rationale provided in the commit message for why this test is being added now. The context suggests it is a defensive measure, but the motivation is unclear.
- No discussion yet on whether this test is necessary or sufficient as a safeguard against a Rust rewrite.
- No feedback or review comments from other participants at this stage.

Participant positions: none yet; this is the initial patch submission.

Related work: none mentioned in the thread. The patch is not directly tied to any in-flight efforts (e.g., Rustification, ODB abstraction, or reftable backend). The hypothetical Rust rewrite of `git subtree` is not an active or announced project.