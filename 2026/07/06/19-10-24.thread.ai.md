Thread identity: standalone patch improving error reporting in `git rm` by switching from `die()` to `die_errno()` in `builtin/rm.c` when `remove_file_from_index()` fails. Author: not specified in the summary.

Core problem or goal: enhance error messages from `git rm` by preserving the system error (`errno`) when file removal from the index fails, making failures easier to diagnose.

Current status: patch posted, under review. Initial submission; no prior version. Junio C Hamano has raised a **high-weight** technical objection: he doubts that `remove_file_from_index()` (or its call chain) reliably sets `errno` on failure, making `die_errno()` potentially misleading. The patch is now blocked pending call-graph analysis to confirm `errno` provenance.

Key technical details:
- File: `builtin/rm.c`
- Function: error path in `cmd_rm()`
- Change: replace `die()` with `die_errno()`
- Subsystem: error reporting; no behavior change beyond message content
- No new symbols, no on-disk format changes, no test coverage
- Call chain under scrutiny: `cmd_rm()` → `remove_file_from_index()` → ... → system/library call that sets `errno`

Open questions and loose ends:
- **Primary**: Does `remove_file_from_index()` (or any function it calls) reliably set `errno` on failure? (Junio requests call-graph analysis.)
- **Secondary**: If `errno` is not reliably set, is there an alternative mechanism (e.g., returning an error code with `errno` capture at the point of failure) to preserve diagnostic value?

Participant positions:
- **Junio C Hamano**: objects to the patch as currently written, citing risk of misleading error messages. Position is **substantive** (engages with correctness and reliability of `errno` propagation). No Reviewed-by or Acked-by; this is a blocking objection.

Related work: none. This remains a standalone fix unrelated to any larger series or in-flight topics.