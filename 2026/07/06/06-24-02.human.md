This is the seventh patch in Patrick Steinhardt’s nine-patch series making `GIT_TEST_LONG` tests reliable and efficient for CI. It introduces consistent boolean parsing for the `GIT_TEST_LONG` environment variable using the `test_bool_env` helper, replacing ad-hoc checks scattered across the test suite and CI scripts.

The change is small but important for usability: previously, setting `GIT_TEST_LONG=false` had no effect because the test suite only checked whether the variable was non-empty. With this patch, users can now explicitly disable long tests by setting the variable to `false`, and the CI scripts (`ci/lib.sh`) and test library (`t/test-lib.sh`) will respect that setting. The patch updates two files: `ci/lib.sh` (changing `GIT_TEST_LONG=YesPlease` to `GIT_TEST_LONG=true`) and `t/test-lib.sh` (replacing `GIT_TEST_LONG=t` with `GIT_TEST_LONG=true` and updating the `EXPENSIVE` prerequisite to use `test_bool_env`).

This is a clean, uncontroversial refactoring that aligns with Git’s test infrastructure conventions. The commit message is clear and the change is well-motivated, addressing a minor but real pain point for developers who want to control test execution. The patch is part of a larger, well-received series that has already addressed all substantive feedback, and this specific change has not drawn any objections or discussion. It’s ready to merge.