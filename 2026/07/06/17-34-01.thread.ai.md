Thread identity: patch series (single patch), originating author unknown (name not provided in summary), thread type: bug fix / test portability.

Core problem or goal: fix `t0213-trace2-ancestry.sh` test failures in user-mode emulation environments (e.g., QEMU) where `/proc/<pid>/stat` reports the emulator's name ("qemu-riscv64") instead of the guest process ("test-tool" or "git"), causing false negatives despite correct Trace2 ancestry tracking functionality.

Current status: initial submission; no prior versions or discussion referenced. Patch is under review (no merge status indicated).

Key technical details:
- Files touched: `t/t0213-trace2-ancestry.sh` (test script).
- Subsystem: Trace2 ancestry tracking (test infrastructure).
- Behavior change: updates test prerequisite check to spawn `test-tool` from itself and verify the child's ancestry contains "test-tool", skipping tests if process names are unreliable (e.g., under emulation).
- No new/renamed symbols, CLI options, or on-disk format changes.
- Test coverage: targets edge-case environments; no impact on native platforms.

Open questions and loose ends: none identified; patch is narrowly scoped and appears uncontroversial.

Participant positions: none recorded (initial submission).

Related work: none mentioned. Fix is self-contained to the test script introduced in commit `3c8c638df6`.