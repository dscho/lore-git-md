Thread identity: patch series, v1, 9 patches, originating author Patrick Steinhardt, subject "performance improvements for expensive tests".

Core problem or goal: accelerate the `t4141-apply-too-large.sh` test, which currently takes 6–40 minutes due to inefficient generation of a 1 GiB patch file via one-byte-at-a-time `dd` reads.

Current status: under review; this is the third patch in the v1 series. No prior versions exist.

Key technical details: test script `t4141-apply-too-large.sh`; replaces `test_copy_bytes` + `dd ibs=1` with a single `test-tool genzeros 1g` call; retains the `EXPENSIVE` prerequisite; no new symbols, no on-disk format changes, no CLI changes.

Open questions and loose ends: none reported so far.

Participant positions: none recorded yet.

Related work: part of a larger series targeting expensive tests; no external dependencies.