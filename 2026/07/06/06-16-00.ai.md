This is a **substantive review reply** in Jeff King’s memory-leak bugfix series, focusing on the idempotency design for `git_hash_discard()`. Patrick Steinhardt **strongly endorses a strict approach** to idempotency, proposing that calls to `git_hash_update()` or `git_hash_final()` on an uninitialized context should trigger a `BUG()` rather than silently reinitializing or ignoring the operation. His position aligns with Jeff’s concern about papering over bugs and prioritizes fail-fast behavior to catch misuse early.

Key technical details from the reply:
- **Proposed behavior**: Any attempt to use a hash context after it has been finalized or discarded should `BUG()` (e.g., `git_hash_update()` on an inactive context).
- **Initialization safety**: Patrick agrees with Jeff that `git_hash_init()` should not check the `active` flag (since the input context may be uninitialized), but suggests `git_hash_start()` (a hypothetical wrapper) could enforce strictness by `BUG()`-ing if called on an already-active context.
- **Series integration**: Both agree the idempotency work should land **after** the current series, as the existing patches already avoid double-calls to `final()`/`discard()` and could later be simplified.

The reply **resolves the open question** about idempotency semantics by favoring strictness, but leaves the implementation details (e.g., `git_hash_start()`) as follow-up work. No objections to Jeff’s proposed `active` flag or the broader series direction are raised. The discussion now centers on **how** to enforce idempotency (via `BUG()`) rather than **whether** to do so.