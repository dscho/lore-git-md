**Summarizer brief mode**

Taylor Blau replies to Patrick Steinhardt’s proposal to leave the traversal APIs (`traverse_bitmap_commit_list()` and `traverse_commit_list()`) unchanged, accepting the pragmatic trade-off for this series but signaling intent to address the inconsistency later. Instead of adapting `traverse_commit_list()` (which would require widespread changes), Taylor proposes the opposite direction: splitting the callback type used by `for_each_bitmapped_object()` from `show_reachable_fn`, keeping the former abortable (returning `int`) while making the latter return `void`. This approach preserves the existing non-bitmap traversal machinery while aligning `traverse_bitmap_commit_list()` with `traverse_commit_list()` without large-scale refactoring. Taylor has a two-patch follow-up series ready to implement this change on top of v3. Key technical details: the change affects `for_each_bitmapped_object()` (in `pack-bitmap.c`), `show_reachable_fn` (used by `traverse_commit_list()` in `list-objects.c`), and their callers in `builtin/pack-objects.c` and `builtin/rev-list.c`. The follow-up will introduce a new callback type for bitmapped object iteration, decoupling it from the non-bitmap traversal path. No objections are raised; the tone is collaborative and forward-looking.