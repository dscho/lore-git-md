This is Junio Hamano reverting the merge of v16 of Pablo Sabater’s `remote-object-info` series to the `next` branch. The series, which adds `git cat-file --batch-command` support for querying object metadata from remotes, had been marked as ready for `next` after 17 iterations, but Junio discovered a procedural issue: the cover letter still attributed the work to Eric Ju and Calvin Wan despite Pablo having taken over as the primary author and implementer. While the technical content of v16 was sound (and v17 only removed an unnecessary `#include`), the attribution mismatch violates Git’s contribution guidelines. The revert is purely administrative—no code changes are involved—and the series will be re-merged once the cover letter is corrected. This does not affect the series’ readiness; it remains on track for `next` pending the trivial fix.