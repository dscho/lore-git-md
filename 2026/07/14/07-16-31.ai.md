This is a **test modernization** patch series, now at v2, addressing feedback on the second patch’s commit message. The series updates `t/t1100-commit-tree-options.sh` to use modern test style conventions without altering test behavior or coverage.

Patch 1/2 remains unchanged from v1: it replaces the old backslash-continued `test_expect_success` blocks with the current quoted-body form, using tabs for indentation. Patch 2/2 relocates a here-doc that generates expected output into the existing setup test (`test_expect_success 'test preparation: write empty tree'`), ensuring it runs under the test harness. The only change in v2 is the commit message for patch 2/2, which now uses present tense ("is created") to describe the here-doc’s behavior, as Junio requested.

The series is a straightforward cleanup, part of the ongoing effort to modernize legacy test scripts. It touches only `t/t1100-commit-tree-options.sh`, a script testing `git commit-tree` options, and maintains all existing test coverage (5 passing tests). The changes follow the pattern Eric Sunshine recommended for similar modernizations, such as `t7001`. No new symbols or behaviors are introduced, and the patches are unlikely to be controversial.

Junio’s review of v1 confirmed patch 1/2 was technically correct and patch 2/2 "trivially correct" aside from the tense issue. With that addressed, the series now meets project style guidelines and appears ready for `next`. The author, a new contributor, has verified the tests pass after each patch and adhered to established modernization practices. Key identifiers: test script `t1100-commit-tree-options.sh`, `test_expect_success` quoted-body blocks, and the here-doc relocation to the setup test.