This is the fifth patch in Harald Nordgren’s **v18 `--delete-merged` series**, now **implementation-complete and ready for merging** pending a few mechanical pre-merge fixes. The email captures a final design agreement: **Junio C Hamano has requested that the refined stacked-branch protection (abort-and-clear) be extended to `git branch -d` for consistency**, and Harald has agreed to implement this as a follow-up after the series lands.

The patch itself adds the core `--delete-merged` subcommand, which safely deletes local branches merged into their upstream while preserving branches used as upstreams for unmerged branches via **refined stacked-branch protection (abort-and-clear)**. The command now **fails to delete a branch if it is used as an upstream for an unmerged branch**, but **clears the upstream config for kept (merged) branches whose own upstream is being deleted**, avoiding broken dependency chains while cleaning up stale configs. The command also supports `<branch>` arguments to limit deletion candidates, reusing the `--forked` filter machinery for consistency.

The series is **feature-complete** and addresses all prior feedback, including per-branch opt-out (`branch.<name>.deleteMerged=false`), `--dry-run` preview, and the `--forked` filter. The only remaining pre-merge actions are:
1. A mechanical fix to patches 2-4/7 (replacing local boolean uses with direct flag checks in the flags-based API).
2. Rejecting the `--dry-run --quiet` combination as nonsensical.
3. Extending the refined stacked-branch protection to `git branch -d`, as agreed in this email.

Phillip Wood’s review of this patch is complete, and his latest feedback (test improvements, branch naming clarity, and redundant command removal) has already been applied. The test suite now explicitly configures `branch.main.pushRemote` to prevent `main` from being misclassified as a forked branch, exposing a **known limitation** where branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked when `remote.pushDefault` is set. This edge case is documented but does not affect the core logic or safety checks.

The series is **ready for merging** once the mechanical fixes are applied, and the stacked-branch protection extension to `git branch -d` will follow shortly after. Future summarizers should note that the `--forked` filter’s proposed refinement (simulating push/fetch refspecs) is accepted but deferred as a follow-up.