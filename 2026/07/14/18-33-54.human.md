This is Junio Hamano acknowledging a procedural hiccup in the integration of Pablo Sabater’s `remote-object-info` series. The maintainer had already marked the sixteenth version as ready for `next` and merged it earlier on the same day, only to discover that Pablo had sent a seventeenth version shortly afterward. Junio will revert the v16 merge and replace it with v17, which contains only a single mechanical cleanup (removing an unnecessary `#include "compat/posix.h"` in `transport.c`) on top of the already-reviewed and technically complete v16. No substantive changes remain; the series is otherwise ready for integration.