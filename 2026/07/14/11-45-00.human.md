This is a small but important type-safety fix in the `fetch-pack.c` file, part of the larger `remote-object-info` feature series. The patch corrects a type mismatch where `hash_algo_by_name()` (which returns `unsigned int`) was being assigned to an `int` variable. The fix changes the variable to `const unsigned int` to match the function's return type and adds `const` since the value is never modified.

The change is purely mechanical and localized to a single line in `fetch-pack.c`, with no behavior modification. It addresses minor technical debt introduced during earlier refactoring in the series. This patch was previously part of v16 (as patch 14/14) and has been carried forward unchanged into v17, confirming its stability and lack of controversy.

While small, this fix is emblematic of the series' broader attention to type safety and code hygiene. The series as a whole is now complete and ready for merging, with all substantive issues resolved. This particular patch doesn't affect functionality but eliminates a potential source of subtle bugs in future modifications to the same code path.