**Summarizer brief mode**

Jeff King (Peff) replies to Taylor Blau’s proposal to split the callback type used by `for_each_bitmapped_object()` from `show_reachable_fn` as a follow-up to Patrick Steinhardt’s ODB series. Peff endorses the idea as a "nice cleanup" but raises a potential risk: some callbacks in the bitmapped object iteration path might rely on the current abort-on-nonzero behavior. He suggests that the only way to confirm is to implement the change and observe whether any callers break. The exchange is brief and technical, focusing on the feasibility of the proposed follow-up rather than the ODB series itself. Key technical detail: the change would affect `for_each_bitmapped_object()` (in `pack-bitmap.c`), `show_reachable_fn` (used by `traverse_commit_list()` in `list-objects.c`), and their callers in `builtin/pack-objects.c` and `builtin/rev-list.c`). No objections are raised; the tone is exploratory and pragmatic.