This is a refactoring patch targeting the `strbuf` subsystem. René Scharfe removes a redundant `strbuf_reset()` call in `strbuf_getwholeline()` when the `HAVE_GETDELIM` variant is used. The commit message explains that the reset is unnecessary because `getdelim(3)` does not rely on the buffer's prior state, and the function already handles buffer initialization or reset on both success and error paths. The change is minimal—just two lines removed from `strbuf.c`—and appears to be a straightforward cleanup with no behavioral impact.

The patch is self-contained and does not reference any broader effort (e.g., `the_repository` removal or ODB abstraction). It touches core string-handling code, which is widely used across Git, but the modification itself is isolated to the `strbuf_getwholeline()` function. The rationale is clear: eliminating redundant work in a low-level utility function.

René is a frequent contributor known for meticulous refactoring patches, and this aligns with that pattern. The change is unlikely to be controversial, though it may attract review to confirm the absence of edge cases (e.g., whether `getdelim` could ever depend on the buffer's prior allocation state). The patch includes no tests, as the behavior is unchanged and the function is already exercised by existing tests.

Key technical details for follow-up discussion:
- File: `strbuf.c`
- Function: `strbuf_getwholeline()`
- Macro: `HAVE_GETDELIM`
- Removed call: `strbuf_reset(sb)`
- Related system call: `getdelim(3)`