This is a test infrastructure refactoring patch that moves three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is part of the larger `remote-object-info` series (now at v17) and is purely mechanical, with no behavior modification.

The patch follows Git's established practice of centralizing test helpers (like `lib-loose.sh`, which is already sourced in `t1006-cat-file.sh`). The extracted functions are simple shell utilities for string manipulation, and their relocation enables code reuse in later patches—specifically, the upcoming `remote-object-info` command implementation. The new library file is immediately sourced by the original test script, ensuring no disruption to existing tests.

This is a minor but necessary preparatory step in the series. The commit message clearly states the motivation (code reuse and readability) and the immediate consumer (a subsequent patch). The change itself is uncontroversial and aligns with Git's testing conventions. No review feedback is apparent in this iteration, suggesting the refactoring is straightforward and non-disruptive.