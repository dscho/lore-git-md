This is the third patch in René Scharfe's five-patch series introducing repository-aware variants of the lockfile API as part of the ongoing `the_repository` removal effort. The patch adds `repo_hold_lock_file_for_update()`, `repo_hold_lock_file_for_update_timeout()`, and `repo_hold_lock_file_for_update_mode()`—mirroring the existing non-repo variants—and wires them into the lockfile internals.

The implementation is straightforward: the new functions delegate to `lock_file_timeout()`, which now takes a `struct repository *` parameter and passes it to `repo_create_tempfile_mode()` (introduced in patch 1). The existing non-repo entry points (`hold_lock_file_for_update_timeout_mode()`, etc.) are converted into thin wrappers that call the repo-aware variants with `the_repository`. This keeps the change mechanical and ensures no behavior change for existing callers. The diffstat shows +31 lines in `lockfile.h` (new declarations) and +22 lines in `lockfile.c` (new definitions and wiring), with 8 lines removed (the old non-repo implementations).

No callers are converted in this patch; that happens in patch 5. The patch is pure API scaffolding, consistent with the series' goal of pushing the repository parameter outward through the lockfile layer. The commit message is minimal but sufficient for the mechanical nature of the change. As with the rest of the series, this is plumbing-only work with no user-visible impact. The patch is well-aligned with the broader `the_repository` removal effort and looks like a clean, incremental step.