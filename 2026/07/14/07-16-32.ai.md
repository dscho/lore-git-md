This is the first patch in a **test modernization** series (v2, 1/2) that updates `t/t1100-commit-tree-options.sh` to use the current Git test style. The patch replaces old-style `test_expect_success` blocks—where the test title and body are passed as separate backslash-continued arguments—with the modern quoted-body form, where the body is a single-quoted block indented with tabs. The change is purely stylistic; no test logic or behavior is altered, and all five tests continue to pass.

The script tests `git commit-tree` options, and the modernization follows the pattern Eric Sunshine recommended for similar updates (e.g., `t7001`). The patch touches only `t/t1100-commit-tree-options.sh`, converting four `test_expect_success` blocks to the new format. Junio’s review of v1 confirmed this patch was technically correct with no changes needed, and it remains unchanged in v2.

Key identifiers include the test script name (`t1100-commit-tree-options.sh`), the `test_expect_success` quoted-body blocks, and the use of tabs for indentation. The patch is part of an ongoing effort to modernize legacy test scripts, and its scope is limited to style improvements. No new symbols, behaviors, or edge cases are introduced, making it a straightforward cleanup suitable for `next`. The author, a new contributor, has verified the tests pass after the change.