This is a user feedback email on the `git history squash` feature, now in its eighth iteration. Matt Hunter raises a **usability edge case**: when `--reedit-message` is omitted, the command ignores any `amend!` message in the range that targets the first folded commit, even though the command is already aware of `fixup!`/`squash!`/`amend!` semantics (e.g., rejecting out-of-range targets). He suggests it might be more consistent—and useful—to incorporate the `amend!` message into the default squashed commit message, mirroring how the command already consolidates multiple `fixup!` commits when their target is in-range.

The observation is **narrow but substantive**: it questions the default behavior’s internal consistency and proposes a small but meaningful improvement to commit hygiene. Hunter acknowledges the trade-off (explicitness vs. convenience) and does not demand a change, but his framing—comparing the command’s awareness of `amend!` semantics in one context (validation) but not another (message construction)—makes the case for reconsidering the default. This could reignite the unresolved debate from earlier in the thread about whether `--reedit-message` (or `--edit`) should be the default, though Hunter does not explicitly reopen that discussion. The email is **constructive and focused**, likely to prompt a response from Harald or other reviewers about whether this edge case warrants a tweak before the series is queued.