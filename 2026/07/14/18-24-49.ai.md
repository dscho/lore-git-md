This is the third patch in Harald Nordgren’s **v19 `git branch --delete-merged` series**, which is now implementation-complete and ready for merging. The patch introduces a new `DELETE_BRANCH_SKIP_UNMERGED` flag to the branch-deletion API, allowing bulk callers (like the upcoming `--delete-merged` command) to silently skip unmerged branches instead of erroring out. This is a **refactoring change** that prepares the API for the series’ core functionality while preserving existing behavior for all current callers.

The change is **mechanical and behavior-preserving** for existing code paths. The `check_branch_commit()` and `delete_branches()` functions now check for the new flag before emitting errors or returning failure for unmerged branches. When the flag is set, unmerged branches are skipped without error messages, enabling the `--delete-merged` command to process multiple branches in a single invocation without aborting on the first unmerged branch. The patch does not alter any user-facing behavior—it only extends the internal API to support the series’ bulk-deletion use case.

The implementation is **clean and minimal**: the new flag is added to the `delete_branch_flags` enum, and the error-handling logic in `check_branch_commit()` and `delete_branches()` is wrapped in a conditional check. The change is **well-tested** as part of the broader series, which includes comprehensive integration tests for the `--delete-merged` command’s bulk-deletion behavior. No new tests are introduced in this patch, as the functionality is exercised by the higher-level command in later patches.

This patch is **a necessary precursor** to the core `--delete-merged` command (patch 5/7), which relies on the `DELETE_BRANCH_SKIP_UNMERGED` flag to implement its "delete all merged branches" behavior without aborting on unmerged branches. The flag is also used in conjunction with other safety checks (e.g., stacked-branch protection, per-branch opt-out) to ensure the command behaves predictably in complex workflows.

The patch is **low-risk** and **uncontroversial**, addressing a straightforward API gap identified during the series’ development. Phillip Wood’s earlier review of the series noted the need for this flag to avoid noisy errors during bulk operations, and the implementation aligns with the series’ broader goal of providing safe, automated branch cleanup. No further changes are expected for this patch.