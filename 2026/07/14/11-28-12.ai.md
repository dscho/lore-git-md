This is a minor follow-up to the core client-side implementation patch (10/13) in the GSoC v16 `remote-object-info` series. The author, Pablo Sabater, reports a small but incorrect `#include "compat/posix.h"` line that was accidentally added to `transport.c` in the original patch. The include was not needed and slipped through CI testing without causing issues. Pablo plans to remove it in the next reroll of the series.

This is a surface-level correction with no impact on functionality or security. The series remains ready for merging to `next` as previously assessed, with all substantive issues resolved. The fix is purely mechanical and will not affect the patch’s behavior or review status.