This is a brief exchange in the `git history squash` thread where **Junio C Hamano endorses D. Ben Knoble’s vision** of using `git history squash` and `git replay` as a faster, non-interactive backend for `git rebase --autosquash`. The tone is **optimistic and forward-looking**, treating Knoble’s speculation as a plausible long-term goal rather than idle speculation.

Junio’s reply is short but significant: he **agrees that this architectural shift would be "an ideal future"** and notes that the ongoing work on `git history` (including `squash` and `replay`) is already moving the project in that direction. His phrasing—"by the time that happens, we will have replaced the internals of `rebase [-i]` with a new implementation that does not need to touch the working tree"—suggests this is not just a performance tweak but a **fundamental redesign** of Git’s history-editing machinery. The implication is that the current `rebase` implementation, with its repeated conflict stops and working-tree churn, is seen as a legacy bottleneck, and the new commands are stepping stones toward a cleaner, more modular architecture.

The exchange does not introduce new technical details or debate unresolved issues; it simply **ratifies Knoble’s idea as a shared goal**. For a busy reader, this is a signal that `git history squash` is not just a standalone feature but part of a **larger strategic effort** to modernize Git’s history-editing tools. The series itself is already marked for merging, but this thread hints at where it might fit into Git’s future roadmap.