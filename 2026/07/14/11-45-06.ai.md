This is the core client-side implementation patch of the `remote-object-info` feature series, now in its seventeenth and final iteration. It introduces the `fetch_object_info()` function and integrates it into Git's transport layer, completing the infrastructure needed for `git cat-file --batch-command` to query object metadata (currently just size) from remote repositories without downloading full objects.

The patch adds new files `fetch-object-info.c` and `fetch-object-info.h` that implement the client-side protocol logic for the `object-info` capability. The function `fetch_object_info()` handles the full workflow: verifying server support for the capability, constructing and sending the request, parsing the response, and managing memory. It strictly enforces protocol v2 compliance and includes careful input validation through `parse_object_size()`, which rejects malformed size values. When servers return empty strings for unrecognized objects (per protocol specification), the function sets the corresponding size pointer to NULL, allowing callers to detect and handle missing objects gracefully.

Transport layer integration is achieved through a new vtable entry (`fetch_object_info`) implemented for both smart transport and transport-helper backends. The smart transport implementation (`fetch_object_info_via_pack()`) follows the same buffer-based approach as `fetch-pack.c`, writing the entire request before sending it. The transport-helper implementation delegates to the helper process, maintaining the established extensibility pattern. Both implementations manage connection setup, protocol version discovery, and cleanup, with proper error handling throughout.

The patch demonstrates careful attention to security and memory safety. It includes strict protocol enforcement, capability checks, and input validation. Memory management is handled carefully, with proper cleanup for stateless RPC and explicit NULL checks for size pointers. The implementation maintains separation from existing fetch infrastructure while following Git's established patterns for extensibility.

This is the final technical piece needed to enable the user-facing `remote-object-info` command introduced in patch 10/10 of the series. The implementation appears solid, with comprehensive error checking and integration that matches the project's standards. All review feedback from previous iterations has been addressed, and the patch is ready for merging as part of the completed series.