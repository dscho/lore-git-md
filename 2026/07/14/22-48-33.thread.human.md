Mode: human

This thread is a v3 patch series from Johannes Schindelin addressing 11 instances where Git code ignores return values from functions that can fail, identified by Coverity static analysis. The series adds missing error checks to prevent silent failures, hardening code paths in the HTTP, config, reftable, transport-helper, bisect, and last-modified subsystems.

All open correctness issues from v2 have been resolved, including a reftable block-writer memory leak, a bisect-run resource leak, and a maintainer question about `close()` return values. The series is now technically complete and ready for integration, with no remaining open questions. Junio Hamano, Jeff King, and Patrick Steinhardt reviewed earlier versions, focusing on error-handling correctness; their feedback has been fully incorporated. The patches add only fatal error paths or propagate errors to callers, with no behavior changes or on-disk format modifications.

The reftable-specific patch in this latest update fixes a latent bug where `reftable_writer_new()` could return a partially initialized writer object if block-writer initialization failed, now ensuring proper cleanup. The series is uncontroversial and self-contained, touching files like `http.c`, `reftable/writer.c`, and `bisect.c` without introducing new symbols or dependencies.