This thread is a straightforward patch series from Johannes Schindelin addressing 11 instances where Git code ignored return values from functions that can fail, as flagged by Coverity static analysis. The goal is to harden these code paths against silent failures without changing behavior or on-disk formats.

The series is now complete with its 11th patch, which fixes an unchecked `dup(1)` call in the bisect subsystem’s `bisect_run()` function. This final patch ensures proper cleanup and error reporting if `dup()` fails, preventing silent output redirection corruption. Like the rest of the series, it’s mechanical—adding error checks that either `die()` or propagate failures with descriptive messages—and touches only `builtin/bisect.c`.

The patches have been under review since the initial posting, with no substantive feedback yet. No maintainer decisions or integration branch assignments have been made. The changes are uncontroversial, focusing on low-risk error path hardening in subsystems like HTTP, config, reftable, transport helpers, and bisect. The existing test suites will implicitly cover the new error paths, and no new symbols or on-disk format changes are introduced. The series remains a clean, targeted effort to address latent bugs.