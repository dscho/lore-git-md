Mode: human

This thread is a v2 patch series from Johannes Schindelin, fixing 11 instances where Git code ignores return values from functions that can fail, as flagged by Coverity static analysis. The goal is to harden these code paths against silent failures without changing behavior or on-disk formats.

All substantive review feedback from Junio C Hamano and Patrick Steinhardt has been addressed in this second version. Junio raised four correctness concerns in v1 (on the fifth and ninth patches), and Patrick suggested two design improvements (centralizing error handling in the bisect subsystem and extending a `dup2()` check). These are now fully resolved by six fixup patches in v2. The latest email confirms Junio’s acceptance of the design choice in the tenth patch—using a boolean flag to simplify error handling for most callers—without raising new issues.

The series touches several subsystems (`http.c`, `bisect.c`, `reftable/`, `transport-helper.c`, and others) but only adds fatal error paths or propagates errors to callers. No new symbols or on-disk format changes are introduced. The patches are now technically complete, uncontroversial, and ready for final review and integration. No open questions remain.