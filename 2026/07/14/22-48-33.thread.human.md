This thread is a patch series from Johannes Schindelin addressing 11 instances where Git code ignores return values from functions that can fail, identified by Coverity static analysis. The goal is to harden these code paths against silent failures without changing behavior or on-disk formats. The series is currently under review, with no prior versions or maintainer decisions yet.

Junio C Hamano has raised two substantive correctness concerns. The first, in the fifth patch (`builtin/last-modified.c`), involves improper resource cleanup in `last_modified_run()` when skipping unparsable commits, risking memory leaks. The second, in the ninth patch (bisect subsystem), identifies a resource leak in `get_terms()` where a partially initialized struct could be left with a dangling pointer if the second line read fails. Both issues are narrowly focused on error-handling logic and require fixes before the patches can proceed.

The series touches several files, including `http.c`, `builtin/config.c`, `reftable/block.c`, and `bisect.c`, adding fatal error paths or propagating failures to callers. No new symbols or on-disk format changes are introduced. The patches are exercised by existing test suites, though no new coverage was added. Johannes has not yet responded to Junio’s feedback, and no other reviewers have weighed in. The next steps will likely involve addressing these correctness issues in a revised version.