This is a **substantive design discussion** in the final stages of Harald Nordgren’s `--delete-merged` series, specifically about refining the `--forked` filter’s logic and extending stacked-branch protection to `git branch -d`. Phillip Wood confirms his review of the rest of the series is complete, clearing the way for Harald to prepare the final version (v19) with only mechanical pre-merge fixes remaining.

The email centers on two key points:
1. **`--forked` filter refinement**: Phillip’s proposed heuristic—simulating a push operation (apply push refspec, then fetch refspec) and only treating a branch as forked if the result does *not* match its upstream—has been accepted by Harald. This addresses the current implementation’s misclassification of long-lived branches (e.g., `main`, `next`, `seen`) when `remote.pushDefault` is set. The fix is acknowledged as sound and will be implemented, but it is **not a blocker for v18** and can be added as a follow-up. The series remains ready for merging once the mechanical flags cleanup (patches 2-4/7) and `--dry-run --quiet` rejection are applied.

2. **Stacked-branch protection extension**: Junio C Hamano has requested that the refined abort-and-clear protection (which prevents deleting branches used as upstreams for unmerged branches) be applied to `git branch -d` as well, not just `--delete-merged`. This ensures consistency across the branch deletion commands. The change is expected to be straightforward, as the protection logic is already implemented in shared code.

Phillip’s confirmation that his review is otherwise complete means the only remaining work before merging is:
- The agreed mechanical fixes (flags cleanup, `--dry-run --quiet` rejection).
- Extending stacked-branch protection to `git branch -d`.
- Optional: Documenting the `--forked` filter’s edge case (branches without `branch.<name>.pushRemote` misclassified when `remote.pushDefault` is set) as a known limitation.

The series is **implementation-complete** and addresses all prior feedback, including per-branch opt-out, `--dry-run` preview, and refined stacked-branch protection. The proposed `--forked` filter improvement is a forward-looking enhancement that does not affect the current safety or functionality. Future summarizers should note this as a potential follow-up to improve the filter’s precision.