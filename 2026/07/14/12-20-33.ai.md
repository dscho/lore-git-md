This is the second patch in a two-patch test modernization series for `t/t1100-commit-tree-options.sh`. It moves the creation of the `expected` output file into the existing setup test (`test_expect_success 'test preparation: write empty tree'`), replacing a top-level here-doc that ran outside the test harness. The change uses `<<-EOF` to allow indentation and ensures the operation is protected by the test harness, preventing silent failures. The diff shows a straightforward relocation of the here-doc block with no functional changes—only stylistic improvements to align with current Git test conventions.

The patch addresses Junio’s feedback from v1, which requested a present-tense commit message (already implemented in this version), and follows the pattern Eric Sunshine recommended for similar test modernizations. The author, a new contributor, has verified the script still passes all 5 tests after the change. This is a purely mechanical, uncontroversial cleanup with no impact on test coverage or behavior, and it completes the series’ goal of modernizing the script’s style. The series is now ready for `next` pending Junio’s final confirmation.