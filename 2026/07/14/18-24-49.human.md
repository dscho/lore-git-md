This is the third patch in Harald Nordgren’s **v19 `git branch --delete-merged` series**, which is now implementation-complete and ready for merging. The series introduces a safe, automated local-branch cleanup command with comprehensive filtering and safety controls, including per-branch opt-out, `--dry-run` preview, and refined stacked-branch protection.

This specific patch adds a **skip-unmerged mode** to the `delete_branches()` and `check_branch_commit()` functions, allowing bulk callers (like the new `--delete-merged` command) to silently skip unmerged branches instead of erroring out with the standard "use `git branch -D`" advice. This is a **refactoring change** that prepares the API for the core `--delete-merged` functionality in patch 5/7, where skipping unmerged branches is a deliberate design choice rather than an error condition. Existing callers (e.g., `git branch -d`) are unaffected, as the new `DELETE_BRANCH_SKIP_UNMERGED` flag is not set by default.

The change is **mechanical and behavior-preserving** for current users, but it’s a key enabler for the series’ safety architecture. By allowing the `--delete-merged` command to skip unmerged branches without failing, it ensures that automated cleanup can proceed smoothly while still respecting Git’s merge-checking rules. The patch is **well-explained** in the commit message and aligns with the series’ broader goal of providing precise, safe branch management.

This patch is part of a **flags-based API refactoring** (patches 2–4/7) that replaces boolean arguments with an `unsigned int flags` parameter, improving maintainability and extensibility. The v19 series has resolved all prior feedback, including a maintenance risk in earlier versions where local boolean caching could lead to stale values—this patch avoids that issue by ensuring the new flag is the single source of truth. The series is **ready for merging**, with no known issues remaining.