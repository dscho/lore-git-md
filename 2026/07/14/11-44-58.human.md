This is a refactoring patch that improves code hygiene in preparation for the larger `remote-object-info` series. The patch moves loop counter declarations (`int i`) inside their respective `for()` statements in `builtin/cat-file.c` (three loops) and `fetch-pack.c` (one loop). Where appropriate, the counters are also changed from `int` to `size_t` to match loop bounds like `nr` or `ARRAY_SIZE()`, eliminating signed/unsigned comparison warnings.

The changes are purely mechanical and localized, with no behavior modification. They follow Git's coding style by keeping variable declarations close to their use and using the correct type for loop indices. This patch is part of the v17 series that has addressed all substantive review feedback and is now considered ready for merging to `next`. The improvements here are minor but contribute to the overall code quality of the series.