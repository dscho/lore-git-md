This is the penultimate patch in the seventeenth iteration of a security-hardened feature series that adds the `remote-object-info` command to `git cat-file --batch-command`. The patch implements the user-facing functionality that allows querying object metadata (currently just size) from remote repositories without downloading full objects.

The implementation adds a new `remote-object-info` command that works with all three transport protocols (`git://`, `file://`, `http://`) when the server advertises the `object-info` capability. Key features include:

- Multi-object batching: accepts multiple object IDs in a single command to minimize network overhead
- Input validation: enforces strict limits (10,000 objects max, 8KB URL length) and validates OIDs
- Transport support: works with smart HTTP, git, and file protocols
- Integration: maintains compatibility with existing `--buffer` mode and batch-command workflow
- Error handling: properly handles unsupported capabilities, malformed OIDs, and missing objects
- Memory management: uses the new `free_object_info_contents()` helper for cleanup
- Security: strict protocol v2 enforcement, capability checks, and input size limits

The patch includes comprehensive test coverage (680 lines) in `t/t1017-cat-file-remote-object-info.sh` that verifies functionality across all transport types, edge cases, and error conditions. The implementation follows Git's established patterns while introducing the necessary changes to support remote queries in the batch-command workflow.

This patch completes the series by providing the actual user-facing functionality that was prepared in earlier patches. The implementation appears well-crafted with careful attention to security, compatibility, and maintainability. All technical issues from previous versions have been resolved, and the series is now ready for merging to `next`.