This is the first patch in an 11-part bugfix series addressing unchecked return values flagged by Coverity. It targets the HTTP transport layer in `http.c`, where `get_active_slot()` previously ignored the possibility that `curl_easy_duphandle()` could return NULL on memory allocation failure. The fix is minimal: a two-line addition that checks for NULL and calls `die()` with a clear error message. The impact is broad—every HTTP operation in Git (clone, fetch, push, bundle-uri downloads) goes through this function, so a silent failure here could crash the process when `curl_easy_setopt()` is later called with a NULL handle.

The patch is well-motivated and uncontroversial. The commit message clearly explains the risk (undefined behavior leading to crashes) and the rationale for `die()` (no reasonable recovery from out-of-memory conditions). The change is mechanical and self-contained, touching only the error path without altering behavior or on-disk formats. This is exactly the kind of low-risk hardening that static analysis tools excel at identifying.

As part of the larger series, this patch sets the tone: small, focused fixes that close silent failure paths. The HTTP subsystem is a critical dependency for many users, so plugging this hole is a clear win. No review feedback has appeared yet, but the patch looks ready to merge as-is. If you care about HTTP stability or Coverity-driven hardening, this is worth a quick skim.