This is the **final patch (7/7) in Pablo Sabater’s v11 series** implementing visual root indentation for `git log --graph`. Chandra Pratap’s reply is a **surface-level review** that catches two typos in the documentation and test files: "deafault" instead of "default" in `Documentation/rev-list-options.adoc`, and "graph.indent" instead of "log.graphIndent" in the same file. The review also notes that the test comment `# graph.indent true and no --option is the default state` should use `log.graphIndent` instead of `graph.indent`.

The feedback is **purely mechanical** and does not engage with the patch’s substance. The patch itself remains **ready for final review**, with no unresolved technical or usability questions. The typos are trivial to fix and will not affect the patch’s functionality or integration. The series as a whole is **technically complete**, with all design decisions resolved and no further changes planned by the author. The only remaining step is Junio Hamano’s final review before the series can proceed to `next`.