Thread identity: Refactoring patch to remove redundant `strbuf_reset()` call in `strbuf_getwholeline()` when `HAVE_GETDELIM` is defined. Originating author: René Scharfe. Thread type: standalone patch (cleanup/refactoring).

Core problem or goal: Eliminate an unnecessary `strbuf_reset()` call in `strbuf_getwholeline()` for the `HAVE_GETDELIM` code path, as `getdelim(3)` does not depend on the buffer's prior state and the function already handles buffer initialization/reset on both success and error paths. The change is purely a performance micro-optimization with no behavioral impact. Historical context: the redundant call was introduced to address a missing reset on error in the original implementation, but that issue was fixed in commit `b70904306f` (2016), making the preparatory reset unnecessary.

Current status: Accepted for integration; queued in the `next` branch (2025-XX-XX, date unknown) for integration testing. No prior versions or related series. Not yet merged to `master`.

Key technical details:
- File: `strbuf.c`
- Function: `strbuf_getwholeline()`
- Macro: `HAVE_GETDELIM`
- System call: `getdelim(3)`
- Removed call: `strbuf_reset(sb)` (two lines deleted)
- Subsystem: `strbuf` (core string-handling utility)
- No new/renamed symbols, on-disk format changes, or CLI/config changes.
- Test coverage: Existing tests exercise the function; no new tests added.
- Historical commit: `b70904306f` (2016) fixed the original missing reset on error.

Open questions and loose ends: None. The maintainer has confirmed the rationale and accepted the patch.

Participant positions:
- René Scharfe (author): Proposed the change as a safe cleanup with no behavioral impact.
- Junio C Hamano (maintainer): Reviewed and accepted the patch, marking it for `next`. Confirmed that `getdelim(3)` does not depend on the buffer's prior state and that the function's existing error/success paths handle initialization/reset.
- Jeff King (Peff): Surface-level review confirming the patch’s correctness and providing historical context (commit `b70904306f`). The review validates the rationale but does not engage with broader implications or edge cases.

Related work: None. This is a standalone refactoring unrelated to broader efforts (e.g., `the_repository` removal, ODB abstraction).