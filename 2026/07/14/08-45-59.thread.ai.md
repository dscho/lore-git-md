Thread identity: Refactoring patch to remove redundant `strbuf_reset()` call in `strbuf_getwholeline()` when `HAVE_GETDELIM` is defined. Originating author: René Scharfe. Thread type: standalone patch (cleanup/refactoring).

Core problem or goal: Eliminate an unnecessary `strbuf_reset()` call in `strbuf_getwholeline()` for the `HAVE_GETDELIM` code path, as `getdelim(3)` does not depend on the buffer's prior state and the function already handles buffer initialization/reset on both success and error paths. The change is purely a performance micro-optimization with no behavioral impact.

Current status: Under review (initial patch posted). No prior versions or related series. No merge status reported (not in `pu`, `next`, or `master`).

Key technical details:
- File: `strbuf.c`
- Function: `strbuf_getwholeline()`
- Macro: `HAVE_GETDELIM`
- System call: `getdelim(3)`
- Removed call: `strbuf_reset(sb)` (two lines deleted)
- Subsystem: `strbuf` (core string-handling utility)
- No new/renamed symbols, on-disk format changes, or CLI/config changes.
- Test coverage: Existing tests exercise the function; no new tests added.

Open questions and loose ends:
- None raised yet. Potential review focus: confirming `getdelim` cannot depend on the buffer's prior allocation state (edge case validation).

Participant positions:
- René Scharfe (author): Proposes the change as a safe cleanup with no behavioral impact.

Related work: None. This is a standalone refactoring unrelated to broader efforts (e.g., `the_repository` removal, ODB abstraction).