This is the second patch in a two-patch test modernization series for `t/t1100-commit-tree-options.sh`. The patch moves the creation of the `expected` output file into the existing setup test, ensuring it runs under the test harness. This is a pure style improvement with no behavioral changes; all tests continue to pass.

The change addresses a long-standing best practice: code outside test blocks is not protected by the test harness, so failures there can go unnoticed. By relocating the here-doc into the setup test and using `<<-EOF` for indentation, the patch makes the script more robust while keeping the expected output identical. The commit message is written in present tense as Junio requested in his review of v1.

This patch is uncontroversial and ready for `next`. It follows the same pattern as other test modernizations (e.g., `t7001`) and has already incorporated all prior feedback—Junio’s commit message tense adjustment and Patrick Steinhardt’s formatting nit in the first patch. The author, a new contributor, has verified the changes and confirmed the script still passes all five tests. No further discussion is expected.