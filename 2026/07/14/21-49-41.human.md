This is a surface-level review of René Scharfe’s refactoring patch to remove a redundant `strbuf_reset()` call in `strbuf_getwholeline()`. Jeff King (Peff) confirms the change is correct and traces the redundancy to a 2016 commit (`b70904306f`) that already fixed the error-path reset logic. His reply is brief and purely confirmatory—no new concerns, edge cases, or behavioral questions are raised.

The patch itself remains a straightforward cleanup: two lines removed from `strbuf.c`, no behavior change, and no follow-up work needed. It is already queued in `next` and looks ready to graduate to `master`. For a reader scanning the digest, this is a low-risk, uncontroversial refactoring that can safely be ignored unless they are specifically tracking micro-optimizations in the `strbuf` subsystem.