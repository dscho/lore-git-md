This is patch 6/7 in the v12 series implementing visual root indentation for `git log --graph`. The patch refactors configuration reading in `graph.c` by extracting the `log.graphcolors` logic from `graph_init()` into a new helper function, `graph_read_config()`. This is a purely internal change with no user-visible behavior difference.

The patch is preparatory work for patch 7/7, which introduces the `log.graphIndent` configuration option. By moving config reading into a dedicated function, it maintains a clean separation of concerns and simplifies future additions of graph-related configuration. The implementation is mechanical: the new `graph_read_config()` function is called at the start of `graph_init()`, preserving the existing initialization order. The diff shows 9 lines added and 4 removed, all within `graph.c`.

This patch has been stable since v7 and has no unresolved feedback. It is uncontroversial and ready for final review. The thread context confirms that all mechanical feedback for the series has been addressed in v12, and this patch is explicitly marked as ready for integration. No test changes are needed, as the refactoring does not alter functionality. The patch is a low-risk prerequisite for the opt-out mechanism in patch 7/7.