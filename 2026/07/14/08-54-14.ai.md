This is a surface-level review of Christian Couder’s patch to standardize the `git fast-export` usage string and man-page SYNOPSIS.

Patrick Steinhardt’s reply confirms the patch’s direction: it aligns the help text (`git fast-export -h`) and man-page SYNOPSIS with Git’s conventional pattern for revision arguments (`[<options>] [<revision-range>] [[--] <path>...]`), replacing the unusual `| 'git fast-import'` phrasing in the man page and the `[<rev-list-opts>]` placeholder in the usage string. The patch also removes `fast-export` from the list of commands that trigger a test failure in `t/t0450` due to mismatched help vs. documentation.

The review is purely surface-level, focusing on consistency and test alignment rather than functionality. Patrick’s comments are brief and approving, noting only that the change makes sense and matches the pattern used by other Git commands. No substantive concerns or edge cases are raised; the patch is a straightforward cleanup with no behavior change. It remains queued in Junio’s `seen` branch as of the last update.