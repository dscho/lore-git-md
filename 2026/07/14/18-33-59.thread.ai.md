Thread identity: inquiry about performance of the `git last-modified` command (Toon Claes, 2025), comparing its speed to a hand-rolled `git log` pipeline on the ziglang/zig repository.

Core problem or goal: determine why `git last-modified -z -t --max-depth=0` is 2.5× slower (66 ms vs 26 ms) than an equivalent `git log --name-status -c --format=... --parents --no-renames -t -z` pipeline when both are run against the same commit and path filter (`doc/langref/`) in a repository with a precomputed commit-graph and changed-path Bloom filters.

Current status: initial inquiry; no decisions, patches, or reviews yet. The command is under evaluation by a Forgejo contributor (Gusted) but has not been merged or queued in any integration branch.

Key technical details: command under test is `git last-modified` (authored by Toon Claes); benchmark repository is ziglang/zig; both commands use the same commit and path filter; repository has commit-graph with changed-path Bloom filters; measured wall-clock times are 66 ms (last-modified) vs 26 ms (log pipeline).

Open questions and loose ends: (1) Is the 2.5× performance gap expected? (2) Is there a more efficient invocation of `git last-modified` that closes the gap? (3) Can the command itself be optimized to match the hand-rolled pipeline’s speed?

Participant positions: none yet; only the initial inquiry from Gusted (Forgejo contributor).

Related work: none mentioned.