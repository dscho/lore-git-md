This is the second patch in a **test modernization** series (v2) targeting `t/t1100-commit-tree-options.sh`. It addresses Junio’s feedback on the commit message tense by relocating the here-doc that generates the `expected` file into the existing setup test (`test_expect_success 'test preparation: write empty tree'`). The change is purely stylistic: the here-doc is now indented and protected by the test harness, preventing silent failures if it fails to execute. The commit message uses present tense ("is created") as requested, and the patch maintains identical test behavior and coverage.

The diff shows the `expected` file’s content moving from the script’s top-level into the setup test, with the here-doc body indented using `<<-EOF` to align with the surrounding test code. No new symbols or behaviors are introduced, and the patch follows the established pattern for test modernization (e.g., `t7001`). The series remains ready for `next` pending Junio’s confirmation of the commit message style.

Key identifiers: test script `t/t1100-commit-tree-options.sh`, setup test `test preparation: write empty tree`, here-doc relocation, and `expected` file generation. The patch is uncontroversial and part of the ongoing effort to modernize legacy test scripts.