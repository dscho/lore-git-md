This is the first patch in Pablo Sabater’s **v12 series** implementing **visual root indentation for `git log --graph`**, a feature that indents commits with excluded parents to clarify ancestry relationships. This patch is **pure test infrastructure refactoring** and has already been **merged** (as noted in the thread summary).

The patch moves the duplicated `check_graph` helper function from `t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh` into `t/lib-log-graph.sh`, renaming it to `lib_test_check_graph`. The change consolidates **34 lines of duplicated code** into a **5-line helper**, eliminating redundancy while preserving backward compatibility. Both test files already import `lib-log-graph.sh`, so the refactoring is a natural fit.

The motivation is straightforward: the new test file (`t4218-log-graph-indentation.sh`) introduced in later patches needs this helper, and consolidating it now avoids duplication. The patch is **mechanically clean**—no functional changes, no new edge cases, and no dependencies on later patches. Karthik Nayak has already **Acked** it, and the diff shows only the expected relocation of code.

For a busy reader, this patch is **low-stakes but necessary**. It’s a textbook example of test hygiene: small, self-contained, and uncontroversial. The real action in the series starts with patch 2/7, but this sets the stage for the new tests that will validate the indentation feature. No further review is needed here.