This is the final patch in Tian Yuchen’s v12 series migrating global configuration variables into `struct repo_config_values`. It’s a one-line documentation tweak in `repository.h` that updates a stale comment.

The old comment claimed all values in `struct config_values_private_` were parsed by `git_default_config()`, but earlier patches in the series moved several variables (like `pager_program`) to their own config parsers. The new wording—“Repository-specific configuration values”—is accurate and future-proof.

No code changes, no behavior changes, and no controversy; this is pure housekeeping. The patch is ready to merge alongside the rest of the series.