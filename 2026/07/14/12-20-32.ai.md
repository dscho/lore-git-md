This is the first patch in a two-part test modernization series targeting `t/t1100-commit-tree-options.sh`. The patch converts the script from the legacy `test_expect_success` style—where the test title and body are passed as separate backslash-continued arguments—to the modern style, where the body is a single-quoted block indented with tabs. The change is purely stylistic and follows established patterns for test script modernization (e.g., `t7001`). No test logic or behavior is altered, and all five tests continue to pass.

The patch also incorporates a minor formatting suggestion from Patrick Steinhardt: removing an extraneous blank line between the last two `test_expect_success` blocks. This aligns with the project’s preference for compact, consistent test scripts. The diff shows the conversion clearly, with the old style replaced by the modern quoted-body form, and the blank line removed.

The file touched is `t/t1100-commit-tree-options.sh`, a script testing `git commit-tree` options. The changes are mechanical and do not introduce new symbols, behaviors, or edge cases. The modernization ensures the script adheres to current Git test style guidelines, improving readability and maintainability without affecting functionality.

This patch is part of a broader effort to modernize legacy test scripts, a recurring theme in Git development. The series has already addressed all prior feedback, including Junio’s request for a present-tense commit message in the second patch. The author, a new contributor, has verified the changes and confirmed the tests pass after each iteration.

The patch is uncontroversial and ready for inclusion in `next`. Prior reviews focused on surface-level improvements—formatting and style—rather than technical correctness, which was already established. The series is unlikely to generate further discussion, as it aligns with long-standing project conventions.