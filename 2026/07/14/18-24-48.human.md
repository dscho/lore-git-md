This is part 2 of Harald Nordgren’s **seven-patch series** implementing `git branch --delete-merged`. The series is now **implementation-complete and ready for merging**, with all design questions resolved and mechanical pre-merge fixes applied in v19.

This specific patch refactors the branch-deletion API to use a **flags-based argument** instead of separate boolean parameters (`force`, `quiet`). The change is purely internal: it replaces two booleans with an `unsigned int flags` and an enum (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`), ensuring that flags remain the single source of truth and eliminating a maintenance risk where cached boolean values could become stale. The patch **introduces no behavioral changes**—existing callers (like `git branch -d`) continue to work exactly as before.

The refactoring is a prerequisite for later patches in the series, which add new flags (`DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`) to support the `--delete-merged` command and its safety mechanisms. The patch is **well-executed**: it directly tests the flags at each use site, avoiding any local caching, and the enum values are clearly named. The commit message is concise and accurately describes the mechanical nature of the change.

This is a **refactoring patch** that lays the groundwork for the core `--delete-merged` functionality. It is **low-risk** and **uncontroversial**, addressing a technical debt issue noted in earlier reviews. The series as a whole is **well-motivated** (safe automated branch cleanup) and **thoroughly tested**, with this patch being a necessary but unexciting step toward that goal.