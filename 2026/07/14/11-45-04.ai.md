This is a refactoring patch in the `remote-object-info` series, specifically part of the state machine preparation work. The patch moves three variable initializations (`allow_unadvertised_object_request`, `use_sideband`, and `args->deepen`) from the `FETCH_CHECK_LOCAL` state to the function prologue of `do_fetch_pack_v2()` in `fetch-pack.c`. This mechanical change ensures the variables are set regardless of the initial state, which becomes necessary when a later patch introduces an alternative initial state for the `remote-object-info` command.

The moved code block is identical before and after, and the change is purely preparatory with no behavior modification. The patch touches only one file (`fetch-pack.c`) and is uncontroversial, having been reviewed and helped by Jonathan Tan and Christian Couder. This is the eighth patch in the v17 series (originally patch 8/13 in v16) and remains unchanged from the previous version, confirming its stability.

The change directly enables the `remote-object-info` command implementation in patch 10/10 by allowing the state machine to start from a different initial state while maintaining consistent variable initialization. The commit message clearly explains the motivation and confirms there is no behavior change, which aligns with the established pattern for preparatory refactoring in this series.