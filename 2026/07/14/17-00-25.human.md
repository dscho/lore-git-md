This is the final stretch of Harald Nordgren’s `--delete-merged` series, now implementation-complete and ready for merging. The email captures a small but important design decision: Junio C Hamano has requested that the refined stacked-branch protection (abort-and-clear) be extended to `git branch -d` for consistency, and Harald has agreed to implement this as a follow-up after the series lands.

The series itself is in excellent shape—all planned features are implemented, including per-branch opt-out, `--dry-run` preview, the `--forked` filter, and the core `--delete-merged` command with robust safety checks. The only remaining pre-merge tasks are mechanical fixes (flags cleanup in patches 2-4/7, rejecting `--dry-run --quiet`, and now this `git branch -d` extension). Phillip Wood has confirmed his review is otherwise complete, so this is not a blocker for merging.

For a busy reader, the takeaway is clear: this is a well-crafted, safety-focused addition to Git’s branch management tooling, addressing a real workflow pain point (automated cleanup of merged local branches) with careful attention to edge cases. The stacked-branch protection extension to `git branch -d` is a logical consistency improvement that won’t require significant rework. The series is effectively done and should land soon.