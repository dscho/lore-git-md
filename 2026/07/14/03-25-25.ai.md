This is the final patch in Tian Yuchen's v12 series migrating global configuration variables into `struct repo_config_values`. It adjusts a stale comment in `repository.h` to reflect that not all fields in `config_values_private_` are parsed by `git_default_config()`—for example, `pager_program` is now handled in `pager.c`. The change is trivial (one line) but necessary for accuracy as the series completes the migration of eleven variables out of globals and into per-repository storage.

The patch is purely documentary and has no functional impact. It resolves a minor inconsistency introduced by the earlier patches in the series, ensuring the comment aligns with the current code organization. No further technical feedback is expected or required; this is a clean-up follow-up to the substantive work in patches 1–9. The series as a whole is now ready for `next`, with all prior feedback addressed and no open technical concerns.