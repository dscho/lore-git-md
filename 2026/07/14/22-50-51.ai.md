This is Pablo Sabater clarifying next steps after Junio Hamano reverted the merge of v16 due to an attribution mismatch in the cover letter. The technical content of the series is complete and ready—v17 addressed all prior feedback, including Junio’s recent request to add bounds-checking in the final patch (10/10). Pablo is asking whether to send a v18 (a full resend of the series with the cover letter fixed and the bounds-checking added) or a single patch on top of `next` (which would only contain the bounds-checking fix, since the cover letter issue is administrative and doesn’t affect the code). The fix itself is trivial: verify `object_info_values.nr > 1 + size_index` before accessing the array in the response-parsing loop. No other changes are needed. The series remains a **security-hardened feature** implementing `git cat-file --batch-command remote-object-info` with dynamic capability-based placeholder validation, comprehensive test coverage (699 lines), and verified memory safety.