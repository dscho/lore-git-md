Thread identity: build system regression report; thread type: bug report; originating author: Kurt Mielke.

Core problem or goal: Git 2.55.0 fails to build from source on AlmaLinux 10 due to an unconditional requirement for `cargo` (Rust toolchain), which was not present in Git 2.54.0. The user reports the build halts at `CARGO target/release/libgitcore.a` (Makefile line 3021, error code 127) and is bypassed by setting `NO_RUST=1`.

Current status: under investigation; no maintainer response yet. The failure is linked to the ongoing Rustification effort but it is unclear whether the Rust requirement is intentional or an accidental build system regression.

Key technical details: build system (Makefile), Rust toolchain (`cargo`), `NO_RUST=1` as a workaround, no new symbols or on-disk format changes, no test coverage implications reported.

Open questions and loose ends: whether the Rust requirement is intentional for 2.55.0, whether the build system should auto-detect Rust toolchain availability, whether `NO_RUST=1` is a supported configuration, and what platforms are expected to provide Rust tooling by default.

Participant positions: Kurt Mielke (reporter) describes the failure and workaround; no other participants have weighed in yet.

Related work: Rustification effort (Ezekiel Newren), build system (D. Ben Knoble), platform portability (Randall S. Becker, Ramsay Jones).