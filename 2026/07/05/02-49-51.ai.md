This is the **author's follow-up** to the maintainer's acceptance email for the `greplint.pl` series, addressing Junio C Hamano's minor phrasing quibble in the `t/README` documentation. Michael Montalbo acknowledges that the term "guarded" in the guidance for when to use bare `grep` (e.g., "plain guarded 'grep'") is confusing and agrees to simplify it to "plain `grep`" in the next revision. He clarifies that the intent was to distinguish `grep` from `test_grep` (which explicitly rejects missing file arguments with a `BUG`), but the wording risked implying an anti-pattern (e.g., using `grep` to test for file existence).

The change is purely cosmetic and does not affect the series' technical content or approval status. The maintainer's earlier concerns about edge cases (e.g., reftable backend compatibility, NTFS 8.3 short names) and the audit of `# lint-ok:` exemptions remain resolved. This email is a final polish step before the series graduates to `next`. Key files: `t/README` (documentation update), with no other code changes expected. The tone is collaborative and low-stakes, focusing on clarity rather than functionality.