Trevor Gross’s `git rebase -i -x` patch series, which adds a `-x` flag to interactive rebase commands to append "(cherry picked from commit ...)" trailers, remains stalled on fundamental design objections. The series aims to let users record commit origins during rebases without disrupting the flow with `exec git cherry-pick -x`, but Junio C Hamano, Jeff King (Peff), and Phillip Wood have all challenged its conceptual alignment with `git rebase`’s core purpose—moving, not duplicating, history. Junio’s latest reply doubles down on this critique, explicitly endorsing Peff’s and Phillip’s view that the annotation loses meaning when original commits are discarded, reinforcing the philosophical divide between rebase and cherry-pick workflows.

The thread has pivoted toward Peff’s alternative: extending `git cherry-pick` with an interactive mode (`-i`), which would leverage its existing sequencer backend and avoid the semantic inconsistencies of Trevor’s approach. Junio’s brief nod to Phillip’s suggestion of adding `--exec` to `cherry-pick` (mirroring `git rebase --exec`) further highlights usability gaps that could be addressed alongside an interactive mode. Trevor now faces a clear choice: either justify why `rebase` is the right tool for this workflow—answering Phillip’s pointed question about `cherry-pick`’s limitations—or abandon the current patch series in favor of Peff’s proposal. The technical path for `cherry-pick -i` appears straightforward, as Peff’s earlier review confirmed the sequencer already generates a todo file, but the design discussion remains unresolved. No version updates or merge signals have emerged; the series remains under review with its future dependent on Trevor’s next move.