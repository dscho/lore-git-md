Thread identity: patch introducing `--sstat` as a hidden synonym for `git diff --compact-summary`, authored by Junio C Hamano.

Core problem or goal: add a shorter, more convenient command-line option (`--sstat`) for the existing `--compact-summary` diff flag to improve usability in the maintainer's personal workflow.

Current status: patch posted, under review. Phillip Wood has raised a principled objection to the change, arguing the alias is cryptic and unnecessary. No prior versions or series context; this is the initial submission.

Key technical details:
- Files touched: `diff.c` (option parser), `diff-options.adoc` (documentation), `t4000-diff-format.sh` and `t4013-diff-various.sh` (test suite).
- New symbol: `--sstat` CLI option (hidden, no new callback logic; reuses `diff_opt_compact_summary`).
- Behavior: `--sstat` is an exact synonym for `--compact-summary`; no change to existing output format or logic.
- On-disk format: unchanged.
- Test coverage: two new test files verify `git diff-tree --sstat` output format.

Open questions and loose ends: whether the alias is justified. Phillip Wood objects to the alias as cryptic and unnecessary, noting it could be confused with `--shortstat` and does not meaningfully reduce typing effort compared to tab-completing `--compact-summary`. No technical flaws or alternative proposals have been raised.

Participant positions:
- Junio C Hamano: author; proposes the alias for personal workflow convenience.
- Phillip Wood: reviewer; opposes the change on grounds of UI clarity and maintainability, arguing the alias adds clutter without sufficient benefit. Review is substantive (engages with usability and design trade-offs).