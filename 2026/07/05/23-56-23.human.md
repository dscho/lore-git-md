This is a build system discussion about a regression in Git 2.55.0. D. Ben Knoble, a build system contributor, replies to Kurt Mielke’s report that Git 2.55.0 now unconditionally requires `cargo` (the Rust toolchain) to build, whereas 2.54.0 did not.

Ben confirms that Git 2.55.0 is the first release to default to building with Rust enabled, but notes that Rust remains optional via the `NO_RUST=1` flag. He references the ongoing Rustification effort and the plan to make Rust a requirement for Git 3.0, though he is unsure of the exact timeline or whether there has been discussion about delaying the mandate for platforms without Rust support. He suggests that AlmaLinux provides Rust packages, implying that installing them would resolve the build failure.

The email does not address whether the build system should auto-detect Rust toolchain availability or whether `NO_RUST=1` is a supported long-term configuration. It also does not clarify whether the change was intentional or an oversight. The tone is matter-of-fact, treating the issue as a known transition rather than a bug. No further technical details or proposed fixes are offered.