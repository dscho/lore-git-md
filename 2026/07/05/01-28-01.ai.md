This is Junio C Hamano's one-line confirmation that Patrick Steinhardt's v2 six-patch series refactoring `struct object_info` has been queued for merging. The series replaces the `whence` field with an opt-in `struct odb_source_info` to enable multi-source object resolution and pluggable ODB backends, and was previously conceptually approved with all patches reviewed. The v2 iteration resolved naming ambiguity (`struct object_info_source` → `struct odb_source_info`, `sourcep` → `source_infop`) and added documentation (patch 6/6).

The series is now in Junio's integration pipeline and will appear in `next` shortly, then graduate to `master` after cooking. No further discussion or changes are expected—this is a routine queueing notice. If you're tracking the ODB abstraction effort, this is the final administrative step before the refactor lands.