This is a **substantive review** from Junio C Hamano on Trevor Gross’s `pick -x` feature for `git rebase -i`. The review challenges the core design premise of the patch by questioning whether the feature aligns with the fundamental purpose of `git rebase`.

Junio argues that `git rebase` and `git cherry-pick` serve fundamentally different workflows: rebase is about *moving* history (with the intent to abandon the original commits), while cherry-pick is about *duplicating* history (preserving the original commits). The `-x` flag in `cherry-pick` is useful precisely because the original commit remains accessible, but in a rebase, the original commits are meant to be discarded. This creates a conceptual mismatch: if the original commit is gone, the "cherry picked from commit ..." line becomes meaningless or even misleading.

The review also highlights a technical distinction: rebase copies notes from the original commits to the rewritten ones (since the new commits replace the old), whereas cherry-pick does not (since the original commits are preserved). This reinforces the idea that rebase and cherry-pick are not interchangeable, and adding `-x` to rebase might encourage a workflow that contradicts its design.

Junio does not outright reject the feature but frames this as a fundamental question that must be addressed before the patch can proceed. The ball is now in Trevor’s court to either justify why `-x` belongs in rebase despite this tension, propose an alternative design (e.g., a different way to track cherry-picks during rebase), or abandon the effort. The thread’s future hinges on whether the author can reconcile the feature with rebase’s core semantics. No code-level feedback is provided here; this is purely a design-level critique.