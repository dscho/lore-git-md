This is a build system regression report. The user Kurt Mielke reports that Git 2.55.0 fails to build from source on AlmaLinux 10 because the build process now requires `cargo` (the Rust package manager), which is not present on their system. Git 2.54.0 built successfully without Rust tooling. They worked around the issue by setting `NO_RUST=1` during the build, but suggest that Rust should not be a default dependency.

The report is minimal: no bisect was performed, no commit is identified, and the motivation for the Rust requirement is not discussed. The build failure occurs at the `CARGO target/release/libgitcore.a` step, indicating that the build system now assumes Rust toolchain availability. This aligns with the ongoing Rustification effort (introducing Rust code into Git), but the email does not clarify whether this is an intentional new requirement or an accidental omission in the build system logic.

Key technical details: the failure is in the Makefile at line 3021, the error code is 127 (command not found), and the workaround (`NO_RUST=1`) successfully bypasses the Rust build step. The user’s environment has no commands beginning with "rus", confirming the absence of Rust tooling. No other build issues are reported.