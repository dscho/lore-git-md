Thread identity: patch series introducing `http.connectTimeoutMS` configuration option to control HTTP connection timeout at millisecond granularity. Originating author: GalaxySnail (via GitGitGadget). Thread type: feature patch series (single patch).

Core problem or goal: allow users to override libcurl's default 300-second connection timeout (DNS, TCP, TLS/QUIC handshakes) with a millisecond-precision value via Git configuration (`http.connectTimeoutMS`) or environment variable (`GIT_HTTP_CONNECT_TIMEOUT_MS`).

Current status: initial version posted (`[PATCH]`). Under review; no prior versions. No maintainer decisions or merge status reported.

Key technical details:
- Files touched: `http.c` (new config variable, parsing, libcurl integration), `Documentation/config/http.adoc` (documentation), `t/t5585-http-connect-timeout.sh` (new test script), Meson build system (test registration).
- New symbols: `http.connectTimeoutMS` (config key), `GIT_HTTP_CONNECT_TIMEOUT_MS` (environment variable).
- Behavior: overrides libcurl's `CURLOPT_CONNECTTIMEOUT_MS`; follows existing patterns for HTTP config options (e.g., `http.lowSpeedLimit`/`http.lowSpeedTime`).
- Test coverage: positive cases (valid numeric values), edge cases (zero, empty, non-numeric values), environment variable handling.

Open questions and loose ends: none identified in the initial patch. No review feedback yet; no objections or requests for changes.

Participant positions: none recorded (thread root).

Related work: none mentioned.