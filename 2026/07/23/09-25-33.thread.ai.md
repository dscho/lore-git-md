Thread identity: patch series introducing `http.connectTimeoutMS` configuration option to control HTTP connection timeout at millisecond granularity. Originating author: GalaxySnail (via GitGitGadget). Thread type: feature patch series (single patch).

Core problem or goal: allow users to override libcurl's default 300-second connection timeout (DNS, TCP, TLS/QUIC handshakes) with a millisecond-precision value via Git configuration (`http.connectTimeoutMS`) or environment variable (`GIT_HTTP_CONNECT_TIMEOUT_MS`). The feature aims to address use cases where users need to fail fast on unreachable hosts, such as in CI pipelines, automated scripts, or flaky networks.

Current status: initial version posted (`[PATCH]`). Under review; no prior versions. No maintainer decisions or merge status reported. The patch is well-structured but requires revisions to address Junio C Hamano's feedback on use case justification, zero-value behavior, and test script necessity.

Key technical details:
- Files touched: `http.c` (new config variable, parsing, libcurl integration), `Documentation/config/http.adoc` (documentation), `t/t5585-http-connect-timeout.sh` (new test script), Meson build system (test registration).
- New symbols: `http.connectTimeoutMS` (config key), `GIT_HTTP_CONNECT_TIMEOUT_MS` (environment variable).
- Behavior: overrides libcurl's `CURLOPT_CONNECTTIMEOUT_MS`; follows existing patterns for HTTP config options (e.g., `http.lowSpeedLimit`/`http.lowSpeedTime`). The patch currently ignores a zero value silently, but libcurl interprets zero as "use the default timeout." Junio suggests adjusting the condition to `0 <= curl_connect_timeout_ms` to make zero explicitly restore the default.
- Test coverage: positive cases (valid numeric values), edge cases (zero, empty, non-numeric values), environment variable handling. Junio questions the necessity of a dedicated test script (`t5585-http-connect-timeout.sh`), as the tests are parsing checks rather than real-world timeout behavior measurements.

Open questions and loose ends:
1. Use case justification: GalaxySnail must provide concrete scenarios (e.g., CI pipelines, automated scripts, flaky networks) to justify the need for millisecond-level control over the connection timeout.
2. Zero-value behavior: The patch must document what zero means for `http.connectTimeoutMS` and adjust the condition to `0 <= curl_connect_timeout_ms` so that zero explicitly restores the default timeout.
3. Test script necessity: GalaxySnail must either justify the new test script (`t5585-http-connect-timeout.sh`) or refactor the tests into an existing HTTP test file to conserve test numbers.
4. Procedural nit: The author’s real name should be used in the Signed-off-by trailer, per `Documentation/SubmittingPatches`.

Participant positions:
- **Junio C Hamano (maintainer)**: Reviewed the patch and raised three substantive points: (1) need for use case clarification, (2) ambiguity in zero-value behavior, and (3) necessity of a dedicated test script. His feedback is constructive and focused on usability and resource management. No objections to the feature itself, but revisions are required before merging.

Related work: none mentioned.