This is a **surface-level review** of the second patch in the `--reset-when-found` series for `git bisect`. Johannes Sixt points out several minor stylistic and correctness issues in the implementation and tests, all of which are mechanical rather than substantive.

The review focuses on three areas: (1) using Git’s standard error-message boilerplate for option conflicts (e.g., `options '%s' and '%s' cannot be used together`), (2) avoiding `git` in subshells within test helpers to prevent masking failures, and (3) chaining cleanup commands with `&&` in `test_when_finished` for consistency with existing test patterns. The feedback is purely about code hygiene and does not question the feature’s design or behavior.

The patch itself remains uncontroversial and is already marked for `next`. These are the kind of tweaks that are typically addressed in a final reroll before integration.