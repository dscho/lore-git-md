This is the second patch in a two-part bugfix series for `git mv`, and Junio C Hamano’s reply here is the maintainer’s review of it. The patch adds an early check for the existence of the destination directory, allowing `git mv --dry-run` to catch the problem before the actual rename syscall. Junio’s feedback is narrow: he accepts the TOCTOU race as unavoidable (“the best we can do”) but flags the overly deep indentation in the new code block as “horrible” and asks for it to be fixed. The rest of the patch—logic, tests, and edge-case handling—goes unchallenged, so the only action needed is a cosmetic cleanup. The series as a whole remains on track for merging once the indentation is tidied up.