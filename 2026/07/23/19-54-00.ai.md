This is a procedural update from Junio C Hamano on the config file locking timeout patch. The patch, which adds a configurable timeout (`core.configLockTimeout`, default 1000ms) for acquiring config file locks, is now explicitly cleared for merging to `next`. Junio confirms that all previously identified technical issues have been resolved, including the design tradeoffs around process-wide vs. per-repository timeout behavior, which were deferred to a follow-up patch. The patch remains procedurally ready for integration, with no blocking concerns—Junio’s note merely acknowledges that the topic had temporarily slipped through the cracks in his review queue.

The patch targets a real and well-documented problem: concurrent Git operations (e.g., `git worktree add -b`) failing due to transient lock contention on config files, particularly on slower filesystems like NTFS. It follows the established pattern of `core.packedRefsTimeout` and includes thorough test coverage for both immediate failure and retry scenarios. Johannes Schindelin’s earlier endorsement and Patrick Steinhardt’s review contributions signal broad technical acceptance, and the patch’s integration status is now solely a matter of Junio’s merge queue management.

Key technical details to track for follow-up discussion: the timeout is implemented with a process-wide cache, making it effectively global despite being passed as a repository parameter. This design choice was debated but deferred, with Junio preferring a simpler process-wide approach and Patrick advocating for the current implementation. The patch’s documentation explicitly covers the read-after-lock semantics and last-writer-wins behavior, addressing earlier reviewer questions. No changes to the patch itself are implied by this email—it is a green light for merging.