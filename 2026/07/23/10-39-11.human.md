This is a **brief technical reply** from Jeff King (Peff) in the ongoing discussion about the `git receive-pack` connectivity check performance regression. Peff acknowledges Junio’s suggestion to use application-specific hints for optimizing connectivity checks but notes that the current regression—triggered by the quarantine area during `receive-pack`—would not benefit from such hints. He clarifies that `check_connected()` serves two distinct purposes: a loose "do we probably have these objects?" check (used in fetches) and a strict "are these objects definitely present?" check (used in `receive-pack`). The quarantine area only affects the latter, so optimizations targeting the former (like Junio’s proposed hints) would not address the regression.

Peff’s response is **substantive but narrow**: it doesn’t propose a fix or reject Junio’s idea outright, but it effectively shelves the hint-based approach as orthogonal to the immediate problem. The thread’s focus remains on the ODB lookup order regression and Patrick Steinhardt’s transaction-aware ODB write proposal, with Peff’s email serving as a minor course correction rather than a major shift. Expect further discussion to center on those two tracks.