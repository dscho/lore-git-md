**Summarizer brief mode**

This is Junio C Hamano’s final decision on Harald Nordgren’s `--track=fetch` feature series, marking it for discard in the upcoming "What's cooking" report. The email explicitly states the topic is stalled and unlikely to proceed further.

Junio reiterates his long-standing workflow concern: automatically fetching the remote branch during checkout could silently update other branches that forked from the same remote-tracking ref, making them appear "stale" without the user’s awareness. He views the two-command workaround (`git fetch <remote> <branch> && git checkout -b <topic> <remote>/<branch>`) not as a pain point but as a deliberate design choice that encourages users to inspect upstream changes before forking. The dismissive use of "workaround" in the commit message reinforces his skepticism that the feature’s benefits outweigh its risks.

The decision is framed as procedural rather than technical: the series is more than a month old, and Junio’s draft report now lists it among topics to be discarded. No new technical feedback is provided; the rejection hinges entirely on workflow considerations. The email does not engage with Harald’s reframing of the debate (whether Git should serve user needs or enforce an idealized workflow) or Kristofer Karlsson’s architectural argument that the use case is better addressed outside Git’s core.

Key details for future thread tracking:
- **Files touched**: `branch.c`, `branch.h`, `builtin/checkout.c`, `t/t2027-checkout-track.sh`
- **Functions introduced**: `find_tracking_remote_for_ref()`, `advise_ambiguous_fetch_refspec()`, `fetch_remote_for_start_point()`
- **Config/test impact**: new `--track=fetch` option for `git checkout`/`git switch`; test suite streamlined in v15
- **Prior technical approval**: Junio and Phillip Wood approved the implementation in v12/v13; no technical objections remain
- **Workflow rationale**: feature intended to reduce merge conflicts and duplicated work by ensuring users start from the latest remote tip
- **Maintainer’s counterargument**: automatic fetches could encourage "blind updates" and silently invalidate other branches
- **Thread outcome**: series marked for discard; no further revisions expected unless new arguments are presented