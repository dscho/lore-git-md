This is a **substantive technical reply** from Jeff King (Peff) in the ongoing ODB performance regression thread. Peff acknowledges Junio’s proposal to optimize `git rev-list` connectivity checks with application-specific hints but clarifies that such optimizations would not address the immediate regression in `git receive-pack`. The issue stems from the quarantine area, which is only used during strict connectivity checks for incoming objects (like pushes), not during fetches. Peff’s response effectively narrows the scope of Junio’s suggestion, positioning it as a *separate* optimization opportunity rather than a solution to the current regression.

The key takeaway is that the regression’s root cause—redundant loose object checks in the quarantine directory during connectivity traversals—remains unaddressed by Junio’s proposed hints. Peff’s framing reinforces the need for a targeted fix, such as Patrick Steinhardt’s transaction-aware ODB write proposal or a lookup-order adjustment, to resolve the performance degradation in the short term. The discussion now splits into two tracks: (1) a near-term fix for the regression, and (2) longer-term optimizations for `check_connected()` that may benefit other use cases. Peff’s reply carries high weight due to his deep familiarity with Git’s internals and the regression’s specifics.