This is the second patch in Jean-Noël Avila’s v2 documentation series converting `git format-patch` to the new AsciiDoc synopsis style. It is part of the ongoing effort to standardize all Git man pages on consistent markup.

The patch mechanically replaces `[verse]` with `[synopsis]` in the SYNOPSIS block, removes single-quote formatting from the command name, backtick-quotes all option terms, converts placeholders to `_<placeholder>_` form, and updates prose references to commands and tools (e.g., `patch(1)` → `` `patch`(1) ``). Two included files are also touched: `diff-options.adoc` (backtick-quoting the format-patch–specific options `-p`, `--no-stat`, and `--max-depth=<depth>`) and `format-patch-caveats.adoc` (converting `patch(1)` to `` `patch`(1) ``). The diffstat shows 289 lines changed in `git-format-patch.adoc` and 8 in `diff-options.adoc`, reflecting the purely presentational nature of the conversion—no behavior, option semantics, or CLI interface is altered.

The patch is ready for final review; the two stylistic inconsistencies Junio identified in v1 (backtick usage and dash count in delimited blocks) are now resolved. Given the mechanical nature of the changes and the author’s established track record on this conversion effort, the patch looks uncontroversial and likely to graduate to `next` once reviews are complete.