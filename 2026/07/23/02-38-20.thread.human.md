This thread is Junio C Hamano’s biweekly "What’s cooking in git.git" report for July 2026, summarizing the state of Git’s integration branches. In the latest update, the seventh batch of the cycle graduated 11 topics to `master`, including Christian Couder’s `fast-export` synopsis cleanup, Toon Claes’s `includeIf` worktree path support, and Patrick Steinhardt’s ODB-transaction conversion for `receive-pack` and Bloom-filter acceleration for `odb_for_each_object()`.

The report also lists new topics under review, such as Hugo Santos’s `hs/rebase-continue-edit` (adding `--[no-]edit` to `rebase --continue`), Elijah Newren’s submodule remote matching fix, and Taylor Blau’s `send-pack` delta-capability patch. Among these, Patrick Steinhardt’s `ps/cat-file-remote-object-info` series—a 13-patch effort adding a `remote-object-info` batch command to `git cat-file`—has drawn attention. Karthik Nayak reviewed it and deemed it ready, noting only minor nits that didn’t require a reroll. Junio’s latest follow-up gently nudges Patrick to address those nits anyway, reasoning that while one nit might be overlooked, multiple are worth fixing before the series graduates to `next`. The series itself is otherwise on track, with no new technical objections raised. Other topics in `next` or awaiting review include Phillip Wood’s rebase fixup fixes and Han-Wen Nienhuys’s `git history squash` extension. Stalled topics, like Khaled’s doc trailers series, remain on hold pending rerolls.