Here’s the human-mode summary for a developer catching up:

The July 2026 “What’s cooking” report (#10) from Junio Hamano gives a snapshot of Git’s integration branches. The seventh batch of the cycle graduated 11 topics to `master`, including Christian Couder’s `fast-export` synopsis cleanup, Toon Claes’s `includeIf` worktree-path support, Patrick Steinhardt’s ODB-transaction conversion of `receive-pack`, Bloom-filter acceleration for `odb_for_each_object()`, and Phillip Wood’s rebase fix for dropped commits.

New topics under review include Hugo Santos’s `--[no-]edit` knob for `rebase --continue`, Toon Nyman’s stash and packfile-URI fixes, Elijah Newren’s submodule remote-matching tweak, Taylor Blau’s `send-pack` ref-delta control, and Junio’s own `--exclude-first-parent-only` fix. About 20 topics are cooking in `next` or awaiting review, among them Patrick Steinhardt’s loose-object writing refactor, Phillip Wood’s rebase fixup fixes, Han-Wen Nienhuys’s bisect and `git history squash` extensions, and Taylor Yao’s executable-bit config migration.

The only loose end still open is Patrick Steinhardt’s 13-patch `ps/cat-file-remote-object-info` series, which adds a `remote-object-info` batch-command to `git cat-file`. The command fetches object metadata (currently size) from a remote via protocol v2 without downloading the object. Karthik Nayak’s substantive review found it ready, noting only two minor nits—code style and commit message wording—that Pablo Sabater has now volunteered to address. Junio has accepted the fixes in principle, so the series is effectively approved and on track to graduate to `next` once the trivial changes land.