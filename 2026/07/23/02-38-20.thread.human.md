Here’s a concise narrative for a developer catching up on the Git mailing list:

This thread is Junio Hamano’s mid-July 2026 “What’s cooking” report, summarizing the state of Git’s integration branches. The big news is the seventh batch of topics graduating to `master`: Christian Couder cleaned up `fast-export`’s synopsis, Toon Claes made `includeIf` conditions work with worktree paths, Patrick Steinhardt converted `receive-pack` to use ODB transactions and accelerated `odb_for_each_object()` with Bloom filters, and Phillip Wood fixed a rebase bug that was incorrectly recording dropped commits as rewritten.

Under review are six new topics: Hugo Santos’s `rebase --continue --[no-]edit` and config knob, Toon Nyman’s stash optimization to avoid sparse-index expansion, Elijah Newren’s submodule remote matching respecting `url.*.insteadOf` aliases, Taylor Blau’s `send-pack` honoring `no-ref-delta`, Toon Nyman’s fix for concurrent appends during packfile-URI downloads, and Junio’s own fix for `--exclude-first-parent-only` when the first parent is already SEEN. Another 20+ topics are cooking in `next` or awaiting review, including Patrick Steinhardt’s ODB refactoring to move loose-object writing into the backend, Phillip Wood’s rebase fixup/squash fixes, Han-Wen Nienhuys’s bisect and `git history` enhancements, and Taylor Yao’s migration of `core.trustExecutableBit` into the per-repo config. Two stalled series—Khaled’s doc trailers and Jörg’s config-lock timeout—are still waiting for rerolls.