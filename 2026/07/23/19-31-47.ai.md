This is a reply in the ongoing RFC thread introducing `git repack --drop-filtered` for partial clones. The email is a brief acknowledgment from reviewer Siddharth Asthana of Junio’s **substantive review** identifying a **blocking logical flaw** in the `--drop-filtered` vs `--write-bitmap-index` validation.

The core issue is now settled: the current validation occurs too late in the control flow (after `parse_options()` and `repo_config()` have already set `write_bitmaps`), so it cannot distinguish between a user explicitly requesting bitmaps on the command line (which should be rejected) and bitmaps enabled by configuration (which should be silently disabled). Asthana agrees this must be fixed in v2 by restructuring the validation to handle the two cases differently. No alternative implementation is proposed in this email, but the agreement removes ambiguity about whether the flaw is real and whether it blocks progress.

The email is short and technical, adding no new context beyond confirming the flaw and the intent to address it. The thread remains in active design iteration; no integration branch has been assigned. The next step is the v2 series, which will also incorporate **safety guards** (merge/rebase/cherry-pick checks and index validation) and **function signature reorganization** as previously scoped.