This is a **brief maintainer reply** in the `--reset-when-found` series for `git bisect`. Junio C Hamano acknowledges Johannes Sixt’s suggestion to use Git’s standard dual-option error template for the `--reset-when-found`/`--no-checkout` conflict and muses about introducing a new helper function family (`error_for_incompatible_optX()`) that returns an error instead of dying, similar to the existing `die_for_incompatible_optX()` functions. He asks whether there are many other places in the codebase that could benefit from such a helper.

The reply is purely exploratory and does not block the series’ progress. It suggests a potential future refactoring to reduce boilerplate and improve consistency in error handling, but this would be a separate effort unrelated to the current feature. The `--reset-when-found` series itself remains on track for integration into `next`, with only minor mechanical fixes (like the error message template) needed to address Sixt’s feedback. No new technical concerns or design questions are raised.