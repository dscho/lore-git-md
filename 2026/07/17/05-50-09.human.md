This is a review comment on the second patch of a two-patch bugfix series for gitweb. The series fixes a rendering issue where object IDs in commitdiff index lines (e.g., `index <old>..<new> 100644`) were displayed as full, unlinked hashes instead of shortened, clickable ones.

Junio C Hamano suggests squashing the test patch (this one) with the code fix in the first patch, arguing it would make review easier. He also recommends using `test_grep` and `test_grep !` instead of raw `grep` for better test diagnostics and to comply with an in-flight topic that enforces stricter test script rules. The review is surface-level, focusing on test style rather than the substance of the fix itself.

The series as a whole is well-motivated and narrowly scoped, addressing a clear regression in gitweb’s output. The test in this patch is minimal but sufficient, covering the common `100644` mode case with both positive and negative checks. If the author incorporates Junio’s suggestions, the series should be uncontroversial and ready for merging.