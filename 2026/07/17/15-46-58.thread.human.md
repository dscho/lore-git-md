Here’s the human-mode update:

The `git last-modified` Bloom filter optimization series, which speeds up wildcard pathspec queries by reusing Git’s Bloom filters, hit a snag after Junio C Hamano queued the v3 patches in his `seen` branch. CI caught a test failure in `t8020-last-modified.sh` under the project’s standard test environment, and Junio confirmed the issue locally. The series is now back under review, with no fix posted yet. The failure doesn’t invalidate the core optimization—Toon Claes’s v3 addressed all prior technical feedback, including correctness fixes for `--show-trees` and wildcard pathspecs—but it does block integration until resolved. The base commit for the series is 41365c2a9b ("The 4th batch for Git 2.56"), so the fix will likely be a small, targeted patch rather than a full reroll. Key participants (Toon, Jeff King, Taylor Blau) are still engaged, and the maintainer’s quick ejection suggests this is a straightforward issue to address.