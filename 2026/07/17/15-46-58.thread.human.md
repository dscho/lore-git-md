Here’s the updated human-mode summary for a developer catching up after this new email:

---

This thread is about a four-patch series from Toon Claes to speed up the experimental `git last-modified` command by reusing Git’s Bloom filters, making it as fast as `git log` for queries like `*.c`. The series is under active review, with v1 posted and no prior versions. Key participants include the author (Toon Claes), Junio C Hamano (maintainer), Taylor Blau, and Jeff King (Peff).

The core optimization reuses Bloom filters to avoid redundant lookups, cutting runtime from 52 ms to 14 ms on the ziglang/zig repository. However, reviews have surfaced several issues. Junio and Taylor identified unintended behavioral changes in patch 2/4, including a counter increment that should be reverted. Taylor also flagged a correctness bug in patch 3/4 when `--show-trees` is used, which may invalidate the cover-letter benchmarks, and a test-coverage gap in patch 4/4. Jeff King’s latest review critiques patch 1/4’s design, arguing that the new public helper function’s tristate return value (`0`, `1`, or `-1`) is error-prone and should be simplified to a boolean. The author has acknowledged missing trailers for Jeff’s earlier contributions but hasn’t addressed the technical feedback yet.

The series touches `revision.c`, `revision.h`, and `builtin/last-modified.c`, exposing new symbols like `revs_maybe_changed_in_bloom`. Open questions include whether the remaining behavioral changes in patch 2/4 are intentional, how to fix the `--show-trees` bug, and whether to adopt Jeff’s boolean-return proposal. The author plans a reroll, but no timeline has been shared.