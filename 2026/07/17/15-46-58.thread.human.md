Here’s the human-readable summary for a developer catching up on the thread:

This thread is about Toon Claes’s patch series to speed up the experimental `git last-modified` command by reusing Git’s Bloom filters, aiming to match the performance of `git log` (14 ms vs. the current 52 ms on the ziglang/zig repository). The series is at v1, under review, with no prior versions.

Key participants include Toon (the author), Junio C Hamano (the maintainer), Jeff King (Peff), and Taylor Blau. The main technical focus has been on correctness and maintainability, particularly around Bloom filter statistics, API design, and a bug in `--show-trees` where ancestor tree entries not covered by the original pathspec could appear in the output. The latter issue is now the primary blocker: the fix—filtering paths in `add_path_from_diff()` using `match_pathspec()`—was agreed upon in principle but hasn’t been implemented yet. Jeff King’s maintainability concern about patch 4/4 (restoring `bloom_filter_settings` after `prepare_revision_walk()` clears it) is acknowledged but not considered a blocker. Junio’s latest email reaffirms that the `--show-trees` fix must be addressed in the next reroll. Other loose ends include a design question about the public API’s return semantics (tristate vs. boolean) and missing trailers for Jeff King’s contributions. The series touches `revision.c`, `revision.h`, and `builtin/last-modified.c`, introducing new symbols like `repo_may_have_commit_with_changes` and `revs_maybe_changed_in_bloom`. No on-disk format changes or new test files were added, though gaps in test coverage were noted. The next step is a reroll from Toon incorporating the agreed fixes.