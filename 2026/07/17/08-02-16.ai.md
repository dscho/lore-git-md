This is a substantive review reply in the ongoing `git last-modified` performance thread. Jeff King (Peff) clarifies a key misunderstanding about the benchmark: Gusted’s hand-rolled `git log` pipeline does not loop over individual files but instead walks the entire subtree in a single pass, then post-processes the output to extract per-file results. The performance gap Peff identified earlier—where `last-modified` checks every path against every commit’s Bloom filter—still holds, but the baseline comparison is now more accurate: the `git log` approach scales with the number of commits that actually touched the subtree, not the total repository size, while `last-modified` scales with the product of paths and total commits. Peff reaffirms that `last-modified` should be able to match or exceed the `git log` pipeline’s efficiency by adopting similar top-level commit filtering, setting the stage for the unified patch series he and Toon Claes are preparing. No new code or benchmarks are introduced here; the email refines the technical context for the optimization effort.