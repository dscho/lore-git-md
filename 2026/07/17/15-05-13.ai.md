This is a test infrastructure refactoring patch, part 3 of 13 in the GSoC v19 series that implements `git cat-file --batch-command` remote object info support. The patch extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The motivation is code reuse: a later patch in the series will need these same helpers, and future cat-file tests can now share them without duplication.

The change is mechanical and low-risk. The new library file is straightforward, containing only the three moved functions with no behavioral changes. The patch also updates a single line in `t1006-cat-file.sh` to follow Git's coding guideline for sourcing library files (changing `. "$TEST_DIRECTORY/lib-loose.sh"` to `. "$TEST_DIRECTORY"/lib-loose.sh`). The diff shows clean moves: the helpers are deleted from the test script and added verbatim to the new library, which is then sourced by the original script.

This patch does not touch the core functionality of the series (remote object info queries) but is a necessary prerequisite for the test coverage in later patches. The commit message is clear and concise, explaining both the "what" (extraction) and the "why" (reuse and readability). No substantive review issues are apparent; this is a routine test infrastructure improvement.