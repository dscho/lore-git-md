This is a maintainer response to a feature patch adding a built-in Swift userdiff driver. Junio C Hamano acknowledges the patch and queues it in the `seen` branch for review, but explicitly states that it requires validation from active Swift users before it can progress to `next` and eventually be merged. The response is brief and procedural, with no technical feedback or objections—just a clear dependency on external review from domain experts.

The patch itself remains unchanged from its initial submission: it introduces Swift-aware patterns for function declarations and word boundaries, includes 13 test cases, and updates documentation. Junio’s note does not question the implementation’s correctness or completeness, only its real-world applicability. The ball is now in the court of Swift developers on the list to test the patterns against actual codebases and report whether the regexes capture the language’s syntax accurately.

Key details for follow-up: the patch is in `seen`, not `next`; the blocker is validation, not code quality; and the thread is now waiting on feedback from Swift users. No further action from the author is needed unless reviewers request changes.