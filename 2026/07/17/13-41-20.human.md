This is a brief follow-up from the author of the gitweb bugfix series. Travor Liu acknowledges Junio C Hamano’s review feedback on the test mechanics in patch 2/2. He confirms he will squash the test into the first patch and switch from raw `grep` to `test_grep`/`test_grep !` in the next version (v2). The response is procedural and does not revisit the technical substance of the fix itself, which remains unchanged: reordering the regex logic in `gitweb/gitweb.perl` so that file modes are stripped before object IDs are shortened and hyperlinked. The series still targets the same narrow bug—full, unlinked object IDs appearing in commitdiff index lines when a trailing mode is present.