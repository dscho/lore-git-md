This is the core feature patch in a long-running, security-hardened series that adds `remote-object-info` support to `git cat-file --batch-command`. The series is now at v19, queued for `next`, and this patch implements the actual client-side command that lets users query object metadata (currently just size) from a remote repository without downloading the full objects.

The patch adds a new `remote-object-info` command to `git cat-file --batch-command`. Unlike the existing `info` command, which processes one object at a time, `remote-object-info` accepts multiple object IDs in a single request, reducing network overhead. The command works with all smart transports (`git://`, `file://`, `http://`) and requires protocol v2 and the `object-info` capability on the server. Security measures include a 10,000-object batch limit, an 8KB URL length cap, and strict input validation that rejects short OIDs or malformed responses. The implementation dynamically adapts to the server’s advertised capabilities, silently omitting unsupported format placeholders (like `%(objecttype)`) rather than failing.

The patch is well-tested, with 719 lines of new tests in `t/t1017-cat-file-remote-object-info.sh` covering edge cases like missing objects, unsupported protocols, and malformed OIDs. The tests also verify that the command works with `--buffer` and `-Z` (null-delimited output) options, and that it doesn’t leak its default format into subsequent `info` commands. The commit message clearly explains the design rationale, including why the command processes objects in bulk and how it avoids remote communication overhead in non-buffer mode.

This is a polished, production-ready feature that addresses a real use case: querying object metadata from remotes without incurring the cost of full downloads. The implementation is careful about security and edge cases, and the thorough test coverage suggests it’s ready for wider exposure. The only remaining work is minor cosmetic nits (documentation, translation context, alphabetical ordering) that are unlikely to block merging. If you care about partial clones or remote repository introspection, this is worth watching.