This is a **design discussion** in the `git replay --linearize` thread, where Junio C Hamano responds to Elijah Newren’s argument about the **multi-branch ambiguity** with `--linearize`. Junio pushes back on Elijah’s framing that concatenating unrelated branches is an "emergent, unintended behavior," instead arguing that the user’s intent is clear: they want a **linear history**, and the command is delivering exactly that.

Junio’s position is that the current behavior is **reasonable and intentional**. He breaks down the user’s command (`git replay --linearize --onto X branchA branchB branchC`) into two explicit requests:
1. Update multiple branches (`branchA`, `branchB`, `branchC`).
2. Linearize the history (via `--linearize`).

From this perspective, the concatenation of branches into a single linear chain is not an unintended side effect but the **direct result of combining these two features**. Junio acknowledges that this interpretation differs from Elijah’s but maintains that it is a **valid and defensible design choice**.

The email also clarifies Junio’s role in the discussion. He explicitly states that he is **not the primary audience** for this feature and has **no desire to dictate the design**. Instead, he defers to the topic author (Toon Claes) to weigh the competing arguments and decide how to proceed. As a procedural step, he marks the topic as **"On hold, waiting for response"**, pausing further integration until Toon addresses the open design question.

Key technical details:
- The **multi-branch ambiguity** remains unresolved. Junio’s argument hinges on the idea that `--linearize` is an **all-or-nothing flag**: if the user wants a linear history, they should expect all specified branches to be flattened into a single chain.
- Junio’s position does not engage with Elijah’s **recoverability asymmetry** argument (that untangling concatenated branches is harder than concatenating independent ones). This leaves Elijah’s usability concerns unaddressed from Junio’s side.
- The discussion is now **stalled** pending Toon Claes’s response. Toon must reconcile Junio’s defense of the status quo with Elijah’s proposal to restrict `--linearize` to a single positive ref (or implement per-branch linearization).

The ball is firmly in Toon’s court to either:
1. **Defend the current behavior** (aligning with Junio’s interpretation).
2. **Adopt Elijah’s minimal fix** (restrict `--linearize` to a single positive ref).
3. **Propose a third alternative** (e.g., one of Junio’s earlier syntax suggestions, such as limiting the command to a single negative ref plus one or more positive refs).

Until then, the series remains **on hold** in `next`.