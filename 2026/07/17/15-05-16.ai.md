This is a small refactoring patch in the ongoing `git cat-file --batch-command` remote object info series (GSoC v19, 6/13). It aligns variable types with the return type of `hash_algo_by_name()`—changing `int` to `const unsigned int`—and marks the variables `const` since they are never modified. The change is mechanical, touching only three lines in `connect.c`, and has no functional impact on the series' security or protocol features.

The patch is part of a larger effort to tighten type consistency across the codebase, a recurring theme in this series (e.g., earlier patches adjusted loop counters and array indices to `size_t`). While trivial on its own, it reflects the series' broader attention to code hygiene and correctness. No review feedback is attached, suggesting this is a preemptive cleanup rather than a response to prior criticism. The patch is queued for `next` alongside the rest of the series.