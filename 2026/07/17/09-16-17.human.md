This is a brief reply in the ongoing `git bisect --auto-reset` feature thread. The author, Harald Nordgren, accepts Junio C Hamano’s proposal to redesign the `--auto-reset` option as a parameterized form (`--auto-reset=<where>` with values `original` or `found`). No new technical details or objections are raised; the thread is now waiting for a revised patch series (v2) that implements the flexible design. The preparatory refactoring and quiet-checkout patches remain uncontroversial and will likely carry forward unchanged.