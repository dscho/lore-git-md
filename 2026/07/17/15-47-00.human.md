This is the second patch in Toon Claes’s four-part series to speed up the experimental `git last-modified` command by reusing Git’s Bloom filter machinery. The patch extracts the core Bloom-filter-checking logic from `check_maybe_different_in_bloom_filter` into a new public helper, `revs_maybe_changed_in_bloom`, so that other built-ins can consult the same filter without paying the cost of a redundant lookup. The original function becomes a thin wrapper around the new helper.

The change is a clean refactoring: it moves the loop that checks each pathspec key vector against the Bloom filter into its own function, adds a small precondition check, and exposes the new symbol in `revision.h`. No behavior changes are visible to callers, and the commit message makes it clear that the motivation is to avoid duplicate filter lookups in the next patch. The code is straightforward and the diff is easy to follow, suggesting the patch is well-crafted and ready for review.