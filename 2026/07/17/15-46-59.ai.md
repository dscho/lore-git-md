This is the first patch in Toon Claes's four-patch series optimizing the experimental `git last-modified` command by reusing Bloom filters. The patch is a small refactoring in `revision.c` that moves a precondition check (`revs->bloom_keyvecs_nr != 0`) into the `check_maybe_different_in_bloom_filter` function itself, eliminating duplicate checks at the two callsites. The change is mechanical and behavior-preserving: the new return value of `-1` when no Bloom key vectors are present is unobservable, as Bloom statistics are only reported when key vectors exist. The patch touches only `revision.c` and is a straightforward cleanup that paves the way for the next patch, which will expose a new helper function for external use. No substantive concerns are apparent; this looks like a well-contained preparatory step.