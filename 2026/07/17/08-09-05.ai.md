This is a **performance optimization follow-up** for the `git last-modified` command, continuing the thread where Jeff King (Peff) and Toon Claes are collaborating to address its 2.5×–4× slower performance compared to a hand-rolled `git log` pipeline. The email is a short technical exchange between Peff and Toon, clarifying the implementation approach for Peff’s proof-of-concept patch that reuses Bloom filter logic from `revision.c`.

Peff responds to Toon’s earlier message, confirming that the optimization is unlikely to regress performance even in edge cases (e.g., directories touched by nearly every commit), as checking a single Bloom filter key is cheap and the pre-filtering step will almost always be a net win. The core of the discussion centers on how to expose the internal `check_maybe_different_in_bloom_filter()` helper from `revision.c`. Peff suggests refactoring the function into two parts—one to fetch the Bloom filter and another to check it against the `rev_info`—and making the latter public, rather than exposing the entire function. This aligns with Toon’s plan to adopt Peff’s approach, avoiding direct access to `rev_info` internals and keeping the logic for interpreting Bloom filter key vectors within `revision.c`.

The email is **substantive but narrow**, focusing on a single implementation detail that could affect code hygiene and maintainability. There are no new benchmarks or user-visible changes here; the value lies in the coordination between two experienced contributors to ensure the patch is clean and future-proof. The thread remains on track for a unified v2 series, with Toon and Peff now aligned on the refactoring strategy. The only remaining technical loose end is whether GitHub’s internal `blame-tree` implementation (the inspiration for `last-modified`) has already solved this problem, but that question is deferred for now. This patch is likely to land smoothly, given the demonstrated speedups and the authors’ attention to code quality.