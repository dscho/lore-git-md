This is the latest reply in the long-running thread to refactor `git show-branch` to use a commit-slab instead of the shared `object.flags` field. Junio C Hamano escalates Patrick Steinhardt’s earlier concern about AI-generated communication to the patch itself, arguing that the v7 patch shows signs of being machine-generated and sent without proper human review. He points to two specific inconsistencies: the commit message claims to have removed the `max_revs` limit entirely, yet the documentation still mentions a 64-branch limit, and the patch removes the local `UNINTERESTING` definition while leaving a comment advising future migration to the shared definition. These discrepancies suggest the patch was not carefully reviewed before submission, undermining its technical readiness.

The patch itself remains a well-motivated refactoring that eliminates the risk of flag-bit collisions and removes the previous 27-branch limit by using a runtime-computed commit-slab. Earlier versions addressed behavioral regressions in `show_independent()` and `join_revs()`, and the current version is technically sound apart from the internal inconsistencies Junio highlights. The concern here is not about the code’s correctness but about the process by which it was produced and whether the author has genuinely engaged with the changes.

Junio’s message is a strong signal that the patch will not be merged until the author demonstrates a deeper, human-level understanding of the changes. This is not a technical rejection but a procedural one: the project expects contributors to think through their patches, not merely relay machine-generated output. The thread is now paused until the author responds with a v8 that addresses the inconsistencies and, more importantly, shows evidence of human oversight.