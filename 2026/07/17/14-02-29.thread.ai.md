Thread identity
Subject: [PATCH] userdiff: add built-in Swift language driver
Series version: standalone patch (no prior versions)
Originating author: Shlok Kulshreshtha
Thread type: patch submission (feature addition)

Core problem or goal
Add a built-in userdiff driver for the Swift programming language so that `git diff` and `git log -p` produce meaningful hunk headers and word-level diffs for `.swift` files without requiring manual `.gitattributes` configuration.

Current status
Queued in `seen` (not `next` or `master`). Junio C Hamano has explicitly stated that the patch requires validation from active Swift users before it can progress further. No changes have been requested; the patch is complete as submitted.

Key technical details
- Files: `userdiff.c` (new patterns), `Documentation/gitattributes.adoc` (documentation), `t/t4018/` (13 new Swift test fixtures).
- New symbols: `builtin_swift_patterns` (funcname and word_regex entries).
- Behavior: replaces generic heuristic with Swift-aware patterns for function declarations and word boundaries.
- On-disk format: none; purely in-memory regex compilation.
- Test coverage: 13 test cases covering classes, structs, enums, protocols, extensions, actors, attributes, modifiers, generics, failable initializers, and operator functions.

Open questions and loose ends
Validation pending: the patch must be tested against real-world Swift codebases by active Swift users on the list to confirm the regexes accurately capture Swift syntax. No technical objections or change requests have been raised.

Participant positions
- **Junio C Hamano (maintainer)**: queued in `seen`; requires validation from Swift users before progression to `next`. Position is procedural, not technical.
- **Shlok Kulshreshtha (author)**: submitted complete patch; no further action required unless reviewers request changes.

Related work
Part of the “add a userdiff driver for a language” microproject; no prior Swift driver exists. No dependencies or conflicts with other in-flight topics.