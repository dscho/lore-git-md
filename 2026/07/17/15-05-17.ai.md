This is a refactoring patch in the `remote-object-info` series (GSoC v19, 7/13). It generalizes `write_fetch_command_and_capabilities()` into `write_command_and_capabilities()` by adding a `command` parameter, allowing the function to be reused for non-fetch protocol v2 operations like `object-info`. The change is mechanical: the function is renamed, its signature updated, and all callers adjusted to pass `"fetch"` explicitly. No behavior changes are introduced.

The patch prepares the ground for later commits in the series that will use the new generic function to implement the `object-info` command. It is authored by Calvin Wan but carried forward by Pablo Sabater, with `Helped-by` credits to Jonathan Tan and Christian Couder. The diff touches only three lines in the implementation and updates the function declaration and documentation in `connect.h`. Call sites in `fetch-pack.c` are updated to pass the command string explicitly.

This is a clean, preparatory refactor with no functional impact on the series' security or protocol features. The patch aligns with the series' broader goal of code reuse and modularity, and the thread context confirms it is part of a technically complete, security-hardened feature now queued for `next`. No review issues or edge cases are introduced here.