This is a small refactoring patch in the ongoing `git cat-file --batch-command` remote object info series. It is the sixth of thirteen patches in v19, which is now queued for `next`.

The patch aligns variable types with the return type of `hash_algo_by_name()`, which returns an `unsigned int`. Three variables in `connect.c` that store this return value are currently declared as `int`; the patch changes them to `const unsigned int` and marks them `const` since they are never modified. The change is purely mechanical and does not alter behavior.

This is a clean, well-scoped cleanup that fits the series’ broader focus on code hygiene and security hardening. The commit message is clear and the diff is minimal. No controversy or open questions remain.