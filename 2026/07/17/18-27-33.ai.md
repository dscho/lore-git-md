This is the second patch in the v2 series adding the `--auto-reset` option to `git bisect`. It prepares the `bisect_reset()` function for quiet checkout by adding a `quiet` parameter that suppresses progress and branch-status output when set. No behavior changes yet—this is purely plumbing for the upcoming `--auto-reset` feature.

The patch modifies `bisect_reset()` to conditionally pass `--quiet` to the underlying `git checkout` command, and updates all callers to pass `0` (false) for the new parameter. The change is minimal and focused: the function signature is extended, the checkout command is adjusted to insert `--quiet` when requested, and the existing call sites are updated to maintain the current (non-quiet) behavior. No tests are added here, as the quiet behavior will only be exercised in the next patch.

This is a straightforward refactoring that aligns with the series' goal of reducing output noise during auto-reset. The implementation is clean and uncontroversial, with no edge cases or error-handling changes introduced at this stage. The patch is ready for integration and should not require further review scrutiny beyond verifying the plumbing is correct.