This is the first patch in a four-part performance series for the experimental `git last-modified` command. It’s a small refactoring in `revision.c` that moves a precondition check (`revs->bloom_keyvecs_nr != 0`) into the helper function `check_maybe_different_in_bloom_filter()`. The change consolidates two identical checks into one place, making the code slightly cleaner without altering behavior.

The patch is mechanical and safe: the new guard clause returns `-1` when no Bloom key vectors are present, which the existing callers already handle. The commit message notes that this tweak is unobservable to users because Bloom statistics are only reported when key vectors exist. No new tests or documentation are needed.

This is a typical setup patch for a performance series—it doesn’t deliver the speed-up itself but paves the way for the real optimizations in later patches. The change looks well-justified and uncontroversial.