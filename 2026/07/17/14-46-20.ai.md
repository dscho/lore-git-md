This is a performance-minded refactoring patch in the `wt-status` subsystem, now at v2. The patch replaces repeated O(n²) `string_list_insert()` calls with a single O(n) `string_list_append()` pass followed by one O(n log n) sort-and-deduplicate operation per list (`s->untracked` and `s->ignored`). The change is purely internal to `wt_status_collect_untracked()` in `wt-status.c` and preserves the existing sorted, duplicate-free output.

The v2 revision incorporates all substantive feedback from Patrick Steinhardt and Jeff King: it uses `string_list_sort_u()` instead of open-coding the sort and deduplication, and the commit message now avoids overstating the performance impact, framing the patch primarily as an improvement to code clarity and maintainability. The patch remains small (6 insertions, 2 deletions) and is ready for further review or integration.

Key technical context for follow-up discussion: the patch touches only `wt-status.c`, modifies `wt_status_collect_untracked()`, and relies on the existing `string_list_sort_u()` helper. The base commit is `44de1520f08d1dfebc3ab2d9f644208eaa5ac925`. The unresolved question about whether `dir.entries` and `dir.ignored` can contain duplicates is no longer central to the patch’s justification, but it may still surface in review.