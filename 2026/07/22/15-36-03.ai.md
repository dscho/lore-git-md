This is a brief follow-up in the ongoing discussion about adding a `--[no-]edit` option to `git rebase --continue`. Junio C Hamano acknowledges a minor but telling error in his earlier review: he mistakenly wrote `GIT_SEQUENCE_EDITOR=:` when he meant `GIT_EDITOR=:`. The correction is trivial in itself, but it underscores how easily the existing workaround can be misapplied, and it quietly reinforces the thread’s broader theme—that the current design space is subtle and error-prone.

The exchange is surface-level: no new technical ground is covered, no edge cases are revisited, and no stance on the feature’s necessity is reaffirmed or revised. Junio’s tone is light (“absolutely”, “X-<”), but the substance is minimal. The thread remains stalled at the same open questions: whether the feature is justified, how `rebase.noEdit` should interact with interactive rebase steps (`edit`, `reword`), and whether users would need to remember to pass `--edit` in non-conflict scenarios. Phillip Wood’s prior feedback—correcting the environment variable, referencing unresolved 2024 discussion, and aligning with Junio’s skepticism—still stands as the last substantive input.