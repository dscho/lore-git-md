This is the **final response** in the `git branch --delete-merged` thread, resolving the last substantive review question about the **order-independent stacked-branch protection** refactor. Harald Nordgren explains the core logic change in v20: replacing the previous two-phase approach (marking spared branches during iteration, then clearing their upstreams afterward) with a **single-pass graph traversal** that collects all protected branches before mutating the deletion set. The motivation is to eliminate an **order dependency** where the previous implementation could produce inconsistent results depending on ref iteration order (e.g., `b1→b2` vs. `b2→b1`). The new `collect_stacked_branch_bases()` function explicitly handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) by walking the entire upstream chain for each branch and marking all transitively required branches as "protected" before any deletions occur. This addresses Junio C Hamano’s blocking correctness concern and makes the behavior deterministic. The refactor also clarifies variable naming (e.g., `deletable_branch_names` instead of `deletable`) to distinguish branch names from full refnames. The series remains **unblocked and ready for merging**, with this change resolving the last correctness issue. The only follow-up work is extending stacked-branch protection to `git branch -d` for consistency, which is not a blocker. Key files touched: `builtin/branch.c` (refactored `spare_stacked_bases()` and new `collect_stacked_branch_bases()`), and `t/t3200-branch.sh` (updated tests to verify order independence). The discussion confirms the refactor is sound and that the series is now complete.