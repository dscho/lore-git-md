This is a surface-level review exchange in the final stages of the `git cat-file --batch-command` remote-object-info series, now at v20 and queued for `next`. Karthik Nayak and Pablo Sabater discuss two cosmetic issues in the last patch (12/13), neither of which is treated as blocking.

The first issue is the unintentional restoration of `CAVEATS` references in the `git-cat-file` documentation. Pablo added it for consistency after noticing the `objectsize:disk` atom already referenced the section, but agrees it is unnecessary. Karthik confirms no reroll is needed, calling it a minor lesson for future patches.

The second issue is the reformatting of the `EXPAND_DATA_INIT` macro into one line per item. Pablo attributes this partly to Junio’s earlier suggestion, though he acknowledges the macro is unlikely to grow. Karthik suggests a slightly different style—starting the macro on a new line, ending each field with a comma, and avoiding trailing whitespace—to make future additions cleaner. He does not insist on a reroll, but recommends the change if the series is rerolled for other reasons.

The discussion remains entirely about style and documentation hygiene; no technical concerns are raised. The series is technically complete and ready for `next`, with these nits left as optional polish before graduation to `master`.