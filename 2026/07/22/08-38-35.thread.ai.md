Thread identity
Subject: [PATCH] git-p4: skip commit metadata in extractLogMessageFromGitCommit
Series version: standalone patch (no series)
Originating author: Hardik Kumar (hardikxk)
Thread type: bugfix patch

Core problem or goal
The `extractLogMessageFromGitCommit` helper in `contrib/git-p4.py` contains a `# fixme` comment dating to 2007, indicating that the function should return only the commit message body but currently does not. The patch proposes to address this by scanning for the first blank line in the commit object and returning only the message body. However, testing reveals the function already excludes metadata (author, tree, SHAs) and instead drops the commit *subject* (the first line of the message), leaving the original intent of the `# fixme` ambiguous.

Current status
Under review; no prior versions. Patch is a single commit touching only `git-p4.py`. The patch’s stated goal (skipping metadata) does not match its actual effect (preserving metadata while dropping the subject), and the `# fixme`’s original purpose remains unclear. Author has been advised to reformat the commit message for line length and omit the redundant cover letter, but no substantive technical feedback has been provided.

Key technical details
- File: `contrib/git-p4.py`
- Function: `extractLogMessageFromGitCommit`
- Change: replaces naive commit-object read with logic that scans for the first blank line and returns only the message body
- Subsystem: Perforce interoperability tooling (contrib)
- New behavior: returns message body *after* the first blank line (dropping the subject if no blank line exists)
- Old behavior: returns message body *after* the first blank line (already excluding metadata)
- No on-disk format changes, no new symbols, no test coverage provided
- Historical context: `# fixme` comment added in 2007 with no accompanying discussion

Open questions and loose ends
- Original intent of the `# fixme`: was it meant to exclude metadata, the subject, or both?
- No reproduction case or user-facing scenario demonstrating the bug’s impact
- No test coverage for the fix or the existing behavior
- Unclear whether downstream consumers of this function rely on the current output (subject dropped, metadata excluded)
- Patch does not address the ambiguity; further investigation or historical research required before proceeding

Participant positions
- **Hardik Kumar (author)**: Proposes the patch to address the `# fixme` by excluding metadata, unaware of the existing behavior (subject already dropped). Has not yet responded to the editorial feedback.
- **Pablo Sabater (reviewer)**: Provided surface-level review flagging the mismatch between the patch’s intent and actual behavior, and suggested minor editorial improvements (commit message line length, redundant cover letter). Review is mechanical (presentation, not technical substance) and does not engage with broader implications, workflow impact, or correctness. No Reviewed-by/Acked-by.