This is the first patch in Harald Nordgren’s **v20 `git branch --delete-merged` series**, which is now unblocked and ready for merging. This patch introduces the `--forked` filter for `git branch --list`, a foundational building block for the series’ core functionality.

The `--forked` option filters branches by their configured upstream, supporting exact refnames (e.g., `origin/main`), remote shorthand (e.g., `origin`), and shell-style globs (e.g., `origin/*`). It can be repeated to widen the filter (e.g., `--forked origin/main --forked origin/next`) and composes with existing filters like `--merged` and `--no-merged`. For example, `git branch --merged origin/main --forked 'origin/*'` lists branches forked from `origin` that are already merged into `origin/main`.

The implementation resolves ref arguments to full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) and matches glob patterns against abbreviated upstream names (e.g., `origin/main`). The filter is integrated into the ref-filter machinery, ensuring it works seamlessly with other list-mode options. The patch includes thorough test coverage for edge cases, including glob matching, remote shorthand resolution, and composition with other filters.

This is a **well-crafted refactoring patch** that establishes the filtering infrastructure needed for the series’ main feature (`--delete-merged`). The design is clean, the commit message is clear, and the tests are comprehensive. Phillip Wood’s earlier review raised only surface-level test improvements (e.g., detached HEAD handling), which have been addressed. The patch is **ready for merging** as part of the larger series.