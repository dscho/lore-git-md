This is a review reply in the ongoing discussion about Hugo Sales' patch to add `--[no-]edit` to `git rebase --continue`. Phillip Wood, a frequent and thorough reviewer, weighs in with a substantive but narrow clarification.

Phillip corrects Junio’s earlier reference to `GIT_SEQUENCE_EDITOR` (which controls the rebase todo-list editor) by pointing out that the correct environment variable for suppressing the commit-message editor is `GIT_EDITOR`. He then links to a previous thread where he shared an alias that suppresses the editor only if HEAD hasn’t changed since the rebase was continued, but notes that even that approach doesn’t prevent users from accidentally skipping the editor during a `reword` step. His tone is neutral and constructive, but the reference to the earlier discussion reinforces Junio’s skepticism: the existing workaround is already more nuanced than the patch’s proposed `--no-edit` flag, and the patch doesn’t address the `reword` edge case that Phillip’s alias also fails to handle.

The review is substantive in that it engages with the technical details of the existing workaround and the edge cases Junio raised, but it doesn’t propose a path forward or suggest how the patch might be revised. It’s a targeted clarification rather than a full review, so readers should treat it as a footnote to Junio’s feedback rather than a new direction for the thread. The ball remains in Hugo’s court to respond to the open questions about `reword`, `edit`, and the scope of `rebase.noEdit`.