This is a small bugfix patch targeting the `git-p4.py` script, which is part of Git's Perforce interoperability tooling in `contrib/`. The patch modifies the `extractLogMessageFromGitCommit` function to skip commit metadata (author, tree, parent SHAs, etc.) and return only the commit message body, fixing a behavior where the function previously included the entire raw commit object data.

The change is contained to a single file (`git-p4.py`) and adjusts a helper function used during Perforce-to-Git import operations. The commit message is clear about the intent, though it does not provide a specific user-facing scenario or reproduction case where the old behavior caused problems. The patch itself is straightforward: it replaces a naive read of the commit object with logic that scans for the first blank line (the separator between headers and message body) and returns only what follows.

This is not part of any larger series or ongoing effort; it appears to be a standalone fix. The patch looks technically sound, but the lack of test coverage or a concrete example of the bug's impact might leave reviewers wanting more context. It is likely to be uncontroversial unless someone identifies an edge case where the metadata is actually needed by downstream consumers of this function.