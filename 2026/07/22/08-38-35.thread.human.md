This thread is about a long-standing `# fixme` comment in `git-p4.py` that dates back to 2007. Hardik Kumar proposed a patch to address it by ensuring the `extractLogMessageFromGitCommit` function returns only the commit message body, but testing revealed the function already excludes metadata (author, tree, SHAs) and instead drops the commit subject—the first line of the message. The original intent of the `# fixme` remains unclear: was it meant to exclude metadata, the subject, or both?

The discussion has since shifted to historical research rather than patch review. Hardik acknowledges the mismatch between the patch’s stated goal and its actual effect and now questions whether the effort is worthwhile without clearer context. Pablo Sabater previously flagged the discrepancy and suggested minor editorial improvements, but no substantive technical feedback has emerged. The thread is effectively stalled until someone can clarify the 2007 comment’s purpose, either through list archives or input from long-time contributors. No test coverage or user-facing impact has been demonstrated, and downstream dependencies on the function’s current behavior remain unknown.