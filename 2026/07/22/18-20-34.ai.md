This is a user request for a configuration knob to re-enable symlinks in submodule paths, reversing a security-motivated restriction introduced in commit e8d060894448 ("submodule--helper: reject symlinks in submodule paths", 2024-05-15).

The email describes a concrete use-case: multiple worktrees sharing a single copy of their submodules via symlinks. The author acknowledges the security rationale but argues that the blanket prohibition is overly paternalistic and has broken long-standing workflows. They point to an existing TODO comment in `builtin/submodule--helper.c` that explicitly proposes a `safe.submodule.path` configuration variable to allow opting out of the restriction.

No patch is attached; the author is asking whether the project would accept one. The tone is polite but frustrated, and the request is narrowly scoped: implement the TODO so users can re-enable symlinks if they choose. The thread is new, so there is no prior discussion to reference.