This is the second patch in Justin Tobler’s v2 series converting `git-receive-pack` to use ODB transactions instead of `tmp_objdir`. It is a purely mechanical rename: `fsync_loose_object_transaction()` becomes `odb_transaction_files_fsync()` to align with the naming scheme established in the first patch. The helper’s logic is also slightly reordered for clarity, but the behavior is unchanged.

The patch touches only `object-file.c` (10 insertions, 6 deletions) and is part of the preparatory work that makes the later transaction-flag and error-handling changes clearer. No user-facing behavior changes; the series remains focused on internal refactoring. The commit message explicitly ties the rename to consistency with other “files” ODB transaction helpers, signaling that this is a deliberate cleanup rather than an accidental churn.