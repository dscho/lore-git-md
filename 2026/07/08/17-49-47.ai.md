This is the first patch in Kristofer Karlsson's v3 performance optimization series for Git's priority queue. It extracts the bubble-up logic from `prio_queue_put()` into a standalone `sift_up()` helper function—a pure refactoring with no behavior change. The patch is a preparatory step for the subsequent commit, which will introduce the cascade-down optimization for `prio_queue_get()`.

The change is minimal (12 insertions, 9 deletions) and mechanically straightforward: the loop that promotes a newly inserted element up the heap is moved verbatim into its own function, then called from the original location. This refactoring was explicitly suggested by René Scharfe during earlier review and aligns with the series' goal of cleanly separating the new optimization logic. The commit message clearly states the purpose ("preparing for reuse") and credits the reviewer.

This patch is uncontroversial and appears ready to merge. It touches only `prio-queue.c` and maintains API compatibility. The real substance of the series lies in the second patch, which will build on this refactoring to implement the cascade-down optimization.