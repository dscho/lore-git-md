This is the fifth patch in Jeff King’s seven-patch v2 series refactoring the `git_hash_*()` API. The series as a whole makes `git_hash_discard()` idempotent and standardizes usage of the hash API across the codebase.

This patch simplifies the cleanup logic in `csum-file.c` by taking advantage of the new idempotent behavior of `git_hash_discard()`. The changes are straightforward: it removes the now-unnecessary `free_hashfile_memory()` helper function and eliminates redundant hash-discard calls when `f->skip_hash` is set. The patch is a direct follow-up to the idempotency change introduced in patch 4, and it demonstrates the intended benefit of that change by reducing code complexity and eliminating manual tracking of whether a hash context has already been finalized or discarded.

The patch touches only `csum-file.c` and is purely a refactoring with no behavior changes. The commit message clearly explains the motivation and the specific simplifications, making it easy to follow. This is a well-crafted, low-risk cleanup that fits neatly into the larger series. No objections or loose ends remain; the patch is ready for integration.