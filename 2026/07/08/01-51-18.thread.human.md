Here’s the human-readable summary for a developer catching up on the thread:

---

This thread is about a bug in `git merge --abort` where the autostash created by `merge --autostash` is lost if the abort fails (e.g., due to a locked index). Kris Point submitted a patch to fix this by deferring deletion of the `MERGE_AUTOSTASH` ref until after the reset succeeds, but Phillip Wood identified a flaw: the patch would incorrectly save the autostash even if it applies cleanly, and the reset logic’s reliance on `cmd_reset()` could introduce unintended side effects.

Junio C Hamano has now weighed in, agreeing with Phillip’s critique and endorsing his proposed solution—decoupling the reset logic from `cmd_reset()` entirely. This would involve directly calling lower-level functions like `reset_working_tree()` or `reset_head()` with a new flag (e.g., `RESET_WORKING_TREE_MERGE`) to control autostash behavior. Junio framed this as part of a broader effort to eliminate the anti-pattern of one built-in command calling another as a subroutine, aligning with Patrick Steinhardt’s `ps/history-drop` branch in `seen`. The fix is now blocked pending Kris Point’s response: whether they’ll implement the more involved redesign or seek further clarification. No maintainer decision has been made yet, and the patch remains under review.

The key open question is how the autostash should behave in edge cases (e.g., conflicts during application or reset failures). Phillip’s approach ensures the autostash is only saved if conflicts arise and that merge state persists if the reset fails, but it may require additional test coverage. Kris hasn’t yet responded to the critique.

---