This is a maintainer response in the "What's cooking" thread, addressing a procedural dependency between two commit-reach topics.

Junio acknowledges Kristofer Karlsson’s clarification about the blocker for `kk/merge-base-exhaustion` (which depends on `kk/commit-reach-find-all-fix` and `kk/commit-reach-optim`) but sidesteps the suggested solutions (status update or rebase). Instead, he describes a workflow for handling patch dependencies: contributors should base their work on `master` and explicitly merge prerequisite branches (`B` and `C`) into their topic branch (`A`) before submitting. This approach avoids rebasing integration branches and keeps the dependency graph transparent. The response is terse and technical, offering no opinion on the specific topics but reinforcing a standard practice for managing interdependent series.

The key detail for future summarization is the workflow recommendation itself: when a patch series depends on multiple topics, contributors are encouraged to merge those branches into their own branch (based on `master`) rather than relying on integration branches like `next`. This is now the authoritative guidance for resolving similar dependency chains in the future. No changes to the "What's cooking" status are implied, and the procedural blocker for `kk/merge-base-exhaustion` remains unresolved pending Kristofer’s next steps.