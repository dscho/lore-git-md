This is the first patch in the v3 series that resolves the post-merge issues with the parse-options help exit code fix. It addresses the overly restrictive `SVN` test prerequisite by introducing a simplified check that tests only for the availability of the Subversion Perl modules, dropping the problematic Perl version comparison that caused false negatives in earlier versions.

The patch modifies `t/t1517-outside-repo.sh` to add a new `SVN` prerequisite that runs a minimal Perl script to verify the presence of `SVN::Core` and `SVN::Repos`. This ensures that `git-svn` tests are skipped only when the modules are genuinely unavailable, rather than failing due to version string parsing issues. The change also updates the test loop to use this prerequisite for `svn` commands, aligning it with the existing pattern for `instaweb` (which uses `PERL`).

This is a targeted cleanup of the merged series, not a reopening of the core exit-code debate. The prerequisite logic is now consistent with `t/lib-git-svn.sh` and avoids the shell-quoting bug that previously caused the version check to misbehave. The patch is uncontroversial and directly resolves one of the two post-merge issues identified in the thread.