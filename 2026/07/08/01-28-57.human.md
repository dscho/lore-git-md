This is a post-merge cleanup patch from Junio C Hamano, responding to the recently merged four-patch series that standardized help-flag exit codes across Git’s parse-options API. The patch fixes an overly restrictive test prerequisite in `t/t1517-outside-repo.sh` that previously caused `git-svn` tests to skip incorrectly when Subversion Perl modules were present but the version check failed due to shell-quoting issues.

The change replaces the problematic Perl version comparison (`$SVN::Core::VERSION gt '1.1.0'`) with a minimal Perl script that simply tests for the availability of `SVN::Core` and `SVN::Repos`. This aligns the prerequisite logic with the existing pattern in `t/lib-git-svn.sh` and ensures tests skip only when the modules are genuinely unavailable. The patch is uncontroversial and directly addresses one of the two post-merge issues identified in the thread, with Junio noting that the original version was "broken" due to the version check it omitted.

This is a small but necessary follow-up to the larger exit-code standardization effort, ensuring test reliability without reopening the core behavioral debate. The patch is already merged, reflecting its low-risk, high-utility nature.