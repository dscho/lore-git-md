This is Junio's **merge confirmation** for Henrique Ferreiro's performance bugfix in `unpack-trees.c`. The email is a terse acknowledgment ("Thanks.") signaling that the patch is now **merged to 'master'**.

The fix eliminates a quadratic-time index scan in `next_cache_entry()` triggered by `git diff` with a pathspec matching a subtree whose entries appear early in the index. The optimization—adding a `cache_bottom` field to `o->internal` to track forward-scan progress—is minimal (4 lines) and preserves existing ordering invariants, as Junio explicitly approved in prior review ("sensible"). Performance impact is dramatic: an 8-minute operation on a Chromium-scale repository now completes in 0.07 seconds. The regression test (`t/perf/p0009-diff-pathspec.sh`) scales entry count via the `EXPENSIVE` prerequisite (10,000 entries by default, 100,000 when enabled), showing a 99.7% speedup.

Key technical anchors for follow-up discussion:
- **File modified**: `unpack-trees.c`
- **Function modified**: `next_cache_entry()`
- **New field**: `o->internal.cache_bottom`
- **Test added**: `t/perf/p0009-diff-pathspec.sh`
- **Benchmark**: `git diff HEAD -- <pathspec>` with a subtree path as the first index entry
- **Safety argument**: The optimization is safe because `find_cache_pos()` already advances `.cache_bottom` past unpacked entries (commit `e53e6b4433`)

The patch is now part of the next Git release. No further review or iteration is expected unless edge cases emerge in integration testing.