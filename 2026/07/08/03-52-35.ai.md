This is the v2 of Jeff King’s seven-patch series refactoring the `git_hash_*()` API. The goal remains unchanged: make `git_hash_discard()` idempotent and standardize usage of the hash API across the codebase. v2 addresses all review feedback from v1—cosmetic fixes (unnecessary braces, typos, trailing blank lines), improved documentation clarity, and additional rationale in the final patch explaining why `git_hash_final()` is not made idempotent. The series is now 24 files changed, 177 insertions, 88 deletions, with a new Coccinelle semantic patch enforcing consistent wrapper usage. No technical objections remain; the series is ready for integration. Key symbols: `git_hash_ctx.active`, `git_hash_discard()`, `git_hash_init()`, Coccinelle rule `tools/coccinelle/hash.cocci`. Files touched span core object-writing code, built-ins, and test helpers. The design choice to leave `git_hash_final()` unprotected (triggering a `BUG()` on misuse) is now explicitly justified in the commit message, aligning with industry-standard practice for hash libraries.