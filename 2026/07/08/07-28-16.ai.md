This is a **test-placement discussion** in the reftable backend performance regression fix thread. Kristofer Karlsson agrees with Patrick Steinhardt’s suggestion to drop the newly added correctness test (`t0610-reftable-basics.sh`) rather than relocate it to `t1400-update-ref.sh`. He acknowledges that the delete-then-recreate flow is already implicitly covered by existing tests in `t1400`, which share repository state across runs, and concedes that adding an explicit, self-contained test would risk bloating the test suite without adding meaningful coverage. The decision removes a minor point of contention ahead of the next patch iteration, leaving only the perf test (`p1401-ref-store-tombstones.sh`) and the core backend fix in place. No technical objections remain; the thread is now focused on finalizing the performance test’s measurement isolation.