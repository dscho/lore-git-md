This is a **test design discussion** in the ongoing reftable backend performance regression thread. Patrick Steinhardt follows up on Kristofer Karlsson’s reply about the correctness test (`t0610-reftable-basics.sh`) introduced in the first patch of the series.

Patrick now suggests consolidating the test into `t1400-update-ref.sh`, Git’s existing test suite for ref operations, rather than keeping it in `t0610-reftable-basics.sh`. His reasoning is straightforward: the test verifies general ref behavior (delete-then-recreate with tombstones present), not backend-specific logic. While Kristofer couldn’t find an existing test covering this exact scenario, Patrick points out that `t1400` already exercises similar flows implicitly. Moving the test would avoid duplicating coverage and keep ref-related tests centralized.

The suggestion is minor but practical—it doesn’t challenge the test’s necessity or the patch’s core fix, just its placement. Kristofer’s earlier defense of the test (that tombstone interaction isn’t explicitly covered elsewhere) remains valid, but Patrick’s proposal offers a cleaner organization. No technical objections or new issues are raised; the discussion is now about test hygiene rather than functionality. The next step is likely a v2 patch that either moves the test or justifies its standalone status.