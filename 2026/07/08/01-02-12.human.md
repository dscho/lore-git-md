This is the maintainer's merge signal for the `git replay --linearize` feature series. Junio C Hamano considers the v7 iteration technically complete, with all prior review feedback addressed, and is marking the topic for inclusion in the `next` branch. The series teaches `git replay` a `--linearize` option that flattens merge commits into a linear history, offering a simpler alternative to Johannes Schindelin’s earlier merge-replay implementation.

The last substantive discussion point—whether `--linearize` and `--revert` should be mutually exclusive—was resolved in v7 by removing the restriction and allowing the two options to be used together. Junio had previously raised an edge case (replaying a partial merge range with both options) but did not block the series, instead leaving room for incremental polishing. The series is now ready for wider integration testing in `next`, where any remaining issues can surface before graduation to `master`.

No new technical objections or interface design debates remain open. The series is expected to land in `next` shortly, with potential trivial conflicts noted against Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series.