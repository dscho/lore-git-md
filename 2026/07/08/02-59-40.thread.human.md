This thread fixes race conditions in Git’s HTTP daemon test helpers that could cause HTTP 500 errors during concurrent requests. Michael Montalbo’s four-part series replaces non-atomic shell operations in two CGI scripts (`apply-one-time-script.sh` and `http-429.sh`) with atomic alternatives (`rm` without `-f` and `mkdir`), ensuring robust handling of shared state under Apache’s concurrent request model. The series is now complete after addressing all feedback, including Junio C Hamano’s and Patrick Steinhardt’s editorial suggestions on commit-message clarity and documentation placement. Patrick’s latest endorsement ("the whole series reads a lot better now") signals readiness for integration into `next`.

The core issue—a race between `test -f` and `rm`/`touch` operations—was resolved by simplifying the logic to use single atomic operations, with careful attention to error handling (e.g., suppressing expected `stderr` when scripts are removed by concurrent requests). The documentation update, now in `t/lib-httpd.sh`, explicitly recommends PID-inclusive scratch file names and atomic operations for concurrency safety. Michael’s final reply confirms the series is stable, with no further changes needed, and links to recent CI failures (GitGitGadget and GitHub’s Git mirror) caused by the same race, underscoring the fix’s urgency. The series is a targeted bugfix with no dependencies or broader implications.