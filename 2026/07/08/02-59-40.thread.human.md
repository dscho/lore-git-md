This thread is about a three-patch series from Michael Montalbo that fixes race conditions in Git's HTTP daemon test helpers. The series targets two CGI scripts in `t/lib-httpd/` that manage shared state using non-atomic shell operations, which can fail when Apache serves concurrent requests—such as during a partial fetch that retrieves a promisor object while the first response is still being processed. The fixes replace the racy operations with atomic alternatives: `rm` (without `-f`) for one-shot markers and `mkdir` for first-request election, ensuring the test helpers behave correctly under concurrency.

Junio C Hamano and Patrick Steinhardt have reviewed the series, with Junio marking it as ready for integration after v2. The latest version (v3) addresses all feedback: patch 1 now uses `rm` instead of `mv` for atomicity, patch 2 replaces a non-atomic "check-and-set" with `mkdir`, and patch 3 relocates documentation to `t/lib-httpd.sh` for better context. A new test (`t5567-one-time-script.sh`) verifies the one-shot marker logic deterministically. The only remaining discussion is about commit message verbosity—Michael has agreed to rewrite the AI-generated messages for conciseness, but no technical changes are needed. The series is complete and awaits integration into Junio's `next` branch.