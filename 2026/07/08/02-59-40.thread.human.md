This thread fixes race conditions in Git's HTTP daemon test helpers, making the test suite more robust against concurrent execution. Michael Montalbo's v2 series (three patches) replaces non-atomic shell operations in `t/lib-httpd/` scripts with atomic alternatives—`mv` for one-shot markers and `mkdir` for first-request election—to prevent HTTP 500 errors when multiple requests race. Junio C Hamano reviewed both versions, marking the series ready for integration after Michael addressed feedback on commit-message clarity and rationale. The only remaining discussion was editorial: Patrick Steinhardt suggested making the new documentation section HTTP-specific, but deferred to Michael's argument that the atomic idioms are broadly useful. The series is now complete, with no open technical questions, and targets only the test infrastructure (`t/` directory). A new test (`t5567-one-time-script.sh`) exercises the race deterministically, and the documentation patch (3/3) now includes Patrick's minor wording improvements while retaining its generic scope. The fixes are internal to the test helpers and introduce no user-visible changes.