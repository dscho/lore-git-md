This is the third and final patch in the **v8 Rustification build system series**, which is **queued in Junio's integration branches** (status: under review, in `next`). The patch **adds CI verification for the macOS credential helper (`git-credential-osxkeychain`)** by wiring it into `contrib/Makefile` on macOS, ensuring it is built and tested during standard CI runs with `TEST_CONTRIB_TOO=yes`.

The change is straightforward: it introduces a new `test` target in `contrib/credential/osxkeychain/Makefile` that depends on building the helper, and extends `contrib/Makefile` to include `credential/osxkeychain` in the `all`, `test`, and `clean` targets when running on macOS (Darwin). The patch also explicitly includes `../config.mak.uname` at the top of `contrib/Makefile` to ensure `$(uname_S)` is correctly defined, fixing a build system correctness issue identified in v7.

This is a **well-scoped, low-risk change** that closes a long-standing gap in CI coverage for the macOS credential helper. The patch is **ready for integration** and aligns with the series' overall goal of improving build reliability and platform support for Rust components. No new technical concerns were raised in this iteration, and the unresolved artifact layout question (Patrick Steinhardt's feedback) does not affect this patch. The series is likely to graduate to `next` in the current cycle.