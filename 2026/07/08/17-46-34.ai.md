This is a maintainer response in an ongoing bugfix thread about `git merge --abort` and autostash preservation. Junio C Hamano acknowledges Phillip Wood’s critique of the original patch and agrees with the identified flaw: the current approach would incorrectly save the autostash even if it applies cleanly, and the reset logic’s reliance on `cmd_reset()` introduces unintended side effects (e.g., redundant stash creation via `remove_branch_state()`).

Junio endorses Phillip’s proposed solution—decoupling the reset logic from `cmd_reset()`—calling it a "great thing to do" and framing it as part of a broader effort to eliminate the anti-pattern of one built-in command calling another as a subroutine. This aligns with the target branch (`ps/history-drop` in `seen`), where similar refactoring is already underway. The thread now hinges on whether Kris Point (the original author) will implement the more involved fix or seek further clarification.

Key technical details for follow-up: the fix will involve modifying `builtin/merge.c` to avoid calling `cmd_reset()`, instead directly invoking lower-level functions like `reset_working_tree()` or `reset_head()` with a new flag (e.g., `RESET_WORKING_TREE_MERGE`) to control autostash behavior. The goal is to ensure the autostash is only saved if conflicts arise during application and that merge state persists if the reset fails. No new CLI options or on-disk format changes are expected, but the refactoring may require additional test coverage for edge cases (e.g., autostash application conflicts). The patch’s current state is effectively blocked pending this redesign.