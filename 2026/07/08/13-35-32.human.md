This is a **bugfix** for the `merge --abort` command, but the current patch is under **substantive review** that questions its approach. The patch aims to prevent data loss by deferring deletion of the `MERGE_AUTOSTASH` ref until after `git reset --merge` succeeds, but Phillip Wood argues the fix is incorrect because it misaligns with Git's intended behavior: the autostash should only be saved if it applies with conflicts, and if the reset fails, the merge state (including `MERGE_AUTOSTASH`) should be preserved rather than converted into a stash.

Phillip proposes a more involved solution: bypass `cmd_reset()` entirely and extend `reset_working_tree()` (or `reset_head()` in newer branches) to support a "merge" reset mode. This would allow proper sequencing—reset first, apply the autostash, then clean up merge state only if the reset succeeds. He suggests basing the fix on the `ps/history-drop` branch in `seen`, which has renamed the relevant function. The review is thorough and identifies a real flaw in the patch's logic, though the fix will require more work than the original submission.

The discussion is still open, with no revised patch yet. Kris Point’s initial fix is well-motivated but flawed in its execution, and Phillip’s feedback provides a clear path forward—though one that demands deeper changes to the reset machinery. If you care about `merge --abort` robustness, this thread is worth following.