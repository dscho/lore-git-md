This is a **performance bugfix** for `unpack-trees.c` that resolves a quadratic-time index scan in `next_cache_entry()`. The patch is now in its final stages of review, with Junio explicitly acknowledging that Henrique’s argument about the safety of the new `cache_bottom` optimization—specifically, that `find_cache_pos()` already advances `.cache_bottom` past unpacked entries (citing commit `e53e6b4433`)—is "sensible." This directly addresses the critical correctness concern raised in Junio’s earlier substantive review, leaving only a minor test improvement (scaling the perf test’s entry count via the `EXPENSIVE` prerequisite) as the remaining action item.

The fix itself is minimal (4 lines in `unpack-trees.c`) and introduces a `cache_bottom` field to `o->internal` to track forward-scan progress, allowing subsequent calls to skip already-unpacked entries. The performance improvement is dramatic: on a Chromium-scale repository (~500k index entries), `git diff HEAD -- <pathspec>` drops from 8 minutes to 0.07 seconds (99.7% speedup). The included perf test (`t/perf/p0009-diff-pathspec.sh`) creates a 100k-entry index with a problematic layout (pathspec `aaa/file` as the first entry) to reproduce the issue, though Junio’s suggestion to make the entry count configurable via `EXPENSIVE` remains unimplemented.

Key technical details for follow-up discussion:
- **File/function**: `unpack-trees.c`, `next_cache_entry()`.
- **New field**: `o->internal.cache_bottom` (forward-scan progress tracker).
- **Test**: `t/perf/p0009-diff-pathspec.sh` (needs `EXPENSIVE` scaling for entry count).
- **Base commit**: `e90019fcafe0040228b8631c30f97ae1adb61bcd`.
- **Safety argument**: The optimization preserves ordering invariants because `find_cache_pos()` advances `.cache_bottom` past unpacked entries (commit `e53e6b4433`).

The patch is well-motivated, thoroughly tested, and now addresses the primary correctness concern. If the `EXPENSIVE` test tweak is incorporated, this is likely to land soon. The only remaining risk is whether further review uncovers edge cases in the interaction between the new `cache_bottom` and the existing `.cache_bottom` logic, but Junio’s explicit approval of Henrique’s reasoning suggests this is unlikely.