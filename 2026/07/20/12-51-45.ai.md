This is a substantive discussion in the `git config` bug report thread about the interaction between `--global` and `include.*` directives. Jeff King (Peff) clarifies that the current behavior—where `git config --global` does not respect `include.*` by default—is intentional, rooted in backwards compatibility and a design choice to avoid surprising users who explicitly request a specific file. He traces the behavior to the original implementation of `--include`, where `--global` was treated as a synonym for `--file=$HOME/.gitconfig`, and the parser was designed to avoid reading outside the explicitly named file.

Peff also critiques the `lbmk` project’s use of `--global` in its config-checking logic, arguing that it is overly restrictive. He suggests alternatives: omitting `--global` entirely (letting `git config` resolve the value as Git would internally) or using `git var GIT_AUTHOR_IDENT` for identity checks, which accounts for environment variables and derived values. His analysis includes a nuanced discussion of what `lbmk` might actually want to check—whether the user configured Git explicitly (favoring `git config user.name`) or whether Git can derive an ident at all (favoring `git var`).

The email advances the thread by resolving the "is this a bug?" question (it’s not) and shifting focus to the broader design implications. It also introduces a potential follow-up: whether `lbmk`’s usage is correct or should be revised. The tone is constructive, and the technical details (e.g., the `author.*`/`committer.*` split, `git var` behavior) are precise enough to inform future discussion.