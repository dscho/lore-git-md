This is a **substantive review reply** in the ongoing discussion of patch 3/4 of Toon Claes’s `git last-modified` Bloom filter optimization series. Jeff King (Peff) responds to Taylor Blau’s proposed fix for the `--show-trees` correctness bug, where ancestor tree entries (e.g., `d/`) appear in the output even when the pathspec targets a subpath (e.g., `d/a`).

Peff agrees with Taylor’s approach of filtering out uncovered paths in `add_path_from_diff()` using `match_pathspec()`, calling it "exactly what I was thinking." He raises a minor concern about whether `match_pathspec()` is the right tool, noting that tree diffs internally use `tree-walk.c:do_match()` instead. However, he concludes this is likely an existing oddity and not a blocker, since the two functions are meant to be equivalent.

Peff also points out a corner case: when querying a directory (e.g., `Documentation/technical/`), the output includes both the directory itself and its contents. While the proposed fix would remove the parent directory (`Documentation/`), it would still show the queried directory (`Documentation/technical/`), which may not align with user expectations. He suggests this is a minor quirk and not worth over-engineering, especially for an experimental command.

The discussion remains focused on **correctness and usability trade-offs**, with both reviewers now aligned on the simpler solution—removing uncovered paths entirely. The next step is likely a reroll of patch 3/4 incorporating this logic. No new technical concerns are raised, and the tone suggests this issue is close to resolution.