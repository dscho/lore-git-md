This is a bugfix follow-up to the fifth patch in Johannes Schindelin’s 11-patch series addressing Coverity-reported unchecked return values. Junio C Hamano provides a one-line fix (`continue` → `goto cleanup`) to ensure that the `active_paths` slab is properly cleaned up when a commit in the `--not` queue fails to parse. The change is minimal and surgical: it plugs a resource leak without altering the patch’s overall goal or introducing new behavior. The fix is clearly motivated and directly addresses the correctness concern Junio raised earlier in the thread. No controversy or alternative approaches are proposed; this looks like a straightforward correction that should be uncontroversial. The patch is ready to be squashed into the original fifth patch before the series advances.