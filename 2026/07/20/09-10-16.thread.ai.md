Thread identity
Subject: [PATCH 0/2] remote: fix push tracking after remote rename
Series version: v2 (v1 previously posted)
Originating author: Harald Nordgren (via GitGitGadget)
Thread type: patch series

Core problem or goal
When a remote is renamed (e.g., from `origin` to `upstream`), Git’s push tracking logic loses the configured remote name and `git status` stops displaying the push destination, even though the actual push still works. The goal is to restore the push-tracking display by resolving the URL of the renamed remote to any remaining remote that shares the same URL. The series also extends support to allow a URL (rather than just a remote name) as the value for `branch.<name>.pushRemote`, treating it as a lookup key for a uniquely matching configured remote. The extension is motivated by workflows involving GitHub’s `gh` CLI, which renames remotes during forking.

Current status
v2 posted with revised commit messages that clarify the design intent: URL-based push-remote resolution is an *extension* to support workflows involving renamed remotes (notably those created by GitHub’s `gh` CLI), not a fix for an existing capability. Junio C Hamano has confirmed the revised commit messages address his feedback. Patch 1/2 (mechanical `the_repository` removal) has been reviewed and requires no changes. Patch 2/2 (URL-based fallback logic) is under active review; Junio has identified a **technical gap** in the patch’s logic: it only considers the `url` member of a remote when looking for a fallback, but should also consider the `pushurl` member (and ignore `url` when `pushurl` exists). The author has not yet responded to this new feedback. D. Ben Knoble’s earlier substantive question about the real-world workflow remains unanswered.

Key technical details
- Files touched: `remote.c`, `remote.h`, `transport.c`, `Documentation/config/branch.adoc`, `Documentation/revisions.adoc`, new test script `t/t5505-remote.sh` (104 lines).
- Subsystem: remote tracking, specifically the push branch display logic.
- New behavior: if the originally configured push remote is missing (due to rename) or is a URL, Git consults the URL-to-remote map to find a fallback remote that shares the same URL or pushurl. If exactly one such remote exists, it is used to resolve `@{push}` and update the remote-tracking branch after a push; if multiple remotes share the URL, the behavior remains unchanged (no tracking) to avoid ambiguity.
- New symbols: `repo_remote_for_push_tracking()` in `remote.h` (consults the URL-to-remote map), `tracking_for_push_dest()` now takes an explicit `struct repository *` parameter (used in patch 2/2).
- On-disk format: unchanged.
- Test coverage: 104-line test script in `t/t5505-remote.sh` exercises rename scenarios, URL resolution, up-to-date pushes, and the edge case of duplicate URLs. The test does not currently cover the `pushurl` case identified by Junio.
- Documentation: `Documentation/config/branch.adoc` and `Documentation/revisions.adoc` updated to document that `branch.<name>.pushRemote` may now be a URL and how `@{push}` behaves in that case.

Open questions and loose ends
1. Junio has identified a technical gap in patch 2/2: the fallback logic must consider both `url` and `pushurl` members of a remote, preferring `pushurl` when it exists. The author must revise the patch to address this oversight.
2. D. Ben Knoble’s substantive question remains unanswered: what is the concrete, command-by-command recipe that reproduces the problem? The question centers on whether the issue arises when `branch.<name>.pushRemote` is set to a *remote name* (which should be updated on rename) or only when it is set to a *URL* (which would not be updated). The author has not yet provided a clarification or pointed to the test cases that demonstrate the scenario.

Participant positions
- Harald Nordgren (author): posted v1 with preparatory refactoring (patch 1/2) and core fix (patch 2/2), then v2 with revised commit messages clarifying the design intent (URL support as an extension, not a fix). Patch 1/2 is a mechanical `the_repository` removal step; patch 2/2 implements the URL-based fallback logic. The commit messages and test coverage are thorough, addressing fork workflows and edge cases.
- Junio C Hamano (maintainer): reviewed patch 1/2 and patch 2/2 in detail. Confirmed the revised commit messages for patch 1/2 address his feedback. Identified a **technical gap** in patch 2/2: the fallback logic must consider both `url` and `pushurl` members of a remote, preferring `pushurl` when it exists. Provided a concrete code suggestion for the upfront check and loop condition. No other technical objections raised; the rest of the patch appears uncontroversial.
- D. Ben Knoble: raised a substantive question about the real-world workflow that motivates the change, specifically whether the problem occurs when `branch.<name>.pushRemote` is set to a *remote name* or only when it is set to a *URL*. The question does not object to the patch but seeks clarification to better understand the scenario. No technical objections or approvals.

Related work
- Patch 1/2 is part of the ongoing `the_repository` removal effort.