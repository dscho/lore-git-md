This is the maintainer's reply to the **v3 patch series** adding the `--reset-when-found[=<where>]` option to `git bisect`. Junio notes that the range-diff between v2 and v3 is noisy, but the changes are mostly mechanical—renaming the option and internal symbols from `--auto-reset` to `--reset-when-found` to better reflect the behavior. The core implementation remains unchanged from the settled v2 design, including the deferred reset for `git bisect run` (addressing platform-compatibility concerns) and the parameterized target (`original` or `found`).

The email is brief and positive: Junio describes the series as "looking good" and signals intent to queue it in `next` if no further objections arise. There are no new technical concerns raised, and the thread context confirms all prior feedback has been addressed. The series is now effectively in the final review stage before integration. For readers tracking `git bisect` improvements, this is a green light that the feature is on track for the next release.