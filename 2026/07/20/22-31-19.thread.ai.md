Thread identity
Subject: [PATCH 0/2] stash push: avoid index expansion for cone-internal pathspecs
Series version: v1 (initial)
Originating author: tnyman@openai.com
Thread type: patch series

Core problem or goal
Optimize `git stash push -- <pathspec>` in sparse-checkout repositories by avoiding unnecessary sparse-index expansion when the pathspec is wholly inside the sparse-checkout cone. The current implementation expands the index to full form before checking pathspec matches, causing performance to scale with total tracked paths rather than sparse-directory entries. The series also fixes a latent heap-buffer-overflow bug in `pathspec_needs_expanded_index()` triggered by wildcard pathspecs from subdirectories.

Current status
**Cooking in `next`** (marked for integration by Junio C Hamano on 2025-06-10). Fully reviewed; v1 posted. No prior versions. Patch 1/2 received a **substantive review** from Taylor Blau (ttaylorr@openai.com), confirming a heap-buffer-overflow fix with ASan reproduction and endorsing the mechanical correctness of the pathspec helper refactor. Patch 2/2 received a **surface-level review** from Taylor Blau, approving the patch’s structure and removal of a TODO comment but not engaging with sparse-index logic or performance claims. All tests pass (sparse-index, stash, pathspec, SHA-256, ASan regression, p2000 performance suite). No open questions or objections raised.

Key technical details
- Files touched: `pathspec.c` (refactor + bugfix), `builtin/stash.c` (optimization), `t/t7012-skip-worktree.sh` (tests), `t/perf/p2000-sparse-operations.sh` (performance test), `t1092-sparse-checkout-compatibility.sh` (additional test coverage)
- New/renamed symbols: none
- Behavior change: `git stash push -- <pathspec>` now checks pathspec matches against the sparse index before deciding whether to expand; if the pathspec is cone-internal, the index remains sparse. The pathspec helper now consistently uses `item.match` (prefixed path) instead of `item.original` (unprefixed argument) for all comparisons, fixing a heap-buffer-overflow when `item.match` is longer.
- Performance: cone-internal stash push improved from 18.9 s → 0.06 s in cone mode; full-index mode unchanged (1.62 s → 1.65 s).
- On-disk format: unchanged
- Test coverage: pathspec edge cases (prefixed wildcards, subdirectory invocations, literal/prefixed/wildcard/file-based/multiple/staged/missing pathspecs), heap-buffer-overflow regression test (ASan-confirmed), p2000 performance suite addition (`git stash push -- $SPARSE_CONE/a`).

Open questions and loose ends
None; series is self-contained, passes all listed test suites, and includes ASan regression coverage. Junio’s merge signal indicates readiness for integration.

Participant positions
- Author (tnyman@openai.com): posted v1; reports dramatic performance improvement, full test coverage including ASan regression, prerequisite bugfix for the optimization, and leverages existing sparse-index-aware logic from `git rm` (commit bcf96cfca6).
- Taylor Blau (ttaylorr@openai.com): **substantive review** of patch 1/2 (reproduced heap-buffer-overflow with ASan, confirmed crash in `strspn()` due to `item.original` misuse, endorsed mechanical replacement as correct and consistent with function intent, approved added test in `t1092-sparse-checkout-compatibility.sh`); **surface-level review** of patch 2/2 (approved patch structure and TODO removal, no technical engagement with sparse-index logic or performance claims). No concerns or open questions raised; both patches appear ready.
- Junio C Hamano (gitster@pobox.com): **merge signal** (2025-06-10), marking series for `next` branch. No additional technical feedback.

Related work
- Leverages logic from `git rm` (commit bcf96cfca6) and `git reset` for sparse-index-aware pathspec matching.
- Fixes a latent bug in the pathspec helper introduced in 2021.
- Targets the sparse-index subsystem (Derrick Stolee, Elijah Newren).