This is a **substantive review** in the ongoing `git config` bug report thread. Ben Knoble engages with the reported behavior rather than just the surface details, suggesting alternative approaches and probing the design intent behind the current implementation.

Knoble’s reply questions the necessity of using `--global` in the first place. He proposes two alternatives: omitting the scope entirely (`git config user.name`), which would respect includes by default, or using `git var GIT_AUTHOR_IDENT`, which retrieves the author identity without requiring explicit config file handling. His comment implicitly challenges the assumption that `--global` is the right tool for the job in the `lbmk` project’s use case, though he does not outright dismiss the reported inconsistency.

The review is concise but substantive—it shifts the discussion from "why doesn’t `--global` respect includes?" to "should `--global` even be used here?" This could reframe the thread if other participants weigh in on whether the current behavior is a bug or a deliberate design choice that users should work around. No patch or code change is proposed, but the feedback introduces a new angle for the original author and future reviewers to consider.