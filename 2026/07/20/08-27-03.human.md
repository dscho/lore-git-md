This is the fourth patch in Harald Nordgren’s v10 series introducing `git history squash`. It refactors the sequencer’s squash-message helpers into reusable functions and moves the `todo_item_flags` enum to `sequencer.h` so other commands can produce the same template format as `git rebase -i --autosquash`.

The patch extracts three static helpers from `sequencer.c` and exposes them in `sequencer.h`: `add_squash_combination_header` (the "This is a combination of N commits" banner), `add_squash_message_header` (the numbered headers above each kept or skipped message), and `squash_subject_comment_len` (determines whether to comment out a `fixup!`/`squash!`/`amend!` subject). It also moves the `todo_item_flags` enum to the header so callers can distinguish `amend!` from plain `fixup!` when reading the output of `todo_list_rearrange_squash()`.

This is pure code motion with no behavior change. The refactoring enables patch 5/5 to implement `--reedit-message` in `git history squash` using the same template as `git rebase -i --autosquash`, ensuring consistency. The patch is small (70 insertions, 31 deletions) and focused, touching only `sequencer.c` and `sequencer.h`. It is ready to merge and uncontroversial.