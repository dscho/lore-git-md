This is a **performance optimization** for `git stash push` in sparse-checkout repositories, completing the two-patch series. Taylor Blau’s review of patch 2/2 is **surface-level**: he confirms the patch is well-structured and removes a TODO comment, but does not engage with the technical details of the sparse-index logic or performance claims. His Reviewed-by is based on the patch’s clarity and alignment with the series’ goal, not on substantive testing or edge-case analysis.

The patch itself is the payoff of the series: it modifies `builtin/stash.c` to call `pathspec_needs_expanded_index()` before expanding the index, avoiding unnecessary expansion when stashing paths inside the sparse-checkout cone. Performance gains are dramatic (18.9s → 0.06s in cone mode) with no regression in full-index mode (1.62s → 1.65s). Test coverage in `t1092-sparse-checkout-compatibility.sh` exercises literal, wildcard, and multiple pathspecs, while a new p2000 performance test case (`git stash push -- $SPARSE_CONE/a`) quantifies the speedup.

The series is now fully reviewed (patch 1/2 received a **substantive review** from Taylor, confirming a heap-buffer-overflow fix; patch 2/2 received this **surface-level** approval). No open questions remain, and the patches pass all listed test suites (sparse-index, stash, pathspec, SHA-256, ASan). The optimization leverages existing sparse-index-aware logic from `git rm` (commit bcf96cfca6) and is self-contained. Key files touched: `builtin/stash.c`, `t/perf/p2000-sparse-operations.sh`, and `t/t1092-sparse-checkout-compatibility.sh`. No new symbols or on-disk format changes.