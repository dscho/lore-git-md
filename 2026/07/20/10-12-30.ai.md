This is a **brief follow-up in the `the_repository` removal series** (v7, patch 2/4). Tian Yuchen acknowledges SZEDER Gábor’s correction about the availability of the `UNUSED` macro in `read-cache.h`. The misunderstanding was that `read-cache.h` cannot be compiled standalone (it is always included after `git-compat-util.h`), so the `UNUSED` macro is already available. This resolves the build-breaking issue cleanly: the `repo` parameter in `ce_mode_from_stat()` can now be marked as `UNUSED` in patch 2/4, avoiding the need for temporary workarounds like `(void)repo` or `TODO` comments. The fix preserves the series’ mechanical structure and aligns with Git’s established idiom for staged refactorings. No further changes are needed for this issue, and the series remains on track for integration.