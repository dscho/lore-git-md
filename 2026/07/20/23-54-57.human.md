This is the second patch in a two-patch performance optimization series targeting `git stash push` in sparse-checkout repositories. The patch applies sparse-index-aware logic to avoid unnecessary index expansion when stashing paths wholly inside the sparse-checkout cone, delivering a dramatic speedup (18.9 seconds → 0.06 seconds in cone mode) with no regression in full-index mode.

Taylor Blau’s review is brief but substantive: he confirms the patch is reasonable, notes the removal of a TODO comment as a positive step, and awards a `Reviewed-by`. The series is now fully reviewed by a domain expert (Taylor) and appears ready for integration. No open questions or concerns were raised in this reply.

The patch itself is minimal (two lines added, two removed in `builtin/stash.c`) and leverages existing logic from `git rm` and `git reset`. Test coverage is thorough, including a new p2000 performance test case and expanded sparse-checkout compatibility tests. The series is self-contained, passes all test suites, and includes AddressSanitizer regression coverage for the latent bug fixed in patch 1/2. This is a clear win for sparse-checkout users.