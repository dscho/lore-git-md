This is the first patch in Harald Nordgren’s `git history squash` series (v10, 1/5). It is a pure refactoring that extracts a helper function, `first_parent_tree_oid()`, to centralize the logic for resolving a commit’s first-parent tree (or the empty tree for a root commit). The helper is used in three places in `builtin/history.c`, replacing duplicated code that parsed the parent commit and copied its tree OID. There are no behavior changes—this is strictly a code hygiene improvement to reduce duplication and improve maintainability.

The patch is small (26 insertions, 32 deletions) and mechanical, touching only `builtin/history.c`. It introduces no new user-facing functionality, configuration, or test coverage. The helper is internal to the `history` builtin and is not exposed outside the file. This refactoring is a prerequisite for the later patches in the series, which reuse the helper to implement the core `squash` functionality.

Given the series’ overall status—functionally complete, with all feedback addressed in v10—this patch is ready to merge. It is uncontroversial and aligns with the project’s preference for small, focused refactorings that improve code clarity without altering behavior. The helper’s name and signature are clear, and the commit message succinctly explains the motivation. No further discussion or changes are expected.