This is a **substantive review discussion** in the ongoing `no-ref-delta` capability thread, focusing on the trade-offs between Taylor Blau’s proposed design and Jeff King’s suggested alternative.

Jeff King expands on the technical challenges of delta resolution in a streaming context, particularly the data dependencies introduced by `REF_DELTA` entries. While a "bases-before-deltas" capability would address ordering issues, `REF_DELTA` still complicates parallel processing: workers resolving deltas cannot immediately determine which worker is handling the base object, potentially disrupting caching strategies that rely on locality (e.g., reusing intermediate results from earlier in a delta chain). Even with `OFS_DELTA`, streaming resolution faces inherent challenges, such as unpredictably long delta chains that may reference bases far earlier in the pack, making it difficult to optimize intermediate-state caching in a single pass.

Peff acknowledges that restricting packs to `OFS_DELTA` could enable optimizations but remains hesitant about adding a protocol capability that Git itself wouldn’t use. His concerns center on whether the feature is a perfect fit for the write-through proxy’s needs or if it might later require adjustments, leaving Git with an unused or suboptimal protocol addition. The discussion highlights a tension between the proxy’s specific requirements (offset-based reconstruction, memory efficiency) and broader protocol design principles (flexibility, future-proofing). No consensus has emerged yet, but the exchange clarifies the technical stakes: the proxy’s design relies on `OFS_DELTA` for deterministic base resolution, while Git’s general-purpose delta resolution could benefit from more permissive alternatives. The thread remains in the design phase, with no code changes proposed in this email.