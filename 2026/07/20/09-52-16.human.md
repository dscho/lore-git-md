This is a **substantive review reply** in the ongoing `the_repository` removal series, specifically addressing a **build-breaking issue** in patch 2/4 of v7. The patch adds a `struct repository *repo` parameter to the `static inline` helper `ce_mode_from_stat()` in `read-cache.h`, but the parameter is unused in this patch, triggering `-Werror=unused-parameter` under `DEVELOPER=1`. SZEDER Gábor clarifies that the `UNUSED` macro *can* be used here because all C source files including `read-cache.h` must first include `git-compat-util.h`, where `UNUSED` is defined. This contradicts Tian Yuchen’s earlier claim that `UNUSED` was unavailable in this context.

The issue is **blocking** for integration, as it prevents clean builds during review. SZEDER’s correction simplifies the fix: instead of Tian’s proposed workaround (casting the parameter to `void` with a `TODO` comment), the parameter can be marked with the `UNUSED` macro directly. This is a **cleaner, more idiomatic solution** that aligns with Git’s existing practices for unused parameters in headers. The series remains uncontroversial and part of the broader `the_repository` removal effort, with all prior feedback already addressed. The only remaining hurdle is this **process-level oversight**, which does not challenge the technical design. Once resolved, the series is likely to proceed to `next`.