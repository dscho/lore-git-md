Thread identity
Subject: `git config --global` does not respect `include.*` directives by default
Series version: N/A (bug report)
Originating author: Hendrik Jaeger
Thread type: bug report

Core problem or goal
The thread investigates an unintuitive interaction between `git config --global` and `include.*` directives: `git config --global <key>` (without `--includes`) fails to find values moved to files included via `include.*`, while repository-level `git config <key>` respects includes by default. The discrepancy affects real-world usage (e.g., the `lbmk` project’s config checks) and raises questions about design intent and user expectations.

Current status
The "is this a bug?" question is resolved: the current behavior is intentional, rooted in backwards compatibility and a design choice to avoid surprising users who explicitly request a specific file. Junio C Hamano has definitively endorsed Jeff King’s analysis, closing the design discussion. The thread’s focus has shifted entirely to educating downstream projects (e.g., `lbmk`) about more appropriate alternatives for their use cases. No patch has been proposed or is expected.

Key technical details
- **Command**: `git config --global <key>` vs. `git config <key>`
- **Config files**: Global Git config (`~/.gitconfig`) with `include.*` directives
- **Behavior**: Repository-level `git config` respects includes by default; `--global` does not, treating `--global` as `--file=$HOME/.gitconfig` to avoid reading outside the explicitly named file
- **Man page claim**: `--global` is listed as an example of "giving a specific file" (implying includes should be respected, but the implementation does not match this expectation)
- **Workaround**: Explicit `--includes` flag or omitting `--global` entirely
- **Alternatives**:
  - `git config <key>` (no scope) to resolve values as Git would internally
  - `git var GIT_AUTHOR_IDENT` for identity checks, which accounts for environment variables, split `author.*`/`committer.*` settings, and all config layers—including includes—without requiring the caller to specify a scope
  - `git config user.name` (without scope) to check explicitly configured values
- **Design rationale**: `--include` was implemented to avoid surprising users who request a specific file, and `--global` was treated as a synonym for `--file=$HOME/.gitconfig`. This design is now confirmed as correct and will not change.

Open questions and loose ends
1. Should the man page be clarified to reflect the actual behavior of `--global` (i.e., that it does not respect `include.*` by default)? *(No resolution yet; no patch proposed.)*
2. How should downstream projects like `lbmk` be educated about the more appropriate alternatives (e.g., `git var GIT_AUTHOR_IDENT` or omitting scope)? *(No concrete plan proposed yet.)*

Participant positions
- **Hendrik Jaeger** (author): Reports the issue, demonstrates the inconsistency, and questions the design intent. Notes that `--includes` resolves their practical problem but leaves the broader question open.
- **Ben Knoble**: Substantive review questioning the necessity of `--global` in the first place. Proposes alternatives (`git config <key>` without scope or `git var GIT_AUTHOR_IDENT`) that respect includes by default, implicitly challenging whether the current behavior is a bug or a deliberate design choice.
- **Jeff King (Peff)**: Resolves the "is this a bug?" question by explaining the intentional design and backwards-compatibility rationale. Critiques `lbmk`’s use of `--global` as overly restrictive and suggests alternatives (omitting scope or using `git var`). Provides precise technical context (e.g., `--global` as `--file=$HOME/.gitconfig`, `author.*`/`committer.*` split, `git var` behavior).
- **Junio C Hamano**: Endorses Peff’s analysis definitively, closing the design discussion. Reinforces that `git var GIT_AUTHOR_IDENT` is the correct tool for checking whether Git can derive an author identity, as it accounts for all config layers and environment variables without requiring scope specification.

Related work
None mentioned.