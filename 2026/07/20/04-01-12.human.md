This is a brief but substantive exchange in the ongoing `git repo info` path-keys series. Junio C Hamano responds to Lucas Seiki Oshiro’s concern about the `path.grafts` keys returning a path even when the grafts file does not exist. Junio frames the issue as a design question: whether the command should report the *intended* location (even if the file is absent) or only the *actual* location (if the file exists). He leans toward the former, noting that scripts may want to know where to *create* the file, not just where to read it. This aligns the behavior with `git rev-parse --git-path`, which also returns paths without existence checks.

The reply does not resolve the question outright but clarifies the trade-off: reporting a non-existent path is not a bug if the goal is to expose Git’s internal path resolution logic. The discussion is now squarely about user expectations and consistency with existing Git commands. No code change is proposed yet, but the thread’s direction is now clearer: the series’ current behavior is defensible, and any change would require broader agreement on what `git repo info` should promise.