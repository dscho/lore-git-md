This is the first patch in Junio C Hamano’s four-part `git add --resolved` series (v3). It is a pure refactoring: a whitespace-only reindentation of the `skip_submodule()` function in `read-cache.c`. The misindented block was introduced five months ago in commit a16c4a245a, which added `--force` support for submodules with `ignore=all` configuration. The patch realigns the code to match the surrounding style, making it easier to read and maintain.

The change is mechanical and uncontroversial. It touches only indentation, not logic, and is a prerequisite for the later patches in the series that modify the same function. The diff shows 36 lines reindented to match the prevailing style in the file. No behavior changes are introduced, and the patch is unlikely to attract substantive review comments.

This patch is part of the ongoing `git add --resolved` effort, which aims to let users stage only conflict-resolved paths without accidentally including unrelated local changes. The series as a whole is technically complete and addresses all prior feedback, but remains under discussion due to a conceptual disagreement over the command-line interface design. This particular patch, however, is purely cosmetic and unrelated to that debate.