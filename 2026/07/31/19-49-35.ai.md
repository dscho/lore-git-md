This is a test portability patch, part of Pablo Sabater’s GSoC v2 series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch replaces a shell pipeline (`wc -c <two.t | xargs`) with Git’s `test_file_size()` helper in `t/t5701-git-serve.sh`, addressing a review comment from Junio C Hamano in the v1 discussion. The change is mechanical and uncontroversial, improving cross-platform consistency without altering test logic. It is the second patch in the six-patch series, which is now complete and ready for maintainer consideration after addressing all v1 feedback. The helper is already used elsewhere in the test suite, so this aligns with established practice. No new symbols or behavior changes are introduced; the patch simply makes the test more robust.