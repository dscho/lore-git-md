This is the third patch in Junio C Hamano’s four-part `git add --resolved` series (v3). It introduces `remove_file_from_index_with_flags()`, a new helper in `read-cache.c` that brings index-removal operations in line with the existing `add_file_to_index()` API by supporting `--dry-run` (`ADD_CACHE_PRETEND`) and `--verbose` (`ADD_CACHE_VERBOSE`) flags. The patch is a refactoring that consolidates scattered reporting logic in `update_callback()` into a single, reusable function.

The change is small and mechanical: the new helper wraps `remove_file_from_index()` and handles the pretend/verbose logic that callers previously had to duplicate. The only caller updated in this patch is `update_callback()`, but the helper is clearly designed for broader use, particularly by the upcoming `--resolved` implementation in patch 4/4. The commit message is concise and accurately describes the motivation and scope.

This is a well-crafted refactoring that addresses a real inconsistency in the codebase. The patch is uncontroversial and ready for review; the only open question is whether future callers (like `--resolved`) will need additional flags, but that can be handled in follow-up patches if needed. Reviewers should verify that the helper behaves correctly with edge-case flag combinations (e.g., both `ADD_CACHE_PRETEND` and `ADD_CACHE_VERBOSE` set) and that the updated `update_callback()` logic remains functionally identical.