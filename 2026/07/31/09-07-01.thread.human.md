This thread is about a small but useful documentation improvement to the `git-refs(1)` man page. Kristoffer Haugsbakk sent a two-patch series (v1) that reorganizes an important warning about ref migration limitations—previously buried at the end of the page—into a prominent "CAUTION" admonition block right under the `migrate` subcommand description. The second patch also adds a proper cross-reference to `git-maintenance(1)` using the `linkgit` macro, making the man page more navigable.

Junio C Hamano, the maintainer, has reviewed and approved the series without reservations, calling it "logically sound" and queuing it for integration. The patches are now expected to appear in the `seen` branch shortly and should graduate to `next` in the next "What's cooking" report. Since the changes are purely presentational (no code or behavior is altered), no further discussion or revision is needed. The series is uncontroversial and ready to land.