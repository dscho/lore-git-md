This thread is about a small but useful documentation improvement to the `git-refs(1)` man page. Kristoffer Haugsbakk proposed a two-patch series to reorganize a warning about ref migration limitations into a more visible admonition block and add a proper cross-reference to `git-maintenance(1)`. Junio C Hamano reviewed and approved the series without reservations, and it’s now queued in `seen`, ready to move to `next` in the next integration cycle.

The only follow-up discussion was a minor, surface-level observation from Patrick Steinhardt about the indentation of the admonition block in the rendered man page. Kristoffer replied with context, showing that the indentation is intentional and consistent with other parts of Git’s documentation, but the exchange didn’t change anything about the series itself. The thread is fully resolved, and no further action is needed.