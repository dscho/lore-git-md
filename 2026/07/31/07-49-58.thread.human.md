This is a bug report from Yuri about unexpected behavior in `git stash push`. When Yuri runs `git stash push` with specific paths (e.g., `textproc/py-docling`), Git stashes changes from an unrelated directory (`misc/github-copilot-language-server`), including a new untracked file (`distinfo-node22`) that wasn’t part of the command-line arguments. The report is reproducible and suggests a potential bug in `git stash`’s path-filtering logic, particularly around how it handles untracked files or directory traversal.

So far, this is just the initial report—no responses or proposed fixes have been posted yet. The thread is likely to attract attention from contributors familiar with the stash subsystem or those who’ve encountered similar edge cases in path-based operations. If you’re debugging this, the key detail is the inclusion of untracked files outside the specified paths, which may help narrow down where the filtering logic is going wrong.