This is a user request for a Git configuration option to suppress warnings about trailing blank lines at the end of files. The thread remains a discussion about workflow preferences rather than a technical proposal.

Thomas Nemeth clarifies his earlier request, emphasizing that the trailing blank line is a personal preference rather than a necessity. He explains that in his Vim workflow, having a blank line at the end of files makes it easier to select and move code blocks, particularly when working near the end of a file. This is a quality-of-life request for users who share his editing style.

In response to Johannes Sixt’s suggestion to use the existing `core.whitespace` configuration, Thomas acknowledges that disabling `blank-at-eof` via `git config --global core.whitespace "-blank-at-eof"` achieves the desired suppression of warnings. However, this workaround affects all whitespace checks, not just trailing blank lines, which may not be ideal for users who want to retain other whitespace warnings (e.g., trailing spaces or indentation issues).

The thread has not yet produced a patch or a concrete technical proposal. The discussion is still focused on whether the feature is justified and whether the existing `core.whitespace` mechanism is sufficient. No maintainer or other contributor has weighed in yet, so the request remains open-ended. If Thomas or someone else were to submit a patch, it would likely involve a new configuration option (e.g., `core.ignoreTrailingBlankLine`) to provide finer-grained control over this specific warning. For now, the thread is a low-stakes exchange about ergonomics and configuration flexibility.