This is the first patch in Junio C Hamano’s `git add --resolved` v3 series, a whitespace-only reindentation fix. The patch corrects misindented code introduced in commit a16c4a245a (February 2026), which added `--force` support for `ignore=all` submodules in `read-cache.c`. The misindentation affected the entire `skip_submodule()` function and its call site in `update_callback()`, making the code harder to read and inconsistent with surrounding style.

The change is purely mechanical: 36 lines are reindented to use hard tabs consistently, matching the rest of the file. No logic or behavior is altered. The patch is a prerequisite for the rest of the series, ensuring that subsequent functional changes (e.g., the new `remove_file_from_index_with_flags()` helper in patch 2/4) are applied to properly aligned code. This is a routine cleanup patch, uncontroversial and unlikely to attract substantive review feedback. It demonstrates the author’s attention to code hygiene, even in a series primarily focused on feature addition. Reviewers can safely treat this as a no-op for behavioral purposes.