This is a bug report about `git stash push`. The user, Yuri, is working in the FreeBSD ports tree and tries to stash changes in only three specific directories (`textproc/py-docling`, `textproc/py-docling-mcp`, `textproc/py-docling-slim`) using `git stash push -m "docling: some updates" <paths>`. However, the stash unexpectedly includes changes from an unrelated directory (`misc/github-copilot-language-server`), which was not listed on the command line.

The report is clear and includes a concrete example with the exact command used and the unexpected output from `git stash show -p`. The user is puzzled and asks for help understanding why this happens, noting they’ve seen similar behavior before. There is no patch or proposed fix here—just a description of what appears to be a bug in path-limited stashing. The issue looks real and reproducible, and it touches a commonly used command (`git stash`), so it is likely to attract attention from reviewers familiar with the stash internals. If this behavior is confirmed, it could be a candidate for a bugfix patch.