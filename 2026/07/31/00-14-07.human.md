This is a **substantive review reply** in the ongoing `git config --global` documentation/behavior discrepancy thread. Ben Knoble weighs in to support Junio C Hamano’s position, reinforcing the view that the documentation — not the code — should be fixed to match the current single-file behavior.

Ben dismisses Nils Fahldieck’s first claim (about "doesn’t exist" vs. "not readable") as pedantic, agreeing with Junio that a zero-byte readable file is functionally equivalent to an existing one and does not contradict the documentation. On the core issue — that `--global` reads only one file despite the documentation claiming it reads both — Ben explicitly sides with Junio: the documentation is wrong and should be corrected to reflect the code’s actual behavior. He cites a recent thread (July 20, 2026) about unintuitive `--global`/`--no-includes` interaction as further evidence that the current behavior, while surprising, is not an isolated oversight.

The email advances the thread by adding a second voice in favor of Option B (fix the documentation to match the code), but does not introduce new technical arguments or propose a specific wording change. The discussion remains unresolved on whether to prioritize user expectations (Option A) or code stability (Option B), with the maintainer and now Ben leaning toward the latter.