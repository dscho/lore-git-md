This is a reply in the ongoing thread about a feature request for a Git configuration option to suppress warnings about trailing blank lines at the end of files.

Junio C Hamano clarifies a misunderstanding in the earlier discussion. He initially misread Thomas Nemeth’s request as a desire to enforce *exactly one* trailing blank line (flagging zero or multiple lines as problematic), rather than simply suppressing warnings about trailing blank lines altogether. Junio now acknowledges that if the goal is merely to disable warnings for *any* number of trailing blank lines (including zero), the existing `core.whitespace=-blank-at-eof` setting would suffice. This aligns with Johannes Sixt’s earlier suggestion but reframes it as a direct answer to the original request.

The email does not propose a new feature or implementation, nor does it endorse or reject the idea of a dedicated configuration option. Instead, it corrects the record about what the existing `blank-at-eof` setting can already achieve. This leaves the thread’s status unchanged: the request remains open-ended, with no clear consensus on whether a new option is needed or how it might be designed. The discussion is still focused on clarifying the problem rather than advancing toward a solution.