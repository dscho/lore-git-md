This is a bug report about `git stash push` behavior. The author, Yuri, describes a scenario in the FreeBSD ports tree where running `git stash push` with explicit paths (`textproc/py-docling`, `textproc/py-docling-mcp`, `textproc/py-docling-slim`) unexpectedly stashes changes from an unrelated directory (`misc/github-copilot-language-server`). The stash includes a new file (`distinfo-node22`) that was not part of the command-line arguments.

The report is clear and reproducible: Yuri provides the exact command, the observed output (`git stash show -p`), and notes that this has happened before with other ports. There is no patch or proposed fix—this is purely a request for help diagnosing why the command behaves this way. The key technical detail is that `git stash push` with paths is supposed to stash *only* the specified files, but here it is including untracked or modified files outside those paths.

This is likely to generate discussion about whether the behavior is a bug in `git stash`’s path-filtering logic, a misunderstanding of how `stash push` interacts with untracked files, or a side effect of the FreeBSD ports tree’s structure (e.g., symlinks, submodules, or ignored files). The mention of `distinfo-node22` being a new file suggests untracked files may be involved, which could be a clue for reviewers. No prior thread context is provided, so this appears to be the root of a new discussion.