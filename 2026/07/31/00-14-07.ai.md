This is a **substantive review reply** in the ongoing `git config --global` documentation/behavior discrepancy thread. Ben Knoble weighs in on the two claims raised by Nils Fahldieck, aligning with Junio C Hamano’s assessment but adding a small new piece of context.

Ben dismisses the first claim (documentation wording around "doesn’t exist" vs. "not readable") as immaterial, echoing Junio’s view that a readable zero-byte file is functionally equivalent to an existing file and thus the current wording is not misleading. He does not engage with the pedantic distinction Nils raised.

On the second claim—the core issue that `--global` reads only one file despite the documentation claiming it reads both—Ben explicitly agrees the documentation is wrong and needs correction. He cites a recent thread (July 20, 2026) discussing unintuitive behavior with `--global` and `--no-includes` as further evidence that the current behavior is problematic. This reference introduces a new data point: the issue is not isolated to the XDG file coexistence scenario but also affects how `--global` interacts with include directives, suggesting the problem may be more systemic than initially described.

The reply does not advocate for either of Nils’s proposed fixes (Option A: align code with docs by reading both files; Option B: align docs with code by documenting single-file behavior). Instead, it reinforces the maintainer’s position that the documentation is incorrect and adds weight to the argument that the current behavior is unintuitive. The thread remains unresolved on the choice of fix, but Ben’s input nudges the discussion toward acknowledging the broader implications of the discrepancy.