This is Junio C Hamano’s reply to the sixth and final patch in brian m. carlson’s RFC series proposing a Git 3.0 breaking change: restricting hex object IDs to lowercase only. The patch itself activates the enforcement mechanism introduced in earlier infrastructure patches, but Junio’s response focuses on the series’ fundamental premise rather than the implementation details.

Junio invokes the Robustness Principle (Postel’s Law) to argue that Git should remain liberal in what it accepts, even in a breaking-change release. He suggests distinguishing between object IDs used internally (where Git could enforce lowercase) and those exchanged with external tools or reimplementations (where Git should continue accepting uppercase). This position directly challenges the series’ core motivation—brian’s argument that Git’s current case-insensitive parsing has caused real-world security vulnerabilities in external tools that assume hex uniqueness.

The reply does not engage with the patch’s code or test changes but instead reopens the philosophical debate about whether the breaking change is justified at all. Junio’s framing implies that the project might accept stricter internal enforcement while maintaining backward compatibility for network-facing operations, though he does not propose a concrete alternative implementation. This leaves the series’ future uncertain: the infrastructure patches may proceed independently, but the final enforcement patch (this one) now hinges on resolving the broader design question.

Key technical context for follow-up discussion: the distinction between "local" and "network" object IDs, the scope of `HEX_KIND_OID` (whether it should cover derived hashes like packfile names), and the potential for a compromise that enforces lowercase internally while preserving case-insensitive parsing for external inputs. No maintainer decision or merge status is implied; the thread remains in RFC limbo.