This is a follow-up in the ongoing feature request for a configuration option to suppress warnings about trailing blank lines at the end of files.

Thomas Nemeth acknowledges Hannes’s suggestion to use the existing `core.whitespace` configuration (`-blank-at-eof`) as a workaround for his use case. He confirms that this setting achieves the desired suppression of the warning, though it does so by disabling the `blank-at-eof` check entirely rather than introducing a dedicated option. The reply is brief and pragmatic, effectively conceding that the existing mechanism is sufficient for his needs, at least for now. No further technical proposal or design discussion is offered, and the thread appears to have reached a natural pause.

The exchange leaves the original request unresolved in a formal sense: the project has not committed to implementing a new configuration option, nor has Thomas explicitly withdrawn his request. However, the practical outcome is that the immediate ergonomic issue is addressed for Thomas via `core.whitespace`, which may reduce the urgency or perceived necessity of a dedicated option. The thread remains open-ended, with no maintainer or broader community engagement yet. Future discussion could revisit the request if other users express similar needs or if the limitations of `core.whitespace` (e.g., its global effect on all whitespace checks) become a recurring pain point. For now, the thread is effectively dormant.