Thread identity: Patch series improving the `git-refs(1)` man page by reorganizing a ref migration warning into a `WARNING` admonition block and adding a cross-reference to `git-maintenance(1)`. Series version: v3. Originating author: Kristoffer Haugsbakk. Thread type: documentation patch series.

Core problem or goal: The `git-refs(1)` man page previously buried an important warning about ref migration limitations (concurrent writes can lead to inconsistent state) deep in the prose, making it easy for users to overlook. The goal is to elevate this warning into a prominent `WARNING` admonition block directly under the `migrate` subcommand synopsis and add a missing cross-reference to `git-maintenance(1)` for better navigation.

Current status: **Queued in `next` for graduation to `master`.** Junio C Hamano explicitly marked the topic for promotion to `next` after Patrick Steinhardt confirmed satisfaction with v2 (which addressed the only feedback from v1: switching the admonition block from `CAUTION` to `WARNING`). The series is now in `next` and expected to graduate to `master` in the next "What's cooking" cycle. The v3 update added Patrick Steinhardt’s and Karthik Nayak’s `Acked-by` to both patches, reflecting their explicit approval. No further discussion or revision is anticipated.

Key technical details:
- Files touched: `Documentation/git-refs.txt` (AsciiDoc source for `git-refs(1)`).
- New or renamed symbols: None (documentation-only).
- On-disk format changes: None.
- Test coverage: Not applicable (documentation-only).
- Subsystems touched: Documentation.
- Specific changes:
  - Patch 1/2: Moves the ref migration warning from the "KNOWN LIMITATIONS" section at the end of the page into a `WARNING` admonition block (updated from `CAUTION` in v1) directly under the `migrate` subcommand description. The warning text is unchanged; only its location and admonition type are altered. The AsciiDoc syntax uses a `+` continuation line, producing a minor indentation in the rendered output, which the author confirmed is intentional and consistent with existing patterns in other man pages (e.g., `git-clone(1)` and `git-blame(1)`).
  - Patch 2/2: Replaces the plain-text reference to `git-maintenance(1)` in the warning admonition block with the `linkgit` macro, enabling clickable hyperlinks in HTML output and proper cross-references in the rendered man page.

Open questions and loose ends: None. The series is uncontroversial, fully resolved, and technically complete as of v3.

Participant positions:
- **Junio C Hamano (maintainer)**: Approved the series without reservations and promoted it to `next` after Patrick Steinhardt’s confirmation of satisfaction with v2. Review was substantive (engaged with the logical placement of the warning and the purpose of the cross-reference) and unqualified.
- **Kristoffer Haugsbakk (author)**: Proposed the changes; provided context clarifying the intentional use of admonition block indentation and updated the admonition block from `CAUTION` to `WARNING` in v2 to align with AsciiDoc’s documented semantics. No further action required.
- **Patrick Steinhardt**: Raised two minor, surface-level presentation nits in v1:
  1. Admonition block indentation in the rendered man page (Patch 1/2), later acknowledged as intentional and consistent with existing patterns.
  2. The choice of `CAUTION` over `WARNING` for the ref migration admonition block (Patch 1/2), citing AsciiDoc documentation’s distinction between the two. Both comments were framed as subjective and did not challenge the substance of the changes. Explicitly confirmed satisfaction with v2, and his `Acked-by` was added to both patches in v3.
- **Karthik Nayak**: Added a surface-level review in v3 ("look good to me"), confirming the series remains ready for integration. The acknowledgment carries no new technical content and does not alter the series' status.