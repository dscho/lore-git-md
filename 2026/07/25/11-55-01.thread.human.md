This GSoC feature series by Pablo Sabater is now complete, extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The series, now at v3 with eight patches, has undergone significant refactoring to introduce a dedicated `struct fetch_object_info_results` and bypass the older `struct object_info` entirely, addressing all prior review feedback from Junio Hamano, Jeff King (Peff), and others.

The final patch unifies the default output format for all commands—local and remote—to `%(objectname) %(objecttype) %(objectsize)`, removing a temporary workaround that excluded `%(objecttype)` from remote queries. This change is mechanical and uncontroversial, as the server-side and client-side logic for object type support was already implemented in earlier patches. The documentation has been updated to reflect the new support, and tests now verify consistent output across all transport protocols (git://, file://, http://) and confirm backward compatibility when mixing `info` and `remote-object-info` commands.

The series is feature-complete, with all architectural decisions finalized and robustness fixes in place. The GSoC deadline is imminent, and this patch is the last piece needed for the feature to be merge-ready. Key participants—Junio, Peff, Chandra Pratap, and Karthik Nayak—have provided feedback that has been fully addressed, and no open questions remain. The implementation is narrowly scoped to object type metadata, with other potential extensions (disk size, delta base, object mode) explicitly ruled out as unreliable or context-dependent.