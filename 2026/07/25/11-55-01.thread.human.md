This GSoC feature series, now at v2, extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder for remote-object-info queries, completing the metadata picture after Eric Sunshine’s recently merged v21 series. Pablo Sabater has addressed all review feedback, reordering patches for clarity and updating tests and documentation. The series is now complete and awaiting Junio’s merge decision.

Junio has reviewed the first four patches, confirming their correctness and removing any safety concerns. The only remaining discussion centers on two minor points: the commit message for the test portability fix (patch 2) and memory allocation inefficiencies in the client-side type parsing (patch 4). Pablo has just clarified the motivation for the test change—`wc -c` on macOS pads its output with blanks, making `xargs` necessary for stripping, while Git’s `test_file_size()` helper avoids this issue entirely—and will improve the commit message and restructure the test to avoid redundant commands. Junio’s feedback on patch 4’s memory allocation pattern is advisory and not a blocker, though it may prompt a future cleanup of the `odb.h:struct object_info` design.

The series touches `protocol-caps.c`, `fetch-object-info.c`, `serve.c`, and related test and documentation files, adding end-to-end support for object type metadata in remote queries. All open questions are now about commit message clarity or optional cleanups, not core functionality. The series remains under review, with no objections to its substance.