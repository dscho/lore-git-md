This GSoC feature series, now at v2, extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries, completing the metadata picture after Eric Sunshine’s recently merged v21 series. Pablo Sabater has addressed all review feedback—reordering patches for clarity, fixing test portability, and incorporating Junio’s and Chandra Pratap’s nits—leaving only minor shell quoting adjustments and a preparatory memory allocation patch before the series is ready for Junio’s merge decision.

The core functionality is uncontroversial: the server now advertises and transmits object type metadata, and the client dynamically requests and displays it, unifying the default format for local and remote queries. Junio’s review confirmed the first four patches are correct, though he noted two inefficiencies in the client-side parsing (patch 4). Pablo has agreed to simplify the memory allocation pattern for `object_info_data[j].typep` in a preparatory patch, but the broader `odb.h:struct object_info` cleanup—replacing pointer indirection with bitfields—remains advisory and out of scope for this series. The thread’s focus stays on landing the v2 patches, with no new blockers introduced.