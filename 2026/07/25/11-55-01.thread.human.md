This patch series, now at version 5, extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries, completing the last piece of metadata guaranteed to be identical between client and server after a fetch. The series is feature-complete and merge-ready, with all prior review feedback addressed.

The key architectural change in this version is the integration of Jeff King’s (Peff) refactoring to remove object-info fields from `git_transport_options` and pass them as explicit parameters, clarifying ownership semantics and eliminating a latent bug. Pablo Sabater has also implemented a hybrid design (single array of lightweight structs, caller-controlled field population) to replace the `object_info`-based parsing logic, avoiding sentinel values and boolean flags. The series now includes robustness fixes—such as validating server response lengths and failing fast on protocol violations—and has moved the `ask_type` flag to its correct location to address a forward-compatibility concern raised by Junio.

Junio has explicitly endorsed several patches as ready to merge, including this one (patch 3/10), which removes the `struct object_info_args` wrapper and passes its members directly to `fetch_object_info()`. The change is purely mechanical and aligns with Peff’s advocacy for avoiding semi-opaque structs. All tests are passing, and the series is now effectively complete, with no open technical concerns. The final patch unifies the default format for all queries, ensuring consistent output across transport protocols.