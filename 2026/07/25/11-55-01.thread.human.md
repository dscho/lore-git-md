This GSoC feature series, now at v4, extends `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries, completing the last piece of metadata guaranteed to be identical on client and server after a fetch. Pablo Sabater has addressed all prior review feedback, including Junio’s protocol robustness concerns (validating under-length and over-length server responses) and Karthik Nayak’s commit message clarifications. The architectural refactoring—replacing `struct object_info` with a dedicated `struct fetch_object_info_results`—is finalized, and the end-to-end implementation for object type metadata is in place.

The series is now effectively complete after Pablo accepted Jeff King’s (Peff) proposed refactoring to improve the transport layer’s object-info handling. Peff’s patch removes the `object_info_oids` and `object_info_results` fields from `git_transport_options` and passes them as explicit parameters to `transport_fetch_object_info()`, clarifying ownership semantics and eliminating a latent bug where dangling pointers could occur if the transport were reused. The change is mechanical and uncontroversial, touching seven files without altering behavior. Pablo plans to integrate it as-is, with only minor commit message typos corrected.

Tests in `t5701` (server-side) and `t1017` (client-side) verify the new functionality, including the `%(objecttype)` placeholder in batch-command output, and ensure backward compatibility across all transport protocols. The series is merge-ready, with no open technical questions or loose ends. The only deferred discussion—Peff’s suggestion to make the `remote-object-info` protocol stateful—is speculative and slated for a follow-up series.