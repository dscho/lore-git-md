This is the fourth patch in Pablo Sabater's GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch advertises the "type" capability on the server side, allowing the client to request object type metadata.

The change is minimal: it updates the `object_info_advertise()` function in `serve.c` to include "type" in the advertised capabilities string (previously only "size" was advertised). The test suite (`t/t1017`) is updated to reflect that `%(objecttype)` is now supported, replacing an earlier test that expected an empty string for unsupported placeholders. The test now verifies that the correct object type ("blob") is returned for a known object.

This is a straightforward mechanical change that follows the established pattern from the predecessor series (Eric Sunshine's v21). The patch is well-scoped and appears to be a necessary step for end-to-end functionality. No substantive issues are apparent, though the patch will need to be reviewed in the context of the full series to ensure the capability advertisement aligns with the client-side request logic and protocol handling. The test update is clear and verifies the new behavior.