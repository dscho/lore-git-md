This thread is a three-patch series from Tian Yuchen that migrates four global configuration variables (`minimum_abbrev`, `default_abbrev`, `pack_size_limit_cfg`, and `assume_unchanged`) into the repository-scoped `repo_config_values` struct. The work is part of the ongoing effort to remove implicit reliance on the global `the_repository`, enabling safer code paths in library-like contexts (e.g., `merge-ort`).

The series is now complete with the third patch, which moves `assume_unchanged` into `repo_config_values`. Like the earlier patches, this is a mechanical refactoring with no intended behavior change—existing functionality like the `core.ignorestat` configuration option remains unaffected. The changes touch several subsystems, including configuration handling, object abbreviation, pack-objects, index updates, and merge machinery, but are positioned as uncontroversial infrastructure work. No review feedback has been recorded yet, and the series is expected to rely on existing test coverage for verification.

Key details include the introduction of repository-aware getters for `minimum_abbrev` and `default_abbrev`, while `pack_size_limit_cfg` and `assume_unchanged` are accessed directly due to their limited call sites. The series builds on prior `repo_config_values` work and is part of René Scharfe’s broader `the_repository` removal effort. No open questions or objections have been raised so far.