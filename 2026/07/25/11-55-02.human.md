This is the first patch in a five-part GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch teaches the server to advertise and handle the "type" capability in the object-info protocol extension, allowing clients to request object type metadata from the server.

The change is straightforward: it adds a `type` bit field to the `requested_info` struct in `protocol-caps.c`, updates the server to parse "type" requests from clients, and extends the response logic to include the object type (using `type_name()`) when requested. The patch also fixes a minor style nit in the existing `size` bit field declaration. Test coverage is added in `t5701-git-serve.sh` to verify the new capability works end-to-end.

The implementation follows the established pattern from the recently merged remote-object-info series (Eric's v21), making this a mechanical and well-scoped addition. The commit message is clear, and the code appears clean. No substantive reviews have been posted yet, but the patch looks ready for technical feedback. If the rest of the series maintains this quality, it should integrate smoothly.