This is the final confirmation from Junio C Hamano that the **v23 iteration of Harald Nordgren’s `git branch --delete-merged` series is ready for merging**. The series introduces a safe, automated local-branch cleanup command with comprehensive filtering and safety controls, including order-independent stacked-branch protection, per-branch opt-out, `--dry-run` preview, and repeatable upstream selectors.

The only changes in v23 were mechanical fixes to patch 1/7 (the `--forked` filter for `--list` mode): declaring the loop variable as `size_t` to match the array size, and rewriting a helper function for clarity. These changes address Junio’s last review comments and have no functional impact. The rest of the series remains identical to v22, which Junio had already confirmed as having no outstanding issues.

The series is now **unblocked and will appear in an upcoming "What’s cooking" report**. It is a well-crafted, thoroughly tested feature that addresses a real workflow pain point—safe, automated cleanup of merged local branches—while preserving Git’s safety expectations. The implementation is complete, with all planned features and safety mechanisms in place, and the only agreed follow-up work (extending stacked-branch protection to `git branch -d`) will be handled after the series lands.