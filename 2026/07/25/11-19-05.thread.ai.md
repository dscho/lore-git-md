Thread identity
Subject: [PATCH 0/2] Fix out-of-bounds read in `git worktree add` leading to data loss
Series version: v1 (initial submission; v2 likely after review feedback)
Originating author: Matthias Aßhauer
Thread type: bugfix patch series

Core problem or goal
Fix a critical memory-safety bug in `worktree_basename()` (`builtin/worktree.c`) that causes `git worktree add` to perform an out-of-bounds read when given an empty string *with an explicit branch or commit reference* (e.g., `git worktree add "" HEAD` or `git worktree add "//" HEAD`). The bug can lead to silent data corruption (recursive deletion of the working directory, including `.git`) or trigger a BUG message (`How come '' becomes empty after sanitization?`). The issue was discovered in the wild (Git for Windows #6346), likely via unset environment variables (e.g., `git worktree add "$UNSET_VAR" HEAD`). The root cause is a design flaw in `worktree_basename()`: the function returns a pointer to the basename but requires callers to compute the length via fragile pointer arithmetic (`path + len - name`), which underflows when `len` is zero.

Current status
Under active review; no prior versions. Two patches:
1. **Needs rework**: The first patch attempts to prevent the out-of-bounds read by tracking the original string length but introduces a new risk: when `path` is a string like `"///"`, the proposed fix can still underflow the pointer, leading to `strbuf_add()` allocating an enormous buffer (due to negative length wrapping to `SIZE_MAX`). Junio C Hamano and René Scharfe both identify flaws in the current approach. Junio suggests avoiding pointer arithmetic entirely when `len` is zero or using an integer index loop to eliminate underflow risk. René proposes a more fundamental refactoring: have `worktree_basename()` return the *length* of the basename directly, making the function harder to misuse and addressing the root cause.
2. **Needs adjustment**: The second patch adds explicit validation to reject empty strings, but its necessity and placement were initially questioned. René Scharfe’s latest review clarifies that the validation is *not* redundant—it catches the edge case where an empty string is passed *with* a reference (e.g., `HEAD`), which bypasses earlier branch-name validation (`git branch "" HEAD`) and exposes the memory-safety flaw. However, the patch’s current placement (in `add_worktree()`) may be suboptimal; validation could be moved to `add()` or `dwim_branch()` to fail faster. The commit message should also be updated to reflect this nuance (e.g., "reject empty worktree paths when a reference is provided").

Key technical details
- **Files touched**: `builtin/worktree.c`
- **Root cause**: `worktree_basename()` decrements the `name` pointer below the start of the input string when the input is empty or contains only separators, leading to undefined behavior. The function’s interface is error-prone because it relies on callers to compute the length via pointer arithmetic (`path + len - name`).
- **Callers affected**: `add_worktree()`, `dwim_branch()`, and `add()` in `builtin/worktree.c`. Two callers (`dwim_branch()` and `add()`) avoid immediate issues only because `xstrndup()` with a length of zero does not dereference the pointer, but this is an implementation detail rather than a safe design.
- **New behavior**: The series aims to make `git worktree add "" HEAD` and `git worktree add "//" HEAD` fail with an error instead of corrupting the working directory. The second patch’s validation is now confirmed as necessary for the `"" HEAD` case.
- **No new symbols, CLI options, or on-disk format changes**.
- **No test coverage included** in the series; reviewers have requested reproduction steps or test cases to verify the fix (especially for the `"" HEAD` case) and prevent regressions. The bug’s manifestation is call-path-dependent, with some edge cases (e.g., `git worktree add ""`) already handled by earlier validation.
- **Bug context**: Discovered via Git for Windows issue #6346; likely triggered by unset environment variables in scripts or strings that become empty after sanitization.

Open questions and loose ends
- **Reproduction steps**: No test cases or minimal reproduction steps are provided for the `"" HEAD` case; reviewers may request them to verify the fix and prevent regressions.
- **Edge cases**: Junio’s review highlights that strings like `"///"` (only separators) are not fully addressed by the current patch. René’s feedback confirms the second patch’s necessity but suggests its placement (in `add_worktree()`) may need adjustment.
- **Patch interdependence**: The second patch’s logic (rejecting empty strings) may need to be moved to an earlier point in the call chain (e.g., `add()` or `dwim_branch()`) to fail faster. Its commit message should also be updated to clarify the specific edge case it targets.
- **Style issues**: Junio notes minor style inconsistencies (missing spaces, brace usage) that will need correction in v2.
- **Design consensus**: René’s feedback suggests a broader refactoring (returning length instead of pointer) may be needed for patch 1/2, but it is unclear whether this will be adopted in v2 or deferred to a follow-up series. The second patch’s necessity is now confirmed, but its implementation may change.
- **No prior discussion**: This is the first public disclosure of the bug; no prior thread context exists.

Participant positions
- **Junio C Hamano**: Provided a **substantive technical review** of the first patch, identifying a critical flaw in the proposed fix (pointer underflow risk). Suggests safer alternatives (integer index loop or avoiding pointer arithmetic when `len` is zero). Feedback implies the patch needs rework before acceptance. No position on the second patch yet.
- **René Scharfe**: Provided **substantive technical reviews** for both patches. For the first patch, identifies a broader design flaw in `worktree_basename()`: the function’s interface is error-prone because it relies on callers to compute the length via pointer arithmetic. Proposes refactoring the function to return the basename length directly, eliminating the need for fragile arithmetic. For the second patch, initially thought the validation was redundant but later clarified that it is necessary for the `"" HEAD` case. Suggests adjusting its placement (to `add()` or `dwim_branch()`) and updating the commit message to reflect the specific edge case it targets. Feedback implies both patches require revision, but the second patch’s necessity is now confirmed.
- **Matthias Aßhauer**: Author of the series; has not yet responded to review feedback. Likely to post a v2 addressing the comments from Junio and René.

Related work
- **Git for Windows issue #6346**: Upstream reference for the bug report. No other related in-flight topics mentioned.