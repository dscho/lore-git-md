This thread is about adding test coverage for the `--is-ancestor` option in `git merge-base`. Nikolaus Schuetz submitted a patch via GitGitGadget that introduces 34 lines of new tests in `t/t6010-merge-base.sh`, filling a gap where this widely-used option previously had no test coverage.

The tests verify the command's exit codes (0 when A is an ancestor of B, 1 when not, and 128 for invalid arguments) and error conditions, including edge cases like self-ancestry, diverged commits, and invalid combinations with `--all`. The patch is self-contained, only adding tests without modifying production code, and follows the existing test style. Since `--is-ancestor` is commonly used in scripts, this contribution is both practical and uncontroversial. No reviews or feedback have appeared yet, so the patch is still awaiting initial reactions.