This email is a focused design agreement between the author (Siddharth Shrimali) and a key reviewer (Siddharth Asthana) in the ongoing RFC series for `git repack --drop-filtered`. The thread has now settled the **safety guard requirements** and **CLI design** for the next iteration (v2).

The author confirms that **safety guards**—preventing drops during merges, rebases, or cherry-picks, and refusing to drop blobs referenced by the current index—will be implemented in v2. These were previously deferred but are now explicitly scoped as blocking prerequisites, addressing Asthana’s insistence that they are non-negotiable for a production-ready tool. The **index guard** is particularly critical: without it, users could drop blobs only to immediately re-fetch them, negating the space-reclaim benefit.

On the **drop log**, the author agrees to **pull it out of the core series** for v2, treating it as a follow-up enhancement. The log’s remote-object attribution (recording which promisor remote each object is recoverable from) will also remain deferred, pending the `remote-object-info` protocol command. This pragmatic decision keeps the v2 scope focused on the **bitmap validation fix**, **safety guards**, and **API reorganization** identified as blocking issues in earlier feedback.

The **CLI design** (`--drop-filtered` + `--dry-run`) is now finalized for v2, with the author committing to document the rationale in the commit message. This leaves the door open for a future `--drop-filtered=<mode>` syntax if more drop-specific options emerge, but avoids over-engineering the interface prematurely. The choice aligns with Christian Couder’s earlier endorsement of the two-option split.

The email does not address the **bitmap validation flaw** (Junio’s blocking concern) or the **function signature churn** (internal API reorganization), but those were already confirmed as v2 priorities in the thread AI summary. The next steps are clear: implement the safety guards, restructure the bitmap validation, and reorganize the APIs before posting v2. No integration branch has been assigned yet.