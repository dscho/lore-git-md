This is a two-patch bugfix series targeting `git worktree add`. The author reports that passing an empty string (e.g., via an unset variable) can trigger an out-of-bounds memory read in `worktree_basename()`, leading to inconsistent behavior—sometimes a BUG message, but in worse cases, recursive deletion of the current working directory including `.git`. The patches aim to prevent the out-of-bounds read and explicitly reject empty input.

The issue is clearly serious: data loss is a critical bug, and the commit message provides a plausible reproduction scenario. The fix touches only `builtin/worktree.c` and is narrowly scoped, which is appropriate for a bugfix. The series is well-structured, with the first patch addressing the memory safety issue and the second adding validation to prevent the problematic input entirely.

No prior discussion or review is visible in this email, so the patches are fresh. Given the severity of the bug (data loss), this series is likely to attract attention and should be prioritized for review. The author appears to be a first-time contributor via GitGitGadget, so the patches may benefit from extra scrutiny, but the problem and solution are straightforward.