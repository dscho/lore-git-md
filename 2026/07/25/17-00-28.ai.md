This is a test patch targeting the `git merge-base` command. It adds comprehensive test coverage for the `--is-ancestor` option in `t/t6010-merge-base.sh`, which previously had none.

The patch introduces 34 lines of new tests that verify the exit codes and error conditions of `git merge-base --is-ancestor A B`. Specifically, it checks that the command exits with 0 when A is an ancestor of B, 1 when it is not, and 128 (not 1) when given invalid arguments. It also tests that `--is-ancestor` and `--all` cannot be combined, and that the error message explicitly names both options.

The tests cover edge cases like self-ancestry (a commit is its own ancestor), diverged commits, and non-existent commits. The setup creates a small repository with three commits to exercise these scenarios. The patch is well-scoped: it only adds tests, does not modify production code, and follows the existing test style in `t6010-merge-base.sh`.

This looks like a straightforward, well-motivated contribution. The `--is-ancestor` option is widely used in scripts, so having proper test coverage is valuable. The patch is self-contained and unlikely to be controversial. Key identifiers introduced: test file `t6010-merge-base.sh`, option `--is-ancestor`, exit codes 0/1/128, and the error message about mutually exclusive options.