This is Junio’s "What’s cooking in git.git" report for July 2026 (#11), summarizing the current state of integration branches and in-flight topics.

The report lists 47 topics, 7 of which have just graduated to `master` in the seventh batch. Below are the key highlights for a future AI summarizer session, focusing on technical details and status changes:

**New topics (recently added, likely needing review):**
- **af/clone-revision-v0-segfault-fix**: Fixes a segfault in `git clone --revision` when falling back to protocol v0. One-liner fix; marked for `next`.
- **lo/mv-missing-dest-dir-check**: `git mv` now checks for missing destination directories during the dry-run phase (`-n`) and improves error messages. Two patches; waiting for author response.
- **ps/odb-make-creation-pluggable**: Makes ODB on-disk structure creation pluggable, deferring ODB initialization and detangling loose-object map loading. Five patches; needs review.

**Stalled topics (awaiting response or reroll):**
- **tb/repack-geometric-cruft**: Combines `--geometric` and `--cruft` in `git repack`. Eleven patches; stalled due to unresolved feedback (e.g., bare `grep !` in SQUASH???).
- **hn/checkout-track-fetch**: Extends `git checkout --track` with a "fetch" mode to refresh the start-point branch. Two patches; stalled for months.
- **za/completion-hide-dotfiles**: Hides dotfiles in path completion unless explicitly requested. Two patches; stalled.
- **kh/doc-trailers** and **kh/doc-replay-config**: Documentation updates for `git interpret-trailers` and `git replay`. Ten and four patches respectively; stalled awaiting rerolls.

**Cooking topics (in `next` or `seen`, likely to advance):**
- **jt/config-lock-timeout**: Adds `core.configLockTimeout` to retry acquiring `config.lock`. Already merged to `next`; will merge to `master`.
- **tn/stash-avoid-sparse-index-expansion**: Optimizes `git stash push` to avoid sparse-index expansion for in-cone paths. Two patches; merged to `next`.
- **ps/odb-move-loose-object-writing**: Moves loose-object writing logic from `object-file.c` to `odb/source-loose.c`. Ten patches; will merge to `next`.
- **ps/refs-wo-the-repository**: Removes `the_repository` dependency from refs subsystem. Seven patches; merged to `next`.
- **ps/cat-file-remote-object-info**: Adds `remote-object-info` to `git cat-file --batch-command` for querying object metadata (e.g., size) from remote servers via protocol v2. Thirteen patches; merged to `next`.
- **hn/branch-delete-merged**: Adds `--delete-merged` to `git branch` to delete branches merged into their upstream. Seven patches; marked for `next`.
- **ps/shift-root-in-graph**: Indents root commits in `git log --graph` to visually distinguish them. Seven patches; merged to `next`.

**Notable refactorings and cleanups:**
- **ps/odb-pluggable-housekeeping**: Makes ODB housekeeping (e.g., repacking) pluggable. Twelve patches; waiting for response.
- **rs/tempfile-wo-the-repository**: Removes `the_repository` dependency from tempfile/lockfile APIs. Five patches; merged to `next`.
- **ty/migrate-trust-executable-bit**: Migrates `trust_executable_bit` to `repo_config_values`. Four patches; merged to `next`.

**Performance and correctness fixes:**
- **sc/wt-status-avoid-quadratic-insertion**: Optimizes `git status` by avoiding quadratic complexity in untracked/ignored file enumeration. One patch; merged to `next`.
- **kk/merge-base-exhaustion**: Optimizes merge-base computation by terminating early when one side’s commits are exhausted. Eleven patches; needs review.

**Controversial or complex topics:**
- **tb/send-pack-no-ref-delta**: Teaches `git send-pack` to honor `no-ref-delta` capability. Four patches; needs review.
- **mm/diff-process-hunks**: Introduces `diff.<driver>.process` to allow external tools to control hunk detection. Nine patches; ejected due to conflicts with `mm/line-log-limited-ops`.

**Experimental features:**
- **hn/history-squash**: Adds `git history squash` to fold a range of commits into one. Five patches; merged to `next`.

**Key names and subsystems touched:**
- **Commands**: `git clone`, `git mv`, `git stash`, `git cat-file`, `git branch`, `git log --graph`, `git repack`, `git fetch`, `git rebase`, `git bisect`.
- **Subsystems**: ODB (object database), refs, tempfile/lockfile, sparse-index, merge-base, diff, protocol v2.
- **Config keys**: `core.configLockTimeout`, `fetch.submoduleErrors`, `log.graphIndent`, `branch.<name>.deleteMerged`.
- **New APIs**: `odb_write_object_ext()`, `repo_hold_lock_file_for_update()`, `next_commit_to_show()`.

**Likely points of contention:**
- ODB pluggability (`ps/odb-make-creation-pluggable`, `ps/odb-pluggable-housekeeping`).
- External diff drivers (`mm/diff-process-hunks`).
- Merge-base optimization edge cases (`kk/merge-base-exhaustion`).