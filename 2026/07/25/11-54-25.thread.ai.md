Thread identity: `[PATCH 0/3] v2` series migrating global configuration variables (`pack_size_limit_cfg` and `assume_unchanged`) into `repo_config_values` as part of the ongoing `the_repository` removal effort. Originating author: Tian Yuchen. Thread type: patch series (infrastructure refactoring).

Core problem or goal: Eliminate process-global configuration variables by making them repository-scoped, enabling code paths that explicitly avoid `the_repository` (e.g., future libification efforts) to access them safely. The series is a continuation of prior `repo_config_values` work, motivated by the broader goal of removing implicit reliance on the global `the_repository`. The v2 scope is narrowed to two variables after Junio’s objection to the v1 design for `minimum_abbrev` and `default_abbrev`.

Current status: Under review (v2 posted). Series scope reduced to two patches:
- Patch 1/2: Migrates `pack_size_limit_cfg` from a standalone global in `environment.c` into the `repo_config_values` struct. All direct references updated to use the repository-specific instance. No getter function introduced due to limited call sites (only two). Backward compatibility maintained by accessing the value via `the_repository` in `object-file.c`.
- Patch 2/2: Migrates `assume_unchanged` from `environment.h` into the `repo_config_values` struct. The global variable is removed, and all direct references are updated to use the repository-specific instance. No getter function introduced due to limited call sites and no hardcoded fallback values. Backward compatibility maintained by accessing the value via `the_repository`’s config in affected code paths.

Key technical details:
- Files touched: `environment.c`/`environment.h` (central location for the variables), with callers updated in `builtin/pack-objects.c`, `builtin/update-index.c`, `object-file.c`, `read-cache.c`, `replay.c`, and `sequencer.c`.
- New or renamed symbols: `repo_config_values.pack_size_limit_cfg`, `repo_config_values.assume_unchanged`. No repository-aware getters introduced for these variables.
- Subsystems affected: Configuration handling, pack-objects, index updates, replay, sequencer, and ODB transaction logic.
- No behavior change intended; the series remains purely infrastructure for future libification.
- Test coverage: Not explicitly discussed, but the mechanical nature of the changes suggests existing tests should suffice. Edge cases in ODB transaction logic (`object-file.c`) and `core.ignorestat` behavior (`read-cache.c`) may warrant attention.

Open questions and loose ends:
- **API design precedent**: The v2 series avoids silent fallbacks to `the_repository` when `repo != the_repository`, resolving Junio’s objection from v1. This sets a constraint for future repository-scoped getter designs: they should not delegate to `the_repository` implicitly.
- **Follow-up work**: The concession leaves open the question of how to properly extend APIs (e.g., `merge-ort.c`) to handle repository-scoped configuration without relying on `the_repository`. This may require separate follow-up work.
- **Variable selection**: The v2 scope is limited to `pack_size_limit_cfg` and `assume_unchanged`. Future iterations may address other globals if these are deemed insufficient for meaningful progress.

Participant positions:
- **Junio C Hamano**: Substantive technical objection to v1’s design (fallback to `the_repository` for `minimum_abbrev` and `default_abbrev`). Proposed `BUG()` as a forcing function to resolve API limitations correctly. His objection was accepted without counter-argument, and the v2 series complies with the constraint.
- **Tian Yuchen**: Conceded Junio’s objection and dropped `minimum_abbrev` and `default_abbrev` from the series. No feedback on patches 1/2 or 2/2 in v2, which remain uncontested.

Related work: Part of the ongoing `the_repository` removal effort (primary driver: René Scharfe). The series builds directly on prior `repo_config_values` work. No external dependencies or related in-flight topics mentioned.