This is a test patch for the `git merge-base --is-ancestor` option. The author, Nikolaus Schuetz, adds comprehensive tests to `t6010-merge-base.sh` to verify the behavior of this frequently used but untested feature.

The tests cover all key scenarios: when commit A is an ancestor of B (exit code 0), when it is not (exit code 1), and when invalid arguments are provided (exit code 128). They also confirm that `--is-ancestor` and `--all` cannot be combined, with the error message explicitly naming both options. The setup is minimal but sufficient, creating a small repository with linear and diverged histories.

The patch is well-scoped and clearly motivated. The commit message is concise and the test cases are thorough without being excessive. This looks like a straightforward, uncontroversial addition that fills an obvious gap in the test suite. No prior discussion or cover letter is referenced, so this appears to be a standalone patch rather than part of a larger series.