This is a **surface-level review** of the test infrastructure patch introducing `test_trace2_data_singular()`. René Scharfe points out a minor stylistic nit: the two-line string literal (`*"
"`) used to detect multi-line values in the Trace2 data could be replaced with the existing `$LF` variable (defined in `t/test-lib.sh`), which already holds a newline character. The change would make the code slightly more readable and consistent with other parts of the test suite.

The patch itself remains **non-blocking** and **low-risk**; this is purely a cosmetic suggestion. The series as a whole is **queued and procedurally unblocked**, awaiting additional review bandwidth before advancing further. This feedback does not affect the core performance optimization or correctness of the `paint_down_to_common()` changes.