Thread identity: patch series adding test coverage for `git merge-base --is-ancestor` in `t/t6010-merge-base.sh`; originating author Nikolaus Schuetz; thread type patch series (test-only), now at v2.

Core problem or goal: add comprehensive test coverage for the `--is-ancestor` option of `git merge-base`, which previously had none, to verify exit codes, error conditions, and edge cases.

Current status: **merged to `master`** (2025-06-18, per Junio's "Will queue. Thanks" and prior "What's cooking" report). The patch was previously queued in Junio's `seen` branch (2025-06-12) and has now graduated. v2 addressed the only substantive feedback (reusing the existing test repository setup) and added a test case for rejecting excess arguments.

Key technical details: test file `t/t6010-merge-base.sh`; option `--is-ancestor`; exit codes 0 (ancestor), 1 (not ancestor), 128 (invalid arguments), 129 (usage errors); error messages for mutually exclusive `--is-ancestor` and `--all` ("options '--is-ancestor' and '--all' cannot be used together") and for excess arguments ("--is-ancestor takes exactly two commits"); edge cases include self-ancestry, diverged commits, non-existent commits, and invalid argument counts; 34 lines of new tests; setup reuses the existing test repository history (`E---D---C---B---A` and `G/H` merges); no production code changes.

Open questions and loose ends: none; all prior feedback has been addressed, and the patch is now merged.

Participant positions:
- Junio C Hamano **merged the patch to `master`**, signaling final approval. His prior discussion of maintainability trade-offs (test setup reuse) was resolved in v2.
- Phillip Wood reviewed substantively, calling the patch "a very useful improvement" and suggesting reuse of the existing test history; his feedback was fully addressed in v2.