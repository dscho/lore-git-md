Thread identity: patch series adding test coverage for `git merge-base --is-ancestor` in `t/t6010-merge-base.sh`; originating author Nikolaus Schuetz; thread type patch series (test-only).

Core problem or goal: add comprehensive test coverage for the `--is-ancestor` option of `git merge-base`, which previously had none, to verify exit codes, error conditions, and edge cases.

Current status: queued in Junio C Hamano's `seen` branch (2025-06-12, per "What's cooking" report); no prior versions; uncontroversial and ready for integration. Phillip Wood raised a **medium-weight** technical suggestion to reuse the existing test repository setup rather than creating a dedicated one, but explicitly endorsed the patch as an improvement in its current form.

Key technical details: test file `t/t6010-merge-base.sh`; option `--is-ancestor`; exit codes 0 (ancestor), 1 (not ancestor), 128 (invalid arguments); error message for mutually exclusive `--is-ancestor` and `--all` ("options '--is-ancestor' and '--all' cannot be used together"); edge cases include self-ancestry, diverged commits, and non-existent commits; 34 lines of new tests; no production code changes; setup follows existing style in the file, though the new tests use a separate repository (`is-ancestor`) instead of reusing the commits from the first test.

Open questions and loose ends: whether to refactor the test setup to reuse the existing repository (raised by Phillip Wood); no objections to correctness or completeness.

Participant positions:
- Junio C Hamano queued the patch for integration, signaling approval.
- Phillip Wood reviewed substantively, calling the patch "a very useful improvement" while suggesting a potential simplification by reusing the existing test history. His feedback is actionable but not blocking.