Thread identity: patch series adding test coverage for `git merge-base --is-ancestor` in `t/t6010-merge-base.sh`; originating author Nikolaus Schuetz; thread type patch series (test-only), now at v2.

Core problem or goal: add comprehensive test coverage for the `--is-ancestor` option of `git merge-base`, which previously had none, to verify exit codes, error conditions, and edge cases.

Current status: queued in Junio C Hamano's `seen` branch (2025-06-12, per "What's cooking" report); v2 posted addressing the only substantive feedback (reusing the existing test repository setup instead of creating a separate one). The patch remains uncontroversial and ready for integration.

Key technical details: test file `t/t6010-merge-base.sh`; option `--is-ancestor`; exit codes 0 (ancestor), 1 (not ancestor), 128 (invalid arguments), 129 (more than two commits); error messages for mutually exclusive `--is-ancestor` and `--all` ("options '--is-ancestor' and '--all' cannot be used together") and for excess arguments ("--is-ancestor takes exactly two commits"); edge cases include self-ancestry, diverged commits, non-existent commits, and invalid argument counts; 34 lines of new tests; no production code changes; setup now reuses the existing test repository history (`E---D---C---B---A` and `G/H` merges) for consistency and maintainability.

Open questions and loose ends: none; the prior open question (whether to reuse the existing repository) has been resolved in favor of reuse, and no further objections or requests for changes have been raised.

Participant positions:
- Junio C Hamano queued the patch for integration, signaling approval, and discussed the maintainability trade-offs of test setup reuse without expressing a preference; the v2 resolution aligns with his stated openness to either approach.
- Phillip Wood reviewed substantively, calling the patch "a very useful improvement" and suggesting reuse of the existing test history; his feedback has been fully addressed in v2.