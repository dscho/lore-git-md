This is the final exchange in the RFC series introducing `git repack --drop-filtered` for partial clones. The thread confirms that the drop log (`$GIT_DIR/objects/info/promisor-dropped`) will be included in the upcoming v2, though its format and functionality remain minimal for now. The log records which objects were removed by `--drop-filtered`, but currently lists *all* configured promisor remotes rather than the specific one each object is recoverable from — a known limitation that depends on the future `remote-object-info` protocol command. Siddharth Shrimali agrees to document the log path in `gitrepository-layout` and add a test to verify that real drops append entries, addressing Siddharth Asthana’s feedback.

The discussion here is purely about housekeeping: the log’s existence is settled, but its documentation and testing are deferred to the v2 patch series. This patch (7/7) completes the core functionality, though the safety guards (merge/rebase/cherry-pick checks and index validation) and remote verification logic remain slated for follow-up work. The series as a whole is still in design iteration, with no maintainer decision on integration. For a busy reader, this email signals that the drop log is now a confirmed part of the v2 design, but its current implementation is intentionally minimal and will evolve later.