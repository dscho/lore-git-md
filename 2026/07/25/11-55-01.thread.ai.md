Here is the updated AI thread summary with the new email incorporated:

---

Thread identity: GSoC feature patch series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. Series version: v6 (ten patches, feature-complete). Originating author: Pablo Sabater. Thread type: patch series.

Core problem or goal: Extend the recently merged `git cat-file --batch-command` remote-object-info functionality to include object type metadata, the last piece of metadata guaranteed to be identical on client and server after a fetch. The series adds end-to-end support: server capability advertisement, client request logic, protocol extension, and client-side parsing/display. The goal is narrowly scoped to object type, with other metadata (disk size, delta base, object mode) explicitly ruled out as unreliable or context-dependent.

Current status: Under review. Series is at v6, addressing a static-analysis warning identified in CI for v5. The v5 series was feature-complete, with all prior review feedback addressed and the architectural refactoring finalized. The final patch (10/10) unifies the default format for `remote-object-info` and local `info` commands, removing the temporary workaround that used a different default format (`%(objectname) %(objectsize)`) for remote queries. The unified default format is now `%(objectname) %(objecttype) %(objectsize)` for all queries, ensuring consistent output across all transport protocols (git://, file://, http://) and confirming that mixing `info` and `remote-object-info` commands produces identical results. Documentation is updated to reflect the new functionality, and the warning in the man page about the temporary format has been removed. The patch touches only four files (`Documentation/git-cat-file.adoc`, `Documentation/gitprotocol-v2.adoc`, `builtin/cat-file.c`, and `t/t1017-cat-file-remote-object-info.sh`), removing the conditional format logic in `parse_cmd_remote_object_info()` and updating the protocol documentation to list "type" as a supported attribute. The test suite is updated to expect the full default format (including object type) for all queries. All tests are passing in v5, except for the static-analysis warning that prompted v6.

The architectural refactoring proposed by Jeff King (Peff) and implemented in patch 6/10 removes the `object_info_oids` and `object_info_results` fields from `git_transport_options` and passes them as explicit parameters to `transport_fetch_object_info()` and its vtable implementations. This change clarifies ownership semantics, eliminates a latent bug (dangling pointers if the transport were reused), and prepares the codebase for future enhancements like connection reuse. The refactoring is mechanical (25 insertions, 19 deletions) and touches seven files (`transport.c`, `transport-helper.c`, `builtin/cat-file.c`, `fetch-object-info.c`, `fetch-object-info.h`, `transport-internal.h`, `transport.h`). No behavior changes or protocol-level adjustments are involved.

Peff and Pablo have discussed a potential future optimization: making the `remote-object-info` protocol stateful to allow multiple object-info requests over a single transport connection. Peff proposes leaving the current syntax (`remote-object-info <remote> <objs...>`) unchanged and letting `git cat-file` handle connection reuse internally as an optimization. This would avoid protocol changes while still allowing the transport to remain open between requests, reducing overhead for repeated queries. Peff notes that the cost of keeping a connection open (e.g., an SSH session) is low, as most transports either time out naturally or are used in contexts where additional requests are likely. Pablo agrees but defers the optimization to a follow-up series, citing time constraints and the current focus on object type support. Peff endorses this approach, emphasizing that the protocol remains unchanged and the optimization can be added later without affecting the current patch series. The discussion is speculative and does not alter the immediate patch set.

The hybrid design (single array of lightweight structs, caller-controlled field population) replaces the `object_info`-based parsing logic in `fetch-object-info.c` and `builtin/cat-file.c`. The design uses a single array of lightweight structs where the caller controls which fields are populated, avoiding sentinel values and boolean flags. Junio and Peff confirmed that sentinel values like `OBJ_NONE` are unsuitable for fields like `size` and that a separate bit is preferable for signaling unknown objects. Pablo implemented this design in v5, including the mechanical refactoring in patch 3/10 (explicitly endorsed by Junio as ready to merge) that removes `struct object_info_args` and passes its members directly to `fetch_object_info()`, and patch 4/10 that introduces `struct fetch_object_info_results` and drops `object_info_options`, `remote_atom_map[]`, and `free_object_info_contents()`. The commit message for patch 3/10 has been updated to clarify that `fetch_object_info()` *does* use all three fields of `struct object_info_args` in its current implementation, addressing Karthik Nayak’s feedback that the original phrasing was misleading.

Patches 5/10, 7/10, 8/10, and 9/10 complete the server-side and client-side implementation for object type metadata, enabling the server to advertise, transmit, and parse type data as a string (e.g., `"blob"`). Patch 5/10 converts the last error path in `fetch_object_info()` to `die()`, aligning with Git’s defensive programming practices and addressing a latent robustness issue. The patch updates the function signature to `void` and removes the final `return 0`, reflecting that the function no longer returns success/failure codes. Callers in `transport.c` are updated to drop the now-unused return value check. Patch 7/10 adds a `type` bit field to `struct requested_info` in `protocol-caps.c` and updates the server-side handler to parse the "type" keyword in client requests, enabling the server to transmit object type as a string (e.g., `"blob"`) alongside the existing size field. The patch also includes two minor cleanups—fixing a bit-field style nit (`size:1` → `size: 1`) and wrapping an over-long line in `send_info()`—that were noted in earlier reviews. Test coverage is expanded in `t5701-git-serve.sh` with a new test verifying the server handles requests for both "size" and "type", including duplicate OIDs. Patch 8/10 adds the `wants_type` flag and `types` array to `struct fetch_object_info_results`, mirroring the existing `wants_size`/`sizes` pattern. In `fetch_object_info()`, it extends the protocol request to include the "type" keyword when the caller requests it and the server supports the feature. The response parsing logic maps the server’s type string (e.g., `"blob"`) to an internal `enum object_type` using `type_from_string_gently()`, with a `die()` if the string is unrecognized. The `builtin/cat-file.c` caller is updated to populate the `typep` field from the results and append "objecttype" to the allowed-atoms list when the server provides type data. Patch 9/10 appends "type" to the advertised capabilities string in `serve.c` and updates `t1017` to expect the actual type ("blob") for `%(objecttype)` and confirm that unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings.

Patch 1/10 is a test cleanup targeting `t5701-git-serve.sh`, replacing `wc -c | xargs` with `test_file_size()` and caching the output of `git rev-parse two:two.t` in variables (`two_oid` and `two_size`). The change is purely mechanical and uncontroversial, motivated by portability and the preservation of command exit statuses within here-documents. The original version masked the exit status of `wc -c` if it failed; the new version preserves exit statuses and is functionally identical. Junio has explicitly endorsed this patch as ready to merge, noting that the change is a straightforward improvement with no behavior changes or technical concerns. The commit message has been updated to clarify the rationale, and the patch is marked as ready for integration.

Patch 2/10 hardens the client against malformed server responses by explicitly checking both under-length (fewer objects than requested) and over-length (more objects than requested) cases. Previously, the loop in `fetch_object_info()` stopped either when the reader returned something other than `PACKET_READ_NORMAL` or when it had read as many lines as requested, but neither boundary was validated. A server sending too few objects left the end of the result arrays uninitialized, while a server sending too many left extra packets unread—only caught on stateless transports by `check_stateless_delimiter()`. The fix extracts `packet_reader_read()` from the loop condition, so the loop no longer consumes the final packet. Inside the loop, if the read is not `PACKET_READ_NORMAL`, the client now `die()`s with a clear message indicating how many objects were expected versus received. After the loop, the client expects a flush packet; if anything else arrives, it `die()`s to fail fast. This aligns with Git’s defensive programming practices and addresses Junio’s protocol hygiene concern from the thread. The change is purely mechanical (10 insertions, 4 deletions) and touches only `fetch-object-info.c`. It introduces no new symbols or behavior changes beyond the robustness checks. Junio has explicitly endorsed this patch as ready to merge, noting it is uncontroversial and addresses a preexisting latent bug.

Open questions and loose ends: The v6 series will address a static-analysis warning identified in CI for v5. No technical details about the failure are provided in the email, but the tone suggests it is a minor issue (e.g., uninitialized variable, style nit, or similar) rather than a fundamental flaw. All other technical concerns, design refinements, and review feedback have been addressed. The series remains on track for integration once the static-analysis warning is resolved. The potential optimization to make the `remote-object-info` protocol stateful for connection reuse is deferred to a follow-up series, with no impact on the current patch set.

Participant positions:
- **Junio C Hamano**: Proposed an array-of-structs design (`struct { struct oid *oid; enum object_type type; size_t size; }`) to replace Pablo’s `object_info_results`, using `OBJ_NONE` or `NULL` to signal missing data. Refined this design with Peff, confirming that sentinel values like `OBJ_NONE` work for `type` but do not extend cleanly to other fields such as `size`, and that a separate bit is preferable for signaling unknown objects. The hybrid design is now implemented in v5. Provided surface-level feedback on patch 1/10, noting that the new version preserves the exit status of commands run inside `$(...)` within a here-document, whereas the old version using `wc -c | xargs` would have masked failures. Pablo has acknowledged this observation, noting that the change is a purely mechanical, uncontroversial test cleanup and that no code changes are needed. The commit message for patch 1/10 has been updated to reflect this rationale, and the patch is now explicitly marked as ready to merge. Raised two substantive technical concerns:
   - **Protocol robustness (patch 2/10)**: The current implementation detected under-length responses (fewer objects than requested) but did not address over-length responses (more objects than requested). The patch is safe against buffer overflow, but Junio asked whether the client should detect and reject over-length responses as a protocol violation. In v5, Pablo has implemented a fix: after reading the expected number of objects, the client now checks for an immediate flush packet; if anything else arrives, it `die()`s to fail fast. This aligns with Git’s defensive programming practices and addresses the protocol hygiene concern. The patch now explicitly validates both under-length and over-length responses. Junio’s latest reply explicitly endorses this patch as mechanical, uncontroversial, and ready to merge.
   - **Premature `ask_type` flag (patch 4/9 in v3)**: The `ask_type` flag and related machinery were introduced before the client-side parsing logic (patch 6/9) and server-side capability advertisement (patch 7/9). If a user requested `%(objecttype)` at this stage and the server supported "type", the client would hit a `BUG()`. In v5, the `ask_type` flag and related machinery are now introduced in patch 8/10, where they fit correctly, addressing the forward-compatibility concern. This is a protocol hygiene issue and does not block merging.
   - **Patch 3/10 (mechanical refactoring)**: Explicitly endorsed as ready to merge. Junio confirmed that the patch is purely mechanical, removing `struct object_info_args` and passing its members directly to `fetch_object_info()` and `send_object_info_request()`, with no behavior changes or partial feature additions. The commit message has been updated to address Karthik Nayak’s feedback, clarifying that `fetch_object_info()` *does* use all three fields of `struct object_info_args` and explaining the rationale for removing the struct.
   - **Patch 4/10 (mechanical refactoring)**: Noted that the patch removes about 30 lines of code by eliminating `remote_atom_map[]` and its associated logic, aligning with the new design that avoids string-based lookups. Raised a curiosity-driven question about why "type" was already present in `remote_atom_map[]` despite not being supported until later patches. Pablo clarified that the list was designed to be idempotent, allowing new placeholders to be added without changing behavior until the server advertised support. The "type" entry was included to test the client’s handling of known-but-unsupported placeholders, leveraging the fact that `struct object_info` already had a type field. With the new design, each attribute requires a dedicated array, so carrying an unused `types` array would be wasteful. Pablo opted to drop the "type" entry here and implement it fully in the later patches where it belongs. This is harmless and does not require changes; the patch remains uncontroversial and ready for integration.
   - **Patch 5/10**: Explicitly endorsed as ready to merge. The patch converts the last error path in `fetch_object_info()` to `die()`, aligning with Git’s defensive programming practices and addressing a latent robustness issue. The change is purely mechanical and uncontroversial, with no behavior changes or new functionality.
   - **Patch 6/10**: Explicitly endorsed as ready to merge. The patch is a mechanical refactoring by Peff that removes object-info fields from `git_transport_options` and passes them as explicit parameters, clarifying ownership semantics and eliminating a latent bug. The change is uncontroversial and does not alter behavior.
   - **Patch 7/10**: Explicitly endorsed as ready to merge. The patch adds the `type` bit field to `struct requested_info` and updates the server-side handler to parse and transmit type data, following the established pattern from the predecessor series. The patch includes minor cleanups and expands test coverage in `t5701`.
   - **Patch 8/10**: Explicitly endorsed as ready to merge. The patch adds the `wants_type` flag and `types` array to `struct fetch_object_info_results`, mirroring the existing `wants_size`/`sizes` pattern, and updates the client-side parsing logic to handle type data. The patch is mechanical and follows the established pattern for size metadata.
   - **Patch 9/10**: Explicitly endorsed as ready to merge. The patch appends "type" to the advertised capabilities string in `serve.c` and updates `t1017` to expect the actual type ("blob") for `%(objecttype)` and confirm that unsupported placeholders still return empty strings. The patch completes the end-to-end chain for object type metadata, enabling the server to advertise, parse, and transmit type data, and the client to request and display it.
   - **Patch 10/10**: Explicitly endorsed as ready to merge. The patch unifies the default format for all `cat-file` batch commands, removing the temporary workaround and ensuring consistent output across all transport protocols. The change is mechanical and uncontroversial, touching only documentation, the `cat-file` builtin, and the test suite.
- **Jeff King (Peff)**: Refined Junio’s design into a hybrid approach: a single array of lightweight structs where the caller decides which fields to populate, avoiding sentinels entirely. Highlighted that the remote-object-info protocol transmits raw strings (e.g., `"blob"`, `"1234"`), so the refactoring can bypass `read_object_info()` and its parameter-passing conventions. Confirmed with Junio that sentinel values are unsuitable for fields like `size` and that a separate bit is preferable for signaling unknown objects. The hybrid design is now implemented in v5. Also advocated for passing `struct object_info_args` members directly rather than grouping them into semi-opaque structs, a point Pablo cited in his response to Karthik Nayak’s feedback. Proposed a targeted refactoring (patch 6/10) to improve the design of the transport layer’s object-info handling, addressing a latent bug and clarifying ownership semantics. The current design stored pointers to the OID array and results struct in `git_transport_options`, but these are only valid for a single request. This created a subtle risk—if the transport were reused, the struct could retain dangling pointers to stack variables from the previous call. The proposed fix removes the `object_info_oids` and `object_info_results` fields from `git_transport_options` and instead passes them as explicit parameters to `transport_fetch_object_info()` and its vtable implementations. This makes ownership clearer: the caller retains responsibility for the OID array and results, and the transport no longer needs to manage their lifetimes. Peff also marks the OID array as `const` to emphasize its input-only role. The change is mechanical (25 insertions, 19 deletions) and touches seven files, primarily `transport.c`, `transport-helper.c`, and `builtin/cat-file.c`. The patch does not alter behavior but reduces technical debt and prepares the codebase for future enhancements, such as reusing transport connections for multiple object-info requests. Peff’s patch is signed-off, tested, and uncontroversial; it has been integrated into Pablo’s series with only minor commit message typo fixes. Feedback is substantive and actionable, with no merge status implied.

  Proposed a future optimization to make the `remote-object-info` protocol stateful for connection reuse, allowing multiple object-info requests over a single transport connection. Peff suggests leaving the current syntax (`remote-object-info <remote> <objs...>`) unchanged and letting `git cat-file` handle connection reuse internally as an optimization. This would avoid protocol changes while still allowing the transport to remain open between requests, reducing overhead for repeated queries. Peff notes that the cost of keeping a connection open (e.g., an SSH session) is low, as most transports either time out naturally or are used in contexts where additional requests are likely. Pablo agrees but defers the optimization to a follow-up series, citing time constraints and the current focus on object type support. Peff endorses this approach, emphasizing that the protocol remains unchanged and the optimization can be added later without affecting the current patch series.
- **Chandra Pratap**: Substantive review in v1 endorsing the narrow scope and proposing patch reordering for clarity (now implemented in v2). Raised technical points (style nits, test portability, macro usage, test restructuring, documentation wording) that were all addressed in v2. Follow-up discussion on robustness testing was optional and deferred. Provided surface-level feedback on patch 5 in v2 (shell quoting nit), which Pablo has incorporated in v3. The nit aligned with Git’s conventions and was advisory. No merge status implied. Also prompted the test cleanup in patch 1 of v3 (replacing `wc -c | xargs` with `test_file_size()` and caching `git rev-parse` output), which was incorporated as a purely mechanical, uncontroversial improvement.
- **Karthik Nayak**: Provided surface-level feedback in v1 (style adjustments, commit message rewording, design question about string vs. integer type transmission). Retracted the design question after confirming the server transmits type as a string. In v3, raised a surface-level concern about the commit message phrasing in patch 3/10, noting that the claim that `struct object_info_args` “suggests that fetch_object_info() uses all three of them” was misleading because the function *does* use all three fields. Pablo acknowledged the feedback and clarified that the struct’s purpose was to reduce parameter count but created a misleading abstraction, as the fields are accessed individually and the struct is constructed immediately before the function call. The commit message was reworded to accurately reflect this rationale. Karthik’s latest reply confirms the updated commit message now clearly explains why the wrapper is being removed—it grouped unrelated pointers accessed individually, so the abstraction added no value. The feedback is advisory and does not require technical changes.
- **Pablo Sabater**: Addressed all review feedback in v2, v3, v4, and v5, including patch reordering, test portability fixes, macro usage, test restructuring, documentation updates, Chandra Pratap’s shell quoting nit, and the test cleanup in patch 1. Implemented the hybrid design proposed by Junio and Peff, replacing `struct object_info` with `struct fetch_object_info_results` and avoiding sentinel values and boolean flags. Added robustness fixes in patches 2/10 and 8/10 to `die()` if the server sends fewer or more object-info lines than requested OIDs or an invalid type string, preventing uninitialized data from being used by the caller. Implemented a fix for Junio’s over-length response concern: after reading the expected number of objects, the client now checks for an immediate flush packet and `die()`s if anything else arrives. This aligns with Git’s defensive programming practices and addresses the protocol hygiene issue. Moved the `ask_type` flag and related machinery to patch 8/10 in v5, addressing the premature flag issue. Also implemented the mechanical refactoring in patch 3/10 (now endorsed by Junio as ready to merge) that removes `struct object_info_args` and passes its members directly to `fetch_object_info()`, citing Peff’s advocacy for avoiding semi-opaque structs. Integrated Peff’s refactoring (patch 6/10) to remove object-info fields from `git_transport_options` and pass them as explicit parameters to `transport_fetch_object_info()`, fixing two minor commit message typos. Patches 7/10, 8/10, and 9/10 complete the end-to-end implementation for object type metadata, enabling the server to parse, transmit, and advertise type data as a string. Patch 10/10 unifies the default format for all queries, ensuring consistent output across all transport protocols and backward compatibility. The series is now at v6 to address a static-analysis warning identified in CI for v5. Acknowledged Junio’s observation about exit status preservation in patch 1/10, noting that the change is a purely mechanical, uncontroversial test cleanup and that no code changes are needed. The commit message for patch 1/10 has been updated to reflect this rationale, and the patch is now explicitly marked as ready to merge. In patch 5/10, converted the last error path in `fetch_object_info()` to `die()`, aligning with Git’s defensive programming practices and addressing a latent robustness issue. Clarified the design intent behind `remote_atom_map[]` in patch 4/10, explaining that the "type" entry was included to test the client’s handling of known-but-unsupported placeholders and that the new design avoids carrying unused arrays. Deferred Peff’s suggestion to make the `remote-object-info` protocol stateful to a follow-up series, citing time constraints.

Key technical details:
- Files touched: `protocol-caps.c`, `fetch-object-info.c`, `fetch-object-info.h`, `serve.c`, `transport.c`, `transport.h`, `transport-internal.h`, `transport-helper.c`, `builtin/cat-file.c`, `object-file.c`, `odb.h`, `Documentation/git-cat-file.adoc`, `Documentation/gitprotocol-v2.adoc`, `Documentation/technical/protocol-v2.txt`, and test files `t1017` and `t5701`. The architectural changes focus on `fetch-object-info.c` and `builtin/cat-file.c`, replacing the `object_info`-based parsing logic with the hybrid design (single array of lightweight structs, caller-controlled field population). Patch 1 specifically touches `t/t5701-git-serve.sh`, replacing `wc -c | xargs` with `test_file_size()` and caching `git rev-parse two:two.t` output in variables for robustness, motivated by portability and the preservation of command exit statuses within here-documents. Pablo has acknowledged Junio’s observation about exit status preservation, noting that the change is a purely mechanical, uncontroversial test cleanup and that no code changes are needed. The commit message for patch 1/10 has been updated to reflect this rationale, and the patch is now explicitly marked as ready to merge. Patch 2 adds safety checks in `fetch_object_info()` to validate the server response length (under-length and over-length responses) and now checks for an immediate flush packet after reading the expected number of objects. The patch explicitly validates both under-length and over-length responses, addressing Junio’s protocol hygiene concern. Junio’s latest reply explicitly endorses this patch as ready to merge. Patch 3 (now endorsed by Junio as ready to merge) removes `struct object_info_args` and updates function signatures to pass its members directly. The commit message for patch 3/10 has been updated to clarify that `fetch_object_info()` *does* use all three fields of `struct object_info_args` and to explain the rationale for removing the struct. Karthik Nayak’s latest reply confirms the updated commit message now clearly explains why the wrapper is being removed. Patch 4 introduces `struct fetch_object_info_results` and drops `object_info_options`, `remote_atom_map[]`, and `free_object_info_contents()`. The patch also introduces `free_fetch_object_info_results()` and converts the final `else` in `fetch_object_info()` to `die()`. Patch 6 removes object-info fields from `git_transport_options` and passes them as explicit parameters, marking the OID array as `const`. Patch 5 converts the last error path in `fetch_object_info()` to `die()`, updating the function signature to `void` and removing the final `return 0`. The `ask_type` flag and related machinery are now introduced in patch 8/10. Patch 7 adds the `type` bit field to `requested_info` and updates the server-side handler to parse and transmit type data. The patch also includes two minor cleanups—fixing a bit-field style nit (`size:1` → `size: 1`) and wrapping an over-long line in `send_info()`—and adds a test in `t5701` verifying the server handles requests for both "size" and "type", including duplicate OIDs. Patch 8 adds the `types` array to `fetch_object_info_results` and updates the client-side parsing logic. Patch 9 appends "type" to the advertised capabilities string in `serve.c` and updates `t1017` to expect the actual type ("blob") for `%(objecttype)` and confirm that unsupported placeholders still return empty strings. The patch modifies `serve.c` to append "type" to the advertised capabilities string in `object_info_advertise()`, changing the comment from "Currently only size is supported" to "Currently only size and type are supported". The test suite in `t/t1017-cat-file-remote-object-info.sh` is updated to replace the now-obsolete test for unsupported placeholders with two new tests: one verifying that `%(objecttype)` returns the actual type ("blob") for a known object, and another confirming that truly unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings. The patch also adds type variables (`hello_type`, `tree_type`, `commit_type`, `tag_type`) to the test preamble, mirroring the existing size and OID variables. Patch 10 removes the temporary format exception and ensures the default format is uniform, touching only `Documentation/git-cat-file.adoc`, `Documentation/gitprotocol-v2.adoc`, `builtin/cat-file.c`, and `t/t1017-cat-file-remote-object-info.sh`.
- New or renamed symbols: The "type" capability in the protocol, `%(objecttype)` placeholder in `cat-file` batch-command format strings, `requested_info->type` bit field in `protocol-caps.c`, `wants_type` flag and `types` array in `struct fetch_object_info_results`, and the unified default format string `%(objectname) %(objecttype) %(objectsize)` for all queries. Patch 1 rewords a test to clarify why `wc -c | xargs` is unreliable for file size checks and replaces it with `test_file_size()`, preserving the exit status of commands run inside `$(...)` within a here-document. Pablo has acknowledged Junio’s observation about exit status preservation, noting that the change is a purely mechanical, uncontroversial test cleanup and that no code changes are needed. The commit message for patch 1/10 has been updated to reflect this rationale, and the patch is now explicitly marked as ready to merge. Patch 2 adds safety checks in `fetch_object_info()` to validate server response length (under-length and over-length responses) and now checks for an immediate flush packet. The patch explicitly validates both under-length and over-length responses, addressing Junio’s protocol hygiene concern. Junio’s latest reply explicitly endorses this patch as ready to merge. Patch 3 (now endorsed by Junio as ready to merge) removes `struct object_info_args` and updates function signatures to pass its members directly. The commit message for patch 3/10 has been updated to clarify that `fetch_object_info()` *does* use all three fields of `struct object_info_args` and to explain the rationale for removing the struct. Karthik Nayak’s latest reply confirms the updated commit message now clearly explains why the wrapper is being removed. Patch 4 introduces `struct fetch_object_info_results` and drops `object_info_options`, `remote_atom_map[]`, and `free_object_info_contents()`. The patch also introduces `free_fetch_object_info_results()` and converts the final `else` in `fetch_object_info()` to `die()`. Patch 6 removes object-info fields from `git_transport_options` and passes them as explicit parameters, marking the OID array as `const`. Patch 5 converts the last error path in `fetch_object_info()` to `die()`, updating the function signature to `void` and removing the final `return 0`. The `ask_type` flag and related machinery are now introduced in patch 8/10. Patch 7 adds the `type` bit field to `requested_info` and updates the server-side handler to parse and transmit type data. Patch 8 adds the `types` array to `fetch_object_info_results` and updates the client-side parsing logic. Patch 9 appends "type" to the advertised capabilities string and updates tests to expect the actual type for `%(objecttype)`.
- Behavior: Client requests and displays object type metadata from the server via remote-object-info, matching local queries. The server parses client requests for "type" (patch 7/10), transmits type data as a string (e.g., `"blob"`, `"tree"`), and advertises the "type" capability (patch 9/10). The client maps this string to an internal enum using `type_from_string_gently()` (patch 8/10). Patch 1 ensures backward compatibility by dynamically including all supported options (currently "size" and "type") in client requests. Patch 2 adds defensive checks to fail fast if the server violates protocol invariants (under-length or over-length responses). Patches 7/10, 8/10, and 9/10 complete the end-to-end chain for object type metadata. Patch 10 unifies the default format for all queries, ensuring consistent output across all transport protocols and backward compatibility. Peff’s refactoring (patch 6/10) does not alter behavior but clarifies ownership semantics in the transport layer, reducing technical debt. The proposed optimization to make the `remote-object-info` protocol stateful for connection reuse is deferred to a follow-up series and would not alter the current behavior.
- On-disk format: No changes.
- Test coverage: Expanded in `t5701` (server-side) and `t1017` (client-side), including verification of the `%(objecttype)` placeholder in batch-command output. Patch 1 updates `t5701` to use `test_file_size()` and cached variables for robustness, motivated by portability and the preservation of command exit statuses within here-documents. Pablo has acknowledged Junio’s observation about exit status preservation, noting that the change is a purely mechanical, uncontroversial test cleanup and that no code changes are needed. The commit message for patch 1/10 has been updated to reflect this rationale, and the patch is now explicitly marked as ready to merge. Patch 3 adds a test verifying the server handles requests for both "size" and "type". Patch 7 adds a test in `t5701` verifying the server handles requests for both "size" and "type", including duplicate OIDs. Patch 9 updates `t1017` to expect the actual type ("blob") for `%(objecttype)` and confirms that unsupported placeholders (e.g., `%(objectmode)`, `%(objectsize:disk)`) still return empty strings. The patch replaces the now-obsolete test for unsupported placeholders with two new tests: one verifying that `%(objecttype)` returns the actual type ("blob") for a known object, and another confirming that truly unsupported placeholders still return empty strings. The new tests use the same helper functions and setup as the rest of the test file, ensuring consistency. The patch also adds type variables (`hello_type`, `tree_type`, `commit_type`, `tag_type`) to the test preamble, mirroring the existing size and OID variables. Patch 10 validates the unified format for all transport protocols and confirms mixing `info` and `remote-object-info` commands produces consistent output. All tests are passing in v5, except for the static-analysis warning that prompted v6.

Related work: Based directly on Eric Sunshine's v21 "cat-file: add remote-object-info to batch-command" series, which was recently merged. Peff’s proposed refactoring is now integrated into Pablo’s series. The potential optimization to make the `remote-object-info` protocol stateful for connection reuse is deferred to a follow-up series.

---