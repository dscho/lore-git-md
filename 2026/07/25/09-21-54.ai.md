This is a **substantive review follow-up** from Jeff King (Peff) in the ongoing thread about Ted Nyman’s v4 series fixing packfile URI download race conditions. Peff continues diagnosing the intermittent `--stress` test failure in `t5550-http-fetch-dumb.sh` (test 34), which blocks the series from progressing to `next`.

The core observation is that curl’s client-side strace shows an unexpected EOF on the socket (`recvfrom(12, "", 16384, 0, NULL, NULL) = 0`), even though the server process neither closed its socket nor exited. This suggests the connection is being terminated by an intermediary (e.g., a firewall, proxy, or kernel-level timeout) rather than by the server or client. Peff speculates that the custom Perl server’s socket handling—unique to this test—may be introducing subtle bugs, and proposes rewriting the test to use Apache (like the rest of Git’s HTTP tests) to eliminate this variable.

No fix is proposed yet, but the email narrows the problem to the test infrastructure rather than the core download logic. The series remains **blocked** until the test flakiness is resolved. Key technical details: the failure occurs in the overlapping-download test, involves a 1 MB transfer, and is reproducible under `--stress` but not in normal runs. The strace evidence rules out server-side issues, leaving the custom socket handling as the prime suspect.