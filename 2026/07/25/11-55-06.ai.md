This is the final patch in Pablo Sabater's GSoC series extending `git cat-file --batch-command` to support the `%(objecttype)` placeholder in remote-object-info queries. The patch unifies the default format string used by `cat-file` for both local and remote object queries, removing the temporary workaround that excluded `%(objecttype)` from remote queries.

The patch updates the default format from `%(objectname) %(objectsize)` (for remote queries) to `%(objectname) %(objecttype) %(objectsize)` (for all queries), aligning the behavior of `remote-object-info` with the rest of `cat-file`. Documentation is updated to reflect the new support for `%(objecttype)` in remote queries, and the warning about the temporary default format is removed. The test suite is adjusted to expect object type metadata in the output, replacing earlier tests that verified the absence of type data.

The implementation is mechanical and follows the established pattern from earlier patches in the series. The patch touches `builtin/cat-file.c` (removing the temporary format override), `Documentation/git-cat-file.adoc` and `Documentation/gitprotocol-v2.adoc` (updating documentation), and `t/t1017-cat-file-remote-object-info.sh` (updating tests). The changes are narrowly scoped to the unification of the default format and do not introduce new functionality beyond what was added in earlier patches.

This patch completes the series, which now provides end-to-end support for object type queries via remote-object-info. The series as a whole appears well-structured, with thorough test coverage and clear documentation updates. No open questions or loose ends are evident in this patch.