This is the second patch in Harald Nordgren’s **`git branch --delete-merged`** series (v23, 7 patches total). It refactors the `delete_branches()` and `check_branch_commit()` functions in `builtin/branch.c` to replace separate `force` and `quiet` boolean parameters with a single `unsigned int flags` argument and an enum (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`). The patch is **purely mechanical**, introduces no behavioral changes, and is **low-risk and well-tested**.

The refactoring eliminates local boolean caching (e.g., `int force = 1`) to ensure flags remain the single source of truth, addressing a maintenance risk identified in earlier reviews. Each use site now tests the flags directly (e.g., `flags & DELETE_BRANCH_FORCE`), preventing stale cached values when flags are mutated or forwarded. The changes are confined to `builtin/branch.c` and align with the series’ broader goal of introducing a flags-based API for branch deletion, which later patches extend with `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, and `DELETE_BRANCH_DRY_RUN`.

This patch is a prerequisite for the rest of the series, which implements the `--delete-merged` subcommand, per-branch opt-out, and `--dry-run` functionality. The thread context confirms the series is **unblocked and ready for merging**, with only mechanical fixes to patch 1/7 remaining. The flags-based API is critical for the `--delete-merged` subcommand’s safety checks and composability (e.g., repeatable upstream selectors). No substantive review feedback remains outstanding for this patch.