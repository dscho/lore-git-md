This is a follow-up in the packfile URI race-condition fix series. Jeff King (Peff) proposes a simple test-design change to resolve the intermittent `--stress` failure in `t5550-http-fetch-dumb.sh` (test 34). The fix drops the `test -s` check and associated trace greps, eliminating the false positive caused by the race between the server’s `write()` and the client’s disk flush. The change preserves the test’s ability to catch real corruption while making it reliable under stress. No production code is touched; the series remains blocked only by this test synchronization issue. Peff’s patch is minimal, well-motivated, and directly addresses the root cause identified in his earlier analysis. If adopted, the series should be ready to proceed.