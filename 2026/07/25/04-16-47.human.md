This is the first patch in Harald Nordgren’s **v22 `git branch --delete-merged` series**, which is now unblocked and ready for merging. Junio C Hamano’s reply here is a **surface-level review** of patch 1/7, the foundational `--forked` filter for `git branch --list`. The review focuses on two minor mechanical points: the readability of a `skip_prefix` cascade in `short_upstream_name()` and a type mismatch between `int i` and `size_t filter->forked.nr` in `filter_forked_match()`.

Junio suggests rewriting the `short_upstream_name()` helper to use a more explicit two-step `skip_prefix` sequence instead of the current chained `||` expression, calling the current version "tricky to read." This is purely a style preference and does not affect functionality. The second point flags a type mismatch in the loop variable `i` (declared as `int`) used to iterate over `filter->forked.nr` (a `size_t`). While this is technically correct on most platforms, it is a potential portability issue and should be addressed by declaring `i` as `size_t`.

Neither issue is substantive or blocking. The `--forked` filter itself is well-designed, thoroughly tested, and already addresses the edge case that previously blocked the series (protecting branches if pushing them by name would update their upstream). The patch is low-risk, touching only `ref-filter.c`, `builtin/branch.c`, and the test suite, and establishes the core filtering logic that the `--delete-merged` subcommand will use. Junio’s feedback here is purely mechanical and does not challenge the design or implementation. The series as a whole remains ready for merging, with no outstanding issues.