This is Junio C Hamano's review of Tian Yuchen's `[PATCH v1 0/3]` series migrating four global configuration variables into `repo_config_values` as part of the `the_repository` removal effort. The review is substantive, questioning the architectural approach in patch 1/3.

Junio expresses confusion about the series' handling of repository-scoped configuration getters, particularly the unconditional fallback to `the_repository` when `repo != the_repository`. He argues this "papers over" an API limitation rather than addressing it: if a subsystem (like `merge-ort`) explicitly forbids `the_repository` but the underlying API only supports `the_repository`, the correct fix should be extending that API to support arbitrary repositories, not adding conditional logic in the getter. His proposed alternative is to `BUG()` when `repo != the_repository`, forcing the issue to be resolved properly.

The review targets the core design decision in patch 1/3 (the `minimum_abbrev`/`default_abbrev` migration) and implies the series may be premature or misaligned with the `the_repository` removal goals. No feedback is provided on patches 2/3 or 3/3, suggesting they are uncontroversial. The tone is puzzled rather than dismissive, but the technical objection is clear: the current approach risks accumulating "unfinished work" that will need later cleanup. This is likely to prompt a v2 revision or deeper discussion about how to handle repository-scoped configuration in subsystems that reject `the_repository`.