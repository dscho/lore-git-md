This is the first patch in Patrick Steinhardt’s five-part series making on-disk ODB structures pluggable, part of the ongoing ODB abstraction effort. It fixes a bug in the loose-object map loading logic: the function `load_one_loose_object_map()` was ignoring the passed-in ODB source and always loading the map from the common objects directory. The patch ensures the map is loaded from the correct source-specific path instead.

The change is small (10 insertions, 8 deletions in `loose.c`) but important for correctness. It replaces a hardcoded path construction with one that respects the loose source’s own directory, using `xstrfmt()` to build the path dynamically. The fix is mechanical and doesn’t alter behavior for existing repositories, but it’s a prerequisite for the later patches in the series that introduce pluggable directory creation.

The commit message is clear and concise, explaining both the bug and the fix without overstating the impact. This patch looks like a straightforward, well-motivated correction—exactly the kind of preparatory work needed before larger refactoring. No controversy or open questions are evident yet.