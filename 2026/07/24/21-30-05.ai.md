This is a maintainer review of Jeff King's bugfix patch for the `git diff --relative --cached` segfault. Junio C Hamano confirms the fix is correct: when `diff_unmerge()` returns NULL for paths outside the `--relative` prefix, the added NULL check prevents a crash by skipping further processing of that path. The review is substantive, explaining why the fix works (no filepair is queued, so the path is safely ignored) and implicitly endorsing the minimal approach. Junio does not raise any concerns or suggest changes, and the tone suggests this patch is likely to be merged as-is. The test case is also accepted without comment. No new technical details or open questions emerge; this appears to be the final review before integration.