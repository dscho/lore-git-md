This is a **CI/build system** and **refactoring** follow-up in the Coccinelle performance regression thread. Junio C Hamano clarifies the project's path forward on the type consistency question raised earlier. He recommends a two-phase approach: first, apply the immediate, low-risk fix (changing the second loop index in `builtin/branch.c` from `size_t` to `int` to match `candidates.nr`), and second, defer the broader standardization of ref-counting types to `size_t` for a future, quieter moment. The note explicitly avoids disrupting the already-merged `hn/branch-delete-merged` topic and leaves a `#leftoverbits` marker in the mailing list archive as a reminder for later discussion. This email resolves the open question about the loop index type while keeping the larger ref-counting type debate alive but tabled. The immediate action is clear: a follow-up patch correcting the loop index type is expected, with no further changes to `struct ref_array` or related code at this time.