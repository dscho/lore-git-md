This is the maintainer’s final check-in on Harald Nordgren’s `git branch --delete-merged` series, now at v21. The only change since v20 is a mechanical tweak to loop variable initialization (`size_t i` instead of `size_t i = 0`) to sidestep a Coccinelle CI timeout—no functional or design changes were made. Junio’s message confirms he sees no remaining issues and is treating the series as complete, pending only this CI workaround. The topic is unblocked and ready for merging; the only agreed follow-up is extending stacked-branch protection to `git branch -d` for consistency, which will land after this series.