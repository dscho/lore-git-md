This is a Google Summer of Code progress update from Siddharth Shrimali on his effort to improve disk space recovery for partial clones. The post covers weeks 7 and 8 of his project, continuing the work described in earlier updates.

The blog post refines the design for safely pruning unneeded promisor objects in partial clones. Based on prior context, it likely addresses integration details for the proposed `git maintenance run --task=gc-promisor` subcommand, such as its scheduling relative to the existing `git gc` task and performance considerations for large promisor packs. The post may also expand on the per-remote `.keep`-file safety mechanism introduced earlier, which records pruned objects to enable re-fetching if needed. No formal patches have been posted yet, so the work remains in the prototyping phase.

Key areas of focus in this update include the promisor packfile machinery (`promisor-remote.c`, `packfile.c`), the maintenance task scheduler (`builtin/maintenance.c`), and test coverage for the new functionality. The author explicitly invites feedback, signaling that the design is still open to adjustment. Open questions from earlier—such as handling shallow clones, grafted history, and alternate object directories—may be clarified or deferred in this post.

The update is well-timed for broader review, as the project is still in the design phase. If the post includes concrete refinements (e.g., revised config keys, CLI options, or edge-case handling), it could spark discussion among contributors familiar with the `git maintenance` infrastructure or promisor remote implementation. The work remains promising but is not yet ready for merging.