This is the maintainer's final sign-off on the previously discussed bugfix for the `git clone --revision` segfault. Junio explicitly praises the updated commit message, which now clarifies that the crash only occurs under protocol v0 and explains the root cause (servers advertising more refs than requested). The patch itself remains unchanged from the agreed NULL-check fix in `update_remote_refs()`; the only new material is the expanded rationale in the commit log. With this acknowledgment, the series is now fully approved and ready for merging to `master`. No further discussion or changes are expected.