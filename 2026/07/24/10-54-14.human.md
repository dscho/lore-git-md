This is a test infrastructure patch, part of the ongoing `git cat-file --batch-command` remote-object-info series (v21). It extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is purely mechanical: the functions are moved verbatim, and the original script now sources the new library instead of defining them inline.

The motivation is code reuse: a later patch in the series will need these helpers, and centralizing them avoids duplication. The commit also fixes a minor style nit in the original script, changing `. "$TEST_DIRECTORY/lib-loose.sh"` to `. "$TEST_DIRECTORY"/lib-loose.sh` to match Git’s coding guidelines. No functional changes are introduced.

This patch is uncontroversial and well-motivated. The series as a whole is technically complete and queued for `next`, so this refactoring is a straightforward cleanup that won’t block progress. The new `lib-cat-file.sh` file is small (16 lines) and focused, making it easy to audit. If you’re following the remote-object-info effort, this is a minor but necessary step in the test infrastructure.