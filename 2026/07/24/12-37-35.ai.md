This is a bugfix patch that adds the missing regression test for the recently agreed one-line NULL-check fix in `git clone --revision`. The test confirms that the segfault no longer occurs when cloning from a protocol-v0 server that advertises more refs than the client requested. The patch touches `t/t5606-clone-options.sh`, adding a new test case that uses `GIT_TEST_PROTOCOL_VERSION=0` and a custom `upload-pack` hook to simulate the problematic server behavior. The fix itself remains unchanged; this is purely test coverage. With the test now provided, the patch series is complete and ready for final review.