This is a test infrastructure refactoring patch, part 3 of the 13-patch `git cat-file --batch-command` remote-object-info series (v21). It extracts three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The change is purely mechanical: the functions are moved verbatim, and the only style tweak is adjusting the path quoting in the sourcing line to match Git's coding guidelines.

The motivation is code reuse: a later patch in the series (likely patch 12/13, which introduces the `remote-object-info` command) will need these helpers, and extracting them now avoids duplication. The new library file is minimal (16 lines) and contains no behavioral changes. The patch is authored by Eric Ju and carries Pablo Sabater's sign-off, reflecting the collaborative nature of the series.

This is a low-risk, non-controversial change that aligns with the project's preference for shared test infrastructure. The series as a whole is technically complete and queued for `next`, so this patch is effectively a prerequisite for the feature work that follows. No further changes are expected here.