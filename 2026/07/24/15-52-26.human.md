This is Junio’s one-line confirmation that the twenty-first iteration of the `git cat-file --batch-command` remote-object-info series is now **cleared for `next`**. The series enables Git clients to query object metadata (size, with type support designed for future expansion) from remote repositories without downloading full objects, using the `object-info` protocol v2 capability. After 21 revisions addressing all technical, security, and design concerns—including dynamic capability negotiation, memory leak fixes, and robust edge-case handling—the series is technically complete and ready for integration testing. Junio’s approval follows Pablo Sabater’s final cosmetic tweaks (documentation formatting, macro style, and typo fixes) and confirms the series will graduate to `next` in the upcoming integration cycle. No further changes are expected.