This thread began with a patch from tnyman@openai.com that fixes a severe performance regression in Coccinelle 1.1.1 static analysis, triggered by a specific code pattern in `builtin/branch.c`. The patch itself is a straightforward, mechanical change—moving loop index declarations into their respective `for` loops to avoid a Coccinelle rule that causes exponential slowdowns—reducing the analysis time from a timeout to under a second while preserving the function’s behavior. All tests pass, and the fix is narrowly scoped to the problematic file.

Jeff King (Peff) has since weighed in, acknowledging the issue but questioning the approach. He traces the root cause to a known Coccinelle bug (fixed in version 1.3.0+) and suggests upgrading the CI toolchain instead of working around the problem in the codebase. While he agrees the patch’s change is stylistically cleaner, he also points out a minor type mismatch in the original fix (using `size_t` for an `int` counter) that should be corrected. The discussion now centers on whether the project should tolerate toolchain workarounds or prioritize keeping dependencies current. No consensus has emerged yet, but the patch remains a valid solution to an immediate CI pain point.