This thread is a small, focused feature addition by Fabian Pottbäcker to make `git config --url` accept scp-style SSH URLs (like `host:path`), matching the behavior of nearly every other Git command. The series is now complete with its third patch, which switches the URL-parsing logic to the shared `url_parse` function already used elsewhere in Git. This change is purely mechanical—just one line in `builtin/config.c`—and brings the command into consistency with the rest of Git’s URL handling.

The motivation is scripting convenience: tools that extract or manipulate remote URLs from Git configuration (e.g., from "clone" buttons) often encounter scp-style URLs and need to parse them. The series is narrowly scoped, avoiding larger efforts like `fmt_ident` or automatic identity switching. Test coverage is thorough, mirroring existing patterns for `--get-urlmatch` and adding unit tests for edge cases. No objections or open questions have been raised yet, and the series appears uncontroversial. It’s currently under review, with no prior versions or related in-flight work.