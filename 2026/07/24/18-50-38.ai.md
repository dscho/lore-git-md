This is a review reply in the ongoing ODB abstraction series. Junio C Hamano responds to Patrick Steinhardt’s third patch, which defers object database (ODB) creation until after the ref database is set up.

The review is substantive. Junio walks through the patch’s logic step by step, confirming the intent and mechanics: the new `APPLY_REPOSITORY_FORMAT_SKIP_ODB_CREATION` flag allows `apply_repository_format()` to skip ODB creation, leaving the repository in a partially-initialized state. The caller (`init_db()`) then invokes the renamed `create_object_database()` later, after the ref database is created. Junio’s commentary suggests he understands the patch as a necessary infrastructure change to support future pluggable ODB backends, where the backend-specific on-disk layout (e.g., `objects/pack`, `objects/info`) will be created by the backend itself rather than by generic setup code.

No objections or correctness issues are raised; the feedback is purely explanatory and confirmatory. Junio’s closing rhetorical question (“I guess this is a prelude to specifying ‘no, we are not doing the files backend but are using this new thing’ in the global configuration?”) indicates he sees the patch as a logical step toward the series’ larger goal. The review depth is substantive: it engages with the patch’s purpose, the initialization sequence, and the implications for future backend pluggability.