This is the third patch in Harald Nordgren’s **v22 `git branch --delete-merged` series**, which is now **implementation-complete, unblocked, and ready for merging**. This patch adds a `DELETE_BRANCH_SKIP_UNMERGED` flag to enable bulk callers (like the upcoming `--delete-merged` subcommand) to silently skip unmerged branches instead of failing with the usual "not fully merged" error and advice. The change is minimal, targeted, and preserves existing behavior for all current callers.

The patch modifies `check_branch_commit()` and `delete_branches()` to suppress the error and advice when the new flag is set, ensuring unmerged branches are skipped without triggering a failure. This is critical for the `--delete-merged` subcommand, which needs to process many branches in bulk without stopping on unmerged ones. The flag is checked at both the commit-checking and deletion stages, making the behavior consistent throughout the deletion pipeline.

The implementation is **low-risk and well-tested**: it touches only `builtin/branch.c` (10 lines changed), introduces no behavioral changes for existing callers, and is exercised by later patches in the series that use the `--delete-merged` subcommand. The patch is part of the **flags-based API refactoring** (patches 2-4/7) that replaces boolean arguments with an `unsigned int flags` parameter, eliminating local boolean caching to ensure flags remain the single source of truth. This refactoring was motivated by maintenance concerns and has been confirmed to introduce no functional regressions.

For a busy reader, this patch is a **small but necessary plumbing change** that enables the bulk-deletion workflow in the series’ main feature. It’s uncontroversial, well-explained, and directly supports the larger goal of safe automated branch cleanup. The series as a whole is **ready for merging**, and this patch is a straightforward step toward that outcome.