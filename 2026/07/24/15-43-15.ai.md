This is the maintainer's final check-in on Harald Nordgren's `git branch --delete-merged` series, now at v21. The series is **implementation-complete and ready for merging**, with no functional changes since v20—only a mechanical loop variable adjustment (`size_t i` instead of `size_t i = 0`) to prevent a CI timeout in Coccinelle's `strvec.cocci` rule. Junio explicitly confirms there are no other outstanding issues and asks whether the topic is "pretty much done."

The series introduces a safe, automated local-branch cleanup command with comprehensive filtering and safety controls, including per-branch opt-out (`branch.<name>.deleteMerged=false`), `--dry-run` preview, and **order-independent stacked-branch protection** (resolving the last correctness concern via a single-pass graph traversal). All prior feedback—including Junio's blocking order-dependency issue and Phillip Wood's API consistency and test improvements—has been addressed. The only agreed follow-up is extending stacked-branch protection to `git branch -d` for consistency, which Harald has committed to implementing post-merge.

The patches touch `builtin/branch.c`, `ref-filter.c`, documentation, and tests, with a flags-based API refactoring (patches 2-4/7) and core command implementation (patches 5-7/7). The series is **well-motivated, thoroughly tested, and aligned with Git's API consistency expectations**, making it a strong candidate for `next`. Junio's question signals the topic is effectively unblocked; expect it to appear in a "What's cooking" report soon.