This is the first patch in a three-patch feature series adding scp-style URL support to `git config --url`. It focuses on normalizing default ports for SSH and FTP in Git's URL-matching code.

The patch ensures that URLs like `ssh://host:22` and `ssh://host` are treated identically by stripping the default port (22) during normalization, just as Git already does for HTTP (80) and HTTPS (443). The same logic is applied to FTP (port 21). The change is implemented in `urlmatch.c` and tested via new unit tests in `t/unit-tests/u-urlmatch-normalization.c`, which verify that URLs with explicit default ports are normalized to their canonical form without the port.

The code is straightforward: it adds two new conditional blocks to the existing port-normalization logic, mirroring the structure used for HTTP/HTTPS. The commit message is minimal but sufficient, and the patch appears to be a necessary preparatory step for the later patches in the series. No behavioral changes are visible to users yet—this is purely internal plumbing. The patch is uncontroversial and aligns with Git's existing URL-handling conventions.