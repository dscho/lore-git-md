This is the final patch in the 13-part `git cat-file --batch-command` remote-object-info series, now at **v21** and **queued for `next`**. The patch makes the client's atom allow-list dynamic, adapting to what the server actually supports rather than hardcoding `"objectname"` and `"objectsize"`.

The change is straightforward: the client now queries the server for its advertised `object-info` capabilities (e.g., `"size"`, `"type"`), maps those to the corresponding placeholders (`%(objectsize)`, `%(objecttype)`), and populates a `remote_allowed_atoms` string list in `struct expand_data`. In `expand_atom()`, only atoms in this list are expanded; others (like `%(objecttype)` when the server doesn’t support `"type"`) return an empty string, matching `for-each-ref` behavior. The patch also ensures output is always produced—even if no attributes are supported, the client emits a blank line.

The implementation is clean: `get_remote_info()` is refactored to accept an externally constructed `object_info_options` list, and a new `remote_atom_map` array maps protocol options to placeholders. The server-side filtering in `fetch_object_info()` removes unsupported options by swapping with the last element, so the list is no longer sorted, but this has no practical impact since the list is tiny (currently two entries). Tests verify that unsupported placeholders expand to empty strings and that `%(objectname)`-only requests still work.

This is a well-motivated, low-risk change that future-proofs the series for new `object-info` capabilities. The patch is mechanical, thoroughly tested, and ready for integration.