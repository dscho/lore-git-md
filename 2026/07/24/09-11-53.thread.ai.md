Thread identity: patch series (single patch), originating author tnyman@openai.com, thread type: build system / CI fix.

Core problem or goal: resolve a pathological performance regression in Coccinelle 1.1.1 static analysis caused by a specific code pattern in `builtin/branch.c`. The `separate_loop_index` rule from `tools/coccinelle/strvec.cocci` triggers an exponential slowdown when a single loop index variable declared at function scope is reused across two separate loops in `delete_merged_branches()`. This causes the static-analysis job on the `seen` integration branch to hit its six-hour timeout.

Current status: under review. The patch is a narrowly scoped, mechanical fix: declare each loop index in its respective `for` loop instead of sharing one declared at function scope. This avoids triggering the expensive Coccinelle rule while preserving the function's behavior. The author reports the fix reduces the Coccinelle check from a timeout to 0.06 seconds, and the full `coccicheck` run completes in 47 seconds. All 190 tests in `t3200-branch.sh` pass with the change. The patch applies on top of `hn/branch-delete-merged` and touches only `builtin/branch.c`, changing three lines.

Key technical details:
- Files touched: `builtin/branch.c` (three lines changed).
- Subsystem: build system / CI (Coccinelle static analysis).
- New or renamed symbols: none.
- Old-vs-new behavior: identical runtime behavior; the change is purely to avoid a static-analysis performance issue.
- On-disk format changes: none.
- Test coverage: `t3200-branch.sh` (190 tests, all passing).

Open questions and loose ends: none identified. The patch is self-contained, includes test results, and addresses a clear CI pain point.

Participant positions: none recorded yet (initial patch submission).

Related work: applies on top of `hn/branch-delete-merged` (no further details on that topic's status). The fix is specific to Coccinelle 1.1.1 and the `separate_loop_index` rule.