This is a maintainer review of Jeff King’s (Peff) bugfix patch that prevents a segfault in `git diff --relative --cached` when operating on unmerged paths outside the current directory. Junio C Hamano confirms the fix is correct: the added NULL check in `do_oneway_diff()` ensures that if `diff_unmerge()` returns NULL for paths outside the `--relative` prefix, the function safely returns without attempting to fill or queue the filepair. The test case, which verifies that `--relative --cached` now ignores unmerged paths outside the prefix, is also endorsed.

The patch is small, targeted, and well-explained, with clear regression history and test coverage. Junio’s review is substantive and confirms the fix’s correctness without raising any concerns. This looks like a straightforward bugfix that should proceed to `next` without controversy. No further discussion or revisions appear necessary at this stage.