This is a **CI/build system** follow-up in the ongoing discussion about the Coccinelle performance regression in `builtin/branch.c`. Junio C Hamano expands on Jeff King’s (Peff) earlier observation about the type mismatch in the second loop index, agreeing that `int` is the correct type for the loop index (matching `candidates.nr`) and that the `size_t` declaration—introduced to appease `-Wsign-compare`—was unnecessary and counterproductive. He notes that the cast to `size_t` is "gross" and should be removed, but also floats a broader idea: standardizing all ref-counting in the codebase to `size_t` by updating `struct ref_array` in `ref-filter.h`. This would eliminate similar type mismatches elsewhere but would require a more invasive change. The immediate takeaway is that the second loop’s index should be corrected to `int` in a follow-up patch, while the larger standardization question remains open for future discussion. The thread’s focus remains on balancing pragmatic fixes with long-term codebase consistency.