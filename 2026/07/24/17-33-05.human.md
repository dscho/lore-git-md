This is a brief follow-up in the ongoing discussion about the Coccinelle performance regression fix in `builtin/branch.c`. Junio C Hamano clarifies his stance on the type mismatch in the second loop index, recommending an immediate, narrowly scoped correction: change the loop index from `size_t` to `int` to match `candidates.nr` in `struct ref_array`. He emphasizes that this should be done within the current topic (`hn/branch-delete-merged`) to avoid disrupting its stability, while explicitly deferring the broader question of standardizing all ref-counting to `size_t` (e.g., by updating `struct ref_array` itself) to a future, quieter moment. The email leaves a clear action item—correct the loop index type in a follow-up patch—but firmly closes the door on any larger refactoring for now. The tone is pragmatic, prioritizing topic stability over long-term consistency, and the recommendation aligns with earlier feedback from Jeff King (Peff). No new technical details or objections are introduced; this is purely a procedural and scope-setting clarification.