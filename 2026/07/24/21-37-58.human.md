This is a review reply from Junio C Hamano to Michael Montalbo’s refactoring patch that makes `get_commit_action()` a pure predicate in the revision-walking machinery. Junio expresses surprise at the patch’s scope—over 100 lines of new code—and asks for clarification on whether this is a code cleanup, bugfix, or new feature.

The patch itself is a well-motivated refactoring targeting a long-standing impurity in `revision.c`: `get_commit_action()` mutates tracked line ranges when used with `git log -L` (line-level log) without parent rewriting, making it unsafe for out-of-order evaluation. The change moves the mutation logic to `simplify_commit()`, leaving `get_commit_action()` as a pure predicate, and includes a new test subcommand (`line-log-peek`) to verify the function remains side-effect-free. The thread summary confirms this is a standalone, self-contained improvement with no prior versions or dependencies, though Junio’s reply suggests he may not recall the earlier discussion or the rationale for the patch’s size.

Junio’s question is procedural rather than technical—he’s not challenging the patch’s correctness but seeking to reorient himself on its purpose. The patch’s commit message and test coverage are thorough, but his reaction hints that the change might benefit from a clearer upfront summary of its goals and scope in the cover letter or subject line. For a busy reader, this exchange underscores that the patch is under review and may need a concise reintroduction to align reviewers.