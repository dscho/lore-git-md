This is Junio C Hamano reporting a test hang in the `seen` integration branch after merging Ted Nyman’s v3 packfile URI race-condition series. The hang occurs in `t/t5550-http-fetch-dumb.sh`, one of the two test scripts the series modifies.

The hang is not present in the baseline `seen` branch (728e180b7b) but appears consistently when the series is merged on top (05d0dd408c). The baseline already has leak-check failures from other topics, but the new tests in `t5550` are the only change that could explain the hang. Junio suspects an interaction between the new tests and another topic in `seen`, but the exact cause is still unknown.

Ted is asked to investigate. The CI logs for the failing run are linked, and the baseline run is provided for comparison. No other technical feedback is given; this is purely a procedural heads-up about a test-blocker. Until the hang is resolved, the series cannot graduate to `next`.