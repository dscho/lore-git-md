This is a Google Summer of Code (GSoC) progress update from Siddharth Shrimali on his project to improve disk space recovery for partial clones. The email links to his combined week 7 and 8 blog post, continuing the design and prototyping work described in earlier updates.

The post likely advances the proposed `git maintenance run --task=gc-promisor` subcommand and its integration with Git’s maintenance scheduler. Based on prior context, it may address unresolved questions from weeks 5–6, such as the scheduling order of `gc-promisor` relative to `gc`, performance implications of pruning large promisor packs, or refinements to the per-remote `.keep`-file safety mechanism. The work remains focused on the promisor packfile machinery (`promisor-remote.c`, `packfile.c`) and the maintenance task infrastructure (`builtin/maintenance.c`), with test coverage still planned for `t/t5329-partial-clone-prune.sh`.

No formal patches have been posted yet, and the email does not include new feedback from other contributors. The author’s invitation for review suggests the design is still open to input, particularly on edge cases (e.g., shallow clones, alternate object directories) and configurability (e.g., grace period tuning). The post may also clarify whether the grace period should be exposed via CLI (`--grace-period=<days>`) or remain config-only. Performance testing on large promisor packs, mentioned as pending in week 6, may now be underway or deferred.

The update is part of an ongoing GSoC effort, so major design shifts are still possible before formal patch submission. Key technical details to watch for in the blog post include any changes to the proposed config keys (`maintenance.gcPromisor.enabled`, `maintenance.gcPromisor.gracePeriod`), the handling of promisor packs from multiple remotes, and the interaction between `gc-promisor` and existing maintenance tasks. The thread remains an explicit call for wider review, signaling that the project is still in a formative phase.