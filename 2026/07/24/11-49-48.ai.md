This is a **CI/build system** discussion that revisits the earlier patch fixing a Coccinelle performance regression.

Jeff King (Peff) replies to the original patch, acknowledging the problem but questioning the approach. He identifies the root cause as a Coccinelle bug (or performance limitation) in version 1.1.1, which has since been improved in newer releases (1.3.0+). The fix in Coccinelle (commit 58619b8fe) specifically addresses exponential slowdowns in pattern matching for loop indices, which aligns with the issue observed in `builtin/branch.c`. Peff suggests upgrading the CI job's Coccinelle version (available in Ubuntu 25.10) instead of working around the problem in the codebase, arguing this would prevent similar issues in the future.

He also notes that the patch's mechanical change—moving loop indices into their respective `for` loops—is stylistically cleaner, as it avoids reusing the same variable for unrelated loops. However, he points out a minor oversight in the patch: the second loop uses `size_t` for `i`, but `candidates.nr` is an `int`, so the cast is unnecessary and the variable should use the correct type. This is a small but substantive improvement over the original patch.

The discussion shifts the focus from a narrow fix to a broader question: should the project tolerate workarounds for toolchain limitations, or invest in keeping tooling up to date? Peff's preference for the latter is clear, but the thread is still open—no consensus has emerged yet. The patch itself remains well-motivated and technically sound, but the review introduces a potential alternative solution.