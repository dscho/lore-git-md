Updated AI thread summary:

Git replay linearization series summary:

Subject: [PATCH v5 0/3] replay: introduce --linearize option
Author: Toon Claes
Status: **v5 fully merged to 'master' (2026-07-15)**, but **three critical issues identified post-merge**:
1. **Silent commit dropping regression** in single-branch replay with merge commits (Johannes Schindelin, highest priority).
2. **CLI design inconsistency** with `git rebase` (Patrick Steinhardt, strategic concern).
3. **Merge commit divergence handling** (Junio Hamano, Phillip Wood, secondary concern).
Type: Feature patch series (3 patches)

Core goal: Implement `--linearize` option for `git replay` to flatten merge commits into linear history by walking only first parents, providing server-side functionality equivalent to `git rebase --no-rebase-merges`.

Technical details:
- **Patch 1/3 (refactoring)**: Introduced `put_mapped_commit()`/`get_mapped_commit()` getter/setter pair for `replayed_commits` khash OID map. No behavior change.
- **Patch 2/3 (new in v5)**: Added explanatory comments and base-selection logic fix in `pick_regular_commit()`:
  - Renamed `onto` to `default_base`, reordered parameters.
  - 10-line comment documenting base-selection logic.
  - Fixed `replayed_base` handling for `REPLAY_MODE_REVERT`.
- **Patch 3/3 (implementation)**: Core `--linearize` functionality:
  - CLI/config option documented, including `--linearize`/`--revert` mutual exclusion.
  - Merge commit handling (maps to first parent).
  - Unified `pick_regular_commit()` with `replayed_base` parameter.

**Critical issues (post-merge)**:
1. **Single-branch replay regression**:
   - v5's base-selection logic **inadvertently removed the `replayed_base` mechanism** that preserved linearized history for single branches.
   - **Silent commit dropping**: Only the tip commit is replayed, intermediate commits (including merges) are lost.
   - **Johannes Schindelin confirmed** with test case (`master~2..master`), showing v5 replays only tip commit directly onto `--onto`, dropping "Git 2.55-rc2" commit entirely.
   - **Root cause**: Removal of `replayed_base` logic in `pick_regular_commit()`.

2. **Merge commit divergence handling**:
   - Current implementation silently discards one side of divergent merges (confirmed by Junio Hamano and Phillip Wood).
   - **Design limitation**: Recognized as secondary to regression but requires clarification.

3. **CLI design inconsistency** (Patrick Steinhardt):
   - `--linearize` deviates from `git rebase`'s established syntax (`--rebase-merges`, `--no-rebase-merges`, `--rebase-merges=rebase-cousins`).
   - **Johannes Schindelin's counterargument**: Rejects `git rebase` as a usability anti-pattern, advocating for **intentional divergence** with clearer option names (e.g., `--replay-merges=<mode>` instead of `--rebase-merges`).
   - **Design split**: Consistency (Patrick) vs. UX clarity (Johannes). No resolution yet.

Current status:
- **Series fully merged** to 'master' on 2026-07-15.
- **Three critical issues identified post-merge**:
  1. **Silent commit dropping regression** (highest priority, requires urgent patch).
  2. **CLI design inconsistency** (strategic concern, now a **philosophical debate**).
  3. **Merge commit divergence handling** (secondary, may be addressed in follow-up).
- **Follow-up work required**:
  - **Urgent patch** to restore `replayed_base` logic or redesign multi-branch handling.
  - **CLI design decision**: Adopt `git rebase` syntax, diverge with clearer UX, or hybrid approach (e.g., `--replay-merges` with `git rebase`-compatible modes).

Design discussion updates:
- **Johannes Schindelin**:
  - **Identified critical regression** with silent data loss, providing test case.
  - **Rejected `git rebase` CLI precedent**, arguing for **UX-driven divergence** (e.g., `--replay-merges=<mode>`).
  - Position: **Substantive** (high weight); regression must be fixed before release; CLI should prioritize clarity over consistency.
- **Patrick Steinhardt**:
  - **Raised CLI design inconsistency**, advocating for consistency with `git rebase`'s `--rebase-merges` syntax.
  - Position: **Substantive** (high weight); frames this as a **design principle** for project-wide consistency.
- **Toon Claes (author)**:
  - Delivered complete implementation addressing all v4 feedback; series now merged.
  - Expected to address regression and CLI concerns with follow-up patches.
- **Junio C Hamano (maintainer)**:
  - Approved and merged series after v5 addressed all concerns.
  - Noted minor style nit post-merge (redundant `\n` in `BUG()`).
  - Previously identified merge commit divergence edge case.
- **Phillip Wood**:
  - Confirmed merge commit divergence edge case with technical precision.

Participant positions:
- **Johannes Schindelin**:
  - **Critical regression**: Silent commit dropping in single-branch replay; must restore `replayed_base` logic.
  - **CLI design**: **Rejects `git rebase` syntax**, proposes `--replay-merges=<mode>` for clarity.
- **Patrick Steinhardt**:
  - **CLI design**: Adopt `git rebase`'s `--rebase-merges` syntax for consistency.
- **Toon Claes**: Expected to address regression and CLI concerns.
- **Junio Hamano**: Merged series; aware of regression and CLI debate.
- **Phillip Wood**: Confirmed merge commit divergence edge case.

Key technical symbols:
- Functions: `put_mapped_commit()`, `get_mapped_commit()`, `pick_regular_commit(replayed_base, default_base)`.
- Data structures: `replayed_commits` (khash OID map), `struct replay_revisions_options` (`linearize` field).
- CLI options: `--linearize`, `--revert`, `--advance`, `--onto`, **proposed: `--replay-merges=<mode>`, `--no-merges`, `--replay-cousins`** (Johannes); **`--rebase-merges`, `--no-rebase-merges`, `--rebase-merges=rebase-cousins`** (Patrick).
- Files: `replay.c`, `replay.h`, `builtin/replay.c`, `Documentation/git-replay.txt`, `t/t3650-replay-basics.sh`.
- **Regression root cause**: Removal of `replayed_base` logic in `pick_regular_commit()`.

Related work:
- Johannes Schindelin’s merge-replay series (alternative approach, topology-preserving by default).
- Patrick Steinhardt’s `git history drop` series.
- Kristoffer Haugsbakk’s documentation improvements.

The series is **fully merged** but **critically flawed** in three dimensions:
1. **Silent commit dropping regression** (highest priority, urgent patch required).
2. **CLI design inconsistency** (strategic concern, now a **philosophical debate** between consistency and UX clarity).
3. **Merge commit divergence handling** (secondary, may be addressed in follow-up).
**Johannes Schindelin’s regression report** provides a concrete test case and argues for a robust solution, while his **CLI critique** rejects `git rebase` as a usability anti-pattern. **Patrick Steinhardt’s review** elevates the CLI debate to a **project-wide consistency question**. A **follow-up patch is urgently required** to address the regression, and a **design decision** is needed to resolve the CLI inconsistency before the next release. The merge commit divergence edge case remains a secondary concern. The thread now reflects a **clash of design principles** (consistency vs. UX) that may delay resolution.