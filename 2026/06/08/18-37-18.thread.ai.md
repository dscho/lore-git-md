Git replay linearization series summary:

Subject: [PATCH 0/3] replay: introduce --linearize option
Author: Toon Claes
Status: v1 (3/3 patches complete)
Type: Feature patch series (3 patches)

Core goal: Add `--linearize` option to `git replay` that flattens merge commits into linear history, similar to `git rebase --no-rebase-merges`. This provides an alternative to Johannes Schindelin's (Dscho) separate merge-replay approach while sharing some infrastructure.

Technical details:
- Patch 1/3 refactored `replay.c` by converting `enum replay_mode` to boolean `reverse` flag and simplifying control flow
- Patch 2/3 introduced `put_mapped_commit()` helper for storing rewritten commits, creating symmetrical API with `get_mapped_commit()`
- Patch 3/3 (new) implements `--linearize` core functionality:
  - Adds CLI/config option with clear documentation
  - Skips merge commits when linearizing (maps to first parent)
  - Uses new `pick_regular_commit()` helper for linear/non-linear cases
  - Maintains existing behavior for non-linear cases
  - Explicitly disallows combining with `--revert`
  - Test coverage in t3650
- Touches `replay.c` commit mapping logic and `replay.h` option definition
- Explicitly not added to `git history` command (author considers it a poor fit)

Open questions/coordination:
- Documentation may need alignment with Kristoffer Haugsbakk's work
- Relationship to Patrick Steinhardt's `git history drop` series remains to be clarified
- Dscho's merge-replay approach remains as alternative solution

Participant positions:
- Toon Claes (author) driving the series with contributions from Dscho
- Dscho appears supportive based on collaborative development
- No other reviews received on v1 series yet

Related work:
- Dscho's merge-replay series (alternative approach)
- Kristoffer Haugsbakk's documentation work (potential conflict)
- Patrick Steinhardt's `git history drop` series (mentioned as adjacent)