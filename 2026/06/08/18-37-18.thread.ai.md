Mode: ai

Thread identity
Subject: [PATCH 0/3] replay: introduce --linearize option
Series version: v9 (v1–v9 summarized)
Originating author: Toon Claes (with preparatory work by Johannes Schindelin; authorship of patch 3/3 updated to Toon Claes in v7)
Thread type: patch series (feature addition)

Core problem or goal
Add a `--linearize` option to `git replay` that flattens merge commits, producing a linear history. The series provides a predictable, all-or-nothing flattening behavior that avoids dangling commits and ensures all replayed commits are linearized into a single topology. The goal is to offer a simpler alternative to Johannes Schindelin’s earlier merge-replay implementation (pull.2106).

Current status
v9 posted, **technically complete** and addressing all prior feedback, including the **multi-branch ambiguity** resolved in v8 and **UX terminology refinements** in v9. The series is now **restricted to single-branch usage** with `--linearize`, `--revert`, and `--advance` via the shared `set_up_branch_mode()` helper, aligning with Elijah Newren’s "minimal fix" proposal and `git cherry-pick`'s existing behavior. Junio has **removed the previous version from `next`** and will **queue v9 for review** but has **not yet merged it to `next`**, indicating it remains under active review. The **regression** in `--linearize` behavior (fixed in v7) and the **interface design debate** (resolved in favor of standalone `--linearize`) remain unchanged. The series is **ready for review** but not yet in integration testing. Potential trivial conflicts with Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series are noted.

**Primary resolved issues**:
- The **multi-branch ambiguity** with `--linearize` is resolved by disallowing its use with multiple revision ranges or `--contained`. This eliminates emergent reachability issues while preserving the option’s core functionality. The restriction now also applies to `--revert` and `--advance` modes, as Elijah Newren proposed extending the guard to prevent concatenating unrelated histories in an unpredictable order (matching `git cherry-pick`'s behavior).
- **UX terminology refinements**: Error messages and documentation now use "multiple branches" instead of "multiple revision ranges" and simplify the rationale for the single-branch restriction (e.g., "Due to current implementation limitations..." instead of a technical explanation). These changes address feedback from Justin Tobler and Elijah Newren in v8 and are finalized in v9.
- **Refactoring (patch 1/3)**: The preparatory refactoring in v9 (introducing `put_mapped_commit()` and renaming `mapped_commit()` to `get_mapped_commit()`) is **non-controversial** and serves as a clean foundation for the `--linearize` implementation. The changes are purely internal, with no behavior changes, and were previously debated in v2–v5 before settling on the current design in v5.
- **Refactoring (patch 2/3)**: The base-selection logic is now consolidated in `replay_revisions()`, reducing `pick_regular_commit()`'s parameters from five to four and improving clarity. The change is purely internal, introduces no behavior changes, and is preparatory for `--linearize`. The commit message explains the motivation: bundling base-selection logic in one place improves maintainability and reduces confusion. The patch distinguishes between regular replay (base is either the replayed parent or `onto`) and revert replay (base is always `last_commit`), with the logic now centralized in `replay_revisions()`.
- **Implementation (patch 3/3)**: The `--linearize` option is now fully implemented, with merge commits dropped and subsequent commits reparented onto the last non-merge commit. The restriction to single branches ensures no emergent reachability issues, and the updated error messages and documentation reflect the finalized UX terminology.

**Edge case handling**: The **partial merge range edge case** (Junio’s concern) remains unresolved but is **no longer blocking graduation to `next`**. The mutual exclusivity between `--linearize` and `--advance` is enforced; compatibility with `--revert` is retained but now subject to the same single-branch restriction. **UX clarity**: Toon Claes has incorporated Justin Tobler’s and Elijah Newren’s feedback to simplify the documentation’s rationale and update error message terminology. These changes are **non-blocking** and finalized in v9.

Key technical details
- New CLI option: `git replay --linearize` (implemented in patch 3/3)
- **New restrictions**: `--linearize`, `--revert`, and `--advance` cannot be used with multiple revision ranges (e.g., `topic2 topic3 topic4`) or `--contained` (error messages updated in v9 to use "branches" instead of "revision ranges").
- Internal flags: `reverse` (boolean, retained), `linearize` (boolean, introduced in patch 3/3)
- Files touched: `replay.c`, `replay.h`, `builtin/replay.c`, `Documentation/git-replay.adoc`, `t3650-replay-basics.sh`
- New helper functions: `put_mapped_commit` (static, in `replay.c`, introduced in patch 1/3), `get_mapped_commit` (static, renamed from `mapped_commit` in patch 1/3), `resolve_replay_base` (static, introduced in patch 2/3)
- Modified functions: `pick_regular_commit` (restored `replayed_base` parameter, reordered parameters for clarity), `replay_revisions` (skips merge commits when `--linearize` is active, resolves replay base upfront, uses `put_mapped_commit` and `get_mapped_commit` for commit mapping, enforces single-branch restriction for `--linearize`, `--revert`, and `--advance`)
- **Regression fix**: The `replayed_base` parameter has been restored to ensure all replayed commits are linearized into a single topology. Without it, the replay machinery loses track of the correct parent for commits following a merge, causing commit-dropping when replaying a single branch containing merge commits (e.g., `master~2..master` with `--linearize --onto master~2` now replays all commits, not just the tip).
- **Refactoring (patch 1/3)**: Introduces `put_mapped_commit()` and renames `mapped_commit()` to `get_mapped_commit()` for symmetry, encapsulating the logic for storing and retrieving rewritten commits in the `replayed_commits` map. This reduces code duplication in `replay_revisions()` and improves clarity. The change is purely internal and preparatory, with no behavior changes. Feedback from Justin Tobler and Patrick Steinhardt on earlier iterations (v2–v5) was addressed by reverting an enum-based approach in favor of this simpler design.
- **Refactoring (patch 2/3)**: Moves the logic for resolving the replay base from `pick_regular_commit()` to `replay_revisions()`, consolidating base-selection logic in one place. This improves clarity and maintainability, reduces `pick_regular_commit()`'s parameters from five to four, and prepares the ground for `--linearize`. The change is purely internal and introduces no behavior changes. The commit message explains the motivation: bundling base-selection logic in one place improves maintainability and reduces confusion about where the replay base is determined. The helper functions `put_mapped_commit` and `get_mapped_commit` (from patch 1/3) are used to maintain consistency in how replayed commits are tracked. The patch distinguishes between regular replay (where the base is either the replayed parent or `onto`) and revert replay (where the base is always `last_commit`), with the logic now centralized in `replay_revisions()`.
- **Algorithmic behavior**: `--linearize` ensures all replayed commits are flattened into a single linear history. Merge commits are dropped, and subsequent commits are reparented onto the last non-merge commit. The restriction to single branches in v8 ensures no emergent reachability issues.
- **Behavioral difference from `git rebase --no-rebase-merges`**: `git replay --linearize` drops merges and keeps only one branch (e.g., `A->X'` or `A->Y'` for a merge of `A->X` and `A->Y`), while `git rebase --no-rebase-merges` rewrites both branches and drops the merge (e.g., `A->X'->Y'` or `A->Y'->X'`). This difference is intentional, documented, and tested.
- **Interface design**: The series implements `--linearize` as a standalone flag, diverging from `git rebase`'s `--rebase-merges=<mode>` syntax. The commit message justifies this divergence by arguing that `--linearize` better describes the predictable, all-or-nothing flattening behavior and that mirroring `git rebase`'s syntax would repeat past UX mistakes.
- **Compatibility**: `--linearize` and `--revert` can be used together (mutual exclusivity removed in v7), but both are now subject to the **single-branch restriction**. **Mutual exclusivity with `--advance` is enforced**; documented in the man page.
- **Edge case handling**: root commits are explicitly replayed (merges are dropped); the if/else structure distinguishes merges, roots, and regular commits; `last_commit` handling during merge linearization is documented with clarifying comments. The **partial merge range edge case** (Junio’s concern) is not yet covered by tests.
- Base commit: `ab776a62a785`
- Change-Id: `20260604-toon-git-replay-drop-merges-807fa008d395`
- Subsystems: replay machinery, sequencer internals
- Test coverage: Thorough, including cases for replaying to the root commit, merge commit rejection, `--linearize` with `--advance`, `--revert`/`--linearize` compatibility (now restricted to single branches), replaying multiple divergent branches (now restricted), and verifying the behavioral difference from `git rebase --no-rebase-merges`. The regression scenario (`master~2..master` with `--linearize --onto master~2`) is covered. New tests verify the multi-range and `--contained` restrictions, and error messages now use "branches" instead of "revision ranges". Documentation explicitly notes the behavioral difference from `git rebase` and the new restrictions.

Open questions and loose ends
- **Edge case**: Whether the replay machinery can safely handle the edge case where `--linearize` and `--revert` are combined on a partial merge range (e.g., a range that includes a merge commit (`M`) and one side of the merge (`A`), but excludes the other side (`B`) and the merge base (`O`)). Junio has cautioned that the current implementation may not reliably handle this scenario, but the issue is **no longer blocking graduation to `next`**.
- **Future work**: Whether `--linearize` should eventually support per-branch linearization (e.g., via `--ref` syntax). This is now deferred, with the v8 restriction serving as an interim solution. Toon’s `--ref` proposal (requiring `--ref <ref>` when `--linearize` is used) remains a potential future direction but is not implemented in v9.
- Whether `--linearize` should also be added to `git history` (author sees no clear use case but invites feedback).
- Potential trivial conflicts with Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series.

Participant positions
- **Toon Claes**: Author; implemented the feature, wrote tests and documentation, and addressed all prior feedback. **Updated position**: Restricted `--linearize`, `--revert`, and `--advance` to single-branch usage in v8 via `set_up_branch_mode()`, resolving the multi-branch ambiguity. Incorporated Justin Tobler’s and Elijah Newren’s feedback in v9 to simplify the documentation’s rationale (e.g., "Due to current implementation limitations...") and update error messages to use "branches" instead of "revision ranges". **Edge case**: Flags `--contained` as incompatible with `--linearize` due to similar reachability issues; the combination is now disallowed.
- **Johannes Schindelin**: Co-author of preparatory patch; Signed-off-by retained on patches 1/3 and 2/3. **Substantive review of v5–v9**:
  - Identified the regression in `--linearize` behavior and explained why the `replayed_base` parameter is necessary.
  - **Interface design**: Strongly opposes mirroring `git rebase`'s `--rebase-merges=<mode>` syntax, calling it user-hostile and advocating for distinct option names that reflect the "replay" concept. Supports the explicit justification in the commit message.
  - **Authorship**: Explicitly disclaimed authorship of patch 3/3 in v7, supporting Toon Claes as the primary author.
- **Junio C Hamano**: Surface-level review on v1–v9, focusing on typos, logic clarity, test coverage, and documentation presentation. Noted minor style nits in v5 (BUG message capitalization, trailing newline). **Substantive contribution**: Clarified the behavioral difference between `git replay --linearize` and `git rebase --no-rebase-merges`, proposing a test to lock in the current semantics. The test was added in v6. **Feedback addressed in v7–v9**:
  - Removed mutual exclusivity between `--linearize` and `--revert`, allowing them to be used together.
  - Updated authorship of patch 3/3 to Toon Claes, addressing the procedural nit about attribution.
  - **Edge case**: Raised the partial merge range edge case with `--linearize` and `--revert`, cautioning that the current implementation may not reliably handle such cases. The issue is **no longer blocking graduation to `next`**.
  - **Multi-branch ambiguity**: Defended the status quo in v7 but did not object to the v8 restriction. **Latest position**: Removed the previous version from `next` and will queue v9 for review but has not yet merged it to `next`, indicating it remains under active review.
- **Justin Tobler**: **Substantive review of patch 1 in v2** (enum-to-bool refactoring concerns, resolved in v5) and **substantive review of patch 3 in v8–v9**:
  - **v8 3/3**: Suggested simplifying the documentation’s rationale for the single-branch restriction (e.g., "multiple branches are not supported" instead of a technical explanation) and improving error message terminology ("revision ranges" → "branches"). These suggestions were **accepted by Toon Claes** and incorporated in v9.
  - **v9 1/3 and 2/3**: Acknowledged both refactorings as non-controversial and clean foundations for the `--linearize` feature. No objections raised.
  - **v9 3/3**: Confirmed the implementation is technically complete and addresses all prior feedback, including the UX terminology refinements.
- **Elijah Newren**: **Substantive review of patch 3 in v2 and v7–v9**:
  - **v2**: Focused on implementation clarity, edge cases, and test coverage. Suggested clarifying comments and additional test cases (e.g., `--advance` compatibility, `--revert`/`--linearize` incompatibility); all suggestions incorporated in v3–v9.
  - **v7**: Identified the **multi-branch ambiguity** with `--linearize`, arguing that the current behavior violates the principle of least astonishment and is inconsistent with other `git replay` modes. **Latest position**: Proposed the "minimal fix" (restricting `--linearize` to single branches) as an interim solution, with per-branch linearization deferred. The v8 restriction aligns with this proposal, resolving the ambiguity while preserving flexibility for future work. **New detail**: Proposed extending the single-branch restriction to `--revert` and `--advance` modes to prevent concatenating unrelated histories in an unpredictable order (matching `git cherry-pick`'s behavior). This has been **implemented in v8** via `set_up_branch_mode()` and remains unchanged in v9.
- **Patrick Steinhardt**: Substantive review of patch 1 in v4, questioning the enum-to-bool refactoring on readability and extensibility grounds; feedback led to the refactoring’s reversion in v5. **Substantive review of patch 3 in v4–v5**:
  - Approved the core logic (skipping merge commits when `--linearize` is active).
  - Proposed adopting `git rebase`'s `--rebase-merges=<mode>` syntax for consistency.
  - **Updated position**: Acknowledged that consistency with `git rebase` is not mandatory but requires explicit justification for divergence. This requirement has been met in v6–v9.
- **Phillip Wood**: Substantive review of the behavioral difference between `git replay --linearize` and `git rebase --no-rebase-merges`, clarifying the algorithmic difference and reinforcing the need for documentation and test coverage. No objections to the implementation itself. The behavioral difference is now accepted as intentional and documented.

Related work
- Johannes Schindelin’s earlier merge-replay implementation (pull.2106), which this series aims to supersede.
- Patrick Steinhardt’s `git history drop` series (potential trivial conflicts).
- Kristoffer’s documentation changes (potential trivial conflicts).