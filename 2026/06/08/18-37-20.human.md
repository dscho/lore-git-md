This is the second patch in Toon Claes' series adding a `--linearize` option to `git replay`. It's a refactoring patch that extracts commit mapping logic into a new helper function.

The patch introduces `put_mapped_commit()` to handle storing commit mappings in the `replayed_commits` hashmap, cleaning up the main `replay_revisions()` function. The change is mechanical - it moves existing code into a dedicated function while maintaining the same behavior. The helper joins its counterpart `get_mapped_commit()` (renamed from `mapped_commit` in this patch) to provide symmetric operations for the commit mapping functionality.

This is a straightforward code organization improvement that prepares for the series' final patch implementing the linearization feature. The changes are limited to `replay.c` and don't introduce new behavior, keeping the focus on maintainability improvements before adding new functionality.