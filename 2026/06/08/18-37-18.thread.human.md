This thread is about Toon Claes’s `[PATCH v5 0/3]` series introducing a `--linearize` option to `git replay`. The feature flattens merge commits into linear history by walking only first parents, providing server-side functionality equivalent to `git rebase --no-rebase-merges`. After five iterations and thorough review, the series is now complete and ready for merging.

The v5 patches address all feedback from earlier rounds. Patch 1/3 refactored internal helpers to improve code clarity without changing behavior. Patch 2/3, new in v5, added detailed explanatory comments and fixed a subtle base-selection logic error in `pick_regular_commit()`, resolving architectural concerns raised by Junio Hamano and Patrick Steinhardt. Patch 3/3 implements the core `--linearize` functionality, including merge commit handling, CLI/config documentation, and expanded test coverage for edge cases like root commits and `--onto` scenarios.

Key participants include Toon Claes (author), Junio Hamano (maintainer), Johannes Schindelin (Dscho), Patrick Steinhardt, Justin Tobler, and Elijah Newren. Junio’s “Will merge to ‘next’” approval is now actionable, and all prior objections—including interface scope and callsite clarity—have been resolved. The series reconciles with Johannes Schindelin’s merge-replay approach while offering an alternative implementation, and the test suite now verifies patch preservation via `git range-diff`. No open questions remain.