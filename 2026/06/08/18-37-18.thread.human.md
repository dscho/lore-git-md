Mode: human

The thread introduces a `--linearize` option for `git replay`, allowing users to flatten merge commits into a linear history. The feature, now in its ninth iteration (v9), is authored by Toon Claes with preparatory work by Johannes Schindelin. After extensive review and refinement, the series has reached technical completion, addressing all prior feedback, including a critical multi-branch ambiguity resolved in v8 by restricting `--linearize` (and related modes `--revert` and `--advance`) to single-branch usage.

Key participants include Elijah Newren, who recently gave the series his **Reviewed-by**, signaling readiness for integration, and Junio C Hamano, who removed the previous version from the `next` branch and will queue v9 for review. Other reviewers—Johannes Schindelin, Justin Tobler, Patrick Steinhardt, and Phillip Wood—have weighed in on interface design, edge cases, and behavioral differences from `git rebase --no-rebase-merges`, all of which are now documented and tested. The v9 update focuses on UX polish, simplifying error messages and documentation to use "multiple branches" instead of "multiple revision ranges" and clarifying the rationale for the single-branch restriction.

The series is structured as three patches: two preparatory refactorings (introducing `put_mapped_commit()` and consolidating base-selection logic) and the core `--linearize` implementation. The feature is now restricted to single branches to avoid emergent reachability issues, and it enforces mutual exclusivity with `--advance` while remaining compatible with `--revert`. Test coverage is thorough, including edge cases like replaying to the root commit and verifying the behavioral difference from `git rebase`. The only remaining open question is whether `--linearize` should eventually support per-branch linearization, but this is deferred for future work. The series is unblocked and awaits Junio’s integration into `next`.