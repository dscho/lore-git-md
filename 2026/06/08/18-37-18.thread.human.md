Here’s the updated **human-mode** summary for a developer catching up on the thread:

---

The thread introduces a `--linearize` option for `git replay` that flattens merge commits into a linear history. The feature is now technically complete in its seventh iteration (v7), with all prior feedback addressed, and has been queued in Junio’s `next` branch for integration testing. The core behavior is straightforward: when replaying commits with `--linearize`, merges are dropped, and all commits are rewritten into a single linear chain. This differs from `git rebase --no-rebase-merges`, which rewrites both sides of a merge without flattening, and is intended as a simpler alternative to Johannes Schindelin’s earlier merge-replay implementation.

The series has one major unresolved design question: what should happen when `--linearize` is used with multiple branches? The current implementation flattens all branches into a single linear history, concatenating them in an order determined by Git’s revision walk. Junio C Hamano defends this as a reasonable and intentional behavior—users who want to avoid concatenation can invoke `git replay` separately for each branch—while Elijah Newren argues it violates the principle of least astonishment and creates a recoverability problem: untangling concatenated branches is harder than concatenating independent ones. Elijah proposes restricting `--linearize` to a single branch as a minimal fix, with per-branch linearization deferred. Junio has pushed back on this framing, insisting the current behavior is a valid design choice, but has left the decision to the author, Toon Claes. The series is now on hold in `next` until Toon responds.

Other technical details include a regression fix in v7 that ensures all replayed commits are linearized into a single topology (previously, replaying a single branch containing merges could drop commits), and the removal of mutual exclusivity between `--linearize` and `--revert`. The interface uses `--linearize` as a standalone flag, diverging from `git rebase`'s `--rebase-merges=<mode>` syntax, with the divergence justified in the commit message. The series touches the replay machinery, sequencer internals, and adds thorough test coverage, though the multi-branch ambiguity remains untested. Potential trivial conflicts with Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series are noted.

---