The `git replay --linearize` series has reached its sixth iteration and is now poised for re-merge after addressing all critical post-merge issues from the flawed v5. Originally merged as a feature to flatten merge commits into linear history, v5 introduced a silent regression that dropped commits in single-branch scenarios, along with design ambiguities around merge commit handling and CLI naming. Toon Claes’s v6 fixes the regression by restoring the `replayed_base` logic, documents the intended behavior (including edge cases like divergent merges), and justifies the `--linearize` naming in both commit messages and the `git-replay` man page.

Junio C Hamano’s recent review raises two process questions and one documentation nit. He asks whether Toon should now be the sole author, given that the bulk of v6’s work—regression fixes, expanded test coverage, and design clarifications—was his, while the patch still carries Johannes Schindelin as a co-author. Junio also accepts the `--linearize` design as documented but requests either a rationale or a relocation of the blanket incompatibility with `--revert` to a LIMITATIONS section. These are minor points; the series remains ready for re-merge once addressed. No further design changes are proposed, and the philosophical debate over CLI consistency versus UX clarity is now documented rather than contested. The thread is effectively converged, awaiting final documentation tweaks before Junio picks it up again.