Mode: human

The `git replay --linearize` patch series, now at version 8, introduces an option to flatten merge commits into a linear history. Authored by Toon Claes with preparatory work from Johannes Schindelin, the series has evolved through eight iterations to address all prior feedback, including a critical multi-branch ambiguity that had blocked its progress in Junio’s `next` branch. The latest version restricts `--linearize` to single-branch usage, ensuring predictable behavior and resolving the last major design concern raised by Elijah Newren and Junio.

The series is technically complete and ready for review, with all substantive objections addressed. Junio has removed the previous version from `next` and will queue the new iteration, though he has not yet merged it into `next`, indicating a cautious approach to ensure no further issues arise. The core functionality—flattening merges while avoiding dangling commits—is now well-defined, with clear restrictions (e.g., disallowing `--linearize` with multiple revision ranges or `--contained`) and thorough test coverage. The behavioral difference from `git rebase --no-rebase-merges` is intentional and documented.

Open questions remain about edge cases, such as combining `--linearize` with `--revert` on partial merge ranges, but these are no longer blocking graduation. Future work may explore per-branch linearization, though the current implementation serves as a minimal, stable foundation. The series is expected to unblock the hold in `next` once Junio’s final review is complete.