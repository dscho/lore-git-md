Mode: human

The thread introduces a `--linearize` option for `git replay`, allowing users to flatten merge commits into a linear history. The feature, now in its seventh iteration (v7), was originally proposed by Toon Claes with preparatory work by Johannes Schindelin. After addressing all prior feedback—including a regression fix for single-branch replay scenarios and interface design debates—the series is now technically complete and has been queued in Junio C Hamano’s `next` branch for integration testing.

Key participants include Toon Claes (author), Johannes Schindelin (co-author of preparatory patches), and Junio (maintainer). The series diverges from `git rebase`'s `--rebase-merges` syntax, opting for a standalone `--linearize` flag to emphasize its predictable, all-or-nothing flattening behavior. While mutual exclusivity between `--linearize` and `--revert` was removed in v7, Junio previously raised an edge case where combining these options on a partial merge range (e.g., replaying a merge commit and only one of its parents) could produce undefined behavior. This concern remains unresolved but is no longer blocking progress.

The implementation touches the replay machinery, sequencer internals, and documentation, with thorough test coverage for edge cases like root commits and multi-branch replay. The series is now cooking in `next`, with no major open questions beyond the lingering edge case. Potential trivial conflicts with other in-flight topics (e.g., documentation changes or Patrick Steinhardt’s `git history drop` series) may require minor adjustments before final graduation to `master`.