Here’s the **human-mode** summary for a developer catching up on the thread:

---

This thread introduces a `--linearize` option for `git replay`, letting users flatten merge commits into a linear history. The feature is now in its ninth iteration (v9), with Toon Claes as the primary author, building on preparatory work by Johannes Schindelin. The goal is to provide a simpler alternative to Johannes’s earlier merge-replay implementation, offering predictable, all-or-nothing flattening that avoids dangling commits.

The series is technically complete and addresses all prior feedback, including a critical fix in v7 for a regression that could drop commits when replaying a branch with merges. A key design decision in v8 restricted `--linearize` (and now also `--revert` and `--advance`) to single-branch usage, resolving an ambiguity where multiple branches could produce unpredictable results. The interface uses a standalone `--linearize` flag, diverging from `git rebase`'s `--rebase-merges` syntax but justified as clearer for the replay use case. Junio has removed the previous version from his `next` branch and will queue v9 for review, but it hasn’t yet been merged, so the series remains under active consideration.

Reviews have been substantive and largely supportive. Elijah Newren identified the multi-branch ambiguity and proposed the single-branch restriction, while Justin Tobler and others refined error messages and documentation to improve clarity. Phillip Wood and Junio clarified the behavioral difference from `git rebase --no-rebase-merges` (which rewrites both sides of a merge, unlike `--linearize`). The only unresolved edge case—whether the machinery can safely handle partial merge ranges with `--linearize` and `--revert`—is no longer blocking integration. Future work might explore per-branch linearization, but for now, the series is ready for broader testing.

---