Here’s the updated **human-mode** summary for a developer catching up on the thread:

---

This thread introduces a `--linearize` option for `git replay` that flattens merge commits into a linear history, offering a simpler alternative to Johannes Schindelin’s earlier merge-replay implementation. The feature is technically complete in its seventh iteration (v7), addressing all prior feedback and fixing a regression where replaying a single branch with merge commits would drop commits. The series is queued in `next` for integration testing, but a key design debate remains unresolved: how `--linearize` should handle multiple branches.

The current implementation flattens all replayed commits into a single linear chain, even when multiple independent branches are provided. Junio C Hamano defends this behavior, arguing that users can avoid unintended concatenation by invoking `git replay` separately for each branch. He also suggests syntax alternatives, like restricting the command to a single negative ref plus one or more positive refs, to reduce ambiguity. Elijah Newren, however, argues that the current behavior violates the principle of least astonishment, ignores command-line order, and creates a recoverability asymmetry—untangling concatenated branches is harder than concatenating them manually. He proposes restricting `--linearize` to a single positive ref as a minimal fix, with per-branch linearization deferred to a later patch.

The discussion has shifted from technical correctness to design philosophy, with Toon Claes (the author) yet to respond to the latest arguments. The series is otherwise ready for merging, with thorough test coverage and documentation, but the multi-branch ambiguity remains the primary blocker. Other minor concerns, like edge cases with `--revert` and `--linearize` on partial merge ranges, are no longer blocking graduation to `next`.

---