Here’s the updated human-readable summary for a developer catching up on the thread:

---

The `git replay --linearize` series, originally merged as v5, introduced a critical regression where commits were silently dropped when replaying multiple branches. Toon Claes has now posted v6 (three patches) to fix this and clarify the feature’s design. The series is now a bugfix and documentation update, ready for re-review and re-merge to replace the flawed v5.

The regression occurred because v5 accidentally removed the `replayed_base` logic, causing only the tip commit to be replayed in single-branch scenarios. Patch 2/3 in v6 restores this logic by refactoring base resolution outside `pick_regular_commit()`, ensuring all commits in the input range are replayed. The fix is paired with expanded test coverage to verify edge cases, including divergent merges and multiple branches. Patch 3/3 documents the design intent: `--linearize` produces a *single linear sequence* regardless of input branches, even if this duplicates shared history. This behavior is intentional and extends the feature’s scope beyond merge commits, though it diverges from `git rebase --rebase-merges` syntax. The commit message now justifies this UX choice, addressing Patrick Steinhardt’s demand for explicit rationale.

Johannes Schindelin identified the regression and advocated for clarity over consistency with `git rebase`. Phillip Wood confirmed edge cases around merge commit divergence, which are now documented and tested. Junio C Hamano, who merged v5, is likely to merge v6 as a bugfix. The philosophical debate over CLI consistency remains unresolved but is now documented in the commit messages. No further design changes are proposed—this is purely a fix for the regression and a documentation update.