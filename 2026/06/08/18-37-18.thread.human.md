The `git replay --linearize` feature is now in its sixth iteration, with Toon Claes as the driving force. The series adds a `--linearize` option that flattens merge commits into a single linear history, offering a simpler alternative to Johannes Schindelin’s earlier merge-replay work. After addressing all prior feedback, including a regression fix that ensures all replayed commits are properly linearized, the series is technically complete and ready for review.

The main debate—whether to mirror `git rebase`'s `--rebase-merges=<mode>` syntax or use a standalone `--linearize` flag—has been settled in favor of the latter, with an explicit justification in the commit message. Johannes Schindelin and Toon Claes argue that `--linearize` better reflects the all-or-nothing flattening behavior, while Patrick Steinhardt, though initially preferring consistency with `git rebase`, accepted the divergence after the reasoning was documented. The behavioral difference between `git replay --linearize` and `git rebase --no-rebase-merges` is now clearly documented and tested.

Junio C Hamano’s latest review raises minor documentation points: whether Toon should take over authorship from Johannes given the extent of changes, and how to present the `--linearize`/`--revert` incompatibility. These are surface-level nits and don’t affect the code’s readiness. The series remains under review, with no merge status reported. Open questions include whether `--linearize` should be added to `git history` and potential trivial conflicts with other in-flight series.