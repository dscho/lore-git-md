Here’s the updated human-mode summary for a developer catching up:

The thread introduces a `--linearize` option for `git replay` that flattens merge commits into a linear history, offering a simpler alternative to Johannes Schindelin’s earlier merge-replay work. Authored by Toon Claes (with preparatory patches from Schindelin), the series is now at v8, which restricts `--linearize` to single-branch usage to avoid emergent reachability issues—a change driven by Elijah Newren’s "minimal fix" proposal. This resolves the last major blocker that had kept the series out of Junio’s `next` branch, though Junio has not yet queued v8 for integration testing.

Key participants include Toon (author), Elijah Newren (who identified the multi-branch ambiguity and proposed the restriction), Johannes Schindelin (co-author of preparatory patches and advocate for distinct option names), and Justin Tobler (who raised UX concerns about documentation clarity). The series is technically complete, with all prior feedback addressed, including a regression fix for commit-dropping in partial merge ranges. The behavioral difference from `git rebase --no-rebase-merges` is intentional, documented, and tested. Open questions are minor: whether `--linearize` should eventually support per-branch linearization (deferred), and whether the restriction should also apply to `--revert` (a new suggestion from Elijah). Junio’s latest move—removing the prior version from `next`—signals readiness for review, but the series remains under active discussion.