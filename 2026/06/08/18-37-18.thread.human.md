Mode: human

The Git mailing list thread for Toon Claes's `[PATCH 0/3] replay: introduce --linearize option` has reached its eighth iteration (v8) and is now technically complete, addressing all prior feedback. The series adds a `--linearize` flag to `git replay` that flattens merge commits into a linear history, offering a simpler alternative to Johannes Schindelin's earlier merge-replay implementation.

Key progress in v8 includes resolving the long-standing **multi-branch ambiguity** by restricting `--linearize` to single-branch usage, which aligns with Elijah Newren's "minimal fix" proposal and removes the blocker for inclusion in Junio's `next` branch. The series also retains fixes for a regression in `--linearize` behavior (from v7) and maintains the interface design choice to use a standalone `--linearize` flag rather than mirroring `git rebase`'s syntax. All substantive review feedback—from Johannes Schindelin, Elijah Newren, Patrick Steinhardt, and Phillip Wood—has been incorporated, and the series is now ready for final review.

The implementation touches the replay machinery in `replay.c`, adds thorough test coverage (including edge cases like root commits and merge rejection), and documents the behavioral difference from `git rebase --no-rebase-merges`. One edge case remains unresolved: combining `--linearize` and `--revert` on a partial merge range may not be handled reliably, but this is no longer considered a blocker. The series also notes potential trivial conflicts with Kristoffer's documentation changes and Patrick Steinhardt's `git history drop` work.

With the multi-branch restriction in place, the series should now graduate to `next` without further delays. The only open questions are whether `--linearize` should eventually support per-branch linearization (deferred for now) and whether it should be added to `git history` (no clear use case yet). The thread has seen broad engagement from core contributors, and the current implementation is stable and well-tested.