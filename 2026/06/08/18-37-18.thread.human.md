Here’s the human-mode summary for a developer catching up on this thread:

---

This thread is about Toon Claes’s three-patch series introducing a `--linearize` option to `git replay`, which flattens merge commits into linear history by walking only first parents—essentially replicating `git rebase --no-rebase-merges` on the server side. The series is now complete in its third version (v3) and ready for merging, with all substantive feedback incorporated.

The implementation touches `replay.c`, `replay.h`, the man page, and the test suite (`t3650-replay-basics.sh`). Key changes include a refactored boolean `reverse` flag (replacing an enum), a new `put_mapped_commit()`/`get_mapped_commit()` getter/setter pair for the `replayed_commits` OID map, and the core `--linearize` logic, which drops merge commits while cherry-picking regular commits. The test suite now covers edge cases like root commits, `--linearize`/`--advance` combinations, and mutual exclusion with `--revert`.

The series has received thorough review from Junio Hamano, Elijah Newren, Justin Tobler, and Johannes Schindelin (who signed off on all patches). Feedback addressed includes test improvements, documentation clarifications, and architectural refinements (e.g., the boolean vs. enum debate). No outstanding technical concerns remain, and the implementation is well-documented and tested. Toon has also offered to reconcile this work with Johannes Schindelin’s earlier merge-replay series if needed.

---