The `git replay --linearize` series, which introduced a `--linearize` option to flatten merge commits into a linear history, was merged to `master` on July 15, 2026, but has since revealed three critical issues. The most urgent is a **silent commit-dropping regression** in single-branch replay scenarios, where intermediate commits (including merges) are lost, leaving only the tip commit replayed directly onto `--onto`. Johannes Schindelin confirmed this with a test case, and Toon Claes, the author, acknowledged the regression, agreeing that `--linearize` should produce a single linear sequence regardless of input branches—a design choice intended to ensure predictable behavior. A follow-up patch is now required to restore the `replayed_base` logic or redesign multi-branch handling, along with a documentation update to clarify the feature’s behavior.

The second issue is a **CLI design inconsistency** with `git rebase`, where `--linearize` deviates from the established `--rebase-merges` syntax. Patrick Steinhardt initially argued for consistency but conceded the UX argument after Johannes Schindelin rejected `git rebase` as a usability anti-pattern, advocating instead for clearer option names like `--replay-merges=<mode>`. The debate has since shifted to a **philosophical and process-level discussion**, with Patrick now insisting that any divergence from established CLI patterns must be **explicitly justified in commit messages** to avoid arbitrary inconsistency. Toon Claes defended the current `--linearize` name as appropriate for the feature’s scope but agreed that clearer documentation is needed.

The third issue, raised by Junio Hamano and Phillip Wood, involves **merge commit divergence handling**, where the current implementation silently discards one side of divergent merges. Toon’s latest reply clarifies that this behavior is intentional—`--linearize` produces a single linear sequence, even if it means duplicating shared history—but acknowledges it may surprise users. He proposed workarounds (e.g., `--ref` or peeling commits) but noted they don’t fully address the core issue. A follow-up patch will document this behavior and add test cases, though the fundamental design trade-off remains unresolved.

The thread now reflects a **clash of design principles** (predictability vs. consistency) and a **new process-level expectation** for decision documentation. While the series is merged, the regression and CLI debate demand urgent attention, with Schindelin’s regression report providing a concrete test case and Patrick’s insistence on justification setting a precedent for future CLI changes. The merge commit divergence issue, while secondary, further underscores the need for clearer user guidance. A follow-up patch is imminent, but the philosophical questions about UX and consistency may linger.