Here's a human-readable summary of this thread:

Toon Claes has proposed adding a `--linearize` option to `git replay` that would flatten merge commits, similar to `git rebase --no-rebase-merges`. The 3-patch series includes preparatory refactoring (simplifying control flow and adding helper functions) before implementing the core functionality. Johannes Schindelen (Dscho) collaborated on the implementation, suggesting it builds on prior merge-replay work.

The new option rewrites merge commits as single-parent linear history by walking only first parents during replay. The changes are focused in `replay.c`'s commit mapping logic. Notably, this feature is being added specifically to `git replay` rather than the newer `git history` command, as the author feels it better fits `replay`'s use cases.

The cover letter notes potential coordination needed with Dscho's separate merge-replay work and mentions documentation conflicts with Kristoffer Haugsbakk's and Patrick Steinhardt's ongoing series, though these are considered minor. Test coverage has been added in t3650.