Here’s the updated **human-mode** summary for a developer catching up on the thread:

---

The thread introduces a `--linearize` option for `git replay`, allowing users to flatten merge commits into a linear history. The feature is now in its seventh iteration (v7), with Toon Claes as the primary author after extensive revisions to address feedback. The goal is to provide a simpler, more predictable alternative to Johannes Schindelin’s earlier merge-replay implementation, ensuring all replayed commits form a single linear topology without dangling commits.

The series is technically complete, with all prior feedback incorporated. Key changes in v7 include fixing a regression where `--linearize` would drop commits when replaying a single branch containing merges (restoring the `replayed_base` parameter to maintain correct parentage) and removing the mutual exclusivity between `--linearize` and `--revert`, allowing them to be used together. The interface design debate—whether to mirror `git rebase`'s `--rebase-merges=<mode>` syntax—has been resolved in favor of `--linearize` as a standalone flag, justified in the commit message as better reflecting the all-or-nothing flattening behavior. The behavioral difference from `git rebase --no-rebase-merges` (which rewrites both branches and drops the merge) is intentional, documented, and tested.

Reviews have been substantive and supportive. Johannes Schindelin identified the regression and advocated for the standalone `--linearize` flag, while Junio C Hamano clarified the behavioral difference and suggested tests to lock in the semantics. Phillip Wood and Patrick Steinhardt engaged with the interface design, with Patrick initially proposing consistency with `git rebase` but accepting the divergence once it was justified. Elijah Newren and Justin Tobler contributed technical feedback on edge cases and implementation clarity, all of which has been addressed. The series touches `replay.c`, `replay.h`, `builtin/replay.c`, documentation, and tests, with thorough coverage for edge cases like root commits, merge handling, and compatibility with other flags.

The only open questions are minor: whether `--linearize` should also be added to `git history` (Toon sees no clear use case but invites feedback) and potential trivial conflicts with other in-flight series. The series is ready for final review and integration, with no unresolved technical objections.

---