Here’s the human-mode narrative summary for a developer catching up on the thread:

---

This thread is about fixing and documenting the `--linearize` option for `git replay`, which flattens merge commits into a linear history. The feature was merged in v5 but had critical regressions—most notably, it silently dropped commits in single-branch scenarios and failed to document how it handled divergent merges. Toon Claes (with early input from Johannes Schindelin) posted v6 on July 20 to address these issues, restoring the `replayed_base` logic, clarifying the design intent, and expanding test coverage. The series is now a bugfix and documentation update rather than a new feature.

Junio C Hamano reviewed v6 and raised two process-level concerns: whether Toon should be the sole author of the final patch (given his extensive v6 work) and whether the documented incompatibility between `--linearize` and `--revert` needed a clearer rationale. Toon’s latest reply resolves both. He’ll take sole authorship in v7, acknowledging Schindelin’s original work with a `Based-on-patches-by` trailer, and has confirmed that `--linearize` and `--revert` can safely be combined, removing the restriction entirely. The core design—producing a single linear sequence even if it duplicates shared history—remains unchanged, and the philosophical debate about CLI consistency with `git rebase` is documented but unresolved.

The series is ready for re-merge once v7 is posted. All critical issues are fixed, and the remaining work is purely documentation and process cleanup. Phillip Wood and Patrick Steinhardt contributed substantive reviews, while Junio’s feedback focused on accountability and clarity. No further design changes are expected.

---