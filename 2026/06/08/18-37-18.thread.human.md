Here’s the updated **human-mode** summary for a developer catching up on the thread:

---

The thread is about adding a `--linearize` option to `git replay` that flattens merge commits into a linear history. The feature is technically complete in its seventh iteration (v7), with all prior feedback addressed, and is currently queued in Junio’s `next` branch for integration testing. The core debate has shifted from implementation details to **design philosophy**: how `--linearize` should handle multiple branches.

The current behavior flattens all provided branches into a single linear chain, concatenating them in an order determined by Git’s revision walk—not the user’s command-line order. Junio argues this is acceptable for cases like octopus merges where order doesn’t matter, and users who want separate linearized branches can simply run `git replay` multiple times. Elijah Newren, however, calls this a **usability trap**, pointing out that the command ignores command-line order, creates unpredictable topologies, and makes it hard to recover from unintended concatenations. He proposes restricting `--linearize` to a single branch (aligning with `--advance`/`--revert`) as a minimal fix, preserving future flexibility.

Junio’s latest email doubles down on the status quo, framing the issue as a **user responsibility** rather than a design flaw. He suggests two syntax alternatives—persistent state or a more restrictive CLI—but leans toward leaving things as-is, provided users understand the limitations. The discussion now hinges on whether the project prioritizes **flexibility** (Junio’s view) or **predictability** (Elijah’s). Toon Claes, the author, has yet to respond to this latest exchange, but Elijah’s arguments make a strong case for restricting the interface, at least temporarily.

The series also fixed a regression in v7 where replaying a single branch with merges (e.g., `master~2..master --linearize`) would drop commits, and removed the mutual exclusivity between `--linearize` and `--revert`. A lingering edge case—combining `--linearize` and `--revert` on a partial merge range—remains unresolved but isn’t blocking graduation. The implementation touches `replay.c`, adds tests, and diverges from `git rebase`'s `--rebase-merges` syntax, with the commit message justifying the choice as more intuitive for the "replay" use case.

Key participants: Toon Claes (author), Junio C Hamano (maintainer), Elijah Newren (reviewer), and Johannes Schindelin (preparatory work). The thread is now focused on **usability trade-offs**, with no clear consensus yet on the multi-branch behavior.

---