This is a performance optimization patch for ref-filter's `--contains`/`--no-contains` operations, now split into two parts based on reviewer feedback. The patch series builds on prior work to unify and dramatically speed up commit ancestry checking across Git commands.

In this follow-up email, Tamir Duberstein responds to Karthik Nayak's review by splitting the original patch into two logical changes:

1. First fixes the existing tag-specific algorithm to properly handle cyclic graphs (introduced by replacement refs) by adding CONTAINS_IN_PROGRESS state and falling back to cycle-safe `repo_is_descendant_of()` when cycles are detected.

2. Then extends this improved algorithm to all ref-filter callers (branch, for-each-ref etc.), removing the special-case tag implementation and showing substantial performance gains (up to 223x faster in benchmarks with many refs).

The response also addresses Karthik's test organization suggestions by:
- Moving the ref-count verification from setup to the actual perf test
- Adding explicit tests for cycle handling and error cases

The technical approach remains unchanged from the original patch - it still:
- Caches traversal results across reference checks
- Makes ancestry parsing failures consistently fail
- Maintains the same dramatic performance improvements

This appears to be the final iteration before potential inclusion, with all substantive review feedback addressed and only minor organizational nits remaining. The split into two patches makes the changes easier to review while preserving the optimization's benefits.