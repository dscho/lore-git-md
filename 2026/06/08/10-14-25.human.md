This is a utility patch adding `strtoul_ul()` to `git-compat-util.h` as part of a larger security-focused series enabling remote object metadata queries. The function provides error-checked string-to-unsigned-long conversion, filling a gap in Git's existing string conversion utilities.

The patch is straightforward - it follows the same pattern as `strtoul_ui()` but returns an unsigned long instead of unsigned int. The implementation includes proper error checking for negative values, invalid characters, and overflow via errno. The commit message explains this is needed by a subsequent change in the series.

As the second patch in a 12-part series that has already undergone extensive review, this appears to be a well-contained preparatory change. The implementation matches Git's existing style for similar functions and includes the same safety checks. Since the series has reached v12 with core maintainer approval, we can assume this utility function meets the project's standards despite its simplicity.