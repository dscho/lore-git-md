This is a brief follow-up from D. Ben Knoble in the ongoing discussion about `git history reword` behavior. The email responds to Junio's earlier critique of the proposed "abort on unchanged message" behavior by noting that Vim users can already achieve this by exiting the editor with an error code (:cquit). Knoble raises the question of whether there should be a more explicit way to signal this intent across all editors, though he doesn't propose a specific solution.

The exchange highlights a practical workaround that some users already employ, while leaving open the larger design questions Junio raised about whether the default behavior should change (with a `--force` option) versus maintaining the status quo where even unchanged messages rewrite the commit with new timestamps. Knoble's comment doesn't take a position on the broader debate, but provides useful context about current editor integration behavior.