This is the second patch in Toon Claes' 3-part series adding a `--linearize` option to `git replay`. The patch refactors `replay.c` by extracting commit mapping logic into a new helper function `put_mapped_commit()`, which handles storing rewritten commits in the `replayed_commits` hashmap. 

The change is purely mechanical - it moves existing functionality from `replay_revisions()` into a dedicated function while maintaining identical behavior. The new helper parallels the existing `get_mapped_commit()` function, creating a more symmetrical API for working with the commit mapping. 

This refactoring prepares for the series' final patch which will implement the actual `--linearize` functionality. The clean separation of concerns here suggests the author is following Git's convention of splitting logical changes from mechanical refactorings. The patch touches only `replay.c` and the diff shows 20 lines added versus 11 removed, with no functional changes to the commit mapping logic itself.

Key technical context for future reference: the new `put_mapped_commit()` function handles storing the mapping between original and rewritten commits in the `kh_oid_map_t` hashmap, which is central to `git replay`'s operation. The BUG() check for duplicate commits remains unchanged from the original implementation.