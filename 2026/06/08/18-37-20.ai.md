This is the second patch in Toon Claes's three-patch series introducing `--linearize` to `git replay`. It is a refactoring patch that extracts a small, self-contained helper function (`put_mapped_commit`) from the lengthy `replay_revisions` function in `replay.c`.

The change is purely internal: it moves eleven lines of code into a new static helper, renames the existing `mapped_commit` function to `get_mapped_commit` for symmetry, and updates the single call site. The patch does not alter behavior, add new features, or touch any user-facing code. It is motivated by readability and maintainability, preparing the codebase for the upcoming `--linearize` implementation in patch 3.

The diff is clean and focused, touching only `replay.c`. The new helper is well-named and follows Git's coding conventions. No tests or documentation are added, which is appropriate for a refactoring of this scale. The patch is based on the same commit as the rest of the series (`9ac3f193c05c`) and carries the same Change-Id (`20260604-toon-git-replay-drop-merges-807fa008d395`).

This patch is unlikely to be controversial; it is a textbook example of a safe, preparatory refactoring. Reviewers may check that the helper's logic is correct and that the call site update preserves the original semantics, but the change is small enough that substantive objections are improbable. The patch is part of the replay machinery subsystem and is a necessary step toward the `--linearize` feature introduced in the next patch.