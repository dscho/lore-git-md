This is the first patch in Toon Claes' 3-part series adding a `--linearize` option to `git replay`. It's a refactoring patch that simplifies the code in preparation for the new feature.

The patch converts an `enum replay_mode` with two values (`REPLAY_MODE_PICK` and `REPLAY_MODE_REVERT`) into a simple boolean `reverse` flag. This change:
- Removes the enum definition and replaces all enum checks with boolean logic
- Simplifies control flow by eliminating the need to handle invalid enum values
- Makes the code more straightforward where ternary operators were previously used
- Maintains the same functionality while reducing complexity

The changes are mechanical and focused on `replay.c`, touching the commit creation and picking logic. This appears to be a clean-up that will make the subsequent `--linearize` implementation easier to add. The patch follows Git's style guidelines and includes a clear commit message explaining the rationale.

This is a straightforward preparatory change that doesn't introduce new behavior but makes the code more maintainable for the feature being added in patch 3/3. The diff shows careful attention to preserving existing functionality while simplifying the implementation.