This is a technical discussion about optimizing `git branch --contains` performance, specifically around the cached DFS algorithm (`contains_tag_algo`) when generation numbers are available via commit-graph. Jeff King (Peff) responds to Kristofer Karlsson's benchmarks showing significant speedups (3.7-16x) from enabling this optimization in branch operations.

Peff agrees the approach is worth pursuing, noting it's superior to an alternative patch that unconditionally enables the optimization. He highlights the importance of the generation-number precondition that Kristofer identified, which ensures the optimization is safe and effective. The email connects two parallel efforts (Kristofer's benchmarks and another contributor's patch) while endorsing the more conservative, conditional approach.

The thread has progressed from initial problem diagnosis (replace refs disabling optimizations) to concrete optimization proposals with production-scale validation. Peff's response signals this is likely to move forward, though implementation details around commit-graph preconditions remain to be worked out. The change would particularly benefit large repositories with deep histories and many refs.