This is a performance optimization discussion for ref-filter's `--contains`/`--no-contains` operations, continuing the thread about unifying the ancestry checking implementation.

Tamir Duberstein responds to Jeff King's (Peff) technical review points from the previous email. The key developments:

1. **Algorithm selection**: Tamir confirms Peff-identified regression case (100k-commit history without generation numbers shows 18x slowdown) and agrees to make the algorithm choice dynamic - using memoized depth-first walk only when generation numbers are available, otherwise falling back to breadth-first. References Kristofer's recent patch on generation number usage.

2. **Cycle handling**: Now split into a separate preparatory patch per review feedback, addressing the existing cycle-handling issue in `git tag --contains` before the broader optimization. The solution tracks in-progress commits via CONTAINS_IN_PROGRESS state in the slab.

3. **Implementation plan**: The series will now be restructured as:
   - First patch: Fix cycle handling for existing depth-first tag algorithm
   - Second patch: Add dynamic algorithm selection (depth-first with generations, breadth-first otherwise)
   - Third patch: Unify implementations across ref-filter commands

Performance measurements show dramatic improvements in the target case (62,174 refs now 223x faster) while acknowledging the need to avoid regressions in deep histories without generation numbers. The discussion demonstrates careful balancing of optimization benefits against edge case correctness.