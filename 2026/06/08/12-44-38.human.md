This is a refactoring patch that introduces a new helper function for validating option dependencies in Git's parse-options API. The patch implements `die_for_missing_opt()` to standardize checks where one option requires another (e.g., "--pathspec-file-nul" requires "--pathspec-from-file").

The patch shows good responsiveness to prior review feedback - it has been squashed into a single commit, renamed for clarity (`die_for_missing_opt` instead of the original `die_for_required_opt`), and focused on a clean example conversion in `builtin/add.c`. The implementation matches the style of existing parse-options helpers and maintains the same error message format (now centralized and marked for translation).

The change is well-justified by existing test coverage in t3704 and related pathspec tests, meaning no new tests are needed to verify the behavior. This appears to be a solid incremental improvement to the parse-options API that could enable future cleanups across other commands. The patch is small, focused, and clearly motivated by both code hygiene and i18n consistency benefits.