Here’s the updated human-readable summary for a developer catching up on the thread:

The thread began as a performance investigation by D. Ben Knoble into why `git diff` runs dramatically slower in secondary worktrees (223ms vs 3.4ms in the main worktree). The root cause was traced to racy-git scenarios—where second-granularity timestamps force Git to perform expensive full-content checks. Jeff King (Peff) proposed a simple fix: removing the `#ifdef USE_NSEC` guard in `read-cache.c` to enable nanosecond-aware timestamp comparisons unconditionally. This 8-line change eliminates racy entries at the source by leveraging existing nanosecond precision, resolving the performance issue without architectural tradeoffs. Junio C Hamano initially acknowledged the patch as "cute," signaling approval of its elegance, but has since raised a critical edge case.

Junio’s latest review highlights a potential flaw in Peff’s approach: if two files (or a file and the index) are modified within the same millisecond, the kernel’s inode cache might not preserve nanosecond precision equally for both. For example, if one entry loses its nanosecond precision due to cache eviction while the other retains it, the comparison could incorrectly treat a file as racy or non-racy. This undermines the patch’s goal of eliminating raciness entirely, as the original `USE_NSEC` conditional was designed to avoid such stat-matching failures. The concern shifts the focus to validating whether the patch holds up under real-world conditions, particularly on systems with high filesystem activity.

The thread now centers on whether Peff’s patch is robust enough or if alternatives—like Junio’s earlier proposal to track racy entries in the `istate` structure—are needed as a fallback. While Peff’s solution remains the leading candidate, the discussion has pivoted from performance to correctness, with testing likely to focus on edge cases involving kernel inode cache behavior. The outcome will determine whether the fix can proceed as-is or requires refinement to handle timestamp precision inconsistencies.