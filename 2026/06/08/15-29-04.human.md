This is a substantive review of patch 4/16 in Patrick Steinhardt's series converting Git's packed object storage to use `struct odb_source` as part of the ODB abstraction effort.

Karthik Nayak provides feedback on the implementation details while acknowledging the patch's overall approach. The review focuses on two main points:

1. A minor suggestion about variable naming consistency in the diff, noting that using `packed` consistently from the start would have made for a cleaner diff, though he considers this a trivial point.

2. A more substantive observation about the unused `name` parameter in `chdir_notify_register()` calls throughout the codebase. Karthik questions whether this parameter (meant for tracing) should be removed entirely if it's consistently unused, tagging this as potential cleanup work (#leftoverbits).

The review maintains the thread's technical focus on the ODB abstraction work, while surfacing an architectural question about chdir notification internals that extends beyond this specific patch. Karthik's feedback is constructive and grounded in the code, though he doesn't raise any blocking concerns about the current implementation.