This is a review comment on an RFC patch proposing behavior changes for the experimental `git history reword` command. The patch is part of Patrick Steinhardt's larger `git history` feature effort.

Reviewer D. Ben Knoble offers a minor test improvement for the patch, suggesting the test could be simplified by using `GIT_EDITOR=true` instead of creating a separate fake editor script. The review is surface-level, focusing only on test implementation details rather than engaging with the substantive behavior change being discussed in the thread.

The review doesn't address the core debate about whether `git history reword` should abort on unchanged messages (the patch's main change) or how it should handle feedback (the thread's broader discussion). This appears to be a quick drive-by suggestion from a contributor who works primarily on build system and test infrastructure improvements.