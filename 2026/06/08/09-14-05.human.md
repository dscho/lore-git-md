This is a technical discussion about API design for the `reset_head()` function in Patrick Steinhardt's `git history drop` series. The exchange focuses on how to best extend the function's capabilities while maintaining clear semantics.

Phillip Wood had suggested making HEAD updates opt-in rather than opt-out in the API, and raised concerns about the function's appropriateness if HEAD updates become optional. Patrick acknowledges the naming awkwardness this creates, agreeing that "reset_head" becomes less accurate if HEAD updates are optional. He considers alternatives like `reset_working_tree()` but notes potential confusion with worktree-related functionality.

The discussion reveals an underlying tension in API evolution - Phillip advocates for a cleaner, more intentional design where callers explicitly request each operation, while Patrick seems hesitant about the additional complexity this would introduce for existing callers. Both agree the current naming would need reconsideration if the API shifts to make HEAD updates optional.

This is a substantive design discussion between two experienced contributors about how to best extend Git's reset machinery while maintaining clear interfaces. The outcome will likely influence how future callers interact with this low-level functionality.