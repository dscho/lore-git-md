Junio C Hamano (the Git maintainer) weighs in on Pablo Sabater's RFC patch to add success feedback to `git history reword`. His comment focuses on consistency within the broader `git history` command family, questioning whether other subcommands like `split`, `drop`, and `fixup` exhibit similar verbosity patterns.

This is a brief but significant intervention that shifts the discussion from philosophical debates about Git's "rule of silence" to a more concrete design question about command family consistency. Junio's comment implies that if the feedback behavior is justified for `reword`, it should likely be implemented uniformly across all `history` subcommands rather than as a one-off addition.

The comment doesn't reject the idea outright but sets a clear next step for the discussion: Pablo (or other participants) should examine the existing `history` subcommands' output behavior and either:
1. Make the case for why `reword` merits different treatment, or
2. Propose a consistent feedback approach for the entire command family

This moves the thread toward resolving the design question at a subsystem level rather than debating individual command behavior in isolation. Junio's framing suggests the feedback mechanism may need to be generalized if adopted.