This is a follow-up in the thread about supporting large objects (>4GB) on Windows by converting object header length handling from `unsigned long` to `size_t`. The series has been maintained in Git for Windows for 4.5 years and is now being proposed for upstream inclusion.

Junio C Hamano notes that while the technical approach is sound (switching to `size_t` is appropriate for memory regions), the series as submitted conflicts with current in-flight topics. He requests an updated version rebased on a more recent codebase, but indicates there's no urgency as he works through backlog.

The context shows this is a well-tested change (validated in production for years) that addresses a real platform limitation, with Johannes Schindelin's sign-off confirming Windows-specific validation. The only remaining step is rebasing the patches to resolve conflicts with other ongoing work before merging.