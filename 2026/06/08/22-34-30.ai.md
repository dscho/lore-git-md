This is a performance optimization discussion for ref-filter's `--contains`/`--no-contains` operations, continuing the thread about unifying the ancestry checking implementation.

Jeff King (Peff) raises several technical points about the proposed changes:

1. **Algorithm selection**: Notes the original reason for having separate implementations - the tag-specific depth-first algorithm can be slower than breadth-first when generation numbers aren't available and the merge base is nearby. Suggests making the algorithm choice dynamic based on generation number availability, referencing Kristofer's recent patch.

2. **Cycle handling**: Observes that cycle handling should be fixed as a separate patch since it affects existing `git tag --contains` behavior. Questions how cycles are currently processed and whether the proposed solution (tracking in-progress commits) is the right approach.

3. **Performance tradeoffs**: While acknowledging the impressive speedups shown (223x faster with many refs), points out there may be regression cases - particularly deep histories with few diverging branches when generation numbers are unavailable.

The discussion reveals deeper considerations about the optimization's behavior in edge cases and different repository topologies. Peff's comments suggest the changes may need to be split further to properly address cycle handling separately from the algorithm unification.

This continues the substantive technical review of the ref-filter optimization, now focusing on the finer points of algorithm selection and cycle detection. The exchange demonstrates the careful balancing act between performance improvements and maintaining correct behavior across all cases.