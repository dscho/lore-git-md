This is the first patch in a 3-part series introducing a `--linearize` option to `git replay`. The patch is a simple refactoring that converts an enum with two values (`REPLAY_MODE_PICK` and `REPLAY_MODE_REVERT`) into a boolean flag (`reverse`). 

The change is straightforward and mechanical - it simplifies control flow by removing the enum checks and BUG() assertions, replacing them with direct boolean logic. The patch touches only `replay.c`, updating all the function signatures and call sites to use the new boolean parameter instead of the enum. 

This appears to be a preparatory cleanup before adding the new linearization functionality in patch 3. The commit message clearly explains the change and its benefits (simpler code, fewer error paths). The diff shows careful attention to detail in updating all affected code paths consistently.

As a refactoring with no behavior change, this patch is low-risk and uncontroversial. It follows Git's preference for small, focused changes that make the codebase incrementally better while paving the way for new features.