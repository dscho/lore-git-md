This is a follow-up discussion in the thread about deprecating `git-init-db` in favor of `git-init`. Kristoffer Haugsbakk provides additional rationale for including a runtime warning when the deprecated command is used.

The email reports on a search of the mailing list archives which found only one recent use of `git-init-db` (as a bug reproducer), suggesting the command is rarely used but may still exist in muscle memory or old scripts. Kristoffer argues that a simple warning would be valuable since script failures might otherwise obscure the root cause when the deprecated command is used.

The discussion shows consensus solidifying around including both the `DEPRECATED` flag (as previously suggested by Kristoffer) and a runtime warning (as suggested by Phillip Wood), though Patrick Steinhardt had expressed mild reservations about whether the warning was strictly necessary. Kristoffer's point about debugging scenarios provides concrete justification for the warning.

This exchange represents the final refinement of the technical approach before Patrick is expected to send an updated patch series implementing both deprecation mechanisms. The thread has now addressed all major questions about how to handle the deprecation, leaving only the implementation work remaining.