The thread discusses deprecating the historical `git-init-db` command alias in favor of the canonical `git-init` form. Patrick Steinhardt's initial patch series proposed the technical changes (renaming files/functions while maintaining compatibility) and documentation updates. Reviewers Kristoffer Haugsbakk and Phillip Wood suggested enhancements - adding a `DEPRECATED` flag in the command table and printing a runtime warning when the old command is used.

Kristoffer's latest email provides concrete evidence that while `git-init-db` is rarely used (only one recent mailing list reference), a warning would help users debugging scripts that might still reference the old command. This addresses Patrick's earlier hesitation about whether the warning was necessary. The technical approach is now settled, with consensus to include both deprecation mechanisms in the next version of the patch series.

All that remains is for Patrick to send the updated implementation, which will include the file/function renames, documentation updates, `DEPRECATED` flag, and optional warning message - completing this straightforward cleanup of a long-standing command alias. The changes follow established patterns for deprecating commands in Git.