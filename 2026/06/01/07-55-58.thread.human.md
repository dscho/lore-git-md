This thread introduces a straightforward but useful cleanup from Patrick Steinhardt, deprecating the legacy `git-init-db` command alias in favor of the more modern `git-init`. The two-patch series makes both mechanical and documentation changes: first renaming the implementation file from `builtin/init-db.c` to `builtin/init.c`, then officially marking `init-db` as deprecated in the documentation while maintaining backward compatibility. 

The changes are well-contained, touching build systems (Makefile and meson) and documentation (including a new redirect man page), but don't modify any core functionality. The motivation appears to be general housekeeping - removing historical aliases to promote command name consistency across the codebase. Since this is a documentation-focused change that maintains backward compatibility, it's unlikely to generate much discussion unless reviewers spot specific technical issues with the implementation.