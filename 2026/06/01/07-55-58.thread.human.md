Patrick Steinhardt's patch series to deprecate the `git-init-db` command alias in favor of `git-init` has progressed through collaborative review. The initial proposal to rename the implementation (moving from `init-db.c` to `init.c`) while maintaining backward compatibility has been refined with two key suggestions from reviewers: Kristoffer Haugsbakk proposed marking the old command with a `DEPRECATED` flag in Git's command table, while Phillip Wood recommended adding a runtime warning when users invoke the deprecated form.

In the latest exchange, Patrick acknowledges Phillip's suggestion to use Git's standard `you_still_use_that()` warning mechanism, though he questions whether it's strictly necessary given how straightforward the `git-init` replacement is. Despite this mild reservation, he appears likely to include both suggestions in the next version of his patches, recognizing they would provide consistent deprecation signaling alongside the already-planned documentation updates and command table marking.

The thread has now reached consensus on the technical approach, with all that remains being for Patrick to send an updated series implementing these refinements. This continues Git's pattern of carefully managing command deprecations, balancing backward compatibility with clear signaling to users about preferred alternatives. The changes will make `git-init` the clearly documented canonical form while gently steering users away from the historical `init-db` alias that has been secondary since 2007.