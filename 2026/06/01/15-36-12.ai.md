This is a cover letter for a 2-patch series introducing a new "drop" subcommand to `git history`, part of Patrick Steinhardt's ongoing work on the experimental history command. The series builds on commit 1666c12652 ("builtin/history: implement replay subcommand", 2026-05-15).

The new `git history drop` command removes a specified commit from history and replays descendant branches on top of its parent. The implementation splits ref update handling into two phases (patch 1) to support this operation, then adds the drop subcommand itself (patch 2). The series includes extensive tests (513 lines across 4 new test scripts) and updates the documentation.

This appears to be a well-structured addition to the history command, with thorough test coverage and clear commit messages. The change is relatively small (2 focused patches) but provides significant new capability. Given Patrick's track record with this command and the careful approach shown here, this seems likely to be uncontroversial. The series touches builtin/history.c and adds new test coverage, following the established pattern of previous history command additions.