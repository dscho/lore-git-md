Patrick Steinhardt’s `git history drop` series, now at its eighth iteration, has been accepted by Junio C Hamano and is ready for merging. The series adds a new `drop` subcommand to the experimental `git history` command, allowing users to remove a commit from history and replay its descendants on top of its parent. This work also modernizes the reset API, advances the removal of the `the_repository` global variable, improves reference update architecture, and includes robust conflict detection, bare repository support, and preservation of local changes.

All prior feedback has been addressed, including a critical dispute over ref resolution in `find_head_tree_change()`, which was resolved in v8 by ensuring the function correctly handles detached-HEAD states. The series has been thoroughly reviewed and approved by Christian Couder, with only a minor whitespace nit remaining—a spurious space in a `BUG()` macro call in `reset.c`. Patrick has acknowledged the issue and is coordinating with Junio on whether to fix it directly or send a new patch version. This procedural detail is the last loose end before the series is queued for merging.

The implementation is complete, with 561 lines of new test coverage verifying edge cases like detached `HEAD`, bare repositories, and conflict detection. The reset API has been modernized to support dry-run mode, opt-in HEAD updates, and explicit "from" commit specification, while the ref-update machinery has been split into computation and application phases to enable conflict detection in the `drop` subcommand. The series also eliminates code duplication between replay and history code paths and fixes a latent bug in the reset machinery. No further technical discussion is expected, and the thread is effectively closed.