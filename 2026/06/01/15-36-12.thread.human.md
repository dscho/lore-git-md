Here’s the human-readable summary for a developer catching up on the thread:

The `git history drop` series, now at version 6, adds a new subcommand to remove commits from history and replay their descendants. The core functionality is complete, with all review feedback addressed, and the series has earned a "looks great to me" from Christian Couder. The only remaining blocker is a code duplication concern: two functions—`compute_pending_ref_updates()` in `builtin/history.c` and `replay_result_queue_update()` in `replay.c`—both handle reference updates with nearly identical logic. Christian flagged this as a maintainability issue, and Patrick has agreed to refactor the code to expose a shared helper, resolving the redundancy.

The series touches several subsystems, including the reset API (now modernized with explicit `UPDATE_HEAD` control and `the_repository` removal), reference updates (split into analysis and execution phases), and index handling (with fixes for dirty index cases). Tests cover edge cases like dry runs, bare repositories, and conflict detection. Junio has marked the series as "Waiting for response(s) to review comment(s)," but the technical work is done—this final cleanup is procedural. Once the duplication is addressed, the series is likely to land.