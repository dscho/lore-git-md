Here's the AI mode thread update:

Jacob Keller's patch series fixes a bug in `git describe` where `--exclude` and `--match` options were not properly forwarded to `--contains` when used with `--all`. The issue affected pattern matching against tags, branches, and remote branches. Version 2 of the patch improves test coverage and uses the `check_describe` helper.

The fix modifies `builtin/describe.c` to:
- Always pass tag patterns with `refs/tags/` prefix
- When `--all` is used, also pass patterns with `refs/heads/` and `refs/remotes/` prefixes
- Properly handle combinations of include/exclude patterns

Test coverage in `t/t6120-describe.sh` now verifies:
- Basic tag matching behavior
- Branch name matching
- Remote branch matching
- Combined include/exclude cases

The changes align the implementation with the documented behavior where `--all` should consider all refs when matching patterns. The patch is focused on the pattern forwarding logic without broader architectural changes. Version 2 incorporated test improvements from review.