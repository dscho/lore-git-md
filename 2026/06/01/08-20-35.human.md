This is patch 12/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `freshen_object()` callback for the loose object backend, moving the implementation from object-file.c to odb/source-loose.c where it belongs with the other loose source operations.

The change inlines the helper function `check_and_freshen_source()` since it's no longer needed elsewhere, and updates the files backend to use the new callback-based approach rather than calling the loose backend directly. The diff shows a net reduction of 19 lines while maintaining identical behavior - a typical example of Patrick's careful, incremental refactoring style in this series.

This continues the pattern established in previous patches of converting direct function calls to callback-based operations through the odb_source interface. The change is mechanical and well-contained, touching only the loose object freshening logic while leaving the actual file freshening behavior unchanged. With no substantive technical feedback on earlier patches in the series, this appears to be another straightforward step toward the goal of making Git's object storage fully pluggable.