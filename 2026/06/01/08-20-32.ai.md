This is patch 9/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `find_abbrev_len` callback for loose object sources, continuing the pattern of converting direct function calls to callback-based operations.

The change moves `odb_source_loose_find_abbrev_len()` and its helper functions from object-file.c to odb/source-loose.c, where it becomes part of the loose source's callback interface. The implementation remains functionally identical - it still calculates the shortest unique prefix length for a given object ID by comparing against other objects in the loose object store. The files backend is updated to use the callback-based interface rather than calling the function directly.

This is a straightforward mechanical change that follows the same pattern as previous patches in the series. The diff shows 39 lines removed from object-file.c and 40 added to odb/source-loose.c, with no behavioral changes. The patch maintains test coverage through existing tests that exercise object abbreviation functionality.

As with earlier patches in the series, this continues the incremental refactoring approach that has been well-received so far. The change is uncontroversial and builds on the established pattern of moving loose object operations into the odb/source-loose.c file while maintaining the same functionality through callbacks.