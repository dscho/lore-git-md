This is patch 9/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `find_abbrev_len` callback for loose object sources, continuing the methodical conversion of direct function calls to callback-based operations.

The change moves the abbreviation length calculation logic from object-file.c to odb/source-loose.c, where it becomes part of the loose source's callback interface. The implementation remains functionally identical - it still finds the shortest unique prefix for a given object ID while respecting a minimum length requirement. The patch follows the established pattern of the series by relocating code while maintaining all existing behavior.

This is a straightforward infrastructure patch that continues the series' clean separation of concerns. Like previous patches in the series, it removes code from object-file.c (39 lines) while adding slightly more (40 lines) to odb/source-loose.c to properly encapsulate the functionality. The changes are well-contained and follow the same careful approach seen in earlier patches.

With no substantive technical feedback on previous versions and this being another incremental step in a well-structured series, the patch appears ready for inclusion as part of the larger ODB abstraction effort.