This is patch 13/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch focuses on the loose object map functions, which currently operate on a generic `struct odb_source*` but implicitly expect it to be the "files" backend. 

The key change is refactoring these functions to explicitly take a `struct odb_source_loose` parameter instead, eliminating the implicit dependency on the files backend. This makes the code's intent clearer and removes an unnecessary indirection through the files source. The changes are mechanical - updating function signatures and call sites while preserving all behavior.

The patch touches core object handling code in loose.c and object-file.c, with corresponding updates to loose.h. It follows the established pattern of the series by making focused, behavior-preserving changes to move toward the odb_source callback interface. Test coverage remains unchanged as this is purely an internal reorganization.

This continues the incremental refactoring approach seen in previous patches, with each commit wiring up specific aspects of the odb_source interface. The changes are straightforward and maintain the series' careful structure. With no substantive technical feedback on earlier versions, this appears to be progressing the ODB abstraction effort in a well-contained manner.