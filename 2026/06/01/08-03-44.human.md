This is a response from Patrick Steinhardt regarding signature handling in the `git history` command. The thread began with a user noticing that `git history` currently drops commit signatures during operations, unlike `git rebase` which preserves them via its `-S` flag.

Steinhardt confirms the current behavior but agrees signature preservation would be a worthwhile addition. However, he notes it's not currently a priority for him as he's focused on other `git history` features. The response leaves the door open for others to implement this functionality, suggesting it may be some time before the core team addresses it themselves.

The exchange highlights an intentional gap in the new `git history` command's functionality compared to the more mature `git rebase`. While the issue is acknowledged as valid, the response makes clear this isn't something likely to be resolved in the near term without community contribution. The tone is welcoming to outside participation while being transparent about the maintainer's current priorities.