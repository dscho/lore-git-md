Part of Patrick Steinhardt's ongoing ODB abstraction effort, this patch refactors loose object writing code to explicitly operate on `struct odb_source_loose` rather than the more generic `struct odb_source`. The change makes the dependency on loose object handling clearer while maintaining identical behavior.

The patch updates function signatures across object-file.c and related files to use the specific loose source type, including core writing functions like `write_loose_object()` and `odb_source_loose_write_stream()`. It also adjusts callers in http-walker.c and http.c to properly downcast the source. The changes are mechanical - no logic is altered, just the types and access patterns are updated to be more precise.

This continues the series' pattern of incremental, behavior-preserving changes to prepare for fully moving loose object handling into odb/source-loose.c. The patch notes that some shared logic with `force_object_loose()` remains to be moved in future work. Test coverage remains unchanged, showing the refactoring doesn't affect functionality.

The changes touch core object writing paths but maintain the same careful approach seen in earlier patches of this series - focused updates that make the codebase ready for deeper architectural changes while keeping everything working exactly as before. With no substantive technical feedback on previous versions, this appears to be progressing the ODB abstraction effort in a straightforward way.