This is a feature proposal for mirror repositories as alternate submodule sources. Benson Muite suggests adding the ability to specify mirror repositories for submodules, which would be checked when the primary source times out or has bandwidth limitations during operations like `git clone --recurse-submodules` or `git submodule update`.

The proposal is motivated by practical concerns around bandwidth limits and reliability when fetching submodules from a single source. The email doesn't specify implementation details but raises the concept as something that could be useful for projects using multiple hosting services. This appears to be an initial inquiry about whether such a feature would be considered for inclusion rather than a concrete implementation proposal.

The idea touches on several existing Git subsystems: submodule handling, remote configuration, and fetch behavior. If implemented, it would likely require changes to how submodule URLs are specified and processed, potentially adding new configuration options for alternate sources. The proposal doesn't reference any prior work or similar existing features in Git, suggesting this may be a novel suggestion rather than an extension of existing functionality.