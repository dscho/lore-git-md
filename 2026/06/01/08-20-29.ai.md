This is patch 6/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `read_object_info()` callback for the loose object source, moving the implementation from object-file.c to odb/source-loose.c.

The key change is relocating `odb_source_loose_read_object_info()` to its proper home in the loose source implementation file and registering it as the vtable callback. Callers now use the generic `odb_source_read_object_info()` interface rather than calling the function directly. The patch also adjusts `read_object_info_from_path()` to explicitly take a `struct odb_source_loose*` parameter since it remains shared with `for_each_object_wrapper_cb()`.

The changes are mechanical and maintain existing behavior - this continues the series' pattern of incrementally converting direct function calls to callback-based operations through the odb_source interface. The patch touches core object handling code but doesn't modify functionality, focusing on the internal reorganization needed for the ODB abstraction. Test coverage confirms the behavior remains unchanged.

This patch builds on the infrastructure established in the first five patches of the series, further integrating the loose object source with Git's generic object storage layer interfaces. With no substantive technical feedback on previous versions, this appears to be progressing as planned in Patrick's long-term effort to make Git's object storage pluggable.