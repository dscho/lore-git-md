This is patch 8/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `for_each_object()` callback for the loose source by moving the implementation from object-file.c to odb/source-loose.c.

The changes follow the established pattern of the series - relocating functionality while maintaining behavior. Key technical aspects:
- Moves `odb_source_loose_for_each_object()` and helpers to the new location
- Makes `for_each_file_in_obj_subdir()` non-static for shared use
- Updates callers to use the callback-based interface
- Removes 299 lines from object-file.c while adding 264 to odb/source-loose.c
- Preserves all existing functionality through the transition

The patch continues the incremental approach of converting direct function calls to callback-based operations through the odb_source interface. Like previous patches in the series, this is a mechanical change that doesn't modify behavior but moves code to its logical home as part of the ODB abstraction work.

No substantive technical feedback has been raised on this aspect of the series yet. The changes appear well-contained and follow the established pattern of the earlier patches that wired up other callbacks. The series continues to progress methodically through the loose object handling refactoring.