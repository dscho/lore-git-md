This is a bugfix series addressing resource cleanup issues in HTTP pack index handling. The two-patch series from Lorenzo Pegorari follows up on earlier review feedback from Jeff King (Peff), now properly splitting the changes into logical units.

The first patch removes redundant tempfile cleanup code that became unnecessary after commit 63aca3f7f1 made the tempfile subsystem handle cleanup automatically. The second patch fixes a memory leak in error paths of `fetch_and_setup_pack_index()` where pack index resources weren't being freed properly.

The changes are well-motivated and demonstrate careful attention to error path resource management. Jeff King's thorough review has helped shape the patches into their current form, with consensus now reached on the approach. The series shows the typical Git project workflow where patches mature through multiple iterations with reviewer feedback.

This appears ready for merging, addressing both the original memory leak and cleaning up tempfile handling in the HTTP transport layer's pack index processing. The changes are focused and unlikely to be controversial.