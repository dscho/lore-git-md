This is a review comment in the ongoing thread about deprecating `git-init-db` in favor of `git-init`. Phillip Wood builds on the previous discussion about marking the command as deprecated in the code, suggesting that the deprecation should also trigger a runtime warning when users execute the old command name.

The review is substantive, proposing a user-facing change to make the deprecation more visible beyond just documentation and code markings. Phillip notes that users who have muscle memory for `git-init-db` may not see man page warnings, so a runtime message would be more effective at raising awareness. This continues the thread's focus on implementation details while expanding the scope to consider user experience.

Key technical points:
- The suggestion would require modifying the command dispatch logic to detect and warn about deprecated commands
- This would follow the pattern of other deprecated Git commands that print warnings
- The change would make the deprecation more visible to end users

The thread remains focused on refining the deprecation mechanism rather than questioning the deprecation itself. Phillip's review moves the discussion from internal code markings to considering how users will experience the transition. This is a typical pattern in Git's deprecation processes, where initial technical implementation is followed by discussion of user communication.