This is the final merge-ready version of the `--max-count-oldest` feature, which provides an optimized way to view the earliest commits in a history. The patch series has gone through eight substantive iterations and multiple rounds of review, with all technical concerns now resolved.

The feature implements a new `--max-count-oldest` option that complements the existing `--max-count` by showing the oldest N commits rather than the newest. It uses Jeff King's sliding window algorithm (`retrieve_oldest_commits()`) with O(K) space and O(N) time characteristics. The implementation properly handles edge cases and maintains compatibility with other revision walk features like `--boundary` and `--graph`.

Key technical details:
- Extends `struct rev_info` with `max_count_type` and `max_count_stage` flags
- Places counting logic in `get_revision_internal()` per Johannes Sixt's recommendation
- Includes comprehensive error handling for invalid option combinations
- Fixes graph output artifacts via proper CHILD_SHOWN marking
- Correctly excludes boundary commits from the count limit

The documentation clearly contrasts `--max-count-oldest` with `--max-count` and the test suite covers all option combinations and edge cases in `t4202-log.sh. All CI portability issues have been resolved, including using `test_line_count` instead of non-portable `wc -l` and making cleanup robust with `rm -f`.

Junio C Hamano has signed off on the final version after verifying all interface, test portability, and formatting requirements are met. The thread is now fully complete with all review feedback incorporated through eight substantive iterations and multiple final polish commits. The feature provides a clean solution for viewing earliest commits while maintaining Git's architectural standards.