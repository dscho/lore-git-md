This is a review comment from Junio C Hamano in the ongoing thread about deprecating `git-init-db` in favor of `git-init`. The thread has largely settled on the technical approach (adding both a DEPRECATED flag and runtime warning), but Junio now introduces a dissenting opinion about the runtime warning mechanism.

Junio expresses skepticism about the value of `you_still_use_that()` warnings in general, calling them "a mistake" for this case. His argument has two parts: (1) the `init`/`init-db` transition is so straightforward that a warning is unnecessary, and (2) runtime warnings don't help scripted usage where the output might be ignored. This contrasts with Phillip Wood's earlier position that runtime warnings help users who don't read man pages.

The comment suggests Junio may push back against including the runtime warning in the final patch, despite the earlier consensus between Patrick Steinhardt, Kristoffer Haugsbakk, and Phillip Wood. As the maintainer, Junio's opinion carries significant weight in the decision, though he hasn't explicitly blocked the warning yet.

This introduces a new point of contention in what had appeared to be a settled discussion. The thread now awaits Patrick's response to Junio's concerns and whether he will adjust the patch accordingly. The core deprecation (via documentation and DEPRECATED flag) remains uncontested.