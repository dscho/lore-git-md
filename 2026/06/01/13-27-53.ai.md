Bugfix follow-up splitting tempfile cleanup into separate patch.

Lorenzo Pegorari responds to Jeff King's review of the HTTP pack index memory leak fix, agreeing to split the tempfile cleanup portion into a separate preparatory patch. This addresses Peff's observation that the original combined patch had two logically distinct changes: fixing the memory leak and removing a redundant `unlink()` call.

The thread has now reached consensus on both technical approaches - the memory leak fix is sound, and the tempfile cleanup simplification is also correct (though could theoretically be split out). Peff had noted the diminishing returns of further polishing, but Lorenzo chooses to make the separation for clarity. This represents the final refinement before the series is ready for merging, with no outstanding technical questions.

The patch continues to focus on `fetch_and_setup_pack_index()` in http.c, specifically its error paths. The tempfile behavior remains unchanged from the previous understanding - the unlink is redundant since tempfile registration ensures cleanup, and keeping it could theoretically cause issues with concurrent fetches. The split will better document these considerations in the commit history.