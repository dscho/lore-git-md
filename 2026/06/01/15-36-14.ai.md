This is the second patch in Patrick Steinhardt's series adding a "drop" subcommand to the experimental `git history` command. The patch implements the core functionality to remove a commit from history and replay its descendants on top of its parent.

The implementation builds on the refactoring from patch 1 (which split ref updates into compute/apply phases) to:
- Add the `git history drop <commit>` subcommand
- Handle working tree and index updates when HEAD is affected
- Prevent dropping root commits or merge commits (current limitations)
- Include extensive test coverage (513 lines across 4 new test scripts)

Key technical details:
- New `update_worktree()` function handles merging changes when HEAD moves
- Careful error checking prevents overwriting local modifications
- Supports `--dry-run`, `--update-refs`, and `--empty` options like other history commands
- Updates documentation with examples and usage

The patch appears well-structured with thorough test coverage and clear commit message. It follows the established pattern of previous `git history` subcommands. Given Patrick's track record with this command and the careful approach shown here, this seems likely to be uncontroversial. The implementation touches builtin/history.c and adds comprehensive test coverage in t/t3454-history-drop.sh.

This completes the initial implementation of the drop subcommand, providing another building block in the experimental history command's capabilities. The limitations around root/merge commits are noted as future work.