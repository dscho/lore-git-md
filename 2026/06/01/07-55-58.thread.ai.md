Thread identity: Patch series from Patrick Steinhardt deprecating the `git-init-db` command alias in favor of `git-init`.

Core problem or goal: Clean up historical command aliases by formally deprecating `git-init-db` and standardizing on `git-init` as the canonical command name, while maintaining backward compatibility. This continues project-wide efforts to rationalize command naming.

Current status: First version (v1) of the series with two patches, now incorporating Kristoffer Haugsbakk's suggestion to use a `DEPRECATED` flag in the command table rather than just conditional compilation. The author has acknowledged the suggestion positively but has not yet sent an updated patch series.

Key technical details:
- File renamed: `builtin/init-db.c` -> `builtin/init.c`
- Function renamed: `cmd_init_db` -> `cmd_init`
- Both command names (`init` and `init-db`) remain valid through compatibility mapping
- Documentation updates in `BreakingChanges.adoc` noting deprecation
- New redirect man page `git-init-db.adoc` with deprecation warning
- Build system updates (Makefile, meson.build) handling deprecation
- Test script updates to use `git-init` consistently
- Planned addition of `DEPRECATED` flag to `init-db` entry in command table (`git.c`)

Open questions and loose ends: Awaiting updated patch series from Patrick Steinhardt incorporating the `DEPRECATED` flag suggestion. The technical approach is now settled but the implementation needs to be completed.

Participant positions:
- Patrick Steinhardt: Author of initial series, has accepted the `DEPRECATED` flag suggestion
- Kristoffer Haugsbakk: Suggested using a `DEPRECATED` flag in command table for clearer deprecation marking

Related work: Part of ongoing project-wide cleanup of historical command aliases and naming consistency, following similar previous efforts to standardize command names. The patch notes `git-init` has been the preferred form since 2007. The `DEPRECATED` flag approach follows patterns used for other deprecated commands in Git's codebase.