Thread identity: Patch series from Patrick Steinhardt deprecating the `git-init-db` command alias in favor of `git-init`.

Core problem or goal: Clean up historical command aliases by formally deprecating `git-init-db` and standardizing on `git-init` as the canonical command name, while maintaining backward compatibility. This continues project-wide efforts to rationalize command naming.

Current status: First version (v1) of the series with two patches, now expanded to include both Kristoffer Haugsbakk's `DEPRECATED` flag suggestion and Phillip Wood's runtime warning proposal. The technical approach is settled with consensus reached on including both mechanisms. Patrick Steinhardt is expected to send an updated implementation soon.

Key technical details:
- File renamed: `builtin/init-db.c` -> `builtin/init.c`
- Function renamed: `cmd_init_db` -> `cmd_init`
- Both command names remain valid through compatibility mapping
- Documentation updates in `BreakingChanges.adoc` noting deprecation
- New redirect man page `git-init-db.adoc` with deprecation warning
- Build system updates (Makefile, meson.build) handling deprecation
- Test script updates to use `git-init` consistently
- `DEPRECATED` flag added to `init-db` entry in command table (`git.c`)
- Command dispatch logic modified to print runtime warning via `you_still_use_that()` when deprecated command is used
- Archive search confirms `git-init-db` is rarely used but may appear in old scripts

Open questions and loose ends: Awaiting final implementation from Patrick Steinhardt incorporating both deprecation mechanisms. The runtime warning justification is now strengthened by Kristoffer's point about debugging scenarios where silent failures could occur.

Participant positions:
- Patrick Steinhardt: Author of initial series, now accepts both deprecation mechanisms
- Kristoffer Haugsbakk: Provided `DEPRECATED` flag suggestion and additional rationale for runtime warnings based on archive search
- Phillip Wood: Originally proposed runtime warning, now accepted as valuable for debugging

Related work: Part of ongoing project-wide cleanup of historical command aliases. The `DEPRECATED` flag and runtime warning patterns follow established practices for other deprecated commands in Git's codebase. The patch notes `git-init` has been the canonical form since 2007.