Feature patch series introducing a new "drop" subcommand for `git history`. This is part of Patrick Steinhardt's ongoing work on the experimental `git history` command, which provides alternative history manipulation tools.

The series consists of two patches:
1. A refactoring that splits ref update handling in the history command into two phases, likely preparing for the new functionality.
2. The actual implementation of the "drop" subcommand, which removes a specified commit from history and replays descendant branches on top of it.

The implementation comes with extensive documentation updates and a substantial new test file (513 lines) covering the functionality. Given Patrick's track record with complex history manipulation features and the thorough test coverage, this appears to be a carefully crafted addition to the experimental history` command family.

The patch doesn't mention any particular motivation or use cases for the new subcommand, but it logically extends the command's capabilities for history rewriting operations. Since this is part of an experimental command, it's likely aimed at gathering user feedback rather than solving an immediate production need.