This thread documents a GSoC participant's work to improve path handling in `git repo info` by introducing explicit variants for gitdir and commondir paths. The series introduces a reusable `append_formatted_path()` helper in `path.c` that handles all path formatting cases (canonical, relative, etc.), then refactors `rev-parse` to use this shared implementation.

The work has progressed through multiple iterations with substantive technical review from Phillip Wood, Lucas Seiki Oshiro, and Justin Tobler, plus maintainer feedback from Junio C Hamano. Key design decisions include requiring explicit `.absolute` and `.relative` variants for plumbing commands (per Junio's guidance) and implementing a clean API where `PATH_FORMAT_DEFAULT` maps to `UNMODIFIED` while allowing caller overrides.

The final patch adds `path.gitdir.absolute` and `path.gitdir.relative` support, completing the series' goal of stable machine-readable paths. All substantive technical discussion has concluded with consensus around the design, and reviewers have confirmed the `rev-parse` integration is clean. The series appears ready for integration pending minor documentation and test output adjustments noted in the cover letter. The shared path formatting approach successfully eliminates code duplication while maintaining robust edge case handling across all path variants.