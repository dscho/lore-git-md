This is a review comment on Patrick Steinhardt's patch to deprecate the `git-init-db` command in favor of `git-init`. Kristoffer Haugsbakk suggests marking the deprecated command with a `DEPRECATED` flag in the command table, rather than just conditionally compiling it out with `WITH_BREAKING_CHANGES`. 

The review is substantive - it engages with the technical implementation choice of how to mark deprecated commands, suggesting an alternative approach that would make the deprecation status more explicit in the code. The suggestion implies that the `DEPRECATED` flag could be used to trigger warning messages or other deprecation behaviors, though the exact semantics aren't specified.

This feedback comes early in the thread's lifecycle - the patch is still at version 1 with no other reviews yet. The suggestion doesn't challenge the overall goal of deprecating `init-db`, but proposes a refinement to how the deprecation is implemented in the code. The thread context shows this is part of an ongoing project-wide cleanup of historical command aliases.