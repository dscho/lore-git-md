This is a review of the new `die_for_required_opt()` helper function in the parse-options API. Jean-Noël Avila raises a naming concern about the function and its parameters, suggesting they don't clearly convey the asymmetric nature of the dependency check.

The review focuses on improving clarity in the API design, specifically:
1. The function name `die_for_required_opt` could imply it checks for required options in general, rather than specifically enforcing that one option requires another
2. The parameter names `opt1` and `opt2` don't indicate which is the dependent option and which is the prerequisite
3. Suggests alternative naming: `die_for_missing_opt()` with parameters named `tested_opt` and `required_opt` to make the relationship clearer

This is a substantive review that engages with the API design rather than just surface-level issues. The reviewer brings a documentation/translation perspective (noting this will help translators) while focusing on making the interface more self-documenting. The feedback doesn't challenge the technical implementation but suggests improvements to make the function's purpose and usage more obvious to callers.

The review doesn't address the second patch in the series (which demonstrates usage in `builtin/add.c`), keeping its scope focused on the core API addition. This suggests the reviewer agrees with the concept but wants to refine the interface before wider adoption.