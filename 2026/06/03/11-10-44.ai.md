This is the second patch in a 2-part series introducing a new `die_for_required_opt()` helper for parse-options validation. The patch demonstrates the helper's usage by converting manual option dependency checks in `builtin/add.c` to use the new standardized function.

The changes replace two explicit conditional blocks that previously checked for:
1. `--ignore-missing` requiring `--dry-run`
2. `--pathspec-file-nul` requiring `--pathspec-from-file`

The conversion maintains identical behavior while reducing the code by 4 lines. The error messages remain the same in content but are now generated through the centralized helper rather than being duplicated inline. This aligns with the series' goal of improving consistency and making translations easier to maintain.

The patch follows the pattern established in the first patch of the series, using the new helper's simple interface that takes the option requiring another and the required option as parameters. The changes are straightforward mechanical conversions that don't introduce any new logic or behavior changes.

This appears to be a clean application of the new helper that successfully demonstrates its value in reducing code duplication while maintaining the same user experience. The patch serves its purpose well as a proof-of-concept for the helper's utility in real command implementations.