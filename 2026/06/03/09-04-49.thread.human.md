This thread addresses a focused build system fix for git-gui's Makefile to properly respect `make -s` silent mode during installation and uninstallation. The original version from Harald Nordgren modified the silent mode detection logic to use substring matching (matching `shared.mak`'s approach) rather than an exact equality check, which Johannes Sixt questioned on technical grounds. Harald clarified this was necessary due to Makefile syntax constraints, not an intentional behavior change.

In the v2 revision, Harald improved the commit message to better explain the motivation - the install/uninstall recipes were incorrectly emitting output via echo calls even in silent mode. The technical changes remain the same (6 lines modifying git-gui/Makefile), now with clearer justification that the implementation matches `shared.mak`'s approach (lines 40 and 46). Johannes' substantive review prompted this improved documentation, though his concern about standalone builds without `shared.mak` remains an open question not addressed in this revision.

The discussion shows careful technical engagement on build system edge cases, with the patch now better documented while maintaining its narrow focus on silent mode behavior. The unresolved `shared.mak` dependency question may warrant future follow-up, but doesn't block this improvement to build output formatting.