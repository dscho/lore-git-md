Here's the human-readable thread summary:

This thread introduces a new helper function in Git's parse-options infrastructure to validate required option dependencies (like "option X requires option Y"). The two-patch series standardizes error handling for these cases while reducing code duplication and improving message consistency for translation.

The first patch adds the core `die_for_required_opt()` helper to parse-options.c, implementing a simple API for single-option dependencies that matches existing helpers in the file. Christian Couder suggested the concept. The second patch demonstrates its use by converting manual validation checks in `git add` for the `--ignore-missing` and `--pathspec-file-nul` options to use the new helper, reducing duplicate code while maintaining identical behavior.

The changes are straightforward mechanical conversions that don't alter functionality but make the codebase more maintainable. The error messages remain the same but now come from a centralized location, which helps with translation efforts. The series has just started, so there haven't been any reviews yet, but the implementation appears clean and well-aligned with existing parse-options patterns.