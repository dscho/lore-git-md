This thread introduces a new helper function to standardize how Git commands validate option dependencies. The series, from contributor [Name TBD], adds `die_for_required_opt()` to parse-options.c - a centralized way to check that required options are present (like ensuring "--pathspec-file-nul" is only used with "--pathspec-from-file"). 

The first patch implements the core helper function, while the second demonstrates its value by converting manual option checks in `git add` to use the new API. This eliminates duplicate validation code and ensures consistent error messages - particularly helpful for localization efforts. The approach is deliberately minimal at first, focusing on single-option cases, but designed to accommodate more complex dependencies in future extensions.

The change appears well-scoped and non-controversial, offering clear maintainability benefits by reducing boilerplate in command implementations. The `git add` conversion shows the helper working in practice, removing several lines of manual checking while preserving the exact same behavior. This seems like a useful addition to Git's option-parsing toolkit that could enable similar cleanups across other commands.