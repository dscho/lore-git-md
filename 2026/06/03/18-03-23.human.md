CI / build system patch continuing the git-gui Makefile silent mode discussion.

Harald Nordgren responds to Johannes Sixt's technical questions about the silent mode detection in git-gui's Makefile. He acknowledges the point about `shared.mak` references needing clarification for standalone builds (though doesn't yet provide the fix). 

On the substantive question about the silent flag detection logic change, Harald explains that the old `ifeq` format didn't work correctly with `ifneq`, so he adopted the `findstring` pattern used elsewhere in `shared.mak`. This suggests the change is motivated by practical compatibility rather than being a deliberate behavioral change.

The exchange remains focused on build system minutiae, with Harald providing context for his implementation choice while leaving Johannes' first concern about `shared.mak` references unaddressed in this reply. The patch remains small and narrowly scoped to silent mode behavior in git-gui's build process.