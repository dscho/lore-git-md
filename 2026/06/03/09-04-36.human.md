This is patch 3/6 in Harald Nordgren's v12 series adding `git branch --prune-merged` for safe automated branch cleanup. The series has undergone extensive review over 12 iterations and appears technically complete.

The current patch prepares the underlying `delete_branches()` function for bulk operations by adding two new flags:
- `no_head_fallback` - Skips HEAD comparison when checking if branches are merged (since bulk caller already verifies upstream status)
- `dry_run` - Adds consistent "Would delete branch..." output matching the actual deletion messages

The changes maintain all existing safety checks while allowing the upcoming `--prune-merged` feature to operate efficiently on multiple branches. The patch carefully preserves current behavior for existing callers by defaulting both flags to 0.

Key technical details:
- Modifies `branch_merged()` to skip HEAD comparison when `head_rev` is NULL
- Adds dry-run output that matches the format of actual deletions
- Keeps all existing error handling and ref existence checks
- Maintains compatibility with current `git branch -d` behavior

This is a well-contained plumbing change that enables the user-facing feature while demonstrating careful attention to backward compatibility and consistent behavior. The patch follows the series' pattern of incremental, safety-focused improvements to the branch deletion infrastructure.