Build system patch improving makefile behavior for git-gui's silent mode.

This patch fixes an inconsistency where git-gui's install/uninstall recipes would still emit output when run with `make -s`, despite the silent flag. The change wraps the existing quiet block in the standard silent flag check from shared.mak, removing a redundant narrower check that only affected QUIET_GEN.

The modification is straightforward - it properly silences all output during installation when requested, bringing git-gui's behavior in line with expectations. The patch touches only the git-gui/Makefile, changing 6 lines total (4 deletions, 2 additions). This appears to be a clean, focused improvement to build system behavior.