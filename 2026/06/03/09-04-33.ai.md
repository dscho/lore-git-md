This is v12 of Harald Nordgren's series adding `git branch --prune-merged` for safe automated local branch cleanup, now with `--dry-run` support and a reworked `--forked` interface. The series has evolved through 12 iterations with all major technical concerns addressed.

Key changes in v12:
- Reworked `--forked` from a standalone action into a `--list-mode` filter that composes with other list filters like `--merged`
- Made both `--forked` and `--prune-merged` take repeatable OPT_STRING_LIST options
- Dropped bare remote name resolution in `--forked` (now requires explicit ref/glob)
- Added comprehensive tests for the new filtering behavior
- Kept all prior safety checks (push tracking comparison, unmerged protection)

The implementation now:
- Uses wildmatch for glob pattern matching
- Combines literal and glob arguments via union
- Preserves branches by name (not just by upstream)
- Includes `--dry-run` support showing exactly what would be deleted
- Maintains configurable opt-out via `branch.<name>.pruneMerged`

The thread context shows this version addresses all prior feedback, with clean range-diffs between versions. The series appears technically complete and ready for final review before potential merging. Phillip Wood's review is pending as the final step.

Notable technical details:
- Reachability checks use local refs only (no implicit fetch)
- Push/upstream comparison prevents trunk branch deletion
- Dry-run output matches exactly what would be deleted
- Config opt-out preserves branches for further development

The safety model has been strengthened through collaborative review, particularly around tracking branch protection and interface design. The series is in its final phase with all major concerns addressed.