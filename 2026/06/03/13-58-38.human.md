This is a documentation and configuration patch in Patrick Steinhardt's series standardizing Git contributor workflow around the `b4` tool. The patch adds project-level b4 configuration files to align with Git's preferred practices.

Toon Claes raises a practical concern about version compatibility - the `send-same-thread` configuration requires b4 v0.15 or higher, which is relatively new (released just two months ago). He asks what happens when users run older versions that don't support this setting, as many distributions may not yet have updated their packaged versions.

This is a valid consideration for adoption, as the patch assumes modern b4 features but doesn't address backward compatibility. The question doesn't challenge the technical implementation but rather probes its real-world applicability, particularly for contributors who might be on older systems. The answer would help assess whether additional documentation or fallback behavior is needed to support users during the version transition period.

The thread context shows this is the final patch in a v2 series that has already addressed substantive technical and pedagogical questions about threading models and documentation. Toon's query represents a final practical consideration before the series is ready for maintainer review.