New AI thread summary:

Subject: [PATCH 0/2] parse-options: add helper for required option dependencies

This thread introduces a new helper function `die_for_required_opt()` in parse-options.c to standardize validation of option prerequisites (e.g. "option X requires option Y"). The series aims to reduce code duplication in option checking across Git commands while improving error message consistency for localization.

The implementation provides a simple API for single-option dependencies, with potential for future extension to more complex cases. Patch 1 adds the core helper function with documentation. Patch 2 demonstrates usage by converting manual checks in `builtin/add.c` for `--ignore-missing` and `--pathspec-file-nul` options, removing duplicate validation code while maintaining identical behavior.

Key technical details:
- New function in parse-options.c with standardized error message format
- Takes the option requiring another and the required option as parameters
- Current focus on single-option cases (can be extended later)
- First conversion target: `builtin/add.c` option validation

The motivation stems from discussions about:
- Reducing duplicate validation code
- Improving error message consistency
- Making translations easier to maintain

Open questions:
- Whether the API should support more complex multi-option dependencies from the start
- Potential additional commands that could benefit from conversion

Related work:
- Ongoing parse-options API improvements
- Localization/i18n efforts in Git

Participant positions:
- Original author proposing the helper function and initial conversion
- No reviews yet (thread just started)