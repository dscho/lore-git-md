This is a review from Junio C Hamano on Patrick Steinhardt's patch introducing the `RESET_HEAD_SKIP_REF_UPDATES` flag for the `reset_head()` function. The flag allows callers to update the working tree and index without modifying references, which will be useful for the upcoming `git history drop` functionality.

Junio acknowledges the careful safety check Patrick added to prevent contradictory flag combinations (requesting both ref updates and skipping them). He suggests extending this validation to also catch the case where both `skip_ref_updates` and `update_orig_head` are set, as this would likely indicate a programming error.

The review is substantive but focused - Junio identifies a potential edge case in the flag validation logic that wasn't covered by the initial implementation. This follows his typical review style of catching subtle correctness issues that might otherwise slip through. The feedback is constructive and directly actionable, requiring only a small addition to the existing BUG check rather than any major rework.

This exchange shows the careful attention being paid to the reset machinery changes that underpin the new `git history drop` functionality. The review helps ensure the foundational changes are robust before building higher-level features on top of them.