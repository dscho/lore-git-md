This is a build system patch addressing git-gui's silent mode behavior, with Johannes Sixt providing a substantive review of the technical implementation.

Johannes points out that the original condition in the Makefile was actually backwards - the `findstring` parameters were in the wrong order (needle,haystack) and would never match as intended. The new version correctly checks for 's' in the MAKEFLAGS. He suggests this subtle but important correction should be mentioned in the commit message.

The review digs into the Makefile mechanics, showing that the old condition would fail for both normal `make` (checking '-' in 's') and `make -s` (checking '-s' in 's'). The new version properly implements the intended silent mode detection by looking for 's' in the flags string.

This continues the thread's pattern of technical engagement around build system details, with Johannes catching an implementation nuance that wasn't fully explained in the commit message. The feedback suggests one more revision may be needed to document this subtle but important fix in the condition logic.