This is patch 3/6 in the final v12 of Harald Nordgren's series adding `git branch --prune-merged` functionality for safe automated local branch cleanup. The series has undergone extensive review over 12 iterations with all major technical concerns addressed.

The current patch prepares the `delete_branches()` function for bulk operations by adding two new flags:
- `no_head_fallback`: Skips the HEAD comparison when checking if branches are merged (since bulk operations already know branches have upstreams)
- `dry_run`: Enables rehearsal mode that shows what would be deleted using the same output format as actual deletions

Key technical details:
- Maintains all existing safety checks while adapting them for bulk operations
- Preserves the warning about branches merged to upstream but not HEAD when HEAD comparison is relevant
- Uses the same output format for dry-run and actual deletions
- Keeps backward compatibility for existing callers by defaulting both flags to 0

The changes are mechanical plumbing work to support the upcoming `--prune-merged` functionality while maintaining the safety guarantees established in earlier patches. The implementation carefully handles edge cases like NULL head_rev and ensures dry-run output matches real behavior.

This patch represents the final technical adjustments needed before introducing the main `--prune-merged` feature in subsequent patches. The series appears technically complete and ready for merging pending Phillip Wood's final review.