Git-gui build system fix for silent mode behavior.

The thread concerns a patch modifying git-gui/Makefile to properly respect `make -s` silent mode during installation/uninstallation. Currently these operations emit output despite the silent flag. The change wraps the relevant commands in the standard silent flag check from shared.mak, removing a redundant QUIET_GEN-specific check. This brings git-gui's behavior in line with expectations for silent builds.

The patch is small (6 lines changed) and focused exclusively on git-gui/Makefile. It appears uncontroversial - a straightforward improvement to build system behavior with no technical objections raised. The change affects only build-time output formatting, not runtime behavior or functionality.