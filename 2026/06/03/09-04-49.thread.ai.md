Git-gui build system fix for silent mode behavior.

The thread concerns a patch series modifying git-gui/Makefile to properly respect `make -s` silent mode during installation/uninstallation. Johannes Sixt raised substantive technical questions about the implementation details, which Harald Nordgren has addressed across two versions:

1. In v1, the silent mode detection logic was changed from exact equality check to substring matching to match `shared.mak`'s approach, addressing Makefile syntax constraints. Harald noted this matches lines 40 and 46 of `shared.mak`.

2. The v2 revision (current state) maintains the same technical change but improves the commit message to better explain the motivation - specifically that install/uninstall recipes were emitting output even in silent mode via embedded calls. The patch remains small (6 lines: 4 deletions, 2 additions) and focused on wrapping the existing quiet block in the standard silent flag check while removing a redundant narrower check.

Key open questions:
- The `shared.mak` dependency concern for standalone builds remains unresolved, as acknowledged by Harald. This affects build portability but is deferred for potential future work.

Technical engagement has been substantive throughout, with Johannes' reviews focusing on build system edge cases and Harald responding with implementation clarifications. The current patch demonstrates careful attention to silent mode behavior while leaving the standalone build question open. The changes are confined to build-time output formatting with no runtime impact.