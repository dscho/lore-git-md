Git-gui build system fix for silent mode behavior.

The thread concerns a patch series modifying git-gui/Makefile to properly respect `make -s` silent mode during installation/uninstallation. Harald Nordgren has addressed Johannes Sixt's substantive technical feedback across multiple versions:

1. v1 changed silent mode detection from exact equality to substring matching (matching `shared.mak`'s approach), with Harald noting this matches lines 40/46 of `shared.mak`.

2. v2 improved the commit message to explain the motivation (install/uninstall recipes emitting output in silent mode) while maintaining the technical change.

3. v3 corrects the inverted `findstring` parameter order (needle,haystack) that prevented silent mode from working correctly in all cases, as identified by Johannes. The condition now properly looks for 's' in MAKEFLAGS rather than the other way around. The commit message documents both the original issue and this final correction.

Key technical details:
- Build system change only affects output verbosity during make execution
- Fixes silent mode by correcting `findstring` parameter order to check 's' in MAKEFLAGS
- Maintains compatibility with `shared.mak` approach
- Changes confined to 6 lines (4 deletions, 2 additions)

Open questions:
- `shared.mak` dependency for standalone builds remains unresolved (deferred as separate issue)

Participant positions:
- Johannes Sixt: provided substantive technical review catching the inverted condition
- Harald Nordgren: responsive to feedback, incorporated all identified fixes through v3

The patch appears ready for merging, having addressed all substantive technical concerns while cleanly documenting the changes. The remaining standalone build question is properly deferred as it falls outside the scope of this silent mode fix. The thread demonstrates careful attention to build system edge cases and effective collaboration between contributor and reviewer.