CI / build system patch addressing silent mode behavior in git-gui's Makefile.

Johannes Sixt follows up on Harald Nordgren's patch to fix silent mode (`make -s`) behavior in git-gui's installation recipes. While acknowledging the issue is real, he raises two technical questions about the implementation:

1. The patch references `shared.mak` without clarifying this is a Git repository file, which might confuse those building git-gui standalone.

2. The old and new condition expressions for detecting silent mode differ in more than just `ifeq` vs. `ifneq` - the old checked `$(firstword -$(MAKEFLAGS))` against `s`, while the new checks if `s` appears in `$(firstword -$(MAKEFLAGS))`. Johannes asks whether this difference indicates the old check was incorrect.

The thread remains focused on build system details, with Johannes seeking clarification on the technical rationale behind the specific implementation choice. This appears to be a substantive review probing the correctness of the silent mode detection logic, though the patch itself remains small and focused on the git-gui/Makefile.