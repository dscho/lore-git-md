This is a follow-up in the worktree metadata tracking thread. The original patch proposed adding creation timestamps and user notes to worktrees, storing the notes in a new `note` file.

Norbert Kiesel, the patch author, responds to Junio C Hamano's suggestion to use the existing `description` file mechanism instead of introducing a new `note` file. Norbert agrees with the suggestion and indicates he'll update the patch to use `$GIT_COMMON_DIR/worktrees/$worktree/description` for storing worktree notes.

The response is brief and procedural - it doesn't reopen any design questions or propose alternatives, just confirms the author will implement the maintainer's suggested approach. This moves the thread forward toward a revised version that aligns with Git's existing conventions for repository descriptions.

The core functionality (tracking creation time and descriptions) remains unchanged, but the implementation will now be more consistent with how Git already handles repository identification. This appears to be the last open question before the patch can proceed toward merging.