This is a preparatory refactoring patch from Patrick Steinhardt's v2 series adding a "drop" subcommand to the experimental `git history` command. The patch splits out index handling logic from `repo_read_index_unmerged()` into a new standalone function `index_state_unmerged_to_stage0()` that can operate on any index state, not just the repository's main index. 

The change enables future code to process unmerged index entries in contexts beyond just reading the repository index. The patch also fixes a signed/unsigned integer mismatch in the index iteration. This is a mechanical refactoring with no behavior change, serving as groundwork for subsequent patches in the series that will need this functionality.

The patch demonstrates Patrick's typical careful approach - small, focused changes with clear commit messages explaining both the what and the why. The change touches core index handling code but is straightforward and well-contained, with the new function properly documented in the header file. This appears to be solid infrastructure work supporting the larger feature.