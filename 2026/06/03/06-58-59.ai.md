This is the first patch in a v2 series promoting the `b4` tool for Git mailing list workflows, focusing on documentation updates. Patrick Steinhardt proposes changing the threading model recommendation in `MyFirstContribution.adoc` from deep threading (where each version replies to the previous one) to shallow threading (where all versions reply to the initial cover letter).

The change aims to address readability concerns with deeply nested threads while maintaining traceability of patch series iterations. The patch cleanly modifies two lines in the tutorial to recommend always referencing the v1 Message-ID rather than the previous version's ID. This aligns with b4's default behavior and represents a concrete step toward resolving the threading model discussion from the v1 review.

As a documentation-only change that improves new contributor experience without altering technical behavior, this patch appears uncontroversial. It sets up the subsequent patches in the series by establishing the preferred threading model before introducing b4 configuration details. The change shows responsive iteration based on prior feedback while maintaining the series' goal of standardizing on b4 workflows.