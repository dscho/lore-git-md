This is a documentation patch that adds cross-references for `git replay` configuration between the main config documentation and the command-specific man page. It's part of Kristoffer Haugsbakk's v3 series to standardize `replay.refAction` documentation.

The patch mechanically includes the replay configuration section in both `git-config(1)` and `git-replay(1)` man pages, ensuring users can find the configuration options in both places. The change is straightforward - just adding two `include` directives to pull in the shared configuration documentation. This follows the project's documentation conventions where command-specific config options should be referenced in both the central config documentation and the command's man page.

The patch builds on prior work (commit 336ac90c) that originally added the replay config option but didn't properly link it into the documentation structure. Kristoffer discovered this omission while working on another topic, showing his systematic approach to documentation maintenance. The change has no functional impact - it purely improves discoverability of existing configuration options.

With this patch and the rest of the series, the `replay.refAction` documentation is now properly integrated and consistent across both man pages, following all of Junio Hamano's requested formatting conventions from earlier reviews. The patch appears ready for integration as part of the complete documentation standardization effort.