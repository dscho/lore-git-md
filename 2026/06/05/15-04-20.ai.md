This is a feature patch in the final stages of review for Harald Nordgren's series adding `git branch --prune-merged` functionality (with potential renaming to `--delete-forked` under discussion). The series has undergone 12 iterations and is now technically complete, with this patch implementing the core deletion behavior.

Phillip Wood provides final syntax refinements, focusing on two key points:
1. **Command naming**: Questions whether `--prune-merged` is the best name, suggesting `--delete-forked` as an alternative that better aligns with Git's existing terminology for branch deletion commands. He notes the current name doesn't accurately reflect that it doesn't delete branches listed by `--merged`.

2. **Implementation details**: Confirms the core logic is sound but notes the code will need adjustments for full refname matching and filtering placement in `apply_ref_filter()`. He indicates the remaining patches in the series look sensible without needing detailed review.

The patch itself implements safe automated branch cleanup with comprehensive safety checks:
- Only deletes branches whose tips are reachable from their upstream
- Preserves branches checked out in any worktree
- Skips branches with unresolvable upstreams
- Protects branches where push destination equals upstream (to avoid deleting freshly pulled trunks)
- Warns rather than errors on unmerged branches

The implementation shares infrastructure with the existing `--forked` filtering and goes through Git's standard `delete_branches()` path with appropriate flags. Extensive tests verify all safety mechanisms and edge cases.

This appears to be one of the final reviews before merging, with all major technical concerns having been addressed across the 12 versions. The remaining discussion focuses on command naming and minor syntax refinements rather than fundamental implementation issues.