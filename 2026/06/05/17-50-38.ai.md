This is a brief reply in the ongoing thread about adding `git branch --delete-forked` functionality (formerly called `--prune-merged`). Harald Nordgren responds to Phillip Wood's review of the first patch in the v12 iteration, which introduces the `--forked` filter for branch listing.

The key exchange here is about whether to support bare remote names (like "origin") as shorthand for `$remote/HEAD` to match `git checkout -b` behavior. Harald acknowledges the suggestion but indicates the implementation became simpler after removing this feature, suggesting it may not be included in the next version. This is a minor refinement point in an otherwise technically complete series that has already addressed all major concerns through 12 iterations.

The thread remains focused on final polishing before merging, with naming (`--delete-forked` now preferred) and small implementation details being the only remaining open questions. Harald's response signals he's considering Phillip's feedback but may prioritize implementation simplicity over full behavioral parity with `git checkout -b` in this case.