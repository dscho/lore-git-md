This is a review comment from Phillip Wood on Patrick Steinhardt's v2 patch series adding a "drop" subcommand to `git history`. The specific patch being reviewed modernizes the flag handling in Git's reset machinery by converting from `#define` macros to an `enum`.

Phillip suggests improving the naming of the `RESET_HEAD_ORIG_HEAD` flag, which was renamed from `RESET_ORIG_HEAD` as part of the modernization. He finds the new name hard to parse and proposes `RESET_HEAD_UPDATE_ORIG_HEAD` as a clearer alternative if the `RESET_HEAD_` prefix must be kept.

This is a substantive review focused on code readability and naming consistency - a typical example of Phillip's thorough reviewing style that often catches subtle but important details. The feedback is constructive and non-blocking, suggesting an improvement rather than identifying a functional issue. Patrick will need to decide whether to incorporate this naming suggestion in the upcoming v3 of the series.