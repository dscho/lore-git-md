This is patch 4/6 in Harald Nordgren's v13 series adding `git branch --prune-merged` for safe automated local branch cleanup. The series is in its final polishing phase before merging.

The patch introduces the core `--prune-merged` functionality that deletes local branches whose work has been integrated into their upstream. It builds on the `--forked` filter capability added in earlier patches, now applying it with safety checks to determine which branches can be safely removed. The implementation:

1. Takes branch patterns as positional arguments (e.g. `git branch --prune-merged origin/main 'topic*'`)
2. Preserves branches that are checked out, have missing upstreams, or push to the same location as their upstream
3. Warns rather than fails on unmerged branches during bulk operations
4. Uses the shared `filter_refs()` infrastructure for branch collection
5. Includes comprehensive tests covering all edge cases (checked-out branches, missing upstreams, push/upstream equality, etc.)

The safety model has been refined through 13 iterations of review, with this version incorporating feedback to:
- The branch pattern handling (now using `strvec`)
- The warning behavior for unmerged branches
- The upstream/push comparison logic
- The dry-run and warn-only modes added in previous patches

The documentation clearly explains the behavior and safety mechanisms, including the three cases where branches are preserved. The test suite verifies all key scenarios with particular attention to the push/upstream distinction that identifies fork workflow branches versus mainline tracking branches.

This appears to be the final implementation of the feature, with only minor documentation and code organization refinements remaining in the series. The patch maintains all the safety guarantees established through earlier review while providing the core branch cleanup functionality.