This is a naming discussion for Harald Nordgren's `git branch --prune-merged` feature, which is now in its 12th iteration and technically complete. The series adds safe automated cleanup of local branches that have been merged upstream, with comprehensive safety checks to prevent accidental deletions.

Phillip Wood suggests renaming the option to `--delete-forked` to better align with Git's existing terminology (which uses "delete" rather than "prune" for branch removal operations) and because the current name might imply it deletes branches listed by `--merged` (which it doesn't). However, he notes this alternative name doesn't fully capture the "merged" aspect of the functionality.

The discussion is now focused on final naming and syntax refinements, with all major technical concerns having been addressed through the extensive review process. The feature includes robust protections against deleting checked-out branches, branches with unresolved upstreams, or branches that push back to their upstream (to avoid deleting main branches that just look "fully merged").

This represents one of the final steps before the series, which has evolved through careful iteration to provide a safe, configurable way to automate branch maintenance in fork-based workflows. The naming question is the last substantive issue to resolve before merging.