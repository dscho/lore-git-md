Platform compatibility patch refining Clang version detection for unused parameter warnings.

This v2 patch from first-time contributor Dominik Loidolt implements the maintainer-requested changes to properly handle Clang's version requirements for the `UNUSED` macro. The patch introduces a new `GIT_CLANG_PREREQ` macro that mirrors the existing `GIT_GNUC_PREREQ` pattern, then uses it to enable the more descriptive "parameter declared as UNUSED" warning for Clang 2.9+ (released 2011) while maintaining the current behavior for older versions.

The change is small and focused, touching only the compiler attribute logic in `compat/posix.h`. The implementation follows Git's standard pattern for version checks and addresses Junio Hamano's review feedback from the initial submission. The patch maintains careful backwards compatibility while improving warning messages for modern Clang versions, demonstrating good attention to Git's platform support requirements.

This appears ready for inclusion - the technical approach is sound, the change is well-contained, and the contributor has responded constructively to review feedback. The patch represents a straightforward but useful improvement to developer experience when working with unused parameters under Clang.