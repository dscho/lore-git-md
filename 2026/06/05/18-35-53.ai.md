This is the final patch in Harald Nordgren's series adding `git branch --prune-merged` for safe automated local branch cleanup. The patch introduces a `--dry-run` option that shows which branches would be deleted without actually removing them, completing the safety features for this new functionality.

The implementation:
- Adds `--dry-run` flag that works only with `--prune-merged`
- Prints "Would delete branch <name>" for each candidate branch
- Maintains all existing safety checks during dry-run
- Includes comprehensive tests verifying dry-run behavior
- Rejects `--dry-run` when used without `--prune-merged`

The dry-run mode is particularly useful for previewing deletions when using branch patterns like `origin/*` before executing the actual cleanup. This final patch rounds out the series by providing visibility into what the command would do, complementing the existing safety mechanisms (warn-only mode, configurable opt-outs, and upstream verification).

With this addition, the series appears complete and ready for merging, having gone through 13 iterations of refinement with all major technical concerns addressed. The implementation preserves the safety guarantees while providing flexible control over branch cleanup operations.