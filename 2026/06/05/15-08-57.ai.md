This is a review comment from Phillip Wood on Patrick Steinhardt's v2 patch series for the `git history drop` feature. The email focuses on a specific naming concern in patch 3/9, which modernizes the flags passed to `reset_head()`.

Phillip suggests that the new flag name `RESET_HEAD_ORIG_HEAD` (which replaced `RESET_ORIG_HEAD`) is hard to parse. He proposes `RESET_HEAD_UPDATE_ORIG_HEAD` as a clearer alternative if the `RESET_HEAD_` prefix must be kept. This is a surface-level review focused on naming clarity rather than functionality.

The comment is brief and constructive, offering a concrete suggestion while acknowledging the context of the change (the flag prefix standardization). It doesn't require any architectural reconsideration - just a potential naming tweak that would make the code more readable. Patrick will need to decide whether to incorporate this suggestion in v3 revision of the series.

This feedback fits within the broader thread context where Patrick is modernizing the reset machinery's flag handling to support the new drop functionality while maintaining consistency with Git's evolving conventions. The review doesn't challenge the technical approach but offers a small improvement to the implementation details.