This thread is about Matt Hunter’s 7-patch series (v2) that adds a global `fetch.followRemoteHEAD` configuration option. The goal is to let users set a default fetch behavior across all remotes while still allowing per-remote overrides, following the same pattern as `fetch.prune` and `remote.*.prune`.

The implementation is complete and addresses all prior review feedback. Key details include a new global setting that acts as a fallback when no remote-specific setting exists, case-sensitive value matching, and thorough test coverage (including edge cases like dangling symrefs and refspec fetches). Documentation is fully updated, including man pages, configuration behavior, and advice subsystem entries. Junio Hamano and Jeff King have reviewed the core design, and all technical concerns—such as case sensitivity and error handling—have been resolved.

The series is now ready for final review and merging. The only remaining discussion points are minor: documentation notes an edge case around remote definition behavior (left unchanged as a historical artifact), and there’s a possible future effort to unify clone behavior (distinct from this fetch-focused work). The latest patch is a trivial refactor (renaming `report_set_head` to `warn_set_head`) to improve naming consistency, with no functional changes.