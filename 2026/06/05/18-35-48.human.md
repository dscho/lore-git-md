This is the first patch in Harald Nordgren's v13 series adding `git branch --delete-forked` (formerly `--prune-merged`), which provides safe automated cleanup of local branches. The series has undergone extensive review over 13 iterations and is now in final polishing before merging.

This particular patch introduces the core filtering mechanism - a new `--forked` option for `git branch --list` that shows only branches whose upstream matches a given branch or pattern. The filter supports both exact refnames (like "origin/main") and glob patterns (like "origin/*"), and can be combined with other filters like `--merged`. The implementation integrates cleanly with Git's existing ref-filter infrastructure.

Key technical aspects:
- The filter is implemented in `ref-filter.c` with proper handling of both exact and glob matches
- Upstream resolution follows Git's standard branch tracking rules
- The option can be repeated to widen the filter criteria
- Includes comprehensive tests covering various matching scenarios and edge cases

The patch appears well-crafted with thorough test coverage. It builds on established Git patterns while introducing the new filtering capability needed for the larger branch cleanup feature. As the foundational piece of the series, it has been refined through multiple rounds of review and appears technically sound.