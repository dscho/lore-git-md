This is a review comment on patch 2/6 in Harald Nordgren's v12 series adding `git branch --prune-merged` functionality. The series aims to provide safe automated local branch cleanup with comprehensive safety checks.

Phillip Wood suggests a small but meaningful implementation refinement: replacing the three boolean parameters in `check_branch_commit()` with a single `flags` bitfield. This would improve the function's signature and avoid the need for individual parameter comments in later patches. The change would maintain all existing functionality while making the code more maintainable.

The review is substantive but focused - it identifies a specific opportunity to improve the code organization without questioning the overall approach. Phillip notes this is building on Harald's work to add warning-only mode for unmerged branches during bulk operations, a feature that was well-received in prior iterations of the series.

This type of focused, implementation-level feedback is typical of the final polishing stage of a mature patch series where the core design has already been vetted through multiple rounds of review. The suggestion aligns with Git's coding conventions around flags parameters and would make the interface more consistent with other parts of the codebase.