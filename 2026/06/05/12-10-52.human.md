This is a continuation of the discussion about submodule mirroring for archival purposes, with Simon Richter responding to Benson Muite's proposal for fallback URLs in `.gitmodules`. Simon highlights a fundamental tension in how Git currently handles submodule URLs - they're treated as versioned data in `.gitmodules` rather than as configurable metadata.

The key insight is that absolute URLs in `.gitmodules` break mirroring use cases because they hardcode the source location, forcing mirrors to either rewrite history (problematic) or rely on fragile `insteadOf` hacks. Simon argues this conflates two concerns: the project's identity (which should be stable) and its hosting location (which may change).

The email doesn't propose a concrete solution but reinforces the need for a way to decouple submodule identity from location, connecting this to broader discussions about project identity in Git. The thread now has three technical approaches on the table (repository-level mirroring, submodule fallback URLs, and remote URL reuse), with Simon's input helping clarify why the current system is problematic for archival workflows.

This moves the discussion forward by sharpening the core issue - the mismatch between how Git treats URLs (mutable configuration) versus how they're actually used (as immutable identifiers) - while still leaving open how best to resolve it. The archival use case remains the driving motivation, with Debian's needs as a concrete example.