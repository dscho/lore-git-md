This is a documentation patch that adds missing configuration documentation links for `git replay`. It's part of a 4-patch series (v3) to standardize the `replay.refAction` documentation between man pages.

The patch adds the missing `include::config/replay.adoc[]` directive in both `git-config(1)` and `git-replay(1)` man pages. This ensures the configuration options for `git replay` appear in both places, fixing an oversight from when the `replay.refAction` option was originally added in commit 336ac90c.

The change is purely mechanical - it just links existing documentation that wasn't properly included before. The patch follows the project's documentation conventions by using the standard include directive format seen throughout the config files. This is the first step in Kristoffer's series to fully synchronize and standardize the replay configuration documentation.

The patch has already been reviewed in previous iterations, with maintainer feedback incorporated. It represents a straightforward, uncontroversial documentation improvement that makes the configuration options more discoverable.