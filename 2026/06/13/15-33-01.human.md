This is a refactoring patch in the ongoing `the_repository` removal effort, moving the `protect_hfs` and `protect_ntfs` configuration variables from global scope into repository-specific storage. The patch has already been merged into the `next` branch after thorough review.

The changes in this final v3 version are minimal administrative tweaks - just updating the commit subject line and a reference link. The substantive technical work was completed in v2, which introduced helper functions `repo_protect_hfs()` and `repo_protect_ntfs()` to maintain all existing filesystem protection behavior while moving the variables into the `repo_config_values` struct. The patch preserves special case handling for uninitialized repositories and maintains identical Windows path validation semantics.

This is a well-executed step in the larger effort to eliminate global variables from Git's codebase. The patch follows established patterns from prior migrations and has received positive review from multiple mentors (Christian Couder, Ayush Chandekar, Olamide Caleb Bello) as well as maintainer approval from Junio Hamano. The thread demonstrates Git's typical workflow where significant review happens early, leaving only minor polish for the final version.