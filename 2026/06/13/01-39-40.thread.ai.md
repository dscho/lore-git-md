Thread identity: Feature proposal to add environment variable support for shallow clones via `GIT_CLONE_DEPTH` (renamed from initial `DEPTH` proposal), allowing shallow clones when users don't control the clone command directly (e.g. in build scripts/CI). Originating author identified as Matt Hunter.

Core problem/goal: Enable shallow clones through environment variables for automation scenarios where users can't specify `--depth` directly. Current implementation in `builtin/clone.c` needs naming convention and memory management fixes.

Current status: v1 proposal updated with two substantive changes - variable renamed to `GIT_CLONE_DEPTH` to match Git conventions, and memory leak fixed where depth string wasn't freed. No maintainer feedback yet.

Key technical details: Modifies `builtin/clone.c` to:
1. Check `GIT_CLONE_DEPTH` environment variable when `--depth` isn't provided
2. Properly free allocated depth string using existing cleanup infrastructure
3. Apply same validation as `--depth` option
No other subsystems affected.

Open questions/loose ends: Still needs:
1. Documentation updates for new `GIT_CLONE_DEPTH` variable
2. Potential maintainer feedback on approach
3. Confirmation that environment variable precedence matches Git norms

Participant positions: Matt Hunter (author) driving the feature and addressing initial feedback. First review identified naming convention violation and memory leak - substantive technical issues requiring changes to original implementation.

Related work: Builds on existing shallow clone functionality while making it more flexible for automation. Follows Git's pattern of `GIT_`-prefixed environment variables for configuration.