Thread identity: Feature proposal to add environment variable support for shallow clones via `DEPTH` variable, allowing shallow clones when users don't control the clone command directly (e.g. in build scripts/CI). Originating author not yet identified from this initial patch.

Core problem/goal: Currently shallow clones can only be requested via `--depth` command-line option, with no configuration or environment variable alternative. This change would allow setting `DEPTH=1` once to make all nested clones shallow in automation scenarios.

Current status: Initial proposal (v1) implementing basic functionality in `builtin/clone.c` where environment `DEPTH` is checked if `--depth` wasn't provided, with command-line taking precedence. No reviews or discussion yet.

Key technical details: Touches only `builtin/clone.c`, adding 6 lines to read `DEPTH` environment variable and apply same validation as `--depth`. No other files or subsystems affected.

Open questions/loose ends: Needs documentation updates (man pages) to advertise new environment variable support. No design objections or alternative approaches raised yet.

Participant positions: No reviews or other participants yet. Author makes case for usefulness in automation/CI scenarios where users don't control clone commands directly.

Related work: Builds on existing shallow clone functionality (`--depth` option but makes it more flexible for automation use cases.