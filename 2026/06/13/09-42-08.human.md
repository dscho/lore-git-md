This is a performance optimization discussion for Git's merge-base calculation algorithm, specifically the `paint_down_to_common` function in commit-reach.c. Kristofer Karlsson provides concrete measurements showing how the current inefficiency manifests in real-world repository shapes, with a 10ms vs 4.85s difference between two graph topologies that should theoretically have similar performance.

The key insight is that Git currently walks unnecessary portions of the commit graph when calculating merge bases, particularly in repositories with complex merge histories (like monorepos or release-branch workflows). Karlsson demonstrates this with two test cases - one where the optimization would help (Shape 2 with a merge bypass) and one where it wouldn't (simple Shape 1). The 485x slowdown in the problematic case comes from Git continuing to traverse an entire branch's history even after the relevant merge base has been found.

Derrick Stolee had previously suggested alternative approaches like depth-first search, but after deeper discussion both agree that Karlsson's proposed single-sided termination condition is the better path forward. Karlsson has now formalized the correctness argument in a detailed proof (linked in the email) covering edge cases like generation number handling and stale commit propagation.

The discussion has progressed from theoretical proposal to concrete validation, with both contributors operating at a high technical level. The optimization appears particularly promising for large repositories with complex histories, where merge-base calculations are currently a bottleneck. Next steps would involve implementing and testing the change against real-world repository topologies to verify the expected performance gains.