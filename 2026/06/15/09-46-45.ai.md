This is a review of patch 1/6 in Harald Nordgren's v14 series adding `git branch --delete-forked` functionality. Phillip Wood provides substantive feedback on the implementation details and test coverage.

The review focuses on two main areas for improvement in the `--forked` filter implementation:

1. **Upstream shorthand support**: Suggests adding support for "origin" as shorthand for the branch origin/HEAD points to, matching the behavior of `git checkout -b`. This would provide more consistent user experience.

2. **Test coverage gaps**: Notes several areas where the test suite could be strengthened:
   - The test setup has all local branches pointing directly at their upstreams, which isn't realistic - suggests adding some local commits
   - Missing test coverage for combining `--forked` with explicit branch name arguments
   - Clarifies that the "detached" branch name in tests could be confusing since it typically refers to HEAD state rather than a branch name

Phillip also observes that the refactoring into ref-filter.c has reduced the patch size by about 50 lines while maintaining all functionality. The review is thorough but constructive, identifying specific areas for improvement while acknowledging the overall soundness of the implementation.

This continues the pattern from the thread summary where Phillip Wood is providing detailed implementation feedback to refine the series before final inclusion. The suggestions here are focused on making the feature more robust and user-friendly rather than challenging the core approach.