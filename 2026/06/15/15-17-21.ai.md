This is a substantive review of Harald Nordgren's `git rebase --squash` proposal, suggesting an alternative approach via a hypothetical `git history squash` command. D. Ben Knoble engages with the core use case while questioning whether rebase is the right vehicle for this functionality.

The review acknowledges the counting problem Harald identified with `reset --soft` but offers alternative revision selection methods (`rev-list`, pattern-based revisions). More significantly, it proposes moving the squash operation out of rebase entirely into a new `git history` subcommand, arguing this would provide more flexible range selection and potentially cleaner merge commit handling.

The suggestion to involve Patrick Steinhardt (cc'd) reflects awareness that `git history` is Patrick's domain, positioning this as a potential extension to that experimental command rather than a rebase feature. The review is technically substantive, proposing concrete alternatives while recognizing open questions around merge handling. It moves the discussion forward by offering a different architectural approach rather than just critiquing the current proposal.