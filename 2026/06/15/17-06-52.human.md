Performance optimization follow-up for `git cat-file` formatting. Jeff King (Peff) continues exploring potential speed improvements beyond René Scharfe's initial 3-7% optimization by testing an alternative approach that replaces callback functions with a switch statement. His benchmarks show no additional performance gain from this change, confirming that the callback-based design from his earlier prototype remains the faster approach (though still only marginally better than René's solution for custom formats.

Peff concludes that while pre-parsing format strings could yield small additional gains (3-15% depending on use case), the complexity likely isn't justified for `cat-file` alone. He suggests the more substantial architectural change would make sense only as part of a broader unification of format parsing across Git commands like `ref-filter` and `--pretty=format`.

This is a technical deep-dive that maintains the constructive tone of the thread, with Peff transparently sharing negative results (the switch statement not helping) while keeping sight of the bigger picture around format parsing in Git. The discussion remains focused on measurable performance characteristics rather than style preferences.