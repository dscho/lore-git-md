This is a simple bugfix for Git's `.gitattributes` file, correcting a typo in the line-ending normalization attribute for Perl scripts. The patch changes `eof=lf` to `eol=lf` for `.pl` files, matching the established pattern used for `.perl` and `.pm` files.

The v2 update addresses Patrick Steinhardt's review feedback by using the contributor's full name (Koutian Wu) rather than just the GitHub username in the commit metadata. The technical content remains unchanged - this is still a straightforward one-line correction to use the proper `eol` attribute instead of the non-existent `eof` variant.

The change ensures Perl scripts will be checked out with consistent LF line endings, aligning with Git's existing conventions for similar file types. The patch appears ready for merging now that the authorship naming convention has been corrected. No further technical discussion is expected given the narrow scope and uncontroversial nature of the fix.