Performance optimization discussion for `git cat-file` batch formatting. Jeff King (Peff) responds to René Scharfe's patch that optimized the default batch format output by replacing `strbuf_addf()` with more efficient string operations. While acknowledging René's improvement is "obviously right", Peff explores whether further optimization is possible by pre-parsing the format string into a tree of callbacks.

Peff shares benchmark results showing his experimental approach provides a 3% speedup for custom formats (600ms vs 618ms), though it remains slightly slower than the already-optimized default format path (580ms). The proposed implementation would replace the current linear format expansion with a parsed representation that could theoretically handle more complex formats in the future.

This is a substantive technical discussion between two experienced contributors about micro-optimizations in a performance-critical path. Peff's response demonstrates deep knowledge of string formatting internals while maintaining a pragmatic perspective - he clearly labels his ideas as speculative" and includes working code to ground the discussion. The exchange highlights the careful balance Git maintainers strike between performance gains and code maintainability.