This thread began with Harald Nordgren’s patch to add `git rebase --abort` hints to failure and status messages, aiming to improve consistency and discoverability for new users. The uncontroversial part—adding the hint to rebase status messages—remains unchanged, but the exec-failure addition has sparked deeper debate.

The core disagreement now centers on whether the hint should appear for *all* exec failures. Harald argues it should, framing it as a universal "escape hatch" for users stuck in a rebase. Phillip Wood and Junio, however, argue that aborting is rarely the right response to exec failures, which are often recoverable (e.g., missing files) or better handled by `--skip` or editing the todo list. Junio’s latest reply goes further, questioning whether the patch’s target workflow—aborting a rebase after a single exec failure—is even a "normal" use case for `git rebase`. He suggests such users might be misusing rebase as a glorified loop for running checks, implying the patch might cater to a niche workflow that could be better served by a dedicated tool.

The patch itself is technically sound: it splits exec-failure messages into a failure-specific warning and a neutral `--abort` hint, suppressing the hint for conflict-induced failures. But the discussion has shifted from implementation details to fundamental questions: Is the hint’s scope justified? Should users be guided toward `--skip` or todo-list editing instead? And is the underlying workflow even valid for `git rebase`? No new code or tests have been proposed, and the thread remains open-ended, with no clear consensus on how—or whether—to proceed.