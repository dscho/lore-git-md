This is a simple bugfix for Git's `.gitattributes` file, correcting a typo in the line-ending normalization attribute for Perl scripts. The patch changes `eof=lf` to `eol=lf` for `.pl` files, matching the established pattern used for `.perl` and `.pm` files.

The v2 update addresses Patrick Steinhardt's review feedback by using the author's full name (Koutian Wu) rather than just the GitHub handle in the commit message and sign-off. The technical content remains unchanged - this is still a straightforward one-line correction that ensures Perl scripts will be checked out with consistent LF line endings.

The fix is clearly correct and uncontroversial, aligning with existing conventions in the file. With the authorship naming convention now properly followed, this patch appears ready for merging. No further discussion seems necessary for such a minor but clear improvement.