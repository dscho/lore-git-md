This is a small bugfix for Git's `.gitattributes` file, correcting the line-ending normalization attribute for Perl scripts.

The patch fixes an incorrect `eof=lf` attribute (which isn't a valid built-in attribute) for `.pl` files, changing it to `eol=lf` to match the neighboring rules for `.perl` and `.pm` files. This ensures Perl scripts will be checked out with LF line endings consistently.

The change is straightforward and clearly motivated - the commit message explains that `eof=lf` wasn't the intended attribute and shows the correct form is already used for similar file types. The one-line diff touches only the relevant part of `.gitattributes` without introducing any other changes.

This appears to be a simple, uncontroversial fix that aligns with Git's existing line-ending handling conventions. The patch is well-formed with a proper commit message and sign-off. No review discussion is included in the provided context, but the change seems unlikely to generate controversy given its narrow scope and clear purpose.