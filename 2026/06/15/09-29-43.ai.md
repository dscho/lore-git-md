This is a brief exchange about a minor documentation suggestion in Johannes Schindelin's series to handle large (>4GB) objects on Windows. The patch in question (4/7) converts `unpack_entry()` and related functions to use `size_t` for object sizes rather than `unsigned long`.

Patrick Steinhardt had previously suggested adding a `NEEDSWORK` comment in `fast-import.c` to document the transitional size casting that maintains compatibility during the conversion. Dscho responds with a lighthearted comment that the `cast_size_t_to_ulong()` function name itself serves as an implicit "NEEDSWORK" marker.

The exchange confirms the temporary nature of this casting code while showing the authors' shared understanding that it will be removed once the full conversion to `size_t` is complete. This follows the pattern seen throughout the series where transitional scaffolding is carefully maintained during the conversion process. The tone remains collegial, with Dscho acknowledging the suggestion while indicating the existing code already signals its temporary status.