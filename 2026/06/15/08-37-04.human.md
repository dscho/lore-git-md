This is a test cleanup patch in Harald Nordgren's series introducing a new `--squash` option for `git rebase`. The patch fixes a test hygiene issue where a `prepare-commit-msg` hook was left lingering after the "pick and fixup respect commit.cleanup" test in t3415-rebase-autosquash.sh.

The change is straightforward - it adds `test_when_finished` to ensure the hook is removed after the test completes, preventing it from affecting subsequent tests. This is a common pattern in the Git test suite to maintain isolation between test cases.

As part of the larger series (which proposes a new rebase feature to squash commit ranges while preserving messages), this patch represents a minor but important test improvement that was likely identified during review. The change is uncontroversial and aligns with established test suite practices.