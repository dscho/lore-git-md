This thread addresses a straightforward bugfix for Git's `.gitattributes` file. The patch corrects a typo in the line-ending normalization attribute for Perl scripts, changing `eof=lf` (an invalid attribute) to the correct `eol=lf` form. The fix aligns with the existing attributes for similar Perl-related file types (`.perl` and `.pm`) and ensures consistent LF line endings for checked-out Perl scripts. The change is small, clearly motivated, and uncontroversial - the one-line diff precisely targets the incorrect attribute without introducing other modifications. The patch appears ready for merging as-is.