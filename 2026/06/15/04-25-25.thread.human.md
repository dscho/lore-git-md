This thread addresses a straightforward bugfix in Git's `.gitattributes` file, correcting a typo in the line-ending normalization attribute for Perl scripts. The original patch changed `eof=lf` to `eol=lf` for `.pl` files to match the established pattern used for `.perl` and `.pm` files, ensuring consistent LF line endings when checking out Perl scripts.

Patrick Steinhardt reviewed the initial submission, confirming the technical correctness of the change while noting the commit should use the contributor's full name (Koutian Wu) rather than just a GitHub username. The v2 patch addresses this feedback by updating the authorship metadata while keeping the same one-line correction. With this process issue resolved and no technical concerns raised, the patch appears ready for merging. Given the narrow scope and uncontroversial nature of the fix, no further discussion is expected.