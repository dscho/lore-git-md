This is a platform compatibility discussion in the ongoing thread about handling large (>4GB) objects on Windows (LLP64) systems. Patrick Steinhardt raises a technical consideration about the series' approach of converting from `unsigned long` to `size_t` for object header lengths.

Patrick questions whether introducing a platform-independent typedef like `objsize_t` (as `uint64_t`) would be preferable to using `size_t`, given that the series aims to ensure consistent behavior across platforms. He speculates whether any supported platforms still use 32-bit `size_t` and whether such systems could realistically handle large objects anyway.

The comment doesn't propose a concrete change to the current patches but rather opens a design consideration about type consistency. This comes after the series has already been validated in production via Git for Windows, with Johannes Schindelin's sign-off indicating the `size_t` approach works in practice. The thread context shows this is a mature series that's awaiting rebase, so Patrick's comment appears to be a last-minute architectural consideration rather than a blocking concern.