Thread identity: Documentation patch adding `git rebase --abort` hints to failure and status messages, authored by Harald Nordgren.

Core problem or goal: Improve rebase user interface consistency by adding `--abort` advice in two scenarios where only `--continue` was previously mentioned - when an "exec" step fails during rebase, and in status help text during commit splitting/editing.

Current status: Initial version (v1) of patch series. Changes are mechanical additions of parallel advice text to match existing patterns for conflict cases.

Key technical details: Modifies three files - `sequencer.c` adds abort advice to exec step failure messages, `wt-status.c` includes abort option in rebase status messages, and `t/t7512-status-help.sh` tests verify the new messages appear correctly. The changes are straightforward text updates without behavioral modifications.

Participant positions: No reviews or discussion yet (initial patch submission). The changes follow established patterns from similar rebase message improvements.