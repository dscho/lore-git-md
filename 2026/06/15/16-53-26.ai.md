Performance optimization discussion for `git cat-file` batch formatting. Jeff King (Peff) responds to René Scharfe's patch that optimized the default format output by replacing `strbuf_addf()` with more efficient alternatives. While acknowledging René's improvement (3-7% speedup), Peff explores whether a more radical parser redesign could yield further gains by pre-parsing format strings into a callback structure.

Peff shares benchmark results showing his prototype achieves an additional 3% speedup for custom formats (600ms vs 618ms), though it remains slightly slower than the optimized default format path. His approach would replace the current linear string expansion with a parsed format tree, similar to ref-filter's architecture but with less overhead. The patch demonstrates this with atom-specific callbacks for each format placeholder.

This is a substantive technical review that digs into deeper optimization possibilities while maintaining the original improvement. Peff positions his exploration as speculative ("nerd-sniping") rather than a direct critique, noting the tradeoffs between parser complexity and performance. The discussion highlights ongoing interest in optimizing Git's bulk object query paths, with both approaches showing measurable gains over the status quo.