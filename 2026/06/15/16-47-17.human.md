This is a refactoring patch in Harald Nordgren's `git branch --delete-merged` series (v15 2/7), which introduces safe automated local branch cleanup functionality. The patch converts the `delete_branches()` and `check_branch_commit()` functions from using separate boolean parameters to a single bitmask flags argument.

The change is purely mechanical - no behavior is altered. The author replaces the `force` and `quiet` boolean parameters with a flags enum (`DELETE_BRANCH_FORCE` and `DELETE_BRANCH_QUIET`), then splits them back into local boolean variables within the functions to maintain the same readable control flow. This prepares for future flags to be added without growing the function signatures further.

The patch is clean and focused, with the diff showing straightforward conversions while preserving all existing logic. This is typical preparatory work for the larger feature being developed in the series. The commit message clearly explains the motivation (avoiding parameter bloat) and confirms no functional changes.