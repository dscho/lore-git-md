This is a minor test infrastructure fix from Junio C Hamano, addressing a linting issue in Taylor Blau's patch series that integrates `--delta-islands` with `--path-walk` in `git pack-objects`. The series has already undergone substantive technical review, with the current focus being on optimization validation.

Junio points out that the test script uses raw `grep` rather than the preferred `test_grep` helper function, which would trigger the project's updated test linter. This is purely a style/consistency fix - the test's functionality remains correct, it just needs to use the project's standard test helper for better error reporting and consistency with other tests.

The change is trivial and uncontroversial, falling under the "contributor housekeeping" category. It doesn't affect the core functionality being developed in the series, which has already demonstrated significant performance improvements (99.6% time reduction in some cases) by enabling bitmap usage with path-walk operations. Junio's comment is the kind of minor polish the maintainer often provides before queuing a patch series.