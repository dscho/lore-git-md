Thread identity: Bugfix for `.gitattributes` line-ending normalization, correcting `eof=lf` to `eol=lf` for Perl scripts (`.pl` files), now at v2 with Reviewed-by.

Core problem: The `.gitattributes` file contained an invalid `eof=lf` attribute for Perl scripts, which has been corrected to `eol=lf` to match the established pattern for `.perl` and `.pm` files, ensuring consistent LF line endings when checking out Perl scripts.

Current status: v2 patch has received Patrick Steinhardt's Reviewed-by after addressing authorship naming convention feedback. The technical content remains a straightforward one-line correction. No further changes expected.

Key technical details: Changes one line in `.gitattributes` to use the correct `eol=lf` attribute for `.pl` files, matching the pattern for other Perl-related extensions (`.perl`, `.pm`). The fix is minimal and maintains consistency with existing line-ending rules.

Participant positions: Patrick Steinhardt provided the only review, confirming technical correctness in v1 and noting the authorship naming convention (addressed in v2), then gave Reviewed-by in final approval. The review was surface-level (process compliance) with no technical objections raised.

Related work: The change aligns with existing line-ending normalization rules in the same file for similar file types. No other related patches or discussions are referenced. The patch is now ready for merging.