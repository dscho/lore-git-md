Thread identity: Bugfix for `.gitattributes` line-ending normalization, correcting `eof=lf` to `eol=lf` for Perl scripts (`.pl` files).

Core problem: The `.gitattributes` file contained an incorrect attribute `eof=lf` (invalid syntax) for Perl scripts, which should have been `eol=lf` to match the neighboring rules for `.perl` and `.pm` files. This ensures consistent LF line endings when checking out Perl scripts.

Current status: Single patch fixing the incorrect attribute, properly signed off. No version history as this appears to be a one-off fix.

Key technical details: Changes one line in `.gitattributes` to use the correct `eol=lf` attribute for `.pl` files, matching the established pattern for other Perl-related extensions (`.perl`, `.pm`).

Participant positions: No reviews or discussion yet, but the change is straightforward and aligns with existing conventions. The commit message clearly explains the fix.