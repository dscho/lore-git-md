Thread identity: Bugfix for `.gitattributes` line-ending normalization, correcting `eof=lf` to `eol=lf` for Perl scripts (`.pl` files), now at v2.

Core problem: The `.gitattributes` file contained an invalid `eof=lf` attribute for Perl scripts, which has been corrected to `eol=lf` to match the established pattern for `.perl` and `.pm` files, ensuring consistent LF line endings when checking out Perl scripts.

Current status: v2 patch fixes the incorrect attribute with proper authorship (Koutian Wu) as requested in review. The technical content remains unchanged - a straightforward one-line correction. Patrick Steinhardt's review has been addressed and no further changes are expected.

Key technical details: Changes one line in `.gitattributes` to use the correct `eol=lf` attribute for `.pl` files, matching the pattern for other Perl-related extensions (`.perl`, `.pm`). The fix is minimal and maintains consistency with existing line-ending rules.

Participant positions: Patrick Steinhardt provided the only review, confirming technical correctness in v1 and noting the authorship naming convention, which has been addressed in v2. No other participants have commented. The review was surface-level (process compliance) with no technical objections raised.

Related work: The change aligns with existing line-ending normalization rules in the same file for similar file types. No other related patches or discussions are referenced. The patch appears ready for merging.