This is a **substantive review** of the stacked-branch protection mechanism in Harald Nordgren’s `git branch --delete-merged` series, now at v17. Phillip Wood, a key reviewer with deep expertise in Git’s branch and sequencer subsystems, raises two technical concerns about the implementation of upstream config clearing—a core safety feature designed to prevent stale references when merged branches are used as upstreams for unmerged branches.

First, Phillip notes that the current v17 patch appears to **preserve all upstreams indiscriminately**, rather than selectively clearing the upstream configuration of merged branches that cannot be deleted due to dependencies. The intended behavior is to clear `branch.<name>.remote` and `branch.<name>.merge` for merged branches that are upstreams of unmerged branches, while leaving the upstreams of unmerged branches intact. The current implementation may be treating all upstreams as protected, which would defeat the purpose of the safety mechanism.

Second, he flags an **efficiency issue**: the patch builds a separate list of upstream branches, rather than filtering the deletion candidates directly in the `refs_for_each_branch_ref()` callback. This approach is described as "wasteful," likely because it requires an additional pass over the refs and extra memory allocation, when the logic could be integrated into the existing iteration.

The review is brief but pointed, focusing on correctness and performance—both critical for a command that could affect many branches in a repository. Phillip’s feedback suggests the v17 implementation may not yet fully align with the agreed design (upstream clearing for merged branches only), and the extra data structure could introduce unnecessary overhead. This is not a surface-level nit; it goes to the heart of whether the safety mechanism works as intended and scales efficiently.

For a reader deciding whether to engage with the series, this email signals that **the stacked-branch protection logic may still need refinement**, even though the series is otherwise feature-complete. The thread context confirms that upstream clearing was the final design resolution, so resolving these issues is likely the last hurdle before the series can graduate to `next`. If the concerns are addressed cleanly, the series looks poised to land; if not, the discussion may reopen the trade-off between simplicity and functionality.