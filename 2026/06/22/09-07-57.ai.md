This is the **stacked-branch protection design discussion** for Harald Nordgren’s `git branch --delete-merged` series (now at v17). Phillip Wood’s email refines the safety mechanism for branches used as upstreams for other branches (e.g., `feature1` if `feature2` stacks on it). The key update: Phillip now **endorses the "abort-and-notify" approach** as the default behavior, citing concerns that automatic retargeting (e.g., updating `feature2@{u}` to `origin/master`) would break `git rebase --fork-point` by introducing stale commits. He also confirms the protection should apply uniformly to both `--delete-merged` and `-d`, ensuring consistency across branch deletion commands.

The email advances the thread by resolving the last major design ambiguity: **how to handle dependent branches when deleting a merged upstream**. Phillip’s preference for aborting (rather than retargeting) aligns with Junio’s earlier safety-first stance and simplifies implementation. The next iteration (v17) will likely implement this as a hard failure with a clear error message (e.g., "Cannot delete 'feature1' because 'feature2' depends on it"), preserving backward compatibility for stacked workflows. No new technical debt is introduced, and the solution avoids edge cases like stale rebase commits. This paves the way for final review of the series, which now includes all planned features (per-branch opt-out, `--dry-run`, and stacked-branch protection).