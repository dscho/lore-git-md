This is the v4 cover letter for the `--linearize` option in `git replay`, a feature addition that flattens merge commits to match `git rebase --no-rebase-merges`. The series remains a three-patch set: two refactoring patches (unchanged since v3) and the core implementation patch, now with a minor test adjustment to use `test_grep` instead of a bare `grep` in preparation for `mm/test-grep-lint`. The change is purely mechanical and does not affect functionality or documentation.

The series is now fully reviewed and addresses all prior feedback: Junio’s surface-level comments, Elijah Newren’s substantive review (clarity, edge cases, test coverage), and Justin Tobler’s design concern about extensibility. The test suite has been expanded to verify `--linearize` with `--advance`, `--revert`/`--linearize` incompatibility, and patch equivalence via `git range-diff`. No unresolved technical objections remain, and the series is positioned as a simpler alternative to Johannes Schindelin’s earlier merge-replay work (pull.2106).

Key technical details for follow-up discussion: the new CLI option is `git replay --linearize`; internal flags are `reverse` (boolean) and `linearize` (boolean); the implementation is mutually exclusive with `--revert` (enforced by `die_for_incompatible_opt2`); and root commits are explicitly replayed while merges are skipped. Files touched include `replay.c`, `replay.h`, `builtin/replay.c`, `Documentation/git-replay.txt`, and `t3650-replay-basics.sh`. The base commit is `9ac3f193c05c`, and the series has a `Change-Id` of `20260604-toon-git-replay-drop-merges-807fa008d395`.

The author reiterates that `--linearize` is intentionally omitted from `git history` due to lack of a clear use case but invites feedback. Potential trivial conflicts with Kristoffer’s documentation changes and Patrick Steinhardt’s `git history drop` series are still noted but expected to be easy to resolve. The only open question is whether Justin Tobler’s forward-looking design concern about the enum-to-bool refactoring remains unresolved, as there has been no reply to Toon’s v3 explanation.