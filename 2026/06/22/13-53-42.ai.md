This is the second patch in the v4 series introducing `--linearize` to `git replay`. Patrick Steinhardt replies to Toon Claes’s refactoring patch that extracts a `put_mapped_commit()` helper and renames `mapped_commit()` to `get_mapped_commit()`.

The review is **surface-level**: Patrick explicitly approves the extraction of the khash wrapper, calling it "sensible" given the awkwardness of the raw khash interface. He does not engage with the patch’s role in the series, its naming choices, or its impact on future extensibility. The comment is brief and positive, but it does not constitute a substantive or tested review. No objections or requests for changes are raised.