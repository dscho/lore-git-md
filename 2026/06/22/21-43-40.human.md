This is the maintainer’s final sign-off on Phillip Wood’s two-patch bugfix series for `git status` during interactive rebase. Junio C Hamano points out a single style nit in the call to `sequencer_parse_todo_command()`—the missing space between `char` and `**`—but otherwise the series is now merged to `master`. The thread summary above already covers the technical substance: the patches fix incorrect OID abbreviation in “fixup -C”/“merge -C” commands and preserve refnames (e.g., “reset main”) in the todo list output, using a shared parsing function and command-specific display logic. No further changes are expected.