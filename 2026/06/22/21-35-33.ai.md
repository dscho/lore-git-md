This is Junio C Hamano’s follow-up to his own review of the first patch in Harald Nordgren’s usability-improvement series. The patch aims to improve error messages when users accidentally omit the slash in `git branch --set-upstream-to origin/main`, typing `origin main` instead. Junio’s earlier review questioned whether the current error message (“branch 'main' does not exist”) is actually misleading, arguing that the command’s semantics require the local branch to exist first, so the error is accurate.

In this email, Junio attempts to reconstruct the user’s likely intent—setting the upstream for the *current* branch to `origin/main`—and suggests a clearer commit message that explicitly states this expectation. He provides a draft commit message that frames the problem as a common slip where users confuse the syntax, and proposes that the advice should only appear when `origin/main` actually exists. The tone is constructive and collaborative, focusing on improving the patch’s clarity rather than rejecting its premise outright. However, the underlying question—whether the current error message is sufficient—remains unresolved, and Junio’s phrasing (“I *think* (but I am guessing)”) signals that the author still needs to justify the change more convincingly. The ball is now back in Harald’s court to either refine the commit message or adjust the patch’s logic to better align with Junio’s interpretation of the user’s intent.