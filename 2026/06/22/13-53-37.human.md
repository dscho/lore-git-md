This is the first patch in the v4 series introducing `git replay --linearize`. Patrick Steinhardt replies to Toon Claes’s refactoring of `enum replay_mode` into a boolean `reverse` flag, questioning whether the change improves readability and raising a forward-looking concern about extensibility if a third replay mode is ever needed.

The patch itself is a mechanical refactoring that replaces the two-value enum with a boolean, eliminating redundant checks and simplifying the code before the `--linearize` feature is added in patch 3. Toon’s commit message and prior cover letters have addressed the extensibility question by noting that the current two-mode design (pick/revert) is stable and future modes would require a separate flag, but Patrick’s reply suggests lingering unease about the trade-off between simplicity and future flexibility.

This is a substantive review comment, not a surface-level nit. It engages directly with the design choice and its long-term implications, and it comes from a contributor with deep familiarity with Git’s internals. The exchange does not indicate a fundamental objection—Patrick does not ask for a revert or an alternative—but it does signal that the refactoring may not be universally seen as an improvement. The thread remains under review, and this feedback could prompt further discussion or a v5 iteration before the series is queued.