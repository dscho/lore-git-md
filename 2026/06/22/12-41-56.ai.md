This is the second patch in the v4 series introducing the `--linearize` option to `git replay`. It is a pure refactoring patch with no behavior change, extracting the logic that stores a commit mapping into a new helper function `put_mapped_commit()` and renaming the existing `mapped_commit()` to `get_mapped_commit()` for symmetry.

The patch touches only `replay.c`, splitting a 12-line block that inserts a commit into the `replayed_commits` hash map into a standalone static function. The new helper is then called in place of the original inline code. The rename from `mapped_commit` to `get_mapped_commit` is mechanical and makes the getter/setter pair (`get_mapped_commit` / `put_mapped_commit`) immediately recognizable. The change is small (20 insertions, 11 deletions) and self-contained; it prepares the codebase for the `--linearize` implementation in patch 3 by making the commit-mapping logic reusable and clearer.

Thread context confirms that this patch is unchanged from v3 and that all prior review feedback has been addressed. The series as a whole is positioned as a simpler alternative to Johannes Schindelin’s earlier merge-replay work, and the refactoring here is explicitly described as stable for the current two-mode design (pick/revert). No unresolved technical objections remain, and the patch is ready for final review.