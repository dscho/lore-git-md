This is the second patch in Toon Claes's three-patch series introducing `--linearize` support to `git replay`. It is a refactoring patch that extracts and renames helper functions in `replay.c` to improve code clarity and prepare for the `--linearize` implementation in patch 3/3.

The patch introduces `put_mapped_commit()`, a new helper function that encapsulates the logic for adding a commit to the `replayed_commits` khash OID map. It also renames the existing `mapped_commit()` to `get_mapped_commit()` to create a clear getter/setter pair. This refactoring reduces duplication in `replay_revisions()` and makes the code more readable by abstracting the mapping logic. The changes are confined to `replay.c` and do not alter behavior.

This patch directly addresses feedback from earlier iterations, where reviewers (including Elijah Newren and Justin Tobler) emphasized the importance of clean, maintainable code as a foundation for the `--linearize` feature. The getter/setter pattern is a standard refactoring technique and aligns with Git's coding style. The patch is technically sound, with a clear commit message and focused scope.

The series as a whole is now complete and ready for merging, with all review feedback incorporated. This patch is a necessary preparatory step for the `--linearize` implementation and carries Johannes Schindelin's Signed-off-by, indicating his support. The refactoring is minimal and targeted, touching only the commit mapping logic in `replay_revisions()`. Key symbols introduced or modified include `put_mapped_commit()`, `get_mapped_commit()`, and the `replayed_commits` khash map.