This is a review comment on the first patch of Tian Yuchen’s six-part series optimizing `paint_down_to_common()` for one-sided histories. Derrick Stolee raises a design concern: the patch decouples `ahead_behind()` from the `nonstale_queue` abstraction, but only because the next patch plans to replace that abstraction entirely. Stolee argues this leaves the code temporarily messier and suggests instead that patch 2 should introduce a *new* data structure rather than replacing the existing one, even though `nonstale_queue` currently has only one remaining caller.

The review is substantive but narrowly scoped—it questions the refactoring strategy without challenging the series’ overall goal (early termination for asymmetric histories). Stolee’s preference for a cleaner intermediate state could lead to a v2 that either reorders the patches or reworks patch 2 to avoid disrupting `ahead_behind()`. The comment is unlikely to derail the series, but it signals that the patch sequence may need adjustment before the optimization itself can be evaluated. For now, the thread’s focus shifts to whether the preparatory work is justified.