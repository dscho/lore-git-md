This is a brief follow-up in the `git branch --delete-merged` series thread, specifically addressing Phillip Wood’s review of the stacked-branch protection mechanism in **v17**. Harald Nordgren acknowledges two technical issues in the current implementation of upstream config clearing:

1. **Logical inconsistency**: The patch clears upstream settings for merged branches, but those branches are already slated for deletion—so clearing their upstream config is redundant. This confirms the "critical bug" identified in the thread summary: the implementation preserves *all* upstream configurations (including those of merged branches) instead of selectively clearing only the upstreams of *unmerged* branches that depend on them. The current behavior contradicts the design goal of avoiding stale references.

2. **Performance inefficiency**: Harald agrees that building a separate list of upstream branches (rather than pruning the deletion candidate set in-place during the `refs_for_each_branch_ref()` callback) is wasteful, especially in repositories with many branches.

The email is **minimal and focused**, serving as a confirmation rather than a new proposal. It signals that Harald recognizes the problems and will likely revise the implementation in the next iteration. For a future AI summarizer, the key takeaway is that **v17’s stacked-branch protection is broken in both design and execution**, and the fix will require:
- **Correct behavior**: Only clear upstream configs for *unmerged* branches that depend on merged branches (not the merged branches themselves).
- **Efficient filtering**: Integrate the pruning logic directly into the `refs_for_each_branch_ref()` callback to avoid redundant traversals.

No new design questions are raised—this is purely an implementation bug. The thread remains in active development, with the next version expected to address these two issues.