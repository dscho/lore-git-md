This is the final substantive review in the `git replay --linearize` series, focusing on the third patch that implements the core functionality. Patrick Steinhardt raises a **design-level concern** about the interface: Git’s `rebase` command supports three modes (`--no-rebase-merges`, `--rebase-merges`, and `--rebase-merges=rebase-cousins`), but the new `--linearize` option only mirrors the first. He wonders whether the interface should be expanded now to cover all three modes, rather than risking a future breaking change.

The review is **technically thorough**—Patrick walks through the code, notes the core change (skipping merge commits instead of dying), and suggests a minor documentation tweak (lower-casing the BUG message). He does not object to the implementation itself, but the interface question is significant: if the project later decides to support cousin rebasing, the current `--linearize` flag would need to be deprecated or extended, complicating the CLI.

The patch itself remains **ready to merge**—all prior feedback has been addressed, test coverage is comprehensive, and Junio has already signaled approval. The open question is whether the project wants to future-proof the interface now or accept a simpler design that matches today’s needs. This is a classic trade-off between simplicity and extensibility, and the discussion is likely to be brief given the series’ otherwise uncontroversial status.