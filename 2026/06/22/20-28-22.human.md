This is a **substantive review exchange** in the ongoing performance optimization series for `paint_down_to_common()`. Derrick Stolee and Kristofer Karlsson finalize two key procedural details for the upcoming v2.

Stolee follows up on his earlier suggestion to instrument the code with **trace2 counters** that track the number of commits walked during merge-base calculations. He clarifies that `trace2_data_intmax()` is the right API for structured, machine-readable output that tests can parse via `GIT_TRACE2_EVENT` logs. This instrumentation will let the test suite rigorously verify the algorithmic improvement—specifically, that the optimization reduces the number of commits traversed in one-sided histories—without relying on fragile timing measurements. The counters will also be human-readable in `GIT_TRACE2_PERF` logs, aiding debugging.

Kristofer had already experimented with similar counters locally and agrees they are valuable for demonstrating the optimization’s impact. He notes that the commit count is more meaningful than relative performance numbers, since the change is algorithmic rather than a micro-optimization. The counters will be particularly useful in `t6600`, where the three test backends (full graph, half graph, no graph) each have distinct traversal characteristics.

This exchange resolves the last open procedural question from the thread summary: **performance instrumentation is now settled**. The v2 series will include trace2 counters to empirically validate the 500x+ speedup on large one-sided histories, complementing the existing hyperfine benchmarks. The only remaining technical loose end is the finite-generation guard’s activation timing, which this email does not address. The series is now procedurally ready for v2.