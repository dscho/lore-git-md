This is a brief but substantive review comment in the ongoing worktree diff performance thread. Junio C Hamano responds to Jeff King’s proposal to enable nanosecond-precision timestamp comparisons unconditionally in `is_racy_stat()`. He acknowledges the core idea—using nanoseconds to eliminate racy entries—but raises a subtle edge case: if the kernel’s in-memory cache evicts the inode for either the file or the index, the nanosecond portion of the timestamp could be lost asymmetrically, leading to incorrect comparisons.

The concern is that if two events (e.g., writing a file and updating the index) occur within the same millisecond but in a specific order, and only one of the two inodes is evicted from the cache, the surviving timestamp might retain nanosecond precision while the other does not. This asymmetry could cause Git to misjudge the order of events, potentially reintroducing raciness or even causing false positives in stat-matching. Junio’s point is not a rejection of Peff’s patch but a cautionary note about a potential pitfall that might not be caught by typical testing.

This review is substantive because it identifies a real (if unlikely) failure mode in the proposed solution. It does not propose an alternative but instead highlights a scenario that would need validation—either through testing or by confirming that the kernel’s behavior in this case is benign. The comment is narrowly focused on the timestamp comparison logic and does not revisit broader architectural questions, such as Junio’s earlier `istate` field proposal or Peff’s `DIFF_RACY_IS_MODIFIED` patch.

For a reader following the thread, this exchange signals that Peff’s patch is close to being viable but may need additional scrutiny around edge cases. The performance problem itself remains well-understood, and the solution space is narrowing to a small, targeted fix rather than a larger refactoring. If this edge case can be addressed or dismissed, the patch is likely to proceed.