This is a CI/build system patch, a tested review follow-up confirming the v3 patch’s behavior on a newer macOS system (Xcode 26.1) with both Makefile and Meson builds. D. Ben Knoble reports that the patch successfully suppresses the duplicate-library warning in Makefile builds while leaving Meson builds unchanged (as expected, since the patch only touches `config.mak.uname`). The warning appears in both build systems on this newer machine, confirming the patch’s relevance and effectiveness.

The test setup is now complete: Knoble has verified the patch on two macOS systems (Xcode 14.2 and 26.1), covering both older and newer linker versions. On the older system (ld64-711), the version gate correctly omits the flag, and no warnings or errors occur. On the newer system (ld-1230.1), the flag is applied, suppressing the warning in Makefile builds. Meson builds continue to emit the warning, as the patch does not address them, but Paolo Bonzini’s endorsement and Patrick Steinhardt’s assignment to implement suppression in Meson builds resolve this as a follow-up effort.

Key technical details remain unchanged from the v3 patch: the flag `-Wl,-no_warn_duplicate_libraries` is version-gated for Xcode 15+ (ld64-907+, dyld-1009.5+) using a regex that now matches ld, ld64, and dyld. The patch preserves the ability to list libraries multiple times for circular dependencies, and the warning suppression is cosmetic, with no impact on symbol resolution. The test results confirm the version gate is robust and the patch behaves as intended across Xcode versions.

This report removes the last open question about the patch’s compatibility, advancing it to a ready-to-merge state. The thread’s focus now shifts to the follow-up Meson integration, which has clear ownership (Patrick Steinhardt) and maintainer support (Paolo Bonzini). No new issues or edge cases are introduced; the report is purely confirmatory. The patch is well-motivated, thoroughly tested, and aligned with cross-project precedent, making it a straightforward improvement to Git’s build system.