This is a review follow-up from Junio C Hamano on the first patch in Harald Nordgren’s usability-improvement series. Junio now offers a clearer articulation of what he believes the user’s *intent* might have been—setting the upstream for the *current* branch to `origin/main`—and suggests a rewritten commit message that explicitly states this expectation. The proposed message removes ambiguity by framing the error as a mismatch between user intent and command syntax, rather than a deficiency in the existing error message itself.

Junio’s rewrite still treats the patch as a helpful hint rather than a fundamental fix, but it provides a more precise problem statement: the command fails because it interprets `origin main` as a request to set the upstream for a non-existent *local* branch `main`, not as a remote-tracking branch. The suggested commit message also clarifies the conditions under which the hint appears (only when `origin/main` exists), which was missing from the original.

This reply does not resolve the underlying tension—whether the current error message is sufficient or the hint is justified—but it gives Harald a concrete path forward: adopt Junio’s wording, and the patch’s motivation becomes harder to dispute. The ball is now in Harald’s court to either accept the revised framing or push back with additional evidence that the current error message is actively misleading. No other reviewers have weighed in yet, so the thread’s next move will likely determine whether this patch advances or stalls.