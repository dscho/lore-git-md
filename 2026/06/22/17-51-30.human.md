This is a small, focused refactoring patch from Junio C Hamano that extracts pack-exclusion logic into a helper function, `should_exclude_pack()`. It’s the first step in a three-patch series aimed at generalizing promisor object handling in Git’s connectivity checks, part of Patrick Steinhardt’s broader ODB abstraction effort.

The patch itself is purely preparatory—it moves existing conditional logic from `odb_source_packed_for_each_object()` into a standalone function without changing behavior. The new helper checks whether a pack should be skipped based on caller-provided flags (e.g., `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY` or `ODB_FOR_EACH_OBJECT_LOCAL_ONLY`), making the code more modular and reusable. The diff is clean and mechanical, touching only `odb/source-packed.c`.

Junio’s review here is minimal but positive, calling the change "quite straight-forward." This aligns with the patch’s nature: it’s a low-risk refactor that sets up the next patch, which will lift a restriction on using flags with object prefix iteration. There’s little here to object to, though reviewers might later scrutinize whether the extracted logic covers all edge cases (e.g., interactions between flags) when the generalization lands.

This patch is part of a larger push to make Git’s connectivity checks backend-agnostic, a key step for supporting alternative ODB backends like reftable. If the series succeeds, it could unblock future work on pluggable storage layers. For now, this first patch looks solid and uncontroversial—worth watching only as a precursor to the more substantive changes coming in patches 2 and 3.