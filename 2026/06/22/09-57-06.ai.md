This is a **substantive review** of the stacked-branch protection mechanism in Harald Nordgren’s `git branch --delete-merged` series, specifically targeting the implementation of upstream config clearing in **v17**. Phillip Wood’s feedback identifies two technical concerns with the current approach:

1. **Incorrect behavior**: The v17 patch preserves *all* upstream configurations, rather than selectively clearing the upstream settings of merged branches that are used as upstreams for unmerged branches. This contradicts the agreed design (clearing upstreams of merged branches to avoid stale references) and undermines the safety mechanism.

2. **Inefficient implementation**: The patch builds a separate list of upstream branches instead of pruning the deletion candidate set directly in the `refs_for_each_branch_ref()` callback. This adds unnecessary overhead, especially in repositories with many branches.

The review is **substantive** because it engages with the core logic of the patch—how upstream dependencies are handled—and suggests a more efficient implementation path. Phillip’s earlier proposal to clear (rather than reassign) upstreams was adopted as the final design, so this feedback directly addresses whether the implementation matches the agreed behavior. The tone is constructive, focusing on correctness and performance without questioning the overall goal.

For a reader tracking the series, this email signals that **v17 is not yet ready for merging** despite being feature-complete in other areas. The fix will likely require a targeted update to the upstream-clearing logic and a refactoring of the deletion candidate filtering. The thread remains in active development, with no open design questions—only an implementation bug to resolve.