This is a gentle ping on a usability improvement series for Git's error messages, specifically targeting two common command-line mistakes: `` `git branch --set-upstream-to` `` with a missing slash (e.g., `origin main` instead of `origin/main`) and `` `git push` `` with a slash instead of a space (e.g., `origin/main` instead of `origin main`). The series consists of two patches that add tailored advice messages for these cases, configurable via new `advice.*` settings, and includes corresponding tests.

The author, Harald Nordgren, resubmits the series (v1) after no feedback on the initial submission, framing it as a modest but meaningful improvement for new users. The patches are narrowly scoped, touching only `` `builtin/branch.c` `` and `` `builtin/push.c` ``, and the implementation appears clean and non-disruptive. The thread AI summary confirms the series is complete and well-structured, with clear separation of concerns and good test coverage.

The motivation is straightforward: these mistakes are common for beginners, and the current error messages do not guide users toward the correct syntax. While the author acknowledges the ideal solution (unifying the syntax) would break backward compatibility, this series offers a practical middle ground by improving the error messaging. The patches introduce no new behavior beyond the advice messages, so they are unlikely to be controversial.

No reviews have been posted yet, so the main open question is whether the wording of the advice messages and the detection logic will hold up under scrutiny. The series is a good candidate for inclusion if reviewers agree the changes are helpful and the implementation is sound. For a future AI summarizer, note the key files touched (`builtin/branch.c`, `builtin/push.c`), the new config options (`advice.setUpstreamFailure`, `advice.pushRepoLooksLikeRef`), and the test files (`t/t3200-branch.sh`, `t/t5529-push-errors.sh`).