This is a substantive review comment in the ongoing worktree diff performance thread. Junio C Hamano responds to Jeff King’s (Peff) proposed patch that unconditionally enables nanosecond-aware timestamp comparisons in `is_racy_stat()`. Junio raises a critical edge case: when two files (or a file and the index) are modified within the same millisecond, their nanosecond timestamps might not be equally preserved by the kernel’s in-memory cache. If one entry loses its nanosecond precision (e.g., due to cache eviction) while the other retains it, the comparison could yield incorrect results, potentially treating a file as "racy" when it is not, or vice versa.

The concern centers on the original rationale for the `USE_NSEC` conditional: avoiding stat-matching failures when the kernel drops nanosecond precision from inodes evicted from its cache. While Peff’s patch focuses on timestamp *comparisons* (not stat-matching), Junio’s scenario highlights that even simple comparisons can become unreliable if precision is inconsistently preserved. This could reintroduce racy entries in edge cases, undermining the patch’s goal of eliminating them. The example—where a file and the index are written in the same millisecond but only one retains nanosecond precision—illustrates how the patch might fail to resolve raciness in practice, despite its theoretical elegance.

Junio does not reject Peff’s approach outright but signals that the edge case warrants further scrutiny. His comment shifts the discussion toward validating whether the patch’s assumptions hold in real-world scenarios, particularly on systems where inode cache eviction is frequent or unpredictable. The thread’s next steps will likely involve testing the patch under conditions that stress the kernel’s inode cache (e.g., high filesystem activity) or exploring alternative solutions that avoid relying on nanosecond precision altogether. This review is substantive because it identifies a potential correctness issue, not just a stylistic or implementation detail.