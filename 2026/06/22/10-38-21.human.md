This is a preparatory patch for Patrick Steinhardt’s ongoing source-tree reorganization, specifically the move of `libgit.a` components into a new `lib/` directory. It is the first of two patches in the v2 series.

The patch adjusts a single include path in `t/helper/test-example-tap.c` from `"t/unit-tests/test-lib.h"` to `"../unit-tests/test-lib.h"`. This change ensures the test helper continues to build correctly once the surrounding code is relocated into `lib/`. The modification is purely mechanical and does not alter any functionality; it is a one-line fix to maintain buildability during the directory restructuring. No new symbols or behaviors are introduced, and the patch is uncontroversial.