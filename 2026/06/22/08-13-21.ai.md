This is a **build system** and **platform compatibility** follow-up in the `USE_NSEC` Meson parity thread. Patrick Steinhardt replies to Jeff King (Peff)’s substantive review, engaging with the deeper technical concerns around `USE_NSEC`’s reliability rather than just the patch mechanics.

Patrick questions whether the `nanosec` Meson option should exist at all if `USE_NSEC` remains subtly broken on some filesystems. He suggests that if the concerns Peff raised about coarse timestamp granularity on network filesystems (CEPH, CIFS, NTFS, UDF) still apply, the build option might not be worth exposing—even for parity. His position leans toward removing the knob entirely rather than propagating it to Meson, arguing that a broken or rarely-used option adds no value. However, he acknowledges this is "kind of" outside the scope of D. Ben Knoble’s original patch, which only mirrors the existing Autotools/Makefile behavior.

The exchange surfaces a tension between build system consistency and technical debt. While the patch itself remains queued for merging, Patrick’s comments could reignite discussion about whether `USE_NSEC` should be deprecated or investigated further. For now, the thread’s focus shifts from "how to add the option" to "whether the option should exist," though no immediate action is proposed. Key technical details to track: the `Documentation/technical/racy-git.adoc` reference, the Linux 2.6.11 partial fix, and the list of affected filesystems (CEPH, CIFS, NTFS, UDF).