This is the **stacked-branch protection resolution** in Harald Nordgren’s `git branch --delete-merged` series. Phillip Wood formally endorses the simplified approach proposed in the parent email: **aborting deletion (with a clear error message) when a branch is used as an upstream for another branch**, rather than attempting to automatically retarget dependent branches. This decision resolves the last major design ambiguity in the series by opting for the safer, simpler default behavior.

Key technical details for follow-up discussion:
- **Subsystem**: Branch deletion logic (`delete_merged_branches()` in `builtin/branch.c`).
- **Behavior**: The command will now **abort and notify** (e.g., "Cannot delete 'feature1' because 'feature2' depends on it") if a branch is an upstream for any other branch, avoiding the complexity of recursive retargeting.
- **Edge cases handled**: Self-referential upstreams (e.g., `b3→b3`) and chains of dependencies (e.g., `b1→b2→b3`) are sidestepped entirely by this approach.
- **Implementation impact**: Reduces code complexity significantly; retargeting logic (which would require iterative resolution and careful traversal order) is deferred to a future optional flag (e.g., `--retarget-upstreams`).
- **Backward compatibility**: Retargeting can be added later without breaking existing behavior, as the abort-and-notify default preserves all current workflows.
- **Test coverage needed**: A test case verifying the error message and behavior for stacked branches (e.g., `b1→b2`) is now a clear next step.
- **Documentation**: The commit message and man page should note the limitation and that retargeting may be added later.

This reply effectively **closes the design debate** by aligning with Junio C Hamano’s earlier preference for abort-and-notify as the default. The series is now unblocked for final review, with all high-weight feedback (API consistency, per-branch opt-out, `--dry-run`, and stacked-branch safety) addressed. The next steps are implementation of the abort logic, test coverage, and documentation updates.