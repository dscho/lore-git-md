Here’s the human-mode summary for a developer catching up on the thread:

This thread is the final chapter in Patrick Steinhardt’s v3 patch series to refactor Git’s connectivity checks, removing hardcoded packfile assumptions so the code can work with any ODB backend—key for his larger ODB abstraction effort. The series is now fully approved and ready to merge. Junio C Hamano and Christian Couder both signed off on the last patch, which replaces the packfile-specific `find_pack_entry_one()` with the generic `odb_for_each_object_ext()` API, using a callback to track promisor objects. A new test in `t/t5616-partial-clone.sh` verifies the optimization works during partial fetches and that the fallback to `rev-list` behaves correctly, fixing a logical inversion that surfaced in v2.

The earlier patches in the series laid the groundwork: extracting pack-exclusion logic, enabling flags in ODB iteration, and isolating promisor-specific code. All substantive feedback—including control flow clarity, error propagation, and performance—has been addressed. The changes are internal to `connected.c` and the test suite, with no user-facing impact or on-disk format changes. With both reviewers satisfied, this series is now technically complete and should land in `next` soon. It’s a critical step toward pluggable ODB backends, like reftable or custom storage layers.