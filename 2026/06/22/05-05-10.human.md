This is a **CI/platform-compatibility** discussion, specifically a maintainer’s reaction to the ongoing macOS CI hang investigation.

Junio C Hamano briefly weighs in on Jeff King’s earlier concern about the proposed fix for the macOS CI hangs in `t5551` and `t5559`. He echoes Peff’s pessimism, agreeing that the current workaround—packing refs and aligning `$JOBS` with the runner’s core count—may only mask a deeper HTTP/2 flow-control deadlock rather than eliminate it. Junio does not propose an alternative or reject the patch outright, but his tone ("I share the pessimism") signals that the fix is not yet considered definitive. The thread remains open, with the root cause still unresolved and no maintainer decision on whether to merge the current patch or pursue further environment updates (e.g., libcurl or macOS build image upgrades).