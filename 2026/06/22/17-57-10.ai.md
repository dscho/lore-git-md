This is a substantive review comment from Junio C Hamano on the third patch in Patrick Steinhardt’s ODB abstraction series. The patch replaces packfile-specific promisor object checks with a generic ODB iteration mechanism, but Junio flags a potential regression: the removal of the `odb_reprepare()` call before the loop.

Junio’s concern is that the old code explicitly refreshed the ODB’s view of packfiles (`odb_reprepare(the_repository->objects)`) before iterating, ensuring that any newly added promisor packs were visible. The new code skips this step, relying instead on `odb_for_each_object_ext()` to handle the iteration. While the fallback to `rev-list` ensures correctness, Junio questions whether this change could cause the code to miss promisor objects in packs added after the loop begins, potentially leading to false negatives in connectivity checks.

The review is narrowly focused but substantive: it identifies a specific behavioral difference (the missing `odb_reprepare()`) and asks for clarification on its implications. This is not a surface-level nit—it touches on correctness and edge-case handling. The question is likely to prompt discussion about whether the new iteration mechanism is robust enough to handle dynamic packfile changes or if the refresh step should be reintroduced (possibly inside `odb_for_each_object_ext()` itself). No other aspects of the patch are addressed in this reply.