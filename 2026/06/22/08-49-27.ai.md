This is the first patch in a three-part refactoring series that generalizes promisor object handling in Git’s connectivity checks, part of Patrick Steinhardt’s ongoing ODB abstraction effort. The patch extracts logic for skipping certain packs during object iteration into a helper function, `should_exclude_pack()`, to prepare for the next patch, which will lift a limitation on using flags with object prefix iteration.

The change is confined to `odb/source-packed.c` and is purely preparatory: it moves existing conditional logic into a standalone function without altering behavior. The new helper checks whether a pack should be excluded based on the caller’s flags (e.g., `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`, `ODB_FOR_EACH_OBJECT_LOCAL_ONLY`), making the code more modular and reusable. The diff is clean and mechanical, with no functional impact on its own.

This patch is a small but necessary step toward the series’ goal of making connectivity checks backend-agnostic. It looks well-crafted and uncontroversial, though its value will become clearer in the context of the next patch, which introduces the actual generalization. Reviewers may focus on whether the extracted logic covers all relevant flags or if edge cases (e.g., interactions between flags) are handled correctly. The commit message is concise and accurately describes the change.