This is a performance optimization discussion in the ongoing thread about Git's priority queue implementation. Kristofer Karlsson responds to René Scharfe's benchmark results and suggested tweak to the lazy_queue optimization series. The exchange focuses on final performance tuning before the series can be merged.

Kristofer confirms René's observation of a 1% performance gap between the optimized version and current code, noting it appears CPU-dependent with higher variance on his laptop but consistent 1.3% improvement on an idle server. He identifies an edge case in René's proposed flush_get() change where consecutive get() calls on a single-element queue could return stale data. To address this, Kristofer has inlined the flush logic directly into get()/peek() functions, which also eliminates the forward declaration.

The technical discussion includes detailed benchmark results from 30 interleaved runs on an idle Xeon server, showing all three variants (original v2, René's tweak, and Kristofer's inlined version) performing within 1.3% of each other while consistently faster than baseline. Kristofer plans to send a v3 with the inlined flush implementation, suggesting this will be the final iteration before merging.

This email represents the final refinement of the optimization series, addressing the last remaining edge case while maintaining the performance improvements. The thread demonstrates the careful attention to correctness and performance that characterizes Git's review process, with measurable validation of the changes.