RFC patch series proposing behavior changes for `git history reword` (experimental command in Patrick Steinhardt's `git history` feature). Author Pablo Sabater is new to this subsystem. Two patches:

1. Makes `git history reword` abort when new commit message matches original (unlike `git commit --amend`/`git rebase -i`), citing expensive branch updates (since it updates all branches containing the original commit) and in-memory-only operation (no worktree/index side effects to preserve) as rationale. Implementation adds message comparison check in builtin/history.c. Open question whether diverging from amend/rebase behavior is justified by the different constraints of `git history`.

2. Adds explicit success feedback after reword operation, using `repo_find_unique_abbrev()` to show original and rewritten commit hashes on stderr. Addresses UX gap compared to similar commands. Well-contained changes to builtin/history.c and tests in t/t3451-history-reword.sh.

Both patches build on Patrick Steinhardt's v11 `git history` series (Jan 2026). Well-structured with clear commit messages and thorough test coverage. The RFC aspect specifically seeks feedback on whether the no-op abort behavior in patch 1 is appropriate given the command's constraints. Implementation quality suggests author has Git codebase familiarity despite being new to this subsystem.