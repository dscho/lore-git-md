Here's a human-readable summary of the thread:

Pablo Sabater is proposing changes to the experimental `git history reword` command (part of Patrick Steinhardt's larger `git history` feature) to make it abort when commit messages are unchanged - but only when explicitly requested via a new flag. This comes after Junio suggested this approach in response to Pablo's initial version.

The current patch series (v2) has two parts: first refactoring the underlying commit message handling to support flags, then implementing the actual abort behavior. The changes are carefully scoped to only affect `git history reword` while preserving how other commands like `git commit --amend` work. Tests verify both the new abort behavior and that existing functionality remains unchanged.

The discussion has narrowed significantly from the original proposal based on Junio's feedback, focusing just on the core technical solution. Questions about how to name the flag (`--force` vs `--force-rewrite`) and how to handle similar cases like `git history fixup --reedit-message` are being deferred to future work. The implementation looks solid, with good test coverage and clear separation of concerns in the code.

Patrick Steinhardt, who maintains the broader `git history` feature, hasn't yet reviewed this version, but the changes address his earlier concerns about scope. The patch appears ready for consideration, with the main outstanding question being whether the same abort behavior should eventually apply to related commands like `fixup`.