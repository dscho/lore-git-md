This is the first patch in the v2 series introducing `--linearize` to `git replay`. It refactors the internal `enum replay_mode` into a simple boolean `reverse` flag, eliminating the need for a two-valued enum and removing redundant BUG() checks.

The patch is purely preparatory: it changes no user-visible behavior. The conversion touches only `replay.c`, replacing every occurrence of `enum replay_mode` with `bool reverse` and simplifying the conditional logic that previously checked the enum. The diff is mechanical and straightforward, with no new edge cases introduced. This sets the stage for the `--linearize` option that arrives in patch 3.