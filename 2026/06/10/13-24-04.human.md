This is a build system patch addressing duplicate library references in Makefile link recipes. Harald Nordgren's v2 iteration redefines the $(LIBS) variable to eliminate macOS linker warnings while preserving the critical left-to-archive ordering required for correct symbol resolution.

The patch restructures $(LIBS) to explicitly list prerequisite archive files first (from $^), then filters subsequent libraries to avoid duplicates. This solution is applied consistently across test helpers, fuzz programs, and unit test build rules. The approach shows careful attention to both the cosmetic warning issue and the underlying technical constraints of linker behavior.

The v2 version represents a significant improvement over v1 by tackling the root cause through $(LIBS) restructuring rather than just removing duplicates at individual call sites. Johannes Sixt is now being looped into the discussion, likely due to his platform expertise (particularly on Windows and build system matters). The patch appears well-considered and addresses Junio Hamano's earlier feedback about maintaining correct ordering.