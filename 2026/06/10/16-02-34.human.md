This is a technical discussion about the behavior of `git history reword` (an experimental command in Patrick Steinhardt's `git history` feature) when handling cases where the commit message is unchanged but the committer identity may need correction.

Junio C Hamano responds to Patrick Steinhardt's suggestion to model the behavior after `git commit --reset-author`. While acknowledging the parallel, Junio notes that `--reset-author` doesn't fully solve the use case where only the committer identity needs correction while preserving the author information. 

He proposes a phased approach: initially defaulting to always creating a new commit (even when only timestamps differ) with an option to skip updates, then potentially flipping this default in a future major version. This maintains flexibility for users who need to correct committer metadata while keeping the core behavior simple.

The discussion continues to refine the nuanced handling of identity correction workflows, with Junio providing clear implementation guidance that separates the concerns of commit message comparison (for rewrite avoidance) from committer identity correction. The thread has largely converged on technical principles, with only the specific UX details of the skip mechanism remaining open.