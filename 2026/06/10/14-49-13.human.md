This is the second patch in the v2 series introducing a `--linearize` option to `git replay`. It is a pure refactoring patch with no behavior change, extracting the logic that stores a commit mapping into a new helper function `put_mapped_commit()` and renaming the existing `mapped_commit()` to `get_mapped_commit()` for symmetry.

The patch touches only `replay.c`, moving eleven lines of code into a new static helper and updating the single call site. The change is mechanical and well-contained: the new helper is immediately used in `replay_revisions()`, and the renamed function is updated in one place inside `pick_regular_commit()`. The commit message is concise and accurately describes what the patch does.

This refactoring is clearly preparatory for the third patch, which will use the new helper to implement the `--linearize` logic. The patch itself is uncontroversial and appears to be a clean, low-risk extraction. No review feedback has surfaced yet, but the change looks like a textbook example of a safe refactor.