This is the second patch in Patrick Steinhardt's 7-part series to eliminate global state in setup.c, continuing the `the_repository` removal effort. The patch refactors worktree configuration logic in `builtin/init-db.c` to be more straightforward now that the global `git_work_tree_cfg` variable is no longer being used.

The changes simplify the code by:
1. Only computing the worktree path when actually needed (when no explicit worktree was provided)
2. Renaming the local variable from `git_work_tree_cfg` to `work_tree_cfg` to avoid confusion with the (now unused) global
3. Reorganizing the control flow to make the logic clearer
4. Moving the `free()` call closer to where the memory is allocated

The patch maintains all existing behavior while making the code more maintainable as part of the larger effort to remove global state. It's a clean, focused change that follows Patrick's typical pattern of careful, incremental refactoring. The diff shows 18 lines added and 13 removed, with straightforward changes that don't introduce new complexity.