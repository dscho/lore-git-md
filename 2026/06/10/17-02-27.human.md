This is the final patch in the v2 series introducing a `--linearize` option to `git replay`. Junio’s reply is a surface-level review of the third patch, focusing on two small points: the handling of root commits and a test case description.

The patch implements the core functionality: when `--linearize` is given, `git replay` drops merge commits and replays only regular (single-parent) commits, matching the default behavior of `git rebase --no-rebase-merges`. Junio’s review confirms that the code now explicitly distinguishes between merges and root commits, which was the intent after his feedback on v1. He also flags a minor wording issue in a test description—“down to root commit” is misleading because the test range excludes the root commit itself—but this is purely editorial and does not affect functionality.

The series as a whole is clean, well-motivated, and directly addresses the goal of enabling server-side rebase. The refactoring patches (1 and 2) are mechanical and uncontroversial; the feature patch (3) is straightforward and includes thorough test coverage. No substantive objections remain, and the implementation looks ready for integration.