This is part 9/10 of Patrick Steinhardt's v4 series adding a `git history drop` subcommand, which builds on the experimental `git history` command infrastructure. The patch refactors reference update handling in preparation for the drop command implementation.

The change splits the existing `handle_reference_updates()` function into two distinct phases:
1. `compute_pending_ref_updates()` - Gathers all references that need updating
2. `apply_pending_ref_updates()` - Executes the actual reference transaction

This separation allows for better control flow in the upcoming drop command implementation while maintaining the same functionality. The patch is a mechanical refactoring with no behavior changes - all existing test coverage continues to pass.

The changes are part of the broader reset API modernization effort that has been carefully reviewed across multiple iterations of this series. The split follows good software engineering practices by separating computation from side effects, making the code more maintainable and testable.

This appears to be a straightforward preparatory change that sets up the final patch in the drop command implementation while keeping the diff focused and reviewable. The series as a whole has shown steady progress through multiple rounds of review feedback.