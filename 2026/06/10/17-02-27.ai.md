This is a review reply in the `git replay --linearize` series, specifically on the third patch (the core implementation). Junio’s feedback is substantive but narrow: he flags a discrepancy between the commit’s behavior and the author’s earlier statement about how root commits should be handled, and he questions whether a test case’s description matches its actual range.

The review does not challenge the feature’s design or merge readiness. Instead, it focuses on two technical points:

1. **Root commit handling**: In v1, the code treated root commits and merge commits identically (both were skipped when linearizing). Junio’s v1 review suggested this might be confusing, and Toon responded that the behavior was intentional. However, the v2 patch no longer skips root commits, creating a mismatch between the stated intent and the implementation. Junio asks for confirmation that this change is deliberate.

2. **Test description accuracy**: The test case claims to replay “down to the root commit” (`A..topic-with-merge`), but the range actually excludes `A` (the root). Junio asks whether the description is misleading.

The feedback is precise and actionable, but it does not uncover a bug or require a major rework. It is a clarifying question about intent and a minor documentation nit. The patch remains well-structured, the feature is clearly motivated, and the implementation is clean. If the author confirms the root-commit behavior is correct, the series is likely to proceed without further delay.