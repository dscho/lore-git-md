Test cleanup patch from the maintainer. This fixes an issue in t1400-update-ref.sh where a test creates FIFOs named "in" and "out" but doesn't clean them up, which could cause later tests to hang if they try to use files with those names. The patch simply adds a `test_when_finished` cleanup handler to remove the FIFOs after the test completes.

This is a straightforward maintenance fix - the kind of small but important cleanup that keeps the test suite reliable. The change is minimal (just one line added) and clearly correct, coming directly from Junio who would be most familiar with the test suite's needs. No review needed for this sort of obvious fix.