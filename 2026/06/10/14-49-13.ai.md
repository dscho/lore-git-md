This is a refactoring patch (2/3) in Toon Claes' series adding `--linearize` support to `git replay`. The patch extracts commit mapping logic from `replay_revisions()` into a new helper function `put_mapped_commit()` and renames the existing `mapped_commit()` to `get_mapped_commit()` for symmetry.

The changes are mechanical and focused on code organization:
- The new `put_mapped_commit()` encapsulates the hashmap insertion logic previously inline in `replay_revisions()`
- The renamed `get_mapped_commit()` maintains identical behavior but with a name that better pairs with the put operation
- No functional changes are introduced - this is purely a cleanup to make the subsequent `--linearize` implementation in patch 3/3 cleaner

The patch touches only `replay.c`, modifying the commit mapping infrastructure that tracks rewritten commits during replay operations. The changes follow the pattern established in the codebase and don't introduce new concepts - this is straightforward preparation for the feature implementation coming next in the series.

Technical details for future reference:
- Introduces `put_mapped_commit()` helper with same error checking as original inline code
- Renames `mapped_commit()` to `get_mapped_commit()` for API consistency
- Maintains all existing BUG() checks for duplicate commits
- Keeps the same khash OID map structure for tracking rewritten commits
- Sets up cleaner separation of concerns before the merge commit handling in patch 3/3