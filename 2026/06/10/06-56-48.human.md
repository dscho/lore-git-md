This is the third patch in Patrick Steinhardt's 7-part series to eliminate global state in setup.c as part of the ongoing `the_repository` removal effort. The patch removes the `git_work_tree_cfg` global variable after refactoring the code to use repository format information instead.

The change is straightforward - the variable was already made redundant by previous refactoring, and this patch cleans it up by:
1. Removing the variable declaration
2. Replacing all its uses with equivalent checks against `repo_fmt->work_tree`
3. Simplifying some conditional blocks that became unnecessary

The diff shows this is a mechanical change that doesn't affect behavior, just internal structure. As with the rest of the series, this continues Patrick's methodical approach to removing global state - first making callers pass repository information, then eliminating the now-unused globals. The patch touches only setup.c and follows the established pattern of this architectural cleanup effort.