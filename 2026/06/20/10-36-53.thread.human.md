This thread is about a performance optimization for Git’s `paint_down_to_common()` function, which speeds up merge-base calculations in repositories with asymmetric histories (such as those with import grafts or shallow histories) by 100-1000x. The series, now at version 8, is technically complete, fully reviewed, and queued in Junio’s integration tree. The only remaining work is finalizing documentation wording.

The latest exchange between Kristofer Karlsson and Elijah Newren focuses on how to describe the generation-number regions in the technical document and commit messages. They’ve agreed to replace terms like "finite" and "infinite" with "ordered" and "unordered" to better reflect the semantic boundary between regions where topological ordering holds and where it doesn’t. Kristofer will update the documentation to clarify how v1 commit-graphs (which use `GENERATION_NUMBER_V1_MAX` as a topological ceiling) fit into this model. This is purely an editorial change—no code or behavior is affected.

The series touches the commit-reach subsystem, introducing early termination in `paint_down_to_common()` when one side of the history is exhausted, removing the old commit-date fallback, and adding a topological ceiling for v1 commit-graphs. It also includes performance instrumentation (Trace2 step counts), regression tests for side-exhaustion edge cases, and clock-skew hardening. All substantive review feedback has been addressed, and the series is procedurally unblocked. If you’re tracking this work, the documentation updates are the last loose end before the series advances further.