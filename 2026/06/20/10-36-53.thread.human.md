This thread is about a performance optimization to Git’s `paint_down_to_common()` function, which calculates merge bases. The series, now at **v5**, is fully reviewed and ready to merge. Authored by **Tian Yuchen** and co-developed with **Kristofer Karlsson**, it eliminates unnecessary traversal of large one-sided histories—yielding **100-1000x speedups** in asymmetric queries (e.g., repositories with import grafts or shallow histories).

The final iteration (v5) is a polished, regression-free version that incorporates all feedback. Key improvements include:
- **A new technical document** (`Documentation/technical/paint-down-to-common.adoc`) explaining the algorithm, termination conditions, and the removal of the obsolete commit-date fallback.
- **Trace2 diagnostics** (step-count instrumentation and clock-skew topology tests) to verify correctness and performance across all commit-graph modes.
- **Extended test coverage** for edge cases, including diamond-shaped topologies, mixed finite/INFINITY generation numbers, and clock-skew scenarios.
- **Mechanical cleanups**, such as squashing patches to avoid `-Wunused-function` warnings and removing dead code.

The series has received **unanimous approval** from reviewers, including **Derrick Stolee** (who confirmed the commit-date fallback is now obsolete) and **SZEDER Gábor** (who requested the squash). **Junio C Hamano** ejected an earlier version (v3) due to a regression, but v5 addresses all concerns and is **ready for merging**. Benchmarks show no performance regressions, with dramatic improvements in asymmetric queries (e.g., a 2.6M-commit monorepo query drops from **3.67s to 5ms**).

The thread is now **technically complete**, with no open questions or loose ends. Follow-up work (e.g., retroactive trace2 instrumentation) is non-blocking.