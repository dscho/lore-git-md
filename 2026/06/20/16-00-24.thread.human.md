Here’s the human-mode summary for a developer catching up after being away:

This thread started as a simple patch to add a `nanosec` Meson build option, mirroring Autotools’ `USE_NSEC` for sub-second file timestamp tracking. What began as a build system parity effort has since grown into a broader debate about whether `USE_NSEC` is even necessary—or viable—today. Jeff King’s testing showed that modern Linux filesystems reliably preserve nanosecond timestamps, undermining the historical "racy Git" concerns that originally justified the knob. However, new interoperability risks emerged: mixing `USE_NSEC` and non-`USE_NSEC` implementations (like Git and JGit) could degrade performance, not correctness.

The discussion has since split into competing proposals. Brian M. Carlson suggested flipping the default to `true` for most users, with a config knob for overrides and future auto-detection via `statfs`. Patrick Steinhardt went further, arguing the build-time knob should be removed entirely in favor of runtime configuration (like `core.ignoreCase`), auto-detected during repository initialization. Junio C Hamano pushed back on treating the two cases identically: while `core.ignoreCase` is mandatory for correctness on case-insensitive filesystems, `USE_NSEC` is optional even on capable systems, improving performance but not required. The original patch remains queued for merging, but no consensus has emerged on whether to flip the default, deprecate the knob, or convert it to runtime control. For now, the Meson parity goal is met, but the thread’s focus has shifted to whether `USE_NSEC` should exist at all.