Here’s the human-mode summary for a developer catching up on the thread:

This thread started as a simple patch to add a `nanosec` Meson build option mirroring Git’s existing `USE_NSEC` setting—a macro that enables sub-second file timestamp tracking. What began as a build system parity effort has since grown into a broader debate about whether `USE_NSEC` is still necessary, how it should be configured, and whether it should exist at all.

The original patch by D. Ben Knoble was accepted by Junio, but the discussion quickly expanded when Jeff King (Peff) shared test results showing that modern Linux filesystems reliably preserve nanosecond timestamps, making the historical "racy Git" concerns obsolete. However, Peff also identified a new risk: mixing `USE_NSEC` and non-`USE_NSEC` implementations (e.g., Git and JGit) could degrade performance. Brian M. Carlson proposed flipping the default to `true` for most users, with a config knob for overrides, while Patrick Steinhardt argued for removing the build-time option entirely in favor of runtime configuration—similar to `core.ignoreCase`. Junio later clarified that `USE_NSEC` is optional, not mandatory for correctness, which complicates Patrick’s analogy.

The latest exchange between Peff and Patrick focuses on the practicality of runtime configuration. Peff agrees in principle but notes there’s no reliable, portable way to auto-detect whether a filesystem preserves nanoseconds, making a runtime knob harder to implement than it seems. The thread now hinges on whether the project prefers a runtime setting with a conservative default (to avoid edge cases) or retains the simpler build-time option. No new patches have been submitted, and the discussion remains open. Key unresolved questions include whether to flip the default, deprecate the knob, or convert it to runtime control—and if so, how to handle the default value and interoperability risks.