Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread started as a simple patch to add a `nanosec` Meson build option mirroring Git’s existing `USE_NSEC` knob for sub-second file timestamps, but it’s since grown into a broader debate about whether `USE_NSEC` is still useful—or even harmful. D. Ben Knoble’s original patch aimed to give Meson users the same build-time control as Autotools/Makefile users, but Jeff King’s testing revealed that modern Linux filesystems (ext4, ext2, XFS, vfat) now reliably preserve nanosecond timestamps, making the historical "racy Git" concerns obsolete. That discovery sparked three competing proposals: Knoble’s patch (now queued by Junio), Brian M. Carlson’s suggestion to flip the default to `true`, and Patrick Steinhardt’s idea to make it runtime-configurable like `core.ignoreCase`.

The discussion has stalled on unresolved trade-offs. Knoble argues his patch should proceed unless `USE_NSEC` is deprecated entirely, as it provides parity for source distributions like Gentoo. He confirmed XFS support, bolstering the case that the knob isn’t subtly broken, but he hasn’t addressed the interoperability risks of mixing `USE_NSEC` and non-`USE_NSEC` implementations (e.g., Git and JGit). Peff’s feedback complicates Patrick’s runtime-configuration proposal by showing auto-detection isn’t reliably portable, while Brian’s default-flip idea lacks consensus on edge cases. Junio hasn’t weighed in on the bigger questions, leaving the thread in limbo: the patch is ready to merge, but the underlying mechanism’s future remains undecided. The next steps likely hinge on whether someone proposes a concrete runtime-configuration patch or a deprecation plan.