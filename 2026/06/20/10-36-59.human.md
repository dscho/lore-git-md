This is the final patch in Tian Yuchen’s six-part performance series optimizing `paint_down_to_common()` for one-sided histories. It adds a new technical document (`Documentation/technical/paint-down-to-common.adoc`) that explains the merge-base algorithm, its two use cases (finding all merge bases vs. ancestry checks), the paint-flag mechanics, the INFINITY vs. finite-generation regions, and the termination conditions—including the side-exhaustion optimization introduced earlier in the series.

The document is thorough and clearly written, with diagrams and concrete examples that make the algorithm’s behavior accessible to reviewers unfamiliar with the commit-reach internals. It explicitly ties the side-exhaustion condition to the finite-generation region, clarifying why the optimization cannot activate earlier. The patch also updates the build system (`Makefile` and `meson.build`) to include the new document in the generated technical documentation.

This patch is purely documentation and does not change any code behavior. It rounds out the series by providing the technical background that reviewers and future maintainers will need to understand the optimization’s design and edge cases. The inclusion of this document suggests the series is well-prepared for review and likely to be taken seriously.