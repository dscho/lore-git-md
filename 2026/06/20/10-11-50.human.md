This is the first patch in Pablo Sabater’s v6 series implementing cascading indentation for visual roots in `git log --graph`. It is a test infrastructure cleanup that moves a duplicated helper function (`check_graph`) from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into the shared `lib-log-graph.sh` library, renaming it to `lib_test_check_graph`.

The change is mechanical and uncontroversial: both test files already source `lib-log-graph.sh`, so the consolidation reduces code duplication without altering any test behavior. The patch is a prerequisite for the next commit, which adds new graph tests in a third file that will reuse the same helper. Karthik Nayak previously reviewed and approved this refactoring in an earlier iteration.