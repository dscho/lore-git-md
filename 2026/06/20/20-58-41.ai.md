This is a CI/build system patch, a tested review response to Junio C Hamano’s call for verification of Harald Nordgren’s macOS linker warning suppression series. D. Ben Knoble reports successful testing on an older macOS system (Xcode 14.2) with both Makefile and Meson builds, confirming the version gate works as intended and no regressions occur.

The patch under discussion suppresses macOS linker warnings about duplicate library references by adding `-Wl,-no_warn_duplicate_libraries` to `config.mak.uname`, version-gated for Xcode 15+ (ld64-907+, dyld-1009.5+). Knoble’s test on Xcode 14.2 shows the flag is correctly omitted on older systems, and neither build system emits warnings or errors. This addresses Junio’s due-diligence request for broader testing across Xcode versions and build systems, providing confidence the version-gate regex (matching ld, ld64, and dyld) is robust.

The test setup is minimal but effective: a single older macOS machine (Xcode 14.2) with both Makefile and Meson builds. Knoble notes he has observed the original warning on a newer machine, implying the patch will suppress it there, but does not yet have results. The report is concise and directly responsive to the open questions in the thread, focusing on the version gate and build system compatibility rather than the patch’s core functionality.

This response advances the thread toward merging by confirming the patch behaves as expected on older macOS versions and with Meson. No new issues or edge cases are introduced; the report is purely confirmatory. The patch remains ready for merging pending any further test reports, but this confirmation removes a key blocker.