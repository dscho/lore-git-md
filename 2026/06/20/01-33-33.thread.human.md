Here’s the human-readable summary for a developer catching up on this thread:

The June 2026 "What’s cooking in git.git" report (#07) from Junio C Hamano provides a snapshot of the integration branches as Git 2.55 enters feature freeze. Most in-flight topics have either merged or been deferred, but one notable exception is Patrick Steinhardt’s `ps/gitlab-ci-windows` series, which migrates GitLab CI Windows builds away from Chocolatey due to persistent reliability issues. Junio has now agreed to merge this change into `master` for the upcoming release, calling it low-risk and a targeted fix—unlike some recent merges with broader implications.

The patch itself is minimal, touching only the CI configuration (`.gitlab-ci.yml`), and addresses what Patrick described as "extremely annoying" stability problems. No objections or open questions remain, and the change is confirmed for Git 2.55. Other key updates in this cycle include new plumbing commands like `git refs create|delete`, optimizations to `git log -L`, and repository-scoped config for core settings, but these are either already merged or held for future releases. The thread is effectively closed on this topic.