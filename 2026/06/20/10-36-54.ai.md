This is the first patch in Tian Yuchen’s six-part series optimizing `paint_down_to_common()` for one-sided histories. It refactors the `ahead_behind()` helper to stop using the `nonstale_queue` abstraction, replacing it with a plain `prio_queue` and a local `max_nonstale` pointer. The change is preparatory: the next patch will introduce a `paint_queue` struct that tracks per-side commit counts, which `ahead_behind()` does not need. No behavior change is introduced here.

The patch touches only `commit-reach.c`, moving the non-stale tracking logic into the `insert_no_dup()` helper. The diff is mechanical: `nonstale_queue_put()` and `nonstale_queue_get()` are replaced with direct `prio_queue` operations, and the `max_nonstale` pointer is updated inline. The commit message is clear about the purpose—decoupling `ahead_behind()` from the abstraction that will soon be replaced—and the code changes align with that goal.

This is a clean refactoring with no functional impact, setting the stage for the core optimization in patch 3. The patch is well-scoped and unlikely to be controversial, though reviewers may check that the local `max_nonstale` logic correctly mirrors the original `nonstale_queue` behavior. The series’ overall goal—early termination for one-sided histories—remains the key context for understanding why this refactoring is necessary.