This is the first patch in Pablo Sabater's v6 series implementing cascading indentation for visual roots in `git log --graph`. It performs test infrastructure cleanup by moving the `check_graph` helper function from two test files (`t4215-log-skewed-merges.sh` and `t6016-rev-list-graph-simplify-history.sh`) into `lib-log-graph.sh`, renaming it to `lib_test_check_graph`.

The patch touches three files: `t/lib-log-graph.sh` (adding the shared function), `t4215-log-skewed-merges.sh`, and `t6016-rev-list-graph-simplify-history.sh` (removing their duplicate implementations and updating all calls to use the shared version). This is a pure refactoring with no behavior changes - every test case remains identical, only the location of the helper function changes.

This cleanup is preparatory work for the next patch in the series, which will add new graph tests to a different file that also needs this helper. The change is straightforward and mechanical, replacing 34 lines of duplicate code with a single shared implementation. Karthik Nayak previously reviewed and approved this refactoring in an earlier version of the series. The patch appears well-crafted and uncontroversial, addressing a minor technical debt item while enabling future test additions.