Here’s the updated human-readable summary for a developer catching up on the thread:

---

This thread began as a bug report about CI hangs in Git’s HTTP tests (`t5551` and `t5559`) on macOS and Linux runners, traced to an Apache `mod_http2` timeout bug. The fix—a two-patch series from Jeff King (Peff) increasing Apache’s `Timeout` directive and isolating a slow test case—was merged for Git 2.55. A follow-up to add a GitLab CI status badge to the `README` was also accepted, while a third patch to align GitLab’s test coverage remains on hold pending discussion.

After the immediate issue was resolved, the thread pivoted to performance analysis of Git’s reftable backend. Peff identified a quadratic-time scalability problem during bulk ref deletion and re-creation (e.g., `git for-each-ref | git update-ref --stdin`), where runtime grows non-linearly (e.g., 1.8s → 7.1s for a 2x input size increase). Patrick Steinhardt confirmed this stems from reftable’s tombstone design: deleted refs are marked as tombstones rather than purged, forcing subsequent operations to scan all tombstones to verify refname availability. While this avoids expensive compaction, it creates a performance cliff for bulk workflows. Patrick noted the issue is deprioritized for now, as it primarily affects artificial benchmarks, but left room for future optimizations like early iterator termination.

Key takeaways: The CI fixes are done, but the reftable backend’s quadratic-time behavior—now tied to a deliberate architectural trade-off—remains an open performance concern. No immediate action is planned, but the discussion provides a clear path for future work if real-world workflows hit this bottleneck.

---