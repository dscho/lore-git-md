This thread began with a CI hang in Git’s macOS and Linux tests, traced to an Apache HTTP/2 timeout bug and resolved with a merged 2-patch series that increased the server timeout and isolated the problematic test. The discussion then pivoted to low-hanging performance optimizations in Git’s reftable backend, where Jeff King identified a quadratic-time scalability issue during bulk ref deletion and re-creation. Kristofer Karlsson’s subsequent patch—exposing tombstones to iterator bounds checks—has now been validated as a meaningful fix, delivering a 300x speedup (e.g., 27s → 0.09s for 8,000 refs) in realistic workflows like migrating refs from `old-*` to `new-*`. The patch touches `reftable/merged.c` and related call sites, adding tombstone-skipping loops to maintain correctness while eliminating the O(n²) bottleneck.

The original CI fixes (timeout increase and test isolation) are already merged for Git 2.55, along with a GitLab CI badge addition to improve visibility. The remaining open question about enabling `GIT_TEST_LONG` for GitLab CI is still under discussion, with Junio suggesting GitLab could run a superset of GitHub’s tests rather than strict duplication. On the performance front, Kristofer’s patch resolves the last major uncertainty—its real-world impact—by demonstrating measurable improvements in workflows that involve lexicographically ordered ref deletions and re-creations. The patch is now ready for formal submission, with no technical objections remaining. Other reftable optimizations, such as redundant `stat()` calls and unbuffered reflog writes, remain unaddressed but are orthogonal to this fix.