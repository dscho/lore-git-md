The macOS CI hang issue in `t5551` and `t5559` is now effectively resolved with Jeff King’s 2-patch series, which increases Apache’s `Timeout` directive and isolates the expensive test case. The series is ready for merging, with all key participants—Jeff King, Patrick Steinhardt, and Junio C Hamano—agreeing on the approach after dropping an initially proposed ref-packing optimization as redundant.

The latest development is a small but important follow-up from Patrick Steinhardt to extend `GIT_TEST_LONG` coverage to GitLab CI for pull/merge requests. This ensures GitLab runs the same long-running tests as GitHub, closing a gap where flaky tests might otherwise go unnoticed. The patch is a straightforward adjustment to `ci/lib.sh` and aligns with the broader goal of stabilizing CI across platforms. With the Apache timeout fix in place, the risk of hangs is now mitigated, making this change a safe and logical next step. Expect it to be merged alongside or shortly after Jeff’s series.