AI mode summary:

Thread identity
Subject: [PATCH v4 0/8] commit-reach: optimize `paint_down_to_common()` for one-sided histories
Series version: v5 (final, ready-to-merge iteration; squashes mechanical fixes, removes commit-date fallback, adds trace2 diagnostics, clock-skew topologies, regression tests, and new technical documentation)
Originating author: Tian Yuchen
Thread type: patch series (performance optimization, cleanup, hardening, instrumentation, documentation, regression fix, and dead-code removal)

Core problem or goal
Optimize the `paint_down_to_common()` function in Git’s commit-reach subsystem to terminate early when one side of a merge-base query exhausts its commit queue, eliminating unnecessary traversal of large one-sided histories (e.g., repositories with import grafts or shallow histories). The optimization yields **100-1000x speedups** for asymmetric queries. This final iteration (v5) **squashes mechanical fixes**, **removes the obsolete commit-date fallback entirely**, **adds trace2 diagnostics**, **introduces clock-skew topologies**, **extends test coverage**, **updates documentation**, and **adds a new technical document** explaining the algorithm. The series is now **fully reviewed, regression-free, and ready to merge**.

Current status
Series is **fully approved and ready to merge**. All patches (1-10/10 in v5) are **stable, uncontroversial, and incorporate all feedback**. Key updates in v5:
- **Added a new technical document** (`Documentation/technical/paint-down-to-common.adoc`) explaining the algorithm, use cases, generation-number regions, termination conditions, and the removed commit-date fallback (patch 1/10).
- **Squashed patches 5/8 and 6/8 from v4** into patch 7/10 to resolve `-Wunused-function` warnings with `DEVELOPER=1` (addressing SZEDER Gábor’s feedback). This patch introduces the `paint_state` struct to track per-side commit counters (PARENT1-only, PARENT2-only, and merge-base candidates) and replaces the old `nonstale_queue` termination logic with a counter-based approach. The change is **mechanical and correctness-preserving**, with step counts in tests dropping by one in some topologies due to an earlier loop exit (documented as equivalent behavior).
- **Patch 9/10 (new in v5)** consolidates the `min_generation` termination check into `paint_queue_get()`, moving `last_gen` into the `paint_state` struct to avoid redundant calls to `commit_graph_generation()`. The change is **structural only**, with no behavior change—it centralizes generation-number handling and ensures the BUG assertion fires only when `min_generation` is set.
- **Removed the commit-date fallback entirely** (patch 10/10), deleting the unused `corrected_commit_dates_enabled()` function and simplifying the codebase. A comment was added at queue initialization to document the topological ordering requirement. Benchmarks confirm the fallback is obsolete: `git merge-base --all v4.8 v4.9` on the Linux kernel repo now runs in **17ms** (v1 commit-graph) with **5,725 steps**, down from **3.25s** and **167,413 steps** in the baseline.
- **Added trace2 diagnostics** (patch 2/10 and **patches 5/10-8/10**): `test_trace2_data_singular()` helper in `t/test-lib-functions.sh` improves diagnostic output for trace2 assertions (e.g., "expected X, got Y" instead of silent grep failures). The helper verifies trace2 events appear exactly once and match expected values, providing detailed error messages on FD 4 for debugging. **Patch 5/10 adds step-count instrumentation** to `paint_down_to_common()`, logging the number of commits processed via `trace2_data_intmax()`. **Patch 6/10 adds clock-skew topologies and step-count assertions** for v1 commit-graph edge cases, ensuring the optimization remains correct under date-ordering violations. **Patch 8/10 implements the core early-termination logic**, enabling observable performance metrics across all commit-graph modes (none, full, half, no-gdat) and complementing wall-clock benchmarks.
- **Introduced clock-skew topologies** (patch 6/10): New test cases (`se-`, `se2-`, `skew-*`) cover side-exhaustion edge cases with v1 commit graphs, including step-count assertions for deterministic verification.
- **Extended test coverage** (patches 3/10 and 4/10): Added **eight new test cases** across `t/t6600-test-reach.sh` and `t/t6099-merge-base-side-exhaustion.sh` for edge cases not covered by the existing 10x10 grid topology:
  - `t6600-test-reach.sh`: `in_merge_bases_many:self`, `get_merge_bases_many:duplicate-twos`, `get_merge_bases_many:pending-stale`, `get_merge_bases_many:infinity-both-sides`, **`se-*` and `se2-*` clock-skew topologies**, and **mixed finite/INFINITY generation cases**.
  - `t6099-merge-base-side-exhaustion.sh`: **Diamond-shaped topology** (`B` and `C` as merge-base candidates, where `B` is an ancestor of `C`), verifying `remove_redundant` correctly excludes the redundant ancestor.
- **Updated `Documentation/technical/paint-down-to-common.adoc`** to clarify that infinite-to-finite generation transitions do not always hold, document the removal of the commit-date fallback, and explain the side-exhaustion termination condition (patch 8/10).
- **Added regression tests** (patches 6/10 and 10/10) covering the scenario that previously required the commit-date fallback and side-exhaustion edge cases.
- **Benchmarks confirm no performance regression**: `git merge-base --all v4.8 v4.9` on the Linux kernel runs in **17 ms** for both v1 and v2 commit-graph data, demonstrating the side-exhaustion optimization renders the fallback obsolete. Dramatic improvements are observed in asymmetric queries (e.g., 2.6M-commit monorepo: **3.67s -> 5ms**).

The series is **technically complete** and **ready for Junio to merge**. No further design discussion or rerolls are expected.

Key technical details
- **Subsystem**: commit-reach (merge-base calculation).
- **Files touched**: `commit-reach.c`, `commit-graph.c`, `commit-graph.h`, `Documentation/technical/paint-down-to-common.adoc`, test scripts (`t6099-merge-base-side-exhaustion.sh`, `t6600-test-reach.sh`), `t/test-lib-functions.sh`, build files (`Makefile`, `meson.build`).
- **New/renamed symbols**:
  - `paint_state` struct (with counters `parent1_count`, `parent2_count`, `mb_candidate_count`, `min_generation`, `last_gen`, and **new `steps` counter**).
  - `paint_count_update()`, `paint_queue_put()`, `paint_queue_get()`.
  - `test_paint_down_steps()` helper.
  - `test_trace2_data_singular()` (trace2 diagnostics helper in `t/test-lib-functions.sh`).
- **Removed symbols**:
  - `nonstale_queue_put_dedup()`, `nonstale_queue_get_dedup()` (unused wrappers).
  - `corrected_commit_dates_enabled()` (obsolete fallback logic).
- **Behavior change**:
  - Walk terminates early when `(!state->parent1_count || !state->parent2_count) && !state->mb_candidate_count && last_gen < GENERATION_NUMBER_INFINITY` (patch 8/10).
  - The generation-monotonicity BUG assertion now fires **only when `min_generation` is set** (patch 9/10).
  - The commit queue ordering is **strictly topological** (generation-based); the fallback to commit-date ordering is **removed entirely**.
  - **Step counts drop by one in some topologies** (e.g., `t6600-test-reach.sh` from 6 to 5) due to an earlier loop exit, documented as equivalent behavior.
- **Performance instrumentation**:
  - Trace2 step counts (e.g., `paint_down_to_common steps 9` vs. `80` in git.git) via `trace2_data_intmax()` (patch 5/10).
  - Wall-clock benchmarks (e.g., `3.67s -> 5ms` for a 2.6M-commit monorepo, `188ms -> 17ms` for v1 commit-graph data with side-exhaustion).
- **Test coverage**:
  - Edge cases: self/duplicate/pending-stale/infinity-both-sides, diamond topologies (`ps-*`, `pi-*`), mixed finite/INFINITY generation numbers, criss-cross merges, grafts, shallow histories, region transitions, redundant merge-base candidates, single-merge-base selection, **clock-skew topologies (`se-*`, `se2-*`, `skew-*`)**.
  - Deterministic step-count assertions for all commit-graph modes (full, half, none, no-gdat) via `test_paint_down_steps()`.
  - The new regression test covers the scenario that previously required the commit-date fallback.
  - **Diamond-shaped topology** in `t6099` verifies `remove_redundant` correctly excludes deeper merge-base candidates.
  - **Mixed finite/INFINITY generation cases** test region transition logic.
  - **Clock-skew topologies** expose premature side-exhaustion and wrong merge-base selection under date-ordering violations (e.g., `se-D` dequeued before `se-C`, missing the merge base; `se2-MB2` returned instead of `se2-MB1`).
- **Documentation**:
  - `paint-down-to-common.adoc` clarifies that infinite-to-finite generation transitions do not always hold, documents the removal of the commit-date fallback, explains the algorithm’s use cases, priority-queue ordering, flag propagation, termination conditions, **side-exhaustion logic (patch 8/10)**, and **trace2 instrumentation**.

Open questions and loose ends
- **All technical and procedural issues resolved**. The series is **ready to merge**.
- **Follow-up work remains open but non-blocking**:
  - **Retroactive trace2 instrumentation** for earlier test patches (v2 2/7 and 3/7).
  - **Step-count checks in side-exhaustion tests** (e.g., `t6099`).

Participant positions
- **Tian Yuchen (author)**: Incorporated all feedback, squashed mechanical fixes, added trace2 diagnostics, clock-skew topologies, the new technical document, step-count instrumentation, and extended test coverage, and prepared v5. The series is **ready to merge**.
- **Kristofer Karlsson (co-signer, reviewer)**:
  - **Fixed the regression** by removing the commit-date fallback and adding a queue-initialization comment.
  - **Updated documentation** to clarify generation-number transitions and side-exhaustion logic.
  - **Added regression tests** covering the fallback scenario and clock-skew topologies.
  - **Squashed patches 5/8 and 6/8** (now patch 7/10) to resolve the `-Wunused-function` warning.
  - **Co-authored patch 9/10**, consolidating the `min_generation` check into `paint_queue_get()` and moving `last_gen` into `paint_state`.
  - **Provided benchmarks** confirming no performance regression.
  - **Co-signed patch 3/10**, incorporating Elijah Newren’s test cases.
  - **Co-authored patch 6/10**, adding clock-skew topologies and step-count assertions.
  - **Co-authored patch 7/10**, introducing the `paint_state` struct and counter-based termination logic.
  - **Confirmed the series is ready to merge**.
- **Derrick Stolee (reviewer)**:
  - **Approved the removal of the commit-date fallback**, confirming the side-exhaustion optimization renders it obsolete.
  - **Approved the queue-initialization comment** and documentation updates.
  - **Explicitly approved the entire series** in the final email, removing the last open technical question.
- **SZEDER Gábor (reviewer)**: **Requested the squash of patches 5/8 and 6/8** to avoid `-Wunused-function` warnings. **No technical objections to the removal itself**.
- **Elijah Newren (patch 2/8 and 3/10 author)**: Contributed the trace2 diagnostics helper and edge-case test coverage. No further comments; test cases remain incorporated.
- **Junio C Hamano (maintainer)**: **Ejected v3 from `seen`** due to the regression but has not yet commented on v5. The series is **ready for merging**.
- **René Scharfe (reviewer)**: Feedback on patch 5/8 (counter renaming and types) was **fully addressed in v4**.

Related work
- Commit 091f4cf3 (2018), which introduced the commit-date fallback. **Now obsolete**.
- Elijah Newren’s parallel work (PR #2150) on merge-base edge cases, whose test cases are reused here.
- No external dependencies.