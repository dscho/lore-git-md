AI mode summary:

Thread identity
Subject: [PATCH v4 0/8] commit-reach: optimize `paint_down_to_common()` for one-sided histories
Series version: v6 (final, ready-to-merge iteration; procedurally unblocked and queued in Junio’s tree)
Originating author: Tian Yuchen
Thread type: patch series (performance optimization, cleanup, hardening, instrumentation, documentation, regression fix, and dead-code removal)

Core problem or goal
Optimize the `paint_down_to_common()` function in Git’s commit-reach subsystem to terminate early when one side of a merge-base query exhausts its commit queue, eliminating unnecessary traversal of large one-sided histories (e.g., repositories with import grafts or shallow histories). The optimization yields **100-1000x speedups** for asymmetric queries. The series is **technically complete, fully reviewed, and procedurally unblocked**, but **review bandwidth remains the primary bottleneck** for advancement beyond its current queued state.

Current status
Series is **queued in Junio’s tree** (integration branch unspecified, but procedurally ready for `seen`). Key updates:
- **Base resolution confirmed**: Junio validated Kristofer Karlsson’s synthetic base approach (merging only `kk/commit-reach-find-all-fix` into `master`) as correct and integrated the series into his tree.
- **Dependency status**: `kk/commit-reach-find-all-fix` remains in `next`; no new dependencies.
- **Action required**: **Additional reviewers needed** to move the series forward. Elijah Newren has resumed his review, which may provide the final substantive feedback required for advancement. Kristofer Karlsson’s proposed simplifications (deferring fallback cleanup, dropping/squashing patches) remain non-blocking but aim to accelerate review.
- **Seasonal context**: Reviewer availability may still be impacted by vacation schedules.
- **Documentation feedback**: Elijah Newren’s **substantive review** of the technical document (`Documentation/technical/paint-down-to-common.adoc`) previously identified **two precision gaps**:
  1. The document does not account for **v1 commit-graphs**, where generation numbers saturate at `GENERATION_NUMBER_V1_MAX` and can violate topological ordering, similar to the `INFINITY` region. The invariant ("paint on already-visited commits is final") may not hold in all cases.
  2. The wording around the `min_generation` cutoff ("No merge base can have a generation below this threshold") could mislead future callers into assuming the cutoff applies universally, when it only works for callers that **do not need deeper merge bases** (e.g., reachability checks among input commits). This is a **forward-compatibility concern** for future maintainers.

Key technical details
- **Subsystem**: commit-reach (merge-base calculation).
- **Files touched**: `commit-reach.c`, `commit-graph.c`, `commit-graph.h`, `Documentation/technical/paint-down-to-common.adoc`, test scripts (`t6099-merge-base-side-exhaustion.sh`, `t6600-test-reach.sh`), `t/test-lib-functions.sh`, build files (`Makefile`, `meson.build`).
- **New/renamed symbols**: `paint_state` struct (with counters `parent1_count`, `parent2_count`, `mb_candidate_count`, `min_generation`, `last_gen`, `steps`), `paint_count_update()`, `paint_queue_put()`, `paint_queue_get()`, `test_paint_down_steps()`, `test_trace2_data_singular()`, `skew_commit` (test helper).
- **Removed symbols**: `nonstale_queue_put_dedup()`, `nonstale_queue_get_dedup()`, `corrected_commit_dates_enabled()`, `state.gen_ordered`.
- **Behavior change**: Early termination when `(!state->parent1_count || !state->parent2_count) && !state->mb_candidate_count && last_gen < GENERATION_NUMBER_INFINITY`. The commit-date fallback is removed entirely, fixing a correctness issue.
- **Performance instrumentation**: Trace2 step counts via `trace2_data_intmax()` (e.g., `paint_down_to_common steps 9` vs. `80` in git.git).
- **Test coverage**: Edge cases (self/duplicate/pending-stale/infinity-both-sides, diamond topologies, mixed finite/INFINITY generation numbers, clock-skew topologies), deterministic step-count assertions for all commit-graph modes, regression tests for fallback scenarios and ancestor exclusion.
- **Documentation**: `paint-down-to-common.adoc` explains the algorithm, generation-number regions, termination conditions, side-exhaustion logic, and Trace2 instrumentation. **Open questions** remain about its handling of v1 commit-graphs and `min_generation` wording.

Open questions and loose ends
- **Review bandwidth**: The series is **queued and awaiting additional review**. Elijah Newren’s resumed review may provide the final substantive feedback needed to advance the series. Kristofer’s proposed simplifications (deferring fallback cleanup, dropping/squashing patches) remain non-blocking but aim to accelerate review.
- **Documentation precision**: Elijah Newren’s review raises **two non-blocking but important** concerns about the technical document:
  1. **v1 commit-graph handling**: The document should clarify that `GENERATION_NUMBER_V1_MAX` can violate topological ordering, similar to `INFINITY`.
  2. **`min_generation` wording**: The phrasing should explicitly state that the cutoff only applies to callers that do not need deeper merge bases (e.g., reachability checks).
- **Test infrastructure feedback**: René Scharfe’s suggestion to use `$LF` in `test_trace2_data_singular()` is **cosmetic and non-blocking**; the patch remains valid as-is.
- **Non-blocking design points** (unchanged):
  - Feedback on the test diagnostic helper (`test_trace2_data_singular()`, patch 2/10) and the commit-date fallback removal (patch 10/10) remains welcome but is not required for merging.
  - Follow-up work (e.g., retroactive Trace2 instrumentation for earlier test patches) remains open but non-blocking.

Participant positions
- **Tian Yuchen (author)**: Series is technically complete; no further action required unless additional review feedback surfaces.
- **Kristofer Karlsson (co-signer, reviewer, patch contributor)**:
  - Co-authored 9/10 patches (documentation, test helpers, edge-case tests, Trace2 instrumentation, refactoring, and core logic).
  - Added the technical document, Trace2 instrumentation, and clock-skew test coverage.
  - Implemented the early-termination logic and fallback removal.
  - **Clarified and validated the procedural base resolution** (synthetic merge of `kk/commit-reach-find-all-fix` into `master`).
  - **Proposes simplifications** to reduce series complexity and ease review (defer fallback cleanup, drop/squashing patches).
  - Notes that **seasonal vacation schedules** may be impacting reviewer availability.
- **Derrick Stolee (reviewer)**: Approved the fallback removal, refactoring, and entire series.
- **SZEDER Gábor (reviewer)**: Approved the refactoring and fallback removal after addressing `-Wunused-function` warnings.
- **Elijah Newren (patch contributor, reviewer)**:
  - Contributed edge-case test coverage and Trace2 diagnostics.
  - **Substantive review** of the technical document (`paint-down-to-common.adoc`), identifying **precision gaps** in v1 commit-graph handling and `min_generation` wording. This feedback is **non-blocking** but aims to improve long-term maintainability.
  - **Resumed review** of the series, which may address remaining open questions about core logic, test coverage, or documentation.
- **René Scharfe (reviewer)**: Provided a **surface-level review** of `test_trace2_data_singular()` (patch 2/10), suggesting a cosmetic improvement (use of `$LF` instead of a hardcoded newline). This feedback is **non-blocking** and does not affect the core optimization.
- **Junio C Hamano (maintainer)**:
  - **Confirmed the synthetic base approach** and integrated the series into his tree.
  - **Explicitly requests additional reviewers** to move the series forward.
  - The series is **queued and procedurally unblocked**, but review bandwidth is the remaining bottleneck.

Related work
- **`kk/commit-reach-find-all-fix`**: Required dependency (in `next`) to resolve the procedural merge conflict and test failure. The synthetic base includes this topic.
- **Commit 091f4cf3 (2018)**: Introduced the commit-date fallback, now removed.
- **Elijah Newren’s PR #2150**: Provided test cases reused in this series.