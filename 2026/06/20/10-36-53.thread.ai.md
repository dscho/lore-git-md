AI mode summary:

Thread identity
Subject: [PATCH v4 0/8] commit-reach: optimize `paint_down_to_common()` for one-sided histories
Series version: v5 (mechanical follow-up to v4; squashes patches 5/8 and 6/8, removes commit-date fallback, adds queue-initialization comment, updates documentation, and adds regression test)
Originating author: Tian Yuchen
Thread type: patch series (performance optimization, cleanup, hardening, instrumentation, documentation, regression fix, and dead-code removal)

Core problem or goal
Optimize the `paint_down_to_common()` function in Git’s commit-reach subsystem to terminate early when one side of a merge-base query exhausts its commit queue, eliminating unnecessary traversal of large one-sided histories (e.g., repositories with import grafts or shallow histories). The optimization yields **100-1000x speedups** for asymmetric queries. This iteration (v5) **squashes mechanical fixes**, **removes the obsolete commit-date fallback**, **adds a queue-initialization comment**, **updates documentation**, and **adds a regression test**. The series is now **fully reviewed, regression-free, and ready to merge**.

Current status
Series is **fully approved and ready to merge**. All patches (1-8/8) are **stable, uncontroversial, and incorporate all feedback**. The **mechanical squash** (patches 5/8 and 6/8) and **follow-up regression fix** (removal of commit-date fallback) are now **incorporated into v5**. Key updates in v5:
- **Squashed patches 5/8 and 6/8** to avoid `-Wunused-function` warnings with `DEVELOPER=1`.
- **Removed the commit-date fallback entirely**, deleting the unused `corrected_commit_dates_enabled()` function and simplifying the codebase. A comment was added at queue initialization to document the topological ordering requirement.
- **Updated `Documentation/technical/paint-down-to-common.adoc`** to clarify that infinite-to-finite generation transitions do not always hold.
- **Added a regression test** covering the scenario that previously required the commit-date fallback.
- **Benchmarks confirm no performance regression**: `git merge-base --all v4.8 v4.9` on the Linux kernel runs in **17 ms** for both v1 and v2 commit-graph data, demonstrating the side-exhaustion optimization renders the fallback obsolete.

The series is **technically complete** and **ready for Junio to merge**. No further design discussion or rerolls are expected.

Key technical details
- **Subsystem**: commit-reach (merge-base calculation).
- **Files touched**: `commit-reach.c`, `Documentation/technical/paint-down-to-common.adoc`, test scripts (`t6099-merge-base-side-exhaustion.sh`, `t6600-test-reach.sh`).
- **New/renamed symbols**: `paint_state` struct (with counters `parent1_count`, `parent2_count`, `mb_candidate_count`, `min_generation`, `last_gen`), `paint_count_update()`, `paint_queue_put()`, `paint_queue_get()`, trace2 step counter, `test_paint_down_steps()` helper.
- **Removed symbols**: `nonstale_queue_put_dedup()`, `nonstale_queue_get_dedup()` (unused wrappers), `corrected_commit_dates_enabled()` (obsolete fallback logic).
- **Behavior change**: Walk terminates early when `(!state->parent1_count || !state->parent2_count) && !state->mb_candidate_count && last_gen < GENERATION_NUMBER_INFINITY`. The generation-monotonicity BUG assertion now fires **only when `min_generation` is set**, and the commit queue ordering is **strictly topological** (generation-based). The fallback to commit-date ordering is **removed entirely**.
- **Performance instrumentation**: Trace2 step counts (e.g., `paint_down_to_common steps 9` vs. `80` in git.git) and wall-clock benchmarks (e.g., `3.67s -> 5ms` for a 2.6M-commit monorepo, `188ms -> 17ms` for v1 commit-graph data with side-exhaustion).
- **Test coverage**: Edge cases (self/duplicate/pending-stale/infinity-both-sides), diamond topologies (`ps-*`, `pi-*`), mixed finite/INFINITY generation numbers, criss-cross merges, grafts, shallow histories, region transitions, redundant merge-base candidates, and single-merge-base selection. Deterministic step-count assertions for all commit-graph modes (full, half, none, no-gdat). The new regression test covers the scenario that previously required the commit-date fallback.
- **Documentation**: `paint-down-to-common.adoc` now clarifies that infinite-to-finite generation transitions do not always hold and documents the removal of the commit-date fallback.

Open questions and loose ends
- **All technical and procedural issues resolved**. The series is **ready to merge**.
- **Follow-up work remains open but non-blocking**:
  - **Retroactive trace2 instrumentation** for earlier test patches (v2 2/7 and 3/7).
  - **Step-count checks in side-exhaustion tests** (e.g., `t6099`).
  - **Trace2 diagnostics helper** (`test_trace2_data_singular`).

Participant positions
- **Tian Yuchen (author)**: Incorporated all feedback, squashed mechanical fixes, and prepared v5. The series is **ready to merge**.
- **Kristofer Karlsson (co-signer, reviewer)**:
  - **Fixed the regression** by removing the commit-date fallback and adding a queue-initialization comment.
  - **Updated documentation** to clarify generation-number transitions.
  - **Added a regression test** covering the fallback scenario.
  - **Squashed patches 5/8 and 6/8** to resolve the `-Wunused-function` warning.
  - **Provided benchmarks** confirming no performance regression.
  - **Confirmed the series is ready to merge**.
- **Derrick Stolee (reviewer)**:
  - **Approved the removal of the commit-date fallback**, confirming the side-exhaustion optimization renders it obsolete.
  - **Approved the queue-initialization comment** and documentation updates.
  - **Explicitly approved the entire series** in the final email, removing the last open technical question.
- **SZEDER Gábor (reviewer)**: **Requested the squash of patches 5/8 and 6/8** to avoid `-Wunused-function` warnings. **No technical objections to the removal itself**.
- **Elijah Newren (patch 2/8 author)**: No further comments; test cases remain incorporated.
- **Junio C Hamano (maintainer)**: **Ejected v3 from `seen`** due to the regression but has not yet commented on v5. The series is **ready for merging**.
- **René Scharfe (reviewer)**: Feedback on patch 5/8 (counter renaming and types) was **fully addressed in v4**.

Related work
- Commit 091f4cf3 (2018), which introduced the commit-date fallback. **Now obsolete**.
- Elijah Newren’s parallel work (PR #2150) on merge-base edge cases, whose test cases are reused here.
- No external dependencies.