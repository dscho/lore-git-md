Maintainer feedback on patch submission format.

Junio C Hamano (the Git maintainer) provides guidance on proper patch submission format for Tamir Duberstein's performance optimization patch. The key points:

1. Patch series versions should properly reference previous iterations using In-Reply-To headers rather than just including URLs in the changelog. This maintains proper threading in mail clients.

2. The v2 submission appears to be a new thread rather than a direct follow-up to v1, which breaks the discussion continuity. Junio emphasizes the importance of maintaining the thread structure.

The feedback is purely about submission mechanics - no technical concerns are raised about the patch content itself, which had already received positive review from Jeff King in the previous iteration. The optimization itself (filtering pathspecs before lstat() calls in `git ls-files`) remains sound, with the v2 having addressed Jeff's performance concern about multiple pathspecs.

This is standard maintainer guidance about the project's email-based workflow conventions rather than commentary on the technical merits. The patch appears ready for merging once properly threaded.