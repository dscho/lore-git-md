This is patch 4/6 in Harald Nordgren's v14 series adding `git branch --prune-merged` functionality for safe automated local branch cleanup. The series has undergone extensive review over 14 iterations with all major technical concerns now resolved.

The patch introduces the core `--prune-merged` command which deletes local branches that are reachable from their configured upstream (as identified by the `--forked` filter from earlier patches). The implementation includes multiple safety mechanisms:
- Skips branches checked out in any worktree
- Preserves branches whose upstream no longer resolves locally
- Exempts branches where push destination equals upstream (to avoid deleting freshly pulled trunks)
- Warns rather than errors on unmerged branches to allow bulk operations to continue

The command accepts multiple branch/pattern arguments and unions them, with comprehensive test coverage verifying all edge cases. The implementation builds on the series' earlier work with `--forked` filtering and integrates with the existing branch deletion infrastructure while maintaining backward compatibility.

Key technical details:
- Uses `ref_filter_forked_add()` from previous patches to identify candidate branches
- Compares `@{push}` vs `@{upstream}` to distinguish fork workflow branches
- Leverages existing `delete_branches()` infrastructure with new `DELETE_BRANCH_WARN_ONLY` and `DELETE_BRANCH_NO_HEAD_FALLBACK` flags
- Includes extensive test suite covering all safety mechanisms and edge cases

The patch appears well-polished after 14 iterations, with clear commit message documentation and thorough test coverage. It represents the core functionality promised by the series' goal of safe automated branch cleanup for fork workflows.