Here is the dense AI thread summary for this new thread:

Junio C Hamano proposes adding a `--rename` option to `git update-ref` to enable low-level ref renaming with reflog preservation. The syntax is `git update-ref --rename <old-ref> <new-ref>`, which fails if the old ref doesn't exist or new ref already exists. The implementation warns users to prefer `git branch --rename` for branch refs (`refs/heads/`) since `update-ref` doesn't handle worktree HEAD updates or branch tracking configuration. The patch does not yet support renames in `--stdin` batch mode, as that would require deeper ref transaction API changes for atomicity. Changes include updates to the update-ref documentation, command-line parsing, core rename logic in refs.c, and new test cases covering basic functionality, error conditions, and reflog handling. This is a focused plumbing addition that addresses a gap in low-level ref manipulation while deferring transactional batch support for future work.