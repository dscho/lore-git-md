This is patch 3/6 in Harald Nordgren's v14 series adding `git branch --delete-forked` (formerly `--prune-merged`), which provides safe automated local branch cleanup with configurable protections. The series has undergone extensive review over 14 iterations, with all major technical concerns now resolved.

The current patch prepares the `delete_branches()` function for bulk branch deletion by adding two new modes:
1. A `NO_HEAD_FALLBACK` mode that checks only against a branch's upstream (not falling back to HEAD when no upstream exists)
2. A `DRY_RUN` mode that shows what would be deleted without actually removing refs

The changes maintain backward compatibility for existing callers while enabling the new functionality needed for `--delete-forked`. The implementation carefully preserves all the safety checks developed in previous iterations, including protection for checked-out branches and configurable opt-outs. The dry-run output matches Git's existing deletion message format for consistency.

This is a well-tested infrastructure patch that builds on the series' solid foundation. The diff shows careful attention to edge cases and clear separation between the new modes and existing behavior. With the earlier remote-tracking branch regression now fixed, this version appears ready for final review.