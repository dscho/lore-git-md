This is a review comment on a refactoring patch in the `the_repository` removal effort. Christian Couder provides feedback on Tian Yuchen's patch that moves `protect_hfs` and `protect_ntfs` configuration variables from global scope to repository-specific storage.

The review suggests two substantive improvements: first, a minor subject line tweak to standardize the area reference ("environment:" rather than "environment.c:"), and second, the introduction of helper functions to encapsulate the repository-specific configuration access pattern. The proposed functions would handle both the repository lookup and default value fallback, making the code more maintainable and providing a clearer path for future refactoring when `the_repository` is fully removed.

The review is technically focused, identifying a specific pain point in the current implementation (the ternary expression checking `gitdir`) and offering a concrete solution that aligns with the project's architectural direction. The suggestion to add explanatory comments about the `gitdir` check's purpose shows attention to long-term code understandability. This is a substantive review that engages with the technical merits of the change while respecting the author's overall approach.