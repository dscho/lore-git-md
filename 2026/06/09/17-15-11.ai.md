Performance follow-up on worktree diff behavior. D. Ben Knoble responds to Jeff King's analysis of the racy-git issue affecting `git diff` performance in worktrees. Knoble confirms that running `git update-index --refresh` resolves the slowdown, matching King's observation that the issue stems from files sharing modification times with index entries, forcing full content checks.

The discussion now explores two solution paths: whether `git diff` should automatically refresh the index (seen as a potential band-aid) and whether new worktrees should perform an index refresh upon creation to avoid the racy state. Knoble notes that some Git operations like `git restore` may already implicitly update the index in this way.

On the technical front, Knoble investigates the USE_NSEC build option (suggested by King as a more fundamental solution via nanosecond-precision timestamps), finding that while Meson automatically handles some timestamp-related configurations, it lacks explicit USE_NSEC support. He proposes to add this and test its effectiveness, potentially submitting patches for both the build system change and the worktree creation behavior.

The exchange remains technical and collaborative, with Knoble incorporating King's diagnostic suggestions and considering multiple angles to address the underlying racy-git behavior that manifests in worktree operations. The thread has evolved from initial performance observations to exploring both immediate workarounds and more systemic solutions.