This is the first patch in the final v14 series introducing `git branch --delete-forked` (formerly `--prune-merged`), which provides safe automated cleanup of merged local branches. The series has evolved through 14 iterations with all major technical concerns resolved.

This particular patch adds the foundational `--forked` filter for `git branch --list` mode, which identifies branches whose upstream matches a given ref or glob pattern. This becomes the building block for the deletion functionality in later patches. The implementation handles both exact ref matches (like "origin/main") and glob patterns (like "origin/*"), with proper validation of input patterns.

Key aspects:
- The filter integrates with existing branch listing options like `--merged` and `--no-merged`
- Includes comprehensive test coverage of various matching scenarios
- Handles both remote-tracking and local upstream branches
- Properly validates input patterns and provides clear error messages

The patch appears well-structured with clean separation between the new filtering logic and existing code. The documentation is thorough and the test suite covers edge cases like glob patterns, repeated arguments, and combination with other filters. This looks like a solid foundation for the deletion functionality to follow in the series.