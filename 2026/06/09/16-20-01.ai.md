This is a continuation of the discussion around Pablo Sabater's RFC patch series for the experimental `git history reword` command, focusing on behavior when no changes are made to a commit. Junio C Hamano is responding to Phillip Wood's suggestion about checking committer identity in addition to message content.

Junio questions the fundamental premise of aborting when no changes are detected, whether for messages or committer info. He challenges the idea that an unchanged state should trigger an error condition, suggesting instead that silently proceeding or noting the no-op might be more appropriate than failing. This pushes back against the current implementation's approach of treating unchanged messages as an error case.

The exchange reveals a deeper design question about how Git should handle no-op modifications across commands like `reword`, `rebase -i`, and `commit --amend`. Junio's position suggests favoring silent no-op handling over explicit failure, which would require rethinking the current `COMMIT_TREE_ABORT_ON_SAME_MESSAGE` flag approach.

This moves the discussion beyond just implementation details to more fundamental UX questions about how Git should behave when users attempt modifications that don't change anything. The thread is now grappling with establishing consistent principles that could apply across multiple history-modifying commands.