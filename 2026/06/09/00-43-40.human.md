This is a technical discussion about Git's trailer parsing behavior, specifically addressing how URLs in commit messages can be incorrectly interpreted as trailers. Jeff King (Peff) responds to Kristoffer Haugsbakk's earlier report about URLs like `http://example.com` being normalized into malformed trailers (`http: //example.com`).

Peff clarifies that the core issue lies in Git's loose trailer matching rather than the `--only-trailers` option itself. He demonstrates how any trailer operation can incorrectly parse URLs as trailers when they appear at message endings. While acknowledging the problem's validity, he suggests a more precise solution than simply special-casing "https" - his proposed patch modifies the separator detection logic to explicitly exclude URL-style `://` patterns from being considered valid trailer separators.

The discussion remains focused on technical specifics, with Peff providing a concrete code change that would prevent URL schemes from being parsed as trailer keys while maintaining the existing trailer functionality for legitimate cases. This follows the thread's established context about the tension between strict parsing rules and real-world usability concerns with URL handling.