This is patch 8/17 in Patrick Steinhardt's v2 series converting packed object storage to use `struct odb_source` as part of the ODB abstraction effort. The patch refactors `has_object_pack()` to use the higher-level `packfile_store_read_object_info()` interface rather than directly calling the low-level `find_pack_entry()`.

The change is straightforward - instead of using `find_pack_entry()` to check for object existence in packfiles, it now uses the more abstracted `packfile_store_read_object_info()` which will eventually call the same underlying function. This is part of the larger architectural goal of hiding implementation details behind the ODB source interface.

The patch is small (6 lines changed) and mechanically converts one call site to use the new interface while maintaining identical behavior. This continues the series' pattern of incrementally moving packed object functionality behind the ODB source abstraction layer. The change appears uncontroversial and aligns with the architectural direction already approved in earlier reviews.