This is a review comment from Kristoffer Haugsbakk in the ongoing discussion about the `git history reword` command's behavior for unchanged commit messages. The email focuses on naming conventions for the proposed `--force` flag.

Key points:
- Questions the generic `--force` naming, suggesting it should be more descriptive (e.g. `--force-rewrite`) to clearly indicate what is being forced
- References `git-clean`'s precedent of having multiple specific force options as a model
- Agrees with the general direction of the discussion (using a force flag rather than default abort behavior) but wants the implementation to follow Git's established patterns for force options

The review is substantive, engaging with the technical design of the flag naming rather than surface-level concerns. It builds on Junio's earlier suggestion of `--force` while pushing for more precise terminology. The reference to `git-clean` provides concrete precedent for how Git handles similar cases.

This moves the discussion forward by addressing an implementation detail that will need resolution in Pablo's next version of the patch series. The comment doesn't challenge the core design direction but ensures the solution aligns with Git's existing conventions for force-like options.