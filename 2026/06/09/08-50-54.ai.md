This is the first patch in Patrick Steinhardt's v2 series converting packed object storage to use `struct odb_source` as part of the ODB abstraction effort. The patch performs a mechanical rename of `struct packfile_store` to `struct odb_source_packed` to better reflect its role as an object database source.

The change aligns the packed storage backend's naming with the project's ODB abstraction goals, where all storage backends will implement the `odb_source` interface. The patch updates all related function signatures and documentation to use the new name while maintaining identical functionality. 

This is a straightforward preparatory change that:
- Renames the core structure and all related functions/types
- Updates header files to match
- Preserves all existing behavior
- Touches 3 files with 46 insertions and 46 deletions

The patch follows the same pattern previously applied to the loose object storage backend and represents the first step in converting packed storage to a proper ODB source. As noted in the commit message, this naming change reflects the clearer architectural vision that has emerged since the structure was originally introduced.