This is a follow-up in the ongoing discussion about behavior changes for the experimental `git history reword` command. The thread has evolved from a specific proposal about aborting when commit messages are unchanged to a broader discussion about feedback patterns across all `git history` subcommands.

Pablo Sabater responds to D. Ben Knoble's observation about editor exit codes by noting that while Vim/NeoVim can abort via error exits (:cq/:cquit), this editor-specific behavior shouldn't be required when the messages are identical. The exchange remains surface-level, not engaging with Junio Hamano's earlier substantive points about timestamp updates and established behavior patterns.

The discussion continues to revolve around several key questions: whether no-op rewording should abort by default or require `--force`, how much feedback `git history` subcommands should provide (balancing Git's "rule of silence" against porcelain usability), and how these behaviors should align with existing commands like `git commit --amend` and `git rebase -i`. Junio's intervention has elevated the conversation from a single-command tweak to a subsystem-wide design consideration.