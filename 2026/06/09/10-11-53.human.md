This is part 2/6 of Harald Nordgren's v14 series adding `git branch --delete-forked` (formerly `--prune-merged`), which provides safe automated local branch cleanup. The series has gone through extensive review and iteration, with all major technical concerns now resolved.

The current patch introduces a warn-only mode for bulk branch deletion operations. Rather than erroring out when encountering unmerged branches (with the full "use git branch -D" advice), it can now emit a short warning and continue processing. This is a key enabler for the series' main functionality while maintaining safety - existing callers remain unchanged and will still error as before.

The implementation:
- Adds new DELETE_BRANCH_WARN_ONLY flag
- Modifies check_branch_commit() to conditionally warn instead of error
- Updates delete_branches() to pass flags through properly
- Maintains all existing force/quiet flag behavior
- Preserves the full error+advice path for non-bulk deletions

This change is well-contained and follows the pattern established by previous iterations of the series. The patch appears ready for merging, with no outstanding technical concerns noted in the thread. It builds on the extensive safety mechanisms already developed in prior versions (checked-out branch protection, configurable opt-outs, etc.) while enabling the new bulk cleanup use case.