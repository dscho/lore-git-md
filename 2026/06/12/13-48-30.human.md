This is a test infrastructure patch that's part of Pablo Sabater's v4 series improving `git log --graph` rendering. The patch moves a shared test helper function (`check_graph`) from being duplicated in two test files (t4215 and t6016) into the common lib-log-graph.sh library where it can be shared more cleanly.

The change is straightforward and mechanical - it extracts the identical function definitions from both test files, renames the function to follow the library's naming convention (`lib_test_check_graph`), and updates all callers in both test files to use the new shared version. The patch includes Karthik Nayak and Chandra Pratap as mentors, suggesting this may be part of a new contributor onboarding process.

This is purely a refactoring with no behavior change, done to prepare for the next patch in the series which will add new graph tests that also need this helper. The patch looks clean and follows Git's established patterns for test infrastructure sharing. Since it's part of a larger series that's already been through several iterations, this particular change is unlikely to be controversial.