This is the implementation patch for the global `fetch.followRemoteHEAD` configuration option, part of Matt Hunter's 7-patch series. The patch adds the new global setting that serves as a default for all remotes when no remote-specific `remote.<name>.followRemoteHEAD` is defined, following the same precedence pattern as `fetch.prune`/`remote.*.prune`.

The implementation:
- Adds config parsing for `fetch.followRemoteHEAD` with the same values as the per-remote version (except "warn-if-not-$branch")
- Updates the fetch machinery to check the global config when no per-remote setting exists
- Includes comprehensive test coverage verifying:
  * Feature parity between global and per-remote settings
  * Precedence rules (per-remote overrides global)
  * Edge cases like dangling symrefs and refspec fetches
  * Interaction with --quiet flag
- Rewords documentation and advice messages to clarify the relationship between the two settings

The patch maintains backward compatibility while adding the new default behavior. The test additions mirror existing tests for the per-remote version, providing thorough coverage of the new functionality. Documentation updates clearly explain the interaction between global and per-remote settings.

This appears to be a well-executed implementation of the design discussed earlier in the thread, with careful attention to edge cases and clear documentation. The changes are focused and maintainable, touching only the necessary parts of the fetch machinery while reusing existing patterns.