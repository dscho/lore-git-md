Harald Nordgren’s thread proposes two usability improvements to Git’s error messages for common command-line mistakes: one for `git branch --set-upstream-to` when users omit the slash between remote and branch (e.g., `origin main` instead of `origin/main`), and another for `git push` when users use a slash instead of a space (e.g., `origin/main` instead of `origin main`). The series has stalled slightly while Junio C Hamano seeks clarification on the term “slip” for naming the topic branch, but the real debate centers on the first patch.

Junio has pushed back on the branch upstream case, arguing that the current error message—“branch 'main' does not exist”—is accurate because the command requires the local branch to exist. In his latest email, he tries to bridge the gap by reconstructing the user’s likely intent (setting the upstream for the *current* branch to `origin/main`) and suggests a clearer commit message that frames the advice as appearing only when `origin/main` actually exists. This is a constructive step, but the core question remains: is the current error message sufficient, or does it need improvement? Junio’s phrasing (“I *think* (but I am guessing)”) signals that Harald still needs to justify the change more convincingly. The second patch, addressing the `git push` case, has drawn technical feedback from Junio but no fundamental objections; it’s now a matter of refining the implementation to avoid unnecessary computation when advice is disabled.

No other reviewers have engaged substantively, leaving the discussion between Harald and Junio. The next move is Harald’s: he’ll need to either adjust the patch to better match Junio’s interpretation of user intent or provide stronger evidence that the current error message is genuinely misleading. Until then, the series remains in limbo, with the push case closer to resolution than the branch upstream case.