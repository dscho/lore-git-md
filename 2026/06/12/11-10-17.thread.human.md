This thread is about a two-patch series from Harald Nordgren that improves Git's error messages for two common beginner mistakes: omitting the slash in `git branch --set-upstream-to=origin main` and using a slash instead of a space in `git push origin/main`. The patches add helpful advice messages that guide users toward the correct syntax, controlled by new `advice.*` configuration options. The implementation is narrowly scoped, touching only `builtin/branch.c` and `builtin/push.c`, and includes tests in `t/t3200-branch.sh` and `t/t5529-push-errors.sh`.

The series has been resubmitted as v1 after receiving no initial feedback. Harald frames it as a modest but meaningful usability improvement that doesn’t break backward compatibility, offering a practical middle ground while acknowledging that unifying the syntax would be ideal but disruptive. The patches introduce no new behavior beyond the advice messages, so they’re unlikely to be controversial—assuming reviewers agree the wording and detection logic are sound.

No reviews have been posted yet, so the main open question is whether the advice messages and implementation will hold up under scrutiny. If reviewers find the changes helpful and the code clean, this series is a strong candidate for inclusion. The thread is now waiting for feedback from the community.