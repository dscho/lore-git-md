This is the 6/7 patch in Matt Hunter's series introducing a global `fetch.followRemoteHEAD` configuration option, building on the per-remote `remote.<name>.followRemoteHEAD` from Git 2.48. The patch implements the core functionality of the new global setting while maintaining backward compatibility with per-remote configurations.

The key changes in this patch:
- Adds `fetch.followRemoteHEAD` as a fallback for remotes without explicit settings
- Documents the new option alongside the existing per-remote counterpart
- Updates the advice message to mention both configuration options
- Adds comprehensive test coverage verifying the interaction between global and per-remote settings
- Ensures per-remote settings still take precedence over the global default

The implementation follows Git's established configuration patterns, similar to how `fetch.prune` and `remote.*.prune` interact. The tests demonstrate all the expected behaviors, including edge cases like dangling symrefs and quiet mode operation. The documentation clearly explains the relationship between the two configuration options.

This appears to be a well-executed implementation of the design discussed earlier in the thread, with thorough test coverage and clear documentation. The patch maintains backward compatibility guarantees while providing the new configuration flexibility that was the series' goal.