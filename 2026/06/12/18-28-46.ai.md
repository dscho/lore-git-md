This patch introduces `path.commondir` support to `git repo info` with explicit absolute and relative variants, completing the path formatting work in this GSoC series. The implementation builds on the shared `append_formatted_path()` helper from earlier patches to provide consistent path handling.

Key technical details:
- Adds `path.commondir.absolute` (canonical path) and `path.commondir.relative` (CWD-relative path) fields
- Uses `repo_get_common_dir()` to handle worktree cases where the common dir differs from the git dir
- Extends the test infrastructure with `test_repo_info_path` helper that:
  - Creates isolated repos per test case
  - Handles both absolute and relative path verification
  - Supports environment variable overrides (GIT_COMMON_DIR/GIT_DIR)
- Includes test cases for standard repos, GIT_COMMON_DIR overrides, and GIT_DIR-only scenarios

The patch follows the established pattern from the `path.gitdir` work earlier in the series, maintaining the same explicit variant design that Junio C Hamano approved. The implementation looks clean and well-tested, with the new functionality fitting naturally alongside the existing path handling code.

This appears to be the last substantive patch in the series, completing the path formatting functionality for both gitdir and commondir cases. The thorough test coverage and careful handling of edge cases (like environment variable overrides) suggest this is ready for integration pending any final documentation polish noted in the cover letter.