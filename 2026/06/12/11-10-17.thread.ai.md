Thread identity: Usability improvement series to make Git's error messages more helpful when users accidentally misformat remote/branch arguments (e.g., `git branch --set-upstream-to=origin main` or `git push origin/main`). Originating author: Harald Nordgren. Series consists of two patches: (1) `git branch --set-upstream-to` missing-slash case, (2) `git push` slash-as-space case. Topic branch name `hn/branch-push-mistake-advise`. Current version: v3.

Core problem: Addresses two common command-line mistakes: (1) `git branch --set-upstream-to=origin main` (missing slash between remote and branch name), where the user likely intended `origin/main` but the command fails because the local branch `main` does not exist, and (2) `git push origin/main` (using slash instead of space to separate remote and refspec). The series improves error messages to guide users toward correct syntax. The first patch's detection logic is refined to only suggest the correct `<remote>/<branch>` form when the remote-tracking ref (`origin/main`) exists, reconstructing the user's likely intent as setting upstream for the *current* branch. The second patch handles the push case by validating the remote portion and excluding existing paths/repos.

Current status: Series is complete and ready for merging. Junio has queued both patches in `next` after addressing all technical feedback from v1, v2, and v3. The redundant advice check in the first patch, while noted as a minor design quibble by Junio, is documented as intentional and will not be changed. Both patches include thorough test coverage with `test_grep` assertions, and the config-based advice approach is now validated for these use cases. The author's final sign-off on the first patch confirms no further changes are expected.

Key technical details:
- Introduces two new config options: `advice.setUpstreamFailure` (branch upstream case) and `advice.pushRepoLooksLikeRef` (push case), documented in `Documentation/config/advice.adoc`.
- **First patch (branch upstream case)**: Modifies `builtin/branch.c` to detect missing slash in upstream argument, verify remote existence, and confirm matching remote-tracking ref (`origin/main`). Advice triggers only when the remote-tracking ref exists, via the helper function `die_if_upstream_looks_like_remote()`. The function uses `advise_if_enabled()` internally, while the callsite (`cmd_branch()`) redundantly checks `advice_enabled(ADVICE_SET_UPSTREAM_FAILURE)` for readability; this redundancy is documented as intentional. Test coverage in `t/t3200-branch.sh` covers the happy path (advice triggers when the remote-tracking ref exists), disabled-advice case, no-matching-ref case (no advice), local branch operations (no false positives), and slashed branch names (correct reconstruction).
- **Second patch (push case)**: Modifies `builtin/push.c` to identify slash-separated argument, validate remote portion, and exclude existing paths/repos. The helper function `die_if_repo_looks_like_ref()` gates remote lookups on the advice config to avoid performance penalties when advice is disabled. Test coverage in `t/t5529-push-errors.sh` covers edge cases (branches with slashes, unconfigured remotes, trailing slashes, existing paths).
- Files touched: `builtin/branch.c`, `builtin/push.c`, `advice.{c,h}`, `Documentation/config/advice.adoc`.
- Helper functions: `die_if_upstream_looks_like_remote()`, `die_if_repo_looks_like_ref()`.
- Test files: `t/t3200-branch.sh` (branch cases), `t/t5529-push-errors.sh` (push cases).
- Implementation maintains strict separation of concerns; changes are non-disruptive and focused solely on error messaging. No regressions in existing functionality.

Participant positions:
- **Harald Nordgren**: Author of both patches. Resubmitted v2 and v3 after addressing Junio's feedback, refining detection logic, commit messages, and test style. Added callsite comments to clarify the redundant advice check in the first patch, confirming it is intentional and correct. Final sign-off on the first patch closes discussion for that patch; no further changes expected.
- **Junio C Hamano**: Maintainer review of v1, v2, and v3. Initially critiqued the first patch's premise in v1 but approved the technical implementation in v2 and v3 after refinements. Flagged test style issues (bare `grep` vs. `test_grep`) and the redundant advice check in the first patch as minor design quibbles but deemed them non-blocking. Queued the series in `next` and confirmed it is ready for merging. Tone is constructive and approving.
- **D. Ben Knoble**: Substantive review of v1, supporting Harald's usability argument with a concrete example (`origin/HEAD` behavior). Reinforces the case for improved advice.
- **Weijie Yuan**: Surface-level endorsement of the problem statement for the first patch in v1; no engagement with implementation or later refinements.

Open questions:
- The edge case in the first patch where the local branch exists, the remote portion is invalid as an upstream, but the remote-tracking ref (`<remote>/<branch>`) exists. Junio noted this as a non-blocking follow-up but it remains unresolved.
- Whether the config-based advice approach will be extended to other commands (e.g., `git fetch`, `git pull`) or remain limited to these two use cases. This depends on broader community validation of the current implementation.

Related work:
- Series is now complete and ready for merging. All technical feedback has been addressed, including test style fixes and documentation of the redundant advice check.
- Could serve as a pattern for similar usability improvements if the approach is validated by community adoption.
- No external dependencies or prior versions; this is the final submission.