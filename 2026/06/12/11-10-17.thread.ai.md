Thread identity: Usability improvement series to make Git's error messages more helpful when users accidentally swap or misformat remote/branch arguments, consisting of two patches: (1) `git branch --set-upstream-to` missing slash case, (2) `git push` slash-as-space case. Originating author: Harald Nordgren.

Core problem: Addresses two common command-line mistakes that frustrate new users: (1) `git branch --set-upstream-to=origin main` (missing slash between remote and branch name), and (2) `git push origin/main` (using slash instead of space to separate remote and refspec). Current error messages fail to guide users toward correct syntax.

Current status: v1 series resubmitted after initial silence. Both patches in play: first handles branch upstream detection, second handles push refspec parsing. Series has received one surface-level endorsement (Weijie Yuan) but no substantive technical review. Author has pinged the list; patches remain unreviewed in terms of implementation or design.

Key technical details:
- Introduces two new config options: `advice.setUpstreamFailure` (branch upstream case) and `advice.pushRepoLooksLikeRef` (push case).
- Modifies `builtin/branch.c` to detect missing slash in upstream argument, verify remote existence, and confirm matching remote-tracking ref.
- Modifies `builtin/push.c` to identify slash-separated argument, validate remote portion, and exclude existing paths/repos.
- Test coverage added in `t/t3200-branch.sh` (branch cases) and `t/t5529-push-errors.sh` (push cases).
- Implementation maintains strict separation of concerns; changes are non-disruptive and focused solely on error messaging.

Participant positions:
- Harald Nordgren: author of both patches, resubmitted v1 after no initial feedback. Frames series as modest but meaningful improvement for new users.
- Weijie Yuan: surface-level endorsement of the problem statement; no engagement with implementation, test coverage, or advice message wording. Review is supportive but not substantive.
- No substantive reviews or other participants yet.

Open questions:
- Reviewer feedback needed on advice message wording, detection logic, and test coverage.
- Potential future extensions to other commands with similar usability issues (e.g., `git fetch`, `git pull`).
- Whether the config-based advice approach is the right long-term solution or if the errors should be unconditionally improved.

Related work:
- Series now complete with these two patches.
- Could serve as pattern for similar usability improvements elsewhere (e.g., other common command-line mistakes).
- No external dependencies or prior versions; this is the initial submission.