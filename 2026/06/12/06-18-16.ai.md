This is a technical discussion in Patrick Steinhardt's ref backend refactoring series, responding to Jeff King's performance concerns about moving to absolute paths. The exchange demonstrates the depth of historical context that informs Git's design decisions.

Patrick acknowledges Peff's point about a 2008 optimization that used relative paths for performance, noting the lack of reproducible benchmarks makes it hard to assess the current impact. He then proposes a more ambitious solution: introducing a higher-level filesystem abstraction (`struct fsroot`) inspired by Go's filesystem interfaces. This would use `openat(3p)`-style operations to maintain both performance (by avoiding path recomputation) and security (through mechanisms like `O_BENEATH`), while being more portable than direct `openat` emulation on Windows.

The proposal represents a significant architectural direction beyond the immediate ref backend changes, potentially affecting how Git handles paths across multiple subsystems. Patrick specifically mentions applying this to repository working tree management to prevent security issues. The Windows compatibility challenge remains, but the higher-level abstraction aims to make cross-platform implementation more tractable.

This response elevates the discussion from a specific performance consideration to a broader design opportunity, showing how review feedback can spark larger architectural thinking. The thread now contains both the immediate ref backend changes and this forward-looking filesystem abstraction proposal as potential paths.