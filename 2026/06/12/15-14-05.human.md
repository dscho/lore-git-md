This is Junio C Hamano's response to Randall S. Becker's build failure report regarding Rust components in Git v2.55.0-rc0. Junio points out that the Makefile already has a `NO_RUST` flag to disable Rust compilation, suggesting this as a workaround for NonStop systems where the Rust toolchain is unavailable.

The exchange highlights the ongoing tension between Git's Rustification efforts and platform compatibility. While the build system provides an escape hatch (the `NO_RUST` flag), Randall's concern about the default-enabled Rust components remains valid for platforms without Rust support. Junio's response is pragmatic but doesn't address the broader question of whether Rust should remain opt-out rather than default-on in this release candidate.

This is a typical maintainer response - pointing to existing documentation rather than proposing new solutions. The thread continues the pattern of Rust-related discussions where platform support concerns meet the project's forward momentum on memory safety improvements.