This is a test cleanup patch in Matt Hunter's series introducing a global `fetch.followRemoteHEAD` configuration option. The change prepares the test suite for a new test that will be added later in the series by ensuring proper cleanup of a temporary remote.

The patch modifies `t/t5510-fetch.sh` to add a `test_when_finished` cleanup step that removes the 'custom-head' remote after the test completes. This prevents a conflict when a subsequent test in the series tries to create the same remote. The change is minimal and focused - just adding the cleanup hook while preserving all existing test behavior.

This is a straightforward preparatory change that follows Git's test suite conventions. The commit message clearly explains the change's purpose and its relationship to the broader series. The patch demonstrates good attention to test isolation, a pattern that will be important as more tests are added for the new global configuration option.