This is a **reply in the `git replay --linearize` series**, specifically addressing Justin Tobler’s design question about the enum-to-bool refactoring in patch 1/3. Toon Claes responds to the concern that the current simplification (replacing `enum replay_mode` with a `bool reverse` flag) might need to be reverted if a third replay mode is introduced later. He clarifies that the enum was primarily used to distinguish direction (forward vs. reverse), not to define distinct modes, and that future options (like `--linearize`) are unlikely to require a new mode. He leaves open the possibility of reintroducing an enum or adding a second boolean flag if a genuinely new mode becomes necessary, but sees no immediate need.

The response is pragmatic and does not attempt to predict future requirements, focusing instead on the current design’s adequacy. This closes the open question from Justin’s review, though it does not preclude further discussion if other reviewers see a need for a different approach. The series remains under review, with no merge status reported.