This is a test cleanup patch in Matt Hunter's series introducing a global `fetch.followRemoteHEAD` configuration option. The change prepares the test suite for a new test that will be added later in the series by ensuring proper cleanup of a temporary remote.

The patch modifies `t/t5510-fetch.sh` to remove the 'custom-head' remote after the test completes, preventing conflicts with a similar test that will be introduced in patch 5/7. This is a straightforward maintenance change that follows Git's test hygiene practices by using `test_when_finished` to clean up test artifacts.

The change is small and focused - just adding one line to ensure proper test isolation. It demonstrates good attention to detail in test maintenance, though the impact is limited to the test suite itself rather than affecting any runtime behavior. This is typical preparatory work we often see in multi-patch series where later tests build on earlier test infrastructure.