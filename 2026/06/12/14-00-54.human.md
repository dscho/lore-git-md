This is a documentation clarification question from Matt Hunter about the final patch in his 7-part series implementing a global `fetch.followRemoteHEAD` configuration option. The series builds on Git 2.48's per-remote `remote.<name>.followRemoteHEAD` feature to provide a default behavior across all remotes.

Matt notes that the documentation currently uses `$branch` syntax for the "warn-if-not-$branch" value, but wonders if it should instead use `<branch>` angle-bracket notation to match Git's standard documentation style. This is a minor but potentially important consistency point for the man pages.

The question comes after the implementation is otherwise complete, with the series having already addressed core functionality, test coverage, edge cases, and documentation of the global/per-remote interaction pattern. The thread has seen positive feedback on the technical approach from Jeff King and Bence Ferdinandy.

This appears to be a final polish question before the series is ready for merging - the sort of last-minute documentation detail that often surfaces after the main implementation work is done. The answer will determine whether a minor documentation tweak is needed to align with Git's style guidelines.