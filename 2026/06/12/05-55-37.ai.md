This is a documentation fix in Matt Hunter's series implementing the global `fetch.followRemoteHEAD` configuration option. The patch corrects an advice message in `builtin/fetch.c` that incorrectly included the word "branch" when referring to the `warn-if-not-*` configuration value. The fix aligns the message with both the actual implementation in `remote.c` and the documented behavior.

The change is straightforward - it removes the extraneous "branch" from the suggested configuration command in the advice text. This is a preparatory patch in the 7-part series, cleaning up a minor inconsistency before introducing the new global configuration option in subsequent patches. The fix demonstrates attention to detail in ensuring all parts of the codebase remain consistent when referring to this configuration value.