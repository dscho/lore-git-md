**AI thread summary**:

Junio Hamano's biweekly "What's cooking in git.git" report provides a snapshot of Git development status across integration branches (maint/master/next/seen). Key developments include:

**Core changes**:
- HTTP auth headers now persist through redirects
- `b4` tool recommended for patch submission
- `rebase --update-refs` handles branch symrefs correctly
- `describe --contains --all` respects `--match`/`--exclude`

**Performance**:
- Streamlined revision walks using priority queues
- `index-pack` delta cache optimization
- Major bitmap generation speedups
- `stash -p` reuses cached index entries

**Documentation**:
- New cover letter guidelines
- `push.default=simple` behavior clarified
- Ongoing synopsis-style manpage conversions
- `--word-diff` docs improved

**Infrastructure**:
- Xcode 16.3+ macOS linker warning fixed
- HTTP pack index memory leaks plugged
- Robust subprocess handshaking
- Configuration variables migrating to per-repo storage

**Experimental**:
- New `history drop` subcommand for commit removal
- `diff.<driver>.process` for external hunk providers
- `branch --prune-merged` option

**Stalled topics** (risk discard):
- `the_repository` removal patches
- Subtree split recursion improvements

**Status updates**:
- Patrick Steinhardt's test fixes series (ps/t7527-fix-tap-output) is awaiting review (v2 sent, no feedback yet) - corrects TAP output in t7527, silences test noise including Windows EBUSY errors, and makes 'prove' fail on invalid TAP output. The "What's cooking" report incorrectly listed it as needing a reroll rather than awaiting review. Jeff King has since identified a test failure pattern in CI related to these TAP output fixes, where dockerized environments with minimal locale setups show warnings about locale settings being misinterpreted as invalid TAP output. Jeff notes these warnings were likely present before but only now being caught due to the stricter TAP parsing, raising the question of whether this indicates the tests were never working as intended on these platforms or if it's just harmless noise that's newly visible. This suggests the test fixes may need adjustment to handle these edge cases in constrained environments.

Junio notes limited changes expected next week due to his absence. The report reflects ongoing work in ODB abstraction, repository configuration modernization, and documentation standardization alongside performance optimizations and new features. The status clarification for Patrick's test series and Jeff's CI observation highlight the importance of accurate tracking and real-world testing in the "What's cooking" reports.