This is a follow-up discussion in Patrick Steinhardt's series refactoring repository initialization and object database setup, specifically addressing patch 4/8. The exchange clarifies the rationale behind the changes to ODB initialization in `repo_set_gitdir()`.

Patrick acknowledges Karthik Nayak's observation about the previous behavior being confusing - where the ODB would still be created if `skip_initializing_odb` was true but `repo->objects` didn't exist. He explains this was an intermediate step in his effort to centralize `odb_new()` calls, noting the previous state (with multiple ODB recreations) was even more problematic.

The discussion confirms the current patch represents progress toward cleaner initialization paths, though Patrick characterizes it as only "somewhat sensible" rather than final - suggesting further refinements may come in future iterations. This aligns with the series' broader goal of cleaning up repository setup as part of the ODB abstraction work. The exchange serves to document the technical reasoning behind the change while indicating this remains work in progress.