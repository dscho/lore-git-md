Here is the dense AI thread summary capturing all key information from this initial report:

This thread reports a bug in Git's trailer parsing where URLs in commit messages are incorrectly interpreted as trailers. The issue affects both `--only-trailers` and `%(trailers:only)` formatting. When a standalone URL appears (particularly at message endings), the URL scheme (`http`, `https`, `file`) gets parsed as a trailer key, normalizing lines like `https://example.com` to malformed `https: //example.com`. Reporter Kristoffer Haugsbakk provides three concrete cases: 1) Non-trailer URLs getting mangled (NTL), 2) Line-wrapped URLs creating false trailers (LW), and 3) Patch processing introducing bad trailers (NL). Analysis of Linux kernel history shows 245 real-world instances of `https: //` trailers, demonstrating this occurs in accidental cases and through normalization pipelines. While technically consistent with current parsing rules, this violates the design principle of avoiding false positives in trailer detection. The solution would require special-casing URL schemes but presents edge case challenges (like C-style comments after URLs). The bug impacts users processing commit messages with trailer normalization, particularly in projects where URLs commonly appear at message endings. No proposed fix yet; the report establishes the problem space and real-world prevalence.