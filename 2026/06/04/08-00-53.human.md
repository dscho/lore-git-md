This is a follow-up discussion about naming the new `die_for_required_opt()` helper function in the parse-options API. Christian Couder weighs in on Jean-Noël Avila's earlier naming concerns, agreeing that the current function name and parameters could be clearer.

Christian supports Jean-Noël's suggestion to rename the function to `die_for_missing_opt()` and make the parameter names more explicit about the dependency relationship. He further proposes using "dependent_opt" instead of "tested_opt" for the first parameter, which would make the asymmetric nature of the check even more obvious to callers.

The exchange shows consensus forming around improving the API's clarity through better naming. Both reviewers are focused on making the function's purpose and usage more self-documenting, particularly for cases where one option requires another. This is a substantive discussion about API design that will likely lead to a v2 of the patch with revised naming.