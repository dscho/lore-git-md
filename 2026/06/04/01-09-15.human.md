This is a response from the maintainer regarding a feature proposal about mirror repositories for submodules. Junio C Hamano pushes back on the initial idea from Benson Muite, arguing that a mirror fallback mechanism shouldn't be limited to just submodules if implemented at all.

The maintainer's position is clear: he sees the core idea (fallback mirrors) as potentially useful, but objects to implementing it only for submodules. He suggests that such a feature should work more generally across Git operations rather than being submodule-specific. This indicates the proposal in its current form is unlikely to progress without being reworked to address the broader use case.

The response doesn't reject the concept outright but does set a higher bar - any implementation would need to consider mirror fallbacks as a general mechanism rather than a submodule-specific feature. This suggests the thread may continue with either a revised proposal or discussion about how such a general mechanism might work.