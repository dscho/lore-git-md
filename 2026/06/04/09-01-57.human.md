This is a technical follow-up in Patrick Steinhardt's `git history drop` series, specifically addressing Junio C Hamano's review of the new `RESET_HEAD_SKIP_REF_UPDATES` flag for `reset_head()`.

Patrick concurs with Junio's suggestion to add another BUG check that prevents the logically inconsistent combination of `skip_ref_updates` and `update_orig_head` flags. This brief exchange confirms the technical correctness of Junio's observation while maintaining the thread's focus on robust API design for the reset machinery changes.

The interaction demonstrates the project's attention to detail in internal interfaces, even for supporting functionality. Patrick's quick agreement suggests this will be incorporated cleanly into the upcoming v3 of the series, which is already undergoing significant refactoring to support the new `git history drop` subcommand.