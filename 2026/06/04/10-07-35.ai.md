This is a test infrastructure patch in Patrick Steinhardt's series addressing TAP output validation issues. The patch fixes a specific problem in t7810 (grep tests) where a multibyte regex check was breaking TAP output format when it failed.

The change converts the MB_REGEX capability check from an immediate test to a lazy prerequisite, which prevents the test-tool output from corrupting the TAP stream if the check fails. This matches the pattern used elsewhere in the test suite for similar capability checks.

The fix is straightforward and mechanical - it just wraps the existing check in test_lazy_prereq(). This maintains the same functionality while making the test output more robust. The patch was prompted by Jeff King's report of the TAP output issue and fits into the broader series' goal of making test output reliably parseable.

This is one of several test fixes in the series, with this version specifically addressing grep test reliability. The change is small and focused, touching only t/t7810-grep.sh with a simple 5-line modification.