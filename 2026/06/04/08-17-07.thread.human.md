The thread continues to explore workflow improvements for the experimental `git history split` command, with users providing concrete examples of how the current implementation could be enhanced. Rasmus Villemoes originally suggested two key features: an iterative splitting mode to avoid manual SHA-1 lookups between operations, and an "atomize" subcommand for hunk-based splitting. Patrick Steinhardt, the command's author, responded positively to both suggestions, proposing `--iter` and `--explode` as potential option names and revealing ongoing work on a TUI interface.

Kristoffer Haugsbakk has now joined the discussion, demonstrating through a real-world example how the current workflow requires repetitive manual steps when splitting a commit with subject "anchor" multiple times. His example specifically shows the need to restart the split process after each operation, reinforcing Rasmus' point about the tedium of multi-step splits. Both contributors' examples highlight how an iterative mode would streamline the process by automatically working with remaining changes after each split. The thread remains productive, with Patrick's initial positive response, these user experience improvements appear likely to be implemented in future versions of the command.