This is patch 6/8 in Patrick Steinhardt's series refactoring repository initialization and object database setup, part of the ongoing ODB abstraction effort. The patch removes an oddity where Git would initialize an object database even when no repository exists, specifically when `GIT_DIR_EXPLICIT` points to a non-existent directory.

The key change moves object database initialization inside the `have_repository` check in `setup_git_directory_gently()`, since there's no need for an ODB when there's no repository. The one exception - `git-index-pack` running outside a repository - is handled by modifying `parse_commit_graph()` to check for the object database rather than the gitdir.

This is a mechanical cleanup that maintains existing behavior while removing a confusing special case. The change is well-motivated in the commit message, which explains both the historical oddity and the one case that needed special handling. The patch follows the series' pattern of incremental, behavior-preserving refactoring steps toward the final goal of centralizing ODB creation.

The diff shows focused changes to just two files, with clear safety checks preserved. This continues the series' careful progression toward patch 8/8's final centralization of ODB creation in `apply_repository_format()`.