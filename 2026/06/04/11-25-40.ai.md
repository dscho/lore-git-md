This is patch 13/16 in Patrick Steinhardt's series converting packed object storage to use `struct odb_source` as part of the ODB abstraction effort. The patch wires up the final required callback - `freshen_object()` - for the packed source implementation.

Key changes:
- Moves `packfile_store_freshen_object()` from packfile.c to odb/source-packed.c
- Implements it as the `odb_source_packed_freshen_object()` callback
- Removes the last external caller of `find_pack_entry()`, allowing it to be made static
- Updates the files backend to use the new callback interface
- Maintains identical behavior while hiding implementation details

The patch continues the mechanical conversion pattern seen throughout the series, with 24 lines added and 29 removed across 5 files. It completes the callback implementation for packed sources, leaving no remaining loose ends in the interface wiring. The changes are focused and well-contained, following the established pattern from previous patches in the series.

This marks a significant milestone in the ODB abstraction work - with all callbacks now implemented, the packed storage backend has been fully converted to the new interface. The remaining patches in the series will likely focus on cleanup and final integration.