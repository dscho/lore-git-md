This is patch 13/16 in Patrick Steinhardt's series converting packed object storage to use `struct odb_source` as part of the ODB abstraction effort. The patch wires up the final callback (`freshen_object()`) for the packed source implementation.

Key changes:
- Moves `packfile_store_freshen_object()` from packfile.c to odb/source-packed.c
- Makes `find_pack_entry()` static since it now has no external callers
- Updates the files backend to use the new callback interface
- Maintains identical behavior while hiding implementation details

The patch continues the mechanical conversion pattern seen throughout the series, with +24/-29 lines of mostly code movement. It completes the callback implementations for the packed source, leaving no remaining loose ends in the interface wiring. The changes appear straightforward and well-contained, following the established pattern from previous patches in the series.

This brings the ODB abstraction effort one step closer to completion, with just 3 patches remaining in the series. The work remains unreviewed but follows Patrick's proven approach to this multi-year architectural change.