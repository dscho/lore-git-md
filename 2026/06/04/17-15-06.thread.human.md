This thread addresses Windows-specific limitations with handling large objects (>4GB) on LLP64 systems where `unsigned long` is 32-bit. The patch series, originally contributed by Philip Oakley and maintained in Git for Windows, converts object header length handling from `unsigned long` to `size_t` across key code paths. The changes have been in production use for years in Git for Windows, validated by Johannes Schindelin who has signed off on all patches.

The series is effectively complete with comprehensive test coverage across all major code paths, including direct file hashing, `--stdin` input, and filter processing. Junio C Hamano has confirmed the technical approach is sound but requested a rebase due to conflicts with in-flight topics. Patrick Steinhardt has suggested making the 5GB test case unconditional (while keeping its EXPENSIVE marker) and continues an architectural discussion about whether to use `size_t` or introduce an `objsize_t` typedef for cross-platform consistency.

The latest addition is a test case for basic file hashing, which Patrick reviewed with a minor organizational note about commit structure. This follows the established pattern of the series and doesn't raise new technical concerns. With all production code already validated and only administrative work remaining, the series appears ready for merging once rebased and pending consideration of Patrick's type consistency question.