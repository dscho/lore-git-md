This is the 7/16 patch in Patrick Steinhardt's series converting packed object storage to use `struct odb_source` interface as part of the ODB abstraction effort. The patch refactors `has_object_pack()` to use the higher-level `packfile_store_read_object_info()` rather than directly calling the low-level `find_pack_entry()`, which will eventually become file-local to the packed source implementation.

The change is straightforward - replacing a direct packfile lookup with the new abstraction layer while maintaining identical behavior. The diff shows this is a simple 4-line reduction with no functional change, just moving the implementation detail behind the new interface. This continues the mechanical conversion pattern seen in earlier patches of the series.

The patch appears well-contained and low-risk, following the established approach of the ODB abstraction work. It's one of several similar interface conversions needed to complete the packed source implementation. Reviewers will likely focus on verifying the behavioral equivalence and checking for any subtle differences in error handling between the old and new paths.