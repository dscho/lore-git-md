Here's a human-readable summary of the thread's current state:

Patrick Steinhardt continues his major refactoring of Git's object storage system, now at patch 14/16 in his series converting packed object storage to use the new `struct odb_source` interface. This work completes a key milestone in the ongoing ODB abstraction effort that will eventually allow pluggable storage backends.

The latest patch handles the remaining callback functions needed for packed storage, implementing no-op stubs for write operations (since packed storage is read-only) and alternates handling. This follows the established pattern of the series - maintaining existing behavior while moving implementation details behind the new interface. With this patch, all required callbacks are now either properly implemented or explicitly marked as not applicable.

The series has now completed the core technical work of implementing the interface, with just two patches remaining for final integration and cleanup. While the changes are substantial (touching 24 files with over 1400 lines changed), each patch has been carefully structured to maintain identical behavior while hiding implementation details. The work builds on Patrick's earlier conversion of loose object storage and represents another step toward making Git's storage layer fully pluggable.

So far the series hasn't received review feedback, which isn't unusual for complex refactoring work at this scale. The remaining open questions center around integration with other in-flight work (particularly around midx and repack handling) and final cleanup of temporarily exposed internals.