This thread is about Patrick Steinhardt’s 17-part patch series (v3) completing the ODB (object database) abstraction for Git, specifically converting the packed object storage to use `struct odb_source_packed`. The series has achieved its mechanical goals—aligning the packed and loose ODB source interfaces, wiring up all callbacks, and refactoring MIDX (multi-pack-index) interfaces—but remains blocked by unresolved refdb lifecycle issues (memory leaks, duplicate refdb creation, and initialization ordering) that Patrick is addressing in a separate 11-patch fix series.

Junio C Hamano, the maintainer, has acknowledged the series as mechanically sound and ready for integration once the refdb issues are resolved. The latest email adds a minor merge-fix note: Junio flagged a type conflict in patch 15/17 (which wires up stub callbacks for unsupported operations) where the `len` parameter in `odb_source_packed_write_object()` collides with an in-flight topic changing it from `unsigned long` to `size_t`. He’s already prepared an evil merge to resolve this for the Windows build, so no action is needed from Patrick. This is a routine merge coordination detail and doesn’t affect the series’ substance.

Justin Tobler has provided surface-level mechanical reviews, confirming type-system consistency and callback wiring patterns, but no deeper architectural engagement. The series is now fully complete in terms of ODB abstraction changes, with all callbacks registered, MIDX interfaces refactored, and the packed source’s interface aligned with the loose source’s precedent. The only remaining hurdle is the refdb fix series, which depends on Patrick’s earlier "ps/setup-centralize-odb-creation" work. Once those issues are resolved, the series should be ready for merging.