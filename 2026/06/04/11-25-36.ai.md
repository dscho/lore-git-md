This is the ninth patch in Patrick Steinhardt's 16-part series converting Git's packed object storage to use the `struct odb_source` interface as part of the broader ODB (object database) abstraction effort.

The patch wires up the `read_object_stream()` callback for packed object sources, implementing the interface required for streaming object contents. Key changes:
- Moves the streaming implementation from `packfile.c` to `odb/source-packed.c`
- Updates the files backend to use the new callback via `odb_source_read_object_stream()`
- Removes the now-unused `packfile_store_read_object_stream()` helper
- Maintains identical behavior while hiding implementation details

The changes are mechanical and follow the same pattern as previous patches in the series, continuing the conversion of packed object storage to the new abstraction layer. The patch touches 4 files with a net change of 0 lines (+17/-17), indicating pure code movement rather than functional changes.

This brings the series closer to completing all required callbacks for packed storage, with 4 remaining after this patch (`for_each_object()`, `count_objects()`, `find_abbrev_len()`, and `freshen_object()`). The changes appear straightforward and well-contained, following the established pattern from earlier in the series.