This is a follow-up discussion about workflow improvements for the experimental `git history split` command. Kristoffer Haugsbakk responds to Rasmus Villemoes' earlier feedback about iterative splitting process, providing a concrete example of how they would use the command with the current workflow.

The email demonstrates the real-world need for an iterative mode by showing the manual steps required when splitting a commit with subject "anchor" multiple times having to restart the split process after each operation. This reinforces Rasmus' original point about the current workflow being tedious for multi-step splits.

The exchange continues the productive discussion about user experience improvements for this new command, with Kristoffer's example serving as additional evidence that the proposed iterative mode would be valuable. The thread remains focused on practical enhancements to the splitting functionality, with both contributors providing clear use cases from their own workflows.

For the summarizer brief: The email shows a user demonstrating the current multi-step process for splitting commits with `git history split`, specifically using `:/anchor` to select a commit by subject. This provides concrete evidence supporting Rasmus' earlier request for an iterative mode that would automate the repeated splitting of remaining changes. The example centers around maintaining the original "anchor" commit subject through multiple splits.