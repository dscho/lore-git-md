[AI mode thread summary]

This thread addresses Windows-specific LLP64 limitations for large objects (>4GB) by converting object header lengths from `unsigned long` to `size_t` across object-file and hash algorithm code paths. The series, originally authored by Philip Oakley 4.5 years ago and maintained in Git for Windows, is effectively complete with all production code changes merged and validated in Git for Windows.

Current status:
- Complete series with 6 test cases covering all major code paths, including a new test for basic file hashing verification.
- All production code changes already merged and validated in Git for Windows with Johannes Schindelin's sign-off on all patches.
- Junio C Hamano confirms the technical approach is sound but requests a rebase due to conflicts with in-flight topics.
- Patrick Steinhardt proposed removing the `!LONG_IS_64BIT` test prerequisite to make the 5GB test run on all platforms supporting large files; this change has been accepted by Johannes Schindelin.
- Patrick also raised an architectural discussion about using `size_t` vs introducing an `objsize_t` typedef, which Johannes firmly rejected, reaffirming `size_t` as the correct choice for memory-addressable sizes.
- Johannes clarified that `off_t` is appropriate only for streaming scenarios on 32-bit platforms.
- No urgency indicated for the rebase as Junio works through his backlog.

Key technical details:
- Core changes in `object-file.c`, `sha1dc_git.c`, and `hash.h`.
- SHA1DC interface modified to use `size_t` while maintaining abstraction.
- Test infrastructure in `t/t1007-hash-object.sh` (previously `t/t1006-hash-object.sh`) handles 5GB test files.
- Tests validate:
  * Direct file hashing (new test)
  * `--stdin` input
  * Standard hashing path
  * Clean filter processing
- The 5GB test is now unconditional (keeping the `EXPENSIVE` marker) and no longer platform-specific.
- Automatic platform skipping remains for irrelevant configurations.

Validation:
- Years of production use in Git for Windows.
- All test cases pass for 5GB files across all code paths.
- Johannes Schindelin's sign-off confirms Windows-specific validation.

Open items:
- Rebase needed to resolve conflicts with current in-flight topics.
- No outstanding technical objections or architectural concerns.
- Follow-up conversions in other subsystems would use the same pattern.

Related work:
- Builds on Philip Oakley's original LLP64 compatibility work.
- Complements ongoing large-file handling improvements.
- Test infrastructure reusable for other platform validation.
- Conflicts with unspecified in-flight topics in Junio's integration branches.

Participant positions:
- Johannes Schindelin:
  - Signed off on all patches, confirms production validation.
  - Firmly rejects `objsize_t` approach, reaffirms `size_t` as correct choice.
  - Clarifies type rationale: `size_t` for memory-addressable sizes, `off_t` for streaming scenarios.
  - Accepted Patrick's suggestion to remove the `!LONG_IS_64BIT` test prerequisite.
- Philip Oakley: Original author, series maintained in Git for Windows fork.
- Junio C Hamano: Approves technical approach but requests rebase due to integration conflicts.
- Patrick Steinhardt:
  - Proposed making 5GB test unconditional by removing `!LONG_IS_64BIT` prerequisite (now accepted).
  - Raised architectural discussion about type consistency (now resolved).
  - Noted minor test organization improvement (addressed).