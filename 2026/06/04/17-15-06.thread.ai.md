[AI mode thread summary]

This thread addresses Windows-specific limitations with large objects (>4GB) on LLP64 systems where `unsigned long` is 32-bit. The series, originally contributed by Philip Oakley 4.5 years ago and maintained in Git for Windows, converts object header length handling from `unsigned long` to `size_t` across the object-file and hash algorithm code paths.

The progression so far:
1. Initial test case demonstrating 5GB file hashing failure on LLP64 (`t/t1006-hash-object.sh`)
2. Mechanical conversion of `object-file.c` functions (`hash_object_file()`, `write_object_file_prepare()`)
3. SHA1DC implementation updated to use `size_t` for length parameters
4. New test verifying `git hash-object --stdin` handles large inputs correctly on LLP64
5. Additional test case (current patch) for basic `git hash-object` without special flags

Key technical details:
- Changes focus on the `--literally` path in `--stdin` and direct file hashing
- SHA1DC interface modified while maintaining abstraction layers
- Call path safety explained via `git_hash_sha1_update()` indirection
- Core files affected: `sha1dc_git.c`, `hash.h`, `object-file.c`
- Test file generation logic shared across test cases
- New test validates standard hashing path (non-`--literally`) for completeness

Validation:
- Johannes Schindelin has signed off all patches
- Code proven in Git for Windows production use
- All test cases now pass for 5GB files on LLP64 (direct file, stdin, and standard hashing paths)
- Tests automatically skip on platforms where `size_t` isn't 64-bit or where `long` is already 64-bit

Open items:
- Remaining subsystems needing similar conversions
- Potential follow-up for any edge cases in non-standard hashing scenarios
- The series remains at version 1 on the main list but has real-world validation from its Git for Windows history. Subsequent patches are expected to address remaining areas while maintaining the established pattern of mechanical `unsigned long` to `size_t` conversions.