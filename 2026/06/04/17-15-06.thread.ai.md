[AI mode thread summary]

This thread addresses Windows-specific LLP64 limitations for large objects (>4GB) by converting object header lengths from `unsigned long` to `size_t` across object-file and hash algorithm code paths. The series, originally authored by Philip Oakley 4.5 years ago and maintained in Git for Windows, is now **merged to `next`** in its second iteration (`v2`) with all production code changes and test cases finalized.

**Core problem or goal**
The series resolves 32-bit truncation of object sizes on Windows (LLP64) by systematically replacing `unsigned long` with `size_t` in object-file handling code (`object-file.c`, `sha1dc_git.c`, `hash.h`, `odb/source-*`). The goal is to enable Git to correctly process objects larger than 4GB on all platforms where `size_t` is 64-bit, while maintaining backward compatibility and avoiding regressions on 32-bit systems.

**Current status**
- **Merged to `next`** by Junio C Hamano; no further changes expected.
- All patches (`v2 1/6` through `6/6`) are present and conflict-free. The series resolves prior conflicts with `ps/odb-source-loose` and removes the `!LONG_IS_64BIT` test prerequisite. The 5GB test now runs unconditionally on all platforms supporting large files (retaining the `EXPENSIVE` marker).
- All production code changes (type conversions) were validated in Git for Windows prior to upstream submission.
- **Patch 1/6** (baseline test) adds a 5GB file-hashing test case to `t/t1007-hash-object.sh` demonstrating the original failure on 32-bit `unsigned long` platforms. The test uses `git hash-object --literally --stdin` and is marked `EXPENSIVE`.
- **Patch 2/6** updates `object-file.c` and ODB source files (`odb/source-files.c`, `odb/source-inmemory.c`, `odb/source-loose.c`) to use `size_t` for memory buffer lengths in `hash_object_file()`, `write_object_file_prepare()`, and their callers.
- **Patch 3/6** updates the SHA1DC interface (`sha1dc_git.c` and `sha1dc_git.h`) to use `size_t` for section lengths, removing the implicit downcast in the `git_SHA1_Update` macro. The test in `t/t1007-hash-object.sh` is updated to reflect the fix (5GB file-hashing case changes from `test_expect_failure` to `test_expect_success`).
- **Patches 4/6, 5/6, and 6/6** add test cases verifying `--stdin`, standard file-hashing, and clean filter code paths with 5GB files, respectively. All are marked `EXPENSIVE` and `SIZE_T_IS_64BIT`.
- **GitGitGadget (GGG) range-diff mechanics**: GGG uses `git range-diff --creation-factor=95` to force aggressive patch matching between iterations, ensuring heavily modified patches are paired rather than treated as deletions/additions. This value was empirically determined and adopted by Junio for local workflow sanity checks.

**Key technical details**
- **Files touched**: `object-file.c`, `sha1dc_git.c`, `sha1dc_git.h`, `hash.h`, `odb/source-*` (type conversions from `unsigned long` to `size_t`).
- **Test infrastructure**: `t/t1007-hash-object.sh` (10 test cases covering direct file hashing, `--stdin` input, standard hashing, clean filter processing, and both `--literally` and standard methods with 5GB files). The baseline test uses `test-tool genzeros $((5*1024*1024*1024))` and expected hashes for SHA-1 (`0be2be10a4c8764f32c4bf372a98edc731a4b204`) and SHA-256 (`dc18ca621300c8d3cfa505a275641ebab00de189859e022a975056882d313e64`).
- **SHA1DC interface**: Updated to use `size_t` while preserving abstraction; the `git_SHA1_Update` macro no longer downcasts from `size_t` to `unsigned long`.
- **Test prerequisites**: The 5GB tests are unconditional (no platform-specific prerequisites beyond `SIZE_T_IS_64BIT` for `--stdin`, standard hashing, and filter tests) but retain the `EXPENSIVE` marker. Automatic skipping remains for irrelevant configurations (e.g., platforms lacking large-file support).
- **Symbols updated**: `hash_object_file()`, `write_object_file_prepare()`, `hash_object_body()`, `git_SHA1DCUpdate()`, and their callers in `odb/source-*` now use `size_t` for buffer lengths.

**Open questions and loose ends**
- None. The series is technically complete; all design discussions (type consistency, test organization), procedural items (conflict resolution, prerequisite removal), and range-diff mechanics are resolved.

**Participant positions**
- **Junio C Hamano** (high weight): Merged the series to `next`; confirmed technical soundness and conflict resolution. Adopted GitGitGadget's `--creation-factor=95` setting for local range-diff workflows after Johannes Schindelin's explanation.
- **Johannes Schindelin** (high weight):
  - Signed off on all patches; confirmed production validation in Git for Windows.
  - Rejected `objsize_t` approach, reaffirming `size_t` as correct for memory-addressable sizes.
  - Clarified type rationale: `size_t` for memory-addressable sizes, `off_t` for streaming on 32-bit platforms.
  - Accepted Patrick Steinhardt’s suggestion to remove `!LONG_IS_64BIT` prerequisite.
  - Preferred separate test commits for clarity (rejected Patrick’s suggestion to combine the file-hashing test with the initial commit).
  - Explained GitGitGadget's range-diff mechanics (`--creation-factor=95`) in response to Junio's procedural question.
- **Philip Oakley** (medium weight): Original author; series maintained in Git for Windows.
- **Patrick Steinhardt** (high weight):
  - Proposed removing `!LONG_IS_64BIT` prerequisite (accepted).
  - Raised architectural discussion about type consistency (resolved).
  - Suggested minor test organization improvement (rejected).

**Related work**
- Builds on Philip Oakley’s original LLP64 compatibility work.
- Complements ongoing large-file handling improvements (e.g., `ps/odb-source-loose`).
- Test infrastructure reusable for other platform validation efforts.