This is a follow-up discussion in Patrick Steinhardt's series refactoring repository initialization and object database setup, specifically addressing patch 4/8. The exchange clarifies the motivation behind the changes to ODB initialization in `repo_set_gitdir()`.

Karthik Nayak had previously noted the confusing behavior where the ODB would still be created if `skip_initializing_odb` was true but `repo->objects` didn't exist. In this response, Patrick acknowledges the oddity, explaining that this was an intermediate step toward centralizing `odb_new()` calls. He contrasts it with the previous even more confusing behavior of recreating the ODB multiple times, noting the current patch makes things "somewhat sensible."

The discussion shows both contributors aligning on the technical merits of the change, with Patrick providing historical context for the transitional state being fixed. This is a minor but clarifying exchange in a series that has already received thorough review and maintainer approval.