This is a review comment on a test infrastructure series introducing a lint tool to convert bare 'grep' assertions to 'test_grep' in Git's test suite. The comment comes from D. Ben Knoble in response to patch 4/6 of the series.

The review is surface-level, focusing on a maintainability improvement suggestion for the Makefile integration rather than engaging with the technical substance of the lint tool itself. Knoble observes that the inline shell script in the Makefile for checking lint-style tests is complex and suggests extracting it to a separate script for easier maintenance. The comment is constructive but doesn't address the core functionality or implementation details of the lint tool.

The original patch being reviewed adds the core lint-style.pl tool with its first rule for handling test_grep negation patterns, converting shell-level negation (`! test_grep`) to use test_grep's built-in negation (`test_grep !`). The series as a whole aims to improve test suite quality by detecting and fixing problematic grep patterns that hide diagnostic output on failure.

This review interaction is still early in the thread's lifecycle, with no substantive technical feedback yet provided on the series. The author (Michael Montalbo) will need to consider whether to incorporate Knoble's Makefile suggestion in a future iteration.