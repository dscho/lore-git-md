This is a technical discussion about repository mirroring and submodule URL resolution, with Jeff King (Peff) proposing an alternative approach to the UUID-based solution mentioned earlier in the thread.

Peff suggests leveraging Git's `url.*.insteadOf` configuration mechanism as a way to handle repository identity and mirroring without introducing new UUID infrastructure. His key points:

1. The `insteadOf` feature already provides URL rewriting capabilities that could solve the mirroring problem without new protocols or UUIDs
2. Demonstrates how submodule URLs could be rewritten either temporarily (with `-c`) or permanently (via config files)
3. Questions whether UUIDs are actually needed when URL rewriting can achieve similar results
4. Notes the current limitation that URL rewriting must be configured per-user rather than per-repository

The email provides concrete examples of how the existing `insteadOf` system could be used to:
- Map abstract repository identifiers to concrete URLs
- Support different mirror locations for the same logical repository
- Handle protocol switching (e.g., https to ssh)

Peff's proposal shifts the discussion toward using existing Git mechanisms rather than inventing new ones, while acknowledging there may be gaps in his understanding of the original use case. This continues the thread's evolution from submodule-specific solutions toward more general repository mirroring approaches.