This is the final patch in the v4 series optimizing `paint_down_to_common()` for one-sided histories. It consolidates the `min_generation` termination check into `paint_queue_get()`, moving the `last_gen` tracking into `struct paint_state` so that `commit_graph_generation()` is called exactly once per dequeued commit. The change is purely internal to the commit-reach subsystem and has no user-visible behavior change.

The patch addresses Derrick Stolee’s concern about the `last_gen` abstraction by encapsulating it within `struct paint_state`, ensuring the generation number is computed once and reused across all termination checks (stale entries, side exhaustion, and generation cutoff) and the monotonicity BUG assertion. The `min_generation` check is now documented as a fourth termination condition in the new `paint-down-to-common.adoc` technical document, making the algorithm’s behavior more transparent.

This is a clean refactoring that improves code clarity and maintainability without altering functionality. The series as a whole is now fully reviewed, regression-free, and ready to merge. The test suite robustly verifies correctness across all commit-graph modes, and benchmarks show dramatic performance improvements for asymmetric queries. If you care about merge-base performance in large repositories, this series is worth your attention.