This is the author's final sign-off on the first patch of the v3 usability series (`hn/branch-push-mistake-advise`), confirming acceptance of Junio's minor design quibble about the redundant advice check. The patch modifies `builtin/branch.c` to detect missing slashes in `--set-upstream-to` arguments (e.g., `origin main` instead of `origin/main`) and suggest the correct syntax when the remote-tracking ref exists. The series is now fully approved, with both patches queued in `next` and ready for merging. Key details: the helper function `die_if_upstream_looks_like_remote()` uses `advise_if_enabled()` internally, while the callsite redundantly checks `advice_enabled(ADVICE_SET_UPSTREAM_FAILURE)` for readability—this redundancy is documented as intentional. The patch includes test coverage in `t/t3200-branch.sh` for all edge cases (disabled advice, no matching ref, local branches, slashed names). No further changes are expected; this email closes the discussion on the first patch. The second patch (handling `git push origin/main` mistakes) was already approved in the same thread. Files touched: `builtin/branch.c`, `advice.{c,h}`, `Documentation/config/advice.adoc`, and test file `t/t3200-branch.sh`. The new config option `advice.setUpstreamFailure` controls the behavior.