This is a **refactoring patch** in the `commit-reach` subsystem, part of the v4 series optimizing `paint_down_to_common()` for one-sided histories. Patch 6/8 removes two unused wrapper functions—`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`—that became redundant after earlier refactoring in the series. The core `nonstale_queue` functions remain in use by `ahead_behind()`, so this is a clean dead-code removal with no behavior change.

The patch touches only `commit-reach.c`, deleting 18 lines of code. The wrappers were introduced to deduplicate commits by checking the `ENQUEUED` flag, but the series’ prior work (patch 5/8) replaced this logic with a more explicit `paint_state` struct and counter-based tracking. This change is uncontroversial and aligns with the series’ goal of simplifying the codebase while preparing for the core optimization in patch 7/8.

No test updates are needed, as the functions were unused and the patch is purely mechanical. The removal is safe and ready to merge.