This is a **substantive review** of Johannes Schindelin’s HTTPS-proxy regression fix. Junio C Hamano replies to the patch, noting that the missing `return 0` in the HTTPS branch is glaringly obvious in hindsight and questioning whether the function should return an error at all.

The review identifies a design-level issue: `set_curl_proxy_type()` currently returns `-1` only when the proxy scheme is unsupported, but the pre-regression version of the function (before `663d7abe07ea`) did not treat unsupported schemes as an error condition. Junio suggests refactoring the function to return `void` instead, removing the error-return path entirely, which would make the fix even simpler and eliminate the possibility of similar fall-through bugs in the future.

The patch itself remains a valid, minimal fix for the immediate regression, but Junio’s observation shifts the discussion toward a more robust long-term solution. No objections to the current patch are raised; the review is purely additive, proposing an alternative approach that could be implemented as a follow-up. The thread is now open to whether the function’s contract should be tightened or relaxed.