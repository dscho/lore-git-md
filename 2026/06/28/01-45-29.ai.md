This is a **test suite bugfix** that has now been queued by Junio C Hamano. The patch corrects a fragile test in `t/t3420-rebase-autostash.sh` where `! grep dirty file3` was incorrectly used to verify the absence of dirty content after `git rebase --quit`. The test should instead assert that `file3` does not exist at all, using `test_path_is_missing file3`. The fragility arose because `grep` fails when the file is missing, which could mask other test failures.

The patch was originally proposed nearly five years ago by SZEDER Gábor, resurfaced by him, and has since received substantive endorsement from Phillip Wood, who confirmed the correctness of the change. Todd Zullinger identified a minor commit message typo ("Thighten" → "Tighten"), which will need correction before merging. Junio’s reply indicates he will review and queue the patch, signaling it is now on track for inclusion.

The fix is self-contained, touching only test logic in a single file (`t/t3420-rebase-autostash.sh`) and affecting one test case executed under three rebase strategies (`--apply`, `--merge`, `--interactive`). It aligns with Git’s test suite conventions (prior art: commit 2745817028) and addresses a bug introduced in commit 9b2df3e8d0. No dependencies or follow-up work are involved.

Key technical details for future discussion: the test file (`t3420-rebase-autostash.sh`), the test helper (`test_path_is_missing`), and the specific test case modified. The patch carries minimal risk, as it changes only test assertions without altering production code. Junio’s queuing resolves the last open question—whether the patch would be accepted—leaving only the typo fix as a remaining action.