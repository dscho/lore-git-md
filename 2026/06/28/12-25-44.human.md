This is a cleanup patch in the ongoing `paint_down_to_common()` optimization series. Part of v4 of an eight-patch effort to speed up merge-base calculations, this patch removes two wrapper functions—`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`—that became unused in the previous commit. The core `nonstale_queue_put()` and `nonstale_queue_get()` functions remain, as they are still used by the `ahead_behind()` function.

The change is minimal and mechanical: eighteen lines are deleted from `commit-reach.c`, leaving the rest of the codebase untouched. There is no behavior change, no new tests, and no documentation impact. The patch is uncontroversial and ready to merge, serving as a straightforward tidying step before the series’ core optimization lands.