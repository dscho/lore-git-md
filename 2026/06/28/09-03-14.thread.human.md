This thread started with Jeff King (Peff) fixing a memory leak in the reftable backend’s `reftable_writer_new` function, where an allocation was performed before input validation. The fix was straightforward—moving the allocation after the block size check—and was quickly merged. However, Peff later noted a minor textual conflict with a recent commit that split up write options. The resolution is simple: keep the new `hash_id` initialization with the other option setups while moving the allocation after validation. This is purely a mechanical adjustment to keep the code clean during integration, with no functional changes. The reftable backend, an ongoing effort by Patrick Steinhardt and Karthik Nayak, remains unaffected beyond this trivial merge resolution.