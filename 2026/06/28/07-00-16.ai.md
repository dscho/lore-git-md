This is the maintainer's final review of the first patch in Harald Nordgren's v3 usability series. Junio C Hamano acknowledges a minor design quibble—he would have preferred the `advise_if_enabled()` call inside `die_if_upstream_looks_like_remote()` to be the sole config check, rather than having the caller redundantly check `advice_enabled()`—but declares it too small to warrant a rewrite. The patch is now marked ready for the `next` branch, confirming the series will graduate in the next integration cycle. No further changes are expected. Files touched: `builtin/branch.c`; helper function `die_if_upstream_looks_like_remote()`.