This is a **substantive review reply** in the `git replay --linearize` thread, addressing a **regression in v5** that drops commits when replaying a single branch containing merge commits. Johannes Schindelin identifies a specific test case (replaying `master~2..master` with `--linearize --onto master~2`) where the v5 implementation incorrectly replays the tip commit directly onto the `--onto` target, omitting the first replayed commit ("Git 2.55-rc2"). He traces the issue to the removal of the `replayed_base` parameter, which was originally introduced to prevent this exact commit-dropping behavior.

The review engages deeply with the **algorithmic design** of `--linearize` when handling multiple branches, noting that the problem becomes "very tricky" when replaying three branches where one is a merge of the other two. Schindelin argues that the `replayed_base` design is necessary to ensure all replayed commits are linearized into a single topology, and warns that attempts to preserve non-reachability between branches would likely surprise users. His example topology (branches A, B, and C where C merges A and B) illustrates the complexity of the edge case. The message is **tested**: it includes a concrete reproduction scenario, expected vs. actual output, and a clear explanation of why the current approach fails. No alternative implementation is proposed, but the critique is precise enough to guide a fix. This feedback is likely to block merging until addressed.