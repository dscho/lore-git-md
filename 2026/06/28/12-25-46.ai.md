This is the eighth and final patch in the v4 series optimizing `paint_down_to_common()` for one-sided histories. It consolidates the `min_generation` termination check into `paint_queue_get()`, moving the `last_gen` monotonicity tracking into `struct paint_state` so that `commit_graph_generation()` is called exactly once per dequeued commit. The change is purely internal to `commit-reach.c` and does not alter behavior or on-disk formats.

The patch addresses Derrick Stolee’s concern from v3 about the abstraction leak of `last_gen` by encapsulating it inside `struct paint_state`. The `min_generation` cutoff is now documented as a fourth termination condition alongside stale entries, side exhaustion, and the finite-generation guard. The diff is minimal and focused: the `min_generation` check and BUG assertion move from the main loop into `paint_queue_get()`, and the "pop first" form (pop, check, then decrement counters) ensures the optimization remains correct.

This patch is uncontroversial and ready to merge. It completes the series by tidying up the termination logic and improving code clarity without introducing new edge cases. The test suite already verifies the `min_generation` behavior, and the trace2 instrumentation added in patch 4/8 will catch any performance regressions.