This is the first patch in the v4 series optimizing `paint_down_to_common()` for one-sided histories. It adds a new technical document, `Documentation/technical/paint-down-to-common.adoc`, explaining the algorithm, its use cases, generation-number regions, and termination conditions.

The document is thorough and well-structured, covering both the paint-walk mechanics and the subtleties of generation-number regions (finite vs. INFINITY). It clarifies the four termination conditions (queue empty, stale entries, generation cutoff, single result) and explains why early termination is safe. The cross-reference to `commit-graph.adoc` is a nice touch for readers unfamiliar with generation numbers.

This is a documentation patch with no behavior changes. The only code change is a one-line comment in `commit-reach.c` pointing to the new document. The patch is uncontroversial and ready to merge; it provides essential context for the rest of the series, particularly the side-exhaustion optimization introduced in patch 7/8. The build system updates (`Makefile` and `meson.build`) are mechanical and correct.