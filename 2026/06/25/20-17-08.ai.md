This is a **test suite bugfix** and a thread resolution note from Junio C Hamano. The email closes the loop on the no-op test in `t/t4216-log-bloom.sh` that broke TAP compliance on non-x86 architectures. Junio decides to merge Patrick Steinhardt’s deletion patch (which Todd Zullinger has already tested) in time for Git 2.55.0, sidestepping the unresolved debate about whether the test’s original intent—sanity-checking murmur3’s behavior on signed `char`—is still relevant.

The key takeaway is the **release-blocking fix is now settled**: the patch will land as a simple deletion, restoring TAP compliance without further delay. Junio leaves the door open for Taylor Blau to resurrect the test later (e.g., via `test_lazy_prereq`) if he can justify its value, but the immediate regression is resolved. The decision reflects the project’s usual pragmatism: when a fix is low-risk and the alternative risks delaying a release, the mechanical solution wins.

For the future AI summarizer, note the technical details: the patch touches `t/t4216-log-bloom.sh`, removes the `test_signed_char` prerequisite and its associated `test_cmp` call, and has no impact on user-facing behavior or on-disk formats. The unresolved question about murmur3’s sensitivity to `char` signedness remains, but it is now decoupled from the release timeline. Todd’s `Tested-by` trailer confirms the fix works, and Junio’s tone suggests no further discussion is needed before merging.