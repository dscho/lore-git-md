This is a documentation patch that finalizes the long-overdue documentation for `git log`'s `--no-follow` option. The patch updates `Documentation/git-log.adoc` and `Documentation/config/log.adoc` to clearly document `--no-follow` alongside `--follow`, including how it overrides the `log.follow` configuration setting.

The patch is the fourth iteration of a series that began in May 2026, with each version incorporating feedback from Junio Hamano, the project maintainer. This version keeps the focus narrow: it documents `--no-follow` and its precedence over `log.follow` while leaving the existing limitations of `--follow` (single-file pathspecs, undefined behavior with directory pathspecs or non-linear history) unchanged. The commit message notes a textual conflict with another topic in `next` (`mv/log-follow-mergy`), but the resolution is straightforward—this patch simply appends the `--no-follow` override documentation to the existing text.

The documentation now clearly states that `--follow` only works for a single file (not directories or multiple pathspecs) and that `--no-follow` disables this behavior regardless of whether it was enabled via command line or configuration. The patch also preserves references to the original implementation commits, which is a nice touch for historical context.

This is a well-scoped, uncontroversial documentation improvement. The collaboration between Tamir Duberstein and Junio Hamano demonstrates the project's commitment to precise, accurate documentation, even for small features. The patch should be ready for merging once the minor conflict with `mv/log-follow-mergy` is resolved.