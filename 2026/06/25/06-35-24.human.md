This is a brief follow-up in Patrick Steinhardt’s **v5 refactoring series** for Git’s reference backend, specifically addressing Justin Tobler’s review of **patch 7/11**. The patch moves parsing of `core.logAllRefUpdates` back into the ref backends to avoid premature config reads that could trigger recursive initialization.

Patrick acknowledges Justin’s observation that `refs_parse_log_all_ref_updates_config()` cannot return `LOG_REFS_UNSET`, making the second condition in the `if` statement redundant. He confirms the patch’s intent is solely to check whether the configuration is already set, which the first condition (`repo_config_get_string_tmp()`) already handles. The fix will simplify the logic by removing the unnecessary check.

This is a minor, uncontroversial adjustment that doesn’t affect the series’ core goal of lazy-loading write configuration. The change is purely mechanical and aligns with the broader effort to unify backend config handling. No further discussion is expected beyond this clarification.