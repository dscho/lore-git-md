This is **patch 5/11 in Patrick Steinhardt’s v6 refactoring series** for Git’s reference backend, a **mechanical cleanup** that removes the now-unused `chdir_notify_reparent()` function. The patch is a direct follow-up to patch 4/11, which eliminated the last callers of this function by fixing lifecycle management in the `chdir_notify` subsystem.

The change is **minimal and uncontroversial**: it deletes 26 lines of dead code from `chdir-notify.c` and updates the header comment in `chdir-notify.h` to remove references to the deprecated convenience function. The diff shows no functional behavior changes—only the removal of an API that was rendered obsolete by earlier patches in the series. This is a **preparatory step** for the lazy-loading refactor, ensuring the `chdir_notify` subsystem is stable and free of unused code before introducing deferred config parsing.

The patch touches only two files (`chdir-notify.c` and `chdir-notify.h`) and has no test coverage of its own, as it removes code rather than changing behavior. Reviewers are unlikely to raise objections here, given the clear motivation (dead code removal) and the fact that the function’s callers were already removed in patch 4/11. This is a **low-risk, high-hygiene** change that aligns with the series’ goal of simplifying the ref backend’s initialization sequence.

For the broader series, this patch is a **minor but necessary** step. It clears the way for the lazy-loading design (patches 8–11/11) by ensuring the `chdir_notify` subsystem is in a clean state. The removal of `chdir_notify_reparent()` also signals that the series is moving toward a more explicit, callback-driven model for path updates, which may reduce future maintenance burden. No follow-up work is expected beyond what’s already in the series.