This is a refactoring patch in the ongoing `git cat-file --batch-command` security series (GSoC v14, patch 7/13). It generalizes the `write_fetch_command_and_capabilities()` function into `write_command_and_capabilities()` by making the protocol command name a parameter rather than hardcoding "fetch". The change is mechanical: the function now takes a `const char *command` argument, and callers in `fetch-pack.c` pass "fetch" explicitly.

The patch is part of the series' broader effort to enable protocol command reuse, specifically preparing for the new `remote-object-info` command introduced later in the series. The refactoring itself is straightforward and behavior-preserving, with no functional changes—only signature and call-site adjustments. The commit message and code changes clearly document the purpose and scope, and the `Helped-by` trailers indicate substantive review from Jonathan Tan and Christian Couder.

This is a clean, low-risk refactoring that directly supports the series' goal of secure remote object metadata queries. The patch touches only three files (`connect.c`, `connect.h`, and `fetch-pack.c`) and maintains backward compatibility while enabling future extensibility. No review feedback or test failures are noted, and the change aligns with the series' established pattern of preparatory refactoring.