This is the first patch in Harald Nordgren’s two-part usability series, which improves error messages for common command-line mistakes like `git branch --set-upstream-to=origin main` (missing slash). The patch is technically sound and ready for merging, but this short reply addresses a minor implementation detail raised by Junio C Hamano.

Harald confirms that the `advise_if_enabled()` wrapper inside `die_if_upstream_looks_like_remote()` is intentional, even though the caller already checks the same config option. His reasoning is that the wrapper ensures the advice message includes the standard "this can be disabled" footer, maintaining consistency with Git’s other advice messages. This is a small but deliberate choice—it doesn’t change the patch’s behavior, but it keeps the user experience uniform.

The exchange is purely mechanical and doesn’t reopen any of the earlier discussions about edge cases or test coverage. Junio’s question was the last open item from his review, so this reply effectively closes the loop on the first patch. The series as a whole remains on track for merging, with no further technical feedback needed on this part.