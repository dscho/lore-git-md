This is the ninth patch in the fourteenth iteration of a security-focused feature series implementing `git cat-file --batch-command` support for querying remote object metadata. The patch enables servers to advertise their support for the `object-info` capability, specifically the "size" feature, which allows clients to request object sizes without downloading the full objects.

The change is minimal and targeted: it modifies `object_info_advertise()` in `serve.c` to populate the `value` strbuf with the string "size" when the `transfer.advertiseobjectinfo` configuration is enabled. This ensures clients can discover whether the server supports object size queries before attempting them. The patch preserves the existing behavior of disabling the capability by default (`advertise_object_info = 0`) unless explicitly configured.

The commit message is clear and concise, explaining the purpose of the advertisement mechanism: to let clients decide whether to query for object metadata or fall back to fetching objects. The `Helped-by` trailers credit Jonathan Tan and Christian Couder, indicating substantive review of the approach. The patch itself is straightforward, with no complex logic or edge cases, and aligns with the series' broader goal of secure, capability-based remote object metadata queries.

This patch is a necessary step in the series, bridging the gap between the client-side implementation (introduced in later patches) and the server's ability to signal its capabilities. The change is well-motivated and uncontroversial, with no apparent risks or downsides. It complements the earlier preparatory refactoring (e.g., function relocation and command generalization) and the subsequent patches that implement the client-side logic and format validation. The series as a whole remains on track for merging, with all prior feedback addressed and no outstanding objections.