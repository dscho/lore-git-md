This is a refactoring patch preparing for a larger feature series that implements `git cat-file --batch-command` for remote object metadata queries. Specifically, this is patch 5/13 in the v14 iteration of the series, which is now considered ready for merging after addressing all prior review feedback.

The patch modifies `fetch-pack.c` to make `write_fetch_command_and_capabilities()` more portable before it gets moved to `connect.c` in a later patch. The key change is replacing the static `advertise_sid` variable with direct initialization via `repo_config_get_bool()`. This is safe because the function is only used in protocol v2 (where the v1-specific `advertise_sid` modification doesn't apply), and `connect.c` already depends on `config.h`.

The patch is small and mechanical, changing only four lines. It includes a minor type adjustment (`int` to `const unsigned int` for `hash_algo`) that improves type safety without changing behavior. The commit message clearly explains the motivation and safety considerations, and the patch carries sign-offs from three contributors, including the original authors of related preparatory work.

This looks like a well-justified cleanup that enables future code sharing without introducing risk. The series as a whole has been thoroughly reviewed and addresses security-sensitive protocol design, so this preparatory step appears solid.