This is a small but important utility patch in the ongoing `git cat-file --batch-command` security series. It adds `strtoul_szt()`, a new helper function that safely converts strings to `size_t` values with proper error handling. The function mirrors existing helpers like `strtoul_ui()` but targets `size_t`, which is needed for the upcoming changes to `object_info.sizep` in later patches.

The implementation is straightforward: it wraps the standard `strtoul()` call with checks for negative values, conversion errors, and trailing characters, returning `-1` on failure and `0` on success. This matches Git's established pattern for numeric conversion helpers and ensures consistent error handling across the codebase.

This patch is the second in a 13-patch series that has been through 14 iterations. The series as a whole is now considered ready for merging, with all substantive review feedback addressed. The addition of `strtoul_szt()` here is a direct response to a recent change in the Git codebase where `object_info.sizep` was switched from `unsigned long` to `size_t`—this helper ensures that conversion remains safe and consistent.

The patch itself is uncontroversial and well-motivated. It touches only `git-compat-util.h`, adding a single static inline function with no behavior changes elsewhere. The commit message clearly explains the purpose and context, and the implementation follows Git's established style for numeric conversion helpers. There are no open questions or objections to this change.