This is the final review comment on Patrick Steinhardt’s `git history drop` series, now at v6. Christian Couder notes that the series looks ready to merge, with one remaining concern: code duplication between `compute_pending_ref_updates()` (introduced in this series) and `replay_result_queue_update()` (in `replay.c`). Both functions allocate and populate lists of reference updates, but Christian does not propose a specific refactoring—just flags the duplication as a potential maintenance issue.

The review is substantive but narrow: it engages with the code’s structure and long-term maintainability rather than surface-level details. Christian also points out a minor stylistic improvement (using `is_bare_repository(repo)` instead of the global variant) that will become possible once Patrick’s parallel `ps/setup-drop-global-state` series lands. His tone is positive, calling the series “great” and thanking the author.

For a reader scanning the digest, this email signals that the series is nearly done. The only open question is whether the duplication is intentional or worth addressing in a follow-up patch. The rest of the series—including the core `drop` implementation, dry-run support, reset API modernization, and reference update phase separation—appears uncontroversial and well-reviewed. If you care about history-editing tools or the ongoing `the_repository` removal effort, this is a series to watch as it approaches merge.