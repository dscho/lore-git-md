This is a minor follow-up in the `hn/branch-push-mistake-advise` usability series, specifically about the first patch (the `git branch --set-upstream-to` case). The series as a whole is ready for merging, with all technical feedback addressed and Junio having queued it in `seen`.

Junio raises a small but valid readability concern: the `die_if_upstream_looks_like_remote()` helper already wraps its advice in `advise_if_enabled()`, yet the caller (`cmd_branch()`) also gates the call on `advice_enabled(ADVICE_SET_UPSTREAM_FAILURE)`. This double-check is functionally redundant and could confuse future readers. Junio suggests adding a comment at the callsite to clarify why the redundant check is present (to ensure the standard "this can be disabled" footer appears in the advice message).

Harald’s response confirms the design intent: the wrapper ensures consistency with Git’s advice-message formatting conventions, even if it creates a redundant check. This is a deliberate choice, not an oversight, and the code is correct as-is. The only remaining question is whether a comment is needed to prevent future confusion. This is a surface-level documentation issue, not a technical one, and does not block merging. The discussion is now purely about code clarity for maintainers, not about functionality or correctness.