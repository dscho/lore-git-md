This is a refactoring patch in the ongoing `git cat-file --batch-command` security series (GSoC v14, patch 8/13). The patch moves variable initialization from the `FETCH_CHECK_LOCAL` state to the start of `do_fetch_pack_v2()`, preparing for a future change where the state machine might begin in a different initial state. The change is mechanical and behavior-preserving: the same variables (`allow_unadvertised_object_request`, `use_sideband`, and `args->deepen`) are set to the same values, just earlier in the function. The commit message explicitly notes there is no functional change, and the `Helped-by` trailers indicate substantive review from Jonathan Tan and Christian Couder. This is a safe, preparatory step that enables later patches in the series without introducing risk.