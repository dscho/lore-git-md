Thread identity
Subject: [PATCH v6 1/11] setup: inline check_and_apply_repository_format
Series version: v6 (11 patches)
Originating author: (not yet recorded; will be captured from future emails)
Thread type: patch series (refactoring preparatory to avoiding chdir-notify reparent)

Core problem or goal
Refactor the repository setup code in `setup.c` to eliminate the `check_and_apply_repository_format()` helper function, inlining its logic into its two callsites (`enter_repo()` and `init_db()`). This is a mechanical, no-functional-change patch that prepares for a later patch in the series which will reorder the reading and applying of the repository format, making the helper unsuitable.

Current status
Patch v6 1/11 is the first step in the series. No review feedback has been recorded yet. The patch is uncontroversial in isolation but is justified only by the follow-up changes.

Key technical details
- File: `setup.c`
- Functions modified: `enter_repo()`, `init_db()`
- Structures touched: `struct repository_format`, `struct strbuf`
- Flags: `APPLY_REPOSITORY_FORMAT_HONOR_ENV`
- Inlined logic: `check_repository_format_gently()` and `apply_repository_format()` calls, including initialization and cleanup of the `repository_format` struct and a `strbuf`.

Open questions and loose ends
None yet; the patch is self-contained and awaits review.

Participant positions
None recorded yet.

Related work
This patch is part of a larger series (v6, 11 patches) whose stated goal is "refs: avoid chdir-notify reparent". The inlining is preparatory for a later patch that will change the order of reading and applying the repository format.