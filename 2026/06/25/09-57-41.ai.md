This is the third patch in Patrick Steinhardt’s four-patch v3 series refactoring Git’s connectivity checks to generalize promisor object handling. The patch extracts the promisor-specific logic from `check_connected()` into a new helper function, `check_connected_promisor()`, improving readability and maintainability. This is a pure refactoring with no behavior changes, but it sets the stage for the next patch (4/4), which replaces the packfile-specific `find_pack_entry_one()` with a generic ODB iteration.

The extracted function, `check_connected_promisor()`, encapsulates the logic for checking whether all new tips are present in promisor packs. It returns `1` if all objects are found (indicating full connectivity and no need for a rev-list scan), `0` if any object is not in a promisor pack (triggering a fallback to the rev-list-based connectivity check), or a negative error code on failure. The function also ensures the latest packfiles are loaded via `odb_reprepare()` before iterating through packs. The original nested loops and gotos are replaced with a cleaner control flow, addressing Christian Couder’s feedback from v1 about the complexity of the original implementation.

The patch touches only `connected.c` and does not introduce new symbols or modify existing behavior. The extracted function is static and used solely within `check_connected()`. The commit message explicitly notes that further simplification will occur in the next patch, signaling that this is an intermediate step toward the series’ goal of making connectivity checks backend-agnostic. The `Suggested-by` trailer credits Christian Couder for the refactoring idea, reinforcing the collaborative nature of the change.

This patch is uncontroversial and aligns with the series’ broader goal of enabling ODB abstraction. The refactoring is mechanical and well-motivated, with no edge cases or performance implications introduced. Reviewers are unlikely to raise substantive concerns here, though they may scrutinize the interaction between this function and the generic ODB iteration logic in the next patch. The patch’s value lies in its preparatory role for Patch 4, where the real generalization occurs. For the AI summarizer, key details include the new `check_connected_promisor()` function, its return values (`1`, `0`, or negative error codes), and its role in isolating promisor-specific logic.