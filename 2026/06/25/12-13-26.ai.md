This is a test infrastructure refactoring patch, part of the ongoing `git cat-file --batch-command` security-hardened feature series (GSoC v14, patch 4/13). The patch extracts three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. This change is explicitly preparatory: the commit message states these utilities will be needed by a subsequent patch in the series, and the goal is to improve code reuse and readability for future tests.

The patch itself is mechanical and uncontroversial. It creates the new library file, moves the functions verbatim, and updates `t1006-cat-file.sh` to source the new library. The functions are low-level string helpers with no behavioral changes, so the risk of introducing bugs is minimal. The diff is clean and the commit message clearly explains the motivation.

This is a small but necessary step in the larger series, which has already addressed all substantive review feedback from prior versions. The thread context confirms the series is now ready for merging, and this patch is part of the shared test infrastructure that supports the new `remote-object-info` command and its associated tests. No review feedback on this specific patch is visible in the provided context, but given its simplicity and the series' overall readiness, it appears to be a straightforward improvement.