This is a surface-level review comment from Christian Couder on patch 9/10 of Patrick Steinhardt’s `git history drop` series (v6). The thread is now complete, with all functionality implemented and all prior feedback addressed.

Christian points out that the new code in `builtin/history.c` for collecting reference updates appears to duplicate logic already present in `replay_result_queue_update()` from `replay.c`. The duplicated pattern involves allocating and growing an array of reference updates, then populating each entry with the refname, old OID, and new OID. This observation is purely about code structure and potential redundancy; Christian does not suggest a specific fix or raise any behavioral concerns.

The patch itself is a preparatory refactoring that splits reference update handling into two phases—computation and application—without changing the actual behavior. This separation is intended to make the code more maintainable and to support the upcoming `drop` subcommand. The comment is a minor note on code hygiene rather than a substantive objection, and it does not appear to block the series from moving forward.