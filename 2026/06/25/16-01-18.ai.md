This is a documentation patch, the final iteration (v4) of a series that clarifies the behavior of `git log`'s `--follow` and `--no-follow` options. The patch documents the long-standing but previously undocumented `--no-follow` option and its interaction with the `log.follow` configuration setting.

The patch is narrowly scoped: it adds documentation for `--no-follow` alongside the existing `--follow` entry in `git-log.txt`, and updates `log.adoc` to note that `--no-follow` overrides `log.follow`. The changes are minimal and follow the project's standard pattern for documenting `--foo`/`--no-foo` option pairs. The commit message and in-patch commentary make clear that this iteration limits itself to the `--no-follow` override behavior, leaving the existing `--follow` limitations (single-file, linear history) unchanged.

This version resolves the textual conflict with the `mv/log-follow-mergy` topic in `next` by preserving that topic's wording while appending the `--no-follow` override. The patch is well-motivated: it addresses a real gap in the documentation (the `--no-follow` option has existed since 2012 but was never documented) and incorporates feedback from Junio Hamano, the maintainer, across multiple iterations. The documentation now clearly states that `--no-follow` overrides both command-line `--follow` and the `log.follow` config setting.

The patch touches only documentation files (`Documentation/git-log.adoc` and `Documentation/config/log.adoc`) and includes references to the original implementation commits. It is uncontroversial and appears ready for merging. The thread context confirms that this is the final, approved version of the series, with no open questions or loose ends.