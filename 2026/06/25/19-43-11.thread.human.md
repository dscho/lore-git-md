This thread is about a small but user-visible bugfix in Git’s GPG/SSH signature verification. Currently, if `gpg.ssh.allowedSignersFile` isn’t configured, Git errors out entirely when trying to show SSH signatures (e.g., with `git show --show-signature`), forcing users to set up the file even if they just want to inspect the signature’s key or validity. The patch, authored by an unnamed contributor, changes this behavior so that Git falls back to a "novalidate" mode when the file is missing, displaying the signature details (key type, fingerprint, and "Good"/"Bad" status) while advising the user to configure the file for full validation.

The fix is narrowly scoped to `gpg-interface.c` and adds a hint via `advice.h` to guide users toward proper configuration. The commit message includes clear before/after examples, showing how the output improves without requiring additional setup. There are no test or documentation updates, as the change is purely behavioral and self-explanatory. The motivation is to avoid blocking users from basic signature inspection just because they haven’t configured an optional validation feature—a classic case of progressive enhancement. So far, there’s been no review or discussion on the list, so the patch’s fate (merge, feedback, or revision) remains open.