This is the maintainer’s final note on the test-style fix for Harald Nordgren’s usability series. The series—two patches improving error messages for common command-line mistakes like `` `git branch --set-upstream-to=origin main` `` and `` `git push origin/main` ``—is now complete and queued for tonight’s `seen` push-out.

Junio C Hamano points out that the test-suite style issue (bare `grep` instead of `test_grep`) could have been caught by locally testing the topic branch merged into `seen`. This is a gentle reminder of the workflow: contributors should always verify their patches against the integration branch to catch such mechanical problems before they reach the list. The fix itself is trivial—seven lines in six test cases—and Harald has already accepted it, so the series is ready to graduate. No further discussion is expected.