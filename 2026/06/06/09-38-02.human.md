This is a build system patch finalizing a fix for silent mode behavior in git-gui's Makefile.

Johannes Sixt (Hannes) gives the final approval to Harald Nordgren's v3 patch, confirming the technical changes are correct. The patch now properly handles `make -s` silent mode by fixing the inverted `findstring` parameter order and guarding the echo statements appropriately. Hannes notes one remaining administrative issue - the email addresses in the From header and Signed-off-by trailer don't match - but indicates this can be resolved without requiring another revision since Harald has used the gmail address consistently in other patches.

The thread demonstrates effective collaboration between contributor and reviewer, with Harald incorporating all substantive feedback across three versions to produce a clean fix for the silent mode behavior. The only outstanding item is a trivial email address consistency check that Hannes can handle when applying the patch. This appears ready for merging into the git-gui build system.