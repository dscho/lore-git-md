This is a technical discussion in the ongoing worktree diff performance thread, focusing on architectural refinements for handling racy-git entries. Jeff King (Peff) responds to Junio C Hamano’s proposal to add a new field to the `istate` structure for tracking racy entries, expressing cautious agreement but reaffirming his preference for addressing the root cause via nanosecond timestamp precision.

Peff acknowledges Junio’s suggestion as "fairly clean" but reiterates that eliminating racy entries entirely—via his nanosecond-precision patch—would be the ideal solution. He briefly addresses Junio’s tangential question about `refresh_index_quietly()` in `cmd_diff()`, suggesting it could be moved to `builtin_diff_files()` but noting that its current placement is harmless. He also observes that `--no-optional-locks` (a mechanism to avoid index refreshes when background processes might conflict) is not widely used with `git diff`, implying the refresh call’s impact is minimal in practice.

The email advances the thread by clarifying Peff’s stance: while he is open to Junio’s `istate` field proposal as a fallback, his nanosecond-precision patch remains the preferred approach. No new patches or performance data are introduced; this is a design discussion refining the tradeoffs between architectural changes (Junio’s proposal) and root-cause fixes (Peff’s patch). Key technical details include the `istate` field, `refresh_index_quietly()`, and `--no-optional-locks`. The open question is whether Peff’s nanosecond patch will encounter edge cases, leaving Junio’s proposal as a viable alternative.