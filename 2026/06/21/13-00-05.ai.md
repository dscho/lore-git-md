This is a build system patch targeting the gitk and git-gui repositories, part of the ongoing effort to align their Makefile behavior with core Git's quiet build conventions. The email is a review from Johannes Sixt (gitk maintainer) responding to Harald Nordgren's v2 patch that removes `--statistics` from `msgfmt` invocations and eliminates gitk's "Generating catalog" echo.

Sixt points out that the gitk portion of the patch still doesn't achieve full silence during `make -s` because it lacks the conditional suppression logic (the "if -s is in the flags" bracket) that core Git uses. He explicitly approves combining both changes—removing `--statistics` *and* adding the conditional silence—in a single patch, simplifying the approach. He also requests a commit message style adjustment (present tense for current state) and reminds the author that gitk, git-gui, and core Git are separate repositories, so changes to each should be split into distinct commits.

The review is substantive but narrowly focused on build mechanics and repository boundaries. No technical objections are raised; the feedback is about implementation completeness and project conventions. The thread is now poised for a v3 that incorporates these suggestions, with the maintainer's guidance effectively resolving the open question about conditional vs. unconditional suppression. Both gitk and git-gui maintainers will need to sign off on the final version, but the path forward is clear.