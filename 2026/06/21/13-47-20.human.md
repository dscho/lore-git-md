This is a brief procedural reply in the ongoing build-system cleanup thread for `gitk` and `git-gui`. Johannes Sixt (the `gitk` maintainer) clarifies how Harald Nordgren should submit the final patch for `gitk` to complete the series. Sixt instructs Harald to keep the patch in the same thread rather than starting a new one, noting that GitGitGadget (the tool Harald is using to send patches) will handle it as part of the existing series. The reply is purely logistical and contains no technical discussion or review feedback.

The thread is now effectively complete, with only this minor procedural detail remaining before the final patch is submitted. Once that patch arrives, Johannes Sixt will likely perform a final review, but no further technical work or coordination is expected. The series has already achieved its goal of aligning `gitk` and `git-gui` with core Git’s quiet build conventions for translation catalog generation.