This is the first patch in Harald Nordgren’s **v4 `git history squash` series** (4/4), which is now code-complete and ready for final assessment. This patch is a **refactoring** that extracts a helper function, `first_parent_tree_oid()`, to centralize the logic for resolving the tree of a commit’s first parent (or the empty tree for root commits). The helper replaces three nearly identical code blocks in `builtin/history.c`, reducing duplication and improving maintainability.

The change is purely mechanical: no behavior is altered, and the new helper is immediately used in the three places where the logic was previously repeated. The helper takes a `struct repository *`, a `struct commit *`, and an output `struct object_id *`, returning 0 on success or -1 on error. The existing callers are updated to use the helper, simplifying their error handling.

This patch is a preparatory step for the core squash logic introduced in later patches, where the first-parent tree resolution is a key operation. The refactoring is clean and uncontroversial, with no substantive discussion or objections in the thread. It is a typical example of the kind of small, focused cleanup that precedes larger feature work in Git. The patch is well-tested indirectly through the existing functionality it refactors, and the expanded test coverage in the series (340 lines in `t/t3455-history-squash.sh`) will further validate its correctness.

The files touched are limited to `builtin/history.c`, with 26 insertions and 32 deletions. The helper is straightforward and follows Git’s coding conventions. This patch is unlikely to attract further review attention, as it is a clear improvement with no downsides. The real substance of the series lies in the later patches, which implement the squash logic and ref-handling behavior.