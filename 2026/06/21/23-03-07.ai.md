This is the third patch in Taylor Blau’s 4-patch **v3** series integrating `--delta-islands` with `--path-walk` in `git pack-objects`. It is a **refactoring** patch that extracts the tree-depth bookkeeping logic from `show_object()` into a new helper function, `record_tree_depth()`.

The patch prepares for the next commit, which will reuse this logic in a second call site during path-walk traversal. The helper function itself is straightforward: it computes the depth of a tree object from the number of slashes in its name, looks up the object in the packing list, and updates the recorded depth if the new path yields a deeper value. The behavior is unchanged from the existing code in `show_object()`.

The diff touches only `builtin/pack-objects.c`, moving 14 lines of logic into a new 16-line helper. No new functionality is introduced, and the commit message clearly states the preparatory nature of the change. This is a clean, mechanical refactor that sets the stage for the subsequent patch without altering any behavior.