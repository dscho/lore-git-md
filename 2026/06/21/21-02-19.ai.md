This is the maintainer's feedback on the first patch of the v7 series that introduces `append_formatted_path()` and refactors `rev-parse` to use it. Junio C Hamano reviews the mechanical conversion of the existing `print_path()` logic into the new helper, noting that the patch preserves exact backward compatibility and even optimizes away an unnecessary `xgetcwd()` call in some cases. The feedback is substantive but narrowly focused: Junio suggests a minor refactoring of the control flow in `print_path()` to handle the `FORMAT_DEFAULT` case first, then map the result to the new `path_format` enum in a single `switch` statement. He also observes that the helper's API could be slightly cleaner (e.g., whether it should "append" or "set" the result in the destination buffer), but acknowledges that the current design accommodates future callers who may want to append rather than overwrite. The review is constructive and does not identify any functional issues; the suggested changes are stylistic and optional. The patch remains ready for pickup, with the ball now in the author's court to decide whether to incorporate Junio's suggestions before graduation to `next`.