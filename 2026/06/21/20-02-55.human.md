This is a **brief follow-up review** in the ongoing `git cat-file --batch-command` remote-object-info series. Junio C Hamano endorses Chandra Pratap’s observation about the unnecessary `static` declarations in `parse_cmd_remote_object_info()`. The variables in question (`remote_object_info` and `object_info_oids`) are fully allocated, used, and freed within a single command cycle, so the `static` qualifier is misleading and could introduce thread-safety concerns. Junio’s reply is concise and aligns with Chandra’s earlier substantive review, reinforcing that this is a valid correctness issue rather than a stylistic nit. The patch series as a whole remains in its final v13 state with all other technical and design feedback resolved.