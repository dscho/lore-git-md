This is the third patch in Taylor Blau’s **v3** series integrating `--delta-islands` with `--path-walk` in `git pack-objects`. It’s a **refactoring** that extracts the tree-depth bookkeeping logic from `show_object()` into a new helper, `record_tree_depth()`.

The change is straightforward: the patch moves the delta-islands depth calculation (which counts slashes in the object’s name to determine its tree depth) into a standalone function. This prepares for a later patch where the same logic will be reused from a second call site. The helper preserves the existing behavior—only updating the depth if the new path yields a deeper value—so there’s no functional change here.

The patch touches only `builtin/pack-objects.c` and is purely mechanical. It’s a clean, well-contained refactor with no risk of introducing regressions. The commit message clearly explains the motivation (preparing for a future call site) and the logic being factored out. This is a typical preparatory step in a larger series, and it looks solid.