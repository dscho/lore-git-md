This is a technical clarification in the ongoing worktree diff performance investigation. Junio C Hamano corrects D. Ben Knoble's analysis of the conditional guard in `builtin/diff.c` that controls index refresh behavior.

The key point is that the guard `1 < rev.diffopt.skip_stat_unmatch` is not simply a boolean check on `diff_auto_refresh_index`. Instead, `skip_stat_unmatch` serves as a counter that tracks how many paths were stat-dirty but had no actual content changes. The counter is initialized to 1 when `diff_auto_refresh_index` is true, and incremented by `diffcore_skip_stat_unmatch()` for each ghost change. The refresh only occurs if the counter exceeds 1, meaning at least one ghost change was found.

Junio explains that the conditional is not dead code as Knoble suspected, but rather a nuanced mechanism that only triggers an index refresh when there are actual stat-dirty paths without content changes. He acknowledges that using a counter for this dual purpose is potentially confusing, but maintains it is functionally correct. The email provides the commit reference (aecbf914) and clarifies the control flow through `diffcore_std()` and `diffcore_flush()`.

This clarification advances the thread by ruling out the "dead code" hypothesis and refocusing attention on whether the counter-based logic is the right architectural choice for index refresh behavior. The files involved remain `builtin/diff.c` and the diffcore subsystem, with no new symbols introduced. The discussion continues to center on why `git diff` does not persist its internal index refreshes, and whether this counter-based guard is the root cause or merely a symptom of a deeper design issue.