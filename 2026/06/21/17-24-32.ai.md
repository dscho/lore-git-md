This is a substantive technical discussion in the ongoing worktree diff performance thread. Jeff King (Peff) engages directly with Junio’s clarification about the counter-based guard in `builtin/diff.c`, identifying the root issue: **racy-git entries** (files whose mtimes match the index timestamp) are not being counted as "dirty" by `diffcore_skip_stat_unmatch()`, which prevents the counter from incrementing and thus blocks the index refresh. This means `git diff` in secondary worktrees repeatedly performs expensive content checks instead of persisting the refreshed index.

Peff frames the problem as a choice between two architectural fixes: (1) modify `diffcore_skip_stat_unmatch()` to count racy-git entries as dirty, or (2) alter the index to mark these entries with a zeroed `st_size` field (per the Racy Git documentation), though he notes the latter could introduce spurious diffs in plumbing commands. He defers to Junio’s deeper historical knowledge of the racy-git machinery but signals skepticism about the counter’s dual-purpose design.

The email advances the thread by pinpointing the exact mechanism—racy-git entries slipping through the counter logic—and narrowing the solution space to two concrete code paths. Key symbols involved are `diffcore_skip_stat_unmatch()`, `refresh_index_quietly()`, and the `st_size` field in index entries. No new files are introduced, but the discussion now centers on whether the fix belongs in the diffcore subsystem or the index writeback logic. The tradeoff between correctness (avoiding spurious diffs) and performance (persisting index refreshes) remains unresolved.