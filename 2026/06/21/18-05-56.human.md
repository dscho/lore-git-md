This is a **substantive technical discussion** between Jeff King (Peff) and Kristofer Karlsson about the architectural implications of Pablo Sabater’s `git log --graph` visualization series (v5). Peff confirms Kristofer’s observation that the current prio-queue refactoring (`dd4bc01c0a`) does not directly conflict with `--graph` rendering (which uses `REV_WALK_TOPO` or `REV_WALK_LIMITED`), but he uncovers a deeper issue: `rewrite_parents()` is populating the `commit_queue` even in topo mode, leading to an ever-growing queue that is never drained. While this does not currently break functionality, it suggests a potential lurking bug in history simplification when combined with `--topo-order`.

Peff endorses Kristofer’s proposed abstraction layer in `revision.c` (two helper functions: `revision_has_more_commits()` and `revision_peek_next_commit()`) as a clean way to insulate `graph.c` from walker internals. However, he clarifies that the graph code needs **full iteration capability**, not just a single-element peek, because it must scan for commits that will actually be shown (not just the next one). This requirement makes the abstraction slightly more complex than Kristofer’s initial sketch, but the core idea—dispatching based on walk mode—remains sound.

The discussion also touches on whether multi-element peeking is truly necessary. Peff notes that while the graph code does examine characteristics of the peeked commit, it may not matter if the commit is the *next* one to be shown or just any arbitrary commit. This could simplify the implementation, though the exact requirements will depend on Pablo’s visualization logic.

This exchange is a critical step toward resolving the fragility in `graph_peek_next_visible()`. The proposed abstraction would not only fix the immediate issue but also future-proof the code against further refactoring (such as Peff’s planned removal of `revs->commits`). The next move likely falls to Pablo: adopt the abstraction, validate whether the three key flags (`is_next_visual_root`, `next_has_column`, etc.) can be set correctly without in-order traversal, and rework the series accordingly. The thread is now unblocked on the architectural front, pending implementation.