This is a brief procedural update in the ongoing effort to make `make -s` builds fully silent for gitk and git-gui by aligning their translation catalog generation with core Git’s quiet build conventions. Johannes Sixt (gitk’s maintainer) clarifies the next steps: the git-gui portion of the patch is ready, but the gitk part still needs work. He asks Harald Nordgren to send a new patch specifically for gitk under the topic “make `make -s` silent,” noting that removing `--statistics` from `msgfmt` is just one part of the broader goal.

The thread remains in a straightforward handoff phase. No technical objections or open questions are raised here—just a clear request for a resubmission focused on gitk. The git-gui maintainer (Johannes Schindelin) still needs to review that portion, but Sixt’s sign-off on the git-gui part suggests it’s likely to be uncontroversial. The patch’s scope is narrow (build output verbosity only) and follows an established pattern from core Git, so it should proceed smoothly once the gitk resubmission arrives.