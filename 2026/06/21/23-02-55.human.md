This is the cover letter for the fourth iteration of Taylor Blau’s 4-patch series that makes `git pack-objects --path-walk` work with reachability bitmaps and delta-islands. The series is now merge-ready: all review feedback has been addressed, the performance test (p5311) has been tightened to show that `--path-walk`’s compression benefit is preserved even when bitmaps are written, and a minor test-portability nit (grep → test_grep) has been fixed. The patches touch only the pack-objects core, the corresponding test scripts, and the new perf test; no on-disk format changes are involved. If you care about pack-objects performance—especially in repositories where `--path-walk` already helps—this is the final look before it lands.