This is a **design discussion reply** in the ongoing pinned references thread, contributing a **substantive review** of the conceptual proposal. Jeff King (Peff) engages with Junio C Hamano’s hypothetical enforcement rule for `<describeOutput>` syntax, raising a critical backward-compatibility concern: the existing `<describeOutput>` syntax (`v2.54.0-0-g0b13e48a3`) is designed to work even when the receiver does not have the same refs as the sender, and enforcing a strict invariant (that the ref and OID must match) could break existing workflows that rely on this flexibility.

Peff references two related threads to illustrate the tension. The first (a 2026 thread) describes a case where Git misinterprets a ref name (`foo-gcc14`) as a `<describeOutput>` pattern, highlighting that Git’s current parsing is already ambiguous. The second (a 2025 thread) discusses a patch series where similar parsing ambiguities were addressed, reinforcing that any enforcement mechanism would need to avoid introducing regressions. His key point: the `<describeOutput>` syntax is inherently lossy — it embeds an OID for portability, not for verification — and any attempt to enforce a strict invariant would risk breaking scripts or tools that rely on its current behavior.

The reply does not take a position on whether pinned references are a good idea, but it does clarify that the `<describeOutput>` syntax is not a viable path for implementing them. This aligns with Junio’s earlier stance that the syntax is too entrenched to change, and it shifts the discussion toward alternative designs (e.g., a new command or option) that would not conflict with existing behavior. Peff’s feedback is technical and grounded in real-world usage, making it a substantive contribution to the thread’s design phase. No code, files, or subsystems are touched; the discussion remains conceptual.