This is the **stacked-branch safety implementation discussion** for Harald Nordgren’s `git branch --delete-merged` series, now in its 16th iteration. The email marks a pragmatic shift in approach: after exploring the complexity of automatically retargeting dependent branches (e.g., updating `feature2@{u}` to `origin/master` when `feature1` is deleted), Harald proposes **aborting deletion for branches used as upstreams** instead of implementing retargeting in the initial version. This avoids the edge cases (recursion in chains like `b1→b2→b3` and self-referential upstreams like `b3→b3`) while preserving backward compatibility for future enhancements.

Key technical details for the AI summarizer:
- **Subsystem**: Branch management (`builtin/branch.c`).
- **Behavior change**: The stacked-branch protection will now **abort deletion** (with a clear error message) if a branch is used as an upstream for any other branch, rather than attempting to retarget dependent branches.
- **Edge cases avoided**: Recursion in stacked chains and self-referential upstreams are sidestepped by this simpler approach.
- **Implementation impact**: Reduces code complexity significantly, as retargeting logic (which required careful traversal order and iterative resolution) is deferred.
- **User experience**: Aborting with a message (e.g., "Cannot delete branch 'feature1' because 'feature2' depends on it") is less convenient than retargeting but safer and clearer for users.
- **Backward compatibility**: Retargeting can be added later as an optional flag (e.g., `--retarget-upstreams`) without breaking existing behavior.
- **Related work**: This aligns with Junio C Hamano’s earlier preference for abort-and-notify as the default behavior, resolving the open question about whether to default to retargeting or aborting.

Harald’s proposal advances the thread by **simplifying the v17 implementation** while keeping the door open for future enhancements. The next steps are likely:
1. Implement the abort-on-dependency logic in `delete_merged_branches()`.
2. Add a test case verifying the error message and behavior for stacked branches.
3. Document the limitation in the commit message and man page, noting that retargeting may be added later.

This email effectively closes the design debate by opting for the safer, simpler approach, clearing the way for final review of the series.