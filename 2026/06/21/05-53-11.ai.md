This is the fourth iteration of Harald Nordgren’s `git history squash` feature, now fully addressing the ref-handling design concerns that were the last open issue in v3. The series remains a four-patch set introducing a new `git history squash <range>` subcommand that folds a commit range into its oldest commit while preserving descendant history.

The key update in v4 is the implementation of the ref-handling logic: by default, the command now **detects and rejects** operations where any ref (branch, tag, or other) points to a commit inside the squashed range. The error message includes an `advice.historyUpdateRefs` hint suggesting `--update-refs=head` to explicitly retarget only the current branch, leaving other refs untouched. This matches the design Patrick Steinhardt and Junio C Hamano finalized in the thread, closing the last open design question. The advice is configurable via `advice.historyUpdateRefs` and is tested in the new test script.

The test coverage has expanded significantly (now 340 lines in `t/t3455-history-squash.sh`), validating the ref-handling behavior with branches and tags, the `--update-refs=head` mode, and the advice message. The tests also confirm that ranges with internal merges, merge commits at the tip or base, and single-base merges are all handled correctly. The `--dry-run` prediction is now verified to match the actual rewrite, including tree and commit count checks.

The implementation touches `builtin/history.c` (328 insertions, 38 deletions), adds the advice entry in `advice.c`/`advice.h`, and updates the man page in `Documentation/git-history.adoc`. The range-diff shows the ref-handling logic and test updates; the core squash logic (single three-way merge, descendant replay) is unchanged from v3.

This version directly addresses all three technical concerns Patrick raised about range resolution: input validation is now handled by the ref-checking logic, ancestry ambiguity is avoided by the single-base requirement, and merge commit handling is explicitly tested and documented. The series is now **code-complete** and ready for Junio’s final assessment, with no unresolved design questions or technical objections. The implementation appears solid, with thorough test coverage and clear documentation.