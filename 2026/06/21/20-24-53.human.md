This is a brief but substantive reply from Junio C Hamano in the ongoing worktree diff performance thread. Junio acknowledges Jeff King’s patch and its tradeoffs, but shifts focus to a simpler, more fundamental solution: leveraging nanosecond timestamp precision (`USE_NSEC`) to eliminate racy entries at the source.

Junio’s core idea is to delay writing the index file by a small, unnoticeable amount (e.g., 50ms) when nanosecond resolution is available. This would allow the filesystem to advance timestamps naturally, avoiding the second-granularity collisions that create racy entries in the first place. The proposal is motivated by the observation that racy entries are an artifact of coarse-grained timestamps, and that modern systems with `USE_NSEC` could sidestep the problem entirely without complex runtime logic or architectural changes.

The reply implicitly questions whether Peff’s patch—while technically sound—is over-engineering for a problem that might be better solved at the filesystem level. Junio’s suggestion aligns with earlier discussions about `USE_NSEC` as a build-time or runtime option, but frames it as a low-cost, high-impact fix that could make the entire racy-entry machinery obsolete. No code is proposed here, but the direction is clear: prioritize eliminating racy entries over optimizing how Git handles them.

This is a notable intervention from the maintainer, and it may steer the thread toward simpler solutions. If adopted, it could reduce the need for Peff’s patch or runtime configuration proposals, though it would still require build system updates (e.g., Meson) to enable `USE_NSEC` by default on supported platforms. The tradeoff—whether a 50ms delay is acceptable in all contexts—remains an open question.