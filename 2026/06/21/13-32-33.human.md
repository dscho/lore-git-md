This is a tiny procedural follow-up in the ongoing effort to make `make -s` builds fully silent in gitk and git-gui. Harald Nordgren asks Johannes Sixt whether the upcoming gitk patch should be part of the same series or sent as a separate one. The question is purely logistical—no new technical details or objections are raised.

The thread remains on track: the git-gui portion is effectively approved, and the gitk patch (removing `--statistics` and adding conditional suppression of "Generating catalog" messages) is now expected as a standalone submission under the topic "make `make -s` silent." The change is still a build-system tweak with no functional impact, targeting only output verbosity during silent builds.