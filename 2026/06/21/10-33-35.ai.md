This is a build system patch for git-gui, part of the ongoing cleanup to align translation catalog generation with core Git's precedent. Johannes Sixt (the gitk maintainer) follows up on Harald Nordgren's v2 patch by further simplifying the git-gui Makefile's quiet msgfmt rule.

The change removes the now-unnecessary shell variable capture and text processing that previously handled `--statistics` output from `msgfmt`. With the `--statistics` flag already dropped in the earlier patch, the rule is reduced to a minimal echo line, matching the style of other quiet rules in the Makefile. The diff shows a straightforward cleanup: replacing the two-line `QUIET_MSGFMT0`/`QUIET_MSGFMT1` logic with a single `QUIET_MSGFMT` echo, and removing the shell variable capture from the msgfmt invocation.

This is a mechanical follow-up that completes the thread's goal of eliminating unnecessary build output. The patch is small, uncontroversial, and consistent with the established pattern in core Git. It requires Johannes Schindelin's (git-gui maintainer) review or ack before integration, but no technical objections are expected. The series is now effectively ready for merging once both maintainers sign off.