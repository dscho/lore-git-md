This is a **substantive technical discussion** between Jeff King (Peff) and Kristofer Karlsson about the architectural implications of Pablo Sabater’s `git log --graph` visualization series (v5). Peff’s reply advances the thread by confirming the fragility of `graph_peek_next_visible()`’s direct access to `revs->commits` and exploring edge cases in the dual-queue interaction.

Peff’s instrumentation reveals that `--graph` mode (using `REV_WALK_TOPO`) populates the `commit_queue` via `rewrite_parents()` even though commits are never returned from it. While this doesn’t break current behavior, it raises a potential bug in history simplification scenarios and underscores the need for the abstraction layer Kristofer proposed. Peff agrees that insulating `graph.c` from walker internals is the right path but notes that the graph code requires *iteration* (not just peeking) to determine whether any commit will be shown, not just the next one. This suggests the abstraction may need to support full traversal rather than a single-element peek.

The discussion also clarifies that multi-element peeking (a key concern in Pablo’s workaround) might not be strictly necessary—checking queue size could suffice for the flags in question, though Peff remains uncertain about whether the *specific* commit peeked at matters for the logic. This leaves an open question for Pablo’s implementation: whether the abstraction should provide arbitrary iteration or just size checks.

**Key technical details for follow-up:**
- **Files/subsystems:** `graph.c`, `revision.c`, `revision.h` (abstraction layer)
- **New symbols:** `revision_has_more_commits()`, `revision_peek_next_commit()` (proposed)
- **Edge case:** `rewrite_parents()` populates `commit_queue` in topo mode even though commits are never returned from it
- **Forward-compatibility:** Abstraction would future-proof against Jeff King’s planned removal of `revs->commits`
- **Open question:** Does the graph code need arbitrary iteration (for commit characteristics) or just queue size checks?

**Next steps likely involve:**
1. Pablo adopting Kristofer’s abstraction layer (with iteration support)
2. Validating whether queue size checks suffice or if full traversal is needed
3. Reconciling with Jeff King’s `revs->commits` removal plans

The thread remains **architecturally blocked** until the abstraction is implemented, but Peff’s feedback removes ambiguity about the dual-queue interaction and reinforces the need for insulation.