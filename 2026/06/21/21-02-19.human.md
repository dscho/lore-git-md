This is the maintainer’s feedback on the first patch in the v7 series that introduces `append_formatted_path()` and converts `rev-parse` to use it. Junio C Hamano reviews the mechanical refactoring closely, noting that the patch preserves exact backward compatibility while consolidating duplicated path-formatting logic. His comments are minor and stylistic: he suggests a slightly cleaner way to map the old `format_type`/`default_type` enums to the new `path_format` enum, and observes that the helper’s API could be more efficient by directly writing to the destination buffer rather than appending (though he acknowledges the design choice is defensible for future callers). The review is substantive but approving; the patch is technically sound and ready for integration once the trivial enum-mapping suggestion is addressed. This is the final hurdle before the series graduates to `next`.