This is the final review feedback on the `git cat-file --batch-command` remote-object-info series (v13/12), now ready for merging. Chandra Pratap provides a **substantive review** addressing the last open design questions and mechanical concerns.

The email focuses on two design points raised in the cover letter. First, Chandra rejects the "???" placeholder proposal for unsupported format atoms, arguing that it would force scripts to add brittle string-matching logic and could collide with legitimate data. Instead, he suggests failing cleanly when an unsupported atom is requested — a position that aligns with Git’s existing fail-fast philosophy in protocol v2 and avoids ambiguity in output parsing. Second, he dismisses the idea of supporting short OIDs in the remote-object-info command, noting that the server cannot verify whether the returned data corresponds to the intended object, and that Git’s network protocol (including `fetch`) already requires full OIDs for safety and consistency.

The feedback is concise, technically grounded, and directly responsive to the open questions flagged in the series. It does not introduce new issues or request code changes, but rather clarifies the intended behavior for future-proofing the feature. With this input, the series now has full consensus on all design and implementation points, removing the last barrier to integration.