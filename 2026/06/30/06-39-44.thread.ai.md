Thread identity
Subject: [PATCH 0/2] fix memory leaks in format-patch and test harness
Series version: v1 (initial submission)
Originating author: Jeff King (Peff)
Thread type: bugfix patch series

Core problem or goal
Fix two memory leaks: one in `git format-patch` (`prepare_bases()` in `builtin/log.c`) and one in the test harness (LSan error output misdirected to stdout instead of stderr, causing TAP parse errors with `prove`). The leaks are small, self-contained, and discovered opportunistically rather than as part of a systematic audit. The `format-patch` leak has existed since the `--base` option was introduced in 2016 (commit `fa2ab86d18`) but only became detectable after other leaks were fixed, making it stand out in LeakSanitizer (LSan) reports. The broader context is an ongoing effort to improve Git's memory hygiene, including CI infrastructure for leak detection and fixes for related leaks in bloom filters, OpenSSL SHA-1/SHA-256 contexts, and other subsystems.

Current status
Series is at v1; no prior iterations. Both patches are merged in principle: the test harness fix (patch 1/2) and the `format-patch` fix (patch 2/2) are mechanical, low-risk, and touch only two files (`builtin/log.c` and `t/test-lib.sh`). Patrick Steinhardt’s review confirmed the `format-patch` fix is correct, and no further changes are expected for the series itself. The discussion has expanded to CI infrastructure improvements for leak detection, specifically consolidating the `linux-reftables-leaks` and `linux-TEST-vars` jobs into a single job (`linux-reftables-leaks-TEST-vars`). The non-leak `linux-reftable` job is now recognized as redundant and will be dropped. These CI changes are procedural and not blockers for merging the original patches.

Key technical details
- Files: `builtin/log.c` (one line: `release_revisions(&revs)` call in `prepare_bases()`), `t/test-lib.sh` (three lines: redirect LSan output to stderr via descriptor 4).
- Functions: `prepare_bases()`, `release_revisions()`, `check_test_results_san_file_()`, proposed `git_hash_discard()`.
- Symbols: `rev_info` struct (leaked, now freed via `release_revisions()`), `topo_walk_info` (allocated when commit-graph is present), OpenSSL SHA-1/SHA-256 context leaks (in `csum-file.c` during aborted hash operations).
- Trigger conditions: commit-graph support (`GIT_TEST_COMMIT_GRAPH=1`) exercises the revision machinery’s topological walk, exposing the `format-patch` leak; OpenSSL SHA-1 leaks surface when Git is built with `OPENSSL_SHA1_UNSAFE` and hash operations are aborted.
- Related commits: `fa2ab86d18` (introduced `--base` option in 2016), `389c83025d` (recent `prove` TAP parsing change affecting test harness).
- CI proposal: merge `linux-reftables-leaks` and `linux-TEST-vars` into a single job (`linux-reftables-leaks-TEST-vars`), drop the redundant `linux-reftable` job. Overhead benchmarked at ~3x slowdown, deemed acceptable for targeted use.
- No new CLI options, config keys, or on-disk format changes.
- Test coverage: existing test suite (`t4014-format-patch.sh`) exercises the fixed paths; no new tests added. The test harness change preserves verbose-logging behavior while fixing TAP parse errors.

Open questions and loose ends
- **CI infrastructure**: Jeff King’s proposal to consolidate `linux-reftables-leaks` and `linux-TEST-vars` into `linux-reftables-leaks-TEST-vars` is now aligned with Patrick Steinhardt’s suggestion to drop the redundant `linux-reftable` job. No objections remain, but explicit approval or implementation is pending.
- **OpenSSL SHA-1/SHA-256 leaks**: Peff has draft patches for exposing a `git_hash_discard()` primitive in each hash implementation’s API to free OpenSSL contexts. The patches need polishing, and SHA-256 support is untested under LSan. This is follow-up work, not part of the current series.
- **Bloom filter leaks**: Peff reports patches for these have already been sent (affecting `t0095`, `t4013`, `t4211`), but their status is not detailed in this thread.
- **Unrelated LSan failures**: Patrick Steinhardt noted leaks in `t3451`, `t3452`, `t3453` (attributed to `git-history` and missing `repo_unuse_commit_buffer()` calls), but these are outside the scope of this series.

Participant positions
- Jeff King (author): submitted the series as a targeted cleanup. The test harness fix (patch 1/2) is motivated by a real-world annoyance (TAP parse errors) and aligns with the test suite's existing error-reporting patterns. The `format-patch` fix (patch 2/2) includes a thorough commit message tracing the leak’s origin, explaining its delayed detection, and providing a clear reproduction case. Peff also drives the broader leak-fixing effort, proposing CI improvements and drafting patches for OpenSSL SHA-1/SHA-256 leaks. His position is that the original series is complete and correct, while related work (CI, OpenSSL leaks) is follow-up.
- Patrick Steinhardt (reviewer): **substantive review** confirming the `format-patch` fix is correct (no early returns to adjust, `revs` struct always initialized). Tests the patch and reports unrelated LSan failures in other test suites (`t0095`, `t3451`, `t3452`, `t3453`, `t4013`, `t4211`), attributing them to missing `repo_unuse_commit_buffer()` calls in `git-history` and bloom filter leaks. Proposes enabling LSan for the `linux-TEST-vars` CI job to improve leak detection, benchmarking the overhead as ~3x slowdown. Agrees with Peff’s CI consolidation proposal and clarifies that the non-leak `linux-reftable` job is redundant and should be dropped.

Related work
- **Leak fixes**: Bloom filter leaks (patches sent by Peff), OpenSSL SHA-1/SHA-256 leaks (draft patches by Peff), `git-history` leaks (unrelated to this series).
- **CI infrastructure**: Proposal to merge `linux-reftables-leaks` and `linux-TEST-vars` into a single job (`linux-reftables-leaks-TEST-vars`) and drop the redundant `linux-reftable` job. Tied to the project’s broader goals of improving leak detection.
- **Test harness**: The fix in patch 1/2 is tied to a recent `prove` TAP parsing change (commit `389c83025d`).
- **ODB abstraction**: Patrick’s unrelated LSan failures involve `git-history`, which is part of the ODB abstraction effort, but this is not directly relevant to the current series.