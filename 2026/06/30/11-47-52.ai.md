This is the **final patch in Patrick Steinhardt's 13-patch refactoring series** to separate repository discovery from setup in Git's core code. The patch marks `set_git_work_tree()` as `static` in `setup.c` and removes its declaration from `setup.h`, as all external callers were eliminated in the previous patch (12/13).

The change is a mechanical cleanup that reinforces the series' architectural goal: consolidating worktree configuration logic within `setup.c`. By making `set_git_work_tree()` file-local, the patch ensures the function is only used internally, preventing future misuse and aligning with the broader effort to reduce scattered setup logic. The commit message is concise and directly ties the change to the preceding patch, making the rationale clear.

This patch is uncontroversial and unlikely to generate discussion—it’s a straightforward follow-up to the worktree parameter consolidation in patch 12/13. The series as a whole remains focused on foundational refactoring, with no behavior changes, and this final step tidies up the API surface. Reviewers will likely focus on whether the function’s removal from `setup.h` breaks any hidden dependencies (e.g., in downstream forks or scripts), but the patch itself is well-motivated and minimal.

**Key technical details for follow-up**:
- Symbol removed from public API: `set_git_work_tree()` (now `static` in `setup.c`).
- Files touched: `setup.c` (function marked `static`), `setup.h` (declaration removed).
- No new symbols or behavior changes.
- Depends on patch 12/13, which consolidated worktree setup into `init_db()`.