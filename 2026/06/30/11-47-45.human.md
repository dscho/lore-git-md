This is the sixth patch in Patrick Steinhardt’s 13-patch **refactoring** series to separate repository discovery from setup in Git’s core code. The patch embeds the `struct repository_format` directly into the new `struct repo_discovery`, eliminating the need to pass both structures separately to discovery functions. This is a mechanical but important step toward consolidating repository configuration logic into `repo_init()`.

The change is straightforward: `repo_discovery` now owns its format data, and all discovery functions (`repo_discover_explicit_gitdir`, `repo_discover_implicit_gitdir`, `repo_discover_bare_gitdir`) stop taking a separate `struct repository_format` parameter. The patch updates call sites in `setup_git_directory_gently()` to use the embedded format instead of a local variable, and ensures proper cleanup via `repo_discovery_release()`. No behavior changes are introduced, but the refactor makes the discovery phase more self-contained.

The patch touches only `setup.c` and is well-scoped, with clear commit message and code changes. It builds directly on the earlier patches in the series (e.g., the introduction of `struct repo_discovery` in patch 5/13) and aligns with Patrick’s broader goals of cleaning up repository setup. The next patches will likely extend `repo_discovery` further (e.g., adding prefix/worktree fields) and adjust callers to use the new API.

This is a solid, incremental refactor—no red flags, but worth watching for follow-up discussion about the `repo_discovery` API design (e.g., whether embedding the format struct is the right long-term choice). Reviewers familiar with repository setup (e.g., Jeff King, Taylor Blau) may weigh in on edge cases or future implications.