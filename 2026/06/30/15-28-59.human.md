This is the ninth patch in Phillip Wood’s 11-patch series fixing how `git rebase` handles dropped commits. It is a **refactoring** that simplifies control flow in `pick_one_commit()` by returning early when a commit is successfully recorded as rewritten.

The patch moves the `return 0` statement into the `else if (!res)` block, making it clear that once the commit is recorded, the function’s work is done. The trailing `return res` is replaced with a `BUG()` macro, which will trigger if `do_pick_commit()` ever returns a value that isn’t explicitly handled. This change is purely mechanical and does not alter behavior; it prepares the code for the introduction of the `PICK_DROPPED` enum member in the next patch.

The diff is minimal (three lines added, one removed) and the commit message is concise. This is a typical preparatory refactoring in a well-structured series: it tidies the code so the subsequent behavioral change is easier to reason about. No controversy or open questions here; the patch looks clean and uncontroversial.