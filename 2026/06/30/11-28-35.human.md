This is a brief follow-up in Patrick Steinhardt’s ODB refactoring series, specifically addressing a naming nit in the second patch. Justin Tobler noted that the field name `sourcep` in `struct object_info` could be misleading, as it suggests a pointer to `struct odb_source` (which appears later in the series) rather than the new `struct object_info_source`. Patrick acknowledges the ambiguity and proposes an alternative: renaming the struct to `odb_source_info` and the field to `source_infop` to clarify its purpose. The exchange is purely about readability and has no technical impact on the patch’s functionality. The tone is collaborative, and the suggestion is framed as optional rather than a blocker. For readers tracking the ODB abstraction effort, this is a minor cosmetic tweak that won’t affect the series’ overall trajectory.