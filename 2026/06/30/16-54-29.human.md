This is a review reply in the ongoing ODB refactoring series from Patrick Steinhardt, specifically addressing patch 3/6. Junio C Hamano responds to Patrick’s earlier defense of the opt-in `source` parameter design, now focusing on a low-level implementation detail: how the `source` field is assigned in `packed_object_info_with_index_pos()`.

Junio’s email clarifies his earlier confusion about the pointer assignment (`oi->sourcep->source = &source->base`). He acknowledges that the current implementation—using the address of the `base` member to avoid an explicit cast—is technically correct (since a pointer to a struct always points to its first member) but finds it visually jarring ("Yuck"). He accepts the approach as safe and explicit enough, though he notes it might confuse readers unfamiliar with this corner of the codebase. The email does not revisit the larger architectural debate (centralized vs. opt-in source tracking) or press for changes, effectively closing the discussion on this specific point.

The series remains conceptually approved, with no new objections raised. The unresolved architectural question from earlier in the thread (whether to track sources at the `packed_git` level) is not addressed here, but this exchange removes one minor technical hurdle. The patch is now one step closer to merging, pending any further feedback on the broader design. For readers tracking the ODB abstraction effort, this email confirms the implementation detail is settled, but the higher-level design tension lingers.