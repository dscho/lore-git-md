This is a **substantive technical follow-up** in the ongoing discussion about macOS CI hangs in `t5551` and `t5559`. The email focuses on a performance quirk in Git’s reftable backend, where bulk ref creation triggers excessive `fstat` calls on the `tables.list` file—accounting for up to 85% of runtime in synthetic benchmarks.

Patrick Steinhardt acknowledges the issue, tracing it to `refs_resolve_ref_unsafe()`, a generic function that reloads the reftable stack unnecessarily during ref verification. While he agrees with Jeff King’s assessment that the current performance (50,000 refs in 150ms) is "good enough" compared to the files backend, he sees this as a **low-hanging optimization opportunity**—one that could yield significant speedups with relatively little effort. He plans to file an issue to track the work but doesn’t expect to tackle it immediately.

The exchange is **exploratory rather than actionable**: it surfaces a potential optimization for future reftable work but doesn’t propose a patch or reopen the broader CI hang discussion. The tone is pragmatic, balancing the trade-off between immediate needs (CI stability) and long-term improvements (reftable performance). For readers following the thread, this is a useful data point about reftable’s internals, but it doesn’t change the status of the already-merged fixes.