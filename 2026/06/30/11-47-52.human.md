This is the final patch in Patrick Steinhardt’s 13-patch refactoring series that separates repository discovery from setup in Git’s core code. The patch itself is a small cleanup: it marks `set_git_work_tree()` as `static` in `setup.c` and removes its declaration from `setup.h`, since the function is now only used internally after the previous patch consolidated worktree configuration into `init_db()`.

The change is mechanical and uncontroversial, reflecting the series’ broader goal of reducing global state and tightening function boundaries. By making `set_git_work_tree()` file-local, Patrick ensures that worktree configuration is now handled in one place (`init_db()`), rather than being scattered across multiple call sites. This aligns with the series’ architectural direction and prepares the codebase for future refactoring efforts like the `the_repository` removal and ODB abstraction.

The patch is well-motivated and clearly ties back to the series’ earlier steps, particularly the worktree parameter consolidation in patch 12/13. There’s no behavior change or risk of regression here—just a tidy conclusion to the refactor. Reviewers are likely to approve it quickly as part of the larger series.