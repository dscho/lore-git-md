This is a **user experience discussion** about recoverability and reflog design in the context of the `git history squash` subcommand. Matt Hunter follows up on Harald Nordgren’s broader reflog critique with a narrower, actionable question: *how do you comprehensively undo a `git history` operation that moved multiple branch refs?*

Matt confirms his understanding that `git reset --hard` alone is insufficient if `--update-refs` retargeted other branches, leaving them pointed at unwanted commits. He notes that Git’s reflog doesn’t clearly show which refs were affected, making a full reset non-obvious. This is a **practical usability gap**—not a flaw in the `squash` implementation itself, but a missing affordance in Git’s recovery tooling. His observation about `git status` relying on checkout reflog entries to track detached HEAD origins is a tangential but relevant detail, illustrating how deeply Git’s UX depends on reflog metadata.

The email doesn’t propose a technical solution (e.g., a new `--undo` flag or reflog query tool), but it **sharpens the problem statement**: users need a way to audit and revert all ref changes made by a single `git history` invocation. This could inspire future work, such as a `--dry-run --verbose` mode that lists affected refs or a `git reflog undo` command that resets all branches touched by the last operation. For now, it’s a **clear call for better recoverability documentation** in the `git history` man pages.

Matt’s tone is collaborative, and his feedback is **surface-level but valuable**—it highlights a real pain point without demanding immediate changes. The thread remains focused on usability, not implementation, and this email keeps the discussion grounded in concrete workflows rather than abstract design.