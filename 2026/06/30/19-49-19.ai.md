This is a usability discussion about `git blame -b` output formatting, specifically the interaction between commit hash abbreviation and the `-b` (blank boundary) option.

Junio C Hamano replies to Laszlo Ersek’s report about `git blame -b` reserving an extra nibble for a caret marker that is never displayed. Junio questions whether the real issue lies in `git rebase`’s parser, which he suggests should accept longer abbreviations (up to 16 hex digits) as long as they uniquely identify the commit. He notes that Git already extends abbreviations beyond `core.abbrev` when necessary to avoid ambiguity, so a parser that strictly expects the configured length may itself be buggy. While he agrees that reserving an unused column is aesthetically suboptimal, he implies the practical impact is minor unless a downstream tool (like `git rebase`) fails to handle variable-length hashes.

The email shifts the focus from `git blame`’s output formatting to the robustness of `git rebase`’s input parsing, framing the latter as the more pressing concern. No code changes are proposed yet, but the discussion now centers on whether `git rebase` should be more lenient with abbreviated commit hashes. The thread remains open-ended, with no clear consensus on whether to adjust `git blame`, `git rebase`, or both. Key technical details include the `core.abbrev` config key, the `git rebase` TODO list parser, and the file `builtin/rebase.c` (likely where the parser lives).