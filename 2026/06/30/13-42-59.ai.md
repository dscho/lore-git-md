This is a **design clarification reply** in the ongoing `git replay --linearize` series (v5). The author, Toon Claes, acknowledges Johannes Schindelin’s argument that the `replayed_base` parameter must be restored to prevent commit-dropping when replaying multiple branches with `--linearize`. He agrees that `--linearize` should always flatten all replayed commits into a single linear topology, even when multiple branches are involved, and accepts that this behavior must be clearly documented.

The reply reinforces the **interface design choice** to keep `--linearize` as a standalone flag rather than adopting `git rebase`'s `--rebase-merges=<mode>` syntax. Toon argues that the term `--linearize` better describes the predictable, all-or-nothing flattening behavior, and that users can achieve branch-separate replay by invoking `git replay` multiple times (though this risks commit duplication). The message does not propose a code fix for the regression but signals alignment with Schindelin’s technical direction, leaving the implementation work (restoring `replayed_base`) as the next step. The tone is pragmatic, prioritizing predictability over edge-case flexibility.