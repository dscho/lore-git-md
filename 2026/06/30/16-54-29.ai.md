This is a **substantive architectural review reply** in the ongoing ODB refactoring series, specifically addressing patch 3/6 ("odb: add `source` field to struct object_info_source"). Junio C Hamano responds to Patrick Steinhardt’s defense of the opt-in `source` parameter design, closing the loop on the earlier design question while raising a **low-level implementation concern** about pointer casting.

Junio accepts Patrick’s explanation that the `source` parameter is not "almost always NULL" but rather that most callers use the higher-level `odb_read_object_info()` API (which already has source context) and thus don’t appear in the diff. He also acknowledges the naming ambiguity of `sourcep` but does not press for a rename, leaving that as a non-blocking follow-up. The architectural tension—whether source tracking should be opt-in or centralized at `packed_git` initialization—remains unresolved but is now framed as a settled design choice for this iteration.

The new concern is **implementation-level**: Junio finds the pointer assignment `oi->sourcep->source = &source->base` "yuck" because it hides an implicit upcast. He explains that while the pattern is technically safe (a pointer to a struct always points to its first member), it obscures the type conversion for readers unfamiliar with the codebase. He ultimately accepts the current implementation but signals that the pattern might confuse future maintainers. This is a **surface-level but technically substantive** critique—it doesn’t block the patch but flags a readability issue that could warrant follow-up.

The thread is now conceptually approved ("everything makes sense") and the series remains on track for `master`. Key technical details for follow-ups:
- The `source` parameter’s role in `packed_object_info()` and its distinction from `odb_read_object_info()`.
- The pointer assignment pattern (`&source->base`) and its implicit upcast.
- The unresolved naming ambiguity of `sourcep` (no proposed rename yet).
Files touched: `odb.h`, `odb.c`, `packfile.c`, `odb/source-*.c`. No behavior changes or on-disk format modifications.