This is a **substantive design discussion** in the `git replay --linearize` thread, continuing the **CLI consistency vs. UX clarity debate** between Patrick Steinhardt and Johannes Schindelin. Patrick acknowledges Johannes’s critique of `git rebase`’s usability and accepts the argument for **intentional divergence**, but insists that any deviation must be **explicitly justified in the commit message**.

Key developments:
- Patrick **concedes the philosophical point**: he is open to diverging from `git rebase`’s syntax if the rationale is documented, but wants the reasoning to be **transparent and upfront** in the patch history. This shifts the focus from "should we diverge?" to "how do we justify divergence?"
- The discussion now centers on **commit message expectations**: Patrick’s request for a clear explanation in the commit message (rather than just in email) ensures that future contributors understand the design trade-offs. This is a **process-level refinement** that could influence how similar debates are resolved in the future.
- No new technical proposals are introduced; the exchange remains a **meta-discussion** about how to handle CLI design disagreements.

Thread context:
- The **v5 series is already merged**, but this debate will shape **follow-up patches** addressing the post-merge regression (silent commit dropping) and CLI expansion (e.g., `--replay-merges=<mode>`).
- Johannes’s earlier position—that `--linearize` should remain distinct from merge-handling modes—remains unchallenged here. Patrick’s reply does not re-litigate the technical design, only the **documentation of the decision**.
- The **broader implication** is that Git may adopt a hybrid approach: diverge from `git rebase` for UX reasons, but require explicit justification in commit messages to avoid arbitrary inconsistency.

Key symbols referenced:
- CLI options: `--linearize`, `--replay-merges=<mode>`, `--no-merges`, `--replay-cousins` (Johannes’s proposal); `--rebase-merges`, `--no-rebase-merges` (Patrick’s original suggestion).
- Commands: `git rebase`, `git replay`.
- **Process artifact**: commit message documentation as a tool for resolving design debates.

This reply is **substantive but narrow**: it does not advance the technical discussion, but it **raises the bar for future CLI changes** by demanding explicit justification. The thread remains in a **philosophical holding pattern** until a follow-up patch (or Junio’s intervention) resolves the regression and CLI design.