This is a usability-focused discussion about `git blame -b` output formatting. Junio C Hamano proposes a straightforward fix to the issue Laszlo Ersek reported: when `git blame -b` is used, the extra hex digit currently reserved for an unused caret marker should be removed. This would align the abbreviated commit hash length with the user’s `core.abbrev` setting, eliminating the manual truncation step Laszlo described when pasting hashes from `git blame -b` output into an interactive rebase TODO list.

The proposal is minimal and targeted—no new configuration variable (like `blame.abbrev`) is needed, and the change affects only the `-b` mode of `git blame`. The patch would touch `blame.c` to adjust the output formatting logic, specifically removing the extra nibble reservation for the caret marker when boundary commits are shown as blank. No on-disk formats or other subsystems are involved, and the impact is limited to manual workflows like Laszlo’s.

Junio’s tone suggests this is a low-risk, uncontroversial adjustment. The thread has already clarified that `git rebase`’s parser is not at fault, so the fix can focus solely on `git blame`’s output. No backward-compatibility concerns for scripts or tools parsing blame output have been raised yet, but if they arise, they would likely center on whether any downstream tools rely on the current fixed-width formatting of `git blame -b` output.

The discussion is now converging on a specific solution, but no patch has been posted. If one follows, it will likely be a small, self-contained change to `blame.c` with a clear commit message explaining the usability improvement. The thread remains open for further feedback, but the path forward appears straightforward.