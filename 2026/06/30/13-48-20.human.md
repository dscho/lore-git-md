This is the final usability polish discussion for the `git history squash` feature, now in its sixth iteration. Phillip Wood provides a concrete example of his proposed `--reedit-message` template format, addressing Harald Nordgren’s request for clarity on how to reduce visual clutter while preserving context.

The example shows a cleaner layout: a summary list of commit subjects (grouping `fixup!`/`amend!` commits with their targets), followed by the full messages of non-`fixup!`/`amend!` commits, separated by minimal visual markers (`# ------------------------------------`). Phillip suggests omitting numbered markers entirely, arguing they add noise without sufficient benefit, and proposes commenting out `squash!` subjects to delineate original vs. appended content. He remains uncertain whether to explicitly label `amend!` commits in the template.

This exchange resolves the last open question before Junio’s final review. The proposed template is a clear improvement over the current version, reducing clutter while maintaining readability. No technical changes are needed—just a minor adjustment to `build_squash_message()`—and the series remains on track for integration. The discussion reflects the project’s attention to detail in user-facing features, even at the tail end of a long review cycle.