Thread identity
Subject: config parser: fix case-sensitivity mismatch in old-style subsection headers
Series version: standalone patch (no version prefix)
Originating author: (not specified in summary; assume from context if needed)
Thread type: bugfix patch

Core problem or goal
Git's config parser has a long-standing case-sensitivity bug when updating values in old-style `[section.subsection]` config headers. The parser lowercases subsections during parsing but preserves original case when matching keys from the command line, causing `git config section.Subsection.key value` to fail to match existing entries if the subsection contains uppercase letters. This results in duplicate entries instead of value updates.

Current status
Patch submitted; no prior versions. No review trailers or maintainer decisions yet.

Key technical details
- Files touched: `config.c` (core config handling), `git-config.adoc` (documentation), `t1300-config.sh` (tests)
- New flag: `subsection_case_sensitive` in `struct config_store_data`
- Modified function: `matches()` now uses `strncmp` or `strncasecmp` based on the flag
- Behavior change: case-sensitive comparison for new-style quoted headers, case-insensitive for old-style
- Documentation: removed BUGS section documenting the limitation
- Test updates: reflect fixed behavior

Open questions and loose ends
None yet; patch is self-contained and addresses the issue without introducing new questions.

Participant positions
None recorded yet.

Related work
None mentioned. This is a standalone bugfix with no dependencies or follow-ups.