This is a **design discussion** in the `git replay --linearize` thread, where Johannes Schindelin pushes back against Patrick Steinhardt’s call for CLI consistency with `git rebase`. Johannes argues that `git rebase`’s interface is a **poor usability precedent**—citing its "hostile" history and confusing option names—and advocates for a cleaner, more intuitive design in `git replay`.

The core disagreement centers on whether `--linearize` should be replaced with `git rebase`-style options (`--rebase-merges`, `--no-rebase-merges`, etc.). Johannes rejects this, insisting that `--linearize` is conceptually distinct from merge-handling modes and should remain a standalone flag. He also criticizes Patrick’s proposed mapping of `--no-rebase-merges` to "flatten commits," calling it misleading (it would imply "abort on merges" to him). Instead, he suggests future options like `--replay-merges=<mode>` could coexist with `--linearize`, but with clearer, replay-specific terminology.

This exchange **elevates the CLI debate to a philosophical level**: Patrick frames consistency as a design principle, while Johannes prioritizes usability and warns against repeating `git rebase`’s mistakes. The outcome will likely require maintainer input, as it touches on project-wide interface conventions. For now, the merged `--linearize` implementation remains in place, but this discussion may shape follow-up patches addressing both the **post-merge regression** (silent commit dropping) and **CLI design**.