This is a **substantive technical follow-up** in the macOS CI hang thread, focusing on a performance quirk in Git’s reftable backend. Patrick Steinhardt responds to Jeff King’s observation about reftable’s repeated `fstat` calls during bulk ref creation, confirming the issue stems from `refs_resolve_ref_unsafe()`’s generic design, which makes it difficult to optimize for reftable’s stack-based internals.

Patrick agrees with Peff’s suggestion that the backend could trust its loaded stack after locking, avoiding redundant filesystem checks, but notes this would require deeper refactoring. While he acknowledges reftable’s current performance (50,000 refs in 150ms) is "good enough" compared to the files backend, he frames the `fstat` overhead as a **low-hanging optimization opportunity**, accounting for 85% of the runtime in Peff’s `strace` analysis. He plans to file an issue to track the work but doesn’t expect to address it soon.

**Key technical details for future reference**:
- **Subsystem**: reftable backend (`refs_resolve_ref_unsafe()`, `tables.list` handling).
- **Observed inefficiency**: 2,000 `fstat` calls during bulk ref creation (via `strace`/`perf`), consuming 85% of runtime.
- **Root cause**: Generic ref-resolution logic that doesn’t leverage reftable’s stack-based design.
- **Proposed fix**: Trust the loaded stack after locking to avoid redundant filesystem checks.
- **Performance context**: 50,000 refs in 150ms (reftable) vs. ~220ms (files backend); current speedup is ~30%.
- **Files touched**: None in this email, but future work would target `refs/reftable-backend.c`.

This exchange is **not a call for immediate action**—it’s a post-mortem on reftable’s behavior and a note for future optimization. The tone is pragmatic, balancing the cost of refactoring against the benefit of a potential speedup. The discussion remains tied to the broader thread’s context (CI hangs in `t5551`/`t5559`) but shifts focus to reftable’s internals rather than the original bugfix.