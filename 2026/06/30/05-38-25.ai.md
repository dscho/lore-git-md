This is a follow-up in the memory-leak fix thread for the `git history reword` command, focusing on test coverage and diagnostics. Jeff King (Peff) provides a concrete reproduction recipe using `SANITIZE=leak` and `GIT_TEST_COMMIT_GRAPH=1` to expose the leak, confirming the commit-graph as the primary reason commits bypass the slab cache. He suggests documenting this command sequence in the commit message rather than adding a narrowly targeted test, given the combinatorial complexity of feature flags and the risk of missing other leak scenarios. The tone is pragmatic, acknowledging the trade-off between specificity and maintainability in test design. No new code or objections are introduced; this is purely diagnostic guidance.