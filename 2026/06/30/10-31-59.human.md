This is a one-line fix to the fourth patch in Patrick Steinhardt’s five-part series adding reference-writing subcommands to `` `git refs` ``. The series is already merged into Junio’s `next` branch, and this email is the final reroll sent solely to correct a typo in the error message for `` `git refs create` ``.

The typo—"null old object ID" instead of "null new object ID"—was caught by Junio in his review of v2. Patrick acknowledges the correction and sends no other changes. The patch is otherwise unchanged from the approved v2 version, which introduced the `` `git refs create` `` subcommand with explicit atomic creation semantics (rejecting creation if the reference already exists). This fix is purely cosmetic and does not affect functionality, tests, or design. The series is now complete and ready for the next release cycle.