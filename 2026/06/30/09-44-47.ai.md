This is a **substantive design discussion** in the `git replay --linearize` thread, focusing on **command-line interface (CLI) consistency** and **user experience (UX) philosophy**. Johannes Schindelin responds to Patrick Steinhardt’s call for alignment with `git rebase`’s `--rebase-merges` syntax, **rejecting the precedent** as a usability anti-pattern and advocating for **intentional divergence** in favor of clearer, more intuitive option names.

Key points:
- **Usability critique of `git rebase`**: Johannes argues that `git rebase`’s CLI is historically burdened with "usability warts" (e.g., the term "rebase" itself, which he calls "hostile"). He **explicitly opposes mirroring its syntax**, warning that doing so would perpetuate poor UX.
- **Terminology preference**: He insists on using "replay" (not "rebase") in option names to reflect the command’s purpose more naturally (e.g., `--replay-merges` instead of `--rebase-merges`).
- **Conceptual distinction**: Johannes treats `--linearize` as a **separate axis** from merge-handling modes. He suggests `--linearize` should remain a standalone option, while merge-handling could use a `--replay-merges=<mode>` enum (e.g., `--replay-merges=abort`, `--replay-merges=replay-cousins`). This contrasts with Patrick’s proposal to unify all modes under `--rebase-merges`.
- **Design principle**: His stance prioritizes **intuitive UX over consistency**, framing `git rebase`’s interface as a cautionary tale rather than a template.

Technical context:
- The discussion centers on **future-proofing** the CLI for `git replay`, which currently supports `--linearize` (v5) but lacks options for aborting on merges or replaying cousins.
- Related work: Johannes’s earlier merge-replay series (default topology-preserving behavior) and Patrick’s `git history` command.
- **Key symbols**: `--linearize`, `--replay-merges=<mode>`, `--no-merges`, `--replay-cousins`, `git rebase --rebase-merges`.

Thread impact:
- **Elevates the debate** from a technical detail to a **philosophical split** between consistency (Patrick) and UX clarity (Johannes). This could delay resolution until Junio Hamano weighs in or a compromise emerges.
- **No immediate patch impact**: The v5 series is already merged, but this exchange will shape **follow-up patches** addressing the post-merge regression and CLI expansion.
- **Broader implications**: The outcome may influence whether Git adopts a unified CLI vocabulary for history-editing commands (`rebase`, `replay`, `history`) or allows divergence for UX reasons.

Johannes’s reply is **high-weight**: his critique of `git rebase`’s UX carries authority, and his preference for "replay" terminology aligns with the command’s existing naming. However, Patrick’s earlier argument for consistency remains unaddressed—this thread is now a **clash of design principles** (consistency vs. UX) rather than a technical dispute.