This is a brief follow-up in the ongoing memory-leak thread tied to Patrick Steinhardt’s `git history` command and broader slab management. Jeff King reports that the distinct leak in `t4014`—where the topological walk slab (`init_topo_walk`) was not being freed—has now been fixed in a separate patch. That fix is independent of the original `git history reword` leak and can be merged on its own. The message also notes a minor TAP-output formatting issue in `prove` that was addressed in the same thread, but this is purely test-infrastructure noise and not related to the core slab-management problem. The original `git history reword` fix remains correct and merged; the thread’s focus has now narrowed to confirming that all known leaks are resolved, with no new systemic issues introduced. Key technical details to track: the topological walk slab (`init_topo_walk`), the `t4014` test script, and the separate patch link for the slab fix.