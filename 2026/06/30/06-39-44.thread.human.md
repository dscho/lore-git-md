This thread began with Jeff King (Peff) submitting a two-patch series to fix memory leaks in `git format-patch` and the test harness. The `format-patch` leak, present since 2016, was exposed only after other leaks were addressed, while the test harness issue caused TAP parse errors with `prove`. Both fixes are small, self-contained, and ready for merging.

The discussion expanded to CI improvements for leak detection, where Peff, Patrick Steinhardt, and Junio Hamano agreed to consolidate the `linux-reftables-leaks` and `linux-TEST-vars` jobs into a single `linux-TEST-vars-leaks` job, drop the redundant `linux-reftable` job, and rename `linux-TEST-vars` to `linux-exotic-flags` for clarity. Junio endorsed a minimalist approach, ruling out macOS or compiler-specific expansions due to limited benefits. Peff later clarified his stance on compiler diversity, noting that while rare, compiler-specific behavior can occasionally reveal latent undefined behavior, though he agreed the current CI streamlining is sufficient.

The original patches remain uncontested and ready for integration. Follow-up work includes polishing Peff’s draft patches for OpenSSL SHA-1/SHA-256 leaks and addressing unrelated leaks in bloom filters and `git-history`. The thread is now fully converged, with no open technical or procedural questions.