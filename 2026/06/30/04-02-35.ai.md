This is a follow-up to an RFC proposing new `--only`/`--except` options for `git clone` to specify sparse-checkout paths during clone.

The author, Pushkar Singh, sends a brief ping to the mailing list after no responses to the initial RFC. The email is purely procedural: it restates the goal (allowing sparse-checkout paths to be specified during `git clone` to eliminate a separate `git sparse-checkout set` step) and asks whether the proposal seems worth pursuing. There is no new technical content, interface discussion, or implementation detail—just a request for feedback on the idea's viability.

The thread remains at the proposal stage with no community engagement yet. The original RFC outlined two interface options (comma-separated values or repeated flags) and proposed reusing existing sparse-checkout machinery, but no reviewers have weighed in on usefulness, design, or potential concerns. The lack of response leaves the feature's prospects uncertain; a busy reader can safely defer attention until substantive discussion emerges.