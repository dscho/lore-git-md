This is a brief follow-up in the ongoing memory-leak thread tied to Patrick Steinhardt’s `git history` command. Jeff King reports that the newly discovered leak in the topological walk slab (`init_topo_walk`), which surfaced in the `t4014` test, has already been fixed in a separate patch. That fix is independent of the original `git history reword` leak and can be merged on its own. Jeff also notes a minor test-harness issue (TAP output formatting) that was addressed in the same thread. The original `git history reword` fix remains correct and merged; this message simply closes the loop on the additional leak that appeared during broader testing. No further action is needed on the `git history` side.