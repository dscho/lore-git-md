This is a **follow-up discussion** in Patrick Steinhardt’s six-patch series refactoring `struct object_info` to use a `source` field, part of the ODB abstraction effort. The email responds to Justin Tobler’s **substantive review** of patch 1/6, which questioned the architectural choice of threading an optional `odb_source_packed *source` parameter through `packed_object_info()` instead of tracking the source at the `packed_git` level.

Patrick clarifies that tracking the source in `packed_git` would be a layering violation, as packfiles can exist independently of their source. He acknowledges the current design feels "iffy" but argues it’s pragmatic: most direct callers of `packed_object_info()` don’t need source information, and the long-term goal is to eliminate direct use of this function in favor of `odb_read_object_info()`. He notes ongoing work to refactor `git-cat-file` and the commit-graph to align with this vision, though other commands (e.g., `git-pack-objects`) remain unaddressed. The response leaves the door open for future design iterations but defends the current approach as a necessary intermediate step.

For AI summarizer brief mode: The discussion centers on **patch 1/6**’s design, touching files `packfile.c`, `odb/source-packed.c`, and `odb.h`. Key symbols include `packed_object_info()`, `odb_source_packed`, and `odb_read_object_info()`. The architectural tension remains unresolved—whether to centralize source tracking in `packed_git` or keep it opt-in via `packed_object_info()`—but Patrick’s response suggests the latter is a deliberate trade-off for incremental progress. No code changes are proposed here; this is purely a design rationale exchange.