This is the first patch in a two-patch bugfix series from Jeff King addressing minor memory leaks and test harness output issues. This specific patch adjusts the test harness to redirect LeakSanitizer (LSan) error output from stdout to stderr (descriptor 4, which respects verbose output settings). The change fixes an annoyance where LSan reports were being misinterpreted as invalid TAP output when running tests under `prove`, leading to parse errors and cluttered test summaries. The fix is purely infrastructural—three lines modified in `t/test-lib.sh`—and does not alter Git's behavior or leak detection logic, only where the reports are sent.

The patch is well-motivated: the commit message clearly explains the problem (TAP parse errors since a recent change to `prove` behavior) and demonstrates the impact with a real-world example. The solution is minimal and targeted, redirecting only the LSan-related output while preserving the existing verbose-logging behavior. No new edge cases or test failures are introduced, and the change aligns with the test suite's existing patterns for error reporting.

This is a low-risk, high-value cleanup that improves the developer experience when running leak tests. The patch is ready for review and likely uncontroversial, given its narrow scope and clear rationale. For follow-up discussion, note the specific files (`t/test-lib.sh`) and functions (`check_test_results_san_file_`) involved, as well as the context of the recent `prove` TAP parsing change (commit `389c83025d`). No new CLI options, config keys, or user-visible changes are introduced.