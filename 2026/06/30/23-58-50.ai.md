This is a **performance analysis follow-up** in the macOS CI hang thread, now uncovering a **quadratic-time behavior in the reftable backend** during bulk ref deletion and re-creation. Jeff King (Peff) identifies a previously unnoticed scalability issue: when deleting and then re-creating a large number of refs (e.g., 8,000 tags), the runtime grows quadratically (1.8s → 7.1s for a 2x input size increase), suggesting an inefficient iteration over tombstone entries.

The problem surfaces in a specific workflow: `git for-each-ref --format="delete %(refname)" | git update-ref --stdin` followed by re-creating the same refs. Peff’s test script isolates the issue, showing that the quadratic behavior is reproducible outside `hyperfine` but requires a large input size (thousands of refs). The root cause appears to be the reftable backend’s handling of deleted refs—likely iterating through tombstone entries during subsequent operations—but the exact mechanism remains unclear.

This finding shifts the focus from the earlier micro-optimizations (redundant `stat()` calls, unbuffered reflog writes) to a more impactful performance bottleneck. Peff frames it as "probably a more interesting and productive problem" than the prior optimizations, given its non-linear scalability. The issue is not yet actionable (no patch or proposed fix), but it provides a clear direction for future work: diagnosing why the backend’s iteration over deleted refs scales poorly and whether tombstone handling can be optimized.

**Key technical details for future reference**:
- **Subsystem**: reftable backend (`reftable/merged.c`, `merged_iter_next_entry()`).
- **Trigger**: Bulk ref deletion followed by re-creation (e.g., `git for-each-ref | git update-ref --stdin`).
- **Symptom**: Quadratic runtime growth (4x for 2x input size).
- **Test case**: Script provided in the email; requires thousands of refs to reproduce.
- **Files touched**: None in this email, but future work would likely target `reftable/merged.c`.
- **Related context**: Earlier discussion of `stat()` overhead and reflog writes is now secondary to this larger issue.

The tone remains pragmatic—Peff notes the quadratic behavior is "super weird" but defers deeper investigation, leaving the door open for others to explore. This is a **substantive technical observation** that could inform future reftable optimizations, particularly for large repositories.