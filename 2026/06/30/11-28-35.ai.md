This is a **brief follow-up in the ODB subsystem refactoring thread**, specifically addressing a surface-level naming concern in the second patch of Patrick Steinhardt’s six-patch series. The exchange focuses on the `sourcep` field in `struct object_info`, which Justin Tobler noted could be misread as a pointer to `struct odb_source` (a later-introduced type) rather than the intended `struct object_info_source`. Patrick acknowledges the ambiguity but struggles to find a concise alternative, proposing `source_infop` if the struct itself were renamed to `odb_source_info`. This is a minor, non-blocking discussion—no technical objections or design questions are raised, and the patch’s core opt-in mechanism for backend-specific source tracking remains unchallenged. The thread’s architectural debate (opt-in vs. centralized source tracking) is not revisited here. For AI summarization, note the proposed naming tweak (`source_infop`) and the unchanged status of the patch’s functionality.