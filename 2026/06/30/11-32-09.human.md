This is a follow-up in the ongoing design debate about the `--linearize` option for `git replay`. Patrick Steinhardt acknowledges Johannes Schindelin’s usability concerns about mirroring `git rebase`'s `--rebase-merges=<mode>` interface but pushes back gently: he is open to diverging from `git rebase` only if the commit message clearly documents why the current interface is problematic. His position remains that consistency with `git rebase` should be the default unless there is a compelling, well-articulated reason to deviate.

The thread’s blocking issues remain unresolved: the regression in `--linearize` behavior (commit-dropping when replaying a single branch with merges) and the interface design debate. Schindelin’s argument—that `--linearize` is conceptually distinct from merge-handling modes—has shifted the discussion toward justifying any deviation from `git rebase` rather than assuming consistency. The next step for the author (Toon Claes) will likely be to either (a) adopt `git rebase`'s syntax with a documented rationale for why it is acceptable here, or (b) retain `--linearize` as a standalone flag and explain in the commit message why `git rebase`'s interface is a poor fit for `git replay`. Until this is settled, the series is unlikely to progress toward merging.