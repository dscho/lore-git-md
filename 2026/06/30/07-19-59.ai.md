This is a **user experience discussion** about recoverability in the `git history` suite, sparked by Matt Hunter’s question about undoing operations. Harald Nordgren’s reply shifts the focus from the `squash` subcommand to a broader critique of Git’s reflog design.

Harald acknowledges Matt’s concern—`git reset --hard` may not fully restore state if `--update-refs` moved other branches—but pivots to a more ambitious proposal: **a human-centered reflog**. He argues that the current reflog prioritizes low-level operations (like branch switches) over high-level workflows (like rebasing or squashing), making it difficult to track the "before" and "after" states of multi-commit operations. His vision includes **visual hierarchy** (e.g., indentation for sub-steps) to reflect the logical structure of actions, rather than treating every reflog entry as equally important.

The reply is **not a patch or technical proposal**, but a design idea that could influence future work on Git’s user interface. It’s notable for its **user-centric framing**: Harald doesn’t suggest a specific implementation (e.g., new reflog flags or formats), but instead describes how the tool *feels* to use. This aligns with his earlier focus on usability in the `squash` subcommand (e.g., `--reedit-message` template formatting).

For the thread, this email **expands the scope** beyond the `squash` feature to a recurring pain point in Git’s UX. While it doesn’t resolve Matt’s immediate question about `git history` recoverability, it signals that the project’s attention to usability extends beyond individual commands. Future discussion might explore whether this idea gains traction—perhaps as an RFC or a patch to `git reflog`’s output formatting. For now, it’s a **thought experiment** rather than a concrete plan.