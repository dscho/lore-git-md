This is a **feature addition** to the `git replay` command, part of the ongoing v5 series introducing a `--linearize` option. The email is a reply in the thread where Toon Claes acknowledges a **blocking regression** in the current implementation: when replaying multiple branches with `--linearize`, the commits are flattened into a single linear history, which may not match user expectations.

Toon agrees with Johannes Schindelin’s earlier critique that the `replayed_base` parameter (removed in v5) is necessary to prevent commit-dropping and ensure predictable behavior. He accepts that `--linearize` should always produce a single linear topology, even when replaying multiple branches, and proposes documenting this clearly rather than attempting to preserve branch non-reachability. This stance reinforces the argument for keeping `--linearize` as a standalone flag (rather than adopting `git rebase`'s `--rebase-merges=<mode>` syntax), since the behavior is intentionally distinct and simpler.

The email does not include a fix for the regression, but it signals the author’s intent to restore `replayed_base` and update the documentation to reflect the predictable (if potentially surprising) behavior. The discussion remains focused on **design clarity** and **user expectations**, with no new technical implementation details or test cases provided. The series is still under review, and this reply does not resolve the interface design debate with Patrick Steinhardt.