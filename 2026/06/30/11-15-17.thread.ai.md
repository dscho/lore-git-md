Thread identity
Subject: git blame -b output formatting and commit hash abbreviation
Series version: none (standalone patch)
Originating author: Laszlo Ersek (user report)
Thread type: bug report / usability discussion leading to a patch

Core problem or goal
The thread began with a report that `git blame -b` (show boundary commits as blank) reserved an extra hex digit in commit hash abbreviations for a caret marker that was never displayed, causing non-boundary commit hashes to exceed the user’s `core.abbrev` setting by one character. This created a usability friction in manual workflows: when using `git blame -b` to identify a commit for editing during an interactive rebase, the abbreviated hash in the blame output was one nibble longer than the hashes in the rebase TODO list, forcing manual truncation when pasting.

Current status
A patch by René Scharfe has been posted and **confirmed by the original reporter (Laszlo Ersek) as fully resolving the issue**. The patch refactors the mark-handling logic in `blame.c` to count and print boundary/ignored/unblamable marks only when they are actually shown, ensuring the abbreviated commit hash length aligns with the user’s `core.abbrev` setting. Laszlo’s **Tested-by** trailer and explicit endorsement close the usability loop. Junio C Hamano’s review marks the patch as **ready for integration** with no unresolved technical objections. The patch touches `blame.c`, updates `Documentation/git-blame.adoc`, and adjusts existing test expectations in `t/t8002-blame.sh`. **Test coverage remains absent**; René and Junio have not proposed or required additional tests, though the patch’s dynamic mark-handling logic could benefit from validation of edge cases (e.g., multiple marks per line).

Key technical details
- Command: `git blame -b`
- Related command: `git rebase -i` (manual workflow interaction)
- Subsystems: blame output formatting (`blame.c`)
- Config keys: `core.abbrev` (no `blame.abbrev` proposed or needed)
- Behavior: `git blame -b` previously abbreviated non-boundary hashes to `core.abbrev + 1` hex chars to reserve space for an unused caret marker; the patch removes this reservation, aligning the output with `core.abbrev`. The fixed-width nature of the output is preserved.
- Files: `blame.c` (output formatting logic), `Documentation/git-blame.adoc` (updated description of `--abbrev`), `t/t8002-blame.sh` (existing test expectations adjusted)
- Symbols: new helper functions `print_marks()` and `count_marks()` introduced to centralize mark-handling logic; `maybe_putc()` for conditional character output; `find_alignment()` updated to dynamically calculate mark columns based on the maximum number of marks observed in the output
- On-disk format: none affected
- Test coverage: **absent**; the patch lacks tests to verify the fix and prevent regressions, including edge cases like multiple marks per line

Open questions and loose ends
- **Test coverage**: Should tests be added to verify the fix and prevent regressions? The patch’s dynamic mark-handling logic should accommodate edge cases (e.g., multiple marks per line), but this has not been validated. No tests have been proposed, and Junio’s review did not require them.
- **Backward compatibility**: Are there risks for scripts or tools parsing `git blame -b` output? The patch preserves the fixed-width nature of the output, but downstream tools expecting a specific hash length might be affected. No such tools have been identified in the thread, and Junio’s review did not raise concerns.
- **Edge cases**: René did not verify whether multiple marks per line are possible in practice, though the patch’s logic should handle them if they exist.

Participant positions
- **Laszlo Ersek** (original reporter): Identified the issue as a usability friction in manual workflows involving `git blame -b` and `git rebase -i`. **Confirmed the patch resolves the problem** with a **Tested-by** trailer and explicit endorsement. No further concerns or objections; considers the issue resolved.
- **Junio C Hamano** (maintainer): Initially reframed the issue as a potential robustness problem in `git rebase`’s parser but later shifted focus to `git blame`’s output formatting. Proposed the targeted fix (removing the extra hex digit) and reviewed René’s patch. **Marked the patch as ready for integration** with no unresolved technical objections. Did not raise concerns about test coverage, backward compatibility, or edge cases.
- **René Scharfe** (patch author): Implemented the fix by refactoring the mark-handling logic in `blame.c`. The patch is minimal, well-structured, and directly addresses the core problem. René notes the **lack of test coverage** and acknowledges potential edge cases (e.g., multiple marks per line) but did not verify their feasibility.

Related work
None mentioned. The discussion remains self-contained to `git blame -b`’s output formatting and its interaction with manual workflows.