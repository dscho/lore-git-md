This thread began with Laszlo Ersek reporting a minor but persistent annoyance in his workflow: when using `git blame -b` (which shows boundary commits as blank) to identify a commit for editing during an interactive rebase, the abbreviated commit hashes in the blame output are one character longer than those in the rebase TODO list. This happens because `git blame -b` reserves an extra hex digit for a caret marker that is never displayed, effectively using `core.abbrev + 1` characters for non-boundary commits. While Junio C Hamano initially framed this as a potential robustness issue in `git rebase`’s parser—suggesting it should accept variable-length hashes up to 16 digits—Laszlo clarified that the problem is purely a usability friction in manual workflows, not a parser failure.

The discussion now centers on whether `git blame -b` should stop reserving the extra nibble for the unused caret marker, even if the impact is limited to copy-paste workflows. Junio acknowledged the aesthetic suboptimality but implied the practical impact is minor, while Laszlo’s follow-up highlights a concrete, if niche, pain point. No patches have been proposed yet, and the thread remains open-ended. The key technical details involve `core.abbrev`, the `git blame -b` output format, and the absence of a `blame.abbrev` override. Backward-compatibility risks for scripts or tools parsing blame output have not been addressed. For now, the thread is a low-stakes but illustrative example of how small formatting inconsistencies can disrupt user workflows.