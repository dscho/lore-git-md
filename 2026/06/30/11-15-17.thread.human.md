The thread began with a report that `git blame -b` (which shows boundary commits as blank) unnecessarily reserves an extra hex digit in commit hash abbreviations, making them one character longer than the user’s `core.abbrev` setting and breaking copy-paste workflows. Junio C Hamano has now weighed in, suggesting the real issue may lie with `git rebase`’s parser, which he believes should accept longer abbreviations (up to 16 hex digits) as long as they remain unambiguous. He notes that Git already extends abbreviations beyond `core.abbrev` when needed to avoid ambiguity, implying that a parser expecting strictly fixed-length hashes might itself be flawed.

The discussion has shifted from `git blame`’s output formatting to the robustness of `git rebase`’s input handling, with Junio framing the latter as the more pressing concern. No code changes have been proposed yet, and the thread remains open-ended—it’s unclear whether the solution will involve adjusting `git blame`, `git rebase`, or both. The key technical details now include `core.abbrev`, the `git rebase` TODO list parser, and the likely location of the parser in `builtin/rebase.c`. No other participants have contributed positions yet.