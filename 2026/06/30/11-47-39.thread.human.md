Patrick Steinhardt has sent a 13-patch series to refactor Git’s repository discovery and setup code, separating the two phases to reduce global state and consolidate configuration logic. The series introduces a new `struct repo_discovery` to hold discovery results, replacing the current interleaved approach with distinct, self-contained steps. Junio C Hamano has begun queuing patches, starting with the second one after fixing a minor typo in its commit message—no substantive concerns have been raised so far.

The refactoring touches core setup logic in `setup.c`, including the extraction of a new `repo_discover()` function, the removal of static buffers, and the consolidation of worktree configuration into `init_db()`. Key changes include embedding `struct repository_format` into `repo_discovery`, propagating the repository prefix through the new structure, and eliminating redundant state management. The series builds on recent work like `ps/setup-drop-global-state` and `jk/repo-info-path-keys`, with no intended behavior changes but potential edge cases in repository initialization.

So far, only Junio has commented, addressing a trivial typo in patch 2/13. The rest of the series remains unchallenged, though open questions include how the new API will handle environment variable overrides, worktree configuration, and integration with ongoing efforts like `the_repository` removal. The next patches are expected to proceed without major revisions.