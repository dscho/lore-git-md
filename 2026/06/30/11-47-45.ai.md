This is **patch 6/13** in Patrick Steinhardt’s refactoring series to separate repository discovery from setup in Git’s core code. The patch embeds the `struct repository_format` directly into the new `struct repo_discovery`, eliminating the need to pass both structures separately to discovery functions. This is a mechanical but important step toward consolidating repository configuration logic into a single, self-contained phase.

The change touches only `setup.c`, updating the `repo_discovery` structure to include a `repository_format` field and adjusting all discovery functions (`repo_discover_explicit_gitdir`, `repo_discover_implicit_gitdir`, `repo_discover_bare_gitdir`) to populate and use the embedded format instead of an external parameter. The patch also simplifies `setup_git_directory_gently()` by removing the now-redundant local `repo_fmt` variable, relying instead on the discovery structure’s format field. The diff is clean, with no behavior changes intended—just a structural shift to align with the series’ goal of making discovery and setup distinct phases.

This patch is a natural progression from earlier work in the series (e.g., patch 5/13’s introduction of `struct repo_discovery`). It further reduces the surface area of the setup API by encapsulating the repository format alongside the discovered paths, which will likely simplify future patches that extend `repo_discovery` to include additional metadata (e.g., prefix, worktree config). The commit message is concise but clear, and the code changes are straightforward, suggesting this is a well-considered intermediate step rather than a speculative refactor.

Key technical details for follow-up discussion:
- **New structure layout**: `struct repo_discovery` now embeds `struct repository_format format` (initialized via `REPOSITORY_FORMAT_INIT`).
- **Function signature changes**: Discovery functions drop the `struct repository_format *` parameter, using `&discovery->format` instead.
- **Call-site updates**: `setup_git_directory_gently()` no longer declares a local `repo_fmt`, relying on the discovery structure’s format field throughout.
- **Memory management**: `repo_discovery_release()` now calls `clear_repository_format()` to clean up the embedded format.
- **Files touched**: Only `setup.c` (60 lines changed, 29 insertions, 31 deletions).

This patch is unlikely to be controversial on its own, but it may prompt discussion about the broader `repo_discovery` API design—particularly how it will handle edge cases like environment variable overrides or worktree configuration in later patches. The series’ trajectory suggests this is a stepping stone toward a more modular setup process, so reviewers may focus on whether the embedded format approach scales to future requirements.