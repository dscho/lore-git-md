This is the latest exchange in Justin Tobler’s six-patch series that replaces `tmp_objdir` with ODB transactions in `git receive-pack`. The thread is now finalizing v2 after all review feedback has been addressed, and this specific email confirms one remaining design decision.

Justin and Patrick Steinhardt explicitly agree to defer error-handling improvements—specifically, making `odb_transaction_files_prepare()` failures fatal—to a future series. The current patch keeps the existing “YOLO” behavior (callers ignore the return value) to preserve backward compatibility, but both acknowledge it is suboptimal. Justin notes that the lazy directory creation itself may be unnecessary and plans to investigate its performance impact in follow-up work. The exchange removes the last technical blocker for v2, which is now queued for `next`.