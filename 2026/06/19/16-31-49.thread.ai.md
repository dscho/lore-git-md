Thread identity: bug report about data loss in sparse-checkout mode when `git checkout` silently overwrites untracked files despite the presence of a warning that suggests the file was preserved. Originating author: charmocc. Thread type: bug report.

Core problem or goal: `git checkout` in sparse-checkout mode fails to abort when an untracked file would be overwritten, unlike its behavior in normal mode. Instead, it silently overwrites the file and emits a misleading warning ("The following paths were already present and not updated despite sparse patterns"), which falsely implies the file was preserved. This inconsistency creates a risk of data loss for users who rely on Git's safety checks.

Current status: newly reported; no prior discussion, patches, or resolutions. The issue is confirmed reproducible via the provided steps.

Key technical details: affects the sparse-checkout subsystem; the flaw lies in the interaction between sparse-checkout logic and the working directory state check (`would_lose_untracked_file`). The misleading warning originates from `unpack-trees.c` (likely the `already_exists` path). No new symbols, files, or on-disk format changes are introduced; this is purely a behavioral regression in the checkout operation.

Open questions and loose ends: root cause not yet identified; no proposed fix; no maintainer response. The report does not specify Git version behavior across releases, so it is unclear whether this is a recent regression or long-standing issue.

Participant positions: charmocc (author) has provided a clear, actionable report with reproduction steps, expected/actual behavior, and system details. No other participants have weighed in yet.

Related work: touches the sparse-checkout/sparse-index effort (Derrick Stolee, Elijah Newren), but no direct connection to in-flight refactoring. The issue may intersect with recent work on `unpack-trees.c` or checkout safety checks.