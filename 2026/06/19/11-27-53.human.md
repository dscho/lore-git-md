This is a cleanup patch in Patrick Steinhardt's 10-part refactoring series (v4) modernizing Git's reference backend infrastructure. This specific patch (5/10) removes the now-unused `chdir_notify_reparent()` function from the `chdir_notify` subsystem after the previous patch eliminated all its callers.

The change is straightforward and mechanical: the patch deletes the 26-line function implementation from `chdir-notify.c` and updates the header file to remove its declaration, while preserving the documentation comment that now points users to the `reparent_relative_path()` helper instead. This cleanup follows the pattern established in patch 4/10, which properly implemented registration/unregistration of reference stores with the `chdir_notify` system rather than using the one-way `chdir_notify_reparent()` function.

The patch is uncontroversial and appears well-justified - it's a natural follow-up to the lifecycle management improvements made earlier in the series. The commit message is clear and the diff is minimal, focusing only on removing dead code. This looks like a clean, low-risk change that helps simplify the codebase as part of the larger ref backend modernization effort.