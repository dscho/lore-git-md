This is a test infrastructure patch, part of the `git cat-file --batch-command` security-hardened feature series (v13, patch 4/12). The patch extracts three utility functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) from `t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`. The goal is to improve code reuse and readability for future tests, particularly the upcoming `t1017-cat-file-remote-object-info.sh` test script that will need these same helpers.

The change is purely mechanical: no behavior is altered, and the original script now sources the new library. The extracted functions are low-level string helpers used for testing `git cat-file` output formatting and length calculations. The commit message is clear about the purpose, and the patch itself is small and focused. This is a routine test modernization effort that aligns with Git's practice of factoring out common test utilities.

The patch was authored by Eric Ju and carries Pablo Sabater's sign-off, reflecting the collaborative nature of the series. No review feedback has been raised on this specific patch yet, but given its straightforward nature and the series' overall readiness for merging, it is unlikely to be controversial. The extraction is a sensible preparatory step for the larger feature.