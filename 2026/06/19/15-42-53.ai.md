This is a **design-level clarification** in the `git branch --delete-merged` series, specifically addressing the **stacked-branch safety** question. Junio C Hamano acknowledges Phillip Wood’s point about `@{u}` (upstream shorthand) breaking if merged branches used as upstreams are deleted. This concession shifts the discussion: the protection mechanism is now seen as necessary to preserve workflows like `git log @{u}..` and implicit `git rebase`, rather than an optional nicety.

Key details for the **Summarizer brief**:
- **Subsystem**: branch management (`git branch --delete-merged`).
- **Behavioral impact**: Deleting merged branches (e.g., `feature1`) would break `@{u}` references for stacked branches (e.g., `feature2`), disrupting commands like `git log @{u}..` and `git rebase`.
- **Junio’s stance**: Previously skeptical, now agrees the protection is justified to avoid breaking these workflows.
- **Files/functions**: The resolution will affect `delete_merged_branches()` in `builtin/branch.c`, likely requiring logic to detect and preserve branches used as upstreams for unmerged branches.
- **Next steps**: Harald Nordgren may proceed with implementing the stacked-branch safety check, as the primary objection has been withdrawn. This could finalize the series (v17) or prompt further discussion about *how* to implement the protection (e.g., automatic upstream updates vs. silent preservation).