This is a brief follow-up in the ongoing build system cleanup thread about macOS linker warnings for duplicate libraries. Harald Nordgren acknowledges Junio C Hamano’s architectural suggestion to suppress the warnings at the linker level using `-Wl,-no_warn_duplicate_libraries` rather than restructuring the Makefile. He agrees that version-gating the flag via `LD_MAJOR_VERSION` (matching the earlier `__DATA,__common` alignment fix) is a cleaner approach.

The email signals convergence on Junio’s proposed solution: the cosmetic warning will be handled by a macOS-specific linker flag in `config.mak.uname`, preserving the existing Makefile logic that sometimes requires listing libraries multiple times for circular dependencies. No new technical details or objections are introduced; this is essentially a +1 to the linker-flag suppression direction. The thread now awaits a final patch implementing the agreed change.