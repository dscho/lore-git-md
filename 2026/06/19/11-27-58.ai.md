This is the final patch in Patrick Steinhardt's 10-part refactoring series (v4) modernizing Git's reference backend infrastructure. It performs a small but meaningful cleanup in `refs.c` by eliminating an unnecessary local buffer in `refs_compute_filesystem_location()`.

The patch removes a redundant `struct strbuf sb` that was only used as an intermediate step before calling `strbuf_realpath()`. The function already supports using the same buffer for both input and output, so the extra allocation and copy were superfluous. The change reduces memory churn and simplifies the code by directly operating on `ref_common_dir` where possible. The diff is minimal (6 lines removed, 2 added) and focused on a single function, making it a straightforward cleanup with no behavioral impact.

This patch ties off the series by addressing a minor inefficiency in path computation, complementing the earlier architectural changes (like the `ignore_refdb` flag introduced in patch 9/10) and memory leak fixes. While small, it reflects the series' broader goal of tidying up the ref backend code. The change is uncontroversial and aligns with Git's preference for eliminating unnecessary allocations. No test failures or edge cases are expected, as the logic remains identical—just more efficient.