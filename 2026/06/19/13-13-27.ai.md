This is a **substantive review reply** in the ongoing `git branch --delete-merged` series, specifically addressing the **stacked-branch safety** question. Phillip Wood responds to Junio C Hamano’s skepticism about protecting branches that serve as upstreams for unmerged stacked branches.

Phillip clarifies the practical concern: if `feature1` (merged into `origin/master`) is deleted by `--prune-merged`, then `feature2` (stacked on `feature1`) loses its upstream. This breaks common workflows like `git log @{u}..` and `git rebase` (without an explicit upstream), which rely on the upstream relationship. While Junio suggested that users could manually update `feature2`’s upstream after `feature1` is merged, Phillip questions whether the tool should *force* this by deleting `feature1` outright.

The reply is **technically grounded** and **workflow-aware**, citing real-world commands (`@{u}..`, `git rebase`) that would break. It does not propose a specific implementation fix but reinforces the need for the protection mechanism discussed earlier in the thread. This keeps the door open for Harald Nordgren to either retain the safety check or argue for an alternative (e.g., automatically updating upstreams).

For the **Summarizer brief**, key details:
- **Subsystem**: branch management (`git branch --delete-merged`).
- **Key issue**: Whether deleting merged branches should preserve those used as upstreams for unmerged stacked branches.
- **Phillip’s stance**: Preserving such branches is necessary to avoid breaking `git log @{u}..` and implicit `git rebase` workflows.
- **Contrast with Junio**: Junio saw the protection as unnecessary; Phillip counters with concrete user-facing breakage.
- **Files/functions**: No direct code references, but the discussion affects `delete_merged_branches()` in `builtin/branch.c`.
- **Likely contention**: This remains a design-level disagreement about safety vs. simplicity. The resolution will shape the final behavior of `--delete-merged`.