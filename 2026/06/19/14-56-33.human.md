This is a test infrastructure refactoring patch, part of the ongoing `git cat-file --batch-command` security-hardened feature series (v13, patch 4/12). The patch moves utility functions from `t/t1006-cat-file.sh` into a new shared library file `t/lib-cat-file.sh`, enabling reuse across multiple test scripts.

The change is straightforward: three helper functions (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) are extracted from the existing cat-file test script into a dedicated library. The commit message explains this is done to improve code reuse and readability, which aligns with the series' broader goal of supporting new remote object-info tests (like the upcoming `t1017-cat-file-remote-object-info.sh`).

The patch itself is mechanical and well-contained, touching only test infrastructure files. The diff shows clean extraction with no behavioral changes - the functions are simply moved and the original script now sources the new library. This is a sensible cleanup that follows established Git test patterns (similar to `lib-loose.sh` which is already sourced in the same file).

While small, this refactoring demonstrates good attention to test infrastructure hygiene in what's otherwise a security-focused feature series. The patch appears ready for merging as part of the larger series.