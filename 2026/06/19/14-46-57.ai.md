This is the fourth substantive exchange in Taylor Blau’s 4-patch series integrating `--delta-islands` with `--path-walk` in `git pack-objects`. The email clarifies the final piece of the interaction model: when both `--use-bitmap-index` and `--path-walk` are specified, Git first attempts a bitmap-based traversal (which may reuse existing pack data) and falls back to path-walk only if bitmaps are unavailable or insufficient. This fallback is a one-way escape hatch, not a combined algorithm—bitmaps remain the preferred fast path, and path-walk is never used to augment bitmap results. The discussion confirms that the implementation matches the design goal of feature compatibility without regressing performance or correctness. Files involved are still `pack-objects.c`/`h`; no new symbols or on-disk changes are introduced. The thread is now technically complete, pending the proposed p5311 perf test addition to document the sequential interaction empirically.