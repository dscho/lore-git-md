This is a **substantive review** from Kristofer Karlsson, the author of the prio-queue refactoring (`dd4bc01c0a`) that introduced the dual-queue traversal model now blocking Pablo Sabater’s `git log --graph` visualization series. Kristofer clarifies the immediate impact of his change and proposes a forward-looking abstraction to decouple graph rendering from revision-walk internals.

Kristofer confirms that the current conflict is theoretical: the prio-queue refactoring only affects `REV_WALK_STREAMING` mode, while `--graph` uses `REV_WALK_TOPO` or `REV_WALK_LIMITED`, so `revs->commits` remains the active queue. However, he flags `graph_peek_next_visible()`’s direct access to `revs->commits` as fragile, especially if Jeff King’s planned removal of `revs->commits` proceeds. His proposed abstraction—`revision_has_more_commits()` and `revision_peek_next_commit()`—would hide the walker’s data structure behind a stable API, insulating graph.c from future changes.

On the multi-element peek question, Kristofer suggests that the lookahead logic in Pablo’s series does not actually need to inspect the *value* of the next commit, only whether one exists. He proposes replacing peek-based checks with queue-size queries, which would sidestep the prio-queue’s single-entry peek limitation entirely. This observation simplifies the blocker: the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) can likely be set correctly without scanning both queues, provided the logic is refactored to rely on queue state rather than commit content.

The review advances the thread by offering a concrete path forward—adopt the abstraction layer and refactor the lookahead logic to use queue size—while also signaling that the prio-queue refactoring itself is not the root cause of the current incompatibility. Pablo now has a clear technical direction: implement the abstraction, adjust the lookahead logic, and demonstrate that the flags remain correct. The series is still blocked, but the blocker is now narrower and more tractable.