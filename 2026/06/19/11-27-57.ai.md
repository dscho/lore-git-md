This is the core patch in Patrick Steinhardt's v4 refactoring series (9/10), introducing the architectural fix for recursive initialization in `get_main_ref_store()` when "onbranch" config conditions are present. The patch adds a new `ignore_refs` flag to `struct config_options` and uses it to selectively disable "onbranch" config evaluation during ref store initialization, preventing the chicken-and-egg problem where config parsing requires a ref store and ref store initialization requires config parsing.

The change modifies `include_by_branch()` in `config.c` to skip "onbranch" conditions when `ignore_refs` is set, and updates both the files and reftable backends (`files-backend.c`, `reftable-backend.c`) to parse their configuration with this flag enabled during initialization. A static `initializing` flag in `get_main_ref_store()` acts as a sanity check to catch future recursions. The patch is well-motivated by the thread's earlier discussion, directly addressing Jeff King's critique of v3's workaround approach.

This is a targeted, low-risk solution that preserves other config include conditions (like `includeif.gitdir`) while fixing the specific recursion issue. The implementation is clean, with clear commit message reasoning and minimal code changes. The patch should satisfy reviewers who wanted a root-cause fix rather than a symptom workaround, and it completes the series' architectural goal of simplifying ref backend initialization. Test coverage from earlier patches in the series confirms behavior remains unchanged.