Here’s the human-readable summary for a developer catching up on the thread:

---

This thread started as a bug fix for a broken test in `t/t4216-log-bloom.sh` that caused TAP compliance failures on non-x86 architectures, blocking the Git 2.55.0 release. The test checked whether the system’s `char` type is signed by default, but it was a no-op (guarding only a `true` statement) and became problematic after commit 389c83025d tightened the test harness’s handling of invalid TAP output. The original patch proposed deleting the test entirely, while Taylor Blau suggested preserving it in a TAP-compliant form using `test_lazy_prereq`.

The discussion quickly shifted to urgency: Todd Zullinger confirmed the deletion patch fixed the regression and framed it as a low-risk, release-blocking fix. Junio C Hamano ultimately decided to merge the deletion patch for Git 2.55.0, prioritizing the immediate regression over the unresolved debate about the test’s original intent (sanity-checking murmur3’s behavior). Taylor Blau can revisit the test’s value later if he chooses, but the fix is now settled. The patch removes 21 lines from the test script, restores TAP compliance, and has no impact on user-facing behavior or on-disk formats. Todd’s testing confirmed the fix works, and Junio’s tone suggests no further discussion is needed before merging.

---