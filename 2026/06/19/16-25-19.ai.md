This is a **design discussion reply** in the ongoing pinned references thread. Junio C Hamano responds to Patrick Steinhardt’s observation about Git’s existing `<describeOutput>` syntax with a thought experiment: what if Git *did* enforce the invariant that the tag and the object ID must match when the "hop count" is `-0-`?

Junio notes that changing the existing `<describeOutput>` behavior to enforce this invariant is "very unlikely" — the syntax has been stable for years, and breaking existing scripts or workflows that rely on its current behavior would be disruptive. However, he engages with the idea as a hypothetical, outlining how such enforcement might work: if the hop count is `-0-`, Git would verify that the hexadecimal part exactly matches the object ID the tag points to; if the hop count is non-zero, Git would verify that the hexadecimal part names a commit that is a descendant of the tag’s target.

This reply does not endorse or oppose Erik Östlund’s original pinned references proposal, but it does clarify that the existing `<describeOutput>` syntax is not a viable path for introducing enforcement — its behavior is too entrenched to change. The discussion remains conceptual, with no code, patches, or concrete implementation plans. Junio’s input suggests that if pinned references are to become a real feature, they will likely need to be implemented as a new mechanism (e.g., a new command-line option, a new syntax, or a new command) rather than as an extension of `<describeOutput>`.

The thread is still in the exploratory phase, focused on terminology, prior art, and design direction. No files, subsystems, or symbols have been identified yet, and the conversation remains centered on use cases and syntax alternatives. Junio’s reply adds weight to the idea that any solution will need to be additive rather than modifying existing behavior.