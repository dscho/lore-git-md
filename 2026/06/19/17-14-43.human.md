This is a maintainer response to a refactoring patch that is already in the `next` branch. The patch moves the `protect_hfs` and `protect_ntfs` settings into the per-repository `repo_config_values` struct as part of the ongoing libification effort.

Junio acknowledges the change is good for consistency with two other in-flight topics (which also deal with repository-scoped configuration values) but notes that because the patch is already in `next`, it cannot be replaced wholesale. Instead, he proposes a follow-up patch that adjusts the accessor functions to check `repo->initialized` instead of `repo->gitdir`. The rationale is to align with how other repository-scoped settings are guarded against uninitialized repository objects. The diff itself is tiny—just four lines changed in two files—and the commit message should explain the consistency motivation.

The thread is now at a point where the author needs to send a small incremental patch on top of the existing topic in `next`, rather than a full v5 resend. This is a routine integration step for topics that have already graduated from `seen`.