This is a trivial follow-up to Taylor Blau's patch series integrating `--delta-islands` with `--path-walk` in `git pack-objects`. The series itself is technically complete and well-tested, with only minor polish remaining before merge.

Junio C Hamano caught a test hygiene nit: the test script used raw `grep` instead of the preferred `test_grep` helper, which provides better error reporting. Taylor acknowledges the fix immediately. This is purely a mechanical change to satisfy the test linter, with no impact on the patch's functionality.

The broader series (4 patches) has already addressed all substantive review concerns about correctness, performance, and edge cases. This particular patch (2/4) focuses on making reachability bitmaps work with path-walking, a key technical piece of the integration. The series maintains the 33.3% speedup from bitmaps while adding path-walk support, and includes thorough test coverage in `t5310-pack-bitmaps.sh`.

Derrick Stolee's remaining questions about optimization efficacy (pack size reduction, deep feature integration) are the only open items, but they don't block this specific test fix. The series builds cleanly on `ds/path-walk-filters` and appears ready for merging once these final nits are addressed.