This is a design question about the `git history squash` feature, now queued in `next`. Junio C Hamano raises a previously unaddressed edge case: what should happen to branch or tag refs that point to commits *inside* the squashed range (A or B in the example `o---A---B---C`)?

The email is directed at Patrick Steinhardt, framing it as a question about the broader `git history` command vision. Junio contrasts two existing Git commands: `git replay` (which rewrites all refs involved) and `git rebase` (which by default updates only the target branch unless `--update-refs` is used). He asks whether `git history squash` should follow the rebase model, with an optional `--update-refs` flag to control whether other refs in the range are updated.

Junio then explores two possible behaviors for the case where `--update-refs` is enabled: either *remove* the refs that pointed to now-vanished commits (A and B), or *retarget* them to the squashed commit (X). He offers a plausible rationale for removal (e.g., manual bisection markers that lose meaning after squashing) but struggles to justify retargeting, leaving the question open.

The email is brief and speculative, not a review or objection. It surfaces a design ambiguity that could affect user workflows but does not block the current implementation. Expect follow-up discussion between Patrick and Harald to clarify the intended behavior before the feature graduates to `master`.