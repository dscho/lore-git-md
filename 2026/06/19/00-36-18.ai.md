This is a **test suite bugfix** targeting platform compatibility. Junio C Hamano acknowledges Jeff King’s clarification about the immediate trigger for the TAP parse failures in `t4216-log-bloom.sh`: the June 2026 commit `389c83025d` tightened the TAP harness to fail on invalid output, exposing a long-standing issue where `test_cmp` diff output leaks into the TAP stream on architectures where `char` is unsigned by default. The email itself does not introduce new code or arguments but confirms the missing context that explains why the problem surfaced only now, despite existing since mid-2024.

The patch will remove the `SIGNED_CHAR_BY_DEFAULT` prerequisite and its associated `test_cmp` check from `t/t4216-log-bloom.sh`, as previously agreed by Junio, Patrick Steinhardt, and Peff. The fix touches only the test script, leaving the Bloom filter logic and on-disk format untouched. The key symbols involved are `SIGNED_CHAR_BY_DEFAULT`, `test_cmp`, and the `highbit1` test case, which currently runs `true` regardless of the outcome, making the prerequisite redundant.

No new objections or questions are raised in this email; Junio’s reply simply accepts Peff’s explanation of the timing. The thread remains focused on the agreed solution, and the fix is expected to be uncontroversial. The unresolved question—whether the Bloom filter logic should vary by platform—is acknowledged but not blocking the immediate fix. The test failure affects non-x86 architectures (e.g., ARM, PowerPC) and is not a regression in the Bloom filter code itself.