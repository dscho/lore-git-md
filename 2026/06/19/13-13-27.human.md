This is a **substantive review discussion** in the ongoing `git branch --delete-merged` series, focusing on a key safety question: whether the command should protect branches that serve as upstreams for unmerged stacked branches.

Phillip Wood responds to Junio C Hamano’s skepticism about the need for stacked-branch protection by providing a concrete workflow example. If `feature1` (merged into `origin/master`) is deleted by `--prune-merged`, then `feature2` (which tracks `feature1` as its upstream) would break common commands like `` `git log @{u}..` `` and `` `git rebase` `` (without an explicit upstream). Phillip suggests that while updating `feature2`’s upstream after `feature1` is merged might be the "correct" workflow, forcing users to do so by deleting `feature1` could be disruptive. His tone is measured but implies that the protection might still be worth the complexity to avoid silently breaking user setups.

The exchange is now a two-sided technical debate: Junio questions whether the protection is necessary given typical rebase workflows, while Phillip highlights a real-world scenario where its absence could cause friction. Neither has dismissed the other’s position outright, so the discussion remains open. For the series author (Harald Nordgren), this means the stacked-branch safety mechanism is not yet a settled issue—it may need to be either defended with stronger arguments or dropped if no consensus emerges. The outcome will directly affect the final design of the `--prune-merged` feature.