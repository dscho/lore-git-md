This is a brief follow-up in the `ignore_case` libification series, now at v3. Tian Yuchen acknowledges Junio C Hamano’s feedback on the cover letter’s link clarity and confirms that the v3 cover letter now directly summarizes the rationale behind the getter approach and the reasons Derrick Stolee’s earlier attempt failed. The only mechanical change mentioned is a consistency update: the three existing getters (`protect_hfs`, `protect_ntfs`, and `core_partial_clone_filter_default`) will also switch from checking `repo->gitdir` to `repo->initialized` to match the new `repo_ignore_case()` pattern. This is a minor cleanup, not a functional change, and keeps the series aligned with the established convention. The thread remains on track for integration.