This is a minor follow-up in Patrick Steinhardt’s 17-part ODB abstraction series, specifically addressing patch 15/17. The patch itself wires up stub callbacks for the packed object source’s unsupported operations (`write_object`, `write_object_stream`, `begin_transaction`, `write_alternate`) and the no-op `read_alternates`, completing the callback registration phase for the packed ODB source. Junio C Hamano flagged a trivial type conflict in the `len` parameter of `odb_source_packed_write_object()`—declared as `unsigned long` but soon to be `size_t` due to an in-flight topic (`po/hash-object-size-t`). He resolved it with an evil merge (changing the type to `size_t`), ensuring Windows build compatibility. Patrick acknowledges the fix as correct and offers to rebase if needed, though no action is required at this stage.

The exchange confirms the patch is mechanically sound and ready for integration once the series’ blocking refdb issues are resolved. The conflict is routine and does not affect the patch’s logic or the series’ broader goals. No deeper technical discussion or objections are raised here; this is purely a coordination note about merge logistics. The thread remains focused on completing the ODB abstraction, with the packed source now fully wired up and symbol encapsulation finalized. The only remaining blocker is the refdb lifecycle management issues Patrick is addressing in a separate 11-patch series.