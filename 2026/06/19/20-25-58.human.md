This is a brief follow-up in the ongoing build system cleanup thread about macOS linker warnings for duplicate libraries. Harald Nordgren acknowledges Junio C Hamano’s suggestion to suppress the warnings at the linker level using `-Wl,-no_warn_duplicate_libraries` rather than restructuring the Makefile. He agrees that version-gating the flag via `LD_MAJOR_VERSION` (following the precedent set for `__DATA,__common` alignment) is a cleaner approach.

The email signals convergence: Harald accepts Junio’s architectural direction, ending the debate between Makefile restructuring and linker flag suppression. The thread now appears ready to move toward implementation of the linker-level fix, which will preserve the existing build logic while eliminating the cosmetic warnings. No new technical details or objections are introduced.