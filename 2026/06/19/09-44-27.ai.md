This is the final cleanup patch in Matt Hunter's fully merged 8-patch series implementing the global `fetch.followRemoteHEAD` configuration option. The patch fixes a minor comment alignment issue in `builtin/fetch.c`: a single line that was incorrectly indented (one extra tab) relative to the surrounding block. The change is purely cosmetic and has no functional impact.

The series as a whole introduced a global fallback configuration for `fetch.followRemoteHEAD`, following the established `fetch.prune`/`remote.*.prune` pattern. This patch represents the last of the post-review polish: earlier patches in the v3 iteration addressed substantive concerns like configuration value validation (adding warnings for invalid values), documentation consistency, test infrastructure isolation, and refactoring of the fetch machinery to decouple configuration source from behavior. The entire series is now merged into `master`.

For future AI summarization, note the file (`builtin/fetch.c`), the specific line (1793), and the nature of the fix (comment indentation). This patch is unlikely to be referenced in follow-up discussion, but the series' key technical details remain relevant: the fallback chain (remote-specific → global → built-in default), the exclusion of "warn-if-not-$branch" at the global level, and the refactoring of `set_head()` to accept the mode as an explicit argument.