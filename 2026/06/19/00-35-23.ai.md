This is a **CI/platform compatibility** request for help from Junio C Hamano, the Git maintainer, about recurring reliability issues with macOS (osx-*) jobs in GitHub Actions CI.

Junio reports that macOS CI jobs in GitHub Actions have recently started running for the full 6-hour timeout and being killed, but the behavior is inconsistent. He links to a full CI run (testing commit `95e20213`) where both `osx-clang` and `osx-gcc` jobs were terminated after 6 hours, and a subsequent minimal run (same commit, but with most jobs disabled) where the macOS jobs completed in under 30 minutes. The only difference between the two runs was the removal of all other jobs from the workflow file, leaving only config and macOS jobs.

The email does not speculate on root causes or propose fixes—it is purely a call for help from anyone familiar with macOS CI quirks or GitHub Actions flakiness. The thread context suggests this is a new and puzzling regression, not a long-standing issue. Future discussion will likely focus on identifying environmental factors (e.g., resource contention, runner image changes, or test suite hangs) that could explain the timeout disparity.