This is a coordination note in Patrick Steinhardt’s 17-part ODB abstraction series, specifically about patch 15/17. Junio C Hamano reports that he has resolved a trivial type conflict in the `odb_source_packed_write_object()` stub by changing the `len` parameter from `unsigned long` to `size_t` via an evil merge, aligning it with the in-flight `po/hash-object-size-t` topic. The fix is routine and ensures Windows build compatibility. Patrick acknowledges the change as correct and offers to rebase if needed, but Junio confirms the merge-fix is low-overhead and expects the other topic to graduate soon after Git 2.55 final. No technical objections or deeper discussion arise; this is purely a merge logistics update.

The patch itself wires up stub callbacks for the packed ODB source’s unsupported operations (`write_object`, `write_object_stream`, `begin_transaction`, `write_alternate`) and the no-op `read_alternates`, completing the callback registration phase. The series remains blocked by refdb lifecycle issues Patrick is addressing separately, but this exchange confirms the mechanical soundness of the current patch and the broader series. The type conflict resolution does not affect the patch’s logic or the series’ goals.