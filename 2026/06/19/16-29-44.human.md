This is a **test suite cleanup** with a maintainer’s reply to Taylor Blau’s review. Junio C Hamano weighs in on the debate over whether to remove or preserve the no-op test in `t/t4216-log-bloom.sh` that checks for signed `char` by default.

Junio’s email shifts the discussion from *how* to fix the TAP compliance issue to *whether* the test’s original intent is still valuable. He questions the significance of the check (commit `52a9...` in the script) and suggests the commit message should justify whether the test’s removal loses anything meaningful beyond a simple "is `char` signed?" check. His comment implies skepticism about the test’s utility—if it’s only verifying a platform quirk rather than guarding against a real regression, deletion may be the cleaner choice.

He also points out a naming inconsistency in the prerequisite (`SIGNED_CHAR_BY_DEFAULT`), noting that the name misleadingly implies the test reflects the platform’s *default* behavior, even though a builder could configure the compiler to use signed `char` on a platform where it’s normally unsigned. This observation subtly undermines the test’s clarity and purpose.

The thread is now at a crossroads: Taylor’s earlier review argued for preserving the test (via `test_lazy_prereq` or silencing output) to retain its potential as a murmur3 sanity check, while Junio’s reply suggests the test may not be worth keeping at all. No consensus has emerged yet, and the outcome hinges on whether other contributors see value in the test’s original intent. The patch’s fate likely depends on a clearer explanation of what, if anything, the test was actually protecting against.