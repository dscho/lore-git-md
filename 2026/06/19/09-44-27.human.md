This is the final cleanup patch in Matt Hunter’s now-merged 8-patch series implementing the global `fetch.followRemoteHEAD` configuration. It fixes a single misaligned comment in `builtin/fetch.c`: the line that reads “make sure it’s valid” was indented one level too far, making it look like it belonged to the preceding `if` block when it actually applies to the following check. The change is purely cosmetic—no logic is touched—and simply restores the comment to the correct column. The patch is uncontroversial and rounds out the series with a minor polish pass.