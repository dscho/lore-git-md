This is a routine follow-up in Patrick Steinhardt’s 17-part ODB abstraction series, specifically addressing patch 15/17. The patch itself wires up stub callbacks for the packed object source’s unsupported operations (`write_object`, `write_object_stream`, `begin_transaction`, `write_alternate`) and the no-op `read_alternates`, completing the callback registration phase for the series.

Junio C Hamano flagged a minor type conflict: the `len` parameter in `odb_source_packed_write_object()` was declared as `unsigned long`, but an in-flight topic (`po/hash-object-size-t`) changes it to `size_t`. He has already prepared an evil merge to resolve this for the Windows build, which would otherwise fail. The fix is trivial (changing the type) and does not affect the patch’s logic. Patrick acknowledges the change as correct and offers to rebase if needed, though no action is required—the conflict will be handled during integration.

This exchange is purely mechanical and confirms the patch is ready for merging once the series’ blocking refdb issues are resolved. The ODB abstraction work remains on track, with all callbacks now registered and type-system consistency maintained. The series is still blocked by refdb lifecycle fixes, but this particular patch is uncontroversial and well-prepared.