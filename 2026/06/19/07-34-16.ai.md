This is a **substantive review** from Kristofer Karlsson (author of the prio-queue refactoring in `dd4bc01c0a`) addressing the critical dual-queue interaction blocking Pablo Sabater’s `git log --graph` visualization series. Kristofer clarifies that the current prio-queue change does *not* affect `--graph` rendering because it only applies to `REV_WALK_STREAMING` mode, while `--graph` uses `REV_WALK_TOPO` or `REV_WALK_LIMITED`. However, he flags `graph_peek_next_visible()`’s direct access to `revs->commits` as fragile, especially if Jeff King’s planned removal of `revs->commits` proceeds.

Kristofer proposes a lightweight abstraction in `revision.c` to insulate `graph.c` from the walker’s internal data structures: two new helper functions (`revision_has_more_commits()` and `revision_peek_next_commit()`) that dispatch based on the walk mode. This would let `graph.c` remain agnostic to whether the walker uses a linked list or a prio-queue, and would future-proof the code against further refactoring. He also suggests that multi-element peeking (a key concern in Pablo’s workaround) is unnecessary here—checking the queue size suffices for the flags in question.

The review advances the thread by removing one blocker (the prio-queue change is not currently problematic) while introducing a cleaner architectural path forward. The proposed abstraction would resolve the immediate dual-queue interaction and could serve as a stepping stone toward Jeff King’s goal of eliminating `revs->commits` entirely. Pablo’s next step is likely to adopt this abstraction and revalidate whether the three key flags can be set correctly without full in-order traversal.