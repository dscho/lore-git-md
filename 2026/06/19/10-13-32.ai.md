This is a **substantive review** of the sequencer notes-during-rebase bugfix patch, focusing on the unresolved fixup-commit edge case and broader correctness issues in the rebase machinery.

Phillip Wood steps in to address the technical debt exposed by Uwe Kleine-König’s patch. He confirms the core issue (notes being incorrectly copied from dropped commits) is real and agrees with Uwe’s general approach—notes should disappear when their commits are dropped, especially since users already lose the commit message in this scenario. However, Phillip identifies a critical flaw in the current implementation: the patch’s logic for skipping `record_in_rewritten()` when a commit is dropped could prevent pending commits from being flushed, particularly in sequences where a non-dropped commit is followed by a dropped fixup. This would incorrectly map notes to the wrong rewritten commit, compounding the original bug.

Beyond this edge case, Phillip surfaces three additional bugs in the rebase machinery related to dropped commits:
1. **Final fixup cleanup**: If the last commit in a sequence is a dropped fixup, the commit message is not cleaned up.
2. **Edit command handling**: Dropping an "edit" command during rebase causes `git rebase --continue` to incorrectly record the commit as rewritten to `HEAD`, leading to misattributed notes.
3. **Skip command handling**: Running `git rebase --skip` after conflicts causes the conflicting commit to be recorded as rewritten to `HEAD`, again misattributing notes.

Phillip proposes to take ownership of these issues, offering to send follow-up patches within the next two weeks. His review is **tested-level**: he traces the code paths across `sequencer.c` and identifies specific failure modes, though he does not yet provide reproduction steps or test cases. The discussion now shifts from Uwe’s narrow fix to a broader audit of how rebase handles dropped commits, with Phillip’s observations suggesting the sequencer’s `record_in_rewritten()` logic may need a systemic overhaul. The unresolved question is whether these bugs should block Uwe’s patch or be addressed in a separate series.