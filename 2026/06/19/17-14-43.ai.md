This is a maintainer response to a refactoring patch (v4) that moves `protect_hfs` and `protect_ntfs` into `struct repo_config_values` as part of the libification effort. The patch is already queued in Junio’s `next` branch, so he is proposing a follow-up patch rather than replacing the existing one.

Junio’s reply focuses on the mechanical change to the accessor functions (`repo_protect_hfs()` and `repo_protect_ntfs()`), where he suggests replacing the `repo->gitdir` check with `repo->initialized` for consistency with other repository-scoped config topics. The proposed patch is tiny (4 lines changed across two files) and purely defensive: it ensures the accessors do not call `repo_config_values()` on an uninitialized repository object. The commit message would explain this as aligning with existing patterns for similar config fields (e.g., "X bit and Y bit" topics).

This is a procedural clarification rather than a technical objection. Junio is not questioning the direction of the patch—he explicitly calls the change "good"—but is guiding the author on how to handle updates to a topic already in `next`. The implication is that the follow-up patch will be small, uncontroversial, and likely to graduate quickly. No broader review or edge-case discussion is prompted; the focus is strictly on the initialization guard.