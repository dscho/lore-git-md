This is a test cleanup patch, part of Matt Hunter’s now-merged 8-patch series that adds the global `fetch.followRemoteHEAD` configuration option. Patch 3/8 prepares the test suite for a later patch in the same series by ensuring a dangling-symref test does not leak its “custom-head” remote into subsequent tests. The change is minimal: a single `test_when_finished` line is added to `t5510-fetch.sh` so the remote is removed automatically after the test finishes. The patch is uncontroversial and purely mechanical, serving only to keep the test environment clean.