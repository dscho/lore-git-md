This is a technical follow-up in Patrick Steinhardt's series to eliminate global state in setup.c, part of the ongoing `the_repository` removal effort. The exchange clarifies a subtle point about memory management in the transition away from the `git_work_tree_cfg` global variable.

Patrick explains to Justin Tobler why the patch now explicitly frees `candidate->work_tree` in the `has_common` case - a change Justin had questioned. The explanation reveals the implicit behavior of the old code (which set `git_work_tree_cfg = NULL` without needing to free) versus the new direct approach that requires explicit cleanup. Patrick acknowledges the need to document this in the commit message.

This interaction demonstrates the careful attention both contributors are giving to resource management during this architectural refactoring. The discussion is narrowly focused on preserving existing semantics while removing technical debt, with no new issues raised. The patch appears sound and the series continues moving forward with this clarification.