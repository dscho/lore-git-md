This is a technical discussion thread about the `git log --follow` rename tracking improvements, specifically addressing Junio Hamano's review comments on Miklos Vajna's v2 patch.

Junio raises an important edge case regarding path tracking at fork points in the commit history, where a single commit (A) might be reached via different paths from different children (B and C). He provides a detailed example showing how renames on separate branches (path0->path1 on one branch, path0->path2 on another) should be handled when they merge later. Junio confirms that a single-path-per-commit approach should suffice for Git's current rename-following capabilities, since Git doesn't handle path splitting/merging cases.

The discussion reveals a subtle workaround in the code where the patch temporarily disables `follow_renames` when multiple paths are encountered at a commit, since the underlying diff machinery can't handle multiple paths. This maintains the existing single-path limitation while fixing the non-linear history case.

Key technical points:
- The conversation focuses on ensuring correct behavior at history fork points
- Junio confirms the slab-based single-path tracking is sufficient for Git's current capabilities
- The patch includes a safety mechanism to handle cases where pathspecs might temporarily diverge
- No fundamental flaws are identified in the approach, though the workaround for multiple paths is noted as subtle

This appears to be final polishing discussion before the patch can be merged, with Junio verifying the solution handles all expected cases correctly. The thread shows the maintainer's careful attention to edge cases even in what appears to be a well-constructed patch.