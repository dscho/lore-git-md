This is the first patch in Toon Claes’s v2 series introducing a `--linearize` option to `git replay`. The patch refactors the internal `enum replay_mode` into a single boolean `reverse` flag, removing all BUG checks for invalid enum values and simplifying the code.

Justin Tobler raises a reasonable question about future extensibility: if another replay mode is added later, this change would likely need to be reverted. Toon does not address this concern in the thread, but the patch itself is purely mechanical and correct—it changes no behavior, only the internal representation of the mode. The refactoring is a clean preparation for the `--linearize` feature in patch 3, and the diff shows the change is straightforward.

This is a refactoring patch with no user-visible impact. It looks well-crafted and uncontroversial, though the long-term maintainability question Justin raises is worth keeping in mind if the replay machinery grows more complex. For now, it’s a solid foundation for the rest of the series.