Performance optimization for `git describe` that limits ref iteration to tags by default. This is the final review response for Tamir Duberstein's v3 patch, which has already addressed all technical feedback and is now just awaiting merge.

Jeff King (Peff) gives his final sign-off, noting the patch looks good to go. He expresses some mild curiosity about the performance numbers - the 120k-ref test case shows a more dramatic speedup (171ms to 9.9ms) than his own testing reveals (around 10ms for 100k refs). He speculates this might be due to differences in packing or filesystem performance, but ultimately concludes the optimization is clearly beneficial regardless.

The key technical point remains unchanged from previous iterations: by default, `git describe` now only iterates through tag refs rather than all refs, providing significant speedups in repositories with many non-tag references. The v3 changes focused on test improvements and commit message refinements rather than altering the core optimization.

This marks the conclusion of the technical review cycle for this optimization. The remaining discussion about AI-assisted contributions (which this thread initially sparked) has been split to a separate thread, leaving this patch ready for integration.