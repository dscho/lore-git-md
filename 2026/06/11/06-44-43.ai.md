This is patch 5/7 in Patrick Steinhardt's v2 series refactoring global state in setup.c, part of the ongoing `the_repository` removal effort. The patch splits the dual-purpose `is_bare_repository_cfg` global variable into two distinct concerns: `startup_info->force_bare_repository` tracks whether the user passed `--bare`, while `repository->bare_cfg` tracks the actual bare state of the repository.

The change clarifies the previously confusing single-variable approach that mixed command-line intent with repository state. The patch updates 9 files with mechanical but precise changes, including moving the bare state resolution to `apply_repository_format()` and adding documentation for the new fields. This enables future patches to make `is_bare_repository()` independent of global state.

The changes are well-contained and maintain existing behavior while improving the architecture. As with the rest of the series, this is a careful refactoring that touches core repository handling code but doesn't introduce functional changes. The patch follows the established approach of methodically eliminating global state dependencies in setup.c.