Here’s the human-readable summary for a developer catching up on the thread:

This thread wraps up a long-standing limitation in `git log --follow`, which previously failed to track renames through merge commits. Miklos Vajna’s patch series (now at v4) fixes the issue by replacing a problematic global variable with per-commit pathspec tracking, ensuring rename detection works even in non-linear history like subtree merges. The solution is backward-compatible, maintains Git’s single-path constraint, and includes thorough test coverage for edge cases.

The series has been fully reviewed and approved, with Junio Hamano (the maintainer) already resolving a minor merge conflict in his integration branch. No further changes are needed—the patches are queued for the next release. Key changes live in `revision.c`, `log-tree.c`, and `diff.c`, and the documentation has been updated to remove the outdated caveat about `--follow`’s limitations. This is a clean, well-scoped fix that addresses a documented pain point without expanding Git’s existing path-tracking capabilities.