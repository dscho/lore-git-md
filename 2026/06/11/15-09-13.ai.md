This is a review comment on the first patch of Toon Claes's `git replay --linearize` series. Justin Tobler provides a substantive review, questioning the long-term maintainability of converting the `enum replay_mode` to a boolean `reverse` flag.

Justin raises a key design consideration: while the current implementation only needs two modes (pick and revert), future extensions might require additional modes, making the boolean simplification premature. He notes that this change would need to be reverted if more modes are added later. The review is technically precise, pointing to the specific tradeoff between current simplicity and future extensibility.

The review acknowledges the patch is "trivially correct" in its current form, showing Justin has verified the mechanical correctness of the code. His focus on the architectural implications demonstrates engagement with the immediate change while considering the subsystem's evolution. This is characteristic of Justin's reviews, which often examine lower-level object storage and long-term design impacts.

The comment doesn't require immediate action from the author but provides valuable perspective for the series' maintainability. It suggests the boolean simplification, while clean for now, might not be the most future-proof approach if `git replay` gains additional modes beyond pick/revert/linearize. The feedback is constructive and grounded in Git's development practices where enum-based approaches are commonly used for mode selection in commands.