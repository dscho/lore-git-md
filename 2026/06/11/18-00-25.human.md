This is a brief review from Junio C Hamano on patch 6/10 of Patrick Steinhardt's `git history drop` series, which modernizes Git's reset machinery. Junio spots a potential issue where the new `RESET_WORKING_TREE_UPDATE_HEAD` flag might cause reference updates to occur during dry-run operations, contrary to expectations.

The concern is technical and specific: when both `refs_only` and `update_head` are set, the code will call `update_refs()` regardless of the `dry_run` flag, and `update_refs()` doesn't appear to check for dry-run mode. This could lead to unintended reference updates during what should be a read-only operation.

This is a substantive review point that identifies a concrete behavioral inconsistency in the proposed changes. Junio's comment suggests the dry-run functionality - an important safety feature - might be compromised in certain cases. The observation will likely prompt Patrick to either adjust the logic to respect dry-run mode consistently or clarify why the current behavior is intentional.

The exchange shows the maintainer engaging with the technical details of an otherwise mechanical API modernization, ensuring edge cases are properly handled before the changes are merged.