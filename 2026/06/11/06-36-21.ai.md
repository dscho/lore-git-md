This is a technical follow-up in Patrick Steinhardt's series to eliminate global state in setup.c, part of the `the_repository` removal effort. The exchange clarifies a subtle memory management detail in patch 3/7 regarding the removal of the `git_work_tree_cfg` global variable.

Patrick explains to Justin Tobler why the patch now explicitly frees `candidate->work_tree` in the `has_common` case - a change Justin had questioned in his review. The explanation reveals that while the old code implicitly set `git_work_tree_cfg = NULL` in this path, the new approach requires explicit cleanup of `candidate->work_tree` to maintain equivalent behavior without leaking memory. Patrick acknowledges this warrants an addition to the commit message to document the rationale.

This interaction demonstrates the careful attention both contributors are giving to resource management during this architectural refactoring. The clarification addresses Justin's review question while confirming the correctness of the change, keeping the series on track toward merging.