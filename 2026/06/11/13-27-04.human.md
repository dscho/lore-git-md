This is the first patch in Patrick Steinhardt's v5 series introducing the `git history drop` command. It's a preparatory refactoring that splits out index handling logic from `repo_read_index_unmerged()` into a new standalone function `index_state_unmerged_to_stage0()`.

The change makes the unmerged-to-stage-0 conversion reusable for any index state, not just the main repository index. This will be needed later in the series for the drop operation's index handling. The patch also fixes a signed/unsigned mismatch in the index iteration.

This is a straightforward mechanical change that follows Git's typical refactoring pattern - extracting functionality into a reusable helper while maintaining the same behavior. The new function is immediately used by the existing `repo_read_index_unmerged()` to keep everything working as before. The patch is small (12 lines changed) and focused, with a clear commit message explaining both the "what" and the "why".

As the first patch in a well-reviewed series that has already gone through several iterations, this change appears ready for inclusion. It lays groundwork for the more substantive `git history drop` functionality that follows in later patches.