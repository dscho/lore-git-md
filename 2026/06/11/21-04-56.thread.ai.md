Thread identity: Test failure report for HTTP authentication test t5563.18 in Git 2.55.0-rc0, involving curl behavior changes between Fedora 44 (working) and Fedora 45 (failing), now identified as a known curl regression (curl/curl#21943).

Core problem: Test t5563.18 ("http.emptyAuth=auto attempts Negotiate before credential_fill") fails due to a curl behavior change where `--anyauth -u :` no longer attempts Negotiate authentication. This was introduced in curl commit 8f71d0fde515 ("creds: hold credentials") and affects Git's test expectations of three HTTP 401 responses (now receiving two).

Key technical details: The regression is confirmed in curl 8.21.0-rc2. A workaround exists (using non-empty username with `-u`) but requires validation against other auth schemes. The httpd version remains unchanged between test environments. The test was added in Git commit 9b1630b972 to verify `http.emptyAuth=auto` behavior.

Current status: The root cause is identified as a curl regression. The reporter has linked the issue to upstream curl tracking and provided technical details of the behavior change.

Open questions: Whether curl maintainers will treat this as a bug to fix or intended behavior. If the latter, Git will need to adapt either the test expectations or `http.emptyAuth` implementation. The reporter is monitoring the curl issue before proposing Git-side changes.

Participant positions: The reporter has progressed from initial observation to root cause analysis, identifying the test failure as stemming from a specific upstream regression. No maintainer responses yet regarding Git-side adaptation strategy. The next steps depend on curl's resolution of their issue #21943.

Related work: The test was added in commit 9b1630b972; the curl regression is tracked in curl/curl#21943 and stems from commit 8f71d0fde515. The workaround using non-empty -u needs cross-scheme validation before being proposed for Git.