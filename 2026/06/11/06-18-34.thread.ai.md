Thread identity: Patch series addressing `git log --follow` limitations with non-linear history, specifically improving rename tracking across merge commits. Originating author not specified in this summary.

Core problem: Current `--follow` implementation fails to properly track renames through merge commits due to using a single global variable (`opt->diffopt.pathspec`) that gets overwritten when traversing different parent paths. This particularly affects subtree merges where the rename history may diverge across branches.

Current status: v2 of the patch, incorporating Junio's feedback on commit message wording and adding handling for pathspec convergence cases. The solution introduces a commit-to-paths mapping (`follow_pathspec_slab`) to track paths per commit parent.

Key technical details:
- New `follow_pathspec_slab` structure stores per-commit pathspec information
- Modified traversal logic propagates pathspecs to parent commits
- Maintains single-path command line restriction but handles renames through multiple branches
- Touches revision.c and diff.c for the core logic changes
- Adds three test cases in t4015 demonstrating:
  * Subtree merge rename tracking
  * Renames from both merge sides
  * Diverged paths converging in common ancestors

Open questions: None raised in this iteration - appears ready for merging pending final review.

Participant positions:
- Junio provided feedback on v1 (commit message wording and edge case handling), which has been addressed in v2
- No other reviewers mentioned in this summary

Related work: Builds on long-standing `--follow` implementation, addressing known limitations documented in the man pages. The solution maintains backward compatibility while fixing the non-linear history case.