Runtime configuration proposal for timestamp precision. brian m. carlson builds on Junio C Hamano and Jeff King's discussion about nanosecond timestamp support (USE_NSEC) by suggesting runtime controls similar to untracked-cache. The proposal offers two approaches: explicit user configuration via `git update-index` options (like `--use-nsec`) and automated detection through system calls (`uname` and `statfs`) when requested with `--test-use-nsec`.

This positions between Jeff King's runtime-configurable vision and Junio's concerns about auto-probing complexity. The suggestion acknowledges platform-specific knowledge (btrfs on Linux as a known-safe case) while providing escape hatches for users to manually enable optimizations. The technical specifics focus on index operations rather than build-time decisions, keeping with the thread's shift toward operational solutions for the worktree diff slowdowns.

The proposal introduces concrete implementation hooks while respecting the maintainer's caution about automatic detection. It suggests Git could leverage existing configuration patterns (like untracked-cache controls) to address the timestamp precision performance issues without requiring invasive filesystem probing. This moves the discussion toward actionable next steps while maintaining compatibility with the architectural direction established earlier in the thread.