Performance optimization for `git ls-files` with pathspec filtering. This email concludes the technical discussion around Tamir Duberstein's patch to optimize `--deleted` and `--modified` flags by applying pathspec filtering before `lstat()` calls. Jeff King (Peff) had initially questioned the arbitrary single-pathspec optimization boundary but now accepts either keeping that approach or removing the restriction entirely, noting the real solution would be improving pathspec matching itself.

The exchange represents final consensus on the patch series, with no remaining technical objections. Tamir indicates he may investigate broader pathspec improvements in the future but considers this series complete as-is. The only outstanding item is Junio Hamano's request for proper email threading in the final submission. The optimization delivers dramatic performance wins (60x speedup in tested cases) by avoiding unnecessary filesystem operations when filtering with pathspecs.