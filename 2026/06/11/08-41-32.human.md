Performance optimization follow-up discussion for `git ls-files` pathspec filtering.

Jeff King (Peff) responds to Tamir Duberstein's justification for the current single-pathspec optimization boundary in the `git ls-files --deleted`/`--modified` performance patch. While acknowledging the arbitrariness of the current approach, Peff suggests removing the pathspec.nr check entirely and always applying the optimization, despite a minor theoretical regression in an artificial test case.

The discussion highlights Git's pragmatic approach to performance optimizations - the current patch provides clear wins for common cases while leaving room for future improvements. Peff notes the real underlying issue is the pathspec matching performance itself, suggesting that would be a more fundamental fix than tweaking thresholds in this specific caller.

This exchange represents the final technical discussion on the patch, with both parties satisfied the current implementation is correct and ready for merging. The only remaining action is Tamir's resubmission with proper email threading as requested by Junio Hamano. The significant performance improvements (60x in tested cases) remain uncontested.