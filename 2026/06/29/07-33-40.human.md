This is a brief follow-up in the `git history drop` series, specifically addressing Christian Couder’s review comment about code duplication in patch 9/10. Patrick Steinhardt acknowledges the duplication between the new `compute_pending_ref_updates()` function in `builtin/history.c` and the existing `replay_result_queue_update()` in `replay.c`. He notes that the latter is currently internal to `replay.c` but suggests exposing it as a shared helper might be worthwhile. The tone implies he plans to refactor this in a future iteration rather than leaving the duplication in place.

This is a small but meaningful step toward resolving the last open technical concern before the series can graduate. The change itself is straightforward—moving or exposing a helper function—but it touches on maintainability, a recurring theme in Git development. The series as a whole remains well-motivated: it implements the `git history drop` subcommand, modernizes the reset API, advances the `the_repository` removal effort, and improves reference update architecture. All other review feedback has been addressed, and the series is otherwise complete and approved. This follow-up suggests the author is treating the duplication seriously, which should reassure reviewers that the final version will be clean.