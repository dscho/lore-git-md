This is a **substantive review follow-up** in the `paint_down_to_common()` optimization series, closing the last open technical question. Derrick Stolee **fully endorses Kristofer Karlsson’s proposal to remove the commit-date fallback entirely**, confirming that the side-exhaustion optimization renders the 2018 workaround (commit 091f4cf3) obsolete. The benchmarks Kristofer provided—showing that `git merge-base --all v4.8 v4.9` on the Linux kernel now performs equally well with v1 and v2 commit-graph data (17 ms in both cases)—were decisive.

The fix for the newly identified regression will be straightforward: **always use `compare_commits_by_gen_then_commit_date`**, eliminating the fallback and the unused `corrected_commit_dates_enabled()` function. Stolee suggests adding a **comment at queue initialization** to document the requirement for topological ordering, but this is a minor documentation touch, not a blocker. The change will be incorporated into a **follow-up patch** before Git 2.55 final, likely requiring a **v5 reroll** to squash it with the mechanical fix for `-Wunused-function` warnings.

With this approval, the series is now **technically complete and ready to merge** once the two mechanical adjustments (squash and follow-up) are applied. The only remaining work is procedural: a final reroll to address build hygiene and the regression fix, both of which are uncontroversial and fully reviewed.