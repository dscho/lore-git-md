This is a short clarification request in the ongoing `git history squash` series. Harald Nordgren asks Patrick Steinhardt whether the planned rejection of merge commits should extend even to "simple merges" (those whose parents are all within the squashed range), or only to merges with external parents. The current implementation already handles simple merges, and Harald wants to know if he should remove that capability.

The thread has already settled that merges with external parents will be rejected by default, with a possible future `--allow-external-parents` flag. This question is about the narrower case of merges whose parents are entirely within the range — a scenario the code already supports. If Patrick’s answer is "yes, remove it," it would mean the feature becomes more restrictive than the current patch set, potentially breaking existing test cases. If the answer is "no, keep it," the current behavior stays, and only external-parent merges are rejected.

The tone is neutral and technical; Harald is simply seeking confirmation before finalizing v6. The answer will likely be a one-line reply, but it could affect the scope of the feature’s merge-commit handling.