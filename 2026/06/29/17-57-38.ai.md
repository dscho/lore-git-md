This is the fifth patch in Patrick Steinhardt’s six-patch series refactoring `struct object_info` to replace the `whence` field with a more flexible `source` field, part of the ongoing ODB abstraction effort. The patch removes the now-unused `whence` field from `struct object_info` and all its assignments across the ODB backends (`odb.h`, `odb.c`, `odb/source-inmemory.c`, `odb/source-loose.c`, `packfile.c`). The change is mechanical and uncontroversial—every line setting `oi->whence` is deleted, and the enum definition is dropped—with no behavior changes.

Justin Tobler’s review is **surface-level**, confirming the patch "looks good" without engaging with deeper technical implications. This aligns with the patch’s nature as a clean-up step following the mechanical conversion of callers in patch 4/6. The series as a whole has Junio C Hamano’s conceptual approval ("Great"), and this patch should be ready to merge once the rest of the series is reviewed.

Key technical context for follow-up discussion: the `whence` field was replaced by `oi->sourcep->source->type` in patch 4/6, and this patch simply removes the dead code. The series targets `master` and depends on `ps/odb-source-packed`. No new symbols or files are introduced here; the focus is on tidying up after the refactor. If earlier patches hold up, this one is a straightforward merge.