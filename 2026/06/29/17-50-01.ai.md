This is a maintainer query about integration details for the two-patch bugfix series that fixes a clock-skew regression in `git merge-base`. The series itself is already well-motivated and technically sound: it gates an early-exit optimization in `paint_down_to_common()` on whether the queue is generation-ordered, and includes a thorough test in `t6600-test-reach.sh`.

Junio’s question is narrow but important: where the new `gen_ordered` flag should live once the series is merged alongside the in-flight `kk/merge-base-exhaustion` topic. He suggests two possibilities: adding it to the existing `paint_state` struct (which `kk/merge-base-exhaustion` already touches) or keeping it as a local on-stack variable inside `paint_down_to_common()`. The question is purely about code organization and has no bearing on the correctness of the fix itself.

No answer is provided in this email, so the thread is now waiting on the author to clarify the preferred approach. The choice is unlikely to be controversial, but it will need to be resolved before the series can graduate to `next`.