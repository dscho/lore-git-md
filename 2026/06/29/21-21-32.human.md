This is the maintainer’s closing note on the `greplint.pl` series, now focused on resolving the latent test bug in `t3420-rebase-autostash.sh`. Junio confirms that Gábor Szeder’s 2021 fix—replacing the flawed `! grep dirty file3` assertion with `test_path_is_missing file3`—has been merged to `next`, addressing the root cause of the bug. The linter’s `# lint-ok:` exemption, which preserved the incorrect test logic, is no longer needed for this case.

Junio suggests a final small reroll (v3) of the series to remove the exemption for this line and audit any remaining `# lint-ok:` comments for similar issues. The tone is pragmatic: the tool’s automation goals are accepted, but only after ensuring it doesn’t mask pre-existing bugs. The series is now on a clear path to completion, pending minor cleanup. For readers tracking test infrastructure improvements, this marks the end of a long debugging detour and a return to the original goal of standardizing `grep` assertions. The outcome reinforces the project’s preference for fixing bugs at the source over workarounds, even in tooling.