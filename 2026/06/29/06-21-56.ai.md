This is a brief follow-up in the reftable memory-leak thread. Patrick Steinhardt replies to Jeff King’s already-merged patch that moved the `reftable_calloc` allocation after the block-size check in `reftable_writer_new`. Patrick notes that the fix is logical and idiomatic, but he briefly considers (and dismisses) an alternative: a common error-exit path that would free the struct on any early return. The thread is now closed; no further action is expected. Key technical detail for future reference: the function has one other early return, but it already frees the writer before returning, so the leak was confined to the single case Peff fixed.