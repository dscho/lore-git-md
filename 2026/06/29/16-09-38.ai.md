This is the **sixth iteration of a feature patch series** introducing `` `git history squash` ``, a new subcommand that folds a commit range into its oldest commit while preserving descendant history. The series is now **code-complete and ready for final review**, with all prior feedback addressed—including input validation, merge commit handling, ref-handling design, and usability improvements to the ``--reedit-message`` template.

Harald Nordgren’s reply here is a **clarification request** about merge commit handling. In the parent email, Patrick Steinhardt suggested punting on merge commits entirely ("a can of worms"), but Harald notes that the current implementation *does* handle "simple merges" (those whose parents are all within the squashed range). His question is whether Patrick’s comment means even these should be rejected by default, or only merges with external parents (parents outside the range). The thread summary confirms that the final design rejects merges with external parents to avoid ambiguity, while allowing fully contained merges—so Harald’s current behavior aligns with the consensus. This reply is a narrow technical check to ensure the implementation matches the agreed design before posting v6.

The series touches ``builtin/history.c``, ``advice.c``/``.h``, and the test suite, with new helpers like ``resolve_squash_range()`` and ``build_squash_message()``. Key behaviors include rejecting operations with refs pointing to interior commits (with advice to use ``--update-refs=head``), supporting multiple revision arguments (e.g., ``HEAD~3.. ^topic``), and omitting ``fixup!``/``amend!`` noise from the ``--reedit-message`` template. The only remaining open questions are minor usability gaps (e.g., scripting flexibility for revision syntax), which do not block the series. Junio’s final review is the next step.