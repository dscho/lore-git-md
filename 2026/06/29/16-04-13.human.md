This is a brief follow-up in the ongoing bugfix thread for `git history --reword`. Patrick Steinhardt acknowledges Junio C Hamano’s plan to send a v2 that restores error checking on the `fwrite()` call in `fill_commit_message()`. He clarifies that even if the broader error-handling gap in `wt_status_print()` remains, the v2 patch will at least ensure the commit message itself is either fully written or the command dies—preventing silent loss of the user’s intended message. The original issues (file stream leak and inefficient file handling) are now uncontested, and the only remaining action is Junio’s v2 with the restored error check. No other reviewers have weighed in, and the patch is effectively ready to proceed once the v2 is sent.