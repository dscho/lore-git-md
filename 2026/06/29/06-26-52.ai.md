This is a **substantive review reply** in the `git history squash` thread, clarifying design decisions and addressing Phillip Wood’s usability concerns. Patrick Steinhardt responds to four key questions raised in the parent review, offering technical rationale and project conventions to guide the feature’s finalization.

Patrick agrees that **merge commits with external parents** should be rejected by default, calling them "a can of worms" and suggesting a future `--allow-external-parents` flag if demand arises. This aligns with the thread’s earlier consensus to avoid ambiguity in the initial implementation. He also endorses documenting **branch ref handling**, which the thread summary confirms is now resolved: the command rejects operations where refs point to commits inside the squashed range, advising users to use `--update-refs[=head]`.

On **input validation**, Patrick defends the current behavior of accepting non-ancestor ranges, noting that other `git history` subcommands (like `replay`) already allow rewriting arbitrary branches. This suggests the feature’s scope is intentionally broader than just the current branch, though Phillip’s concern about single-commit ranges (`@^!`) remains unaddressed—Harald may need to add explicit validation for that case. The **single-argument limitation** is also defended as consistent with existing subcommands, though Phillip’s example (`^:/base :/tip`) highlights a usability gap that might warrant future enhancement.

The reply does not directly answer Phillip’s questions about **`fixup!`/`squash!`/`amend!` commits** or **commit message rewording mechanisms**, leaving those for Harald to clarify in the next revision. However, the thread summary confirms that `--reedit-message` is the sole rewording mechanism, and `fixup!`/`squash!` commits are folded without special handling.

This exchange advances the thread by resolving the merge-commit and ref-handling questions, but leaves input validation and scripting flexibility as open points. The tone is collaborative, with Patrick’s responses grounded in Git’s existing patterns rather than personal preference. The next steps will likely involve Harald incorporating these clarifications into the v6 series, which the thread summary describes as otherwise complete.