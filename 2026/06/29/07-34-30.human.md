This is part four of Patrick Steinhardt’s eleven-patch v7 series implementing the `git history drop` subcommand. The patch modernizes the flags passed to `reset_working_tree()` by converting them from `#define` macros to an `enum reset_working_tree_flags` and updating the flag names to match the function’s new name (previously `reset_head()`).

The change is purely mechanical: every `RESET_HEAD_*` flag is renamed to `RESET_WORKING_TREE_*` and the `flags` field in `struct reset_working_tree_options` is now typed as the new enum instead of an unsigned integer. The patch updates all call sites in `builtin/rebase.c` and `sequencer.c` to use the new names, and the internal logic in `reset.c` is adjusted accordingly. No behavior changes are introduced—this is a preparatory refactoring that keeps the reset machinery’s functionality identical while making the API more idiomatic and self-documenting.

The patch is well-scoped and clearly motivated: the commit message explains the shift from macros to enums as a modernization step, and the rename aligns the flag names with the function’s new purpose after the earlier rename from `reset_head()` to `reset_working_tree()`. The diff is clean and the changes are confined to the reset subsystem, making this a straightforward, uncontroversial step in the larger series. Given that the thread summary indicates all substantive review feedback has been addressed, this patch looks ready to merge.