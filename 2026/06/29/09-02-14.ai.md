This is the first patch in Patrick Steinhardt’s **v2 security-hardening series for the reftable backend**, introducing Meson build support for libFuzzer. The patch adds a new `-Dfuzzers_link_args=` build option to pass linker arguments (e.g., `-fsanitize=fuzzer`) specifically for fuzzing executables, plugging a gap in Meson’s existing `-Db_sanitize=` option which only handles compiler flags. The documentation in `meson.build` now includes a clear setup guide for users, demonstrating how to combine `-Db_sanitize=address,fuzzer-no-link` with the new option to enable coverage-guided fuzzing.

The change is minimal and targeted: it modifies only `meson.build`, `meson_options.txt`, and `oss-fuzz/meson.build`, adding 18 lines of code and documentation. No existing functionality is altered, and the new option is inert unless explicitly configured. This patch is purely infrastructure, enabling the fuzzer introduced in patch 2/12 to link against libFuzzer. The commit message is concise and actionable, explaining the motivation (libFuzzer’s linker requirements) and the solution (a dedicated build option).

For reviewers, the key technical detail is the separation of compiler and linker flags: `-Db_sanitize=` handles sanitizers like ASan, while `-Dfuzzers_link_args=` handles libFuzzer’s runtime. The documentation snippet provides a ready-to-use example, lowering the barrier for contributors who want to run fuzzers locally. This patch is uncontroversial and appears ready for integration, assuming no objections to the Meson-specific approach. It lays the groundwork for the reftable fuzzer and may be reused for fuzzing other Git subsystems in the future.