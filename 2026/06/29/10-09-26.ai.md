This is a **mechanical follow-up** to the `paint_down_to_common()` optimization series (v4 6/8). Kristofer Karlsson acknowledges Gábor SZEDER’s observation that the two static functions removed in this patch—`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`—became unused in the *previous* patch (v4 5/8), breaking the build with `DEVELOPER=1` due to `-Wunused-function` warnings. He agrees to squash the two patches in v5 to avoid the intermediate broken state, prioritizing build hygiene over commit granularity.

The issue is purely about patch organization; there are no technical objections to the removal itself. The fix is uncontroversial and resolves the last mechanical blocker before the series can be merged. The squash will not affect the series’ correctness or performance claims, which remain fully reviewed and regression-free.