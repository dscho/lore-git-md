This is the **final approval and procedural wrap-up** for the `paint_down_to_common()` optimization series. Kristofer Karlsson and Derrick Stolee confirm the last technical hurdle—removing the 2018 commit-date fallback—is resolved. The plan for **v5** is now purely mechanical:

1. **Squash patches 5/8 and 6/8** to avoid `-Wunused-function` warnings (per SZEDER Gábor’s feedback).
2. **Remove the commit-date fallback entirely** in a follow-up patch, with a comment at queue initialization documenting the topological ordering requirement (per Stolee’s suggestion).
3. **Update documentation** to clarify that infinite-to-finite generation transitions do not always hold.
4. **Add a test** (or expand existing ones) to cover the regression scenario.

The series is **technically complete and ready to merge** once these uncontroversial adjustments are made. No further design discussion is needed; the remaining work is procedural. The benchmarks Kristofer provided (17 ms for `git merge-base --all v4.8 v4.9` on both v1 and v2 commit-graph data) decisively demonstrate the side-exhaustion optimization renders the fallback obsolete. The follow-up patch will delete the unused `corrected_commit_dates_enabled()` function and the fallback logic, simplifying the codebase.