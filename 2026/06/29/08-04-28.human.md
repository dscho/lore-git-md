This is a **substantive design discussion** in the ongoing `git replay --linearize` thread, specifically Patrick Steinhardt’s reply to Toon Claes’s proposal for future CLI options. The email pushes back on Toon’s suggestion to invent new flags (`--no-merges`, `--replay-cousins`) rather than reusing `git rebase`’s existing `--rebase-merges=<mode>` syntax.

Patrick argues for **interface consistency** with `git rebase`, noting that `git rebase` already supports three modes (`--no-rebase-merges`, `--rebase-merges`, `--rebase-merges=rebase-cousins`) and that `git replay` should mirror this design unless there’s a compelling reason not to. He isn’t demanding immediate implementation of all modes, but wants the CLI to be **future-proof** and **documented**—if the project deviates from `git rebase`’s interface, the rationale should be explicit.

The email is **not a review of code** but a **design critique**, and it carries weight: Patrick is a prolific contributor with deep expertise in Git’s ref and object subsystems. His concern is architectural—avoiding a fragmented user experience where similar commands (`rebase` vs. `replay`) behave differently without justification. The tone is collaborative but firm: he’s asking for either alignment with `git rebase` or a clear explanation of why divergence is necessary.

For a busy reader, this is a **signal to watch**: if the series merges with `--linearize` as a standalone flag, future patches may need to reconcile this with `git rebase`’s established interface, potentially requiring breaking changes or deprecation cycles. The discussion is still open, but Patrick’s position is a strong counterpoint to Toon’s preference for separate flags.