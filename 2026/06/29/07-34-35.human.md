This is the ninth patch in Patrick Steinhardt’s 11-patch v7 series implementing the `git history drop` subcommand. The patch exposes the static helper `replay_result_queue_update()` so it can be reused by the upcoming `git history drop` implementation. This directly addresses Christian Couder’s earlier concern about code duplication between the replay and history code paths.

The change itself is minimal: four lines moved from `replay.c` to `replay.h` and the function made non-static. No behavior changes or new functionality are introduced. The patch is clean, well-motivated, and clearly part of a larger, already-reviewed effort. Given that the series as a whole is ready for final review and this patch merely enables reuse of existing logic, it looks like a straightforward and uncontroversial step toward completing the feature.