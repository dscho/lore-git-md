This is the ninth patch in Patrick Steinhardt’s 11-patch v7 series implementing `git history drop`. It exposes the static helper `replay_result_queue_update()` in `replay.h` so that `builtin/history.c` can reuse it in the next patch. The change is mechanical: the function is moved from `replay.c` to the header, its signature unchanged, and its body lifted verbatim. Christian Couder suggested this refactoring in an earlier review to eliminate code duplication between the replay and history code paths; this patch delivers that cleanup. No behavior changes or new functionality are introduced. The patch is small (9 lines added, 4 removed), self-contained, and ready for final review.