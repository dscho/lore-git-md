This is a **substantive review** from Junio C Hamano on the final patch of Patrick Steinhardt’s `git history drop` series. Junio identifies a **logical flaw** in the `find_head_tree_change()` helper: when `--update-refs=head` is used, the `compute_pending_ref_updates()` function filters out all refs except `HEAD` (via `decoration->type != DECORATION_REF_HEAD`), but `find_head_tree_change()` then looks for the *symbolic name* of the current branch (e.g., `refs/heads/master`) in the `result->updates[]` array. Because the array only contains `HEAD` (not the branch name), the lookup fails, and the function incorrectly concludes that HEAD does not move—even when it does. This would cause the `drop` command to skip necessary index/worktree updates, potentially leaving the repository in an inconsistent state.

The review is **tested-level**: Junio traces the code path across multiple functions (`compute_pending_ref_updates`, `find_head_tree_change`, `cmd_history_drop`) and identifies the exact mismatch in ref resolution logic. He also clarifies the behavior of `refs_resolve_ref_unsafe()` in detached HEAD cases, noting that the current code overwrites `head_target` unnecessarily. The issue is **high-impact**—it affects core functionality (index/worktree synchronization) and could lead to silent failures in edge cases (e.g., detached HEAD or branch updates). No fix is proposed in the email, but the analysis is precise enough to guide a targeted correction.