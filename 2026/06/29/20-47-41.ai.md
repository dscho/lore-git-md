This is Junio C Hamano's reply in the ongoing ODB refactoring thread, engaging with Justin Tobler's **substantive review** of patch 3/6. The email focuses on the architectural design of the new `source` field in `struct object_info_source`, which Patrick Steinhardt introduced to enable multi-source object resolution.

Junio echoes Justin's earlier question from patch 1/6: if the `source` parameter to `packed_object_info_with_index_pos()` is frequently `NULL`, why include it at all? He suggests the design might be overcomplicating the API by making source tracking opt-in, when the ODB source could instead be recorded once during `struct packed_git` initialization and then referenced statically. This would eliminate the need for callers to pass an optional `source` parameter and simplify the logic in backends. Junio does not propose an alternative implementation, but his phrasing ("what is it good for") signals skepticism about the current approach.

The email also highlights confusion around naming and data flow. Junio misreads the code snippet, assuming `oi->sourcep->source` should be `oi->sourcep->base` (it is not; the field is correctly named `source`). This suggests the patch's intent is not immediately clear, even to the maintainer. The underlying issue is whether the `source` field should be populated by the caller (current design) or by the backend during initialization (Justin and Junio's implied preference). No resolution is offered, but the question is now explicitly on the table for Patrick to address in a future revision.

This reply does not block the patch—Junio's earlier "Great" comment indicates conceptual approval—but it does raise a **medium-weight architectural concern** that could shape the series' final design. The discussion remains open, with no clear consensus yet on how to balance flexibility (opt-in source tracking) against simplicity (centralized source recording). Future replies will likely revisit this tension.