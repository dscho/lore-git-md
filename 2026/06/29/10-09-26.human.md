This is a minor follow-up in the **`paint_down_to_common()` optimization series** (v4, 8 patches). The series is otherwise fully reviewed and ready to merge, but patch 6/8—a dead-code removal—needs to be squashed with the preceding patch (5/8) to avoid `-Wunused-function` warnings when built with `DEVELOPER=1`.

Kristofer Karlsson (a co-author of the series) acknowledges Gábor SZEDER’s feedback and confirms the fix: the two unused wrapper functions (`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`) will be removed in the same commit that made them obsolete. This is a purely mechanical adjustment—no technical changes, just patch organization. The author notes that while this makes the commit slightly less clean, it’s the right trade-off to keep every intermediate state buildable.

The series as a whole is a **well-motivated performance optimization** targeting asymmetric merge-base queries (e.g., repositories with import grafts or shallow histories). It delivers **100-1000x speedups** in pathological cases, fixes a regression introduced in v3, and includes thorough test coverage, trace2 instrumentation, and a new technical document explaining the algorithm. All substantive feedback has been addressed, and the only remaining action is this squash. If you care about merge-base performance or the commit-reach subsystem, this is worth tracking—but the fix itself is trivial.