This is a **brief follow-up in the `git history drop` series**, specifically addressing Christian Couder’s review comment about code duplication between `compute_pending_ref_updates()` (in `builtin/history.c`) and `replay_result_queue_update()` (in `replay.c`).

Patrick acknowledges the duplication and proposes exposing `replay_result_queue_update()` as a shared helper rather than reinventing the logic. The response is concise and action-oriented: he signals intent to refactor the code to eliminate the redundancy, which would resolve the maintainability concern flagged in the review. No new technical details or objections are introduced; this is purely a procedural step toward cleaning up the implementation before the series lands.

The patch remains part of the broader effort to enable commit-dropping via `git history drop`, modernize the reset API, and advance `the_repository` removal. The resolution of this duplication will likely appear in a future iteration (v7 or later) of the series.