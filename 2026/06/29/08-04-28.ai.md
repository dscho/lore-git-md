This is a **substantive review reply** in the `git replay --linearize` thread, focusing on the **command-line interface design** for merge-handling options. Patrick Steinhardt pushes back on Toon Claes’s earlier response, arguing for **consistency with `git rebase`’s `--rebase-merges` syntax** rather than inventing new flags like `--linearize`, `--no-merges`, or `--replay-cousins`.

Patrick’s core point: since `git replay` and `git rebase` both manipulate commit history, they should share the same CLI vocabulary (`--rebase-merges`, `--no-rebase-merges`, `--rebase-merges=rebase-cousins`) to avoid user confusion. He acknowledges that supporting all modes immediately isn’t necessary, but insists the interface should be **future-proof** and **documented** if deviating from `git rebase`. His tone is measured but firm—he frames this as a **design principle** (consistency) rather than a technical blocker, though the implication is that the current `--linearize` approach risks painting the project into a corner.

Key technical symbols referenced:
- CLI options: `--rebase-merges`, `--no-rebase-merges`, `--rebase-merges=rebase-cousins`, `--linearize`, `--no-merges`, `--replay-cousins`.
- Commands: `git replay`, `git rebase`, `git history`.
- Related work: Johannes Schindelin’s merge-replay series (default topology-preserving behavior).

The email advances the thread by **shifting the debate from implementation details to interface philosophy**. While Toon’s earlier response treated `--linearize` as a reasonable MVP, Patrick’s reply elevates the discussion to a **project-wide consistency question**, which may require maintainer input (Junio) to resolve. The outcome could influence whether future `git replay` options adopt `git rebase`’s syntax or chart a separate path. No new patches are proposed here, but this exchange is likely to shape the **follow-up work** addressing the post-merge regression in v5.