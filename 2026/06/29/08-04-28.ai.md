This is a substantive review reply in the ongoing `git replay --linearize` series (v5). Patrick Steinhardt responds to Toon Claes’s earlier design exploration about whether `--linearize` should mirror `git rebase`'s three-mode interface (`--no-rebase-merges`, `--rebase-merges`, and `--rebase-merges=rebase-cousins`).

Patrick pushes back on Toon’s incremental approach, arguing that if the eventual goal is to support all of `git rebase`'s modes, the interface should be designed consistently from the start rather than reinvented later. He proposes adopting `git rebase`'s `--rebase-merges=<mode>` syntax (with modes `no-rebase-merges`, `rebase-merges`, `abort`, and `rebase-cousins`) instead of introducing a separate `--linearize` flag, noting that this would avoid user confusion and align the two commands’ behavior. While he does not insist that all modes be implemented immediately, he emphasizes that consistency should be the default unless there is a compelling reason to deviate, and asks Toon to document any such rationale if the current interface is retained.

The email does not introduce new code or resolve the design question, but it raises the stakes: the interface choice is now framed as a potential long-term inconsistency between `git replay` and `git rebase`. This feedback is likely to require a response before the series can proceed, either by adopting the `--rebase-merges` syntax, documenting a principled deviation, or justifying the incremental approach more explicitly. The regression in `--linearize` behavior (commit-dropping when replaying a single branch containing merge commits) remains the blocking technical issue, but this interface discussion is now a second open question that could delay merging.