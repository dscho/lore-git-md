This is a **substantive technical follow-up** in the macOS CI hang thread, focused on the now-dropped third patch (`t5551: pack refs after creating many tags`). Jeff King (Peff) responds to Patrick Steinhardt’s review with a deeper exploration of why the patch was unnecessary and what alternatives exist for optimizing ref creation performance.

Peff first acknowledges the existing `REF_TRANSACTION_FLAG_INITIAL` optimization (used in clones and ref migrations) but notes its limitations: it only works in empty repositories, and extending it to tools like `git fast-import` (which runs on "almost-empty" repos) would be non-trivial. He then proposes a **hypothetical ad-hoc optimization** for the test case—writing directly to `packed-refs` without locking—which would match reftable’s ~30% speedup but is dismissed as "gross and fragile" due to ordering and backend-conditional risks.

The email also surfaces a **performance quirk in reftable**: it eagerly stats the `tables.list` file during bulk ref creation, which Peff observed via `strace` and `perf` as a non-trivial cost. While he speculates this could be optimized (e.g., by trusting the stack after locking), he concludes it’s likely not worth the effort, as reftable’s current performance (50,000 refs in 150ms) is already "good enough" compared to the files backend.

**Key technical details for future reference**:
- Files touched: `t/t5551-http-fetch-smart.sh` (hypothetical diff).
- Subsystems: ref backends (files, reftable), `git fast-import`, `git update-ref`, `git pack-refs`.
- Performance data: reftable’s ~30% speedup over files backend; 50,000 refs in 150ms (reftable) vs. ~220ms (files).
- Observed inefficiency: reftable’s repeated `fstat` calls on `tables.list` during bulk ref creation.
- Related work: `REF_TRANSACTION_FLAG_INITIAL`, reftable backend, prior HTTP/2 fixes.

The tone is **exploratory and pragmatic**, reinforcing the thread’s consensus that the third patch was redundant. Peff’s analysis provides useful context for future ref-backend optimizations but does not reopen the discussion about merging the patch. The email is **not a call for action**—it’s a post-mortem on why the patch was unnecessary and a note on reftable’s current behavior.