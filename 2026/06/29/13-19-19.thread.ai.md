Thread identity
Subject: [PATCH 0/2] commit-reach: fix clock-skew regression in merge-base
Series version: v1 (initial submission)
Originating author: (name redacted in input; placeholder retained)
Thread type: patch series (bugfix)

Core problem or goal
Fix a regression in `git merge-base` (without `--all`) introduced by 93e5b1680e (2025-04-10). The bug surfaces when all three conditions hold: (1) the repository uses a v1 commit-graph (no corrected commit dates), (2) the commit topology has clock skew, and (3) the correct merge base has a lower committer date than one of its ancestors. The series gates the early-exit optimization on whether the queue is generation-ordered and adds a regression test in `t6600-test-reach.sh`.

Current status
Agreed: the problem is real and the fix direction is correct.
Merged: not yet.
Version: v1 (initial submission).
Changes between versions: n/a.

Key technical details
Files touched: commit-reach.c (ten lines changed in patch 2 to conditionally gate the early-exit optimization), t6600-test-reach.sh (41-line test added in patch 1, now flipped to `test_expect_success`).
Subsystems: commit-reach.
New/renamed symbols: `gen_ordered` flag in `paint_down_to_common()`.
Behavior change: the early-exit optimization in `paint_down_to_common()` is now conditional on the queue being generation-ordered; the fallback to commit-date ordering (for v1 commit graphs) clears the flag, preventing incorrect early exits.
On-disk format: unchanged.
Test coverage: new test in t6600-test-reach.sh constructs a clock-skew topology and verifies the fix; the test was initially marked `test_expect_failure` and is now expected to pass after patch 2 lands.

Open questions and loose ends
Junio has asked where the new `gen_ordered` flag should live once the series is merged alongside the in-flight `kk/merge-base-exhaustion` topic: either as a field in the existing `paint_state` struct (which `kk/merge-base-exhaustion` already touches) or as a local on-stack variable inside `paint_down_to_common()`. The author has not yet responded, so the question remains open.

Participant positions
Junio C Hamano: raised the integration question; no technical objection to the fix itself.

Related work
Regression introduced by 93e5b1680e (2025-04-10). The series must be integrated with the in-flight `kk/merge-base-exhaustion` topic.