This thread is a two-patch bugfix series that addresses a clock-skew regression in `git merge-base` introduced by a commit in April 2025. The issue occurs under specific conditions: a v1 commit-graph, clock skew in the commit topology, and a merge base with a lower committer date than one of its ancestors. The fix gates an early-exit optimization in `paint_down_to_common()` on whether the queue is generation-ordered, and includes a regression test in `t6600-test-reach.sh`.

The series itself is uncontroversial and technically sound, with no objections or review feedback raised so far. However, Junio has asked a narrow but important question about code organization: whether the new `gen_ordered` flag should be added to the existing `paint_state` struct (which another in-flight topic, `kk/merge-base-exhaustion`, already touches) or kept as a local variable inside `paint_down_to_common()`. The author has not yet responded, so the thread is now waiting on their input before the series can move forward. The choice is unlikely to spark debate, but it will need to be resolved before the patches can be merged.