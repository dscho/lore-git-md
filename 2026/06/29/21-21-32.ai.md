This is a **maintainer follow-up** in the `greplint.pl` series, now at the resolution stage. Junio C Hamano confirms that Gábor Szeder’s 2021 patch (replacing `! grep dirty file3` with `test_path_is_missing file3` in `t3420-rebase-autostash.sh`) is the correct fix and has already been merged to the `next` branch. The email proposes a path forward for the series: a final reroll (v3) that omits the problematic conversion for this line and audits other `# lint-ok:` comments for similar latent bugs.

The discussion has shifted from debugging to **series cleanup**. Junio’s suggestion implies that the `# lint-ok:` mechanism—while useful for exempting edge cases—should not be used to preserve incorrect test logic. The remaining work involves:
1. **Auditing `# lint-ok:` comments**: The series introduced a handful of these exemptions; Junio recommends reviewing them to ensure none mask other bugs where files *should not exist* but are incorrectly grepped.
2. **Final reroll**: A minimal v3 that removes the exemption for `t3420` (since the fix is already in `next`) and addresses any other questionable `# lint-ok:` cases. No major tooling changes are requested—just a targeted cleanup.

**Key technical details**:
- **Files**: `t3420-rebase-autostash.sh` (the problematic test), `greplint.pl` (the linter tool).
- **Symbols**: `# lint-ok:` comments (exemptions), `test_path_is_missing` (correct assertion for missing files).
- **Related work**: Gábor’s 2021 patch (now merged) and the broader `chainlint.pl` infrastructure.

**Thread trajectory**: This email **closes the technical debate** and sets a clear path for merging. The series is now a **test infrastructure improvement** with a minor cleanup task, rather than a contentious automation tool. The maintainer’s tone suggests confidence in the outcome, provided the author addresses the remaining `# lint-ok:` audit. Expect a v3 shortly.