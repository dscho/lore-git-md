This is a maintainer query about the ongoing **bugfix series** that fixes a clock-skew regression in `git merge-base`. The series was previously summarized as well-motivated and uncontroversial, but Junio now raises a **merge conflict concern** with another in-flight topic: **kk/merge-base-exhaustion**.

Junio asks where the new `gen_ordered` flag should live—whether it belongs in the `paint_state` struct (which was recently modified by the **kk/merge-base-exhaustion** topic) or as an on-stack variable local to `paint_down_to_common()`. This is a **technical integration question**, not a review of the patch itself. The answer will determine whether the series needs a rebase to accommodate the other topic or can proceed as-is.

The question is narrow but important: if `gen_ordered` is added to `paint_state`, it will interact with the changes from **kk/merge-base-exhaustion**, which also touches that struct. If it remains on-stack, the two topics can merge cleanly without coordination. No objections to the fix itself are raised; this is purely about **code organization** and **merge safety**.

For a busy reader, this email signals that the series is **not yet ready to merge**—it needs a minor adjustment to resolve the conflict with **kk/merge-base-exhaustion**. The fix itself remains sound, but the author will likely need to send a **v2** to address Junio’s question.