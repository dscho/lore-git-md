This is the fourth patch in Patrick Steinhardt’s 11-patch `git history drop` series (v7). It modernizes the flags passed to `reset_working_tree()` by converting them from `#define` macros to an `enum reset_working_tree_flags` and updating the flag names to match the function’s new name (previously `reset_head()`). The patch touches `reset.h`, `reset.c`, `builtin/rebase.c`, and `sequencer.c`, ensuring all call sites use the new enum and flag names.

The change is purely mechanical: the enum values mirror the old `#define` values, so behavior is unchanged. The new names (`RESET_WORKING_TREE_*` instead of `RESET_HEAD_*`) reflect the function’s purpose more accurately after the rename in the previous patch. The patch also updates the `reset_working_tree_options` struct to use the new enum type for its `flags` field, improving type safety.

This is a refactoring patch that advances the series’ broader goals—modernizing the reset API and preparing for future HEAD-update-optional functionality—without introducing new behavior. The commit message is clear, and the diff is straightforward, making this a low-risk, high-hygiene change. It resolves the naming inconsistency noted in earlier reviews and aligns with Git’s current preference for enums over `#define` for flag sets. No substantive review feedback is expected beyond style nitpicks.