This is a bugfix follow-up in the promisor remote subsystem. Patrick Steinhardt confirms that the reported hang in `git receive-pack`—triggered by pushing a zero-object ref update into a promisor-configured repository—was already fixed by commit d9982e8290 ("connected: close err_fd in promisor fast-path", 2026-05-15). The fix will ship in Git 2.55, which is scheduled for release today.

The email includes a minimal test case (a patch to `t/t5616-partial-clone.sh`) that reproduces the hang. Patrick notes that while the fix exists, it lacks test coverage, and he invites Wei Hu to contribute the test or offers to send a patch himself. This is a straightforward housekeeping opportunity: the test would help prevent future regressions in an area that has seen multiple bugs.

The thread is now effectively resolved on the technical side; the only remaining action is adding the test. No controversy or open questions remain. The reproduction script and test case provide clear context for anyone picking up the work.