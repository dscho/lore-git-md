This is the **final acknowledgment** in the thread resolving macOS CI hangs in `t5551` and `t5559`. Jeff King confirms Junio C Hamano’s plan to merge the first two patches (Apache `Timeout` increase and test isolation) while dropping the third (GitLab CI alignment), with a minor stylistic tweak to patch 2/2 already applied. The exchange is purely procedural; no new technical discussion or policy debate occurs here.

The **2-patch series is now effectively merged** and will ship in Git 2.55. The root cause — Apache bug 70131 triggering HTTP/2 stalls or mid-transfer aborts during `ls-refs` advertisement of 100,000 loose refs — is addressed by increasing Apache’s `Timeout` directive from 300 to 600 seconds and isolating the expensive test case into its own repository. The isolated repository is now initialized as bare to match the script’s convention, a cosmetic change Junio applied locally.

The **third patch (GitLab CI alignment)** remains on hold pending a policy decision about CI resource allocation and visibility. Junio has rejected strict alignment with GitHub’s coverage, suggesting GitLab could run a superset of GitHub’s tests instead. No concrete plan for improving GitLab CI visibility has been proposed yet.

Key technical details carried forward: files touched (`t/lib-httpd/apache.conf`, `t/t5551-http-fetch-smart.sh`), subsystems (HTTP/2 transport, test suite, ref backends), and the performance impact (script runtime reduced from ~95 seconds to ~37 seconds in EXPENSIVE runs). The thread’s resolution leaves no open technical questions; the only remaining discussion is about CI policy, which is orthogonal to the patches themselves.