This is Junio Hamano checking in on the architectural crossroads for Pablo Sabater’s `git log --graph` cascading indentation series, specifically the debate between the v6 peek-based abstraction and Kristofer Karlsson’s proposed lookahead buffer redesign.

The ball is now in Junio’s court. Kristofer has laid out a technically sound alternative to the v6 peek functions—using a two-slot lookahead buffer populated by `get_revision_internal()`—that resolves the three failing test cases and eliminates the fragility of peeking at unsimplified commits. His prototype demonstrates the approach works, though it requires minor timestamp tweaks for determinism and a small adjustment to `graph_is_interesting()` to treat lookahead commits as "interesting" regardless of their `SHOWN` flag. The redesign removes the need for the two new peek functions entirely, simplifying the interface between `graph.c` and `revision.c`.

Junio’s question ("Whose court is the ball in now?") is rhetorical; he is signaling that he is ready to make the call between the two approaches. The thread has reached a decision point: either proceed with incremental improvements to the v6 peek-based abstraction (adding defensive checks and robustness improvements) or adopt Kristofer’s lookahead buffer, which would require a v7 resend of the series. The lookahead approach appears to address the core semantic issues more cleanly, but Junio’s preference for incremental vs. radical change will likely determine the outcome. Pablo Sabater’s next steps depend on this decision.