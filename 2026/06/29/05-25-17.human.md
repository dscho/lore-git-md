This is a **build hygiene review** on patch 6/8 of Tian Yuchen’s `paint_down_to_common()` optimization series. The reviewer, SZEDER Gábor, points out that the patch removes two static functions (`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`) that became unused in the previous patch (5/8). Because the functions are static and their last callers were removed in the same series, the code fails to build with `DEVELOPER=1` due to unused-function warnings.

SZEDER’s feedback is purely mechanical: he asks that the removal be squashed into the previous patch (5/8) to avoid a broken intermediate state. This is a surface-level review — it does not question the correctness or design of the change, only its presentation. The fix is straightforward and uncontroversial; the series is otherwise fully reviewed and ready to merge.