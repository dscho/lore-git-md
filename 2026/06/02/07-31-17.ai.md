This is a review comment on Patrick Steinhardt's `git history drop` patch series. Pablo Sabater raises two technical points:

1. **Inconsistent error messages**: Notes that the error message for root commits includes context about why they can't be dropped ("it has no parent to replay onto") while the merge commit error is more terse. Suggests adding similar explanatory context for merge commits.

2. **Test helper duplication**: Points out that the `expect_graph` test function appears identically in multiple test files (t6016, t4215) under different names. Suggests:
   - Moving this common function to `lib-log-graph.sh` to avoid duplication
   - Removing redundant `--graph` flag since it's already included in `lib_test_cmp_graph`

The review is substantive - it identifies real technical issues rather than just surface-level nits. Pablo demonstrates familiarity with the test infrastructure by:
- Knowing where else this helper appears
- Understanding the underlying `lib_test_cmp_graph` implementation
- Offering concrete suggestions for improvement

The feedback doesn't challenge the core functionality but would improve code quality and consistency. Since this is a review rather than a new patch, the ball is now in Patrick's court to either:
- Incorporate these suggestions in a v3
- Explain why the current approach is preferable
- Defer the test helper consolidation to Pablo's upcoming series

The review maintains a constructive tone while identifying clear opportunities for improvement in this experimental command's implementation.