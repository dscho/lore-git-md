This is a follow-up discussion in the thread about deprecating the `git-init-db` command in favor of `git-init`. Phillip Wood suggests adding a runtime warning when `git-init-db` is used, proposing a lighter-weight alternative to Git's standard `you_still_use_that()` deprecation mechanism.

The exchange shows the thread moving toward implementation details of the deprecation, with Phillip suggesting a simple warning message approach that would require creating a separate `cmd_init_db()` wrapper function to issue the warning before calling the main `cmd_init()`. This would make the deprecation more visible to users who might not read the documentation.

The discussion remains focused and technical, with both participants agreeing that while the standard deprecation mechanism is too heavy for this case, some runtime notification would be helpful. The thread appears to be converging on a solution that balances visibility with minimal code impact, though the maintainer's earlier intervention has already established that the alias will remain supported indefinitely.