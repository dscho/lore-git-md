This is a brief acknowledgment in the ongoing `git describe` pattern matching thread. Junio C Hamano responds to Jacob Keller's plan for a v2 patch that will restructure the test case to eliminate ambiguous outputs by adding more `--exclude` patterns.

The exchange confirms alignment on the approach - rather than enforcing artificial stability in the implementation (as previously proposed via `STABLE_QSORT`), the test will be modified to expect unambiguous results through stricter pattern matching. Junio signals approval of this direction with a simple acknowledgment that the plan "sounds workable."

The thread remains focused on test design rather than implementation changes, with Jacob now preparing a revised version that will tighten the test case to properly validate the command's flexible matching behavior. The underlying question about how `--exclude` should match remote ref names remains open but is not addressed in this brief response.