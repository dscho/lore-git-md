This is a test infrastructure patch in Taylor Blau's series integrating `--delta-islands` with `--path-walk` in `git pack-objects`. The patch simplifies the p5311 performance test script by removing redundant test permutations for bitmap lookup tables, which are now considered stable after nearly four years of deployment.

The change drops the dual test runs (with and without lookup tables) to make room for upcoming tests of path-walk behavior. The test function `test_fetch_bitmaps()` is retained for future use with new parameterization. The patch follows Derrick Stolee's suggestion to streamline the test setup before adding new variations.

This preparatory change helps isolate performance impact measurements for the path-walk optimization work in subsequent patches. The modification is minimal and focused, removing 5 lines while keeping the core test structure intact for future extensions. The commit message notes this is part of a larger effort but makes only the necessary test adjustment at this stage.