This is a review comment on Patrick Steinhardt's `git history drop` patch series. Pablo Sabater raises two specific points about the implementation:

1. **Inconsistent error messages**: Notes that the error message for root commits includes context about why they can't be dropped ("it has no parent to replay onto"), while the merge commit error message doesn't provide similar context. This is a style nit about making error messages consistently informative.

2. **Test helper duplication**: Points out that the `expect_graph` test function appears identical to `check_graph` in other test files (t6016 and t4215) and suggests moving it to `lib-log-graph.sh` to avoid duplication. Also notes that the `--graph` flag in `expect_graph` is redundant since it's already included in `lib_test_cmp_graph`.

The review is surface-level, focusing on code organization and style rather than the core functionality. Pablo acknowledges they haven't tested exhaustively but confirms basic functionality works. The suggestions are constructive but not blocking - they could be addressed in a follow-up cleanup rather than requiring a v3 of the series.

This continues the discussion from Junio's earlier review of v2, adding smaller polish items to consider alongside the substantive feedback about code reuse in the working tree update logic.