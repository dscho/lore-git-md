This is a brief concluding exchange in the settled discussion about Git's `init` command naming. The thread had already reached consensus that the legacy `git-init-db` alias would remain permanently supported without deprecation warnings, despite its inconsistency with Git's typical `cmd_foo()` naming pattern.

Patrick Steinhardt and Phillip Wood agree that such exceptions create cognitive friction for new contributors, but acknowledge the maintainer's decision to preserve the status quo. Patrick suggests that when such exceptions must exist, they should at least align with the most commonly known variant (in this case, `git init` rather than `git-init-db`).

The exchange serves as a postscript to the discussion rather than introducing new arguments or proposals. It highlights the project's pragmatic approach to balancing naming consistency with backward compatibility, while recognizing the occasional maintenance costs of such decisions. The thread is now fully concluded with no outstanding action items.