This is a follow-up discussion in the thread about the `git-init-db` alias, now definitively settled as a documentation-only matter after maintainer intervention. Phillip Wood responds to Patrick Steinhardt's earlier consideration of deprecation warnings.

Phillip suggests a lighter-weight warning approach than the standard `you_still_use_that()` mechanism, proposing a simple message that would appear when users run `git-init-db`. He notes this would require creating a separate `cmd_init_db()` wrapper function to emit the warning before calling the main `cmd_init()` implementation.

The exchange shows the thread's final cleanup phase, where participants are considering the minimal possible documentation and warning improvements while respecting Junio's decision to preserve the technical status quo. Phillip's suggestion represents a middle ground between full deprecation warnings and complete silence, though even this minor change would require some code modification to implement cleanly.

This discussion is now purely about optional user-facing messaging, as all technical changes to the command's implementation or removal have been conclusively ruled out by the maintainer. The thread appears to be in its final stages, with participants wrapping up the last details of how to document the legacy alias while keeping the code itself unchanged.