This is a follow-up in the concluded discussion about Git's `init` command naming, where SZEDER Gábor responds to Patrick Steinhardt's position on renaming internal identifiers.

The thread had already reached consensus to maintain the status quo - keeping both the `git-init-db` alias and internal `init-db` naming without changes. SZEDER points out that renaming the implementation (as Patrick had suggested) would create an ongoing maintenance burden rather than a one-time adjustment, since historical context in `git log` would be split across the rename boundary. This reinforces Junio's earlier position that such renames merely shift the burden rather than eliminate it.

The exchange serves as additional rationale for the already-settled decision to preserve existing names, highlighting how even well-intentioned renames can have lasting negative consequences for code archaeology. SZEDER's technical observation about `git log` behavior provides concrete support for the maintainer's judgment call to avoid disruptive changes when the benefits are marginal.