This is the final email in the thread about the `git-init-db` alias, with Junio Hamano weighing in on whether to add a deprecation warning. The discussion has already concluded that the internal implementation will remain unchanged (`init-db` naming preserved) and the alias will stay supported indefinitely.

Junio responds to Phillip Wood's suggestion of a lightweight warning message, arguing that since there are no plans to ever remove the `init-db` alias, even a simple warning may be unnecessary. He notes the alias is already hidden from help output and that documentation consistently recommends `git init`. The only potential downside Junio sees is if someone wants to create a custom alias that conflicts with the built-in `init-db`, which he considers an unlikely edge case.

This effectively closes the thread with a decision to make no changes - neither technical nor warning messages - beyond possible minor clarifications in documentation. The exchange demonstrates Git's conservative approach to legacy features, where historical compatibility is preserved unless there's a compelling reason to break it. Junio's position as maintainer carries the day, with all participants having accepted that the technical status quo will remain.