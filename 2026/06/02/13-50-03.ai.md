This is a final discussion in the now-concluded thread about the `git-init-db` alias, focusing on whether to add a deprecation warning. The thread has already established that the alias will remain supported indefinitely with no code changes.

Junio responds to Phillip Wood's suggestion of a lightweight warning message for `git-init-db` usage. He notes that since there are no plans to ever remove the alias (unlike truly deprecated commands that eventually get deleted), a warning may be unnecessary. The alias is already hidden from help output, and the project consistently documents `git init` as the primary form.

The exchange confirms the thread's resolution: no technical changes will be made to the implementation, the alias remains permanently available, and any documentation updates would be the only remaining action. Junio's position makes clear that even Phillip's minimal warning proposal is likely superfluous given the alias's indefinite support status.

This email effectively closes the discussion, with the maintainer's guidance establishing that the status quo - where `git-init-db` quietly continues working as an undocumented alias - is the intended long-term state. The thread serves as an example of Git's conservative approach to legacy features, prioritizing backward compatibility over internal naming consistency when the cost of change outweighs the benefits.