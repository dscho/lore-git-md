Test cleanup patch fixing a hook leak in the rebase autosquash test suite. This is the first of two patches in a series introducing a new `--fixup-all` option for `git rebase --autosquash`.

The patch fixes a test case in `t3415-rebase-autosquash.sh` that was leaving behind a `prepare-commit-msg` hook, which could affect subsequent tests. The fix uses `test_when_finished` to ensure cleanup happens after the test completes. This is a straightforward housekeeping change that improves test hygiene.

The change is mechanical and uncontroversial - it doesn't modify any behavior, just ensures proper test cleanup. The patch is well-formed with a clear commit message explaining the issue and solution. This sets up clean test state for the main feature patch that follows in the series.