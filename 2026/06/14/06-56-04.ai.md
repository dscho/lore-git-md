This is a **substantive review** of the final patch in the `git replay --linearize` series (v2 3/3). Elijah Newren engages deeply with the implementation, raising several technical points about clarity, edge cases, and test coverage.

The review identifies three **surface-level omissions**: the new `--linearize` flag is missing from both the command synopsis in the man page and the `replay_usage[]` array in `builtin/replay.c`, and the word "ignore" in the option description is ambiguous (Elijah suggests "drop" instead). These are straightforward to address.

More substantively, Elijah asks whether the documentation should mention the mutual exclusivity of `--linearize` and `--revert` (currently enforced only in code), and notes that the code path handling merge commits in `--linearize` mode does not update `last_commit`, a detail that could be clarified in a comment to help future readers. He also points out that the test case claiming to replay "down to the root commit" is incorrect: the range `A..topic-with-merge` excludes `A`, so the test does not actually reach the root.

The review proposes two **additional test cases**: one to verify that `--linearize` works with `--advance`, and another to explicitly test the `--revert`/`--linearize` incompatibility. Elijah also suggests a more robust test for patch equivalence, using `git range-diff` to confirm that the linearized history produces the same changes as the original, regardless of replay order.

Overall, the feedback is constructive and focused on correctness and maintainability. The patch is already well-structured, but these suggestions would further tighten its edge-case handling and documentation. No fundamental objections are raised; the review signals approval in principle ("looking good") once the minor issues are resolved.