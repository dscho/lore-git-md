Performance optimization for `git cat-file`'s default batch format. René Scharfe updates the code to use more efficient string buffer operations (`strbuf_add_oid_hex()` and `strbuf_add_uint()`) instead of the slower `strbuf_addf()` for formatting object information. The change affects the default output format when using `--batch-check` with the standard `%(objectname) %(objecttype) %(objectsize)` pattern.

Benchmarks show a 3-7% speed improvement, with the new implementation processing objects in 979ms compared to 1.051s for the old version. The patch is small (6 lines added, 3 removed) and focused, touching only the formatting logic in `builtin/cat-file.c`. This continues René's pattern of sending clean, targeted performance optimizations.

The change maintains backward compatibility while making a common operation faster. Since this modifies a performance-critical path that handles bulk object queries, even small improvements can have noticeable impact at scale. The patch includes thorough benchmarking data showing the improvement across multiple runs.