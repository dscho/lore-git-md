This is a performance optimization discussion for Git's commit reachability algorithms, specifically around the `paint_down_to_common` function used in merge-base calculations. Elijah Newren chimes in to note that GitHub has independently implemented a similar optimization, though their version doesn't fully handle the infinite generation number case (when commit-graph isn't available).

The key revelation here is that multiple parties have converged on the same core insight: merge-base computations can terminate early once one side of the traversal is exhausted, rather than continuing the full graph walk. Newren offers to either hand off GitHub's implementation (currently in PR #2150) for Kristofer Karlsson to rebase and extend, or to handle it himself after returning from vacation.

The discussion highlights how this optimization has emerged organically from different corners of the Git ecosystem, suggesting it addresses a real and widely-felt pain point in large repository performance. The GitHub implementation provides additional validation of the approach, though its current limitations around infinite generation numbers indicate there's still work to be done before upstreaming. This exchange moves the thread closer to concrete implementation, with clear next steps available for either contributor to advance the optimization.