Test cleanup patch in a series introducing `--fixup-all` for `git rebase --autosquash`. This is the first of two patches, focusing on fixing a test hygiene issue in preparation for the main feature.

The patch addresses a test leak in `t3415-rebase-autosquash.sh` where a `prepare-commit-msg` hook was left behind after the "pick and fixup respect commit.cleanup" test. The fix adds `test_when_finished` to clean up the hook file, preventing it from affecting subsequent tests.

This is a straightforward test infrastructure improvement with no behavior changes. The one-line diff adds the cleanup call immediately after the test creates the hook. The change is clearly motivated and correctly scoped for its position as a preparatory patch in the series.

The commit message follows Git conventions, explaining both the problem and solution concisely. As the first patch in a series introducing new functionality, this establishes good hygiene before making substantive changes to the rebase machinery in the following patch.