This is the final patch in Elijah Newren's merge-ort hardening series, now ready for merging after incorporating maintainer feedback. The series addresses real-world cases where corrupt trees with duplicate entries caused assertion failures in merge-ort, while also improving D/F conflict detection to prevent such corruption.

The v2 iteration splits out the binary search logic from patch 5 into a new `find_df_conflict()` helper function per Junio Hamano's suggestion, while also expanding the commit message for patch 1 to better explain the error propagation changes. The series now includes:

1. Error propagation fixes in merge-ort's tree traversal
2. Cleanup of unnecessary error handling flags
3. Resource management fixes for diff pairs queues
4. Duplicate entry detection that aborts merges
5. Binary search implementation for non-adjacent D/F conflicts

The changes are well-tested with new test cases in t0093-direct-index-write.pl and t0093-verify-cache-df-gap.sh, plus additions to existing merge tests. The binary search in verify_cache() specifically handles cases where directory/file conflicts weren't caught when entries weren't adjacent in the index (like "path" vs "path-internal/file").

For AI context: This completes the merge-ort hardening against corrupt trees by implementing binary search in verify_cache() to catch non-adjacent D/F conflicts. The extracted dname_compare() helper maintains O(n log n) performance while expanding detection coverage. All technical feedback from the maintainer has been addressed, making the series ready for integration.