Feature implementation adding `--fixup-all` to `git rebase --autosquash`, completing the two-part series. This patch introduces a new option that automatically folds a range of commits into their oldest ancestor while preserving the original commit message.

The implementation modifies `sequencer.c` to handle the range-folding behavior and updates `builtin/rebase.c` to add the new flag. The change includes extensive test coverage (105 lines in `t3415-rebase-autosquash.sh`) verifying various scenarios:
- Basic range folding
- Handling existing fixup! commits in the series
- Edge cases like single-commit ranges and empty ranges
- Interaction with merge commits
- Validation of required flags and incompatible options

The documentation clearly explains that `--fixup-all` keeps the first commit as a pick and converts subsequent commits to fixups, folding them into one while preserving the first commit's message. The option requires `--autosquash` and cannot be combined with `--rebase-merges`.

This appears to be a well-constructed implementation of the feature proposed in the first patch. The test coverage is thorough, and the commit message clearly explains both the motivation and technical approach. The change touches core rebase machinery but maintains backward-compatible behavior.