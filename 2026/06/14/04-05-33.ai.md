This is a review comment by Junio Hamano on Pablo Sabater's v5 patch series improving `git log --graph` rendering. The focus is on the test infrastructure changes in patch 2/2, specifically the method used to disable commit-graph functionality during testing.

Junio questions the approach of manually removing `.git/objects/info/commit-graph*` files in the test helper `unset_commit_graph()`. While acknowledging he doesn't fully understand why commit-graph affects the test results, he suggests there should be a more stable/abstract way to disable commit-graph functionality than directly manipulating implementation-specific files.

The comment indicates this is a test infrastructure concern rather than a critique of the core visualization changes. Junio has CC'd Taylor Blau (a packfile/commit-graph expert) for his opinion on the commit-graph interaction. This follows Junio's pattern of involving domain experts when touching specialized subsystems.

The review is substantive - it engages with the technical approach rather than surface-level issues - but doesn't block the patch's core functionality. The question suggests either:
1) The test could use Git's existing mechanisms for controlling commit-graph behavior, or
2) The interaction between commit-graph and graph rendering may need deeper investigation if the current behavior is truly unpredictable.

This appears to be the documentation/edge case Junio referenced in earlier rounds, now surfacing as a specific implementation concern. Pablo will likely need to either justify the current approach or switch to a more abstract disable mechanism in v6.