Here’s the human-readable summary for a developer catching up on the thread:

The thread is about adding a `--squash` option to `git rebase` that folds a range of commits into one while preserving their messages, originally proposed by Harald Nordgren. The implementation is technically complete, with thorough test coverage and edge-case handling, and the naming (`--squash`) has been settled. However, the discussion has shifted toward an alternative architecture: moving the functionality into a new `git history squash` command instead of extending `rebase`. D. Ben Knoble first suggested this approach, and Patrick Steinhardt—who is developing the `git history` tool—has now endorsed it, calling it a natural fit for his roadmap. This endorsement effectively settles the architectural debate in favor of the `git history` path.

The original rebase implementation remains viable and well-tested, but the momentum is now behind the `git history` alternative. The open question is no longer whether to add the feature to `rebase`, but how `git history squash` should behave—particularly around revision selection and integration with existing workflows. Harald still prefers the rebase approach for its discoverability, while Junio’s final decision is pending. The thread is now likely to focus on designing the `git history` subcommand rather than refining the rebase patch.