Here’s the updated human-readable summary for a developer catching up on the thread:

The `git history squash` feature, proposed by Harald Nordgren, is now complete in its sixth iteration and ready for Junio’s final review. This new subcommand lets users collapse a range of commits—including those with merge commits—into a single commit at the oldest end of the range, avoiding the repeated conflict stops of a rebase-based workflow. The series has addressed all prior feedback, including input validation, merge commit handling, and usability improvements to the `--reedit-message` flag, which now presents a cleaner template with numbered markers for clarity.

Key decisions are now settled: the command rejects operations where refs point to commits inside the squashed range (with advice to use `--update-refs=head` to retarget the current branch), allows squashing ranges containing fully contained merges but rejects merges with external parents, and supports flexible input syntax like `HEAD~3.. ^topic`. Harald has also agreed to Phillip Wood’s suggestion to error out if a `fixup!` commit’s target lies outside the squashed range, a change that will likely appear in the next revision.

The only remaining discussion is a narrow usability trade-off: Harald and Phillip disagree on whether the `--reedit-message` template should include visual separators between folded-in commit messages. Harald argues the current numbered markers improve readability, especially for multi-paragraph messages, while Phillip prefers a more minimalist approach. This is a cosmetic issue and not a blocker. The series is otherwise code-complete, with no further design changes expected. Junio’s final assessment is the next step.