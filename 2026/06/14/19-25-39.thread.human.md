Here’s the human-readable summary for a developer catching up on the thread:

---

The `git history squash` command, proposed by Harald Nordgren, is nearly ready for integration after a long review process. This new subcommand efficiently collapses a commit range—including merge commits—into its oldest commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based approach. The series is now at version 14, with all technical and procedural feedback addressed except for one final policy decision.

The last unresolved question was whether autosquash markers (like `fixup!`, `squash!`, and `amend!`) should enforce strict case-sensitive matching for commit OIDs or adopt a permissive approach that accepts all-uppercase or all-lowercase OIDs, matching `git rebase --autosquash`. Junio C Hamano has now clarified that the project will enforce **strict case-sensitive matching**, aligning with Git’s historical convention of emitting only lowercase OIDs. This decision requires a one-line change in Patch 7/8 (replacing `istarts_with()` with `starts_with()`) and additional test coverage for the strict policy. Phillip Wood had argued for permissive matching to maintain consistency with `git rebase`, but Junio’s position—citing prior work (`bc/restrict-hex-to-lowercase`)—settles the matter.

The series is functionally complete and marked "Will replace" by Junio, signaling intent to queue it for the next release once the final reroll (v15) is posted. Key features include descendant ref protection (rejecting operations that would leave local branches dangling), merge-parent preservation, and autosquash-style message editing. The implementation is split into eight focused patches, with the core logic decomposed into range validation, ref safety checks, commit creation, and message editing. All other feedback—including trailer ordering, test robustness, and CLI style—has been resolved in v14.

Harald is expected to post v15 shortly with the case-sensitivity fix and tests. Once that’s done, the series will likely move from `seen` to `next` and eventually graduate to `master`. The only remaining discussion point is whether the strict policy might create user-facing inconsistency with `git rebase --autosquash`, but the technical path forward is now clear.