Here’s the human-readable summary for a developer catching up on the thread:

The `git history squash` feature, proposed by Harald Nordgren, is now functionally complete in its eighth iteration and ready for merging. This new subcommand efficiently collapses a range of commits—including those with merge commits—into a single commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based approach. The series has addressed all prior technical feedback, including critical fixes for `rev-list` option validation and improved handling of `fixup!`/`squash!`/`amend!` commits.

Key improvements in v8 include autosquash alignment, where the `--reedit-message` template now groups related fixup/squash commits under their targets (matching `git rebase -i --autosquash`), and stricter input validation that sanitizes `rev-list` options to prevent silently incorrect results. The command rejects ranges with external-parent merges but allows fully contained merges, and by default blocks operations where refs point to commits inside the squashed range, guiding users to `--update-refs=head` if they want to retarget the current branch.

The only unresolved discussion is whether `--reedit-message` (or `--edit`) should be the default, with Phillip Wood arguing for commit hygiene and Harald preferring the current behavior. Junio Hamano has signaled intent to queue the series, marking it "Will replace" in his last review, so this minor usability tweak won’t block integration. The implementation touches `builtin/history.c`, the sequencer, and adds a 776-line test script covering edge cases like merge handling, ref safety, and template construction. All patches are now uncontroversial and ready for the next release.