Here’s the human-mode summary for a developer catching up on the thread:

The thread introduces `git history squash`, a new command that collapses a range of commits—including merges—into a single commit while preserving descendant history. Harald Nordgren’s 14th iteration of the series is now complete and ready for integration, addressing every piece of prior feedback. The command avoids the repeated conflict stops of a rebase-based workflow, making it a faster alternative for linearizing history.

Key behavior: by default, the command rejects operations that would leave any local branch descended from the squashed range dangling, with an advice message listing the blocking branches in sorted order. Users can override this with `--update-refs=head` to rewrite only the current branch. The default workflow opens an editor with an autosquash-style template that groups `fixup!`, `squash!`, and `amend!` commits under their targets, matching `git rebase -i --autosquash`; the `--no-edit` flag skips the editor and uses the oldest commit’s message (or the last `amend!` targeting it). The implementation is strict: `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and markers whose targets lie outside the selected commits are rejected outright.

Phillip Wood’s thorough reviews shaped the series, particularly around ref-handling safety, merge-parent preservation, and autosquash strictness. Junio C Hamano signaled intent to queue the series with “Will replace” in v7, and the latest version incorporates his test robustness fix and Phillip’s missing `Signed-off-by:` trailer. The series is now cooking in `seen` and should graduate to `next` in the next “What’s cooking” report. Future work may integrate `git history squash` as a backend for `git rebase --autosquash` via `git replay`, but that’s not a blocker for this series.