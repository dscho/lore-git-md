Here’s the human-mode catch-up summary for a developer returning to the thread:

The `git history squash` feature, proposed by Harald Nordgren, is now in its sixth iteration and essentially complete. It lets users collapse a range of commits—including those with merge commits—into a single commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based workflow. The series has addressed all prior feedback, including input validation, merge commit handling, and usability refinements to the `--reedit-message` flag, which now omits `fixup!`/`amend!` noise and uses numbered markers to separate folded-in commit messages.

Key participants have converged on the design: Junio Hamano endorsed the default behavior of rejecting operations where refs point to commits inside the squashed range (with advice to use `--update-refs=head`), Patrick Steinhardt clarified that merges with external parents are rejected by default (while fully contained merges are allowed), and Phillip Wood’s usability critiques—such as stricter input validation and a cleaner commit message template—have been incorporated. The only remaining open question is a cosmetic one: whether the `--reedit-message` template should include numbered markers (Harald’s preference) or omit them for brevity (Phillip’s suggestion). This is a minor trade-off with no impact on functionality.

The series is ready for Junio’s final review, with no further design changes expected. Test coverage is thorough, and the implementation touches `builtin/history.c`, plumbing helpers like `commit_tree_ext()`, and new advice messages. A follow-up patch to fix option ordering in other `git history` subcommands is planned but doesn’t block this effort.