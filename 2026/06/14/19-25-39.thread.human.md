Here's the updated human-readable summary:

Harald Nordgren has proposed adding a `--squash` option to `git rebase` that would fold a range of commits while preserving their messages, addressing a workflow gap where users currently need to chain `reset --soft` with `commit --amend`. The implementation is technically complete with thorough test coverage, including handling of merge commits and clear documentation of its incompatibility with `--rebase-merges`. Junio initially raised naming concerns (it was originally called `--fixup-all`) but those appear resolved, leaving only the question of whether this functionality belongs in rebase at all.

D. Ben Knoble's recent review engages substantively with the core problem while suggesting an alternative direction. He acknowledges the counting issue Harald identified with current workflows but proposes solving it through different revision selection methods. More significantly, he suggests this functionality might better belong in Patrick Steinhardt's experimental `git history` command rather than as a rebase feature, noting this could offer more flexible range selection and potentially cleaner merge handling. The review thoughtfully advances the discussion by proposing an architectural alternative rather than just critiquing the current approach.

The thread now awaits either Junio's decision on whether to accept the rebase implementation or Patrick's input on whether the `git history` command might be extended to cover this use case. The technical merits appear settled; what remains is determining the best home for this functionality in Git's command structure.