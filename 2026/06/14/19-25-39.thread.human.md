Here’s the human-readable catch-up summary for the thread:

This thread started with Harald Nordgren’s proposal to add a `--squash` option to `git rebase`, letting users fold a range of commits into one while keeping the original messages. The series is now in its second version, with tests merged and the implementation technically complete—it handles edge cases like merge commits and conflicts with `--rebase-merges`. Junio’s earlier naming concerns have been resolved, but the thread has since taken a sharp turn toward a competing architectural idea: Patrick Steinhardt and D. Ben Knoble argue the feature belongs in `git history` instead, as a dedicated subcommand that would offer more flexible range selection and fit better with Patrick’s long-term roadmap.

Phillip Wood’s recent review shifts the focus back to the rebase implementation, raising two key concerns. First, he points out that the current approach—processing commits one by one—could force users to resolve the same conflicts repeatedly if the rebase base changes mid-operation. He suggests a more efficient design: compute a single three-way merge of the entire commit range against the upstream base, then let users resolve conflicts once. Second, he flags a usability issue: the implementation preserves the first commit’s message without giving users a chance to edit it, which could be problematic if the folded commits introduce new context. These aren’t dealbreakers, but they’re substantive enough that any future patch would need to address them.

The thread is now at a crossroads. The `git history` alternative has strong backing from Patrick, but the rebase implementation is feature-complete and has its own advocates. Junio hasn’t weighed in on the architectural question yet, so the next move likely depends on whether the discussion converges on one approach or continues to explore both. Either way, the efficiency and usability concerns Phillip raised will need to be addressed before the feature lands.