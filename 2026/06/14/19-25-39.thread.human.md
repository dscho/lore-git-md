Here's the human-readable thread summary:

Harald Nordgren has proposed adding a `--squash` option to `git rebase` that would fold a range of commits into their first commit while preserving the original message. This provides a more discoverable alternative to the current workflow of using `git reset --soft` followed by `git commit --amend`. The implementation is technically complete, with thorough test coverage (117 new test lines) that verifies edge cases like handling merge commits in the range and interactions with existing fixup! commits. The option implies the merge backend and cannot be combined with `--rebase-merges`.

After some discussion, the naming was settled on `--squash` to align with `merge --squash`, resolving Junio's initial concerns. The remaining open question is whether this convenience feature justifies adding new rebase machinery versus continuing to use the composed commands. The thread shows the implementation is ready, with Junio's decision on whether to accept the feature being the final step.