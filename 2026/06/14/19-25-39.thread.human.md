Here’s the human-readable summary for a developer catching up on the thread:

The `git history squash` feature, proposed by Harald Nordgren, is now functionally complete and ready for integration. This new subcommand efficiently collapses a commit range (including merge commits) into its oldest commit while preserving descendant history—avoiding the repeated conflict stops of a rebase-based approach. The series has reached its eighth iteration (v8), addressing all prior feedback, including a critical `rev-list` option validation gap that could produce silently incorrect results in earlier versions.

Key improvements in v8 include autosquash alignment (the `--reedit-message` template now matches `git rebase -i --autosquash`), better handling of `fixup!`/`squash!`/`amend!` commits (rejecting only those with out-of-range targets), and stricter input validation (sanitizing `rev-list` options to prevent walk-order changes). The command also now incorporates `amend!` messages targeting the first folded commit into the default squashed message, resolving an internal inconsistency raised by Matt Hunter. By default, the command rejects operations where refs point to commits inside the squashed range, with an advice message guiding users to `--update-refs=head` to explicitly retarget the current branch.

The series has been marked "Will replace" by Junio Hamano, signaling his intent to queue it for the next release. All technical concerns—range resolution, merge handling, ref behavior, and input validation—are resolved. The only open question is whether `--reedit-message` (or `--edit`) should be the default to encourage commit hygiene, but this is a minor usability tweak and not a blocker. Harald plans a final v9 iteration with improved test readability, but no substantive changes are expected. The implementation touches `builtin/history.c`, the sequencer, and adds comprehensive test coverage.