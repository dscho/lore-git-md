Here’s the human-readable summary for a developer catching up on the `git history squash` thread:

The thread is about Harald Nordgren’s proposal to add a `git history squash` command, which collapses a range of commits into a single commit while preserving descendant history. This avoids the repeated conflict stops of a rebase-based approach. The series is now in its sixth iteration (v6) and is considered code-complete, addressing all prior feedback, including input validation, merge commit handling, and CLI/documentation style corrections.

Key participants include Harald (the author), Phillip Wood (who raised usability concerns about input validation and merge commit handling), Junio C Hamano (the maintainer, who endorsed the ref-handling design and requested minor CLI/documentation tweaks), and Patrick Steinhardt (who clarified design decisions, such as rejecting merge commits with external parents by default). The core behavior is now finalized: the command rejects operations where refs point to commits inside the squashed range, advising users to use `--update-refs[=head]` to retarget the current branch. The `--reedit-message` flag allows editing the squashed commit message, seeded with all folded-in commit messages (oldest first).

The series touches `builtin/history.c`, adds new helpers like `commit_tree_ext()` (with a `message_template` parameter for `--reedit-message`), and includes extensive test coverage. Open questions from earlier rounds—such as input syntax flexibility and merge commit handling—have been resolved, though Phillip’s concern about single-commit ranges (`@^!`) remains unaddressed. The next step is Junio’s final review, with no further design changes expected. A follow-up patch to fix option ordering in other `git history` subcommands is planned but doesn’t block this series.