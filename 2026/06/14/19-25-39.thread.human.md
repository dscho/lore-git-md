Here’s the human-mode summary for a developer catching up on the thread:

The `git history squash` feature, which collapses a range of commits into a single commit while preserving merge topology and descendant history, is now in its 13th iteration. Originally proposed by Harald Nordgren, the series has evolved through extensive review, particularly from Phillip Wood, who contributed critical fixes for range validation, merge-parent preservation, and ref handling. Junio C Hamano has signaled intent to merge the feature, marking it as "Will replace" in earlier versions.

Version 13 splits the implementation into eight patches, breaking the previously monolithic final patch into logical components: range parsing, validation, branch protection, commit creation, and message editing. This addresses Phillip Wood’s request for incremental review and makes the 1,500-line core implementation more digestible. Key behaviors are now finalized: the command rejects root commits, multi-tip ranges, and merges with external parents; protects all local branches descended from the squashed range by default (with `--update-refs=head` to restrict updates to the current branch); and handles `fixup!`, `squash!`, and `amend!` messages with stricter rules to prevent accidental message loss. The default behavior opens an editor with an autosquash-style template matching `git rebase -i --autosquash`, while `--no-edit` uses the oldest commit’s message or the last `amend!` targeting it.

The series is technically complete and ready for integration, but procedural friction remains. Junio’s earlier feedback highlighted that Harald’s rapid rerolls—sending new versions before reviewers could finish feedback—created a "bottomless time sink" that discouraged participation. While v13 demonstrates progress by splitting the patches, Harald hasn’t explicitly addressed Junio’s advice to coordinate with reviewers to avoid invalidating their work. Phillip Wood’s roadmap for v13 (e.g., fixing a ref-filter display bug) also remains unaddressed. The feature’s future hinges on whether Harald can adapt to the project’s review rhythm or if another contributor steps in to finalize the remaining adjustments. If resolved, the series is poised to merge, with Junio and D. Ben Knoble noting its potential as a faster backend for `git rebase --autosquash` when combined with `git replay`.