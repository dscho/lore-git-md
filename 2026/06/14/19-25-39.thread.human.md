Here’s the human-readable summary for a developer catching up on the thread:

The `git history squash` feature, now in its 12th iteration by Harald Nordgren, is ready to land. This new subcommand folds a commit range into its oldest commit while preserving descendant history—eliminating the need for manual rebase or reset sequences. The implementation is robust, incorporating all feedback from Phillip Wood’s thorough review of earlier versions.

Key improvements in v12 include stricter range validation (rejecting root commits, multiple tips, or external parents), proper handling of merge commits (preserving their full parent lists), and autosquash-style message editing by default. The command groups `fixup!`, `squash!`, and `amend!` commits under their targets in the editor template, matching `git rebase -i --autosquash` behavior. It also protects all local branches descended from the squashed range by default, with an option to restrict updates to the current branch.

The series has received strong support: Junio Hamano signaled intent to merge it ("Will replace" in v7), Phillip Wood’s fixup patches were fully adopted, and Matt Hunter confirmed v10 addressed all his concerns. The only remaining discussion is forward-looking—Junio and D. Ben Knoble see this as a foundational piece for a faster `git rebase --autosquash` backend using `git replay`. The code is technically complete and awaits queuing in `next`.