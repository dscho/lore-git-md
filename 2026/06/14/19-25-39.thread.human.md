The `git rebase --squash` feature proposal remains in limbo, awaiting resolution of key architectural concerns. Harald Nordgren's patch series introduces a `--squash` option to fold a range of commits (defaulting to `@{upstream}..HEAD`) while preserving their messages, but Phillip Wood has raised significant efficiency and user-experience issues: the current implementation risks repeated conflict resolution if the rebase base changes mid-operation, and it preserves the first commit's message without allowing interactive editing. Meanwhile, D. Ben Knoble and Patrick Steinhardt have proposed an alternative—`git history squash`—which would offer more flexible range selection and fit naturally into the `git history` roadmap.

The thread has stalled on these technical questions, with no substantive replies yet from Harald addressing Phillip's concerns. The latest exchange was procedural: Harald asked whether to include the original cover letter in every version of the series to help latecomers understand the feature's purpose. While this doesn't advance the core discussion, it suggests the next revision will be more accessible to reviewers. For now, the thread's fate hinges on whether the rebase implementation can be refined to address efficiency and UX gaps, or if the `git history` alternative gains enough traction to supersede it. Junio's position on feature acceptance remains pending.