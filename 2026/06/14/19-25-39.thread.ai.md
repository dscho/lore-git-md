Here is the updated dense AI thread summary incorporating Phillip Wood’s merge-parent preservation bugfix:

---

Proposal to fold commit ranges into a single commit, implemented as `git history squash` standalone subcommand. **Series v11 by Harald Nordgren is posted, functionally complete, and addresses all feedback**, including the usability debate from v10 by making message editing the default behavior. **Marked "Will replace" by Junio C Hamano in v7**, signaling intent to queue for the next release. **Finalized in v11**:
- **Default message editing**: The command now **opens an editor with the autosquash-style template by default**, matching `git rebase -i --autosquash`. The `--no-edit` flag skips the editor, keeping the oldest commit’s message (or the last `amend!` targeting it). This resolves the usability debate from v10, where Phillip Wood argued for encouraging commit hygiene.
- **Stricter range validation**: Rejects ranges that reach a root commit, have more than one tip, or have more than one base, ensuring the selected history has exactly one boundary and one tip. This tightens input validation while preserving the ability to handle interior merges. **Aligned with Phillip Wood’s v10 review patches 3 and 4**, which proposed similar constraints. **Phillip’s fixup patch for multi-tip rejection** replaces the `--boundary`-based approach with a precise mechanism tracking each commit’s parents and using object flags (`SQUASH_SEEN` and `SQUASH_TIP`) to count tips, failing with a clear error if anything other than exactly one tip is found. The patch also improves error reporting by checking that every parent of each commit has been seen, producing more specific messages than the generic "more than one base" message it replaces. **Phillip’s fixup patch corrects a logic error in boundary detection** by replacing a check for `UNINTERESTING` commits with a check for `BOTTOM` commits, ensuring the code correctly identifies the base of the squash range. This prevents misidentifying unrelated `UNINTERESTING` commits (e.g., from excluded side branches) as the range boundary, which could lead to incorrect validation or silent data loss. **Phillip’s bugfix patch explicitly rejects squashing down to a root commit**, adding an early check in `resolve_squash_range()` to error out with a clear message (`"cannot squash down to root commit"`). The test case demonstrates the rejection by creating a disconnected root commit and verifying the error.
- **Merge-parent preservation**: **Phillip Wood’s bugfix patch** ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology. The fix modifies `resolve_squash_range()` to no longer return a `base` commit (the parent of the oldest commit) and updates `cmd_history_squash()` to use `oldest->parents` directly when constructing the squashed commit. This matches the behavior of `git commit` and `git merge`, which both preserve the parent list of the commit being amended or merged into. The test case verifies the fix by creating a merge commit, amending it with a fixup, and squashing the range to confirm the resulting commit has the correct parents and tree. **Critical for correctness**: Without this fix, squashing a range whose oldest commit is a merge would silently drop all but the first parent, corrupting history.
- **Ref-handling refinement**: **Protects all local branches descended from the squashed range**, not just those pointing directly at a squashed commit. This stricter validation ensures consistent behavior and clearer error messages. The command now rejects operations if *any* local branch (including those pointing to descendants of squashed commits) would be left dangling. **Matches Phillip Wood’s v10 review patch 5 and his subsequent ref-handling fixup patch**, which replaces the `oidset`-based interior-ref detection with a `ref-filter`-based approach. The fixup patch also **explicitly excludes detached HEADs** from protection, treating them as experimental state not worth preserving. **Error messages could be further polished** to list problematic branches explicitly, though the current implementation provides clear guidance.
- **Reflog message accuracy**: Continues to record the **full revision expression** (e.g., `squash: updating start..HEAD ^keep`).
- **Rev-list option sanitization**: Preserves the `boundary` invariant while enforcing `--reverse --topo-order` and standardizing option handling via direct `rev_info` struct manipulation (per Phillip Wood’s refactoring patch). This ensures future changes won’t silently alter walk behavior.
- **Documentation clarity**: Explicitly states that `amend!` messages replace the target’s message only when targeting the oldest commit in the range.
- **Autosquash alignment**: The default template **exactly matches `git rebase -i --autosquash`**, including grouping `fixup!`/`squash!`/`amend!` commits under their targets and fully commenting out `fixup!` messages while retaining `squash!`/`amend!` bodies.
- **Fixup/squash/amend handling**: A marker is **refused only when its target lies outside the range**, allowing multiple fixups for the same in-range commit to fold together. A range made entirely of markers for one below-range target is combined into a single commit, taking its message from the last `amend!` if present. **Phillip Wood’s v10 review patch 6 proposed stricter message preservation**, but v11 adopts a more flexible approach. **Phillip’s subsequent review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`), preventing accidental message loss. The patch also **preserves `amend!` messages** when squashing into a `fixup!` commit by converting the subject prefix to `amend!` and appending the body, and **supports nested fixups** (e.g., `fixup! fixup! target`) by recursively stripping autosquash prefixes. **Targets are validated more strictly**, rejecting `fixup! HEAD^` or `fixup! main` in favor of fixed object IDs or subject-based targets. **Authorship is maintained** from the first commit in the range, matching `git rebase --autosquash` behavior.
- **Merge handling**: Interior merges are folded as long as the range has a single base, with `--ancestry-path` ensuring only commits descended from the base are included. A range reaching more than one base is rejected. **Phillip Wood’s v10 review patch 7 fixed a bug in merge handling**; v11 incorporates this fix.
- **`amend!` message handling**: Incorporates `amend!` messages targeting the first folded commit into the default squashed commit message.
- **Test suite readability**: Uses test helpers (`stage_file`, `commit_with_message`, `check_commit_count`, `check_log_subjects`, `check_log_messages`) to reduce duplication and improve clarity.
- **Patch 1/4 (v11)**: Pure refactoring extracting `first_parent_tree_oid()` helper to centralize logic for resolving a commit’s first-parent tree (or the empty tree for a root commit). Used in three places in `builtin/history.c`; no behavior change. **Status**: ready to merge.
- **Patch 2/4 (v11)**: **Refactoring patch** adding an optional `message_template` parameter to the internal `commit_tree_ext()` helper in `builtin/history.c`. When the caller supplies a template, the function uses it instead of the commit message from the passed-in commit object; when NULL, the original behavior is preserved. This plumbing change enables the default message editing feature. Modifies three existing callers (`commit_tree_with_edited_message`, `cmd_history_fixup`, and `split_commit`) to pass NULL, preserving their current behavior. **No user-visible behavior change**. **Status**: ready to merge.
- **Patch 3/4 (v11)**: **Refactoring patch** preparing the sequencer’s squash-message helpers for reuse by `git history squash`. Extracts three static functions from `sequencer.c`—`add_squash_combination_header`, `add_squash_message_header`, and `squash_subject_comment_len`—and moves them to `sequencer.h` so other commands can produce the same template format as `git rebase -i --autosquash`. Also relocates the `todo_item_flags` enum (including `TODO_REPLACE_FIXUP_MSG`) to the header. **No user-visible behavior change**. Enables the next patch (4/4) to give `git history squash` the same autosquash-style editor template as `git rebase -i`. **Files touched**: `sequencer.c` (31 deletions), `sequencer.h` (70 insertions). **Key names introduced**: `add_squash_combination_header`, `add_squash_message_header`, `squash_subject_comment_len`, `TODO_REPLACE_FIXUP_MSG`.
- **Patch 4/4 (v11)**: **Core implementation** of the `git history squash` subcommand, combining the sequencer preparation and message-editing logic. Implements:
  - **Range resolution and validation** via `resolve_squash_range()`, which ensures the selected history has exactly one boundary and one tip, rejects ranges reaching a root commit, and standardizes `rev-list` option handling by setting options directly in the `rev_info` struct (per Phillip Wood’s refactoring patch). **Incorporates Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection (using object flags `SQUASH_SEEN` and `SQUASH_TIP` to count tips and fail if anything other than exactly one tip is found), his fixup patch correcting boundary detection by checking for `BOTTOM` commits instead of `UNINTERESTING` commits, and **his bugfix patch explicitly rejecting root commits**.
  - **Fixup/squash/amend handling** via `reject_dangling_fixups()`, which blocks markers whose targets lie outside the range (unless the range consists entirely of markers for one target). **Phillip’s subsequent review patch replaces this logic** with `squash_check_subject()`, which tracks commit subjects and their autosquash targets using a `strintmap` to enforce stricter message preservation rules. The new mechanism **rejects `squash!` or `amend!` commits** unless their target is the first commit in the range or the message is explicitly edited (`--reedit-message`), **preserves `amend!` messages** when squashing into a `fixup!` commit, **supports nested fixups**, and **validates targets more strictly**.
  - **Default message editing** via `build_squash_message()`, which constructs an autosquash-style template matching `git rebase -i --autosquash`.
  - **Ref-handling** via the `ref-filter`-based approach, which **protects all local branches descended from the squashed range** (not just direct pointers) and **explicitly excludes detached HEADs**. **Matches Phillip Wood’s v10 review patch 5 and his subsequent ref-handling fixup patch**.
  - **Merge commit handling**, allowing fully contained merges (all parents within the range) while rejecting merges with external parents. **Incorporates Phillip Wood’s v10 review patch 7 and his merge-parent preservation bugfix patch**, which ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range).
  The implementation reuses sequencer helpers (extracted in patch 3/4) to ensure template consistency with `git rebase -i --autosquash`. **Status**: ready to merge.

**Core problem**: Efficiently collapse a linear or merge-commit-containing range into its oldest commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based approach. **Finalized behavior**:
- Refs pointing to commits inside the squashed range are **detected and rejected by default for all local branches descended from the range**, with an `advice.historyUpdateRefs` message suggesting `--update-refs=head` to explicitly retarget the current branch to the squashed commit. **Matches Phillip Wood’s v10 review patch 5 and his ref-handling fixup patch**.
- Ranges whose **oldest commit is a `fixup!`/`squash!`/`amend!` are no longer rejected** unless the marker’s target lies outside the range. The command **groups markers under their targets** in the default template, matching `git rebase -i --autosquash`. **Phillip Wood’s v10 review patch 6 proposed stricter message preservation**, but v11 adopts a more flexible approach. **Phillip’s subsequent review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`), preventing accidental message loss.
- **Rev-list options are fully validated and standardized**: The command enforces `--reverse --topo-order` and preserves the `boundary` invariant by setting options directly in the `rev_info` struct (per Phillip Wood’s refactoring patch), ensuring future changes won’t silently alter walk behavior. **Phillip’s fixup patch for multi-tip rejection** replaces the `--boundary`-based approach with object flags (`SQUASH_SEEN` and `SQUASH_TIP`) to count tips and fail if anything other than exactly one tip is found, improving error reporting by checking that every parent of each commit has been seen. **Phillip’s fixup patch corrects boundary detection** by checking for `BOTTOM` commits instead of `UNINTERESTING` commits, preventing misidentification of unrelated commits as the range boundary. **Phillip’s bugfix patch explicitly rejects squashing down to a root commit**, adding an early check in `resolve_squash_range()` to error out with a clear message.
- **Merge-parent preservation**: **Phillip’s bugfix patch** ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology. Without this fix, squashing a range whose oldest commit is a merge would silently drop all but the first parent, corrupting history.
- **`amend!` message handling**: When `--no-edit` is used, the command **incorporates `amend!` messages targeting the first folded commit** into the default squashed commit message, mirroring how it consolidates multiple `fixup!` commits. **Phillip’s subsequent review patch further refines this** by preserving `amend!` messages when squashing into a `fixup!` commit and supporting nested fixups.

**Open questions**:
- **Future integration with `git rebase --autosquash`**: **D. Ben Knoble** and **Junio C Hamano** endorse using `git history squash` as a faster, non-interactive backend for `git rebase --autosquash` when combined with `git replay`. This would avoid the repeated conflict resolution of the current rebase implementation, offering a performance improvement in the common case where all `fixup!`/`squash!`/`amend!` targets lie within the range. Junio confirms this is a **project-level aspiration**, with the `squash` command positioned as a foundational piece of a larger refactoring effort to modernize Git’s history-editing infrastructure.
- **Message preservation rules**: **Phillip Wood’s review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`). This conflicts with v11’s more permissive approach (editing the message by default) and may inform future iterations, particularly for users who rely on `amend!`/`squash!` messages for commit hygiene.

**Implementation details**:
- **Patch 1/4 (v11)**: Pure refactoring extracting `first_parent_tree_oid()` helper to centralize logic for resolving a commit’s first-parent tree (or the empty tree for a root commit). Used in three places in `builtin/history.c` (`commit_tree_with_edited_message`, `commit_became_empty`, and `split_commit`); no behavior change. **Details**: The helper returns the tree OID of the first parent if one exists, or the repository’s empty tree OID otherwise. Eliminates redundant parse-and-copy logic in the three callers.
- **Patch 2/4 (v11)**: Adds an optional `message_template` parameter to the internal `commit_tree_ext()` helper in `builtin/history.c`. When the caller supplies a template, the function uses it instead of the commit message from the passed-in commit object; when NULL, the original behavior is preserved. This plumbing change enables the default message editing feature. **No user-visible behavior change**. **Details**: Modifies three existing callers (`commit_tree_with_edited_message`, `cmd_history_fixup`, and `split_commit`) to pass NULL, preserving their current behavior.
- **Patch 3/4 (v11)**: Refactoring patch preparing the sequencer’s squash-message helpers for reuse. Extracts three static functions (`add_squash_combination_header`, `add_squash_message_header`, `squash_subject_comment_len`) and the `todo_item_flags` enum (including `TODO_REPLACE_FIXUP_MSG`) from `sequencer.c` to `sequencer.h`. **No user-visible behavior change**. Enables Patch 4/4 to reuse the same autosquash-style template as `git rebase -i`.
- **Patch 4/4 (v11)**: Core implementation of the `git history squash` subcommand. Introduces:
  - `resolve_squash_range()`: Handles range parsing, validation, and resolution; **standardizes `rev-list` option handling** by setting options directly in the `rev_info` struct (per Phillip Wood’s refactoring patch) while preserving the `boundary` invariant. **Incorporates Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection (using object flags `SQUASH_SEEN` and `SQUASH_TIP` to count tips and fail if anything other than exactly one tip is found), his fixup patch correcting boundary detection by checking for `BOTTOM` commits instead of `UNINTERESTING` commits, and **his bugfix patch explicitly rejecting root commits**. **No longer returns a `base` commit** (the parent of the oldest commit) to preserve merge topology; `cmd_history_squash()` now uses `oldest->parents` directly.
  - `reject_dangling_fixups()`: Blocks ranges where any marker’s target lies outside the range. **Phillip’s subsequent review patch replaces this logic** with `squash_check_subject()`, which tracks commit subjects and their autosquash targets using a `strintmap` to enforce stricter message preservation rules. The new mechanism **rejects `squash!` or `amend!` commits** unless their target is the first commit in the range or the message is explicitly edited (`--reedit-message`), **preserves `amend!` messages** when squashing into a `fixup!` commit, **supports nested fixups**, and **validates targets more strictly**.
  - **Ref-handling**: Uses a `ref-filter`-based approach to **protect all local branches descended from the squashed range** (not just direct pointers) and **explicitly excludes detached HEADs**. **Matches Phillip Wood’s v10 review patch 5 and his subsequent ref-handling fixup patch**.
  - `build_squash_message()`: Walks the range in reverse topo-order, gathers each commit’s message, and constructs the template for default message editing.
  - **Key names introduced**: `resolve_squash_range`, `reject_dangling_fixups`, `--[no-]edit`, `ADVICE_HISTORY_UPDATE_REFS`, `SQUASH_SEEN`, `SQUASH_TIP`, `squash_check_subject`, `--reedit-message`, `is_autosquash_subject`, `skip_one_autosquash_prefix`, `truncate_message_to_subject`, `subject_data`.
- **Phillip Wood’s refactoring patch**: Standardizes `rev-list` option handling in `resolve_squash_range()` by replacing `strvec`-based argument passing with direct `rev_info` struct manipulation, aligning with `git replay`. This eliminates redundancy, adds defensive checks for `--ancestry-path`, and moves reflog message construction earlier in `cmd_history_squash()` for clarity. **No user-visible behavior change**.
- **Phillip Wood’s fixup patch for multi-tip rejection**: Replaces the `--boundary`-based approach with a precise mechanism tracking each commit’s parents and using object flags (`SQUASH_SEEN` and `SQUASH_TIP`) to count tips. If the walk ends with anything other than exactly one tip, the command fails with a clear error message. The patch also improves error reporting by checking that every parent of each commit has been seen, producing more specific messages than the generic "more than one base" message it replaces. **Test coverage expanded** to verify the new behavior: a new test case creates a synthetic commit with the same tree as an existing commit but a different parent, then attempts to squash it alongside another commit, expecting the command to fail. Existing merge-handling tests updated to expect the new error message.
- **Phillip Wood’s fixup patch**: Corrects boundary detection in `resolve_squash_range()` by replacing a check for `UNINTERESTING` commits with a check for `BOTTOM` commits, ensuring the code correctly identifies the base of the squash range. This prevents misidentifying unrelated `UNINTERESTING` commits (e.g., from excluded side branches) as the range boundary, which could lead to incorrect validation or silent data loss. **No new test cases are added**, but the change is **tested implicitly** through the existing test suite covering range resolution.
- **Phillip Wood’s bugfix patch (root commit)**: Explicitly rejects squashing down to a root commit by adding an early check in `resolve_squash_range()` to error out with the message `"cannot squash down to root commit"`. The test case creates a disconnected root commit and verifies the rejection, explaining why `--ancestry-path` complicates walking back to the root. **Files touched**: `builtin/history.c` (4 insertions), `t/t3455-history-squash.sh` (12 insertions). **No new helpers or configuration options introduced**.
- **Phillip Wood’s bugfix patch (merge parents)**: Ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology. The fix modifies `resolve_squash_range()` to no longer return a `base` commit and updates `cmd_history_squash()` to use `oldest->parents` directly. The test case verifies the fix by creating a merge commit, amending it with a fixup, and squashing the range to confirm the resulting commit has the correct parents and tree. **Critical for correctness**: Without this fix, squashing a range whose oldest commit is a merge would silently drop all but the first parent, corrupting history. **Files touched**: `builtin/history.c` (10 insertions, 5 deletions), `t/t3455-history-squash.sh` (15 insertions).
- **Phillip Wood’s ref-handling fixup patch**: Replaces the `oidset`-based interior-ref detection with a `ref-filter`-based approach to **protect all local branches descended from the squashed range** and **explicitly exclude detached HEADs**. **Error messages could be further polished** to list problematic branches explicitly. **Files touched**: `builtin/history.c` (refactoring `resolve_squash_range` and removing the now-unused `find_interior_ref` helper), `t/t3455-history-squash.sh` (updating test cases to match the stricter validation).
- **Phillip Wood’s review patch on message preservation**: Overhauls `fixup!`/`squash!`/`amend!` handling by introducing `squash_check_subject()`, which tracks commit subjects and their autosquash targets using a `strintmap`. The patch:
  - **Rejects `squash!` or `amend!` commits** unless their target is the first commit in the range or the message is explicitly edited (`--reedit-message`).
  - **Preserves `amend!` messages** when squashing into a `fixup!` commit by converting the subject prefix to `amend!` and appending the body.
  - **Supports nested fixups** (e.g., `fixup! fixup! target`) by recursively stripping autosquash prefixes.
  - **Validates targets more strictly**, rejecting `fixup! HEAD^` or `fixup! main` in favor of fixed object IDs or subject-based targets.
  - **Maintains authorship** from the first commit in the range, matching `git rebase --autosquash` behavior.
  - **Introduces new helpers**: `is_autosquash_subject()`, `skip_one_autosquash_prefix()`, `truncate_message_to_subject()`, and a `subject_data` struct.
  - **Refactors `resolve_squash_range()`** to return the constructed squash message and integrate subject validation.
  - **Updates tests** with new cases for nested fixups, `squash!`/`amend!` message preservation, and rejection of invalid targets.
- **Files touched**: `builtin/history.c` (545+ insertions, 113+ deletions total across series), `sequencer.c`/`sequencer.h` (101 insertions, 31 deletions total), `advice.c`/`advice.h`, `Documentation/git-history.adoc`, `t/t3455-history-squash.sh` (830+ lines, with improved test helpers for readability).
- **New helpers**:
  - `first_parent_tree_oid()`: Refactored to centralize logic for resolving a commit’s first-parent tree.
  - `commit_tree_ext()` with `message_template` parameter: Plumbing change to support default message editing.
  - Extracted sequencer helpers: `add_squash_combination_header`, `add_squash_message_header`, `squash_subject_comment_len`, `TODO_REPLACE_FIXUP_MSG`.
  - `resolve_squash_range`, `reject_dangling_fixups`, `build_squash_message`, `squash_check_subject`, `--reedit-message`, `is_autosquash_subject`, `skip_one_autosquash_prefix`, `truncate_message_to_subject`, `subject_data`.
  - Object flags: `SQUASH_SEEN`, `SQUASH_TIP`.
  - Test helpers: `stage_file`, `commit_with_message`, `check_commit_count`, `check_log_subjects`, `check_log_messages`.
- **Test coverage**: Range parsing, merge commit handling (including merge-parent preservation), descendant replay, authorship preservation, default message editing (including autosquash grouping), ref-handling behavior (including stricter branch protection and detached HEAD exclusion), `--dry-run` prediction accuracy, input validation (including shape-based validation, `rev-list` option sanitization, root-commit rejection, and multi-tip rejection), fixup/squash/amend edge cases (including `amend!` message incorporation, nested fixups, and stricter target validation), CLI/documentation style compliance, and test suite readability.

**Resolved technical concerns (v4–v11)**:
1. **Input validation**: Handled by `resolve_squash_range()` (rejects single-commit ranges, empty ranges, non-range arguments, multiple bases, malformed histories, ranges reaching a root commit, ranges with more than one tip, and ambiguous ranges). Supports multiple arguments (e.g., `HEAD~3.. ^topic`). **Standardizes `rev-list` option handling** by setting options directly in the `rev_info` struct (per Phillip Wood’s refactoring patch) while preserving the `boundary` invariant. **Incorporates Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection (using object flags `SQUASH_SEEN` and `SQUASH_TIP` to count tips and fail if anything other than exactly one tip is found), his fixup patch correcting boundary detection by checking for `BOTTOM` commits instead of `UNINTERESTING` commits, and **his bugfix patch explicitly rejecting root commits**.
2. **Range resolution**: The base is always the oldest in-range commit’s parent; any other boundary commit is treated as evidence of multiple bases and rejected.
3. **Merge commit handling**: Explicitly rejects merges with external parents but allows fully contained merges (all parents within the range). Uses `--ancestry-path` to ensure only commits descended from the base are included. **Incorporates Phillip Wood’s v10 review patch 7 and his merge-parent preservation bugfix patch**, which ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology.
4. **`fixup!`/`squash!`/`amend!` commits**: Folded like any other commit, but the feature now groups markers under their targets in the default template. **Rejects only markers whose targets lie outside the range**. **Phillip Wood’s v10 review patch 6 proposed stricter message preservation**, but v11 adopts a more flexible approach. **Phillip’s subsequent review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`), preventing accidental message loss.
5. **Code maintainability**: Patch 1/4 refactors `first_parent_tree_oid()`; Patch 2/4 adds `message_template` parameter to `commit_tree_ext()`; Patch 3/4 extracts sequencer helpers; Patch 4/4 implements the core functionality. **Phillip Wood’s refactoring patch standardizes `rev-list` option handling** in `resolve_squash_range()`, aligning with `git replay`. **Phillip Wood’s v10 review proposed a single-pass commit processing approach**, which v11 does not adopt. **Phillip’s fixup patch for multi-tip rejection** improves input validation and error reporting. **Phillip’s fixup patch corrects boundary detection** in `resolve_squash_range()`. **Phillip’s bugfix patches explicitly reject root commits and preserve merge parents**. **Phillip’s ref-handling fixup patch** replaces the `oidset`-based interior-ref detection with a `ref-filter`-based approach.
6. **Ref-handling design**: Finalized to reject operations if **any local branch descended from the squashed range** would be left dangling, with advice guiding users to `--update-refs[=head]`. **Matches Phillip Wood’s v10 review patch 5 and his ref-handling fixup patch**.
7. **Default message editing**: Implements the feature using a template that **exactly matches `git rebase -i --autosquash`**, resolving the usability debate from v10. **Phillip Wood advocated for making message editing the default**, which Harald adopted in v11.
8. **Template consistency**: **Fully resolved** by adopting a template that matches `git rebase -i --autosquash` and extracting sequencer helpers to ensure consistency.
9. **CLI/documentation style**: Addressed Junio’s feedback by moving dashed options before `<revision>...` in the SYNOPSIS and replacing `@` shorthand with `HEAD` in documentation examples.
10. **`amend!` message handling**: **Resolved in v9** by incorporating `amend!` messages targeting the first folded commit into the default squashed commit message, addressing Hunter’s consistency concern. **Documentation clarified in v10** to explicitly state the behavior. **Phillip’s subsequent review patch further refines this** by preserving `amend!` messages when squashing into a `fixup!` commit and supporting nested fixups.
11. **Reflog message accuracy**: **Resolved in v10** by recording the full revision expression (e.g., `squash: updating start..HEAD ^keep`).
12. **Rev-list option sanitization**: **Resolved in v10** by preserving the `boundary` invariant and standardizing option handling via direct `rev_info` struct manipulation (per Phillip Wood’s refactoring patch).
13. **Test suite readability**: Improved in v9 by introducing test helpers, following the pattern established in the `delete-merged` topic.
14. **Documentation clarity**: **Resolved in v10** by tightening the `amend!` and `--reedit-message` descriptions to avoid ambiguity.
15. **Usability debate**: **Resolved in v11** by making message editing the default behavior, with `--no-edit` as the opt-out flag.
16. **Root-commit rejection**: **Resolved by Phillip Wood’s bugfix patch**, which adds an explicit check in `resolve_squash_range()` to error out with a clear message when attempting to squash down to a root commit. The test case verifies the rejection and explains the `--ancestry-path` complication.
17. **Multi-tip rejection**: **Resolved by Phillip Wood’s fixup patch**, which replaces the `--boundary`-based approach with object flags (`SQUASH_SEEN` and `SQUASH_TIP`) to count tips and fail if anything other than exactly one tip is found. The patch improves error reporting and expands test coverage.
18. **Ref-handling refinement**: **Resolved by Phillip Wood’s ref-handling fixup patch**, which replaces the `oidset`-based interior-ref detection with a `ref-filter`-based approach to **protect all local branches descended from the squashed range** and **explicitly exclude detached HEADs**.
19. **Merge-parent preservation**: **Resolved by Phillip Wood’s bugfix patch**, which ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology. The test case verifies the fix.

**Design resolution**:
- **Default behavior**: Reject the operation if **any local branch descended from the squashed range** would be left dangling, with an `advice()` message guiding users to `--update-refs[=head]`. **Matches Phillip Wood’s v10 review patch 5 and his ref-handling fixup patch**.
- **`--update-refs` flag**: Supports `=head` to retarget only the current branch to the squashed commit.
- **Input syntax**: Accepts multiple arguments (parsed like `git rev-list`), supporting compound ranges such as `HEAD~3.. ^topic`. **Standardizes `rev-list` option handling** via direct `rev_info` struct manipulation (per Phillip Wood’s refactoring patch).
- **Merge commit handling**: Rejects merges with external parents but allows fully contained merges. Uses `--ancestry-path` to ensure only commits descended from the base are included. **Incorporates Phillip Wood’s v10 review patch 7 and his merge-parent preservation bugfix patch**, which ensures the squashed commit retains the full parent list of the original merge commit.
- **Input validation**: Uses shape-based validation to reject malformed histories and standardizes `rev-list` option handling. **Incorporates Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection, his fixup patch correcting boundary detection, and **his bugfix patch explicitly rejecting root commits**.
- **Default message editing**: Opens an editor with the autosquash-style template by default, matching `git rebase -i --autosquash`. **Phillip Wood advocated for this in v10**, and Harald adopted it in v11.
- **`amend!` message handling**: Incorporates `amend!` messages targeting the first folded commit into the default squashed commit message. **Phillip’s subsequent review patch further refines this** by preserving `amend!` messages when squashing into a `fixup!` commit and supporting nested fixups.
- **Reflog message accuracy**: Records the full revision expression to preserve user intent.
- **Message preservation rules**: **Phillip’s review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`). This conflicts with v11’s more permissive approach and may inform future iterations.

**Participant positions**:
- **Harald Nordgren**: **Addressed all feedback** in v11:
  - **Default message editing**: Made message editing the default behavior, resolving the usability debate from v10. **Adopted Phillip Wood’s advocacy for encouraging commit hygiene**.
  - **Stricter range validation**: Rejects ranges reaching a root commit, with more than one tip, or with more than one base. **Incorporates Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection, his fixup patch correcting boundary detection, and **his bugfix patch explicitly rejecting root commits**.
  - **Merge-parent preservation**: **Incorporates Phillip Wood’s bugfix patch**, ensuring the squashed commit retains the full parent list of the original merge commit.
  - **Ref-handling refinement**: Protects all local branches descended from the squashed range. **Matches Phillip Wood’s v10 review patch 5 and his ref-handling fixup patch**.
  - **Patch consolidation**: Streamlined the implementation by folding sequencer preparation and message-editing logic into Patch 4/4 and extracting sequencer helpers in Patch 3/4.
  - **Disagreement with Phillip Wood resolved**: Adopted `--no-edit` as the opt-out flag, aligning with Phillip’s argument for encouraging commit hygiene.
- **Phillip Wood**: **Substantive review** of v10 and v11 identifying robustness, edge-case, and structural improvements:
  - **Stricter range validation** (patches 3 and 4): Rejects ranges reaching a root commit, with more than one tip, or with more than one base. **Adopted in v11**, including his fixup patch for multi-tip rejection (replacing the `--boundary`-based approach with object flags `SQUASH_SEEN` and `SQUASH_TIP` to count tips and fail if anything other than exactly one tip is found), his fixup patch correcting boundary detection by checking for `BOTTOM` commits instead of `UNINTERESTING` commits, and **his bugfix patch explicitly rejecting root commits**.
  - **Merge-parent preservation** (bugfix patch): Ensures the squashed commit retains the full parent list of the original merge commit (the first commit in the range), preserving merge topology. **Critical for correctness**: Without this fix, squashing a range whose oldest commit is a merge would silently drop all but the first parent, corrupting history.
  - **Ref-handling refinement** (patch 5 and ref-handling fixup patch): Restricts protection to all local branches descended from the squashed range, not just direct pointers. **Adopted in v11**.
  - **Fixup/squash/amend message preservation** (patch 6 and subsequent review patch): Proposed stricter message preservation, ensuring no `squash!`/`amend!` message is discarded. **v11 adopts a more flexible approach**, but Phillip’s subsequent review patch introduces stricter rules for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`). The patch also **preserves `amend!` messages** when squashing into a `fixup!` commit, **supports nested fixups**, and **validates targets more strictly**.
  - **Merge commit handling** (patch 7): Fixed a bug in squashing merges. **Adopted in v11**.
  - **Code structure**: Proposed a single-pass commit processing approach for efficiency. **Not adopted in v11**.
  - **Advocated for making message editing the default** to encourage commit hygiene. **Adopted in v11**.
  - **Refactoring patch**: Standardized `rev-list` option handling in `resolve_squash_range()` by replacing `strvec`-based argument passing with direct `rev_info` struct manipulation, aligning with `git replay`. **No user-visible behavior change**; improves maintainability.
  - **Fixup patch for multi-tip rejection**: Replaces the `--boundary`-based approach with object flags (`SQUASH_SEEN` and `SQUASH_TIP`) to count tips and fail if anything other than exactly one tip is found, improving error reporting. **Test coverage expanded** to verify the new behavior.
  - **Fixup patch**: Corrected boundary detection in `resolve_squash_range()` by replacing a check for `UNINTERESTING` commits with a check for `BOTTOM` commits, ensuring the code correctly identifies the base of the squash range.
  - **Bugfix patch (root commit)**: Explicitly rejects squashing down to a root commit by adding an early check in `resolve_squash_range()` to error out with a clear message. The test case verifies the rejection and explains the `--ancestry-path` complication.
  - **Bugfix patch (merge parents)**: Ensures the squashed commit retains the full parent list of the original merge commit, preserving merge topology. The test case verifies the fix.
  - **Ref-handling fixup patch**: Replaces the `oidset`-based interior-ref detection with a `ref-filter`-based approach to **protect all local branches descended from the squashed range** and **explicitly exclude detached HEADs**. **Error messages could be further polished** to list problematic branches explicitly.
  - **Noted that v11’s release reduced the urgency of his patches**, but his feedback remains a valuable reference for future improvements.
- **Junio C Hamano**: **Endorsed Patrick’s ref-handling proposal** (reject by default, advise `--update-refs`). **Requested template consistency improvement**, which Harald implemented in v8. **Signaled intent to queue the series** with "Will replace. Thanks." in response to v7. **Fully endorses D. Ben Knoble’s vision** of using `git history squash` and `git replay` as a faster, non-interactive backend for `git rebase --autosquash`, calling it "an ideal future" and confirming this as a **project-level aspiration**.
- **Patrick Steinhardt**: **Finalized ref-handling design** (reject by default, advise `--update-refs`). **Clarified merge commit handling** (reject external-parent merges; allow fully contained merges). **Defended input validation behavior** (accept non-ancestor ranges for consistency with other `git history` subcommands).
- **Matt Hunter**: **User feedback** raising **usability questions**:
  - **`amend!` message handling**: Pointed out an internal inconsistency where the command processed `amend!` semantics for validation but ignored `amend!` messages for message construction. **Now resolved** in v9 and clarified in v10.
  - **Reflog message truncation**: Identified the issue in v9; **resolved in v10** by recording the full revision expression.
  - **Rev-list option sanitization**: Raised the `revs.boundary` question in v9; **resolved in v10** by preserving the `boundary` invariant.
  - **v10 sign-off**: Explicitly stated "v10 looks good to me!", confirming that all his prior concerns had been addressed. This served as a **final technical sign-off** from a participant who had open questions in v9.
- **D. Ben Knoble**: **Forward-looking discussion** on **performance and architectural integration**. Speculates that `git history squash` could serve as a faster, non-interactive backend for `git rebase --autosquash` when combined with `git replay`. **Junio’s endorsement elevates this to a project-level aspiration**, with the `squash` command positioned as a foundational piece of a larger refactoring effort.

**Status**:
- Series is **posted as v11**, **functionally complete**, and **ready for integration**. Junio’s "Will replace" sign-off on v7, the resolution of all technical feedback in v10 and v11 (including Phillip Wood’s fixup patches, bugfix patches, and review patch), and **Matt Hunter’s explicit v10 sign-off** signal that the series is on track for the next release.
- **Resolved**: range resolution, merge commit handling (including merge-parent preservation), ref-handling design (including stricter branch protection and detached HEAD exclusion), input syntax flexibility, input validation (including stricter constraints from Phillip Wood’s v10 review, standardized `rev-list` option handling, corrected boundary detection, root-commit rejection, and multi-tip rejection), code maintainability (including the `first_parent_tree_oid()` and `commit_tree_ext()` refactorings, sequencer helper extraction, Phillip Wood’s refactoring patch, his fixup patches, and his bugfix patches), CLI/documentation style compliance, fixup/squash/amend handling, template consistency, sequencer helper refactoring, default message editing, `amend!` message handling, reflog message accuracy, rev-list option sanitization, test suite readability, documentation clarity, **all prior usability concerns from v10**, and **the usability debate over default message editing**.
- **Open questions**:
  - **Future integration**: Junio’s endorsement confirms that using `git history squash` and `git replay` as a backend for `git rebase --autosquash` is a **project-level aspiration**. This is no longer speculative; the `squash` command is a foundational piece of this effort.
  - **Message preservation rules**: **Phillip Wood’s review patch introduces stricter rules** for `squash!` and `amend!` commits, requiring their targets to be the first commit in the range or the message to be explicitly edited (`--reedit-message`). This conflicts with v11’s more permissive approach and may inform future iterations.
- **Integration status**: **Ready to queue**. Junio’s "Will replace" sign-off on v7, the resolution of all feedback in v10 and v11 (including Phillip Wood’s fixup patches, bugfix patches, and review patch), and **Matt Hunter’s v10 sign-off** indicate intent to merge. The series is **technically sound** and awaits Junio’s next "What's cooking" report for formal queuing in `next`.

---