Here is the updated dense AI thread summary incorporating the new email:

---

Proposal to fold commit ranges into a single commit, implemented as `git history squash` standalone subcommand. **Series v14 by Harald Nordgren is posted, functionally complete, and technically ready for integration**, fully incorporating all prior review feedback, Phillip Wood’s fixup patches, Junio’s test robustness fixup, and the structural decomposition requested in prior reviews. **Marked "Will replace" by Junio C Hamano in v7**, signaling intent to queue for the next release. **Finalized in v14**:
- **Trailer ordering**: Adds Phillip Wood’s `Signed-off-by:` trailer **above his `Helped-by:`**, followed by Harald’s `Signed-off-by:`, per Kristoffer Haugsbakk’s clarification. This reflects the chain of custody and resolves the last procedural blocker.
- **Test robustness**: Replaces the fragile `tr a-f A-F` pipeline in the `--no-edit`/`amend!` test case with `git rev-parse --short`, eliminating the dependency on lowercase hexadecimal output and avoiding a conflict with the in-flight `bc/restrict-hex-to-lowercase` topic. This aligns with Junio’s queued fixup and finalizes the test’s revision.
- **Structural decomposition**: The series remains split into **eight patches**, with the core implementation divided into five focused patches (4–8/8) isolating range validation, ref protection, commit creation, and message editing. This addresses Phillip Wood’s request for incremental review.
  - **Patch 1/8 (v14)**: Pure refactoring extracting `first_parent_tree_oid()` helper to centralize logic for resolving the tree of a commit's first parent (or the empty tree for root commits). Replaces three nearly identical code blocks in `builtin/history.c` with a single helper, behavior-preserving and uncontroversial. The helper takes a repository, a commit, and an output OID buffer, returning 0 on success or -1 on error after printing an appropriate message.
  - **Patch 2/8 (v14)**: Adds `message_template` parameter to `commit_tree_ext()` to enable default message editing. The parameter is optional; existing callers (`cmd_history_fixup` and `split_commit`) pass `NULL` to preserve current behavior. When provided, the template is used as the commit message body instead of the original commit’s message. This is a preparatory refactoring with no behavior changes, enabling later patches to seed the squashed commit’s message from multiple sources.
  - **Patch 3/8 (v14)**: Refactoring patch extracting sequencer helpers for squash-message formatting. Moves three static helpers from `sequencer.c` into public functions in `sequencer.h`—`add_squash_combination_header` (the “This is a combination of N commits” banner), `add_squash_message_header` (the “This is the 1st/Nth commit message” or “will be skipped” header), and `squash_subject_comment_len` (determines whether a subject line should be commented out)—and relocates the `todo_item_flags` enum so callers can distinguish `amend!` from plain `fixup!` after `todo_list_rearrange_squash`. The changes are behavior-preserving; the only caller is still `git rebase -i`, but the next patch will reuse these helpers in `git history squash` to give it the same autosquash-style template.
  - **Patch 4/8 (v14)**: **Establishes the revision input contract**: adds the command-line entry point, parses options (`--dry-run`, `--update-refs=(branches|head)`, `--[no-]edit`), and hands remaining arguments to `setup_revisions()` to accept standard revision ranges (`HEAD~3..topic`) and `rev-list` options. The function `setup_squash_revisions()` enforces required walk settings (`--reverse --topo-order`, no history simplification, `ancestry_path=1`, `boundary=0`) and rejects any range lacking a bottom commit (a commit outside the selected range to serve as the new base). The patch is intentionally minimal, returning an “unimplemented” error to defer graph validation and rewrite logic to later patches.
  - **Patch 5/8 (v14)**: **Range validation** via `resolve_squash_range()`, enforcing single-tip, no-root, and no-external-parent invariants. The function walks the selected commits in topological order, marking each with `SQUASH_SEEN` and tracking remaining tips with `SQUASH_TIP`. It rejects ranges that reach a root commit, have more than one tip, or include parents outside the range (e.g., a merge arm that entered from elsewhere). The validation logic is strict: it accepts only fully contained merges, ensuring the oldest commit’s parents are preserved and the single tip’s tree is used for the rewrite. The patch adds a new test script (`t/t3455-history-squash.sh`) with 65 lines covering error cases for empty ranges, single commits, root commits, multiple tips, and external parents.
  - **Patch 6/8 (v14)**: **Ref-handling safety mechanism** protecting local branches descended from the squashed range from being left dangling. The patch is a direct adoption of Phillip Wood’s review patch, now fully integrated into the series. It adds a new `advice.historyUpdateRefs` message that appears when `git history squash` refuses to proceed because one or more local branches would become unreachable. The error message lists the blocking branches in sorted order (short names only) and suggests `--update-refs=head` as a way to rewrite only the current branch. The check is performed early, before any objects are created, using Git’s existing `ref-filter` infrastructure to enumerate local branches whose tips are reachable from the range but whose histories do not include the range’s tip. The implementation matches the refs that the default rewrite mode would update, leaving tags and remote-tracking refs untouched. The patch updates the test suite with two new cases: one verifying the error message and branch listing (including sorted order), and another confirming the advice can be silenced with `advice.historyUpdateRefs=false`. The test setup creates a small commit graph with three branches (`feature`, `topic-1`, `topic-2`) and ensures the error message includes both topic branches in sorted order.
  - **Patch 7/8 (v14)**: **Commit creation with `--no-edit`**, implementing the core squash logic with autosquash handling and merge-parent preservation. The patch walks the selected range in topological order, resolves autosquash markers (`fixup!`, `squash!`, `amend!`) using a subject-to-flags map, and constructs the squashed commit’s message by either preserving the oldest commit’s message or incorporating the last applicable `amend!` body. It enforces strict rules: `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and markers whose targets lie outside the selected commits are rejected. A range made entirely of related markers can still be consolidated, with the last `amend!` message supplying the final commit message. The patch ensures reflog messages record the full revision expression and maintains dry-run and `--update-refs` behavior. New helpers include `is_autosquash_subject`, `skip_one_autosquash_prefix`, `truncate_message_to_subject`, `squash_amend_message`, `squash_squash_message`, `squash_check_can_autosquash`, `squash_check_autosquash_subject`, and `squash_check_subject`. The test suite expands coverage to 633+ lines, validating autosquash handling (nested fixups, `amend!`/`squash!` interactions), merge commit folding (single-base, interior, and octopus merges), ref-handling edge cases (tags, remote-tracking refs, branches pointing into the range at merges), and error conditions (external parents, root commits, multiple tips).
  - **Patch 8/8 (v14)**: **Message editing**, implementing the default editor workflow with autosquash-style template generation. The patch records the selected commits as a `todo` list, rearranges it using Git’s existing sequencer machinery, and builds an editor template that groups `fixup!`, `squash!`, and `amend!` commits under their targets. The template follows the same rules as `git rebase -i --autosquash`: `fixup!` messages are commented out entirely, `squash!` messages retain their bodies with only the marker subject commented, and `amend!` replaces its target’s message unless a preceding `squash!` requires both bodies. The `--no-edit` flag skips the editor, using the oldest commit’s message (or the last `amend!` targeting it). The patch adds the `build_squash_message` helper to `builtin/history.c`, updates `Documentation/git-history.adoc` to document the behavior, and extends the test suite (`t/t3455-history-squash.sh`) with 200+ lines of coverage for editor interaction, empty-message aborts, marker handling, and template formatting. Test cases verify the template matches `git rebase -i --autosquash`’s output, including edge cases like nested markers and `amend!`/`squash!` interactions.
- **Default message editing**: The command **opens an editor with the autosquash-style template by default**, matching `git rebase -i --autosquash`. The `--no-edit` flag skips the editor, keeping the oldest commit’s message (or the last `amend!` targeting it).
- **Stricter range validation**: Rejects ranges that reach a root commit, have more than one tip, or have external parents. **Fully adopts Phillip Wood’s v10 review patches 3 and 4**, including his fixup patch for multi-tip rejection (using object flags `SQUASH_SEEN` and `SQUASH_TIP`) and his bugfix patch rejecting root commits.
- **Merge-parent preservation**: The squashed commit **retains the full parent list of the original merge commit**, preserving merge topology. **Fully adopts Phillip Wood’s bugfix patch**.
- **Ref-handling**: **Protects all local branches descended from the squashed range by default**, rejecting operations that would leave any such branch dangling. The `--update-refs=head` flag restricts updates to the current branch. **Fully adopts Phillip Wood’s v10 review patch 5 and his ref-handling fixup patch**. **Error messages now include sorted branch names**.
- **Autosquash message handling**: **Fully adopts Phillip Wood’s review patch**, enforcing stricter rules for `squash!` and `amend!` commits, supporting nested fixups, and preserving `amend!` messages when squashing into a `fixup!` commit.

**Core problem**: Efficiently collapse a linear or merge-commit-containing range into its oldest commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based approach. **Finalized behavior**:
- Refs pointing to commits inside the squashed range are **detected and rejected by default for all local branches descended from the range**, with an `advice()` message suggesting `--update-refs=head`.
- Ranges whose oldest commit is a `fixup!`/`squash!`/`amend!` are **grouped under their targets in the default template**, matching `git rebase -i --autosquash`.
- **Rev-list options are fully validated and standardized**: The command enforces `--reverse --topo-order` and preserves the `boundary` invariant by setting options directly in the `rev_info` struct.
- **Merge-parent preservation**: The squashed commit retains the full parent list of the original merge commit.
- **`amend!` message handling**: When `--no-edit` is used, the command incorporates `amend!` messages targeting the first folded commit into the default squashed commit message.
- **Autosquash strictness**: `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and markers whose targets lie outside the selected commits are rejected. A range made entirely of related markers can still be consolidated, with the last `amend!` message supplying the final commit message.

**Open questions**:
- **None remaining**. All technical and procedural feedback has been addressed in v14, including:
  - Phillip Wood’s **missing `Signed-off-by:` trailer** (now added above his `Helped-by:`).
  - The **ref-filter display bug** (implicitly resolved by the refined ref-handling logic in v13/v14).
  - **Test robustness** (resolved by Junio’s fixup and v14’s `git rev-parse --short` replacement).
  - **Trailer ordering** (resolved by Kristoffer Haugsbakk’s clarification).
- **Future integration with `git rebase --autosquash`**: **Junio C Hamano and D. Ben Knoble endorse using `git history squash` as a faster, non-interactive backend for `git rebase --autosquash` when combined with `git replay`**. This remains a **project-level aspiration** but is not a blocker for integration.

**Implementation details**:
- **Files touched**: `builtin/history.c` (1,660+ insertions), `sequencer.c`/`sequencer.h`, `advice.c`/`advice.h`, `object.h`, `Documentation/git-history.adoc`, `t/t3455-history-squash.sh` (1,086+ lines).
- **New helpers**: `first_parent_tree_oid` (Patch 1/8), `resolve_squash_range`, `squash_check_subject`, `squash_amend_message`, `setup_squash_revisions`, `--[no-]edit`, `--update-refs=(branches|head)`, `ADVICE_HISTORY_UPDATE_REFS`, `SQUASH_SEEN`, `SQUASH_TIP`, `SQUASH_AMEND_TARGET`, `build_squash_message`, `add_squash_combination_header`, `add_squash_message_header`, `squash_subject_comment_len`, `is_autosquash_subject`, `skip_one_autosquash_prefix`, `truncate_message_to_subject`, `squash_squash_message`, `squash_check_can_autosquash`, `squash_check_autosquash_subject`.
- **Test coverage**: Range parsing, merge commit handling, descendant replay, authorship preservation, default message editing (including editor interaction, empty-message aborts, marker handling, and template formatting), ref-handling behavior (including error message formatting and sorted branch listing), `--dry-run` prediction accuracy, input validation (empty ranges, single commits, root commits, multiple tips, external parents), fixup/squash/amend edge cases (including nested fixups, `amend!`/`squash!` interactions, and external-target rejection), CLI/documentation style compliance, and reflog message accuracy.

**Resolved technical concerns (v4–v14)**:
1. **Input validation**: Handled by `resolve_squash_range()` (rejects single-commit ranges, empty ranges, non-range arguments, multiple bases, malformed histories, ranges reaching a root commit, ranges with more than one tip, and ranges with external parents).
2. **Range resolution**: The base is always the oldest in-range commit’s parent; any other boundary commit is treated as evidence of multiple bases and rejected.
3. **Merge commit handling**: Rejects merges with external parents or more than two parents (octopus merges) but allows fully contained merges.
4. **`fixup!`/`squash!`/`amend!` commits**: Folded like any other commit, with markers grouped under their targets in the default template. `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and markers whose targets lie outside the selected commits are rejected.
5. **Code maintainability**: Patches 1–3/8 refactor existing code; Patches 4–8/8 implement the core functionality in a decomposed structure.
6. **Ref-handling design**: Rejects operations if any local branch descended from the squashed range would be left dangling, with an `advice()` message guiding users to `--update-refs[=head]`. The error message lists blocking branches in sorted order and can be silenced with `advice.historyUpdateRefs=false`.
7. **Default message editing**: Implements the feature using a template that matches `git rebase -i --autosquash`.
8. **Template consistency**: Resolved by adopting a template that matches `git rebase -i --autosquash`.
9. **CLI/documentation style**: Addressed Junio’s feedback by moving dashed options before `<revision>...` in the SYNOPSIS.
10. **`amend!` message handling**: Incorporates `amend!` messages targeting the first folded commit into the default squashed commit message and the editor template.
11. **Reflog message accuracy**: Records the full revision expression to preserve user intent.
12. **Rev-list option sanitization**: Preserves the `boundary` invariant and standardizes option handling via direct `rev_info` struct manipulation.
13. **Test suite robustness**: Junio’s fixup and v14’s `git rev-parse --short` replacement resolve fragility issues in the `--no-edit`/`amend!` test case.
14. **Trailer ordering**: Phillip’s `Signed-off-by:` now appears above his `Helped-by:`, followed by Harald’s `Signed-off-by:`.

**Design resolution**:
- **Default behavior**: Reject the operation if any local branch descended from the squashed range would be left dangling, with an `advice()` message guiding users to `--update-refs[=head]`.
- **`--update-refs` flag**: Supports `=head` to retarget only the current branch to the squashed commit.
- **Input syntax**: Accepts multiple arguments (parsed like `git rev-list`), supporting compound ranges such as `HEAD~3.. ^topic`.
- **Merge commit handling**: Rejects merges with external parents or more than two parents but allows fully contained merges.
- **Input validation**: Uses shape-based validation to reject malformed histories and standardizes `rev-list` option handling.
- **Default message editing**: Opens an editor with the autosquash-style template by default, matching `git rebase -i --autosquash`.
- **`amend!` message handling**: Incorporates `amend!` messages targeting the first folded commit into the default squashed commit message and the editor template.
- **Autosquash strictness**: `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and markers whose targets lie outside the selected commits are rejected.
- **Reflog message accuracy**: Records the full revision expression to preserve user intent.

**Participant positions**:
- **Harald Nordgren**:
  - **Technical contributions**: Addressed all feedback in v14, including:
    - Adding Phillip Wood’s `Signed-off-by:` trailer above his `Helped-by:`.
    - Replacing the fragile `tr a-f A-F` pipeline with `git rev-parse --short`.
    - Introducing the `first_parent_tree_oid()` helper (Patch 1/8) to centralize logic and improve maintainability.
    - Adding the `message_template` parameter to `commit_tree_ext()` (Patch 2/8) to enable default message editing while preserving existing behavior for `NULL` templates.
    - Extracting sequencer helpers for squash-message formatting (Patch 3/8) to enable consistent autosquash-style templates across commands.
    - Implementing the revision input contract (Patch 4/8) with strict `rev-list` option enforcement and bottom-commit validation.
    - Implementing **range validation** (Patch 5/8) with comprehensive test coverage for error cases (empty ranges, single commits, root commits, multiple tips, external parents).
    - Implementing **ref-handling safety** (Patch 6/8) with sorted branch listing, early validation, and `advice.historyUpdateRefs` guidance.
    - Implementing **commit creation with `--no-edit`** (Patch 7/8), including autosquash strictness, merge-parent preservation, and reflog accuracy.
    - Implementing **message editing** (Patch 8/8) with autosquash-style template generation, editor interaction, and 200+ lines of test coverage for marker handling, empty-message aborts, and template formatting.
  - **Process contributions**: Engaged with Phillip’s procedural demands by adding the missing trailer, resolving test robustness, and slowing down rerolls to await feedback.
- **Phillip Wood**: **Substantive review** identifying robustness, edge-case, and structural improvements. His latest email demanded:
  - **Slower iterations** (Harald complied by waiting for feedback before rerolling).
  - **Explicit replies to open questions** (Harald addressed the trailer and test robustness).
  - **Missing `Signed-off-by:` trailer** (now added in v14).
  - **Trailer ordering** (resolved by Kristoffer Haugsbakk’s clarification).
  - **Ref-handling safety** (now fully integrated as Patch 6/8, including sorted branch listing and `advice.historyUpdateRefs`).
- **Junio C Hamano**:
  - **Signaled intent to queue the series** with "Will replace. Thanks." in v7.
  - **Diagnosed procedural stall** caused by rapid rerolls and advised slowing down.
  - **Identified test fragility issues** and queued a fixup, now incorporated in v14.
  - **Clarified security context** for case-insensitive OID matching, finalizing the test’s revision.
- **Kristoffer Haugsbakk**: **Clarified trailer ordering**, resolving the last procedural ambiguity.
- **D. Ben Knoble**: **Endorsed future integration** with `git rebase --autosquash` via `git replay`.

**Status**:
- **Technical status**: Series is **posted as v14**, **functionally complete**, and **technically ready for integration**. All technical and procedural feedback has been addressed.
- **Process status**: **All procedural blockers resolved**. Phillip Wood’s `Signed-off-by:` trailer is now included, and the test robustness issue is fixed.
- **Integration status**: **Ready for integration**. Junio’s "Will replace" sign-off, the resolution of all feedback, and the inclusion of Phillip’s trailer indicate intent to merge. The series is **cooking in `seen`** pending Junio’s next "What's cooking" report.

---