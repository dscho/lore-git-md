This is a surface-level review of the first patch in the `git replay --linearize` series (v5 1/3). Junio points out a minor formatting nit: the `BUG()` message should not end with a newline, as Git’s error-reporting functions automatically append one. The rest of the patch — a straightforward refactoring that extracts `put_mapped_commit()` and renames `mapped_commit()` to `get_mapped_commit()` — is already approved and ready for integration. The feedback is purely stylistic and will be trivial to address in the next iteration.