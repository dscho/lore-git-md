This is a brief follow-up in the thread about the regression in **patch 7/8** of the `paint_down_to_common()` optimization series. Kristofer Karlsson confirms that the test suite *did* catch the bug—specifically, **`t6600-test-reach.sh` (test 12, `get_merge_bases_many`)**—but he missed running it locally before submitting the patch. The failure was later flagged by GitGitGadget’s CI, not by manual inspection.

The key takeaway is that the **test coverage is robust**: the regression (an unconditional generation-monotonicity check that breaks correctness when `min_generation` is not set) was automatically detected, and the test suite’s assertions for merge-base results (`commit-5-6` and `commit-4-7`) held firm. This validates the series’ existing edge-case tests (e.g., mixed finite/INFINITY generation numbers, redundant candidates) and the new trace2 step-count instrumentation (patch 4/8), which ensures deterministic behavior across all commit-graph modes (`none`, `full`, `half`, `no-gdat`).

Kristofer’s reply closes the loop on Derrick Stolee’s question about test coverage, removing any doubt that the regression slipped through undetected. The focus now shifts to the **v4 rework**, where the author will either revert the problematic change or unify halt conditions while preserving correctness. The rest of the series (patches 1–6/8 and 8/8) remains unaffected and ready for merging.