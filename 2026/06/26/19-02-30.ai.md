This is a **refactoring patch** in Taylor Blau’s RFC series to combine `git repack --geometric` and `--cruft`. It is the sixth patch (6/10) in the series and removes the now-unused `pack_geometry_remove_redundant()` function and its declaration from `repack-geometry.c` and `repack.h`.

The patch is a direct follow-up to the previous patch (5/10), which routed geometric pack deletion through the shared `existing_packs` machinery instead of the geometry-specific helper. With that change in place, `pack_geometry_remove_redundant()` became redundant and is now removed. The diff deletes 44 lines from `repack-geometry.c` and 5 lines from `repack.h`, with no behavior changes—this is purely a cleanup step.

The patch is low-risk and uncontroversial, as it only removes dead code. It is part of the refactoring phase of the series (patches 1/10–5/10), which prepares the cruft pack machinery for the later integration of geometric and cruft repacks. The key context is that the `existing_packs` machinery now handles all pack deletion logic, including geometric repacks, so the geometry-specific helper is no longer needed.

No new functions, options, or tests are introduced here. The patch touches only the repack geometry subsystem (`repack-geometry.c` and `repack.h`) and has no cross-cutting impact. The removal is straightforward and aligns with the series’ goal of unifying repack logic under the `existing_packs` framework.