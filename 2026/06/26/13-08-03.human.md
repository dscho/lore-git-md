This is a **cleanup patch** in the ongoing `paint_down_to_common()` optimization series. It removes two wrapper functions—`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`—that became unused after the previous patch (5/8) introduced `struct paint_state` and replaced the old `nonstale_queue` logic. The core `nonstale_queue` functions remain in place for `ahead_behind()`, which was deliberately left untouched to avoid coupling.

The patch is **mechanical and low-risk**: it deletes 18 lines of dead code with no behavior change. The commit message is clear and concise, explaining why the functions are now redundant. This is a textbook example of a follow-up cleanup—no controversy, no new functionality, just removing what’s no longer needed after a refactoring.

For readers tracking the series, this is the sixth of eight patches in v3, which is itself a post-merge follow-up to the already-landed v2. The series as a whole is well-motivated (100-1000x speedups for asymmetric merge-base queries) and thoroughly tested, with deterministic step-count assertions across all commit-graph modes. This particular patch is a minor but necessary housekeeping step that keeps the codebase tidy. No further review is needed beyond verifying the functions are truly unused.