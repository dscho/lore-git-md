This is a surface-level review of the first patch in the v5 `git replay --linearize` series. Junio notes a single style nit: the `BUG()` message in the new `put_mapped_commit()` helper should not end with a newline, as the `BUG()` macro already appends one. The rest of the patch — a straightforward refactoring that extracts commit-mapping logic into a helper function — is approved. This patch remains uncontroversial and ready for integration. The feedback is minor and will be addressed in the next iteration.