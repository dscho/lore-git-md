This thread is now complete. Tian Yuchen’s patch series to move the global `excludes_file` variable into `struct repo_config_values` has been merged into Junio C Hamano’s `next` branch, marking the end of a six-version journey. The work eliminates a global variable as part of Git’s broader libification effort, replacing it with a heap-allocated field and a dedicated getter, `repo_excludes_file()`. The series followed a three-phase migration plan—silent early return, then a `BUG()` assertion, and finally no guard at all—proposed by Christian Couder and endorsed by Junio, who insisted that the final phase (removing the guard) be completed before merging. That requirement is now met, and the code is integrated.

The patches touch the ignore-file subsystem, XDG config resolution, and repository initialization, but introduce no user-visible changes. Memory management is handled via an extended `repo_config_values_clear()` helper, though a `NEEDSWORK` comment notes that submodule support for `excludes_file` and `attributes_file` is still pending. All prior feedback—squashing patches, resolving an unused parameter, and aligning with the guardrail plan—has been addressed, and the series passed CI without issues. The work sets a precedent for migrating other lazy-loaded repository fields, particularly `attributes_file`, and reinforces the pattern of centralized initialization checks in `repo_config_values()`. With the merge into `next`, the thread is closed.