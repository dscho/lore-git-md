Here’s the human-readable summary for a developer catching up on the thread:

This thread is a refactoring series by Tian Yuchen to eliminate the global `excludes_file` variable by moving it into `struct repo_config_values`, part of Git’s ongoing libification effort. The v2 series has been under review, with two key issues blocking progress: a build-breaking unused parameter in the new `repo_excludes_file()` getter (flagged by SZEDER Gábor) and a design dispute over a temporary guard in the getter and cleanup functions (raised by Junio C Hamano). The guard, which bypasses `BUG()` assertions for uninitialized submodules, was previously approved but is now contested as masking bugs.

Junio has since proposed a pragmatic fix for the build issue—marking the unused `repo` parameter as `UNUSED` in the first patch and removing the annotation in the second patch when the parameter is actually used. He also floated the idea of squashing the two patches for clarity, though no decision was made. The series is now at a crossroads: Tian must address both the build issue and the guard design before the patches can move forward. No other reviewers have weighed in with substantive feedback, leaving the discussion focused on Junio’s technical objections and SZEDER’s build fix. The outcome will shape how Git handles per-repository configuration in future libification work.