This is a **CI/platform-compatibility** discussion follow-up in the thread about macOS CI hangs in `t5551` and `t5559`. Junio C Hamano briefly endorses Patrick Steinhardt’s and Jeff King’s (Peff) shared stance against client-side mitigations or retry logic for flaky tests, reinforcing the project’s long-standing preference for addressing flakes at their source. He also acknowledges Patrick’s proposal to **increase Apache’s timeout value** as a potential workaround for the underlying Apache `mod_http2` bug (70131), which causes HTTP/2 stalls or `curl 18` mid-transfer aborts during `ls-refs` advertisements of 100,000 loose refs. Junio’s reply is minimal but signals agreement with the direction of the discussion, leaving the door open for further exploration of timeout adjustments as a pragmatic, Git-side fix.

The email does not introduce new technical details or objections but serves as a lightweight maintainer endorsement of the current consensus: avoid masking flakes with retries or client-side workarounds, and instead focus on solutions that directly address the root cause—such as Apache’s 300-second timeout. The thread remains unresolved, with no concrete patch yet proposed for the timeout adjustment, but Junio’s input suggests this approach is likely to be acceptable if implemented cleanly. The next steps will likely involve testing the timeout increase and evaluating its impact on CI reliability.