This is the first patch in the v5 series introducing `--linearize` to `git replay`. It is a refactoring patch that extracts the logic for storing a commit mapping into a new helper function `put_mapped_commit()` and renames the existing `mapped_commit()` to `get_mapped_commit()` for symmetry. The changes are purely mechanical and do not alter behavior.

The patch touches only `replay.c`, moving 11 lines of code into a new static function and updating the single callsite. The motivation is to reduce the length and complexity of `replay_revisions()`, making the code easier to read and maintain. This refactoring was carried over from earlier versions of the series and has already been reviewed; it is uncontroversial and ready for integration.

No new functionality is introduced here, and there are no open questions or unresolved feedback. The patch is a straightforward cleanup that paves the way for the `--linearize` implementation in patch 3. It is part of the replay machinery subsystem and has no dependencies beyond the base commit (`ab776a62a785`). The change is small, well-scoped, and aligns with the project's preference for incremental refactoring.