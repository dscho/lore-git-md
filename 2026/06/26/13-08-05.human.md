This is the final patch in a well-executed performance optimization series targeting Git’s commit-reach subsystem. Patch 8/8 consolidates the `min_generation` termination check into `paint_queue_get()`, simplifying the main loop in `paint_down_to_common()` and ensuring the generation-monotonicity assertion is shared across all halt conditions. The change is mechanical and low-risk, moving logic that was previously duplicated in the loop body into a single location where it can be reused.

The patch also introduces `last_gen` into `struct paint_state`, eliminating redundant calls to `commit_graph_generation()` and ensuring the generation number is computed exactly once per dequeued commit. This is a small but meaningful efficiency improvement, and the consolidation makes the code easier to reason about. The generation-monotonicity BUG assertion now fires unconditionally for every dequeued commit, not just when `min_generation` is set, which hardens the code against future regressions.

The accompanying documentation update in `paint-down-to-common.adoc` adds a clear explanation of the generation cutoff condition, rounding out the technical deep-dive that was introduced earlier in the series. The new section is concise and well-integrated, making it easy for future contributors to understand why the walk terminates when it dequeues a commit below the `min_generation` threshold.

This patch is the capstone of a series that has already delivered dramatic performance improvements (100-1000x speedups for asymmetric queries) and thorough test coverage. The changes are internal to the commit-reach subsystem, with no on-disk format impact, and the series as a whole is ready for merging. The only loose end is a deferred follow-up to integrate `min_generation` more deeply into `paint_state`, but that is a minor cleanup and not a blocker. If you care about merge-base performance in large or shallow repositories, this series is worth reviewing.