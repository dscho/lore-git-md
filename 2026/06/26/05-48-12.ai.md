This is the second patch in Toon Claes's v5 `--linearize` feature series for `git replay`. It replaces the controversial enum-to-boolean refactoring from v4 with a targeted improvement: **detailed explanatory comments** and a **base-selection logic fix** in `pick_regular_commit()`.

The patch clarifies how `pick_regular_commit()` determines the commit to replay onto by:
1. Renaming the confusing `onto` parameter to `default_base` (a more accurate name for its fallback role).
2. Reordering function parameters to group `struct commit` arguments together.
3. Adding a **10-line explanatory comment** that documents the base-selection logic, including the special case for `REPLAY_MODE_REVERT` where commits are replayed in reverse order.
4. **Fixing a logic error** in `replayed_base` handling when rebasing multiple divergent branches with `--onto`. The new code explicitly sets `base = last_commit` during reverts, ensuring correct stacking.

The change is minimal (16 insertions, 4 deletions) and focused on `replay.c`. It directly addresses Junio Hamano's v4 callsite clarity concerns and Patrick Steinhardt's interface scope question by making the base-selection logic explicit and correct, rather than debating enum vs. boolean design. The fix for the `--onto` edge case (where `replayed_commits` lookups fail) was discovered during the refactoring work and is now verified by new test coverage in Patch 3/3.

This patch is **uncontroversial** and **ready for merging**—it improves code clarity without changing behavior, while fixing a subtle bug that would have affected complex rebase scenarios. The explanatory comments will help future maintainers understand the replay logic, particularly the revert-mode special case.