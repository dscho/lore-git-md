This is the second patch in Toon Claes’s three-part series introducing a `--linearize` option for `git replay`. The patch addresses feedback from the v4 review by **replacing a controversial boolean refactoring with detailed explanatory comments** and **fixing a logic error in base-commit selection** when replaying multiple divergent branches with `--onto`.

The core change is in `replay.c`: the `pick_regular_commit()` function now has clearer parameter ordering (struct commits grouped together) and a renamed `onto` parameter to `default_base`, which better reflects its role as a fallback when no replayed ancestor is found. The commit message and in-code comments explain the function’s behavior in both forward (`REPLAY_MODE_REPLAY`) and reverse (`REPLAY_MODE_REVERT`) modes, resolving Junio Hamano’s earlier callsite-clarity concerns.

A subtle but important fix is the handling of `base` in revert mode: the code now explicitly uses `last_commit` as the fallback, ensuring commits stack correctly when replayed in reverse order. This addresses Patrick Steinhardt’s v4 concern about interface scope and edge-case correctness. The patch is small (16 insertions, 4 deletions) and focused, touching only the internal mechanics of `pick_regular_commit()` without changing its external behavior.

The series as a whole is now technically complete, with this patch resolving the last architectural debate from v4. The `--linearize` feature is ready for merging, and Junio’s "Will merge to 'next'" approval is now actionable.