This is a **cleanup patch** in the `commit-reach` subsystem, part of the ongoing `paint_down_to_common()` optimization series (v3 6/8). The patch removes two unused wrapper functions—`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`—that were rendered obsolete by the introduction of `struct paint_state` in an earlier patch (v3 5/8). The core `nonstale_queue` functions remain in use by the `ahead_behind()` function, so this is purely a dead-code removal with no behavior change.

The patch is **mechanical and low-risk**: it deletes 18 lines of code that are no longer referenced anywhere in the codebase. The wrappers were originally introduced to deduplicate commits by checking the `ENQUEUED` flag, but the new `paint_state` counters (`p1_count`, `p2_count`, `pending_merge_bases`) now handle this logic directly in `paint_down_to_common()`. The removal aligns with the series’ goal of simplifying the codebase while preserving the existing functionality for `ahead_behind()`.

This is the sixth patch in an eight-patch series, and it follows the structural refactoring in v3 5/8. The series as a whole is **post-merge follow-up work**—all v2 patches are already merged, and v3 adds instrumentation, documentation, and cleanup. The patch touches only `commit-reach.c` and is **self-contained**; no test updates are needed because the removed functions were unused. The commit message is concise and accurately describes the change.

The patch is **ready for merging** and should not require further discussion. It is a straightforward cleanup that reduces technical debt without introducing risk.