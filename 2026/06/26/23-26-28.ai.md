This is a **CI/platform-compatibility** follow-up in the ongoing thread about macOS CI hangs in `t5551` and `t5559`. Michael Montalbo responds to Patrick Steinhardt’s proposal to increase Apache’s `Timeout` directive as a workaround for Apache bug 70131, which causes HTTP/2 stalls or `curl 18` mid-transfer aborts during `ls-refs` advertisements of 100,000 loose refs.

Michael confirms that increasing the timeout resolves the issue in practice, reporting successful test completion on artificially slowed CI runners with an extended timeout. He tested with an hour-long timeout (which he acknowledges is excessive) and suggests a more reasonable range of 5–10 minutes, though he does not propose a specific value. The experiment demonstrates that the approach is viable but leaves the exact timeout duration as an open question. No patch is included in this email, but the results support Patrick’s suggestion and shift the discussion toward implementation details.

The email reinforces the emerging consensus that adjusting Apache’s timeout is the most pragmatic solution, avoiding client-side mitigations or retry logic while directly addressing the root cause. The tone remains collaborative, with Michael deferring to the maintainers on the final timeout value. Key technical details for follow-up include the Apache `Timeout` directive in `t/lib-httpd.sh`, the `t5551`/`t5559` test scripts, and the GitHub Actions CI configuration. The thread is now focused on finalizing the timeout adjustment rather than revisiting alternative approaches.