This is the latest exchange in Toon Claes’s **`git replay --linearize`** feature series, now in its fourth iteration. The thread has narrowed to a **design debate** about whether the `--linearize` option should hardcode a single mode (flattening merges) or mirror `git rebase`’s full flexibility (three modes: linearize, replay merges, replay merges including cousins).

Toon responds to Patrick Steinhardt’s concern by **sketching a future roadmap** for `git replay`’s merge-handling options. He proposes four modes:
- Default: replay merges preserving topology (aligning with Johannes Schindelin’s merge-replay series).
- `--linearize`: flatten merges (the current patch).
- `--no-merges`: abort on merges (restoring the pre-patch behavior).
- `--replay-cousins`: replay all merges, including cousins (equivalent to `git rebase --rebase-merges=rebase-cousins`).

Toon argues that **command-line options should be designed for the future**, even if the internal implementation starts simple. He acknowledges Patrick’s point about extensibility but defends the current boolean-based approach as sufficient for now, noting that code can evolve later. He also accepts Patrick’s **surface-level nits** (lowercase BUG message, though he defers the hint suggestion).

The exchange leaves the **architectural question unresolved**: should the series expand to support all `git rebase` modes before merging, or is `--linearize` alone a reasonable MVP? Toon’s preference is to merge the current patch and iterate, while Patrick’s review suggests the interface may be too narrow. Junio Hamano’s earlier "Will merge to 'next'" approval remains contingent on resolving this debate, so a **v5 iteration** is likely. The discussion is constructive and focused on long-term design, not technical correctness.