This is the second patch in Taylor Blau’s RFC series to combine `git repack --geometric` and `--cruft`. It is a **refactoring** that extracts a small helper function, `locate_existing_pack()`, from the existing `existing_packs_retain_cruft()` function. The helper converts a pack basename to a `string_list_item` and will be reused in a later patch that introduces a new function operating on a different `string_list`.

The change is confined to `repack.c` and is purely internal: it moves the basename lookup logic into a reusable static function without altering any behavior. The commit message explains the motivation clearly—future code will need the same lookup—and the patch itself is straightforward, consisting of a mechanical extraction with no functional changes. This is a low-risk preparatory step in the series, and the code quality appears solid. No tests are added or modified, as the patch does not change observable behavior.