This is the fifth iteration of Harald Nordgren’s `git history squash` feature, now code-complete and ready for Junio’s final review. Phillip Wood’s reply here is a substantive review that surfaces several edge cases and usability concerns that remain unaddressed in v5.

Phillip’s key points: the command currently accepts single-commit ranges (e.g., `HEAD^!`) where there is nothing to squash, and it silently accepts ranges that are not ancestors of HEAD (e.g., `git history squash --dry-run origin/seen^2^!` from `master`), producing no error. He also notes that the command’s single-argument limitation prevents specifying revisions by message (e.g., `git history squash ^:/base :/tip`), and he asks for clarification on how the command handles `fixup!`, `squash!`, and `amend!` commits, whether users can reword the commit message, and what happens to branches pointing inside the squashed range.

The thread context shows that all prior technical concerns (range resolution, merge commit handling, ref safety, `--ancestry-path` adoption) were resolved in v5, but Phillip’s feedback suggests the implementation still has usability gaps and insufficient input validation. His questions about special commit markers (`fixup!`, etc.) and rewording are particularly important for users migrating from `git rebase -i`. The series appears solid under ideal conditions, but Phillip’s review indicates it may behave unexpectedly in common real-world scenarios.