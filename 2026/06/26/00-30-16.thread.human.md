Here’s a concise narrative summary for a developer catching up on the Git mailing list thread:

The June 2026 "What’s cooking in git.git" report from Junio C Hamano (#10) provides a snapshot of all in-flight topics as Git 2.55-rc2 enters deep feature-freeze. The tree is now locked down: only bugfixes and documentation updates will graduate from `next` to `master` until after the 2.55 release. There are 48 active topics, split into 8 new, 3 stalled, and 37 cooking.

Among the new topics, Patrick Steinhardt’s work dominates: his `ps/odb-drop-whence` and `ps/reftable-hardening` series refactor the object database and harden the reftable backend against corruption, while `ps/connected-generic-promisor-checks` makes connectivity checks backend-agnostic. Other notable additions include a GPG signature parsing fix (`ad/gpg-strip-cr-before-lf`), a `git-receive-pack` refactor to use ODB transactions (`jt/receive-pack-use-odb-transactions`), and a usability tweak suggesting typofixes for misplaced slashes in branch/push commands (`hn/branch-push-slip-advice`). Three stalled topics—config lock timeout, subcommand autocorrection, and conditional config on worktree paths—remain blocked pending author responses.

The cooking topics highlight ongoing architectural shifts. Patrick’s `ps/odb-source-packed` (already merged to `next`) and `ps/setup-drop-global-state` continue the push to eliminate global state, while `ps/refs-avoid-chdir-notify-reparent` fixes memory leaks in repository construction. Performance improvements include `kk/merge-base-exhaustion` (speeding up merge-base for one-sided histories) and `tb/midx-incremental-custom-base` (fixing bitmap reachability). Documentation updates clarify review practices (`wy/doc-clarify-review-replies`) and design critique handling (`jc/submittingpatches-design-critiques`). Open questions linger around `ps/cat-file-remote-object-info` (whether to expose more object metadata) and `hn/branch-delete-merged` (whether to add an opt-out config). The `ps/shift-root-in-graph` series, which indents root commits in `git log --graph`, may drop its peek-ahead logic if feedback remains critical.

Key dependencies center on Patrick’s ODB refactoring: `ps/odb-source-packed` is a prerequisite for four other series. The most active discussion points revolve around complexity trade-offs in remote object info and graph rendering, as well as the implicit fetch behavior in `hn/checkout-track-fetch`. With the freeze in place, expect minimal movement beyond bugfixes until 2.55 final ships.