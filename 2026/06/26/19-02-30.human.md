This is a refactoring patch in Taylor Blau’s RFC series to combine `git repack --geometric` and `--cruft`. It is the sixth patch in the ten-patch series (though the cover letter and thread summary reference eight patches, this email shows 10/10—likely a versioning discrepancy).

The patch removes the now-unused `pack_geometry_remove_redundant()` function and its declaration from `repack-geometry.c` and `repack.h`. The previous patch (5/10) routed geometric pack deletion through the shared `existing_packs` machinery, making this geometry-specific helper redundant. The change is purely internal and does not alter behavior; it simply cleans up dead code after the refactoring in patch 5/10.

The diff is straightforward: 44 lines removed from `repack-geometry.c` and 5 from `repack.h`. The commit message clearly explains the rationale and ties it to the prior patch. This is a low-risk cleanup that follows logically from the earlier refactoring, and it aligns with the series’ goal of unifying repack logic. No controversy or open questions are evident here—it’s a mechanical follow-up to a well-motivated change.