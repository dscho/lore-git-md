This is a **substantive review** of Patrick Steinhardt’s ODB refactoring patch (1/2 in the series), focusing on the new `prepare()` callback’s design and implementation. Toon Claes raises two technical questions about the flag-based API and downcasting behavior, suggesting potential edge cases or inconsistencies in the patch.

The review centers on two key points:
1. **Downcasting in backends**: Toon notes that the patch moves downcasting (e.g., `odb_source_packed_downcast`) into backend implementations but doesn’t document this shift in the commit message. While he acknowledges it "makes sense," he implies the change could benefit from explicit justification, especially since the old `reprepare()` API relied on direct calls to backend-specific functions (e.g., `odb_source_packed_prepare()`).

2. **Flag usage in `odb_source_packed_prepare()`**: Toon questions why the patch omits `ODB_PREPARE_FLUSH_CACHES` in some calls (e.g., `find_pack_entry()`) where the old code *did* flush caches. He also asks why the `packed` backend’s `read_object_info()` callback now uses the flag where it previously called `odb_source_reprepare()` directly, hinting at a possible behavioral regression or inconsistency. His tone suggests these are not mere nits but potential correctness issues.

The review is **not surface-level**—it engages with the patch’s technical design and edge cases—but stops short of proposing fixes or testing the changes. The questions are precise (citing specific lines and functions) and highlight areas where the patch’s behavior might diverge from the original intent. This feedback could prompt Patrick to clarify the commit message or adjust flag usage to preserve existing semantics. No prior discussion of these points appears in the thread context.