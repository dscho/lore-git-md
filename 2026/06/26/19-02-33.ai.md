This is a **refactoring patch** in the RFC series to combine `git repack --geometric` and `--cruft` modes. It is the seventh patch (7/10) in the series and part of the second phase, which prepares for the introduction of the new `--stdin-packs=follow-reachable` mode in `git pack-objects`.

The patch extracts the core pack enumeration logic from `stdin_packs_add_pack_entries()` into a new helper function, `stdin_packs_add_all_pack_entries()`. The original function now delegates to this helper after building the list of packs to process. This refactoring is purely internal and does not change behavior; it is a preparatory step for the next patch (8/10), which will introduce an alternate code path for `--stdin-packs=follow-reachable`. That mode will determine the set of objects to include via a reachability walk rather than eagerly adding all objects from the selected packs.

The change is confined to `builtin/pack-objects.c` and is mechanical in nature: the extracted helper takes the list of pack keys and the revision context as arguments, while the original function retains the responsibility for building and sorting the pack list. The commit message is clear and concise, explaining the purpose of the refactoring and its role in the larger series. The patch itself is small (28 insertions, 21 deletions) and appears well-structured, with no obvious risks or edge cases introduced.

This patch is unlikely to attract controversy on its own, as it is a straightforward refactoring. However, the series as a whole is complex, and the later patches (particularly the introduction of `--stdin-packs=follow-reachable`) may face closer scrutiny due to their potential impact on reachability traversal and pack selection logic. The refactoring here is a necessary step to enable that work, and its correctness will be critical for the success of the subsequent patches.