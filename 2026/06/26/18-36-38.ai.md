This is the **v6 preparation update** for the `git history squash` feature series. Harald Nordgren acknowledges Junio C Hamano’s feedback about the command’s input syntax limitation (single `<base>..<tip>` argument) and signals intent to explore supporting multiple arguments in a future iteration. This email is a brief but important signal that the series is still active and that usability concerns raised in the thread are being taken seriously.

The core functionality of `git history squash`—collapsing a commit range into its oldest commit via a three-way merge—remains unchanged. The unresolved questions from v5 (handling `fixup!`/`squash!`/`amend!` commits, merge commits with external parents, and documentation of branch ref behavior) are not addressed here, but the thread context confirms Harald plans to tackle them in v6. Junio’s earlier observation about the command’s inability to combine multiple revision constraints (e.g., excluding a branch) is now on the radar, though no concrete design changes are proposed yet.

For a future AI summarizer, note the technical detail: the revision parser’s limitation with `:/<text>` syntax (treated as a regex) is now settled as a constraint, but the broader usability gap around input flexibility remains open. This email doesn’t introduce new code or tests—it’s purely a procedural update indicating the author’s responsiveness to feedback. The next step is Harald’s v6 submission, which should include stricter input validation, expanded test coverage, and documentation clarifications.