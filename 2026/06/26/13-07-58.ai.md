This is a **documentation patch** that adds a technical deep-dive on the `paint_down_to_common()` algorithm, part of the **v3 post-merge follow-up series** optimizing merge-base calculations. The new document (`Documentation/technical/paint-down-to-common.adoc`) explains the algorithm’s purpose, its two use cases (finding all merge bases vs. ancestry checks), the paint-walk mechanics, generation-number regions (finite vs. INFINITY), and termination conditions. It also clarifies the role of the `nonstale_queue` and why the walk can safely terminate once all queued commits are stale.

The patch is **authored by Kristofer Karlsson** (a co-signer of the original optimization) and touches four files: the new `.adoc` document, `Makefile`/`meson.build` to include it in the build, and a small update to `commit-reach.c` to add a cross-reference in the function’s doc comment. The document is **comprehensive and well-structured**, covering both the algorithm’s high-level logic and low-level details like queue ordering and paint-flag propagation. It fills a gap in Git’s technical documentation, as the commit-reach subsystem previously lacked a dedicated explanation beyond code comments.

This is **not a code change**—the diff in `commit-reach.c` is purely a doc-comment tweak to point readers to the new document. The patch is **low-risk and uncontroversial**, addressing a long-standing need for clarity in a performance-critical subsystem. The timing (post-merge follow-up) suggests it was deferred to avoid bloating the original optimization series, but the content is **directly tied to the recent `paint_down_to_common()` changes** and will help reviewers and future contributors understand the new termination logic.

The document is **technically precise** but accessible, using diagrams (ASCII art for generation regions) and clear sectioning to separate concepts like "Use cases," "Algorithm," and "Termination." It also cross-references related documentation (`commit-graph.adoc`), reinforcing Git’s documentation ecosystem. No prior knowledge of the optimization series is required to benefit from it, but readers familiar with the v2/v3 patches will recognize the terminology and edge cases discussed.

This patch is **ready to merge** and should be uncontroversial. It’s a **valuable addition** for anyone working on merge-base calculations, commit-graph traversals, or performance-critical reachability queries. The only open question is whether the document will need updates if the `paint_down_to_common()` implementation evolves further (e.g., the deferred `min_generation` integration mentioned in the thread summary), but that’s a minor concern—technical docs are expected to evolve alongside the code.