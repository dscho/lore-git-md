This is a short clarification from Junio C Hamano in the ongoing `git history squash` feature discussion. Junio weighs in on the syntax debate between Harald Nordgren and Phillip Wood about how to specify commit ranges for the new command.

The key point: Junio confirms Phillip’s observation that `^:/base..:/tip` is invalid because Git’s revision parser treats everything after the first `:/` as a regex. He offers a workaround—`HEAD^{/base}..HEAD^{/tip}`—which avoids the regex issue by using the `^{/pattern}` syntax for commit searches. However, he acknowledges that even this workaround doesn’t fully address Phillip’s broader concern: the command’s current design (accepting only a single `<base>..<tip>` argument) prevents users from adding extra constraints, like excluding commits already in `main`.

This exchange doesn’t propose new code or design changes, but it reinforces the need for stricter input validation in the next version (v6) and may influence how the command’s syntax is documented. The core functionality (three-way merge, ref-handling, and `--reedit-message`) remains unaffected. For now, the discussion is still focused on usability—specifically, whether the command should support more flexible range specifications or stick with the simpler but more restrictive single-argument form.