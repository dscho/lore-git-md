```
Thread identity
Subject: [PATCH v2] history: fix file stream leak and inefficient file handling in reword
Series version: standalone patch (v2)
Originating author: Junio C Hamano
Thread type: bugfix patch

Core problem or goal
Fix two related issues in `git history --reword`: (1) an inefficient file-handling pattern in `fill_commit_message()` that wrote a buffer to a file, flushed it, then reopened the file in append mode; (2) a file stream leak that could cause problems on Windows by leaving the file handle open before launching the external editor. The patch also restores explicit error checking on the initial `fwrite()` call, addressing a reliability concern raised by Patrick Steinhardt in the v1 review. The broader error-handling gap in `wt_status_print()` is acknowledged but deferred as out of scope.

Current status
Patch v2 is ready for integration. The original goals (stream leak and Windows compatibility) are uncontested, and the reliability concern (restoring error checking on `fwrite()`) has been fully addressed. Patrick Steinhardt's latest review confirms the patch now ensures the commit message (critical data) will either be fully written or trigger a `die()`, while the status information (non-critical) remains at risk of truncation in `wt_status_print()`. The broader `wt_status_print()` issue is explicitly deferred.

Key technical details
Files touched: `builtin/history.c`
Function modified: `fill_commit_message()`
New behavior: opens file once in write mode, writes buffer directly via `fwrite()` with explicit `die()` on failure, closes stream before editor launch
Old behavior: wrote buffer via `write_file_buf()` (which dies on failure), flushed, reopened in append mode, left stream open
Platform impact: fixes Windows compatibility issue (open file handles blocking other processes); error-checking gap affects all platforms but is now addressed for the initial write
Test coverage: no test failures or edge cases mentioned; fix is well-contained and includes explicit error checking on the initial write
Symbols/functions: `fill_commit_message()`, `fwrite()`, `fclose()`, `wt_status_print()`

Open questions and loose ends
- The broader error-handling gap in `wt_status_print()` remains unresolved but is explicitly out of scope for this patch. No reviewers have suggested it should be addressed in a follow-up.

Participant positions
Junio C Hamano: author, describes patch as "tested and merge-ready" for the original issues (stream leak and Windows compatibility); incorporated Patrick's feedback to restore error checking on `fwrite()` in v2
Johannes Schindelin: credited with diagnosing the original problem (no explicit review trailer)
Patrick Steinhardt: **substantive review** identifying the error-checking regression in v1 and confirming v2 fully addresses his concern. Explicitly approves the patch, noting that while the `fwrite()` error check is technically redundant (errors would be caught by `fclose()`), it doesn’t hurt to include it. Position is that the commit message (critical data) will either be fully written or trigger a `die()`, while the status information (non-critical) remains at risk of truncation in `wt_status_print()`

Related work
Issue introduced in d205234c (January 2026); Junio originally suggested the fix in GitGitGadget PR #2158. The error-checking concern is now resolved in v2, but the broader `wt_status_print()` issue remains open. No dependencies or related in-flight topics.
```