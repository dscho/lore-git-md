Here’s the human-readable summary of the thread so far:

This thread is an RFC patch series from Taylor Blau that aims to let `git repack` combine `--geometric` and `--cruft` modes, which are currently incompatible. The goal is to allow repositories to maintain a geometric progression of reachable packs while simultaneously collecting unreachable objects into cruft packs, streamlining maintenance workflows. The series introduces `--stdin-packs=follow-reachable` to enable precise reachability filtering during pack generation and fixes a critical correctness issue by adding `--refs-snapshot=<path>` to ensure consistency between `git pack-objects` and the MIDX bitmap writer.

The series is now complete with all 10 patches submitted, but it remains in the RFC stage and unmerged. The final patch (10/10) integrates the preparatory work, enabling `--geometric` and `--cruft` to work together by relaxing the `ALL_INTO_ONE` implication of `--cruft` when `--geometric` is given. The series touches core repack machinery, pack-objects, MIDX retention logic, and cruft pack generation, and includes 8 new tests in `t/t7704-repack-cruft.sh` to verify functionality.

So far, the only review has come from Junio C Hamano, who provided surface-level feedback on patch 4/10 and a more substantive review of patch 8/10. In the latter, Junio identified a potential correctness issue in the two-phase traversal logic for `--stdin-packs=follow-reachable`. The current implementation risks retaining unreachable tags and objects reachable only from those tags, which could incorrectly include unreachable objects in the output pack. This is a non-trivial issue that could affect the core functionality of the series, and addressing it may require rethinking the pre-walk phase to verify reachability from refs before adding tags to the pending list.

The series is complex, and its integration of geometric and cruft logic may surface additional edge cases, particularly around MIDX retention, reachability traversal, and pack selection. Taylor’s upcoming role change could also impact follow-up bandwidth. While patches 1/10–9/10 are low-risk refactorings or preparatory work, the final patch (10/10) may attract further scrutiny due to its complexity and potential impact on repack workflows. No other reviewers have weighed in yet, so the series remains untested beyond Junio’s feedback.