This is a surface-level review of the ninth patch in the v14 series implementing `git cat-file --batch-command` support for remote object metadata queries. Karthik Nayak asks a clarification question about the server capability advertisement format in `serve.c`.

The review focuses on the design intent behind the `object_info_advertise()` function, specifically how future metadata features (beyond the current "size" support) will be advertised to clients. Karthik notes the current implementation appends only "size" to the `value` strbuf when `advertise_object_info` is enabled, and asks whether additional features will follow the same pattern and what format they will use. This is a forward-looking question about protocol extensibility, not a critique of the current patch, which remains unchanged and uncontroversial.

The question is narrowly scoped to the wire protocol design: whether future features will be space-separated in the same `value` strbuf, or if a different delimiter or structure will be used. The answer will inform how clients parse the capability string and whether the current implementation needs adjustment to accommodate future growth. No code changes are suggested or implied; the review is purely informational.