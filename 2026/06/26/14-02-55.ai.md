This is a **design clarification** in the ongoing `git history squash` feature discussion, specifically addressing input syntax flexibility. Junio C Hamano weighs in on Phillip Wood’s concern about the command’s single-argument limitation (`<base>..<tip>` form) by demonstrating alternative revision syntaxes that could support more complex use cases.

Junio acknowledges Phillip’s point that the current design restricts users to a single range argument, which may not accommodate scenarios requiring additional constraints (e.g., excluding commits already in `main`). He provides a workaround using `HEAD^{/pattern}` syntax to match commits by regex while maintaining the `..` range operator, but notes this still doesn’t solve the broader issue of combining multiple revision constraints. His example (`HEAD^{/base}..:/tip ^main`) illustrates a hypothetical use case where a user might want to squash a range while explicitly excluding a branch, which the current implementation cannot handle.

The exchange doesn’t propose code changes but reinforces the need for **input validation and documentation clarity** in the next iteration (v6). For a future AI summarizer, note the technical detail: Git’s revision parser supports `{/pattern}` for regex matching within commit histories, but this doesn’t resolve the core limitation of the command’s single-argument design. The discussion remains focused on usability, with no impact on the underlying three-way merge or ref-handling logic. Junio’s input suggests that while the current design is functional, it may need to evolve to support more complex workflows.