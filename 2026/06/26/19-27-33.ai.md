This is a procedural follow-up in the build system quieting effort for gitk and git-gui. Harald Nordgren asks Johannes Sixt whether the now-merged gitk patch (and the effectively superseded git-gui patch) can be expected to land in `master`. The thread context confirms the series is technically complete: the gitk portion is already merged into Sixt’s tree, and the git-gui portion is rendered moot by Sixt’s pre-existing `hn/silence-make-s` branch in git-gui.

Key technical details to carry forward:
- **Files touched**: `gitk-git/Makefile` (merged), `git-gui/Makefile` (superseded)
- **New symbol**: `QUIET_MSGFMT` helper (gitk only)
- **Behavior change**: `make -s` now produces fully silent translation-catalog generation in gitk and git-gui, matching core Git’s precedent (commit 2f12b31b746c)
- **Related work**: Johannes Sixt’s `hn/silence-make-s` branch in git-gui already implements equivalent changes, making the git-gui patch unnecessary

The question is purely about integration timing; no technical discussion remains. The outcome depends on Sixt’s release cycle for gitk and git-gui, which operate independently of core Git’s `master`.