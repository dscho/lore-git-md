This is a **regression report** from Junio C Hamano identifying a **critical bug** in the v3 series. The failure occurs in `t6600-test-reach.sh` (test 6600.12, `get_merge_bases_many`), where the newly widened generation-monotonicity BUG assertion in `paint_down_to_common()` fires unconditionally, breaking correctness when `min_generation` is not set. The test expects `get_merge_bases_many(A,X)` to return commits `commit-5-6` and `commit-4-7`, but the assertion triggers first, causing the test to fail.

The regression is **directly tied to patch 7/8** (new in v3), which implements the side-exhaustion optimization but **incorrectly widens the BUG assertion** to fire for any generation ordering violation, not just when `min_generation` is set. The assertion now enforces correct generation ordering for all callers, but this is **too strict**: some callers (like `get_merge_bases_many`) do not set `min_generation` and thus do not require the same invariants. The test failure confirms the test suite robustly catches the regression, as predicted in the thread AI summary.

Junio’s report is **actionable and high-priority**: he has already ejected the series from his `seen` integration branch, and the author (Tian Yuchen) and Kristofer Karlsson have acknowledged the regression and plan to rework the logic for v4. The fix will likely involve either reverting the problematic change (simpler) or fully unifying halt conditions while preserving correctness (cleaner). The regression does **not affect the rest of the series** (patches 1-6/8 and 8/8), which remain stable and ready for merging once the regression is resolved.

Key technical details:
- **Subsystem**: commit-reach (`paint_down_to_common()`).
- **Files touched**: `commit-reach.c` (patch 7/8).
- **Behavior change**: The generation-monotonicity BUG assertion now fires unconditionally, breaking callers that do not set `min_generation`.
- **Test failure**: `t6600-test-reach.sh` test 12 (`get_merge_bases_many`), which expects `commit-5-6` and `commit-4-7` but hits the BUG assertion first.
- **Root cause**: The side-exhaustion optimization depends on correct generation ordering, but the assertion was widened without accounting for callers that do not set `min_generation`.
- **Impact**: The regression **blocks merging** of the entire series until fixed. The rest of the series (patches 1-6/8 and 8/8) is unaffected and remains ready for review.

The report is **minimal but sufficient**: it provides the failing test case, the exact assertion failure (`bad generation skip 9 > 8`), and the commit OID where the violation occurs. No additional context is needed to diagnose the issue. The regression is **confirmed and reproducible**, and the fix is already planned for v4.