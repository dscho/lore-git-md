This is a substantive review of Harald Nordgren’s `[PATCH v2 0/2]` series adding a `--squash` option to `git rebase`. Phillip Wood, a respected contributor with deep expertise in the sequencer and rebase machinery, engages directly with the implementation’s efficiency and user experience.

Phillip’s main concern is performance: the current implementation picks commits one by one, which risks redundant conflict resolution if the base changes. He proposes an alternative approach that cherry-picks the entire range at once using `git merge-tree`, avoiding repeated conflict stops. He also suggests letting users edit the final commit message to reflect the squashed changes, and raises the question of how “amend!” commits should behave in this context.

The review is substantive: it identifies a concrete performance pitfall, proposes a technical solution, and discusses user-facing behavior. It does not test the code but clearly understands the code paths involved. The thread context shows that the architectural debate has shifted toward a `git history squash` subcommand, but Phillip’s feedback remains relevant regardless of where the feature lands. If the project proceeds with the rebase implementation, these points will need to be addressed.