This is the second patch in Toon Claes's three-patch series introducing the `--linearize` option to `git replay`. It is a refactoring patch that extracts a helper function and renames an existing one to improve code clarity in `replay.c`.

The patch adds `put_mapped_commit()`, a helper function that encapsulates the logic for inserting a commit into the `replayed_commits` khash OID map. It also renames the existing `mapped_commit()` function to `get_mapped_commit()` to create a clear getter/setter pair. This change is purely internal and does not alter behavior; it prepares the codebase for the `--linearize` implementation in patch 3/3 by making the commit mapping logic more readable and maintainable.

The diff touches only `replay.c`, moving 11 lines of mapping logic into the new helper and updating all call sites to use the renamed function. The commit message is concise and accurately describes the change. This refactoring is well-motivated: the `replay_revisions()` function is indeed lengthy, and extracting this logic makes the code easier to follow. The patch is part of a series that has already addressed all substantive feedback, and this specific change appears uncontroversial.

For a future AI summarizer, note the key technical details: the new `put_mapped_commit()` helper, the renamed `get_mapped_commit()` function, the khash OID map (`replayed_commits`), and the file modified (`replay.c`). The patch is a prerequisite for the `--linearize` option's merge commit handling in the next patch.