This is the sixth and final patch in the v2 series addressing Windows LLP64 compatibility for large objects (>4GB). It adds a test case to `t/t1007-hash-object.sh` verifying that the `clean` filter code path correctly handles files larger than 4GB on platforms where `size_t` is 64-bit.

The test reuses the 5GB file (`big`) from earlier test cases if it exists, or regenerates it using `test-tool genzeros`. It configures a trivial `null-filter` (a no-op `cat` filter) via Git configuration and `.gitattributes`, then verifies that `git hash-object` produces the expected hash for the filtered input. The test is marked `EXPENSIVE` and `SIZE_T_IS_64BIT`, ensuring it only runs on relevant platforms and is skipped otherwise.

This patch completes the test coverage for the series, exercising the last major code path (filtered input) that could be affected by the `unsigned long` to `size_t` conversion. The test is narrowly scoped to avoid writing to the object store, focusing solely on the filter interface's ability to handle large memory buffers. The expected hash (`large5GB`) matches the precomputed values used in earlier test cases, ensuring consistency across the test suite.

The patch is straightforward and well-motivated, addressing a specific edge case in the filter subsystem that could otherwise regress on Windows. It builds on the infrastructure established in earlier patches and maintains the series' focus on minimal, targeted changes. No substantive discussion or controversy is expected for this test addition.