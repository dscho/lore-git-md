This is a **documentation patch** in the ongoing discussion about improving `git rebase` user guidance. Junio C Hamano weighs in on the contested part of Harald Nordgren’s patch: whether to add `--abort` hints to the "failed exec" scenario during rebase.

Junio acknowledges Phillip Wood’s concern that exec failures (e.g., test failures) are usually minor and best resolved by continuing, not aborting. However, he frames the issue differently: for new users, offering an "escape hatch" like `--abort` can be reassuring when they’re stuck, even if experienced users rarely need it. He suggests the patch’s approach—adding `--abort` as a separate hint alongside the existing `--continue` advice—serves as a harmless "training wheel" rather than a workflow endorsement.

The message doesn’t resolve the debate but shifts the focus from workflow realism (Phillip’s argument) to user experience for beginners. Junio’s tone implies the patch is likely acceptable as-is, though the thread remains open for further input. The status-message changes (in `wt-status.c`) are still uncontroversial.