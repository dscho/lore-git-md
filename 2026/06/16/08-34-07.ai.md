This is a brief follow-up in the `git rebase --squash` thread, where Patrick Steinhardt (the author of the experimental `git history` command) endorses D. Ben Knoble’s architectural alternative. Patrick confirms that a `git history squash` subcommand would fit naturally into his planned roadmap for the `git history` tool and welcomes someone else to implement it first.

The email is short and decisive: it removes the last technical objection to the `git history` approach by establishing that the command is both appropriate and already on the radar. No new arguments are introduced, but the endorsement from the subsystem owner shifts the discussion’s center of gravity away from the rebase implementation and toward the `git history` design space. Future discussion is now likely to focus on how `git history squash` should behave rather than whether it is a viable path.