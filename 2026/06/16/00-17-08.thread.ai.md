SHA-1/SHA-256 interoperability work update from brian m. carlson. Core functionality now operational with passing test suite, covering protocol extensions for shallow clones, submodule handling, partial clones, and recursive delta resolution in `git index-pack` (reworked iteratively). Current limitations include filtered bundles, multi-pack index usage, packfile URIs, and edge cases; unresolved technical challenges involve object map batching and performance optimization. The 200+ patch series is not targeted for Git 3.0 due to scope and testing requirements, with the author open to others upstreaming the work. The branch remains available for testing but unstable, with no `Reviewed-by` or `Acked-by` tags.

Junio C Hamano raised two high-weight security/correctness concerns in response:
1. **Packfile URIs**: How will fetch/push traffic handle object name recomputation (a security measure) when packfiles are downloaded via URIs? Whether the interoperability layer will reuse the same logic or require a distinct approach.
2. **Large-object promisors**: Whether the server must possess complete history to provide object mappings, and if clients must trust server mappings without verification.

No other limitations or broader status (cleanup, performance, upstreaming) were addressed in this reply. Participant positions remain unchanged; the thread is now awaiting clarification on these two points.