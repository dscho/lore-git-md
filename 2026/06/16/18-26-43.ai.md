This is a **substantive review reply** in the GSoC path-formatting series. Phillip Wood responds to the author’s proposed v6 plan to remove `PATH_FORMAT_DEFAULT` from the shared API and instead use a local enum in `rev-parse.c`.

Phillip suggests an even simpler approach: **keep the existing `rev-parse` enums unchanged** and handle the conversion to `PATH_FORMAT_*` values inside `print_path()`. This avoids introducing a new local enum and preserves the current option-parsing logic, reducing churn. The author had already acknowledged that the necessary conversion logic exists in the codebase, so this adjustment should be straightforward.

The exchange confirms the design is now settled: the backward-compatibility quirk will remain confined to `rev-parse`, and the shared `append_formatted_path()` API will stay clean. No further objections are raised, and the author plans to send v6 shortly. This reply effectively clears the last technical hurdle before the series can graduate to `next`.