This is a brief but consequential reply in the `git rebase --squash` thread. Patrick Steinhardt, the architect of the experimental `git history` command, endorses D. Ben Knoble’s suggestion to move the squash functionality out of `git rebase` and into `git history`. He confirms that a `git history squash` subcommand aligns with his roadmap and invites others to implement it.

The message does not weigh in on the technical merits of Harald Nordgren’s current rebase implementation, but its effect is clear: it shifts the architectural discussion toward `git history` as the preferred home for this feature. This likely reduces the chances that the rebase-based approach will be merged, though the door remains open for Harald (or someone else) to rework the patch as a `git history` subcommand instead. The thread now hinges on whether anyone will pick up that task.