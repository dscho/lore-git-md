This is a follow-up in the `git cat-file` performance optimization thread, focusing on the trade-offs of René Scharfe’s opcode-based format representation. René responds to Jeff King’s (Peff) architectural concerns about scalability and maintainability.

René acknowledges Peff’s point about the overhead of storing literal characters in the opcode-based approach, noting that while the default format’s two literal characters are manageable, the design could become inefficient for formats with longer literal text. He briefly explores alternatives, such as an opcode for fixed-size strings or variable-length encoding, but concedes that such optimizations may not be worth the added complexity for Git’s current use cases. His response underscores the pragmatic balance between performance gains and code simplicity, particularly since the primary motivation for the optimization—speeding up Gitaly’s usage of `git cat-file`—may not justify over-engineering for broader scenarios.

The email also reflects René’s acceptance of Peff’s skepticism about the long-term viability of a linear opcode design, especially if Git ever unifies its formatting systems (e.g., `ref-filter`, `--pretty=format`). He agrees that the current approach, while effective for `cat-file`’s default format, may not generalize well to more complex formatting needs. The discussion remains grounded in the thread’s recurring theme: whether targeted micro-optimizations are preferable to a more ambitious, unified formatting architecture. René’s tone suggests he views the opcode-based design as a reasonable experiment but not necessarily the final direction.

Key technical details include the overhead of literal character storage (50% for opcode + payload vs. >90% for pointer + size) and the trade-offs between strbuf operations and opcode complexity. The reference to Gitaly highlights the real-world impact of these optimizations, though René notes that most users may not require such performance tuning. The thread’s open questions—whether to pursue deeper optimizations or a unified formatting system—remain unresolved, but this exchange clarifies that the opcode approach is unlikely to be the path forward.