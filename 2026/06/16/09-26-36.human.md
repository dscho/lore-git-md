This is the second patch in Toon Claes’s three-part series adding `--linearize` to `git replay`. It is a pure refactoring with no behavior change: the 31-line `replay_revisions()` function is split by extracting the logic that stores a commit mapping into a new static helper `put_mapped_commit()`, and the existing `mapped_commit()` helper is renamed to `get_mapped_commit()` so the two functions form a clear getter/setter pair. The patch touches only `replay.c` and is a straightforward code hygiene improvement that paves the way for the `--linearize` implementation in patch 3. All prior review feedback on this patch has been addressed; it is uncontroversial and ready for final review.