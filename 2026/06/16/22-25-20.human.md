This is the sixth patch in Matt Hunter’s 7-patch series implementing the global `fetch.followRemoteHEAD` configuration option. It introduces the core configuration variable and wires it into the fetch machinery, following the established `fetch.prune`/`remote.*.prune` precedence pattern.

The patch adds `fetch.followRemoteHEAD` as a global fallback setting that applies to any remote lacking an explicit `remote.<name>.followRemoteHEAD` configuration. It supports the same values as the per-remote setting—`never`, `create`, `warn`, and `always`—except for `warn-if-not-$branch`, which remains remote-specific. The implementation includes a new `FOLLOW_REMOTE_UNCONFIGURED` enum state to distinguish between an unset configuration and an explicit system default, ensuring the built-in default (`create`) only applies when neither setting is defined.

The documentation updates clearly explain the relationship between the global and per-remote settings, and the advice messages have been reworded to reflect the new configuration hierarchy. The patch also adds comprehensive test coverage in `t5510-fetch.sh`, verifying feature parity between the two settings, precedence rules, and edge cases like dangling symrefs and `--quiet` interaction.

This is a well-executed implementation that addresses the configuration management pain point identified in earlier discussion. The changes are minimal and focused, and the test coverage is thorough, suggesting the series is now ready for final consideration. The only notable exclusion—the `warn-if-not-$branch` value at the global level—is a deliberate design choice, as that behavior is inherently tied to individual remotes.