This is the fifth patch in a six-patch series addressing Windows LLP64 compatibility for handling objects larger than 4GB. The series systematically converts object header lengths from `unsigned long` to `size_t` across core object-file code, and this patch adds a test case to verify that `git hash-object` (without any special options) correctly processes files larger than 4GB on platforms where `size_t` is 64-bit.

The patch adds a single test case to `t/t1007-hash-object.sh` that exercises the standard `git hash-object` code path with a 5GB file. It reuses the `big` file from earlier tests if it exists to avoid redundant generation, otherwise creates it using `test-tool genzeros`. The test is marked `EXPENSIVE` and `SIZE_T_IS_64BIT`, meaning it will only run on platforms where `size_t` is 64-bit and when the expensive test suite is explicitly requested.

This test complements the earlier test cases in the series that verified `--stdin` and `--literally` code paths. The series is now technically complete, with all production code changes already validated in Git for Windows production use. The test infrastructure added here is straightforward and focused, covering the final major code path for large object handling.

The patch is well-motivated and uncontroversial. It follows the established pattern from earlier tests in the series and doesn't introduce any new technical concerns. The test case is clearly written and maintains consistency with the rest of the test script. This appears to be a solid, incremental addition to the series' test coverage.