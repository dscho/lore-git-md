This is the final patch in Toon Claes' three-part series implementing `--linearize` for `git replay`. The patch introduces the core functionality that flattens merge commits into linear history by following only first parents, providing server-side equivalent to `git rebase --no-rebase-merges`.

The email responds to Junio C Hamano's substantive review of the test coverage. Toon confirms that the current implementation intentionally treats root commits differently from merge commits under `--linearize`—a divergence that was not caught in earlier testing due to an incorrect revision range in the test case. He acknowledges Junio's catch and commits to fixing both the test description and the range specification to properly verify root-commit behavior.

This exchange resolves the last open technical question in the series. The remaining work is mechanical: updating test descriptions and splitting overly long command lines for readability. The series has already incorporated all other review feedback from Junio, Elijah Newren, and Justin Tobler, including documentation improvements, expanded test coverage, and architectural refinements. With these final adjustments, the patch should be ready for merging.

The thread demonstrates the project's collaborative review culture, where even maintainer feedback is framed as questions seeking clarification rather than directives. The `--linearize` option now appears technically sound, well-tested, and aligned with the series' goal of providing a server-side alternative to client-side rebase operations.