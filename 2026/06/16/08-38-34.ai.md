This is the third patch in the `git replay --linearize` series, addressing Junio’s clarifying questions from his review of v2. The email confirms two intentional changes in response to feedback:

First, the patch now explicitly treats root commits differently from merge commits—root commits are replayed, while merges are dropped. This corrects a mismatch between the v1 implementation (which skipped both) and the author’s stated intent, and was caught by Junio’s test case that properly verifies root-commit behavior.

Second, the test description and command will be fixed to accurately reflect the range: `A..topic-with-merge` excludes the root commit `A`, so the test will no longer claim to replay “down to root.” The command will also be split across two lines for readability.

No new technical issues are raised; the author accepts both points and plans to incorporate them in the next iteration. The series remains on track, with all substantive feedback from Junio, Justin Tobler, and Elijah Newren now addressed or accepted. The only remaining work is mechanical: updating the test script and documentation to match the agreed behavior.