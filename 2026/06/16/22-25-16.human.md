This is a documentation patch, part of Matt Hunter’s 7-patch series implementing the global `fetch.followRemoteHEAD` configuration option. It adds a brief entry to `Documentation/config/advice.adoc` explaining the new `fetchRemoteHEADWarn` advice message that appears when `remote.<name>.followRemoteHEAD` is set to `warn` or `warn-if-not-<branch>` and the fetched remote HEAD disagrees with the locally configured one.

The change is minimal and self-contained: four lines in a single file, following the established format for advice documentation. The wording is clear and matches the behavior already implemented in Git 2.48 for the per-remote setting. This patch completes the documentation coverage for the feature and aligns with the final polish noted in the thread context, including the corrected advice message format (`warn-if-not-%s`). No controversy or open questions remain; it looks ready to merge.