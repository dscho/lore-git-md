This is the latest exchange in Harald Nordgren’s long-running series to add `git branch --delete-merged` (formerly `--prune-merged`). The patch in question is the test suite for the new command, and Phillip Wood’s review raises **structural and efficiency concerns** that are now blocking progress.

Harald acknowledges Phillip’s feedback about **test readability and realism**—specifically, the overuse of per-test repositories (`pm-upstream`, `pm-fork`, `pm-merged`, etc.), the lack of nested subshells in setup functions, and the tendency to test individual behaviors in isolation rather than verifying the command’s holistic safety guarantees. He agrees to consolidate repositories (e.g., `upstream`, `fork`, `repo`) and explore nested subshells for setup, but notes the risk of flip-flopping between styles if reviewers disagree.

The discussion highlights a **tension between test granularity and realism**. Phillip’s critique—that the tests resemble "unit tests" rather than end-to-end validation—is particularly pointed: they don’t confirm that *non-deleted* branches (e.g., unmerged or checked-out branches) remain untouched, nor do they model realistic workflows (e.g., local branches with commits). His suggestion to use `test_cmp` instead of `test_grep` for precise output checks is a concrete improvement.

This is a **substantive, actionable review** that doesn’t challenge the feature’s design but demands a **test suite overhaul** before the series can advance. The changes will likely require a v16 iteration, but the focus remains on polish rather than core functionality. For readers tracking the thread, this is the key hurdle to watch.