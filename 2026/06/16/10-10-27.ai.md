This is a **substantive review** of Harald Nordgren’s `git rebase --squash` feature proposal, now in its second iteration. Phillip Wood engages directly with the implementation strategy, raising two fundamental concerns about efficiency and user experience that could materially affect the feature’s viability.

The review focuses on the **sequencer-level logic** rather than surface details. Phillip argues that the current approach—picking commits one by one—is inefficient and risks forcing users to resolve the same conflicts repeatedly if the rebase base changes mid-operation. He proposes an alternative: compute the **three-way merge** of the entire commit range against the upstream base in a single step, then present the combined diff for a single conflict resolution pass. This would avoid redundant conflict cycles and align with how `git merge --squash` works.

Phillip also flags a **user-experience gap**: the current implementation preserves the first commit’s message without giving users a chance to edit it. He suggests this should be an interactive step, especially since the folded commits may introduce new context that the original message doesn’t capture. He further notes that the design should explicitly consider how `amend!` commits (which replace the commit message) behave when rebased, hinting at a potential edge case that could confuse users.

The review is **not a rejection**—Phillip explicitly says he is not opposed to the feature—but it shifts the discussion from naming and test coverage to deeper architectural questions. If the thread converges on the `git history squash` alternative (as the thread AI summary suggests), these concerns may become moot; if not, they will likely shape the next revision of the rebase implementation. Either way, the points raised are substantive enough that any future patch addressing them will need to demonstrate awareness of the trade-offs involved.