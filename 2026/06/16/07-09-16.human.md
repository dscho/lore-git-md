This is the **final patch in the `git replay --linearize` series**, now updated to address Elijah Newren’s substantive review of v2 3/3. The patch implements the `--linearize` option, which flattens merge commits to match `git rebase --no-rebase-merges` behavior, and is part of a broader effort to enable server-side rebase-like operations.

Toon Claes **fully accepts all of Elijah’s suggestions** in this reply. The changes include:
- Adding `--linearize` to the command synopsis and `replay_usage[]` array (previously omitted).
- Replacing the ambiguous "ignore" with "drop" in the option description.
- Documenting the mutual exclusivity of `--linearize` and `--revert`.
- Clarifying the comment about `last_commit` handling in merge commits to explicitly note that it is intentionally not updated.
- Fixing the test description to accurately reflect the range (`A..topic-with-merge` excludes `A`).
- Adding three new test cases: `--linearize` with `--advance`, `--revert`/`--linearize` incompatibility, and a `git range-diff` check to verify patch equivalence between the original and linearized histories.

The patch is **well-motivated and technically sound**, with thorough test coverage and clear documentation. Elijah’s review signals approval in principle, and the remaining work is mechanical. The series remains under review, but this version resolves all substantive feedback and looks ready for integration once Junio’s clarifying questions about root-commit handling (from the v1 review) are addressed. The implementation is a clean, focused addition to the replay machinery, with no obvious regressions or design flaws.