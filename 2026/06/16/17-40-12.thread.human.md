This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copies notes from dropped commits to the current HEAD. The issue quickly expanded into a broader audit of how the rebase machinery handles dropped commits across all operations—empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures.

Phillip Wood took the lead with an 11-patch series that systematically addresses the problem. The series is now complete, queued in Junio’s tree, and carries Uwe’s Tested-by. The final patch (11/11) introduces the core fix by adding `PICK_RESULT_DROPPED` to `enum pick_result` and ensuring `pick_one_commit()` skips `record_in_rewritten()` for dropped commits. Test coverage has been expanded to include both "apply" and "merge" backends, fixup sequences, edit commands, skip operations, and edge cases like external merge strategy failures and command execution failures. The series also includes preparatory refactoring (patches 2–10) to simplify control flow, though these are purely internal changes with no behavior impact.

The only known limitation is that the "edit" command still incorrectly records dropped commits as rewritten when `rebase --continue` is run, but this is documented and flagged for a separate fix. Junio has queued minor typofixes and resolved a `b4 am` tooling issue caused by a duplicate message in the thread. Phillip has acknowledged the oversight (forgetting to send the series as `v2`) and confirmed he will squash the typofixes locally before sending a reroll. The series is ready for final review, and no further action is expected from Uwe unless Junio requests it.

Oswald Buddenhagen provided surface-level feedback on patch organization and code aesthetics, but Phillip rejected these suggestions as unnecessary churn. The series remains focused on its technical goals, with no unresolved objections. Junio’s latest logistical note confirms the series is queued and awaiting further substantive review before a reroll.