Here’s the human-mode narrative for a developer catching up on the thread:

This thread began as a simple bugfix—when `git rebase` drops a commit, it incorrectly copies that commit’s notes to the current HEAD, corrupting note history. Uwe Kleine-König’s initial patch exposed a deeper issue: the sequencer’s handling of dropped commits was inconsistent across operations like empty changes, fixup sequences, and skip commands. Phillip Wood took over with a nine-patch systemic overhaul (v3) that audits and repairs how the rebase machinery treats dropped commits, including edge cases like merge tool crashes, permission errors, and command execution failures.

The series is now complete and approved by Junio Hamano, ready for the `next` branch once minor typofixes are squashed. It addresses all known edge cases for `--empty=drop` (the non-interactive default) and carries Uwe’s Tested-by. However, a regression remains: the fix doesn’t work for *interactive* rebase (`-i`), where notes from dropped commits are still copied in two scenarios: user-initiated drops (e.g., `git rebase --skip`) and the quirky `--empty=ask` behavior (which drops commits on `--continue` but keeps them on `--skip`). Phillip has clarified this limitation and plans a follow-up to address it.

The discussion also touched on procedural meta-issues. Junio defended the mailing list’s nuanced review culture, distinguishing between `Acked-by` (low-confidence) and `Reviewed-by` (substantive), while Oswald Buddenhagen argued Gerrit’s scoring system already encodes such distinctions. These exchanges were purely about tooling ergonomics and didn’t affect the series’ technical status. Reviews from Oswald and Andrei Rybak were surface-level (grammar, punctuation), while Uwe and Phillip provided substantive, tested feedback. The series is now queued for `next`, with the interactive rebase regression slated for a separate fix.