Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread started with Uwe Kleine-König’s patch to stop `git rebase` from incorrectly copying notes from dropped commits to the current HEAD. What began as a targeted bugfix has since grown into a thorough audit and repair of how the rebase machinery handles dropped commits across all operations—empty changes, fixup sequences, edit commands, skip operations, external merge failures, and even command execution failures (like a failing `git commit`).

Phillip Wood took the lead and sent an 11-patch series that addresses every known edge case. The final patch (11/11) introduces the core fix by teaching `pick_one_commit()` to skip `record_in_rewritten()` for dropped commits, ensuring notes and post-rewrite hooks no longer receive misleading information. The series is self-contained, includes expanded test coverage for both the "apply" and "merge" backends, and simplifies the sequencer’s control flow with pure refactoring patches. Junio has already queued a few typofix commits on top after a minor `b4` tooling hiccup, but no substantive objections have been raised. The series is now ready for final review and is expected to land soon.

Uwe’s original patch is functionally redundant at this point, though he hasn’t withdrawn it. The only known limitation—a quirk where the "edit" command still records dropped commits as rewritten—is documented and slated for a separate fix. The thread is effectively converged, with the focus now on logistics rather than design.