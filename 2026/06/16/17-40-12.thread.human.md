Here’s the human-mode summary for a developer catching up on the thread:

This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD. Phillip Wood expanded it into a 9-patch series (v3) that systematically audits and repairs how the rebase machinery handles dropped commits—whether due to empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures (e.g., merge tool crashes), or command execution failures (e.g., `git commit` failing to run). The series also clarifies the scope of refactoring needed, with Phillip advocating for incremental changes to minimize disruption.

The patches are now complete and approved by Junio, who has proposed marking the topic ready for the `next` branch. Key changes include fixing error handling for external merge strategies (e.g., distinguishing between merge conflicts and unrecoverable failures), addressing command execution failures, and introducing `enum pick_result` to make the sequencer’s return values more explicit. The final patch ensures notes and post-rewrite hooks no longer receive information about dropped commits. Test coverage has been expanded to verify both rebase backends ("apply" and "merge"), fixup sequences, and edge cases like merge tool failures.

One known limitation remains: the "edit" command still erroneously records dropped commits as rewritten when `rebase --continue` is run, but this is documented and flagged for a separate fix. Minor typofixes (grammar, punctuation) have been queued and will be squashed before the series advances. Uwe’s original patch is now superseded, and no further action is expected from him. The series is expected to land in the next Git release.