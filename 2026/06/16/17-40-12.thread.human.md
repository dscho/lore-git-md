The thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD. That patch is now functionally superseded by Phillip Wood’s comprehensive 11-patch series, which not only fixes the original issue but also addresses a systemic audit of how the rebase machinery handles dropped commits across all operations—empty changes, fixup sequences, edit commands, skip operations, final fixup cleanup, and external merge strategy failures.

Phillip’s series is now the focus. The fourth patch in the series, just arrived, fixes a subtle edge case: when `git commit` fails to execute (e.g., due to a missing Git installation), the sequencer incorrectly reschedules the command instead of treating the pick as successful. The fix is minimal and aligns with the series’ broader goal of making the rebase machinery more robust. The series as a whole is ready for review, having addressed all of Junio’s earlier feedback, including the newly identified external merge strategy edge case. It restores lost test coverage for the "apply" rebase backend, refactors control flow in `sequencer.c`, and ensures that notes and post-rewrite hooks no longer receive information about dropped commits.

Uwe has not objected to the shift in ownership, and Junio is expected to view the series favorably given its thoroughness. The only remaining open question is the sequencing of the merge—whether Junio will take Phillip’s series before or after Uwe’s original patch—but the series is designed to land independently. No further action is expected from Uwe unless Junio requests it.