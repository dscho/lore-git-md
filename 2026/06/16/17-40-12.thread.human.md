This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD. The discussion quickly expanded into a broader audit of how the rebase machinery handles dropped commits, leading to Phillip Wood’s 11-patch systemic overhaul. That series is now complete, queued in Junio’s tree, and carries Uwe’s Tested-by.

The overhaul addresses all known edge cases—empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures—by introducing `PICK_RESULT_DROPPED` to ensure notes and post-rewrite hooks no longer receive information about dropped commits. Test coverage has been expanded for both the "apply" and "merge" backends, and the series includes pure refactorings to simplify control flow. A minor tooling issue (a `b4 am` glitch caused by a duplicate message) was resolved by Uwe, and Junio has queued typofix commits for editorial corrections. The only known limitation is a documented edge case with the "edit" command, which will require a separate fix.

The thread is now in its final stages. Phillip’s series is functionally independent of Uwe’s original patch, which is superseded but remains under review. No further technical changes are expected unless substantive feedback emerges during final review. The next steps are for Phillip to verify the typofixes and for Junio to merge the series.