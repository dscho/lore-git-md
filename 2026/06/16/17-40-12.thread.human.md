This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD. The discussion quickly expanded into a broader audit of how the rebase machinery handles dropped commits across all operations, led by Phillip Wood. Phillip’s 11-patch series is now complete and queued in Junio’s tree for final review, addressing the original issue and all known edge cases—including fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures.

The series introduces a core behavioral change: notes and post-rewrite hooks no longer receive information about dropped commits, ensuring the repository’s note history remains accurate. Test coverage has been expanded to include both the "apply" and "merge" backends, and the series restores test coverage for the "apply" backend while adding new tests for edge cases like merge strategy failures. A minor tooling issue with `b4 am` was resolved manually, and Junio has queued typofix commits for editorial corrections.

Uwe has confirmed the series works as expected in his environment, providing a **Tested-by** trailer and noting the fix resolves the original regression. The series is functionally independent of Uwe’s original patch, which is now redundant. The only remaining loose end is a documented limitation with the "edit" command, which still records dropped commits as rewritten when `rebase --continue` is run—this is flagged for a separate fix. No further action is expected from Uwe unless Junio requests it, and the series is poised to land after any additional substantive feedback.