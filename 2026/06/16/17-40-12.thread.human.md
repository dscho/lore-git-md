Here’s the human-mode summary for a developer catching up on the thread:

This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD, corrupting note history. The discussion quickly expanded into a systemic audit of how the rebase machinery handles dropped commits across all operations—empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures. Phillip Wood took the lead with a 9-patch v3 series that addressed these edge cases, earning Junio C Hamano’s approval and a green light for the `next` branch once minor typofixes are squashed.

The series is now technically complete for non-interactive rebase and `--empty=drop` in interactive rebase, with expanded test coverage for both the "apply" and "merge" backends. However, a regression emerged: the fix doesn’t work for *user-initiated drops* in interactive rebase (e.g., `git rebase --skip` or `--continue` without committing), where notes from dropped commits are still incorrectly copied. Phillip identified the root cause—the series only handles commits dropped by `--empty=drop`, not the `--empty=ask` default in interactive rebase, which behaves inconsistently (drops on `--continue`, keeps on `--skip`). He’s now designing a fix for this, though Junio’s approval suggests it may land as a follow-up rather than blocking the current series.

Key participants include Uwe (original patch author and regression reporter), Phillip (series author and primary responder), Junio (maintainer, approving the series), and Oswald Buddenhagen and Andrei Rybak (surface-level reviewers whose feedback was incorporated as typofixes). The thread also clarified tooling logistics (e.g., `b4 am` usage) and refactoring rationale, with Phillip rejecting broader changes to avoid unnecessary churn. The only loose end is the interactive rebase regression, which Phillip is actively addressing. Once the typofixes are squashed, the series is expected to graduate to `next` and eventually `master`.