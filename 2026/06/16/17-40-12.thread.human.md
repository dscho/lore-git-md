Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread started as a simple bugfix—preventing Git from incorrectly copying notes from dropped commits during rebase—but has grown into a systemic audit of how the rebase machinery handles dropped commits across all operations. The original patch by Uwe Kleine-König was superseded by Phillip Wood’s 9-patch v3 series, which Junio approved for the `next` branch. The series fixes the core issue (notes no longer leak from dropped commits in non-interactive rebase) and addresses edge cases like fixup sequences, merge tool failures, and command execution errors. It also introduces `enum pick_result` to make the sequencer’s control flow clearer.

However, a regression emerged: the fix works for non-interactive rebase but fails in interactive mode (`-i`). Phillip’s latest email explains why: interactive rebase’s `--empty=ask` behavior (which prompts users to decide whether to keep or drop empty commits) wasn’t accounted for. When users manually skip commits (e.g., via `git rebase --skip`), the sequencer still records them as "rewritten," causing their notes to be copied. The fix will need to extend `PICK_RESULT_DROPPED` logic to cover user-initiated drops, but Phillip notes this might require revisiting `--empty=ask`’s quirky behavior (e.g., dropping commits on `--continue` but keeping them on `--skip`). The series is on hold until this is resolved, though the core design remains sound.

Key participants: Uwe reported the regression and provided reproduction steps; Phillip is driving the fix and has clarified the scope of the issue; Junio approved the series but paused integration pending the interactive rebase fix. Oswald Buddenhagen and Andrei Rybak contributed minor feedback on wording and style, while Junio playfully corrected a typo in Phillip’s commit message. The next step is a targeted patch to handle interactive rebase’s edge cases, likely localized to `sequencer.c`.