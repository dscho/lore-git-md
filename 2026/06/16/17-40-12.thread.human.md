Here’s the human-mode summary for a developer catching up on the thread:

This thread started with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copied notes from dropped commits to the current HEAD. The issue expanded into a broader audit of how the rebase machinery handles dropped commits across all operations—empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures (like `git commit` failing to run).

Phillip Wood took the lead with an 11-patch series that systematically addresses these edge cases. The series is now ready for review and includes fixes for all previously identified gaps, including the newly discovered failure scenarios. The refactoring patches (like the ninth one, which simplifies control flow in `pick_one_commit()`) prepare the codebase for the core behavioral change: ensuring notes and post-rewrite hooks no longer receive information about dropped commits. The series also restores test coverage for the "apply" rebase backend and adds tests for external merge strategy failures and command execution edge cases.

Uwe’s original patch is now functionally superseded by Phillip’s work, though it hasn’t been formally withdrawn. Junio’s earlier concerns about documentation and edge cases have been addressed, and the series is designed to land independently. The only remaining open question is whether Junio will merge Phillip’s series before or after Uwe’s patch, but this is a sequencing detail rather than a technical blocker. The thread is effectively converging on Phillip’s solution.