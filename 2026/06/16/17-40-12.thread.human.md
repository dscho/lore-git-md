This thread began with Uwe Kleine-König’s patch to fix a bug where `git rebase` incorrectly copies notes from dropped commits to the current HEAD. The discussion quickly expanded into a broader audit of how the rebase machinery handles dropped commits, led by Phillip Wood, who sent an 11-patch series addressing all known edge cases—empty changes, fixup sequences, edit commands, skip operations, external merge strategy failures, and command execution failures.

Phillip’s series is now complete and queued in Junio’s tree for final review. It carries Uwe’s Tested-by and resolves all of Junio’s earlier concerns, including test coverage for both the "apply" and "merge" backends, fixup sequences, and newly identified failure modes like external merge strategy errors. The core fix (patch 11/11) introduces `PICK_RESULT_DROPPED` to ensure notes and post-rewrite hooks no longer receive information about dropped commits. A minor known limitation remains: the "edit" command still records dropped commits as rewritten, but this is documented and flagged for a separate fix. Junio has queued typofix commits for minor editorial corrections, which Phillip will squash locally before any reroll.

The only recent activity is a surface-level review from Oswald Buddenhagen, who pointed out redundant checks in patch 8/11. The feedback is mechanical and doesn’t affect the series’ correctness or scope. Uwe’s original patch is now redundant, and no further action is expected unless Junio requests it. The series is ready to land, pending any additional substantive feedback.