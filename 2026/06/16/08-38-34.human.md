This is the third patch in the `git replay --linearize` series, addressing Junio’s clarifying questions from his review of v2. The patch itself is a feature addition to the replay machinery, offering a `--linearize` option that flattens merge commits to match `git rebase --no-rebase-merges`.

Toon confirms that the change in root-commit handling is intentional: v2 no longer skips root commits when linearizing, unlike v1. The earlier version treated roots and merges identically (both skipped), but the new behavior explicitly includes roots. This aligns with the test case Junio prompted, which now correctly verifies that replaying down to the root commit works as expected. The test description will be fixed to accurately reflect the range (`A..topic-with-merge` excludes `A`), and the overly long command line will be split for readability.

The patch remains well-structured and clearly motivated. Junio’s feedback was narrow—focused on intent and test clarity rather than design or correctness—and Toon’s responses resolve both points. With Elijah Newren’s earlier review suggestions already accepted and planned for the next iteration, this series looks ready to proceed once the minor test adjustments are made. No merge status is reported; the series is still under review.