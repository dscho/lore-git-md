This is **Patch 1/3 of the `git replay --linearize` series**, where the author (Toon Claes) responds to architectural feedback on converting `enum replay_mode` to a boolean `reverse` flag. The patch is now **under active debate** after Junio Hamano and Patrick Steinhardt raised concerns about callsite clarity and future extensibility.

Toon acknowledges the critique and concedes that the boolean refactoring does not actually solve the core readability issue—the ternary operator in the `pick_regular_commit()` call remains problematic regardless of whether an enum or boolean is used. He also pushes back mildly on the "future extensibility" argument, noting that speculative design for hypothetical third modes is not typical practice, though he agrees to revisit the patch in the next iteration. The response is constructive and signals willingness to address the concerns, but no concrete alternative has been proposed yet.

The key technical symbols in play are the `pick_regular_commit()` function, the `reverse` boolean (or `mode` enum), and the `replayed_commits` khash map. The discussion hinges on whether the callsite clarity trade-off (boolean vs. enum) is worth the risk of painting the code into a corner if a third replay mode is ever needed. Junio’s example—contrasting C’s positional arguments with named parameters in other languages—illustrates why the boolean approach may obscure intent at the callsite.

This email advances the thread by clarifying the author’s position and setting the stage for a v5 iteration, but the architectural debate remains unresolved. The series’ fate now depends on whether Toon can propose a refactoring that satisfies both readability and extensibility concerns. Patch 2/3 (getter/setter helpers) and Patch 3/3 (`--linearize` implementation) are unaffected by this discussion and remain ready for merging pending resolution here.