This is a **reply to a substantive review** of patch 6/12 in the `git cat-file --batch-command` security-hardened series. The author, Pablo Sabater, acknowledges and agrees with Karthik Nayak’s two technical concerns.

First, Pablo confirms that the change from `GIT_HASH_SHA1_LEGACY` to `GIT_HASH_SHA1` in the hash algorithm check was unintentional and incorrect. He explains that this change appeared between v11 and v12 of the series but was not part of his intended modifications. The original code in v11 used `GIT_HASH_SHA1_LEGACY` to represent servers that do not support v2 and thus cannot negotiate object formats, and Pablo agrees this logic should be preserved. He will revert this change in v14.

Second, Pablo accepts the documentation nit about the function comment placement. He will move the comment from above the forward declaration in `connect.h` to above the function definition in `connect.c` in the next version. Regarding the suggestion to remove the forward declaration by including `string-list.h`, Pablo notes that this was previously discussed and Patrick Steinhardt preferred keeping the forward declaration to avoid potential circular dependencies.

The reply advances the thread by clarifying that the protocol compatibility regression risk is real and will be fixed, and that the documentation issue will be addressed. No new technical questions are raised, and the author’s response is straightforward and actionable. The series is now blocked on these two fixes before it can proceed to merging.