Here is the updated dense, loss-free AI-mode summary for future invocations:

---
Thread identity: patch series, v1, "refactor `struct object_info` to use `source` field", Patrick Steinhardt, ODB subsystem refactoring.
Core problem: `struct object_info`'s `whence` field is too coarse to uniquely identify an object's source (e.g., which packfile), blocking multi-source object resolution and pluggable ODB backends. The series replaces `whence` with a new `struct object_info_source` that carries both source type and backend-specific data, making provenance information opt-in rather than unconditionally populated.
Current status: v1, unmerged, no substantive review feedback yet. Series is self-contained and targets `master`; based on `ps/odb-source-packed`. Patches 1/6 (preparatory plumbing) and 2/6 (opt-in source struct) have been posted; the latter converts all `oi->u.packed` accesses to `oi->sourcep->u.packed` and makes backend union population conditional on `sourcep` being non-NULL.
Key technical details: 6 patches, 15 files touched (`odb.h`, `odb.c`, `packfile.c`, `odb/source-*.c`, `builtin/cat-file.c`, `commit-graph.c`, `reachable.c`, `builtin/pack-objects.c`, `pack-bitmap.c`, `t/helper/test-bitmap.c`, `builtin/index-pack.c`). New symbols: `struct object_info_source`, `oi->sourcep` (replaces `oi->whence` and `oi->u`). Patch 1/6 adds an optional `struct odb_source_packed *source` parameter to `packed_object_info()` and `packed_object_info_with_index_pos()`; patch 2/6 introduces the opt-in `sourcep` pointer and converts all callers. No on-disk format changes; behavior is identical except callers can now opt out of source population. Test coverage: existing ODB/packfile tests exercise the changes.
Open questions: none raised yet (first iteration).
Participant positions: none recorded.
Related work: part of Patrick Steinhardt's ODB abstraction effort; depends on `ps/odb-source-packed`.
---