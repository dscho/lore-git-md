This is the first patch in a seven-part performance optimization series targeting Git’s merge-base computation. It adds a new technical document (`Documentation/technical/paint-down-to-common.adoc`) that explains the `paint_down_to_common()` algorithm in detail, covering its use cases, the paint walk, generation number regions, and termination conditions.

The document is thorough and well-structured, breaking down the algorithm’s two primary use cases (finding all merge bases and ancestry checks), the mechanics of the paint walk (PARENT1/PARENT2 flags, priority queue ordering), and the critical distinction between the INFINITY and finite generation regions. It also clarifies the termination logic, including the role of the `nonstale_queue` and the `max_nonstale` heuristic. The explanation is clear enough for a developer unfamiliar with the commit-reach subsystem to understand how the algorithm works and why certain design choices (e.g., generation-number ordering) matter.

The patch itself is minimal: it adds the new document to the build system (Makefile and meson.build) and includes a cross-reference comment in `commit-reach.c`. The document is ready for review and appears to be a solid foundation for the rest of the series, which builds on this algorithm to introduce an early-exit optimization. No concerns are apparent here—this is a well-crafted documentation patch that serves its purpose.