This is a **bugfix follow-up** to patch 6/12 in the ongoing `git cat-file --batch-command` security-hardened series. The author, Pablo Sabater, acknowledges two issues raised by Karthik Nayak in his substantive review of the patch.

First, the accidental change from `GIT_HASH_SHA1_LEGACY` to `GIT_HASH_SHA1` in the hash algorithm check was unintended and breaks backward compatibility with pre-v2 servers. Pablo confirms this was not part of the intended refactoring and will revert it in v14. This is a clear regression risk: the legacy constant was used to detect servers that cannot negotiate object formats, and the substitution alters the logic without justification.

Second, Pablo agrees to move the function comment from above the forward declaration in `connect.h` to above the function definition in `connect.c`. He also notes that the forward declaration was added at Patrick Steinhardt’s suggestion, but does not address Karthik’s question about whether a circular dependency prevents including `string-list.h` instead.

The email is brief and focused, responding directly to the review without introducing new material. It advances the thread by confirming the protocol compatibility issue and committing to fix both the code and documentation in the next version. The tone is matter-of-fact and collaborative. No testing is reported, but the issues are straightforward enough that they can be verified with existing test infrastructure. The response demonstrates that the series is still in active refinement, with at least one significant correctness issue remaining before it can be considered ready for merging.