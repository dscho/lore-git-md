This is the first patch in the **v2 performance optimization series** for Git’s commit-reach subsystem, adding technical documentation for the `paint_down_to_common()` algorithm. The patch introduces a new AsciiDoc file (`Documentation/technical/paint-down-to-common.adoc`) that explains the merge-base computation logic, including the paint walk, generation number regions, and termination conditions.

The documentation is **comprehensive and well-structured**, covering the algorithm’s two primary use cases (finding all merge bases and ancestry checks), the mechanics of the paint walk (PARENT1/PARENT2 flags, priority queue ordering), and the distinction between the INFINITY and finite generation regions. It also clarifies the termination conditions, particularly the role of the `nonstale_queue` and the `max_nonstale` heuristic, which are critical for understanding the optimization introduced later in the series.

The patch touches four files: it adds the new documentation file, updates the `Makefile` and `meson.build` to include it in the build, and adds a cross-reference comment in `commit-reach.c` linking to the new document. The commit message is minimal but sufficient, and the patch itself is **clean and uncontroversial**—it’s purely additive and doesn’t change any behavior.

This patch sets the stage for the rest of the series by providing the necessary context for the refactoring and optimization in later patches. It’s a **documentation patch** in the strictest sense, and its inclusion as the first patch in the series reflects the reviewers’ preference for establishing clarity before making code changes. No objections or open questions remain for this patch.