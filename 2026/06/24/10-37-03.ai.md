This is the first patch in Patrick Steinhardt’s v2 series refactoring Git’s connectivity checks to generalize promisor object handling. It is a preparatory refactoring that extracts pack-exclusion logic into a new helper function, `should_exclude_pack()`, without changing behavior. The patch touches only `odb/source-packed.c` and is unchanged from v1.

The new helper function consolidates four separate conditional checks that determine whether a pack should be skipped during object iteration, based on caller-provided flags (`ODB_FOR_EACH_OBJECT_LOCAL_ONLY`, `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`, etc.). This extraction is a prerequisite for the next patch, which will lift the current restriction that disallows flags when an object prefix is specified. The commit message is clear and concise, and the code change is mechanical and self-contained.

Junio C Hamano’s review of v1 called this patch "quite straight-forward" but did not engage with substantive aspects such as flag interactions or edge cases (e.g., whether all relevant flags are covered). The patch is well-crafted and appears correct, but reviewers may still ask whether `should_exclude_pack()` handles all flag combinations robustly, especially as the series evolves to support more complex iteration scenarios. No test coverage is mentioned, but the change is purely internal and should not affect observable behavior.