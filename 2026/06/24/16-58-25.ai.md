This is a maintainer response to a bugfix patch targeting the GPG/SSH signature normalization logic in `gpg-interface.c`. Junio C Hamano has reviewed the patch and signals intent to queue it for merging without requesting changes.

The patch, authored by Antonio De Stefani, corrects an over-aggressive CR-stripping behavior that has been present since 2008. The original `remove_cr_after()` helper stripped *all* CR characters (`\r`) from signature output, but only `\r\n` sequences (Windows line endings) should be normalized to `\n`. The fix renames the helper to `strip_cr_before_lf()` and modifies its logic to preserve lone CR characters, addressing a NEEDSWORK comment left in the code. The change affects both GPG and SSH signing code paths and is narrowly scoped to `gpg-interface.c`.

Junio’s brief reply ("Looking good. Will queue. Thanks.") indicates satisfaction with the patch as-is. There are no open questions or requests for additional test coverage in this response, though future reviewers may still raise such points. The patch is now likely to graduate to the `next` branch in the next integration cycle, barring any last-minute objections from other contributors. This is a clean, well-motivated fix with clear historical context and no apparent controversy.