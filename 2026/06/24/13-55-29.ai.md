This is a **substantive post-merge review** of patch 6/7 in Tian Yuchen’s now-merged `paint_down_to_common()` optimization series. Derrick Stolee’s reply to Kristofer Karlsson’s patch confirms the removal of two unused wrapper functions (`nonstale_queue_put_dedup()` and `nonstale_queue_get_dedup()`) in `commit-reach.c`. The patch is a straightforward cleanup—deleting 18 lines of dead code—following the introduction of the `paint_state` struct in patch 5/7. Stolee’s feedback is brief and positive ("nice cleanup"), signaling no concerns about correctness or impact.

The context is important: this patch is part of a **7-patch performance optimization** that achieved **100-1000x speedups** for merge-base queries in asymmetric histories (e.g., repositories with import grafts or shallow histories). The series is fully merged, and this specific patch is uncontroversial—it merely removes code that became obsolete after the refactoring in patch 5/7. The core `nonstale_queue` functions remain in use by `ahead_behind()`, so this is purely a housekeeping step to simplify the codebase before the final optimization (patch 7/7).

While Stolee’s reply here is minimal, his broader engagement with the series (detailed in the thread summary) reveals ongoing **medium-weight technical questions** about benchmark data and test infrastructure. For example, he previously asked why the benchmark table omitted step-count data for a key test case, and proposed extending `test_all_modes` to capture trace2 step-counts for better regression detection. Those questions remain open, but they do not affect this patch’s correctness or its role in the series. This reply simply acknowledges the cleanup as a logical follow-up to the refactoring.