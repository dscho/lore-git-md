This is the **third patch in the v2 series** optimizing `paint_down_to_common()` for one-sided histories. It adds **regression tests** to ensure the side-exhaustion optimization works correctly when multiple merge-base candidates exist, including cases where one candidate is an ancestor of another.

The patch introduces a new test script, `t6099-merge-base-side-exhaustion.sh`, which verifies that `git merge-base --all` correctly excludes redundant merge-base candidates (e.g., an ancestor of another candidate) even when the side-exhaustion optimization exits early. The test covers both commit-graph and non-commit-graph scenarios, confirming the optimization respects the finite-generation guard. It also extends `t6600-test-reach.sh` with a test for mixed finite/INFINITY generation numbers, where one tip is outside the commit-graph (INFINITY generation) and the other is inside (finite generation). This exercises the region transition logic, ensuring the optimization activates only after entering the finite-generation region.

The tests are **well-motivated and thorough**, with ASCII diagrams documenting the commit topology and clear explanations of expected behavior. They directly address edge cases that could break the optimization, such as redundant merge-base candidates and non-monotonic commit dates. The patch is **ready to land** and complements the earlier documentation and test coverage patches in the series. No controversies or open questions remain.