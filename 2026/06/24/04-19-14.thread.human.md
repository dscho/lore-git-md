This thread is about Justin Tobler’s 11-patch series converting `git-receive-pack` from using `tmp_objdir` to the new ODB transaction subsystem, completing a key step in Patrick Steinhardt’s ODB abstraction effort. The series is now functionally complete with v4, which addresses all prior feedback—including safety concerns, error-handling gaps, and redundant validation—while preserving existing quarantine semantics. All patches have been reviewed by Patrick Steinhardt and are queued for Junio’s `next` branch pending integration.

The final patch (11/11) replaces the last direct `tmp_objdir` usage in `builtin/receive-pack.c` with ODB transaction calls, ensuring the "files" backend maintains receive-pack’s quarantine behavior via the `ODB_TRANSACTION_RECEIVE` flag. The changes are mechanical, with no user-facing impact, and rely on the existing test suite for coverage. Two design-level questions remain deferred: whether to merge `odb/transaction.h` and `odb/source.h` (due to tight coupling) and whether to add explicit `odb_transaction_abort()` calls in error paths (currently handled via `atexit()` cleanup). These are scoped for follow-up work and don’t affect the series’ correctness. The series is now ready for integration.