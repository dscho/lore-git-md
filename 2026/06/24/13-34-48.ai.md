This is a **substantive review comment** on the already-merged performance optimization series for `paint_down_to_common()`. Derrick Stolee asks why the benchmark table omits step-count data for the `git merge-tree (across import)` case in the 2.6M-commit monorepo, despite the wall-clock improvement (8.11s → 100ms) being substantial.

The question is narrow but technically relevant: the series introduced **trace2 instrumentation** (patch 4/7) to expose the "commits walked" metric via `trace2_data_intmax()`, and the benchmarks in the cover letter claim deterministic step counts for all other cases. The omission here is conspicuous because the wall-clock speedup suggests a large reduction in steps, yet the table leaves the field blank. Stolee’s tone is neutral, but the implication is that the data should be available and its absence may be an oversight rather than intentional.

No response is required from the author (the series is already merged), but the question could prompt a follow-up if others are curious about the exact step-count reduction for `merge-tree`. The context suggests the missing data is likely a reporting gap rather than a technical issue, since the instrumentation is in place and the wall-clock improvement aligns with the optimization’s goals.