Patrick Steinhardt’s six-patch series refactoring `struct object_info` to use a `source` field has cleared its first hurdle: Junio C Hamano has given it a terse but unambiguous "Great," signaling conceptual approval. The series, part of Patrick’s broader ODB abstraction effort, replaces the coarse `whence` field with a new `struct object_info_source` to uniquely track object provenance—critical for multi-source resolution and pluggable backends. All six patches are now posted (v1), touching 21 files across the ODB, packfile, and builtin subsystems, but no substantive review feedback has landed yet.

With the maintainer’s blessing, the focus shifts to implementation review. The changes are opt-in (callers can skip source population) and preserve existing behavior, but the new `sourcep` pointer and backend-specific data structures will need scrutiny. Given the series’ foundational role in enabling pluggable ODBs, expect discussion around the call-site conversions and whether the `struct object_info_source` design is sufficiently flexible for future backends. For now, the series is queued for `master` pending review.