Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is the second iteration of Patrick Steinhardt’s six-patch series refactoring `struct object_info` to replace its coarse `whence` field with a new `source_infop` pointer, part of the broader ODB abstraction effort. The goal is to let Git track precisely where an object comes from (e.g., which packfile or ODB backend) without forcing every caller to pay the cost of populating that information. Junio C Hamano has conceptually approved the series, calling it “everything makes sense,” and the design—using an opt-in `source_infop` parameter—is now settled.

The series is complete at v2, with all six patches posted and reviewed. The first three patches introduce the new `struct odb_source_info` and populate it in the ODB backends; the fourth (just summarized) mechanically converts all remaining callers from the old `whence` field to the new `source->type` field. The last two patches remove the now-redundant `whence` field and add documentation. Justin Tobler gave substantive feedback on the first three patches, questioning the opt-in design but approving the later ones as “looking good.” Junio flagged one readability concern—a pointer assignment pattern in patch 3—but accepted it as technically correct.

The series touches 18 files, primarily in the ODB and packfile subsystems, with no on-disk format changes or new test coverage needed. It’s based on Patrick’s `ps/odb-source-packed` branch and targets `master`. The only loose end is Junio’s note about the pointer assignment’s readability, which may prompt a follow-up but doesn’t block merging. For now, the series is ready to go once the remaining patches are queued.