Here’s the human-mode narrative for the thread so far:

This thread is Patrick Steinhardt’s six-patch series refactoring `struct object_info` to replace its coarse `whence` field with a new `source` field, part of the broader ODB abstraction effort. The goal is to let Git track where objects come from (e.g., which packfile) without forcing all callers to pay the cost of populating that information. Junio C Hamano gave the series a conceptual thumbs-up ("Great"), but a key architectural question remains unresolved: whether the current opt-in `source` parameter design—where callers explicitly request source tracking—is the right approach, or if source tracking should instead be centralized at the `packed_git` level.

Patrick argues the opt-in design is a pragmatic step toward eliminating direct use of `packed_object_info()` in favor of the higher-level `odb_read_object_info()` API, which already handles source tracking. Most callers use the latter, so the changes in this series only touch the few low-level holdouts. Justin Tobler and Junio, however, prefer centralized tracking for simplicity, seeing the current approach as overly complex. The series itself is technically sound—patches 1–5 refactor the struct and callers, while patch 6 adds documentation—and has received substantive review from Justin and Junio. A minor naming ambiguity (`sourcep` vs. `source_infop`) was noted but isn’t blocking progress.

The series is ready to merge from a code perspective, but the architectural debate could delay it. If you’re catching up, focus on the `source` parameter’s role in `packed_object_info()` and the trade-offs between opt-in and centralized tracking. The thread is otherwise uncontroversial, with no open technical objections to the patches themselves.