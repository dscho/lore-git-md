Here’s the human-mode narrative for the thread so far:

This thread is about Patrick Steinhardt’s six-patch series to refactor `struct object_info` to use a new `source` field, part of his broader ODB abstraction effort. The goal is to replace the coarse `whence` field with a more detailed `struct object_info_source`, allowing Git to track where objects come from (e.g., which packfile) and enabling future multi-source object resolution. Junio C Hamano gave the series conceptual approval early on, calling it "Great," but it’s still awaiting deeper review before merging.

The series is now at version 1, with all patches posted and reviewed. Justin Tobler provided substantive feedback on the first three patches, focusing on the architectural design of the `source` field. He questioned whether the opt-in approach—where callers pass an optional `source` parameter—is the right choice, suggesting instead that the ODB source could be recorded once during `packed_git` initialization. Junio echoed this concern in his latest reply, arguing that the current design might overcomplicate the API by making source tracking optional when it could be handled more simply. Neither has proposed an alternative implementation yet, but the discussion is now explicitly about balancing flexibility (opt-in) against simplicity (centralized tracking).

The rest of the series is less contentious: patches 4 and 5 (mechanical cleanup of `whence`) and patch 6 (documentation) received surface-level approval from Justin. The changes touch 21 files, including `odb.h`, `packfile.c`, and several built-in commands, but don’t alter on-disk formats or behavior—except that callers can now opt out of source population. The open question is whether Patrick will revise the design to address the architectural feedback or defend the current approach in a future iteration. For now, the series remains unmerged, with the maintainer’s conceptual approval but no clear consensus on the best path forward.