This is the fifth iteration of Harald Nordgren’s `git history squash` feature, a new subcommand that folds a commit range into its oldest commit while preserving descendant history. The series is now code-complete and addresses the last open technical question from v4: the `--ancestry-path` requirement for range resolution.

The key change in v5 is the adoption of `--ancestry-path` for the revision walk, ensuring only commits descended from the base are folded. This means single revisions (e.g., `HEAD` or `HEAD~1`) are now explicitly rejected with a clear error message, rather than being treated as a squash down to the root. The implementation also tightens input validation: ranges must now be in `<base>..<tip>` form, and the code checks for the presence of a boundary commit (marked `UNINTERESTING`) to enforce this. The test suite has expanded significantly (now 497 lines) to cover complex merge topologies, including nested merges, octopus merges, and cases where a merge arm forks before the base.

The ref-handling logic introduced in v4 remains unchanged: the command rejects operations where any ref points to a commit inside the squashed range, with an advice message suggesting `--update-refs=head` to retarget the current branch. The `--reedit-message` flag continues to seed the editor with all folded-in commit messages, mirroring `git rebase -i` behavior. The core squash logic—a single three-way merge of the range against the oldest commit’s parent, followed by replaying descendants—is also unchanged.

The series touches `builtin/history.c` (341 insertions, 38 deletions), `advice.c`/`advice.h`, and adds a new test script (`t/t3455-history-squash.sh`). The range-diff shows the `--ancestry-path` adoption, stricter range validation, and expanded test coverage. All prior technical concerns (range resolution, merge commit handling, ref safety) are resolved, and the implementation appears solid, with thorough test coverage and clear documentation. The series is ready for Junio’s final assessment.