This is the final patch in Patrick Steinhardt’s six-part series refactoring `struct object_info` to use a `source` field instead of the older `whence` field. The patch adds missing documentation to the struct’s fields in `odb.h`, completing the series.

The changes are straightforward: each field in `struct object_info` now has a clear, concise comment explaining its purpose. For example, `typep` is described as "The object type," `sizep` as "The inflated object size in bytes," and `contentp` as "The object contents. Ownership of memory goes over to the caller." The patch also clarifies the struct’s overall role in the comment block above it, noting that it contains both the query and response for functions that read object information.

This is a documentation-only patch, touching just one file (`odb.h`) with 17 insertions and 1 deletion. It doesn’t change behavior but makes the code more maintainable by ensuring all fields are properly documented. The commit message is minimal but sufficient, and the patch itself is clean and focused.

For readers tracking the ODB abstraction effort, this patch is a minor but necessary cleanup to wrap up the series. It’s unlikely to be controversial, and the series as a whole now looks ready for review. If you’ve been following the earlier patches, this one completes the picture.