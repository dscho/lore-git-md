This is a follow-up in the ongoing bundle-URI protocol bugfix discussion, now focused on implementation details of the server-side validation. Toon Claes responds to Justin Tobler's proposed server-side fix, expressing mild disagreement with the approach while still acknowledging the value of addressing the root cause.

Toon accepts that server-side validation is worthwhile but argues his client-side patch doesn't silently mask the issue since it still emits a warning to users. He suggests this warning would naturally prompt users to report problems to server administrators, creating organic pressure to fix misconfigurations. The key technical detail here is his concern about how to effectively notify server admins of invalid configurations - he questions whether a single log line would be sufficient or likely to be noticed.

The email includes specific code discussion: Toon critiques Justin's proposed server-side patch (adding `&& value && *value` to the bundle-URI packet line generation) as being too silent, though he acknowledges it passes existing tests. This reveals the remaining open question: how to balance server-side prevention with effective error reporting that actually reaches administrators. Toon doesn't propose an alternative implementation, leaving that aspect of the solution still unresolved.

The thread appears to be converging toward a two-part solution (client resilience + server validation) but is now stuck on the practical details of how the server should handle and report invalid configurations. Toon's position suggests he's open to the combined approach but wants to ensure the server-side component doesn't become a silent fix that administrators never notice.