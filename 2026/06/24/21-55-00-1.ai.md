This is the first patch in Harald Nordgren’s **v18 `git branch --delete-merged` series**, which is now implementation-complete and ready for final review. This patch adds the `--forked` filter to `git branch --list`, enabling users to list only branches whose configured upstream matches a given pattern (e.g., `origin/main`, `origin/*`, or a remote name like `origin`). The filter is composable with existing filters like `--merged` and `--no-merged`, forming the foundation for the `--delete-merged` command introduced later in the series.

The implementation resolves ref arguments to their full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) and supports shell-style globs (e.g., `origin/*`). Exact ref matches are compared against the full upstream refname, while glob patterns are matched against the abbreviated upstream name (e.g., `origin/main` instead of `refs/remotes/origin/main`). The filter can be repeated to widen the selection, and it integrates cleanly with the existing `ref_filter` infrastructure. The patch includes thorough test coverage for exact ref matching, glob expansion, remote shorthand resolution (e.g., `origin` for `origin/HEAD`), and composition with other filters.

This is a **well-crafted refactoring** that introduces no behavior changes on its own but establishes the filtering machinery needed for the `--delete-merged` command. The commit message clearly explains the motivation and usage, and the code follows Git’s conventions (e.g., `strvec` for string lists, `wildmatch` for globs). The test suite covers edge cases like self-referential upstreams, missing upstreams, and composition with `--no-merged`. No controversies or unresolved issues remain for this patch; it is a straightforward, necessary building block for the series.