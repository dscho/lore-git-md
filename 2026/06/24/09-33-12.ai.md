This is a reply from Patrick Steinhardt acknowledging a substantive correctness issue in Patch 3 of his ODB abstraction series. The patch removes a call to `odb_reprepare()` before iterating over promisor objects, which Junio C Hamano flagged as potentially causing connectivity checks to miss newly added promisor packs. Patrick confirms the issue is real: the removal was unintentional (he misread `odb_reprepare()` as `odb_alternates_prepare()`) and would indeed cause the code to miss promisor objects in packs added after the loop begins. He does not propose a fix in this email but agrees the call should be restored or handled differently.

The email advances the thread by resolving the open question about `odb_reprepare()`—it was not an intentional design choice but an oversight. This clarification shifts the discussion toward how to reintroduce the refresh step without reintroducing packfile-specific assumptions, which is critical for the ODB abstraction goal. The fallback to `rev-list` ensures correctness, but the performance and maintainability implications of the missing refresh remain unresolved. No other aspects of the patch (e.g., control flow complexity, flag handling) are addressed here. Expect follow-up discussion on whether to restore `odb_reprepare()` directly or refactor the iteration logic to handle refreshes internally.