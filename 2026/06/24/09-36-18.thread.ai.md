Thread identity
Subject: [PATCH] gpg-interface: fix over-aggressive CR stripping in signature normalization
Series version: standalone patch (no series)
Originating author: Antonio De Stefani
Thread type: bugfix patch

Core problem or goal
Correct an over-aggressive CR-stripping behavior in Git’s GPG/SSH signature handling. The current `remove_cr_after()` helper removes *all* CR characters (`\r`) from signature output, but only `\r\n` sequences (Windows line endings) should be normalized to `\n`. Lone CR characters should be preserved. The fix affects both GPG and SSH signing code paths.

Current status
Patch v1 sent; no prior iterations. No reviews or acks yet. The patch is narrowly scoped to `gpg-interface.c` and addresses a NEEDSWORK comment left in the code.

Key technical details
- File touched: `gpg-interface.c`
- Helper renamed: `remove_cr_after()` -> `strip_cr_before_lf()`
- Logic change: skip CR only when immediately followed by LF (`\n`)
- Old behavior: strip every CR
- New behavior: strip CR only in `\r\n` sequences
- On-disk format: unchanged; this is purely about signature text normalization
- Test coverage: none yet; reviewers may request a test verifying lone CR preservation

Open questions and loose ends
- Whether a test case is needed to verify lone CRs are preserved in signature output
- No objections or requests for changes raised so far

Participant positions
- None yet

Related work
- Historical context traced through three prior commits: c4adea82 (2008), 2f47eae2, 29b31577 (2021)
- No dependencies on other in-flight topics