This is a **substantive review reply** in the ongoing security-hardened `git cat-file --batch-command` series. Karthik Nayak’s review of patch 6/12 identified two technical issues, and Pablo Sabater’s reply confirms both and outlines the fixes for v14.

Karthik spotted an unintended change in the hash-algorithm check: the code switched from `GIT_HASH_SHA1_LEGACY` to `GIT_HASH_SHA1` when detecting pre-v2 servers that cannot negotiate object formats. Pablo acknowledges this was an accidental regression between v11 and v12 of the series and will revert it in the next version, preserving backward compatibility. This is a real protocol-level risk that must be fixed before merging.

The second issue is a documentation nit: the function comment for `write_command_and_capabilities()` is placed above the forward declaration in `connect.h` rather than above the function definition in `connect.c`. Pablo agrees and will move the comment in v14. He also explains why the forward declaration is kept (to avoid circular dependencies, per earlier discussion with Patrick Steinhardt), which clarifies the design choice without requiring further changes.

The reply is concise, actionable, and resolves the open questions from the review. The series is now blocked on these two fixes, but the path to v14 is clear and uncontroversial. If the rest of the series holds up as well as this patch, it looks like a solid, well-reviewed effort that addresses a real gap in Git’s remote-object querying capabilities.