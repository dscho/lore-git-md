This is the final patch in Harald Nordgren’s **v18 `git branch --delete-merged` series**, adding `--dry-run` support to the new `--delete-merged` subcommand. The patch completes the series’ safety-focused design by allowing users to preview which branches would be deleted before committing to the operation.

The implementation is straightforward: when `--dry-run` is combined with `--delete-merged`, the command prints "Would delete branch <name>" for each candidate branch (matching the real deletion output exactly) and exits without modifying any refs. The same filtering and safety checks apply—branches are spared if they are checked out, missing their upstream, pushing to the same ref as their upstream, opted out via `branch.<name>.deleteMerged=false`, or protected by the **refined stacked-branch protection** (abort-and-clear) logic introduced earlier in the series. The patch also rejects `--dry-run` when used without `--delete-merged`, ensuring the flag is only meaningful in the intended context.

The changes are minimal and focused: a new `DELETE_BRANCH_DRY_RUN` flag is added to the deletion API, the `delete_branches()` function is updated to skip ref deletion when the flag is set, and the command-line option is wired up in `cmd_branch()`. The documentation and tests are updated to reflect the new functionality, including a test that verifies the output matches the real deletion output and another that ensures `--dry-run` is rejected without `--delete-merged`.

This patch is **well-crafted and ready for merging**. It addresses a real user need (safe preview of branch deletions) with a clean, consistent implementation that integrates seamlessly with the rest of the series. The `--dry-run` feature is a natural complement to the safety mechanisms already in place, and the patch’s attention to detail—such as matching the real deletion output exactly—demonstrates thoroughness. The series as a whole is now **implementation-complete**, with all design questions resolved and comprehensive test coverage.