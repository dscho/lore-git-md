This is a **follow-up discussion** in the now-merged performance optimization series for Git’s commit-reach subsystem. The thread is complete and the patches have landed, but Derrick Stolee raises a **minor benchmarking question** about the missing step-count data for the `merge-tree` case in the 2.6M-commit monorepo.

Stolee notes that the wall-clock improvement for `merge-tree` (8.11s → 100ms) is substantial and asks why the corresponding step-count metric—measured via the trace2 instrumentation added in patch 4—was omitted. The question is purely about **benchmark completeness**, not correctness or design; the optimization itself is already merged and validated. The missing data point would help quantify how much of the speedup comes from the early-exit logic versus other factors (e.g., commit-graph lookups or I/O).

No action is required from the author (Tian Yuchen) or reviewers; this is a low-priority observation for future benchmarking consistency. The thread remains closed.