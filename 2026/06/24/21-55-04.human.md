This is the core patch in Harald Nordgren’s **implementation-complete** `git branch --delete-merged` series, now ready for merging. Patch 5/7 adds the new `--delete-merged` subcommand, which safely deletes local branches whose work has already landed on their upstream while preserving branches that are checked out, missing their upstream, pushing back to their upstream, or used as an upstream by another branch.

The patch implements the **refined stacked-branch protection** design (abort-and-clear) that was finalized in this iteration. When a branch is used as an upstream by another branch that is not being deleted (e.g., because it is unmerged), the command **keeps the base branch** but **clears its upstream config** if the base’s own upstream is being deleted. This avoids breaking dependency chains while still cleaning up stale configs. For example, in a chain like `lower→mid→tip` where `tip` is unmerged, deleting `lower` would clear `mid`’s upstream config (since `lower` is gone) but preserve `mid` itself (because `tip` still needs it).

The implementation is **well-tested**, with new integration tests covering the stacked-branch protection logic, edge cases like self-referential upstreams, and the interaction between `--delete-merged` and other safety checks. The commit message and documentation are clear and thorough, explaining the command’s behavior, safety mechanisms, and edge cases. The code itself is clean and follows Git’s conventions, with no obvious red flags.

This is a **strong candidate for merging**, addressing a real workflow pain point (safe automated branch cleanup) with careful attention to edge cases and API consistency. The stacked-branch protection refinement resolves the last major design question, and the series as a whole is now **implementation-complete** with all planned features and safety mechanisms in place. If you care about branch management or have been following the series, this is the patch to review.