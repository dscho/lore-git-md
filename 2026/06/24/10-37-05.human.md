This is the third patch in Patrick Steinhardt’s four-patch series (v2) refactoring Git’s connectivity checks to support ODB backend independence. It extracts the promisor-object logic from `check_connected()` into a new helper function, `check_connected_promisor()`, without changing behavior. The commit message frames this as a readability improvement that also sets the stage for the next patch, which will generalize the logic to work with non-packfile backends.

The patch is a pure refactoring: it moves 34 lines of nested loops and gotos into a standalone function, adds a descriptive comment block, and updates the call site to use the new helper. The logic remains identical—it still iterates over promisor packs, checks for object presence, and returns early if all objects are found. The only functional change is restoring the `odb_reprepare()` call that was accidentally dropped in v1, ensuring the ODB’s view of packfiles is up-to-date before iteration begins.

This addresses Christian Couder’s feedback from v1, which criticized the original patch’s complex control flow. By isolating the promisor-specific logic, the patch makes the code easier to review and modify, particularly for the next patch in the series. The commit message explicitly credits Christian’s suggestion, signaling responsiveness to review.

The patch touches only `connected.c` and is small (51 insertions, 34 deletions), making it straightforward to verify. It introduces no new edge cases or error conditions, and the existing test suite (e.g., `t5616-partial-clone.sh`) should continue to pass. Reviewers are likely to focus on whether the split is clean—whether `check_connected_promisor()` is now clearly scoped and free of mixed concerns—and whether the function’s interface (taking an `oid_iterate_fn` and `cb_data`) is appropriate for future generalization.

This looks like a well-motivated, low-risk refactoring that directly responds to review feedback. It’s a necessary step toward the series’ goal of ODB abstraction, but it stands on its own as a readability improvement. The patch is ready for review, and substantive feedback is more likely to target the next patch, which will modify this new helper to use generic ODB iteration.