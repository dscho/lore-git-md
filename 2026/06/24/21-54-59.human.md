This is the first patch in Harald Nordgren’s **v5 series introducing `git history squash`**, a new subcommand that folds a commit range into its oldest commit while preserving descendant history. This patch is a **refactoring** that extracts a helper function, `first_parent_tree_oid()`, to eliminate code duplication in `builtin/history.c`.

The helper consolidates logic used in three places to resolve the tree of a commit’s first parent (or fall back to the empty tree for root commits). Each caller previously repeated the same parse-and-copy dance; now they delegate to the helper. The change is purely mechanical and introduces no behavior changes. The patch is small (26 insertions, 32 deletions) and focused, making the codebase slightly cleaner and reducing the risk of future inconsistencies in this low-level operation.

This is a **well-crafted preparatory patch** for the larger feature. The commit message clearly states the motivation (reducing duplication) and the scope (no behavior change). The helper is sensibly named and placed, and the diff is easy to follow. There is nothing controversial here—this is the kind of tidy refactoring that reviewers appreciate as a foundation for more complex work. The patch is ready for integration and should not hold up the rest of the series.