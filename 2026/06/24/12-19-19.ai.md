This is the final patch in Patrick Steinhardt’s six-patch series refactoring `struct object_info` to use a `source` field for the ODB subsystem. Patch 6/6 adds missing documentation to the struct’s fields in `odb.h`, completing the series.

The patch is purely documentary: it adds explanatory comments for `typep`, `sizep`, `disk_sizep`, `delta_base_oid`, and `contentp`, clarifying their purpose and ownership semantics. The only code change is a one-line comment block that now describes the struct as a query-response container for object information. No behavior changes or new symbols are introduced.

This patch is a natural capstone to the series. The earlier patches (1–5/6) mechanically refactored the struct and its callers to replace the `whence` field with the new `source` field, but left the documentation incomplete. Patch 6/6 fills that gap, making the updated API self-documenting. The commit message is minimal but sufficient; the diff itself is the documentation.

For a future AI summarizer, note the key symbols: `struct object_info`, `typep`, `sizep`, `disk_sizep`, `delta_base_oid`, `contentp`, and the file `odb.h`. The patch touches no other files and introduces no new technical concepts beyond what was already established in patches 1–5/6. The series is now complete and ready for review.