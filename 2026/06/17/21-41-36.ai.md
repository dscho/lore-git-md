This is a **surface-level review** of the first patch in Kristoffer Haugsbakk’s now-merged five-part documentation series updating `SubmittingPatches` for trailer conventions. Junio C Hamano points out a minor AsciiDoc formatting inconsistency: the patch places the `[[commit-trailers]]` anchor *after* the `=== Commit trailers` section header, whereas most other sections in `SubmittingPatches` (and other Git documentation files) place the anchor *before* the header. The example given is the `[[patch-flow]]` section, which follows the "anchor-first" pattern.

The feedback is purely stylistic and does not affect the substance of the patch. Junio does not insist on a specific order but notes the inconsistency and suggests aligning with the existing pattern for consistency. This is a typical "nit" observation, common in documentation patches where formatting conventions are enforced to maintain readability and tooling compatibility. The thread context confirms the series is already merged, so this comment is likely retrospective or intended for future reference.