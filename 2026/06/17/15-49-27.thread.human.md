This thread is about a two-patch series (v3) that moves the global `ignore_case` variable into `struct repo_config_values`, eliminating a global and continuing the `the_repository` removal effort. The series is now complete and ready for integration.

Key participants are Justin Tobler (author of patch 1/2) and Tian Yuchen (author of patch 2/2), who have fully addressed Junio C Hamano’s earlier review concerns. The changes align with project conventions, particularly the use of `repo->initialized` in the new `repo_ignore_case()` getter to match the pattern established by `protect_hfs` and `protect_ntfs`. The series touches several subsystems, including the index, filesystem operations, and Windows compatibility layer, but retains existing test coverage and introduces no new tests.

The only remaining loose end is a potential performance overhead in `dir.c` due to repeated getter calls, though this is acknowledged as a necessary trade-off for eliminating global state. The cover letter has been updated to clarify the rationale behind the design, addressing Junio’s feedback. With no further technical changes expected, the series is now on track for merging.