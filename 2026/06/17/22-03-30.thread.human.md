Here’s the updated human-readable summary for a developer catching up on the thread:

This thread reports and resolves a platform-compatibility bug in the `t4216-log-bloom.sh` test, where a long-standing assumption about `char` signedness (true on x86, false on many other architectures) caused TAP parse errors after a June 2026 change tightened the test harness. The issue surfaced when `test_cmp` diff output—previously ignored—began breaking the harness on non-x86 platforms, revealing that the test’s `SIGNED_CHAR_BY_DEFAULT` prerequisite was both unnecessary and problematic. The fix, now uncontroversial, is to remove the prerequisite entirely: it was only used by one test (`highbit1`), which runs `true` regardless of the outcome, making the check redundant.

Todd Zullinger reported the issue, noting it wasn’t a regression but a latent bug exposed by stricter TAP parsing. Patrick Steinhardt initially proposed a workaround to suppress the errors, but Junio C Hamano argued for removing the prerequisite outright, as it added no value. Jeff King later clarified the timing: the bug existed since mid-2024 but only became visible after the TAP harness was made stricter. No objections remain, and the patch will touch only the test script, leaving the Bloom filter logic and on-disk format unchanged. Taylor Blau, who authored the original 2024 change, hasn’t weighed in, but Junio’s position suggests the fix will proceed without further debate.