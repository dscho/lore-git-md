This is a substantive review comment on patch 7/8 of Patrick Steinhardt’s refactoring series for Git’s reference backend. Justin Tobler questions the design choice in the patch that temporarily sets `ref_storage_format` to `REF_STORAGE_FORMAT_UNKNOWN` as a signal to prevent recursive initialization when evaluating "onbranch" config conditions.

The review targets the core logic in `get_main_ref_store()` in `refs.c`, where the patch introduces a temporary override of the repository’s ref storage format to gate the evaluation of "onbranch" conditions during ref store setup. Justin argues that using the storage format as a proxy for initialization state is awkward and suggests that `include_by_branch()` (the function that triggers the recursion) should not rely on it in the first place. His comment implies that a cleaner design might involve a dedicated flag or state variable to track initialization progress, rather than repurposing the storage format field.

This feedback touches on a sensitive part of the series: the interplay between ref store initialization and config parsing. The patch’s current approach is pragmatic—it fixes the recursion without requiring broader changes to the config subsystem—but Justin’s critique highlights a potential technical debt. The review is narrowly focused but substantive, as it challenges the patch’s fundamental design rather than its implementation details. It does not propose an alternative solution, but the implication is that the patch might need rethinking to avoid introducing a fragile pattern.

The thread context shows this series is otherwise well-reviewed and close to integration, so this comment could prompt a meaningful design discussion. If Patrick revises the patch, it may involve either justifying the current approach or refactoring the initialization logic to use a more explicit signal. Either way, the outcome will likely affect how future ref backend changes interact with config parsing.