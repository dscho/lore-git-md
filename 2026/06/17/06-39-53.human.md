This is the tenth patch in Patrick Steinhardt’s 17-part ODB abstraction series, which is the final large-scale restructuring of Git’s object database before shifting to incremental work. The patch wires up the `read_object_stream()` callback for the packed source, completing another step in the interface alignment between loose and packed ODB sources.

The change is mechanical: it moves the logic from `packfile_store_read_object_stream()` in `packfile.c` into a new `odb_source_packed_read_object_stream()` function in `odb/source-packed.c`, registers the callback in the packed source’s constructor, and updates the files source to call it via the ODB interface. The implementation remains functionally identical—it still uses `find_pack_entry()` to locate the object and delegates to `packfile_read_object_stream()`—but now follows the established ODB callback pattern. The diff is small (17 insertions, 17 deletions) and touches only the plumbing layer, with no user-visible behavior change.

This patch continues the series’ theme of exposing packed-source functionality through the ODB interface while maintaining backward compatibility. The callback registration mirrors the loose source’s precedent, and the downcasting pattern (`odb_source_packed_downcast()`) is consistent with earlier patches. No substantive review has occurred yet, but the mechanical nature of the change suggests it will be uncontroversial. The series remains blocked by unresolved refdb lifecycle issues, so this patch won’t progress until those are addressed.