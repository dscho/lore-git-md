This is a substantive review reply to patch 3/8 in Patrick Steinhardt’s refactoring series for Git’s reference backend. Justin Tobler engages with the patch’s logic and suggests a more precise placement for the environment-variable handling.

The patch moves the `GIT_REFERENCE_BACKEND` environment variable application so it only happens when a repository is actually discovered, eliminating a redundant call to `repo_set_ref_storage_format()`. Justin’s feedback is narrowly focused: he agrees with the intent but proposes nesting the new logic immediately above the existing `apply_repository_format()` call rather than where it currently sits. This would make the control flow clearer, since `apply_repository_format()` is the function that actually applies the format configuration, and the environment variable should logically override the repository config just before that happens. The change is small but sharpens the patch’s correctness. No other issues are raised, and the review does not touch on broader series goals or performance implications.