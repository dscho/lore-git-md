This is patch 11/17 in Patrick Steinhardt’s ODB abstraction series, targeting the packed object storage backend. The patch wires up the `for_each_object()` callback for the packed source by relocating `packfile_store_for_each_object()` and its supporting helpers from `packfile.c` to `odb/source-packed.c`. The implementation remains functionally identical, including MIDX and packfile traversal logic, prefix filtering, and object info lookups. The callback is registered in the packed source’s constructor, following the established pattern from earlier patches in the series.

The diffstat (269 insertions, 280 deletions) reflects the mechanical move: the entire `for_each_object` machinery is lifted from `packfile.c` and dropped into `odb/source-packed.c`, with callers in `builtin/cat-file.c`, `builtin/pack-objects.c`, `commit-graph.c`, and `odb/source-files.c` updated to use the new ODB-prefixed interface. The only new public symbol is `packed_object_info_with_index_pos()`, which is exposed to support the wrapper callback but is slated to become file-local once all remaining callers in `packfile.c` are migrated.

Justin Tobler’s surface-level review of the mechanical changes in v2/v3 is now complete for this patch, confirming the callback registration pattern aligns with the loose source precedent. No substantive review of the callback logic, edge cases (e.g., error handling in MIDX traversal, packfile corruption scenarios), or performance implications has occurred yet. The patch remains blocked by the unresolved refdb lifecycle issues that Patrick is addressing in a separate 11-patch fix series.