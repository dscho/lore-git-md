This is Junio C Hamano’s final reply in the thread implementing the global `fetch.followRemoteHEAD` configuration option. The series is now functionally complete and ready for merging, but Junio identifies a remaining design shortcoming in how invalid configuration values are handled.

The open concern centers on the configuration parser in `builtin/fetch.c`, which silently ignores unrecognized values for `fetch.followRemoteHEAD` without distinguishing whether the value is actually needed. Junio outlines an ideal behavior: invalid values should be silently ignored if the fetch operation doesn’t consult them (e.g., one-shot fetches or when a per-remote setting overrides the global one), but should either warn or die if the value is used. He notes that the existing per-remote `remote.<name>.followRemoteHEAD` parser likely behaves the same way, so this isn’t a regression—but it does propagate an existing design limitation. While not a blocker, he suggests adding an in-code `NEEDSWORK` comment to flag this for future improvement.

The rest of the series is uncontroversial and well-executed. It introduces a global fallback setting that follows the established `fetch.prune`/`remote.*.prune` precedence pattern, excludes the "warn-if-not-$branch" value at the global level (as it’s inherently remote-specific), and includes thorough documentation updates (`git-fetch(1)`, `config/fetch.adoc`, `config/remote.adoc`, and a new `advice.fetchRemoteHEADWarn` entry) and test coverage (`t5510-fetch.sh`). All prior review feedback—including case-sensitive value matching, non-fatal error handling, and terminology consistency—has been addressed. Junio’s tone is positive, calling the work "excellent" and thanking the author, which signals the series is otherwise merge-ready. The configuration validation issue may inspire a follow-up patch but doesn’t hold up this effort.