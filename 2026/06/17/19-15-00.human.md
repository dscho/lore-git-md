This is the maintainer's response to the `--track=fetch` feature series, now at version 13. Junio C Hamano reviews the final iteration after a long silence and voices lingering reservations about the workflow the feature encourages, though he acknowledges the implementation itself is solid.

The email confirms the series is mechanically complete: the refactoring in patch 1/2 exposes the tracking-remote lookup logic from `branch.c` so `git checkout`/`git switch` can reuse it, and patch 2/2 implements `--track=fetch` with consistent error messages and behavior. Junio calls out one minor header-include nit in patch 1/2 (removing `string-list.h` in favor of a forward declaration) but otherwise finds the code well executed.

The key takeaway is the tension between implementation quality and design philosophy. Junio remains unconvinced that automatically fetching and checking out remote branches without first inspecting their contents is a safe or desirable workflow, even though the feature solves a documented pain point. His closing line—“I am not sure if we are implementing a good thing”—leaves the merge decision unresolved despite the series being technically ready.