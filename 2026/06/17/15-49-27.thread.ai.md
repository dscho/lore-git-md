Thread identity
Subject: [PATCH 0/2] libify `ignore_case` into `struct repo_config_values`
Series version: v3 (cover letter plus two patches)
Originating author: Justin Tobler (patch 1/2), Tian Yuchen (patch 2/2)
Thread type: patch series (refactoring / libification)

Core problem or goal
Move the global `ignore_case` variable into `struct repo_config_values` to eliminate a global and tie the configuration value to the repository instance it was read from. This continues the `the_repository` removal initiative by encapsulating another piece of repository-specific state without altering its eager-parsing behavior.

Current status
Series is at v3, unmerged, and complete. The first patch (1/2) introduces the `repo_ignore_case()` getter and adds the `ignore_case` field to `struct repo_config_values`; the second patch (2/2) migrates all usage sites. Junio C Hamano’s substantive review concerns from v1 were fully addressed in v2: the getter is named `repo_ignore_case()` and uses `repo->initialized` to match the pattern established by `protect_hfs` and `protect_ntfs`. In v3, the cover letter was updated to directly summarize the rationale behind the getter approach and the reasons Derrick Stolee’s earlier attempt failed, addressing Junio’s feedback on link clarity. Additionally, the three existing getters (`protect_hfs`, `protect_ntfs`, and `core_partial_clone_filter_default`) were updated to check `repo->initialized` instead of `repo->gitdir` for consistency with the new `repo_ignore_case()` pattern. The series is now fully aligned with project conventions and ready for integration; no further technical changes are expected.

Key technical details
- New symbol: `repo_ignore_case()` getter function
- Files touched: `environment.c`, `environment.h`, `read-cache.c`, `name-hash.c`, `dir.c`, `compat/win32/path-utils.c`, and 9 built-in commands (`fetch`, `mv`, `add`, `checkout`, `diff`, `ls-files`, `merge`, `reset`, `rm`)
- Subsystems affected: index, filesystem operations, Windows compatibility layer, built-in commands
- No new tests added; existing test coverage retained
- Builds on prior work migrating `protect_hfs` and `protect_ntfs`
- Introduces a dependency on `repository.h` in `compat/win32/path-utils.c` to avoid refactoring function signatures
- Performance note: repeated getter calls in `dir.c` (e.g., in `git_fspathcmp`, `fspatheq`, `directory_exists_in_index`) may introduce measurable overhead, but the authors justify this as a necessary trade-off for eliminating global state
- Design resolution: the getter includes a `repo->initialized` check, returning the default value (0) when `repo->initialized` is false, matching the behavior of the other three getters. All four getters now consistently check `repo->initialized` instead of `repo->gitdir`.

Open questions and loose ends
1. Performance overhead in `dir.c` due to repeated getter calls (acknowledged but not addressed in this version; no follow-up work planned yet)

Participant positions
- **Justin Tobler** (patch 1/2 author): Clarified early-initialization design in the commit message in v2; aligned with maintainer feedback by adding the `repo->initialized` check to the getter. Position remains fully aligned with project conventions.
- **Tian Yuchen** (patch 2/2 author): Addressed Windows layering and performance trade-offs in the commit message in v2; accepted maintainer feedback, adjusting the getter to use `repo->initialized` instead of `repo->gitdir`. Updated the cover letter in v3 to improve rationale clarity and consistency. Position remains fully aligned with the maintainer’s expectation.
- **Junio C Hamano** (maintainer): Substantive review in v1 questioned the getter’s design, specifically the omission of the repository state check. Position evolved from probing to satisfied after v2 changes; no remaining objections after v3 updates. The series is now ready for integration.

Related work
- Prior migration of `protect_hfs` and `protect_ntfs` into `struct repo_config_values` (pattern for this series)
- Derrick Stolee’s earlier attempt at a similar change
- Broader `the_repository` removal initiative (René Scharfe as primary driver)