This is a zsh completion bugfix that addresses feedback from Junio C Hamano on the initial patch. The thread is a follow-up to a standalone patch that fixed completion for `git -C <path> <command>` in zsh by adding `-C` support to the `_arguments` spec and adjusting the command index calculation.

Junio’s reply points out an inconsistency in the patch: while the `_arguments` spec now includes `-C` alongside other global options like `--git-dir` and `--bare`, the command-index logic only handles `-C` and ignores the others. He suggests the zsh wrapper should mirror the bash implementation (`__git_main` in `git-completion.bash`), which scans all global options (`--git-dir`, `--work-tree`, `-c`, etc.) before determining the command index. The implication is that the current patch leaves other global options (e.g., `git --git-dir=... <command>`) broken in zsh, even though the commit message notes this as a known limitation and offers to extend the fix in a follow-up.

The feedback is substantive but constructive: it identifies a broader architectural issue (inconsistent handling of global options) and provides a clear reference (the bash implementation) for how to address it. No objections to the patch’s direction are raised, and Junio explicitly invites the author to ignore his comments if they prefer. The tone is collegial, and the technical point is well-illustrated with a code snippet from the bash helper.

Key technical details for follow-up discussion:
- File: `contrib/completion/git-completion.zsh`
- Missing logic: handling for `--git-dir`, `--work-tree`, `-c`, and other global options in the command-index calculation
- Reference implementation: `__git_main` in `git-completion.bash` (lines 100–140)
- Likely next step: a v2 patch extending the `while` loop to cover all global options, not just `-C`

The patch remains a well-motivated bugfix for a common workflow (`git -C`), but Junio’s feedback elevates it from a narrow fix to a potential cleanup of zsh’s global-option handling. The author’s response will determine whether the fix stays scoped to `-C` or expands to address the broader issue.