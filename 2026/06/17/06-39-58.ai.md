This is patch 15/17 in Patrick Steinhardt’s ODB abstraction series, completing the callback wiring for the packed object source. The patch adds stub implementations for five callbacks that the packed backend does not support (`write_object`, `write_object_stream`, `begin_transaction`, `write_alternate`) and one that is a no-op (`read_alternates`). Each stub returns an appropriate error or zero, matching the loose source’s precedent. The callbacks are registered in the packed source’s constructor, bringing the interface into full alignment with the loose source’s structure.

The change is purely mechanical and follows the established pattern: each callback is defined with `UNUSED` parameters, returns an error (or zero for `read_alternates`), and is wired up in `odb_source_packed_new()`. No behavioral changes are introduced; the packed backend remains read-only, and the alternates logic is a no-op because packed sources do not support alternates. The diff is small (42 lines) and confined to `odb/source-packed.c`.

This patch is the penultimate step in the series, leaving only the final interface cleanup (patch 17/17) to complete the ODB abstraction. The series remains blocked by unresolved refdb lifecycle issues, but this patch itself is uncontroversial and ready for merging once the dependency is resolved. No substantive review has occurred yet, but the implementation is straightforward and consistent with the loose source’s design.