This is a short, friendly exchange in the ongoing discussion about Harald Nordgren’s patch to add `--abort` hints to rebase failure and status messages. The thread is now effectively settled.

Junio C Hamano (the maintainer) and Phillip Wood (the primary reviewer) are aligned on the approach for the exec-failure case in `sequencer.c`: the rebase will print two separate messages—one explaining what went wrong and how to continue, followed by a generic, neutral hint about `--abort`. Junio also notes that he is fine with the hint being non-configurable, treating it as a low-cost training aid rather than a workflow recommendation.

No code changes have been posted yet, but the discussion is now purely about implementation details. The next step is likely a revised patch from Harald that splits the exec-failure message into the agreed two-part structure. The rest of the series (status-message additions in `wt-status.c`) remains uncontroversial.