This is patch 11/17 in Patrick Steinhardt’s ODB abstraction series, specifically a reply clarifying the visibility of `packed_object_info_with_index_pos()`. The exchange resolves a mechanical uncertainty from Justin Tobler’s surface-level review: Patrick confirms the function is intentionally exposed as a permanent public symbol, not a temporary intermediate like `find_pack_entry()`. The use case is direct packfile lookups outside the packed ODB source (e.g., single-pack scenarios), so the symbol remains part of the public API.

The context is the ongoing ODB abstraction effort, where this patch wires up the `for_each_object()` callback for the packed source. The thread remains blocked by unresolved refdb lifecycle issues (memory leaks, duplicate refdb creation) requiring an 11-patch fix series, but this specific clarification removes a minor ambiguity about symbol exposure. No deeper technical discussion of the callback’s logic or edge cases occurs here.