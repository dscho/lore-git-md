This is a process and API-design discussion in the final stages of Harald Nordgren’s `git branch --delete-merged` series (v15). Phillip Wood gently reminds Harald of the project’s convention to wait for discussion to settle before sending a new iteration, noting that sending a revision shortly after a partial review creates unnecessary list churn. Harald acknowledges the awkwardness of the current double assignment of the `force` flag but agrees that unpacking all flags at the start of the function is clearer and safer, resolving one of the two remaining API consistency issues. The second issue—replacing the boolean `dry_run` parameter in `delete_merged_branches()` with the flags-based API—remains unresolved and still blocks test integration for the `--dry-run` feature. No new technical concerns are raised; the thread is now waiting on the final mechanical patch to complete the API alignment.