This is a brief, positive review comment from Junio C Hamano on the `git refs update` subcommand in Patrick Steinhardt’s patch series. Junio confirms that the proposed `git refs update` command correctly mirrors the atomic creation check already implemented in `git update-ref`—specifically, using `$NULL_OID` (or `0{40}`) as the `<old-value>` to ensure the reference does not already exist before writing. He notes a minor typo (missing `<refname>` in the example) but otherwise endorses the approach.

The comment signals that **Patch 3/5 (`git refs update`)** is on the right track for atomicity, a key design concern from earlier in the thread. Junio’s tone is constructive and suggests he now sees the subcommand as technically sound, though the broader debate about whether `update` should retain its implicit creation behavior (or require explicit use of a new `create` subcommand) remains unresolved. Expect this point to resurface in future iterations or discussions.