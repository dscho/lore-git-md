This is the final patch in Patrick Steinhardt’s 17-part ODB abstraction series, completing the interface alignment between the packed and loose object database sources. The patch removes the `files` parent pointer from `struct odb_source_packed`, allowing the packed source to function as a standalone backend rather than being tied to the files source.

The change is mechanical: the constructor `odb_source_packed_new()` is updated to take the object database, path, and locality flag directly instead of a `struct odb_source_files *`, and all internal references to the parent source are replaced with direct access to the packed source’s own fields. The diff touches four files (`odb/source-files.c`, `odb/source-packed.c`, `odb/source-packed.h`, and `packfile.c`), updating constructor calls, downcasting logic, and path handling to reflect the new standalone status. No behavioral changes are introduced; the packed source’s functionality remains identical, but its interface is now fully consistent with the loose source’s precedent.

This patch resolves the last structural asymmetry in the ODB abstraction, ensuring both sources use an embedded `struct odb_source` base, share identical constructor signatures, and carry no parent pointers. The series as a whole is now callback-complete, with all 13 required callbacks wired up and MIDX interfaces refactored to use the concrete packed source type. The only remaining blocker is the unresolved refdb lifecycle issues Patrick has diagnosed, which require an 11-patch fix series dependent on the earlier “ps/setup-centralize-odb-creation” work. Justin Tobler’s surface-level review of the mechanical changes in this patch confirms the interface alignment but does not engage with deeper ODB design or refdb implications.