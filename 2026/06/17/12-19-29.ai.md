This is a **substantive maintainer reply** in the ongoing discussion about Harald Nordgren’s patch series to add `--abort` hints to rebase failure and status messages. The thread is now converging on implementation details for the exec-failure case in `sequencer.c`.

Junio C Hamano clarifies and endorses the agreed design: the exec-failure message will be split into two parts—first, an explanation of the failure and how to continue, followed by a generic `--abort` hint. This structure preserves the existing warning while adding the abort option as a neutral escape hatch, avoiding any implication that aborting is the recommended action. Junio explicitly agrees with Phillip Wood that the "failed test" (exec) case should *not* trigger the abort hint if the failure is due to a conflict, reinforcing the distinction between scenarios where continuing is more likely (exec failures) versus those where aborting might be more common (conflicts).

The maintainer also signals openness to making the abort hint non-configurable, though this is presented as a minor aside rather than a requirement. The discussion is now purely about implementation: the next patch revision is expected to split the exec-failure message in `sequencer.c` while leaving the status-message additions in `wt-status.c` unchanged. Consensus has been reached on the approach, with no remaining objections from the maintainer or primary reviewer.