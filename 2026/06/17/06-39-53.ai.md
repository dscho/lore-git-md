This is the tenth patch in Patrick Steinhardt’s 17-part ODB abstraction series (v3), aimed at completing the conversion of packed object storage to the `struct odb_source` interface. This patch wires up the `read_object_stream()` callback for the packed source, moving the logic from `packfile.c` into `odb/source-packed.c` and exposing it through the ODB interface. The change is mechanical: the implementation (`find_pack_entry()` followed by `packfile_read_object_stream()`) remains identical, but the function is now registered as a callback in the packed source’s constructor and invoked via `odb_source_read_object_stream()` in the files source. The diff removes the now-redundant `packfile_store_read_object_stream()` from `packfile.h` and updates the files source to use the ODB-prefixed interface.

The patch follows the established pattern of the series: relocate logic to the ODB subsystem, register the callback, and update callers to use the new interface. No behavioral changes are introduced, and the callback’s signature and behavior are preserved. This continues the alignment of the packed source’s interface with the loose source’s precedent, ensuring both sources expose the same set of callbacks. The patch is uncontroversial and has not received review comments yet, but it is part of a larger effort that remains blocked by unresolved refdb lifecycle issues (memory leaks, duplicate refdb creation) requiring an 11-patch fix series. Key technical details include the callback’s registration in `odb_source_packed_new()`, the use of `odb_source_packed_downcast()` to access the concrete type, and the removal of the internal `packfile_store_read_object_stream()` API. The patch touches `odb/source-files.c`, `odb/source-packed.c`, `packfile.c`, and `packfile.h`, with a diffstat of +17/-17.