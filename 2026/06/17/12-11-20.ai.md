This is a **brief, substantive reply from Junio C Hamano** in the ongoing thread about Patrick Steinhardt’s `git refs update` subcommand (part of the larger `git refs` consolidation series). The email **accepts Patrick’s proposed solution** to the atomicity concern raised in the parent review.

Junio acknowledges that the existing `git update-ref` already supports atomic creation checks via the `$NULL_OID` sentinel value for `<oldoid>`, and confirms that Patrick’s plan to mirror this behavior in `git refs update` (along with adding a missing test) is the correct approach. The missing `refname` in Patrick’s example is a minor typo, not a design issue.

This reply **resolves the core atomicity debate** for the `update` subcommand: the new `git refs update` will retain the same implicit creation semantics as `git update-ref`, using `$NULL_OID` to enforce atomic "must not exist" checks. The earlier proposal for a dedicated `git refs create` subcommand (introduced in v2) now complements this, giving users both an explicit atomic creation path (`create`) and a backwards-compatible implicit path (`update`).

**Key implications for the series**:
- **Patch 3/5 (`git refs update`)** is now **technically uncontroversial** and ready to merge, pending the addition of the promised test.
- The **design tension** between atomicity and backwards compatibility is resolved in favor of compatibility for `update`, while `create` provides the atomic alternative.
- The thread’s focus shifts to **implementation details** (test coverage, error handling) rather than high-level design.

**Technical details to note for follow-up**:
- The `update` subcommand will use `$NULL_OID` (or `0{40}`) for `<oldoid>` to enforce atomic creation checks.
- The missing test for this behavior will be added in the next iteration.
- The `refname` argument (omitted in Patrick’s example) is a required positional parameter in both `update-ref` and `git refs update`.

This exchange **clears the last major blocker** for the series, leaving only minor polish (tests, documentation) before integration. Expect a v3 iteration with the test fix and any remaining cleanup.