This is a process-oriented reply in the `git branch --delete-merged` series, now at v15. Phillip Wood gently reminds Harald Nordgren of the project’s convention to wait for discussion to settle before posting a new iteration, especially when a reviewer has signaled they will send more feedback soon. The exchange clarifies a minor API detail: Harald agrees to unpack the `force` flag at the start of `delete_branches()` for future-proofing, even though it creates a small stylistic awkwardness (a double assignment). This resolves the first of two API consistency issues that were blocking test integration; the second—replacing the boolean `dry_run` parameter in `delete_merged_branches()` with the flags-based API—remains unresolved and still prevents the `--dry-run` test suite from being integrated. No new technical concerns are raised; the thread is now waiting on the final mechanical patch to align the API.