This is a **substantive review reply** in the ongoing discussion about Harald Nordgren’s patch to add `--abort` hints to rebase failure and status messages. The thread remains focused on the exec-failure case in `sequencer.c`, where Phillip Wood now clarifies and softens his earlier objection.

Phillip acknowledges Junio C Hamano’s framing of the `--abort` hint as a low-cost "training wheel" for new users rather than a workflow recommendation. He explicitly accepts the proposal to print two separate messages: one explaining the failure and how to continue, followed by a generic `--abort` hint. This resolves the earlier tension—Phillip no longer sees the hint as misleading, provided it is presented as a neutral escape hatch rather than a suggested action. The change would affect only the exec-failure path in `sequencer.c`; the status-message additions in `wt-status.c` remain uncontroversial.

No code changes have been proposed yet, but the discussion has shifted from "whether" to "how" to implement the hint. The next step is likely a revised patch that splits the exec-failure message into two parts, keeping the existing warning intact and appending a generic `--abort` suggestion. The thread is now closer to consensus, with the maintainer and primary reviewer aligned on the approach.