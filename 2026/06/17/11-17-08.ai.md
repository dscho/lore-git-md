This is the latest update in Harald Nordgren’s `git branch --delete-merged` series, now at v15. The email is a brief reply to Phillip Wood’s feedback on API consistency, confirming that Harald will adopt Phillip’s suggestion to unpack the `force` flag at the start of `delete_branches()` for better future-proofing. The change is mechanical and uncontroversial—Harald acknowledges the awkwardness of the current double assignment but agrees that the clarity and safety of unpacking all flags upfront outweighs the minor stylistic cost.

The series remains blocked on two API consistency issues (the `force` flag and the boolean `dry_run` parameter in `delete_merged_branches()`), but this reply signals that the first of those is now resolved in principle. The test suite overhaul (shared fixtures, consolidated assertions, realistic workflows) is still in progress, but test integration for `--dry-run` cannot proceed until the second API issue is fixed. No new technical concerns are raised; the thread is now waiting on the final mechanical patch to align the API.