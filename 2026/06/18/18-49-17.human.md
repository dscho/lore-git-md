This is a **feature-adjacent documentation patch** in the final stages of review. Harald Nordgren provides a real-world example to reinforce the motivation for his patch series, which adds `git rebase --abort` hints to failure and status messages. The example demonstrates a common pain point: during an ill-planned rebase with `-x` (exec), a user accidentally runs a test that doesn’t exist yet, leaving them stuck with no clear "escape hatch" other than `--continue`. The current output omits any mention of `--abort`, even though starting over might be the most sensible action in this scenario.

The email is not a code change but a narrative supplement to the ongoing discussion. It illustrates the user experience gap the patch aims to fill—making `--abort` more discoverable in all rebase interruption contexts, not just conflicts. The example aligns with the consensus reached in prior discussion: the exec-failure message should split into two parts (failure explanation + neutral `--abort` hint) to avoid implying abort is the recommended action. Junio and Phillip Wood have already endorsed this design, and the next patch revision is expected to implement it.

This contribution is well-motivated and uncontroversial. The example strengthens the case for the patch by grounding it in a concrete, relatable workflow. The series touches three files (`sequencer.c`, `wt-status.c`, and a test script) and follows established patterns for advice messages. No further objections are expected; the thread is now focused on final implementation details.