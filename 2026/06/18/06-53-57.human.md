This is a brief, matter-of-fact reply from Patrick Steinhardt to Justin Tobler’s review of patch 3/8 in the reference-backend refactoring series. Patrick accepts Justin’s suggestion to move the `GIT_REFERENCE_BACKEND` environment-variable handling so it sits immediately above the `apply_repository_format()` call, where it more clearly overrides the repository config just before the format is applied. The change is purely mechanical and uncontroversial; no further discussion or follow-up is needed. The patch remains a small, well-motivated cleanup that tightens the initialization sequence.