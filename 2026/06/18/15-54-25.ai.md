This is the maintainer's merge notice for the ref-filter performance regression fix. Junio C Hamano confirms that the v4 patch from Tamir Duberstein fully addresses all review feedback—particularly Patrick Steinhardt's comments—and marks the topic ready for the `next` integration branch. The patch has already been merged into `maint` as a regression fix for Git 2.51.0, restoring prefix-scoped iteration performance in `git branch`, `git branch --remotes`, and `git tag` commands.

The email itself is brief and procedural, but it signals the end of the review cycle for this patch. No new technical content is introduced; the focus is on the maintainer's acceptance and branch placement. The patch is now considered stable and will graduate to `master` in the next integration cycle unless new issues emerge during broader testing in `next`. This is a routine confirmation of a well-reviewed, uncontroversial regression fix.