This is a **test suite fix** for a platform-compatibility issue in `t4216-log-bloom.sh`, specifically addressing the `SIGNED_CHAR_BY_DEFAULT` prerequisite that causes TAP parse errors on non-x86 architectures. Junio C Hamano replies to Patrick Steinhardt’s proposed fix, noting that the problematic code dates back to mid-2024 and questioning why the issue wasn’t caught earlier on non-x86 platforms.

Junio agrees with Patrick’s assessment that the `SIGNED_CHAR_BY_DEFAULT` prerequisite serves no functional purpose—it is only used by a single test that runs `true` regardless of the outcome. He leans toward simply removing the entire check, as the prerequisite is unused elsewhere and the test’s behavior (skipping on non-x86) doesn’t justify the complexity. This aligns with Patrick’s first suggested fix but goes further by eliminating the prerequisite entirely rather than converting it to a `test_lazy_prereq`.

The email does not include a revised patch, but Junio’s preference is clear: the simplest solution is to drop the `test_cmp` check and the associated prerequisite. This would resolve the TAP parse errors while avoiding any changes to the test’s logic or the underlying Bloom filter code. The discussion remains open on whether Taylor Blau (the original author of the 2024 change) has additional context, but Junio’s position suggests the fix is likely to be uncontroversial. Key symbols: `SIGNED_CHAR_BY_DEFAULT`, `test_cmp`, and the `highbit1` test case in `t4216-log-bloom.sh`.