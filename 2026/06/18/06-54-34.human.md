This is a refactoring patch in Patrick Steinhardt’s 8-part series modernizing Git’s reference backend. It removes the now-unused `chdir_notify_reparent()` function from the `chdir-notify` subsystem after earlier patches in the series eliminated all its callers.

The change is mechanical and uncontroversial: the function’s only purpose was to register a callback that adjusted relative paths when the working directory changed, but the series has already migrated all ref backends to absolute paths, making the helper obsolete. The patch deletes 26 lines from `chdir-notify.c` and updates the header comment in `chdir-notify.h` to remove references to the dropped function. No behavior changes remain; this is pure cleanup.

This patch sits in the middle of the series (5/8) and depends only on the preceding commit, which removed the last caller. It’s a small but necessary step toward the series’ broader goal of simplifying ref backend path handling. The commit message is concise and accurate, and the diff is minimal and focused. There’s no indication of review feedback yet, but the change is straightforward enough that it’s unlikely to attract debate.