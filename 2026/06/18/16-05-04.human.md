This is a **substantive review** from Jeff King (Peff) in the ongoing thread about Pablo Sabater’s patch series improving `git log --graph` rendering of commits with excluded parents. The series is currently blocked by a critical interaction between the lookahead logic in `graph_peek_next_visible()` and the prio-queue refactoring in `revision.c` (commit `dd4bc01c0a`, now in `master`).

Peff’s email digs into the technical feasibility of Pablo’s proposed workaround for the prio-queue limitation. He confirms that the three key lookahead flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) can indeed be set by inspecting only the next commit in the queue, even though the original implementation scanned further ahead. This is a meaningful clarification: while the lookahead was designed as an optimization to avoid redundant indentations, it is not a correctness requirement, and the graph will still render correctly with limited lookahead. Peff’s analysis suggests the workaround is viable, though he notes he hasn’t deeply thought through the problem space beyond reading the existing code.

The email also resolves a procedural question: Pablo had asked whether to base his work on the `next` branch (which included `dd4bc01c0a`), but Peff clarifies that the prio-queue refactoring is already in `master`, so basing there is fine. He adds a useful general note for future contributions: avoid building on `next` unless the dependency is explicit and intentional, as `next` contains many in-flight topics that may not graduate to `master`.

This review advances the thread by validating the technical path forward. The series is still **not ready for merging**, but the blocker is now better understood, and the workaround looks feasible. The next step is for Pablo to implement the limited-lookahead version and seek further review. Peff’s engagement here is a strong signal that the series is being taken seriously, and his willingness to believe the workaround is sound (based on Pablo’s explanation) suggests the core visualization logic is on the right track.