This is the final exchange in Matt Hunter’s 7-patch series introducing the global `fetch.followRemoteHEAD` configuration option. The thread is now focused solely on a design nuance around configuration value validation.

Junio’s earlier review identified that the current implementation silently ignores invalid values for `fetch.followRemoteHEAD`, even when those values are actively used. He proposed an ideal behavior where invalid values would be silently ignored if unused (e.g., in one-shot fetches) but would warn or die if consulted. However, he acknowledged that the existing configuration callback mechanism makes this difficult to implement cleanly, and that the per-remote `remote.<name>.followRemoteHEAD` parser likely has the same limitation. He suggested adding an in-code `NEEDSWORK` comment to flag this for future improvement but did not consider it a blocker for merging.

Matt responds by clarifying his stance: he prefers *not* to die on unrecognized values, even when they are used, because future Git versions might introduce new valid values. Instead, he suggests falling back to the "never" behavior (effectively disabling `followRemoteHEAD`) and emitting a warning. He offers to add the `NEEDSWORK` comment and a warning during parsing to match the per-remote behavior, then call the series ready for merging.

The tone is collaborative and pragmatic. Junio’s earlier praise ("this looks excellent") signals the series is otherwise complete and well-executed. The remaining discussion is about how to document and handle a known design shortcoming, not about whether the feature itself is sound. This is a minor cleanup step before the series lands.