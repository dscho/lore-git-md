This is a brief maintainer note on patch 15/17 of Patrick Steinhardt’s ODB abstraction series. Junio C Hamano flags a minor type mismatch in the `write_object` callback stub: the `len` parameter is declared as `unsigned long`, but an in-flight topic (`po/hash-object-size-t`) changes it to `size_t`. Junio has already prepared an evil merge to reconcile the two, ensuring Windows builds remain clean. The patch itself is uncontroversial and the fix is purely mechanical—no functional changes or deeper review are needed. This is a routine integration detail rather than a substantive comment on the series.