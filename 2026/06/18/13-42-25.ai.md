This is a **substantive review** of the test suite for Harald Nordgren’s `git branch --delete-merged` series (v14, patch 4/6). Phillip Wood identifies a new, high-impact design question: whether the command should protect branches that serve as upstreams for *other* branches not slated for deletion. This is a **stacked-branch safety concern**—if a merged branch has unmerged branches stacked on top of it, deleting it could break the user’s workflow.

The review also revisits earlier feedback on test structure, now with a sharper focus on **realistic scenarios** and **safety validation**. Phillip suggests:
- **Repository naming**: Replace `pm-*` prefixes with clearer names (`upstream`, `fork`, `repo`) to improve readability.
- **Subshell usage**: Use subshells for long command sequences in the same directory (e.g., setup), but avoid them for `test_config()` calls (which rely on `test_when_finished()`).
- **Test consolidation**: Merge isolated test cases into fewer, comprehensive scenarios that verify both deleted *and* preserved branches (e.g., confirm unmerged branches are skipped while merged ones are deleted).
- **Output validation**: Replace `test_grep` with `test_cmp` for exact output matching.

The **new safety proposal**—building an `strset` of branches to delete, then filtering out those that are upstreams of non-deleted branches—would add a layer of protection for stacked workflows. This is a **non-trivial behavioral change** that could affect the series’ core logic, though Phillip frames it as an open question rather than a blocking issue.

The review is **tested-level**: Phillip does not report running the code, but his feedback is grounded in deep familiarity with Git’s branch management and test infrastructure. The stacked-branch concern is particularly relevant for users of `git rebase --onto` or `git worktree`, where branches often depend on each other.

This email advances the thread by introducing a **potential safety enhancement** and refining the test suite’s scope. The series is otherwise complete (v15 addressed all prior feedback), but this new question could require another iteration if adopted. The test improvements are uncontroversial and will likely be implemented as suggested.