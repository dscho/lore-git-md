This is a **follow-up in the `git log --graph` cascading indentation series**, specifically Pablo Sabater’s response to Jeff King’s critical review identifying a blocker: the patch’s lookahead logic is incompatible with the prio-queue refactoring introduced in commit `dd4bc01c0a`.

Pablo acknowledges the issue and proposes a pragmatic way forward. He confirms that the lookahead mechanism (`graph_peek_next_visible()`) currently scans `revs->commits`, which no longer holds the full queue since `dd4bc01c0a` moved commits into `revs->commit_queue`. While this breaks the lookahead’s ability to scan ahead in queue order, Pablo argues that the three lookahead flags can still be set with limited peeking: `is_next_visible` (whether any commit will be rendered next) can be determined by scanning the queue without order, while `next_has_column` and `is_next_visual_root` only need the first entry. He suggests basing the fix on the `next` branch (which already includes `dd4bc01c0a`) and is willing to attempt a redesign.

The fallback is that even if the lookahead cannot be fixed, the core indentation logic remains correct—it just loses the optimization that avoids redundant indentations. This means the feature would still work, albeit with slightly less elegant output. Pablo’s response is constructive and focused on keeping the series moving forward despite the blocker.

This email advances the thread by clarifying the scope of the problem and proposing a concrete next step (working with `next`). The ball is now in Pablo’s court to send a v6 that addresses the prio-queue interaction, or to document the limitation if the lookahead cannot be salvaged. The series remains **not ready for merging** until this is resolved, but the path forward is clearer.