This is a maintainer integration tweak to Harald Nordgren’s `git history squash` series, now queued in `next`. Junio C Hamano reports two mechanical adjustments he made before merging patch 3/4.

First, he renamed the test script from `t3454-history-squash.sh` to `t3455-history-squash.sh` because `t3454` was already claimed by another in-flight topic. Second, he replaced four raw `grep` invocations with `test_grep` to avoid triggering a new test-lint rule that flags unguarded `grep` calls. The second hunk also shows a suggested improvement: instead of pre-filtering the output with `grep` and then piping to `awk`, the test could let `awk` do the pattern matching directly, but Junio did not apply that change—he only replaced the `grep` calls with `test_grep`.

The email is purely procedural; it documents the trivial merge fixes and does not re-open any technical discussion. The series remains on track for graduation to `master`.