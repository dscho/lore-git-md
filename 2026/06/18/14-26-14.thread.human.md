Here’s the updated human-readable summary for a developer catching up on the thread:

---

This thread proposes a small but useful improvement to Git’s interactive typo correction: when `help.autoCorrect=prompt` is set and you mistype a command (e.g., `git comit`), the prompt will now show the full corrected command *with your original arguments* (`Run 'git commit -m "Hello world"' instead [y/N]?`), reducing ambiguity about whether those arguments will be preserved. The v2 patch, sent by Jishnu C K, is a narrowly scoped follow-up to an earlier version that sparked debate over whether the interactive prompt should become the default. This version sidesteps that debate entirely, focusing instead on enhancing the existing prompt behavior.

Junio C Hamano’s latest feedback is a mix of mechanical and technical critiques. He flags several patch-formatting nits (e.g., extraneous headers in the email body, misplaced auxiliary comments) and a critical correctness issue: the prompt’s arguments aren’t shell-quoted, so copying and pasting the suggested command could misbehave with spaces or special characters. He also questions an unexplained change in the patch that removes the interactive prompt for single-candidate corrections, replacing it with a static list of similar commands—this looks like an accidental regression. The patch doesn’t apply cleanly to the public tree, suggesting it was generated against a modified base. While Junio hasn’t weighed in on the core usability improvement (argument preservation), his feedback implies the patch isn’t ready for merging until these issues are addressed.

Justin Tobler’s earlier review questioned whether the feature was necessary at all, but his concerns were partially addressed by v2’s shift to enhancing the existing `autocorrect=prompt` option rather than changing defaults. Jishnu’s v2 patch avoids the broader debate about default behavior, but the thread leaves open whether the argument-preservation feature is worthwhile as-is or needs further refinement (e.g., deprecating the old prompt format or improving documentation). The patch touches only `help.c` and adds tests for the new behavior, with no changes to on-disk formats or other subsystems.

The next step is for Jishnu to send a v3 that fixes the shell-quoting bug, clarifies the single-candidate prompt change, and rebases cleanly. If those issues are resolved, the patch is likely to be accepted as an incremental improvement—though the broader questions about discoverability of the `autocorrect=prompt` option and whether to make it the default remain unresolved.

---