This is the fourth patch in Harald Nordgren's v16 series introducing `git branch --delete-merged`, a safe automated local branch cleanup command. This patch prepares the existing `delete_branches()` function for bulk callers by adding two new modes: one that skips the HEAD fallback when checking merge status, and another that performs a dry run without actually deleting any refs.

The changes are straightforward and well-scoped. The patch adds two new flags to the `delete_branch_flags` enum: `DELETE_BRANCH_NO_HEAD_FALLBACK` (which prevents falling back to HEAD when a branch has no upstream) and `DELETE_BRANCH_DRY_RUN` (which simulates deletions without modifying the repository). The logic for both is cleanly integrated into the existing function, preserving the behavior for current callers while enabling the new `--delete-merged` functionality. The dry-run mode prints "Would delete" messages instead of performing deletions, matching the style of other Git commands like `git clean --dry-run`.

This patch is a necessary refactoring step for the series, addressing Phillip Wood's earlier feedback about API consistency. The implementation is minimal and focused, avoiding unnecessary complexity. The commit message clearly explains the purpose and scope of the changes. No test coverage is included here, but the next patches in the series (which implement the actual `--delete-merged` logic) appear to cover these new modes thoroughly.

The series as a whole is now technically complete and ready for final review, with all prior blocking issues resolved. This patch itself is uncontroversial and looks like a clean, well-executed refactoring.