This is the final patch in Patrick Steinhardt’s 8-part refactoring series modernizing Git’s reference backend. It removes an unnecessary local buffer in `refs_compute_filesystem_location()` that was only used as an intermediate step before calling `strbuf_realpath()`. The change is a straightforward cleanup: `strbuf_realpath()` can safely operate on the same buffer for both input and output, so the extra allocation and copy step were redundant. The patch deletes six lines and simplifies the control flow without altering behavior.

This is a minor but well-justified refactoring. The commit message clearly explains the motivation, and the diff is small and focused. It’s the kind of tidy improvement that fits neatly at the end of a larger series, wrapping up the preparatory work before any future architectural changes. No controversy is expected—this looks like a clean, uncontroversial cleanup ready for integration.