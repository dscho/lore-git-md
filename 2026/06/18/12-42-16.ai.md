This is a **follow-up response** in the ongoing thread about Pablo Sabater’s v5 patch series improving `git log --graph` rendering of commits with excluded parents. The email is Pablo’s reply to Jeff King’s **substantive review** identifying a critical interaction with the prio-queue refactoring in `revision.c` (commit `dd4bc01c0a`).

Pablo acknowledges the issue and confirms that the lookahead logic in `graph_peek_next_visible()` will not work as-is with the prio-queue traversal model. He proposes a workaround: since the prio-queue only allows peeking at the first entry, the lookahead can still set the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) by inspecting only the next commit, even if it cannot scan further ahead. While this would not preserve the full lookahead functionality (which avoids redundant indentations), it would maintain correct graph rendering.

He asks whether to base his work on the `next` branch (which includes `dd4bc01c0a`) rather than `master`, and expresses intent to attempt a fix. Pablo also clarifies that the lookahead is an optimization, not a correctness requirement: the graph would still render correctly without it, just with potentially redundant indentations.

This response advances the thread by accepting the blocker and outlining a potential path forward. The discussion is now focused on redesigning the lookahead mechanism to work within the constraints of the prio-queue, rather than on the core visualization logic. The series remains **not ready for merging** until this interaction is resolved.