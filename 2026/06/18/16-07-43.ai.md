This is a **substantive follow-up review** from Jeff King (Peff) in the ongoing thread about Pablo Sabater’s `git log --graph` visualization series. Peff clarifies a critical technical constraint for Pablo’s proposed workaround to the prio-queue interaction blocker.

The key new detail: the revision traversal machinery currently uses *two* queues—`revs->commits` (a simple linked list) and `revs->commit_queue` (a prio-queue)—and commits may appear in *both* during iteration. This means Pablo’s lookahead logic in `graph_peek_next_visible()` must scan both queues to avoid missing commits or misordering them. Peff suggests refactoring the loop body into a helper function to avoid code duplication, but does not propose changing the underlying queue design, noting that `dd4bc01c0a` intentionally preserved the dual-queue structure to minimize disruption.

This follow-up tightens the scope of the blocker: Pablo’s workaround (limited lookahead) is still viable, but must now account for the dual-queue iteration. The correctness of the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) depends on inspecting *all* pending commits, not just those in the prio-queue. Peff’s observation does not introduce new objections but adds a concrete implementation hurdle that Pablo must address before the series can proceed. The thread remains focused on whether the limited lookahead can work within these constraints.