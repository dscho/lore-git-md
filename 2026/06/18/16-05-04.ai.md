This is a **substantive review** from Jeff King (Peff) in the thread about Pablo Sabater’s `git log --graph` visualization series. Peff engages directly with the critical prio-queue interaction blocker that emerged in v5, responding to Pablo’s proposed workaround and clarifying technical constraints.

Peff confirms that the prio-queue refactoring (commit `dd4bc01c0a`, now in `master`) fundamentally limits lookahead to the first entry in the queue, making the current `graph_peek_next_visible()` implementation incompatible. He analyzes Pablo’s proposed workaround—limiting lookahead to the next commit—and asks pointed questions about whether the three key flags (`is_next_visible`, `next_has_column`, `is_next_visual_root`) can still be set correctly without full in-order traversal. His tone is collaborative but cautious: he defers to Pablo on the graph-rendering logic but presses for clarity on whether the flags’ correctness depends on commit ordering.

Notably, Peff adds Kristofer Haugsbakk (author of `dd4bc01c0a`) to the thread, signaling that the prio-queue design itself may need input. He also advises Pablo to base future work on `master` (not `next`) and explains the project’s preference for topic-based dependencies over `next` to avoid unintended coupling.

The review advances the thread by validating the feasibility of Pablo’s workaround while leaving open whether the limited lookahead will introduce redundant indentations or other artifacts. The discussion is now focused on whether the three flags can be set correctly with only the next commit, and whether the prio-queue’s constraints can be worked around without redesigning its underlying data structure. The series remains blocked until this is resolved.