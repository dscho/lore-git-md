This is the fourteenth iteration of Harald Nordgren’s feature series adding `--track=fetch` to `git checkout` and `git switch`. The series remains split into two patches: the first refactors `branch.c` to expose tracking-remote lookup helpers, and the second implements the fetch-on-checkout behavior. Version 14 makes only a minor header cleanup—replacing an explicit `#include "string-list.h"` with a forward declaration of `struct string_list`—to address Junio’s suggestion from v13.

The technical implementation is complete and approved; all substantive feedback from Phillip Wood’s v6 review has been incorporated, and the test suite now covers 276 lines of edge cases. The series is ready to merge from a code perspective, but Junio has repeatedly expressed reservations about whether the workflow benefit justifies the feature. Harald’s earlier reflection on the process—acknowledging that technical review proceeded before the feature’s fundamental value was settled—remains unresolved. No new input on the workflow question has appeared in this iteration, so the merge decision still hinges on Junio’s final assessment of the feature’s merit.