This is a surface-level review reply in the now-closed `git history squash` thread. D. Ben Knoble notes a minor procedural point: he thought a recent test-lint rule change had explicitly allowed bare `grep` when used as a filter (i.e., when its output is redirected), but Junio C Hamano’s merge tweak replaced four such instances with `test_grep` anyway. The observation is purely about test-lint policy, not about the squash implementation itself, and the thread remains closed with the series queued in `next`.