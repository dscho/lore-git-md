This is the maintainer's merge notice for a performance regression fix that has now landed in the `next` branch. The patch restores prefix-scoped iteration in the ref-filter subsystem, fixing a 25x slowdown in `` `git branch` ``, `` `git branch --remotes` ``, and `` `git tag` `` commands introduced in Git 2.51.0.

Junio marks the topic ready for `next` after Tamir Duberstein sent a v4 that incorporated all review feedback: the commit message now explains the historical context, the performance tests run against both ref backends, and the patch preserves the `--start-after` functionality while eliminating the unnecessary I/O that caused the regression. The fix is targeted at the `maint` branch because it affects released versions.

The discussion involved Tamir, Karthik Nayak, Patrick Steinhardt, and Junio; all review comments have been addressed. Benchmarks confirm the fix brings performance back to pre-regression levels (0.11 s vs 2.8 s with 10 000 loose refs). This looks like a clean, well-tested regression fix that is ready to graduate.