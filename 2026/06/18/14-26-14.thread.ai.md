Thread identity
Subject: interactive typo correction for Git commands
Series version: v2 (standalone patch)
Originating author: Jishnu C K
Thread type: feature patch

Core problem or goal
When a user mistypes a Git command, the existing `help.autoCorrect=prompt` configuration option provides an interactive prompt to re-execute the corrected command, but it omits the user’s original arguments from the prompt. This leaves users uncertain whether their arguments will be preserved. The v2 patch preserves the full command line (including arguments) in the prompt, improving usability without altering the default behavior.

Current status
The v2 patch is a narrowly scoped, uncontested enhancement to the existing `help.autoCorrect=prompt` code path. It directly addresses Junio C Hamano’s feedback by preserving arguments in the prompt while avoiding the v1 debate over changing the default behavior. The patch is technically ready and aligns with Junio’s preference for incremental improvements. The broader design question—whether to make the interactive prompt the default—remains unresolved but is orthogonal to this patch.

Key technical details
Files touched: `help.c`, `t/t9003-help-autocorrect.sh`
New behavior: `help_unknown_cmd()` now accepts the full argument vector, allowing the prompt to show the complete corrected command (e.g., `Run 'git checkout neo' instead [y/N]?`). Callers that cannot provide the argument vector (e.g., `builtin/help.c`) pass `NULL`, and the prompt falls back to the old format (showing only the command name). The change is backward-compatible, touches only the help subsystem, and introduces no new symbols or on-disk format changes.
Test coverage: verifies argument preservation in the prompt when `help.autoCorrect=prompt` is set. Tests for the v1 default-interactive behavior were dropped.

Open questions and loose ends
- Should the argument-preservation feature be merged as-is, or does it require further refinement (e.g., deprecating the old prompt format or improving documentation)?
- Is the broader debate about changing the default behavior now moot, or will it resurface in a future patch?
- How should the project address discoverability of the `help.autoCorrect=prompt` option? The v2 patch does not tackle this, leaving it as a documentation or UX issue.

Participant positions
Junio C Hamano: **high weight** -- prefers incremental improvements to the existing `autocorrect=prompt` code path over changing defaults. The v2 patch aligns with this preference and is likely to be accepted if the argument-preservation feature is deemed worthwhile.
Justin Tobler: **high weight** -- questioned the necessity of the v1 patch, arguing the existing `autocorrect=prompt` option was sufficient. His concerns are partially addressed by v2, which enhances rather than replaces the existing functionality. His stance on the argument-preservation feature remains unclear.
Jishnu C K: **high weight** -- shifted focus from changing the default behavior (v1) to enhancing the existing `autocorrect=prompt` option (v2). Argues that argument preservation improves usability by reducing ambiguity in the prompt. Does not address backward compatibility of the config option or discoverability in this version.

Related work
The existing `help.autoCorrect` configuration option, which this patch enhances by adding argument preservation to the `prompt` variant. The thread’s broader debate about default behavior and discoverability remains unresolved but is orthogonal to this patch. No other related efforts are mentioned.