Thread identity
Subject: interactive typo correction for Git commands
Series version: standalone patch (no version trailer)
Originating author: Jishnu C K
Thread type: feature patch

Core problem or goal
When a user mistypes a Git command, Git currently prints a list of close matches and exits. The patch proposes to change the default behavior so that, when there is exactly one close match and the session is interactive (stdin and stderr are terminals), Git prompts the user with the full corrected command line (including original arguments) and re-executes if the user answers 'y'. The author argues this improves usability by eliminating the need for users to discover or configure `help.autoCorrect=prompt` and by providing more context in the prompt (preserving arguments). The existing `help.autoCorrect=prompt` configuration option provides identical functionality except for argument preservation in the prompt.

Current status
Patch is under review; no prior versions. The technical implementation (TTY detection, argument preservation) is uncontested, but the necessity and design of the patch are contested. The thread has shifted from technical review to a design debate: whether to change the default behavior (as the patch proposes), enhance the existing `help.autoCorrect=prompt` option (e.g., adding argument preservation and improving documentation), or abandon the patch in favor of documentation improvements alone. Junio C Hamano, the maintainer, has signaled skepticism about changing the default behavior and suggested the patch should build on the existing `help.autoCorrect=prompt` code rather than reimplementing it.

Key technical details
Files touched: `builtin/help.c`, `git.c`, `help.c`, `help.h`
New behavior: `help_unknown_cmd()` now takes the full argument vector to reconstruct the intended command line; prompt logic is gated on `isatty(0) && isatty(2)`. The prompt now shows the full corrected command (e.g., `Did you mean 'git checkout neo'? [y/N]`) instead of just the corrected command name (e.g., `Run 'checkout' instead [y/N]?`).
Test coverage: new tests in `t/t9003-help-autocorrect.sh` verify interactive prompt, non-interactive fallback, TTY detection, and argument preservation.
No on-disk format changes; no new symbols exposed outside the help subsystem.
The patch duplicates most functionality of `help.autoCorrect=prompt` but differs in two ways: (1) it changes the default behavior, and (2) it preserves arguments in the prompt.

Open questions and loose ends
- Should the default behavior change, or should the patch be refactored to enhance the existing `help.autoCorrect=prompt` option (adding argument preservation and improving documentation)?
- If the patch proceeds, should the `help.autoCorrect=prompt` config option be deprecated, retained for backward compatibility, or modified to match the new behavior?
- Does the argument-preservation feature (showing full command context in the prompt) justify the patch’s inclusion, or is it a marginal improvement?
- How should the project balance discoverability (changing the default) against potential disruption to users who rely on the current behavior?
- Is improving documentation for the existing `help.autoCorrect=prompt` option sufficient to address discoverability concerns, or is a code change necessary?

Participant positions
Junio C Hamano: **high weight** -- skeptical of changing the default behavior and questions the patch’s decision to reimplement `help.autoCorrect=prompt` from scratch rather than extending it. Prefers incremental improvements (e.g., adding argument preservation to the existing option) over altering long-standing defaults. Does not reject the feature outright but signals a strong preference for a config-based approach.

Justin Tobler: **high weight** -- questions the patch’s necessity, arguing the existing `help.autoCorrect=prompt` option is sufficient. Suggests improving documentation to address discoverability rather than altering defaults. Indifferent to the argument-preservation feature, seeing limited added value.

Jishnu C K: **high weight** -- argues the patch improves usability by (1) making the interactive prompt the default (removing the configuration barrier) and (2) preserving arguments in the prompt for better context. Proposes two paths forward: keep the patch as-is (changing the default) or rework it to enhance `autocorrect=prompt` (adding argument preservation and improving documentation). Does not address backward compatibility of the config option.

Related work
The existing `help.autoCorrect` configuration option, which provides similar functionality but requires explicit configuration and does not preserve arguments in the prompt. The thread now centers on whether to modify this option (to add argument preservation) or improve its documentation, with Junio favoring the former as an incremental step. No other related efforts are mentioned.