This is a minor follow-up in the `git history squash` thread. D. Ben Knoble replies to Junio C Hamano’s note about replacing raw `grep` calls with `test_grep` in the test script. Ben observes that he thought another in-flight topic had already acknowledged that using `grep` as a filter (with its output redirected) was acceptable, but he concedes his memory might be off.

The exchange is purely procedural and doesn’t reopen any technical discussion. The series remains queued in `next` with Junio’s trivial adjustments, and no further changes are expected. Readers following the thread can safely ignore this message unless they’re specifically tracking test-lint rule evolution.