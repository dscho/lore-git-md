This is the first patch in Patrick Steinhardt’s 8-part refactoring series (v3) modernizing Git’s reference backend infrastructure. It performs a straightforward refactoring: inlining the `check_and_apply_repository_format()` helper function into its two callsites in `setup.c`.

The motivation is clear: a later patch in the series will modify the order of repository format reading and application at one of these callsites, making the shared helper function awkward. By inlining now, Patrick prepares the ground for that change without disrupting the current behavior. The patch itself is mechanical—no logic changes, just code movement—and the diff is clean, showing only the removal of the helper and its direct incorporation into `enter_repo()` and `init_db()`.

This is a classic preparatory refactoring: small, safe, and well-justified by the series’ larger goal. It touches core repository initialization code, so reviewers familiar with `setup.c` (like Junio C Hamano or Justin Tobler) would be well-placed to confirm the change doesn’t introduce regressions. Given the thread context—where earlier versions of the series have already seen substantive review—this patch looks uncontroversial and ready for integration. It’s a minor but necessary step toward the series’ broader cleanup of reference backend initialization.