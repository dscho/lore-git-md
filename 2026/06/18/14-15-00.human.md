This is a review follow-up in Patrick Steinhardt’s 8-part refactoring series for Git’s reference backend. The thread focuses on patch 7/8, which resolves a circular dependency during repository initialization by temporarily overriding `ref_storage_format` to prevent recursive refdb construction when evaluating "onbranch" configuration.

Justin Tobler’s reply accepts the awkwardness of the current design but suggests an alternative: adding an `initialized` boolean flag to `struct ref_store` as a cleaner signal. He acknowledges the complexity of introducing this change but sees it as potentially more intuitive than the current `REF_STORAGE_FORMAT_UNKNOWN` workaround. The proposal is framed as a question rather than a demand, leaving room for Patrick to assess feasibility.

The discussion remains technical and pragmatic, with no objections to the patch’s core goal. This exchange is the last open question in an otherwise well-reviewed series, so the next step will likely be either a v3 incorporating this suggestion or a decision to proceed with the current approach. The tone is collaborative, and the focus is on finding the least intrusive solution to a fundamental design tension in Git’s initialization sequence.