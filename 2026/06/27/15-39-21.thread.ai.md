Thread identity
Subject: `git ls-remote` hangs at 100 % CPU against specific repository
Series version: none (standalone bug report)
Originating author: Steffen Nurpmeso
Thread type: bug report

Core problem or goal
`git ls-remote https://gitlab.xiph.org/xiph/opus.git` enters an endless loop consuming 100 % CPU on Git 2.54.0. The reporter can reproduce the issue reliably but has not provided debug output, bisect results, or a stack trace. The hang is a denial-of-service condition that could affect scripts or CI pipelines.

Current status
Reported; no analysis or fix yet. The issue is reproducible against a public repository, but the root cause is unknown—possibilities include a network-protocol deadlock, a parsing loop in the remote helper, or a credential-subsystem bug.

Key technical details
- Command: `git ls-remote`
- Repository URL: `https://gitlab.xiph.org/xiph/opus.git`
- Git version: 2.54.0
- Dependency updated without effect: expat
- Subsystems potentially involved: transport, credential helpers, remote-curl, HTTP protocol

Open questions and loose ends
- What is the exact call stack during the hang?
- Does the issue occur with other protocols (e.g., `git://`) or only HTTPS?
- Is the problem specific to the repository’s ref advertisement, or does it affect any HTTPS remote?
- Has the reporter tested with `GIT_TRACE=1`, `GIT_TRACE_PACKET=1`, or `strace`?
- Are there any custom credential helpers or Git configuration settings in play?

Participant positions
- Steffen Nurpmeso: reported the bug; provided reproduction case but no further analysis.

Related work
None mentioned.