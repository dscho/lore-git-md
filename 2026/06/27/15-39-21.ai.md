This is a bug report about an **endless loop at 100% CPU** when running `` `git ls-remote` `` against a specific repository (`https://gitlab.xiph.org/xiph/opus.git`). The reporter, Steffen Nurpmeso, can reproduce the issue reliably but has not investigated further. They mention updating `expat` (a dependency) without resolving the problem, suggesting the issue might lie elsewhere in Git or its network handling.

The report is minimal: no stack trace, no bisect results, no debug output. It targets Git 2.54.0, implying the bug is recent or newly triggered. The thread context is empty, so there is no prior discussion or analysis to reference.

This is a **high-severity issue**—a hang at 100% CPU is a denial-of-service condition that could affect scripts or CI systems. However, the lack of detail means the root cause is unknown: it could be a network protocol deadlock, a parsing loop in the remote helper, or a bug in the credential subsystem. The fact that it is reproducible against a public repository makes it actionable for others to investigate, but the report itself provides no technical leads beyond the command and URL.