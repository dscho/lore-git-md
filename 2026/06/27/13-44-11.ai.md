This is a **substantive review** in the `git replay --linearize` thread, responding to Junio C Hamano’s post-merge observation about merge-commit divergence. Phillip Wood confirms Junio’s analysis: `git replay --linearize` does **not** flatten history like `git rebase --no-rebase-merges`; instead, it grafts only the first-parent branch onto the new history, leaving the other branch dangling. Phillip’s reply clarifies the **mechanism**—replay cherry-picks each commit onto its rewritten parent, whereas rebase replays all commits in a linear sequence—without judging whether the behavior is correct. He implicitly endorses Junio’s suggestion that the divergence should be documented and possibly made configurable.

The review is **technically precise**, referencing the exact code path (`pick_regular_commit`, `replayed_commits` mapping) and confirming the “loss” of one branch (X’ or Y’) when replaying a merge commit (Z). Phillip does not propose a fix, but his confirmation strengthens the case for a follow-up patch to either adjust the behavior or add user control. The thread’s next step is likely a documentation/test patch from Toon Claes or Junio to codify the current behavior, followed by discussion on whether to offer a `--first-parent=<branch>` option.