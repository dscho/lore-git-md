This is a clarification response in the `git cat-file --batch-command` remote object metadata series, specifically addressing Karthik Nayak’s question about the server capability advertisement format in patch 9/13. The author, Pablo Sabater, confirms the planned extensibility approach for future metadata features (e.g., `objecttype`) and proposes a documentation update to `gitprotocol-v2.adoc` to formalize the space-separated format (e.g., `object-info=size type`).

The reply directly answers the forward-looking design question: future features will be appended to the same `value` strbuf as space-separated strings, matching the current "size" implementation. Pablo also clarifies that the attribute names (e.g., "size", "type") are arbitrary and chosen for simplicity, and that the protocol will handle unrecognized object IDs by omitting all attributes in the response. The proposed documentation update tightens the protocol spec by requiring full object IDs and explicitly documenting the response format for unknown objects.

No code changes are introduced in this email, but the discussion establishes consensus on the wire protocol’s extensibility mechanism. The response is narrowly focused on protocol design and does not address the architectural concerns raised in later patches (e.g., error handling in patch 10/13 or documentation style in patch 11/13). The thread remains in a clarification phase for this specific patch, with no unresolved technical objections.