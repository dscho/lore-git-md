This is a **minor technical clarification** in the ongoing RFC series to combine `--geometric` and `--cruft` repack modes. The email resolves a brief confusion about a single line in patch 8/10 (`pack-objects: introduce '--stdin-packs=follow-reachable'`).

Taylor Blau initially misread the conditional `if (type != OBJ_COMMIT && type != OBJ_TAG)` as incorrect, proposing to change it to `||`. Junio C Hamano clarified that the original `&&` was correct—it filters out blobs and trees, which are neither commits nor tags. Taylor acknowledges the mistake and confirms the patch remains unchanged. This exchange is purely about code clarity, not functionality, and has no impact on the series’ core correctness or design.

The **larger unresolved issue**—Junio’s earlier critique of the two-phase traversal logic in `--stdin-packs=follow-reachable`—remains untouched. That flaw risks retaining unreachable tags and objects, undermining the series’ goal of cleanly separating reachable and unreachable objects. Taylor previously proposed reversing the traversal order to fix it, but no concrete implementation has been submitted. This patch (and the series) is still **not ready for merging** without further iteration.

For readers tracking the repack machinery overhaul, this email is a footnote; the substantive discussion about reachability correctness is the real blocker. The series’ other patches (especially the integration in 10/10) hinge on resolving this issue. If you’re interested in geometric repacks or cruft pack generation, keep an eye on future revisions addressing the traversal logic. Otherwise, this thread is still in the "wait and see" phase.