This is a **surface-level reply** in the RFC thread combining `--geometric` and `--cruft` repack modes. The email clarifies a minor logical detail in patch 8/10 (`pack-objects: introduce '--stdin-packs=follow-reachable'`), but leaves the series' core correctness issue unresolved.

Taylor Blau acknowledges a misreading of Junio C Hamano’s earlier comment about the conditional `if (type != OBJ_COMMIT && type != OBJ_TAG)` in `mark_included_pack_tip()`. The original code was correct: it filters out blobs and trees (objects that are *neither* commits *nor* tags), while Taylor initially proposed changing `&&` to `||`, which would have broken the logic by rejecting all objects. The exchange is purely a technical clarification—no code changes are made or proposed.

The **larger correctness issue** identified in Junio’s prior review—the two-phase traversal logic in `--stdin-packs=follow-reachable` retaining unreachable tags—remains unaddressed. Taylor previously acknowledged this flaw and proposed reversing the traversal order (prioritizing reachability from refs before marking objects in included packs), but this email does not advance that discussion. The series is still **not ready for merging**, and further iteration is expected to resolve the reachability flaw. This reply is a narrow correction with no impact on the series’ overall design or readiness.

Key technical details referenced:
- **File**: `builtin/pack-objects.c` (function `mark_included_pack_tip()`).
- **Conditional**: `if (type != OBJ_COMMIT && type != OBJ_TAG)` (confirmed correct).
- **Flaw**: Two-phase traversal logic in `--stdin-packs=follow-reachable` may retain unreachable tags (unresolved).
- **Proposed fix**: Reverse traversal order (not implemented in this reply).