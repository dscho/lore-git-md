This is the final patch (7/7) in Karthik Nayak’s v2 series extending `` `git log -L` `` to support range-scoped diff stat, whitespace checking, and the ``-G`` pickaxe. The patch teaches ``-G`` to honor the tracked line range by routing the xdiff pass through the line-range filter introduced earlier in the series. The implementation exposes ``diff_emit_line_ranges()`` in ``diff.c``, threads the filepair’s ``line_ranges`` through the pickaxe callback, and skips scoping under textconv (whose output lacks original line coordinates). ``-S`` is deliberately left whole-file due to architectural constraints; the documentation now clearly distinguishes the scoped behavior of ``-G`` from the whole-file behavior of ``-S``.

The patch touches ``diffcore-pickaxe.c``, ``diff.c``, ``xdiff-interface.h``, and the line-range documentation, and adds extensive test coverage in ``t/t4211-line-log.sh``. The tests verify that ``-G`` matches only in-range changes, confirm the textconv fallback (whole-file search when a textconv driver is active), and ensure the behavior aligns with the documented scoping rules. The series is now complete: all seven patches are implemented, documented, and tested, and the only prior feedback (documentation phrasing on patch 5) has been incorporated. The exclusion of ``--dirstat`` and ``-S`` is explicitly justified, and the code changes are minimal and focused.