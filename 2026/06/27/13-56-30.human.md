This is a brief process follow-up in the ongoing refactoring series that moves the global `excludes_file` variable into `struct repo_config_values`. The author, Tian Yuchen, acknowledges Junio C Hamano’s earlier note about missing threading headers (`In-Reply-To` and `References`) in the v2 submission and promises to include them in future iterations. No technical changes or responses to the substantive guard-design or build-issue feedback are included here; this email is purely about patch-submission hygiene. The thread remains at the same decision point: the build issue has a clear fix (mark the unused `repo` parameter as `UNUSED`), but the guard controversy is still unresolved.