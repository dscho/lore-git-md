This is a **test fix** targeting the rebase autostash test suite. SZEDER Gábor resurfaces a five-year-old patch that corrects a fragile test in `t3420-rebase-autostash.sh`. The test was incorrectly using `! grep dirty file3` to verify that a file no longer contained dirty content after `git rebase --quit`, but the file *should not exist at all* in the conflicted state. The patch replaces the `grep` check with `test_path_is_missing file3`, which properly asserts the file’s absence. This change affects a single test case that runs under three rebase strategies (`--apply`, `--merge`, and `--interactive`), ensuring the fix applies uniformly.

The patch is small (one line changed) and well-motivated, citing prior art (commit 2745817028) and the commit that introduced the bug (9b2df3e8d0). The commit message explains the fragility clearly: `grep` fails not because the content is missing, but because the file itself is missing, which could mask other test failures. The fix aligns with Git’s test suite conventions by using the dedicated `test_path_is_missing` helper.

No reviews or objections have been recorded yet, but the patch still applies cleanly to the current codebase. The thread context confirms this is a standalone fix with no dependencies or follow-up work. The test file (`t3420-rebase-autostash.sh`) and the specific test helper (`test_path_is_missing`) are the key technical details for future discussion. The patch touches no production code, only test logic, so it carries minimal risk of introducing regressions. The author’s note that the patch "fell through the cracks" suggests it was overlooked rather than rejected, and its continued applicability implies it remains relevant.