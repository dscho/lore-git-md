This is a brief follow-up in the ongoing refactoring series to move the global `excludes_file` variable into `struct repo_config_values`. The author, Tian Yuchen, agrees with Junio C Hamano’s suggestion to squash the two patches in the series and to mark the unused `repo` parameter in `repo_excludes_file()` with the `UNUSED` macro in the first patch, removing it in the second. This resolves the build failure under `DEVELOPER=1` flagged by SZEDER Gábor.

The thread is now converging on a v3 that will implement these changes. Squashing the patches simplifies the series by eliminating the intermediate state where the `repo` parameter is declared but unused, which should improve readability and avoid the build issue entirely. The guard design—replacing the temporary `if (repo != the_repository)` check with a stricter `repo->initialized` check—has already been agreed in principle, so the remaining work is mechanical.

This is a well-scoped refactoring patch with no user-visible behavior changes, part of the broader `the_repository` removal effort. The discussion has been constructive and focused on correctness, and the series looks poised to land once the v3 is sent. If you’re following libification work, this is a small but foundational step worth noting.