This is a **reply in the ongoing RFC thread** combining `--geometric` and `--cruft` repack modes. Taylor Blau corrects a minor logical error in patch 8/10 (`pack-objects: introduce '--stdin-packs=follow-reachable'`) but leaves the larger correctness issue unresolved.

The email focuses on a single line in the patch: the conditional `if (type != OBJ_COMMIT && type != OBJ_TAG)` should use `||` instead of `&&` to ensure only commits *or* tags are processed by `mark_included_pack_tip()`. Junio C Hamano clarifies that the original `&&` was not actually wrong—it correctly filters out blobs and trees—but Taylor’s proposed `||` would break the logic entirely, as it would reject all objects (since no object is both a commit and a tag). The exchange is a narrow technical clarification, not a substantive fix.

The **core issue** identified in Junio’s earlier review—the two-phase traversal logic retaining unreachable tags—remains unaddressed. Taylor’s prior reply acknowledged the problem and proposed reversing the traversal order, but this email does not advance that discussion. The patch is still **not ready for merging**, and further iteration is expected to resolve the reachability flaw. This reply is a surface-level correction with no impact on the series’ overall correctness or design.