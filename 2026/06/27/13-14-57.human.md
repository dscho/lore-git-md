This is a surface-level review of Pablo Sabater's patch adding the `remote-object-info` command to `git cat-file --batch-command`. Karthik Nayak focuses on documentation clarity and minor code hygiene issues, not the core functionality.

The review suggests two documentation improvements: first, inverting the approach to documenting which format atoms are supported by `remote-object-info` (adding "Supported by remote-object-info" only to `objectsize` rather than "Not supported" to every other atom), and second, removing the speculative "for now" from the default format description since there's no timeline for adding `objecttype` support. Both are stylistic tweaks to avoid clutter and ambiguity in the man page.

Karthik also flags three minor code issues: adding a "TODO" comment to the format override logic, using `split_cmdline_strerror()` for better error reporting, and questioning why the code checks `opt->format` when it sets the value itself earlier in the function. None of these affect behavior—they're about maintainability and error clarity.

The review is constructive but shallow; it doesn't engage with the patch's architectural decisions (like error handling or transport integration) or test coverage. The feedback is easy to address and doesn't block merging, but it leaves larger questions about the feature's design unexamined.