This is a bugfix for the HTTP transport layer. Johannes Schindelin identifies a regression introduced in commit `663d7abe07ea` (May 2026) that mistakenly rejects HTTPS proxy URLs. The patch adds a missing `return 0` in `set_curl_proxy_type()` so that `https://` proxies are accepted again, restoring the behavior that existed before the regression. The fix is minimal—two lines—and the commit message clearly explains the problem and the solution. Given the author and the straightforward nature of the change, this looks like a safe, uncontroversial correction that should be merged promptly.