This thread began when Steffen Nurpmeso reported that `git ls-remote` would hang with 100% CPU usage when run against the Xiph Opus repository. The issue turned out to be transient—it resolved on its own without any changes to Git’s code—and was likely caused by external factors like the Anubis proxy or network conditions. Michael Montalbo helped diagnose the problem, suggesting temporary configuration tweaks (such as forcing HTTP/1.1 or adjusting low-speed limits) as workarounds, though he later clarified these were meant for debugging rather than permanent fixes.

The thread is now effectively closed. The hang no longer occurs, and no patch or deeper investigation was pursued, leaving the root cause within Git unidentified. While the workarounds provided a way forward for Steffen, the episode remains a curiosity—no similar reports have surfaced, and it’s unclear whether the issue was specific to the proxy, network, or some edge case in Git’s HTTP handling. For now, the matter rests as a resolved but unexplained anomaly.