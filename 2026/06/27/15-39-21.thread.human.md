This thread is a bug report about a severe issue: **`git ls-remote` hanging indefinitely at 100% CPU** when run against the public repository `https://gitlab.xiph.org/xiph/opus.git`. The reporter, Steffen Nurpmeso, has confirmed the problem on Git 2.54.0 but provided no further details—no stack trace, debug output, or bisect results—leaving the root cause unclear. The hang suggests a potential denial-of-service risk for scripts or CI systems relying on `ls-remote`.

The report is currently minimal, with no responses or analysis from other contributors yet. The issue is reproducible against a public repository, which should make it easier for others to investigate, but the lack of diagnostic information means the next steps will likely involve someone else attempting to reproduce the problem and gathering more data (e.g., `strace`, `GIT_TRACE`, or a debugger backtrace). Until then, the thread remains open with no resolution or workaround.