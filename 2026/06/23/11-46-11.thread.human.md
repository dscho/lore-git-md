Siddharth Shrimali’s GSoC 2026 project to improve disk space recovery for partial clones continues to evolve, with weeks 7–8 focusing on refining the proposed `git maintenance run --task=gc-promisor` subcommand. The latest blog post builds on the earlier design, which centers on a configurable grace period (default 30 days) and a per-remote `.keep`-file mechanism to safely prune unreachable promisor objects without breaking future fetches. While no formal patches have been posted yet, the update likely addresses lingering questions about integration with Git’s maintenance scheduler—such as whether `gc-promisor` should run before or after `git gc`—and may include early performance observations or deferrals for large promisor packs.

The technical scope remains anchored in the promisor packfile machinery (`promisor-remote.c`, `packfile.c`) and the maintenance task infrastructure (`builtin/maintenance.c`), with test coverage still planned for a new script (`t/t5329-partial-clone-prune.sh`). Key unresolved issues include edge cases like shallow clones and alternate object directories, as well as configurability trade-offs (e.g., whether the grace period should be set via CLI or config only). The per-remote `.keep`-file safety mechanism, introduced in week 6 to handle promisor packs from multiple remotes, may see further refinements, though its sufficiency for recovery remains an open question. No new feedback from other contributors has emerged yet, leaving the design open to input—particularly on performance implications and safety guarantees. The project is still in a prototyping phase, with major design shifts possible before formal patch submission.