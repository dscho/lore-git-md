This is a bugfix patch targeting GPG signature handling in Git. A new contributor, Antonio De Stefani, fixes a long-standing issue in the `gpg-interface.c` code: the function that was supposed to normalize Windows-style line endings (`\r\n`) was incorrectly stripping *all* carriage returns (`\r`), even those not followed by a line feed (`\n`). The patch renames the function to `strip_cr_before_lf` to clarify its purpose and adjusts the logic to only remove `\r` when it immediately precedes `\n`.

The change is small (18 lines total, 7 insertions, 11 deletions) and focused: it updates the loop condition to check the next character before removing a `\r`, and propagates the rename through both call sites in the file. The commit message is clear and the NEEDSWORK comment that previously flagged this issue is now resolved. This looks like a well-crafted, self-contained fix that addresses a real correctness problem without introducing new complexity.

No test failures or edge cases are mentioned in the commit message, but the change is narrow enough that it seems unlikely to break anything else. The patch is not part of a larger series, so there’s no need to track dependencies or cover letters. If you care about GPG signature handling or Windows compatibility, this is worth a quick look.