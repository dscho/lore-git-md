Here’s a concise narrative summary for a developer catching up on the Git mailing list after a break:

---

The June 2026 "What’s cooking" report from Junio Hamano (#09) captures the state of Git development as 2.55-rc2 enters deep feature-freeze. The most active work revolves around **object database (ODB) abstraction**, led by Patrick Steinhardt, with `ps/odb-source-packed` (now merged to `next`) refactoring packed object sources into a proper `struct odb_source`. Follow-up topics like `ps/connected-generic-promisor-checks` and `ps/odb-generalize-prepare` generalize connectivity checks and introduce a unified `odb_prepare()` interface, enabling non-packfile repositories. Steinhardt’s `ps/refs-avoid-chdir-notify-reparent` (12 commits) also modernizes reference backends by eliminating `chdir_notify_reparent()` calls, though reviewers have flagged potential recursion issues.

**Performance optimizations** are another focus: Karthik K’s `kk/merge-base-exhaustion` speeds up merge-base computation, while Taylor Blau’s `tb/pack-path-walk-bitmap-delta-islands` (merged to `next`) enables reachability bitmaps and delta-islands with `--path-walk` in `git pack-objects`. Karthik’s priority queue optimizations (`kk/prio-queue-*`) further refine commit traversal.

**Experimental commands** are expanding: `hn/history-squash` and `ps/history-drop` add range folding and commit removal to `git history`, while `tc/replay-linearize` introduces `--linearize` to drop merge commits during replay—though this may spark debate about replay semantics. On the usability front, `mh/fetch-follow-remote-head-config` (merged to `next`) adds `fetch.followRemoteHEAD` to simplify tracking remote default branches, and `ps/cat-file-remote-object-info` (12 commits) lets clients request object metadata from remotes without downloading objects, using dynamic capability filtering.

**Stalled topics** include `cl/conditional-config-on-worktree-path` (worktree-scoped `includeIf` conditions) and `jt/config-lock-timeout` (config file locking retries), both awaiting resolution of review feedback. Documentation updates (`jc/submittingpatches-design-critiques`, `kh/submittingpatches-trailers`) and test infrastructure improvements (`mm/test-grep-lint`, `mm/diff-process-hunks`) are also in flight, with the latter introducing `diff.<driver>.process` to let external tools control hunk detection—a design that’s drawn scrutiny over integration complexity.

Key contributors to watch: Patrick Steinhardt (ODB/ref backends), Taylor Blau (pack optimizations), Elijah Newren (merge machinery), and Karthik K (performance). The report highlights 32 "cooking" topics, with several needing rerolls or reviews, so expect activity to continue even as the release cycle tightens.

---