This is the final administrative note in the thread that delivered a well-crafted bugfix for `git log --follow`. The patch series, which improves rename tracking across merge commits, is now complete and queued for the next release.

Miklos Vajna acknowledges Junio Hamano’s confirmation that the trivial merge conflict resolution (already applied to Junio’s `seen` branch) matches exactly what Miklos provided. No further changes are needed; the series will graduate to `master` without a reroll.

The fix addresses a long-standing limitation in `--follow` when dealing with non-linear history, particularly subtree merges. It replaces a problematic global variable with per-commit pathspec tracking, maintains backward compatibility, and includes thorough test coverage. The implementation has reached consensus and is ready for release.