This is a **substantive technical follow-up** in the `paint_down_to_common()` optimization series, finalizing the last implementation detail for v2. Derrick Stolee and Kristofer Karlsson agree to store `last_gen` directly in the `paint_state` struct (renamed from `paint_queue` in v2) rather than peeking at the queue’s top commit. This makes `paint_queue_get()` self-contained and simplifies the loop’s termination condition in `paint_down_to_common()`.

The discussion confirms the **halt condition logic**—`(!p1_count || !p2_count) && !pending_merge_bases` gated by `last_gen < GENERATION_NUMBER_INFINITY`—will live in `paint_queue_get()`, eliminating the need for external parameters. The awkwardness of peeking (checking halt conditions *after* popping but *before* updating counters) is avoided by internalizing state. The `mb_flags` field (for the older fast-exit optimization) is explicitly deferred for a follow-up patch, as its value is diminished by the new side-exhaustion logic.

This exchange resolves the **sole remaining open question** for v2: how to encapsulate state in `paint_state`. The agreed approach—storing `last_gen` in the struct—is a clean, minimal change that preserves the optimization’s correctness while reducing coupling. No behavioral changes or new performance claims are introduced; the focus is purely on code structure. The tone remains collaborative, and the series is now **technically complete** for v2, pending Tian Yuchen’s update. Expect the final version to land with this refinement.