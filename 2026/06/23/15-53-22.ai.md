This is a **bugfix refactoring** in the sequencer subsystem, specifically the first patch in Phillip Wood’s two-patch series (v3) that fixes `git status` display issues during interactive rebase. The patch factors out todo-command parsing into a new `sequencer_parse_todo_command()` function to enable shared logic between parsing and display code, a preparatory step for the second patch’s display fixes.

The refactoring is uncontroversial and addresses Junio C Hamano’s feedback from the parent email. It tightens input validation by rejecting lone CRs (previously treated as comments) and swaps the order of comment detection (now checking for concrete commands before comment lines). Both changes are explicitly accepted by Junio as minor and acceptable. The new function handles NUL-terminated input and uses `starts_with()` instead of `starts_with_mem()`, simplifying the code while preserving core functionality.

Key technical details include the new function’s enum-based dispatch for todo commands, explicit handling of blank lines (NUL, CR LF, or LF), and the removal of redundant comment-line checks in the original `parse_insn_line()` logic. The patch maintains backward compatibility for valid inputs and lays groundwork for the second patch’s display fixes by centralizing parsing logic.

This patch is **ready for merging**—all review feedback has been incorporated, and the maintainer’s final comments confirm the refactoring is sound. The series as a whole targets long-standing usability bugs in interactive rebase workflows, with this patch establishing shared infrastructure for future sequencer improvements. No substantive concerns remain.