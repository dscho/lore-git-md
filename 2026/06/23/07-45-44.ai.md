This is a substantive review of Patrick Steinhardt’s Patch 3/3 in the ODB abstraction series, focusing on the removal of `odb_reprepare()` and the complexity of the control flow in `check_connected()`. Christian Couder echoes Junio C Hamano’s earlier concern about the `odb_reprepare()` removal, noting that the call was originally added in commit b739d971 to handle corner cases and questioning whether `odb_for_each_object_ext()` implicitly refreshes the ODB’s view of packfiles. He requests that the commit message explicitly address why the original rationale no longer applies.

The review also identifies the patch’s control flow as difficult to follow due to nested loops, multiple `goto` statements, and mixed error handling. Christian proposes a preparatory refactoring to split `check_connected()` into two smaller, focused functions: `check_connected_promisor()` (which would handle the promisor-object search and return a clear status) and `check_connected_rev_list()` (which would handle the fallback rev-list logic). This would make the code easier to reason about and review, particularly for edge cases like partial matches or error propagation.

The feedback is technical and specific, targeting correctness and maintainability rather than style or documentation. It suggests that the patch may need revision before merging, especially given the potential regression risk around `odb_reprepare()`. Reviewers focused on ODB abstraction or connectivity checks are likely to engage with these points.