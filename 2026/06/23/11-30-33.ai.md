This is the final administrative note in the thread wrapping up Miklos Vajna’s `git log --follow` improvement series. The series is now complete and queued for merge to `master` after resolving all technical and merge-conflict issues.

Miklos acknowledges Junio’s earlier message confirming that the trivial merge conflict (with commit 42d960748e, line-log integration) was already correctly resolved in Junio’s `seen` branch. No further action is needed; this email is purely a courtesy reply to close the loop.

The patch series itself is a well-crafted bugfix that removes the long-standing limitation of `--follow` failing to track renames across merge commits. It introduces a `follow_pathspec_slab` to store per-commit pathspec information, replacing the problematic global variable approach, while maintaining backward compatibility and Git’s single-path constraint. The implementation touches `revision.c`, `log-tree.c`, and `diff.c`, and includes thorough test coverage in `t4219-log-follow-merge.sh`. Documentation has been updated to remove the outdated caveat about non-linear history. The series has reached consensus and is ready for the next release.