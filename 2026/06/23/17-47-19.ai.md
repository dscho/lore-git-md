This is the author’s response to Phillip Wood’s **substantive review** of the `--track=fetch` feature in `git checkout`/`git switch`. Harald Nordgren directly addresses the core question Phillip raised: *why* the feature is useful, rather than engaging with the code-level feedback.

The email argues that the feature’s value lies in aligning Git’s behavior with user expectations. Harald frames the manual fetch-and-checkout workflow ("`git fetch origin master && git checkout -b topic origin/master`") as a friction point that other tools have automated away. He posits that users intuitively expect `git checkout -b topic origin/master` to start from the *latest* version of `origin/master`, not a potentially stale local copy. By automating the fetch, the feature reduces merge conflicts and duplicated work in fast-moving projects—precisely the workflow benefit Phillip requested in his review.

Harald’s justification is pragmatic: the cost of a fetch is negligible, and the feature matches the mental model of users who assume they’re working with up-to-date remote branches. This response is clearly aimed at building broader consensus, as Junio and Phillip have both emphasized that technical correctness alone isn’t sufficient for merging. The email does not address Phillip’s code nits (e.g., `RESOLVE_REF_NO_RECURSE`, initialization style), suggesting those will be handled in a follow-up patch if the feature’s rationale gains traction.

The tone is measured but slightly frustrated, reflecting the thread’s history: the series is technically complete and approved, yet its inclusion hinges on subjective workflow judgments. Harald’s appeal to Phillip’s earlier feedback ("I assume their mental model is already 'start from the LATEST origin/master'") is a strategic attempt to bridge the gap between technical reviewers and maintainers. Whether this argument suffices to overcome Junio’s reservations remains the open question.