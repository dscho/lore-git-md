This is a follow-up email regarding the newly merged `status.compareBranches` feature, which allows configurable branch comparisons in `git status`. Harald Nordgren is responding to Junio Hamano's post-merge observation about potentially expanding the branch specification to support more ref types using Git's existing DWIM (Do What I Mean) rules.

The core discussion point here is whether to immediately implement support for additional ref types (like tags and local branches) using Git's `ref_rev_parse_rules[]` machinery, or defer this as a follow-up enhancement. Harald suggests treating this as future work since the current implementation (which focuses on remote-tracking branches) is already merged and working well, and expanding the functionality wouldn't require interface changes.

This exchange shows the typical post-merge refinement discussion where the maintainer identifies potential improvements while the contributor assesses whether they warrant immediate action or can be addressed later. The thread has already seen extensive review (28 iterations) and the feature is production-ready in its current form, so Harald's stance here seems reasonable - the suggested enhancement could naturally fit into future development without blocking the current release.