This is a response from the maintainer (Junio C Hamano) to a user feature request about Git's pager behavior. The thread began with Harald Dunkel reporting inconsistent pager usage across Git commands and frustration with `core.pager=cat` breaking `git -p` functionality.

Junio clarifies that `-p` and `-P` flags work as designed by overriding each command's default pager behavior. He points to the existing `pager.<cmd>` configuration in the documentation that allows per-command pager control, suggesting this as a solution for the reported inconsistency. As a concrete example, he shows how to disable paging for `git branch` specifically while preserving the ability to force paging with `-p`.

The response maintains that the current behavior is intentional but acknowledges the configuration complexity. Junio doesn't propose any code changes, instead treating this as a documentation and configuration issue. His tone is characteristically matter-of-fact, focusing on explaining existing mechanisms rather than entertaining behavior changes. The exchange highlights Git's philosophy of providing granular control through configuration rather than imposing universal defaults.