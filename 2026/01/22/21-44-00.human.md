This is a follow-up discussion on Derrick Stolee's `--maximal-only` feature for `git rev-list`, now being reviewed by Junio Hamano.

Junio is asking for clarification on how the new `--maximal-only` option relates to the existing `git merge-base --independent` functionality. Both deal with commit reachability, but it's not immediately clear from the patch whether they serve the same purpose or operate differently under the hood.

The thread context shows this is the second iteration of the patch, which has already addressed naming concerns and added thorough documentation about its behavior and limitations (particularly its incompatibility with `--boundary`). The implementation uses bit 28 of the commit flags and has comprehensive test coverage.

This appears to be the final round of review, with Junio probing for any remaining edge cases or conceptual overlaps before deciding whether to merge. The question suggests he's verifying that this new feature isn't duplicating existing functionality in the merge machinery. Derrick will likely need to explain the mathematical distinction between maximal commits in a reachability order versus merge-base independent sets.