This is a user-facing feature request about Git's pager behavior. Harald Dunkel expresses frustration with the current inconsistent handling of pagers across different Git commands, where some commands use pagers by default while others don't, and the interaction between command-line flags (`-p`/`-P`) and configuration settings like core.pager.

The email doesn't propose a specific technical solution but highlights a real usability pain point - the current behavior can feel unpredictable to users. The mention of `core.pager` being set to "cat" breaking `git -p` suggests there may be an underlying technical issue where command-line flags and configuration don't interact in an intuitive way.

This appears to be the start of a discussion thread rather than a patch submission. The core issue touches on Git's output handling subsystem and how it integrates with the pager infrastructure. The inconsistency mentioned could be worth addressing, though the email doesn't yet suggest how to resolve it. Future discussion in this thread would likely explore whether this is a documentation issue (better explaining current behavior) or warrants actual code changes to make pager behavior more consistent across commands.