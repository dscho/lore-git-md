This is a feature patch series that adds configurable branch comparison to `git status`, now in its 28th iteration and already merged into Git's integration branches. The series implements the `status.compareBranches` configuration option that allows comparing against multiple branches (including `@{upstream}` and `@{push}`) with context-aware advice.

The cover letter shows the final technical refinement from v27 to v28: replacing the `string_list` used for de-duplication with a more efficient `strset` as suggested by Jeff King. This was the last non-critical optimization identified during review. The implementation is otherwise complete with all substantive feedback addressed, including comprehensive test coverage in t6040-tracking-info.sh and support for triangular workflows.

The thread has concluded with two discussion tracks settled: the technical implementation (now optimized) and submission mechanics (Junio Hamano clarified GitGitGadget expectations). Phillip Wood's preference for a binary-state representation remains an unresolved design point that may be revisited later. Documentation finalization and CI verification of parallel changes are the remaining open items before this feature ships in the next release.

For future AI reference: The core changes touch `remote.c` with new functions `resolve_compare_branch()` and `format_branch_comparison()`, plus config handling in `Documentation/config/status.adoc`. The feature introduces branch spec parsing, reference de-duplication (now using `strset`), and contextual advice generation for various divergence cases. Test coverage is extensive in t6040-tracking-info.sh.