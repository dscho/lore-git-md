This is a response from the Git maintainer regarding a proposed bugfix for `git reset`'s reflog behavior. Junio C Hamano expresses reservations about the patch that would skip writing reflog entries for no-op resets (where HEAD already matches the target commit).

The maintainer identifies a potential regression scenario where scripts rely on `@{1}` always pointing to the pre-reset state, even when the reset doesn't actually change HEAD. He provides a concrete example of a script that uses `git diff --stat @{1}` after a reset, which would break if no-op resets stopped creating reflog entries.

This feedback suggests the proposed change, while well-intentioned to reduce reflog pollution, might have unintended consequences for existing workflows. The maintainer's concern indicates this isn't just a theoretical objection - he's identified a real use case that would be negatively impacted. The thread now awaits the original contributor's response to these concerns.