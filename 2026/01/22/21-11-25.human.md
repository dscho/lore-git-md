This is a follow-up discussion about the newly merged `status.compareBranches` configuration feature, which allows `git status` to compare against multiple branches. The conversation focuses on potential future behavior regarding branch name resolution.

Junio Hamano responds to Harald Nordgren's suggestion that expanding the branch specification rules (to support tags and local branches via Git's DWIM rules) could be deferred to a follow-up. Junio argues this would be decided now rather than later, since the current implementation hardcodes remote-tracking branch resolution (prepending `refs/remotes/`) which would make any future change in behavior appear as a regression to users.

The discussion is technical but amicable, with Junio explaining how Git's existing `ref_rev_parse_rules[]` system works for branch disambiguation, and why the current implementation's simpler approach could cause problems if expanded later. This isn't blocking the merged feature - which remains production-ready for its current remote-tracking branch comparisons - but establishes that any broader ref type support would need to be designed carefully to avoid surprising users.

The exchange shows thoughtful consideration of backward compatibility implications, typical of late-stage review for a feature that has already gone through 28 iterations. While the immediate functionality is settled, this thread ensures any future expansion will maintain consistent behavior.