Here's a human-readable thread summary:

Harald Dunkel has raised a concern about Git's inconsistent pager behavior across different commands. Some commands automatically use a pager while others don't, and the interaction between command-line flags (-p/-P) and core.pager configuration isn't always intuitive. A particularly confusing case occurs when core.pager is set to "cat", which can break the -p flag's expected behavior.

This appears to be a genuine usability pain point that affects many users, though the email doesn't yet propose specific technical solutions. The discussion is just beginning, and future replies may explore whether this requires documentation improvements to better explain current behavior or actual code changes to make pager handling more consistent across Git commands. The issue touches core output handling and pager integration, areas that could benefit from more predictable behavior.