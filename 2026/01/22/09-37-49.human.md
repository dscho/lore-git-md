This is a feature proposal about Git's pager behavior. Harald Dunkel expresses frustration with the inconsistent default pager usage across different Git commands - some use it when they shouldn't, others don't when they should. They note that setting `core.pager` to `cat` as a workaround breaks the `-p` flag.

The email doesn't propose specific changes but rather identifies what the author sees as a usability problem with Git's current pager handling. The issue appears to be about making pager behavior more consistent and predictable across all commands, though no concrete implementation suggestions are provided yet.

This seems like a valid ergonomic concern that could benefit from discussion, though the lack of specific proposals means it's more of a starting point than a ready solution. The thread may develop into a discussion about standardizing pager behavior or adding more control over when pagers are invoked.