This is a feature patch series that has already been merged into Git's integration branches, implementing configurable branch comparison in `git status`. The key addition is a new `status.compareBranches` configuration option that allows comparing the current branch against multiple other branches (including `@{upstream}` and `@{push}`) with context-aware advice.

The series consists of two patches: a preparatory refactoring of `format_branch_comparison()` followed by the main implementation. The technical work appears complete with all substantive feedback addressed, including Jeff King's suggestion to use `strset` instead of `string_list` for de-duplication (now implemented in this version). The changes touch documentation, remote.c for the core functionality, and add extensive test coverage in t6040-tracking-info.sh.

The thread has largely concluded with only minor open questions around final documentation phrasing and potential follow-up on Phillip Wood's preference for binary-state tracking. The implementation is solid and well-tested, representing a useful enhancement for users who want richer branch comparison information. The patch author has demonstrated responsiveness to review feedback across 28 iterations, with the changes now in good shape for inclusion.