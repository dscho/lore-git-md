This is a follow-up discussion in the `--maximal-only` feature thread, where Junio C Hamano asks for clarification on how the new option relates to `git merge-base --independent`.

Junio's question probes whether there's conceptual overlap between the new `--maximal-only` flag (which filters commits to only those that are maximal in the reachability order) and the existing `--independent` flag for merge bases (which finds a minimal set of commits that can reach all input commits). The author (Derrick Stolee) had previously mentioned using this for bundle URIs and bitmap optimizations, where identifying frontier commits is valuable.

The thread context indicates the implementation is otherwise complete with naming consensus reached, thorough test coverage, and all prior feedback addressed. This appears to be a final clarification point before potential merging. The question touches on mathematical foundations (partial order theory concepts in Git's revision walking) and whether the new feature duplicates or complements existing functionality.

No technical changes are proposed in this email - it's purely a conceptual question about how the new capability fits into Git's existing reachability query toolkit. The answer would help situate `--maximal-only` in relation to similar operations, which could inform both documentation and potential future optimizations.