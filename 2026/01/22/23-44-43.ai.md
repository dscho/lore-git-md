This is a follow-up discussion in Patrick Steinhardt's ODB abstraction series, specifically about patch 02/14 which deals with flag parameter typing. Taylor Blau is responding to Patrick's earlier message about the prevalence of `unsigned flags` versus enum-based flags in the codebase.

Taylor agrees with Jeff King's (Peff) earlier position that enum flags are preferable to unsigned integers for new code, citing better documentation and type safety benefits. While acknowledging there are currently 219 instances of `unsigned flags` versus only 56 enum-based flag declarations, Taylor suggests the convention should evolve where it makes sense - not advocating for mass conversion of existing code, but recommending enum flags for new development.

This represents a settled discussion point in the thread, with consensus forming around using enum flags for new code while leaving existing `unsigned flags` declarations unchanged. The exchange is purely about coding style preferences rather than any technical blocking issue for the ODB abstraction series.