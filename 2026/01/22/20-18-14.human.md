This is the final confirmation from Junio Hamano that Harald Nordgren's `status.compareBranches` feature is ready for merging after 27 iterations. The feature allows configuring multiple branch comparisons in `git status` outputting (e.g. showing both upstream and push relationships). 

The thread shows all technical concerns have been addressed, including Jeff King's suggestion to use a `strset` for more efficient branch name deduplication. Junio's brief note signals his satisfaction with the state of the patches and that they will be included in an upcoming release. 

This concludes a long-running development thread that introduced a useful workflow enhancement while maintaining backward compatibility. The implementation touches core branch-handling code in `remote.c` and adds extensive test coverage, representing a significant but well-vetted contribution.