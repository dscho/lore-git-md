This is a technical discussion about the ODB abstraction series, specifically addressing the design of the `odb_for_each_object()` interface in patch 7/14. Patrick Steinhardt responds to Justin Tobler's question about why `struct object_info` is passed to the iteration function rather than being handled internally.

Patrick explains three key reasons for the current design: (1) some callers need access to object metadata like mtime or size during iteration, (2) the `whence` field allows callers to identify which backend provided each object, and (3) performance optimization - particularly for the packfile backend where object info is already available during iteration and can be populated without additional lookups. The discussion highlights careful consideration of both current needs and future extensibility in this foundational interface for the pluggable storage backend system.

The exchange demonstrates the series reaching its final refinement stage, with Justin probing edge cases and Patrick providing detailed rationale for design decisions. This patch is part of the larger performance-oriented refactoring to enable alternative object storage backends while maintaining efficiency. The technical depth of the discussion suggests both contributors have substantial context about the ODB subsystem's internals and the series' architectural goals.