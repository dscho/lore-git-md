This is a feature patch series that finalizes the `git repo info --keys` implementation and format unification work. The series builds on previous iterations that were reviewed by Junio Hamano, Patrick Steinhardt, and Jean-Noël Avila, and is now ready for integration into `next`.

The key changes in this version are:
1. Documentation now uses `_NUL_` consistently (per Jean-Noël Avila's feedback)
2. The default output format for `--keys` is now called "lines" (with "default" as a synonym), addressing Patrick Steinhardt's suggestion while keeping the naming clearer for users

The implementation spans `builtin/repo.c` and includes comprehensive tests in `t/t1900-repo.sh` and `t/t1901-repo-structure.sh`. The changes introduce a new `FORMAT_LINES` enum value and ensure consistent behavior across the `repo` subcommands. The series replaces the previously queued `lo/repo-info-keys` topic while maintaining all functionality.

This appears to be the final iteration of this work, with all substantive feedback addressed. The only remaining open point is a minor documentation naming preference ("lines" vs "newline") that doesn't block integration. The patch series is well-structured and follows Git's conventions, with clear commit messages and thorough test coverage.