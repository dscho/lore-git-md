This is a follow-up discussion in an ongoing thread about optimizing sparse-checkout pattern handling. Pushkar Singh points out an additional test case that wasn't covered in Amisha Chhajed's v5 patch series - the handling of semantically equivalent but syntactically different path patterns (like "folder1", "folder1/", and "./folder1") in cone mode.

Pushkar notes that while the current tests cover duplicate identical patterns well, they don't exercise this path canonicalization behavior. He suggests adding a test that modifies the sparse-checkout file directly (bypassing the 'add' command's deduplication) to better verify the 'list' command's normalization behavior. He references a test patch he previously sent that explores this case.

The email builds on Derrick Stolee's earlier review of Amisha's v5 series, where Stolee had recommended squashing the test coverage patch into the main optimization patch and suggested more realistic test cases with nested directory patterns. Pushkar's observation extends this testing discussion to cover another subtle aspect of pattern handling.

This is a constructive addition to the thread, identifying a real but previously untested edge case in the sparse-checkout pattern processing. The optimization itself (switching from O(n^2) to O(n log n) sorting) appears sound and ready, but the test coverage continues to be refined based on community input.