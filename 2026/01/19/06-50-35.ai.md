Test fix for flaky reftable test on Cygwin/Windows 11. This is a follow-up to Ramsay Jones' patch that replaces shell redirection with `test-tool truncate` to work around an intermittent failure in test #29 of t0610-reftable-basics.sh.

Patrick Steinhardt confirms the approach, recalling a previous similar case where switching to `test-tool truncate` resolved flakiness on Cygwin. While the root cause remains unclear (the failure occurs only in parallel test runs on Windows 11), the solution appears reliable. The change is minimal - a one-line substitution in the test script that replaces `: >"$f"` with `test-tool truncate "$f" 0`.

The discussion reveals some investigation was done (including strace analysis) but didn't pinpoint why the shell redirection fails intermittently. Patrick suggests merging the fix despite the unknown root cause, prioritizing test stability over complete understanding. The patch follows Git's pattern of preferring test helpers over shell builtins for reliability, particularly on Windows platforms.