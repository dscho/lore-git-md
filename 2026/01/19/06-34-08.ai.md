This is a review response in an ongoing thread about memory management fixes for push tracking references. Patrick Steinhardt is replying to Jeff King's patch that fixed a memory leak in `branch_get_push_1()` when invalid "simple" push configuration is encountered.

The exchange is brief - Patrick confirms the leak was made visible by Jeff's earlier refactoring work in the series. The fix itself (shown in the diff) adds a `free(cur)` when rejecting mismatched push destinations in the "simple" push mode. This plugs a leak that occurred when the function returned an error without freeing the allocated tracking reference string. 

This continues the thread's technical discussion about ownership semantics for push tracking references, with Patrick providing positive feedback on the fix. The interaction demonstrates the careful review process for even small memory management changes in Git's core code.