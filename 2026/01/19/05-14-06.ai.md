This is a bugfix response in an ongoing thread about configurable branch comparison in `git status`. Jeff King identifies a memory management issue in Harald Nordgren's implementation of the `status.compareBranches` feature.

The key technical point is a potential double-free in the `branch_release()` function where `push_tracking_ref` could be freed twice - once explicitly in the new line and again via `merge_clear()`. This stems from historical memory management in the push tracking code from commit e291c75a95.

Jeff notes this issue is related to memory leaks Harald has been debugging in the overall branch comparison work. He indicates he'll follow up with a patch series to properly fix both the immediate double-free and the related leaks, suggesting the solution requires untangling some legacy memory management in the remote/branch tracking code.

The email shows the thread is progressing toward resolution of the last major technical blocker (memory management issues), with Jeff providing expert review of the low-level memory handling. His response confirms the implementation's structural soundness while identifying a subtle resource management bug that needs addressing before the feature can be finalized.