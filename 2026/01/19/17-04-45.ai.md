Performance optimization for sparse-checkout pattern handling, with test coverage for deduplication behavior.

Derrick Stolee provides final review feedback on Amisha Chhajed's v5 patch series optimizing sparse-checkout pattern sorting. The series changes the pattern collection from O(n^2) insertion sort to O(n log n) via string_list_append() followed by sorting, with explicit duplicate removal. Stolee notes the duplicate removal is already present in some cases outside the diff context, and suggests squashing the test coverage patch into the main optimization patch for a cleaner history.

For the test cases, Stolee recommends more realistic pattern examples that exercise nested directory handling in cone mode, showing how parent/child relationships interact with deduplication. He also points out that the current 'list' command test doesn't fully exercise the deduplication since the 'add' command already handles it, suggesting modifying the sparse-checkout file directly instead. The review maintains the distinction between cone mode (where order doesn't matter and deduplication is safe) and non-cone mode (where pattern order is significant).

This appears to be the final round of review before merging, with all substantive technical issues resolved and only minor test improvements suggested. The thread shows consensus on both the optimization approach and the importance of proper test coverage for the deduplication behavior.