This is a bugfix response in an ongoing thread about configurable branch comparison in `git status`. Jeff King (Peff) identifies a memory management issue in Harald Nordgren's implementation of the `status.compareBranches` configuration.

Peff spots a problematic `free()` call in `branch_release()` that could lead to a double-free when `push.default=upstream` is set, since `push_tracking_ref` may point to memory already freed by `merge_clear()`. He traces this back to legacy code from 2015 (`e291c75a95`) and notes this is related to memory leaks Nordgren has been encountering in the feature implementation.

The response indicates Peff will follow up with a patch series to properly fix both this issue and the leaks Nordgren has observed. This comes at a key moment - the thread summary shows the feature is otherwise structurally complete with thorough test coverage, with the memory leak being the primary remaining blocker before merging.

Peff's intervention here is characteristic of his deep knowledge of Git's memory management patterns and his ability to untangle tricky legacy code issues. His proposed fix should unblock progress on this long-running feature branch comparison work.