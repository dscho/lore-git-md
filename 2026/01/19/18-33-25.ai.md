Performance optimization for sparse-checkout pattern handling, with discussion of path normalization edge cases.

Pushkar Singh follows up on Amisha Chhajed's v5 patch series that optimized sparse-checkout pattern sorting from O(n^2) to O(n log n). While the current tests cover duplicate identical patterns, Pushkar points out they don't test path normalization cases where semantically equivalent but syntactically different paths (like "folder1", "folder1/", "./folder1") should collapse to a single canonical form. He references a separate patch he sent exploring this behavior and offers to help draft additional tests if needed.

This builds on Derrick Stolee's earlier review suggesting more realistic test cases with nested directory patterns and direct sparse-checkout file modification to properly test the 'list' command's deduplication. The thread shows the optimization itself is merge-ready (having addressed all prior feedback), with this being a refinement of test coverage for edge cases in path normalization.

Key technical points:
- The optimization itself (switching from string_list_insert to append+sort) is complete and tested for basic duplicates
- Remaining test gap involves path normalization cases not yet covered
- Pushkar's example shows three syntactically different but equivalent paths that should deduplicate
- Testing may require direct file modification since 'add' command performs its own deduplication first

The discussion remains focused on test coverage rather than the core optimization, which has already been through multiple rounds of review and approval.