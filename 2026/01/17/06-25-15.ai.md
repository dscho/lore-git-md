This is a test modernization patch, part of the ongoing effort to update Git's test suite to use more robust helper functions. Tian Yuchen updates `t1005-read-tree-reset.sh` to replace five instances of the legacy `! test -f` pattern with the preferred `test_path_is_missing` helper.

The change is mechanical but meaningful - the test helper provides better diagnostics when tests fail by explicitly stating that a path exists when it shouldn't, rather than silently failing the negation check. The patch touches only test assertions, not the actual test logic or behavior being verified.

This appears to be a straightforward, uncontroversial improvement following established test suite conventions. The patch is small and focused, changing exactly what it claims to in the commit message. It's part of a broader pattern where contributors are systematically updating tests to use the more informative helper functions introduced in recent years.