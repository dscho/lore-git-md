Test modernization patch updating `t1005-read-tree-reset.sh` to use the preferred `test_path_is_missing` helper instead of raw `! test -f` checks. This is part of the ongoing effort to improve test diagnostics - when these assertions fail, they'll now show clearer output about what went wrong (e.g. "Path exists:" rather than just silently failing).

The changes are mechanical and straightforward, replacing five instances where the test was verifying a file should not exist. The patch comes with a clear one-line commit message explaining the improvement. This appears to be a clean, uncontroversial change that aligns with established test suite conventions.