This is a technical discussion about type safety in the xdiff refactoring series, specifically around the `ivec` implementation's pointer casting behavior. The thread continues from earlier concerns about undefined behavior when casting between different `IVec_*` struct types.

Phillip Wood reiterates his concern that casting between `IVec_c_void*` and `IVec_u8*` (via `void*`) violates C standards and constitutes undefined behavior, despite likely working in practice on Git's target platforms. He provides a concrete example showing how the current implementation could trigger UB when `ivec_init()` operates across translation units.

René Scharfe confirms the standards violation and shares a GCC documentation link illustrating how compilers might optimize aggressively when they detect such UB. He also notes some syntax errors in Phillip's proposed alternative implementation but agrees the approach is viable - passing individual members rather than whole structs would avoid the type safety issues while maintaining functionality.

The discussion reveals two key points: (1) the current implementation's type-unsafe casts are definitively UB per C standards, and (2) Phillip's alternative design (passing struct members separately) would resolve this while potentially simplifying the implementation by removing the need for an `element_size` member. The main open question is how this approach would impact the Rust side of the implementation, which Phillip hopes would remain manageable through type-safe wrappers.

This exchange represents a significant technical consideration for the series, as it challenges a core assumption in the original design and proposes an alternative that would require non-trivial changes to both the C and Rust implementations. The thread remains unresolved, awaiting input on the Rust-side implications and potential next steps.