This is part 2 of a 3-patch series in the `the_repository` removal effort, specifically migrating the `core.sparseCheckout` configuration from global scope to repository scope. The patch moves the `core_apply_sparse_checkout` global variable into the new `struct repo_config_values` embedded in `struct repository`, affecting 12 files across various commands that interact with sparse checkouts.

The change follows Phillip Wood's suggestion to use a dedicated config struct rather than `repo-settings`, maintaining existing behavior while eliminating global state. The patch updates all call sites that previously referenced the global variable to instead access it through the repository's config struct. This includes commands like `sparse-checkout`, `clone`, `grep`, and `mv` that need to check or modify sparse checkout settings.

The implementation demonstrates the pattern for future config migrations in this series - moving repository-dependent configs out of global scope while preserving initialization sequences and behavior. The patch shows careful attention to edge cases like submodule handling in `grep` and cone mode interactions in `sparse-checkout` operations.

This continues the series' steady progress on implementation details while leaving broader architectural questions about config scoping for future work. The changes are mechanical but thorough, touching all relevant code paths to ensure consistent behavior.