This is part of Patrick Steinhardt's ongoing ODB abstraction series, specifically addressing mtime handling in object info requests. The patch implements Taylor Blau's suggested compromise for safer handling of object info structs during iteration.

The key change makes `object_info` parameters `const` when used as input requests, ensuring callers can't accidentally modify them during iteration. The implementation now creates a local copy of the request struct for each object, populating response fields cleanly without risk of leftover data from previous iterations. This resolves Taylor's concern about potential uninitialized fields while maintaining the series' overall direction.

The patch touches several files (`object-file.c`, `odb.c`, `packfile.c`) to propagate the new const-correctness through the iteration APIs. While this introduces some churn, it provides a cleaner intermediate state while Patrick works toward his planned larger refactoring of the object info request/response separation.

The change represents productive collaboration - Patrick acknowledges Taylor's safety concerns and implements a solution that satisfies both the immediate need and the long-term architectural goals. With this resolved, the series moves closer to completion, leaving only the higher-level mtime abstraction discussion as the remaining open question.