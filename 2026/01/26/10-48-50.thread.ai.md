Refactoring Git's xdiff (diff engine) subsystem to reduce memory usage, building on prior cleanup work in 'en/xdiff-cleanup-2'. Phillip Wood proposes two focused changes: (1) removing the `line_hash` field from `xrecord_t` (used only during initial line classification), and (2) eliminating unused data from `xdlclass_t`. These are internal optimizations that don't affect diff output. 

The series conflicts with 'en/xdiff-cleanup-3' (another active xdiff cleanup effort changing line classification timing). Phillip expresses skepticism about that approach but suggests if kept, hashes could be passed separately rather than stored in `xrecord_t`. The changes are small and targeted, touching only xdiff preparation code. As a contributor with diff/merge expertise, Phillip's proposals likely represent well-considered cleanups. Coordination needed with the conflicting 'en/xdiff-cleanup-3' series before merging.