Refactoring Git's xdiff (diff engine) subsystem to reduce memory usage, continuing the cleanup work started in 'en/xdiff-cleanup-2'. Phillip Wood's series introduces two optimizations: (1) removing the `line_hash` field from `xrecord_t` (used only during initial line processing), reducing structure size by 25-40%, and (2) restructuring `xdlclass_t` to store only essential line data (pointer and size), eliminating unnecessary copying from `xrecord_t`. This second change effectively reverts part of commit 6d507bd41a while maintaining functionality.

Maintainer Junio C Hamano has decided to move forward with Phillip Wood's conservative xdiff refactoring series, temporarily removing Elijah Newren's competing 'en/xdiff-cleanup-3' series from the integration branch. The decision prioritizes Phillip's incremental optimizations that maintain existing classification timing (where lines are processed during diff preparation) over Elijah's more disruptive restructuring. The changes are localized to xdiff/xprepare.c and xtypes.h, focusing on memory efficiency in frequently allocated structures during diff operations.

Junio's approval as maintainer carries significant weight, though he leaves open the possibility of revisiting classification timing changes later if needed. The thread demonstrates Git's careful approach to performance-critical subsystems, where even mechanical improvements receive scrutiny due to their impact on core algorithms. Phillip's expertise in the diff/merge subsystem lends credibility to these targeted optimizations, which build on prior work splitting hash fields (commit 6a26019c81). The current status is that Phillip's series is being integrated while Elijah's competing approach is set aside, with potential for future discussion about reconciling the approaches.