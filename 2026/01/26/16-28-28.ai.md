This is a maintenance patch series in the ODB abstraction effort, now being merged by Junio C Hamano after review. The three patches from Patrick Steinhardt clean up inconsistencies in how `OBJECT_INFO_*` flags are defined and used across the codebase. 

The series addresses some subtle but non-critical issues that Patrick discovered while working on the object database layer. The first two patches fix incorrect flag usage in `builtin/backfill.c` and `builtin/fsck.c`, while the third restructures the flag definitions themselves into a proper enum with contiguous values and documentation. 

Junio's brief response indicates he's satisfied with the changes and is merging them. The patches are small, focused improvements that make the ODB layer's flag handling more maintainable without changing any user-visible behavior. This represents incremental but meaningful progress in the larger ODB abstraction work that Patrick has been leading.