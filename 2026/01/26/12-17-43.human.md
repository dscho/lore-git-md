This is the third and final patch in Patrick Steinhardt's series cleaning up `OBJECT_INFO_*` flag definitions as part of the ODB abstraction work. The patch addresses historical gaps in the flag values by converting them to an enum with proper bit shifts.

The change removes confusion from the code where two previously removed flags (`OBJECT_INFO_SKIP_CACHED` and `OBJECT_INFO_ALLOW_UNKNOWN_TYPE`) left gaps in the bit values. Patrick reorganizes the flags into a proper enum, using sequential bit positions (1<<0 through 1<<3) and maintaining all existing behavior. The patch also adds clearer documentation for each flag.

This is a straightforward internal cleanup with no user-visible impact. The changes are confined to `odb.h` and maintain all existing flag combinations (like `OBJECT_INFO_FOR_PREFETCH` being a combination of other flags). As with the previous patches in the series, this demonstrates Patrick's careful attention to detail in the ODB layer. The patch appears well-considered and uncontroversial, completing the series' goal of making the flag usage more maintainable and explicit.