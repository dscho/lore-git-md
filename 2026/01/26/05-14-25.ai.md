Regression fix discussion for `git subtree` functionality, continuing from the thread about breakage in Git 2.53.0-rc1. Colin Stagner responds to Junio C Hamano's plan to revert the problematic commit (28a7e27cff) in 2.53.0-rc2 unless a fix emerges, confirming they won't have a solution ready in time. 

The discussion reveals deeper issues with the subtree split logic - the "exclude other subtrees" optimization introduced in 98ba49ccc2 has been subtly changing split output across multiple iterations, despite documentation promising identical output for identical inputs. Colin suggests this behavior might be unavoidable and proposes making the optimization opt-in via a new flag like `--fast-exclude` to preserve backward compatibility.

Technical details show the regression manifests when splitting the Arch Linux AUR monorepo's google-chrome subtree, producing different commit IDs before/after the problematic patch. Colin verifies the pre-patch behavior (commit e6f461379) matches expectations and confirms other subtrees remain unchanged when the patch is reverted.

This email advances the thread by moving from regression confirmation to solution exploration, highlighting the tension between optimization and stability guarantees in the subtree command. The immediate path forward remains Junio's planned revert, but Colin's analysis suggests the underlying issue may require a more nuanced long-term solution that maintains the documented behavior guarantees while allowing optimizations for those who need them.