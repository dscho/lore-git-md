Refactoring in Git's xdiff subsystem to optimize memory usage. This is the second patch in Phillip Wood's series building on prior xdiff work, focusing on cleaning up data structures used during diff calculation.

The change removes unnecessary data copying in `xdlclass_t` by storing only the line pointer and size needed for line interning, rather than embedding the full `xrecord_t` structure. This follows from a previous commit that split the `ha` field into separate hashes, leaving some fields unused in the classification phase. The patch effectively reverts part of commit 6d507bd41a while maintaining the same functionality.

The diff shows mechanical changes to `xdiff/xprepare.c` updating the structure definition and initialization code. As with the first patch in the series, this is a focused optimization that reduces memory usage during diff operations by eliminating wasted space in frequently allocated structures. The changes are straightforward and localized, touching only the line classification logic.

This continues Phillip Wood's careful work on xdiff memory efficiency, building on his expertise in Git's diff and merge machinery. The patch series remains in discussion due to its overlap with Elijah Newren's competing 'en/xdiff-cleanup-3' series, requiring coordination before either can be merged.