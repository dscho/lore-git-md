Patrick Steinhardt's three-patch series cleaning up `OBJECT_INFO_*` flag definitions has been merged by Junio C Hamano, completing this phase of the ODB abstraction effort. The series systematically addressed flag usage inconsistencies across three files (`builtin/backfill.c`, `builtin/fsck.c`, `odb.h`), converting the flags to a proper `enum object_info_flags` with contiguous values and documentation. While technically correct and merged, Junio suggests an alternative approach could have better demonstrated the enum's value by first converting `odb_read_object_info_extended()` to take the enum type (enabling compiler warnings) before fixing the bugs. He also notes `odb_has_object()` similarly uses an unsigned parameter despite having enum flags. Junio explicitly thanks Patrick for the work ("well spotted and nicely done") while offering these observations for future reference. The thread is now concluded with the series merged, though Junio's feedback may inform Patrick's ongoing ODB abstraction work regarding type safety in flag handling.