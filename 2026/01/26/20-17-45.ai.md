This is a follow-up discussion in the ODB abstraction thread, where Derrick Stolee confirms Patrick Steinhardt's fix for incorrect flag usage in `builtin/backfill.c`. The original patch identified that `fill_missing_blobs()` was passing `OBJECT_INFO_FOR_PREFETCH` flags to `odb_has_object()`, which expects `HAS_OBJECT_*` flags. While the current behavior happened to work by coincidence (due to how the flags mapped to bits), Stolee agrees this was dangerous and supports removing the flags entirely.

The exchange is brief but reinforces the importance of proper flag usage in the ongoing ODB abstraction work. Stolee's acknowledgment suggests consensus on this particular fix, though the broader thread continues to explore deeper questions about enum type safety in the codebase. This reply doesn't introduce new technical discussion but serves as a positive review from another domain expert (Stolee being known for his work on Git's object storage and performance optimizations).