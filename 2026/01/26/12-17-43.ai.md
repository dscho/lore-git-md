This is the third and final patch in Patrick Steinhardt's series cleaning up `OBJECT_INFO_*` flag definitions as part of the ODB abstraction work. The patch focuses on internal codebase hygiene by:

1. Converting the scattered `#define` constants into a proper `enum object_info_flags` with bit-shifted values, following modern C best practices
2. Eliminating historical gaps in the flag values (previously at 4 and 8) that remained after flags were removed in 2020 and 2025
3. Adding clearer documentation comments for each flag's purpose

The changes are purely internal - no behavior changes or user-facing effects. The patch touches only `odb.h`, reorganizing the flag definitions while maintaining all existing functionality. This continues the series' theme of making the ODB layer's flag handling more maintainable and self-documenting. With this patch, the flag values are now contiguous and explicitly documented, removing potential confusion from the historical gaps.

The technical approach is sound, building on Patrick's deep expertise in the ODB layer. The patch follows the same pattern as the previous two in the series - small, focused improvements that make the code more robust without changing what it does. This completes the flag cleanup work started in the earlier patches.