Refactoring patch targeting Git's xdiff (diff engine) subsystem, part of a series to reduce memory usage during diff operations. This second patch follows an earlier change that removed the `line_hash` field from `xrecord_t`, now focusing on optimizing `xdlclass_t` storage.

Phillip Wood removes unused data from `xdlclass_t`, specifically the embedded `xrecord_t` structure that was carrying unnecessary fields. Instead, the patch stores only the pointer and size needed for line comparison, effectively reverting a previous change that had embedded the full record. The change is mechanical and localized to `xdiff/xprepare.c`, touching only the type definition and three lines of classification logic.

This continues the series of targeted optimizations to Git's diff engine, building on prior work to split hash fields and reduce memory overhead. The changes are small but meaningful for large diffs where these structures are created per-line. As with the first patch, this demonstrates Phillip Wood's characteristic attention to diff subsystem details and memory efficiency. The patch appears straightforward and uncontroversial, though it needs coordination with the parallel 'en/xdiff-cleanup-3' series that's also modifying this area.