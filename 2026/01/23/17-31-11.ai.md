This is a post-merge discussion about localization considerations in the `git last-modified` command's error messages, specifically around how to handle mixed localized and non-localized strings in a single error output. Junio C Hamano notes that while the error message itself should be localized (via `_()`), the object type name from `type_name()` will always be in English since it comes from Git's internal object handling. He questions whether it would be better to explicitly keep both the type name and "commit-ish" in English to avoid a mixed-language message, suggesting an alternative format that would pass "commit-ish" as a literal string rather than translating it.

The discussion touches on an edge case in Git's localization approach where technical terms from the codebase interface with user-facing messages. This is not blocking any functionality (the patch is already merged) but represents ongoing attention to detail from the maintainer about message consistency. The thread shows how even after a series is merged, Git's maintainers continue to scrutinize implementation choices that could affect user experience or future maintenance.