This is a feature patch from Derrick Stolee adding a `--maximal-only` option to `git rev-list`, now in its second iteration. The option helps select "maximal" commits (those not reachable from others in the input set) which is useful for bundle URIs and bitmap optimizations.

The thread has reached consensus on the feature's behavior and naming, with Junio C Hamano clarifying that both this new option and the existing `--independent` flag fundamentally filter input sets for unreachable commits, differing only in `--maximal-only`'s support for negative revisions. Johannes Sixt provided key behavioral clarification that helped resolve earlier confusion about union/intersection semantics.

The patch is now technically complete with comprehensive tests covering edge cases, first-parent interactions, and positive/negative reference ranges. Documentation has been updated to accurately describe the behavior. The implementation uses bit 28 in revision.c/commit.c's flag space and explicitly documents incompatibility with `--boundary`.

With all feedback addressed and Junio's confirmation of understanding, this appears ready for merging. The only open questions are about future optimizations for bundle URIs and long-term bit allocation strategy, but these don't block the current work. The patch represents a well-vetted addition to Git's revision walking capabilities.