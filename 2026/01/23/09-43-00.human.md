This is a technical follow-up in Patrick Steinhardt's ODB abstraction series, specifically addressing Taylor Blau's review feedback on patch 10/14 which converts `for_each_loose_object()` calls to the new ODB iteration API.

Patrick agrees to add a TODO comment as suggested by Taylor, clarifying why this particular case in `builtin/cat-file.c` can't use the higher-level `odb_for_each_object()` yet. He explains that future work will introduce a `struct odb_for_each_object_options` to handle object filtering (like "--filter=blobs:none") at the backend level, which will allow moving the current logic into the ODB layer. The options struct would also support ID prefix matching and other future needs.

This exchange shows the series moving from core architectural decisions to polishing details, with Patrick providing insight into planned follow-up work that wasn't ready to include in the current series. The interaction is collaborative, with Taylor's review prompting useful clarification of the roadmap while not blocking the current changes.