This is a post-merge follow-up discussion about a bugfix in the `git last-modified` command, which was recently merged after multiple iterations. The patch in question (v4 4/4) hardened the command against invalid inputs by using `repo_peel_to_type()` to verify that revision arguments are commit-ish objects, preventing a BUG() when tree OIDs were passed.

Junio C Hamano notes that while the implementation works, the commit message could be clearer about whether we're validating "the parsed revision" or "the command line argument". He suggests rewording to emphasize that we're ensuring the given revision peels to a commit. He also observes that the implementation discards the peeled result and re-peels later in `diff_tree_oid()`, which is inefficient but currently unavoidable.

The discussion shows the maintainer's attention to both technical correctness and clarity in commit messages, even after a patch series has been merged. This exemplifies Git's culture of continuous refinement - where merged work still receives scrutiny for potential improvements in wording or implementation details that may have been overlooked during initial review.

Key technical details include the use of `repo_peel_to_type()` for validation, the error message format for type mismatches, and the observation about redundant peeling operations. The thread context indicates this was part of a comprehensive hardening effort that addressed input validation, memory leaks, and error messaging throughout the command's operation.