This is a technical discussion about the ODB abstraction series, specifically addressing patch 12/14 which converts `builtin/pack-objects` to use the new `packfile_store_for_each_object()` API. Taylor Blau raises thoughtful questions about the design of mtime handling in the object storage layer.

Taylor suggests an alternative approach to mtime tracking that would keep GC-specific logic out of the core ODB layer. His proposal introduces a generic `statp` field in `struct object_info` that callers could use to implement their own mtime calculations, rather than baking the GC-specific mtime logic into the ODB layer. He provides a sample `object_info_gc_mtime()` helper function showing how this could work.

The discussion also touches on locality handling differences between the new source-based iteration and the old pack-based iteration, with Taylor questioning whether the checks for local-only objects are equivalent between the two approaches. He notes that the series drops the `ODB_FOR_EACH_OBJECT_LOCAL_ONLY` flag when dispatching to the iterator, but this appears safe as the check is now handled at the source level.

This email represents one of the final technical discussions in the ODB abstraction series, focusing on design decisions that affect both the current patch and the broader architecture. Taylor's comments suggest he's generally comfortable with the direction but wants to ensure the mtime handling is properly abstracted and future-proof. The conversation continues the pattern of Taylor providing detailed, thoughtful review feedback on the packfile-related aspects of this large refactoring effort.