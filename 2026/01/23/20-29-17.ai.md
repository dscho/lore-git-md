This is a technical follow-up in the ongoing discussion about hash algorithm detection in `git show-index`. Brian m. carlson responds to Patrick Steinhardt's earlier suggestion about introducing index v5 headers to properly encode hash algorithms, rather than relying on size-based guessing.

Brian notes that their `sha256-interop` branch already implements index v3 format which includes hash algorithm encoding, suitable for both single-hash and multi-hash implementations. They suggest this could be enabled by default in the future as a more robust solution than the current size-based detection approach being debated in the thread.

The email provides a concrete implementation path forward (index v3 from Brian's branch) that would address the core technical concern about fragile size-based detection. This represents a potential long-term solution to the problem, though it doesn't directly resolve the immediate question about how to handle the current `show-index` behavior while waiting for such format changes to land.

The response maintains the thread's technical focus while adding new information about existing work that could influence the direction of the discussion. Brian's status as the primary architect of Git's hash algorithm agnosticism gives weight to this implementation suggestion.