Jean-Noël Avila's documentation standardization effort continues with parallel work on multiple Git man pages including `git-submodule`, `git-show`, `git-clone`, `for-each-ref-options`, and `pretty-formats.adoc`, systematically converting them to consistent AsciiDoc synopsis-style formatting. Kristoffer Haugsbakk provides thorough review feedback across these pages, verifying technical details of the conversion.

Key refinements include standardizing option presentation (explicit `--reject-shallow`/`--no-reject-shallow` instead of `--[no-]` shorthand), converting manual `*NOTE*` markup to proper AsciiDoc `NOTE:` macros, fixing placeholder formatting (e.g., `_<when>_`), and using imperative mood for option descriptions. The work introduces synopsis-style block definitions in `asciidoc.conf.in` and refines formatting across all targeted man pages.

For `git-show.adoc`, the discussion now focuses on rendering problems with decoration options in the `%(decorate:)` format specifier, particularly the inconsistent display of the `->` pointer symbol and plus-sign (`++`) verbatim formatting. Haugsbakk identified that while most decoration options render acceptably, the pointer option's hyphen and greater-than symbol display differently (with the hyphen in verbatim typeface but the `>` not). He also notes confusing rendering of plus signs in example formatting blocks. Avila acknowledges these are asciidoctor parsing issues and mentions working on a custom parser extension that could potentially be integrated into Git's build system to handle these cases more robustly, while suggesting temporary fallback to unformatted text when asciidoctor cannot process the markup correctly.

For `git-clone.adoc`, previous discussion focused on aligning long option formatting with gitcli(7) recommendations, specifically addressing the "stuck form" (`--option=<value>`) versus "unstuck form" (`--option value`). Haugsbakk noted the inconsistency, and Avila confirmed they will update the formatting in a reroll, including changing `--config <key>=<value>` to the stuck form `--config=<key>=<value>` after verifying the option parser handles it correctly. Similar refinements were previously made to `git-show.adoc` and `pretty-formats.adoc`, where Haugsbawk confirmed successful conversion of command references to backtick-wrapped forms (`` `-o <name>` ``) and proper placeholder formatting.

The collaboration demonstrates meticulous attention to documentation quality, with each iteration refining formatting details like converting option lists from bullet-points to AsciiDoc definition lists with double semicolons (`;;`) and correcting placeholder markup (`s/<number>/_<number>_/`). All changes maintain cross-toolchain compatibility between asciidoc.py and asciidoctor while improving clarity without altering command behavior. The effort remains strictly focused on documentation presentation, with future work likely addressing remaining rendering nuances like `++--->++` decoration option consistency, completing the long option formatting alignment across all man pages, and potentially integrating custom parser extensions for special character handling.