Jean-Noël Avila's documentation standardization effort continues with parallel work on multiple Git man pages including `git-submodule`, `git-show`, `git-clone`, `for-each-ref-options`, and `pretty-formats.adoc`, systematically converting them to consistent AsciiDoc synopsis-style formatting. Kristoffer Haugsbakk provides thorough review feedback across these pages, verifying technical details of the conversion.

Key refinements include standardizing option presentation (explicit `--reject-shallow`/`--no-reject-shallow` instead of `--[no-]` shorthand), converting manual `*NOTE*` markup to proper AsciiDoc `NOTE:` macros, fixing placeholder formatting (e.g., `_<when>_`), and using imperative mood for option descriptions. The work introduces synopsis-style block definitions in `asciidoc.conf.in` and refines formatting across all targeted man pages.

For `git-show.adoc`, the discussion focuses on rendering problems with decoration options in the `%(decorate:)` format specifier, particularly the inconsistent display of the `->` pointer symbol and plus-sign (`++`) verbatim formatting. Haugsbakk identified display issues with hyphen and greater-than symbols in pointer options, plus confusing plus-sign rendering. Avila is working on a potential custom parser extension for robust handling while considering temporary fallback solutions.

For `git-clone.adoc`, Haugsbakk confirmed there is no automated tool to enforce the preferred stuck form convention (`--option=<value>`) documented in gitcli(7), which Junio Hamano has endorsed. The discussion shows meticulous alignment of long option formatting across man pages, with Avila planning updates to ensure `--config=<key>=<value>` and similar options consistently use the stuck form after verifying parser compatibility. This follows similar refinements made to `git-show.adoc` and `pretty-formats.adoc` where Haugsbakk verified successful conversions of command references and placeholder formatting.

The collaboration continues refining documentation presentation without altering command behavior, maintaining cross-toolchain compatibility between asciidoc.py and asciidoctor. Future work includes addressing remaining rendering nuances like decoration option consistency, completing long option formatting alignment, and potentially custom parser extensions for special character handling. The effort remains strictly focused on documentation presentation quality, with each iteration systematically improving formatting details across the targeted man pages.