This is a technical discussion in Patrick Steinhardt's ODB abstraction series, specifically addressing patch 06/14 which introduces a new function `packfile_store_for_each_object()`. The conversation focuses on the handling of `struct object_info` during iteration.

Taylor Blau raises concerns about the current implementation's reuse of `object_info` structs across iterations, noting potential fragility if `packed_object_info()` were to conditionally assign fields in the future. He suggests two alternative approaches: splitting the struct into separate request/response components, or pushing the object info handling entirely to callers.

Patrick acknowledges the awkwardness of the current mixed in/out behavior but argues for deferring structural changes to avoid scope creep in this series. He explains the performance rationale for combining iteration with object info requests, particularly the ability to avoid redundant lookups in packfiles and potential optimizations for future backends like remote databases.

The exchange shows Taylor's careful attention to API design and long-term maintainability, while Patrick prioritizes keeping the current series focused on its core abstraction goals. This appears to be one of the last remaining design questions before the series can be integrated.