This is a follow-up discussion in the `--maximal-only` feature thread, where Junio C Hamano and Derrick Stolee are finalizing the conceptual alignment of the new option with the existing `--independent` functionality. The thread has already reached consensus on the implementation details, and this exchange serves to confirm the relationship between the two features.

Junio suggests that `--maximal-only` could potentially subsume the `--independent` functionality from `git merge-base`, since both fundamentally filter input sets to find commits not reachable from others. The key difference is that `--maximal-only` supports negative revisions and other walk options that `--independent` does not. Derrick notes that his implementation already covers test cases beyond what `merge-base --independent` handles, and Junio agrees that the revision walking infrastructure is a more natural home for this functionality than the merge-base command.

The discussion confirms that all technical points have been resolved, with Junio's authoritative clarification settling the behavioral relationship between the features. The patch appears ready for merging with no outstanding questions, having addressed all feedback from previous rounds including naming, documentation, and test coverage. The thread has effectively concluded with consensus that this is the right approach.