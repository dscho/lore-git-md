This is a feature patch from Derrick Stolee adding a `--maximal-only` option to `git rev-list`, now in its second version with all feedback addressed. The patch enables selecting "frontier" commits (those maximal in reachability order) which is useful for bundle URIs and bitmap optimizations.

The key technical details are now finalized after Junio C Hamano's clarification that both this new option and the existing `--independent` flag fundamentally filter input sets for commits not reachable from others, differing only in `--maximal-only`'s support for negative revisions. The implementation uses bit 28 (`CHILD_VISITED`) in revision.c/commit.c infrastructure and includes comprehensive tests covering positive/negative reference ranges, `--first-parent` interaction, and edge cases in reachability graphs.

The patch is now ready for merging with no outstanding technical questions, having incorporated all feedback including naming consensus (finalized as `--maximal-only`), documentation updates with accurate behavioral descriptions, and exhaustive test coverage. Johannes Sixt contributed key behavioral clarification that helped solidify the conceptual model.

While the immediate need is addressed, the thread notes potential future considerations around bundle URI optimizations, long-term bit allocation strategy (with bit 28/32 now used), and possible follow-ups for similar reachability queries. The documentation has been refined to emphasize negative revision handling as the sole differentiator from `--independent`.