This is a feature patch adding the `--maximal-only` option to `git rev-list`, now in its second iteration with all feedback addressed. The option helps select "frontier" commits (those not reachable from others in the input set) for use cases like bundle URIs and bitmap optimization.

The thread has reached consensus on naming (`--maximal-only` instead of the original `--maximal` proposal) and clarified the behavior's relationship to `git merge-base --independent`. Johannes Sixt points out the key difference is that `--maximal-only` handles negative revisions while `--independent` does not. Junio confirms this understanding, noting that `--independent` simply finds commits in the input set that aren't reachable from others, without computing merge bases.

The patch is now well-documented with explicit incompatibility notes regarding `--boundary` and comprehensive test coverage. Junio's response indicates satisfaction with the technical explanation, suggesting this is likely ready for merging. The implementation uses bit 28 in revision.c's flags, raising a minor open question about long-term bit allocation strategy, but this doesn't block the current change.