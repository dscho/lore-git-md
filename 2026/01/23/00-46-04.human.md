This is a review response in the ongoing ODB abstraction series, where Taylor Blau provides feedback on patch 10/14 that converts callers from `for_each_loose_object()` to the new ODB iteration API. The patch updates `builtin/cat-file.c` and `commit-graph.c` to use the new backend-specific iteration functions rather than the legacy ones that will be removed later in the series.

Taylor notes that the patch demonstrates a benefit of the new API - it provides callers with the object_info response they requested, avoiding redundant lookups. However, he reiterates his earlier concerns about how object_info is passed around in the API. He also asks about the refactoring needed to eventually use the higher-level `odb_for_each_object()` function, wondering if adding a TODO would be helpful and what the remaining obstacles are.

The commit-graph.c changes receive unqualified approval, with Taylor calling them "great". The discussion here is focused on implementation details rather than fundamental design, suggesting the series is nearing completion with only minor points left to address.