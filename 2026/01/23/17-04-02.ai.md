This is a follow-up bugfix in the already-merged `git last-modified` series, addressing a memory leak in the error path when multiple revisions are provided. The patch fixes a case where `struct pathspec` wasn't being properly cleaned up when the function exits early due to detecting multiple input revisions.

The change is straightforward - it restructures the error handling to ensure `clear_pathspec()` is called in all code paths, including the error case where multiple revisions are detected. The author (Toon Claes) adds a `goto out` pattern to consolidate the cleanup, and Junio C Hamano acknowledges the fix as sensible.

This represents final polish on the series' resource management, building on earlier hardening that included input validation and error message improvements. The patch demonstrates Git's typical attention to detail in ensuring all allocated resources are properly freed, even in error conditions. No new discussion points are raised - this is purely a technical correction to an already-reviewed change.