This thread discusses a v3 patch series (3 parts) removing `the_repository` and `the_hash_algo` globals from `wt-status.c`, part of René Scharfe's ongoing effort to eliminate global state. The series systematically replaces these globals with repository-specific instances where available, focusing on status reporting functionality. 

Patch 2/3 established that all `struct wt_status *s` functions can safely use `s->repo` instead of `the_repository`, making 24 mechanical replacements affecting ref handling (`get_main_ref_store()`), path operations (`repo_git_path*`), and object ID handling. Patch 3/3 completes the series by replacing `the_hash_algo` with repository-specific hash algorithms (`s->repo->hash_algo` or `r->hash_algo`) in three locations handling cherry-pick/revert state tracking (using `null_oid`) and uncommitted change detection (using the empty tree).

The changes are behavior-preserving internal refactorings following established patterns for `the_repository` removal - well-scoped, mechanically replacing globals with local repository instances without adding new test coverage. The series appears complete at v3 with no outstanding objections or requested changes noted in the thread, successfully eliminating all `the_repository` and `the_hash_algo` usage from `wt-status.c`.