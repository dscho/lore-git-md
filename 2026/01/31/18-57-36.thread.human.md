This thread discusses a refactoring effort to eliminate dependencies on the global `the_repository` variable in `wt-status.c`, part of a broader project-wide cleanup. The author has sent a three-part series, with the second patch now under review.

The changes are mechanical but thorough, focusing on making repository dependencies explicit by passing repository state through function parameters rather than using globals. The second patch addressed stash counting, rebase state reading, and commit ID abbreviation, updating these to use repository instances from local structs. The modifications follow established patterns from René Scharfe's ongoing `the_repository` removal work, maintaining identical behavior while improving the code's architecture.

So far, the series appears to be progressing smoothly with no objections raised. The changes are relatively small (32 lines modified in this patch) and focused, following the project's standard incremental approach to such refactorings. The work aligns with the project's direction of making repository dependencies explicit, a cleanup that has been happening across the codebase. The third patch in the series is expected to complete the refactoring of `wt-status.c`.