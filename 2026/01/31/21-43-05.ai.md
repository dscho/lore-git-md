This is the first patch in a series adding new traversal options to `git submodule foreach`. It introduces comprehensive test coverage for the upcoming `--reversive` flag and its constituent options (`--reverse-traversal` and `--append-superproject`).

The test file `t/t7425-submodule-reversion.sh` creates a complex submodule hierarchy with varying depths and sibling relationships to thoroughly validate the new traversal behaviors. The tests are currently marked as `test_expect_failure` since the features are not yet implemented, but will be flipped to `test_expect_success` in subsequent patches.

Key test scenarios include:
- Verifying the new flags parse and execute
- Confirming post-order traversal with `--reverse-traversal` (children before parents)
- Testing superproject inclusion with `--append-superproject`
- Validating the combined `--reversive` shorthand behavior
- Checking edge cases like empty repositories and nested submodule contexts

The test setup creates a multi-branch submodule tree with 10 repositories to exercise different traversal patterns. This appears to be a well-structured test suite that will provide solid coverage for the new functionality. The commit message clearly explains the test organization and what each test group verifies.

For AI summarizer context: This test file will be referenced in subsequent patches that add the actual implementation. It defines the expected behavior for `--reverse-traversal` (post-order processing), `--append-superproject` (including the superproject in operations), and their combination via `--reversive`. The test hierarchy includes submodules at varying depths (1-3 levels) with multiple sibling relationships to validate traversal order correctness.