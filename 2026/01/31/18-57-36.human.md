This is a refactoring patch series that's part of the ongoing `the_repository` removal effort, targeting the working tree status reporting code in `wt-status.c`.

The series from Shreyansh Paliwal systematically eliminates global state usage in the working tree status machinery by:
1) Replacing `the_repository` with local repository instances already available in structs
2) Updating function signatures to explicitly pass repository context
3) Switching from the global `the_hash_algo` to repository-specific hash algorithms

The changes are mechanical but thorough, touching 76 lines across the file with a balanced mix of additions and deletions. The commit messages clearly explain each step of the refactoring. This appears to be a well-scoped contribution that follows the established pattern for `the_repository` removal work, though as with any refactoring, careful review will be needed to ensure no subtle behavior changes were introduced.

The series is small (3 patches) and focused exclusively on `wt-status.c`, making it relatively easy to evaluate. Since this is part of a well-established refactoring effort with clear patterns to follow, it's likely to receive prompt review attention from contributors familiar with the `the_repository` removal work.