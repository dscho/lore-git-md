Bugfix for `--shallow-since` edge case in shallow clone functionality, addressing incorrect handling of descendant relationships between border commits. This is the third version of Samo Pogačnik's patch, now resolving the terminology inconsistency ("border" vs "boundary") that Junio C Hamano noted in previous reviews.

The patch modifies `shallow.c` to ensure all shallow boundary commits remain reachable after a clone with `--shallow-since`. The key issue was that the original implementation could mark some boundary commits as unreachable when they had parent-child relationships within the candidate list. The new implementation uses a more sophisticated painting algorithm with NOT_SHALLOW/SHALLOW flags to properly handle these cases.

The changes include:
1. Updated documentation clarifying the boundary commit selection logic
2. A new test case 'clone shallow-since all shallows reachable' that demonstrates the fix
3. More robust parent commit handling that ensures boundary commits are always reachable
4. Cleaned up terminology throughout to consistently use "boundary" rather than "border"

The patch appears technically sound with thorough test coverage and has addressed all prior review feedback. It's now awaiting final review from shallow infrastructure experts before potential integration. The changes follow Git conventions with clear commit messages and focused implementation.