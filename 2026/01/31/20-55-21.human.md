This is a follow-up in the ongoing discussion about adding a shorthand for referencing a repository's primary branch, now considering Junio Hamano's suggestion to use `repo_default_branch_name()` instead of the remote-tracking approach.

Harald Nordgren examines the suggested function and finds it problematic because it hardcodes the fallback to either "main" or "master" (depending on compilation flags) rather than reflecting what's actually used in each repository. This would make the shorthand less useful in repositories that use other branch names like "trunk" or "development".

The email highlights a key tension in the discussion: whether the shorthand should reflect the local repository's actual primary branch (which the current remote-tracking approach attempts) versus a configured or hardcoded default (which may not match reality). Nordgren's analysis suggests the function Junio proposed doesn't solve the original problem of handling diverse branch naming conventions across repositories.

This moves the discussion forward by providing concrete technical analysis of the alternative approach while maintaining focus on the original goal of creating a truly useful shorthand that works across varied real-world scenarios. The thread remains unresolved but is progressing toward either finding a better technical solution or clearer agreement about the shorthand's intended semantics.