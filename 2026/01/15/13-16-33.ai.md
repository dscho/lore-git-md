This is a refactoring patch targeting Git's oidmap implementation, with an emphasis on making memory management more explicit. The patch comes with a single commit that modifies the oidmap subsystem to require explicit cleanup of entries when clearing the map, rather than relying on implicit behavior.

The change affects three files: `oidmap.c` gets the main implementation change, `oidmap.h` gains a new function declaration, and `t/unit-tests/u-oidmap.c` adds new test coverage. The author argues this makes the API clearer and less error-prone by removing implicit behavior around when and how stored entries are freed.

The patch appears well-structured with a clear commit message explaining the motivation and approach. The addition of unit tests to verify the new behavior is a positive sign of thoroughness. This seems like a focused, low-risk improvement to a core data structure, though reviewers may want to confirm the new API design aligns with Git's broader patterns for similar container types.