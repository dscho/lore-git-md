Here's a human-readable summary of the thread so far:

The thread began with Jon Forrest reporting inconsistent behavior in `git add` when handling invalid paths. The command fails completely when given non-existent files, but only warns (while still adding valid files) when some paths are ignored. This inconsistency could trip up scripts that rely on predictable failure modes.

Jeff King confirmed the current behavior, noting that `git add` fails entirely for non-existent files but proceeds with a warning for ignored files. The discussion then turned to whether this is a bug or intentional design.

Junio Hamano has weighed in, arguing the current behavior makes practical sense - if ignored files blocked all additions, common patterns like `git add .` would become unusable in directories with ignored files. He also pointed out the technical challenge of distinguishing between explicitly typed filenames and shell-expanded ones. Junio suggests any change would constitute a new feature rather than a bugfix, and remains skeptical about the value of changing this long-standing behavior given the shell expansion limitations.

The thread remains open-ended, with the core question being whether to document the current behavior as-is or consider standardizing the failure modes. The discussion highlights the technical constraints around shell expansion and the tradeoffs between strict consistency and practical usability.