This is a cover letter for a 14-patch series introducing a new object database iteration API, part of Patrick Steinhardt's ongoing ODB abstraction effort. The series aims to make object iteration backend-agnostic by introducing `odb_for_each_object()` and converting existing callers to use it.

The series builds on two recently merged topics (object info reading improvements and packfile store integration) and follows a clear progression: first cleaning up iteration flags, then introducing the new infrastructure, converting callers, and finally removing the old functions. The changes touch core object handling across multiple commands (`fsck`, `pack-objects`, reachability calculations) and infrastructure (commit-graph, promisor objects).

This represents significant architectural work in the object storage layer, continuing Patrick's multi-year effort to abstract Git's object storage backends. The series appears carefully structured with each commit focused on a logical step in the transition. Given Patrick's track record with similar large-scale refactorings and the thoroughness of the cover letter, this is likely a well-considered change that merits close review from object storage experts.