Performance optimization for sparse-checkout pattern handling, addressing Jeff King's review comments from the initial version. This v2 patch expands the scope to cover all three locations in the sparse-checkout code where pattern lists were being built with inefficient O(n^2) insertion sorting.

The patch converts all instances of `string_list_insert()` to `string_list_append()` followed by a single `string_list_sort()` in `builtin/sparse-checkout.c`. This changes the time complexity from O(n^2) to O(n log n) for building sorted pattern lists. The changes affect both the `sparse_checkout_list()` function and two locations in `write_cone_to_file()`.

Notably, the patch removes redundant `string_list_remove_duplicates()` calls since the hashmap source already guarantees uniqueness. The commit message is brief but clear about the performance improvement being made. This appears to fully address the review feedback from the initial version, making the optimization consistent across all similar code paths in the sparse-checkout implementation.

The changes are straightforward and maintain the same functionality while improving performance. The patch follows Git's coding style and the diff is clean. This looks ready for inclusion pending any final review comments.