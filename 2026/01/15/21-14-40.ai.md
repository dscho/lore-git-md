Documentation fix for `git-config`'s `--get-urlmatch` option. Shreyansh Paliwal reviews Pushkar Singh's patch that corrects an inaccuracy in the man page regarding the replacement syntax for the deprecated `--get-urlmatch` option. The original documentation incorrectly suggested combining `--url` with `--all`, which is not actually supported by the implementation.

The patch itself is straightforward - it removes the unsupported `--all` flag from the documented replacement syntax. Shreyansh confirms the fix looks good and mentions they were working on a similar correction. They also note an opportunity for follow-up work to better document the constraints around the `--url` option in both the man page and command help output, suggesting the synopsis could potentially be split to show URL-specific usage separately.

This is a small but important documentation update that prevents user confusion when transitioning to the newer config syntax. The thread shows good collaborative review with Shreyansh both validating the fix and identifying related documentation gaps that could be addressed in future patches.