This is a refactoring patch targeting Git's oidmap implementation, making memory management more explicit when clearing the map. The patch comes from Seyi Kuforiji as a single-commit series.

The change introduces a new `oidmap_clear_with_free()` function that requires callers to explicitly specify how entries should be freed when clearing the map, replacing the previous layout-dependent approach. The existing `oidmap_clear()` is kept as a wrapper for backward compatibility. The patch includes thorough unit tests verifying both the new explicit cleanup path and the case where no cleanup is requested.

This appears to be a well-motivated change that improves code clarity and removes implicit behavior. The implementation looks clean, with clear documentation of the new API and comprehensive test coverage. The patch touches core data structure code (oidmap.c/h) and adds new test cases, but the changes are focused and maintain backward compatibility.

The thread is just starting, so we'll need to see how reviewers respond to the API design choice, but this looks like a solid improvement to Git's internal data structure handling.