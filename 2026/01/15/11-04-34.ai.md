Performance and refactoring patch in Patrick Steinhardt's ODB abstraction series (5/14). This intermediate step extracts the core packfile iteration logic from `for_each_packed_object()` into a new `packfile_store_for_each_object_internal()` function, preparing for upcoming backend-agnostic object iteration. The change:

- Splits the existing function to separate the per-packfile-store iteration from the repository-wide alternates handling
- Maintains all existing flag behaviors (LOCAL_ONLY, PROMISOR_ONLY, etc.)
- Preserves error reporting through the `pack_errors` out-parameter
- Keeps the MRU (most recently used) pack tracking behavior
- Touches only packfile.c, with no interface changes

This is a mechanical refactoring that doesn't change functionality but enables the next patch to introduce a variant that works with `struct object_info`. The changes follow the series' pattern of separating iteration logic from backend specifics, continuing the ODB abstraction effort that will eventually provide pluggable storage backends. The patch is narrowly focused and appears well-contained, with the diff showing mostly code movement plus some variable renaming for clarity.