Thread summary:

Documentation fix for `git-config`'s `--get-urlmatch` option syntax. Pushkar Singh corrects an inaccuracy in the man page where it incorrectly suggested combining `--url` with `--all` - an unsupported combination. The patch provides the proper invocation syntax that matches the actual behavior. This is a minimal but important documentation update to prevent user confusion when transitioning to newer config syntax. The commit message clearly explains both the documentation error and the correct usage. No technical changes to the implementation - purely a documentation fix.