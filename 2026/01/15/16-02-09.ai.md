This is a follow-up discussion about terminology in the already-merged bugfix series for `git last-modified`. Kristoffer Haugsbakk provides a minor editorial suggestion about the commit message wording, noting that "peels to a commit-ish" is redundant since a commit-ish by definition is something that peels to a commit. He suggests either simplifying to just "commit-ish" or using "peels to commit" instead.

The email also questions whether the memory leak fix mentioned in the commit message should have been in a separate commit, though this appears to be an academic point since the series is already merged. The thread context shows this was the final patch in a v2 series that addressed input validation issues, error handling, and terminology in the `git last-modified` command, with all technical changes already settled.

The discussion serves as a post-merge refinement of documentation style rather than proposing any code changes. It demonstrates the project's attention to precise wording even after technical consensus is reached, with Kristoffer's note adding to the thread's value as a reference for future terminology decisions.