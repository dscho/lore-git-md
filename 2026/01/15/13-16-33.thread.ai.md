Thread identity: Refactoring of Git's oidmap implementation to make memory management more explicit, focusing on requiring explicit cleanup of entries when clearing the map. Single-patch series from Seyi Kuforiji.

Core problem: The current oidmap implementation has implicit behavior around memory management when clearing the map. The patch makes this explicit by modifying the API to require callers to properly clean up entries while maintaining backward compatibility.

Current status: First version sent for review. Changes include:
- New `oidmap_clear_with_free()` function taking a callback for entry cleanup
- Modified `oidmap_clear()` behavior (now a wrapper for backward compatibility)
- Added unit tests verifying both callback and no-callback behaviors

Key technical details:
- Introduces `oidmap_free_fn` typedef for cleanup callbacks
- Replaces layout-dependent approach with explicit entry iteration
- Touches oidmap.c (implementation), oidmap.h (API), and t/unit-tests/u-oidmap.c
- Maintains same header file API while changing internal behavior

Open questions:
- Whether the new design aligns with Git's patterns for similar container types
- Potential impact on existing oidmap users
- Whether reviewers agree with the backward compatibility approach

Participant positions: None yet (initial patch submission)

Related work: Part of ongoing efforts to improve code clarity and maintainability in Git's core data structures. The patch includes thorough test coverage and maintains backward compatibility while making memory management behavior more transparent. Implementation appears clean with clear commit message explaining rationale.