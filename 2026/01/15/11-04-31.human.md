This is a refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series (patch 2/14). It fixes the type signature of flags parameters in object iteration functions to use `unsigned` instead of `enum odb_for_each_object_flags` where appropriate, bringing them in line with Git's conventions for bitfield parameters.

The changes are mechanical and touch four files (`object-file.c/h`, `packfile.c/h`), updating function signatures and their declarations to consistently use `unsigned` for flags parameters. This is a preparatory change for the new object database iteration API being introduced in the series. The patch appears straightforward and well-contained, with no behavioral changes - just type signature updates to match established patterns in the codebase. As part of a larger architectural effort, it will need to be considered in the context of the full series when reviewed.