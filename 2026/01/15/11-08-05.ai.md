Documentation fix correcting the replacement syntax for `--get-urlmatch` in `git-config`. The patch notes that the current documentation incorrectly suggests combining `--url` with `--all`, which doesn't work in practice. Pushkar Singh provides a straightforward correction to show the proper invocation that matches the actual behavior of the original option.

The change is minimal - just a one-line update to the man page - but fixes an important documentation inaccuracy that could mislead users trying to transition to the newer config syntax. The commit message clearly explains both the problem and solution, making this a well-formed documentation patch.