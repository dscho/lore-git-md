This is the first patch in Patrick Steinhardt's 14-part series introducing a new object database iteration API as part of the ongoing ODB abstraction effort. The patch performs a mechanical rename of `FOR_EACH_OBJECT_*` flags to use an `ODB_` prefix, preparing for the new `odb_for_each_object()` function that will be introduced later in the series. 

The changes are straightforward but widespread, touching 11 files across core object storage paths including `packfile.c`, `object-file.c`, and various callers like `fsck`, `pack-objects`, and commit-graph generation. The rename maintains all existing flag semantics while making them consistent with the upcoming API's naming convention.

This patch appears to be a preparatory refactoring with no behavior changes - a clean, focused change that follows Git's convention of small, incremental steps in large refactoring efforts. The series builds on recently merged work and follows Patrick's typical pattern of careful, structured progression in architectural changes.