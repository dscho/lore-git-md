This is part 2/14 of Patrick Steinhardt's series to introduce a new object database iteration API, focusing on a technical cleanup in the ODB abstraction effort. The patch standardizes the type used for flags parameters in object iteration functions, changing them from `enum odb_for_each_object_flags` to `unsigned` to match Git's convention for bitfield parameters. The changes touch core object-file and packfile handling code, modifying function signatures in both headers and implementations. 

The patch is mechanical and focused - it doesn't introduce new functionality but ensures type consistency across the object iteration API. This is preparatory work for the larger series that will make object iteration backend-agnostic. The changes affect low-level object storage paths but maintain backward compatibility, with callers like `fsck`, `pack-objects`, and commit-graph generation being updated to use the new signatures. 

As part of the broader ODB abstraction, this change helps establish consistent interfaces that will work across different storage backends. The patch follows Git's coding conventions for flag parameters and appears to be a straightforward improvement, though reviewers will want to verify it doesn't break any assumptions about flag handling in the object storage layer.