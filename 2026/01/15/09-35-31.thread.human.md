The thread continues to discuss Patrick Steinhardt's proposal to standardize three commit list function names (`copy_commit_list`, `reverse_commit_list`, `free_commit_list`) to follow Git's modern `struct S_<verb>()` pattern as `commit_list_copy()`, `commit_list_reverse()`, and `commit_list_free()`. 

Junio Hamano, as maintainer, has raised concerns about the long-term maintenance burden this would create, since compatibility wrappers would need to remain in place indefinitely across both maint and master branches. Patrick acknowledges this concern but argues the change would improve developer experience by addressing particularly confusing naming inconsistencies, while noting he'll drop the series if the project deems it too disruptive.

The discussion highlights a fundamental tension in the project between the value of API consistency and the practical costs of maintaining backward compatibility. Patrick's incremental refactoring approach contrasts with Junio's maintainer perspective that weighs each change against its ongoing support burden. While Patrick makes a case for addressing these specific outliers in the API, Junio questions whether the project's naming rules should be revised to grandfather well-established legacy functions rather than forcing mechanical renames.

The changes would affect numerous core commands (merge, rebase, commit-graph, etc.) and test code, with all modifications maintaining strict backward compatibility through wrappers. The thread remains at an impasse, with the project facing a choice between proceeding with standardization (and accepting permanent wrappers), adjusting guidelines to exempt legacy functions, or finding a compromise for this specific case.