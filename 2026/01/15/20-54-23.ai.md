This is a technical discussion about the object database (ODB) abstraction effort, specifically addressing patch 4/14 in Patrick Steinhardt's series. Justin Tobler is reviewing the introduction of a new `odb_source_loose_for_each_object()` function that provides a more uniform interface for iterating objects across different storage backends.

The patch replaces the existing `for_each_loose_object()` with a new function that uses `struct object_info` to handle backend-specific and generic object information consistently. Justin asks two clarifying questions: whether the end goal is a unified `for_each_object()` callback interface across all backends, and whether `struct object_info` will need expansion to accommodate future backends. The discussion confirms this is part of the larger ODB abstraction work that will eventually remove the legacy loose/packed object iteration functions entirely.

Key technical points for future reference:
- New function: `odb_source_loose_for_each_object()`
- Uses `struct object_info` union for backend-specific data
- Legacy `for_each_loose_object()` remains temporarily
- Part of unifying iteration interfaces for pluggable backends
- Touches object-file.c and related ODB infrastructure

The exchange shows active review engagement on a technically complex series, with Justin probing the design's forward compatibility as the ODB interface evolves. No objections are raised - just clarification questions about the architectural direction.