This is the first patch in Patrick Steinhardt's 14-part series introducing a new object database iteration API as part of the ongoing ODB abstraction effort. The patch performs a mechanical rename of `FOR_EACH_OBJECT_*` flags to use an `ODB_` prefix, preparing for the new `odb_for_each_object()` function that will be introduced later in the series.

The changes are straightforward and touch multiple files that use these flags, including `packfile.c`, `commit-graph.c`, and `pack-objects.c`. The rename ensures naming consistency between the upcoming function and its associated options. While this is a simple refactoring, it's part of a larger architectural change to make object iteration backend-agnostic.

The patch appears well-structured with a clear commit message explaining the motivation. As the first step in a significant series, it sets up the naming conventions that will be used throughout the rest of the work. Reviewers will likely want to see how these flags are used in the subsequent patches that implement the actual iteration functionality.