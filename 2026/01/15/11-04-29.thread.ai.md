Patrick Steinhardt's ODB abstraction series progresses to v3 (14 parts), with part 13/14 converting `reachable.c` to use the new object storage iteration APIs. The series systematically converts Git's object storage layer to support pluggable backends, building on previous parts' infrastructure.

Current technical state:
1. New in part 13:
   - Replaces `for_each_loose_object()` and `for_each_packed_object()` with unified `odb_for_each_object()`
   - Uses part 11's `object_info` struct for consistent mtime/type access
   - Eliminates redundant mtime lookup paths (55 net line reduction)
   - Maintains in-core pack handling while simplifying iteration logic

2. Previous work (parts 1-12):
   - Part 12: Converted `pack-objects` cruft pack handling
   - Part 11: Added mtime tracking via `object_info`
   - Parts 7-10: Established core iteration APIs and converted `cat-file`, commit-graph, promisor objects
   - Parts 1-6: Built foundation with flags standardization, path handling, and iteration framework

Implementation status:
- Sixth major subsystem (`reachable.c`) converted to new APIs
- Demonstrates unified iteration simplifying specialized cases
- Continues pattern of replacing global state with ODB-specific access
- Maintains identical functionality while reducing duplication
- Receiving review input from Justin Tobler and Karthik Nayak
- One part remains in series (14/14) before Junio Hamano review

The `reachable.c` conversion in part 13 shows how the ODB abstraction eliminates redundant iteration paths while preserving specialized behaviors. The series continues making methodical progress through Git's object storage layer, with each patch building on the last to prepare for pluggable backends. The remaining part 14 will complete the series' core technical work.