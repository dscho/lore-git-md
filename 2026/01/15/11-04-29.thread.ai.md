Patrick Steinhardt's ODB abstraction series reaches completion with v3 part 14/14, removing the legacy object iteration functions after converting all callers to the new unified APIs. The series systematically restructured Git's object storage layer to support pluggable backends.

Final technical state:
1. Part 14 removes now-unused functions:
   - Eliminates `for_each_loose_object()` and `for_each_packed_object()`
   - Inlines `packfile_store_for_each_object_internal()` (single caller)
   - Net reduction of 97 lines across object-file and packfile code
   - Completes transition to part 11's `object_info` struct for mtime/type access

2. Series progression (parts 1-13):
   - Converted six major components: `cat-file`, commit-graph, promisor objects, `pack-objects` cruft packs, `reachable.c`
   - Established core iteration APIs (parts 7-10)
   - Standardized flags and path handling (parts 1-6)
   - Added mtime tracking via `object_info` (part 11)
   - Unified iteration while preserving specialized behaviors

Implementation status:
- All object iteration now uses new ODB abstraction
- Eliminated redundant code paths while maintaining functionality
- Received review input from Justin Tobler and Karthik Nayak throughout
- Ready for Junio Hamano's review and potential merge
- Sets foundation for future pluggable backend work

The series demonstrates how methodical conversion of Git's object storage layer can both simplify the codebase (net line reduction) and enable future extensibility. With the removal of legacy iteration functions in part 14, the technical work is complete, having converted all callers to use the new unified APIs while maintaining identical functionality. This positions Git project for future work on alternative object storage backends.