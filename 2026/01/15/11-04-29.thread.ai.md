Patrick Steinhardt's ODB abstraction series (v4 0/14) continues progressing with patch 11/14 introducing mtime tracking for objects. The change adds a `mtimep` field to `struct object_info` to expose modification times, crucial for garbage collection of unreachable objects. The implementation handles both loose objects (via stat()) and packed objects (including cruft pack .mtimes files), while documenting limitations around objects existing in multiple locations.

This follows patch 10/14's conversion of `builtin/cat-file.c` and `commit-graph.c` to use backend-specific interfaces through transitional functions like `odb_source_loose_for_each_object()`. The series maintains its incremental approach: foundational infrastructure (1-3/14), loose iteration (4/14), packed refactoring (5-6/14), unified facade (7/14), fsck adoption (8/14), promisor handling (9/14), cat-file/commit-graph (10/14), and now mtime tracking (11/14).

Key technical details include:
- New `mtimep` field in `object_info` with clear usage warnings
- Special handling for cruft pack mtimes
- Backward-compatible interface extensions
- Explicit guidance against using `odb_read_object_info()` for mtime queries
- Continued reduction of direct backend knowledge in callers

Review consensus holds with Taylor Blau, Justin Tobler and Karthik Nayak validating critical paths. Open items include finalizing the GC logic placement discussion (patch 12/14) and potential follow-ups for callers needing refactoring to fully use `odb_for_each_object()`. Junio Hamano previously noted straightforward merge conflicts in `packfile.c`. The series demonstrates real-world applicability through multiple subsystem conversions while maintaining the incremental transition approach.