Performance optimization for sparse-checkout pattern handling, completing the conversion from O(n^2) to O(n log n) sorting across all relevant code paths. This v3 patch from Amisha Chhajed addresses all three locations in builtin/sparse-checkout.c where pattern lists were being built using the inefficient string_list_insert() approach, switching them to string_list_append() followed by string_list_sort(). 

The changes affect the pattern collection in sparse_checkout_list() and two locations in write_cone_to_file(). The redundant string_list_remove_duplicates() calls are removed since the hashmap source already guarantees uniqueness. The patch maintains identical functionality while improving the time complexity throughout the sparse-checkout implementation, with no behavioral changes beyond the performance improvement.

This appears to be the final iteration of this optimization, having incorporated Jeff King's suggestion to expand the changes to all similar cases. The diff remains clean and focused, touching only the cone mode pattern handling code paths. The patch seems ready for inclusion, representing a straightforward but meaningful performance win in the sparse-checkout subsystem.