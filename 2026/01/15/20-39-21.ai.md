This is a technical response about delta islands and bitmaps interaction from Jeff King (Peff) to Martin Fick's question about configuring delta islands per head.

Jeff clarifies that delta islands don't actually require reachability bitmaps to function, despite some confusing bitmap usage in the implementation. He explains the delta islands mechanism is independent of on-disk bitmaps and provides the correct configuration syntax to create per-head islands using a regex pattern. 

The response notes this approach may significantly increase packfile sizes since it prevents delta reuse across branches, but acknowledges this tradeoff may be acceptable if users primarily fetch branches individually. Jeff's answer suggests the original issue was likely a configuration misunderstanding rather than a technical limitation, though he cautions about the storage implications of the requested setup.

Key technical details: the `[pack] island = refs/heads/(.*)` configuration pattern, the distinction between delta island bitmaps and reachability bitmaps, and the performance tradeoffs of per-head islands. This appears to resolve Martin's core question about whether bitmap coverage affects delta island behavior.