This thread addresses a documentation correction for `git-config`'s `--get-urlmatch` option. The original documentation incorrectly suggested combining `--url` with `--all`, which doesn't work as described. Pushkar Singh submitted a straightforward one-line fix to the man page to accurately reflect the proper invocation syntax for this option.

The patch is small but important, as it prevents users from being misled when transitioning to the newer config syntax. The commit message clearly explains both the documentation inaccuracy and the correction needed. This appears to be a clean, uncontroversial documentation fix that should be ready for merging.