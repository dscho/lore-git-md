This is a refactoring patch series from Patrick Steinhardt that standardizes the naming of commit list functions to follow Git's coding guidelines. The series renames three functions that didn't conform to the `commit_list_*` prefix pattern:

1. `copy_commit_list()` -> `commit_list_copy()`
2. `reverse_commit_list()` -> `commit_list_reverse()`
3. `free_commit_list()` -> `commit_list_free()`

The changes are mechanical but widespread, touching 40 files across the codebase. Patrick has thoughtfully included compatibility wrappers for the old function names to avoid breaking in-flight patch series. The series is based on recent master (8745eae506) and avoids conflicts with the "seen" branch.

This is a straightforward cleanup that improves consistency in the codebase. The patch series appears well-structured with each commit focused on one rename operation. Given Patrick's track record with similar refactoring work and the non-controversial nature of aligning with existing coding guidelines, this series seems likely to be merged without significant discussion.