This is a refactoring patch in Patrick Steinhardt's series addressing bitmap and bisect functionality issues with exact ref matching. Karthik Nayak reviews the first patch which consolidates duplicate bitmap tip iteration logic from pack-objects.c and repack-midx.c into a new `for_each_preferred_bitmap_tip()` function in pack-bitmap.c.

The change removes the last external caller of `bitmap_preferred_tips()`, allowing it to be made static to pack-bitmap.c. The new helper function handles the config check internally, simplifying callers. Karthik's review confirms the changes look correct, noting the config check is now centralized in the new function rather than duplicated at each callsite.

This is preparatory work for fixing a BUG() condition that occurs when processing exact ref matches, with the actual fix coming in subsequent patches. The refactoring maintains existing behavior while cleaning up the code structure - a typical Steinhardt approach of separating mechanical improvements from functional changes. The review interaction shows the patch is on track, with Karthik verifying the config handling simplification works as intended.