This is a bugfix for git-gui's diff viewer alignment on Windows with Tcl/Tk 8.6. Chris Idema is working to make git-gui's tab alignment match gitk's behavior across different diff contexts, following maintainer Johannes Sixt's detailed specifications.

The patch addresses inconsistent tab alignment that makes diffs effectively unreadable in git-gui while working correctly in gitk. Johannes Sixt has provided exact offset requirements for each context: regular diffs need offset 1, combined diffs (merge conflicts) need offset 2, and untracked files need offset 0. This builds on commit a43c5f51a4b1 which originally handled 3-way diffs.

Chris confirms testing across all diff states and identifies the remaining work: implementing context detection to apply the correct offsets while preserving the staging interface functionality. The solution will use Tk's native `apply_tab_size` configuration, now fully accepted after earlier debate about text conversion approaches. The commit message will frame this as "shifting tabstops to account for first column" per maintainer guidance.

This represents the final implementation phase after technical consensus was reached, with Johannes Sixt providing precise requirements and Chris Idema working through the remaining edge conditions. The fix is particularly important for Windows users where the misalignment is most severe in Git for Windows' Tcl/Tk 8.6 environment.