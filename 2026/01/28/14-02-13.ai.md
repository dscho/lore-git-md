This is a bugfix thread for git-gui addressing tab alignment issues in the diff viewer. The discussion has evolved through several technical considerations:

Chris Idema's latest email responds to Johannes Sixt's feedback about the proposed `apply_tab_size` solution. The key points in this update are:

1. **Unstaged file clarification**: Chris questions the distinction between unstaged file content and patch text, noting that context lines in diffs also have incorrect tab stops when indented with spaces.

2. **Implementation issue**: The current patch fails with a "no such variable" error for `have_tk85`, indicating a missing variable declaration that needs to be addressed.

3. **User impact**: Chris emphasizes the alignment issue makes git-gui diffs effectively unreadable, treating it as a critical usability problem.

The thread reveals several unresolved technical questions:
- How to properly handle tab stops for different diff contexts (unstaged files, single-parent diffs, combined diffs)
- The correct initialization of the `have_tk85` variable
- Whether the simple one-line fix (using `apply_tab_size 1`) is sufficient despite not being the "official" approach

Johannes Sixt had previously raised concerns about:
- Suboptimal tabstop calculations (first tab at column 1 being ineffective)
- Need for different `apply_tab_size` values per context
- Historical context from gitk's development

The patch currently solves the immediate alignment issue but requires more work to handle all edge cases correctly while maintaining compatibility with gitk's behavior. The discussion continues to focus on finding a solution that preserves staging functionality while achieving visual consistency across all diff viewing scenarios.