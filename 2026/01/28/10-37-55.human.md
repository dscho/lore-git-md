This is a refactoring patch in Patrick Steinhardt's series addressing bitmap and bisect functionality issues with exact ref matching. Karthik Nayak reviews the first patch which consolidates duplicate logic for iterating over preferred bitmap tips (configured via `pack.preferBitmapTips`) into a new `for_each_preferred_bitmap_tip()` function.

The change removes the last external caller of `bitmap_preferred_tips()`, allowing it to be made file-local to pack-bitmap.c. The refactoring prepares for subsequent fixes to handle exact ref matches correctly while maintaining the existing behavior where a ref like "refs/heads/base" matches both itself and "refs/heads/base-something".

Karthik's review confirms the changes look correct, noting the config check is now centralized in the new function rather than duplicated at each callsite. The patch appears to be a clean preparatory refactoring with no behavior changes, setting up the actual bugfix in later patches of the series.