This is a feature patch adding a `--maximal-only` option to `git rev-list` and related commands, marking commits that are not reachable by any other commits (effectively the "tips" of the commit graph). The patch was previously discussed and is now ready to merge into the `next` integration branch according to Derrick Stolee, who notes that further enhancements (like using the `git merge-base --independent` algorithm or extending the option to other commands) can be deferred to future work.

The patch is part of Stolee's ongoing work on commit-graph optimizations and reachability queries. It appears uncontroversial at this stage, with no outstanding objections or required changes. The discussion has settled on merging this version as-is, leaving potential follow-ups for later. This fits into Git's incremental development model where small, focused changes are preferred over bundling multiple features into a single patch series.