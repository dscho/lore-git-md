Here's a human-readable summary of the thread so far:

The discussion explores a proposal to extend Git's ignore mechanism by automatically including additional files matching `*.gitignore` alongside the standard `.gitignore`. The original RFC from ZH L suggests this could help organize ignore rules in large repositories while maintaining backward compatibility, noting some projects currently generate `.gitignore` files via scripts.

brian m. carlson has raised thoughtful objections, arguing that editor-specific rules belong in personal ignore files rather than being project-wide, and pointing out that automatic loading wouldn't be truly backward compatible with older Git versions. They suggest an explicit inclusion syntax might be preferable, though challenging to implement cleanly. The response includes practical observations about ignore file sizes in various projects and questions whether the feature is actually needed given existing mechanisms like per-directory `.gitignore` files.

Key open questions remain about naming patterns, precedence rules, interaction with existing ignore mechanisms, and performance impact. The discussion is still in the exploratory phase, with no clear consensus yet on whether to pursue implementation.