This is a feature patch from Derrick Stolee adding a `--maximal-only` option to `git rev-list`, now in its second iteration. The thread has reached consensus on the implementation, with Junio C Hamano providing final conceptual alignment on how this relates to the existing `--independent` functionality.

The patch enables selecting "frontier" commits (maximal in reachability order) which is useful for bundle URIs and bitmap optimization. Key technical details include using the `CHILD_VISITED` flag (bit 28) in the revision walking infrastructure, documented incompatibility with `--boundary`, and comprehensive test coverage of edge cases. Junio clarified that both `--maximal-only` and `--independent` fundamentally filter input sets for commits not reachable from others, differing mainly in `--maximal-only`'s support for negative revisions.

The implementation is now complete with all feedback addressed, including naming consensus, finalized documentation, and exhaustive test coverage. Junio has confirmed full understanding of the behavior and the thread has effectively concluded with consensus. Open questions remain about future optimizations for bundle URIs and the long-term bit allocation strategy, with no outstanding technical issues blocking merge.

Derrick indicates he's open to future extensions of this functionality to other commands like `git show`, but prefers to handle those in a separate series. The patch appears well-considered and ready for integration, with clear understanding of its relationship to existing functionality and potential future directions.