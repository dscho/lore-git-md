Part of the ongoing ODB abstraction effort, this patch modifies transaction handling to track object sources rather than just the object database. Justin Tobler's change updates `struct odb_transaction` to reference a specific odb_source instead of the broader object_database, aligning with the project's goal of making object storage more modular. The patch touches core object-file handling code, carefully replacing all references to the old `odb` field with the new `source->odb` indirection while maintaining identical behavior. This appears to be a mechanical but necessary step in the larger refactoring effort, with clean commit message and thorough updates across the transaction lifecycle functions. Reviewers will want to verify the indirection doesn't introduce any subtle behavioral changes in edge cases.