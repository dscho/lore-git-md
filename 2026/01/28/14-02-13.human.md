This is a bugfix patch in the git-gui project addressing tab alignment inconsistency in the diff viewer. The thread involves Chris Idema (patch author), Johannes Sixt (git-gui maintainer), and Junio Hamano discussing the technical details of tab rendering in Tcl/Tk.

The current patch attempts to fix misaligned tabs in git-gui's diff display by using Tk's `apply_tab_size` configuration rather than modifying text content, preserving the ability to stage individual hunks. However, Johannes Sixt notes several remaining issues:

1. The current implementation's tabstop calculation may be suboptimal since diff lines always start with a character ('+', '-', or space), making the first tabstop position ineffective
2. Different contexts (regular diffs, combined diffs, unstaged files) may require different tab sizing
3. The solution needs to consider historical behavior established in commit a43c5f51 (2012)

Chris Idema responds seeking clarification on when unstaged file content appears without patch text, and reports an error with the `have_tk85` variable. He maintains that his one-line fix resolves the immediate readability issue, which he describes as a "show stopper" for their workflow, but is open to alternative solutions.

The patch appears technically sound in its approach (using Tk configuration rather than text modification) but needs refinement to handle all diff contexts correctly. The discussion shows active collaboration between the author and maintainer to find a robust solution that maintains compatibility with gitk's behavior.