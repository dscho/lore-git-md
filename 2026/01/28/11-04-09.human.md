This is a bugfix patch in a series addressing issues with exact ref matching in Git's bitmap and bisect functionality. Karthik Nayak is reviewing Patrick Steinhardt's second patch, which specifically fixes a BUG() that occurs when `pack.preferBitmapTips` is set to an exact reference name rather than a prefix.

The patch replaces `refs_for_each_ref_in()` with `refs_for_each_fullref_in()` in the bitmap handling code, avoiding the assertion failure when processing exact refs. Karthik confirms the fix works as intended, noting that the test case demonstrates how an exact ref in `pack.preferBitmapTips` now correctly adds the commit to bitmap coverage. He points out a minor nit about the test setup (questioning whether `--message="%s"` is needed) but overall agrees with the solution.

This is a focused technical fix that maintains existing matching behavior while resolving a clear bug. The review is positive, with Karthik validating both the approach and test coverage. The patch appears ready for merging as part of the larger series addressing ref iteration issues.