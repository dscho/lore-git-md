Thread identity: Feature patch from Derrick Stolee adding `--maximal` option to `git rev-list` to filter output to commits not reachable from others in the range.

Core problem/goal: Provide a way to select minimal reference points (e.g. for bundle URIs) by showing only commits that are maximal in the reachability order, helping find "frontier" commits not dominated by others in the input set.

Key technical details:
- New `--maximal` option for `git rev-list` (implementation in revision.c/commit.c)
- Adds `CHILD_VISITED` flag (bit 28) to track reachability during walk
- Extends Git's object flag space usage to 29/32 bits
- Thorough test coverage including interaction with `--first-parent`
- Potential interaction with `--boundary` that may need resolution

Current status: Initial version with complete implementation and tests. Interface design under active discussion, particularly naming and documentation. Debate has intensified around option naming and flag bit usage.

Open questions:
- Flag bit allocation concern (28/32 bits used) may need broader discussion about long-term implications
- Documentation clarity and placement - current description considered unclear
- Option naming debate expanded with new alternatives:
  * Stolee maintains `--maximal` is mathematically precise (order theory)
  * Johannes Sixt proposes: `--only-maximal`, `--hide-reachable`, `--range-head-only`, `--starting-point`, `--topmost-only`
  * Discussion of Git's historical preference for intuitive over mathematical terminology
- Potential need to make `--maximal` and `--boundary` mutually exclusive
- Applications beyond bundle URIs (e.g. reachability bitmap optimization)

Participant positions:
- Derrick Stolee (author) maintains feature utility justifies flag bit usage and defends mathematical naming
- Johannes Sixt deepens interface concerns:
  * Stronger case against `--maximal` as potentially confusing
  * Provides multiple concrete naming alternatives
  * Notes unexpected `--boundary` interaction
  * Continues to avoid challenging implementation quality
- Discussion remains constructive with focus on user experience

Related work: Builds on existing revision walking infrastructure. May relate to bundle URI and reachability bitmap work. Interface questions parallel existing options like `--boundary`. Naming debate reflects Git's historical preference for intuitive terminology. Flag bit usage discussion connects to broader object storage concerns.