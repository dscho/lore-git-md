Here's a concise human-readable summary of the thread:

Derrick Stolee proposed adding a `--maximal-only` option to `git rev-list` to select "frontier" commits (those not reachable from others), particularly useful for bundle URIs and bitmap optimizations. After discussion, the thread reached full consensus with Junio Hamano's endorsement of both the implementation and long-term direction. The new option improves upon the existing `--independent` functionality by properly handling negative revisions, and is expected to eventually replace it across Git commands.

The technical implementation uses a dedicated bit flag in the revision walking infrastructure, with comprehensive test coverage and updated documentation. Junio confirmed this as the right approach, noting the clear distinction between "independent" and "maximal" commit selection. With all feedback addressed in v2 and no outstanding issues, the patch is ready for merging. Future work may extend this functionality to other commands like `git show` while maintaining `--maximal-only` as the preferred interface.