This thread discusses Derrick Stolee's proposal to add a `--maximal` option to `git rev-list` that would filter output to show only commits not reachable by others in the range - useful for identifying "frontier" commits in operations like bundle URIs. The implementation is complete with thorough test coverage, but debate has centered on interface design, particularly the option name.

Johannes Sixt has led concerns about the mathematical `--maximal` terminology potentially confusing users, especially since `git log` has an unrelated `--minimal` option with different semantics. He's proposed several alternatives (`--only-maximal`, `--hide-reachable`, etc.) that might better match Git's historical preference for intuitive names. The discussion remains constructive, with Stolee defending the mathematical precision of his naming choice while engaging with the feedback. 

While the technical implementation appears sound, the naming question remains unresolved, with the thread now exploring how the option would be understood in different contexts (like `git log` versus plumbing commands). The earlier concern about flag bit usage (28 of 32 bits now allocated) also remains open but wasn't revisited in this latest exchange. The feature's utility is uncontested, but its final form may depend on finding naming consensus that balances technical accuracy with user expectations.