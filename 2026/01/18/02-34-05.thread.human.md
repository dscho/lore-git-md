Here's a human-readable summary of the thread:

Derrick Stolee has proposed adding a `--maximal-only` option to `git rev-list` to select "frontier" commits (those not reachable from others), which will be useful for bundle URIs and bitmap optimizations. After some initial discussion about how this relates to the existing `--independent` flag, Junio clarified that both commands fundamentally do the same thing - filtering input sets for unreachable commits - with the key difference being that `--maximal-only` supports negative revisions.

The second version of the patch has addressed all feedback, including settling on the final name (`--maximal-only`), documenting the behavior precisely, and adding comprehensive tests. Johannes Sixt provided important clarification about the behavior that helped solidify everyone's understanding. The implementation uses bit 28 in the revision walking infrastructure and explicitly documents incompatibility with `--boundary`.

With all technical questions resolved and consensus reached, the patch is now ready to be merged. While the immediate need is addressed, the discussion notes some future considerations around optimization opportunities, bit allocation strategy, and potential follow-up work for similar reachability queries. The documentation has been updated to clearly explain that support for negative revisions is what distinguishes this new option from `--independent`.