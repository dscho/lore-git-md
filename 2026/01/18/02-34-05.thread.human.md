Derrick Stolee's `--maximal-only` feature for `git rev-list` has reached a mature state after addressing all feedback in its second iteration. The option, which filters commits to only those maximal in reachability order (useful for bundle URIs and bitmap optimizations), now has consensus on its name, comprehensive test coverage, and clear documentation about its incompatibility with `--boundary`. The implementation uses bit 28 in the revision walking machinery while noting this as a consideration for future bit allocation strategies.

Junio C Hamano has now raised a final conceptual question about how this new capability relates to `git merge-base --independent`, which finds minimal sets of reachable commits. While mathematically related (both dealing with partial order concepts in practice they serve different purposes - `--maximal-only` identifies frontier commits for efficient transfer, while `--independent` finds common ancestry. This clarification point appears to be the last open item before potential merging, with all technical and interface concerns already resolved. The thread shows strong consensus among participants including Johannes Sixt and Junio, with Derrick having incorporated all prior feedback into the current implementation.