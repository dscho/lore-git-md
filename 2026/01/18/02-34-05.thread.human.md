Here's a concise human-readable summary of the thread:

Derrick Stolee proposed adding a `--maximal-only` option to `git rev-list` to select "frontier" commits (those not reachable from others) while supporting negative revisions - a capability missing from the existing `--independent` flag in `git merge-base`. After productive discussion, the thread reached full consensus on both the implementation and long-term direction.

The v2 patch implements the feature using bit 28 (CHILD_VISITED) in the revision walking code, with comprehensive tests covering edge cases. Junio Hamano confirmed the `--maximal-only` name is appropriate and endorsed it as eventually replacing `--independent` across Git commands, since it provides strictly more functionality. The only difference is `--maximal-only`'s support for negative revisions.

With all technical questions resolved and Junio's approval of both the immediate implementation and future vision, the patch is ready for merging. Stolee noted openness to follow-up work extending this to commands like `git show`, but those would be separate efforts. This establishes `--maximal-only` as the canonical way to query independent commits, particularly for bundle URI and bitmap optimization use cases.