Test improvement addressing exit code masking in t2203-add-intent.sh. This v2 patch from Tian Yuchen implements Junio Hamano's suggested simplification from the thread discussion, replacing `git status | grep -v` pipelines with direct use of `git status -uno` to exclude untracked files. 

The change eliminates the need for grep filtering while preserving the test's intent, as the test never expects untracked file output ('^?' lines in porcelain formats). This approach more reliably catches `git status` failures by not masking its exit code. The patch updates all three porcelain format checks in the test script (default, --porcelain, --porcelain=v2) to use the new method.

This is a straightforward improvement that follows the maintainer's guidance exactly. The patch is part of Tian's GSoC work (now properly tagged in the subject line) and appears ready for merging after this revision. No further discussion is expected on this thread.