This is a follow-up in Harald Nordgren's ongoing series about enhancing `git status` to show divergence from both upstream and push tracking branches. The email responds to Junio Hamano's suggestion about making the push-tracking comparison optional via a new `status.compareBranches` configuration.

The key development here is that Harald has implemented Junio's proposed configuration approach, allowing users to specify which branches to compare against (e.g. `@{upstream}`, `@{push}`) in the status output. However, the implementation currently has a memory leak that Harald hasn't been able to resolve despite multiple CI runs.

The thread context shows this builds on a stable merged implementation, with the discussion now focused on configuration design and workflow preferences. Notable is the emerging consensus that triangular workflow practitioners (like Jeff King and Phillip Wood) often prefer binary state information over detailed ahead/behind counts, and commonly use `push.default=current` configuration.

The memory leak issue suggests this latest iteration may need more debugging before being ready for merging, though the conceptual direction aligns with the thread's progression toward flexible configuration. The implementation appears to be following Junio's suggestion for an extensible comparison mechanism that could accommodate future branch comparison types beyond just upstream and push relationships.