Test improvement patch that simplifies exit code handling in t2203-add-intent.sh. This is the second version of a patch from Tian Yuchen, addressing feedback from Junio Hamano in the thread.

The patch replaces several instances of `git status | grep -v` with `git status -uno` to properly propagate exit codes while excluding untracked files. This change makes the test more reliable by ensuring failures in `git status` itself are caught, rather than being masked by the grep pipeline. The modification is consistent across all three status formats (default, --porcelain, and --porcelain=v2).

The patch appears well-executed, implementing Junio's suggested simplification while maintaining the test's original intent. The changes are focused and mechanical, touching only the test file in question. This is part of Tian's GSoC work, as indicated by the [GSoC] prefix added to the subject line per Junio's request. The thread shows clear consensus on the approach, and this version seems ready for merging.