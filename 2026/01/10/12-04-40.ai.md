This is a documentation patch in the context of an ongoing feature addition to the `git repo info` command. Jean-Noël Avila provides a small but important correction to Lucas Seiki Oshiro's patch that adds a `--keys` flag to `git repo info`.

The correction standardizes the documentation to use `_NUL_` (with underscores) when referring to the NUL character, following Git's documentation conventions for special character names. This is a minor but meaningful improvement to maintain consistency across the documentation.

The change is minimal - just a one-line fix to the man page - but shows careful attention to detail in the documentation. Jean-Noël, as the documentation maintainer, is ensuring the new feature's documentation matches the project's established style. The patch being corrected is part of a larger series that has already gone through multiple iterations and is now ready for integration, with Junio Hamano having confirmed it will replace the queued `lo/repo-info-keys` topic.