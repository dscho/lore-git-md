Junio C Hamano provides a minor but important formatting suggestion for Samo Pogačnik's patch series on shallow repository handling. This is a follow-up to the v2 series that fixes a memory leak and implements relative-depth fetching.

The maintainer points out that the three-dash line in the example output could be misinterpreted by `git am` as a patch divider. He recommends indenting all displayed material (like commit graphs and shallow commit lists) to avoid this parsing ambiguity. This is a common pitfall when including example output in commit messages.

The suggestion doesn't change the technical content of the patches, which have already been through thorough review from Patrick Steinhardt. The series remains focused on shallow.c and upload-pack.c changes, with:
- Memory leak fix via `object_array_clear(&stack)`
- New `get_shallows_depth()` function for relative-depth calculation
- Removal of redundant `get_reachable_list()`
- Additional test coverage in t5500-fetch-pack.sh

Junio's comment is purely about commit message hygiene - a maintainer's polish touch on an otherwise technically sound series that appears ready for integration. The patches demonstrate effective collaboration, with Samo having addressed all substantive review feedback in v2.