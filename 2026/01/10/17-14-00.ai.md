This is a response in the ongoing discussion about the `git-history` command proposal, which aims to add flexible history editing capabilities (currently `reword` and `split` subcommands) to Git. The email comes from SZEDER Gábor, who raises a cautionary note about merging the series without considering future subcommands that might introduce conflicts.

The response pushes back against the optimistic assessment from Elijah Newren (who had reviewed the v9 series and found only minor issues remaining). SZEDER argues that the series should not be merged until there are concrete plans for handling future subcommands that could cause merge conflicts, drawing a parallel to the `git switch/restore` situation where early design decisions became constraints.

This introduces a new concern in the thread about forward compatibility and design constraints. While the technical implementation appears sound (as noted by Newren), SZEDER is flagging a higher-level architectural consideration about how the command might evolve. The reference to `git switch/restore` suggests a worry that merging the current design could lock in decisions that might prove limiting when more complex history-editing operations are added later.

The comment doesn't propose specific changes but serves as a warning that the series might need more comprehensive planning before being considered merge-ready, despite the technical polish it has achieved through nine iterations. This perspective adds a new dimension to the review process, shifting focus from implementation details to long-term maintainability concerns.