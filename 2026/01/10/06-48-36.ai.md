This is a follow-up to the `git repo info --keys` feature series, addressing format handling consistency across `git repo` subcommands. The patch series is now complete with all technical requirements met and ready for integration.

The email from Junio Hamano asks for clarification on how this version relates to the previously queued `lo/repo-info-keys` topic (commit ac3e74d2). The key changes in this final version are:

1. Added `FORMAT_DEFAULT` enum to cleanly handle default format cases, extending support to `git repo structure` in addition to `git repo info`. This ensures consistent behavior when resetting output formats across subcommands.

2. The `--keys` implementation remains functionally the same as in the queued version, but now sits on top of the refactored format handling infrastructure. The commit message for the `--keys` patch will need to be rebased on the new preparatory changes.

The series has been thoroughly reviewed with all feedback addressed, including comprehensive test coverage for format behaviors and explicit tests for format reset functionality. Patrick Steinhardt has verified the test coverage completeness, and the implementation aligns with Junio's original suggestion for programmatic key discovery.

This appears to be the final polish before integration, with the only remaining question being how to handle the relationship with the already-queued topic. The changes are well-motivated and the implementation looks solid, building on the established direction for machine-readable repository metadata access.