This is a final review comment from Junio C Hamano on the long-running effort to enhance `git status` with push tracking information. The email focuses on variable naming in the refactored comparison logic, specifically addressing Harald Nordgren's question about renaming the `sti` variables.

Junio clarifies that simply renaming to `cmp_fetch` and `cmp_fetch_push` wouldn't be helpful unless the names clearly contrast fetch vs push directions. He reiterates his earlier concern that `sti' (from `stat_tracking_info`) is too cryptic and suggests `cmp` (or `compare`) would be better, though still imperfect. The key point is that variable names should reflect meaning rather than just their source function.

This represents the final polish on a series that has gone through 21 iterations, with all major technical issues already resolved. The discussion here is purely about naming conventions - a sign the implementation is otherwise complete and ready for merging. Junio's comments carry particular weight as the maintainer, indicating this is one of the last remaining items before he can pick up the series.