This is a feature patch series that finalizes the `git repo info --keys` implementation and format unification work. The series replaces an earlier queued version (ac3e74d2) with an improved approach that builds on new format infrastructure while maintaining the same `--keys` functionality.

Junio confirms the relationship with the previously queued work - these patches are intended to replace the older version in the `lo/repo-info-keys` topic. The key improvements in this final iteration include:

1. The introduction of a `FORMAT_DEFAULT` enum value to standardize format handling across `git repo` subcommands
2. Comprehensive format initialization refactoring to eliminate hardcoded values
3. Strict validation of option combinations
4. Full test coverage including format precedence and cross-subcommand consistency checks

The implementation provides machine-readable repository metadata access through `--keys` while unifying output format behavior. The series is now ready for integration into `next`, having addressed all review feedback and clarified its relationship with existing queued work.