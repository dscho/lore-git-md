This is a feature patch series that finalizes the `--keys` flag implementation for `git repo info` with format unification. The series replaces an earlier queued version (ac3e74d2) and has now been approved for integration by Junio Hamano.

The two patches add machine-readable key discovery via `--keys` while standardizing output format handling across `git repo` subcommands. Key improvements over the earlier version include introducing a `FORMAT_DEFAULT` enum for consistent default format behavior and comprehensive test coverage of format combinations and cross-subcommand behavior.

The implementation has been thoroughly reviewed by both Junio Hamano (who originally suggested the feature) and Patrick Steinhardt, with all technical requirements met. The series builds on existing format infrastructure (bdc5341ff6) and is now cleared for merging into the `next` branch after addressing the final integration question about its relationship to the queued topic.

This represents the completion of a well-executed feature addition that improves both programmatic access to repository metadata and the consistency of Git's output formatting. The careful attention to test coverage and format unification suggests this will be a maintainable addition to the `git repo` command family.