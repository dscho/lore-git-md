This is a technical review response from Elijah Newren regarding a patch in Patrick Steinhardt's `git-history` series. The patch adds a `final_oid` field to track the last rewritten commit in the replay machinery, which Patrick intends to use for HEAD-only updates in the new command.

Newren questions the necessity of this field, pointing out that when updating just HEAD (as mentioned in Patrick's commit message), the existing `updates` array in `replay_result` should already contain the needed information (as `updates[0]`). He suggests the only scenario where `final_oid` might be useful is when rewriting commits that aren't branch tips - which would contradict Patrick's stated use case.

The exchange reveals a potential disconnect between the proposed solution and the described problem. Newren's review focuses on whether the change is actually needed given the existing data structures, rather than finding fault with the implementation itself. This continues the thread's pattern of rigorous technical scrutiny of the replay infrastructure extraction.

Key technical details:
- The patch adds a `struct object_id final_oid` to `replay_result`
- Patrick's justification references upcoming HEAD-only updates in `git-history`
- Newren identifies possible redundancy with existing `updates` array
- The discussion centers on proper data modeling rather than functional concerns

This interaction shows the careful review process the replay infrastructure is undergoing as it gets extracted for broader use. Newren's feedback suggests either the commit message needs clarification about the real need for `final_oid`, or the change may be unnecessary for the stated use case.