This is a review comment on the final patch in Patrick Steinhardt's series to refactor Git's ref backend verification. Shejialuo suggests extracting a common helper function for checking if a ref is HEAD, noting that similar logic exists in `ref-log.c`'s `is_head()` function.

The patch being reviewed moves HEAD-specific checks from `builtin/fsck.c` into the refs subsystem's `refs_fsck_symref()` function. Shejialuo's observation points out an opportunity to avoid code duplication by centralizing the HEAD detection logic that currently appears in multiple places.

This is a minor but thoughtful suggestion that would improve code maintainability. The series has already received thorough review from Shejialuo, who has identified several implementation details throughout the 17-part refactoring. The current suggestion doesn't block the patch but offers a clean-up opportunity that could be addressed in a follow-up.