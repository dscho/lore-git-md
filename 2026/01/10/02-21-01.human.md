This is the final review comment from Junio C Hamano on Harald Nordgren's long-running patch series to enhance `git status` with push tracking comparisons. The series has gone through 19 iterations with extensive review from multiple contributors, and Junio is now giving his approval with just one minor naming suggestion.

Junio notes that while the variable name "sti" (for the fetch tracking comparison) was already somewhat awkward but understandable, the new parallel variable "push_sti" for the push tracking comparison compounds the issue. He suggests renaming both to more meaningful names like "cmp_fetch" and "cmp_push" to make the code's intent clearer. 

This is a small polish on an otherwise well-reviewed series that has already addressed all technical concerns. The implementation now cleanly shows both upstream and push tracking comparisons in `git status` when they differ, with comprehensive test coverage. Junio's comment indicates the series is effectively ready to merge, with just this optional variable naming improvement remaining. The extensive review process has confirmed the design is sound and the implementation matches Git's conventions.