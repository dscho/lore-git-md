This is the 21st iteration of Harald Nordgren's feature series to enhance `git status` to show divergence from both upstream and push tracking branches. The series is now in its final form after extensive review, with all feedback incorporated.

The v21 version makes a final naming refinement suggested by Junio C Hamano, renaming variables from `sti`/`push_sti` to the clearer `cmp_fetch`/`push_cmp_fetch` to better reflect their purpose. This change appears in both patches of the series but doesn't affect functionality.

The implementation remains unchanged from v20 in all other respects:
- Patch 1/2 refactors the comparison formatting logic into a new `format_branch_comparison()` helper
- Patch 2/2 adds push tracking display when it differs from upstream
- The output maintains the same familiar format on a separate line for push tracking
- Comprehensive test coverage (262 lines) verifies all cases
- The solution handles remapped push refspecs and maintains all existing behavior

With this naming polish applied, the series appears merge-ready after 21 iterations of refinement. All technical concerns have been addressed, and the implementation has received approval from multiple reviewers including Junio Hamano, Ben Knoble, and Phillip Wood. The changes are well-contained to `remote.c` and the test suite.