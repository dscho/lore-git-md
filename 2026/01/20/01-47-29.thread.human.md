This thread documents a coordinated effort to improve Git's documentation infrastructure, specifically around the `git replay` manual page and related linter updates. The work follows patterns previously established for `git shortlog` documentation cleanup.

D. Ben Knoble led the first part, modifying `git-replay.adoc` to conditionally exclude an unnecessary "Commit Formatting" section when included via `rev-list-options.adoc`. This required introducing new multiple-attribute `ifndef` syntax. Jean-Noël Avila then updated the documentation linter to properly handle this syntax, initially with Junio C Hamano's help to refine the regex patterns, and later with a more robust solution that completely ignores conditional block macros rather than trying to parse them as inline elements.

All technical issues have been resolved through this collaboration, with the changes working across both AsciiDoc processors (Python/Ruby implementations) and making the linter more resilient against false positives. The patches are now fully complete with all sign-offs obtained, marking another step forward in Jean-Noël Avila's broader effort to standardize Git's manual pages using consistent AsciiDoc markup patterns.