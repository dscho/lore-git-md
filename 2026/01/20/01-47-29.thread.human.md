This thread concerns a documentation improvement to clean up the `git-replay` manual page by removing an unnecessary "Commit Formatting" section when included via `rev-list-options.adoc`. D. Ben Knoble authored the patch series (now at v2) to implement this using a novel multiple-attribute `ifndef` syntax in AsciiDoc - a pattern previously established for `git shortlog` but now extended with more complex conditional logic.

Junio C Hamano has reviewed and approved the technical approach, confirming the syntax works correctly with both AsciiDoc processors. The only remaining issue is that Git's `lint-docs` check currently flags this valid syntax as an error. Junio suggests updating the linter to recognize this pattern, after which the patch will be ready for merging. This represents a small but precise improvement in Git's ongoing effort to standardize manual page formatting and conditional section inclusion.