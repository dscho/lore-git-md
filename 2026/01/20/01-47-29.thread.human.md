The thread began with D. Ben Knoble's patch to clean up the `git replay` manual page by removing an unused "Commit Formatting" section, following the pattern established for `git shortlog`. This required introducing a novel multiple-attribute `ifndef` syntax in the AsciiDoc files. After Junio C Hamano reviewed and resolved a final linter issue, the patch was deemed ready for merging.

Jean-Noël Avila has now followed up with a refinement to the documentation linter (`lint-gitlink.perl`) to better handle conditional section directives like `ifdef` and `ifndef`. His change simplifies the linter by skipping these block macro lines entirely rather than trying to parse them, which prevents false positives with the new multiple-attribute syntax used in the `git-replay` documentation. This builds on Junio's earlier observation about needed linter updates and provides a more robust solution that maintains the tool's core linkgit reference checking while accommodating the documentation patterns being established.

With these two patches - D. Ben Knoble's content change and Jean-Noël's tooling adjustment - the documentation cleanup for `git replay` is now fully addressed, with both the content and tooling sides properly aligned. The changes represent incremental improvements to Git's documentation infrastructure while maintaining consistency with existing patterns.