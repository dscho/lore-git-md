This is a follow-up in the ongoing thread about configurable branch comparison in `git status`. Harald Nordgren reports that Jeff King's memory management fixes for the branch tracking code (intended to address leaks in both the legacy push tracking code and Harald's new feature) did not resolve the memory issues in his implementation.

The email is brief but indicates that despite applying Peff's fixes, CI tests are still detecting memory leaks in Harald's branch comparison work. He provides a GitHub PR link showing the failing CI runs. This suggests there may be additional leak sources beyond what Peff's initial fixes addressed, keeping this thread active as the memory management issues are not yet fully resolved.

The core functionality of the branch comparison feature appears stable, but these lingering memory issues need to be cleaned up before the work can be considered merge-ready.