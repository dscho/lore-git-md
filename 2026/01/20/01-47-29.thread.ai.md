Thread identity: Documentation patch for `git replay` manual page cleanup of unused formatting options.

Core problem or goal: Remove unnecessary "Commit Formatting" section from `git-replay.adoc` by conditionally excluding it when included via `rev-list-options.adoc`, following the same pattern as `git shortlog`. The technical challenge involves verifying the multiple-attribute `ifndef` directive syntax works across both AsciiDoc processors (Python and Ruby implementations)

Current status: Patch v1 from D. Ben Knoble proposes adding a `git-replay` attribute to skip the formatting options section. Junio C Hamano reviewed and approved while noting this is the first use of multiple attributes in an `ifndef` directive in Git's documentation. Knoble confirms in follow-up that the syntax works with the AsciiDoc Python processor and expects it to work with Ruby as well.

Key technical details: Modifies `Documentation/git-replay.adoc` to define the new attribute and updates `Documentation/rev-list-options.adoc` to skip the "Commit Formatting" section when either `git-shortlog` or `git-replay` is defined. The multiple-attribute `ifndef` syntax is a new pattern in Git's documentation that requires explicit verification across both AsciiDoc implementations (Python and Ruby)

Participant positions: D. Ben Knoble authored this documentation improvement and confirmed the multiple-attribute syntax works as expected. Junio C Hamano reviewed and approved the change while noting the syntactic novelty, with both agreeing this observation should be included in the commit message. The change continues Git's documentation housekeeping effort to keep manual pages focused on relevant content.

Open questions and loose ends: None remaining - the technical verification of the syntax across both AsciiDoc processors addresses the only outstanding concern. The patch appears ready to proceed with the noted commit message adjustment.