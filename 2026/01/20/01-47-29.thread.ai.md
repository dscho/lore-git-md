Thread identity: Documentation patch for `git replay` manual page cleanup of unused formatting options.

Core problem or goal: Remove unnecessary "Commit Formatting" section from `git-replay.adoc` by conditionally excluding it when included via `rev-list-options.adoc`, following the pattern established for `git shortlog`. The technical challenge involves verifying the multiple-attribute `ifndef` directive syntax works across both AsciiDoc processors (Python and Ruby implementations).

Current status: Patch v2 from D. Ben Knoble incorporates Junio C Hamano's feedback from v1 by expanding the commit message to document the novel use of compound `ifndef` directives. The change is confirmed to work with both AsciiDoc processors. The patch appears ready for inclusion.

Key technical details: Modifies `Documentation/git-replay.adoc` to define a new `git-replay` attribute and updates `Documentation/rev-list-options.adoc` to skip the "Commit Formatting" section when either `git-shortlog` or `git-replay` is defined. Introduces Git's first use of multiple-attribute `ifndef` syntax (using commas for "any" and "+" for "all" conditions), verified to work with both Python and Ruby AsciiDoc processors. Follows the same pattern previously established for `git shortlog` documentation.

Participant positions: D. Ben Knoble authored and iterated on this documentation improvement, confirming the multiple-attribute syntax works as expected. Junio C Hamano reviewed and approved the change while noting the syntactic novelty, with both agreed should be documented in the commit message. The v2 patch incorporates this feedback.

Open questions and loose ends: None remaining - the technical verification of the syntax across both AsciiDoc processors and expanded commit message address all concerns. The patch is ready for merging as a straightforward documentation improvement in Git's ongoing manual page cleanup effort.