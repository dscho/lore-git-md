Thread identity: Documentation patch for `git replay` manual page cleanup of unused formatting options.

Core problem or goal: Remove unnecessary "Commit Formatting" section from `git-replay.adoc` since it includes `rev-list-options.adoc` but doesn't use these formatting options, following the same pattern as `git shortlog`.

Current status: Patch v1 from D. Ben Knoble proposes adding a `git-replay` attribute to conditionally exclude the formatting options section in `rev-list-options.adoc`. Junio C Hamano has reviewed and approved the change while noting this is the first use of multiple attributes in an `ifndef` directive in Git's documentation, suggesting this novelty be mentioned in the commit message.

Key technical details: Modifies `Documentation/git-replay.adoc` to define the new attribute and updates `Documentation/rev-list-options.adoc` to skip the "Commit Formatting" section when either `git-shortlog` or `git-replay` is defined. The syntax of multiple attributes in an `ifndef` directive is a new pattern in Git's documentation that may need explicit documentation in the commit message.

Participant positions: D. Ben Knoble authored this straightforward documentation improvement following established patterns. Junio C Hamano reviewed and approved the change while noting the syntactic novelty of multiple attributes in an `ifndef` directive, suggesting this observation be included in the commit message for clarity. The change continues Git's documentation housekeeping effort to keep manual pages focused on relevant content.