This is a documentation patch that cleans up the manual page for `git replay` by removing unnecessary formatting options inherited from `rev-list`. 

The patch notes that while `git-replay.adoc` includes `rev-list-options.adoc`, it doesn't actually use the formatting options section. D. Ben Knoble adds a `git-replay` attribute to conditionally exclude these unused options from the documentation. The change is straightforward - it modifies the conditional in `rev-list-options.adoc` to skip the "Commit Formatting" section when either `git-shortlog` or the new `git-replay` attribute is defined.

The patch appears well-motivated and correctly implemented. It follows the established pattern used by other commands (like `git shortlog`) that include `rev-list` options but don't need the formatting section. The commit message clearly explains the change and the diff shows the new attribute being properly set in `git-replay.adoc` and referenced in `rev-list-options.adoc`. This is a minor but sensible improvement to the documentation.