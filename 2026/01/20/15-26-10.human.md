This is the final patch in Patrick Steinhardt's 14-part ODB abstraction series, marking the completion of the object storage API unification effort. The patch removes the now-obsolete `for_each_loose_object()` and `for_each_packed_object()` functions after all callers have been converted to use the new backend-agnostic `odb_for_each_object()` API. It also inlines a helper function that's no longer needed with the simplified architecture.

The changes are straightforward cleanup - deleting unused code and consolidating the packed object iteration logic directly into its only remaining caller. The diff shows a net reduction of 96 lines across four files, continuing the series' trend of simplifying the codebase while maintaining all existing functionality. 

With this patch, the ODB abstraction layer is now fully realized, having replaced all major object iteration patterns in core Git commands (fsck, pack-objects, cat-file, commit-graph, and reachable) with unified interfaces. The series has been thoroughly reviewed, with Junio Hamano indicating it's ready for integration pending minor documentation tweaks. This represents a significant architectural improvement that makes the codebase more maintainable and paves the way for future pluggable storage backends.