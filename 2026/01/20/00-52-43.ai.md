This is the final version of a feature patch series adding `--keys` flag and format unification to the `git repo` subcommands. The series has gone through four iterations with all review feedback addressed, including documentation standardization (using `_NUL_` convention) and format naming clarification ("lines" with "default" as synonym per Patrick Steinhardt's suggestion).

The implementation provides machine-readable repository metadata access through `git repo info --keys` while unifying format handling across related commands. Key technical aspects include new `FORMAT_DEFAULT` enum, strict option validation, and comprehensive test coverage. The series replaces the previously queued `lo/repo-info-keys` topic and is now ready for integration into `next`.

Junio Hamano confirms the naming choice ("lines" format) feels natural for newline-delimited output. The thread shows consensus among reviewers (Junio, Patrick Steinhardt, Jean-Noël Avila) with no remaining open questions. This represents a well-executed addition to Git's programmatic interface capabilities.