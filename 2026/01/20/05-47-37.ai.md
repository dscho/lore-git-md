This is a follow-up discussion in the ongoing thread about gettext support detection in `git version --build-options`. Patrick Steinhardt acknowledges Junio's position that the version number provides sufficient context to distinguish between disabled gettext in modern Git versus older versions lacking the feature entirely, but maintains a mild preference for explicit "enabled/disabled" reporting for user convenience.

Patrick concedes that while the version number can technically indicate whether gettext detection is available (by comparing against the Git version that introduced this feature), he finds this approach indirect - requiring users to know implementation timelines. He argues that reporting both states would provide unconditional information without significant downside, though he explicitly states he won't push for changes given Junio's maintainer stance.

The exchange highlights a minor philosophical difference in interface design between explicit state reporting versus minimal output, but doesn't propose any code changes. Patrick's position is framed as a suggestion rather than a blocking concern, indicating the discussion is effectively settled with the current v2 implementation that only shows "gettext: enabled" when present. The thread appears to conclude with this being an acceptable resolution, leaving the broader question of feature detection consistency (with Rust/libcurl reporting) as a potential future discussion point.