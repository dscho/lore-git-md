This is a final review comment on a performance optimization patch for sparse-checkout pattern handling, specifically addressing test formatting issues.

Derrick Stolee points out whitespace inconsistencies in Amisha Chhajed's test cases for the sparse-checkout pattern deduplication. The tests themselves are technically correct and verify the intended behavior (duplicate pattern handling in both file writing and listing), but the indentation mixes spaces and tabs in a way that doesn't match Git's coding style. Stolee suggests using `git rebase --whitespace=fix` to correct this before the patch series can be merged.

The core optimization (switching from O(n^2) to O(n log n) pattern sorting) is already approved and not being questioned here - this is purely about final polish. The thread context shows this patch has gone through six iterations with thorough review from Jeff King and others, addressing all technical concerns about duplicate handling and performance implications. Stolee's comment represents the last remaining issue before Junio can merge the series.