This is the final patch in Patrick Steinhardt's ODB abstraction series, completing the transition to new backend-agnostic object storage APIs. The patch removes the now-unused `for_each_loose_object()` and `for_each_packed_object()` functions after all callers have been converted to use the new `odb_for_each_object()` interface. It also inlines `packfile_store_for_each_object_internal()` since it only has one remaining callsite, simplifying the callback indirection.

The changes demonstrate the success of the ODB abstraction effort - the removal of 96 lines of code across 4 files shows how the new unified API has eliminated redundant iteration logic. The patch maintains all existing behavior while cleaning up the intermediate scaffolding used during the transition. With this change, the series achieves its goal of providing symmetric interface for all storage types (loose, packed, cruft) while reducing code duplication.

Key technical points for future reference:
- Removed functions were the last remaining direct access points to old iteration APIs
- Inlining consolidates packed object iteration logic into `packfile_store_for_each_object()`
- All flag handling (`LOCAL_ONLY`, `PROMISOR_ONLY`, etc.) preserved through the transition
- Error handling paths simplified while maintaining same semantics
- The removal confirms complete adoption of new APIs across core commands (fsck, pack-objects, cat-file, commit-graph, reachable)

This marks the functional completion of the ODB abstraction layer, with only minor documentation cleanup remaining before the series can be integrated. The changes have been thoroughly reviewed by Justin Tobler and Karthik Nayak, with Junio Hamano's approval pending final adjustments.