Part of Patrick Steinhardt's ODB abstraction series, this patch introduces a new packed object iteration function that matches the loose object interface added earlier in the series. The new `packfile_store_for_each_object()` provides a consistent callback interface (using `struct object_info`) across both loose and packed object iteration, which will be used to implement the unified `odb_for_each_object()` in a later patch.

The implementation wraps the existing packed object iteration with a new callback that handles object info population, maintaining the same error handling patterns as the existing code. The patch keeps the old `for_each_packed_object()` function for now (to be removed later in line with the series' incremental approach), while adding the new interface that will form part of the ODB abstraction layer.

This is a well-structured technical patch that follows the established pattern seen in previous parts of the series, with clear documentation and careful attention to maintaining existing behavior while introducing the new abstraction. The changes are focused on the packfile subsystem, touching both packfile.c and packfile.h to expose the new interface.