This is a documentation and implementation patch for the experimental `git-last-modified` command, part of a series standardizing its depth control behavior. The patch focuses on properly documenting the `--max-depth` option and moving its handling into the command's own option parsing.

The changes:
1. Adds comprehensive documentation for `--max-depth` in the man page, explaining its behavior with pathspecs and noting that negative values mean no limit
2. Updates the SYNOPSIS section to include the new option
3. Moves `--max-depth` handling from diff machinery to the command's own `parse_options()`
4. Prepares for a future default behavior change by initializing `max_depth` to -1 (unset)
5. Updates the command's usage string to show all available options including `--max-depth`

The patch maintains all existing functionality while making the depth control more self-contained within the command. It's part of a larger effort to make `git-last-modified`'s behavior more predictable and better documented, particularly around subdirectory traversal. The changes follow Junio C Hamano's earlier suggestions to simplify the implementation by handling command-specific options locally rather than through diff machinery.

This continues the thread's progression toward a more polished and well-documented experimental command, with all major feedback from previous iterations now addressed. The patch appears technically sound and ready for review, completing one of the final pieces needed before this series could be considered for merging.