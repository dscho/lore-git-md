This is a feature patch discussion about adding a `--maximal` option to `git rev-list`, now focusing on naming and interface design concerns raised by Johannes Sixt and Junio C Hamano.

The thread has moved beyond implementation details (which appear solid) to debate the user-facing aspects of the new option. Johannes Sixt strongly objects to the bare `--maximal` name as too generic and potentially confusing, especially given Git's historical preference for intuitive over mathematical terminology. He suggests several alternatives like `--only-maximal`, `--hide-reachable`, and `--range-head-only`. 

Junio weighs in to support making `--maximal` and `--boundary` mutually exclusive options, noting they represent opposite ends of the same reachability concept. The discussion remains constructive but highlights how even technically sound features can face pushback on interface design in Git's conservative ecosystem.

The patch author (Derrick Stolee) will likely need to either defend the mathematical precision of `--maximal` or adopt one of the more descriptive alternatives. This exchange shows how Git's most experienced contributors scrutinize new features for potential user confusion years down the line, even when the underlying implementation is uncontroversial.