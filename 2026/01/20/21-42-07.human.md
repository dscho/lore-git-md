This is a follow-up in the long-running thread about configurable branch comparison in `git status`. The maintainer Junio C Hamano confirms that the implementation is now technically complete, with Jeff King's memory leak fixes properly applied and the v26 patch series queued for merging. 

The thread has progressed from technical implementation through several rounds of review and memory leak fixes, and now only documentation remains before this feature can be merged. The feature allows configuring which branches `git status` compares against (upstream, push, or both) via the new `status.compareBranches` config.

Junio's brief note signals that this is effectively done from an implementation perspective, with the patches passing review and the last known issues resolved. The tone suggests this will likely appear in an upcoming release once the remaining documentation is finalized. This represents the culmination of a multi-month effort to make branch comparison more flexible while maintaining backward compatibility.