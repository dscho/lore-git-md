This is a feature patch thread discussing the proposed `--maximal` option for `git rev-list`, now focusing on interface design concerns raised by Johannes Sixt and Junio C Hamano. The discussion has shifted from implementation details to naming and user experience considerations, with Junio weighing in on the mathematical semantics.

The core issue remains selecting "frontier" commits not reachable from others in the input set, but the debate now centers on command-line ergonomics. Johannes Sixt has proposed several alternative names (`--only-maximal`, `--hide-reachable`, `--range-head-only` etc.) arguing the current `--maximal` is too generic and potentially confusing alongside `git-log`'s existing `--minimal` option. Junio agrees with the incompatibility between `--maximal` and `--boundary`, framing it mathematically as requesting both minimal and maximal elements simultaneously.

Key technical points under discussion include the flag bit allocation (now using 28 of 32 bits) and the behavior when combined with other options like `--first-parent`. The thread shows healthy debate about Git's interface design philosophy - whether to prefer mathematically precise terms (`--maximal`) or more intuitive phrasing (`--range-head-only`). Junio's participation signals this is nearing decision time, with his framing of the boundary/maximal relationship suggesting he's leaning toward treating them as mutually exclusive concepts.

The patch remains well-motivated for use cases like bundle URIs, but the naming debate reflects Git's historical preference for clear, non-technical terminology in user-facing interfaces. The discussion remains constructive, with Johannes Sixt providing concrete alternatives rather than blocking the feature outright. This appears to be the final design hurdle before potential merging.