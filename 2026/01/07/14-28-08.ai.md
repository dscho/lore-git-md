This is a tangential discussion about Rust's text handling in the context of the ongoing `.gitignore` encoding thread. Phillip Wood responds to Collin Funk's concern about Rust programs disregarding system locales, noting that while Rust can support non-UTF-8 encodings, it requires more effort than using the standard UTF-8 String type. He specifically points out the inconvenience of `cargo run` refusing to pass non-UTF-8 arguments even when programs are written to handle them.

The exchange highlights a potential consideration for Git's Rustification effort, though it doesn't directly impact the current `.gitignore` encoding debate. Phillip confirms Git's need to maintain non-UTF-8 encoding support, which aligns with the broader thread's discussion about text handling policies. The comment serves as a note about implementation challenges that may arise if more Rust code is introduced, particularly around locale-sensitive operations, but doesn't propose any changes to the current patch or its approach.