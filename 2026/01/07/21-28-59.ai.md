This is a follow-up to the `git repo info --keys` feature series, addressing final review feedback from Patrick Steinhardt. The patch series adds a `--keys` flag to list available keys programmatically, building on prior work in `lo/repo-struct-z`.

The current email responds to two specific requests from Patrick's review of the need for additional test coverage and consistency between related commands. Lucas agrees to:
1. Add a test case for `--format=default` behavior
2. Verify the last `--format` option wins when multiple are specified (though notes this is already partially tested)
3. Extend the default format support to `git repo structure` for consistency

The exchange shows the series is in its final polishing phase, with all major technical questions resolved in v3. The remaining work is straightforward test and documentation updates before the feature can be integrated. Patrick's re-engagement after a brief stall suggests the review process is back on track, with the feature likely to graduate to `next` soon.