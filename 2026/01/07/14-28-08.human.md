This is a tangential discussion about Rust's handling of non-UTF-8 encodings, sparked by the ongoing thread about `.gitignore` file encoding issues. Phillip Wood responds to Collin Funk's concern about Rust programs often ignoring system locales by noting that while Rust can support non-UTF-8 encodings, it requires more effort than using Rust's standard UTF-8 strings.

The email doesn't directly address the core `.gitignore` encoding debate but provides context about potential implications if Git's Rustification effort progresses. Phillip confirms Git will need to maintain non-UTF-8 support but notes Rust makes this more cumbersome than in C, with specific frustration about `cargo run` not properly passing non-UTF-8 arguments.

This is a side conversation that doesn't advance the main thread's resolution but highlights a relevant consideration for future Rust integration work. The core encoding discussion remains focused on whether to enforce UTF-8 for exclude files or maintain current byte-sequence matching behavior.