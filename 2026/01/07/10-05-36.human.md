This is a technical discussion about implementing auto-filter resolution in Git's promisor remote functionality, part of a larger 9-patch series. Patrick Steinhardt is reviewing Christian Couder's implementation of filter combination logic for the `--filter=auto` feature.

The email focuses on three main technical points in patch 7/9:
1. Suggests using `filter_spec_append_urlencode()` instead of direct URL encoding in the new `list_objects_filter_combine()` function
2. Questions whether `list_objects_filter_resolve_auto()` should BUG() out when given a non-auto filter, rather than silently returning
3. Raises concerns about the handling of `allow_auto` flags during filter resolution, suggesting resolved filters should no longer accept "auto" as a valid value

The discussion builds on previous patches in the series that refactored filter handling and introduced the auto-filter infrastructure. Patrick's review continues his pattern of detailed technical feedback on this series, having previously commented on naming conventions, test coverage, and error handling in earlier patches.

This is a focused technical exchange between two experienced contributors working on a complex feature. The feedback suggests some edge cases and API design questions that need resolution before the patch can be considered complete. The conversation is constructive and grounded in the technical details of the implementation.