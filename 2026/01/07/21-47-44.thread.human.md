This thread appears to be a user mistakenly sending a support question to the development mailing list. The original poster installed Git through the `mingw-w64-x86_64-git-extra` package (likely from MSYS2) and is asking what modifications it makes to their Git installation. 

As this is a packaging-specific question rather than a development discussion about Git itself, it's unlikely to receive responses from core contributors. The Git project doesn't maintain these third-party packages, and such support questions are typically handled through distribution-specific channels rather than the development mailing list. The thread doesn't contain any technical details about problems encountered or patches proposed - it's purely a user support request that was misdirected to the wrong forum.