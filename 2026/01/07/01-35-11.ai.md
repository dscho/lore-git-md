This is a tangential discussion about text encoding handling in Rust programs, sparked by the ongoing thread about `.gitignore` file encoding issues.

Collin Funk raises a concern about Rust programs (including Microsoft's new Edit text editor) potentially not handling non-UTF-8 text properly when system locales specify alternative encodings. He notes that Rust's string handling typically assumes UTF-8, which could cause problems with characters encoded in legacy encodings like ISO-8859-1. The email suggests Git should consider this limitation before introducing more Rust code.

The message connects to the broader thread about text encoding in Git, but shifts focus from the original `.gitignore` discussion to Rust's text handling model. It mentions `OsString` as a potential workaround for non-UTF-8 system strings in Rust, though notes many programs don't use this approach.

This appears to be a side discussion rather than direct engagement with the core encoding debate in the thread. The Rust-specific concerns may become relevant if Git's Rustification effort progresses further, but don't directly address the immediate questions about UTF-16LE handling in exclude files. The comment about Microsoft's Edit editor defaults reinforces brian m. carlson's earlier point about UTF-8 becoming the standard, while introducing new concerns about backward compatibility with legacy encodings.