This is a tangential discussion about Rust's text handling in Git, sparked by the ongoing thread about `.gitignore` file encoding but not directly addressing that patch. brian m. carlson responds to Collin Funk's concerns about Rust programs ignoring system locales, particularly for non-UTF-8 character encodings.

The email clarifies Git's current stance on text encoding: Git generally doesn't convert input encodings unless explicitly configured (like with `working-tree-encoding`), and often doesn't honor LC_ALL for things like commit messages. brian notes the Rust contributors have been careful to avoid assuming all data is UTF-8, though they face challenges with `OsString`'s platform-dependent behavior.

Key technical points include Rust's WTF-8 handling on Windows versus Unix's byte-permissive approach, and the limitations around stable byte encoding conversions in Rust. brian mentions having custom code to handle these conversions across platforms. The response also touches on practical constraints - some C interfaces will likely remain necessary for low-level system access that Rust doesn't fully expose.

This is more of a general policy clarification than a direct contribution to the encoding thread. brian reinforces Git's pragmatic approach to text handling while acknowledging Rust's constraints, suggesting the project will maintain flexibility rather than enforce strict UTF-8 requirements everywhere. The discussion remains relevant to the broader encoding questions but doesn't propose concrete changes to the `.gitignore` handling patch under discussion.