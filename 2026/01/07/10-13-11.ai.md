This is a review comment on patch 4/10 of Patrick Steinhardt's series refactoring packfile storage to track packs per object database source. The patch addresses misleading code in `unuse_one_window()` that handles packfile window unmapping.

Toon Claes responds positively to Patrick's explanation of the historical context - the function originally accepted NULL packfiles but hasn't needed to since 2019, yet still contained confusing conditional logic. Patrick's refactor makes the code more straightforward by having callers pass the object database directly rather than a packfile that was only being used to get at the repository.

The change is a small but meaningful cleanup within the larger architectural shift to source-aware packfile operations. Toon's review confirms the improvement while noting he hadn't considered the edge case Patrick described where two packfiles could theoretically have identical `last_used` timestamps (though this doesn't occur in practice due to strictly increasing counters).

This interaction shows the series receiving careful review from domain experts like Toon, with Patrick providing historical context to justify the changes. The exchange focuses narrowly on making one function's behavior match its actual requirements in the new architecture.