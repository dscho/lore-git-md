This is part of a feature series introducing runtime configuration for submodule gitdir paths via the new `extensions.submodulePathConfig` extension. The series is now in its 7th iteration and has received Junio Hamano's ack for merging after final polish.

In this specific patch (v7 05/11), Adrian Ratiu implements the automatic enabling of the submodule path extension through a new config option. Patrick Steinhardt suggests two improvements: renaming the config from `init.autoSetupSubmodulePathConfig` to `init.defaultSubmodulePathConfig` for consistency with other extension-related settings, and moving the initialization logic from `init_db()` to `initialize_repository_version()` where other extensions are handled. Adrian agrees to both changes for v8.

The patch is part of a mature series that has undergone extensive review, with all major architectural questions settled. The remaining work consists of minor polish items like naming consistency and code organization - the kind of final tweaks that often appear even after a series is acked. The implementation looks solid, with clear motivation and thorough test coverage.