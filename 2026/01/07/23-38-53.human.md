This is a tangential discussion about Rust's text handling in Git project, spun off from the main thread about `.gitignore` file encoding issues. brian m. carlson responds to concerns raised by Collin Funk about Rust's UTF-8 assumptions potentially conflicting with system locales.

The email clarifies Git's current position on text encoding: Git generally doesn't honor system locales for input handling (except in specific cases like `working-tree-encoding`) and maintains this approach even when introducing Rust code. brian notes the Rust contributors have been careful to avoid assuming all data is UTF-8, though they acknowledge some platform-specific challenges with `OsString` handling.

This is more of a philosophical clarification than a technical proposal - it reinforces Git's existing text handling policies while addressing concerns about how Rust's design might interact with them. The discussion remains relevant to the broader encoding questions raised in the main thread, but doesn't directly advance the `.gitignore` encoding solution. brian's response suggests the project will maintain its current encoding-agnostic approach even as Rust code is introduced.