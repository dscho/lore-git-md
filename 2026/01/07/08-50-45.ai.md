Performance optimization patch in Patrick Steinhardt's ODB abstraction series. This is patch 1/7 in v3 of the series, fixing an inconsistency in loose object handling where the `OI_LOOSE` flag wasn't being set in early return paths from `odb_source_loose_read_object_info()`. While no current callers rely on this behavior, the patch proactively ensures the `whence` field is always set to `OI_LOOSE` on success, preventing future bugs.

The change touches `object-file.c`, modifying the quick-check and stat-based paths to consistently populate the `whence` field. Karthik Nayak reviews the patch positively but suggests considering a `goto cleanup` pattern instead of duplicating the `whence` assignment. The patch maintains the existing functionality while making the behavior more predictable for future ODB abstraction work.

This is a low-risk change that improves code consistency in preparation for the larger ODB abstraction effort. The series as a whole (as noted in the thread summary) already shows measurable performance improvements (9% speedup in `git rev-list --disk-usage`) and interface cleanup. This particular patch doesn't affect performance directly but ensures correct behavior for loose object handling.