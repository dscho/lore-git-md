This is part 4 of Patrick Steinhardt's v8 series introducing the `git-history` command, specifically focused on refactoring the replay machinery to support the new functionality. The patch exposes the object ID of the final rewritten commit through a new `final_oid` field in the replay infrastructure.

The change is a straightforward plumbing addition that will be used by the upcoming `git-history` command to support HEAD-only reference updates. The commit message clearly explains the use case and limitations of the new field - it's most reliable when rewriting a single strand of commits, and has some edge cases when dealing with complex branch topologies.

The implementation is clean, with appropriate documentation in the header file about the field's semantics. This appears to be a well-considered preparatory change that doesn't affect existing behavior while enabling the new functionality planned for the history command. The patch fits naturally into the series' progression toward using replay.c as the foundation for history rewriting operations.