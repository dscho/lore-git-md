This is a technical review response from Elijah Newren addressing final refinements in Patrick Steinhardt's `git-history` RFC series (specifically patch 1/8). The email focuses on three key areas:

First, Newren strongly objects to using `-1` as the return code for merge conflicts in the replay logic, arguing it should follow Git's standard convention of `1` for conflicts (like `merge-tree` does) while reserving negative values for system errors. He warns this non-standard choice could cause maintenance headaches.

Second, he suggests simplifying the option validation logic for `--contained` to make its relationship with `--onto` more explicit, proposing a clearer error message. This isn't critical but would improve user experience.

Third, while acknowledging Patrick's reasons for keeping the `merge_conflict` field for future patches, Newren reiterates his concern about the return code handling in the conflict case. The discussion shows the series is in its final polishing stages, with Junio Hamano having previously reviewed the boolean logic simplification that Patrick implemented (using `!onto_name ^ !!*advance_name`).

The email demonstrates the careful attention to detail expected in Git's final review rounds, where even small technical choices about error codes and option validation receive scrutiny to ensure long-term maintainability. Newren's feedback suggests the series is nearly ready, pending these final adjustments to align with Git's established conventions.