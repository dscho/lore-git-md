Here's a human-readable summary of the thread so far:

Manuel Rego Casasnovas has identified a documentation inconsistency in `git shortlog`'s `--committer` option. The man page currently shows two conflicting forms: `--committer` (no value) and `--committer=<pattern>` (implying it accepts a pattern). Testing reveals the implementation only supports the no-value version, rejecting attempts to pass a pattern with an error. 

The thread presents two clear resolution paths: either update the implementation to support pattern matching as the documentation suggests, or align the documentation with the current behavior by removing the pattern-matching variant. This is a straightforward but important issue affecting command-line interface consistency. No maintainer response or proposed patch has appeared yet.