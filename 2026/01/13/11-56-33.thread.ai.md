Thread identity: Bugfix discussion addressing multiple issues in Git's hook subsystem - stdout/stderr separation in pre-push hooks, stdin feeding performance in `feed_pipe`, and potential deadlocks in hook execution. Authored by Chris Darroch with participation from brian m. carlson, Patrick Steinhardt, Adrian Ratiu, Junio C Hamano, Jeff King (Peff), and Kristoffer Haugsbakk.

Core problem: Three interrelated issues in hook execution:
1. Stdout/stderr separation regression causing pre-push hooks to lose separate stream capability
2. Stdin feeding performance issues in `feed_pipe` when not using `ungroup=1`
3. Newly identified deadlock scenario when feeding stdin while reading stderr

Current status: The fixes are being split into three distinct commits for v3:
1. Stdout/stderr separation (validated and ready for client-side hooks)
2. Stdin feeding improvements (now addressing deadlock risk and performance impact)
3. Proper `ungroup` flag initialization (newly identified issue)

Technical details:
- Stdout/stderr solution (ready for client-side):
  * New `stdout_to_stderr` flag in `run_hooks_opt` (defaults to 1)
  * Explicit handling for pre-push hooks in transport.c
  * Comprehensive test coverage in `t/t1800-hook.sh`
- Stdin feeding solution (updated with Jeff King's findings):
  * Demonstrated deadlock scenario when pipe buffer fills up
  * Measured 100ms performance penalty when exceeding 500-line batch size
  * Solution approach now concrete:
    1. Server-side hooks will use proper poll() implementation
    2. Client-side hooks maintain current behavior (non-parallel)
  * Performance impact quantifiable via pre-receive hook test case
- Ungroup flag fix (new):
  * Correct initialization based on historical behavior from commit 857f047e40
  * Will ensure backward compatibility

Open questions: 
1. Final verification of poll() implementation for server-side hooks
2. Whether stderr buffering (to prevent interleaving) is worth losing real-time updates
3. Final verification of `ungroup` flag initialization against historical behavior
4. Performance impact assessment for large ref sets (501+ refs)

Participant positions: Chris Darroch reported initial issues. Adrian Ratiu is implementing fixes, now focusing on poll() implementation for server-side hooks while preserving current behavior for client-side. Jeff King provided critical review with concrete demonstrations of deadlock and performance issues, informing the solution approach. Patrick Steinhardt raised test coverage concerns. Kristoffer Haugsbakk confirmed his separate issue is understood. Junio Hamano provided historical perspective. Consensus exists on separating fixes while maintaining backward compatibility, with stdin feeding approach now resolved to use poll() for server-side hooks.

Related work: Part of ongoing hook subsystem work. The deadlock discussion reveals deeper architectural considerations about hook execution design. Jeff King's reference to commit 857f047e40 provides important historical context. The test coverage added for stdout/stderr serves as foundation. Performance measurements provide concrete data for decision-making. Potential implications for other hook types being considered, particularly around real-time output vs. buffering tradeoffs. Discussion continues to educate contributors about hook execution internals through detailed analysis of edge cases. The solution now distinguishes between server-side (poll()) and client-side (current behavior) approaches.