This is a documentation and implementation consistency issue regarding `git shortlog --committer` behavior. The thread continues discussion about whether to extend the implementation to match the documented pattern-matching form (`--committer=<pattern>`) or clarify the documentation to reflect the current implementation (which only accepts `--committer` without a value).

Junio C Hamano weighs in with technical analysis, noting that while implementing pattern matching is possible, it would create inconsistency with how `git log` handles similar options. He points out the ambiguity in `git shortlog --committer -n` - it could either mean "show commits by committer matching '-n'" or "show commits by committer and limit to n entries". This suggests the current no-value-only implementation may be the more maintainable path forward.

The key technical consideration is that `git shortlog`'s option parsing differs from `git log`'s, making direct behavioral alignment difficult. Junio's analysis leans toward keeping the simpler implementation rather than introducing complex parsing rules to support a pattern-matching variant that might confuse users familiar with `git log`'s behavior.

This moves the discussion toward likely resolving by updating the documentation rather than changing the implementation, though no final decision is stated. The patch would need to clarify that `--committer` is indeed an alias for `--group=committer` without pattern matching capability, removing the documented but unimplemented `<pattern>` variant.