Thread identity: Bugfix for HTTP authentication in `remote-curl.c` affecting `probe_rpc()` requests during clone operations with bearer tokens.

Core problem: When cloning large repositories with blob filters where requests exceed `http.postBuffer`, the `probe_rpc()` call fails with 401 errors because it wasn't including authentication headers despite the main request having successfully authenticated. This breaks bearer token authentication flows for large requests.

Current status: Patch v2 addresses feedback from Patrick Steinhardt on test setup while maintaining the core fix. The change remains minimal (2 lines) with comprehensive test coverage. Patrick has indicated the patch is acceptable as-is but may be rerolled to incorporate suggested test improvements.

Key technical details:
- Fix targets `probe_rpc()` in `remote-curl.c` by adding `http_append_auth_header()`
- Test creates repo with 2000 refs to force large requests
- Configures small `http.postBuffer` to trigger `probe_rpc()`
- Uses `git-credential-msal` for bearer token auth testing
- Verifies both failure case and successful fix
- Patrick suggested improvements: use `test_when_finished` for cleanup and inline test repo creation

Open questions: None remaining - Patrick's review confirms the technical approach is sound. The author may choose to reroll with test improvements.

Participant positions: Patrick Steinhardt has reviewed v2 and confirmed the test fails without the fix. He suggests minor test improvements but indicates the patch is acceptable as-is. His review effectively resolves any open questions about the approach.

Related work: Builds on existing HTTP authentication infrastructure. Similar auth header handling exists elsewhere in the codebase for regular requests. The test uses `git-credential-msal` which is part of Git's credential helper ecosystem.