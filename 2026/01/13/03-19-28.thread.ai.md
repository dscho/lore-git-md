Thread identity: Bugfix for HTTP authentication in `remote-curl.c` affecting `probe_rpc()` requests during clone operations with bearer tokens.

Core problem: When cloning large repositories with blob filters where requests exceed `http.postBuffer`, the `probe_rpc()` call fails with 401 errors due to missing authentication headers despite successful main request authentication, breaking bearer token flows for large requests.

Current status: Patch v4 being prepared by Aaron Plattner incorporating all feedback, now including Jeff King's test portability improvement using `test_seq` format strings instead of `xargs` for ref creation. Technical approach remains approved by Patrick Steinhardt and Junio Hamano. Final adjustments complete with this test refinement and prior presentation tweaks (subject line capitalization, blank line removal). Discussion now extends to applying similar test optimizations in `pack-refs-tests.sh`.

Key technical details:
- Fix adds `http_append_auth_header()` to `probe_rpc()` in `remote-curl.c`
- Test creates repo with 2000 refs using portable `test_seq` format strings (per Jeff King's suggestion) to force large requests
- Configures small `http.postBuffer` to trigger `probe_rpc()`
- Uses `git-credential-msal` for bearer token auth testing
- Verifies both failure case and successful fix
- Patrick's prior test cleanup suggestions remain incorporated
- Test now avoids `xargs` process overhead while maintaining coverage
- Discussion opened about applying `test_seq -f` optimization to `pack-refs-tests.sh`

Open questions: Whether to backport test infrastructure improvements to `pack-refs-tests.sh` as follow-up work. No technical blockers remain for core fix.

Participant positions: Patrick Steinhardt approved technical approach in v2. Junio Hamano provided final presentation feedback. Jeff King contributed test portability refinement now confirmed for inclusion and extended discussion to related test files. Aaron Plattner preparing v4 with all changes and raising broader test optimization question. Consensus on core fix remains strong.

Related work: Builds on HTTP authentication infrastructure. Test improvements relate to ongoing portability discussions. Uses `git-credential-msal` from credential helper ecosystem. Potential follow-on work for test pattern standardization in ref-related tests.