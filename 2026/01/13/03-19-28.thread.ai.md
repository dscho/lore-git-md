Thread identity: Bugfix for HTTP authentication in `remote-curl.c` affecting `probe_rpc()` requests during clone operations with bearer tokens.

Core problem: When cloning large repositories with blob filters where requests exceed `http.postBuffer`, the `probe_rpc()` call fails with 401 errors due to missing authentication headers despite successful main request authentication, breaking bearer token flows for large requests.

Current status: Patch v4 being prepared by Aaron Plattner incorporating all feedback. Technical approach approved by Patrick Steinhardt and Junio Hamano. Final adjustments include presentation tweaks (subject line capitalization, blank line removal) and test portability improvements suggested by Jeff King (using `test_seq` format strings instead of `xargs` for ref creation).

Key technical details:
- Fix adds `http_append_auth_header()` to `probe_rpc()` in `remote-curl.c`
- Test creates repo with 2000 refs (using portable `test_seq` format strings per Peff's suggestion) to force large requests
- Configures small `http.postBuffer` to trigger `probe_rpc()`
- Uses `git-credential-msal` for bearer token auth testing
- Verifies both failure case and successful fix
- Patrick's prior test cleanup suggestions remain incorporated

Open questions: None technical. Only awaiting v4 submission with all agreed refinements.

Participant positions: Patrick Steinhardt approved technical approach in v2. Junio Hamano provided final presentation feedback. Jeff King contributed test portability refinement. Aaron Plattner preparing v4 with all changes. Consensus on core fix remains strong.

Related work: Builds on HTTP authentication infrastructure. Test improvements relate to ongoing portability discussions. Uses `git-credential-msal` from credential helper ecosystem.