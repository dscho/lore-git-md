This is a documentation patch addressing an inconsistency in `git shortlog`'s `--committer` option behavior. Pushkar Singh confirms Junio C Hamano's earlier analysis that there's a mismatch between the documentation (which suggests `--committer` can take a pattern) and the actual implementation (which treats it as a no-value alias for `--group=committer`).

The thread has already established consensus that updating the documentation is preferable to changing the implementation, as adding pattern matching would create parsing ambiguities and misalign with `git log` behavior. Pushkar now plans to prepare a patch to clarify the documentation, making it clear that `--committer` is simply a no-value option that groups by committer.

This is a straightforward fix that aligns with the project's preference for simple, unambiguous interfaces. The patch should remove any mention of pattern matching for `--committer` while maintaining its documented behavior as a grouping option. Given the clear technical rationale and Junio's earlier input, this change is unlikely to be controversial.