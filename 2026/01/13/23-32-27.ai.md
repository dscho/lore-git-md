This is a technical discussion about the MIDX compaction feature in Taylor Blau's multi-pack-index series, specifically addressing Patrick Steinhardt's review comments on patch 16/17. The exchange clarifies several edge cases and implementation details of the new `git multi-pack-index compact` command.

Taylor confirms that compacting a MIDX subrange without `--incremental` completely replaces the MIDX with one covering only the specified packs. He also explains that bitmap ordering during compaction must match the pre-compaction state to avoid breaking reachability bitmaps. The discussion reveals careful attention to edge cases like `from_midx == to_midx` (a no-op) and validates the pack population logic.

Patrick's review catches a documentation issue (premature reference to `--bitmap`) and suggests additional test coverage for invalid input cases. Taylor agrees to add tests for nonsensical ranges and identical from/to arguments. The exchange demonstrates the final polishing phase of this feature, with only minor clarifications and test improvements remaining before the series can be considered complete.