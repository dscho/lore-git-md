This is a documentation and behavior clarification regarding `git shortlog --committer` options. The thread continues discussion about whether the `--committer` option should accept a pattern value (as the documentation suggests) or remain a no-value option (as currently implemented).

Junio C Hamano weighs in on the technical constraints, noting that while it would be possible to modify `git shortlog` to accept `--committer=<pattern>`, it couldn't match the behavior of other commands like `git log` because `git shortlog` treats `--committer` as a standalone option rather than one that takes a separate parameter. He highlights the ambiguity this creates with commands like `git shortlog --committer -n`, which would have to be interpreted differently than in `git log`.

The discussion remains open-ended, suggesting this may require either documentation updates to clarify the actual behavior or code changes to align with the documented behavior. Junio's analysis indicates the latter path would involve non-trivial design decisions to maintain consistency with other Git commands.