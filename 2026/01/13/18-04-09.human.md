Test improvement fixing a potential test oversight. This is a small but correct change to `t/t0100-previous.sh` that ensures the test properly checks the exit code of `git cat-file` rather than potentially masking failures by piping directly to grep. The patch changes the test to first capture the output to a temporary file, then grep that file. This is a common pattern in Git's test suite to ensure command failures aren't silently ignored. The change is straightforward and uncontroversial - it follows established test patterns and clearly improves the test's reliability. No functional changes to Git itself, just a test hygiene improvement.