This is a follow-up to the now-approved `git status` divergence tracking series, addressing Junio Hamano's observation about the advice-display logic. The original 2-patch series (v23) added push tracking information to `git status` output while maintaining existing upstream tracking behavior, with Junio having approved it for merging into `next` with just one minor stylistic observation.

Harald Nordgren responds to Junio's note about the divergence advice check (`show_divergence_advice`) following a different pattern than the push/pull advice checks. Junio had suggested it might be cleaner to incorporate the divergence flag into the existing `BRANCH_MODE_*` flags, though explicitly framed this as an observation rather than a blocking concern. Harald confirms implementing this change, noting it simplifies the code by removing a parameter.

The exchange represents the final polish on a long-running effort (23 iterations) to enhance `git status` tracking information. With this last adjustment made, the series appears fully resolved and ready for wider testing in `next`. The implementation cleanly handles both upstream and push tracking divergence while maintaining all existing behavior and output formats, with thorough test coverage in t6040-tracking-info.sh.