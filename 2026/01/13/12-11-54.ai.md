This is a feature enhancement for `git status` that has already gone through extensive review and is now in its 25th iteration. The series adds push tracking branch comparison to `git status` output, showing both upstream and push relationships when they differ.

The v25 update adds a new test case for the scenario where the upstream branch is ahead but the push branch is up-to-date, ensuring the advice display works correctly in this edge case. It also includes minor test cleanup by renumbering test branches to avoid potential conflicts. The implementation has already been approved by Junio Hamano for merging into `next`.

Key technical details:
- Uses `format_branch_comparison()` helper for consistent formatting
- Handles remapped push refspecs properly
- Shows push branch status on a new line when it differs from upstream
- Maintains all existing behavior including quick mode (`--no-ahead-behind`)
- Includes comprehensive test coverage (285 lines in t6040-tracking-info.sh)

The patch is now fully baked after addressing all feedback, including Phillip Wood's concerns about `push.default` interactions and advice display edge cases. The v25 changes are minimal test improvements, confirming the implementation is robust across all expected usage scenarios.