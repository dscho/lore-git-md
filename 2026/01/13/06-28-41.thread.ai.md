gitk color configuration bugfix for ref/tag drawing, addressing two specific inconsistencies: `tagfgcolor` was unused (all labels used `headfgcolor`) and head outlines were hardcoded black instead of respecting `headoutlinecolor`. The patch modifies the `drawtags` procedure while maintaining backward compatibility for non-head ref colors (black outline, #ddddff fill). Discovered during VSCode extension theme export work.

Johannes Sixt (Hannes) reviewed the technically sound patch but suggested improving the commit message to better document gitk's ref styling system. Key points needing clarification: what remains hardcoded vs configurable, and implications of the change given these settings aren't UI-visible (potentially surprising users). The fix remains narrowly scoped to `drawtags` with no new complexity. Ready for merge once commit message addresses Hannes' documentation feedback about current behavior and change impact.