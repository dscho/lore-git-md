This is a test fix addressing a subtle issue in the test suite. The patch modifies `t/t0100-previous.sh` to properly capture the exit code of `git cat-file` during a merge test. Previously, piping the output directly to `grep` would mask any failure of the `git cat-file` command itself. The change redirects output to a temporary file first, then greps from that file, ensuring test failures will be properly detected if the Git command fails.

The fix is straightforward and clearly motivated - it follows a common test pattern used elsewhere in the suite. The change is small (just 2 lines modified) and focused on improving test reliability without altering any actual Git functionality. The commit message explains the problem and solution concisely.

This appears to be a solid, uncontroversial improvement to the test suite. The change aligns with Git's testing best practices where we typically want to verify both command output and exit codes. No thread context is provided, but this seems like a standalone fix rather than part of a larger series.