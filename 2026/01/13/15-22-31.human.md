This is a documentation bug report regarding inconsistent behavior in `git shortlog`'s `--committer` option. Manuel Rego Casasnovas points out that while the documentation shows both `--committer` (no value) and `--committer=<pattern>` forms, the command currently rejects the pattern-matching version with an "option takes no value" error.

The discrepancy appears between the command's actual behavior (which only accepts the bare `--committer` flag) and its documentation (which suggests pattern matching is possible). The email doesn't propose a specific fix but suggests either implementing the documented pattern-matching functionality or correcting the documentation to match reality.

This seems like a straightforward documentation issue that should be easy to verify and fix. The reporter is using Git 2.51.0 (a future version at the time of this email) and the problem appears in the online docs as well. The patch would likely involve either updating the man page or implementing the missing pattern-matching functionality in the command itself.