This is a follow-up in a thread about a performance regression in pre-push hooks introduced by commit 857f047e ("hook: allow overriding the ungroup option"). Adrian Ratiu confirms this is the same issue previously discussed in another thread and mentions that a v1 fix has already been verified to work with git-lfs. He indicates he's preparing a v2 patch series that will include regression tests, acknowledging the breakage and thanking the reporter for their patience.

The regression causes significant slowdowns (from seconds to over a minute) when pre-push hooks read from stdin, affecting CI/CD workflows. The maintainer (Junio) had previously linked this to prior discussions about hook execution behavior. Adrian's response suggests the fix is actively being worked on and will include test coverage to prevent future regressions. The thread shows coordinated progress between the reporter, maintainer, and patch author to resolve a concrete performance issue impacting real-world hook usage.