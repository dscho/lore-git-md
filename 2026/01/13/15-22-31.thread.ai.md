Git shortlog --committer documentation inconsistency

Thread identity: Documentation issue regarding `git shortlog --committer` option behavior vs. documentation. Originating author Manuel Rego Casasnovas. Thread type: bug report with documentation or implementation fix needed.

Core problem: The `git shortlog` man page lists both `--committer` (no value) and `--committer=<pattern>` (with pattern) as valid options, but the implementation only accepts the no-value form. This creates a user-facing inconsistency between documented and actual behavior.

Current status: Issue identified but no resolution path chosen yet. Two options proposed: (1) implement pattern matching to match documentation, or (2) correct documentation to remove pattern form. Tested with Git 2.51.0 showing current implementation rejects `--committer=foo` with "takes no value" error.

Key technical details: Affects `Documentation/git-shortlog.txt` and potentially `builtin/shortlog.c` if implementation changes are made. The `--committer` option currently filters commits by committer rather than author when used without a value.

Open questions: Which resolution path should be taken - extend implementation or correct docs? Are there backward compatibility concerns with either approach? Does the pattern-matching version have clear use cases that would justify the implementation work?

Participant positions: Manuel Rego Casasnovas identified the issue but has not yet proposed a specific fix direction. No other participants have weighed in yet.