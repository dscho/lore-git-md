This is a feature enhancement for `git status` that has already been approved for merging into `next`. The 2-patch series (now at v25) extends `git status` to show divergence information for both the upstream tracking branch and the push tracking branch when they differ.

The key changes in this final iteration are:
- Added a new test case showing status when upstream is ahead but push branch is up-to-date
- Improved test coverage for remapped push refspecs with clearer branch naming
- Maintains all previous functionality including proper `push.default` handling and advice display

The implementation is now fully baked after 25 iterations of review, with all edge cases identified by reviewers (particularly Phillip Wood) properly addressed. The changes are focused on test coverage refinements rather than core logic changes at this point. 

This represents a clean extension of `git status` output that will help users understand their local state relative to both upstream and push destinations, particularly in workflows where these diverge (e.g., tracking `origin/main` but pushing to a feature branch). The extensive test coverage (now 285 lines in t6040-tracking-info.sh) gives confidence in the implementation.