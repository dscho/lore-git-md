This is a bugfix discussion thread about hook output handling, specifically addressing a regression in the pre-push hook's behavior. Junio C Hamano confirms through historical analysis that pre-push was indeed the only hook that previously didn't redirect stdout to stderr by default, validating the proposed fix.

The patch in question makes the stdout-to-stderr redirection configurable while maintaining backward compatibility for pre-push hooks. Junio notes the current patch title ("make stdout_to_stderr optional") could be clearer since the default is actually "on" - he suggests alternatives like "allow stdout_to_stderr optionally off" to better reflect the actual behavior.

This marks the maintainer's approval of the technical approach after verifying the historical context. The discussion has now shifted to final polish details like commit message wording, indicating the substantive technical discussion is complete and the patch is likely to be merged soon. The thread demonstrates Git's careful attention to backward compatibility even when fixing regressions.