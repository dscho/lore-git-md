This is a technical review response from Elijah Newren addressing final polish issues in Patrick Steinhardt's `git replay` series (specifically patch 1/8 of v10 iteration). The email focuses on three main points:

First, Newren strongly objects to using `-1` as the return code for merge conflicts in the replay logic, noting this contradicts Git's established convention where `1` signals conflicts and `-1` indicates unexpected errors. He points to `builtin/merge-tree.c` as precedent and warns this inconsistency could cause maintenance headaches.

Second, he suggests simplifying the option validation logic for `--contained` to make its relationship with `--onto` more explicit, proposing a clearer error message. This isn't critical but would improve user experience.

Third, while acknowledging Patrick's desire to keep the `merge_conflict` field for future patches, Newren reiterates his preference for introducing such fields only when actually needed. His tone is collegial but firm on the return code issue, framing it as an important convention to maintain.

The review represents final polishing touches on an otherwise technically complete series, with Newren acting as a domain expert ensuring the merge machinery behaves consistently with Git's established patterns. His feedback is narrowly focused on implementation details rather than architectural concerns.