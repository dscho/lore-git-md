This is a feature patch series (v24) that enhances `git status` to show divergence information for both upstream and push tracking branches when they differ. The series has already been approved by Junio C Hamano for merging into `next` after 24 iterations of review.

The first patch is a refactoring that extracts the branch comparison formatting logic into a helper function. The second patch implements the core functionality - when a branch's push target differs from its upstream, `git status` will now show both comparisons. The implementation handles remapped push refspecs and maintains all existing behavior including the quick mode (`--no-ahead-behind`).

Key changes in v24 include:
- Renamed flag constants for clarity (BRANCH_MODE_* -> ENABLE_ADVICE_*)
- Simplified advice display logic by moving divergence flag into the flags parameter
- Cleaned up push reference resolution code
- Maintained all test coverage (262 lines in t6040-tracking-info.sh)

The series represents a complete feature that cleanly extends `git status` output while maintaining backward compatibility. All major technical requirements have been met and all feedback from previous rounds has been incorporated. The remaining open question about `push.default` interaction can be addressed in follow-up work.