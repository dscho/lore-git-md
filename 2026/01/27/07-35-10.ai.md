This is a review comment on patch 17/18 in Taylor Blau's MIDX compaction series, focusing on version compatibility checks. Patrick Steinhardt notes that the patch adds sensible validation for MIDX compaction endpoints but questions a redundant error check in the implementation.

The patch introduces several new validations for MIDX compaction operations: ensuring source and target MIDX files exist, preventing self-compaction, and verifying the ancestry relationship between MIDX layers. Patrick approves these checks as reasonable."

A key point of discussion is version compatibility - the patch enforces that compaction requires MIDX v2 format, with explicit error if v1 is configured. Patrick observes that this means users with midx.version=1 cannot compact, which appears intentional based on the implementation.

Patrick flags what may be a redundant error check - the same version validation appears both in an early die() call and later BUG() assertion. This suggests either the BUG() is unnecessary or the validation logic could be consolidated. The thread context shows this is part of ongoing refinement of the version handling implementation that Patrick has been reviewing throughout the series.

The exchange continues the pattern of Patrick providing detailed technical review of Taylor's MIDX work, with this particular comment focusing on error handling consistency rather than the core compaction functionality. The thread shows the series is in late-stage review with maintainer approval on major components, now addressing final implementation details.