This is a review comment on a patch in the `git repo info --keys` feature series, focusing on naming consistency for output formats. Patrick Steinhardt provides detailed feedback on Lucas Seiki Oshiro's v5 patch that renames the "keyvalue" format to "lines" across the `git repo` subcommands.

Patrick suggests a more thorough commit message explaining why "keyvalue" was problematic (it doesn't indicate newline-delimited nature, is redundant with "nul" format, and may need to handle non-key-value output later). He proposes alternative wording that better justifies the change while noting the experimental status of the command makes this rename acceptable.

On the technical side, Patrick observes the new `FORMAT_LINES` enum value looks unbalanced next to `FORMAT_NUL_TERMINATED` and suggests `FORMAT_NEWLINE_TERMINATED` might be more symmetric. However, he explicitly states he doesn't feel strongly about this naming point, leaving the final decision to the patch author.

The review maintains the thread's technical focus on format naming consistency while providing concrete suggestions for improvement. Patrick's feedback is constructive and specific, offering both rationale and optional implementation tweaks without blocking the patch's progress. This follows the established pattern in this thread where naming details receive careful attention before final integration.