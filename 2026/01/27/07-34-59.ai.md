This is a review comment on a patch in Taylor Blau's multi-pack-index compaction series, specifically addressing version compatibility between MIDX v1 and v2 formats. Patrick Steinhardt provides feedback on patch 10/18, which removes the requirement for packs to be lex-sorted in MIDX v2 while maintaining backward compatibility.

The key technical points discussed are the version handling implementation in `midx-write.c`, where Patrick notes the version validation could be made more explicit by using an enum type for the version field. He also examines the pack name comparison logic, suggesting alternative approaches like using sorted pointer arrays or lazy sorting, though acknowledging the trade-offs in ownership semantics and implementation complexity.

Patrick's review focuses on code clarity and maintainability rather than functional correctness, as the core version compatibility approach (using configurable MIDX_VERSION_V1/V2) appears sound. The discussion continues the thread's pattern of refining implementation details while maintaining the series' overall goal of improving MIDX scalability through compaction. This patch represents an important transitional step enabling the subsequent bitmap support work while preserving compatibility with older tools.