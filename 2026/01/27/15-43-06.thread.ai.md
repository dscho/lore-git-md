Thread identity: Feature patch from Abraham Samuel Adekunle enhancing `git add -p` interface navigation between files (v2).

Core problem/goal: Improve file navigation in interactive staging by adding context-sensitive '<' and '>' options that appear only when moving between files is possible (not shown for single file, only '<' at first file, both in middle files), while fixing undocumented 'q' submission behavior.

Current status: Second version of patch focusing on `add-patch.c` (102 insertions, 37 deletions) with new navigation options and state management. Patch application now happens after all file navigation completes rather than per-file. Test updates in t/3701-interactive.sh still pending UI consensus.

Key technical details:
- Adds dynamic '<' and '>' navigation in "All hunks decided" state
- Restructures patch application timing to post-navigation
- Fixes undocumented 'q' submitting all decisions when hunks decided
- Modifies `add-patch.c` state handling and UI flow
- Maintains current workflow until all hunks decided, then shows navigation

Open questions/loose ends:
- Test coverage needs implementation after UI finalization
- Potential for further UI refinements based on feedback

Participant positions: Currently only author Abraham Samuel Adekunle has participated in v2 discussion. Previous round feedback not reflected in current implementation.

Implementation notes: Quality-of-life improvement focused on interactive staging workflow, preserving existing architecture while adding intuitive navigation. Changes are well-contained to `add-patch.c` with clear behavioral modifications documented in commit message.