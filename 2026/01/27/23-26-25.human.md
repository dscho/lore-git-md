This is a bugfix thread addressing tab alignment inconsistencies in git-gui's diff viewer on Windows with Tcl/Tk 8.6. The discussion has evolved through several iterations, with Junio C Hamano now raising deeper technical concerns about the proposed solution.

Junio points out a potential inconsistency in how tab sizes are currently handled between regular diffs (single prefix column) and combined diffs (two prefix columns). The existing code uses `apply_tab_size 1` for combined diffs, while the new patch proposes using the same setting for regular diffs. Junio questions whether this uniform approach can be correct for both contexts, suggesting there may be an off-by-one error lurking in the display logic.

The thread reveals this behavior dates back to a 2012 commit by Michael Lutz that added configurable tab sizes, modeled after gitk's implementation. Junio CCs the original authors in case they can provide historical context, while noting the patch series needs to either justify why the 14-year-old behavior is wrong or adjust its approach to handle different diff contexts separately.

The core challenge remains: fix the tab alignment without breaking the "Stage Line/Hunk for Commit" functionality, while now also accounting for these newly identified edge cases in combined diff handling. The solution appears technically sound but requires refinement to address these subtle interactions before final approval.