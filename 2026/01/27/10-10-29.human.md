This is part 10 of a 12-patch series refactoring Git's hook subsystem to support parallel execution. The patch focuses on improving I/O handling in the parallel execution case by polling both stdin and stdout file descriptors simultaneously, eliminating previous 100ms delays.

The discussion here is finalizing the naming of a helper function that checks whether a child process is sending output (via stderr). Adrian Ratiu proposes `child_is_sending_output()` as the name, explaining that it matches existing naming conventions and accurately reflects the behavior since all parallel hooks redirect stdout to stderr. Junio Hamano had jokingly suggested `child_is_spewing_error()`, but Adrian's proposal is more technically precise.

This is a minor but important polish to the parallel execution infrastructure. The patch series as a whole is in its final stages, with all major technical work completed and maintainer approval secured. This particular naming question appears to be one of the last remaining details before the series can be merged. The tone is collegial, with the discussion focused on finding the clearest possible name for future maintainers.