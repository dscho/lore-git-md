Junio C Hamano has improved the advice messages shown when `git switch` encounters ambiguous remote branch names. The two-patch series first refactored the underlying code to track which command (`checkout`, `switch`, or `restore`) triggered the operation, then updated the advice to match - now suggesting `git switch --track` when appropriate rather than always recommending `git checkout --track`. 

The changes make Git's behavior more consistent by ensuring the advice matches the command actually used. The implementation is cleanly split between infrastructure changes (adding an enum to track the command type) and the user-visible improvement (updating the advice text). Thorough tests verify both the refactoring maintains existing behavior and the new advice appears correctly. This small but thoughtful enhancement improves the user experience when dealing with ambiguous branch names.