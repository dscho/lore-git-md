This is a review comment on patch 17/18 in Taylor Blau's series implementing MIDX compaction functionality. Patrick Steinhardt is providing feedback on the implementation details of version compatibility checks in the multi-pack-index compaction code.

Patrick notes that the patch adds sensible validation checks for MIDX compaction endpoints, ensuring they exist and form a proper ancestor chain. He then focuses on the version compatibility handling, observing that the patch correctly prevents compaction when using the older v1 format (which requires lex-sorted packs).

His main technical observation is that there appears to be redundant error handling - both a `die()` and a `BUG()` check for the same v1 format incompatibility condition. This suggests the error checking could be simplified by removing one of the duplicate checks. The comment is brief and to the point, typical of Patrick's thorough but concise review style.

The exchange shows the series is in its final refinement stages, with reviewers like Patrick examining subtle implementation details after the core functionality has been established. This patch builds on earlier discussions about MIDX version handling that were summarized in previous thread updates.