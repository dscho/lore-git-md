This is a bugfix for git-gui that aligns its tab display behavior with gitk's longstanding implementation, addressing a nearly two-decade inconsistency in how tabs are rendered in diff views. The patch modifies the tabstop positioning in git-gui's diff viewer to account for the first column containing diff markers ('+', '-', or space), ensuring text alignment matches what users see in their editors.

The technical solution applies Tk's `apply_tab_size` configuration with different offsets: 1 for regular diffs and 2 for combined diffs, while preserving the existing behavior for untracked files (offset 0). Johannes Sixt has rewritten the commit message to better explain the context and motivation, noting this isn't strictly a bugfix but rather an improvement to match gitk's behavior since 2007. The change is minimal (just 2 lines added to `diff.tcl`) but addresses a subtle workflow issue where tab alignment would break when diff markers pushed text past tab stops.

All technical questions have been resolved in the thread, with Junio Hamano confirming the historical context and Johannes Sixt providing final documentation guidance. The patch is now merge-ready as a routine improvement that brings git-gui's behavior in line with gitk's established precedent while maintaining all staging functionality.