This is a technical discussion about object database (ODB) transaction handling, part of the ongoing ODB abstraction effort. Patrick Steinhardt is responding to Justin Tobler's patch that changes `struct odb_transaction` to store an ODB source reference rather than a direct object database reference.

Patrick agrees with the change's direction but raises a subtle architectural point about the `tmp_objdir` subsystem. He notes that while the patch correctly updates the transaction structure to reference the source, the underlying `tmp_objdir_create()` call still takes a repository parameter rather than a source parameter. This creates a conceptual inconsistency since the transaction is meant to operate at the source level.

The suggestion is to first refactor `tmp_objdir_create()` to accept a source parameter before proceeding with the transaction changes. Patrick acknowledges this wouldn't change current behavior (since only the primary source writes objects today) but sees it as a cleaner architectural step. He notes this should be straightforward given there are only three call sites to update.

This is a focused technical discussion between two key contributors to the ODB abstraction work, addressing a subtle architectural alignment issue in the transaction handling code. The exchange shows the careful attention to design consistency that characterizes this long-running refactoring effort.