This is a review comment from Junio C Hamano on Shreyansh Paliwal's patch to make `git show-index` warn when falling back to SHA-1 outside a repository. The patch is part of an ongoing discussion about hash algorithm detection in the `show-index` command.

Junio's feedback focuses on patch organization rather than the technical merits. He notes that while the core change (adding a warning when falling back to SHA-1) is small (just 3 lines), it's buried in a larger patch that also internationalizes error messages with `_()` wrappers. He suggests splitting this into two patches - one focused purely on the warning behavior, and another handling the i18n changes - to make the purpose of each change clearer.

The review also gently chides the author for using an unusually narrow 50-column width in the commit message, recommending the standard ~70 columns to allow for quoting in email threads. This is typical of Junio's attention to both technical and presentation details in patches.

This email doesn't introduce new technical discussion about the hash algorithm detection approach itself (which was debated in earlier messages), but rather provides maintainer guidance on how to structure the changes for clean integration. The suggested split would make it easier to review the warning behavior change in isolation from the mechanical i18n updates.