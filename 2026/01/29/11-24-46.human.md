This is a response to a feature proposal about `git config --get-regexp`'s case-insensitive matching behavior. Jeff King (Peff) explains why the suggested approach of using `REG_ICASE` wouldn't work correctly due to Git's nuanced case sensitivity rules for config keys.

Peff clarifies that while most of a config key is case-insensitive, the subsection (middle part between dots) remains case-sensitive. This means the current implementation of selectively lowercasing parts of the regex pattern is actually the correct approach, despite its limitations with complex patterns. He notes this behavior is documented canonicalization.

The response suggests that rather than changing the implementation, the status quo might be preferable, as the current behavior helps match common usage patterns (like `fetch.unpackLimit`) while maintaining subsection case sensitivity. Peff acknowledges the current approach could theoretically mangle some regex patterns but notes this hasn't been a practical problem.

This appears to be a thoughtful rejection of the proposed direction, with detailed technical rationale about Git's config key semantics. The thread now seems settled unless the original author has counterarguments about edge cases or documentation improvements.