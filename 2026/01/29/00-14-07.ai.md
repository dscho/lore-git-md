This is a feature patch finalizing the `--maximal-only` option for `git rev-list`, with Junio C Hamano confirming the long-term direction and naming consensus. The thread has reached full technical agreement on the implementation, which selects "frontier" commits (maximal in reachability order) while supporting negative revisions - a capability that distinguishes it from the existing `--independent` flag in `git merge-base` and `git show-branch`.

Junio explicitly endorses the `--maximal-only` name as appropriate for this superset functionality, noting it should eventually replace `--independent` across Git commands. The current patch focuses on `rev-list` integration while leaving door open for future extensions to `git show` and others. Documentation will need updates to guide users migrating from `--independent`, but those can come later.

The technical details remain unchanged from v2: CHILD_VISITED bit usage, `--boundary` incompatibility, and comprehensive test coverage. With Junio's final approval of both the immediate implementation and long-term vision, this thread concludes with no outstanding issues. The patch is ready for merging, establishing `--maximal-only` as the canonical way to query independent commits while supporting the bundle URI and bitmap optimization use cases that motivated it.