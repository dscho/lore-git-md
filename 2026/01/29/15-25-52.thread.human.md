This thread explores adding a shorthand syntax to reference a repository's primary branch, with Harald Nordgren's implementation evolving from `@{default}` to `@{primary}` across three versions. The latest approach resolves the branch via upstream tracking (typically `origin/HEAD`), addressing Junio Hamano's earlier concerns about config reliability while introducing new questions about semantics.

Junio's latest response highlights a disconnect between the original goal (a shorthand for `init.defaultBranch`) and the current implementation (which tracks remote HEAD). He notes that if the feature now represents something distinct from the default branch, the "primary" terminology might be appropriate - but questions whether this solves the originally stated problem. The discussion has shifted from implementation details to fundamental design questions about what "primary branch" should mean in Git's reference system.

Technical implementation appears sound, with tests showing the shorthand works in `rev-parse`, `checkout`, and `status` contexts. However, open questions remain about whether remote-tracking behavior matches user expectations across all workflows, especially in repositories without clear upstreams or with multiple primary branches. The thread reflects an ongoing tension between convenience and semantic precision, with Junio suggesting the discussion needs to revisit the feature's core purpose before finalizing. Kristoffer Haugsbakk's earlier observation about `git var GIT_DEFAULT_BRANCH` providing similar functionality remains an unresolved counterpoint in the conversation.