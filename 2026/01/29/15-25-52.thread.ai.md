Thread introduces shorthand syntax for referencing a repository's primary branch, now in its fourth iteration with naming shifting between `@{primary}` and `@{default}`. Harald Nordgren's implementation resolves to the remote-tracking branch pointed by `refs/remotes/origin/HEAD`, reusing branch tracking infrastructure with parsing logic in object-name.c. The v4 approach differs from earlier local `init.defaultBranch`-based versions, creating tension between:

1. Semantic precision of "primary"/"default" terminology versus implementation behavior
2. Remote-tracking (origin/HEAD) resolution versus local configuration
3. Original use case (local branch switching) versus current remote-centric approach

Junio Hamano raises fundamental concerns about:
- Nomenclature mismatch between `@{default}` name and remote-tracking behavior
- Confusion with existing `init.defaultBranch` and `git var GIT_DEFAULT_BRANCH` mechanisms
- Whether remote-HEAD resolution adequately represents "primary" across repository types
- Original motivation alignment given the implementation pivot

Technical implementation remains sound with expanding test coverage (rev-parse, checkout, status). Documentation describes resolution as "the primary branch via upstream tracking." Kristoffer Haugsbakk's earlier observation about `git var` overlap remains unresolved.

Key open questions:
- Whether to maintain remote-tracking approach or revisit local configuration via `repo_default_branch_name()`
- Clearer definition of "primary branch" semantics in workflows without clear upstreams
- Potential need for fallback mechanisms when origin/HEAD is absent
- Naming resolution (`@{default}` vs alternative like `@{dumbo}`) to avoid implying local config relation

Junio's persistent questioning indicates the feature requires stronger conceptual foundation regarding:
- Relationship to existing branch-naming mechanisms
- Precise definition of what problem it solves
- How naming reflects actual behavior
- Whether remote-tracking resolution meets original local workflow needs

The discussion has shifted from implementation refinement to re-examining first principles, with Junio's workflow scenario highlighting potential disconnects between the shorthand's design and practical utility.