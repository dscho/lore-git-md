New AI thread summary:

This thread introduces a new `@{default}` shorthand for referring to a repository's default branch (typically "main" or "master"), following the pattern of existing shorthands like `@{upstream}` and `@{push}`. The implementation adds `branch_get_default_ref()` which resolves the default branch by checking `init.defaultBranch` configuration, falling back to "main" (or "master" in older Git versions). The shorthand is integrated into revision parsing alongside existing branch mark handlers. Documentation and tests are updated to explain and verify the behavior. The motivation is to provide a consistent way to reference the default branch without hardcoding names, particularly useful when working across repositories with different default branch names. The implementation appears well-contained, touching revision parsing, remote/branch handling, documentation, and test cases. Tests cover basic functionality and integration with existing features. This is a quality-of-life improvement aligning with Git's existing branch reference conventions. No reviews or objections have been raised yet in this initial patch submission.