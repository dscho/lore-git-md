Refactoring of path normalization logic. Pushkar Singh restructures `normalize_path_copy_len()` in `path.c` by extracting three helper functions: `skip_slashes()`, `handle_dot_component()`, and `strip_last_component()`. The change is purely mechanical - the behavior remains identical as verified by the existing test suite (t0060-path-utils.sh). This makes the control flow more modular and easier to follow while maintaining all current functionality for path normalization, including handling of dot components (`./` and `../`) and trailing slashes.

The patch appears well-contained and focused scope, touching only the path normalization logic without introducing any behavioral changes. The commit message clearly states the intent and verification method. This seems like a straightforward code hygiene improvement that should be uncontroversial to merge.