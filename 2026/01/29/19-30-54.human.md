This is a technical discussion about the ongoing ODB (object database) abstraction effort, specifically around transaction handling for pluggable backends. Justin Tobler is responding to Patrick Steinhardt's suggestion about refactoring the `tmp_objdir` subsystem to better align with the ODB source model.

The exchange focuses on a subtle architectural point: while the current patch makes transactions aware of their ODB source, the underlying `tmp_objdir_create()` still operates at the repository level rather than the source level. Patrick notes this creates a conceptual inconsistency, even though it doesn't affect current behavior since only the primary object source is used. Justin agrees and plans to address this in the next version of the patch series with a preparatory commit to refactor `tmp_objdir_create()`.

This is a small but meaningful refinement to the larger ODB abstraction work, showing the careful attention to architectural consistency that characterizes this effort. The discussion remains focused and technical, with both contributors aligned on the direction. The change will help future-proof the code for when non-primary object sources are implemented.