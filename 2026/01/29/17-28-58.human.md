Bugfix for bisect ref handling. This is the final patch in a series addressing misuse of `refs_for_each_ref_in()` that could trigger a BUG() when processing exact ref names. The patch simplifies bisect's ref cleanup code by using `refs_for_each_fullref_in()` to avoid manual string manipulation.

Jeff King follows up with a cleanup to the string_list memory management, switching from NODUP to DUP mode now that ref names come directly from iteration (via xstrdup). This removes redundant string duplication while maintaining identical functionality. Junio C Hamano approves the changes, noting they improve code clarity.

The changes are uncontroversial and build on earlier approvals from Karthik Nayak. This concludes the technical discussion of the series, with only minor clarifications to documentation remaining as noted in Junio's earlier response. The bisect-specific changes require no test updates as existing coverage remains valid.