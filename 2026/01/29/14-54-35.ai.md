This is a refactoring patch targeting Git's path normalization logic. Pushkar Singh restructured `normalize_path_copy_len()` in `path.c` by extracting three helper functions: `skip_slashes()`, `handle_dot_component()`, and `strip_last_component()`. The changes are purely mechanical - the commit message and test results confirm behavior remains identical to before.

The patch breaks down the original function's nested conditionals for handling dot components (`./` and `../`) into more focused helper functions. This makes the control flow in the main function more linear and easier to follow. The `skip_slashes()` helper handles consecutive slashes, `handle_dot_component()` processes `.` and `..` sequences, while `strip_last_component()` manages the path traversal when handling `..` components.

The changes touch only implementation details - no new functionality is added and no existing behavior changes. The patch includes test coverage note indicating the existing `t0060-path-utils.sh` tests verify the refactored code works as before. This appears to be a straightforward code hygiene improvement with low risk of introducing regressions.