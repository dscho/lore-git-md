This is a feature patch finalizing the `--maximal-only` option for `git rev-list`, with Junio C Hamano confirming the conceptual alignment and future direction. The option selects "frontier" commits (maximal in reachability order) for use cases like bundle URIs and bitmap optimization, serving as a superset of the existing `--independent` functionality from `git merge-base` and `git show-branch`.

Junio agrees the current implementation in `rev-list` is the right home for this feature, noting it properly handles both positive and negative revisions where the older `--independent` variants do not. The naming (`--maximal-only`) is now settled, and the thread has reached consensus that this could eventually replace `--independent` in other commands, though that would be future work. The implementation uses bit 28 in the revision walking infrastructure and includes comprehensive tests covering edge cases and interactions with options like `--first-parent`.

The discussion is effectively concluded with no outstanding technical questions. Junio's final email emphasizes that while extensions to commands like `git show` would be valuable, they can wait for a separate series. The key takeaway is that `--maximal-only` is now ready to merge as a more flexible alternative to `--independent`, with clear documentation of its behavior and relationship to the older feature.