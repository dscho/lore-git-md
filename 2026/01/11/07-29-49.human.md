Test modernization patch introducing a helper function for post-checkout hook testing in t5403. This is the first of two patches in a series that continues recent work to improve test robustness by replacing shell tests with helper functions.

The patch adds a `check_post_checkout` helper that encapsulates the repeated pattern of validating post-checkout hook arguments (old SHA, new SHA, and flag). The helper reads the arguments from a file and compares them against expected values, replacing 21 lines of repetitive test code with cleaner function calls. No production code behavior is changed - this is purely a test infrastructure improvement.

The changes follow established patterns seen in other test modernization efforts, making the tests more maintainable and providing better failure diagnostics by using proper test assertions rather than raw shell comparisons. The helper includes a BUG check to ensure it's called with the correct number of arguments, adding an extra layer of validation.

This appears to be a straightforward, well-scoped improvement to the test suite. The patch follows the project's style for test modernization and builds logically on prior work in this test script. The commit message clearly explains the motivation and scope.