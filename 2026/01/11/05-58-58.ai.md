This is a fixup patch in the ongoing `git-history` command series, addressing final technical and documentation issues raised in review. Elijah Newren provides several targeted improvements to the v9 series:

1. **Documentation fix**: Corrects a typo ("decendants" -> "descendants") in the man page.

2. **Code organization**: 
   - Simplifies `handle_reference_updates()` in `builtin/history.c` by removing redundant `final_oid` handling and unifying HEAD/branch update paths
   - Cleans up error handling in `builtin/replay.c`

3. **Critical bugfix**: 
   - Fixes detached HEAD handling that affected both `git-history` and `git-replay`
   - Adds proper error code standardization (returning 1 for conflicts)

4. **Header cleanup**: 
   - Removes unused `merge_conflict` flag and questionable `final_oid` field from `replay_result` struct
   - Drops unnecessary repository parameter from `replay_revisions()`
   - Prunes unused includes while adding needed `strmap.h`

The changes demonstrate careful attention to edge cases (like detached HEAD) while streamlining the internal APIs. The removal of `final_oid` field resolves Newren's last open question from review, suggesting this may be the final technical iteration before merging. These fixups should be squashed into their respective patches in the v9 series.