This is a UI improvement for `git add -p`, part of a GSoC project that has gone through 7 iterations. The patch adds visibility to previous hunk decisions by showing "(was: y)" or "(was: n)" in the prompt when navigating between hunks during interactive staging.

The change is well-contained in `add-patch.c` and comes with test updates in `t/t3701-add-interactive.sh`. The implementation tracks decisions via a new `hunk_use_decision` string while maintaining the existing hunk selection logic. Junio C Hamano originally suggested this approach and has now given preliminary approval, suggesting it's nearly ready for inclusion in the 'next' branch after a brief waiting period for any final feedback.

The patch appears to have addressed all technical concerns through its 7 revisions, with v7 specifically focusing on prompt width optimization and readability improvements. This looks like a clean, focused improvement to a common workflow that should make interactive staging more user-friendly.