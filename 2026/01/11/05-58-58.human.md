This is a fixup patch in the ongoing `git-history` command series, addressing final review feedback from Elijah Newren. The patch makes several targeted improvements to the v9 series:

1. Fixes a typo ("decendants" -> "descendants") in the documentation
2. Simplifies the replay result handling by removing the `final_oid` field that Newren questioned as potentially unnecessary
3. Improves detached HEAD handling by using "HEAD" directly rather than resolving its OID
4. Cleans up header file dependencies in the replay machinery
5. Standardizes error code returns between `git-replay` and `git-history`

The changes are focused on code quality and correctness rather than adding new functionality. Newren's patch demonstrates the typical Git workflow where reviewers provide fixup suggestions that get squashed into the original patches before final merging. The series appears to be in its final polishing stage, with this addressing the last substantive technical issues raised in review.