This is a UI improvement for `git add -p` that appears ready for merging after 7 iterations of refinement. The patch adds visual feedback showing whether each hunk was previously selected ('y') or skipped ('n') when navigating between hunks during interactive staging, addressing a usability issue originally reported by Junio C Hamano.

The implementation is clean - it modifies prompt strings in `add-patch.c` to append "(was: y/n)" indicators while preserving the existing interface. Version 7 tightens the implementation by reducing prompt width, removing redundant checks, and improving readability. The changes are well-contained with passing tests in `t3701-add-interactive.sh` that verify both regular and colored output formats.

As a GSoC project guided by Junio, this represents a successful example of mentored contribution. The feature is small but useful, the implementation is solid, and the thread shows careful iteration based on review feedback. Junio's latest response suggests it will likely be merged to 'next' branch soon barring any last-minute objections. The patch touches only the interactive add interface and doesn't affect other commands or require configuration.