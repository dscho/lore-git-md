This is a fixup patch series addressing technical issues in Patrick Steinhardt's `git-history` command proposal, specifically targeting bugfixes for detached HEAD handling and code cleanup. Elijah Newren provides two patches to be squashed into the main series:

1. A general cleanup patch fixing documentation typos, code organization in `builtin/history.c`, and removing redundant fields from the `replay_result` struct. It also improves error code consistency and simplifies reference handling.

2. A critical fix for detached HEAD handling that affects both the new `git-history` command and the existing `git-replay` infrastructure. The patch adds proper HEAD inclusion in revision walks, decoration type checks to prevent duplicate updates, and detached state tracking in `replay.c`. It includes 47 lines of new test coverage in `t/t3451-history-reword.sh` to verify the fix works in two detached HEAD scenarios.

The changes demonstrate careful attention to edge cases while streamlining internal APIs. The detached HEAD support in particular closes a functional gap in the shared replay infrastructure. These patches represent the final technical refinements needed before the series, with all major review feedback now addressed.