This is a follow-up to Patrick Steinhardt's `git-history` RFC series, providing two fixup patches from Elijah Newren. The patches address both documentation/code organization issues in the series and a pre-existing bug in detached HEAD handling that affects both `git-replay` and the new `git-history` command.

The first patch contains minor documentation and code fixes that Elijah suggested in earlier reviews, now formatted as a "SQUASH ME" patch for Patrick to incorporate. The second patch fixes a bug where detached HEAD states weren't properly handled in the replay machinery, which affects both the existing `git-replay` and the new `git-history` command. The fix adds test coverage in `t/t3451-history-reword.sh` to verify proper behavior.

Key technical details:
- Fixes sentence fragments and typos in `Documentation/git-history.adoc`
- Improves code organization in `builtin/history.c` and `replay.c`
- Addresses a 47-line gap in test coverage for detached HEAD handling
- Modifies replay logic to properly handle detached HEAD states
- Reduces unnecessary header file dependencies in `replay.h`

The patches represent final polishing for the series, with Elijah indicating these were the last outstanding issues he had noted in his reviews. The detached HEAD fix is particularly important as it corrects behavior in both the new command and existing replay functionality. The changes are relatively small and focused, suggesting the series is nearing readiness for merging after these adjustments.