This is a test modernization patch that updates t1420-lost-found.sh to use Git's test helper functions instead of raw shell commands. The change replaces direct filesystem checks (`test -f`) and line counting (`ls | wc -l`) with the more robust `test_path_is_file` and `test_line_count` helpers.

The patch is part of an ongoing effort to standardize the test suite by using Git's test helper functions, which provide better error messages when tests fail. The changes are straightforward and focused - just updating three test assertions while maintaining the same verification logic.

The commit message clearly explains the change and its benefits (better diagnostics and consistency), and the patch itself is small and well-contained. This appears to be a routine test suite improvement that aligns with the project's testing conventions.