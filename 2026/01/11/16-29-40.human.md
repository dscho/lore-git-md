This is a user support response about rebase workflows, not a patch submission. Pushkar Singh provides a detailed explanation to Alan Mackenzie understand why his `git rebase` operation is attempting to replay 2012 commits (the entire divergence history of the stable branch) rather than just his local changes.

The key insight is that Alan needs to rebase from the original merge-base point rather than the current tip of the stable branch. Pushkar offers concrete commands to:
1. Find the correct base point with `git merge-base`
2. Perform a targeted rebase with `git rebase --onto`
3. Preview changes with `git range-diff`

The response is technically sound and addresses the core misunderstanding in Alan's approach - that rebasing from a moving branch tip will include all intermediate changes rather than just the local commits. No Git bugs or improvements are discussed; this is purely workflow guidance for a specific rebase scenario.