This is the second patch in a two-part test modernization series focused on improving the post-checkout hook tests in t5403. The series follows the project's ongoing effort to replace shell test patterns with more robust helper functions and better diagnostic output.

The patch replaces direct shell comparisons of hook arguments with `test_cmp`, which provides clearer failure messages when tests don't match expectations. The hook output is reformatted to use key=value pairs (e.g., "old=HEAD new=HEAD flag=1") to make mismatches more readable. This change is purely about test quality - it doesn't modify any production code behavior.

The author, Deveshi Dwivedi, appears to be a new contributor based on the email address. The patch follows established patterns seen in other test modernizations and looks straightforward. Since this is the second patch in the series, reviewers will likely evaluate both together when providing feedback. The changes are small and focused, touching only the test script with no risk to core functionality.