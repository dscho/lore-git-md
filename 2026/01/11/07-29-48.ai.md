This is a test modernization series focusing on the post-checkout hook testing in t5403. The two patches continue the ongoing effort to improve test robustness and maintainability by replacing ad-hoc shell tests with more reliable test helpers.

The first patch introduces a `check_post_checkout` helper function to consolidate repeated patterns of hook argument validation. This is a pure refactoring that doesn't change test behavior but makes the code more maintainable by eliminating duplication. The second patch further improves the tests by using `test_cmp` for comparing hook arguments rather than individual test commands, which will provide better failure diagnostics.

The changes are focused on the test script t/t5403-post-checkout-hook.sh, which verifies the behavior of the post-checkout hook. The series appears to be a straightforward quality improvement - the author notes it builds on a recently merged patch that modernized test patterns in this area. The patches are small and focused, touching only test infrastructure without modifying any production code.