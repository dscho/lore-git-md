This is a UI improvement patch for `git add -p`'s interactive hunk selection interface, part of an ongoing GSoC project. The patch series (currently v6) adds visual feedback showing previous hunk decisions ('use'/'skip') when navigating between hunks.

The current discussion focuses on refining the prompt formatting to balance clarity with line length constraints. Phillip Wood raised concerns about some prompts exceeding 80 characters with the current "(previous decision: use/skip)" notation. The author, Abraham Samuel Adekunle, proposes several alternative compact notations including "(choice: use)", "(use: yes)", and moving the status indicator to a separate line. The implementation modifies prompt strings in `add-patch.c` and maintains comprehensive test coverage in `t3701-add-interactive.sh`.

Technical details include removing an unnecessary `hunk_nr` check in `patch_update_file()` and adjusting string formatting to preserve readability when no decision has been made. The changes remain well-contained within the interactive add-patch workflow, touching only the UI layer without altering core hunk selection behavior. The thread shows active collaboration between Phillip Wood (sequencer expert) and Ben Knoble (build system contributor) to optimize the prompt display format while maintaining the core functionality that shows decision history during hunk navigation.