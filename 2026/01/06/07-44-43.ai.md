This is a review response from Patrick Steinhardt regarding Samo Pogačnik's patch to fix relative-depth fetching in shallow repositories. The patch is part 2 of a series addressing shallow fetch issues, following an earlier memory leak fix that Patrick had already reviewed.

Patrick's review focuses on several areas needing improvement. He notes the commit message needs better explanation of both the problem (unclear behavior when deepening beyond merged branches) and solution (new depth calculation logic). He suggests including an ASCII commit graph to illustrate the scenario. The test case contains some leftover debug output that should be removed, and Patrick points out style issues in the shell loop formatting.

Technically, Patrick questions whether the new `get_shallows_depth()` function could share more logic with the existing `get_shallow_commits()`, as they appear similar. The patch restructures upload-pack.c's deepening logic by introducing this new function and removing the now-redundant `get_reachable_list()`. Key changes involve using a `commit_depth` slab to track minimum depths during parent walks and handling graft points in shallow history.

The review leaves the technical implementation of the depth calculation itself uncommented, focusing instead on documentation and code organization concerns. Patrick's feedback suggests the patch is on the right track but needs polishing before it can be accepted. The thread shows Patrick engaging with an area (shallow clone logic) he admits is not his primary expertise, while still providing constructive feedback on clarity and maintainability.