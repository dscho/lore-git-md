This is a cover letter for a two-patch series addressing performance test infrastructure issues. Jeff King (Peff) identifies two bugs that affect using the `t/perf/run` script to compare Git versions:

1. The first patch fixes incorrect assignment of `TEST_OUTPUT_DIRECTORY` in the performance test library, which could cause output to be written to the wrong location.

2. The second patch ensures `GIT_PERF_*` environment variables are properly preserved when running performance tests, maintaining important configuration across test runs.

The changes are focused and mechanical, touching only the performance test infrastructure (`t/perf/`). Given Jeff King's deep expertise in Git's test infrastructure and his reputation for precise, well-explained fixes, these patches are likely straightforward improvements to the performance testing framework. The series appears to be a follow-up to earlier meson build system integration work, addressing some fallout from that transition.