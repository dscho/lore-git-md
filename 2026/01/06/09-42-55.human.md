This is a follow-up in a thread about a bugfix for the `git last-modified` command. The original issue involved two problems: a memory safety crash (now fixed) and confusing error messages (still open).

Gusted confirms that building Git from source with commit 05491b90ce ("last-modified: support sparse checkouts", 2025-11-29) resolves the crash they encountered when using `git last-modified` in bare repositories. They ask whether this fix will be included in the upcoming v2.52.1 release.

The thread shows good progress - a user has verified the fix works in practice, and the remaining issue (error message clarity) is now clearly separated from the resolved crash. Gusted's question about backporting the fix to the stable release is reasonable, though that decision would typically come from the maintainer (Junio) based on the severity and impact of the original bug.