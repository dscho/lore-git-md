Thread identity: Performance test infrastructure fixes by Jeff King (Peff), expanded to include Ramsay Jones' feedback on `GIT-BUILD-OPTIONS` handling.

Core problem: Initial two-patch series addressed: (1) incorrect `TEST_OUTPUT_DIRECTORY` assignment in performance tests, (2) `GIT_PERF_*` environment variable preservation. Ramsay Jones surfaces a third, deeper issue about `GIT-BUILD-OPTIONS` override behavior - the inability to selectively override build options via environment variables, a limitation exposed during meson integration.

Current status: Jeff King's v1 series remains complete for its immediate scope. Ramsay's response introduces a prototype from abandoned `meson-wip` branch showing potential solution (line-by-line `GIT-BUILD-OPTIONS` parsing with env var overrides), but flags it as a "disgusting hack" that doesn't resolve the architectural question of which options should be overrideable. No patches proposed yet for this larger issue.

Key technical details: Jeff's fixes remain unchanged (delayed `TEST_OUTPUT_DIRECTORY` assignment, `GIT_PERF_*` preservation pattern from `perf-lib.sh`). Ramsay's concern involves build system fundamentals: `GIT-BUILD-OPTIONS` currently loads as unconditional assignments, preventing env var overrides. His prototype demonstrates shell evaluation tricks to allow overrides but highlights the need for clear policy on overrideable vs fixed options. This connects to broader test infrastructure evolution during meson transition.

Open questions: Should `GIT-BUILD-OPTIONS` support selective overrides? If so, which options and through what mechanism? Requires design decision about build/test system flexibility boundaries.

Participant positions: Jeff King provided initial fixes with thorough historical analysis. Ramsay Jones, as build/portability expert, surfaces related architectural concern from meson integration work, sharing experimental approach but not proposing immediate solution. Discussion shifts from bugfix to design question.

Related work: Builds on Jeff's performance test fixes (4638e8806e, 5756ccd181, 32b74b9809) and connects to ongoing meson build system integration. Ramsay references abandoned `meson-wip` branch work that attempted similar variable handling.