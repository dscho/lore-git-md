This is a follow-up discussion in the `git status` branch tracking enhancement series, focusing on a subtle control flow issue uncovered during testing. Harald Nordgren points out that changing a condition in `format_tracking_info()` from `!sti` to `!ours && !theirs` causes test failures in the quick mode (ABQ) cases, even on the unmodified `master` branch. 

The issue appears related to how `stat_tracking_info()` handles quick mode returns, where it returns 1 for ABQ cases without properly clearing the counts. Harald suggests this might be better addressed as a follow-up fix since it predates his push tracking work. The discussion shows careful attention to preserving existing behavior while identifying a potential underlying issue in the tracking info machinery.