This is a follow-up technical discussion about edge cases in the `git status` branch tracking display feature. Harald Nordgren has identified an unexpected test failure when modifying the control flow in `format_tracking_info()` - the change causes failures in the "ABQ" (AHEAD_BEHIND_QUICK) test cases. 

The issue stems from the interaction between two conditions: the existing `!sti` check and the proposed `!ours && !theirs` check. Harald's test demonstrates that simply replacing one with the other breaks quick mode behavior, suggesting these checks serve distinct purposes in the control flow. The quick mode path (`abf == AHEAD_BEHIND_QUICK`) returns early with status 1, which appears to interact with these conditions in subtle ways.

Harald proposes treating this as a separate follow-up fix since it predates his push tracking changes and isn't directly related to that feature. The discussion reveals deeper technical nuances in Git's branch tracking display logic that warrant careful analysis to maintain all edge case behaviors. This appears to be the final technical hurdle before the series can be merged.