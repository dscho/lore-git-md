This is a bugfix discussion regarding the `git last-modified` command's behavior in bare repositories. The thread has identified and resolved one issue while leaving another open.

Patrick Steinhardt confirms that a memory safety bug in `git last-modified` (fixed in commit 05491b90ce on master) was causing crashes when processing refspec paths in bare repositories. The original reporter, Gusted, verifies that the fix works in their Forgejo integration context. However, the discussion reveals a remaining UX issue where error messages don't properly disambiguate between unknown revisions and paths even when using `--` syntax.

A new question emerges about whether the memory safety fix should be backported to the upcoming v2.52.1 release. Patrick indicates this would be Junio's decision and CCs him on the thread. The error message improvement remains an open issue that would require a separate patch series.

The thread demonstrates a productive interaction where a real-world integration (Forgejo) helped surface both a crash and a usability issue, with one already resolved in master and the other now properly tracked for future work. The backport question adds release management considerations to what began as a straightforward bug report.