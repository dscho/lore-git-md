This is a review comment on a bugfix patch about shallow repository handling. Patrick Steinhardt is responding to Samo Pogačnik's patch that fixed a memory leak in `get_shallow_commits()` by adding `object_array_clear(&stack)` in shallow.c.

Patrick agrees with the fix but offers a more precise technical analysis: while the patch correctly addresses the memory leak, the original commit message slightly mischaracterized the issue. He notes that the array elements themselves are properly handled during iteration, but the array's internal storage wasn't being freed. He questions why this leak wasn't caught by existing tests and suggests adding test coverage for shallow clone scenarios that would expose such leaks.

The exchange shows Patrick's characteristic attention to technical precision in memory management details. His response indicates the patch is fundamentally sound but could be improved with better test coverage and more accurate commit message wording. This is typical of his review style - thorough, constructive, and focused on both correctness and maintainability.