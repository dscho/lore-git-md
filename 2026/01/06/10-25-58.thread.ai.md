Performance regression and fix for `git cat-file` bitmap usage. The thread addresses a significant performance degradation in `git cat-file` introduced by commit 8002e8ee18, where bitmap indexes were unnecessarily loaded even for non-filtered operations. Jeff King (Peff) provides the patch and detailed performance analysis showing a 331% slowdown when listing all objects in sorted order, while confirming bitmap optimizations remain beneficial for filtered cases (65% speedup for blob listing).

The fix modifies `builtin/cat-file.c` to only prepare bitmaps when `opt->objects_filter.choice` indicates an active filter, avoiding unnecessary bitmap loading and sorting operations. The patch includes new performance tests covering three scenarios: sorted object listing (the regressed case), unsorted listing, and filtered listing. Key technical changes involve conditional bitmap loading on filter state, maintaining optimizations for filtered cases while restoring original performance for unfiltered operations.

In response to Patrick Steinhardt's review, Peff defends keeping the bitmap index assignment within the conditional, explaining that separating it would complicate the logic. He notes the `for_each_bitmapped_object()` API could be improved to return the bitmap index, which would simplify future callers, but considers this out of scope for the immediate fix. The discussion reveals technical nuances about persisting the bitmap index for `bitmap_index_contains_pack()` checks and tradeoffs between minimal fixes versus API improvements.

Patrick Steinhardt had reviewed the patch, confirming the technical approach is sound while expressing stylistic preference against assignments in conditionals. He acknowledged the original regression may have been masked by I/O noise in earlier benchmarks. The exchange between these experienced contributors shows consensus on the core fix, with Peff's response addressing the stylistic concern through technical rationale about bitmap persistence needs. The discussion leaves open potential future API changes while settling on the current approach as the right balance for this performance fix.

This remains a focused performance fix from trusted contributors (Jeff King and Patrick Steinhardt) with clear benchmark evidence demonstrating both the regression and resolution. The patch maintains existing functionality while addressing a clear performance issue in the bitmap handling logic, with all substantive technical concerns resolved and only minor API improvement possibilities noted for future consideration.