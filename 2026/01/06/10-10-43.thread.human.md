This thread began with Jeff King (Peff) addressing two specific performance test infrastructure issues: fixing `TEST_OUTPUT_DIRECTORY` assignment and preserving `GIT_PERF_*` environment variables. The discussion took a turn when Ramsay Jones, drawing from his meson integration work, pointed out a deeper architectural concern about how `GIT-BUILD-OPTIONS` handles environment variable overrides - a limitation that affects both the traditional Makefile and newer meson build systems.

Peff has now responded to Ramsay's concerns, acknowledging that the current approach is indeed problematic. He proposes a potential solution using shell variable expansion that would allow the build system to specify which options can be overridden, while keeping others fixed. However, he notes this would need consistent implementation across multiple test infrastructure files (`test-lib.sh`, `perf-lib.sh`, and `t/perf/run`) and raises questions about compatibility with Makefile usage.

The core issue has evolved from specific bugfixes to a broader design question about build option flexibility during the meson transition. The key open questions remain: which build options should be overrideable, what mechanism should enforce this policy, and how to implement it across both build systems. While Peff and Ramsay have surfaced the problem and proposed potential approaches, this appears to require further discussion among build and test infrastructure maintainers before reaching resolution. The thread remains active with this architectural challenge at its center.