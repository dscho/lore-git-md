Test modernization: Andrew Chitester updates `t1420-lost-found.sh` to adopt Junio C Hamano's suggested approach for verifying `git fsck`'s lost-found behavior. The patch replaces the previous combination of line counting and individual file checks with an explicit directory listing comparison using `test_cmp`.

The new version directly lists the contents of `.git/lost-found` and compares it against an expected file listing containing the two known lost objects (one commit and one other object). This makes the test's expectations more transparent and avoids potential fragility from relying on `ls` output line counts.

This change follows through on Junio's feedback from the initial discussion, demonstrating how the project's test modernization efforts go beyond mechanical helper substitutions to consider more robust assertion patterns. The patch is small and focused, cleanly implementing the maintainer's suggested improvement.