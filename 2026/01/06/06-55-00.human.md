This is patch 4/7 in Patrick Steinhardt's series refactoring Git's object info handling, part of the ongoing ODB (object database) abstraction effort. The patch ensures consistent population of packfile metadata when reading object information.

Currently, there's an inconsistency between two code paths that read object info from packs - `packed_object_info()` sometimes leaves out pack-specific fields while `packfile_store_read_object_info()` always populates them. This patch makes the behavior uniform by moving the pack metadata population into `packed_object_info()` and removing the redundant code from `packfile_store_read_object_info()`.

The change is mechanical but important for the series' goal of cleaning up object info handling. It touches the same packfile.c file modified in previous patches, continuing the pattern of small, focused changes that improve internal consistency. The patch doesn't introduce new functionality but removes a subtle inconsistency that could cause problems for future ODB abstraction work.

As with the rest of the series, this change maintains all current behavior while preparing for future needs. The v3 iteration shows careful attention to detail, with Patrick having incorporated feedback from earlier versions (as seen in the thread context). The patch appears ready for integration.