Here's a human-readable summary of the thread so far:

Johannes Sixt (Hannes) reported a bug in `git diff --relative` where the option incorrectly treats its argument as a simple text prefix rather than a directory path. He demonstrated that `--relative=Ma` matches and strips the prefix from "Makefile", producing the nonsensical output "kefile". This suggests the implementation is doing naive string matching when it should be path-aware.

Git maintainer Junio C Hamano has now responded, confirming the current behavior is wrong. He raised an important clarifying question about what the correct output should look like if "Ma" were actually a directory (suggesting "../Makefile" might be the proper relative path in that case). Junio's engagement signals this will likely receive prompt attention, though no patch has been proposed yet. The discussion remains focused on nailing down the intended semantics before proceeding with a fix. Given Hannes' expertise with Windows paths and Junio's quick response, this appears to be on track for resolution in the near future.