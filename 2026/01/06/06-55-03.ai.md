This is the final patch in Patrick Steinhardt's 7-part series optimizing object info handling as part of the ODB abstraction effort. The patch removes a redundant `repository` parameter from `packed_object_info()` since the same information is already available via the `packed_git` struct.

The change is straightforward and mechanical, updating all callers to omit the now-unneeded repository parameter. The patch touches several key subsystems that interact with packfiles - `cat-file`, `pack-objects`, `commit-graph`, and the bitmap code - but makes no functional changes beyond removing the redundant parameter.

This cleanup follows naturally from the previous patches in the series, particularly the ODB abstraction work. It's a small but meaningful simplification that makes the code more maintainable by reducing unnecessary parameter passing. The patch has already been reviewed as part of the larger series and appears ready for inclusion.