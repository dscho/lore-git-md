Feature implementation (submodule gitdir path encoding) - final review feedback from Patrick Steinhardt on the v7 series. This is the last email in the thread before merging, with Patrick confirming the architectural soundness and noting only minor nits remain.

The series implements a new `extensions.submodulePathConfig` extension that enables runtime configuration of submodule gitdir paths through `submodule.<name>.gitdir`, replacing the previous build-time configuration. Key technical components include path encoding to prevent filesystem conflicts, a four-tier fallback system for path resolution, and atomic migration of existing repositories.

Patrick's review confirms the core architecture is now sound after seven iterations, with all major concerns addressed. He notes the remaining work is limited to minor nits (formatting, wording) that could be addressed in one final version. The series has received acks from Junio Hamano and thorough review from multiple contributors (Josh Steadmon, Emily Shaffer, Phillip Wood), demonstrating broad consensus on the approach.

Files touched include `submodule.c`, `builtin/submodule--helper.c`, and `setup.c`, with new tests in `t7425-submodule-gitdir-path-extension.sh`. The implementation handles edge cases like nested submodules and case-insensitive filesystems, with runtime restrictions ensuring the extension only activates for new repositories.

This marks the successful conclusion of a long-running effort to make submodule gitdir paths configurable at runtime rather than build time, with all architectural discussion now settled and the implementation ready for merging pending final polish.