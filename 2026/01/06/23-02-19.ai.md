Performance investigation of `git pack-refs` on NFS continues, with Martin Fick responding to Patrick Steinhardt's suggestions about NFS being the likely culprit. Martin acknowledges NFS's known issues but emphasizes its continued necessity for their Gerrit deployment due to its unique replication capabilities across hundreds of mirrors. He identifies a specific pain point where Git holds the `packed-refs.lock` for extended periods during slow packing operations, which particularly impacts Gerrit servers that rely heavily on packed-refs for atomic updates. 

The discussion has shifted from initial I/O concerns to examining memory access patterns, particularly around mmap performance over NFS. Martin suggests architectural improvements where Git could potentially pre-read necessary data (including tag peeling information) before acquiring the lock, reducing the critical section duration. While this wouldn't speed up the packing operation itself, it would mitigate the blocking impact on servers. 

Patrick's recommendation to use `perf(1)` for flame graph analysis remains pending due to server constraints, but the conversation continues to refine understanding of whether the bottleneck is in NFS mmap behavior or Git's in-memory algorithms. The thread highlights a tension between Git's current implementation and real-world deployment constraints in large-scale Gerrit environments.