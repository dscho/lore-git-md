This thread discusses an improvement to `git add -p`'s interactive hunk selection interface, adding visual feedback about previous decisions when navigating between hunks. The patch, now in its sixth iteration, modifies prompt strings in `add-patch.c` to show whether each hunk was previously marked for use ('y') or skip ('n').

Junio C Hamano originally suggested this change to improve clarity during hunk navigation. The implementation consistently updates all prompt strings across different patch modes (stage, stash, unstage) while leaving the core selection logic unchanged. Corresponding test updates in `t3701-add-interactive.sh` verify the new behavior across various scenarios including navigation, regex matching, and color output.

The v6 patch addresses a minor metadata issue (a duplicate Reported-by tag) from earlier versions. The changes are focused and well-contained, touching only the prompt display functionality. With no apparent technical issues remaining, the patch appears ready for final review and potential merging.