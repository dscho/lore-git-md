This is the second patch in a two-part test modernization series for the post-checkout hook testing in t5403. The patch converts the test assertions to use `test_cmp` rather than individual test commands, improving failure diagnostics by showing exactly which argument (old ref, new ref, or flag) mismatched when tests fail.

The changes are straightforward: the post-checkout hook now outputs arguments in a consistent "key=value" format, and the `check_post_checkout` helper compares this output against an expected template using `test_cmp`. This matches the pattern seen in other test scripts and addresses feedback from Eric Sunshine and Pushkar Singh about improving test robustness and maintainability.

The patch touches only the test script (t/t5403-post-checkout-hook.sh) with no functional changes to Git itself. It modifies the hook implementation to use the new format and updates both the main test case and clone scenario to use the improved validation. The changes preserve all existing assertions while making failures easier to diagnose.

This concludes the test modernization effort for this script, following patterns established in similar test files like t5510-fetch.sh and t5520-pull.sh. The series has been merged after incorporating all review feedback.