This is a helpful response in an ongoing thread about rebase challenges in the Linux kernel development workflow. Kristoffer Haugsbakk suggests using `git merge-tree` as an alternative to the requested `git rebase --dry-run` functionality that Alan Mackenzie was seeking.

The key technical contribution here is proposing `git merge-tree` as a way to preview merge conflicts before actually performing a rebase operation. This addresses Alan's frustration with having to resolve conflicts one at a time during an actual rebase, with no way to see the full conflict landscape upfront.

The suggestion is particularly relevant for Alan's case of moving patches between stable branches in the Linux kernel repository, where large divergences between branches can lead to numerous unexpected conflicts in files the developer didn't modify. The `merge-tree` approach would let Alan see all potential conflicts at once rather than discovering them incrementally during rebase.

This is a focused technical suggestion rather than a comprehensive solution to Alan's workflow challenges. It doesn't address the broader questions about kernel patch maintenance strategies or tools like `git range-diff`, but provides a concrete command that could help with the immediate rebase conflict preview problem. The response maintains the thread's technical focus while offering a practical workaround to one specific pain point.