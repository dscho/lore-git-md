This is the first patch in Harald Nordgren's 2-part series to enhance `git status` tracking information, now approved for merging into `next`. The patch refactors the branch comparison formatting logic in preparation for adding push tracking support in the follow-up patch.

The changes extract the core `format_branch_comparison()` helper from `format_tracking_info()`, centralizing the logic that generates the "ahead/behind" messages. The refactoring maintains all existing behavior including:
- Quick mode (`AHEAD_BEHIND_QUICK`) output
- Upstream-gone detection
- Advice messages
- Pluralization handling
- All output formats

Key technical details:
- Renames variables to `cmp_fetch` for clarity (per Junio's earlier feedback)
- Separates concerns between the caller and new helper function
- Preserves exact message formatting and advice conditions
- Keeps the pluralization fix from previous iterations in the series

The patch represents the final iteration (v23) of this preparatory refactoring, with all review feedback addressed. It enables the push tracking functionality to be added cleanly in the next patch while maintaining the existing upstream tracking behavior. The changes are focused entirely on internal restructuring with no user-visible behavior changes at this stage.