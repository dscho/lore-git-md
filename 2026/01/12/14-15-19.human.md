This is patch 7/8 in Patrick Steinhardt's v10 series introducing the new `git history` command, part of a larger feature effort to provide more flexible history editing operations. The patch adds the basic command infrastructure while explicitly marking it as experimental in the documentation.

The patch creates the skeleton of the `git history` command with:
- Basic command-line parsing via `parse_options()`
- Minimal documentation explaining its purpose and differences from `git rebase`
- Build system integration (Makefile, meson.build)
- A simple test verifying basic argument handling

The documentation positions `git history` as an alternative to `git rebase` for common history editing tasks, noting key differences like bare repository support and automatic branch updates. It clearly states the experimental nature and current limitations (no merge support, no conflict handling). The command currently has no implemented subcommands - those come in later patches.

This is a well-structured foundation patch that follows Git's standard patterns for new commands. The documentation is thorough about current limitations and experimental status, which should help set user expectations. The test coverage is minimal but appropriate for this scaffolding stage. As part of the larger series, this appears ready for merging as infrastructure to build upon.