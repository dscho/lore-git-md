This is a discussion about potential Git configuration mechanisms for template files, continuing an ongoing thread about tracking base versions while ignoring local modifications. brian m. carlson responds to Krzysztof Zabłocki's proposal for a `.gittemplates` configuration by pointing to existing documentation about why tracked-but-ignored files present challenges for Git's design.

The response argues that the proposed approach would create ambiguity in Git's operations - particularly around commands like `git reset --hard` that need clear semantics about whether to preserve or overwrite working tree files. brian m. carlson notes that the current FAQ explains Git's inability to automatically determine whether local changes should be preserved during operations like checkout. They suggest that script-based solutions (potentially via hooks) provide more flexibility for merging template updates with local configurations, pointing to GitHub's "scripts to rule them all" pattern as an example of this approach in practice.

The email highlights key technical constraints in Git's design that make the proposed feature problematic, particularly around the working tree synchronization model. It suggests the current workaround of maintaining separate template and local files, while potentially cumbersome, aligns better with Git's fundamental assumptions about file tracking. The response doesn't reject the underlying use case but argues that solving it requires approaches outside core Git's file tracking model.