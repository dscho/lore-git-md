Refactoring patch in Patrick Steinhardt's reftable backend validation series. This is patch 12/17 in the second iteration of the series, which aims to centralize ref validation checks across different backend implementations.

The patch extracts the worktree-specific backend retrieval logic from `backend_for()` into a new helper function `backend_for_worktree()`. This is a clean refactoring that:
- Moves 27 lines of worktree handling code into a dedicated function
- Maintains all existing behavior while making the control flow clearer
- Prepares for future changes by isolating worktree backend management

The change follows the series' pattern of incremental improvements to the reftable backend code. The commit message is straightforward, explaining this is preparatory work for a subsequent change. The diff shows a net addition of 16 lines (43 new, 27 deleted) with no functional changes - just code reorganization.

This continues the series' progression toward unified backend validation, with Patrick methodically cleaning up the codebase before introducing new validation logic. The patch appears well-contained and maintains the high quality seen throughout this series.