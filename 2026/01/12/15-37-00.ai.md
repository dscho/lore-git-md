This is a technical refinement in the ongoing `git-history` feature development, specifically addressing code style feedback from Junio Hamano on the boolean logic in the replay revisions core.

Patrick Steinhardt acknowledges Junio's suggestion to simplify the XOR-based boolean check for the mutually exclusive `--onto` and `--advance` options. The original implementation used a double-negation pattern (`!!`) to normalize pointers to boolean values before applying XOR (`^`), which Junio found unnecessarily cryptic. The proposed alternative directly compares the negated pointers (`!onto_name != !*advance_name`) for clearer intent.

Steinhardt notes a minor correction to Junio's suggested syntax (the condition should use `==` rather than `!=` to maintain the original logic) and confirms the change has been implemented locally. This exchange represents final polishing of the replay revisions interface as part of the v10 series, with Junio's review indicating the patch is nearing merge-readiness. The interaction demonstrates the maintainer's attention to code clarity even in small details of this major new feature.