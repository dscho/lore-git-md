Test modernization patch improving post-checkout hook testing in t5403. This is the second and final patch in a series that has now been merged, following up on review feedback from Eric Sunshine and Pushkar Singh.

The patch switches from individual test commands to using `test_cmp` for validating post-checkout hook arguments, which provides clearer failure diagnostics when tests don't match expectations. The hook output format is changed to key=value style (e.g., "old=HEAD new=master flag=1") to make mismatches more readable. The changes affect both the main test case and the clone scenario, while preserving all the original assertions.

This completes the test modernization effort for t5403, bringing it in line with patterns seen in other test scripts like t5510-fetch.sh. The implementation follows the same approach as the broader test suite standardization effort, focusing on maintainability and better debugging output rather than changing test behavior.