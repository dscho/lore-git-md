This is part 1 of an 11-patch series implementing submodule gitdir path encoding, now in its 9th iteration. The series has already received merge approval from Junio Hamano and addresses final polish comments from Patrick Steinhardt.

The patch makes a small but important change to `builtin/submodule--helper.c`, replacing a hardcoded path construction with the proper `submodule_name_to_gitdir()` helper function. This ensures consistent path handling across all submodule operations, particularly important for the new path encoding scheme being introduced in the series. The change is straightforward - it narrows variable scope and uses the existing helper function rather than manually constructing paths. This helps prevent test failures that could occur with the new path encoding behavior.

As part of a well-reviewed series that has undergone extensive testing (including real-world validation at Google), this patch represents a final polish before merging. The change is small but contributes to the overall goal of making submodule gitdir paths more robust, particularly for cases involving case-folding and special characters.