This is a feature proposal for a new Git configuration mechanism to handle template files. The author suggests adding a `.gittemplates` file (or config parameter) that would work similarly to `.gitignore`, but with a key difference: files listed there would be tracked in their initial state but subsequent changes would not be committed. This is positioned as a solution for common workflow pain points like `.env` files and machine-specific configs, where currently users must maintain separate template and local versions.

The proposal is brief and lacks technical details about implementation or edge cases. It doesn't reference any prior art (like existing worktree or assume-unchanged functionality that might overlap) or show awareness of Git's current mechanisms for similar use cases. The motivation is clear - reducing manual work when dealing with templated config files - but the email reads more like an initial idea than a fully-formed patch proposal. Given the complexity of Git's index and diff machinery, this would likely need significant design work before being implementable.