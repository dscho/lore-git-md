This is a test modernization patch that introduces a helper function for post-checkout hook testing in t5403. It's part of a merged 2-patch series that improves test robustness by consolidating validation patterns into a reusable helper.

The patch adds a `check_post_checkout` helper function that encapsulates the repeated pattern of validating post-checkout hook arguments (old ref, new ref, and flag). The function takes 4 parameters - the args file and the three expected values - and performs all validations in one place. This replaces scattered individual test commands with a single consistent interface.

Key improvements include:
- Using `git rev-parse` to validate commit hashes rather than just comparing ref names
- Adding a BUG check for argument count
- Extending validation to the clone test case which previously only checked file existence
- Maintaining identical test coverage while making the code more maintainable

The changes are purely to the test infrastructure - no Git behavior is modified. This follows the broader test modernization effort to use standardized patterns and documented helpers. The implementation preserves all original assertions while improving diagnostics and reducing duplication.

The patch builds on prior work in this area and addresses all review feedback from previous iterations, leading to its eventual merge. It demonstrates careful attention to test hygiene while keeping the scope focused on the post-checkout hook validation pattern.