AI thread summary:

Patrick Steinhardt's v10 patch series introduces a new `git history` command (1/8). This patch extracts core revision replay logic from `builtin/replay.c` into a new `replay_revisions()` function in preparation for moving it into libgit.a. The function encapsulates three phases: input handling, replay execution, and reference updates, using new `replay_revisions_options` and `replay_result` structs. Behavior remains unchanged.

Technical details: handles `--onto` and `--advance` modes, tracks ref updates via `replay_ref_update` entries in `replay_result`, uses existing merge machinery (`merge_options`, `merge_result`), maintains current error codes (0/1/128). Current limitations (FIXMEs) include no root/merge commit support. This is part of broader infrastructure work to enable new history manipulation capabilities, with this step focused on library accessibility. The change is well-contained with clear commit message.