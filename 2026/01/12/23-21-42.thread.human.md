This thread addresses a regression in pre-push hook behavior where a recent change incorrectly redirected hook output from stdout to stderr, breaking existing workflows. The issue was first reported by Chris Darroch (Git LFS maintainer), who noted it affected test suites and production systems that had relied on stdout behavior since 2017. brian m. carlson traced the technical root cause to a commit that copied sideband communication behavior from other hooks without considering pre-push's different needs.

Adrian Ratiu, who has been working on hook improvements, submitted a targeted fix that restores stdout behavior for pre-push hooks while maintaining stderr redirection for other hook types. He verified the solution against Git LFS's test suite as part of his broader config-based hooks work. Chris has now confirmed that Adrian's fix successfully addresses the regression in Git LFS's test cases, particularly in t-fetch-include.sh where tests were only capturing stdout.

The thread has established clear consensus on the need to restore backward compatibility, with Adrian's solution providing a technically sound approach that preserves sideband communication for hooks that need it. With positive confirmation from an affected downstream user, the discussion appears ready to move toward merging the fix.