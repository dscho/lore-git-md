RFC patch series by Olamide Caleb Bello (Outreachy intern mentored by Christian Couder) introducing `struct repo_config_values` to eliminate global configuration variables as part of `the_repository` removal effort. The series migrates `core.attributesFile`, `core.sparseCheckout`, and `branch.autoSetupMerge` configuration values to repository-specific storage in the new struct embedded in `struct repository`, affecting 30+ files across multiple commands.

The current iteration (v3) introduces `struct repo_config_values` in part 1, migrating `core.attributesFile` from global `git_attributes_file` to `the_repository->config_values.attributes_file_path`, updating `attr.c` and `git_default_core_config()`. Phillip Wood's review of part 1 suggested improvements including concrete examples of global variable issues, reconsidering the `_path` suffix, and better documentation. Toon Claes provided feedback on NULL-safety, comments, and commit message focus, which the author acknowledges will be addressed in the next iteration.

Part 2 migrates `core.sparseCheckout` from global `core_apply_sparse_checkout` to `the_repository->config_values.sparse_checkout`, updating 12 files including sparse-checkout command behavior, clone initialization, and submodule handling. Phillip Wood identified a critical flaw where config values are only populated for `the_repository`, leaving other repository structs with defaults regardless of actual configuration. He suggested fixes including clarifying limitations, fixing unsafe usage in backfill.c, improving documentation, and refining naming (`apply_sparse_checkout` or `sparse_checkout_apply`). Toon Claes noted minor issues like a duplicate commit message line and typo in variable name.

Part 3 moves `branch.autoSetupMerge` to `the_repository->config_values.git_branch_track` (later renamed to drop `git_` prefix per Phillip Wood's suggestion), affecting branch, checkout, push, and submodule--helper commands. The v3 iteration shows improved commit message clarity and better organization with struct initialization moved to part 1. Phillip Wood's review reiterates core concerns about repository-scoped access validity and proper handling of non-primary repository instances, suggesting this architectural challenge must be resolved across all three migrated variables.

Junio C Hamano's open questions remain deferred: (1) formal criteria for config variable scope (repository vs global), with Bello maintaining only repository-dependent variables are being moved, and (2) comprehensive solution for initialization when mixing scoped/global configs. The implementation maintains existing behavior while handling edge cases like submodule grep and cone mode interactions, though Phillip Wood's reviews reveal the critical gap in non-primary repository handling that blocks progress.

The series establishes patterns for future work in the `the_repository` removal effort. While most technical concerns from Toon Claes and Phillip Wood have been addressed through iteration, Phillip's latest reviews identify the unresolved architectural issue regarding non-primary instance handling as the current blocking point. This otherwise well-reviewed series has made substantial progress in eliminating global configuration variables, with the remaining challenge being proper support for multiple repository instances.