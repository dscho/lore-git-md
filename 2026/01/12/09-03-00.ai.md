Refactoring patch in Patrick Steinhardt's ref backend validation series. This is patch 11/17 in the effort to centralize ref consistency checks across both files and reftable backends.

The patch makes two straightforward changes to `refs/reftable-backend.c`:
1. Sorts the `#include` directives alphabetically (moving `iterator.h` after `ident.h`)
2. Updates include paths to be relative to the "refs/" directory (removing the standalone `parse.h` and using the correct path)

These are purely mechanical changes that improve code hygiene without altering functionality. The patch follows the series' pattern of first cleaning up the existing code before introducing new shared validation infrastructure. The changes align with Git's coding guidelines for include ordering and path consistency.

This continues the incremental approach seen in earlier patches, where each step carefully prepares the codebase for the larger architectural changes to come. The patch is small, focused, and uncontroversial - exactly the sort of preparatory work needed before introducing more complex shared validation logic in later patches.