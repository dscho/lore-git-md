This is a helpful response to Alan Mackenzie's rebase difficulties in the Linux kernel repository. Kristoffer Haugsbakk suggests using `git merge-tree` as a way to preview conflicts that would occur during a rebase, addressing Alan's complaint about the lack of a `--dry-run` option in `git rebase`.

The suggestion is practical - `git merge-tree` can show merge conflicts between two branches without actually performing the merge or rebase. This provides the conflict preview functionality Alan was looking for, though it requires understanding that merge-tree shows conflicts from the perspective of merging the two specified commits, which is similar to what rebase would encounter when replaying commits on top of a new base.

The response is concise and directly addresses the technical need, though it doesn't delve into the broader context of Alan's kernel maintenance workflow. It's a useful tip that could help Alan (and others) avoid the "drip feed" of conflicts during problematic rebases. The suggestion appears technically sound and relevant to the specific pain point raised.