Existing AI thread summary:

This thread is about a regression in pre-push hook behavior introduced by commit 3e2836a742d8b2b2da25ca06e9d0ac3a539bd966 ("transport: convert pre-push to hook API"). The change causes pre-push hook output that previously went to stdout to now be redirected to stderr. The thread was started by Chris Darroch from the Git LFS project, who reports this breaks existing workflows and test suites that expect stdout output from pre-push hooks.

The issue stems from `pick_next_hook()` in hook.c setting `stdout_to_stderr` in the child_process arguments. Chris provides historical context showing this stdout behavior has been stable in Git LFS since at least 2017. He seeks clarification on whether this change was intentional (in which case Git LFS will adapt) or if it might be reconsidered for backward compatibility. The tone is constructive, acknowledging their code could be adjusted if needed while raising concerns about broader impact on other users' workflows.Updated AI thread summary:

This thread addresses a regression in pre-push hook behavior caused by commit 3e2836a742d8b2b2da25ca06e9d0ac3a539bd966 ("transport: convert pre-push to hook API"). The change redirects pre-push hook output from stdout to stderr due to `stdout_to_stderr` being set in `pick_next_hook()`'s child_process arguments. Chris Darroch (Git LFS) reports this breaks existing workflows and test suites that rely on stdout output, noting this behavior has been stable since at least 2017. He seeks clarification on whether this change was intentional (Git LFS will adapt if so) or if backward compatibility might be preserved. The discussion remains constructive, balancing the need for API evolution with maintaining existing behavior that tools may depend on. No maintainer response has been posted yet regarding the intentionality of the change or potential paths forward.