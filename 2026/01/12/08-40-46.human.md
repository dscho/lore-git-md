This is a review comment on a feature patch adding the `--keys` flag to `git repo info`. The patch is part of a series that standardizes output format handling across `git repo` subcommands.

Patrick Steinhardt suggests a minor documentation improvement - he notes that while the default format (newline-separated output) can't be explicitly selected, it might be clearer to allow specifying it as `--format=newline` for consistency, even though `keyvalue` wouldn't make sense in this context. This is a few steps into an already well-reviewed thread where the implementation itself looks solid.

The series has gone through multiple iterations (v1-v3) with input from Junio Hamano and documentation polish from Jean-Noël Avila. It's now in its final form, ready for integration into `next`, replacing an earlier queued implementation. The core functionality provides machine-readable repository metadata access while unifying format handling across related commands.