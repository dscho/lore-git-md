This is a final review comment from the maintainer on Harald Nordgren's long-running series to enhance `git status` with push tracking information. The series has already been approved for merging into `next` after 23 iterations of review.

Junio Hamano notes a minor inconsistency in how divergence advice flags are handled compared to the pull/push advice flags, wondering if they could be unified in the `base_branch_modes` flag word. However, he explicitly states this is just an observation, not a blocking concern, and calls the patchset "a pleasant read" overall.

The comment comes at the very end of a 23-patch journey that has already addressed all major technical requirements, with thorough test coverage and careful handling of edge cases. The implementation cleanly extends `git status` to show divergence from both upstream and push tracking branches while maintaining all existing behavior. 

This appears to be a final polish point on an otherwise complete feature that is ready for wider testing in `next`. The maintainer's positive tone suggests this is unlikely to require another revision unless someone spots an issue during the `next` soak period.