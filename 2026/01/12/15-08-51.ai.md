This is a code review comment from Junio Hamano (the Git maintainer) on Patrick Steinhardt's `git-history` series, specifically addressing a boolean logic expression in the replay revisions core logic.

Junio suggests simplifying a cryptic `!!VAR ^ !!VAR` pattern checking for mutually exclusive options into a more readable `!VAR != !VAR` form. The change doesn't affect functionality but improves code clarity - the current version uses double-negation and XOR to enforce that exactly one of `--onto` or `--advance` was specified, while Junio's proposed version expresses the same constraint more directly comparable NULL checks.

The comment shows Junio's typical maintainer style: technically precise while advocating for readability over cleverness. This is a minor nit in an otherwise complete series, indicating the patch is in final polishing stages before potential merging. The exchange demonstrates the project's attention to code quality even in mechanical conversions, with the maintainer weighing in on idiomatic C style for what will become public API surface in libgit.a.