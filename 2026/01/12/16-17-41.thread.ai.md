Thread identity: Bugfix series for `git last-modified` command from Toon Claes, addressing input validation issues and error handling.

Core problem: The `git last-modified` command would hit a BUG() when given a tree OID (like HEAD^{tree}) instead of a commit, had a memory leak in error paths of populate_paths_from_revs(), and had suboptimal error messaging.

Current status: Patch v2 expands the fix into three distinct improvements based on v1 feedback. It now includes:
1. More technically accurate error message when multiple revisions are given (using "revision" instead of "tree")
2. Removal of a duplicate error message
3. The original v1 fix (commit object validation and memory leak fix)
Test coverage now includes annotated tags as suggested in v1 review.

Key technical details:
- Validates commit-ish inputs before diff operation
- Fixes pathspec memory leak in populate_paths_from_revs() using goto
- Improves error messaging throughout
- Tests verify error messages for tree objects and annotated tags
- Touches builtin/last-modified.c and t/t8020-last-modified.sh
- Junio's v1 review confirmed get_revision() expects commit-ish input
- Patrick's v1 suggestions on error message clarity and test coverage addressed
- All v1 feedback incorporated with no remaining open questions

Participant positions: Toon Claes authored the series based on real-world reports. Junio C Hamano reviewed v1 and confirmed technical approach. Patrick Steinhardt provided constructive feedback on error messaging and test coverage, all addressed in v2. Both reviewers' concerns have been satisfied.

Related work: `git last-modified` is a relatively new builtin command; this series improves its robustness for edge cases. The thread shows consensus on the technical approach with all feedback addressed, making it ready for merging.