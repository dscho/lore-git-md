This is Junio Hamano's final review comment on Harald Nordgren's long-running series to enhance `git status` with push tracking information. The series has already been approved for merging into `next` after 23 iterations of review.

Junio notes a minor inconsistency in the advice-display logic where the divergence advice check (`show_divergence_advice`) follows a different pattern than the push/pull advice checks. He suggests it might be cleaner to incorporate the divergence flag into the existing `BRANCH_MODE_*` flags, though he explicitly states this is just an observation rather than a blocking concern.

The comment comes after all major technical issues have been resolved, including:
- Finalizing the advice display behavior (with input from Phillip Wood)
- Maintaining quick mode (`--no-ahead-behind`) compatibility
- Comprehensive test coverage in t6040
- Handling of remapped refspecs
- Preserving existing output formats

This represents the final pre-merge feedback on a feature that cleanly extends `git status` to show divergence from both upstream and push tracking branches when they differ. The implementation centralizes formatting logic in `format_branch_comparison()` while maintaining all existing behavior. Some follow-up work around `push.default` interactions remains possible, but the core functionality is now merge-ready.