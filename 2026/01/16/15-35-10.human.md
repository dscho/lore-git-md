This is the final documentation patch in a series that aligns `git add` behavior with other commands when handling submodules configured with `ignore=all`. The patch updates three documentation files to reflect that `--force` is now required to stage updates to such submodules.

Claus Schneider incorporates feedback from Ben Knoble to improve phrasing, particularly adding "When set to" for grammatical consistency. The changes clarify that while `ignore=all` prevents submodules from showing as modified by default, they can still be staged using `git add --force <path>`. The documentation now accurately describes this behavior across `git-add`, `gitmodules`, and submodule config files.

The patch completes the technical implementation from earlier in the series, ensuring users understand the new requirement and which commands respect the `ignore` setting. With these documentation updates, the series appears ready for final review and potential merging.