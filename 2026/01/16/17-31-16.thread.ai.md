Thread summary:

Johannes Schindelin (Dscho) proposed a CI optimization patch to skip git-svn tests during leak-checking CI jobs by introducing `NO_SVN_TESTS` in `ci/lib.sh`. The change targets the Perl-based git-svn tests which don't contribute to C memory leak detection while adding ~13% runtime overhead (42+ minutes across 65 tests). The patch includes detailed metrics and rationale, with an AI-assisted log analysis trailer. Junio C Hamano (the Git maintainer) reviewed and approved the change, noting its clean implementation and clear justification. He indicated he will merge it into the integration branches. The change is uncontroversial with no outstanding questions or requested modifications. The optimization affects only CI configuration (`ci/lib.sh`) and maintains existing test coverage through other paths.