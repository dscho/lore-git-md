Updated AI summary:

Thread summary:

Johannes Schindelin (Dscho) initiated a CI optimization to skip git-svn tests during leak-checking jobs via NO_SVN_TESTS in ci/lib.sh, saving ~42 minutes of runtime from Perl-based tests irrelevant to C memory leak detection. Junio C Hamano approved this approach and extended it to CVS/Perforce tests (24-minute savings) by introducing NO_CVS_TESTS and NO_P4_TESTS variables with early-exit checks in t/lib-cvs.sh and t/lib-git-p4.sh, following the established pattern for linux-leaks and linux-reftable-leaks jobs. Phillip Wood confirmed the technical approach and raised whether these exclusions should also apply to ASan (AddressSanitizer) jobs, leading Junio to agree and propose considering a more general NO_FOREIGN_CODE_TESTS variable.

In subsequent discussion, Phillip Wood and Junio refined the conceptual framing around test exclusion criteria, establishing that the key factor is whether the code being tested is under Git's control rather than implementation language or SCM interaction characteristics. They examined hypothetical scenarios including:
- A C-based `git svn` calling external libraries (libsvn) - leaks in Git's own code paths would be detectable but not library leaks unless built with sanitizers
- The current Perl implementation's interpreter leaks being outside Git's control
- The academic nature of this discussion since no C rewrite of `git svn` is planned

Junio maintained focus on practical CI configuration (ci/lib.sh and related test libs) while Phillip provided technical refinements about leak detection boundaries. The thread shows consensus on:
1. Skipping foreign SCM tests in leak-checking jobs as implemented
2. The controllability principle as the primary exclusion criterion
3. The current approach being practically sound despite theoretical edge cases

The discussion demonstrates careful consideration of the boundaries between Git's testable code and external dependencies, with both participants contributing to a nuanced understanding of leak detection limitations while keeping CI improvements actionable. The thread remains focused on configuration changes already implemented (NO_*_TESTS variables) while thoroughly exploring the conceptual foundations behind them.