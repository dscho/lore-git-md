This is a bugfix patch for the `git last-modified` command, part of a completed series that has already been merged. The patch adds input validation to prevent tree OIDs from being passed to the command, which previously triggered a BUG() message. The fix uses `repo_peel_to_type()` to verify the input is commit-ish (a commit or tag that can be peeled to a commit) and adds a test case to confirm the behavior.

The change is straightforward - just 5 lines of code to add the validation check plus corresponding tests. This was the last remaining edge case in a series that has already addressed memory leaks, terminology improvements, and other error handling issues. The patch follows the established pattern of the series by using the `goto out` error handling flow and includes clear error messaging.

Since this is post-merge polish for an already-complete series, it's unlikely to generate much discussion unless reviewers spot something unexpected in the implementation. The change looks correct and well-tested, matching the high quality of the rest of the series from Toon Claes.