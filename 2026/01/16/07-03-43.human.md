This is a technical discussion thread about patch 10/14 in Patrick Steinhardt's ongoing object database (ODB) abstraction series. The patch removes legacy `for_each_{loose,packed}_object()` functions in favor of the new backend-aware iteration interface.

Justin Tobler noticed that `builtin/cat-file.c` still handles packed object iteration in a backend-specific way rather than using the new unified interface. Patrick confirms this was a deliberate choice to keep the series focused, acknowledging that bitmap-related iteration needs future attention but isn't blocking the current work.

The exchange shows the series is in its final polishing stages, with reviewers catching subtle implementation choices while the author maintains focus on the core abstraction goals. This patch specifically touches `builtin/cat-file.c`, converting its object iteration to use the new `packfile_store_for_each_object()` while leaving some backend-specific behavior for future refinement.