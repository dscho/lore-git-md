This is a minor follow-up in the ongoing `git-last-modified` series, addressing a documentation consistency point. The patch in question moves the `--max-depth` option handling into the command's own option parsing to ensure it appears in the `-h` help output, matching what was already documented in the man page.

Junio Hamano notes a potential sequencing issue - since the option is now being handled directly by the command rather than through diff machinery, he questions whether this change should have been made in the same patch that introduced the documentation. The current split means there's a brief window where the documentation describes behavior that isn't yet fully implemented in the code.

The thread context shows this is part of a larger effort to improve the experimental `git-last-modified` command's usability, particularly around depth control behavior. The series has already addressed several rounds of feedback, including NUL-termination support (`-z`) and making the default behavior non-recursive. This particular patch represents a small but important polish to ensure command-line help stays in sync with the formal documentation.

From a technical perspective, the change is straightforward - it moves option parsing from being inherited through diff machinery to being handled directly by the command. The functional behavior remains identical; this is purely about improving discoverability through the help text. The thread summary indicates this series is nearing completion, with most major design questions already resolved and only minor polish remaining.