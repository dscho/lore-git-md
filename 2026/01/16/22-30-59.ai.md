This is the v4 cover letter for Samo Pogačnik's 2-patch series fixing issues with shallow repository fetching. The series has undergone multiple rounds of review from Patrick Steinhardt and Junio C Hamano, with the latest version addressing a typo ("allways" -> "always") noted by Kristoffer Haugsbakk.

The key focus remains on fixing edge cases when fetching with `--deepen` in shallow repositories, particularly when deepening beyond merged branches where previous versions could leave unreachable shallow commits. The solution unifies absolute and relative depth calculations through a new `get_shallows_depth()` function that properly tracks minimum depths of existing shallow commits.

Changes in v4 are minimal - just the typo fix and some minor commit message clarification about avoiding logic duplication between `get_shallows_depth()` and `get_shallow_commits()`. The technical implementation remains the same as v3, with the memory leak fix in patch 1 and the relative-depth handling in patch 2.

The series appears ready for integration after clearing all review feedback. The patches are well-tested (including leak-checking tests) and address a real bug in shallow repository handling while maintaining backward compatibility. The collaboration history shows effective iteration based on reviewer input, with all identified issues now resolved.