Performance optimization for sparse-checkout pattern handling, completing the series that changes from O(n^2) to O(n log n) sorting. This final version from Amisha Chhajed adds explicit duplicate removal after sorting, addressing the regression identified in v3. The patch modifies `builtin/sparse-checkout.c` to use `string_list_append()` followed by sorting and `string_list_remove_duplicates()` rather than the original `string_list_insert()` approach.

Junio C Hamano's review confirms the technical correctness of the solution, noting it properly handles both the parent_hashmap (guaranteed unique) and recursive_hashmap (may contain duplicates) cases. While the patch appears merge-ready, Junio suggests two follow-up considerations: adding performance measurements to quantify the improvement, and creating a test case that would catch duplicate-handling regressions. He also muses about a potential future API improvement - combining sort-and-deduplicate into a single `string_list_sort_u()` function - but explicitly marks this as out of scope for the current change.

The thread has reached consensus that the core optimization is correct and valuable, with the remaining suggestions being optional enhancements rather than blockers. This represents the completion of a focused performance improvement to the sparse-checkout subsystem's pattern handling, building on earlier work by Derrick Stolee and Elijah Newren in this area. The changes touch the delicate balance between pattern uniqueness guarantees and performance in the cone mode pattern lists.