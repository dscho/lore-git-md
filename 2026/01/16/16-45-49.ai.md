Performance fix for `git cat-file` bitmap handling. Jeff King (Peff) responds to Patrick Steinhardt's review of his patch that addresses a performance regression in `git cat-file` when using unfiltered operations. The regression occurred because bitmaps were being loaded unnecessarily even when no filtering was applied.

Peff explains his design choice to keep the assignment within the conditional, noting that avoiding it would make the logic more convoluted. He suggests the underlying `for_each_bitmapped_object()` API could be improved to return the bitmap index, which would simplify the calling code, but acknowledges this would be a larger change than needed for the immediate fix.

The discussion reveals technical details about bitmap handling: the need to persist the bitmap index for subsequent `bitmap_index_contains_pack()` checks, and the tradeoffs between making a minimal fix versus improving the API for future callers. Peff's response shows the patch is technically sound but leaves open the possibility of future API improvements.

This continues the thread's focus on precise bitmap handling optimizations while maintaining existing functionality. The exchange between these experienced contributors demonstrates careful consideration of both immediate needs and long-term code health, with consensus on the current approach despite minor stylistic preferences.