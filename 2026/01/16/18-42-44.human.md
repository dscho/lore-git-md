This is a minor review comment from Junio C Hamano on the `git last-modified` patch series, specifically addressing how the `--max-depth` option is being documented and exposed in the help text.

Junio points out that the current split approach - where the option handling is moved to `parse_options()` in one commit while the default behavior change comes later - seems inconsistent. He suggests that if the option is being handled directly in the command's own option parsing, both the help text and documentation should be updated in the same step to maintain consistency between `-h` output and the man page synopsis.

The comment is narrowly focused on this documentation/help text synchronization issue, not questioning the overall direction of the series which has already been through several iterations. It's a typical maintainer-level nit about keeping related changes atomic rather than spreading them across multiple commits when they logically belong together.

This doesn't represent any fundamental objection to the feature - the thread context shows the series is otherwise in good shape with comprehensive documentation and test coverage. Junio is just ensuring the patch structure follows Git's conventions for option handling consistency.