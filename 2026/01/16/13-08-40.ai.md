Bugfix for `git last-modified` command. This is a follow-up patch in a completed series that adds input validation to prevent BUG behavior when given a tree OID rather than a commit-ish. The patch introduces `repo_peel_to_type()` to verify the input is a commit object before proceeding, and adds test coverage including both positive (annotated tag) and negative (tree OID) cases.

The change is straightforward - a 5-line addition to the core logic that checks object types before processing, plus corresponding test updates. This completes the hardening of the command against invalid inputs, building on earlier fixes for memory leaks and error handling. The test additions follow the established pattern of the existing test file, checking both that valid non-commit objects (tags) work and that invalid ones (trees) produce clear errors.

This represents the final technical polish for the `last-modified` command after its earlier terminology and UX improvements. The patch maintains the command's established behavior while preventing crashes from edge cases, demonstrating attention to both robustness and backward compatibility.