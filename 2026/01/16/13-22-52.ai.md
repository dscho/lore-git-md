This is a documentation and UX improvement patch for the experimental `git-last-modified` command, part of an ongoing series to refine its behavior and help output. The patch moves the `--max-depth` option handling into the command's own option parsing rather than inheriting it through diff machinery, which enables changing the default behavior in a follow-up commit.

The change makes `--max-depth` appear in the command's help output (`-h`), improving discoverability. It maintains the same underlying behavior while preparing for a planned change to default depth handling. The patch includes clear documentation of the transitional state, noting the `-1` default will change in a subsequent commit.

This continues the thread's focus on making the command's behavior more predictable and its options more visible to users. The implementation remains cleanly integrated with Git's existing diff machinery while providing better control surfaces. The patch follows the series' pattern of incremental improvements to both functionality and documentation.