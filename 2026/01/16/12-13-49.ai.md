This is a documentation patch for the experimental `git-last-modified` command, specifically documenting the `--max-depth` option. The patch is part of an ongoing 3-patch series that has evolved from pure documentation to deeper design discussions about the command's behavior.

The current email shows Toon Claes responding to Karthik Nayak's review feedback about whether to link to `diff-options.adoc` rather than duplicating option documentation. Toon argues against this approach, noting that many diff options aren't relevant to this command. He also discusses technical limitations around handling diffopt flags in `git-last-modified`, suggesting that properly restricting them would require significant refactoring of `setup_revision()`.

Key technical points in this exchange:
- The patch adds `--max-depth` documentation showing it implies `--recursive` when positive
- Karthik suggested linking to existing diff options documentation rather than duplicating
- Toon explains why this isn't practical given many irrelevant diff options
- Discussion reveals architectural questions about how `git-last-modified` handles diffopt flags
- The command inherits all diffopts from `setup_revision()`, including irrelevant ones like `--no-prefix`

The thread has progressed from simple documentation to examining deeper implementation questions about how the experimental command should interact with Git's diff machinery. While this patch focuses on documenting current behavior, the surrounding discussion suggests potential future work to better isolate the command's option handling.