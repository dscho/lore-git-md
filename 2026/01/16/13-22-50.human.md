This is a documentation and UX improvement patch for the experimental `git-last-modified` command, part of a v2 series that changes the command's default behavior and improves its help output. 

The patch moves the handling of the `-z` option (which controls NUL-terminated output) from the diff machinery to the command's own option parsing, ensuring it appears in the help text. This makes the option more discoverable to users while maintaining the same functionality. The change is straightforward - it replaces the diffopt-based line termination check with a direct boolean flag in the command's own struct.

This continues the series' theme of making the command's behavior more transparent and user-friendly. The author (Toon Claes) has been responsive to feedback throughout this thread, with previous iterations addressing documentation gaps and default behavior concerns. The patch is cleanly implemented and well-motivated as part of the ongoing effort to refine this experimental command's UX.