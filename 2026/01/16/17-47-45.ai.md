This is a technical discussion in the final stages of Patrick Steinhardt's 14-part ODB abstraction series, specifically addressing patch 10/14 which converts object iteration to use the new backend-aware interfaces. Justin Tobler notes that while most iteration is now handled generically via `odb_for_each_object()`, packed object iteration remains backend-specific through `packfile_store_for_each_object()`. 

Patrick confirms this is an intentional temporary compromise to avoid expanding the already substantial series, acknowledging that future work could generalize packed iteration. The exchange reveals an implementation detail where bitmap handling currently requires backend-specific code paths. Justin suggests documenting this design decision in the commit message to aid future maintainers, a minor but thoughtful suggestion that Patrick is likely to incorporate before the series merges.

The discussion exemplifies the careful architectural review this foundational refactoring has received, with Justin validating interface decisions across the series. The thread shows consensus on the current approach while leaving the door open for future generalization of packed object iteration. This patch completes the conversion of `builtin/cat-file.c` to use the new ODB interfaces, maintaining functionality while paving the way for pluggable backends.