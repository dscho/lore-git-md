CI optimization patch that skips git-svn tests in leak-checking CI jobs, now queued by the maintainer. The patch from Johannes Schindelin (previously summarized) proposes skipping the Perl-based git-svn tests during memory leak detection runs, as these tests don't exercise C code being checked for leaks and account for significant CI runtime (42+ minutes). Junio C Hamano has reviewed and accepted the change, noting the clear rationale and minimal implementation (a single environment variable addition in `ci/lib.sh`). The patch will now proceed through the integration branches. No further discussion is needed on this straightforward optimization.