This is a reply in the ongoing "What's cooking" thread, where Elijah Newren provides feedback on several in-flight topics mentioned in Junio's status report.

For the `ac/sparse-checkout-string-list-cleanup` patch, Elijah notes it should first restore the `string_list_remove_duplicates()` calls that were previously discussed in other emails (referencing specific message IDs). This suggests the current version may be missing an important cleanup step that reviewers had requested.

Regarding `pw/replay-drop-empty`, Elijah observes that its dependency (`ps/history`) has now stabilized, so this topic should be ready to move forward. The patch teaches `git replay` to drop commits that become empty during the operation (as opposed to those that were empty originally).

Finally, for the major `ps/history` series (a 9-patch series introducing the new `git history` command), Elijah confirms it's ready to graduate to 'next'. This feature provides a new history rewriting interface and has been cooking for several iterations, with dependencies now resolved.

The email shows Elijah acting in his typical role as a merge/rebase expert, providing clear technical assessments of when dependent topics are ready to progress. His comments suggest these patches are technically sound but need minor adjustments (`ac/`) or are now unblocked (`pw/`, `ps/`).