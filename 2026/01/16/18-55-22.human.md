This is a documentation and implementation refinement for the experimental `git-last-modified` command, part of an ongoing series that has evolved through multiple versions. The current discussion focuses on clarifying the relationship between the `--recursive` flag and `--max-depth` parameter, with Junio Hamano providing review feedback.

The patch in question changes the default behavior of `git-last-modified` to be non-recursive (depth=0) by default, while maintaining recursive capability through explicit flags. Junio notes that the documentation could better explain that `--recursive` is equivalent to setting `--max-depth` to any negative value (not just -1), to match the existing description of `--max-depth` behavior.

Junio also questions the implementation's current handling of recursion flags, suggesting that the code could be simplified by eliminating the separate `recursive` flag and having `--recursive` directly set `max_depth` to -1. This would make the behavior more straightforward to reason about while maintaining the same functionality.

The thread shows the command maturing through careful review, with attention to both user-facing documentation and internal implementation cleanliness. As an experimental feature, these refinements are particularly important to establish clear behavior before stabilization.