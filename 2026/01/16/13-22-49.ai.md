This is a documentation patch for the experimental `git-last-modified` command, part of a v2 series that changes the default depth behavior. The patch adds missing documentation for the `-z` NUL-termination option that was already implemented but not previously documented.

The changes include:
1. Adding `-z` to the SYNOPSIS section
2. Documenting the option's behavior in the OPTIONS section
3. Adding an OUTPUT section that shows both the default LF-terminated and NUL-terminated formats
4. Updating the command usage string in the C code to include `-z`

The patch follows the thread's progression from documenting existing behavior to improving UX, with this change focused on making the NUL-termination option more discoverable. It maintains consistency with other Git commands that use `-z` for similar output control. The patch is straightforward and uncontroversial, addressing an obvious documentation gap in the experimental command.