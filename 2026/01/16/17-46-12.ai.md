This is a technical discussion thread about the ODB abstraction series, specifically focusing on patch 7/14 which introduces the `odb_for_each_object()` interface. Justin Tobler is questioning the design choice of passing a `struct object_info` parameter to this function, noting that it seems primarily needed for internal callback use rather than caller configuration.

The exchange reveals an implementation detail of the ODB iteration mechanism: while the `object_info` struct is populated during iteration (with backend-specific data in its union field), Justin's question suggests the API might be over-exposing implementation details. This is a subtle point about API design in the context of the larger ODB abstraction effort - whether iteration state should be fully managed internally or partially exposed to callers.

The thread shows the careful review process this series is undergoing, with Justin probing the architectural decisions even as the patches approach final integration. This particular question touches on the balance between abstraction cleanliness and implementation flexibility in the new ODB interfaces.