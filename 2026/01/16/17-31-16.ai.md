This is a CI/build system patch from Johannes Schindelin that optimizes the leak-checking CI jobs by skipping git-svn tests. The commit provides detailed metrics showing git-svn tests account for ~13% of total runtime (42+ minutes) while offering minimal value for leak detection since git-svn is Perl-based and only exercises Git commands that are already tested elsewhere.

The change adds a simple environment variable `NO_SVN_TESTS` to the leak-checking job configurations in `ci/lib.sh`. The rationale is well-argued - since leak checking only applies to C code and git-svn is Perl, these tests don't contribute to the primary purpose of the leak-checking jobs while significantly increasing CI runtime.

The patch appears straightforward and low-risk, touching only CI configuration. The commit message is thorough, including both quantitative analysis (65 tests taking 42 minutes) and qualitative justification (Perl code not relevant to C leak detection). The author notes they used AI assistance for log analysis, which is transparently disclosed via a trailer.