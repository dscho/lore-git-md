This is a technical discussion about patch 10/14 in Patrick Steinhardt's ongoing object database (ODB) abstraction series. The patch converts `git cat-file` to use the new ODB iteration interface rather than directly calling `for_each_packed_object()`.

Justin Tobler noticed that the conversion still handles packed object iteration in a backend-specific way (using `packfile_store_for_each_object`) rather than using the more generic `odb_for_each_object()` interface introduced earlier in the series. He speculates this might be related to bitmap iteration requirements.

Patrick confirms this was a deliberate choice to limit scope - while making iteration fully generic would be ideal, he decided to defer that work to keep the series manageable. The current implementation maintains existing behavior while still moving toward the pluggable backend architecture. This leaves room for future refinement of the iteration interface, particularly around bitmap handling.

The exchange shows careful technical review from Justin and pragmatic scope management from Patrick on this complex refactoring. The discussion doesn't indicate any blocking issues - just a note about potential future work to make object iteration more uniform across backends.