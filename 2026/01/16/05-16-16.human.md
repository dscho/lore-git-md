This is a follow-up in a discussion about `git clean` behavior with nested repositories. Simon Cheng acknowledges that the existing `-ff` (double force) flag already provides the functionality they initially requested - removing ignored nested repositories. They suggest improving discoverability by adding a hint to the "Skipping repository" message that appears when `git clean` encounters a nested repo, proposing specific wording to inform users about the `-ff` option.

The exchange shows this was a case of the feature already existing but not being well-known, rather than needing new code. Simon's suggestion focuses on making the existing capability more visible through better error messaging. The thread appears resolved from a technical standpoint, with only the UX improvement remaining open for consideration.