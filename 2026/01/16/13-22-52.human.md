This is a documentation and UX improvement patch for the experimental `git-last-modified` command, part of a series that's refining its behavior and interface. The patch moves the `--max-depth` option handling into the command's own option parsing rather than inheriting it through the diff machinery, which allows the option to appear in the command's help output (`-h`).

The change is straightforward - it adds explicit handling for `--max-depth` in `parse_options()` while maintaining the same underlying behavior. The patch notes this is groundwork for changing the default depth behavior in a future commit. The implementation carefully preserves the existing functionality while making the option more discoverable to users.

This continues the thread's trend of improving the command's documentation and user interface. The author (Toon Claes) is systematically addressing feedback from earlier versions, with this change following up on previous addition of `--max-depth` documentation by now exposing it in the help text. The patch is clean and focused, with a clear commit message explaining both the what and why of the change.