This is a discussion about the `git-last-modified` command's depth control behavior, specifically addressing documentation and implementation details in the v2 patch series.

Junio Hamano provides feedback on two aspects of the patch:

1. **Documentation clarity**: He suggests improving the description of how `--recursive` relates to `--max-depth` by making it clear that any negative value (not just `-1`) means no limit, matching the existing documentation for `--max-depth`.

2. **Implementation simplification**: He questions the current logic where `recursive` and `max_depth` flags interact in a complex way, proposing instead to eliminate the separate `recursive` flag entirely and have `--recursive` simply set `max_depth` to `-1`. This would simplify the control flow while maintaining the same behavior.

The discussion builds on previous iterations where:
- The command's behavior was changed to default to non-recursive (depth=0)
- NUL-termination handling was moved to the command's own option parsing
- Documentation was updated to better explain the depth control options

Junio's comments represent final polishing points before this experimental feature could be considered ready for merging, focusing on making the implementation as straightforward as possible while ensuring the behavior is clearly documented. The thread shows the command evolving from its initial implementation through multiple rounds of review toward a more polished and user-friendly design.