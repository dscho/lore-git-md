Here's the updated human-readable summary:

The thread began with Johannes Schindelin proposing to skip git-svn tests during memory leak checks in CI, saving significant runtime. This was quickly expanded to include CVS and Perforce tests, with Junio Hamano approving the approach. The discussion then turned to whether these exclusions should also apply to ASan jobs and how to best name the exclusion variables.

Phillip Wood contributed an important distinction - the tests are skipped not because they interface with foreign version control systems, but because they exercise minimal Git C code where leaks would occur. In the latest message, Junio refines this further, arguing the key factor is whether Git has control over the code being tested, not just its implementation language. He notes that even a C-based git-svn would still depend on external libraries (like libsvn) that Git can't fix, similar to how Git can't address Perl interpreter leaks across all systems.

The discussion remains focused on finding the right principle for test exclusions - one based on what leaks Git can reasonably act on, rather than implementation details. While the practical CI speed improvements are clear, participants are carefully considering how to properly frame and name these exclusions to capture the underlying rationale. The solution may involve a variable like NO_FOREIGN_CODE_TESTS, but with careful attention to what "foreign" truly means in this context.