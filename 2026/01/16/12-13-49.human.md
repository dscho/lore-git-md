This is a documentation patch for the experimental `git-last-modified` command, part of an ongoing series. The patch adds documentation for the `--max-depth` option, which controls directory traversal depth when showing file modification times.

The discussion reveals some architectural tensions. Karthik Nayak suggests linking to existing diff-option documentation rather than duplicating it, but author Toon Claes pushes back, noting that most diff options aren't relevant here. There's also discussion about whether the command should filter out unrelated diff options during parsing, which would require deeper changes to the revision setup code.

The patch itself is straightforward - it documents the option in both the man page and help text, and ensures the option is properly passed through to the underlying diff machinery. The implementation allows negative values (for unlimited depth) and automatically enables recursion when a depth is specified.

This continues the thread's evolution from pure documentation toward deeper design questions about how the command should handle diff options. While the patch itself is uncontroversial, the surrounding discussion hints at potential future work to refine the command's option handling architecture.