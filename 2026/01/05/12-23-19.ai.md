This is a discussion about configuration interface design for the lockfile PID debugging feature, part of the ongoing effort to improve stale lock detection. Patrick Steinhardt weighs in on Jeff King's proposal to simplify the configuration from per-component granularity to a simple boolean.

The thread has already established the technical implementation details: using `~pid.lock` files with tilde separators to avoid refname collisions, cross-platform support including Windows, and comprehensive test coverage. The current debate centers on whether the configuration should maintain subsystem-specific toggles (like `core.fsync`) or switch to a simpler on/off model.

Patrick agrees with Jeff's suggestion that a boolean would suffice, noting that while refs are the most performance-sensitive case where this feature matters, their overhead would dominate any other subsystem's impact. He leaves the door open for future per-component configuration if use cases emerge but expresses no strong preference between the approaches.

This email moves the discussion toward consensus on simplifying the interface, addressing one of the remaining open questions from the thread summary. The technical safety and implementation details appear settled, with only documentation accuracy (per Junio's review) and this configuration design question remaining before final merging.