This is the third patch in Patrick Steinhardt's series to fix geometric repacking with promisor remotes, part of his ongoing repack machinery overhaul. The patch refactors the promisor object repacking code by extracting the finalization logic into a separate function, `finish_repacking_promisor_objects()`, to prepare for reuse by future callers.

The change is a straightforward code reorganization - it splits the existing `repack_promisor_objects()` function into two parts, moving the pack finalization steps (reading pack output and handling the resulting packfiles) into the new helper function. The original function retains the setup and object enumeration logic. This follows a common Git pattern of breaking up larger functions into reusable components when new use cases emerge.

The patch maintains all existing behavior while making the code more modular. The commit message clearly explains the motivation (preparing for a second caller) without over-explaining the simple refactoring. As with Patrick's typical contributions, this is a well-structured change that fits cleanly into his larger repack machinery modernization effort.