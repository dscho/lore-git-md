This is a feature enhancement for `git status` that shows divergence from both upstream and push tracking branches. The thread has seen extensive discussion and iteration, with the implementation now in its final form after addressing all review feedback.

Junio C Hamano points out a subtle but important control flow issue in the latest version of the patch. The original code checked for no differences (`!sti`) before handling the `AHEAD_BEHIND_QUICK` case, ensuring quick mode would only report divergence when branches actually differed. The current patch reversed this order, which could incorrectly report divergence even when branches are in sync. 

Harald Nordgren initially thought removing the `sti` parameter was the right approach based on earlier feedback, but Junio clarifies that simply checking `!ours && !theirs` first (which is equivalent to the original `!sti` check) would maintain the correct behavior without needing to reintroduce the parameter. This preserves the important invariant that quick mode divergence messages only appear when branches actually differ.

The series is now complete with comprehensive test coverage (262 lines) and handles all edge cases including custom refspec mappings and quick mode operation. With Junio's latest guidance on this final control flow issue, the implementation appears ready for merging after one last adjustment to the condition ordering.