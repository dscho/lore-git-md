This is the fourth patch in Patrick Steinhardt's series fixing geometric repacking with promisor remotes, part of his ongoing repack machinery overhaul. The patch is a refactoring that extracts the logic for removing redundant packs into a separate function `remove_redundant_packs()`, making it reusable for future callers.

The change is straightforward - it takes the existing pack removal code from `pack_geometry_remove_redundant()` and moves it to a new static function with slightly different signature (taking raw pack arrays rather than the full geometry struct). The original function becomes a thin wrapper that calls the new one. This prepares for upcoming changes where this functionality will be needed in another context.

The patch maintains all existing behavior while making the code more modular. It's a typical preparatory refactoring in Patrick's style - clean, focused, and clearly motivated by upcoming needs in the series. The commit message explains the "why" concisely without needing to restate the technical details visible in the diff.