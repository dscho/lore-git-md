This is a discussion about the ongoing deprecation of `git whatchanged`. Kristoffer Haugsbakk responds to Wayne Rowcliffe's report of daily usage by explaining the rationale for deprecation and providing migration guidance.

The response notes that `git whatchanged` has been functionally equivalent to `git log` with certain flags for a long time, just with different defaults. The suggested replacement is `git log --no-merges --raw`, with alternatives like `--stat` or `--name-only` offered for better human-readable output.

The email advances the thread by moving from a simple usage report to concrete advice for affected users, though it doesn't address whether Rowcliffe's specific workflow might have nuances not covered by these alternatives. The tone is helpful and explanatory rather than argumentative, focusing on easing the transition rather than debating the deprecation decision itself.