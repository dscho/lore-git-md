This is the third patch in Patrick Steinhardt's series to fix geometric repacking with promisor remotes, part of his ongoing repack machinery overhaul. The patch refactors the promisor object repacking logic by extracting the finalization step into a separate function `finish_repacking_promisor_objects()` from `repack_promisor_objects()`. 

The change is purely structural - no behavior is modified. The existing code that handles reading pack-objects output and processing the resulting packfiles is moved into the new helper function. This prepares for future callers that will need to perform the same finalization steps. The patch maintains all existing functionality including the NEEDSWORK comment about potential delta optimization improvements.

Key technical details:
- Extracted logic handles closing the pack-objects process, reading its output, and processing the generated packfiles
- New `finish_repacking_promisor_objects()` takes the child process, names list and temp directory as parameters
- Original `repack_promisor_objects()` now focuses on setup and object enumeration
- All error handling and cleanup behavior remains unchanged

This is a straightforward refactoring that follows the series' goal of improving the repack machinery's handling of promisor objects while maintaining compatibility with existing behavior. The change appears well-contained and mechanically sound, with no obvious issues in the diff.