This is a review comment on the `git repo info --keys` feature addition. Patrick Steinhardt points out two minor documentation and test style issues in Lucas Seiki Oshiro's patch series.

The first note observes that the preceding commit should have updated the synopsis to include the "default" format option for `git repo info`. The second is a style nit about removing an extra space in a test redirection (`> expect` should be `>expect`).

These are small polish items on what appears to be a nearly-ready patch series. Patrick's comments suggest the implementation is otherwise sound, just needing these final tweaks. The thread context shows this is the third iteration of the patch, with previous versions having addressed more substantive feedback about format handling and option validation.