This is a review comment on a patch series adding `--keys` support to `git repo info`. Patrick Steinhardt points out two missing pieces in Lucas Seiki Oshiro's preparatory refactoring patch that introduced a `FORMAT_DEFAULT` enum value.

The patch in question is part of a series that has already gone through several iterations (now at v3) to implement Junio Hamano's suggestion for a key-listing feature. The core functionality appears solid, with comprehensive format support now implemented after review feedback.

Patrick notes two specific gaps: the lack of a test verifying format precedence (when `--format=nul` and `--format=default` are combined), and the omission of updating `git repo structure` to handle the new "default" format. These are polish items rather than fundamental issues - the kind of final tweaks that surface when a series is nearly ready for integration.

The tone is constructive and the points are well-motivated, typical of Patrick's thorough review style. Since this is part of an otherwise well-reviewed series that has already addressed its major technical questions, these comments likely represent the last steps before the patches can be merged.