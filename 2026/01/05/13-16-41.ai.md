Performance patch from Patrick Steinhardt addressing an incompatibility between geometric repacking and promisor remotes. This is the first in a 5-part series that's part of his ongoing repack machinery overhaul.

The patch modifies `pack-objects.c` to allow combining `--stdin-packs` with `--exclude-promisor-objects`, which previously conflicted. The change is needed to support promisor objects in geometric repacks. The implementation handles two cases: direct pack specification (aborting if a promisor pack is explicitly included while excluding promisor objects) and transitive reachability (modifying the revision walk to respect promisor exclusions).

Key technical details:
- Adds validation to reject self-contradictory requests involving promisor packs
- Modifies revision walk setup in `read_stdin_packs()` to respect promisor exclusions
- Introduces new test cases in `t5331-pack-objects-stdin.sh` covering promisor scenarios
- Maintains existing behavior for non-promisor cases

The patch appears well-constructed with thorough test coverage. It's a focused change that solves a specific technical blocker for the larger geometric repack effort while maintaining backward compatibility. The commit message clearly explains the rationale and edge cases considered.