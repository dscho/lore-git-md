This is a documentation note about the recently merged `git-last-modified` feature. Kristoffer Haugsbakk points out a minor formatting issue in the man page for the command, noting that the pathspec>` markup should use underscores rather than backticks in the synopsis section to match Git's documentation conventions. However, since the patch is already in the `next` integration branch, this correction will need to wait for a follow-up patch.

The thread context shows this is the final approved version of the command, which changed its default behavior to be non-recursive (max-depth=0) while maintaining backward compatibility via the `-r/--recursive` option. The documentation now reflects these defaults and option relationships, though this particular formatting nit was missed during review.

This is a very minor issue that doesn't affect functionality - just documentation presentation. The command's behavior and core documentation are already correct and approved. The pathspec terminology is otherwise standardized throughout the documentation, and all test cases verify the intended behavior. The note serves as a reminder for future documentation cleanup rather than indicating any problem with the merged implementation.