This is a test patch adding coverage for the string-list API, part of Amisha Chhajed's RFC series introducing `string_list_sort_u`. The patch focuses on expanding test coverage in `t/unit-tests/u-string-list.c` to support evaluating the main change in the series.

The patch adds comprehensive unit tests for several string-list functions that previously lacked coverage: `string_list_remove_empty_items()`, `unsorted_string_list_has_string()`, `unsorted_string_list_delete_item()`, `string_list_has_string()`, `string_list_insert()`, `string_list_sort()`, and `string_list_remove()`. The tests follow established patterns from existing string-list unit tests, using helper functions like `t_create_string_list_dup()` and `t_string_list_equal()`.

Test cases cover various edge conditions including empty strings, duplicates, and out-of-bounds scenarios. For example, the `string_list_remove()` tests verify correct behavior when removing non-existent items, while the `string_list_sort()` tests confirm proper ordering of mixed empty and non-empty strings. The implementation appears thorough, with each function getting multiple test cases that exercise both typical and boundary conditions.

This patch directly addresses Junio Hamano's earlier request for complete test coverage before technical review of the main `string_list_sort_u` functionality. The expanded test suite will help ensure no regressions are introduced by the proposed API change while also improving overall robustness of the string-list test coverage. The mechanical nature of the changes and the focus on testing rather than core logic make this a low-risk addition to the series.