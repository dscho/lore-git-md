Performance optimization for the p3400 rebase test setup. Tian Yuchen's v3 patch replaces the shell-based commit generation in the test_perf script with `git fast-import`, cutting setup time from 29s to 22s while maintaining comparable rebase test times (~13s). The approach pre-computes file contents to avoid repeated `test_seq` and `sort` calls, then streams 100 commit pairs via fast-import followed by a repack for consistent object storage.

This iteration addresses all substantive feedback from v2: combining sort operations into a single process, adding proper fast-import stream termination, using standard test environment variables, and switching to temporary files for better error handling. Reviewers Johannes Sixt, Phillip Wood, and Junio C Hamano have signed off on the technical approach, with Junio noting the primary motivation is performance rather than pipe error handling.

The patch appears ready for merging, having resolved all identified issues while delivering a 26% speedup in test setup. Some open questions about delta compression strategies remain for storage experts, but these don't block the immediate improvement. The changes are contained to the performance test script and don't affect production code.