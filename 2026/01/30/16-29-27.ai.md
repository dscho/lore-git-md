Performance optimization for the p3400 rebase test setup. Tian Yuchen's v3 patch replaces the shell-based commit generation (which took ~29s) with `git fast-import` (now ~22s after repacking), while maintaining comparable rebase test times (~12.78s vs original ~13.34s). The key changes are pre-generating file contents (`content_fwd` and `content_rev`) to avoid repeated `test_seq`/`sort` calls, then streaming 100 commit pairs via fast-import with a final `git repack -a -d` for delta compression.

This version addresses all substantive feedback from v2:
- Combines `sort -nr content_fwd` into single process (Johannes Sixt)
- Adds proper fast-import stream termination with `--done` marker (Phillip Wood)
- Uses standard test environment variables and corrects commit message formatting
- Switches to temporary file input for fast-import (Junio C Hamano, Johannes Sixt)

The patch now shows clean error handling and debuggability while preserving its 26% setup speed improvement. Junio's review focuses on implementation refinements rather than raising new concerns, suggesting this is likely to proceed to merging. Open questions remain from storage experts (Jeff King, Patrick Steinhardt) about delta compression's role and alternative repacking strategies, but these don't block the current approach.