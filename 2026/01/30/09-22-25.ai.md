Feature patch from Abraham Samuel Adekunle addressing navigation improvements in `git add -p` (v2 follow-up).

This email responds to Junio Hamano's detailed technical feedback on the v2 patch that adds file navigation between files in interactive staging. The author agrees with several suggested improvements: making the feature optional via configuration, replacing the `get_file_diff_index()` helper by passing file index directly, using an enum for navigation state tracking instead of magic numbers, and potentially splitting the patch into smaller logical changes.

Key technical discussion points:
- The author defends moving patch application from `patch_update_file()` to the caller (`run_add_p()`) to prevent inconsistent states when users change hunk decisions after navigating between files. This addresses a real issue where initial hunk selections could persist incorrectly after being overridden.
- Junio expresses concern about losing incremental updates (where partial work is preserved if interrupted), suggesting the change may negatively impact interactive workflow.
- The thread reveals architectural tension between immediate feedback (current behavior) and transactional consistency (author's proposed change). The author appears receptive to further guidance on resolving this tradeoff.

Implementation notes for next version:
- Will introduce configuration option for the navigation feature
- Will restructure to pass file indices directly rather than using helper function
- Will replace magic numbers with enum for state tracking
- May split into smaller patches for architectural changes vs feature addition
- Needs to address test coverage once UI details are finalized

This remains a quality-of-life improvement for interactive staging, now undergoing deeper architectural review. The core navigation concept has approval but implementation approach may need restructuring based on maintainer feedback about the patch application timing tradeoffs.