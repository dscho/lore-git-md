This is a follow-up discussion in an ongoing thread about improving file navigation in `git add -p`. The patch author (Abraham Samuel Adekunle) has proposed moving the patch application timing from `patch_update_file()` to its caller `run_add_p()` to maintain state consistency when navigating between files, and is seeking maintainer feedback on this architectural change.

Junio C Hamano responds with cautious approval, noting that as long as the new behavior remains optional (configurable), users who prefer the current incremental application behavior can opt out. His "I am not sure" suggests some remaining uncertainty about the tradeoffs, but he seems willing to accept the change if properly gated behind configuration.

The key technical point here is the shift from immediate patch application (current behavior where each hunk decision applies immediately) to transactional application (where decisions are batched until navigation or completion). Junio's conditional acceptance indicates this may be resolved by making it configurable rather than mandatory.

This moves the thread forward by resolving one of the open questions from the previous summary - the patch application timing debate now has a likely path forward via configuration, though the exact implementation details remain to be worked out in the next version. The core navigation behavior itself remains approved, with the architectural discussion now focusing on how to properly expose these timing semantics to users.