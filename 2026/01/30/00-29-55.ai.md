This is a documentation patch finalizing the clarification of `git worktree` terminology and behavior. The v3 iteration, now squashed into a single commit, standardizes the language around "prune" vs "expire" and explicitly documents that `--expire` only affects missing worktrees (those whose working directory paths no longer exist). The patch also expands the description of the `prune` subcommand itself to better explain its use cases.

The changes touch both the man page (`Documentation/git-worktree.adoc`) and in-code help text (`builtin/worktree.c`), bringing consistency to previously mixed terminology ("unused" vs "missing") and making the scope of `--expire` unambiguous. Eric Sunshine confirms this version addresses all prior feedback and is ready for merging to the `next` branch. 

The patch builds on earlier discussion (v1 and v2) where Junio Hamano suggested expanding the subcommand descriptions and reviewers collaborated on wording. With no open questions remaining and maintainer approval indicated by Junio's lack of further requests for changes, this appears to be the final form of a low-risk documentation improvement that clarifies user-facing behavior without modifying any actual functionality.