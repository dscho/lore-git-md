This is a documentation patch converting the `git-blame` options documentation to the new synopsis-style format, part of Jean-Noël Avila's ongoing standardization effort. Michael Lyons splits the work into two patches (this one focusing on `blame-options.adoc`) following maintainer feedback.

The changes are mechanical formatting updates without altering content: standardizing option references with backticks (`` `-L` ``), using `_<placeholder>_` syntax consistently, and improving grammar in some descriptions (e.g., making the `--progress` documentation more precise). The patch follows established patterns from prior documentation conversions, with special attention to complex cases like the `-L` range syntax and porcelain format markers.

This is a well-executed iteration that cleanly implements the maintainer's requested split and refinements. The changes are straightforward and align with the project's documentation modernization goals.