This is a review comment on patch 6/11 in the submodule gitdir path encoding series, which implements a migration command for converting existing submodule gitdirs to use the new path configuration system.

Patrick Steinhardt points out that the error messages in the migration command could be more readable with proper line wrapping. The patch adds a `git submodule--helper migrate-gitdir-configs` command that atomically converts existing submodule gitdirs to use the new `submodule.<name>.gitdir` configuration, with version checking and extension validation.

The series as a whole has already been acked by Junio Hamano for merging, with all major technical issues resolved. This is a minor polish suggestion on the user-facing error messages in an otherwise complete implementation. The feedback is constructive but not blocking, coming from one of the key reviewers who validated the runtime-only approach earlier in the thread.