This is a platform compatibility patch series addressing issues with iconv on macOS 14/15. The two-patch series builds on previous work ('rs/macos-iconv-workaround') and introduces additional workarounds for character encoding conversions on newer versions of macOS.

The first patch prepares the groundwork by adding necessary infrastructure in utf8.c, while the second patch enables the actual workaround. The changes touch the Makefile, config.mak.uname, and utf8.c files, suggesting this involves both build system configuration and runtime behavior modifications.

The patches appear focused and well-scoped, targeting a specific platform issue. The reference to building on existing work ('rs/macos-iconv-workaround') indicates this is part of an ongoing effort to maintain macOS compatibility. The changes are relatively contained (21 lines total) and seem to follow the pattern of previous platform-specific fixes in Git.

For future reference in case this thread continues: the workaround involves modifications to the UTF-8 handling code, with changes to both build configuration (Makefile and config.mak.uname) and runtime behavior (utf8.c). The patches are specifically addressing iconv behavior on macOS versions 14 and 15, suggesting there may be known issues with character encoding conversions on these platforms.