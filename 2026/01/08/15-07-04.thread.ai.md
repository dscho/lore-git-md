Thread identity: `git add -p` UI improvement showing previous hunk decisions during interactive staging, 7th iteration of patch series. Originating author not specified in this summary, but Junio C Hamano suggested the original approach.

Core problem or goal: Improve usability of `git add -p` by making previous hunk decisions (y/n) visible when navigating between hunks, helping users track their selections during interactive staging.

Current status: Patch v7 implements the feature by appending "(was: y)" or "(was: n)" to hunk prompts. Changes are well-contained in `add-patch.c` and tests pass. The implementation tracks decisions via new `hunk_use_decision` string and modifies prompt formatting.

Key technical details: Modifies prompt strings in `add-patch.c` to show previous decisions. Updates tests in `t/t3701-add-interactive.sh` to verify new prompt format works with both regular and colored output. Changes are minimal and focused on the UI improvement without altering core hunk selection logic.

Open questions and loose ends: None mentioned in this summary - appears ready for final review. Previous iterations addressed prompt width and readability concerns.

Participant positions: Junio C Hamano suggested the original approach. Multiple review rounds (given v7 status) but specific reviewer feedback not captured in this summary.

Related work: Builds on existing `git add -p` interactive staging infrastructure. No other related patches or dependencies.