Thread identity: `git add -p` UI improvement showing previous hunk decisions during interactive staging, 7th iteration of patch series. Originating author is a GSoC participant with guidance from Junio C Hamano, who originally reported the usability issue.

Core problem or goal: Improve usability of `git add -p` by making previous hunk decisions (y/n) visible when navigating between hunks, helping users track their selections during interactive staging.

Current status: Patch v7 implements the feature by appending "(was: y)" or "(was: n)" to hunk prompts. Changes are well-contained in `add-patch.c` and tests pass. Version 7 tightens implementation by reducing prompt width, removing redundant checks, and improving readability while preserving the existing interface. Junio's latest response suggests it will likely be merged to 'next' branch soon barring any last-minute objections.

Key technical details: Modifies prompt strings in `add-patch.c` to show previous decisions via new `hunk_use_decision` string. Updates tests in `t/t3701-add-interactive.sh` to verify new prompt format works with both regular and colored output. Changes are minimal and focused on the UI improvement without altering core hunk selection logic. The patch touches only the interactive add interface and doesn't affect other commands or require configuration.

Open questions and loose ends: None remaining - appears ready for final review and merging pending Junio's approval. Previous iterations addressed prompt width and readability concerns.

Participant positions: Junio C Hamano suggested the original approach and has been involved in guiding the GSoC participant through multiple review rounds. The implementation has evolved through 7 versions with incremental improvements based on feedback. As a GSoC project guided by Junio, this represents a successful example of mentored contribution.

Related work: Builds on existing `git add -p` interactive staging infrastructure. No other related patches or dependencies.