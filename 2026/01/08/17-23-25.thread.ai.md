Thread identity: Platform compatibility patch series addressing iconv issues on macOS 14/15, building on previous 'rs/macos-iconv-workaround' work.

Core problem: Character encoding conversions via iconv on newer macOS versions (14/15) require additional workarounds beyond existing macOS-specific fixes. The series aims to maintain consistent UTF-8 handling across platforms.

Current status: Initial two-patch series sent, with first patch adding infrastructure in utf8.c and second implementing the workaround. Total changes span 21 lines across Makefile, config.mak.uname, and utf8.c.

Key technical details: Modifies UTF-8 handling code paths involving iconv conversions. Build system changes in config.mak.uname and Makefile to conditionally enable workarounds for affected macOS versions. Runtime behavior adjustments in utf8.c to handle platform-specific iconv quirks.

Open questions: None raised yet in initial submission. Potential future discussion points could include testing verification on affected macOS versions or whether similar workarounds are needed for other platforms.

Participant positions: Only initial submission by author at this point. Previous work referenced ('rs/macos-iconv-workaround') suggests René Scharfe may have relevant context if they choose to review.

Related work: Builds directly on 'rs/macos-iconv-workaround' series, indicating this is part of ongoing macOS platform compatibility maintenance.