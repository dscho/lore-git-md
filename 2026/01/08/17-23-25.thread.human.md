This thread addresses platform-specific issues with character encoding conversions on macOS 14 and 15. The two-patch series builds on previous work ('rs/macos-iconv-workaround') to introduce additional fixes for iconv behavior on newer macOS versions. The first patch lays groundwork in utf8.c, while the second implements the actual workaround, touching both build configuration (Makefile and config.mak.uname) and runtime behavior. The changes are focused and relatively small (21 lines total), following Git's established pattern for platform-specific fixes. The patches appear well-scoped, targeting a clear compatibility issue without broader implications for other platforms.