This is a documentation patch in a 6-part series improving the `git-patch-id` man page. Patch 5/6 clarifies that `git diff-tree` requires the `--patch` option when used with `git patch-id`, since the default `diff-tree` output format isn't compatible. The change is small but meaningful - it helps users avoid a potential pitfall by being explicit about the required flag.

The patch follows Kristoffer Haugsbakk's established pattern of precise documentation improvements, building on their previous work standardizing terminology and formatting for this command. Like the earlier patches in the series, this is a straightforward correction that aligns with Git's documentation style guidelines. The change affects only the man page, with no code modifications.

This continues the author's focused effort to polish `git-patch-id`'s documentation through small, incremental improvements. The series as a whole demonstrates careful attention to detail in making the command's behavior and requirements clearer to users.