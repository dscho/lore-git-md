Existing AI thread summary:

[No existing summary - this is the thread root]  

AI thread summary:

This thread introduces a two-patch platform compatibility series from Torsten Bögershausen addressing iconv behavior on macOS 14/15. Building on René Scharfe's prior rs/macos-iconv-workaround (merged in master), the series handles cases where macOS's iconv omits byte order marks (BOM) during UTF encoding conversions.

Patch 1 adds build infrastructure: a new ICONV_OMITS_BOM Makefile variable and corresponding config.mak.uname setting for macOS. Patch 2 implements the workaround in utf8.c with special handling for affected macOS versions. Changes are minimal (21 lines across 3 files) and focused on the specific platform quirk, following Git's pattern of targeted compatibility fixes. The commit messages explicitly reference macOS 14/15 as the affected versions.