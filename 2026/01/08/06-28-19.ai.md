This is the fifth patch in a documentation series improving the `git-patch-id` man page. The change is small but precise: it clarifies that `git diff-tree` output needs the `--patch` option to work with `git patch-id`, since the default `diff-tree` output format is raw diff rather than patch format.

The patch continues Kristoffer Haugsbakk's established pattern of documentation improvements, this time focusing on making the `git-patch-id` man page more technically accurate. The change helps users avoid confusion by explicitly stating the required flag when using `diff-tree` output with `patch-id`. This follows naturally from the previous patches in the series which standardized terminology and improved grammar.

The modification is straightforward and unlikely to be controversial - it simply adds missing information that could trip up users trying to use these commands together. The patch maintains the same style and approach as the rest of the series, keeping changes minimal while improving clarity.