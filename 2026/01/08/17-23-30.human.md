This is a platform compatibility patch addressing an issue with iconv on macOS 14/15 (Sonoma/Sequoia). It's part 2 of a 2-patch series (the first patch introduced the workaround, this one enables it).

The patch adds build system logic to detect when macOS's built-in iconv library is buggy (versions 14+) and no alternative from MacPorts or Homebrew is available. In those cases, it enables a workaround (`ICONV_RESTART_RESET`) that was introduced in the previous patch. The changes touch the Makefile and config.mak.uname to add the necessary detection and flag-setting logic.

The patch appears well-motivated and narrowly scoped to address a specific platform compatibility issue. It follows Git's established patterns for macOS-specific workarounds and maintains compatibility with existing build configurations. The commit message clearly explains the conditions under which the workaround will be activated.