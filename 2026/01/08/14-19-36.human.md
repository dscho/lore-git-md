This is a final discussion about edge cases in the PID-based lock debugging feature that was recently implemented. D. Ben Knoble chimes in to reinforce a concern about PID reuse that he had mentioned earlier in the thread - that just because a process with a given PID exists when checking a lock conflict, it doesn't necessarily mean that's the same process that originally created the lock.

The thread context shows this was a comprehensive effort led by Paulo Casaretto to add PID tracking to Git's locking mechanism, with contributions from several core developers. The feature is production-ready with thorough testing and handles most edge cases, but this exchange highlights one remaining theoretical limitation that's inherent to PID-based approaches.

Ben's note serves as a helpful reminder about this subtlety, though it doesn't propose any changes to the implementation since this is a known tradeoff. The discussion appears to be winding down now that all major technical concerns have been addressed. For users, this means the feature will work well in practice while being transparent about its limitations in rare PID-reuse scenarios.