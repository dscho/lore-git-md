Here's a human-readable summary of the thread so far:

Martin Fick has encountered unexpected behavior when trying to use Git's delta islands feature to treat each repository head as its own island. He's found that delta islands appear to ignore objects not covered by bitmaps, which prevents proper isolation between heads. This seems to be either an undocumented limitation or an implementation detail that isn't working as he expected.

The issue involves the interaction between two Git optimization features: delta islands (which group related objects for better delta compression) and bitmaps (which speed up object traversal). Martin has examined the code but found no documentation explaining this behavior. He's asking whether this is indeed a real limitation and if there are any workarounds - specifically whether it's possible to force bitmap creation for all heads to achieve the desired isolation.

This appears to be a technical question about edge case behavior in Git's object storage optimizations, where two features that normally work independently may have some unexpected interactions in this particular configuration.