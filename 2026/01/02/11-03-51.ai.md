This is a test modernization patch, part of the ongoing effort to replace shell primitives with test helper functions in Git's test suite.

Pushkar Singh updates t2021-checkout-overwrite.sh to use `test_path_is_file` and `test_path_is_symlink` instead of the raw `test -f` and `test -h` commands. This change makes test failures more descriptive (the helpers print clearer error messages) and brings the test in line with the project's preferred style for test scripts.

The patch is small and focused, touching only two test assertions while maintaining their original behavior. This is a straightforward improvement that follows established patterns - similar conversions have been made across the test suite by various contributors. The commit message clearly explains the motivation and benefit of the change.