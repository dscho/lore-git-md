This is a test modernization patch, part of the ongoing effort to replace shell primitives with test framework helpers in Git's test suite. Pushkar Singh updates `t/t1300-config.sh` to use `test_path_is_file` and `test_path_is_symlink` instead of raw `test -f` and `test -h` checks.

The changes are straightforward mechanical replacements that improve test robustness and consistency. The test helpers provide better diagnostics when tests fail, making debugging easier. The patch touches only four lines in a single test case that verifies Git's behavior with symlinked configuration files.

This is the second version of the patch, with an expanded commit message that clarifies which shell tests are being replaced (regular file checks vs symbolic link checks). The changes are small and uncontroversial, following established patterns seen in similar test modernization patches from other contributors.