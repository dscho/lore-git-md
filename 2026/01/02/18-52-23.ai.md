This is patch 9/10 in a series refactoring xdiff internals, part of the broader Rustification effort. The patch removes `xdl_cleanup_records()`'s dependency on the classifier by introducing a new `xoccurrence` vector type to track line matches between files.

The key change replaces the classifier's per-line counts with direct tracking using the new `IVec_xoccurrence` structure, which counts occurrences of each line in both files. This allows moving the cleanup logic out of the classifier's domain while maintaining the same functionality. The patch:
- Adds a new `xoccurrence` struct to track line counts per file
- Uses the `ivec` infrastructure introduced earlier in the series
- Updates `xdfenv_t` to track the minimal perfect hash size
- Simplifies `xdl_classify_record()` by removing per-pass counting
- Maintains identical behavior for classic diff while avoiding overhead for patience/histogram diffs

This continues the series' pattern of restructuring xdiff internals to be more modular and Rust-compatible. The changes appear mechanical and focused - the diff shows careful attention to maintaining existing behavior while cleaning up the implementation. As with previous patches in the series, this prepares for future optimizations by separating concerns more cleanly.