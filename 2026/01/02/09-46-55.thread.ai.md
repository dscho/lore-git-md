Thread identity: Test modernization patch updating t1300-config.sh to use test helper functions for file existence and symlink checks.

Core problem or goal: Replace direct shell primitive tests (`test -f`, `test -h`) with Git's test helper functions (`test_path_is_file`, `test_path_is_symlink`) in configuration file tests to improve failure diagnostics while maintaining identical test coverage.

Current status: v2 patch modifying four assertions in t1300-config.sh, specifically targeting regular file and symlink checks. Commit message clarifies the scope is limited to `-f` and `-h` test conversions.

Key technical details: Changes affect test case verifying Git's behavior with symlinked config files. Updates occur in t/t1300-config.sh, replacing:
- `test -f` -> `test_path_is_file`
- `test -h` -> `test_path_is_symlink`

Open questions and loose ends: None explicitly raised, though reviewers may check for other shell primitives in the file that could benefit from similar updates.

Participant positions: No reviews or feedback shown in this initial patch submission.

Related work: Part of ongoing test suite modernization effort to consistently use framework helper functions instead of direct shell tests.