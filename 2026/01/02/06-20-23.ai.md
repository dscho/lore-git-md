Test modernization patch updating t1300-config.sh to use test helper functions instead of direct shell commands. Pushkar Singh replaces four instances of shell primitives (`test -f` and `test -h`) with the more robust test helpers `test_path_is_file` and `test_path_is_symlink` in a test case that verifies Git's behavior with symlinked config files. The changes are straightforward and maintain the same assertions while providing better error messages when tests fail. This appears to be part of the ongoing test modernization effort to standardize on the test helper functions throughout the test suite. The patch is small, focused, and clearly motivated by improving test reliability and maintainability.