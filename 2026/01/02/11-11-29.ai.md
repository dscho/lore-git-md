This is a bugfix patch in the `git replay` error handling series, now at v2. Kristoffer Haugsbakk is addressing feedback from Elijah Newren about root commit handling in the replay command. The patch focuses on cleaning up the FIXME comments and error messages around replaying root commits, though the actual root commit support remains out of scope for this series.

Elijah suggests keeping an updated FIXME comment about future root commit support while removing the current if-block that rejects root commits. He also proposes clearer wording for the error message ("replaying from root commit" rather than "replaying down to root commit") and describes potential future syntax for root commit replay using `--root` or `--advance` with a new branch.

Kristoffer plans to split the changes into two parts: removing the if-block in this patch, and handling the comment/error message updates in a follow-up patch. The discussion shows the series is nearing completion, with only minor wording refinements remaining before merging. The technical changes remain focused on `builtin/replay.c` and its test coverage in `t/t3650-replay-basics.sh`.