This is part 9/10 of Ezekiel Newren's series refactoring xdiff internals to enable future optimizations and Rust interoperability. The patch focuses on cleaning up record handling in the diff preparation phase.

The key change removes `xdl_cleanup_records()`'s dependence on the classifier data structure, instead using the new `ivec` (interoperable vector) infrastructure introduced earlier in the series to track line occurrences. This allows the function to be moved to `xdiffi.c` in a future change (though not done in this patch). The modification also means the patience and histogram diff algorithms won't pay the cost of line counting they don't need.

The patch introduces a new `xoccurrence` struct to track line counts between files and uses the `ivec` type to manage this data. It maintains the same cleanup behavior while making the code more modular. The changes touch core diff preparation logic but appear carefully implemented with thorough updates to all affected code paths.

This continues the series' pattern of methodical refactoring that maintains existing behavior while improving code organization. The changes are focused on the xdiff subsystem internals and build on the infrastructure work from earlier patches in the series.