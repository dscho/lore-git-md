Here's a human-readable summary of the thread so far:

Rostislav Krasny proposed modernizing Git's byte-swapping functions in `compat/bswap.h`, replacing manual bit manipulation with `memcpy`-based approaches for better maintainability and potential performance gains. The changes cover the full network byte order API while adding new 16-bit variants and renaming functions for consistency.

Jeff King (Peff) provided a thorough analysis of the benchmark results, noting that while some compiler optimizations showed speedups (particularly with Clang at -O2), the real-world impact might be limited. He acknowledged the code quality improvements but questioned whether the performance claims held up under scrutiny.

In response, Krasny clarified that the primary goal is code simplicity and consistency across the byte-swapping functions, with performance being a secondary benefit. He agreed that production builds typically use higher optimization levels where the differences are less pronounced, while maintaining there's no performance regression. The discussion highlights a difference in perspective - Peff evaluating the changes through a performance lens while Krasny emphasizes maintainability improvements to this critical low-level code.

The thread remains open for further review, particularly around portability across different architectures and compilers, as well as whether the benefits justify modifying these sensitive functions. No new code changes have been proposed yet in response to the feedback.