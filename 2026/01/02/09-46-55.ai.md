This is a test modernization patch that updates t1300-config.sh to use Git's test helper functions instead of direct shell primitives. The patch replaces `test -f` with `test_path_is_file` and `test -h` with `test_path_is_symlink` in a test case that verifies Git's behavior with symlinked configuration files.

The changes are mechanical and focused on improving test diagnostics - the test helpers provide more descriptive output when assertions fail compared to the raw shell tests. The v2 iteration clarifies in the commit message that these changes specifically target regular file checks (`-f`) and symbolic link checks (`-h`).

The patch touches only the test file, modifying four assertions while maintaining the same test logic. This appears to be part of the ongoing effort to modernize Git's test suite by using the framework's helper functions consistently. The changes are straightforward and unlikely to be controversial, though reviewers might check whether there are other similar shell primitives in the file that could also be updated.