This is a test modernization patch, part of the ongoing effort to replace shell primitives with test helper functions throughout Git's test suite.

Pushkar Singh updates `t/t1300-config.sh` to use the test framework's helper functions `test_path_is_file` and `test_path_is_symlink` instead of the raw shell commands `test -f` and `test -h`. These helpers provide better error messages when tests fail and help maintain consistency across the test suite.

The changes are straightforward mechanical replacements in a test that verifies Git can read configuration through symlinked files. The patch maintains all existing test logic while improving the test's diagnostic capabilities. This is a small but worthwhile improvement that aligns with the project's test modernization efforts.