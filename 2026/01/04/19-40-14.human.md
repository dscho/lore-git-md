This is a bugfix discussion about handling non-UTF-8 encoded `.gitignore` files, particularly focusing on UTF-16LE files with BOM created by Windows PowerShell. The thread has seen input from multiple contributors, with brian m. carlson now weighing in with a counterpoint to the original proposal.

brian m. carlson challenges the fundamental premise that Git assumes UTF-8 for exclude files, pointing out that Git historically allows arbitrary byte sequences to match Unix filename conventions. They demonstrate this with a test case showing Git handling non-UTF-8 patterns correctly. The core issue appears to be Windows-specific, where UTF-16LE files with BOMs are common but Git expects UTF-8 encoded paths.

The response argues against the proposed comprehensive Unicode handling solution, noting that:
1. Git doesn't strip BOMs in other contexts (like `git diff` output)
2. The problem might be better addressed through Windows-specific documentation or tooling
3. Any changes would need to be consistent across all text-based Git files (configs, attributes, etc.)

This moves the discussion toward whether to implement a narrow Windows-specific fix versus maintaining the current cross-platform behavior. The technical debate now centers on whether UTF-8 should be enforced for exclude files (with escape mechanisms) or if the status quo of arbitrary byte handling should remain. The exchange highlights Git's tension between platform conventions and universal behavior.