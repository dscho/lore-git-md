This is a test modernization patch in the ongoing effort to standardize Git's test suite. Pushkar Singh's patch converts direct shell `test` commands to the test helper functions `test_path_is_file` and `test_path_is_symlink` in t1300-config.sh, which tests Git's handling of symlinked config files.

Junio C Hamano points out a minor terminology correction in the commit message - the `test` command is not technically a shell primitive (unlike language keywords like `if` or `for`), even though it's commonly built into shells. This is a nit rather than a substantive objection to the change itself.

The patch has already received positive review from Karthik Nayak, who confirmed it aligns with the test suite modernization goals. Pushkar indicates they'll send a v2 with an updated commit message to address Junio's wording suggestion. The change maintains the same assertions while improving test diagnostics, following the established pattern for similar conversions in the test suite. This appears to be a straightforward improvement that's on track for merging.