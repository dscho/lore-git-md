This is a bugfix submission for the `git subtree` regression identified in the thread. The reporter provides a minimal reproduction script that clearly demonstrates how commit 83f9dad7d6 ("subtree: fix split after annotated tag was squashed merged") broke subtree operations when dealing with linearized squash commits.

The key technical insight is that the regression occurs when:
1. A repository contains multiple subtrees
2. One subtree (subB) was added via a linear squash commit (not the normal merge commit)
3. Another subtree (subA) has changes committed after this linear squash commit
4. The new `should_ignore_subtree_split_commit()` logic fails to properly traverse ancestry when examining commit trailers

The reproduction script creates this exact scenario and shows the split operation producing an incomplete history (fewer commits than expected). The reporter also confirms the issue doesn't exist in Git 2.43.0 (pre-dating the problematic commit) and provides timing metrics showing the regression also introduced a performance impact.

This is a well-constructed bug report with:
- Clear demonstration of the regression
- Minimal reproduction case
- Version comparison showing working vs broken behavior
- Technical analysis of the root cause
- Evidence that tree objects remain correct (only history reconstruction is broken)

The next steps would likely involve either:
1. Reverting 83f9dad7d6 if its fix was too narrow
2. Modifying the commit trailer examination to properly handle ancestry
3. Adding special case handling for linear squash commits

The patch quality is high - it provides everything needed to understand and fix the issue while being careful to isolate the specific failure mode. The reporter's methodical approach (from initial monorepo reproduction to minimal test case) makes this particularly valuable.