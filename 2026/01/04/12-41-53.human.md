Test modernization patch updating shell test assertions to use helper functions. Pushkar Singh's v3 revision replaces direct `test -f` and `test -h` calls with `test_path_is_file` and `test_path_is_symlink` in t1300-config.sh and t2021-checkout-overwrite.sh. The changes maintain the same assertions while providing better diagnostics when tests fail.

This version incorporates Junio C Hamano's feedback about avoiding the phrase "shell primitive" when referring to `test` in the commit message. The patch has already received positive review from Karthik Nayak confirming it improves test diagnostics without changing the underlying assertions. The changes are part of Git's ongoing test suite modernization effort to standardize on helper functions.

The patch is straightforward and uncontroversial, with no substantive objections raised during review. The v3 update addresses the minor wording clarification from the maintainer, putting it in good shape for merging. The changes touch test files that verify Git's behavior with symlinked configuration files and checkout operations involving symlinks.