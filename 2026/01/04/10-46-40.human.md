This is a bugfix patch for the reftable backend, addressing undefined behavior in the indexed table reference iterator. The issue occurs when accessing union members without first checking the reference type, which could lead to reading invalid memory.

The patch fixes `indexed_table_ref_iter_next()` to properly check the `value_type` before accessing either `val1` or `val2` members of the union, matching the pattern already used elsewhere in the code. The change is straightforward and clearly correct - it was even marked with a "BUG" comment indicating the issue was known but not yet addressed.

This appears to be a solid, focused fix that properly handles both single-value (VAL1) and double-value (VAL2) reference types. The patch is small (13 lines changed) and touches only the reftable iterator logic, with no apparent side effects beyond fixing the undefined behavior. Given that reftable is a relatively new and actively developed backend, such correctness fixes are important for stability.