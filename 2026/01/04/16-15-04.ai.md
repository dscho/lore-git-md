Test modernization patch updating `t1420-lost-found.sh` to use `test_path_is_file` instead of raw `test -f` for better error reporting. This is part of the ongoing effort to modernize Git's test suite by replacing direct shell test commands with the project's dedicated test helper functions.

The change replaces two instances where the test was checking for the existence of files in the lost-found directory after a `git fsck` operation. The new version using `test_path_is_file` will provide more descriptive error messages if the assertions fail, making it easier to diagnose test failures. This is a straightforward, mechanical change that follows established patterns seen in other test modernization patches.

The patch is small and focused, touching only the test file with no behavioral changes. It appears to be a good candidate for merging as part of the test suite cleanup effort. The commit message clearly explains the motivation (avoiding silent failures) and the change itself is simple and obviously correct.