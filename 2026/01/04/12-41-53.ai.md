This is a test modernization patch, part of the ongoing effort to standardize on test helper functions throughout Git's test suite. Pushkar Singh updates t1300-config.sh and t2021-checkout-overwrite.sh to use test_path_is_symlink and test_path_is_file helpers instead of direct shell test commands.

The patch replaces `test -h` with `test_path_is_symlink` and `test -f` with `test_path_is_file` in test cases that verify Git's behavior with symlinked config files and checkout operations. These changes maintain the same assertions while providing better error diagnostics when tests fail. The v3 iteration incorporates Junio C Hamano's feedback about avoiding the term "shell primitive" in the commit message, though the technical content remains unchanged.

The changes are straightforward mechanical conversions following established patterns from prior test modernization work. Karthik Nayak has already reviewed the patch positively, confirming it improves diagnostics without altering the test assertions. With the wording clarification addressed in v3, this appears ready for merging as part of the broader test suite cleanup effort.