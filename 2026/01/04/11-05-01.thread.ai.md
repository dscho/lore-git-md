Git thread summary:

Bug report and proposed fix for `git stash` behavior when untracked directories conflict with previously tracked files. Current implementation uses `reset --hard` which silently overwrites untracked content, while contributor suggests safer `reset --merge` that would fail instead of destroying data. Issue is reproducible and already documented as known but unfixed in test suite (`t/t2500-untracked-overwriting.sh` marked `test_expect_failure`).

Proposed change modifies `builtin/stash.c` to use `--merge` flag. Contributor (a beginner) seeks confirmation this is indeed a bug worth fixing and whether solution is acceptable. Change appears well-motivated as it prevents data loss while maintaining same behavior in non-conflict cases. Presence of existing test case suggests prior awareness of issue, though unresolved status unclear.