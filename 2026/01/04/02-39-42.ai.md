This is a test modernization patch that replaces shell commands with test helper functions in t1300-config.sh. Junio C Hamano provides a minor correction to the commit message's terminology, noting that `test` is not technically a shell primitive (unlike `if` or `for`) even though it is often implemented as a shell builtin. 

The patch itself (from Pushkar Singh) updates tests to use `test_path_is_file` and `test_path_is_symlink` instead of direct `test -f` and `test -h` invocations, which improves diagnostic output while maintaining the same assertions. This change is part of the ongoing test suite modernization effort to standardize on test framework helpers. 

Junio's comment is purely about wording - he's not objecting to the technical content of the patch, which has already been reviewed positively by Karthik Nayak. The thread shows this is a straightforward improvement that's on track for merging once the commit message is updated to use more precise terminology. The changes focus on test infrastructure quality without modifying Git's actual config file handling behavior.