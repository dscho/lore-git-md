This is a documentation patch clarifying the pack protocol specification, with Junio Hamano providing a maintainer's review.

The patch from Heath Dutton adds a missing definition for 'timestamp' in the pack protocol grammar, mirroring the existing format specification for 'depth' (both defined as '1*DIGIT'). This addresses an inconsistency in the protocol documentation where 'timestamp' was referenced but not formally defined. Junio confirms the change is correct, noting the timestamp format aligns with the underlying implementation that uses parse_timestamp() (essentially strtoumax()).

The review is straightforward - Junio verifies the technical accuracy of the change and its historical context (the timestamp support was added in commit 569e554b for the deepen-since feature). There are no outstanding questions or requested changes. The patch appears ready for inclusion as a simple documentation improvement to make the protocol specification more complete and precise.

For future reference: this touches Documentation/gitprotocol-pack.adoc, specifically the grammar section defining protocol elements. The change adds consistency between how 'depth' and 'timestamp' are specified, both now explicitly defined as sequences of digits ('1*DIGIT'). The clarification helps protocol implementers by removing ambiguity while matching existing behavior.