This is a follow-up in the fsmonitor memory leak thread, where the fix should land. The original patch (v2) fixed a 40-byte memory leak per client request in the fsmonitor daemon's `do_handle_client()` by properly using `kh_destroy_str()` instead of `kh_release_str()` for khash table cleanup. 

Junio notes this fix is already included in v4 of a larger fsmonitor-linux series, but Paul Tarjan explains he submitted it separately since it addresses an existing bug that may warrant quicker merging. The technical solution has been reviewed and confirmed correct by René Scharfe, with no outstanding objections. This exchange represents the final coordination step before the fix can be merged, either as part of the larger series or as an independent patch. The discussion is now effectively complete, with Junio's "Thanks" signaling acceptance of the separate submission rationale.