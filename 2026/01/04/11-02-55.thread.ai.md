Here is the dense AI thread summary:

This is a v4 patch series from a GSoC participant improving `git add -p`'s UI by showing previous hunk decisions (use/skip) when navigating between hunks with 'K' (previous) and 'J' (next) commands. The change modifies prompt strings in `add-patch.c` to display "(previous decision: use)" or "(previous decision: skip)" when revisiting hunks, helping users track their choices. The implementation updates the `prompt_mode` struct fields and affects all interactive patch modes (stage/unstage, stash, reset, checkout). Key technical details: uses `USE_HUNK`/`SKIP_HUNK` constants, checks `hunk->use` via `hunk_use_decision()`. Test coverage is thorough - `t/t3701-add-interactive.sh` is updated to expect new prompt formats across all modes. The v4 fixes a macOS-specific test case skipped in v3. Junio Hamano requested this improvement as part of ongoing work to enhance the interactive patch interface. The changes are mechanical but comprehensive, touching prompt strings and test expectations while maintaining clean separation between display logic and core functionality.