Bug report thread summary: Jon Forrest reports inconsistent behavior in `git add` when handling ignored vs missing files. Current behavior: `git add` fails completely when given a mix of existing and non-existent files (adding nothing), but partially succeeds when given a mix of ignored and non-ignored files (adding non-ignored files with warning). Reproduction shows `git add file1 jon` (file1 ignored, jon exists) adds jon while warning about file1, whereas same command with non-existent file would add nothing. Open question: Is this difference in behavior intentional? Issue touches error handling logic in `git add` - should ignored and missing files be treated equivalently (either fail completely or succeed partially in both cases)? Could be bug (if behaviors should be consistent) or documentation issue (if difference is intentional but undocumented). Well-structured report with clear test cases.