Heath Dutton has submitted a documentation patch to clarify the pack protocol specification. The patch addresses an inconsistency where the protocol grammar defined 'depth' with a formal specification ('1*DIGIT') but referenced 'timestamp' without similar precision. The change adds the missing definition, making it clear that timestamp should also be specified as one or more digits.

The change is minimal - just a single line addition to Documentation/gitprotocol-pack.adoc that mirrors the existing depth definition. While the commit message references an external issue tracker (not the standard Git project infrastructure), the patch itself appears well-formed and uncontroversial, cleanly addressing a documentation gap in the protocol specification.