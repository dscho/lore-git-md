This is a bugfix discussion about path formatting inconsistencies in `git status` when using NUL-terminated output (`-z`) with the `status.relativePaths` configuration. The thread has moved from initial bug report to design clarification, with Jeff King (Peff) identifying two distinct issues and proposing a partial fix.

Pushkar Singh confirms Peff's analysis and offers to help with further investigation or testing once the intended behavior is clarified. The key open questions are whether porcelain v2 should respect `status.relativePaths` (currently does but undocumented) and whether to preserve historical quirks or fix them. Peff's proposed fix introduces a `print_with_nul` helper function to standardize NUL-terminated output handling in the `--short` case, but notes it needs expansion to cover all affected code paths.

The discussion now awaits input from status.c subsystem experts to determine the correct semantics. The issues span multiple output modes: porcelain v1 with `-z` intentionally bypasses config (historical behavior), while porcelain v2 with `-z` and `--short -z` exhibit inconsistent behavior. The interaction between `status.relativePaths` and `-z` appears undertested, and porcelain v2's config respect is undocumented.