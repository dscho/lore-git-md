Here is the dense AI thread summary:

RFC patch series to add warnings about skipped merge commits in `git format-patch`. Original author not yet identified in project context. Core change is to make format-patch more transparent when it silently skips merge commits during patch generation.

Current status: v1 sent as RFC, received from Junio Hamano and Jeff King (Peff). Junio suggested limiting warnings to cases where no patches are generated at all, avoiding noise for expected skips like back-merges. Jeff identified a potential NULL-deref when handling root commits (commit->parents NULL check needed).

Technical details: modifies `builtin/log.c` to track merge commits separately from patch count. New warning message is concise: "No patch generated. Note merge commits are skipped." Only shown when both conditions met: (1) zero patches generated, (2) at least one merge commit was skipped. Does not show per-commit details to avoid verbosity.

Open items: author plans to send v2 with test coverage for the new warning behavior. No outstanding objections to the approach, just implementation refinements needed. Changes are small and focused on merge commit handling in format-patch.

Participant positions: Junio - agree with warning but only when no output; Jeff - identified NULL-deref bug, implicit approval of concept with that fix. No other reviewers yet.