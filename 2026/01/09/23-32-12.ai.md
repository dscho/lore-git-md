This is a review response in the ongoing thread about geometric repacking with promisor remotes. Taylor Blau is responding to Patrick Steinhardt's first patch in the series, which enables combining `--stdin-packs` with `--exclude-promisor-objects` in `git pack-objects`.

Taylor points out that the error message for the conflicting flags case is currently "cannot use internal rev list with --stdin-packs" rather than something more specific about promisor objects. He also questions why the `--stdin-packs=follow` case needs separate handling from the base `--stdin-packs` case, asking Patrick to elaborate on the distinction.

The review notes that the test case could potentially be simplified by using `test_commit` helper instead of manually creating commits, though this is a minor style suggestion rather than a substantive issue. Taylor explicitly holds off on code-level comments to focus first on the higher-level design questions about promisor object handling.

This is an early technical review from someone deeply familiar with Git's pack machinery (Taylor has contributed significantly to the pack subsystem) - his questions suggest the patch may need more explanation of its design choices around promisor pack handling before proceeding. The tone is collegial and focused on understanding the implementation rationale.