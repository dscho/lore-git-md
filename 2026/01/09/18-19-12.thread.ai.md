Bugfix patch addressing compiler warnings in hook subsystem. Fixes potential NULL pointer dereferences in `run_hooks_opt()` by moving initialization of struct fields after NULL check on `options` parameter. While current callers never pass NULL, change makes code more robust against future misuse and silences static analyzer warnings. Also corrects error message referencing wrong function name (`run_hooks` instead of `run_hooks_opt`). Changes suggested by Patrick Steinhardt who noted two additional needed fixes in v2. Patch based on `next` branch where warning was introduced. Changes are behavior-preserving for correct usage but add defensive programming. CI passes.