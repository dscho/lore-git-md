This is a follow-up discussion about a memory leak fix in the shallow repository handling code. Patrick Steinhardt confirms that Samo Pogačnik's patch 1/2 correctly fixes a memory leak in `get_shallow_commits()` by freeing the `object_array` allocations, though he notes the original commit message could be clearer about which specific allocations were being leaked (the array itself rather than its elements).

The key new information here is that Samo's test case from patch 2/2 fails leak-checking CI runs without this fix, empirically validating the leak's existence. Patrick suggests improving the commit message to mention this test coverage, making the fix's necessity obvious to reviewers. The exchange shows the patch is technically sound but could benefit from clearer documentation of how the issue was discovered.

This email doesn't introduce new technical changes - it's a confirmation that the leak fix works as intended and a request for better commit message documentation. The thread continues to show Patrick's characteristic focus on test coverage and documentation quality alongside technical correctness.