Simon Cheng proposed adding a new flag to `git clean` to remove ignored nested Git repositories, which would help with build system workflows like Arch Linux's AUR`makepkg` that creates temporary source repositories. The initial discussion considered whether this warranted a new flag versus modifying existing behavior.

brian m. carlson has now pointed out that the existing `git clean -dxff` (with a second `-f` flag) may already handle this case as documented, and asked whether this meets Simon's needs. If not, they've requested a concrete reproduction case to better understand the scenario. The thread is now focused on determining whether the requested functionality already exists or if there's a specific edge case that requires new development. This helpful response moves the discussion toward either confirming the existing solution works or getting more details about any remaining gaps.