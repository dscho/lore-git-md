This is part 12 of Patrick Steinhardt's 17-patch series refactoring Git's ref backends (files and reftable) to improve consistency checks. The series is building shared verification logic that will work across both backends.

This patch is a straightforward refactoring that extracts the worktree backend lookup logic from `backend_for()` into a new `backend_for_worktree()` function. The change is mechanical - no behavior is altered, just code organization. The new function will be reused in upcoming patches that implement validation logic.

The patch follows the series' pattern of preparing the reftable backend to match the files backend's verification capabilities. It touches only `refs/reftable-backend.c`, moving 27 lines into the new function while keeping the same error handling and initialization patterns. The change is progressing cleanly through the series' three phases, with this being part of the shared verification infrastructure work.