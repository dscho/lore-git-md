This is a test modernization patch, part of the ongoing effort to update Git's test suite to use more informative helper functions. The patch updates t/t7101-reset-empty-subdirs.sh to replace direct `test` operations with the modern test helper functions:

- Replaces `test -f` with `test_path_is_file`
- Replaces `test -d` with `test_path_is_dir` 
- Replaces `! test -[df]` with `test_path_is_missing`

The changes are mechanical but improve test output by providing more descriptive failure messages. The patch also fixes a typographical error in a test description ("checking checking" -> "checking") and corrects a path reference in another test description (path1/COPYING-TOO -> path0/COPYING-TOO).

This appears to be a straightforward, well-scoped contribution from a new contributor (the email address suggests this may be part of a Google Summer of Code project). The changes align with the project's test modernization efforts and don't modify any actual test logic - just the way assertions are made. The patch is small (18 lines changed) and focused on a single test file.