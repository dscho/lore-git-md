This is a follow-up discussion about a memory leak fix in the shallow repository fetching code. The original patch (1/2 in the series) added `object_array_clear(&stack)` in `get_shallow_commits()` to fix a leak. Patrick Steinhardt had reviewed it, confirming the fix while questioning the commit message phrasing and suggesting improved test coverage.

In this email, Samo Pogačnik responds to Patrick's review, noting that the test case added in patch 2/2 actually fails in leak-checking test runs (`linux-leaks` and `linux-reftable-leaks`) without this memory fix. This provides concrete evidence that the leak is real and the fix is necessary, addressing Patrick's question about test coverage.

The exchange shows the patch is progressing toward acceptance, with the author providing additional validation of the fix's importance. The discussion remains focused on technical details, with Patrick's review style evident in his attention to precise wording and test coverage. The thread context indicates this is part of a larger effort to improve shallow repository handling, with patch 2/2 implementing core functionality for relative-depth fetching in repos with merged branches.