This is a feature patch adding `--format=default` support to `git repo info` and `git repo structure` commands, part of a series to improve programmatic repository metadata access. The patch introduces a `FORMAT_DEFAULT` enum (0-value) to cleanly handle default format cases, making the output format behavior more explicit and consistent across commands.

The changes touch documentation, command implementations, and tests. For `git repo info`, the default format is now explicitly set to `FORMAT_DEFAULT` which maps to `FORMAT_KEYVALUE`, while `git repo structure` similarly maps `FORMAT_DEFAULT` to `FORMAT_TABLE`. The patch includes tests verifying that `--format=default` works as a synonym for the default format and that it can reset the format after a non-default specification.

This appears to be well-executed work that follows through on Junio Hamano's earlier suggestions, with thorough test coverage and clean implementation. The changes are part of a larger effort to improve machine-readable repository metadata access while reducing test maintenance burden. The patch series is in its final polishing phase with all major technical questions resolved.