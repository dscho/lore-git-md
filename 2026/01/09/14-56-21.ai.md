This is a refactoring patch in a series that enhances `git status` to show divergence from both upstream and push tracking branches. The patch extracts the branch comparison formatting logic from `format_tracking_info()` into a new `format_branch_comparison()` function, while maintaining all existing behavior.

Phillip Wood reviews the refactoring and suggests two minor improvements: making the `sti` parameter more descriptive by renaming it to `up_to_date`, and changing some `int` parameters to `bool` where appropriate. He confirms the refactoring is faithful to the original code and agrees with moving the upstream-is-gone handling to the caller.

The changes preserve the precise control flow needed for quick mode (`AHEAD_BEHIND_QUICK`) where branch states must be checked in a specific order. The patch is part of a well-reviewed series that has gone through 17 iterations, with all technical requirements now satisfied and comprehensive test coverage in place. The series is considered merge-ready pending maintainer action.