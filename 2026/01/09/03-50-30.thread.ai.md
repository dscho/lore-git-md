Thread summary:

Simon Cheng proposes adding a flag to `git clean` to remove ignored nested Git repositories, motivated by Arch Linux's AUR packaging system where `makepkg` creates source repositories in `./src/NAME` directories that need cleanup after builds. The initial proposal would modify `builtin/clean.c` and require documentation updates, presenting this as a new flag need. brian m. carlson responds by pointing to the existing `git clean -dxff` (double `-ff`) behavior which is already documented to handle nested repositories, asking if this meets Simon's needs or if there's a specific case it doesn't cover. The thread is focused on determining whether the feature request can be satisfied by existing functionality or requires new code changes. Key open questions are whether Simon's AUR use case has special requirements not addressed by `-ff`, and if not, whether documentation should be made more prominent about this existing capability. The discussion remains solution-oriented, with brian m. carlson seeking concrete reproduction steps if the current behavior is insufficient.