This is a follow-up discussion in Samo Pogačnik's shallow repository patch series, specifically addressing feedback on patch 2/2 which implements relative-depth fetching (`--deepen`). The thread context shows this is part of a 2-patch series where patch 1/2 fixed a memory leak in shallow.c.

Patrick Steinhardt had previously requested several improvements to patch 2/2: (1) a more explanatory commit message with ASCII graph, (2) removal of debug output, (3) shell loop formatting fixes, and (4) potential logic deduplication between `get_shallows_depth()` and `get_shallow_commits()`. In this response, Samo acknowledges Patrick's feedback and indicates plans to address all points, including updating patch 1/2's commit message to reference how the test in patch 2/2 fails without the leak fix.

The technical changes remain focused on shallow repository handling, with the key addition being the new `get_shallows_depth()` function that replaces `get_reachable_list()`. The discussion shows steady iteration, with Patrick providing detailed code review while acknowledging the core algorithm appears sound. Samo's response suggests the series is moving toward finalization once the documentation and test hygiene issues are addressed.