Patrick Steinhardt's 17-patch series refactors Git's ref backends (files and reftable) to improve consistency checks. The series has three phases: files backend validation (1-8), shared verification infrastructure (9-14), and fsck check migration (15-17). Patch 15/17 migrated the zero-OID ref check to `refs_fsck_ref()`, while patch 16/17 now moves HEAD ref validation from `builtin/fsck.c` to `refs_fsck_symref()`, introducing `FSCK_MSG_BAD_HEAD_TARGET` for structured error reporting.

The changes standardize verification across backends by centralizing logic in `refs_fsck_ref()` and `refs_fsck_symref()`, with both files and reftable backends now performing identical checks. Test updates in t0602/t0614/t1450 reflect the new error message formats, including a more realistic invalid ref test case (`refs/heads/../HEAD`). The series maintains focus on `refs/files-backend.c`, `refs/reftable-backend.c`, `refs.c`, and `builtin/fsck.c`.

Key technical details:
- New error codes: `FSCK_MSG_BAD_REF_OID` (patch 15), `FSCK_MSG_BAD_HEAD_TARGET` (patch 16)
- Shared infrastructure: `refs_fsck_ref()`, `refs_fsck_symref()`, `backend_for_worktree()`
- Backend coverage: files and reftable now use identical validation paths
- Test impact: updated expectations in t0602 (reftable), t1450 (fsck)

One remaining patch (17/17) will complete the fsck check migration. The series demonstrates consistent progress with each patch building on the shared verification framework established in patches 9-14.