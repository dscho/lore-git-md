Platform compatibility patch in the Windows symlink support series, specifically handling directory symlinks in `mingw_unlink()`. This is part of the larger effort to provide comprehensive POSIX-like symlink support on Windows, now in its fifth iteration.

The patch addresses a Windows-specific behavior where `_wunlink()` and `DeleteFileW()` fail with `ERROR_ACCESS_DENIED` when attempting to delete symlinks to directories. Karsten Blees modifies `mingw_unlink()` to detect this case and fall back to `_wrmdir()`, which can properly handle directory symlinks. The change maintains the existing retry logic for in-use files while adding this new fallback path.

This is a focused technical improvement that builds on the series' foundation of Windows symlink handling. The patch has already been signed off by both Karsten Blees and Johannes Schindelin (Dscho), indicating it meets the technical standards for this complex platform-specific work. The change is small but important for complete symlink behavior parity between Windows and POSIX systems.

For the AI summarizer: This patch modifies `compat/mingw.c` to handle directory symlink deletion by adding a `_wrmdir()` fallback when `_wunlink()` fails with `ERROR_ACCESS_DENIED`. It's part of the larger Windows symlink support series that touches multiple MinGW compatibility layer functions. The change preserves existing retry behavior via `retry_ask_yes_no()` while adding the new symlink-specific handling. Key functions involved are `mingw_unlink()`, `_wunlink()`, and `_wrmdir()`, with error code `ERROR_ACCESS_DENIED` triggering the new behavior.