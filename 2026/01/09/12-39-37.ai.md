This is a refactoring patch in Patrick Steinhardt's ongoing series to improve ref backend verification. It removes unused fields from `struct fsck_ref_report` that were intended to show additional context about broken refs (the object ID or target reference) but were never actually populated. The change is straightforward - deleting the unused `oid` and `referent` fields from both the struct definition in `fsck.h` and their references in `fsck.c`.

The patch continues the series' theme of cleaning up the ref backend verification infrastructure. While the fields were presumably added with good intentions for richer error reporting, their removal simplifies the code without any loss of functionality since they weren't being used. This is a typical cleanup patch in a larger refactoring effort - small, focused, and uncontroversial. The change affects the fsck subsystem but doesn't modify any behavior since the fields were dead code.