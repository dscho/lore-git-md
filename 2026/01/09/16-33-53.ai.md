This is a follow-up discussion about a memory leak fix in the shallow repository handling code. Patrick Steinhardt confirms that Samo Pogačnik's patch 1/2 correctly addresses a memory leak in `get_shallow_commits()` by freeing the `object_array` allocations, though he notes the original commit message could be more precise about what exactly is being freed (the array itself rather than individual elements). 

The key new information is that Samo's test case from patch 2/2 fails the leak-checking CI runs (`linux-leaks` and `linux-reftable-leaks`) without this fix, empirically validating the leak's existence. Patrick suggests updating the commit message to mention this test coverage, which would help reviewers understand the practical impact. 

This exchange shows the patch is technically sound and now has additional validation through CI failures, moving it closer to acceptance. The remaining work is documentation polish - Patrick wants the commit message enhanced to explicitly connect the fix to the test case that catches the leak. The thread demonstrates Git's careful review culture where even correct fixes get scrutinized for clarity and test coverage justification.