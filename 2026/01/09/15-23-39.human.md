This is a feature enhancement for `git status` command, part of a 2-patch series that shows divergence from both upstream and push tracking branches. The current email is a response to Phillip Wood's review of the refactoring patch (first in the series).

Harald Nordgren confirms Phillip's suggestion to rename the `sti` parameter to `up_to_date` for clarity, as it serves a distinct purpose from the `ours` and `theirs` counts when in quick mode. He also converts some parameters to `bool` type where appropriate, though keeps the signature of `format_tracking_info()` unchanged to avoid wider impacts.

The exchange shows the patch is nearing final form after extensive review (17 iterations), with Phillip Wood confirming the refactoring preserves original behavior while improving code structure. The series appears well-vetted, with comprehensive test coverage and careful attention to edge cases like quick mode behavior and upstream-gone scenarios. 

This is a preparatory refactor that enables the subsequent patch to cleanly implement the push tracking display feature. The discussion demonstrates thoughtful consideration of parameter naming and type safety, suggesting the changes are being implemented with care.