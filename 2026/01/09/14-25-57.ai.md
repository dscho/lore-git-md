Documentation patch improving the `git-patch-id` man page formatting. This is part 1 of a 6-part series that has already gone through positive review and is now in its final iteration (v2).

The change is small but meaningful: it fixes inconsistent punctuation in the man page by removing a semicolon after a list item that was actually a full sentence, replacing it with a period. The commit message cites style guidelines about when semicolons are appropriate in bullet lists (only when listing short items, not full sentences). 

This patch continues Kristoffer Haugsbakk's established pattern of documentation improvements, specifically targeting the `git-patch-id` command. The series as a whole standardizes terminology, clarifies behavior around the `--verbatim` and `--stable` options, and fixes various grammar/style issues. 

The change is uncontroversial - v1 received positive reviews from Pushkar Singh and Patrick Steinhardt, with only a minor markup nit that was fixed in v2. The patch touches only the documentation file (`Documentation/git-patch-id.adoc`) with a simple one-line change. This is the kind of polish that makes the documentation more professional and consistent with project style guidelines.