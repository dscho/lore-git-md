This is a platform compatibility patch in the Windows symlink support series, specifically addressing the `stat()` implementation. The email is a follow-up discussion about commit message accuracy in patch 2/18 of the series.

Johannes Schindelin (Dscho) responds to Johannes Sixt's (Hannes) observation that the commit message contains a disconnected paragraph about `ELOOP` error handling that doesn't match the actual code changes. Dscho confirms this was a remnant from an earlier version of the patch and explains the historical context - the `ELOOP` behavior was intentionally removed because it would incorrectly fail when reading existing symlinks with `core.symlinks=false` on Windows 7.

The patch itself (not shown in this email) implements proper symlink resolution in `mingw_stat()` by using file handles and `GetFileInformationByHandle()`, ensuring Windows resolves symlinks before returning file information. This is part of the larger effort to bring comprehensive POSIX-like symlink support to Git's Windows compatibility layer.

Key technical details include the removal of outdated documentation about `ELOOP` behavior, confirmation that symlink reading should work even when creation is disabled via `core.symlinks`, and the use of Windows API calls for proper symlink resolution. The discussion shows careful attention to both technical correctness and commit message accuracy in this mature patch series.