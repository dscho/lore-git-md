This is a minor typo fix in the ongoing Windows symlink support series. Johannes Schindelin corrects a typo ("woutl" -> "would") in patch 10/18 that Ben Knoble spotted during review. The patch itself (authored by Karsten Blees) handles directory symlink deletion on Windows, where the native `_wunlink()` and `DeleteFileW()` functions return `ERROR_ACCESS_DENIED` when attempting to remove symlinks to directories.

The series is now in its fifth iteration and has been approved for integration after addressing all technical and administrative issues. This particular email is just a small editorial correction to the commit message, showing the final polish being applied before the series lands. The thread context indicates this is part of a major effort to bring comprehensive POSIX-like symlink support to Windows, building on years of work in Git for Windows.