This thread documents the final stages of René Scharfe's series removing implicit `the_repository` dependencies from Git's tree-parsing subsystem - a major milestone in the multi-year effort to eliminate global state. The technical work is complete, with new repository-aware APIs (`repo_parse_tree*()`) now implemented and callers converted across bloom filters, delta-islands, and other subsystems. A Coccinelle semantic patch automates future conversions.

The remaining discussion focuses on documentation approach. Patrick Steinhardt and Junio Hamano have converged on an advisory rather than version-bound deprecation notice for the transitional wrappers, avoiding artificial constraints on the release schedule. René pragmatically notes that while version references might help, simple reminder comments would suffice to prevent oversight. The only outstanding question is minor code organization - whether to move the `DEFAULT_MAX_ALLOWED_TREE_DEPTH` constant to a more specific header.

With all substantive feedback addressed, this series represents a significant architectural improvement that maintains backward compatibility while advancing Git's modularity. The careful attention to documentation wording reflects the project's balance between clear guidance and release flexibility. Expect this to merge soon after the final polishing touches.