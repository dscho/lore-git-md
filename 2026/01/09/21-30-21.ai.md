This is the final patch in René Scharfe's series removing implicit `the_repository` dependencies from tree parsing functions. The patch provides a Coccinelle semantic patch to automate converting remaining calls to `parse_tree()`, `parse_tree_gently()`, and `parse_tree_indirect()` to their repository-aware variants (`repo_parse_tree*()`).

The semantic patch is applied across 27 files, touching core commands like `checkout`, `merge`, and `reset`, as well as lower-level plumbing. The changes are mechanical - each call site is updated to pass `the_repository` explicitly. The patch includes a manual fix for whitespace in `builtin/commit.c` that the automated transformation would have broken.

This completes the tree-parsing portion of the long-running `the_repository` removal effort. The series has followed the established pattern: first introducing repository-aware variants, then converting callers systematically, and finally deprecating the old API. The semantic patch will help maintainers convert any new code that might still use the old functions.

The changes are low-risk since they don't modify behavior - they make explicit what was previously implicit. The thorough coverage across builtins and plumbing suggests this is ready for merging, continuing the project's steady progress toward eliminating global state.