This is a bugfix patch addressing a compiler warning in the hook subsystem. The patch fixes a potential NULL pointer dereference in `run_hooks_opt()` by moving the access to `options->ungroup` after the NULL check. While the function is never actually called with NULL options in current code, the change makes the code more robust against future misuse.

The patch also corrects a BUG message that had the wrong function name. The changes are straightforward and focused - just moving one field assignment and fixing an error message. The patch comes with sign-offs from both the reporter (correctmost) and Patrick Steinhardt, who suggested the fix, indicating it has already received some review.

This appears to be a low-risk change that silences a compiler warning while maintaining the existing behavior. The patch is based on the 'next' branch where the warning was introduced, suggesting it's targeting near-term inclusion. The CI run passing adds confidence in its correctness.