This thread documents an update to the Git FAQ recommending stash import/export as the preferred way to sync work-in-progress changes between machines. The patch follows Git 2.51's introduction of stash import/export functionality, which provides a more reliable alternative to directly syncing working tree files with tools like rsync.

The revised FAQ section now clearly explains the three-step stash-based workflow (export, transfer via Git, import) as the primary method, while keeping rsync as a fallback option with appropriate warnings. The documentation maintains its cautious tone about data integrity but reframes rsync as a secondary choice rather than the main recommendation. The patch appears to be a straightforward improvement building on recent functionality, with no outstanding questions or objections noted in the thread.