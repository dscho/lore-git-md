This is a refactoring patch in Patrick Steinhardt's ongoing `git-history` command series (v9 5/7). It introduces a new function `wt_status_collect_changes_trees()` that computes and formats the differences between two arbitrary trees, similar to how `wt-status` shows working tree changes. 

The patch extends the working tree status machinery to handle tree-to-tree comparisons, which will be needed for the upcoming `split` subcommand of `git-history`. The new function reuses the existing diff callback infrastructure but operates on tree objects rather than the working tree/index. The implementation carefully preserves all existing diff configuration options (rename detection, limits, etc.) from the status struct.

This is a well-contained change that follows the series' pattern of refactoring core functionality into reusable components. The commit message clearly explains the motivation (avoiding open-coding tree diffs) and acknowledges the slight conceptual mismatch with "working tree" status. The patch includes thorough documentation of the new public function in the header file.

As with previous patches in this series, this continues the trend of careful, incremental refactoring to support the history command's needs while maintaining clean separation of concerns. The change appears ready for inclusion, with no obvious technical issues or missing pieces.