Thread summary:

Documentation patch updating the Git FAQ to recommend stash import/export for syncing working trees across machines, following Git 2.51's new stash import/export functionality. The patch rewrites the FAQ section to prioritize the new mechanism over direct filesystem syncing with rsync, explaining the three-step process of creating/exporting stashes (including untracked files with --include-untracked), transferring them via Git push/fetch, and importing/applying them on the destination machine. While still mentioning rsync as an alternative, the revised text clearly positions it as a secondary option with more caveats, maintaining but reframing the existing warnings about rsync's data integrity risks as fallback advice rather than primary recommendation. The patch builds on recent functionality to provide clearer guidance, with thorough documentation changes that preserve the existing cautionary tone while updating the workflow recommendations.