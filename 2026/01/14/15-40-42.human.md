This is a refactoring patch that removes unused headers from refs.c as part of a larger bugfix series addressing regression in error message reporting for batched reference updates.

The patch removes two unused header includes (`run-command.h` and `sigchain.h`) from refs.c, which is a straightforward cleanup. This appears to be preparatory work for the subsequent patches in the series that will improve error reporting in reference updates. The change is mechanical and uncontroversial - simply deleting unused includes that were left behind from previous refactoring. 

As the first patch in a 6-part series, this serves as groundwork for the more substantive changes to come. The series as a whole aims to restore detailed error messages that were lost during batched reference updates in commands like `git update-ref --batch-updates`, `git fetch`, and `git receive-pack`. While this particular patch doesn't address the core issue, it's a clean, focused change that helps maintain code hygiene of the refs subsystem.