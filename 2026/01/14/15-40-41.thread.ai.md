Thread identity: Bugfix series addressing regression in error message reporting for batched reference updates in `git update-ref --batch-updates`, `git fetch`, and `git receive-pack`.

Core problem: When performing batched reference updates, these commands lost detailed error messages that were previously available, only returning generic error codes. This regression affected user debugging when reference updates failed.

Current status: Series from Karthik Nayak and Jeff King (v1, 6/6) merged by Junio Hamano. Post-merge discussion now focuses on error message formatting in `git update-ref --batch-updates`. Jeff King suggests preserving machine-readable output while adding backend-provided details via stderr, noting the existing format appears documented in git-update-ref(1) and may need test coverage if considered stable.

Key technical details: Changes include:
- Added `rejection_details` field to `struct ref_update` with Jeff King reviewing implementation details
- Modified callback signatures throughout refs code
- Implemented detailed error display in `builtin/update-ref.c`, `builtin/fetch.c`, and `builtin/receive-pack.c`
- Test suite expanded with 20 test cases across multiple test files
- All three ref backends updated to pass through error messages
- Current discussion focuses on `print_rejected_refs()` output - Jeff King proposes outputting raw details to stderr via `error()` while preserving machine-readable format
- Identified concerns: potential redundancy in details (already include refnames), possible loss of machine-readability, uncertainty about backend message quality

Open questions and loose ends: Two outstanding items:
1. Error message verbosity with repeated refnames (previously noted)
2. Optimal formatting of `update-ref` output - whether to prioritize backend details over machine-readable format or combine both (current Jeff King proposal)
Jeff King's suggestions indicate potential follow-up work to refine output formatting while maintaining the merged infrastructure changes.

Participant positions: Karthik Nayak driving the fix with Jeff King as co-author. Jeff King now providing post-merge feedback on output formatting, suggesting conservative approach to maintain backward compatibility. Junio Hamano previously reviewed and merged series, no new input in this exchange. Discussion demonstrates project's attention to both functionality and output stability.

Related work: Part of ongoing ref backend work (reftable/files/packed backend unification). Builds on commit 0e358de64a where batched updates were introduced to `fetch`. Changes maintain backward compatibility while restoring lost functionality.