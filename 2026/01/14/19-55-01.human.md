This is part 15 of Taylor Blau's 18-patch series implementing MIDX compaction with bitmap support. The series has already addressed major review feedback and established technical foundations for the new `git multi-pack-index compact` functionality.

The current patch refactors the fanout layer processing in `midx-write.c` by extracting the core fanout addition logic from `compute_sorted_entries()` into a new helper function `midx_fanout_add()`. This makes the code more modular and prepares for future compaction logic that will need similar fanout processing but with different parameters.

The change is purely structural - no behavior is altered. The patch follows the series' pattern of careful, incremental improvements to the MIDX codebase while maintaining backward compatibility. Taylor demonstrates his characteristic attention to clean API boundaries in this refactoring, which should make the upcoming compaction logic easier to implement and review.

This continues the high-quality work seen throughout the series, with each patch building logically on previous ones while keeping changes focused and well-explained. The MIDX compaction functionality is taking shape methodically through this disciplined approach.