This is a test fix addressing exit code handling in `t/t0100-previous.sh`. The original patch from amisha correctly identified that piping `git cat-file` directly into `grep` was masking the command's exit status, which could hide failures. The solution redirects output to a temporary file before grepping, ensuring test reliability.

Pushkar Singh's review suggests a minor improvement to use Git's `test_grep` helper instead of raw `grep` for better failure diagnostics and consistency with other tests. The change is small (2 lines modified) and clearly motivated, following established test patterns. This appears to be a straightforward improvement to test robustness with no apparent downsides.