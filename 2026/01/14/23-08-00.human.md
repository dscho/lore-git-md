This is a continuation of the discussion about configuring `git status` to show divergence from both upstream and push tracking branches, with a focus on triangular workflows (where the push remote differs from the fetch remote). The thread has already seen a merged implementation of the push tracking display functionality.

Harald Nordgren weighs in on the ongoing debate about whether the feature should be enabled by default. He agrees with D. Ben Knoble's position that showing `@{push}` information by default would help users discover triangular workflows more easily, while more experienced users could disable it if desired. Harald notes that most users don't use separate tracking and push branches anyway, so the default-on approach wouldn't affect them negatively.

The email reinforces the pro-default-on position in the configuration debate, suggesting the feature's discoverability benefits outweigh potential UI clutter concerns. This comes after Jeff King's detailed technical analysis of how `@{push}` behaves in various push configurations, which showed it works correctly in triangular workflows except when `push.default=simple`.

The discussion remains focused on finding the right balance between helping new users understand their workflow while not overwhelming experienced users with information they don't need. Harald's perspective as the original implementer carries weight in this ongoing configuration design discussion.