This is a test patch (part 3/5) in a series about aligning `git add` behavior with `ignore=all` submodules. The patch adds comprehensive test coverage for the new `--force` flag behavior when adding ignored submodules.

The test file `t2206-add-submodule-ignored.sh` contains 8 test cases that verify:
1. Basic setup with submodule repositories
2. Default behavior with `ignore=none`
3. Status/log behavior after setting `ignore=all`
4. Interaction between `--ignore-submodules=none` and status
5. Normal `git add` behavior with ignored submodules
6. `git add --force` with directory paths
7. Explicit submodule path handling
8. Full workflow with `--force` add and commit verification

The tests confirm that submodules marked `ignore=all` are properly skipped by default but can be staged using `--force`, matching the behavior established in earlier patches of this series. The test file is also added to meson.build for execution in the test suite.

This appears to be a thorough test suite covering both `.gitmodules` configuration and command-line overrides, with trace logging added for debugging. The implementation follows the design agreed upon in earlier thread discussion, using `--force` as the consistent interface for overriding ignore rules.