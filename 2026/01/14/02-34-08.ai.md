This is a discussion about triangular workflows in Git, specifically regarding the recently merged push-tracking status feature in `git status`. Jeff King (Peff) responds to Harald Nordgren's suggestion about using `push.default=upstream` in scripted workflows.

Peff explains that while he could configure `push.default=upstream` in his CI scripts (which already explicitly name the remote), he occasionally uses bare `git push` commands to manually trigger CI runs. He finds it counterintuitive that a push would go to the upstream remote (where he lacks write access) rather than his intended mirror destination.

The discussion reveals nuanced differences in workflow patterns:
- Harald's original manual push setup that motivated the status feature
- Peff's scripted mirror workflow where push destinations are usually explicit
- The occasional need for bare `git push` in Peff's workflow

Peff also shares some advanced usage of `@{push}` for comparing branch iterations with `git range-diff`, noting this serves as a proxy for "last sent to mailing list" state. He mentions `branch.*.pushRemote` exists but hasn't found it useful for his single-mirror setup.

This exchange continues the thread's exploration of real-world workflow implications for the new push-tracking status feature, particularly around configuration tradeoffs in triangular workflows. No code changes are proposed - the discussion serves to document edge cases and usage patterns that may inform future refinements.