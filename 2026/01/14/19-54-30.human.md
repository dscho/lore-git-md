Documentation patch correcting outdated MIDX bitmap compatibility information. This is part of Taylor Blau's 18-part series implementing MIDX compaction functionality.

The patch fixes a documentation discrepancy in the `git-multi-pack-index` man page. While the text claimed `--incremental` and `--bitmap` options were incompatible, this restriction was actually removed in an earlier commit (27afc272c49) that implemented incremental MIDX bitmaps. The change simply removes the incorrect incompatibility note while keeping all other description of the `--incremental` option.

This is a straightforward documentation update that aligns the man page with the actual code behavior. The change is minimal (one line removed) and uncontroversial, serving as a small cleanup in the larger MIDX compaction series. The patch follows the series' pattern of careful attention to detail in both implementation and documentation.