This is part 1 of a 5-patch series addressing submodule `ignore=all` behavior consistency, specifically focusing on `git add` alignment with other commands. The series has gone through multiple iterations and is now in its third version.

The patch updates the plumbing function `add_files_to_cache()` to take a new `ignored_too` parameter, which will be used to control whether ignored submodules should be added to the index. The change propagates through several callers in `builtin/add.c`, `checkout.c`, and `commit.c`. The implementation maintains the existing behavior while preparing for the new `--force` flag functionality that will be introduced in later patches.

The changes are mechanical plumbing updates that don't yet implement the new behavior - that comes in patch 2. This is a straightforward change that follows the established pattern in the codebase, though the thread context notes there are still some minor style issues to address before final acceptance. The patch series as a whole appears well-structured, with clear separation between plumbing changes, behavior implementation, and test updates.