Test fix in Taylor Blau's MIDX compaction series. This is patch 7/18 in a series implementing `git multi-pack-index compact` functionality with reachability bitmaps support.

The patch corrects a test name in t5319-multi-pack-index.sh that was incorrectly copied from a previous test. The test actually verifies handling of missing packs (as described in its commit message) rather than packnames out of order. This is a straightforward documentation fix within the test suite - renaming the test to match its actual purpose while keeping all behavior identical.

The change is minimal (a single line edit) and uncontroversial, aligning with the series' broader focus on MIDX correctness and test coverage. The patch follows the series' pattern of careful attention to detail, even in supporting infrastructure like test names.