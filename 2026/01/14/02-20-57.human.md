This is a bugfix for HTTP authentication in `remote-curl.c`, specifically addressing an issue where `probe_rpc()` requests during clone operations weren't properly including authentication headers when using bearer tokens.

Jeff King (Peff) provides a minor optimization suggestion for the test case in Aaron Plattner's upcoming v4 revision. The test creates 2000 refs to force large requests that trigger the `probe_rpc()` path. Peff points out that `test_seq` now supports format strings, allowing the ref creation to be done more efficiently with fewer processes compared to the `xargs` approach Junio had suggested.

The core fix - adding auth headers to `probe_rpc()` - remains technically sound and approved by both Patrick Steinhardt and Junio Hamano. This exchange represents the final polish before the patch can be merged, addressing the last remaining portability and presentation concerns. The discussion shows the project's attention to both correctness and efficiency, even small test improvements.