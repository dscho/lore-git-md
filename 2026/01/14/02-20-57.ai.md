Bugfix follow-up addressing test portability in HTTP authentication patch.

Jeff King (Peff) provides a refinement to Aaron Plattner's v4 preparation for the HTTP auth fix in `remote-curl.c`. The thread has already established the core technical solution - adding auth headers to `probe_rpc()` calls - and this exchange focuses on final test implementation details.

Peff suggests simplifying the test_seq` usage in the test case that creates 2000 refs to trigger large HTTP requests. Rather than piping through `xargs`, he points out that `test_seq` now supports format strings directly, allowing a more concise and portable one-liner. This addresses Junio Hamano's earlier concern about potential ARG_MAX limitations while maintaining the test's effectiveness.

The exchange confirms the test will produce identical results with this approach, validating it as a suitable replacement. This is expected to be incorporated into Aaron's upcoming v4 submission, which has already received technical approval from Patrick Steinhardt and Junio. The change is purely about test implementation elegance and portability, not the underlying auth fix.