This is a refactoring patch that's part of Taylor Blau's 18-part series implementing MIDX compaction functionality. It's the first patch in the second iteration of the series.

The change marks the `get_midx_checksum()` function's parameter as `const` to make clear it doesn't modify the passed `multi_pack_index` struct. This is a straightforward code hygiene improvement that follows the project's general practice of using `const` where possible to document immutability. The commit message notes this is preparatory work for a follow-up patch that will rename the function to better reflect its behavior.

As the first patch in a larger series, this serves as groundwork for more substantive changes to come. The thread context shows this series has already gone through significant review, with all major technical feedback addressed. This particular change appears uncontroversial and well-justified.