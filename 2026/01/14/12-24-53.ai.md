This is a refactoring patch in Patrick Steinhardt's series about geometric repacking with promisor remotes. The patch extracts the core algorithm for computing repack splits into a new `compute_pack_geometry_split()` function, making it reusable for future callers (including the upcoming promisor pack handling in later parts of the series).

The change is mechanical - the same logic is moved from `pack_geometry_split()` to the new function with minimal adjustments (like taking explicit parameters rather than accessing them through the geometry struct). Toon Claes confirms the behavior remains identical, noting the refactoring improves readability.

This is a preparatory change for the series' later patches that will implement separate geometric progression for promisor packs. The refactoring follows good practice by keeping the actual behavior unchanged while making the code more modular. The patch touches only `repack-geometry.c`, moving 39 lines with a net change of +21/-18.

Key technical details for future reference:
- New static function `compute_pack_geometry_split()` encapsulates the split calculation logic
- Takes explicit `pack`, `pack_nr`, and `split_factor` parameters instead of geometry struct members
- Original `pack_geometry_split()` becomes a thin wrapper around this new function
- Behavior remains identical as confirmed by reviewer
- Part of larger effort to support promisor packs in geometric repacking