Refinement discussion about triangular workflows and push tracking in `git status`.

Jeff King (Peff) responds to Harald Nordgren's suggestion about using `push.default=upstream` in mirror-based setups. He explains that while his CI scripts already specify the remote directly, he occasionally uses bare `git push` for manual CI triggering, making the upstream push target undesirable (since he lacks write access to the upstream remote). 

The discussion reveals nuanced workflow differences: Harald's original motivation came from manually specifying push destinations while tracking different branches, while Peff's mirror workflow involves scripted pushes to a different remote. Peff also shares useful tricks like using `@{push}` with `git range-diff` to compare local and pushed versions of a branch.

This exchange highlights how the new push-tracking status feature, while valuable for some workflows, can be noisy in mirror-based setups where the push destination is consistently different from the tracked branch. The thread continues to explore real-world usage patterns that may inform future configuration refinements, though no code changes are currently proposed.