This is a refactoring patch in Patrick Steinhardt's series to fix geometric repacking with promisor remotes. The patch extracts the core repacking split calculation logic from `pack_geometry_split()` into a new `compute_pack_geometry_split()` function, making the code more modular and preparing for reuse by future callers.

Toon Claes provides a brief but positive review, noting the change makes the code easier to understand while maintaining identical behavior. The patch appears straightforward - it's a pure refactoring with no behavior changes, just moving code into a more reusable form. This is part of Patrick's larger effort to modernize Git's repack machinery, with this particular series focusing on making geometric repacking work properly with promisor packs.

The patch has already been through review (this is version 2 of the series) and appears uncontroversial. It follows good Git development practices by keeping the change focused and well-explained in the commit message. The series as a whole is progressing well, with Taylor Blau (the pack subsystem expert) having already approved some of the other patches in the set.