This is a follow-up discussion about the recently merged `git status` enhancement that shows divergence from both upstream and push tracking branches. Harald Nordgren responds to D. Ben Knoble's workflow concerns by sharing his own setup that relies on bare `git push` commands while still benefiting from the new status reporting.

Harald describes his personal configuration that makes branch creation and pushing more convenient, using `remote.pushDefault=origin and push.default=current` to ensure bare pushes go to the expected destination. His workflow involves creating branches from either upstream or origin/HEAD and then using simple push/pull commands without arguments. This exchange highlights how different workflow patterns (Harald's interactive development vs Peff's CI mirror setup) lead to different preferences around push defaults and status reporting. The thread continues to explore configuration ergonomics without suggesting changes to the core implementation, which remains stable after merging.