[PATCH v4] remote-curl: include auth headers in probe_rpc() requests

Fixes authentication failure in remote-curl transport when large requests trigger probe_rpc() path, which previously omitted auth headers despite successful initial auth. Affects operations like cloning with blob filters where initial auth succeeds but subsequent blob requests fail with HTTP 401. Problem demonstrated via reproduction scenario: clone with blob filters fails when request size exceeds http.postBuffer, forcing probe_rpc() path without auth headers.

Changes in v4: addressed style nits from review, improved test portability using xargs. Patch modifies remote-curl.c to include auth headers in probe_rpc() matching other request paths. Test case verifies fix by:
1. Creating repo with 2000 refs to ensure large request size
2. Configuring small http.postBuffer to force probe_rpc()
3. Setting up bearer token auth
4. Confirming successful clone with proper auth handling

Touches remote-curl.c and t/lib-httpd.sh with minimal logic changes. Fix is straightforward - adding auth header append call to probe_rpc(). Comprehensive test coverage validates solution.