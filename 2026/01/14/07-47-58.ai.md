This is a test update patch in a series addressing submodule `ignore=all` behavior. It's part 4 of 5 in the third iteration (v3) of the patch series.

The patch updates existing tests to account for the new requirement to use `--force` when adding submodules with `ignore=all` configuration. It modifies four test files (with changes shown for two of them) to add the `--force` flag to `git add` commands that interact with submodules. The changes are mechanical updates to maintain test compatibility with the new behavior introduced earlier in the series.

Key technical details:
- Updates `t/lib-submodule-update.sh` (a shared test library) with `--force` on three `git add` operations
- Modifies `t/t7508-status.sh` to use `--force` when testing staged-but-ignored submodules
- Maintains all existing test assertions while adapting to the new `--force` requirement
- Follows the series' established pattern of preserving warning messages while requiring explicit `--force` for ignored submodule adds

The patch demonstrates good test hygiene by:
1. Updating dependent tests to match new behavior
2. Maintaining test coverage of the modified scenarios
3. Using the shared test library pattern for submodule tests
4. Keeping changes focused and mechanical

This is a straightforward test adaptation patch in a well-structured series that's nearing completion. The changes are exactly what we'd expect to see at this stage - updating existing tests to match the new `--force` behavior while preserving their verification roles.