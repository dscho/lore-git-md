Thread identity: Performance optimization for sparse-checkout pattern sorting, changing from O(n^2) to O(n log n) by switching from string_list_insert() to string_list_append() followed by sorting, initiated by Amisha Chhajed.

Core problem: The sparse-checkout pattern list building used inefficient O(n^2) insertion sort via string_list_insert() when collecting patterns from hashmap. While parent_hashmap entries are unique, recursive_hashmap can contain duplicates from user input (e.g. via `git sparse-checkout add --stdin`), requiring careful duplicate handling during optimization.

Current status: v4 patch from Amisha Chhajed completes the optimization by adding explicit duplicate removal with string_list_remove_duplicates() after sorting, addressing the regression identified in v3. Junio C Hamano has reviewed the solution and confirmed it correctly handles both parent_hashmap (guaranteed unique) and recursive_hashmap (may contain duplicates) cases while maintaining O(n log n) performance. The patch is merge-ready with two suggested follow-ups: adding performance measurements and a test case for duplicate handling.

Key technical details: Changes affect pattern collection in sparse_checkout_list() and write_cone_to_file() in builtin/sparse-checkout.c. The solution properly distinguishes between parent_hashmap (skips duplicate check) and recursive_hashmap (requires string_list_remove_duplicates()). Performance improvement is confirmed while maintaining correctness. Test suite passes but lacks explicit verification of duplicate pattern handling and performance metrics. Junio suggests future API improvement with string_list_sort_u() combining sort-and-deduplicate, but this is out of scope for current change.

Participant positions: Initial optimization from Amisha Chhajed, with critical review from Jeff King identifying the duplicate-handling edge case. Amisha addressed all feedback in v4. Junio has reviewed the final version and agrees it is technically sound, suggesting optional test coverage and performance measurement as follow-up work. Consensus exists that the core optimization is ready for merging.

Open questions: Whether Amisha will add explicit test coverage for duplicate pattern handling and performance measurements as follow-up work, though these don't block merging. Potential future API improvement with string_list_sort_u() noted but deferred.

Related work: Part of ongoing sparse-checkout/sparse-index optimizations (Derrick Stolee, Elijah Newren et al), though this is a localized performance improvement rather than architectural change. Builds on earlier work in this subsystem while maintaining compatibility with cone mode pattern handling.