Thread identity: Performance optimization for sparse-checkout pattern sorting, changing from O(n^2) to O(n log n) by switching from string_list_insert() to string_list_append() followed by sorting, initiated by Amisha Chhajed.

Core problem: The sparse-checkout pattern list building used inefficient O(n^2) insertion sort via string_list_insert() when collecting patterns from hashmap. While parent_hashmap entries are unique, recursive_hashmap can contain duplicates from user input (e.g. via `git sparse-checkout add --stdin`), requiring careful duplicate handling during optimization.

Current status: v4 patch from Amisha Chhajed completes the optimization by adding explicit duplicate removal with string_list_remove_duplicates() after sorting, addressing the regression identified in v3. The optimization now correctly handles both parent_hashmap (guaranteed unique) and recursive_hashmap (may contain duplicates) cases while maintaining O(n log n) performance. Amisha has identified a test coverage gap for duplicate patterns and is considering adding a test case, though this doesn't block the current optimization.

Key technical details: Changes affect pattern collection in sparse_checkout_list() and write_cone_to_file() in builtin/sparse-checkout.c. The solution properly distinguishes between parent_hashmap (skips duplicate check) and recursive_hashmap (requires string_list_remove_duplicates()). Performance improvement is confirmed while maintaining correctness. Test suite passes but lacks explicit verification of duplicate pattern handling.

Participant positions: Initial optimization from Amisha Chhajed, with feedback from Jeff King (Peff) identifying the duplicate-handling edge case. Amisha has addressed all feedback in v4, which now properly handles duplicates while maintaining performance gain. Consensus exists on the solution, with only potential test coverage improvement remaining as follow-up work.

Open questions: Whether to add explicit test coverage for duplicate pattern handling as follow-up work, though this doesn't block the current optimization. The patch appears merge-ready.

Related work: Part of ongoing sparse-checkout/sparse-index optimizations (Derrick Stolee, Elijah Newren et al), though this is a localized performance improvement rather than architectural change.