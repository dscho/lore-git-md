Thread identity: Performance optimization for sparse-checkout pattern sorting, changing from O(n^2) to O(n log n) by switching from string_list_insert() to string_list_append() followed by sorting, initiated by Amisha Chhajed.

Core problem: The sparse-checkout pattern list building was using inefficient O(n^2) insertion sort via string_list_insert() when collecting patterns from a hashmap. While hashmap entries are unique in parent_hashmap, the recursive_hashmap can contain duplicates from user input (e.g. via `git sparse-checkout add --stdin`), requiring careful handling of duplicates during the optimization.

Current status: v3 patch from Amisha Chhajed converted all three locations in builtin/sparse-checkout.c to use string_list_append() followed by string_list_sort(). Jeff King (Peff) and René Scharfe identified a regression where duplicate patterns from user input would appear in the output. The optimization itself (O(n^2) -> O(n log n)) remains correct but needs to preserve duplicate suppression for recursive_hashmap case. Amisha has acknowledged the issue and is investigating why tests didn't catch this, suggesting possible redundant deduplication in original code.

Key technical details: Changes affect pattern collection in sparse_checkout_list() and two locations in write_cone_to_file(). The patch must now distinguish between parent_hashmap (guaranteed unique, can skip duplicate check) and recursive_hashmap (may contain duplicates, needs string_list_remove_duplicates()). All changes remain in builtin/sparse-checkout.c's cone mode handling. The performance improvement is confirmed valid, just needs proper duplicate handling restoration.

Participant positions: Initial optimization from Amisha Chhajed, expanded to all cases per Peff's suggestion. Peff and René both identified the duplicate-handling edge case while affirming the performance improvement. Amisha is investigating test coverage gaps. Consensus exists on maintaining the sorting optimization while restoring proper duplicate handling for recursive_hashmap.

Open questions: Whether Amisha will send v4 with the duplicate handling fix, or if Junio will apply the change during merge. The test coverage gap for duplicate patterns needs investigation to prevent similar regressions. The core optimization is uncontested, just needs this edge case addressed.

Related work: Part of ongoing sparse-checkout/sparse-index optimizations (Derrick Stolee, Elijah Newren et al), though this is a localized performance improvement rather than architectural change.