This is a minor cleanup patch in the refs subsystem, part of a larger series addressing error message reporting in batched reference updates. The patch removes two unused header includes (`run-command.h` and `sigchain.h`) from `refs.c`. 

Junio notes that while `run-command.h` is being removed, its functionality is still indirectly available through `hook.h`, so the change has no practical effect. The removal of `sigchain.h` appears to be a straightforward cleanup of an unused dependency. 

As the first patch in a merged series, this serves as a simple preparatory change that doesn't affect functionality. The thread context shows this was part of a well-received regression fix that restored detailed error messages across several Git commands. The maintainer's comment suggests this particular change could have gone further by also removing `hook.h` if it wasn't needed, but that wasn't blocking for the series.