This is a documentation issue regarding the syntax for `git config --get-urlmatch`. The user reports a discrepancy between the documented replacement syntax (`git config get --all --show-names --url=<URL> <name>`) and actual behavior they encountered, which rejects combining `--url` with `--all`.

The commit message does not provide historical context about when or why this change was made, nor does it explain the current correct syntax. The error message suggests the documented replacement is incorrect, as `--url` appears to be mutually exclusive with `--all`, contrary to what the documentation states.

This appears to be a straightforward documentation bug where the suggested alternative syntax doesn't work as advertised. The patch would need to either correct the documentation to show the proper syntax or explain why the current behavior differs from what's documented. There's no indication of whether this is part of a larger series or migration effort.