This is part 4 of a 5-patch series updating tests to account for the new `--force` requirement when adding submodules with `ignore=all` set. The patch updates several existing test scripts that previously relied on `git add <submodule>` without `--force` to now explicitly use `--force` where needed. The changes are mechanical updates to test expectations, with modifications in `t/lib-submodule-update.sh` (a shared test library) and `t/t7508-status.sh`.

The changes align with the series' goal of making `git add` behavior consistent with `git status` and `git diff` when dealing with ignored submodules. The test updates verify that the new `--force` requirement works correctly across different submodule scenarios, including nested submodules and status reporting.

This patch follows the implementation work in earlier parts of the series and helps ensure existing tests don't break with the new behavior. The changes are straightforward and focused on test maintenance rather than introducing new functionality.