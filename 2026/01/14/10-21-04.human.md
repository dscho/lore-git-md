This is a documentation issue regarding the syntax for `git config --get-urlmatch`. The user reports that the documented replacement syntax (`git config get --all --show-names --url=<URL> <name>`) doesn't work as advertised, producing an error when trying to combine `--url` with `--all`.

The email is asking for clarification on the correct syntax to use instead. This appears to be a case where the documentation hasn't been properly synchronized with actual command behavior - a common issue when commands evolve but their docs aren't fully updated. The error suggests these flags are mutually exclusive in the current implementation, contrary to what the documentation states.

The thread context isn't provided, so it's unclear if this is a known issue or if the documentation was recently changed. The question is straightforward and points to a specific documentation problem that needs resolution.