This is a performance optimization follow-up in the sparse-checkout pattern sorting thread. Jeff King (Peff) responds to amisha's patch that changed the pattern collection from O(n^2) to O(n log n) by using string_list_append() followed by sorting rather than string_list_insert().

Peff acknowledges the improvement but notes two additional issues: first, the commit message formatting and sign-off identity could be more conventional (though acceptable as-is), and second, he spots that the same inefficient pattern exists in two other places in write_cone_to_file() that weren't addressed by the original patch. 

The response suggests this was likely an oversight in the original implementation rather than an intentional design choice, since the sorting happens immediately after insertion. Peff's review indicates this is a straightforward cleanup that should be extended to cover all similar cases in the sparse-checkout code. The thread appears headed toward a revised patch that will address all three locations consistently.