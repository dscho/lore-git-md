This is a refactoring patch that removes unused headers from refs.c as part of a larger bugfix series addressing regression in error message reporting for batched reference updates.

The patch removes two unused header includes (`run-command.h` and `sigchain.h`) from refs.c, which is the core file handling Git's ref operations. This appears to be preparatory cleanup work before implementing the main error reporting fixes in subsequent patches of the series.

The change is straightforward and mechanical - simply deleting two #include lines that are no longer needed. The commit message is brief but sufficient for this type of cleanup change. Since this is part of a larger series (1/6), the real substance will come in later patches that actually address the error reporting regression.

For the AI summarizer's context: this touches refs.c but doesn't yet modify any behavior - it's just removing unused dependencies as groundwork for the error reporting improvements to come. The headers being removed (`run-command.h` and `sigchain.h`) suggest they were previously used for functionality that has since been moved elsewhere in the codebase.