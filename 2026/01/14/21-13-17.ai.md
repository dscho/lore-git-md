Maintainer comment on MIDX compaction series. Junio C Hamano notes that Taylor Blau's patch to remove unnecessary `pack_perm` usage in `midx-write.c` is correct, since the permutation doesn't affect the bitmap position clearing operation. This is a small optimization in the multi-pack-index compaction work that simplifies the code by directly indexing into `ctx->info` rather than going through the permutation array. The change is straightforward and doesn't impact functionality, as Junio confirms the permutation would have produced the same result anyway. This appears to be the final review comment before the series can be merged.