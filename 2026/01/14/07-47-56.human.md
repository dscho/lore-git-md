This is the second patch in a 5-patch series that aligns `git add`'s behavior with `git status` and `git diff` when handling submodules configured with `ignore=all`. The patch implements the core behavior change, requiring `--force` to add such submodules when explicitly specified in the pathspec.

The change introduces a new `skip_submodule()` helper function in `read-cache.c` that checks whether a path should be skipped based on submodule configuration and pathspec matching. When a submodule has `ignore=all` set, it will now be skipped during `git add` unless both conditions are met: (1) the path is explicitly given in the pathspec, and (2) `--force` is used. The implementation maintains the existing warning system through `override_submodule_config` and provides advice messages similar to those shown for regular ignored files.

The motivation is to support workflows where submodules are used more loosely (similar to Google's repo tool) without requiring constant SHA-1 tracking, while keeping behavior consistent with how Git handles ignored files. The patch appears well-structured, with clear trace output for debugging and proper handling of edge cases like path normalization. It builds on the plumbing changes from patch 1 and will be followed by test updates and documentation in subsequent patches.