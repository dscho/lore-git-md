This is a documentation patch in Taylor Blau's ongoing MIDX compaction series (patch 6/18). It aligns the `git-multi-pack-index` man page's SYNOPSIS section with the actual `-h` help output, which had drifted out of sync. The patch also documents additional write subcommand options that were previously missing from the man page.

The changes are straightforward: the SYNOPSIS now properly lists all subcommands (write, verify, expire, repack) with their respective options, matching what `git multi-pack-index -h` shows. A test that was flagging this discrepancy is updated to stop expecting a mismatch for this command.

This is a minor but necessary cleanup as part of the larger MIDX work. The patch follows the project's documentation standards and helps ensure users see accurate information about available options.