This is a small but important optimization in Taylor Blau's ongoing `git multi-pack-index compact` series. The patch removes an unnecessary indirection when assigning bitmap positions during MIDX writing - instead of using the `pack_perm` array to access pack info, it indexes directly into the `ctx->info` array since we're already iterating in the correct order.

Junio confirms the correctness of this change, noting that since `pack_perm` is just a permutation of the indices anyway, the end result would be identical, but the direct access is more straightforward. This is the kind of small but meaningful optimization that experienced contributors like Taylor Blau regularly identify in performance-sensitive code paths.

The change is part of the larger effort to ensure bitmap compatibility during MIDX compaction operations, which is critical for maintaining good performance in large repositories. While this particular patch is minor, it demonstrates the attention to detail being applied throughout this series.