Test improvement addressing exit code handling in merge test verification. This is a reply to Amisha's patch that fixed `t/t0100-previous.sh` by properly capturing the exit status of `git cat-file` during merge verification.

Pushkar Singh agrees with the core approach of redirecting output to a temporary file rather than piping directly to grep, which ensures test will detect command failures. He suggests a minor style improvement to use the `test_grep` helper instead of raw grep, which would provide better failure diagnostics and align with common test patterns. The suggestion doesn't change the fundamental fix but would make the test more consistent with Git's test suite conventions.

The original patch modified two lines to separate the command execution from output verification, addressing a case where command failures could be masked by the pipe. Pushkar's feedback focuses on test suite best practices rather than the underlying issue, indicating the core solution is sound. This appears to be a straightforward test reliability improvement with consensus forming around the approach.