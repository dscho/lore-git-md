Refactoring patch in Taylor Blau's MIDX compaction series. This is patch 15/18 in the second iteration of the series, which introduces `git multi-pack-index compact` functionality with reachability bitmap support.

The patch factors out fanout layer processing from `compute_sorted_entries()` into a new `midx_fanout_add()` helper function. This prepares for upcoming compaction logic that will need similar but not identical fanout processing. The change is purely structural - no behavior is modified, just code organization improved.

Key technical details:
- Extracts fanout layer accumulation logic that handles both MIDX and pack sources
- Maintains special handling for preferred packs
- Preserves existing sorting behavior after fanout accumulation
- Keeps all the same conditions and edge cases (incremental MIDX, preferred pack position)
- The new `midx_fanout_add()` function takes the same parameters as the extracted code block plus the fanout structure to populate
- The change follows the series' pattern of careful API evolution while maintaining backward compatibility

This continues the series' trend of methodical preparation for the actual compaction functionality, with each patch making incremental improvements to the code structure. The patch appears well-contained and focused, with a clear commit message explaining the motivation.