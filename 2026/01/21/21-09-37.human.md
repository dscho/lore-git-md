This is a clarification email in an ongoing discussion about `.gitignore` behavior with directory patterns. Junio C Hamano chimes in to support Jeff King's (Peff) point that the original explanation about directory traversal pruning was potentially misleading.

The thread centers on how Git handles directories that match unanchored ignore patterns (like `backup_STOCKS*/`). Junio agrees with Peff's observation that the original response could confuse readers about how path prefixes work - specifically, that `backup_STOCKS*/` wouldn't match a directory named just `backups/`.

Junio's email reinforces the key technical nuance: when a directory contains only ignored files (likely `backup_STOCKS_*` subdirectories in this case), Git won't show it in `git status` output. This isn't a bug but rather Git's standard behavior for ignored directories. The email serves to keep the discussion technically precise while acknowledging the original reporter's confusion was understandable.

The thread has now clearly established both the pattern matching rules and Git's directory traversal behavior, with multiple experienced contributors aligning on the explanation. This email doesn't introduce new solutions but solidifies the consensus around the technical details.