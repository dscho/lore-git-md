This is a documentation patch completing a small but technically precise improvement to Git's manual pages and their linter. The change follows up on two related patches that have already been finalized:

1. D. Ben Knoble's update to `git-replay.adoc` that conditionally excludes the "Commit Formatting" section when included via `rev-list-options.adoc`, matching the pattern used for `git shortlog`. This required introducing a new `git-replay` attribute and updating the conditional syntax.

2. Jean-Noël Avila's complementary linter improvement that simplifies handling of conditional blocks (like `ifndef`) in documentation, treating them as block-level constructs rather than trying to parse them as inline elements. This avoids false positives when checking `linkgit:` macros in files with complex conditionals.

Junio C Hamano has already reviewed and approved both changes, with Jean-Noël's final patch resolving the last remaining linting issue. The email shown here is just D. Ben Knoble acknowledging the resolution while noting he may be slow to respond due to travel.

This is a clean, well-executed documentation improvement that follows established patterns and maintains consistency across Git's manual pages. The changes are small but demonstrate careful attention to detail in both the documentation content and the tooling that verifies it. No further action appears needed - these patches are ready for merging.