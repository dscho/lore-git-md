This is the final cleanup for the PID file debugging feature, addressing a minor code quality issue in the lockfile implementation. Junio C Hamano responds to Jeff King's observation about redundant file descriptor cleanup in `create_lock_pid_file()`, providing a patch that:

1. Removes the duplicate `close(fd)` calls that Jeff identified as unnecessary
2. Reverts some unrelated whitespace changes in `commit.c` and `gc.c` that had crept in
3. Simplifies the error handling flow while maintaining the Windows-specific requirement (confirmed earlier by Johannes Sixt) to keep the pre-unlink close

The changes are mechanical and non-controversial, representing the final polish before this feature can be merged. This concludes the technical discussion that began with Paulo Casaretto's initial proposal, with all reviewers (Jeff King, Patrick Steinhardt, Eric Sunshine, Johannes Sixt, and Junio) now satisfied with the implementation.