This is the final patch in Patrick Steinhardt's 14-part ODB abstraction series, completing the conversion of Git's object storage layer to support pluggable backends. The patch removes the now-unused `for_each_loose_object()` and `for_each_packed_object()` functions, which have been fully replaced by the new unified iteration APIs introduced earlier in the series. It also inlines a helper function that had become single-purpose after the refactoring.

The changes are mechanical cleanup - deleting obsolete code paths that were made redundant by the series' earlier work. The patch demonstrates the series' success in consolidating object iteration logic while maintaining all existing functionality. With this change, the ODB abstraction work appears complete from an API perspective, though follow-up patches may still be needed to fully realize pluggable backends.

This is a well-executed conclusion to a major architectural effort that has systematically modernized Git's object storage layer. The series has received thorough review from Justin Tobler and Karthik Nayak along the way, suggesting it's likely to be merged once Junio Hamano reviews the complete set.