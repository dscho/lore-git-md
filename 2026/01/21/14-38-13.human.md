This is a follow-up discussion in the `git repo info --keys` feature series, focusing on final naming decisions for the format option. The thread has reached consensus that the "keyvalue" format name should be changed to "lines" for consistency with the related `git repo structure` command, as both commands now share the same output format handling.

Lucas Seiki Oshiro proposes dropping the earlier patch that added a "default" format option in favor of simply renaming "keyvalue" to "lines" throughout. Patrick Steinhardt agrees this is reasonable, noting that since the `git repo` command is still marked experimental, such naming refinements are appropriate at this stage. The change will eliminate the need for special-case handling of the `--keys` output format while maintaining consistency across commands.

This appears to be the final polish before the series is ready for integration, addressing the last remaining naming inconsistency identified during review. The discussion shows all technical aspects have been settled, with only this straightforward renaming left to align the implementation with the project's preferred terminology.