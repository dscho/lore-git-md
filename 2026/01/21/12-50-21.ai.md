This is part 5/14 of Patrick Steinhardt's v3 series on ODB (object database) abstraction, continuing the effort to make Git's object storage layer support pluggable backends. The patch refactors packed object iteration to prepare for backend-agnostic handling.

The change extracts the core iteration logic from `for_each_packed_object()` into a new helper function `packfile_store_for_each_object_internal()`. This follows the pattern established in earlier patches for loose object handling, creating a clear separation between the high-level iteration function and the store-specific implementation. The new helper maintains all existing flag handling (LOCAL_ONLY, PROMISOR_ONLY, etc.) while making the code more modular.

Key technical details:
- The extracted function operates on a single `packfile_store` rather than iterating sources
- Preserves all existing flag behaviors and error handling
- 78 lines modified (45 added, 33 removed) in packfile.c
- Follows the series' pattern of introducing backend-agnostic infrastructure
- Prepares for upcoming changes to object iteration APIs

The patch continues the series' methodical approach, with each change building toward the larger ODB abstraction goal while maintaining existing behavior. Reviewers Justin Tobler and Karthik Nayak have been actively engaged in the thread, and the changes appear well-considered within the series' context.