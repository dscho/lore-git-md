This is a technical discussion about type safety in the xdiff refactoring series, specifically regarding the IVec (integer vector) implementation that spans the C/Rust boundary.

Jeff King (Peff) responds to Ezekiel Newren's suggestion to add `clar` unit tests verifying that different IVec types have consistent sizes and layouts. Peff expresses skepticism about runtime tests for undefined behavior properties, noting that compilers can insert padding that might mask underlying pointer size mismatches. He suggests that directly checking pointer sizes would be more reliable than comparing struct sizes, but acknowledges he's not fully qualified to judge Rust/C ABI concerns.

The exchange highlights an important technical consideration in the series' goal of Rust interoperability - ensuring type representations match across language boundaries. Peff's caution reflects Git's conservative approach to platform compatibility, where assumptions about memory layout can cause subtle portability issues. This discussion occurs in the context of patch 1/10 which introduces the C-side IVec implementation, with implications for later patches that will use these types at the Rust/C boundary.

The thread shows careful technical review of a foundational piece of the xdiff refactoring, with Peff raising valid concerns about testing strategy while acknowledging the Rust-specific context may require different approaches than pure C code. The discussion remains unresolved but establishes important constraints for the series' implementation.