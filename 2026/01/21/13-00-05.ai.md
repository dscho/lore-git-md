This is a performance optimization patch for the sparse-checkout subsystem, part of Amisha Chhajed's series to improve pattern sorting from O(n^2) to O(n log n). The patch finalizes the optimization by switching from `string_list_insert()` to `string_list_append()` followed by sorting and deduplication in `builtin/sparse-checkout.c`.

The changes affect two key functions: `sparse_checkout_list()` and `write_cone_to_file()`, where pattern collection now builds an unsorted list first before sorting and deduplicating. The patch also adds two new test cases in `t1091-sparse-checkout-builtin.sh` to verify that cone-mode patterns are properly deduplicated both in the sparse-checkout file and in the `sparse-checkout list` output.

The thread context shows this is now the seventh iteration of the patch, with all technical concerns addressed. Junio C Hamano has indicated the series is merge-ready pending final whitespace fixes in test formatting. The optimization maintains the distinction between cone mode (order-independent) and non-cone mode (order-sensitive) handling while improving performance for large pattern sets. The discussion revealed subtle edge cases where duplicates can still occur despite parent_hashmap's duplicate prevention, requiring the duplicate check to remain in the optimized code path.