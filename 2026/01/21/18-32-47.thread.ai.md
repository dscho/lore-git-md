Thread identity: Bug report about unexpected `.gitignore` pattern matching behavior where `backup_STOCKS*/` incorrectly suppresses both targeted `backup_STOCKS*` directories and unrelated `backups/` directory.

Core problem: Git's pattern matching appears to have a bug where a pattern like `backup_STOCKS*/` in `.gitignore` incorrectly matches both the intended `backup_STOCKS_*` directories and an unrelated `backups/` directory that should not match. The reporter provides clear reproduction steps showing this behavior in Git 2.43.0 on WSL2/Linux 6.6.87.2.

Current status: Initial bug report with no responses yet. Needs investigation from someone familiar with Git's dir.c or dir.h internals that handle pattern matching.

Key technical details: The pattern `backup_STOCKS*/` is being overmatched, affecting both:
- Expected matches: `backup_STOCKS_*` directories
- Unexpected match: `backups/` directory (should not match)

The report includes full `.gitignore` contents confirming no other patterns could explain this behavior.

Open questions: Why is the pattern matching failing in this case? Is this specific to patterns with underscores and trailing slashes? Does it affect other similar pattern constructions?

Participant positions: Alf Clement (reporter) has provided detailed reproduction case. No other participants yet.

System context: Git 2.43.0 on WSL2/Linux 6.6.87.2. Likely involves dir.c/dir.h pattern matching logic.