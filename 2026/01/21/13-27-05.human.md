This is a documentation patch addressing a linter issue that arose from the `git replay` manual page cleanup. The thread has progressed through several iterations and is now in its final stages.

Jean-Noël Avila proposes a solution to the remaining linting problem by modifying `Documentation/lint-gitlink.perl` to properly handle `ifdef`/`ifndef` block macros. The patch simplifies the linter by explicitly skipping these macro blocks rather than trying to parse them as inline elements. This follows up on Junio C Hamano's earlier work to resolve the same issue through regex adjustments.

The change is straightforward and low-risk, coming from an experienced documentation contributor. It completes the technical requirements for merging the original `git replay` formatting cleanup, which removes an unused "Commit Formatting" section from the manual page. With this linter adjustment, the documentation change can proceed without triggering false positives.

This represents the final piece needed to merge the documentation improvement that follows the pattern previously established for `git shortlog`. The patch appears ready for inclusion with no outstanding concerns.