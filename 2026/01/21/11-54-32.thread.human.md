Here's a human-readable summary of the thread so far:

This thread explores a proposal from Simon Richter to support Git repositories with intentionally missing objects, specifically addressing Debian's need to store source packages while excluding non-redistributable files or vendored dependencies. The core idea is to enable a workflow where Debian could transition from tarballs to Git-based storage while maintaining the ability to omit certain content.

The proposal outlines several key requirements: verifying the integrity of included files, tracking what was removed, allowing normal development on clones (as long as omitted paths aren't modified), and maintaining a minimal archive structure that includes commits, trees, and optionally signed tags. Simon suggests this approach might also help recover repositories with broken objects.

The discussion is currently at an early conceptual stage, focusing on whether this direction aligns with Git's design principles before considering implementation details. The use case appears particularly relevant for Debian's packaging workflow, though the technical challenges of maintaining repository integrity with intentional omissions would need careful consideration. No specific interface changes or implementation plans have been proposed yet - the email is primarily seeking feedback on the core concept's viability.