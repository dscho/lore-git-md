This is a discussion about removing global state from `git show-index` as part of the ongoing `the_repository` removal effort. Phillip Wood raises concerns about the patch's approach, noting that passing a repository* parameter to builtin commands may be the wrong direction since these commands typically operate on a single repository anyway. He points out this could cause segfaults when running outside a repository (a supported use case for `show-index`) and suggests focusing instead on removing globals from library code.

The exchange reveals a philosophical divide in the project - while removing `the_repository` is broadly agreed upon, there's disagreement about whether builtin commands should be part of this effort. Phillip argues the current approach creates more problems than it solves for commands that naturally operate on one repository, and suggests alternative cleanup opportunities in `wt-status.c`.

The technical discussion remains civil but highlights an architectural tension: how far the global variable removal should extend when it complicates rather than simplifies the implementation. Phillip's response provides useful context about where such contributions might be more impactful, while still engaging with the technical merits of the specific patch.