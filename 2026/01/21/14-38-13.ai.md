This is a follow-up discussion in the `git repo info --keys feature addition thread, focusing on final naming decisions for the output format. The thread has reached consensus that the "keyvalue" format name should be changed to "lines" for consistency with the related `git repo structure` command, as both commands now share the same format handling code.

Lucas Seiki Oshiro proposes dropping the previously submitted patch that added a "default" format option in favor of simply renaming "keyvalue" to "lines" throughout. This change would maintain consistency across commands while avoiding the need for special-case format handling. Patrick Steinhardt had previously expressed support for this naming unification, noting that the experimental nature of the `git repo` command makes such adjustments reasonable at this stage.

The technical impact would be:
- Removal of the `FORMAT_KEYVALUE` enum in favor of using `FORMAT_LINES` consistently
- Updated documentation references from "keyvalue" to "lines"
- Simplified format validation logic since both commands would share identical format names
- No need for the separate default format patch since "lines" would serve as the canonical name

This change would complete the format unification work before the series is queued in `next`, replacing the earlier `lo/repo-info-keys` topic. All substantive review feedback has been addressed, and the final decision rests on this naming clarification between the patch author and reviewers.