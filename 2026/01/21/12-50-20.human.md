This is part 4/14 of Patrick Steinhardt's ongoing ODB abstraction series, introducing a unified interface for iterating through objects. The patch adds `odb_source_loose_for_each_object()`, which provides a consistent way to iterate loose objects while populating object info, matching the behavior of `odb_source_loose_read_object()`. 

The new function serves as a stepping stone toward unifying the divergent iteration interfaces for loose and packed objects. It maintains existing functionality while adding support for object info population during iteration. The implementation handles flags consistently with the rest of the series (like `ODB_FOR_EACH_OBJECT_PROMISOR_ONLY`) and preserves the existing `for_each_loose_object()` as a temporary bridge.

This is a well-structured patch that continues the series' methodical approach to ODB abstraction. The commit message clearly explains the motivation and relationship to other parts of the system, and the implementation appears clean with appropriate documentation. Review feedback from Justin Tobler and Karthik Nayak has already been incorporated in earlier iterations.