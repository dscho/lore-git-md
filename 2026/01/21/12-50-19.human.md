This is part of Patrick Steinhardt's ongoing ODB abstraction series (v3 03/14), focusing on the object database backend infrastructure. The patch extracts a helper function `read_object_info_from_path()` from the existing `odb_source_loose_read_object_info()` to handle reading object metadata from a given path, rather than computing the path internally. This allows the path computation and object reading to be separated, making the code more modular.

The change removes the intermediate `stat_loose_object()` wrapper since its functionality is now handled directly by the new helper. The patch appears to be a straightforward refactoring with no behavior changes - it's part of the larger effort to clean up and abstract Git's object storage layer. The commit message clearly explains the motivation and impact, and the diff shows a net reduction in code while maintaining all existing functionality. This looks like a solid incremental step in the ODB abstraction work.