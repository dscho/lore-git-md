This is a final review comment on the long-running configurable branch comparison feature in `git status`, specifically addressing memory leak fixes and remaining behavior observations.

Jeff King confirms that v26 of the series (now queued by Junio) resolves his main concern about being able to disable the feature. He notes the default behavior remains unchanged, which was a point of earlier discussion. The implementation appears technically sound in his review, with one minor observation about inconsistent advice message handling that predates this feature.

The advice display quirk Jeff notes relates to `show_divergence_advice` only affecting the "use git pull" hint while other advice messages remain visible. He identifies this as originating from commit b6f3da5132 in 2023 rather than being introduced by the current series. While worth noting for future consideration, Jeff explicitly states this shouldn't block the current feature.

With Junio having queued the series and Jeff's memory leak concerns addressed, this email effectively marks the technical completion of the multi-year effort to make branch comparison in `git status` configurable. The only remaining open items are documentation updates, which were noted as a separate follow-up task in earlier messages. The thread appears to be concluding with consensus that the implementation is ready for release.