This is a refactoring patch in the hook subsystem series, specifically part 2 of 12 in version 7. The patch introduces a named `parallel_child` structure and a helper function `child_is_working()` to improve readability when checking parallel process states in the run-command machinery.

The change extracts the anonymous struct from `parallel_processes` into a named `parallel_child` type and replaces repeated state checks with the new helper function. This is preparatory work for upcoming patches that will add more helpers and state checks. The patch maintains all existing functionality while making the code more maintainable.

Junio Hamano suggested this approach, and the implementation follows through on that suggestion by providing a clearer way to work with parallel child processes. The changes are mechanical and focused on code organization rather than behavior modification, touching only `run-command.c` with straightforward additions and substitutions.

This patch represents incremental progress in the larger hook subsystem refactoring effort, which is standardizing hook execution with proper stream handling and parallel execution capabilities. The series has already received approvals from key reviewers and is nearing completion.