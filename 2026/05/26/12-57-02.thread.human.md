Here's a human-readable summary of the thread so far:

Frieder Hannenheim has proposed adding a new feature to `git mv` that would allow updating Git's index to reflect a file move without actually moving the file on disk. This would be useful when files have already been moved outside of Git (such as by an external tool or manual operation) and the user wants Git to recognize the rename after the fact.

The initial proposal is light on implementation details but raises several design questions that would need to be addressed, including what to name the new flag (`--index-only` and `--no-move` are possibilities), how to handle cases where the source file still exists, and whether directory moves should be supported. The change would require modifications to builtin/mv.c and documentation updates.

At this early stage, the thread consists solely of the initial proposal with no replies yet. The author appears to be seeking feedback on whether this functionality would be useful before proceeding with implementation work. Key open questions include whether there's consensus that this feature would be valuable, and what specific behavior it should have in edge cases.