This is the test suite for the proposed `git son` command, completing the RFC patch series. The tests cover the core functionality of creating child repositories with parent remotes, `.gitignore` updates, and various edge cases.

The test file `t5151-son.sh` includes nine test cases that verify:
- Basic child repo creation with proper directory and Git setup
- Correct configuration of the "parent" remote in the child
- Automatic addition of the child directory to the parent's `.gitignore`
- Clean failure handling when the target directory exists
- Validation of flag combinations (requiring `--inherit` with `--branch`)
- Clean rollback on validation failures
- History inheritance functionality with `--inherit`

The tests follow Git's standard test patterns using `test_expect_success` and `test_must_fail`. The file is properly integrated into both the Makefile and meson build systems, appearing in the test sequence between existing tests t5150 (request-pull) and t5200 (update-server-info).

This completes the technical implementation of the feature, providing the test coverage to validate the behavior described in the earlier documentation patch. The test cases appear well-designed to catch common failure modes while verifying the core use cases of the command. With this patch, the series now has all components needed for substantive review: implementation, documentation, and tests.