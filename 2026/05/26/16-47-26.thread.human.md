Here's the human-readable summary of the thread so far:

Evan Haque proposed a new `git son` command to create loosely-coupled child repositories, where each child would have a "parent" remote pointing back to the original repository. The implementation includes thorough documentation and test coverage, following Git's standards. However, D. Ben Knoble questioned whether this functionality is truly needed, pointing out that existing features like `git clone . child`, worktrees, and subtrees might cover similar use cases without requiring new porcelain.

Now, Claus Schneider has entered the discussion with an alternative approach. He suggests improving submodule behavior to support looser relationships, referencing his own work that makes ignored submodules truly ignored in status and add operations unless forced. This could address some of the same workflow needs that motivated `git son`, but within the existing submodule framework. Claus invites the thread to evaluate whether his submodule changes would satisfy the use case, or if there are gaps that still require a new command.

The core question remains whether Git needs a new porcelain command for this functionality, or if the needs can be met by either existing features (as Ben suggests) or enhanced submodules (as Claus proposes). Evan has not yet responded to these substantive critiques of the proposal's fundamental premise. The discussion is now focused on comparing these different approaches to managing repository relationships.