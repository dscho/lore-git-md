This is a review from Junio Hamano on the `diff.<driver>.process` feature implementation, focusing on error handling and global state management in the diff subprocess protocol.

Junio raises several technical concerns about the current implementation:

1. **Global variables**: Questions whether the new `subprocess_map` and `subprocess_map_initialized` globals are necessary, suggesting they might belong in existing structs like `userdiff_driver` or `diff_options` instead.

2. **Error handling**: Points out inconsistent error reporting:
   - Silent returns for normal cases (no process configured, capability not supported)
   - Lack of error messages for actual failures (process startup failure, protocol errors)
   - Suggests distinguishing between "not configured" (silent fallback OK) and "failed" (should warn user)

3. **Input validation**: Notes missing validation for negative size in `send_file_content()`

4. **Fallback behavior**: Disagrees with silently falling back to builtin diff for all failures, arguing some errors should be surfaced to users so they can fix their external tools.

The review is substantive, engaging with the code's behavior and architectural choices rather than surface-level issues. Junio focuses on maintainability (global state), robustness (error handling), and user experience (when to notify about problems). His comments suggest the patch needs refinement in these areas before he'd consider merging it.

This continues the thread's pattern of Junio providing detailed technical feedback while remaining neutral-to-skeptical about the feature's readiness. The review doesn't reject the concept but identifies specific implementation concerns that would need addressing, particularly around error reporting semantics and global state management.