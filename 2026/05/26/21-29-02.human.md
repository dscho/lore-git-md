This is a brief technical clarification in the ongoing discussion about adding an `--index-only` flag to `git mv`. D. Ben Knoble chimes in to note that `git add old new` would achieve the same index state as the proposed `git mv --index-only`, reinforcing Chris Torek's point that existing commands can already handle this use case.

The exchange highlights that the proposed feature is primarily about convenience rather than introducing new functionality. Knoble's observation about the `git add` syntax being even more concise than the separate `add`/`rm` sequence Torek mentioned further underscores that the core capability already exists in Git's plumbing.

This continues the thread's technical assessment that while the flag might save some CPU cycles by avoiding redundant object lookups, the fundamental rename detection behavior would remain unchanged. The discussion remains focused on whether the ergonomic improvement justifies adding a new flag to `git mv`.