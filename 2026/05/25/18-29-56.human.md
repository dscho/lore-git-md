This is a technical patch in the ongoing `diff.<driver>.process` series, which aims to enable external tools to inject diff hunks into Git's diff and blame pipelines. The patch specifically adds the configuration plumbing for specifying a long-running diff process per driver, following the same pattern as `filter.<driver>.process`.

The change is straightforward: it adds a new `process` field to the `userdiff_driver` struct and implements the config parsing logic in `userdiff_config()`. The implementation mirrors existing patterns for other driver-specific configurations like textconv and diff algorithms.

This is patch 2/4 in the series, building on the xdiff infrastructure changes from patch 1 and laying groundwork for the actual diff processing implementation in subsequent patches. The series as a whole is responding to Junio Hamano's feedback from earlier versions, with this particular piece being a relatively uncontroversial configuration addition.

The patch appears well-contained and follows established Git patterns. It's a necessary building block for the larger feature but doesn't introduce any particularly novel or risky changes on its own. Reviewers will likely focus on whether the config handling aligns with Git's existing practices (which it appears to do) rather than debating the merits of the overall feature at this stage.