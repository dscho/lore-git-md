Performance optimization discussion in commit-reach.c algorithms. Jeff King (Peff) provides a substantive review of Kristofer Karlsson's patch that optimizes ahead-behind calculations by replacing O(n) queue scans with O(1) counter-based tracking.

Peff confirms the technical approach makes sense but raises a subtle edge case regarding the PARENT2 flag - it's not cleared when commits are popped from the queue, which could theoretically lead to revisiting commits with out-of-order dates. However, he notes this doesn't actually cause problems because the same flag prevents both duplicate queue entries and incorrect counter increments. The current behavior remains correct as PARENT2 effectively serves as a "seen" marker.

This continues the thread's deep technical discussion about optimizing graph traversal performance while maintaining correctness. Peff's review demonstrates careful consideration of edge cases that characterizes high-quality feedback in the Git performance optimization space. The exchange shows the patch is technically sound but benefits from this kind of rigorous peer review.