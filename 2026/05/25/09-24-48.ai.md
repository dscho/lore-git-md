This is a follow-up discussion in the worktree path matching patch series, where Junio C Hamano seeks clarification about the Windows path handling changes in v5. 

Junio notes that the v5 changelog's explanation about using `**` instead of `/` for Windows compatibility was somewhat misleading - the core issue wasn't just about forward vs backslash path separators, but rather that Windows paths from `strbuf_realpath()` include drive letter prefixes (like "D:/a/git/...") which don't start with a slash at all. This explains why the more flexible `**` glob pattern was necessary for reliable matching.

Junio suggests adding an in-code comment to document why `**/.path` is used in the tests, as this pattern might otherwise appear unnecessarily loose or unusual to future readers. The exchange provides useful context about platform-specific path handling that could inform similar features in the future, though it doesn't require any immediate changes to the patch series.

The discussion remains technical and focused, with Junio explicitly noting his question was for understanding rather than requesting revisions. The thread shows healthy review engagement where even after multiple iterations, maintainers continue probing edge cases to ensure robust cross-platform behavior.