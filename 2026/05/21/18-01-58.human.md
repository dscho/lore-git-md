This is the first patch in a documentation series improving `git replay` configuration references. Kristoffer Haugsbakk adds the missing `replay.adoc` config file to both `git-config(1)` and `git-replay(1)` man pages. The change ensures the `replay.refAction` configuration option (added in late 2025) is properly documented in both places. 

The patch is straightforward - it simply includes the config documentation that was previously orphaned. This is typical documentation maintenance work from an experienced contributor, ensuring users can find configuration options where they'd expect them. The change follows Git's documentation conventions by adding the config reference in both the command-specific man page and the central config.adoc file.