This is a review comment from Junio C Hamano on Patrick Steinhardt's test patch for `GIT_OBJECT_DIRECTORY` handling. The patch is part of a larger 8-part series refactoring repository initialization and object database setup.

Junio points out a typo in Patrick's commit message where "environment" is misspelled as "enviroment". Beyond this minor correction, he approves the patch with a simple "Nice." - indicating the test case is well-constructed and fills an important gap in test coverage.

The review is surface-level, focusing on documentation correctness rather than technical implementation. This makes sense given the patch's straightforward nature - it adds a single test case verifying documented behavior that was previously untested. The test itself creates a custom object directory, initializes a repository with `GIT_OBJECT_DIRECTORY` set, and verifies objects are created in the specified location rather than the default `.git/objects`.

This interaction shows the maintainer's attention to detail in catching small issues even in simple patches, while also recognizing the value of the test coverage being added. The positive but brief feedback suggests the patch is on track for inclusion in the series.