This is the final patch in Harald Nordgren's series adding `git branch --prune-merged`, implementing the requested `--dry-run` mode for previewing deletions. The patch completes the functionality by:

1. Adding a `--dry-run` option that prints which branches would be deleted without actually removing them
2. Including comprehensive tests verifying:
   - Dry-run output format
   - Skipping of unmerged branches
   - Proper error when used without `--prune-merged`
3. Maintaining all existing safety checks (protected branches, config opt-outs) in dry-run mode

The implementation cleanly integrates with the series' core functionality while following Git's pattern for dry-run operations. The tests demonstrate the happy path and edge cases, including proper handling of both merged and unmerged branches. This appears to be the final piece of the series, which has now addressed all prior feedback through 10 iterations.