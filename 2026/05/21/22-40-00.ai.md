This is v10 of Harald Nordgren's series adding `git branch --prune-merged` for safe automated local branch cleanup, now with a revised interface design based on maintainer feedback. The series has pivoted from remote-specific pruning to supporting both remote-tracking and local upstream branches via a more flexible branch/glob syntax.

Key changes in v10:
- Replaces the `--forked <remote>` and `--all-remotes` flags with a more general `<branch>` argument that accepts:
  - Remote-tracking branches (e.g. `origin/master`)
  - Local branches (e.g. `main`)
  - Shell-style globs (e.g. `origin/*`)
- Adds `--dry-run` mode to preview deletions
- Retains all safety checks from previous versions:
  - Protects checked-out branches
  - Preserves default branch names
  - Skips branches with unmerged commits
  - Honors `branch.<name>.pruneMerged=false` opt-out

The implementation now aligns with Git's existing patterns (like `git checkout -b` behavior) while maintaining the original goal of safe automated cleanup. The series appears technically complete and addresses all prior feedback, with clean range-diffs showing focused changes between versions.

Notable technical details:
- Uses wildmatch for glob pattern matching
- Combines literal and glob arguments via union
- Preserves branches by name (not just by upstream)
- Includes comprehensive tests covering:
  - Local vs remote upstreams
  - Default branch protection
  - Dry-run output
  - Error cases

The thread context indicates Junio C Hamano had approved the safety model in v9 but requested the interface generalization, which this version delivers. Phillip Wood's suggestions around local branch support and dry-run mode are also incorporated. The series looks ready for final review.