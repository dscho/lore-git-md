This is patch 14/18 in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `write_object()` callback for the loose source implementation.

The key change moves the `odb_source_loose_write_object()` function from object-file.c to odb/source-loose.c and connects it as the loose source's write callback. This continues the mechanical conversion of direct function calls to callback-based operations through the odb_source interface. The patch also exposes some helper functions (`write_object_file_prepare()` and `write_loose_object()`) that are now shared between subsystems.

The changes are focused and follow the established pattern from previous patches in the series. The diff shows the typical structure of this refactoring work: moving code to its new home while maintaining all existing functionality. The risk appears low since this is an internal reorganization rather than a behavior change.

This patch represents steady progress in the larger ODB abstraction effort, systematically converting another piece of the loose object handling to the new callback-based interface. The series continues to demonstrate Patrick's methodical approach to this complex refactoring.