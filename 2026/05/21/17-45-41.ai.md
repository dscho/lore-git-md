This is a technical discussion about fixing git-gui's worktree detection for linked worktrees, continuing from a previous patch submission. The thread focuses on path resolution issues where git-gui fails to properly handle worktrees created via `git worktree add`.

Mark Levedahl responds to Shroom Moo's critique of the current implementation, which looks for a literal "gitdir" file in the current working directory rather than using the proper `$_gitdir` path that would point to `.git/worktrees/<name>/gitdir`. Mark argues the code can only execute when already in the correct gitdir context, suggesting the path resolution might not be as broken as Shroom claims.

The discussion reveals differing interpretations of when and where the worktree detection logic runs. Shroom maintains the current approach is fundamentally flawed since it searches in the wrong location, while Mark contends the context ensures proper execution. This back-and-forth highlights the need for clearer documentation of git-gui's execution flow and worktree detection assumptions.

The technical debate centers on whether to:
1. Fix the path resolution by properly using `$_gitdir`
2. Implement an alternative approach checking for .git files pointing to worktree directories
3. Clarify the execution context constraints

This appears to be a substantive discussion between two contributors with different perspectives on how to solve the linked worktree detection problem in git-gui. The disagreement suggests the patch may need revision to address both the technical issue and the conceptual differences in approach.