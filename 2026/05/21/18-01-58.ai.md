This is the first patch in a documentation series improving `git replay` configuration references. Kristoffer Haugsbakk adds the missing `replay.adoc` config file to both `git-config(1)` and `git-replay(1)` man pages, establishing the cross-reference that was introduced in commit 336ac90c but never properly linked. The change is straightforward - just two include directives added to connect the configuration documentation to its relevant command documentation. This is typical documentation maintenance work from a contributor known for careful attention to man page completeness and cross-references. The patch follows the project's standard for config documentation inclusion.