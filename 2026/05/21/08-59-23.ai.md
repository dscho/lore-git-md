This is a CI/build system patch series from Patrick Steinhardt that updates GitLab's macOS CI configuration. The two patches make minor adjustments to the macOS test environment:

1. Upgrades the macOS runner hardware specification
2. Updates the macOS base image version

The changes are straightforward infrastructure updates rather than functional modifications to Git itself. The series appears to be well-motivated (keeping CI environments current) and low-risk, touching only the `.gitlab-ci.yml` file with simple version number changes. Patrick has tested the changes in GitLab's own CI pipeline (linked in the cover letter) before submitting.

As this is infrastructure maintenance from a GitLab employee who regularly contributes CI-related patches, it's likely to be non-controversial and quickly merged. The changes don't affect Git's behavior or require review from subsystem experts - they just keep the macOS test environment up to date.