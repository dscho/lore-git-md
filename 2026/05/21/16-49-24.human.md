This is a review from Junio C Hamano on Patrick Steinhardt's test patch for `GIT_OBJECT_DIRECTORY` behavior in `git init`. The patch is part of a larger 8-part series refactoring repository initialization and object database setup.

Junio points out a typo in Patrick's commit message ("enviroment" instead of "environment") but otherwise approves the patch with a simple "Nice." The patch itself is straightforward - it adds a test case verifying that `git init` correctly creates the object database in the location specified by `GIT_OBJECT_DIRECTORY` rather than the default `.git/objects` location.

The review is surface-level, focusing on the typo rather than the technical content. This makes sense given the patch's simplicity - it's a small test addition that clearly fills a gap in test coverage. The test case itself looks solid, checking both that objects aren't created in the default location and that they are created in the specified custom location.

This interaction shows the maintainer's typical attention to detail, catching minor wording issues even in straightforward patches. The quick approval suggests the test is uncontroversial and well-aligned with the series' goals of improving initialization reliability.