This is the 9/18 patch in Patrick Steinhardt's series refactoring Git's loose object handling as part of the ODB (object database) abstraction effort. The patch wires up the `find_abbrev_len` callback for the loose object source, continuing the mechanical conversion of loose object functionality to the new callback-based interface.

The changes move the abbreviation length calculation code from object-file.c to odb/source-loose.c and update the files backend to use the new callback-based interface. The implementation remains functionally identical - it's just being relocated and wired into the new abstraction layer. The patch follows the established pattern from previous patches in the series, with clean code movement and straightforward interface updates.

This continues the systematic conversion of loose object handling to the odb_source interface, with each patch focusing on wiring up a specific callback. The changes are well-contained and low-risk since they don't modify behavior, just the organization and calling conventions. The series as a whole represents another solid step in Patrick's ongoing work to make Git's object storage pluggable.