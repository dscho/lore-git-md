This is the second patch in Tuomas Ahola's five-part series fixing edge cases in Git's date approximation logic. The patch enhances the date parsing test infrastructure in t0006-date.sh to support time adjustments, which will be used to verify the series' fixes for special date/time formats.

The key addition is the `add_time_offset()` helper function that calculates time offset in hours or days, plus modifications to `check_approxidate()` to temporarily adjust the test date when needed. This allows testing date parsing behavior across day/month/year boundaries without hardcoding multiple test dates. The patch demonstrates the new capability with a test case for "January 5th yesterday" that verifies correct behavior when crossing a year boundary (+2 days offset).

The test infrastructure changes are cleanly implemented and well-motivated, providing the foundation for the more substantive fixes in subsequent patches. The patch follows Git's testing conventions and maintains backward compatibility with existing tests. As noted in the thread summary, this infrastructure patch was already accepted in previous iterations of the series.