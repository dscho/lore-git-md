This is patch 7/8 in Patrick Steinhardt's series refactoring repository initialization and object database setup. The patch eliminates a redundant read of the loose object map during repository initialization.

The change removes a second call to `repo_read_loose_object_map()` in `repo_init()` since the map is already read by `repo_set_compat_hash_algo()` via `apply_repository_format()`. This is a straightforward cleanup that doesn't change behavior but removes unnecessary work.

The patch follows the series' pattern of making repository setup more efficient and intentional. It touches `repository.c` with a simple 3-line deletion. As with the rest of the series, this is internal plumbing work that maintains existing functionality while cleaning up the codebase in preparation for future ODB abstraction work.