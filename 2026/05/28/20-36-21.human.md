This is a concluding technical clarification in the now-resolved `git mv --index-only` discussion thread. Junio Hamano confirms D. Ben Knoble's observation about index/working-tree divergence being a normal Git state, not a special concern for the proposed feature.

Junio provides concrete examples showing how `git mv` already preserves the index state without rehashing, whether operating normally or in a hypothetical `--cached` mode. The key insight is that Git's index is designed to track the repository state, not necessarily match the working tree - a fundamental behavior that would apply equally to an index-only move operation.

The exchange serves as a final quality check, confirming that even if implemented, the proposed `--index-only` flag wouldn't introduce any new edge cases beyond Git's existing handling of index divergence. With Junio's authoritative confirmation and the thread having reached consensus on the niche nature of the use case, this effectively closes any remaining discussion points.