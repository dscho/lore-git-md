This is Junio Hamano (the Git maintainer) responding to Harald Nordgren's patch that initialized a large packet buffer to avoid a macOS linker warning. Junio expresses skepticism about the linker's behavior, calling it "nuts" that a simple character array would trigger alignment constraints. 

He dislikes the proposed solution of zero-initializing the array, worrying it might bloat object files on some compilers (though testing shows modern compilers optimize this away). Instead, he suggests trying `-fno-common` as a compiler flag workaround, proposing this could be conditionally added in `config.mak.uname` for affected macOS versions.

The exchange highlights a classic platform compatibility dilemma: whether to accommodate a toolchain quirk or push back on what appears to be unreasonable behavior. Junio's response indicates he views this as a linker/compiler issue rather than a Git problem needing a code change, though he remains open to practical solutions for affected users. The thread may continue with testing results from different compiler versions or macOS environments.