This thread is about a two-patch bugfix series (now at v3) by Son Luong Ngoc that fixes edge cases in `git rebase --update-refs` when dealing with symbolic references (symrefs). The issue occurs when branches or other refs are symrefs—for example, when `main` points to `master` or a symref points to a tag—causing rebase failures even if the primary branch rebases successfully. This affects workflows like branch rename migrations and cross-worktree setups.

The series is now complete, with all review feedback from Phillip Wood incorporated. The first patch handles symrefs that point to other branches, while this second patch extends protection to non-branch symrefs (like tags) and prevents duplicate updates when multiple aliases point to the same ref. The implementation is focused and minimal, touching `sequencer.c` and `branch.c`, with thorough test coverage in `t3404-rebase-interactive.sh` for edge cases like symref chains, cross-worktree conflicts, and deduplication.

The series is ready for maintainer consideration, with no open technical questions. Phillip Wood’s reviews were substantive, covering symref resolution, checked-out branch detection, and cross-worktree conflicts, and all concerns have been addressed. Junio C Hamano’s earlier architectural question about ref filtering consistency was resolved by the current implementation. The patches are split logically, with clear commit messages and test coverage demonstrating the fixes.