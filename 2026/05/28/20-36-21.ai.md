This is a final clarification in the concluded `git mv --index-only` discussion thread, where Junio Hamano confirms the technical behavior of index/working-tree divergence.

Junio reinforces that Git's design intentionally allows divergence between the index and working tree as a normal state, using the existing `git mv` behavior as an example. He demonstrates how a dirty working tree file (modified after `git add`) remains properly tracked after a move operation without requiring rehashing. The key insight is that an index-only move would maintain this same intentional divergence pattern - the index entry would correctly reflect the pre-move state while the working tree shows the post-move state.

This serves as the authoritative confirmation that D. Ben Knoble's concern about divergence is not just acceptable but actually aligns with Git's existing design principles. The exchange provides closure to the thread by showing that even the edge case behavior would be consistent with how Git already handles similar situations. With both the technical approach and user experience implications now fully addressed, the discussion appears complete.