Bugfix for HTTP pack index handling, addressing both a memory leak and redundant cleanup.

This v2 patch from Lorenzo Pegorari builds on the initial memory leak fix in `fetch_and_setup_pack_index()` by additionally removing an unnecessary `unlink()` call. The change is based on Jeff King's review of the first version, which noted that tempfile cleanup is now properly handled by the tempfile API since commit 63aca3f7f1.

The patch maintains the core fix - ensuring proper cleanup of mmap'd pack indexes and allocated memory when verification fails - while simplifying the code by relying on the tempfile subsystem's process-exit cleanup. The diff shows careful attention to error paths, with cleanup now happening in a consistent order regardless of success/failure.

The discussion has evolved from a straightforward leak fix to considering broader resource management patterns in the HTTP transport code. While this version appears ready to merge (the leak is fixed and the tempfile handling is now correct), Peff's earlier comments suggest there may still be room for follow-up work to make the tempfile handling more consistent across all error cases.