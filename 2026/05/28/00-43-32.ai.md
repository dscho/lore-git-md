This is a bugfix for `git describe` regarding the invalid combination of `--contains --all` with pattern matching options (`--match` and `--exclude`).

The thread confirms the issue persists in current Git - when users specify pattern matching options with `--contains --all`, the patterns are silently ignored rather than being rejected as invalid. Jacob Keller had submitted an initial patch to make this combination fail with an error, but it was never merged. Tuomas Ahola independently encountered the same issue, providing concrete examples showing both `--match=bogus` and `--exclude="*"` having no effect when combined with `--contains --all`.

The technical solution remains Jacob's original approach - adding validation to explicitly reject this option combination with a clear error message. While the patch was initially untested, the independent confirmation from Tuomas suggests this is a real-world issue worth fixing. Eric Sunshine had previously reviewed the patch with only minor surface-level suggestions about commit message wording and an optional code structure change (converting nested if/else to else-if).

This appears ready for Jacob to revive with minimal changes needed - likely just addressing the commit message typo ("bail of" -> "bail if") and potentially adding tests to prevent regression. The fix is straightforward and uncontroversial, with clear user impact demonstrated. The thread shows consensus that silently ignoring these patterns is worse than failing with an explanation.