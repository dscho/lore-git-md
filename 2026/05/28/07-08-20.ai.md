This is a review comment on a bugfix series for `git rebase --update-refs` behavior with branch symrefs. Kristoffer Haugsbakk points out a stylistic issue with how the fix was structured across two patches.

The reviewer notes that while the approach of first adding a failing test (`test_expect_failure`) and then fixing it in a subsequent patch is technically valid, the Git project prefers combining the bug fix and regression test into a single patch. They reference a message from Junio Hamano (the maintainer) that establishes this preference.

This is a surface-level review focused on project conventions rather than the technical content of the fix itself. The comment doesn't challenge the correctness of the solution for handling symrefs during rebase operations, which was established in the previous patches. The feedback is purely about patch organization - suggesting that future contributions should bundle tests with their corresponding fixes rather than splitting them across commits.

The review is brief and to the point, citing project norms rather than making technical suggestions. It doesn't require any changes to the actual code or test logic, just a different organization approach for similar fixes in the future.