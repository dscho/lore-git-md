Here's the updated human-readable thread summary:

The thread addresses a bug in `git describe` where `--exclude` and `--match` patterns weren't properly applied when used with `--contains --all`, despite the documentation suggesting they should work together. Jacob Keller's initial patch fixed the basic pattern forwarding, but Junio C Hamano spotted an edge case where exclude patterns fail to match full remote branch names (like "remotes/origin/remote_branch_A") when they should be filtered out.

Tuomas Ahola has now responded with a minimal follow-up patch that changes the sorting algorithm in `name-rev.c` from `QSORT` to `STABLE_QSORT`, which at least stabilizes the test output on Alpine Linux. While this makes the test results consistent across platforms, it's unclear whether it fully resolves the core issue of remote ref pattern matching. The thread remains open on whether the solution requires deeper changes to the pattern matching logic or if the documentation needs adjustment to clarify the expected behavior with remote refs. Junio's original concern about proper exclusion of remote branches still needs to be addressed, though Ahola's patch may help isolate the problem by eliminating sorting instability as a variable.