Thread identity: bugfix for `git describe`'s handling of `--exclude` and `--match` with `--contains` and `--all`.

Core problem: `git describe --contains --all` was not properly forwarding `--match` and `--exclude` patterns to `git name-rev` despite documentation claiming these options should work together. The patterns were silently ignored in `--all` mode.

Current status: Jacob Keller has sent a patch fixing this by restructuring the pattern passing logic to handle both modes correctly. The fix properly passes patterns to `name-rev` with appropriate ref prefixes (refs/tags, refs/heads, refs/remotes) depending on `--all` usage.

Key technical details: The change affects `builtin/describe.c`'s handling of pattern matching when `--contains` is combined with `--all`. Four new tests in `t6120-describe.sh` verify the fixed behavior across combinations of `--match` and `--exclude` with branches and tags.

Open questions: None raised yet - this appears to be a straightforward documentation/implementation mismatch being corrected.

Participant positions: Jacob Keller authored the fix. No reviews or other participants mentioned yet.

Related work: This touches the same area as previous `git describe` improvements around pattern matching and ref selection behavior.