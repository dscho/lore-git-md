Thread identity: bugfix for `git describe`'s handling of `--exclude` and `--match` with `--contains` and `--all`.

Core problem: `git describe --contains --all` was not properly forwarding `--match` and `--exclude` patterns to `git name-rev` despite documentation claiming these options should work together. The patterns were silently ignored in `--all` mode, and a follow-up CI failure revealed they also weren't being correctly applied to remote refs.

Current status: Jacob Keller's initial patch fixed the basic pattern forwarding but Junio C Hamano has identified an edge case where exclude patterns fail to match remote branch names (e.g., "remotes/origin/remote_branch_A" appears when it should be excluded). The test expects "branch_A" but gets the full remote ref name instead.

Key technical details: The change in `builtin/describe.c` restructures pattern passing logic for `--contains --all` modes. While the initial implementation correctly handled local branches and tags, the CI failure shows remote ref matching needs adjustment. The test suite (`t6120-describe.sh`) now needs to cover this remote ref case.

Open questions: How to properly apply exclude patterns to remote ref names when using `--all`. The current behavior may need pattern matching adjustments or documentation clarification if the current output is actually correct but unintuitive.

Participant positions: Jacob Keller authored the original fix. Junio C Hamano identified the remote ref edge case through CI failures. No other substantive reviews yet.

Related work: Builds on previous `git describe` improvements around pattern matching and ref selection behavior. The remote ref handling may need to align with how other commands process remote branch names with pattern matching.