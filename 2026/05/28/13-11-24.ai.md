This is a Windows platform compatibility patch discussion regarding signal handling in the Windows compatibility layer. Johannes Schindelin (Git for Windows maintainer) responds to Junio's earlier query about distinguishing between SIGTERM and SIGKILL on Windows.

Schindelin reveals that Git for Windows has a more sophisticated implementation than what's being proposed - it actually differentiates SIGTERM by allowing processes to run `atexit()` handlers, while the current patch would treat all signals identically via immediate termination. He indicates this divergence should have been upstreamed earlier and commits to preparing those patches for submission.

This introduces new context that wasn't apparent from Microsoft's documentation alone - while Windows' native termination model is indeed simpler than Unix signals, Git for Windows has already implemented additional emulation behavior that preserves some Unix semantics. The revelation suggests the current patch may need to be reworked or delayed until Git for Windows' more complete solution can be upstreamed.

Key technical points:
- Git for Windows already handles SIGTERM differently from other signals
- Current patch would conflict with this existing behavior
- Schindelin plans to upstream the more complete solution
- Windows signal emulation is more nuanced than the platform's native API suggests

The thread now awaits Schindelin's promised patches to see how they reconcile Unix signal expectations with Windows process management realities. This response effectively puts the current patch on hold pending that more comprehensive solution.