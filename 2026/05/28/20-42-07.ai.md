This is a follow-up discussion from the maintainer (Junio C Hamano) regarding the `git rebase --update-refs` symref handling bugfix series. Junio raises a question about overlap with another recent topic that dealt with similar ref filtering logic, though he doesn't recall the exact details.

The core observation is whether the current symref resolution approach (which filters out non-concrete branch names) might subsume or conflict with another recent change that filtered non-branch names. Junio is essentially asking if these two efforts are solving similar problems in different contexts, and whether their solutions could be unified or if one might make the other redundant.

This isn't a blocking concern - Junio notes he only "vaguely recalls" the other topic - but it's the kind of architectural question maintainers often raise to ensure changes fit coherently. The author may need to investigate whether there's actual overlap with the `git describe` case Junio mentions, or whether these are distinct scenarios requiring separate handling.

The thread context shows this is the second version of a well-tested fix that already addresses the symref edge case in rebase. Junio's comment suggests looking at the broader picture of how Git handles ref filtering across different commands, but doesn't indicate any problems with the current series' technical approach.