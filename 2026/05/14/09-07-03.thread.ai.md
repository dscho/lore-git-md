Thread identity: [PATCH v4 0/3] git bisect: respect user-configured alternate bisect terms (Jonas Rebmann, three-patch series).

Core problem or goal: `git bisect` status output and `git rev-parse --bisect` ref lookup hardcode "good"/"bad" terms even when the user has configured alternate terms (e.g. "old"/"new").

Current status: v4 has been submitted and is complete. All review feedback from Junio C Hamano and Phillip Wood has been addressed. No outstanding design objections remain. The series is ready for merge.

Key technical details:
- Files touched: `bisect.c` (status messages), `builtin/bisect--helper.c` (error messages), `builtin/rev-parse.c`, `t/t6030-bisect-porcelain.sh`, `t/t1517-rev-parse-bisect.sh`.
- Patch 1: fixes `bisect_print_status()` (three messages) and `bisect_run()` (two error messages) to use `terms->term_good`/`terms->term_bad` instead of hardcoded strings.
- Patch 2: adds single-quoting around `%s` format specifiers for bisect terms in all status and error messages in `bisect.c` and `builtin/bisect.c` (affects "first bad commit" output, "waiting for both good and bad commits" status lines, "mistook good and bad" error messages, log-file comments, and run-command output) -- formatting-only change, no behavioral alteration.
- Patch 3: fixes `git rev-parse --bisect` to read actual terms from `BISECT_TERMS` via `read_bisect_terms()`, builds ref prefixes dynamically with `xstrfmt()` (each call has a matching `free`), and lists the correct custom-named refs. Test creates a bisect session with `--term-old=known --term-new=curious`, deliberately creates both custom-termed refs (`refs/bisect/curious-*`, `refs/bisect/known-*`) and standard ones (`refs/bisect/bad`, `refs/bisect/good`), and verifies only the former are listed -- confirming the hardcoded ref fallback is correctly bypassed. Comment in test explains why the standard refs should be ignored.
- v4 changes from v3: memory leaks fixed in `read_bisect_terms()` and `xstrfmt()`; new patch 2 split out for quoting consistency; tests use `test_grep` instead of bare `grep`; commit messages improved. Patch 2's test changes account for 114 lines of diff in `t6030-bisect-porcelain.sh` -- every one a mechanical string update from unquoted to quoted output patterns.
- Test coverage: porcelain test script (124 lines changed) -- tests grep output for dynamic terms; new rev-parse test case.

Open questions and loose ends: None. Series is ready for merge.

Participant positions:
- Junio C Hamano: substantive reviewer across v1-v3; requested consistent quoting, which led to v4 patch 2.
- Phillip Wood: substantive reviewer across v1-v3; identified memory leak issues (fixed in v4).
- No other reviewers have participated. No positions changed. The only review feedback provided was iterative polish; no one has independently verified correctness at a deep level, but the fixes are mechanically straightforward. The fix in patch 3 is correct: every `xstrfmt` has a matching `free`.

Related work: None.