This is a maintainer response to a surface-level code review, part of Pablo Sabater's patch series for fixing `git log --graph` rendering of commits with excluded parents (including root commits).

Pablo replies to Jeff King's Coverity-spotted dead-code finding and confirms the issue: the `if (seen_this) continue;` guard inside the `else` block is indeed unreachable. He explains that the code was written to match the pattern used in `graph_output_commit_line()`, where `seen_this` *can* be true when a placeholder is encountered -- but in `graph_output_post_merge_line()`, the control flow guarantees that execution only reaches that branch when `seen_this` is false. The `continue` guard is cargo-culted logic that never fires, and Pablo says he will drop it.

This is a clean, low-friction resolution. Pablo acknowledges the oversight without defensiveness, explains *why* the dead code exists (copying a pattern without adapting it to the different control flow), and commits to the fix. No sign of deeper logic errors -- just a leftover guard that should not have been there.

From a reviewer's perspective, this is the right outcome: the dead code gets removed, the patch becomes slightly cleaner, and Peff's sharp eye has served its purpose. The series is one small step closer to being ready for inclusion, assuming no other surprises turn up.