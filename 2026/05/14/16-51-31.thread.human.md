**Thread Summary:**

This thread introduces a performance optimization for Git's revision walking code, specifically targeting the `limit_list()` function used in commands like `--left-right`, bisect, and rebase's fork-point computation. The patch replaces a date-sorted linked list (with O(n) insertion cost) with a priority queue (binary heap) that offers O(log n) insertion, yielding 2.5-3x speedups when processing large numbers of commits in merge-heavy repositories. Benchmark results from a 2.3M-commit monorepo demonstrate the improvement scales with commit set size, while small traversals show no regression. The change is cleanly implemented (21 lines added, 17 removed) since `process_parents()` already supported both data structures. The commit message provides thorough justification of the algorithmic improvement and its practical impact across several Git commands.