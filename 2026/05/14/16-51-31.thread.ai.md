Thread summary:

Performance optimization patch for revision walking, replacing a linked list with a priority queue in `limit_list()` to improve merge-heavy repository performance. The change switches from O(n) to O(log n) insertion cost when processing commits in the symmetric difference, showing 2.5-3x speedups for large commit sets (thousands of commits) while maintaining parity for small traversals. Affects commands using `limit_list()` including `--left-right`, `--cherry-mark`, bisect, rebase fork-point, and `git status --ahead-behind`. Implementation leverages existing `process_parents()` support for both data structures (21+17-). Includes benchmarks from a 2.3M-commit monorepo demonstrating scaling impact. The commit message provides thorough algorithmic analysis and practical implications.