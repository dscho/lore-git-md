Performance optimization patch series (v2) for revision walking's `limit_list()` has been approved and merged, replacing a linked list with a priority queue for O(log n) insertion in merge-heavy repositories (1.6-4.3x speedups). Key participants Derrick Stolee, Jeff King, and Junio C Hamano approved the changes, which affect symmetric difference operations (`--left-right`, `--cherry-mark`, bisect, rebase fork-point, `git status --ahead-behind`). Kristofer Karlsson identified potential extension to `get_revision_1()` (3.3x speedup for `git rev-list HEAD` in monorepos), with Jeff King confirming viability and sharing unpublished local patches implementing this idea.

Current discussion compares two approaches for extending priority queue usage: Kristofer's hybrid linked-list + priority queue (4 files touched, 4% faster in limited walks) versus Jeff's full conversion to priority queues (12 files touched). Both show 3-5x speedups in 2.4M-commit monorepo benchmarks. Jeff King's latest response examines tradeoffs - his comprehensive approach simplifies traversal logic but requires more invasive changes, while Kristofer's vtable-based walk strategy system maintains more of the existing structure but may complicate caller requirements. Jeff identifies specific concerns around `cherry_pick_list()` ordering dependencies and questions whether the vtable abstraction improves clarity. Benchmark results remain comparable between approaches, with Kristofer's showing slight edge in limited walks. René Scharfe's historical benchmarking (v2.0.1 to v2.50.1) showed consistent 1.16-1.28x speedups for linear histories, confirming the optimization's broad applicability.

Open questions center on architectural direction: whether to prioritize comprehensive conversion (Jeff) or incremental optimization with abstraction layer (Kristofer), with particular attention needed to edge cases in cherry-pick and symmetric difference operations. Jeff has not taken a firm position but has surfaced key technical considerations that need resolution before either approach can move forward. The thread represents a natural progression from the merged `limit_list()` work, with both contributors demonstrating viable paths to significant performance gains.