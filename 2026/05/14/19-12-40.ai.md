Performance optimization follow-up addressing Junio's review feedback on the st_add() Clang optimization patch.

Junio Hamano responds to René Scharfe's Clang-specific optimization of st_add() (which showed 5% speedup on M1 for strbuf operations) by suggesting an alternative approach that might simplify the code. Rather than having a Clang-specific variant of st_add(), Junio proposes modifying the underlying unsigned_add_overflows() helper to match __builtin_add_overflow()'s signature - taking a pointer parameter for the result. This would allow all callers (currently 18 sites) to benefit from the optimization without needing compiler-specific variants.

The suggestion reflects Junio's maintainer perspective of looking for architectural consistency rather than just local improvements. While René's patch was narrowly focused on the performance-critical st_add() case, Junio is considering how to generalize the approach across all overflow checks. This would require updating more call sites but could provide broader benefits and reduce future divergence between compiler implementations.

The thread continues the discussion from the previous patch about balancing performance, safety, and code maintainability in Git's low-level arithmetic operations. Junio's response indicates the optimization is conceptually sound but may need refinement to fit Git's coding patterns more naturally. No resolution is reached yet - the ball is back in René's court to either adapt the approach or justify keeping the Clang-specific variant.