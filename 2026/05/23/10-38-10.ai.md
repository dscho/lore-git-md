This is a technical infrastructure patch in Siddh Raman Pant's series adding external notes display support. The patch introduces timeout and deadline handling for read operations in preparation for safely executing external notes display commands.

The changes add new variants of read functions (`xread_timeout`, `xread_deadline`, and corresponding `read_in_full` versions) that allow callers to specify timeouts in milliseconds or absolute deadlines in nanoseconds. The implementation uses `poll()` to wait for file descriptor readiness with timeout support, falling back to the existing non-blocking behavior when no timeout is specified.

Key technical aspects:
- Adds timeout-aware versions of core I/O functions in wrapper.c
- Implements deadline support by calculating remaining time from absolute timestamps
- Maintains backward compatibility with existing non-timeout functions
- Includes comprehensive error handling for timeout and non-blocking cases
- Modifies strbuf.c to support the new timeout variants for line reading

The patch appears well-structured with clear separation between the new timeout functionality and existing code paths. The changes are focused on the infrastructure needed for the external notes feature rather than implementing notes-specific logic directly. This suggests the series is following Git's common pattern of building general-purpose plumbing before adding feature-specific code on top.