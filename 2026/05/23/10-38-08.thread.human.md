This thread is about adding support for external commands to display Git notes, allowing custom storage backends or transformation logic. The author, Siddh Raman Pant, has submitted a 4-patch series (v3) that introduces a new protocol for delegating notes content retrieval to external commands, positioning it as a solution for TOCTOU issues in large repositories and a general mechanism for integrating external data into Git's notes display.

The series has sparked an architectural debate with Johannes Sixt (Hannes), who argues the problem could be solved by improving Git's native notes implementation or tracking review metadata externally. Despite two pings, the thread has seen no technical review of the implementation itself. The latest email from Siddh explicitly asks whether Hannes' silence should be interpreted as a NACK, signaling uncertainty about the series' future.

Key details include the protocol design (external commands receive commit OIDs via stdin and return length-prefixed notes content), new configuration options (`notes.externalCommand`, `notes.externalCommandTimeoutMs`), and integration with commands like `log`, `range-diff`, and `describe`. The series includes robust test coverage and security measures like protected configuration scope and timeout enforcement. However, open questions remain about architectural fit, security risks, protocol robustness, and performance impact. No other reviewers have engaged with the thread, leaving the debate unresolved.