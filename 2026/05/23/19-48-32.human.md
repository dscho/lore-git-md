Feature patch series adding `--track=fetch` mode to `git checkout`/`git switch`, now in its 13th iteration. This version makes two key refinements to the implementation:

1. **Code reuse**: The patch now properly shares logic with `git branch --track` by exposing and using the existing `find_tracking_remote_for_ref()` and `advise_ambiguous_fetch_refspec()` helpers from branch.c. This ensures consistent behavior when multiple remotes could match the tracking ref.

2. **Error messaging**: The ambiguous remote case now uses the same wording as `git branch --track` when advising users about conflicting remotes, rather than inventing new phrasing. This appears to be the final polish before merging, having addressed all prior technical concerns and review feedback. The series has been through extensive review with Junio C Hamano and Phillip Wood, who have approved the architectural approach. 

The feature itself automates the common workflow where users want to both create a tracking branch and fetch its upstream in one step. The implementation carefully handles edge cases like ambiguous remote mappings and missing HEAD references while maintaining consistency with existing Git behaviors. With comprehensive test coverage and documentation updates, this looks ready for final maintainer approval.