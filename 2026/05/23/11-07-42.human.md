This is the second patch in a two-part test modernization series from a GSoC participant. It follows up on the first patch's conversion of legacy test constructs by cleaning up now-unused debug code and variables in t2000.

The changes are straightforward:
- Removes the `show_files` helper function that was previously used with `test_debug` calls (which were eliminated in the first patch)
- Deletes an unused `tree3` variable assignment that was left over from earlier test logic

The patch addresses feedback from Junio C Hamano about keeping the test script clean after modernization. Like the first patch, this is a mechanical change that doesn't affect test behavior - it just removes dead code that became obsolete after the test pattern updates. The changes are small (12 lines removed) and focused, continuing the test file's transition to modern Git test helper conventions.