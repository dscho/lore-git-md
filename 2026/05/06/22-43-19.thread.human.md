Here's a human-readable summary of the thread so far:

Johannes Schindelin has proposed an RFC series to add merge commit support to the relatively new `git history` command (introduced in Git 2.54). The implementation builds on Elijah Newren's design and currently focuses on handling two-parent merges while explicitly deferring support for octopus merges and merge reverts. The core approach uses a three-way merge composition strategy involving auto-merges of both the original and rewritten parents.

The most recent patch in the series updates the command's documentation to reflect these new capabilities and limitations. It replaces the previous statement about merge incompatibility with detailed explanations of what is now supported (the R/O/N merge algorithm) and what remains out of scope. The documentation also helps users understand when they should fall back to interactive rebase instead.

The implementation includes performance optimizations for common cases (like when only commit messages change) and extensive test coverage. The series remains in RFC status, with Johannes seeking feedback on the merge conflict handling strategy, edge case behaviors, and the new optimization's impact. Reviewers are encouraged to examine both the core merge composition logic and the optimization's correctness guarantees.