Thread identity: rebase --update-refs bugfix addressing edge case with rebase.instructionFormat and non-branch refs, now in v3 with confirmed approach for local tag handling and parallel code path identification.

Core problem: When rebase.instructionFormat includes %d (show all ref decorations), the rebase machinery could incorrectly generate update-ref instructions for non-branch refs like HEAD. The solution filters to DECORATION_REF_LOCAL types, with v3 confirming this correctly handles the intended branch refs through existing load_branch_decorations() filtering in sequencer.c's todo_list_add_update_ref_commands().

Current status: Patch v3 remains unchanged from v2 with Phillip Wood's Reviewed-by, as the original concern about local tag handling was resolved by understanding the existing branch filtering. Phillip has identified a related issue in make_script_with_merges() needing the same decoration filtering, providing a follow-up patch for that separate code path.

Key technical details:
- Original fix in sequencer.c correctly handles branch refs via load_branch_decorations()
- Newly identified parallel issue in make_script_with_merges() needs same DECORATION_REF_LOCAL filtering
- Test improvements in t/t3404-rebase-interactive.sh remain valid
- Targets --update-refs feature from Git 2.38
- Phillip Wood's follow-up snippet shows how to extend the fix to make_script_with_merges()

Open questions: Whether to combine the make_script_with_merges() fix with the current patch or handle it separately. The original author needs to decide on next steps for the additional code path.

Participant positions:
- Phillip Wood provided authoritative clarification about existing branch filtering
- Confirmed current solution is correct while identifying related issue
- Proposed follow-up fix for parallel code path
- Maintainer Junio Hamano's initial concern has been addressed
- Original author needs to respond to the new development

Related work: Builds on --update-refs feature from Git 2.38. Phillip Wood is sequencer expert. The discussion now covers two related code paths in the rebase machinery that need consistent ref filtering.