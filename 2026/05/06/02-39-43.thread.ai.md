Thread identity: rebase --update-refs bugfix for rebase.instructionFormat with non-branch refs; now at v3 with Reviewed-by and approach confirmed by Junio C Hamano. Series is complete as-is.

Core problem: When rebase.instructionFormat includes %d (ref decorations), the rebase machinery incorrectly generates update-ref instructions for non-branch refs like HEAD or tags in both interactive and non-interactive modes. The fix in sequencer.c filters decorations to DECORATION_REF_LOCAL types via load_branch_decorations().

Current status: Patch v3 has Phillip Wood's Reviewed-by and Junio C Hamano's confirmation that the approach is correct. Regression tests in t/t3404-rebase-interactive.sh verify the fix. Phillip Wood's latest analysis confirms the make_script_with_merges() path does not need parallel changes, making the series complete as submitted.

Key technical details:
- Fix in todo_list_add_update_ref_commands() merges two if statements into one for cleaner logic.
- load_branch_decorations() already filters to local branches; the additional DECORATION_REF_LOCAL guard catches edge cases.
- The --update-refs feature was introduced in Git 2.38.
- Both interactive and non-interactive rebase share the affected code path.
- make_script_with_merges() path is unaffected because it loads decorations before commit formatting.

Open questions: None remaining - series is ready for merging.

Participant positions:
- Phillip Wood: Reviewed-by on v3; confirmed non-interactive sharing is expected architecture; provided final analysis showing make_script_with_merges() needs no changes.
- Junio C Hamano: Confirmed the approach is correct and defended %d usage as valid.
- Abhinav Gupta: Validated the issue in non-interactive mode; agrees with the fix.

Related work: Builds on the --update-refs feature from Git 2.38.