Thread identity: rebase --update-refs bugfix addressing edge case with rebase.instructionFormat and non-branch refs, now in v3 with confirmed approach for local tag handling and parallel code path identification, expanded to cover non-interactive rebase cases.

Core problem: When rebase.instructionFormat includes %d (show all ref decorations), the rebase machinery incorrectly generates update-ref instructions for non-branch refs like HEAD in both interactive and non-interactive modes. The solution filters to DECORATION_REF_LOCAL types, with v3 merging two if statements into one for cleaner logic while maintaining correct branch ref handling through load_branch_decorations() filtering in sequencer.c's todo_list_add_update_ref_commands().

Current status: Patch v3 has Phillip Wood's Reviewed-by and Junio C Hamano's confirmation that the approach is correct. Additional confirmation from Abhinav Gupta shows the issue affects non-interactive rebase identically, reinforcing that the branch ref limitation is intentional behavior. Regression tests in t/t3404-rebase-interactive.sh verify the fix works as intended. A related issue in make_script_with_merges() needing the same decoration filtering remains open but doesn't block this fix.

Key technical details:
- Fix in sequencer.c correctly handles branch refs via load_branch_decorations()
- V3 simplifies logic by merging two if statements while preserving behavior
- Now confirmed to affect both interactive and non-interactive rebase modes
- Junio confirms existing behavior was already limited to branch refs
- Parallel issue in make_script_with_merges() needs same DECORATION_REF_LOCAL filtering
- Targets --update-refs feature from Git 2.38
- Phillip Wood provided follow-up snippet for make_script_with_merges() fix

Open questions: Whether author will submit make_script_with_merges() fix as separate patch or combine with current changes. The core fix is complete and ready for inclusion.

Participant positions:
- Phillip Wood confirmed solution correctness and identified related issue
- Provided Reviewed-by for current patch
- Suggested follow-up for parallel code path
- Junio C Hamano confirmed approach is correct and intentional
- Abhinav Gupta validated issue occurs in non-interactive mode
- Original author needs to decide on handling the additional fix

Related work: Builds on --update-refs feature from Git 2.38. Phillip Wood is sequencer expert. Discussion now covers two related code paths needing consistent ref filtering in rebase machinery, with maintainer approval for current approach confirmed across both interactive and non-interactive use cases.