Thread identity: rebase --update-refs bugfix addressing edge case with rebase.instructionFormat and non-branch refs, now in v3 with confirmed approach for local tag handling and parallel code path identification.

Core problem: When rebase.instructionFormat includes %d (show all ref decorations), the rebase machinery could incorrectly generate update-ref instructions for non-branch refs like HEAD. The solution filters to DECORATION_REF_LOCAL types, with v3 merging two if statements into one for cleaner logic while maintaining correct branch ref handling through load_branch_decorations() filtering in sequencer.c's todo_list_add_update_ref_commands().

Current status: Patch v3 has Phillip Wood's Reviewed-by and is ready for inclusion. The discussion identified a related issue in make_script_with_merges() needing the same decoration filtering, which will be addressed separately. Regression tests in t/t3404-rebase-interactive.sh verify the fix works as intended.

Key technical details:
- Fix in sequencer.c correctly handles branch refs via load_branch_decorations()
- V3 simplifies logic by merging two if statements while preserving behavior
- Parallel issue in make_script_with_merges() needs same DECORATION_REF_LOCAL filtering
- Targets --update-refs feature from Git 2.38
- Phillip Wood provided follow-up snippet for make_script_with_merges() fix

Open questions: Whether author will submit make_script_with_merges() fix as separate patch or combine with current changes. The core fix is complete and ready.

Participant positions:
- Phillip Wood confirmed solution correctness and identified related issue
- Provided Reviewed-by for current patch
- Suggested follow-up for parallel code path
- Original author needs to decide on handling the additional fix

Related work: Builds on --update-refs feature from Git 2.38. Phillip Wood is sequencer expert. Discussion now covers two related code paths needing consistent ref filtering in rebase machinery.