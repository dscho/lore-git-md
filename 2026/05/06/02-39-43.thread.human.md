This thread addresses a bug in `git rebase --update-refs` where using `rebase.instructionFormat=%d` could incorrectly generate update instructions for non-branch refs like HEAD. The original patch from the author (v1) correctly filters decorations to only process local branches, with comprehensive test coverage.

Phillip Wood has now reviewed the patch with substantive technical suggestions to improve the test case. He recommends simplifying the test by removing redundant branch setup, checking the complete `update-ref` output directly rather than with `grep` patterns, and explicitly verifying tag handling. His feedback tightens the test while maintaining all necessary coverage - a clear improvement to an already correct patch.

The fix itself remains uncontested; this is purely about refining the verification. Given Phillip's expertise in rebase and testing (he was instrumental in the recent `git replay --revert` series), his suggestions carry significant weight. The author will likely send a v2 incorporating these test improvements, after which the patch should be ready for merging.