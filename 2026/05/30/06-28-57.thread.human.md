Here's a human-readable summary of the thread so far:

The thread explores improving `git log --follow`'s behavior with merge commits, particularly for subtree merges where files might be added in one parent but renamed in another. The core challenge is determining which merge parent to follow when tracking file history across renames.

Miklos Vajna originally proposed handling cases where a path is unchanged (TREESAME) in all parents except one, following only the parent with changes. Jeff King raised concerns this might be too restrictive, potentially missing valid rename scenarios. In the latest message, Miklos suggests refining the rule: when a path is either unchanged *or just added* in all parents except one, follow the parent where it was meaningfully modified. This aims to better handle subtree merge cases while addressing Jeff's concern about over-filtering.

The discussion remains technical but productive, with Miklos seeking Jeff's feedback on whether this adjusted approach strikes the right balance. The proposed solution is intentionally narrow - focusing first on the subtree merge case rather than attempting a full rework of `--follow`'s merge handling. Both contributors are carefully considering edge cases and trying to avoid regressing existing behavior.