Bugfix follow-up for `git describe` pattern matching with `--all`.

Junio C Hamano notes an unexpected CI failure in Jacob Keller's patch that fixed `git describe`'s handling of `--exclude` and `--match` with `--contains --all`. The test expects output "branch_A" but gets "remotes/origin/remote_branch_A" instead, suggesting the exclude patterns aren't being applied correctly to remote branches.

This appears to be an edge case in the pattern matching behavior that wasn't caught by the original test coverage. The failure is consistent across CI jobs but wasn't anticipated in the initial implementation. Junio's comment suggests this may require revisiting how the exclude patterns are applied to remote refs when using `--all`.

The thread context shows this is a follow-up to a previously well-received patch that aligned `git describe`'s implementation with its documented behavior for pattern matching. The current issue seems to be a refinement case rather than a fundamental problem with the approach.