This is a feature request regarding the `git history` command's handling of commit signatures. The email notes that while `git rebase` has a `-S` option to preserve or re-sign commits, the newer `git history` command currently drops signatures during its operation.

The author, Alix Brunet, asks whether signature preservation will be implemented for `git history`. The question appears to be motivated by consistency with `git rebase`'s behavior, though the email doesn't provide specific use cases or technical details about the signature-dropping behavior.

This touches on the ongoing work around the `git history` command, which has been a focus of Patrick Steinhardt's recent work. The question seems straightforward but may require consideration of how signature handling interacts with `git history`'s underlying mechanisms. The thread could benefit from input from maintainers familiar with both the signature verification infrastructure and the `git history` implementation.