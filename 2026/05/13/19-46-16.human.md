This is the author's reply to Taylor Blau's question about the `tree:0` filter patch, specifically why the `/tagged-trees` sentinel path applies to directly-listed trees. Stolee's answer is significant and honest: the confusion is because the previous version of the patch was actually buggy. The test setup was accidentally passing because the tagged trees in the test were also the root trees of commits, so the bug was invisible. Fixing it will require both a setup patch that corrects the test fixture and a substantially reworked version of this patch.

This is a substantive technical correction from the co-author to the co-author. It means patch 10/12 is not just awaiting a minor clarification -- it has a real correctness bug that needs a structural fix, and the v4 will need careful review of the tagged-trees logic. The "surface-level" characterization of Blau's question in the parent summary was accurate (Blau asked a design question without spotting the bug), but the outcome is more significant than that summary implied: a wrong implementation was identified and acknowledged.

For a busy reader: if you were only spot-checking this series, patch 10/12 is now the one that warrants the most attention in v4. The `tree:0` logic had hidden test-coverage gaps, and the fix will change how tagged trees interact with tree-level filtering.