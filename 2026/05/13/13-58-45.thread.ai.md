Thread identity: "config: hint about `git config set` syntax on '=' in key" -- single standalone patch, not a series. Author: (not specified in summary, but the email summary should be assumed to come from the AI digest agent; for AI mode, future sessions must be able to reason about who authored it based on the input).

Core problem or goal: The user error of typing `git config foo.key=value` (as if using INI-file syntax or `--flag=value` style) instead of the correct `git config set foo.key value` is common. When a `=` character is present in the key string, the existing error message (`error: invalid key: ...`) provides no guidance on the correct syntax. This patch fixes that by adding a specific hint when the offending character is `=`.

Current status: Single patch, no versions indicated (v1). No merge status. No replies mentioned in the new email AI summary.

Key technical details: File touched: `config.c`. Symbol: `git_config_parse_key()`. Logic: inside the existing error path for an invalid character in a config key, a new condition checks if `c == '='`. It uses `%.*s` with pointer arithmetic to split the key at the `=` and then constructs a suggestion with `%s` for the value portion, producing output like: `error: invalid key: foo.key=value\n  hint: (did you mean "git config set foo.key value"?)`. No new tests added. The heuristic intentionally fires only for `=` to avoid false positives.

Participant positions: New email AI summary indicates no reviews have been received yet. No Reviewed-by, Acked-by, or any participant position mentioned. The summary notes the lack of tests as a minor shortcoming, but no one has raised this objection yet.

Open questions and loose ends: Whether tests are required for this change is an open question -- the project typically expects new error messages to be covered, but this has not been raised in the thread yet.

Related work: The commit message analogizes with the existing "did you mean" style hints elsewhere in the codebase. No external dependencies or other threads mentioned.