Thread identity: "config: hint about `git config set` syntax on '=' in key" -- two-part series (v6) by Harald Nordgren. UX improvement addressing common `git config` command-line syntax errors through clearer error messages and contextual hints, with completed API restructuring and value-missing case handling.

Core problem/goal: Resolve user confusion between invalid `git config foo.key=value` and correct `git config set foo.key value` syntax via improved error messaging, implemented through layered API design and comprehensive case coverage.

Current status: v6 merged with:
1. Core API restructuring:
   - New internal `do_parse_config_key()` with quiet flag
   - Preserved `git_config_parse_key()` as backward-compatible wrapper
   - Added `git_config_key_is_valid()` validation-only API
2. User-facing improvements:
   - Three-case error structure with contextual hints:
     * Valid key with `=` syntax: error + hint
     * Valid key without value: error only
     * Invalid key: error without hint
   - Comprehensive test coverage for all cases

Key technical details:
- File changes: `config.c` (API restructuring), `builtin/config.c` (error handling), `t/t1300-config.sh` (expanded tests)
- Implementation highlights:
  - Strict separation of parsing (internal) vs validation (public API)
  - Optional key storage via NULL parameter checks
  - Quiet flag restricted to internal helper
  - Behavior preserved for all edge cases
- No behavior changes for valid inputs
- Final API design refinement from Junio:
  - Clarified that quiet mode and key storage should not be independent features
  - Suggested ideal split between low-level error-only helper and public wrapper
  - Current implementation accepted as practical compromise

Participant positions:
- Junio C Hamano: Provided final API design guidance, approved merge
- Harald Nordgren: Implemented all requested changes across six iterations
- Kristoffer Haugsbakk: Earlier workflow discussion (resolved)

Implementation status: Merged with all requested changes implemented, including final API refinements. Maintains consistent UX patterns with other Git commands.

Related work: Builds on existing config parsing infrastructure. Follows same "did you mean" hint pattern used in fetch/prune messaging.