This appears to be a case of thread hijacking - the original discussion was about improving config syntax error messages, but the new email introduces a completely unrelated feature proposal about fetch pruning behavior. 

The config syntax discussion remains open with Junio's substantive review suggesting we consider accepting `foo.key=value` syntax while preserving backward compatibility with existing config files that legitimately contain equals signs in section headers. This needs resolution on whether to proceed with just a hint system or implement broader syntax acceptance, plus test coverage for whichever approach is chosen.

Meanwhile, the new fetch pruning proposal is at too early a stage for serious consideration - it lacks implementation details, test cases, or analysis of how it would interact with existing pruning options. If the author wants to pursue this, they should start a new thread with a properly formatted patch series including motivation, implementation, and tests.

For now, the thread should remain focused on the config syntax question. The fetch pruning idea, while potentially useful, belongs in its own discussion.