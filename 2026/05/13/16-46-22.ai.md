The author replies to René Scharfe with a light follow-up on the ls-files padding patch, confirming that a more elaborate approach using a `decimal_length_u64()` helper was not meaningfully faster than René's original `memmove()` version. The conversation then drifts into an idle technical tangent about a hypothetical custom `vsnprintf()` for Git, with Peff acknowledging René's observation that C99 `%zu` is already in tree as of commit 82c36fa0a9, suggesting MinGW-related portability concerns may be easing.

This is a clean, conversational close to the review cycle. No technical issues remain, no changes are requested to any of the four patches, and no v2 is expected. The series is ready for maintainer pickup.