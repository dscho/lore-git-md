This is a **substantive review reply** from Elijah Newren, responding to Derrick Stolee's earlier architectural questions about where batch-prefetching logic should live. The thread is in a post-merge-to-`next` phase, with a re-roll already expected.

Elijah pushes back politely but firmly on Stolee's suggestion to centralize the prefetch logic into the diff library (`diff.c`). He walks through two concrete, cherry-specific filtering constraints that would not generalize: (1) `git cherry` skips prefetching entirely for commits whose file list does not match the commit of interest, and (2) binary files are deliberately skipped because patch-ID computes their content from the OID string rather than reading blob contents. The accumulation across *many* commits (not just one diff) is also fundamentally different from what `diff_queued_diff_prefetch()` does inside `diffcore_std()`. He makes a point of having checked the 11 existing `promisor_remote_get_direct()` callsites and found none that share the same pattern.

He is open to factoring out a helper in the future -- but only when a second caller such as `git archive` actually materializes. Until then, he prefers keeping both new prefetch implementations local to their builtins.

The reply also contains a small but revealing piece of process information: Elijah acknowledges that while Stolee's review was of a single patch, it reflects his feelings about the entire series after reading both patches 2 and 3. This suggests Stolee's review was broader than the patch header implies.

The email is well-structured, technically precise, and reads like an experienced author who has thought carefully about the API boundaries of his code. The deferral of centralization is reasoned, not dismissive -- Elijah even invites counterarguments with "I'm happy to be told I've missed the boat." This positions the architectural question as an open, friendly technical disagreement rather than a blocking issue. For a busy reader: Stolee's architectural suggestion is not happening in this iteration, the series remains on track for its re-roll with the two fixups (pathspec test and redundant-prefetch bug), and the core design decision has been explained and defended.