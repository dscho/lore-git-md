Existing thread summary:

- Thread by Jeff King on sideband.c handling of ANSI SGR sequences. The sideband code in `handle_ansi_sequence()` parses SGR escape codes (e.g., `\e[31m`) in progress messages (e.g., from `git push`), but only accepts semicolons (`;`) as separators between numeric parameters. The ECMA-48 specification allows colons (`:`) as sub-parameter separators within a single numeric parameter, which are required for 256-color (`\e[38:5:NNm`) and true-color (`\e[38:2:R:G:Bm`) escape codes. Many terminals emit these colon-containing sequences, and Git's current parser leaves them as raw garbage in the output.

- Jeff King's patch (originally authored by Mantas Mikulėnas) makes a one-character change in `sideband.c` line 217: adding `src[i] != ':'` to the character-class check in the SGR parsing loop. The commit message cites the upstream xterm source code to support the standard encoding. The change is additive (accepting more input) and does not alter handling of previously accepted sequences. No tests are included.

- **Review status**: Johannes Schindelin reviewed the patch and called it "well-motivated and well-executed," explicitly confirming this as an **Acked-by**. Junio C Hamano has noted the patch "flew below [his] radar" but does not object to the change, though he mildly questions the project's long-standing whitelist-based design philosophy (accepting only known-good patterns) rather than blacklisting known-bad ones. No substantive objections exist.

- **Current status**: The patch is effectively approved for merging. Junio has not yet queued it, but the absence of objections and Schindelin's Acked-by signal readiness. The only remaining procedural step is Junio's integration into his queue (likely `pu` or `next` in the next "What's cooking" report).

- **Open questions**: Whether Junio will request test coverage for the new codepath (unlikely given the additive nature of the change) or merge it as-is. The whitelist vs. blacklist design discussion is noted but not actionable for this patch.

- **Key technical details**:
  - **File touched**: `sideband.c`, function `handle_ansi_sequence()` (line 217).
  - **Behavior change**: Parser now accepts `:` alongside `;` as valid separators in SGR sequences, enabling support for 256-color and true-color escape codes.
  - **Symbols**: No new or renamed symbols; the change is confined to a single character in an existing conditional.
  - **Test coverage**: None included; the patch relies on existing sideband tests.

- **Participant positions**:
  - **Jeff King (Peff)**: Authored the patch (originally by Mantas Mikulėnas). No explicit position stated beyond the patch itself.
  - **Johannes Schindelin**: Acked-by, calling the patch "well-motivated and well-executed." Surface-level review (no technical engagement with edge cases or test coverage).
  - **Junio C Hamano**: Neutral-to-positive; no objections but questions the whitelist design philosophy. No request for changes.