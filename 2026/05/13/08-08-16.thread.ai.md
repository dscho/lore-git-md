**THREAD IDENTITY**: `[PATCH v4 0/2]` cover letter. Subject: `worktree: add worktree condition in includeIf for git-config`. Author: Chen Linxuan (previously Kohei Enami on v3 -- relationship unknown). Thread type: patch series, fourth iteration, two patches.

**CORE PROBLEM OR GOAL**: Extend Git's conditional config inclusion (`includeIf` in `.gitconfig`) with `worktree:<pattern>` and `worktree/i:<pattern>` conditions that match against the working tree's checkout path, complementing existing `gitdir:<pattern>` and `onbranch:<pattern>`. In multi-worktree setups, linked worktrees share the same `.git` directory via `.git/worktrees/<id>/`, making `gitdir` patterns unable to distinguish worktrees by checkout path. A single global config like `[includeIf "worktree:/home/user/work/"] path = work.inc` now suffices.

**CURRENT STATUS**: Fourth iteration (v4), completed. Both patches have arrived. The series is finished awaiting maintainer pickup.

**KEY TECHNICAL DETAILS**:

- **Subsystems touched**: `config.c`, `t/t1305-config-include-worktree.sh`.
- **Patch 1/2 (mechanical refactoring)**: Renames `include_by_gitdir()` to `include_by_path()`. Changes signature from `struct config_options *opts` (extracting `opts->git_dir` internally) to `const char *path` (caller provides the path). Both existing callers (for `gitdir:` and `gitdir/i:` conditions) updated to pass `opts->git_dir` explicitly. 19 lines changed, all in `config.c`. No behavior change.
- **Patch 2/2 (new feature)**: Adds `worktree:` and `worktree/i:` conditions. Implementation: ~6 lines in `config.c` -- passes worktree path from `repo_get_work_tree()` into `include_by_path()`. Returns NULL for bare repos and during early config reading (no repository object available). Documentation added: explains relationship to `extensions.worktreeConfig`, gives concrete use-case example. Tests: 113 new lines covering bare repos, multiple worktrees (including nested path with trailing-`/` prefix matching), symlinked worktree paths, case-insensitive variant, early config reads, non-repository scenarios.

**OPEN QUESTIONS AND LOOSE ENDS**: None. All prior review feedback (from Patrick Steinhardt and Junio C Hamano on v3) has been addressed. The series appears complete and ready for integration.

**PARTICIPANT POSITIONS**: No review activity on the v4 patches themselves has been seen yet. The v4 cover letter stated that prior v3 feedback from Patrick Steinhardt and Junio C Hamano was incorporated. Participant positions from earlier iterations are not recoverable from the current data -- the AI thread summary began with the v4 cover letter and has no access to v3 discussion content. Author changed from Kohei Enami (v3) to Chen Linxuan (v4); relationship unknown.

**PARTICIPANT NAME NOTE**: Author name discrepancy between v3 (Kohei Enami) and v4 (Chen Linxuan) remains unexplained. Neither name appears in the project context document.

**RELATED WORK**: N/A. Standalone series. No external dependencies or related in-flight topics noted.