**Thread identity** -- "[PATCH v3] config: add includeIf worktree support". Author: Chen Linxuan. Thread type: patch series (single patch). Current version: v3.

**Core problem or goal** -- Add `[includeIf "worktree:/path"]` and `[includeIf "worktree/i:</path/glob>"]` conditions to Git's config system, analogous to the existing `gitdir` and `gitdir/i` conditions. Users would be able to conditionally include configuration based on which worktree they are in.

**Current status** -- v3 is under review. Patrick Steinhardt's review raised three points; Chen Linxuan has acknowledged all three. Two are accepted for v4: (1) deduplicate documentation by reusing the gitdir section, (2) add a test for the "outside a repository" case.

**Key technical details** -- Files/topics touched: config parsing subsystem, documentation. No new symbols, config keys, CLI options, or structural changes identified so far.

**Open questions and loose ends** -- One unresolved item: Patrick requested a test for the worktree condition with early-config parsing, analogous to the existing `gitdir` early-config test. Chen acknowledges the feature does not work with early-config parsing (the commit message already states this) and says they are working on a fix but are unsure whether this is a must-fix. This creates an asymmetry between `gitdir` (works in early config) and `worktree` (does not). Resolution is pending.

**Participant positions** -- Patrick Steinhardt: substantive technical review (requested documentation deduplication, additional tests, early-config handling). Chen Linxuan: accepted two of three review points, expressed uncertainty about the early-config requirement.

**Related work** -- None identified. The existing `gitdir`/`gitdir/i` conditions in Git's config system are the direct analogue.