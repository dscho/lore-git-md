This is a conversational follow-up from Jeff King (Peff) in a thread about René
Scharfe's `strbuf_add_uint()` series — a thread that is otherwise fully
closed and ready for maintainer pickup.

Peff's email is an idle technical sidebar, not a review of the patches.
After the main design discussion concluded (all implementation variants
benchmarked equivalently, Peff endorsed René's choice), Peff tosses off
a closing thought: if you wanted to further micro-optimize strbufs, using
`__builtin_add_overflow()` for overflow detection in `strbuf_grow()`
might be faster than the current `unsigned_add_overflows()` + manual add.

He introduces this with "btw, one final thing to look at if you are
interested" — it is clearly speculative and offhand, not a request or a
recommendation. No patch is proposed, no performance data is shown, and
Peff's tone suggests he is thinking aloud rather than advocating for a
change.

This email has no bearing on the series. The thread remains closed; the
patches are ready for Junio to pick up. The note about `__builtin_add_overflow`
is trivia that might interest someone working on the strbuf layer, but it
is not an actionable item.