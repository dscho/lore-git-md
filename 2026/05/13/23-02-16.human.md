This is a review comment from Jeff King (Peff) on Pablo Sabater's v3 patch that improves `git log --graph` rendering for commits with excluded parents. The patch has been sitting in Junio's queue since early April awaiting final review.

Peff flags a Coverity static analysis finding: the `if (seen_this) continue;` inside the `is_placeholder` branch is dead code. The logic is nested inside an `else` block that is only reached when `seen_this` is already false -- a prior `else if (seen_this)` branch would have caught the true case. Peff notes he hasn't looked at the patch closely, so he can't tell whether the fix is simply to delete the redundant check or whether it points to a deeper logic error.

This is a surface-level review -- a single-line observation about unreachable code from a static analysis tool report, not a deep examination of the patch's correctness. That said, Peff's name on a review is always worth attention; he is one of the project's most thorough contributors and his "I haven't looked closely" disclaimer is worth taking at face value. The observation itself is modest but real: the dead code should be cleaned up before merging. Whether that cleanup is a one-line deletion or reveals a misunderstanding of the control flow depends on whether the author intended `seen_this` to be usable in that branch at all, or simply added the check defensively without realizing it was unreachable.

The core question for Pablo: is the `seen_this` guard a vestige from an earlier revision that got left behind when the `else if` structure was added, or does it hint that the placeholder block should be reachable under different conditions than what the current control flow allows?