**Patch 1/4 follow-up: strbuf_add_uint() implementation detail**

This is a quick self-correction from Jeff King on his alternative `strbuf_add_uint()` sketch from the previous email. He points out a one-character bug in the diff he attached: the line `p = sb->buf + digits` should be `p = sb->buf + sb->len + digits`. The bug only matters when the strbuf is non-empty before the call, which the `cat-file` benchmark (always writing into an empty buffer) would not have exposed, but `ls-files` with its padding logic would have hit.

The tone is collegial and self-deprecating -- Peff notes, with the smiley, that the buggy line was "the part I wrote by hand" compared to the ChatGPT-generated `decimal_length_u64()` helper. The remark carries no broader significance and does not open a new discussion point. The thread remains closed, the design choice is settled, and no follow-up from René is expected.