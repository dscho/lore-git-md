**Thread identity** -- bug report from Alexander Sandström. Subject: linked worktrees produce wrong commit for lowercase `head` on case-insensitive filesystems. Not a patch series.

**Core problem** -- On case-insensitive filesystems (macOS APFS/HFS+), `git rev-parse head` in a linked worktree resolves to the *main* worktree's HEAD instead of the linked worktree's HEAD. `git rev-parse HEAD` (uppercase) correctly goes through the per-worktree ref at `.git/worktrees/<name>/HEAD`. Lowercase `head` falls through to general ref resolution, which does a file lookup; on case-insensitive fs, this accidentally matches `.git/HEAD` (the main worktree's HEAD). Without linked worktrees both codepaths produce the same result, so the bug is latent. With linked worktrees the two HEADs diverge and the behavior silently changes. Practical impact: `git reset --soft head~1` in a linked worktree operates on the wrong commit.

**Current status** -- Single initial report, no follow-up discussion. No patch submitted.

**Key technical details** -- File: `refs/files-backend.c` or similar ref resolution path. No new symbols. The fix would be a case-insensitive comparison in the ref-resolution fallthrough path before doing filesystem file lookup. Two fix options proposed by reporter: (a) reject lowercase `head` with an error (matching Linux behavior where it already fails with "unknown revision"); or (b) normalize to `HEAD` and route through the per-worktree codepath.

**Open questions and loose ends** -- No discussion yet. The reporter explicitly states they are not a C programmer, so no patch is expected from them. Broader question: should Git treat lowercase `HEAD` as valid at all? This could generate discussion. Bug only manifests on case-insensitive filesystems with linked worktrees -- two conditions that together are relatively uncommon, which may affect prioritization.

**Participant positions** -- Only Alexander Sandström (reporter, non-C-programmer, no patch). No reviews.

**Related work** -- None.