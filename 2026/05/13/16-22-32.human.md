Classification: refactoring, minor performance optimization.

This is Peff following up on René's earlier performance digression about how to format an unsigned integer into a strbuf. Having heard René's benchmark results, Peff is now nerd-sniping himself with ChatGPT -- he posts a diff showing a completely different implementation that precomputes the digit count using a log10 approximation via `__builtin_clzll` and a power-of-10 lookup table, then writes the digits directly into the strbuf's buffer without a temporary.

The approach eliminates the on-stack buffer and the memcpy entirely: the function grows the strbuf to fit the digits, points `p` at the end of that space, writes digits backwards in-place, and calls `strbuf_setlen()` to advance the length. The digit-count computation uses a branch-free log10 trick -- `63 - __builtin_clzll(n)` to get the bit index, multiply by a magic constant (1233 >> 12) for a close approximation of log10, then correct with a lookup table check if the estimate was low.

Peff reports he got identical benchmark timings for `cat-file`'s `%(objectsize:disk)` between René's submitted version and this alternative, so the patch is not proposing a change -- it is sharing a curiosity. He is candid that he did not figure out the off-by-one corner cases himself but got the implementation from ChatGPT, which he notes "looks plausibly correct."

This is a light, conversational post between two veteran contributors who enjoy discussing implementation tradeoffs. There is no actionable feedback, no request for René to re-roll, and no substantive disagreement with the accepted series. The email is best read as a technical footnote: the precomputed-digit-count approach is functionally equivalent in performance, and the series as submitted is fine as-is. No further action needed.