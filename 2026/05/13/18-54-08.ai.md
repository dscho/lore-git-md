Review / follow-up. Jeff King (Peff) responds to Junio C Hamano's suggestion of a broader cleanup of ineffective `strbuf_grow()` calls.

Peff agrees with Junio's idea and confirms it could be a `#leftoverbits` item (a project-internal label for cleanup work that is acknowledged but not prioritized). He adds a practical caution: anyone picking this up needs to read the full function context, not just the immediate line, because there might be a subtle dependency on the grown buffer that is not obvious from the two-line pattern.

He then sketches a cleanup that goes beyond just removing `strbuf_grow()`: in `builtin/fast-import.c`'s `mktree()`, the pre-computation loop that calculates `maxlen` becomes entirely unused once the `strbuf_grow()` call is removed. That means the whole `maxlen` variable and its accumulation loop can also be deleted together with the grow call. He presents this as a diff, showing a net reduction of about a dozen lines.

The review surface is effectively zero here -- Peff is not requesting any change to his own patch (it has already been accepted by Junio), and he is not submitting a new patch. He is engaging in precisely the kind of "yes, and" discussion that Junio invited, offering a concrete example of how the broader cleanup could yield more satisfying results than a mechanical removal. The tone is light ("So probably some satisfying cleanup opportunities") and aimed at encouraging someone interested to pick the work up.

This is a thread conclusion, not a substantive technical contribution to the original patch. No further action needed.