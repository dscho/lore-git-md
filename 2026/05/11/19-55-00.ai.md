This is a documentation follow-up patch v2 from D. Ben Knoble, part of the per-worktree ignore patterns thread. The patch corrects `gitignore(5)` to say `$GIT_COMMON_DIR/info/exclude` instead of `$GIT_DIR/info/exclude`, matching Git's actual behavior where exclude files live in the common directory shared across worktrees, not in each worktree's individual `$GIT_DIR`.

Phillip Wood provided a thorough review by running `git grep` and identifying five additional instances in `gitignore.adoc` that still reference the old path, plus mentions in other documentation files. His feedback is constructive and detailed -- a substantive review that catches scope creep and prevents an incomplete documentation fix from landing. D. Ben Knoble acknowledges the catch and says he will adjust those remaining ignore-related references in a v3, leaving other documentation files for a separate pass.

The classification is documentation, specifically a fix to align the man page with existing behavior. No code changes are involved. The quality is solid: the original patch was correct in intent, and the reviewer correctly pushed for a more comprehensive sweep rather than settling for a partial fix. The thread appears to be on its final iteration -- v3 should be the last version before merging, provided it catches everything. The author has already agreed to handle the identified gaps, and the reviewer has no objection to the approach.