This is a documentation fix for Kristoffer Haugsbakk's `git format-rev` series, addressing a formatting issue in the man page for the new command. The patch corrects a malformed nested definition list in the `--stdin-mode` option documentation.

The `git format-rev` command, now in its fourth iteration, provides on-demand pretty formatting of revisions from stdin, with two input modes: one for processing revision expressions line-by-line (`revs` mode) and another for finding and formatting commit object names in freeform text (`text` mode). The command includes robust stream handling features like NUL-termination support and safe interactive operation.

This particular patch fixes an AsciiDoc syntax issue in the documentation that was likely caught during final review. The correction ensures the man page renders correctly, maintaining the high standard of documentation quality expected for new Git commands. The fix is straightforward and uncontroversial, addressing a purely presentational concern rather than changing any functionality.

The series has already received substantive reviews from Phillip Wood and others, with all major technical feedback incorporated. This documentation tweak represents the final polish before the feature is ready for merging. The command remains marked as EXPERIMENTAL, allowing for further refinement based on real-world usage.