Performance optimization follow-up discussing API design. Patrick Steinhardt suggests converting the boolean flags in `paint_down_to_common()` to an explicit flags parameter using an enum, noting this would improve readability at call sites despite being more verbose. The current implementation (from Kristofer Karlsson's v2 patch) passes multiple boolean parameters including `find_all` for controlling merge-base calculation behavior.

This is a stylistic suggestion rather than a technical concern - the optimization itself appears sound and well-tested from the v2 summary. The flags proposal would affect not just `paint_down_to_common()` but also functions that pass these parameters through. Patrick acknowledges the tradeoff between verbosity and clarity, leaving it to the author to decide whether to implement this in a potential v3.

The thread context shows this is a performance-critical path (100x speedup for large repos) that has already addressed Junio's v1 review feedback. This new comment focuses purely on API ergonomics rather than the core algorithm.