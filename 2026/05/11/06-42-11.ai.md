This is a technical discussion about fixing the maintenance daemonization locking issue, with Patrick Steinhardt proposing an alternative implementation to Taylor Blau's solution. The thread has progressed from identifying the repository corruption risk in `git maintenance run --detach` to evaluating specific fixes.

Patrick agrees with Taylor's test demonstrating proper lock handling in daemonized maintenance, though notes potential Windows portability concerns. He then proposes a more targeted solution than Taylor's general tempfile ownership transfer - specifically adding `lock_file_reassign_owner()` functionality to explicitly transfer lockfile ownership during daemonization.

Key aspects of Patrick's proposal:
1. Introduces a new `daemonize_without_exit()` function that returns the child PID rather than exiting
2. Adds explicit lockfile ownership transfer in maintenance code using new `lock_file_reassign_owner()`
3. Maintains the existing behavior for non-lockfile tempfiles
4. Still addresses the core issue of preventing parent from cleaning up child's lock

The discussion remains at a decision point between:
1. Minimal revert of geometric repack default (Stolee's preference)
2. Taylor's comprehensive tempfile ownership solution
3. Patrick's more targeted lockfile-specific approach

Patrick's solution offers a middle ground - fixing the immediate issue without the broader implications of Taylor's approach. The test coverage Taylor provided remains relevant for validating this approach. The thread now awaits evaluation of whether this narrower solution satisfies Stolee's concerns while being safer for inclusion in a point release.