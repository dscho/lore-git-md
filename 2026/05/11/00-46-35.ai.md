This is a documentation patch that finalizes the specification of `git log --follow` behavior, following Junio Hamano's authoritative review. The email exchange confirms that the existing documentation already correctly captures the key constraints: `--follow` only works with single filenames on linear history, with all other cases being undefined.

Junio affirms that the current wording in `git-log.txt` ("works only for a single file") and the `log.follow` config description ("cannot be used to follow multiple files and does not work well on non-linear history") sufficiently communicate these limitations without needing to document implementation details of unsupported cases. He explicitly equates directory pathspecs and non-linear history with the already-documented multiple-paths case - all are equally undefined.

The interaction demonstrates Git's documentation philosophy where undefined behavior remains unspecified to preserve implementation flexibility. The thread has now reached consensus with no outstanding issues, marking the completion of this documentation improvement effort. The patch series serves as a model for how technical documentation reviews should establish clear boundaries between supported and unsupported functionality.