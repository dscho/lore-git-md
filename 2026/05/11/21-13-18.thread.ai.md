**Thread identity** -- thread root plus two replies; originating author Thorsten Glaser, replying participants Brian M. Carlson and Thorsten Glaser again. Thread type: feature request / design discussion, now closed without action.

**Core problem or goal** -- Git always invokes alias commands via `/bin/sh`. The proposal was a config option `core.shell` to override the hard-coded shell path, motivated by an alias that the user believed needed mksh-specific features.

**Current status** -- resolved without any project change. Thorsten's second email confirms he misidentified the problem: the alias in question used a recent POSIX construct that his customer's `/bin/sh` did not yet implement, not an mksh-specific feature. By rewording the alias, the practical need for `core.shell` disappeared. No patch was ever written; no version of the proposal exists.

**Key technical details** -- Brian M. Carlson's rebuttal (first reply) established the full technical objection:
- `/bin/sh` is intentionally hard-coded to guarantee consistent behavior across system-level aliases in `/etc/gitconfig`.
- The build-time `SHELL_PATH` variable already exists for systems whose `/bin/sh` is not POSIX-compatible (such as Solaris) and would cover the mksh use case.
- A per-user `core.shell` would break system aliases that assume `/bin/sh` semantics.
- The "with-shell" alias execution mode uses a POSIX quoting format (`%s "$@"`) that would need reworking for non-POSIX shells like zsh or PowerShell.
- Brian referenced a prior PowerShell discussion covering overlapping quoting and security concerns.

**Open questions and loose ends** -- none. Thorsten explicitly withdrew the ask. The only forward-looking remark -- that one could imagine pointing to a portable mksh on macOS -- was offered as a casual thought, not a proposal. No narrower design (e.g. `core.shell` limited to POSIX-compatible shells or user-level aliases only) was proposed or discussed.

**Participant positions** -- Brian M. Carlson: opposed to the proposal as stated, with detailed technical rationale. Thorsten Glaser: original proponent who, after understanding the distinction between "with-shell" and "without-shell" command modes, acknowledged the issue was not what he thought it was and confirmed his problem is now resolved. No participants pushed back against Brian's objections; no other contributors weighed in.

**Related work** -- Brian referenced a prior discussion about PowerShell support covering overlapping quoting and security concerns. Thorsten's withdrawal makes that reference no longer actionable in this thread.