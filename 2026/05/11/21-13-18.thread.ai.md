This is a feature request (proposal, not a patch) from Thorsten Glaser asking Git to allow users to configure which shell is used to run alias commands, rather than hard-coding `/bin/sh`. The concrete ask is for a config key like `core.shell` that would override the hard-coded path.

**Thread identity** -- thread root, no subject line given; originating author: Thorsten Glaser. Thread type: feature request / design discussion.

**Core problem or goal** -- Git always invokes alias commands via `/bin/sh`. Aliases that rely on shell features not available in POSIX `sh` -- or that are tested against a specific shell like `mksh` -- can break. There is currently no configuration option to point Git at a different shell. The author would like a config key, tentatively `core.shell`, that could be set to e.g. `/bin/mksh`.

**Current status** -- initial proposal only. No patch, no implementation detail, no discussion yet.

**Key technical details** -- no files or subsystems identified by the author. The email does not analyze where in Git's source the hard-coded `/bin/sh` path is used. The implementation would involve `run_command()` plumbing and how Git spawns shell commands across platforms. No reproduction steps, data structures, or CLI options proposed.

**Open questions and loose ends** -- everything is open. The author does not address portability concerns (not every platform has or wants an alternative to `/bin/sh`), the scope of the change, or any trade-offs. The right next step is for interested parties to chime in with use cases and constraints before anyone writes code.

**Participant positions** -- none yet. This is the thread root.

**Related work** -- none identified. No prior versions, external dependencies, or in-flight topics mentioned.