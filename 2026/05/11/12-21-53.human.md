This is a refactoring patch targeting the sequencer subsystem. The change removes an unused `commit` field from `struct todo_add_branch_context` in `sequencer.c`. The field was being written to but never actually read - the commit is passed directly to `add_decorations_to_list()` when needed.

The patch is straightforward and appears correct - it eliminates dead code without changing any behavior. The commit message clearly explains the removal and the diff shows the two simple deletions required. This looks like a clean, uncontroversial cleanup that should be easy to merge.