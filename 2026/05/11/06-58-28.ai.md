Feature: Finalized branch pruning functionality via standalone `git branch` commands in v5 series.

This final patch in Harald Nordgren's series adds a `--all-remotes` flag to complement the `--forked` and `--prune-merged` branch operations. The flag allows acting on all configured remotes rather than requiring explicit remote arguments, while maintaining all the safety mechanisms established in earlier patches.

Key technical details:
- New `--all-remotes` flag works with both `--forked` and `--prune-merged`
- Rejects standalone use with clear error message
- Combines with explicit remote arguments (validating them against configured remotes)
- Comprehensive test coverage for:
  - Basic functionality with `--forked`
  - Error cases (standalone use, invalid remote names)
  - Integration with `--prune-merged` across multiple remotes
- Implementation uses `for_each_remote()` to collect all remote names

The patch maintains the architectural separation from fetch operations while providing convenient multi-remote support. The test cases verify edge conditions including invalid remote names and the flag's interaction with the core safety mechanisms (default branch preservation, unpushed work protection).

This completes the v5 series with all agreed functionality now implemented through standalone `git branch` commands. The implementation appears clean and well-tested, with clear documentation updates matching the new flag's behavior. No technical objections have been raised in the thread, suggesting the design consensus holds.