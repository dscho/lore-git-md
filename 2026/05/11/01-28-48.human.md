This is the final confirmation in a documentation thread about `git log --follow` behavior. The discussion has reached consensus that the current documentation already correctly captures the key constraints: `--follow` only works with single filenames on linear history, with all other cases being undefined.

Junio Hamano confirms that the existing wording in `git-log.txt` ("works only for a single file") and the `log.follow` config description ("cannot be used to follow multiple files and does not work well on non-linear history") sufficiently communicate these limitations. He explicitly equates directory pathspecs and non-linear history with the already-documented multiple-paths case - all are equally undefined behavior.

The thread demonstrates Git's documentation philosophy where undefined behavior remains unspecified to preserve implementation flexibility. Tamir Duberstein's original concern about explicitly listing undefined cases is addressed by Junio's authoritative position that the current binary supported/unsupported classification is sufficient. This marks the completion of the documentation improvement effort with no outstanding issues.