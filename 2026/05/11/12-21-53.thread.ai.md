Thread identity: Refactoring patch removing unused `commit` field from `struct todo_add_branch_context` in sequencer.c, authored by [author name not provided in input].

Core problem or goal: Clean up dead code in the sequencer subsystem by removing an unused field that was being written to but never read during rebase operations. The commit is passed directly to `add_decorations_to_list()` when needed rather than being stored in this context structure.

Current status: Single patch proposed, not yet picked up. The change is straightforward and appears correct - it eliminates unused code without changing behavior.

Key technical details: Modifies `struct todo_add_branch_context` in sequencer.c by removing the `commit` field. This structure is used internally by the sequencer when processing rebase commands to update refs. The field was part of state tracking but proved unnecessary for the decoration logic.

Participant positions: No reviews or discussion yet - this appears to be the first message in the thread. The patch looks like a simple, safe cleanup that would typically receive quick Acked-by or Reviewed-by from sequencer experts like Phillip Wood if no issues are found.