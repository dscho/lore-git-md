Thread identity: A single-patch refactoring series removing an unused `commit` field from `struct todo_add_branch_context` in `sequencer.c`, authored by an unnamed contributor and sent to the Git mailing list.

Core problem or goal: Clean up dead code in the sequencer subsystem. The `commit` field in `struct todo_add_branch_context` has been written to but never read since the struct was first introduced in commit 900b50c242 ("rebase: add --update-refs option", 2022-07-19) -- the field was dead from the outset.

Current status: Single patch (v1) proposed, not yet picked up by Junio. The change is straightforward and correct on its face.

Key technical details: Modifies `struct todo_add_branch_context` in `sequencer.c` by removing the `commit` field. The commit is passed directly to `add_decorations_to_list()` when needed rather than being stored in this context structure.

Participant positions: Patrick Steinhardt reviewed the patch. He confirmed the field has never been read ("dead code since the struct was first introduced"). He offered a surface-level nit: the `if` block containing only the now-removed assignment could have its braces dropped per coding guidelines. He explicitly stated this is not worth a reroll.

Open questions and loose ends: None. The review raised no substantive concerns; the single surface-level suggestion is explicitly non-blocking.

Related work: None. This is an independent cleanup.