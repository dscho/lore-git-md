This is the fourth patch in Kristoffer Haugsbakk's v5 series introducing the new `git format-rev` command. The patch adds a dedicated test case for the `--annotate-stdin --name-only` option combination in `git name-rev`, which was recently refactored in the series.

The test ensures that the name-only output from `git name-rev` matches between direct invocation and the stdin annotation mode, verifying that the refactored code paths produce identical results. The test follows the same pattern as existing tests in the file, generating expected output by calling the command directly on each revision, then comparing against the stdin-based output.

This is a straightforward test addition that closes a coverage gap in the test suite. The patch maintains the series' high quality with clean test structure and clear commit message explaining the motivation. The test itself is robust - it verifies all revisions in the repository and handles sorting to account for non-deterministic output order.

The patch builds on the infrastructure work from earlier in the series while being narrowly focused on testing. It demonstrates good patch hygiene by keeping test improvements separate from the core implementation work.