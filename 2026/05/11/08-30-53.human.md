This is a follow-up discussion in the `git fetch --deepen` bugfix thread, where Junio C Hamano and René Scharfe are considering whether to add a warning when `--deepen` is used on non-shallow repositories.

Junio agrees with René's earlier position that adding such a warning would be a separate concern from the core bugfix (which prevents `--deepen` from incorrectly converting full clones to shallow ones). René had noted that `--deepen` has silently been a no-op on non-shallow repos since its introduction in 2016, and that any warning should be implemented in the `fetch` command itself rather than in the low-level shallow handling code.

The exchange suggests the technical fix is settled, with this being a minor user experience consideration that could be addressed later if desired. The thread appears to be winding down, with no immediate plans to modify the patch further.