Here's the human-readable thread summary:

Patrick Steinhardt has proposed a two-patch series fixing maintenance mode issues in Git. The first patch addressed a lockfile cleanup problem in `--detach` mode that could cause subsequent maintenance runs to fail. The second patch, just posted, fixes a regression where the `gc.auto` configuration was no longer being respected after Git switched from using `git gc` to `git maintenance` for auto-maintenance tasks.

The changes restore the expected behavior where `gc.auto=0` prevents auto-maintenance from running, while maintaining backward compatibility with the newer `maintenance.auto` configuration (which takes precedence when both are set). The patch includes comprehensive tests verifying both config options work as expected. This addresses a user-reported regression where the behavior change surprised users who had relied on `gc.auto=0` to disable maintenance.

The series appears complete with thorough test coverage and addresses two specific maintenance subsystem bugs. While no reviews have been posted yet, the changes are well-constructed and should be uncontroversial, particularly coming from Patrick who has been actively working on maintenance infrastructure improvements. The patches target Git 2.54.0 and build on maintenance infrastructure originally introduced in Git 2.27.0.