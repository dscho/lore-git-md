Patrick Steinhardt has accepted Taylor Blau's design feedback on the v2 bugfix series. Taylor had argued that the lockfile ownership transfer should be folded into `daemonize()` itself rather than handled at the call site. Patrick initially kept the fix minimal but now agrees — with only two callers of `daemonize()` in the codebase, the automatic approach is safer. A v3 is expected, effectively matching Taylor's off-list sketch, and Taylor's Co-authored-by and Signed-off-by will likely appear on the commit.

Patch 2 (the `gc.auto` config fix) remains settled and unchanged.