Bugfix follow-up in the rebase --update-refs thread, where Abhinav Gupta confirms the issue also affects non-interactive rebase operations when rebase.instructionFormat includes %d decorations. The same error occurs when trying to update HEAD during a non-interactive rebase, demonstrating this is a broader issue than just the interactive case.

This reinforces the thread's earlier conclusion that the current patch's approach of filtering to branch refs is correct, since the failure would have occurred in prior versions anyway when attempting to update non-branch refs. The new example shows the problem manifests identically in both interactive and non-interactive rebase modes, as both use the same underlying todo list mechanism.

The email doesn't propose any changes to the v3 patch, but provides additional validation that the fix's scope is appropriate. With Phillip Wood's Reviewed-by and Junio's confirmation of the approach, this thread appears ready for the patch to be picked up, while the parallel issue in make_script_with_merges() remains for future work.