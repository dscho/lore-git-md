This is the final exchange in a documentation patch series that clarifies the behavior of `git log --follow` and `--no-follow` options. The thread has reached consensus on how to document these features, with Junio Hamano providing authoritative guidance.

The key points established are:
- `--follow` is strictly documented as working only with single filenames on linear history
- All other usage scenarios (like non-linear history or directory pathspecs) are explicitly marked as undefined behavior
- The documentation avoids describing current implementation details for unsupported cases to preserve future flexibility

Junio notes that the existing documentation already largely covers these points, particularly the single-file limitation. The `log.follow` config description similarly notes the same constraints. He suggests treating all unsupported cases (multiple pathspecs, directories, non-linear history) uniformly as undefined behavior rather than trying to document implementation quirks.

This exchange concludes a well-executed documentation effort where Tamir Duberstein worked through multiple iterations to precisely capture the feature's boundaries under Junio's guidance. The result is clear, technically accurate documentation that properly sets user expectations about supported vs. undefined behavior.