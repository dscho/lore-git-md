This is a bugfix follow-up in the rebase --update-refs thread, confirming that the issue with non-branch refs (like HEAD) affects both interactive and non-interactive rebase modes. The author provides a concrete reproduction case showing how a simple rebase operation fails when trying to update HEAD, demonstrating that the problem isn't limited to the interactive path.

The email reinforces the thread's consensus that the current patch's approach of filtering to branch refs is correct, since even prior versions would have failed when attempting to update non-branch refs. The new example shows the failure occurs in the same way across rebase modes, suggesting the fix will need to cover both code paths consistently.

With Junio's earlier approval and Phillip Wood's review, this appears to be the final confirmation needed before the patch can be merged. The parallel issue in make_script_with_merges() remains outstanding but doesn't block this core fix. The thread is now effectively resolved, with the author just providing additional validation of the solution's scope.