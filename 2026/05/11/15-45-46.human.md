This is a preparatory style cleanup patch (part 2/5) in Kristoffer Haugsbakk's series introducing the new `git format-rev` command. The patch applies clang-format to `builtin/name-rev.c` to standardize formatting before the subsequent refactoring and feature work.

The changes are purely mechanical:
- Normalizes pointer arithmetic spacing (e.g., `*(p+1)` becomes `*(p + 1)`)
- Aligns continuation lines in option definitions
- Adjusts spacing around operators and function parameters

This follows the project's pattern of separating mechanical changes from functional work, making the actual refactoring in later patches easier to review. The patch shows attention to detail by handling both code and documentation strings consistently.

As part of a well-structured series, this patch demonstrates good patch hygiene by isolating formatting changes from the substantive work that follows. The changes are straightforward and unlikely to be controversial, serving as a clean base for the subsequent patches that will introduce the new `git format-rev` functionality.