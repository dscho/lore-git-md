This is a documentation clarification thread that has now reached consensus, with Tamir Duberstein and Junio Hamano agreeing on the final wording for `git log --follow` behavior. The exchange confirms that the existing documentation already correctly captures the key constraints: `--follow` only works with single filenames on linear history, with all other cases being undefined.

Junio affirms that the current `git-log.txt` wording ("works only for a single file") and the `log.follow` config description ("cannot be used to follow multiple files and does not work well on non-linear history") sufficiently communicate these limitations. He explicitly equates directory pathspecs and non-linear history with the already-documented multiple-paths case - all are equally undefined behavior that should not be specified in detail, as that would imply support where none exists.

The thread demonstrates Git's documentation philosophy where undefined behavior remains unspecified to preserve implementation freedom. Tamir's initial concern about needing more detail is addressed by Junio's explanation that the current text already establishes the proper boundaries between supported and unsupported functionality. With this confirmation, the documentation effort is now considered complete, having gone through multiple iterations to reach this authoritative conclusion.