This is a follow-up discussion in the `git fetch --deepen` regression thread, where Junio C Hamano and René Scharfe agree that adding warnings for `--deepen` usage on non-shallow repositories is a separate concern from the core bugfix.

Junio acknowledges René's point that `--deepen` has silently been a no-op on non-shallow repos since its 2016 introduction (commit cccf74e2da). They concur that if warnings were to be added, they should be implemented in the `fetch` command layer rather than the shallow-handling code, and that this would warrant its own patch.

The exchange confirms that the technical fix for the regression (preventing incorrect conversion of full clones to shallow ones) is settled, with this being purely a discussion about potential future user experience improvements. The thread appears to be winding down, with no new technical concerns raised about the core fix in `shallow.c`.