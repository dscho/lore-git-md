Feature: Implementation of `git branch --prune-merged` for safe branch cleanup in v5 series.

This patch (3/5) from Harald Nordgren introduces the core `--prune-merged` functionality to `git branch`, completing the architectural shift from fetch integration to standalone branch commands. The new option safely deletes local branches whose upstream counterparts have been deleted, with several key protections:

1. Safety mechanisms:
   - Preserves checked-out branches in any worktree
   - Protects default branches by name (even if tracking different refs)
   - Requires `--force` to delete branches with unpushed commits
   - Honors per-branch `branch.<name>.pruneMerged` config (not shown in diff but referenced in docs)

2. Implementation details:
   - Uses `branch_get_push()` to determine push destinations
   - Verifies reachability from upstream before deletion
   - Handles multi-remote scenarios via `--all-remotes`
   - Includes comprehensive test coverage in t3200-branch.sh

The patch builds on the `--forked` plumbing from earlier in the series while adding the actual pruning logic. The tests verify edge cases including:
- Protection of default branches
- Behavior with unpushed commits
- Push ref vs upstream ref divergence
- Checked-out branch preservation

This represents the final technical implementation of the feature as agreed in the architectural discussion, with all fetch integration removed and functionality consolidated in `git branch`. The code appears cleanly implemented with thorough test coverage and clear documentation updates. The series is now technically complete and ready for final maintainer consideration.