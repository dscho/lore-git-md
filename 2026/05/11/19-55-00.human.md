This is a documentation fix, part of the per-worktree ignore patterns thread.

D. Ben Knoble responds to Phillip Wood's review of his v2 patch that updates `gitignore(5)` to say `$GIT_COMMON_DIR/info/exclude` rather than `$GIT_DIR/info/exclude`, since that is what Git actually uses in worktrees. Phillip identified five additional instances in the same file that still reference the old path, plus mentions in other documentation files. Knoble agrees to fix the ignore-related references in v3 and leave the rest for a future cleanup.

The review here is substantive -- Phillip did not just flag the missing instances, he ran `git grep` and gave the author the exact lines to fix. This is a small-bore documentation follow-up, not a controversial topic, and it is proceeding smoothly toward a v3 that makes the fix comprehensive within its scope.