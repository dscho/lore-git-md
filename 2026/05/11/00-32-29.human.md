This is the final documentation patch in a series clarifying the behavior of `git log --follow` and `--no-follow` options, which have existed since 2012 but lacked proper documentation. The thread has seen multiple iterations with authoritative review from Junio Hamano.

The patch establishes clear boundaries for what is supported versus undefined behavior. Junio firmly maintains that documentation should not describe current implementation quirks for unsupported cases, as that would lock in behavior that may change. The final specification documents that `--follow` works only with single filenames on linear history, with all other usage scenarios explicitly marked as undefined.

Tamir Duberstein has incorporated this guidance through several revisions, resulting in precise documentation that follows Git's standards for marking undefined behavior rather than describing implementation details. The exchange demonstrates Git's documentation philosophy in action - maintaining flexibility by clearly delineating supported from unsupported cases rather than exhaustively documenting current quirks.

With Junio's definitive review, this thread has reached resolution with no outstanding issues. The documentation now properly reflects the constraints that have existed in the implementation for over a decade.