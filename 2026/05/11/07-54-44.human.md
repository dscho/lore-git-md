Review of IPv6 and network address handling fixes in git-daemon.

Patrick Steinhardt provides feedback on Sebastien Tardif's patch fixing network address handling in `daemon.c`. While acknowledging the patch addresses three clear bugs, Patrick suggests splitting it into three focused patches - one for each issue. He also proposes an alternative implementation for the IPv4/IPv6 address handling that would deduplicate some code.

The review identifies two additional points:
1. The NULL port check in `execute()` seems unnecessary since ports shouldn't be unset in practice, though the defensive check is still an improvement
2. Confirms the buffer size fix in `ip2str()` is correct, noting the parameter was incorrectly sized for the output buffer rather than input

This is a substantive review that engages with both the technical correctness and code organization. Patrick doesn't request changes but offers constructive suggestions while validating the core fixes. The thread shows good technical alignment on the underlying issues, with Patrick's review adding value through implementation alternatives and deeper context about expected behavior.