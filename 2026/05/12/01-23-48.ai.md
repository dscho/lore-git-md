**Patch 04/12 -- path-walk: always emit directly-requested objects**

Review from Taylor Blau, substantive.

Taylor's review engages with the core design of the fix: he flags the
`path_is_for_direct_objects()` heuristics (checking whether a path starts
with `'/'`) as "somewhat brittle" but, by his own admission, does not have a
better suggestion ready. He openly states he is not familiar enough with the
path-walk internals to propose an alternative. This is an honest assessment
of the limits of the review, not a blocking concern.

The review also raises a secondary point: the new helper function does not
guard against a NULL `path` pointer. Taylor suggests at least an assertion
(`ASSERT()`) to turn a potential segfault into a controlled exit -- a
reasonable defensive-programming concern.

This is a thoughtful, engaged review that correctly identifies a
design-pattern tension (string-prefix heuristics vs. explicit metadata),
offers a concrete defensive-coding suggestion, and is transparent about the
reviewer's limited domain familiarity with path-walk internals. The NULL
assertion is a low-cost improvement. The brittleness concern is real but no
alternative has been proposed, so in practice the existing approach is likely
to stand unless a better idea emerges later in the thread.

No action needed from the reader here -- this is a routine review-in-progress
on an established series, not a new direction or a blocking issue.