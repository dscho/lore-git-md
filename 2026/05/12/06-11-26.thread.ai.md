Subject: [PATCH] builtin/merge: replace roundabout octopus up-to-date check with repo_in_merge_bases()

Thread author: Kristofer Karlsson (Spotify)

Type: refactoring patch

Core change: In builtin/merge.c, the octopus merge strategy's up-to-date check for each remote head was calling `repo_get_merge_bases()` to compute all merge bases between HEAD and the remote head, then extracting the first result and comparing its OID to the remote head -- a complex way of asking "is this remote head already an ancestor of HEAD?" The patch replaces this with `repo_in_merge_bases(base_commit, heads[i])`, which directly answers the reachability question. Diff is -14/+4 lines.

Performance: While the change provides some optimization (using `can_all_from_reach()` when generation numbers are available), the author clarifies the primary motivation was code cleanup and semantic clarity rather than measurable performance gains. The direct reachability check better expresses the intent of verifying ancestry rather than computing full merge bases just to compare OIDs.

Naming observation: The author suggests `repo_in_merge_bases()` might be better named `repo_is_ancestor()` to more clearly reflect its purpose, though acknowledges the practical challenges of renaming an existing function. This provides useful context about the function's behavior.

Testing: Adds a single test case for the octopus up-to-date scenario to `t6408-merge-up-to-date.sh`.

History context: The original code dates to Junio C Hamano's 2008 builtin-merge conversion. Derrick Stolee's 2018 generation number work made `repo_in_merge_bases()` possible via `can_all_from_reach()`, but this call site was never updated.

Participant positions:
- Derrick Stolee reviewed v1, confirming technical correctness while asking for performance measurements.
- Author Kristofer Karlsson clarified in follow-up that the change was primarily for code clarity rather than performance optimization.

Status: No outstanding technical concerns. The thread appears ready for integration with the clarification provided about the change's motivation being code hygiene rather than performance optimization. The naming observation about `repo_in_merge_bases()` remains an interesting side note but doesn't block the patch.