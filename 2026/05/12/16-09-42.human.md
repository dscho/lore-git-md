This is a follow-up from **Junio C Hamano**, the maintainer, replying to Phillip Wood's design question about whether the `includeIf worktree:` condition is actually needed given that Git already supports per-worktree config via `extensions.worktreeConfig` and `git config --worktree`.

Junio does not answer Phillip's question himself. Instead, he acknowledges it as an "excellent point" and says the documentation for the new feature should be strengthened to explain when each approach (path-based include vs. per-worktree config) is more appropriate, and for what use cases.

This is significant because Junio is the maintainer and the person who ultimately decides whether a series merges. His response implicitly validates Phillip's concern as a real gap in the patch's justification -- he is not dismissing it. He is effectively asking Chen Linxuan to add use-case guidance to the documentation, which is a stronger requirement than a simple request for clarification. The author will need to do this in v4 if the series is to proceed.

The bottom line: the feature's *implementation* has broad support (Patrick approved it), but its *justification* is now under scrutiny from the two people whose opinion matters most -- Phillip (as a trusted reviewer who caught the gap) and Junio (who signals that the gap needs to be filled before merging). The author's next revision will need to decide not just *how* to document the tradeoff but also whether there is a compelling enough use case to justify the feature at all.