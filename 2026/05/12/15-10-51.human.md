This is a follow-up from Phillip Wood in the ongoing `rebase --update-refs` bugfix thread, and it's one of those short but useful messages that confirms the diagnosis and wraps up a loose end.

Phillip is responding to Abhinav Gupta's observation that the bug -- where a decorated `rebase.instructionFormat` causes non-branch refs (like `HEAD`) to appear as bogus `update-ref` instructions -- also breaks non-interactive rebase. Phillip confirms: yes, that makes sense, because non-interactive rebase is essentially just `git rebase -i` with the editor set to a no-op. Barring a few special cases, the same code path runs and the same bug triggers.

This is not a new finding or a proposed change. It's a quiet validation of the thread's trajectory. The v3 patch, which filters decorations to local branch refs only, has already been reviewed by Phillip (who gave it a `Reviewed-by`) and endorsed by Junio. This email just reassures everyone that the fix's scope is correct: it covers both interactive and non-interactive rebase, which operate on the same todo-list machinery.

The tone is matter-of-fact and brief -- Phillip is known for concise, technically confident messages, and this one shows that. He is not calling for more work; he is basically saying "yep, that's consistent with what we already knew."

The separate parallel issue in `make_script_with_merges()` is still open, but this email doesn't touch it. Whoever picks up the main fix should probably address that as a follow-up, but there is no pressure to combine them.

**Bottom line for a busy reader:** Nothing to act on here. The core fix is done and validated. This email is a footnote that confirms the fix's scope includes non-interactive rebase, which the thread had already concluded was the case.