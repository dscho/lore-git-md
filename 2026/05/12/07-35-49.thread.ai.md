**Thread identity**: Bug report from Daniel Mach at SUSE ("git clone --dissociate is failing when the alternate has a commit-graph"). Standalone report with reproducer -- not a patch series. Thread type: bug report.

**Core problem**: `git clone --dissociate --reference <old-repo>` crashes during checkout with `fatal: unable to parse commit <SHA>` when the reference repository has a commit-graph. With `--dissociate`, Git repacks objects from the alternate and removes the `objects/info/alternates` file, but the clone process has already opened the alternate's commit-graph. After the alternate is no longer valid, the clone does not invalidate or reload the object store, so it attempts to parse commits using a commit-graph that references the now-unlinked alternate, causing parse failure.

**Current status**: No patch proposed yet; this is a first report with reproducer. No version or iteration number applicable. The root cause is stated by the author as AI-generated and caveated.

**Key technical details**: Files/subsystems touched: clone logic (likely `builtin/clone.c`) and the commit-graph/object-store interaction layer (likely `commit-graph.c`, possibly `object-store.h` or `object-store.c`). The key interaction is between `--dissociate` (which clears alternates and repacks from them) and the commit-graph (which is loaded from the alternate and remains in memory). No new symbols, config keys, or on-disk format changes involved. The reproducer is a shell script (attached to the original email). The fix will likely involve invalidating the commit-graph or reloading the object store after the dissociate operation completes.

**Open questions and loose ends**: The root cause analysis is the author's own but flagged as AI-generated and caveated -- it should be independently confirmed. No design objections, review requests, or conditional approvals yet. The thread awaits either a confirmation from a maintainer or a patch.

**Participant positions**: Only the original reporter (Daniel Mach) has posted. No reviews, acknowledgements, or objections from anyone else. The thread is in its very early stage.

**Related work**: None mentioned. This is a standalone bug report, not connected to any in-flight series or prior discussion.