This is a brief follow-up from Junio C Hamano (the maintainer) in the thread about Patrick Steinhardt's two-patch series fixing maintenance mode issues. Junio is responding to Jeff King's review of patch 2.

Junio agrees with Peff's observation that `gc.auto` is being read as a boolean (`repo_config_get_bool()`) when it is actually an integer threshold — the number of loose objects after which auto-maintenance should trigger. He was about to send the same message himself, which is a strong signal that this is a clear correctness issue that the author needs to fix.

No new technical content beyond that endorsement. Junio's note effectively confirms that patch 2 will need a v2 revision to switch `repo_config_get_bool()` to `repo_config_get_int()` for the `gc.auto` config key. Patch 1 (the lockfile cleanup in detached mode) has already been reviewed without blockers and could potentially proceed ahead of patch 2, or the whole series could be re-rolled together.