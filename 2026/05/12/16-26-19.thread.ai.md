**Thread identity** -- "http-walker.c: fix missing slash detection in absolute alternate URL parsing" by Jeff King (Peff). Single-patch bugfix (v1) with follow-up discussion about HTTP protocol evolution.

**Core problem or goal** -- Original patch fixed a NULL dereference in `http-walker.c` when parsing absolute alternate paths from dumb HTTP servers (changing `strchr()` to `strchrnul()`). Discussion expanded to examine dumb HTTP's security/efficiency tradeoffs versus smart protocols, with Patrick Steinhardt proposing packfile URIs as a potential middle ground.

**Current status** -- Bugfix patch accepted by maintainer (Junio C Hamano). No version history. Discussion continues about long-term protocol direction without concrete proposals.

**Key technical details** -- Files touched: `http-walker.c` (fixed). New considerations: packfile URIs could theoretically match dumb HTTP's server efficiency by serving static packfiles while using smart protocol framing. Security constraint: `fetch.uriProtocols` currently empty by default prevents redirect attacks; Patrick suggests same-host restriction might enable safer adoption.

**Open questions and loose ends** -- Whether packfile URIs with same-host restrictions could viably replace dumb HTTP for static content serving. No consensus yet on deprecating dumb protocol despite its historical bug-proneness. Eric Sunshine's prior argument about dumb HTTP's efficiency for large static repos remains unmeasured against modern smart protocol capabilities.

**Participant positions** -- Jeff King (Peff) authored original fix. Junio C Hamano accepted it while expressing wish to deprecate dumb HTTP. Patrick Steinhardt introduced packfile URI middle-ground idea with security caveats. Eric Sunshine's historical position (dumb HTTP efficient for static repos) referenced but not revisited.

**Related work** -- Original `http-walker.c` bug dates to 2005 (b3661567cf). Packfile URI feature exists but constrained by security defaults. No direct connection to other in-flight HTTP topics.