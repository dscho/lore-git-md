This is a **bugfix** patch (or rather, a pointer to prior discussion -- the email itself does not contain a patch). It is Jeff King (Peff) confirming that the bug report from Daniel Mach matches a known issue Peff had already been investigating. It is a thread-root follow-up, not a patch submission.

Peff identifies the root cause and outlines two possible fix strategies without committing to either. The first approach is to lazily fall back to manual parsing of commit data when the commit-graph is no longer usable; the second is to pre-populate all the relevant tree fields at the time the commit-graph is closed. The design decision hinges on access patterns -- whether most commits in the clone are parsed before the commit-graph is invalidated, or after.

This is an authoritative confirmation from one of the project's most respected contributors, which significantly raises the signal on this bug. The analysis from Daniel's original report is now independently corroborated. The thread has moved from "unconfirmed bug report with AI-generated diagnosis" to "known bug, fix approaches under consideration." The next step will likely be Peff sending a patch after settling on one approach.

No concrete code changes are proposed here; this is a status update and design discussion note.