Here is the updated AI thread summary incorporating Jeff King's review:

**Thread identity**: Subject: `git log --follow` fails to follow files across subtree merges. Single-patch bugfix, v2 (resend of earlier version). Author: Miklos Vajna. Thread type: bugfix patch.

**Core problem or goal**: Fixes inconsistency where `git log --follow` fails to track file history across subtree merges (when a file first appears via a subtree merge), while `git blame --follow` already handles this case correctly. The issue occurs when a file exists only in a subtree-merged inner repository - `git log --follow` shows only post-merge commits, missing pre-merge history.

**Current status**: Patch v2 sent after v1 received no replies. Junio Hamano and Jeff King have both expressed reservations about the approach, arguing that while the patch isn't technically wrong, it doesn't fundamentally address `--follow`'s limitations with merge commits. Author explicitly requested review from Elijah Newren (merge expertise) but has not yet received that input.

**Key technical details**: Modifies `log-tree.c` to add parent-checking loop in `log_tree_diff()` for merge commits, mirroring `blame.c`'s existing logic. Before early return, iterates over parents with `diff_tree_oid()` to find file under different path/name. Sets `found_follow` if found, allowing parent walk. Test case constructs two-repo scenario with subtree merge (inner.txt -> inner/inner.txt). Jeff King's review notes this still makes arbitrary parent selection and won't follow paths independently down both sides of history. Suggests exploring TREESAME checks as alternative approach.

**Open questions and loose ends**: Fundamental question remains whether incremental improvements to `--follow` are worthwhile given its inherent merge limitations. Jeff King's review raises new technical considerations about TREESAME interaction with path limiting. Thread now awaits either: (1) Elijah Newren's merge-specific perspective, or (2) author's response addressing Peff's technical suggestions about alternative approaches.

**Participant positions**: Junio maintains the patch isn't wrong but questions its value given `--follow`'s design constraints. Jeff King substantively reinforces this view with technical analysis of parent selection tradeoffs, suggesting alternative approaches via TREESAME. Author's position (incremental improvement matching `blame` behavior) now faces stronger counterarguments from two respected contributors. Elijah Newren's pending review could provide decisive technical perspective.

**Related work**: Explicitly references `blame --follow` implementation as precedent. Connects to broader discussions about history traversal limitations at merges. Jeff King's mention of TREESAME flags links to other threads about history simplification. Potential cross-reference to threads about `--follow` behavior with renames and merges in other contexts.