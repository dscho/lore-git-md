This is a follow-up in the bugfix thread for `rebase --update-refs` with `rebase.instructionFormat`. Phillip Wood replies to Abhinav Gupta's confirmation that the same issue affects non-interactive rebase, simply noting that non-interactive rebase is effectively `git rebase -i` with no sequence editor, so sharing the same code path is expected behavior rather than a surprise.

The whole conversation appears settled. With Phillip Wood's Reviewed-by already on the v3 patch and Junio having confirmed the approach, this email adds no new technical content or requests for changes -- it is essentially confirmation that the observed behavior is consistent with the architecture. The email is brief and substantive only in the sense that it explicitly ties the non-interactive case to the same root cause, but that was already established.

The remaining open work -- the parallel code path in `make_script_with_merges()` needing the same `DECORATION_REF_LOCAL` filtering -- was already identified by Phillip in the previous round and is not addressed here. The core fix is complete and ready for merging.