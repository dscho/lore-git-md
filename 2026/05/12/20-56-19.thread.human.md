Here is an update to the thread summary.

The thread about `git diff --word-diff` and whitespace confusion has progressed. Michael Montalbo's documentation patch, which explained the two-phase diff algorithm, stalled after Junio C Hamano questioned whether a description of the machinery was as helpful as practical guidance.

Now, Vincent Lefevre (the original bug reporter) has chimed in, agreeing with Junio. He suggests the documentation should recommend `--ignore-space-change`. He clarifies that this is better than Johannes Sixt's earlier suggestion of `--ignore-all-space` (`-w`), because `-w` is too aggressive and might hide a genuine structural change where spaces act as delimiters (e.g., one word vs. three separate letters). However, Vincent honestly notes the fix isn't perfect; `--ignore-space-change` wouldn't help if the whitespace changes affect line boundaries, as in his original `"foo bar"` vs. `"foo\nbar"` example. He estimates those cases are rare.

The thread now has a clear path forward: rewrite the patch to offer `--ignore-space-change` as practical advice, with Vincent's nuance about its limitations. Michael Montalbo still needs to provide an updated patch. The core open question remains whether explaining those rare corner cases is necessary for the documentation to be truly useful.