This is a review response to a refactoring patch that removes an unused `commit` field from `struct todo_add_branch_context` in `sequencer.c`.

The review is from Patrick Steinhardt, who adds historical context that the `commit` field has been dead code since the struct was first introduced in commit 900b50c242 ("rebase: add --update-refs option", 2022-07-19) -- it was never read, even from the beginning. This is a nice bonus observation that strengthens the case for the removal.

The review is **surface-level**: a single nit about braces (the assignment was the only statement in the `if` block, so after removal the braces could be dropped per coding guidelines), accompanied by a quick note that this isn't worth a reroll. No substantive concerns are raised.

For the thread: the parent patch was a straightforward cleanup with no opposition, and this review confirms it is correct and welcome. The only feedback (the braces nit) is explicitly not blocking. The patch looks ready to be picked up without any changes.