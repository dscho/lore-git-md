This is a follow-up reply from **Derrick Stolee** (the patch author) in the negotiation-control series (v3), acknowledging and responding to **Matthew John Cheetham's** review of patch 2/7 (the `--negotiation-restrict` rename).

The email is short and entirely procedural: Stolee accepts the two minor documentation nits (old option name references still hanging around in `Documentation/fetch-options.adoc` under `--negotiate-only`, and in `Documentation/config/fetch.adoc`), says he will fix them in the next version. He also independently catches a real-ish issue the reviewer flagged as a minor wish -- that the `git pull` passthrough should use `OPT_ALIAS` instead of duplicating the full `OPT_PASSTHRU_ARGV` for the new option name. Stolee agrees and says he will fix that too.

Nothing controversial here. This is the kind of low-friction, accept-and-fix exchange that signals a patch is on track. The remaining open questions from the review (the two real bugs in patch 4/7 -- the documentation inaccuracy and the `||` vs `&&` logic error) are not addressed in this email because this reply is only about patch 2/7.

The summary: author acknowledges two trivial doc nits and one duplication-reduction opportunity, all scheduled for the next iteration. No design disagreement, no technical pushback. The series is moving smoothly toward an updated v4.