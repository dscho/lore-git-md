This is a brief follow-up email from Taylor Blau on his own patch (patch 10/12 of the `--path-walk` plus `--filter` series). It is not a new patch or a substantive review of someone else's work — it is a self-directed question that the author raised while reading the final version of his own patch.

Blau notes a minor asymmetry in the patch: the code introduces a `/tagged-trees` sentinel path for trees pointed to by annotated tags (so they are still emitted even under `tree:0`), and there is existing prior art for a `/tags` sentinel path. But he observes that the same treatment was *not* applied to directly-listed trees (trees passed on the command line) when `--filter=tree:0` is active. He asks, essentially: "We did this for `/tagged-trees` and `/tags` — why didn't we also do it for directly-listed trees?"

This is not a blocking issue or a change request — Blau is just noting the inconsistency and asking for the rationale. The parent summary confirms that the patch handles the tagged-trees case correctly, and Blau's question here does not suggest he thinks the current behavior is wrong; he is trying to understand the design intent.

This is a minor clarification message, not actionable feedback. It reflects that Blau is doing a careful read of his own patch and noticed an asymmetry worth understanding. A short reply confirming the rationale (e.g., that directly-listed trees under `tree:0` are intentionally dropped because the user explicitly asked to suppress all trees) would settle this quickly.