Thread identity: strbuf_add_uint() helper function. Standalone utility patch authored by René Scharfe. Part of a series adding specialized strbuf_add_*() helpers.

Core improvement: Adds strbuf_add_uint(struct strbuf *sb, uintmax_t value) to Git's string-buffer API. Formats an unsigned integer to decimal digits directly, avoiding the vsnprintf(3) call that strbuf_addf() would perform. The project already has similar specialized functions (e.g., strbuf_addstr()), and integer formatting is common enough to justify the overhead savings.

Current status: Single clean patch, no objections raised. Unlikely to be controversial.

Key technical details: Files touched are strbuf.c (18 lines) and strbuf.h (declaration). Uses a stack-allocated buffer sized by DIV_ROUND_UP(bitsizeof(value) * 10, 33) -- a conservative formula based on the bits-to-decimal-digits ratio, large enough for any uintmax_t. Builds the string right-to-left. Function signature takes uintmax_t for maximum flexibility. No new symbols beyond the function itself. No test files or on-disk format changes.

Open questions and loose ends: None apparent. Patch appears ready for review and merging.

Participant positions: Sole author René Scharfe. No reviews or objections recorded yet. The broader series direction is toward replacing strbuf_addf() usage with simpler specialized helpers where applicable.

Related work: Part of a series from René Scharfe adding several strbuf helper functions in the same vein.