**Thread identity:** "pretty: remove possible overflow in rfc2047 encoder" -- single patch, Jeff King (Peff). No series version; standalone cleanup.

**Core problem or goal:** Remove a single line (`strbuf_grow(sb, len * 3 + strlen(encoding) + 100)`) from `pretty.c`'s RFC 2047 email header encoding path. On 32-bit systems, `len * 3` could overflow `SIZE_MAX` when processing extremely large inputs (~1.4GB author names), triggering undefined behavior. The line was originally a direct buffer sizing from 2007 that became a pure optimization when the code was converted to use strbufs later that same year.

**Current status:** The patch is submitted for review. No review replies, no objections, no Acked-by or Reviewed-by trailers yet. Trivially correct; expected to be uncontroversial.

**Key technical details:**
- File touched: `pretty.c`
- Symbol removed: one `strbuf_grow()` call, the only line changed
- Behavior change: none -- the strbuf's built-in amortized growth handles all real-world inputs without measurable slowdown
- No test changes; the patch removes dead optimization code, not observable behavior
- Commit message addresses the obvious alternative ("just use checked arithmetic") and explains why deletion is the correct approach

**Open questions and loose ends:** None. No objections, no requests for changes, no follow-ups flagged.

**Participant positions:** Only the author, Jeff King, has participated. No reviews of any weight level have been received.

**Related work:** None mentioned in the thread.