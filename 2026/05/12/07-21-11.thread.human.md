Here's a human-readable summary of the thread:

Miklos Vajna has proposed a fix for `git log --follow` to properly track file history across subtree merges, where files first appear via a merged-in repository. The patch makes `log --follow` behave like `blame --follow` already does in these cases, modifying `log-tree.c` to check all merge parents for renamed files rather than just the first parent.

Junio Hamano responded that while the patch isn't wrong, it only addresses one narrow case of `--follow`'s broader limitations with merge commits, which were originally designed for linear history. Miklos has now reached out specifically to Elijah Newren (merge expert) and Jeff King (diff/performance) for their review, acknowledging Junio's point but arguing this incremental improvement is still valuable since it matches existing `blame` behavior.

The discussion now centers on whether such targeted fixes are worthwhile given `--follow`'s inherent merge limitations. The patch appears technically sound but needs domain experts to weigh in on whether this direction makes sense or if a more comprehensive solution would be preferable. Miklos has offered to clarify in the commit message that this doesn't solve all `--follow` merge issues. The next steps depend on feedback from the requested reviewers.