Here's the updated human-readable summary:

Miklos Vajna sent a patch to fix `git log --follow`'s failure to track file history across subtree merges, where files first appear via a subtree merge. While `git blame --follow` already handles this case correctly, `git log` was missing pre-merge history. The patch mirrors blame's approach by modifying `log-tree.c` to properly check merge parents for the file under different paths.

Junio Hamano has now responded with important context about `--follow`'s fundamental limitations. He explains that the feature was designed for linear history and struggles with merges where files exist under different names in different branches. While Miklos's patch may improve the specific subtree case, Junio notes it doesn't address `--follow`'s broader design constraints with merge commits.

The discussion has shifted from a straightforward bugfix to a deeper question about whether incremental improvements to `--follow` are the right approach. Junio hasn't rejected the patch but suggests reviewers should consider if a more fundamental redesign would be needed to properly handle merge cases. Miklos will need to either find reviewers who see value in this specific fix or reconsider the approach needs to be more comprehensive. The patch remains in limbo until either more domain experts weigh in or the author proposes a different solution.