This is a documentation fix patch from D. Ben Knoble, now in v3. The patch corrects the gitignore(5) man page (and several other files) to state that per-repository exclude files live at `$GIT_COMMON_DIR/info/exclude` rather than `$GIT_DIR/info/exclude`. The change aligns the documentation with Git's actual behavior since 2.20 -- no code behavior changes.

What is new in v3 is that the author has addressed Phillip Wood's v2 review, which pointed out via `git grep` that the old path still appeared in five additional spots within `gitignore.adoc` beyond those the author had originally noticed. This iteration also extends the fix to three other documentation files (`git-ls-files.adoc`, `git-svn.adoc`, `gitformat-index.adoc`) and three source-code comments (`dir.c`, `dir.h`). Six files are touched, with thirteen lines changed. The diff is purely substitutional.

The commit message and notes clearly situate this patch within the thread's history: the original v1 proposed implementing actual per-worktree ignore files, which received negative feedback from brian m. carlson, Phillip Wood, and Junio C Hamano. The author scaled back to a documentation-only fix in v2, and v3 expands the scope to all the instances that were flagged in review. The notes also provide the concrete use case (worktrees with different LLM skill-file directories) that motivated the original proposal, which is useful context even though the feature itself was dropped.

This is a well-executed, focused documentation correction responding thoroughly to review feedback. The v3 does what v2 should have done the first time -- it sweeps all the stale references, not just the most visible ones. The patch is unlikely to be controversial; the substantive debate about whether to add per-worktree ignores is over and the author has accepted the outcome. Should be ready for merging once it gets a final look. A reader who only cares about code changes can safely skip this one.