**Thread identity** -- Subject: [PATCH 0/4] Add and use strbuf_add_uint() convenience function [v1]. Series v1. Originating author: Rene Scharfe. Thread type: patch series. This thread is closed to further discussion.

**Core problem or goal** -- Replace four uses of `strbuf_addf(sb, "%"PRIuMAX, (uintmax_t)value)` with dedicated `strbuf_add_uint()` that formats unsigned integers via division-and-pointer loop, avoiding printf machinery. Target: high-volume commands like `git cat-file --batch-all-objects --batch-check='%(objectsize)'`.

**Current status** -- v1 submitted and reviewed. All review feedback was conversational with no actionable requests. Both author and reviewer confirmed performance equivalence of implementation variants. Series is ready for maintainer pickup. Subsequent discussion about `__builtin_add_overflow()` in `strbuf_grow()` was purely observational with no patch submission or action requested.

**Key technical details** -- New function in `strbuf.h/c`: `void strbuf_add_uint(struct strbuf *sb, unsigned int value)`. Formats via `x / 10` and `x % 10` into on-stack buffer, appends with `memcpy()`. Call-sites converted in `builtin/cat-file.c`, `builtin/ls-files.c`, `builtin/ls-tree.c` (patches 2-4). Benchmark shows ~4% speedup on `git cat-file --batch-all-objects`. Patch 3 handles field-width padding via `strbuf_insert()` with `memmove()`. No test/config/format changes. Follow-up discussion noted `__builtin_add_overflow()` gives 2% speedup on Apple Clang/ARM64 in `strbuf_grow()` but no effect on GCC 15.2.

**Open questions and loose ends** -- None. Design choice settled. `__builtin_add_overflow()` observation was conversational only. Stray email about unrelated `log.diffMerges` series was misdirected and irrelevant to this thread.

**Participant positions** -- Rene Scharfe (author): Confirmed benchmarks, tested variants, reported ARM64 `__builtin_add_overflow()` findings as observation only. Jeff King (reviewer): Endorsed approach, independently verified, acknowledged ARM64 findings as interesting but not actionable. Both consider thread closed.

**Related work** -- Standalone optimization. No dependencies. `%zu` precedent (commit 82c36fa0a9) noted. `log.diffMerges` series was unrelated misdirection. `__builtin_add_overflow()` discussion was unsolicited observation about adjacent code.