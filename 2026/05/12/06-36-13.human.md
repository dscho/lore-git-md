This is a refactoring patch removing an unused field from a sequencer data structure, with a review from Patrick Steinhardt.

The patch itself is minimal: Abhinav Gupta removes the `commit` field from `struct todo_add_branch_context` in `sequencer.c`. The field was being written to but never read — the commit is passed directly to `add_decorations_to_list()` as a function argument instead. The commit message is clear and the diff is just a couple of lines. A straightforward dead-code cleanup.

Patrick Steinhardt reviewed and confirmed the patch is correct, adding historical context: the field has been dead code since the struct was first introduced in 2022 by commit 900b50c242. He also points out a minor style nit — dropping the curly braces now that the assignment inside the conditional block is gone — but explicitly says it's not worth a re-roll. The review is substantive: it verifies the claim in the commit message and adds relevant background, but it's also brief and approving.

This looks like a clean, uncontroversial, and well-received cleanup. The sort of patch that typically gets applied swiftly.