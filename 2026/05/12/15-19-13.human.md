This is a brief follow-up from Justin Tobler responding to Jeff King’s bugfix review on the ODB abstraction series.

**Classification:** bugfix follow-up

The thread context: Jeff King (Peff) identified a file descriptor leak in Patch 3/7 of Justin Tobler’s ODB write stream interface series, caught by Coverity static analysis. The leak occurs in `odb_source_loose_write_stream()` when an error from `odb_write_stream_read()` triggers a `goto cleanup` without closing the file descriptor opened earlier.

Justin acknowledges the issue in a one-line reply and says he will send another version to fix it. There is no argument about the diagnosis, no discussion of alternatives, and no other review feedback in this email — just a clean acknowledgment and a promise to respin. This is a straightforward acceptance of a valid bug report.

For a busy reader: this email changes nothing about the patch itself; it just confirms that the v4 will include the fix. The series remains on track, now with a specific resource management issue to resolve before it can merge.