Here's a human-readable summary of the thread so far:

The thread explores an interesting edge case in Git's handling of rewritten history during fetch operations. Jens Tröger encountered a situation where fetching a specific commit by SHA (dda8db1) that was previously tagged as 1.20.0 resulted in Git retrieving a different commit (fda7769) that now carries that same tag. Despite explicitly requesting the old SHA, Git's output shows the original SHA in FETCH_HEAD but actually checks out the replacement commit.

This behavior suggests Git is somehow connecting the rewritten commits behind the scenes, possibly through object replacement mechanisms or commit grafting. The core question revolves around understanding how Git makes this connection between the original and rewritten commits during fetch operations, and how one could manually trace this relationship. The discussion touches on fundamental aspects of Git's object model and its handling of history rewrites.