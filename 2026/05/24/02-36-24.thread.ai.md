Thread identity: Shell completion patch modifying path completion behavior for dotfiles in `git rm`, `git mv`, and `git ls-files`.

Core problem: Git's path completion was inconsistent with shell conventions by showing dotfiles (e.g. `.gitignore`) even when not explicitly requested with a leading dot. The change aligns completion behavior with standard shell practices while maintaining explicit dotfile access.

Current status: Initial patch version implementing the behavior change. Modifies internal `__git_index_files` helper with new `hide-dotfiles` parameter and updates AWK processing to filter dotfiles when appropriate.

Key technical details:
- Changes affect completion for `git rm`, `git mv`, `git ls-files`
- Dotfiles hidden when completing empty path components
- Dotfiles shown when explicitly completing paths starting with dot (e.g. `git rm .` completes to `.gitignore`)
- Test suite updated to verify both new default behavior and explicit dot completion

Open questions: None raised yet - initial patch appears complete with tests.

Participant positions: Single-author patch with no reviews yet.

Related work: Builds on existing shell completion infrastructure. Follows standard shell completion conventions for dotfile handling.