This performance optimization series from Kristofer Karlsson has reached its final form, implementing an O(1) solution for commit-reach.c algorithms through a `nonstale_queue` wrapper structure. The three-patch series eliminates O(n) linear scans in merge-base and ahead-behind calculations, delivering 2-3x speedups in benchmarks while maintaining all algorithm invariants.

Junio Hamano has now reviewed the second patch, which introduces queue entry deduplication as a prerequisite for the final optimization. His review is positive, noting the flag-based approach is likely the right choice compared to alternatives like hashtables. While performance-neutral on its own, this intermediate step enables the more significant O(1) optimization in the following patch. The series has addressed all technical concerns raised during review, with Jeff King and Derrick Stolee also contributing to the solution's refinement.

With the design validated and implementation complete, this optimization is poised for merging. The changes are well-contained to commit-reach.c, showing measurable improvements in large repositories (50% to 4% runtime reduction in paint_down_to_common()) without regressing simpler cases. Some documentation and assertion-based verification remain as future work, but these are separable from the core optimization.