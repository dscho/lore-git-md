Thread identity: Bugfix for `git subtree split` command in contrib/subtree/git-subtree.sh.

Core problem: The `git subtree split` command fails with "fatal: cache for <hash> already exists!" when processing merge commits that make the same commit reachable via multiple paths in the DAG, due to improper handling of duplicate paths in the command's internal caching mechanism.

Current status: Initial patch version proposed, not yet reviewed or merged. The change modifies the script to make cache_set() idempotent by checking for matching existing entries before adding new ones (7 lines added, 1 removed). The fix maintains error behavior for conflicting mappings while allowing duplicate-but-identical paths.

Key technical details: Fix targets the caching logic in the shell script implementation, specifically the cache_set() function. The solution handles commits reachable through multiple paths in merge scenarios by making the cache operation idempotent for identical mappings while preserving error detection for conflicting ones. The commit message clearly describes both the failure case and solution approach.

Open questions: None raised yet - this appears to be a focused fix awaiting review.

Participant positions: Only the original author (not specified in this summary) has participated so far.

Related work: This is a maintenance fix for the contrib/subtree implementation, which is maintained separately from core Git commands. The patch does not appear to relate to any larger ongoing efforts.