Performance optimization for `git fetch` connectivity checks.

Junio C Hamano responds to Kristofer Karlsson's patch that speeds up post-fetch connectivity checks by properly passing the transport object through the call chain. The patch had shown impressive results - reducing connectivity check time from 22s to 5s when fetching 200 new commits in a large repository.

Junio notes that the optimization builds on well-tested infrastructure, referencing prior work from 2013 and 2026 that established this optimization path. He observes that while this particular call site benefits from the transport parameter, other calls to `check_connected()` in `builtin/fetch.c` wouldn't benefit in the same way since they occur before any packs are fetched. 

The response suggests looking for other potential optimization opportunities where `check_connected()` is called with local `opt` variables that might similarly benefit from transport awareness. Junio's tone is positive about the specific change while thoughtfully considering its broader implications. The minimal patch (3 lines added, 1 modified) appears likely to be accepted given its clear performance benefits and alignment with existing optimization patterns.