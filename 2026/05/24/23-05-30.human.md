# Performance optimization for sparse index in `git restore`

Junio C Hamano provides a review of Derrick Stolee's second patch optimizing `git restore --staged` behavior with sparse indexes. The patch modifies `update_some()` in builtin/checkout.c to avoid unnecessary index expansion when handling staged changes.

Junio's review is substantive but suggests a minor improvement - extracting the newly added sparse directory handling logic into a helper function for better readability. He notes the deep nesting in the current implementation but otherwise finds the change sound, calling it "very nice" in his final comment.

This continues the thread's focus on optimizing sparse index behavior, with Junio's review indicating the technical approach is solid and likely to proceed with only minor refactoring. The change builds on Stolee's established pattern of first demonstrating the issue with tests (in patch 1) before implementing the solution (patch 2).