This is a question about Git's behavior when fetching objects that have been force-pushed or otherwise rewritten in the remote repository. Jens Tröger observes that when fetching a specific commit by SHA (dda8db1) that was previously tagged as 1.20.0 but has since been replaced by a different commit (fda7769) with the same tag, Git somehow resolves the fetch to the new commit despite the explicit SHA.

The key points are:
1. The user fetches a specific commit SHA (dda8db1) that was previously tagged
2. The remote repository has since rewritten history, replacing that commit with a different one (fda7769) under the same tag
3. Git's fetch output shows the original SHA in FETCH_HEAD but checks out the replacement SHA
4. The user wants to understand how Git makes this rewrite connection and how to manually trace it

This touches on Git's object model and how it handles rewritten history during fetch operations. The behavior suggests Git is using some form of object replacement or commit grafting mechanism, though the exact mechanism isn't clear from the email alone. The question is well-formed but would benefit from more technical context about Git's internal handling of rewritten objects during fetch.