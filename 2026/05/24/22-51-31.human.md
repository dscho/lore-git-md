This is a review comment from Junio C Hamano on Derrick Stolee's sparse-index optimization series for `git restore --staged`. The thread involves performance improvements to prevent unnecessary index expansion when working with staged changes in sparse checkouts.

Junio confirms his understanding of the issue - that `git restore --staged` currently expands the sparse index unnecessarily when processing pathspecs like '.', even though it doesn't need to modify SKIP_WORKTREE entries. He notes that while the current implementation produces correct results, it does so inefficiently by expanding the index first.

The review validates the technical approach taken in the test coverage patch (PATCH 1/2), agreeing that establishing the current behavior as a baseline is the right first step before optimization. Junio's comment shows he has carefully considered both the correctness implications and performance improvement potential of the proposed change.

This is a substantive review that engages with the core technical challenge - distinguishing between cases where index expansion is truly necessary versus when sparse directory entries can be processed directly. Junio's approval of the test-first approach suggests he expects the optimization in the follow-up patch to be well-grounded.