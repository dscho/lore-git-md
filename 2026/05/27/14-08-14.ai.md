This is a small refactoring patch in Christian Couder's URL-based promisor remote configuration series (v4). It changes the `allow_globs` parameter in `url_normalize_1()` from a `char` to a proper `bool` type, since it's being used as a boolean value. The patch also updates the two call sites to pass `true`/`false` instead of `1`/`0`.

The change is straightforward and uncontroversial - it's a code hygiene improvement that makes the interface more type-correct. The patch is part of the larger promisor remote auto-configuration work that has already gone through multiple rounds of review and is now in its final polishing stage. This particular change doesn't affect functionality but improves code clarity.

The patch follows Git's coding conventions and maintains consistency with the rest of the codebase where boolean parameters are typically declared as `bool`. It's a small but meaningful improvement that helps prevent potential misuse of the function in the future.