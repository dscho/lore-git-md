Here is the dense AI thread summary for this new thread:

RFC proposal for a new `--word-diff-align` feature to highlight edits within moved code blocks. Author identifies a gap between `--word-diff` (word-level changes) and `--color-moved` (block movement) where small edits in moved sections become hard to spot. Prototype implementation takes language-agnostic approach: tokenizes lines and scores pairs based on token overlap within 5-line windows. Current state includes core pairing logic, temporary renderer, and test case. Acknowledged limitations include rough UI and scoring thresholds. Initial testing shows meaningful edits detected in ~13% of sampled diffs from Git/CPython/Rust. Seeking feedback on whether to pursue in core Git. Key technical aspects: operates on tokenized lines, window-based matching, separate from existing diff algorithms. Open questions: scoring model refinement, UI integration, performance impact on large diffs.