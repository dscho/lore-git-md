This is a straightforward cleanup patch that replaces deprecated commit list functions with their modern equivalents. It's part of a two-patch series to remove functions that were marked for deprecation earlier this year.

The patch mechanically updates callers of `free_commit_list()` to use `commit_list_free()` instead, touching three files: `builtin/history.c`, `replay.c`, and `upload-pack.c`. The changes are simple one-line substitutions with no behavioral impact. The author notes they've verified no remaining uses exist in the `seen` or `next` branches.

This appears to be a well-executed maintenance patch that follows through on a planned deprecation. The changes are focused and clearly motivated by the prior deprecation notice. The patch is likely to be non-controversial and should merge cleanly.