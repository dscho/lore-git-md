Test modernization patch that simplifies test setup in the promisor remote test suite.

This is the first patch in Christian Couder's v4 series for URL-based promisor remote configuration. It makes a straightforward improvement to the test script by replacing two-step directory creation and initialization (`mkdir` followed by `git -C init`) with the simpler `git init` form that handles both operations in one command. The change appears in two test cases that verify promisor remote behavior.

The modification is purely mechanical and uncontroversial - it doesn't affect test behavior but makes the code slightly cleaner and more idiomatic. This kind of test cleanup is common in Git's development process, especially when preparing a series for final submission. The patch has already been reviewed as part of the larger series and is ready for merging.

The change touches only the test file `t/t5710-promisor-remote-capability.sh`, reducing 4 lines to 2 in each of two test cases. This is a minor but worthwhile cleanup that aligns with Git's coding guidelines favoring simpler, more direct test constructs where possible.