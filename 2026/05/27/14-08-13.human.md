This is a test cleanup patch that's part of Christian Couder's v4 series on URL-based auto-configuration for promisor remotes. The patch simplifies test setup in t5710 by replacing `mkdir` followed by `git -C init` with the more direct `git init` command.

The change is purely mechanical and improves test readability while maintaining the same functionality. It touches two test cases in t5710-promisor-remote-capability.sh that create a client repository for testing promisor remote configurations. The patch is well-contained and clearly motivated - it's a straightforward improvement that makes the test code slightly more concise and idiomatic.

This is a minor but sensible cleanup that's part of a larger, already-reviewed series. The change doesn't affect the test behavior or the promisor remote functionality being tested - it just makes the test setup code a bit cleaner. The patch has Christian's sign-off and follows the project's coding style.