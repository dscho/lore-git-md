This is a cleanup patch series removing deprecated functions from the codebase. The two patches target functions in `commit.h` that were marked for removal after Git 2.53.0 (which is now several years old).

The first patch updates the few remaining callers of `free_commit_list()` to use the modern alternative `release_revisions()`. The second patch removes the deprecated functions themselves from `commit.h`. The changes are mechanical and touch only 4 files, with straightforward replacements.

Kristoffer notes they've checked `seen` and `next` branches and found no in-flight uses of these functions, suggesting the removal is safe. The patch appears well-motivated and uncontroversial - it's cleaning up technical debt that was explicitly marked for future removal. The small diff size and focused scope make this an easy review.