This is a style discussion about commit message trailers in the context of a bugfix for `receive-pack`. Kristoffer Haugsbakk points out that the Git project does not use `Fixes` trailers in commit messages, unlike the kernel project where they serve a backporting purpose. The comment references a previous email thread discussing this convention.

The exchange occurs on a thread about fixing an interaction between `updateInstead` and `core.worktree` configurations in `receive-pack`. The technical content of the patch itself (which moves `GIT_WORK_TREE` environment variable handling to prevent leaks between operations) is not disputed - the discussion is purely about commit message formatting conventions.

This is a minor procedural note that doesn't affect the technical merit of the underlying fix. The observation is correct - Git commit messages typically reference prior commits in the message body rather than using `Fixes` trailers - but the comment doesn't engage with the substance of the bugfix itself. The thread's technical discussion about the receive-pack behavior remains settled.