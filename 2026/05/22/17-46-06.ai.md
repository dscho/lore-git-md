Follow-up discussion about the `git fetch` zombie process issue in container environments.

D. Ben Knoble responds to SURA's bug report about `git fetch` leaving zombie processes when running as PID 1 in containers. He points to a related discussion from February 2026 (lore.kernel.org/git/202602231615147.3294516-1-cshung@gmail.com) that may contain relevant context or solutions. The reference suggests this might be a known issue with established workarounds, though Knoble doesn't propose a specific fix.

The thread continues to focus on the core problem where fast-terminating `git fetch` subprocesses (`git-remote-https`, `git-index-pack`) aren't properly reaped when Git runs as PID 1 in containerized environments. SURA's reproduction steps demonstrate how zombie processes accumulate in Docker containers where the service acts as PID 1, eventually exhausting PID resources.

Key technical details remain unchanged from the initial report: the issue appears to have been introduced in Git 2.53.0 and primarily affects containerized deployments (particularly Go services). While not urgent (as `tini` provides a workaround), this represents a real resource leak that warrants fixing in Git's process handling logic.

The discussion hasn't yet progressed to solution proposals, but Knoble's reference to prior art suggests the community may have existing approaches to consider for proper signal handling or process waiting when running as PID 1. The next steps would likely involve identifying the exact regression commit through bisection and proposing targeted fixes to Git's subprocess management.