This is a design discussion in Harald Nordgren's `git branch --forked` series, focusing on the composability of branch filters. Johannes Sixt responds to Harald's concern about implementing combined `--merged/--not-merged` with `--forked` by clarifying the UI constraints that must be preserved for future extensibility.

Johannes emphasizes that while composability can be deferred, the current implementation must carefully structure the `--forked` option to avoid painting itself into a corner. He explains that accepting multiple branch patterns after a single `--forked` (like `git branch --forked foo bar`) would conflict with future `--list` functionality, so the syntax should require explicit `--forked` flags for each source branch (`--forked foo --forked bar`).

The exchange shows the careful balance between immediate implementation and long-term design in Git's CLI evolution. Johannes is ensuring the current work leaves room for the composable filters that Junio and others have endorsed, even if that feature comes later. This maintains the thread's trajectory of introducing robust branch management capabilities while keeping the architecture clean for future enhancements.