This is a review comment on Harald Nordgren's `git branch --forked` patch, which is part of a series adding safe branch pruning functionality. Johannes Sixt suggests making `--forked` a filter option for `--list` mode rather than a standalone operation, allowing it to be combined with other filters like `--merged`. The review is substantive, proposing concrete changes to both the implementation approach and documentation wording.

Key technical points from the review:
1. Recommends restructuring `--forked` as a filter that can be combined with other `--list` options rather than a separate mode
2. Suggests each `--forked` should take a single argument (supporting globs) with the option repeatable for multiple patterns
3. Proposes clearer documentation phrasing that drops redundant "local branches" and improves the description of pattern matching
4. Notes the useful combination of `--merged` and `--forked` for identifying integrated branches

The review demonstrates engagement with the patch's technical design while maintaining a constructive tone. Johannes clearly understands both the immediate change and how it fits into the larger branch management workflow, suggesting improvements that would make the feature more flexible and consistent with existing Git conventions. This feedback will likely require some implementation adjustments but doesn't challenge the core functionality.