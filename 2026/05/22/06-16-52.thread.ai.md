Summary:

This thread introduces a Windows platform compatibility patch modifying process signal handling. The change extends `mingw_kill()` in `compat/mingw.c` to accept both `SIGTERM` and `SIGKILL` signals, treating them identically via `TerminateProcess()` with exit code -1. The implementation removes the need for platform conditionals in calling code by unifying signal handling behavior between these two termination signals on Windows.

The change is technically sound given Windows' process termination model, which doesn't distinguish between graceful and forced termination at the API level. The patch could be strengthened with more context about motivating use cases or specific callers that would benefit from the unified signal handling. No reviewers have commented yet on this straightforward platform-specific change.