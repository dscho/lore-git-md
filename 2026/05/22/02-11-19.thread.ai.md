### AI Thread Summary

RFC proposal for a new `diff.<driver>.process` feature enabling external tools to inject diff hunks into Git's diff/blame pipelines. The now-complete 6-patch series introduces a protocol mirroring `filter.<driver>.process`, allowing specialized tools to participate mid-process while preserving downstream features like word-diff and color-moved.

Current state:
- Patch 1: xdiff modifications to accept pre-computed hunks (landed)
  * Added `xdl_hunk` struct and extended `xpparam_t` with `external_hunks` fields
  * Implemented validation logic with fallback to built-in diff
  * Preserved original line numbers by skipping `trim_common_tail`
  * 430-line test coverage in xdiff subsystem

- Patch 2: Configuration plumbing (landed)
  * Added `process` field to `userdiff_driver` struct
  * Followed existing patterns from textconv/algorithm configs
  * Minimal changes focused on infrastructure enablement

- Patch 3: Core diff implementation (landed)
  * Implements process protocol mirroring `filter.<driver>.process`
  * Handles capability negotiation (`hunks`), error recovery
  * Preserves downstream features by feeding external hunks into xdiff
  * Extensive test coverage (338 lines) including:
    - Hunk boundary validation
    - Error cases (tool crashes, invalid hunks)
    - Session state management
    - Integration with --word-diff, git log -p
    - Zero-hunk special case

- Patch 4: Blame integration (landed)
  * Modifies `diff_hunks()` in blame.c to use diff process when configured
  * Implements zero-hunk skipping for blame attribution
  * Handles cleanup of allocated hunk data
  * Test cases demonstrate:
    - Normal blame behavior (fallback when no process)
    - Skipping reformatting commits via zero-hunk detection
    - Memory management correctness

- Patch 5: Built-in whitespace normalizer demo (just arrived)
  * Provides reference implementation via `git diff-process-normalize`
  * Uses same logic as `git diff -w` (xdiff_compare_lines with XDF_IGNORE_WHITESPACE)
  * Key behaviors:
    - Returns zero hunks for whitespace-only changes
    - Falls back to built-in diff for non-whitespace changes
  * Test coverage includes:
    - Whitespace suppression verification
    - Mixed change handling
    - Pipeline integration (diff/blame)

The series is now complete with both infrastructure and practical demonstration. The whitespace normalizer serves as both a useful tool (particularly for blame workflows) and documentation of the protocol's intended use. All patches have thorough test coverage across success/error paths. No outstanding technical concerns remain - the series appears ready for final review and potential merging. The implementation successfully balances new capability introduction with preservation of existing diff/blame functionality.