RFC proposal for `diff.<driver>.process` enabling external tools to inject diff hunks into Git's diff/blame pipelines. The 6-patch series introduces a protocol mirroring `filter.<driver>.process` while preserving downstream features like word-diff and color-moved. Current state:

- Infrastructure landed (Patches 1-4):
  * xdiff modifications to accept pre-computed hunks (xdl_hunk struct, validation logic)
  * Configuration plumbing following textconv/algorithm patterns
  * Core diff logic with capability negotiation and error recovery
  * Blame integration with zero-hunk skipping and memory management
  * 768 lines of test coverage across success/error paths

- Demo implementation (Patch 5):
  * Built-in whitespace normalizer (`git diff-process-normalize`)
  * Uses XDF_IGNORE_WHITESPACE logic
  * Returns zero hunks for whitespace-only changes

Maintainer feedback (Junio Hamano) includes:
1. High-level concerns about justification vs textconv (cover letter)
2. Technical review on Patch 1 implementation:
   * `changed[]` array -1 offset noted as brittle but likely necessary
   * Suggested fallback path improvements:
     - Avoid code duplication between fallback/normal diff
     - Add warning messages for tool authors
     - Consider automatic disabling of misbehaving tools
3. Neutral-to-skeptical stance on merging as-is ("I dunno")
4. Unresolved questions about blame performance (subprocess per commit)

Open questions:
- How to better articulate unique value beyond textconv
- Whether to optimize blame performance (e.g., caching)
- If additional motivating use cases are needed
- If AST/structural diff scenarios should be emphasized
- Implementation refinements from Junio's review

The series remains technically complete but requires addressing both the high-level concerns about justification and the specific implementation feedback before likely progressing. Junio's technical suggestions indicate the xdiff changes are acceptable with refinement, while the feature's ultimate fate depends on resolving the broader design questions.