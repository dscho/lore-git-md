Here is the updated AI thread summary incorporating the new email:

---

RFC proposal for a **diff provider interface** (`diff-provider.h`) that establishes a unified abstraction layer for consulting alternate sources of diff hunks before falling back to Git's builtin xdiff computation. The series (11 patches, v7) introduces a repository-owned chain of providers, each queried in turn until one answers or fails, with the builtin xdiff as the terminal provider. This framework enables future extensions (e.g., content-carrying requests, additional backends) without requiring consumers to know provider implementations. The series ships two example providers: an **in-process cache** (`diff-hunks` store) and an **external process driver** (`diff.<driver>.process`), both answering by object identity alone. The goal is to unify diff/blame integration under a single interface while preserving downstream features (word-diff, color-moved, function context, `--stat`, `git blame`, `git log -L`). The RFC explicitly seeks design feedback on the framework direction, not a merge of the 11 patches as one.

**Core infrastructure (Patches 1-2):**
- **Interface design (Patch 2):** Introduces `diff-provider.h` with two core functions:
  - `diff_provider_consult_fn`: Queries a provider for hunks given a `diff_request` struct (old/new blobs, xdiff options, flags).
  - `diff_provider_emit_fn`: Optional callback for providers to emit hunks directly (e.g., for caching).
  Providers are registered in a repository-owned chain (`repo->diff_providers`) and consulted in order until one answers or fails. The chain is built on first consultation and released in `repo_clear()`, ensuring provider state (e.g., a running process) never outlives its repository. Consumers express requests via a single `diff_request` struct and read outcomes (answered, unanswered, or failed) without naming providers, enabling transparent integration of new backends. The interface enforces hunk validation via `diff_provider_check_hunk()`: coordinates must fit `int32`, hunks must be ordered/non-overlapping, and unchanged runs between hunks must align on both sides. The first wired consumer is `git blame`'s `pass_blame_to_parent()`, whose content loads move into a fill callback that runs only when ranges are computed.
- **Internal contract:** Adds `diff-provider-internal.h` (modeled after `refs/refs-internal.h`) to define the implementor contract, including provider dispositions (answer, pass, fail, refuse) and chain composition rules. The walk in `diff-provider.c` maps these dispositions onto the public outcome set (`answered`, `unanswered`, `failed`, `refused`) and uses `BUG()` to enforce that only the computing provider fails and that it passes on a consult-only walk. Chain order carries authority resolution: earlier providers override later ones.
- **Documentation (Patch 1):** Adds `git-diff-provider(7)` and `gitformat-diff-provider(5)` to explain the interface, provider chain, and example backends. Clarifies that providers may be authoritative (e.g., external process) or non-authoritative (e.g., cache), and that failures degrade to the builtin diff. Documents the `diff_request` struct fields and expected behavior for providers.

**In-process cache (`diff-hunks` store, Patches 3-6):**
- **Store design (Patch 3):** A non-authoritative cache at `$GIT_DIR/objects/info/diff-hunks` that may only reproduce the builtin diff. Keyed by `(old_blob, new_blob, xdl_opts)` and recorded only when the pair's trimmed and untrimmed diffs agree, ensuring one entry serves both blame (zero context) and stat formats (any context). The file uses a chunked binary format with a sorted index (DHIX) and hunk data segment (DHDT), plus a trailing checksum. Entries are deduplicated (identical hunk blocks share storage) and flushed atomically, merging with the existing store rather than replacing it. The reader is loaded once and cached on the object database; writing is opt-in (`GIT_DIFF_HUNKS_WRITE=1` or `diffHunks.write`), so ordinary commands read without modifying the store.
- **Warming mechanism (Patch 4):** Teaches stat output (`--stat`, `--numstat`, `--shortstat`) to record precomputed hunks into the store when writing is enabled. A warming run collects hunk coordinates in a single xdiff pass, sums counts from those hunks, and records the pair into the store. The store records only "trim-stable" pairs—those whose zero-context trimmed diff (blame) and untrimmed diff (stat) are identical—so one entry serves both formats. The warming run displays trimmed counts (matching a store-less run), while untrimmed counts are used only for stability comparison. Exclusions: `--ignore-blank-lines` (coalesces hunks differently between text-emitting and coordinate-callback paths), `-I` patterns, `--anchored`, and `-B` (shape the diff outside the key). Recording requires both sides to be valid regular files with named blobs, so working-tree sides, textconv output, and gitlinks are skipped.
- **Stat integration (Patch 5):** Wires stat output (`--stat`, `--numstat`, `--shortstat`) to consult the provider chain before falling back to the builtin xdiff. The core change is in `builtin_diffstat()`, which calls `diff_provider_consult()` with a summing callback (`diffstat_sum_hunk_cb()`) that accumulates provided hunk counts into the diffstat entry. On an answer, the blobs were already loaded for the binary check, so the only cost saved is the diff computation itself. On an unanswered outcome, the code computes as before and, if writing is enabled, records what it computed. The request carries the pair’s object IDs and the diff options read by the exclusions; sides that are not stored blobs (working-tree files, gitlinks) have NULL IDs, so the store passes them by and the terminal provider computes them. Exclusions are handled as follows:
  - `-I` patterns, `--anchored` anchors, and break detection (`-B`) map to `DIFF_PROVIDER_DISP_STOP_NO_RECORD`, ensuring such requests are neither served nor recorded for any consumer.
  - `--ignore-blank-lines` is excluded before consulting because it coalesces hunks differently between text-emitting and coordinate-callback paths, so a served answer would not match a store-less run’s output.
  - A `log -L` range-scoped stat neither reads nor records, as the line-range filter computes it as before.
- **Blame integration (Patch 6):** Wires `git blame` to consult the provider chain via `pass_blame_to_parent()`. The store is consulted only for plain blob-pair diffs (no reverse blame, ignored revisions, textconv, or working-tree/`--contents` pseudo-commits). Requests with `-I` patterns or anchors bypass the store entirely. Blame’s default `xdl_opts` now derive from `DIFF_HUNKS_DEFAULT_XDL_OPTS`, ensuring key alignment with stat warming runs. `--show-stats` reports store hits/misses via consultation counters. Test coverage (355 new lines in `t4220-diff-hunks.sh`) verifies:
  - Output parity for plain, `--porcelain`, and `--incremental` blame.
  - Hit/miss accounting across warming runs.
  - Bypass conditions: `-w`, indent heuristics, `--reverse`, textconv, `-M`/`-C`, `--ignore-rev`, `--contents`, and rename/merge handling.
  - Corruption recovery (truncated store, corrupt signature, over-claimed chunk count, zero-hunk record).
  Performance tests (`t/perf/p4218-diff-hunks.sh`) show a ~1.26× speedup for blame on `git.git` with a warmed store.
- **Format and recovery:** On-disk format documented in `gitformat-diff-hunks(5)`. Lacks fsck integration or expiry; recovery relies on `git diff-hunks clear` and re-warming. Corruption (e.g., checksum mismatch, malformed chunks) is detected on read and handled gracefully (fallback to builtin diff). The `git diff-hunks verify` command checks the checksum, chunk table, sort order, and every entry's hunk sequence; `git diff-hunks clear` removes the file.
- **Performance:** On `git.git`, a warmed store speeds up `log --stat -5000` by ~1.9x and `git blame` by ~1.26x, with hit rates of 54-74% depending on the file. The store serves blame at zero context and stat formats at any `-U`.

**External process provider (`diff.<driver>.process`, Patches 7-11):**
- **Plumbing (Patch 7):** Adds the `process` field to `struct userdiff_driver` and teaches the config parser to populate it from `diff.<driver>.process`. This field names the long-running hunk provider process for a given diff driver but is not yet used by the consult logic. The change is mechanical: two lines in `userdiff.h` to declare the field and its owned string, and seven lines in `userdiff.c` to parse the config key and manage ownership. This plumbing enables the external process provider introduced in later patches.
- **Protocol (Patch 11):** A long-running pkt-line protocol (version=1, capability=hunks) over stdin/stdout. The handshake negotiates version/capability, then per-file requests send object names (`old-oid`/`new-oid`) and xdiff options. The tool responds with hunk lines (`hunk <old_start> <old_count> <new_start> <new_count>`) and a status packet (success/error/abort). Git validates hunks for bounds, overlap, and per-gap alignment before feeding them into xdiff. On error/abort, Git falls back to the builtin diff for that file or the entire session, respectively. The protocol is **identity-only**: only object names are sent, enabling providers to answer before any blob is read (e.g., for caching or fetching blobs themselves). The process is **authoritative**: its answers may differ from the builtin diff and override the `diff-hunks` store. A pair the process answers is never recorded into the store, ensuring the store holds only builtin results.
- **Key refinements from v6:**
  - **Identity-only requests:** The protocol sends only object names (no file content), enabling providers to answer before any blob is read. This suits caches or processes that fetch blobs themselves (e.g., via `git cat-file --batch`).
  - **Per-command opt-in:** Consulting is opt-in per command (like `textconv`), with failures degrading to the builtin diff. Porcelain commands (`git diff`, `git log`, `git blame`) allow it by default; plumbing commands (e.g., `diff-tree`) forbid it unless explicitly enabled.
  - **Gating:** Options the process is never told about (e.g., whitespace-ignoring flags, `--ignore-matching-lines`, `--anchored`, or a forced diff algorithm) bypass the process entirely. `--diff-process`/`--no-diff-process` control it independently of `--ext-diff`.
  - **Forward-compatible design:** Tools ignore unknown request keys; Git ignores unknown trailing fields on hunk lines.
  - **Subprocess lifecycle:** Managed via the provider chain, ensuring the process is launched on first use and terminated with the repository. A failed process is retained in the pool to avoid retries.
  - **Error handling:** Protocol errors (malformed packets, unrecognized status, or process crashes) disable the process for the remainder of the command, falling back to the builtin diff. Semantically invalid coordinates (e.g., overlapping hunks) skip only the affected pair while keeping the process alive.
- **Subprocess API refinements (Patches 10-11):**
  - **Patch 10:** Adds `subprocess_read_status_gently()` to `sub-process.c` and `sub-process.h`, a gentle variant of `subprocess_read_status()` that returns -1 instead of dying on protocol errors (malformed length headers, truncated packets, or empty packets where a status line or flush is expected). This is essential for the diff process provider, which treats its external process as optional: any protocol error must degrade to the builtin diff rather than abort the command. The existing `subprocess_read_status()` and its callers are unchanged, preserving fatal behavior for required subprocesses like filters.
  - **Patch 11:** Extracts process lifecycle management from hashmap management in `sub-process.c`, introducing `subprocess_start_command()` and `subprocess_stop_command()` as lower-level primitives. This decouples the subprocess API from the global hashmap, allowing the diff process provider to manage its own process pool with custom membership rules (e.g., retaining failed commands to avoid retries). The change is mechanical: `subprocess_start()` and `subprocess_stop()` become thin wrappers around the new `_command` variants. No behavior changes; this is purely about decoupling concerns.
- **Integration (Patch 11):** Wires `git blame` and the summary formats (`--stat`, `--numstat`, `--shortstat`) to consult the external process via the provider interface. The external process is consulted after the `diff-hunks` store but before the builtin xdiff, enabling authoritative answers to override the cache. A `status=success` response with zero hunks asserts the blobs equivalent, omitting the pair from the stat or passing the whole commit through in blame. Patch output and `git log -L` range tracking remain builtin, as the protocol currently lacks content-carrying requests.

**Documentation clarification:**
- **Scope of external diff drivers:** A documentation patch updates `gitattributes(5)` to explicitly state the limitations of traditional external diff drivers (pre-`diff-provider` interface). These drivers *only* replace textual patch output and do not integrate with Git's builtin features like word diff, function context (`-W`), `--color-moved`, or syntax coloring. Summary formats (`--stat`, `--numstat`, `--shortstat`, `--dirstat`), `git blame`, and `git log -L` do not invoke external drivers at all and continue using the builtin diff. This clarification underscores the motivation for the `diff-provider` interface, which aims to unify and extend how external diff sources interact with Git's full feature set.

**Bypass mechanisms:**
- `--no-ext-diff` disables all external providers (including the process driver).
- `format-patch` forces builtin diff via `no_diff_process` flag.
- `--diff-algorithm` and whitespace-ignoring flags bypass all providers.
- 16+ test cases verify bypass behaviors.

**Technical refinements:**
1. **Provider chain:** Repository-owned, built on first consultation, released in `repo_clear()`.
2. **Request struct:** Unified `diff_request` for all providers, with flags for context (`DIFF_CONTEXT_ZERO`), stat formats, and blame.
3. **Validation:** Hunks must be ordered, non-overlapping, and keep unchanged runs aligned on both sides; misaligned gaps trigger fallback.
4. **Error handling:** Graceful fallback for malformed responses, transient errors (`status=error`), and permanent withdrawal (`status=abort`). The gentle status read (`subprocess_read_status_gently()`) ensures protocol errors in the external process provider degrade to the builtin diff rather than aborting the command.
5. **Performance:** Default path (no configured providers) runs within noise of pre-series code, confirming the abstraction adds no measurable overhead. The `diff-hunks` store adds no overhead when disabled and delivers 1.26-1.9x speedups when warmed.
6. **Windows compatibility:** Path handling with spaces addressed.
7. **Test coverage:** 1747+ lines in `t/t4220-diff-hunks.sh` (1412+ prior + 355 in Patch 6) and 593 lines in `t/t4080-diff-process.sh`, plus performance measurements in `t/perf/p4218-diff-hunks.sh`. The `diff-hunks` store includes tests for corruption handling, atomic flush, deduplication, warming behavior (e.g., store creation, refresh, trim-divergent pairs, binary/mode-only pairs, and corruption recovery), stat integration (output parity, exclusion handling, algorithm keying), and blame integration (bypass conditions, hit/miss accounting, output parity). The external process provider tests cover protocol handshake, adversarial responses (malformed hunks, crashes, unrecognized statuses), gating, and error paths.
8. **Integration scope:** The external process provider is wired into `git blame` and summary formats (`--stat`, `--numstat`, `--shortstat`). Patch output and `git log -L` remain builtin due to the lack of content-carrying requests in the protocol.

**Key technical details:**
- **Files touched:** `diff-provider.c`, `diff-provider.h`, `diff-provider-internal.h`, `diff-hunks.c`, `diff-hunks.h`, `diff-process.c`, `diff-process.h`, `diff.c`, `diff.h`, `xdiff-interface.h`, `blame.c`, `builtin/blame.c`, `builtin/diff.c`, `builtin/log.c`, `range-diff.c`, `sub-process.c`, `sub-process.h`, `userdiff.c`, `userdiff.h`, `Documentation/git-diff-provider.txt`, `Documentation/git-diff-hunks.txt`, `Documentation/gitformat-diff-hunks.txt`, `Documentation/gitformat-diff-provider.txt`, `Documentation/config/diff.adoc`, `Documentation/diff-algorithm-option.adoc`, `Documentation/diff-options.adoc`, `Documentation/gitattributes.txt`, `t/helper/test-diff-process-backend.c`, `t/t4220-diff-hunks.sh`, `t/t4080-diff-process.sh`, `t/perf/p4218-diff-hunks.sh`.
- **New symbols:**
  - Interface: `diff_provider_consult_fn`, `diff_provider_emit_fn`, `diff_request`, `diff_provider`, `repo_diff_providers`, `diff_provider_check_hunk()`, `enum diff_provider_outcome` (`answered`, `unanswered`, `failed`, `refused`), `diffstat_sum_hunk_cb()` (summing callback for stat integration), `DIFF_HUNKS_DEFAULT_XDL_OPTS`.
  - Store: `diff_hunks_consult()`, `diff_hunks_emit()`, `chunk-format.h` (on-disk format), `git diff-hunks` (plumbing command), trace2 "read-hits" datum.
  - Protocol: `PKT_REQUEST`, `PKT_HUNK`, `PKT_STATUS` (version=1, capability=hunks), `diff_process_provider_new()`, `struct xdl_hunk`.
  - Subprocess API: `subprocess_start_command()`, `subprocess_stop_command()` (lower-level primitives for process lifecycle management), `subprocess_read_status_gently()` (gentle status read for optional processes).
  - Userdiff: `struct userdiff_driver::process` (field added in Patch 7 to name the external process provider).
- **OID fields:** `old-oid`/`new-oid` sent for identity-only requests; content-carrying requests deferred to future series.
- **Validation:** Bounds, overlap, per-gap alignment (not just total unchanged line counts).

**Participant positions:**
- **Michael Montalbo:**
  - Restructured the series as a unified framework with two example providers (in-process cache and external process).
  - Implemented the provider interface (`diff-provider.h`) with repository-owned chain, transparent fallback to builtin xdiff, and robust hunk validation.
  - Added `diff-provider-internal.h` to define the implementor contract and chain composition rules.
  - Added `diff-hunks` store (Patch 3) as a non-authoritative cache with chunked binary format, atomic flush, deduplication, corruption handling, and warming mechanism for stat output. The store is keyed by `(old_blob, new_blob, xdl_opts)` and recorded only when trimmed/untrimmed diffs agree, ensuring compatibility with blame (zero context) and stat formats (any context). Writing is opt-in (`GIT_DIFF_HUNKS_WRITE=1` or `diffHunks.write`), and the reader is cached on the object database for zero-overhead reads.
  - Implemented warming mechanism (Patch 4) for stat output (`--stat`, `--numstat`, `--shortstat`), enabling side-effect cache filling during a warming run. The mechanism enforces trim-stability (blame vs. stat compatibility), excludes incompatible options (`--ignore-blank-lines`, `-I`, `--anchored`, `-B`), and skips non-file pairs (working-tree sides, textconv, gitlinks). Tests verify store creation, refresh, trim-divergent pairs, binary/mode-only pairs, and corruption recovery.
  - Wired stat output to consult the provider chain (Patch 5), adding a summing callback (`diffstat_sum_hunk_cb()`) to accumulate hunk counts into diffstat entries. The integration handles exclusions (`-I`, `--anchored`, `-B`, `--ignore-blank-lines`) and range-scoped stats (`log -L`) correctly, ensuring output parity with store-less runs. Tests cover output parity, exclusion bypass, and algorithm keying.
  - Wired `git blame` to the store via the provider interface (Patch 6), consulting the store only for plain blob-pair diffs (no reverse blame, ignored revisions, textconv, or working-tree/`--contents` pseudo-commits). Requests with `-I` patterns or anchors bypass the store. Blame’s default `xdl_opts` now derive from `DIFF_HUNKS_DEFAULT_XDL_OPTS` to align keys with stat warming runs. `--show-stats` reports store hits/misses. Tests verify output parity, bypass conditions, hit/miss accounting, and corruption recovery.
  - Added plumbing for the external process provider (Patch 7): the `process` field in `struct userdiff_driver` and config parsing for `diff.<driver>.process`. This field names the long-running hunk provider process but is not yet used by the consult logic.
  - Implemented the external process provider (Patch 11) with identity-only requests, per-command opt-in, and authoritative answers. The protocol is versioned (version=1, capability=hunks) and forward-compatible. The process is managed via a repository-owned pool, launched on first use, and terminated with the repository. Failed processes are retained to avoid retries. The provider is wired into `git blame` and summary formats (`--stat`, `--numstat`, `--shortstat`), with patch output and `git log -L` remaining builtin.
  - **Subprocess API refinements:**
    - **Patch 10:** Added `subprocess_read_status_gently()` to handle protocol errors gracefully for optional processes like the diff process provider. This ensures malformed status packets degrade to the builtin diff rather than aborting the command.
    - **Patch 11:** Extracted process lifecycle management from hashmap management in `sub-process.c`, introducing `subprocess_start_command()` and `subprocess_stop_command()` as lower-level primitives. This decouples the subprocess API from the global hashmap, allowing the diff process provider to manage its own process pool with custom membership rules (e.g., retaining failed commands to avoid retries).
  - Wired all diff/blame features to consult the interface, preserving downstream features (word-diff, color-moved, function context, `--stat`, `git blame`, `git log -L`).
  - Added comprehensive documentation (`git-diff-provider(7)`, `gitformat-diff-provider(5)`, `git-diff-hunks(1)`, `gitformat-diff-hunks(5)`) and clarified the scope of traditional external diff drivers in `gitattributes(5)`.
  - Provided performance measurements showing no overhead for the default path and significant speedups for warmed caches.
  - **Patch breakdown:**
    - **Patch 1:** Documentation for the provider interface and example backends.
    - **Patch 2:** Core interface (`diff-provider.h`), internal contract (`diff-provider-internal.h`), and repository integration.
    - **Patches 3-4:** `diff-hunks` store implementation (format, keying, corruption handling, warming mechanism, plumbing command).
    - **Patch 5:** Wired stat output (`--stat`, `--numstat`, `--shortstat`) to the provider chain, adding exclusion handling and summing callback.
    - **Patch 6:** Wired `git blame` to the store via the interface, with key alignment, bypass conditions, and hit/miss accounting.
    - **Patch 7:** Added plumbing for the external process provider (`struct userdiff_driver::process` and config parsing).
    - **Patches 8-9:** Subprocess plumbing and protocol for the external process provider.
    - **Patch 10:** Added `subprocess_read_status_gently()` for graceful protocol error handling in optional processes.
    - **Patch 11:** Implemented the external process provider, including protocol, process pool, request/response parsing, validation, and integration with `git blame` and summary formats.
- **Junio Hamano:**
  - **Merge conflict resolved:** Acknowledged the rebase onto `mm/line-log-limited-ops` in v6; no further objections to the structural conflict.
  - **Technical completeness:** Declared the prior series (v6) "technically complete" and ready for merging after addressing the merge conflict.
  - **No new feedback:** No additional review comments on v7; prior endorsement of the C-based test helper and Windows compatibility carries forward.

**Open questions:**
- **Design feedback:** The RFC explicitly asks whether the direction is right, not for the 11 patches to merge as one. If accepted, the work will return as separate series:
  1. Interface and `diff-hunks` store (Patches 1-6).
  2. External process provider (Patches 7-11).
  3. Content enrichment (content-carrying requests and patch output integration, deferred to future series).
- **Scope:** The series focuses on identity-keyed requests; content-carrying requests (for patch output) are deferred to later work. The external process provider currently supports only `git blame` and summary formats (`--stat`, `--numstat`, `--shortstat`); patch output and `git log -L` remain builtin.

**Progress indicators:**
1. **Interface complete:** `diff-provider.h` establishes a unified abstraction for consulting alternate hunk sources, with robust validation and error handling.
2. **Internal contract defined:** `diff-provider-internal.h` clarifies implementor responsibilities and chain composition rules.
3. **Providers implemented:** Two example backends (`diff-hunks` store and external process) demonstrate authoritative and non-authoritative use cases. The `diff-hunks` store (Patches 3-4) adds a chunked binary format with atomic flush, deduplication, corruption handling, and warming mechanism for stat output. Patches 5-6 wire stat output and `git blame` to the provider chain, handling exclusions, key alignment, and bypass conditions correctly. The external process provider (Patch 11) adds identity-only requests, per-command opt-in, and authoritative answers, with integration into `git blame` and summary formats.
4. **Plumbing added:** Patch 7 adds the `process` field to `struct userdiff_driver` and config parsing for `diff.<driver>.process`, enabling the external process provider.
5. **Subprocess API refined:** Patches 10-11 decouple process lifecycle management from hashmap management and add gentle status reading for optional processes, ensuring protocol errors degrade gracefully.
6. **Integration complete:** All diff/blame features wired to consult the interface, preserving downstream features. The external process provider is integrated into `git blame` and summary formats; patch output and `git log -L` remain builtin due to the lack of content-carrying requests.
7. **Documentation updated:** Clarified the limitations of traditional external diff drivers, underscoring the need for the `diff-provider` interface.
8. **Performance validated:** Default path adds no overhead; warmed cache delivers 1.26-1.9x speedups. The `diff-hunks` store adds no overhead when disabled.
9. **Error handling robust:** Graceful fallback for malformed responses, transient errors, and permanent withdrawal. The gentle status read ensures protocol errors in the external process provider degrade to the builtin diff.
10. **Documentation complete:** Man pages explain the interface, store format, protocol, warming mechanism, exclusion handling, and scope of external diff drivers.
11. **Test coverage expanded:** 2340+ lines of tests (1747+ for `diff-hunks`, 593 for external process), including performance measurements, corruption handling, warming behavior, stat integration, blame integration, protocol handshake, adversarial responses, gating, and error paths.
12. **Merge conflict resolved:** Series rebased onto `mm/line-log-limited-ops`; no structural conflicts remain.
13. **Bypass mechanisms unified:** `--no-ext-diff`, `format-patch`, and whitespace-ignoring flags work consistently.

The series is **technically complete for its scope** and ready for design feedback. All substantive review feedback from v6 has been addressed, including the merge conflict with `mm/line-log-limited-ops`, the documentation clarification for external diff drivers, the stat/blame integration details (exclusion handling, key alignment, output parity, hit/miss accounting), the subprocess API refinements (gentle status read and decoupled lifecycle management), and the external process provider implementation (protocol, process pool, validation, and integration). The only remaining work is splitting into reviewable topics and addressing any design feedback on the framework.

---