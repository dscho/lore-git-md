This is the second patch in a series introducing a new `diff.<driver>.process` feature that allows external tools to inject diff hunks into Git's diff and blame pipelines. The RFC proposal aims to enable specialized tools (like syntax-aware diff tools) to participate in Git's diff processing while preserving downstream features like word-diff and color-moved.

This specific patch adds the configuration plumbing for the new feature, following the established pattern of Git's filter process protocol. It introduces a `process` field to the `userdiff_driver` struct and handles the config parsing for `diff.<driver>.process`. The implementation is minimal and focused - just 9 lines of code changes across two files.

The patch appears technically sound, building on existing Git patterns. As part of an RFC series, it's laying groundwork for the more substantial protocol implementation that will follow in subsequent patches. The author is clearly following Git's conventions for configuration handling and memory management (note the `_owned` field pattern for allocated strings).

This is a straightforward infrastructure patch that enables the more interesting functionality to come. Readers interested in the overall feature can likely skip diving deep on this one, as it's mostly boilerplate configuration plumbing. The real meat of the proposal will be in the protocol implementation and integration patches that follow.