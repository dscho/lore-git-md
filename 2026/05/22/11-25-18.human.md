This is a design discussion in Harald Nordgren's ongoing series adding `git branch --forked` for safe local branch cleanup. Johannes Sixt provides implementation guidance on how to structure the command-line interface to allow future composability with other filters like `--merged`.

The key point is that while the composable filter functionality can be deferred to a follow-up series, the current implementation needs to carefully design the argument parsing to avoid painting itself into a corner. Johannes explains that `--forked` should accept only a single branch argument (e.g. `--forked foo bar` where `bar` is the pattern to match) rather than multiple branch arguments, as the latter would conflict with potential future `--list` mode integration.

This is a substantive review focused on forward-compatible API design, noting that while the full composability can come later, the current patch needs to lay the groundwork correctly. The discussion shows the careful balancing act between immediate feature delivery and long-term maintainability that characterizes mature open source projects.