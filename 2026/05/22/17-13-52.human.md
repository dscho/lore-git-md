This is a bug report thread about repository corruption during rapid `git add`/`git commit` sequences. Fabrice SALVAIRE confirms experiencing crashes and corruption in Git 2.54 when running automated Python subprocess calls to Git, with a 1-second delay between operations working around the issue. 

The discussion has narrowed to background maintenance operations (like `git gc`) as the likely culprit, based on Kristoffer Haugsbakk's earlier references to Jeff King's warnings about this behavior in recent Git versions. The timing-sensitive nature (100ms fails while 1s works) strongly suggests a race condition, though the exact interaction between maintenance and index updates remains to be fully diagnosed.

This appears to be a real and serious issue affecting automated workflows, with evidence pointing to a regression in Git's locking behavior during background operations. The thread has progressed from initial observation to a plausible technical explanation, though verification against the original case and a proper fix are still needed.