This is a preparatory patch in the fifth iteration of Derrick Stolee's series to enable `--path-walk` and `--filter` interaction in `git pack-objects`. The patch temporarily disables the `GIT_TEST_PACK_PATH_WALK` test variable in three test cases in `t/t5620-backfill.sh` to avoid warnings about the current incompatibility between `--path-walk` and `--filter` options.

The changes are straightforward test modifications that add `GIT_TEST_PACK_PATH_WALK=0` to suppress warnings that would otherwise appear in the test output. This is a temporary measure until the full series implements proper compatibility between these features. The patch demonstrates good attention to test hygiene by ensuring clean test output while the underlying functionality is being developed.

As the first patch in a well-developed series that has already gone through multiple rounds of review, this appears to be a minor but necessary adjustment to keep the test suite clean during development. The commit message clearly explains the purpose and temporary nature of the change.