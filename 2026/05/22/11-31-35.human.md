This is patch 3/6 in Harald Nordgren's v11 series adding `git branch --prune-merged` for safe automated branch cleanup. The patch prepares the underlying `delete_branches()` function for bulk operations by adding two new flags:

1. `no_head_fallback` - Skips the implicit HEAD comparison when checking if branches are merged, since the bulk operation only cares about upstream status
2. `dry_run` - Adds preview capability with "Would delete branch..." output matching the live operation's wording

The changes maintain all existing behavior for current callers (who pass 0 for both flags) while enabling the upcoming `--prune-merged` feature to operate safely at scale. The patch carefully handles edge cases like NULL head_rev and ensures dry-run output precisely matches what would happen in a real run.

This is a technical refactoring that builds on the safety mechanisms established in earlier patches, with clean diffs showing focused changes to support the series' goals. The implementation appears solid, with clear attention to maintaining existing behavior while adding the new capabilities needed for bulk operations.