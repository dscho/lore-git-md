This is the final patch in the `pack-objects --path-walk --filter` integration series, implementing support for the `combine` filter type. The patch enables composition of multiple filter types (like `blob:none`, `tree:0`, `object:type`) through intersection logic.

The key technical aspects are:
- Each filter type is handled to preserve monotonicity when combined (e.g. taking the minimum blob limit when multiple `blob:limit` filters are combined)
- The implementation splits `prepare_filters()` into a recursive helper to properly handle nested filter structures without premature releases
- Comprehensive test coverage verifies various filter combinations including edge cases and error conditions
- Documentation is updated to list `combine` as a supported filter form

The patch builds on the previous work in the series that implemented individual filter types, now allowing them to create more complex filtering conditions. The implementation carefully handles edge cases like:
- Preserving trees when blobs are requested (needed for tagged objects)
- Properly rejecting invalid combinations (like multiple `sparse:oid` filters)
- Maintaining consistent behavior regardless of filter ordering

With this patch, the series' goal of full `--path-walk` and `--filter` interoperability is complete. The implementation demonstrates robust handling of all major filter types and their combinations while maintaining the performance benefits of the path-walk approach.