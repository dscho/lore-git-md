This is a platform compatibility patch targeting Windows (mingw) process handling. The change allows `SIGKILL` to work alongside the existing `SIGTERM` support in the Windows-specific process termination code, eliminating the need for platform-specific conditionals in calling code.

The patch modifies `mingw_kill()` in `compat/mingw.c` to accept both `SIGTERM` and `SIGKILL` signals, treating them identically when terminating processes on Windows. The implementation remains simple - it just opens the process handle and calls `TerminateProcess()` with the same -1 exit code for both signals.

The commit message is straightforward but could benefit from more context about why this change is useful now or what specific callers would be simplified by it. The change itself appears correct and low-risk, as Windows doesn't distinguish between these signals in its process termination API. This aligns Windows behavior slightly closer to Unix while maintaining the existing simple process termination model.