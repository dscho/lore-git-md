This is Junio C Hamano's reply to Johannes Sixt's review of Harald Nordgren's `git branch --forked` patch. The exchange is part of an ongoing discussion about adding safe branch cleanup features to Git.

Junio agrees with Johannes' suggestion that combining `--forked` with other filters like `--merged` would be useful, noting that `--forked origin/* --merged origin/main` would show integrated branches while `--forked origin/* --not-merged origin/main` would show work still in progress. His brief reply signals approval of Johannes' proposed filter-oriented redesign while adding the observation about the `--not-merged` use case.

The discussion remains technical and constructive, with both reviewers now aligned on making `--forked` a flexible filter that can be combined with other branch listing options. Junio's response suggests the design direction is sound and the patch series is moving toward a mergeable state once the implementation matches the agreed approach.