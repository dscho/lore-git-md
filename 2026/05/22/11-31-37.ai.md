This is patch 5/6 in Harald Nordgren's v11 series adding `git branch --prune-merged` functionality, focusing on the branch-specific opt-out mechanism. The patch introduces a `branch.<name>.pruneMerged` config option that allows exempting specific branches from automated cleanup while still permitting explicit deletion.

The key addition is a configurable opt-out that preserves branches marked with `branch.<name>.pruneMerged=false` during `--prune-merged` operations, while maintaining all existing safety checks. The implementation:
- Config lookup during branch collection
- Non-quiet mode reports skipped branches with explanation
- Explicit `git branch -d` remains unaffected by the setting
- Comprehensive tests verifying both the opt-out behavior and the continued functionality of explicit deletion

The patch fits cleanly into the series' established safety model, adding another layer of control while preserving the core guarantees around branch protection. The tests demonstrate the interaction between the new config and existing protections like push-tracking comparison and checked-out branch detection.

This appears to be a well-contained addition that completes the series' configurability story, leaving only the final documentation polish remaining in patch 6/6. The implementation follows the same careful approach seen throughout the series, with clear test coverage of both the happy path and edge cases.