This is v11 of Harald Nordgren's series adding `git branch --prune-merged` for safe automated local branch cleanup, now with a push-tracking safety check and dry-run support. The series has evolved through extensive review to refine both the safety mechanisms and interface design.

Key changes in v11:
- Replaces remote-specific flags with a more flexible branch/glob syntax that handles both remote-tracking and local upstream branches
- Adds `--dry-run` mode to preview deletions
- Introduces a push-tracking safety check (comparing `@{push}` vs `@{upstream}`) to avoid deleting trunk branches
- Adds `branch.<name>.pruneMerged` config to opt-out specific branches
- Preserves all prior safety checks (checked-out branches, missing upstreams)

The implementation now:
- Uses wildmatch for glob pattern matching
- Combines literal and glob arguments via union
- Preserves branches by name (not just by upstream)
- Includes comprehensive tests covering:
  - Local vs remote upstreams
  - Default branch protection
  - Dry-run output
  - Error cases

The thread context shows this version addresses all prior feedback from Junio C Hamano and Phillip Wood, with clean range-diffs showing focused changes between versions. The series appears technically complete and ready for final review.

Notable technical details:
- Reachability checks use local refs only (no implicit fetch)
- Push/upstream comparison prevents trunk branch deletion
- Dry-run output matches exactly what would be deleted
- Config opt-out preserves branches for further development

The safety model has been strengthened through collaborative review, particularly around tracking branch protection and interface design. The series is in its final phase with all major concerns addressed.