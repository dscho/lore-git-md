This is the final patch (11/11) in Johannes Schindelin's series addressing Windows platform issues with large objects (>4GB) during cloning. The patch focuses on CI configuration to optimize test execution time.

The change modifies `ci/lib.sh` to enable expensive tests (marked with the EXPENSIVE prereq) only for push builds to integration branches (next, master, main, maint), not for every PR iteration. This implements Derrick Stolee's suggestion to run these resource-intensive tests at a regular cadence rather than on every PR build, as the extra runtime (200-850 seconds per test) isn't justified for every iteration.

The patch cleanly concludes the series by addressing the CI performance concern raised earlier in the thread. It maintains validation coverage while reducing CI burden, following the pattern established in previous patches (5/11-10/11) that optimized test infrastructure for large object handling. The change is straightforward and well-motivated, with clear attribution to Derrick Stolee's input.

This completes the series' systematic approach to fixing Windows-specific truncation issues while balancing thorough testing with practical CI constraints. The series as a whole appears ready for integration, having addressed all substantive review feedback across its three iterations.