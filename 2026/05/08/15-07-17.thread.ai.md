**Thread identity**: "paint_down_to_common: early exit when one merge-base suffices" -- patch series (v4) from Kristofer Karlsson. Series is complete and queued by the maintainer.

**Core problem or goal**: Speed up single merge-base calculation in large repositories with generation numbers by exiting `paint_down_to_common()` early after finding the first valid merge-base, skipping the STALE commit frontier processing needed only when `--all` is requested.

**Current status**: Series accepted, queued, thread closed. v4 2/2 (the optimization, the final substantive patch) was queued by Junio. All feedback from all versions has been addressed. The final email is a social thank-you from the author with no technical content.

**Key technical details**:
- Files touched: `commit-reach.c`, `merge-recursive.c`
- `enum merge_base_flags` introduced as a new type replacing old boolean parameter (v4 1/2 -- pure refactoring)
- `MERGE_BASE_FIND_ALL` flag added; callers that want all merge-bases pass this flag; the default (no flag) enables early exit
- Optimization activates when generation numbers exist and are finite
- 100x speedup reported (5s -> 24ms in 2.2M-commit monorepo)
- Test coverage verified behavior with and without commit-graphs
- Minor naming inconsistency (`flags` vs `mb_flags` at one call site) noted by Junio but explicitly not a requested change

**Open questions and loose ends**: None. The series is complete and accepted.

**Participant positions**:
- Junio Hamano: approved and queued v4. Called the optimization patch "Very nicely done and well described." Noted but explicitly did not request fixing a minor variable name inconsistency.
- Patrick Steinhardt: requested API improvements and the commit split in earlier versions; resolved.
- Kristofer Karlsson: author, responsible to all feedback across all four versions. Final email is a thank-you reply to Junio.

**Related work**: Builds on existing generation number infrastructure. Complements other performance work in `commit-reach.c`. No dependencies on other in-flight topics.

A single low-weight social email closed the thread with no substantive content beyond the author thanking the maintainer for accepting the series.