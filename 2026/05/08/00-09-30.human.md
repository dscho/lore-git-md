This is a follow-up discussion about the `--max-count-oldest` feature, specifically addressing Junio C Hamano's skepticism about how many scripts might rely on the current behavior of negative `--max-count` values. 

Mirko Faina explains a potential use case where scripts might use `--max-count=-1` to avoid conditional logic for when to include the option, though he acknowledges this relies on undocumented behavior. He concedes that if the impact would be minimal, he's willing to remove the `--max-count-oldest` restriction that prevents negative values.

The exchange appears to be a minor clarification point rather than a substantive issue, as the core feature implementation remains unchanged. The discussion highlights Git's careful consideration of backward compatibility, even for undocumented behaviors that might be in use. Mirko maintains a pragmatic stance - willing to adjust if the maintainer judges the compatibility risk to be low.