This is a response to a bug report about `git diff --word-diff`'s whitespace handling. Johannes Sixt explains that the behavior Vincent Lefevre reported is actually expected - `--word-diff` is not whitespace-agnostic by default, and users who want to ignore whitespace changes should use the `-w` flag. 

The response clarifies that the word-diff output is based on the underlying line-diff behavior, which does show changes when whitespace differs. This matches Git's standard diff behavior where whitespace changes are considered meaningful unless explicitly ignored with `-w`. 

This isn't a bug fix but rather a clarification of intended behavior. The exchange highlights that while GNU wdiff handles whitespace differently by default, Git's behavior is deliberate and can be adjusted with existing flags. The thread may continue with discussion about whether this default behavior is optimal or if the documentation should be clearer.