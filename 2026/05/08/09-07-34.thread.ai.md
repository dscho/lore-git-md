Thread identity: Feature patch for `git-jump` contrib script by Greg Hurrell, adding automatic mode selection when run without arguments.

Core problem/goal: Improve usability of `git-jump` by automatically detecting and selecting between "merge" mode (when unmerged paths exist) and "diff" mode (when unstaged changes exist) when no arguments are provided.

Current status: Initial patch version proposing the change, with documentation updates explaining the new behavior.

Key technical details:
- Uses `git ls-files -u` to detect unmerged paths (merge mode)
- Uses `git diff --quiet` to detect unstaged changes (diff mode)
- Falls back to showing usage if neither condition is met
- Maintains backward compatibility with explicit mode specification
- Changes are confined to the `git-jump` script in contrib/
- Documentation updates explain the new automatic behavior

Open questions/loose ends: None raised yet - this is the first patch in the thread.

Participant positions: Only the author (Greg Hurrell) has participated so far, proposing the change.

Related work: Standalone change to a contrib script, not part of any larger ongoing effort.