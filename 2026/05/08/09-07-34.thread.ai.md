Thread identity: Feature patch for `git-jump` contrib script by Greg Hurrell, adding automatic mode selection when run without arguments.

Core problem/goal: Improve usability of `git-jump` by automatically detecting and selecting between "merge" mode (when unmerged paths exist) and "diff" mode (when unstaged changes exist) when no arguments are provided, while maintaining backward compatibility with explicit mode specification.

Current status: Patch has reached consensus on core functionality with Jeff King (Peff) approving both the automatic behavior and the restructuring to use an explicit `auto` mode. Documentation improvements are now being implemented per Erik Cervin Edin's suggestions. The optional addition of staged whitespace checking (`git diff --cached --check`) remains under consideration.

Key technical details:
- Will use explicit `auto` mode instead of previous implicit behavior
- Detection mechanisms remain:
  - `git ls-files -u` for unmerged paths (merge mode)
  - `git diff --quiet` for unstaged changes (diff mode)
  - Falls back to showing usage if neither condition is met
- Maintains backward compatibility with explicit mode specification
- Usage text will document auto-detect heuristics
- Optional whitespace checking addition being considered

Open questions/loose ends:
- Final implementation of usage text documentation
- Decision on whether to include staged whitespace checking in current patch
- No remaining technical objections to core functionality

Participant positions:
- Greg Hurrell (author): Implementing changes based on-list feedback
- Jeff King (Peff): Approved core functionality and explicit `auto` mode restructuring, neutral on whitespace checking addition
- Erik Cervin Edin: Suggested documentation and implementation improvements that are now being adopted
- Consensus remains that core functionality is mergeable

Related work: Standalone change to a contrib script, with additional context about Homebrew distribution increasing the user base. Discussion has progressed through implementation refinements while maintaining readiness of current patch.

Resolution: Thread moving toward final implementation incorporating explicit `auto` mode and documentation improvements, with whitespace checking as optional follow-up. Core functionality remains approved and mergeable.