Follow-up discussion on `git diff --word-diff` whitespace behavior. Vincent Lefevre responds to Johannes Sixt's explanation that whitespace sensitivity is expected behavior, arguing the documentation doesn't make this clear. He demonstrates that piping regular `git diff` output to GNU `wdiff` produces different (and in his view, more correct) results for whitespace-only changes.

The key point of contention remains whether Git's word-diff should match external tools' behavior by default. Vincent's example shows GNU wdiff handling the same input differently, suggesting either a documentation gap or potential behavior mismatch. This extends the thread by introducing a concrete comparison case, though it doesn't fundamentally challenge Johannes' technical explanation about Git's internal whitespace handling rules.

The exchange highlights a documentation/expectation issue more than a code defect - the current behavior is intentional but may not meet all users' needs or expectations based on other tools they use. The thread remains unresolved regarding whether Git should adjust its behavior or just better document the current design.