This is a bugfix for the `rebase --update-refs` feature, addressing an edge case with non-branch ref decorations. Phillip Wood has reviewed and approved the v2 patch, which now appears ready for inclusion.

The fix prevents `rebase --update-refs` from incorrectly generating update instructions for non-branch refs like HEAD when `rebase.instructionFormat` includes `%d` (show all decorations). The v2 iteration simplifies the test case as suggested in review, making it more robust while maintaining the core fix that filters decorations to only process local branch refs.

The patch is small and focused, with clear verification that it:
1. Still correctly updates branch refs
2. Properly ignores non-branch decorations
3. Maintains the documented behavior of `--update-refs`

With Phillip Wood's approval (he's a recognized expert on rebase/sequencer code) and no outstanding concerns, this looks like a straightforward correctness fix for a relatively new feature (introduced in Git 2.38). The thread shows productive collaboration between the author and reviewer to refine the solution.