This is a follow-up discussion about the `--max-count-oldest` feature's interaction with negative values for `--max-count`. Mirko Faina responds to Junio C Hamano's skepticism about scripts relying on the undocumented behavior where negative values disable the limit.

Key points:
- Mirko provides a concrete example of how scripts might use `--max-count=-1` to avoid conditional logic, though acknowledges this relies on undocumented behavior
- He maintains his position that changing this behavior could break existing scripts, but defers to Junio's judgment on whether the impact would be significant
- The discussion remains academic as the `--max-count-oldest` implementation path was already settled in previous emails, with negative values explicitly rejected for the new option

This exchange doesn't introduce new technical concerns or require changes to the feature, serving primarily as a final clarification about edge case behavior. The thread remains resolved with all substantive feedback addressed in previous iterations.