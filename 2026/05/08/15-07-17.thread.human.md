This thread discusses a performance optimization for calculating merge bases in large repositories, which achieves a 100x speedup (from 5 seconds to 24ms in a 2.2 million-commit monorepo) by modifying `paint_down_to_common()` to exit early when only one merge base is needed. The optimization, proposed by Kristofer Karlsson, leverages generation numbers to skip unnecessary work while maintaining correctness.

The series is now at v2, having addressed Junio's initial review feedback about code clarity and comments. The core algorithm remains unchanged - it stops searching after finding the first valid merge base when generation numbers guarantee no better candidate exists. Patrick Steinhardt has now suggested an API improvement, proposing to replace boolean parameters with an explicit flags enum for better readability at call sites. While this would be a stylistic change rather than a functional one, it could affect several functions in this performance-critical path. The author will need to decide whether to incorporate this suggestion in a potential v3. With Junio's earlier concerns addressed and no new technical objections raised, the patch appears merge-ready pending final design decisions.