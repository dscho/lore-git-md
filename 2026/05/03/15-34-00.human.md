This is a type safety fix in the ongoing remote group push feature series. The patch addresses compiler warnings by changing the `nr` and `alloc` fields in `struct push_cas_option` from `int` to `size_t`, ensuring consistent type usage when comparing against size-based values. This was the last remaining issue identified in the series, following Junio Hamano's earlier guidance.

The change is straightforward and mechanical - it simply updates the type declarations in `remote.h` to match how these counters are used elsewhere in the codebase. The patch carries Junio's Suggested-by tag, indicating maintainer approval of the approach. With this fix, the series appears ready for merging, having addressed all architectural concerns and compiler warnings through five iterations of review.