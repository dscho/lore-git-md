This is a bugfix for submodule parallel fetching configuration. The patch fixes an issue where the `submodule.fetchJobs` setting in `.gitmodules` was being ignored due to a pointer indirection bug.

The v2 patch maintains the same technical fix as v1 (removing an incorrect `&` operator when passing the `max_jobs` parameter) but now includes a clearer commit message explaining the problem. The fix ensures that parallel submodule operations properly respect the configured job count from `.gitmodules`.

A new test case has been added to verify the fix by checking that a configured `submodule.fetchJobs` value of 67 in `.gitmodules` is actually used during submodule updates. The test looks for the expected "67 tasks" message in the Git trace output.

The patch appears straightforward and well-tested, addressing a clear correctness issue in submodule configuration handling. The author has incorporated the feedback from the v1 review by improving the commit message while keeping the technically sound core fix. This seems ready for inclusion.