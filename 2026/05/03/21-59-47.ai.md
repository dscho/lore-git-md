This is a technical clarification from Junio C Hamano about the limitations of the split-commit workaround for Git's rename detection in Ruby namespace moves.

Junio responds to Phillip Wood's earlier suggestion of splitting namespace moves into two commits (first move, then modify) by noting this approach has significant limitations. While it helps for `git log -M` and `git rebase` operations, it fails to address rename detection problem in other common scenarios like `git diff` and `git merge` operations.

The maintainer's intervention highlights that the workaround is incomplete - it doesn't solve the fundamental issue that Git's rename detection is snapshot-based and struggles with cases where most lines change (like Ruby namespace moves that alter indentation). This suggests the problem may require deeper solution than simple workflow adjustments.

The email doesn't propose an alternative solution, instead focusing on setting accurate expectations about the current workaround's scope. This positions the thread as still needing a more comprehensive solution for cross-operation rename detection in language-specific cases.