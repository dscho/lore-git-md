This is a bugfix from Junio C Hamano addressing an uninitialized variable warning in Kristoffer Haugsbakk's `git format-rev` patch series. The compiler caught that `struct commit *commit` could be used uninitialized in the error path when processing peeled tags. 

The issue occurs in the experimental `format-rev` command's tag-handling logic, where a commit pointer is only initialized when peeling succeeds, but then checked unconditionally. Junio's analysis shows the exact control flow problem and suggests initializing the variable to NULL as the straightforward fix.

This is a typical maintainer-level catch - Junio is verifying the series builds cleanly with all warnings enabled before queuing it. The fix should be trivial for Kristoffer to incorporate into the next iteration. The warning doesn't indicate any deeper design flaw in the `format-rev` command itself, just a minor oversight in one error path.

The thread continues to show the careful review process for this new command, with Junio now weighing in on implementation correctness after the earlier substantive reviews from Phillip Wood. All indications are that the series remains on track for eventual merging once these final polish items are addressed.