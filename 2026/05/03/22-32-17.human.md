This is a new patch submission from Harald Nordgren introducing an `--autostash` option for `git checkout` branch switching, separate from the previous `--track=fetch` discussion. 

The patch appears to be a straightforward addition of the existing `--autostash` behavior (which was previously only available in `git rebase` and `git pull`) to `git checkout` when switching branches. The autostash feature automatically stashes local changes before an operation and reapplies them afterward, preventing the need for manual stashing when switching branches with uncommitted changes.

Notably, this is not part of the contentious `--track=fetch` thread - it's a fresh submission that happens to come from the same author. The patch seems well-contained and addresses a clear workflow pain point (branch switching with dirty worktree) by extending existing functionality consistently across commands. 

Given that autostash is already an accepted concept in Git (used in rebase/pull) and this simply extends it to checkout, it's likely to be less controversial than the previous tracking branch proposal. The implementation appears clean and the commit message (not shown here) would presumably reference the existing autostash infrastructure.

This looks like a practical quality-of-life improvement that aligns with Git's existing patterns. Unless there are unexpected edge cases in the implementation, it should have smoother sailing than the author's previous submission.