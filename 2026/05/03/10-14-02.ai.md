This is a bugfix for the submodule configuration system, specifically how it reads the `submodule.fetchJobs` setting from `.gitmodules`. The patch fixes a pointer indirection issue where `max_jobs` was being passed incorrectly to `config_from_gitmodules()`, causing the fetch jobs configuration to be ignored.

The change is small (a single character fix removing an `&`) but important - it ensures the parallel submodule fetch configuration is properly honored. The patch includes a new test case that verifies the fix by checking that a configured `submodule.fetchJobs` value in `.gitmodules` is actually used during submodule updates.

The bug appears to be a simple oversight in the parameter passing rather than a deeper architectural issue. The fix is straightforward and well-tested, with the new test case providing clear regression protection. This seems like a low-risk change that improves correctness for users who configure parallel submodule fetching.