This is a technical discussion about HTTP protocol efficiency in Git, continuing from a recently merged bugfix in the dumb HTTP walker code. Jeff King (Peff) responds to Patrick Steinhardt's suggestion that packfile URIs could offer similar server efficiency to dumb HTTP while using the smart protocol.

Peff clarifies that while packfile URIs help with pack generation overhead, they don't eliminate the server-side computation needed to determine which objects the client requires. He contrasts this with dumb HTTP's ability to let clients fetch pre-generated static packs directly, reducing server load for top-off fetches. The discussion highlights an inherent tradeoff between protocol efficiency and security constraints.

The thread remains speculative, with no concrete proposals yet for changing the current `fetch.uriProtocols` defaults. Both participants acknowledge the security implications of allowing arbitrary packfile URIs while recognizing the potential benefits of same-host restrictions. The exchange provides useful context for future protocol evolution discussions but doesn't advance any immediate changes.