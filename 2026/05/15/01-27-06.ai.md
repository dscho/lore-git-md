This is Junio's response in the ongoing discussion about the "today" alias in Git's approxidate parsing. He clarifies his position on whether to formalize the current behavior where "today" is treated like "now" (which currently happens as a fallback for unrecognized strings).

Junio makes several key points:
1. The current behavior where "today" equals "now" is an accidental side effect of the fallback logic, not an intentional design choice
2. While Git generally preserves existing behavior to avoid breaking users, accidental behaviors can be fixed when they don't make sense
3. The discussion isn't about whether to codify the status quo, but whether the current behavior is sensible enough to keep

This moves the conversation forward by establishing that:
- The team has latitude to change this behavior if warranted
- The bar for changing it is whether the current behavior is reasonable, not just whether it was intentional
- Any solution should be evaluated on its merits rather than just preserving the current implementation

The response keeps the door open for Tuomas's proposed middle-ground solution (setting just the day component without specifying time) while making clear this would be considered a behavior change that needs justification. Junio's stance suggests he's looking for a solution that makes semantic sense for users rather than just formalizing the current implementation.