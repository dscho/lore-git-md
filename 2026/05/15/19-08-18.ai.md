This is a technical review from Jeff King (Peff) raising concerns about René Scharfe's ALLOC_GROW macro refactoring. Peff identifies a subtle type safety issue that could emerge when callers pass non-size_t arguments to the macro, particularly with the new st_alloc_nr() helper function.

The review is substantive, digging into potential integer overflow scenarios that could occur when smaller integer types (like int) are used by callers. Peff notes that while the current code handles this safely via alloc_nr(), the proposed change might introduce array overflow vulnerabilities in such cases. He provides a concrete example with alloc_node() as a known case using int rather than size_t.

This feedback suggests the patch may need adjustment to maintain safety across all calling patterns, particularly for legacy code using smaller integer types. The observation shows careful consideration of edge cases and backwards compatibility - typical of Peff's thorough review style. The thread now has an open question about how to handle mixed-type usage of this core allocation macro.