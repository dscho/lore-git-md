Thread identity: Bug report and fixes from Junio C Hamano and Jeff King regarding UBSan failures in Git's CI pipeline affecting the 'next' integration branch, specifically memory leaks and integer overflows in edge cases.

Core problem: Two undefined behavior issues surfaced when EXPENSIVE tests were enabled in CI. The first was a signed integer overflow in `commit.c` during UTF-8 verification of large commit messages (over 2GB), and the second was a memory leak in `apply.c` when handling "patch too large" errors (over ~1GiB). Both were edge cases hidden behind EXPENSIVE test prerequisites.

Current status: Jeff King has provided complete solutions for both issues. The `commit.c` fix (15 lines added, 16 removed) restructures UTF-8 verification to use `size_t` for buffer handling, splits return values for clarity, and makes internal counting unsigned. The `apply.c` patch (net +2 lines) fixed the memory leak in error paths. These changes stabilize the 'next' branch where the issues were discovered.

Key technical details: The integer overflow occurred in `commit.c` during UTF-8 verification where buffer positions were tracked as `int`. The fix transitions to `size_t` throughout the verification chain and carefully handles edge cases like `offset-1` operations. The memory leak in `apply.c` was introduced in commit f1c0e3946e (2022-10-25) when size checking was added. Both fixes demonstrate the value of expanded CI testing with UBSan and EXPENSIVE tests.

Open questions: None - both issues have been conclusively addressed. The thread validates the decision to enable thorough testing in CI.

Participant positions: Junio reported the CI failures initially. Johannes Schindelen enabled the EXPENSIVE tests that revealed them. Jeff King provided technically precise fixes for both issues - his latest UTF-8 verification changes show particular care in handling type transitions and edge cases while maintaining code clarity. The small but impactful patches reflect his characteristic attention to memory safety and undefined behavior.

Related work: This follows recent efforts to expand CI test coverage, particularly around undefined behavior detection. The fixes address a regression from 2022's size checking improvements and help validate the testing infrastructure. The UTF-8 verification changes future-proof the code against similar edge cases while maintaining safety for normal-sized commit messages.