Thread identity: Performance optimization patch series for commit-reach.c's `tips_reachable_from_bases()` function, used by ref-filter operations like `for-each-ref --merged` and `branch/tag --contains`. Current version is v2.

Core problem: Original implementation had O(C*T) complexity from linear tip scans. The optimization achieves O(C+T) by using faster lookup structures, with v2 switching from decoration API to object flags after review feedback.

Key technical details: The commit-reach.c change now uses the RESULT object flag to mark tip commits, replacing the v1 decoration hash approach. This maintains the 4x speedup (2.3M commit monorepo: 2.3s->0.31s) while being more straightforward. The solution naturally deduplicates tips (multiple refs to same commit) via shared commit object flags. Removes unused index field from commit_and_index struct and fixes v1's early termination bug with duplicate tips.

Current status: v2 implements Jeff King's suggested object-flag approach, validated on both monorepo and linux.git with synthetic branches. The +11/-12 line diff shows simplification while preserving optimization. Core algorithmic improvement is production-ready pending final review.

Open questions: None remaining - v2 addresses all prior concerns. The object-flag approach has been accepted as the preferred solution over decorations, commit-slabs, or other alternatives previously considered.

Participant positions:
- Kristofer Karlsson (author): Implemented object-flag approach per feedback, confirmed performance benefits
- Jeff King: Recommended object flags over decorations in v1 review, now satisfied with v2 solution

Related work: Builds on Git's commit-reach infrastructure. Relevant to performance-sensitive ref-filter operations in large repositories, connecting to broader ref handling efficiency efforts (reftable, geometric repacking).