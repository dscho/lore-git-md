Thread identity: Performance optimization patch for commit-reach.c's `tips_reachable_from_bases()` function, which is used by ref-filter operations like `for-each-ref --merged` and `branch/tag --contains`.

Core problem: The current implementation has O(C*T) complexity due to a linear scan of tips per commit walked. The patch replaces this with O(C+T) complexity by using Git's decoration mechanism to store tip indices for O(1) lookup.

Key technical details: The optimization targets the commit-reach.c machinery, specifically changing how `tips_reachable_from_bases()` checks if commits are reachable from tip references. The implementation adds 9 lines and removes 6, using the existing API rather than introducing new infrastructure.

Current status: Initial version demonstrating significant speedups (4x improvement shown in benchmarks for `for-each-ref` on repos with 10,000 refs). Other commands see smaller gains as they check fewer tips. The author notes some implementation rough edges (pointer casting, scope indentation) that could be cleaned up in follow-ups if the approach is accepted.

Open questions: Whether to refine the implementation details (pointer casting, indentation) in this patch or defer to follow-ups. No substantive technical concerns raised yet.

Participant positions: Only the author has commented so far, noting the optimization's benefits and implementation details that could be improved. No reviews or feedback from others yet.

Related work: Builds on Git's existing decoration mechanism and commit-reach infrastructure. Particularly relevant to ref-filter operations and performance-sensitive commands in large repositories.