Thread identity: Performance optimization patch series for commit-reach.c's `tips_reachable_from_bases()` function, used by ref-filter operations like `for-each-ref --merged` and `branch/tag --contains`. Current version is v3.

Core problem: Original implementation had O(C*T) complexity from linear tip scans. The optimization achieves O(1) flag checks using the RESULT object flag, maintaining 4x-6x speedups (2.3M commit monorepo: 2.3s->0.31s) while being more straightforward than the v1 decoration hash approach. The solution naturally deduplicates tips via shared commit object flags.

Key technical details: The commit-reach.c change uses object flags per Jeff King's v2 suggestion, removing the unused index field from commit_and_index struct. V3 adds comprehensive regression tests covering multiple-refs-to-same-commit cases (duplicate tips) that exposed v1's decoration hash collision issue. Tests verify reachability reporting, unreachable cases, and edge behavior at minimum generation (testing early termination). Implementation remains clean (+11/-12 lines core change) while fixing v1 issues (early termination bug, decoration overhead). Performance impact is most significant for operations checking many refs (like for-each-ref), with smaller gains for commands examining fewer tips (branch/tag).

Current status: v3 addresses all outstanding review feedback with complete test coverage. The optimization is production-ready, validated on both monorepo and linux.git with synthetic branches. Core algorithmic improvement and requested verification are complete pending final maintainer approval.

Open questions: None remaining - all review requests (object-flag approach from Jeff King, regression tests from Derrick Stolee) have been addressed in v3.

Participant positions:
- Kristofer Karlsson (author): Implemented object-flag approach per feedback, added comprehensive regression tests, confirmed performance benefits
- Jeff King: Recommended object flags over decorations in v1 review, satisfied with v2 solution
- Derrick Stolee: Requested regression test for multiple-refs-to-same-commit case - now fully implemented in v3 with three test scenarios

Related work: Builds on Git's commit-reach infrastructure. Relevant to performance-sensitive ref-filter operations in large repositories, connecting to broader ref handling efficiency efforts (reftable, geometric repacking). The optimization provides a key primitive for these larger initiatives.