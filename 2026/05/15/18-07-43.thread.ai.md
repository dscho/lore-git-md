Thread identity: Performance optimization patch for commit-reach.c's `tips_reachable_from_bases()` function, used by ref-filter operations like `for-each-ref --merged` and `branch/tag --contains`.

Core problem: Current implementation has O(C*T) complexity from linear tip scans. The optimization aims for O(C+T) by using faster lookup structures. Initial performance claims have been validated across repository types after fixing an early termination bug.

Key technical details: The commit-reach.c change replaces linear scans with a lookup structure for tip references. After fixing a generation-index tracking bug related to decoration hash collisions, the optimization now shows consistent 4.3x speedups (1.33s->0.31s) in both monorepo and linux.git cases. Implementation alternatives under consideration include decoration API (current interim solution), object-flags bit, commit-slab, oidmap, khash with custom values, or oidset - each with tradeoffs in boilerplate, memory use, and lookup speed.

Current status: Performance benefits confirmed across repository types after fixing the early termination logic. The core algorithmic improvement is validated but implementation needs refinement to move away from the current decoration API hack ((void*)(i+1)) to a more appropriate data structure.

Open questions:
1. Which data structure to standardize on (object-flags vs commit-slab vs others)
2. Whether the decoration API can be used temporarily or should be replaced immediately
3. Final performance validation on additional repository shapes

Participant positions:
- Kristofer Karlsson (original author): Fixed the performance regression, validated speedups, acknowledges need for cleaner implementation
- Jeff King: Initially identified performance inconsistencies and architectural concerns about decoration API usage

Related work: Builds on Git's commit-reach infrastructure. Connects to broader ref handling efficiency efforts (reftable, geometric repacking). Relevant to performance-sensitive ref-filter operations in large repositories.