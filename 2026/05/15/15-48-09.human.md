This is the first patch in Philippe Blain's 3-part documentation series clarifying Git's raw diff output format. The change removes a mention of `git diff-tree`-specific behavior (printing hashes of compared objects) from the general diff format documentation, since this only applies when using `--stdin` mode with `diff-tree` and is already documented in the `git-diff-tree` man page. 

The patch makes the documentation more accurate by focusing only on the common output format shared across all diff commands (`diff-tree`, `diff-index`, `diff-files`, and `diff --raw`). It's a straightforward documentation cleanup that doesn't change any behavior, just improves the accuracy of the docs. The commit message clearly explains the rationale, and the change itself is minimal and well-scoped. 

This appears to be an uncontroversial improvement that makes the documentation more precise about what behavior applies universally versus what's command-specific. The patch follows the pattern seen in the rest of the series - small, focused documentation tweaks that correct inaccuracies or fill gaps in the existing docs.