RFC patch series proposing to make "today" resolve to midnight in Git's date parsing, now at v2 with maintainer approval. Original proposal adds `date_today()` function to handle "today" as midnight when no time is specified, fixing `git log --since=today` including future commits due to current time fallback. Junio C Hamano initially noted the `--since`/`--until` tradeoff (midnight works well for `--since` but creates ambiguity for `--until`), suggesting documenting the tradeoff while keeping the more useful `--since` behavior.

Key changes in v2:
- Expanded commit message explaining the `--since`/`--until` tradeoffs
- Added documentation in rev-list-options.adoc
- Maintains existing behavior for time-qualified expressions like "noon today"

Implementation remains focused on `date_today()` in approxidate.c (10 LOC plus tests). Junio now approves the v2 approach ("I like this construction of argument") and asks how it relates to Tuomas Ahola's previous work on "noon"/"tea" time specifications, suggesting potential coordination with those pending changes. This indicates the core technical solution is accepted pending integration with related date-handling improvements. The thread appears headed toward resolution with no outstanding technical objections, positioning this quality-of-life improvement for merging after final coordination.