RFC patch series proposing to make "today" resolve to midnight in Git's date parsing, now effectively approved and ready for merging after final coordination. Original proposal adds `date_today()` function to handle "today" as midnight when no time is specified, fixing `git log --since=today` including future commits due to current time fallback. Junio C Hamano initially noted the `--since`/`--until` tradeoff (midnight works well for `--since` but creates ambiguity for `--until`), suggesting documenting the tradeoff while keeping the more useful `--since` behavior.

Key changes in v2:
- Expanded commit message explaining the `--since`/`--until` tradeoffs
- Added documentation in rev-list-options.adoc
- Maintains existing behavior for time-qualified expressions like "noon today"

Implementation remains focused on `date_today()` in approxidate.c (10 LOC plus tests). Junio approved the v2 approach ("I like this construction of argument") and raised integration questions with Tuomas Ahola's pending "noon"/"tea" time specification work. Tuomas has now confirmed successful rebasing of his series on top of this change, with an upcoming v4 that will remove redundant handling. This demonstrates clean technical integration between the two efforts.

The solution addresses a real usability pain point (unexpected future commits in `--since=today` results) with a clean implementation that fits Git's date handling patterns. No outstanding technical objections remain; the thread shows maintainer-contributor coordination converging toward merging this quality-of-life improvement after Tuomas submits his updated series. The change affects approxidate.c parsing and rev-list documentation while maintaining backward compatibility with time-qualified expressions.