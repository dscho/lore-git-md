This is the first patch in Philippe Blain's 3-part documentation series clarifying Git's raw diff output format. The change removes a sentence from `diff-format.adoc` that specifically described `git diff-tree` behavior (printing hashes of compared objects) since this only applies in `--stdin` mode and is already documented in `git-diff-tree.adoc`. The edit makes the section focus solely on the common output format shared across all diff commands (`diff-tree`, `diff-index`, `diff-files`, and `diff --raw`).

The patch is a straightforward documentation cleanup that eliminates redundant information while maintaining accuracy. As noted in the thread context, this appears to be an uncontroversial improvement caught while working on another topic. The commit message clearly explains the rationale for the change.