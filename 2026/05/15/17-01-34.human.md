This is a technical discussion about HTTP protocol efficiency in Git, continuing from a recently merged bugfix in the dumb HTTP walker code.

Jeff King (Peff) responds to Patrick Steinhardt's suggestion that packfile URIs could provide a middle ground between dumb HTTP's efficiency and smart protocol's security. Peff clarifies that while packfile URIs help with pack generation overhead, they don't eliminate the server-side computation needed to determine which objects the client requires - a key advantage of dumb HTTP for static repositories. He notes that even with bitmaps, smart protocol still requires non-trivial server resources compared to dumb HTTP's simple static serving model.

The discussion touches on security constraints around packfile URIs, with both agreeing the current empty default for `fetch.uriProtocols` is necessary to prevent redirect attacks. Peff also mentions that server-side tooling for packfile URIs may not be mature enough for widespread adoption, though he acknowledges his knowledge here may be outdated.

This exchange represents experienced contributors exploring technical tradeoffs rather than proposing concrete changes. The conversation remains speculative, with no immediate action items beyond the already-merged bugfix that started the thread. It provides useful context for anyone considering future HTTP protocol improvements in Git.