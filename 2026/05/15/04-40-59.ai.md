Performance optimization discussion regarding overflow checking implementation details.

Jeff King (Peff) responds to René Scharfe's patch that introduced Clang-specific optimization for `st_add()` using `__builtin_add_overflow()`. While generally supportive of the approach, Peff raises two technical considerations:

1. Suggests the optimization could potentially be applied more broadly under `__GNUC__` (which covers both GCC and Clang) since GCC already handles the existing implementation well, though acknowledges this doesn't materially affect the outcome.

2. Notes the duplication of the error handling code between implementations and muses about a more unified primitive that could share the `die()` message while maintaining type safety. He identifies the tension between wanting a clean macro solution and the type system challenges of an inline function approach.

The reply maintains the thread's technical depth while focusing narrowly on implementation tradeoffs rather than reopening the broader overflow checking strategy discussion. Peff's comments reflect Git's pragmatic approach to performance optimizations - accepting localized improvements while considering how they might generalize or integrate with the existing codebase. The exchange continues the pattern of experienced contributors collaborating on low-level infrastructure improvements with attention to both correctness and practical performance impacts.