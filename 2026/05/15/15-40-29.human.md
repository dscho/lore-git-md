Bugfix follow-up in the rebase --update-refs thread, where Phillip Wood provides additional technical analysis confirming that the current patch doesn't need to address the make_script_with_merges() case after all. 

Phillip explains that while make_script_with_merges() does call load_branch_decorations(), the timing of decoration loading means rebase.instructionFormat with "%d" won't affect it - decorations are loaded before any commits are formatted in this path. This differs from the --update-refs case where decorations load after commit formatting, which is why that path needed fixing.

This email resolves the outstanding question from earlier in the thread about whether the make_script_with_merges() path needed parallel changes. The analysis shows it doesn't, meaning the current v3 patch (with Phillip's Reviewed-by) can proceed as-is without requiring additional follow-up patches. The thread appears technically complete, with all identified edge cases now addressed.