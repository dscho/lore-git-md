This is a review comment in the ongoing `the_repository` removal effort, specifically addressing the migration of `core.precomposeunicode` configuration. Tian Yuchen raises a substantive architectural point about how the precompose_unicode flag should be passed through the filesystem wrapper functions.

The review suggests that rather than looking up the repository configuration in each `precompose_utf8_readdir()` call (as shown in the diff), it would be more efficient to initialize the flag once during `opendir()` and pass it through the directory handle. This would better match the POSIX wrapper's conceptual role and avoid repeated repository lookups.

The feedback is technically focused on the interaction between the filesystem wrappers and repository configuration, suggesting a more optimal way to structure the code while maintaining the same functionality. It doesn't block the patch but offers an architectural improvement that could be considered for a future iteration. The tone is constructive and the technical rationale is clearly explained.