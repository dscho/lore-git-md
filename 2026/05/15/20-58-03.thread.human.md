Here's a human-readable summary of this thread so far:

The thread begins with an RFC patch proposing to make "today" a valid special date format in Git, resolving to midnight rather than the current time. Currently, commands like `git log --since=today` produce confusing results - either showing future commits (when falling back to current time) or failing entirely (with `@{today}`). The patch introduces a new `date_today()` function that handles this case cleanly while maintaining backward compatibility for expressions like "noon today".

The implementation is small and focused, adding just 10 lines of core code plus comprehensive test cases. It fits neatly alongside Git's existing special date handlers like "now" and "never". The change appears well-motivated as a quality-of-life improvement that would make date handling more intuitive for common workflows.

As an RFC, the main open question is whether this behavioral change might impact any existing scripts that rely on the current fallback behavior. The patch seems ready for review, with the author likely seeking feedback on both the technical approach and the broader compatibility implications before proceeding with a non-RFC version.