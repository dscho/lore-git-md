This is a discussion about improving the documentation for `git diff --word-diff` behavior with whitespace changes. Phillip Wood weighs in on Vincent Lefevre's suggestion to recommend `--ignore-space-change` as a workaround for cases where word-diff shows changes between files differing only in whitespace.

Phillip expresses caution about unconditionally recommending `-w` (or even `--ignore-space-change`), noting that in practice he hasn't encountered problems when using `--word-diff` for code review. He suggests a more fundamental solution would be to improve the word-diff algorithm itself - specifically by computing a single word-diff per hunk rather than splitting at context lines, while keeping the hunk headers accurate.

The thread remains unresolved, with several perspectives now on the table: the original algorithmic documentation proposal, Vincent's practical workaround suggestion, and Phillip's idea for an algorithmic improvement. The patch author (Michael Montalbo) has not yet responded to any of these suggestions. This is a case where the documentation discussion is revealing potential areas for actual code improvement in the diff machinery.