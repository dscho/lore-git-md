This is a technical review from Jeff King (Peff) on Kristofer Karlsson's performance optimization patch that replaces a linked list with a priority queue in `limit_list()`. 

Peff identifies a potential edge case regarding commit ordering when `revs->unsorted_input` is set, though he concludes it's likely not an issue in practice because `limit_list()` isn't called when `no_walk` is set. He bases this analysis on his own experience working with similar priority queue conversions elsewhere in the revision walking code.

The review is substantive - it engages with the technical details of the change and considers interaction with other parts of the codebase. Peff confirms the overall approach looks correct while flagging (and then resolving) a subtle ordering concern. He also notes this change aligns well with his own ongoing work to convert `revs.commits` to a priority queue, suggesting the patch is part of a broader architectural direction.

This feedback validates the core approach while demonstrating careful consideration of edge cases, typical of Peff's thorough review style. The patch appears to be on solid ground technically, with the minor ordering question already resolved by the reviewer's analysis.