This thread introduces a performance optimization for Git's commit-reachability checks, specifically targeting the `tips_reachable_from_bases()` function used by ref-filter operations like `for-each-ref --merged` and `branch --contains`. The patch replaces an O(C*T) linear scan with an O(C+T) hash lookup using Git's decoration mechanism, yielding significant speedups - up to 4x faster in benchmarks on repositories with 10,000 refs.

The optimization is well-motivated by real-world performance data and takes a minimal approach, adding just 9 lines while removing 6. The author acknowledges some implementation rough edges (pointer casting and scope indentation) that could be cleaned up in follow-ups, but the core optimization itself is straightforward and leverages existing Git infrastructure. This appears to be a low-risk, high-reward change for a clear bottleneck in ref filtering operations.