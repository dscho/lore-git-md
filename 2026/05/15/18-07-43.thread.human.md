This thread discusses a performance optimization for `tips_reachable_from_bases()` in commit-reach.c, which speeds up ref-filter operations like `for-each-ref --merged`. The original implementation had O(C*T) complexity, but the new version using object flags achieves O(1) flag checks with 4x-6x speedups in large repositories.

Kristofer Karlsson authored the series, with Jeff King suggesting the object-flag approach (replacing an earlier decoration hash method) and Derrick Stolee requesting regression tests. The current v3 addresses all feedback, including new test cases that verify correct handling when multiple refs point to the same commit - a scenario that exposed problems in the initial implementation. The tests cover reachable/unreachable duplicates and edge cases at minimum generation numbers.

All reviewers appear satisfied with the solution, which provides both performance gains and robust test coverage. The optimization is particularly valuable for operations checking many refs and connects to broader efforts improving ref handling efficiency. With no outstanding issues, the series now awaits maintainer approval for merging.