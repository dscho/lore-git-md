This is a technical follow-up in the rebase --update-refs bugfix thread, where Phillip Wood provides additional analysis of the decoration handling behavior. He clarifies that the make_script_with_merges() path doesn't actually need the same DECORATION_REF_LOCAL filtering as the main fix, because the branch decorations are loaded before any commit formatting occurs in that code path. This means the earlier concern about parallel code paths was overly cautious - the current patch series is complete as-is for handling the core issue with rebase.instructionFormat and %d decorations.

Phillip's analysis confirms that the problematic interaction only occurs in the --update-refs path where decorations are loaded after commit formatting begins. His message effectively closes the remaining question about whether the make_script_with_merges() path needed parallel changes, allowing the current patch series to proceed without requiring additional modifications. This appears to be the final technical clarification needed before the maintainer can merge the fix.