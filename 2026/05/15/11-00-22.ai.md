This is a review comment on a git-gui patch that refactors repository selection code into a procedure. The reviewer (Aina Boot) points out that the new `pick_repo` proc creates variables locally without declaring them as global, which would prevent them from being accessed outside the proc. This is a substantive review that identifies a real scoping issue in the code.

The patch is part of a larger series (position 4/11) refactoring git-gui's repository handling, though without seeing the cover letter we can't say what the overall goal is. The review focuses narrowly on the technical correctness of the variable scoping in this specific change.