This is a brief follow-up in the already-concluded thread about René Scharfe's `strbuf_add_uint()` optimization series. Jeff King (Peff) is responding to René's earlier empirical test comparing `unsigned_add_overflows()` with `__builtin_add_overflow()` in `strbuf_grow()`.

René had found that while x86-64 compilers generate identical code for both approaches, Clang on ARM64 fails to optimize away a redundant comparison with the current implementation. He observed a 2% speedup on Apple M1 when using the intrinsic, though this was just an informal observation rather than a proposed patch.

Peff's reply acknowledges the findings, noting he had assumed there might be optimization potential here but that compilers are generally smarter than expected. The exchange is purely conversational - the `strbuf_add_uint()` series itself remains complete and ready for maintainer pickup, with no outstanding action items. This is just some friendly technical banter about adjacent optimization possibilities.