This is a maintainer follow-up in the thread about fixing the `submodule.fetchJobs` configuration bug. Junio C Hamano weighs in with technical context about why the bug went undetected and suggests test style improvements.

The core technical explanation notes that the `config_from_gitmodules()` function's `void*` parameter type masked the pointer indirection bug at both compile and runtime, since the incorrect address write didn't immediately cause crashes or warnings. This insight helps explain why the issue persisted despite Git's test coverage.

Junio also provides two suggested rewrites for the test case to follow modern Git test conventions:
1. Using `test_grep` instead of raw `grep` for better failure output
2. Either properly formatting subshell parentheses on their own lines, or
3. Avoiding the subshell entirely with `git -C` for cleaner structure

These suggestions are stylistic rather than substantive changes to the patch's functionality. The thread shows consensus on the technical solution, with this being final polish before potential merging. Junio's commentary provides useful background about the type system interaction that allowed the bug to slip through, which could inform similar future static analysis efforts.