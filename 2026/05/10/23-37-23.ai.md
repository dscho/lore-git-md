Bugfix follow-up in the rebase --update-refs thread, where Junio C Hamano confirms the approach to ignore non-branch refs (like local tags) is correct after Phillip Wood's explanation. The existing `load_branch_decorations()` call already filters to branch refs, making the current patch's behavior intentional rather than a regression. 

Junio notes that even prior versions would have failed when trying to update HEAD or tags via the rebase sequence, so the limitation to branch refs is a sensible hardening of the behavior. This aligns with the thread's earlier conclusion that the fix correctly handles the core case while identifying a related issue in `make_script_with_merges()` that needs similar filtering.

The exchange shows consensus forming around the patch's approach, with Junio's confirmation as maintainer suggesting this is ready for inclusion. The parallel issue in `make_script_with_merges()` remains open but doesn't block the current fix. This moves the thread toward resolution while maintaining focus on precise ref handling during rebase operations.