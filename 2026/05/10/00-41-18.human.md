This is the final polish for the `--max-count-oldest` feature, which provides a more user-friendly way to show the oldest N commits in a revision walk (previously requiring complex shell pipelines). The patch series has gone through multiple iterations and is now implementation-complete with all substantive technical feedback addressed.

Mirko Faina responds to Jean-Noël Avila's documentation and error message suggestions from the previous review round. The changes requested are minor but important for consistency and completeness:

1. The documentation will be rewritten to make `--max-count-oldest` self-contained rather than defining it relative to `--max-count`, following Git's style for option descriptions.

2. Error messages will be standardized using `die_for_incompatible_opt2()` for better consistency and translation support, fixing a typo ("con't" → "can't") in the process.

3. Additional test coverage will be added to verify error cases for invalid option combinations.

The thread shows the feature is effectively ready for merging, with this being the final round of review focused on documentation quality and error handling polish rather than any functional changes. All major technical concerns about the implementation (including performance characteristics, edge case handling, and integration with other revision walk features) were resolved in earlier iterations.