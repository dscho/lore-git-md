Bugfix follow-up in the rebase --update-refs thread, addressing maintainer concern about local tag handling. Phillip Wood confirms the current patch is correct for its context (sequencer.c's todo_list_add_update_ref_commands()) since that function already filters to branches via load_branch_decorations(). However, he identifies a related issue in make_script_with_merges() where the same decoration filtering should be applied when rebase.instructionFormat includes "%d".

Phillip provides a follow-up patch snippet that would extend the same DECORATION_REF_LOCAL filtering to make_script_with_merges(), though he suggests this could be handled separately from the current fix. The response clarifies that the original patch doesn't actually regress local tag handling in the main case, while acknowledging there's room for improvement in the parallel code path.

This moves the discussion forward by:
1. Confirming the current solution works as intended in its primary context
2. Identifying a related but distinct code path that needs the same treatment
3. Proposing a clean follow-up while keeping the original fix valid
4. Maintaining the thread's focus on precise ref filtering during rebase operations

The exchange demonstrates careful consideration of both the immediate issue and related code paths, with Phillip Wood (as a sequencer expert) providing authoritative clarification about the existing branch decoration filtering. The thread appears ready for the original author to either proceed with the current approach or incorporate the additional make_script_with_merges() fix.