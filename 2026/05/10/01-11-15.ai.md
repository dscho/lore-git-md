Bugfix follow-up addressing maintainer feedback on the rebase --update-refs edge case. Junio Hamano raises a concern that the current fix (filtering to DECORATION_REF_LOCAL) might be too broad, as it would also skip local tags that users might want moved during rebase. He suggests the original problem (HEAD being incorrectly processed) could be solved more narrowly by combining the existing HEAD check with the new condition.

The maintainer provides an alternative patch that merges the two exclusion conditions (HEAD and non-local refs) into a single if-statement, maintaining the same control flow but with clearer intent. This version would still prevent HEAD updates while allowing local tags to be processed. The feedback indicates the fix needs refinement to avoid regressing valid use cases with local tags.

This is a substantive review from the maintainer that identifies a potential unintended consequence of the original solution. The thread appears to be converging toward a more precise fix that maintains all desired functionality while still solving the HEAD edge case. The ball is now back in the original author's court to either adjust the approach or justify why excluding all non-DECORATION_REF_LOCAL is the correct behavior.