This is a maintainer confirmation in the rebase --update-refs bugfix thread. Junio C Hamano acknowledges Phillip Wood's explanation that the current patch correctly handles local tags by relying on the existing `load_branch_decorations()` filtering in `sequencer.c`. 

Junio recognizes that while older Git versions might have included tag updates in the rebase instruction sequence when using `%d` in `rebase.instructionFormat`, those sequences would have failed anyway due to the HEAD issue. He concludes that since using local tags as markers was never a supported workflow, the patch's approach of limiting updates to local branch refs is the right solution.

The exchange confirms the technical correctness and resolves Junio's initial concern about potential regression. This clears the way for the patch to proceed, with the understanding that the parallel issue in `make_script_with_merges()` (identified earlier in the thread) can be addressed separately. The discussion demonstrates careful consideration of backward compatibility and supported workflows.