Junio C Hamano raises a substantive concern about a rebase bugfix that was previously thought to be ready for inclusion. The patch aimed to prevent `--update-refs` from processing non-branch refs like HEAD when using `rebase.instructionFormat` with `%d`, but Junio points out this could unintentionally block legitimate use cases with local tags.

The core issue is that the current fix's use of `DECORATION_REF_LOCAL` is too broad - while it correctly filters out HEAD, it would prevent moving local tags that point into the rebased topic branch, which is a valid operation. Junio suggests an alternative implementation that would combine the HEAD check with the non-branch filtering in a single conditional, while noting the exact filtering criteria may need adjustment.

This is a significant review from the maintainer that identifies a potential regression in behavior that wasn't caught by the test suite. The feedback suggests the original solution needs refinement to properly distinguish between truly invalid refs (like HEAD) and valid local tags that should still be processed by `--update-refs`. The thread now requires the original author to either justify the current approach or revise the patch to handle local tags appropriately.