This is a technical discussion about refining a bugfix for `git rebase --update-refs` when used with `rebase.instructionFormat`. The thread involves maintainer Junio Hamano and sequencer expert Phillip Wood.

The core issue is that when `rebase.instructionFormat` includes `%d` (showing ref decorations), the rebase machinery could incorrectly generate `update-ref` instructions for non-branch refs like HEAD. The current fix filters decorations to `DECORATION_REF_LOCAL` types, but Junio raised concerns this might be too broad as it would skip local tags that users might want moved during rebase.

Phillip Wood now clarifies that `sequencer.c:todo_list_add_update_ref_commands()` already calls `load_branch_decorations()`, so the patch is correct in not updating tags. However, he identifies a related issue in `make_script_with_merges()` that should also filter decorations, and provides a follow-up diff to address this. He suggests the current patch is still a strict improvement that can land while the additional fix is handled separately.

The discussion shows careful attention to edge cases in the sequencer's ref handling, with Phillip providing authoritative technical clarification about the existing behavior. The patch appears ready to proceed while leaving room for future refinement of related code paths.