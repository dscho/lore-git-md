Thread identity: Bug report about a warning in `git-svn` due to duplicate option specification in Perl code, triggered by Getopt::Long 2.55 changes.

Core problem: In `git-svn.perl`, options `--id` and `-i` both map to same variable `$Git::SVN::default_ref_id`. While functionally equivalent, Getopt::Long 2.55 now warns about case-sensitive duplicates when `ignore_case` is on, and will make this fatal in future releases.

Current status: Issue reported but not yet fixed. Currently produces warning on systems with Getopt::Long 2.55+ (e.g. Fedora 43 aarch64 with git 2.54.0). Needs patch to remove one of the duplicate option specs while preserving functionality.

Technical details: Affects `git-svn.perl` option parsing. Problem occurs when `ignore_case` is enabled in Getopt::Long configuration. Reporter provided links to both the problematic code and Getopt::Long changelog explaining the behavior change.

Open questions: None yet - straightforward fix needed before Getopt::Long change becomes fatal. May need verification on different Perl/Getopt::Long versions.

Participant positions: Original reporter identified issue clearly with reproduction details and relevant links. No other participants yet.

Related work: Getopt::Long 2.55 changelog documents this behavior change regarding case-sensitive duplicate options.