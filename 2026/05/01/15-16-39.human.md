This is the second patch in Phillip Wood's two-part bugfix series improving `git status` display during interactive rebase. Building on the refactored sequencer parsing from patch 1, this change fixes two specific issues in how the rebase todo list appears in status output.

The patch corrects the display of object IDs in "fixup -C" and "merge -C" commands (which were shown in full rather than abbreviated) and preserves refnames in commands like "reset main" (which were incorrectly replaced with object IDs). The implementation carefully handles edge cases - only abbreviating when the argument starts with an abbreviated OID to avoid modifying branch names that happen to be all hex digits.

Test coverage in t7512 is expanded to verify the fixes while maintaining existing cases. The changes demonstrate Phillip Wood's characteristic attention to detail in the sequencer subsystem, with thorough edge case handling and clear commit messages. All substantive technical questions from reviewers (Elijah Newren, Tian Yuchen) have been addressed in this v2 iteration.

The patch touches wt-status.c (display logic) and test updates, building on the shared parsing infrastructure added in patch 1. With no outstanding review concerns, this series appears ready for merging.