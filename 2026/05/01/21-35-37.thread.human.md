Here’s the human-mode summary for a developer catching up on the thread:

Harald Nordgren’s `git branch --delete-merged` series has reached its final form in v23 and is now ready to merge. This feature adds a safe, automated way to clean up local branches that have been merged into their upstream, with robust controls to prevent accidental data loss. The command supports repeatable `--delete-merged` arguments (e.g., `--delete-merged origin/main --delete-merged origin/next`) to widen the upstream match, optional positional patterns (like `topic-*`) to limit deletion scope, and a `--dry-run` preview mode that shows what would be deleted without making changes. Safety mechanisms include per-branch opt-out via `branch.<name>.deleteMerged=false`, protection for checked-out branches, and order-independent stacked-branch protection—ensuring a branch isn’t deleted if it’s used as an upstream for an unmerged branch, regardless of the order branches are processed.

The series has been through extensive review, with key feedback from Phillip Wood and Junio C Hamano shaping its design. Phillip proposed the refined stacked-branch protection logic (now implemented as a single-pass graph traversal) and helped tighten the API, while Junio’s reviews focused on correctness and consistency. The v23 update addressed Junio’s final mechanical concerns in patch 1/7 (a type mismatch and style nit), leaving the rest of the series unchanged from v22, which he had already deemed complete. The implementation is well-tested, with coverage for edge cases like self-referential upstreams and missing remotes, and the flags-based API refactoring in patches 2-4/7 preserves backward compatibility. The only planned follow-up is extending stacked-branch protection to `git branch -d` for consistency, which Harald will tackle after the series lands. The series is now unblocked and will appear in an upcoming "What’s cooking" report for inclusion in `next`.