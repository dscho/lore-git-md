This thread is the final chapter of Harald Nordgren’s `git branch --delete-merged` series, now at **v18 and ready for merging**. The series introduces a new `--delete-merged` subcommand that automates safe local branch cleanup with fine-grained control and robust safety mechanisms. The latest patch adds `--dry-run` support, allowing users to preview deletions before committing to them—completing the series’ safety-focused design.

Key participants include Harald Nordgren, who implemented the entire series, and Phillip Wood, whose high-weight feedback shaped critical aspects of the design, including the **refined stacked-branch protection** (abort-and-clear) and the flags-based API refactoring. Junio C Hamano has endorsed the final design, signaling the series is ready for merging. The implementation is now feature-complete, with all planned functionality—including per-branch opt-out via `branch.<name>.deleteMerged=false`, the `--forked` filter for precise branch selection, and comprehensive safety checks—fully realized and tested.

The series addresses a long-standing gap in Git’s branch management tooling by enabling safe, automated cleanup of merged branches while preserving those still in use. The `--dry-run` feature added in this patch provides a zero-risk preview of deletions, ensuring users can verify the command’s behavior before making changes. With all design questions resolved and thorough test coverage, the series is poised for inclusion in the next Git release.