Here’s the human-mode summary for a developer catching up after being away:

Harald Nordgren’s `git branch --delete-merged` series has reached its final form in v17, offering safe automated cleanup of local branches with robust filtering and safety controls. The command deletes branches merged into their upstream while preserving those used as upstreams for other branches—now with a clear "abort-and-notify" approach (e.g., "Cannot delete 'feature1' because 'feature2' depends on it") to avoid stale rebase commits. Key features include per-branch opt-out via `branch.<name>.deleteMerged=false`, a `--dry-run` preview, and the `--forked` filter for precise branch selection.

The series is now fully implemented, with all major design questions resolved. Phillip Wood and Junio C Hamano endorsed the simplified stacked-branch protection (abort-and-notify) over complex retargeting logic, clearing the last hurdle for review. The API has been refactored to use flags for consistency, and tests cover edge cases like chains of upstream dependencies. The only remaining work is final polish—documentation updates and test coverage for the new error messages—before the series can be merged. The design balances safety and simplicity, deferring retargeting to a future optional flag.