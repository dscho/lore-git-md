Here’s the human-mode summary for a developer catching up on the thread:

Harald Nordgren’s `git branch --delete-merged` series, now at v20, is ready for merging after resolving the last correctness issue—a long-standing order dependency in its stacked-branch protection logic. The command safely automates local branch cleanup with precise filtering (including per-branch opt-out via `branch.<name>.deleteMerged=false`), `--dry-run` previews, and protection for branches used as upstreams for unmerged work. The v20 refactor, proposed by Phillip Wood and endorsed by Junio Hamano, ensures consistent behavior by collecting all protected branches in a single pass before any deletions occur, eliminating edge cases where iteration order could affect results.

Key participants have signed off on the design: Phillip Wood provided high-weight feedback on the stacked-branch protection and API consistency, while Junio Hamano confirmed the refactor addresses his earlier blocking concern. The only agreed follow-up is extending this protection to `git branch -d` for consistency, but that won’t block the current series. Tests now cover all edge cases, including chains of branch dependencies and self-referential upstreams. The series touches `builtin/branch.c`, `ref-filter.c`, and documentation, with no known issues remaining.