Here’s the human-mode summary for a developer catching up on the thread:

---

This thread is the fifteenth iteration of Harald Nordgren’s `git branch --delete-merged` series, which adds a safe, automated way to clean up local branches that have been merged into their upstream. The feature includes filtering options (like `--forked`), per-branch opt-out via config, and a `--dry-run` preview mode. After extensive iteration, the implementation is complete, and all test failures have been resolved, but one final API consistency issue is blocking progress.

The core logic is in place: branches are checked for reachability, push divergence, and opt-out config, and the `--dry-run` mode mirrors the actual deletion logic. However, Phillip Wood’s review identified two API inconsistencies, of which one remains unresolved. Harald has already addressed the first (unpacking all flags at the start of `delete_branches()`), but the second—replacing the boolean `dry_run` parameter in `delete_merged_branches()` with the flags-based API—is still open. Until this is fixed, the test suite for `--dry-run` cannot be integrated, and the series cannot move forward. Phillip’s feedback has been the primary driver of these changes, and his reviews have emphasized API uniformity as a critical requirement.

Harald is also midway through a test suite overhaul to improve realism and reduce redundancy, but this work is on hold until the API issue is resolved. Junio and Johannes Sixt have weighed in earlier in the thread, but the current focus is purely mechanical: the series is otherwise ready, and the remaining task is a straightforward refactor. Once the API is aligned, the tests can be finalized, and the series should be ready for merging.

---