This thread is about the final version (v20) of Harald Nordgren’s patch series adding `git branch --delete-merged`, a new command for safe automated local branch cleanup. The series is now complete and unblocked for merging after resolving the last correctness issue—an order dependency in the stacked-branch protection logic—via a structural refactor. Key participants are Harald Nordgren (author), Phillip Wood (primary reviewer), and Junio C Hamano (maintainer).

The feature provides precise control over branch deletion through repeatable upstream selectors (e.g., `--delete-merged origin/main --delete-merged origin/next`), per-branch opt-out via `branch.<name>.deleteMerged=false`, `--dry-run` preview, and order-independent stacked-branch protection (which aborts deletion if a branch is used as an upstream for an unmerged branch). All planned functionality is implemented, including the `--forked` filter foundation, flags-based API refactoring, and comprehensive test coverage. The only agreed follow-up is extending stacked-branch protection to `git branch -d` for consistency, which will happen post-merge.

Junio’s earlier blocking concern about the order dependency has been addressed, and Phillip Wood’s feedback—including the abort-and-clear design for stacked-branch protection—has been fully incorporated. The series is now ready for merging, with no known issues or open technical questions.