Harald Nordgren's `git branch --prune-merged` series has reached its tenth iteration, now implementing a complete solution for safe automated branch cleanup with dry-run support. The feature allows pruning local branches that have been merged into their upstream (either another local branch or a remote-tracking ref) while protecting checked-out branches, default branches, and those with unmerged changes. 

Junio C Hamano has reviewed the foundational `--forked` patch in v10, finding the core branch identification logic sound but requesting several refinements. He suggests splitting out unrelated changes to deletion functions, optimizing the glob pattern matching, and potentially improving warning messages. While these are implementation details rather than design concerns, they'll need addressing before the series can proceed. 

The most significant outstanding issue remains Junio's earlier feedback about removing the automatic fetch behavior for remote-tracking branches, which Harald will need to revise to match Git's existing semantics. Phillip Wood's suggested dry-run mode and local branch support have been successfully added in previous iterations. With these final adjustments, the long-running series appears close to completion.