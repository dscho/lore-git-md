Harald Nordgren’s `git branch --delete-merged` series is now in its final stages, with all planned features implemented and only minor refinements needed before merging. The series introduces a new `--delete-merged` subcommand that safely automates local branch cleanup, addressing a long-standing pain point for users who work with many topic branches. Key features include refined stacked-branch protection (abort-and-clear), per-branch opt-out via `branch.<name>.deleteMerged=false`, a `--dry-run` preview mode, and the foundational `--forked` filter for precise branch selection.

The core implementation is complete, with all safety mechanisms—checked-out branches, missing upstreams, push divergence, per-branch opt-out, and stacked-branch protection—fully operational and tested. Phillip Wood’s latest review identified two actionable items: a mechanical fix to the flags-based API in patches 2–4 (replacing local booleans with direct flag checks to avoid maintenance risks) and test improvements in patch 5/7 (comments, helper functions, and moving a premature `--dry-run` test case to patch 7/7). These are straightforward changes that do not affect the series’ functionality or design. Junio C Hamano has already endorsed the refined stacked-branch protection approach, and the series is otherwise ready for merging.

Harald has addressed all prior feedback, including Phillip’s style suggestions and the shift from an all-or-nothing stacked-branch protection model to the current abort-and-clear logic. The remaining work is minimal and should not delay the series’ integration once the fixes are applied. This is a well-scoped, user-focused addition that complements Git’s existing branch management tooling while introducing new safety controls for automated cleanup.