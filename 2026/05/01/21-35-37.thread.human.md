Here’s the human-mode summary for a developer catching up on the thread:

Harald Nordgren’s `git branch --delete-merged` series is now complete and ready for merging, with all design questions resolved and only minor mechanical fixes remaining. The feature provides safe, automated local branch cleanup with fine-grained control, including stacked-branch protection (abort-and-clear), per-branch opt-out via `branch.<name>.deleteMerged=false`, `--dry-run` preview, and the `--forked` filter for precise branch selection.

The latest update focuses on test refinements for patch 5/7, addressing Phillip Wood’s feedback to improve clarity. The artificial `mainline` branch was replaced with `main`, and `branch.main.pushRemote` is now explicitly configured to prevent `main` from being misclassified as a forked branch. This change also revealed a subtle edge case: when `remote.pushDefault` is set, branches without an explicit `pushRemote` may be incorrectly treated as forked unless manually exempted. Harald acknowledges this as a known limitation but defers further discussion, as the core logic remains unchanged. The test suite now uses clearer branch names and removes redundant setup steps, making the tests more readable without altering functionality.

The series has received thorough review from Phillip Wood, whose feedback shaped key aspects of the design, including the flags-based API and stacked-branch protection. Junio C Hamano has endorsed the refined protection approach, and the only remaining task is a mechanical cleanup of the flags API in patches 2-4/7, which Harald has agreed to address before merging. All substantive issues—such as per-branch opt-out, `--dry-run` functionality, and `<branch>` argument support—are now resolved, and the series is poised for inclusion.