Here’s the human-mode summary for a developer catching up on the thread:

---

This thread is about the fifteenth iteration of Harald Nordgren’s `git branch --delete-merged` series, which adds a safe, automated way to clean up local branches that have been merged. The command includes filtering options (like `--forked`), per-branch opt-out via config, and a `--dry-run` preview mode. After extensive iteration, the implementation is complete, but two key issues remain unresolved.

The first is an API consistency blocker: Phillip Wood pointed out that `delete_merged_branches()` still uses a boolean `dry_run` parameter instead of the flags-based API used elsewhere in the series. The second is a new safety proposal from Phillip to protect branches that serve as upstreams for other branches not slated for deletion—this could require another behavioral change. Test integration, particularly for `--dry-run`, is blocked until these are resolved.

Harald has addressed all prior feedback, including a test suite overhaul with better fixtures and assertions, and has agreed to wait for Phillip’s full review before sending another version. Phillip’s latest email confirms he’s done with his current round of feedback, and the thread is now in a holding pattern while Harald works on the remaining issues. No new patches will be posted until the API and stacked-branch questions are settled.

---