Here’s the human-mode summary for a developer catching up on the thread:

Harald Nordgren’s `git branch --delete-merged` series is now complete and ready for merging, with all design questions resolved and implementation finalized in v18. The series introduces a safe, automated way to clean up local branches merged into their upstreams, with fine-grained control via filters, safety checks, and per-branch opt-out. The core command deletes merged branches while preserving those used as upstreams for unmerged work, and it now supports a `<branch>` argument to limit deletion candidates—addressing a usability gap Phillip Wood flagged in earlier reviews.

The latest update to patch 5/7 adds the `<branch>` argument (reusing the `--forked` filter’s machinery) and overhauls the test suite for clarity. Tests now use descriptive helper functions like `create_branch_with_upstream`, replace manual config checks with `git config --get-regexp`, and remove redundant setup steps. Phillip’s feedback also prompted cleanup of edge-case tests and improved branch naming. The series includes other key features: a `--dry-run` preview, per-branch opt-out via `branch.<name>.deleteMerged=false`, and refined stacked-branch protection that aborts deletion if a branch is needed as an upstream but clears stale configs for kept branches.

All substantive issues are resolved. The remaining work is mechanical: Harald has agreed to fix a minor flags maintenance issue in patches 2–4/7 (replacing local booleans with direct flag checks) and apply final test tweaks to patch 5/7. Junio endorsed the stacked-branch protection design, and Phillip’s latest review raised no functional concerns—only test improvements that have since been addressed. The series is now implementation-complete and awaiting Junio’s queue.