Harald Nordgren’s `git branch --delete-merged` series is now complete and ready for merging. The thread began as a proposal to add safe, automated local branch cleanup with precise filtering and robust safety controls. Harald implemented the feature across 24 iterations, addressing all technical concerns raised by Junio C Hamano and Phillip Wood.

The final version includes a `--delete-merged` subcommand that deletes local branches merged into their upstream while preserving branches used as upstreams for unmerged work. Key features are order-independent stacked-branch protection (which now clears stale upstream configs for kept branches), per-branch opt-out via `branch.<name>.deleteMerged=false`, and a `--dry-run` preview mode that avoids modifying refs or config. The command supports repeatable `--delete-merged` arguments to widen the upstream match and optional positional patterns to limit deletion scope. Junio has confirmed the series is ready for merging, and Phillip Wood’s two blocking technical concerns—push-detection logic and stacked-branch protection—have been resolved in the latest update.

The implementation touches `builtin/branch.c`, `ref-filter.c`, and the test suite, with documentation updates for the new config key and command options. All safety checks (checked-out branches, missing upstreams, push divergence, opt-out) are in place, and the test suite covers edge cases like self-referential upstreams and chains of dependencies. The only remaining work is a follow-up to extend stacked-branch protection to `git branch -d` for consistency, which Harald has agreed to implement after the series lands.