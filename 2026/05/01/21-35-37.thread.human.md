Harald Nordgren’s `git branch --delete-merged` series is now in its final v18 revision, implementation-complete and ready for merging once a few mechanical cleanups are applied. The series introduces safe automated local branch cleanup with precise filtering and multiple safety controls, including per-branch opt-out via `branch.<name>.deleteMerged=false`, `--dry-run` preview, and refined stacked-branch protection that aborts deletion if a branch is used as an upstream for an unmerged branch while clearing stale upstream configs for kept branches.

The core `--delete-merged` command (patch 5/7) is fully implemented, using a new `--forked` filter (patch 1/7) to select branches by exact refnames, remote shorthand, or shell globs. The deletion API has been refactored across patches 2-4/7 to use flags instead of boolean arguments, improving extensibility. Phillip Wood’s review identified a maintenance risk in these patches—the `flags` variable is split into local booleans, creating two sources of truth—and Harald has agreed to replace all local boolean uses with direct flag checks before merging. Phillip also suggested test improvements for patch 5/7 (comments, helper functions, redundant command removal) and noted a premature `--dry-run` test case that will be moved to patch 7/7. Junio has endorsed the refined stacked-branch protection design, and all other design questions are resolved.

The series is now waiting on these final mechanical fixes: the flags maintenance issue in patches 2-4/7, test improvements to patch 5/7, and moving the `--dry-run` test case. Once these are addressed, the series will be ready for merging. The implementation is robust, with comprehensive test coverage for all safety mechanisms, opt-out behavior, and edge cases like self-referential upstreams.