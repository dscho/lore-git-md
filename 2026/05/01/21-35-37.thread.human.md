Here’s the human-mode narrative summary for a developer catching up on the thread:

---

Harald Nordgren’s `git branch --delete-merged` series has landed in `next` and is ready for graduation to `master`. This 25th iteration of the patch series introduces a safe, automated way to clean up local branches merged into specified upstreams, with robust filtering and safety controls. The core command, `git branch --delete-merged <upstream>`, deletes local branches merged into `<upstream>` while preserving those used as upstreams for unmerged branches (via order-independent stacked-branch protection), checked-out branches, or branches opted out via `branch.<name>.deleteMerged=false`. The series also adds a `--dry-run` preview mode and a foundational `--forked` filter for branch listing.

The final version (v25) wraps long test command lines in patch 1/7 for readability but makes no functional changes since v24, which resolved the last two technical blockers: a push-detection logic flaw (fixed by correctly applying push/fetch refspecs) and a stacked-branch protection test discrepancy (simplified to clear upstream configs for kept branches). Junio C Hamano and Phillip Wood have both confirmed the series is ready for merging, and Junio has queued it in `next` for integration testing. The only remaining follow-up is extending stacked-branch protection to `git branch -d` for consistency, which Harald has agreed to implement after the series lands.

Key features include repeatable `--delete-merged` arguments (e.g., `--delete-merged origin/main --delete-merged origin/next`), optional positional patterns (e.g., `topic-*`), per-branch opt-out, and a flags-based API refactoring to eliminate maintenance risks. The implementation is thoroughly tested, covering edge cases like self-referential upstreams, missing upstreams, and push divergence. The series touches `builtin/branch.c`, `ref-filter.c`, and documentation, and is expected to graduate to `master` in the current cycle.

---