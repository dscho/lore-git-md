Harald Nordgren's `git branch --delete-merged` series has reached a major milestone: it is now **merged to 'next'** and ready for integration testing, with all planned features and safety mechanisms implemented. The series introduces a new `--delete-merged` subcommand that automates safe local branch cleanup with precise control, including order-independent stacked-branch protection, per-branch opt-out via `branch.<name>.deleteMerged=false`, `--dry-run` preview, and the foundational `--forked` filter.

The journey to this point has been thorough. After 25 iterations, the series resolved two critical correctness issues in v24: a flaw in the push-detection logic that could incorrectly preserve branches, and a test discrepancy in the stacked-branch protection logic. The v25 update was purely mechanical, wrapping long test command lines for readability without changing functionality. Both Junio C Hamano and Phillip Wood have explicitly confirmed the series is ready for merging, and Junio has now queued it in 'next' for integration testing. The only remaining follow-up work—extending stacked-branch protection to `git branch -d` for consistency—will be addressed after the series lands.

Key participants have shaped the design and implementation. Harald Nordgren drove the entire effort, addressing feedback from Phillip Wood (who proposed the abort-and-clear protection approach and identified the flags maintenance issue) and Junio (who requested the protection extension to `git branch -d`). The series is now feature-complete, with a robust test suite covering edge cases like push divergence, missing upstreams, and self-referential branch dependencies. The flags-based API refactoring (patches 2-4/7) and per-branch opt-out (patch 6/7) are particularly notable for their attention to maintainability and user control. Unless integration testing uncovers unexpected issues, the series is on track to graduate to `master` in the current cycle.