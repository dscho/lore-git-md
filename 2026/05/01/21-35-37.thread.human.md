Here’s the human-readable summary for a developer catching up on the thread:

Harald Nordgren’s `git branch --delete-merged` series has reached its final form in v17, now fully implemented with all planned features and safety mechanisms. The command automates local branch cleanup while addressing long-standing workflow pain points, particularly for stacked branches. Key improvements include a `--forked` filter for precise branch selection, per-branch opt-out via `branch.<name>.deleteMerged=false`, and a `--dry-run` preview mode that shows exactly which branches would be deleted or modified.

The most significant design resolution in this version is the stacked-branch protection mechanism. Instead of aborting when a merged branch is used as an upstream for unmerged work (e.g., `feature1` merged but `feature2` depends on it), the command now clears the upstream configuration of the blocked branch. This avoids stale references without requiring complex recursive retargeting, handles edge cases like self-referential upstreams uniformly, and maintains backward compatibility. The change reflects feedback from Phillip Wood, who proposed the simpler approach, and Junio C Hamano, who raised no objections.

With all safety checks in place—including protections for checked-out branches, missing upstreams, and push divergence—the series is now feature-complete and ready for final review. The test suite covers all core functionality, including the new upstream-clearing behavior, and the `--dry-run` mode provides a zero-risk preview of deletions and config changes. The only remaining work is likely minor polish, such as additional test cases for warning messages. The series builds on familiar patterns like `fetch.prune` while introducing new safety-focused capabilities tailored to iterative topic branch workflows.