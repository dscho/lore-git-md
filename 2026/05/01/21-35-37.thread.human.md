This thread is about the final version (v15) of Harald Nordgren's `git branch --delete-merged` series, which introduces a safe, automated way to clean up local branches with robust filtering, safety checks, and a `--dry-run` preview. The implementation is complete after 15 iterations, with all test failures resolved and comprehensive safety mechanisms in place—including per-branch opt-out via config, push divergence protection, and `--forked` filtering.

The series is now blocked on a single mechanical issue: Phillip Wood has identified an API consistency problem in `delete_merged_branches()`, where a boolean `dry_run` parameter must be replaced with the flags-based API used elsewhere in the code. This change is required before test integration (particularly for `--dry-run`) can proceed. Harald has already addressed a related API issue in `delete_branches()` and is working on a test suite overhaul with shared fixtures and realistic workflows, but these improvements are also paused until the API is finalized.

Phillip’s feedback has been instrumental in shaping the series, and his review remains the key hurdle—once the API consistency issue is resolved, the series should be ready for merging. Junio and Johannes Sixt have previously weighed in on platform and CLI design, but no further technical objections are outstanding. Harald has confirmed he’ll wait for Phillip’s full review before sending another iteration, acknowledging the need to balance responsiveness with mailing-list etiquette during his paternity leave.