Here’s the human-readable summary for a developer catching up on the thread:

---

This thread is about the final stages of Harald Nordgren’s `git branch --delete-merged` series, which adds a safe, automated way to clean up local branches that have been merged. The feature includes filtering options (`--forked`), safety checks (e.g., preserving checked-out branches or those with push divergence), per-branch opt-out via config, and a `--dry-run` preview mode. After 16 iterations, the implementation is technically complete, but one design question remained unresolved: whether to protect branches that are used as upstreams for unmerged stacked branches (e.g., preserving `feature1` if `feature2` depends on it).

The debate centered on whether deleting such branches would break workflows like `git log @{u}..` or implicit `git rebase`. Phillip Wood argued for the protection, while Junio C Hamano initially considered it unnecessary. In the latest email, Junio conceded the point, agreeing that the protection is justified to avoid disrupting these workflows. This removes the last major obstacle to merging the series. The next step is likely a final implementation of the stacked-branch safety check, which Harald may address in a v17 iteration. The feature is otherwise ready for review, with all other feedback incorporated.