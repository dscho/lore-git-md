This is a security-focused bugfix for Git's HTTP proxy handling, building on the previous patch in the thread. The patch explicitly rejects proxy URLs with unsupported schemes (like "htpp://") rather than silently accepting them by stripping the scheme and using just the host portion.

The implementation refactors the proxy scheme validation into a new `set_curl_proxy_type()` helper function that checks all supported schemes (HTTP, HTTPS, and various SOCKS variants). When an unsupported scheme is encountered, Git now fails with a clear error message rather than defaulting to HTTP proxy behavior. The change maintains backward compatibility with the existing host:port format while adding a regression test to verify the new validation behavior.

The patch appears well-considered - it addresses a real security concern where typos in proxy URLs could lead to unintended behavior, and does so with clear error messaging and test coverage. The code is focused (79 lines modified) and touches only the relevant portions of `http.c` and the proxy test script. Given the security implications of proxy handling, this hardening measure seems worthwhile.