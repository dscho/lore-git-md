AI thread summary (updated):

Thread identity: Final v24 of Harald Nordgren's `git branch --delete-merged` series, **implementation-complete and ready for merging**. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls, including **order-independent stacked-branch protection**, per-branch opt-out, `--dry-run` preview, and the foundational `--forked` filter. This version **resolves the two blocking correctness issues** identified in v23:
1. **Corrected push-detection logic** in `branch_pushes_to_upstream()` to accurately determine whether pushing a branch would update its upstream, regardless of `remote.pushDefault` configuration.
2. **Simplified stacked-branch protection** to clear upstream configs for kept (merged) branches whose own upstream is being deleted, aligning the implementation with the design goal and resolving the v23 test discrepancy.

The rest of the series is **identical to v23**, which Junio C Hamano confirmed has no outstanding issues and is ready for merging. **Junio and Phillip Wood have both explicitly confirmed the series is ready for merging after final review of the v24 fixes.**

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) **Repeatable upstream selector** for branch selection (fully implemented), supporting exact refnames, remote shorthand, shell-style globs, and **positional branch patterns** to limit deletion scope. The `--delete-merged` option can be specified multiple times (e.g., `--delete-merged origin/main --delete-merged origin/next`) to widen the upstream match.
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`).
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, **order-independent stacked-branch protection**).
4) `--dry-run` preview capability (fully implemented, **avoids modifying config during dry runs**).

Current status:
1) **Series ready for merging**: All planned features, safety mechanisms, and correctness fixes are implemented. The v24 changes address the two blocking issues from v23:
   - **Push-detection logic**: The `branch_pushes_to_upstream()` function now correctly applies the *push* refspec to the local branch, applies the *fetch* refspec in reverse to the result, and compares that to the upstream branch. This ensures branches like `main` tracking `origin/main` are preserved even when `remote.pushDefault` names a fork, fixing the v23 flaw.
   - **Stacked-branch protection**: The logic now protects only the *direct* upstream of a surviving branch, deleting deeper merged bases and clearing their stale upstream configurations. This resolves the v23 test discrepancy by aligning the implementation with the design goal. The change removes the graph traversal and updates the test to expect cleared upstream configs for kept (merged) branches.
   **Junio C Hamano and Phillip Wood have both confirmed the series is ready for merging pending final review of the v24 fixes.**
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream (matching one of the `--delete-merged` arguments) while preserving branches used as upstreams for unmerged branches via **order-independent stacked-branch protection**. The command now **fails to delete a branch if it is used as an upstream for an unmerged branch**, and **clears the upstream config for kept (merged) branches whose own upstream is being deleted**. The command supports repeatable `--delete-merged` arguments to widen the upstream match and optional positional patterns (e.g., `topic-*`) to limit deletion scope. Junio C Hamano has **confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`)** is correct and sound.
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values. **The maintenance risk in patches 2-4/7 (two sources of truth for flags) has been resolved by eliminating local boolean caching and ensuring flags remain the single source of truth.**
   - **Patch 2/7 (v24 2/7)**: Converts `force` and `quiet` parameters to flags (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`), with direct flag testing at each use site to prevent stale cached values. The patch is purely mechanical, introduces no behavioral changes, and is **low-risk and well-tested**.
   - **Patch 3/7 (v24 3/7)**: Adds `DELETE_BRANCH_SKIP_UNMERGED` to enable bulk callers to skip unmerged branches silently, preserving existing behavior for all current callers.
   - **Patch 4/7 (v24 4/7)**: Adds `DELETE_BRANCH_NO_HEAD_FALLBACK` to prevent falling back to `HEAD` when no branches are specified, ensuring `--delete-merged` only deletes branches merged into their upstream.
   - Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` for `--dry-run` support, completing the flags-based API.
4) **Per-branch opt-out implemented and finalized**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`. The opt-out is checked during candidate filtering in `delete_merged_branches()` and produces a non-quiet skip message. The implementation is complete, with documentation added to `branch.adoc` and `git-branch.adoc`, and tests covering opt-out behavior, skip messages, explicit deletion, and branch state verification via `git for-each-ref`. **Phillip Wood's surface-level review suggested a mechanical test improvement (`for-each-ref` verification via `check_branches`), which has been applied.**
5) **Stacked-branch protection design and implementation**: **Finalized as order-independent abort-and-clear via single-pass logic**, with the v24 update simplifying the implementation to protect only the *direct* upstream of a surviving branch. The logic handles chains of dependencies (e.g., `b1→b2→b3`) by keeping only the branches needed by unmerged branches and clearing upstream configs for kept (merged) branches whose own upstream is being deleted. The `protected_branch_names` strset is indexed by **branch names** (e.g., `main`). **Junio C Hamano has requested that this protection be extended to `git branch -d` for consistency, and Harald Nordgren has agreed to implement this as a follow-up after the series lands.**
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`). The filter supports exact refnames, remote shorthand, shell-style globs, and **positional branch patterns for `--delete-merged`**. The v23 implementation **fixed mechanical correctness issues**, and the v24 changes do not affect this component. **This patch (v24 1/7) is purely additive and serves as the foundation for the `--delete-merged` subcommand, with no behavioral changes to existing functionality.**
7) **`--dry-run` preview implemented and finalized**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch without modifying refs or config. The output matches the real deletion/protection output exactly, and `--dry-run` is rejected without `--delete-merged`. The `--dry-run --quiet` combination is rejected as nonsensical. **The implementation avoids modifying config during dry runs** by skipping config updates in `spare_stacked_bases()`.
8) **Test suite complete**: Expanded to cover order-independent stacked-branch protection, per-branch opt-out, `--dry-run` functionality, repeatable `--delete-merged` arguments, positional branch patterns, and edge cases like self-referential upstreams and missing upstreams. The v24 updates add tests to verify:
   - The corrected push-detection logic in `branch_pushes_to_upstream()`.
   - The simplified stacked-branch protection logic, including the clearing of upstream configs for kept (merged) branches.
   The test suite now explicitly configures `branch.main.pushRemote` to prevent false positives in `--forked` filtering and verifies that the stacked-branch protection logic produces consistent results regardless of ref iteration order.
9) Resolved issues:
    - **Push-detection logic**: Fixed in v24 by correctly applying the *push* and *fetch* refspecs to determine whether pushing a branch would update its upstream.
    - **Stacked-branch protection test discrepancy**: Resolved in v24 by simplifying the logic to clear upstream configs for kept (merged) branches whose own upstream is being deleted, and updating the test to match this behavior.
    - **Stacked-branch protection design**: Finalized as order-independent abort-and-clear via single-pass logic (v20), with the v24 update simplifying the implementation.
    - **API consistency**: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`, with local boolean caching eliminated to ensure flags remain the single source of truth.
    - **`--forked` filter edge case**: Fixed in v19 by protecting branches if pushing them by name would update their upstream, regardless of `remote.pushDefault` configuration.
    - **Repeatable `--delete-merged` arguments**: Implemented in v19 to widen the upstream match (e.g., `--delete-merged origin/main --delete-merged origin/next`), with optional positional patterns (e.g., `topic-*`) to limit deletion scope.
    - **`--dry-run` bug**: Fixed in v19 to avoid modifying config during dry runs. Added test coverage to verify this.
    - **Safety architecture**: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, order-independent stacked-branch protection).
    - **Per-branch opt-out**: Implemented via `branch.<name>.deleteMerged=false` config key, with documentation and tests. **Phillip Wood's surface-level review suggested a mechanical test improvement (`for-each-ref` verification via `check_branches`), which has been applied.**
    - **`--dry-run` preview**: Implemented in patch 7/7, **fixed to avoid modifying config during dry runs**, and rejects the nonsensical `--dry-run --quiet` combination.
    - **Test improvements**: All suggested changes (comments, helper functions, redundant command removal, branch naming clarity, `for-each-ref` verification) have been applied. The test suite now explicitly configures `branch.main.pushRemote` to prevent false positives in `--forked` filtering.
    - **Design clarification**: Junio C Hamano has confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`) is correct and sound.
    - **CI timeout**: Resolved in v21 and v22 by adjusting loop variable initialization (`int i` instead of `size_t i` or `size_t i = 0`), with no functional impact.
    - **Flags-based API maintenance risk**: Resolved in patches 2-4/7 by eliminating local boolean caching (e.g., `int force = 1`) to ensure flags remain the single source of truth.
    - **Patch 1/7 mechanical issues**: Junio C Hamano's review identified two **mechanical correctness issues**, which have been fixed in v23 (style nit in `short_upstream_name()`, type mismatch in loop variable).
    - **Surface-level test feedback**: Harald has addressed Phillip Wood's feedback on documentation clarity, test style, and test logic redundancy in patch 5/7.
    - **Usage message syntax**: The original `(--delete-merged <branch>)...` syntax is correct (indicating the option must be given at least once), and Harald has retained it.
    - **Test command line wrapping**: Phillip Wood's **surface-level nit** about unwrapping overly long test command lines in patch 1/7 has been noted. While not a blocker, this could be addressed in a final version if Junio requests it.
10) **Unresolved issues**:
    - **Follow-up work**:
      - Extending order-independent stacked-branch protection to `git branch -d` for consistency, as requested by Junio C Hamano and agreed by Harald Nordgren. This is **not a blocker for v24** but will be implemented after the series lands.
      - **Future improvement**: Implementing Phillip Wood's proposed refined heuristic for the `--forked` filter (simulating push/fetch refspecs) to better distinguish true forks from long-lived branches. This is **not a blocker for v24** and is explicitly noted as a non-blocking future improvement.

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using the `--forked` filter machinery, supporting exact refnames, remote shorthand, shell-style globs, and **repeatable `--delete-merged` arguments to widen the upstream match** (e.g., `--delete-merged origin/main --delete-merged origin/next`).
   - **Optional positional patterns** (e.g., `topic-*`) limit deletion scope.
   - Verifies tip reachability from upstream (skips unmerged branches).
   - Checks push divergence (preserves branches where pushing by name would update their upstream, e.g., `main` tracking `origin/main`). The v24 update fixes the push-detection logic to apply the correct sequence (push → fetch).
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7).
   - Never touches checked-out branches (any worktree).
   - Supports `--dry-run` preview mode (**avoids modifying config during dry runs**).
   - **Order-independent stacked-branch protection**: Fails to delete a branch if it is used as an upstream for an unmerged branch, and **clears the upstream config for kept (merged) branches whose own upstream is being deleted**. The v24 update simplifies the logic to protect only the *direct* upstream of a surviving branch. The `deletable` and `protected` strsets are indexed by **branch names** (e.g., `main`).
   - **`--dry-run --quiet` handling**: The combination is rejected as nonsensical.
   - **`DELETE_BRANCH_NO_HEAD_FALLBACK` flag**: Ensures `--delete-merged` only deletes branches merged into their upstream, without considering `HEAD` as a fallback.

2) Implementation highlights:
   - **Push-detection fix (v24)**:
     - The `branch_pushes_to_upstream()` function now correctly applies the *push* refspec to the local branch, applies the *fetch* refspec in reverse to the result, and compares that to the upstream branch. This ensures accurate detection regardless of `remote.pushDefault` configuration.
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out).
     - Checked during candidate filtering in `delete_merged_branches()` using `repo_config_get_bool()`.
     - Non-quiet message explains opt-out skip (e.g., "Skipping 'kept' (branch.kept.deleteMerged is false)").
     - Ignored by traditional `git branch -d` (continues to use normal merge check).
     - **Test improvement**: Added `for-each-ref` verification via `check_branches` as suggested by Phillip Wood.
   - **Order-independent stacked-branch protection (v24 update)**:
     - **Design and implementation**: Fails to delete a branch if it is used as an upstream for an unmerged branch, and **clears the upstream config for kept (merged) branches whose own upstream is being deleted**. The v24 update simplifies the logic to protect only the *direct* upstream of a surviving branch, removing the graph traversal.
     - Handles chains of dependencies (e.g., `b1→b2→b3`) by keeping only the branches needed by unmerged branches and clearing upstream configs for kept (merged) branches.
     - **Will be extended to `git branch -d` for consistency as a follow-up after the series lands**, as requested by Junio C Hamano and agreed by Harald Nordgren.
     - **Dry-run behavior**: During `--dry-run`, `spare_stacked_bases()` skips config updates (e.g., clearing upstreams) to avoid side effects.
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) or accepts shell-style globs (e.g., `origin/*`).
     - `filter_forked_match()` checks each branch's upstream against registered patterns, with exact matches compared against full upstream refnames and globs matched against abbreviated upstream names (e.g., `origin/main`).
     - Supports remote shorthand (e.g., `origin` for `origin/HEAD`).
     - Composable with existing filters (`--merged`, `--no-merged`).
     - **Edge case resolved**: Branches are now protected if pushing them by name would update their upstream, regardless of `remote.pushDefault` configuration.
   - **Repeatable `--delete-merged` arguments**:
     - Supports multiple `--delete-merged` arguments to widen the upstream match (e.g., `--delete-merged origin/main --delete-merged origin/next`).
     - Optional positional patterns (e.g., `topic-*`) limit deletion scope.
   - **Flags-based API (patches 2-4/7)**:
     - **Maintenance issue resolved**: Local boolean caching eliminated to ensure flags remain the single source of truth.
     - **Patch 2/7 (v24 2/7)**: Refactors `delete_branches()` and `check_branch_commit()` to replace `force` and `quiet` booleans with `unsigned int flags` and enum values (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`). Flags are tested directly at each use site to prevent stale cached values.
     - **Patch 3/7 (v24 3/7)**: Adds `DELETE_BRANCH_SKIP_UNMERGED` to enable bulk callers to skip unmerged branches silently.
     - **Patch 4/7 (v24 4/7)**: Adds `DELETE_BRANCH_NO_HEAD_FALLBACK` to prevent falling back to `HEAD` when no branches are specified, ensuring `--delete-merged` only deletes branches merged into their upstream.
     - Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` for `--dry-run` support, completing the flags-based API.
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using the `--forked` filter machinery and supports repeatable `--delete-merged` arguments and optional positional patterns.
     - Applies safety checks and order-independent stacked-branch protection.
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API.
     - `delete_branches()` skips ref/config deletion when the flag is set and prints "Would delete" or "Would skip" messages.
     - `spare_stacked_bases()` skips config updates (e.g., clearing upstreams) during dry runs to avoid side effects.
     - Prints output matching the real deletion/protection output exactly.
     - Rejects `--dry-run` without `--delete-merged` and the nonsensical `--dry-run --quiet` combination.
   - Safety checks: Checked-out branches, missing upstreams, push divergence (fixed in v24), per-branch opt-out, order-independent stacked-branch protection.
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out).

3) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared.
     - Confirms branches with missing upstreams or push-equals-upstream (fixed in v24) are preserved.
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion.
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check.
     - Tests `--dry-run` functionality: output matches real deletion/protection output exactly, config is preserved, and `--dry-run` is rejected without `--delete-merged`.
     - **Order-independent stacked-branch protection**: Tests verify abort-and-clear behavior, error messages, clearing upstreams for kept branches, and handling of chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`).
     - **`--forked` filter**: Tests exact refnames, remote shorthand, shell globs, repeated arguments, composition with other filters (`--merged`, `--no-merged`), and edge cases like self-referential upstreams and missing upstreams.
     - **Repeatable `--delete-merged` arguments**: Tests confirm the command respects multiple `--delete-merged` arguments to widen the upstream match and optional positional patterns to limit deletion scope.
     - **Flags-based API**: Tests confirm the refactoring in patches 2-4/7 introduces no behavioral changes for existing callers and correctly implements the new flags (`DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`).
     - **Push-detection fix (v24)**: Tests verify the corrected `branch_pushes_to_upstream()` logic, including edge cases like `remote.pushDefault` configuration.
     - **Stacked-branch protection (v24)**: Tests verify the simplified logic, including the clearing of upstream configs for kept (merged) branches.
     - **Per-branch opt-out**: Tests verify `for-each-ref` output via `check_branches` as suggested by Phillip Wood.
     - **`--dry-run`**: Tests verify output accuracy, config preservation, stacked-branch protection during dry runs, and rejection of invalid combinations (`--dry-run --quiet`).

Open questions:
1) **Follow-up work**:
   - Extending order-independent stacked-branch protection to `git branch -d` for consistency, as requested by Junio C Hamano and agreed by Harald Nordgren. This is **not a blocker for v24** but will be implemented after the series lands.
   - **Future improvement**: Implementing Phillip Wood's proposed refined heuristic for the `--forked` filter (simulating push/fetch refspecs) to better distinguish true forks from long-lived branches. This is **not a blocker for v24** and is explicitly noted as a non-blocking future improvement.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v24, including core deletion logic, safety checks, **per-branch opt-out (patch 6/7)**, `--dry-run` functionality (patch 7/7), **order-independent stacked-branch protection via single-pass logic (v24 update)**, the `--forked` filter (patch 1/7), **repeatable `--delete-merged` arguments and positional branch patterns**, and the flags-based API refactoring (patches 2-4/7). Addressed Phillip Wood's style suggestions and test improvements for patches 1/7 and 5/7. **Applied all test improvements to patch 6/7 (`for-each-ref` verification via `check_branches`).** Fixed the `--dry-run` bug to avoid modifying config during dry runs, the `--forked` filter edge case, and **resolved the order dependency issue in stacked-branch protection** via the single-pass logic. **Resolved the CI timeout in v21 and v22 by adjusting loop variable initialization (`int i` instead of `size_t i` or `size_t i = 0`).** **Has agreed to extend stacked-branch protection to `git branch -d` for consistency as a follow-up after the series lands.** Addressed Phillip Wood's feedback on patch 7/7 by rejecting the nonsensical `--dry-run --quiet` combination. **Addressed Junio C Hamano's mechanical correctness issues in patch 1/7 (style nit in `short_upstream_name()`, type mismatch in loop variable) in v23.** **Resolved the two blocking correctness issues in v24**:
   - Rewrote the push-detection logic in `branch_pushes_to_upstream()` to apply the correct sequence (push → fetch).
   - Simplified the stacked-branch protection logic to clear upstream configs for kept (merged) branches whose own upstream is being deleted, and updated the test to match this behavior.
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, `--dry-run` requirements, **repeatable `--delete-merged` arguments**, and the `--forked` filter edge case. **Proposed and implemented the refined stacked-branch protection approach (abort-and-clear)**, which was adopted in v18. His feedback was instrumental in shaping the final design, including the flags-based API refactoring and the `DELETE_BRANCH_SKIP_UNMERGED` flag (added in patch 3/7). **Reviewed patch 1/7 (--forked filter) and raised only surface-level test-related points (detached HEAD removal, test reordering), which have been addressed.** Identified the flags maintenance issue in patches 2-4/7 and recommended eliminating local boolean caching to ensure flags remain the single source of truth. **Latest review of patch 5/7 suggested test improvements (comments, helper functions, redundant command removal, branch naming clarity), which have been applied.** Final review of patch 7/7 suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk, which Harald Nordgren addressed by rejecting it as nonsensical. **Surface-level review of patch 6/7 suggested a mechanical test improvement (`for-each-ref` verification via `check_branches`), which has been applied.** Acknowledged the `--forked` filter edge case fix in v19 and confirmed the **forward-looking refinement** (simulating push/fetch refspecs) is **not a blocker for v24**. **Proposed the structural refactor that resolved the order dependency issue in v20** and provided a **substantive review** of the refactor, asking for clarification about the logic changes and why the previous approach was insufficient. His questions confirm the refactor addresses the core correctness issue. **Latest review of v23 identified two blocking technical concerns**, which Harald Nordgren has resolved in v24:
   - **`branch_pushes_to_upstream()` correctness**: The v24 update fixes the logic to apply the correct sequence (push → fetch).
   - **Stacked-branch protection test discrepancy**: The v24 update simplifies the logic to clear upstream configs for kept (merged) branches and updates the test to match this behavior.
   - **Minor feedback**: Clarified that the original `(--delete-merged <branch>)...` syntax in the usage message is correct, and suggested unwrapping overly long test command lines in patch 1/7. These are **surface-level improvements** and not blockers.
   - **Final review of v24**: Confirmed the series "looks ready to be merged" after verifying the v24 fixes address the last technical blockers. Noted the stacked-branch protection logic "pretty much takes us back to V19" but with the critical improvement of pruning protected branches from the deletable set *after* visiting all branches, ensuring order independence.
- **Junio C Hamano**: **Endorsed the refined stacked-branch protection design (abort-and-clear)** as philosophically consistent with `git branch -d`. **Identified the blocking correctness issue (order dependency) in earlier versions and remained unconvinced by Harald Nordgren's initial fix.** The **order dependency issue has now been resolved** via Phillip Wood's proposed single-pass logic, addressing Junio's correctness concerns. **Requested that the protection be extended to `git branch -d` for consistency, and Harald Nordgren has agreed to implement this as a follow-up after the series lands.** **Confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`) is correct and sound**, resolving his initial concern about the use of `up_short`. **Explicitly confirmed there are no outstanding issues for the series as a whole and asked whether the topic is "pretty much done," signaling it is ready for merging pending resolution of Phillip Wood's technical concerns.** **Latest review of patch 1/7 identified two mechanical correctness issues, which have been fixed in v23**:
   - **Style nit in `short_upstream_name()`**: Rewritten for clarity.
   - **Type mismatch in loop variable**: Fixed by declaring `i` as `size_t`.
   **Has now given final approval for merging the series contingent on resolving Phillip Wood's technical concerns, which have been addressed in v24.**

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v24 series is **ready for merging**. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter and repeatable `--delete-merged` arguments establish a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work as an optional flag (e.g., `--retarget-upstreams`). **Phillip Wood's speculative `--forked=recurse` idea is noted but not pursued in this series.** The test suite now covers all edge cases, including the corrected push-detection logic and simplified stacked-branch protection. **The stacked-branch protection extension to `git branch -d` will be implemented as a follow-up after the series lands.** Key files touched: `builtin/branch.c`, `ref-filter.c`, `Documentation/git-branch.adoc`, `Documentation/config/branch.txt`, and `t/t3200-branch.sh`. Patches 2-4/7 specifically touch `builtin/branch.c` to add the flags-based API (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`). Patch 6/7 adds the `branch.<name>.deleteMerged` config key and updates `Documentation/config/branch.txt`. Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` and updates `Documentation/git-branch.adoc`. The series is **well-motivated, thoroughly tested, and aligned with Git’s API consistency expectations**. **Merge status: Ready for `next`.**