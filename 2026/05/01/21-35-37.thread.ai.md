AI thread summary:

Thread identity: v8 of Harald Nordgren's series (5 patches) adding `git branch --prune-merged` for safe automated local branch cleanup when upstream branches are deleted. A sub-discussion has emerged around a separate `fetch.pruneLocalBranches` config concept.

Core problem or goal: Provide safe, automated local branch cleanup through explicit `git branch` commands with upstream-reachability semantics. The series consists of: `--forked <remote>` (show branches tracking a remote), `--prune-merged` (delete local branches whose tip is reachable from their configured upstream remote-tracking branch), `branch.<name>.pruneMerged` config (per-branch opt-out), and `--all-remotes` (convenience flag to apply `--prune-merged` or `--forked` to all remotes).

Current status: v8 is complete (all 5 patches sent). Junio C Hamano has responded with a split decision: he strongly approves of the upstream-reachability safety model (the core redesign from v8), calling it "a lot better" than the previous round. However, he objects to the `--force` behavior that allows bypassing the mergedness check entirely -- he argues `--prune-merged` should always prune only merged branches, regardless of `--force`. This is a narrow, mechanically fixable issue (affects patch 3 only). The series is essentially accepted in principle pending this one fix.

Harald has now responded and agrees to remove the `--force` functionality from `--prune-merged`, confirming he already lost work to that behavior once. He also floated a separate concept in the same email: a `fetch.pruneLocalBranches` config option that would automatically delete local branches during `git fetch` when their upstream counterpart has been removed. This is a preliminary idea -- no details on safety checks, per-branch opt-out, or interaction with `fetch.prune`. It is not a patch and not part of the `--prune-merged` series; it is a separate proposal being aired for discussion.

Key technical details:
- Patch 1: `--forked <remote>` infrastructure, ~110 lines.
- Patch 2: `warn_only`/`n_not_merged` parameters to `delete_branches()`, ~20 lines, mechanical refactoring.
- Patch 3: `prune_merged_branches()` core logic, ~140 lines. Junio's objection targets the `--force` bypass of the reachability check -- he wants `-D` to not skip the mergedness check for `--prune-merged`, meaning `git branch -D --prune-merged <remote>` should not become equivalent to `git branch -D $(git branch --forked <remote>)`. Harald has agreed to remove this `--force` override; fix is expected in v9.
- Patch 4: `branch.<name>.pruneMerged` config option, ~25 lines, `--force` does NOT override it (and Junio's objection does not touch this patch).
- Patch 5: `--all-remotes` flag, ~80 lines. Uses `for_each_remote()` to enumerate all configured remotes; additive with explicit remote arguments; rejected when used without `--forked` or `--prune-merged`.

Safety model (v8 redesign, accepted by Junio): A local branch is safe to delete only if its tip is reachable from its configured upstream remote-tracking branch (`branch.<name>.merge`). Three safeguards: not checked out in any worktree, resolvable upstream, and if upstream is the remote's default branch the local branch must not share the same name. Branches with unresolvable upstream refs (remote-tracking branch was deleted) are silently skipped.

The separate `fetch.pruneLocalBranches` concept: Harald revealed that the `--prune-merged` series emerged from an originally broader "prune local branches" idea. He is now floating the `fetch.pruneLocalBranches` config as a separate, automatic mechanism. No details on safety model, opt-out, or interaction with `fetch.prune` have been specified. No reviewer has reacted to this proposal yet.

Open questions and loose ends:
- The `--force` interaction with `--prune-merged` needs to be reworked per Junio's objection. Harald has agreed to fix this; v9 is expected. This remains the only blocker for the `--prune-merged` series.
- The `fetch.pruneLocalBranches` concept is a new, separate proposal. No review or discussion has occurred yet. It may or may not end up replacing or supplementing `--prune-merged`, but for now it is just a concept being aired.

Participant positions:
- Harald Nordgren: Author. v8 redesign was fully accepted in principle by Junio. Agreed to remove the `--force` override from `--prune-merged`. Independently floated the `fetch.pruneLocalBranches` concept as a separate, preliminary idea.
- Junio C Hamano: Strongly approved the v8 safety model ("matches my understanding...a lot better"). Objected narrowly to `--force` bypassing mergedness check. Substantive review engaging with the design, no test results reported. Has not yet reacted to the `fetch.pruneLocalBranches` proposal.

Related work: Uses `fetch.prune` infrastructure. The `branch.<name>.pruneMerged` config parallels existing `branch.<name>` patterns. The `--all-remotes` design follows similar "all remotes" flags elsewhere in Git. The `fetch.pruneLocalBranches` concept would extend `fetch.prune` to operate on local branches.