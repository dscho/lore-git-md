AI thread summary (updated):

Thread identity: v9 of Harald Nordgren's 5-patch series adding `git branch --prune-merged` for safe automated local branch cleanup when upstream branches are deleted. The series is complete in its final iteration. The related `fetch.pruneLocalBranches` concept remains a separate, unattended proposal.

Core problem or goal: Provide safe, automated local branch cleanup through explicit `git branch` commands with upstream-reachability semantics. The series: `--forked <remote>` (show branches tracking a remote), `--prune-merged` (delete local branches whose tip is reachable from their configured upstream remote-tracking branch), `branch.<name>.pruneMerged` config (per-branch opt-out), and `--all-remotes` (convenience flag applying `--forked` or `--prune-merged` to all configured remotes).

Current status: v9 is complete; all five patches have been sent and described. The series resolves Junio C Hamano's sole remaining objection from v8 (`--force` should not bypass the mergedness check). Safety model fully approved by Junio in v8; the narrow `--force` objection is resolved in v9. The series should be ready for `next`.

Key technical details:
- Patch 1 (v9, unchanged from v8): `--forked <remote>`. Lists local branches whose configured upstream falls within the given remote or remote-tracking branch. Implementation in `builtin/branch.c` via `list_forked_branches()`, `parse_forked_args()`, `branch_is_forked()`, `collect_forked_branch()` -- about 110 lines of C. Six tests in `t/t3200-branch.sh`.
- Patch 2 (v9, unchanged from v8): Refactors `check_branch_commit()` and `delete_branches()` to accept `warn_only` (suppresses per-branch refusal to a single-line warning) and `n_not_merged` (incremented for each skipped branch for bulk summary). No behavioral change to existing callers.
- Patch 3 (v9): `--force` no longer bypasses the mergedness check; reachability check is always enforced. Synopsis drops `[-f]`. Error message changed. New `collect_forked_set()` helper factors collection logic previously specific to `--forked` list. New `prune_merged_branches()` function iterates over candidates, filtering by safety checks, and passes survivors to `delete_branches()` with `warn_only=1`. 226 insertions across three files. Eight new tests.
- Patch 4 (v9): Adds `branch.<name>.pruneMerged` config option (default true). When `false`, exempts a branch from `--prune-merged` even with `--force`. Explicit `git branch -d` unaffected. 23 lines in `builtin/branch.c`, plus documentation and two tests.
- Patch 5 (v9, final patch): Adds `--all-remotes` convenience flag. A `NONEG` boolean option that populates a `string_list` via `for_each_remote(collect_remote_name)`, merged with any explicit `<remote>` arguments before filtering into `collect_forked_set()`. Requires `--forked` or `--prune-merged` present; dies otherwise. Synopsis updated to `(--forked|--prune-merged) (<remote>... | --all-remotes)`. 81 lines across three files. Four new tests: `--forked --all-remotes` across multiple remotes, invalid explicit remote still caught with `--all-remotes`, `--all-remotes` alone rejected, and a `--prune-merged --all-remotes` integration test with two remotes.
- Safety model (unchanged from v8, approved by Junio): A local branch safe to delete only if its tip is reachable from its configured upstream remote-tracking branch. Four safeguards: not checked out in any worktree, resolvable upstream, if upstream is the remote's default branch the local branch must not share the same name, and `branch.<name>.pruneMerged` is not `false`.

Open questions and loose ends: None for the `--prune-merged` series. The separate `fetch.pruneLocalBranches` concept remains a preliminary, unattended proposal -- no review or discussion has occurred on it.

Participant positions:
- Harald Nordgren: Author. v8's safety model fully accepted by Junio; v9 addresses Junio's narrow objection precisely. The series is complete in its final state. Independently floated the separate `fetch.pruneLocalBranches` concept.
- Junio C Hamano: Strongly approved v8 safety model. Objected only to `--force` bypassing the mergedness check. That objection is fully resolved in v9.

Related work: Uses `fetch.prune` infrastructure. `branch.<name>.pruneMerged` parallels existing `branch.<name>` patterns. `--all-remotes` follows established patterns using `for_each_remote()` the same way as other "all remotes" flags in Git.