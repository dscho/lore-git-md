AI thread summary (updated):

Thread identity: Final v16 of Harald Nordgren's `git branch --delete-merged` series, providing safe automated local branch cleanup with comprehensive safety mechanisms, filtering capabilities, and `--dry-run` preview functionality. The series is now technically complete with the stacked-branch safety design fully resolved and implementation details clarified.

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented and tested)
2) Flag-based API for deletion behavior (fully refactored and extended with `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms to prevent accidental data loss (checked-out branches, missing upstreams, push divergence, per-branch opt-out via `branch.<name>.deleteMerged`, **stacked-branch upstream preservation**)
4) `--dry-run` preview capability (fully implemented and tested)

Current status:
1) Series implementation and feature set complete through 16 iterations
2) **Stacked-branch safety design fully resolved**: Junio C Hamano and Phillip Wood agree that branches used as upstreams for unmerged stacked branches must be preserved to avoid breaking `@{u}` references and implicit `git rebase` workflows. The protection will **abort deletion and notify the user** by default (e.g., "Cannot delete branch 'feature1' because 'feature2' depends on it"), with optional retargeting via a new flag (e.g., `--retarget-upstreams`).
3) Resolved issues:
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`
   - `--forked` filter: Implemented with remote shorthand/glob support and exact ref matching
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, **stacked-branch upstream preservation**)
   - Test suite: Comprehensive integration tests verifying deleted/preserved branches, realistic workflows, exact output matching, opt-out behavior, and `--dry-run` functionality
4) Core deletion logic implemented with:
   - `--forked` filter integration (tip reachability from upstream)
   - Push divergence checks (preserves tracking branches)
   - Per-branch opt-out via `branch.<name>.deleteMerged` config (checked after safety checks but before deletion)
   - `--dry-run` preview mode (mirrors actual deletion logic, prints "Would delete branch <name>" per branch, exits without touching refs)
   - **Stacked-branch protection**: Aborts deletion if the branch is used as an upstream for any other branch (e.g., `feature1` if `feature2` stacks on it), with optional retargeting via `--retarget-upstreams`.

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()`
   - Verifies tip reachability from upstream (skips unmerged branches)
   - Checks push divergence (preserves branches where push == upstream)
   - Respects `branch.<name>.deleteMerged` config opt-out (checked after safety checks but before deletion)
   - Never touches checked-out branches (any worktree)
   - Supports `--dry-run` preview mode (fully implemented, rejects use without `--delete-merged`)
   - **Stacked-branch protection**: Aborts deletion if the branch is used as an upstream for any other branch (e.g., via `branch.<name>.merge` config), with optional retargeting via `--retarget-upstreams`.

2) Implementation highlights:
   - New `delete_merged_branches()` function in `builtin/branch.c`
   - Unified flags-based API: `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`, **`DELETE_BRANCH_RETARGET_UPSTREAMS`** (proposed)
   - Memory management: Single `strbuf` reuse for efficiency (including config key lookup)
   - CLI plumbing: Natural integration with existing `--merged`/`--no-merged` filters
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, **stacked-branch upstream preservation**
   - Config integration: `branch.<name>.deleteMerged` (false = opt-out, true/default = eligible)
   - `--dry-run` implementation: `OPT_BOOL` in `builtin/branch.c`, passes `DELETE_BRANCH_DRY_RUN` flag, prints "Would delete branch <name>" per branch, exits without ref modification
   - **Stacked-branch detection**: Identifies branches used as upstreams via `branch.<name>.merge` config (e.g., `feature2` → `feature1`).
   - **User feedback**: Aborts with a clear error message (e.g., "Cannot delete branch 'feature1' because 'feature2' depends on it") unless `--retarget-upstreams` is specified.

3) Safety architecture:
   - Never deletes checked-out branches (any worktree)
   - Preserves branches with missing upstreams
   - Preserves branches where push destination equals upstream (e.g., `main` tracking `origin/main`)
   - Silently skips unmerged branches (non-fatal)
   - Per-branch opt-out mechanism (addresses iterative topic branches)
   - `--dry-run` preview capability (fully implemented, mirrors actual deletion logic)
   - **Stacked-branch protection**: Aborts deletion if the branch is used as an upstream for any other branch, with optional retargeting via `--retarget-upstreams`.

4) Test coverage:
   - Two comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared
     - Confirms branches with missing upstreams or push-equals-upstream are preserved
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion, but `git branch -d` still works
     - Tests `--dry-run` functionality: output matches real deletion output, option rejected when used alone
     - **Pending**: Tests for stacked-branch protection (e.g., `feature1` preserved if `feature2` stacks on it, abort/error message, and `--retarget-upstreams` behavior).

Open questions:
- **Final implementation details for stacked-branch protection**:
  - Whether to make `--retarget-upstreams` opt-in (default abort) or opt-out (default retarget).
  - Whether to extend protection to other dependencies (e.g., reflog references, worktrees, or non-upstream relationships).
- **Final review**: The series is now ready for final review pending implementation of the stacked-branch protection. A v17 iteration will incorporate this change.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v16, including core deletion logic, safety checks, per-branch opt-out, `--dry-run` functionality, and test suite improvements. Will implement stacked-branch protection in v17. Series ready for final review.
- **Phillip Wood**: Provided high-weight feedback on API consistency and **stacked-branch safety**, successfully arguing that preserving branches used as upstreams for unmerged stacked branches is necessary to avoid breaking `@{u}` references and implicit `git rebase` workflows. Advocated for clear user feedback (abort-and-notify) as the default behavior. Review complete pending implementation.
- **Junio C Hamano**: Initially skeptical of stacked-branch protection, now fully endorses it and proposes the **abort-and-notify** default with optional `--retarget-upstreams` flag. This resolution clears the way for final review.
- **Johannes Sixt**: Contributed to CLI design decisions (medium weight).

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v16 series is mature and ready for final review, with stacked-branch protection as the last remaining implementation task. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows.