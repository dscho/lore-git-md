AI thread summary (updated):

Thread identity: Final v17 of Harald Nordgren's `git branch --delete-merged` series, now fully implemented with all planned features and safety mechanisms, including stacked-branch protection via **upstream config clearing**, per-branch opt-out, and `--dry-run` preview. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls. This version includes the core `--delete-merged` command (patch 5/7), the foundational `--forked` filter (patch 1/7), a refactored flags-based deletion API (patches 2-4/7), per-branch opt-out (patch 6/7), **stacked-branch protection via upstream config clearing** (fully implemented), and `--dry-run` preview (patch 7/7).

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented in patch 1/7)
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, **stacked-branch protection via upstream config clearing**)
4) `--dry-run` preview capability (fully implemented in patch 7/7)

Current status:
1) **Series implementation complete**: All planned features and safety mechanisms are implemented, including per-branch opt-out, `--dry-run` preview, and **stacked-branch protection via upstream config clearing**.
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream while preserving branches used as upstreams for unmerged branches by **clearing their upstream config** (e.g., "Cleared upstream of 'feature1' because 'feature2' depends on it").
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values.
4) **Per-branch opt-out implemented**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`.
5) **Stacked-branch protection finalized**: **Upstream config clearing** is now fully implemented. Branches used as upstreams for unmerged branches have their upstream config (`branch.<name>.remote` and `branch.<name>.merge`) cleared, avoiding stale references without recursive retargeting. Handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) uniformly. Warning message: "Cleared upstream of '<branch>' because '<dependent>' depends on it".
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`).
7) **`--dry-run` preview implemented**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" or "Would clear upstream of <name>" for each affected branch without modifying refs. The preview reflects stacked-branch protection edge cases.
8) **Test suite complete**: Expanded to cover stacked-branch protection (upstream config clearing), per-branch opt-out, `--dry-run` functionality, and `--forked` filter behavior. Test cases for upstream-clearing messages and self-referential upstreams are now included.
9) Resolved issues:
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`
   - `--forked` filter: Implemented with remote shorthand/glob support and exact ref matching
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch protection via upstream config clearing)
   - **Per-branch opt-out**: Implemented via `branch.<name>.deleteMerged=false` config key
   - **Stacked-branch protection**: **Fully implemented as upstream config clearing**, avoiding recursive retargeting complexity. Self-referential upstreams are handled by clearing the config without warning. **Design resolution**: Phillip Wood proposed this approach; Junio C Hamano has not objected.
   - **`--dry-run` preview**: Implemented in patch 7/7, providing zero-risk preview of deletion candidates and upstream-clearing actions
   - Test coverage: Comprehensive integration tests verifying deleted/preserved branches, opt-out behavior, `--dry-run` functionality, stacked-branch protection (upstream config clearing), and `--forked` filter behavior

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()` (patch 1/7)
   - Verifies tip reachability from upstream (skips unmerged branches)
   - Checks push divergence (preserves branches where push == upstream)
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7)
   - Never touches checked-out branches (any worktree)
   - Supports `--dry-run` preview mode (fully implemented in patch 7/7)
   - **Stacked-branch protection**: **Clears upstream config** of branches used as upstreams for unmerged branches (e.g., "Cleared upstream of 'feature1' because 'feature2' depends on it"), avoiding stale references without recursive retargeting. Handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) uniformly.

2) Implementation highlights:
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out)
     - Checked during candidate filtering in `delete_merged_branches()`
     - Non-quiet message explains opt-out skip
     - Ignored by traditional `git branch -d` (continues to use normal merge check)
   - **Stacked-branch protection (upstream config clearing)**:
     - **Fully implemented**: Clears `branch.<name>.remote` and `branch.<name>.merge` for branches used as upstreams for unmerged branches
     - Warning message: "Cleared upstream of '<branch>' because '<dependent>' depends on it"
     - Avoids traversal complexity by clearing config without recursive retargeting
     - Handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) uniformly
     - Applies uniformly to both `--delete-merged` and `-d` for consistency
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames or accepts shell-style globs
     - `filter_forked_match()` checks each branch's upstream against registered patterns
   - **Flags-based API (patches 2-4/7)**:
     - Replaces boolean arguments with `unsigned int flags` and enum values
     - Adds `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using `--forked` machinery
     - Applies safety checks and stacked-branch protection (upstream config clearing)
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API
     - Prints "Would delete branch <name>" or "Would clear upstream of <name>" for each affected branch
     - Rejects `--dry-run` without `--delete-merged`
     - Output matches real deletion/clearing output exactly
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch protection (upstream config clearing)
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out)
   - `--dry-run` implementation: Prints preview messages, exits without ref modification

3) Safety architecture:
   - Never deletes checked-out branches (any worktree)
   - Preserves branches with missing upstreams
   - Preserves branches where push destination equals upstream
   - Silently skips unmerged branches (non-fatal)
   - Per-branch opt-out mechanism (`branch.<name>.deleteMerged=false`)
   - `--dry-run` preview capability (fully implemented in patch 7/7)
   - **Stacked-branch protection**: **Clears upstream config** of branches used as upstreams for unmerged branches, avoiding stale references without recursive retargeting

4) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared
     - Confirms branches with missing upstreams or push-equals-upstream are preserved
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check
     - Tests `--dry-run` functionality: output matches real deletion/clearing output exactly
     - **Stacked-branch protection**: Verifies upstream config is cleared for branches used as upstreams for unmerged branches, warning messages are clear, and chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) are handled uniformly
     - **`--forked` filter**: Tests exact refnames, remote shorthand, shell globs, composition with other filters

Open questions: None. The series is now **feature-complete** and ready for final review. All planned safety mechanisms (per-branch opt-out, `--dry-run` preview, and stacked-branch protection via upstream config clearing) are implemented, and the test suite is comprehensive. The design trade-off (simplicity over recursive retargeting) aligns with Phillip Wood's feedback and Junio C Hamano's lack of objection, suggesting broad consensus.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v17, including core deletion logic, safety checks, per-branch opt-out, `--dry-run` functionality (patch 7/7), **stacked-branch protection via upstream config clearing**, and test suite improvements. Adopted Phillip Wood's upstream-clearing approach as the final design resolution.
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, and `--dry-run` requirements. **Proposed upstream config clearing** as a simpler alternative to abort-and-notify, which Harald implemented in v17. Feedback fully addressed.
- **Junio C Hamano**: Initially skeptical of stacked-branch protection, now **has not objected** to the upstream config clearing approach. The series is now ready for final review and potential graduation to `next`.

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v17 series is mature and ready for integration, with all safety mechanisms (per-branch opt-out, `--dry-run` preview, and upstream config clearing for stacked-branch protection) fully implemented. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter establishes a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work as an optional flag (e.g., `--retarget-upstreams`).