AI thread summary (updated):

Thread identity: Final v23 of Harald Nordgren's `git branch --delete-merged` series, **implementation-complete but blocked on two unresolved correctness issues** identified by Phillip Wood. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls, including **order-independent stacked-branch protection**, per-branch opt-out, `--dry-run` preview, and the foundational `--forked` filter. This version contains **only mechanical fixes to patch 1/7** (type mismatch in loop variable and style nit in `short_upstream_name()`), addressing Junio C Hamano's final review comments. The rest of the series is **identical to v22**, which Junio confirmed has no outstanding issues and is ready for merging. **Junio has explicitly confirmed the series is ready for merging pending resolution of Phillip Wood's technical concerns.**

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) **Repeatable upstream selector** for branch selection (fully implemented), supporting exact refnames, remote shorthand, shell-style globs, and **positional branch patterns** to limit deletion scope. The `--delete-merged` option can be specified multiple times (e.g., `--delete-merged origin/main --delete-merged origin/next`) to widen the upstream match.
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`).
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, **order-independent stacked-branch protection**).
4) `--dry-run` preview capability (fully implemented, **fixed to avoid modifying config during dry runs**).

Current status:
1) **Series blocked on two unresolved correctness issues**: All planned features and safety mechanisms are implemented, but Phillip Wood's review of v23 identified two **blocking technical concerns** that remain unresolved:
   - **Stacked-branch protection test discrepancy**: The test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, but the design goal is to **clear stale configs for kept (merged) branches whose own upstream is being deleted**. Harald Nordgren's reply confirms the intent is to preserve the upstream chain of surviving *tips*, but the test expectation contradicts this. Resolution requires either:
     - Fixing the stacked-branch traversal logic to align with the test expectation (preserving upstream configs for kept merged branches), or
     - Updating the test to match the design goal (clearing upstream configs for kept merged branches whose own upstream is being deleted).
   - **`branch_pushes_to_upstream()` correctness**: The current implementation checks whether the branch’s *fetch* refspec maps to its upstream, but it should instead:
     - Apply the *push* refspec to the local branch.
     - Apply the *fetch* refspec in reverse to the result.
     - Compare that to the upstream branch.
     This ensures the check accurately reflects whether pushing the branch would update its upstream, regardless of `remote.pushDefault`. The current implementation risks **false negatives** (deleting branches that should be preserved, e.g., `main` tracking `origin/main`) or **false positives** (keeping branches that should be deleted). Harald acknowledges this will require re-adding some of the push logic removed earlier for simplicity.
   **Junio's prior approval is contingent on resolving these issues.**
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream (matching one of the `--delete-merged` arguments) while preserving branches used as upstreams for unmerged branches via **order-independent stacked-branch protection**. The command now **fails to delete a branch if it is used as an upstream for an unmerged branch**, but **clears the upstream config for kept (merged) branches whose own upstream is being deleted**, avoiding broken dependency chains while cleaning up stale configs. The command supports repeatable `--delete-merged` arguments to widen the upstream match and optional positional patterns (e.g., `topic-*`) to limit deletion scope. Junio C Hamano has **confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`)** is correct and sound, as it aligns with `branch_get()` and `branch_get_upstream()` behavior and avoids mixing local and remote refs.
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values. **The maintenance risk in patches 2-4/7 (two sources of truth for flags) has been resolved by eliminating local boolean caching and ensuring flags remain the single source of truth.**
   - Patch 2/7 converts `force` and `quiet` parameters to flags (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`), with direct flag testing at each use site to prevent stale cached values. The patch is purely mechanical, introduces no behavioral changes, and is **low-risk and well-tested**. The refactoring eliminates local boolean caching (e.g., `int force = 1`) to ensure flags remain the single source of truth, addressing a maintenance risk identified in earlier reviews.
   - Patch 3/7 adds `DELETE_BRANCH_SKIP_UNMERGED` to enable bulk callers to skip unmerged branches silently, preserving existing behavior for all current callers. The patch is minimal and targeted, modifying `check_branch_commit()` and `delete_branches()` to suppress the "not fully merged" error and advice when the flag is set. The flag is checked at both the commit-checking and deletion stages, ensuring unmerged branches are skipped without triggering a failure. This is critical for the `--delete-merged` subcommand. **The patch is now explicitly described as enabling silent skipping of unmerged branches for bulk operations, with no impact on existing callers.**
   - Patch 4/7 adds `DELETE_BRANCH_NO_HEAD_FALLBACK` to prevent falling back to `HEAD` when no branches are specified, ensuring `--delete-merged` only deletes branches merged into their upstream. The flag is checked at both the commit-checking (`branch_merged()`) and deletion (`delete_branches()`) stages, suppressing the `HEAD` fallback and the "gentle reminder" message when the caller explicitly opts out. The patch is **minimal and targeted**, introducing no behavioral changes for existing callers, and the new flag is only exercised by the `--delete-merged` subcommand (patch 5/7). **The new email confirms this patch is purely mechanical, plumbing the flag through the existing API without altering behavior for existing callers.**
   - Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` for `--dry-run` support, completing the flags-based API. The implementation is **minimal and targeted**, suppressing ref and config modifications while printing "Would delete" or "Would skip" messages for each affected branch. The output matches the real deletion/protection output exactly, making it a reliable preview mechanism. The flag is plumbed through `delete_branches()` and `delete_merged_branches()`, and `spare_stacked_bases()` skips config updates during dry runs to avoid side effects. The flag is only accepted when `--delete-merged` is specified, and the nonsensical `--dry-run --quiet` combination is rejected. The patch updates `Documentation/git-branch.adoc` and adds three new tests in `t/t3200-branch.sh` to verify `--dry-run` functionality, including edge cases like stacked-branch protection and the rejection of `--dry-run` without `--delete-merged`.
4) **Per-branch opt-out implemented and finalized**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`. The opt-out is checked during candidate filtering in `delete_merged_branches()` and produces a non-quiet skip message. **Phillip Wood's surface-level review of patch 6/7 suggested a mechanical test improvement (using `git for-each-ref` instead of `rev-parse` for branch state verification), which has been applied.** The implementation is now complete, with documentation added to `branch.adoc` and `git-branch.adoc`, and tests covering opt-out behavior, skip messages, explicit deletion, and branch state verification via `git for-each-ref`. **The new email confirms the opt-out mechanism is straightforward, well-scoped, and aligns with the series' design, with no outstanding issues.**
5) **Stacked-branch protection design and implementation**: **Finalized as order-independent abort-and-clear via single-pass graph traversal**, but **Phillip Wood's review identified a test discrepancy** suggesting either a **logic error** or **test expectation mismatch**. The logic handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) by treating the upstreams of unmerged branches as sacrosanct while clearing stale configs for kept (merged) branches. **The order dependency issue has been resolved** via a **single-pass graph traversal** that collects all protected branches before mutating the deletion set, ensuring consistent results regardless of ref iteration order. The refactor introduces:
   - `collect_stacked_branch_bases()`: A new helper function that traverses upstream chains from each branch, marking all transitively required branches as "protected" before any deletions occur.
   - `visited_branch_names` strset: Avoids infinite loops in self-referential upstream chains (e.g., `b3→b3`).
   - `protected_branch_names` strset: Replaces the previous `spared` set with clearer semantics and is indexed by **branch names** (e.g., `main`).
   - `refs_for_each_ref_ext()` with `prefix` option: Limits iteration to local branches.
   The `deletable` and `protected` strsets are indexed by **branch names** (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`). **Junio C Hamano has requested that this protection be extended to `git branch -d` for consistency, and Harald Nordgren has agreed to implement this as a follow-up after the series lands.**
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`). The filter supports exact refnames, remote shorthand, shell-style globs, and **positional branch patterns for `--delete-merged`**. **Phillip Wood's review of patch 1/7 raised only surface-level test-related points (detached HEAD removal, test reordering), which Harald Nordgren has addressed.** The v23 implementation **fixes the mechanical correctness issues identified by Junio C Hamano**:
   - **Type mismatch in loop variable**: The loop variable `i` is now declared as `size_t` to match `filter->forked.nr`, resolving a compiler warning and potential undefined behavior.
   - **Style nit in `short_upstream_name()`**: The function was rewritten to use explicit `if (!skip_prefix(...)) skip_prefix(...)` statements instead of the `(void)(skip_prefix(...) || ...)` pattern, improving readability.
   The v22 implementation also **fixed a critical edge case: branches are now protected if pushing them by name would update their upstream (e.g., `main` tracking `origin/main`), regardless of `remote.pushDefault` configuration**, ensuring the filter correctly distinguishes true forks from long-lived branches. **However, Phillip Wood's review identifies a flaw in the push-detection logic** that undermines this fix.
7) **`--dry-run` preview implemented and finalized**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch without modifying refs or config. The output matches the real deletion/protection output exactly, and `--dry-run` is rejected without `--delete-merged`. The `--dry-run --quiet` combination is rejected as nonsensical. **The implementation now explicitly avoids modifying config during dry runs, resolving the v18 issue.**
8) **Test suite complete but with discrepancies**: Expanded to cover order-independent stacked-branch protection, per-branch opt-out, `--dry-run` functionality (**fixed to avoid modifying config during dry runs**), repeatable `--delete-merged` arguments, positional branch patterns, and edge cases like self-referential upstreams and missing upstreams. **However, Phillip Wood's review identifies a test discrepancy in the stacked-branch protection logic**, where the test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, contradicting the design goal of clearing stale configs for kept (merged) branches. **The test suite now verifies that the stacked-branch protection logic produces consistent results regardless of ref iteration order, and that per-branch opt-out behavior is correctly verified using `git for-each-ref`.** Harald has agreed to address Phillip's **surface-level test feedback** (documentation clarity, test style, and test logic redundancy).
9) Resolved issues:
    - **Stacked-branch protection design**: Finalized as order-independent abort-and-clear via single-pass graph traversal (Phillip Wood's proposal).
    - **API consistency**: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`, with **local boolean caching eliminated to ensure flags remain the single source of truth.**
    - **`--forked` filter edge case**: Fixed in v19 by protecting branches if pushing them by name would update their upstream, regardless of `remote.pushDefault` configuration. **However, Phillip Wood's review identifies a flaw in the push-detection logic** that undermines this fix.
    - **Repeatable `--delete-merged` arguments**: Implemented in v19 to widen the upstream match (e.g., `--delete-merged origin/main --delete-merged origin/next`), with optional positional patterns (e.g., `topic-*`) to limit deletion scope.
    - **`--dry-run` bug**: Fixed in v19 to avoid modifying config during dry runs. Added test coverage to verify this.
    - **Safety architecture**: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, order-independent stacked-branch protection).
    - **Per-branch opt-out**: Implemented via `branch.<name>.deleteMerged=false` config key, with documentation and tests. **Phillip Wood's surface-level review suggested a mechanical test improvement (`for-each-ref` verification via `check_branches`), which has been applied.** **The new email confirms the opt-out mechanism is minimal, focused, and aligns with the series' design.**
    - **`--dry-run` preview**: Implemented in patch 7/7, **fixed to avoid modifying config during dry runs**, and rejects the nonsensical `--dry-run --quiet` combination.
    - **Test improvements**: All suggested changes (comments, helper functions, redundant command removal, branch naming clarity, `for-each-ref` verification) have been applied. The test suite now explicitly configures `branch.main.pushRemote` to prevent false positives in `--forked` filtering.
    - **Design clarification**: Junio C Hamano has confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`) is correct and sound.
    - **CI timeout**: Resolved in v21 and v22 by adjusting loop variable initialization (`int i` instead of `size_t i` or `size_t i = 0`), with no functional impact.
    - **Flags-based API maintenance risk**: Resolved in patches 2-4/7 by eliminating local boolean caching (e.g., `int force = 1`) to ensure flags remain the single source of truth. The patches are **purely mechanical, introduce no behavioral changes, and are low-risk and well-tested**.
    - **Patch 1/7 mechanical issues**: Junio C Hamano's review identified two **mechanical correctness issues**, which have been fixed in v23:
      - **Style nit in `short_upstream_name()`**: Rewritten for clarity.
      - **Type mismatch in loop variable**: Fixed by declaring `i` as `size_t`.
    - **Surface-level test feedback**: Harald has agreed to address Phillip Wood's feedback on documentation clarity, test style, and test logic redundancy in patch 5/7.
10) **Unresolved issues**:
    - **Stacked-branch protection test discrepancy**: The test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, but the design goal is to **clear stale configs for kept (merged) branches whose own upstream is being deleted**. Harald confirms the intent is to preserve the upstream chain of surviving *tips*, but the test expectation contradicts this.
    - **`branch_pushes_to_upstream()` correctness**: The current implementation risks **false negatives** (deleting branches that should be preserved) or **false positives** (keeping branches that should be deleted). Harald acknowledges this will require re-adding some of the push logic removed earlier for simplicity.

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using the `--forked` filter machinery, supporting exact refnames, remote shorthand, shell-style globs, and **repeatable `--delete-merged` arguments to widen the upstream match** (e.g., `--delete-merged origin/main --delete-merged origin/next`).
   - **Optional positional patterns** (e.g., `topic-*`) limit deletion scope.
   - Verifies tip reachability from upstream (skips unmerged branches).
   - Checks push divergence (preserves branches where pushing by name would update their upstream, e.g., `main` tracking `origin/main`). **However, Phillip Wood's review identifies a flaw in the push-detection logic** that undermines this safety check.
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7).
   - Never touches checked-out branches (any worktree).
   - Supports `--dry-run` preview mode (**fixed to avoid modifying config during dry runs**).
   - **Order-independent stacked-branch protection**: Fails to delete a branch if it is used as an upstream for an unmerged branch, but clears the upstream config for kept (merged) branches whose own upstream is being deleted. The new implementation ensures this logic is **order-independent** via a **single-pass graph traversal** that collects all protected branches before mutating the deletion set. The `deletable` and `protected` strsets are indexed by **branch names** (e.g., `main`).
   - **`--dry-run --quiet` handling**: The combination is rejected as nonsensical.
   - **`DELETE_BRANCH_NO_HEAD_FALLBACK` flag**: Ensures `--delete-merged` only deletes branches merged into their upstream, without considering `HEAD` as a fallback. The flag is checked at both the commit-checking (`branch_merged()`) and deletion (`delete_branches()`) stages. **The new email confirms this flag is plumbed through the existing API without altering behavior for existing callers.**

2) Implementation highlights:
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out).
     - Checked during candidate filtering in `delete_merged_branches()` using `repo_config_get_bool()`.
     - Non-quiet message explains opt-out skip (e.g., "Skipping 'kept' (branch.kept.deleteMerged is false)").
     - Ignored by traditional `git branch -d` (continues to use normal merge check).
     - Documentation added to `branch.adoc` and `git-branch.adoc`.
     - Tests verify opt-out behavior, skip messages, explicit deletion via `git branch -d`, and use `git for-each-ref` for branch state verification via the `check_branches` helper. **The new email confirms the implementation is clean, minimal, and follows Git's conventions.**
   - **Order-independent stacked-branch protection**:
     - **Design and implementation**: Fails to delete a branch if it is used as an upstream for an unmerged branch, but clears the upstream config for kept (merged) branches whose own upstream is being deleted. Implemented via:
       - `collect_stacked_branch_bases()`: A new helper function that traverses upstream chains from each branch, marking all transitively required branches as "protected" before any deletions occur.
       - `visited_branch_names` strset: Avoids infinite loops in self-referential upstream chains (e.g., `b3→b3`).
       - `protected_branch_names` strset: Replaces the previous `spared` set with clearer semantics and is indexed by **branch names** (e.g., `main`).
       - `refs_for_each_ref_ext()` with `prefix` option: Limits iteration to local branches.
     - Handles chains of dependencies (e.g., `b1→b2→b3`) by keeping only the branches needed by unmerged branches and clearing upstream configs for kept (merged) branches whose own upstream is being deleted.
     - **Will be extended to `git branch -d` for consistency as a follow-up after the series lands**, as requested by Junio C Hamano and agreed by Harald Nordgren.
     - **Dry-run behavior**: During `--dry-run`, `spare_stacked_bases()` skips config updates (e.g., clearing upstreams) to avoid side effects.
     - **Test discrepancy**: The test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, but the design goal is to **clear stale configs for kept (merged) branches whose own upstream is being deleted**. Harald confirms the intent is to preserve the upstream chain of surviving *tips*, but the test expectation contradicts this.
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) or accepts shell-style globs (e.g., `origin/*`).
     - `filter_forked_match()` checks each branch's upstream against registered patterns, with exact matches compared against full upstream refnames and globs matched against abbreviated upstream names (e.g., `origin/main`).
     - Supports remote shorthand (e.g., `origin` for `origin/HEAD`).
     - Composable with existing filters (`--merged`, `--no-merged`).
     - **Edge case resolved**: Branches are now protected if pushing them by name would update their upstream, regardless of `remote.pushDefault` configuration. **However, Phillip Wood's review identifies a flaw in the push-detection logic** that undermines this fix.
     - **Mechanical issues fixed in v23**:
       - **Type mismatch in loop variable**: `i` is now declared as `size_t`.
       - **Style nit in `short_upstream_name()`**: Rewritten for clarity.
   - **Repeatable `--delete-merged` arguments**:
     - Supports multiple `--delete-merged` arguments to widen the upstream match (e.g., `--delete-merged origin/main --delete-merged origin/next`).
     - Optional positional patterns (e.g., `topic-*`) limit deletion scope.
   - **Flags-based API (patches 2-4/7)**:
     - **Maintenance issue resolved**: Local boolean caching eliminated to ensure flags remain the single source of truth.
     - Patch 2/7 refactors `delete_branches()` and `check_branch_commit()` to replace `force` and `quiet` booleans with `unsigned int flags` and enum values (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`). Flags are tested directly at each use site to prevent stale cached values. The patch is **low-risk**, **well-tested**, and touches only `builtin/branch.c`. The refactoring eliminates local boolean caching (e.g., `int force = 1`) to ensure flags remain the single source of truth, addressing a maintenance risk identified in earlier reviews.
     - Patch 3/7 adds `DELETE_BRANCH_SKIP_UNMERGED` to enable bulk callers to skip unmerged branches silently, preserving existing behavior for all current callers. The patch is minimal and targeted, modifying `check_branch_commit()` and `delete_branches()` to suppress the "not fully merged" error and advice when the flag is set. The flag is checked at both the commit-checking and deletion stages, ensuring unmerged branches are skipped without triggering a failure. This is critical for the `--delete-merged` subcommand. **The patch is now explicitly described as enabling silent skipping of unmerged branches for bulk operations, with no impact on existing callers.**
     - Patch 4/7 adds `DELETE_BRANCH_NO_HEAD_FALLBACK` to prevent falling back to `HEAD` when no branches are specified, ensuring `--delete-merged` only deletes branches merged into their upstream. The flag is checked at both the commit-checking (`branch_merged()`) and deletion (`delete_branches()`) stages, suppressing the `HEAD` fallback and the "gentle reminder" message when the caller explicitly opts out. The patch is **minimal and targeted**, introducing no behavioral changes for existing callers, and the new flag is only exercised by the `--delete-merged` subcommand (patch 5/7). **The new email confirms this patch is purely mechanical, plumbing the flag through the existing API without altering behavior for existing callers.**
     - Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` for `--dry-run` support, completing the flags-based API. The flag is plumbed through `delete_branches()` and `delete_merged_branches()`, and `spare_stacked_bases()` skips config updates during dry runs to avoid side effects. The implementation is **minimal and targeted**, suppressing ref and config modifications while printing "Would delete" or "Would skip" messages for each affected branch. The output matches the real deletion/protection output exactly, and the flag is only accepted when `--delete-merged` is specified. The nonsensical `--dry-run --quiet` combination is rejected. The patch updates `Documentation/git-branch.adoc` and adds three new tests in `t/t3200-branch.sh` to verify `--dry-run` functionality, including edge cases like stacked-branch protection and the rejection of `--dry-run` without `--delete-merged`.
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using the `--forked` filter machinery and supports repeatable `--delete-merged` arguments and optional positional patterns.
     - Applies safety checks and order-independent stacked-branch protection. **Test improvements applied**: Added comments to helper functions, renamed them for clarity, simplified test patterns, removed redundant commands, and improved branch naming clarity.
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API.
     - `delete_branches()` skips ref/config deletion when the flag is set and prints "Would delete" or "Would skip" messages.
     - `spare_stacked_bases()` skips config updates (e.g., clearing upstreams) during dry runs to avoid side effects.
     - Prints output matching the real deletion/protection output exactly.
     - Rejects `--dry-run` without `--delete-merged` and the nonsensical `--dry-run --quiet` combination.
     - Updates `Documentation/git-branch.adoc` and adds three new tests in `t/t3200-branch.sh` to verify `--dry-run` functionality, including edge cases like stacked-branch protection and the rejection of `--dry-run` without `--delete-merged`.
   - Safety checks: Checked-out branches, missing upstreams, push divergence (**flawed implementation identified by Phillip Wood**), per-branch opt-out, order-independent stacked-branch protection.
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out).

3) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared.
     - Confirms branches with missing upstreams or push-equals-upstream (**flawed implementation identified by Phillip Wood**) are preserved.
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion.
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check.
     - Tests `--dry-run` functionality: output matches real deletion/protection output exactly, config is preserved, and `--dry-run` is rejected without `--delete-merged`. **Fixed to avoid modifying config during dry runs.**
     - **Order-independent stacked-branch protection**: Tests verify abort-and-clear behavior, error messages, clearing upstreams for kept branches, and handling of chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`). **However, Phillip Wood's review identifies a test discrepancy** in the "keeps the upstream chain of a surviving branch" test, where the test expects the upstream config of `lower` (a kept merged branch) to remain intact, contradicting the design goal of clearing stale configs.
     - **`--forked` filter**: Tests exact refnames, remote shorthand, shell globs, repeated arguments, composition with other filters (`--merged`, `--no-merged`), and edge cases like self-referential upstreams and missing upstreams. **Edge case resolved**: Tests now verify branches are protected if pushing them by name would update their upstream, regardless of `remote.pushDefault` configuration. **However, Phillip Wood's review identifies a flaw in the push-detection logic** that undermines this fix.
     - **Repeatable `--delete-merged` arguments**: Tests confirm the command respects multiple `--delete-merged` arguments to widen the upstream match and optional positional patterns to limit deletion scope.
     - **Flags-based API**: Tests confirm the refactoring in patches 2-4/7 introduces no behavioral changes for existing callers and correctly implements the new flags (`DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`). Patch 3/7's `DELETE_BRANCH_SKIP_UNMERGED` flag is tested in later patches that exercise the `--delete-merged` subcommand. Patch 4/7's `DELETE_BRANCH_NO_HEAD_FALLBACK` flag is exercised by the `--delete-merged` tests in patch 5/7 and later. Patch 7/7's `DELETE_BRANCH_DRY_RUN` flag is tested in the new `--dry-run` tests.
     - **CI timeout**: Tests now include the mechanical loop variable adjustment (`int i` instead of `size_t i` or `size_t i = 0`) to prevent CI timeouts, with no functional impact.

Open questions:
1) **Blocking correctness issues**:
   - **Stacked-branch protection test discrepancy**: The test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, but the design goal is to **clear stale configs for kept (merged) branches whose own upstream is being deleted**. Harald confirms the intent is to preserve the upstream chain of surviving *tips*, but the test expectation contradicts this. Resolution requires either:
     - Fixing the stacked-branch traversal logic to align with the test expectation (preserving upstream configs for kept merged branches), or
     - Updating the test to match the design goal (clearing upstream configs for kept merged branches whose own upstream is being deleted).
   - **`branch_pushes_to_upstream()` correctness**: The current implementation risks **false negatives** (deleting branches that should be preserved) or **false positives** (keeping branches that should be deleted). Resolution requires a **rewrite of the push-detection logic** to apply the *push* refspec to the local branch, apply the *fetch* refspec in reverse to the result, and compare that to the upstream branch.
2) **Follow-up work**:
   - Extending order-independent stacked-branch protection to `git branch -d` for consistency, as requested by Junio C Hamano and agreed by Harald Nordgren. This is **not a blocker for v23** but will be implemented after the series lands.
   - **Future improvement**: Implementing Phillip Wood's proposed refined heuristic for the `--forked` filter (simulating push/fetch refspecs) to better distinguish true forks from long-lived branches. This is **not a blocker for v23** and is explicitly noted as a non-blocking future improvement.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v23, including core deletion logic, safety checks, **per-branch opt-out (patch 6/7)**, `--dry-run` functionality (patch 7/7, **fixed to avoid modifying config during dry runs**), **order-independent stacked-branch protection via single-pass graph traversal**, the `--forked` filter (patch 1/7, **with mechanical fixes in v23**), **repeatable `--delete-merged` arguments and positional branch patterns**, and the flags-based API refactoring (patches 2-4/7, **with local boolean caching eliminated**). Addressed Phillip Wood's style suggestion (replaced manual loop with `strset_for_each_entry()`) and test improvements for patch 1/7 (detached HEAD removal, test reordering). **Applied all test improvements to patch 5/7 (comments, helper functions, redundant command removal, branch naming clarity) and patch 6/7 (`for-each-ref` verification via `check_branches`).** Fixed the `--dry-run` bug to avoid modifying config during dry runs, the `--forked` filter edge case by protecting branches if pushing them by name would update their upstream, and **resolved the order dependency issue in stacked-branch protection** via the single-pass graph traversal. **Resolved the CI timeout in v21 and v22 by adjusting loop variable initialization (`int i` instead of `size_t i` or `size_t i = 0`), with no functional impact.** **Has agreed to extend stacked-branch protection to `git branch -d` for consistency as a follow-up after the series lands.** Addressed Phillip Wood's feedback on patch 7/7 by rejecting the nonsensical `--dry-run --quiet` combination. **Addressed Junio C Hamano's mechanical correctness issues in patch 1/7 (style nit in `short_upstream_name()` and type mismatch in loop variable) in v23.** **Acknowledges the two blocking technical concerns (stacked-branch protection test discrepancy and `branch_pushes_to_upstream()` correctness) and plans to address them in the next revision.**
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, `--dry-run` requirements, **repeatable `--delete-merged` arguments**, and the `--forked` filter edge case. **Proposed and implemented the refined stacked-branch protection approach (abort-and-clear)**, which was adopted in v18. His feedback was instrumental in shaping the final design, including the flags-based API refactoring and the `DELETE_BRANCH_SKIP_UNMERGED` flag (added in patch 3/7). **Reviewed patch 1/7 (--forked filter) and raised only surface-level test-related points (detached HEAD removal, test reordering), which have been addressed.** Identified the flags maintenance issue in patches 2-4/7 and recommended eliminating local boolean caching to ensure flags remain the single source of truth. **Latest review of patch 5/7 suggested test improvements (comments, helper functions, redundant command removal, branch naming clarity), which have been applied.** Final review of patch 7/7 suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk, which Harald Nordgren addressed by rejecting it as nonsensical. **Surface-level review of patch 6/7 suggested a mechanical test improvement (`for-each-ref` verification via `check_branches`), which has been applied.** Acknowledged the `--forked` filter edge case fix in v19 and confirmed the **forward-looking refinement** (simulating push/fetch refspecs) is **not a blocker for v23**. **Proposed the structural refactor that resolved the order dependency issue in v20** and provided a **substantive review** of the refactor, asking for clarification about the logic changes and why the previous approach was insufficient. His questions confirm the refactor addresses the core correctness issue. **Latest review of v23 identifies two blocking technical concerns**:
   - **Stacked-branch protection test discrepancy**: The test for "keeps the upstream chain of a surviving branch" expects the upstream config of `lower` (a kept merged branch) to remain intact, but the design goal is to **clear stale configs for kept (merged) branches whose own upstream is being deleted**. This suggests either a **logic error in the stacked-branch traversal** or a **test expectation mismatch**.
   - **`branch_pushes_to_upstream()` correctness**: The current implementation risks **false negatives** (deleting branches that should be preserved) or **false positives** (keeping branches that should be deleted). The correct implementation should apply the *push* refspec to the local branch, apply the *fetch* refspec in reverse to the result, and compare that to the upstream branch.
- **Junio C Hamano**: **Endorsed the refined stacked-branch protection design (abort-and-clear)** as philosophically consistent with `git branch -d`. **Identified the blocking correctness issue (order dependency) in earlier versions and remained unconvinced by Harald Nordgren's initial fix.** The **order dependency issue has now been resolved** via Phillip Wood's proposed single-pass graph traversal, addressing Junio's correctness concerns. **Requested that the protection be extended to `git branch -d` for consistency, and Harald Nordgren has agreed to implement this as a follow-up after the series lands.** **Confirmed the design choice to index `deletable` and `protected` strsets by branch names (e.g., `main`) rather than full refnames (e.g., `refs/heads/main`) is correct and sound**, resolving his initial concern about the use of `up_short`. **Explicitly confirmed there are no outstanding issues for the series as a whole and asked whether the topic is "pretty much done," signaling it is ready for merging pending resolution of Phillip Wood's technical concerns.** **Latest review of patch 1/7 identified two mechanical correctness issues, which have been fixed in v23**:
   - **Style nit in `short_upstream_name()`**: Rewritten for clarity.
   - **Type mismatch in loop variable**: Fixed by declaring `i` as `size_t`.
   **Has now given final approval for merging the series contingent on resolving Phillip Wood's technical concerns.**

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v23 series is **blocked on two unresolved correctness issues** identified by Phillip Wood. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter and repeatable `--delete-merged` arguments establish a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work as an optional flag (e.g., `--retarget-upstreams`). **Phillip Wood's speculative `--forked=recurse` idea is noted but not pursued in this series.** The test suite now covers all edge cases, including the fixed `--forked` filter behavior and order-independent stacked-branch protection, but **Phillip Wood's review identifies two blocking issues**. **The stacked-branch protection extension to `git branch -d` will be implemented as a follow-up after the series lands.** Key files touched: `builtin/branch.c`, `ref-filter.c`, `Documentation/git-branch.adoc`, `Documentation/config/branch.txt`, and `t/t3200-branch.sh`. Patches 2-4/7 specifically touch `builtin/branch.c` to add the flags-based API (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`). Patch 6/7 adds the `branch.<name>.deleteMerged` config key and updates `Documentation/config/branch.txt`. Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` and updates `Documentation/git-branch.adoc`. The v23 update introduces no new structural changes; the only updates are mechanical fixes to patch 1/7. The series is **ready for merging pending resolution of Phillip Wood's technical concerns.**