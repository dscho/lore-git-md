AI thread summary (updated):

Thread identity: Final v17 of Harald Nordgren's `git branch --delete-merged` series, now fully implemented with all planned features and safety mechanisms, including stacked-branch protection, per-branch opt-out, and `--dry-run` preview. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls. This version includes the core `--delete-merged` command (patch 5/7), the foundational `--forked` filter (patch 1/7), a refactored flags-based deletion API (patches 2-4/7), per-branch opt-out (patch 6/7), stacked-branch protection, and `--dry-run` preview (patch 7/7).

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented in patch 1/7)
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, stacked-branch upstream preservation)
4) `--dry-run` preview capability (fully implemented in patch 7/7)

Current status:
1) **Series implementation complete**: All planned features and safety mechanisms are implemented, including stacked-branch protection, per-branch opt-out, and `--dry-run` preview.
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream while preserving branches used as upstreams for other branches.
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values.
4) **Per-branch opt-out implemented**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`.
5) **Stacked-branch protection finalized**: Branches used as upstreams for unmerged stacked branches are preserved by default with clear error messages. Uses iterative `strset` tracking to ensure full protection for chains of upstreams.
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`).
7) **`--dry-run` preview implemented**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" for each matching branch without modifying refs.
8) **Test suite complete**: Expanded to cover stacked-branch protection, per-branch opt-out, `--dry-run` functionality, and `--forked` filter behavior.
9) Resolved issues:
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`
   - `--forked` filter: Implemented with remote shorthand/glob support and exact ref matching
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch upstream preservation)
   - **Per-branch opt-out**: Implemented via `branch.<name>.deleteMerged=false` config key
   - **Stacked-branch protection**: Implemented using iterative `strset` tracking to ensure full protection for chains of upstreams
   - **`--dry-run` preview**: Implemented in patch 7/7, providing zero-risk preview of deletion candidates
   - Test coverage: Comprehensive integration tests verifying deleted/preserved branches, opt-out behavior, `--dry-run` functionality, stacked-branch protection, and `--forked` filter behavior

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()` (patch 1/7)
   - Verifies tip reachability from upstream (skips unmerged branches)
   - Checks push divergence (preserves branches where push == upstream)
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7)
   - Never touches checked-out branches (any worktree)
   - Supports `--dry-run` preview mode (fully implemented in patch 7/7)
   - **Stacked-branch protection**: Preserves branches used as upstreams for other branches with clear error messages

2) Implementation highlights:
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out)
     - Checked during candidate filtering in `delete_merged_branches()`
     - Non-quiet message explains opt-out skip
     - Ignored by traditional `git branch -d` (continues to use normal merge check)
     - Documentation updates cover config key and safety conditions
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames or accepts shell-style globs
     - `filter_forked_match()` checks each branch's upstream against registered patterns
   - **Flags-based API (patches 2-4/7)**:
     - Replaces boolean arguments with `unsigned int flags` and enum values
     - Adds `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using `--forked` machinery
     - Applies safety checks and stacked-branch protection
     - Uses iterative `strset` tracking to preserve chains of upstreams
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API
     - Prints "Would delete branch <name>" for each matching branch
     - Rejects `--dry-run` without `--delete-merged`
     - Output matches real deletion output exactly
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch upstream preservation
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out)
   - `--dry-run` implementation: Prints preview messages, exits without ref modification

3) Safety architecture:
   - Never deletes checked-out branches (any worktree)
   - Preserves branches with missing upstreams
   - Preserves branches where push destination equals upstream
   - Silently skips unmerged branches (non-fatal)
   - Per-branch opt-out mechanism (`branch.<name>.deleteMerged=false`)
   - `--dry-run` preview capability (fully implemented in patch 7/7)
   - **Stacked-branch protection**: Preserves branches used as upstreams for other branches with clear error messages

4) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared
     - Confirms branches with missing upstreams or push-equals-upstream are preserved
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check
     - Tests `--dry-run` functionality: output matches real deletion output exactly
     - **Stacked-branch protection**: Verifies branches used as upstreams are preserved, chains of upstreams are fully protected
     - **`--forked` filter**: Tests exact refnames, remote shorthand, shell globs, composition with other filters

Open questions:
- **Final review**: The series is now ready for final review with no known blocking issues remaining. All high-weight feedback (including Phillip Wood's stacked-branch safety concerns, API consistency goals, per-branch opt-out design, and `--dry-run` requirements) has been addressed.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v17, including core deletion logic, safety checks, per-branch opt-out, `--dry-run` functionality (patch 7/7), stacked-branch protection, and test suite improvements. Adopted the simplified stacked-branch protection (preserve-on-dependency) to avoid edge cases while preserving backward compatibility. Introduced the `--forked` filter and refactored the internal deletion API for consistency.
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, and `--dry-run` requirements. Feedback fully addressed in v17.
- **Junio C Hamano**: Initially skeptical of stacked-branch protection, now fully endorses the preserve-and-notify default as the safer, simpler approach. This resolution clears the way for final review.
- **Johannes Sixt**: Contributed to CLI design decisions (medium weight).

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v17 series is mature and ready for final review, with all safety mechanisms (including stacked-branch protection, per-branch opt-out, and `--dry-run` preview) implemented and tested. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter establishes a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work.