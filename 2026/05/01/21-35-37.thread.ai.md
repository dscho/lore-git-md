AI thread summary (updated):

Thread identity: v9 of Harald Nordgren's 5-patch series adding `git branch --prune-merged` for safe automated local branch cleanup when upstream branches are deleted. The related `fetch.pruneLocalBranches` concept remains a separate, unattended proposal.

Core problem or goal: Provide safe, automated local branch cleanup through explicit `git branch` commands with upstream-reachability semantics. The series: `--forked <remote>` (show branches tracking a remote), `--prune-merged` (delete local branches whose tip is reachable from their configured upstream remote-tracking branch), `branch.<name>.pruneMerged` config (per-branch opt-out), and `--all-remotes` (convenience flag applying `--forked` or `--prune-merged` to all configured remotes).

Current status: v9 is complete; the cover letter and all five patches have been sent. The series resolves the sole remaining objection from Junio C Hamano in v8 (`--force` should not bypass the mergedness check). Patch 2 is a pure refactoring that adds `warn_only` and `n_not_merged` parameters to `check_branch_commit()` and `delete_branches()`, preparing the deletion machinery for bulk use with no behavioral change to existing call sites (all pass 0/NULL); the diff is 29 lines in a single file, and the patch is unchanged from v8. Patches 1 and 2 are unchanged from v8. Patch 3 drops `--force` from the synopsis and changes the error message; the reachability check is always enforced. Patches 4 and 5 update tests and parameter passing to match. The series is substantively accepted in principle; v9's mechanical fix addresses what was explicitly requested.

Key technical details:
- Patch 1 (v9, unchanged from v8): `--forked <remote>`. Lists local branches whose configured upstream (`branch.<name>.merge`) falls within the given remote or remote-tracking branch. Accepts remote names (e.g. `origin`) or specific refs (e.g. `origin/master`); multiple arguments are unioned. Implementation in `builtin/branch.c` via `list_forked_branches()`, `parse_forked_args()`, `branch_is_forked()`, `collect_forked_branch()` -- about 110 lines of C. Six tests in `t/t3200-branch.sh` cover setup, matching by remote name, matching by specific tracking branch, union behavior, rejection of unknown arguments, and required-argument check.
- Patch 2 (v9, unchanged from v8): Refactors `check_branch_commit()` and `delete_branches()` to accept `warn_only` (suppresses multi-line per-branch refusal to a single-line warning) and `n_not_merged` (incremented for each skipped branch for bulk summary). No behavioral change to existing callers.
- Patch 3 (v9): `--force` no longer bypasses the mergedness check; the reachability check is always enforced. Synopsis drops `[-f]`. Error message changed from "re-run with --force to delete it anyway" to "delete it with 'git branch -D' if you are sure". Tests for `--prune-merged --force` removed; remaining tests that used `--force` in non-bypass scenarios (protect-the-default-branch tests) now use `--prune-merged` without `--force`.
- Patch 4 (v9): Drops its `--force` test.
- Patch 5 (v9): Updates synopsis and parameter passing accordingly.
- Safety model (unchanged from v8, approved by Junio): A local branch is safe to delete only if its tip is reachable from its configured upstream remote-tracking branch (`branch.<name>.merge`). Three safeguards: not checked out in any worktree, resolvable upstream, and if upstream is the remote's default branch the local branch must not share the same name. Branches with unresolvable upstream refs are silently skipped.

Open questions and loose ends: None for the `--prune-merged` series. The separate `fetch.pruneLocalBranches` concept (Harald floated in v8 response) remains a preliminary, unattended proposal -- no review or discussion has occurred on it.

Participant positions:
- Harald Nordgren: Author. v8's safety model fully accepted by Junio; v9 addresses Junio's narrow objection precisely, without scope creep. The series is in its final state. Independently floated the separate `fetch.pruneLocalBranches` concept.
- Junio C Hamano: Strongly approved v8 safety model ("matches my understanding...a lot better"). Objected only to `--force` bypassing the mergedness check. That objection is fully resolved in v9.

Related work: Uses `fetch.prune` infrastructure. The `branch.<name>.pruneMerged` config parallels existing `branch.<name>` patterns. The `--all-remotes` design follows similar "all remotes" flags elsewhere in Git.