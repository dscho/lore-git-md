AI thread summary:

Thread identity: v8 of Harald Nordgren's series (5 patches) adding `git branch --prune-merged` for safe automated local branch cleanup when upstream branches are deleted.

Core problem or goal: Provide safe, automated local branch cleanup through explicit `git branch` commands with upstream-reachability semantics. The series consists of: `--forked <remote>` (show branches tracking a remote), `--prune-merged` (delete local branches whose tip is reachable from their configured upstream remote-tracking branch), `branch.<name>.pruneMerged` config (per-branch opt-out), and `--all-remotes` (convenience flag to apply `--prune-merged` or `--forked` to all remotes).

Current status: v8 is complete (all 5 patches sent). The entire series has been submitted. Junio C Hamano has not yet responded to v8; the previous v7 received fundamental design objections from Junio about the safety model, which were completely addressed in the v8 redesign.

Key technical details:
- Patch 1: `--forked <remote>` infrastructure, ~110 lines.
- Patch 2: `warn_only`/`n_not_merged` parameters to `delete_branches()`, ~20 lines, mechanical refactoring.
- Patch 3: `prune_merged_branches()` core logic, ~140 lines, redesigned around upstream-reachability safety model.
- Patch 4: `branch.<name>.pruneMerged` config option, ~25 lines, `--force` does NOT override it.
- Patch 5 (just received): `--all-remotes` flag, ~80 lines. Uses `for_each_remote()` to enumerate all configured remotes; additive with explicit remote arguments; rejected when used without `--forked` or `--prune-merged`. Implements via `collect_remote_name()` callback into a string list merged with explicit `<remote>` arguments in `collect_forked_set()`.
- Files and subsystems: `builtin/branch.c`, branch utilities header/implementation, `t/t3200-branch.sh`, `Documentation/git-branch.txt`, `Documentation/config/branch.txt`.

Safety model (v8 redesign, patch 3): A local branch is safe to delete only if its tip is reachable from its configured upstream remote-tracking branch (`branch.<name>.merge`). Three safeguards: not checked out in any worktree, resolvable upstream, and if upstream is the remote's default branch the local branch must not share the same name. Branches with unresolvable upstream refs (remote-tracking branch was deleted) are silently skipped. Previous push-ref existence check and upstream-gone-fallback-to-remote-HEAD logic from v7 are removed entirely.

Open questions and loose ends:
- The only remaining question is whether Junio and other reviewers find the upstream-reachability safety model acceptable. No objections have been raised yet on v8.
- The separate `fetch.pruneLocalBranches` concept remains posted in the thread but is not part of this series and has not been addressed.

Participant positions:
- Harald Nordgren: Author. Complete v8 redesign submitted one day after Junio's v7 concerns; strong signal of engagement.
- Junio C Hamano: Raised fundamental issues on v7; has not yet responded to v8.
- No other substantive review on v8.

Related work: Uses `fetch.prune` infrastructure. The `branch.<name>.pruneMerged` config parallels existing `branch.<name>` patterns. The `--all-remotes` design follows similar "all remotes" flags elsewhere in Git.