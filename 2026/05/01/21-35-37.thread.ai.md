AI thread summary:

Thread identity: v8 of Harald Nordgren's series (5 patches) adding `git branch --prune-merged` for safe automated local branch cleanup when upstream branches are deleted. The separate `fetch.pruneLocalBranches` patch from the thread is not part of this series.

Core problem or goal: Provide safe, automated local branch cleanup through explicit `git branch` commands. Components: `--prune-merged`, per-branch opt-out via `branch.<name>.pruneMerged`, multi-remote support via `--all-remotes`, and refined merged-ness checking using upstream-reachability semantics.

Current status: v8 has been submitted. The core design has been fundamentally redesigned in response to Junio C Hamano's v7 concerns. The safety model now checks whether a local branch's tip is reachable from its configured upstream remote-tracking branch -- a much more intuitive and defensible criterion. Previous fallback-to-remote-default logic and the `no_head_fallback` parameter have both been removed. Patch 2 (unchanged from v7) introduces `warn_only` and `n_not_merged` parameters to `delete_branches()` and `check_branch_commit()` so that bulk pruning can suppress per-branch error messages and instead report a summarized count. This is a clean, mechanical refactoring -- about 20 lines of C in `builtin/branch.c` -- with all existing call sites passing `0` and `NULL` and unaffected. If this design holds up, the series may be on track to progress beyond `seen`. Junio has not yet responded to v8.

Key technical details:
- Files and subsystems: `builtin/branch.c`, branch utilities header/implementation, `t/t3200-branch.sh`, `Documentation/git-branch.txt`, `Documentation/config/branch.txt`.
- Patch 1 (unchanged from v7): `--forked <remote>` option added in `parse_forked_args()` and `branch_is_forked()`. This infrastructure function enumerates local branches whose configured upstream (`branch.<name>.remote` and `branch.<name>.merge`) falls within a given remote or tracking ref. About 110 lines of new code across `builtin/branch.c`, documentation, and tests. Takes one or more arguments (configured remote names or specific `refs/remotes/` refs); dies on unknown arguments. Covers basic scenarios: list branches tracking `origin`, filter to single tracking ref, union of multiple arguments, rejection of unknown remote, required-argument error. No behavioral change for users not using `--forked`.
- Patch 2 (unchanged from v7): `warn_only` parameter to `delete_branches()` -- downgrades multi-line advice about `-D` to a one-line warning per branch, `n_not_merged` output parameter lets caller tally refusals and report a summarized count. About 20 lines, all existing callers pass `0` and `NULL`.
- Patch 3 (redesigned in v8): `prune_merged_branches()` -- core prune logic. Safety check: a branch is safe to delete only if its tip is reachable from its configured upstream remote-tracking branch (`branch.<name>.merge`). If the upstream ref cannot be resolved locally, the branch is skipped with a message. Previous push-ref existence check and upstream-gone-fallback-to-remote-HEAD logic are removed entirely. No `no_head_fallback` parameter. Test suite rewritten for new semantics. Documentation rewritten to describe upstream-reachability rather than push-ref checking.
- Patch 4 (updated for v8): `branch.<name>.pruneMerged` config for per-branch opt-out. Mechanics unchanged from v7 -- config check in loop, `--force` does not override opt-out. Documentation and tests adjusted for v8 core semantics.
- Patch 5 (updated for v8): `--all-remotes` flag. Mechanics unchanged from v7. Tests include verifying standalone `--all-remotes` rejection and multi-remote cleanup.

Open questions and loose ends:
- The only remaining question is whether Junio and other reviewers find the new upstream-reachability model acceptable. No objections have been raised yet on v8.
- The separate `fetch.pruneLocalBranches` patch remains posted in the thread but has not been addressed by this v8 and is not part of the series. Its safety concerns are not affected by the v8 redesign.

Participant positions:
- Harald Nordgren: Author. Responded to Junio's v7 concerns with a complete redesign one day later. Strong signal of engagement with maintainer feedback. v8 patch 1 (`--forked`) is clean infrastructure code, patch 2 is a straightforward mechanical refactoring.
- Junio C Hamano: Raised fundamental design concerns on v7 about the safety model and `no_head_fallback` parameterization. The v8 redesign directly addresses both points. Has not yet responded to v8.
- No other substantive review this round.

Related work: Builds on `fetch.prune`. The `fetch.pruneLocalBranches` concept remains separate and controversial. The `warn_only`/`n_not_merged` mechanism in patch 2 is a generic utility that could serve other bulk-deletion callers in the future.