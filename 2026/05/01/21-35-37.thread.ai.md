AI thread summary (updated):

Thread identity: Final v17 of Harald Nordgren's `git branch --delete-merged` series, now technically complete with all safety mechanisms implemented, including stacked-branch protection. The series provides safe automated local branch cleanup with comprehensive safety checks, filtering capabilities, and `--dry-run` preview functionality. This version introduces the `--forked` filter as the foundational building block for branch selection and refactors the internal deletion API for consistency.

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented and tested in patch 1/7, serving as the foundation for `--delete-merged`)
2) Flag-based API for deletion behavior (refactored in patch 2/7 with `DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`; extended in later patches with `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms to prevent accidental data loss (checked-out branches, missing upstreams, push divergence, per-branch opt-out via `branch.<name>.deleteMerged`, **stacked-branch upstream preservation**)
4) `--dry-run` preview capability (fully implemented and tested)

Current status:
1) **Series implementation complete**: All features and safety mechanisms are now implemented, including stacked-branch protection.
2) **API refactoring complete**: Patch 2/7 introduces a flags-based API for `delete_branches()`, replacing boolean arguments (`force`, `quiet`) with an `unsigned int flags` parameter and enum values (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`). Patch 3/7 extends this API with `DELETE_BRANCH_SKIP_UNMERGED` to enable silent skipping of unmerged branches during bulk deletion. This refactoring is purely mechanical with no behavior changes and serves as a clean foundation for later patches.
3) **Stacked-branch protection finalized**: Branches used as upstreams for unmerged stacked branches (via `branch.<name>.merge` config) will **abort deletion by default** with a clear error message (e.g., "Cannot delete branch 'feature1' because 'feature2' depends on it"). This simpler approach avoids recursion/self-reference edge cases while preserving backward compatibility for future retargeting enhancements (e.g., `--retarget-upstreams`).
4) **Test suite complete**: Expanded to cover stacked-branch protection, verifying that branches used as upstreams are preserved and that chains of upstreams (e.g., `b1→b2→b3`) are fully protected.
5) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, which lists only branches whose configured upstream matches a given ref, remote name, or shell glob. This filter is composable with existing filters (`--merged`, `--no-merged`) and serves as the foundation for `--delete-merged` in patch 5/7.
6) Resolved issues:
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`, now including `DELETE_BRANCH_SKIP_UNMERGED` for silent skipping of unmerged branches.
   - `--forked` filter: Implemented with remote shorthand/glob support and exact ref matching (patch 1/7)
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, **stacked-branch upstream preservation**)
   - **Stacked-branch protection**: Implemented using `refs_for_each_branch_ref()` and a `strset` to track deletable branches, iterating until the set stabilizes. This ensures full protection for chains of upstreams.
   - Test coverage: Comprehensive integration tests verifying deleted/preserved branches, realistic workflows, exact output matching, opt-out behavior, `--dry-run` functionality, and **stacked-branch protection**.

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()` (introduced in patch 1/7)
   - Verifies tip reachability from upstream (skips unmerged branches)
   - Checks push divergence (preserves branches where push == upstream)
   - Respects `branch.<name>.deleteMerged` config opt-out (checked after safety checks but before deletion)
   - Never touches checked-out branches (any worktree)
   - Supports `--dry-run` preview mode (fully implemented, rejects use without `--delete-merged`)
   - **Stacked-branch protection**: Aborts deletion if the branch is used as an upstream for any other branch (via `branch.<name>.merge` config), with a clear error message. Uses `refs_for_each_branch_ref()` and a `strset` to track deletable branches, iterating until the set stabilizes.

2) Implementation highlights:
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` function in `ref-filter.c` resolves ref arguments to full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) or accepts shell-style globs (e.g., `origin/*`).
     - `filter_forked_match()` checks each branch’s upstream against registered patterns using exact matching for full refnames and `wildmatch` for globs.
     - CLI plumbing in `builtin/branch.c` parses `--forked` arguments and passes them to the ref filter.
     - Test coverage verifies exact refnames, remote shorthand, shell globs, repeated arguments, composition with other filters, and error handling.
   - **Flags-based API (patches 2/7 and 3/7)**:
     - Patch 2/7 replaces boolean arguments (`force`, `quiet`) in `delete_branches()` with an `unsigned int flags` parameter and enum values (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`).
     - Patch 3/7 adds `DELETE_BRANCH_SKIP_UNMERGED` to enable silent skipping of unmerged branches during bulk deletion, plumbed through `check_branch_commit()` and `delete_branches()`.
     - No behavior changes for existing callers; the function body extracts flags into local booleans with the same names.
     - Updates all call sites in `builtin/branch.c` to pass the new flags.
     - Prerequisite for later patches, which add `DELETE_BRANCH_NO_HEAD_FALLBACK` and `DELETE_BRANCH_DRY_RUN`.
   - Unified flags-based API: `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`
   - Memory management: Single `strbuf` reuse for efficiency (including config key lookup)
   - CLI plumbing: Natural integration with existing `--merged`/`--no-merged` filters
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, **stacked-branch upstream preservation**
   - Config integration: `branch.<name>.deleteMerged` (false = opt-out, true/default = eligible)
   - `--dry-run` implementation: `OPT_BOOL` in `builtin/branch.c`, passes `DELETE_BRANCH_DRY_RUN` flag, prints "Would delete branch <name>" per branch, exits without ref modification
   - **Stacked-branch detection**: Identifies branches used as upstreams via `branch.<name>.merge` config (e.g., `feature2` → `feature1`).
   - **Stacked-branch protection**: Uses `refs_for_each_branch_ref()` to collect upstreams and a `strset` to track deletable branches, iterating until the set stabilizes. This ensures full protection for chains of upstreams (e.g., `b1→b2→b3`).

3) Safety architecture:
   - Never deletes checked-out branches (any worktree)
   - Preserves branches with missing upstreams
   - Preserves branches where push destination equals upstream (e.g., `main` tracking `origin/main`)
   - Silently skips unmerged branches (non-fatal) when `DELETE_BRANCH_SKIP_UNMERGED` is set
   - Per-branch opt-out mechanism (addresses iterative topic branches)
   - `--dry-run` preview capability (fully implemented, mirrors actual deletion logic)
   - **Stacked-branch protection**: Aborts deletion if the branch is used as an upstream for any other branch, with a clear error message.

4) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared
     - Confirms branches with missing upstreams or push-equals-upstream are preserved
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion, but `git branch -d` still works
     - Tests `--dry-run` functionality: output matches real deletion output, option rejected when used alone
     - **Stacked-branch protection**: Verifies branches used as upstreams are preserved, chains of upstreams are fully protected, and error messages are clear.
     - **`--forked` filter (patch 1/7)**: Tests exact refnames, remote shorthand, shell globs, repeated arguments, composition with other filters, and error handling.
     - **Flags-based API (patches 2/7 and 3/7)**: No behavior changes; tests confirm existing functionality is preserved and that `DELETE_BRANCH_SKIP_UNMERGED` enables silent skipping of unmerged branches.

Open questions:
- **Final review**: The series is now ready for final review with no known blocking issues remaining. All high-weight feedback (including Phillip Wood's stacked-branch safety concerns and API consistency goals) has been addressed.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v17, including core deletion logic, safety checks, per-branch opt-out, `--dry-run` functionality, **stacked-branch protection**, and test suite improvements. Adopted the **simplified stacked-branch protection** (abort-on-dependency) to avoid edge cases while preserving backward compatibility for future retargeting enhancements. Introduced the `--forked` filter in patch 1/7 as the foundation for `--delete-merged` and refactored the internal deletion API in patches 2/7 and 3/7 for consistency.
- **Phillip Wood**: Provided high-weight feedback on API consistency and **stacked-branch safety**, successfully advocating for preserving branches used as upstreams for unmerged stacked branches to avoid breaking `@{u}` references and implicit `git rebase` workflows. Feedback fully addressed in v17.
- **Junio C Hamano**: Initially skeptical of stacked-branch protection, now fully endorses the **abort-and-notify default** as the safer, simpler approach. This resolution clears the way for final review.
- **Johannes Sixt**: Contributed to CLI design decisions (medium weight).

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v17 series is mature and ready for final review, with all safety mechanisms (including stacked-branch protection) implemented and tested. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter (patch 1/7) establishes a composable foundation for future branch management features. Retargeting (inspired by GitHub’s stacked-PR automation) is deferred for future work.