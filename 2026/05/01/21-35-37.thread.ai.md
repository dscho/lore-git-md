AI thread summary (updated):

Thread identity: Final v17 of Harald Nordgren's `git branch --delete-merged` series, now fully aligned with maintainer-approved design principles. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls, including **stacked-branch protection via abort-and-notify**, per-branch opt-out, and `--dry-run` preview. This version includes the core `--delete-merged` command (patch 5/7), the foundational `--forked` filter (patch 1/7), a refactored flags-based deletion API (patches 2-4/7), per-branch opt-out (patch 6/7), **stacked-branch protection via abort-and-notify** (design finalized), and `--dry-run` preview (patch 7/7).

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented in patch 1/7)
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, **stacked-branch protection via abort-and-notify**)
4) `--dry-run` preview capability (fully implemented in patch 7/7)

Current status:
1) **Series implementation mostly complete**: All planned features and safety mechanisms are implemented, including per-branch opt-out and `--dry-run` preview. **Stacked-branch protection design finalized** (abort-and-notify), but **implementation requires revision** to replace the broken upstream config clearing approach.
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream while preserving branches used as upstreams for unmerged branches via **abort-and-notify** (e.g., "Cannot delete 'feature1' because 'feature2' depends on it").
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values.
4) **Per-branch opt-out implemented**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`.
5) **Stacked-branch protection design**: **Finalized as abort-and-notify** (Junio C Hamano's confirmation). The command now **fails with a clear error** when attempting to delete a branch used as an upstream for another branch, rather than clearing upstream configs. This aligns `--delete-merged` with `git branch -d` behavior and simplifies edge-case handling. **Implementation must be revised** to replace the broken upstream config clearing logic.
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`).
7) **`--dry-run` preview implemented**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch without modifying refs. The preview reflects stacked-branch protection edge cases.
8) **Test suite complete**: Expanded to cover stacked-branch protection (abort-and-notify), per-branch opt-out, `--dry-run` functionality, and `--forked` filter behavior. Test cases for upstream dependency messages and self-referential upstreams are now included.
9) Resolved issues:
   - **Stacked-branch protection design**: Finalized as abort-and-notify (Junio's confirmation), rejecting upstream config clearing.
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`.
   - `--forked` filter: Implemented with remote shorthand/glob support and exact ref matching.
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch protection via abort-and-notify).
   - Per-branch opt-out: Implemented via `branch.<name>.deleteMerged=false` config key.
   - `--dry-run` preview: Implemented in patch 7/7, providing zero-risk preview of deletion candidates and protection actions.
10) **Implementation bugs**:
    - **Stacked-branch protection**: The current implementation (upstream config clearing) is **confirmed broken** and **must be replaced** with abort-and-notify logic. Harald Nordgren will revise this in the next iteration.

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()` (patch 1/7).
   - Verifies tip reachability from upstream (skips unmerged branches).
   - Checks push divergence (preserves branches where push == upstream).
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7).
   - Never touches checked-out branches (any worktree).
   - Supports `--dry-run` preview mode (fully implemented in patch 7/7).
   - **Stacked-branch protection**: **Now finalized as abort-and-notify** (fails with error if branch is used as upstream for another branch). The current implementation (upstream config clearing) is **broken and must be replaced**.

2) Implementation highlights:
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out).
     - Checked during candidate filtering in `delete_merged_branches()`.
     - Non-quiet message explains opt-out skip.
     - Ignored by traditional `git branch -d` (continues to use normal merge check).
   - **Stacked-branch protection (abort-and-notify)**:
     - **Design intent**: Fail with error if attempting to delete a branch used as an upstream for another branch (e.g., "Cannot delete 'feature1' because 'feature2' depends on it").
     - **Current bug**: Implementation incorrectly uses upstream config clearing instead of abort-and-notify. Must be revised to match the finalized design.
     - Handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) by treating the entire chain as sacrosanct.
     - Applies uniformly to both `--delete-merged` and `-d` for consistency.
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames or accepts shell-style globs.
     - `filter_forked_match()` checks each branch's upstream against registered patterns.
   - **Flags-based API (patches 2-4/7)**:
     - Replaces boolean arguments with `unsigned int flags` and enum values.
     - Adds `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`.
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using `--forked` machinery.
     - Applies safety checks and stacked-branch protection (abort-and-notify).
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API.
     - Prints "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch.
     - Rejects `--dry-run` without `--delete-merged`.
     - Output matches real deletion/protection output exactly.
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, stacked-branch protection (abort-and-notify).
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out).
   - `--dry-run` implementation: Prints preview messages, exits without ref modification.

3) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared.
     - Confirms branches with missing upstreams or push-equals-upstream are preserved.
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion.
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check.
     - Tests `--dry-run` functionality: output matches real deletion/protection output exactly.
     - **Stacked-branch protection**: Tests verify abort-and-notify behavior, error messages, and handling of chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`).
     - **`--forked` filter**: Tests exact refnames, remote shorthand, shell globs, composition with other filters.

Open questions:
1. **Critical implementation bug**: The stacked-branch protection logic must be revised to replace upstream config clearing with abort-and-notify. Harald Nordgren will address this in the next iteration.
2. **Edge-case robustness**: Ensure abort-and-notify handles self-referential upstreams (`branch@{u} = branch`) and complex dependency chains without false positives.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v17, including core deletion logic, safety checks, per-branch opt-out, `--dry-run` functionality (patch 7/7), and **stacked-branch protection via upstream config clearing** (implementation contains critical bug). **Will revise stacked-branch protection** to use abort-and-notify as finalized by Junio.
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, and `--dry-run` requirements. Initially proposed upstream config clearing, but **endorsed abort-and-notify** after Junio's clarification. **Identified critical bug in stacked-branch protection implementation** (upstream config clearing is broken and contradicts the finalized design).
- **Junio C Hamano**: **Finalized stacked-branch protection design as abort-and-notify**, emphasizing philosophical consistency with `git branch -d`. The series is **not yet ready for final review** due to the critical implementation bug.

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v17 series is **not yet ready for integration** due to the critical bug in the stacked-branch protection implementation. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter establishes a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work as an optional flag (e.g., `--retarget-upstreams`).