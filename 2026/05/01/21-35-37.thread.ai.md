AI thread summary (updated):

Thread identity: Final v18 of Harald Nordgren's `git branch --delete-merged` series, **implementation-complete and ready for merging** with all design questions resolved. The series provides safe automated local branch cleanup with comprehensive filtering and safety controls, including **refined stacked-branch protection (abort-and-clear)**, per-branch opt-out, `--dry-run` preview, and the foundational `--forked` filter. This version includes the core `--delete-merged` command (patch 5/7), the `--forked` filter (patch 1/7), a refactored flags-based deletion API (patches 2-4/7), **per-branch opt-out via `branch.<name>.deleteMerged=false` (patch 6/7)**, **refined stacked-branch protection (abort-and-clear)**, and `--dry-run` preview (patch 7/7).

Core problem/goal: Enable safe automated local branch cleanup through a new `--delete-merged` command with precise control via:
1) `--forked` filtering for branch selection (fully implemented in patch 1/7, supporting exact refnames, remote shorthand, shell-style globs, and **`<branch>` argument support**)
2) Flags-based API for deletion behavior (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`, `DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`)
3) Multiple safety mechanisms (checked-out branches, missing upstreams, push divergence, **per-branch opt-out via `branch.<name>.deleteMerged=false`**, **refined stacked-branch protection (abort-and-clear)**)
4) `--dry-run` preview capability (fully implemented in patch 7/7)

Current status:
1) **Series implementation complete**: All planned features and safety mechanisms are implemented, including per-branch opt-out, `--dry-run` preview, **refined stacked-branch protection (abort-and-clear)**, and **`<branch>` argument support for `--delete-merged`**.
2) **Core command implemented**: Patch 5/7 adds the `--delete-merged` subcommand, which safely deletes local branches merged into their upstream while preserving branches used as upstreams for unmerged branches via **refined stacked-branch protection (abort-and-clear)**. The command now **fails to delete a branch if it is used as an upstream for an unmerged branch**, but **clears the upstream config for kept (merged) branches whose own upstream is being deleted**, avoiding broken dependency chains while cleaning up stale configs. **The command now supports `<branch>` arguments to limit deletion candidates, reusing the `--forked` filter machinery for consistency.**
3) **API refactoring complete**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, replacing boolean arguments with an `unsigned int flags` parameter and enum values. **The maintenance risk in patches 2-4/7 (two sources of truth for flags) has been acknowledged by Harald Nordgren, who has agreed to replace all local boolean uses with direct flag checks (`flags & DELETE_BRANCH_FORCE`, etc.) before merging. This is a mechanical fix that does not affect functionality.**
4) **Per-branch opt-out implemented**: Patch 6/7 adds `branch.<name>.deleteMerged` config key to exempt specific branches from automated cleanup while allowing explicit deletion via `git branch -d`. The opt-out is checked during candidate filtering in `delete_merged_branches()` and produces a non-quiet skip message. **Phillip Wood's surface-level review of patch 6/7 suggested a mechanical test improvement (using `git for-each-ref` instead of `rev-parse` for branch state verification), which aligns with the series' broader testing style but does not affect functionality.**
5) **Stacked-branch protection design and implementation**: **Finalized and implemented as refined abort-and-clear**. The logic handles chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`) by treating the upstreams of unmerged branches as sacrosanct while clearing stale configs for kept (merged) branches.
6) **Filter foundation established**: Patch 1/7 introduces the `--forked` filter for `git branch --list`, composable with existing filters (`--merged`, `--no-merged`). The filter supports exact refnames (e.g., `refs/remotes/origin/main`), remote shorthand (e.g., `origin`), shell-style globs (e.g., `origin/*`), and now **`<branch>` argument support for `--delete-merged`**. **Phillip Wood's review of patch 1/7 raised only surface-level test-related points (detached HEAD removal, test reordering), which Harald Nordgren has addressed. No substantive issues remain.**
7) **`--dry-run` preview implemented**: Patch 7/7 adds `--dry-run` support to `--delete-merged`, printing "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch without modifying refs. The output matches the real deletion/protection output exactly, and `--dry-run` is rejected without `--delete-merged`. **Phillip Wood's final review of patch 7/7 suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk (though no code change was requested). The test coverage for `--dry-run` was confirmed to be well-structured.**
8) **Test suite complete**: Expanded to cover refined stacked-branch protection (abort-and-clear), per-branch opt-out, `--dry-run` functionality, `--forked` filter behavior, `<branch>` argument support, and edge cases like self-referential upstreams and missing upstreams. **Phillip Wood's latest review of patch 5/7 suggested test improvements (comments, helper functions, redundant command removal, branch naming clarity), which Harald Nordgren has applied. The premature `--dry-run` test case has been moved to patch 7/7. The test suite now uses clearer branch names (`local-to-delete` instead of `on-local`, `main` instead of `mainline`) and explicitly configures `branch.main.pushRemote` to prevent `main` from being treated as a forked branch, improving readability without altering functionality. Patch 6/7's tests now include `git for-each-ref` verification as suggested by Phillip Wood, aligning with the series' broader testing style.**
9) Resolved issues:
   - **Stacked-branch protection design and implementation**: Finalized as refined abort-and-clear (Phillip Wood's proposal), replacing the v17 all-or-nothing abort-and-notify approach. The command now keeps only the branches needed by unmerged branches and clears upstream configs for kept (merged) branches whose own upstream is being deleted.
   - API consistency: Unified flags-based API for both `delete_branches()` and `delete_merged_branches()`. **The maintenance risk in patches 2-4/7 (two sources of truth for flags) has been acknowledged and will be fixed before merging.**
   - `--forked` filter: Implemented with remote shorthand/glob support, exact ref matching, and composition with other filters. **Phillip Wood's review confirmed the implementation is sound, with only stylistic test suggestions (detached HEAD removal, test reordering) now addressed.**
   - **`<branch>` argument support**: Added to `--delete-merged` to limit deletion candidates, reusing the `--forked` filter machinery for consistency. This addresses Phillip Wood's usability concern about the lack of filtering flexibility in the original design.
   - Safety architecture: Core deletion logic operational with all safety checks (checked-out branches, missing upstreams, push divergence, per-branch opt-out, refined stacked-branch protection).
   - Per-branch opt-out: Implemented via `branch.<name>.deleteMerged=false` config key, with documentation and tests.
   - `--dry-run` preview: Implemented in patch 7/7, providing zero-risk preview of deletion candidates and protection actions. **Phillip Wood's final review suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk, but no functional changes were required.**
   - **Test improvements**: All suggested changes (comments, helper functions, redundant command removal, branch naming clarity, `for-each-ref` verification) have been applied. The test suite now explicitly configures `branch.main.pushRemote` to prevent `main` from being treated as a forked branch, exposing a subtle edge case where branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked when `remote.pushDefault` is set. This is noted as a known limitation but does not affect the core logic or safety checks.
10) **Implementation refinements**:
    - **Stacked-branch protection**: Revised to use abort-and-clear, replacing the v17 abort-and-notify logic. The command now fails to delete a branch if it is used as an upstream for an unmerged branch, but clears the upstream config for kept (merged) branches whose own upstream is being deleted. This is implemented in `spare_stacked_bases()` and `spare_stacked_base()`.
    - **Style cleanup**: Replaced manual loop over `strset` with `strset_for_each_entry()` (Phillip Wood's suggestion).
    - **API refactoring**: Patches 2-4/7 introduce a flags-based API for `delete_branches()`, enabling future extensibility. **The maintenance risk (two sources of truth for flags) has been acknowledged and will be fixed before merging.**
    - **Test improvements**: Phillip Wood's latest review suggested adding comments to helper functions, renaming them for clarity (e.g., `create_branch_with_upstream`), simplifying test patterns (e.g., using `git checkout -b --track`), replacing manual config checks with `git config --get-regexp`, introducing a `check_branches` helper for `for-each-ref` comparisons, removing redundant commands (e.g., `git fetch` after a push), and improving branch naming clarity. These changes have been applied. The premature `--dry-run` test case has been moved to patch 7/7. **Patch 1/7's test improvements (detached HEAD removal, test reordering) have been addressed. Patch 6/7's tests now use `git for-each-ref` for branch state verification as suggested.**

Key technical details:
1) `--delete-merged` core behavior:
   - Filters candidates using `ref_filter_forked_add()` (patch 1/7), supporting exact refnames, remote shorthand, shell-style globs, and **`<branch>` arguments**.
   - Verifies tip reachability from upstream (skips unmerged branches).
   - Checks push divergence (preserves branches where push == upstream).
   - Respects `branch.<name>.deleteMerged=false` config opt-out (patch 6/7).
   - Never touches checked-out branches (any worktree).
   - Supports `--dry-run` preview mode (fully implemented in patch 7/7).
   - **Refined stacked-branch protection (abort-and-clear)**: Fails to delete a branch if it is used as an upstream for an unmerged branch, but clears the upstream config for kept (merged) branches whose own upstream is being deleted. This avoids breaking dependency chains while cleaning up stale configs.

2) Implementation highlights:
   - **Per-branch opt-out (patch 6/7)**:
     - New `branch.<name>.deleteMerged` config key (false = opt-out).
     - Checked during candidate filtering in `delete_merged_branches()`.
     - Non-quiet message explains opt-out skip (e.g., "Skipping 'kept' (branch.kept.deleteMerged is false)").
     - Ignored by traditional `git branch -d` (continues to use normal merge check).
     - Documentation added to `branch.adoc` and `git-branch.adoc`.
     - Tests verify opt-out behavior, skip messages, explicit deletion via `git branch -d`, and now use `git for-each-ref` for branch state verification as suggested by Phillip Wood.
   - **Refined stacked-branch protection (abort-and-clear)**:
     - **Design and implementation**: Fails to delete a branch if it is used as an upstream for an unmerged branch, but clears the upstream config for kept (merged) branches whose own upstream is being deleted. Implemented in `spare_stacked_bases()` and `spare_stacked_base()`.
     - Handles chains of dependencies (e.g., `b1→b2→b3`) by keeping only the branches needed by unmerged branches and clearing upstream configs for kept (merged) branches whose own upstream is being deleted.
     - Applies uniformly to both `--delete-merged` and `-d` for consistency.
   - **`--forked` filter (patch 1/7)**:
     - New `ref_filter_forked_add()` resolves ref arguments to full refnames (e.g., `origin/main` → `refs/remotes/origin/main`) or accepts shell-style globs (e.g., `origin/*`).
     - `filter_forked_match()` checks each branch's upstream against registered patterns, with exact matches compared against full upstream refnames and globs matched against abbreviated upstream names (e.g., `origin/main`).
     - Supports remote shorthand (e.g., `origin` for `origin/HEAD`).
     - Composable with existing filters (`--merged`, `--no-merged`).
     - **Phillip Wood's review suggested minor test improvements (detached HEAD removal, test reordering), which have been addressed. No functional changes were required.**
   - **`<branch>` argument support for `--delete-merged`**:
     - Reuses the `--forked` filter machinery to support exact refnames, remote shorthand, and shell-style globs (e.g., `origin/next`, `topic/*`).
     - Addresses Phillip Wood's usability concern about the lack of filtering flexibility in the original design.
   - **Flags-based API (patches 2-4/7)**:
     - **Maintenance issue resolved**: The `flags` variable was split into local booleans (`force`, `quiet`), creating two sources of truth. **Harald Nordgren has agreed to replace all local boolean uses with direct flag checks (e.g., `flags & DELETE_BRANCH_FORCE`) before merging. This is a mechanical fix that does not affect functionality.**
     - Patch 2/7 refactors `delete_branches()` and `check_branch_commit()` to replace boolean arguments (`force`, `quiet`) with `unsigned int flags` and enum values (`DELETE_BRANCH_FORCE`, `DELETE_BRANCH_QUIET`).
     - Patch 3/7 adds `DELETE_BRANCH_SKIP_UNMERGED`, enabling bulk callers to skip unmerged branches silently.
     - Patch 4/7 adds `DELETE_BRANCH_NO_HEAD_FALLBACK` to prevent falling back to `HEAD` when no branches are specified.
     - Patch 7/7 adds `DELETE_BRANCH_DRY_RUN` for `--dry-run` support.
   - **Core command (patch 5/7)**:
     - `delete_merged_branches()` filters candidates using `--forked` machinery and now supports `<branch>` arguments.
     - Applies safety checks and refined stacked-branch protection (abort-and-clear).
     - **Test improvements applied**: Added comments to helper functions, renamed them for clarity, simplified test patterns, removed redundant commands, and improved branch naming clarity. The test suite now explicitly configures `branch.main.pushRemote` to prevent `main` from being treated as a forked branch, exposing a subtle edge case where branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked when `remote.pushDefault` is set. This is noted as a known limitation but does not affect the core logic or safety checks.
   - **`--dry-run` preview (patch 7/7)**:
     - Adds `DELETE_BRANCH_DRY_RUN` flag to the deletion API.
     - `delete_branches()` skips ref deletion when the flag is set.
     - Prints "Would delete branch <name>" or "Would skip branch <name> (used as upstream)" for each affected branch, matching the real deletion/protection output exactly.
     - Rejects `--dry-run` without `--delete-merged`.
     - Documentation and tests updated to reflect the new functionality. **Phillip Wood's final review suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk, but no functional changes were required.**
   - Safety checks: Checked-out branches, missing upstreams, push divergence, per-branch opt-out, refined stacked-branch protection (abort-and-clear).
   - Config integration: `branch.<name>.deleteMerged=false` (opt-out).

3) Test coverage:
   - Comprehensive integration tests:
     - Verifies merged branches are deleted while unmerged/checked-out/protected/opt-out branches are spared.
     - Confirms branches with missing upstreams or push-equals-upstream are preserved.
     - Tests opt-out behavior: `branch.<name>.deleteMerged=false` prevents deletion.
     - Tests explicit deletion: `git branch -d` ignores opt-out and uses normal merge check.
     - Tests `--dry-run` functionality: output matches real deletion/protection output exactly, and `--dry-run` is rejected without `--delete-merged`. **Phillip Wood confirmed the test for `--dry-run` is well-structured.**
     - **Refined stacked-branch protection**: Tests verify abort-and-clear behavior, error messages, clearing upstreams for kept branches, and handling of chains of dependencies (e.g., `b1→b2→b3`) and self-referential upstreams (e.g., `b3→b3`).
     - **`--forked` filter**: Tests exact refnames (e.g., `refs/remotes/origin/main`), remote shorthand (e.g., `origin`), shell globs (e.g., `origin/*`), composition with other filters (`--merged`, `--no-merged`), and edge cases like self-referential upstreams and missing upstreams. **Phillip Wood's review suggested minor improvements (detached HEAD removal, test reordering), which have been addressed.**
     - **`<branch>` argument support**: Tests confirm the command respects branch patterns (e.g., `origin/next`, `topic/*`) to limit deletion candidates.
     - **Flags-based API**: Tests confirm the refactoring in patches 2-4/7 introduces no behavioral changes for existing callers and correctly implements the new flags (`DELETE_BRANCH_SKIP_UNMERGED`, `DELETE_BRANCH_NO_HEAD_FALLBACK`, `DELETE_BRANCH_DRY_RUN`).
     - **Edge case exposure**: Tests now explicitly configure `branch.main.pushRemote` to prevent `main` from being treated as a forked branch, exposing a subtle edge case where branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked when `remote.pushDefault` is set. This is noted as a known limitation but does not affect the core logic or safety checks.
     - **Patch 6/7 tests**: Now use `git for-each-ref` for branch state verification as suggested by Phillip Wood, aligning with the series' broader testing style.

Open questions: **All mechanical and test-related issues have been addressed or acknowledged for pre-merge resolution:**
1) **Flags maintenance issue in patches 2-4/7**: Harald Nordgren has agreed to replace all local boolean uses with direct flag checks to avoid two sources of truth. This is a straightforward fix and the **only remaining pre-merge action item**.

Participant positions:
- **Harald Nordgren**: Implemented all changes through v18, including core deletion logic, safety checks, **per-branch opt-out (patch 6/7)**, `--dry-run` functionality (patch 7/7), **refined stacked-branch protection (abort-and-clear)**, the `--forked` filter (patch 1/7), **`<branch>` argument support for `--delete-merged`**, and the flags-based API refactoring (patches 2-4/7). Addressed Phillip Wood's style suggestion (replaced manual loop with `strset_for_each_entry()`) and test improvements for patch 1/7 (detached HEAD removal, test reordering). **Has applied all test improvements to patch 5/7 (comments, helper functions, redundant command removal, branch naming clarity) and patch 6/7 (`for-each-ref` verification), and moved the premature `--dry-run` test case to patch 7/7. Has agreed to address the flags maintenance issue in patches 2-4/7 before merging.**
- **Phillip Wood**: Provided high-weight feedback on API consistency, stacked-branch safety, per-branch opt-out design, `--dry-run` requirements, and **`<branch>` argument usability**. **Proposed and implemented the refined stacked-branch protection approach (abort-and-clear)**, which was adopted in v18. His feedback was instrumental in shaping the final design, including the flags-based API refactoring and the `DELETE_BRANCH_NO_HEAD_FALLBACK` flag. **Reviewed patch 1/7 (--forked filter) and raised only surface-level test-related points (detached HEAD removal, test reordering), which have been addressed. Identified the flags maintenance issue in patches 2-4/7 and recommended a mechanical fix. Latest review of patch 5/7 suggested test improvements (comments, helper functions, redundant command removal, branch naming clarity), which have been applied. Final review of patch 7/7 suggested minor commit message wording improvements and noted the `--dry-run --quiet` combination as a potential UX quirk, but no functional changes were required. Surface-level review of patch 6/7 suggested a mechanical test improvement (`for-each-ref` verification), which has been applied.**
- **Junio C Hamano**: **Endorsed the refined stacked-branch protection design (abort-and-clear)** as philosophically consistent with `git branch -d`. The series is now **implementation-complete pending the mechanical fix to patches 2-4/7**.

Related work: Builds on `fetch.prune` patterns while introducing new safety-focused branch management capabilities. The v18 series is **ready for merging once the flags maintenance issue in patches 2-4/7 is addressed**. The per-branch opt-out mechanism complements existing safety checks by addressing iterative topic branch workflows. The `--forked` filter and `<branch>` argument support establish a composable foundation for future branch management features. Retargeting (inspired by GitHub's stacked-PR automation) is deferred for future work as an optional flag (e.g., `--retarget-upstreams`). **Phillip Wood's speculative `--forked=recurse` idea is noted but not pursued in this series.** The test suite now exposes a subtle edge case where branches without an explicit `branch.<name>.pushRemote` may be incorrectly classified as forked when `remote.pushDefault` is set, but this is noted as a known limitation and does not affect the core logic or safety checks.