This is patch 8/16 in Taylor Blau's incremental MIDX repacking series, which aims to improve Git's handling of large repositories through more efficient repacking strategies. 

The patch is a straightforward refactoring that renames an internal helper function (`midx_contains_pack_1`) to the more descriptive `midx_layer_contains_pack` and exposes it in the midx.h header. The key difference is that this function checks only a single MIDX layer, whereas the existing `midx_contains_pack` recursively checks the entire chain. This change will be used in a subsequent commit to specifically check the tip MIDX layer.

The patch makes no functional changes - it's purely a code organization improvement to support upcoming functionality in the series. The implementation appears clean and well-contained, with the rename being mechanically straightforward. This continues the series' pattern of careful, incremental changes to the MIDX infrastructure.