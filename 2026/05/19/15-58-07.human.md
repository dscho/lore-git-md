This is patch 10/16 in Taylor Blau's incremental MIDX repacking series, which aims to improve Git's repacking strategy for large repositories. The patch is a refactoring that extracts the pack-writing logic from `write_midx_included_packs()` into a new helper function `repack_fill_midx_stdin_packs()`.

The change is mechanical - it prepares for a future patch that will need to capture the MIDX checksum from the command's output when using `--no-write-chain-file`. The new helper handles the stdin/stdout plumbing for the `git multi-pack-index write --stdin-packs` subprocess, making the calling code cleaner.

This is a straightforward internal refactoring with no behavior changes. The series has already addressed substantive feedback in earlier iterations, and this patch appears uncontroversial as it simply reorganizes existing code for better reusability. The comprehensive test coverage in t7705-repack-incremental-midx.sh should verify no regressions were introduced.