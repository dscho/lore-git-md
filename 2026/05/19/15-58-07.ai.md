This is patch 10/16 in Taylor Blau's incremental MIDX repacking series, which implements improved repacking strategies for large repositories. The patch is a refactoring that extracts the pack-writing logic from `write_midx_included_packs()` into a new helper function `repack_fill_midx_stdin_packs()`.

The change is purely structural - no behavior is modified. The new helper handles the complete lifecycle of writing pack names to stdin when running `git multi-pack-index write` as a child process, including argument setup, process startup, writing the pack names, and process completion. This prepares for a future change where the same helper will be reused to capture the MIDX checksum from stdout when using the new `--no-write-chain-file` option.

The patch follows the series' pattern of careful, incremental refactoring with comprehensive test coverage. It addresses no outstanding issues but rather continues the architectural work needed to support the series' goal of efficient incremental MIDX repacking. The change is straightforward and unlikely to be controversial, building on prior patches that have already been reviewed.