This is a follow-up ping for a bugfix patch that addresses `git log --follow` behavior with merge commits. The original submission (v1) received no replies, so the author Miklos Vajna is now directly reaching out to Junio Hamano for review while keeping the list CC'd.

The patch itself (summarized in the parent email) fixes a real inconsistency where `git log --follow` fails to track file history across subtree merges, while `git blame --follow` already handles this case correctly. The implementation mirrors the existing blame logic and includes a test case.

The current email doesn't introduce new technical content - it's primarily checking on the patch's status given Junio's recent absence. The author is politely asking whether they should wait longer or resubmit, acknowledging the maintainer's backlog. This suggests the patch is still awaiting review but appears uncontroversial based on the lack of negative feedback to v1.

The patch remains in the same state as before: a well-motivated, narrow fix with clear precedent in the codebase and test coverage. The main open question is simply when/if it will get maintainer attention for merging.