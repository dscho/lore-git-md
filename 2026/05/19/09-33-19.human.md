This is a technical discussion about extending the recently approved priority queue optimization for Git's revision walking code. Kristofer Karlsson compares two approaches for applying the same optimization to `get_revision_1()` - his own implementation versus Jeff King's unpublished prior work.

Kristofer's approach keeps the existing linked list for setup phases while adding a separate priority queue for the walk phase, minimizing changes to callers (4 files changed). Jeff's more comprehensive approach replaces the linked list entirely with a priority queue (12 files changed), requiring more conversions at boundaries but potentially cleaner long-term. Both show similar performance gains (~3-5x speedups in benchmarks), with Kristofer's version slightly faster (4%) in limited walks due to avoiding some queue rebuilds.

The email includes detailed performance numbers and links to Kristofer's draft implementation. He notes the tradeoffs between the approaches but defers to the community's judgment on which direction to pursue, as both achieve the core performance improvements. The discussion represents productive collaboration between contributors building on the recently merged `limit_list()` optimizations.