This is the fourth patch in Taylor Blau's performance optimization series for the pack-bitmap-write subsystem. The patch refactors the `find_object_pos()` function to consolidate its success path, preparing for future optimizations around object-position lookups.

The change restructures the function to:
1. Store the calculated position in a local `pos` variable rather than returning immediately
2. Share a common success path between the packlist and MIDX cases
3. Maintain the same behavior for the `found` out-parameter

This is a preparatory refactoring that doesn't change functionality but makes the code more maintainable for the optimizations coming in later patches. The consolidation of success paths will make it easier to add instrumentation or additional bookkeeping around position lookups.

The patch follows Taylor's typical style of small, focused changes with clear commit messages. As part of a larger performance series that has already demonstrated significant speedups in earlier patches, this change appears to be a low-risk internal refactoring that maintains the existing behavior while improving code structure.