This is the third and final patch in Jeff King's (Peff) refactoring series cleaning up the quote.[ch] files. The series removes unused code and simplifies the dequoting implementation.

The patch eliminates an internal helper function (`sq_dequote_to_argv_internal`) that was previously shared between two public interfaces, one of which has already been removed in patch 2/2. Now that only the strvec-based interface remains, the implementation can be simplified by inlining the helper's logic directly into `sq_dequote_to_strvec()`. The change removes 14 lines while maintaining identical functionality.

Peff notes that while the interface could theoretically be made more caller-friendly by accepting a const input string, the current implementation (which modifies the input) would require deeper parser changes. He proposes leaving that optimization for future work since no current callers need it.

This is a straightforward cleanup that follows the pattern of Peff's precise, small refactorings - removing dead code paths and simplifying the implementation now that fewer interfaces need to be supported. The series as a whole removes 32 lines of code with no behavior changes, continuing Git's ongoing code hygiene efforts.