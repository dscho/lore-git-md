This is patch 7/18 in Patrick Steinhardt's series completing the `the_repository` removal effort in Git's setup subsystem. The patch converts `verify_non_filename()` to take an explicit `struct repository *` parameter instead of implicitly using the global `the_repository` variable.

The change follows the established three-step conversion pattern seen throughout this series: updating the function signature, modifying callers to pass the repository explicitly, and adjusting the implementation. The patch affects 6 files with straightforward mechanical changes to builtin commands (checkout, grep, reset) and revision handling code.

Key technical details:
- `verify_non_filename()` now checks paths against the passed repository rather than the global state
- Callers in checkout, grep.c, reset.c, and revision.c are updated to pass `the_repository` explicitly
- The function's behavior remains identical, just with explicit repository parameterization
- Maintains thread-safety by removing implicit global access
- Follows the same proven conversion methodology as previous patches in the series

This continues the systematic elimination of `the_repository` from the setup subsystem, building on the pattern validated by earlier reviews from Karthik Nayak and Junio C Hamano. With all technical concerns already addressed in previous rounds, this patch represents another incremental step toward completing the setup.c conversion.