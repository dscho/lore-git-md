This is a follow-up in the URL-based promisor remote auto-configuration series, addressing Toon Claes' review of v2. Christian Couder confirms that:

1. The remote naming scheme (e.g., "example-com-8443") with numeric suffixes is handled correctly by the collision resolution logic, as the code doesn't parse existing remote numbers but simply appends new suffixes.

2. The interaction between `promisor.acceptFromServerUrl` and `promisor.acceptFromServer` was indeed problematic in v2 but has been fixed and clarified in v3. The behavior is now properly documented regarding configuration precedence.

The exchange shows the review feedback was fully addressed in the subsequent version, with Christian noting the improvements made in v3 to both the implementation and documentation. This wraps up the technical discussion on these points, leaving the series ready for merging with all substantive concerns resolved. The remaining open question about configuration precedence when `promisor.acceptFromServer="all"` appears to have been settled through the v3 revisions.