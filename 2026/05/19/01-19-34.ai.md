This is the second patch in Jeff King's (Peff) refactoring series cleaning up the quote.[ch] files. The patch removes the unused `sq_dequote_to_argv()` function and its internal helper, which became obsolete after commit f9dbb64fad switched to using `sq_dequote_step()`.

Peff provides a clear rationale for removal: the function represents a "worst-of-both-worlds" interface that's neither as fast as the low-level `sq_dequote_step()` nor as convenient as the higher-level `sq_dequote_to_strvec()`. The change removes 14 lines of code (5 from quote.c and 9 from quote.h) while maintaining all existing functionality.

The patch follows Peff's typical style - a focused, well-explained change that removes dead code without introducing new behavior. Given the straightforward nature of the cleanup and Peff's reputation for careful work, this change is unlikely to generate controversy. The series appears to be progressing as expected for this type of mechanical refactoring.