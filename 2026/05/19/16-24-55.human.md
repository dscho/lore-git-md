This is the final patch in Derrick Stolee's series to improve negotiation controls for fetch and push operations. The patch completes the push integration by passing the `negotiationInclude` and `negotiationRestrict` config values from the remote configuration to the underlying `git fetch --negotiate-only` process that runs during push negotiation.

The changes:
- Extends the `send-pack` infrastructure to accept negotiation config parameters
- Updates documentation to note these configs affect push operations
- Adds tests verifying the config values are properly passed through during push
- Maintains backward compatibility when no config is set

The implementation is clean and follows the pattern established in earlier patches. The test coverage is thorough, verifying both include and restrict behaviors work as expected during push operations. With this patch, the series is now fully integrated for both fetch and push workflows.

This represents the culmination of several iterations addressing all review feedback, and the patch carries Matthew Cheetham's Reviewed-by tag. The series is now complete and ready for maintainer consideration.