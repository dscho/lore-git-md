This is the 7th patch in Taylor Blau's performance optimization series for the pack-bitmap-write subsystem. The patch focuses on restructuring how pseudo-merge parent bitmaps are handled during bitmap generation.

The key change moves the storage of pseudo-merge parent bitmaps from temporary arrays to being stored directly on the `bitmapped_commit` struct. This maintains the same on-disk output format but aligns with the series' goal of optimizing the bitmap generation process. The patch:

1. Adds a new `pseudo_merge_parents` field to `struct bitmapped_commit`
2. Updates the cleanup path to free this new field
3. Modifies `write_pseudo_merges()` to store parent bitmaps persistently rather than using temporary arrays
4. Adds BUG() checks to verify invariants about pseudo-merge state

The changes are focused on internal implementation details rather than user-visible behavior, continuing the series' pattern of low-risk optimizations. The restructuring appears to be preparation for further optimizations in the final patch of the series, likely related to how pseudo-merges are built and written.

This patch follows the established pattern of the series - careful, incremental changes to the bitmap generation internals with clear performance motivations. The implementation looks clean and maintains all existing functionality while setting up for future improvements.