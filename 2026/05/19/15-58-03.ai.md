This is patch 9/16 in Taylor Blau's incremental MIDX repacking series, a refactoring that extracts common MIDX command preparation logic into a helper function. The patch factors out the shared setup for `git multi-pack-index` commands (setting up progress and bitmap flags) from `write_midx_included_packs()` into a new `repack_prepare_midx_command()` function.

The change is to support upcoming patches that will need to construct similar commands for both `write` and `compact` subcommands. The refactoring leaves caller-specific arguments (like `--stdin-packs`) to be added by individual callers. The implementation is straightforward with no behavior changes - it just moves existing code into a reusable helper while maintaining all current functionality.

This continues the series' pattern of careful refactoring to support the incremental MIDX repacking feature. The patch follows the established style of the surrounding code and maintains test coverage through the existing `t/t7705-repack-incremental-midx.sh` tests. As with previous patches in the series, this appears to be a well-considered mechanical change that advances the overall architecture without introducing new risks.