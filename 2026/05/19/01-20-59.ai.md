This is the third and final patch in Jeff King's (Peff) quote.[ch] cleanup series, completing the refactoring work to simplify the dequoting internals. The patch removes the now-unused `sq_dequote_to_argv_internal()` wrapper function and inlines its remaining functionality directly into `sq_dequote_to_strvec()`, since the strvec-based API is now the only remaining consumer.

The change eliminates 14 lines of code while maintaining all existing functionality. Peff notes in the commit message that while the API could theoretically be made more caller-friendly by accepting a const input string, that would require deeper parser rework and isn't currently needed by any callers. This follows the pattern of the series - precise, focused cleanups that remove dead code without introducing new complexity.

The series as a whole (now complete with this patch) represents a net reduction of 32 lines across quote.c and quote.h, with no behavior changes. Given Peff's reputation for careful cleanup work and the straightforward nature of these changes, this series appears ready for merging with minimal additional review needed.