This is patch 6/16 in Taylor Blau's incremental MIDX repacking series, introducing a new `--base` option for MIDX writes and compactions. The patch adds flexibility to specify which existing MIDX layer should serve as the base for new incremental layers, with special handling for creating root layers (with no parent) via `--base=none`.

The change allows the incremental repacking machinery to write layers based on intermediate ancestors rather than just appending to the tip, which will be needed when replacing bottommost chain entries. The implementation carefully maintains chain invariants - for example, requiring `--no-write-chain-file` when using `--base` with `write` to prevent creating invalid chain states.

The patch is thorough, with:
- Clear documentation updates explaining the new option
- Careful validation of base layer relationships
- Comprehensive test coverage for both valid and invalid cases
- Integration with the existing incremental MIDX machinery

This appears to be a well-constructed piece of the larger MIDX repacking effort, building on prior patches that have already addressed technical feedback. The implementation looks solid and the tests verify the key behaviors around chain integrity and base layer selection.