This is a test modernization patch from Christian Couder, part of his v3 series adding URL-based auto-configuration for promisor remotes. The patch simplifies test setup in t5710 by replacing `mkdir` followed by `git -C init` with the more direct `git init` command.

The change is straightforward and mechanical - it updates two test cases to use the simpler initialization pattern. This is a minor cleanup that doesn't affect test behavior, just makes the code slightly more concise and efficient. The patch has already been reviewed as part of the larger series which is now reached its final form with all technical feedback addressed.

Since this is just a test cleanup in an already-approved series, it's unlikely to generate discussion. The change demonstrates good attention to test hygiene even in small details.