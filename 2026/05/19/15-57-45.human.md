This is patch 3/16 in Taylor Blau's incremental MIDX repacking series, a performance-focused effort to improve Git's handling of large repositories. The patch refactors how the `keep_hashes` array is populated during MIDX (multi-pack-index) writing.

The change replaces a somewhat confusing reverse-indexing approach with a clearer two-step process: first collecting MIDX layers into a temporary array, then iterating backwards through that array to fill `keep_hashes` sequentially. This makes the control flow more straightforward and prepares for a future change to use `strvec` for the array management.

The patch is a clean refactoring with no behavior change - it just reorganizes existing logic to be more maintainable. The commit message clearly explains both the immediate benefit (easier to follow code) and the forward-looking motivation (preparing for `strvec` usage). The implementation includes an assertion to verify the final array count matches expectations, showing attention to correctness.

As part of a larger series that has already addressed all substantive review feedback, this patch appears ready for merging. It's a typical example of the careful, incremental improvements that characterize Taylor's work on the pack and MIDX subsystems.