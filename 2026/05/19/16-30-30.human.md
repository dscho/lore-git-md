This is a documentation patch that's part of a larger series introducing external command support for Git notes. It fixes an oversight in the `git-range-diff` man page by adding the missing `--no-notes` and `--notes[=<ref>]` options to the command's synopsis section.

The change is straightforward and correct - these options were already implemented in the code but accidentally omitted from the documentation. As the first patch in a 9-part series, this serves as a simple preparatory cleanup before introducing the more substantial external notes functionality in later patches.

The patch follows Git's documentation conventions precisely, maintaining the existing formatting while adding the missing options in the appropriate place. It's a small but necessary piece of the larger feature puzzle.