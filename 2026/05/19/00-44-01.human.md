This is a performance optimization and code cleanup patch series from René Scharfe, with Jeff King (Peff) providing final review approval. The series refactors Git's overflow checking patterns in the strbuf_grow() function to take advantage of Clang's __builtin_add_overflow() intrinsic, particularly benefiting ARM64 (Apple M1) performance.

The key technical change is the introduction of st_add_overflow(), a helper that mimics the compiler intrinsic for size_t operations while maintaining safety. This builds on previous discussion where Junio Hamano raised evaluation order concerns that have now been addressed. The patch shows a 4% performance improvement for strbuf-heavy operations like `git cat-file --batch-all-objects`.

Jeff King's review confirms the approach is sound and sufficient for Git's needs, noting that while a more type-generic solution would be harder, this focused optimization meets the practical requirements. The series has progressed through multiple iterations with all substantive feedback incorporated, demonstrating René's characteristic precision in refactoring core infrastructure.

This appears to be the final approval needed before the series can be merged, representing a small but meaningful improvement to Git's memory safety patterns and ARM performance. The changes are well-contained (22 lines) and maintain backwards compatibility while optimizing a critical path used throughout Git's codebase.