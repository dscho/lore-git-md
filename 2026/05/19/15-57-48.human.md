This is patch 4/16 in Taylor Blau's incremental MIDX repacking series, which refactors the code to use `strvec` for tracking MIDX file checksums that should be retained during pruning. The change simplifies memory management of the `keep_hashes` array in `write_midx_internal()` by replacing manual allocation and cleanup with the more ergonomic `strvec` interface.

The patch demonstrates careful attention to maintaining the existing behavior while improving the code structure. The ordering of checksums remains unchanged (important for the MIDX chain file), and the conversion is straightforward since the previous version already built the array in forward order. The changes include replacing indexed assignments with `strvec_push()`, removing manual counting and allocation, and simplifying cleanup via `strvec_clear()`.

This is a preparatory refactoring that makes the code more maintainable while keeping all functionality intact. The series as a whole has received thorough review and appears ready for merging, with this patch representing one of the final cleanups before the feature is complete.