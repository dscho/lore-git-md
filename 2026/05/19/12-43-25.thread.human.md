The thread discusses optimizing `git stash -p` to avoid unnecessary filesystem refreshes when fsmonitor is enabled. The current implementation shells out to `git read-tree HEAD`, which discards cached file status information. Adam Johnson proposed replacing this with an in-process `unpack_trees()` call that preserves cached entries for unchanged files, dramatically improving performance (from 35 seconds to 0.7s in large repos).

Junio C Hamano reviewed the patch, initially questioning whether `git read-tree -m` could achieve similar results (Adam explained why not) and later identifying a behavioral difference in how the new version handles index refresh errors more strictly. In the latest email, Adam acknowledges this was unintentional and agrees to make the refresh unconditional to maintain the original lenient behavior - an important consideration since `stash -p` is often used with dirty working trees.

With this last concern addressed, the technical approach appears sound and the patch seems ready for an updated version. The optimization builds on prior fsmonitor work by Paul Tarjan and aligns with similar handling in `git restore -p`. Both Junio and Adam have engaged constructively, with Adam promptly addressing each review point while preserving the core performance improvement.