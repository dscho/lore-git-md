This is a documentation patch follow-up discussing whether to include `[no-]` variants in option documentation. The author, Brandon, responds to Junio's review by observing that most Git commands don't document the `[no-]` variants for string-valued options like `--message`, even though the code supports them. 

Brandon notes this appears to be a stylistic convention rather than an oversight, pointing out that `git-commit`, `git-notes`, `git-svn`, and `git-tag` similarly omit these variants in their documentation. The argument is that since the behavior is consistent (passing `--no-message` unsets the option) and not unique to `git-merge`, documenting it would create inconsistency with other commands' docs.

The response suggests the current patch's approach of only documenting `--message` (matching existing documentation patterns) may be preferable to adding `--no-message`, though it leaves the question open for further discussion. This positions the thread at a decision point about whether to follow existing documentation conventions or make `git-merge`'s docs more technically complete by including all supported options.