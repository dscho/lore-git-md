Thread identity: Build system cleanup patch from Harald Nordgren addressing duplicate library references in Makefile link recipes.

Core problem: Three Makefile recipes (test-helper programs, fuzz programs, unit tests) were listing archive files twice - once through explicit filtering and again via $(LIBS) variable, causing duplicate library warnings on macOS.

Current status: Patch v1 proposes removing the explicit library references since $(LIBS) alone provides the necessary linkage, matching the pattern used elsewhere in the Makefile.

Key technical details: Affects link commands in Makefile for test helpers, fuzz targets, and unit tests. No functional changes - purely removes redundant linker arguments that were causing warnings.

Participant positions: No reviews yet (initial patch submission). The change appears straightforward and aligns with existing Makefile patterns.