Thread identity: Build system cleanup patch series from Harald Nordgren addressing macOS linker warnings about duplicate library references in Makefile link recipes. The series explores linker flag suppression as the agreed architectural approach. Thread type is patch series with build system focus.

Core problem: macOS linker emits warnings (`ld: warning: ignoring duplicate libraries`) when the same library appears multiple times in link commands, despite Git's build system sometimes intentionally listing libraries multiple times to handle circular dependencies between archives. The challenge is to eliminate the cosmetic warning without breaking symbol resolution or build system maintainability.

Current status: The series has concluded with the v3 patch implementing Junio C Hamano’s proposed solution: suppressing the warning at the linker level via `-Wl,-no_warn_duplicate_libraries` in `config.mak.uname`, version-gated for XCode 15+ (ld64-907+, dyld-1009.5+) using `LD_MAJOR_VERSION`. This approach preserves the existing Makefile logic while addressing the developer experience issue. The patch is now ready for merging.

Key technical details:
- Files touched: `config.mak.uname`
- New symbols/flags: `-Wl,-no_warn_duplicate_libraries` (macOS linker flag), version-gated via `LD_MAJOR_VERSION` regex matching ld64/dyld versions
- Linker behavior: macOS ld warns on duplicate libraries; GNU ld silently accepts them. The flag suppresses the warning without affecting symbol resolution
- Circular dependency handling: listing the same library multiple times remains necessary for correct symbol resolution (e.g., libA.a -> libB.a -> libA.a)
- Version gating: flag only applied to Xcode 15+ linkers (ld64-907+, dyld-1009.5+), avoiding errors on older macOS versions
- Alternative syntax: GNU ld's `--start-group`/`--end-group` remains less portable and was not adopted
- Build configurations: macOS-specific linker flag, version-gated using existing `LD_MAJOR_VERSION` infrastructure
- Test coverage: implicit in repeated test executions (e.g., rebase workflows) where warnings previously accumulated

Open questions and loose ends:
- Could this linker flag approach be extended to other platform-specific warnings in Git's build system?
- What precedent does this set for handling platform-specific linker behaviors in Git's build system?
- Are there other platforms with similar linker behaviors that might benefit from analogous suppression?

Participant positions:
- Harald Nordgren: Author of the series. Initially proposed Makefile restructuring (v2) but converged on Junio’s linker flag suppression approach (v3), particularly the version-gating via `LD_MAJOR_VERSION`. Focused on developer experience (reducing terminal noise during test runs) and build system maintainability. Demonstrated responsiveness to review feedback and practical understanding of workflow impacts. Final patch reflects thread consensus.
- Junio C Hamano: Advocated for linker flag suppression (`-Wl,-no_warn_duplicate_libraries`) to preserve the ability to list libraries multiple times when needed. Emphasized correctness and maintainability, arguing the warning is cosmetic and should be suppressed at the linker level rather than complicating the Makefile. Provided concrete example of circular dependency use case, proposed the linker flag approach, and suggested version-gating for XCode 15+.

Related work: The discussion establishes a pattern for handling platform-specific linker behaviors in Git's build system. The version-gating technique aligns with the earlier `__DATA,__common` alignment fix. The circular dependency use case and linker flag approach may inform future build system cleanups or platform-specific adjustments. No direct dependencies on prior patches.