This thread began with Harald Nordgren’s patch series addressing duplicate library references in Git’s Makefile, which were causing macOS linker warnings. The v2 patch took an architectural approach, restructuring `$(LIBS)` to explicitly list prerequisite archives first while preserving the critical left-to-right linker ordering. Junio Hamano, however, has now raised a fundamental concern: rather than modifying the Makefile to avoid duplicate libraries, the linker itself should suppress these warnings. He argues that listing the same library multiple times is not only harmless but sometimes necessary for correct symbol resolution, particularly in cases involving circular dependencies between libraries. His proposed solution—adding `-Wl,-no_warn_duplicate_libraries` to macOS linker flags—addresses the cosmetic warning at the linker level while preserving the existing Makefile logic.

The discussion now centers on a tension between two valid perspectives. Harald’s approach prioritizes developer experience, eliminating terminal noise during test runs, while Junio’s emphasizes maintainability and correctness, ensuring the build system remains flexible enough to handle complex dependency scenarios. The outcome may set a precedent for how Git handles platform-specific linker behaviors in the future. Key technical details for the next round of discussion include the circular dependency use case, the `-no_warn_duplicate_libraries` flag, and whether version-gating for XCode 15 or later is necessary. The thread is at a crossroads, with the next steps likely hinging on whether the linker-level suppression is deemed an acceptable compromise.