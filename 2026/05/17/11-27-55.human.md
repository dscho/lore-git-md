This is a follow-up discussion in the abandoned config file locking timeout thread, where Johannes Schindelin provides additional technical context from his experience implementing a similar solution in Microsoft's Git fork.

The email confirms the fundamental issues that led to the patch's rejection remain unresolved, but offers real-world validation that configurable timeouts can work in practice. Schindelin shares details from his implementation in Scalar's test suite, including:
- A working example of configuring the timeout via config (contrary to earlier concerns)
- OS-specific timeout requirements (150ms needed for NTFS vs. the original 100ms proposal)
- An admission that the patch missed handling config section renames

While reinforcing that the core race conditions in concurrent config writes can't be fully resolved by timeouts alone, Schindelin's practical experience provides useful data points for future consideration. The email serves as a knowledge-sharing exercise now that the thread has effectively concluded, documenting implementation nuances that could inform any future attempts to address this class of problems.