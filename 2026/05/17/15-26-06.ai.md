This is a follow-up discussion in the performance optimization thread for `limit_list()`, where Kristofer Karlsson reports additional findings about applying the priority queue optimization to `get_revision_1()`. 

Kristofer notes that extending the priority queue approach to `get_revision_1()` yields a 3.3x speedup for `git rev-list HEAD` in a monorepo, while making `commit_list_insert_by_date` obsolete. However, the change complicates `get_revision_1()` by requiring both a priority queue and a linked list in the `rev_info` struct, with commits needing to be flushed from the list to the queue during execution.

He asks whether to pursue this as a separate thread or defer it, acknowledging he already has multiple optimization patches in progress. The message is a direct reply to Derrick Stolee's earlier approval of the main `limit_list()` optimization, where Stolee accepted the lack of public benchmark repositories as not blocking the change.

This suggests the original optimization is effectively complete, with Kristofer now exploring adjacent opportunities. The new findings indicate the priority queue approach may have broader applicability in revision walking, though the implementation tradeoffs warrant separate consideration from the already-reviewed work.