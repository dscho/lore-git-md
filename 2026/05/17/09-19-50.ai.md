Bugfix report for a segfault in `git rebase --continue` during commit signing. The crash occurs in the merge machinery when parsing a tree object, with the backtrace showing:

1. The crash originates in `repo_parse_tree_gently()` when passed a NULL `item` parameter
2. This is called during merge operations in the rebase sequence (`merge-ort.c`)
3. The rebase was in progress when the user entered a signing PIN, suggesting this might be related to GPG signing integration

The reporter provides detailed debugging information including:
- Version: Git 2.54.0 built from commit 94f057755b
System details show:
- x86_64 Linux (Arch)
- Rust enabled in build
- Using SHA-1 as default hash
- OpenSSL 3.6.2 for crypto operations

This appears to be a NULL-dereference bug in the interaction between the rebase sequencer and merge-ort backend, possibly triggered when handling signed commits. The stack trace suggests the merge machinery is failing to properly handle a missing tree object during the rebase operation. The report is well-formatted with complete debugging information, making it straightforward to investigate.