This is a technical discussion about config-based pre-push hook argument handling, continuing from Wesley Schwengle's report of unexpected behavior when using npm commands as hooks.

Jeff King (Peff) clarifies that Wesley's initial attempts failed due to config file parsing - the `#` character was being interpreted as a comment rather than as an argument discard marker. He provides corrected syntax using proper quoting (`command = "npm run test #"`) to ensure the `#` is preserved. Peff also explains that the shell function approach failed for similar quoting reasons.

The discussion touches on deeper design questions about hook invocation patterns. Peff argues that hooks should not receive arguments when run manually via `git hook run` violates the hook contract, as some hooks may legitimately need those arguments. He also notes that pre-push hooks receive data via both arguments (remote info) and stdin (ref updates), which could potentially cause issues for commands like `npm run test` that might read from stdin.

This email reinforces that the current behavior is intentional and documents the correct quoting approach, while acknowledging the underlying tension between different hook invocation patterns. The thread remains technically unresolved regarding whether config-based hooks should adopt stdin-based argument passing for consistency with traditional hooks.