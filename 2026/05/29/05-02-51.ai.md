This is a documentation patch review from the maintainer, pointing out that the proposed change to document `--message` in `git-merge` doesn't fully match the actual implementation's behavior. Junio C Hamano notes that the command also accepts `--no-message` (as seen in `git merge -h` output), and that several other options similarly support `[no-]` prefixes that aren't documented.

The review identifies a gap between the documentation and implementation - while the patch correctly adds `--message`, it misses the `--no-message` variant that exists in the code. Junio also observes this isn't an isolated case, as other merge options with similar behavior are also undocumented.

Key technical details for future reference:
- The actual implementation in `builtin/merge.c` supports both `--message` and `--no-message`
- The `--no-message` variant suppresses the merge commit message (equivalent to `GIT_EDITOR=:`)
- Other options in `git merge` have similar undocumented `[no-]` variants
- The review suggests the documentation should be updated to reflect this full behavior

This moves the thread from a simple uncontroversial documentation update to one that may require a more thorough review of merge option documentation to capture all the `[no-]` variants consistently. The maintainer's comment implies the patch should either be expanded to include `--no-message` or the scope of the inconsistency should be acknowledged in the commit message.