This is a follow-up to the bug report about Git's date parsing for small Unix timestamps. Kristoffer Haugsbakk points out that using an `@` prefix (like `@0 +0000`) works for small epoch values, suggesting this might be an undocumented workaround for the issue.

The reply confirms the core problem - that small timestamps below 100000000 behave differently between environment variables and the `--date` option - while offering a potential temporary solution. This adds a new data point to the discussion, showing that the `@` prefix syntax (normally used for Unix timestamps) might bypass the problematic code path.

The response is brief but useful, identifying a syntax that appears to work where the raw numeric format fails. This suggests the bug might be specific to the numeric parsing path rather than affecting all timestamp handling uniformly. The thread now has both a clear bug report and a workaround, though the underlying inconsistency in date parsing still needs addressing.