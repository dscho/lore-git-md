Refactoring patch to commit-reach.c removing redundant sorting, with follow-up discussion about potential optimizations.

René Scharfe's patch removes an unnecessary date-based sort in `paint_down_to_common()`, since none of its three callers require sorted output. Jeff King (Peff) reviewed this positively as an "easy win" while suggesting the API could be refined further to avoid building result lists when callers don't need them. 

In this follow-up, René agrees the NULL pointer approach would work but notes the performance gains would likely be minimal since merge base lists are typically small. The exchange remains focused on technical merits, with both contributors acknowledging the primary value is code simplification rather than performance improvement. This continues the thread's productive discussion between two experienced contributors about cleaning up Git's commit graph traversal code.