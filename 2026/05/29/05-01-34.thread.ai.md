Updated AI thread summary:

Subject: Question about argument handling in config-based pre-push hooks

Core issue: Wesley Schwengle reports unexpected argument passing behavior in gitconfig-configured pre-push hooks, where automatic `git push` execution passes remote name/URL arguments that interfere with their `npm run test` command. This reveals documentation gaps and practical limitations in current workarounds.

Current status: Jeff King (Peff) confirmed the argument passing behavior is expected, providing shell-based workarounds. Wesley's testing showed initial failures with these approaches, but a follow-up demonstrates proper quoting (`command = npm run test "#"`) successfully prevents argument passing to npm. This provides a functional solution while broader design questions remain.

Technical evolution: The discussion has uncovered:
1. Current behavior where automatic push passes remote info as arguments
2. Workaround viability with proper shell quoting
3. Underlying tension between general scripting flexibility and hook conventions

Key participant positions:
- Jeff King (Peff): Maintains current behavior is by design, provided workarounds
- Wesley Schwengle: Initially showed workaround failures, later confirmed quoted comment solution works

Open questions:
- Should config-based hooks adopt stdin argument passing for consistency?
- Is there a need for configuration knobs for argument handling modes?

Implementation considerations remain regarding:
- Backward compatibility
- Interaction with other hook events
- Documentation clarity

Related work: Adrian Ratiu's hook configuration work remains relevant to future design decisions about hook argument patterns. The thread shows that while current behavior has workable solutions, the design may benefit from reevaluation to better accommodate common hook use cases. The quoted-comment workaround resolves the immediate issue but doesn't address the underlying conceptual mismatch between the two hook invocation patterns.