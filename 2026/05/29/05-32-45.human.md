Bugfix follow-up discussing tempfile cleanup behavior in HTTP pack index handling.

Jeff King (Peff) weighs in on Lorenzo Pegorari's uncertainty about removing an `unlink()` call in the HTTP pack index error path. Peff confirms that the only behavioral difference would be whether the temporary file is deleted immediately on error or at process exit - functionally equivalent from the user's perspective unless the system crashes. 

This continues the thread about proper resource cleanup in `fetch_and_setup_pack_index()`, where Peff had previously identified both a memory leak and this tempfile cleanup issue. The exchange shows the project's attention to detail in resource management, even for edge cases like crash behavior. Peff's response suggests Lorenzo could safely proceed with removing the redundant `unlink()`, though the thread remains open for any final confirmation before the patch gets picked up.