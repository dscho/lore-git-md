This is a follow-up discussion about a refactoring patch to `commit-reach.c` that removes redundant sorting in the `paint_down_to_common()` function. The original patch from René Scharfe eliminated an unnecessary date-based sort since none of the function's callers required sorted output.

In this response, René agrees with Jeff King's suggestion that some callers could potentially avoid building the result list entirely by passing NULL when they don't need it, though notes this would require adding NULL checks to the function. Both acknowledge that while this could be a further optimization, the primary benefit of the current patch is code simplification rather than performance improvement, since merge base lists are typically small in practice.

The exchange shows two experienced contributors converging on a clean, minimal solution - René's original patch stands as a clear improvement, while Peff's suggestion about NULL result pointers remains a potential (though likely minor) optimization for future consideration. The discussion remains technical and focused, characteristic of both contributors' styles.