Thread identity: Bug report about Git's date parsing behavior for small Unix timestamps (below 100000000) in both environment variables (GIT_AUTHOR_DATE/GIT_COMMITTER_DATE) and the `--date` option.

Core problem: Git incorrectly rejects or misinterprets Unix timestamps below 100000000 (9 digits) when used in the "raw" format. The behavior differs between environment variables (which reject small timestamps) and `--date=raw` (which sometimes falls back to system time or produces incorrect dates). This affects users trying to create commits with very old timestamps (pre-1973 in Unix time).

Current status: Bug reported by Luna Schwalbe with reproduction steps. Observed in commit 2f8565e1d14d2de4cfbc9da0132131bf0d0dc087. No fixes or workarounds mentioned yet.

Key technical details: The issue appears in Git's date parsing logic, specifically the "raw" format handling. The inconsistency suggests multiple code paths need alignment. The bug manifests when timestamps are below 100000000 (9 digits), regardless of timezone offset or zero-padding.

Open questions: Why do environment variables and `--date` option handle small timestamps differently? Which specific date parsing functions are involved? Are there other affected formats beyond "raw"?

Participant positions: Luna Schwalbe reported the bug with clear reproduction steps. No other participants yet.