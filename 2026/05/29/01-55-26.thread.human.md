This thread began with a documentation patch to add the `--message` long option to `git-merge`'s man page, matching its existing `-m` short option. Junio Hamano pointed out that the implementation also supports `--no-message` (which suppresses the merge commit message) and suggested the documentation should cover this and other undocumented `[no-]` variants. 

The author, Brandon, has now responded with an interesting observation: while the code universally supports `[no-]` variants for string-valued options like `--message`, most Git commands' documentation (including `git-commit`, `git-notes`, and others) omit these variants. Brandon suggests this appears to be a deliberate stylistic convention rather than an oversight, since the behavior is consistent across commands. 

The thread now faces a design question: should `git-merge` follow the existing convention of omitting `[no-]` variants from documentation (keeping consistency with other commands but leaving some functionality undocumented), or break with convention to provide more complete technical documentation? Junio's initial feedback leaned toward completeness, while Brandon's response suggests there may be value in maintaining consistency with how other commands document similar options. The next steps likely depend on whether Junio considers the current documentation pattern intentional or accidental.