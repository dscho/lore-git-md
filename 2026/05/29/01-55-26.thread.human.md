Here's the updated human-readable thread summary:

The thread began with a straightforward documentation patch proposing to add the long-form `--message` option to `git-merge`'s man page, matching the existing `-m` short option. The initial patch followed the pattern used in `git-commit` documentation and required no code changes since the option was already implemented.

Maintainer Junio C Hamano pointed out that the patch doesn't fully capture the implementation's behavior. While correctly documenting `--message`, it misses the `--no-message` variant that exists in the code (which suppresses the merge commit message). Junio also noted this isn't an isolated case - several other merge options have undocumented `[no-]` variants. 

The discussion has now shifted from a simple documentation addition to considering whether to expand the patch to document all these variants consistently. The maintainer's review suggests the author should either include `--no-message` in this update or explicitly acknowledge the remaining documentation gaps in the commit message. This may lead to a more thorough review of merge option documentation in subsequent versions of the patch.