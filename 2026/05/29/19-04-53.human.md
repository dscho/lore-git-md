This is a follow-up discussion in a thread about optimizing commit graph traversal in `commit-reach.c`. The original patch from René Scharfe removed an unnecessary sort operation in `paint_down_to_common()`, which Jeff King (Peff) had reviewed positively while suggesting further optimizations.

In this brief exchange, Peff notes that while passing NULL to avoid building result lists entirely (as he previously suggested) would require more NULL checks than initially thought, it would also eliminate redundant cleanup code in error paths. Both contributors clearly see this as a potential future optimization, though they've already agreed the current patch is a clear improvement on its own.

The discussion remains technical and focused, characteristic of both contributors' styles. The thread appears to be winding down with consensus that the original patch is good to merge, while leaving the door open for possible follow-up optimizations around result list handling. This is a common pattern in Git development - incremental improvements that each stand on their own merits while potentially enabling future refinements.