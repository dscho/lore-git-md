This is a follow-up in the thread about Git's date parsing behavior for small Unix timestamps. Luna Schwalbe responds to Kristoffer Haugsbakk's discovery that using an `@` prefix (like `@0 +0000`) works as a workaround for the timestamp issue.

Luna expresses appreciation for the workaround and suggests documenting this behavior in `Documentation/date-formats.adoc`. The exchange confirms that the `@` prefix syntax successfully bypasses the problematic code path that rejects small timestamps in raw numeric format, providing users with a functional alternative while the underlying bug remains unresolved.

The thread now has both a clear bug report and a practical workaround, though the inconsistency in date parsing between environment variables and the `--date` option still needs to be addressed in the code. Luna's suggestion about documentation hints at a potential quick win to help other users encountering this edge case.