This is a documentation patch that adds the long-form `--message` option to the `git-merge` man page, matching the existing short `-m` option. The change aligns the merge documentation with both the implementation in `builtin/merge.c` and the similar documentation in `git-commit`.

The patch is straightforward and uncontroversial - it simply documents an already-supported option that was previously only mentioned in its short form. The commit message clearly explains the motivation (consistency with both implementation and other commands' documentation.

Key technical details for future reference:
- Touches `Documentation/git-merge.adoc`
- Documents the `--message=<msg>` long option alongside existing `-m <msg>`
- Matches behavior already implemented in `builtin/merge.c`
- Follows pattern from `git-commit` documentation

No functional changes or behavior modifications - purely a documentation improvement. The patch appears well-formed with a proper sign-off and follows Git's documentation conventions.