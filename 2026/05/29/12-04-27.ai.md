This is a review response for the final version of a patch series integrating `git log -L` with Git's standard diff output pipeline. The series has already been through multiple iterations and is now merged into the `next` branch.

D. Ben Knoble provides a brief but positive review, confirming that the changes look good as previously discussed. He notes that while he didn't scrutinize the tests in detail, the rationale and overall code structure appear sound. The range-diff showing the minor test syntax changes (switching from `! test_grep` to `test_grep !`) also meets his approval.

This marks the conclusion of the technical discussion around this series, which successfully unified line-log output processing with Git's standard diff pipeline while maintaining range-specific behavior. All substantive feedback from earlier rounds has been addressed, and the only remaining change in v3 was the test syntax adjustment. The series now provides consistent metadata format support (`--raw`, `--name-only`, etc.) for line-log output while properly blocking unsupported stat formats.