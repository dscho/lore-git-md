This is a follow-up discussion in the commit-reach.c refactoring thread about removing redundant sorting in `paint_down_to_common()`. Jeff King (Peff) builds on René Scharfe's agreement that callers could potentially avoid building the result list entirely by passing NULL when unneeded.

Peff notes two technical details: first, that implementing this optimization would require adding NULL checks at multiple points in `paint_down_to_common()` where the result list is currently accessed directly. Second, he observes that the function currently duplicates cleanup of the output list during error handling - a redundancy that would naturally disappear if callers passed NULL when they didn't need results.

The exchange remains focused on potential future optimizations rather than requesting changes to the current patch, maintaining the thread's constructive tone. Both contributors recognize these as nice-to-have improvements rather than blockers, with the understanding that the original patch's removal of the unnecessary sort already provides clear value. The discussion exemplifies Git's iterative refinement process, where experienced contributors identify complementary optimizations while keeping scope manageable.