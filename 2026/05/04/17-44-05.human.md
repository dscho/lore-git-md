This is a cleanup patch in a refactoring series (v4 1/9) that standardizes reference backend behavior. It removes an unused typedef `ref_transaction_commit_fn` from the refs internal header file.

The change is straightforward - the typedef was previously defined but never used in the codebase. This is part of a larger series (already on its fourth iteration) that moves object validation and peeling logic from individual ref backends to the shared layer. 

While this particular patch is minor, it's part of a well-reviewed effort to consolidate backend behavior that has been through multiple rounds of review. The series as a whole has addressed all identified issues in previous versions and appears ready for merging.