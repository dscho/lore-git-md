This is a follow-up in the thread about `git clone --dissociate` failures during checkout. Rasmus Villemoes confirms that Jeff King's proposed workaround (`-c core.commitGraph=false`) successfully avoids the race condition where commit-graph data becomes invalidated prematurely during clone operations. 

The email also reports that Jeff's patch (which makes `repo_get_commit_tree()` resilient to missing commit-graph data) works as intended, though it triggers a compiler warning about a const-correctness issue in the function signature. This appears to be a minor technical detail that doesn't affect functionality.

The confirmation from the original reporter validates both the diagnosis and proposed solutions, suggesting the fix is ready for integration. The warning about const qualifiers may need addressing in a follow-up patch, but doesn't impact the core correctness of the solution. This moves the thread closer to resolution, with the main technical questions now answered and the fix verified in practice.