This appears to be a misdirected patch submission from Harald Nordgren - the email subject and content don't match the ongoing thread about `fetch.pruneBranches`. The patch claims to add a `--autostash` option to `git checkout` for branch switching, but the message body instead discusses the `fetch.pruneBranches` functionality that was clearly meant as a reply in the previous thread.

The patch itself (which would normally be attached) seems to be missing from the provided content. Based on the subject line alone, a `--autostash` option for `git checkout` branch switching would be a logical extension of the existing `git rebase --autostash` behavior, allowing users to automatically stash changes when switching branches. However, without seeing the actual patch content or implementation details, it's impossible to assess the quality or completeness of the proposed feature.

Given the mismatch between subject and content, this appears to be an accidental misposting rather than an intentional new feature submission. The maintainer will likely ask the author to resend either the correct patch or clarify the intended purpose of this email.