Refactoring patch in the refs backend consolidation series (4/9). This patch changes `ref_transaction_update()` to return a specific error enum (`ref_transaction_error`) rather than a generic -1, preparing for more detailed error handling in future validation. The change is mechanical - existing error cases now return `REF_TRANSACTION_ERROR_GENERIC` to maintain current behavior while establishing the infrastructure for more specific error reporting.

This is part of Karthik Nayak's larger effort to standardize ref backend behavior by moving validation and error handling into the shared refs layer. The patch touches core refs functionality but doesn't yet introduce new error cases - it's laying groundwork for subsequent patches in the series. The changes are straightforward and maintain backward compatibility, affecting all callers of `ref_transaction_update()` (including `update-ref` and `receive-pack`). With the v4 series having already addressed prior review feedback, this incremental change appears uncontroversial.