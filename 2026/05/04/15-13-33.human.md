This is a bugfix discussion for git-gui's handling of bare repositories and worktree detection. Mark Levedahl responds to Johannes Sixt's proposal for preserving backward compatibility with `.git` directory behavior, arguing that the issue requires more comprehensive changes than a simple exception.

Mark identifies a deeper inconsistency in how git-gui handles `GIT_WORK_TREE` versus `git -C` invocations, noting they should be equivalent but currently behave differently depending on the starting directory. He suggests the core issue lies in when environment variables are processed during startup, proposing either moving `GIT_WORK_TREE` handling earlier in the process or potentially disallowing these environment variables altogether for git-gui.

The discussion highlights a tension between preserving historical behavior (particularly the special case of `.git` directories from 2010) and implementing more robust, consistent handling of repository states. Mark's position suggests the current patch may need further refinement to properly address the environment variable interactions he's identified, though he hasn't yet proposed specific code changes to implement his observations.

This reply indicates the thread is moving into deeper technical discussion about git-gui's startup sequence and environment variable handling, beyond the initial focus on bare repository detection. The solution will need to balance backward compatibility with more principled behavior for environment-controlled invocations.