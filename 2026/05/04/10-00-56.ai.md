This is a technical discussion about improving Git's rename detection for whitespace-sensitive languages like Ruby. Jeff King (Peff) responds to Sébastien Stettler's suggestion about ignoring whitespace in similarity calculations.

Peff notes that while Git has `-w` for ignoring whitespace in diffs, this capability hasn't been extended to rename detection. He provides a concrete code location in `diffcore-delta.c` where whitespace could be filtered out during similarity hashing, but identifies two key challenges: 1) plumbing the whitespace-ignore option through the diffcore infrastructure, and 2) potential edge cases where size-based heuristics might incorrectly reject renames when whitespace is ignored.

The response moves the discussion forward by showing where in Git's internals such a feature would need to be implemented, while also acknowledging practical considerations about when whitespace-ignoring comparisons might produce misleading results. This suggests the change is technically feasible but would require careful design to handle edge cases. The email keeps the focus on the core technical challenge while providing concrete next steps for anyone interested in prototyping a solution.