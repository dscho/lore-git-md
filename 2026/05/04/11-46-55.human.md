This is a review of a preparatory refactoring patch in Christian Couder's URL-based promisor remote auto-configuration series. The patch adds infrastructure to track locally configured remote names separately from server-advertised names, which will be used in later patches for auto-generating remote names.

Toon Claes confirms the patch is purely mechanical at this stage - the new `local_name` field isn't yet populated, so there's no behavior change. The review is surface-level, just verifying the fallback behavior works as described. This matches the expected pattern for a preparatory refactoring that's already been reviewed positively in the thread.

The patch is part of a larger effort to improve promisor remote configuration by allowing URL pattern matching and auto-generation of remote names. This particular change enables the series to maintain traceability of original advertised names while supporting the new auto-configuration features coming in subsequent patches.