Here's a human-readable summary of the thread so far:

A user has reported concerning behavior where Git is consuming far more system resources than configured, particularly during basic operations like `git add` and `git commit`. Despite setting explicit limits (`gc.auto=0` to disable automatic garbage collection and `pack.threads=1` to restrict parallel packing), Git is spawning numerous pack-objects processes that use nearly all available CPU threads (28 out of 32) and significant disk space.

The report includes detailed system specifications (Git 2.51.0 on Linux 6.17 with glibc 2.42) and demonstrates a clear mismatch between the configured limits and Git's actual behavior. This suggests either a configuration parsing issue or pack-objects failing to properly respect resource constraints. The problem appears serious enough to potentially impact production environments where predictable resource usage is essential. The reporter has provided thorough reproduction details that should help maintainers investigate whether this is a regression or an underlying design limitation.