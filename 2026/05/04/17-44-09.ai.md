This is patch 5/9 in Karthik Nayak's v4 series refactoring Git's reference backend validation logic. The patch moves the `print_rejected_refs()` function earlier in `builtin/update-ref.c` to avoid needing a forward declaration when the function is used in a later commit of the series.

The change is purely mechanical - the function is moved verbatim with no modifications to its implementation. This is a preparatory step for upcoming changes in the series that will need to call this function from earlier in the file. The function itself handles printing error messages when ref updates are rejected during batch operations.

As part of the larger backend consolidation effort, this small adjustment helps enable the standardization of error reporting across Git's reference backends. The patch maintains all existing functionality while cleaning up the code organization in anticipation of future changes. Since this is a straightforward code movement with no behavioral impact, it's unlikely to generate discussion.