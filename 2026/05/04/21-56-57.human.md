This is a follow-up in the thread about fixing a compiler warning in `git name-rev`. The discussion has already converged on Junio C Hamano's preferred solution to eliminate the problematic variable entirely, and this email provides additional debugging context.

Kristoffer Haugsbakk shares his build configuration that initially didn't trigger the "may be used uninitialized" warning for the `commit` variable in `cmd_format_rev()`. His setup used `-O0` optimization, which apparently suppressed the warning. After switching to the full `config.mak.dev` settings (which likely includes higher optimization levels), the warning appears as expected.

This confirms that the warning is indeed sensitive to compiler optimization levels, as Ramsay Jones had noted earlier in the thread. The email serves as a useful data point about build configurations that can affect warning detection, though it doesn't change the already-agreed-upon solution path. The thread appears to be wrapping up with this final piece of diagnostic information.