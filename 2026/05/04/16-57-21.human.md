This is a technical question about Git's clean/smudge filter infrastructure, specifically regarding binary file handling.

Randall Becker asks how binary files (marked as such in .gitattributes) interact with clean/smudge filters when they match the filter pattern. He's concerned about portability when processing binary data through stdin/stdout and wants to know if there's a way for filters to explicitly ignore binary files - either by passing through the data unchanged or returning non-zero exit codes.

The motivation comes from a directory containing many text files and some binary files, where incorrect .gitattributes specifications are causing problems. Randall wants to make the system more robust against human error in file classification.

As a NonStop platform developer, Randall raises implicit portability concerns that may be relevant given Git's cross-platform nature. The question appears well-motivated by a real-world maintenance pain point, though it doesn't propose a specific solution yet. This seems like the start of a discussion that could lead to either documentation clarification or potential filter infrastructure improvements.