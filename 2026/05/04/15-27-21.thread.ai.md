Thread identity: Bug report about Git's resource usage exceeding configured limits during `git add` and `git commit` operations.

Core problem: Git appears to ignore resource-limiting configurations (`gc.auto=0`, `pack.threads=1`), spawning excessive pack-objects processes and consuming all available CPU threads (28/32) plus disk space during routine operations. This violates expected behavior where these settings should constrain resource usage.

Key technical details:
- Affects Git 2.51.0 on Linux 6.17 with glibc 2.42
- Pack operations spawn despite `gc.auto=0`
- `pack.threads=1` setting not respected (multiple pack-objects processes observed)
- Impacts CPU, RAM, and storage resources
- Clear reproduction case provided with configuration details

Open questions:
- Is this a configuration parsing bug or pack-objects not honoring settings?
- Does this affect other Git versions/platforms?
- What specific operations trigger the uncontrolled resource usage?

Participant positions:
- Original reporter provided detailed system info and reproduction steps showing the configuration-behavior mismatch

Related work: None mentioned yet, but potentially connected to general resource management and pack subsystem behavior.