This is patch 4/9 in a refactoring series that standardizes reference backend behavior across Git's files, packed, and reftable backends. The patch changes `ref_transaction_update()` to return a specific error type (`enum ref_transaction_error`) rather than a generic -1, setting up infrastructure for more detailed error reporting in future changes.

The change is mechanical - existing error cases now return `REF_TRANSACTION_ERROR_GENERIC` to maintain current behavior, while the function signature is updated to indicate richer error reporting will be possible. This is part of a larger effort to move validation and error handling logic from individual backends into the shared refs layer, making the backends more consistent and focused on storage implementation.

The patch has minimal impact on callers since it preserves existing behavior, but enables future patches in the series to introduce more specific error conditions. As part of a well-reviewed series that has already addressed feedback across multiple iterations, this appears to be a straightforward preparatory change.