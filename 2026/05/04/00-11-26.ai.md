This is patch 4/7 in Taylor Blau's RFC series addressing `--path-walk` filter compatibility. The patch adds support for the `combine` filter type, which allows composing multiple filters through intersection (objects must pass all child filters).

The implementation handles filter composition by:
- Treating each filter type as a monotonic restriction (applying multiple filters only tightens the criteria)
- Taking the minimum blob size limit when combining `blob:limit` filters
- Maintaining existing behavior for `sparse:oid` (only allowing one instance)
- Rejecting unsupported subfilters (like `tree:1`) with appropriate warnings

The patch includes comprehensive test coverage in t6601, verifying:
- Basic filter collapsing (`blob:none+tree:0` -> `tree:0`)
- Combined type and size limits
- Empty results from disjoint type filters
- Proper error reporting for unsupported subfilters

The changes touch `path-walk.c` with a refactored filter preparation system that separates the recursive filter application from resource cleanup. Documentation is updated to list `combine:` as a supported filter form.

This appears to be a well-constructed patch that follows the pattern established in earlier parts of the series. The test coverage is thorough and the implementation handles edge cases thoughtfully. As an RFC patch, it's likely to generate discussion about the composition semantics and whether any additional filter combinations need special handling.