This is a review of a bugfix for `git grep`'s column number reporting when using `--only-matching`. The patch, originally from René Scharfe, corrects an issue where subsequent matches on the same line showed incorrect column positions relative to GNU grep's behavior.

Phillip Wood provides a substantive review, confirming the technical approach of tracking positions from the line start rather than relative to previous match ends. His review notes the fix properly calculates column numbers by maintaining a pointer to the line start (`start = bol`) and using it to compute absolute positions (`cno = bol - start + match.rm_so + 1`).

The review validates that the change makes Git's output match GNU grep's behavior, showing correct absolute column numbers for each match. Phillip explicitly approves the fix with "That looks good, thanks for fixing it," indicating no further changes are needed. This follows earlier consensus from multiple reviewers that the current behavior was unintentional and matching GNU grep is the right approach.

The patch affects tools parsing `git grep --column` output for precise match locations, such as editor integrations. The change is low-risk, touching only the column calculation logic while preserving other grep functionality. With this review, the fix appears ready for merging.