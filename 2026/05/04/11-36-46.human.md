This is a confirmation and follow-up on the `git clone --dissociate` bugfix thread. Rasmus Villemoes reports that both the suggested workaround (`-c core.commitGraph=false`) and Jeff King's proposed patch successfully resolve the checkout failures they were experiencing. 

The confirmation comes with a minor build warning about a const-correctness issue in the patched code - while functionally correct, the warning suggests the function signature could be tightened to accept a `const struct commit*`. This is a polish point rather than a blocker, as the core race condition fix is working as intended.

The thread demonstrates a successful debugging cycle: from initial bug report, through root cause analysis (identifying the commit-graph dissociation race), to working solution and verification. With the fix confirmed by the original reporter, this likely clears the path for the patch to be picked up in the next integration window.