This is a refactoring patch that removes an unused typedef from the refs subsystem, part of Karthik Nayak's v4 series to consolidate ref backend logic. The change is straightforward - it deletes the `ref_transaction_commit_fn` typedef from `refs/refs-internal.h` since it's not referenced anywhere in the codebase.

This is a minor cleanup that prepares for the larger architectural changes in subsequent patches of the series. The removal is safe since the type isn't used, and it helps reduce unnecessary declarations in the header file. As the first patch in a well-reviewed series that has already gone through multiple iterations, this change can be considered low-risk and uncontroversial.