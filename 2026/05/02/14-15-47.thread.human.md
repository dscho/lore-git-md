The thread continues with the tenth patch of Derrick Stolee's v4 series to make `--path-walk` work with `--filter` options. This patch fixes a serious bug in the test infrastructure: the existing tagged-tree tests in `t6601-path-walk.sh` were accidentally passing because the tagged trees were also reachable from commits. Stolee discovered this while working on the `tree:0` filter behavior, and the fix creates genuinely tag-only-reachable tree objects using `git mktree` and `git hash-object -w` instead of extracting trees from commits. This is a well-motivated test fix that guards against the silent dropping of tagged trees under `tree:0` -- the very correctness issue that prompted the v4 rewrite. All nine previously posted patches remain unreviewed, and the critical SEEN-flag fix and `sparse:oid` integration patches continue to lack independent scrutiny. The series is still being posted incrementally, with patches 10/13 and 11/13 (the `tree:0` fix) remaining.