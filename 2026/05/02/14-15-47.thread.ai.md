**Thread identity:** Subject: [PATCH v3 00/12] pack-objects: add --path-walk and --filter interaction. Authors: Derrick Stolee, Taylor Blau. Series at v3, 12 patches, complete. Junio's "What's cooking" confirms the topic is in final refinement.

**Core problem or goal:** Make the `--path-walk` option to `git pack-objects` fully interoperable with all existing `--filter` options (`blob:none`, `blob:limit=<size>`, `sparse:<oid>`, `tree:0`, `object:type=<type>`, `combine`), moving filter logic from `builtin/pack-objects.c` into the `path-walk.c` API.

**Current status:** Series at v3, complete with 12 patches. Junio tracking without pressure. Stolee posted three additional bitmap-related patches on top. V3 changes include: passed all tests with `GIT_TEST_PACK_PATH_WALK=1`, uncovered test failures in `t5620-backfill.sh`, required moving directly-requested object emission earlier, prompted extensive test updates in `t6601-path-walk.sh`.

**Key technical details:**

Core architecture moves filter logic into path-walk API (`path-walk.c`). Key implementation patterns:
- `blob:limit` checks each blob's size via `odb_read_object_info()` before emission
- `sparse:oid` validates entire path groups against sparse-checkout patterns at once (biggest speedups: git/git sparse repack 77.91s to 31.41s)
- `object:type` adds `strict_types` flag to prevent direct object requests from bypassing type restrictions
- `combine` filters use recursive `prepare_filters_one()` helper

Patch details:
- **Patch 03/12:** New performance benchmark script `p5315-pack-objects-filter.sh`
- **Patch 10/12 (`tree:0` filter):** Suppresses all trees and blobs, keeping only commits and tags. Tagged trees collected into `/tagged-trees` pseudo-path. Non-zero tree-depths trigger fallback to standard revision walk.
- **Patch 11/12 (`object:type=<type>` filter):** Maps `LOFC_OBJECT_TYPE` onto per-type flags via bitwise AND. Fix: `handle_commit()` drops pending trees when `revs->tree_objects` is zero before path-walk sees them; fix sets `revs->tree_objects` during setup, resets to zero after `prepare_revision_walk()`. New `strict_types` flag gates direct-object-request bypass.
- **Patch 12/12 (`combine` filter):** Every supported child filter is a monotonic restriction, intersected via `path_walk_info`. `blob:limit` takes minimum; `sparse:oid` allowed once, refuses overwriting. `prepare_filters()` split into recursive helper.

**Open questions and loose ends:**
- `strict_types` behavior for `object:type` (not fully resolved)
- Stolee's warning that `sparse:oid` and `treesparse:oid` should not be supported by Git servers (local bundle construction only)
- `GIT_TEST_PACK_PATH_WALK=1` CI flag and placement decision remain open
- Bitmap documentation is a follow-up
- `git-pack-objects.adoc` typo ("Whe" instead of "When") will be fixed in v4
- Commit message wording on patch 02/12 ("once"/"one", "was not"/"did not") flagged but without certainty from the co-author

**Participant positions:**

- **Taylor Blau** (co-author): Three surface-level reviews across the series:
  1. Patch 05/12: single typo correction ("s/Whe/The/")
  2. Patch 02/12: two uncertain commit-message word-choice suggestions ("s/once/one/ ? Not sure", "s/was not/did not/ ? Also not sure"), then "Looks good."
  3. Patch 03/12: flags that `git ls-tree` should not be on the left-hand side of a pipe (testing hygiene), and suggests moving a `sed` pipeline step outside a loop to reduce process overhead. Offered as "not a huge deal" with "I don't feel strongly about it." The test itself is praised as "great."

No other reviewer has engaged with any patch in the series. The entire 12-patch series has received only surface-level reviews from the co-author. The code's behavior and correctness remain independently unverified by anyone outside the two co-authors (Stolee and Blau). No substantive technical review discussion has occurred.

**Related work:** Three additional bitmap-related patches posted on top by Stolee (separate work). Junio tracking without pressure indicates the topic is progressing but not yet merged.