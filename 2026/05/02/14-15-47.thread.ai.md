**Thread identity:** Subject: `[PATCH v3 00/12] pack-objects: add --path-walk and --filter interaction`. Authors: Derrick Stolee and Taylor Blau. Series at v3, 12 patches. Junio's "What's cooking" confirms the topic is in final refinement.

**Core problem or goal:** Make the `--path-walk` option to `git pack-objects` fully interoperable with all existing `--filter` options (`blob:none`, `blob:limit=<size>`, `sparse:<oid>`, `tree:0`, `object:type=<type>`, `combine`), moving filter logic from `builtin/pack-objects.c` into the `path-walk.c` API.

**Current status:** Series at v3, 12 patches, complete. Junio tracking without pressure. Stolee posted three additional bitmap-related patches on top. V3 changes included: passed all tests with `GIT_TEST_PACK_PATH_WALK=1`, uncovered test failures in `t5620-backfill.sh`, required moving directly-requested object emission earlier, prompted extensive test updates in `t6601-path-walk.sh`. All twelve patches have now received some review from co-author Blau, but depth varies widely. The series remains independently unverified by anyone outside the two co-authors.

Blau has now posted a final-iteration reply to the v3 cover letter confirming he has read the full series and left inline comments (mostly surface-level, with a few genuinely substantive but non-blocking questions). He characterizes his remaining comments as "more for my own edification than identifying anything to change" and states he is "really excited to see this all moving along," mentioning plans to deploy the feature at GitHub. He expects the next reroll (v4) to be "pretty close to the finish line." This is a social sign-off from the co-author/reviewer but does not constitute independent technical validation -- the series has still received zero review from anyone outside the two co-authors.

`git-pack-objects.adoc` typo ("Whe" instead of "When") will be fixed in v4. Commit message wording on patch 02/12 ("once"/"one", "was not"/"did not") flagged but without certainty.

**Key technical details:**

Core architecture moves filter logic into path-walk API (`path-walk.c`). Key implementation patterns:
- `blob:limit` checks each blob's size via `odb_read_object_info()` before emission.
- `sparse:oid` validates entire path groups against sparse-checkout patterns at once (biggest speedup: git/git sparse repack 77.91s to 31.41s).
- `object:type` adds `strict_types` flag to prevent direct object requests from bypassing type restrictions.
- `combine` filters use recursive `prepare_filters_one()` helper.

Patch details:
- **Patch 03/12:** New performance benchmark script `p5315-pack-objects-filter.sh`.
- **Patch 04/12 (always emit directly-requested objects):** Introduces `path_is_for_direct_objects()` heuristic checking if a path starts with `/`. Blau flags this as brittle but offers no alternative; admits limited familiarity with path-walk internals. Also suggests an `ASSERT()` to guard against a NULL path pointer. Neither concern is blocking.
- **Patch 06/12:** Replaces blanket rejection of `--filter` in `git backfill` with a call to `path_walk_filter_compatible()`. Blau engaged with the question of whether error messages should name which child filter is incompatible within a `combine` filter, then self-resolved that suggestion as impractical. No change requested.
- **Patch 07/12 (`blob:limit=<size>`):** Uses `odb_read_object_info()` during the path walk to check blob sizes. Blau raises the question of whether this should use `odb_read_object_info_extended()` with `OBJECT_INFO_SKIP_FETCH_OBJECT` to avoid downloading blobs just to check their size in a partial clone, or temporarily disable `fetch_if_missing`. Blau admits he does not know enough about `git backfill` to judge which behavior is more appropriate. `builtin/backfill.c` already has its own extra `blob:limit` guard for this reason (backfill's batch-download strategy would be defeated by on-demand fetch); that guard may need updating if Blau's suggestion is adopted. Blau signed off with "The rest looks good to me." No change requested; question left for Stolee to answer.
- **Patch 10/12 (`tree:0` filter):** Suppresses all trees and blobs, keeping only commits and tags. Tagged trees collected into `/tagged-trees` pseudo-path. Non-zero tree-depths trigger fallback to standard revision walk. Blau sent a follow-up question asking why `/tagged-trees` -- the same sentinel pseudo-path pattern used for `/tagged-blobs` and `/tags` -- was applied to *directly-listed trees* under `--filter=tree:0`, rather than only to tagged trees discovered via annotated tags. The likely answer is that directly-listed trees (objects specified via `--stdin` or `--revs`) are handled by `setup_pending_objects()`'s direct-object routing and never reach the path-walk's `tree:0` suppression logic, so the sentinel path is only needed for trees discovered *during* the walk via annotated tags. Stolee has not replied yet.
- **Patch 11/12 (`object:type=<type>` filter):** Maps `LOFC_OBJECT_TYPE` onto per-type flags via bitwise AND. Fix: `handle_commit()` drops pending trees when `revs->tree_objects` is zero before path-walk sees them; fix sets `revs->tree_objects` during setup, resets to zero after `prepare_revision_walk()`. New `strict_types` flag gates direct-object-request bypass.
- **Patch 12/12 (`combine` filter):** Every supported child filter is a monotonic restriction, intersected via `path_walk_info`. `blob:limit` takes minimum; `sparse:oid` allowed once, refuses overwriting. `prepare_filters()` split into recursive helper.

**Open questions and loose ends:**
- `strict_types` behavior for `object:type` not fully resolved.
- Stolee's warning that `sparse:oid` and `treesparse:oid` should not be supported by Git servers (local bundle construction only).
- `GIT_TEST_PACK_PATH_WALK=1` CI flag and placement decision remain open.
- Bitmap documentation is a follow-up.
- `git-pack-objects.adoc` typo ("Whe" instead of "When") will be fixed in v4.
- Commit message wording on patch 02/12 ("once"/"one", "was not"/"did not") flagged but without certainty from the co-author.
- The `path_is_for_direct_objects()` heuristic brittleness (patch 04/12) acknowledged with no alternative proposed; NULL-pointer guard (`ASSERT()`) suggested by Blau but not yet applied.
- Patch 07/12: Blau raises the question of whether `odb_read_object_info()` in `blob:limit` should be replaced with `odb_read_object_info_extended()` using `OBJECT_INFO_SKIP_FETCH_OBJECT` to avoid on-demand fetch of blobs for size checking in partial clones. No change requested; left for Stolee to resolve.
- Patch 10/12: Blau's question about `/tagged-trees` sentinel application to directly-listed trees under `--filter=tree:0` versus only tagged-trees. No change requested, no action suggested. Stolee has not replied yet.

**Participant positions:**

- **Taylor Blau** (co-author): Has now reviewed twelve of twelve patches, plus a final cover-letter sign-off:
  - Final sign-off (cover letter reply): States he has read the full series and left inline comments. Characterizes remaining comments as "more for my own edification than identifying anything to change." Expresses excitement about the feature and plans to deploy at GitHub. Expects v4 to be "pretty close to the finish line." This is a social green light but not independent technical validation.
  - Patch 04/12 (substantive): Engages with heuristic design, flags brittleness, suggests NULL-pointer guard. Admits limited path-walk familiarity.
  - Patch 06/12 (substantive-light): Considers verbose-error-message design trade-off for combined filters, then self-resolves. No change requested.
  - Patch 07/12 (moderately substantive): Raises one legitimate technical design question about `odb_read_object_info()` vs. `odb_read_object_info_extended()` with `OBJECT_INFO_SKIP_FETCH_OBJECT` to avoid on-demand fetch in partial clones. Openly admits limited knowledge of `git backfill`. Signs off with "The rest looks good to me." No change requested; question left for Stolee to answer.
  - Patch 10/12 (surface-level follow-up): Single inline question about design rationale for `/tagged-trees` sentinel. No change requested, no bug described, no action suggested. Likely thinking out loud while re-reading his own co-authored patch.
  - Patches 02/12, 03/12, 05/12 (surface-level): Typo fixes, uncertain word-choice suggestions, pipe-placement fix, test-loop optimization. "Not a huge deal" / "I don't feel strongly" characterize the tone.
  - Patches 01/12, 08/12, 09/12, 11/12, 12/12 (unreviewed): Received no review comment at all from Blau or any other participant. The co-author's silence on five of the twelve patches, combined with the lightweight nature of the feedback received on the reviewed patches, means the series has effectively received zero independent code review from anyone outside the two co-authors. Blau's social sign-off does not change this assessment.

No other reviewer has engaged with any patch in the series.

**Related work:** Three additional bitmap-related patches posted on top by Stolee (separate work). Junio tracking without pressure indicates the topic is progressing but not yet merged.