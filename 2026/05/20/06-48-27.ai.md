Bugfix addressing a memory leak in the patch application code, following up on an earlier fix by Jeff King (Peff). The patch corrects Junio's initial attempt to fix a strbuf leak in `apply.c`, noting that Jeff had already addressed this issue as part of enabling expensive tests.

The change is minimal - just removing the redundant strbuf_release() call since the leak is already handled elsewhere. This is a follow-up cleanup to ensure consistency, coming directly from the maintainer after realizing the duplication. The thread shows the project's attention to resource management, with multiple eyes catching and fixing the same issue through different paths.

Key details remain unchanged from the original report: the leak occurred when `read_patch_file()` failed after partially reading into a strbuf. The fix touches only error handling in `apply.c`, maintaining the same simple approach of making the caller responsible for cleanup. This email serves as an acknowledgment that the issue was already resolved, closing the loop on the maintainer's initial patch attempt.