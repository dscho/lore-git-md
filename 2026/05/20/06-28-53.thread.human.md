This thread documents a quick back-and-forth between Junio C Hamano and Jeff King (Peff) about fixing a memory leak in Git's patch application code. The issue involved a strbuf that wasn't being properly freed when `read_patch_file()` failed during patch processing.

Junio initially sent a straightforward fix to clean up the strbuf in error paths, but Jeff pointed out that he had already addressed this same leak as part of enabling some expensive tests. Junio's follow-up email acknowledges this and removes his now-unnecessary fix, keeping only Jeff's original solution. The exchange shows how multiple contributors independently identified and fixed the same issue through different paths - a testament to the project's attention to resource management.

The thread is effectively closed with this acknowledgment; no further action is needed since the leak is already properly handled. Both fixes were minimal (just a few lines touching error handling in `apply.c`), and the end result maintains clean error paths without redundant cleanup code.