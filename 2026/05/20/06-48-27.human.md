This is a follow-up correction from Junio regarding a memory leak fix in `apply.c`. The original patch (summarized in the thread context) addressed a strbuf leak in the patch application code by making `apply_patch()` responsible for releasing the buffer when `read_patch_file()` fails. 

Junio now notes that this particular issue was actually already fixed by Jeff King (Peff) as part of enabling expensive tests, implying this patch may be redundant. The tone suggests this was an oversight rather than a new discovery - Junio is acknowledging that the fix was already been handled elsewhere.

No technical changes are proposed in this email; it's simply a retraction notice about the previous patch. Given that the maintainer is withdrawing the change and the original issue is already resolved, this thread can likely be considered closed. The exchange demonstrates the project's careful attention to resource leaks, even when catching duplicates.