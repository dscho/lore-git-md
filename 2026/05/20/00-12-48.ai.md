This is a review comment from Junio C Hamano on Christian Couder's promisor remote configuration series. The email focuses on naming consistency in the patch that adds local name handling for auto-generated promisor remotes.

Junio acknowledges the technical correctness of the changes but questions the naming choices, particularly the `promisor_info_internal_name()` helper function that returns either the local or advertised name. He notes the inconsistency where this same function's return value is stored in variables called both "remote_name" and "local" in different parts of the code, suggesting `promisor_info_local_name()` might be a clearer name.

The review is substantive - it engages with the code's design and naming conventions rather than just surface-level issues. Junio also observes (but doesn't insist on changing) the existing pattern of casting away constness when freeing struct members, noting this is inherited behavior rather than newly introduced.

This feedback comes late in the series' lifecycle (v3 patch 4/8) and doesn't raise any blocking concerns, focusing instead on making the naming more intuitive. The thread context shows the overall promisor remote functionality is technically complete and approved, with this being a minor polish point in an otherwise ready-to-merge series.