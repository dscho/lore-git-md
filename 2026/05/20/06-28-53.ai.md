Bugfix addressing a memory leak in the patch application code. The patch fixes a case where `read_patch_file()` could fail after partially reading data into a strbuf, leaving the buffer allocated. The change makes the caller (`apply_patch()`) responsible for releasing the strbuf in the error case, which simplifies the error handling flow.

The fix is straightforward - it wraps the error return with a `strbuf_release()` call. This is a typical cleanup patch from the maintainer, catching a resource leak that was likely noticed during code review or testing. The change is small (4 lines) and focused, touching only the error path in `apply.c`. Since it comes from Junio himself, we can assume it's been tested and will likely be merged quickly.