This is the final polish for the `--max-count-oldest` feature, addressing CI portability issues in the test suite. Mirko Faina acknowledges Junio C Hamano's feedback about three test script problems that caused CI failures: using plain `rm` instead of `rm -f` for cleanup, piping `git log` output through `wc -l` (which masks the command's exit status), and non-portable `wc -l` output formatting across platforms. The fix adopts Git's `test_line_count` helper for reliable line counting and makes the cleanup more robust.

This represents the last remaining issue before the feature can be merged, following eight previous iterations that refined the implementation and addressed all technical concerns. The patch maintains the feature's core functionality while ensuring it works reliably across platforms. With these final CI fixes, the implementation appears ready for integration.