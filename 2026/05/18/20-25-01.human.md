This is a refactoring patch from René Scharfe that simplifies overflow checking in `strbuf_grow()` by using the `st_add3()` helper function. The change replaces manual overflow checks with a single call to `st_add3()`, which handles the arithmetic safely while also improving the diagnostic message when overflows occur (it will now show the values that caused the overflow).

The patch is part of an ongoing effort to improve Git's memory safety patterns, specifically around overflow checking. It follows up on previous discussion where Junio Hamano raised concerns about evaluation order in the `ALLOC_GROW` macro usage. René's solution here stores the result of `st_add3()` in a variable before passing it to `ALLOC_GROW`, addressing that concern while maintaining the same safety guarantees.

The change is small (just 4 lines modified) but meaningful - it makes the code more maintainable by using the existing overflow-checking infrastructure rather than open-coding the checks. This continues René's pattern of careful, precise refactoring that improves code quality without changing behavior. The patch appears ready for merging, having incorporated all substantive feedback from the previous version.