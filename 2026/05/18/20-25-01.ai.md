This is a refactoring patch from René Scharfe that simplifies overflow checking in `strbuf_grow()` by using the existing `st_add3()` helper. The change replaces manual overflow checks with a single call to `st_add3()`, which handles the arithmetic safely while also providing more detailed error messages when overflows occur (showing the problematic values).

The patch is part of an ongoing effort to improve Git's memory safety patterns, specifically around overflow checking. It builds on previous discussions about optimizing these checks while maintaining safety. The v2 iteration addresses Junio Hamano's earlier concern about evaluation order by storing the `st_add3()` result in a variable before passing it to `ALLOC_GROW`.

This is a small but meaningful improvement to core infrastructure - typical of René Scharfe's precise, thoughtful refactoring style. The change maintains all safety guarantees while making the code more maintainable. The series appears ready for merging, having incorporated all substantive feedback from the v1 discussion.