This is a feature patch that enhances `git commit --fixup` to support non-interactive message specification via `-m` and `-F` options when using `--fixup=amend:` or `--fixup=reword:`. 

The patch builds on previous discussion from 2021 where Junio Hamano noted the inconsistency in how `-m` was handled. Currently these fixup types require using an interactive editor, which limits automation potential. The change enables workflows like batch-applying rewritten messages from files or AI-generated content.

Key technical changes include:
- Modifying `builtin/commit.c` to properly handle message flags with fixup types
- Updating documentation to reflect the new behavior
- Adding comprehensive test coverage in `t/t7500-commit-template-squash-signoff.sh`

The implementation maintains backward compatibility while addressing a genuine workflow limitation. The author (a first-time contributor) has kept the scope focused, explicitly noting they're not yet handling `-c/-C` options to keep the change manageable. The patch appears well-crafted with thorough test coverage and clear documentation updates.

This change would be particularly useful for automated workflows and tools that want to programmatically modify commit messages without requiring interactive editor sessions. The patch seems ready for review and likely uncontroversial given its focused scope and clear benefits.