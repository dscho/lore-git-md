This is a performance optimization patch from René Scharfe, part of the ongoing effort to improve Git's memory safety patterns. The patch specifically targets overflow checking in the `st_add()` function used throughout Git's codebase.

The key technical change introduces a new `st_add_overflow()` helper that uses Clang's `__builtin_add_overflow()` intrinsic when available, falling back to Git's existing overflow check otherwise. This provides a measurable 4% performance improvement on ARM64 (Apple M1) for operations that heavily use strbuf growth, as demonstrated by René's benchmark of `git cat-file --batch-all-objects`.

The patch builds on previous discussion with Jeff King and Junio Hamano, addressing their feedback about evaluation order and optimization paths. It maintains all safety guarantees while taking advantage of compiler optimizations - Clang can now use carry flag checks on ARM64 like it already does on x64. The change is small (22 lines) but impactful, touching core memory arithmetic infrastructure.

This appears ready for merging, having incorporated all substantive review feedback while achieving its performance goals. It's a typical René Scharfe patch - precise, well-measured, and improving Git's foundations in a maintainable way. The optimization will benefit all Clang users, particularly on ARM platforms where the improvement is most noticeable.