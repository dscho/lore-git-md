This is patch 7/8 in Derrick Stolee's final v5 series introducing negotiation controls for fetch/push operations. The patch adds config support for the `--negotiation-include` feature via `remote.<name>.negotiationInclude`, mirroring the existing `negotiationRestrict` config pattern.

The key addition is a multi-valued config option that specifies refs or glob patterns whose tips should always be sent as "have" lines during fetch negotiation with the remote. The implementation:
- Follows the same pattern as `negotiationRestrict` for consistency
- Uses the same `resolve_negotiation_include()` codepath as CLI options
- Is additive with normal negotiation (config-specified refs are sent unconditionally on top of heuristically selected commits)
- Supports empty values to reset the list (ignoring earlier configs)
- Includes comprehensive tests covering:
  - Basic config usage
  - Glob pattern matching
  - CLI override behavior
  - Interaction with `--negotiation-restrict`

The patch carries Matthew Cheetham's Reviewed-by tag, confirming the technical approach matches the established pattern from earlier patches in the series. This completes the config integration for the negotiation controls, with the final patch (8/8) handling push integration. The implementation is thorough and maintains consistency with the rest of the series' design.