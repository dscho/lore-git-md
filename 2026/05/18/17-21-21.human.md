This is the final patch in Derrick Stolee's series adding negotiation controls for fetch/push operations. The patch completes the feature by wiring up the new `--negotiation-include` and `--negotiation-restrict` config options for push operations when `push.negotiate` is enabled.

Matthew Cheetham, who has been the primary reviewer throughout the series, gives this patch his approval (LGTM) while noting one minor documentation issue - a stale reference to `--negotiation-tip` that should be updated to `--negotiation-restrict` since the option was renamed earlier in the series.

The implementation follows the expected pattern from previous patches, passing the configured negotiation parameters through to the child `git fetch --negotiate-only` process that handles push negotiation. The patch includes appropriate test coverage with two new tests that verify the include and restrict behaviors work as expected during push operations.

With this review, all 8 patches in the v4 series now have LGTM-level approval from Cheetham, marking the series as technically complete pending the minor documentation fix. The series provides important controls for monorepo scenarios where fine-grained negotiation behavior can significantly reduce unnecessary object transfers.