**Review of the `--all-remotes` flag implementation in the `--prune-merged` series.** Phillip Wood raises a substantive interface design question about whether allowing additional remote arguments alongside `--all-remotes` is the right approach, suggesting the behavior may be unintuitive.

The current implementation permits explicit remote names to be specified alongside `--all-remotes`, which will combine both the configured remotes and any additional names provided. Phillip questions this design, implying it might be clearer to either:
1. Make `--all-remotes` fully self-contained (ignoring any additional remote arguments), or
2. Reject the combination entirely with an error

This is a meaningful interface consideration that wasn't previously discussed in the thread. The question doesn't challenge the core functionality or safety model (already approved by Junio), but probes whether the UX follows the principle of least surprise. The current behavior could be seen as either a useful combination feature or an unnecessary complication - a judgment call that warrants discussion.

The series remains technically complete, with this being a final polish question rather than a blocking issue. Harald will need to decide whether to adjust the behavior or justify the current design in response.