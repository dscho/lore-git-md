This is a performance optimization and refactoring patch series from René Scharfe, continuing the discussion around improving Git's overflow checking patterns. The series builds on previous work to standardize and optimize memory safety checks, specifically focusing on the `st_add()` helper function used throughout Git's codebase.

The main change in v2 introduces a new `st_add_overflow()` helper that mimics Clang's `__builtin_add_overflow()` intrinsic for size_t operations, while maintaining compatibility with GCC. The patch shows measured performance improvements on ARM64 (M1) processors while keeping the same safety guarantees. René also addresses feedback from the v1 review by passing a variable rather than an expression to ALLOC_GROW in strbuf_grow().

The series represents the culmination of technical discussions between René, Jeff King, and Junio Hamano about the best way to handle overflow checking in Git's core. The current approach combines type safety, single-evaluation guarantees, and platform-specific optimizations while maintaining backwards compatibility. The patches are characteristically clean and focused from René - they don't introduce new functionality but improve the code's robustness and performance in a maintainable way.

This appears ready for merging, representing a clear improvement over the current implementation with no outstanding technical concerns raised in the review thread. The changes are low-risk but provide measurable benefits, particularly on ARM architectures.