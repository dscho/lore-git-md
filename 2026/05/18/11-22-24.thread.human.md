This thread is about Erik Cervin-Edin's patch series adding non-interactive message specification (`-m`, `-F`, `-c`, `-C`) to `git commit --fixup` for amend-style commits. The feature is now complete in its second version, with all substantive feedback from the first round addressed—including author identity consistency, code reuse, and test hygiene.

The series has reached the final review stage. Junio Hamano has explicitly asked for last looks on the second patch, which implements `-c`/`--edit` and `-C`/`--reuse-message` support. Phillip Wood, who reviewed the sequencer-related aspects, has seen his suggestions incorporated. The implementation is robust, with unified message handling through `prepare_to_commit()`, thorough test coverage, and careful edge-case management.

The only remaining loose end is a minor code hygiene issue: a redundant check for incompatible options (`-C`, `-c`, `-F`) remains in `die_for_incompatible_opt4()` after the same logic was removed from `die_for_incompatible_opt3()`. Erik plans to clean this up after returning from travel, but it’s not blocking the feature, which he’s already been using successfully for months. The series is otherwise production-ready.