This is a performance optimization and refactoring patch series from René Scharfe, continuing the discussion around improving Git's overflow checking patterns. The series builds on previous work to optimize `st_add()` using Clang's `__builtin_add_overflow()` while maintaining safety.

The key changes in v2:
1. Introduces a new helper `st_add_overflow()` that mimics `__builtin_add_overflow()` for size_t, avoiding code duplication in the `st_add()` implementation
2. Modifies the strbuf change to store the result of `st_add3()` in a variable before passing to `ALLOC_GROW`, addressing Junio's earlier concern about multiple evaluation
3. Maintains the same overflow safety guarantees while optimizing the Clang code path

The interdiff shows René has carefully considered the feedback from v1, particularly around evaluation order in the ALLOC_GROW macro usage. The new version separates the overflow calculation from the allocation, making the control flow clearer while preserving the performance benefits.

This continues René's pattern of precise, thoughtful refactoring - each change is well-justified and maintains backward compatibility. The series appears ready for merging, having addressed the substantive review feedback while keeping its original optimization goals intact. The changes are small but meaningful improvements to Git's core memory safety infrastructure.