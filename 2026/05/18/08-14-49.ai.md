This is the final patch in Harald Nordgren's series adding `git branch --prune-merged` for safe local branch cleanup. The thread context shows the series is now complete after addressing Junio C Hamano's sole remaining concern about `--force` bypassing safety checks in v8.

Harald's brief follow-up email serves as a final call for reviews, noting the removal of the potentially dangerous `--force` override makes the feature "less of a foot-gun" while acknowledging it may be seen as "less useful" without that escape hatch. The safety model remains intact - branches are only pruned if their tips are reachable from their upstream remote-tracking branches, with additional safeguards against deleting checked-out branches or default branches with matching names.

With all technical concerns resolved and no further discussion needed, this appears ready for integration. The series provides a well-considered mechanism for automated branch cleanup that aligns with Git's conservative approach to data removal. The separate `fetch.pruneLocalBranches` concept mentioned earlier remains an independent proposal not part of this finalized work.