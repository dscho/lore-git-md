This is a technical discussion about the implementation details of a patch that adds configurable timeout behavior for config file locking. The patch (v2) was previously rejected but reconsidered after incorporating maintainer feedback.

Patrick Steinhardt weighs in on Junio C Hamano's review of the caching mechanism in the timeout implementation. While Junio suggested making the timeout explicitly process-wide (using `the_repository`) since the current caching makes it effectively process-wide anyway, Patrick argues the opposite direction - that the per-repository behavior should be preserved by removing the caching entirely. His rationale is that config file locking doesn't happen frequently enough for the caching optimization to matter, and the overhead of repeated config lookups would be negligible compared to the actual file operations.

This represents a substantive technical discussion between two core contributors about implementation tradeoffs, with Patrick providing a counterpoint to Junio's earlier suggestion. The exchange remains focused on the technical merits rather than revisiting the broader concept, which was already settled in previous discussion. The thread appears to be converging toward a resolution, with both parties engaging constructively on the remaining design question.