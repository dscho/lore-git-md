This is a follow-up in the thread about `git maintenance` error handling with unavailable remotes. Anselm Schüler clarifies that while the systemd timers do use `--keep-going` (as Phillip Wood noted was fixed in Git 2.45.3), there's still an issue where within a single repository, if one remote fails, the prefetch task won't attempt the remaining remotes for that repo.

The key new observation is that `--keep-going` only prevents failures from cascading across different repositories, but doesn't address the sequential nature of remote fetching within a single repository. This means the original problem - where one bad remote prevents fetching from other remotes in the same repo - persists even with the systemd timer fix.

Anselm's clarification suggests the scope of the issue is broader than initially understood in Phillip's response. The thread now has two distinct aspects to resolve:
1. Better error messages to identify which remote failed (original request)
2. Making prefetch continue to other remotes after a failure within a single repo (newly clarified behavior)

The exchange shows the reporter is technically engaged and understands the behavior well enough to identify this nuance. While Phillip's earlier response provided a useful partial solution, this follow-up indicates there's more work needed for a complete fix. The ball is now back in Phillip's court to either confirm this is a valid issue worth addressing or explain why the current behavior is intentional.