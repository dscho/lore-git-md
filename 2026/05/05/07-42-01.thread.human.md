Here's a human-readable summary of the thread:

This thread addresses a bug in `git log --follow` where it fails to track file history across subtree merges, despite `git blame` correctly handling such cases. The patch modifies `log-tree.c` to properly follow renames through merge commits by checking each parent for potential renames, bringing behavior in line with `git blame`.

The change is particularly useful for repositories that use subtree merges to import external projects. The commit includes a clear explanation of both the current limitation and the solution, along with a new test case that verifies the fixed behavior by creating a repository with a subtree merge and confirming `git log --follow` can now trace the file's history across the merge boundary. The implementation appears clean and focused, extending existing rename-following logic without introducing unnecessary complexity.