This is a technical clarification in Johannes Schindelin's ongoing series addressing Windows' 4GB+ object handling limitations. The exchange focuses on test performance metrics between patch 8/11 (precomputed packs) and the earlier unsafe SHA-1 optimization from patch 5/11.

Schindelin confirms the benchmark numbers in patch 8/11 already reflect the unsafe SHA-1 optimization's limited impact, explaining that the performance gain only applies to certain CI jobs (specifically `linux-TEST-vars`) and not Windows builds. This reveals an important platform-specific constraint - Windows builds can't leverage OpenSSL's faster SHA-1 implementation, so the unsafe optimization provides no benefit there.

The response highlights the series' careful balancing act between test accuracy and practicality. While the precomputed pack optimization shows dramatic speedups (reducing pack generation from minutes to seconds), the author acknowledges the Windows-specific limitations that prevent fully optimized test runs across all platforms. This level of transparency about platform constraints is valuable for reviewers evaluating the series' tradeoffs.

The exchange doesn't require any patch modifications - it simply provides context about why the performance numbers appear as they do. This type of clarification is typical for a complex, multi-platform series where optimizations may have uneven effects across different environments.