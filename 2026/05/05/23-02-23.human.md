This is the third patch in a bugfix series addressing negative context values in diff options. The patch adds defensive `BUG()` checks in xdiff layer to catch invalid negative context lengths, even though the earlier patches should prevent such values from reaching this point.

The change guards against negative values in xdiff's `ctxlen` and `interhunkctxlen` fields, which must remain signed for correct arithmetic in hunk generation. The patch explains why unsigned types can't be used here (they would break the signed subtraction logic in context window calculations) while still ensuring invalid inputs trigger an immediate failure.

This is a defense-in-depth measure that complements the earlier CLI validation patches. The change is straightforward and well-explained, touching only xdiff's emit.c with clear safety checks. The series continues to look like a solid correctness improvement for an edge case that could otherwise produce malformed diffs.