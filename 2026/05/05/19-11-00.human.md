This is a review comment on the first patch in Johannes Schindelin's series addressing Windows platform limitations with large objects (>4GB). Torsten Bögershausen raises a technical question about variable types in the patch.

The review focuses on a specific implementation detail: whether the variable `c` (used for reading packed object data) should be declared as `uint8_t` rather than `unsigned long`. The reviewer notes that since `c` is assigned from an `unsigned char*` and only uses the lower 7 bits (via `& 0x7f`), using a smaller type might be more appropriate.

This is a substantive review that engages with the technical details of the change, though it's narrowly focused on one aspect of the implementation. The question doesn't challenge the overall approach of using `size_t` for object sizes (which is the main point of the patch), but rather suggests a possible refinement in how the data is read.

The review maintains a constructive tone while pointing out what appears to be a potential inconsistency in the type usage. This level of attention to detail is typical for Git code reviews, where even small implementation choices are often scrutinized for correctness and consistency.