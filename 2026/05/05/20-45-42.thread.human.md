Here's the human-readable summary:

Kristoffer Haugsbakk has improved Git's documentation to clarify that commit-graph optimizations are disabled when using replace objects or commit grafts. This behavior was previously under-specified in the manuals. The patch adds clear "CAVEATS" sections to both the `git-commit-graph` and `git-replace` man pages, with cross-references between them.

After initial review feedback from commit-graph expert Derrick Stolee, the v2 version refined the documentation structure - keeping the `git-commit-graph` man page focused on core functionality while moving explicit performance impact warnings to the `git-replace` documentation. Stolee has now approved the final version with an "LGTM", indicating the patch is ready to be picked up. This completes a clean documentation improvement that will help users understand an important performance consideration when working with these features.