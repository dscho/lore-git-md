This is a technical follow-up in Kristoffer Haugsbakk's ongoing `git format-rev` series, specifically addressing implementation details in patch 3/5. The email clarifies two points from Phillip Wood's earlier review regarding control flow and printing behavior.

The key technical clarification concerns the printing logic differences between `git name-rev` and the new `git format-rev` command. Haugsbakk explains that while both commands handle object name lookups similarly, they differ in how they report lookup failures - `name-rev` prints the failure case before continuing, while `format-rev` uses separate print statements for both success and failure cases. This distinction affects where the shared printing code can reasonably be placed.

On the control flow question, Haugsbakk confirms reverting to using `continue` rather than `goto start` after verifying the behavioral difference is negligible in practice (only affecting pathological cases by potentially reprocessing the same character). The author notes unsuccessful attempts to trigger edge cases that would expose any parsing bugs from this control flow choice.

The exchange demonstrates the careful attention to detail characteristic of this review thread, with the author engaging substantively with Phillip Wood's feedback while providing clear technical rationale for the current implementation choices. The discussion remains focused on ensuring correct behavior as the series progresses toward finalization.