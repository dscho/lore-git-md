This is a technical follow-up in the ongoing discussion about integrating path-walk optimizations with filter options in `git pack-objects`. Taylor Blau identifies a specific test failure in t5310 when running with `GIT_TEST_PACK_PATH_WALK=1`, where a partial clone operation fails due to missing blob objects that should have been included.

The issue stems from patch 03/10 in Derrick Stolee's series, where the blob:none filter implementation incorrectly skips directly-referenced blobs during path-walk. Taylor provides a concrete diff that fixes this by introducing a new `prune_tree_blobs` flag to properly handle blob filtering while preserving necessary objects. The proposed change maintains the filtering behavior while ensuring required blobs are still sent.

Taylor notes this is just one of several test failures uncovered by the path-walk testing, with others likely related to delta compression differences. He suggests adding `GIT_TEST_PACK_PATH_WALK=1` to the CI test matrix if there are no historical reasons against it, as it would help catch similar issues earlier.

The exchange demonstrates the careful validation process for this performance-sensitive change, with both contributors collaborating to identify and resolve edge cases in the path-walk/filter interaction before the series can be finalized.