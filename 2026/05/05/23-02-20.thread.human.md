This thread addresses a bug where negative values for diff context parameters (`-U`/`--unified` and `--inter-hunk-context`) would produce malformed output. Michael Montalbo's patch series converts these options to use unsigned values with proper validation, ensuring negative inputs are rejected consistently with existing config behavior.

The technical solution has been thoroughly reviewed across four patches: the first two convert the CLI options to unsigned values with validation, the third adds a `BUG()` check in xdiff's hunk generation as defense-in-depth, and the fourth clarifies documentation around `PARSE_OPT_NONEG` after Junio pointed out it should only concern boolean negation. The changes touch core diff machinery and xdiff internals while maintaining backward compatibility.

Junio has accepted the technical approach after several rounds of review, with the final exchange being purely administrative - Michael acknowledged some formatting issues in the cover letter that will be cleaned up in future submissions. With no outstanding technical objections, the series appears ready for merging. The thread demonstrates proper handling of numeric validation through `OPT_UNSIGNED` while keeping `PARSE_OPT_NONEG` focused on its original purpose for boolean options.