This is a build system patch that ensures the macOS credential helper properly links against Rust libraries when Rust support is enabled in Git. The change is part of the ongoing Rustification effort.

The patch introduces a new `RUST_LIBS` variable to cleanly handle Rust library dependencies for the `git-credential-osxkeychain` helper. When Rust is disabled (`NO_RUST`), this becomes a no-op, avoiding build failures on systems without Rust tooling. The change addresses previous reviewer feedback by simplifying the variable naming and avoiding potentially confusing "LINK" terminology.

The modification is straightforward - it adds the Rust library dependency to the helper's build target while maintaining the existing framework dependencies (-framework Security -framework CoreFoundation). The patch appears well-contained and properly motivated, with a clear commit message explaining both the technical change and its rationale.

This is a small but meaningful step in ensuring Git's Rust integration works correctly across all components, including platform-specific helpers like the macOS credential manager. The change is unlikely to be controversial as it follows established patterns in the build system.