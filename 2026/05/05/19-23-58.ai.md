Feature: Final patch in v4 series adding `--all-remotes` convenience flag to `git branch --forked` and `--prune-merged`.

This patch completes Harald Nordgren's v4 series by introducing a `--all-remotes` option that works with both the `--forked` and `--prune-merged` flags. When used, it automatically includes all configured remotes in the operation while still allowing explicit remote arguments. The implementation:

1. Adds the new option to both commands with proper documentation
2. Includes comprehensive tests verifying:
   - Basic functionality with `--forked`
   - Error handling for invalid combinations
   - Integration with `--prune-merged` across multiple remotes
3. Maintains all existing safety checks from previous patches
4. Preserves the series' architectural separation between listing (`--forked`) and deletion (`--prune-merged`)

The change is straightforward and well-tested, building naturally on the decomposed design approach championed by Junio Hamano earlier in the thread. The tests demonstrate thoughtful consideration of edge cases, including interaction with explicit remote arguments and validation of remote names.

This completes the technical implementation of the feature set, leaving only Johannes Sixt's naming concern about `--prune-merged` as an outstanding issue. The series now provides both granular control (via explicit remotes and per-branch configuration) and convenience (via `--all-remotes` and `fetch --prune-merged` integration) while maintaining robust safety mechanisms.