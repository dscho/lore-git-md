Thread identity: Documentation patch clarifying commit-graph behavior with replace objects and grafts, authored by Kristoffer Haugsbakk.

Core problem or goal: Improve user-facing documentation to explicitly state that commit-graph functionality is disabled when replace objects or commit grafts are present, a detail previously only covered in technical documentation.

Key technical details: Adds new "CAVEATS" sections to both `git-commit-graph` and `git-replace` man pages, with cross-references between them. The change documents existing behavior where the commit-graph optimization is bypassed when either replace objects (managed via `git-replace`) or commit grafts (legacy mechanism for rewriting parent relationships) are active.

Participant positions: Kristoffer Haugsbakk as author, no other participants yet in this new thread. The patch follows Git's documentation conventions and appears technically accurate based on the existing implementation behavior described in `technical/commit-graph.adoc`.

Related work: References the existing technical documentation (`technical/commit-graph.adoc`) where this behavior was previously documented internally but not exposed to end users through the command manuals.