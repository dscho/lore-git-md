This is a bugfix response from Jeff King addressing UBSan failures in Git's CI pipeline. The issues were originally reported by Junio C Hamano when EXPENSIVE tests were enabled in CI, revealing two previously hidden problems.

The first patch fixes a memory leak in `apply.c` when handling "patch too large" errors. The second and more substantial patch addresses the signed integer overflow in `commit.c` that occurs during UTF-8 verification of large commit messages. The fix involves restructuring the verification logic to avoid the problematic arithmetic operation on INT_MIN.

The changes are relatively small (19 lines added, 18 removed across two files) but address real issues that could affect users. The fact that these were only caught with EXPENSIVE tests enabled suggests they're edge cases, but still represent legitimate bugs that needed fixing. Jeff King's response is characteristically direct and technically precise - he identifies the root causes and provides clean solutions without unnecessary commentary.

These fixes will help stabilize the 'next' integration branch where the issues were discovered. The patches demonstrate the value of expanded CI testing, as these issues might have otherwise gone unnoticed until hitting real-world edge cases.