This is a review comment from Derrick Stolee on Kristofer Karlsson's performance optimization series for `tips_reachable_from_bases()` in commit-reach.c. The thread has progressed through two versions, with v2 switching from a decoration-based approach to using object flags after Jeff King's feedback.

Stolee's comment is brief but substantive - he identifies a test coverage gap that allowed the early termination bug in v1 to slip through. The bug occurred when multiple refs pointed to the same commit, causing the decoration hash to overwrite entries and break the generation index comparison. While the algorithmic fix is now in place (showing consistent 4.3x speedups), Stolee rightly notes the need for a regression test covering this edge case.

The review is technically engaged - it doesn't just rubber-stamp the solution but considers long-term maintainability by calling for test coverage of the failure mode. This is typical of Stolee's contributions, which often focus on ensuring robustness of performance optimizations. The comment assumes familiarity with the thread's technical details (object flags vs decorations, generation numbers) but doesn't rehash them, efficiently moving the discussion forward.