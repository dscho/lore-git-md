This is a follow-up in the thread about improving Git's config command syntax handling, where Harald Nordgren responds to Junio's review of their initial patch. The original patch aimed to help users who mistakenly use `=` in config keys (like `git config foo.key=value`) by showing a "did you mean" hint.

Harald acknowledges Junio's suggestion to instead accept the `=` syntax transparently, but reports that implementing this proved more complex than expected. They're now proposing to fall back to the original warning approach but with improved correctness. The email also includes an off-topic note about switching to plain text mode in Gmail to avoid misdirected replies.

The technical discussion remains focused on the core challenge: how to best handle this common user mistake without breaking existing config files that legitimately contain `=` in section headers. Harald's response indicates they're still working through the implementation tradeoffs between Junio's suggested approach and their original warning-based solution.

This continues the substantive technical discussion from Junio's review, though the email is brief and doesn't provide details about what made the "liberal acceptance" approach complex to implement. The thread remains active with no clear resolution yet on which direction to take.