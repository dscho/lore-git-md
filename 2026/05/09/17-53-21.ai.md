This is a technical response in the ongoing thread about Git's maintenance subsystem issues, specifically addressing concerns about default behavior and repository corruption risks. Jeff King (Peff) clarifies the distinction between different maintenance modes and their associated risks.

Key clarifications:
- The problematic behavior is not from scheduled maintenance jobs but from the "auto" mode triggered by commands like `git commit`
- This auto mode has existed for years but was previously handled by `git gc --auto` before being moved to `git maintenance run --auto`
- The locking issue is specific to `--detach` mode in auto maintenance, not time-based scheduled runs
- Peff confirms the regression became significantly worse in Git 2.54 due to changes in the maintenance implementation

The response helps disentangle several overlapping concerns in the thread:
1. Differentiates between time-based maintenance (via `git maintenance register`) and auto-triggered maintenance
2. Explains the historical context of the auto maintenance feature
3. Narrows the locking issue to specific circumstances (`--detach` mode)
4. Reiterates the version-specific regression (v2.54) while acknowledging broader concerns

This moves the discussion forward by providing clearer technical boundaries around the problem space, though it doesn't yet propose solutions. The email maintains a measured tone while acknowledging the severity of the issues raised earlier in the thread.