This is a final polish for the `--max-count-oldest` feature, addressing a portability issue in the test suite. Junio C Hamano points out that the test uses `tac` (reverse `cat`), which fails on macOS. He suggests replacing it with a more portable approach using `head` after `--reverse`.

The change is minor and doesn't affect the feature's core functionality, which was already finalized in previous iterations. The test case verifies that `--max-count-oldest` works correctly with `--reverse`, showing the oldest commits in forward order. Junio's proposed alternative maintains the same verification while working across all platforms.

This is the last remaining issue before the feature can be merged, showing the maintainer's attention to cross-platform compatibility even in test infrastructure. The patch author will likely send a v6 with this small test fix, after which the series should be ready for inclusion.