This is a final polish on the `--max-count-oldest` feature, addressing a portability issue in the test suite. Junio C Hamano points out that the test case uses `tac` (a GNUism) which fails on macOS. He suggests replacing it with a more portable combination of `--reverse` and `head`.

The change is minor and non-controversial - it's purely about test portability without affecting the feature itself. The suggested alternative produces the same expected output while working across platforms. This follows the project's practice of avoiding non-POSIX commands in tests unless absolutely necessary.

The thread context shows this is the last remaining issue before the feature can be merged, with all substantive technical and interface concerns already resolved in previous iterations. The maintainer's quick response here indicates the patch is otherwise ready for inclusion.