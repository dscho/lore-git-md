This is a continuation of the discussion about `git diff --word-diff`'s whitespace handling behavior. Vincent Lefevre pushes back on Johannes Sixt's earlier position that the current behavior is correct, arguing that the documentation doesn't adequately explain that `--word-diff` inherits Git's line-diff whitespace sensitivity.

Vincent makes two key points:
1. The documentation for `--word-diff` doesn't mention its dependence on line-diff behavior, only referring to "changed words"
2. External tools like GNU wdiff handle the same input differently (and in his view, more intuitively) by not highlighting whitespace-only changes as word differences

The exchange highlights a documentation gap - while Johannes maintains the technical correctness of Git's current implementation, Vincent argues the behavior is confusing and undocumented. The core disagreement remains between Git's internal consistency (matching its own diff engine) versus user expectations shaped by other tools.

This appears to be reaching an impasse on technical merits, suggesting the next steps might involve either:
- Documentation updates to better set expectations
- A feature request to add a whitespace-agnostic word-diff mode
- Or accepting the current behavior as a known divergence from some external tools

The thread shows no movement toward consensus yet, with both sides maintaining their positions about what constitutes correct behavior.