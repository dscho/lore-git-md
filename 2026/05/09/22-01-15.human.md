This is a documentation clarification from Junio in response to the final patch of a series that fixed bugs around negative diff context values. Junio pushes back on the proposed documentation change, arguing that:

1. The `PARSE_OPT_NONEG` flag was never involved in the bugs being fixed (which stemmed from losing implicit negative value rejection when converting to `OPT_INTEGER_F`)

2. The documentation should simply clarify what "NONEG" means (preventing `--no-<option>` forms) without adding extra verbiage about what it doesn't do

Junio's position is that the additional sentences about negative numeric values and `OPT_UNSIGNED` are unnecessary since they don't relate to the actual flag behavior. The maintainer suggests stopping after explaining the boolean negation aspect.

This is a minor but precise documentation nit from Junio on a patch that was otherwise ready to go. The exchange shows the maintainer's attention to keeping documentation focused and accurate, even for small clarifications.