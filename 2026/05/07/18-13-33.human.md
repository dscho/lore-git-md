Documentation update for `git log --no-follow` behavior.

This v2 patch improves the documentation of `--no-follow` in `git log`, building on the initial version. The changes clarify that the option works with pathspecs (not just single files) and consistently uses this terminology in both the command and config documentation. The patch also explicitly notes that `--no-follow` can override the `log.follow` config setting.

The update references three historical commits that introduced and modified this functionality, showing good attention to Git's history. The changes are focused and non-controversial, touching only the documentation files with clear, precise wording improvements. The patch appears ready for inclusion, as it simply brings the documentation in line with long-standing behavior.