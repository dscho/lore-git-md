This is a documentation patch that clarifies the interaction between commit-graph functionality and replace objects/grafts. It's the second version of a change that adds explicit warnings to both `git-commit-graph` and `git-replace` man pages about this behavior.

The patch adds new "CAVEATS" sections to both documentation files, noting that the presence of replace objects or commit grafts will disable commit-graph reading/writing. The v2 revision incorporates feedback from Derrick Stolee (a commit-graph expert) by adding a note about potential performance implications in the `git-replace` documentation while keeping the `git-commit-graph` warning more neutral.

The change surfaces an important implementation detail that was previously only documented in the technical documentation (`technical/commit-graph.adoc`). The patch follows Git's documentation conventions well, with clean cross-references between the man pages. The interdiff shows the specific wording improvement made in response to review feedback.

This is a straightforward documentation improvement that helps users understand a non-obvious limitation of the commit-graph feature when working with replace objects or grafts. The patch appears ready for merging with all review feedback addressed.