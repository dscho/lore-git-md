This is a refactoring patch from Kristoffer Haugsbakk, part of the v4 series introducing the experimental `git format-rev` command. The patch makes a purely mechanical change to `builtin/name-rev.c` to align with Git's coding guidelines by adding braces around both blocks of an if-else statement where one side already required braces.

The change enforces consistency as specified in `CodingGuidelines` - when any arm of a conditional requires braces, all arms should use braces for uniformity. The patch touches two conditional blocks in the file, both following the same pattern of adding braces to the single-line `if` case to match the braced `else` case.

This is a straightforward preparatory change with no functional impact, serving as groundwork for the larger `format-rev` feature. The patch has already been through multiple iterations (now at v4) with previous feedback addressed, including fixing stray formatting in an earlier version. The diff shows the simple brace additions with no other modifications to the logic or behavior.