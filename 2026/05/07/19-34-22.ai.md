This is a refactoring patch (3/5) in Kristoffer Haugsbakk's v4 series introducing the new `git format-rev` command. The patch factors out common code from `name-rev.c` to be shared with the upcoming command.

Key changes:
- Introduces a `command` struct with tagged union to handle command-specific payloads (currently only `name-rev` is supported)
- Moves the core line-processing logic into a more generic `name_rev_line` function that takes the command type as input
- Maintains all existing functionality while preparing for the new command
- Follows reviewer feedback by reverting to `continue` instead of `goto` and improving pointer arithmetic

The refactoring is cleanly separated from behavior changes - the patch modifies control flow organization but doesn't alter what the code actually does. This continues the series' good practice of keeping preparatory changes distinct from new functionality.

Technical details:
- Uses enum+union pattern for command-specific behavior
- Preserves all existing error handling and edge cases
- Maintains the same memory management patterns
- Prepares for `format-rev`'s text processing needs while not implementing them yet

The patch shows careful attention to reviewer feedback from Phillip Wood while maintaining the series' overall direction. It demonstrates good patch hygiene by keeping the refactoring mechanical and well-contained.