This thread documents and closes a long-standing gap in Git's documentation: the `--no-follow` option for `git log`, which has existed since 2012 but was never officially documented. Tamir Duberstein authored a four-iteration patch series to add this documentation, working closely with Junio Hamano, the Git maintainer, to ensure accuracy and consistency with Git's documentation style.

The final version clarifies how `--no-follow` interacts with the existing `--follow` option and `log.follow` configuration, establishing a clear precedence hierarchy (`--no-follow` overrides both). The documentation also preserves the existing constraints of `--follow`—such as its limitation to single filenames and linear history—while intentionally leaving behaviors for directories and non-linear history unspecified, in line with Git's documentation philosophy. Junio's review confirmed the documentation accurately reflects the implementation, and the patch was merged into the `next` branch without further changes.

The thread is now conclusively closed, with no open questions or loose ends. The work builds on the original 2012 `--follow` implementation by Junio and later `log.follow` configuration work by Jeff King, and it resolves a minor integration conflict with the concurrent `mv/log-follow-mergy` topic. This effort serves as a reference case for how Git handles technical documentation, balancing precision with restraint in specifying undefined behavior.