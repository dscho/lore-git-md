This thread documents the previously-undocumented `--no-follow` option for `git log`, which has existed since 2012 but was only mentioned in code until now. Tamir Duberstein sent an initial patch (v1) followed by a refined v2 that improved the technical precision around pathspec handling. The v2 patch clarifies that `--follow` works with a single pathspec (not just a single file) and ensures consistent terminology between the man page and config documentation.

Junio Hamano has now reviewed v2, supporting the documentation effort but questioning whether "single pathspec" is technically accurate for rename tracking, which operates at the file level. He suggests minor wording adjustments to better match Git's actual behavior and proposes a clearer commit subject line. The discussion remains focused on documentation accuracy, with no objections to documenting the feature itself. The patch appears nearly ready, needing only small refinements to the wording about pathspec handling before being merged.