This thread documents the `--no-follow` option for `git log`, which has existed in the code since 2012 but was missing from the official documentation. The option allows users to override the `log.follow` configuration when it's enabled by default. The patch cleanly adds this documentation alongside the existing `--follow` description in the man pages and updates the config documentation to note this override capability. 

The change is straightforward and non-controversial, simply bringing the documentation in line with long-standing behavior. The patch references the relevant historical commits that introduced the functionality, demonstrating good attention to Git's history while making a focused improvement to the user-facing docs.