This is a brief discussion about the behavior of negative values for `--max-count` in Git's revision walking options. Junio C Hamano questions Mirko Faina's assertion that many scripts rely on the current behavior where `--max-count=-1` acts as if no limit was specified.

Junio expresses skepticism about how widespread this usage actually is, asking what practical scenarios would require scripts to use a negative value to override another `--max-count` setting. The exchange comes as part of finalizing the new `--max-count-oldest` feature, where Mirko had previously noted that repurposing negative values wasn't viable due to this existing behavior.

The discussion appears to be a minor clarification point rather than a substantive issue - the core implementation and interface of `--max-count-oldest` remain settled from earlier in the thread. Junio's questioning seems aimed at understanding rather than challenging the design decision, as the feature has already addressed all major review feedback through seven iterations.