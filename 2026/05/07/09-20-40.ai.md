This is a brief discussion about the behavior of negative values for `--max-count` in Git's revision walking options. Junio C Hamano questions Mirko Faina's assertion that many scripts might rely on the current behavior where negative values disable the limit.

Key points:
- Junio expresses skepticism about how many scripts actually depend on the current behavior of `--max-count=-1` acting as "no limit"
- He suggests the only plausible use case would be to defeat a `--max-count` value coming from elsewhere, but questions how common this scenario is
- The exchange occurs in the context of finalizing the `--max-count-oldest` feature, where using negative values for the new option was considered but rejected due to this existing behavior

This appears to be a minor clarification point rather than a substantive issue, as the `--max-count-oldest` implementation path was already settled in previous emails. The discussion doesn't introduce new technical concerns or require changes to the feature.