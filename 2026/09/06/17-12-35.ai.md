This is Junio C Hamano’s substantive review of the final patch in Kristoffer Haugsbakk’s abandoned `--[no-]range-diff-notes` series. The email drills into the usability problem that led to the feature’s abandonment: the interaction between `--no-range-diff-notes` and the toggle sequence (`--no-range-diff-notes --range-diff-notes`).

Junio first restates the intended design—two independent notes lists, one for patch output (`--notes`) and one for range-diff (`--range-diff-notes`)—and confirms that the rewritten documentation snippet correctly describes it. He then focuses on the toggle mechanism, which was meant to revert the override and restore the default state (both contexts using the same notes). Junio argues that the current implementation’s rule—`--no-range-diff-notes` merely pretends no `--range-diff-notes` was ever given—is counterintuitive and leads to confusing edge cases. For example, if an alias defines `--range-diff-notes=foo` and the user runs `git fmtr --no-range-diff-notes`, the command falls back to using `--notes` for the range-diff, which may not be what the user expects.

Junio proposes an alternative rule: `--no-range-diff-notes` should mean “no notes for range-diff,” period. Under this rule, the toggle sequence (`--no-range-diff-notes --range-diff-notes`) would become a no-op, and the user would need to explicitly specify `--range-diff-notes=<ref>` to re-enable range-diff notes. This would eliminate the need for the toggle mechanism and make the behavior more predictable. He asks Kristoffer whether this alternative would simplify the design and improve clarity.

The email does not reopen the feature or suggest a path forward; it merely dissects the usability issue that blocked the series. The thread remains closed, and no further action is expected. If the feature is revived, Junio’s proposed rule would likely be the starting point for a redesign.