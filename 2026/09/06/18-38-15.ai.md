This is a **substantive review reply** to patch 5/13 in Patrick Steinhardt’s ODB refactoring series, which removes the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c`. Justin Tobler’s response confirms his understanding of the historical context (the 2019 workaround for `config_with_options()`’s limitation) and the 2021 fix that made it obsolete. He explicitly acknowledges that the patch safely removes the workaround now that the `cache-tree` subsystem no longer depends on `the_repository`, and his tone ("Nice to have one less workaround") signals approval.

The review is **substantive** because it engages with the *why* of the change—verifying the correctness of removing a legacy mechanism—rather than just the *how*. Justin’s initial confusion ("I had to reread the commit message a couple of times") and subsequent clarification ("Ok, now we can properly read objects...") demonstrate he traced the code path and understood the implications for submodule object resolution. No new issues are raised; this is a positive, confirming review from a contributor familiar with the ODB subsystem (Justin Tobler is closely involved in the ODB abstraction effort alongside Patrick Steinhardt).

For the **summarizer brief**, key details to retain: the patch removes the last non-test caller of `odb_add_submodule_source_by_path()` in `submodule-config.c`, eliminating a 2019 workaround that became unnecessary after 2021’s `e3e8bf046e`. The review confirms the cleanup is safe because earlier patches in the series (notably patch 2/13) removed implicit dependencies on `the_repository` in the `cache-tree` subsystem. The `#define USE_THE_REPOSITORY_VARIABLE` is also removed as it is no longer needed. No behavioral changes or test failures are expected. Justin Tobler’s review adds confidence to the patch’s correctness but does not introduce new technical points or objections.