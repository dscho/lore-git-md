This is a documentation clarification for the abandoned `--[no-]range-diff-notes` feature in `git format-patch`. Kristoffer Haugsbakk provides a rewritten documentation snippet to address Junio C Hamano’s earlier criticism that the original phrasing obscured the intended behavior.

The new text explicitly describes how the feature maintains two separate notes lists—one for the final patch output (controlled by `--notes`) and one for the range-diff section (controlled by `--range-diff-notes`). It walks through scenarios: using the same notes in both contexts by default, overriding the range-diff notes with a specific ref, and toggling the override off to revert to the patch notes. The rewrite does not alter the implementation or revive the topic; it merely corrects the record by showing that the code already followed the independent-control design Junio sketched, even if the original documentation failed to communicate it.

The clarification does not resolve the conceptual flaw Junio identified—the ambiguous fallback semantics when `--range-diff-notes` is used without `--range-diff`—but it confirms that the implementation was closer to the intended design than the documentation suggested. No further action is expected; the thread remains closed.