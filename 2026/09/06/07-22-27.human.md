This is the final clarification in the abandoned `[PATCH 0/3] format-patch: add --[no-]range-diff-notes` series. Kristoffer Haugsbakk acknowledges that the documentation in the original patch was unclear about how the proposed `--range-diff-notes` option actually worked, and provides a rewritten explanation to correct the record.

The key point is that the implementation *did* already follow the design Junio C Hamano sketched—where `--notes` controls only the final patch output and `--range-diff-notes` controls only the range-diff material—even though the documentation suggested otherwise. The confusion arose because the docs described only the edge case where the range-diff notes list is empty, obscuring the independent control the code already provided. Kristoffer’s rewrite spells out how the two options can be used together to build separate notes lists for patches and range-diffs, and how to toggle the override behavior.

This email does not propose reviving the topic or sending a v2; it merely clarifies what the code already did. The thread remains closed, and the feature is still abandoned due to the unresolved usability concern Junio raised: the interaction between `--range-diff-notes` and the existing `--notes` cannot be defined in simple terms, and the fallback behavior (e.g., `--no-notes --range-diff-notes` suppressing notes in both contexts) is unintuitive. If someone revisits this feature, they will need to address that conceptual flaw, likely by enforcing explicit separation of the two notes lists and clarifying the CLI semantics.