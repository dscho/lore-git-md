This is the seventh patch in Patrick Steinhardt’s 8-patch series refactoring ODB alternates handling, part of the ongoing ODB abstraction effort. The patch wires up the new `odb_create_on_disk_options` mechanism to eliminate the last direct use of `odb_add_to_alternates_file()` in `git clone`.

Justin Tobler’s review is surface-level, offering brief approval ("This patch looks good") without engaging with technical details, edge cases, or testing. His earlier comments in the thread (on patches 1 and 6) were more substantive, raising architectural questions about repository initialization and ODB interface design, but here he focuses only on the mechanical change: replacing the manual alternates-writing loop with a call to `create_object_database(the_repository, &alternates)`.

The patch itself is a straightforward integration step. It removes 6 lines from `builtin/clone.c` and updates `setup.c` to pass the alternates `strvec` (collected by `collect_alternates()` in earlier patches) to `create_object_database()`. The change is backward-compatible: existing callers like `init_db()` continue to work by passing `NULL` for the alternates parameter. The diff is minimal, touching only three files, and the commit message clearly ties the change to the series’ broader goal of making alternates an ODB backend implementation detail.

No user-visible behavior changes are expected, and the patch’s correctness depends on the lockfile API adoption in patch 6 (which resolved TOCTOU race concerns) and the alternates consolidation in patches 1–5. The patch is unlikely to be controversial, as it merely connects the new infrastructure to the existing codepath. Reviewers may briefly check whether the alternates `strvec` is properly handled in edge cases (e.g., empty or `NULL` vectors), but the implementation appears robust.

The patch is now ready for integration, pending final review of the remaining patches in the series (particularly patch 8, which removes the unused `write_alternates()` infrastructure). The series as a whole is technically complete, with the lockfile API adoption addressing the primary blocker raised in v1. Justin Tobler’s earlier feedback on interface design remains open but does not block this patch.