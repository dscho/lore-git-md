This is a follow-up discussion in the abandoned `[PATCH 0/3] format-patch: add --[no-]range-diff-notes` thread, focusing on the usability and documentation of the proposed toggle mechanism for reverting range-diff notes overrides. The exchange between Kristoffer Haugsbakk and D. Ben Knoble clarifies how the `--no-range-diff-notes --range-diff-notes` sequence was intended to work: it acts as a toggle to undo prior overrides (e.g., `--range-diff-notes=changelog`) and restore the default state where both patches and range-diffs use the same notes (those specified by `--notes`). Kristoffer provides a concrete example showing how this sequence avoids the need to repeat all `--notes` arguments, arguing that the flexibility justifies the added complexity.

The discussion also touches on a tangential issue: `format-patch`’s handling of default notes. Kristoffer suggests `format-patch` should respect `format.notes` rather than `notes.displayRef`, as the latter is more suited for personal use (e.g., `git log`) than for generating patches intended for email distribution. Ben agrees, noting that `format.notes` is documented as a boolean but can also specify a ref, implying a potential inconsistency in the current behavior.

No code changes or new designs are proposed here. The conversation remains theoretical, as the feature was abandoned due to Junio C Hamano’s substantive usability concerns about the option semantics and toggle mechanism. The exchange serves primarily to document the intended behavior for future reference, should the feature be revived. The key takeaway is that the toggle mechanism was a deliberate (if complex) design choice, and any redesign would need to address its unintuitive edge cases while preserving the ability to independently control notes in patches versus range-diffs.