This is a review reply in Patrick Steinhardt’s 13-patch series removing ad-hoc ODB source linking. The series is part of the ongoing ODB abstraction and `the_repository` removal efforts.

Justin Tobler replies to patch 5/13, which removes the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c`. The patch deletes four lines that registered a submodule’s object database as an in-memory source of `the_repository` when reading `.gitmodules` from a blob in a non-main repository. This cleanup is now safe because earlier patches in the series (notably patch 2/13) eliminated implicit dependencies on submodule ODB registration in the `cache-tree` subsystem, ensuring all object lookups use the correct repository context.

Justin’s review is **surface-level**: he restates the commit message in his own words, confirming his understanding of the historical workaround and its removal. He describes the problem ("a gitmodule blob from a repository that is not `the_repository` would fail to be read"), the solution ("now we can properly read objects from their respective repository source"), and expresses approval with a brief "Nice to have one less workaround." There is no engagement with the code’s behavior, edge cases, or technical implications beyond what the commit message already explains.

The patch itself is uncontroversial and well-motivated. It touches only `submodule-config.c`, removing dead code that became unnecessary after the 2021 config machinery improvements. The series has already verified the cleanup is complete by passing the `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` test mode, which BUGs on any lazy registration. No new issues or objections are raised in this reply. The patch is a straightforward refactoring step in a larger, well-reviewed series.