This is a reply in the ongoing discussion about inconsistent branch name validation in Git's porcelain commands, specifically whether bare `@` should be rejected as a branch name.

Kristoffer Haugsbakk cites a 2024 thread where Junio C Hamano argued that `@` as a synonym for `HEAD` should be deprecated and removed, except in compound names like `refs/remotes/origin/@`. The citation is presented as evidence that the current behavior is intentional and that tightening validation for bare `@` would align with a broader design direction. Haugsbakk does not add new technical analysis or propose a patch, but the reference to Junio’s stance carries weight: it suggests that rejecting bare `@` in `--branch` mode would not be a rogue change but part of a deliberate, if slow-moving, project goal.

The reply is brief and does not engage with the usability trap (inability to check out `@` via porcelain commands) or the specific validation logic in `check-ref-format --branch`. It functions primarily as a pointer to prior authority, nudging the discussion toward the conclusion that stricter validation is the right path. No new open questions are introduced; the thread remains focused on implementation.