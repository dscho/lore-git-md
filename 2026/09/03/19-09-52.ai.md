This is a **surface-level style discussion** in the final stages of the `git checkout -m` autostash conflict handling series. The series itself is already approved and queued for merging to `next`; this exchange focuses narrowly on brace-style consistency in `stash.c`.

Junio notes that when one branch in an `if/else if/else` cascade uses braces, all branches should follow suit for visual consistency. Harald acknowledges the oversight and expresses interest in adopting a linter (like Linux’s `checkpatch.pl`) to catch such issues automatically. Junio then asks whether Git’s existing `.clang-format` configuration already enforces this rule. Harald confirms it does not, as GitHub CI does not flag mismatched braces. The discussion is purely mechanical and does not affect the series’ readiness for integration—it remains on track for `next`. The exchange highlights a recurring contributor pain point (style nits) and a potential tooling gap in Git’s CI pipeline.