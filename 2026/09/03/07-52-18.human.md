This is a bugfix for the `git history` command. The patch prevents a crash in `git history split` when the parent commit’s tree object is missing from the object store—whether due to corruption, manual removal, or an incomplete restore. The fix is a simple NULL check in `write_ondisk_index()` that turns a SIGSEGV into a clean error message, following Git’s established pattern for handling unloadable objects.

The patch is now in its second iteration, addressing only cosmetic nits: the commit message no longer mentions UBSan, and the From address now matches the Signed-off-by line. Patrick Steinhardt reviewed it and called the fix “obviously good,” so it is effectively ready to merge once Junio picks it up. No test was added, but the commit message includes clear reproduction steps that could be turned into a regression test later if needed.

This is a minimal, uncontroversial change that fixes a real crash. It touches only `builtin/history.c` and leaves the on-disk format untouched. If you care about `git history` stability, this patch is worth noting; otherwise, it’s safe to treat as a routine bugfix that will land in the next cycle.