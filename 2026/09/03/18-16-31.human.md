This is the final exchange in the `dk/use-nsec-runtime` series, which graduated to `next` in July 2024. The thread now centers on a forward-looking architectural concern: the series’ new dependency on `repo_config_values()`—which only works for `the_repository`—creates a latent bug when `is_racy_stat()` is called on a repository that isn’t the global one. Junio flags this as a blocker for future recursive submodule operations (e.g., `git diff` or `git status` traversing submodules without spawning subprocesses), while Ben Knoble acknowledges the issue but argues that papering over it with a safe default might reduce the impetus to fix the underlying limitation. Both agree that `git grep --recurse-submodule` (which already works recursively) doesn’t exercise the racy-entry code paths, so its success offers no assurance against latent bugs in other recursive contexts.

The discussion is purely speculative at this point—no code changes are proposed or needed. Junio’s concern is that the current stopgap (treating `core.useNanosec` as false when `istate->repo != the_repository`) could become a long-term obstacle, while Knoble suggests that future series can add safe fallbacks if needed. The tone is collaborative, and the exchange reinforces the architectural constraint documented in the thread summary: deeper refactoring of `repo_config_values()` (or a shift to `struct repo_settings`) will be required to support recursive submodule operations fully. For now, the series remains in `next` awaiting routine conflict resolution with `ty/repo-config-cleanups`.