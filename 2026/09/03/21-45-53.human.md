This is the documentation patch that completes a two-part series clarifying a long-standing but undocumented edge case in `git cherry-pick --no-commit`. The patch updates `Documentation/git-cherry-pick.adoc` to explicitly state that `CHERRY_PICK_HEAD` is *not* created when a `--no-commit` cherry-pick fails due to conflicts, contrasting this with `git revert --no-commit` (which *does* write `REVERT_HEAD`). The commit message cites the 2011 commit (d7e5c0cbfb) that introduced this behavior but left it undocumented, and the patch itself adds a bullet point to the conflict-handling list and a dedicated paragraph under `--no-commit` explaining the asymmetry and its implications for `git commit`.

The patch is small and focused, but the thread context makes it more significant. Junio’s reply to the test patch (part 1) revealed he initially misinterpreted the intent, assuming the patch was proposing to *change* the behavior to write `CHERRY_PICK_HEAD` in the conflict case. His AI-assisted rewrite of the commit message framed the current behavior as potentially incorrect, elevating this documentation patch from a simple clarification to a defense of the existing design. The documentation now explicitly justifies the asymmetry with `git revert`, which may prompt broader discussion about sequencer consistency. Reviewers are likely to focus on whether the current behavior is intuitive or whether it should be aligned with `git revert`.

The patch touches no code, introduces no new symbols or CLI options, and makes no on-disk format changes. It references `CHERRY_PICK_HEAD`, `REVERT_HEAD`, and the `--no-commit` option as key symbols, and mentions the `git commit -c <commit>` workaround for preserving authorship. The documentation is clear and well-motivated, but the thread’s trajectory suggests this could become a flashpoint for larger questions about how the sequencer should handle conflicts and intermediate state. If the current behavior is deemed correct, this patch will serve as a useful reference; if not, it may catalyze a follow-up series to align `git cherry-pick` and `git revert`. Either way, it’s a worthwhile read for anyone interested in sequencer internals or user-facing edge cases.