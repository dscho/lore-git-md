This is a test patch that plugs a small gap in the cherry-pick conflict test suite.

The patch adds a new test case to `t3507-cherry-pick-conflict.sh` that verifies `CHERRY_PICK_HEAD` is *not* created when a `git cherry-pick --no-commit` operation fails due to conflicts. The commit message explains that this edge case—conflicting pick under `--no-commit`—was previously untested, even though it is a common user scenario. The new test sits alongside existing tests that already check the ref’s absence after a clean pick and its presence after a conflicting pick without `--no-commit`.

The change is minimal (six lines) and self-contained; it does not touch production code, only the test script. The rationale is clear and the test follows the established pattern in the file. Nothing here looks controversial or under-motivated—it is a straightforward test coverage improvement.