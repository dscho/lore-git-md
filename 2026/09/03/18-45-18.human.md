This is a reply in the ongoing design discussion about whether Git should allow `@` as a branch name. Junio C Hamano, the project maintainer, weighs in to clarify his 2024 stance on deprecating bare `@` as a synonym for `HEAD`. He notes that the partial quote used earlier in the thread omits critical context: his original argument was not a blanket rejection of `@` but a targeted proposal to remove its *special-cased* interpretation as `HEAD` while preserving its use in compound names like `foo@bar` or `long-branch-name{^..otherbranch,..@}`. The distinction matters because the current inconsistency—where porcelain commands allow creating a branch named `@` but then fail to check it out—stems from this special-cased behavior.

The email does not propose a patch or resolve the design split, but it reframes the debate. Junio’s clarification suggests that the usability argument for `@` (e.g., in complex expressions) is compatible with tightening validation, as long as the fix targets only the bare `@` case. This nudges the thread toward a potential compromise: reject `@` as a standalone branch name (to prevent the usability trap) while keeping it functional in compound names (to preserve workflow ergonomics). The ball is now back in the contributors’ court to propose a patch that aligns with this nuance.