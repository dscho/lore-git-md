This is a **tooling discussion** in the final stages of the `git checkout -m` autostash conflict handling series. The series itself is approved and queued for `next`; this exchange focuses on whether Git’s `.clang-format` configuration enforces consistent brace usage in `if/else if/else` cascades.

Junio confirms that Git’s local `make style` target runs `git clang-format` to check style on modified C and header files, but this check is not part of GitHub CI. Harald notes that GitHub CI does not flag mismatched braces, implying the `.clang-format` configuration does not enforce this rule. The discussion is purely mechanical and does not affect the series’ integration status—it remains on track for `next`. The exchange highlights a contributor pain point (style nits) and a potential gap in Git’s CI tooling.