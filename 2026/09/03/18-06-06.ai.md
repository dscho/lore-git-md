This email is a brief follow-up in the ongoing `[PATCH v2 0/2]` thread fixing a pathspec edge case in Git’s directory traversal code. The author, Yannik Tausch, notes they just discovered a related discussion (Junio’s message linked as [1]) and plan to review it before responding further.

The thread itself remains a two-patch bugfix series addressing a long-standing issue where an exclude pathspec appearing first in the list causes `common_prefix_len()` to incorrectly return zero, even when subsequent positive pathspecs share a common directory. The v2 iteration adopted Junio’s output-parameter design (`common_prefix_len(pathspec, &matched_prefix)`), split the fix into a memory-safety prerequisite and the core logic change, and added thorough test coverage. Discussion had converged on the design, with only a minor implementation detail (`char **` vs. `const char **`) left unresolved.

This follow-up does not introduce new technical content or alter the series’ status—it merely signals the author’s intent to incorporate additional context from the linked discussion before proceeding. The series is still effectively ready for merging pending final review.