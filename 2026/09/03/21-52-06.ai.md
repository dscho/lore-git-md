This is a **substantive review** of the final patch in the `git repack --drop-filtered` series, targeting the safety guard that prevents dropping blobs referenced by the current index. Samuel Bronson reports **tested feedback** after using the feature on a real `llvm-project` clone, confirming the core functionality works but identifying a **usability pain point**: the current implementation **dies** when encountering an index-referenced blob, forcing the user to restart the entire (slow) enumeration process. Bronson proposes a **behavioral change**—skip index-referenced blobs with a warning instead of failing—arguing this is more user-friendly while preserving safety. The attached patch demonstrates the change but omits test updates and does not yet address `--filter=blob:none` support.

The review is **tested** (Bronson ran the feature on a large repository) and **substantive** (engages with the code’s behavior, not just style). It surfaces a real-world friction point: the current safety guard, while technically correct, creates a poor user experience by aborting after a potentially long enumeration phase. The proposed alternative—silently excluding index-referenced blobs—would require careful consideration of edge cases (e.g., whether the warning is sufficient, how to handle `--dry-run` output) but aligns with Git’s general principle of graceful degradation. The author (Siddharth Shrimali) will need to decide whether to adopt this approach or defend the current fail-fast behavior, likely sparking discussion about the trade-off between strict correctness and usability. The thread is now **proposed for `next`** but may need a v6 to address this feedback before integration.