This is the final approved version of the `--missing-only` feature for `git rev-list`, now queued for `next`. The patch adds a new `--missing-only` option that filters output to show only missing objects, complementing the existing `--missing=print` and `--missing=print-info` options. The implementation is complete, with no code changes from v2; only the commit message was updated to clarify the workflow motivation for GitLab's Gitaly use case.

The feature addresses a specific scripting need: instead of post-processing `git rev-list --missing=print` output to strip the `?` prefix and filter out present objects, callers can now use `--missing-only` to get a clean list of missing object IDs in a single command. This is particularly useful for GitLab's Gitaly, which uses the list during transaction packing to record missing objects as dependencies (not for immediate fetching). The commit message now explicitly states this is a "one-shot walk" rather than a fetch loop, resolving Junio's earlier request for clarification.

Key technical details remain unchanged from v2: the new flag requires `--missing=print` or `--missing=print-info`, preserves the selected format (including `path=` and `type=` fields in `print-info` mode), and rejects incompatible options (`--count`, `--disk-usage`) with clear error messages. The implementation introduces a helper function `should_collect_missing()` to avoid duplicating the missing-action check, and updates the test suite with comprehensive coverage of edge cases and mode interactions.

The patch touches `builtin/rev-list.c`, `Documentation/rev-list-options.adoc`, and adds new tests in `t/t6022-rev-list-missing.sh`. All review feedback has been incorporated, and the thread summary confirms the design consensus: `--missing=` controls formatting, `--missing-only` controls filtering, and the separation of concerns is clean. The feature is well-motivated, thoroughly tested, and ready for integration.