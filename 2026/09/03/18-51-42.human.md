This is a brief maintainer follow-up in the ongoing bugfix series for pathspec handling. Junio C Hamano confirms that his preparatory patch (declaring `match` and `original` fields in `struct pathspec_item` as `const char *`) will be queued separately, and a synthetic merge base will be created for Yannik Tausch’s two-patch series. This means Yannik does not need to resend the series as three patches—Junio will handle the integration.

The series itself remains ready for merging. It fixes a memory-safety issue in `match_pathspec_with_flags()` and a long-standing edge case in `common_prefix_len()` where leading exclude pathspecs incorrectly zero the common prefix. The preparatory patch resolves a compiler warning in the unit test and is uncontroversial. No further changes are expected, and the thread has no open technical questions. The unexplained phone number from an earlier reply remains irrelevant.