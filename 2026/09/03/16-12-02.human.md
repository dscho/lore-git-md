This is a documentation patch targeting the partial clone subsystem, part of an ongoing thread to correct inaccuracies in `Documentation/technical/partial-clone.adoc`. The patch itself is small and focused: it narrows a long-standing claim about dynamic object fetching being slow "because objects are fetched one at a time" to apply only to the fallback path in `odb.c`, while clarifying that the other ten call sites of `promisor_remote_get_direct()` batch multiple objects into a single `git fetch --stdin` subprocess. The change also updates the subprocess name from "fetch-pack" to "git fetch" to match the actual implementation.

Elijah Newren’s review is brief but positive. He acknowledges the patch’s clarity ("Both paragraph changes read well") and the commit message’s thoroughness ("Commit message makes sense"), though he suggests it could be shorter. His feedback is purely editorial and does not raise technical concerns. The review is substantive in that it engages with the patch’s content, but it does not test the changes or identify edge cases.

The patch is well-motivated and addresses a real source of confusion in the partial clone design document. The author’s decision to limit the change to the fallback path—while documenting adjacent inaccuracies (e.g., protocol V0 behavior) for future patches—is pragmatic and avoids introducing new contradictions. The patch applies cleanly to both `maint` and `master` and passes basic checks. This is a clear improvement to the documentation and unlikely to be controversial. Worth a quick look for anyone working with partial clones.