This is a bugfix for the `git history` command. Patrick Steinhardt replies to the original patch with two minor nitpicks: he suggests dropping the UBSan details from the commit message and notes that the author's From address does not match the Signed-off-by line. Otherwise, he confirms the fix is "obviously good" and does not see a need for a test, though the reproduction steps in the commit message are clear enough to serve as a regression check.

The patch itself remains unchanged in substance: it adds a NULL check in `write_ondisk_index()` to prevent a crash when the parent commit’s tree object is missing. The fix is minimal, follows Git’s established error-handling pattern, and is uncontroversial. No further discussion or objections have appeared yet.