This is a bugfix for the `git history` command, now in its second iteration. The patch prevents a NULL-pointer dereference crash in `write_ondisk_index()` when the parent commit’s tree object is missing from the object store. The v2 submission addresses the two surface-level review comments Patrick Steinhardt raised on v1: the commit message no longer mentions UBSan, and the From header now matches the Signed-off-by trailer.

The fix itself remains unchanged—it is still a single NULL check in `builtin/history.c` that converts a SIGSEGV into a graceful error message. Patrick’s reply explicitly marks the patch as ready: “this version looks good to me.” No further technical discussion or test requests are pending, so the patch is likely to be queued for `next` in the next integration cycle.