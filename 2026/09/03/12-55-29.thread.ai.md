Thread identity: Bugfix for `pack-objects` performance regression in partial clones (SHA: not yet assigned). Originating author: Aleksei Sviridkin. Thread type: single-patch bugfix.

Core problem or goal: Fix a logic error in `prefetch_to_pack()` that causes inefficient fetching of missing objects in partial clones. The bug occurs when loose objects are present locally, causing the prefetch batch to start at the wrong offset and forcing individual fetches for each missing object instead of batching them.

Current status: Under review. Patch v1 posted; no prior versions. No maintainer decision or merge status reported.

Key technical details:
- Subsystem: `pack-objects` (partial clone prefetch logic).
- File touched: `builtin/pack-objects.c` (function `prefetch_to_pack()`).
- New test: `t/t5300-pack-object.sh` (verifies fix under SHA-1 and SHA-256).
- Behavior change: Restores correct batching of missing objects, reducing fetch count from N to 1 in synthetic benchmarks.
- On-disk format: No changes.
- Symbols: None renamed; fix involves an explicit pointer to the sorted array.

Open questions and loose ends: None identified. The patch includes a thorough commit message, synthetic benchmarks, and test coverage. No review feedback yet.

Participant positions: None recorded (initial submission).

Related work: Bug introduced in commit `e00549aa9b` (context not provided in this email). No dependencies on other in-flight topics.