**Summarizer brief mode**

D. Ben Knoble and Junio C Hamano exchange final remarks on the architectural constraint introduced by the `dk/use-nsec-runtime` series: the new dependency on `repo_config_values()` in `is_racy_stat()`, which currently only works for `the_repository`. Knoble acknowledges Junio’s concern that this limitation could hinder future recursive submodule operations (e.g., `git diff` or `git status` traversing submodules without subprocesses) but argues that papering over the issue with a safe default might reduce the impetus to fix the underlying restriction. He notes that testing `git grep --recurse-submodule` (which does not exercise racy-entry code paths) provides only negative evidence and reiterates his reluctance to alter the series at this stage. The discussion closes with Knoble offering to revisit the safe-default approach if future work demands it, but no immediate changes are proposed. Key technical details: the latent bug remains unresolved, the `repo_config_values()` limitation is now explicitly flagged as a blocker for deeper submodule integration, and the series’ current stopgap (treating `core.useNanosec` as false when `istate->repo != the_repository`) is confirmed as a temporary measure. No new files, functions, or config keys are introduced; the focus is solely on the architectural trade-off.