This is the documentation patch (part 2 of 2) in a series that clarifies a long-standing but undocumented edge case in `git cherry-pick --no-commit`. The patch updates `Documentation/git-cherry-pick.adoc` to explicitly state that `CHERRY_PICK_HEAD` is *not* created when a `--no-commit` cherry-pick fails due to conflicts, contrasting this with `git revert --no-commit` (which *does* write `REVERT_HEAD`). The commit message cites the 2011 commit (d7e5c0cbfb) that introduced this behavior, framing it as intentional but never documented.

The thread context is critical here. Junio’s reply to the test patch (part 1) revealed he initially misinterpreted the intent, assuming the patch was proposing to *change* the behavior to write `CHERRY_PICK_HEAD` in the conflict case. His AI-assisted rewrite of the commit message (included in the parent email) questioned whether the current behavior is correct, elevating this documentation patch from a simple clarification to a defense of the existing design. The patch explicitly justifies the asymmetry with `git revert` and explains the implications for `git commit` (e.g., the need to use `git commit -c <commit>` to preserve authorship), which may become a point of contention if reviewers argue the behavior is unintuitive.

The patch itself is small and focused, touching only the documentation file. It adds a bullet point to the conflict-handling list and a dedicated paragraph under `--no-commit` to explain the absence of `CHERRY_PICK_HEAD` and the asymmetry with `git revert`. No new symbols, CLI options, or on-disk formats are introduced, but the patch names `CHERRY_PICK_HEAD`, `REVERT_HEAD`, and `git commit -c <commit>` as key references for follow-up discussion. The series is well-scoped, but the thread’s trajectory suggests this could expand into a broader discussion about sequencer consistency, particularly if reviewers challenge the asymmetry between `cherry-pick` and `revert`.

The patch looks technically sound and addresses a real gap in the documentation. The commit message is clear about the historical context and the rationale for the current behavior, which should help reviewers evaluate whether the asymmetry is justified. However, the thread context makes it likely that this patch will attract scrutiny beyond the documentation itself, as it may serve as a catalyst for revisiting the sequencer’s design choices. The author’s response to Junio’s feedback (in the parent email) also indicates a v2 of the test patch is forthcoming, which may further clarify the intent.