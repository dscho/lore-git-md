This thread is a focused documentation improvement by Aleksei Sviridkin that corrects a long-standing inaccuracy in Git’s partial clone documentation. The patch clarifies that the oft-repeated claim—"dynamic object fetching invokes fetch-pack once per object and is slow"—applies *only* to the fallback path in `odb.c`, not to the ten other call sites of `promisor_remote_get_direct()`. Those call sites batch multiple objects into a single `git fetch --stdin` subprocess, which is far more efficient. The update also fixes a minor terminology mismatch (replacing "fetch-pack" with "git fetch") and documents the fallback’s role in the batching mechanism.

The patch is self-contained, well-justified, and applies cleanly to both `maint` and `master`. Aleksei’s commit message is unusually thorough, citing specific call sites, release versions, and even reproduction steps to support the change. While the author notes other inaccuracies in the document (e.g., protocol V0 vs. V2 behavior), they deliberately avoid scope creep by leaving those for future patches. There’s no indication of controversy or outstanding objections—this looks like a straightforward improvement that should land without drama. No prior discussion or review feedback is mentioned, so the thread may remain quiet unless a reviewer spots an edge case.