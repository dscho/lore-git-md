**Summarizer brief mode**

Junio C Hamano replies to D. Ben Knoble’s final message in the `dk/use-nsec-runtime` thread, shifting focus from the immediate correctness of the patch series to its long-term architectural implications. He acknowledges Knoble’s earlier point about testing challenges but expresses deeper concern that the new dependency on `repo_config_values()`—which currently only works for `the_repository`—will obstruct future features, specifically recursive submodule operations (e.g., `git diff` or `git status` traversing submodules without spawning subprocesses). Junio notes that `git grep --recurse-submodule` (which already operates recursively) does not exercise the racy-entry code paths, so its successful execution offers no assurance that the latent bug in `is_racy_stat()` will not surface in other recursive contexts. The message implicitly reinforces the architectural constraint documented in the thread summary: the series’ safe default (treating `core.useNanosec` as false when `istate->repo != the_repository`) is a stopgap, not a fix, and the underlying limitation in `repo_config_values()` remains a blocker for deeper submodule integration. No new code or test changes are proposed; the reply is a forward-looking caution about future work.