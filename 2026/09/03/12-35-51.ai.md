This is a reply in the ongoing discussion about whether Git should reject bare `@` as a branch name in porcelain commands. The thread remains focused on the design question of validation strictness, with no patch proposed yet.

Bence Csókás responds to Kristoffer Haugsbakk’s citation of Junio C Hamano’s 2024 stance advocating deprecation of bare `@` as a synonym for `HEAD`. Csókás pushes back, arguing that `@` is a useful shorthand for `HEAD` in complex expressions (e.g., `git range-diff long-branch-name{^..otherbranch,..@}`) and that removing it would harm usability. His position is pragmatic: the convenience of `@` outweighs the risk of accidental branch creation, and the current inconsistency is a minor footgun rather than a critical flaw.

The reply does not engage with the technical implementation (e.g., tightening `check-ref-format --branch` validation) or the usability trap (inability to check out `@` via porcelain commands). Instead, it reframes the discussion as a trade-off between convenience and safety, introducing a new dimension to the thread. The argument is grounded in real-world usage (bash history examples) rather than abstract design principles, which may resonate with contributors who prioritize workflow ergonomics.

No new open questions are introduced, but the thread is now split between two clear positions: stricter validation (to prevent user mistakes) and preserving `@` as a shorthand (for usability). The discussion remains civil and focused, with no signs of escalation or tangential concerns. The next step will likely be a patch proposal that either tightens validation or preserves the status quo, accompanied by a commit message addressing both the usability trap and the convenience argument.