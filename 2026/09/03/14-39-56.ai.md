This is the **v5 revision of a two-patch series** refining `git checkout -m` autostash conflict handling, now **queued for merging to `next`** after all prior review feedback has been addressed. The series introduces no new technical changes—only **commit message improvements and a helper function (`do_stash_apply`)** to improve code clarity. All substantive design decisions (exit code conventions, conflict detection logic, advice formatting) were settled in v4 and remain unchanged.

The patches touch the **stash and checkout subsystems**, specifically:
- `stash.h` (new `enum stash_apply_result` with `STASH_APPLY_CLEAN`, `STASH_APPLY_CONFLICT`, `STASH_APPLY_ERROR`)
- `builtin/stash.c` (exit code mapping: 1 for conflicts, 128 for errors)
- `sequencer.c` (autostash apply logic, now using the helper function)
- `builtin/checkout.c` (advice formatting: blank line separates conflict message from branch-switch confirmation)
- `Documentation/git-stash.adoc` (updated exit code documentation)
- Tests (`t/t3903-stash.sh`, `t/t7201-co.sh`) validate the new behavior, including the fast-path for no tracked changes and the visual separation of advice.

Key symbols to track in follow-up discussion: `do_stash_apply()`, `enum stash_apply_result`, and the exit code convention (`1` for conflicts, `128` for other failures). The series is **uncontroversial** and ready for integration.