**Summarizer brief mode**

This is the final reply in the `dk/use-nsec-runtime` series (v6, now in `next`). D. Ben Knoble confirms he will **not** produce a v7 reroll to consolidate the `NO_NSEC` build-time differences using `ST_XTIME_NSEC(st)`, as he and Jeff King (Peff) agree the preprocessor guard is not worth removing at this stage. He acknowledges Junio C Hamano’s lingering architectural concern about the latent bug in `is_racy_stat()` when operating on a repository that is not `the_repository` (due to the new call to `repo_config_values()`), but defers deeper investigation, citing time constraints and the complexity of testing across subsystems. The series remains technically complete and ready to graduate from `next` to `master`; the only remaining action is Junio’s local amendment of the typo fix ("comparisions" → "comparisons") in `Documentation/config/core.adoc`. Key technical details unchanged: `core.useNanosec` config key, `NO_NSEC` build knob, `is_racy_stat()` in `read-cache.c`, and the safe default (treating `core.useNanosec` as false) when `istate->repo != the_repository`. The discussion leaves the latent bug unresolved but documented as a forward-looking constraint.