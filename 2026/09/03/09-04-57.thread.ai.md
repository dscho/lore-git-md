Thread identity
Subject: [PATCH 1/2] rerere: extract logic for stale entry detection
Series version: v1 (two-patch series)
Originating author: Patrick Steinhardt
Thread type: patch series (refactoring)

Core problem or goal
Refactor the `rerere` subsystem to extract the logic that determines whether a rerere entry is stale into reusable helper functions (`rerere_gc_cutoffs` and `rerere_id_is_stale`). This is a preparatory step for a subsequent patch that will introduce a second caller for this logic.

Current status
Under review. This is the first patch in a two-patch series; the second patch has not yet been posted in this thread.

Key technical details
- Files touched: `rerere.c`
- New symbols: `rerere_gc_cutoffs()`, `rerere_id_is_stale()`
- Subsystem: `rerere` (reuse recorded resolution)
- Behavior: unchanged; this is a pure refactoring with no functional impact
- Test coverage: not explicitly mentioned, but the existing `rerere` test suite should cover the unchanged behavior

Open questions and loose ends
None identified in this patch. The next patch in the series will introduce the new caller and may raise questions about its use of the extracted logic.

Participant positions
None yet; this is the initial patch submission.

Related work
This patch is part of a two-patch series by the same author. No external dependencies or related in-flight topics mentioned.