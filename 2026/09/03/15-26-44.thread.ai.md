Existing AI thread summary (absent, this is the thread root):

Thread identity
Subject: Memory leak in `git history reword` introduced by `ps/receive-pack-shallow-optim` merge
Series version: N/A (bug report)
Originating author: Kristoffer Haugsbakk
Thread type: bug report

Core problem or goal
Identify and fix a 283-byte memory leak in `git history reword --dry-run` that was introduced by the merge of Patrick Steinhardt’s `ps/receive-pack-shallow-optim` branch (commit `13763ecf`, 2026-03-02). The leak surfaces in the object database (ODB) layer during commit buffer handling, specifically in `unpack_compressed_entry()` in `packfile.c`.

Current status
Under investigation; leak confirmed present in `master`, `next`, and `seen` as of 2026-09-02. Bisect points to the merge commit `13763ecf` as the regression source, but the exact culprit within the merge is not yet identified. No fix proposed.

Key technical details
- **Files/subsystems touched**: `packfile.c` (`unpack_compressed_entry()`), ODB layer (`repo_get_commit_buffer()`, `odb_read_object()`, `packfile_store_read_object_info()`).
- **Triggering command**: `git history reword --dry-run` (leak does not occur on the original `git history reword` commit `d205234c`).
- **Repository conditions**: Leak requires a "real repo" with packfiles and/or commit graph; minimal test repos do not reproduce the issue.
- **Stack trace**: Leak originates from `repo_get_commit_buffer()` -> `odb_read_object()` -> `packfile_store_read_object_info()`.
- **Leak size**: 283 bytes per `git history reword --dry-run` invocation.

Open questions and loose ends
- Exact change in `ps/receive-pack-shallow-optim` that introduced the leak (bisect points to the merge commit, but the specific patch is unknown).
- Whether the leak affects other commands that read commit buffers or unpack objects.
- Potential missing `free()` in the object unpacking path, possibly related to shallow commits or packfile entry handling.

Participant positions
- **Kristoffer Haugsbakk**: Reporter; confirmed leak via bisect, provided reproduction steps, stack trace, and bisect log. No substantive review or fix proposed yet.

Related work
- `ps/receive-pack-shallow-optim` (Patrick Steinhardt): The merge commit (`13763ecf`) that introduced the regression. This branch optimized shallow clone handling in `git receive-pack`.
- `git history reword` (original commit `d205234c`): The command affected by the leak; leak was not present at its introduction.