This is a refactoring patch from Patrick Steinhardt, part of a two-patch series targeting the `rerere` (reuse recorded resolution) subsystem. The patch extracts the logic that determines whether a rerere entry is stale into two new helper functions, `rerere_gc_cutoffs()` and `rerere_id_is_stale()`. The motivation is to make this logic reusable for a subsequent commit in the series, though the commit message does not yet reveal what that second caller will be.

The change itself is mechanical and clean: the existing staleness-checking code is lifted out of `prune_one()` and `rerere_gc()`, parameterized, and given explicit return values. The new `rerere_id_is_stale()` function returns a boolean, making the control flow in `rerere_gc()` slightly clearer. No behavior is altered, and the patch is small (27 insertions, 15 deletions in a single file).

This looks like a well-crafted preparatory step for a larger maintenance-related improvement. If the follow-up patch is equally focused, the series is likely to be uncontroversial and worth a quick review for anyone interested in the `rerere` or maintenance subsystems.