This thread began with Aleksei Sviridkin’s two-patch series to add test coverage and documentation for an edge case in `git cherry-pick --no-commit`: when the operation fails due to conflicts, `CHERRY_PICK_HEAD` is *not* created, unlike the default cherry-pick mode or `git revert --no-commit` (which *does* write `REVERT_HEAD`). The first patch added a test; the second, now under discussion, updates `Documentation/git-cherry-pick.adoc` to explicitly document this asymmetry and its implications for `git commit` (e.g., the need to use `git commit -c <commit>` to preserve authorship).

Junio’s reply to the test patch revealed a key misunderstanding: he initially thought the series aimed to *change* the behavior to write `CHERRY_PICK_HEAD` in the conflict case, rather than merely documenting the existing behavior. His rewritten commit message (included in the email) questions whether the current behavior is correct, elevating the documentation patch from a simple clarification to a potential flashpoint. The patch itself is small and focused, but the thread’s context suggests it could spark broader discussion about sequencer consistency, particularly the asymmetry with `git revert`. No reviews have been posted yet, but the maintainer’s skepticism may draw attention from other contributors familiar with the sequencer. The series remains at v1, with no open questions beyond the implicit one: *is the current behavior justified, or should it be changed?*