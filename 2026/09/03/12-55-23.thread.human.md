This thread is a simple two-patch series from Aleksei Sviridkin that adds a missing test case to Git’s test suite. The first patch, just posted, adds a test in `t3507-cherry-pick-conflict.sh` to verify that `CHERRY_PICK_HEAD` is *not* created when a `git cherry-pick --no-commit` operation fails due to conflicts. The gap was discovered when a user encountered the behavior by accident, and the patch ensures it’s explicitly covered by the test suite.

The change is small and focused—just six lines of test code following the existing pattern in the file. There’s no debate yet about whether the current behavior is correct; the patch simply documents it. No reviews or replies have arrived so far, so the thread is still at its starting point. The second patch in the series hasn’t been posted yet.