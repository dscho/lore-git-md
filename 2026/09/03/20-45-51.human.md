This is the final patch in a short series adding a `--missing-only` option to `git rev-list`. The feature is now approved and queued for `next`.

The patch implements a script-friendly way to list only missing objects in a partial clone, without requiring post-processing to strip the `?` prefix. This directly supports GitLab's Gitaly workflow for transaction packing, where missing objects are recorded as dependencies in a single pass. The commit message has been strengthened to clarify this workflow: it's a one-shot walk, not a fetch loop, and the `?` prefix is stripped by callers (Gitaly and tests) to identify objects that must already exist in the main repository.

The implementation is clean and well-scoped. The new `--missing-only` flag filters output to show only missing objects, requires `--missing=print` or `--missing=print-info`, and rejects incompatible options (`--count`, `--disk-usage`) with clear error messages. The code introduces a small helper function (`should_collect_missing()`) to replace a repeated conditional, and the documentation clearly explains the flag's semantics and use cases. Tests cover all modes and edge cases, using direct OID comparison for verification.

This is a targeted feature addition with a clear use case and no obvious downsides. The design consensus from the thread is solid: the separation between `--missing=` (formatting) and `--missing-only` (filtering) is logical, and the script-friendly output (no `?` prefix) is confirmed as correct. All review feedback has been incorporated, and the topic is ready for integration.