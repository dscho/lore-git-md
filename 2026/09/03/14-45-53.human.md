This is the final minor mechanical tweak in the `git checkout -m` autostash conflict-handling series, now cooking in `next`. Harald Nordgren replies to Phillip Wood’s style nit about inconsistent brace usage in an `if`/`else if` cascade. The patch itself is technically complete and uncontroversial—Junio has already queued the series for merging—but this exchange is about whether to adopt a stricter style rule to catch such inconsistencies automatically.

Harald suggests borrowing the Linux kernel’s `checkpatch.pl` linter rule that enforces uniform bracing across all branches of a conditional cascade. The tone is light and practical: he frames it as a way to avoid wasting reviewer time on trivial style nits. No technical objections remain; this is purely a process discussion about tooling. The thread is effectively closed on substance, and this reply is unlikely to generate further debate unless someone objects to the idea of adopting a linter rule.