Summarizer brief mode:

This is a substantive reply in the `git push` shallow-clone performance thread, where Elijah Newren responds to Derrick Stolee’s review of his v2 patch. The core technical point is a **correction of Stolee’s over-exclusion critique**: the identified flaw is not a repository corruption risk but a **false-positive failure mode** (rejecting valid refs unnecessarily) due to `receive-pack`’s connectivity checks (commit `52fed6e1ce07`). Elijah demonstrates that Stolee’s test case does not corrupt the receiver repository—it safely rejects the push, albeit with an inscrutable error message. He acknowledges the usability issue (poor error messages) and proposes follow-up patches to improve them, including hints for users to retry pushing individual refs as a simple recovery mechanism.

Elijah also **rejects Stolee’s alternative approach** (repurposing `push.negotiate` for shallow repos) by referencing his earlier flowchart of four failure scenarios where negotiation alone is insufficient: rebased PRs with sensitive data, unrelated repositories, disabled negotiation, and protocol limitations (e.g., SSH without `GIT_PROTOCOL=version=2`). He reiterates that the v2 patch’s assumption (server has the shallow graft) is **vastly superior** in most cases and that the edge case (server lacks the graft) is rare and recoverable. The discussion now centers on **trade-offs between performance (Elijah’s patch) and false-positive failures (Stolee’s concern)**, with Elijah arguing that the latter is preferable to resending gigabytes of data.

Key technical details:
- **Files touched**: `send-pack.c`, `t5538-push-shallow.sh` (follow-up patches for error messages).
- **Behavior**: The v2 patch treats shallow grafts as uninteresting tips during pack generation, reducing redundant transfers. When the assumption fails (server lacks the graft), the push is rejected with a connectivity error.
- **Error handling**: Elijah plans to improve error messages and add hints for users to retry individual refs.
- **Test case**: Stolee’s test (pushing a shallow ref alongside a non-shallow ref) now passes with the v2 patch, but the error message is unclear. The test confirms no corruption occurs.
- **Config**: `push.shallowExcludeBoundary` (default: `true`) remains the proposed mechanism, with no immediate plans to repurpose `push.negotiate`.
- **Related work**: Follow-up patches for error messages are implied but not yet submitted.

Open questions:
- Will Stolee accept Elijah’s refutation of the corruption risk, or does he still consider the false-positive failure mode a blocker?
- Is the project comfortable with the trade-off of occasional false-positive failures (recoverable via retries) in exchange for significant bandwidth savings?
- Should the documentation warn users about the edge case where the server lacks the shallow graft?
- Will Elijah’s follow-up patches for error messages address the usability concerns raised by Stolee’s test case?