Summarizer brief mode

This is Junio C Hamano’s follow-up clarification in the thread improving the `git maintenance` heuristic for the `rerere gc` task. The email raises a **potential misunderstanding** about the series’ scope: it does not address lock contention between *multiple concurrent* `rerere gc` invocations, only the race between `git maintenance` and `git rebase`. The heuristic change (dynamic threshold based on estimated stale entries) reduces unnecessary `rerere gc` runs but does not alter the locking mechanism itself. If two `rerere gc` tasks trigger simultaneously, the second will still fail due to `MERGE_RR.lock` and `LOCK_DIE_ON_ERROR`.

Key technical context for future discussion:
- **Locking behavior**: `rerere gc` uses `MERGE_RR.lock` with `LOCK_DIE_ON_ERROR`, meaning concurrent invocations will abort.
- **Heuristic scope**: The series targets *frequency* of `rerere gc` runs (via `maintenance.rerere-gc.auto`), not concurrency control.
- **Files/functions**: No changes to locking logic in `rerere.c` or `lockfile.h`; the focus remains on `rerere_gc_needed()` and its helpers.
- **Test implications**: Existing tests (`t7900-maintenance.sh`) verify threshold behavior but not concurrent execution.

Junio’s question suggests a possible gap in the series’ documentation or motivation: while the cover letter (and his earlier summary) framed the problem as a `maintenance`/`rebase` race, the solution does not prevent collisions between two `maintenance` runs. This could become a point of discussion if reviewers or users expect broader concurrency fixes. The series itself is otherwise ready for integration, with no code changes requested.