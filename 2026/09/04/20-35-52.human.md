This is the first patch in a two-part test modernization series. The author, Mark C. Chu-Carroll, mechanically replaces older shell test idioms (`test -f` and `! test -f`) with more semantic helper functions (`test_path_is_file` and `test_path_is_missing`) across 34 test scripts in the `t/` directory. The changes are purely syntactic and do not alter test behavior or coverage.

The patch touches a broad but shallow surface: performance tests (`t5300-*`), core Git functionality (`t0031-lockfile-pid.sh`, `t2030-unresolve-info.sh`), and various plumbing and porcelain commands (`t4014-format-patch.sh`, `t7400-submodule-basic.sh`). The substitutions are straightforward—every instance of `test -f <path>` becomes `test_path_is_file <path>`, and `! test -f <path>` becomes `test_path_is_missing <path>`—with no changes to surrounding logic or test structure.

This is a routine test modernization effort, following the established pattern of replacing low-level shell constructs with clearer, more maintainable helpers. The patch is uncontroversial and unlikely to introduce regressions, as the new functions are drop-in replacements with identical semantics. The division into two patches appears arbitrary (no clear subsystem boundary), but the split keeps the review surface manageable.

No maintainer feedback or integration status is recorded yet, but the series aligns with the project’s ongoing test hygiene efforts. For a busy reader, this patch is worth a quick skim to confirm the mechanical nature of the changes, but it doesn’t demand deep attention.