This is the sixth patch in Patrick Steinhardt’s 11-patch series unifying Git’s reference storage terminology. It updates the build-info output (`git version --build-options`) to rename the `default-ref-format` field to `default-ref-storage`, aligning it with the rest of the series. The change is minimal—two lines in `help.c` and one test adjustment in `t/t0001-init.sh`—but it closes the last remaining inconsistency in the CLI’s presentation of this concept. The patch is purely mechanical and follows the established pattern of prior patches in the series. No new edge cases or backward-compatibility concerns are introduced here; the focus is on internal consistency. Reviewers tracking the series will likely see this as a straightforward follow-through on the earlier renames.