This is a reply in the ongoing `git repack --drop-filtered` series, specifically addressing Samuel Bronson’s usability concern about the index validation guard. The author (Siddharth Shrimali) agrees with Bronson’s proposal to replace the fatal error with a warning that skips index-referenced blobs, acknowledging that aborting the entire operation after a slow enumeration phase is a poor user experience. The discussion now focuses on implementation details: Shrimali notes that `oidset_remove()` would mutate the `drop_oids` set in place, which would break `--dry-run` output (it should still report skipped blobs as candidates). Instead, he suggests collecting index-referenced OIDs into a separate "skip-set" to preserve the original `drop_oids` for reporting. The conversation also touches on follow-up work: the planned drop log must not record skipped blobs, as they were never actually dropped.

The thread remains **proposed for `next`** but will likely require a v6 to implement this behavioral change. The author’s agreement with Bronson’s core argument—prioritizing usability without compromising safety—suggests the change will be adopted, though the exact implementation (skip-set vs. in-place removal) and test updates are still open. This is a **substantive, user-facing adjustment** to the series’ final patch, and the resolution will directly impact real-world usability for partial-clone users.