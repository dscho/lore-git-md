This is patch 7/11 in Patrick Steinhardt’s refactoring series to unify Git’s reference-storage terminology. It extracts the URI-parsing logic from `setup.c` into a new public function `ref_storage_format_by_uri()` in `refs.c`/`refs.h`, then updates all call sites to use it. The patch is a pure refactoring: no behavior change, only code movement and cleanup.

The new function takes a URI string in the form `<format>[://<payload>]` and returns the parsed `enum ref_storage_format` plus an optional payload string. Three places in `setup.c` that previously duplicated this logic now call the central function instead, eliminating 36 lines of cut-and-paste code. The patch also updates error messages to show the full URI rather than just the format name, which is a minor improvement in diagnostic clarity.

This is a straightforward, well-motivated cleanup that prepares the ground for the next patch in the series, which will add more URI-parsing call sites. The commit message is clear, the diff is clean, and the change is obviously correct at a glance. No backward-compatibility concerns here—only internal plumbing.