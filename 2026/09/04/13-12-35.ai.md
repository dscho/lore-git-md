This is patch 7/11 in Patrick Steinhardt’s refactoring series to unify Git’s reference-storage terminology. The patch extracts URI-parsing logic from `setup.c` into a new public function `ref_storage_format_by_uri()` in `refs.c`/`refs.h`, then updates all call sites to use it. This is a pure refactoring with no behavior change, but it improves code hygiene by eliminating 36 lines of duplicated logic and centralizing URI parsing for future use.

The new function parses URIs in the form `<format>[://<payload>]` and returns the parsed `enum ref_storage_format` along with an optional payload string. Three places in `setup.c` that previously duplicated this logic now call the central function instead. Error messages are also updated to show the full URI rather than just the format name, which improves diagnostic clarity. The patch is well-motivated, as it prepares the ground for the next patch in the series, which will add more URI-parsing call sites.

Karthik Nayak’s review of this patch is surface-level, limited to confirming the code movement and cleanup. There are no substantive objections or edge-case concerns raised here. The patch itself is straightforward and appears correct at a glance, with no backward-compatibility issues since it only touches internal plumbing.

This patch is part of a larger effort to standardize terminology and enable URI-based configuration for reference storage backends. The series as a whole is under active review, with patch 7 serving as a foundational step for the URI payload functionality introduced in patch 11. The commit message is clear, and the diff is clean, making this a low-risk refactoring.

Key technical details for follow-up discussion include the new `ref_storage_format_by_uri()` function, the updated error messages, and the elimination of the now-redundant `parse_reference_uri()` helper. The patch also introduces a minor improvement in diagnostic output by including the full URI in error messages, which could help users debug misconfigured backends.