This is a **contributor housekeeping** follow-up in the Coccinelle performance regression thread. A new contributor, Emmanuel Ugwu, asks whether now is a good time to tackle the deferred `#leftoverbits` item—standardizing ref-counting types to `size_t` (or a custom signed equivalent) by updating `struct ref_array` in `ref-filter.h`—or if the project would prefer to wait until Harald Nordgren’s `hn/branch-delete-merged` topic has fully settled.

The email is brief and confirmatory of the thread’s current status: the immediate follow-up patch (correcting the loop index type in `builtin/branch.c` from `size_t` to `int` to match `candidates.nr`) remains the sole outstanding action item, while the broader type standardization question is acknowledged as a long-term goal but deferred for a quieter moment. Emmanuel’s question signals interest in driving the deferred work, but the thread’s established consensus—endorsed by Junio, Peff, and Taylor Blau—is that the broader discussion should wait. No new technical points or dissent are introduced; the email simply surfaces the existing plan and asks for timing guidance.