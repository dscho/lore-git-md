This is patch 7/11 in Patrick Steinhardt’s series to unify Git’s reference-storage terminology. The patch extracts URI-parsing logic from `setup.c` into a new public function `ref_storage_format_by_uri()` in `refs.c` and updates all call sites to use it. This is a pure refactoring with no behavior change—just code movement and cleanup that prepares for the next patch, which will add more URI-parsing call sites.

The new function parses URIs in the form `<format>[://<payload>]` and returns the parsed `enum ref_storage_format` along with an optional payload string. The patch eliminates 36 lines of duplicated code across three places in `setup.c` and improves error messages by showing the full URI instead of just the format name. The commit message is clear, the diff is clean, and the change looks obviously correct.

Karthik Nayak’s review here is brief but substantive: he confirms the logic is sound and the refactoring is well-motivated. There are no objections or requests for changes in this reply—just a straightforward acknowledgment that the patch does what it claims. This is a low-risk, high-value cleanup that fits neatly into the larger series.