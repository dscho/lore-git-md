This is the second patch in a two-part test modernization series. It mechanically replaces older shell test idioms (`test -f`, `! test -f`) with more semantic helper functions (`test_path_is_file`, `test_path_is_missing`) in 29 test scripts under `t/`. The files touched span performance tests, core Git functionality, and various plumbing and porcelain commands, including `t/t5601-clone.sh`, `t/t7004-tag.sh`, and `t/t9001-send-email.sh`.

The changes are purely syntactic and follow the established pattern of the ongoing test modernization effort. No behavioral changes or new test coverage are introduced; the patch simply updates file existence checks to use clearer, more maintainable assertions. The division into two patches appears arbitrary, as the changes are repetitive and could have been split by any convenient boundary.

This patch is uncontroversial and aligns with the project's test guidelines. The review surface is large (29 files), but the changes are straightforward and should be quick to verify. No substantive feedback has been recorded yet, and the series as a whole remains under review. For a future AI summarizer, note the key technical details: the patch replaces `test -f` and `! test -f` with `test_path_is_file` and `test_path_is_missing` across a range of test scripts, maintaining identical behavior. The base commit and change ID are shared with the first patch in the series.