Thread identity: bug report for Git 2.55.0 on macOS, originating from Ramkumar Ramachandra (artagnon@gmail.com). Thread type: regression report; closed with root-cause identification.

Core problem or goal: user reported three distinct behavioral regressions in Git 2.55.0 (Homebrew) on macOS: (1) `git pull` failing with "dangling remote ref", (2) `git diff` exiting 0 with no output despite a dirty worktree, and (3) `git checkout -` failing with "invalid ref" after a pull. The regressions were isolated to `llvm/llvm-project.git` (30M+ lines, high update frequency) and initially suspected to be Git bugs or race conditions triggered by repository scale or concurrency.

Current status: **closed**; root cause identified as interference from CrowdStrike Falcon security software. No Git regression, ref backend issue (files vs. reftable), or environmental factor (e.g., hardware) was responsible. Ramkumar confirmed the resolution after ruling out Git code paths and repository-specific edge cases. No integration status (not applicable).

Key technical details: commands involved: `git pull`, `git diff`, `git checkout -`; platform: macOS; distribution: Homebrew Git 2.55.0; repository: `llvm/llvm-project.git` (30M+ lines, high concurrency). Root cause: CrowdStrike Falcon security software interference, not Git code or ref storage. No new symbols, files, or on-disk format changes. Ref backend migration (files -> reftable) was tested but found unrelated.

Open questions and loose ends: none. The thread is closed with no action required from the Git project. All prior open questions (repository scale, concurrency, ref backend, environmental factors) are resolved.

Participant positions:
- Ramkumar Ramachandra (reporter): observed symptoms in `llvm/llvm-project.git` on macOS 2.55.0 (Homebrew); initially suspected Git regressions or ref backend issues. **Position closed**: confirmed CrowdStrike Falcon as root cause; no further investigation needed.
- brian m. carlson: substantive but speculative response in prior email; ruled out known Git regressions and suggested environmental factors. **Position closed**: no further engagement; CrowdStrike identification supersedes prior speculation.