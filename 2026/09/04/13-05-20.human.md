This is a review reply to the first patch in Patrick Steinhardt’s 11-patch series renaming `--ref-format=` to `--ref-storage=`. The reviewer, Karthik Nayak, raises a single surface-level nit: the `argh` value in the `OPT_STRING` macro should use `ref-storage` instead of `format` to match the new option name. He also notes that the backward-compatibility alias (`--ref-format=`) is correctly implemented and points out a minor inconsistency in the error message wording (still saying "ref storage" even when the old option name was used).

The review is purely mechanical—no substantive concerns about behavior, edge cases, or design choices are raised. The feedback is clear and actionable, but it does not engage with the broader goals of the series or the correctness of the implementation beyond the one-line nit. The patch remains well-motivated and carefully executed, with no red flags for reviewers.