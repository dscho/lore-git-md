This is a brief tooling discussion in the final stages of the `git checkout -m` autostash conflict handling series. The series itself is already approved and queued for `next`, so this exchange does not affect its integration status.

Harald points out that GitHub CI (via `.github/workflows/check-style.yml`) runs `clang-format` checks but does not flag inconsistent brace usage in `if/else if/else` cascades, confirming Junio’s observation that the project’s `.clang-format` configuration does not enforce this style rule. The discussion highlights a minor gap in Git’s CI tooling—style nits like this are caught locally via `make style` but not in automated checks—though it remains purely mechanical and unrelated to the series’ technical content.