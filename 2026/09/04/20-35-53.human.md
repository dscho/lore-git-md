This is the second patch in a two-part test modernization series. The author, Mark C. Chu-Carroll, mechanically replaces older shell test idioms (`test -f`, `! test -f`) with more semantic helper functions (`test_path_is_file`, `test_path_is_missing`) in 29 test scripts under `t/`. This patch covers the remaining scripts not addressed in the first patch, including performance tests (`t/perf/p5302-pack-index.sh`), core Git functionality (`t/t0007-git-var.sh`, `t/t5601-clone.sh`), and various plumbing and porcelain commands (`t/t7004-tag.sh`, `t/t7610-mergetool.sh`).

The changes are purely syntactic and follow the established pattern of the ongoing test modernization effort. The patch is uncontroversial and unlikely to introduce regressions, as it only alters how file existence is asserted, not the logic or behavior of the tests. The division into two patches appears arbitrary—both batches apply the same transformation to different subsets of the test suite.

This patch is ready for review and should be quick to verify, given the repetitive nature of the changes. No new test coverage or behavioral changes are introduced. If the first patch in the series has already been reviewed favorably, this one should require minimal additional scrutiny.