This is the second patch in a v2 series (3/3) that extends auto-maintenance deferral to the sequencer backend for `git rebase`, `git cherry-pick`, and `git revert`. The patch ensures auto maintenance runs exactly once at sequence completion—matching the apply backend’s behavior—rather than during intermediate commits, merges, or exec commands.

The implementation adds calls to `run_auto_maintenance()` in three key exit paths: after a successful sequence (`sequencer_finish()`), after a single pick (`continue_single_pick()` and `single_pick()`), and after a resolved conflict (`sequencer_continue()`). Maintenance errors are intentionally ignored to avoid masking user-facing issues. The patch also updates test coverage in `t/t3418-rebase-continue.sh` and `t/t3510-cherry-pick-sequence.sh`, using `GIT_TRACE2_EVENT` to verify that `git maintenance run --auto` executes exactly once at sequence end and not during intermediate steps.

This patch directly addresses the core goal of the series: consistent auto-maintenance behavior across all sequencer-driven commands. The changes are minimal and focused, touching only the exit paths where the sequencer finalizes its work. The test additions are thorough, covering both single-pick and multi-pick scenarios, and the commit message clearly explains the rationale and implementation. The patch looks well-crafted and ready for integration.