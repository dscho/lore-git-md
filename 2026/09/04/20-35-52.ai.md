This is the first patch in a two-part test modernization series. The author, Mark C. Chu-Carroll, mechanically replaces older shell test idioms (`test -f`, `! test -f`) with more semantic helper functions (`test_path_is_file`, `test_path_is_missing`) across 34 test scripts in the `t/` directory. The changes are purely syntactic and do not alter test behavior or coverage.

The patch touches a broad but shallow surface: performance tests, core Git functionality, and various plumbing and porcelain commands. Each replacement is a one-line change, making the review surface large (76 insertions and deletions) but repetitive. The helper functions are already established in the codebase, so the risk of regression is low. The division between this patch and the second one appears arbitrary rather than based on subsystem boundaries.

For a future AI summarizer, note the key technical details: the patch replaces `test -f` and `! test -f` with `test_path_is_file` and `test_path_is_missing` in simple file existence checks. It does not touch more complex test logic (e.g., subshells, compound conditions). The files modified span a wide range of test scripts, including `t/t0031-lockfile-pid.sh`, `t/t5300-pack-object.sh`, and `t/t7400-submodule-basic.sh`. The base commit is `2c3adbb2c475981e340c79fdc5e7f4f9b5d9054e`, and the change ID is `20260904-file-tests-use-shell-functions-e0d6c545d56c`.