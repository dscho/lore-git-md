This is a **tooling follow-up** in the final stages of the `git checkout -m` autostash conflict handling series. The series itself is already approved and queued for `next`; this exchange clarifies a minor discrepancy in Git’s style enforcement tooling.

Harald points out that Git’s GitHub CI workflow (`.github/workflows/check-style.yml`) does run `git clang-format`, but it does not flag inconsistent brace usage in `if/else if/else` cascades—specifically, the missing braces in an `else if` branch in `stash.c` that Junio and Phillip Wood noted earlier. This confirms that Git’s `.clang-format` configuration does not enforce brace consistency for such constructs, even though the local `make style` target (which runs the same command) is available for contributors to use. The discussion remains purely mechanical and does not affect the series’ integration status.