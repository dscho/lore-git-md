This is a **substantive review reply** in the ongoing discussion about whether to add a test for the absence of `CHERRY_PICK_HEAD` in `git cherry-pick --no-commit` conflict cases. Phillip Wood agrees with Patrick Steinhardt’s broader concern about over-testing but pushes back on the specific case, arguing that the test is justified because the logic around `CHERRY_PICK_HEAD` is "tricky" and the cost of adding it is minimal—just one line in an existing test (`t3507`).

The exchange clarifies the stakes: Phillip sees this as a low-cost safeguard against future regressions in a subtle part of the sequencer, while Patrick’s earlier review framed it as part of a worrying trend of "random tests" without clear value. Phillip’s position aligns with the thread’s earlier consensus (from his own reviews) that the current behavior is intentional, but he now suggests the test could still be worthwhile as defensive coverage. The discussion remains open on whether the test’s justification ("the logic is tricky") meets the bar for inclusion, especially given the author’s prior decision to withdraw the test patch.

Key technical details for follow-up: the test would add a single `test_ref_missing CHERRY_PICK_HEAD` check to `t3507`, leveraging existing conflict-advice infrastructure. The patch touches the sequencer subsystem and references `CHERRY_PICK_HEAD`, `--no-commit`, and the 2011 commit `d7e5c0cbfb`. No new symbols or behaviors are introduced. The thread’s focus has shifted from *whether* the behavior is correct (settled) to *how aggressively* Git should test edge cases in areas where the logic is non-obvious.