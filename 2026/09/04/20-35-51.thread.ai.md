Thread identity
Subject: [PATCH 0/2] Modernize test scripts: replace `test -f` with `test_path_is_file`
Series version: v1 (initial submission)
Originating author: Mark C. Chu-Carroll
Thread type: patch series (test modernization)

Core problem or goal
Replace older shell test idioms (`test -f`, `! test -f`, `test -d`) with more semantic helper functions (`test_path_is_file`, `test_path_is_missing`, `test_path_is_dir`) in 61 test scripts under `t/`. The goal is purely syntactic modernization, aligning with the project’s test guidelines; no behavioral changes or performance improvements are claimed.

Current status
Under review; no prior versions. The series consists of two patches: the first covers 34 test scripts (performance tests, core Git functionality, and various plumbing/porcelain commands), and the second covers 29 scripts (including `t/t5601-clone.sh`, `t/t7004-tag.sh`, and `t/t9001-send-email.sh`). Changes are mechanical and limited to simple file-existence checks; complex test logic (subshells, compound conditions) is untouched. No maintainer feedback or integration status has been recorded.

Key technical details
- Files touched: 63 test scripts in `t/` (61 previously noted plus `t/t5601-clone.sh`, `t/t7004-tag.sh`, `t/t9001-send-email.sh` from the second patch; exact list recoverable from the patches).
- New symbols: `test_path_is_file`, `test_path_is_missing`, `test_path_is_dir`, and related `test_path_is_*` helpers.
- Old-vs-new behavior: identical; only assertion syntax changes.
- Test coverage: unchanged; no new tests added.
- Base commit: `2c3adbb2c475981e340c79fdc5e7f4f9b5d9054e`.
- Change ID: `20260904-file-tests-use-shell-functions-e0d6c545d56c`.

Open questions and loose ends
None identified; the series is uncontroversial and follows established patterns. The division into two patches is arbitrary and noted as such, but this does not affect correctness or reviewability.

Participant positions
None recorded; the series has not yet received reviews or Acked-by/Reviewed-by trailers.

Related work
Part of the ongoing community-wide test modernization effort (e.g., `! test -f` → `test_path_is_missing`, `test -f` → `test_path_is_file`). No dependencies on other in-flight topics.