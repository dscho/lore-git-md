This is a user bug report targeting Git 2.55.0 on macOS, listing four distinct behavioral regressions or anomalies observed after upgrading via Homebrew.

The report centers on `git replay`, `git pull`, `git diff`, and `git checkout -`. The first item is a positive note: the author appreciates that `git replay` now updates the ref, which simplifies their `git-reply-checkout` script. The remaining three items are potential regressions: (1) `git pull` frequently fails with a "dangling remote ref" error and does not update the current branch, forcing a manual `git reset --hard` to `origin/main`; (2) `git diff` occasionally exits with status 0 without printing any output despite a dirty worktree, requiring a second invocation; (3) `git checkout -` sporadically fails with an "invalid ref" error after a pull when the previous branch was local, a behavior the author has not seen in earlier versions.

No prior thread context is provided, so these issues appear to be newly reported. The author explicitly asks whether any of the problems are already known or have documented explanations before they invest time in root-cause analysis. The report is concise, factual, and free of speculation beyond the observed symptoms.