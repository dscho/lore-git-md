This is a **documentation patch** that now stands alone after the author dropped the test portion of the series. Aleksei Sviridkin has abandoned the proposed test for `CHERRY_PICK_HEAD` absence in conflicted `git cherry-pick --no-commit` operations, responding to Patrick Steinhardt’s review questioning the test’s necessity. The remaining patch updates `git-cherry-pick.txt` to document the long-standing but undocumented behavior: when a `git cherry-pick --no-commit` fails due to conflicts, `CHERRY_PICK_HEAD` is *not* created, unlike `git revert --no-commit` (which *does* write `REVERT_HEAD`).

The documentation explicitly justifies this asymmetry, explaining that the behavior is intentional (confirmed by Phillip Wood) and dates back to a 2011 commit. It also clarifies the implications for users: when picking a single commit, `git commit -c <commit>` must be used to preserve authorship, as `git commit` alone will not have access to the cherry-picked commit’s metadata. The patch touches only the man page and does not propose any code changes, test additions, or behavioral adjustments.

The thread’s trajectory is now straightforward: the documentation patch is uncontroversial and directly addresses the gap identified in earlier discussion. No further iterations are expected unless reviewers raise concerns about the wording or completeness of the update. The patch is well-motivated, with clear historical context and no unresolved technical questions. For users who rely on `CHERRY_PICK_HEAD` or `REVERT_HEAD` for scripting or tooling, this documentation will be a welcome clarification.