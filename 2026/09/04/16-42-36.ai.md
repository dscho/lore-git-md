This is a **tooling discussion** that continues the spin-off conversation about Git’s `.clang-format` enforcement in CI, now focused on the trade-offs of adopting a stricter style-checking regime. The thread is no longer about the `git checkout -m` autostash conflict series, which remains queued for `next` with no open technical questions.

Harald Nordgren and Phillip Wood explore whether Git should move toward fully automated, consistent style enforcement. Harald argues that a linter-enforced, auto-fixable style would eliminate an entire class of code-review discussions, regardless of the specific rules chosen. He acknowledges that applying `clang-format` globally would be disruptive—generating many conflicts with in-flight topics—but sees this as a one-time cost worth paying for long-term consistency. Phillip’s earlier point—that the current CI job is advisory because `clang-format` does not perfectly match Git’s style guide and its output varies across versions—remains the crux of the trade-off: either accept the tool’s deviations and stop debating style, or continue handling style manually.

The discussion is purely about contributor ergonomics and project policy, with no immediate code changes proposed. No decisions or action items emerge; the thread documents the tension between automation and manual review, leaving the status quo intact for now.