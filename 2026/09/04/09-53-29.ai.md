This is a **substantive review reply** in the ongoing thread about Aleksei Sviridkin’s patch series documenting and testing the behavior of `CHERRY_PICK_HEAD` during conflicting `git cherry-pick --no-commit` operations. Phillip Wood’s email clarifies a critical point in the discussion: **the current behavior (not writing `CHERRY_PICK_HEAD` when `--no-commit` fails due to conflicts) is intentional and correct**, directly contradicting Junio’s AI-assisted rewrite of the commit message, which incorrectly assumed the patch proposed writing the ref.

The email is short but pivotal. Phillip’s correction establishes that the test patch (part 1) is not proposing a behavioral change but is instead **defensive coverage** for an existing, undocumented edge case. This shifts the focus of the thread: the question is no longer whether the behavior is correct (it is), but whether the test is justified (Patrick Steinhardt’s review challenges this) and whether the documentation (part 2) sufficiently explains the asymmetry with `git revert`. The email also implicitly validates Aleksei’s position that the behavior is intentional, countering Junio’s earlier skepticism.

Key technical details for follow-up: the review confirms the absence of `CHERRY_PICK_HEAD` in the conflict case is by design, not an oversight. The discussion now hinges on whether the test is necessary (Phillip does not weigh in on this) and whether the documentation adequately justifies the asymmetry. The email does not address the `git commit -c <commit>` workaround mentioned in the documentation patch, but this remains a likely point of contention if reviewers argue the current behavior is unintuitive. No new symbols or files are introduced; the focus remains on `CHERRY_PICK_HEAD`, `--no-commit`, and the sequencer’s conflict-handling logic.