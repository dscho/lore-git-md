This is the **v2 cover letter update** for Thomas Bachem’s patch series deferring auto maintenance until the end of sequencer-driven operations (`git rebase`, `git cherry-pick`, `git revert`). The series now consists of **three patches** (up from two in v1) and addresses all prior feedback from Phillip Wood.

The core change remains the same: auto maintenance is deferred until sequence completion to avoid interference with intermediate steps, matching the apply backend’s behavior. However, v2 expands the scope to cover **all three sequencer commands** (rebase, cherry-pick, revert) for consistency, as Phillip suggested. The implementation is now more robust: a new helper (`git_config_append_parameter()`) in `config.c` handles shell-quoted config string construction, and the config string is cached in `replay_ctx` to avoid repeated allocations. The commit messages have been rewritten for clarity, and test coverage (using `GIT_TRACE2_EVENT`) verifies maintenance runs exactly once at sequence end.

The series is technically complete and ready for review. No open objections remain; the only watch item is a hypothetical edge case about very long sequences where mid-sequence repacking *might* improve performance, but Phillip Wood has explicitly stated this is not a blocker. The patches touch `sequencer.c`, `config.c`, and test files (`t/t3418-rebase-continue.sh`, `t/t3510-cherry-pick-sequence.sh`). Key new symbols include `git_config_append_parameter()` and `disable_auto_maintenance()`, which builds and caches the config string in `replay_ctx`. Error handling is minimal—maintenance errors are ignored to avoid masking user-facing issues.

This is a **refactoring with behavioral impact** (sequencer, auto maintenance, config subsystems) and appears well-motivated and thoroughly tested. The expansion to cherry-pick and revert in v2 addresses Phillip’s earlier concern about consistency, and the reusable config helper improves code hygiene. The series looks ready to proceed.