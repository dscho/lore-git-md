This is the first patch in Patrick Steinhardt’s v2 series improving the `git maintenance` heuristic for `rerere gc`. It refactors the staleness-checking logic in `rerere.c` to make it reusable for the upcoming dynamic heuristic in patch 2/2.

The patch extracts two new helper functions: `rerere_gc_cutoffs()` computes the expiration timestamps for resolved and unresolved `rerere` entries, and `rerere_id_is_stale()` encapsulates the logic that determines whether a given entry is stale based on those cutoffs. The existing `prune_one()` function is updated to call `rerere_id_is_stale()` instead of duplicating the logic. No behavioral changes are introduced; this is purely a refactoring to prepare for the next patch.

The changes are mechanical and well-contained, touching only `rerere.c`. The commit message clearly states the purpose (enabling reuse for a second caller) and the implementation (two new helpers). Derrick Stolee’s review of v1 focused on stylistic preferences (e.g., ternary logic clarity) that were addressed in v2, and his "LGTM" on the series confirms the refactoring is sound. Thomas Bachem’s tested review further validates that the extraction does not break existing functionality.

This patch is a straightforward refactoring with no known issues or open questions. It is ready for integration alongside the rest of the series. Key technical details for follow-up discussion: the new `rerere_id_is_stale()` function and its parameters (`cutoff_resolve`, `cutoff_noresolve`), and the `rerere_gc_cutoffs()` helper that centralizes the cutoff computation.