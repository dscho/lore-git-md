This is the fifth patch in Patrick Steinhardt’s 11-patch series to unify Git’s terminology for "ref storage." It renames the `--show-ref-format` option in `git rev-parse` to `--show-ref-storage` and retains the old name as a backward-compatible alias.

The patch is a straightforward mechanical update: it changes the option name in the documentation, the command-line parser, shell completion scripts, and all test scripts that reference it. The diff shows a clean 1:1 replacement across 10 files, with no behavioral changes—just terminology alignment. The backward-compatible alias ensures scripts and users relying on the old name won’t break, which should minimize disruption.

Given the series’ goal of consistency ahead of the upcoming object storage extension, this patch is uncontroversial in direction. The only likely discussion points would be edge cases (e.g., overlooked references in less common code paths), but the thorough test coverage here suggests those risks are low. The patch is well-scoped and ready for review.