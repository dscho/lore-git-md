This is a v2 patch series for the sequencer subsystem, extending the auto maintenance deferral logic to cover `git rebase`, `git cherry-pick`, and `git revert`. The series now consists of three patches: a new helper in `config.c` to build configuration parameters, and two sequencer patches that ensure auto maintenance runs only once at the end of a sequence and is suppressed during intermediate commands.

The core goal remains the same as v1: prevent auto maintenance from running during intermediate steps of a sequence (rebase, cherry-pick, revert) to avoid races with the sequencer itself. The v2 series addresses Phillip Wood’s feedback by unifying the behavior across all three commands, factoring out the configuration parameter construction into a reusable helper (`git_config_append_parameter()`), and caching the constructed config string in `replay_ctx` to avoid repeated string operations. The commit messages have been rewritten for clarity and conciseness.

The implementation touches `config.c`, `sequencer.c`, and adds test coverage in `t/t3418-rebase-continue.sh` and `t/t3510-cherry-pick-sequence.sh`. The tests verify that auto maintenance runs exactly once at the end of a sequence and not during intermediate steps, using `GIT_TRACE2_EVENT` to inspect subprocess invocations. The series is based on `master` and remains independent of the rerere lock race fix mentioned in the cover letter.

This looks like a well-scoped, well-motivated improvement. The v2 changes address the polish and consistency concerns raised in the v1 review, and the new helper in `config.c` is a sensible abstraction. The test coverage is thorough, and the commit messages now clearly explain the rationale and behavior changes. The series should be uncontroversial and ready for further review.