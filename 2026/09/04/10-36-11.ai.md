This is the tenth patch in Patrick Steinhardt’s 11-patch series unifying Git’s "ref storage" terminology. It renames the config key `init.defaultRefFormat` to `init.defaultRefStorage` while retaining the old name as a backward-compatible alias. The change touches documentation (`BreakingChanges.adoc`, `config/feature.adoc`, `config/init.adoc`), the setup code (`setup.c`), and the test suite (`t/t0001-init.sh`).

The patch is a straightforward mechanical rename: every occurrence of `init.defaultRefFormat` is replaced with `init.defaultRefStorage`, and the old key is kept as an alias so existing configurations continue to work. The test updates are minimal, adjusting test names and variable references to match the new config key. No new functionality is introduced, but the patch ensures that the precedence rules established in earlier patches (e.g., `--ref-storage=` overriding environment variables) extend to the config key as well.

This is a refactoring patch with no behavioral changes beyond the rename. The backward-compatible alias mitigates disruption, and the updated tests verify that the new key behaves identically to the old one. The patch is uncontroversial in isolation, but its value depends on the broader series landing—without the rest of the series, the rename would introduce inconsistency rather than resolve it. Reviewers are likely to focus on whether the alias is sufficient for backward compatibility and whether any edge cases (e.g., config precedence in submodules) were overlooked.