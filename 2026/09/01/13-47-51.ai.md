This is a brief follow-up in the `git last-modified` Bloom filter optimization thread, specifically addressing test environment correctness in the v3 series. Junio C Hamano points out that the test added in patch 6/6 (wildcard pathspecs) fails in CI because `GIT_TEST_COMMIT_GRAPH=1` from the test harness overrides the `-c core.commitGraph=false` setting, making the baseline comparison invalid. He suggests explicitly unsetting `GIT_TEST_COMMIT_GRAPH` (to `0`) for the commit-graph-disabled case and explicitly setting it (to `1`) for the commit-graph-enabled case to ensure the test behaves as intended regardless of the broader test environment.

Toon Claes acknowledges the issue and confirms the fix is already incorporated in the newly posted v4 series. He also raises a minor follow-up: the test in patch 5/6 (which verifies `--show-trees` behavior) does not fail under the same conditions, suggesting its trace2-based checks might be less strict or its logic less sensitive to the test environment. Junio replies that this is likely because the test in patch 5/6 focuses on output correctness rather than internal code paths, so it does not notice if the Bloom filter fast-path is incorrectly bypassed. He notes that tightening the test to catch such cases would require additional instrumentation, but leaves it as a non-blocking observation.

The exchange is purely about test robustness and does not revisit the series' core functionality. The v4 reroll is complete and addresses all previously identified issues, including this test environment interaction. The follow-up about patch 5/6 is unlikely to block integration.