Patrick Steinhardt’s 12-patch series to remove ad-hoc ODB source linking—a refactoring that cleans up the object database subsystem by eliminating in-memory source registration (primarily used for submodules)—is now under review. The series, posted on August 31, 2026, also advances the broader effort to remove `the_repository` global state by excising implicit dependencies in `cache-tree`, `submodule-config`, and related areas. All patches are self-reviewed by Steinhardt, and the series is complete, with the final patch removing the now-dead `odb_add_to_alternates_memory()` function.

Junio Hamano’s review of the first patch is surface-level, focusing on code style rather than the series’ technical substance. He suggests a minor refactoring to avoid line-length issues caused by repeated `"the_repository"` references: make `cache_tree_fully_valid()` file-scoped static and introduce a wrapper to preserve the existing interface for external callers. This change would encapsulate the repository dependency within `cache-tree.c` without altering the patch’s core goal. No objections or deeper technical concerns were raised, and the series remains under review with no external feedback yet. The next steps will likely depend on whether Steinhardt incorporates Junio’s suggestion or proceeds with the current version.