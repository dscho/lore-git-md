Patrick Steinhardt’s twelve-patch series to remove ad-hoc ODB source linking—a refactoring that simplifies the object database subsystem and clears the way for future alternate-handling improvements—continues to advance under review. The series, posted on 2026-08-31, targets incidental cleanups (removing `the_repository` dependencies in `cache-tree`, `submodule-config`, and related callers), core refactoring (eliminating submodule ODB source registration), a memory-leak fix in the packed ODB source, and the final removal of the ad-hoc source-linking API.

Junio C Hamano has now weighed in with a substantive “Yay. Very nice.” on patch 6/12, which deletes the transitional submodule ODB registration mechanism after all its callers have been removed. This unqualified approval signals that the patch is uncontroversial and ready for integration, assuming the rest of the series holds up. Earlier, Junio had offered a surface-level suggestion on patch 1/12—encapsulating the repository dependency in `cache-tree.c`—but that feedback remains unaddressed; the author has not yet indicated whether the change will be incorporated. No other reviewers have engaged with the series, leaving the broader ODB refactoring and `the_repository` removal effort still under discussion. The series touches a wide swath of subsystems (ODB, submodule handling, grep plumbing, cache-tree, checkout, commit, sequencer, sparse-index, unpack-trees, fetch, and test infrastructure) but introduces no on-disk format changes or behavioral regressions beyond a documented breaking change in `git multi-pack-index --object-dir=`. With Junio’s approval on patch 6/12, the thread is moving toward convergence on at least part of the series, though the full set remains under review.