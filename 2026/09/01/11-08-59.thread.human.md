This thread is Patrick Steinhardt’s 12-patch series refactoring the object database (ODB) subsystem to remove ad-hoc source linking, a cleanup that simplifies the codebase and paves the way for future ODB abstraction work. The series, posted on August 31, 2026, targets the elimination of in-memory ODB sources (primarily used for submodules) and continues the broader effort to remove implicit dependencies on the global `the_repository` variable.

The second patch in the series, which this email covers, mechanically updates the `submodule-config` subsystem to accept an explicit `struct repository *` parameter instead of relying on `the_repository`. The changes are confined to function signatures and callers in seven files, with no behavioral impact—existing callers still pass `the_repository`, but the code is now more flexible for future refactoring. This patch is a low-risk, well-scoped step in the series, with no new design questions or loose ends introduced.

So far, the series has seen no review feedback, but it’s unlikely to be controversial given its focus on maintainability and alignment with the project’s long-term goals. The cover letter notes that this work is a prerequisite for future performance improvements in alternate handling, though those changes are not part of this series. The only remaining exception to the ad-hoc source linking removal is the quarantine directory mechanism, which remains untouched. The series touches multiple subsystems (ODB, submodule handling, grep plumbing, and others) but introduces no on-disk format changes or user-visible behavior differences.