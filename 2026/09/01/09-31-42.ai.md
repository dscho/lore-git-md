This is a substantive review reply in the `git checkout -m` autostash conflict handling thread. Phillip Wood endorses Junio C Hamano’s correctness concern about the current patch treating all non-zero returns from `run_command()` as conflicts, but proposes a simpler immediate fix than Junio’s three-step plan. He suggests adjusting the return value logic in `sequencer.c` to distinguish between true conflicts (`ret == 1`), command failures (`ret > 1` or negative), and signals (`ret < 0`), then updating the caller to print a new error message for non-conflict failures. This keeps the patch’s UX goal intact while addressing the correctness gap without requiring changes to `git stash` itself. The proposal is narrowly scoped to the patch’s immediate needs and avoids the broader exit-code audit Junio outlined, making it a pragmatic path forward for v3. Key symbols: `apply_save_autostash_oid()`, `run_command()`, `ret` (return value handling). Files touched: `sequencer.c` (return logic), `checkout.c` (error message addition). The review is tested in spirit—Phillip’s suggestion is a direct response to Junio’s technical critique and includes concrete code snippets.