This is a **substantive review** of the v3 patch in the `git checkout -m` autostash conflict handling series. Phillip Wood’s feedback focuses on three areas: exit code conventions, code clarity, and test coverage.

The review raises a **major design concern** about exit codes: Git’s established convention (used by `merge-tree`, `grep`, and `diff`) is to use exit code 1 for conflicts and other non-zero values for errors. The patch currently uses exit code 2 for conflicts, which Phillip argues is inconsistent and confusing. He suggests reverting to exit code 1 for conflicts to align with the rest of Git.

On code clarity, Phillip flags a nested ternary operator in `do_apply_stash()` as unreadable and notes that the original logic (using exit code 1 for conflicts) would avoid this complexity entirely. He also points out that the indentation in the function signature is odd and that the `STASH_APPLY_ERROR` assignment in one error path is redundant since other error paths still return `-1` implicitly.

For tests, Phillip questions the necessity of a new commit in the `t3903-stash.sh` test (the reset to `initial` should already provide a clean state) and suggests adding coverage for `stash branch` to match the exit code documentation. He praises the switch to `test_cmp` in `t7201-co.sh` for making the expected output clearer.

The review does not block the patch but **strongly advocates for a design change** (exit code 1 for conflicts) that would simplify the code and improve consistency. The author will need to decide whether to adopt this suggestion or argue for the current approach. The thread’s prior consensus on the enum API and advice formatting remains intact.