This is a substantive review reply in the ongoing `git checkout -m` autostash conflict handling thread. Phillip Wood agrees with Junio C Hamano’s diagnosis of the correctness issue in Harald Nordgren’s v2 patch — namely, that the current logic treats all non-zero returns from `run_command()` as conflicts, when in fact `git stash apply` can fail for other reasons (e.g., crashes, permission errors). However, Phillip proposes a simpler, more immediate fix than Junio’s three-step audit plan: he suggests tightening the return-value logic in `sequencer.c` to distinguish between true conflicts (exit code 1) and other failures (exit code > 1 or negative), while still preserving the patch’s core UX goal of printing a blank line only when conflicts occur.

Phillip’s proposal is pragmatic: it keeps the patch minimal and focused, addressing the correctness gap without requiring a broader audit of `git stash` exit codes. He explicitly ties the fix to the patch’s objective — ensuring the caller knows whether the conflict advice was printed — and suggests adding a new error message for non-conflict failures. This approach satisfies Junio’s concern about misleading behavior while avoiding the complexity of a full exit-code overhaul. The review is substantive, as it engages with the code’s behavior, proposes a concrete alternative, and maintains the patch’s scope. If adopted, v3 will likely incorporate this logic, making the patch ready for merging once the return-value semantics are clarified.