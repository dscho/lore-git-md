This is a **usability warning and bug report** about silent binary file corruption on Windows caused by misconfigured global `.gitattributes` rules. The author, Skybuck Flying, provides a detailed account of how a global `* text diff=lfclean` rule combined with a textconv filter corrupted PNG files during checkout while leaving DLLs intact (due to the latter lacking CR bytes). The email shifts from technical diagnosis to a **strong advisory against global `.gitattributes` rules**, framing them as an inherent design risk rather than a fixable edge case.

The issue is well-documented: the author explains the mechanism (Git treating all files as text and applying a CR-stripping filter), provides reproduction steps, and demonstrates the corruption via `fc /b` comparisons. The proposed fix—deleting global `.gitattributes` and related config—is practical but sidesteps deeper project-level questions: Should Git warn users about global `*` rules? Should it ignore `text` for known binary extensions? The author’s tone is urgent and combative, but the technical content is sound and actionable.

This email escalates the thread’s focus from a specific misconfiguration to a **systemic critique of Git’s attribute system**. While the author has disengaged from technical discussion, the report highlights a real usability pitfall: global `.gitattributes` rules with `text` or textconv filters are a footgun, and Git currently lacks safeguards to prevent silent corruption. The thread’s unresolved questions—whether to add warnings, automatic binary detection, or documentation improvements—remain open for maintainer input. For now, the takeaway is clear: global `*` rules with `text` are dangerous and should be avoided.