This is a CI/build system bugfix, part of a short thread wrapping up a one-line fix for a GitLab CI breakage. The patch itself was already summarized in the thread context: it adds `ruby` to the dependency list in `ci/install-dependencies.sh` so that `gem` is available for installing Asciidoctor, which the documentation job requires.

In this reply, Patrick Steinhardt acknowledges Toon Claes’s surface-level review comment—a typo in the commit message (“inus” instead of “in”)—and says he has fixed it locally but won’t send a new version just for that. The tone is matter-of-fact; there is no substantive discussion, no disagreement, and no further action requested. The thread is effectively closed.