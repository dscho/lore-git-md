Thread identity: standalone patch and follow-up series addressing GitLab CI documentation job failures and Asciidoctor version pinning modernization. The thread began with Patrick Steinhardt’s minimal fix (`ps/ci-depends-on-ruby`) to restore Ruby availability for `gem install asciidoctor` after a transitive dependency removal broke the documentation build. It evolved into a forward-looking discussion about CI policy, culminating in Jeff King’s two-patch series (`peff/ci-asciidoctor-bump`) to update the Asciidoctor version tested in CI from 1.5.8 (2018) to a system-provided version (2.0.20 in Ubuntu), eliminating manual version pinning entirely.

Core problem or goal: restore and modernize GitLab CI’s documentation build. The immediate goal was to fix a broken CI job by ensuring Ruby is available for Asciidoctor installation. The broader goal is to eliminate an outdated version pin and simplify CI configuration by using the system-provided Asciidoctor package, reflecting what users are likely to have installed locally and reducing maintenance overhead.

Current status: the original fix (`ps/ci-depends-on-ruby`) is effectively closed and ready for merging. Jeff King’s follow-up series (`peff/ci-asciidoctor-bump`) is now complete and uncontroversial. The first patch removed dead code (`ALREADY_HAVE_ASCIIDOCTOR`) from the retired Azure Pipelines CI, while the second patch replaces the manual `gem install` of Asciidoctor 1.5.8 with an `apt-get install asciidoctor`, relying on the Ubuntu image’s default version (2.0.20). The series is a low-risk maintenance update and is ready for merging once reviewed by Patrick Steinhardt or Junio C Hamano.

Key technical details:
- Files modified: `ci/install-dependencies.sh` (original fix and both patches in the follow-up series)
- Subsystem: GitLab CI infrastructure
- Original fix: explicit installation of `ruby` to ensure `gem` is available for Asciidoctor installation
- Follow-up series:
  - Patch 1: removes unused `ALREADY_HAVE_ASCIIDOCTOR` variable and conditional (dead code from retired Azure Pipelines CI)
  - Patch 2: replaces manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`, eliminating the version pin and simplifying the installation logic. The Ubuntu image’s default version (2.0.20) is now used, matching the version tested in GitHub Actions.
- Root cause: transitive dependency removal in the CI environment (original fix) and outdated version pin (follow-up series)
- Verification: original fix linked to a fixed pipeline in the commit message; follow-up series validated by GitHub Actions test run using Asciidoctor 2.0.20
- Additional context: CI previously pinned Asciidoctor to 1.5.8 (2018) due to historical compatibility concerns with 2.0.0. The blocker was resolved in commit `f6461b82b9` (2019), but the pin was never updated. The system-provided version (2.0.20) is known to work and is now tested in CI. The `concurrent-ruby` gem dependency (introduced in 2024) is no longer relevant with this change, as the system package handles dependencies.

Open questions and loose ends:
- None. The original bugfix and follow-up series are complete and uncontroversial. The question about the `concurrent-ruby` gem’s necessity is now moot, as the system-provided Asciidoctor package handles its own dependencies.

Participant positions:
- Patrick Steinhardt (author of original fix): posted the minimal fix, provided context and verification, acknowledged a minor commit-message typo but declined to revise the patch solely for this correction. In follow-up, agreed the Asciidoctor version pin is outdated but opted to keep the original fix minimal, deferring the version update to Jeff’s series.
- Toon Claes: surface-level review (commit-message typo, acknowledgment); no substantive technical feedback.
- Jeff King (Peff, author of follow-up series): provided historical context for the Asciidoctor version pin, noting the original blocker was resolved in 2019. Posted a two-patch series to eliminate the version pin and simplify installation by using the system-provided Asciidoctor package. The first patch removed dead code, while the second replaced manual installation with `apt-get install asciidoctor`. Framed the update as a minor, high-value maintenance change with no controversy. Validated the change with a GitHub Actions test run.
- Junio C Hamano (maintainer): endorsed Jeff King’s observation that the CI configuration’s Asciidoctor version pin is outdated and no longer necessary. Agreed that newer versions (2.0.x+) work fine locally and that CI should prioritize testing versions people are actually using. The comment was non-blocking and did not propose action items.
- Jean-Noël Avila (cc’d in follow-up series): no response regarding the `concurrent-ruby` gem dependency, but input is no longer relevant as the system-provided Asciidoctor package handles dependencies.