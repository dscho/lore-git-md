This is a **usability rant and configuration advice** email in the ongoing thread about silent corruption in Git workflows on Windows. The author, Skybuck Flying, has shifted from diagnosing the original textconv filter misconfiguration (unquoted backslashes corrupting `git diff` output) to **vehemently condemning global `.gitattributes` rules** after discovering they corrupted binary files (PNGs) during checkout.

The email is **not a patch or technical proposal**—it’s a **passionate, opinionated warning** against using global `.gitattributes` with broad `*` patterns and text filters. Skybuck argues that such configurations are "objectively dangerous" and provides step-by-step instructions to **remove global `.gitattributes` entirely**, along with the associated `diff.lfclean` filter. The tone is combative ("no bullshit," "what a fucking nightmare"), but the core advice—**avoid global filters, use repo-specific `.gitattributes` instead**—is technically sound and aligns with Git’s design philosophy of explicit over implicit behavior.

Key points for a busy reader:
- **Problem**: Global `.gitattributes` with `* text diff=lfclean` silently corrupts binary files (PNGs, JPEGs, etc.) during checkout by applying text filters to them.
- **Root cause**: The `*` pattern is too broad, and Git lacks safeguards to prevent text filters from being applied to binaries.
- **Solution**: Delete global `.gitattributes`, unset the global `core.attributesfile` config, and remove the `diff.lfclean` filter. For CR/LF cleanup, use **repo-specific** `.gitattributes` with explicit patterns (e.g., `*.txt text`).
- **Assessment**: The advice is **correct but overstated**. Global `.gitattributes` *can* be dangerous if misconfigured, but they’re not inherently unsafe—just high-risk. The email’s value lies in its **clear reproduction of the binary corruption issue** and its **practical workaround**, which could inform future documentation or diagnostic improvements (e.g., warnings for global `*` rules with `text`).

This email **does not advance the technical discussion** about potential Git-side fixes (e.g., `--debug-textconv`, size-mismatch warnings, or binary file detection), but it **reinforces the urgency** of addressing the binary corruption issue. Maintainers may treat this as a **documentation gap** (e.g., clearer warnings in `gitattributes(5)`) or a **usability issue** (e.g., built-in safeguards), but no actionable patch or RFC has been proposed here. If you’re following the thread for potential changes to Git, this email is **noise**—but if you’re debugging similar corruption, the removal steps are worth noting.