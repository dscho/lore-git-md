This email is a **user advisory and rant** from Skybuck Flying, responding to their own earlier bug report about binary file corruption caused by a misconfigured global `.gitattributes` filter. The author has abandoned technical discussion and shifted to a **strongly worded warning** against using global `.gitattributes` rules with `text` or textconv filters, framing the issue as an inherent, unacceptable risk rather than a fixable usability gap.

### Key points:
- **Subsystem**: `.gitattributes` handling, textconv filters, checkout process.
- **Risk**: Global `.gitattributes` rules like `* text diff=lfclean` force Git to treat *all* files (including binaries) as text, applying filters that corrupt binary content during checkout. The author now considers this **inherently dangerous** and advocates for **complete removal** of global filters rather than incremental fixes.
- **Proposed solution**: Delete the global `.gitattributes` file and unset all related Git config entries (`core.attributesfile`, `diff.lfclean.*`). For CR/LF cleanup, use **local** `.gitattributes` files with explicit extensions (e.g., `*.txt text`) instead of global `*` rules.
- **Tone**: Combative and urgent, with repeated warnings about "guaranteed corruption" and "silent corruption vectors." The author’s frustration is palpable, but the technical content remains accurate and actionable.
- **Assessment**: While the tone is unprofessional, the core message is valid: global `.gitattributes` rules with `text` or textconv filters are a footgun. The author’s proposed mitigation (removing global filters entirely) is a reasonable workaround, though not a project-level solution.

### What’s new:
- The author has **disengaged from technical discussion** and now treats the issue as a **fundamental design flaw** in Git’s attribute system, rather than a fixable usability gap.
- The email provides **explicit commands** to remove the global filter and verify its deletion, making it a practical guide for affected users.
- The author’s shift from "how to fix this" to "this is too dangerous to use" suggests they no longer expect Git to address the issue with warnings or safeguards.

### Open questions (for the project):
- Should Git **warn users** when global `.gitattributes` rules force text filters onto binary files?
- Should Git **ignore `text` for known binary extensions** (e.g., `*.png`, `*.jpg`) by default?
- Is this a **documentation gap** (e.g., clearer warnings in `gitattributes(5)`) or a **code-level fix** (e.g., built-in safeguards)?

### Related work:
- `.gitattributes` precedence rules (global vs. local files).
- Prior discussions about textconv safety (e.g., the earlier thread about shell quoting).
- Git’s approach to binary file detection (e.g., `core.autocrlf`, `core.safecrlf`).

### Summarizer brief:
This email escalates the binary corruption issue from a **bug report** to a **design critique**, arguing that global `.gitattributes` rules with `text` or textconv filters are inherently unsafe. The author provides a **workaround** (deleting global filters) but no longer engages with potential project-level fixes. The tone is hostile, but the technical content is sound: global `*` rules with `text` are a known footgun, and Git currently lacks safeguards to prevent silent corruption of binary files. Future discussion may focus on warnings, automatic binary detection, or documentation improvements. Key terms: `.gitattributes`, `textconv`, `global vs. local attributes`, `binary file corruption`, `checkout process`.