This email is a **detailed bug report and usability warning** about silent binary file corruption caused by misconfigured global `.gitattributes` rules. The author, Skybuck Flying, documents a real-world case where a global `* text diff=lfclean` rule forced Git to apply a textconv filter to all files—including binaries like PNGs—during checkout, corrupting them by stripping CR bytes. DLL files were unaffected only because they lacked CR bytes, making the corruption appear selective rather than systemic.

The report is thorough: it explains the technical mechanism (global `.gitattributes` precedence, textconv filters applied during checkout), provides reproduction steps, and demonstrates the corruption via `git show` vs. working-tree file comparison. The proposed fix—deleting global `.gitattributes` and related config entries—is practical, though the author now frames the issue as an **inherent design flaw** in Git’s attribute system, arguing that global `*` rules with `text` or textconv filters are too dangerous to allow at all.

Key technical details include the subsystems involved (`.gitattributes` handling, textconv filters, checkout process), the trigger (global `*` rules with `text`), and the symptoms (silent corruption of binaries containing CR bytes). The author’s tone remains combative, but the content is actionable: the corruption is reproducible, the root cause is clear, and the workaround is straightforward. However, the report does not engage with potential project-level solutions (e.g., warnings, automatic binary detection) beyond advocating for the removal of global filters entirely.

This email escalates the issue from a user misconfiguration to a **systemic risk**, highlighting that Git currently lacks safeguards to prevent silent corruption of binary files when global `.gitattributes` rules are misused. Future discussion may focus on whether Git should warn users about global `*` rules, ignore `text` for known binary extensions, or improve documentation to emphasize the dangers of global filters. The thread remains unresolved, with no maintainer feedback yet on the proposed safeguards or design critiques.