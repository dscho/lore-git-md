This thread began with a simple fix to GitLab CI: a missing Ruby dependency broke the documentation job after an upstream change removed it as a transitive dependency of Asciidoctor. Patrick Steinhardt’s one-line patch—adding `ruby` to the dependency list in `ci/install-dependencies.sh`—restored the build and is uncontroversial, ready for merging. Toon Claes noted a minor typo in the commit message, but the author declined to send a new version for such a small correction.

The thread then took a slight detour when Jeff King pointed out that Git’s CI still pins Asciidoctor to version 1.5.8, a holdover from 2018 when compatibility issues with Asciidoctor 2.0.0 were a concern. Peff clarified that the blocker was fixed shortly after the pin was introduced, meaning the version restriction is now unnecessary. While this isn’t a pressing issue—CI works fine as-is—it’s a low-effort maintenance opportunity to bring the configuration in line with modern usage. No one has objected or proposed a follow-up patch yet, so the thread remains effectively closed on the original bugfix, with this version-pinning discussion lingering as a minor open item for future consideration.