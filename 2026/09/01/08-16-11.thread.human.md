This thread began with a simple fix to Git's GitLab CI: a missing Ruby dependency broke the documentation job because Asciidoctor could no longer be installed via `gem`. Patrick Steinhardt posted a one-line patch to `ci/install-dependencies.sh` that restores the ability to install Asciidoctor, and the fix itself is uncontroversial and ready to merge. The patch has been verified in a linked pipeline, and while Patrick acknowledged a minor typo in the commit message ("inus" instead of "in"), he opted not to send a new version just for that correction.

Alongside the fix, Jeff King provided historical context about the CI configuration's Asciidoctor version pin (1.5.8, from 2018), noting that the original compatibility blocker with Asciidoctor 2.0.0 was resolved in 2019 but the pin was never updated. Patrick agreed that the pin is outdated and could be revisited in a future CI maintenance patch, possibly even switching to Ubuntu's system package to avoid `gem` entirely. However, he kept the original fix minimal, leaving the version-pinning discussion as a forward-looking, non-blocking topic. The thread is effectively closed on the bugfix, with no open questions or objections.