This is a short follow-up from Patrick Steinhardt in the "What's cooking" thread. He asks Junio to evict his stalled `ps/fetch-packfile-uris-parallel` topic (two patches that parallelize packfile URI downloads during `git fetch`/`git clone`) because it conflicts with an upcoming series from Justin Tobler that converts `git-fetch-pack` to use object transactions. Patrick plans to resend his series once Justin’s work lands. The topic remains stalled, not discarded, and will reappear in a future report.