This is Junio C Hamano, the maintainer, asking a high-level design question about the `--missing-only` feature for `git rev-list`. The patch itself is technically complete and has strong consensus from reviewers, but Junio is probing the broader workflow in which GitLab's Gitaly uses this feature.

Junio’s question is not a request for implementation changes but an attempt to understand the end-to-end use case: how the list of missing objects is actually consumed. He sketches a potential inefficiency—peeling the onion one layer at a time via repeated `rev-list --missing-only` calls—and wonders whether this is the intended workflow or if there is a smarter batch-fetch strategy that the feature should support or document.

The thread context confirms that all technical feedback on the patch has been addressed and that the implementation is ready for final review. This maintainer query is therefore the last open question before the topic can be queued. No code changes are expected unless the discussion reveals a fundamental flaw in the workflow assumptions.