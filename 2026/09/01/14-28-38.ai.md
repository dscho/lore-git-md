This is the final acknowledgment in the v4 series fixing race conditions in Git’s HTTP daemon test helpers. Michael Montalbo accepts Patrick Steinhardt’s surface-level suggestion to reword the documentation comment in `t/lib-httpd.sh` from "claims the marker" to "is guarded by the marker" for clarity. The exchange confirms the series is now complete, with no further changes needed; Patrick Steinhardt’s earlier endorsement ("the whole series reads a lot better now") signals readiness for integration into `next`. The series replaces non-atomic shell operations in two CGI test helpers (`apply-one-time-script.sh` and `http-429.sh`) with atomic alternatives (`rm` without `-f` and `mkdir`), eliminating race conditions that could cause HTTP 500 errors during concurrent requests. All substantive feedback from prior iterations has been addressed, and the commit messages have been manually rewritten to address verbosity concerns. The documentation update now resides in `t/lib-httpd.sh` (per Patrick’s preference) and explicitly recommends PID-inclusive scratch file names and atomic operations for concurrency safety. The series is a targeted bugfix for test infrastructure, with no dependencies or related work.