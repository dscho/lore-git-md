This is a brief exchange in the ongoing review of the `git last-modified` Bloom filter optimization series. Junio C Hamano points out that the test in patch 6/6 (now 4/4 in v4) fails in CI because the test harness exports `GIT_TEST_COMMIT_GRAPH=1`, which overrides the `-c core.commitGraph=false` setting. He suggests explicitly unsetting the environment variable (`GIT_TEST_COMMIT_GRAPH=0`) for the baseline test and explicitly setting it (`GIT_TEST_COMMIT_GRAPH=1`) for the commit-graph-enabled case to ensure the test behaves as intended regardless of the broader environment.

Toon Claes acknowledges the issue and confirms the fix is already incorporated in the newly posted v4 series. He also raises a minor follow-up: the same environment control might be needed in patch 5/6 (now 3/4 in v4), though the test there does not currently fail without it. Junio replies that the test in patch 5/6 may not be checking the Bloom filter fast-path as strictly, so the difference is not caught. He leaves it as an open question whether the test should be tightened to verify the exact code path, but this is not presented as a blocker for integration.

The exchange is surface-level and pragmatic, focusing on test correctness rather than the patch's core functionality. The v4 reroll is complete and addresses all previously identified issues, including this test environment fix. The follow-up about patch 5/6 is unlikely to delay the series.