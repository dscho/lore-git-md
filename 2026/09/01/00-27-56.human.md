This is the final patch in a three-part bugfix series targeting race conditions in Git’s HTTP daemon test helpers. It adds documentation to `t/lib-httpd.sh` to guide future contributors writing concurrency-safe CGI scripts for the test suite.

The patch updates `t/lib-httpd.sh` with a concise section explaining why state management in these scripts must be atomic and providing concrete examples of safe and unsafe patterns. It highlights the use of `mkdir` for first-request election (as implemented in `http-429.sh`), chaining `rm` (without `-f`) with the logic that consumes a one-shot marker (as in `apply-one-time-script.sh`), and including the process ID (`$$`) in scratch file names to prevent concurrent requests from clobbering each other’s state. The examples are cross-referenced to the actual scripts fixed in the series, making the advice actionable and grounded in real code.

This is a documentation patch, but it is tightly scoped to the technical problem addressed by the series. The placement in `t/lib-httpd.sh` (rather than `t/README`) keeps the guidance close to the code it applies to, addressing earlier reviewer feedback. The wording is clear and prescriptive, avoiding vague advice in favor of specific idioms that have been proven to work in the accompanying patches. There are no open questions or controversies; the patch simply codifies the patterns already implemented in the series.

The series as a whole is ready for integration into `next`. The fixes are minimal, well-tested, and motivated by real-world flakiness in CI. This documentation patch is a sensible finishing touch, ensuring the lessons from the bugfixes are not lost on future contributors.