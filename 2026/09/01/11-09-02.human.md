This is the third patch in Patrick Steinhardt’s 12-patch series refactoring the object database (ODB) subsystem to remove ad-hoc source linking. It continues the `the_repository` removal effort by eliminating two uses of the global `the_hash_algo` in `submodule-config.c`.

The patch is a small, targeted cleanup: one change replaces `the_hash_algo` with the repository’s hash algorithm (already available in the caller’s context), and the other updates the hashmap key computation to use the algorithm of the passed-in object ID. A defensive check is added to guard against uninitialized object IDs, though the commit message notes this case should never occur. The diff is minimal—just four lines changed—and the rationale is clear: removing implicit global state dependencies.

This patch is uncontroversial and well-motivated. It doesn’t introduce new behavior or design questions, and the changes align with the broader goal of making the codebase more modular. No external reviews have been posted yet, but the patch looks solid and ready for integration. It’s a typical example of the incremental, mechanical work that characterizes the `the_repository` removal effort.