This is a brief administrative update in the ongoing "What's cooking" thread. Junio C Hamano acknowledges Patrick Steinhardt's request to evict the `ps/fetch-packfile-uris-parallel` topic from the integration branches (`next` and `seen`). The topic, which enables parallel downloads of packfile URIs when `fetch.packfileURIThreads` is set, was previously marked as "Expecting a reroll" in Junio's report. Patrick explains that the eviction is necessary to avoid conflicts with an upcoming series from Justin Tobler that refactors `git-fetch-pack` to use object transactions. Once Justin's work is merged, Patrick plans to resend his parallel fetching series. No technical objections or review feedback are involved; this is purely a coordination move to keep the integration branches clean. The topic remains viable and will return after the ODB transaction work stabilizes.