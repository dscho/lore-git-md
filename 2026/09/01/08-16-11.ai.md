This is a CI/build system bugfix. The patch addresses a recent failure in Git's GitLab CI "documentation" job, where the `gem` command (needed to install Asciidoctor) became unavailable after a dependency update removed Ruby as a transitive dependency of Asciidoc. The fix explicitly installs Ruby in the job's dependency list, restoring the ability to install Asciidoctor via `gem`.

The change is minimal: a one-line modification to `ci/install-dependencies.sh`, replacing the existing `asciidoc xmlto docbook-xsl-ns make` package list with one that adds `ruby`. The commit message clearly explains the root cause (transitive dependency removal) and provides a link to the fixed pipeline. This is a targeted fix for a specific CI breakage rather than a broader infrastructure change.

Patrick Steinhardt, the author, is a prolific contributor with deep CI and build system expertise, particularly in GitLab CI contexts. His patches are typically well-tested and narrowly scoped, and this one follows that pattern. The failure was recent and observable in public CI, so the fix is both timely and verifiable.

No thread context was provided, so this appears to be a standalone patch rather than part of a larger series. The fix is uncontroversial and unlikely to affect other CI jobs, as it only adds a missing dependency for the documentation build process. The patch is ready for review and likely to be merged quickly given its straightforward nature.