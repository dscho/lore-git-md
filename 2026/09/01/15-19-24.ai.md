This is the refactoring patch in Karthik Nayak’s three-part series introducing the `receive-report` hook. It extracts the status-report generation logic from `report()` and `report_v2()` into a shared helper, `generate_report()`, so the next patch can reuse it for the new hook.

The patch is mechanical: it moves the pkt-line formatting loop into the new helper, adds a boolean parameter (`detailed_report`) to toggle v2-specific reports, and updates the callers to pass a pre-allocated `strbuf`. No behavior changes; the only observable difference is the renamed parameter (`detailed_report` instead of the earlier `add_reports`). All prior feedback—commit-message clarity, function naming, and parameter naming—has been incorporated.

The refactoring is well-motivated: the commit message explicitly states the goal is to prepare for the hook’s failure handling in the next patch. The helper is cleanly factored, and the diff is easy to follow. No technical concerns remain; Patrick Steinhardt’s substantive review called it “looks good” otherwise, and the series as a whole is queued in `seen` awaiting graduation to `next`.