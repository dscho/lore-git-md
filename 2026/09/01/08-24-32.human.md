This is a CI/build system bugfix. Toon Claes reviews Patrick Steinhardt’s one-line patch that restores the GitLab CI “documentation” job after it broke when a dependency update removed Ruby as a transitive dependency of Asciidoc, leaving `gem` unavailable for installing Asciidoctor.

The review is surface-level: Toon catches a minor typo (“inus” → “in us”) in the commit message, confirms the fix matches what he was investigating, and gives a brief “Good!” and “Thanks!” without engaging with the technical rationale or testing. The patch itself is uncontroversial—it simply adds `ruby` to the apt-get install line in `ci/install-dependencies.sh`—and Patrick’s commit message already links to the fixed pipeline, so the change is both minimal and verifiable. Given the narrow scope and the fact that the failure was recent and observable, this is likely to be merged quickly.