This is the tenth patch in Patrick Steinhardt’s 12-patch series refactoring the object database (ODB) subsystem to remove ad-hoc source linking. The patch updates the `test-read-midx` helper to stop linking newly created ODB sources into the main object database, aligning it with the broader goal of simplifying the ODB’s source management.

The change modifies `t/helper/test-read-midx.c` to explicitly create and manage a `struct odb_source_packed` for the multi-pack index (MIDX) being tested, rather than relying on the now-removed `odb_add_to_alternates_memory()` function. The helper now tracks the created source and frees it explicitly after use, ensuring no memory leaks. This is a mechanical adaptation of the test helper to the new ODB API introduced earlier in the series, with no behavioral changes expected.

The patch is part of the ongoing ODB abstraction effort and continues the `the_repository` removal theme. It touches only the test helper, so it carries minimal risk and serves primarily as a cleanup to ensure the test infrastructure remains functional after the core refactoring. The diff is straightforward, replacing the old source-linking logic with direct source creation and cleanup. No external reviews or objections have been raised yet.