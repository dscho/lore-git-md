**Summarizer brief mode**

This email is a follow-up in the trace2 hardening thread, where Jeff King (Peff) and Derrick Stolee continue their discussion about the limitations of the current `banned-die.h` approach and explore potential long-term solutions for making trace2 truly resilient to `die()`-triggering failures.

Jeff expands on his earlier critique, emphasizing that the current series only scratches the surface of the problem. He argues that even if all direct calls to `die()`-prone functions are blocked, trace2 remains vulnerable to indirect calls via dependencies like `strbuf` or `json-writer.c`. He suggests that a "safe strbuf" library would not be sufficient, as it would still need to surface allocation or truncation failures to callers, effectively requiring a new string-handling interface tailored for trace2. His preferred solution is a "ground-up no-dependency rewrite" of trace2, stripping it of all unsafe dependencies to ensure it cannot crash Git under any circumstances. He acknowledges that this would be a significant effort but frames it as the only way to fully achieve the goal of making trace2 "best effort."

Derrick agrees that updating dependencies to be "safe" is non-trivial and could amount to a rewrite, aligning with Jeff’s perspective. He shares a CodeQL query to illustrate the scope of the problem, demonstrating how trace2 functions can still reach `die()` indirectly. Jeff responds that static analysis (like CodeQL) could theoretically solve the "whack-a-mole" problem by identifying all transitive `die()` calls, but notes that this would still require rewriting the code to avoid those dependencies. He speculates that trace2’s role as a passive data consumer (rather than an active participant in Git’s logic) might make it feasible to isolate from unsafe dependencies, though he acknowledges potential challenges in disentangling it from generic Git code.

Key technical details for follow-up:
- **Subsystems at risk**: `strbuf` (memory allocation + `snprintf()` failures), `json-writer.c` (indirect `die()` calls), and any other helper that transitively invokes banned functions.
- **Proposed solutions**:
  - A "ground-up no-dependency rewrite" of trace2, stripping it of all unsafe dependencies to ensure it cannot crash Git. This would involve creating a new, minimal string-handling interface (rather than a "safe strbuf") and avoiding generic Git code that could trigger `die()`.
  - Static analysis (e.g., CodeQL) to detect transitive `die()` calls, though this would still require rewriting the code to avoid those dependencies.
- **Behavioral impact**: If trace2 remains dependent on unsafe helpers, allocation or system call failures could still crash Git, undermining the series’ goal of making trace2 "best effort."
- **Files/context**: The discussion references `strbuf.h`/`strbuf.c`, `json-writer.c`, and Derrick’s CodeQL query, which targets all trace2 `.c` files (`trace2.c` and `trace2/*.c`).
- **Open questions**:
  - Is a ground-up rewrite of trace2 feasible, or would it require too much effort?
  - Can trace2 be fully isolated from generic Git code, or will it always depend on unsafe helpers?
  - Would static analysis be sufficient to enforce `die()`-free code in trace2, or would it require too much upfront work?
  - Should the current series land as a first step, or should it be held until a more comprehensive solution is available?

The tone remains collaborative, with both parties acknowledging the complexity of the problem and the trade-offs involved. Jeff’s preference for a rewrite reflects his skepticism that incremental hardening can fully solve the issue, while Derrick’s CodeQL query underscores the scope of the problem and the need for future work. No objections or technical disagreements are raised; the discussion is forward-looking and exploratory.