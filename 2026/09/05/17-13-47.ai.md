**Summarizer brief mode**

This is a reply in the ongoing test-coverage debate for the `git cherry-pick --no-commit` conflict edge case. Aleksei Sviridkin resubmits the test patch (part 1/2 of the v3 series) with an updated commit message that adopts Phillip Wood’s phrasing: the absence of `CHERRY_PICK_HEAD` is governed by a single “tricky” condition in `do_pick_commit()` that depends on the command (`cherry-pick` vs `revert`), whether the merge started, and the `--no-commit` flag, and the conflicted `--no-commit` case is the only one not already covered by the test suite. The patch still adds exactly one line (`test_ref_missing CHERRY_PICK_HEAD`) to `t3507-cherry-pick-conflict.sh`, touching no other files or behaviors. The thread’s focus remains on whether this minimal test justifies its inclusion given the broader policy concern about test-suite overhead.