**Summarizer brief mode**

This email is a reply in the ongoing `kh/format-patch-range-diff-notes` thread, which was previously marked as "Retracted" in Junio’s "What’s cooking" report. The patch series (3 commits) introduced a `--[no-]range-diff-notes` option to `git format-patch`, allowing users to configure notes specifically for range-diff output, separate from the notes displayed on the patches themselves. The series also included preparatory refactoring: renaming a struct member in `revision.h` to clarify its role in notes handling and simplifying the `get_notes_arg` parameter list.

The author (Kristoffer Haugsbakk) acknowledges the retraction and signals intent to revise the documentation to address clarity concerns. No technical objections to the code itself were noted in the thread context, but the retraction suggests the documentation or user-facing explanation was insufficient. The email does not include a reroll or new patches, only a commitment to draft an updated doc. The series is not currently cooking in any integration branch (`next` or `seen`), and its future depends on whether the revised documentation resolves the earlier concerns.

Key technical details for follow-up:
- **Command**: `git format-patch`
- **New option**: `--[no-]range-diff-notes` (configurable via `format.rangeDiffNotes`)
- **Refactoring**: `revision.h` struct member rename (likely `notes_opt` -> `range_diff_notes_opt` or similar) and `get_notes_arg` parameter simplification
- **Subsystem**: revision machinery, notes handling, and patch formatting
- **Related work**: range-diff output (introduced in earlier series) and notes configuration for other commands
- **Likely discussion points**: clarity of the documentation, whether the separation of range-diff notes from patch notes is justified, and whether the refactoring is sufficiently motivated

The thread is currently stalled pending the doc update; no new code or design changes have been proposed yet.