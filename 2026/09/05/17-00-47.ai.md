This is a documentation patch thread update confirming the correction has already been made.

Kristoffer Haugsbakk replies to Fabian Kratz’s report of a typo in the `git-merge(1)` man page, pointing out that the issue was fixed in Git 2.51.0 via commit 953049ee ("docs: correct ORIG_HEAD example in 'git merge' documentation", July 5, 2025). The fix aligns with Fabian’s observation: `ORIG_HEAD` should point to `G` (the tip of `master`) rather than `C` (the tip of `topic`) before a merge. No further action is needed on this thread, as the correction is already part of the released codebase.

Key details for follow-up: the commit OID (953049ee), the release version (2.51.0), and the file involved (`Documentation/git-merge.txt`). The thread can now be considered resolved.