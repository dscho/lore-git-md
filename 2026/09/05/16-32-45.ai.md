This is a CI/build system patch. Junio C Hamano replies to Jeff King’s patch updating Git’s CI workflows to use Debian 12 instead of Debian 11, confirming he noticed the same Debian 11 job failures and will queue the patch for integration. The patch itself is a straightforward, mechanical update to `.github/workflows/main.yml` and `.gitlab-ci.yml`, replacing `debian:11` with `debian:12` and updating the support window comment to reflect the new LTS end date (2028-06-30).

The thread is now effectively resolved: the patch is well-motivated (Debian 11 is no longer supported), Peff tested the GitHub job and expects the GitLab job to work, and Junio has signaled his intent to merge it. The only open question from the original patch—whether to retain an older LTS job for compatibility testing—remains unaddressed, but Junio’s reply does not raise it, suggesting it is not a blocker. The patch is now in `seen` and will likely graduate to `next` in the next integration cycle. No further discussion or review is evident in this email.