This is the first patch in a now-finalized four-patch documentation series (v3) that was merged to `next`. The series improves the discoverability of the `gitdatamodel(7)` man page, which explains Git's data model but was previously unlinked from other documentation.

This specific patch adds `gitdatamodel(7)` to `command-list.txt`, which ensures it appears in the "Guides" section of `git(1)` and is listed by `git help --guides`. The change is a single-line addition to the file, reflecting the series' minimal, targeted approach. The commit message notes that the page was added in dee8040 ("doc: add an explanation of Git's data model", 2025-11-12) but was effectively invisible due to lack of cross-references.

The patch is purely mechanical and uncontroversial, addressing a straightforward documentation gap. The v2 iteration corrected a minor wording nit in the commit message ("anywhere" instead of "everywhere"), and v3 incorporated final stylistic tweaks to the series as a whole, none of which affect this patch. The series has no open questions, and the author confirmed no regressions after testing against the `seen` branch. This patch is the foundation for the subsequent cross-references added in patches 2–4.