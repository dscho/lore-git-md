This is a brief clarification in the ongoing bugfix series for Git’s pathspec handling. Junio C Hamano acknowledges a misreading of the pre-patch behavior in `common_prefix_len()`: the function did **not** return a bogus string (like `!x`) when the first pathspec item was an exclude pathspec, but instead returned an empty string. This aligns with Elijah Newren’s earlier analysis and refines the understanding of the bug’s manifestation—though the fix itself remains correct and unchanged.

The series, which addresses a memory-safety issue (heap-buffer-overflow) and a logical bug in pathspec prefix calculation, is already queued for merging in Junio’s tree. This exchange is purely about commit-message accuracy and does not affect the integration status or technical content of the patches. No further action is expected from the author or reviewers.