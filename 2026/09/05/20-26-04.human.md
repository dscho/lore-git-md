This is a workflow question from a user seeking advice on integrating GitHub pull requests into a multi-tier Git setup.

The author describes a three-layer infrastructure: GitHub at the top, a local "dispatch" repository in the middle (used only for pulling PRs and pushing local changes), and a development repository at the bottom where daily work happens. They have a working flow for pushing local changes up to GitHub but are struggling with the reverse direction—incorporating GitHub PRs into their local workflow while preserving a clean commit history.

The core problem is that when they apply a PR's changes locally and commit them to their release branch, that commit lacks a direct reference to the original PR. They want to rebase or merge this local commit onto the PR branch in their dispatch repository so that GitHub will recognize it as resolving the PR, even if their local repository is ahead of the PR. Their goal is to do this with pure Git functionality, avoiding GitHub-specific tools like `gh`.

The email includes a clear ASCII diagram illustrating the desired flow and where it breaks down. The author is polite, detailed, and specific about their constraints (no browser-based actions, no external helpers). This is not a patch or code contribution—it’s a request for workflow guidance from experienced Git users.