**Summarizer brief mode**

This is a documentation patch series (`kh/doc-datamodel`) that adds cross-references from key Git man pages (`git(1)`, `glossary(7)`) to the new `gitdatamodel(7)` tutorial. The series consists of four commits:

1. Lists `gitdatamodel(7)` as a concept guide in `git(1)`.
2. Links the `git(1)` page to the tutorial.
3. Links four glossary terms (`blob`, `commit`, `object`, `tree`) to `gitdatamodel(7)`.
4. Adds a reciprocal link from `gitdatamodel(7)` back to the glossary.

The series is currently in `seen` and was marked by Junio as a candidate for `next`. Kristoffer Haugsbakk (the author) explicitly requests that it **not** be merged to `next` yet, citing an upcoming reroll to address feedback from Julia Evans. No technical objections or design concerns are noted in the thread; the reroll is purely to incorporate review suggestions. The patch touches only the `Documentation/` directory and introduces no new behavior or code.