This is the revived test patch (part 1/2) in the v3 series documenting the absence of `CHERRY_PICK_HEAD` during a conflicting `git cherry-pick --no-commit`. The author acknowledges Phillip Wood’s point that the existing test suite (`t3507`) does not actually verify this edge case, and now adds a one-line check (`test_ref_missing CHERRY_PICK_HEAD`) to the existing "advice from failed cherry-pick --no-commit" test. This directly addresses the gap Phillip identified in the "tricky" logic of `do_pick_commit()`, where the `!opts->no_commit` guard could regress without detection.

The patch itself is minimal and narrowly scoped: it pins the current behavior (no `CHERRY_PICK_HEAD` on conflict) as a regression guard, not as a new requirement. The debate over whether this test justifies its inclusion remains unresolved—Junio C Hamano and Patrick Steinhardt have previously opposed it on test-suite overhead grounds—but the author has now met Phillip’s technical objection by ensuring the absence of `CHERRY_PICK_HEAD` is explicitly verified. The companion documentation patch (part 2/2) remains uncontroversial and clarifies the design intent for users. This patch is unlikely to move forward without broader consensus on the value of such narrowly targeted tests.