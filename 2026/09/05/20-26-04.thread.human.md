This thread is a user support question about integrating GitHub pull requests into a custom three-tier Git workflow. The author, Erdhoernchen, describes a setup with three repositories: a GitHub repository at the top, a local "dispatch" repository in the middle (used for pulling PRs and pushing local changes), and a development repository at the bottom where daily work happens. The workflow for pushing local changes to GitHub works smoothly, but the challenge is handling incoming GitHub PRs—specifically, how to integrate a commit from the development repository’s `release` branch into the dispatch repository’s PR branch (`pr/x`) in a way that GitHub recognizes as resolving the PR without introducing extra merge commits or disrupting the commit chain.

The question is purely about Git commands or workflow techniques, not about patch development or code changes. The author wants to avoid manual browser-based actions and seeks a pure Git solution to rebase or merge their local commit onto the PR branch while preserving the commit ID from their development repository, ensuring GitHub closes the PR as merged. No prior discussion or proposed solutions are included in the email, so the thread is just beginning. If you’re familiar with GitHub’s PR mechanics or have experience with multi-repository workflows, this might be a good thread to chime in on.