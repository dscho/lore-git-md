This is a brief follow-up in the "What's cooking" thread about a build-system and runtime-configuration topic. D. Ben Knoble replies to Junio’s note that the three-patch series `dk/use-nsec-runtime` is "Waiting for response" by pointing out that he already sent a reply two days earlier. The series converts the build-time `USE_NSEC` knob into a runtime configuration `core.useNanosec`, letting a single binary adapt to filesystem capabilities without recompilation. No new technical points are raised; the email simply nudges Junio to check the existing response and invites other reviewers to chime in. The patch is uncontroversial and looks ready to proceed once Junio processes the reply.