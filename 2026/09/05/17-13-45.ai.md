This is the revived test patch (part 1/2 of the v3 series) in the ongoing thread about documenting the absence of `CHERRY_PICK_HEAD` during a conflicting `git cherry-pick --no-commit`. The patch adds a single line to `t/t3507-cherry-pick-conflict.sh` to verify that `CHERRY_PICK_HEAD` is *not* created when `git cherry-pick --no-commit` fails due to conflicts. This directly addresses Phillip Wood’s concern that the existing test suite does not guard against regressions in the "tricky" logic of `do_pick_commit()` (specifically, the `!opts->no_commit` guard).

The patch is minimal and narrowly scoped: it adds only one check (`test_ref_missing CHERRY_PICK_HEAD`) to an existing test case ("advice from failed cherry-pick --no-commit"). The commit message acknowledges the earlier misunderstanding in v1 and v2, where the author initially believed the test suite already covered this edge case. The justification for the test is now tied to Phillip Wood’s argument about the fragility of the shared logic in `do_pick_commit()`, though the broader debate about whether the test’s value outweighs its cost remains unresolved. Junio C Hamano and Patrick Steinhardt have previously opposed the test on grounds of test suite overhead, while Phillip Wood sees it as a necessary safeguard.

This patch is unlikely to be controversial in isolation, but its inclusion hinges on the outcome of the larger discussion about test policy. If the test is accepted, it will serve as a regression guard for the documented behavior; if rejected, the documentation patch (part 2/2) will still stand on its own, but the edge case will remain untested. The patch touches no code outside the test suite and introduces no new symbols or behaviors.