This is the final note in the `peff/ci-asciidoctor-bump` series, a CI/build system patch targeting GitLab CI’s Asciidoctor installation. Jeff King adds a small but useful data point to the earlier discussion about version compatibility: even older enterprise distributions like Debian 11 (now out of LTS) and RHEL 8 ship Asciidoctor 2.x, not the 1.5.8 version previously pinned in CI. This reinforces the decision to drop the outdated version pin and rely on the system-provided package (2.0.20 in Ubuntu), as the change aligns CI with real-world user environments.

The email is purely informational and does not introduce new code or objections. It closes the version-compatibility discussion Patrick Steinhardt raised earlier, confirming that testing Asciidoctor 1.5.8 is unnecessary. The series—removing dead code and replacing manual `gem install` with `apt-get install asciidoctor`—is now complete and uncontroversial, with no remaining technical blockers. Junio C Hamano’s earlier endorsement and the absence of further feedback suggest the patch is ready for merging.

Key details for follow-up: the patch touches `ci/install-dependencies.sh`, eliminates the `ALREADY_HAVE_ASCIIDOCTOR` variable (dead code from retired Azure Pipelines), and replaces the version-pinned `gem install` with a system package. The Ubuntu image’s default Asciidoctor version (2.0.20) is now used, matching GitHub Actions and user environments. The `concurrent-ruby` gem’s role remains undocumented in the thread but is orthogonal to this patch.