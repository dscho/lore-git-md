This is a documentation patch reporting a typo in the `git-merge` man page.

The email identifies what appears to be an error in an example diagram in the `git-merge(1)` man page. The example shows a commit history with branches `topic` and `master`, and claims that `ORIG_HEAD` is set to the tip of the current branch (`C`) before a merge operation. The author argues that `ORIG_HEAD` should instead point to `G` (the tip of `master`), citing both their own experiments and logical consistency with the surrounding explanation. The patch does not include a formal diff, but the correction is clear: the man page currently states `C` where it should say `G`.

The contribution is narrowly scoped to a single factual correction in documentation. The author provides a concise rationale and evidence (personal testing), though no formal test case or commit message is included. The patch is well-motivated and unlikely to be controversial, as it addresses an apparent inconsistency in the man page. The fix touches only the `Documentation/git-merge.txt` file, specifically the example section describing `ORIG_HEAD` behavior. Key names involved are `ORIG_HEAD`, `master`, and `topic` branches, and the commit labels `C` and `G`.