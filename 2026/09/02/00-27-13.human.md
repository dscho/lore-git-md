This is a **usability and design discussion** about Git’s handling of text and binary files, specifically a follow-up in the ongoing thread about silent corruption caused by misconfigured `.gitattributes` rules on Windows.

Skybuck Flying steps back from the immediate technical issues (textconv filters, global `.gitattributes`) to critique Git’s foundational assumption that "text files" follow any consistent standard. He argues that the lack of a true universal text format—with variations in line endings (LF, CR, CRLF), encodings (ASCII, ANSI, Unicode), and byte-order marks (BOMs)—makes Git’s text/binary distinction inherently flawed. His tone is speculative and somewhat dismissive of Git’s current approach, but the core observation is valid: Git’s heuristics for detecting text files (e.g., checking for NUL bytes or line endings) are brittle and can lead to silent corruption when misapplied.

The email proposes a radical alternative: treating *all* files as binary by default and designing a new "true text standard" with explicit length prefixes (to avoid NUL-termination issues) and fixed line-numbering schemes. While this is more of a thought experiment than a concrete proposal, it highlights the frustration of users who’ve encountered Git’s edge cases. Skybuck’s suggestion that such a format could simplify diffing, AI processing, or even historical change tracking is intriguing but unlikely to gain traction in the Git project, which prioritizes backward compatibility and incremental improvements.

This reply does not advance the thread’s technical discussion (e.g., no new reproduction steps, workarounds, or code fixes), but it **reframes the problem** as a design flaw rather than a configuration issue. Earlier emails in the thread focused on mitigations like safer `.gitattributes` rules or warnings for binary files subjected to text filters; Skybuck’s email questions whether Git should abandon the text/binary distinction entirely. This aligns with Randall S. Becker’s earlier critique of Git’s attribute system as "inherently risky," though Skybuck’s proposal is more extreme.

**Assessment**: This is a **low-priority contribution** to the thread. While the critique of Git’s text/binary assumptions is worth noting, the email offers no actionable path forward for the project. Maintainers are unlikely to engage with the speculative design ideas, but the underlying frustration—silent corruption from misconfigured filters—remains unresolved. The thread’s practical focus (warnings, documentation, or safeguards) is more likely to drive change than calls for a new file format. For readers, this email serves as a reminder of the complexity lurking beneath Git’s seemingly simple text-handling model.