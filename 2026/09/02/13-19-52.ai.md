**Summarizer brief mode**

This is a thread closure reply in the abandoned `[PATCH 0/3] format-patch: add --[no-]range-diff-notes` series. Kristoffer Haugsbakk clarifies the intended semantics of the proposed `--range-diff-notes` option after Junio C Hamano restated his usability concern in the closure email.

Kristoffer explains that the implementation already followed the alternative design Junio sketched: when both `--notes` and `--range-diff-notes` are present, `--notes` controls only the final patch output and `--range-diff-notes` controls only the range-diff material. The documentation’s phrasing was misleading—it described the behavior only for the edge case where the range-diff notes list is empty, obscuring the independent control that the implementation actually provided. The email does not propose reviving the topic or sending a v2; it merely corrects the record about what the code already did.

Key technical details carried forward: the implementation maintained two separate notes lists (one for patches, one for range-diffs) and used the `rdiff_override_notes` flag to toggle between them. The usability issue Junio raised—where `--no-notes --range-diff-notes` suppressed notes in both contexts—arose because `--no-notes` cleared the notes list that the range-diff fallback path still referenced, not because the design lacked independent control. The clarification does not resolve the conceptual flaw Junio identified, but it shows that the implementation was closer to the sketched alternative than the documentation suggested. No further action is taken; the thread remains closed.