This is a **platform compatibility and usability rant** from the original reporter, Skybuck Flying, in the ongoing thread about silent corruption in Git on Windows. The email is light on new technical content but heavy on frustration and philosophical critique of Git’s design.

Skybuck acknowledges the earlier advice from Randall S. Becker and D. Ben Knoble to use `* text=auto` instead of `* text` in `.gitattributes`, but immediately pivots to questioning whether this is truly safe or just another layer of complexity. The core of the email is a **diatribe against Git’s command-line and configuration syntax**, which he finds unintuitive and archaic. He complains about the use of `*` as a wildcard ("in MS-DOS everything is `*`"), the need to manually declare binary file types, and the lack of a pre-made list of known binary/text file extensions. His tone is combative and resigned, reflecting deep frustration with Git’s cross-platform text-handling quirks.

The email does not advance the technical discussion. There are no new reproduction steps, no proposed fixes, and no engagement with the earlier clarification that the `text` attribute (not the `diff` filter) is the root cause of binary corruption. Instead, Skybuck describes two AI-assisted programs he wrote to scan for corrupted repositories, confirming that PNGs in at least one repository were affected. He expresses relief that text files were not corrupted, framing this as a near-miss disaster, and announces he has deleted his `.gitattributes` file to avoid further risk.

The only constructive suggestion is a vague proposal for a simpler configuration system: a list-based approach where users declare file types (e.g., `PNG binary`, `TXT text`) without wildcards. However, this is presented as a demand ("I would demand a pre-made list") rather than a contribution, and it ignores the existing `text=auto` heuristic that already addresses much of this concern. The email ends with a rhetorical question about why `text=auto` does not solve the `^M` (carriage return) issue in `git diff` output, but provides no follow-up or willingness to explore the answer.

**Assessment**: This is not a technical contribution. It is a **user experience complaint** that underscores the real-world pain caused by Git’s attribute system, but it offers no actionable path forward. The frustration is understandable—silent corruption of binaries is a serious usability issue—but the email’s combative tone and lack of engagement with prior explanations (e.g., `text=auto` vs. `text`) make it unlikely to sway maintainers. Future discussion may focus on whether Git should add warnings for global `*` rules or improve documentation, but this email does not move the needle on those fronts. Readers should treat it as a **data point about user pain**, not as a source of technical insight or solutions.