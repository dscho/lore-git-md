Thread identity
Subject: [PATCH] builtin/history: guard against NULL tree in write_ondisk_index
Series version: standalone patch (v2)
Originating author: zkd18cjb@mail.ustc.edu.cn
Thread type: bugfix

Core problem or goal
Prevent a NULL-pointer dereference crash in `git history split` when the parent commit’s tree object is missing from the object store (e.g., due to repository corruption, object removal, or incomplete restore). The fix adds a guard in `write_ondisk_index()` to check the return value of `repo_parse_tree_indirect()` and error out gracefully.

Current status
Ready for integration; v2 addresses the two surface-level review nits (commit-message wording and Signed-off-by mismatch) raised by Patrick Steinhardt. Patrick’s reply explicitly states “this version looks good to me,” indicating the patch is likely to be queued for `next` in the next integration cycle.

Key technical details
- File touched: `builtin/history.c`
- New symbol: none (only a NULL check and error return added to `write_ondisk_index()`)
- Behavior change: crashes → graceful error message
- On-disk format: unchanged
- Test coverage: none included; reproduction steps provided in commit message

Open questions and loose ends
None; the patch is complete and addresses the crash as described. Patrick dismissed the need for a test, so no test is expected unless a future contributor volunteers one.

Participant positions
- Patrick Steinhardt: surface-level review (nitpicks on commit message wording and Signed-off-by mismatch); explicitly states the fix “looks obviously good” (v1) and “this version looks good to me” (v2). No substantive technical objections.