This is a CI/build system patch series follow-up, proposing a targeted update to GitLab CI configuration. Jeff King (Peff) sends a two-patch series to bump the Asciidoctor version tested in CI from 1.5.8 (2018) to a more recent release, addressing the forward-looking discussion from the earlier thread. The series builds on Patrick Steinhardt’s minimal fix (`ps/ci-depends-on-ruby`) that restored Ruby availability for `gem install asciidoctor` after a transitive dependency removal broke the documentation job.

The motivation is straightforward: the current CI pin (1.5.8) is outdated and no longer reflects what users are likely to have installed locally. Jeff notes that Asciidoctor 2.0.x has worked fine since 2019 (commit `f6461b82b9`), and testing an eight-year-old version provides little value. He also observes that installing Asciidoctor from Ubuntu’s system packages would pull in Ruby as a dependency, making Patrick’s explicit `ruby` installation redundant, but opts to keep the explicit dependency for clarity and robustness. The series does not remove the `concurrent-ruby` gem dependency introduced in 2024, though Jeff questions its necessity and cc’s Jean-Noël Avila for input.

The patches themselves are small and mechanical: one updates the version pin, the other adjusts the installation logic to match. The commit messages are clear and link back to the earlier discussion, making the rationale easy to follow. No controversy is evident; the series is a natural next step after the earlier thread acknowledged the version pin as outdated. The only open question is whether the `concurrent-ruby` gem is still needed, but that does not block this change.

This is a low-risk, high-value maintenance update. It modernizes CI testing without altering behavior, and the technical details are well-documented. The series is ready for review and likely to be merged once Patrick or Junio weigh in.