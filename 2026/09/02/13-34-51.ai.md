This is part 3 of Patrick Steinhardt’s 13-patch series removing ad-hoc ODB source linking, continuing the `the_repository` removal effort. The patch mechanically refactors the `submodule-config` subsystem to accept an explicit `struct repository *` parameter instead of implicitly using `the_repository`. All functions in `submodule-config.c` are updated, and callers in `builtin/fetch.c`, `builtin/grep.c`, `builtin/submodule--helper.c`, `submodule.c`, and `t/helper/test-submodule.c` are adjusted to pass `the_repository` (even when a more specific repository is available in context, preserving behavior during migration).

The changes are purely internal: no new functionality, no behavior changes, and no on-disk format impact. The patch touches seven files, converting 12 function signatures and updating 11 call sites. It is a straightforward, well-contained step in the broader ODB abstraction and `the_repository` removal effort, with no open questions or design concerns. The diff is clean and the commit message clearly explains the migration strategy. No reviews have been posted yet for this specific patch, but the series as a whole has Junio’s substantive approval on a prior patch.