Thread identity
Subject: [PATCH 0/6] parse-options: new sub-API for early argument scanning
Series version: v1 (initial submission)
Originating author: Christian Couder <christian.couder@gmail.com>
Thread type: patch series (bugfix and API refactoring)

Core problem or goal
Introduce a new `parse-options` sub-API (`early_scan_options()`) to fix a class of argument-parsing bugs where hand-rolled early scans (looking for `--` or special flags) fail to account for option values that appear as separate arguments. The new API performs a fast, limited scan of `argv` to locate specific long options, ignoring short options, abbreviations, and edge-case option types like `PARSE_OPT_OPTARG`. The series converts three commands (`git bisect`, `git rev-parse`, and `git fast-import`) to the new API, fixing concrete bugs in each:
- `git bisect start --term-good -- <not-a-rev>` previously mistook the term name `--` for the revision/path separator, rejecting `<not-a-rev>` as an invalid revision.
- `git rev-parse --default -- <not-a-rev>` made the same mistake, producing a confusing "bad revision" error.
- `git fast-import --depth 5 --allow-unsafe-features` silently ignored `--allow-unsafe-features`, refusing unsafe features from the input stream.

Current status
Under review (v1 posted). No prior versions. No merge status reported. CI passed cleanly.

Key technical details
- New functions: `early_scan_options()`, `early_scan_options_from_options()`
- New types: `struct early_scan_option`, `early_scan_fn`, `enum early_scan_flags`
- New macros: `EARLY_SCAN_SKIP_VALUE`, `EARLY_SCAN_WANT`, `EARLY_SCAN_WANT_VALUE`, `EARLY_SCAN_END`
- Files touched: `parse-options.h`, `parse-options.c`, `builtin/bisect.c`, `builtin/rev-parse.c`, `fast-import.c`, `t/helper/test-parse-options.c`, `t/helper/test-tool.{c,h}`, `t/t0040-parse-options.sh`
- Subsystems: argument parsing, command-line interface
- New test helper: `test-parse-options --early-scan-options` (exercises the new API)
- Test coverage: new tests for each bug in `t/t6030-bisect-porcelain.sh` (bisect), `t/t1502-rev-parse-disambiguate.sh` (rev-parse), and `t/t9300-fast-import.sh` (fast-import), plus dedicated API tests in `t/t0040-parse-options.sh`
- Behavior changes: fixes misparsing of `--` and special flags when option values appear as separate arguments; no changes to on-disk format or option semantics beyond the bugfixes

Open questions and loose ends
None raised yet. The series is self-contained and does not introduce new design questions or follow-up work.

Participant positions
- Christian Couder (author): well-motivated, clearly scoped contribution addressing a real problem without over-engineering. The new API is deliberately minimal, and the series confines itself to fixing the immediate bugs. The implementation is clean, idiomatic, and thoroughly tested. (High weight)

Related work
None mentioned. The series is standalone and does not depend on or conflict with other in-flight topics.