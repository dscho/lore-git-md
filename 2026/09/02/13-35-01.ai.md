This is the final patch in Patrick Steinhardt’s 13-patch series refactoring the object database (ODB) subsystem to remove ad-hoc source linking. The patch deletes the `odb_add_to_alternates_memory()` function from `odb.c` and its declaration from `odb.h`, completing the cleanup begun in earlier patches.

The removal is now safe because all callers have been updated to manage ODB sources explicitly (patches 11/13 and 12/13 for test helpers, patch 8/13 for `tmp-objdir`, and patches 5-7/13 for submodule handling). The series’ core goal—ensuring the ODB sources list contains only the primary source and its alternates—is now fully realized, simplifying the codebase and laying the groundwork for future performance improvements in alternate handling. This patch is purely mechanical, removing 14 lines of dead code with no behavioral changes.

The patch is uncontroversial and aligns with Junio C Hamano’s earlier substantive approval of the series’ direction. Its merge readiness depends on the correctness of the preceding patches, particularly the test helper updates and the enforcement of the alternate-only constraint in `git multi-pack-index` (patch 10/13). No new feedback or objections have surfaced in the thread.