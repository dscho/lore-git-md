This is a review reply in the `hooks.allowNoVerify` thread, now at v4. The author, brian m. carlson, echoes Junio’s rejection of the feature and adds their own perspective: they see `--no-verify` as a legitimate user choice, not something that should be administratively restricted. They argue that local hooks are inherently optional and that forcing their use via configuration is both ineffective (easy to bypass via `core.hooksPath`) and harmful to workflows that don’t need them. The tone is firm but not confrontational, framing the objection as a principled stance on user autonomy rather than a technical critique of the patch itself. This is a surface-level review—it engages with the *idea* of the feature but does not comment on the code, tests, or implementation details.