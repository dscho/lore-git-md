**Summarizer brief mode**

Junio C Hamano replies to the final exchange between Jeff King (Peff) and D. Ben Knoble in the `dk/use-nsec-runtime` series (v6, now in `next`), focusing on two technical points. First, he asks for a small final reroll to consolidate the `NO_NSEC` build-time differences by using `ST_XTIME_NSEC(st)` consistently, which would simplify the code by hiding the preprocessor logic behind a helper macro. Second, he reiterates a long-standing architectural concern: the new call to `repo_config_values()` in `is_racy_stat()` introduces a latent bug when operating on a repository that is not `the_repository`, as the function currently only works for the global repository. He suggests adopting a safe default (e.g., treating `core.useNanosec` as false) when `istate->repo != the_repository` as a stopgap, but does not block the series, which remains in `next` and is otherwise considered technically complete. The request for a reroll is procedural rather than substantive; the series is ready to graduate once the cosmetic change is made. Key files: `read-cache.c` (where `is_racy_stat()` lives), `environment.c` (config handling), and `statinfo.c` (where `ST_XTIME_NSEC` is defined). The config key `core.useNanosec` and the `NO_NSEC` build knob remain unchanged.