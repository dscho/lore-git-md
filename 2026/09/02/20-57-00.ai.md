**Summarizer brief mode**

This is a **performance and correctness discussion** in the ongoing thread about improving `git push` from shallow clones. The email is Elijah Newren’s detailed rebuttal to Derrick Stolee’s critique of his `push.shallowExcludeBoundary` patch, focusing on the **over-exclusion flaw** and the broader assumptions behind the patch.

Key technical details:
- **Subsystems/files**: `send-pack.c`, shallow graft handling, `push.negotiate` config, and connectivity checks in `receive-pack`.
- **New context**: Elijah clarifies that **corruption is not a risk** because `receive-pack` enforces connectivity checks (commit `52fed6e1ce07`) regardless of protocol (`file://`, HTTP, or SSH). The "multi-push" issue Stolee raised is thus a **false-positive failure** (rejecting more refs than necessary) rather than corruption.
- **Assumption matrix**: Elijah presents a 6-case matrix comparing the consequences of assuming the shallow graft exists on the server versus not. He argues that assuming the graft exists is **vastly superior** in most cases (faster success or failure) and that the edge case where the assumption is wrong (case C) is rare and recoverable.
- **Race condition**: Elijah adds a new failure mode for `push.negotiate=true`—a race between the client’s "have" check and server-side repacking, where the server’s QUICK lookup may miss the shallow graft, forcing the client to resend its entire history. This reinforces his argument that `push.negotiate` alone is insufficient.
- **Comical edge case**: Elijah describes a scenario where a `--filter=blob:none --sparse` shallow clone triggers a **double transfer** (download from origin, then upload back to the same server) due to the current inefficiency. His patch avoids this.

Advancements in the thread:
1. **Correctness clarified**: Elijah refutes Stolee’s corruption concern, narrowing the over-exclusion flaw to a **false-positive failure** (rejecting valid refs) rather than repository corruption.
2. **New failure mode**: The repacking race condition adds weight to Elijah’s claim that `push.negotiate` is not a complete solution.
3. **Assumption defense**: Elijah’s matrix and edge-case analysis strengthen his case for defaulting `push.shallowExcludeBoundary` to `true`, arguing the trade-offs favor performance and usability.
4. **Protocol neutrality**: The patch’s behavior is protocol-agnostic, relying on `receive-pack`’s connectivity checks rather than transport-specific safeguards.

Open questions:
- **Stolee’s response**: Will Stolee accept Elijah’s refutation of the corruption risk, or does he still consider the false-positive failure mode a blocker?
- **Default behavior**: Is the project comfortable with `push.shallowExcludeBoundary=true` as the default, given Elijah’s assumption matrix?
- **Recovery mechanism**: Elijah dismisses the need for automatic retries, but Stolee’s earlier suggestion (e.g., retrying without the config) remains unaddressed.

Likely next steps:
- Elijah may submit a **v3 patch** addressing minor feedback (e.g., Stolee’s test case) while keeping the core logic intact.
- The discussion may shift to **default config values** or **documentation** (e.g., warning users about edge cases).
- If Stolee remains unconvinced, the thread could pivot to his `push.negotiate`-based alternative.