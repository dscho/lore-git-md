This thread is about a two-patch bugfix series from Yannik Tausch that fixes a long-standing edge case in Git’s pathspec handling. The series addresses a memory-safety issue and a logic bug in `common_prefix_len()`, where an exclude pathspec (e.g., `:!unrelated/path`) appearing first in the list incorrectly caused the function to return zero as the common prefix length, even when subsequent positive pathspecs shared a directory.

The design is settled: Junio C Hamano’s output-parameter approach (`common_prefix_len(pathspec, &matched_prefix)`) was adopted to avoid introducing a new struct, and the series is split into two patches—one for the memory-safety fix and one for the core logic fix. Both patches include test coverage, and the discussion has converged. The only remaining open item is a minor implementation detail about the `const` qualifier on the output parameter, which doesn’t affect correctness.

Yannik’s latest email is a clarification request about an unexplained phone number (`940-842-9147`) that appeared in Darik P’s earlier reply. This is procedural noise and doesn’t engage with the technical content of the series. The thread is effectively ready for merging pending final review.