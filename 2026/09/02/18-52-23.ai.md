This is a **maintainer feedback reply** on Christian Couder’s `[PATCH 0/6] parse-options: new sub-API for early argument scanning` series. Junio’s response is **skeptical of the design direction** but not outright rejecting the series.

Junio opens with a terse “Yay” followed by a pointed “Sigh,” framing the new early-scan API as yet another ad-hoc prescan that behaves differently from the real `parse_options()` machinery. He questions whether the series is merely replacing one set of fragile hand-rolled scans with another, rather than addressing the root problem: the lack of a unified, reusable early-scan mechanism within `parse_options()` itself.

On the specific bug fixes, Junio is dismissive of the `git bisect` and `git rev-parse` cases, saying he fails to see “much practical value” in them. He is more receptive to the `git fast-import` fix—where `--allow-unsafe-features` was silently ignored—but still probes whether the root cause was simply that the ad-hoc prescan missed the value to `--depth`.

Junio then floats a long-term vision: a properly refactored `parse-options` machinery that can perform the prescan itself, perhaps via a “dry-run” mode. He suggests that the interim solution should at least reuse the existing `options[]` array so that future refactoring does not require redoing the conversion work. This is a clear hint that the current design, which introduces a separate `early_scan_option` structure, may be seen as technical debt.

Finally, Junio asks how this series interacts with another stalled topic of Christian’s (not identified in the email). The implication is that he would prefer not to have two non-trivial topics from the same author stalled simultaneously, and wants to know whether advancing this one would help unblock the other.

In summary: Junio’s feedback is **substantive and design-focused**, not a surface-level review. He is not rejecting the series outright, but his questions suggest that the current approach may not be the right long-term solution. The thread is now at a crossroads: Christian will likely need to either defend the current design as a pragmatic interim step or propose a more ambitious refactoring of `parse_options()` itself. Either way, the series is **not yet ready for `next`** and will require at least one more iteration.