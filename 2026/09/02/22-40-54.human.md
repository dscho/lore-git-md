This is the resolution of a correctness discussion about the third patch in Junio’s 8-patch series that refactors `git checkout` internals. The patch moves the validation of `--ours`, `--theirs`, `--force`, and `--merge` option compatibility from the monolithic `checkout_main()` into `checkout_paths()`, where it logically belongs.

The exchange confirms that the patch is correct: the check must be placed *after* the `opts->patch_mode` block to avoid incorrectly triggering in `--patch` mode. Karthik Nayak initially raised the concern, but after closer inspection, he agrees the change is safe because `--patch` mode already rejects incompatible options earlier in `checkout_paths()`. The only remaining action is to update the commit message to document the change in guard condition—from `opts->pathspec.nr` in the original code to unconditional execution in `checkout_paths()`—and explain why it is safe.

Junio’s closing comment is a candid aside: while the series provides a useful starting point for extracting reusable logic, it only scratches the surface of what would be needed for true programmatic use of `git restore` or `git switch` from C. The patches are not controversial, but their value as a foundation for future work may be limited. The series remains an early draft for discussion, not yet intended for merging.