Thread identity
Subject: [PATCH] rerere: fix race condition between rebase and background maintenance
Series version: two-patch series (v3 locking patch posted; sequencer config-override patch pending)
Originating author: Thomas Bachem
Thread type: bugfix patch

Core problem or goal
Fix a race condition in the `rerere` subsystem where background maintenance (`git rerere gc`)—triggered by `git gc`, auto-maintenance, or the geometric maintenance strategy—prunes entries in the shared `rr-cache` directory while a foreground operation (e.g., rebase) is recording conflicts. The race can cause the foreground operation to fail when the lock is held, though the lock itself is per-worktree and does not directly contend between worktrees. The solution remains a two-part series: (1) improve error handling in `rerere_setup()` with configurable locking timeouts and unified error reporting, and (2) disable background maintenance (`gc.auto` and `maintenance.auto`) during rebase via config overrides. The root cause is now framed as any background `rerere gc` interfering with foreground operations, not solely the geometric maintenance strategy’s aggressiveness, though the latter’s defaults remain a contributing factor. Patrick Steinhardt plans a separate patch series to adjust `maintenance.rerere-gc.auto` to consider garbage-collection eligibility.

Key technical details
- Files touched: `rerere.c` (error propagation, locking logic), `rerere.h` (new flags and config key), `builtin/rerere.c`, `builtin/am.c`, `builtin/rebase.c` (caller updates), `Documentation/config/rerere.adoc`, `Documentation/git-rerere.adoc`, `t/t4200-rerere.sh`, `t/t7900-maintenance.sh`.
- New behavior: `rerere_setup()` returns errors instead of dying, using `hold_lock_file_for_update_timeout()` with a configurable timeout (`rerere.lockTimeout`, default: 1000 milliseconds). Non-maintenance callers (e.g., `git commit`, `git merge`, `git rebase`) wait up to the timeout before warning and proceeding without rerere; background maintenance (`git rerere gc`) passes a timeout of 0 to skip the lock entirely (`RERERE_NOWAIT`). Explicit `git rerere`, `git rerere forget`, and `git rerere clear` commands now wait for the full timeout before failing, aligning with other callers. The `RERERE_SKIP_LOCKED` flag is replaced by `RERERE_NOWAIT` (timeout=0) and `RERERE_LOCK_OR_DIE` (fail immediately if lock is held). Error messages are hand-rolled to avoid misleading advice about terminating other Git processes. A `BUG()` assertion checks for incompatible flag combinations (e.g., `RERERE_NOWAIT` with `RERERE_LOCK_OR_DIE`).
- Config overrides: During rebase, both the merge and apply backends will pass `-c gc.auto=false -c maintenance.auto=false` to `git commit`, `git merge`, and exec commands. The merge backend will also call `run_auto_maintenance()` at the end of the rebase sequence to ensure consistency with the apply backend and avoid leaving maintenance disabled indefinitely.
- Error handling: If `rerere_setup()` fails due to a held lock, the rebase stops at the conflict (without rerere recording or replay) and allows `--continue` to proceed normally. No rescheduling of the pick is required.
- Locking changes: The 1-second wait is retained as preexisting behavior to avoid silent failures or crashes. The patch is based on `maint` and resolves a trivial conflict with the recent lockfile helper refactoring (`2e486bfbf7`).
- Test case: 20,000-entry `rr-cache` to trigger the race; focus on verifying error reporting is accurate, the timeout allows rebases to proceed after maintenance completes, and maintenance is disabled during rebase. Tests now also cover the behavior of `git rerere`, `git rerere forget`, and `git rerere clear` under a held lock.
- Architectural context: The geometric maintenance strategy’s aggressive triggering of `rerere gc` (after every commit with even a single `rr-cache` entry) exacerbates the race but is not the sole cause. The root issue is any background `rerere gc` interfering with foreground operations. Patrick’s planned patch series will adjust `maintenance.rerere-gc.auto` to consider garbage-collection eligibility, reducing the frequency of background maintenance.
- User experience: Background maintenance runs are daemonized, closing standard descriptors and redirecting output to `/dev/null`, so their warnings do not mix with foreground output. On Windows, where daemonization isn’t possible, maintenance runs in the foreground of the triggering commit, avoiding the race entirely. Junio C Hamano endorses Phillip Wood’s concern that background-task warnings mixing with foreground output is problematic, suggesting the patch may need adjustments to suppress or redirect such warnings. The only warning users see is from the foreground command (e.g., rebase) when it gives up waiting for the lock.

Current status
v3 of the locking patch posted and incorporates all review feedback from Junio, Phillip Wood, and Patrick Steinhardt. The locking changes (configurable timeout, `RERERE_NOWAIT`, `RERERE_LOCK_OR_DIE`, unified error reporting, `BUG()` assertions) are now settled. The patch is based on `maint`, resolves a trivial conflict with `2e486bfbf7`, and is ready for final review. The sequencer config-override patch (disabling maintenance during rebase) is still pending; Thomas Bachem plans to post it after addressing Phillip’s feedback on consistency between backends. The series is under review, with no objections to the revised approach.

Open questions and loose ends
1. Should rebases using the merge backend run maintenance at all? (Deferred to future discussion; no new input.)
2. Is the fork-based background maintenance design inherently flawed for interactive operations? (Phillip Wood’s critique remains unresolved; Patrick’s review does not address this; Junio’s review aligns with Phillip’s usability concerns.)
3. Will Junio weigh in on the revised approach, particularly the locking changes and geometric maintenance strategy adjustments? (Junio’s review in this update focuses on error handling and usability, not the broader approach.)
4. How will Patrick’s proposed changes to `maintenance.rerere-gc.auto` interact with the rebase-specific config overrides? (No overlap is expected, but coordination may be needed.)
5. Is the 1-second wait sufficient for all use cases, or should it be configurable? (Configurable via `rerere.lockTimeout`; no new input on default value.)
6. Should there be a single maintenance run at the end of the rebase sequence? (Phillip Wood insists on consistency between backends; this will likely be included in the sequencer patch.)
7. Phillip Wood reiterates that garbage collection is useless mid-rebase—all objects and `rr-cache` entries created during the rebase are too fresh to be collected. This position remains unresolved.
8. Why do `git rerere`, `git rerere forget`, and `git rerere clear` fail immediately instead of waiting? (Thomas clarified they now wait for the full timeout before failing, aligning with other callers; Junio’s review reinforces that these commands should fail loudly after waiting, as they are explicit user actions.)
9. Should background-task warnings (e.g., from `rerere gc`) be suppressed or routed to a log file to avoid mixing with foreground output? (Junio endorses Phillip’s concern; this remains unresolved and may require patch adjustments.)
10. Documentation clarity: Junio notes that the current documentation (`git rerere gc does not retry`) is misleading, as all commands respect the timeout but `gc` is the only one that proceeds without error. This may need clarification.

Participant positions
- **Thomas Bachem**: Incorporated all review feedback into v3 of the locking patch. The patch now includes a configurable timeout (`rerere.lockTimeout`, default 1000 ms), `BUG()` assertions for incompatible flags, and clarified documentation. Clarified that the race involves the shared `rr-cache` directory, not the per-worktree `MERGE_RR` lock, and that background maintenance runs are daemonized to avoid mixing output with foreground commands. Plans to implement config overrides to disable maintenance during rebase in both the merge and apply backends, with the merge backend also calling `run_auto_maintenance()` at the end of the rebase sequence. Acknowledges the geometric maintenance strategy’s aggressiveness as a contributing factor but treats it as orthogonal to the rebase-specific fix.
- **Phillip Wood**: Substantive review; initially opposed the timeout-based fix as papering over the root cause, advocating for disabling background maintenance during rebase via config overrides. Now accepts the timeout as a pragmatic stopgap while deeper fixes are implemented. Insists on consistency between the merge and apply backends, requiring the merge backend to call `run_auto_maintenance()` at the end of the rebase sequence. Reiterates that garbage collection is useless mid-rebase and proposes disabling auto-maintenance during rebase as a trial. His critique of the fork-based background maintenance design remains unresolved. Questions about the immediate failure of `git rerere` commands and usability concerns about background-task warnings mixing with foreground output have been partially addressed by Thomas’s clarification, but Junio’s review aligns with Phillip’s usability concerns.
- **Patrick Steinhardt**: Substantive review; identifies the geometric maintenance strategy’s aggressiveness as a contributing factor and proposes adjusting `maintenance.rerere-gc.auto` to consider garbage-collection eligibility. Critiqued the locking implementation, suggesting configurable timeout and unified error reporting. His proposed locking improvements are now incorporated into v3. Endorses the 1-second wait for non-maintenance callers as a preexisting behavior that users would prefer over crashes or silent skips. Plans a separate patch series for the maintenance strategy changes.
- **Junio C Hamano**: Provided substantive review of v2, now incorporated into v3. Clarifies the intent of the timeout mechanism: after waiting, commands should fail rather than proceed, because a held lock signals that another process is actively modifying the `rr-cache` and expects exclusive access. Justifies the distinction between `git rerere gc` (which can silently skip) and `git rerere forget`/`git rerere clear` (which should fail loudly) by their user-visible semantics—GC is a best-effort cleanup, while `forget`/`clear` are explicit user commands that should not silently ignore failure. Endorses Phillip’s concern about background-task warnings mixing with foreground output, calling it "a very good point." His input suggests the patch may need adjustments to error handling, documentation clarity, and suppression of background-task warnings.

Related work
- The race was introduced by the geometric maintenance strategy in Git 2.54, which triggers `rerere gc` after every commit with even a single `rr-cache` entry.
- Similar lock contention issues have arisen elsewhere due to the fork-based background maintenance design.
- Patrick’s planned patch series will address the geometric maintenance strategy’s defaults, which may reduce the need for rebase-specific workarounds.
- Johannes Schindelin fixed a related bug in background maintenance, reinforcing Phillip Wood’s argument that garbage collection is useless mid-rebase.