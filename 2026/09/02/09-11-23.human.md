This is a review reply to the second patch in Jeff King’s two-patch bugfix series that plugs a memory leak in the submodule error path. The series itself is uncontroversial and well-motivated: the leak was flagged by Coverity and the fix is straightforward, adding a missing `free()` call and a test to verify it.

Patrick Steinhardt’s review here is **surface-level**. He agrees with the approach and offers a single nit: initializing the `ret` variable to 0 at declaration rather than setting it later, to avoid potential compiler warnings about uninitialized use. He also notes that the test’s use of `.gitmodules` (rather than `.git/config`) justifies the `test_when_finished` cleanup pattern. Neither comment questions the substance of the patch or suggests deeper changes. The tone is supportive and the feedback is minor, so this reply does not raise any red flags or open new discussion points. The patch remains on track for merging once the nit is addressed or ignored.