This is a **review discussion** on Christian Couder’s `[PATCH 0/6]` series introducing a new `parse-options` sub-API for early argument scanning. Junio C Hamano’s reply expresses skepticism about the approach, questioning whether the series replaces one set of ad-hoc parsers with another rather than addressing the root problem.

Junio’s main concern is that the new `early_scan_options()` API is still an ad-hoc solution—albeit a more consistent one—rather than a proper refactor of `parse_options()` to handle early scanning natively. He suggests that a long-term fix should involve extending `parse_options()` itself (perhaps with a "dry-run" mode) to avoid maintaining parallel parsing logic. He also notes that the series does not reuse the existing `struct option` arrays for the early scan, which could complicate future refactoring.

On the specific bugfixes, Junio is dismissive of the `git bisect` and `git rev-parse` cases, calling their practical value limited. He is more receptive to the `git fast-import` fix, where the early scan failed to recognize `--allow-unsafe-features` because it didn’t account for `--depth 5` consuming the next argument. He asks whether the series interacts with another stalled topic from Christian, hinting at a preference to avoid overlapping work.

The tone is **cautious and critical**, but not outright rejecting. Junio’s feedback suggests the series may need rethinking—either by aligning it more closely with a future `parse_options()` refactor or by demonstrating clearer benefits for the proposed fixes. The discussion is now open for Christian (or others) to address the design concerns or justify the current approach. For now, the series remains **under review with unresolved questions**.