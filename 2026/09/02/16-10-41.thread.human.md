This thread introduces a new `parse-options` sub-API called `early_scan_options()` to fix a class of argument-parsing bugs where early scans for special flags (like `--`) or options (like `--allow-unsafe-features`) fail when option values appear as separate arguments. The series, authored by Christian Couder, is now complete with the sixth patch converting `git fast-import` to use the new API, fixing a bug where `--allow-unsafe-features` was silently ignored in certain cases.

The new API is minimal and focused, addressing the immediate problem without over-engineering. It has been applied to three commands—`git bisect`, `git rev-parse`, and now `git fast-import`—each of which had concrete bugs that are now resolved. The implementation is clean, idiomatic, and thoroughly tested, with new tests covering the fixed behavior in each command. The series also includes a small refactoring to avoid code duplication, making the API easier to use and maintain.

No open questions or objections have been raised so far, and the series has passed CI cleanly. The changes are confined to the command-line interface and do not affect on-disk formats or option semantics beyond the bugfixes. If the current pace of review holds, this series looks poised for integration in the near future.