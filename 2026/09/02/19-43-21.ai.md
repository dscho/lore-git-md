This is a **platform compatibility and usability discussion** continuing the thread about silent corruption in Git on Windows due to misconfigured `.gitattributes` rules. The author, Skybuck Flying, responds to D. Ben Knoble’s clarification about the `text` attribute’s role in binary corruption, but the email shifts from technical analysis to **frustration with Git’s design and syntax**, offering little new actionable insight.

Skybuck acknowledges Knoble’s recommendation (`* text=auto`) as a safer alternative to `* text` but raises three concerns: **how to implement it** (without clear guidance), **whether it is truly safe** (doubting Git’s auto-detection heuristic), and **the cognitive load of Git’s syntax** (criticizing the use of `*` as unintuitive and conflating it with MS-DOS wildcards). The tone is combative and dismissive, framing Git’s attribute system as archaic ("obscure old git manuals from the 70's") and its syntax as nonsensical ("bizar/strange/alien"). While the frustration is understandable—misconfigured `.gitattributes` can silently corrupt binaries—the email does not propose solutions or engage with the technical trade-offs (e.g., why `*` is used for pattern matching or how `text=auto` works).

The author briefly describes **workarounds they’ve adopted** (deleting `.gitattributes`, scanning repositories for corruption) and hints at a **preference for explicit file-type lists** (e.g., `PNG binary`) over pattern matching, but this is presented as a vague demand rather than a concrete proposal. The email also **reiterates the severity of the issue** (corrupted PNGs in "Doc folders") and expresses relief that text files were not affected, but this adds no new technical detail to the thread. Notably, Skybuck **disengages from further experimentation**, citing time constraints and the perceived danger of Git’s configuration system.

**Assessment**: This is **not a technical contribution** but a **user experience critique**. The email underscores the real-world impact of Git’s attribute system—silent corruption of binaries due to misconfiguration—but offers no path forward beyond deleting `.gitattributes`. The frustration is valid, but the combative tone and lack of engagement with prior recommendations (e.g., `text=auto`, explicit binary declarations) make it unlikely to advance the discussion. Future replies may need to **bridge the gap between usability and Git’s design philosophy**, perhaps by proposing clearer documentation, built-in safeguards (e.g., warnings for `*` rules), or tooling to audit `.gitattributes` files. For now, the thread remains stalled on **whether Git should mitigate user misconfigurations or defer to education**.