Thread identity
Subject: [PATCH] hooks: introduce hooks.allowNoVerify to control --no-verify bypass
Series version: v1 (initial submission)
Originating author: Alessio Attilio (via GitGitGadget)
Thread type: patch series

Core problem or goal
Introduce a new configuration option `hooks.allowNoVerify` that allows administrators to prevent accidental or intentional bypass of client-side hooks via the `--no-verify` (`-n`) flag in `git commit`, `git push`, `git merge`, and `git rebase`. The setting is opt-in (defaults to `true` for backward compatibility) and is intended for managed environments or CI workflows where hook execution must be enforced.

Current status
Under review; no prior versions. No maintainer decision or merge status recorded.

Key technical details
- New config key: `hooks.allowNoVerify` (boolean, defaults to `true`)
- Commands affected: `commit`, `push`, `merge`, `rebase`
- Files touched: `builtin/am.c`, `builtin/commit.c`, `builtin/merge.c`, `builtin/push.c`, `builtin/rebase.c`, `Documentation/config/hooks.adoc`
- New test files: multiple tests in `t/` covering CLI overrides, config precedence, and hook failure scenarios
- No on-disk format changes; purely a client-side workflow guardrail

Open questions and loose ends
None raised yet; the patch is self-contained and includes documentation and tests.

Participant positions
None recorded yet; initial submission only.

Related work
None mentioned.