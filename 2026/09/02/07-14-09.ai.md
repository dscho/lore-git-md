This is the first patch in Jeff King’s two-patch CI/build system series (`peff/ci-asciidoctor-bump`). It removes the unused `ALREADY_HAVE_ASCIIDOCTOR` variable and its associated conditional from `ci/install-dependencies.sh`. The variable was a relic from the now-retired Azure Pipelines CI environment, which was removed in 2020. Since then, the conditional has had no effect, so this patch is a straightforward cleanup with no functional impact.

The change is minimal and well-justified: the commit message clearly explains the historical context and why the code is now dead. The patch touches only one line in a single file, making it a low-risk, high-confidence removal. No objections or edge cases are evident; the patch is uncontroversial and ready for merging as part of the series.

This patch sets the stage for the second patch, which updates the Asciidoctor version pin. By removing the unused conditional first, the series keeps the version bump isolated to the actual installation logic, making the overall change easier to review and verify. The series as a whole remains a targeted, forward-looking update to GitLab CI configuration.