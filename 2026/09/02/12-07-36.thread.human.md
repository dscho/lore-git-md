This thread is about a simple but important bugfix for the `git history` command. The patch prevents a crash when `git history split` encounters a missing tree object, replacing a NULL-pointer dereference with a clean error message. The fix is minimal—just a NULL check in `builtin/history.c`—and uncontroversial.

The author, zkd18cjb@mail.ustc.edu.cn, sent the patch initially, and Patrick Steinhardt reviewed it, calling it "obviously good" but asking for minor commit-message tweaks. The second version addressed those nits (removing UBSan details and fixing a Signed-off-by mismatch), and Patrick has now explicitly marked it as ready. No further discussion or test requests are pending, so the patch is likely to be queued for `next` soon. There’s nothing left to resolve—this one is effectively done.