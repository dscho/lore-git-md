Thread identity: standalone bugfix patch addressing a buffer overflow in `match_pathspec_with_flags()` when handling negative pathspecs (marked with `PATHSPEC_EXCLUDE`). Author: Yannik Tausch (first-time contributor). Thread type: bug report and fix.

Core problem or goal: fix a buffer overflow in `match_pathspec_with_flags()` that occurs when a negative pathspec is shorter than the common prefix derived from positive pathspecs. The bug causes `match_pathspec_item()` to read out-of-bounds memory, leading to crashes or undefined behavior. The fix ensures negative pathspecs are matched without advancing beyond their allocation by passing a zero prefix.

Current status: patch posted, under review. No prior versions or integration status reported.

Key technical details:
- Files touched: `dir.c` (core logic), `t/t6132-pathspec-exclude.sh` (regression test).
- Subsystem: pathspec-matching.
- New behavior: negative pathspecs are matched with a zero prefix to prevent out-of-bounds reads.
- Test coverage: regression test reproduces the issue using `git ls-files` with a long positive pathspec and a short negative pathspec, verifying no crash occurs.

Open questions and loose ends: none identified. The patch is minimal, well-motivated, and includes a thorough test. No objections or requests for changes have been raised yet.

Participant positions: none recorded yet. The author has not received any reviews or feedback on the patch.

Related work: none mentioned. The issue was previously reported to the security mailing list but deemed non-embargoed. The fix is standalone and does not depend on other in-flight topics.