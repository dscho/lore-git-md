This is the final reply in the `peff/ci-asciidoctor-bump` series, a CI/build system patch targeting GitLab CI’s documentation job. Jeff King responds to Patrick Steinhardt’s two tangential concerns—forward compatibility with older Asciidoctor versions and the unexplained `concurrent-ruby` gem dependency—without derailing the patch’s readiness for merging.

On forward compatibility, Jeff dismisses the need to test Asciidoctor 1.5.8, noting its age and the fact that the next version (2.0.0) was the only one with significant breaking changes. He suggests that bug reports from users on outdated versions can be handled reactively, avoiding the complexity of testing multiple versions in CI. This closes the compatibility discussion without requiring changes to the patch.

For the `concurrent-ruby` gem, Jeff points Patrick to his cover letter, implying the explanation is already documented there. The gem’s necessity remains unresolved in this email, but the patch itself is unaffected—it simply retains the existing installation logic. The loose end persists, but it’s orthogonal to the patch’s goal of modernizing Asciidoctor installation.

The patch remains a straightforward, low-risk update: it replaces the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`, using the Ubuntu image’s default version (2.0.20). This aligns CI with user environments, eliminates an outdated version pin, and simplifies maintenance. Junio’s earlier endorsement and the lack of technical objections suggest it’s ready for merging. The only open question—the `concurrent-ruby` gem—doesn’t block this change but may warrant follow-up if its purpose remains unclear.