This is a review reply in the ongoing thread about fixing a memory leak in Git’s submodule error path. The discussion has expanded into a deeper architectural conversation about the `parsed_object_pool` API and its relationship to the repository structure.

Patrick Steinhardt responds to Jeff King’s exploratory patch and technical notes on the `parsed_object_pool`’s redundant `repo` pointer. He agrees in principle that the pool should be more self-contained and not depend on the repository, aligning with the broader goal of reducing global state and making subsystems more modular. Patrick’s earlier attempt to refactor the pool as an implementation detail of the object database grew too large, but he still sees value in the direction Peff’s patch illustrates—even if the immediate churn isn’t justified.

The exchange clarifies that while the current two-patch series (fixing the leak and making `repo_clear()` idempotent) remains uncontroversial and ready for review, the deeper API cleanup is acknowledged as a worthwhile long-term goal. Patrick’s position is that if such a refactoring is pursued, it should be done thoroughly—either fully replacing the repository-based interface or not at all—rather than introducing a hybrid API that offers little practical benefit. His comments reinforce the architectural principle that subsystems should receive only the state they actually need, a theme consistent with ongoing efforts like `the_repository` removal.

No new patches or objections emerge from this reply; it’s a conceptual endorsement of the cleanup direction, but one that remains out of scope for the current series. The thread’s focus stays on the immediate bugfix, with this architectural discussion serving as background for future work.