This thread began as a simple two-patch fix from Jeff King (Peff) to plug a memory leak in Git’s submodule error path, where a URL string allocated during repository setup wasn’t freed when initialization failed. The first patch made `repo_clear()` idempotent as defensive groundwork, and the second added the missing `free()` call along with a test to verify the fix. Both patches were straightforward and uncontroversial, with no reviews or objections yet.

Peff has since followed up with a deeper exploration of a related cleanup: the asymmetry in the `parsed_object_pool` API, where objects are allocated via `new` but lack a corresponding `free`. His investigation revealed significant structural challenges—circular header dependencies between `repository.h`, `object.h`, and `hash.h`—that make this cleanup far more complex than initially anticipated. Two potential solutions (splitting `the_repository` into a forward-declaration header or decoupling `the_hash_algo` from `the_repository`) both introduce hidden dependencies and require widespread changes. Peff concluded that the effort isn’t justified for the current bugfix series, but his notes serve as a useful reference for future refactoring work. The discussion remains exploratory, with no patches proposed or objections raised. The original leak fix is still under review, awaiting feedback.