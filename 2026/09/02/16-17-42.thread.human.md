This thread introduces a new configuration option, `hooks.allowNoVerify`, designed to prevent accidental bypass of client-side hooks via the `--no-verify` (`-n`) flag in commands like `git commit`, `git push`, `git merge`, `git rebase`, and `git am`. The feature is opt-in, defaulting to `allow` for backward compatibility, and is aimed at managed environments or CI workflows where hook execution must be enforced. The latest version, v4, addresses a minor oversight in v3 by adding test coverage for `git am --no-verify`, while keeping the core design intact.

The patch is self-contained, touching five built-in commands and introducing new validation logic in `hook.c`. It includes comprehensive tests and documentation, and provides emergency overrides via an environment variable or CLI flag. The commit message clarifies that this is a workflow guardrail, not a security boundary. So far, the patch has not attracted review feedback, but it is well-prepared for discussion, with all prior feedback from earlier versions addressed. The series remains under review, with no maintainer decision recorded.