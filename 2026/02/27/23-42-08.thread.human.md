This thread presents a 5-patch series refactoring Git's oidmap cleanup API to replace the implicit `oidmap_clear(map, 1)` pattern with explicit callback-based memory management via `oidmap_clear_with_free()`. The series systematically converts four key subsystems (rev-list, list-objects-filter, odb, and sequencer) to use type-safe cleanup functions rather than relying on a boolean flag that required internal knowledge of entry layouts.

The final patch completes the work by updating the sequencer's string_entry handling, introducing a dedicated `free_string_entry()` callback and converting two cleanup sites in `make_script_with_merges()`. Like the previous conversions, this maintains identical behavior while making memory management explicit and self-documenting. The series as a whole demonstrates a consistent approach across subsystems, with each patch (typically 8-11 line diffs) focusing on a single logical change while building toward the complete API transition.

The changes improve code organization and API contracts without altering functionality, aligning with ongoing efforts to enhance Git's type safety and resource management. Test coverage comes from existing subsystem tests and new unit tests for the core API changes. With all five patches now submitted, the series appears ready for maintainer consideration, having cleanly addressed all oidmap cleanup sites while maintaining the project's high standards for focused, well-documented changes.