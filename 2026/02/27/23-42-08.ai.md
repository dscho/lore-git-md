This is a refactoring patch series that improves the oidmap cleanup API. The series introduces a new `oidmap_clear_with_free()` function to replace the boolean-based `oidmap_clear(map, 1)` pattern, making memory cleanup more explicit and type-safe.

The key change is moving from an implicit `free()` assumption in the old API to requiring explicit free callbacks when clearing oidmaps. This is particularly important when oidmap entries are embedded in larger structures, where a simple `free()` would be incorrect. The series updates all existing call sites across several subsystems (rev-list, list-objects-filter, odb, and sequencer) to use the new API.

The patch series appears well-structured, with each commit focusing on a specific subsystem. The changes are mechanical but meaningful - they don't alter behavior but make the code more maintainable by removing hidden assumptions about memory management. The series includes test updates in `t/unit-tests/u-oidmap.c` to verify the new functionality.

This is part of ongoing efforts to improve Git's internal APIs for better type safety and explicit resource management. The changes touch core data structures but are unlikely to be controversial as they maintain existing behavior while providing clearer interface. The series is complete with all call sites converted and tests updated.