This is the fourth patch in a series refactoring Git's oidmap cleanup API, focusing on the object database (odb) subsystem. The patch converts the replace_map cleanup in odb.c to use the new `oidmap_clear_with_free()` API introduced earlier in the series, replacing the old `oidmap_clear(map, 1)` pattern.

The change introduces a dedicated `free_replace_map_entry()` callback that properly handles freeing of `struct replace_object` entries via `container_of` to access the containing structure. This matches the pattern established in previous patches for rev-list and list-objects-filter subsystems. The patch maintains the existing behavior while making memory management more explicit and type-safe.

Key technical details: the callback uses `container_of` to get from the oidmap entry pointer to the full `replace_object` structure before freeing it, and the cleanup is guarded by the existing `replace_map_initialized` flag. The change touches core object database management but is well-contained (11-line diff) and follows the series' consistent approach to API modernization.

This continues the series' high-quality implementation with clear commit messages and careful attention to maintaining existing behavior while improving internal APIs. The patch demonstrates the same attention to detail seen in earlier parts of the series, with proper structure access and cleanup centralization. Remaining work appears to be just the sequencer conversion in the final patch.