This is a cleanup patch addressing a duplicate option definition in `git pack-objects`. It's part of the ongoing discussion about duplicate option handling that Junio Hamano raised.

René Scharfe removes a redundant `--stdin-packs` option definition in `builtin/pack-objects.c` that was left behind when the option was enhanced to accept an argument in a previous commit. The old definition was shadowed by the new one but still appeared in the help text, which could confuse developers. The change is straightforward and clearly motivated by Junio's earlier thread about silent duplicate option acceptance.

The patch is small (just 2 lines removed) and uncontroversial - it fixes an obvious oversight rather than changing behavior. René's work is typically this sort of precise, mechanical cleanup. Since it touches the same area as Junio's duplicate options thread but actually fixes a real case rather than just discussing the problem, it seems likely to be picked up quickly.