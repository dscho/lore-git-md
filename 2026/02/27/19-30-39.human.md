This is a refactoring patch in Eslam Reda Ragheb's repository metrics series, which has expanded from its original path-reporting focus. The patch simplifies the key-value output formatting code in `builtin/repo.c` by introducing two helper functions: `print_keyvalue_size()` for single metrics and `print_object_values()` for per-object-type metrics.

The changes reduce code duplication while maintaining identical output format, making the 156-line function more maintainable. The patch demonstrates good attention to detail - it preserves all existing metrics (reference counts, object sizes, and topology measurements) while making the code structure clearer.

This is part of a larger effort that now includes both path reporting and repository metrics functionality, though reviewers may still debate whether these should remain combined. The patch itself is straightforward and unlikely to be controversial, being purely an internal refactoring with no behavior change.