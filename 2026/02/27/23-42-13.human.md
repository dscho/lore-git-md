This is the final patch in a 5/5 series refactoring Git's oidmap cleanup API. The series replaces the old `oidmap_clear(map, 1)` pattern with a new `oidmap_clear_with_free()` function that takes an explicit callback for freeing entries.

The patch converts the sequencer subsystem to use this new approach, introducing a `free_string_entry()` helper to properly clean up `struct string_entry` objects in two oidmaps (`commit2todo` and `commit2label`). The change maintains the same memory management semantics but makes them explicit and type-safe through the callback mechanism.

This completes the series' migration of all oidmap cleanup sites in the codebase to the new API. The change is straightforward - just 8 lines added and 2 changed - and follows the same pattern established in previous patches. The commit message clearly explains the motivation (aligning with the API migration) and the specific change being made.

The series as a whole improves code clarity and type safety around memory management in oidmap usage, and this final patch brings the sequencer subsystem into alignment with that effort. The changes are well-contained and unlikely to be controversial given the careful progression through the series.