This is a documentation and build system refinement patch in the ongoing fsmonitor Unix platform support series. Paul Tarjan responds to Junio Hamano's feedback about the IPC path logic consolidation between macOS and Linux implementations.

The key change clarifies that the patch actually renames the existing macOS (darwin) implementation to "unix" since the logic applies broadly to Unix-like systems, rather than merging two separate implementations. The build system now uses `FSMONITOR_OS_SETTINGS=unix` for both macOS and Linux platforms, with `FSMONITOR_DAEMON_BACKEND` handling platform-specific components. This addresses Junio's observation about the original description not matching the code changes.

The patch maintains all existing functionality while simplifying the build configuration by eliminating conditional blocks. Paul notes he has updated both commit messages to accurately reflect this renaming approach rather than the initial "merging" characterization. The changes appear to be a documentation and commit message refinement rather than a functional modification to the IPC logic itself.