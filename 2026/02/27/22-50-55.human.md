This is a review comment on a bugfix patch series about Git's option parsing validation. Jeff King (Peff) is responding to René Scharfe's patch that adds runtime checks for duplicate long option names in command option tables.

Peff agrees with René's assessment that the performance impact of the new validation is likely negligible, but suggests that if optimization were needed, a runtime toggle would be preferable to a compile-time DEVELOPER-mode flag. He notes a minor optimization opportunity in the implementation - using the return value from `strset_add()` to check for duplicates rather than making a separate `strset_contains()` call - while acknowledging that memory allocation overhead is probably the dominant cost anyway.

The exchange shows consensus on the technical approach while leaving the door open for future optimization if needed. Peff's review reinforces that this is a solid solution to the problem Junio originally identified, with only minor implementation details worth considering. The thread appears to be converging toward merging this validation improvement.