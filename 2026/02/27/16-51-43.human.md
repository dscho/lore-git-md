Test modernization patch improving error detection in t3700-add.sh.

Siddharth Shrimali sends a straightforward patch to improve the test suite's error detection in t3700-add.sh. The change modifies several test cases that were piping `git ls-files` output directly into `grep`, which suppresses `git ls-files`'s exit code. The new version first redirects output to a file, then runs `grep` on that file, ensuring any crash in `git ls-files` would be caught.

The patch touches 15 test cases, consistently applying the same pattern change to each one. This appears to be a mechanical but worthwhile improvement to test reliability, though the commit message could be more specific about why these particular tests were chosen for the change (they all involve checking `git ls-files` output).

The changes are simple and obviously correct, following established test suite patterns. This seems like a low-risk improvement that makes the tests more robust by ensuring they'll catch failures in the `ls-files` command itself, not just mismatched grep patterns.