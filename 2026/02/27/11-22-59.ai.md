This is a performance optimization patch series from Patrick Steinhardt targeting lock contention in `git-upload-pack` during high-concurrency scenarios. The series addresses an issue that becomes visible when hundreds of upload-pack processes are simultaneously writing data to pipes at multi-gigabit speeds, as evidenced by a flame graph showing lock contention.

The first patch fixes a debug statement that was incorrectly placed, while the second patch (the main focus) reduces lock contention when writing packfile data. The changes are relatively small (11 lines modified across upload-pack.c) but target a specific performance bottleneck in server-side operations.

The patch appears well-motivated with clear evidence-based justification (referencing a flame graph analysis) and comes from a contributor with deep expertise in Git's server-side performance characteristics. Given Patrick's track record with similar optimizations and the focused nature of these changes, this seems like a straightforward improvement worth considering for merging.