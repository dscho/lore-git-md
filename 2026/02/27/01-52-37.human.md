This is a refactoring patch that's part of a larger series (v3 2/4) implementing configurable cover letter formatting for `git format-patch`. The patch moves the shortlog summary generation code from `make_cover_letter()` into a new dedicated function `generate_shortlog_cover_letter()`.

The change is purely structural - it extracts existing functionality without modifying behavior, preparing for subsequent patches that will introduce customizable formatting. The new function maintains all the same shortlog configuration (wrapping, indentation, author grouping) that was previously inline.

This appears to be a straightforward and well-contained refactoring that follows the project's preference for breaking changes into logical steps. The commit message clearly explains the motivation (preparation for format string support) and the patch itself is mechanically simple, just relocating code while preserving all functionality. As part of a larger series that's already received maintainer feedback, this change seems uncontroversial and likely to be merged.