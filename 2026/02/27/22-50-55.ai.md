This is a review comment on René Scharfe's patch to detect duplicate long options in Git's option parser. Jeff King (Peff) is responding to the performance considerations raised in the patch.

Peff agrees the runtime cost of the new validation is likely negligible, suggesting that if optimization were needed, a runtime toggle would be preferable to a DEVELOPER-mode-only approach. He notes this would allow enabling the checks in test scripts while keeping them off for regular use.

The technical discussion focuses on a micro-optimization opportunity in the implementation - using `strset_add()`'s return value to avoid redundant checks, though Peff acknowledges the string allocation overhead would likely dominate. The patch itself adds comprehensive validation for duplicate long option names and numerical options in `parse_options_check()`, building on René's earlier fix for a concrete `pack-objects` case.

This exchange shows the patch is technically sound but invites consideration of deployment strategy (always-on vs conditional) and minor implementation refinements. The thread remains focused on refining the solution rather than debating its merit, with both contributors aligned on the value of the validation.