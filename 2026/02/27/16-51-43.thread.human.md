Siddharth Shrimali's patch series to improve exit code handling in t/t3700-add.sh has progressed through two rounds of review. The initial version converted raw `grep` usage to the `test_grep` helper to properly catch failures, addressing a common test suite issue where exit codes were being suppressed. Junio Hamano reviewed v1 and suggested several improvements around test helper usage and edge case handling, all of which were incorporated into v2.

Now Johannes Sixt (Hannes) has identified two remaining issues in v2. First, while the patch fixed many exit code problems, it still uses a subshell pattern that could mask failures when checking file modes. Second, it accidentally removed the newline at end of file. These are subtle but important shell scripting details - particularly the subshell issue, as it undermines the patch's core goal of proper exit code handling. Hannes' review suggests the test modernization needs one more iteration before it's fully baked.

The thread shows the project's careful attention to test reliability, with multiple experienced reviewers ensuring even seemingly small changes don't introduce hidden problems. Siddharth will likely send a v3 addressing Hannes' feedback to complete this improvement to the add command's test coverage.