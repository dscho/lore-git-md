Refactoring patch converting list-objects-filter to use the new oidmap cleanup API. This is the third in a 5-part series replacing implicit memory cleanup (via boolean flag) with explicit callback-based cleanup through the new `oidmap_clear_with_free()` function introduced earlier in the series.

The patch updates the list-objects-filter subsystem to handle cleanup of `seen_map_entry` structures stored in the `seen_at_depth` oidmap. It introduces a dedicated `free_seen_map_entry()` callback that properly frees each entry, replacing the previous approach that relied on passing `1` to `oidmap_clear()` for implicit cleanup. The change maintains identical behavior while making memory management more explicit and type-safe.

The implementation follows the pattern established in previous patches, using `container_of` to properly access the containing structure from the hashmap entry. This continues the series' goal of improving API contracts around resource management in Git's core data structures. The change is straightforward and well-contained, touching only the list-objects-filter.c file with a clear 9-line diff (8 additions, 1 deletion).

Test coverage for this specific change would be handled by the existing list-objects-filter tests, which exercise the depth-based filtering logic that uses this data structure. The patch maintains the series' high quality with a clear commit message explaining both the what and why of the change.