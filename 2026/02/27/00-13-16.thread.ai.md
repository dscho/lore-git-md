AI thread summary:

Thread subject: option parser silently accepts duplicate long options. Thread type: bug report. Originating author: Junio C Hamano.

Core problem: Git's option parsing infrastructure (`parse-options.c`) silently accepts duplicate long-form option names in command option tables, which could lead to subtle bugs. Demonstrated by duplicate `--batch-check` in `cat-file` and redundant `--stdin-packs` in `pack-objects`.

Current status: René Scharfe has submitted a two-patch series addressing the issue. First patch fixed the concrete `pack-objects` case by removing shadowed `OPT_BOOL` version of `--stdin-packs`. Second patch adds comprehensive runtime verification of option tables, detecting duplicate long names and numerical options using a `strset`.

Key technical details: New validation in `parse_options_check()` now checks for: 1) duplicate long names via `strset`, 2) multiple `OPTION_NUMBER` entries, while maintaining existing short option checks. Implementation includes proper `strmap.h` include and memory cleanup. Performance impact acknowledged but left for future optimization (possibly DEVELOPER-mode only).

Open questions: Whether to keep validation always-on or restrict to developer builds. The systematic solution is now implemented but optimization approach remains open.

Participant positions: Junio Hamano initially identified the general issue. René Scharfe provided both the concrete `pack-objects` fix and the comprehensive validation solution, demonstrating practical impact and systematic approach. No other contributors have commented yet on the validation patch.

Related work: Builds on Junio's initial report and connects to Git's option parsing infrastructure. The `pack-objects` fix relates to cd846bacc7 which introduced the duplicate `--stdin-packs`. The validation patch stands as a general solution to prevent similar issues across all commands.