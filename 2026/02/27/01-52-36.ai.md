This is a technical implementation patch adding new formatting placeholders to `pretty.c` as part of the ongoing configurable cover letter formatting series. It introduces `%(count)` and `%(total)` placeholders that will be used for patch numbering in formatted output.

The patch adds two new format specifiers that access the `rev_info` structure's `nr` (current patch number) and `total` (total patches in series) fields. These are currently only used by `format-patch` for email subjects but are being generalized for broader use. The implementation includes proper error handling when used without a revision context and maintains consistent width for the count field.

Key technical details:
- Adds validation to prevent use outside revision-aware contexts
- Implements `%(count)` with zero-padded formatting matching the width needed for the total
- Uses simple decimal output for `%(total)`
- Both placeholders return length 7 to skip past the specifier in parsing
- The changes are narrowly scoped to `pretty.c` with no other files affected
- Follows the established pattern for format placeholder implementations
- This is patch 1/4 in v3 of the series, building on prior discussion about numbering formats
- Implements the technical design Jeff King provided earlier in the thread
- Maintains backward compatibility while enabling the new cover letter formatting features

The implementation looks clean and focused, with the error handling and width formatting showing attention to detail. As the first patch in the series, it provides the foundation for the subsequent cover letter formatting changes.