Performance optimization discussion for `git-upload-pack` lock contention. Jeff King (Peff) responds to Patrick Steinhardt's patch that reduces write syscalls by buffering packfile data, suggesting alternative approaches that might avoid the keepalive timeout edge case while achieving similar performance gains.

Peff proposes two main alternatives: (1) increasing the write size from pack-objects by modifying `csum-file.c`'s buffer size (demonstrating this reduces write calls from ~420k to ~160k when cloning Linux), and (2) using `writev()` in `sideband.c` to combine header and data writes (further reducing calls to ~82k). He references prior discussion about packet-forming optimizations and asks about Patrick's measurement methodology, noting that strace can distort timing-sensitive measurements.

The response maintains the thread's technical depth while offering concrete, tested alternatives to the buffering approach. Peff's suggestions demonstrate his characteristic attention to low-level performance details and preference for solutions that avoid introducing new edge cases. The discussion remains focused on optimizing the write path without revisiting the core problem statement from the original patch.