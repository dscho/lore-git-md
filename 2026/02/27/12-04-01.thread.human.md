Here's a human-readable thread summary:

The thread reports a bug where `git clone --bundle-uri` fails to follow HTTP redirects when fetching bundle files, causing problems for real-world use cases like cloning the Linux kernel. Xavier Morel demonstrated that when trying to clone the kernel using its advertised bundle URI (which redirects to a mirror), Git silently fails with only a warning because the underlying HTTP client (curl) doesn't follow redirects by default. This behavior differs from common tools like wget and breaks the kernel's recommended cloning workflow.

The issue affects users trying to leverage bundle URIs for faster clones of large repositories that use redirects for their bundle distribution. While no patch has been proposed yet, the report clearly documents the problem and its impact on a major real-world use case, with references to kernel.org's official bundle usage documentation.