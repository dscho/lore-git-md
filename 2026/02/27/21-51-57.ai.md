This is the final patch in a feature series implementing configurable cover letter formatting for `git format-patch`. The thread context shows the implementation is effectively complete, with all technical components and documentation in place. The only remaining discussion point is how to handle format string parsing to remain future-proof.

The author (Mirko Faina) responds to Junio's suggestion about format string parsing rules, proposing to consistently require the "log:" prefix in both command-line and config file inputs. This would simplify the implementation by treating all non-shortlog formats uniformly, avoiding complex DWIM rules while still allowing for future format additions.

Key technical details from the context:
- The series introduces `format.commitListFormat` config alongside `--cover-letter-format` CLI option
- Core formatting is handled by new functions `generate_commit_list_cover()` and `generate_shortlog_cover_letter()`
- Numbering uses `%(count)`/`%(total)` placeholders with zero-padding
- Comprehensive test coverage exists for all components

The discussion has reached consensus on all major points, with only this final syntax detail remaining before the series can be merged. Junio had suggested two approaches (explicit prefixing rules vs. verbatim input), and the author is leaning toward requiring explicit "log:" prefixes for clarity and consistency. This would maintain backward compatibility while keeping the door open for future format additions.