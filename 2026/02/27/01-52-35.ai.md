This is a feature patch series extending `git format-patch` with customizable cover letter formatting, now in its third iteration. The series builds on prior maintainer feedback and incorporates Jeff King's request for patch numbering customization.

The key additions in v3 are:
1. New `%(count)` and `%(total)` placeholders in `pretty.c` for patch numbering, with consistent width handling
2. Refactored shortlog generation into a dedicated function
3. Expanded test coverage for the new formatting options
4. Config support via `format.commitListFormat`

The series maintains the architectural approach established in the thread - single validation point in `make_cover_letter()` and deferred default value handling. The author has addressed prior review feedback but notes testing limitations for the numbering placeholders since they're specific to cover letters.

Technical implementation touches `builtin/log.c` for the core letter logic, `pretty.c` for the new placeholders, and adds comprehensive tests in `t4014-format-patch.sh`. The changes follow Git's established patterns for format string handling and config integration.

Open questions from earlier iterations appear resolved, with the thread now focused on finalizing the numbering customization feature. The series demonstrates careful attention to maintainer feedback and Git's code quality standards, though the utility of the numbering customization remains under discussion (Peff had expressed complexity concerns). With tests added and implementation refined, this looks ready for maintainer consideration pending any final feedback.