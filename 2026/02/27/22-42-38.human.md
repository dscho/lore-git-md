This is a technical discussion about environment variable handling in `git for-each-repo`, part of Derrick Stolee's ongoing effort to improve repository-less operation and worktree isolation.

Jeff King (Peff) provides detailed feedback on the environment variable propagation logic in Stolee's v2 patch. He points out inconsistencies between the current implementation and `prepare_other_repo_env()`'s handling of config-related variables (`GIT_CONFIG_*`) and `NO_REPLACE_OBJECTS`. Peff argues for consolidating this logic into a single policy point rather than having subtly different behaviors across commands.

The discussion reveals nuanced considerations around which environment settings should propagate to child processes. Peff suggests that operation-level variables like `NO_REPLACE_OBJECTS` and `GIT_LITERAL_PATHSPECS` should likely persist across repository boundaries, while repository-specific config variables should not. He traces the historical inclusion of `NO_REPLACE_OBJECTS` in `local_repo_env` back to a 2010 refactoring, questioning whether it still belongs there.

The exchange demonstrates the careful attention being paid to edge cases in environment handling, with Peff's review helping ensure consistent behavior across Git commands. The thread shows productive technical discussion, with Stolee's implementation evolving through maintainer and reviewer feedback. The remaining open questions center around consolidating environment handling logic and clarifying which variables should propagate in which contexts.