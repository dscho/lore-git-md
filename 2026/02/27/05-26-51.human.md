This is a follow-up in the `.git` directory validation bugfix thread, where a Windows-specific regression has surfaced after the series was merged. Tian Yuchen responds to Junio Hamano's report that the changes cause `GIT_DIR=/dev/null` (or `nul` on Windows) to fail where it previously worked.

Yuchen hypothesizes that Windows' `stat("nul")` fails with an unexpected errno, triggering the new fatal error path. He suggests two possible fixes - either making certain error conditions non-fatal again (which would backtrack on prior security-focused decisions) or updating the test expectations - but acknowledges neither aligns with the series' goals. The email ends with a lighthearted retreat, as Yuchen lacks Windows expertise to propose a proper solution.

The exchange highlights the challenge of cross-platform behavior in security-sensitive code: the validation changes were carefully designed but introduced an unexpected Windows edge case. The thread remains in a holding pattern until someone with Windows knowledge can investigate the `nul` handling quirk. The core architectural decisions from earlier discussion (strict error handling for security) remain uncontested - this is purely about platform-specific path handling.