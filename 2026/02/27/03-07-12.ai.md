This is a refactoring patch in the context of Taylor Blau's RFC series on incremental MIDX/bitmap-based repacking. The patch builds on Junio Hamano's earlier infrastructure improvements to the `strvec` API.

Taylor responds to Junio's suggestion about optimizing `strvec_init_alloc()` to handle zero-allocation cases consistently with the existing `strvec_init()` optimization. The key change moves the zero-allocation optimization into `strvec_init_alloc()` and makes `strvec_init()` a simple wrapper that calls it with zero elements. This consolidates the initialization logic while preserving the performance optimization for empty strvecs.

The patch touches `strvec.[ch]` and maintains all existing functionality while improving the internal structure. Taylor notes a typo in Junio's suggested diff (calling `strvec_init()` recursively) and corrects it before applying the change. This is a small but meaningful cleanup that makes the API more consistent and maintainable.

This change is part of the broader infrastructure work supporting Taylor's geometric repacking series, where efficient string handling matters for MIDX file tracking. The discussion confirms that while `string_list` (used in earlier patches) may not be maximally performant for lookups, it's adequate for the expected scale of MIDX operations.