This is a refactoring patch that improves the oidmap cleanup API by making memory management more explicit. It introduces a new `oidmap_clear_with_free()` function that takes a callback for freeing entries, replacing the less clear `oidmap_clear(map, 1)` pattern where the boolean parameter determined whether entries should be freed.

The change makes the API more type-safe and removes reliance on internal struct layout assumptions. The patch includes thorough unit tests verifying both cases: when a free callback is provided (entries are freed) and when it's not (entries remain allocated). This is part of a 5-patch series that updates all oidmap call sites across Git's codebase to use the new API.

The implementation looks clean and well-tested, with clear documentation update explaining the new function. This appears to be a straightforward improvement to Git's internal APIs that maintains existing behavior while making memory management more explicit. The change touches core data structures but is unlikely to be controversial as it preserves all existing functionality.