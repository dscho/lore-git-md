This is a test refinement in the final patch of the 8-part series implementing reference backend selection. Toon Claes points out two test cases where the `expect` and `actual` file assignments appear to be reversed in Karthik Nayak's test script. The tests verify that the `GIT_REFERENCE_BACKEND` environment variable correctly initializes repositories with alternate ref directories.

The series has already received maintainer approval and is queued for integration into the 'next' branch after eight iterations of review. This minor test fix comes as final polish before merging. The change doesn't affect the core functionality - it just ensures the test assertions are properly structured to match Git's standard test pattern where the expected output comes first.

The patch series enables zero-downtime migrations between ref backends (files<->reftable) through three configuration mechanisms, primarily serving GitLab's migration needs. This final review comment shows the project's attention to detail even in the polishing stages of a major feature.