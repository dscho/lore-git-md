This is a discussion thread about `git log`'s decoration behavior, specifically how the `--decorate` option interacts with format placeholders like `%d`. The conversation builds on earlier exchanges between Alejandro Colomar, Junio Hamano, and now Marc Branchaud.

Marc points out inconsistencies in how `--decorate=auto` behaves differently between default output and explicit `%d` format placeholders. He notes that while the documentation suggests decorations should be suppressed when piping to a file, this doesn't currently happen when using `%d` in a custom format. 

The thread reveals two key tensions: 
1) Whether `--decorate=no` should suppress all decorations (including those from `%d`), which would make the interface more consistent but potentially break existing scripts
2) How to properly document the relationship between format placeholders and decoration options

Junio maintains that format strings should remain stable for backward compatibility, while Marc suggests the current behavior may be unintuitive enough to warrant changes. The discussion remains technical and constructive, with Marc providing concrete examples of the inconsistencies and considering real-world usage patterns.

This exchange shows the careful balancing act in Git's development between interface consistency and backward compatibility, with both technical and documentation implications at stake. The thread appears headed toward either a narrow solution (adding a 'never' value) or potentially broader changes to make decoration behavior more coherent across all output modes.