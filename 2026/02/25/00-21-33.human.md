This is patch 8/14 in Taylor Blau's RFC series introducing incremental MIDX/bitmap-based repacking. The series aims to improve repacking in large repositories through geometric strategies.

This particular change is a straightforward refactoring that extracts common MIDX command preparation logic into a new `repack_prepare_midx_command()` helper function. The helper handles the shared parts of constructing `git multi-pack-index` commands (setting up the subcommand, progress options, and bitmap flags) while leaving subcommand-specific arguments to the caller.

The patch is purely mechanical - it introduces no behavior differences, just reorganizes code to avoid duplication as the series adds more MIDX command variants. It's a typical preparatory patch in a larger series, cleaning up the codebase before introducing new functionality that will need to reuse this logic.

The change is small (30 lines total) and well-contained to `repack-midx.c`. It follows the series' pattern of careful infrastructure work before adding new features. Given Taylor's expertise in the pack/MIDX subsystems and the non-controversial nature of this refactoring, this patch seems likely to be accepted with little discussion.