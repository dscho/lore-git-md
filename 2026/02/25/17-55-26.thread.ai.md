Updated AI thread summary:

Alejandro Colomar's proposal to modify `git log`'s decoration behavior has evolved through discussion with Junio C Hamano and Marc Branchaud. The thread examines how `--decorate` interacts with format placeholders, particularly `%d` in `pretty.c`. Initial discussion saw Junio rejecting direct changes to `%d` behavior due to backward compatibility concerns, proposing instead a new `%(decorate:optional=yes)` placeholder. Alejandro countered with extending `--decorate` with a 'never' value, which Junio accepted.

Marc Branchaud's contribution adds empirical observations about current behavior:
- `--decorate=no` doesn't affect `%d` output despite Alejandro's original assumption
- Existing precedent shows `--decorate` values (short/full) already influence `%d` display
- Potential bug identified where `--decorate=auto` fails to suppress `%d` when piping to file
- Marc suggests either:
  1. Making `--decorate=no` affect all decoration placeholders (including proposed new syntax)
  2. Introducing 'never' value specifically for `%d` suppression

Technical considerations:
- Core formatting in `pretty.c` needs modification for new behavior
- Current inconsistencies suggest the interface isn't fully coherent
- Two viable approaches now on table with different tradeoffs:
  - Broad approach (`--decorate=no` affects all) might break obscure use cases
  - Narrow approach ('never' value) adds complexity but preserves more compatibility

Junio's approval of the 'never' approach remains valid, but Marc's observations about current behavior add weight to considering the broader solution. The thread now contains:
1. Clear problem statement with empirical data about current behavior
2. Two technically sound solutions with identified tradeoffs
3. Maintainer preference for one approach
4. New evidence suggesting the interface may need deeper reconsideration

Future discussion may focus on:
- Whether current inconsistencies warrant breaking changes
- Real-world usage patterns of `--decorate` with custom formats
- Potential deprecation path for any changed behavior
- Implementation details in `pretty.c` and decoration subsystem

The thread demonstrates Git's characteristic design process where empirical observation informs API evolution, with all solutions maintaining the core principle that format strings should remain stable while options control behavior.