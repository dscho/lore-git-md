Updated AI thread summary:

Alejandro Colomar's proposal to modify `git log`'s decoration behavior has evolved through discussion rounds with Junio C Hamano and Marc Branchaud. The thread examines `--decorate` interactions with format placeholders (particularly `%d` in `pretty.c`), revealing deeper interface considerations.

Current state:
- Junio rejected direct `%d` behavior changes due to backward compatibility, proposing `%(decorate:optional=yes)`
- Alejandro suggested extending `--decorate` with 'never' value, which Junio accepted
- Marc Branchaud provided empirical observations showing:
  - `--decorate=no` doesn't affect `%d` output
  - `--decorate=auto` fails to suppress `%d` when piping to file
  - Existing precedent where `--decorate` values influence `%d` display

Junio's latest clarification establishes:
- `--decorate=auto` behavior (suppressing decorations when piped) only applies to default format, not explicit `%d`
- Clear distinction between implicit decoration behavior and explicit format placeholders
- Maintains position that format strings should remain stable while options control behavior

Technical landscape:
1. Two viable solutions on table:
   - Narrow: 'never' value for `%d` suppression (Junio's preference)
   - Broad: Make `--decorate=no` affect all decorations (Marc's observations suggest this might be more coherent)
2. Implementation requires `pretty.c` modifications
3. Interface inconsistencies identified:
   - `--decorate=auto` behavior mismatch between default and custom formats
   - Disconnect between `--decorate=no` and `%d` output

Open questions:
- Real-world usage patterns of `--format` with `--decorate` (Marc's unanswered question)
- Whether current inconsistencies warrant breaking changes
- Potential deprecation path for any changed behavior
- Exact implementation approach in decoration subsystem

Key participants:
- Alejandro Colomar (original proposal)
- Junio C Hamano (maintainer, favoring narrow solution)
- Marc Branchaud (empirical analysis, raised broader concerns)

The thread demonstrates Git's design process where:
- Empirical observation informs API evolution
- Backward compatibility constraints shape solutions
- Maintainer judgment balances interface coherence with stability
- Technical discussion remains focused despite multiple solution paths

Future discussion likely to address:
- Resolution between narrow/broad approaches
- Implementation details
- Documentation updates
- Potential follow-up work on interface coherence