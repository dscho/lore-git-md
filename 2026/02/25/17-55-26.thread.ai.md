Updated AI thread summary:

Thread summary:

Alejandro Colomar's proposal to modify `git log`'s `%d` format specifier in `pretty.c` to respect `--decorate` has evolved through discussion. The initial suggestion to change `%d`'s unconditional decoration output was rejected by Junio C Hamano due to backward compatibility concerns with existing scripts. Junio proposed introducing a new `%(decorate:optional=yes)` placeholder as an additive solution.

In follow-up, Alejandro countered with an alternative approach: extending the `--decorate` option itself with a 'never' value that would suppress decorations even from `%d`, rather than creating new format syntax. This maintains backward compatibility Junio emphasized while providing decoration control through the existing option mechanism.

The discussion centers on two viable solutions that both respect Git's compatibility principles:
1. Junio's preferred path of introducing new syntax (`%(decorate:optional=yes)`)
2. Alejandro's counter-proposal to extend `--decorate` with a 'never' value

Key technical considerations remain:
- The core formatting logic in `pretty.c` needs modification for either approach
- All existing format strings must continue working unchanged
- The `%d` specifier's current unconditional output must be preserved
- New functionality should integrate cleanly with Git's decoration subsystem

Junio's maintainer position makes his preference for additive changes authoritative, but Alejandro's proposal offers potential UI simplification. The thread demonstrates Git's careful balance between evolution and stability, with both solutions providing paths to achieve the goal of optional decoration control while preserving backward compatibility.