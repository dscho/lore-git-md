Junio C Hamano responds positively to Alejandro Colomar's alternative proposal for controlling `git log` decoration output. Rather than introducing a new format placeholder (`%(decorate:optional=yes)`), Junio now favors extending the existing `--decorate` option with a 'never' value that would suppress decorations even from the `%d` format specifier.

This marks a shift from the earlier discussion where Junio had advocated for an additive syntax change to maintain backward compatibility. The 'never' approach achieves the same goal of making decorations optional while working through Git's established option mechanism rather than adding new format string syntax. The solution elegantly preserves all existing behavior - scripts using `%d` will continue working as before unless explicitly opting into the new 'never' mode.

The exchange demonstrates Git's characteristic balancing act between evolution and stability, with both participants converging on a solution that meets the project's compatibility requirements. Junio's approval as maintainer suggests this is likely the direction the change will take, though the thread may see further technical discussion about implementation details in `pretty.c`'s formatting logic.