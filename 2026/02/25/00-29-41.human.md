This is a feature patch adding configurable cover letter formatting to `git format-patch`. The patch series is currently in active development, with Junio C Hamano providing detailed review feedback and squashing fixes into the 'seen' branch.

The current email shows Junio responding to a design choice about newline handling in the cover letter formatting. The author (Mirko Faina) explains they're intentionally preserving user control over newlines in the format strings rather than automatically appending them during processing. Junio acknowledges this approach is reasonable if appending newlines during configuration parsing would be non-trivial.

The thread shows healthy back-and-forth between maintainer and contributor, with Junio offering suggestions but ultimately deferring to the author's judgment on implementation details. The feature itself provides both command-line (`--cover-letter-format`) and config-based (`format.commitListFormat`) control over how commit lists appear in cover letters, using Git's existing pretty-printing infrastructure.

The series appears technically sound and well-reviewed, with some final polishing needed around documentation and test coverage before promotion to 'next'. This is the kind of focused, incremental improvement that typically gets merged after addressing the remaining nits.