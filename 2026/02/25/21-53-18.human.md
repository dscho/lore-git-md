This is a technical discussion about the `git replay` revert functionality, specifically focusing on the message formatting implementation. Junio Hamano responds to Toon Claes' concerns about the split between handling `commit_use_reference` configuration in two different places and the use of NULL for commit OID in the shared `sequencer_format_revert_header()` function.

The exchange reveals some remaining design questions about where configuration handling should live and whether the shared function's interface could be improved. Junio notes the tension between making this behave as plumbing (ignoring user config) versus potentially being a porcelain command, though he doesn't take a firm position on which it should be.

This is a relatively minor implementation detail in the broader `git replay` revert feature series, but it touches on important Git design principles around configuration handling and plumbing/porcelain separation. The discussion suggests the technical approach is sound but there may be room for further refinement in how the pieces fit together.