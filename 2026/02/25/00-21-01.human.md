This is a preparatory infrastructure patch in Taylor Blau's RFC series introducing incremental MIDX/bitmap-based repacking. It's part of a larger effort to improve repacking strategies for large repositories.

The patch adds a new `strvec_init_alloc()` function that allows callers to pre-allocate space in a strvec (Git's string array type) when they know in advance how many elements will be storing. This avoids repeated reallocations as elements are added. The implementation carefully handles the NULL terminator slot and uses Git's `st_add()` helper for safe arithmetic.

While this change is small and focused, it serves an important role in the series by providing more efficient string array handling that will be used in subsequent patches dealing with MIDX and packfile management. The commit message is clear about the motivation and implementation, though it doesn't explicitly connect to the larger series' goals - that context comes from the thread summary.

Given Taylor Blau's expertise in the pack/MIDX subsystem and the preparatory nature of this change, it's likely to be uncontroversial. The patch follows Git's coding style and includes the necessary documentation updates. It's a typical example of the kind of small infrastructure improvements that often appear early in larger patch series.