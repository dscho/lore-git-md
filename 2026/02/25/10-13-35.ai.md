This is a test infrastructure patch in Patrick Steinhardt's series to make geometric repacking the default maintenance strategy. The patch explicitly sets the maintenance strategy to "gc" for the t6500 test file, which tests git-gc behavior directly. 

The change ensures the test suite continues to test the legacy gc behavior even after the default strategy switches to geometric repacking. Toon Claes confirms the global config setting is safe since each test file runs in isolation. This is one of the preparatory patches (6/8) that ensures the test suite remains stable before the final behavior change in patch 8.

The patch demonstrates careful attention to backward compatibility in the test suite - rather than disabling maintenance entirely, it preserves the ability to test gc-specific behavior while allowing the default to change. This aligns with the series' overall approach of infrastructure-first changes before behavior modifications.