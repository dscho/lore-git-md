Bugfix for URL handling in `git fetch`'s display logic.

This is a one-line fix addressing undefined behavior in the URL trailing-slash trimming code in `builtin/fetch.c`. The patch corrects the evaluation order in a loop condition that removes trailing slashes from URLs before display. The original code could attempt to access `url[-1]` when `i` became -1, which is undefined behavior. The fix simply reorders the conditions to check the array bounds first.

The change is straightforward and appears correct - it matches the common C idiom for safe array traversal. However, the commit message could be more detailed about the actual impact (this affects URL display formatting, not core functionality) and the bug's manifestation (likely harmless in practice since the loop would terminate immediately after the invalid access). The patch comes from a relatively new contributor (cuiweixie) via GitGitGadget.