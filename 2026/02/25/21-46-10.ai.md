This is a discussion thread about `git log`'s decoration behavior, specifically around the interaction between the `--decorate` option and format placeholders like `%d`. The conversation builds on earlier exchanges between Alejandro Colomar, Junio Hamano, and now Marc Branchaud.

Marc responds to Junio's clarification about `--decorate=auto` behavior, noting that while the current implementation only affects the default format (not explicit `%d` placeholders), the documentation doesn't make this distinction clear. He points out that the documentation suggests `%d` and `--decorate` are connected, which could reasonably lead users to expect consistent behavior between default and custom formats.

The thread reveals two key inconsistencies in the current implementation:
1. `--decorate=no` doesn't suppress `%d` output (it behaves like `--decorate=short`)
2. `--decorate=auto` only suppresses decorations in the default format, not when `%d` is explicitly used

Marc suggests that since `--decorate=no` currently has no meaningful effect on `%d` output (it's equivalent to `--decorate=short`), changing its behavior to actually suppress decorations would be a safe change. He notes that users who want the current behavior could simply use `--decorate=short` instead.

The discussion highlights the tension between maintaining backward compatibility and improving interface coherence. While Junio favors a narrow solution (adding a 'never' value for `%d`), Marc's analysis suggests a broader approach (making `--decorate=no` affect all decorations) might provide more consistent behavior. The thread remains technical and focused, with participants carefully considering real-world usage patterns and documentation implications.