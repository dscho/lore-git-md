This is a review response in the ongoing thread about configurable cover letter formatting for `git format-patch`. Jeff King (Peff) provides technical feedback on the implementation details, focusing on the handling of format strings and default values.

Peff suggests several improvements to the patch's logic:
1. Removing an unnecessary `assert()` and moving default value handling later in the flow
2. Proposing more robust format string validation that would error on invalid inputs like "--cover-letter-format=foo"
3. Questioning whether the "log:" prefix is strictly necessary, suggesting the format could be inferred from content

The discussion reveals some subtle design considerations around when to set defaults and how strictly to validate inputs. Peff's comments reflect Git's typical approach of being permissive with internal state but strict with user input validation. He also considers future extensibility, suggesting the design should leave room for additional output formats beyond just "shortlog" and custom formats.

This continues the thread's pattern of thorough technical review, with Peff providing both specific code suggestions and higher-level design questions. The author will need to address these points in the next revision while maintaining the already-established architectural approach that uses `repo_format_commit_message()` for formatting.