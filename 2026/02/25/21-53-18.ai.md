This is a technical discussion about the `git replay` revert functionality, specifically focusing on the message formatting implementation. The thread has evolved from the initial feature introduction to now debating interface details and code organization.

Junio Hamano responds to Toon Claes' concerns about the split between `opts->commit_use_reference` handling in different functions. The core issue is whether `sequencer_format_revert_header()` should fully encapsulate the reference formatting logic when called with a NULL OID parameter. Patrick Steinhardt had previously suggested consolidating this behavior, but the current implementation keeps some logic in the caller.

The discussion reveals tension between making `git replay` behave consistently as plumbing (ignoring user configs) versus potentially exposing some user-facing behavior. The message formatting touches both the revert header generation and how commit references are displayed, with the current implementation splitting this responsibility between the shared helper and its caller.

This patch iteration represents ongoing refinement of the revert functionality, with attention now turning to internal API boundaries and configuration handling. The technical capability is established, but the discussion shows careful consideration of how to structure the code for both current needs and future maintenance.