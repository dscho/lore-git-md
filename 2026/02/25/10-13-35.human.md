This is a test infrastructure patch in Patrick Steinhardt's series to make geometric repacking the default maintenance strategy. The patch ensures the `t6500-gc.sh` test explicitly uses the "gc" strategy rather than inheriting the upcoming new default of "geometric", since the test specifically wants to verify `git gc` behavior.

Toon Claes confirms the global config setting is safe because each test file runs in its own isolated directory. This is a small but important preparatory change that maintains test reliability as the default strategy changes. The patch has already been reviewed and acked in the context of the full series, with no new concerns raised in this follow-up.