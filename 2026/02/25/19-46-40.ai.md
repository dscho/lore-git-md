This is a discussion about refining the behavior of `git log`'s decoration formatting, building on the earlier thread about making `%d` respect `--decorate` options. Marc Branchaud joins the conversation with some empirical observations about current behavior.

Marc points out that while `--decorate=no` currently doesn't affect `%d` output (contrary to Alejandro's original proposal), there's already precedent for `--decorate` values influencing format placeholders - the `short`/`full` variants change how `%d` displays ref prefixes. He suggests this makes Alejandro's proposal more reasonable than initially thought, and even identifies what might be considered a bug: `--decorate=auto` doesn't suppress `%d` output when piping to a file as the documentation suggests it should.

The email offers two alternative approaches: either make `--decorate=no` affect all decoration-related placeholders (including the proposed `%(decorate:...)` syntax), or introduce a new `never` value specifically for suppressing `%d`. Marc notes the potential confusion of having both `no` and `never` values, and questions whether anyone actually uses `--decorate` with custom formats containing `%d` in practice.

This continues the thread's technical discussion about how to cleanly separate decoration control between formatting placeholders and command-line options while maintaining backward compatibility. Marc's observations about current inconsistencies add weight to the argument for some change, while his suggestions aim to find a solution that's both intuitive and minimally disruptive.