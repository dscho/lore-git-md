This is a technical discussion thread regarding `git log`'s decoration behavior, specifically how `--decorate` interacts with format placeholders like `%d`. The thread has progressed from Alejandro Colomar's initial proposal through several rounds of feedback, with Junio Hamano now responding to Marc Branchaud's empirical observations about current behavior.

Junio clarifies that `--decorate=auto`'s documented behavior (suppressing decorations when output is piped) applies only to the default output format, not when `%d` is explicitly requested in a custom format. He distinguishes between the implicit decoration behavior and explicit format placeholders, suggesting these are intentionally separate concerns.

Marc had raised two key observations: that `--decorate=no` doesn't affect `%d` output (contrary to initial assumptions), and that `--decorate=auto` fails to suppress `%d` when piping to a file. He also questioned whether users actually combine `--format` with `--decorate`, hinting at a potential interface design issue.

Junio acknowledges the interesting nature of Marc's question about real-world usage patterns but doesn't let it directly influence the technical decision path. The thread shows the maintainer working to preserve backward compatibility while considering how to make the interface more coherent. The discussion remains focused on whether to implement a narrow solution (new 'never' value) versus a broader one (making `--decorate=no` affect all decorations), with Junio currently favoring the former approach.