This is a continuation of the discussion about `git log` decoration behavior, specifically addressing how `--decorate=auto` interacts with format placeholders like `%d`. Junio clarifies that the documented behavior of `--decorate=auto` (not showing refs when output is piped) applies only to the default output format, not when explicitly requesting decorations via `%d` in a custom format. This maintains consistency that format placeholders do what they say regardless of decoration settings.

Marc Branchaud had observed that `%d` still shows refs when piped to a file, which Junio explains is expected behavior rather than a bug when using explicit format strings. The thread also touches on whether users typically combine `--format` with `--decorate`, though Junio notes this doesn't directly impact the design decision at hand.

The discussion remains focused on finding the right balance between backward compatibility and interface coherence, with Junio maintaining his earlier approval of adding a 'never' value to `--decorate` as the solution. The exchange demonstrates Git's careful approach to behavior changes, where documentation and user expectations are given significant weight alongside technical considerations.