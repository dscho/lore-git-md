Here's the updated human-readable summary:

The discussion around `git log`'s decoration behavior has progressed with new insights from Marc Branchaud. The core issue remains how `--decorate` interacts with format placeholders like `%d`, where current behavior shows some inconsistencies between documentation and implementation.

Key developments:
- Marc identified that while `--decorate=auto` only affects the default format (not explicit `%d`), the documentation doesn't clarify this distinction, potentially misleading users
- He observed that `--decorate=no` currently has no meaningful effect on `%d` output (behaving like `--decorate=short`), suggesting this could be safely changed to actually suppress decorations
- The thread has revealed two main approaches: Junio's preference for a narrow solution (adding a 'never' value for `%d`), versus Marc's proposal for broader consistency (making `--decorate=no` affect all decorations)

The discussion remains productive, with participants carefully weighing backward compatibility against interface coherence. Marc's empirical analysis has added important context about real-world usage patterns and documentation clarity. Next steps will likely involve deciding between the competing approaches and addressing the documentation gaps that have been identified.