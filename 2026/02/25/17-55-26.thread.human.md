Here's the updated human-readable thread summary:

The thread began with Alejandro Colomar proposing to make `git log`'s `%d` format specifier respect the `--decorate` option, bringing it in line with other format specifiers. Junio Hamano firmly rejected changing the existing behavior due to backward compatibility concerns, instead suggesting a new `%(decorate:optional=yes)` placeholder that would respect decoration settings while preserving current `%d` behavior.

In the latest development, Alejandro has countered with an alternative proposal: extending `--decorate` with a 'never' value that would suppress decorations even from `%d`, rather than introducing new format syntax. This approach would maintain backward compatibility while providing centralized control over decorations through the existing option mechanism.

The discussion remains focused on finding a solution that both preserves existing behavior (a core Git principle) and improves the user experience. While Junio's position as maintainer makes his preference for additive changes the likely outcome, Alejandro's proposal offers a potentially simpler interface. The thread demonstrates Git's careful balance between evolution and stability, with both technical and usability considerations being weighed.