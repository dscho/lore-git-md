Here's the updated human-readable summary:

The thread explores proposed changes to how `git log` handles decorations (like branch/tag names) when using custom formats. Alejandro Colomar initially suggested modifying the `%d` placeholder's behavior, but Junio Hamano raised backward compatibility concerns. The discussion has evolved through several alternatives, with Marc Branchaud providing key insights about current behavior.

We now understand that:
- `--decorate=no` currently affects only default output, not explicit `%d` placeholders
- `--decorate=auto` (which suppresses decorations when piping) also doesn't affect `%d`
- There's some inconsistency in how decoration controls interact with format strings

Junio has approved adding a new 'never' value to `--decorate` specifically for suppressing `%d` output, favoring this narrower solution over making `--decorate=no` affect all decorations. Marc's observations suggest the interface might benefit from deeper reconsideration, but Junio is prioritizing backward compatibility.

The thread shows the project's typical careful approach to changing established behavior - even when inconsistencies are identified, changes are made incrementally to avoid breaking existing use cases. The discussion remains open whether to pursue a more comprehensive solution in the future.