Here's a human-readable summary of the thread's current state:

The discussion about `git log`'s decoration behavior has reached a technical consensus among Alejandro Colomar, Marc Branchaud, and Junio Hamano. The core issue involves unexpected interactions between the `--decorate` option and format placeholders (`%d`), where the current implementation doesn't match the documentation.

Key findings show that `--decorate=auto` only affects default formats (not explicit `%d` placeholders), while `--decorate=no` currently behaves the same as `--decorate=short` - a potentially confusing implementation detail. Colomar has joined Marc's position that making `--decorate=no` fully suppress all decorations would be a safe change, since any existing uses could simply switch to `--decorate=short` for the current behavior.

The conversation has evolved from identifying inconsistencies to evaluating specific solutions. While Junio initially favored a more conservative approach, the technical merits of unifying the decoration behavior appear to be gaining traction. The discussion remains focused on the decoration subsystem in `pretty.c`, with participants carefully weighing interface consistency against backward compatibility concerns. Documentation updates will be needed regardless of the final decision to clarify these behaviors.