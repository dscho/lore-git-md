This is part 2 of an 8-patch refactoring series focused on naming consistency in the `repo-info` subsystem. The patch mechanically renames `repo_info_fields` to `repo_info_field` in `builtin/repo.c`, following the new CodingGuidelines rule established in patch 1 that recommends singular names for arrays accessed element-wise. 

The change is straightforward - it updates the array name and all references to it, including the comparison function name (`repo_info_fields_cmp` -> `repo_info_field_cmp`). The patch maintains the same way the array is used with `bsearch()` and iteration. This continues the series' pattern of applying consistent naming while preserving behavior.

Key technical details:
- Changes only `builtin/repo.c`
- Updates array name and related function name
- Keeps all existing functionality including lexicographical ordering requirement
- Follows the singular naming convention for element-wise accessed arrays

The patch appears well-executed and properly motivated by the series' goal of establishing consistent conventions. It builds on patch 1's guideline while setting up for subsequent patches that will further standardize the subsystem's naming.