This is a discussion about Git's pretty-format behavior regarding reference decorations. Junio C Hamano responds to Alejandro Colomar's proposal to make the `%d` format specifier respect `--decorate` flags, explaining why changing `%d`'s behavior would be problematic for backward compatibility with existing scripts.

Junio firmly rejects altering `%d`'s current unconditional decoration behavior, noting it would break numerous existing scripts. However, he suggests a forward-looking solution: introducing a new `%(decorate:optional=yes)` placeholder that would honor the `--decorate` flag, allowing users who want decoration control in format strings to opt into the new behavior without disrupting existing usage.

The exchange highlights Git's careful balance between maintaining backward compatibility and evolving functionality. Junio's response demonstrates the project's conservative approach to changing established behavior while still providing pathways for new features through additive changes rather than modifications. The discussion touches core formatting logic in `pretty.c` and would require careful implementation to handle the new placeholder while preserving all existing format string behaviors.