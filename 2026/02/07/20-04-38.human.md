This is a small but important type-safety patch in the larger Rust interoperability series. It changes the `count` parameter of `hashwrite()` from `unsigned int` to `uint32_t` to ensure consistent type sizes when called from Rust code. The change is straightforward and well-justified - it maintains compatibility across language boundaries while not affecting existing behavior, since `unsigned int` is already 32 bits on all supported platforms. 

As part of brian m. carlson's multi-part effort to enable SHA-1/SHA-256 repository interoperability with Rust components, this patch helps ensure type consistency at the FFI boundary. The change is mechanical and low-risk, touching only the function signature in both declaration and definition. The commit message clearly explains the motivation without overstating the impact. This is the kind of careful groundwork needed for safe Rust/C interop.