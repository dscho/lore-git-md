This is a housekeeping patch in a series addressing `git am` security vulnerabilities. Phillip Wood starts with infrastructure groundwork by ensuring Git's sample hook files (which end in `.sample`) are properly configured in `.gitattributes` and `.editorconfig`. This prepares for subsequent patches that will add security-focused hooks.

The change is straightforward: it adds a `.gitattributes` rule to handle whitespace and line endings for hook templates, and updates `.editorconfig` to apply consistent tab-based indentation to these files. While small, this is part of a larger effort to mitigate security risks around patch parsing ambiguities in `git am`.

The patch appears well-scoped and uncontroversial - it's clearly setting up for more substantive changes to come in the series. The commit message could be more explicit about how this relates to the security thread, but the connection is clear from the context.