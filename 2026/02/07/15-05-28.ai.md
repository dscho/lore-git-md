This is the final patch in Kristoffer Haugsbakk's documentation series improving the `git-patch-id` man page. The patch adds a "SEE ALSO" section referencing `git-cherry(1)`, completing the cross-referencing between these related commands. 

The change is minimal (+4 lines) but meaningful - it formally documents the relationship between `git-patch-id` and `git-cherry` that was already implied by the examples and existing cross-links in the opposite direction. The commit message notes this symmetry, pointing out that since `git-cherry` already links to `git-patch-id`, it makes sense to reciprocate.

This follows naturally from the previous patch's addition of an example script showing how to use patch IDs to correlate commits across branches - a use case that overlaps with `git-cherry`'s functionality. The series as a clear focus on making `git-patch-id`'s capabilities and relationships more discoverable through documentation, with no code changes or controversial elements.