This is a security-focused discussion about `git am`'s vulnerability to embedded diffs and separator lines in commit messages, specifically addressing Junio Hamano's workflow counterexample where legitimate separator line usage would trigger false positives in Phillip Wood's enhanced commit-msg hook.

Kristoffer Haugsbakk proposes making the hook's security check optional via an environment variable, building on Wood's earlier implementation that detects dangerous patterns (unindented diffs and "---" separators) while preserving Signed-off-by verification. The suggestion comes in response to Junio's concern that his personal workflow of using separator lines as commit message placeholders would be disrupted by the hook's strictness.

Key technical points include the hook's current behavior of scanning only above the scissors line, supporting configurable comment characters, and detecting both traditional diff headers and standalone separators. The discussion now centers on balancing security against workflow compatibility, with Haugsbakk's proposal offering a middle ground where users could bypass the check via `--no-verify` or a hypothetical environment variable when intentionally using separator lines.

This thread represents the ongoing tension between hardening `git am` against malicious/misformatted patches and maintaining backward compatibility with established workflows. The solution space has evolved from pure parser fixes (Peff) through indentation-based approaches (Keller) to Wood's layered defense combining hook-based prevention with parser awareness, now considering additional escape hatches for power users.