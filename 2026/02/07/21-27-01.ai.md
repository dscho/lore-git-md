This is a security-focused enhancement to the sample `commit-msg` hook that detects separator lines ("---") in commit messages, building on Phillip Wood's earlier work to prevent `git am` confusion from embedded diffs. The patch adds detection of unindented "---" lines that would cause `git am` to truncate commit messages, while preserving the existing diff detection logic.

Junio Hamano raises a valid counterpoint about his personal workflow where he intentionally uses "---" lines in-progress commits as a reminder for future patch formatting. He notes this technique works because `git apply` ignores content before the diff, while `git am` stops at the first "---". While acknowledging the hook can be bypassed with `--no-verify`, he questions whether the safety check is worth breaking legitimate workflows.

The technical implementation handles edge cases like custom comment characters (including those with trailing "$") by stripping commented lines before checking for separators. The error messages clearly explain how to fix issues by indenting problematic content. This represents a practical mitigation while deeper parser solutions continue being debated, though Junio's feedback suggests the tradeoffs between safety and workflow flexibility may need further discussion.

Key points for future thread summaries:
- The hook now checks for unindented "---" lines that would confuse `git am`
- Junio's workflow counterexample shows legitimate uses of separator lines
- Error messages guide users to indent rather than reject outright
- Works with custom `core.commentChar`/`core.commentString` configurations
- Maintains backward compatibility via `--no-verify` escape hatch
- Part of layered security approach while parser ambiguities remain unresolved