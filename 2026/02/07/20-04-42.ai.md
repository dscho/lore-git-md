This is part of the ongoing Rust infrastructure effort for SHA-1/SHA-256 interoperability, specifically addressing build system integration. The change introduces a Rust `build.rs` script to properly link test binaries against `libgit.a` and zlib, while updating the Makefile to ensure correct build ordering.

The patch adds a new `build.rs` file that specifies link dependencies for Rust test binaries, explicitly declaring they need to link against `libgit.a` and zlib. This prepares for future changes where test binaries will require functionality from the core Git library. The Makefile is adjusted to make the Rust library depend on `libgit.a`, preventing race conditions during parallel builds.

Key technical details:
- The `build.rs` script uses Cargo's build script mechanism to specify link paths and libraries
- Three `println!` directives configure the linker to:
  - Search the current directory (`cargo:rustc-link-search=.`)
  - Link against `libgit.a` (`cargo:rustc-link-lib=git`)
  - Link against zlib (`cargo:rustc-link-lib=z`)
- Makefile change ensures `libgit.a` is built before the Rust library

This is a straightforward infrastructure patch that follows established Rust/Cargo conventions while integrating with Git's existing build system. It demonstrates the careful attention being paid to build system integration as part of the broader Rustification effort.