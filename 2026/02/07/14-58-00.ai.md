This is a contributor housekeeping patch that updates Git's configuration files to properly handle sample hook files. Phillip Wood makes two small but related changes:

First, he adds the sample hook files (which end in `.sample`) to `.editorconfig` so they follow the same tab-based indentation rules as other script files. This ensures consistent formatting when contributors edit these files.

Second, he adds a new rule to `.gitattributes` specifically for the sample hooks in `templates/hooks/`. The rule enforces consistent line endings (LF), whitespace handling, and text file attributes for these files. This is needed because while the files are shell scripts, their `.sample` extension means they wouldn't inherit the standard shell script rules.

The changes are straightforward and uncontroversial - they're purely about maintaining consistency in how Git's own files are formatted and handled. The patch is part of a 3-patch series, though the context doesn't indicate what the other patches contain. Given the thread context about `git am` security vulnerabilities, this appears to be preparatory work for subsequent patches that may modify sample hooks (possibly including security-related changes to commit message handling).