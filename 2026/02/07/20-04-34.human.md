This is a Rust infrastructure patch in brian m. carlson's multi-part series introducing Rust components for SHA-1/SHA-256 interoperability. The patch adds a fundamental `ObjectID` struct that mirrors Git's C struct `object_id` for seamless FFI interaction between Rust and C code.

The new `ObjectID` struct serves as the Rust counterpart to Git's core object identifier representation, with identical memory layout (`#[repr(C)]`) to enable direct passing between languages. It includes both the hash bytes and algorithm identifier, matching Git's internal structure. The implementation prioritizes performance (noted as important for hot paths like `index-pack`) over Rust idioms, reflecting the pragmatic approach needed for this foundational interoperability work.

This is patch 4/16 in part 2 of the series (v3), which focuses on Rust-based loose object mapping. The change is straightforward but significant - it establishes the basic type that will be used throughout the Rust/C boundary for object identification. The patch includes the expected build system updates (Makefile and meson) to incorporate the new source file.

The implementation appears clean and well-contained, with proper licensing and documentation. As part of Git's first major production Rust code, this represents an important building block for the hash algorithm interoperability effort. The patch follows the series' pattern of careful, incremental changes to introduce Rust infrastructure while maintaining compatibility with existing C code.