Part of the UTF-8 config subsections series, addressing memory leaks in alias handling. This email from Junio Hamano responds to Jonatan Holmgren's two follow-up patches (documentation fixes and empty subsection handling) by pointing out memory leaks reported in the thread and proposing fixes.

The proposed changes modify two locations where string lists containing alias names are cleared - in `git.c` and `help.c`. The key fix is changing the `string_list_clear()` calls to free the string data (by passing `1` instead of `0`).

This continues the thread's focus on polishing the UTF-8 subsection implementation before final merge. The memory leak fixes join other recent improvements like documentation formatting and empty subsection handling. Junio's response indicates these should be the final fixes needed, suggesting the series is nearing completion after addressing the leak reports from Jacob Keller.

The changes are mechanical but important for correctness, ensuring proper cleanup of allocated strings when listing aliases. The diff shows Junio's typical direct style - identifying the issue, proposing a solution, and asking the author to verify and incorporate it. This maintains the thread's trajectory toward resolving all outstanding issues before the feature graduates from `next` to `master`.