This is the final patch in Eslam Reda Ragheb's series enhancing `git repo info` with structured path reporting and object metrics. The patch focuses on code quality improvements in the metrics output formatting.

The change refactors the metrics printing code to use two new helper functions - `print_keyvalue_size()` for single-value metrics and `print_object_values()` for per-object-type metrics. This reduces code duplication while maintaining identical output format. The refactoring makes the 156-line function more maintainable by breaking it into logical units and eliminating repetitive printf patterns.

The patch is a straightforward internal cleanup that doesn't change functionality, just improves code organization. It comes after the series has already added comprehensive path reporting and metrics capabilities, with thorough test coverage and documentation completed in earlier patches. The change appears well-contained and follows the project's preference for small, focused improvements.

As the 10th patch in v4 of this series, it represents the final implementation polish before potential merging. The series as a whole provides a robust foundation for programmatic repository inspection, with this patch ensuring the metrics output code is as maintainable as the rest of the feature.