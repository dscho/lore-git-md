This is a memory leak fix for the UTF-8 alias subsection implementation, now in its 7th iteration. Jacob Keller confirms Junio Hamano's proposed fixes for two memory leak paths in the alias listing functionality. The changes ensure proper cleanup of heap-allocated alias command strings in both `git.c` (via `string_list_clear(&list, 1)`) and `help.c` (via `string_list_remove_duplicates(list, 1)`). 

With Junio's confirmation that these changes look correct, this appears to be the final fix needed for the memory management aspects of the feature. The series is now in `next` with only one remaining open issue - a regression affecting dot-prefixed aliases that impacts Johannes Schindelin's workflow. The memory leak fixes complete the technical implementation work, leaving just this backward-compatibility question to resolve before final merge.