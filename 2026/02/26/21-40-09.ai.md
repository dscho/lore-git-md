This is a feature patch in the ongoing series about configurable cover letter formatting for `git format-patch`. The email focuses on a specific implementation detail regarding validation timing for the cover letter format string.

The author (Mirko Faina) is seeking confirmation from Junio C Hamano about whether to keep an early validation check for the cover letter format string. The check currently happens before any files are created, which the author feel is cleaner error handling. Junio had suggested in a previous review that validation could potentially be deferred to the point of use in `make_cover_letter()`, since that function would need to validate the format anyway.

The thread context shows this is part of a broader effort to make cover letter formatting configurable, with recent discussion expanding to include patch numbering customization. The current patch maintains all prior maintainer-squashed fixes while working through these design decisions.

Key technical points at play:
- The validation occurs in `cmd_format_patch()` before file creation
- Alternative would be to validate in `make_cover_letter()` after file creation
- The author prefers early validation to avoid partial output
- Junio's review implies the later validation might be sufficient

This represents the kind of implementation refinement that often happens in later rounds of Git patch review - working through the tradeoffs of when and how to validate user input. The author appears responsive to feedback while making a case for their preferred approach based on clean error handling semantics.