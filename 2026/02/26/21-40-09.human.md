This is a follow-up discussion in the ongoing series about configurable cover letter formatting for `git format-patch`. The author, Mirko Faina, is seeking clarification on whether to keep an early validation check for the cover letter format string.

The patch series introduces a new `commitListFormat` configuration that allows customizing how commit lists appear in cover letters. The current discussion focuses on where exactly to validate the format string. Junio Hamano had suggested that validation might not need to happen early in the control flow, since the code that actually uses the format string would validate it anyway.

Mirko explains their rationale for the early check - it prevents `format-patch` from creating files or printing output before failing on an invalid format. The author is now asking for confirmation on whether to keep this defensive early check or remove it in favor of letting `make_cover_letter()` handle validation.

This is a relatively minor implementation detail in an otherwise well-advanced patch series that has already incorporated several rounds of feedback. The core functionality around format string processing and validation appears solid, with comprehensive error checking already in place. The thread shows the careful attention to edge cases and error handling that characterizes mature Git contributions.