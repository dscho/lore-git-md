This is a documentation patch in the ongoing UTF-8 alias subsections series, now at version 7 in the `next` branch. The patch fixes a formatting issue in the alias documentation where a list continuation marker (`+`) was missing, causing improper rendering of the example showing the equivalence between `alias.last` and `alias.last.command` syntax.

The change is straightforward - it adds the missing `+` markers to properly connect the shell session block with the following explanatory text. This was one of the final polish items noted in the thread's summary, and the patch confirms all documentation issues in the series have now been resolved.

The series is nearly ready for final merge, with only one remaining issue (dot-prefixed alias regression) still to be addressed. This documentation fix is part of the final cleanup before that last hurdle.