This is a follow-up discussion in the ongoing thread about configurable cover letter formatting for `git format-patch`. The specific focus here is on when to validate the format string - whether to check it in `cmd_format_patch()` (early) or let `make_cover_letter()` handle it (later).

Junio C Hamano responds to Mirko Faina's question about keeping an early validation check. He suggests that while early validation could prevent wasted work, the validation should fundamentally live in `make_cover_letter()` since it's the function that actually uses the format. Duplicating the check is only worthwhile if it provides clear user benefit by failing fast before any files are created.

The exchange shows the project's careful consideration of error handling patterns - balancing early failure against code duplication. Junio's response maintains the project's preference for clean, maintainable code where validation lives closest to where the data is used, unless there's a compelling user-facing reason to do otherwise.

This is a relatively minor implementation detail in the larger feature, but it demonstrates the project's attention to both technical correctness and user experience in the review process. The thread continues to refine the implementation while maintaining all prior maintainer-approved changes.