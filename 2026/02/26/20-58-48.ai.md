This is a refactoring patch in the context of Taylor Blau's MIDX/bitmap repacking series. Junio C Hamano proposes a modification to Taylor's `strvec_init_alloc()` function to better handle the zero-allocation case consistently with Git's existing `strvec` optimization.

The patch makes two key changes:
1. Moves the zero-allocation optimization (which avoids allocating memory when no elements are needed) into `strvec_init_alloc()`
2. Makes `strvec_init()` a simple wrapper that calls `strvec_init_alloc()` with zero allocation

The change ensures that both initialization paths (explicit zero allocation via `strvec_init_alloc(&array, 0)` and the traditional `strvec_init()`) behave identically by sharing the same optimization. This maintains the existing memory efficiency while providing a cleaner internal implementation.

The patch touches only `strvec.c`, modifying the initialization logic while keeping all other `strvec` functionality unchanged. This is a preparatory change supporting Taylor's larger series, specifically helping with MIDX string handling where the number of elements may be known in advance.

Junio's implementation carefully preserves the empty-strvec optimization that avoids allocating memory for zero-element cases, while still allowing pre-allocation when the size is known. The change demonstrates Git's characteristic attention to both performance (via the zero-allocation optimization) and code maintainability (by centralizing the initialization logic).