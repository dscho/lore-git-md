This is a follow-up on the push group functionality thread, where the maintainer (Junio C Hamano) checks in on progress of a volunteered implementation. The original thread identified an inconsistency where `git push group` fails while `git fetch group` works for remote groups defined in config.

Usman Akinyemi had previously volunteered to implement the shared remote group infrastructure that would enable push to gain the same group expansion behavior as fetch. This email from Junio serves as a gentle nudge, asking if any work has been done since the initial commitment. No technical details or patches are discussed here - it's purely a status check on the implementation that was previously agreed to be worthwhile.

The change would involve refactoring group handling code from builtin/fetch.c to remote.c to make it between both fetch and push operations. While the technical approach was outlined in the original thread, this email indicates the work may have stalled after the volunteer stepped forward. The next expected development would be either patches from Usman or further discussion if obstacles have been encountered.