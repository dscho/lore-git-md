Test modernization patch updating t7605-merge-resolve.sh to use the preferred test_path_is_file helper instead of direct test -f checks. This is part of the ongoing effort to standardize test assertions across Git's test suite.

The change replaces three instances of `test -f` with `test_path_is_file` in the merge_c1_to_c2_cmds test block. The commit message notes this was found via a grep for legacy test patterns. The patch is straightforward and mechanical - exactly the sort of cleanup the project encourages.

This appears to be Mansi's first contribution to Git (based on the email address and sign-off format). The patch follows the expected conventions and touches only test infrastructure, making it low-risk. Reviewers will likely check that the test still fails appropriately when the expected files are missing, but the change itself looks correct.