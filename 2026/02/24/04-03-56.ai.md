Bugfix patch addressing a NULL pointer dereference in `commit_format_is_empty()` discovered during implementation of configurable cover letter formats. This is the first of three patches in a series implementing printf-style formatting for `git format-patch` commit lists.

The issue occurs when checking whether a user-specified format string exists. The function incorrectly dereferences `user_format` before checking if it's NULL, causing a crash when no format is set. The fix changes the condition to check the pointer itself rather than its contents.

This is a straightforward correctness fix that prevents a segfault in the pretty-printing code. The patch is minimal (a single line change) and clearly motivated by the bug discovered during development of the larger feature. It follows Git's convention of splitting fixes into separate patches even when found during feature work.

The change touches `pretty.c`, which handles commit formatting across Git. While the impact is limited to the specific function, reviewers will want to verify this doesn't affect other formatting code paths. The fix aligns with the thread's established direction and has no visible controversy - it simply corrects an implementation error found while building an approved feature.