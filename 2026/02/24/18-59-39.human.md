This is a refactoring patch in Taylor Blau's multi-pack-index compaction series, which is now in its third iteration. The patch renames `get_midx_checksum()` to `midx_get_checksum_hash()` to follow Git's documented naming conventions for struct-related functions. The new name also clarifies that the function returns a raw hash (not hex-encoded), setting up for a future `_hex()` variant.

The change is mechanical but thorough, updating all callers across the codebase including midx-write.c, pack-bitmap.c, and test helpers. Patrick Steinhardt suggested the rename, which aligns with documentation added in commit 541204aabea. The patch is well-contained and clearly motivated, with no behavior changes beyond the function name.

As part of the larger MIDX compaction effort, this is a small but meaningful step toward more consistent API naming. The series as a whole has addressed all technical feedback and appears ready for merging pending Junio's final review.