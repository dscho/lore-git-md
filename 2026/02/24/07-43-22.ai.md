This is a technical discussion thread about NULL pointer handling in the cover letter formatting series. The debate centers on whether `commit_format_is_empty()` should use `BUG()` when encountering a NULL `user_format` with `CMIT_FMT_USERFORMAT` set (Junio's position) or silently handle it as valid for config probing (author Mirko Faina's position).

The email shows Mirko analyzing the `get_commit_format()` function to understand how it handles NULL arguments, suggesting the current behavior might be a bug. He CC's Jeff King (Peff) for input, indicating this touches deeper questions about the pretty-printing subsystem's contracts. The thread remains blocked on resolving this fundamental design question before the series can progress. The discussion is narrowly focused on implementation correctness rather than the feature's user-facing aspects.