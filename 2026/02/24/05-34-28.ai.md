This is a test modernization patch, part of the ongoing effort to replace legacy test patterns with more robust helper functions. Mansi Singh updates t7605-merge-resolve.sh to use `test_path_is_file` instead of direct `test -f` calls in the merge_c1_to_c2_cmds block.

The change is straightforward - three instances of `test -f` checking for c0.c, c1.c and c2.c are replaced with the preferred test helper. The commit message notes this was found via a grep for legacy test patterns across the test suite. The helper provides better failure messages and follows current conventions.

This appears to be a clean, mechanical change with no functional impact. The patch is small and focused, touching only the test file with no other dependencies. It aligns with the project's test modernization efforts and should be uncontroversial. The commit message could be slightly more detailed about why this particular test was chosen for updating, but the technical content looks correct.