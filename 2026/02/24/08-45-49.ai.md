This is the fifth patch in Patrick Steinhardt's series to make geometric repacking the default maintenance strategy, continuing the pattern of updating tests to explicitly use the "gc" strategy where needed. The patch modifies t5510-fetch.sh to ensure the test's auto-gc verification continues working as expected after the strategy change.

The change is straightforward - it adds `maintenance.strategy=gc` to a test that verifies auto-gc behavior during fetches. This maintains the test's existing expectations about how temporary objects are handled, which currently relies on the gc strategy's pruning behavior. The test specifically checks that auto-gc doesn't lock up when fetching with low pack limits.

Like previous patches in the series, this is a mechanical update that follows the established pattern of preserving current test behavior while preparing for the eventual switch to geometric repacking. The patch is unlikely to need discussion as it's a clear-cut case of test maintenance for the upcoming change.