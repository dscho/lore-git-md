This thread has been working through the `--trailer` option for `git rebase`, now in its 8th iteration. Phillip Wood (with Li Chen assisting) has led the effort to enable in-process trailer manipulation during rebase by refactoring trailer handling to be reusable across commands. The series avoids forking `git interpret-trailers` while maintaining existing functionality.

A key discussion point about trailer ordering has been resolved. While Junio initially suggested following command-line order, the consensus settled on matching `git commit` behavior where signoffs always appear after other trailers. The implementation now handles all rebase modes, preserves trailers through conflicts, and properly processes fixup/squash commits while maintaining this ordering.

The technical work is largely complete, with core trailer logic moved to `process_trailers()` while keeping I/O in `interpret_trailers()`, using atomic tempfile writes, and sharing helper functions across commands. All major reviews from Phillip Wood and Kristoffer Haugsbakk have been addressed, with both authors now signed off.

Just a few final tasks remain before merging: documentation updates and some error handling improvements to the rebase state file. The series represents a significant step in making trailer handling more reusable across Git commands while maintaining backward compatibility.