This is a feature patch series implementing configurable cover letter formatting for `git format-patch`, following up on earlier design discussions. The series builds on maintainer Junio Hamano's guidance to use a printf-style format string approach.

The series introduces two main changes: a new `--cover-letter-format` command-line option and a `format.commitListFormat` configuration setting. These allow customizing how commits are listed in cover letters, with the default preserving the current "[N/M] subject" format. The implementation modifies `builtin/log.c` to handle the new formatting options while fixing a NULL pointer dereference in `pretty.c` that was discovered during development.

The patches show careful attention to the maintainer's requirements from earlier discussion, particularly around keeping the command-line and config options distinct. The NULL dereference fix in patch 1/3 is a straightforward correctness improvement that stands on its own merits. The implementation appears clean and well-motivated, with the config option following Git's standard patterns.

This represents the implementation phase of a feature that has already gone through design review, so it's likely to progress smoothly unless unexpected edge cases emerge in testing. The changes are focused and don't appear to introduce significant new complexity.