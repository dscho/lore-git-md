This is part of Taylor Blau's multi-pack-index compaction series, specifically introducing a convenience function for working with MIDX checksums. The patch adds `midx_get_checksum_hex()` to complement the existing `midx_get_checksum_hash()`, reducing repetitive hex-encoding boilerplate in callers that need the checksum in string form.

The new function follows Git's established pattern for similar hash-to-hex conversions, returning a pointer to a static buffer containing the hexadecimal representation of the MIDX's checksum. This matches how functions like `oid_to_hex()` work elsewhere in the codebase. The patch updates several callers in midx-write.c, pack-bitmap.c, and test helpers to use the new function, making the code more concise and consistent.

This change builds on Patrick Steinhardt's earlier suggestion to rename the checksum-related functions for clarity, now establishing a clear pair: `midx_get_checksum_hash()` for raw bytes and `midx_get_checksum_hex()` for the hex string representation. The implementation is straightforward and well-contained, with no apparent technical concerns. As part of the larger MIDX compaction series, this is a small but useful quality-of-life improvement for working with MIDX checksums.