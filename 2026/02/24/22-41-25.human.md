This is a technical discussion about extending `git fast-import`'s signature handling capabilities, specifically regarding the proposed `re-sign-if-invalid` mode and its interaction with hash algorithm conversion.

brian m. carlson responds to Justin Tobler's patch series, focusing on the handling of `extensions.compatObjectFormat` when regenerating signatures. They provide concrete technical guidance about the existing APIs for object ID conversion (`repo_oid_to_algop` and `convert_object_file`) that could be used to properly handle hash algorithm interoperability when re-signing commits. The response suggests either properly implementing the compatibility object conversion now (with testing instructions) or failing explicitly if not implemented, rather than silently ignoring the compatibility format.

The message carries significant technical weight as brian m. carlson is the primary architect behind Git's hash algorithm interoperability work. They emphasize that the current `compatObjectFormat` implementation is still experimental and subject to change, recommending either full support or an explicit failure mode to prevent future compatibility issues. The suggestion to add specific test coverage (modeled after t7004's signed tag tests) indicates this is being treated as a potential data-corruption surface that needs careful handling.

This moves the thread forward by providing clear implementation options for one of the open questions from the initial proposal, while setting expectations about the experimental nature of the hash algorithm interoperability features being discussed.