Here's a human-readable summary of the thread so far:

Titania Sugiarto proposed adding selective file/folder merging between branches, noting that Git currently replaces entire files during merges rather than allowing partial merges. The initial proposal identified this as a user-requested feature but didn't specify implementation details or how it would integrate with Git's existing merge machinery.

Thomas Guyot responded by suggesting workarounds using existing Git features - specifically using `git merge --no-commit` followed by manual index editing to exclude unwanted changes, or using cherry-picking for commit-level selection. While these approaches provide some selective control, they don't address the core proposal of implementing true partial-file merging at the merge strategy level.

The discussion remains at a conceptual stage, with no technical details yet about how selective merging would work with Git's merge strategies (recursive/ort) or handle conflicts. The thread would benefit from more concrete use cases demonstrating where current workarounds fall short and what new functionality would uniquely provide.