This is a feature patch (part 2/2) adding configurable cover letter formatting to `git format-patch`. The patch builds on the command-line option introduced in patch 1/2 by adding a matching `format.commitListFormat` configuration option.

The implementation follows the same pattern as the command-line version, allowing either "shortlog" (the default) or a "log:<format>" specification to control how commits are listed in cover letters. The config option is handled through Git's standard configuration machinery, with proper memory management via `format_config_release()`.

Junio C Hamano's review notes several style and organization issues that could be improved: unnecessary whitespace changes mixed with the feature work, overly long lines that should be wrapped, and questions about whether some implementation choices (like reusing the pretty_print_context) are intentional. He suggests the feature would have been cleaner if split into preliminary refactoring steps first.

The patch appears technically sound but could benefit from addressing Junio's feedback about code organization and style. The feature itself is well-motivated - it gives patch submitters more control over how their changes are presented in cover letters, addressing a common complaint that the default shortlog grouping by author obscures chronological order.

For the AI brief: This completes the cover letter formatting feature by adding config support alongside the existing command-line option. The config key is `format.commitListFormat` with identical syntax to `--cover-letter-format` ("shortlog" or "log:<format>"). Implementation shares core formatting logic between both interfaces using `repo_format_commit_message()`. The changes are focused in `builtin/log.c` with careful attention to memory management. Junio's review suggests some code organization improvements but does not raise fundamental technical objections.