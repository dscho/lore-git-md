This is a feature patch in a series about configurable cover letter formatting for `git format-patch`. Jeff King (Peff) provides a detailed technical review of patch 2/3, which introduces the core formatting engine via `--cover-letter-format`.

Peff identifies that the current implementation incorrectly uses `get_commit_format()` and `pp_commit_easy()` from the pretty-printing subsystem, which relies on a problematic global `user_format` variable. He explains the historical context behind this design and why it's unsuitable for the cover letter use case. Instead, he recommends using `repo_format_commit_message()` which avoids the global state and provides a cleaner interface for one-off formatting.

The review includes a concrete diff showing how to restructure the implementation to use the correct API. Peff acknowledges the pretty-printing subsystem's complexity ("a horrible minefield of booby traps") but provides clear guidance on navigating it. He also notes this is part of a larger architectural issue with the pretty-printing code that may need future cleanup.

This email represents a key turning point in the series - Peff's expertise resolves the technical debate from patch 1/3 and provides the architectural direction needed for patch 2/3. The author will need to rework the implementation based on this feedback before the series can progress. The core functionality (printf-style formatting for cover letter commit lists) remains sound, but the integration with Git's internals requires adjustment to respect subsystem boundaries and avoid global state.