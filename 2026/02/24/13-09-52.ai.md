This is a documentation formatting fix for the ref storage backend configuration series, which has already been approved for integration. Karthik Nayak responds to Kristoffer Haugsbakk's observation about incorrect list continuation markers appearing in the HTML output of the `git-config(1)` man page.

The patch removes the `+` list continuation markers that were causing formatting artifacts in the rendered documentation. The change is minimal - just removing the markers while keeping the content flow intact. The fix maintains all the technical information about reference storage formats and payload specifications, including the URI syntax (`<format>://<payload>`) and path resolution rules.

This appears to be the final polish on the documentation side for the ref backend series, following earlier substantive changes that implemented the configuration mechanisms. The thread context shows the overall series has gone through eight iterations with extensive review and is now cleared for merging.