This is a technical discussion about NULL pointer handling in Git's pretty-printing subsystem, specifically around the `commit_format_is_empty()` function. Jeff King (Peff) weighs in on the debate between Mirko Faina and Junio C Hamano about whether NULL `user_format` with `CMIT_FMT_USERFORMAT` should trigger a programming error or be silently handled.

Peff clarifies that the function's original purpose was to check for empty format strings (NUL terminator as first character), not NULL pointers. He confirms that NULL `user_format` with `CMIT_FMT_USERFORMAT` violates the subsystem's assumptions, though existing code paths appear safe by design. The discussion reveals Mirko's patch 1/3 incorrectly conflated NULL checking with empty-string checking.

The exchange highlights a deeper architectural question about whether new code should work around these assumptions or enforce them strictly. Peff indicates he'll address the higher-level design questions in a separate response to patch 2/3, suggesting the current approach may need rethinking. This keeps the broader cover-letter formatting feature blocked until these foundational questions are resolved.