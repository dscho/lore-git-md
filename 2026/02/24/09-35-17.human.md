This is a cleanup patch in a series about improving signature handling in `git fast-import`. The first patch removes an obsolete function declaration from commit.h that was left behind when `sign_with_header()` was removed in a previous commit (6206089cbd). 

Patrick Steinhardt confirms the cleanup is correct, noting the function no longer exists. This is a straightforward housekeeping change that prepares for the second patch in the series, which will add a new `re-sign-if-invalid` mode to `git fast-import` for handling invalid commit signatures during import. The series appears well-structured, with this initial patch cleaning up technical debt before introducing new functionality.