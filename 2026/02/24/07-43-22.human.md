This is a follow-up in the ongoing discussion about NULL pointer handling in the cover letter formatting series. The author, Mirko Faina, is now questioning whether the original issue (a NULL dereference in `commit_format_is_empty()`) is actually a bug, based on examining how `get_commit_format()` handles NULL input.

The key observation is that `get_commit_format()` - which sets up the format state - explicitly checks for NULL input and falls back to default formatting in that case. This suggests the NULL case might be expected behavior rather than a programming error, contrary to Junio Hamano's earlier position that it should trigger a `BUG()`.

The email doesn't propose a specific resolution but seeks input from Jeff King (Peff), likely for his expertise in the pretty-printing subsystem. The thread remains blocked on this fundamental question about whether NULL format states represent valid configuration probing or programmer error.

This exchange highlights how the series has uncovered a deeper design question about the contract of format-handling functions in Git's pretty-printing code. The discussion is now moving from the specific patch to broader questions about the intended behavior of these interfaces.