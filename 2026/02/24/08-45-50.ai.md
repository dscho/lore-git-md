This is the sixth patch in Patrick Steinhardt's series to make geometric repacking the default maintenance strategy. The patch modifies t6500-gc.sh to explicitly set the maintenance strategy to "gc" rather than the upcoming default "geometric" strategy.

The test suite t6500-gc.sh specifically tests git-gc behavior and makes assumptions about the on-disk state of the repository. The patch ensures these tests continue to verify gc-specific behavior by forcing the traditional gc strategy rather than inheriting the new geometric one. This follows the pattern established in earlier patches of the series where tests that depend on gc behavior are updated to explicitly request it.

The change is straightforward - a single line addition to the test setup that sets maintenance.strategy=gc globally. This matches the approach taken in patches 4 and 5 for other test scripts with similar dependencies. The patch maintains the series' careful progression of first updating all test infrastructure before changing the default behavior in the final patch.

Technical details:
- Modifies t/t6500-gc.sh to set maintenance.strategy=gc
- Preserves test assumptions about gc's object packing behavior
- Follows same pattern as previous test updates in the series
- Part of systematic preparation for geometric strategy default
- Touches same configuration key (maintenance.strategy) as patches 4-5

The patch appears uncontroversial and aligns with the series' established approach to test updates. It represents another incremental step toward the geometric repacking default by ensuring gc-specific tests continue to work as intended.