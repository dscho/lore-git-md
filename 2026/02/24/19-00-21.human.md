This is a refactoring patch in Taylor Blau's multi-pack-index compaction series. It extracts a helper function `fill_pack_from_midx()` from the existing `fill_packs_from_midx()` to reduce code duplication. The new helper handles loading pack information from a MIDX file for a single pack, which will be reused in the upcoming compaction functionality.

The change is straightforward - it moves the pack preparation logic into a separate function while maintaining the same error handling. This is part of the larger effort to enable `git multi-pack-index compact` functionality while preserving MIDX chain structure and bitmap support. The patch appears well-contained and follows the series' pattern of careful refactoring before introducing new features.