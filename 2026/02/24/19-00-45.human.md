This is the final patch in Taylor Blau's series adding `git multi-pack-index compact` functionality with bitmap support. The patch enables reachability bitmaps during MIDX compaction by combining all existing bitmaps from the compacted layers.

The implementation preserves the pseudo-pack ordering from previous commits, treating all objects as non-preferred during compaction to maintain consistent object ordering. The changes include:

1. Adding `--[no-]bitmap` option to the `compact` subcommand documentation and usage
2. Modifying the pack order calculation to ignore preferred flags during compaction
3. Extensive test coverage verifying bitmap preservation and object selection

The commit message notes this lays groundwork for more sophisticated bitmap strategies in future, like generating new bitmaps when references have moved significantly. The patch completes the technical implementation of the series, with all prior review concerns addressed in previous iterations.

This appears ready for merging - it has thorough test coverage, clear documentation updates, and builds on the well-reviewed infrastructure from earlier patches in the series. The changes are focused and maintain backward compatibility while enabling the new bitmap functionality.