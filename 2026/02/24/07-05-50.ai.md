This is the fourth patch in a 5-part series (v7) adding `--trailer` support to `git rebase`. The patch refactors how `--trailer` arguments are handled in `git commit` and `git tag` to align with the new trailer processing infrastructure introduced earlier in the series.

The key change switches from using `OPT_PASSTHRU_ARGV` to `OPT_STRVEC` for parsing `--trailer` options, which means the trailer arguments no longer include the `--trailer=` prefix. This allows removing the prefix-stripping logic in `amend_file_with_trailers()` since the callers now pass the raw trailer values directly. The change affects both the commit and tag commands, updating their option parsing while maintaining the same user-facing behavior.

The patch is a straightforward mechanical change that builds on the earlier refactoring work. It demonstrates good attention to backward compatibility by noting the historical context of the `--trailer=` prefix handling while cleaning it up in a way that simplifies the code. The change is well-contained, touching only the necessary files with minimal churn.

This appears to be a clean, incremental improvement to the series that helps unify the trailer handling across commands. As part of the larger effort to enable in-process trailer manipulation during rebase, it helps move away from the fork-based approach while maintaining compatibility with existing callers.