This is a technical follow-up in the configurable cover letter formatting series, where Jeff King (Peff) provides implementation guidance for patch 2/3. The patch aims to add `--cover-letter-format` support to `git format-patch`.

Peff identifies that the current approach incorrectly uses `get_commit_format()` and `pp_commit_easy()`, which rely on a problematic global `user_format` variable. Instead, he recommends using `repo_format_commit_message()` as a cleaner alternative that avoids the global state. He provides a concrete diff showing how to restructure the formatting code to use this interface properly.

The email also includes broader context about the historical challenges in Git's pretty-printing subsystem, explaining why the current design has these rough edges. Peff notes this is part of a larger architectural issue that may need addressing in the future, but for now the suggested approach provides a clean solution for the immediate feature.

This is a valuable review that both unblocks the current series and documents important subsystem constraints. The suggested changes should make the implementation more robust while maintaining the desired functionality. The thread shows healthy collaboration where an experienced contributor helps guide a newer one through Git's internals.