This is a preparatory patch in Taylor Blau's multi-pack-index compaction series, now in its third iteration. The patch marks the `get_midx_checksum()` function's argument as `const` to clarify that the function doesn't modify the MIDX structure it operates on. This is a straightforward code hygiene change that follows Git's coding conventions for const-correctness.

The change is minimal (just two lines modified) and well-explained in the commit message, which also hints at a future rename of this function in the next patch to better reflect its behavior. As the first patch in a larger series, this serves as groundwork for the more substantial MIDX compaction functionality that follow. The change is uncontroversial and unlikely to need further revision.