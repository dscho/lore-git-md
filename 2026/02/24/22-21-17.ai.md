The maintainer signals tentative approval for a simplified version of the branch comparison feature in `git status`. Junio C Hamano responds positively to Jeff King's proposal to initially limit the `status.compareBranches` config to only support `@{upstream}` and `@{push}` comparisons, deferring more complex ref resolution cases. This represents a potential middle path forward for Harald Nordgren's long-running patch series.

Jeff's suggested approach would handle the core use case (comparing against upstream/push targets) while avoiding the edge cases around arbitrary ref resolution that surfaced in earlier discussion. The implementation would manually resolve these special refs rather than using the full `dwim_ref` machinery. The door remains open to expand the feature later if demand emerges, possibly with placeholder substitution syntax like `%s` for the current branch name.

This exchange suggests the maintainer sees merit in the feature's basic premise but prefers to start with a more constrained version. The thread remains open for further discussion, but Junio's response indicates the most likely path forward would be a simplified initial implementation focused on the upstream/push comparisons that multiple contributors have identified as their primary need.