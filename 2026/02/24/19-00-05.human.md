This is a technical patch in Taylor Blau's ongoing series to add `git multi-pack-index compact` functionality. It fixes a subtle issue in how bitmapped pack positions are assigned during MIDX (multi-pack-index) writing.

The patch removes an unnecessary dependency on the `pack_perm` array when initializing bitmap positions, instead indexing directly into the pack info array. This prepares for future compaction work where the current approach would cause problems. The change is small (just one line) but important for correctness, ensuring bitmap positions are properly initialized regardless of pack ordering.

The commit message clearly explains the current behavior, why it's problematic for upcoming compaction work, and how the change addresses it. This appears to be a well-justified preparatory change in a mature patch series that has already undergone significant review. The fix is straightforward and unlikely to be controversial.