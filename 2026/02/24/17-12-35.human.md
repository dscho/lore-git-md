This is a documentation patch fixing a formatting issue in the alias configuration documentation, part of the ongoing UTF-8 subsection support effort.

The patch corrects a rendering problem where the example showing the equivalence between `alias.last` and `alias.last.command` was missing list continuation markers (`+`). The missing markers caused the explanatory text to detach from the list item in the rendered output. The change adds proper AsciiDoc list continuation syntax while preserving all the technical content.

This is a straightforward documentation fix that follows up on earlier discussion in the thread. It addresses one of the minor polish items identified during review of the UTF-8 subsection feature that was recently merged to `next`. The patch maintains the existing explanation about command hiding behavior while just fixing the formatting.

The change is clean and focused - it doesn't introduce any new content or behavior, just corrects the documentation markup. As part of a v7 series that's already in `next`, this appears to be a final polish before the feature stabilizes.