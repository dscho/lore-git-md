Maintainer follow-up on cover letter formatting feature. Junio C Hamano has queued Mirko Faina's v2 patch series in the 'seen' branch with some build and error handling fixes, but notes several outstanding review comments remain unaddressed. The series adds configurable format for commit lists in `git format-patch` cover letters via both command-line (`--cover-letter-format`) and config (`format.commitListFormat`) options.

Key changes in this update:
- Fixes const-correctness issue by making `cover_letter_fmt` const
- Adds missing error handling for invalid format strings
- Simplifies the default format selection logic
- Includes a completion script update for the new option

Junio indicates the series still needs work on memory management, test coverage, and addressing his earlier review feedback about potential leaks and validation timing. The current state represents progress but isn't yet ready for promotion to 'next'. The maintainer has made some temporary fixes (marked with "SQUASH???") while waiting for the author to address the remaining issues.