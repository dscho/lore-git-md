This is a follow-up in the Linux fsmonitor implementation thread, now at version 5. The patch adds inotify-based filesystem monitoring for Linux, completing the cross-platform fsmonitor support alongside existing Windows and macOS backends.

Paul Tarjan reports having squashed Patrick Steinhardt's meson build support changes into the v5 patch, addressing Junio Hamano's request to integrate the build system updates. However, CI is now failing with a new error unrelated to the meson changes. The thread context indicates this implementation has been stable in production use for two months, with only two small memory leaks remaining (512-byte at startup and 40-byte in IPC handling).

Patrick had previously suggested keeping the meson support integrated rather than split out, and now confirms this approach after reviewing v5. The technical implementation appears complete with all major concerns addressed across five iterations. The remaining CI failure appears to be a new issue that will need investigation, separate from the previously resolved concerns about memory safety and build system integration.

Key subsystems touched include the fsmonitor daemon backend, inotify event handling, and meson build configuration. The patch represents the final iteration before potential merging, pending resolution of the CI failure and Junio's approval. The thread shows consensus forming around treating the remaining memory leaks as non-blocking given their small size and the implementation's proven stability in production.