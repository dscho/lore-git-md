This is a refactoring patch in Taylor Blau's multi-pack-index compaction series (patch 4/17). It centralizes the `--progress` option handling for all `git multi-pack-index` subcommands by moving it into the common options array, eliminating duplicate declarations in each subcommand. The change is straightforward: the progress flag was already supported by all subcommands but was redundantly defined in each one's option parsing code. The patch also updates the documentation to note that `--progress` is a common option.

The change is a minor cleanup that doesn't affect functionality but improves maintainability by reducing code duplication. It's part of the larger effort to improve MIDX infrastructure, though this particular patch is purely about code organization rather than the core compaction feature. The patch has no visible impact on users beyond the documentation update noting the option's availability across subcommands.