This is a technical discussion about NULL pointer handling in Git's pretty-printing subsystem, specifically around the `commit_format_is_empty()` function in `pretty.c`. Jeff King (Peff) weighs in on the debate between Mirko Faina and Junio C Hamano regarding whether NULL `user_format` with `CMIT_FMT_USERFORMAT` should trigger a `BUG()` or be handled silently.

Peff clarifies the original intent of `commit_format_is_empty()` - it was designed to check for empty format strings (NUL terminator as first character), not NULL pointers. He asserts that `user_format` should never be NULL when using `CMIT_FMT_USERFORMAT` in the existing code paths, as this is enforced by `save_user_format()` and `get_commit_format()`. 

The discussion reveals that Mirko's patch 1/3 incorrectly conflates NULL pointer checking with empty string checking. Peff suggests the real issue may lie in how patch 2/3 interacts with these assumptions rather than needing changes to the core `commit_format_is_empty()` behavior. He indicates he'll address the architectural concerns separately when reviewing patch 2/3.

This email moves the discussion forward by:
1) Confirming the original contract of `commit_format_is_empty()`
2) Validating Junio's position that NULL `user_format` with `CMIT_FMT_USERFORMAT` indicates a programming error
3) Identifying that Mirko's approach in patch 1/3 would break the empty-string detection feature
4) Hinting that the solution may require adjusting patch 2/3's design rather than changing core pretty-printing behavior

The thread remains blocked pending Peff's review of patch 2/3, which may provide the architectural guidance needed to resolve the NULL handling question while maintaining the existing pretty-printing subsystem contracts.