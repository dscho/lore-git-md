This is the final patch in Taylor Blau's series adding `git multi-pack-index compact` functionality with bitmap support. The patch enables reachability bitmaps during MIDX compaction by combining all existing bitmaps from the compacted layers while preserving the pseudo-pack ordering established in previous patches.

Key technical aspects:
- The compaction process treats all objects as non-preferred during bitmap generation to maintain consistent object ordering
- The implementation builds on the MIDX v2 format's ability to handle arbitrary pack ordering
- New test cases verify bitmap preservation works correctly through compaction operations
- Documentation is updated to reflect the new `--bitmap` option for the compact subcommand

The patch demonstrates combining existing bitmaps as a foundation for more sophisticated bitmap strategies in future work. It completes the series' goal of providing infrastructure for efficient MIDX layer compaction while maintaining bitmap compatibility.

The changes include:
- Adding `--bitmap` option to `git multi-pack-index compact`
- Modifying `midx_pack_order()` to handle compaction cases
- Extensive new test cases in t5335 verifying bitmap behavior
- Documentation updates for the new option

This appears ready for merging, having addressed all prior review feedback and demonstrating robust test coverage of the bitmap preservation behavior. The series as a whole provides important infrastructure for future repacking strategies while maintaining backward compatibility.