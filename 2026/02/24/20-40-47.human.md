This is an RFC patch proposing a behavior change for the experimental `git repo info` command, which currently shows no output by default. The author, SoutrikDas, suggests making it show all available fields when run without arguments, arguing this would be more user-friendly than the current silent behavior.

The patch is small (just 2 lines added) but touches on a design question that was previously debated in the original thread. The author references past discussions where Phillip Wood argued for requiring explicit field selection (the current behavior) as a plumbing command principle, while others saw value in having default output.

The change would make `git repo info` show its 4 available fields by default. The author acknowledges this might not be the right approach but offers it as a starting point for discussion, particularly as they're exploring this as a potential GSoC project. The patch appears correctly formatted with a Signed-off-by line.

This seems like a reasonable RFC to reopen the default behavior discussion, though reviewers may reiterate the plumbing command philosophy that favors explicit over implicit output. The patch's simplicity makes it a good conversation starter regardless of whether the change is ultimately accepted.