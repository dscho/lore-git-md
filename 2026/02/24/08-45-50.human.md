This is part 6 of Patrick Steinhardt's 8-patch series to make geometric repacking the default maintenance strategy. The patch updates t6500-gc.sh to explicitly use the "gc" strategy rather than the upcoming default "geometric" one, since the test suite specifically wants to verify git-gc behavior.

The change is straightforward - it adds a global config setting to force the traditional gc strategy for this test file. This follows the pattern established in earlier patches of the series where tests that depend on gc-specific behavior are updated to request it explicitly rather than relying on the default. The patch maintains the series' careful approach of preparing the test-by-test before changing the actual default behavior in the final patch.

As with previous test updates in this series, this appears to be a mechanical change that preserves existing test expectations while allowing the broader strategy change to proceed. The series continues to show Patrick's characteristic thoroughness in ensuring test coverage remains intact across the transition.