This is the second patch in Patrick Steinhardt's 8-part series to make geometric repacking the default maintenance strategy. The patch focuses on test reliability by disabling auto-maintenance in tests that verify object database structure.

The change modifies 11 test scripts to explicitly set `maintenance.auto=false` when testing packfile and object storage behavior. This ensures tests aren't affected by background maintenance operations that might alter the expected object database layout. The affected tests include pack delta chain verification, multi-pack-index behavior, and partial clone operations.

This is a preparatory patch that ensures test suite stability before the later patches change the default maintenance strategy. The changes are mechanical but important - they prevent flaky tests that could otherwise occur if maintenance ran during test execution. The patch follows the series' careful approach of first ensuring test reliability before making behavioral changes.

Reviewers should focus on whether all relevant tests are covered and whether the placement of the config changes makes sense in each test's flow. The patch appears thorough, touching all major test areas that interact with packfile structure.