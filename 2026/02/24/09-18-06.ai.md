This is a technical discussion about environment handling in `git for-each-repo`, continuing the thread about fixing worktree operation. Jeff King (Peff) responds to Eric Sunshine's suggestion about more comprehensive environment cleanup, agreeing that just unsetting `GIT_DIR` may be insufficient.

Peff notes the current patch unsets `GIT_DIR` in the parent process, which could affect other code running in the original repository. He suggests moving the environment cleanup to the sub-process instead, using the `env` list in the `child_process` struct. He points to `prepare_other_repo_env()` in run-command.h as a helper function that handles most Git environment variables while preserving config settings, but cautions it may need adaptation since it assumes control over `GIT_DIR` setting.

The discussion reveals deeper complexity around repository discovery - particularly how `for-each-repo` currently relies on `git -C` for path resolution, which interacts subtly with bare/non-bare repos and subdirectory cases. Peff outlines two potential approaches: either refactoring `prepare_other_repo_env()` to separate its concerns, or having `for-each-repo` handle repo discovery upfront. Both would require careful handling of edge cases around path resolution and environment inheritance.

This moves the conversation beyond the immediate fix, surfacing architectural questions about how Git commands should manage environment variables when operating across multiple repositories. The thread now awaits input from the original author (Derrick Stolee) on whether to expand the scope of changes.