This is a preparatory patch in Taylor Blau's multi-pack-index compaction series, which introduces a new `git multi-pack-index compact` subcommand. The patch makes a small but meaningful change to mark the `get_midx_checksum()` function's argument as `const`, clarifying that the function does not modify the MIDX structure it receives.

The change is straightforward - it serves as groundwork for a follow-up commit that will rename this function to better reflect its behavior (returning raw checksum bytes rather than a hex-encoded string). The patch touches both the implementation in midx.c and its declaration in midx.h, maintaining consistency between the two.

This is part of a larger 17-patch series that has already undergone significant review, with all technical feedback addressed by this v3 iteration. The change aligns with Git's coding conventions around const-correctness and helps prevent accidental modifications to the MIDX structure. The series as a whole is focused on the pack/midx subsystem and is currently under final review by maintainer Junio C Hamano for potential merging.