This is a follow-up in the configurable cover letter formatting series, specifically addressing patch 2/3. Mirko Faina acknowledges Jeff King's (Peff) detailed technical review and confirms understanding of the proper approach for handling NULL format strings in the cover letter generation code.

The email shows the author accepting Junio's earlier position on NULL handling as correct after Peff's explanation clarified the internal contracts of Git's pretty-printing subsystem. Mirko plans to drop the first patch of the series (which dealt with NULL handling) and proceed with implementing patch 2/3 using the recommended `repo_format_commit_message()` API instead of the problematic `get_commit_format()`/`pp_commit_easy()` combination.

Key technical points confirmed:
- NULL `user_format` with `CMIT_FMT_USERFORMAT` is properly treated as a programming error per Git's internal contracts
- The series will now use the correct one-off formatting API that avoids global state issues
- Memory management in `format_config_release()` remains sound
- Backward compatibility is maintained through the redesign

The thread shows productive technical discussion leading to a clear resolution of the architectural questions. With this clarification, the series can move forward with a technically sound implementation of the cover letter formatting feature. The author's responsiveness to the review feedback suggests the revised patches will properly integrate with Git's pretty-printing subsystem while providing the desired user-configurable behavior.