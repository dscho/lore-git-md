This is a refactoring patch in Taylor Blau's multi-pack-index compaction series, specifically renaming `get_midx_checksum()` to `midx_get_checksum_hash()` to follow Git's documented naming conventions for struct-related functions. The change aligns with the project's style guidelines established in commit 541204aabea, which prefers `structname_verb()` ordering over `verb_structname()`.

The patch updates all callers across the codebase, including midx-write.c, pack-bitmap.c, and test helpers. The new name also clarifies the function's behavior by adding "_hash" to indicate it returns a raw hash rather than a hex-encoded string, setting up for a potential future "_hex" variant. This is a straightforward mechanical change that doesn't affect functionality but improves code consistency and readability.

As part of the larger MIDX compaction series (now in its third iteration), this patch follows through on Patrick Steinhardt's suggestion and maintains the codebase's evolving naming standards. The change is well-contained and touches only the expected call sites, with no observable impact on behavior or performance.