This is a documentation patch in Taylor Blau's ongoing multi-pack-index compaction series. It aligns the SYNOPSIS section of the `git-multi-pack-index` man page with the actual `-h` help output, ensuring consistency between documentation and implementation.

The patch makes three changes: First, it expands the SYNOPSIS to properly document all subcommands (`write`, `verify`, `expire`, `repack`) and their options rather than the previous overly simplified version. Second, it updates the corresponding usage string in the C code to match. Third, it removes `multi-pack-index` from the test file that checks for documentation mismatches since the inconsistency is now fixed.

This is a straightforward documentation improvement that brings the man page in line with the actual command behavior. The changes are mechanical and uncontroversial, typical of the kind of polish that gets applied to a patch set as it nears completion. Since this is part of a larger series about MIDX compaction, the documentation update helps users understand the full range of multi-pack-index subcommands and options available to them.