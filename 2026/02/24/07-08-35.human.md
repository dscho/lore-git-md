This is a technical discussion about NULL pointer handling in the cover letter formatting series. The patch in question (1/3) fixes a NULL dereference in `commit_format_is_empty()` when checking user-defined formats, but maintainer Junio C Hamano questions whether silent handling is appropriate versus treating it as a programming error with `BUG()`.

Mirko Faina pushes back against using `BUG()`, arguing that the NULL check serves a legitimate purpose in complex config setups and that crashing seems too harsh for what might be a valid edge case. They note the function's name (`commit_format_is_empty`) doesn't imply it should crash on invalid states, and point out the static visibility of `user_format` makes this the only way to check its state externally.

The exchange reveals a philosophical difference: Junio sees the NULL case as an invariant violation indicating programmer error, while Mirko views it as a recoverable condition that callers might reasonably check. This foundational question must be resolved before the series can progress, as it affects how robust the cover letter formatting feature needs to be against malformed input. The discussion remains technical and constructive, focused on design intent rather than implementation details.