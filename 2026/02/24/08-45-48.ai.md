Performance patch (part of geometric repacking series) that updates test infrastructure.

This is the fourth patch in Patrick Steinhardt's series to make geometric repacking the default maintenance strategy. It modifies t5400-send-pack.sh to explicitly use the "gc" strategy rather than relying on the default, which will soon change to "geometric". The test verifies that git-receive-pack runs automated repository maintenance by checking for pruning of temporary objects - an effect that only occurs with the "gc" strategy.

The change is straightforward, adding a single config line to ensure the test continues working after the default strategy changes. This follows the series' pattern of methodically updating tests before changing behavior. The patch maintains the test's original intent while preparing for the upcoming default switch.

Key technical details:
- Modifies t5400-send-pack.sh to set `maintenance.strategy=gc`
- Preserves test behavior that expects gc-style pruning of temporary objects
- Part of broader test hardening before changing defaults
- Follows same pattern as patches 1-3 (test infrastructure updates first)

The patch appears uncontroversial and aligns with the series' careful approach. It's one of several preparatory test changes needed before the final behavior switch in patch 8. Reviewers have already approved the overall direction, and this mechanical test update seems unlikely to generate discussion.