This is part 5 of Patrick Steinhardt's 8-patch series to make geometric repacking the default maintenance strategy. The series is methodically updating tests to explicitly specify the "gc" strategy before the final switch to geometric repacking in patch 8.

This patch modifies t5510-fetch.sh to explicitly set `maintenance.strategy=gc` in a test that verifies auto-gc behavior during fetches. The test ensures Git doesn't lock up when automatically packing repositories during fetch operations. The change is straightforward - just adding one config line to maintain the test's current expectations about how maintenance behaves.

The patch follows the series' pattern of isolating test changes from the actual strategy switch. It's a mechanical update that shouldn't be controversial, similar to the previous test fixes in patches 1-4. The series continues to show careful attention to test stability before changing default behavior.