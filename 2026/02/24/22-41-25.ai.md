This is a technical discussion about signature handling in `git fast-import`, specifically around the proposed `re-sign-if-invalid` mode and its interaction with hash algorithm interoperability.

brian m. carlson provides detailed feedback on Justin Tobler's patch series that would add a mode to re-sign invalid commit signatures during import. The key technical points raised are about handling `extensions.compatObjectFormat` when regenerating signatures - an area where brian m. carlson has deep expertise from their work on SHA-256 interoperability.

The email suggests two approaches for dealing with hash algorithm compatibility: either properly implement support using existing conversion APIs (`repo_oid_to_algop` and `convert_object_file`), or explicitly fail when encountering compatibility objects rather than silently ignoring them. brian m. carlson strongly recommends the former approach and offers access to their `sha256-interop` branch for testing.

The message also recommends adding test coverage case similar to t7004's "signed tag with embedded PGP message" test to ensure proper handling when the feature is extended to tags. This reflects a concern about potential signature corruption during hash algorithm conversion scenarios.

This is a substantive technical review from a domain expert that will likely require Justin Tobler to revisit parts of the implementation. The feedback suggests the current approach of ignoring `extensions.compatObjectFormat` may not be sufficient for production use, and that proper hash algorithm conversion support should be considered either now or as a follow-up with clear failure modes.