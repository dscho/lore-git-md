Feature patch for the new `git config-batch` command, part of a series to optimize configuration operations. This is patch 2/11, building on the initial scaffolding from patch 1.

The patch introduces the core protocol parsing loop for the new command, establishing how it will handle input commands and respond to unknown requests. The design uses a line-based protocol over stdin/stdout, with versioning support for future extensibility. When receiving an unrecognized command, it responds with "unknown_command" to help clients handle compatibility gracefully.

Key technical elements added:
- Command parsing infrastructure with version checking
- Response emission helper functions
- Basic error handling for malformed input
- Initial test cases covering empty input, unknown commands, and version parsing failures

The implementation follows Git's standard patterns for new commands and includes documentation updates explaining the protocol basics. While this patch doesn't yet implement any actual configuration operations (those come in later patches), it establishes the framework they'll build upon.

The patch appears well-structured and carefully considered, with clear attention to future extensibility. Test coverage is minimal but appropriate for this foundational piece. As part of an RFC series, this invites discussion about the protocol design before more functionality is built on top.