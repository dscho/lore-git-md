Adrian Ratiu's config-based hooks series (v2, 8 patches) implements `hook.<name>.command`, `hook.<name>.event`, and `hook.<name>.enabled` configuration options while maintaining filesystem hook compatibility. The series introduces `struct hook` infrastructure supporting multiple hooks per event, a `git hook list` subcommand with `-z` output, and repository-level strmap caching.

Key technical developments:
- Refactored hook callback data management from stack allocation to dynamic allocation via new `feed_pipe_cb_data_alloc`/`free` in `run_hooks_opt`
- Updated reference transaction and pre-push hooks to use new allocation pattern
- Junio Hamano approved the refactoring while suggesting stylistic improvements
- Latest review focused on patch 4/8 implementing config-based hook definitions:
  - Will track config scope (global/local) in `struct hook` for debugging
  - Renaming `<name>` to `<friendly-name>` in documentation to match struct field
  - Consolidating `hook_cache_clear()` and `FREE_AND_NULL()` into single `hook_cache_free()`
  - Adrian confirms these changes will appear in v3

Current status:
- Core functionality complete at v2 with all 8 patches
- Prerequisite hooks-conversion series (v8) marked for 'next' branch inclusion
- Config hooks progressing through integration pipeline with maintainer engagement
- Maintainer approval for both architectural direction and specific details
- Minor documentation and API improvements suggested but no blocking concerns

Open items:
- Documentation updates per naming convention changes
- Implementation of config scope tracking in struct
- API consolidation for hook cache cleanup
- Awaiting prerequisite series merge to 'next'
- Final review of config hooks series once dependencies land

The thread shows steady progress toward configurable hooks, with maintainer engagement now focused on improving debuggability (via config scope tracking) and API cleanliness (consolidated cleanup functions). The series has moved from architectural approval to refinement of implementation details, with Adrian preparing a v3 incorporating these final improvements.