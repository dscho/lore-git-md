Adrian Ratiu's config-based hooks series (v4, 12 patches) has been integrated into 'master' after prerequisite work landed in 'next'. The series implements `hook.<friendly-name>.command`, `hook.<friendly-name>.event`, and `hook.<friendly-name>.enabled` configuration options while maintaining filesystem hook compatibility, with core functionality including out-of-repo execution support (using `RUN_SETUP_GENTLY`), NUL-terminated output (`-z` option), scope visibility (`--show-scope`), and disabled hook display in `git hook list`.

Key technical implementation:
- Patch 1-7/12: Introduced alloc/free helpers, `struct hook`, `git hook list`, config parsing with strmap cache, enabled/disabled control, and event clearing via empty values
- Patch 8/12: Enables out-of-repo execution while maintaining filesystem hook restrictions
- Patch 9-12/12: Adds `-z` output, refactors cache storage, implements scope display, and shows disabled hooks with "(disabled)" annotation
- Core architecture maintains strict separation between config hooks (work anywhere) and filesystem hooks (require repository)
- Test coverage in t1800-hook.sh verifies all behaviors including disabled hook visibility, with SZEDER Gábor identifying a test script hygiene issue regarding missing `&&` chains in helper functions `setup_hooks()` and `setup_hookdir()`

Review and integration status:
- Junio Hamano approved core approach while requesting documentation updates and suggested scope visibility feature
- Patrick Steinhardt provided detailed reviews including modernization to `CALLOC_ARRAY`, suggested event clearing behavior, and proposed `-z` option
- Series has now landed in 'master' with all major technical challenges addressed
- Adrian Ratiu confirms the test script `&&` chaining issue will be fixed in a follow-up patch as final polish

Final open items:
- Test script fix for `&&` chaining in t1800-hook.sh helper functions
- Documentation updates for naming conventions and scope display
- API consolidation for hook cache cleanup
- Future expansion of `git hook list` to show config-based hooks
- brian m. carlson's pending review of parallel hooks v2 series

The series has completed its core technical goals with only minor test infrastructure cleanup remaining, representing the successful introduction of config-based hook management while maintaining full filesystem hook compatibility. The follow-up test fix ensures proper error handling during test setup, marking the final integration step for this feature.