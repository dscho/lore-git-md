Adrian Ratiu's config-based hooks series (v3, 8 patches) implements `hook.<name>.command`, `hook.<name>.event`, and `hook.<name>.enabled` configuration options while maintaining filesystem hook compatibility. The series introduces `struct hook` infrastructure supporting multiple hooks per event, a `git hook list` subcommand with `-z` output, and repository-level strmap caching. Junio Hamano has approved the architectural approach while noting documentation updates needed in API comments, and the prerequisite hooks-conversion series (v8) is marked for 'next' branch inclusion.

Key technical developments:
- Refactored hook callback data management from stack allocation to dynamic allocation via new `feed_pipe_cb_data_alloc`/`free` in `run_hooks_opt`, with Junio suggesting more concise validation checks using boolean negation/XOR operators (!A != !B) instead of explicit conditionals
- Updated reference transaction, pre-push, and receive-pack hooks to use new allocation pattern with `xcalloc` and designated initializers
- `git hook list` subcommand implementation and defensive `BUG()` checks
- Two-phase config parsing with intermediate strmaps to collect hook configurations before final cache assembly
- Clear separation between cached config data (strmaps) and runtime hook state (`struct hook` instances cannot be cached due to containing pipe callback data)
- `hook.<name>.enabled` config option (default true) with state stored directly in `struct hook` per Patrick Steinhardt's suggestion
- Patrick Steinhardt's detailed reviews across all patches including modernization to `CALLOC_ARRAY`, renaming cleanup functions, and adding `BUG()` checks
- Junio Hamano and Patrick Stehtardt agree to defer `--disabled` flag functionality while keeping door open for future `git hook list` enhancements
- Adrian confirms `run_hooks_opt()` now fully manages feed pipe callback data lifecycle via alloc/free functions
- Patch 4/8 tracks config scope (global/local) in `struct hook` and renames `<name>` to `<friendly-name>` in docs
- Receive-pack hook demonstrates proper dynamic allocation pattern with `run_hooks_opt_clear()` cleanup

brian m. carlson raises forward-looking considerations about parallel hook execution (not in current series), with follow-up clarifying this will be addressed in a separate v2 parallel hooks series that:
- Maintains sequential execution as default for compatibility
- Introduces opt-in parallel execution for known-safe hooks
- Uses run-command's muxer for combined stdout/stderr output
- Preserves TTY handling (Phillip Wood's prior concern)
- Allows user control over parallelization
- Enforces serial execution for unsafe hooks

Current status:
- Core functionality complete at v3 with all 8 patches
- Maintainer approval for both direction and specific details
- Adrian preparing v3 with updated header files, API documentation, and all suggested refinements
- Prerequisite series expected to merge to 'next' soon
- Received constructive review from Patrick Steinhardt confirming core approach
- Clear technical justification established for current caching approach
- Parallel execution concerns addressed in separate upcoming series (v2)

Open items:
- Documentation updates per naming convention changes
- Implementation of config scope tracking
- API consolidation for hook cache cleanup
- Final stylistic improvements to allocation validation
- Future work on parallel execution output handling (via separate series)
- Future consideration of `git hook list` showing disabled hooks
- Final review of config hooks series once dependencies land

The thread shows steady progress toward configurable hooks, with maintainer and contributor engagement focused on implementation refinements. brian m. carlson's parallel execution considerations are now explicitly addressed as a separate effort, with Adrian confirming Phillip Wood-identified TTY preservation will be handled in the parallel hooks series. Junio's architectural approval and Patrick's constructive reviews confirm the current series is in its final polishing phase, with parallel execution work intentionally decoupled for future development. The prerequisite series' imminent merge to 'next' positions this work for integration once final reviews are complete.