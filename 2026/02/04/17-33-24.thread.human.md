This thread continues Adrian Ratiu's work on parallel hook execution, now in its fourth iteration. The series introduces multiple layers of control for running hooks in parallel while maintaining safety through conservative defaults. Junio Hamano has already acked key parts of the implementation, including the memory leak fix in patch 1/9 and the global `hook.jobs` configuration in patch 3/9.

The discussion has now shifted to documentation improvements. Junio suggests expanding the explanation of why certain hooks (like `applypatch-msg`, `pre-commit`, and `post-commit`) must always run serially, rather than just listing them. He provides the example of message-editing hooks that operate on a single file as a model for the kind of rationale that would help future contributors understand the constraints. This feedback doesn't require code changes to the already-approved implementation but aims to make the design principles clearer for long-term maintenance.

The series remains on track with its layered approach: global configuration, per-hook parallel markers, command-line overrides, and output stream handling. With the technical foundation solid and only documentation refinements remaining, this feature appears likely to graduate to `next` soon.