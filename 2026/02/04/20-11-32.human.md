This is a bugfix addressing pathspec handling in `git add` and `git stash` when dealing with excluded paths (`:!path`) that are also in `.gitignore`. The discussion has evolved from a specific fix to a deeper examination of pathspec semantics.

The core issue is that Git currently errors when an excluded pathspec matches an ignored file, even though the exclusion should take precedence. The proposed fix would modify `dir.c` to skip the ignored-file check for excluded pathspecs, but this raises broader questions about how positive and negative pathspecs should interact.

Junio C Hamano and other contributors are now discussing fundamental pathspec behavior, including whether to implement a two-phase matching approach where paths must (1) not match any negative patterns and (2) match at least one positive pattern. Special cases like all-negative pathspecs (e.g., `git add ":!x"`) are also being considered, as these currently behave like `git add ":!x" .` by implicitly matching everything not excluded.

The thread has revealed this touches long-standing ambiguities in pathspec semantics. While the initial bug is straightforward to fix, the discussion suggests the solution may need to address deeper design questions about how pathspecs should work across Git commands. Test cases are being added to clarify expected behavior for edge conditions like `git add x :!x`.

This is part of an ongoing effort to clarify and potentially standardize pathspec behavior, with implications for multiple Git commands. The discussion remains technical and focused, with participants working through the implications of different approaches.