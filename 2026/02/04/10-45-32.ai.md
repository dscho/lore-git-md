Documentation patch addressing inconsistencies in the newcomer guide.

Soutrik Das points out two documentation issues in `MyFirstContribution.adoc` that could confuse new contributors. First, the tutorial references `git_default_config` as being in `config.h` when it has moved to `environment.h`. Second, it directs readers to `Documentation/technical/api-config.adoc` for information about `repo_config()`, but this file no longer exists in the repository.

The author notes they found references to `repo_config()` in other documentation files but couldn't locate a comprehensive explanation. They reference some prior discussion about related configuration APIs but recognize they may be getting off track and ask for guidance.

This appears to be a legitimate documentation issue worth addressing, though the author acknowledges it may be too small for a GSoC project. The patch would help future newcomers by ensuring tutorial references match current code organization. The author demonstrates careful attention to detail in verifying the discrepancies.