Here's the human-readable thread summary:

This thread addresses a long-standing inconsistency in how Git handles excluded pathspecs (`:!path`) when the excluded path is in `.gitignore`. The initial patch fixed cases where `git add :!ignored` and `git stash --include-untracked -- ':!x'` would incorrectly fail when the ignored file existed. The solution modified `dir.c` to skip ignored-file checks for excluded pathspecs, aligning with documentation stating they should be treated like normal pathspecs.

Junio Hamano has raised an important design question about whether this solution fully captures the intended semantics. He suggests the interaction between positive and negative pathspecs might need more nuanced handling - where a path should only be included if it matches at least one positive pattern *and* no negative patterns. This goes beyond the original bugfix to touch on fundamental pathspec behavior that has been ambiguous for years.

While the patch clearly fixes a documented inconsistency, the discussion has revealed a deeper design question about how positive and negative pathspecs should interact in Git's core matching logic. This may require broader input from contributors familiar with pathspec internals to reach consensus on the ideal behavior.