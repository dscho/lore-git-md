This thread began as a bug report about `git add` and `git stash` incorrectly erroring when excluded pathspecs (`:!path`) referenced ignored files. The initial fix modified `dir.c` to skip ignored-file checks for excluded paths, but the discussion has uncovered deeper questions about pathspec semantics that have been ambiguous for years.

Junio Hamano and Tian Yuchen are now examining fundamental interaction between positive and negative pathspecs. Yuchen proposed a two-phase matching approach where paths must (1) not match any exclusions and (2) match at least one positive pattern. Junio agrees this direction seems reasonable but notes the special case of all-negative pathspecs (like `:!x`) may need to match everything except the excluded paths, similar to current behavior with `:!x .`.

The thread has evolved from a straightforward bugfix to considering changes to core pathspec behavior that could affect multiple commands. Both agree more test cases are needed for edge conditions, and the resolution may require broader input from pathspec experts to decide between maintaining backward compatibility or clarifying long-ambiguous behavior. The discussion remains focused on `dir.c`'s matching logic, with no code changes proposed yet for the newly raised design questions.