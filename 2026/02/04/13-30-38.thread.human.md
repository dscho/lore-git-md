Here's a human-readable summary of the thread so far:

This thread addresses a bug in how Git handles excluded pathspecs (`:!path`) when the excluded path is listed in `.gitignore`. The issue was discovered when `git add :!ignored` would fail if the ignored file existed, contrary to the documented behavior that exclude pathspecs should work consistently regardless of ignore status.

The patch fixes this by modifying `dir.c` to skip the ignored-file check for pathspecs marked with `PATHSPEC_EXCLUDE`, bringing the behavior in line with expectations. The change also resolves a related issue in `git stash --include-untracked` when using exclude patterns. The fix comes with new tests in both the add and stash test suites to verify the corrected behavior.

The commit message provides clear context about this being a long-standing edge case (10 years back) and demonstrates the problem with straightforward reproduction steps. The change appears focused and well-tested, addressing a specific inconsistency in pathspec handling without broader implications.