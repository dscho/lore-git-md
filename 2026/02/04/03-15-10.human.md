Platform compatibility patch addressing const-correctness warnings with glibc 2.43 and C23 standards. This is a follow-up discussion to Collin Funk's initial patch that changed `git_find_last_dir_sep()` to return `const char*` to match `strrchr()`'s behavior.

Jeff King (Peff) provides a detailed analysis of the broader implications, identifying approximately 65 similar cases across the codebase that need attention. He categorizes the issues into two main patterns: functions that secretly modify const parameters (particularly in rev-parse internals) and incorrect constness propagation through helper functions like `skip_prefix()`. The Windows implementation already handles this with explicit casting, suggesting multiple viable approaches.

The discussion reveals tradeoffs between different solutions - full const propagation versus selective casting versus alternative declarations using C11 generic selections. Peff notes the particular complexity around rev-parse's dotdot parsing which operates on const strings that may come from command-line arguments or string literals.

Collin acknowledges the complexity and plans to handle the changes in phases, starting with straightforward `const char*` conversions before tackling the trickier cases. The thread shows this is more than just compiler warning cleanup - it touches fundamental string handling patterns across multiple subsystems and will require careful review of each case.