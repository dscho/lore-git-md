Documentation follow-up clarifying two inconsistencies in the newcomer guide. Shreyansh Paliwal confirms that both issues raised by Soutrik Das have already been addressed: the `git_default_config` location change (now in `environment.h`) is fixed in a patch currently in the `next` branch, and the broken `api-config.adoc` link should point to `config.h` instead, as documented in a prior commit. 

This email serves as a helpful resolution to the thread, providing clear references to the existing fixes ([1] and [2]) while gently steering the newcomer toward additional microproject opportunities ([3]). The response demonstrates Git's documentation maintenance workflow in action - small corrections are welcomed, but contributors are also encouraged to explore more substantial work. No further action appears needed on these specific issues.