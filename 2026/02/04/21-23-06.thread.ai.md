Thread identity: Bugfix for whitespace handling of symbolic links in diff/apply machinery, addressing incorrect triggering of incomplete-line checks (WS_INCOMPLITE_LINE) on symlinks where such endings are normal. Patch from maintainer Junio C Hamano, with technical review from Patrick Steinhardt.

Core problem: Recent whitespace checking improvements introduced incorrect behavior for symbolic links by applying WS_INCOMPLITE_LINE checks (which verify files end with newlines) to symlinks, where such endings are not required. Needed careful special-casing in diff/apply code paths, particularly for mode-change cases like text-to-symlink transitions.

Current status: Final patch version addressing the core issue by modifying both `apply.c` and `diff.c` to disable WS_INCOMPLITE_LINE checks for symbolic links using refined rules. The implementation now skips checks when the post-image side (accounting for `--reverse`) is a symlink, properly handling text-to-symlink transitions while maintaining whitespace validation for regular files. Theoretical edge cases like newline-terminated symlink targets are explicitly deferred as not worth the complexity.

Key technical details:
- Changes in `apply.c` and `diff.c` to skip WS_INCOMPLITE_LINE checks for symlinks
- Final implementation uses refined rule: disable check when post-image side (accounting for `--reverse`) is symlink
- Properly handles text-to-symlink transitions where we shouldn't complain about missing newlines in post-image
- New test cases in `t4015-diff-whitespace.sh` and `t4124-apply-ws-rule.sh` verify behavior for both forward and reverse operations
- Implementation includes careful mode-change handling in `apply.c` to determine pre-/post-image based on patch direction
- Earlier discussion clarified this only concerns filesystem symlinks (not symrefs)
- Newline-terminated symlink targets explicitly deferred as theoretical edge case

Open questions: None remaining - all substantive technical questions resolved between participants. Theoretical edge cases acknowledged but explicitly deferred.

Participant positions:
- Junio C Hamano as maintainer and patch author proposed initial fix and refined through discussion
- Patrick Steinhardt provided technical review identifying mode-change edge cases and suggesting refinements
- Both agreed on deferring theoretical edge cases (newline-terminated symlinks)
- Final implementation represents consensus between maintainer and reviewer
- Patch is now considered ready for integration with no outstanding concerns

Related work: Builds on recent whitespace checking improvements that introduced the original behavior. No other related in-flight topics mentioned.