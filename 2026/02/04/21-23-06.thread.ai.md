Thread identity: Bugfix for whitespace handling of symbolic links in diff/apply machinery, addressing incorrect triggering of incomplete-line checks (WS_INCOMPLETE_LINE) on symlinks where such endings are normal. Patch from maintainer Junio C Hamano.

Core problem: Recent whitespace checking improvements introduced incorrect behavior for symbolic links by applying WS_INCOMPLETE_LINE checks (which verify files end with newlines) to symlinks, where such endings are not required. This needs special-casing in the diff and apply code paths.

Current status: Single patch version addressing the issue by modifying both `apply.c` and `diff.c` to explicitly disable WS_INCOMPLETE_LINE checks for symbolic links. Comprehensive test coverage added in `t4015-diff-whitespace.sh` and `t4124-apply-ws-rule.sh`.

Key technical details:
- Changes in `apply.c` and `diff.c` to skip WS_INCOMPLETE_LINE checks for symlinks
- New test cases verifying correct handling of symlinks while preserving incomplete-line checks for regular files
- Maintains existing whitespace checking behavior for non-symlink files

Open questions: None apparent - this appears to be a complete, self-contained fix for the identified edge case.

Participant positions: Junio C Hamano as both maintainer and patch author, suggesting this is a straightforward maintenance fix likely to be merged without controversy.

Related work: Builds on recent whitespace checking improvements that introduced the original behavior, but no other related in-flight topics mentioned.