This is a documentation patch in an 8-part series enhancing Git's promisor remote functionality. It adds missing documentation for the `--filter` option in `git fetch`, which was previously only documented for `git clone`.

The patch copies the existing `--filter` documentation from `clone` to `fetch` verbatim, acknowledging that while the wording could be improved, it's accurate enough for now. This is a straightforward change that brings consistency between related commands.

The patch is part of a larger effort (LOP - Large Object Promisors) that includes both technical changes (like adding `promisor.storeFields` config and `--filter=auto` mode) and documentation updates. Jean-Noël Avila had previously suggested formatting improvements for this documentation, though this particular patch doesn't appear to incorporate any new feedback beyond the basic addition.

As a documentation-only change in an otherwise technical series, this patch is unlikely to be controversial and serves to complete the feature's user-facing documentation. The commit message is clear about its limited scope and leaves room for future improvements to the wording.