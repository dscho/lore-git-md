Git thread summary: Feature patch series introducing parallel hook execution (v2 4/10), building on Adrian Ratiu's prior configurable hooks work. The series expands from 5 to 10 patches with significant redesign making parallel execution opt-in via `hook.jobs` config and `hook.<name>.parallel` markers, addressing v1 feedback from Patrick Steinhardt and Phillip Wood.

Current patch (4/10) introduces parsing for the global `hook.jobs` configuration that will control maximum concurrent hook executions. The setting defaults to serial execution (jobs=1) and accepts positive integers, with validation rejecting invalid inputs. The value flows through the refactored hook config cache infrastructure from patch 3/10, though not yet affects runtime behavior. Documentation updates in `Documentation/config/hook.adoc` explain the new setting.

Series context: initial patches include a memory leak fix in `repo_init()` (1/10), new unsigned integer config infrastructure (2/10), and hook config cache refactoring (3/10). Core functionality will parse job counts with precedence (compile-time -> per-event -> global -> CPU count), implement parallel execution (with Emily Shaffer's contributions), and maintain backward-compatible output handling. Tests in t/t1800-hook.sh use sentinel files for reliable parallel testing.

Key technical details carried forward: parallel execution remains opt-in (default jobs=1), with `hook.<name>.parallel` markers distinguishing parallel-safe hooks. Output streams preserve backward compatibility through `extensions.hookStdoutToStderr` and runtime overrides. The series depends on Adrian's unmerged configurable hooks work. The new `hook.jobs` configuration establishes the first layer of control before per-hook overrides are added in later patches.