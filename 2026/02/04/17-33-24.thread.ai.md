Git thread summary: Feature patch series introducing parallel hook execution (v2 10/10), building on Adrian Ratiu's prior configurable hooks work. The 10-patch series implements parallel hook execution with multiple opt-in control points: `hook.jobs` config (4/10), `hook.<name>.parallel` markers (5/10), `-j/--jobs` command-line option (7/10), per-event `hook.<event>.jobs` overrides (8/10), output stream handling via `extensions.hookStdoutToStderr` (9/10), and now a runtime `hook.forceStdoutToStderr` config (10/10) for global output stream control.

Final patch (10/10) completes the output handling strategy by adding a boolean `hook.forceStdoutToStderr` config that enables the `extensions.hookStdoutToStderr` behavior globally, allowing parallel execution for hooks like pre-push that normally require separate streams. Key technical additions:
- New runtime config option working with existing repository extension
- Support for both new (`git init`, `git clone`) and existing repositories
- Comprehensive tests covering repository creation, cloning, and push operations
- Maintains backward compatibility while providing migration path for git-lfs

Series context: initial patches include leak fix in `repo_init()` (1/10), unsigned integer config infrastructure (2/10), hook config cache refactoring (3/10), followed by parallel execution controls (4-8/10) and output stream handling (9-10/10). The design addresses v1 feedback with strict opt-in requirements at five levels: global config, event config, CLI, per-hook markers, and output configuration.

Implementation now supports:
- Parallel execution with configurable job counts at multiple levels
- Critical hook serialization when needed
- Output stream consistency enforcement
- Global override for stdout-to-stderr merging
- Backward compatibility throughout

The series depends on Adrian's unmerged configurable hooks work. With this final patch, the parallel hook execution feature is complete, providing comprehensive control over parallelization while maintaining safety through explicit opt-in requirements at all levels. The design accommodates existing tools like git-lfs while enabling performance improvements through parallel execution where explicitly configured.