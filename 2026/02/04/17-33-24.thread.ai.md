Git thread summary: Feature patch series (v6 0/9) introducing parallel hook execution, building on Adrian Ratiu's prior configurable hooks work. Core patches have maintainer acks from Junio C Hamano. Series establishes comprehensive parallel execution: global `hook.jobs` default (now supporting `-1` for CPU core count via final v6 12/12 patch), per-hook `hook.<name>.parallel` flags, CLI `-j/--jobs` override (confirmed by Patrick Steinhardt to override config and supporting `-1`), per-event `hook.<event>.jobs` settings (new in v6, also supporting `-1`), and per-event `hook.<event>.enabled` master switch (added in v6 11/12). Implementation handles output streams with special pre-push hook behavior, resolving `-1` to `online_cpus()` at parse time with validation for invalid values (warnings for config, fatal errors for CLI).

Patch 5/12 (v6) enables parallel pre-push execution while maintaining backwards compatibility - serial mode (jobs=1) keeps historical stream behavior while parallel mode merges stdout into stderr via `merge_output_if_parallel()`. Patch 6/12 marks seven hooks as non-parallelizable by default due to shared resources (message-related and working-tree-interacting hooks) using RUN_HOOKS_OPT_INIT_FORCE_SERIAL, with Emily Shaffer's signoff. Patch 7/12 introduces `-j/--jobs` CLI option for `git hook run` with full override capability including `-1` support, with TTY behavior tests. Patch 8/12 adds per-event job count configuration via `hook.<event>.jobs` with new `event_jobs` strmap cache and `-1` support. Patch 9/12 adds misconfiguration warnings for hook friendly-name/event name confusion using new `is_friendly_name()` helper. Patch 11/12 introduces `hook.<event>.enabled` configuration to disable all hooks for a given event, showing "event-disabled" status in `git hook list` and handling naming collisions through fatal errors (known event names as friendly-names) and warnings (ambiguous friendly-names). Patch 12/12 completes the job count system by adding `-1` support across all configuration points, matching conventions in subsystems like `fetch.parallel`.

Refactoring (v5 10/12) moves `is_known_hook()` from static helper in `builtin/hook.c` to public function in `hook.c` with proper declaration, preparing for friendly-name collision detection. Improves code by changing return type from `int` to `bool` and renaming iterator variable from `p` to `h`. Mechanical change with no behavior difference, part of approved series.

Series infrastructure includes: `repo_config_get_uint()`, global `hook.jobs` config, parallel execution logic with opt-in controls, safety constraints, and now complete `-1` support. Test coverage in t/t1800-hook.sh verifies TTY handling, output ordering, precedence rules, and new `-1` behavior. Key feedback incorporated: Patrick Steinhardt suggested `bool` types and config warnings, Junio noted numbering issue (fixed) and suggested friendly-name warnings. V6 includes memory leak fix, rebase on config-cleanups-v3, NULL dereference fix, documentation clarifications, and final `-1` support. All core components have maintainer approval, completing the parallel hook execution feature set.