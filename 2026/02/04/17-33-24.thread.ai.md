Git thread summary: Feature patch series introducing parallel hook execution (v3 0/9), building on Adrian Ratiu's prior configurable hooks work. This rebase-only update from v2 (10/10) to v3 (9/9) combines output stream handling patches without functional changes. The series implements parallel hook execution with multiple opt-in control points: `hook.jobs` config (3/9), `hook.<name>.parallel` markers (4/9), `-j/--jobs` command-line option (6/9), per-event `hook.<event>.jobs` overrides (7/9), and output stream handling via `extensions.hookStdoutToStderr` (8-9/9).

Patch 1/9 (updated in v3) fixes a memory leak in `repo_init()` when initializing repositories with v0 format but v1 extensions enabled. Patch 2/9 (new in this email) adds unsigned integer config infrastructure (`git_config_uint()`) to support the upcoming 'hook.jobs' configuration, including parse, configset, and repository-level accessors. This fills a gap in the config API which previously only handled signed integers and unsigned longs.

Series context from v2 remains valid: initial patches include leak fix (1/9), unsigned integer config infrastructure (2/9), hook config cache refactoring (3/9), followed by parallel execution controls (4-7/9) and output stream handling (8-9/9). The design continues to address prior feedback with strict opt-in requirements at five levels while maintaining backward compatibility.

Implementation supports:
- Parallel execution with configurable job counts at multiple levels
- Critical hook serialization when needed
- Output stream consistency enforcement
- Global override for stdout-to-stderr merging
- Backward compatibility throughout

The series builds on Adrian's unmerged configable hooks work, providing comprehensive control over parallelization while maintaining safety through explicit opt-in requirements. The design accommodates existing tools like git-lfs while enabling performance improvements through parallel execution where explicitly configured.