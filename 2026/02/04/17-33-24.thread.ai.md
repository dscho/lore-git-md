Git thread summary: Feature patch series introducing parallel hook execution (v2 9/10), building on Adrian Ratiu's prior configurable hooks work. The series expands from 5 to 10 patches with significant redesign making parallel execution opt-in via multiple control points: `hook.jobs` config (4/10), `hook.<name>.parallel` markers (5/10), `-j/--jobs` command-line option (7/10), per-event `hook.<event>.jobs` overrides (8/10), and now output stream handling via `extensions.hookStdoutToStderr` (9/10).

Current patch (9/10) standardizes hook output behavior for parallel execution by introducing a repository configuration that forces stdout to stderr merging when enabled. Key technical additions:
- New `extensions.hookStdoutToStderr` repository extension enabling consistent output behavior
- Special handling for pre-push hook (the only current exception to stdout-to-stderr behavior)
- Transport.c updates to respect the setting for pre-push hooks
- Comprehensive tests verifying output behavior in both parallel and sequential modes
- Backward compatibility maintained via sequential fallback when extension is disabled

Series context: initial patches include a leak fix in `repo_init()` (1/10), new unsigned integer config infrastructure (2/10), hook config cache refactoring (3/10), `hook.jobs` config parsing (4/10), core parallel execution (5/10), critical hook serialization (6/10), CLI job control (7/10), and event-specific job counts (8/10). The design addresses v1 feedback by making parallel execution strictly opt-in with multiple control points.

Open questions remain about:
- Final list of non-critical hooks to be marked parallel-safe in the last patch
- Potential edge cases in TTY handling
- Interaction between event-specific overrides and future per-hook parallel markers
- Migration path for git-lfs and other tools relying on pre-push hook stdout

The series still depends on Adrian's unmerged configurable hooks work. The implementation now supports five levels of parallel execution control (global config, event config, CLI, per-hook markers, and output stream configuration). The safety invariants remain enforced throughout - parallel execution requires explicit opt-in at all levels, with this patch adding output stream consistency as a new requirement for parallel operation.