Updated AI thread summary:

Bugfix for `git clone --revision` behavior with protocol v0/v1, addressing a segmentation fault when using shorthand revision names (e.g. `--revision master`). The issue occurs when older protocols fetch all references without filtering, potentially leaving `remote_head_points_at->peer_ref` NULL during reference updates.

Junio C Hamano raised substantive concerns about the initial fix approach (adding a NULL check in `builtin/clone.c`), arguing it masks deeper architectural issues where `peer_ref` should have been properly initialized. He proposes restructuring `update_remote_refs()` to separate connectivity checking from ref-writing operations, as `--revision` clones should verify connectivity but not update refs.

Nitro Cao has engaged with Junio's architectural concerns, agreeing the simple NULL check isn't sufficient. The discussion has evolved to fundamental questions about `--revision` clone behavior:
1. Whether these clones should write any refs at all (current behavior writes none)
2. How to properly structure ref update logic across protocol versions
3. Nitro suggests conditionally calling `find_ref_by_name()` as an alternative approach

Key technical details:
- The segfault occurs in `builtin/clone.c` during reference updates
- Protocol v0/v1 fetch all refs while v2 filters by wanted-refs
- `remote_head_points_at->peer_ref` may be NULL when no matching ref exists

Open questions and next steps:
- Finalize architectural approach (Junio's separation of concerns vs Nitro's conditional lookup)
- Add comprehensive regression tests covering:
  - Segfault prevention
  - Resulting repository state (detached HEAD)
  - Protocol version differences
  - Edge cases with various revision specs

The thread has progressed from a simple crash fix to reconsidering the clone implementation's structure. Nitro has committed to adding tests once the correct approach is determined. Junio's participation signals this requires careful handling to avoid technical debt. The solution will need to address both the immediate crash and the underlying design questions about `--revision` behavior across protocol versions.