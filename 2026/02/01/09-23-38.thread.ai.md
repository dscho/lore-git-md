Updated AI thread summary:

Bugfix for `git clone`'s handling of `--revision` with protocol v0/v1, addressing a segmentation fault when using shorthand revision names (e.g. `--revision master`). The issue occurs because older protocols fetch all references without filtering, potentially leaving `remote_head_points_at->peer_ref` NULL during reference updates. The fix adds a NULL check in `builtin/clone.c` before updating HEAD. The v2 revision maintains the same technical content while improving commit message formatting. The patch clearly documents the protocol version behavior differences but still lacks a regression test case, which remains an open gap. This fixes a real-world issue where shorthand revision names are commonly used with older protocols. The change is localized to the clone command's reference update logic and does not affect other protocol versions or commands.