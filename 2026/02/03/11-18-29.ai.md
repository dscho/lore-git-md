This is a follow-up in the ongoing discussion about default branch syntax, where Harald Nordgren provides a concrete example of tooling that requires being on a named branch rather than a detached HEAD. 

The email highlights a practical limitation of the previously agreed-upon `git switch -d origin` detached-HEAD solution - some third-party tools (in this case `release-it`) explicitly require working on named branches and reject detached HEAD states. This reinforces Harald's earlier point that while the detached-HEAD approach works for Git's native operations, real-world workflows often interact with tools that make different assumptions.

The example shows `release-it` (a release management tool) failing when run from a detached HEAD, requiring specific branch naming patterns (`release/*`, `hotfix/*`, `develop`). This suggests the detached-HEAD solution, while elegant from Git's perspective, may not fully address ecosystem compatibility concerns. The thread continues to balance Git's internal design against practical developer experience considerations.