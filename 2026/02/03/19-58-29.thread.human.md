Here's the updated human-readable summary:

Chris Packham started a documentation gap in `git format-patch` - while the man page mentions that `--no-renames` can override configuration, it doesn't specify which config option controls rename detection for this command. Jeff King has now confirmed that `git format-patch` shares the `diff.renames` setting with `git diff`, though this relationship isn't currently documented.

The discussion stems from Chris's practical use case: they want to keep rename detection enabled for interactive diffs while disabling it for patch generation to ensure compatibility with the basic `patch` utility. Jeff notes there's currently no way to configure this divergence in behavior - users must explicitly pass `--no-renames` when needed.

While suggesting that a `format.renames` option (falling back to `diff.renames`) could theoretically be added to support separate configuration, Jeff presents this more as an observation than a concrete proposal. The thread currently leans toward a documentation patch to clarify the existing behavior rather than introducing new configuration options. No other participants have weighed in yet on whether they've encountered similar needs for divergent rename detection behavior.