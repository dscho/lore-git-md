This is a platform compatibility discussion about const-correctness changes needed for glibc 2.43 and C23 standards compliance. Jeff King (Peff) responds to Collin Funk's initial patch that changed `git_find_last_dir_sep()` to return `const char*` to match `strrchr()`'s behavior under C23.

Peff confirms the warning issue with glibc 2.43 and estimates about 65 similar const-correctness fixes needed across the codebase. He identifies two problematic patterns: functions that secretly modify const parameters (like some rev-parse internals), and cases where constness propagates incorrectly through helper functions like `skip_prefix()`. The discussion highlights tricky tradeoffs between type safety and practical string manipulation needs.

Regarding the specific patch, Peff notes the change introduces constness that callers then have to cast away, suggesting alternative approaches might be better - either reverting to a macro implementation or using glibc-style type-generic declarations. He points out Windows already handles this with explicit casting in its implementation.

This email advances the thread by identifying broader implications of the const-correctness effort and potential implementation strategies. The technical depth suggests this will be a non-trivial series to review, with some judgment calls needed about how aggressively to propagate constness versus using targeted casts. Peff's analysis indicates the changes will touch sensitive string-handling code in multiple subsystems.