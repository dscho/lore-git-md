This is a bugfix and behavior clarification for `git stash`'s handling of ignored files, part of an ongoing thread about adding `--overwrite-ignore` options. Elijah Newren raises several substantive concerns about the design choices in Pushkar Singh's patch series.

Newren questions the fundamental motivation for the change, pointing out that the patch introduces a new option (`--no-overwrite-ignore`) to solve a problem that wasn't previously configurable. He challenges the logic of silently downgrading `--all` to exclude ignored files when `--no-overwrite-ignore` is given, arguing this could surprise users who expect `--all` to mean all files. The discussion reveals a tension between backward compatibility (keeping existing behavior by default) and clear option semantics (making contradictory options explicitly incompatible).

The email highlights that while the technical implementation appears sound, the higher-level design decisions need more justification. Newren suggests starting from user needs rather than implementation details - what concrete problems do users face that this change would solve? The thread remains open on whether to keep the silent downgrade behavior or make the options explicitly incompatible.

This is a key design discussion that will shape how `git stash` handles ignored files going forward. The patch author has incorporated most technical feedback but needs to revisit the fundamental rationale and option interaction design before the series can progress.