Thread identity: Documentation clarification for `git format-patch --no-renames` option, initiated by Chris Packham, with Jeff King (Peff) now participating.

Core problem: The man page doesn't specify that `git format-patch` shares the `diff.renames` configuration with `git diff` for which `--no-renames` can override. Chris Packham's use case requires disabling renames for patch generation (for `patch` utility compatibility) while keeping them enabled for interactive diffs.

Current status: Jeff King confirmed the shared `diff.renames` behavior and noted the documentation gap. No patches have been proposed yet - the thread is still in the problem-identification phase.

Key technical details: `git format-patch` inherits rename detection behavior from `git diff` via `diff.renames` config, though this isn't documented. The `--no-renames` option can override this setting. There's currently no way to configure different rename behaviors between diff viewing and patch generation without runtime overrides.

Open questions: Should the documentation be updated to explicitly link `format-patch` to `diff.renames`? Is there sufficient need for a new `format.renames` config (falling back to `diff.renames`) to support divergent behaviors? Jeff's suggestion of this pattern (like `merge.renames`/`status.renames`) remains hypothetical.

Participant positions: Chris Packham identified the documentation gap with a concrete use case. Jeff King confirmed the technical behavior and suggested potential solutions while indicating the current limitations. No other participants have joined yet.

Related work: This connects to Git's broader rename detection configuration patterns across commands (`diff.renames`, `merge.renames`, `status.renames`), though no specific related threads or patches are referenced yet. The thread may lead to either a documentation patch or a feature proposal for format-specific rename configuration.