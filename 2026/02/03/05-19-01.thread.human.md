This thread has evolved from a specific const-correctness fix for `git_find_last_dir_sep()` to a broader discussion about handling string modifications across Git's codebase in light of glibc 2.43 and C23 standards. Jeff King (Peff) has taken the lead in analyzing the scope, identifying around 65 problematic cases that fall into two categories: functions incorrectly modifying const parameters and constness issues in string helper functions.

The discussion has revealed particularly concerning behavior in `revision.c`, where `handle_revision_arg()` could segfault when processing string literals containing ".." due to in-place modifications. Peff proposes making copies of strings before modification in such cases, noting this is acceptable for non-performance-critical paths. He's provided concrete examples of fixes, including addressing a potential infinite loop in `http.c`'s cookie handling that stricter const-correctness would expose.

Participants are weighing three main approaches: comprehensive const propagation (more type-safe but invasive), selective casting at boundaries (less disruptive but potentially hiding issues), or using C11 generic macros (complex but future-proof). The Windows implementation serves as a reference for the casting approach, while Peff's analysis suggests favoring clarity and safety over cleverness, even if it means more extensive changes. The thread remains open on whether to pursue a unified solution or handle subsystems differently, with rev-parse internals likely needing special consideration.