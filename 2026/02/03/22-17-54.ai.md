This is the first patch in a 5-part series adding repository statistics to `git repo`. The series focuses on identifying storage outliers by extending `git repo`'s output with metrics like largest objects by type and commits with most parents.

Patch 1 refactors the object counting logic in `builtin/repo.c` to process statistics per-object rather than in bulk. The change moves the statistics updates inside the object iteration, switching from accumulating totals per batch to incrementing counts per individual object. This prepares for the later patches that will track additional per-object metrics like maximum sizes.

The implementation carefully maintains the same statistics while changing when they're updated, with the switch statement now inside the object loop. The diff shows the mechanical nature of this change - it's purely a refactoring with no behavior modification, just moving the same logic to a different place in the control flow. The patch includes test coverage (implied by the series context though not shown in this email) and follows Git's style for error handling with the `BUG()` call for invalid object types.

This foundational change enables the more interesting statistics additions in subsequent patches while keeping the series bisectable. Reviewers will want to verify the refactoring preserves all existing behavior, particularly around edge cases in object iteration and counting.