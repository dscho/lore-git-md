This is a platform compatibility discussion about const-correctness changes needed for glibc 2.43 and C23 standards compliance. Jeff King (Peff) responds to Collin Funk's initial patch that changed `git_find_last_dir_sep()` to return `const char*` to match `strrchr()`'s behavior under C23.

Peff confirms the warnings appear with glibc 2.43 and estimates about 65 similar fixes will be needed across the codebase. He identifies two categories of tricky cases: functions that secretly modify const parameters (like some rev-parse internals) and cases where constness propagates incorrectly through helper functions like `skip_prefix()`. 

The discussion highlights a specific challenge with `git_find_last_dir_sep()` - while making it return `const char*` fixes warnings for most callers, it requires explicit casting in cases like `chop_last_dir()` where the result is modified. Peff explores alternatives like reverting to a macro or using glibc-style type-generic declarations, but notes these come with their own complications.

This appears to be the start of a significant const-correctness effort that will touch many parts of the code. Peff's response suggests the changes are worthwhile but will require careful handling of edge cases where constness assumptions don't match reality. The discussion provides useful technical context for anyone working on similar string-handling code in Git.