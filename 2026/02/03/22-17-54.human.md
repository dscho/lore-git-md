This is the first patch in a series adding repository statistics to `git repo`, focusing on refactoring the object counting logic. 

The patch restructures how `count_objects()` tracks repository statistics by moving from batch processing (updating totals after processing all objects) to per-object updates. This prepares the codebase for the subsequent patches that will add more detailed statistics like largest objects and extreme commit/tree cases.

The change is purely internal - no user-visible behavior is modified. The diff shows careful attention to maintaining the same totals while changing when they're calculated. The switch statement is moved inside the object iteration, updating counts one object at a time rather than accumulating totals first.

This appears to be a well-motivated preparatory change that makes the code more amenable to the new statistics features coming in later patches. The implementation looks straightforward and maintains all existing functionality while setting up for the series' goals. The commit message clearly explains the why behind the refactoring.