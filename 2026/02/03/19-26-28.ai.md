The maintainer (Junio C Hamano) raises substantive concerns about this bugfix for `git clone --revision` with older protocols. While acknowledging the segfault issue, he questions whether simply adding a NULL check is the right approach, suggesting it might mask deeper problems where `peer_ref` should have been set but wasn't. 

Junio proposes a more architecturally sound solution: splitting `update_remote_refs()` to separate the connectivity checking (which should still run for `--revision`) from the ref-writing operations (which shouldn't run in this mode). He also notes the patch lacks test coverage to verify both the crash fix and the expected repository state (detached HEAD at specified revision with no other refs).

This moves the discussion beyond the immediate segfault to consider the proper behavior of `--revision` cloning. The maintainer's response indicates the patch needs significant reworking to properly handle the feature's requirements rather than just papering over the crash. Test coverage will be essential for any revised version.