This is a follow-up in the ongoing discussion about default branch syntax in Git. Harald Nordgren provides a concrete example of tooling (release-it) that requires being on a named branch rather than a detached HEAD, even when working with the default branch. 

The example shows that while the detached-HEAD workflow (`git switch -d origin`) works for Git itself, some third-party tools still expect to operate on named branches. This reinforces Harald's earlier point that remote naming conventions and branch requirements in external tools create practical friction, even if Git's core functionality can handle detached HEADs.

The message doesn't propose any new solutions but serves as evidence supporting the need for better default branch handling, whether through new syntax or other means. The thread continues to balance Git's internal capabilities against real-world ecosystem constraints.