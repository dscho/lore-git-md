This is a bugfix and behavior clarification for `git stash`'s handling of ignored files, continuing the discussion from previous patch versions. Elijah Newren raises several substantive questions about the design approach in Pushkar Singh's v3 patch.

Newren challenges the fundamental rationale for the new `--no-overwrite-ignore` option, pointing out that since this is a new flag (not existing behavior being fixed), the justification needs to focus on user-visible improvements rather than internal consistency. He questions why the patch silently downgrades `--all` to exclude ignored files when `--no-overwrite-ignore` is given, rather than treating these as explicitly incompatible options that should error out.

The email reveals a disconnect between the patch's implementation choices and its stated goals. Newren probes the claims about backward compatibility (irrelevant for a new option) and the logic of honoring the "more conservative" option. He suggests starting from user needs rather than implementation details when designing the behavior.

This exchange highlights the key unresolved questions in the thread: whether silent behavior modification is appropriate versus explicit errors for incompatible options, and whether the new flag's design actually solves a user-facing problem. The technical implementation appears sound, but the higher-level design rationale remains under scrutiny. Newren's feedback suggests the patch may need to revisit its fundamental approach before being merge-ready.