Thread identity: Platform compatibility discussion addressing const-correctness changes needed for glibc 2.43 and C23 standards compliance, focusing on string-handling patterns throughout Git's codebase. Originated with Collin Funk's patch for `git_find_last_dir_sep()` but expanded to broader architectural concerns.

Core problem: glibc 2.43 enforces C23 const-correctness for string functions, causing compilation warnings with GCC 15's default `-std=gnu23`. Approximately 65 problematic cases identified across the codebase, with two main categories: functions incorrectly modifying const parameters (notably in rev-parse internals) and incorrect constness propagation through string helpers like `skip_prefix()`. A critical case in `revision.c`'s `handle_revision_arg()` could segfault when modifying string literals containing "..".

Current status: Jeff King (Peff) has provided detailed analysis of scope and tradeoffs, proposing making copies of strings before modification in performance-non-critical paths like `handle_revision_arg()`. Toon Claes now proposes a C11 generic selection macro approach for `git_find_last_dir_sep()`, identifying 28 remaining const-correctness warnings. Peff responds that C11 generics would be acceptable if limited to supporting compilers, though a simpler macro might suffice for immediate needs. Collin Funk adds technical details about glibc macro behavior (`__GLIBC_USE(ISOC23)`) and demonstrates a workaround involving undefining `__glibc_const_generic` after including `sys/cdefs.h`. He confirms some fixes are already in Junio's "next" branch while others remain pending review. The Windows implementation (`win32_find_last_dir_sep()`) remains a reference point. Discussion now includes compiler-specific workarounds (`-Wno-discarded-qualifiers`), clang/glibc header interaction issues, and broader GNU project impacts.

Key technical details: Changes affect multiple subsystems including config.c, remote.c, scalar.c, strbuf.c, and revision.c. Peff identified three solution paths: (1) comprehensive const propagation, (2) selective casting (Windows-style), or (3) C11 generic selection macros. Toon's macro implementation provides a concrete example of approach (3). The `revision.c` case shows a clear pattern where string copying is the safest approach. Collin's new input reveals glibc's `__GLIBC_USE(ISOC23)` macro triggers under two conditions (C23 flags or `_GNU_SOURCE` definition) and demonstrates a workaround that avoids modifying system headers. Peff also found and fixed a potential infinite loop in `http.c`'s cookie handling that these changes would expose. New details emerge about problematic glibc preprocessor conditionals when building historical Git versions and clang-specific warning flag incompatibilities.

Open questions: Whether to pursue a unified approach across all cases (macro-based vs. string copying vs. casting) or handle subsystems differently. The rev-parse modifications may need special handling strategy. Toon's macro approach needs evaluation against the remaining 28 cases. No consensus yet on const propagation versus casting, though Peff's examples demonstrate a preference for clarity and safety over cleverness. Additional question raised about handling clang/glibc header interactions that may require upstream fixes. Collin's workaround adds another dimension to consider regarding long-term maintainability versus immediate compatibility.

Participant positions: Collin Funk initiated with the `git_find_last_dir_sep()` patch and now provides deeper glibc macro analysis. Peff's analysis has framed the architectural discussion, identifying the systemic nature of the problem and tradeoffs between solutions, now also addressing compiler-specific workarounds. Toon Claes contributes a macro-based implementation approach, expanding the solution space. Peff's input suggests this will require careful coordination across multiple subsystems, with particular attention to safety-critical cases like `revision.c` and compiler compatibility constraints. Collin's latest contribution provides concrete technical details about the glibc behavior affecting all GNU projects.

Related work: Part of ongoing platform compatibility maintenance for newer compiler standards. Connects to broader string-handling patterns but remains distinct from larger refactoring efforts like `the_repository` removal. Windows implementation provides a reference point, while C11 generic macros offer a potential future-looking solution. The discussion now also touches on historical version buildability, clang/glibc interoperability issues beyond Git's direct control, and broader GNU ecosystem impacts (Gnulib and other projects facing similar challenges). Some fixes already in Junio's "next" branch while others remain pending review.