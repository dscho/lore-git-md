Thread identity: Platform compatibility discussion addressing const-correctness changes needed for glibc 2.43 and C23 standards compliance, starting with `git_find_last_dir_sep()` but expanding to broader string-handling patterns.

Core problem: glibc 2.43 enforces C23 const-correctness for string functions, causing warnings when Git compiles with GCC 15's default `-std=gnu23`. Initial patch changed `git_find_last_dir_sep()` to return `const char*` to match `strrchr()`'s behavior, but discussion reveals deeper issues with approximately 65 similar cases needing attention across the codebase.

Current status: Jeff King (Peff) identifies two problematic patterns beyond the initial function - functions that secretly modify const parameters (particularly in rev-parse internals) and incorrect constness propagation through helper functions like `skip_prefix()`. The Windows implementation already handles this with explicit casting, suggesting multiple viable approaches.

Key technical details: The `git_find_last_dir_sep()` change propagates constness to callers in `config.c`, `remote.c`, `scalar.c`, and `strbuf.c`, with `remote.c` requiring explicit casts. Peff notes the tradeoffs between type safety and practical string manipulation, suggesting alternatives like macro implementations or glibc-style type-generic declarations might be preferable to widespread const propagation.

Open questions: Whether to pursue a comprehensive const-correctness effort (affecting ~65 locations) or targeted fixes. Implementation strategy remains undecided - full const propagation versus selective casting versus alternative declarations. The rev-parse internals may require special attention due to their modification of const parameters.

Participant positions: Collin Funk's initial patch demonstrated the basic issue. Peff's response provides architectural perspective, identifying the scope of related changes and tradeoffs between different solutions. His analysis suggests this will require careful review of string-handling patterns across multiple subsystems.

Related work: Part of ongoing platform compatibility maintenance for newer compiler versions and C standards. Connects to broader string-handling patterns in the codebase but remains distinct from larger refactoring efforts like `the_repository` removal or ODB abstraction. Windows implementation provides a reference point for alternative approaches.