This is a review comment on Adrian Ratiu's config-based hooks series, specifically addressing patch 5/8 which implements the `hook.<name>.enabled` configuration option. Patrick Steinhardt suggests storing the enabled state directly in the `struct hook` rather than maintaining a separate `disabled_hooks` string list.

Patrick's feedback continues his pattern of constructive technical suggestions from previous reviews in this thread. He notes this approach would provide more flexibility, such as potentially listing disabled hooks in `git hook list` output. The comment is narrowly focused on the data structure design rather than raising broader concerns, indicating the series is in its final refinement stage.

The exchange shows Patrick engaging with implementation details while maintaining support for the feature's direction, consistent with the thread's progression toward configurable hooks. This follows Junio Hamano's earlier architectural approval and Patrick's previous reviews focusing on memory management patterns and API design.