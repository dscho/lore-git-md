The final patch in Patrick Steinhardt's ref iteration API unification series removes the last specialized interface, `refs_for_each_fullref_in()`, completing the transition to the new `refs_for_each_ref_ext()` API introduced earlier in the series. This patch updates all remaining callers across bisect, receive-pack, rev-parse, show-ref, and test helpers to use the unified interface, then removes the now-obsolete function from refs.c and its declaration from refs.h.

The changes follow the same pattern seen throughout the series: each caller is updated to initialize a `refs_for_each_ref_options` struct with the appropriate prefix and exclude patterns, then passes this to the new unified function. The patch maintains all existing behavior while eliminating one of Git's many specialized ref iteration variants. With this change, only `refs_for_each_replace_ref()` remains as a specialized interface, though the commit message doesn't indicate whether that will be addressed in a follow-up.

This concludes a well-structured series that systematically simplified Git's ref iteration API from 14 specialized functions down to a single configurable interface. The changes are mechanical but thorough, touching protocol-critical components like HTTP ref advertisement and upload-pack while maintaining identical behavior. The series demonstrates Patrick's deep understanding of Git's ref handling internals.