This is a minor code review comment in the final stages of a series implementing reference storage backend selection. The thread has already gone through multiple iterations and received maintainer approval, with this being one of the last remaining tweaks.

Toon Claes points out a small inconsistency in Karthik Nayak's implementation of URI parsing for the refstorage configuration. The code checks for the presence of "://" to split the URI into format and payload components, but Toon questions why the negative case (no "://" found) is handled in the `if` clause rather than the more conventional positive case. This is purely a stylistic observation rather than a functional issue.

The patch is part of a larger effort to enable zero-downtime migrations between different reference storage formats (files and reftable), primarily for GitLab's needs. The series has already addressed all substantive technical concerns and is ready for integration, with this being one of the final polish items before merging.