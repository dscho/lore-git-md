This is a feature patch adding client certificate support to `git send-email`. The patch introduces new configuration options allowing users to specify client certificates for SMTP authentication, addressing a security use case where certificate-based authentication is required.

The changes add three new configuration options to both the documentation and implementation: `sendemail.certfile` for the certificate file path, `sendemail.keyfile` for the private key file path, and `sendemail.keypassword` for the key passphrase. The Perl implementation is updated to handle these new options when establishing the SMTP connection, with appropriate error handling for missing files or incorrect passwords.

The patch appears well-structured with documentation updates matching the code changes. It follows the established pattern of other SMTP-related options in `git send-email`. The commit message could benefit from a more detailed explanation of the use case and security implications, but the technical implementation looks sound. This would be particularly useful for users in environments requiring certificate-based authentication for email submission.