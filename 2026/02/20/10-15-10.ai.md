This is part 6 of Patrick Steinhardt's 8-patch series changing git maintenance's default strategy from "gc" to "geometric". The patch focuses on test stability by explicitly setting the maintenance strategy to "gc" in t6500-gc.sh.

The test file t6500-gc.sh specifically tests `git gc` behavior and makes assumptions about the on-disk state of the repository. The patch series' upcoming change to make "geometric" the default strategy would break these assumptions. Rather than disabling auto-maintenance entirely (which would be another valid approach), Patrick opts to pin the strategy to "gc" since the entire test file is focused on gc behavior anyway.

The change itself is minimal - a single line addition to the test setup that sets `maintenance.strategy=gc` globally. This follows the pattern seen in earlier patches of the series where tests making specific assumptions about maintenance behavior are updated to explicitly declare their expectations rather than relying on defaults.

This patch continues the series' thorough approach to test coverage, ensuring the core strategy change won't cause test failures in gc-specific tests. The change is well-motivated and follows the established pattern from previous patches in the series.