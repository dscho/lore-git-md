This is a patch in Adrian Ratiu's config-based hooks series, addressing feedback from Patrick Steinhardt on the internal state management for hook callbacks. The changes focus on memory allocation patterns and cleanup in the hook infrastructure.

The patch introduces alloc/free callbacks for hook state management, moving from stack allocation to dynamic allocation of callback data structures. Patrick's review suggests several small but meaningful improvements: using `CALLOC_ARRAY` for allocation, initializing structs directly with designated initializers, and renaming the callback typedefs to be hook-specific. Adrian confirms he'll incorporate all these suggestions in v3.

Key technical points include:
- The receive-pack hook now uses dynamic allocation via `xcalloc` for its feed state
- The `run_hooks_opt_clear()` function properly cleans up allocated state
- Callback typedefs will be renamed to `hook_data_free_fn` and `hook_data_alloc_fn` for better scoping

The discussion shows steady progress on the implementation details after the architectural approach was approved by Junio Hamano. Patrick's review focuses on code quality and consistency rather than fundamental design, indicating the series is in its final polishing phase. Adrian demonstrates responsiveness to feedback while maintaining his original design decisions where they make sense (like keeping the single state copy approach rather than splitting into config/data structs).

This patch represents incremental refinement of the hook infrastructure that will support the larger config-based hooks functionality. The changes are mechanical but important for proper memory management as the series moves toward integration.