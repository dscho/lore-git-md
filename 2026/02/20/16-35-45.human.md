This is a performance optimization discussion thread about using Linux's `sendfile()` system call for file copying operations in Git. The latest email comes from Ed Maste, who notes that FreeBSD's `sendfile()` only works on sockets, suggesting `copy_file_range()` as an alternative if benchmarks show it would be beneficial.

The thread has evolved from initial implementation details to questioning whether the optimization is worth the complexity, with Jeff King (Peff) raising concerns about whether Git's typical file operations would see measurable benefits. The key missing piece remains benchmark data to justify the change, especially given platform-specific constraints like FreeBSD's socket-only limitation.

Ed's contribution adds another platform consideration to the mix, suggesting FreeBSD would need a different approach if this optimization proves worthwhile. The discussion remains technically focused but is currently stalled pending concrete performance measurements to validate the proposed changes.