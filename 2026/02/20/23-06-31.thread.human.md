Here's a human-readable summary of the thread's current state:

This RFC patch series proposes configurable formatting for cover letter commit lists in `git format-patch`, allowing both command-line (`--cover-letter-format`) and config-based (`format.commitListFormat`) control. The implementation is mostly ready, but progress is blocked by a fundamental debate about NULL pointer handling in the pretty-printing subsystem.

The key sticking point is in patch 1/3, where author Mirko Faina and maintainer Junio C Hamano disagree on how to handle NULL `user_format` when `CMIT_FMT_USERFORMAT` is set. Junio argues this should trigger a `BUG()` as it indicates a programming error, while Mirko maintains silent handling is necessary for config probing. Mirko has now dug deeper into `get_commit_format()`'s behavior and suspects the current NULL handling might itself be buggy, bringing in Jeff King (Peff) for his expertise on the pretty-printing internals.

The rest of the series (patches 2/3 and 3/3) remains technically sound but can't move forward until this foundational question is resolved. The debate has revealed this touches broader questions about contracts and error handling in Git's formatting subsystem. While the feature itself - giving users more control over cover letter presentation - isn't controversial, the implementation must first establish correct low-level behavior that aligns with the project's error handling philosophy.