Here's the human-readable thread summary:

This RFC thread proposes configurable commit list formatting for `git format-patch` cover letters, allowing both command-line (`--cover-letter-format`) and config-based (`format.commitListFormat`) control. The series is currently paused at patch 1/3 while the maintainer and author debate a foundational design question.

The key issue is how to handle pointer validation in `commit_format_is_empty()` - maintainer Junio C Hamano believes encountering a NULL `user_format` with `CMIT_FMT_USERFORMAT` should trigger an immediate crash via `BUG()`, treating it as a programming error. Author Mirko Faina argues for silent handling instead, noting the function's dual role in both validation and status checking, particularly for complex configuration scenarios.

The outcome will affect the subsequent patches implementing the core formatting engine (2/3) and config layer (3/3), which otherwise appear technically sound with proper memory management and backward compatibility. While no other technical objections have been raised, the series can't progress until this design question is resolved, as it impacts the stability of the entire feature.

The discussion has revealed an ambiguity in the function's intended contract that wasn't previously apparent - whether it's purely for validation or also for state probing. This deeper architectural question may require revisiting some assumptions before the series can move forward.