Here's a human-readable summary of the thread:

This thread discusses a mature patch series (now at v7) that adds configurable cover letter formatting to `git format-patch`. The implementation provides both command-line (`--cover-letter-format`) and configuration (`format.commitListFormat`) options while maintaining backward compatibility with the existing shortlog-based output.

The technical implementation introduces new functions in `builtin/log.c`, special placeholders (`%(count)` and `%(total)`), and comprehensive test coverage. After seven iterations, the code is production-ready with all technical requirements met. Key participants including Junio C Hamano, Jeff King (Peff), and Phillip Wood have reviewed the series, with their feedback now fully incorporated.

The current discussion focuses on finalizing the behavior of the config option. Junio suggests treating boolean values specially (`false` for shortlog, `true` for default format) to make the interface more intuitive when using `-c` to set values. The current implementation handles unset/empty/shortlog/custom cases with strict validation of format strings.

With documentation complete (now reorganized per Phillip Wood's suggestion) and only this final config behavior question remaining, the series appears ready for merging once this last design point is resolved. The author Mirko Faina has confirmed v7 as final pending this change, and all other maintainer feedback has been addressed.