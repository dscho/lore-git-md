This is a review discussion in Adrian Ratiu's config-based hooks series, which introduces configuration options for Git hooks while maintaining filesystem compatibility. The specific patch being discussed adds a `hook.<name>.enabled` config option to disable hooks without removing their configuration.

Patrick Steinhardt suggests storing the enabled state directly in the `struct hook` rather than maintaining a separate list of disabled hooks, which would allow for potential future features like listing disabled hooks. Adrian confirms this is a good idea and plans to implement it in v3, though he'll keep the scope focused on the base functionality rather than adding new commands.

The exchange shows constructive technical discussion between the author and a respected reviewer (Patrick Steinhardt), with Adrian receptive to the feedback. This is part of the final polishing phase for a series that has already received maintainer approval for its architecture. The change being discussed will improve the internal design while keeping the user-visible behavior the same.