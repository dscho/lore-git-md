This is the final call for review on a patch series that aligns `git add`'s behavior with `git status` and `git diff` when handling submodules marked with `ignore=all`. Maintainer Junio C Hamano is checking if there are any remaining concerns before merging.

The series (now at v4) makes submodules with `ignore=all` skipped by default in `git add`, requiring explicit `--force` to stage them. This addresses workflow pain points where developers unintentionally stage submodule updates. The implementation is thorough, with plumbing changes, comprehensive tests, and updated documentation.

Key technical aspects are already settled from previous rounds: the `--force` naming, backward compatibility, test coverage. The current version incorporates documentation phrasing improvements from Ben Knoble and maintains all resolved technical constraints. Junio's note indicates the sign-off line formatting change was intentional and proper.

With no substantive reviews on this latest iteration, the maintainer appears ready to merge pending confirmation that all feedback has been addressed. The series represents a well-vetted improvement to submodule handling consistency.