Thread identity: Feature patch adding client certificate support to `git send-email`, enabling certificate-based SMTP authentication.

Core problem or goal: Currently `git send-email` lacks support for client certificate authentication, which is required in some security-sensitive environments. The patch aims to add this capability while maintaining compatibility with existing SMTP authentication methods.

Current status: First version of the patch has been submitted, introducing the feature with both configuration options and command-line flags. The implementation appears complete but awaits initial review feedback.

Key technical details: Adds three new settings for client certificate authentication:
- `sendemail.smtpSSLClientCert` config option and `--smtp-ssl-client-cert` flag for client certificate file (PEM or PKCS12 format)
- `sendemail.smtpSSLClientKey` config option and `--smtp-ssl-client-key` flag for private key (required for PEM certificates)
- Updates the Perl implementation to pass these parameters to IO::Socket::SSL
Maintains Git's existing patterns for SMTP configuration while adding proper error handling for file access and certificate verification.

Open questions and loose ends: The commit message could be enhanced with more detail about security implications and real-world use cases. Implementation details may need refinement based on review feedback, particularly around error handling edge cases and documentation clarity.

Participant positions: No reviews or feedback from other contributors yet. The author remains the sole participant at this early stage.

Related work: Builds upon existing SMTP authentication infrastructure in `git send-email`, following similar patterns to other SMTP-related configuration options. Leverages Perl's IO::Socket::SSL library rather than introducing new dependencies.