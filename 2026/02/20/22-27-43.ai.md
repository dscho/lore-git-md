This is a patch series addressing submodule `ignore=all` behavior consistency, now awaiting final confirmation before integration. The v4 series has been through multiple iterations and appears technically complete, with all prior feedback addressed.

The core change makes `git add` respect `submodule.<name>.ignore=all` configurations by default, matching the behavior of `git status` and `git diff`. When users need to override this, they must explicitly use `--force`. The implementation is split across plumbing changes, core logic, comprehensive tests, and documentation updates.

Key technical aspects include:
- A new `skip_submodule()` helper that centralizes the ignore logic
- Plumbing changes to propagate the `--force` flag through the diff machinery
- 8 new test cases specifically for this behavior
- Updates to existing tests to use `--force` where needed
- Clear documentation of the new requirements across multiple man pages

The series has evolved through several versions with input from multiple reviewers. Junio's note suggests the technical work is complete and the series is ready for merging if no new concerns surface. The changes appear well-considered, with thorough test coverage and clear documentation updates explaining both the behavior change and its workflow benefits.