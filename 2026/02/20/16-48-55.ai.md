This is a performance optimization discussion about platform-specific file copying methods, continuing the thread about using Linux's `sendfile()` system call optimization in Git.

Collin Funk chimes in with additional context about `copy_file_range(2)` on FreeBSD, noting that while it could serve as an alternative to `sendfile()` (which is socket-only on FreeBSD), it comes with its own set of issues. He points to known problems in GNU Coreutils' implementation, including handling of files larger than INT_MAX bytes, and questions whether the complexity is justified given Git's use cases.

The email reinforces Jeff King's earlier point about needing concrete benchmark data to evaluate whether these platform-specific optimizations would provide meaningful benefits for Git's typical operations (which mostly deal with small files). The discussion remains technically grounded but increasingly skeptical about the optimization's value proposition versus implementation complexity.

Key technical details mentioned include FreeBSD's socket-only `sendfile()` limitation, `copy_file_range(2)` edge cases, and the INT_MAX file size limitation. The thread continues to wait for performance measurements that would justify proceeding with either the Linux `sendfile()` optimization or FreeBSD alternatives.