This is a discussion thread about edge case behavior in `git cat-file --batch-check` when handling object names containing whitespace alongside the `%(rest)` format specifier.

The thread has evolved from a specific test fix proposal to questioning whether the behavior is fundamentally unfixable due to interface constraints. Junio Hamano now clarifies that the current behavior is actually the documented one - the command explicitly splits input at the first whitespace when using `%(rest)`, making it impossible to properly handle object names containing spaces in this context.

This means the original test's FIXME comment was correctly documenting a known limitation rather than pointing to a bug that needs fixing. The discussion suggests we should either:
1. Keep the test as-is to document this edge case
2. Remove the test if we consider this behavior too obscure to warrant special attention

Junio's latest email strongly implies the first option is more appropriate, as it accurately reflects the command's documented behavior. The thread shows how careful analysis of test failures can sometimes reveal that the "bug" is actually working as designed.