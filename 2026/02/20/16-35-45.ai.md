This is a performance optimization discussion thread about using platform-specific file copying mechanisms in Git.

Ed Maste (FreeBSD maintainer) chimes in to confirm Chris Torek's earlier point about FreeBSD's `sendfile()` being socket-only, noting that if benchmarking shows the optimization is valuable, FreeBSD would need to use `copy_file_range(2)` instead. This reinforces the platform-specific nature of the proposed optimization and the need for separate implementations across operating systems.

The email doesn't introduce new technical arguments but serves as an authoritative confirmation from a FreeBSD expert about platform constraints. It keeps the conversation grounded in real-world portability considerations while leaving the door open for the optimization if benchmarks justify it. The thread remains focused on whether the potential performance gains (still unmeasured) warrant the added complexity of maintaining platform-specific code paths.