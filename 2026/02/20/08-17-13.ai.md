This is a feature patch adding client certificate authentication support to `git send-email`. The patch introduces new configuration options and command-line flags to enable mutual TLS authentication with SMTP servers that require client certificates.

The implementation adds three new settings:
- `sendemail.smtpSSLClientCert` config option and `--smtp-ssl-client-cert` flag for specifying a client certificate file (PEM or PKCS12 format)
- `sendemail.smtpSSLClientKey` config option and `--smtp-ssl-client-key` flag for an optional private key (required for PEM certificates)
- Updates the Perl implementation to pass these parameters to the underlying IO::Socket::SSL library

The patch follows Git's existing patterns for SMTP configuration, adding both config options and matching command-line flags. It includes documentation updates and proper error handling for cases like missing key files. The implementation appears straightforward, leveraging Perl's existing SSL library support rather than introducing new dependencies.

This addresses a real need in security-sensitive environments where SMTP servers require mutual TLS authentication. The patch is well-structured with clear documentation, though the commit message could be enhanced with more detail about security implications and real-world use cases. As this is the first submission, we can expect review feedback to potentially refine the implementation details and documentation wording.