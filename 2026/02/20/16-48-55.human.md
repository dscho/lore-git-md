This is a performance optimization discussion about using platform-specific file copying mechanisms in Git. Collin Funk chimes in with experience from GNU Coreutils' use of `copy_file_range(2)` on FreeBSD, noting its implementation challenges (particularly with large files) despite measurable performance benefits.

The thread has established that Linux would use `sendfile()` while FreeBSD would need `copy_file_range(2)`, but the key question remains whether the optimization is worthwhile for Git's typical workloads. Collin's input adds practical context about the maintenance burden of such platform-specific code, suggesting the performance gains seen in Coreutils' `cp` may not translate directly to Git's use cases.

The discussion remains technically grounded but is effectively blocked pending benchmark data to quantify the potential speedup for Git operations like bundle transfers and local clones. The tradeoff between performance gains and added code complexity appears to be the central consideration moving forward.