This is a review comment from Junio C Hamano on a patch series about fixing `strbuf_attach()` call sites. The discussion focuses on memory allocation behavior when attaching buffers to strbufs, particularly around NUL-termination requirements.

Junio agrees with most of the patch's changes where callers pass len+1 for NUL-terminated strings, but questions the approach for `ll_merge` cases where the buffer isn't NUL-terminated. He analyzes the tradeoffs between:

1. Original approach using `strbuf_attach` which may trigger reallocation
2. Patch's proposed `strbuf_add`+`free` which always copies
3. Alternative of explicit `realloc` before `strbuf_attach`

Junio notes the original implementation might be preferable due to potential allocator optimizations, though he leaves the final judgment open. The comment shows his characteristic attention to low-level memory management details while maintaining a neutral stance on the preferred solution.

This is part of a larger series (position 2/5) about fixing strbuf allocation handling, with the immediate context being proper accounting for NUL terminators. The thread shows Junio engaging in technical review of memory management subtleties, typical of his maintainer role in assessing patch quality.