This is a technical discussion about refactoring revert message formatting in the `git replay` series. The patch in question extracts shared logic for formatting revert commit messages into a new `sequencer_format_revert_header()` function, which handles both regular reverts and reverts-of-reverts. The extracted code will be reused by `builtin/replay.c` for the new `--revert` functionality.

Toon Claes raises a concern about the split handling of `commit_use_reference` between the new helper function and the calling code, particularly when the helper is called with NULL for the commit OID. He references an earlier suggestion from Patrick Steinhardt that may have proposed a more unified approach to this logic. The discussion appears to be about implementation details rather than fundamental design, as the core functionality has already been agreed upon in previous iterations of the patch series.

The exchange shows ongoing refinement of the code organization as this feature moves toward finalization, with attention to clean separation of concerns and maintainability. The thread context indicates this is part of a larger effort to add server-side revert capability to `git replay`, primarily for GitLab's Gitaly service.