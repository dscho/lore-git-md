This is a tangential discussion about `git send-email` functionality rather than a patch submission. David Timber notes that their email setup requires client certificate authentication, which `git send-email` currently doesn't support. They mention that since `git send-email` is implemented as a single Perl script, they could potentially contribute a patch to add this capability if there's interest from the community.

The email is an off-topic reply in a thread about email header issues, and the author is gauging whether such a patch would be welcome before investing time in implementation. This isn't a formal RFC or patch submission yet, but rather a preliminary inquiry about a potential future contribution to `git send-email`'s SMTP authentication options.