RFC patch series implementing configurable cover letter commit list formatting for `git format-patch`, with patch 1/3 now under active debate regarding NULL pointer handling philosophy in `pretty.c`. The series aims to complete maintainer Junio C Hamano's requirements for both command-line (`--cover-letter-format`) and config-based (`format.commitListFormat`) control over commit list presentation.

Key technical implementation:
- Patch 1/3 (current debate) fixes NULL dereference in `commit_format_is_empty()` with maintainer advocating for `BUG()` on NULL `user_format` with `CMIT_FMT_USERFORMAT`, while author Mirko Faina defends silent handling as necessary for config probing
- Patch 2/3 introduces core formatting engine via `--cover-letter-format` with printf-style specifiers (%s=subject, %a=author, "log:" prefix)
- Patch 3/3 adds persistent `format.commitListFormat` config with same formatting syntax
- New `generate_commit_list_cover()` handles all formatting cases
- Memory management properly handled in `format_config_release()`
- Falls back to traditional shortlog when unconfigured
- Command-line option overrides config as expected

Status and verification:
- Patch 1/3 requires resolution on fundamental NULL handling approach - whether function serves validation-only (BUG) or dual validation/probing (silent) role
- Core formatting engine from patch 2/3 remains unchanged but depends on this foundational decision
- Config layer in patch 3/3 matches established pattern but requires stable base
- Backward compatibility must be maintained through any changes
- Series builds logically with each patch depending on previous

Open items:
- Critical decision needed for patch 1/3 NULL handling strategy - BUG() vs silent
- Test coverage for format combinations remains pending resolution
- Final man page documentation complete but may need adjustment
- Default behavior verification depends on stable implementation
- Need to clarify function's intended contract in documentation

Participant positions:
- Mirko Faina argues silent handling as necessary for config state probing, noting `get_commit_format()` doesn't always set `user_format`
- Junio C Hamano maintains position that NULL with `CMIT_FMT_USERFORMAT` indicates programming error warranting `BUG()`
- No other technical objections raised yet
- Series progression blocked pending resolution of this design philosophy debate

The feature aims to provide both temporary and persistent control over cover letter commit list presentation, but the maintainer's scrutiny of the foundational NULL handling has revealed a deeper question about function contracts in the codebase. The current debate goes beyond this series, touching on broader error handling patterns when validating format states. The careful patch-by-patch approach remains sound, but the series cannot progress until this design question is resolved, either through consensus or maintainer directive.