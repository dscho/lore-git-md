RFC patch series implementing configurable cover letter commit list formatting for `git format-patch`, with patch 1/3 debate now resolved after Jeff King's (Peff) clarification of `pretty.c` subsystem contracts. The series aims to complete maintainer Junio C Hamano's requirements for both command-line (`--cover-letter-format`) and config-based (`format.commitListFormat`) control over commit list presentation.

Key technical implementation:
- Patch 1/3 NULL handling debate settled: Peff confirmed `commit_format_is_empty()` was designed for NULL checking, only empty strings, validating Junio's position that NULL `user_format` with `CMIT_FMT_USERFORMAT` indicates programming error
- Core issue identified as patch 2/3's interaction with these assumptions rather than needing changes to `commit_format_is_empty()`
- Patch 2/3 introduces core formatting engine via `--cover-letter-format` with printf-style specifiers (%s=subject, %a=author, "log:" prefix), now requires redesign to respect pretty-printing contracts
- Patch 3/3 adds persistent `format.commitListFormat` config with same formatting syntax
- New `generate_commit_list_cover()` will need adjustment based on Peff's upcoming review
- Memory management properly handled in `format_config_release()`
- Falls back to traditional shortlog when unconfigured
- Command-line option overrides config as expected

Status and verification:
- Patch 1/3 debate resolved with Peff's subsystem expertise confirming original function contract
- Core formatting engine in patch 2/3 requires redesign to properly interface with pretty-printing subsystem
- Config layer in patch 3/3 remains sound but depends on stable base
- Backward compatibility must be maintained through redesign
- Series progression unblocked but requires architectural adjustments

Open items:
- Peff to review patch 2/3 with focus on proper integration with pretty-printing subsystem
- Mirko Faina to redesign patch 2/3 based on Peff's feedback
- Test coverage for format combinations remains pending redesign
- Final man page documentation may need adjustment
- Default behavior verification depends on stable implementation
- Need to document function contracts in code comments

Participant positions:
- Jeff King (Peff) clarified subsystem contracts, confirming `commit_format_is_empty()` is for empty strings not NULL checks
- Junio C Hamano's position validated regarding NULL handling as programming error
- Mirko Faina now understands need to adjust patch 2/3's architecture
- Series can progress once patch 2/3 is redesigned to respect pretty-printing contracts

The feature remains valuable for providing both temporary and persistent control over cover letter commit list presentation. With the NULL handling debate resolved through Peff's subsystem expertise, attention shifts to properly integrating the new formatting engine with existing pretty-printing contracts. The careful patch-by-patch approach continues, with patch 2/3 now requiring redesign before the series can advance. Peff's upcoming review will provide the architectural guidance needed to resolve the remaining integration questions while maintaining the pretty-printing subsystem's established behavior patterns.