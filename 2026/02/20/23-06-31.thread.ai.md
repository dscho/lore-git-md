RFC-to-feature patch series implementing comprehensive cover letter formatting for `git format-patch`, now in final form (v7). The series provides configurable formatting through both CLI (`--cover-letter-format`) and config (`format.commitListFormat`) interfaces while maintaining backward compatibility with shortlog-based output.

Core implementation includes:
- New `generate_commit_list_cover()` and refactored `generate_shortlog_cover_letter()` functions in `builtin/log.c` (this patch completes the structural separation)
- Special "shortlog" handling cleanly separated into dedicated function (finalized in this patch)
- `%(count)` (zero-padded using `decimal_width()`) and `%(total)` placeholders in `pretty.c` with strict `rev_info` context validation
- Config behavior matrix (unset->shortlog, empty->default format, custom formats) via `format.commitListFormat`
- "log:` prefix requirement for all non-shortlog formats
- Integration with existing `make_cover_letter()` and `repo_format_commit_message()`
- Bash completion support
- Comprehensive test coverage in `t4014-format-patch.sh` and `t9902-completion.sh`

Documentation complete with:
- `--cover-letter-format` CLI option details including example format strings with new placeholders
- `format.commitListFormat` config variable and behavior matrix
- Clear "log:` prefix requirement explanation
- Demonstration of numbering scheme usage and example alternative formats
- Configuration examples section updates matching standard documentation style
- Full coverage in both config and command man pages
- Junio C Hamano's final documentation suggestions incorporated

Implementation status:
- Production-ready code with all technical requirements met
- Seven iterations completed addressing all feedback (v7 being final)
- Shortlog generation cleanly modularized via pure refactoring (completed in this patch)
- Finalized `pretty.c` modifications with context enforcement
- Documentation now complete with all agreed design points
- Ready for merging with no outstanding issues

Key resolved design decisions:
- "log:` prefix handling consensus
- Numbering format (Peff's design with `decimal_width()` alignment) implemented
- Single validation point established
- Config behavior matrix finalized (unset/shortlog/log: modes)
- CLI/config precedence clarified
- Contextual enforcement of placeholder usage (`rev_info` requirement) implemented

Participant positions:
- Junio C Hamano: Approved architecture, confirmed documentation complete with minor wording improvements, accepts implementation
- Jeff King (Peff): Designed numbering approach using `decimal_width()`, approved implementation
- Mirko Faina (author): Implemented all components including context validation and structural refactoring (finalized in this patch), maintained test coverage, proposed final syntax resolution, confirms v7 is final

The series represents a complete implementation ready for merging, having addressed all technical requirements and maintainer feedback through seven iterations. The modular implementation preserves backward compatibility while introducing flexible formatting options with comprehensive documentation and testing, including the new `%(count)`/`%(total)` placeholders with strict context validation. The config option provides persistent configuration while maintaining the same validation rules and format string processing established in the series. The final v7 confirmation from author Mirko Faina and Junio's documentation approval indicate no further changes are needed before merging. This patch completes the structural refactoring by fully separating shortlog generation into its dedicated function while maintaining all existing behavior.