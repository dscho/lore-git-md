RFC patch proposing cover letter commit list format change for `git format-patch`. Author seeks feedback on replacing current author-grouped shortlog with numbered chronological list showing each commit as "[N/M] abcd123: summary". 

Modifies `builtin/log.c` to:
- Remove shortlog dependency for cover letters
- Add `generate_cover_commit_list()` producing sequential numbered output
- Maintain existing line wrapping while changing presentation order
- Use `pp_commit_easy()` for one-line summaries with allocated hash formatting

Motivation is that developers find current author-grouped shortlog less useful for understanding patch series flow. New format makes commit order and content more immediately apparent. Implementation appears straightforward with proper memory handling (alloc/free balanced).

Open questions:
- Should format be configurable (current patch hardcodes new style)?
- Needs test coverage for new output format
- Requires man page documentation update
- Follows prior discussion about cover letter usefulness
- Implementation considered straightforward but marked RFC for design feedback

Key technical details:
- Changes cover letter generation only, not patch email subjects
- Preserves all information while reordering presentation
- Memory handling appears correct (alloc/free balanced)
- Would need accompanying documentation updates if accepted

The patch is a focused improvement to a specific pain point in patch submission process. Change is well-contained and implementation looks clean, though as RFC it's seeking feedback on design approach before finalizing.