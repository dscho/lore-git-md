RFC patch series implementing configurable cover letter commit list formatting for `git format-patch`, completing with patch 3/3 adding the `format.commitListFormat` config option. The series realizes maintainer Junio C Hamano's requirements for both command-line (`--cover-letter-format`) and config-based control over commit list presentation.

Key technical implementation:
- Patch 1/3 fixed NULL dereference in `commit_format_is_empty()` (`pretty.c`)
- Patch 2/3 introduced core formatting engine via `--cover-letter-format` with printf-style specifiers (%s=subject, %a=author, "log:" prefix)
- Patch 3/3 adds persistent `format.commitListFormat` config with same formatting syntax
- New `generate_commit_list_cover()` handles all formatting cases
- Memory management properly handled in `format_config_release()`
- Falls back to traditional shortlog when unconfigured
- Command-line option overrides config as expected

Status and verification:
- All three patches now complete and interdependent
- NULL dereference fix remains properly integrated
- Core formatting engine from patch 2/3 unchanged
- Config layer in patch 3/3 matches established pattern
- Backward compatibility maintained throughout
- Builds logically with each patch depending on previous

Open items addressed:
- Test coverage for format combinations now complete
- Final man page documentation included
- Default behavior verified against Git 3.0 expectations

Participant positions:
- Mirko Faina delivered full implementation matching maintainer requirements
- Junio C Hamano's design direction fully realized
- No outstanding technical objections
- Series ready for maintainer consideration

The feature now provides both temporary (`--cover-letter-format`) and persistent (`format.commitListFormat`) control over cover letter commit list presentation, completing the transition from RFC to production-ready implementation. The careful patch-by-patch progression demonstrates adherence to Git's incremental development philosophy, with each stage building on verified foundations.