RFC-to-feature patch series implementing comprehensive cover letter formatting for `git format-patch`, now in final form ready for merging (v6). The series provides configurable formatting through both CLI (`--cover-letter-format`) and config (`format.commitListFormat`) interfaces while maintaining backward compatibility with shortlog-based output.

Core implementation includes:
- New `generate_commit_list_cover()` and `generate_shortlog_cover_letter()` functions in `builtin/log.c` (the latter extracted from `make_cover_letter()` in patch 2/5)
- Special "shortlog" handling now cleanly modularized in dedicated function
- New `%(count)` (zero-padded using `decimal_width()`) and `%(total)` placeholders in `pretty.c` for patch numbering, with strict `rev_info` context validation
- Config behavior matrix (unset->shortlog, empty->default format, custom formats)
- "log:" prefix requirement for all non-shortlog formats
- Integration with existing `make_cover_letter()` and `repo_format_commit_message()`
- Bash completion support
- Comprehensive test coverage in `t4014-format-patch.sh` and `t9902-completion.sh`

Documentation complete with:
- `--cover-letter-format` CLI option details including example format strings with new `%(count)`/`%(total)` placeholders
- `format.commitListFormat` config variable specification
- Demonstration of numbering scheme usage
- Clear "log:" prefix requirement explanation
- Configuration examples section updates

Implementation status:
- Production-ready code with all technical requirements met
- Six iterations completed addressing all feedback (v6 being final)
- Shortlog generation cleanly modularized in patch 2/5 via pure refactoring
- Finalized `pretty.c` modifications with context enforcement
- Documentation now complete in final patch
- Ready for merging with no outstanding issues

Key resolved design decisions:
- "log:" prefix handling consensus
- Numbering format (Peff's design with `decimal_width()` alignment)
- Single validation point established
- Config behavior matrix finalized
- CLI/config precedence clarified
- Contextual enforcement of placeholder usage (`rev_info` requirement)

Participant positions:
- Junio C Hamano: Approved architecture, confirmed documentation complete, accepts implementation (sign-off removed in v6 as series is ready)
- Jeff King (Peff): Designed numbering approach using `decimal_width()`, approved implementation
- Mirko Faina (author): Implemented all components including context validation and structural refactoring (patch 2/5), maintained test coverage, proposed final syntax resolution, confirms v6 is final

The series represents a complete implementation ready for merging, having addressed all technical requirements and maintainer feedback through six iterations. The modular implementation preserves backward compatibility while introducing flexible formatting options with comprehensive documentation and testing, including the new `%(count)`/`%(total)` placeholders with strict context validation. The final v6 confirmation from author Mirko Faina indicates no further changes are needed before merging. Patch 3/5 specifically introduces the `--cover-letter-format` CLI option with its format string processing in `generate_commit_list_cover()`, building on patch 2/5's refactoring of shortlog generation into `generate_shortlog_cover_letter()`.