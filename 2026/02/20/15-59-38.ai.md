This is a review comment from the maintainer (Junio C Hamano) on a proposed feature addition - the `branch.addCurrentBranchAsPrefix` config option that would automatically prefix new branch names with the current branch name.

Junio raises a standard project policy concern: new configuration options should typically be preceded by command-line flags that have proven useful in practice. He notes that even if this config option were added, users would still need a corresponding command-line flag (like `--no-current-branch-prefix`) to override it in specific cases.

The comment doesn't reject the feature outright but sets a clear bar for its acceptance - the concept should first be implemented and validated as a command-line option before graduating to a configuration setting. This follows Git's conservative approach to configuration proliferation, where new knobs need to demonstrate real-world utility before becoming permanent parts of the interface.

The thread context shows this is about a feature that would recursively prefix branch names (e.g., creating "main-test" when on "main", or "main-test-sub" when on "main-test"). While the implementation appears technically sound (with tests covering the behavior), Junio's comment suggests the conceptual approach needs more vetting before becoming a configurable default.