This is a minor documentation fix in the ref backend selection series. Toon Claes points out two small issues in Karthik Nayak's patch:

1. A duplicate "store" word in the comment
2. Suggests clarifying "store files" to "store references" since that's the specific context

The change is trivial but improves accuracy in documenting the ref storage backend infrastructure. This comes late in a well-reviewed series (v7) that has already received maintainer approval, so it's just final polish before integration. The thread context shows the broader work is about enabling zero-downtime migrations between ref backends (files<->reftable) with multiple configuration mechanisms.