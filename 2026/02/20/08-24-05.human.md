This is the first patch in Patrick Steinhardt's 17-part series to unify Git's ref iteration interfaces. It's a preparatory refactoring that moves `refs_head_ref_namespaced()` next to `refs_head_ref()` in refs.h, since they share similar single-ref semantics rather than the multi-ref iteration pattern of the other `refs_for_each_*` functions.

The change is straightforward - just relocating the function declaration to be grouped with its logical counterpart. This makes the header organization more intuitive before the larger API unification work that follows in later patches. The commit message clearly explains the rationale for the move.

As the first patch in a larger series, this serves as groundwork for the more substantial changes to come. It demonstrates Patrick's characteristic attention to detail in structuring complex refactoring work - starting with small, uncontroversial cleanups before tackling the core problem. The patch looks clean and well-justified, with no functional changes beyond the declaration move.