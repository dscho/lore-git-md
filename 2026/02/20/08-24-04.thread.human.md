This thread documents Patrick Steinhardt's effort to consolidate Git's ref iteration API into a single unified function, replacing 14 specialized variants. The 17-patch series introduces `refs_for_each_ref_ext()` with a configurable options structure, maintaining all existing behavior while significantly improving performance (demonstrating a 100x speedup for glob iteration). 

Karthik Nayak has reviewed and approved the series after Patrick incorporated all feedback, including documentation improvements around namespace-prefix interactions and better handling of prefix stripping in glob matching. The implementation carefully preserves behavior across all converted callers (bisect, rev-parse, pack-bitmap, etc.) while adding safety checks and standardizing callback types.

A few final details remain under discussion, particularly around the `trim_prefix` length parameter and some edge cases in glob matching. Patrick has deferred potential simplifications of the `trim_prefix` interface as future optimizations, preferring to keep the current more flexible approach. The series leaves only `refs_for_each_replace_ref()` as a remaining specialized case, which may be addressed separately later.

This work represents a significant simplification of Git's ref iteration internals and is part of the broader ref backend modernization effort. With Karthik's approval and all substantive feedback addressed, the series appears ready for merging pending resolution of the minor open questions about prefix handling details.