Final patch in the UTF-8 alias support series, addressing zsh completion. This is the fourth commit in v5, which adds a new `--aliases-for-completion` option to `git help` to properly handle subsection-style aliases (like `[alias "förgrena"]`) in zsh completion. The patch follows the existing `--config-for-completion` pattern, outputting NUL-separated name/value pairs that correctly process both traditional and subsection alias formats.

D. Ben Knoble reviews the implementation, noting that while sorting the alias list may not be strictly necessary (and could even be a feature to preserve config order), the overall approach is sound. The zsh completion change is straightforward, leveraging the new interface to fix the subsection handling. With this final technical issue resolved, the series appears ready for merging, having addressed all feedback across its four commits.