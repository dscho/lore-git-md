Security patch series addressing repository discovery hardening, currently at v3. This email from Junio Hamano confirms the conceptual approach Tian Yuchen is taking to classify different failure modes when checking `.git` entries during repository setup. The discussion has narrowed to precisely four distinct states that need separate handling: missing paths (ENOENT), directories (IS_A_DIR), non-file special files (NOT_A_FILE), and true errors (READ_FAILED/INVALID_FORMAT).

Junio agrees with Tian's proposed state classification but raises an important follow-up question about the code's behavior when encountering a directory that isn't actually a valid Git repository. This suggests the next version will need to clarify whether such cases should be treated as fatal errors or whether Git should continue searching parent directories, maintaining compatibility with existing behavior while still catching truly dangerous cases like FIFOs.

The exchange shows the thread converging on a solution that properly distinguishes security-critical failures from benign conditions in repository discovery. Tian acknowledges the current implementation's logic needs cleaning up and plans to include test cases in the next version, addressing Junio's earlier concern about test coverage. The core security premise - rejecting unsafe `.git` attachments - remains uncontested, with the discussion now focused on refining the implementation details.