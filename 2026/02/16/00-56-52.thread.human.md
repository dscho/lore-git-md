Here's a human-readable summary of the thread so far:

Pierre Houston proposed adding a `git stash diff` command to make it easier to view stash differences without needing to know implementation details like stash references or discover the `-p` flag. Eric Sunshine pointed out that this functionality already exists via `git stash show -p`, though he acknowledged the discoverability problem. 

The discussion is now weighing whether to: (1) add a new porcelain command for better usability despite the existing capability, or (2) instead improve documentation to make the current `show -p` option more visible. Eric suggests that enhancing the man pages to better highlight the `--patch` option for both `show` and `list` subcommands might be preferable to adding a new command. The core questions remain whether the learning curve for `show -p` justifies a dedicated command, and how Git should balance discoverability against command proliferation. So far no other contributors have joined the discussion.