Feature proposal for improving stash diff usability. Eric Sunshine responds to Pierre Houston's request for a simpler way to view stash diffs, pointing out that `git stash show -p` already provides this functionality. The exchange highlights a documentation discoverability issue - while the capability exists, it's not obvious from the command structure or man page organization. 

Eric's response suggests the feature request may be unnecessary since the functionality is implemented, albeit poorly documented. This is a common pattern in Git discussions where existing features are overlooked due to documentation gaps rather than implementation gaps. The thread now turns toward whether better documentation would suffice or if a dedicated `git stash diff` command would improve usability despite the overlap with existing options.