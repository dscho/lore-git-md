This is the final patch in Patrick Steinhardt's v3 series improving the `git history` command's UI. The series has evolved through multiple iterations with all major feedback addressed, and this patch completes the documentation by specifying that `--update-refs` defaults to updating branches (rather than just HEAD) when no value is provided.

The change is straightforward - a one-line documentation update that makes the default behavior explicit in the man page. This follows naturally from the previous patches in the series, which renamed `--ref-action` to `--update-refs` and introduced clearer flag semantics. The patch maintains consistency with the implementation while improving user-facing documentation.

With this patch, the series appears complete and well-polished after three iterations, with all reviewer feedback incorporated. The changes are focused and uncontroversial, representing the final refinement of a feature that has already received thorough review.