This is the second patch in a 4-part series implementing UTF-8 support for Git configuration subsections, specifically for aliases (like `[alias "förgrena"]`). The series is now reached its fifth iteration with all known issues addressed, including a shell completion bug fixed in v5.

The current change prepares for subsection aliases by switching from `skip_prefix()` to `parse_config_key()` for alias parsing in `help.c`. This properly handles the three-level config key structure needed for subsection support. The change introduces a compatibility break by no longer accepting malformed "alias.subsection.key" entries (unless the key is "command"), which was never an intended feature.

The patch is well-motivated and clearly explained, noting both the technical change and its backward compatibility implications. It's part of a mature series that has undergone extensive review, with all prior feedback incorporated. The implementation looks solid, replacing the simple prefix check with proper config key parsing while maintaining the existing functionality for simple aliases.

This is a refactoring that enables the main feature (UTF-8 subsection aliases) in the next patch while fixing what was effectively a bug in the old implementation. The series as a whole appears ready for merging based on the thorough review history and maintainer approvals noted in the thread context.