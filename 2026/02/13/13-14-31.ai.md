This is a technical review comment on patch 3/5 of Justin Tobler's repository statistics series, focusing on OID annotations in the table output. Patrick Steinhardt provides detailed feedback on the implementation details, suggesting potential optimizations and discussing tradeoffs in the user interface.

The key technical points address memory management in the string handling code, noting that while the current implementation is correct, it could be made more efficient by using `strbuf_detach()` to avoid an extra string copy. He also questions the NULL OID handling case and whether it might occur when certain object types are empty.

For the test output format, Patrick considers but ultimately rejects the idea of using descriptive labels (like "[commits]") instead of numeric indices for the OID annotations, recognizing that while more readable, this would make the table columns too wide. The discussion remains focused on implementation quality rather than the feature's overall design, which appears settled from earlier thread discussion.

This email continues the pattern of thorough technical review for this series, with Patrick providing concrete suggestions while acknowledging the existing implementation's correctness. The exchange shows the series progressing toward finalization with attention to both performance and output formatting details.