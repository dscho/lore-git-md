This is part 2 of Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. The patch introduces a new 'gently' parameter to `format_config()` that will be used in later patches to enable type filtering during initial parsing rather than after full parsing.

The change is purely mechanical at this stage - adding the parameter and passing 0 (false) to all existing callers. The parameter is marked UNUSED to avoid compiler warnings since it's not yet functional. This prepares the infrastructure for the actual type-filtering logic that will be added in subsequent patches.

The patch touches only `builtin/config.c`, modifying the `format_config()` function signature and updating its callers. This is a straightforward change that follows the series' pattern of incremental refactoring to enable the performance improvements while maintaining existing behavior. The commit message clearly indicates this is groundwork for future changes rather than implementing new functionality itself.