This is patch 7/13 in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. The series refactors config parsing to enable type filtering during initial parsing rather than after full parsing, improving efficiency particularly for path-type values.

This specific patch extracts the boolean-or-string formatting logic into a dedicated helper function `format_config_bool_or_str()`. The change is purely mechanical - it moves existing code without altering behavior, maintaining the gentle parsing approach where invalid boolean values fall back to string output. The helper follows the pattern established in earlier patches for bool and bool-or-int cases, keeping the codebase consistent as the series progresses toward its goal of type-aware parsing.

The patch touches only `builtin/config.c`, modifying how config values are formatted when the type is `TYPE_BOOL_OR_STR`. It's a straightforward refactoring that builds on the infrastructure changes from patches 1-6, with no open questions or controversy apparent in the implementation. The commit message clearly states this is preparation for later behavioral changes rather than introducing new functionality itself.