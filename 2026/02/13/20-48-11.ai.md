This is a response to review comments on a patch in the shallow repository handling series. Samo Pogačnik is addressing feedback from Patrick Steinhardt on v4 of the second patch, which implements correct relative-depth fetching (`--deepen`).

The review focuses on several implementation details in the shallow.c and upload-pack.c changes:

1. Code style improvements - Patrick suggests declaring loop variables inline and removing unnecessary braces in conditional blocks, which Samo agrees with.

2. Behavioral inconsistencies - Patrick questions why some operations (updating result, setting shallow_flag) are skipped when the `shallows` parameter is provided. Samo explains this is intentional as the function's behavior differs based on whether it's measuring depths or producing shallow commits.

3. Function organization - Patrick recommends inlining the `get_shallows_depth()` helper function into its single caller in upload-pack.c, which Samo acknowledges.

4. Redundant operations - Patrick points out the current implementation calls `get_shallow_commits()` twice in some cases, which seems inefficient. Samo expresses caution about making broader changes that might affect other scenarios beyond relative-depth fetching.

The exchange shows productive technical discussion between an experienced reviewer (Patrick) and a contributor (Samo) who is carefully considering the feedback while maintaining awareness of the patch's scope. Samo indicates they will address all points in a future version, suggesting this thread is nearing completion after multiple review iterations.

The technical focus remains on proper handling of shallow repository edge cases during fetch operations, particularly around depth calculations for relative deepening scenarios. The changes touch core repository functionality but appear well-contained to the specific issue being addressed.