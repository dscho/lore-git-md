This is the final patch in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. The series refactors config parsing to enable type filtering during initial parsing rather than after full parsing, addressing a performance bottleneck particularly for path-type config values.

Patch 13/13 restructures the core `format_config()` function to use a switch statement instead of nested if/else conditions, following the pattern established in previous patches where type-specific formatting was moved to helper functions. The change reduces nesting depth and improves readability while maintaining identical behavior. A new `goto terminator` pattern handles the early exit case when omitting values.

The patch completes the series' transformation of the config formatting logic, which now cleanly separates concerns between the main dispatch logic and type-specific handlers. All major design questions from earlier in the thread have been resolved, with this patch representing the final polish to the implementation structure. The series as a whole achieves its goal of improving performance for type-filtered config listing while maintaining backward compatibility.