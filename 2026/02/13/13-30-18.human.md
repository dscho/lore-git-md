This is a patch in the ongoing HTTP 429 rate limiting series from Vaidas Pilkauskas, addressing review feedback from Taylor Blau. The series adds RFC-compliant handling of Retry-After headers when servers return HTTP 429 (Too Many Requests) responses.

The author responds to several specific review comments:
1. Changes the default `http.retryAfter` value from -1 to 0, dropping the idea of advising users about configuration since this is expected to be used by advanced users anyway
2. Leaves HTTP_RATE_LIMITED handling in legacy http-push.c unchanged per Jeff King's earlier suggestion
3. Removes an unnecessary `header_is_last_match` assignment that wasn't doing anything useful
4. Acknowledges but doesn't handle header continuations for Retry-After since they're not expected in practice
5. Defers to Jeff King's upcoming review on whether `run_active_slot()` is the right place for retry delay logic
6. Notes an unrelated strbuf.c change that was meant to fix a CI-reported leak will be split into a separate commit

The patch shows good responsiveness to review feedback while appropriately deferring on architectural questions where deeper expertise is needed. The discussion remains focused on technical details, with the author making clear choices about which suggestions to implement immediately versus which to defer for further input. The series appears to be in good shape pending resolution of the remaining open questions about retry implementation details.