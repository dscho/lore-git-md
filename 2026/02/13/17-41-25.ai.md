This is a security-focused patch series addressing vulnerabilities in `git am` patch parsing. Phillip Wood's v3 iteration of the commit-msg hook now includes refined handling of edge cases that could trigger false positives while still catching dangerous patterns:

The hook now specifically targets unindented "diff -" and "Index: " lines in commit message bodies while allowing:
1. Email headers starting with "diff -" (since they won't be parsed as patch content)
2. The standard "---" separator line between commit message and diff
3. Scissors lines (commit message truncation markers)
4. Comment characters

The implementation includes test coverage for `git commit --cleanup=scissors --verbose` scenarios. This builds on Kristoffer Haugsbakk's documentation warnings (already merged to `next`) about patch roundtrip hazards, forming part of a layered security approach that also includes Jeff King's proposed `--strict` mode for `git am` and Patrick Steinhardt's `--accept-ambiguous-patch` flag.

Junio's response indicates satisfaction with the current state, having queued the documentation patch and now reviewing this hook implementation. The thread shows consensus forming around combining documentation, defensive hooks, and parser improvements to address the long-standing issue where tools can misinterpret commit message content as patch data.