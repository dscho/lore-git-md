This is a follow-up review comment on Amisha Chhajed's patch to standardize string list sorting in `git help -c` output, which is part of the v2 series about consistent string list behavior across Git commands.

Eric Sunshine points out two minor test script improvements for the edge case demonstration test:
1. The `sed` and `sort` commands in the test pipeline should be dedented to align with the `grep` command for consistent formatting
2. Suggests consolidating the `grep` and `sed` operations into a single `sed` command where possible, following common test script conventions

The comment builds on Junio's earlier feedback about test formatting and represents final polishing touches before the patch can be merged. This is typical of the meticulous attention to test script style that Eric Sunshine consistently provides in his reviews. The core functionality of the patch remains unchanged - it's just about refining the presentation of the test case that demonstrates why the string list sorting standardization is needed.