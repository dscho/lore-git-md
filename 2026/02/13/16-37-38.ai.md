This is a security-focused patch series addressing repository discovery hardening. Tian Yuchen responds to Junio Hamano's feedback on their v2 patch that validates `.git` file types during repository setup.

The key technical point is Junio's suggestion to refine the error taxonomy in `read_gitfile_gently()` by introducing new error codes (`STAT_ENOENT`, `IS_A_DIR`) while making other cases (`STAT_FAILED`, `NOT_A_FILE`) consistently fatal when `die_on_error` is true. Tian acknowledges this approach and commits to implementing it in v3, noting it will require auditing all callers but should simplify the overall logic.

The exchange shows the discussion moving toward consensus on preserving historical symlink support (as raised by brian m. carlson) while tightening validation for other special file types. Junio's architectural guidance shifts the focus from the original `.git` validation to a broader error handling refactor in the repository setup path. Tian's response indicates they understand the scope of changes needed and will proceed with the more comprehensive solution.

This email marks a transition point where the thread moves from debating specific validation behavior to planning a structural change in how Git reports repository discovery errors. The security motivation remains, but the implementation is evolving to address both safety and backward compatibility concerns.