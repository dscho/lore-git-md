This is a straightforward refactoring patch that's part of a larger effort to standardize string list sorting behavior across Git. The patch modifies `builtin/sparse-checkout.c` to use `string_list_sort_u()` (which combines sorting and deduplication) instead of separate calls to `string_list_sort()` and `string_list_remove_duplicates()`.

The change affects how `git sparse-checkout list` displays patterns - the output will now be consistently sorted and deduplicated in one operation. This matches the behavior of other commands being standardized in the series. The patch has already been reviewed and acked by Junio Hamano as "obviously correct" in the thread discussion.

The technical change is minimal - just replacing two function calls with one equivalent call in the pattern display logic. The patch maintains all existing functionality while making the code more consistent with Git's evolving string list API conventions. This is a mechanical change with no expected behavior difference, as the end result of sorting plus deduplication remains the same.