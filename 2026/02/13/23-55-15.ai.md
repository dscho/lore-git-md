This is patch 10/13 in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. The series refactors config parsing to enable type filtering during initial parsing rather than after full parsing, improving efficiency particularly for path-type values.

This specific patch introduces `format_config_expiry_date()`, extracting expiry date handling into a dedicated function that supports gentle parsing (silently skipping invalid dates when requested). The change makes `git config list --type=expiry-date` quiet when encountering non-date values, matching the behavior pattern established earlier in the series for other types.

The implementation maintains the same core behavior but with two key differences from the original: 1) moves the expiry date logic into its own function following the series' pattern, and 2) uses gentle parsing to avoid error messages for non-date values when filtering. The test changes confirm the new silent behavior while still verifying valid dates are properly formatted.

This continues the series' incremental refactoring approach, with each patch focusing on one config type's handling. The changes are mechanical but important for the overall goal of enabling early type filtering. The patch follows the established pattern seen in earlier parts of the series (like patch 9's path handling) and maintains test coverage while simplifying error output expectations.