Performance improvement patch (11/13) in Derrick Stolee's series to optimize `git config list --type filtering`. This patch introduces a gentle parsing variant for color values, following the pattern established earlier in the series for other config value types.

The change adds `color_parse_gently()` as a non-error-throwing version of `color_parse()`, implemented by factoring out the core parsing logic into a new helper function `color_parse_mem_1()` that takes a 'gently' parameter. When gently parsing fails, it returns -1 rather than showing an error message. This matches the behavior pattern seen in previous patches for bool, int, path, and expiry date parsing.

The implementation maintains all existing functionality while enabling silent failure - important for the config list filtering use case where invalid values should be quietly skipped rather than erroring out. The patch includes the expected header declaration and minimal code reorganization to keep related functions together.

This continues the series' consistent approach to type-specific parsing improvements, with color handling patch coming after similar changes for other types. The change is mechanical and well-contained, following the established pattern from earlier patches in the series.