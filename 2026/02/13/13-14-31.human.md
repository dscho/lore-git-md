This is a review comment on patch 3/5 of Justin Tobler's series adding repository statistics to `git repo`. Patrick Steinhardt provides feedback on the implementation details:

1. He suggests using `string_list_append_nodup()` to avoid an extra string copy when adding OID annotations to the statistics table output. This is a minor optimization to the memory management.

2. He confirms there's no memory leak in the existing code despite the late `strbuf_release()` call, since the buffer was previously detached.

3. He notes that the NULL OID case (when no objects exist of a given type) is handled correctly by skipping annotation.

4. He considers but dismisses the idea of using more descriptive annotation labels (like "[commits]") instead of numbers, as it would make the table output too wide.

The discussion remains focused on implementation details of displaying object IDs alongside size statistics, with Patrick raising no major objections to the approach. This continues the thread's pattern of refining the technical implementation while the higher-level feature design questions (like octopus merge tracking) were settled earlier in the thread.