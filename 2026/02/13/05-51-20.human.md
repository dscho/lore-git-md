This is a brief follow-up in Patrick Steinhardt's series improving the `git history` command UI. The exchange clarifies a technical implementation point regarding string array handling in the new merge detection logic.

In response to D. Ben Knoble's observation about the string copying approach in patch 2, Junio Hamano points out that `strvec_pushv()` provides the desired functionality for efficiently copying string arrays. Patrick confirms he'll use this existing API instead of the manual loop shown in the patch.

The thread context shows this is part of broader changes to move validation earlier in `git history`'s execution, specifically checking for unsupported merge commits before any user interaction. The discussion remains focused and technical, with Patrick quickly accepting the suggested improvement to his implementation.