This is a follow-up discussion about test infrastructure in Patrick Steinhardt's `git history` series. Junio Hamano confirms the purpose of using `-c core.editor=false` in the test case - it ensures the editor is never invoked by making the editor command fail immediately, which would produce a different error message if the test's primary validation check didn't catch the expected error first. 

Junio suggests adding a clarifying comment to explain this testing technique, though he's unsure whether the explanation would be too verbose. The exchange shows the series is in its final polishing stages, with reviewers now on the same page about the test approach. This is a minor documentation refinement to an already-reviewed patch that performs early validation checks in the `git history` command.