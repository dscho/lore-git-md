Feature implementation for HTTP 429 rate limiting support. Vaidas Pilkauskas responds to Taylor Blau's review feedback on the v2 series implementing RFC-compliant handling of HTTP 429 responses with Retry-After headers. The key changes in this response:

1. Agrees to change the default `http.retryAfter` value from -1 to 0, dropping the advisory message since advanced users will likely be configuring this feature.

2. Confirms leaving the HTTP_RATE_LIMITED error handling in http-push.c and http-walker.c unchanged per Jeff King's earlier suggestion to avoid implementing full retry logic in legacy protocols.

3. Removes an unnecessary `header_is_last_match` assignment that had no effect since the code path immediately exits.

4. Acknowledges but does not implement handling for header continuations in Retry-After parsing, considering it an unlikely real-world scenario.

5. Defers to Jeff King's pending review on whether `run_active_slot()` is the appropriate layer for retry delays, indicating this may require significant restructuring.

6. Notes a separate strbuf.c change was accidentally included to address a CI-reported memory issue and will be split into its own commit.

The exchange shows the patch series moving toward consensus on configuration defaults and error handling while awaiting resolution of deeper architectural questions about where to place the retry logic. The author demonstrates responsiveness to review feedback while appropriately deferring on questions requiring more expert input. The thread remains active with Jeff King's major structural concerns still pending.