Thread summary:

Performance optimization discussion for using Linux's `sendfile()` system call in Git's file copying operations, originally proposed by George Hu. The patch modifies `copy.c`'s `copy_fd()` function, with Linux-specific implementation planned for `compat/linux/` per Phillip Wood's maintainer feedback. Technical requirements established include proper error handling (fstat(), EAGAIN, EINVAL) and fallback to read/write loops.

Jeff King (Peff) introduces a key practical concern: whether the optimization provides measurable benefits given Git's typical usage patterns. He notes most `copy_file()` operations handle small files where read/write overhead is negligible, with only a few potential exceptions (bundle-uri operations, local clones without hardlinks). Peff emphasizes the need for concrete benchmark data to justify the added complexity of platform-specific code, especially given FreeBSD's socket-only `sendfile()` restriction (per Chris Torek's earlier input).

Current technical consensus includes: Linux implementation would live in `compat/linux/` with proper error propagation, while maintaining the existing read/write fallback. Key unresolved questions now include both implementation details (error handling strategy, fallback conditions) and the fundamental justification for the change (performance impact evidence).

Participants: George Hu (author), Phillip Wood (maintainer review), Chris Torek (BSD compatibility), Jeff King (performance analysis). The thread has evolved from implementation specifics to weighing optimization value against maintenance costs, while retaining all technical context about platform constraints and code structure. Benchmark data remains the critical missing piece for evaluating whether to proceed with this Linux-specific optimization.