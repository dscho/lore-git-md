Thread summary:

Performance optimization for Linux file copying using sendfile(). The thread begins with a patch to `copy.c` introducing Linux-specific optimization via `sendfile()` system call for copying between file descriptors, replacing the read/write loop with kernel-space operation under `#ifdef __linux__`. Maintains portability with fallback to existing implementation. Handles partial writes despite `sendfile()` typically completing in one operation on modern kernels. Preserves existing error handling via `COPY_WRITE_ERROR`. Patch is narrowly scoped to copy functionality without broader architectural impact. Missing benchmark data to quantify improvement.