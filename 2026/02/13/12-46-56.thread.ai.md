Thread summary:

Performance optimization for file copying using sendfile() system call, originally proposed by George Hu with Linux-specific implementation via `#ifdef __linux__` in `copy.c`. Chris Torek's latest input reveals FreeBSD's `sendfile()` only works with sockets, unlike Linux's general file-to-file capability, confirming significant API divergence between platforms. This makes a portable solution complex, potentially requiring either Linux-only optimization or sophisticated compatibility layer. Phillip Wood's earlier review identified implementation issues: recommends moving platform-specific code to `compat/`, notes missing `fstat()` error handling, improper `sendfile()` error propagation, and suggests handling `EAGAIN` for non-blocking writes with fallback to read/write loops on `EINVAL`. Discussion remains focused on technical feasibility without revisiting optimization premise, still lacking benchmark data to quantify performance improvement. Current status shows Linux-specific approach may be only viable path given platform API differences, with Phillip's review establishing additional requirements for error handling and code organization before merge consideration. Open questions remain about whether performance gains justify complexity, with FreeBSD's socket limitation now clearly established. Participants: George Hu (author), Chris Torek (BSD compatibility input), Phillip Wood (maintainer-level code quality review).