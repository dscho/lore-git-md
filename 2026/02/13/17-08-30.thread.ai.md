Git apply --directory path handling fix

Thread identity: Bugfix for `git apply --directory` option when given paths starting with `./`. Originating author Joaquim Rocha. Thread type: bugfix.

Core problem: When `git apply --directory=./some/sub` is used, the `./` prefix causes the command to fail with an invalid path error, despite this being common notation. The behavior differs from non-prefixed paths.

Current status: Joaquim Rocha's v2 patch implements Patrick Steinhardt's suggestion to use `strbuf_normalize_path()` for comprehensive path normalization rather than just stripping "./" prefixes. Discussion has evolved to focus on user experience justification for the more general solution versus a minimal fix.

Key technical details: Implementation uses `strbuf_normalize_path()` to handle: `./` prefixes, `././` sequences, `some/./sub` paths, and `../` traversal attempts (rejecting leading `../`). Tests in `t4128-apply-root.sh` verify all cases. Performance impact remains negligible given the function's existing use elsewhere.

Open questions: Whether user experience concerns justify comprehensive normalization versus Junio Hamano's preference for minimal solutions. Rocha now argues the current behavior is unintuitive enough to warrant the general solution, even for rare edge cases, if there's no clear downside. The thread awaits Junio's decision on whether this justification meets his bar.

Participant positions: Joaquim Rocha maintains support for comprehensive solution based on user experience arguments. Patrick Steinhardt remains ambivalent, having raised but not strongly defended the generalization. Junio Hamano's position (preferring minimal fixes) remains unchanged but he hasn't yet responded to the specific UX justification in v2.

Related work: Builds on `strbuf_normalize_path()` infrastructure. Discussion reflects ongoing tension between comprehensive solutions and minimal targeted fixes in the project, now with added focus on experience rather than just technical correctness.