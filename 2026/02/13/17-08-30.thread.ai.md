Git apply --directory path handling fix

Thread identity: Bugfix for `git apply --directory` option when given paths starting with `./`. Originating author Joaquim Rocha. Thread type: bugfix.

Core problem: When `git apply --directory=./some/sub` is used, the `./` prefix causes the command to fail with an invalid path error, despite this being common notation. The behavior differs from non-prefixed paths.

Current status: Patch accepted by Junio Hamano. The v2 implementation using `strbuf_normalize_path()` for comprehensive path normalization will be merged. This resolves all path format cases including `./` prefixes, `././` sequences, `some/./sub` paths, and rejects invalid `../` traversal attempts.

Key technical details: Final implementation uses `strbuf_normalize_path()` for consistent handling of all path formats. Tests in `t4128-apply-root.sh` verify behavior. Performance impact remains negligible as the function is already widely used. The solution maintains backward compatibility while fixing the unintuitive failure cases.

Open questions: None remaining - Junio's acceptance closes the discussion. The thread demonstrates that user experience concerns can justify more comprehensive solutions when they address real usability pain points with minimal technical cost.

Participant positions: Joaquim Rocha's UX-focused argument prevailed. Patrick Steinhardt's initial suggestion to use `strbuf_normalize_path()` was implemented. Junio Hamano accepted the general solution after initially preferring minimal fixes, indicating the UX justification met his standards.

Related work: Builds on existing `strbuf_normalize_path()` infrastructure. Serves as a case study in balancing minimalism against usability improvements when both approaches are technically sound.