**Follow-up review on help command test cleanup**

Eric Sunshine provides final polishing feedback on Amisha Chhajed's patch to standardize config key sorting in `git help -c` output. This is part of the now-complete v2 series addressing string list consistency across Git commands.

Sunshine notes two small test script improvements needed before merging:
1. The `sed` and `sort` commands in the test pipeline should align with the `grep` indentation level
2. Suggests consolidating the `grep` and `sed` operations into a single `sed` command where possible, following Git's test script conventions

These are minor style tweaks to the test case that demonstrates how the patch prevents potential future issues with config key sorting. The core functionality has already been accepted, with Junio previously suggesting some architectural follow-ups for the help command output handling.

The thread shows the final stages of review where experienced contributors help polish the last details before a patch is ready to merge. Sunshine's feedback reflects Git's emphasis on consistent test script style and avoiding unnecessary command pipelines.