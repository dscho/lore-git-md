Here's a human-readable thread summary:

This thread addresses a bug in `git apply --directory` where paths starting with `./` (like `--directory=./some/sub`) would fail with an invalid path error. The patch provides a straightforward fix by stripping the `./` prefix before processing the directory path, making it behave consistently with non-prefixed paths.

The change is minimal (just 4 lines in `apply.c`) but solves a real usability issue where a common notation would unexpectedly fail. A new test case in `t4128-apply-root.sh` verifies the fixed behavior matches the existing handling of non-prefixed paths. The patch appears well-contained and focused, with a clear commit message explaining both the problem and solution.