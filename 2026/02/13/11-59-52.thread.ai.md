Here is the dense AI thread summary for future reference:

This RFC series from Shreyansh Paliwal refactors how Git represents the primary worktree in the worktree API to eliminate inconsistent representations (NULL pointers, NULL id fields, and "/" keys). The goal is to standardize on worktree structs with wt->id set to "/" as the sole representation, reducing global state in wt-status and removing ambiguity.

The series consists of two patches: (1) changes worktree.c internals to use "/" instead of NULL for primary worktree identification, and (2) introduces get_primary_worktree() helper and updates API callers to use it rather than passing NULL. The changes touch core worktree handling code across builtins (fsck, worktree), refs handling, and revision walking infrastructure.

Key technical details: The primary worktree currently has three representations that must be handled equivalently - NULL pointers, worktree structs with NULL id fields, and the "/" key in ref-store maps. This leads to scattered NULL checks and implicit global access patterns. The new approach would consistently use worktree structs with wt->id="/" everywhere.

Open questions: As an RFC, the author is specifically soliciting feedback on whether this standardization approach is the right direction before proceeding. The changes are mechanical but touch core worktree code, so careful review of the design is needed. No specific objections or alternative approaches have been raised yet in the thread.