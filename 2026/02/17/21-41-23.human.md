This is a bugfix patch addressing an inconsistency in `git format-patch`'s handling of the `--from` option in cover letters.

The patch follows up on a discussion where Junio C Hamano suggested documenting a change in behavior, but the author argues this should be treated as fixing an obvious bug rather than introducing new behavior. The issue is that `--from` currently doesn't affect the From header in cover letters, which the author considers clearly unintended.

The patch will modify the documentation to clarify this behavior without framing it as a change. This appears to be a straightforward correction of what the author sees as an obvious oversight in the current implementation. The change seems uncontroversial and well-motivated, though it's worth noting the discussion shows there was some initial disagreement about how to characterize the fix.