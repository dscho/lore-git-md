This is a bugfix patch addressing an inconsistency in `git format-patch`'s handling of the `--from` option in cover letters. The thread context shows Junio C Hamano and Patrick Steinhardt previously discussing whether this should be considered a behavior change or a bugfix. The author, Mirko Faina, argues this is clearly a bug since `--from` should logically apply to cover letters by default, and plans to update the documentation to remove any ambiguity.

The patch appears straightforward, targeting a specific edge case in `format-patch` where the From header wasn't being properly propagated to cover letters when the `--from` option was used. This affects users who rely on `--from` to set authorship information consistently across all parts of a patch series. The discussion suggests this was an oversight rather than a deliberate design choice, making the bugfix classification appropriate.

For future summarization: this touches `builtin/log.c` and related documentation, fixing how `--from` applies to cover letters. The key point of discussion was whether to frame this as a behavior change (as Junio initially suggested) or a bugfix (the author's position that prevailed). The documentation updates will clarify the expected behavior.