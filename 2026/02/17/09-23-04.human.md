This is a worktree API refactoring patch that's part of an ongoing effort to clean up how Git handles worktree operations. Phillip Wood follows up on his earlier patch with a small correction to a variable name (`commondir` -> `gitdir`) in the new `get_worktree_from_repository()` helper function.

The patch continues the thread's focus on eliminating NULL worktree parameters in favor of explicitly constructed worktree structs. It adds BUG() assertions for NULL worktree parameters in `wt_status_check_rebase()` and `wt_status_check_bisect()`, and updates `wt_status_get_state()` to use the new helper function. The changes maintain backward compatibility while making the API more robust.

Phillip mentions he'll wait for additional feedback before sending a v2, which may include a new test case for running `git status` during a rebase in a linked worktree. This is a small but meaningful step in the larger worktree refactoring effort that has been progressing steadily with Junio Hamano's guidance.