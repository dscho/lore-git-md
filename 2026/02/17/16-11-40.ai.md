This is a performance optimization discussion in the context of Derrick Stolee's config type-filtering series. The exchange focuses on a minor implementation detail in patch 3/13 regarding output formatting efficiency.

Patrick Steinhardt suggests that `puts()` could potentially replace the current `fwrite()` call for writing formatted config output, while acknowledging the efficiency of knowing the buffer length beforehand. Junio C Hamano counters that `puts()` wouldn't be suitable since the series doesn't always use LF-delimited output (the terminator can be configured via `opts->term`).

The discussion is purely about implementation minutiae in an otherwise settled patch - the core design of type-filtered config listing is not in question here. This represents the final polishing phase of the series where reviewers are examining small details before the patches are ready for merging. The thread context shows the broader series has already addressed all substantive technical concerns.