This is a security-related discussion thread about hardening Git's repository discovery by validating `.git` file types. The conversation focuses on error handling semantics in the `read_gitfile_gently()` function, which is part of Tian Yuchen's patch series (currently at v4).

Junio Hamano provides authoritative clarification on the error handling strategy, defending the current split of stat() failures into `ERR_STAT_ENOENT` (non-fatal) versus `ERR_STAT_FAILED` (fatal). He explains the semantic distinction between "path doesn't exist" (where Git should continue searching) versus "invalid path" (where it should fail). Karthik Nayak confirms understanding and agrees with Junio's explanation.

The discussion resolves a point of confusion about where to place error exclusions in the code, with Junio arguing they should be in `read_gitfile_error_die()` rather than at the caller site. This represents final polishing of the implementation details after earlier rounds of review addressed the core security concerns. The series appears to be in its final stages, with only minor build system integration remaining to be addressed.