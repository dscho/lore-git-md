This is a follow-up discussion about a bugfix for `git apply --directory` path handling. The original patch from Joaquim Rocha addressed a specific case where paths starting with `./` would fail, but Patrick Steinhardt suggests a more comprehensive solution.

Patrick points out that the initial fix only handles the `./` prefix case, leaving other path normalization issues unaddressed (like `././`, `some/./sub`, or paths with `..`). He proposes using `strbuf_normalize_path()` instead, which would handle all these scenarios by properly normalizing the path before processing. This change would make the behavior more robust and consistent with how Git typically handles paths.

The technical change is minimal - just one line added to `apply.c` to call the existing path normalization function. This approach has the advantage of leveraging Git's existing path handling infrastructure rather than adding special cases. The discussion is still early, but Patrick's suggestion appears to be a more complete solution to the underlying issue.