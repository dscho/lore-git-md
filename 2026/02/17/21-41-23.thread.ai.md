Thread identity: Bugfix for `git format-patch --from` behavior in cover letters, authored by Mirko Faina. Addresses an inconsistency in how the `--from` option applies to cover letters versus individual patches.

Core problem: The `--from` option in `git format-patch` was not propagating authorship information to cover letters, creating an inconsistency within patch series. This was an oversight in the original implementation rather than a deliberate design choice.

Current status: The patch is being framed as a bugfix (not a behavior change) since the `--from` option should logically apply consistently to all parts of a patch series. Documentation updates will clarify this expected behavior.

Key technical details: Modifies `builtin/log.c` to properly handle `--from` for cover letters. The fix ensures consistent authorship information appears in both cover letters and individual patches when `--from` is specified.

Participant positions:
- Mirko Faina (author) maintains this is clearly a bugfix
- Junio C Hamano initially questioned whether to consider it a behavior change
- Patrick Steinhardt participated in earlier discussion about the classification

Related work: Builds on existing `format-patch` functionality, particularly the cover letter generation and `--from` option handling.