This is a performance optimization discussion in the context of Derrick Stolee's series to improve `git config list --type=<X>` filtering.

Junio C Hamano responds to Patrick Steinhardt's observation about using `fwrite()` versus `puts()` for output formatting. Junio notes that `puts()` wouldn't be suitable since the output isn't always LF-delimited (the terminator can be configured via `opts->term`). The discussion here is a minor implementation detail in patch 3/13 of the series, which refactors config value formatting to enable type filtering during parsing rather than after.

The exchange shows the maintainer weighing in on a low-level implementation choice, confirming that the current `fwrite()` approach is correct given the flexibility needed in the terminator character. This is a settled point in an otherwise complete series that has already addressed all major design questions.