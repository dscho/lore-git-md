This is a review comment on the final patch in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. Patrick Steinhardt provides feedback on the refactored `format_config()` function in builtin/config.c.

Patrick approves of the switch statement structure as being more readable than the previous if-else chain, though he notes a stylistic preference against the empty newlines between cases. His substantive suggestion is to make the switch statement more defensive by explicitly listing all valid TYPE_* cases and using BUG()` in the default case to catch invalid types at runtime.

The comment comes late in a well-developed series where the technical approach is already settled. Patrick's feedback focuses on final polish rather than fundamental changes, indicating the series is nearing completion. His suggestion about defensive programming aligns with Git's coding style but doesn't affect the core functionality or performance improvements of the series.

This patch wraps up a 13-part series that optimizes config value filtering by moving type checks earlier in the parsing process, particularly benefiting path-type config values that were a bottleneck for git-credential-manager. The thread shows consensus on the technical approach with only minor style points remaining.