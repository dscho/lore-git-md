This is a bugfix patch (v3) for `git format-patch`'s the `--from` option handling in cover letters. The patch corrects an inconsistency where the `--from` option was being applied to individual patches but not to cover letters in a series.

The author Mirko Faina notes this is a resend of the previous version (v3 marking was accidentally omitted before) with the sender name corrected. The core fix remains unchanged from v2, which had already addressed all technical feedback from reviewers including Junio Hamano, Patrick Steinhardt, and Jeff King. 

The patch modifies `builtin/log.c` to ensure the `--from` option properly sets the From header in cover letters, matching the behavior already working for individual patches. Test coverage in t4014 verifies the fix while maintaining existing functionality. Documentation has been updated to explicitly state that `--from` applies to both patches and cover letters.

This version appears ready for merging, with all prior review feedback incorporated. Junio had suggested two follow-up improvements (changing the cover letter patch listing format and ensuring commit message clarity) that remain as future work. The patch remains focused and small (6 lines modified) while resolving a clear inconsistency in email generation behavior.