This is a follow-up discussion about a bugfix for `git format-patch`'s handling of the `--from` option in cover letter generation. The thread has already established that the fix is technically sound (with v2 addressing all prior feedback), and now focuses on final documentation and test refinements.

D. Ben Knoble points out an amusing circularity in the documentation: while Jeff King noted that `git send-email`'s cover letter generation is more commonly used, the `git send-email` man page actually demonstrates using `git format-patch` to create the cover letter. This observation reinforces the need to properly document `--from` behavior for both individual patches and cover letters, since the tools are often used together in practice.

The exchange highlights how subtle documentation issues can persist across tools that interact in non-obvious ways. With both Patrick Steinhardt and Jeff King having reviewed the technical aspects, this appears to be a straightforward fix nearing completion, with only minor wording adjustments remaining before merge. The discussion remains focused and constructive, typical of final polish for a well-understood bugfix.