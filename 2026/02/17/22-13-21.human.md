This is a minor version bump (v3) of a bugfix patch for `git format-patch`'s cover letter generation, specifically addressing the `--from` option handling. The patch is unchanged from v2, which had already addressed all technical feedback from reviewers including Junio Hamano, Patrick Steinhardt, and Jeff King. The only changes in this submission are correcting the version marker to v3 and fixing the sender name in the email headers - the author notes this was an oversight in the v2 submission.

The core fix ensures the `--from` option (which specifies an alternative sender identity) is properly respected in cover letter From fields, matching the behavior already present for individual patches in a series. The patch appears ready for merging, with all maintainer feedback incorporated and technical correctness confirmed by multiple reviewers. Junio has suggested some follow-up documentation improvements, but those are noted as separate future work.