This is a test infrastructure patch from Patrick Steinhardt addressing CI failures on Windows/MSVC platforms, part of the ongoing series about `iconv(1)` executable availability when Git's ICONV support is enabled.

The patch series has already addressed all review feedback, including Christian Couder's latest note about clarifying whether the fix covers all MSVC+Meson CI failures or just the t4xxx tests. Patrick acknowledges this as a fair point and plans to split the changes in v3 to make the scope clearer. The core issue remains handling the distinction between libiconv (linked into Git) and the `iconv(1)` executable (used in scripts) across multiple test files.

Key technical points include the conditional use of iconv(1) only when the ICONV prerequisite is met, with UTF-8 as a fallback, and proper handling of test expectations. The discussion references GitHub issue #6083 about iconv(1) in Git for Windows, where the binary was unintentionally removed from recent builds. While that will be fixed separately, the test improvements make the suite more robust by not assuming the executable's presence.

The thread shows consensus on the approach, with all feedback incorporated. Patrick's response indicates the series is nearing final form, with only minor restructuring needed for clarity in v3 before merging. This continues the project's broader effort to improve test reliability across platforms, particularly for Windows/MSVC builds.