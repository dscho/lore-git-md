This is a review comment on patch 6/13 of Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. Patrick Steinhardt is verifying the behavior of the new `format_config_bool_or_int()` function, which handles both boolean and integer values in a "gentle" parsing mode (where invalid values don't error out).

Patrick notes that the function returns `1` when given a NULL value, matching Git's convention for implicit truthiness, and confirms this aligns with the existing `git_config_bool_or_int()` behavior. This is a minor implementation detail check rather than a substantive issue - the thread has already resolved all major design questions, and this series is in its final polishing phase before merging.