This is a technical discussion thread about the ongoing worktree API refactoring effort, specifically addressing how to handle NULL parameters in worktree-related functions.

Junio Hamano responds to Phillip Wood's suggestion about potentially embedding worktree reference in the repository structure (`struct repository`). Junio agrees with Phillip's conservative approach, noting that while adding a worktree reference to `struct repository` might be useful in the future, it requires more careful consideration and is not immediately necessary for the current wt-status.c cleanup.

The exchange confirms that the immediate goal is to clean up the problematic use of NULL to indicate "current worktree" in wt-status.c, possibly through a dedicated "get_current_worktree()" helper function, while deferring larger architectural changes like embedding worktree references in the repository structure. This maintains focus on the current scope of standardizing worktree representation and reducing NULL ambiguity without introducing potentially disruptive changes to core data structures.

The discussion shows alignment between Phillip Wood (who has been driving the technical details of the worktree API changes) and Junio (as maintainer) on keeping the changes incremental and focused, while leaving the door open for future structural improvements once the immediate cleanup is complete.