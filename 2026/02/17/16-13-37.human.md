This is a performance optimization patch in Derrick Stolee's series to improve `git config list --type=<X>` filtering. The discussion here focuses on a minor implementation detail in patch 3/13 regarding output formatting efficiency.

Patrick Steinhardt and Junio C Hamano are discussing whether to use `fwrite()` or `puts()` for writing formatted config output. Junio points out that since the output terminator is configurable (not always a newline), `fwrite()` is the correct choice despite Patrick's initial suggestion of `puts()`. Patrick quickly acknowledges this oversight.

This exchange represents the final polishing of implementation details in what appears to be a well-reviewed series. The core performance improvements (early filtering during config parsing) were already settled in previous discussion, and this thread shows the maintainers dotting the i's and crossing the t's on minor technical points. The series is clearly in its final stages before being picked up for integration.