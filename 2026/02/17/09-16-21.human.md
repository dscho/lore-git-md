This is a minor bugfix in the ongoing ref backend selection series, which is now in its final polishing phase with maintainer approval secured. The series enables zero-downtime migrations between ref backends (files<->reftable) through multiple configuration methods.

Karthik Nayak points out a trailing whitespace in an error message from Patrick Steinhardt's earlier patch. The message appears in `refs_compute_filesystem_location()` when validating worktree paths. Patrick confirms the issue and Karthik acknowledges it will be fixed.

This is a trivial formatting fix in an otherwise technically complex series that has already addressed all major architectural concerns. The thread shows the typical attention to detail in Git's review process, where even minor style issues get caught and corrected before merging. With this last polish, the series appears ready for final integration.