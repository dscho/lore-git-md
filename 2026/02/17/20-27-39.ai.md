This is a bugfix discussion thread about `git apply --directory` path handling.

Junio C Hamano is responding to Patrick Steinhardt's suggestion to use `strbuf_normalize_path()` for comprehensive path normalization in the `--directory` option. While Patrick's approach would handle not just the immediate `./` prefix issue but also other path normalization cases (like `././` and `..` components), Junio expresses skepticism about whether this level of generalization is necessary.

The core question is whether to:
1. Accept Joaquim Rocha's minimal fix that only strips `./` prefixes (the original reported issue)
2. Implement Patrick's more robust solution using Git's existing path normalization infrastructure

Junio's "don't do it then" comment suggests he may favor keeping the behavior simple and predictable rather than adding complexity to handle edge cases that may not be common in practice. The thread is still open with no clear resolution yet - this is Junio's first response to Patrick's suggestion, and we can expect further discussion about the tradeoffs between minimalism and robustness in path handling.