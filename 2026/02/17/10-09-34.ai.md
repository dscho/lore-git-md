This is a patch in the ongoing worktree API refactoring effort, specifically addressing how NULL worktree parameters are handled in wt-status.c. The discussion clarifies that `wt==NULL` should represent "use current worktree" (resolving to repo->gitdir) while `wt->id==NULL` means "main worktree" (resolving to repo->commondir), correcting a previous misunderstanding.

Phillip Wood proposes introducing a `get_current_worktree()` helper to construct worktree structs from repository data, allowing gradual cleanup of NULL parameters rather than a flag-day change. The thread also considers whether `wt_status_get_state()` should take a worktree parameter instead of repository, though this would require caller analysis.

Key technical points include:
- Maintaining backward compatibility while reducing NULL ambiguity
- Using BUG() assertions for NULL worktrees in status-checking functions
- Constructing temporary worktree structs in `wt_status_get_state()`
- Correcting gitdir/commondir terminology in implementation

The discussion shows steady progress on standardizing worktree representation while preserving existing behavior, with Phillip Wood providing implementation guidance and Junio Hamano offering architectural oversight. The patch represents incremental progress toward a cleaner worktree API with explicit parameter handling.