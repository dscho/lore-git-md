Here's a human-readable summary of the thread:

This thread addresses a bug in `git format-patch` where the `--from` option wasn't properly applying to cover letters, despite being applied to individual patches. Mirko Faina identified this as an inconsistency that should be fixed, since users would reasonably expect `--from` to work consistently across all parts of a patch series. 

The discussion initially saw some back-and-forth between Junio C Hamano and Patrick Steinhardt about whether this constituted a behavior change or a genuine bugfix. Mirko successfully argued it was clearly a bug, as the current behavior contradicts the logical expectation that `--from` should affect the entire output. The patch modifies `builtin/log.c` to fix this and includes documentation updates to remove any ambiguity about the intended behavior. 

This is a focused change that improves consistency for users who rely on `--from` to set authorship information, particularly when sending multi-patch series. The fix has been accepted as addressing a genuine oversight rather than changing intended functionality.