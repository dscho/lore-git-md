This is part 3 of a 4-patch series introducing a `--auto-advance` option for interactive patch selection (`add -p`, `checkout -p`, etc.). The current patch refactors the patch application logic in preparation for implementing the new navigation behavior.

The change extracts the patch application code from `patch_update_file()` into a new `apply_patch()` function. This is a mechanical refactoring that maintains the existing behavior while making the code more reusable for the upcoming navigation commands. The commit message explains that this prepares for implementing consistent patch application when users toggle between files with `--no-auto-advance`.

The patch is well-scoped, touching only `add-patch.c` with straightforward logic extraction. It follows the series' pattern of incremental preparation before introducing new functionality in later patches. The commit message clearly links this change to the series' overall goal of improving interactive patch navigation.

This appears to be a solid preparatory change that keeps the series moving forward. The refactoring is clean and the motivation is clear from both the commit message and the surrounding thread context. The next patch will presumably build on this to implement the actual navigation behavior.