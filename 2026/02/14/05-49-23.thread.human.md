This thread addresses a bug in `git format-patch` where the `--from` option wasn't being respected for cover letters, even though it worked correctly for individual patches in a series. The original patch fixed the issue with a one-line change in `builtin/log.c` to properly check the configured sender identity before falling back to default committer information.

Junio Hamano reviewed the initial version, confirming the technical approach was sound but requesting improvements to the commit message, a variable rename for clarity, and additional test coverage. The author has now sent a second version addressing all feedback: the commit message better explains the problem and solution, the variable has been renamed from `committer` to `from` to accurately reflect its purpose, and a new test case verifies the fix works while ensuring redundant in-body headers are still omitted. With these changes incorporated, the patch appears ready for merging pending any final review comments. The change is small (6 lines modified) and well-contained, with test coverage preventing future regressions.