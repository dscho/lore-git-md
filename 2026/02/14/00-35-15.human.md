This is a documentation and refactoring patch that renames the "keyvalue" output format to "lines" in the experimental `git repo` command. The change better reflects the format's newline-delimited nature and prepares for future extensions where output may not strictly be key-value pairs.

The patch updates all references across documentation, code, and tests, including renaming the internal enum from `FORMAT_KEYVALUE` to `FORMAT_NEWLINE_TERMINATED` while keeping the user-facing "lines" name for consistency. The commit message provides a clear rationale for the change, noting that "keyvalue" was both limiting and not descriptive of the actual behavior.

This appears to be the final iteration of this change in a series that has already received thorough review from Patrick Steinhardt and Junio Hamano. The implementation is clean, with comprehensive test updates and documentation alignment. As the `git repo` command is still experimental, this naming change can be made without backward compatibility concerns.