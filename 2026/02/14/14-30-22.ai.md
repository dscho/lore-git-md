This is a discussion about refactoring Git's worktree API to handle primary worktree identification more explicitly. Phillip Wood weighs in on Junio C Hamano's question about the difference between `wt==NULL` and `wt->id==NULL` in the worktree context.

Phillip clarifies that `wt==NULL` historically meant "use current worktree" while `wt->id==NULL` meant "main worktree", explaining why they used different paths (`repo->gitdir` vs `repo->commondir`). He suggests introducing a new `get_current_worktree()` function to properly handle the current worktree case, allowing gradual cleanup of NULL usage rather than a flag-day change.

The email reveals an important distinction in the worktree API that wasn't fully documented - the dual meaning of NULL parameters. Phillip's approach would maintain backward compatibility while moving toward more explicit worktree handling. He also raises the question of whether `wt_status_get_state()` should take a worktree parameter instead of a repository, though notes this would require examining callers.

This discussion shows the careful consideration needed when refactoring core APIs, particularly around edge cases and backward compatibility. Phillip's suggestion of a gradual transition path aligns with Git's conservative approach to changes in fundamental interfaces.