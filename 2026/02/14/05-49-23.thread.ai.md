Thread identity: Bugfix for `git format-patch` cover letter generation regarding `--from` option handling.

Core problem: The `--from` option (for specifying alternative sender identity) was not being respected in cover letter From headers while working correctly for individual patches in a series, creating an inconsistency in sender identity between cover letters and patches.

Current status: Second version of patch submitted, addressing all maintainer feedback. Change is technically sound with improved commit message, clearer variable naming, and added test coverage. Both Patrick Steinhardt and Jeff King have reviewed v2 and confirmed technical correctness while raising minor refinements to documentation and test structure. Discussion has now shifted to documentation updates to explicitly include cover letters in `--from` option's scope.

Key technical details: Fix modifies `make_cover_letter()` in `builtin/log.c` to properly check configured `from` value before falling back to default committer info. Variable renamed from `committer` to `from` to accurately reflect purpose. New test case in t4014-format-patch.sh verifies both the fixed behavior (cover letter respects `--from`) and maintains existing functionality. Change remains small (6 lines modified) and focused.

Open questions: Documentation needs explicit clarification - Jeff King notes current man page wording ("each commit email") stems from his 2013 implementation that didn't consider cover letters, and agrees it should be updated to explicitly include them. D. Ben Knoble identifies related documentation inconsistency in `git send-email` man page examples that assume cover letters are generated by `format-patch`. Test case could be refined to check cover letter file separately for clearer verification. These are final polishing items before merge.

Participant positions: Original author Mirko Faina proposed fix and incorporated all maintainer feedback in v2. Junio Hamano initially reviewed v1, confirming technical correctness while requesting improvements (commit message, variable naming, test coverage) - all addressed in v2. Patrick Steinhardt and Jeff King reviewed v2, both confirming technical soundness while suggesting documentation clarification (Jeff noting historical context). D. Ben Knoble contributes additional documentation analysis showing cross-command inconsistency. Consensus forming that man page should explicitly state `--from` applies to both patches and cover letters. No objections to merging after these minor refinements.

Related work: Touches email generation code shared with `git send-email` and other patch-formatting commands, but change remains isolated to cover letter behavior. Jeff King's historical context provides useful background for documentation update. Documentation inconsistency identified by D. Ben Knoble spans multiple commands (`format-patch` and `send-email`).