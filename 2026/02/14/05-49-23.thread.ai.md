Thread identity: Bugfix for `git format-patch` cover letter generation regarding `--from` option handling.

Core problem: The `--from` option (for specifying alternative sender identity) was not being respected in cover letter From headers while working correctly for individual patches in a series, creating an inconsistency in sender identity between cover letters and patches.

Current status: Third version of patch submitted (resend with corrected version marking and sender name). Core fix remains unchanged from v2 which had addressed all technical feedback. Documentation updates completed to clarify `--from` applies to both patches and cover letters. Junio Hamano's historical context and suggestions for follow-up work remain relevant. Patch appears ready for merging.

Key technical details: Fix modifies `make_cover_letter()` in `builtin/log.c` to properly check configured `from` value before falling back to default committer info. Variable renamed from `committer` to `from` to accurately reflect purpose. New test case in t4014-format-patch.sh verifies both the fixed behavior (cover letter respects `--from`) and maintains existing functionality. Change remains small (6 lines modified) and focused. Junio references commit a9080475 (2013) as historical context for the original implementation that didn't consider cover letters. Documentation now explicitly states `--from` applies to both patches and cover letters.

Open questions: Junio's two suggested improvements for future work remain: (1) updating cover letter patch listing format to use `log --oneline --reverse` instead of current `shortlog` output, and (2) ensuring both commit message and documentation reflect the behavioral clarification. D. Ben Knoble's identified documentation inconsistency between `format-patch` and `send-email` man pages remains to be addressed in future work.

Participant positions: Original author Mirko Faina has resubmitted v3 with only version marking and sender name corrections, confirming all prior technical feedback was addressed in v2. Junio Hamano, Patrick Steinhardt, and Jeff King had previously reviewed v2 and confirmed technical soundness while suggesting documentation clarification (Jeff providing historical context). D. Ben Knoble contributed additional documentation analysis. Consensus exists that the patch is ready to merge after final documentation updates, with Junio's suggestions for cover letter format improvements noted as potential follow-up work.

Related work: Touches email generation code shared with `git send-email`. Jeff King's historical context (commit a9080475) provides background. Documentation inconsistency spans multiple commands (`format-patch` and `send-email`). Junio's comments about cover letter listing format hint at future workflow improvements.