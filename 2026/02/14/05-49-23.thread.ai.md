Thread identity: Bugfix for `git format-patch` cover letter generation regarding `--from` option handling.

Core problem: The `--from` option (for specifying alternative sender identity) was not being respected in cover letter From headers while working correctly for individual patches in a series.

Current status: Initial patch version proposed, addressing the inconsistency between cover letter and patch email headers when `--from` is specified.

Key technical details: One-line modification in `builtin/log.c` to check for configured `from` value in `cfg` struct before falling back to default committer info. Affects cover letter generation path specifically.

Open questions: None raised yet - appears to be a straightforward fix with no obvious loose ends.

Participant positions: Single author so far (no reviews or other participants yet). Patch appears technically sound and focused.

Related work: Touches email generation code shared with `git send-email` and other patch-formatting commands, but change is isolated to cover letter behavior.