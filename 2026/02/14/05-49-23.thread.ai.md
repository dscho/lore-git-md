Thread identity: Bugfix for `git format-patch` cover letter generation regarding `--from` option handling, now at v4 with historical discussion.

Core problem: The `--from` option (for specifying alternative sender identity was not being respected in cover letter From headers while working correctly for individual patches in a series, creating an inconsistency in sender identity between cover letters and patches.

Current status: Patch approved by Junio Hamano for merging after four iterations (v4 resubmitted with corrected version marking and sender name). Core fix remains unchanged from v2 which had addressed all technical feedback. Documentation updates completed to clarify `--from` applies to both patches and cover letters. Test coverage verifies both fixed behavior and maintains existing functionality.

Key technical details: Fix modifies `make_cover_letter()` in `builtin/log.c` to properly check configured `from` value before falling back to default committer info. Variable renamed from `committer` to `from` to accurately reflect purpose. New test case in t4014-format-patch.sh verifies the fixed behavior. Change remains small (6 lines modified) and focused. Documentation now explicitly states `--from` applies to both patches and cover letters. Historical context traces to commit a9080475 (2013) where cover letter case was overlooked.

Open questions: Junio's two suggested improvements remain for future work: (1) updating cover letter patch listing format to use `log --oneline --reverse`, and (2) additional documentation consistency between `format-patch` and `send-email` man pages as noted by D. Ben Knoble. These are not blockers for current fix.

Participant positions: Original author Mirko Faina submitted all versions, with v4 being final approved iteration. Junio Hamano approved technical implementation after requesting commit message clarification about historical context. Patrick Steinhardt and Jeff King confirmed technical soundness in v2 reviews. D. Ben Knoble contributed documentation analysis. Jeff King (original `--from` author) provided historical context about feature usage patterns, noting most users generate cover letters via `format-patch` rather than `send-email`, explaining why the inconsistency persisted undetected for nearly a decade. Consensus remains that patch is ready to merge.

Related work: Touches email generation code shared with `git send-email`. Jeff King's historical context (commit a9080475) provides background. Documentation inconsistency spans multiple commands. Junio's cover letter format comments hint at future workflow improvements. Discussion reveals `--from` is likely a rarely-used feature, explaining long-standing oversight.