This is a minor follow-up in the string list sorting standardization effort. The patch itself is already merged - Junio notes this exact change was included in the 'next' integration branch days earlier. 

The change replaces two operations (`string_list_sort()` followed by `string_list_remove_duplicates()`) with the combined `string_list_sort_u()` in `sparse-checkout.c`, matching the pattern established earlier in the series. This is purely a code cleanup with no behavior change, as the new function provides identical sorted+deduplicated output.

The email serves as confirmation that this piece of the series is complete and integrated, while the thread continues with final polishing of the remaining help command refactoring patches. No further action is required on this particular change.