This is the fourth patch in a 10-part series introducing parallel hook execution, building on Adrian Ratiu's earlier work on configurable hooks. The patch adds parsing for a new `hook.jobs` configuration that will control how many hooks can run simultaneously.

The change introduces a global `hook.jobs` setting that defaults to 1 (serial execution) when unspecified. The value is parsed during configuration and stored in the hook cache, though it's not yet used in this patch. The implementation includes validation to ensure the value is a positive integer, with appropriate warnings for invalid inputs.

This is preparatory work for the parallel execution functionality coming in later patches. The series appears carefully structured, with this patch focusing narrowly on the configuration plumbing while maintaining backward compatibility. The documentation is minimal but sufficient for this incremental change, with a note that it will be expanded in future commits.

The patch follows the series' pattern of small, focused changes with clear commit messages. It builds on the refactoring from patch 3/10 while setting up for the parallel execution implementation to come. The careful attention to type safety and configuration precedence noted in the thread summary continues here.