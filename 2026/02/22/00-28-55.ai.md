This is a bugfix patch that addresses a memory leak in the repository initialization code. It's the first patch in a 10-part series introducing parallel hook execution, but this particular change is a standalone fix for an existing issue.

The patch fixes a memory leak in `repo_init()` that occurs when initializing a repository with v0 format but with v1 extensions enabled - an edge case that would only happen in development environments. The leak happens because `clear_repository_format()` wasn't being invoked in the error path. The fix is straightforward: adding the missing cleanup call before `repo_clear()` in the error handler.

While this is a small change, it's important infrastructure work for the larger parallel hooks series. The fix demonstrates good attention to memory management details that will be important when dealing with parallel execution contexts. The commit message clearly explains both the problem and solution, including why this wasn't caught before (it requires a specific development-time configuration to trigger).

This patch serves as groundwork for the more substantial changes that follow in the series, showing the author is being careful to fix underlying issues before building new functionality on top. The change is simple enough that it likely won't generate much discussion, but it's a good example of the kind of preparatory cleanup that makes larger features possible.