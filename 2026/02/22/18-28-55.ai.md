This is a test modernization patch in the "[PATCH 0/4] repo: enhance repository info and structure reporting" series. The patch makes the test suite more portable by:

1. Making the tests hash-agnostic by replacing hardcoded SHA-1 references with `test_oid` calls
2. Improving shell script portability by removing unnecessary quotes and using POSIX-compliant arithmetic comparisons
3. Replacing static object counts with dynamic calculations using new helper functions like `object_type_count` and `object_type_total_inflated_size`

The changes affect two test files (t1900-repo.sh and t1901-repo-structure.sh) that verify the new `git repo` command's functionality. The modifications ensure the tests will work correctly regardless of the repository's hash algorithm (SHA-1 or SHA-256) and across different shell implementations.

Key technical details:
- Introduces dynamic counting of references (branches, tags, remotes) using `git for-each-ref`
- Replaces hardcoded object counts with calls to new helper functions
- Maintains the same test coverage while making the tests more robust
- Part of preparing the test infrastructure for the upcoming `git repo` command features

The patch appears well-scoped and follows Git's test modernization patterns. It's a preparatory step for the main functionality patches that will follow in the series.