This is a post-merge follow-up discussion in a bugfix series that improves error handling for `.git` directory validation. The series has already gone through 9 iterations and was merged, with v10 incorporating all final fixes. Junio Hamano is now raising a subtle edge case about how to handle `ENOTDIR` errors (when a path component is not a directory) versus `ENOENT` (file not found) in the context of submodule validation.

The core issue is that when checking if a submodule's `.git` directory exists, the current code treats `ENOENT` as a benign "not found" case but doesn't yet handle `ENOTDIR` the same way. This could matter when a submodule directory is replaced with a regular file - the code would then get `ENOTDIR` when trying to check `sm1/.git` but currently doesn't have special handling for that case. Junio is questioning whether these two error conditions should be treated equivalently in the `read_gitfile_gently()` function.

This is a nuanced discussion about error classification in a security-sensitive area (validating Git repository locations). The thread shows the careful attention to detail that follows even after a patch series is merged, with the maintainer identifying a potential edge case that wasn't fully addressed in the original work. The discussion remains open-ended, with Junio noting he doesn't have a clear answer yet about whether `ENOTDIR` should always be treated like `ENOENT` in this context.