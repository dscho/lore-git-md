Bugfix for `git fsck` packfile verification, addressing an infinite loop during MRU list traversal.

Junio C Hamano acknowledges brian m. carlson's patch with a rare moment of levity ("this is a fun test"), indicating the patch will be queued. The fix prevents `git fsck` from entering an infinite loop when verifying multiple packfiles by adding a `skip_mru_updates` flag to avoid modifying the most-recently-used list during verification. 

The test case is minimal - just running `git fsck` - because the issue was discovered organically during development of the t1050-large.sh test file. While the test might seem trivial, it reliably reproduces the MRU list corruption that caused the infinite loop. 

This appears to be a straightforward, well-contained fix for a real issue, with the maintainer's quick approval suggesting it's uncontroversial. The patch touches core object database code but doesn't appear to conflict with ongoing ODB abstraction work.