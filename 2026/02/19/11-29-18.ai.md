This is a follow-up discussion in a bugfix thread about `git format-patch`'s `--from` option handling in cover letters. The thread has already reached consensus on the technical fix (now approved for merging in v4), and this email explores some historical context around the feature.

Jeff King (the original author of the `--from` feature in 2013) reflects on the surprising discovery that most users generate cover letters via `format-patch` rather than `send-email`, which explains why the inconsistency went unnoticed for so long. He speculates that the feature sees little use since most people don't need different sender identities between commits and emails, and expresses mild amusement at his own past design decisions.

The email doesn't propose any changes to the already-approved patch, but provides useful background about the feature's origins and usage patterns. It suggests the `--from` option may be a rarely-used corner case, which helps explain why the cover letter bug persisted for nearly a decade before being noticed and fixed. The discussion remains lighthearted, with Jeff taking a self-deprecating tone about his own memory of the feature's rationale.