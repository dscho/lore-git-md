This is a documentation patch that corrects the man page for `git-format-patch` to properly reference `format.noprefix` instead of `diff.noprefix`. The change uses AsciiDoc conditionals to show the correct config variable depending on whether the documentation is being generated for `git-format-patch` or other diff-related commands.

Junio Hamano has reviewed and accepted the patch, noting that while the AsciiDoc conditional syntax is new to Git's documentation, the change makes logical sense. The patch clarifies that `git-format-patch` only respects its own `format.noprefix` setting and not the `diff.noprefix` configuration, fixing an inconsistency where the documentation previously claimed otherwise.

This is the second patch in a series that also includes changes to make `format.noprefix` behave as a proper boolean value. The documentation update aligns with those behavioral changes while maintaining accuracy about which configuration options affect `git-format-patch`. The patch is straightforward and uncontroversial, with Junio indicating it will be included in the next integration cycle.