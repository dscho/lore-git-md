This is a follow-up discussion in a bugfix thread about `git format-patch`'s cover letter generation, specifically regarding the `--from` option handling. The thread has already reached consensus on the technical fix (now in its v4 iteration), and this email shifts focus to a related but separate issue about cover letter formatting.

Jeff King (Peff) responds to Junio Hamano's complaint about the current cover letter patch listing format, which uses `shortlog` and makes it hard to reference individual patches. Peff agrees the format is suboptimal and shares his personal workaround using `sed` to reformat the output, though he notes it doesn't handle header continuations well. He suggests that implementing a better format directly in `format-patch` would be straightforward since the code already has access to the complete patch list and subjects.

The discussion reveals this is a long-standing pain point (Peff has been using his workaround since 2009) but not one that has generated much user complaint. While this tangent isn't blocking the current `--from` fix, it surfaces a potential future improvement area for cover letter generation. The email provides useful context about real-world usage patterns and constraints that would inform any such change.