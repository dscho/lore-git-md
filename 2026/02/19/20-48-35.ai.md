This is a discussion about test philosophy in the context of `git cat-file`'s handling of whitespace in object names when using the `%(rest)` format specifier. The thread centers on whether a test marked with `test_expect_failure` should be modified to pass (by avoiding the problematic case) or kept as-is to document the known limitation.

Junio C Hamano responds to Victoria Dye's argument that the FIXME comment was meant to highlight a real limitation in `cat-file`'s behavior rather than being a test-specific issue. He questions whether the issue is fundamentally unfixable due to the inherent incompatibility between object names containing whitespace and the `%(rest)` format specifier's parsing behavior.

The technical heart of the matter involves how `git cat-file --batch-check` processes input lines when both an object name (which may contain spaces) and additional content (captured by `%(rest)`) are present. The test in `t1006-cat-file.sh` currently demonstrates this edge case by conditionally marking the test as expected to fail when whitespace is present in the object name.

Junio's position appears to be that this may not be a fixable limitation of `%(rest)`, implying the test might be better framed as documenting expected behavior rather than marking it as a failure case. This moves the discussion from a simple test fix to a deeper consideration of how Git should handle this edge case in its command-line interface.