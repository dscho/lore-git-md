This is the final patch in Patrick Steinhardt's CI/build system series improving test infrastructure reliability across GitLab and GitHub CI, now picked up by Junio for integration. The patch completes the standardization between CI systems by adding failure handling for MSVC+Meson jobs in GitLab CI that was already present in GitHub CI.

The patch adds three key pieces to GitLab's MSVC+Meson test job: test output printing via `ci/print-test-failures.sh`, artifact collection in `t/failed-test-artifacts`, and proper `after_script` handling - all matching GitHub CI's existing behavior. It also establishes a consistent `TEST_OUTPUT_DIRECTORY` (C:/Git-Test) for test artifacts. The changes ensure both CI systems now handle test failures identically when running MSVC+Meson builds, using Git for Windows' bash for the failure reporting scripts.

This concludes the series that emerged from debugging MSVC+Windows CI failures, with all identified issues addressed including Johannes Schindelin's Windows+VS test-slicing fix in patch 5/7. The work provides full test infrastructure parity between GitLab and GitHub CI for MSVC+Meson builds, with identical test execution paths via the shared `ci/run-test-slice-meson.sh` script. Junio has picked up the complete series for integration, marking this as the final piece in making Git's test infrastructure robust across platforms.