This is a documentation discussion about clarifying the behavior of `git fetch --jobs=0`. The original patch added a note that a value of 0 uses "some reasonable default", matching the existing documentation for `fetch.parallel=0`. Patrick Steinhardt suggests improving both documentation sites to explicitly state that the default is based on `online_cpus()` (the number of available CPU cores), which is the actual implementation behavior.

The discussion is straightforward and non-controversial, focusing on making the documentation more precise about an existing implementation detail. Patrick's suggestion would require updating two documentation locations to explicitly mention the CPU count fallback rather than the vague "reasonable default" phrasing. This would help users better understand the parallel fetch behavior when they specify 0 jobs.

The thread shows a typical pattern where an initial documentation improvement prompts a suggestion for making it even more precise by surfacing implementation details that are already in the code but not clearly documented. The change would affect `Documentation/fetch-options.adoc` and likely the config documentation for `fetch.parallel`.