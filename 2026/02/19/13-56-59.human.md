This is a build system patch addressing dependency tracking for the generated `config-list.h` header. Patrick Steinhardt is reviewing D. Ben Knoble's v5 patch, which ensures the header gets rebuilt when documentation files defining config options change.

The solution now handles both Makefile and Meson builds consistently, with proper path escaping and a single-sed approach that addresses earlier Windows performance concerns. Patrick notes the conditional check for `$DEPFILE` could potentially be dropped since both build systems now pass it, but considers this a minor point. He approves the overall approach, particularly the optimized sed usage that reduces process spawning.

The patch appears technically sound, with testing already confirmed across multiple platforms (GitHub CI, GitLab CI, local builds) by both Patrick and Phillip Wood. While there was some earlier discussion about stylistic choices like printf vs echo, those points are noted but not blocking. This looks ready for merging as a robust solution to the dependency-tracking problem.