This is a test fix addressing an edge case in `git cat-file`'s `--batch-check` format handling. The patch corrects a test that was marked as `test_expect_failure` due to ambiguity when parsing object names containing whitespace (like "HEAD:path with spaces") alongside the `%(rest)` placeholder.

The fix changes the test to use the resolved object ID (OID) rather than the raw object name as input, since OIDs are guaranteed not to contain whitespace. This makes the parsing unambiguous and allows the test to pass reliably. The patch removes the conditional `test_expect_failure` wrapper and associated FIXME comment that previously acknowledged the limitation.

The change is straightforward and well-motivated, touching only test code in `t1006-cat-file.sh`. It demonstrates good awareness of Git's object naming conventions and the test suite's idioms. The commit message clearly explains both the problem and solution, making this a clean, focused improvement to the test suite.