This is a technical discussion about improving robustness in Git's commit parsing, specifically around handling commit objects that come from commit-graphs. The conversation follows up on Patrick Steinhardt's patch to make `repo_parse_commit_no_graph()` more robust by unparsing commits first when they're detected as coming from a graph.

Justin Tobler raises a thoughtful question about whether this fix belongs in `repo_get_commit_tree()` instead, suggesting it might be more conceptually correct to have that function handle missing tree information. Patrick responds with a detailed analysis of the complexities involved, including concerns about partial clones and object existence. He provides a prototype implementation that moves the tree parsing logic into `repo_get_commit_tree()`, but notes it fails some tests related to partial clones.

The discussion highlights the subtle tradeoffs in Git's object parsing layer - between robustness, performance, and handling edge cases like partial clones. Patrick's response shows careful consideration of the architectural implications while acknowledging the practical challenges of making such changes. This is part of Patrick's ongoing series optimizing shallow push performance, where these low-level parsing details directly impact real-world performance.