Build system fix for config-list.h dependency tracking. This patch ensures the generated `config-list.h` header stays in sync with Git's documentation files that define configuration options, addressing cases where documentation changes weren't properly triggering rebuilds.

Phillip Wood confirms successful testing of the Meson build portion, verifying that the solution correctly handles path quoting and rebuilds when documentation files are modified. The patch now covers both Meson and Makefile builds with identical reliability, using a "script knows its dependencies" approach where `generate-configlist.sh` outputs a depfile listing all input files.

Key improvements include robust path escaping (handling spaces, octothorpes), optimized single-sed command processing, and explicit dependency declarations. While some stylistic questions remain open (printf vs echo), the core functionality is complete and validated across platforms. This prevents stale builds from documentation changes while maintaining portability.