This is a follow-up discussion in the `git format-patch` cover letter thread, focusing on documentation consistency between `format-patch` and `send-email` regarding cover letter generation.

D. Ben Knoble points out that the `send-email` man page's main example for cover letters actually shows using `format-patch` to generate them, contrary to Jeff King's assumption that `send-email` had its own cover letter generation code. This reveals a documentation inconsistency that wasn't previously noticed. Jeff King acknowledges he may have been mistaken about `send-email`'s capabilities and speculates that the `--from` option inconsistency went unnoticed for so long because most users don't change the sender identity when generating cover letters.

The exchange highlights two key points: first, that the documentation could be clearer about the relationship between `format-patch` and `send-email` for cover letter generation, and second, that the `--from` option is likely a rarely-used feature. This discussion doesn't block the current bugfix (which has already been approved) but identifies an area for potential future documentation improvements. The tone is lighthearted, with both participants acknowledging gaps in their understanding of how these features are typically used in practice.