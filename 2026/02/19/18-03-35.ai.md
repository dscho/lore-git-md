This is a review response from Junio C Hamano on Kristoffer Haugsbakk's patch to make `format.noprefix` behave as a proper boolean. Junio agrees with the change but suggests improving the error message wording to be more direct about the behavior change - clarifying that the config previously accepted any value as "true" but now requires proper boolean values. He also notes the message should use `advise()` rather than `advise_if_enabled()` since this is a configuration correction that should always be shown.

The thread context shows this is part of a series to align `format.noprefix` with `diff.noprefix`'s boolean behavior, fixing an inconsistency where the format-patch version previously accepted any value. Junio's review focuses on the user experience when existing configurations hit the new validation, wanting to ensure the error message clearly guides users to update their config to proper boolean values rather than just noting the syntax change. The technical implementation itself (using `git_parse_maybe_bool()`) is not questioned, only the messaging around it.