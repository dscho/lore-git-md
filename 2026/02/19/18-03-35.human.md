This is a review response from Junio C Hamano on Kristoffer Haugsbakk's patch to make `format.noprefix` behave as a proper boolean. The patch is part of a series that aligns `format.noprefix` with `diff.noprefix`'s behavior and fixes related inconsistencies in the man pages.

Junio generally agrees with the change but suggests improving the error message wording to be more direct about the behavior change - clarifying that the config previously accepted any value as "true" but now requires proper boolean values. He also notes the error hint should use `advise()` rather than `advise_if_enabled()` since this is a configuration correction case where the message should always appear.

The review maintains the technical correctness of the patch while focusing on improving the user experience for those affected by the breaking change. Junio's perspective as maintainer helps ensure the transition is as smooth as possible for users with existing non-boolean configurations. The response indicates the patch is on track for acceptance with these minor messaging improvements.