This is a discussion about test philosophy in the `git cat-file` test suite, specifically around handling object names with whitespace when using the `%(rest)` format specifier. The thread has revealed that the original FIXME comment in the test was meant to document a limitation in `git cat-file` itself, not just a test issue.

Victoria Dye argues that the test should remain as-is to keep visibility on the `cat-file` behavior quirk, where `%(rest)` parsing fails when object names contain whitespace. Junio C Hamano questions whether this is even fixable in `cat-file`, suggesting the fundamental design of `%(rest)` may make it incompatible with whitespace-containing names.

The exchange highlights a tension between test reliability (wanting tests to pass) and test documentation (wanting tests to surface known edge cases). The discussion is focused on a single test in `t1006-cat-file.sh` that was originally added in commit 9fd38038 to test generic object specifiers.

This appears to be a low-stakes discussion about test maintenance philosophy rather than a pressing bug or feature. The technical issue at hand is narrow (whitespace handling in one test case), but the underlying question about whether tests should work around known limitations or document them is more broadly applicable to test writing in general.