Documentation patch discussing the `--jobs=0` behavior in `git fetch`. Patrick Steinhardt responds to Daniel D. Beck's patch that documented the existing behavior where a value of 0 uses a "reasonable default" (matching the behavior of `fetch.parallel=0`). 

Patrick suggests improving the documentation by being more specific about what "reasonable default" means - pointing out that the code actually uses `online_cpus()`. He proposes updating both the command-line option documentation and the `fetch.parallel` config documentation to reflect this implementation detail. 

This is a constructive follow-up to a straightforward documentation improvement, suggesting we make the docs more precise by exposing the actual behavior rather than keeping it vague. The discussion remains focused on documentation clarity rather than proposing any behavior changes.