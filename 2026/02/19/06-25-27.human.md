CI / build system patch from Patrick Steinhardt improving test-slicing reliability in the CI pipeline. This is part of a 7-patch series (v2) focused on meson build improvements, though the cover letter wasn't provided in this digest.

The patch fixes a subtle issue in the test-slicing helper script where error codes from the test-tool were being lost due to pipeline execution. The change stores the test list in a variable first before processing, making the failure detection more robust. While small, this is the kind of precise fix that prevents hard-to-diagnose CI failures down the line.

The change is straightforward - just splitting a pipeline into two steps - but shows attention to detail in the CI infrastructure. Given Patrick's track record with build system improvements and the clear explanation, this looks like a solid incremental improvement.