This is a CI/build system patch from Patrick Steinhardt, part of a series improving Meson test execution in GitHub workflows. The patch fixes an issue where Meson tests weren't actually running on Windows because the CI job was incorrectly configured to use PowerShell instead of Bash to execute the test script.

The change is straightforward - it switches the shell from PowerShell to Bash for the test execution step. While simple, the patch addresses a real issue where tests were silently skipped rather than failing, which could have allowed regressions to go unnoticed. The fix aligns this step with how other shell script executions are handled in the CI workflow.

This appears to be a well-motivated, targeted fix from an experienced contributor who has been actively working on CI and test infrastructure improvements. The patch is part of a larger v2 series (position 4/7) about Meson testing improvements, though the summary stands alone well without needing the broader context.