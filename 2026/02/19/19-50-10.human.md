Test fix addressing edge case in `git cat-file --batch-check` format parsing. The patch from Deveshi Dwivedi resolves an ambiguity when object names contain whitespace by using resolved OIDs (which never have spaces) rather than raw names as input. This removes a FIXME comment and converts a `test_expect_failure` case to `test_expect_success`.

Junio C Hamano questions whether this fully addresses the original intent of the test from commit 9fd38038, suggesting an alternative approach that would still test raw names when possible while falling back to OIDs only for problematic cases. The discussion highlights Git's careful approach to test coverage - the patch makes the test reliable but may reduce the scope of what's being tested.

This is a focused test improvement that touches only `t1006-cat-file.sh`. The change appears technically sound but may need refinement to better preserve the original testing intent. Junio's response suggests the author should revisit whether the solution fully meets the test's goals.