Test modernization patch updating t2004 to use test helper functions.

Lambert Duclos-de Guise sends a straightforward test cleanup patch that replaces direct `test -f` calls with the `test_path_is_file` helper function in t2004-checkout-cache-temp.sh. This is part of the ongoing effort to modernize Git's test suite by using more descriptive helper functions that provide better error messages when tests fail.

The patch makes 21 mechanical substitutions across the test file, consistently replacing each `test -f` check with `test_path_is_file`. The changes are purely about test infrastructure - the actual test logic and assertions remain unchanged. This appears to be a clean, focused contribution that follows the project's test modernization patterns.

The commit message notes this was done as part of Google Summer of Code (GSoC), suggesting the author may be a new contributor working under mentorship. The patch looks correct and uncontroversial - the kind of incremental improvement that typically gets merged quickly after minimal review.