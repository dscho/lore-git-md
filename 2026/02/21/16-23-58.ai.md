This is a follow-up patch in the series standardizing string list sorting behavior across Git commands. The patch updates `sparse-checkout.c` to use the unified `string_list_sort_u()` function instead of separate sort-and-deduplicate calls.

The change is mechanical and straightforward - it replaces the existing `string_list_sort()` + `string_list_remove_duplicates()` sequence with a single call to `string_list_sort_u()` in the sparse_checkout_list() function. This matches the behavior established in the previous patch for the help.c changes.

The patch has already been through review in previous iterations and was acked by Junio as a straightforward refactoring. It's part of the broader effort to ensure consistent sorted+deduplicated string list behavior matching `sort -u` across Git commands. The implementation here affects the output of `git sparse-checkout list` by ensuring patterns are consistently sorted and deduplicated.

This version appears ready for merging, with no outstanding review comments or concerns noted in the thread. The change is small, focused, and well-contained to the sparse-checkout command's pattern listing functionality.