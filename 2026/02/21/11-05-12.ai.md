This is a refactoring patch that extracts a `skip_slashes()` helper function in `path.c`'s path normalization logic. The change comes after review feedback pared down an originally more ambitious refactoring to focus on this clear readability improvement.

The patch factors out repeated slash-skipping logic from `normalize_path_copy_len()` into a new `skip_slashes()` helper, addressing one of the three refactoring opportunities identified in earlier versions. As requested in review, it keeps the dot-component handling inline rather than extracting it, maintaining the original control flow. The implementation preserves all existing behavior, verified by the `t0060-path-utils.sh` test suite.

This v3 iteration responds to Junio C Hamano's request to clarify the commit message - it now accurately describes only the actually implemented `skip_slashes()` extraction rather than the broader original proposal. The code changes remain identical to v2, just with better documentation. The patch appears ready for merging with this final adjustment, as all technical concerns were resolved in previous rounds.

Key technical details for future reference: the new `skip_slashes()` helper encapsulates the `while (is_dir_sep(*p)) p++` pattern used in three places within `normalize_path_copy_len()`. The helper is static and only used within this file. The patch touches only `path.c`, modifying 19 lines (13 insertions, 6 deletions) while maintaining identical behavior. The change is purely structural with no functional impact, as confirmed by existing tests.