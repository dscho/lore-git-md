This is a documentation patch clarifying the error handling in Git's HTTP protocol when a client fails to provide any "want" lines during fetch negotiation.

The patch updates `gitprotocol-http.adoc` to specify that when no "want" objects are received, the server should treat this as an invalid request and terminate the negotiation. It provides the exact error message ("ERR no wants received") that servers should return in this case, along with a brief explanation that this indicates the client failed to specify any objects to fetch.

The change is straightforward and fills in a previously marked "TODO" in the protocol documentation. The patch appears to be a simple but useful clarification of expected behavior, though it might benefit from cross-referencing any related code that implements this error case (if such code exists). The commit message could be more detailed about why this clarification is needed now, but the technical content seems correct.