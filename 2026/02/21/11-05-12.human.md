Refactoring patch extracting path normalization helper. This is the third iteration of Pushkar Singh's change to factor out a `skip_slashes()` helper from `normalize_path_copy_len()` in `path.c`. The patch has been through review with Junio C Hamano, who approved the core extraction but requested a more focused commit message in v2. 

The change itself is straightforward - it moves repeated `is_dir_sep()` loop logic into a new helper function while maintaining identical behavior, verified by the existing `t0060-path-utils.sh` tests. The v3 revision tightens the commit message to accurately reflect the now-minimal scope (just the slashes extraction, not broader path normalization restructuring that was in earlier versions).

This appears ready for merging - the code is clean, the tests pass, and the commit message now properly describes what's actually changed. The thread shows a good example of review-driven refinement, with the author responding to maintainer feedback by simplifying the change to its most valuable component.