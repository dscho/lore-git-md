Refactoring patch finalizing string list sorting behavior in Git's help command output. This is the second patch in a v3 series (following v2 which was already accepted) that standardizes on `string_list_sort_u()` for consistent sorted+deduplicated output across Git commands.

The patch splits the config display logic in `builtin/help.c` into three separate functions (`show_config_human`, `show_config_sections`, `show_config_vars`), each handling a different output format. The key change is replacing manual sorting+deduplication with direct use of `string_list_sort_u()`, which combines both operations. This matches the behavior established in the first patch of the series for sparse-checkout output.

The refactoring improves code organization by separating the human-readable output case from machine-readable cases (sections and vars), making the logic clearer. The changes are mechanical but thorough, touching 134 lines with 90 insertions and 44 deletions. The patch has already been acked in previous iterations and is now receiving final polishing on test script formatting before merge.

This completes the series' goal of standardizing string list handling, with Junio having noted an architectural opportunity for further help.c refactoring (CONFIG_HUMAN case handling) that Amisha Chhajed has committed to address separately. No technical concerns remain - this is final cleanup before merge.