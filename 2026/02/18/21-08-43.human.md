This is a documentation patch in an 8-part series focused on naming consistency and code organization in the `repo-info` subsystem. Patch 7 updates the `git-repo.adoc` man page to use `_NUL_` instead of `NUL` when referring to the null character, matching the documentation convention used elsewhere in Git's docs.

The change is purely mechanical, replacing two instances where the null character was previously written without underscores. This follows the project's documentation style where special characters are typically wrapped in underscores (like `_GIT_DIR_` or `_PATH_`). The patch maintains identical technical meaning while improving consistency with other man pages.

This continues the series' pattern of small, focused mechanical improvements - in this case extending the naming consistency work to documentation formatting. The change is well-contained and clearly motivated by prior documentation conventions.