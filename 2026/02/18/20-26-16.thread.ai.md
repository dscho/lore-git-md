Thread summary:

This thread addresses the behavior and documentation of `format.noprefix` configuration. The series from Kristoffer Haugsbakk makes `format.noprefix` behave as a proper boolean (rejecting non-boolean values), aligning it with `diff.noprefix`'s behavior. This is a breaking change but follows precedent from Jeff King's prior work. The implementation changes `builtin/log.c` to properly validate the config value and adds tests in `t/t4014-format-patch.sh`. Documentation in `diff-options.adoc` is updated to correctly reference `format.noprefix` for `git-format-patch` instead of `diff.noprefix`. The changes are focused on consistency and correctness, with clear error messaging for invalid values. The thread currently consists of this initial patch series with no replies yet. Key technical details: the config option will now error on non-boolean values (like `true`, `false`, `1`, `0`), and the documentation now properly separates the format-patch specific option from the diff option. The breaking nature is intentional and follows established patterns for config validation.