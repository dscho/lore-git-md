This is a documentation patch that fills a small but noticeable gap in the `git fetch` man page. The patch adds a clarification that `--jobs=0` will use a reasonable default value, matching the behavior already documented for the equivalent `fetch.parallel=0` config setting.

The change is straightforward and uncontroversial - it simply brings symmetry between the command-line option and config setting documentation. The commit message references the 2023 change (c39952b92) that implemented this behavior but neglected to document it. The patch itself is clean and focused, adding just two lines to the man page.

This is the kind of minor but useful documentation improvement that often gets picked up quickly during review. The author (Daniel D. Beck) appears to be making their first contribution to Git based on the GitGitGadget submission, and they've chosen a good starter patch - small in scope but addressing a real documentation gap.