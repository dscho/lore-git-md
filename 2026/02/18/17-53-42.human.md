This is the third and final part of Shreyansh Paliwal's series to eliminate `the_repository` and `the_hash_algo` globals from `wt-status.c`, part of Git's ongoing effort to remove global state. The patch completes the conversion by replacing the remaining uses of `the_hash_algo` with the hash algorithm from the local repository (`r->hash_algo`).

The changes are mechanical but important for consistency - three locations that previously used the global hash algorithm now use the repository-specific one:
1. Two calls to `wt_status_get_state()` for cherry-pick and revert operations
2. One call in `has_uncommitted_changes()` when checking against the empty tree

With this patch, `wt-status.c` no longer depends on either `the_repository` or `the_hash_algo`, completing the subsystem's transition away from global state. The changes follow the established pattern seen in parts 1 and 2 of the series, and the small, focused diff suggests this is a straightforward completion of work already reviewed in earlier iterations.

The patch appears ready for merging, assuming parts 1 and 2 have been accepted (as indicated by the thread summary). It represents another incremental step in the larger `the_repository` removal effort that has been progressing steadily across the codebase.