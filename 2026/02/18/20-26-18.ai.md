This is a documentation patch that corrects the man page for `git-format-patch` to properly reference `format.noprefix` instead of `diff.noprefix`. The change is part of a two-patch series that makes `format.noprefix` behave as a proper boolean (rejecting non-boolean values) and updates the documentation to reflect the actual behavior.

The patch fixes an inconsistency where the documentation incorrectly claimed that `--default-prefix` overrides `diff.noprefix` for `git-format-patch`, when in fact the command only respects `format.noprefix`. The change uses AsciiDoc conditionals to show the correct config variable depending on whether the documentation is being built for `git-format-patch` or other commands.

This is a straightforward documentation correction that aligns the docs with the actual implementation. The change is small and focused, with a clear explanation in the commit message about why the correction is needed (the original documentation referenced a config that `git-format-patch` ignores). The patch maintains the existing behavior while making it clearer to users which configuration actually affects the command.