This is a security-focused response from Junio C Hamano in the ongoing discussion about push certificate privacy and the "pushee" header. Junio addresses Lorenz Leutgeb's proposal to allow controlled URL substitution in push certificates by drawing a parallel with existing behavior in Git's pull operations.

Junio notes that Git already handles similar URL discrepancies in other contexts - for example, `git pull` records the repository URL from the user's perspective rather than a canonical network identifier. He provides a concrete example from his own workflow where he pushes to `git@github.com:gitster/git.git` even though the public repository is known as `https://github.com/gitster/git`.

The response suggests that while the privacy concern is valid, the current behavior has precedent in other parts of the system. Junio's example demonstrates that push certificates recording local/private URLs rather than public identifiers is consistent with how Git handles repository identities elsewhere. This positions the discussion as less about introducing new behavior and more about whether existing patterns are acceptable for push certificates specifically.

The email doesn't reject Lorenz's proposal outright but provides context that may influence how the security/privacy tradeoffs are evaluated. It keeps the thread focused on real-world usage patterns rather than abstract security principles.