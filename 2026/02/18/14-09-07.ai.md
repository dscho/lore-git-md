This appears to be a refactoring patch that's part of a larger series, though it seems unrelated to the strbuf handling thread mentioned in the context. The patch introduces a new helper function `show_http_message_fatal()` in remote-curl.c that combines error message handling with process termination.

The change consolidates several error handling patterns where HTTP error messages were being shown followed by `die()` calls. The new helper:
1. Takes the same parameters as `show_http_message()` plus a format string for the error message
2. Handles displaying the HTTP error text if present
3. Always terminates with `die()` using the provided error message
4. Marks itself as NORETURN to help compiler analysis

The patch updates four call sites in the discovery code path to use this new helper, reducing duplicate error handling code. The commit message suggests this refactoring will also be useful for upcoming HTTP 429 rate limiting support.

The change is straightforward and appears to be a pure refactoring - it doesn't modify behavior, just consolidates common patterns. The patch is cleanly separated from the strbuf series mentioned in the thread context, suggesting it may have been misclassified in the context provided.