This is the final patch in a series addressing test failures on Windows/MSVC platforms when Git is built with ICONV support but the `iconv(1)` executable is unavailable. The series has gone through three iterations and now has Christian Couder's approval, marking it ready for merging.

The thread shows consensus that the approach is sound: properly distinguishing between libiconv (linked into Git) and the `iconv(1)` executable (used in test scripts), with conditional test execution based on ICONV prerequisites. The final version splits out prerequisite changes into their own commit for better organization, as Christian suggested.

Patrick Steinhardt confirms the series resolves the MSVC CI failures on GitLab CI. The changeset demonstrates careful attention to test portability while maintaining coverage, particularly important for Windows environments where MinGW no longer ships the `iconv(1)` executable. With all feedback incorporated and no outstanding objections, this thread appears concluded and the series awaits maintainer pickup.