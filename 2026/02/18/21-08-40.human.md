This is part 4 of an 8-patch refactoring series focused on naming consistency in the `repo-info` subsystem. The patch renames the generic `struct field` to the more specific `struct repo_info_field` throughout `builtin/repo.c`, making it clearer that this is an internal data type specific to the `git-repo-info` command.

The changes are purely mechanical - updating the struct name, its uses in array declarations, comparison functions, and all access points. This continues the series' pattern of establishing and applying consistent naming conventions, following earlier patches that introduced singular naming for arrays and standardized function names.

The patch is small (27 lines changed) and well-contained, touching only `builtin/repo.c`. Like previous patches in the series, it makes no behavior changes - just improving code clarity through more precise naming. The commit message references the broader context of the `git-repo-info` work, showing this fits into a larger effort to organize and document this subsystem.