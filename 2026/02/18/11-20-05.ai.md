Security patch series addressing repository discovery hardening, now in this email on a specific edge case in error handling. This is part of the ongoing effort to secure Git against malicious `.git` entries while maintaining legitimate symlink support.

The author (Tian Yuchen) acknowledges a regression in error handling behavior introduced in v5 of the patch series. When `die_on_error` is false, encountering `READ_GITFILE_ERR_INVALID_FORMAT` currently causes the program to die, whereas the previous behavior was to return `GIT_DIR_INVALID_GITFILE` and continue repository discovery. This affects the non-fatal error path in `setup_git_directory_gently_1()`.

Key technical points:
- The regression stems from over-aggressive delegation to `read_gitfile_error_die()` without considering the `die_on_error` flag
- For v6, the author plans to modify the logic to only die in three specific cases: happy paths to ignore, security-critical failures, and when explicitly requested via `die_on_error`
- All other error cases will maintain backward-compatible behavior by returning error codes rather than dying
- This addresses Karthik Nayak's concern about preserving the expected control flow in non-fatal error scenarios

The thread shows careful attention to edge cases in the security hardening effort, with the author promptly acknowledging and fixing an oversight in the error handling logic. This level of responsiveness to review feedback suggests the series is nearing readiness for merging, with only this final behavioral adjustment needed.