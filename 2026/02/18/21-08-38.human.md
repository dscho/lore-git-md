This is a refactoring patch that's part of an 8-patch series focused on naming consistency and code organization in the `git-repo-info` and `git-repo-structure` code. 

The patch renames `repo_info_fields` to `repo_info_field` in `builtin/repo.c`, following the new CodingGuidelines rule established in patch 1/8 that array variables should use singular names. This makes element access (like `repo_info_field[0]`) read more naturally. The change is purely mechanical - all references to the array and its associated comparison function are updated consistently.

As part of a larger series that's already been through some review, this patch appears straightforward and well-motivated. The changes are limited to variable names with no behavior modifications, making it a low-risk refactoring. The patch follows the established pattern from earlier work and maintains the existing code organization while improving naming consistency.