This is a refactoring and documentation patch series that follows up on the recent `git-repo-info` and `git-repo-structure` work. The 8-patch series makes various small improvements to naming conventions, test organization, and documentation formatting.

The changes are mostly mechanical cleanups based on prior feedback:
1. Adds a CodingGuidelines rule about using singular names for arrays
2. Renames variables and structs to be more consistent with the new `repo_info_field` naming pattern
3. Simplifies some function logic in `repo.c` to be more consistent between similar functions
4. Updates test names and output matching to follow new conventions
5. Fixes documentation formatting (replacing `NUL` with `_NUL_` and capitalizing paragraphs)

The patches appear to be straightforward follow-ups to earlier work, with each change clearly motivated by specific feedback (referenced via message IDs). The series touches documentation, core code, and tests, but doesn't introduce any behavior changes - it's purely about naming consistency and code organization.

This looks like a well-structured cleanup series that incorporates prior review feedback. The changes are small and focused, with each patch doing one clear thing. The references to specific prior discussions suggest the author has been engaged with reviewer feedback.