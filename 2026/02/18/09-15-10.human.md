This is part of the ongoing `the_repository` removal effort, specifically targeting the merge-ort and replay subsystems. Elijah Newren sends a 5-patch series that eliminates direct uses of the global `the_repository` and `the_hash_algo` variables from these components.

The first three patches perform mechanical conversions - passing repository pointers explicitly to functions like `write_tree()` and replacing global variable accesses with references through the existing `opt->repo` structure. The last two patches add preventative measures: assertions that will catch if these globals accidentally creep back into the code.

The changes appear straightforward and well-contained, focusing only on the stated goal without introducing broader refactoring. The commit messages reference prior discussions about removing these globals, showing this work aligns with established project direction. As merge-ort is Git's default merge strategy and replay is a newer command, keeping them clean of global state is valuable for future maintenance.