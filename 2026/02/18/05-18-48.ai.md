Security patch series finalizing hardening of Git's repository discovery, now in v5 with restructured commits. Tian Yuchen has split the changes into two focused patches per Junio Hamano's request: first refactoring error handling to properly distinguish ENOENT from other stat() failures, then implementing the security checks that reject dangerous `.git` entries (FIFOs/sockets) while allowing valid symlinks. The test script has been renamed to `t0009-git-dir-validation.sh` and integrated into the meson build system as the final build requirement.

The core security model remains unchanged from previous versions - Git now properly validates `.git` entries during repository discovery to prevent attaching to maliciously crafted parent repositories through unsafe filesystem objects. The implementation in `setup.c` uses stat() instead of lstat() to support legitimate symlinks while explicitly rejecting FIFOs and sockets. Error handling has been refined with clear distinctions between fatal errors (invalid paths) and non-fatal cases (missing paths where Git should continue searching).

This version addresses all outstanding feedback from Junio Hamano and Karthik Nayak, with the authoritative error handling approach now implemented and tested. The series appears ready for merging with no remaining technical objections - the changes are well-contained to `setup.c` and `setup.h` with comprehensive test coverage in the new t0009 script. Tian Yuchen's responsiveness to review feedback and careful restructuring suggests this security hardening is now in its final form.