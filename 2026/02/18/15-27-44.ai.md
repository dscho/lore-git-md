This is a review comment in the final polishing phase of the ref backend selection series, focusing on stub management for alternate reference storage locations. Toon Claes suggests improving the clarity of a comment in the refs.c code that handles stub creation/removal for non-files backends.

The comment in question explains the stub management logic when using alternate reference backends (like reftable). Toon finds the current wording confusing because it states "require stubs" while actually doing an early return when stubs aren't needed. He proposes alternative wording that more clearly indicates stubs are only created in the main gitdir, not in alternate locations.

This is a minor documentation refinement in a series that has already received maintainer approval from Junio Hamano. The core technical aspects - including the environment variable support, URI parsing, and stub management architecture - are already settled. The discussion now focuses on making the code's intent as clear as possible through comments.

The series implements flexible reference storage location selection through multiple mechanisms (config, URI, and now environment variable) primarily to support GitLab's migration needs between files and reftable backends. This particular patch adds `GIT_REFERENCE_BACKEND` environment variable support with the same URI format as the config option, enabling runtime backend selection for individual commands.