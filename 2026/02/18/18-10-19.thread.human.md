This thread introduces a test modernization patch that simplifies several test scripts by using `test_seq -f` with direct piping instead of temporary files. The changes were prompted by feedback from Jeff King (Peff) and Junio Hamano in prior discussions.

The patch updates six test scripts across various areas (ref handling, reftable, archive corner cases, and hooks) to eliminate temporary file usage where `test_seq -f` can directly pipe to Git commands like `update-ref` and `mktree`. These are mechanical improvements that follow established test patterns, making the scripts cleaner without altering Git's core behavior. The changes appear well-scoped and focused on test infrastructure modernization.