Kristoffer Haugsbakk's series standardizes the `format.noprefix` configuration to behave as a proper boolean value, rejecting non-boolean inputs with clear error messages. The change aligns the implementation with the long-standing documentation, which always described it as boolean. While technically a behavior change, Jeff King (Peff) considers this effectively a bug fix, noting that only users with unreasonable configurations would be affected. 

Junio C Hamano has reviewed and approved both the behavioral change and accompanying documentation updates. The documentation patches correct references to `diff.noprefix` in the `git-format-patch` man page (which the command never actually respected) and clarify the interaction between `format.noprefix` and `--default-prefix`. 

The thread shows consensus that this is a worthwhile cleanup, with Peff offering a slightly more aggressive stance on treating it as a pure bug fix without transitional messaging. With all feedback incorporated and Junio having queued the patches, this appears ready for merging.