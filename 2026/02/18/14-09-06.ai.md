This is the third patch in a series fixing strbuf buffer handling issues, specifically targeting the `strbuf_attach()` function. The patch converts what was previously an implicit reallocation (via `strbuf_grow()`) into an explicit contract violation check with `BUG()`.

The change enforces the documented requirement that the `alloc` parameter must be larger than `len` to accommodate the NUL terminator. Where the old code would silently reallocate if this wasn't true, the new version will trigger an immediate failure via `BUG()`. The patch also moves the NUL termination directly into `strbuf_attach()` rather than relying on `strbuf_grow()` to handle it.

This continues the series' theme of tightening invariants around string buffer handling. The change is mechanical but important for catching programming errors early. The patch is small (3 lines changed) and focused, touching only `strbuf.c`. It follows the pattern established in the first two patches of the series, which addressed similar issues with buffer sizing in `strbuf_reencode()` and callers of `strbuf_attach()`.

No reviews or objections have been raised yet in the thread, and the systematic approach suggests the author has identified a consistent pattern of issues to address. The remaining two patches in the series will presumably continue this cleanup work.