[PATCH 0/5] Remove `the_repository` from merge-ort and replay
Author: Elijah Newren
Thread type: Patch series (v3)
Subsystems: merge-ort, replay, promisor-remote, diff
Related effort: `the_repository` removal (ongoing project)

This series systematically eliminates direct uses of `the_repository` global variable and `the_hash_algo` from merge-ort.c and replay.c, replacing them with repository-specific parameters via `opt->repo`. The v3 iteration addresses the final issue identified in v2 review by Patrick Steinhardt - the prefetch logic now properly uses `opt->repo` with `repo_has_promisor_remote()` rather than falling back to `the_repository`, completing repository context propagation.

Patch 1 removes obsolete `the_repository` checks from `prefetch_for_content_merges()` in merge-ort.c and corresponding logic in diff.c, diffcore-break.c, and diffcore-rename.c. The v3 version fixes the repository context issue by properly propagating `options->repo` to `repo_has_promisor_remote()`, addressing Patrick Steinhardt's v2 feedback. This ensures consistent repository handling throughout the prefetch logic.

Patches 2-5 remain technically unchanged from previous versions but now represent a complete solution, converting repository access throughout merge-ort.c to use `opt->repo` instead of `the_repository` for tree parsing, blob operations, merge base calculations, configuration handling, and hash algorithm access. These changes have been reviewed positively by Patrick Steinhardt as "welcome cleanup" with no substantive concerns.

Patch 6 introduces compile-time enforcement against `the_repository` usage in merge-ort.c and replay.c via `DO_NOT_USE_THE_REPOSITORY` macro, with all temporary exceptions now removed except for `DEFAULT_ABBREV` which still needs `USE_THE_REPOSITORY_VARIABLE`. The enforcement mechanism follows established patterns from other subsystems.

The series demonstrates:
1. Full resolution of Patrick Steinhardt's prefetch logic concern
2. Maintained tight focus on merge-ort and replay subsystems
3. Elijah Newren's merge-ort expertise ensuring technically sound changes
4. Readiness for merging as part of broader `the_repository` removal project

With all identified issues addressed and positive review feedback incorporated, this series appears complete and well-positioned for integration. The compile-time enforcement in the final patches provides future protection against regression in these subsystems. The interaction exemplifies the careful attention to repository context propagation required in these mechanical conversions.