Thread identity: Test modernization patch simplifying test scripts by using `test_seq -f` with pipes instead of temporary files.

Core problem or goal: Clean up test scripts by eliminating unnecessary temporary file handling when generating sequences for Git commands, following patterns suggested by Jeff King and Junio Hamano.

Current status: Initial patch version updating 6 test scripts across refs and archive test suites. Changes are mechanical replacements of temporary file patterns with direct piping of `test_seq -f` output to Git commands like `update-ref` and `mktree`.

Key technical details: Affected test scripts include `t/pack-refs-tests.sh`, `t/t0613-reftable-write-options.sh`, `t/t1400-update-ref.sh`, `t/t1460-refs-migrate.sh`, `t/t5004-archive-corner-cases.sh`, and `t/t5401-update-hooks.sh`. Changes focus on two patterns: replacing `printf`+`test_seq` with temporary files, and converting `test_seq -f` usage that still used temporary files to direct piping.

Participant positions: Based on suggestions from Jeff King (Peff) and Junio Hamano in prior discussions. Patch appears uncontroversial as it follows established test modernization patterns.