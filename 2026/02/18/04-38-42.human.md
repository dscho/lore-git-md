This is the final patch in Patrick Steinhardt's series addressing test failures related to `iconv(1)` availability on Windows/MSVC platforms. The patch updates t6006-rev-list-format.sh to properly handle cases where Git is built with ICONV support but the `iconv(1)` executable isn't available.

The changes make two tests in t6006 conditionally use `iconv(1)` only when the ICONV prerequisite is met, falling back to UTF-8 otherwise. This matches the pattern established in earlier patches of the series, maintaining test coverage while improving portability. The patch also modernizes the test setup by moving the message generation into the test setup block.

This completes the series' goal of fixing CI failures on Windows/MSVC where tests were failing due to assuming `iconv(1)` availability when only libiconv was present. The changes are cleanly implemented and consistent with the approach taken in the previous patches. With this final test file updated, the series appears ready for merging.