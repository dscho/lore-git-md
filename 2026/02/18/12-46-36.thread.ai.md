Thread identity: Bugfix series (v6) addressing symlinked `.git` directory handling, with security implications, from author Tian Yuchen (Two-patch series, now complete with both patches.)

Core problem: Git's current rejection of all symlinked `.git` directories is overly restrictive for legitimate cases where the symlink points to a directory. The series refines this behavior while maintaining security by still rejecting dangerous cases like FIFOs and invalid gitfiles.

Current status: Sixth iteration incorporating feedback from Junio Hamano and Karthik Nayak. Both patches now ready:
1. First patch (already summarized) refined error handling in `setup_git_directory_gently()` with new error codes
2. This second patch implements the symlink-to-directory allowance while maintaining security boundaries

Key technical details:
- First patch added error codes: `READ_GITFILE_ERR_STAT_ENOENT`, `READ_GITFILE_ERR_IS_A_DIR`, `READ_GITFILE_ERR_STAT_FAILED`
- Second patch modifies `setup_git_directory_gently_1()` to:
  - Use `stat()` via `read_gitfile_gently()` to allow symlinked directories
  - Maintain rejection of non-file cases (FIFOs, sockets) through existing error paths
- Comprehensive test coverage in new `t0009-git-dir-validation.sh` verifies:
  - Valid symlink-to-directory cases work
  - FIFOs and symlinks-to-FIFOs are rejected
  - Garbage gitfile content is rejected
  - Empty directories are handled correctly

Open questions: None explicitly raised in current iteration - appears to have addressed all prior review concerns. Final approval pending security review of the complete changes.

Participant positions:
- Junio Hamano and Karthik Nayak provided review feedback incorporated in v6
- No outstanding objections or requested changes in current version
- Security implications appear to have been carefully considered

Related work: Builds on long-standing security restrictions around `.git` directory handling, particularly protections against malicious repository setups. The changes maintain these protections while allowing legitimate symlink use cases.