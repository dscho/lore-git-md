This is a documentation patch that clarifies the behavior of `git fetch --jobs=0`. The patch adds a note to the man page explaining that a value of 0 will use a reasonable default, matching the behavior of `fetch.parallel=0` which was already documented. 

The change creates symmetry between the command-line option and its config equivalent. The implementation of this behavior was added in commit c39952b92 (2023-02-20) but wasn't properly documented at the time. The patch is straightforward and uncontroversial, simply documenting existing behavior rather than changing any functionality.

The patch touches only the `Documentation/fetch-options.adoc` file, adding two lines to explain the `--jobs=0` case. The commit message clearly explains the motivation and connects it to prior work. This is a small but useful documentation improvement that helps users understand the parallel fetch options more completely.