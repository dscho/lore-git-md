Security patch follow-up discussion about `.git` symlink handling. Karthik Nayak raises a question about behavior changes in the error handling flow between patch versions. The core issue is whether switching from `lstat()` to `stat()` in `setup_git_directory_gently_1()` actually changes how Git handles symlinks to directories, given that `read_gitfile_gently()` was already being called.

Karthik points out a subtle behavioral difference in error handling: in previous versions, certain non-fatal errors (like `READ_GITFILE_ERR_INVALID_FORMAT`) would return `GIT_DIR_INVALID_GITFILE`, but the new version would call `read_gitfile_error_die()` and terminate instead. This appears to tighten the security model by making more cases fatal, though the commit message doesn't explicitly call out this change.

The discussion is technical but focused - Karthik is verifying whether this behavioral change is intentional or an oversight, given that the commit message emphasizes symlink support rather than broader error handling changes. This suggests the series may need minor clarification in either implementation or documentation before final merging.