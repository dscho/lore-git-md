This is a test patch for the `contrib/subtree` component, part of a series to add history depth verification to subtree tests. The patch reintroduces commit count checks that were previously added in commit 28a7e27cff but later reverted.

The changes add `rev-list --count` assertions to verify the expected number of commits in various subtree operations. The tests now validate:
- The commit count after a split with `--rejoin` (expecting 5 commits)
- Commit counts when splitting multiple subtrees (verifying main branch divergence and incremental splits)
- Commit counts when splitting nested subtrees (checking for 2 or 3 commits depending on scenario)

The patch appears well-focused, adding only verification logic to existing tests without changing their core behavior. The commit message could be clearer about why these checks are valuable - it notes they were previously added then reverted, but doesn't explain why they're worth reintroducing now. The test changes themselves look technically sound, with clear expectations about the commit graph structure after each operation.