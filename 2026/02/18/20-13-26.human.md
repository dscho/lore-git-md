This is a technical refinement patch in Justin Tobler's series to add repository statistics reporting via `git repo`. The patch focuses on adding OID annotations to the table output, showing the specific objects referenced in the statistics.

The discussion with Patrick Steinhardt covers several implementation details:
- Justin agrees to use `strbuf_detach()` for more efficient string handling as suggested
- They confirm NULL OID handling works as expected for empty object types
- Patrick considered but rejected using descriptive labels (like "[commits]") for annotations due to column width concerns, especially since later patches will add more annotation types
- The test changes show how OID references are now included in the output with numeric markers that link to full OIDs at the bottom

This patch represents incremental progress on the series, addressing specific technical feedback while maintaining the overall approach. The changes are focused on implementation quality rather than altering the feature's core functionality. The discussion shows careful attention to memory management and output formatting details.