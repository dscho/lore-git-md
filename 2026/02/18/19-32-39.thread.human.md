This thread clarifies the behavior of `git fetch --jobs=0` and `fetch.parallel=0` in response to documentation that was previously vague about how these settings work. The original patch noted that a value of 0 means "some reasonable default," and Patrick Steinhardt suggested explicitly documenting that the fallback is based on `online_cpus()` (the number of available CPU cores), as implemented since 2023. 

Junio C Hamano accepted the improved documentation as it better reflects the actual behavior, though he observed that `--jobs=auto` might have been a more intuitive name for this case. The change creates consistency between the command-line and config documentation while maintaining the existing implementation. While the thread shows consensus on the documentation update, Junio's comment leaves open the possibility of future discussion about renaming the option for clarity. The straightforward, non-controversial patch serves as an example of the project's ongoing effort to align documentation with implementation details that users may find useful.