Here's the updated human-readable thread summary:

This thread documents the behavior of `git fetch --jobs=0` and `fetch.parallel=0`, which currently fall back to using the available CPU count (`online_cpus()`). While this implementation has been in place since early 2023, the documentation wasn't clear about this behavior. 

The discussion has reached consensus that we should explicitly document the CPU fallback behavior in both the command-line options and configuration documentation. Patrick Steinhardt investigated the history and found no evidence the original vague documentation was intentional. Maintainer Junio Hamano noted the original wording might have been deliberately flexible, suggesting `--jobs=auto` could be a clearer name.

New participant Daniel Beck provides a user perspective, explaining his motivation for the documentation patch - he wanted clear guidance on reasonable job counts. While accepting the current CPU-based implementation, he suggests abstract terms like `auto` or more descriptive names like `cpus` could better serve users while maintaining implementation freedom. 

The thread remains open for Johannes Schindelin to confirm if there was specific reasoning behind the original undocumented behavior. Future discussions may consider renaming the option for clarity and whether to backport this documentation change. The conversation exemplifies Git's careful approach to documentation - surfacing implementation details while respecting historical context and user experience.