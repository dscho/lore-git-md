Here's a human-readable summary of the thread:

This thread addresses a bug in `git subtree` where commit exclusion logic during splits was incorrectly altering history in ways that broke the API's stability guarantees. The issue was reported by George and Christian Heusel, who noticed split history regressions. 

The problematic optimization in `should_ignore_subtree_split_commit` was meant to speed up splits by ignoring commits from other subtrees, but ended up changing commit hashes in violation of the API contract. After three prior attempts to fix this (98ba49c, 83f9dad, 28a7e27), the current patch (v2 3/3) takes the straightforward approach of removing the optimization entirely rather than trying to salvage it, as the current implementation was both incorrect and often slower than the naive approach.

The fix ensures split operations maintain consistent history even with rebased or cherry-picked subtree commits. New test cases verify correct behavior with multiple subtrees and rebased commits, providing thorough coverage of the edge cases that triggered the bug. The patch appears well-justified and focused, removing the broken code without introducing new complexity.