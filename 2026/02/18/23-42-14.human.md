This is a refactoring patch that extracts revert message formatting logic into a shared function, part of the ongoing `git replay --revert` feature series. The change moves the duplicate revert message generation code from `do_pick_commit()` into a new `sequencer_format_revert_header()` function in sequencer.c, making it available for reuse by the upcoming replay command.

The new helper handles both regular reverts ("Revert <subject>") and reverts-of-reverts ("Reapply <subject>"), with an optional OID parameter that controls whether the full commit hash is appended. The patch maintains all existing behavior while eliminating code - the test suite would catch any divergence since the sequencer's revert functionality remains unchanged.

This appears to be a clean, focused change that follows Git's typical refactoring pattern: identify duplicated logic, extract it with minimal interface changes, and update callers to use the new shared version. The implementation looks solid, with proper handling of edge cases like nested reverts. As the first patch in a series that has already gone through multiple iterations (now at v3), this foundational change has likely benefited from prior review feedback.