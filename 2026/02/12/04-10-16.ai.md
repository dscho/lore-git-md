This is a refactoring patch that's part of a 2-patch series addressing string list sorting behavior. The patch modifies `builtin/sparse-checkout.c` to use `string_list_sort_u()` instead of separate calls to `string_list_sort()` and `string_list_remove_duplicates()`. The change standardizes the code to match the pattern used elsewhere in the codebase for sorted, unique string output.

The patch is straightforward - it replaces two function calls with one equivalent call in the sparse checkout pattern listing functionality. This affects how Git displays the current sparse checkout patterns when running `git sparse-checkout list`. The new version using `string_list_sort_u()` will produce the same output as before but through a more direct API call.

This appears to be a mechanical change following the pattern established in Elijah Newren's original series about which this is a follow-up. The commit message is brief but correctly identifies what's being changed. The patch is small (3 lines modified) and focused on a single file, making it easy to review. Since this is part of an ongoing effort to standardize string list handling, it's likely to be uncontroversial.