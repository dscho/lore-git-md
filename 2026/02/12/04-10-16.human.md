This is a small refactoring patch in the sparse-checkout subsystem, part of a 2-patch series addressing string list sorting behavior. The patch replaces two function calls (`string_list_sort()` followed by `string_list_remove_duplicates()`) with a single `string_list_sort_u()` call in `builtin/sparse-checkout.c`. This change makes the code more concise while maintaining the same functionality for processing sparse checkout patterns.

The modification is straightforward and appears correct - it's a mechanical change that doesn't introduce new behavior, just uses a more appropriate API function. The commit message is brief but clear about what's being changed. Since this is part of a series standardizing string list handling across Git, it's likely to be uncontroversial.

The patch touches a sensitive area (sparse checkout patterns), but the change is minimal and focused purely on the sorting implementation detail. No test changes are included, which is reasonable since this is an internal refactoring that shouldn't affect observable behavior.