[PATCH] Proposal for .gitallow file to restrict Git commands

Thread identity: Feature proposal for a new `.gitallow` configuration mechanism to restrict which Git commands can be executed in a repository. Originating author is not a core contributor (name not in project context). Thread type is design discussion exploring safety mechanisms.

Core problem: Accidental execution of destructive Git commands (e.g. `git reset --hard`, `clean -fdx`) could be prevented by requiring explicit allow-listing in a repository-specific `.gitallow` file. The proposed format would specify permitted commands and optionally their arguments, blocking anything not listed.

Current status: Initial proposal stage with no implementation. Author acknowledges partial AI assistance in drafting and seeks prior art/feedback before developing further. No version history yet as this is the thread root.

Key technical details: Suggested `.gitallow` would live in repository like `.gitignore`, with syntax to whitelist commands (e.g. `checkout`, `log`) and optionally restrict arguments. No details yet on how this would hook into Git's command dispatch or interact with existing safety mechanisms (e.g. `--dry-run` flags).

Open questions: Whether this has been discussed before (author references unclear Discord conversation), what concrete use cases would justify the complexity, how it would handle command aliases and plumbing commands, and how it would integrate with Git's existing permission model (e.g. filesystem permissions, config scopes).

Participant positions: Only the original author has commented so far, framing this as an exploratory inquiry rather than a concrete proposal. No maintainer or regular contributor feedback yet.

Related work: No specific prior art cited beyond an ambiguous Discord reference. Conceptually adjacent to existing safety mechanisms like `core.protectHFS` and `core.protectNTFS` but operating at command level rather than filesystem.