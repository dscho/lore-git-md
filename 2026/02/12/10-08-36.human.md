This is a documentation patch in a 9-part series enhancing Git's promisor remote functionality. It documents the `--filter` option for `git fetch`, bringing it in line with existing documentation for `git clone`.

The patch adds a section to `fetch-options.adoc` that explains how to use `--filter` for partial clones, mirroring the wording already used in the clone documentation. The description covers basic usage (e.g., `--filter=blob:none`) and references `git-rev-list` for more details on filter specifications.

This is a straightforward documentation alignment that maintains consistency between related commands. The commit message notes this is intentional - keeping the wording identical between clone and fetch documentation for now, even if it means some redundancy. The patch has already incorporated feedback from Jean-Noël Avila on formatting and Christian Couder on back-tick usage in previous iterations.

As part of a larger series introducing new promisor remote features, this documentation update ensures users will have clear guidance about filter options when using the enhanced functionality. The patch appears ready for merging as part of the complete series.