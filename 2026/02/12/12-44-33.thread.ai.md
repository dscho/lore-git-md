Thread summary:

This thread introduces a 4-patch series by Patrick Steinhardt improving the UI of the new `git history` command, part of his ongoing work to expand Git's built-in functionality. The series focuses on two key usability improvements: (1) moving validation (revwalk checks and merge detection) earlier in execution to prevent wasted user input, and (2) renaming the technical `--ref-action` option into clearer alternatives (`--dry-run` and `--update-refs`).

The first patch (current email) specifically addresses validation timing by splitting reference update handling into two phases: `setup_revwalk()` now performs all validation (including revwalk setup and merge detection) before any user interaction, while `handle_reference_updates()` retains the actual reference updating logic. This prevents users from losing work when invalid conditions (like non-ancestor commits) are detected. The implementation cleanly separates this change from the subsequent option renaming, maintaining bisectability while touching core command flow in `builtin/history.c`. Test updates verify the new behavior, particularly around the `--ref-action=head` case where ancestor validation is critical.

The series is structured with validation changes (patches 1-2) followed by option renaming (patches 3-4), showing Patrick's characteristic attention to both user experience and technical implementation quality. These changes resolve conflicts with another in-flight topic and serve as preparation for a larger `git history split` feature. The implementation appears straightforward despite touching core command logic, with comprehensive test coverage.