Review feedback on the help.c string list sorting patch from Junio C Hamano. This is the second patch in a two-part series standardizing string list sorting behavior across Git commands.

Junio questions whether the change is strictly necessary, noting that the current code's `keys_uniq` list should already be sorted due to the prior sorting of `keys`. However, he acknowledges that using `string_list_sort_u()` is a reasonable future-proofing measure since it makes the code's intentions clearer and more robust against future modifications. 

The review also includes broader criticism of the surrounding code structure, describing it as "ugly" and suggesting a more significant refactoring would be valuable. Junio proposes splitting the different output type handling (CONFIG_HUMAN vs others) into separate helper functions called via a switch statement, which would make the control flow clearer. This would be a natural follow-up cleanup opportunity.

While Junio accepts the technical correctness of the patch, his feedback indicates this area of `builtin/help.c` could benefit from deeper restructuring beyond just the string list sorting change. The patch appears likely to be accepted given Junio's pragmatic stance on the change itself, but his comments suggest this might prompt future cleanup work in the help command's config output handling.