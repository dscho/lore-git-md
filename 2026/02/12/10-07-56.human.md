This is the final patch in Christian Couder's 8-part series implementing auto-filter functionality for promisor remotes. The patch wires up the `--filter=auto` logic in fetch-pack, enabling dynamic filter combination from server-advertised specs.

The discussion focuses on two implementation details Patrick Steinhardt raised: 1) whether to directly expose `filter_promisor_remote()` rather than indirectly calling it via `promisor_remote_reply()`, and 2) simplifying error handling by checking `gently_parse_list_objects_filter()`'s return value directly. Christian agrees with both suggestions, proposing a modified function signature for the first case and accepting Patrick's error handling simplification.

The patch builds on previous work in the series to combine filters from accepted promisor remotes while maintaining security constraints. It handles the auto-filter resolution during fetch operations, with careful attention to memory management and error cases. The implementation has been refined through active review, with Junio C Hamano previously confirming the "accepted_filters" naming choice.

This completes the LOP (Large Object Promisors) effort, providing both secure remote field storage (`promisor.storeFields`) and dynamic filter combination (`--filter=auto`). The series has seen thorough review from Patrick Steinhardt and documentation feedback from Jean-Noël Avila, with all outstanding issues now addressed.