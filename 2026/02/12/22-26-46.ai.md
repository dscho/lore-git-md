This is a technical discussion thread about patch 2/4 in Patrick Steinhardt's series improving the `git history` command's UI. The patch moves merge commit validation earlier in the execution flow to avoid wasting user input when merges are present.

Junio C Hamano responds to D. Ben Knoble's review comment about string vector operations, pointing out that `strvec_pushv()` exists for concatenating string arrays. This is a minor implementation detail clarification - the core of the patch (which adds merge detection via `revwalk_contains_merges()`) had already been reviewed and accepted in earlier messages.

The exchange shows the maintainer providing quick, precise technical guidance about Git's internal APIs. While not changing the patch itself, it helps educate contributors about available utilities. The thread context indicates this patch is part of a larger effort to improve `git history`'s validation and dry-run capabilities, with the series already receiving substantial review attention.