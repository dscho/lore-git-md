This is a small refactoring patch series addressing an edge case in string list handling. The author, Amisha Chhajed, identifies a scenario where the sorted behavior of string lists could diverge from expectations when using `string_list_remove_duplicates()`.

The series consists of two straightforward changes:
1. Updates `builtin/sparse-checkout.c` to use `string_list_sort_u()` instead of the current duplicate removal approach
2. Similarly modifies `builtin/help.c` to ensure consistent sorted output

The motivation comes from a specific edge case where the current processing order could produce unsorted results when certain config patterns are added. The commit message includes reproduction steps showing how adding particular config entries could cause test failures when comparing against `sort -u` output.

The changes are small and focused - just 2-3 lines modified in each file - and appear to be a straightforward cleanup. The author has identified a genuine inconsistency, though it's worth noting this affects an edge case rather than being a widespread issue. The patches follow the project's style and include a clear explanation of both the problem and solution.