Maintainer ack for string list sorting standardization. Junio C Hamano has reviewed Amisha Chhajed's patch to update `sparse-checkout list` to use the unified `string_list_sort_u()` function instead of separate sort-and-deduplicate operations. This is part of a 2-patch series (with Elijah Newren mentoring) to standardize string list handling across Git commands.

The change is minimal (3 lines modified) and mechanically sound - it simply replaces two function calls with one equivalent call that combines sorting and deduplication. Junio's "obviously correct" assessment and immediate intention to queue the patch indicates this uncontroversial cleanup will likely land in the next release. The patch maintains identical output behavior while using the preferred API pattern.

This continues an ongoing effort to normalize string list processing throughout the codebase. The sparse-checkout command was one of the remaining spots using the older two-step approach. With Junio's ack, this patch appears ready for merging unless other reviewers raise objections in the short window before the next "What's cooking" report.