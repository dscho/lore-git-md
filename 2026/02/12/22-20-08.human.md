This is a patch review comment in Patrick Steinhardt's series improving the `git history` command's UI. D. Ben Knoble provides feedback on patch 2/4, which moves merge commit validation earlier in the execution flow to avoid wasting user input when merges are present.

The key technical observation is that the patch adds a new `revwalk_contains_merges()` function that checks for merge commits (using `--min-parents=2`) before any user interaction occurs. Knoble notes the implementation is sound but expresses some surprise at the string handling approach and initially gets confused by the merge detection logic before seeing how it all fits together.

The review also echoes Junio Hamano's earlier question about whether using `-c core.editor=false` in the test properly verifies that no editor is invoked. This remains an open question in the thread. Knoble's tone is constructive and engaged, showing careful reading of both the code and prior discussion in the thread. The feedback suggests the patch is technically sound but could benefit from clearer test documentation around the editor behavior verification.