This is a follow-up in the ongoing discussion about syntax for referencing a repository's default branch. Harald Nordgren responds to D. Ben Knoble's workflow example of using `git switch -d origin` to work directly with the remote's default branch without maintaining a local copy.

The email highlights a practical workflow where users might prefer detached HEAD states for default branch work, suggesting they'll disable the "detached HEAD" warning since it's a deliberate choice in this case. This reinforces one of the use cases being considered in the broader thread - whether the proposed `@{default}` or `@{local}` syntax should accommodate both branch-attached and detached-HEAD workflows.

The message doesn't introduce new technical content but provides real-world validation that some users already employ detached-HEAD workflows with default branches, which the syntax proposals would need to support cleanly. It's a small but relevant data point in the ongoing evaluation of whether these use cases justify new syntax additions.