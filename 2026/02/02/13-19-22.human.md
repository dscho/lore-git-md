This is a bugfix for `git stash` that addresses a long-standing FIXME regarding ignored file handling. The patch adds proper support for `--no-overwrite-ignore` to match the behavior of other Git commands like checkout and reset.

The change makes `git stash` respect ignored files when the `--no-overwrite-ignore` option is specified, rather than unconditionally overwriting them. The implementation adds new options to both `stash push` and `stash save` commands, with overwriting enabled by default to maintain backward compatibility.

The patch includes a test case that verifies ignored files are preserved when using `--no-overwrite-ignore`. The change is well-contained, touching only the stash command implementation and its test suite. The commit message clearly explains the motivation and connects it to the existing FIXME comment being addressed.

This appears to be a straightforward improvement that aligns stash behavior with other Git commands while maintaining the expected default behavior. The implementation looks clean and the test provides good coverage of the new functionality.