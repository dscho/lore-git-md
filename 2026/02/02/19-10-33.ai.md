This is a follow-up in the ongoing thread about validating `--prefix` paths in `git subtree split`. Junio C Hamano acknowledges Josh Steadmon's report that the v3 patch (which fixed prefix validation to check against commits rather than the working tree) has introduced a regression in test case 17 ("split requires path given by option --prefix must exist"). 

The issue was caught in a real-world build system at Google and bisected to this commit. Junio notes this is a timely catch since the patch is still in the 'next' integration branch, meaning it can be reverted before reaching 'master'. The test failure suggests the new validation logic may be too strict in some edge cases not covered by the existing test suite.

This email doesn't propose a solution but confirms the regression and indicates the patch will need to be reverted from 'next' while Pushkar Singh (the original author) investigates. The thread shows the careful balancing act required when fixing inconsistent behavior - the original validation was clearly wrong (checking working tree instead of commit), but the fix now appears to handle some valid cases incorrectly. The next iteration will need to properly accommodate both scenarios.