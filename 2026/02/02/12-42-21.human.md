Bugfix for `git blame` coloring when using `--ignore-rev` and `--color-lines` together. This is a follow-up to René Scharfe's patch that fixed incorrect coloring behavior when ignoring revisions.

Junio C Hamano acknowledges the fix works but expresses mild concern that the solution might be treating a specific case rather than addressing a more general pattern in the blame machinery. He notes that similar situations occur naturally during blame calculation (like when two merge parents eventually trace back to the same ancestor), and wonders if there might be a more comprehensive approach that would handle all such cases uniformly.

The patch itself is already working (confirmed by the original reporter) and includes a test case, so this is more of an architectural observation than a blocking concern. Junio accepts the fix as-is while flagging a potential area for future improvement in the blame code's structure. The thread appears complete with this final review comment.