Bugfix follow-up discussion for `git stash`'s ignored file handling. D. Ben Knoble replies to Karthik Nayak's observation about test coverage for the new `--overwrite-ignore` behavior in Pushkar Singh's patch series.

The exchange focuses on verifying that the test suite properly exercises both the overwrite and no-overwrite scenarios. Knoble confirms that even without the new flag, the test case passes, suggesting it may not be adequately testing the intended behavior that motivated the original FIXME. This appears to be catching an oversight in the test coverage added in v2 of the patch.

The discussion remains technical and constructive, with both participants focused on ensuring the test suite properly validates the new functionality. Knoble's observation implies the test case may need adjustment to better distinguish between the default (overwrite) and explicit `--no-overwrite-ignore` behaviors. This is the sort of subtle test coverage gap that often surfaces during review of behavioral changes like this one.