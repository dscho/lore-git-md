This is a bugfix and behavior improvement for `git stash`'s handling of ignored files, addressing a long-standing FIXME in the code. Pushkar Singh's patch adds new `--overwrite-ignore` and `--no-overwrite-ignore` options to control whether stash operations should overwrite ignored files, defaulting to the current behavior (overwrite) for backward compatibility.

The v2 revision makes several important improvements over the initial version: fixing a parse-options API misuse, properly handling the interaction between `-a/--all` and the new option, and adding comprehensive test coverage. Reviewers identified technical and documentation issues in v1 that have now been addressed.

Kristoffer Haugsbakk notes a minor patch submission convention that was missed - version change notes should appear after the `---` separator rather than in the commit message body. This is a common nit for new contributors but doesn't affect the technical quality of the patch itself.

The change aligns `git stash`'s behavior with other commands like `checkout` and `reset` regarding ignored files while maintaining backward-compatible defaults. The implementation appears solid with thorough test coverage, and the discussion suggests it's nearing readiness for inclusion.