This is a brief follow-up in the ongoing discussion about syntax for referencing a repository's default branch. Harald Nordgren responds to D. Ben Knoble's suggestion of using `git switch -d origin` to work with the default branch without maintaining a local tracking branch.

The email highlights a practical workflow where users might prefer detached-HEAD mode for default branch access, avoiding the overhead of local branch maintenance. Harald notes they will also disable the detached-HEAD warning, suggesting this is a deliberate workflow choice rather than an accidental state.

Key technical details for future reference:
- The `git switch -d origin` command resolves to the remote's HEAD (typically the default branch)
- The `advice.detachedHead` config can suppress warnings for this workflow
- This demonstrates a real-world use case where direct default branch access is valuable

The exchange reinforces the thread's exploration of different approaches to default branch referencing, now including both branch-attached and detached-HEAD workflows. The discussion continues to weigh the proposed `@{default}` and `@{local}` syntax options against these practical usage patterns.