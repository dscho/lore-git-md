Bugfix for `git log --graph` formatting when combined with `-p`. The patch reports a visual glitch where graph characters (like `|` and `*`) appear misplaced at the end of lines rather than at the start when using `--graph --pretty=format` together with `--patch`. The author provides clear reproduction steps and shows the difference between expected and actual output, noting that workarounds exist but have their own drawbacks. The issue appears to affect the alignment of graph elements with commit messages and diffs, making the output harder to read. The bug report is thorough, including version information and exploration of related format string behaviors. This looks like a legitimate formatting bug in Git's graph output logic when combined with patch display.