Bugfix for `git stash`'s interaction between `--all` and the new `--no-overwrite-ignore` option. Kristoffer Haugsbakk provides a minor submission-nits review of Pushkar Singh's v2 patch, noting that the "Changes since v1" section should appear after the `---` separator rather than in the commit message body. This is purely a formatting suggestion about patch submission conventions, not a technical critique of the changes themselves.

The underlying patch (which Haugsbakk is not disputing) fixes `git stash`'s handling of ignored files when using `--all`, ensuring `--no-overwrite-ignore` properly prevents ignored files from being removed. The v2 iteration already addressed the substantive technical feedback from Patrick Steinhardt and Karthik Nayak about option parsing and test coverage, making this a clean-up review focused on submission hygiene.

For the AI brief: This email contains only procedural feedback about patch formatting conventions, specifically that version-change notes belong after the `---` separator. The reviewer suggests using `git format-patch --notes` to automate this placement. No new technical discussion of the stash behavior or implementation occurs here - the substantive changes were already made in v2 per prior reviews. The thread remains focused on finalizing the patch's form rather than its function.