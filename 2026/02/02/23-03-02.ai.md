This is a follow-up discussion in the thread about default branch reference syntax, where Harald Nordgren responds to concerns about discovering the upstream remote name. The email doesn't introduce new technical content but rather acknowledges a practical pain point in the workflow being discussed.

Harald confirms that determining the correct remote name (especially in GitHub fork workflows where the 'gh' CLI tool renames 'origin' to 'upstream') remains a non-trivial step, even when using the detached-HEAD approach (`git switch -d origin`) that has emerged as the preferred solution for default branch access. He shares his personal workaround using `git remote | rg '^(origin|upstream)$' | tail -n1` to handle GitHub's remote renaming behavior.

The exchange highlights that while the core technical question of default branch access has been addressed through existing functionality, real-world usage still involves some awkwardness around remote naming conventions - particularly in GitHub's fork-and-PR workflow. This doesn't fundamentally change the thread's direction but provides useful context about why users might still seek more ergonomic solutions.