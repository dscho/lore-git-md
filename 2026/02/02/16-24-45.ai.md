This is a bugfix for `git blame`'s coloring behavior when using both `--color-lines` and `--ignore-rev` together. The thread is already resolved with René Scharfe's patch fixing the issue, and this email is a follow-up discussion about the implementation approach.

René clarifies why his solution handles the specific case of `--ignore-rev` separately rather than using the existing `blame_coalesce()` function. The key technical point is that entries that are ignored need to remain distinct from non-ignored entries for the `blame.markignoredlines` feature to work correctly. This explains why the more general coalescing mechanism isn't appropriate here.

The discussion reveals an interesting implementation detail about how blame handles merge traversal versus explicit revision ignoring, though the core fix remains sound. Junio's question about whether this could be handled more generally shows his maintainer's perspective looking for patterns in the codebase, while René's response demonstrates the specific constraints that make this case different.

This is essentially a post-fix discussion between two experienced contributors about architectural considerations, with no changes needed to the already-working solution. The thread can be considered closed from a practical standpoint, though it surfaces some background about the blame machinery's design.