This is a discussion thread about the proposed `@{default}` shorthand for referencing a repository's primary branch, now in its fourth iteration. The latest exchange between D. Ben Knoble and Harald Nordgren focuses on whether detached HEAD states are sufficient for common workflows or if explicit branch naming is truly necessary.

Ben Knoble counters Harald's suggestion to use detached HEADs by sharing his personal workflow of frequently using `git switch -d origin` to avoid maintaining a local main branch when he doesn't actively need it. This provides a concrete example where detached HEADs are actually preferable to branch tracking, adding nuance to the ongoing discussion about when branch attachment is essential versus when it's optional.

The thread has evolved from abstract justification to examining real-world use cases, with multiple contributors now providing specific scenarios where branch naming is either required (as shown in previous emails) or can be avoided (as Ben demonstrates here). The technical implementation remains sound, but the discussion now hinges on whether the combined examples sufficiently justify introducing new syntax versus relying on existing detached HEAD capabilities.