This thread discusses an unexpected interaction between Git's submodule recursion configuration options. The core issue is that the general `submodule.recurse` setting can override more specific `fetch.recurseSubmodules` and `push.recurseSubmodules` settings depending on parse order, which may not match user expectations. D. Ben Knoble raised this as a potential behavior quirk, noting the current implementation simply overwrites the same internal variable without considering which option is more specific.

The thread is exploring whether this behavior was intentional, whether changing it would break existing configurations, and whether the current order-dependent behavior should at least be documented if kept. This touches on Git's config precedence rules and the balance between backward compatibility and intuitive behavior. The discussion appears to be in early stages, with no clear consensus yet on whether or how to modify the current implementation.