This is a technical discussion in the ongoing thread about Windows-specific error handling for `.git` directory validation, specifically addressing the `/dev/null` (or `nul` on Windows) edge case. Tian Yuchen provides debugger output showing the call stack when `GIT_DIR=nul` fails on Windows, confirming that `stat("nul")` triggers a fatal error in the current code path through `read_gitfile_gently()` and `setup_explicit_git_dir()`. The email serves to illustrate the concrete failure mode Junio had questioned, but Yuchen explicitly notes they lack Windows expertise to propose a definitive solution. The core tension remains between maintaining strict validation (which fails on Windows' `nul` device) versus relaxing error handling (which risks security regressions). The thread now awaits input from Windows-savvy contributors to resolve this platform-specific behavior within the security constraints Junio has emphasized.