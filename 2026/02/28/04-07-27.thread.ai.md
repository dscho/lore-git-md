Thread summary:

This thread begins with Tian Yuchen's patch to migrate encoding-related global variables (`git_commit_encoding` and `git_log_output_encoding`) into `struct repo_settings` as part of the ongoing `the_repository` removal effort. The changes introduce repository-aware APIs (`repo_get_commit_output_encoding()` and `repo_get_log_output_encoding()`) while maintaining fallback to `the_repository` for compatibility. The patch affects 20 files across Git's codebase, primarily touching commit message formatting, mail handling, and revision walking logic. Key technical changes include moving encoding configuration from `environment.c` to `repo-settings.c` and ensuring proper initialization order in `revision.c` to preserve CLI argument precedence over lazy-loaded settings. The author positions this as a proof of concept for their GSoC 2026 proposal on global state libification, noting successful test suite verification and memory checks. The changes align with Git's architectural direction under the broader `the_repository` removal effort led by René Scharfe, though no reviews or responses from other contributors have been recorded yet in this initial submission. The patch appears mechanically sound and well-scoped, focusing specifically on encoding subsystem changes while maintaining backward compatibility through transitional APIs.