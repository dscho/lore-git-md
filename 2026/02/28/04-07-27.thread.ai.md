Updated AI thread summary:

This thread tracks Tian Yuchen's work to migrate encoding-related functionality from global variables to repository-specific storage as part of the `the_repository` removal effort. The series has evolved through multiple versions with significant architectural changes based on feedback.

Thread identity:
- Subject: Migrating encoding globals to repository structure
- Author: Tian Yuchen
- Thread type: Technical refactoring (part of GSoC 2026 proposal)
- Core problem: Remove encoding-related global state (`git_commit_encoding`, `git_log_output_encoding`) by moving to repository context while maintaining behavior
- Current status: Series at v3 with 3 patches:
  1. Move encoding configs to struct repository (changed from repo_settings)
  2. Update commit creation APIs to use repository context
  3. Convert pretty-printing functions (new in this update)

Key technical changes:
- Architectural pivot: encodings now stored directly in `struct repository` instead of `repo_settings` (Phillip Wood's suggestion)
- Commit creation APIs (`commit_tree()`, `commit_tree_extended()`) updated to take `struct repository*` parameter
- Pretty-printing functions (`pretty_print_commit()`, `pp_commit_easy()`) now take `struct repository*` parameter
- 50+ files updated across commit handling, mail operations, revision walking, and builtin commands
- New repository-aware APIs: `repo_get_commit_output_encoding()`, `repo_get_log_output_encoding()`
- Core changes:
  - Builtins (`commit`, `log`, `rebase`, `merge`, `stash`, `notes`, `show-branch`) updated to pass context
  - Sequencer operations propagate repository through formatting chain
  - Range-diff machinery updated to handle repository context
  - Fallbacks maintained for unconverted callers via `the_repository`
  - Config parsing still uses `the_repository` temporarily (future work)

Technical considerations:
- Behavior preserved during transition via fallbacks to `the_repository`
- Repository context propagation through both commit creation and display paths
- Test suite passes with sanitizers
- Follows established patterns for global variable migration
- Mechanical changes across many call sites but maintains existing behavior

Participant positions:
- Phillip Wood suggested architectural change from repo_settings to struct repository
- Author implemented feedback in v3
- No other reviews yet recorded for latest version

Related work:
- Part of GSoC 2026 global state libification proposal
- Builds on prior work moving other subsystems to repository context
- Complements René Scharfe's `the_repository` removal patterns
- Follows similar API migration patterns seen in other subsystems

Open questions:
- Need to verify all commit display paths properly handle NULL repository (fallback case)
- Final integration with other `the_repository` removal efforts pending
- Config parsing migration remains as future work
- Potential follow-up to eliminate remaining fallback paths once all callers converted
- Testing coverage for edge cases in commit creation and display paths with NULL repository
- Review feedback on the pretty-printing changes expected in next iteration