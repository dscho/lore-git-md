This is part of the ongoing `the_repository` removal effort, specifically targeting encoding-related global variables. Tian Yuchen's patch migrates `git_commit_encoding` and `git_log_output_encoding` into `struct repo_settings` to enable repository-specific encoding configurations.

The changes introduce new APIs `repo_get_commit_output_encoding()` and `repo_get_log_output_encoding()` while maintaining backward compatibility through fallbacks to `the_repository` for callers not yet converted. The patch carefully handles initialization order in `revision.c` to ensure CLI arguments like `--encoding` take precedence over lazy-loaded config values.

The diff shows mechanical updates across 20 files, primarily converting direct variable access to repository-aware function calls. The encoding settings are now loaded via `prepare_repo_settings()` rather than through direct config callbacks. The log output encoding maintains its existing fallback behavior to commit encoding when unset.

This appears to be a well-scoped migration following established patterns for global variable removal. The author notes successful test suite verification and positions this as part of their GSoC 2026 proposal on global state libification. The changes touch sensitive areas (commit creation, pretty-printing) but focus narrowly on the encoding subsystem transition.