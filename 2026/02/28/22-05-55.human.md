This is a refactoring patch that's part of a series to add path-related functionality to the new `git repo-info` command. The patch focuses on renaming enums in `builtin/rev-parse.c` to make their purpose clearer.

Lucas Seiki Oshiro's patch renames two enums (`format_type` and `default_type`) and their values by prepending `path_` to better indicate they're used for path formatting. The changes are purely mechanical - no behavior is altered, just naming improved for clarity. The enum values now read as `PATH_FORMAT_RELATIVE` instead of `FORMAT_RELATIVE`, making their purpose more obvious at the call sites.

This appears to be solid preparatory work for the larger series, which aims to share path handling code between `rev-parse` and the new `repo-info` command. The patch follows Git's style well and maintains all existing functionality while improving code clarity. As the first in a series from a new contributor, it's a focused change that sets up for more substantive work to follow.