This is a test suite improvement patch addressing exit code handling in t/t3700-add.sh, following up on Junio Hamano's review feedback.

The patch converts several test cases that previously piped `git ls-files` directly into `grep`, which could mask failures by suppressing the exit code. The v2 version makes three key improvements: replacing raw `grep` with the `test_grep` helper for better failure diagnostics, using `test_grep !` for negative assertions instead of counting lines with `wc -l`, and removing unnecessary shell redirections. 

The changes touch 15 test cases, focusing on patterns like file modes and symlink handling. The patch demonstrates good attention to review feedback - it addresses Junio's concerns about debuggability by adopting `test_grep`, and simplifies the test logic by eliminating subshells and line-counting where simple pattern absence checks suffice. The commit message clearly documents the rationale and changes from v1 to v2.

This work aligns with Git's ongoing test modernization efforts, particularly around proper exit code handling and use of test infrastructure helpers. The patch appears well-considered and ready for inclusion, having incorporated the maintainer's suggestions while maintaining the test suite's correctness.