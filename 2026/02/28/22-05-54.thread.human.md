This thread centers on Lucas Seiki Oshiro's completed 4-patch series adding path-related fields to `git repo-info`, which introduced shared path-handling infrastructure between `repo-info` and `rev-parse`. The core implementation established `path.toplevel` field support through new `path.c` utilities like `strbuf_add_path()` and standardized `PATH_FORMAT_*` enums, with Jayatheerth K endorsing the design's consistency with `rev-parse` for scripting use cases.

Tian Yuchen's recent follow-up raises thoughtful technical concerns about the implementation. He questions whether `strbuf_add_path()` should handle high-level formatting logic, suggests format modifiers (à la `for-each-ref`) for more flexible output control, and flags performance considerations around repeated `xgetcwd()` calls in submodule contexts. His critique of the blanket `prefix`/`format` parameter approach for all `get_*` functions—even when unused—indicates potential scaling issues as more fields are added.

The discussion has evolved from initial implementation to design refinement, with multiple contributors (including GSoC 2026 participants) now engaging. While the core path handling serves as a foundation for planned extensions like additional `path.*` fields and category-based querying, Tian's comments suggest the infrastructure may need iteration before broader adoption. The thread remains constructive, balancing stability for current work with forward-looking architectural considerations.