Git project status update from Junio C Hamano's February 2026 "What's cooking" report tracking topics across integration branches (maint, master, next, seen). Recent graduates to master include test suite cleanups, documentation updates, and `git history reword` UI improvements. Fixes landed for `git apply` and `git diff` whitespace handling with symlinks.

New topics under consideration: `git upload-pack` optimizations, Linux fsmonitor daemon, `git status` branch comparison improvements, and Patrick Steinhardt's object source API restructuring for pluggable backends. Ongoing efforts: `the_repository` removal (René Scharfe), SHA-1/SHA-256 Rust interoperability, MIDX/bitmap improvements.

Adrian Ratiu's hook API conversion series (`ar/config-hooks`), currently in 'next', has encountered a performance regression in `receive-pack` update hooks identified by Patrick Steinhardt. This architectural shift from direct `run_command()` to a configurable hook API introduces key features like `git hook list`, config-based definitions, and multi-hook support - foundational for future parallel execution. Adrian requests holding the merge to 'master' while preparing a v3 revision, clarifying that Patrick's performance concerns are separate from the v3 changes. The regression affects the broader hook conversion effort (including `ar/parallel-hooks`) that has progressed for months. Patrick's assessment carries weight given his expertise in performance-critical subsystems, highlighting the project's careful balance between new functionality and core performance requirements, particularly for server-side operations. The thread shows active coordination between Adrian (implementing features) and Patrick (evaluating operational impact) to resolve the issue before final merging.