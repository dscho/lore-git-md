This is a small bugfix addressing feedback from the maintainer about a previous change to make a warning flag repository-specific when it should remain process-wide.

The patch reverts part of an earlier change that moved the `warn_on_object_refname_` flag from a global variable to repository settings. Junio Hamano pointed out this flag represents a user/process preference rather than being repository-specific. The change simply moves the variable back to being global and adds a clarifying note in the comment to prevent future confusion.

The diff is minimal - just one line reverted and one comment added in `environment.c`. This appears to be a straightforward correction following maintainer feedback, with no test changes needed since it's reverting behavior to match the original design. The commit message clearly states the rationale and attribution.