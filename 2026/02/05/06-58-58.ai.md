This is a CI/build system patch addressing Windows compatibility concerns in the Meson build integration for Git's GUI components (gitk and git-gui). The thread has progressed from architectural discussions to final implementation details, with v2 settling on a symlink-based approach to preserve source layout while meeting Meson's requirements.

Patrick Steinhardt responds to Junio Hamano's concern about Windows symlink reliability, noting that while modern Windows supports symlinks when Developer Mode is enabled, the behavior varies across environments (native, Cygwin, MSYS2). He confirms the symlink approach should work in Cygwin/MSYS2 contexts but hasn't tested it directly. This leaves the Windows compatibility question partially answered - the solution should function in common development environments but may not cover all Windows configurations out of the box.

The exchange represents the last major technical hurdle for this series, which has otherwise achieved consensus on its symlink-based architecture. The patch maintains feature parity with the Makefile build system while accommodating Meson's constraints. Key technical points include Meson's subproject_dir limitations and the conditional build logic for Tcl/Tk toolchains. With this clarification, the path forward depends on verifying Windows behavior in practice, though the response suggests the solution should work in the environments most Git developers use on Windows.