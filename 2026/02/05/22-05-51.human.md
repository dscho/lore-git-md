This is a follow-up to a completed submodule `ignore=all` behavior patch series that has already been through multiple iterations (v3 was the last substantive version). The thread summary shows the technical work is done - the implementation aligns `git add` behavior with `git status`/`git diff` when submodules are set to `ignore=all`, requiring explicit `--force` to stage changes to ignored submodules.

Junio appears confused by Claus Schneider's email because the series is already marked as complete in Junio's "What's cooking" report, with all feedback addressed in v3. The email seems to be an accidental duplicate or misdirected follow-up rather than new work. The substantive discussion concluded with v3, which added comprehensive tests, documentation updates, and implemented the `--force` flag approach as agreed upon in earlier reviews. The series is effectively ready for integration pending final maintainer approval.