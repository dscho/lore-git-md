This is a follow-up in the submodule `ignore=all` behavior thread, where Claus Schneider confirms the implementation of the `--force` option approach is complete and requests final review before integration. The email references previous discussion about naming the option (originally `--include-ignored-submodules` per Phillip Wood's suggestion, but ultimately settled on `--force` for consistency with similar Git behaviors).

The thread has progressed through three iterations, with v3 implementing the `--force` naming and comprehensive handling of submodule ignore behavior. Key technical points from earlier in the thread include:
- New `skip_submodule()` helper function with path verification and config checking
- 8 test cases in t2206-add-submodule-ignored.sh covering all aspects
- Documentation updates across three files (git-add.adoc, config/submodule.adoc, gitmodules.adoc)
- Maintained compatibility with pathspecs and sparse checkouts

Claus's email indicates all feedback has been addressed and the series is ready for Junio's consideration. The implementation aligns `git add` behavior with `git status` and `git diff` when submodules are configured with `ignore=all`, requiring explicit `--force` to stage changes to such submodules. The change supports branch tracking workflows while reducing accidental submodule updates.