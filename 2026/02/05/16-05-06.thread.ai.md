Thread identity: 4-patch series by Matthew John Cheetham extending Git's TRACE2 process ancestry tracing to macOS while refactoring the Windows implementation, building on prior commits 353d3d77 for Windows and 2f732bf1 for Linux. Now acked by Junio C Hamano pending minor final updates.

Core problem: Unify process ancestry tracing across platforms (Linux, Windows, macOS) via TRACE2 while maintaining backward compatibility. The macOS implementation uses `sysctl` with `KERN_PROC` to recursively gather parent process IDs and names with 10-level depth limit (NR_PIDS_LIMIT), matching Linux's format through `trace2_cmd_ancestry()`.

Current status: Series has maintainer ack from Junio after addressing Derrick Stolee's detailed review. Windows implementation was refactored to separate data collection (using `strvec`) from JSON formatting, with dual-emission of legacy JSON and new unified format for backward compatibility. Build system support added across Makefile, CMake and Meson.

Key technical details:
- New `compat/darwin/procinfo.c` for macOS implementation
- Windows refactor moves JSON formatting out of data collection layer
- Cross-platform consistency in depth limiting and output format
- Dual-format emission during transition period

Open questions:
- Test coverage strategy for macOS implementation remains to be finalized
- Future cleanup to potentially align loop structures between Linux/macOS (currently both recursive)
- Documentation standardization in commit messages
- Timeline for removing Windows JSON fallback (suggested after two major versions)

Participant positions:
- Derrick Stolee initially suggested iterative approach but now agrees recursive is acceptable for consistency with Linux
- Junio C Hamano has acked the technical approach pending minor updates
- Matthew John Cheetham maintaining cross-platform consistency as primary goal

Related work: Builds on existing Linux (2f732bf1) and Windows (353d3d77) implementations. Part of broader TRACE2 instrumentation effort.