Updated AI thread summary:

This 4-patch series extends Git's TRACE2 process ancestry tracing to macOS while refactoring and unifying the Windows implementation. The work builds on prior commits 353d3d77) (Windows) and 2f732bf1 (Linux).

Patch 1 implements macOS support via new `compat/darwin/procinfo.c` using `sysctl` with `KERN_PROC` to recursively gather parent process IDs and names up to PID 1 (launchd) with a 10-level depth limit (NR_PIDS_LIMIT). It emits ancestry via `trace2_cmd_ancestry()` matching Linux's format rather than Windows's `data_json`.

Patch 2 adds build system support for the Darwin implementation across Makefile, CMake and Meson, following existing patterns for platform-specific code inclusion.

Patch 3 refactors the Windows implementation to separate data collection from JSON formatting while maintaining backward compatibility, splitting ancestry collection into a `strvec` gathering phase followed by JSON formatting via `trace2_data_json()`.

Patch 4 completes the unification by modifying the Windows implementation to emit ancestry data through both the original JSON format (for compatibility) and the newer `trace2_cmd_ancestry()` interface. The final version includes a minor documentation correction to the commit message format, noting that commit references should use `git show -s --pretty=reference` (optionally with `--abbrev=8`) to include dates and standardize formatting.

The changes are confined to platform-specific compatibility code and build files, with careful attention to cross-platform consistency through incremental refactoring and format unification. The series achieves consistent tracing while preserving backward compatibility, with no technical objections raised in the thread. The implementation details include:
- macOS process info collection via `sysctl` with cycle detection
- Windows implementation split into data collection and JSON formatting phases
- Dual-emission strategy in Windows for backward compatibility
- Consistent 10-level depth limit across platforms
- Build system integration for all major build tools (Make, CMake, Meson)