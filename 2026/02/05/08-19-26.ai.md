This is a feature patch in the `git-history` series, now at version 11 and awaiting final merge. The email from Patrick Steinhardt responds to Junio Hamano's summary of the ongoing discussion about the command's default behavior and documentation needs.

The key points in this update are:

1. Patrick confirms the `git-history` command is intentionally different from `git-rebase`, designed to address use cases that rebase doesn't handle while providing a more opinionated interface for less advanced users. He's open to expanding documentation about these differences.

2. Regarding the debate over whether to rewrite all descendant branches by default, Patrick indicates willingness to add a `history.scope` configuration option (as suggested by Elijah Newren) that would let users restrict operations to just the current branch. The infrastructure for this already exists in the code.

3. Patrick argues the series is ready for merging despite some planned follow-up work, noting that:
   - The current `reword` subcommand cannot produce conflicts
   - The experimental marker allows for future design tweaks
   - Additional subcommands and conflict handling will come later
   - The blocking dependency is on Phillip Wood's unrelated `git replay` changes

The thread appears to be in its final stages, with Junio waiting only for SZEDER Gábor's response to Elijah's points before proceeding. The maintainer has effectively accepted the all-branches rewrite default based on reflog recovery characteristics, with any remaining concerns about safety defaults addressable via the proposed configuration option.