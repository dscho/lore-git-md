This is a discussion about the proposed `git config-batch` feature, specifically questioning whether the functionality could be achieved through existing interfaces rather than introducing a new builtin. Brian m. carlson raises concerns about the necessity of the new command, pointing out that Git LFS has similar needs but meets them by parsing output from `git config -l -z`. They suggest extending existing config interfaces if the current ones are sufficient but need minor enhancements.

The email questions whether the proposed batch processing is truly needed for read operations, given that bulk reading is already possible with `git config -l`. For write operations, they express curiosity about the use case requiring many config modifications, implying skepticism about whether the complexity of a new protocol is justified. This represents a pushback against the RFC's direction, favoring simpler solutions that build on established commands rather than introducing new ones.

The discussion continues to explore the tradeoffs between new interfaces versus extending existing ones, with Brian m. carlson advocating for the latter approach where possible. Their perspective reflects a preference for minimal API surface area and reuse of proven mechanisms, particularly when the performance benefits of a new command are not clearly demonstrated for all proposed use cases.