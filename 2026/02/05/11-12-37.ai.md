This is a discussion thread about the potential deprecation of the legacy `git whatchanged` command. Kristoffer Haugsbakk responds to Sam Berney's objections by explaining that `git whatchanged` is redundant with `git log -p` (they share the same underlying machinery) and provides specific migration instructions showing how to replicate `whatchanged` behavior using `git log` options.

The response notes that Git lacks telemetry to measure command usage, making mailing list feedback the primary signal of user needs. While Sam Berney has expressed strong personal attachment to the command, no technical justification for keeping it has been presented beyond user preference. The thread currently lacks input from maintainers about whether an active deprecation effort exists or is being considered.

Key technical details include the behavioral differences between the commands (`whatchanged` defaults to `--no-merges --raw` output) and the fact that both commands share the same implementation backend. The discussion remains at the user sentiment level rather than addressing concrete maintenance burdens or compatibility concerns.