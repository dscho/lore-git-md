This is a CI / build system discussion about platform-specific test failures uncovered by expanded contrib/ testing.

The thread has identified that some `git subtree` test failures in the reftable CI configuration are actually caused by a bug in Ubuntu's `dirname` utility (fixed upstream in January 2025 but not yet widely deployed). Junio notes this is reminiscent of a previous issue with `sudo-rs` that required a workaround, and wonders if a similar `/etc/alternatives` approach could help here.

The discussion continues the challenges of maintaining contrib/ scripts that rely on platform tools, particularly as CI expands to test more configurations. This follows earlier patches that:
- Fixed `git subtree` validation logic
- Added reftable compatibility for subtree tests
- Adjusted diff-highlight tests for branch naming

The exchange highlights how expanded CI testing is revealing both genuine Git issues and platform dependencies that were previously hidden. Junio's response suggests he's considering whether to implement another workaround while waiting for the Ubuntu fix to propagate.