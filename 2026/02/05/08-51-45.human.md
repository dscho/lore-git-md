This is a follow-up to a bugfix series that aligns `git add` behavior with `git status` and `git diff` for submodules configured with `ignore=all`. The series has gone through multiple iterations and is now ready for integration.

The key change makes `git add` skip submodules marked with `ignore=all` unless explicitly overridden with `--force`. This matches how other commands handle ignored submodules and prevents accidental staging of submodule changes when they're meant to be ignored. The implementation includes comprehensive tests and documentation updates.

The thread shows the typical Git development process - initial proposal, design discussion (including debate over whether to use `--force` or a new flag), implementation refinements, and final polish. The v3 series addresses all feedback, with clean commit messages and thorough test coverage. This looks like a well-executed change that improves consistency in submodule handling.