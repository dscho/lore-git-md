This is a const-correctness patch series addressing compiler warnings from newer versions of glibc (2.43) in C23 mode. The changes are mechanical additions of `const` qualifiers to pointers that receive results from string search functions like `strrchr()` and `memchr()`, where the pointed-to data isn't modified.

The patch touches 28 files but makes no behavior changes - it purely updates type declarations for better const-correctness. Junio Hamano has reviewed and approved the changes, noting they look good aside from one minor stylistic point about NULL initialization removal in gpg-interface.c that Jeff King (Peff) suggested might be better split into a separate patch.

This is part of Git's ongoing effort to maintain compatibility with modern C standards and compiler versions. The changes follow a pattern Peff has used in similar const-correctness work before. While the diffstat looks large due to the number of files touched, each change is small and straightforward. The patch appears ready to merge after addressing the one minor organization suggestion.