Security issue in `git am` where it applies diffs from commit messages, triggered by Matthias Beyer's report of a real-world incident where a `sleep(1)` call was accidentally introduced into a Debian package (i3 window manager) via a diff in a commit message. The current behavior presents a security risk as malicious changes could be hidden in commit messages. Jacob Keller initially acknowledged the surprising behavior but questioned whether it constitutes an attack vector, suggesting users should review commit messages before applying and noting that proper use of scissors markers ("-- >8 --") or indenting diffs could prevent accidental application.

Matthias Beyer strongly argued the current behavior is clearly unintentional and could have serious security consequences beyond the benign `sleep(1)` example, warning this could be exploited to sneak malicious changes into codebases by hiding them in commit message diffs. He proposed modifying `git am` to search for the patch delimiter ("---\n") from the end of the email rather than the beginning, which would prevent it from applying diffs embedded in commit messages.

Kristoffer Haugsbakk added to the discussion by pointing to a related thread about the magic string that `git format-patch` uses, reinforcing Jacob Keller's earlier suggestion that indenting code blocks could prevent this issue. He referenced Junio Hamano's comment that indentation is "just stylistic until it isn't," highlighting how style preferences can have security implications.

The discussion continues to explore the tension between backward compatibility and security, with key participants including Matthias Beyer (reporting the issue), Jacob Keller (initial response), Kristoffer Haugsbakk (additional context), and Jeff King (Peff) as a likely reviewer for any fix. The real-world impact (Debian package incident) gives this issue practical urgency. Technical constraints around fixing the behavior while maintaining compatibility are recognized as a key challenge, with the proposed solutions (end-of-email delimiter search, indentation conventions) requiring careful evaluation for edge cases and compatibility implications. The thread remains unresolved but is actively exploring technical solutions that would prevent `git am` from applying unintended diffs while maintaining compatibility with existing workflows.