This is the second patch in a two-part series adding sparse-index support to the `merge-ours` strategy, part of the ongoing sparse-checkout/sparse-index optimization effort. The patch follows a clean separation where the first patch did preparatory refactoring (removing `the_repository` usage) and this one adds the actual sparse-index functionality.

The change makes `git merge -s ours` properly respect sparse-checkout settings by initializing the sparse-checkout globals via `repo_config()` and marking the command as not requiring a full index. The commit includes a clear explanation of why this was needed - previously the command would unnecessarily expand the index because the relevant config wasn't being loaded. The patch adds thorough tests in t1092 that verify identical behavior across full-checkout, sparse-checkout, and sparse-index modes, plus confirmation that the index isn't expanded.

The changes are small (6 lines of implementation, 14 lines of tests) but meaningful, following established patterns for sparse-index integration. The patch appears well-crafted with a good commit message explaining both the problem and solution. This is a straightforward improvement that should integrate cleanly with the broader sparse-index work.