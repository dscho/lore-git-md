This thread introduces a two-patch series that integrates the `merge-ours` strategy with Git's sparse-index functionality, part of the ongoing sparse-checkout optimization effort. The series comes from [author name TBD] and addresses an oversight where `merge-ours` wasn't properly respecting sparse-checkout settings, causing unnecessary index expansion.

The first patch is a refactoring that removes usage of the `the_repository` global variable from `builtin/merge-ours.c`, following René Scharfe's ongoing effort to eliminate this pattern. It updates three function calls to use a passed repository parameter instead. The second patch, now more detailed in this update, adds proper sparse-index support by initializing sparse-checkout globals via `repo_config()` and explicitly opting out of full index requirements.

The changes are relatively small (9 lines in patch 1, 6 lines in patch 2) but strategically important for sparse-checkout performance. The series includes 14 lines of new tests in t1092 that verify consistent behavior across full-checkout, sparse-checkout, and sparse-index modes while confirming the sparse index isn't expanded during operations. The changes follow established patterns seen in other commands' sparse-index integrations and appear well-contained with clear test coverage.

As this is the initial submission, there haven't been any reviews yet. The work builds directly on two major ongoing efforts: the sparse-checkout/sparse-index optimizations led by Derrick Stolee and Elijah Newren, and the broader `the_repository` elimination project championed by René Scharfe. The changes align well with these established directions in the codebase.