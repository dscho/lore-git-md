This is the fourth patch in a v4 series that aligns `git add` behavior with `git status` and `git diff` when handling submodules marked with `ignore=all`. The patch updates existing tests to account for the new requirement that `--force` must be used when adding ignored submodules.

The changes are mechanical updates to test scripts, modifying `git add` calls to include `--force` where they interact with ignored submodules. The patch touches:

1. `t/lib-submodule-update.sh` - updates three test cases that add submodules to use `--force`
2. `t/t7508-status.sh` - updates one test case checking commit dry-run behavior with ignored submodules

This is part of the larger effort to make submodule handling consistent across Git commands. The changes here are straightforward test adaptations to match the new behavior introduced in earlier patches of the series. The patch maintains all existing test assertions while just adding the necessary `--force` flags.

The series appears technically sound and well-tested, with this patch completing the test coverage needed for the new behavior. It follows the established pattern of the series by being focused and clearly scoped to test updates.