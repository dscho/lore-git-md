This is a sparse-index integration patch, part of the ongoing effort to optimize Git commands for sparse-checkout scenarios. The patch enables the `merge-ours` strategy (used by `git merge -s ours`) to work efficiently with sparse indexes by preventing unnecessary index expansion.

Junio C Hamano provides review feedback on the commit message, suggesting a clearer structure that first describes the current behavior, identifies the problem, then explains the solution. While the technical changes themselves look correct (adding config reading and disabling full-index requirement), the explanation could better highlight that:

1. The command wasn't reading config at all before, which meant sparse-checkout settings weren't be respected
2. The diff machinery was already sparse-aware, but the command wasn't properly opting into that behavior
3. The changes follow the same pattern used for other commands' sparse-index integration

The patch includes new tests in t1092 that verify identical behavior across full/sparse modes and confirm no index expansion occurs. This appears to be a straightforward improvement following established patterns from Derrick Stolee and Elijah Newren's sparse-index work. The changes are small (6 lines) and focused, with good test coverage (14 lines).