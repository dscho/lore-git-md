This is a discussion about the security implications of `git am`'s patch parsing behavior, specifically regarding how it handles diffs embedded in commit messages. Florian Weimer is questioning Jeff King's earlier point about the flexibility of `git am`'s input format, noting that in practice he's found `git am` to be quite rigid - often failing to apply patches that were generated by `git format-patch` and requiring fallback to plain `patch`.

The exchange highlights a tension between Git's historical design (which aimed to handle arbitrary email patches) and real-world usage patterns (where most patches come from Git itself). Florian's experience suggests that `git am` may already be as flexible in practice as Jeff's security-focused solution would make it, implying that stricter parsing might not break as many workflows as feared.

This is part of an ongoing thread about securing `git am` against malicious diffs hidden in commit messages, with participants weighing backward compatibility against security. The discussion remains technical and constructive, with Florian providing valuable user-perspective data about actual `git am` behavior in the wild.