This is a feature patch series (v4) that aligns `git add` behavior with `git status` and `git diff` when handling submodules configured with `ignore=all`. The key change makes `git add` skip such submodules by default, requiring `--force` when explicitly adding them.

The series has evolved through multiple iterations (now at v4) with all technical aspects complete - implementation, tests, and documentation are all in place. The main changes in this version are documentation wording improvements and expanded commit message explanations of the workflow benefits, particularly for branch tracking in submodules.

The implementation introduces a new `skip_submodule()` helper that handles path verification, config checking, and `--force` flag validation. Test coverage is thorough with 8 new test cases in `t2206-add-submodule-ignored.sh` plus updates to four existing test files. Documentation updates precisely specify the new `--force` requirement in three files.

The change enables better workflows where submodules can track branches without forcing SHA-1 updates into the superproject, reducing friction for users who want looser coupling. The series appears ready for merging once version numbering confusion is resolved (Junio had expected v3 but got v4) and any remaining review comments from Phillip Wood are addressed.