This is a discussion about submodule behavior, specifically how `git add -A` interacts with submodules` ignore = all` settings. The thread begins with Oliver Kopp reporting that while `git status` correctly ignores submodules marked with `ignore = all`, `git add -A` still stages changes in those submodules. He argues this is inconsistent behavior since the documentation states `ignore = all` only affects `git status` and diff operations, not `git add`. 

Claus Schneider replies by pointing to a GitHub pull request (#1987) that may address this issue, though the PR details aren't included in this email. The core tension here is between the current documented behavior (where `git add -A` explicitly operates on "all files in the entire working tree") and the user expectation that ignored submodules should be consistently handled across all Git commands.

This appears to be a long-standing pain point, as evidenced by the referenced 2021 StackOverflow question. The discussion touches on submodule configuration semantics and whether the current behavior constitutes a bug or merely an undocumented limitation. The proposed solution would require changing how `git add` interprets submodule ignore settings, which could have implications for existing workflows that rely on the current behavior.