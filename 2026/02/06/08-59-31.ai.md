Security-focused follow-up in the `git am` diff application thread. Florian Weimer (Red Hat) responds to Jacob Keller's concerns about backward compatibility in fixing the issue where `git am` can apply unintended diffs from commit messages.

Weimer argues the format is already unambiguous - diffs appear before the commit message in `git format-patch` output, with statistics separating them. The parsing challenge comes from needing to process the email in reverse to properly distinguish diffs from message content. He notes similar issues have occurred in `git rebase` with malformed "From " lines in commit messages, suggesting this class of problem has precedent.

The email reinforces the technical feasibility of a fix while acknowledging the one-pass parsing constraint. Weimer brings additional context about related tool behavior (`git rebase`'s past issues with commit message parsing) that could inform the solution design. This moves the discussion forward by providing a parsing strategy (reverse processing) and historical context, while the core security concern remains unaddressed - the thread still needs a concrete proposal for how `git am` should change to prevent applying unintended diffs while maintaining compatibility.