Security vulnerability follow-up in `git am` patch parsing. Jakob Haufe responds to Kristoffer Haugsbakk's suggestion about using indentation for code blocks, pointing out that this approach doesn't fully address the issue. He cites the `patch(1)` man page to show that even with indentation handling, the fundamental problem persists - diffs in commit messages can still be misinterpreted as actual patches when passed through standard Unix tools.

The email highlights that any fix in `git am` would only be a solution for Git's own toolchain, as the same problematic diffs could still cause issues when processed by the traditional `patch` command. This reinforces the thread's emerging consensus that the security vulnerability stems from deeper email/diff format ambiguities that span multiple tools in the Unix ecosystem.

The discussion continues to balance security needs against backward compatibility, with this contribution emphasizing that solutions must consider not just Git's behavior but also how patches flow through the broader toolchain. The reference to `patch(1)`'s intentional handling of indented diffs suggests the problem may require coordination beyond just Git's codebase.