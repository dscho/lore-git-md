This is a discussion about submodule behavior, specifically how `git add -A` interacts with submodules marked with `ignore = all` in `.gitmodules`. 

Oliver Kopp reported that while `git status` correctly ignores submodules with `ignore = all`, `git add -A` still stages changes from these submodules. He argues this is inconsistent behavior, since users would expect `git add -A` to match what `git status` shows. The thread includes a link to a GitHub PR (#1987) that may address this, though the email doesn't describe the proposed solution.

The issue touches on a subtle point of Git's submodule handling where different commands interpret the `ignore` setting differently. While this appears to be a genuine usability concern, the discussion is still early - Claus Schneider's reply simply points to a potential fix without analyzing it. The thread references a StackOverflow question indicating this has confused other users as well. 

The core question is whether `git add -A` should align with `git status`'s interpretation of ignored submodules, which would require changing long-standing behavior. This seems like a reasonable change, but would need careful consideration of backward compatibility.