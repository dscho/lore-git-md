This thread addresses a security vulnerability in `git am` where embedded diffs in commit messages can be accidentally applied as patches, stemming from a real-world incident in Debian where `sleep(1)` was unintentionally introduced. The core challenge lies in distinguishing intended patches from commit message content, particularly around separator lines ("---") and unindented diffs.

Phillip Wood has implemented a security-focused enhancement to the sample `commit-msg` hook that detects problematic patterns while maintaining backward compatibility. Kristoffer Haugsbakk contributed documentation warnings about these hazards, which Junio has now queued for inclusion. The solution combines detection of diffs above the scissors line, configurable comment characters, and multiple bypass mechanisms while preserving Signed-off-by verification.

In the latest exchange, Junio responds to Haugsbakk's suggestion for an environment variable escape hatch, expressing concern that while the current hook's strictness improves security, it might interfere with legitimate uses of separator lines in email-based workflows. He emphasizes that the hook is just a sample implementation that projects can customize, suggesting he prefers keeping the default simple rather than accommodating all edge cases. This highlights the ongoing tension between security hardening and workflow flexibility, with Junio advocating for local customization over complex default implementations.

The discussion has progressed from identifying the vulnerability to implementing practical mitigations while documenting risks, now focusing on how to balance security with real-world usage patterns. With the documentation patch queued, attention turns to whether additional escape mechanisms are warranted or if project-specific hook customizations should handle specialized cases.