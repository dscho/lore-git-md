This is the final documentation patch in a series that aligns `git add` behavior with `git status` and `git diff` for submodules with `ignore=all` configuration. The patch updates three documentation files to reflect that submodules marked with `ignore=all` now require explicit `--force` when staging updates.

The changes clarify that while `ignore=all` still suppresses modification detection by default, users can override this with `git add --force <submodule.path>`. The documentation matches the implementation from earlier patches in the series, which added plumbing support and test coverage for this behavior.

Key documentation updates include:
- Explicit mention of `--force` requirement in the submodule config description
- Clearer phrasing about when submodules appear in status output
- Added workflow context about overriding ignore rules
- Consistent cross-references between the three documentation files

The patch follows through on the series' goal of making submodule handling more consistent across Git commands while preserving the ability to explicitly stage submodule changes when needed. With this documentation update, the series appears complete and ready for merging once final administrative points (version numbering confirmation, review feedback acknowledgment) are resolved.