This is a two-patch series integrating the `merge-ours` strategy with sparse-index functionality, part of the ongoing sparse-checkout/sparse-index effort. The series is now at v2, having incorporated maintainer feedback on commit message improvements.

The first patch removes the `the_repository` global from `builtin/merge-ours.c`, following the project's pattern of eliminating repository globals. The commit message now explicitly notes that `RUN_SETUP` guarantees the repository parameter is never NULL, addressing maintainer feedback from Junio and Patrick Steinhardt.

The second patch adds sparse-index support by initializing configuration (needed for sparse-checkout settings) and setting `command_requires_full_index = 0`. This prevents unnecessary index expansion when using `git merge -s ours` in sparse-checkout contexts. The commit message has been restructured to better follow project conventions, explaining both the problem (index was happening unnecessarily) and solution (config initialization + flag setting).

The series includes new tests in t1092 that verify identical behavior across full/sparse modes and confirm no index expansion occurs. The changes follow established patterns from both the `the_repository` removal effort (René Scharfe) and sparse-index work (Derrick Stolee/Elijah Newren). While developed with AI assistance, the implementation aligns with project conventions and has received positive technical review.

With the commit message improvements now incorporated, this appears ready for inclusion in the next integration cycle. The changes are small (15 lines modified in implementation, 14 lines of new tests) but meaningful for sparse-checkout users of the merge-ours strategy.