Security issue in `git am` where it applies diffs from commit messages. This is a follow-up in an ongoing thread about a real-world incident where a `sleep(1)` call was accidentally introduced into a Debian package via a diff in a commit message.

Kristoffer Haugsbakk adds to the discussion by pointing to a related thread about the magic string that `git format-patch` uses, reinforcing Jacob Keller's earlier suggestion that indenting code blocks could prevent this issue. He references Junio Hamano's comment that indentation is "just stylistic until it isn't," highlighting how what might seem like a style preference can have security implications.

The thread continues to explore the tension between backward compatibility and security, with this email providing additional context about existing mechanisms (like `git format-patch`'s magic string) that could inform potential solutions. The discussion remains focused on finding a technical solution that would prevent `git am` from applying unintended diffs while maintaining compatibility with existing workflows.