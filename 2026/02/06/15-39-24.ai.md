This is a follow-up to the ongoing Meson build system integration effort, specifically addressing a Windows-specific toolchain issue that surfaced in CI testing. Patrick Steinhardt confirms that the symlink-based approach for gitk/git-gui subprojects works on Windows, but reveals a missing `msgfmt` (gettext) tool dependency in the GitHub Actions environment.

The issue stems from the Meson build requiring `msgfmt` but not properly marking it as optional, unlike the Makefile's behavior. Patrick has already fixed this upstream in the gitk repository (j6t/gitk#37) by adding `required: false` to the `find_program()` call, matching the Makefile's optional handling. The fix has been merged, and Patrick is now seeking guidance on how to incorporate this change into Git's main repository - either by sending a new patch version or letting Junio handle the subtree update.

This email demonstrates the practical challenges of cross-platform build system modernization, particularly around optional toolchain dependencies. The thread shows good progress on Windows compatibility, with the symlink approach now validated and the msgfmt issue resolved in a way that maintains consistency with existing build behavior. Patrick's proactive upstream fix and CI verification reflect the careful attention to detail needed for such foundational changes.