Thread identity: Two-patch series integrating `merge-ours` strategy with sparse-index functionality, part of sparse-checkout/sparse-index effort.

Core problem: The `merge-ours` strategy (used by `git merge -s ours`) wasn't properly reading sparse-checkout settings, causing unnecessary index expansion. The series fixes this by adding proper sparse-index support.

Current status: Initial version (v1) of the series. First patch removes `the_repository` global usage as preparatory cleanup. Second patch adds sparse-index support by initializing sparse-checkout globals via `repo_config()`.

Key technical details: Changes are focused in builtin/merge.c (15 lines modified) and t/t1092-sparse-checkout-compatibility.sh (14 lines of new tests). Uses `repo_config()` to properly initialize sparse-checkout globals. Series follows common pattern of separating refactoring (patch 1) from functional changes (patch 2).

Participant positions: No reviews yet - this is the initial submission. Author notes use of AI assistance in development but no quality concerns are apparent from the changes.

Related work: Part of broader sparse-checkout/sparse-index effort (historically involving Derrick Stolee, Elijah Newren, and others). Also touches on `the_repository` removal effort (René Scharfe is primary driver).