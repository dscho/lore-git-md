This is a two-patch series integrating the `merge-ours` strategy with Git's sparse-index functionality, part of the ongoing sparse-checkout/sparse-index effort. The first patch is a refactoring that removes reliance on the `the_repository` global variable, continuing the broader `the_repository` removal effort. The second patch fixes an issue where `merge-ours` (used by `git merge -s ours`) would unnecessarily expand the sparse index because it wasn't reading the sparse-checkout configuration. 

The changes are straightforward: the first patch mechanically converts function signatures to take an explicit repository parameter, while the second adds the missing config reading and includes tests verifying sparse-index compatibility. The series appears well-contained, touching only the merge-ours builtin and adding relevant test coverage. 

The use of AI assistance is noted but doesn't raise any obvious red flags - the changes align with established patterns in the codebase. This looks like a solid incremental improvement to sparse-index support.