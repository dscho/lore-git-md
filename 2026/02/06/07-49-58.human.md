Bugfix for bisect ref handling in Git's internal API usage. This patch is part of Patrick Steinhardt's series addressing misuse of `refs_for_each_ref_in()` across the codebase, specifically focusing on the bisect functionality here.

The change fixes two issues in `bisect.c`'s ref iteration: First, it properly adds a trailing slash to the "refs/bisect" prefix when searching for refs, which is required by Git's ref hierarchy matching conventions. Second, it simplifies the code by switching from `refs_for_each_ref_in()` (which strips prefixes) to `refs_for_each_fullref_in()` (which doesn't), eliminating an unnecessary string reconstruction step.

This is a focused technical correction that aligns with the thread's broader goal of consistent ref handling. The patch has already gone through multiple iterations with positive feedback from reviewers like Taylor Blau and Jeff King, and appears ready for inclusion. While small, it's part of an important series fixing subtle but potentially serious bugs in ref iteration patterns.