This patch fixes a misuse of `refs_for_each_ref_in()` in the bisect code that could lead to incorrect ref handling. It's part of Patrick Steinhardt's series addressing ref iteration issues that affect both bitmap and bisect functionality.

The key change switches from `refs_for_each_ref_in()` to `refs_for_each_fullref_in()` when cleaning up bisect refs, ensuring proper handling of refs under "refs/bisect/". The original code would incorrectly strip the prefix and then re-add it, while the new version avoids this unnecessary manipulation. The patch also adds the required trailing slash to the ref pattern, maintaining consistency with Git's ref matching conventions.

This change aligns with the series' broader goal of fixing exact ref matching issues across Git's codebase. The fix is straightforward and well-contained, touching only the bisect cleanup logic while maintaining the same functionality. The patch builds on the thread's established consensus around using trailing slashes for ref hierarchy matching.