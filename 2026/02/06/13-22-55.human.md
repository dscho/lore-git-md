This is the fourth version of a patch series that aligns `git add`'s behavior with `git status` and `git diff` when handling submodules configured with `ignore=all`. The change makes `git add` skip such submodules by default, requiring `--force` to explicitly add them.

The series has evolved through multiple iterations with feedback incorporated, including:
- Using a `--force` flag (originally considered `--include-ignored-submodules`)
- Adding comprehensive test coverage in a new test file
- Updating documentation across three files
- Addressing review comments about commit message clarity and grammar

Key technical changes in this version:
- Improved commit message in patch 2 to better explain the branch tracking workflow motivation
- Documentation wording refinements (e.g., "Set to" -> "When set to" for consistency)
- Maintains all previous technical implementation including the `skip_submodule()` helper and path verification

The series appears ready for merging, having addressed all substantive feedback from previous rounds. The only outstanding items are administrative - confirming this is indeed v4 (there was some version confusion earlier) and ensuring Phillip Wood's earlier review comments were properly addressed. The implementation provides a cleaner workflow for submodule branch tracking while preventing accidental staging of submodule updates.