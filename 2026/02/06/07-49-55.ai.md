This is the third version of Patrick Steinhardt's patch series fixing a bug in `refs_for_each_ref_in()` usage that affects bitmap and bisect functionality when processing exact branch names. The series has evolved through discussion to align with Git's ref matching conventions.

The key change in v3 is switching to use ref hierarchy matches (requiring trailing slashes) for `pack.preferBitmapTips` configuration, consistent with recent changes to ref exclusion patterns (10e8a9352b). This resolves the earlier debate about whether to allow exact ref matches, now following the established pattern matching approach.

The series:
1. Deduplicates bitmap tip iteration logic between pack-objects and midx
2. Fixes the bitmap exact ref match bug by using hierarchy matching
3. Fixes a similar misuse in bisect code
4. Simplifies string_list memory handling in bisect (Jeff King's patch)

Notable technical changes include:
- Added trailing slash handling in `for_each_preferred_bitmap_tip()`
- Comprehensive tests demonstrating the new hierarchy matching behavior
- Documentation updates clarifying the pattern matching semantics
- Bisect fix using `refs_for_each_fullref_in()` with "refs/bisect/"

The thread discussion has largely converged, with only minor string_list memory management details remaining open. The series represents a clean solution that maintains consistency with Git's ref matching conventions while fixing the original BUG() condition. Patrick has been responsive to feedback throughout, and the changes have received approval from key reviewers including Taylor Blau and Junio Hamano.