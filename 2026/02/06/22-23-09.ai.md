This is a follow-up in the ongoing Meson build system integration thread, specifically addressing the resolution of Windows build issues with `gitk`. 

Patrick Steinhardt confirms that the upstream fix for the `msgfmt` toolchain issue has been merged into the `gitk` repository (j6t/gitk#37) and seeks guidance on how to integrate this into Git's main repository. The fix makes `msgfmt` optional in the Meson build, matching the Makefile's behavior, and has been verified to work in Windows CI environments.

Junio Hamano clarifies the integration path: he will pull the updated `gitk` subtree directly from Johannes Sixt (j6t) once it's ready, rather than requiring a new patch version from Patrick's series. This confirms that no changes are needed to the "[PATCH v2] meson: wire up gitk and git-gui" patch itself - the Windows compatibility issue is being handled through the subtree update mechanism.

The thread shows the core architectural questions around the symlink-based Meson integration are settled, with only final integration mechanics remaining.
```