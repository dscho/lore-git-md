Security vulnerability in `git am`'s patch parsing, with discussion of historical context and real-world usability. This email from Jeff King (Peff) responds to Florian Weimer's concerns about `git am`'s flexibility in handling various patch formats. Peff demonstrates that `git am` can indeed process both traditional GNU diff output and MIME-attached patches, explaining this was important in Git's early days when patches came from diverse sources. He acknowledges that while such formats are rare today, the design accommodates them, which complicates fixing the security issue where `git am` can misapply diffs embedded in commit messages.

The thread has established that the vulnerability stems from `git am`'s linear parsing approach, which cannot reliably distinguish between intended patch content and embedded diffs in commit messages. Peff's examples show the tool's historical flexibility - it accepts both inline GNU diff output and MIME attachments - but this very flexibility now makes it harder to secure without breaking backward compatibility. The email provides concrete examples of formats `git am` handles, illustrating why simple solutions like always requiring MIME attachments or strict diffstat formatting won't work for all cases.

Key technical details mentioned include `git am`'s ability to parse both traditional `diff -Nru` output and MIME multipart emails with attached patches, showing the range of formats it was designed to handle. The discussion highlights the tension between maintaining this historical flexibility and addressing the security vulnerability, with no solution yet found that wouldn't break some legitimate use cases. Participants are grappling with how to preserve `git am`'s original design goals while making it secure against maliciously crafted patches.