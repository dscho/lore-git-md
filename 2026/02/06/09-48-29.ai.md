This is a discussion about the security implications of `git am`'s patch parsing behavior, specifically regarding its handling of diffs embedded in commit messages. Florian Weimer is questioning the practical flexibility of `git am`'s format, noting that in his experience it often fails to apply patches that were created with `git format-patch`, forcing him to fall back to the traditional `patch` command.

The email highlights a tension between `git am`'s design goals (to handle a wide variety of patch formats) and its actual behavior in practice. Weimer's experience suggests that despite being theoretically flexible, `git am` in reality seems quite particular about its input format. This observation adds nuance to the ongoing discussion about how to improve `git am`'s security while maintaining compatibility with real-world usage patterns.

The thread has established that the current behavior poses a security risk, as malicious changes could be hidden in commit message diffs. Previous emails have explored various technical solutions, but Weimer's contribution emphasizes that any fix must account for the practical realities of how `git am` is actually used, not just its theoretical capabilities. His comment about frequently needing to use `patch` instead suggests that `git am`'s strictness may already be causing user frustration independent of the security concerns.