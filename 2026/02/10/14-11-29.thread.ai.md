RFC proposal for `git repo info` to expose repository paths as structured alternative to `git rev-parse`, with Pushkar Singh and Patrick Steinhardt collaborating on design. The series adds `path.git-dir` and `path.common-dir` fields to structured output, building on Lucas Skeggs' `--format`/`--keys` work for `repo_info_fields[]`. Implementation uses existing repository path APIs (`repo_get_git_dir()`, `repo_get_common_dir()`) while avoiding invocation-dependent values, keeping scope focused on repository-derived metadata.

Patrick Steinhardt supports moving repository introspection from `git rev-parse` to `git repo` long-term, noting `rev-parse` has outgrown its purpose. The series integrates with `--path-format` for consistent path representation while deferring broader path resolution semantics discussions. Each patch adds one field with tests following established pattern: latest adds `path.common-dir` via `get_path_common_dir()` using `repo_get_common_dir()`, mirroring `rev-parse --common-dir` but in machine-readable format. Implementation is mechanically straightforward (16 lines in `builtin/repo.c`) and builds on Skeggs' prior work while maintaining narrow technical scope.

Thread maintains consensus on making `git repo` canonical home for this functionality, with these additions seen as uncontroversial steps. Patches build clear precedent from existing `rev-parse` behavior while using established path APIs, suggesting smooth review progression. Open questions about path formatting (absolute vs relative) remain deferred to parallel discussions. Each addition follows same focused pattern: one field with tests, avoiding scope creep into unresolved path semantics.