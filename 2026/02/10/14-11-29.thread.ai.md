RFC proposal for `git repo info` to expose repository paths as structured alternative to `git rev-parse`, with Pushkar Singh and Patrick Steinhardt collaborating on design. The latest patch series adds `path.git-dir` and `path.common-dir` fields to the structured output format, building on Lucas Skeggs' earlier `--format`/`--keys` work for `repo_info_fields[]`. The implementation uses existing repository path APIs (`repo_get_git_dir()` etc.) while avoiding invocation-dependent values, keeping scope focused on repository-derived metadata.

Patrick Steinhardt supports moving repository introspection from `git rev-parse` to `git repo` long-term, noting `rev-parse` has outgrown its original purpose. The current series integrates with the new `--path-format` flag for consistent path representation while maintaining narrow technical scope - each patch adds one field with corresponding tests, deliberately avoiding broader questions about path resolution semantics that are being discussed in parallel. The latest patch adds structured output for the Git directory path via `path.git-dir` field, using `repo_get_git_dir()` and following the established `repo_info_field[]` pattern. Open questions remain about path formatting (absolute vs relative), but the implementation defers to those ongoing discussions while providing the mechanical infrastructure.

The thread maintains consensus on making `git repo` the canonical home for this functionality, with these mechanical additions seen as uncontroversial steps toward that goal. Lucas Skeggs' prior work on configurable path resolution is acknowledged but not directly incorporated in this iteration. The patches build clear precedent from existing `rev-parse` behavior while using established path APIs, suggesting they will progress smoothly through review. Each addition follows the same pattern: one focused field with tests, avoiding scope creep into unresolved path semantics questions.