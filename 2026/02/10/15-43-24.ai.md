Junio C Hamano (the Git maintainer) concurs with Patrick Steinhardt's position in this ongoing discussion about handling `iconv(1)` in Git's test suite. The exchange resolves one of the open questions from the thread - whether to modify test expectations when the `iconv` binary is unavailable rather than skipping tests entirely.

Junio agrees with Patrick's reasoning that even if the `iconv` binary were restored in Git for Windows (as Torsten Bögershausen suggested), it remains sensible to handle cases where the binary might be missing on other platforms or when Git is built with `NO_ICONV`. This aligns with Patrick's patch series approach of conditionally modifying test expectations based on ICONV availability rather than skipping tests.

The maintainer's acknowledgment suggests the technical direction is sound, though the broader discussion about test strategy (skip vs modify) and platform-specific packaging decisions remains open. This moves the series closer to potential acceptance while leaving room for further refinement of the implementation details.