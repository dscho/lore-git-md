Updated AI thread summary:

Thread identity: Bug report about `git fast-export` silently dropping GPG signatures from commits during export, reported by Matthias Urlichs.

Core problem: `git fast-export` fails to include GPG signature blocks from commit objects in its output by default, despite preserving all other commit data. While there is an existing `--signed-commits` option, its default behavior differs from the analogous `--signed-tags=verbatim` option for tags, creating an interface inconsistency. This affects tools like `git filter-repo` that rely on `fast-export` for repository rewriting.

Key technical details: Commit signatures appear in `git cat-file` output but are omitted from `fast-export` unless explicitly requested. The `--signed-commits` option exists but defaults differently from `--signed-tags`. Tag signature handling works as expected with `--signed-tags=verbatim`, suggesting the commit signature behavior could be similarly configurable.

Current status: Matthias Urlichs identified that the existing `--signed-commits` option could be improved to default to matching `--signed-tags` behavior unless overridden. This would make signature handling more consistent between commits and tags. No patch has been proposed yet to implement this change.

Open questions: Should the default behavior of `--signed-commits` be changed to match `--signed-tags`? Would this require a deprecation period for existing users relying on current behavior? The underlying signature-dropping behavior remains unaddressed for users not specifying either option.

Participant positions: Matthias Urlichs (reporter) suggests making the interface more consistent by aligning default behaviors between commit and tag signature handling options.