This is an RFC proposal about exposing repository path information through `git repo info` rather than requiring scripts to use `git rev-parse`. The author, Pushkar Singh, notes that while `git repo info` currently reports repository properties like layout and formats, it doesn't expose paths that scripts commonly need (like the git directory, common directory, or hook paths).

The proposal suggests starting with adding `git-dir` to `git repo info` as a more discoverable alternative to `git rev-parse --git-dir`, with the eventual goal of providing structured access to various repository paths. The author is specifically asking for feedback on whether this direction makes sense and how paths should be reported (absolute vs relative).

This appears to be an early-stage proposal focused on improving discoverability and usability of repository metadata. The author is appropriately seeking guidance before implementing anything, suggesting they're looking to align with project conventions. The change would affect scripting use cases more than end users directly. The thread context isn't provided, so we don't know if there's prior discussion about `git repo info`'s intended scope.