This is a build system patch that simplifies the macOS credential helper's Makefile by moving its build targets to the top-level Makefile. Junio C Hamano provides a quick review of Koji Nakamaru's patch, which had already been summarized in the thread as addressing recent build issues with `git-credential-osxkeychain`.

The patch centralizes the build logic while maintaining the required macOS framework linkages. Junio suggests two minor improvements: using `$(MAKE)` instead of direct `make` invocations and a slightly cleaner directory traversal syntax. The changes are straightforward and uncontroversial - this appears to be routine build system maintenance to keep the macOS credential helper building properly after recent Makefile changes.