This is a follow-up discussion about a CI/build system patch series that encountered a GitHub Actions syntax limitation. Jeff King (Peff) responds to Junio Hamano's discovery that the proposed change to `.github/workflows/main.yml` uses unsupported arithmetic in GitHub Actions expressions.

The issue stems from patch 3/5 in Patrick Steinhardt's series, which aimed to make test slicing consistent between Meson and Make builds. The patch tried to use `${{matrix.nr + 1}}` to implement one-based indexing, but GitHub Actions doesn't support arithmetic in expressions. Peff points out this was previously handled in PowerShell (pwsh) syntax, and suggests reverting to that approach.

The exchange highlights a technical constraint in GitHub Actions that wasn't caught during initial review. Peff provides historical context, referencing a 6-year-old discussion about this limitation and pointing to prior commits that handled the arithmetic differently. This is a focused technical discussion about how to properly implement the desired test slicing behavior within GitHub Actions' constraints.

The thread shows the project's attention to CI stability - Junio caught the issue quickly after integration, and Peff is helping identify the correct solution. The core series improvements remain valid but need adjustment to work around this platform-specific syntax limitation.