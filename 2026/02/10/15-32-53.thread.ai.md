Thread identity: Proposal for a new `pre-add` hook (v2), allowing validation of staged changes before they enter the index. Originating author not specified in summaries. Thread type: feature patch series.

Core problem/goal: Git lacks a hook point between index computation and finalization during `git add`. The `pre-add` hook fills this gap, enabling linting, policy enforcement, and validation of staged changes. Version 2 implements maintainer Junio Hamano's suggested architecture from v1 feedback, providing both original index and proposed changes via temporary files.

Current status: Version 2 implements the maintainer's proposed alternative design. Key changes from v1:
- Now provides hook with both original index and proposed changes via temporary files
- Uses tempfile API and lockfile-based rollback
- Maintains same bypass behavior (`--no-verify`) and mode exclusions
- Preserves comprehensive test coverage (t/t3706-pre-add-hook.sh)
- Follow-up email confirms design matches maintainer's approach, notes CI failure due to parallel-hooks conflict

Key technical details:
- Hook receives two arguments: original index tempfile and proposed changes lockfile
- Uses Git's standard tempfile and lockfile APIs for safety
- Rollback mechanism preserves index state on hook rejection
- Still skips interactive/patch/dry-run modes and `git commit -a`
- Example use cases demonstrate policy enforcement (blocking secrets)
- Test file: t/t3706-pre-add-hook.sh
- Blobs written to object store before hook runs (required for index computation)

Maintainer feedback addressed (from v1):
1. Now runs after full index computation (not just pathspec validation)
2. Provides both original and proposed index states
3. Enables diff-based inspection via standard Git tools
4. Implements proper rollback capability
5. Removes questionable `GIT_INDEX_FILE` usage
6. Maintains same exclusions (interactive modes, `commit -a`)

Open questions:
- Performance impact of temporary index creation
- Final behavior with `git commit -a` (still excluded)
- Edge case handling in rollback scenarios
- Long-term integration with other staging operations
- CI failure due to parallel-hooks conflict (noted in follow-up)

Implementation notes:
- Follows maintainer's suggested two-phase index handling
- Uses standard Git APIs (tempfile, lockfile)
- Comprehensive test coverage maintained
- Well-documented with practical examples
- Appears ready for maintainer consideration pending CI resolution

Related work: Builds on existing hook infrastructure but introduces novel index state comparison pattern. Version 2 represents significant architectural improvement over v1, addressing all technical concerns raised by maintainer Junio Hamano. Design now provides robust foundation for staging validation use cases. Follow-up email indicates confidence in design despite CI issue.