This is a follow-up in an ongoing thread about patch application issues related to the `diff.noprefix` and `format.noprefix` configurations. Junio C Hamano clarifies that a proposed fix from 3 years ago (commit 15108de2) has long been part of released Git versions (since v2.41.0-rc0), rather than being something still queued. The thread centers around inconsistent behavior when generating and applying patches with/without prefixes, particularly affecting workflows involving `git format-patch` and `git am`.

Junio's response indicates the original fix is already in place, though the discussion suggests there may still be documentation gaps around how these related but differently-behaving config options (`diff.noprefix` as boolean vs `format.noprefix` as any-value) should be used. The exchange maintains the project's characteristic technical precision while showing Junio's dry humor in responding to the query about ancient history.