This is a test infrastructure patch in an ongoing thread about handling `iconv`-related test failures on Windows/MSVC platforms. The discussion has centered around whether to skip encoding-related tests when the `iconv(1)` executable is unavailable (Junio's initial position) or to modify test expectations to work around the missing binary (Patrick's approach).

In this exchange, Patrick makes the case that even if the `iconv(1)` binary were restored in Git for Windows (as Torsten Bögershausen suggested), it would still be reasonable to handle cases where the binary is missing when Git is built with `NO_ICONV`. Junio acknowledges this point as sensible, indicating some convergence on the technical approach.

The thread remains unresolved on the broader question of test strategy - whether to skip tests or modify them when dependencies are missing - but this particular point about `NO_ICONV` builds appears settled. The discussion continues to touch on platform-specific packaging decisions versus test portability tradeoffs, with implications for Git's CI infrastructure.