This is a review follow-up in the macOS process ancestry tracing series. Derrick Stolee responds to Matthew John Cheetham's question about whether to convert the recursive parent process traversal in the macOS/procinfo.c implementation to a loop, matching feedback he gave earlier.

Stolee notes that since the Linux implementation also uses recursion with a depth limit, consistency with that approach is reasonable. He suggests that if this optimization is worth making, it should be done in both implementations together in a separate follow-up rather than just for macOS.

The exchange shows the series is in its final polishing stage, with reviewers being pragmatic about when to insist on changes versus when to accept existing patterns. Stolee's response indicates the technical approach is sound and the remaining decisions are about code style consistency across platforms rather than functional concerns.