Test modernization patch updating `filter-branch` tests to use Git's built-in path helpers. This is part of the ongoing effort to replace legacy shell test commands (`test -f`, `test -d`) with more precise Git test helpers (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`).

The patch updates six assertions across two test cases in `t7003-filter-branch.sh` that verify file and directory states after filter-branch operations. The changes make the tests more precise by using Git's dedicated path helpers rather than generic shell tests. Junio C Hamano noted in review that the `test_path_is_missing` helper introduces stricter behavior than the original `! test -d` checks, requiring paths to not exist at all rather than just not be directories. The author confirmed through manual verification that no non-directory files exist with the names being checked, making the stricter assertions appropriate.

This is a straightforward mechanical change following established test modernization patterns. The interaction between the author and Junio demonstrates the project's attention to correctness even for seemingly simple test improvements. The patch has no production code impact and improves test precision while maintaining the same logical assertions.