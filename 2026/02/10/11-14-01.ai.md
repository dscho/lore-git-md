This is a test infrastructure patch in the ongoing discussion about handling `iconv` in Git's test suite, specifically addressing Windows/MSVC platform issues. Torsten Bögershausen chimes in to clarify the distinction between two separate concerns: the presence of libiconv (linked into Git for encoding support) versus the availability of the `iconv(1)` executable (used in test scripts). 

The email notes that MinGW and consequently Git for Windows have stopped shipping the `iconv` binary, leading to test failures when scripts assume its presence. Torsten suggests it might make sense to restore the binary in Git for Windows, linking to a related GitHub issue (#6083). This provides important context about the platform-specific constraints that motivated Patrick Steinhardt's original patch series.

The discussion highlights the tension between test portability and accurate encoding behavior verification. While Patrick's approach modifies tests to work without the executable, Junio had questioned whether encoding-sensitive tests should run at all without full ICONV support. Torsten's intervention clarifies the Windows-specific aspect of the problem while implicitly supporting the need for some solution, whether through test restructuring or platform adjustments.