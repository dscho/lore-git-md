This is a follow-up discussion in the ongoing ODB abstraction effort, specifically about flag handling between `OBJECT_INFO_*` and `HAS_OBJECT_*` enums. Karthik Nayak responds to Patrick Steinhardt's patch that fixed incorrect flag passing in `builtin/backfill.c`.

Karthik agrees with the technical correctness of Patrick's fix but raises a broader point about enum usage. He notes that while the current fix works (passing `0` to `odb_has_object()` happens to set the same flags as `OBJECT_INFO_FOR_PREFETCH` would have), it would be cleaner to properly type the `HAS_OBJECT_*` enum and use it explicitly in the function signature. This would prevent similar confusion in the future.

The exchange shows the careful attention being paid to API boundaries in the ODB abstraction work, where flag passing between layers needs to be both correct and self-documenting. Karthik's suggestion indicates this thread may lead to further refactoring beyond the immediate bug addressed in Patrick's patch. The discussion remains technical and collaborative, with Karthik explicitly approving the patch while suggesting future improvements.