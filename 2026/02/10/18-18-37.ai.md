This is a discussion about Derrick Stolee's RFC patch series to improve type filtering in `git config list --type=<X>`, focusing on the conceptual approach to handling config value types.

The thread has reached a key design decision point. Stolee explains his choice to filter out config values that don't parse as the requested type (option 1), rather than showing them as strings (option 2). His rationale is that this matches the behavior of `git config get --type=<X>`, where invalid values would cause the command to die. Junio Hamano had raised concerns about this approach since config values don't have inherent types - they're just strings that callers may interpret differently.

Stolee clarifies that the type parameter represents the caller's expectation, not an inherent property, and argues against trying to auto-detect types. The discussion reveals a fundamental tension between Git's string-based config model and the desire for type-safe filtering. This conceptual question will need resolution before the implementation details (which Stolee has already agreed to adjust based on Junio's earlier code review) can be finalized.

The exchange touches core config handling philosophy while remaining focused on the specific use case that motivated the series (improving performance for git-credential-manager). The next steps will likely involve either consensus on the filtering approach or exploration of alternative designs that satisfy both the performance needs and config model constraints.