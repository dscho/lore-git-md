This is a discussion thread about a patch application issue related to Git's diff handling, specifically concerning the `diff.noprefix` configuration. The thread involves Alejandro Colomar and наб (Ahelenia Ziemiańska) troubleshooting why a patch for a man page (`futex_waitv.2`) fails to apply, with the error indicating file path issues beyond a symbolic link.

The core issue stems from наб having `diff.noprefix=true` set locally, which unexpectedly caused `git format-patch` to generate patches without the usual `a/` and `b/` prefixes. While наб expected `git am` to handle such patches correctly (similar to how `git add -p` does), the patch application instead failed. The thread references a previous discussion where Junio Hamano indicated he would queue a fix to make `format-patch` ignore `noprefix`, but this doesn't appear to have been implemented yet.

Alejandro Colomar asks Junio for an update on that queued fix, noting this is a recurring pain point. The discussion is technical but focused on a specific Git behavior edge case rather than the content of the original man page patch itself. The thread shows contributors encountering and trying to resolve a workflow hiccup in Git's patch application mechanics.