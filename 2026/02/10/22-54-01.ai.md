This is a technical follow-up in the CI/build system thread about test slicing consistency. Jeff King (Peff) responds to Junio Hamano's discovery that GitHub Actions does not support arithmetic operations in YAML expressions, which caused a reversion of Patrick Steinhardt's patch attempting to standardize test slice numbering.

Peff points to two relevant commits in the history: d3d6493dcf which originally used PowerShell (pwsh) syntax to handle the arithmetic, and 17bd1108ea which moved the "+1" calculation into the script itself. The current patch under discussion removes that script-based adjustment, so Peff suggests reverting to the earlier pwsh-based approach as the only viable solution given GitHub Actions' expression limitations.

The exchange highlights a platform-specific constraint that wasn't caught in earlier review - GitHub Actions' inability to perform math in YAML expressions, a known 6-year limitation. Peff's response is characteristically precise, referencing specific past solutions rather than proposing new ones. This suggests the next iteration of the patch series will need to either keep the offset calculation in the script or use pwsh syntax, maintaining the one-based indexing goal while working around the platform limitation.