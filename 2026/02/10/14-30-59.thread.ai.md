Thread identity: Patch application issue involving `diff.noprefix` configuration affecting `git format-patch` and `git am` behavior, with man page patch for `futex_waitv.2` as the test case. Primary participants are Alejandro Colomar and наб (Ahelenia Ziemiańska), with Junio Hamano referenced for a pending fix.

Core problem: When `diff.noprefix=true` is set locally, `git format-patch` generates patches without standard `a/` and `b/` prefixes, causing `git am` to fail on application despite `git add -p` handling such patches correctly. This creates workflow discontinuity for patch creators and recipients.

Current status: Junio Hamano previously indicated intent to queue a fix making `format-patch` ignore `noprefix` configuration, but implementation appears pending. Alejandro Colomar has now explicitly requested status update on this fix.

Key technical details:
- Configuration: `diff.noprefix` local setting unexpectedly affects patch generation
- Commands involved: `git format-patch` output vs `git am` input compatibility
- Test case: man page patch for `futex_waitv.2` fails application due to path handling
- Prior behavior: `git add -p` handles prefixless patches while `git am` does not

Open questions:
- Status of Junio's promised fix to make `format-patch` ignore `noprefix`
- Whether current behavior constitutes a bug or expected edge case
- Best practices for patch creators with `noprefix` set

Participant positions:
- наб encountered the issue when their locally-generated patch failed for others
- Alejandro Colomar confirms this is a recurring pain point and seeks resolution
- Junio Hamano previously acknowledged need for fix but current status unclear

Related work:
- Implicitly references prior discussion where Junio indicated fix would be queued
- Connects to broader Git patch generation/application consistency questions