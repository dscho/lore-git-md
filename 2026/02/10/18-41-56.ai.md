This is a test modernization patch, part of the ongoing effort to update Git's test suite to use built-in test helpers rather than raw shell commands. SoutrikDas updates `t/t7003-filter-branch.sh` to replace legacy `test -f` and `test -d` checks with Git's `test_path_is_file`, `test_path_is_dir`, and `test_path_is_missing` helpers.

The patch affects two test cases that verify file and directory states after filter-branch operations. The changes are mechanical but Junio C Hamano noted in review that the new `test_path_is_missing` assertions are stricter than the original `! test -d` checks - they require paths to not exist at all rather than just not be directories. SoutrikDas manually verified this stricter behavior is correct by checking the test setup with `ls -la` before and after the operations.

This v2 iteration addresses Junio's feedback by documenting the verification in the commit message. The patch follows established patterns for test modernization while improving assertion precision. It touches only test code with no impact on production behavior, and the interaction demonstrates Git's attention to correctness even in mechanical changes. The changes are straightforward and uncontroversial, with the review discussion focused solely on ensuring the test semantics remain accurate.