This is a test infrastructure patch in an ongoing thread about handling `iconv` availability in Git's CI environment. Torsten Bögershausen chimes in to clarify the distinction between two separate `iconv`-related concerns: the library (libiconv) that Git links against for encoding support, versus the `iconv(1)` command-line utility that some platforms (like Windows) may not provide.

The email notes that Git for Windows has stopped shipping the `iconv` binary, which is causing test failures when tests assume its presence. Torsten suggests it might make sense to restore the binary in Git for Windows, linking to an open issue about this. This provides useful context for the broader discussion about how tests should handle encoding conversions when the `iconv` executable is unavailable.

The thread remains open with Junio Hamano having raised architectural questions about whether encoding tests should run at all without ICONV support, versus Patrick Steinhardt's current approach of modifying test expectations. Torsten's input helps ground the discussion in the practical realities of the Windows platform.