This is a test patch adding coverage for `git subtree split` edge cases, part of a 3-patch series. It focuses on fixes from 2018 that addressed issues with `--rejoin` and merge commit handling but lacked test coverage.

The patch adds three new test cases to `t7900-subtree.sh` that verify specific behaviors around `--ignore-joins`, post-rejoin commits, and merge preservation. Each test is carefully constructed to validate subtle interactions in the subtree machinery, with clear references to the original fixes being tested (via commit hashes and original bug reports). The test scenarios involve:
1. Ensuring `--ignore-joins` properly excludes mainline commits from subtree history
2. Correctly handling commits made before/after `--rejoin` operations
3. Preserving merges with unrelated histories even when trees match

The tests follow the project's established patterns for subtree testing, using the `subtree_test_create_repo` helper and standard test assertions. The commit message provides good context by linking to both the original fixes and a minimal test case from the bug report. This appears to be a well-constructed addition to the test suite that fills a coverage gap for non-trivial subtree operations.