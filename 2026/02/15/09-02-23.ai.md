Second patch in Jeff King's ref-filter cleanup series, focusing on memory handling in `lstrip_ref_components()`. This follows the initial patch that introduced `normalize_component_count()` to unify component-count validation between left and right-stripping functions.

The patch simplifies `lstrip_ref_components()` by eliminating an unnecessary intermediate string allocation. Since left-stripping processes the string sequentially from the start, it can directly return the remaining portion after skipping components, rather than making a temporary copy. Jeff identifies this as a case where previous code had unnecessarily copied the pattern from `rstrip_ref_components()` (which does need temporary strings when working from the end).

The change removes 7 lines while adding 2, making the function both more efficient and more straightforward. As with the first patch, this maintains existing behavior while cleaning up the implementation. The commit message clearly explains the rationale and traces the historical context of the redundant allocation pattern. Given Jeff's reputation for careful refactoring and the focused nature of this change, the patch appears low-risk while improving code quality.