This thread addresses a discrepancy between the documented and actual behavior of `git fetch --prune-tags`. The documentation states this flag should both prune local tags not on the remote *and* force-update tags that differ, but the current implementation only handles pruning. Orgad Shaneh submitted a two-patch series to align the code with the docs, adding test coverage and modifying the tag-fetching logic in `builtin/fetch.c`.

Junio C Hamano raised concerns about whether the current behavior was intentional, prompting Orgad to investigate the history. He found that the `--prune-tags` feature predates Git's tag clobbering protection, suggesting the force-update behavior may have been overlooked rather than deliberately omitted. Orgad also clarified that while his tests verify tag replacement, separate existing tests cover pruning behavior.

The discussion remains open on whether to change the code to match documentation, update the docs to reflect current behavior, or introduce a new configuration option. The thread awaits input from Ævar Arnfjörð Bjarmason, who originally implemented the feature, to clarify the design intent. Orgad has shown willingness to expand test coverage if needed, but the maintainer's hesitation suggests this may need more discussion before proceeding.