This thread addresses a bug in `git fetch --prune-tags` where the implementation didn't match the documented behavior. Orgad Shaneh submitted a two-patch series to fix this discrepancy. The first patch added test coverage showing how `--force` correctly updates tags, while the second patch modifies the fetch code to make `--prune-tags` behave as documented - both pruning missing tags and force-updating changed ones.

The changes are minimal but well-constructed: a one-line code change in `builtin/fetch.c` to pass the force flag when pruning tags, plus expanded tests in `t5516-fetch-push.sh` that verify the behavior works for both command-line flags (`--force` and `--prune-tags`) and the equivalent config option (`fetch.prunetags`). The test coverage builds on existing infrastructure and follows established patterns.

So far, this appears to be a straightforward fix for a clear documentation/implementation mismatch in tag handling. The series is awaiting review, but the changes are focused and the test coverage thorough, suggesting this should be uncontroversial. The fetch and tag subsystems are stable, so there are no obvious interactions with other in-flight work that would complicate merging.