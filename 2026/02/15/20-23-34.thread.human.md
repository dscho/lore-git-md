Orgad Shaneh has submitted a two-patch series to fix an inconsistency in `git fetch --prune-tags` behavior. While the documentation states this flag should both prune local tags missing from the remote and force-update tags that differ between local and remote, the current implementation only handles the pruning. The first patch adds test coverage for the force-update behavior, while the second makes the actual code change in `builtin/fetch.c` to pass the force flag when `--prune-tags` is active.

The changes are minimal - just a one-line code modification and four new test lines - but complete the documented functionality. The test coverage verifies both command-line flags and the equivalent config option. So far there haven't been any reviews or feedback on the series, but the implementation appears straightforward and well-tested, following existing patterns in the fetch machinery. This would be a good candidate for someone familiar with tag handling to review, as it fixes a clear documentation/behavior mismatch in stable functionality.