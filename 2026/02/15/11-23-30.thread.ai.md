Thread summary:

This RFC patch continues the `the_repository` removal effort by migrating the `core.trustctime` configuration from a global variable in environment.c to repository-specific storage in `struct repo_settings`. The change is mechanical and behavior-preserving, following established patterns from prior config migrations in this effort.

Key technical details:
- Removes global `trust_ctime` variable from environment.c
- Adds `trust_ctime` field to `struct repo_settings` with default -1 (unset)
- Introduces `repo_settings_get_trust_ctime()` accessor that lazily reads config (defaulting to true if unset)
- Updates statinfo.c to use new accessor
- Follows same pattern as previous config migrations in this effort

The patch references prior discussion about moving global configuration into repo_settings, positioning this as part of that larger effort. The implementation appears correct and well-scoped, though marked RFC to solicit feedback on design before finalizing. No behavior changes are introduced - this purely relocates where the setting is stored while maintaining existing defaults and semantics.

Participant positions:
- Author (René Scharfe) continues systematic work on `the_repository` removal with this focused config migration
- No other participants have commented yet in this thread

Related work:
- Part of ongoing `the_repository` removal effort that has migrated many globals to repository-specific storage
- Follows same pattern as prior config variable migrations in this series