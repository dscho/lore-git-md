Bugfix completing the documented behavior of `git fetch --prune-tags` by making it force-update changed tags, not just prune missing ones. This is the second patch in a two-part series from Orgad Shaneh.

The patch makes a minimal change to `builtin/fetch.c` to pass the force flag when `--prune-tags` is active, matching the documented behavior that `--prune-tags` should "clobber changed tags". The test update in `t/t5516-fetch-push.sh` expands the existing test for force-fetching tags to verify both the `--force` and `--prune-tags` paths, plus the equivalent config option `fetch.prunetags`.

The implementation is straightforward - a one-line code change and test expansion that follow existing patterns. The commit message clearly states the discrepancy being fixed between docs and implementation. This appears to be a focused, well-constructed fix for a clear documentation/behavior mismatch in tag handling. The test coverage is thorough, exercising both command-line flags and config options while building on the existing test infrastructure.