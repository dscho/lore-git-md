Thread identity: Test coverage patch series for `git subtree split` edge cases, focusing on `--rejoin` and merge commit handling scenarios that were fixed in 2018 but lacked test coverage.

Core problem or goal: Add comprehensive test coverage for subtle edge cases in `git subtree split` that were addressed by fixes in 2018 but never had proper test cases written. The goal is to validate the correctness of `--ignore-joins` behavior, post-rejoin commit handling, and merge preservation in the subtree machinery.

Current status: First patch in a 3-patch series. This initial patch adds three new test cases to `t7900-subtree.sh` covering specific edge case behaviors.

Key technical details: Tests added to `t7900-subtree.sh` using the `subtree_test_create_repo` helper. Test scenarios include:
1. `--ignore-joins` exclusion of mainline commits from subtree history
2. Correct handling of commits made before/after `--rejoin` operations
3. Merge preservation with unrelated histories even when trees match
Each test references original fixes by commit hash and bug reports.

Open questions and loose ends: None yet - this is the first patch in the series. Future patches may reveal additional edge cases needing coverage.

Participant positions: Only the author (not yet identified in this first message) has participated so far.

Related work: References original 2018 fixes that addressed these edge cases but lacked test coverage. The tests are designed to validate those historical fixes.