Performance optimization for file copying using Linux's `sendfile()` system call, with discussion of platform compatibility and error handling.

George Hu responds to Phillip Wood's review of his patch to use Linux's `sendfile()` for more efficient file copying in `copy.c`. The thread has established that FreeBSD's `sendfile()` works differently (only with sockets), making a portable solution complex. George agrees to move the Linux-specific implementation to `compat/linux/` per project conventions but seeks guidance on where to place the generic fallback implementation.

Phillip's review identified several technical requirements: proper error handling for `fstat()`, propagation of `sendfile()` errors (currently lost), handling `EAGAIN` for non-blocking writes, and falling back to read/write loops on `EINVAL`. George now questions whether the fallback should trigger for *all* errors except `EAGAIN`, not just `EINVAL`. The discussion remains technical, still lacking benchmark data to quantify the optimization's impact.

The patch touches `copy.c`'s `copy_fd()` function, which is used for various Git operations that copy files. Key technical points include the `COPY_WRITE_ERROR` return value, `xread()` buffer size (8KB), and the need to handle partial writes (`write_in_full()`). The Linux implementation would live in `compat/linux/` with error handling matching Git's conventions, while the fallback remains the existing read/write loop. Platform divergence makes this optimization likely Linux-only unless someone contributes BSD/MacOS equivalents.