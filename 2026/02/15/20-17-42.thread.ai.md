Thread identity: Performance and compatibility patch series for `contrib/subtree` by Colin Stagner, now expanded to include Ian Jackson's Rust/libgit2 prototype as an alternative solution to the recursion limits in `git subtree split`. The thread has evolved into a fundamental architectural debate about the component's future, with brian m. carlson now involved for Rust integration expertise and Junio Hamano weighing in on maintenance policy.

Core problem: The immediate issue remains `git subtree split` failing on deep history graphs due to shell recursion limits, with two competing solution paths: Colin's algorithmic fixes in POSIX shell versus Ian's Rust/libgit2 implementation. The Rust prototype introduces commit metadata handling changes (dummy committers with original attribution in messages) and raises questions about Git's Rust integration strategy. Junio's intervention adds a third dimension - whether the solution should live in-tree at all given its libgit2 dependency.

Current status: Colin's v2 shell script patches remain pending while Ian's working Rust prototype demonstrates performance improvements. Junio has now rejected in-tree hosting for the Rust/libgit2 approach due to its external dependency, suggesting it follow the gitk/git-gui model of external development. The decision matrix now explicitly weighs:
- Colin's shell fix (in-tree)
- Ian's Rust rewrite as standalone tool
- No third option for Rust in git.git

Key technical details: The original shell patch modifies `contrib/subtree/git-subtree.sh` (88 lines added/7 removed). Ian's Rust prototype shows:
- Different commit metadata handling (dummy committers + message attribution)
- 3-5x performance improvements on deep histories
- Dependency on libgit2 rather than Git's internal APIs
Junio's position introduces new constraints:
- `contrib/` components must use Git APIs or go standalone
- Historical precedent of gitk/git-gui externalization
- No appetite for libgit2 dependency in main tree

Open questions: The decision space has narrowed to:
1. Whether to accept Colin's v2 patches as the in-tree solution
2. Whether Ian will pursue the Rust version as a standalone tool
3. If externalized, what README transition would look like
Junio's maintenance concerns now dominate the technical debate, making Colin's shell patches the only viable in-tree path forward.

Participant positions: Colin Stagner remains author of the shell patches. Ian Jackson has a working prototype but now faces packaging constraints. Junio Hamano has ruled out the Rust/libgit2 approach for git.git based on maintenance policy. brian m. carlson's Rust integration expertise may now focus on advising the standalone path. The technical merits of each approach remain acknowledged but secondary to packaging decisions.

Related work: Connects to broader themes of Rust adoption strategy and maintenance of underused components. The discussion now explicitly references gitk/git-gui's externalization as precedent. Ian's proposal faces the same constraints that kept libgit2 itself separate from git.git. The metadata handling discussion remains relevant but may shift to a standalone project's issue tracker. Junio's position reinforces Git's historical resistance to non-core dependencies, even for performance-critical components.