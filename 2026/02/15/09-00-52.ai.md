This is the first patch in Jeff King's refactoring series to clean up the refname component stripping logic in ref-filter.c. It addresses code duplication between `lstrip_ref_components()` and `rstrip_ref_components()` by factoring out their shared logic for handling negative component counts into a new `normalize_component_count()` function.

The patch demonstrates Peff's characteristic attention to detail - not only does it eliminate the duplicated code, but it also improves type consistency by changing the component count from `long` to `int` (with a clear explanation of why this is safe). The commit message thoroughly documents the historical context and rationale for the change.

This is a straightforward refactoring that makes the code more maintainable while preserving all existing behavior. The patch is well-contained (touching only ref-filter.c) and comes with a clear diff showing the pure code movement. As the first in a series, it sets up the subsequent patches to build on this foundation for further improvements to the ref-filter component stripping logic.