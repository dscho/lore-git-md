This thread began as an effort to fix `git subtree split`'s recursion limits, but has evolved into a broader discussion about the component's future. Colin Stagner proposed shell script patches to address the immediate issue, while Ian Jackson developed a Rust/libgit2 prototype offering better performance but requiring external dependencies. Junio Hamano has now made clear that the Rust version cannot live in the main Git tree due to its libgit2 dependency, following the precedent of tools like gitk and git-gui being maintained externally.

Ian's latest email reveals a significant limitation in his approach: the metadata-based optimization for detecting subtree merges only works for `add` operations, not `merge`. This forces a choice between breaking existing histories, creating a separate "v2" tool, or dropping the optimization entirely. While this doesn't change Junio's stance on in-tree inclusion, it does highlight that both solutions have tradeoffs - Colin's shell version is more constrained but compatible, while Ian's Rust approach would either need to relax compatibility or accept reduced optimization.

The discussion has now moved beyond pure implementation to consider versioning and transition paths. Colin's shell patches appear the only viable in-tree solution, while Ian may pursue his Rust version as a standalone tool with different behavior. Junio-enforced constraints have effectively made the maintenance policy the deciding factor, with technical merits now informing how each approach might proceed separately rather than competing directly.