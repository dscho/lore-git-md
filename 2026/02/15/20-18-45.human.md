This is a test patch for the `git subtree` contrib command, adding coverage for several edge cases in the `split --rejoin` functionality. It's part of a 3-patch series (this being patch 1/3) that appears to be improving test coverage for previously fixed bugs.

The patch adds three new test cases to `t7900-subtree.sh` that verify:
1. That `--ignore-joins` correctly ignores mainline content outside the subtree
2. That split properly handles commits made before and after a `--rejoin`
3. That split preserves merges with unrelated histories even when parents are treesame

The tests are well-constructed, each with clear explanations of what they're verifying and references to the original commits that fixed these issues (dd21d43b58, 315a84f9aa, 68f8ff8151). The test scenarios appear to be based on real-world cases from previous bug reports.

This looks like solid test coverage work - the commit message clearly explains the tests' purpose and the patch itself is focused purely on adding test cases without modifying any production code. The tests should help prevent regression of these edge cases in future `split --rejoin` operations.