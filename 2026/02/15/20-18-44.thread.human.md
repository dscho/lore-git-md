Here's a human-readable summary of the thread:

This thread discusses a 3-patch series by Colin Stagner fixing issues in the `contrib/subtree` component, specifically around how the `subtree split` operation handles commit history. The problem stems from an optimization (commit 98ba49ccc2) that incorrectly excluded some commits from split history, with a subsequent fix attempt (83f9dad7d6) introducing regressions. The series takes the approach of completely removing this problematic optimization while adding robust test coverage to ensure correct history preservation.

The first patch, which just arrived, focuses on test infrastructure. It adds 110 lines of new tests in t7900-subtree.sh that verify three key scenarios: proper handling of the `--ignore-joins` flag, correct behavior around `--rejoin` operations, and preservation of merge commits with unrelated histories. The tests use precise commit counting and branch comparisons to validate history integrity, with each test case clearly documenting which historical bug it guards against.

Maintainer D. Ben Knoble has reviewed the entire series and only requested minor formatting fixes to patch trailers. The author plans to address these in v2. With these tests in place, the subsequent patches will remove the problematic optimization function entirely. The series appears well-constructed and nearly ready for merging, with no outstanding technical objections.