Thread identity: Bugfix patch series (2 patches) addressing `git fetch --prune-tags` behavior inconsistency between documentation and implementation. Originating author is Orgad Shaneh.

Core problem: The `--prune-tags` flag is documented to perform two actions: (1) prune local tags not present on remote, and (2) force-update tags that differ between local and remote. Current implementation only does (1), creating a documentation/behavior mismatch. Junio C Hamano questions whether this was an unintentional bug or deliberate design choice from Ævar Arnfjörð Bjarmason's original implementation.

Current status: Initial version (v1) of series pinged by author. First patch adds test coverage for `git fetch --force` behavior with tags. Second patch modifies `builtin/fetch.c` to pass `--force` flag to tag-fetching logic when `--prune-tags` is active. Junio has raised concerns about historical intent and test coverage gaps.

Key technical details:
- First patch adds test in `t5516-fetch-push.sh` verifying `--force` behavior with tags
- Second patch changes `builtin/fetch.c` to pass `TAG_FETCH_FORCE` flag in prune-tags case
- Changes are minimal - four new test lines and one-line code change
- Current test coverage only verifies force-update aspect, missing pruning verification
- Junio suggests examining original implementation context to determine if behavior was intentional
- Implementation follows existing patterns in `test_force_fetch_tag()` and tag-fetching logic

Open questions:
- Was the current behavior (no force-update) an intentional design choice by Ævar?
- Should the code change to match docs, or docs change to match code?
- Need for additional test coverage of both pruning and force-update behaviors

Participant positions:
- Orgad Shaneh: Author proposing code change to match documentation
- Junio C Hamano: Maintainer questioning historical intent and test coverage
- Ævar Arnfjörð Bjarmason: Original implementer (not yet participating in thread)

Related work: Standalone fix not tied to larger efforts. The fetch machinery and tag handling are stable subsystems with few recent changes that would interact with this fix. Would benefit from input from someone familiar with original tag handling implementation history.