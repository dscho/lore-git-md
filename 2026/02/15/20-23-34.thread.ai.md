Thread identity: Bugfix patch series (2 patches) addressing `git fetch --prune-tags` behavior inconsistency between documentation and implementation. Originating author is Orgad Shaneh.

Core problem: The `--prune-tags` flag is documented to perform two actions: (1) prune local tags not present on remote, and (2) force-update tags that differ between local and remote. Current implementation only does (1), creating a documentation/behavior mismatch.

Current status: Initial version (v1) of series. First patch adds test coverage for `git fetch --force` behavior with tags. Second patch modifies `builtin/fetch.c` to pass `--force` flag to tag-fetching logic when `--prune-tags` is active, completing the documented behavior.

Key technical details:
- First patch adds test in `t5516-fetch-push.sh` verifying `--force` behavior with tags
- Second patch changes `builtin/fetch.c` to pass `TAG_FETCH_FORCE` flag in prune-tags case
- Changes are minimal - four new test lines and one-line code change
- Test covers both command-line flags (`--force` and `--prune-tags`) and config option (`fetch.prunetags`)
- Follows existing patterns in `test_force_fetch_tag()` and tag-fetching logic

Open questions: None raised yet in initial submission. Awaiting review feedback on approach.

Participant positions: Only Orgad Shaneh has participated so far as author. No reviews or acks yet.

Related work: Standalone fix not tied to larger efforts. The fetch machinery and tag handling are stable subsystems with few recent changes that would interact with this fix. Builds on existing test infrastructure for force-fetch behavior.