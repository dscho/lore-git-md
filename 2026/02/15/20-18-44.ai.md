This is a patch series targeting the `contrib/subtree` component, specifically addressing issues with the subtree split operation. The series aims to fix a problematic optimization that was incorrectly excluding commits from split history.

The cover letter explains that commit 98ba49ccc2 introduced an optimization for subtree splits that ended up excluding too much history, and that a subsequent fix (83f9dad7d6) also introduced regressions. This series takes a different approach by removing the optimization entirely and adding comprehensive test coverage to prevent future issues.

The series consists of three patches:
1. Adds new test cases to better capture edge cases in subtree operations
2. Specifically tests history depth handling
3. Removes the problematic optimization and processes out-of-prefix subtrees correctly

The changes focus on the `git-subtree.sh` script in contrib/subtree and significantly expand the test coverage in t7900-subtree.sh. The author appears to be taking a careful approach by first adding tests before making the behavioral change, which should help validate the fix and prevent regressions.

This looks like a well-structured series addressing a real issue in subtree handling. The commit messages and cover letter provide clear context about the problem and solution approach. The substantial test additions (nearly 200 lines added vs 50 removed) suggest the author is prioritizing robustness over quick fixes.