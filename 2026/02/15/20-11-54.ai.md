This is the v5 cover letter for Samo Pogačnik's 2-patch series addressing shallow repository handling in fetch operations. The series has evolved through multiple rounds of review with Patrick Steinhardt, Junio C Hamano, and Kristoffer Haugsbakk, and is now focused on final implementation refinements.

The series fixes two issues in shallow repository handling: a memory leak in `get_shallow_commits()` (patch 1) and correct implementation of relative-depth fetching (`--deepen`) (patch 2). The key technical change in v5 is the restructuring of the shallow commit calculation logic into a unified internal function `get_shallows_or_depth()` that can either return shallow commits or calculate current depth, while maintaining a clean external interface. This addresses Patrick Steinhardt's concern about duplicate calls while preserving Samo's goal of reusable components.

The implementation now properly handles both absolute and relative depth calculations, tracks minimum depth using a new `commit_depth` slab, correctly processes graft points, and includes comprehensive test coverage in t5500-fetch-pack.sh. The cover letter notes that this version resolves the remaining organizational concerns from v4 while maintaining all the functionality that had already been reviewed and approved.

The patches are well-tested (including leak-checking tests) and have cleared all substantive technical hurdles. The thread demonstrates productive collaboration, with Samo incorporating feedback across multiple iterations. This v5 appears to be the final version before potential merging, with only minor code organization refinements remaining from the previous discussion.