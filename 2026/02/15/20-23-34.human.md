This is a bugfix patch series addressing an inconsistency in `git fetch --prune-tags` behavior. The cover letter explains that while the documentation states this flag should both prune missing tags and force-update changed ones, the implementation only handled the pruning part.

Orgad Shaneh's two-patch series first adds a test to demonstrate the current behavior (which fails to clobber changed tags), then fixes the implementation to match the documented behavior. The changes are minimal - just a one-line code change in `builtin/fetch.c` and corresponding test updates.

The patch appears straightforward and well-motivated, fixing a clear documentation/implementation mismatch. The test coverage looks appropriate for the change. This seems like a low-risk improvement that aligns behavior with user expectations set by the docs.