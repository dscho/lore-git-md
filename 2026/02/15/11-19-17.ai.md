This is a technical discussion about the final implementation details in Samo Pogačnik's shallow repository patch series (v4). The thread focuses on the second patch, which implements correct relative-depth fetching (`--deepen`).

Samo responds to Patrick Steinhardt's suggestion about consolidating the depth calculation logic into a single call to `get_shallow_commits()`. While acknowledging the suggestion, Samo raises a design consideration about whether depth measurement as a separate operation might have general utility beyond this specific use case. He has prepared a new version that makes a single external call from `upload_pack.c` but internally still uses a common function called twice - once for measurement and once for the actual shallow commits retrieval.

The exchange shows careful consideration of code organization trade-offs between:
- Patrick's preference for eliminating the double call to `get_shallow_commits()`
- Samo's desire to maintain a clean internal architecture with reusable components

Samo indicates he will post this revised version shortly, suggesting this is likely the final iteration before the series can be merged. The discussion remains productive and technical, with both parties focused on achieving the best implementation while maintaining code clarity. All substantive technical issues from earlier reviews appear resolved, leaving only this final organizational question.