Here is the dense AI thread summary:

This thread begins with a patch from René Scharfe continuing the `the_repository` removal effort by refactoring `read_mmblob()` to take an explicit `struct object_database *` parameter instead of implicitly using `the_repository`. The change propagates through callers in `apply.c`, `checkout.c`, `merge-file.c`, `merge-ort.c`, and `notes-merge.c`, updating each to pass `the_repository->objects` explicitly. The patch also replaces `oideq(oid, null_oid(the_hash_algo))` with `is_null_oid(oid)` to reduce hash-algorithm dependencies. This is a mechanical change following the established pattern for removing `the_repository` usage - making object database dependencies explicit in function interfaces rather than relying on globals. The changes touch merge-related subsystems but don't alter their core logic, only their object access patterns. The patch is consistent with René's typical style of small, focused refactorings that improve code hygiene. No technical objections or requests for changes have been raised in response. The change appears ready for merging as part of the ongoing `the_repository` elimination effort.