This is part of Shreyansh Paliwal's ongoing series to remove `the_repository` and `the_hash_algo` globals from `wt-status.c`, part of the broader effort to eliminate global state in Git. The current discussion focuses on worktree-related functions where NULL worktree handling complicates the parameter passing.

Junio Hamano points out that the root issue lies in the worktree API's design, where NULL is used to represent the primary worktree. He suggests this leads to awkward API patterns, citing `worktree_git_path()` as another example of the same problem. While acknowledging the immediate need for Shreyansh's dual-parameter approach (passing both `struct repository *r` and `struct worktree *wt`), Junio proposes that the deeper fix would be to ensure the worktree API always uses a proper worktree object (with NULL `.id` for primary worktrees) rather than NULL.

The exchange reveals this as a classic case where removing globals exposes underlying API design questions. Junio leaves the decision to Shreyansh - either tackle the worktree API first (a larger project) or proceed with the current approach while acknowledging it's not ideal. The thread shows good technical discussion about tradeoffs in API design while working toward the long-term goal of eliminating globals.