This is a refactoring patch from René Scharfe that's part of the ongoing effort to clean up and modernize Git's codebase. The patch replaces a manually managed array of commits in the `rev_collect` struct with the existing `commit_stack` utility structure, which provides the same functionality in a more maintainable way.

The changes are straightforward:
1. The `rev_collect` struct now contains a `commit_stack` instead of separate `commit`, `nr`, and `alloc` fields
2. The code uses `commit_stack`'s push/init/access methods rather than direct array manipulation
3. The loop counter is changed to `size_t` to match `commit_stack`'s type
4. Memory management is handled by `commit_stack_clear`

This is a typical René Scharfe patch - focused, mechanical, and improving code hygiene without changing behavior. The commit message clearly explains what's being done and why. The change touches the commit-graph traversal code in `commit.c`, specifically the fork-point calculation used by commands like `git rebase --fork-point`.

For future reference in this thread, key technical details include:
- Modified struct: `rev_collect` in `commit.c`
- Replaced fields: manual array management with `commit_stack`
- Affected function: `get_fork_point()`
- Behavior remains identical, just implementation cleanup
- Uses existing `commit_stack` API from Git's codebase