Test modernization patch updating path verification in t2003-checkout-cache-mkdir.sh.

Burak Kaan Karaçay sends a straightforward test modernization patch, converting legacy `test -f` and `test -d` checks to the more informative `test_path_is_*` helpers in t/t2003-checkout-cache-mkdir.sh. The changes are mechanical replacements that improve test failure diagnostics by providing verbose error messages when assertions fail. The patch touches 49 lines across the test file, replacing silent checks with their more descriptive counterparts (e.g., `test -f` becomes `test_path_is_file`, `test ! -h` becomes `test_path_is_dir_not_symlink`).

This appears to be part of the ongoing test modernization effort in Git's test suite, though the commit message does not explicitly reference any broader initiative. The changes are focused and correctly implemented - each replacement matches the original intent of the test while adding better failure reporting. The patch is well-scoped to a single test file and makes no functional changes to the tests themselves, only improving their diagnostic output.