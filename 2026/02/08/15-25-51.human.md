This is a bugfix discussion for `git clone`'s handling of `--revision` with older protocols. The thread has evolved from a simple NULL-check fix to a deeper architectural discussion about how `--revision` clones should behave.

Nitro Cao responds to Junio Hamano's critique of their initial patch, agreeing that a simple NULL check would mask underlying issues. Junio had proposed restructuring `update_remote_refs()` to separate connectivity checking from ref-writing operations, since `--revision` clones should verify connectivity but not update refs.

The key points in this email:
1. Nitro acknowledges Junio's concern that the original fix would hide cases where `.peer_ref` should be set but wasn't
2. They ask whether the solution should instead conditionally call `find_ref_by_name()` when `option_rev` is active
3. They confirm understanding that `--revision` should result in a detached HEAD at the specified revision
4. They commit to add tests once the correct approach is determined

The discussion remains open on how to properly structure the ref update logic for `--revision` clones across protocol versions. Nitro appears receptive to Junio's architectural suggestions but seeks clarification on implementation details before proceeding with both the fix and promised test coverage.