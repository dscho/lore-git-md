Patch 1/13 in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. This is a pure refactoring patch that moves the `show_all_config()` function lower in builtin/config.c to prepare for using `format_config()` in a future patch. No behavior changes are introduced - it's just code reorganization to set up the subsequent changes in the series.

The patch is straightforward and mechanical, with a clear commit message explaining the motivation (anticipating future use of `format_config()`). The diff shows the function being moved down 24 lines, with no other modifications. This is typical preparatory work for a larger refactoring effort.