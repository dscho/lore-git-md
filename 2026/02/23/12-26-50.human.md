Performance improvement for `git config list --type=path` handling. This is patch 8/13 in Derrick Stolee's series optimizing config value filtering, specifically addressing path-type values.

The patch extracts path value formatting into a new `format_config_path()` helper function, similar to the type-specific handlers added in previous patches. The key change is properly handling the `:(optional)` macro for paths - when a path doesn't exist, it now silently omits the value (using gentle parsing) rather than failing, which matches the behavior needed for config lists with multiple values.

The implementation maintains compatibility with existing behavior where `git config get` would fail for a missing required path, while allowing config lists to skip invalid paths when other valid values exist. Test updates confirm the corrected handling of missing paths in list mode.

This continues the series' pattern of moving type-specific logic into dedicated functions while improving performance by avoiding unnecessary parsing of values that will be filtered out. The change is straightforward and builds on the foundation established in earlier patches, with no outstanding questions or controversies.