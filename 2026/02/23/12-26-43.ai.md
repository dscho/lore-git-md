First patch in Derrick Stolee's performance improvement series for `git config list --type filtering. This is a pure code movement patch that relocates the `show_all_config()` function lower in builtin/config.c to prepare for using `format_config()` in a future patch. No functional changes are made - the diff shows the identical function being moved after `format_config()` is defined. 

The change is straightforward mechanical refactoring to support the series' goal of optimizing config value parsing by enabling type filtering during initial parsing rather than after. This initial patch sets up the code structure needed by subsequent patches that will implement the actual performance improvements. The commit message clearly states the motivation (anticipating use of format_config()) and confirms no behavioral changes.