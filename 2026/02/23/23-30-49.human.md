This is a documentation and behavior alignment patch series for `format.noprefix`. Kristoffer Haugsbakk follows up on earlier discussion to make `format.noprefix` properly enforce boolean values (rejecting non-booleans) while correcting related documentation.

The series has two parts: First, it changes `builtin/log.c` to use `git_parse_maybe_bool()` with clear error messages when invalid values are provided, following Jeff King's config validation patterns. The error message now includes an advisory note explaining the behavioral change (previously any value was treated as true). Second, it fixes the `diff-options.adoc` documentation to properly reference `format.noprefix` instead of the incorrect `diff.noprefix` for format-patch operations.

The changes are technically breaking for users who relied on the old non-boolean behavior, but the thread has consensus that this is justified as a bug fix to align behavior with documentation. Junio Hamano suggested improvements to the transitional messaging, which have been incorporated in v2. The series includes new tests verifying the boolean behavior and error messaging.

This appears ready for merging - all reviewer feedback has been addressed, the changes are well-contained, and the justification for the breaking change is sound. The patch quality is high with clear commit messages and proper test coverage.