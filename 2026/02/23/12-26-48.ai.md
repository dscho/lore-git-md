Performance improvement patch for `git config list --type=bool-or-int` handling. This is patch 6/13 in Derrick Stolee's series optimizing config value filtering, following the integer-type handling in patch 4 and boolean handling in patch 5.

The patch extracts the bool-or-int formatting logic into a new `format_config_bool_or_int()` helper function that implements the same gentle parsing pattern seen earlier in the series. When gentle parsing is requested (for list` operations), it first tries `git_parse_maybe_bool_text()` and falls back to `git_parse_int()`, avoiding the more aggressive `git_config_bool_or_int()` that might die() on invalid input. The test update confirms the gentle parsing works by no longer failing on invalid bool-or-int values, instead silently skipping them.

This continues the series' consistent approach to type-specific parsing, maintaining the same pattern established for integers and booleans while handling the composite bool-or-int case. The implementation is straightforward and follows the established conventions from earlier patches, with no new technical complexity introduced. The test change demonstrates the expected behavior where invalid values are now filtered out rather than causing errors.