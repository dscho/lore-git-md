Performance improvement series for `git config list --type` filtering, now handling path values. This is patch 8/13 in Derrick Stolee's v3 series addressing the original performance bottleneck in git-credential-manager when filtering path-type config values.

The patch extracts path value formatting into a new `format_config_path()` helper function, maintaining the existing behavior where invalid paths are silently omitted when using `--type=path` filtering. This matches the gentle parsing pattern established in earlier patches for other types (bool, int, bool-or-int). The implementation carefully preserves the special handling of the `:(optional)` macro, which allows ignoring missing paths when multiple values exist while still ensuring `git config get` fails appropriately for single missing values.

Test changes confirm the path filtering works as expected, removing a TODO marker about incorrect handling of missing paths. The patch follows the same architectural pattern seen in previous type handlers (patches 4-7), completing the foundation for type-specific parsing before the series moves on to more complex cases. Performance measurements from earlier in the thread confirm these changes address the original git-credential-manager bottleneck while maintaining Git's config model.