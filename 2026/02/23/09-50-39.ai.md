This is a bugfix series addressing an issue with pack verification of large objects, reported by brian m. carlson. The series comes from Patrick Steinhardt, a major contributor who frequently works on Git's object handling and storage layers.

The core issue fixes a verification problem where large objects in packs weren't being properly checked. The series starts with test helper improvements, then refactors object streaming internals to properly handle large objects during verification. The key changes are in `pack-check.c` where the verification logic is corrected, and `packfile.c` which gets new streaming capabilities needed for the fix.

The series includes thorough test updates across multiple test files (`t1450-fsck.sh`, `t1050-large.sh`, etc.) to verify the fix works. Given Patrick's track record with object storage changes and the clear problem being addressed, this appears to be a well-motivated and carefully constructed fix. The changes touch core object handling code but are focused on the specific verification problem rather than making broader architectural changes.

For future reference in case this thread continues: the series modifies object streaming internals (`stream_object_signature()` and related functions), adds new packfile streaming capabilities, and fixes the pack verification path in `pack-check.c`. Test changes confirm the fix works for large objects across multiple scenarios. The original report came from brian m. carlson but the commit message doesn't detail the exact failure case - just that large object verification was broken.