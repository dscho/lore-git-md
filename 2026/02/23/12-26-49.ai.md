This is patch 7/13 in Derrick Stolee's performance improvement series for `git config list --type=<X>` filtering. The series addresses inefficiencies in config value parsing by moving type-specific handling earlier in the process.

This particular patch extracts the bool-or-string formatting logic into a dedicated helper function `format_config_bool_or_str()`. Unlike the previous patches that introduced gentle/strict parsing duality for integers and booleans, this case was already gentle by design - the extraction is purely for consistency with the rest of the refactoring. The helper maintains the existing behavior where string values are passed through unchanged if they don't parse as booleans.

The change is mechanical and low-risk, following the same pattern established in earlier patches of the series. It doesn't introduce new functionality but helps consolidate the type-specific formatting logic in preparation for the remaining patches that will handle path and color types. The implementation matches the thread's established direction with no new technical questions raised.