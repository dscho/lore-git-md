This thread addresses a subtle but impactful issue with zombie process accumulation when Git operations fail in containerized environments. The original report described Git running as PID 1 (the init process in containers) failing to reap child processes after transport errors during network operations like git-remote-https or ssh. Andrew Au's v2 patch introduced atexit handlers to ensure cleanup regardless of exit path, with careful tracking to avoid double-waiting.

Jeff King (Peff) raised important technical concerns about potential deadlocks in the implementation, suggesting alternative approaches using kill-then-wait semantics and questioning whether child cleanup should be Git's default behavior. The discussion established real-world impact through Andrew's observations of over 6,500 zombie processes in production containerized services.

Junio C Hamano has now entered the thread with a clarifying question about the actual deployment scenario - whether Git itself is PID 1 or if it's being spawned by another long-running service acting as PID 1. This distinction matters for determining where the responsibility for child reaping should lie. The technical solution remains under review, with open questions about both implementation safety (Peff's deadlock concerns) and proper problem framing (Junio's clarification request).

The thread stands at an important juncture where the next steps depend on:
1. Andrew clarifying the exact container architecture where the issue manifests
2. Potential adjustments to either the implementation (addressing Peff's concerns) or the problem statement (per Junio's query)
3. Deciding whether this warrants changes to Git's broader child process handling behavior

Participants are converging on a solution but need to align on the precise nature of the problem before finalizing the approach.