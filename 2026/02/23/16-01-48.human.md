This is a test infrastructure patch in Patrick Steinhardt's series to change Git's default maintenance strategy from "gc" to "geometric". Justin Tobler provides a review of the first patch, which addresses test suite races caused by background maintenance operations.

The patch introduces a new `GIT_TEST_MAINT_AUTO_DETACH` environment variable to make maintenance operations run synchronously during tests, preventing races with test assertions that check repository state. Justin confirms the technical approach looks sound, noting the careful handling of configuration precedence where the new variable only takes effect when neither `maintenance.autodetach` nor `gc.autodetach` is set.

Justin suggests two small improvements: fixing a typo ("varibale" -> "variable") and adding a clarifying comment about why `GIT_TEST_MAINT_AUTO_DETACH` is unset in `t7900-maintenance.sh`. The review is positive overall, with Justin verifying the changes to `run-command.c` and test infrastructure appear correct.

This is a preparatory patch for the larger series - it doesn't change user-facing behavior but ensures the test suite remains stable when the default maintenance strategy changes in later patches. The review interaction shows the series is progressing through normal technical review, with minor polish suggestions being incorporated.