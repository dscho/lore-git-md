This is a follow-up discussion in Patrick Steinhardt's bugfix series addressing pack validation issues with large objects. The thread has already established that the core fix prevents an infinite loop in `git fsck` when verifying large objects across multiple packfiles.

In this exchange, Jeff King raises a subtle concern about whether the refactoring might miss marking corrupted objects as bad when they fail to unpack. Patrick acknowledges this is a valid point and proposes an additional change to explicitly handle `OBJ_BAD` cases by marking the corrupt object before returning an error. The change would modify `packfile_read_object_stream()` to accept the object ID as a parameter so it can properly record corruption.

The discussion shows careful attention to edge cases in the pack validation logic, with both contributors demonstrating deep knowledge of Git's object storage internals. Patrick's proposed follow-up change demonstrates his characteristic thoroughness in addressing reviewer feedback, even when the issue is not directly related to the original bug report. This level of attention to detail is typical of patches touching Git's object storage layer, where correctness is critical.