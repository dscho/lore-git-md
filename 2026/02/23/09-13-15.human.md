This is a discussion about interface design for the `git replay` command's new revert functionality, which is part of an ongoing feature addition primarily for GitLab's Gitaly service. The thread has progressed through technical implementation details and is focusing on the user-facing command structure.

Christian Couder responds to Toon Claes' suggestion to use subcommands (`revert`, `pick`, `replay`) instead of flags (`--revert`, `--advance`, `--onto`), agreeing it's worth considering but proposing to defer the change until after the current `--revert` implementation is merged. He questions whether the `--base` option is necessary if a base branch will nearly always be required, suggesting simpler positional arguments might work better.

The exchange represents a late-stage design discussion after the technical implementation has stabilized through multiple iterations (now at v3). Junio Hamano has indicated support for the subcommand approach, which would require restructuring the current flag-based design. The conversation shows the project's careful consideration of command ergonomics even after core functionality is implemented.