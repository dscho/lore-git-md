This is a technical discussion about `git fsck`'s verification, continuing the thread about an infinite loop bug when processing multiple packfiles. Jeff King (Peff) is responding to Patrick Steinhardt's analysis of the underlying issue.

Peff agrees with Patrick's discovery that the problem involves more than just multiple packfiles - it also depends on `core.bigFileThreshold`. When verifying large objects (above this threshold), `git fsck` uses a different code path that searches by object ID rather than pack offset, which can trigger MRU list updates during iteration and cause the infinite loop.

The discussion has expanded beyond the immediate bug to highlight deeper architectural concerns. Patrick notes that the current behavior could lead to verifying the wrong instance of an object when duplicates exist across packs. Peff strongly agrees, suggesting the root issue is that `fsck` shouldn't be using `find_pack_entry()` in this context at all.

This exchange shows the thread evolving from a specific bugfix to examining fundamental design questions about pack verification. While the immediate fix (adding a `skip_mru_updates` flag) has been approved for merging, the conversation now focuses on whether the MRU list design itself needs reworking to properly handle these edge cases. The discussion remains technical but productive, with both contributors clearly aligned on identifying the real underlying problems.