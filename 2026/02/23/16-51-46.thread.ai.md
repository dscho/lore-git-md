[AI thread summary]

This thread addresses a bug causing zombie processes when Git runs as PID 1 in containers, specifically during abnormal exits from network operations (git-remote-https, ssh). The core issue occurs when transport errors trigger exit(128) before normal cleanup paths (disconnect_helper, finish_connect) can reap child processes, leaving them as zombies when Git is PID 1.

The proposed solution adds atexit handlers in transport-helper.c and connect.c to ensure child process reaping regardless of exit path, while preserving existing cleanup behavior for normal exits. The patch targets two key files with focused changes and includes a detailed explanation linking to a blog post documenting the investigation. This is a container-specific edge case fix addressing real-world deployment pain points. No reviews or alternative approaches have been discussed yet. The patch is marked as RFC, suggesting the author may be open to feedback on the atexit handler approach before finalizing.