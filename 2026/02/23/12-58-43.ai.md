This is a technical discussion in Patrick Steinhardt's bugfix series addressing pack validation issues with large objects. Jeff King (Peff) is providing specific implementation advice for improving test coverage of the fix.

The exchange focuses on how to reliably create test cases that verify corruption detection when the same object exists in multiple packs (one corrupt, one valid). Jeff suggests looking at `do_corrupt_object()` in t5303 as a model, which uses `show-index` to locate an object offset and `dd` to corrupt it. This would allow creating deterministic test cases that don't depend on MRU pack ordering.

The context shows this is part of finalizing the series' test coverage before v2 submission. Patrick had previously acknowledged the test coverage gap and Jeff is now providing concrete guidance to address it. The discussion remains narrowly focused on test implementation details rather than revisiting the core fix, which has already been reviewed and approved.