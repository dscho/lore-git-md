Here's the human-readable summary of the thread so far:

Patrick Steinhardt continues his major refactoring of Git's object database (ODB) subsystem with part 16/17 of his series to enable pluggable storage backends. The latest patch introduces a `write_alternate()` callback, completing the alternates-related operations after the previous patch's `read_alternates()` addition. This follows the series' consistent pattern of converting direct ODB calls to callback-based dispatch.

The implementation carefully moves alternates writing logic from odb.c to the files backend while maintaining all existing behavior - including proper locking and duplicate checking when writing to `info/alternates`. Justin Tobler remains actively engaged in reviewing each patch, showing continued alignment on the architectural approach. The work maintains Patrick's hallmark thorough documentation and error handling while keeping backward compatibility.

This represents steady step in the multi-year effort to make Git's object storage fully pluggable. With only one patch remaining in the series, the ODB abstraction work is nearing completion, having systematically converted all core operations to callback-based dispatch while preserving the existing files backend behavior. The consistent quality and review engagement throughout suggests this foundational change is on track for eventual merging.