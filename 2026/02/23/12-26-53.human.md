Performance improvement for `git config list --type=color` handling. This is part 11/13 of Derrick Stolee's series to optimize config value filtering by type.

The patch extracts color value formatting into a dedicated `format_config_color()` helper function, following the pattern established in previous patches for other config types. The key change is using `color_parse_quietly()` when gently parsing config values during filtering, which suppresses error messages for invalid color values that don't match the requested type. This matches the behavior implemented for other types in earlier patches.

The test changes confirm that invalid color values no longer produce error messages when using `--type=color` filtering, while still correctly showing only valid color values in the output. This maintains consistency with how other type filters work in the series.

The patch continues the series' pattern of moving type-specific handling into dedicated functions while maintaining the same external behavior (just more efficiently). It's a straightforward addition to the established framework, with all design questions already settled in previous iterations of the series.