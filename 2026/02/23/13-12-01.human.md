This is a technical discussion in Patrick Steinhardt's bugfix series addressing pack validation issues, specifically focusing on handling corrupted objects during streaming reads. Jeff King (Peff) is reviewing the third patch which exposes a function for reading object streams from packfiles.

The key points under discussion are:
1. The patch currently doesn't handle the case where `unpack_object_header()` returns `OBJ_BAD`, which could leave some corrupt objects undetected. Patrick proposes adding explicit handling for this case by marking bad objects when header parsing fails.

2. Jeff acknowledges this would restore the original behavior but questions whether it's worth the complexity, noting that many other corruption cases (like zlib stream errors) still wouldn't be caught. He suggests the change might be premature optimization.

3. There's discussion about the interface design - the proposed fix requires passing an OID parameter that isn't used for lookup, which Jeff finds conceptually messy. He suggests an alternative approach using detailed error returns to let callers decide how to handle failures.

This exchange shows the careful attention to edge cases in the packfile streaming code, with both contributors weighing tradeoffs between correctness and interface cleanliness. The discussion is technical but hasn't reached full consensus yet - Patrick seems inclined toward explicit error handling while Jeff is more skeptical about the value versus complexity.