Bugfix follow-up addressing a minor error in the test helper patch. Jeff King points out that Patrick Steinhardt's patch to improve the `genrandom` test helper incorrectly checks the return value of `git_parse_ulong()`. The function returns a boolean success/failure (0/1) rather than the negative error code pattern used elsewhere in Git. This is a straightforward correction to the error handling logic in the test infrastructure patch that preceded the main fsck fix.

The exchange is brief and technical - Jeff King identifies the issue with clear precision, noting the function's actual return value semantics. This doesn't affect the core bugfix series but ensures the preparatory test helper improvements are technically correct. The thread context shows this is part of a larger effort to fix pack validation issues with large objects, but this particular email is narrowly focused on the test helper implementation detail.