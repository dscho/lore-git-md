This is a minor cleanup patch in Christian Couder's series adding re-sign-if-invalid functionality to `git fast-import`. The patch removes an unused forward declaration of `sign_with_header()` from commit.h that was left behind when the function was removed in a previous commit (6206089bd). 

The change is straightforward and uncontroversial - it deletes two lines that are no longer needed after the function they declared was removed. The patch serves as preliminary cleanup before the main feature patch that follows in the series. It touches only the commit.h header file and doesn't affect any functionality.

As part of a larger series about signature handling in fast-import, this patch shows good attention to detail by cleaning up related dead code first. The commit message clearly explains the change and references the commit that made this declaration obsolete. This is the kind of small but worthwhile cleanup that experienced contributors like Justin Tobler often send between larger feature patches.