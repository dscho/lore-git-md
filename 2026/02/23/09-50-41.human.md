Part of Patrick Steinhardt's series fixing pack validation for large objects, this patch refactors `stream_object_signature()` to accept a preconstructed stream rather than opening one internally. The change prepares for upcoming fixes to properly verify large objects in packs.

The modified function now takes a `struct odb_read_stream *` parameter, shifting stream management responsibility to callers. This affects three call sites that now explicitly open/close streams around verification. The change is mechanical but enables future patches to customize stream handling for large objects.

The patch touches core object verification paths in `object-file.c`, `pack-check.c`, and related headers. Test coverage appears in the series' other patches (not shown here). This is a typical Steinhardt patch - carefully structured with clear commit messages and attention to cross-subsystem impacts. The change is low-risk as it doesn't alter behavior, just prepares infrastructure for the actual bugfixes coming later in the series.