This is a documentation patch that clarifies how `--default-prefix` interacts with configuration options in `git format-patch`. The change fixes the man page to correctly reference `format.noprefix` (which `format-patch` actually uses) rather than the unrelated `diff.noprefix` option.

The patch uses AsciiDoc conditionals to show the correct option name depending on whether the documentation is being built for `git-format-patch` or not. This maintains accuracy while avoiding duplication. The commit message notes that while `format.noprefix` exists, it's unlikely to be widely used since `format.prefix` serves as a better escape hatch.

This is the second patch in a two-part series that aligns behavior and documentation for prefix handling. The first patch (already reviewed and queued) enforced proper boolean validation for `format.noprefix`. This follow-up ensures the documentation matches the actual implementation. The change is straightforward and uncontroversial, with the author having incorporated all reviewer feedback from the thread.