This is part 3/17 of Patrick Steinhardt's series to unify Git's ref iteration interfaces, which has already received overall approval from primary reviewer Karthik Nayak. The series aims to consolidate 14 specialized ref iteration functions into a single configurable API.

The current patch is a straightforward code reorganization - it moves the `do_for_each_ref_flags` enum definition higher up in refs.h to prepare for upcoming changes where these flags will be used by more functions. The enum defines various flags that control ref iteration behavior (like including broken refs or per-worktree refs). 

This is a mechanical change with no functional impact - it just relocates existing code to a more logical position in the header file. The patch follows the series' pattern of careful, incremental refactoring that maintains existing behavior while simplifying the codebase. With the series already in final polishing stage, this being a simple preparatory change, it seems unlikely to generate much additional discussion.