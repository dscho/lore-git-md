This is a technical discussion in Patrick Steinhardt's bugfix series addressing pack validation issues with large objects. The exchange focuses on error handling in the newly introduced `packfile_read_object_stream()` function.

Jeff King raises concerns about the interface design, noting that passing an OID parameter purely for marking corrupt objects feels misleading since the function doesn't use it for lookup. He suggests alternative approaches using detailed return codes or error enums to distinguish between different failure modes. Patrick acknowledges the interface is "a bit ugly" but argues the function is unlikely to gain many callers and prefers keeping the explicit OID parameter for consistent error handling across call sites.

The discussion reveals deeper considerations about error reporting in the packfile streaming path. Jeff points out they're only handling header validation (OBJ_BAD) while still missing zlib stream errors, making this a partial solution. Both agree the current patch restores original behavior but debate whether it's worth addressing this specific edge case given larger gaps in corruption detection.

This email represents the final refinement discussion before Patrick prepares v2 of the series. The technical debate shows both contributors weighing interface cleanliness against practical error handling, with Patrick opting for the more explicit (if slightly awkward) OID parameter approach to ensure consistent corruption marking. The exchange demonstrates careful attention to edge cases in the packfile streaming path beyond just the original infinite loop bug.