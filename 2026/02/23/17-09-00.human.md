This is a documentation patch fixing formatting in the alias configuration section. It corrects a list continuation issue introduced in a recent change (ac1f12a9) where a code block wasn't properly marked as part of the surrounding list, causing subsequent paragraphs to be misformatted.

The patch makes three small but precise improvements:
1. Adds list continuation markers (`+`) before and after the code block
2. Changes the indented code block to use explicit `----` delimiters for consistency
3. Standardizes the comment line to use `$ #` to match the terminal prompt style

The changes are straightforward and clearly motivated, with references to both the original commit that introduced the issue and the surrounding documentation's style. The patch is part of a series based on recently merged work (jh/alias-i18n) but stands alone as a clean documentation fix.