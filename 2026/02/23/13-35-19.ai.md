This is a brief discussion thread in Patrick Steinhardt's large ref iteration API unification series (17 patches). The exchange focuses on a minor implementation detail in patch 11/17, which removes the specialized `refs_for_each_ref_in()` function in favor of the new unified `refs_for_each_ref_ext()` interface.

Karthik Nayak (the primary reviewer) questions whether the `trim_prefix` field in the new options struct needs to explicitly specify a length, suggesting it could be simplified to a flag that automatically uses `strlen(prefix)`. Patrick agrees there's no current use case for custom lengths but proposes leaving this as a potential future optimization (#leftoverbit). Karthik accepts this approach.

The discussion is brief and technical, showing the series is in its final polishing stages with only minor details being considered. The thread demonstrates the thorough review process this large API change is undergoing, with Karthik carefully examining even small implementation choices while Patrick remains open to feedback. This patch is part of the broader effort to consolidate Git's ref iteration functions into a single configurable API.