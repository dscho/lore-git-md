Performance patch in Derrick Stolee's config filtering series (4/13). This patch refactors how `git config list` handles integer values when using `--type=int`, making the parsing more robust and consistent with the series' "gentle parsing" approach.

The change moves integer formatting logic into a new `format_config_int64()` helper function, which properly handles both strict parsing (which may die on invalid input) and gentle parsing (which returns an error code). The implementation maintains Git's existing behavior of using `PRId64` formatting while adding proper error handling for the new gentle mode.

A test is updated to verify that `--type=int` now correctly filters invalid integer values rather than failing the entire command. This is part of the series' broader goal to make `git config list --type=<X>` filter during parsing rather than after, improving performance for cases like git-credential-manager's path lookups.

The patch follows the established pattern from earlier in the series and appears well-tested. It's a straightforward refactoring that builds on the foundation laid by patches 1-3, with no outstanding questions or controversies noted in the thread.