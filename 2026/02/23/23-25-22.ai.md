This is a documentation and behavior alignment patch in Kristoffer Haugsbakk's series about `format.noprefix` configuration. The email continues the discussion with Junio C Hamano about how to handle the transition from accepting any value (treating all as true) to requiring proper boolean values (`true`/`false`/`1`/`0`).

Junio suggests strengthening the error message to explicitly state that any value was previously treated as true, making it clearer for users to update their configurations. He also notes that this should use direct `advise()` calls rather than the configurable `advise_if_enabled()` since configuration corrections are always relevant. Kristoffer agrees with both points and notes he should have used this approach in a previous patch as well.

The thread context shows this change is effectively fixing a bug where the implementation didn't match the documented boolean behavior, with Jeff King (Peff) having previously confirmed this is justified despite being technically a behavior change. The patch series is now complete with all feedback incorporated, including proper documentation updates that clarify the relationship between `format.noprefix` and `--default-prefix`.