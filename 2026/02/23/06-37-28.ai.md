Build system fix for config-list.h dependencies.

This is the final iteration of a patch that ensures the generated `config-list.h` header properly tracks dependencies on documentation files that define Git's configuration options. The solution now works correctly across both Meson and Makefile build systems, handling path escaping and cross-platform concerns while maintaining performance. Patrick Steinhardt confirms the implementation is technically sound after thorough testing, including validation on Windows and tricky path cases. The only remaining minor question (about a `DEPFILE` conditional) doesn't block merging. This represents the culmination of six iterations addressing all prior feedback, with all test suites passing across platforms. The patch is now merge-ready.