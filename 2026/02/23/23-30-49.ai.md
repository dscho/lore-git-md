This is a documentation and behavior alignment patch series from Kristoffer Haugsbakk, making `format.noprefix` properly enforce boolean values while correcting related documentation. The series builds on previous discussion where Jeff King confirmed this change aligns with documented behavior despite being technically breaking.

The first patch changes `format.noprefix` to strictly validate boolean values (rejecting non-booleans) in `builtin/log.c`, using `git_parse_maybe_bool()` with clear error messages. It includes transitional messaging via `advise()` to help users understand the behavioral change. New tests in `t/t4014-format-patch.sh` verify the boolean validation and error output.

The second patch fixes documentation in `diff-options.adoc` to correctly reference `format.noprefix` instead of `diff.noprefix` for format-patch, while noting the escape hatch of `format.prefix`. The changes follow Junio Hamano's feedback to improve the error messaging and use proper advisory formatting.

Key technical details:
- Transition from accepting any value (implicitly true) to strict boolean validation
- Error messages explicitly state the behavioral change history for user clarity
- Uses direct `advise()` calls (not `advise_if_enabled()`) for configuration corrections
- Documents `format.noprefix` and `--default-prefix` interaction properly

The series is now complete with all reviewer feedback addressed, showing consensus that this is a proper fix rather than a disruptive change. The documentation patches have already been reviewed and queued by Junio.