This is a technical discussion in Patrick Steinhardt's bugfix series addressing pack validation issues, specifically focusing on error handling in the object streaming path. Jeff King (Peff) responds to Patrick's proposal to handle `OBJ_BAD` cases in `packfile_read_object_stream()` by marking corrupted objects.

Jeff acknowledges that while Patrick's proposed change would restore the original behavior of marking corrupt objects when header parsing fails, he questions whether it's worth addressing this specific case while leaving other corruption scenarios (like zlib stream errors) unhandled. He notes the change would require adding an OID parameter to the function purely for error reporting, which feels misleading since the function doesn't otherwise use the OID.

The discussion reveals a tension between precise error handling and interface cleanliness. Jeff suggests an alternative approach where the function could return more detailed error codes, allowing callers to decide whether to mark objects as bad. This would better separate concerns but would require more extensive interface changes.

This exchange occurs after the core fix for the MRU-triggered infinite loop has already been agreed upon, with both parties now working through edge cases and error handling refinements. The thread shows careful attention to maintaining existing behavior while keeping interfaces clean, with Jeff providing experienced perspective on what level of error handling is pragmatically valuable versus over-engineering.