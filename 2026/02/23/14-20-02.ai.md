This is a bugfix patch in the ongoing strbuf_attach fixes thread, addressing memory allocation in string reencoding. Vaidas Pilkauskas corrects an off-by-one error in `strbuf_reencode()` where the allocation size passed to `strbuf_attach()` was one byte too small, causing unnecessary reallocations. The fix ensures proper buffer reuse by passing `len + 1` to account for the NUL terminator.

The patch is narrowly focused on `strbuf.c` and follows up on previous discussion about strbuf_attach's memory management optimizations. It demonstrates careful attention to the strbuf subsystem's internal behavior, particularly around buffer reuse. The change is small (a single line modification) but meaningful for performance, as it prevents wasteful reallocations when reencoding strings.

This appears to be a straightforward improvement with no obvious downsides. The patch builds on prior consensus about preserving strbuf_attach's optimization benefits, as established in earlier thread discussion with Junio Hamano. The commit message clearly explains both the problem and solution, including the performance implications of the incorrect allocation size.