This is a minor cleanup patch in a series about `git fast-import` signature handling. The patch removes an unused function declaration from commit.h that was left behind when `sign_with_header()` was removed in a previous commit. 

This appears to be a straightforward housekeeping change with no functional impact. The removal is clearly justified by the function no longer existing in the diff is simple, and the commit message properly references the commit that made this declaration obsolete. 

As the first patch in a two-part series, this serves as a warm-up to the more substantive second patch that will introduce new signature handling behavior in `git fast-import`. The change is uncontroversial and unlikely to need discussion - it's the kind of cleanup that typically gets merged quickly after any technical review.