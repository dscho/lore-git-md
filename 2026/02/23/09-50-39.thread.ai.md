Updated AI thread summary:

Bugfix series from Patrick Steinhardt addressing pack validation issues with large objects, originally reported by brian m. carlson. The four-patch series begins with test infrastructure improvements and preparatory refactoring before delivering the core fix for an infinite loop in `git fsck` when verifying large objects across multiple packfiles.

Key technical details:
- Test infrastructure enhanced with size suffixes (k/m/g) in `test-tool genrandom` and input validation via `git_parse_ulong()`, with error handling corrected per Jeff King's review to properly check boolean return value rather than negative error codes
- Refactored `stream_object_signature()` to accept preconstructed `odb_read_stream`, shifting stream lifecycle control to callers (Jeff King reviewed this change, noting the approach is sound but suggesting minor improvements to error messages and redundant checks)
- Extracted new `packfile_read_object_stream()` from `packfile_store_read_object_stream()` for direct packfile access (Jeff King confirmed the core refactoring is sound but noted: 1) removal of redundant delta checks is acceptable, 2) potential pre-existing issue with bad object marking in streaming path)
- Final fix replaces `odb_read_object_stream()` with direct packfile access in `pack-check.c`, preventing MRU-triggered infinite loops (Jeff King approves as "delightfully simple" but suggests improving test case specificity)
- Current test in `t1450-fsck.sh` verifies basic functionality with 200KB blobs across multiple packs but could be strengthened to:
  - Explicitly verify corruption detection when same object exists in multiple packs (one corrupt, one valid)
  - Demonstrate fsck correctly identifies which specific pack contains corruption
  - Avoid nondeterministic results based on MRU pack ordering
- Changes span `pack-check.c`, `packfile.c`, `object.c` with test coverage in `t1450-fsck.sh` and `t1050-large.sh`
- Fixes regression from c31bad4f7d (packfile MRU list changes)

Jeff King has reviewed all patches in the series, confirming the technical soundness of each change while providing focused feedback on test coverage and error handling. The core fix is complete but the test case could be enhanced to more rigorously verify the corruption detection behavior. The series follows Patrick Steinhardt's characteristic approach of careful structural refactoring before implementing the core fix, with all reviewer feedback addressed or acknowledged. Original failure case described as "broken" in commit messages is now fully resolved at the implementation level, with potential test improvements identified. The test helper correction noted in the latest email ensures proper error handling in the preparatory infrastructure changes.