This is a documentation and configuration fix follow-up to Junio's "What's cooking" report. Kristoffer Haugsbakk responds to Junio's note about their patch series `kh/format-patch-noprefix-is-boolean`, which fixes the `format.noprefix` configuration variable to behave as a proper boolean and documents this behavior.

The original patch series (2 commits) had already been reviewed and was marked as ready to merge to 'next' in Junio's report. Haugsbakk indicates they will prepare a new version of the patch, though the context doesn't make clear what changes are needed - this could be in response to feedback referenced in Junio's message (the `cf.` line pointing to another email) or simply to incorporate the changes into a new iteration before merging.

The change itself is relatively small but important for consistency - making `format.noprefix` behave like other boolean configuration options in Git. This affects the `git format-patch` command's output formatting when the `--no-prefix` option is used. The patch appears straightforward and well-contained, addressing both the implementation and documentation aspects.