This is a test case demonstrating a potential bug in how `receive.denyCurrentBranch=updateInstead` handles worktrees when the bare repository's HEAD points to an unborn branch. The test shows that even when a worktree is clean, pushes are incorrectly rejected if the bare repo's HEAD doesn't match the worktree branch.

The test creates a bare repo with a worktree, sets `updateInstead`, then points the bare repo's HEAD to an unborn branch while keeping the worktree clean. A push to the worktree branch fails with "Working directory has staged changes" despite the worktree being clean, suggesting `receive-pack.c` is incorrectly comparing against the bare repo's HEAD rather than the worktree's state.

The patch is well-constructed with a clear commit message explaining the issue and includes a `NEEDSWORK` comment documenting the expected failure. This appears to be a legitimate edge case in the worktree/updateInstead interaction that warrants investigation.