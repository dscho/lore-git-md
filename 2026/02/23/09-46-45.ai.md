This is a technical discussion about improving `git fsck`'s packfile verification, building on the earlier bugfix for an infinite loop during MRU list traversal. Jeff King (Peff) and Patrick Steinhardt are aligning their perspectives on the underlying architectural issues.

The conversation clarifies that there are two distinct but related problems: the immediate issue of MRU list corruption during iteration (fixed in the previous patch with a `skip_mru_updates` flag), and deeper concerns about verification accuracy when duplicate objects exist across packs. Peff notes that any iteration calling `find_pack_entry()` risks MRU modification, making the current solution fragile.

Patrick proposes two potential short-term solutions - either reverting to a separate iteration list (pre-MRU design) or extending `repo_for_each_pack()` with safer iteration semantics. His long-term vision involves eliminating `repo_for_each_pack()` entirely through the ongoing ODB abstraction work. Peff expresses preference for the double-list approach as less hacky than the current skip flag.

The discussion reveals important technical nuances: verification may incorrectly check duplicate objects from wrong packs, large objects follow different verification paths, and MRU modification risks exist in any iteration involving `find_pack_entry()`. Both contributors agree the issues predate the current bug but were exposed by it.

This email moves the thread forward by solidifying consensus on the architectural challenges while maintaining focus on verification correctness. The technical depth has increased regarding iteration semantics, with clear options presented for both immediate fixes and long-term solutions tied to Patrick's ODB abstraction work.