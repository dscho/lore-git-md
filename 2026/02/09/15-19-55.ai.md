This is a follow-up in the ongoing discussion about allowing UTF-8 characters in Git alias names, which currently only support ASCII alphanumerics and hyphens. The thread has evolved from the original proposal to consider broader config key design and security implications.

Jonatan Holmgren responds to Junio Hamano's cautious stance on the UTF-8 aliases proposal, focusing on implementation robustness. He suggests that Jeff King's proposed three-level config key approach (using subsections like `[alias "förgrena"]`) could leverage existing macOS normalization handling through `precompose_argv_prefix()`, avoiding the need for new Unicode infrastructure in Git. This would allow UTF-8 aliases while maintaining backward compatibility with the current ASCII-only syntax.

The email highlights two key paths forward: either extend the current alias syntax to support UTF-8 (with potential Unicode normalization challenges) or adopt Jeff King's subsection-based approach that sidesteps Unicode issues by treating command names as literal byte streams. Jonatan appears to favor the latter as it builds on existing config infrastructure and macOS's normalization handling, aligning with Junio's preference for solutions that minimize new bug surfaces.

This reply moves the discussion toward concrete implementation choices, with Jonatan seeking confirmation that the subsection approach would be technically sound. The thread remains open on whether to pursue targeted UTF-8 support or a config schema redesign, with security concerns and maintenance burden being key considerations. Junio's earlier comments suggest he may be more receptive to solutions that leverage existing macOS normalization rather than introducing comprehensive Unicode handling.