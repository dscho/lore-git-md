This is a continuation of the `the_repository` removal effort, with Junio Hamano weighing in on René Scharfe's patch to refactor `read_mmblob()`. The patch makes the object database dependency explicit by passing a `struct object_database *` parameter rather than implicitly using the global `the_repository->objects`.

Junio notes that while passing a full `struct repository *` would also work, René's approach of passing just the minimal required `object_database` is acceptable. The change propagates through several merge-related subsystems (`apply.c`, `checkout.c`, etc.) but doesn't alter their core logic. Patrick Steinhardt has already reviewed the patch positively, suggesting only that the three separable changes could have been split for clarity, but approving the combined version given its small size.

The patch follows René's characteristic style of small, focused refactorings that improve code hygiene by making dependencies explicit. With Patrick's approval and no outstanding objections, this appears ready to merge as part of the ongoing effort to eliminate `the_repository` usage. Junio's comment suggests he's inclined to accept it as-is, though he notes the alternative design choice of passing the full repository struct.