This is the third patch in a documentation series improving `git am`'s man pages and configuration. The patch adds documentation for the previously undocumented `am.messageId` configuration option, which controls whether `git am` adds a Message-ID trailer to commits based on the email header.

The patch follows the same style as the rest of the series, using `linkgit` markup for command references and linking to related `git-interpret-trailers` documentation. It clearly explains what the option does and connects it to the corresponding `--message-id` and `--no-message-id` command-line options.

As with the rest of the series from this frequent documentation contributor, the change is straightforward and well-formatted. The patch completes the documentation for this particular configuration option, bringing it in line with the command-line flags that were documented in earlier patches of the series. No technical concerns have been raised about this change in the thread so far.