Patrick Steinhardt's series to improve CI/build system reliability continues to evolve, with the latest discussion focusing on GitHub Actions' long-standing limitation that prevents arithmetic operations in YAML expressions. This came to light when Junio Hamano caught an invalid `${{matrix.nr + 1}}` syntax in the patch and reverted it from the 'next' branch.

Jeff King (Peff) provided crucial historical context, pointing to two prior solutions for this issue: using PowerShell (pwsh) syntax for arithmetic (as in commit d3d6493dcf) or moving the calculation into the script itself (17bd1108ea). Patrick has acknowledged the problem and is preparing a revised version, having set up test run #2195 to validate the changes.

The series aims to standardize test execution across GitHub Actions and GitLab CI, particularly for Windows/MSVC environments, by establishing uniform test slicing behavior and failure handling. Justin Tobler has reviewed all patches and approved the overall approach, though he suggested tighter patch grouping earlier in the thread. The core infrastructure improvements remain valid but now require careful platform-specific handling of GitHub's expression syntax constraints. Patrick will need to choose between the PowerShell-based or script-level arithmetic approaches when respinning the patch.