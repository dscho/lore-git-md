Follow-up discussion in the ODB abstraction thread about flag handling.

Justin Tobler confirms Patrick Steinhardt's fix in `builtin/fsck.c` correctly replaces a hardcoded `1` with the named `HAS_OBJECT_RECHECK_PACKED` flag. The original behavior (introduced in commit 9eb86f41de) was working by coincidence, as the value `1` happened to align with the desired flag bit. This validation comes after earlier discussion about enum safety and flag handling patterns in the ODB API.

The exchange shows the project's careful attention to making implicit behaviors explicit, particularly in the ongoing object database abstraction work. While this specific patch is straightforward, it sits within a larger technical discussion about type safety and maintainable API design for the ODB layer.