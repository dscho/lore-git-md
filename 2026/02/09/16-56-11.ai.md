This is a CI/build system patch from Patrick Steinhardt, the first in a 5-part series improving GitLab CI test infrastructure. The change fixes a subtle issue in the test-slice helper script where test failures were being masked due to pipe behavior in shell scripting.

The patch modifies `ci/run-test-slice.sh` to properly capture and handle exit codes from the test slicing logic. Previously, the test slicing was done in a pipeline that could silently swallow errors. The new version first stores the test list in a variable before processing it, ensuring any failures in the slicing step are properly detected and reported.

This is a small but important reliability improvement for the CI system, particularly relevant for Windows/MSVC testing where Patrick has been focusing attention. The change demonstrates his characteristic attention to robust infrastructure - even small details like proper error handling in helper scripts get addressed systematically. The fix will help ensure test failures are properly surfaced rather than being hidden by scripting quirks.