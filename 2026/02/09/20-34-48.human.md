This is a documentation issue report about `git clone` submodule options. Markus notes that while many projects recommend using `git clone --recursive` to initialize submodules, the official Git documentation only mentions `--recurse-submodules`. The email asks whether `--recursive` is outdated or should be documented.

The question touches on a common point of confusion - these two flags are actually aliases that do the same thing, but the documentation currently only shows one form. This is the kind of small but meaningful documentation gap that often gets fixed quickly once pointed out, since it affects a commonly used workflow (cloning repositories with submodules).

The patch would be straightforward - just adding a note that `--recursive` is an accepted alias. No technical changes needed, just documentation clarification. Since this is a simple, well-motivated report about a real documentation inconsistency, it's likely to get a quick response (probably confirming the aliasing behavior and suggesting a patch to document it).