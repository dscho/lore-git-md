This is a follow-up discussion about a bugfix in the ODB abstraction effort, specifically addressing incorrect flag usage in `builtin/backfill.c`. Justin Tobler confirms Patrick Steinhardt's earlier patch that fixed a mismatch between `OBJECT_INFO_FOR_PREFETCH` and `HAS_OBJECT_*` flags in the `fill_missing_blobs()` function.

The key insight is that while the original code worked by accident (due to bit patterns coincidentally matching the desired behavior), it was technically incorrect. Patrick's fix removes the incorrect flag passing entirely, letting the default behavior (which happens to be exactly what was wanted) take over. Justin verifies that passing `0` as the parameter correctly triggers both `OBJECT_INFO_QUICK` and `OBJECT_INFO_SKIP_FETCH_OBJECT` flags in the underlying `odb_read_object_info_extended()` call.

This exchange demonstrates the careful attention being paid to the ODB abstraction layer's API boundaries, even for cases where the current behavior happens to work. The discussion is technical but concise, with Justin providing clear confirmation that the fix achieves its intended purpose without introducing new complexity. The thread shows the project's commitment to correctness in the ongoing object database refactoring work.