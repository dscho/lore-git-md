This is a final polishing round for the ref backend selection series, which enables zero-downtime migrations between reference storage backends (files<->reftable) in large repositories. The series is now technically complete with all substantive feedback addressed, leaving minor documentation wording refinements.

Karthik Nayak responds to Patrick Steinhardt's review of v4, noting that almost all comments were minor nits. The series has undergone significant rework to properly support worktrees, with worktree references now stored in `ref_dir/worktrees/wt_id` and corresponding stubs created in `git_dir/worktrees/wt_id`. It also adds support for relative paths resolved relative to GIT_DIR.

The implementation provides two ways to specify the backend: via `extensions.refStorage` config (persistent) or `GIT_REFERENCE_BACKEND` environment variable (ephemeral, takes precedence). Core changes include URI parsing with strict `://` separator, worktree integration, and comprehensive test coverage in t1423-ref-backend.sh.

Junio Hamano has already acknowledged the series looks good, signaling maintainer approval. The remaining work focuses on final documentation polish, including terminology updates ("payload" instead of "location"), capitalization fixes, and wording refinements like changing "for more description" to "for more details". The series is in its final form after addressing feedback from multiple reviewers across architecture, URI syntax, worktree handling, and documentation.