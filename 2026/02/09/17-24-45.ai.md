This is a test modernization patch, part of the ongoing effort to replace legacy test patterns with Git's built-in test helpers. SoutrikDas updates `t/t7003-filter-branch.sh` to use `test_path_is_file`, `test_path_is_dir`, and `test_path_is_missing` instead of direct shell `test -f` and `test -d` commands.

The changes are straightforward mechanical replacements that improve test reliability and consistency. The patch converts six assertions across two test cases that verify file and directory states after filter-branch operations. The new helpers provide better error messages when tests fail and are part of Git's recommended testing style.

This appears to be a well-scoped, uncontroversial change that aligns with established project practices. The patch is small (6 lines changed) and focused entirely on test modernization without touching any production code or changing test behavior. It's likely be accepted with little discussion, though reviewers might suggest squashing the two test case updates into a single commit since they follow the same pattern.