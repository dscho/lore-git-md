This is a feature proposal for allowing UTF-8 characters in Git alias names, currently restricted to ASCII. The thread has evolved from Jonatan Holmgren's original RFC into a broader discussion about config key design and Unicode handling, with input from several core contributors.

Jonatan responds to Junio Hamano's cautious approval by suggesting a middle path: using Jeff King's proposed subsection syntax (`[alias "förgrena"] command = branch`) which would leverage existing config infrastructure that already handles arbitrary bytes in subsections. This approach avoids introducing new Unicode normalization code while still allowing non-ASCII aliases. 

The discussion highlights key technical constraints: macOS's precompose_argv_prefix() handling, security concerns raised by Theodore Tso regarding Unicode normalization (citing ext4 case folding CVEs), and Junio's strong preference for solutions that won't generate future bug reports. Jonatan's latest proposal appears to align with Junio's criteria by using battle-tested config parsing rather than adding new Unicode support.

The thread now has two viable paths forward: either adopt Jeff King's three-level config schema (which would require changes to alias syntax but avoid Unicode complexities) or proceed with Jonatan's subsection approach (which maintains backward compatibility while enabling UTF-8 aliases). Junio's involvement suggests this may soon reach a decision point.