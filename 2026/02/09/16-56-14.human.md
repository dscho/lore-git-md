This is the fourth patch in Patrick Steinhardt's series improving CI/build system reliability, focusing on test execution consistency across platforms. The patch aligns GitLab CI's Meson test execution with GitHub's approach by using the shared `ci/run-test-slice-meson.sh` script rather than open-coding the parameters.

The change makes the test slicing behavior identical between GitHub and GitLab CI systems when running Meson builds, continuing the series' theme of standardization. The patch is small but meaningful - it replaces a direct Meson invocation with a call to the centralized script, ensuring both CI systems handle test slicing the same way. This is particularly relevant for Windows/MSVC environments where GitLab CI runs.

The patch follows logically from the previous ones in the series, which standardized test slicing terminology and behavior. It demonstrates Patrick's thorough approach to infrastructure work, where even small inconsistencies between systems are addressed systematically. The change should make test failures more consistent and debuggable across different CI platforms.