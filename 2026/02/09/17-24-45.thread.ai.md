Test modernization patch from SoutrikDas updating `t/t7003-filter-branch.sh` to replace legacy shell test commands (`test -f`, `test -d`) with Git's built-in test helpers (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`). The change affects six assertions across two test cases that verify file/directory states after filter-branch operations. The patch follows established patterns for test modernization, providing better error messages while maintaining identical test behavior. Changes are mechanical and limited to test infrastructure with no production code impact. The small scope (6 lines changed) and uncontroversial nature suggest this will likely be accepted with minimal discussion, though reviewers may suggest combining the two test case updates into a single commit.