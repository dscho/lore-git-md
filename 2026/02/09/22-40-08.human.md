This is a technical discussion about Unicode normalization in the context of the ongoing UTF-8 alias names implementation. brian m. carlson raises concerns about Unicode normalization that weren't fully addressed in the original patch discussion.

The email points out that Unicode normalization (handling different byte sequences that represent the same logical character) is not just a macOS issue, contrary to Junio's suggestion. They provide concrete examples showing how composed and decomposed forms of accented characters (like French "où") could cause matching problems if not normalized. The discussion also touches on the complexity of different normalization forms (canonical vs compatibility) and notes that macOS's native normalization doesn't match standard Unicode forms.

This is a substantive technical concern that could affect the reliability of UTF-8 alias matching across different platforms and input methods. While the original patch avoided Unicode normalization by treating names as literal byte streams, this email suggests that approach might lead to user-visible inconsistencies when the same logical alias is written in different normalized forms.

The email comes from a knowledgeable contributor with experience in character encoding issues (they previously worked on Git's hash-algorithm-agnostic internals). Their concerns merit serious consideration, though it's unclear from this single email whether they're suggesting the patch should be modified to include normalization or just documenting the limitation. This could spark further discussion about whether and how to handle normalization in the alias implementation.