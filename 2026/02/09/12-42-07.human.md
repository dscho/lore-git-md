This is a test infrastructure patch from Patrick Steinhardt, part of a series addressing CI failures on Windows/MSVC platforms related to `iconv(1)` availability. The patch focuses on fixing two tests in t6006 that incorrectly assumed the `iconv` executable would be present when testing commit message encoding.

The changes make the tests more robust by using UTF-8 encoding when ICONV support isn't available, rather than failing when the `iconv` executable is missing. The patch restructures the test setup to conditionally use iconv` only when the ICONV prerequisite is met, while maintaining the same test coverage. This continues Patrick's broader effort to improve test reliability across different platforms, particularly for Windows environments where toolchain differences often expose test assumptions.

The patch is well-structured and follows the pattern established in earlier parts of the series, showing careful attention to maintaining test behavior while removing platform-specific dependencies. As with the rest of the series, this appears to be a straightforward improvement to test infrastructure with no obvious downsides.