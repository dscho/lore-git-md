This is a follow-up discussion in the ongoing ODB abstraction thread, specifically addressing flag handling in `builtin/fsck.c`. Justin Tobler confirms Patrick Steinhardt's analysis that passing `1` to `has_object()` in `mark_object()` was coincidentally correct, mapping to `HAS_OBJECT_RECHECK_PACKED` - the intended behavior for skipping promisor object fetches when verifying repository integrity. 

The exchange validates Patrick's patch converting the hardcoded value to the named flag, with Justin noting this makes the intention explicit rather than relying on bitmask alignment. This continues the thread-wide focus on precise flag semantics in the ODB API, though here the discussion is more narrowly focused on confirming a specific fix rather than debating broader design questions.

The technical context includes the earlier-established pattern where `HAS_OBJECT_FETCH_PROMISOR` must be explicitly passed to fetch promisor objects, and the validation that `HAS_OBJECT_RECHECK_PACKED` alone suffices for fsck's corruption-checking needs. This patch represents incremental progress in the larger ODB abstraction effort by replacing an implementation-dependent value with its semantically equivalent named constant.