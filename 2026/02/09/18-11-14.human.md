Test modernization patch from SoutrikDas, reviewed by Junio. Updates `t/t7003-filter-branch.sh` to replace shell test commands (`test -f`, `test -d`) with Git's built-in test helpers (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`). The changes affect assertions verifying file/directory states after filter-branch operations.

Junio notes the new checks are actually stricter than the original ones - the old tests would pass if the path existed but was the wrong type (e.g. a file instead of directory), while the new helpers require the path to be completely missing. He suggests the author should document in the commit message whether they've verified this stricter behavior is correct for these test cases.

This is a straightforward test infrastructure improvement following established patterns. The patch is small (6 line changes) and focused, with no impact on production code. Given Junio's generally positive review and the uncontroversial nature of the change, this will likely be accepted after any minor commit message clarifications.