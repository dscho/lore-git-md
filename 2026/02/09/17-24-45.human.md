Test modernization patch updating path existence tests in t7003-filter-branch.sh.

This is a straightforward test cleanup patch from SoutrikDas, part of the ongoing effort to modernize Git's test suite. The changes replace direct shell `test -f` and `test -d` commands with Git's dedicated test helper functions (`test_path_is_file`, `test_path_is_missing`, `test_path_is_dir`). These helpers provide more descriptive output when tests fail.

The patch touches 6 lines across two test cases in t7003-filter-branch.sh, converting:
- File existence checks (`test -f` -> `test_path_is_file`)
- Directory existence checks (`test -d` -> `test_path_is_dir`)
- Negative checks (`! test -f` -> `test_path_is_missing`)

The changes are purely mechanical and don't affect test behavior - they just make failures easier to diagnose. This appears to be a clean, well-scoped contribution from a new contributor (the GSOC tag suggests this may be part of Google Summer of Code work). The patch follows the established pattern for such modernizations seen across the test suite.