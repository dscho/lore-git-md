This is a minor administrative note from Junio Hamano about patch formatting consistency, not a technical review of the submodule `ignore=all` behavior changes. 

Junio observes that the sign-off line in this version of the series (v4 1/5) lacks a space before "(Eficode)" that was present in previous iterations. He notes this is purely an observation - the author's identity has consistently been without the space in the `From:` line, so this change actually makes the sign-off match the author line formatting. 

This comes in the context of a mature patch series (now at v4) that has already addressed all substantive technical feedback. The series aligns `git add`'s handling of ignored submodules with `git status` and `git diff` behavior, introducing a `--force` option to override the default skipping of ignored submodules. All implementation, testing, and documentation components are complete, with only minor administrative points like this formatting observation remaining before potential merging.