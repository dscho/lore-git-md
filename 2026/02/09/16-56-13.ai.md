CI/build system patch continuing Patrick Steinhardt's series to standardize test execution across platforms. This third patch aligns the Meson build system's test slicing behavior with the Makefile approach by switching from zero-based to one-based indexing, matching the change made in the previous patch. The update modifies both the GitHub workflow configuration and the Meson test runner script to use consistent numbering.

The changes are straightforward - adjusting the index calculation in `.github/workflows/main.yml` and removing the `+1` offset in `ci/run-test-slice-meson.sh`. Patrick notes this doesn't yet affect GitLab CI, which will be addressed in a subsequent patch. This continues the series' focus on eliminating subtle inconsistencies in test execution that could mask failures or produce different behavior across build systems.

Key technical details for future reference: the patch touches GitHub Actions workflow files and Meson test slicing logic, standardizing on one-based indexing for test slices. The `--slice` argument format remains "$slice_number/$total_slices" but now interprets both numbers as one-based. The changes are part of a broader effort to make CI failures more debuggable by ensuring consistent test execution across platforms.