This is the final patch in a documentation series improving `git am`'s man pages, focusing on hook-related content. Kristoffer Haugsbakk completes the series by clarifying how `--verify` and `--no-verify` interact with Git hooks during patch application.

The patch makes several documentation improvements: it adds the positive `--verify` form to the synopsis (previously only `--no-verify` was shown), rewords the hook section to lead with the default behavior rather than the negation, and explicitly notes that `post-applypatch` cannot be skipped. The changes follow established documentation patterns, referencing prior work on synopsis style standardization.

The motivation explains that while historically only the negated forms were documented (since hooks run by default), there are real use cases for explicitly enabling hooks - for example, when overriding a `--no-verify` setting from a Git alias. The patch maintains consistency with other command documentation while adding this missing piece.

This concludes a well-structured series that methodically improved `git am` documentation, touching on command reference formatting, trailer behavior, configuration options, and now hook handling. The changes are mechanical but meaningful, following patterns established in other parts of Git's documentation.