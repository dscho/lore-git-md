This is a technical follow-up discussion about flag handling in the ODB (object database) abstraction effort, specifically addressing a bugfix patch from Patrick Steinhardt that was already merged. Justin Tobler confirms the correctness of Patrick's fix in `builtin/backfill.c`, where passing `0` as the flag value to `odb_has_object()` correctly triggers the desired behavior in `odb_read_object_info_extended()` (setting both `OBJECT_INFO_QUICK` and `OBJECT_INFO_SKIP_FETCH_OBJECT` flags).

The exchange highlights how the current implementation accidentally worked due to bitmask alignment, with Justin noting the importance of making this explicit rather than relying on implementation details. This discussion occurs within the broader context of ongoing work to improve type safety around ODB flags, where compiler warnings have proven insufficient for catching all enum misuse cases. The thread demonstrates careful validation of a subtle interaction between the object database API layers, with contributors cross-checking each other's analysis of low-level flag behavior.