This is a documentation patch from the Git maintainer Junio C Hamano, making two small but meaningful improvements to the `git-rerere` man page.

The changes clarify ambiguous wording in the manual:
1. Replaces "it" (referring to users) with the more direct "users" to avoid confusing pronouns
2. Makes explicit that `rerere forget` operates on paths matching the given pathspec, not literal filename patterns (important since `*.c` is a glob pattern, not a direct filename match)

The patch is straightforward and uncontroversial - a typical maintainer-quality documentation tweak that improves precision without changing functionality. As with most documentation patches from Junio, it demonstrates attention to detail in how concepts are presented to users. The change is minimal (4 lines touched) and focused entirely on improving clarity in the command's documentation.