This is a CI/build system patch completing a series to improve test reliability in Windows/MSVC builds. Justin Tobler reviews the fifth patch, which adds failure handling for MSVC+Meson test jobs in GitLab CI, mirroring GitHub Actions' behavior.

The patch ensures failed tests are properly reported and artifacts collected when running Meson-based tests on Windows. It creates a dedicated test output directory, configures Meson to use it, and adds an after-script that prints test output and moves failure artifacts to a standard location when jobs fail. The artifacts are then uploaded as CI artifacts only on failure.

Justin confirms the patch looks good, noting it completes the series' goal of consistent test handling across CI platforms. The changes are specific to GitLab's Windows/MSVC configuration, touching `.gitlab-ci.yml` to set up the test environment and artifact collection. This follows the series' pattern of first fixing core behaviors (earlier patches standardized test slicing) then propagating improvements to all CI systems.

The technical details include setting `TEST_OUTPUT_DIRECTORY` in PowerShell, configuring Meson with `-Dtest_output_directory`, and using Git's bash to run test helper scripts. The artifact collection mirrors GitHub Actions' approach while accounting for Windows path handling. This demonstrates Patrick's methodical infrastructure work and Justin's thorough review process confirming the changes achieve their goal without introducing new issues.