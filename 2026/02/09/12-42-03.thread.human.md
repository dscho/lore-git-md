This thread addresses test failures on Windows/MSVC platforms when Git is built with ICONV support but the `iconv(1)` executable isn't available. Patrick Steinhardt has led the effort to modify test expectations rather than skip tests in these cases, a solution that has gained consensus from Junio Hamano and other reviewers.

The discussion has clarified several technical points: the distinction between libiconv (linked into Git) and the `iconv(1)` executable (used in scripts), Windows-specific considerations where MinGW no longer ships the binary, and the relationship between build-time `NO_ICONV` and test-time `ICONV` prerequisites. Christian Couder has provided key feedback, identifying a documentation gap about this relationship that should be addressed, though he suggests it could be handled in a follow-up. His latest comments focus on minor polish items - fixing a typo in the cover letter and referencing GitHub issue #6083 for additional context about `iconv(1)` in Git for Windows.

With all major technical questions resolved and only documentation-related loose ends remaining, the series appears ready for Junio to pick up. The solution preserves test coverage while accommodating platform differences, particularly important for Windows/MSVC environments where the `iconv(1)` executable may not be present despite Git having ICONV support.