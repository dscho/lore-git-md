This is a correction to Junio Hamano's "What's cooking" report, fixing two misattributed expectations about pending updates. The first correction clarifies that the `aa/add-p-no-auto-advance` topic (adding a `--rework-with-file` flag to `git add -p`) is awaiting a final reroll, not a gitk update. The second correction properly associates the gitk update expectation with the `ps/meson-gitk-git-gui` topic, which wires up gitk/git-gui builds in the meson build system.

These are minor but important clarifications to keep track of what's pending in the integration branches. The corrections don't change the technical content of either topic - they just ensure the status tracking is accurate. The `add-patch` changes and meson build improvements continue progressing toward inclusion.