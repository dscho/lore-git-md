This is a discussion about parallel hook execution, part of Adrian Ratiu's ongoing work to make Git hooks more configurable. Patrick Steinhardt raises several concerns about the current implementation's approach to parallelization:

1. **Priority of settings**: He questions whether compile-time parameter should override user configuration, though later acknowledges it makes sense for serial-only hooks.

2. **Default behavior**: He's skeptical about defaulting to `online_cpus()` for parallel execution, suggesting this assumes too much about hook behavior and could lead to races if hooks modify shared state.

3. **Opt-in vs opt-out**: Patrick suggests parallel execution should be opt-in rather than opt-out for safety.

4. **Hook-specific control**: He proposes a more granular approach where each hook declares its parallelizability via config (e.g., `hook.my-hook.parallel=true`), allowing serial and parallel hooks to coexist safely.

The discussion reveals tension between convenience and safety in hook execution. Patrick acknowledges the current design's rationale for serial fallbacks but suggests a more flexible approach that puts control in users' hands while maintaining safety through explicit declarations. This feedback will likely shape the next iteration of the parallel hooks feature.