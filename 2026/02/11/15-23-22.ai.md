This is a technical discussion about the `--revert` functionality being added to `git replay`, specifically focusing on whether the command should respect the `revert.reference` configuration setting. The thread context shows this is part of a larger effort to enable server-side reverts for GitLab's Gitaly service.

Kristoffer Haugsbakk questions Toon Claes' suggestion that `git replay --revert` should obey the `revert.reference` config like `git revert` does. He raises a fundamental question about plumbing commands and configuration - whether they should ignore user configs for consistency or require explicit opt-out via `/dev/null` config files. This touches on Git's broader philosophy around plumbing vs porcelain commands.

The discussion occurs in the context of patch v2 which refactored revert message formatting into a shared function (`sequencer_format_revert_header()`). The implementation currently differs between interactive (`git revert`) and non-interactive (`git replay`) contexts, with the latter using full OIDs unconditionally. The thread shows the project grappling with how much behavior should be shared between these related but distinct use cases.

This exchange highlights an architectural decision point that could affect future extensions - whether to unify behavior via config respect or maintain distinct modes. The question remains open, with implications for both the current revert functionality and potential future shared infrastructure between sequencer and replay operations.