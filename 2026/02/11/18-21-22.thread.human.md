This thread centers on Tian Yuchen's proposal to harden Git's repository discovery by validating `.git` file types, particularly to prevent unsafe attachment to parent repositories when `.git` entries are malformed (like FIFOs). The current patch (v2) implements Junio Hamano's suggestion to move the `lstat()` check after the happy-path directory test, now either failing explicitly or returning `GIT_DIR_INVALID_GITFILE` for invalid cases.

Junio initially questioned the security severity, noting that attackers with `.git` access likely have broader privileges, but acknowledged the change could help detect filesystem corruption. He suggested refining error handling in `read_gitfile_gently()` to distinguish non-error conditions (like `STAT_ENOENT` or `IS_A_DIR`) from fatal cases. brian m. carlson raised compatibility concerns, emphasizing Git's historical support for `.git` symlinks (used in early submodules) and cautioning against over-constraining without clear security justification. Junio later agreed symlink support should remain unless proven unsafe, proposing to reuse existing `stat()` calls for validation rather than adding separate `lstat()` checks.

In the latest exchange, brian m. carlson clarified their reference to past security issues (like commit bb6832d552, which warned about symlinked dotfiles opened with `O_NOFOLLOW`), noting this was context rather than a direct parallel. The discussion remains focused on balancing security hardening with backward compatibility, particularly around symlinks. The thread appears to be winding down, with consensus on preserving symlink support while optimizing validation. Key unresolved questions center on error handling granularity and whether to reuse `stat()` calls, but the core security vs. compatibility tradeoffs have been thoroughly explored.