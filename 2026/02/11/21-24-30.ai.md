Maintainer feedback on pre-add hook naming and formatting. Junio Hamano clarifies that his earlier comment about "pre-add" not looking good referred specifically to documentation formatting choices (e.g., whether to use backticks, single quotes, or underscores) rather than the conceptual naming itself. He notes that "pre-add" is the natural parallel to existing "pre-commit" terminology since it hooks into `git add` operations.

This email advances the naming discussion by:
1. Confirming the maintainer sees "pre-add" as semantically appropriate (matching the command it hooks into)
2. Narrowing the formatting question to documentation style rather than concept naming
3. Implicitly rejecting the "pre-staging" alternative terminology suggested by the author

The thread remains technically resolved but now focuses on finalizing documentation presentation details. Junio's position suggests the hook will keep the "pre-add" name with only markup styling remaining to be decided. This moves the series closer to merge readiness as the last documentation polish is typically straightforward to address.