This is a build system patch continuing the discussion about ensuring `config-list.h` stays in sync with Git's documentation files. Phillip Wood weighs in on Patrick Steinhardt's proposed solution for handling dependencies in the Meson build system.

The discussion focuses on how to properly track when documentation changes should trigger a rebuild of `config-list.h`, which contains generated configuration options. Steinhardt had suggested a two-pronged approach: using a `build_always_stale` target to detect file additions/removals, combined with a depfile to track content changes. Wood expresses concern about regenerating the depfile on every build, preferring to minimize unnecessary rebuilds.

Wood notes that changes to `Documentation/config.adoc` (which references all config files) should naturally trigger rebuilds when new files are added, while deletions would be caught by existing dependencies. The exchange shows two build system experts collaborating to find a balance between correctness and build performance, with Steinhardt's latest patch demonstrating working behavior across all modification scenarios (content changes, file additions, and deletions).

The technical discussion remains focused on build infrastructure without touching core functionality, with both approaches aiming to solve the same underlying problem through different dependency tracking mechanisms. No major objections have emerged to either solution, suggesting the thread is converging toward a robust fix.