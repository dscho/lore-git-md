This is a technical discussion about refactoring revert message formatting in the `git replay` command's `--revert` functionality. The conversation focuses on how to share commit reference formatting between `sequencer.c` and `replay.c` while maintaining their different use cases.

The key points are:
1. The current implementation extracts revert message header formatting into `sequencer_format_revert_header()` but doesn't share the `refer_to_commit()` logic due to differing requirements between interactive (sequencer) and non-interactive (replay) modes.
2. Patrick Steinhardt suggests making the formatting configurable via flags rather than `replay_opts` to potentially unify behavior while keeping implementations separate.
3. Toon Claes asks for clarification on whether this would involve minimal changes to pass flags or a larger refactor of `replay_opts`.
4. The discussion indicates the patch series is otherwise ready for v3, with Toon offering to help resolve any remaining questions before submission.

The technical considerations center around:
- Maintaining separation between sequencer and replay implementations
- Whether to respect `revert.reference` config in non-interactive contexts
- The tradeoff between parameter passing approaches (flags vs structs)
- Keeping the change scope manageable for the immediate needs

This appears to be a refinement discussion for an otherwise well-reviewed series that's part of the larger `git replay` feature effort. The participants are working through the final design details before the next version submission.