This is a brief reply in an ongoing discussion about performance improvements to `git config list --type=<X>` filtering. The thread centers on Derrick Stolee's RFC patch series that would make `git config list` properly filter values by type during parsing rather than after, addressing a performance bottleneck in git-credential-manager.

Patrick Steinhardt weighs in to say he doesn't consider the behavior change problematic, viewing the current state where `--type` is accepted but ignored as more of a bug than a feature. His position aligns with the series' direction, seeing the change as acceptable despite potentially affecting existing tools that might be passing `--type` to `git config list`.

The comment is brief and supportive, not raising new technical concerns or design questions. It comes after Junio C Hamano had engaged with deeper questions about Git's config value typing model, making this a relatively minor voice in the broader discussion. The key open question remains whether to filter non-matching values (Stolee's approach) or show them as strings (more aligned with Git's string-based config philosophy).