This is a technical discussion about refactoring commit message formatting in the `git replay --revert` feature. Patrick Steinhardt clarifies his earlier suggestion about using flags rather than a struct parameter for the `sequencer_format_revert_header()` helper function. He confirms to Toon Claes that the change would be limited to the function parameters, not a broader restructuring of the `replay_opts` struct.

The exchange is brief but shows the implementation progressing toward v3 of the patch series, where they're working out details of how to share commit reference formatting logic between `sequencer.c` and `replay.c`. The discussion remains focused on implementation details rather than design questions, suggesting the architectural approach is now settled.