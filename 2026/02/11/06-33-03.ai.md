This is a follow-up in Patrick Steinhardt's CI/build system series addressing test slicing consistency. The discussion centers on GitHub Actions' long-standing limitation that prevents arithmetic operations in YAML expressions (`${{matrix.nr + 1}}`), which Junio Hamano caught when the patch failed validation.

Jeff King (Peff) provides historical context, pointing to two prior solutions: using PowerShell (pwsh) syntax for arithmetic (d3d6493dcf) or moving the calculation into the script itself (17bd1108ea). Patrick acknowledges the issue and indicates he'll revise the patch to use one of these approaches, having now set up a test run via GitHub pull request #2195 to validate the changes.

The exchange confirms the technical path forward while highlighting the need for platform-specific handling of arithmetic in CI configurations. This patch remains part of the broader effort to standardize test execution across GitHub Actions and GitLab CI, particularly for Windows/MSVC environments, but must accommodate GitHub's expression syntax constraints. The solution will likely involve reverting to either PowerShell-based arithmetic or script-level adjustments as seen in prior commits.