This is a review response in an ongoing thread about shallow repository handling improvements. Patrick Steinhardt is providing feedback on Samo Pogačnik's v4 patch series, specifically addressing relative-depth fetching (`--deepen`) in shallow clones.

Patrick raises several technical concerns about the implementation details in `shallow.c` and `upload-pack.c`:

1. He suggests declaring a loop variable `j` inline rather than at function scope since it's only used in one loop.
2. He notes unnecessary braces around conditional checks that could be simplified.
3. He questions why certain flag-setting behavior differs when the `shallows` parameter is provided, particularly around updating the result list and setting `shallow_flag`.
4. He recommends inlining the `get_shallows_depth()` helper function since it only has one caller.
5. He points out an awkward double-call pattern where `get_shallow_commits()` is called twice in sequence with related parameters.

The review maintains a constructive tone while identifying areas where the implementation could be more straightforward. This comes after multiple previous review rounds where other contributors (Junio C Hamano, Kristoffer Haugsbakk) had already provided feedback on documentation and style issues. The patch series appears to be in its final stages, with these being some remaining technical refinements before potential integration.