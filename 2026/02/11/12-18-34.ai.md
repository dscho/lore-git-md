This is a feature proposal discussion about exposing repository paths through a new `git repo info` interface, positioned as a more discoverable alternative to `git rev-parse` for repository metadata.

Patrick Steinhardt supports Pushkar Singh's RFC to add path information to `git repo info`, seeing it as a natural evolution of the command's purpose to provide structured repository metadata. He notes that `git rev-parse` has accumulated functionality beyond its original revision-parsing scope and agrees this belongs in `git repo` instead. The thread's open question about absolute vs relative path reporting remains unresolved, with Patrick recalling that Lucas Skeggs had previously suggested configurable path resolution but not detailing that proposal here.

The exchange suggests alignment on the general direction while leaving technical specifics like path formatting and scope (starting with just `--git-dir` vs including other paths) for further discussion. Patrick's response indicates this is part of a longer-term effort to rationalize Git's command structure, with `git repo` positioned as the future home for repository introspection features that have outgrown `git rev-parse`. The tone is collaborative, with Patrick providing historical context about the command's original purpose while deferring on implementation specifics.