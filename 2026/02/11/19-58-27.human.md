This is a follow-up discussion in the ongoing thread about Meson build system dependency tracking for `config-list.h`. D. Ben Knoble is testing Patrick Steinhardt's proposed solution and reporting some observed behavior that differ from expectations.

The key observations are that while the build system correctly rebuilds when touching core files like `git.c`, it shows confusing behavior with documentation changes. Specifically, Knoble notes that `config-list.h` doesn't automatically rebuild when adding a new config documentation file (`fooconfig.adoc`), though it does rebuild when modifying existing files. The output also appears incomplete ("[1/28]") during some rebuild attempts, making it hard to diagnose what's happening.

Knoble also shares practical motivation for fixing this - they encountered test failures during bisection due to the header becoming stale, requiring manual rebuilds. The discussion remains technical and constructive, with Knoble carefully documenting their test cases to help identify any remaining edge cases in the dependency tracking solution.

This email doesn't introduce new architectural changes but provides valuable real-world testing feedback on the proposed solution. The thread appears to be converging toward a robust implementation, with participants working through the nuances of Meson/Ninja's dependency handling behavior.