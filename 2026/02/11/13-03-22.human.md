This is a technical discussion about refactoring revert message formatting in the `git replay` command's `--revert` feature. The thread focuses on how to best share commit reference formatting logic between `sequencer.c` and `replay.c`.

The key points are:
1. There's debate about whether to make the revert message formatting respect the `revert.reference` config setting in both `git-revert` and `git-replay`, which would require unifying how commit references are formatted
2. Patrick Steinhardt suggests using a flags parameter rather than passing the full struct replay_opts to make the shared function more flexible
3. Toon Claes clarifies this would only apply to the formatting functions, not a complete revamp of replay_opts
4. The discussion is part of preparing a v3 of the patch series, with Toon offering to help resolve any remaining questions

The exchange shows careful consideration of API design boundaries and configuration consistency between related commands. The technical discussion appears focused and productive, with clear next steps toward finalizing the implementation.