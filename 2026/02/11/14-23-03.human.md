This is a follow-up on an object database (ODB) cleanup series that was mentioned in Junio Hamano's "What's cooking" report. Patrick Steinhardt is checking in about whether to proceed with merging his three-patch series that fixes flag bit usage in the ODB API.

The series addresses two specific bugs where incorrect flags were being passed to `odb_has_object()`, along with a cleanup to eliminate gaps in the object info flag values. The technical discussion around whether to use an enum type for the flags appears to have stalled without clear consensus.

Patrick is asking Junio whether he should:
1. Reroll the series to convert the flags to an enum (a more substantial change), or
2. Proceed with merging the current version (smaller, focused fixes)

This represents the tail end of a technical discussion about API design choices in Git's object storage layer. The patches themselves look like straightforward correctness fixes, but the enum question touches on broader API design preferences. The thread shows the typical pattern where a contributor checks in with the maintainer when a discussion hasn't reached clear consensus.