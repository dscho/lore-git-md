Technical refinement to the `git replay` series focusing on configuration handling philosophy. Junio Hamano clarifies that plumbing commands like `git replay` should ignore user configurations by default, ensuring consistent behavior regardless of local settings. This responds to Kristoffer Haugsbakk's question about whether plumbing commands should respect configs like `revert.reference` (which `git revert` does) or require explicit opt-out.

The exchange highlights an important architectural principle as the `--revert` functionality is being added to `git replay`. While the interactive `git revert` command follows user preferences, plumbing commands are expected to behave predictably without configuration influence. This distinction maintains consistency with Git's plumbing/porcelain separation.

This is a brief but significant clarification that will guide the final implementation of the `--revert` feature, ensuring it aligns with Git's conventions for low-level commands. The position reflects Junio's role as maintainer in establishing these architectural boundaries.