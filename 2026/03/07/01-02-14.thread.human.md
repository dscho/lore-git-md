This thread documents the successful effort to improve Git's line-log functionality (`git log -L`) by routing its output through the standard diff pipeline, enabling previously unsupported features like `--word-diff`, `--color-moved`, and pickaxe options (`-S`, `-G`). The series was authored by Michael Montalbo and has now been fully merged after thorough review.

The core change removed the special-case `dump_diff_hacky()` function in favor of using Git's standard `builtin_diff()` machinery, while carefully preserving the line-range filtering behavior that makes `-L` useful. The series included fixes for edge cases (like handling renames with pickaxe options), comprehensive test coverage for the newly enabled features, and clear documentation updates noting both the expanded capabilities and remaining limitations (such as unsupported formats like `--raw` and `--stat`).

After merging, Kristoffer Haugsbakk spotted a minor documentation formatting issue - incorrect indentation in the AsciiDoc source for the newly added options list. Michael Montalbo promptly acknowledged this and will send a correction. The thread demonstrates Git's collaborative attention to detail, where even post-merge improvements to documentation formatting receive prompt attention. With all technical changes merged and this final documentation tweak pending, the series is now complete.