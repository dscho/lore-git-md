This is a test modernization patch in an ongoing thread about improving debuggability in `t3310-notes-merge-manual-resolve.sh`. Francesco Paparatto's v3 patch replaces command substitutions with more robust test helpers, and Eric Sunshine has provided final review feedback with two optional style suggestions.

The changes focus on making test failures more debuggable by avoiding hidden exit codes from `git rev-parse` in command substitutions. The v3 version already addresses all technical requirements, converting patterns like `test_grep -q "$(git rev-parse...)"` to explicit variable assignments or temporary files. Eric's latest review suggests two stylistic improvements: using shell variables instead of temporary files for rev-parse output, and consolidating multiple writes with `test_write_lines`.

Francesco indicates he'll incorporate these suggestions in a v4, though they're marked as non-blocking. The thread demonstrates Git's test suite improvement priorities - maintaining identical coverage while making failures easier to diagnose. With all substantive feedback addressed across three iterations, this appears ready for inclusion once the optional style tweaks are applied.