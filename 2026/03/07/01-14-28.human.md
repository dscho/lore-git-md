Junio C Hamano reports a regression in the leak detection CI job that appears after applying Jeff King's patch to enable `NO_MMAP` for LSan builds. The bisect points to an earlier commit by Patrick Steinhardt that refactored the object streaming logic. 

This is a false positive in the leak detection - the "leak" being caught is actually the expected behavior of the `NO_MMAP` emulation layer (which uses malloc/free instead of mmap/munmap). The issue surfaces because the test wasn't accounting for this difference in memory management behavior between real mmap and the emulation layer.

Junio provides a test script that reproduces the issue and notes this doesn't indicate a real problem with either patch - the series remains technically sound. This appears to be a minor CI configuration quirk rather than a substantive issue with the memory leak fixes. The thread had already reached consensus on the technical approach before this observation.