This is a follow-up discussion about a proposed feature to detect and abort slow Git clones. The original patch suggested adding a `--no-turtle-speed` option to terminate clones when network throughput drops below a threshold, motivated by GitHub load balancing occasionally directing connections to slow servers.

brian m. carlson (a GitHub employee, though clarifying they're participating personally here) provides additional context about GitHub's infrastructure behavior. They explain that GitHub doesn't throttle speeds mid-operation and suggests the root cause might be network issues between the user's ISP and GitHub rather than server-side throttling. They recommend opening a GitHub support ticket with detailed tracing output (`GIT_TRACE=1` etc.) to help diagnose the specific connectivity problem.

While still supportive of the general idea of a speed-based clone abort feature (now framed as `--min-speed`), brian suggests the immediate issue might be solvable through GitHub support channels. The response shifts focus from the patch implementation details to troubleshooting the underlying network problem, while keeping the door open for the feature's usefulness in other contexts.

The exchange highlights how platform-specific infrastructure knowledge can inform Git feature discussions, and shows a maintainer going beyond code review to help diagnose a user's real-world problem. The thread remains constructive, with the feature proposal still in play but now with clearer context about when it would (and wouldn't) be needed.