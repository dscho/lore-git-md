This is a test modernization patch in the ongoing effort to improve Git's test suite reliability and debuggability. The patch updates `t/t3310-notes-merge-manual-resolve.sh` to replace fragile command substitutions with more robust test helpers.

Eric Sunshine provides final review feedback on Francesco Paparatto's v3 patch, which has already addressed previous concerns about proper attribution and clean application. The changes replace patterns like `test $(git rev-parse...)` with explicit variable assignments and dedicated test helpers (`test_cmp_rev`, `test_grep`), making test failures more debuggable by showing expected vs actual output.

Sunshine suggests two minor improvements: using shell variables` instead of temporary files for storing rev-parse output, and potentially consolidating multiple writes with `test_write_lines`. The feedback is constructive but acknowledges these are refinements rather than blockers - the core improvements in the patch are sound.

This represents the final polish on a straightforward test modernization that follows Git's established patterns. The discussion shows the project's attention to both technical correctness and commit hygiene, with Sunshine's review catching subtle issues like preserving command exit codes that might otherwise be lost in command substitutions.