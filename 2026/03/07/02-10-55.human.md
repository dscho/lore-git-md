This is the final clarification in a completed patch series that improves Git's line-log functionality (`git log -L`). The series routes line-log output through Git's standard diff pipeline, enabling previously unsupported features like `--word-diff`, `--color-moved`, pickaxe options, and others to work with `-L`.

The maintainer (Junio C Hamano) confirms that explicitly documenting the remaining unsupported diff formats (like `--raw` and `--stat`) is the right approach, rather than trying to implement support for them. The author (Michael Montalbo) initially misunderstood this point but quickly clarified their understanding.

The series is now complete with all planned patches merged, including:
1. A fix for crashes when combining `-L` with pickaxe options and renames
2. The core change removing the hacky diff implementation
3. Comprehensive test coverage
4. Final documentation updates

This represents significant progress in unifying Git's diff machinery while maintaining backward compatibility. The changes enable powerful new combinations of line-log with existing diff features, though some edge cases (like non-patch diff formats) remain intentionally unsupported. The maintainer has expressed enthusiasm for the direction of this work.