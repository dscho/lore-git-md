This is the final clarification in a completed patch series that improves Git's line-log functionality (`git log -L`) by routing its output through the standard diff pipeline. The series enables previously unsupported diff features like `--word-diff`, `--color-moved`, pickaxe options (`-S`, `-G`), and others to work with `-L`.

The author (Michael Montalbo) and maintainer (Junio C Hamano) are finalizing documentation details about which diff formats remain unsupported with `-L`. The maintainer confirms that explicitly noting these limitations (like `--raw` and `--stat` not working with `-L`) is valuable, and the author clarifies a minor point about the phrasing. This represents the last polish on a series that has already been technically completed, with all four patches merged.

The series is a careful balance between expanding functionality while maintaining backward compatibility, with thorough test coverage and performance measurements showing no regression. The maintainer has expressed enthusiasm for the direction, and the only remaining discussion is about how best to document the intentional limitations.