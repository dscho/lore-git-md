This is a follow-up discussion in an ongoing thread about memory leaks in Git's mmap handling, particularly around the `NO_MMAP` compatibility layer. The thread has evolved from the initial bug report to now include investigation of a separate streaming subsystem leak.

Junio confirms Ramsay Jones's clarification about potential double-munmap issues in the `NO_MMAP` case. Ramsay notes that most `munmap()` calls don't check return values, which could mask problems when the compatibility layer is active. This isn't directly related to Jeff King's patch series (which addresses different mmap leaks), but provides useful background about edge cases in the mmap emulation code.

The exchange shows the thread remains technically focused, with participants carefully distinguishing between the already-fixed pack index leaks and the newly discovered streaming issue. Junio's response indicates the discussion is moving toward resolution, with Jeff's series likely ready once the streaming leak is addressed separately. The tone remains collaborative, with Ramsay providing historical perspective and Junio synthesizing the technical points.