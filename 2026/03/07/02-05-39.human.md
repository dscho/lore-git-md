This is the maintainer's final review comment on a completed patch series that improves Git's line-log functionality (`git log -L`). The series successfully routed line-log output through Git's standard diff pipeline, enabling previously unsupported features like `--word-diff`, `--color-moved`, and pickaxe options to work with `-L`.

Junio clarifies a point about documenting unimplemented features, confirming that the current approach of explicitly noting which diff formats remain unsupported (like `--raw` and `--stat`) is the right one. He corrects a minor misunderstanding - the author had suggested omitting these formats in a follow-up, but Junio points out they're already omitted, and the documentation should continue to note this limitation.

The series is now complete with all four patches merged, representing a significant improvement to line-log functionality while maintaining backward compatibility. The changes enable more powerful diff formatting options while carefully preserving the existing behavior that tools might rely on.