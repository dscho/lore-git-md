This is a follow-up in the memory leak investigation thread, where Junio C Hamano reports an unexpected CI failure when testing Jeff King's patch to enable `NO_MMAP` for LSan builds. The failure appears to be caused by Patrick Steinhardt's earlier commit 4c89d314 ("streaming: rely on object sources to create object stream") rather than Jeff's change, as bisect points to Patrick's commit as the true culprit.

Junio provides detailed reproduction steps showing the leak occurs in `t1060-object-corruption.sh` when built with LSan and `NO_MMAP`. He includes a test script that applies a small change to `compat/mmap.c` (making the `length` parameter unused) to help isolate the issue. The discussion confirms this is a separate leak from the ones Jeff's series fixed, surfacing only with the new `NO_MMAP` setting.

The email serves as a heads-up that while Jeff's series is technically sound, its merge may need to wait until this newly discovered streaming leak is understood. The thread remains focused on memory management issues, now expanding to include interaction between the streaming subsystem and mmap emulation. Junio's investigation maintains the high technical standard of this thread, with clear reproduction and bisection evidence.