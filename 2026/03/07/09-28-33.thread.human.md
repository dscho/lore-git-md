Lorenzo Pegorari, a prospective GSoC contributor, initiated a thread to clarify how partial clone configuration interacts with multiple promisor remotes, specifically exploring behavior around the `extensions.partialClone` global config versus per-remote `promisor` settings. His testing showed that without the global config, remotes are tried in config order when marked as promisors, while the `extensions.partialClone` setting makes its specified remote a last-resort fallback.

Christian Couder has now confirmed Lorenzo's understanding matches both the actual behavior and documented functionality. This validation is important for Lorenzo's GSoC project proposal on implementing promisor remote fetch ordering. Christian emphasized that any new ordering mechanism must maintain backward compatibility by still respecting the `extensions.partialClone` configuration, even as it introduces more flexible ordering preferences. The thread demonstrates careful consideration of both current behavior and future design constraints, with Lorenzo showing strong preparatory work through testing and verification before seeking expert confirmation.