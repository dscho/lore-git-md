This is a test modernization patch in Francesco Paparatto's ongoing series to improve `t/t3310-notes-merge-manual-resolve.sh`. The patch replaces fragile command substitutions with more robust test helpers (`test_cmp` and `test_cmp_rev`) throughout the test script.

The changes make test failures more debuggable by showing both expected and actual output when comparisons fail, rather than silently hiding `git rev-parse` errors. The patch touches 10 test cases, converting patterns like `test "$(git rev-parse A)" = "$(git rev-parse B)"` to use the proper helpers. It also consolidates some temporary file handling and uses `test_write_lines` for cleaner multi-line writes.

This v4 iteration incorporates all of Eric Sunshine's technical feedback from previous rounds, with the remaining changes being optional style refinements. The patch maintains the exact same test coverage while improving failure diagnostics - a textbook example of the test modernization effort. The changes are mechanical and well-contained, with no impact on the actual notes merge functionality being tested.