This is a follow-up email in the thread about configurable partial clone filters via URL patterns (`clone.<url>.defaultObjectFilter`). The author, Alan Braithwaite, acknowledges two final nits from Junio Hamano's review of the v4 patch: a discrepancy between the commit message and documentation regarding supported config forms, and outdated test script patterns that should use `test_cmp` rather than direct shell comparisons.

Alan confirms both issues will be addressed in the upcoming v5 submission, which he considers the final version barring any new concerns. The email shows the patch series is effectively complete, having incorporated all substantive feedback through multiple iterations. The remaining changes are minor documentation and test style adjustments to ensure consistency with Git's conventions.

This exchange represents the final polish phase of a well-reviewed feature that has already resolved all technical concerns raised during earlier discussion. The implementation provides URL-based partial clone filter configuration while maintaining clear boundaries (clone-only scope) and override mechanisms (`--filter`/`--no-filter` command-line options). The thread is now waiting only for these last cosmetic fixes before the feature can be merged.