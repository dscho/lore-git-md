Test modernization patch improving debuggability in notes merge tests. This v4 iteration from Francesco Paparatto completes a series converting fragile `git rev-parse` command substitutions in `t/t3310-notes-merge-manual-resolve.sh` to use Git's test helpers. The changes replace direct comparisons with `test_cmp` (for file content) and `test_cmp_rev` (for revision hashes), which provide clearer failure output by showing expected/actual values.

The patch touches 10 test cases, consistently applying the pattern of writing rev-parse output to temporary files for comparison rather than embedding commands in test expressions. It also incorporates Eric Sunshine's optional style suggestions from v3 review - using shell variables for rev-parse output and `test_write_lines` for multi-line writes. The changes maintain identical test coverage while improving failure diagnosis.

This represents the final technical iteration after Eric's thorough review process, with all substantive requirements addressed in v3 and now incorporating stylistic polish. The changes exemplify Git's test suite modernization priorities - replacing fragile patterns with robust helpers without altering test semantics. The patch series demonstrates effective collaboration between contributor and reviewer, following Git's iterative refinement model for mechanical test improvements.