Test fix with concerning signs of potential LLM involvement.

Junio C Hamano points out several red flags in Mirko Faina's patch to fix test cleanup in `t0008-ignores.sh`. While the actual code change (adding `subdir` to the cleanup command) is correct, the patch has multiple inconsistencies that raise questions:

1. The subject line and commit message reference the wrong test ("large exclude file ignored in tree") when the change actually affects "symlinks not respected in-tree".
2. The description mentions testing file sizes, which is irrelevant to the symlink test being modified.
3. Junio notes this pattern mismatches are characteristic of LLM-generated content.

The maintainer provides a corrected commit message template and questions whether the patch was properly human-reviewed before submission. This serves as both a technical correction and a warning about submission quality control - even for simple test fixes, accuracy in describing changes matters. The thread shows the maintainer's vigilance against low-quality or potentially inauthentic contributions, even when the underlying change itself is valid.