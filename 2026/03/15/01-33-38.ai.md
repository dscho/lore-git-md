This is the final refinement of a patch implementing configurable partial clone filters via URL patterns (`clone.<url>.defaultObjectFilter`). Alan Braithwaite addresses Patrick Steinhardt's last two review comments on the v5 patch: adding a NULL check for URL normalization failures and improving test cleanup hygiene with `test_when_finished`.

The patch allows configuring default partial clone filters based on repository URLs, with command-line `--filter` and `--no-filter` options taking precedence. The implementation uses Git's urlmatch infrastructure to support three levels of URL specificity (domain/namespace/full path). The feature is clone-specific to maintain clear boundaries, with filters stored in `remote.<name>.partialCloneFilter` for subsequent fetches.

Alan confirms both requested changes: guarding against NULL from `url_normalize()` and adding proper test directory cleanup. These are the last open issues before merging. The thread is effectively concluded, with the feature having addressed all technical feedback through multiple iterations. The implementation includes comprehensive tests covering filter application, command-line overrides, URL matching precedence, and edge cases.