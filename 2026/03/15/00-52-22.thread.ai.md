Thread identity: Bug report and implementation discussion for changing `git rev-parse --parseopt` exit code behavior for help flags (`--help`/`-h`), authored by brian m. carlson with Jeff King (Peff) participating. Implementation merged but now causing regression in `git instaweb`.

Core problem: Original issue was `rev-parse --parseopt` exiting with status 129 (error) for help requests, violating Git's standard help-exit convention. The solution modified shell-eval mode to exit 0 while retaining error exits in direct mode for debugging. This caused `git instaweb -h` to now exit 0 outside repositories where it previously exited 129, breaking test expectations in t1517.

Current status: Initial patch merged but causing regression caught by Junio C Hamano during integration testing (queued on v2.53.0). brian m. carlson acknowledges the issue and requests the patch be dropped from the queue while preparing a revised version. The test failure in t1517 test 169 reveals the changes affect any command using the underlying option parsing machinery where error exits for help were intentional.

Key technical details: Regression appears in t1517 test 169 expecting `git instaweb -h` to exit 129 outside repositories. Changes to `parse-options.c` affect all commands using this infrastructure. brian m. carlson admits possibly missing full test suite runs on final revision. Potential solutions under consideration:
1. Make exit code behavior configurable per-command
2. Special-case `instaweb`
3. Re-evaluate whether `instaweb` should adopt new behavior

Open questions: How to resolve regression while preserving original fix's benefits. Whether to make behavior configurable or handle `instaweb` specially. brian m. carlson will address this with a follow-up fix. The discussion highlights tension between standardizing behavior and preserving special cases.

Participant positions: brian m. carlson implemented original solution with Peff's input, now taking responsibility for regression. Junio caught the issue during integration testing. The thread shows careful consideration needed for changes to fundamental option-parsing behavior affecting multiple commands.

Related work: Original patch aligned with Git's standard help behavior but overlooked edge cases. Demonstrates complexity of changing core behavior and value of thorough test suite in catching interactions. Follow-up will need to balance consistency with historical expectations in specific commands like `instaweb`.