Thread identity: Bug report and implementation discussion for changing `git rev-parse --parseopt` exit code behavior for help flags (`--help`/`-h`), authored by brian m. carlson with Jeff King (Peff) participating. Implementation merged but now showing regression in `git instaweb`.

Core problem: Original issue was `rev-parse --parseopt` exiting with status 129 (error) for help requests, violating Git's standard help-exit convention. The solution modified shell-eval mode to exit 0 while retaining error exits in direct mode for debugging. However, this has caused `git instaweb -h` to now exit 0 outside repositories where it previously exited 129, breaking test expectations in t1517.

Current status: Initial patch merged but causing regression. Junio C Hamano identified the issue during integration testing (queued on v2.53.0). The test failure reveals the changes affect more than just `--parseopt` - they impact any command using the underlying option parsing machinery in contexts where error exits for help were intentional.

Key technical details: The regression appears in t1517 test 169, which expects `git instaweb -h` to exit 129 outside repositories. The patch's changes to `parse-options.c` affect all commands using this infrastructure. Possible solutions being considered:
1. Make exit code behavior configurable per-command
2. Special-case `instaweb`
3. Re-evaluate whether `instaweb` should adopt new behavior

Open questions: How to resolve the regression while preserving the original fix's benefits. Whether to make the behavior configurable or handle `instaweb` specially. brian m. carlson will need to address this before further integration.

Participant positions: brian m. carlson implemented the original solution with Peff's input. Junio caught the regression during integration testing. The thread shows brian will need to respond with a follow-up fix. The discussion highlights the tension between standardizing behavior and preserving special cases.

Related work: The original patch aligned with Git's standard help behavior but overlooked some edge cases. This follow-up shows the complexity of changing fundamental option-parsing behavior, particularly when it affects multiple commands with different historical behaviors. The thorough test suite proved valuable in catching the interaction.