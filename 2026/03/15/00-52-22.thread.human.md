Here’s the human-readable summary for a developer catching up on the thread:

This thread began as a bug fix to standardize how Git commands exit when users request help (`--help` or `-h`). The four-patch series, authored by brian m. carlson and now fully merged, corrects a long-standing inconsistency where `git rev-parse --parseopt` and other commands using Git’s parse-options API exited with status 129 (error) instead of the Unix-standard exit 0. The fix touches the core parse-options infrastructure, updates 37 test files, and ensures all built-in commands follow the same convention. Jeff King (Peff) and Junio C Hamano reviewed and approved the change, which was deemed a straightforward bug fix rather than a breaking change for Git 3.0.

After merging, Junio raised a question about the `SVN` test prerequisite logic in the first patch, noting that the current implementation might skip tests unnecessarily in environments where Subversion is available. The discussion took a turn when Peff identified a concrete bug in that same prerequisite: a shell-quoting error in the Perl version check causes incorrect comparisons (e.g., SVN 1.14.5 might not be recognized as newer than 1.2.0). This bug, inherited from existing test infrastructure, is now exposed by the new prerequisite’s inline Perl code. While the core exit-code fix is complete and merged, the prerequisite issue will likely require a follow-up patch to ensure `git-svn` tests run reliably in environments where Subversion is usable. No further changes are expected to the help-exit behavior itself.