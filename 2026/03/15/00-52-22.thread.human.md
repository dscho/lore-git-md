Here’s the human-readable summary for a developer catching up on the thread:

This thread started as a bug fix to standardize Git’s exit codes for `--help` and `-h` flags across all commands using the parse-options API. The inconsistency—where most commands exited with 0 (success) but `git rev-parse --parseopt` and others exited with 129 (error)—was traced back to a 17-year-old oversight and fixed in a four-patch series by brian m. carlson. The series was merged as a regular bugfix ahead of Git 3.0, but not without follow-up discussion about unintended consequences.

The core change ensures that all Git commands now exit with 0 when users request help, aligning with Unix conventions. The exception is `git rev-parse --parseopt` in its direct invocation mode, which still exits with 129 to preserve backward compatibility for debugging use cases. The implementation involved refactoring the parse-options API to introduce a new return code (`PARSE_OPT_HELP_ERROR`) and updating 37 test files to reflect the new behavior. Junio, the maintainer, approved the series but called one part of the implementation ("appending `exit 0` to the shell-eval output") "yuck" while accepting it as functionally correct.

After merging, Jeff King (Peff) raised two post-merge concerns. The first is a theoretical risk: scripts using `git rev-parse --parseopt` in shell-eval mode might now process nonsense output if they accidentally pass `--help` and blindly continue execution. The second is a more concrete regression scenario: scripts that wrap Git commands (e.g., `git log --my-options "$@"`) without parsing their own options could now succeed on `--help` (exit 0) instead of failing (exit 129), potentially feeding garbage to downstream logic. Peff doesn’t propose reverting the change—he acknowledges the trade-off and documents the risks for future reference. The thread also uncovered three technical issues in the prerequisite logic and refactoring, which brian m. carlson is expected to address in follow-up patches.

Key participants include brian m. carlson (author), Peff (reviewer), and Junio (maintainer). The series is now merged, but the discussion highlights how even small behavioral changes can have subtle implications for scripts and tooling built around Git.