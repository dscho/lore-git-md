Thread identity: Test fix for `t0008-ignores.sh` regarding symlink cleanup in ignore-file handling tests.

Core problem: The test "symlinks not respected in-tree" in `t0008-ignores.sh` creates a `subdir` directory and symlinks but only cleans up the `.gitignore` file, leaving `subdir` behind. This could affect subsequent tests.

Current status: Patch v1 from Mirko Faina adds `subdir` to the `test_when_finished` cleanup command, ensuring complete cleanup of test artifacts.

Key technical details: The test verifies Git's behavior of not respecting symlinked `.gitignore` files in the working tree (as opposed to those in `.git/info/exclude`). The fix touches only the test cleanup logic, not the actual ignore-file functionality.

Participant positions: Mirko Faina (new contributor) submitted the straightforward cleanup patch with proper sign-off. No other participants yet as this is the first message in the thread.