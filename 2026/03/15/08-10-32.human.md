Test modernization patch updating pack-refs tests to use standard path-checking helpers.

This is a straightforward test cleanup patch that replaces raw shell `test -f` and `test -e` checks (and their negations) with Git's standard test_path_is_missing and test_path_is_file helpers in the pack-refs test script. The change improves consistency with the rest of the test suite and provides better failure messages when tests fail.

The patch appears well-scoped and mechanical, touching only the test script with no behavior changes. The commit message notes this was done in response to review feedback, suggesting it's part of an ongoing test modernization effort. The changes are all simple substitutions that make the test intentions clearer - checking for path existence/absence is more readable when using the purpose-built helpers rather than negated shell tests.

This is the second version of the patch, with the main change being the switch from negated test_path_exists to test_path_is_missing as suggested in review. The author (Ritesh Singh Jadoun) doesn't appear to be a frequent contributor based on the From address, but the patch follows standard conventions and looks ready to merge.