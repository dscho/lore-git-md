This is a test modernization patch that updates the pack-refs test script to use Git's standard test helper functions instead of raw shell tests. The changes are mechanical and straightforward, replacing `! test -f` and `! test -e` with `test_path_is_missing`, and `test -f` with `test_path_is_file`.

The patch follows the project's ongoing effort to modernize test scripts by using the dedicated test helpers that provide better error messages when tests fail. The author notes this change was suggested in review, indicating it's a follow-up to previous feedback. The changes are limited to the test script and don't affect any actual Git functionality.

The patch appears well-scoped and uncontroversial, touching only test infrastructure without changing behavior. It's a good example of the kind of small, focused cleanup that helps maintain test consistency across the codebase.