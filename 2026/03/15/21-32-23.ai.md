Feature patch implementing configurable partial clone filters via URL patterns, now merged.

This is the final merge notification for Alan Braithwaite's patch adding `clone.<url>.defaultObjectFilter` configuration. The feature allows setting default partial clone filters that automatically apply when cloning matching repositories, with URL patterns following the same matching rules as `http.<url>.*` configuration. Command-line `--filter` and `--no-filter` options override the config as expected.

Junio Hamano notes the patch addresses all review feedback from previous iterations, including Patrick Steinhardt's catch of a potential NULL pointer issue and test cleanup improvements. The implementation now includes proper handling when URL normalization fails and uses `test_when_finished` for test directory cleanup. One remaining question about test file removal ("large.bin") goes unanswered but doesn't block merging.

The patch represents the consensus design that emerged from six iterations of review, providing a clean solution that maintains Git's boundaries while offering useful quality-of-life improvements for partial clone users. With all technical concerns addressed and comprehensive test coverage, Junio indicates he will merge this final iteration.