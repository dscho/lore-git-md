This is the first patch in a feature series enhancing `git stash apply` with customizable conflict markers. The patch adds three new options to `git stash apply`: `--ours-label`, `--theirs-label`, and `--base-label`, allowing users to specify custom labels for conflict markers instead of the default "Updated upstream" and "Stashed changes". 

The implementation modifies the stash documentation to describe the new options and adds a test case verifying the custom labels appear correctly in conflict markers. The changes are straightforward - adding the new options to the command-line interface, updating the usage string in the C code, and testing the functionality works as expected.

This appears to be a well-contained, focused change that cleanly extends existing functionality. The test case provides good coverage of the new behavior. The patch fits into the larger series by enabling better conflict marker labeling that will be used by the autostash behavior in `git checkout -m` in subsequent patches. The implementation looks solid and the documentation is clear.