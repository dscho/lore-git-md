Thread identity: refactoring patch series by Deveshi Dwivedi introducing a Coccinelle rule to detect and fix instances where `struct strbuf` is passed by value rather than by pointer, following up on Jeff King's earlier identification of this anti-pattern.

Core problem: Passing `struct strbuf` by value is inefficient (causes copying) and dangerous (caller/callee operate on different copies, potentially leading to dangling pointers if reallocation occurs). The series provides both detection and remediation for this pattern.

Current status: Second version (v1) with two patches - (1) Coccinelle semantic rule to detect problematic cases, (2) fixes for both remaining instances in `builtin/stash.c` (`show_stash()` and `save_untracked_files()`). The Coccinelle rule performs transformations to suggest pointer conversion, though manual follow-up would be needed for full compilation.

Key technical details: New semantic patch in `strbuf.cocci` detects functions taking `struct strbuf` parameters by value and suggests pointer conversion. Two cases found and fixed in `builtin/stash.c` - both converted to take pointers. Changes include updating function signatures, call sites, and internal usage to properly dereference pointers. Rule follows project's existing Coccinelle patterns by performing transformations rather than just reporting.

Participant positions: Jeff King originally identified the issue in `write_worktree_linking_files()` (already fixed separately). Deveshi Dwivedi proposes systematic solution via Coccinelle. No other reviews or feedback yet on this version. Both patches appear well-implemented and documented, with clear motivation referencing prior discussion. The series now appears complete as it addresses all identified cases of this anti-pattern.

Related work: Builds directly on Jeff King's prior fix for `write_worktree_linking_files()`. Part of ongoing code quality improvements using Coccinelle (similar to work by René Scharfe and others). The fact that only two cases were found suggests good project hygiene around this pattern, and the automated detection will help maintain it going forward.