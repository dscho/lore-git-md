Thread identity: bugfix patch series (now v2) for `git apply` error reporting, submitted by GSoC contributor Jialong Wang.

Core problem: `git apply` error messages for corrupt patches only showed line numbers without indicating input source (filename or stdin), making debugging difficult in multi-patch scenarios. Solution adds source identification to error messages while maintaining backward compatibility.

Current status: v2 incorporates all feedback from initial review, including additional test coverage. Patch appears complete and ready for merging. Changes are minimal and focused - just a few lines in `apply.c` to modify error format strings and reset line counters between patch inputs.

Technical details: changes in `apply.c` modify error message formatting to include `state->patch_input_file`. Test coverage in `t/t4100-apply-corrupt.sh` now includes:
1. Single file input with corruption
2. Stdin input with corruption
3. Multiple patch inputs with one corruption (added in v2 per reviewer request)
Implementation ensures line counter (`state->linenr`) resets for each new patch input.

Participant positions:
- Karthik Nayak (reviewer): positively reviewed initial approach, suggested additional test case which was implemented in v2
- Jialong Wang (author): responsive to feedback, promptly implemented suggested improvements
No outstanding objections or requests - all feedback addressed.

Related work: none beyond immediate patch context. Part of GSoC contribution.