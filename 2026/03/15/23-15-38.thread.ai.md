Thread identity: bugfix patch series (now v3) for `git apply` error reporting, submitted by GSoC contributor Jialong Wang.

Core problem: `git apply` error messages for corrupt patches lacked input source identification (filename/stdin), complicating debugging in multi-patch scenarios. Solution adds source context to error messages while maintaining backward compatibility.

Current status: v3 incorporates all feedback from prior reviews, including expanded test coverage and binary diff suite updates. Changes remain minimal and focused - primarily modifying error format strings in `apply.c` and adding test cases. Ready for merging.

Technical details: `apply.c` changes include:
- Error messages now show `state->patch_input_file` alongside line numbers
- Line counter (`state->linenr`) properly resets between patch inputs
Test coverage expanded in:
- `t/t4100-apply-corrupt.sh` (single file, stdin, multiple patches)
- `t/t4012-diff-binary.sh` updated to expect new error format

Participant positions:
- Karthik Nayak (reviewer): provided initial review, suggested additional test case (implemented in v2)
- Jialong Wang (author): responsive to feedback, maintained clean implementation through iterations
All feedback addressed with no outstanding objections.

Related work: GSoC contribution. No other related patches in flight.