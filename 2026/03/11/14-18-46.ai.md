This is a bugfix for the promisor-remote subsystem that prevents recursive lazy-fetching, addressing a real-world issue where a production system wrote 276GB of promisor packs in 90 minutes. The patch is now in its third iteration, with substantive discussion about whether the root cause lies in client behavior or server protocol violations.

Paul Tarjan responds to Patrick Steinhardt's concerns by providing concrete details from their Anthropic production environment where this manifested. The issue occurs when a blob:none partial clone receives a thin pack with REF_DELTA objects referencing missing bases, causing index-pack to recursively trigger additional fetches. While Patrick questions whether this represents a server protocol violation (since well-behaved servers shouldn't send such deltas), Paul maintains that client-side hardening is still valuable regardless of server behavior.

Key technical points in this response: the test has been strengthened in v3 to demonstrate the actual failure mode rather than just checking environment variables, and the depth-2 fetch scenario is shown to be reproducible using GitHub's actual server behavior. The discussion reveals nuanced understanding that while the immediate fix is sound (propagating GIT_NO_LAZY_FETCH), there may be a separate server-side issue that needs addressing. The patch appears well-justified as defensive programming, though the thread suggests the commit message may need refinement to clearly separate the client fix from potential server bugs.