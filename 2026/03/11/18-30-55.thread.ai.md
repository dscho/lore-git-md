Thread identity: Proposal to add `--show-current-patch` option to `git cherry-pick`, mirroring existing `git rebase --show-current-patch` functionality. Originating author not specified in this email.

Core problem or goal: During a range cherry-pick operation that hits a conflict, users currently have no straightforward way to see which commit's changes are being applied. They must manually inspect `.git/CHERRY_PICK_HEAD` and run `git show`. The proposal aims to add a `--show-current-patch` flag to show the current patch being applied when conflicts occur, matching rebase's behavior.

Current status: Initial proposal stage. No implementation or version history yet.

Key technical details: Would modify `builtin/cherry-pick.c` to add the new flag, likely leveraging similar mechanisms to those used in `git rebase --show-current-patch`. Would show the patch being applied when cherry-pick is in conflict state, error cleanly when no cherry-pick operation is active.

Open questions and loose ends: No technical objections or requests for changes raised yet. Implementation approach not yet discussed. May need to consider how this interacts with other cherry-pick states beyond just conflicts.

Participant positions: No reviews or feedback yet from other contributors. The proposal appears well-motivated but hasn't been technically vetted.

Related work: Directly inspired by existing `git rebase --show-current-patch` functionality. Would provide similar UX for cherry-pick operations.