Test modernization effort to support SHA-256 hash algorithm continues with a focused change in t/t3200-branch.sh. The patch updates a test case for `git branch --merged` behavior with invalid object names, replacing a hardcoded 40-zero string with the test framework's `$ZERO_OID` variable. This ensures the test works correctly regardless of whether Git is configured with SHA-1 or SHA-256 as the hash algorithm. The change follows established patterns for making tests hash-agnostic and maintains the exact same test coverage while being more future-proof. The patch is small and self-contained, touching only the specific test case that needed updating.