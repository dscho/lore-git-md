This is part of a platform compatibility patch series updating Git's IMAP send functionality to use modern OpenSSL APIs, specifically addressing deprecation warnings that would appear in OpenSSL 4.0. The thread has revealed a security concern about patch 3's removal of string length checks in `imap-send.c`.

Junio C Hamano questions the rationale for removing these checks, pointing out that the hostname matching still uses raw string pointers at this stage (the change to use ASN1_STRING comes in patch 4). He asks for clarification on what exactly makes these checks unnecessary now that the API is being updated, noting that Oswald Buddenhagen had already raised similar concerns about the security implications of removing these validations.

The exchange highlights that what appeared to be a straightforward cleanup may actually touch security-sensitive validation logic. The disagreement centers on whether these are redundant internal consistency checks (as the patch author Beat Bolli claims) or necessary security validations that protect against embedded nulls bypassing hostname matching (as Oswald Buddenhagen argues). Junio's probing questions suggest patch 3 may need revision to address these security concerns before proceeding.