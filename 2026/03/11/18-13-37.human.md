This is a refactoring patch that concludes a series addressing strbuf overuse in Git's codebase. The second patch in the series optimizes combine filter parsing in `list-objects-filter-options.c` by replacing `strbuf_split_str()` with direct string walking using `strchrnul()`.

The patch has gone through three iterations with detailed review from Jeff King (Peff) and Junio Hamano. The final version eliminates temporary strbuf arrays, uses a reusable strbuf to avoid per-iteration allocations, and consistently handles empty sub-specs as no-ops. Peff has given his approval to the current implementation, and Junio indicates he will queue the patch for merging.

The changes improve performance by reducing memory allocations during filter parsing while maintaining the same behavior. The thread shows careful attention to edge cases and resource management, with all reviewer feedback incorporated. This marks the completion of the series, with both patches now ready for integration.