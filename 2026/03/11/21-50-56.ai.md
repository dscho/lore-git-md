This is a technical discussion about shell script portability in Git's test suite, specifically around the use of `sed -E` and other non-POSIX options. The exchange between Junio C Hamano and Eric Sunshine clarifies that while the test-lint script (`check-non-portable-shell.pl`) can detect line-continuation cases of `sed -E`, it still has gaps - notably missing `-i` and potentially other non-POSIX flags when they appear in certain contexts.

The conversation builds on prior work (commit a0a630192d) that improved the linter's ability to catch shell script portability issues, but reveals remaining limitations in the detection mechanism. This ties into the broader context of the patch series about standardizing command output sorting, where test reliability across platforms is important.

Key technical details include:
- The linter's current capabilities and blind spots regarding `sed` flag detection
- The specific case of `-i` being missed in patterns like `sed -n -i -e`
- The ongoing balance between adopting modern shell features and maintaining portability

This exchange doesn't introduce new patches but provides important context about test infrastructure constraints that may affect how future test-related patches are reviewed and accepted. The discussion suggests the test-lint script may need further refinement to catch all non-portable `sed` usage patterns.