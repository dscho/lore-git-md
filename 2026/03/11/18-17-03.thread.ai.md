Thread identity: refactoring patch to remove `the_repository` usage in `builtin/mktree.c`, part of the ongoing `the_repository` removal effort.

Core problem/goal: eliminate reliance on the `the_repository` global variable by passing repository state explicitly through the call chain in `mktree` command implementation.

Key technical details:
- Changes `cmd_mktree()` to pass `struct repository *repo` parameter to helper functions `mktree_line()` and `write_tree()`
- Replaces `the_hash_algo` with `repo->hash_algo`
- Updates `parse_oid_hex()` calls to use `parse_oid_hex_algop()`
- Does not modify `oid_to_hex()` which still relies on global state
- Touches `builtin/mktree.c` only - no test changes needed

Open questions:
- How to properly CC domain experts for review (previous similar patches may have been missed)
- Notes that `oid_to_hex()` still uses global state (known limitation)

Participant positions:
- Author identified this as simpler cleanup opportunity after attempting more complex FIXME in t1006-cat-file.sh

Related work: part of larger `the_repository` removal effort (René Scharfe primary driver)