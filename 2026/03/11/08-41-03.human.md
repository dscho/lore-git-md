Documentation patch fixing formatting in the recently merged line-log documentation update. Kristoffer Haugsbakk points out that the newly added section about supported `-L` options in `line-range-options.adoc` incorrectly uses indentation, making it appear as a code block when it should be flush left. This is a follow-up to Michael Montalbo's patch series that enabled various diff options (like `--word-diff` and pickaxe) to work with `git log -L`.

The thread context shows this is a minor final tweak to documentation that was otherwise approved and merged. The series successfully routed line-log output through Git's standard diff pipeline, enabling previously unsupported features while maintaining backward compatibility. This particular email is just about fixing the visual presentation of the documentation, not changing any functionality.