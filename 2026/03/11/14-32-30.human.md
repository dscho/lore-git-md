This is a technical discussion about build system improvements, specifically patch 7/8 in Patrick Steinhardt's series introducing Meson precompiled header support. Phillip Wood questions whether Meson forces inclusion of precompiled headers in all compilation units, noting that GCC's behavior only includes them where the original header is referenced. He observes that splitting compatibility sources into their own library (as this patch does) provides additional build parallelism while precompiling headers, which he sees as beneficial.

The patch moves all platform-specific sources (Windows, Cygwin, Darwin, etc.) and compatibility implementations (strdup, mmap, regex, etc.) into a new 'compat_sources' static library, separating them from the main libgit sources. This prepares for the subsequent PCH implementation by isolating files that intentionally avoid including git-compat-util.h. The change is mechanical but extensive, touching nearly every platform-specific source file in the codebase.

Phillip's question suggests he's generally supportive of the change but wants to confirm the technical details of Meson's PCH behavior. His participation signals this infrastructure work is receiving attention from experienced contributors beyond the primary author. The thread shows the series progressing through review with constructive technical engagement.