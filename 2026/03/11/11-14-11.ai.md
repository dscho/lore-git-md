This is a follow-up discussion in a bugfix thread about `git merge-file --object-id` behavior in worktrees. The thread has already converged on a technical solution (v2 from Mathias Rav fixes the BUG() by avoiding redundant `setup_git_directory()` calls), and this email shifts focus to a minor formatting question about commit references in gitk versus CLI tools.

Kristoffer Haugsbakk points out that Mathias Rav's preference for quoted commit references (from gitk's "Copy commit reference" feature) matches an old 2016 attempt to standardize on that format. The thread context shows this is a non-blocking side discussion about UI consistency, not affecting the core bugfix. The patch itself appears merge-ready with all substantive feedback addressed, and this exchange is more about documenting historical precedent for a formatting choice than about changing the current patch. The architectural concern Junio raised about `setup_git_directory()` idempotence remains an open question for future work, but doesn't block this fix.