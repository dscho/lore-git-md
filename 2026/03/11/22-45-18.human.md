Feature completion: subcommand autocorrection build system integration.

This is the final patch in Jiamu Sun's series adding subcommand autocorrection to Git's parse-options API, now addressing the last remaining build system integration issue. Aaron Plattner noticed that while the test file `t9004-autocorrect-subcommand.sh` was added to the Makefile build, it was missing from meson.build. Jiamu confirms they will add it, completing the series' integration with both build systems.

The thread shows this feature is now fully baked - all technical feedback has been addressed across multiple rounds of review, with thorough test coverage and documentation in place. The implementation matches Git's existing help.c behavior for consistency, using the same Levenshtein distance thresholds. Junio C Hamano has indicated the series is ready for merging in the next cycle.

This will make Git's CLI more forgiving of subcommand typos, initially applied to `git remote` and `git notes` with potential to expand to other commands. The careful architecture separates the autocorrect logic cleanly while maintaining Git's strict coding style - even down to brace placement preferences confirmed by Junio.