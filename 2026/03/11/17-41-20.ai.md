Test modernization patch updating t3200-branch.sh to be hash-agnostic and more robust in its error checking. This is part of the ongoing SHA-256 transition effort.

Siddharth Shrimali's v2 patch addresses Patrick Steinhardt's review feedback from the initial version. The change replaces a hardcoded 40-zero string (SHA-1 null OID) with the `$ZERO_OID` variable to make the test work correctly under both SHA-1 and SHA-256 hash algorithms. More importantly, it now explicitly checks for the expected "must point to a commit" error message using `test_grep` rather than just verifying failure, which ensures the test is validating the intended behavior rather than just syntax.

The patch demonstrates good test hygiene - it maintains the same coverage while being more precise about what it's testing and more future-proof against hash algorithm changes. The commit message clearly explains both the technical issue (40 vs 64 character hash mismatch in SHA-256) and the improved validation approach. This is a straightforward improvement that should merge cleanly into the test modernization effort.