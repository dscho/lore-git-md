This thread discusses a small but precise update to Git's test infrastructure to modernize `sed` usage in the test suite. Junio Hamano initiated the change to allow the `-E` flag (extended regular expressions) in `check-non-portable-shell.pl`, bringing it in line with POSIX Issue 8 standards. The original patch updated the linter to permit `-E` alongside existing flags (`-n`, `-e`, `-f`) and fixed a test in `t6030-bisect-porcelain.sh` that used `-En` to use separate `-E -n` flags.

Ramsay Jones pointed out that while the linter's code was updated, its error message still only listed the older flags, creating a minor inconsistency. Junio promptly addressed this in a v2 revision, fixing both the typo in the commit message and updating the error message to include `-E`. The changes are minimal—touching just the linter script and one test file—but demonstrate the project's attention to detail in maintaining clear, standards-compliant infrastructure. The update is straightforward and uncontroversial, reflecting Git's real-world usage of `-E` since 2022 without issues.