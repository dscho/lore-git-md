This is a refactoring patch targeting the IMAP SSL certificate verification code in `imap-send.c`. The change moves the ASN1_STRING access and cast into the `host_matches()` helper function, simplifying both call sites that perform hostname matching against SSL certificates.

The patch cleans up duplicate ASN1_STRING handling by having `host_matches()` take the raw ASN1_STRING parameter directly rather than requiring callers to extract the string data first. This reduces boilerplate in the two callers (`verify_hostname()` for both subject alternative names and common name checks) while maintaining the same functionality. The change is straightforward and appears to be a pure refactoring with no behavior modification.

The patch is part of a series (position 4/4) but no cover letter or thread context was provided to understand the broader context of this change. The modification is small and focused, touching only 4 lines across two functions while improving code organization. The commit message clearly explains the motivation and impact.