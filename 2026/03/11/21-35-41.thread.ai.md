Summary of test infrastructure patch from Junio C Hamano updating sed usage in Git's test suite, with follow-up from Ramsay Jones. The patch modernizes the test suite by permitting the `-E` flag (extended regular expressions) in `check-non-portable-shell.pl`, aligning with POSIX Issue 8 (2018) standards. Original changes updated the linter to allow `-E` alongside existing permitted flags (`-n`, `-e`, `-f`) and fixed a test in `t6030-bisect-porcelain.sh` that used `-En` to use separate `-E -n` flags.

Ramsay Jones identified and Junio Hamano acknowledged one remaining inconsistency: while the linter's pattern was updated to permit `-E`, its error message still only listed `-n`, `-e`, and `-f` as allowed options. Junio's v2 revision addresses this by updating the error message to include `-E`, completing the modernization effort. The revision also fixes a typo in the commit message ("unwated" -> "unwanted"). Changes remain minimal, touching only the linter script (`check-non-portable-shell.pl`) and one test file (`t6030-bisect-porcelain.sh`).

The thread demonstrates the project's attention to detail in test infrastructure, where even small documentation mismatches in error messages are addressed. Both the original patch and follow-up discussion show careful consideration of standards compliance (POSIX Issue 8), real-world usage patterns (successful `-E` usage since 2022), and developer experience (clear error messages). The changes are uncontroversial and align with Git's standards-compliance goals, with Junio indicating the error message update should be made to complete the effort.