Maintainer follow-up on zombie process cleanup patch. Junio C Hamano is seeking clarification about the real-world impact of the zombie process issue in container environments, questioning whether the problem persists long enough to matter given containers are typically torn down after their main process exits.

This is a maintainer query about the practical significance of a bugfix targeting zombie processes left when Git exits abnormally (via exit(128)) during network operations. The original patch from Andrew Au added atexit handlers to ensure child processes (git-remote-https, ssh) get reaped even when normal cleanup paths are bypassed, specifically addressing container scenarios where Git runs as PID 1.

Junio's question probes the actual deployment context: since containers running Git would presumably terminate after the Git process exits, he wonders whether zombie processes would persist long enough to be problematic. This suggests he's evaluating whether the fix addresses a meaningful production issue or a theoretical edge case. The response will likely need to demonstrate concrete problems caused by lingering zombies in container orchestration systems or similar environments where container lifecycle management might be affected.

The thread context shows the technical solution is already well-formed (atexit handlers with proper cleanup tracking), so this exchange appears focused on establishing the problem's real-world relevance rather than the implementation details. The patch author may need to provide specific container platform behaviors or monitoring scenarios where zombie processes cause issues despite container termination.