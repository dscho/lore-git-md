Test modernization patch updating a hardcoded null OID reference to use the test framework's `$ZERO_OID` variable. This is part of the ongoing SHA-256 transition effort to make tests work with both SHA-1 and SHA-256 hash algorithms.

The change is straightforward - replacing a literal string of 40 zeros with the variable that automatically provides the correct null OID length for the current hash algorithm. The patch affects only one test in `t/t3200-branch.sh` that checks `git branch --merged` behavior with invalid object names.

This appears to be a clean, focused change that follows established patterns for making tests hash-agnostic. The commit message clearly explains the motivation (supporting SHA-256) and the solution (using the provided test variable). The patch is small and touches only what it needs to, with no behavioral changes beyond the intended test modernization.