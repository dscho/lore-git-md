This is a bugfix patch updating `imap-send` to use modern OpenSSL APIs for certificate verification. The change addresses OpenSSL 4.0's deprecation of `X509_NAME_get_text_by_NID()` by switching to the recommended replacement functions that have been available since OpenSSL 1.1.0.

The patch carefully handles version differences - pre-4.0 OpenSSL requires non-const pointers while newer versions use const-correct APIs. The core change replaces a simple string copy with a more robust sequence of calls to get the certificate's common name through proper OpenSSL data structures.

The commit message clearly explains the change's motivation (OpenSSL 4.0 deprecation) and implementation approach (version-aware const handling). The patch looks well-constructed with appropriate version checking and error handling preserved. This appears to be a straightforward maintenance update to keep Git working with modern OpenSSL versions.