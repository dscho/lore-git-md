This is a documentation patch providing a minor formatting correction to the recently merged line-log series. Kristoffer Haugsbakk points out that the indentation in the documentation update for `line-range-options.adoc` is incorrect - the added lines should be flush left rather than indented, as they are part of a code block.

The patch was part of Michael Montalbo's series that enabled various diff options (like `--word-diff` and pickaxe options) to work with `git log -L` by routing the output through Git's standard diff pipeline. The documentation update in question had already been merged, so this is a post-merge correction to the formatting.

The thread context shows this was the final patch in a completed series that had already addressed all technical aspects and received maintainer approval. Kristoffer's observation is purely about the documentation markup, not the functionality. The correction is straightforward and uncontroversial - just removing unnecessary indentation in the AsciiDoc file.