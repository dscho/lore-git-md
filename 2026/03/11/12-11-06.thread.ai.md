AI thread summary:

This thread concerns a refactoring of IMAP SSL certificate verification in `imap-send.c`, specifically around hostname validation against X.509 certificates. The current patch (3/4 in a series) removes redundant string length checks during hostname verification when using OpenSSL APIs.

Key technical details:
- Modifies SAN DNS entry and CN field validation in `imap-send.c`
- Removes explicit length checks that verified ASN1_STRING data length matched internal length fields
- Justification: these checks are redundant when using proper OpenSSL accessors (`ASN1_STRING_get0_data`)
- Maintains same security properties while simplifying code

Open questions:
- No cover letter provided to understand series context
- Commit message could better explain historical rationale for original checks and why removal is now safe
- Impact on other SSL backends (if any) not addressed

The changes appear focused and technically sound, targeting what the author considers unnecessary defensive programming now that proper OpenSSL APIs are used. The patch maintains the core hostname matching functionality while removing redundant validation steps.