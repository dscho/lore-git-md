This is a cleanup patch in the ongoing `git fast-import` signature handling series. It removes an unused function declaration from commit.h that was left behind when `sign_with_header()` was removed in a previous change (commit 6206089cbd). 

The patch is straightforward - it deletes two lines containing the now-obsolete forward declaration. This is part of a v4 series that builds on earlier work to improve signature handling in `git fast-import`, but this particular patch is just a simple housekeeping change to remove dead code. 

The change is uncontroversial and clearly correct - the function being declared no longer exists in the codebase. It follows the pattern of keeping header files clean by removing declarations for functions that have been deleted. The patch is small, focused, and has a clear commit message explaining the change.