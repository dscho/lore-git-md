This is a follow-up discussion in a bugfix thread about `git merge-file --object-id` behavior in worktrees. The technical aspects of the fix were settled in previous emails (v2 of the patch is ready with all feedback incorporated), and this exchange focuses on a minor formatting question about commit references.

Kristoffer Haugsbakk points out that the debate over whether commit references should include quotes (as produced by gitk's "Copy commit reference" feature) has come up before, linking to a 2016 thread where someone proposed removing the quotes. This is a longstanding stylistic preference discussion that surfaces periodically but doesn't block the current patch.

The core patch itself (not shown in this email) fixes a BUG() that occurs when using `--object-id` in worktrees, with thorough test coverage and clear commit message. The current discussion is purely about commit message formatting conventions - an incidental point that doesn't affect the technical merit of the fix. The thread shows the project's attention to detail even on minor presentation aspects, though in this case the formatting preference appears to be a matter of personal taste rather than a hard requirement.