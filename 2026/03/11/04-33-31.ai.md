This is a documentation patch addressing the handling of the `-U` diff option in Git. The thread has established that while `-U` without a number is accepted (defaulting to 3 lines of context like `-p`), this is legacy behavior that should be documented as such rather than presented as an intentional feature.

Tian Yuchen confirms that the latest version (v3) of their patch incorporates Oswald Buddenhagen's suggested wording, which explicitly frames the bare `-U` behavior as "a historical accident" and documents it as silently accepted while maintaining `--unified=<n>` as the canonical form requiring a number. The patch appears ready for merging with Junio's approval, having addressed the core documentation issue while preserving backward compatibility.

Jean-Noël Avila raises a follow-up question about whether the documentation should present `--unified` as optionally taking a number (with `[=<n>]` syntax) even though the implementation requires it, but this appears to be a stylistic documentation consideration rather than a functional issue. The thread shows consensus on the main patch with only minor documentation refinements potentially remaining.