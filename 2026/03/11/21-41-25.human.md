This is a test infrastructure patch from the Git maintainer Junio C Hamano that updates the portability rules for `sed` usage in Git's test suite. The change allows the `-E` flag (extended regular expressions) which has been part of POSIX since 2018 and has been in use in Git's tests since 2022 without issues.

The patch makes two straightforward changes: it updates the `check-non-portable-shell.pl` linter to permit `-E` alongside other allowed flags (`-n`, `-e`, `-f`), and fixes a test in `t6030-bisect-porcelain.sh` that was using `-En` to use separate `-E -n` flags for better clarity and linter compliance. The commit message provides useful historical context about the original POSIX Issue 7-based limitation and justifies the change based on both standards evolution and years of successful `-E` usage in practice.

This is a small but sensible update that aligns the test suite's portability checks with current standards and established usage patterns. As it comes from the maintainer and addresses a known inconsistency, it's likely to be merged without controversy. The patch demonstrates good attention to detail by both updating the linter and fixing the test that was technically violating the old rules.