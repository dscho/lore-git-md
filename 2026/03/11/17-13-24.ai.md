This is a proposal discussion from Pablo regarding a potential extension to their Google Summer of Code (GSoC) project on the `git cat-file --remote-object-info` command. The current project focuses on completing and extending remote object information capabilities, and Pablo is seeking feedback on adding functionality to identify missing blobs in partial clones.

The new idea would have `remote-object-info` analyze a given tree hash and return a list of missing blobs within that tree, sorted by size. This would help users of partial clones understand what objects they're missing and their relative sizes before deciding whether to fetch them. The proposal is at an early stage - Pablo is asking whether this would be too ambitious to include in their existing project scope and whether the concept itself is worthwhile.

The email doesn't provide implementation details but shows the contributor is thinking about practical use cases for partial clones. Since this is a discussion starter rather than a concrete patch, the key points for future thread participants will likely be: 1) whether this functionality belongs in `cat-file` or another command, 2) how it would interact with existing partial clone mechanisms, and 3) whether the sorting-by-size approach provides meaningful value to warrant the implementation effort.