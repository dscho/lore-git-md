This is a cleanup patch in the ongoing `git fast-import` signature handling series. It removes an unused forward declaration of `sign_with_header()` from commit.h that was left behind when the function itself was removed in a previous commit (6206089cbd). The change is straightforward and uncontroversial - it simply tidies up the header file by removing dead code that's no longer referenced anywhere in the codebase.

The patch is part of a v4 series that builds on the merged v2 series which added re-signing capability to `git fast-import`. While this particular patch is minor, it's part of a larger effort to clean up and generalize the signing infrastructure in preparation for future extensions like signing unsigned objects and adding abort-on-invalid behavior. The removal of this declaration doesn't affect functionality but helps maintain code hygiene as the signing system evolves.