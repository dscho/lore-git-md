This is a final review comment on the v5 patch implementing configurable partial clone filters via URL patterns (`clone.<url>.defaultObjectFilter`). Patrick Steinhardt notes two small technical issues that should be addressed before merging.

The first is a potential NULL pointer dereference in `get_default_object_filter()` when `url_normalize()` fails to parse the URL. Patrick suggests gracefully handling this case by ignoring the URL rather than erroring out. The second is a test hygiene suggestion to clean up temporary clone directories using `test_when_finished` in the test script.

These are minor implementation refinements to an otherwise complete patch series that has already addressed all major technical feedback. The core functionality - allowing URL-based configuration of partial clone filters while maintaining clear override mechanisms via `--filter` and `--no-filter` - remains unchanged. The thread is effectively concluded with these being the last open points.