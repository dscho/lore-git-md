This is a refactoring patch for the IMAP SSL certificate verification code in `imap-send.c`. The change moves the ASN1_STRING access and cast into the `host_matches()` helper function, simplifying both call sites that verify hostnames against SSL certificates.

The patch is small and focused - it doesn't change behavior, just cleans up the code by reducing duplication. The author (Beat Bolli) has correctly identified that both callers were doing the same ASN1_STRING access pattern, so moving that into the helper function makes the code more maintainable.

This appears to be part of a larger series (4/4) though no cover letter or thread context was provided. The change looks straightforward and uncontroversial - it's the kind of small cleanup that would likely be merged without much discussion. The patch follows Git's coding style and includes a proper commit message explaining the change.