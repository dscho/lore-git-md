This is a refactoring discussion about eliminating obsolete strbuf.buf NULL checks, specifically focusing on the strbuf_getwholeline() function's interaction with getdelim().

Junio and Jeff King (Peff) are debating whether a proposed rewrite using strbuf_attach() would simplify the strbuf_getwholeline() implementation. Peff provides a concrete diff showing the refactoring but observes it doesn't actually reduce complexity - the same invariants around getdelim()'s allocation behavior still need handling. Junio questions why the EOF path needs conditionals at all, suggesting the function could simply free any allocated buffer and return.

The exchange represents experienced maintainers carefully evaluating edge cases in a systematic cleanup effort. While they agree most strbuf.buf NULL checks are obsolete (the buffer always points to strbuf_slopbuf when empty), this particular case involving getdelim() may need to remain as an exception. The discussion remains technical and focused, with both parties working to understand whether the proposed simplification would actually make the code clearer while maintaining correctness.