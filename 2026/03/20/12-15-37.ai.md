Build system improvement thread follow-up. Toon Claes provides a minor typo correction ("fro" -> "for") in Patrick Steinhardt's patch that moves the `coverage-diff.sh` script from `contrib/` to the new `tools/` directory. This is part of the larger series consolidating build infrastructure, where the script's relocation makes sense since it's core developer tooling rather than optional contrib material. The correction is trivial and doesn't affect the patch's technical content - it just fixes a small typo in the commit message. The series is already in its final iteration (v3) with positive reviews from Phillip Wood and others, so this is likely one of the last minor tweaks before the changes are merged. The thread shows the typical Git workflow where even small patches receive careful review from multiple contributors.