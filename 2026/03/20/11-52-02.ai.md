This is a refactoring patch from Adrian Ratiu's hook configuration series, moving a utility function to a more appropriate location. The patch relocates `unsorted_string_list_remove()` from hook.c to string-list.[ch] where it better fits with similar string-list operations.

The key changes are:
1. Moving the function implementation to string-list.c
2. Adding proper documentation in string-list.h
3. Making the function more generic by adding a `free_util` parameter to match the pattern of other string-list functions
4. Updating all call sites in hook.c to use the new parameter (passing 0 to maintain current behavior)

The change was suggested by Patrick Steinhardt as part of code cleanup in Adrian's larger hook configuration work. This is a straightforward mechanical improvement that makes the codebase more consistent - similar string-list operations already live in string-list.[ch] and the added parameter brings it in line with related functions like `unsorted_string_list_delete_item()`.

The patch appears well-contained and properly documented, with all call sites updated. It's a typical preparatory refactoring that enables cleaner code in subsequent patches of the series while not changing any functionality.