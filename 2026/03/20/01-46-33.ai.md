Refactoring discussion about strbuf_getwholeline() implementation.

Junio Hamano and Jeff King (Peff) continue their technical discussion about eliminating obsolete NULL checks for strbuf.buf, focusing specifically on the strbuf_getwholeline() function. Peff had previously implemented Junio's suggestion to refactor the function using strbuf_attach(), but found it didn't simplify the code as hoped. Junio now questions why the EOF path needs to maintain strbuf invariants at all, suggesting the function could simply free any allocated buffer and return without further modifying the strbuf.

The core issue stems from strbuf_getwholeline()'s interaction with getdelim(), which may or may not allocate memory. Peff's analysis shows that even with the proposed refactoring, the function still needs to handle cases where getdelim() consumes the input buffer, requiring careful state management. Junio's latest response suggests a potentially more radical simplification - abandoning the strbuf invariant restoration on error paths entirely.

This exchange represents experienced maintainers carefully evaluating edge cases in low-level string buffer handling. The discussion remains highly technical, focused on the precise behavior of getdelim() and its implications for strbuf state management. While the overall direction of eliminating unnecessary NULL checks is agreed upon, this particular function may require special handling due to its unusual interaction with the getdelim() system call. The conversation is moving toward determining whether strbuf_getwholeline() should remain an exception case in the broader NULL-check cleanup effort.