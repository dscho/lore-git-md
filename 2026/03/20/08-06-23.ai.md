This is a platform compatibility follow-up regarding macOS regex behavior. Johannes Schindelin continues the investigation into CI build failures related to the REG_ENHANCED flag, building on his previous analysis of the meson build system's handling of this case.

Schindelin corrects his earlier understanding of the USE_ENHANCED_BASIC_REGULAR_EXPRESSIONS flag in config.mak.uname - it doesn't indicate that Git should use compat/regex.c, but rather signals that the system regex library supports enhanced regexes. He now suggests a more targeted solution: adding a NO_REGEX flag specifically for Homebrew's clang on macOS, which would force the build to use Git's compat/regex.c implementation when the system regex support is incomplete.

The proposed change would explicitly handle the case where Homebrew's clang (as of March 2026) appears to lack REG_ENHANCED support, while maintaining the expected regex behavior rather than silently falling back to basic regexes. This approach contrasts with Junio's initial workaround that would have silently changed behavior, and provides a more maintainable solution by documenting the specific platform limitation in the build configuration.

The email reveals deeper insight into the build system's regex handling layers and shows how platform-specific quirks need to be addressed at the configuration level rather than through code workarounds. Schindelin's analysis continues to highlight the importance of maintaining consistent behavior across different build methods while properly documenting platform limitations.