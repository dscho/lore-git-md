Follow-up fix in Patrick Steinhardt's ODB abstraction series.

Patrick Steinhardt acknowledges Ramsay Jones' discovery of a type mismatch in the ODB abstraction work, where a function call was passing `0` instead of `NULL` after a parameter type change. This was caught by the sparse static analyzer after Ramsay updated his toolchain. The issue originated in the 'ps/odb-generic-object-name-handling' branch rather than the 'ps/object-counting' branch where it was initially reported.

The email confirms Patrick has applied the fix locally. This is a straightforward correction in the ongoing effort to make Git's object storage backend-pluggable. The interaction demonstrates how platform-specific tooling can surface cross-branch inconsistencies during large refactors, and how contributors collaborate to resolve them even for minor issues. The fix maintains identical behavior while correcting type safety, showing the project's attention to detail even in mechanical changes.