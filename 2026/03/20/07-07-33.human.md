This is patch 7/14 in Patrick Steinhardt's series to make object name lookups more extensible, part of the ongoing ODB (object database) abstraction effort. The patch refactors `repo_collect_ambiguous()` to use the new backend-generic `odb_for_each_object()` infrastructure rather than calling into backend-specific functions directly.

The change simplifies the code by removing the need to manually handle both loose and packed object cases separately. Instead, it relies on the unified iteration interface that was introduced in earlier patches. The behavior remains exactly the same - collecting objects matching a given prefix - but the implementation is now more maintainable and works with any object storage backend.

The patch follows the established pattern of the series by moving functionality from specialized implementations to the generic ODB interface. It builds on the `parse_oid_prefix()` helper introduced in patch 6/14 and demonstrates the payoff of the earlier refactoring work. The change is mechanical and well-contained, touching only `object-name.c` with straightforward additions and deletions.

Given the series' progression and Patrick's track record with this type of architectural change, this patch appears to be a solid step forward in the ODB abstraction effort. It maintains the high quality seen in previous patches and continues the systematic decomposition of object name handling logic.