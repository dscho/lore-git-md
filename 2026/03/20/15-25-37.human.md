This is a review comment from D. Ben Knoble on Kristoffer Haugsbakk's `git name-rev --format` series. The note points out that while the patch implements `--notes` support for the new `%N` format specifier, the documentation update doesn't mention this capability. 

The observation is technically correct - the documentation diff shown only adds the `--format` option but omits the parallel `--notes` documentation that would explain how to use notes with formatted output. This appears to be an oversight in an otherwise thorough patch that already handles notes display via `display_notes_opt` and includes tests for notes functionality.

The comment is constructive and on-point, identifying a real documentation gap without disputing the technical implementation. It follows naturally from the v2 changes listed in the notes, which explicitly mention implementing `--notes` support. This suggests the series will likely need a v3 to address the documentation completeness.