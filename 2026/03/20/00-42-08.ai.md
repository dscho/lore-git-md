This is a documentation patch for the `diff-highlight` contrib script, part of Jeff King's series to improve this tool. The patch updates the README to clarify build instructions, noting that users must now run `make` in the directory (since the script was modularized in 2017) and can run tests via `make test`. 

The change is straightforward and well-motivated - the current documentation didn't reflect the actual build process. Jeff provides clear context about why this update is needed (the 2017 change that split the code into a module) and what the new workflow should be. This is typical of his thorough documentation style.

For the AI brief: This first patch in the series updates the README to document the build process for `diff-highlight`, which changed when the code was modularized in 2017. It now explains users must run `make` in the directory and can run tests with `make test` (after building Git itself). The patch is purely documentation with no code changes beyond the README update.