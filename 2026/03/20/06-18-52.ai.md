Performance-focused benchmark results for strbuf_getwholeline() buffer reuse.

Jeff King (Peff) provides concrete measurements showing the performance impact of Junio Hamano's proposed strbuf_getwholeline() changes, demonstrating a 7% slowdown in a line-processing heavy workload (`git cat-file --batch-check` on Linux.git objects). The benchmark shows the current buffer reuse optimization saves ~200ms in this case, with the gap narrowing but not disappearing when more work is done per line.

This reply shifts the discussion from theoretical concerns to empirical data, validating Peff's earlier claims about the optimization's real-world value. The test case specifically exercises the strbuf reuse pattern in a tight line-reading loop, making it directly relevant to the strbuf_getwholeline() debate. While the workload is somewhat artificial (processing object names with minimal per-line work), it cleanly isolates the allocation overhead in question.

The results suggest Junio's proposed simplification would impose non-trivial costs in common Git operations that process many lines. This strengthens Peff's position that the buffer reuse optimization is worth preserving despite its added complexity. The thread now has concrete data to evaluate the tradeoff between code simplicity and performance, with the numbers favoring the status quo for this particular case.