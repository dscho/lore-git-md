This is a review comment from D. Ben Knoble on Kristoffer Haugsbakk's `git name-rev --format` patch series. Knoble points out that while the patch implements `--notes` support for the new `%N` format specifier, this option isn't documented in the man page changes shown in the diff. 

The observation is technically correct - the implementation handles notes display (as noted in the v2 changelog and parent summary) but the documentation omission could lead to discoverability issues. This appears to be a minor oversight in an otherwise thorough patch series that has already addressed several rounds of review feedback.

Knoble's comment is characteristically precise and constructive, focusing on documentation completeness rather than raising new technical concerns. The thread context shows this is already a well-vetted feature with comprehensive test coverage, so this appears to be a straightforward documentation gap rather than a deeper implementation issue. The author will likely respond with either a justification for the current state or a follow-up patch to document the `--notes` interaction.