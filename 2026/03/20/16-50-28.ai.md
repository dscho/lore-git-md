This is a platform compatibility follow-up in the ongoing macOS regex behavior discussion. Junio C Hamano responds to Johannes Schindelin's analysis of the REG_ENHANCED issue, agreeing that the proposed workaround would silently change behavior by disabling enhanced regex features like word boundaries (\b). 

Junio acknowledges Schindelin's point that the meson.build system correctly handles this case by defining USE_ENHANCED_BASIC_REGULAR_EXPRESSIONS for macOS/clang combinations, and that this should theoretically be set in config.mak.uname already. The "Hmm" response suggests Junio recognizes there's a discrepancy between the expected and actual build configuration behavior that needs investigation.

The email reinforces that this is fundamentally a build system consistency issue rather than a pure platform limitation, with the meson implementation serving as the reference for correct behavior. Junio's response indicates he would not accept a solution that silently degrades functionality, keeping the focus on proper feature detection and configuration.