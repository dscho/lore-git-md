The thread discusses a bugfix for Git's bisect functionality where custom terminology wasn't being consistently applied throughout the output. The patch addresses an inconsistency that occurred when users specified alternate terms (like "old/new") for the bisect process - while the custom terms worked for the initial setup, various status messages would still use the default "good/bad" language.

The fix modifies the output strings in builtin/bisect.c to properly use the configured terms from the bisect_terms structure. This ensures all messages - including status reports and error outputs - maintain consistent terminology. The change is particularly valuable for cases where the default "good/bad" terms don't match the actual bisect goal, such as when trying to find when a bug was fixed rather than when it was introduced.

The patch appears to be a focused, well-implemented solution to this usability issue, making only the necessary string substitutions without altering the underlying bisect logic. The commit message provides clear examples demonstrating both the problem and the improved behavior.