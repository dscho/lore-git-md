Bugfix for NULL pointer dereference in path-walk.c error handling, addressing a segfault when lookup_tree() or lookup_blob() fails. The fix changes error message formatting to use entry.oid (always valid) rather than o->oid (NULL in this case). Yuvraj Singh Chauhan's patch is technically correct but missing Signed-off-by.

Tian Yuchen provided detailed analysis of error conditions in object_as_type(), noting the current "failed to find object" message conflates two distinct failure modes (missing object vs type mismatch). Junio C Hamano acknowledged this as valid for future refinement but kept it separate from the immediate NULL-dereference fix, showing a sample diff for distinguishing missing trees vs blobs in follow-up work. Tian Yuchen later clarified their position was not to expand the current fix's scope, reinforcing the boundary between the urgent segfault fix and potential error message improvements.

The change remains minimal and localized to error reporting without affecting path resolution logic. Ready for maint with added Signed-off-by. Future work could enhance error message specificity, as demonstrated by Junio's sample modification, but this remains separate from the current bugfix. Path walking is sensitive but this fix is low-risk, with maintainer and contributors carefully separating immediate fix from future enhancement opportunities.