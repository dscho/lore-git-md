Bugfix for NULL pointer dereference in path-walk.c error handling, addressing a segfault when lookup_tree() or lookup_blob() fails. The fix changes error message formatting to use entry.oid (always valid) rather than o->oid (NULL in this case). Yuvraj Singh Chauhan's patch is technically correct but missing Signed-off-by. 

Tian Yuchen provides detailed analysis of error conditions in object_as_type(), noting the current "failed to find object" message conflates two distinct failure modes (missing object vs type mismatch). Junio C Hamano acknowledges this as a valid point for future refinement but keeps it separate from the immediate NULL-dereference fix, providing a sample diff showing how error handling could distinguish between missing trees and missing blobs in a follow-up change.

The change remains minimal and localized to error reporting without affecting path resolution logic. Ready for maint with added Signed-off-by. Future work could enhance error message specificity to distinguish between lookup failures and type mismatches, as demonstrated by Junio's sample modification. Path walking is sensitive but this fix is low-risk, with the maintainer appropriately separating urgent bugfix from potential error message improvements.