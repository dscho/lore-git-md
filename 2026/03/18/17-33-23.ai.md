This is a follow-up in the thread about `git describe --dirty` behavior when used with `--git-dir` outside the repository. Thomas Braun acknowledges the explanations from Junio C Hamano and Patrick Steinhardt, confirming that the behavior is indeed expected and documented.

The key takeaway is that Thomas now understands the proper way to specify repository location is via `-C` rather than `--git-dir` for this use case. He notes his original use of `--git-dir` in 2014 predated the availability of `-C` (added in 2013), providing context for why he initially used the less appropriate option.

This email effectively closes the discussion by confirming the documentation is clear and the original behavior was correct. No further action appears needed beyond Thomas's acknowledgment of the proper usage pattern. The thread demonstrates how Git's repository discovery rules can have subtle implications for commands that check working tree state like `describe --dirty`.