Thread summary:

RFC from Jayesh Daga proposing path representation format for new `git repo info` metadata fields `paths.common_dir` and `paths.toplevel`. Current design question centers on relative vs absolute paths - author suggests defaulting to relative (matching `git rev-parse` convention) with optional `--absolute` flag for explicit needs. This affects script/tool consumption of path metadata. The RFC seeks feedback on this design choice before implementation, particularly regarding backward compatibility and consistency with existing Git conventions. No responses yet; expect discussion from contributors familiar with Git plumbing path handling.