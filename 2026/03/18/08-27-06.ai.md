This is a follow-up ping for a bugfix patch that corrects `git fetch --prune-tags` behavior to match its documentation. The patch series from Orgad Shaneh addresses a discrepancy where the flag was documented to both prune missing tags and force-update changed ones, but only implemented the pruning behavior.

The second patch (being pinged here) makes the minimal code change needed in `builtin/fetch.c` to pass the force flag when `--prune-tags` is active, plus expands the existing tag force-fetch test to verify both command-line flags (`--force` and `--prune-tags`) and the equivalent config option (`fetch.prunetags`). The implementation is straightforward - a one-line code change and test expansion that follow existing patterns.

The patch appears well-constructed with clear commit message documentation and thorough test coverage. It's a focused fix for a specific documentation/behavior mismatch in tag handling, not tied to any larger ongoing efforts. The ping suggests the author is awaiting review - this would be a good candidate for someone familiar with the fetch machinery to look at, as it touches stable but important tag-fetching behavior.