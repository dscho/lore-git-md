This is a bug report about unexpected behavior in `git grep`. Randy Dunlap reports that after applying a Linux kernel patch containing a typo ("vma_area_desc" instead of the correct "vm_area_desc"), `git grep` fails to find the incorrect string in the repository, despite the patch applying cleanly.

The issue appears to be a discrepancy between what `git grep` can find and what the patch system considers part of the repository. This could indicate either:
1. A limitation in `git grep`'s search scope (perhaps not checking certain files or states)
2. A misunderstanding about where the patched content actually resides

The report is brief but raises a valid concern about search reliability. Randy is a long-time kernel contributor (known for documentation work), so this likely represents a real-world workflow issue rather than user error. The thread may benefit from input from Git grep experts to explain or address the behavior.