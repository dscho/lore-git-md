This is a follow-up discussion about the gitweb security fix for hash parameter interpretation. Junio C Hamano responds to Nicolas George's original report, offering additional technical perspective on the issue.

Junio acknowledges the reported behavior but questions whether it truly needs fixing, noting that invalid hashes naturally produce error messages in Git. He suggests the server could redirect stderr to /dev/null if suppressing these messages is the goal. 

The maintainer then makes an interesting technical observation: using `--` to force interpretation as a tree name may actually be incorrect from Git's perspective, since `--` traditionally signals the end of options and start of pathspecs. He proposes `--end-of-options` as a more robust alternative that would work even if the `--` behavior in `ls-tree` were fixed.

While the original patch was straightforward, Junio's response highlights deeper considerations about Git's option parsing semantics. The discussion remains constructive, with Junio providing maintainer-level insight into how the proposed solution interacts with Git's broader design principles. This moves the conversation beyond just fixing the immediate issue to considering the "right" architectural approach.