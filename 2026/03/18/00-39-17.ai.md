Refactoring patch converting branch interpretation flags to an enum type.

This patch replaces the `INTERPRET_BRANCH_*` macro constants with an enum `interpret_branch_name_allowed`, and updates function signatures to use this type where branch interpretation flags are passed. The change affects several files including `object-name.[ch]`, `refs.[ch]`, and `builtin/branch.c`.

The modification maintains the same bitmask behavior (the enum values still use bit-shifted constants) while providing stronger type safety. This is a straightforward code hygiene improvement that makes the branch interpretation flags more self-documenting and type-checked. The patch appears to be a clean, mechanical conversion with no behavior changes.

Key technical details:
- The enum preserves the existing flag values (1<<0, 1<<1, 1<<2)
- Functions like `copy_branchname()` and `reinterpret()` now take the enum type
- The `allowed` field in `interpret_branch_name_options` is updated to use the enum
- Header dependencies are adjusted (refs.h now includes object-name.h)

This appears to be part of Google Summer of Code work, though the commit message doesn't specify a particular larger effort it ties into. The change is small and focused, touching only the type system aspects of branch name interpretation without modifying any behavior or adding new functionality.