Thread identity: Proposal to add `--index` as an alias for `--cached` in `git diff` while deprecating `--cached`, from an unnamed contributor via mobile email.

Core problem or goal: Introduce a more intuitive alternative to `--cached` for showing staged changes, with `--index` being proposed as the new preferred option while maintaining backward compatibility through deprecation rather than removal of `--cached`.

Current status: Initial proposal (v1) with basic implementation in `builtin/diff.c`, documentation updates, and a test case. No reviews or discussion yet.

Key technical details:
- Adds `--index` option to `git diff` with identical behavior to `--cached`
- Marks `--cached` as deprecated in documentation
- Modifies `builtin/diff.c` to handle the new option
- Adds test case in t/t4013-diff-various.sh
- Touches Documentation/diff-options.txt and Documentation/git-diff.txt

Open questions and loose ends:
- No rationale provided for why `--index` is better than `--cached`
- No discussion of potential impact on existing scripts/tools using `--cached`
- No migration plan for deprecation (timeline, warnings, etc.)
- No consideration of whether this change aligns with Git's UI principles

Participant positions: None yet - this is the initial proposal.