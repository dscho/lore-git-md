This is a bug report about unexpected behavior in `git grep`. Randy Dunlap reports that after applying a Linux kernel patch containing a typo ("vma_area_desc" instead of the correct "vm_area_desc"), `git grep` fails to find the incorrect string in the repository, despite the patch applying cleanly.

The issue appears to be a discrepancy between what `git apply` accepts and what `git grep` can find. The email doesn't provide enough technical details to determine whether this is a Git bug or some misunderstanding about the patch application process, but it clearly shows a case where a string that should be present in the codebase isn't being found by Git's search functionality.

The reporter is a known Linux kernel developer (Randy Dunlap) who frequently works on documentation, suggesting this is a legitimate issue rather than a user error. The email is written as a question rather than a bug report with full reproduction steps, so it's likely intended to start a discussion about whether this behavior is expected.