[PATCH] t1900: add tests for `git repo info` with linked worktrees and separate-git-dir

Tests coverage for the `git repo info` command's behavior with two special repository configurations: (1) worktrees created via `git worktree add` and (2) worktrees using `--separate-git-dir`. The new test cases verify both default line-oriented output and nul-delimited format (`--format=nul`), checking correct reporting of bare/shallow status in these cases.

Tian Yuchen reviewed the patch, noting the test correctness but suggesting an improvement to the test setup - creating at least one commit via `test_commit` before making worktrees, as this better reflects real-world usage where worktrees are typically created from repositories with content. While Git technically allows empty-repository worktrees, testing with actual content would be more representative.

The 39-line addition to t1900-repo-info.sh follows existing test patterns with clear setup/verification steps. This fills a testing gap for already-supported functionality without modifying production code. The tests include both positive output comparisons and binary checks for nul-delimited output. The discussion remains focused on test quality improvements rather than core functionality changes.