Bugfix completing the documented behavior of `git fetch --prune-tags` to force-update changed tags, not just prune missing ones. This is the second patch in Orgad Shaneh's two-part series addressing a discrepancy between the documented and actual behavior of tag pruning.

The patch makes a straightforward one-line change to `builtin/fetch.c` to pass the force flag when `--prune-tags` is active, matching the documentation's promise that this option should "clobber changed tags". The test updates in `t/t5516-fetch-push.sh` thoroughly verify both the `--force` and `--prune-tags` paths, plus the equivalent `fetch.prunetags` config option.

Orgad is now following up on the initial submission from a month ago, politely requesting review feedback. The patch appears well-constructed - it's minimal, focused, and comes with comprehensive test coverage that builds on existing test infrastructure. Given the clear documentation/implementation mismatch being fixed and the careful approach taken, this seems like a strong candidate for inclusion once reviewed.