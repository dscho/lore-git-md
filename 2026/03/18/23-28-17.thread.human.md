Here's a human-readable thread summary:

Randy Dunlap reported an unexpected behavior in `git grep` when searching for a typo in a Linux kernel patch. After applying a patch containing the incorrect string "vma_area_desc" (instead of the correct "vm_area_desc"), `git grep` fails to find this string in the repository, despite the patch applying cleanly. This suggests a potential discrepancy between what `git apply` accepts and what `git grep` can locate.

As an experienced Linux kernel developer who frequently works on documentation, Randy's report carries weight, though he presents it as a question rather than a formal bug report with reproduction steps. The core issue appears to be that Git's search functionality isn't finding a string that should be present in the codebase after patch application, which could indicate either a bug in `git grep` or some subtle aspect of how patches modify the working tree that isn't immediately obvious. The thread is just starting, so we'll need to see responses from Git grep experts to understand whether this is expected behavior or a genuine issue.