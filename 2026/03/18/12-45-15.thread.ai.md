Thread identity: Performance optimization discussion for commit-reach.c's `ahead_behind()` function bitmap cleanup, initiated by René Scharfe.

Core problem/goal: Optimizing the bitmap cleanup process in commit-reach.c, initially attempting to replace priority-queue-based cleanup with a bulk-free approach using `deep_clear_bit_arrays()`, but encountering unexpected performance regressions.

Current status: The initial optimization patch (11-line change introducing `free_bitmap_pointer()` helper) showed a 10% performance regression in benchmarks, contrary to expectations. The priority queue approach, despite its heap operations, proves more efficient by only processing unvisited commits rather than visiting all entries (including NULL bitmaps) as the bulk approach does.

Key technical details: The original priority queue implementation in commit-reach.c used heap operations during cleanup. The attempted optimization used `deep_clear_bit_arrays()` for bulk freeing but proved slower due to visiting more commit entries unnecessarily. Alternative approaches considered (reverting or array-order traversal) show equivalent performance in local testing.

Open questions: Whether to revert to the original implementation or pursue another optimization path, given that the theoretically cleaner approach didn't yield expected performance benefits. The performance characteristics may vary across different repository sizes and shapes.

Participant positions: René Scharfe identified and reported the regression promptly after his initial optimization attempt, demonstrating the project's commitment to performance monitoring even for clean refactoring changes. No other contributors have weighed in yet on the regression report.

Related work: This follows René's pattern of focused, clean refactoring patches, though with the unusual outcome of a performance regression rather than improvement. The thread serves as a case study in balancing theoretical cleanliness with practical performance considerations.