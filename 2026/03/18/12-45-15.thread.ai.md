Thread identity: Performance optimization discussion for commit-reach.c's `ahead_behind()` function bitmap cleanup, initiated by René Scharfe.

Core problem/goal: Optimizing the bitmap cleanup process in commit-reach.c, initially attempting to replace priority-queue-based cleanup with a bulk-free approach using `deep_clear_bit_arrays()`, but encountering unexpected performance regressions. The goal remains to streamline cleanup while maintaining or improving performance.

Current status: After an initial 11-line optimization patch showed a 10% performance regression, René has sent a v2 patch that takes a different approach. The new version (4 lines changed) replaces the priority queue extraction with direct array iteration while still avoiding heap operations. This represents a middle ground between the original queue-based approach and the failed bulk-free optimization.

Key technical details: The v2 patch walks the prio_queue's items directly via array indices rather than using queue extraction, maintaining the goal of avoiding heap operations but with simpler traversal logic. It continues using `free_bit_array()` per commit rather than the problematic `deep_clear_bit_arrays()`. The original priority queue approach remains the performance baseline being compared against.

Open questions: Whether this array-traversal approach will show performance improvements in benchmarks compared to both the original and the regressed v1. The thread may need input from performance-focused contributors like Jeff King or Derrick Stolee to evaluate the tradeoffs.

Participant positions: René Scharfe continues driving the optimization effort, quickly iterating from the initial regression to a refined approach. His patches maintain the project's standards for clean, focused changes even when performance results don't match expectations. No other contributors have yet commented on the v2 approach.

Related work: This follows René's pattern of careful refactoring work, now serving as a case study in performance optimization iteration. The thread demonstrates how even theoretically cleaner approaches must prove themselves in practice against Git's performance standards.