This is a follow-up discussion in Kristoffer Haugsbakk's patch series adding `--format` support to `git name-rev`. The email clarifies a technical point about note handling in the implementation.

Kristoffer responds to Junio's question about whether the patch properly handles cases where a commit has no notes. He confirms this is already tested in the series (via the `--name-rev --format=<pretty> with a note` test case) and explains the current implementation gets a fresh pointer from a strbuf before each use. However, he agrees with Junio that using `FREE_AND_NULL()` would be cleaner to avoid any potential dangling pointer issues, even though the current approach is technically correct.

The exchange shows careful attention to memory management details in the `--format` implementation, particularly around the notes display functionality. Kristoffer maintains the original patch's approach while acknowledging the reviewer's suggestion for improved robustness. This continues the thread's pattern of thorough technical review focused on implementation correctness in `builtin/name-rev.c`.