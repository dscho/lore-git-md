Thread summary:

Bug report from Randy Dunlap about `git grep` failing to find a string ("vma_area_desc") that was introduced via a patch that applied cleanly to the Linux kernel repository. The discrepancy between `git apply` accepting the patch and `git grep` not finding the introduced string suggests either a bug in Git's grep functionality or a misunderstanding about how patches modify the working tree. The reporter is a credible Linux kernel developer known for documentation work, indicating this is likely a real issue worth investigating. The email frames it as a question to start discussion rather than providing full reproduction steps, leaving open whether this is expected behavior or a genuine bug. Key technical detail: the string in question was a typo ("vma_area_desc" instead of correct "vm_area_desc") in a kernel patch. No responses or analysis yet about why grep might miss this string despite successful patch application.