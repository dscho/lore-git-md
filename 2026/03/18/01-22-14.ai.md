This is a bugfix discussion about standardizing exit codes for help flags in Git's option parsing. Jeff King (Peff) concurs with brian m. carlson's analysis that changing `git rev-parse --parseopt` to exit with status 0 (success) rather than 129 (error) for `--help`/`-h` requests would be a strict improvement with minimal backward compatibility risk. The thread has reached clear consensus that this should proceed as a regular bug fix rather than waiting for Git 3.0, as the change aligns with Git's standard help behavior and any scripts depending on the current behavior likely represent poor design that should be fixed regardless. 

Peff's response confirms that the technical analysis is complete and the path forward is clear, moving this from discussion to implementation. The change will affect all commands using Git's parse-options infrastructure, with the regression in `git instaweb -h` being the most visible symptom of the current inconsistent behavior. Both participants agree this is a straightforward improvement that users will welcome rather than find disruptive. 

The patch will need to update `parse-options.c` and include appropriate documentation in the RelNotes, but the core technical debate about whether to proceed is now settled with maintainer approval. This represents a small but meaningful step toward consistency in Git's command-line interface behavior.