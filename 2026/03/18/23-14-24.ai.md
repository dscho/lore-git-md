This is a feature patch proposing to introduce a new `--index` option to `git diff` while deprecating the existing `--cached` option. The patch comes from an email that appears to be sent from a mobile device, with minimal context or explanation in the body.

The key technical points are:
- Proposes adding `--index` as an alias for `--cached` in `git diff`
- Marks `--cached` as deprecated in the documentation
- Touches the `builtin/diff.c` implementation and related documentation files
- Includes a test_dwim test case for the new option

The patch is quite minimal in its current form, lacking detailed motivation for why this change is being proposed or what problem it solves. The commit message doesn't reference any prior discussion or rationale for deprecating the long-standing `--cached` option. The implementation appears straightforward but would benefit from more context about the reasoning behind the change and any potential impact on existing scripts or workflows that use `--cached`.