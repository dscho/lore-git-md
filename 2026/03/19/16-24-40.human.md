This is a performance optimization patch from René Scharfe, revisiting his earlier attempt to simplify bitmap cleanup in commit-reach.c's `ahead_behind()` function. The patch follows up on unexpected performance regression discovered in the initial optimization attempt.

The new version takes a different approach - instead of using `deep_clear_bit_arrays()` (which proved slower), it now iterates directly over the priority queue's internal array to free remaining bitmaps. This maintains the original performance characteristics while still simplifying the cleanup logic by removing the priority queue extraction step.

Key changes:
- Replaces the priority queue extraction loop with direct array iteration
- Keeps the same memory safety guarantees (handles NULL pointers correctly)
- Still avoids the overhead of heap rebalancing operations
- Results in cleaner code than the original while matching its performance

This demonstrates René's characteristic attention to detail - when the first optimization didn't pan out as expected, he found an alternative approach that achieves both code cleanliness and performance goals. The patch is small (6 lines modified) but meaningful, showing how subtle implementation choices can impact performance in the commit graph algorithms.