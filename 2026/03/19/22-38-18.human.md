This is a feature patch from Mirko Faina that improves the usability of `git format-patch --commit-list-format` by making the "log:" prefix optional when specifying custom format strings. The change allows users to omit the prefix as long as the format string contains a placeholder (indicated by a '%' character).

The patch updates the documentation to clarify this new behavior and adds two test cases: one verifying that format strings without the prefix work when they contain placeholders, and another confirming that format strings without placeholders still require the prefix. The implementation is straightforward, checking for the presence of '%' to determine whether to treat the input as a custom format string.

This change builds on Mirko's ongoing work to improve the commit list formatting in format-patch, following the recent renaming from `--cover-letter-format` to `--commit-list-format`. The patch appears well-tested and maintains backward compatibility while making the feature more convenient to use. It's a small but thoughtful quality-of-life improvement that aligns with Git's general approach to format string handling.