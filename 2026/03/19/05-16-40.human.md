This is a troubleshooting thread about `git grep` behavior, where Junio C Hamano provides a key clarification about patch application modes. 

Junio points out that Randy Dunlap may have used `git apply` without the `--index` flag, which would leave the new file in an "untracked" state where `git grep` wouldn't search it. This explains the discrepancy between Randy's empty grep results and Jeff King's successful search - Jeff likely used `--index` or committed the patch first. 

The thread has now identified the probable root cause: `git apply` behavior differences rather than a `git grep` bug. This is a subtle but important distinction in Git's workflow that affects how new files are indexed for searching. The discussion remains technical and focused, with Junio providing the missing piece that reconciles the earlier observations.