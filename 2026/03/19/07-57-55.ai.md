This is a review response in an ongoing thread about a refactoring patch to the rerere subsystem. Patrick Steinhardt confirms Junio Hamano's earlier patch that removed obsolete NULL checks for strbuf.buf in the conflict resolution code.

Patrick agrees with Junio's rationale that since 2007, strbuf.buf has been guaranteed to never be NULL (pointing instead to a static NUL byte when empty). He notes this makes the defensive checks unnecessary, as passing the .buf member directly to git_hash_update() is now always safe. The change simplifies the hash computation during conflict resolution while maintaining identical behavior.

As a reviewer with deep knowledge of Git's internals, Patrick's approval carries weight - particularly since he works extensively with low-level data structures. His confirmation that the patch correctly handles the strbuf_slopbuf case provides additional technical validation of Junio's change. The thread shows two senior contributors agreeing on a straightforward cleanup of historical technical debt in the rerere subsystem.