This is part 3 of Patrick Steinhardt's 8-patch series reorganizing Git's build infrastructure. The patch moves the `coverage-diff.sh` script from `contrib/` to the new `tools/` directory, continuing the effort to separate essential developer tools from optional contributions.

The change is straightforward - the script is simply relocated with no functional modifications. The commit message explains that `coverage-diff.sh` (which analyzes test coverage) is more appropriately classified as core developer tooling rather than an optional contribution. This follows the series' goal of cleaning up the `contrib/` directory by moving build-related scripts to their new home in `tools/`.

As with the rest of the series, this is a mechanical change that updates build system references to the script's new location. The patch demonstrates careful attention to maintaining compatibility while improving the project's organization. Since this is part of a well-reviewed series that's nearing merge readiness, it can be expected to proceed smoothly.