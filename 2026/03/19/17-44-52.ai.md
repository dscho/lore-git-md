Performance optimization refinement for commit-reach.c's bitmap cleanup.

Junio C Hamano acknowledges René Scharfe's v2 patch simplifying the bitmap cleanup in `ahead_behind()`. The patch replaces the priority queue extraction loop with direct array iteration to free remaining bitmaps, maintaining the performance characteristics while being conceptually cleaner. Junio notes the change is "quite obvious and straightforward" and indicates he will queue it.

This marks the conclusion of this optimization thread, where initial attempts to use `deep_clear_bit_arrays()` showed regressions, leading to this refined approach. The final solution walks the prio_queue's internal array directly rather than using queue operations, achieving equivalent performance to the original while being simpler. The change is minimal (4 lines modified) and focused, typical of René's precise refactoring style.

The thread demonstrated Git's performance-sensitive review culture, with Jeff King's analysis confirming the priority queue's unexpected efficiency and René iterating to find an equally performant but cleaner solution. While no speedup was achieved, the discussion illuminated important allocation patterns in commit-slab usage that may inform future optimizations.