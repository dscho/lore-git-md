Thread summary:

This thread introduces a new `--cc` option for `get_maintainer.pl` (a Linux kernel development tool) to format its output as comma-separated emails suitable for direct use with `git send-email --cc`. The patch modifies the Perl script's output formatting logic when `--cc` is active, disabling multiline and role information to produce clean email lists. The commit message explains the use case of piping to `git send-email` and mentions a complementary Git patch (not included here) that would enable direct execution of `get_maintainer.pl` as a `git send-email` argument. While targeting Linux kernel infrastructure, this change is being coordinated with Git due to its interaction with `git send-email`. The implementation appears correct and well-contained, focusing solely on output formatting changes.