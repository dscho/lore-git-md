This is a follow-up discussion about the proposed `--cc` option for `get_maintainer.pl`. Joe Perches responds to Jim Cromie's patch with skepticism about its utility, suggesting that using a script would be preferable to the new `--cc` option. The exchange highlights a difference of opinion about the best way to integrate `get_maintainer.pl` with `git send-email`.

The original patch proposed two ways to use the new feature: either by piping the output or by having `git send-email` directly execute the script. Joe's brief reply indicates he doesn't see value in either approach compared to existing scripting solutions. This suggests the feature may face resistance unless stronger use cases are presented.

The thread context shows this is part of a cross-project coordination between Linux kernel and Git tooling, though the practical benefits remain under discussion. The implementation itself (from the parent summary) appears technically sound, but its adoption may hinge on demonstrating clearer advantages over current workflows.