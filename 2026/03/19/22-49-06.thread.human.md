This thread involves Patrick Steinhardt's ongoing work to improve object counting in Git's object database. The current discussion focuses on a small but important fix caught during review.

Ramsay Jones, known for his attention to portability issues, spotted a sparse warning in Patrick's series where a plain integer (0) was being passed where a NULL pointer was expected. The one-line change modifies `odb_source_loose_count_objects()` to properly pass NULL instead of 0 when calling `odb_source_loose_for_each_object()`. While functionally equivalent, this correction makes the code more type-correct and aligns with the function's documented interface.

The fix is intended to be squashed into commit 2b24db1110 from Patrick's 'ps/object-counting' branch currently in the 'seen' integration branch. This is typical of the refinement process as patches move toward being merged - catching minor issues before graduation to more stable branches. The change doesn't affect runtime behavior but improves code quality and static analysis compliance.