This is the third patch in Patrick Steinhardt's 14-part series refactoring Git's object name handling as part of the ODB abstraction effort. The patch introduces a new `struct odb_for_each_object_options` to replace direct flag parameters in object iteration functions, preparing for future extensions like prefix-based iteration.

The change systematically replaces flag parameters with the new options structure across 11 files, including core object database code, packfile handling, and command implementations (`cat-file`, `pack-objects`). The patch maintains all existing functionality while making the interface more extensible - a typical pattern in Patrick's ODB abstraction work.

This is a mechanical but important infrastructure change that follows the series' pattern of careful, incremental refactoring. The patch appears thorough, updating all relevant callers and maintaining backward compatibility through a wrapper function. While not flashy, it's the kind of foundational work needed to enable the series' larger goals of making object handling backend-agnostic.