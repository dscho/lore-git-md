This is a follow-up review comment from Junio Hamano on Patrick Steinhardt's ODB abstraction series, specifically addressing the first patch that modernized the oidtree code. The patch had renamed functions and improved documentation as preparatory work for later changes to object name handling.

Junio notes that while the original code used `GIT_MAX_HEXSZ` to validate the hex length (a conservative global maximum), the new version could potentially use a more algorithm-specific limit since it now has access to the hash algorithm via `prefix->algo`. This suggests tightening the validation to match the actual hash function being used rather than the global maximum.

The comment shows Junio's attention to detail in reviewing even preparatory refactoring patches, looking for opportunities to make the code more precise. Since this is a minor optimization suggestion rather than a blocking issue, and given the productive tone of previous exchanges in this thread, Patrick will likely address this with a simple follow-up patch if he agrees with the suggestion. The comment doesn't indicate any deeper concerns about the architectural direction of the series.