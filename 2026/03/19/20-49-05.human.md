Refactoring patch simplifying strvec operations across multiple files.

René Scharfe sends a straightforward cleanup patch that replaces manual loops pushing individual elements between strvecs with direct calls to `strvec_pushv()`. This affects three files:
- fetch-pack.c: simplifies copying args to index_pack_args
- git.c: streamlines argv construction when executing commands
- submodule.c: cleans up submodule fetch argument handling

The changes are mechanical but improve readability by using the existing strvec API more idiomatically. As expected from René, this is a clean, focused patch with no behavior changes - just removing redundant loops in favor of direct bulk operations. The patch touches core command execution paths but in a way that's clearly safe.