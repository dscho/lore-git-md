This is a refactoring patch from René Scharfe that simplifies code by using `strvec_pushv()` to append all elements of one strvec to another, rather than pushing them individually in loops. The change affects three files:

1. In `fetch-pack.c`, it replaces a loop that copies elements from `cmd.args` to `index_pack_args` with a single `strvec_pushv()` call.
2. In `git.c`, it similarly replaces a loop copying `args` to `cmd.args` with `strvec_pushv()`.
3. In `submodule.c`, it does the same for copying `options` to `spf.args`.

The patch is a straightforward cleanup that reduces code duplication and improves readability by using the existing `strvec_pushv()` helper function. This is typical of René Scharfe's contributions, which often focus on small, clean refactorings that improve code hygiene without changing behavior. The changes are mechanical and unlikely to be controversial, touching only internal implementation details rather than user-facing behavior.