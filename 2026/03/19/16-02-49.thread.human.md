This thread introduces a straightforward test modernization patch that updates a single test assertion in `t5315-pack-objects-compression.sh`. The change replaces a raw shell `test -f` check with Git's preferred `test_path_is_file` helper function, which provides better error messages when tests fail. The patch is part of an ongoing effort to standardize test assertions across Git's test suite. The change is minimal and uncontroversial, touching only the necessary line while preserving the test's original logic. The commit message clearly explains the motivation (alignment with test suite conventions) and how the change was identified (via git grep). This appears to be a routine improvement to test infrastructure that follows established patterns.