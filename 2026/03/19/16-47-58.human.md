This is a discussion thread about extending `git send-email`'s script execution capabilities. D. Ben Knoble responds to Kristoffer Haugsbakk's question about the functional difference between the new script execution feature (for `--to/--cc/--bcc` flags) and the existing `--cc-cmd/--to-cmd` options.

Knoble notes the only apparent difference is that the existing options don't include a `--bcc-cmd` variant, while the new approach would cover all three recipient types uniformly. He shares his personal workflow using `--cc-cmd` with `git-contacts` for Git development, mentioning the current need to manually track discussion participants as a pain point.

The exchange highlights an ongoing ambiguity about whether this change meaningfully extends functionality or just duplicates existing mechanisms in a slightly different form. Knoble's response suggests the patch might fill a genuine gap (BCC automation), but doesn't fully address whether the implementation approach is justified versus extending the existing `--*-cmd` options. The thread remains open for the original author to clarify the design rationale.