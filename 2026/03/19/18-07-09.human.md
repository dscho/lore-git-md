This is a response from Junio C Hamano (the Git maintainer) to Tian Yuchen's question about the side effects in `check_repository_format_gently()`. Junio clarifies that in Git's codebase, the term "check" doesn't imply being side-effect-free - functions with "check" in their name can legitimately modify state as part of their operation. He also explains that "gently" specifically means the function returns an error code rather than calling `die()`, allowing the caller to handle failures.

The response is characteristically terse but authoritative, coming directly from the maintainer. It provides important context about Git's naming conventions that wasn't apparent from just reading the code. While it doesn't address the multi-repository implications Tian raised, it does establish that the current behavior is intentional within Git's design philosophy. This suggests that any changes to make the function more side-effect-free would need to be justified as an intentional design shift rather than framed as fixing an oversight.