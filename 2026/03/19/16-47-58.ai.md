This is a follow-up discussion about the `git send-email` script execution feature. D. Ben Knoble responds to Kristoffer Haugsbakk's question about the distinction between the new script support in `--to/--cc/--bcc` flags and the existing `--cc-cmd` option family. 

Knoble notes the only apparent difference is that while `--cc-cmd` and `--to-cmd` exist, there is no `--bcc-cmd` equivalent in the current implementation. He shares his personal workflow using `--cc-cmd` with `git-contacts` for git.git contributions, mentioning the manual effort still required to track discussion participants.

The response doesn't fully resolve the functional overlap question raised earlier, but provides practical context about how one contributor uses similar existing functionality. This suggests the new feature might be most valuable for filling the `--bcc-cmd` gap rather than replacing existing mechanisms. The thread continues to lack a clear technical distinction between the approaches, leaving open whether this warrants a new interface or should extend the existing `--*-cmd` pattern.