Performance optimization follow-up in commit-reach.c's bitmap cleanup.

René Scharfe sends a revised version of his earlier optimization for the `ahead_behind()` function's bitmap cleanup in commit-reach.c. This v2 patch simplifies the approach from the first version while still addressing the same performance regression.

The key change is moving from using `deep_clear_bit_arrays()` (which proved slower in benchmarks) to a direct array iteration approach. The new version walks the prio_queue's items directly via their array indices rather than extracting them through the queue interface. This maintains the original goal of avoiding unnecessary heap operations during cleanup while being more performant than the bulk-free method.

The patch is minimal (4 lines changed) and focused - it replaces the prio_queue extraction loop with a simple array traversal, still calling `free_bit_array()` on each remaining commit. This appears to be a middle ground between the original queue-based approach and the bulk-free optimization that didn't pan out.

This revision shows the typical Git development pattern where initial optimizations are refined based on benchmark results. The commit message is concise but clear about the intent to avoid unnecessary ordering during cleanup. As with René's typical patches, the change is clean and well-contained, though its performance impact will need verification against the regression seen in the first attempt.