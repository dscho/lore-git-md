This is a test modernization patch that updates a test script to use Git's preferred path-checking helper function.

The patch changes a single test assertion in `t5315-pack-objects-compression.sh` from using the raw shell `test -f` to the Git-specific `test_path_is_file` helper. This is part of an ongoing effort to standardize test assertions across Git's test suite. The change is functionally equivalent but provides better error messages when tests fail.

The commit message is straightforward and correctly identifies the motivation (alignment with test suite conventions) and how the change was found (via git grep). The patch itself is minimal and focused, touching only the necessary line while preserving the test's logic. This appears to be a clean, uncontroversial improvement to test infrastructure.