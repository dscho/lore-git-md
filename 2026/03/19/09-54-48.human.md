This is a review comment on the third patch in Derrick Stolee's series enhancing `git backfill` for partial clones. Patrick Steinhardt suggests using `add_head_to_pending()` rather than directly calling `handle_revision_arg("HEAD")` when no revisions are specified. This is a minor implementation detail that would make the code more consistent with Git's standard revision-walking patterns.

The suggestion comes late in the review cycle - the series already has Junio's acks and is otherwise ready for integration. Patrick's comment is constructive but doesn't raise any concerns about the patch's core functionality, which adds revision argument support to `git backfill`. The change would be a small cleanup rather than a substantive revision.

The thread context shows this is part of a completed series that enables more targeted blob downloads in partial clones, with all technical feedback already addressed. Patrick's suggestion about `add_head_to_pending()` appears to be the last remaining comment before the series can be merged.