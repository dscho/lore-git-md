This is a follow-up discussion about the `git send-email` script execution feature. Kristoffer Haugsbakk questions the distinction between the new `--to/--cc/--bcc` script support and the existing `--cc-cmd` option family, noting they seem functionally similar. The thread context shows this is in response to Jim Cromie's patch that added executable script support to recipient flags, with co-development credit suggesting Google tooling origins.

The key open question here is about the practical difference between the new approach and the existing `--*-cmd` options. The original patch's motivation wasn't entirely clear on this point, and Kristoffer's query highlights a potential overlap in functionality that may need clarification. This could become a sticking point in the discussion unless the original rationale for introducing a new mechanism is explained more thoroughly.

For future reference, the technical context remains: modified `git-send-email.perl` to execute scripts passed as recipient arguments, with tests in `t9001-send-email.sh`. The unresolved question is how this differs from `--cc-cmd`/`--to-cmd`/`--bcc-cmd` behavior, which wasn't addressed in the initial patch. This distinction will likely be central to any follow-up discussion.