This is a follow-up discussion in the `git backfill` feature series, which adds pathspec support to enable targeted blob downloads in partial clones. Patrick Steinhardt weighs in on the handling of complex pathspecs in patch 4/5.

The exchange clarifies the current implementation's behavior when encountering unsupported pathspec patterns - it falls back to downloading extra blobs rather than failing. Patrick agrees this is reasonable for now but emphasizes the need for clear documentation that this behavior may change if future optimizations allow more precise path filtering. This matches the series' incremental approach where wildcard support comes in the next patch.

The discussion shows the series is nearing completion, with only minor documentation polish remaining. Junio has already acked all patches, and this late input from Patrick doesn't block integration but rather provides helpful perspective on communicating the tradeoffs to users. The thread demonstrates how Git contributors coordinate to balance immediate functionality with future extensibility.