This is the final patch in Patrick Steinhardt's 14-part series refactoring Git's object name handling for the ODB (object database) abstraction effort. The patch introduces a new generic `odb_find_abbrev_len()` function that makes object abbreviation length calculation fully pluggable across different storage backends.

The change moves the remaining abbreviation length calculation logic from `object-name.c` into the ODB layer, completing the backend-agnostic approach started in earlier patches. Key changes include:
- A new `odb_find_abbrev_len()` function that serves as the main interface
- Source-specific callback functions to handle backend-specific implementations
- Removal of the now-redundant code from `object-name.c`

This patch follows the same careful, incremental approach seen throughout the series - each change builds on prior infrastructure while maintaining strict behavioral compatibility. The work continues Patrick's systematic effort to make Git's object storage layer more modular, paving the way for alternative backends while keeping the existing functionality intact.

The series appears well-structured and thorough, typical of Patrick's contributions to the ODB abstraction work. While the changes are significant, they're focused and methodical, with each patch clearly advancing the overall goal. This final piece completes the object name handling refactoring that began in patch 1.