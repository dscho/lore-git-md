Platform compatibility fix addressing a sparse warning in Patrick Steinhardt's object counting series.

This is a one-line change to fix a sparse warning about using a plain integer (0) where a NULL pointer is expected. The patch modifies `odb_source_loose_count_objects()` in object-file.c to pass NULL instead of 0 as the final argument to `odb_source_loose_for_each_object()`. The change is straightforward and clearly correct - it aligns with the function's expected interface while maintaining identical behavior.

Ramsay Jones, who specializes in portability fixes, noticed this while reviewing Patrick's ongoing "ps/object-counting" branch. The patch is meant to be squashed into commit 2b24db1110 ("object-file: generalize counting objects") from the 'seen' branch. This is typical maintenance work during the review cycle - catching minor issues before a series graduates to 'next' or 'master'. The change doesn't affect functionality but improves code correctness by properly typing the NULL pointer.