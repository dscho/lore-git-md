This is a test modernization patch that updates a test case to use Git's preferred path-checking helper function.

Bilal El Khatabi replaces a direct `test -f` shell command with the more robust `test_path_is_file` helper in `t5315-pack-objects-compression.sh`. This change is part of an ongoing effort to standardize test assertions across Git's test suite, making tests more consistent and reliable.

The patch is straightforward and uncontroversial - it's a mechanical conversion that doesn't change the test's behavior, just its implementation. The commit message clearly explains the motivation (aligning with Git's test suite conventions) and how the change was found (via a grep for direct test commands).

This appears to be a clean, well-motivated change that follows the project's test modernization patterns. The patch is small and focused, touching only one line while improving test hygiene.