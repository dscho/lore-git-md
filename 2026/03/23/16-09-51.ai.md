This is a documentation and code consistency patch in the `git replay` plumbing command series. It's the second of three patches that introduce a new `--ref` option for more precise control over ref updates during replay operations.

The patch makes two main improvements to the `git-replay` command's interface:

1. Updates the documentation to consistently use `--option=value` syntax rather than space-separated forms, making it clearer that these options require values. This affects the man page synopsis and usage strings for `--onto`, `--advance`, `--revert`, and `--ref-action`.

2. Marks all value-taking options with `PARSE_OPT_NONEG` in the option parsing code to explicitly indicate they cannot be negated (like `--no-option`). This is a defensive coding practice that prevents potential future confusion.

The changes are mechanical but important for consistency - they don't modify any functionality but make the command's interface more precise and self-documenting. The patch follows the command's documentation and built-in help text, and precedes the actual feature addition in patch 3/3.

For future summarizers: This preparatory patch standardizes option syntax documentation and parsing for `git-replay`'s existing options before introducing the new `--ref` feature. It touches `Documentation/git-replay.adoc` and `builtin/replay.c`, focusing on usage strings and option parsing configuration without changing behavior. The changes align with Git's standard practices for documenting value-taking options.