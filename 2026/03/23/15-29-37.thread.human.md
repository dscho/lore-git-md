This thread introduces a documentation and user interface improvement for Git's `repo` subcommands. Mahi Kassa has submitted a patch that makes `git repo info -h` and `git repo structure -h` show only the relevant subcommand's help text, rather than the generic top-level usage information.

The change is well-contained, modifying the builtin implementation to use separate usage arrays for each subcommand while maintaining the existing behavior for `git repo -h`. Tests have been added to verify the new focused help output. This should make it easier for users to quickly find the specific syntax they need when working with these subcommands.

The patch appears ready for merging - it's a straightforward quality-of-life improvement with clear commit messages and appropriate test coverage. No substantive discussion or objections have been raised so far in the thread.