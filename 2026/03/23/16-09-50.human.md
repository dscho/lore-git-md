This is a test cleanup patch, the first in a 3-part series introducing a new `--ref` option to `git replay`. The patch standardizes the formatting of command-line options in the `t3650-replay-basics.sh` test script, consistently using equals signs for options that take values (e.g., `--onto=main` instead of `--onto main`).

The changes are purely mechanical - no test behavior is altered. The author, Toon Claes, explains this makes it easier to distinguish options from positional arguments. The patch touches 35 lines across the test file, all following the same simple pattern of adding equals signs where missing.

This is straightforward test hygiene that improves readability while maintaining the exact same test coverage. The changes look correct and well-scoped, serving as a clean foundation for the subsequent patches that will introduce new functionality.