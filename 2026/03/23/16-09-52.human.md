This is the final patch in a series adding a `--ref` option to `git replay`, allowing users to specify exactly which reference should be updated by the replay operation. The patch builds on the preparatory work from the first two patches in the series, which standardized option syntax and documentation.

The new `--ref` option gives users more control by letting them specify an arbitrary ref to update, rather than relying on the refs implied by `--onto`, `--advance`, or `--revert`. This is particularly useful when working with bare commit IDs instead of branch names. The implementation includes thorough test coverage verifying the new functionality works with all existing replay modes while maintaining backward compatibility.

The patch touches the replay command's documentation, implementation, and test suite. It follows Git's standard practices for option handling and includes clear examples in the documentation. The changes look well-considered and focused, with the tests covering both happy paths and error cases (like incompatible options and invalid refnames). This appears to be a clean, useful addition to the replay plumbing command's capabilities.