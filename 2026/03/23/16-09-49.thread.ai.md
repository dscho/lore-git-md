Updated AI thread summary:

Git replay plumbing command series introducing `--ref` option for precise ref updates, authored by Toon Claes. Builds on Siddharth Asthana's `--revert` work. Complete 3-patch series now available:

1. Patch 1/3 standardized test option syntax in `t3650-replay-basics.sh` to use consistent `--option=value` form
2. Patch 2/3 improved documentation (`Documentation/git-replay.adoc`) and parsing in `builtin/replay.c` by:
   * Using `--option=value` syntax consistently
   * Adding `PARSE_OPT_NONEG` flag to prevent negation of value-taking options
3. Patch 3/3 (new) implements core `--ref` functionality:
   * Adds `--ref` parameter to CLI and internal APIs
   * Requires fully-qualified refs (starting with `refs/`)
   * Takes precedence over `--advance`/`--revert` when specified
   * Includes comprehensive tests for interactions with other options and edge cases

Technical details:
- Maintains backward compatibility
- Touches replay implementation, documentation, tests
- Follows Git standard practices for option handling
- Adds validation for ref naming rules
- Documents practical use cases with examples

Open questions:
- Review feedback pending on the final implementation
- May need verification of ref validation edge cases

Related work:
- Builds on Siddharth Asthana's `--revert` implementation
- Follows established patterns from other Git commands

Participant positions:
- Toon Claes (author) driving the series
- Reviewers yet to weigh in on final implementation
- Expect scrutiny on ref validation and test coverage

The series appears well-structured, with preparatory work in early patches leading to the feature implementation. The `--ref` option provides useful flexibility for scripting scenarios where direct ref specification is preferred over inferred updates.