Thread identity: 2-patch series on improving custom bisect term handling in Git, addressing inconsistencies when alternate terms (e.g. "old"/"new") are used instead of default "good"/"bad". Follow-up to v1 which only partially addressed the issue.

Core problem: When users configure custom bisect terms via `git bisect start --term-(good|bad)`, some commands (`bisect status`, `rev-parse --bisect`) still hardcode "good"/"bad" in their output/behavior rather than respecting the configured terms.

Current status: v2 series with two patches:
1. Fixes `git bisect status` to show configured terms instead of hardcoded "good"/"bad" (detailed in this email)
2. Makes `git rev-parse --bisect` respect configured terms when looking up refs (to follow)

Key technical details:
- Changes in builtin/bisect.c for status output:
  - Updates bisect_print_status() to use terms->term_good/bad
  - Modifies error messages in bisect_run() to use configured terms
- New test coverage in t6030-bisect-porcelain.sh:
  - Verifies status output with custom terms
  - Checks bisect run messages
  - Tests with example terms "term1"/"term2"
- Follows up on v1 by being more comprehensive in term handling

Open questions: None explicitly raised yet - appears ready for review. Previous version (v1) only addressed the bisect status case, so this expands scope to be more complete.

Participant positions: Original author (not named in this cover letter) has iterated from v1 to v2 by adding tests and expanding term handling. No other participants mentioned yet in this thread.

Related work: Builds on Git's existing bisect term configuration capability, which allows custom terminology but had these inconsistencies in some commands.