This is a review response in the `git replay` thread about the new `--ref` option implementation. Tian Yuchen points out two potential issues with Toon Claes' patch:

1. **Comment-code mismatch**: The comment suggests `--ref` ignores `--contained`, but the code shows `die_for_incompatible_opt2()` being called when both `--ref` and `--contained` are used, which would error out rather than ignore.

2. **Multi-branch handling**: When multiple branches are specified (like `topic1 topic2`) along with `--ref`, the current implementation would only update the single `--ref` target while potentially orphaning the other branches, as the `update_refs` logic from `--onto` processing gets discarded.

The reviewer provides a concrete example showing how `git replay --onto main --ref refs/heads/target topic1 topic2` could lead to unexpected behavior. The core concern is that the patch doesn't properly handle the interaction between the new `--ref` option and the existing branch-update logic when multiple branches are involved.

This appears to be a substantive technical review pointing out edge cases the original implementation may have missed. The issues raised suggest the `--ref` option needs either better validation to prevent invalid combinations or additional logic to handle multi-branch scenarios correctly. The reviewer's analysis indicates familiarity with the replay command's internals and the patch's intended behavior.