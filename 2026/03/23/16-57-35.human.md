This is the final patch in Mirko Faina's 8-part series improving `git format-patch --commit-list-format`. The series has evolved through three iterations with feedback incorporated, and this patch adds a usability improvement to the custom format string handling.

The change allows users to omit the "log:" prefix when specifying a custom format string on the command line, as long as the string contains a '%' placeholder. This makes the interface more convenient while maintaining backward compatibility. The patch includes tests verifying both the new behavior (accepting prefix-less formats with placeholders) and the error case (rejecting prefix-less strings without placeholders).

This completes the series' goal of making commit list formatting more flexible and user-friendly. The implementation is straightforward, building on the infrastructure established in earlier patches. With all documentation and edge cases now addressed, the series appears ready for merging.