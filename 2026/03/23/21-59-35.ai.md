This is the final patch in Pablo Sabater's v4 series adding `--graph-lane-limit` support, providing documentation and test coverage for the new feature. The patch adds a clear explanation of the option to `rev-list-options.adoc` and includes two test cases in `t4215-log-skewed-merges.sh` that verify the lane limiting behavior with different graph topologies.

The documentation succinctly describes the option's purpose (limiting graph lanes when `--graph` is used) and its default behavior (no limit). The tests demonstrate practical scenarios: one limiting to 2 lanes on a single tip commit, and another limiting to 3 lanes across multiple tips. Both tests check that excess lanes are properly truncated with '.' markers while preserving the '*' commit markers on the first hidden lane.

This patch addresses all prior feedback from Junio Hamano and other reviewers, completing the series with proper documentation and verification. The implementation now matches Git's conventions for option handling (using `int` type and treating zero/negative as unlimited) and removes the temporary `MINIMUM_GRAPH_COLUMNS` macro. The tests reuse existing graph setups from `t4215` to validate the new behavior without introducing additional test infrastructure.

With this patch, the series fulfills its goal of addressing terminal readability for wide commit histories while maintaining the graph's structural information. The changes are well-contained to graph.c, graph.h, revision.c, and the documentation/tests, with no outstanding technical issues from previous reviews. The patch appears ready for final maintainer consideration.