This thread discusses improvements to Git's bisect functionality when using custom terminology (like "old"/"new" instead of the default "good"/"bad"). The original author has sent a 2-patch series (v2) that builds on previous feedback.

The first patch focuses on making `git bisect status` properly respect configured terms in its output messages, rather than hardcoding "good"/"bad". It updates the status reporting in builtin/bisect.c and adds test coverage to verify the behavior works with custom terms. The second patch (not yet discussed in detail) will extend this consistency to `git rev-parse --bisect` operations.

This is a straightforward improvement that makes bisect's behavior more consistent when using alternative terminology. The changes are well-contained and come with thorough test coverage. The series appears ready for review, having expanded its scope from v1 to address more command-line interfaces. No open questions or objections have been raised yet in the thread.