This is a documentation follow-up to Kristoffer Haugsbakk's `git name-rev --format` patch series, addressing a minor oversight in the man page coverage. The patch series itself adds `--format` support to `git name-rev`, modeled after `git log --format`, with comprehensive implementation and test coverage already established through prior review rounds.

In this email, Haugsbakk acknowledges D. Ben Knoble's observation that while the implementation properly handles `--notes` with the new `%N` format specifier, this interaction wasn't documented in the man page changes. The author confirms this was an unintentional omission during the final revision pass, indicating they'll likely include the documentation in a future version.

The exchange demonstrates the typical refinement process on the Git mailing list - a well-implemented feature receiving final polish through attentive review. Knoble's comment was characteristically precise, catching a documentation gap without raising new technical concerns. Haugsbakk's response is prompt and constructive, showing the patch series remains in good shape with only minor documentation updates needed before finalization.

This thread illustrates how even mature patch series benefit from multiple review passes, with documentation completeness being one of the final quality gates before merging. The technical implementation appears sound, having already addressed memory management, edge cases, and test coverage in prior iterations. The remaining work is purely presentational - ensuring users can discover all available features through the official documentation.