This is a refactoring patch that's part of Derrick Stolee's v2 series enhancing the `git backfill` command for partial clone functionality. The patch fixes a hidden header inclusion dependency in `revision.h` where the `REV_INFO_INIT` macro uses `DEFAULT_ABBREV` from `object-name.h` without explicitly including it.

The change is straightforward - it adds `#include "object-name.h"` to `revision.h` so consumers of `REV_INFO_INIT` don't need to know about this implementation detail. This is a clean, mechanical improvement that follows good header hygiene practices. The patch is small (just one line added) and uncontroversial, as evidenced by Junio C Hamano's ack in the thread context.

This foundational change enables the subsequent patches in the series that work with revision parsing and pathspec handling for the enhanced `git backfill` functionality. While minor on its own, it's an important prerequisite for the more substantive changes that follow in the series.