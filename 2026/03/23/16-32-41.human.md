This is a platform compatibility discussion about a macOS build issue that may resurface despite a temporary fix. Johannes Schindelin provides additional context about why GitHub Actions CI is now passing without his proposed REG_ENHANCED workaround - the runner team rolled back a problematic macOS image that was using Homebrew's LLVM clang instead of Apple system clang. 

However, Johannes notes this is only a temporary reprieve. The underlying Homebrew change that caused the issue (auto-linking versioned LLVM) still exists in their codebase, meaning future runner image updates could reintroduce the build failure. He suggests keeping his one-line workaround in `compat/regex/regcomp.c` as insurance against this recurring.

The email provides valuable technical background about the root cause (Homebrew's LLVM clang not seeing macOS SDK headers) and the sequence of Homebrew changes that led to the issue. While the immediate problem is resolved, this appears to be an unstable situation that could break again with future runner updates. The patch remains small and harmless, making it reasonable to keep as preventative maintenance.