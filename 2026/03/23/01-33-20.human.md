This is the final technical clarification for Derrick Stolee's `git backfill` series, which enhances partial clone functionality with wildcard pathspec support. Stolee confirms that disabling commit pruning (change #2 in the original patch) is actually unnecessary - the pathspec matching will work correctly without it, and keeping pruning enabled will improve performance by avoiding unnecessary tree traversals. 

The series has already been fully reviewed and acked by Junio, with all substantive feedback addressed. This email just cleans up one remaining optimization question, confirming the more efficient approach. The patch is now in its final form, ready for integration. This completes Stolee's work to enable targeted blob downloads in partial clones using both exact paths and wildcard patterns, a feature that will be particularly useful for operations like `git blame` on large repositories.