This is the final patch in Derrick Stolee's v2 series enhancing `git backfill` for partial clones. The series has already been acked by Junio C Hamano and addresses all previous feedback.

The patch adds test coverage for handling unknown arguments in `git backfill`, formalizing the command's behavior when given invalid inputs. It ensures the command properly reports ambiguous arguments rather than silently ignoring them, fixing a behavior noticed by Siddharth Shrimali. The test verifies both standalone invalid arguments and cases where they appear alongside valid flags like `--all` and `--first-parent`.

This completes the series' test coverage, complementing the earlier technical improvements to revision parsing and pathspec handling. The implementation now properly integrates with Git's standard argument parsing infrastructure while maintaining the performance optimizations for partial clone scenarios. With this final test in place, the series appears ready for integration.