This is a feature patch series for the `git replay` plumbing command, introducing a new `--ref` option to give users more control over which ref gets updated. The series comes from Toon Claes and builds on Siddharth Asthana's recent work adding `--revert` to `git replay`.

The cover letter explains that while subcommands were previously considered for `git replay`, the current approach keeps the status quo while adding the useful `--ref` option. This addresses a specific need expressed in earlier discussion (referenced via Message-ID). The first two patches clean up documentation and option handling consistency before introducing the new feature in the final patch.

The series touches several files including the replay implementation, its documentation, and test updates. The changes look focused and well-motivated, with test coverage expanded to verify the new functionality. This appears to be a straightforward plumbing improvement that gives users more precise control over ref updates during replay operations.

For future summarizers: The key technical detail is the new `--ref` option that allows specifying an arbitrary ref to update, rather than relying on the refs implied by the revision range or `--advance`/`--revert` options. The implementation modifies `replay.c` and `builtin/replay.c` to handle this new parameter while maintaining existing behavior when it's not specified. Tests in `t3650-replay-basics.sh` verify both the new option and the documentation/consistency improvements.