Here's an updated human-readable summary of the thread:

Toon Claes is working on enhancing the `git replay` command with a new `--ref` option that gives users precise control over which reference gets updated during replay operations. The current 3-patch series (v2) focuses on standardizing documentation and implementing the new functionality with proper validation.

The first patch brings all documentation into alignment with Git conventions, consistently using the "stuck form" (`--option=value`) syntax throughout both the man page and command-line help. This change, prompted by Kristoffer Haugsbakk's feedback, makes the interface more consistent with other Git commands.

The core functionality comes in patch 3/3, which introduces the `--ref` option with several important safeguards:
- Requires a single tip commit when used with `--onto`
- Validates refname formats
- Explicitly marks the option as incompatible with `--contained`
- Includes comprehensive tests for the new behavior

The implementation touches the replay machinery in `replay.c` and `builtin/replay.c`, while maintaining backward compatibility for existing uses. Toon is currently working through some technical challenges around multi-branch scenarios to prevent orphaned refs, and plans to make option incompatibilities more explicit in the final version.

Reviewers have generally approved the direction, with Kristoffer Haugsbakk's documentation suggestions already incorporated. The series shows steady progress toward a more flexible `git replay` command while maintaining Git's standard practices for option handling and validation.