This is a test cleanup patch in Toon Claes' series adding `--ref` support to `git replay`. The patch standardizes how options with values are written in the `t3650-replay-basics.sh` test file, consistently using `--option=value` syntax rather than mixing with `--option value` forms.

The change is purely mechanical - updating 35 test cases to use the equal-sign form for options like `--onto`, `--advance`, and `--revert`. This makes the tests more consistent and helps visually distinguish options from positional arguments. The patch doesn't modify any test logic or assertions, just the command formatting.

For future summarizers: The key detail is the standardization of option syntax in `t3650-replay-basics.sh` to always use `--option=value` form. This affects commands using `--onto`, `--advance`, `--revert`, and `--ref-action` options throughout the test file. The changes are straightforward and maintain existing test behavior while improving consistency.