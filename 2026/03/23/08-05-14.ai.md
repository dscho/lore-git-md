This is a refactoring patch in Christian Couder's series introducing URL-based whitelisting for promisor remotes. Patch 11/16 restructures the control flow in `should_accept_remote()` to prepare for upcoming URL-based acceptance logic.

The changes move the empty-URL check to the top of the function so it applies to all acceptance modes, not just ACCEPT_KNOWN_URL. It also inverts the URL comparison logic to have a single exit path at the bottom, making the function easier to extend in subsequent patches. The diff shows these are purely structural changes - the behavior remains identical, just organized differently to accommodate future work.

This continues the pattern seen in earlier patches of carefully preparing the codebase before introducing new functionality. The patch maintains the series' focus on security by keeping the strict URL validation while making the control flow more maintainable. The warning messages about ignored remotes remain unchanged, preserving the existing user experience during this transitional refactoring.