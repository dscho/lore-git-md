This is a follow-up discussion about the recently approved bugfix for `git-remote-http` refspec parsing crashes. Jeff King (Peff) raises an architectural question about whether the solution should instead align with an earlier approach that sets a default hash algorithm in repository-less contexts, similar to what was done for `git ls-remote` in commit 9e89dcb66a.

Peff suggests two alternative approaches that would maintain consistency with how other repository-less operations handle hash algorithms: either setting `GIT_HASH_DEFAULT` in `remote-curl.c` when operating outside a repository (matching the existing `ls-remote` behavior), or keeping the current patch and reverting 9e89dcb66a if this is the only remaining case needing special handling. This indicates the discussion may not be fully settled despite Junio's earlier approval.

The email highlights an underlying tension in Git's design around how to handle hash algorithm selection in repository-less operations. Peff's suggestion implies the current solution, while correct, might create an inconsistency with other parts of the codebase that take a different approach to the same fundamental problem. This could reopen discussion about the architectural direction before the patch is merged.