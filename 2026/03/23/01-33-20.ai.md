This is the final technical clarification in Derrick Stolee's series enhancing `git backfill` with wildcard pathspec support for partial clones. Stolee confirms that disabling commit pruning (change #2 in the original patch) is actually unnecessary - the wildcard pathspec functionality works correctly without it, which should improve performance by allowing the revision walk to skip irrelevant commits. 

The key realization is that `tree_entry_interesting()` can handle the wildcard matching during tree traversal without needing to disable pruning at the commit level. This addresses Junio's performance concern while maintaining all the desired functionality. Stolee indicates only changes #1 (the exact_pathspecs flag) and #3 (blob-level pathspec matching) are truly required.

With this clarification, the series appears complete with all substantive feedback addressed. The earlier acks from Junio combined with this performance optimization confirmation suggest the patches are ready for integration. This represents the final piece of Stolee's work to enable efficient targeted blob downloads in partial clones using both exact and wildcard pathspecs.