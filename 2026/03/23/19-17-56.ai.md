This is a documentation/style discussion in Toon Claes' series adding `--ref` support to `git replay`. Kristoffer Haugsbakk responds to the first patch in the series, which standardized test option syntax in `t3650-replay-basics.sh` to consistently use `--option=value` form.

Kristoffer suggests strengthening the commit message's rationale beyond just consistency. He points to `gitcli(7)` which recommends the "stuck form" (no equal sign) as Git's preferred style for command-line options. His argument is that following established Git conventions is a stronger justification than internal test consistency alone, and that the stuck form helps prevent option/argument confusion when writing commands.

The discussion touches on Git's command-line interface guidelines and the tradeoffs between readability (equal sign makes options more visually distinct) versus safety (stuck form reduces parsing ambiguity). This is a minor but meaningful style point that could influence how future tests are written, though the actual test behavior remains unchanged.

For future summarizers: The key detail is Kristoffer's reference to `gitcli(7)` advocating for the stuck form (`--option value`) over equal-sign form (`--option=value`). This suggests the test cleanup might need reconsideration to align with Git's documented CLI guidelines rather than just achieving internal consistency. The discussion could impact how options are written in other test files if this precedent is followed.