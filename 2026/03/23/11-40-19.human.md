This is the final patch in Derrick Stolee's v2 series enhancing `git backfill` for partial clones. The series has already been acked by Junio and addresses all previous feedback.

The patch adds test coverage for handling unknown arguments in `git backfill`, ensuring the command properly rejects invalid inputs with clear error messages. This was motivated by a report from Siddharth Shrimali about the command previously ignoring unknown arguments silently. The test verifies the command now detects and reports ambiguous arguments in both simple and complex cases (including when mixed with valid flags like `--all` and `--first-parent`).

This is a straightforward test addition that completes the series' validation of the enhanced argument parsing` behavior. With this patch, the series appears ready for integration, having addressed all technical concerns and added comprehensive test coverage for the new functionality.