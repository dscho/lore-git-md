This is the final patch in a feature series adding a `--ref` option to `git replay`. The patch introduces the ability to specify an arbitrary ref to update with the replay result, overriding the default behavior of inferring refs from the revision range or `--advance`/`--revert` options.

The implementation adds the `--ref` parameter to both the command-line interface and internal APIs, with validation to ensure the ref is fully qualified (starts with `refs/`). When `--ref` is used, it takes precedence over other ref-updating options. The patch includes comprehensive tests verifying the new functionality works with `--onto`, `--advance`, and `--revert`, while also checking edge cases like invalid refnames and interaction with `--contained`.

The changes touch the replay command's documentation, option parsing, core implementation, and test suite. The documentation updates clearly explain the new option's behavior and include practical examples showing how to use bare commit IDs with `--ref` instead of branch names. The implementation follows Git's standard patterns for option handling and ref validation.

This completes the series that began with documentation/parsing cleanup in patches 1-2. The feature looks well-motivated and implemented, with thorough test coverage. The changes are focused and maintain backward compatibility while adding useful flexibility to the replay command. Reviewers will likely want to verify the ref validation logic and test coverage, but the patch appears ready for consideration.