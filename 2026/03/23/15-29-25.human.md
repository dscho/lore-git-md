This is a test patch completing the `git backfill` feature series, now with Junio's final review comments.

Junio notes that while the new test verifies basic unknown argument handling, the behavior could be more precisely aligned with Git's standard revision parsing rules. He outlines the expected behavior for commands that take both revisions and pathspecs, suggesting the test could better reflect how `setup_revisions()` normally processes arguments (with explicit `--` disambiguation and working tree path validation). 

The patch itself adds straightforward test coverage for invalid arguments, but Junio's response indicates the implementation might benefit from deeper integration with Git's existing argument parsing conventions. His examples show how commands like `git log` handle ambiguous arguments by checking working tree paths when no `--` is present. This isn't a blocking concern - the series already has Junio's acks - but suggests a potential area for future refinement in how `git backfill` integrates with core parsing infrastructure.

The exchange confirms the series is technically complete and ready for merging, while leaving the door open for follow-up work to align argument handling more closely with Git's established patterns. This level of polish is typical for the final review of a feature series before integration.