This is a documentation and consistency patch in Toon Claes' series adding a `--ref` option to `git replay`. It's the second of three patches, following the cover letter and preceding the actual feature implementation.

The patch makes two straightforward improvements to the `git-replay` command's interface: First, it updates the documentation to consistently use `--option=value` syntax rather than space-separated forms, making it clearer that these options require arguments. Second, it marks all option definitions in the code as `PARSE_OPT_NONEG` since none of them are designed to be negatable (like `--no-option`).

These changes are purely about presentation and consistency - they don't modify any behavior. The patch touches both the man page and the option parsing code in `builtin/replay.c`, with the changes looking clean and well-contained. This appears to be preparatory work for the actual feature addition in the next patch, getting the documentation and option handling into a consistent with Git's conventions.