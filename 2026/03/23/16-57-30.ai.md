This is a feature patch (3/8 in v3 series) from Mirko Faina renaming the `--cover-letter-format` option to `--commit-list-format` in `git format-patch`. The change aligns the command-line option name with the existing `format.commitListFormat` config variable and improves tab completion behavior.

The patch makes several key changes:
1. Renames the option to match the config variable name
2. Makes specifying the format imply `--cover-letter` unless `--no-cover-letter` is given
3. Updates documentation to reflect the new name and behavior
4. Modifies tests to use the new option name while maintaining the same functionality
5. Removes the old option from completion tests

The changes are straightforward and maintain backward compatibility through the config variable name. The patch is part of a larger series improving commit list formatting in patch emails, with this particular change addressing naming consistency and usability. Test coverage remains thorough, verifying both the new option name and the implied cover letter behavior.

For the AI summarizer: This patch focuses on the option renaming aspect of the series, changing `--cover-letter-format` to `--commit-list-format` in builtin/log.c, documentation, and tests. Key technical points include the new implied `--cover-letter` behavior when the format is specified, removal of the old option from completion tests, and preservation of all existing functionality under the new name. The changes are localized to option handling and documentation with no core logic modifications.