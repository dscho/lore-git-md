This is a thread continuation about argument validation for `git backfill`, now touching on coordination with planned revision argument support.

Junio C Hamano acknowledges Derrick Stolee's point that the current validation approach (rejecting all non-option arguments) would conflict with an upcoming series that plans to add revision argument support to `git backfill`. Junio notes this makes the current patch "completely irrelevant" if revision arguments will be accepted, as the validation responsibility would shift to the revision walk machinery in that case.

The thread has taken a turn from being merge-ready to needing coordination with parallel development. Junio suggests the revision argument work (#2070) will need to handle its own argument validation, potentially making the current patch unnecessary. This introduces a dependency that wasn't previously considered - the validation logic can't be finalized without knowing how revision arguments will be processed.

The path forward now depends on either:
1. The revision argument series progressing to clarify its validation needs
2. An agreement to proceed with the current strict validation as an interim step
3. A coordinated plan for how both features will handle arguments

The patch itself remains technically sound for its original purpose, but its relevance is now in question due to this newly surfaced context about future functionality.