Christian Couder's 16-patch series introduces URL-based whitelisting for auto-configuring promisor remotes through new `promisor.acceptFromServerUrl` config option. The series systematically refactors promisor-remote handling (1-11), cleans tests (12), implements feature (13-15), and updates documentation (16).

Key technical components:
- New `promisor_info` struct with `local_name` field for remote tracking (consolidated from three separate data structures in patch 7)
- URL pattern validation and normalization (lowercasing, percent-decoding, path resolution)
- `url_matches_accept_list()` helper for secure whitelist matching
- Automatic remote creation via `sanitize_remote_name()` and `promisor_remote_name_from_url()`
- Collision handling with numeric suffixes and `remote.<name>.advertisedAs` for auditing
- Patch 8 refactors internal tracking by removing redundant `accepted` strvec in favor of unified `accepted_remotes` string_list

Security measures include:
- Default `ACCEPT_NONE` policy
- Configurable name prefixes ("promisor-auto-")
- Strict URL normalization to prevent bypass attempts
- Comprehensive test coverage of edge cases

Review activity:
- Junio Hamano raised security concerns about glob pattern matching rules, suggesting restricting `*` in domain portions from matching slashes while maintaining current behavior for path portions
- Patrick Steinhardt has provided detailed feedback across multiple patches (1-10), now examining edge case behavior in URL validation refactoring
  - In patch 10, probes whether moving URL validation earlier in `should_accept_remote()` might unintentionally fix real-world cases where remotes advertise empty URLs
  - Maintains security-conscious review pattern by questioning interaction with malformed remote advertisements
  - Earlier addressed patches 1-9 with implementation refinements (commit message wording, logic confirmation, comment suggestions)
  - Maintains pattern of constructive refinement without blocking progress

The discussion continues to balance security hardening with backward compatibility, particularly around URL matching rules and edge case handling. Junio's reviews focus on security documentation and glob pattern behavior, while Patrick systematically examines implementation details and real-world deployment scenarios. Patches 7-9 have received full approval pending minor documentation tweaks, with current attention on patch 10's refactoring and empty URL handling implications as part of the broader preparation for URL-based whitelisting functionality. The final patch (16) will document both existing and new configuration options with security warnings and integration guidance.