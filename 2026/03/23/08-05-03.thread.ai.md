Christian Couder's 16-patch series introduces URL-based allowlisting for auto-configuring promisor remotes via new `promisor.acceptFromServerUrl` config option. The series refactors promisor-remote handling (1-11), cleans tests (12), implements feature (13-15), and updates documentation (16). Security-focused response addresses Junio Hamano's concerns about URL pattern matching rules.

Key technical components:
- New `promisor_info` struct with `local_name` field for remote tracking (consolidated from three separate data structures in patch 7)
- Strict URL pattern validation enforcing `*` cannot cross part boundaries (scheme/host/path)
- `url_matches_accept_list()` helper for secure allowlist matching
- Automatic remote creation via `sanitize_remote_name()` and `promisor_remote_name_from_url()`
- Collision handling with numeric suffixes and `remote.<name>.advertisedAs` for auditing
- Security model now explicitly documented:
  - Allowlisted URLs bypass `promisor.acceptFromServer=none`
  - Terminology updated from "whitelist" to "allowlist"
  - Warnings about protocol schemes and glob patterns
  - Risks of broad patterns on shared hosting

Review activity:
- Junio Hamano's security concerns addressed with stricter glob matching rules
  - Now awaiting confirmation that documentation improvements will appear in next version
  - Maintainer expects reroll but acknowledges Patrick's reviews may require more than just doc changes
- Patrick Steinhardt continues systematic review:
  - Examining design rationale in patch 14's URL matching
  - Questioning whether new config is preferable to extending existing `promisor.acceptFromRemote`
  - Proposing clearer separation of concerns into distinct config variables
  - Noted patch 14's commit message leans toward implementation over design
  - Suggested adding concrete URL pattern examples in docs
  - Earlier examined patch 13's URL allowlist caching
  - In patch 10, probed URL validation timing in `should_accept_remote()`
  - Maintains security-conscious review approach
- Patrick proposes splitting series:
  - First 12 patches (plus parts of 16) ready for merging:
    - Priority fixes for promisor_remote_get_direct()
    - URLmatch API helper exposure
    - Major refactoring of promisor-remote handling
    - Test cleanup for file:// URI handling
  - More discussion-prone new functionality (13-15) for later review

Open questions:
- Whether to split functionality into distinct config options:
  - `acceptFromServer` for runtime behavior
  - `storeFromServer` for persistence
  - `updateFromServer` for maintenance
- Practical considerations prevent hardcoding secure protocol allowlist
- Final documentation wording for security warnings
- Whether Patrick's ongoing review feedback will require substantive changes beyond documentation

Series restructuring likely to proceed with uncontroversial refactoring patches first, allowing focused security review on remaining functionality. Christian demonstrates careful attention to security implications while maintaining flexibility for real-world use cases. Documentation improvements help administrators understand trust model and make informed decisions. Junio's latest confirms expectation of reroll while recognizing Patrick's reviews may shape substantial changes.