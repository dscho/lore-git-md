This is the final documentation patch in Christian Couder's 16-part series introducing URL-based whitelisting for auto-configuring promisor remotes. The patch improves the documentation for the `promisor.acceptFromServer` config option to address several clarity issues and better integrate with the new `promisor.acceptFromServerUrl` feature introduced earlier in the series.

The changes reorganize the documentation into clearer bullet points that explicitly show all options and mark the default (`none`). It fixes an error where "knownName" was described as checking names "advertised by the client" rather than the server, and adds a note explaining the relationship with the new `acceptFromServerUrl` option for automatic remote creation.

The documentation now better distinguishes between the existing behavior (which only works with pre-configured remotes) and the new automatic configuration capability. The formatting changes make the security implications of each option more visible, with warnings about less secure choices like `all` and `knownName`.

This appears to be a straightforward documentation improvement that completes the series by ensuring users understand both the original and new promisor remote configuration options. The patch maintains the technical accuracy while making the information more accessible.