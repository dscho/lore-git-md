This is the first patch in Christian Couder's 16-part series introducing URL-based whitelisting for promisor remotes. The patch refactors the promisor remote fetching logic to prioritize "accepted" remotes (those explicitly approved by the client) over other configured remotes.

The key change modifies `promisor_remote_get_direct()` to first try fetching from accepted remotes before falling back to non-accepted ones. This better reflects the intended behavior where server-advertised promisor remotes should be preferred when the client has explicitly accepted them. The implementation extracts the fetching logic into a new `try_promisor_remotes()` helper function that can be called with an `accepted_only` flag.

The patch is a preparatory refactoring that lays groundwork for the series' main feature - auto-configuration of promisor remotes based on URL patterns. The change is logically sound and maintains backward compatibility while improving the priority ordering of remote fetches. The diff shows 46 lines changed with a clear separation between the new helper function and the modified main logic.