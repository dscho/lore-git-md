Test modernization patch updating t2107 to use standard test helper.

Jayesh Daga sends a straightforward test script update that replaces a raw `! test -f` check with the standard `test_path_is_missing` helper in t/t2107-update-index-basic.sh. This is part of the ongoing effort to modernize Git's test suite by using consistent helper functions rather than direct shell test operations.

The change is small and mechanical - just a one-line replacement in a test that verifies lockfile cleanup after an index update. The commit message explains the motivation (consistency with other tests) and includes a GSoC-related header that appears to be boilerplate from the submission template rather than directly relevant to this change.

This is a typical test infrastructure improvement patch - uncontroversial, clearly motivated, and following established patterns. It's the kind of change that would likely be picked up quickly during the next merge window.