Junio C Hamano (the Git maintainer) weighs in on the `git branch --show-current -v` bugfix thread, putting the patch on hold pending further discussion. This is a thread continuation about rejecting incompatible flag combinations in `git branch`.

The patch had previously been acked by Junio, but he now notes that context was missing from this iteration, making his earlier ack confusing. More importantly, both Phillip Wood and Sergey have independently questioned whether the flags should be rejected outright or if `--show-current -v` could be made meaningful (perhaps showing verbose information about the current branch).

Junio agrees with Phillip's technical suggestions to use `die_for_incompatible_opt2()` for consistent error messages and to improve the test by verifying the exact error output. However, he won't queue the patch yet because the fundamental approach (rejecting versus implementing the combination) needs reconsideration. The thread is now in a holding pattern while participants explore whether `--show-current -v` could have a sensible meaning rather than being forbidden.

This is a small but instructive case of Git's review process - even straightforward changes get careful scrutiny, and sometimes what appears to be a simple bugfix prompts deeper design questions. The patch may still proceed as-is after discussion, but the maintainer wants to ensure all options are considered first.