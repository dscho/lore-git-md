Thread identity: Test modernization patch fixing incorrect usage of `test_path_is_missing` helper in `t/pack-refs-tests.sh`.

Core problem: The test incorrectly included `-f` flag when calling `test_path_is_missing` helper function, which only accepts a path argument. This caused test failures with "bug in the test script" errors.

Current status: Single-line change committed, removing the `-f` flag while maintaining test intention to verify ref file pruning.

Key technical details: Changes `! test -f` pattern to proper `test_path_is_missing` usage in pack-refs test script. Part of broader test suite modernization effort to replace direct shell test commands with framework helpers.

Participant positions: No reviews or discussion yet, but change is straightforward and clearly correct - follows established test modernization patterns.

Related work: Ongoing test suite modernization effort to use framework helpers (`test_path_is_missing`, `test_path_is_file`) instead of direct shell test commands.