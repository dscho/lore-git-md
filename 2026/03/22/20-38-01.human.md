This is the third and final patch in Pablo Sabater's feature series (v3) adding a `--graph-lane-limit` option to constrain `git log --graph` output width. The patch completes the implementation by adding documentation and test cases.

The documentation update in `rev-list-options.adoc` clearly explains the new option's behavior: it limits displayed graph lanes (with excess shown as '.' markers) when used with `--graph`. The tests verify the truncation behavior works as expected, with one test showing a 2-column limit and another demonstrating a 3-column limit with multiple tips.

The test cases reuse the existing skewed merge graph structure from `t4215-log-skewed-merges.sh`, showing the truncation markers properly handle complex merge scenarios. The implementation appears thorough, with the tests covering both simple and complex graph cases.

This patch follows through on the design established in the earlier patches, providing the necessary documentation and verification for the new feature. The test coverage looks solid, and the documentation is clear about both the behavior and default (no limit). With this patch, the series appears complete and ready for final review.