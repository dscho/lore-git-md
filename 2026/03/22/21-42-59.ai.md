Junio C Hamano responds to Phillip Wood's review of the `--show-current` and `-v` flag interaction patch, putting the change on hold pending further discussion of whether the flags could meaningfully work together rather than being rejected outright. 

The maintainer notes that both Phillip Wood and Sergey have independently questioned the decision to error out, suggesting the behavior should be considered more carefully. Junio withdraws his previous ack (while acknowledging it was technically correct for the patch as originally proposed) and indicates he won't queue this version until the design question is resolved.

Technical specifics mentioned include:
- The current patch uses a direct `die()` call that should be replaced with `die_for_incompatible_opt2()` for consistency
- The test should verify the exact error message using `test_grep`
- The Acked-by tag was incorrectly retained from an earlier iteration

This email effectively resets the discussion back to design phase, asking whether `--show-current -v` could have useful semantics (like showing verbose info for just the current branch) rather than being rejected. The implementation polish suggestions remain relevant but are now secondary to deciding the fundamental approach.