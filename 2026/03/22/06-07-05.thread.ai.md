[PATCH] branch: reject --show-current with -v

Thread identity: bugfix for `git branch` command, addressing incompatible flag combination between `--show-current` and `-v`.

Core problem: `git branch --show-current -v` produces nonsensical output since `--show-current` simply prints the current branch name while `-v` shows verbose ref information. The combination should be explicitly rejected.

Current status: Patch is at v2 with updated commit message (from "remove unnecessary verbose flag" to "reject --show-current with -v"). Junio C Hamano has acked the change in previous iteration.

Key technical details: Adds error check in `builtin/branch.c` to die with clear message when both flags are used together. Test case added in `t3200-branch.sh` verifies the behavior.

Open questions: None - change is straightforward and uncontroversial. Junio's ack indicates readiness for merging.

Participant positions: Junio C Hamano acked previous version. Author improved commit message based on feedback.

Related work: None - isolated bugfix for specific flag interaction in `git branch`.