This is a bugfix for the `git branch` command, addressing an incompatible combination of flags. The patch makes `git branch --show-current` explicitly reject the `-v` (verbose) option when both are used together, as the verbose output doesn't make sense with `--show-current` which simply prints the current branch name.

The change adds an error check in `builtin/branch.c` that dies with a clear message when these options are combined, along with a test case in `t3200-branch.sh` to verify the behavior. The patch is small and focused, with Junio C Hamano having already acked it in a previous iteration. The author is resending with an updated commit message to better reflect the change's nature (from "remove unnecessary verbose flag" to "reject --show-current with -v").

The fix is straightforward and uncontroversial - it simply prevents a nonsensical flag combination rather than changing any core behavior. The test coverage ensures the new error case is properly handled. Given Junio's ack and the patch's limited scope, this appears ready for merging.