This is a follow-up discussion on a bugfix for `git branch` that prevents the incompatible combination of `--show-current` and `-v` flags. Phillip Wood provides review feedback on the v2 patch, suggesting improvements to both the implementation and test coverage.

The core issue remains straightforward - these flags don't make sense together since `--show-current` just prints the branch name while `-v` shows verbose ref information. The patch already has Junio's ack from previous discussion, but Phillip points out two specific areas for improvement:

1. The error message should use `die_for_incompatible_opt2()` for consistency with other commands' error reporting
2. The test should verify the specific error message, not just that the command fails

Phillip also clarifies that the author incorrectly kept Junio's ack tag when resending the patch, since an ack doesn't carry forward across patch versions unless explicitly re-given. He raises an interesting question about whether `--show-current` could be enhanced to work with `-v` rather than rejecting the combination, though this seems more like a side thought than a blocking concern.

The changes requested are minor but would make the patch more robust and consistent with Git's conventions. Given the prior ack and limited scope, this still appears to be a simple, uncontroversial fix that just needs these small adjustments before merging.