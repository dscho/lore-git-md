Performance optimization follow-up for strbuf_getwholeline().

Jeff King (Peff) responds to René Scharfe's concerns about behavioral consistency between the getdelim() and non-getdelim() implementations of strbuf_getwholeline(). The discussion has moved from performance improvements to deeper questions about error handling semantics. Peff agrees that both implementations should match in their treatment of unallocated strbufs, noting the current inconsistency where the non-getdelim() version returns partial reads while letting callers handle errors.

The key unresolved question is whether to discard partial reads (as getdelim() does) or preserve them (as the fallback implementation does). Peff suggests this may be a practical non-issue since once a FILE handle errors, the stream state is effectively unrecoverable regardless. He speculates about getdelim()'s internal error reporting behavior but doesn't investigate implementations, maintaining the pragmatic stance that followed earlier performance testing.

This exchange appears to be winding down the technical discussion, with Peff implicitly accepting that the current inconsistencies may not warrant further changes given the function's stable behavior over time. The thread serves as a reference point for future strbuf.buf NULL check removals, demonstrating Git's collaborative approach to balancing performance, safety, and behavioral consistency.