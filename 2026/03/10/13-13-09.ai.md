Maintainer approval for `git apply -p` input validation fix.

Junio C Hamano has queued Mirko Faina's third version of a patch that fixes argument parsing for `git apply -p`. The change replaces unsafe `atoi()` usage with proper validation via `strtol_i()` in `apply.c`, ensuring malformed inputs like "1q" or non-numeric strings are properly caught rather than silently converted to unexpected values.

The patch now includes comprehensive test coverage in a new `t4120-apply-popt.sh` file, which Junio notes is a better home for these tests than the previous location. Tests verify correct handling of valid inputs and proper rejection of malformed numbers, trailing non-digits, and negative values - all with proper cleanup via `test_when_finished`. The implementation remains technically sound, maintaining backward compatibility while adding validation.

This follows a similar pattern to a prior fix for the `-j` option parsing (commit 8f0a265), though with slightly different behavior requirements. With all requested test improvements implemented and Junio's explicit approval ("Will queue"), this bugfix appears ready for integration.