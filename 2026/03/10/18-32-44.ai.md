This is a follow-up discussion in the GSoC project thread about promisor remote fetch ordering, specifically addressing the prospective contributor's questions about configuration behavior. The email confirms and builds upon earlier technical clarifications from Christian Couder.

Lorenzo Pegorari summarizes their understanding of how `extensions.partialClone` interacts with multiple promisor remotes, noting that: 1) without this config, remotes marked with `remote.<name>.promisor` are tried in config order, and 2) when present, the `extensions.partialClone` remote serves as a fallback. Christian Couder confirms this interpretation matches the documented behavior in `partial-clone.adoc`.

The discussion then turns to backward compatibility - the new fetch ordering mechanism must continue supporting the existing `extensions.partialClone` behavior even as it makes the configuration redundant. Christian agrees this is necessary, and Lorenzo notes they've incorporated this constraint into their GSoC proposal.

Key technical points confirmed: the current precedence rules between global and per-remote promisor configurations, and the requirement that any new ordering system maintain compatibility with existing setups. The exchange demonstrates Lorenzo's thorough preparation through testing and documentation review, with Christian providing authoritative validation of the current behavior. This establishes a solid technical baseline for the GSoC project's design phase.