This is a review comment on the final iteration (v7) of a patch series introducing new pretty-format placeholders for cover letters. Phillip Wood suggests two improvements to the error handling in the new `%(count)` and `%(total)` placeholders:

1. The error message should explicitly name the unsupported format specifier rather than using a generic "this format specifier" message. He provides the concrete suggestion to use `die(_("%s is not supported by this command"), "%(count)");` for better user feedback.

2. He notes that Git's development practice is to avoid adding code that cannot be exercised, suggesting the changes might be better squashed into a later patch that actually introduces the specifier support and documentation.

The context shows this is part of a mature series that has already gone through six previous iterations and received maintainer approval, with the documentation now complete. Phillip's comments focus on polish rather than fundamental issues - improving error messaging and following project conventions about when code should be added relative to its first use. The thread summary indicates this series is otherwise ready for merging, so these would likely be minor follow-up tweaks if accepted.