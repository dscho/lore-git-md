This is a technical clarification in the ongoing performance investigation of slow `git fetch` operations over SMB/CIFS. brian m. carlson responds to R. Diez's question about Git's SSH transport mechanism, explaining that Git does not use standard SSH file transfer operations (SFTP) but instead invokes `git-upload-pack` on the remote side when available. The response clarifies that Git requires the remote system to have Git installed when using SSH transport, as it communicates with the remote Git process over stdin/stdout rather than performing direct file operations.

The email provides a key technical distinction: while mounting a remote filesystem via `sshfs` would allow parallel operations through the FUSE layer, Git's native SSH transport is designed around executing remote Git commands, which provides better performance for typical Git operations (like fetching only needed objects) but requires Git to be installed on the remote system. brian m. carlson includes a `GIT_TRACE` example showing the SSH command Git uses to connect to GitHub, demonstrating the protocol in action.

This response narrows the investigation's scope by confirming that SSH-based fetches follow the same protocol pattern as the SMB/CIFS case being studied, with similar sequential operation characteristics. The clarification about remote Git requirements may help R. Diez understand why certain optimizations aren't available in the protocol's current design, while the `sshfs` mention provides an alternative approach for cases where direct filesystem access is possible.