This is a small refactoring patch in the submodule helper code that has already been reviewed and acked by Patrick Steinhardt. The change replaces a raw `malloc()` with Git's standard `xmalloc()` wrapper (which dies on allocation failure) and modernizes the allocation style to use `sizeof(*temp)` rather than explicitly naming the struct type.

Junio notes that the commit message should explain why removing the type cast is an improvement (likely because in C, `void*` conversions are implicit, making the cast unnecessary and potentially masking errors). The patch itself is straightforward and uncontroversial - it improves consistency with Git's error handling conventions while maintaining identical behavior. 

Since the change has already received positive review from an established contributor and addresses a clear code quality issue, it's likely to be merged soon. The only remaining action is for the author to resend with an updated commit message per Junio's suggestion.