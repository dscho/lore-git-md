Bugfix for `git apply -p` argument parsing. This v2 patch from Mirko Faina addresses the unsafe `atoi()` usage in the `-p` option handler by switching to `strtol_i()` with proper validation. The change ensures malformed inputs like "1q" or non-numeric strings are properly rejected rather than silently converted to partial values.

The patch adds four test cases to `t4103-apply-binary.sh` (moving them from a standalone test as suggested by Junio) that verify the new behavior: valid numeric input, malformed input, non-numeric input, and negative numbers. The implementation maintains backward compatibility for valid inputs while adding proper error handling.

This follows a similar prior fix for the `-j` option and represents a straightforward improvement to make the command more robust against invalid inputs. The technical approach is sound and the test coverage appears thorough. The patch is now properly integrated into the existing test suite per maintainer feedback.