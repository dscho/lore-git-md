Bugfix patch addressing argument parsing in `git apply -p` with maintainer feedback on test improvements.

Junio C Hamano provides review feedback on Mirko Faina's v2 patch that replaces `atoi()` with `strtol_i()` for parsing the `-p` argument in `git apply`. The core technical change is already sound - the conversion to `strtol_i()` with proper error handling for non-numeric and negative inputs. However, Junio identifies several opportunities to improve the test coverage and clarity.

The review suggests making the test assertions more direct (using `test_path_is_dir` rather than counting files) and questions whether the test cleanup is thorough enough to prevent false positives. Junio also notes that while the binary patch test context is valid, the issue being fixed affects all `-p` usage, not just binary patches.

The feedback is constructive but not blocking - Junio explicitly calls the execution "great" and the technical approach sound. The remaining comments focus on test hygiene and clarity rather than fundamental issues. This suggests the patch is nearing readiness for merging, pending minor test refinements. The thread shows a typical maintainer-contributor interaction where the core fix is approved while fine-tuning the test presentation.