This is a feature patch in the final iteration (v7) of a series adding configurable cover letter formatting to `git format-patch`. The specific patch being discussed introduces the `format.commitListFormat` configuration option.

The discussion focuses on the behavior when the config value is empty versus unset. Phillip Wood points out that Git's convention is for empty values to behave the same as unset values, which allows command-line overrides to work predictably. He suggests using "default" as a special value to trigger the default format.

Author Mirko Faina responds that the current behavior defaults to "shortlog" when nothing is set, making "default" an unclear name. They acknowledge the potential issues with distinguishing between empty and unset values when interacting with command-line overrides.

This is a refinement discussion on the final details of a nearly complete feature. The core functionality is already implemented and tested, with this being one of the last design points to resolve before merging. The exchange shows careful attention to Git's configuration conventions and command-line interface consistency.