This is a follow-up discussion in the completed fast-import signature handling series, where Junio C Hamano questions the naming choice of the new `re-sign-if-invalid` mode. The series had already been merged with Christian Couder's cleanup patches and Justin Tobler's implementation of the signature re-signing functionality.

Junio suggests that "sign" might be a more accurate term than "re-sign" since the operation discards the invalid signature and creates a fresh one, rather than layering a new signature on top of the old one. This is a naming refinement discussion rather than a technical objection - the functionality itself is already settled and merged.

The thread context shows this was the final naming chosen after earlier iterations (previously called "resign") and review feedback. Junio's comment appears to be a post-merge observation about possible future naming improvements rather than a blocking concern. The series successfully introduced the capability to either strip invalid signatures (existing functionality) or replace them with fresh signatures (new functionality) during fast-import operations.