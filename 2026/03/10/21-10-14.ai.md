This is a documentation follow-up to a completed patch series about submodule `ignore=all` behavior. The original v4 series (now in `next`) introduced consistent handling of ignored submodules across Git commands, requiring `--force` to add them when `ignore=all` is set. The fifth patch in that series updated documentation across three man pages.

Claus Schneider is responding to Kristoffer Haugsbakk's feedback about two minor documentation issues in the merged patch: a missing space after "`log`" and "underlaying" vs "underlying". The thread context shows this was the final polish for a series that had already addressed all substantive feedback and was deemed ready to merge by Junio Hamano.

The current email is Claus checking whether to submit these fixes as a new patch (6/5) or as an amendment, since the original is already merged. This is purely about documentation wording cleanup for an otherwise complete feature. The changes are trivial but correct important details in the manual pages that explain the new `--force` requirement for submodules with `ignore=all`.