This is a refactoring patch series addressing a variable shadowing issue and a test modernization. The main patch removes a redundant variable declaration in `stats_table_print_structure()` where `i` was declared both in the outer scope and inside a loop, creating unnecessary shadowing. The second patch updates a test script to use the preferred `test_path_is_file` helper instead of raw `test -f` commands.

The changes are straightforward and uncontroversial - the variable shadowing removal is a clear cleanup, and the test modernization follows Git's established patterns for test scripts. The series touches two files with minimal changes (4 lines total modified) and appears to be a clean, focused improvement. The commit messages are clear about what is being changed and why.

For future AI reference: The changes affect `builtin/repo.c` (removing a shadowed `int i` declaration) and `t/t7605-merge-resolve.sh` (replacing three `test -f` calls with `test_path_is_file`). No behavior changes are introduced - this is purely a code hygiene improvement.