This is part 6 of an 8-patch series refactoring repository-specific global variables into `struct repo_config_values`, continuing the project's effort to reduce global state and improve repository isolation. The series is part of the ongoing `the_repository` removal effort.

The current patch moves the `core_sparse_checkout_cone` variable (which controls whether sparse checkout uses cone mode patterns) from a global integer to repository-specific storage. This prevents cross-repository state leakage when working with multiple repositories. The change follows the established pattern seen in previous patches - removing the global from environment.h/c, adding it to the repository struct, and updating all call sites (6 files modified, 29 insertions, 21 deletions).

The patch maintains existing behavior while making it repository-specific, with the variable initialized to 0 by default. The changes primarily affect the sparse-checkout subsystem, touching key files like builtin/sparse-checkout.c and dir.c. The work is mechanical but important for proper repository isolation, especially in library-style usage scenarios.

Like the rest of the series, this patch appears well-executed with thorough updates to all relevant call sites. The commit message could better explain why this particular setting benefits from repository-specific storage, but the technical implementation follows the established pattern from prior patches in the series. The patch has mentoring tags from Christian Couder and Usman Akinyemi, suggesting it may be part of an outreach/mentorship effort.