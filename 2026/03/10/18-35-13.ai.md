This is a test infrastructure patch that improves the readability of unit test output by displaying escape sequence names (like `\n`, `\t`) rather than their octal representations (like `\012`). The change affects the test-lib.c helper that prints character comparisons in test output.

The patch modifies `print_one_char()` to recognize common escape sequences and print their symbolic names. It adds cases for `\a` (bell), `\b` (backspace), `\t` (tab), `\n` (newline), `\v` (vertical tab), `\f` (form feed), and `\r` (carriage return). Unrecognized control characters still print in octal.

The author includes test updates in two places:
1. Adds new test cases in test-example-tap.c that exercise the escape sequence handling
2. Updates the expected output in t0080-unit-test-output.sh to match the new format

The change is straightforward and focused - it makes test failures easier to read by showing familiar escape sequences rather than octal codes. The patch appears well-contained with appropriate test coverage. This seems like a clear improvement to the test output format with no obvious downsides.