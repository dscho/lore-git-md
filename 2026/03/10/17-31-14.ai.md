This is a documentation patch in an ongoing thread about clarifying the behavior of `-U`/`--unified` diff context options. Jean-Noël Avila raises a follow-up question about whether the long option (`--unified`) should also be documented as accepting an optional number parameter, similar to how the short form (`-U`) is being updated.

The thread has already established that bare `-U` must remain supported for backward compatibility (it defaults to 3 lines of context like `-p`), and Tian Yuchen's patch updates the documentation to reflect this. Jean-Noël's question suggests extending this optional-parameter notation to the long form in the docs, though the actual implementation currently requires the number for `--unified`.

This is a documentation style question rather than a functional change - the thread has already settled on preserving the existing behavior where only the short form accepts a bare option. The patch itself is ready for merging with Junio's approval, and Jean-Noël's suggestion could be addressed in a follow-up if desired. The core issue of documenting the legacy `-U` behavior is now resolved.