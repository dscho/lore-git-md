This is a straightforward renaming patch in Jiamu Sun's subcommand autocorrection series. The change renames the `AUTOCORRECT_SHOW` enum value to `AUTOCORRECT_HINTONLY` to better reflect its purpose - showing similar commands without taking any automatic action. The patch updates all three locations where this enum is used: the config parser in autocorrect.c, the enum definition in autocorrect.h, and the help text logic in help.c.

The change is purely semantic - the behavior remains exactly the same, but the name now more clearly indicates that this mode only provides hints rather than performing any automatic correction. This is a minor but meaningful improvement to the API's clarity, following through on the series' earlier refactoring to make the autocorrection subsystem more modular and well-defined.

The patch is cleanly implemented with no functional changes, just the enum rename and corresponding updates to the few places that reference it. This appears to be the final polish on the autocorrection API before the series moves on to actually using it in commands like `git remote` and `git notes`.