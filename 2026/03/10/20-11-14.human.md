This is a cleanup patch in a three-part v3 series for `git fast-import` signature handling. The patch removes an obsolete function declaration from commit.h that was left behind when `sign_with_header()` was removed in a previous commit (6206089cbd).

The change is straightforward - it deletes the unused forward declaration of `sign_with_header()` that had remained after the function itself was removed. This is part of a larger effort to improve signature handling in `git fast-import`, but this particular patch is purely a code hygiene change with no functional impact.

The patch is well-motivated and uncontroversial - it's clearly a leftover declaration that should have been removed when the function was deleted. The commit message clearly explains the context and justification for the change. This is the first of three patches in the series, with the subsequent patches containing the more substantive changes to signature handling.