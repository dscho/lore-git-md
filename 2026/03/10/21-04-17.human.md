This is a documentation patch in the final stages of a series addressing submodule `ignore=all` behavior. The email shows Claus Schneider responding to Kristoffer Haugsbakk's feedback on minor documentation issues in the patch series that is already in `next`. 

The specific fixes are trivial formatting and spelling corrections in the man pages: adding a missing space after "`log`" and correcting "underlaying" to "underlying". These were the last remaining issues in a well-reviewed series that has already gone through multiple iterations (v4 is currently in `next`). 

The broader context is that this patch series makes `git add` respect `ignore=all` submodule configurations consistently with other commands like `status` and `diff`, while providing a `--force` option to override this behavior when needed. The implementation is technically complete and awaiting final merge, with these being the last minor documentation polish items.