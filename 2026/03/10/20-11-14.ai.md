This is a cleanup patch in the final iteration of a series adding signature re-signing support to `git fast-import`. The patch removes an obsolete function declaration from commit.h that was left behind when `sign_with_header()` was removed in a previous commit (6206089cbd). 

The change is straightforward - just deleting two lines that forward-declared the now-removed function. This is housekeeping that completes the earlier removal, ensuring the header file accurately reflects the available interfaces. The patch follows Git's standard practice of cleaning up artifacts when functionality is removed or relocated.

As part of the larger signature handling effort, this small cleanup helps maintain code hygiene while the series introduces new signing capabilities. The change is uncontroversial and was already reviewed in previous iterations of the patch series.