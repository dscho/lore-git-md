This is a reply in the thread about adding a new advice warning for uncommitted changes during branch switches (ADVICE_STASH_BEFORE_CHECKOUT). The thread also includes unrelated patches about diffstat width calculations that have already been merged for Git 2.54.0.

Phillip Wood responds to Arsh Srivastava's patch series, noting two issues. First, he suggests that recommending `git checkout -m` (which merges local changes into the new branch) might be more user-friendly than the current advice to stash changes before switching branches. Second, he points out that the patch series appears to contain unrelated commits that should be included, likely due to a rebase error, and provides instructions to clean it up.

The reply focuses on improving the user experience of the proposed advice feature rather than raising technical concerns. Phillip's suggestion about `-m` indicates the advice could be more users by covering additional workflow options beyond just stashing. The rebase note is a common reminder for contributors to keep patch series focused.

This exchange occurs after the diffstat width calculation patches were already merged, so the advice feature remains the active topic. The thread shows no objections to the technical implementation, only suggestions for enhancing the user-facing behavior.