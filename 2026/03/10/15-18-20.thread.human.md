This thread covers Patrick Steinhardt's work to complete the object database (ODB) abstraction by making object counting functionality pluggable. The six-part series introduces new generic counting APIs while maintaining existing behavior. The current version (v2) has addressed all review feedback from the initial submission.

The second patch, which just received additional confirmation, moves packed object counting logic from `repo_approximate_object_count()` to a new `packfile_store_count_objects()` function. This maintains MIDX support while making the code reusable for future backends. Toon Claes verified the MIDX handling, and Junio has approved the approach. The changes are purely organizational at this stage, with no behavioral differences.

The series has progressed smoothly through review, with all identified issues from v1 (including an uninitialized pointer and documentation fixes) addressed in v2. The patches touch core object storage code but focus on clean refactoring rather than new features. With Junio's approval and no outstanding technical concerns, this work appears ready for merging as part of the larger ODB abstraction effort.