This is a documentation patch that follows up on earlier discussion about clarifying the legacy behavior of `git show -U` without a line count argument. The thread had established that while bare `-U` must continue working (for backward compatibility with test 157 in t4013), the documentation should explicitly mark this as legacy behavior rather than an intentional feature.

Tian Yuchen's patch updates the documentation to reflect this, and Oswald Buddenhagen has provided wording improvements to make the backward compatibility rationale more direct and the phrasing more natural. The key changes are replacing "historical accident" with "fallback behavior for backward compatibility" and rewording "accepted as a silent synonym" to flow better.

The technical substance remains unchanged: `-U` without a number continues to default to 3 lines of context (matching `-p` behavior), while `--unified=<n>` remains the canonical long form. The documentation now clearly marks bare `-U` as legacy behavior with an explicit rationale.

This appears to be the final polish before merging, addressing all core requirements while leaving some minor documentation syntax questions (like adopting `--unified[=<n>]` convention) for potential follow-up. The patch is straightforward and uncontroversial, with the main discussion having settled on wording improvements rather than functional changes.