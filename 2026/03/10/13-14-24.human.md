This is a documentation patch that concludes a thread about clarifying the behavior of `git show -U` when no line count is specified. The patch updates the documentation to explicitly state that bare `-U` (without a number) is accepted as legacy behavior, defaulting to 3 lines of context like `-p` does, while framing this as a historical accident rather than an intentional feature.

The maintainer (Junio C Hamano) is responding to Oswald Buddenhagen's suggested wording improvements, agreeing that "silently accepted as a synonym" is more natural phrasing than the original "silent synonym". This appears to be the final polish before merging, as the core issue has been addressed and no new technical concerns remain.

The patch maintains backward compatibility with existing behavior (particularly test 157 in t4013) while making the documentation clearer about this being legacy behavior. The canonical form remains `--unified=<n>`, with bare `-U` documented as a historical quirk. This seems like a straightforward documentation improvement that's ready to be merged.