Documentation patch clarifying the legacy behavior of bare `-U` in diff options. This third iteration incorporates wording improvements from Oswald Buddenhagen to make the backward compatibility rationale more direct.

The patch updates two documentation files (`diff-context-options.adoc` and `diff-options.adoc`) to explicitly state that `-U` without a number is accepted as legacy behavior, defaulting to 3 lines of context (matching `-p` behavior). The new text frames this as "a historical accident" while preserving the existing functionality that some tests and scripts rely on.

Key technical details carried forward from the thread:
- Maintains backward compatibility with existing test 157 in t4013
- Documents the configurable default (`diff.context`) falling back to 3 lines
- Uses consistent phrasing between the two documentation files
- Preserves the implication that `--unified=<n>` is the canonical form

The patch appears ready for merging, having addressed Junio's original concern about undocumented behavior while incorporating Oswald's suggested wording improvements ("silently accepted as a synonym for `-p` due to a historical accident"). D. Ben Knoble's remaining suggestions about doc syntax alignment and default value clarification could be addressed in follow-up patches if desired.