This is a cleanup patch removing an unused function declaration from `commit.h`. The function `sign_with_header()` was removed in a previous commit (6206089cbd) but its forward declaration was accidentally left behind. Justin Tobler spots and removes it, with Junio C Hamano noting it as a straightforward cleanup.

The patch is part of the ongoing signature handling work in `git fast-import`, though this particular change is purely mechanical - removing dead code rather than modifying behavior. It's a small but correct improvement to code hygiene, and since it touches the same area as the active signature handling discussion, it makes sense to include it in that series. The change is uncontroversial and was merged.