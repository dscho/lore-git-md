This is a refactoring patch in the submodule subsystem that has already gone through review and is now being acknowledged by the maintainer. The change modernizes memory allocation in `submodule_summary_callback()` by replacing raw `malloc()` with Git's `xmalloc()` wrapper and improves type safety by using `sizeof(*temp)` rather than explicitly naming the struct type.

The patch was originally sent by Siddharth Shrimali as part of an effort to standardize memory allocation patterns in the codebase. Version 2 incorporated Junio C Hamano's earlier request to better document the rationale in the commit message. The change eliminates a potential NULL pointer dereference by using Git's standard error-handling allocation wrapper, while also making the code more maintainable by removing an unnecessary type cast and using the more robust `sizeof(*ptr)` pattern.

Patrick Steinhardt has already reviewed and acked the technical correctness of both the error handling improvement and style updates. Junio's latest response indicates the patch now meets Git's standards and is ready for merging. This is a small but clean improvement that follows Git's established patterns for memory management - the single-line change affects only `builtin/submodule--helper.c` and has no behavioral impact beyond making failure cases safer.