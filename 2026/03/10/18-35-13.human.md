This is a test infrastructure patch from Pablo Sabater, part of their GSoC work. It improves the readability of test failure output by displaying escape sequence names (like `\n`, `\t`) rather than their octal representations (like `\012`).

The change modifies `print_one_char()` in the unit test library to recognize common escape sequences and print them in their named form. The patch includes updates to test files to verify the new output format works correctly, adding new test cases for additional escape sequences like `\a` (bell) and `\v` (vertical tab).

The change is straightforward and well-contained, touching only test-related files. The commit message clearly explains the improvement in readability, though it could benefit from mentioning that this affects unit test output specifically (not the main test suite). The patch appears to be a quality-of-life improvement for developers debugging test failures.