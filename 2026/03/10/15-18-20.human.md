This is a patch series introducing generic object counting for pluggable object databases, part of Patrick Steinhardt's ongoing ODB abstraction effort. The series builds on recent work to make the object database backend more modular.

The changes extract existing object-counting logic from various places (packfile handling, object-file utilities) and generalize it to work with any ODB backend. The implementation introduces new interfaces in odb.h and odb/source.h while cleaning up some header inclusions. The series appears to be a well-structured step toward the object database abstraction goal, with each commit focused and the overall diff showing careful attention to maintaining compatibility.

The changes touch core object-handling code but don't appear to affect user-visible behavior - this is plumbing work to support future pluggable backends. Given Patrick's track record with this kind of architectural change and the focused nature of the patches, this looks like solid infrastructure work that's likely to progress smoothly through review.