This is a small refactoring patch targeting the submodule helper code. Siddharth Shrimali replaces a raw `malloc()` call with `xmalloc()` in the `submodule_summary_callback()` function to ensure consistent error handling. The change also includes a minor style improvement by using `sizeof(*temp)` rather than explicitly naming the struct type.

The patch is straightforward and uncontroversial - it follows Git's convention of using `xmalloc()` for memory allocation and makes the code slightly more maintainable. The change is limited to a single line in the submodule helper, so it's unlikely to have any broader impact. This appears to be a clean, focused improvement that aligns with the project's coding standards.