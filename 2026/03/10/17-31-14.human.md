This is a documentation patch that follows up on a thread about the `-U` diff context option's behavior. Jean-Noël Avila asks whether the long option `--unified` should also be documented as accepting an optional number parameter (using `[=<n>]` syntax), similar to how Tian Yuchen updated the short `-U` option's documentation.

The thread had previously concluded that while bare `-U` must remain supported for backward compatibility (it defaults to 3 lines of context like `-p`), this should be clearly documented as legacy behavior. Tian's patch updated the docs to show `-U[<n>]` syntax for the short option, and Jean-Noël is now questioning whether the long option's documentation should follow suit.

This appears to be addressing one of the open questions from D. Ben Knoble in the thread summary about doc syntax alignment. The core issue is already resolved with Junio's approval of the main patch, so this is a follow-up refinement suggestion rather than a new controversy. The email doesn't propose any changes itself - it just raises the question about whether the long option syntax should be updated to match.