This is a refactoring patch that concludes a straightforward improvement to the submodule helper code. The change replaces a raw `malloc()` with Git's safer `xmalloc()` wrapper in `submodule_summary_callback()`, while also modernizing the allocation style to use `sizeof(*temp)` rather than explicitly naming the struct type.

The patch has gone through review with Patrick Steinhardt and Junio C Hamano, who both approved the technical approach. Junio's final response confirms the changes look good, marking this as ready for merging. The improvement follows Git's standard practices for memory allocation and error handling, addressing a potential NULL pointer dereference while making the code more maintainable.

This single-line change in `builtin/submodule--helper.c` is a small but meaningful cleanup that aligns with the project's ongoing code hygiene efforts. With all maintainer feedback addressed in v2, this appears to be a non-controversial improvement that could be picked up in the next merge window.