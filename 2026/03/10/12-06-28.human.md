This is patch 7/8 in a series moving repository-specific global variables into `struct repo_config_values`, part of the ongoing `the_repository` removal effort. The patch relocates the `sparse_expect_files_outside_of_patterns` configuration variable from global scope to per-repository storage.

The variable controls whether Git should warn about files existing outside sparse checkout patterns. Like previous patches in the series, this change preserves behavior while eliminating cross-repository state leakage - an important step toward making Git work better as a library with multiple active repositories. The patch follows the established pattern: removing the global declaration, adding it to the repository struct, updating initialization, and modifying access sites.

Changes touch 3 files (environment.c/h and sparse-index.c) with straightforward mechanical updates. The series as a whole is progressing methodically through various configuration settings, with this being the seventh variable moved. While the commit message could better explain why this particular setting benefits from repository-specific storage, the change itself appears well-executed and follows the project's established patterns for this type of refactoring.