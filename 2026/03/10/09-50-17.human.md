Documentation patch clarifying legacy behavior of `-U` option. This is the third iteration of a change that documents Git's historical acceptance of bare `-U` (without a number) as equivalent to `-p`, defaulting to 3 lines of context. The patch updates two man pages to explicitly state this is backward-compatibility behavior rather than an intentional feature.

The changes incorporate wording improvements suggested by Oswald Buddenhagen, making the backward compatibility rationale more direct while preserving the technical facts: bare `-U` works like `-p` due to historical reasons, defaulting to 3 lines of context (or the `diff.context` config if set). The patch maintains consistency between the two documentation files it touches.

This appears ready for merging, having addressed all substantive feedback from the thread. The remaining minor documentation syntax questions raised by D. Ben Knoble could be handled in follow-up patches if desired, but don't block this change. The patch cleanly solves the original issue by properly documenting the existing behavior without changing any functionality.