This is a small refactoring patch targeting the submodule helper code. The change replaces a direct `malloc()` call with `xmalloc()` in the `submodule_summary_callback()` function, which handles diff output for submodules. The patch also modernizes the allocation by using `sizeof(*temp)` rather than explicitly naming the struct type, making the code more maintainable.

The change is straightforward and appears correct - `xmalloc()` is Git's standard wrapper that dies on allocation failure rather than returning NULL, which is the preferred pattern throughout the codebase. The patch touches only one line in `builtin/submodule--helper.c` and the commit message clearly explains both the safety improvement (avoiding potential NULL dereference) and the style update.

While this is a minor change, it follows Git's conventions well. The patch is properly formatted with a sign-off and clean diff. The modification is low-risk since it doesn't change behavior - it just makes failure handling consistent with the rest of the codebase. This would likely be an uncontroversial candidate for merging.