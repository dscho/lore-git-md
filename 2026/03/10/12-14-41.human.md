Performance optimization in `git-upload-pack`'s packfile writing. This is part of Patrick Steinhardt's ongoing series to reduce write syscalls in high-concurrency scenarios, now down to ~44k writes (vs ~400k originally) when cloning the Linux repo.

The discussion focuses on buffer management strategies, with Jeff King (Peff) questioning the 2/3 buffer fill threshold as arbitrary and suggesting optimizations around keepalive timing. Patrick responds that while the effectiveness depends on downstream commands, he's not convinced the buffering could perform worse than the unbuffered case. He also implements Peff's suggestion to piggyback buffered data on keepalive packets, showing the proposed code change.

The exchange reveals careful attention to edge cases - Patrick notes the need to hold back one byte for error handling, while Peff's earlier review had raised concerns about subtle interactions between buffering layers. Both maintainers are working to preserve the substantial syscall reduction while ensuring robustness across different usage patterns.

This patch remains part of the `writev()`-based approach that Patrick is proceeding with, having considered brian m. carlson's empirical data from Rust implementations showing measurable gains. The technical discussion has now shifted from architectural alternatives to refining specific implementation details of the chosen approach.