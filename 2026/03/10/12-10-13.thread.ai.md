Submodule helper refactoring: replace malloc with xmalloc in submodule_summary_callback(). The change modernizes memory allocation in builtin/submodule--helper.c by using Git's standard xmalloc() wrapper (which dies on failure) instead of raw malloc(), and updates the allocation style to use sizeof(*temp) rather than explicitly naming the struct type. The modification improves consistency with Git's error handling conventions and makes the code more maintainable, while maintaining identical runtime behavior. The patch is properly formatted with a sign-off and affects only one line of code.