This is a documentation patch clarifying the behavior of `-U` in Git's diff output. The patch updates two man pages (`diff-context-options.adoc` and `diff-options.adoc`) to explicitly document that `-U` without a number is accepted as a legacy behavior, defaulting to 3 lines of context (matching `-p`).

The thread established that this lenient parsing must be preserved for backward compatibility, as test 157 in t4013 and likely user scripts depend on it. The documentation now frames this as a "historical accident" rather than an intentional feature. The patch addresses the core issue from the thread while leaving open D. Ben Knoble's suggestions about doc syntax alignment (`--unified[=<n>]`) and default value explanation for potential follow-up.

Key technical details carried forward:
- Default 3-line context matches `-p` behavior
- `--unified=<n>` remains the canonical long form
- Documentation now explicitly marks bare `-U` as legacy
- Default applies both when `<n>` is omitted and when config is unset

The patch appears well-considered, addressing Junio's core requirements while maintaining compatibility. The changes are limited to documentation with no behavioral modifications.