This thread discusses a small but worthwhile refactoring of submodule helper code. The patch replaces a direct `malloc()` call with Git's standard `xmalloc()` wrapper in the `submodule_summary_callback()` function, which handles diff output for submodules. The change improves safety by ensuring allocation failures result in immediate termination rather than potential NULL dereferences, following Git's established patterns.

The patch also modernizes the allocation style by using `sizeof(*temp)` rather than explicitly naming the struct type, making the code more maintainable. The change is minimal - touching just one line in `builtin/submodule--helper.c` - and comes with a clear commit message explaining both the safety improvement and style update. Given its straightforward nature and alignment with project conventions, this appears ready for merging without further discussion.