This is a build system improvement series from Patrick Steinhardt that reorganizes developer tooling and introduces precompiled headers for Meson builds. The 8-patch series has two main parts:

First, it creates a new "tools/" directory to consolidate build infrastructure and developer tools, moving several items from "contrib/" including Coccinelle rules, coverage scripts, and build helper scripts. This follows through on a cleanup promise Patrick made in an earlier series.

Second, it adds precompiled header support to the Meson build system, which provides a ~30% compilation speed boost. The implementation involves restructuring "git-compat-util.h" to isolate warning infrastructure and compiling compatibility sources separately to enable the optimization.

The changes are mechanical but wide-ranging, touching build configurations, Makefiles, and Meson files across the codebase. The series appears well-structured with clean commit separation between the directory reorganization and the precompiled header work. As a build system optimization from an experienced contributor, this is likely to be non-controversial and beneficial for developer workflow.