Test infrastructure patch addressing an output formatting issue in the test helper's escape sequence display. This is a follow-up to the initial patch that improved test output readability by showing symbolic names for escape sequences (like `\n` instead of octal `\012`).

The author (Pablo) noticed an unintended side effect in the test output where backslash handling in string comparisons changed slightly - specifically in how single quotes and backslashes are displayed when tests fail. The original output showed `'\\\\' == '\\''` while the new output shows `'\\\\' == '\''`. While functionally equivalent, this cosmetic difference wasn't the intended scope of the original change.

This appears to be a minor oversight in test expectation updates rather than a bug in the core functionality. The author offers to revert this incidental change in a v2 if reviewers consider it important to maintain exact output matching. The patch remains focused on test infrastructure improvements and doesn't introduce any new technical complexity - it's purely about output formatting consistency in test failure messages.

Key files involved:
- t/helper/test-example-tap.c (test cases)
- t/t0080-unit-test-output.sh (test expectations)

The thread is still in early stages with only the author participating so far. Given the narrow scope and clear explanation of the issue, this follow-up seems unlikely to generate significant discussion unless reviewers have strong opinions about test output consistency requirements. The patch continues the test infrastructure improvement theme from the initial submission but addresses a more subtle aspect of the output formatting.