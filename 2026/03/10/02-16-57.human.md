Test modernization patch updating path checks in merge test script.

This is the first of two patches in a small refactoring series focused on code hygiene. It updates `t/t7605-merge-resolve.sh` to use the preferred `test_path_is_file` helper instead of direct `test -f` calls. The change follows Git's test suite modernization guidelines, where the helper provides better error messages than raw shell tests. 

The patch is straightforward - it replaces three `test -f` checks with the equivalent helper calls in the `merge_c1_to_c2_cmds` test block. The commit message notes these were found via a search for legacy test patterns. This appears to be a clean, uncontroversial improvement that aligns with ongoing test suite maintenance efforts. The change is purely mechanical with no behavior impact.