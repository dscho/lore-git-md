Refactoring patch addressing const-correctness warnings in the ident conversion code.

Collin Funk's patch fixes compiler warnings from glibc-2.43 about discarded const qualifiers in `ident_to_worktree()`. The function processes a const input string but uses non-const pointers (`dollar` and `spc`) to iterate through it. Since these pointers are only used for reading, the patch makes them const as well.

The change is straightforward - just modifying the type declarations of two local variables. It doesn't affect behavior since the variables were already being used in a const-correct way in practice. The patch follows Git's typical style for small, focused refactorings that improve code hygiene without changing functionality.

This appears to be a clean, uncontroversial change that aligns with Git's coding guidelines. The commit message clearly explains both the compiler warnings being addressed and why the change is safe. The diff is minimal and only touches the relevant variable declarations.