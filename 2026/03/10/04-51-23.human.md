This is a documentation patch addressing an inconsistency in `git diff`/`git show` argument handling. The thread has established that `-U` without a number (which defaults to 3 lines of context) is accepted due to historical behavior, while `-Unan` properly errors out. 

Tian Yuchen initially proposed documenting this as an intentional feature, but after Junio Hamano's feedback, agrees it should be framed as legacy behavior rather than encouraged usage. The solution will update the documentation to show the proper `-U<n>` syntax while noting the bare `-U` case as a historical accident that defaults to 3 lines (matching `-p` behavior).

The patch represents the final step in resolving this issue - updating the docs to accurately reflect the implementation while discouraging the inconsistent usage pattern. It follows earlier technical analysis that confirmed the behavior must be preserved for backward compatibility with existing tests. The change is uncontroversial and aligns with Git's principle of documenting reality rather than ideal behavior when the two diverge for historical reasons.