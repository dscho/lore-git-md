This is a documentation patch addressing the handling of the `-U` option in Git's diff output. The thread has established that while `-U` without a line count (which defaults to 3 lines of context) must be preserved for backward compatibility, the documentation should clearly mark this as legacy behavior rather than an intentional feature. 

Tian Yuchen is responding to D. Ben Knoble's review, agreeing that the current documentation could be clearer about when the default 3-line context applies (both when `<n>` is omitted and when the config is unset). The author indicates they'll incorporate Junio's suggested wording while addressing Knoble's feedback about making the default behavior more explicit.

This appears to be a straightforward documentation improvement with consensus on the technical approach. The patch will help users understand the somewhat inconsistent behavior while maintaining compatibility with existing scripts that rely on the current implementation.