This is part 9/10 of Patrick Steinhardt's performance optimization series targeting lock contention in `git-upload-pack`. The patch removes the specialized `hashfd_throughput()` function from `csum-file.c` in favor of the more general `hashfd_ext()` interface introduced earlier in the series.

The change moves the throughput-specific configuration (smaller 8KB buffer for more frequent progress updates) directly into the `pack-objects` caller rather than maintaining a separate helper. This is part of the broader effort to streamline the hashfile interface while preserving the progress reporting behavior needed for pack operations.

The patch demonstrates good attention to detail - it maintains identical behavior while removing a layer of indirection. The commit message clearly explains the change and its context within the larger series. This is a straightforward cleanup that follows naturally from the earlier introduction of `hashfd_ext()` as the unified interface for hashfile creation.

Technical details:
- Removes `hashfd_throughput()` from csum-file.[ch]
- Updates single caller in `builtin/pack-objects.c` to use `hashfd_ext()` directly
- Preserves existing 8KB buffer size and progress meter behavior
- Part of broader effort to generalize hashfile interface while optimizing write performance