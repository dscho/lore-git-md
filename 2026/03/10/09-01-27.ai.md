This is a refactoring patch that moves and renames signing functionality from commit-specific code to a more general location in preparation for the `git fast-import` re-signing feature. It's part of a completed v2 series that adds the ability to re-sign invalid commit signatures during import.

Christian Couder provides review feedback on Justin Tobler's patch that extracts signing logic from `commit.c` into `gpg-interface.c`. The patch creates a new sign_buffer_with_key()` function that handles both key resolution and buffer signing, making this functionality reusable beyond just commit operations. Christian suggests clarifying the commit message to explicitly note that this is a relocation and renaming of existing code rather than entirely new logic.

The change is straightforward - it takes the commit-specific signing helper and generalizes it for any buffer while maintaining the same behavior around default key resolution. This refactoring enables the subsequent patch in the series to use the same signing logic in `git fast-import` when re-signing invalid commits. The thread shows consensus on the approach, with only minor wording nits being raised. The patch appears well-contained and follows Git's typical pattern of moving shared functionality to common locations when multiple components need the same capability.