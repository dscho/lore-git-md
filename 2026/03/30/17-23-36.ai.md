This is the first patch in a completed refactoring series for the xdiff subsystem, specifically cleaning up the `xdl_cleanup_records()` function. The patch removes a redundant `recs` pointer variable in favor of direct array indexing, making the code slightly more readable by reducing indirection.

The change is purely a code simplification with no behavior impact - it modifies four loops in `xdiff/xprepare.c` to use direct array access rather than maintaining a separate pointer. The diff shows 8 lines added and 9 removed, mostly eliminating the pointer declaration and updates. This serves as a straightforward lead-in to the more substantial type safety improvements that follow in subsequent patches.

The series as a whole has already addressed all technical concerns, with test failures fixed and build warnings resolved. This particular patch was the simplest in the series, focusing on basic code clarity before the later patches tackled type safety issues with `uint64_t` and `ptrdiff_t` conversions. The v4 update mentioned in the email refers to a change in the cover letter (using `PTRDIFF_MAX` instead of `SIZE_MAX`) that doesn't affect this specific patch.