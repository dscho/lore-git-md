This is the final patch in Ezekiel Newren's refactoring series for the xdiff subsystem, specifically targeting the `xdl_cleanup_records()` function. The series has been through several iterations addressing type safety and code clarity issues.

The patch simplifies how the `INVESTIGATE` state is handled in the record cleanup process, converting nested ternary operations into explicit if/else ladders with `BUG()` checks for invalid states. The changes make the control flow more transparent while maintaining identical behavior - `INVESTIGATE` records are still resolved to either `KEEP` or `DISCARD` based on `xdl_clean_mmatch()` results.

Key improvements in this specific patch:
- Replaces complex conditional logic with straightforward state transitions
- Adds explicit error handling via `BUG()` for invalid states
- Maintains symmetry between the two file handling paths (xdf1 and xdf2)
- Keeps all the previous type safety improvements from earlier patches in the series

The changes are purely internal - no user-visible behavior is altered. This appears to be the final polish on a well-executed refactoring effort that has already addressed all prior review feedback. The series looks ready for merging.