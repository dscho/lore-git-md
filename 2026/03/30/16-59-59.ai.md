This is a refactoring patch from Ezekiel Newren's series improving type safety in xdiff's record cleanup logic. Patch 2/6 focuses on the `xdl_bogosqrt()` function, changing its parameter and return types from `long` to `uint64_t` for better clarity and to handle larger values safely.

The change makes it explicit that the function deals with unsigned quantities (since square roots of negative numbers are undefined) and ensures the type is wide enough for all use cases. The patch updates all call sites to cast their arguments to `uint64_t` and the results back to `long` where needed, maintaining compatibility with the existing interface.

The modifications touch four files in the xdiff subsystem, with straightforward mechanical changes to function signatures and call sites. This is a clean, focused improvement that follows naturally from the series' goal of making the code's type usage more precise and unambiguous. The patch appears well-contained and unlikely to introduce new issues, as it doesn't change the actual algorithm behavior.