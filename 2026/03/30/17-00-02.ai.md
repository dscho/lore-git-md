Refactoring patch in the xdiff subsystem, part of a series to improve code clarity in `xdl_cleanup_records()`. This is patch 5/6 in the fourth iteration of Ezekiel Newren's series.

The patch converts nested ternary operations in `xdl_cleanup_records()` to explicit if/else ladders when setting `action1` and `action2` arrays. The change makes the decision logic for whether to DISCARD, KEEP, or INVESTIGATE each record more readable while maintaining identical behavior. The modification touches two similar code blocks that handle records from each input file (xdf1 and xdf2).

This continues the series' focus on improving maintainability of the xdiff code without changing functionality. The change is straightforward and unlikely to be controversial, following the pattern of previous patches in the series that have already addressed test failures and build warnings. With all technical issues resolved in earlier iterations, this appears to be a final polish before potential merging.