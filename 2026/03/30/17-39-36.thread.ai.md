Here is the updated dense thread summary combining both patches:

This thread introduces a two-patch series to make submodule fetch errors configurable when unreachable commits are encountered during recursive fetches. The series addresses workflow pain points when working with in-progress upstream branches where missing commits are expected but not required for current work.

The first patch fixes a timing issue in submodule fetch error reporting where failures were being logged too early, before OID-based retry attempts could succeed. It modifies `submodule.c` to introduce a new `record_fetch_error()` helper, restructures `fetch_finish()` to only record failures after all retry phases complete (ref-based fetch followed by OID-based fetch when applicable), and resolves a longstanding NEEDSWORK comment from 2019 about premature error reporting.

The second patch adds the main feature: new configuration options `fetch.submoduleErrors` (with `--submodule-errors` CLI counterpart) allowing users to choose between failing (current default) or just warning when submodule fetches hit unreachable commits. Implementation touches:
- Config parsing in `builtin/fetch.c` for new option
- `submodule.c` fetch logic extended to respect error mode
- New `SUBMODULE_ERRORS_{FAIL,WARN}` constants in `submodule.h`
- Comprehensive tests in `t5526-fetch-submodules.sh` covering:
  - Default failure behavior
  - Config-based warning mode
  - CLI override of config
  - Successful fetch verification

The series is well-structured with the first patch providing a robust foundation for the configurable error handling. Both patches include clear commit messages explaining technical details and user benefits. Test coverage is thorough for both error reporting fixes and new configuration behavior. The changes maintain backward compatibility while solving real workflow issues. No reviews or objections have been raised yet; the complete series is now posted for consideration.