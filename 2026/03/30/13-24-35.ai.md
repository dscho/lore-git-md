Documentation patch finalizing the `git stash` command syntax clarification. Quentin Bernet's v2 submission addresses feedback from the thread by:

1. Correcting both the synopsis and explanation sections to accurately reflect that:
   - The `push` subcommand is optional for most options (`-m`, `-u`, etc.)
   - Pathspecs still require either explicit `push` or `--` disambiguation
   - Removes the incorrect claim that `-p` was a special exception

2. Updating the built-in help text to match the man page changes, ensuring consistency between documentation and implementation.

The patch has maintainer approval from Junio Hamano, who suggested only a minor commit message improvement (noting the `--` disambiguation point more explicitly). This resolves the documentation discrepancy while leaving the underlying parser behavior unchanged - the thread identified potential parser improvements as future work but agreed documenting current behavior was the priority.

Changes from v1 focus on clearer commit message and tighter alignment between the man page and command-line help text. The technical content remains straightforward - this is purely a documentation accuracy fix for the `git stash` interface.