This is the first patch in Patrick Steinhardt's 18-part series refactoring `setup.c` as part of the ongoing `the_repository` removal effort. The patch focuses on converting static functions in `setup.c` to take explicit `struct repository *` parameters instead of relying on the global `the_repository` variable.

The changes are largely mechanical - each static function that previously used `the_repository` now takes a repository parameter, and callers pass `the_repository` as that parameter. The patch touches 188 lines across `setup.c`, with 100 insertions and 88 deletions. The actual behavior remains unchanged; this is purely a refactoring step to prepare for later patches that will propagate the repository parameters further up the call chain.

The patch demonstrates Patrick's characteristic thoroughness in systematically tackling a core subsystem. While the changeset is large, it's well-scoped to just the static functions in `setup.c`. Subsequent patches in the series will build on this foundation to continue removing `the_repository` usage from this critical initialization code.

This appears to be a solid first step in the series - the changes are straightforward and the patch follows Git's established patterns for this type of refactoring. Reviewers will likely want to verify that no subtle behavior changes were introduced, but the mechanical nature of the changes suggests low risk.