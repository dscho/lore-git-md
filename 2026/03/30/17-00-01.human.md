This is the fourth patch in a refactoring series improving the xdiff subsystem's record cleanup logic. The series has already addressed several technical concerns in prior iterations, and this patch focuses specifically on making the limit calculations clearer in `xdl_cleanup_records()`.

The changes introduce explicit per-file limit variables (`mlim1` and `mlim2`) and initialize them upfront rather than recalculating inline. It also removes a redundant condition check (`!need_min`) that is no longer needed with the new structure. The patch maintains all the type safety improvements from earlier versions (using `ptrdiff_t` and `PTRDIFF_MAX`) while making the control flow more straightforward.

This appears to be a straightforward improvement to code clarity in the xdiff internals, building on the solid foundation established by the previous patches in the series. The changes are purely internal with no behavior modification, and all test failures from earlier versions have been resolved. Given the prior review feedback has been addressed, this seems ready for inclusion.