This is a final polish suggestion for the mailmap toggle feature in `git cat-file --batch-command`.

Patrick Steinhardt points out a small code quality opportunity in Siddharth Asthana's implementation - the mailmap subcommand's argument parsing could use the existing `git_parse_maybe_bool()` helper instead of direct string comparisons. This is a minor but sensible suggestion that aligns with Git's codebase conventions, where boolean parsing is typically handled through this centralized function.

The comment comes late in a well-reviewed thread that has already established consensus on the core functionality. Patrick's note doesn't challenge any design decisions but offers a straightforward improvement to make the code more idiomatic. The change would maintain the same user-facing behavior while reducing maintenance surface area by reusing established parsing logic.

This kind of last-mile refinement is common in mature patch series, where the community helps polish details before final merge. The suggestion is low-risk and could be addressed with a trivial follow-up patch if the author chooses to adopt it.