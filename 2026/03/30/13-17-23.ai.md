This is the first patch in Patrick Steinhardt's 18-part series refactoring `setup.c` as part of the ongoing `the_repository` removal effort. The patch focuses on converting static functions in `setup.c` to take explicit `struct repository *` parameters instead of implicitly using the global `the_repository` variable.

The changes are largely mechanical - each static function that previously accessed `the_repository` now takes it as a parameter, and call sites are updated to pass `the_repository` explicitly. The patch touches 188 lines across `setup.c`, with 100 insertions and 88 deletions. Key functions modified include repository initialization helpers like `setup_git_dir()`, `setup_explicit_git_dir()`, and template copying utilities.

This initial patch lays groundwork for subsequent changes that will propagate the repository parameter up through the call chain. The approach is systematic and maintains existing behavior while removing implicit global state. As the first step in a larger series, it demonstrates Patrick's characteristic thoroughness in handling core subsystems, though reviewers will want to verify the mechanical changes don't introduce any subtle behavioral differences.