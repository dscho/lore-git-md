Patrick Steinhardt's ongoing refactoring of `setup.c` continues with patch 10/18 in his series to remove the `the_repository` global variable. This latest patch converts `set_git_work_tree()` to take an explicit `struct repository *` parameter, moving the `git_work_tree_initialized` global flag into `struct repository` as `worktree_initialized`. The changes are mechanical but important - updating callers in `clone.c`, `init-db.c`, and `setup.c` while maintaining the existing safety check against reinitialization with different worktree paths.

The series is systematically working through `setup.c` as part of the broader `the_repository` removal effort that René Scharfe and others have championed. So far the patches have followed a consistent pattern: small, focused changes (this one touches 5 files with 24 insertions and 18 deletions) that push repository awareness up the call stack while preserving behavior. The work remains preparatory, with more substantial logic changes expected in later patches. 

Patrick's careful, incremental approach is evident throughout - each patch builds toward the larger goal while maintaining the existing function signatures and edge case handling. The series continues to progress steadily, with patches 4-10 now posted and reviewed. While the changes are mechanical at this stage, they lay important groundwork for the more complex refactorings to come in this 18-part series modernizing `setup.c`.