Patrick Steinhardt's ongoing refactoring of `setup.c` continues with part 15/18 of his series to remove global state as part of the broader `the_repository` removal effort. The latest patch converts `check_repository_format()` to take an explicit `struct repository *` parameter rather than implicitly using the global `the_repository`. This is a mechanical but important change that follows the established pattern of the series - the function is now static within `setup.c` since it's only used there, and all callers pass `the_repository` explicitly.

The series, which now spans 15 patches touching over 87 files, systematically converts functions that previously relied on global state to instead take explicit repository parameters. Recent patches have focused on core initialization functions like `setup_git_directory()`, `upgrade_repository_format()`, and now `check_repository_format()`. Each change is small and focused, typically modifying 5-35 lines while maintaining existing behavior. The work builds on Junio's March 27 integration branch and represents a significant step toward making repository initialization properly repository-aware.

With three patches remaining in the series, attention will soon turn to any more substantial logic changes that may be coming. So far the changes have been mechanical conversions, but the final patches may address edge cases or behavior changes that could prompt more discussion. The series has not yet received review feedback, likely because these early patches follow an established pattern from the long-running `the_repository` removal effort championed by René Scharfe and others.