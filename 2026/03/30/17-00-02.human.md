This is the fifth patch in Ezekiel Newren's refactoring series for the xdiff subsystem, specifically targeting code clarity in `xdl_cleanup_records()`. The patch converts nested ternary operations into explicit if/else ladders when setting the `action1` and `action2` arrays, making the decision logic more readable while maintaining identical behavior.

The changes are straightforward and mechanical - replacing two instances of `condition ? A : condition ? B : C` with explicit if/else blocks that clearly show the three possible outcomes (DISCARD, KEEP, or INVESTIGATE) based on the value of `nm` compared to the limits `mlim1` and `mlim2`. The patch touches only xdiff/xprepare.c with minimal line changes (12 added, 2 removed).

This continues the series that has already addressed type safety issues and build warnings in previous iterations, with all test failures now resolved. The changes are purely about code readability with no functional impact, and the series appears ready for merging after thorough review.