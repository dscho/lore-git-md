This is a test cleanup patch in a series addressing incorrect behavior in `receive.denyCurrentBranch=updateInstead` with bare repositories containing worktrees. The patch fixes an oversight in the test infrastructure where temporary directories 'cloned' and 'new-wt' weren't being cleaned up after the test, which could cause subsequent tests to fail.

The change is straightforward - it adds a `test_when_finished` cleanup block to remove these directories when the test completes. This is part of a 3-patch series that first demonstrated the bug, then (in this patch) cleans up the test infrastructure, and finally provides the actual fix in the third patch.

The patch appears well-scoped and uncontroversial, following Git's test hygiene practices. It's a necessary precursor to the actual fix in the next patch, ensuring the test environment is properly reset between runs. The change is small (just one line added) and clearly motivated by the test failure scenario described.