This is a documentation patch finalizing the clarification of `git stash` command syntax. Junio Hamano approves Quentin Bernet's v2 submission, which accurately documents that:

1. The `push` subcommand is optional for most options (`-m`, `-u`, etc.), with the syntax now clearly showing `[push]` as optional in both the man page and built-in help text.
2. Pathspecs still require either explicit `push` or `--` disambiguation, with the documentation now removing an incorrect special-case claim about `-p`.

Junio notes the patch correctly aligns the implementation's actual behavior with its documentation, though he mildly prefers the original "for disambiguation" phrasing over the slightly reworded version. The changes are straightforward and maintain consistency between the man page and command-line help text. With Junio's approval, this documentation fix is now queued for inclusion.