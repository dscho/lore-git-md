This is the second patch in a refactoring series focused on improving type safety in xdiff's record cleanup logic. The patch changes `xdl_bogosqrt()` to use `uint64_t` instead of `long` for its parameters and return value, since square roots of negative numbers are undefined and some callers may need the larger range. The change is straightforward but requires adding explicit casts at the call sites to maintain compatibility with the existing interface.

The patch is part of a well-structured series that has already addressed all previous review feedback, including test failures and build warnings. This particular change appears uncontroversial - it's a clear improvement in type safety with no behavior change. The series as a whole has Junio Hamano's approval on the type improvements, suggesting this is likely to be merged.