This is the final patch in Ezekiel Newren's xdiff refactoring series, focusing on improving code clarity in the record cleanup logic. The patch simplifies how INVESTIGATE records are handled in `xdl_cleanup_records()` by converting nested ternary operations into explicit if/else ladders with BUG() checks for invalid states.

The changes make the control flow more straightforward by:
1. Separating the INVESTIGATE case into its own block that resolves to either KEEP or DISCARD
2. Making the KEEP and DISCARD cases distinct with clear conditions
3. Adding BUG() checks to catch any invalid states that might slip through
4. Maintaining identical behavior for both input files (xdf1 and xdf2)

The patch follows the series' pattern of internal refactoring without behavior changes, building on previous improvements to type safety and limit handling. With all test failures and build warnings resolved in earlier iterations, this final patch appears ready for merging as part of the complete series.