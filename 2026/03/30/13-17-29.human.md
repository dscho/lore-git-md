This is part of Patrick Steinhardt's ongoing refactoring series to remove `the_repository` global from `setup.c`. Patch 7/18 converts `verify_non_filename()` to take an explicit `struct repository *` parameter instead of implicitly using `the_repository`.

The change is mechanical but touches several callers in `checkout.c`, `grep.c`, `reset.c`, and `revision.c` that now pass `the_repository` explicitly. The function's logic remains the same - it still checks whether we're inside a worktree or gitdir before validating that an argument isn't mistakenly interpreted as a filename when it should be a refname.

This continues the series' pattern of small, focused patches that incrementally remove global state while maintaining backward compatibility. The changes are straightforward and unlikely to be controversial, though they do require updating multiple call sites. The patch is well-contained (8 lines changed across 6 files) and follows the established approach of the larger series.