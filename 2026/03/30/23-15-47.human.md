This is a review response in Taylor Blau's incremental MIDX repacking series. Jeff King (Peff) provides feedback on patch 5/16, which introduces a `--checksum-only` flag for MIDX writes.

Peff agrees with the technical approach but questions the flag name `--checksum-only`, suggesting it doesn't clearly convey the behavior (writing a MIDX layer without updating the chain file). He proposes alternatives like `--no-write-chain-file` while acknowledging this may be borderline bike-shedding.

The review confirms the implementation correctly:
1. Skips chain file locking when the flag is set
2. Outputs the checksum to stdout
3. Avoids updating the chain file
4. Maintains the requirement that `--incremental` must be used with `--checksum-only`

Peff notes some minor implementation details that could be tweaked (like using lock file state for conditionals) but doesn't insist on changes. The review is generally positive, focusing on naming clarity rather than technical concerns, and indicates he'll continue reviewing the rest of the series later. This feedback comes from a trusted reviewer with deep MIDX/repacking expertise.