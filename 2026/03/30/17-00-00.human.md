This is the third patch in a refactoring series by Ezekiel Newren that improves type safety in xdiff's record cleanup logic. The patch converts several `long` type declarations to `ptrdiff_t` in the `xdl_clean_mmatch()` function and related variables in `xdl_cleanup_records()`. 

The changes are straightforward and focused - they replace ambiguous `long` types with the more precise `ptrdiff_t` for variables that track array indices and positions. This follows the pattern established in earlier patches of the series to use type-safe integer types where appropriate. The diff shows three simple type changes across two functions in xdiff/xprepare.c.

This iteration appears technically sound, building on the previous patches' work to modernize the xdiff code. The changes maintain the series' goal of improving code clarity without altering behavior, and they address the type safety concerns raised in earlier reviews. With the test failures from v3 now resolved (as noted in the thread context), this patch seems ready for inclusion.