Here's a human-readable summary of the thread:

This thread introduces a two-patch series to improve how Git handles submodule fetch errors when encountering unreachable commits. The first patch fixed a timing issue in error reporting, ensuring failures are only logged after all retry attempts complete. The second patch, now posted, adds the main feature: configurable error handling via a new `fetch.submoduleErrors` option (with matching CLI flag).

The new configuration allows users to choose between failing (the current default) or just warning when submodule fetches hit unreachable commits. This is particularly useful when working with in-progress upstream branches where missing commits are expected. The implementation touches fetch machinery in `submodule.c` and option handling in `builtin/fetch.c`, with thorough test coverage verifying both the default behavior and new warning mode.

The series appears well-considered, with the first patch providing a solid foundation for the configurable error handling. The changes maintain backward compatibility while solving a real workflow pain point for developers working with complex submodule setups. The thread is still new, so no reviews or objections have been raised yet, but the implementation looks ready for consideration.