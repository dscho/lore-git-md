Refactoring patch in the xdiff subsystem, part of a series to improve type safety and readability in `xdl_cleanup_records()`. This is the first of six patches in the fourth iteration of Ezekiel Newren's series, which has already addressed all technical concerns from previous reviews.

The patch simplifies the `recs` pointer usage in `xdl_cleanup_records()` by directly indexing the `xdfile.recs` array instead. It removes the redundant `recs` variable that was previously used to traverse the records, making the code more straightforward. The change is purely internal - no behavior is altered, and all tests pass with the modification.

This is a clean, focused change that follows the series' goal of improving code clarity. The patch is well-contained (just 17 lines modified in xdiff/xprepare.c) and builds on prior work that established the type safety improvements. Given the series' progression through multiple iterations and resolution of all previous feedback, this appears ready for inclusion.