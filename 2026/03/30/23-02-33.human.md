This is a refactoring patch in the xdiff subsystem, part of a completed series that has already received maintainer approval. The patch converts nested ternary operations in `xdl_cleanup_records()` to explicit if/else statements, making the logic for handling diff records more readable while maintaining identical behavior.

Junio C Hamano explicitly approves the change, praising its clarity improvement. The modification touches the decision logic for whether to DISCARD, KEEP, or INVESTIGATE each record in the diff algorithm, converting two similar blocks that handle records from each input file. This is patch 5/6 in the fourth iteration of Ezekiel Newren's series, which has already addressed all technical concerns and test failures from earlier rounds.

The change is straightforward and uncontroversial, representing final polish before merging. The series as a whole improves code clarity and type safety in xdiff's record cleanup logic, with all patches now ready for integration into the main branch.