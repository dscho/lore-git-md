This is a documentation and user interface patch for the `git repo` subcommands, following up on earlier review feedback. Mahi Kassa's v2 patch improves the help text output by making each subcommand (`info` and `structure`) show only its own specific help text rather than the generic top-level usage, while also addressing the maintainability concerns raised in the previous round.

The key changes in v2:
1. Factors out the usage strings into macros to avoid duplication between the top-level and subcommand-specific arrays, implementing Patrick Steinhardt's suggestion
2. Restores proper whitespace in the test files as noted by Derrick Stolee
3. Wraps commit message lines as requested

The implementation now defines separate usage arrays for each subcommand while sharing the common strings via macros, making the code more maintainable while preserving the focused help text behavior. The tests verify that each subcommand's `-h` output shows only its own usage information and not the other subcommand's. The changes remain well-contained to the builtin implementation and its tests, with no behavioral changes beyond the intended help text improvements.

This appears ready for merging, having addressed all substantive feedback from the previous round while maintaining the clear benefits of showing subcommand-specific help. The patch demonstrates good responsiveness to review while keeping the scope focused.