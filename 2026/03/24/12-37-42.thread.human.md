This appears to be a case of two unrelated patch series being discussed in the same thread - likely due to a mis-threaded email or incorrect subject line. The existing thread is about Olamide Caleb Bello's v3 series converting repository configurations from globals to per-repository storage, which has already received thorough review and is awaiting merging. The new email describes a completely different change - reverting a Windows `writev()` compatibility layer - that seems unrelated to the repository configuration work.

Since these are clearly separate topics, the thread summary should remain focused on the repository configuration series. The `writev()` revert appears to be either:
1) A misdirected email that belongs in a different thread, or
2) A completely independent change accidentally included in this series

Unless further emails clarify a connection between these changes, the `writev()` patch should not be considered part of this thread's narrative. The repository configuration series summary stands unchanged - it remains complete and ready for merging with no outstanding issues.