Bugfix addressing a memory leak in pack-objects' stdin-packs handling, with follow-up discussion about test coverage.

This is a reply to the first patch in Taylor Blau's series fixing geometric repacking issues. The original patch fixed a straightforward memory leak in `read_stdin_packs()` where a `rev_info` struct wasn't being properly released. Patrick Steinhardt notes that the fix looks correct but questions why the leak wasn't caught by existing tests. 

The exchange is brief and technical - Patrick isn't raising any objections to the change itself, just expressing curiosity about the test gap. This sort of minor test-coverage discussion is common for cleanup patches like this. The thread remains focused on the specific leak fix rather than the larger series' goals, suggesting reviewers are satisfied with this preparatory change before moving on to the more complex pack-handling issues in later patches.