This is part of the ongoing `the_repository` removal effort, specifically targeting the `core.checkstat` configuration. The patch moves the `check_stat` global variable into `struct repo_config_values` to make it repository-specific rather than process-wide.

The change affects how Git checks file status by making the stat comparison behavior (controlled by `core.checkstat`) per-repository. The patch updates four files to remove the global variable and use the repository-specific storage instead, following the established pattern for these migrations. The default behavior remains unchanged (check_stat=1), and all call sites are updated to access the value through the repository's config.

This appears to be a straightforward continuation of the global variable elimination work, touching stat comparison logic in `statinfo.c` and checkout handling in `entry.c`. The changes are mechanical and consistent with prior patches in this series, though the impact on file status checking means it would benefit from testing across different platforms. The patch has mentoring tags from Christian Couder and Usman Akinyemi, suggesting it's part of a guided contribution effort.