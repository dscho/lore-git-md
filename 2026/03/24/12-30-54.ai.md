This is a technical clarification in the ongoing bisect term handling series. Jonas Rebmann responds to Phillip Wood's review of the second patch, which makes `git rev-parse --bisect` respect custom bisect terms. 

Jonas questions whether the NULL check Phillip suggested is actually necessary, pointing to the implementation of `read_bisect_terms()` in bisect.c. The function appears to always return valid strings (defaulting to "good"/"bad" if no terms are configured) rather than NULL pointers. This suggests the segfault scenario Phillip warned about may not occur in practice.

The exchange shows Jonas carefully considering the feedback while seeking to understand whether the proposed change is truly needed. This level of technical engagement is positive - the author isn't just making requested changes blindly, but verifying the underlying rationale. The thread remains focused on the specific implementation question, with no new issues raised beyond what was already identified in the v2 review.

For the AI summarizer's future reference: the key technical detail here is the behavior of `read_bisect_terms()` when no bisect is active. The function appears to handle this case by returning default strings rather than NULL, which may make the NULL check redundant. This is relevant context if future emails debate memory safety or error handling in the bisect term lookup code.