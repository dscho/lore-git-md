Thread examining test infrastructure robustness after discovery of a typo in t4014-format-patch.sh where `test_expected_success()` was incorrectly used instead of `test_expect_success()`. Original issue was introduced in commit 6005932d95 ("format-patch: add ability to use alt cover format") and fixed in Mirko Faina's mf/format-patch-cover-letter-format series.

Maintainer Junio C Hamano has been leading investigation into why test suite didn't catch the macro misspelling, revealing subtle failure modes where misspelled macros cause silent test skipping while reporting success. Discussion has evolved into broader examination of test framework behavior, with Junio proposing to enable `set -e` (exit on error) in test scripts to make such issues more visible. His initial experiments identify specific incompatibilities, including a patch fixing t4032-diff-inter-hunk-context.sh to properly handle unset config values when running under `set -e`.

Key technical details:
- Current test framework allows macro typos to pass silently with incorrect test counts
- `set -e` implementation requires careful handling of conditional test execution patterns
- Example fix shows replacing `git config --unset` with version that won't fail for unset configs

Open questions:
- Full scope of `set -e` compatibility work needed across test suite
- Whether this direction will be pursued as a microproject or larger effort
- How to balance strict error checking with existing test patterns

Participant positions:
- Junio driving investigation into test framework improvements
- Patrick Steinhardt originally identified the t4014 typo
- Mirko Faina had already fixed the typo in their series

The format-patch cover letter functionality itself remains unaffected, with discussion now focused entirely on test infrastructure robustness rather than the original feature. Future work may involve modifications to test-lib.sh and individual test scripts to support stricter error handling.