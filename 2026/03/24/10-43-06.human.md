Bugfix follow-up in the bisect custom terms series, addressing review feedback on output formatting. Phillip Wood suggests two improvements to Jonas Rebmann's patch that makes `git bisect status` respect configured terminology.

The first suggestion is to wrap custom terms in single quotes in status messages (e.g., "waiting for both 'term1' and 'term2' commits") to make it clearer they are term names rather than free text. This would affect the output format in `bisect_print_status()`.

The second is a test hygiene point: recommending `test_grep` instead of plain `grep` in the test script for better failure diagnostics. The existing tests verify that status messages properly incorporate custom terms like "term1"/"term2" instead of the default "good"/"bad".

These are straightforward refinements to the v2 patch that improve clarity of the output and test debuggability without changing the core functionality. The thread shows active review engagement on what appears to be a well-scoped improvement to bisect's custom term handling. The changes are uncontroversial and address the last bits of polish needed before the series could be ready for merging.