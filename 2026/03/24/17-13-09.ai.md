Test infrastructure robustness patch. Junio C Hamano continues investigating how to make the test framework more resilient to errors like misspelled test macros, proposing to enable `set -e` (exit on error) in test scripts. His initial experiments reveal several places where the test suite isn't yet compatible with this stricter mode, including the example patch fixing t4032-diff-inter-hunk-context.sh to handle cases where config values aren't set and tests should be skipped rather than failing.

The discussion has evolved from the original t4014 typo discovery into a broader examination of test framework behavior. Junio identifies specific shell constructs that need adjustment for `set -e` compatibility, though he considers this work too involved for a microproject due to requiring familiarity with the test framework. The patch demonstrates the type of changes needed - replacing `git config --unset` with a version that won't fail when the config isn't set, and fixing conditional test execution patterns.

This represents foundational work toward making the test suite more reliable by catching errors that currently fail silently. While no immediate solution is proposed, the direction suggests future patches may modify test-lib.sh and individual test scripts to support stricter error handling. The changes would help surface issues like macro typos more visibly rather than allowing them to pass silently.