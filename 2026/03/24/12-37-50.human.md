This is part of the ongoing `the_repository` removal effort, specifically targeting the `warn_on_object_refname_ambiguity` global variable. The patch moves this configuration into `struct repo_config_values` to make it repository-specific rather than process-global.

The change affects several commands that handle object references: `git cat-file --batch`, `git pack-objects`, revision walking (used by many commands), and submodule operations. The patch updates these callers to use the repository-specific configuration while maintaining the existing behavior of temporarily disabling the warning during bulk operations.

This appears to be a straightforward mechanical conversion following the established pattern for migrating globals in the `the_repository` removal effort. The changes are focused and consistent with prior work in this area, touching 7 files with simple variable access updates. The patch has been reviewed by Christian Couder and Usman Akinyemi, suggesting it has passed initial scrutiny.

As with other patches in this series, the main benefit is improved repository isolation - this change prevents one repository's configuration from affecting another when multiple repositories are accessed in the same process. The patch doesn't introduce new functionality or behavior changes, just relocates where the configuration is stored.