This is a technical follow-up in the bisect term handling series, where Phillip Wood acknowledges Jonas Rebmann's correct observation about `read_bisect_terms()` behavior. Phillip confirms that the NULL check he previously suggested is indeed redundant, as the function always returns valid strings (defaulting to "good"/"bad" if no terms are configured).

Phillip also proposes a more substantial architectural change - suggesting that `rev-parse` should reuse the existing `for_each_bisect_ref()` function from revision.c rather than manually constructing refnames and using `refs_for_each_ref_ext()`. This would centralize the bisect ref iteration logic and potentially avoid the memory leak issue Junio identified in the thread.

The exchange shows the review process working well - the author caught a subtle implementation detail that the reviewer initially missed, and the reviewer is now suggesting a more robust architectural approach. The discussion remains focused on technical merits without revisiting the core concept (respecting custom bisect terms), which appears settled.

For future reference: the key points here are the confirmed behavior of `read_bisect_terms()` (always returns non-NULL) and the potential refactoring opportunity to use `for_each_bisect_ref()`. Either approach would need to handle the memory allocation issues noted earlier in the thread.