This thread introduces a series of patches to enhance `git fast-import`'s handling of invalid signatures. Justin Tobler's work builds on recent improvements to the `--signed-commits` option by adding an `abort-if-invalid` mode that fails the entire import when encountering invalid commit signatures. The series also brings `--signed-tags` to parity with commits by implementing all three signature handling behaviors (`strip-if-invalid`, `sign-if-invalid`, and `abort-if-invalid`) for tags.

The changes touch multiple areas including the fast-import implementation, GPG interface plumbing, and documentation, with comprehensive tests added to verify the new behaviors in `t9305` and `t9306`. The patches follow Git's standard practice of focused, incremental changes with test coverage, continuing the recent trend of making signature handling more flexible and consistent across different object types in fast-import. The series appears well-structured and ready for review.