This thread covers Justin Tobler's 4-patch series extending `git fast-import`'s signature handling to provide consistent behavior for both commits and tags when encountering invalid signatures. The series introduces three uniform modes (`strip-if-invalid`, `sign-if-invalid`, and `abort-if-invalid`) that now work identically for both `--signed-commits` and `--signed-tags` options.

The first three patches established the framework: adding `abort-if-invalid` for commits, then implementing `strip-if-invalid` and `sign-if-invalid` modes for tags. The final patch, just submitted, completes the symmetry by adding `abort-if-invalid` for tags. This brings full feature parity between commit and tag signature handling in fast-import operations.

The implementation is thorough, with explicit key ID specification support, comprehensive test coverage across all modes (including edge cases and different signature types), and clear documentation updates. The series builds on recent work to make fast-import's signature handling more flexible, and no outstanding issues or review feedback have been raised yet. With this final patch, the feature appears complete and ready for consideration.