Thread identity: 4-patch series extending `git fast-import`'s signature handling for invalid signatures, authored by Justin Tobler. Follows recent work adding `strip-if-invalid` and `sign-if-invalid` modes to `--signed-commits`.

Core problem/goal: Make `--signed-commits` and `--signed-tags` behavior consistent when handling invalid signatures during import operations, adding an `abort-if-invalid` mode and bringing tag handling to parity with commits.

Current status: Initial version proposing three consistent modes for both commit and tag signature handling during import. Test coverage in t9305 and t9306 verifies new behaviors.

Key technical details:
- Adds `abort-if-invalid` mode to `--signed-commits` that fails entire import on invalid commit signatures
- Extends `--signed-tags` with all three modes (`strip-if-invalid`, `sign-if-invalid`, `abort-if-invalid`)
- Modifications to fast-import.c and GPG interface plumbing
- Documentation updates for new options

Open questions/loose ends: None raised yet in initial submission. Series appears complete with comprehensive test coverage.

Participant positions: Initial submission from Justin Tobler, no reviews yet. Builds on prior signature handling work in fast-import.

Related work: Continues recent efforts to make fast-import signature handling more flexible and consistent between commits and tags.