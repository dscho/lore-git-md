This is a follow-up review comment on a documentation and UI patch for `git repo` subcommands. Patrick Steinhardt notes that despite the v2 changelog claiming to factor out shared usage strings into macros, the actual diff still shows duplicated strings in the usage arrays for `repo info` and `repo structure`. 

The patch series, originally from Mahi Kassa, aims to make each subcommand show only its own help text rather than the generic top-level usage. While the v2 iteration addressed other review feedback (commit message wrapping, test whitespace), Patrick points out it missed the key macro refactoring suggestion he made in the previous round. 

This appears to be an oversight in the v2 submission - the changelog claims the macros were implemented, but the code still contains the duplicated strings. The change remains focused on improving help text specificity, but needs one more iteration to properly factor out the common parts as originally suggested. The review cycle continues, though the scope and intent of the same.