Test infrastructure robustness patch. Junio C Hamano continues his investigation into making the test suite compatible with `set -e` (exit on error) mode, this time fixing t6002-rev-list-bisect.sh. The patch addresses a subtle issue where `expr` commands could cause the test to fail under `set -e` even when operating as intended.

The change modifies two `expr` calls that calculate bisection error margins to explicitly check the exit code, allowing the expected cases where `expr` returns 1 (indicating a zero or null result) to pass through. This preserves the test's mathematical logic while making it robust to stricter error checking.

This is part of Junio's broader effort to harden the test framework against silent failures, sparked by the discovery that a misspelled test macro in t4014 went undetected. The patch demonstrates the kind of careful attention needed to make the entire test suite `set -e` compatible - in this case, understanding that `expr` uses exit code 1 as a valid result, not an error condition.

While small, the change is technically interesting as it shows how shell scripting idioms in the tests may need adjustment to work with stricter error handling. The accompanying comment ("It was fun to figure this one out") hints at the non-obvious nature of the fix. This continues the thread's progression from identifying the original typo to systematically improving test reliability.