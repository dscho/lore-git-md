This is a brief code review comment in an ongoing thread about enhancing the `git backfill` command's partial clone functionality. The series is at v2 with all patches already acked by Junio C Hamano.

Patrick Steinhardt points out a potential issue in patch 3/6 where the code uses `PARSE_OPT_KEEP_UNKNOWN_OPT` when setting up revision parsing but doesn't explicitly handle unknown arguments afterward. He suggests adding a `die()` call to fail on unrecognized options, which would make the behavior more robust and consistent with Git's typical command-line parsing.

The comment is narrowly focused on this one implementation detail in an otherwise complete and reviewed patch series. It doesn't challenge the overall approach but suggests a small hardening of the argument parsing logic. Since the series is already acked, this would likely be addressed as a follow-up if Derrick Stolee agrees with the suggestion.