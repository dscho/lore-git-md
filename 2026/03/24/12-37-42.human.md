Part of the ongoing `the_repository` removal effort, this 8-patch series continues migrating repository-specific global variables into `struct repo_config_values`. The work builds on previous infrastructure that stores repository-dependent configuration, with this batch focusing on environment-related globals like compression levels, Unicode handling, and sparse-checkout settings.

The changes are mechanical but important for reducing cross-repository interference when multiple repositories are used in the same process. Each patch moves one global variable into the per-repository config structure and updates all call sites. The v2 iteration drops a now-merged dependency and fixes handling of `pack_compression_level` to avoid inconsistencies during option parsing.

The series touches a wide range of files (20 modified) but makes straightforward, focused changes. The commit messages clearly explain each migration, and the diffstat shows a net reduction in global state (-88 lines, +126). This appears to be well-scoped work that continues an established refactoring direction, though reviewers will want to verify the correctness of each variable's conversion to repository-specific storage.