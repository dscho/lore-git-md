This is a follow-up to the `git replay` root commit handling patch series, addressing the maintainer's design feedback. The patch author (Toon Claes) confirms Junio Hamano's understanding that `mapped_commit()` cannot handle NULL commits, which was the direct assignment of `replayed_base = onto` for root commits. Claes agrees to encapsulate this logic within `mapped_commit()` in v2 for better API consistency.

The email also notes an additional improvement coming in v2: fixing the merge ancestor label for root commits to show "empty tree" rather than "parent of <commit>" in conflict messages. This avoids misleading output when replaying initial commits. The change remains focused on root commit handling without expanding scope - the author explicitly rejects the idea of modifying `repo_get_commit_tree()` to return empty trees for NULL commits as going too far.

This response shows the typical Git mailing list workflow where authors incorporate maintainer feedback while making their own judgments about appropriate scope. The thread appears headed for a clean resolution with v2 addressing both the technical correctness and API design concerns raised.