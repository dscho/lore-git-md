This is a test infrastructure robustness discussion. Jeff King (Peff) responds to Junio Hamano's ongoing effort to make the test suite more resilient by enabling `set -e` (exit on error) to catch misspelled test macros. While acknowledging the goal, Peff expresses concern about `set -e`'s known quirks and proposes an alternative approach focused on detecting unexpected stderr output.

Peff suggests modifying the test harness to capture stderr separately and flag any that produces output, which would catch the t4014 typo case (where `test_expected_success` was misspelled) without relying on `set -e`. He provides a proof-of-concept Makefile patch that demonstrates this approach, noting it works in normal test runs but would need adaptation for --verbose-log mode.

The proposal represents a different technical direction from Junio's systematic `set -e` conversion, offering potentially simpler error detection for macro typos while avoiding `set -e`'s behavioral complexities. As a veteran contributor with deep test infrastructure knowledge, Peff's alternative warrants consideration, though it would need to address the verbose-log case to be production-ready. The discussion continues the thread's evolution from specific bugfix to broader methodology debate about how best to harden the test framework.