Test infrastructure robustness patch. Junio C Hamano continues his systematic effort to make the test suite compatible with `set -e` (exit on error) mode, this time addressing t0008-ignores.sh. The change fixes a case where `grep`'s exit status (indicating no matches) would incorrectly trigger an early exit under `set -e`, even though the command is being used purely for its output filtering capability.

The patch modifies the `test_expect_success_multiple` helper in t0008 to append `|| :` after the `grep -v` command, ensuring the pipeline continues even when no matches are found. This preserves the existing behavior while making it compatible with stricter error checking. The change is minimal but demonstrates the careful attention needed when auditing test scripts for `set -e` compatibility - commands whose exit status is irrelevant to the test logic must be explicitly handled.

This work builds on Junio's previous patches addressing similar issues in t4032 and t6002, showing a clear pattern of methodically identifying and fixing edge cases across the test suite. The cumulative effect will be a more robust framework that can catch errors like misspelled test macros that currently pass silently. The patch follows the established approach of making minimal surgical changes to preserve existing test behavior while enabling stricter error checking.