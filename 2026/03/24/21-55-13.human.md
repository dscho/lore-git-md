Feature enhancement completing parity between `git fast-import`'s commit and tag signature handling. This final patch in Justin Tobler's 4-part series adds the `abort-if-invalid` mode to `--signed-tags`, matching the behavior already available for `--signed-commits`. When enabled, the import will now fail immediately if any tag signature fails verification.

The implementation follows the same pattern as the commit handling - checking signatures during import and calling `die()` on failure. The test suite is extended to verify this new mode works alongside the existing `strip-if-invalid` and `sign-if-invalid` options. With this change, all three signature handling modes are now consistently available for both commits and tags in fast-import.

The series appears well-considered, with thorough test coverage and clear documentation updates. It completes the recent effort to make signature handling more flexible and consistent across different object types in the import process. No open questions remain in the thread, suggesting this is ready for maintainer consideration.