This is a post-merge style discussion in Adrian Ratiu's hook configuration series, which has already been merged to the 'next' branch. Patrick Steinhardt points out that the patch uses `unsigned int:1` bitfields for boolean flags in two structs (`hook_config_cache_entry` and `struct hook`) where modern Git code would typically use the `bool` type instead.

Junio Hamano responds with a lighthearted quip about "improper bools", indicating this is a minor stylistic observation rather than a blocking issue. The exchange shows the project's attention to code consistency even after patches are accepted, though the current implementation is functionally correct. 

The discussion suggests that if Adrian needs to send a v3 of the series for other reasons, switching to `bool` would be a worthwhile cleanup to align with modern Git conventions. However, since the series is already merged, this may be addressed in a follow-up patch rather than requiring a full revision.