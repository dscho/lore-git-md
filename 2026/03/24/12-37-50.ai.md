This is part of the ongoing `the_repository` removal effort, specifically targeting the `warn_on_object_refname_ambiguity` global variable. The patch moves this configuration into `struct repo_config_values` to make it repository-specific rather than process-global.

The change affects several commands that handle object references: `cat-file`, `pack-objects`, revision walking, and submodule operations. The patch follows the established pattern for these migrations - removing the global variable, adding it to the repository config structure, and updating all call sites to use the repository-specific version. The default value (1) is preserved.

The changes are mechanical but touch 7 files across different subsystems. The most notable impact is in `object-name.c` where the warning logic for ambiguous refnames is modified to check the repository-specific setting. The patch maintains the existing behavior of temporarily disabling the warning during batch operations in `cat-file` and `pack-objects`.

This appears to be a straightforward continuation of the global variable elimination work, following the same template as previous patches in the series. The changes are focused and well-contained, with no new functionality or behavior changes introduced. The patch has been reviewed by Christian Couder and Usman Akinyemi, suggesting it has passed initial scrutiny.