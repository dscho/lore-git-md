This is a documentation and user interface patch for the `git repo` subcommands. Mahi Kassa follows up on earlier feedback to improve the help text output for `git repo info` and `git repo structure` by making each subcommand show only its own specific help text.

The v2 patch incorporates Patrick Steinhardt's suggestion to factor out the shared usage strings into macros, avoiding duplication while maintaining the same functionality. It also addresses Derrick Stolee's style feedback by properly wrapping commit message lines and restoring whitespace in the test files.

The changes are clean and focused - each subcommand now gets its own usage array with the appropriate command-line syntax, and tests verify that `-h` shows only the relevant subcommand's help text. The patch maintains the existing top-level `git repo -h` behavior while improving the specificity of subcommand help output.

This appears ready for merging after the incremental improvements from the first round of review. The changes are well-contained to the builtin implementation and its tests, and the commit message clearly explains the motivation and approach. Users will benefit from seeing more relevant help text when they request help for individual subcommands.