This is a memory leak fix in the pack-objects subsystem, part of Taylor Blau's v2 series addressing geometric repacking issues. The patch plugs a potential (though not currently exercised) leak in `read_stdin_packs()` by properly releasing a `rev_info` struct that was previously left undeallocated. 

The change is straightforward - adding a single `release_revisions()` call - but the commit message provides useful context about the function's history and notes that while this is a real cleanup, it doesn't actually fix any currently failing tests (t5331 passes even without this change). The patch maintains the series' focus on pack handling correctness while addressing a minor but legitimate resource management issue.

For the AI brief: This patch adds missing cleanup of a `rev_info` struct in `builtin/pack-objects.c`'s `read_stdin_packs()` function. While not currently causing test failures, the oversight dates back to the function's introduction in 339bce27f4f and subsequent modification in 97ec43247c0. The fix is simple but demonstrates the series' attention to detail in pack-objects resource management.