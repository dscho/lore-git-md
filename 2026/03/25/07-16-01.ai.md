This is a test infrastructure patch in Junio C Hamano's series to make the test suite work with "set -e" enabled. Patrick Steinhardt suggests an alternative approach to checking CVS availability in t9200-git-cvsexportcommit.sh that differs from Junio's status-capture pattern.

Patrick proposes using the more idiomatic shell construct `if ! cvs version` rather than explicitly capturing the exit status. His version is functionally equivalent but more concise, directly negating the command's success rather than checking the numeric exit code. This aligns with his earlier suggestions in the thread favoring clearer shell idioms where possible.

The change maintains the same behavior - skipping CVS-related tests if CVS isn't available - while being more direct about the intent. Patrick's approach also checks for a working CVS (`cvs version`) rather than just any CVS executable, which might be a more robust check. This continues the pattern seen earlier in the thread where Patrick has advocated for using shell constructs that are both "set -e" safe and more expressive.

The exchange shows the ongoing discussion about how to balance explicit error handling patterns with idiomatic shell usage in the test modernization effort. Patrick's suggestion doesn't introduce new technical concerns but offers a stylistic alternative that may be preferable as the test suite evolves.