This is an RFC proposing a new feature for `git add -p` that would allow discarding hunks directly from the working tree. The patch comes from Luiz Campos and introduces a new 'x' action to reverse-apply hunks, addressing a workflow pain point where users currently have to repeatedly skip unwanted hunks.

The core idea is well-motivated - currently `git add -p` only offers staging or skipping hunks, with no way to discard changes entirely from the working tree. The implementation appears to touch the interactive patch machinery in `add-patch.c` and includes test updates, suggesting a serious technical approach. The author acknowledges open questions about timing (immediate vs deferred discard) and edge cases with overlapping/edited hunks, indicating awareness of potential pitfalls.

The patch references prior discussion on the mailing list where Jeff King (Peff) raised related concerns, showing the author has done their homework on the thread history. The implementation seems to build on that context while proposing a concrete solution. The changeset is small (3 files, ~150 lines net) and focused, making it easy to evaluate.

For a future AI summarizer, key details include: the new 'x' action in `add-patch.c`, working tree modification behavior, test cases in t3701 that verify the discard functionality, and the open questions about timing and edge cases that will likely drive discussion. The patch touches interactive patch application logic but doesn't appear to introduce broader architectural changes.