This is the third patch in a feature series for the `git replay` command, introducing the `--ref` option to specify which reference should be updated by the replay operation. The patch comes from Toon Claes and builds on previous work to make `git replay` more flexible.

The patch adds validation and implementation for the `--ref` option, which allows users to override the default ref update behavior. Key technical aspects include:
- New validation rules requiring a single tip when using `--onto` with `--ref`
- Refname format checking to ensure valid refs
- Explicit incompatibility with `--contained`
- Tests verifying the new validation rules and basic functionality

The implementation touches several files:
- `replay.c` gains the core logic for handling `--ref`
- `builtin/replay.c` adds the option parsing
- Documentation updates explain the new option
- `t/t3650-replay-basics.sh` adds test coverage

The changes look well-considered, with thorough test coverage for the new functionality. This appears to be a focused plumbing enhancement that gives users more precise control over ref updates during replay operations while maintaining backward compatibility. The patch follows Git's standard practices for option handling and includes clear documentation of the new behavior.

For future summarizers: The key technical details are the validation rules for `--ref` usage (single tip requirement, refname format checking) and the explicit incompatibility with `--contained`. The implementation modifies the replay machinery to handle these cases while maintaining existing behavior when `--ref` isn't specified.