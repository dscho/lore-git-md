What's cooking in git.git (March 2026) - Junio Hamano's status report on in-flight topics. Graduated to master: Coccinelle rule against strbuf-by-value passing (dd), transport color config leak fixes (jk), fast-import signed commit handling (jt), git history split subcommand (ps), upload-pack write buffering (ps). New topics: fast-import signed modes config (jt), parallel hook execution (ar), HTTP 429 retry support (vp). Ongoing work: the_repository removal (ps), ODB abstraction (ps), MIDX/bitmap improvements (tb), test/doc cleanups, platform fixes. Some topics discarded as superseded. Standard integration branches (maint, master, next, seen) maintained with mirrors.

Jeff King (jk) addressed a Windows test failure in t0061.24 related to child process handling in transport code, replacing the fragile test with a simpler test-tool case that maintains argument-with-spaces testing while avoiding ssh protocol complications (12-line change to t/t0061-run-command.sh).

Shreyansh Paliwal's `sp/add-patch-with-fewer-the-repository` patch (modifying add-patch.c to use repository instance from `add_i_state` rather than global `the_repository`) remains in "cooking" status. Junio Hamano clarifies that while the patch follows the established pattern for `the_repository` removals, it still requires substantive review beyond his initial comment about interaction with other in-flight topics. This reflects the project's standard that even straightforward refactorings need multiple reviews before being considered ready. The patch remains open pending broader review participation.