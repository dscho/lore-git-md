This is a documentation patch in Toon Claes' ongoing series to improve the `git replay` plumbing command. The patch standardizes the command's documentation and help text to consistently use the "stuck form" (`--option=value`) syntax as per gitcli(7) guidelines, rather than the "separate form" (`--option value`).

The changes are mechanical but thorough, updating both the man page (`Documentation/git-replay.adoc`) and the in-code usage message (`builtin/replay.c`). All examples and option descriptions now show the stuck form, and the patch also reorders some documentation strings to better match their presentation order in the docs.

This appears to be a straightforward improvement that follows established Git documentation conventions. The changes are uncontroversial and align with Kristoffer Haugsbakk's earlier feedback about documentation consistency. The patch maintains all existing functionality while making the interface documentation more consistent with Git's style guidelines.