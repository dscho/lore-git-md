This is the second patch in a refactoring series that improves help text handling for `git repo` subcommands. Building on the first patch's macro extraction, it now makes each subcommand (`info` and `structure`) show only its own usage information when called with `-h`.

The change introduces separate usage arrays for each subcommand while maintaining the same functionality. The implementation looks clean and focused, with the behavior change accompanied by new tests verifying that `-h` shows the correct subcommand-specific help text. The tests check both positive cases (showing the right command) and negative cases (ensuring unrelated command help isn't shown).

This appears to be a well-structured patch that completes the refactoring started in part 1. The commit message clearly explains the change, and the implementation follows Git's coding style. The patch adds test coverage for the new behavior while keeping the changes minimal and focused. The version history shows the author has responded to feedback by splitting the behavior change into a separate patch from the initial refactoring.