Refactoring patch in the xdiff subsystem, part of a narrowed-down series from Ezekiel Newren. This first patch simplifies the `xdl_cleanup_records()` function by removing a local `recs` pointer variable and using direct array indexing instead. The change makes the code more straightforward by eliminating pointer arithmetic in favor of explicit array access.

The patch is purely internal - no behavior or performance impact - and focuses on improving readability of the diff algorithm's record cleanup logic. It's a clean, focused change that follows the series' revised goal of making targeted readability improvements after feedback on the broader v1 approach. The modification touches only `xdiff/xprepare.c`, changing how the function accesses record arrays while maintaining identical functionality. The commit message clearly explains the change and suggests using `--color-words` to view the diff for maximum clarity.