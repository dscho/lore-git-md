[PATCH 0/5] refs: continue removing `the_repository` usage

Series from Shreyansh Paliwal continuing the long-running effort to eliminate global state by making refs subsystem functions explicitly take `struct repository *` parameters. Builds on jw/object-name-bitset-to-enum. Five mechanical conversions targeting different aspects of ref handling:

1. Branch name helpers (branch.c, ref-filter.c) now take repository parameter - updated patch makes `copy_branchname()`, `check_branch_ref()`, `validate_branchname()`, and `validate_new_branchname()` explicitly repository-aware, propagating changes through builtin commands (branch, checkout, worktree) where `the_repository` is passed when no specific context exists
2. File ref lock timeout handling (refs/files-backend.c) uses repository context
3. Replaces `the_hash_algo` with repo-specific hash algorithms in refs code
4. Removes last `the_repository` usage in reftable backend (reftable/backend.c)
5. Eliminates final `the_repository` in packed backend (refs/packed-backend.c)

Changes are wide-ranging but focused - each commit tackles a specific pattern of global state usage without altering behavior. Touches multiple files including refs/*, branch.c, ref-filter.c, and builtin commands. Follows established pattern from previous `the_repository` removal work led by René Scharfe. No open questions or objections raised - this appears to be straightforward continuation of uncontroversial technical debt cleanup, with each modified function clearly documented in commit messages. Builds cleanly on previous work (jw/object-name-bitset-to-enum).