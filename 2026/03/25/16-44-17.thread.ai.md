[PATCH 0/5] refs: continue removing `the_repository` usage - now [PATCH 1-4/5]

Series from Shreyansh Paliwal converts refs subsystem to explicit `struct repository *` parameters, now proceeding with patches 2-5/5 after deferring branch helper conversions (original patch 1/5) due to unresolved edge cases. Current version focuses on file ref lock timeouts, packed backend completion, and reftable backend while postponing branch name helper changes.

Key technical changes: Patch 2/5 updates file ref lock timeouts. Patch 3/5 removes `the_hash_algo` in `refs.c`. Patch 4/5 converts reftable backend. Patch 5/5 completes packed backend by replacing last `the_repository` usage with `ref_store->repo` for timeout config, removing `USE_THE_REPOSITORY_VARIABLE` macro.

Open issue remains the repository validity checking edge case discovered by Burak Kaan Karaçay when `GIT_DIR` points to non-existent path (`git --git-dir='non-existing' check-ref-format --branch @{-1}`), where current approaches (Patrick Steinhardt's `repo`-only, Tian Yuchen's `repo && repo->gitdir`, or hybrid `startup_info` checks) all have limitations. This particularly affects branch helper functions, now deferred for separate treatment.

Jeff King's 2019 comment on `startup_info->have_repository` provides historical context. Current tests still lack coverage for invalid `GIT_DIR` scenario. Tian Yuchen's `repo_has_repository()` wrapper proposal remains under consideration as potential temporary solution, though underlying architectural issue requires more work.

Series continues Rene Scharfe's multi-year `the_repository` removal effort, touching `refs.c`, `refs/files-backend.c`, `refs/reftable-backend.c`, `refs/packed-backend.c`, `refs/refs-internal.h` and 9 other files. Remaining work includes: resolving repository validity semantics, static timeout caching integration with `repo-settings`, `startup_info` replacement, and final `is_bare_repository()` conversion in reftable backend. Progress continues on non-branch-helper conversions while complex edge cases get separate attention.