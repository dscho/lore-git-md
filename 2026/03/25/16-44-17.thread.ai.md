[PATCH 0/5] refs: continue removing `the_repository` usage

Series from Shreyansh Paliwal converts refs subsystem to explicit `struct repository *` parameters, eliminating `the_repository` and `the_hash_algo` across branch helpers, lock timeouts, and all three ref backends (files, reftable, packed). Version 5 concludes packed backend conversion after incorporating prior feedback. Patch 1/5 converted branch name functions across 9 files. Patch 2/5 updated file ref lock timeouts. Patch 3/5 removed `the_hash_algo` in `refs.c`. Patch 4/5 converted reftable backend. Patch 5/5 completes packed backend by replacing last `the_repository` usage with `ref_store->repo` for timeout config, removing `USE_THE_REPOSITORY_VARIABLE` macro.

Patrick Steinhardt reviewed patch 1/5's handling of `check_branch_ref()`, agreeing with Tian Yuchen's earlier concern about mixing `startup_info->have_repository` checks with new explicit `repo` parameter. Proposed solutions: either audit all callers for consistent repo state or use `repo && repo->gitdir` as validity check. Tian Yuchen's v2 feedback addressed (build fix for `#include`, deferring deeper `startup_info` conversion). Burak Kaan Karacay provided timeout caching input.

Patrick Steinhardt now confirms the reftable backend conversion is on track, noting remaining `is_bare_repository()` dependency will be handled in his own upcoming work, indicating coordinated progress on eliminating global state. Open items: static timeout caching needing `repo-settings` integration, `startup_info` replacement, and final `is_bare_repository()` conversion in reftable backend - known limitations in Rene Scharfe's multi-year `the_repository` removal effort. Series builds cleanly with no functional changes, touching `refs.c`, `refs/files-backend.c`, `refs/reftable-backend.c`, `refs/packed-backend.c`, `refs/refs-internal.h` and 9 other files while preserving behavior, following Patrick Steinhardt-approved patterns. Ready for merging as significant progress in eliminating refs subsystem global state.