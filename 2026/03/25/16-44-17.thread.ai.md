[PATCH 0/3] refs: continue removing `the_repository` usage - now [PATCH 1-3/3]

Shreyansh Paliwal's series advances the `the_repository` removal effort in the refs subsystem with three patches eliminating global state. Patch 1/3 converts `get_files_ref_lock_timeout_ms()` to use explicit `struct repository*` while maintaining static timeout caching - Tian Yuchen's review notes this partial conversion and suggests clarifying the commit message about the remaining static variable, acknowledging full multi-repo support would require moving configuration to `repo-settings` later. Patch 2/3 removes `the_hash_algo` in core ref transaction functions by using `ref_store->repo`'s hash algorithm. Patch 3/3 finalizes reftable backend conversion, replacing remaining `the_repository` uses in initialization.

Open issues include repository validity edge cases (invalid `GIT_DIR` paths) with Jeff King's 2019 `startup_info->have_repository` comments and Tian Yuchen's `repo_has_repository()` proposal still under consideration. The series touches `refs.c`, `refs/files-backend.c`, `refs/reftable-backend.c` as part of René Scharfe's multi-year effort. Remaining work includes finalizing repository validity semantics, integrating static timeout caching with `repo-settings`, replacing `startup_info`, Patrick Steinhardt's upcoming `is_bare_repository()` conversion, and branch helper updates. The review discussion shows healthy dynamics with constructive feedback being incorporated incrementally.