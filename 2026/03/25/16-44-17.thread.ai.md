[PATCH 0/5] refs: continue removing `the_repository` usage

Series from Shreyansh Paliwal systematically converts refs subsystem to explicit `struct repository *` parameters, eliminating `the_repository` and `the_hash_algo` usage across branch helpers, lock timeouts, and all three ref backends (files, reftable, packed). Version 3 incorporates all prior review feedback: fixed `#include "repository.c"` to `repository.h` (Tian Yuchen's v2 review), extracted long algorithm access chain to local variable for readability (Tian's style suggestion), and maintained Patrick Steinhardt-approved function signature conventions (repository parameters first) and callback design.

Patch 1/5 focuses on branch name handling helpers, adding explicit `struct repository*` parameters to functions like `copy_branchname()`, `check_branch_ref()`, `validate_branchname()`, and `validate_new_branchname()`. These changes propagate through builtin commands (branch, checkout, worktree) that use these helpers, updating 9 files while maintaining behavior. The patch builds on prior work (jw/object-name-bitset-to-enum) that introduced branch interpretation enums, with callers updated to pass `the_repository` where needed.

Key conversions complete except one `is_bare_repository()` dependency in reftable backend (4/5). Packed backend (5/5) now free of `USE_THE_REPOSITORY_VARIABLE` through explicit `ref_store->repo` usage for lock timeout config. File ref lock timeouts (2/5) and hash algorithm usage (3/5) now repository-aware.

Open items deferred for future work: static timeout caching needing `repo-settings` integration (Burak Kaan Karaçay and Tian Yuchen's observations), and potential `startup_info` replacement with `repo->gitdir` checks (Tian's non-blocking suggestion). These remain known limitations in the incremental approach to René Scharfe's multi-year effort. Series maintains behavior while making repository dependencies explicit across 13 files. All mechanical conversions follow established patterns with Patrick's technical approval, Burak's architectural input, and Tian's refinement suggestions addressed. Builds cleanly with no functional changes. Ready for final review with remaining discussion likely focused on deferred items rather than core conversions.