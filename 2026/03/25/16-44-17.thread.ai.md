[PATCH 0/5] refs: continue removing `the_repository` usage

Series from Shreyansh Paliwal completes conversion of refs subsystem to explicit `struct repository *` parameters, eliminating `the_repository` and `the_hash_algo` usage across branch helpers, lock timeouts, and all three ref backends (files, reftable, packed). Version 3 incorporated prior feedback; version 5 concludes packed backend conversion. Patch 1/5 converted branch name handling functions across 9 files. Patch 2/5 updated file ref lock timeouts. Patch 3/5 removed `the_hash_algo` in `refs.c`. Patch 4/5 converted reftable backend. Patch 5/5 completes packed backend by replacing last `the_repository` usage with `ref_store->repo` for timeout config, removing `USE_THE_REPOSITORY_VARIABLE` macro.

Tian Yuchen followed up on patch 1/5 review points: build fix for `#include "repository.c"` -> `repository.h` will be in next version (non-breaking due to static linking), while deeper `startup_info->have_repository` -> `repo->gitdir` conversion was deferred as potentially out of scope for this series. Tian agrees with incremental approach. Series touches `refs.c`, `refs/files-backend.c`, `refs/reftable-backend.c`, `refs/packed-backend.c`, `refs/refs-internal.h` and 9 other files while preserving behavior, following Patrick Steinhardt-approved patterns. Tian Yuchen's v2 feedback addressed (headers, readability). Burak Kaan Karacay provided timeout caching input.

Open items remain: static timeout caching needing `repo-settings` integration, potential `startup_info` replacement, and final `is_bare_repository()` conversion in reftable backend - known limitations in Rene Scharfe's multi-year `the_repository` removal effort. Series builds cleanly with no functional changes, appears ready for merging as significant progress in eliminating refs subsystem global state.