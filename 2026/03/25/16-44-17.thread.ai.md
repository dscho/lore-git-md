[PATCH 0/5] refs: continue removing `the_repository` usage

Series from Shreyansh Paliwal completes conversion of refs subsystem to explicit `struct repository *` parameters, eliminating all `the_repository` and `the_hash_algo` usage across branch helpers, lock timeouts, and all three ref backends (files, reftable, packed). Version 3 incorporated prior review feedback and version 5 now concludes the packed backend conversion.

Patch 1/5 converted branch name handling functions (`copy_branchname()`, `check_branch_ref()` etc) across 9 files. Patch 2/5 updated file ref lock timeouts via `get_files_ref_lock_timeout_ms()` with explicit repository parameter. Patch 3/5 removed all `the_hash_algo` usage in `refs.c`, replacing with repository-specific access. Patch 4/5 converted reftable backend, leaving only one `is_bare_repository()` dependency. Final patch 5/5 now completes packed backend by replacing last `the_repository` usage in `refs/packed-backend.c` with `ref_store->repo` for timeout config, removing `USE_THE_REPOSITORY_VARIABLE` macro.

Series touches `refs.c`, `refs/files-backend.c`, `refs/reftable-backend.c`, `refs/packed-backend.c`, `refs/refs-internal.h` and 9 other files while preserving behavior. All mechanical conversions follow patterns established in prior work, with Patrick Steinhardt-approved function signatures (repository parameters first) and callback designs. Tian Yuchen's v2 review feedback addressed (header fixes, readability improvements). Burak Kaan Karacay provided architectural input on timeout caching.

Open items remain for future work: static timeout caching needing `repo-settings` integration, potential `startup_info` replacement with `repo->gitdir` checks, and final `is_bare_repository()` conversion in reftable backend. These are known limitations in incremental approach to Rene Scharfe's multi-year `the_repository` removal effort. Series builds cleanly with no functional changes and appears ready for merging, marking significant progress in eliminating global state from refs subsystem.