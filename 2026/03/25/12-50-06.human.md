This is a technical response in the ongoing `git replay --ref` implementation thread, where Toon Claes addresses two issues raised by Tian Yuchen's review.

The author acknowledges two problems with the current patch:
1. The comment about `--ref` ignoring `--contained` is misleading since the code actually errors out when both are used. Toon suggests adding explicit validation and updating the comment to clarify the incompatibility.

2. More significantly, the current implementation doesn't properly handle cases where multiple branches are specified alongside `--ref`, potentially leaving branches orphaned. The example `git replay --onto main --ref refs/heads/target topic1 topic2` would only update the target ref while discarding the expected updates for topic1 and topic2.

Toon indicates he'll investigate solutions for both issues. The response shows the author engaging constructively with substantive technical feedback, though the actual fixes aren't proposed in this email. This keeps the thread moving forward while leaving the implementation details for a future iteration.