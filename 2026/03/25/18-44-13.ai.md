This is a feature patch in the `git replay` series, now queued for integration by Junio C Hamano. The patch from Toon Claes adds a `--ref` option to specify which reference should be updated by the replay operation, providing more precise control over ref updates.

The implementation includes validation rules to ensure proper usage: requiring a single tip when using `--onto` with `--ref`, checking refname format validity, and explicitly marking incompatibility with `--contained`. The changes touch replay.c for core logic, builtin/replay.c for option parsing, and add test coverage in t3650-replay-basics.sh.

Junio's response suggests minor documentation tweaks (changing "branch" to "ref" in option descriptions) but otherwise approves the technical approach. He notes the implementation is clean with good validation checks, particularly praising the ref handling logic. The patch maintains backward compatibility while adding the new functionality.

For future summarizers: Key technical points are the refname validation rules (must start with "refs/" and pass format checks) and the single-tip requirement when combining `--ref` with `--onto`. The implementation queues updates differently based on whether `--ref` is specified, using the new validation logic while preserving existing behavior paths.