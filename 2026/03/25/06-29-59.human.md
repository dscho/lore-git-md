This is a small fix in the `git replay --revert` series, addressing a compiler warning about variable declaration order. The patch is part of the v5 series that adds revert capability to `git replay`, which has already undergone extensive review and is now in its final polishing stage.

The issue is purely stylistic - the code declares a variable after a statement, which triggers a warning when compiled with `-Werror=declaration-after-statement`. This is a common coding style requirement in Git's codebase where variables must be declared at the start of blocks. The fix moves the `desired_reverse` variable declaration before the `repo_init_revisions()` call, maintaining the same logic while conforming to Git's coding guidelines.

This minor adjustment shows the series is in its final stages, with only small polish items remaining before merging. The core functionality - enabling server-side reverts on bare repositories through `git replay` - remains unchanged and has already been approved by key reviewers including Phillip Wood, Patrick Steinhardt, and now Junio Hamano.