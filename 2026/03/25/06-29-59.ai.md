This is a final polish patch in the `git replay --revert` series, addressing a minor compiler warning that surfaced during integration testing. The change fixes a `-Werror=declaration-after-statement` violation by moving a variable declaration before other statements in `builtin/replay.c`.

The issue is purely stylistic - the code was functionally correct but violated Git's coding guidelines that require variable declarations to appear at the start of blocks. The fix is straightforward: moving the `desired_reverse` declaration before the `repo_init_revisions()` call. This maintains all the existing logic while conforming to the project's style rules.

This patch represents the final tweak in a well-reviewed series that has gone through five iterations with input from Phillip Wood, Patrick Steinhardt, and Junio Hamano. The core functionality - adding revert capability to `git replay` for server-side use cases like GitLab's Gitaly - remains unchanged. The series is now fully polished and ready for merging.