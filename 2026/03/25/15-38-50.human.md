Refinement of the `git replay --revert` feature series, addressing final review feedback. This is the sixth iteration of a series that adds revert capability to `git replay`, primarily for GitLab's Gitaly service use case.

Siddharth Asthana responds to Toon Claes' review comments on the v5 series, noting two minor issues that will be fixed in v6:

1. A `-Werror=declaration-after-statement` compiler warning in `builtin/replay.c` where a variable declaration appears after other statements, violating Git's coding style. This was already identified in the parent email's patch.

2. A documentation nit where the `<revision-range>` explanation in `git-replay.txt` mentions `--advance` mode but should also reference `--revert` mode for completeness. While Toon considered this minor enough to defer, Siddharth plans to include it in v6 since a reroll is needed anyway for the compiler warning fix.

The technical implementation remains unchanged from the well-reviewed v5 series, which has already addressed all substantive feedback across five iterations with input from Phillip Wood, Patrick Steinhardt, and Junio Hamano. This appears to be the final polish before the feature can be merged.