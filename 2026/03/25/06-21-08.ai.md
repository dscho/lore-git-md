This is the fifth patch in Junio C Hamano's 11-part series to make Git's test suite work with "set -e" enabled. The patch fixes a conditional test in t7450-bad-git-dotfiles.sh that checks symlink handling in Git's index.

The specific change converts a test pattern from `test -n "$var" && test_expect_success` to `test -z "$var" || test_expect_success` to properly handle the case where the variable is empty under "set -e". The test verifies Git's behavior when encountering symlinked dotfiles, with this particular case checking whether Git refuses to load certain symlinked files into the index.

This is a mechanical change required for the broader "set -e" effort - the test's actual functionality remains the same, just expressed in a way that won't trigger false failures under the stricter error checking. The patch follows the pattern seen in earlier parts of the series, showing careful attention to maintaining the original test semantics while adapting to the new error-handling regime.

As with the rest of the series, this change is straightforward and focused - it doesn't introduce new test cases or modify behavior, just adjusts the test to work with the upcoming stricter error detection. The commit message clearly explains both the general pattern being fixed and the specific instance being addressed.