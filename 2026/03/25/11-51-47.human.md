This is a refactoring patch that extracts usage strings for `git repo` subcommands into shared macros. The change is part of a larger series (positioned as 1/2 in v4) aimed at improving subcommand-specific help output.

The patch moves the usage strings for `git repo info` and `git repo structure` commands from direct array entries into `#define` macros (`REPO_INFO_USAGE` and `REPO_STRUCTURE_USAGE`). This allows the same strings to be reused in multiple places without duplication, which will be useful for the follow-up patch that presumably needs to reference these usage patterns elsewhere.

The change is mechanical and straightforward - it just relocates the strings into macros while maintaining their exact content. The commit message clearly states this is preparatory work for subsequent changes, and the patch appears to do exactly what it claims without introducing any functional changes. The diff is clean and limited to the intended scope.

This looks like a well-structured, focused refactoring that follows Git's conventions for such preparatory work. The patch is small and easy to review, with a clear motivation stated in the commit message.