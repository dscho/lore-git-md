This is the final merge notification for Adrian Ratiu's hook configuration modernization series, now fully integrated into Git's development branch ('next'). The series represents a comprehensive overhaul of Git's hook subsystem, adding user-facing improvements while establishing infrastructure for future extensions.

Junio Hamano notes one remaining minor naming quibble - the overlapping use of "event_disabled" in two different contexts (a boolean in `struct hook` and a string-list in `struct repository`).

The series has been through multiple review iterations and addresses all previous feedback. Key user-visible changes include:
- Config scope visibility (`--show-scope`)
- Display of disabled hooks
- Validation against unknown hook names (with an escape hatch for custom hooks)

The implementation appears solid, with careful attention to backward compatibility and thorough test coverage. This work positions the hook subsystem for future metadata additions while delivering immediate usability improvements. The changes are now in 'next' branch for final verification before the next release.