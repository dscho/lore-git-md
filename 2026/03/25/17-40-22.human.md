This is the second and final patch in a refactoring series that makes `git repo` subcommands show only their own help text when invoked with `-h`. The change builds on the first patch's macro extraction work (which Junio has already acked) by introducing separate usage arrays for the `info` and `structure` subcommands.

Junio confirms the implementation looks good and notes that the shared `repo_usage` array remains in use for the top-level `git repo -h` output. He observes that the tests currently expect `-h` to fail (a convention that may change across Git), but approves leaving that as-is for now since it matches existing behavior. The patch includes thorough tests verifying each subcommand's help output contains only its own usage information.

The series appears ready for merging, with both patches reviewed and approved by the maintainer. This is a clean, focused improvement to the command-line interface that follows Git's standard patterns for such changes.