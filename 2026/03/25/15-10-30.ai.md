This is a final review comment on the `git replay --revert` feature series, which adds revert capability to the `git replay` command primarily for GitLab's Gitaly service use case. Toon Claes notes two minor issues that don't require a re-roll:

1. A `-Werror=declaration-after-statement` compiler warning in `builtin/replay.c` where a variable declaration appears after other statements, violating Git's coding style. This was already fixed in the parent email's patch.

2. A documentation nit where the `<revision-range>` explanation in `git-replay.txt` mentions `--advance` mode but should also reference `--revert` mode for completeness. Toon considers this minor enough to defer to a future patch series.

The technical implementation remains unchanged from the well-reviewed v5 series, which:
- Introduces `REPLAY_MODE_REVERT` alongside existing `REPLAY_MODE_PICK`
- Handles reverts via commit-by-commit reversal with individual messages
- Processes reverts newest-first to reduce conflicts
- Uses current user as author for revert commits
- Includes comprehensive test coverage

The series has already addressed all substantive feedback across five iterations with input from Phillip Wood, Patrick Steinhardt, and Junio Hamano. These final polish items don't affect functionality and can be handled post-merge. The feature appears ready for integration into main.