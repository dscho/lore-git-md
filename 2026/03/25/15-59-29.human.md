This is a refactoring patch in the `git replay` plumbing command series, marking several options as non-negatable using `PARSE_OPT_NONEG`. The patch updates the option parsing in `builtin/replay.c` to explicitly prevent negation of `--onto`, `--advance`, `--revert`, and `--ref-action` flags, which don't make sense to negate in this context.

The change is straightforward and mechanical, converting the existing `OPT_STRING` declarations to `OPT_STRING_F` with the `PARSE_OPT_NONEG` flag. This follows Git's standard practices for option handling and makes the command's behavior more consistent with user expectations. The patch maintains all existing functionality while making the interface more robust against invalid usage patterns.

This is part of a larger series introducing the `--ref` option for precise reference updates in `git replay`. The change here is uncontroversial and aligns with the project's coding standards, though it's a minor preparatory step rather than a user-visible feature. The implementation looks correct and the commit message clearly explains the change.