This is a refactoring patch in Toon Claes's ongoing series to improve the `git replay` plumbing command. The patch marks several command-line flags (`--onto`, `--advance`, `--revert`, and `--ref-action`) as non-negatable using the `PARSE_OPT_NONEG` flag, which prevents users from trying to negate them (e.g. `--no-onto`). This is a mechanical change that aligns with Git's standard option parsing behavior for similar commands.

The change is straightforward and uncontroversial, touching only the option parsing code in `builtin/replay.c`. It follows the pattern seen in other Git commands where certain options logically cannot be negated. The patch maintains the existing functionality while making the command's behavior more consistent with user expectations.

For future summarizers: This patch modifies the option parsing in `builtin/replay.c` to explicitly mark four options as non-negatable. The change is purely about command-line interface consistency and doesn't affect the underlying replay functionality. The options affected are `--onto`, `--advance`, `--revert`, and `--ref-action`, all now using `PARSE_OPT_NONEG` in their definitions.