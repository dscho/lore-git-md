Thread identity: RFC and implementation of 'x' (discard) action for `git add -p` from Luiz Campos, enabling direct working tree hunk discards during interactive staging.

Core problem: Eliminates workflow friction where users must manually remove unwanted changes after skipping hunks in `git add -p`. The patch implements a reverse-apply mechanism that discards selected hunks directly from the working tree while addressing concerns about data safety and command boundaries.

Technical details: Introduces comprehensive discard functionality in `add-patch.c` with:
- New `DISCARD_HUNK` state and `REASSEMBLE_DISCARD` assembly logic
- `apply_discard_hunks()` function leveraging `git apply -R`
- Updated prompt strings and help text
- Robust test coverage in t3701 including new discard-specific cases
Phillip Wood's review identified several implementation refinements now addressed:
- Simplified hunk merge logic via direct USE_HUNK/DISCARD_HUNK passing
- Corrected delta application direction during reverse patching
Johannes Schindelin's review adds:
- Readability concern about `reassemble_patch()`'s `continue`-based flow
- Optimization opportunity in `apply_discard_hunks()` via early empty-patch detection
Luiz Campos now responds to D. Ben Knoble's feedback by:
- Acknowledging need for recoverability mechanism (blob storage or undo capability)
- Clarifying workflow motivation vs `git restore -p` while showing openness to design reconsideration

Current status: Initial RFC established need; implementation addresses edge cases like hunk merging during discards. Phillip Wood and Johannes Schindelin confirm technical soundness while identifying specific refinements. Luiz Campos engages constructively with D. Ben Knoble's UX concerns, moving discussion toward recoverability solutions while maintaining flexibility on final design approach.

Open questions: Reviewers examining:
- Safety mechanisms for immediate working tree modification (recoverability via blobs/undo)
- Final decision on command boundary between `add -p` and `restore -p` functionality
- Interaction with hunk editing/splitting operations (additional test coverage requested)
- Scope of availability across `add -p` modes (currently only appears when edit is available)
Phillip Wood maintains philosophical concerns about `git add` modifying working tree but focuses review on implementation quality.

Participant positions: Luiz Campos driving implementation while thoughtfully engaging with feedback, particularly D. Ben Knoble's recoverability concerns. Phillip Wood provided detailed code review while maintaining reservations about the fundamental design choice. Johannes Schindelin adds implementation-focused feedback on readability and optimization. D. Ben Knoble's UX considerations about undo safety and command boundaries now actively shaping design direction.

Context: Part of ongoing `git add -p`/interactive patch machinery improvements, building on Phillip Wood's sequencer work. The patch demonstrates good subsystem awareness while proposing a user-visible behavior change that continues to evolve based on review feedback. Reviews from multiple domain experts (Wood, Schindelin, Knoble) surface both implementation details and design philosophy considerations, with particular attention to preventing data loss while maintaining workflow efficiency.