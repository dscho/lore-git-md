Thread identity: RFC and implementation of 'x' (discard) action for `git add -p` from Luiz Campos, enabling direct working tree hunk discards during interactive staging.

Core problem: Eliminates workflow friction where users must manually remove unwanted changes after skipping hunks in `git add -p`. The patch implements a reverse-apply mechanism that discards selected hunks directly from the working tree.

Technical details: Introduces comprehensive discard functionality in `add-patch.c` with:
- New `DISCARD_HUNK` state and `REASSEMBLE_DISCARD` assembly logic
- `apply_discard_hunks()` function leveraging `git apply -R`
- Updated prompt strings and help text
- Robust test coverage in t3701 including new discard-specific cases
The implementation (3 files, ~150 lines net) cleanly extends Phillip Wood's interactive patch machinery without broader architectural changes.

Current status: Initial RFC discussion established the need; this follow-up provides complete implementation addressing edge cases like hunk merging during discards. Builds on established `add -p` infrastructure while introducing new working tree modification behavior.

Open questions: Reviewers will likely examine:
- Safety of immediate working tree modification vs deferred application
- Interaction with hunk editing/splitting operations
- Scope of availability across `add -p` modes
Jeff King's prior concerns about these aspects remain relevant for discussion.

Participant positions: Luiz Campos driving implementation with awareness of Phillip Wood's subsystem expertise and Jeff King's prior feedback. The change aligns with Git's interactive workflow improvements but introduces new behavior requiring careful review.

Context: Part of ongoing `git add -p`/interactive patch machinery improvements, building on Phillip Wood's sequencer work. The patch demonstrates good subsystem awareness while proposing a user-visible behavior change that may need design validation.