Thread identity: RFC and implementation of 'x' (discard) action for `git add -p` from Luiz Campos, enabling direct working tree hunk discards during interactive staging.

Core problem: Eliminates workflow friction where users must manually remove unwanted changes after skipping hunks in `git add -p`. The patch implements a reverse-apply mechanism that discards selected hunks directly from the working tree while addressing concerns about data safety and command boundaries. Maintainer Junio C Hamano now raises fundamental design objections, suggesting a more comprehensive interactive command might better serve the underlying need.

Technical details: Introduces comprehensive discard functionality in `add-patch.c` with:
- New `DISCARD_HUNK` state and `REASSEMBLE_DISCARD` assembly logic
- `apply_discard_hunks()` function leveraging `git apply -R`
- Updated prompt strings and help text
- Robust test coverage in t3701 including new discard-specific cases
Phillip Wood's review identified several implementation refinements now addressed:
- Simplified hunk merge logic via direct USE_HUNK/DISCARD_HUNK passing
- Corrected delta application direction during reverse patching
Johannes Schindelin's review adds:
- Readability concern about `reassemble_patch()`'s `continue`-based flow
- Optimization opportunity in `apply_discard_hunks()` via early empty-patch detection
Luiz Campos responded to D. Ben Knoble's feedback by:
- Acknowledging need for recoverability mechanism (blob storage or undo capability)
- Clarifying workflow motivation vs `git restore -p` while showing openness to design reconsideration

Current status: Initial RFC established need; implementation addresses edge cases like hunk merging during discards. Phillip Wood and Johannes Schindelin confirm technical soundness while identifying specific refinements. Junio C Hamano now introduces a significant design-level objection, suggesting the problem might be better solved by a new unified interactive tool rather than extending `git add -p`. Luiz Campos' engagement with D. Ben Knoble's UX concerns remains relevant but may need reconsideration in light of Junio's broader suggestion.

Open questions: Reviewers examining:
- Safety mechanisms for immediate working tree modification (recoverability via blobs/undo)
- Fundamental appropriateness of `git add` modifying working tree (Junio's objection)
- Potential for a new unified interactive command combining `add -p`, `reset -p`, `stash -p`, and `restore -p`
- Interaction with hunk editing/splitting operations (additional test coverage requested)
- Scope of availability across `add -p` modes (currently only appears when edit is available)

Participant positions: Luiz Campos driving implementation while thoughtfully with feedback. Phillip Wood provided detailed code review while maintaining reservations about the fundamental design choice. Johannes Schindelin adds implementation-focused feedback. D. Ben Knoble's UX considerations about undo safety remain relevant. Junio C Hamano's maintainer perspective introduces a significant new dimension by questioning the core approach and suggesting alternative solutions, potentially redirecting the thread's focus.

Context: Part of ongoing `git add -p`/interactive patch machinery improvements, building on Phillip Wood's sequencer work. Junio's intervention elevates the discussion from implementation details to design philosophy, suggesting the solution may lie outside `git add -p` entirely. The thread now balances between addressing specific technical feedback on the current patch and considering more radical alternatives to solve the underlying workflow need. Reviews from multiple domain experts (Wood, Schindelin, Knoble, Hamano) surface implementation details, UX concerns, and now fundamental design questions, with particular attention to command boundaries and workflow safety.