This is a refactoring patch from Adrian Ratiu, part of his v3 series modernizing Git's hook configuration system. The patch moves the `unsorted_string_list_remove()` utility function from `hook.c` to the more appropriate `string-list.[ch]` files, making it available for broader use.

The changes include:
1. Relocating the function to the string-list module where it logically belongs
2. Adding documentation comments in the header
3. Making the function more generic by adding a `free_util` parameter to match similar string-list APIs
4. Updating all existing callers in `hook.c` to use the new parameter

This is a clean, mechanical change that improves code organization without altering behavior. The patch has already been reviewed and merged to 'next' as part of the larger hook configuration series. It demonstrates good attention to API consistency by aligning the function's signature with similar utilities in the string-list module.