Here's a human-readable summary of the thread:

The discussion centers on Kristoffer Haugsbakk's proposal for a new `git-format-rev` command to handle revision formatting, which he suggests implementing as a standalone builtin rather than extending the existing `git name-rev` command. This follows an earlier proposal to add formatting capability to `name-rev` that was in the queue for consideration.

Junio Hamano has raised an important design question about whether this functionality truly needs a separate command, suggesting that `git log --format` might already cover the same use cases. His intervention signals that the maintainer sees potential redundancy with existing features and wants to carefully consider whether adding a new command is justified. This has sparked a broader conversation about Git's design philosophy - specifically when it's appropriate to create new commands versus extending existing ones.

The outcome will have documentation implications, as Kristoffer (known for his documentation work) would either need to create a new man page for `git-format-rev` or update existing documentation for `git log` or `git name-rev`. The thread is currently exploring these architectural tradeoffs, with Junio's initial skepticism about command proliferation setting the tone for further discussion.