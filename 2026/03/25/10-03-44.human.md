This is a review comment on a patch adding graph column limiting functionality to `git log --graph`. Johannes Sixt points out a small but important consistency improvement for the error message in the `--graph-lane-limit` implementation.

The patch (part of Pablo Sabater's GSoC project) adds validation to ensure `--graph-lane-limit` is only used with `--graph`. Johannes notes that the new error message's format string should match the existing one below it ("the option '%s' requires '%s'") for translation consistency. This is a minor but thoughtful suggestion that would help translators by keeping message patterns uniform.

The comment doesn't question the overall approach - the patch series has already been through several iterations and the core design is settled. This is purely a polish suggestion for the implementation details. The thread context shows the series is nearing completion, needing only some structural reorganization and this small wording tweak before final review.