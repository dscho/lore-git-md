Thread identity: Refactoring patch series (2/2) from [author] improving help text handling for `git repo` subcommands (info and structure).

Core problem or goal: Complete the refactoring started in patch 1/2 by making each subcommand show only its own usage information when invoked with `-h`, rather than the full command list. This builds on the macro extraction work while introducing the intended behavior change.

Current status: Second and final patch in series. Introduces separate usage arrays for each subcommand that reference the REPO_INFO_USAGE/REPO_STRUCTURE_USAGE macros from patch 1/2, updating parse_options() calls accordingly. Includes tests verifying scoped help output.

Key technical details: Changes limited to builtin/repo.c and new tests in t/tXXXX-repo-help.sh. Each subcommand now uses its own usage array containing only relevant help text. Tests confirm `info -h` shows only info help and `structure -h` shows only structure help, using standard Git test patterns (grep checks on output). Maintains coding style and follows through on preparatory work from first patch.

Participant positions: Still no reviews or discussion (second patch submission). Implementation appears clean and focused, with thorough test coverage for the new behavior. Series appears complete and ready for review.