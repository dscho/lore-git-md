Thread identity: Refactoring patch series (2/2) from Mahi Kassa improving help text handling for `git repo` subcommands (info and structure), with maintainer ack from Junio C Hamano.

Core problem or goal: Complete the refactoring started in patch 1/2 by making each subcommand show only its own usage information when invoked with `-h`, building on macro extraction work while introducing the intended behavior change. Junio has reviewed and approved both patches in the series.

Current status: Both patches approved by Junio. The second patch (2/2) implements subcommand-specific help output as planned, with Junio confirming the shared `repo_usage` array remains properly used for top-level `git repo -h` output. One minor caveat noted about `-h` exit code behavior (currently tested as failing but potentially changing to success in future), deemed acceptable for now given existing test patterns.

Key technical details: Changes limited to builtin/repo.c and new tests in t/tXXXX-repo-help.sh. Patch 1/2 extracted help text into REPO_INFO_USAGE/REPO_STRUCTURE_USAGE macros (acknowledged by Junio). Patch 2/2 introduces separate usage arrays for each subcommand referencing these macros, updating parse_options() calls. Tests confirm `info -h` shows only info help and `structure -h` shows only structure help using grep checks, with thorough coverage of the scoped help output.

Participant positions: Junio C Hamano has reviewed and approved both patches, noting the macro syntax is correct in patch 1/2 and the behavior change makes sense in patch 2/2. Mahi Kassa's implementation is clean and focused, with maintainer approval for merging. The only open consideration is a potential future change to `-h` exit code behavior, but this doesn't block current acceptance.

Related work: The series follows Git's standard pattern for command-line interface improvements, with macro extraction preceding behavior changes, and maintains coding style as confirmed by the maintainer. Builds on established parse_options() patterns while improving subcommand help specificity.