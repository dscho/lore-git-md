This is a continuation of the test infrastructure discussion about error detection methods in Git's test suite. Jeff King (Peff) responds to Junio C Hamano's progress report on making test scripts compatible with `set -e` (exit on error), expressing ongoing concerns about subtle behavioral quirks in shell scripting that could lead to false positives or negatives.

Peff provides a concrete example demonstrating how `set -e` behavior can change unexpectedly depending on whether a function call is part of an &&-chain, potentially causing tests to fail in ways that would be difficult to diagnose. While acknowledging that Junio has made significant progress (80-90% of test scripts now work with `set -e`), Peff remains wary of edge cases that might surface later when unrelated code changes.

The exchange highlights the technical tradeoffs between Junio's `set -e` approach (which has already shown practical success) and Peff's proposed stderr monitoring alternative (which might avoid shell-specific behaviors). The discussion remains constructive, with Peff conceding that problems might be rare enough to be manageable, but maintaining his position that the approach carries inherent risks worth considering. This continues the thread's pattern of rigorous technical evaluation of infrastructure improvements.