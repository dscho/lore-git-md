This is a final polish for the `git replay --revert` feature series, addressing two minor issues in preparation for merging. The patch author Siddharth Asthana confirms they will fix both in v6:

1. A coding style violation where a variable declaration appears after other statements in `builtin/replay.c`, triggering a `-Werror=declaration-after-statement` compiler warning. This was already identified in the parent email.

2. A documentation gap where the `<revision-range>` explanation in `git-replay.txt` mentions `--advance` mode but should also reference `--revert` mode for completeness. Reviewer Toon Claes considered this minor enough to defer, but the author chooses to address it now.

The changes are purely mechanical - the core functionality remains unchanged from the well-reviewed v5 series that introduced revert capability to `git replay`. The feature has already undergone extensive review from Phillip Wood, Patrick Steinhardt, and Junio Hamano across five iterations, with consensus on the technical approach. These final tweaks address the last remaining nits before merging.