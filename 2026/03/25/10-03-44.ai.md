This is a follow-up review comment on the graph column limiting feature (part of the ongoing `--graph-lane-limit` effort). Johannes Sixt points out a minor consistency issue in the error message formatting between the new `--graph-lane-limit` validation and the existing `--grep-reflog` check in `setup_revisions()`. 

The technical observation is straightforward: the two error messages use slightly different phrasing ("requires" vs "cannot be used together") for similar validation cases. While functionally equivalent, standardizing on one phrasing would help translators by reducing the number of similar-but-different strings they need to handle. This matches the project's general preference for consistency in user-facing messages.

The comment doesn't require any code changes beyond potentially adjusting the new error message to match the existing pattern. It's a small polish item that the author can address when preparing the next version of the patch series. The feedback doesn't affect the core functionality or design of the graph limiting feature, which remains on track with all major technical points already settled in prior rounds.