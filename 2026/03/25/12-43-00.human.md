This is a follow-up in the `git replay` series discussion, specifically addressing the test style debate from earlier in the thread. Toon Claes responds to Junio C Hamano's input about whether to standardize test syntax for command-line arguments.

The key development is that Toon has decided to drop the patch that would have enforced consistent `--option=value` syntax in the test suite. This comes after Junio expressed skepticism about the value of such standardization, suggesting that a mix of styles might better reflect real-world usage and exercise more code paths.

The decision resolves one of the open questions in this thread, leaving the test suite with its current variety of option formats. The remaining technical discussion about the `--ref` implementation (particularly around `--contained` behavior and multi-branch scenarios) is still pending, but this email closes the stylistic debate that had emerged around test formatting conventions.

The exchange demonstrates how maintainer input can quickly shape direction on seemingly minor style questions, even when there are reasonable arguments on multiple sides. With this settled, the focus can now return to the substantive functionality changes in the rest of the series.