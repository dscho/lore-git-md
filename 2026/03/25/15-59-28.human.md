This is a feature patch series for the `git replay` plumbing command, introducing a new `--ref` option to give users precise control over which reference gets updated during replay operations. The series comes from Toon Claes and builds on Siddharth Asthana's recent work adding `--revert` to `git replay`.

The cover letter explains that while subcommands were previously considered, the current approach maintains the existing command structure while adding the useful `--ref` option. This addresses a specific need expressed in earlier discussion. The series has evolved through review feedback, with v2 dropping a test-only patch and improving documentation consistency.

Key changes in v2 include:
- Explicitly marking options as non-negatable
- Standardizing documentation to use "stuck form" (`--option=value`)
- Adding validation to ensure single-tip revision ranges when using `--ref` with `--onto`
- Improved error messages and test coverage

The implementation touches replay code, documentation, and tests, with careful attention to backward compatibility. The series appears well-structured and addresses a clear use case for plumbing users who need more control over ref updates. The technical discussion shows active engagement with reviewers to resolve edge cases around multi-branch scenarios and option interactions.

For future summarizers: The core addition is the `--ref` option that bypasses the default ref update behavior, allowing arbitrary refs to be specified. The implementation enforces validation rules and documents use cases while maintaining existing functionality when the option isn't used. Tests verify both the new feature and edge cases around revision range handling.