Test infrastructure patch with an alternative approach to handling arithmetic in tests. Patrick Steinhardt responds to Junio C Hamano's patch that made the bisect test work with "set -e" by checking `expr` exit codes. Patrick suggests using shell arithmetic (`$((...))`) instead, which avoids the need for explicit exit code handling since shell arithmetic doesn't fail on zero/null results.

The proposed change rewrites the arithmetic operations in t6002-rev-list-bisect.sh to use shell arithmetic rather than `expr`, simplifying the logic by removing the need for exit code checks. The patch also reformats the test expectation for better readability. This appears to be a cleaner solution that maintains the same test semantics while better aligning with the goal of making tests work under "set -e".

This is part of the ongoing test modernization effort to make the test suite more robust by enabling stricter error checking. Patrick's alternative approach demonstrates how different contributors may propose solutions to the same problem, with his version being more idiomatic for shell scripting while achieving the same end result.