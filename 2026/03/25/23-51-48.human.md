This is a memory leak fix in the pack-objects subsystem, part of Taylor Blau's v2 series addressing geometric repacking edge cases. The patch plugs a potential (though not currently exercised) leak in `read_stdin_packs()` by properly releasing a `rev_info` struct that was allocated but not freed. 

The change is straightforward - adding a single `release_revisions()` call - and comes with a clear commit message explaining that while the leak isn't currently triggered in practice (tests pass cleanly even without this fix), it's good hygiene to clean up properly. The patch is well-contained, touching only pack-objects.c, and follows the pattern established by other revision-walking code in Git.

As the first patch in a series that has already undergone review (with all feedback incorporated in v2), this appears ready for merging. It's a small but correct improvement that prevents future issues rather than fixing an immediate problem.