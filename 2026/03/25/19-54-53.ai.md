This is a bugfix patch from Adrian Ratiu that properly initializes a strbuf in the receive-pack hook infrastructure. While part of his larger hook configuration series, this patch stands alone as a straightforward cleanup.

The issue fixes an uninitialized strbuf in `struct receive_hook_feed_state` that serves as a template for hook processes. Though the NULL pointer wasn't causing problems in practice (since child processes properly initialize their own copies), the patch prevents potential future issues if the template state were accidentally used. The change is minimal - just adding `strbuf_init()` to ensure proper initialization.

Patrick Steinhardt identified the oversight, and Adrian's fix follows Git's defensive programming practices. The patch touches only `builtin/receive-pack.c` with a one-line addition. As a well-contained fix to an existing merged series, this exemplifies Git's post-merge refinement process where small improvements continue even after initial integration.