This is a refactoring patch that extracts usage strings into shared macros in `builtin/repo.c`. The change moves the command-line help text for `git repo info` and `git repo structure` subcommands into `REPO_INFO_USAGE` and `REPO_STRUCTURE_USAGE` macros respectively, then references these macros in the main `repo_usage` array.

The patch is part of a 2-patch series (position 1/2) and is explicitly marked as preparatory work for future changes to subcommand-specific help output. The author states this is a refactoring with no behavior change - it just makes the usage strings reusable. The implementation looks straightforward, moving the strings while preserving all existing functionality.

The changes are limited to `builtin/repo.c` and focus on code organization rather than adding features or fixing bugs. This appears to be a clean, focused refactoring that follows Git's coding style. The commit message clearly explains the motivation and scope of the change.