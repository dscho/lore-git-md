Mahi Kassa has sent a 2-part series refactoring how `git repo` subcommands display their help text. The first patch extracted the usage strings for `git repo info` and `git repo structure` into shared macros, which Junio C Hamano has now reviewed and approved with a brief ack noting the comma placement looks correct. 

The second patch builds on this by making each subcommand show only its own help text when invoked with `-h`, rather than the full command list. The implementation updates the parse_options() calls in builtin/repo.c to use subcommand-specific usage arrays referencing the new macros, with tests verifying the scoped help output works as intended. 

With Junio's approval of the preparatory refactoring and no outstanding issues raised, this clean series appears ready for merging. The changes are well-contained to the repo command's implementation and test coverage, following through on the goal of improving the help text handling for these subcommands.