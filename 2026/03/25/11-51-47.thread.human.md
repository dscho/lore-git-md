This thread covers a two-patch series from Mahi Kassa improving the help text handling for `git repo` subcommands. The first patch (already reviewed and acked by Junio) extracted common help text into macros, while this second patch completes the work by making each subcommand (`info` and `structure`) show only its own usage information when invoked with `-h`.

Junio has reviewed both patches and approved the changes, noting they appear ready for inclusion. The implementation introduces separate usage arrays for each subcommand while maintaining the shared `repo_usage` array for the top-level command. Tests confirm the scoped help output works as intended - each subcommand's `-h` shows only its relevant help text. Junio did note one minor consideration about the exit code behavior of `-h` (currently tested as failing but potentially changing to success in future), but considers this acceptable for now given existing test patterns.

With the maintainer's approval and thorough test coverage, this cleanly concludes the series by delivering the promised improvement to subcommand help output while following Git's coding standards. The changes are limited to builtin/repo.c and new tests, building on the preparatory refactoring that Junio has already acked. The series appears ready for merging.