This is a build system improvement series from Patrick Steinhardt, combining two infrastructure changes into one 8-patch series. The first half introduces a new `tools/` directory to consolidate build-related scripts and tooling that were previously scattered across the repository, particularly in `contrib/`. The second half adds precompiled header support to the Meson build system, demonstrating a 40% compilation speed improvement.

The v2 iteration makes minor adjustments based on feedback, particularly around the precompiled header implementation. The series is now in good shape technically, with all major concerns addressed and documentation added to explain the PCH design choices. While Junio has expressed some workflow concerns about the Coccinelle relocation, these don't appear to be blocking issues.

The changes are mechanical but wide-ranging, touching multiple build systems (Makefile, CMake, Meson) and requiring updates to CI scripts and documentation. The series shows Patrick's characteristic thoroughness - each commit is cleanly separated, with the first 5 patches handling the directory reorganization and the last 3 implementing the performance optimization. This looks ready for final review and merging.