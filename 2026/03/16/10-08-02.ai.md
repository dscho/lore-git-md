This is a build system patch from Patrick Steinhardt, part of his 8-patch series introducing precompiled header support and reorganizing build tooling. This specific change (7/8) prepares for precompiled headers by splitting compatibility sources into their own static library.

The key technical change moves all platform-specific compatibility sources (Windows, macOS, Cygwin implementations, etc. from `compat/`) into a separate `compat` static library, while keeping the main Git sources in the `git` library. This separation is necessary because some compatibility sources intentionally avoid including `git-compat-util.h`, which would break when precompiled headers implicitly include it for all compilation units.

The patch touches 79 lines in `meson.build`, mostly moving source file references between arrays while maintaining all existing functionality. It preserves the same build configurations and platform-specific logic, just reorganizing where the compatibility implementations live in the build graph. The change is mechanical but strategically important for the upcoming precompiled header support in patch 8/8.

This follows the thread's established pattern of careful build system restructuring to enable performance improvements while maintaining cross-platform compatibility. The change appears well-contained and low-risk, with no behavior changes beyond the internal build organization.