This thread documents a straightforward correction to the Git user manual. The original documentation contained an incorrect example of `git grep` usage, showing the revision specification before the search pattern - an order that would produce an error if actually run. The patch fixes this by swapping the arguments to match Git's actual syntax requirements, where the pattern must precede revision arguments.

The change is small (affecting just one line in `Documentation/user-manual.adoc`) but useful, as it prevents potential confusion for readers trying to follow the manual's examples. The patch appears uncontroversial and correct - it simply aligns the documentation with the command's real behavior. Given the nature of the fix, this thread will likely conclude quickly with the patch being merged without further discussion.