This is a test modernization patch that has been reviewed and accepted by Junio C Hamano for inclusion. The patch updates the pack-refs test script to use Git's standard helper functions (`test_path_is_missing` and `test_path_is_file`) instead of raw shell tests with negation. 

The changes are purely mechanical and focused on test infrastructure consistency - replacing `! test -f` and `! test -e` with the more descriptive `test_path_is_missing`, and `test -f` with `test_path_is_file`. These helpers provide better error messages when tests fail. The patch was suggested in prior review feedback and has now been approved after incorporating that suggestion.

This is a straightforward, uncontroversial change that follows the project's ongoing effort to modernize test scripts. The patch touches only test files with no impact on actual Git behavior. Junio's quick approval indicates this is a well-scoped, low-risk improvement that aligns with the project's test infrastructure goals. The changes are limited to t/pack-refs-tests.sh with 14 lines changed in each direction.