This is a documentation patch fixing a small error in the Quick Reference section of the user manual. The patch corrects the argument order in a `git grep` example, moving the revision specification (`v2.6.15`) to follow the pattern (`"foo()"`) as required by the command's actual syntax.

The change is straightforward and clearly correct - the current documentation shows an invalid invocation that would produce an error if run. The fix aligns the example with Git's actual behavior where the pattern must come before revision arguments. This appears to be a simple oversight in the documentation rather than any deeper issue.

The patch touches only one line in `Documentation/user-manual.adoc` and includes a clear commit message explaining the change. It's a minor but useful correction that will prevent confusion for readers trying to follow the examples.