This is a technical discussion about submodule handling in the ongoing effort to improve conflict resolution during branch switching with autostash behavior. The thread has progressed from an initial proposal for a new `--autostash` flag to a more comprehensive reworking of `checkout -m`'s behavior.

Phillip Wood responds to Junio Hamano's earlier comments about the implementation approach. The key points revolve around two technical questions:

1. **Optimizing the autostash check**: Phillip questions whether the dry-run `unpack_trees()` check is truly necessary, suggesting it might be redundant if the operation could instead stash only when the real `unpack_trees()` fails. However, he acknowledges this isn't straightforward since `unpack_trees()` can fail for multiple reasons, not all related to local changes.

2. **Submodule handling**: Phillip had previously raised concerns about submodule support, but now clarifies his understanding after testing. He notes that `git checkout -m --recurse-submodules` currently discards uncommitted submodule changes rather than merging them, which differs from his initial assumption. This realization may simplify the implementation since the new autostash approach wouldn't need to handle submodules differently from the current behavior.

Junio had previously expressed strong opposition to expanding `git stash` to handle submodules unless absolutely necessary. Phillip's latest email suggests this may not be needed after all, as the existing submodule behavior during checkout is more destructive than he originally thought.

The discussion represents the final technical hurdles for the patch series, with the submodule concern appearing less problematic than initially believed. The exchange shows the contributors converging on an implementation that maintains current submodule behavior while improving the core checkout functionality.