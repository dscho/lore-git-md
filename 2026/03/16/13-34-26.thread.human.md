Here's a human-readable summary of the thread so far:

Pablo Sabater has proposed a new `--graph-max` option for Git's graph output as part of Google Summer of Code. The feature would limit the number of columns shown in graph visualizations, replacing excess columns with a single '.' character while always preserving the commit marker ('*'). 

The initial RFC patch implements this by adding a `graph_max_columns` field to `rev_info` and modifying the graph rendering logic in graph.c. It includes basic test cases demonstrating 2- and 3-column limits. The author has identified several open design questions about the interface and behavior, including whether the option should imply `--graph`, what character to use for truncation, and how to handle merge scenarios with truncated columns.

This appears to be a promising first attempt at addressing a longstanding TODO in the codebase, though the implementation will need careful review of edge cases in complex graph scenarios. The test coverage provides a good starting point but will likely need expansion as the design evolves. The thread is currently in the early RFC stage, with the author seeking feedback on the overall approach before refining the implementation.