This is a technical discussion about submodule handling in the ongoing effort to improve conflict resolution during branch switching with autostash behavior. The thread has progressed from an initial proposal for a new `--autostash` flag to a more comprehensive reworking of `checkout -m`'s behavior, with Junio Hamano now weighing in on Phillip Wood's concerns about submodule support.

Phillip Wood follows up on Junio's earlier response about the implementation approach, clarifying his suggestion about optimizing the autostash behavior. He questions whether the dry-run check in `unpack_trees()` is truly necessary overhead, proposing instead to create the stash only if the actual `unpack_trees()` operation fails. However, he acknowledges this may not work cleanly since `unpack_trees()` can fail for multiple reasons beyond just local changes.

The more substantive discussion revolves around submodule handling. Phillip had previously raised concerns that replacing `checkout -m`'s three-way merge with autostash behavior would break submodule updates, since `git stash` doesn't support `--recurse-submodules`. After testing, Phillip corrects his earlier assumption - he finds that `checkout -m --recurse-submodules` currently discards uncommitted submodule changes rather than merging them, making the autostash approach potentially no worse than the current behavior.

Junio maintains his position against expanding `git stash` to handle submodules unless absolutely necessary, suggesting submodule checkouts could handle their own stashing independently. This exchange represents the final technical hurdle for the patch series, with the submodule question being the last outstanding issue before the improved conflict resolution can be merged. The discussion shows both contributors converging on an approach that maintains functionality without overcomplicating the stash command.