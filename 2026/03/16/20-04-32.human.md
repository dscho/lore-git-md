This is a final technical clarification in the ongoing effort to improve conflict resolution during branch switching with autostash behavior. Junio Hamano responds to Phillip Wood's suggestion about optimizing the autostash implementation by avoiding a redundant dry-run check in `unpack_trees()`. 

Junio agrees with Phillip's proposal to skip the dry-run and instead create the stash only if the actual `unpack_trees()` operation fails, noting this approach makes sense since `unpack_trees()` leaves the working tree untouched on failure. This addresses the last remaining performance concern about the implementation.

The discussion also confirms that the current `checkout -m --recurse-submodules` behavior (which discards uncommitted submodule changes rather than merging them) will remain unchanged by the autostash improvements. This aligns with Junio's earlier position against expanding `git stash` to handle submodules unless absolutely necessary.

With these clarifications, the technical path forward appears clear, leaving only minor documentation polishing as the final step before this feature can be merged. The patch series has successfully addressed all substantive feedback while maintaining backward compatibility and existing workflows.