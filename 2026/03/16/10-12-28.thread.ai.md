Thread identity: User support question about Git for Windows functionality, specifically asking about a non-existent top-level `--format` option across versions 2.51.2 to 2.53.0.

Core problem: User misunderstands Git's command structure, confusing subcommand-specific `--format` parameters (like those in `git log` or `git for-each-ref`) with a top-level Git option. The version difference is irrelevant since no such top-level flag exists in any version.

Current status: No patch or change proposed - this is a misdirected support question. The thread contains only this initial inquiry with no replies yet.

Key technical details: The `--format` parameter exists only for specific subcommands, documented in their respective man pages. Git's top-level `--help` output shows common options but doesn't imply they work at the top level.

Open questions: None - this is a support question that would typically be answered by redirecting to documentation or appropriate support channels.

Participant positions: Single user asking the question, no responses yet.

Related work: None - this is an isolated support inquiry unrelated to ongoing development efforts. The question specifically mentions Git for Windows but doesn't pertain to its development.