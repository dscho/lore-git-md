This is a maintainer response in the ongoing thread about improving `git checkout -m` with autostash behavior. Junio C Hamano acknowledges Phillip Wood's suggestion to optimize the autostash implementation by skipping the initial dry-run check in `unpack_trees()`. The proposed approach would attempt the checkout first, only creating a stash if it fails due to local changes, which Junio agrees makes technical sense.

The discussion also confirms Phillip's testing showing that `checkout -m --recurse-submodules` currently discards uncommitted submodule changes rather than merging them, meaning the autostash approach wouldn't introduce new submodule handling regressions. This aligns with Junio's earlier position against expanding `git stash` to support submodules unless absolutely necessary.

The exchange represents the final technical resolution for this patch series, with all major concerns now addressed. The implementation will proceed with the dry-run optimization and maintain the existing submodule behavior, leaving only minor documentation polishing as the remaining task before the improved conflict resolution can be merged.