This is a refactoring patch in the ongoing `git replay --revert` feature series, focusing on consolidating revert message formatting logic. Toon Claes provides a minor review comment on the patch from Siddharth Asthana that extracts message generation into a shared `sequencer_format_revert_message()` function.

The patch moves revert message formatting (including "Revert"/"Reapply" prefixes and commit references) from `do_pick_commit()` into a reusable helper. Junio Hamano had previously approved the technical approach, noting it makes the code clearer by having the helper do more work and by simplifying parameter passing (using explicit repository and boolean flag rather than full replay_opts).

Toon suggests tightening up the function's documentation comment to be more concise while still covering the key behaviors. This is a polish suggestion rather than a substantive change - the core implementation is already well-reviewed and approved by both Junio and Phillip Wood. The discussion shows the feature is in its final stages, with only minor documentation tweaks remaining before merging.

This represents the last technical piece needed for the revert functionality in `git replay`, following extensive discussion about commit ordering, empty commit handling, and message formatting. The higher-level interface design questions (subcommands vs flags) are being deferred to avoid scope creep on this already well-reviewed series.