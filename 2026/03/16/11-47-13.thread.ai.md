thread identity: GSoC proposal for extending `git repo info` with path metadata, from Jialong Wang. thread type: feature proposal for a new command.

core problem/goal: improve the new `git repo` command by adding structured path information to `git repo info`, replacing current need to use multiple `git rev-parse` calls (`--git-dir`, `--show-toplevel`, etc.). aims to provide more maintainable and user-friendly interface for repository path queries.

current status: proposal stage. no implementation yet. author has done preparatory work through microproject engagement with community. command is new with room for careful design of output format and behavior.

key technical details: would extend `repo-info.c` to expose paths like `git-dir`, `common-dir`, `toplevel`, and `superproject-working-tree`. design questions around relative vs absolute paths need resolution. implementation must maintain compatibility with existing `rev-parse` behavior while adding structure.

open questions: exact output format design, which paths to include initially, handling of relative/absolute paths, whether to include additional metadata beyond core paths. needs community discussion to converge on approach.

participant positions: just initial proposal from Jialong Wang at this point. will need feedback from command's original authors and path-handling experts like René Scharfe, Johannes Schindelin.

related work: builds on original `git repo` command work. must coordinate with any parallel efforts touching repository metadata or path handling. references prior mailing list discussions about repository introspection.