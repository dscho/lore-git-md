Junio C Hamano follows up on Jeff King's memory leak fix with a small but necessary correction. The original patch switched `repo_config_get_string()` to `repo_config_get_string_tmp()` to avoid allocations, but missed that the latter expects a `const char **` parameter rather than `char **`. Junio provides a one-line fix to declare `value` as `const char *` instead, resolving the compiler errors.

This is a straightforward build fix for an already-approved change. The interaction shows the maintainer's attention to detail - catching a type mismatch that slipped through initial review. With this adjustment, both the zombie process cleanup and the incidental memory leak fix are ready to merge. The thread demonstrates how even small, well-contained patches can surface hidden issues when combined with rigorous testing.