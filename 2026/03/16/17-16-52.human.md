This is a documentation patch that has already been reviewed and accepted by the Git maintainer. Junio C Hamano has queued Guillaume Jacob's straightforward fix for an incorrect `git grep` argument order in the user manual's Quick Reference section.

The patch corrects a simple but potentially confusing documentation error where an example showed the revision (`v2.6.15`) before the search pattern (`"foo()"`), which would produce an error if run. The fix swaps these to match Git's actual syntax requirements where the pattern must precede revision arguments.

Junio notes the patch is well-formed - it has a proper title, correct sign-off, and makes only the necessary one-line change to `Documentation/user-manual.adoc`. This is exactly the kind of small but useful documentation correction that gets quickly approved, especially when the fix is clearly correct and uncontroversial. The patch revision (v1) being accepted on first submission suggests the change was obviously right and needed no further iteration.