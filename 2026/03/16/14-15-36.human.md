This is a documentation patch fixing a small error in the Quick Reference section of the user manual. The patch corrects the argument order in a `git grep` example, moving the revision specification (`v2.6.15`) to come after the search pattern (`"foo()"`), which is the correct syntax.

The change is straightforward and clearly motivated - the commit message explains that the arguments were in the wrong order in the original example. The fix aligns with how `git grep` is actually used, where the pattern comes before the revision. This is a minor but useful correction that will help users who refer to the manual for command syntax.

The patch is well-formed with a proper commit message, sign-off, and clean diff. It touches only the intended line in `Documentation/user-manual.adoc`. No review comments or discussion appear in the thread context, suggesting this is a simple uncontroversial fix that will likely be merged without further changes.