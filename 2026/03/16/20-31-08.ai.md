This is a follow-up from Junio C Hamano addressing a compiler error in Jeff King's memory leak fix for transport color configuration. The issue arises from a type mismatch when switching to `repo_config_get_string_tmp()` - the function expects a `const char**` parameter but was being passed a `char**`. Junio provides a straightforward one-line fix to declare the `value` variable as `const char*` instead of `char*`, which resolves the compilation errors while maintaining the intended memory management behavior.

The patch is minimal (just changing the variable type) and clearly correct, as it aligns with the contract of `repo_config_get_string_tmp()`. This is effectively a mechanical adjustment to Jeff's earlier patch, ensuring it compiles cleanly while preserving the leak-fixing properties. The thread shows this as the final polish before merging both the zombie process cleanup and this incidental memory leak fix.