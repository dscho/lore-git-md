This is a follow-up in a user support thread that was mistakenly sent to the development mailing list. John Emmas thanks Kristoffer Haugsbakk for clarifying that `--format` is not a top-level Git option but rather a subcommand-specific parameter (like in `git log`).

The thread remains a simple support question that doesn't belong on the development list, though the exchange is now concluded. John acknowledges Kristoffer's explanation about the distinction between output formatting (available in certain subcommands) and source code formatting (which would require third-party tools like `git-clang-format`). No technical changes or patches are involved - this is purely a resolved user education matter.