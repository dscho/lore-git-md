Test fix improving cleanup in ignore-file tests.

This v2 patch from Mirko Faina addresses test isolation issues in `t0008-ignores.sh` that caused the "large exclude file" test to fail when run verbosely. The root cause was leftover files from the preceding symlink test - specifically a `subdir/.gitignore` symlink that triggered an unwanted warning message.

The patch makes two key changes: First, it expands the cleanup in the symlink test to remove all artifacts (`subdir`, `.gitignore`, `err`, `actual`). Second, it adds proactive cleanup at the start of the expensive test to remove any stray `.gitignore` files. The changes follow the consensus from the thread discussion about proper test isolation.

The commit message now better explains the failure scenario and solution, addressing Junio's feedback about scope. The technical approach is sound - these are exactly the sort of test hygiene improvements the project values. The patch remains focused on fixing the immediate issue while demonstrating understanding of Git's test isolation principles. No behavior changes to the actual ignore-file functionality are involved.