This is a technical discussion thread about the ODB (object database) refactoring series that has already been merged. The email is a follow-up to Justin Tobler's question about header inclusion patterns in the codebase.

Patrick Steinhardt responds to Justin's query about whether to explicitly include the new `odb/source.h` header or rely on its transitive inclusion through `odb.h`. Patrick takes the pragmatic approach, suggesting it's fine to just include `odb.h` rather than being explicit about the nested dependency.

This is a minor implementation detail discussion that surfaced after the main series was completed. The thread shows the kind of post-merge refinement that happens as contributors work with the new code. Patrick's response indicates a preference for simplicity in header management, consistent with Git's general coding style.

The exchange doesn't introduce any new technical concerns or require action - it's simply clarifying an implementation choice in code that's already been reviewed and merged as part of the larger ODB abstraction effort.