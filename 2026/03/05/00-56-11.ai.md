Jeff King (Peff) responds to Patrick Steinhardt's patch about adjusting keepalive timing in `upload-pack` when buffering pack data. This is part of the larger performance optimization series to reduce write syscalls during high-concurrency operations.

Peff agrees with the approach but points out two edge cases that are theoretically possible but unlikely in practice: (1) the "PACK" signature arriving in separate writes, and (2) partial line writes when handling packfile URIs. He notes the current solution should handle these cases adequately.

The main technical discussion focuses on the clock management aspect of the patch. Peff identifies a potential issue if system time moves backward (due to clock reset), which could cause the keepalive timeout calculation to wait longer than intended. He considers this an acceptable edge case since the worst outcome would be delayed keepalives until the next data chunk arrives.

Peff also reviews and approves the logic for tracking when data was last sent, including the handling of stderr output and the new `did_send_data` flag for stdout data from `relay_pack_data()`. His review suggests the patch is technically sound while acknowledging some theoretical edge cases that don't significantly impact real-world operation.

This email represents a positive technical review that moves the larger optimization series forward, with only minor theoretical concerns noted but not blocking progress. The discussion remains focused on the specific patch while maintaining context with the broader performance goals of the series.