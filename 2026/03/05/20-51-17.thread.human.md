This thread began with Jacob Keller reporting memory leaks during clone operations when handling pack index mmaps under sanitizers. Jeff King responded with a 5-patch series fixing these leaks in `connected.c`, `fsck.c`, and the build systems. While reviewing, Junio discovered a separate streaming subsystem leak triggered by Patrick Steinhardt's earlier changes when combined with `NO_MMAP` builds, putting the series on hold pending investigation of this new issue.

The discussion has since expanded to include broader mmap handling concerns. Ramsay Jones raised a potential double-munmap issue in the mmap compatibility layer, though this was clarified to be a general observation rather than a specific problem with Jeff's fixes. Junio confirmed understanding of the concern while noting that most munmap calls don't check return values, and that this doesn't affect the technical soundness of the current patch series.

The thread now has three distinct but related aspects: (1) Jeff's technically complete pack index leak fixes, (2) the newly discovered streaming leak under `NO_MMAP` that's blocking merge, and (3) general mmap handling considerations raised by Ramsay. While the core series remains ready, the project is taking time to ensure all mmap-related edge cases are properly addressed before proceeding.