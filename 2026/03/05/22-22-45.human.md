This is a technical discussion about performance optimization approaches in the ongoing `git-upload-pack` lock contention series. brian m. carlson responds to Johannes Sixt's concerns about using `writev()` in the compatibility layer by sharing empirical evidence from their Rust implementation showing measurable performance gains with `writev()` for similar packet-writing patterns.

The exchange represents one side of a three-way debate about how to best reduce write syscalls in high-concurrency scenarios. brian m. carlson advocates for keeping the `writev()` approach despite portability concerns, citing real-world performance improvements they've observed. They acknowledge the compatibility layer may need to relax some POSIX guarantees but argue the tradeoff is worthwhile for the demonstrated speed benefits.

This email doesn't introduce new code but reinforces the technical merits of the `writev()` approach in the broader discussion about buffer management strategies. The thread remains at an impasse between competing implementations that all achieve the same syscall reduction through different means.