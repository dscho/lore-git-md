A refactoring patch for the `git-subtree` contrib script, part of a 3-patch series. This change modifies how `process_subtree_split_trailer()` communicates its result - instead of setting a variable in the caller's scope, it now prints the result and is invoked in a subshell. The commit message explains this makes the control flow clearer by avoiding side effects.

The patch includes a new comment documenting the function's behavior and updates two call sites to capture the output rather than rely on the side effect. The change is mechanical and focused - it doesn't alter the actual logic, just how the result is passed back to callers. This appears to be a straightforward improvement to make the script's behavior more explicit and self-contained.