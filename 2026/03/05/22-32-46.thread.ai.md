RFC series adding remote group support to `git push`, mirroring existing `git fetch` functionality. Authored by Usman Akinyemi. Core idea: enable pushing to all remotes in a configured group (defined via `remotes.<name>` config) with single command like `git push all-remotes`.

Implementation in four parts:
1. [PATCH 1/4] Refactored group-handling code from fetch.c to remote.c for reuse
   - Moved `get_remote_group()`, `add_remote_or_group()`, and `remote_group_data` struct
   - Updated remote.h declarations
   - Purely mechanical code movement with no functional changes
   - Suggested-by: Junio Hamano
   - Maintainer review: Junio confirms naming/scoping appropriate
   - Author removed `UNUSED` macro from headers per feedback

2. [PATCH 2/4] Implements push-to-group functionality
   - Processes each remote in group sequentially
   - Rebuilds refspec lists per-remote to respect `remote.<name>.push`
   - Isolates mirror/force-push flags per remote
   - Accumulates exit codes via bitwise OR (continues after failures)
   - Now unifies single-remote and group handling via remote_group structure
   - Preserves existing single-remote behavior
   - Suggested-by: Junio Hamano
   - Includes test coverage in t5566-push-group.sh (renumbered to t5528 per Junio)
   - Documentation updates for git-push.txt
   - Handles direct path arguments
   - Checks mirror/refspec conflicts per-remote
   - Includes URL/path fallback support
   - Commit message wording improved per Junio's suggestion (added comma after "to a single remote")
   - Fixed type mismatch in loop counter (changed `int i` to `size_t i` per Junio's review)
   - Final polish: fixed accidental newline removal in builtin/push.c

3. [PATCH 3/4] Enhanced documentation and test coverage
   - Clearly documents failure handling behavior (continues after ref rejections, stops on fatal transport errors)
   - Adds explicit test verification of commit propagation and error isolation
   - Uses `size_t` for loop indices to match remote group count type
   - Final documentation polish per Junio's review

4. [PATCH 4/4] Additional refactoring of remote group resolution
   - Further consolidates remote group handling code in remote.c
   - Prepares infrastructure for future parallel push support
   - Maintains strict behavioral equivalence with previous implementation

Key technical details:
- Touches builtin/fetch.c, builtin/push.c, remote.h, remote.c, Documentation/git-push.txt
- Follows same group config syntax as fetch (`remotes.<name>` lists URLs)
- Significant changes to push.c core logic to handle groups
- Current limitations:
  - No parallel push support (unlike fetch.parallel)
  - Future enhancement possibility

Design decisions resolved by maintainer Junio Hamano:
1. `push.default` behavior: all modes (simple, matching, etc) work with groups, each remote follows its own configuration exactly as if pushed individually. Documentation should not over-specify simple mode.
2. `--force-with-lease` semantics: same commit expectation applies uniformly when specified; when unspecified, each remote uses its own remote-tracking branch state.
3. Core principle established that group push should behave identically to sequential individual pushes (`git push G` == `git push r1; git push r2; ...`).
4. Failure handling: continues pushing to remaining remotes after failure, with final exit code being bitwise OR of individual results. Updated per latest discussion to use single failure mode that exactly mimics sequential individual pushes (with ";" rather than "&&" semantics), making behavior more predictable and easier to extend to parallel push support. Author acknowledged this was an oversight in initial design and agreed with maintainer's technical point about importance for future parallelization.

Test improvements from Junio's review:
- Added more direct source-to-remote ref verification
- Using `test_cmp` with explicit expected files for precise assertions
- Removed redundant test cases
- Style fixes (using `test_grep` instead of plain `grep`)
- Added explicit tests for error isolation and commit propagation
- Future-proofed against Git 3.0 default branch name change by adding `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME=default` to test script (caught by linux-breaking-changes CI job)

Implementation status:
- Architectural alignment complete on core behavior
- Unified single/group remote handling implemented
- All key design questions resolved
- Comprehensive test coverage and documentation
- RFC phase complete
- All final polish items addressed:
  - Test script numbering conflict resolved (5566 -> 5528)
  - Documentation precision improvements completed (including clarification of failure handling semantics)
  - Accidental newline removal fixed
  - Type mismatch in loop counter fixed (`int` -> `size_t`)
  - Enhanced documentation of failure handling
  - Added explicit test verification of commit propagation
  - Future-proofed test against Git 3.0 default branch name change
- Parallel push support remains future enhancement

Maintainer feedback has progressed from architectural guidance to final polish (commit message wording, test quality, type safety). The series is functionally complete with all test refinements and minor documentation fixes addressed, including the latest CI-caught issue regarding default branch naming and the updated failure handling semantics. Junio's attention to both technical design and presentation details indicates high confidence in the implementation. Author has demonstrated responsiveness to all feedback across multiple review rounds, including quickly recognizing and addressing the maintainer's point about failure handling being important for future parallel push support. The feature appears ready for merging in the next integration window.