This is a technical discussion about header file organization in the ongoing object database (ODB) abstraction effort. Justin Tobler follows up on Patrick Steinhardt's patch that split `struct odb_source` into a separate header file, asking whether the new header should be included directly or transitively via `odb.h`. 

Patrick suggests keeping things simple by just including `odb.h`, which already pulls in the new header. Justin agrees this pragmatic approach makes sense, though he was initially curious whether the split was meant to make `odb/source.h` an internal implementation detail. The exchange shows good technical alignment between the two contributors on this minor implementation detail of a much larger architectural change.

This is a small but typical example of the kind of precise, focused review discussion that happens around major refactoring efforts like the ODB abstraction. The thread context shows this was part of a well-executed 17-patch series that successfully abstracted Git's object storage layer.