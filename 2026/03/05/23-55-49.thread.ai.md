Thread identity: Performance optimization patch series (v2 3/3) for `git subtree split` in contrib/subtree, addressing shell recursion depth limitations.

Core problem: `git subtree split --rejoin` hits POSIX shell recursion limits (1000 levels on Debian) when processing repositories with thousands of commits, specifically in the `check_parents` function that recursively processes commit history one commit at a time.

Current status: Third version of patch series, with technical solution now implemented. Changes from previous versions not specified (no prior summary provided), but this appears to be the implementation culmination of the thread's discussion.

Key technical details: Modifies recursion pattern in `check_parents` to process entire commit ranges at once when deepening history, rather than recursing per commit. Handles two scenarios needing pre-rejoin history: merge commits referencing old history, and subtree merges of old split history. Includes ASCII-art diagram in commit message illustrating these cases. Tested against aur.git repository demonstrating the issue.

Open questions/loose ends: None mentioned in this patch - appears ready for review. Previous thread discussion may have raised points now addressed in v2.

Participant positions: Only this patch provided - no reviewer feedback shown in summary. Original author is implementing solution to previously identified recursion issue.

Related work: Part of ongoing maintenance/optimization of contrib/subtree. Builds on existing --rejoin functionality.