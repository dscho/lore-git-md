This is a feature patch for `git-send-email`'s SMTP authentication, specifically targeting OAUTHBEARER support. The patch enables passing the SMTP server's hostname and port to Authen::SASL when available, which is supported starting from version 2.2000 of the Perl module.

The v2 patch follows up on Junio's request to document the backward compatibility behavior in the commit message. Aditya Garg confirms that older versions of Authen::SASL will safely ignore these new parameters, similar to how they already handle the `authname` parameter. The implementation remains a minimal two-line change to `git-send-email.perl`, conditionally including the port parameter when defined.

The thread has reached consensus that this change is ready for merging, with all technical concerns addressed. The patch demonstrates careful consideration of backward compatibility and follows the pattern of existing parameter handling in the code. This is a straightforward improvement that enhances OAUTHBEARER support while maintaining compatibility with older Authen::SASL installations.