This is a refactoring patch for the `contrib/subtree` script, part of a series to improve the `git subtree split` command. The patch functionalizes the rev-list traversal used for splitting commits into a new `find_commits_to_split` function.

The main changes are:
1. Extracting the rev-list command into a dedicated function that takes a starting revision and UNREVS (excluded revisions) as parameters
2. Switching from command-line arguments to stdin for passing UNREVS to avoid argument length limits
3. Removing an unnecessary `eval` that was previously used to execute the rev-list command
4. Adding proper documentation for the new function

The patch appears well-contained and focused - it touches only the subtree script and doesn't modify core Git functionality. The commit message clearly explains the motivation (avoiding command-line length limits and cleaning up the code) and the changes match that description. This is part 2 of a 3-part series, though the context doesn't indicate what the overall series aims to accomplish beyond these specific improvements.

The changes look like straightforward code hygiene - no behavior changes are intended or apparent. The new function is properly documented and the implementation seems correct. This appears to be a solid incremental improvement to the subtree script's internals.