Thread identity: Documentation clarification request about `git stash` pathspec behavior, initiated by Aron Sigfridsson.

Core problem: The gitglossary advises checking individual commands for whether pathspecs are interpreted relative to current directory or toplevel, but `git stash` documentation lacks this specification. This creates potential confusion for users about how relative paths in stash operations are resolved.

Current status: Mirko Faina has confirmed through testing that `git stash push` interprets pathspecs relative to current directory (matching `git status` behavior), though this is not explicitly documented. No consensus yet on whether documentation should be updated.

Key technical detail: Pathspec interpretation for `git stash` operations (push/show/apply) is relative to current directory, consistent with `git status` behavior. This matches common pattern but differs from some other Git commands that use toplevel-relative paths.

Open questions: Should `git stash` man page be updated to explicitly document pathspec interpretation rules? More broadly, should Git document these implicit behaviors more consistently across commands?

Participant positions: 
- Aron Sigfridsson raised the initial documentation gap
- Mirko Faina confirmed current behavior through testing and noted it matches `git status`, but observed most Git commands don't document this explicitly
- No other participants have responded yet

Related work: The discussion implicitly references `git status` behavior as a comparison point, and touches on broader documentation consistency questions across Git commands.