Test modernization patch improving cleanup handling in git-svn test.

Siddharth Shrimali sends a small but thoughtful test cleanup patch for the git-svn test suite. The change moves SVN repository setup into a proper `test_expect_success` block and uses `test_when_finished` to ensure cleanup happens even if the test fails. This replaces the previous approach of doing setup outside the test framework and manually cleaning up with `rm -rf`.

The patch is straightforward and follows established test suite conventions. The author thoughtfully asks whether reviewers would prefer combining the SVN setup and Git SVN initialization into a single block, showing awareness that there may be stylistic preferences here. The change touches only test infrastructure - no behavior is altered.

This appears to be a solid, uncontroversial improvement to test hygiene. The use of `test_when_finished` is particularly welcome as it makes the test more robust against failures. The patch is small enough to review quickly but meaningful enough to be worth including in the digest.