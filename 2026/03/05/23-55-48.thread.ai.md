Thread identity: Refactoring of `git subtree split` command in contrib/subtree script, part 2 of a series.

Core problem or goal: Improve the `git subtree` implementation by refactoring the rev-list traversal logic used in `git subtree split` to be more robust and maintainable, specifically addressing potential command-line length limits.

Current status: This is the second patch in a series. The current change extracts the rev-list traversal into a dedicated `find_commits_to_split` function that takes starting revision and UNREVS (excluded revisions) as parameters, using stdin instead of command-line arguments to pass UNREVS to avoid length limits. The change removes an unnecessary `eval` and adds proper function documentation.

Key technical details: Changes are confined to `contrib/subtree` script. The new `find_commits_to_split` function handles the commit discovery logic previously inline in the script. The function accepts parameters via stdin rather than command line to avoid length limitations. Implementation appears to maintain existing behavior while improving code structure.

Open questions and loose ends: None raised in this patch, though being part 2 of 3 suggests there may be follow-up changes coming in the series. The overall goal of the series beyond these specific refactorings isn't stated in this patch.

Participant positions: Only the author (D. Ben Knoble) appears in this patch. No reviews or feedback from others yet.

Related work: This is part of an ongoing effort to maintain and improve the contrib/subtree script, though not tied to any of the major current refactoring efforts in core Git.