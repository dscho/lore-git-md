Patrick Steinhardt has sent a 3-patch series addressing macOS 15 compatibility issues, particularly around iconv behavior in both Makefile and Meson builds. The series builds on a previous Makefile fix (d0cec08d70) by extending the solution to Meson builds and updating GitLab CI to use macOS 15 images. The changes include comprehensive testing of iconv failure modes and implementing consistent workarounds across both build systems.

Justin Tobler has reviewed the first patch, which simplifies the Meson build configuration for detecting BOM handling in iconv. He confirms the changes maintain the same functionality while making the code more straightforward by removing redundant variables and version-dependent typedefs in favor of a simpler void pointer cast approach. His review is positive, noting the simplification achieves the same behavior without the previous complexity.

The series appears technically complete and ready for merging, with no open questions raised. It demonstrates the project's careful approach to platform updates - fixing technical issues before updating CI configurations. This work is part of ongoing Meson build system adoption and CI infrastructure maintenance, particularly for macOS compatibility.