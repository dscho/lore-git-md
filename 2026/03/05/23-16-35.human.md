This is a follow-up discussion in a bugfix thread about memory leaks during clone operations. Jacob Keller confirms Jeff King's earlier analysis and provides additional context about his build configuration that revealed the issue.

The thread centers around memory leaks that occur when handling pack index mmap operations, particularly visible under AddressSanitizer builds. Jeff had previously identified and fixed several leak paths in the `check_connected()` function, and Jacob now verifies that these fixes address his original report. 

Jacob notes he was using meson with `-fsanitize=address` but wasn't aware of the `NO_MMAP` interaction that makes these leaks detectable. He also shares his workaround for leak detection in his development environment (setting `LSAN_OPTIONS=exit_code=0` to avoid test failures from known leaks.

The discussion shows good technical alignment - Jeff's fixes work as intended, and Jacob provides useful feedback about real-world build configurations where these leaks surface. While the leaks are minor in practice (since they occur just before program exit), both contributors agree it's worth fixing them properly for code hygiene and future maintainability. The thread appears to be wrapping up with no outstanding technical questions.