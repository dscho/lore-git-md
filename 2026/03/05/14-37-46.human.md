This is a discussion about the documentation and design of the proposed `pre-add` hook feature, part of an ongoing patch series. Phillip Wood raises several concerns about the current implementation's limitations and potential user experience issues.

The thread has progressed through five iterations of the patch, with all technical concerns from earlier reviews addressed. The current discussion focuses on documentation clarity and design philosophy rather than implementation details. Phillip suggests reorganizing the documentation to match `git-commit`'s hooks section and expresses concerns about the hook's behavior in dry-run and interactive modes.

Key points from this email:
- Phillip questions why `--dry-run` bypasses the hook when it could help users preview potential rejections
- He suggests the hook should also cover changes staged via `git commit` for consistency
- The term "lockfile" for the second index argument is potentially confusing
- There's concern about not enforcing read-only access to the index files
- The documentation should clarify that all staged changes are checked, not just the current add
- Phillip notes users will need to duplicate validation in both `pre-add` and `pre-commit` hooks

This represents the final polishing stage before potential merge, with the discussion now on higher-level design questions rather than technical implementation. The author will need to consider these UX concerns in any future revisions.