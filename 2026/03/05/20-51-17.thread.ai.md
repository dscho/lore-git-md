Here is the updated dense AI thread summary incorporating the new information:

Thread subject: Memory leak in clone operation during pack index mmap handling. Thread type: bug report and fix series. Originating author: Jacob Keller. Current status: Jeff King has identified root causes and submitted a fix series addressing multiple mmap-related leaks.

Core problem: Memory leaks occur during repository operations (notably clone) when handling pack index mmaps, particularly visible under AddressSanitizer. The leaks stem from improper cleanup in error paths when using the `NO_MMAP` compatibility layer that ASan enables, which converts mmaps to heap allocations. The issue affects core packfile handling code during object connectivity verification.

Key technical details: The fix series addresses three specific issues: (1) missing `close_pack()` calls in `check_connected()` error paths, (2) potential double-mapping of revindex files, and (3) makes leak detection more systematic by enabling `NO_MMAP` for all leak-checking builds. The changes touch `check_connected()`, packfile handling, and build configuration. While the leaks were small (27KB in original report) and previously masked in normal operation, they represent legitimate resource management issues in widely-used code paths.

Current status: Jeff King has submitted a complete fix series (4 patches) that addresses the immediate leaks and improves future leak detection. The series has been tested by running the entire test suite with leak-checking enabled.

Open questions: Whether similar leaks exist in other mmap usage patterns. The discussion may expand to consider broader mmap lifecycle management practices.

Participant positions: Jacob Keller originally reported the issue with ASan output. Jeff King (Peff) led the investigation, identified root causes, and provided comprehensive fixes. His analysis confirms these were real leaks that were previously masked in normal operation. The thorough test coverage (full test suite under leak-checking) lends confidence to the changes.

Related work: Recent pack-related changes in `next` branch may provide context. The mmap infrastructure in `compat/mmap.c` has platform-specific resource management requirements. The interaction between ASan, `NO_MMAP`, and leak detection settings made these issues visible. The fixes align with ongoing efforts to improve resource management in core Git operations.