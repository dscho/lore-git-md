Here is the updated dense AI thread summary incorporating the new email:

Thread subject: Memory leak in clone operation during pack index mmap handling. Thread type: bug report and fix series. Originating author: Jacob Keller. Current status: Jeff King's 4-patch series addressing multiple mmap-related leaks is now fully reviewed and ready for merging, with all patches receiving Reviewed-by tags.

Core problem: Memory leaks occur during repository operations (clone, fsck) when handling pack index mmaps, particularly visible under AddressSanitizer. The leaks stem from improper cleanup in error paths when using the `NO_MMAP` compatibility layer that ASan enables, which converts mmaps to heap allocations. The issues affect core packfile handling across multiple operations.

Key technical details: The completed series includes four fixes: (1) refactoring `check_connected()` to delay pack index opening (now reviewed and approved by Jacob Keller), (2) fixing pack index cleanup during clone operations, (3) preventing `.rev` file double-loading in `fsck`, and (4) enabling `NO_MMAP` by default for LSan builds to ensure future leak detection. Changes span `connected.c`, `fsck.c`, packfile handling infrastructure, and build configuration. The final patch modifies build infrastructure to force heap allocation paths when built with leak sanitizer, ensuring consistent leak detection coverage.

Current status: Series is fully reviewed with all patches receiving Reviewed-by tags. Jacob Keller has confirmed the technical approach for the first patch's refactoring of `check_connected()` to delay pack index opening until after `start_command()`. The discussion revealed subtle interactions between build configurations - ASan enables `NO_MMAP` while leak checking typically uses LSan without it, meaning these leaks were previously invisible due to toolchain quirks rather than being truly harmless.

Open questions: None remaining for this series. The discussion points about potential optimizations (like skipping `revlist` for self-contained packs) were explicitly acknowledged as out of scope for this leak-focused series but may be considered in future work.

Participant positions: Jacob Keller originally reported the issue with ASan output, confirmed the analysis, and has now provided Reviewed-by for the first patch. Jeff King (Peff) led the investigation and fixes, demonstrating his characteristic attention to resource lifecycle details across multiple subsystems. His analysis explained subtle interactions between code paths that only manifest under specific build configurations.

Related work: Recent pack-related changes in `next` branch may provide context. The mmap infrastructure in `compat/mmap.c` has platform-specific resource management requirements. The interaction between ASan, `NO_MMAP`, and leak detection settings made these issues visible. The fixes align with ongoing efforts to improve resource management in core Git operations, particularly around packfile access and verification. The build system change ensures future similar issues will be caught earlier in development.