This is a technical discussion about the object database (ODB) abstraction effort, specifically addressing naming conventions and implementation details in patch 3/17 of the series.

Justin Tobler follows up on Patrick Steinhardt's explanation about why an unused `source` field was introduced in the previous patch (to demonstrate usage before refactoring). The main focus is on naming consistency in the ODB API - Justin finds the distinction between `odb_source_new()`/`odb_source_init()` and `odb_source_free()`/`odb_source_release()` potentially confusing, suggesting `odb_base_source_init()` as a clearer alternative for the initialization function.

Patrick defends the current naming, arguing that `odb_source_free()` needs to remain generic (as it will handle all source types) and that `odb_source_init()` logically operates on `struct odb_source` despite being base-specific. While acknowledging the naming challenges, Justin concedes the current names are probably fine given the clarifying comments in the code.

This exchange shows careful attention to API design details from both contributors, with Patrick maintaining the architectural vision while Justin surfaces potential points of confusion for future readers. The discussion remains constructive, with both parties recognizing the inherent difficulty in naming while prioritizing clarity and consistency in the foundational ODB interfaces.