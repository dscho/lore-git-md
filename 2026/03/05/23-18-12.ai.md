This is a review response in an ongoing thread about memory leaks in pack index mmap handling. Jacob Keller is reviewing Jeff King's patch series that fixes several mmap-related leaks during clone and fsck operations.

The specific patch being reviewed (PATCH 1/4) refactors `check_connected()` to delay opening a pack index until just before it's needed, avoiding the need to clean it up in early-exit paths. The change moves the pack opening code after the `start_command()` call for the rev-list process, since by that point the function is committed to running to completion.

Jacob agrees with the approach, noting it's a good low-hanging-fruit improvement. He provides a Reviewed-by tag for the patch. The discussion also touches on potential optimizations (like skipping rev-list entirely for self-contained packs) that were considered but deemed out of scope for this leak-focused series.

The review confirms the technical approach while acknowledging Jeff's analysis of the tradeoffs involved in more extensive refactoring. This represents the final review step before the series can be considered ready for merging, with all technical points now addressed.