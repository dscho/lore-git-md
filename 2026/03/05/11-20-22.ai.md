This is a CI/build system patch that completes the macOS 15 iconv workaround for Meson builds, mirroring the earlier Makefile fix. It's the second patch in a 3-patch series updating GitLab CI to use macOS 15 images while ensuring compatibility with its broken iconv implementation.

The patch adds a specific test case that detects when iconv fails to properly maintain state between calls (a known issue on macOS 15). When detected, it sets the `ICONV_RESTART_RESET` flag to enable the workaround. The test involves converting a string from ISO-2022-JP to UTF-8 in two chunks, verifying the iconv handle maintains proper state between calls.

The implementation follows the same approach as the Makefile fix (d0cec08d70) but adapts it for Meson's build system. The test is quite thorough - it checks for four distinct failure modes: iconv handle creation, first conversion, second conversion, and output correctness. The patch maintains consistency between Makefile and Meson builds, an important consideration as Git transitions toward Meson as a build system.

This appears to be a straightforward technical follow-up to prior work, with no obvious issues or missing pieces. The commit message clearly explains the macOS-specific bug being worked around and how the test detects it. As the second patch in a small series, it logically builds on the first patch's simplification of Meson's iconv configuration while setting up for the third patch's CI image update.