This is a follow-up in the SMTP authentication thread, where Junio confirms the patch's backward compatibility approach is sound. The original patch from Aditya Garg proposed adding host/port parameters to Authen::SASL calls in `git-send-email.perl` to support OAUTHBEARER authentication in newer library versions while maintaining compatibility with older ones.

Junio initially raised concerns about backward compatibility, which Aditya has now addressed by confirming through testing that older Authen::SASL versions safely ignore unrecognized parameters (just as they do with existing unused parameters like `authname`). Junio accepts this explanation and suggests incorporating these details into the commit message for clarity.

The technical change remains minimal - just two lines modifying the SASL authentication hash construction in the Perl implementation. With Junio's concerns resolved and no outstanding questions, this thread appears ready to proceed to merging once the commit message is updated as suggested. The interaction also serves as a gentle reminder about mailing list submission protocols, though no actual rule changes were involved.