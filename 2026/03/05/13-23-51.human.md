This is a technical discussion about error handling in the object database (ODB) refactoring series. The email is part of Patrick Steinhardt's 17-part series that makes Git's object storage backend pluggable, with this specific exchange focusing on patch 10/17 which makes the `read_object_stream()` function pluggable.

Karthik Nayak points out that the implementation loses error code propagation from the underlying storage operations. Patrick responds that the error codes weren't being propagated in the original code either, and argues that returning a simple `-1` is sufficient since the API doesn't specify different error code meanings. He also notes that propagating only some error codes (from loose objects but not packfiles) would expose implementation details.

The discussion shows careful consideration of API design principles - Patrick is maintaining consistency with existing behavior while avoiding exposing internal implementation details through the interface. This follows the series' broader pattern of thoughtful API evolution to support pluggable backends while preserving stable behavior for existing callers.