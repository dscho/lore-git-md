This is a refactoring patch series that introduces a type-safe cleanup API for oidmap structures. The series has been narrowed down to just two patches after maintainer feedback, focusing on converting the rev-list and odb subsystems to use the new `oidmap_clear_with_free()` function.

The key change replaces the old boolean-based cleanup API (which implicitly assumed plain `free()`) with an explicit callback mechanism. This makes memory management more transparent when `oidmap_entry` is embedded in larger structures. The v3 iteration addresses Patrick Steinhardt's request to rename the callback function in the rev-list conversion.

The series shows good technical hygiene - it includes test coverage in `t/unit-tests/u-oidmap.c` and follows the project's pattern of using `container_of` for type safety. Maintainers Junio Hamano and Patrick Steinhardt have already approved the approach, with Junio indicating the remaining patches can proceed without further discussion of the dropped cases (list-objects-filter and sequencer conversions).

This appears ready for merging once the final naming tweak is in place. The changes are focused and well-motivated, addressing a real API limitation while maintaining backward compatibility. The thread shows healthy maintainer engagement and consensus on scope.