This is a refactoring patch targeting the `contrib/subtree` script, part of a 3-patch series. The change modifies how `process_subtree_split_trailer()` communicates its result, moving from setting a variable in the caller's scope to printing the output and capturing it in a subshell.

The patch improves clarity by:
1. Adding documentation to `process_subtree_split_trailer()` explaining its behavior
2. Making the function return its result via stdout rather than side-effecting the caller's `sub` variable
3. Updating both call sites to capture the output in a subshell

The change is mechanical and focused - it doesn't alter the script's functionality, just makes the data flow more explicit. The commit message clearly explains the motivation (avoiding unclear side effects) and the solution (using subshell invocation).

This appears to be a straightforward quality improvement to the script's structure. The patch is small, well-explained, and touches only the intended behavior. Since it's part of a series, we'd expect to see related follow-up changes in the remaining patches.