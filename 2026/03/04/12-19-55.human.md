This is a test improvement patch addressing a subtle issue in the notes merge test script. The patch fixes a pattern where `git rev-parse` failures could be silently ignored when used in command substitutions within test assertions.

Francesco Paparatto, following a suggestion from Junio Hamano, modifies `t/t3310-notes-merge-manual-resolve.sh` to store `rev-parse` outputs in variables before comparison rather than using direct command substitutions. This ensures test failures from `rev-parse` itself will be caught rather than masked by the test assertion.

The changes are mechanical but important - they make the test more robust by properly propagating errors from the plumbing commands being tested. The patch touches 18 test cases, consistently applying the same pattern of extracting rev-parse results into temporary variables before comparison.

The patch appears well-scoped and correctly implements the suggested improvement. Since this is a test infrastructure change rather than a bugfix in core functionality, it's unlikely to be controversial, though reviewers may want to check that the same pattern isn't present in other test scripts.