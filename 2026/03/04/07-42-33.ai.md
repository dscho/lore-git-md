This is a follow-up review comment in the ongoing fsmonitor daemon thread, specifically addressing a memory leak fix in the Linux inotify implementation. Patrick Steinhardt notes that Paul Tarjan's patch correctly fixes a khash memory leak in the client handling code by moving cleanup to a shared label and properly initializing the hash table pointer to NULL.

Patrick observes that while this fix is correct, the code could be further improved by using Git's strmap interface instead of the lower-level khash directly. However, he explicitly marks this as out of scope for the current series, calling it a "#leftoverbit" (a known improvement to be addressed separately). The comment reflects Patrick's characteristic attention to memory management and preference for Git's higher-level abstractions over direct use of underlying data structures.

This exchange shows the patch series is in its final polishing stages, with reviewers now focusing on minor implementation details rather than core functionality. The original memory leak was in the client response path where the khash table tracking shown paths wasn't always properly cleaned up. Paul's fix ensures the cleanup happens consistently while maintaining the existing behavior.