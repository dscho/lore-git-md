This is a follow-up in the thread about tightening `.git` directory validation, now focusing on how to handle Windows-specific behavior where `/dev/null` translates to `nul` and fails `stat()` calls. Junio Hamano responds to Phillip Wood's analysis of the test failures, clarifying the architectural distinction between repository discovery (where strict validation is desirable) and explicit `GIT_DIR` handling (where leniency may be appropriate).

The key insight is that the security-conscious validation changes from earlier in the series need different handling for explicit `GIT_DIR` specifications versus repository discovery walks. On Windows, this manifests when `GIT_DIR=/dev/null` (common in test infrastructure) gets translated to `nul`, triggering the stricter validation. Junio suggests auditing all `read_gitfile_gently(path, NULL)` calls to determine which should remain lenient versus which should enforce strict validation.

The thread remains blocked on resolving this platform-specific edge case while preserving the security benefits of the original patch series. Junio's response provides clear technical direction but underscores the need for careful auditing of the validation code paths to maintain both security and compatibility. This exemplifies the tension between robust error handling and cross-platform behavior in Git's core directory validation logic.