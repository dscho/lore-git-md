This is a technical discussion about the Linux fsmonitor implementation, specifically addressing filesystem detection and mount point handling in the inotify-based backend.

Patrick Steinhardt raises two main points about the Linux fsmonitor path utilities code:

1. **Build system integration**: He notes that `HAVE_LINUX_MAGIC_H` is only configured for CMake and suggests adding support for Makefile (via `config.mak.uname`) and Meson (via `compiler.has_header()`) builds as well.

2. **Filesystem detection logic**: He questions two aspects of the implementation:
   - The `get_fs_typename()` function only lists network/remote filesystems, omitting common local filesystems like ext4. He suggests better documentation and possibly renaming to clarify its purpose.
   - The `find_mount()` function's scanning of `/proc/mounts` seems redundant since `statfs()` already provides filesystem information. He's unclear why the additional mount point matching is needed when the filesystem ID from `statfs` should be sufficient.

These are follow-up technical questions about implementation details rather than fundamental design issues. The discussion appears to be refining the final touches on an otherwise mature patch series that has gone through multiple iterations and extensive review. Patrick's comments suggest looking for potential simplifications and more precise documentation rather than identifying major flaws.