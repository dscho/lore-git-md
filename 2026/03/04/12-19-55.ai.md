Test modernization patch addressing a subtle issue with command substitution in test scripts.

Francesco Paparatto fixes a common pattern in the test suite where `git rev-parse` failures could be silently ignored when used in command substitutions. The patch modifies `t/t3310-notes-merge-manual-resolve.sh` to store `rev-parse` outputs in variables before comparison, ensuring any failures are properly detected. This follows a suggestion from Junio C Hamano.

The changes are mechanical but important for test reliability - each instance where `rev-parse` was used directly in a test comparison is now split into a variable assignment followed by the comparison. The patch touches 18 lines across 10 test cases, all following the same consistent pattern. While not fixing any actual bugs, this improves the test suite's ability to catch problems in the underlying `rev-parse` operations.

The changes are straightforward and unlikely to be controversial, being a clear improvement to test robustness. The patch follows established patterns seen in other test modernization efforts and has the maintainer's implicit approval via the Suggested-by tag.