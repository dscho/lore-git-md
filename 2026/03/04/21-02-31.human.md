Junio C Hamano responds to Michael Montalbo's patch that rejects pickaxe options (-G/-S/--find-object) when combined with `-L`. While acknowledging the patch solves the immediate issue, Junio questions whether the architectural approach is optimal.

The maintainer notes that `-L` has historically not worked well with other diff options (like `--stat`), so rejecting pickaxe options seems reasonable. However, he raises an important consideration: if this rejection is implemented, it might make the first patch in the series (which fixed a crash when these options were combined) unnecessary.

Junio's feedback suggests the series may need reconsideration - either keeping both patches (crash fix + rejection) or finding a more fundamental solution. His comment implies the current approach might be treating symptoms rather than addressing the root cause of the option incompatibility.

This moves the thread into a design discussion phase, where the author will need to decide whether to:
1. Proceed with both patches as-is
2. Drop the first patch if the rejection makes it redundant
3. Explore deeper architectural changes to properly support the option combination

The maintainer's intervention indicates this series may require another revision before being ready for inclusion.