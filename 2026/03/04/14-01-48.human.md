This is a documentation patch in the final stages of review for a series that aligns `git add`'s behavior with other commands when handling submodules configured with `ignore=all`. The series is already in the `next` integration branch, indicating it's technically complete and awaiting final merge.

Kristoffer Haugsbakk points out two minor documentation issues in the patch: a missing space before a parenthesis ("`log` (due") and a typo ("underlaying" should be "underlying"). These are small copyediting fixes to the man page updates that explain how `--force` overrides the `ignore=all` setting for submodules.

The patch is part of a well-reviewed series (now at v4) that introduces consistent handling of ignored submodules across Git commands. The core change requires using `git add --force` to stage submodules marked with `ignore=all`, matching how `git status` and `git diff` already treat them. The series has gone through multiple iterations with thorough review, and Junio Hamano has indicated it appears ready for merging. These final documentation tweaks are the last polish before the feature ships.