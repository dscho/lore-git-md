This is the second patch in a two-part bugfix series addressing issues with `git log -L` (line-log) when combined with pickaxe options (-G`, `-S`, `--find-object`). 

The first patch fixed a crash that occurred when these options were used together with rename detection. This follow-up patch improves the user experience by explicitly rejecting the unsupported combination rather than silently ignoring the pickaxe options. The change adds an error check in `builtin/log.c` and updates the test suite to verify the new behavior, replacing previous tests that expected silent failure.

The patch is well-contained, touching only the necessary files (`builtin/log.c` and `t/t4211-line-log.sh`). The commit message clearly explains the rationale for rejecting the options rather than continuing to silently ignore them, while leaving the door open for future implementation of this feature combination. The test changes are thorough coverage of the new error cases.

This appears to be a clean, focused improvement from a first-time contributor that addresses both the technical issue and the UX problem in a straightforward way. The series looks ready for merging.