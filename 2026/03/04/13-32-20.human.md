This is a bugfix for git-gui that aligns tab display behavior with gitk's longstanding implementation. The patch addresses an inconsistency where diff markers (+/-/space) in the first column would misalign tab stops in git-gui's diff viewer, while gitk has correctly compensated for this since 2007.

The fix is a small two-line change in `diff.tcl` that applies tab size adjustments before inserting diff lines, with different offsets for regular diffs (1) and combined diffs (2). Johannes Sixt has reviewed and improved the commit message, clarifying this as a workflow improvement rather than a strict bugfix. The change preserves all staging functionality while matching gitk's behavior.

The thread shows consensus that the patch is ready to merge, with all technical questions resolved. While the issue existed for nearly two decades without significant complaint, the fix provides a tangible improvement to code review alignment in git-gui. Johannes Sixt's rewritten commit message better explains the context and scope of the change.