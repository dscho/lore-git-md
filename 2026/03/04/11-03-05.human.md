This is a follow-up in the thread about improving error diagnostics for invalid `.git` directory paths, with a focus on resolving Windows-specific issues with `/dev/null`/`nul` handling.

Tian Yuchen confirms understanding of Junio Hamano's architectural clarification that explicit `GIT_DIR` paths (like `GIT_DIR=/dev/null`) should not trigger repository discovery logic and can be treated more leniently than paths found during discovery. The key insight is that when users explicitly pass a `GIT_DIR`, Git should simply report if it's invalid rather than trying to recover or search further.

The next step is an audit of all `read_gitfile_gently(path, NULL)` call sites to determine which should maintain strict validation (for discovery paths) versus which can be more lenient (for explicit paths). This careful auditing is necessary to maintain security while accommodating platform-specific behaviors like Windows' `nul` device file.

The thread shows good progress in resolving the regression while preserving the original security goals. Tian appears to have grasped the architectural distinction and is proceeding methodically to implement the solution. The patch series remains in good hands, though the implementation work will require careful review when submitted.