This is a follow-up review comment in the ongoing fsmonitor Linux inotify support thread. Patrick Steinhardt notes a memory leak fix in the fsmonitor daemon's client handling code, where a khash table wasn't being properly cleaned up in all code paths.

The patch itself (from Paul Tarjan) fixes a memory leak in the `do_handle_client` function by ensuring the `shown` khash table is properly destroyed in the cleanup path. Patrick agrees with the fix but suggests that longer-term, the code could be improved by using Git's `strmap` interface instead of direct khash usage, though he acknowledges this would be out of scope for the current series.

This is a mature patch series (v7) that has already addressed numerous technical concerns around filesystem monitoring on Linux. The thread shows the typical Git development pattern where reviewers like Patrick catch subtle issues even in later iterations, while being mindful of scope boundaries. The exchange demonstrates the project's attention to memory management details while keeping focused improvements separate from broader refactoring work.