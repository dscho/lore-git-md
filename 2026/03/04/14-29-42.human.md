This is a patch series (v7) by Phillip Wood and Li Chen adding `--trailer` support to `git rebase`, now in-process trailer manipulation that avoids forking `git interpret-trailers`. The series is in its final stages, with all core functionality reviewed and only minor implementation details remaining.

The main change in this version is sharing the temporary file creation code between patches, with Phillip providing an updated version that incorporates this and other small fixes. The series refactors trailer handling to be reusable across commands (commit, tag, rebase) while maintaining existing functionality. The rebase implementation specifically:
- Works with the merge backend only (rejects --apply)
- Implies --force-rebase
- Validates trailers early
- Handles all rebase modes
- Preserves trailers through conflicts
- Properly handles fixup/squash
- Adds trailers via the sequencer

Junio C Hamano has weighed in on the final behavioral question regarding trailer ordering, stating that command-line order should dictate placement. The series now awaits final implementation of this decision before merging. Phillip indicates this could be posted as a final v8 if Li is happy with the changes.

The patch quality appears high, with thorough approach to refactoring and careful attention to edge cases. The discussion shows consensus on the technical direction, with only minor file creation refactoring remaining before the series is ready for merge.