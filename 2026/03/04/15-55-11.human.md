This is a technical discussion about header file organization in the ODB (object database) refactoring effort. Justin Tobler is responding to Patrick Steinhardt's patch that splits `struct odb_source` into a separate header file.

Justin agrees with the general approach but raises a question about whether to explicitly include the new `odb/source.h` header everywhere `struct odb_source` is used, or to rely on the transitive inclusion through `odb.h`. This is a minor stylistic point about header hygiene - whether to be explicit about dependencies or accept some implicit inclusion.

The exchange shows the careful attention to code organization details that characterizes this major refactoring series. Justin's comment suggests he may follow a similar pattern for ODB transaction-related headers in future work. The discussion remains focused on implementation details rather than the broader architectural changes, indicating this is a well-understood and uncontroversial part of the larger ODB abstraction effort.