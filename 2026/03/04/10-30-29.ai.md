This is the final follow-up to Harald Nordgren's long-running feature series adding configurable branch comparisons to `git status`. The thread has reached its technical resolution with maintainer approval, and this email addresses Junio Hamano's final review comments.

Harald confirms he will:
1. Fix inconsistent use of `git -C` vs subshells in tests
2. Rename and expand the deduplication test case to be clearer
3. Add explicit tests for when only `@{push}` or `@{upstream}` is used
4. Document the deduplication behavior when the same branch appears multiple times in the config

The implementation is now complete with comprehensive test coverage of various workflow scenarios (ahead/behind states, triangular workflows, remapped refspecs). The only remaining work is minor documentation polish before the feature merges to 'next'. This concludes Harald's multi-year effort to make branch comparisons configurable via `status.compareBranches`.

The technical details are settled - the feature preserves config order when displaying comparisons and handles deduplication sensibly (showing a single "diverged" message when ahead/behind the same branch). The thorough test matrix in t6040-tracking-info.sh ensures robust behavior across edge cases. With Junio's approval and all feedback addressed, this is now just awaiting final documentation tweaks before integration.