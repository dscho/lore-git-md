This is a bugfix for git-gui that aligns tab display behavior with gitk's longstanding implementation. The patch addresses a minor but noticeable inconsistency where diff markers (+/-/space) in git-gui's diff viewer would misalign tabbed text compared to gitk's correct display, particularly on Windows with Tcl/Tk 8.6.

The solution applies Tk-native tab size adjustments with different offsets: 1 column for regular diffs (compensating for the diff marker) and 2 columns for combined diffs. Johannes Sixt has rewritten the commit message to clarify this is an improvement rather than a strict bugfix, noting it builds on existing infrastructure from 2012 that was only handling 3-way diffs. The change preserves all staging functionality while matching gitk's behavior that has been in place since 2007.

The thread shows consensus that this is ready for merging, with Johannes Sixt providing the final polished commit message and Junio Hamano having reviewed the historical context. The patch is small (just 2 lines in diff.tcl) but completes a nearly two-decade-old alignment between git-gui and gitk's tab display behavior.