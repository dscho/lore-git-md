This is a minor style fix in an ongoing refactoring series about oidmap cleanup. The series (now in its second iteration) replaces callback-based memory management with explicit type-safe functions across several Git subsystems.

In this specific patch, Seyi Kuforiji is converting the `missing_objects` map in `builtin/rev-list.c` to use the new `oidmap_clear_with_free()` API. Patrick Steinhardt points out a naming nit: the callback function should follow Git's convention of `structname_free()` rather than the more generic `free_structname()` pattern used here. This is a straightforward documentation/consistency fix that doesn't affect functionality.

The thread context shows this is one of four subsystem conversions in the series, all of which have been reviewed and approved by Junio C Hamano except for one remaining question about the foundational API change. This particular patch was already approved by Junio with the memory leak fix it introduces, so Patrick's comment here is just a final polish on naming before merging.