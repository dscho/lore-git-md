This is a test modernization patch in a 2-part series, following up on a typo fix in the same test file. Patrick Steinhardt reviews Yuvraj Singh Chauhan's conversion of raw shell tests to Git's diagnostic test helpers in `t/t7412-submodule-absorbgitdirs.sh`.

The patch replaces 11 instances of shell conditionals (`test -f`, `test -d`, `! test -e`) with their more informative test framework equivalents (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`). These helpers provide better failure messages than the silent pass/fail of raw shell tests. The changes are purely mechanical - no test logic or submodule behavior is altered.

Patrick confirms the count is accurate and the changes look correct, while noting some trailing whitespace in the commit message that will likely be stripped during application. This continues the project-wide effort to modernize test infrastructure, though it's a small, self-contained improvement rather than part of a larger architectural change. The review is positive with only minor stylistic feedback, suggesting this should be ready for merging.