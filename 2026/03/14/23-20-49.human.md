This is patch 6/7 in Mirko Faina's series improving `git format-patch --commit-list-format`. The patch adds a new "modern" preset format option for commit lists in cover letters, providing a cleaner alternative to the existing "shortlog" format.

The new "modern" format displays commits as "[n/N] subject" (e.g., "[1/3] Fix bug in foo"), which is more compact than the full shortlog output while still showing the patch position in the series. The implementation builds on the refactoring done in previous patches, adding the new preset alongside existing options. Documentation is updated to reflect the new choice, and tests verify both the direct use of the format and its behavior when set via configuration.

The change appears well-contained and thoughtfully integrated with the existing code. The test coverage is thorough, checking both command-line usage and config-based behavior. This continues the series' pattern of incremental improvements to the commit list formatting feature, now giving users a middle ground between the verbose shortlog and custom format strings.