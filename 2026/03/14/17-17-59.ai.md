This is a technical discussion about submodule handling in the ongoing effort to improve conflict resolution during branch switching with autostash behavior. The thread has progressed from an initial proposal for a new `--autostash` flag to a more comprehensive reworking of `checkout -m`'s behavior, with Junio Hamano now weighing in on Phillip Wood's concerns about submodule support.

Junio responds to Phillip's suggestion about optimizing the autostash behavior by questioning whether the proposed approach would actually work as intended. He struggles to parse Phillip's suggestion about using `unpack_trees()` failure as a trigger for stashing, noting that the dry-run check is essential and not truly "extra" overhead.

The more substantive discussion revolves around submodule handling. Phillip had raised concerns that replacing `checkout -m`'s three-way merge with autostash behavior would break submodule updates, since `git stash` doesn't support `--recurse-submodules`. Junio pushes back slightly on this concern, suggesting that submodule checkouts could handle their own stashing independently, without requiring the parent `git stash` command to understand submodules. However, he clearly states a preference against expanding `git stash`'s responsibilities to handle submodules unless absolutely necessary.

This exchange represents the final technical hurdle for the patch series - resolving how to maintain submodule functionality while implementing the new unified conflict resolution strategy. Junio's response indicates he sees a path forward that doesn't require teaching `git stash` about submodules, though the exact implementation details remain to be worked out. The thread shows the maintainer engaging directly with the technical concerns while steering the solution toward minimal complexity.