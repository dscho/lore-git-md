This is a test modernization patch that updates the pack-refs test script to use the standard `test_path_is_missing` helper instead of negated `test_path_is_file` checks. The change improves test failure reporting by using Git's dedicated test helper functions rather than raw shell conditionals.

The patch makes mechanical replacements across seven test assertions in `t/pack-refs-tests.sh`, converting patterns like `! test_path_is_file` to the more idiomatic `test_path_is_missing`. This is part of an ongoing effort to standardize test assertions throughout Git's test suite.

The changes are straightforward and uncontroversial - they don't modify test behavior, just make the test output more consistent and informative when failures occur. The commit message clearly explains the motivation (better failure reporting) and the patch demonstrates the author's familiarity with Git's test conventions.