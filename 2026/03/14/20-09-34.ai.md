This is a brief follow-up in Phillip Wood's series removing `the_repository` usage from worktree functions. The thread has already seen the technical changes reviewed and approved - this email simply acknowledges Junio C Hamano's observation that the header comment for `get_worktree_git_dir()` still incorrectly documents the old behavior (accepting NULL to return the current worktree's git dir) that was removed in the patch. Phillip confirms he'll send an updated version fixing this documentation discrepancy.

The exchange is straightforward - the substantive work happened earlier in the thread when the actual code changes were discussed. This is just tying up the loose end of ensuring the API documentation matches the implementation now that NULL worktree handling has been hardened to a BUG() condition rather than falling back to global state. The change aligns with the broader `the_repository` removal effort that René Scharfe and others have been driving. No new technical discussion emerges here - it's a simple "yes, I'll fix that" acknowledgment of a documentation oversight.