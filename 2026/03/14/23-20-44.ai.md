This is a small bugfix patch targeting the pretty-formatting code in Git. The patch improves error messages when unsupported format placeholders `%(count)` and `%(total)` are used with commands that don't support them.

The changes are straightforward: instead of a generic "this format specifier can't be used" message, the new error messages explicitly name the problematic placeholder ("%(count)" or "%(total)") and state that it's not supported by the command being run. This makes it clearer to users exactly what went wrong when they use these placeholders with incompatible commands.

The patch is part of a 7-patch series (position 1/7), though the cover letter wasn't provided in this input. The changes are minimal (just 2 lines modified) and focused, with no apparent controversy or complexity. The commit message is brief but adequately explains the purpose. This appears to be a simple quality-of-life improvement for error reporting.