This is a refactoring patch that renames the `--cover-letter-format` option in `git format-patch` to `--commit-list-format`. The change aligns the option name with its configuration variable (`format.commitListFormat`) and improves shell completion by making `--cover-<TAB>` unambiguously expand to `--cover-letter`.

The patch makes two key behavioral changes:
1. The renamed option now implies `--cover-letter` unless explicitly disabled with `--no-cover-letter`
2. The documentation is updated to clarify that the format applies to the commit list in the cover letter

The changes are well-tested with updates to both the test suite and documentation. The patch is part of a larger series (3/7) but stands alone cleanly - the commit message doesn't reference other patches in the series. The implementation looks straightforward, modifying the option parsing in `builtin/log.c` while maintaining backward compatibility through the config variable name.

For future AI reference: This touches the format-patch command's cover letter generation, specifically how the commit list is formatted. Key terms are `--commit-list-format`, `--cover-letter`, and the config `format.commitListFormat`. The tests verify both the new option name and its interaction with cover letter generation.