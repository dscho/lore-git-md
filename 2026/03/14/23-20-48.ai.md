This is patch 5/7 in Mirko Faina's series improving `git format-patch --commit-list-format`. The patch focuses on improving readability of long commit message lines in the generated cover letter.

The change modifies `generate_commit_list_cover()` to wrap overly long commit message lines using Git's existing text-wrapping functionality (`strbuf_add_wrapped_text`). The implementation adds a new `wrapped_line` strbuf alongside the existing `commit_line` buffer, applies the wrapping with standard mail formatting width (MAIL_DEFAULT_WRAP), and ensures proper cleanup of both buffers.

The patch is a straightforward enhancement that builds on the series' earlier refactoring work. It touches only `builtin/log.c`, adding UTF-8 handling and the wrapping logic while maintaining the existing output structure. The change appears well-contained and follows Git's established patterns for text formatting.

This is a technical improvement that addresses a real usability issue (long unwrapped lines in cover letters) without introducing new configuration or user-facing options. It fits naturally into the series' goal of polishing the commit list formatting feature.