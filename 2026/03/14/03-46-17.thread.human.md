The thread concerns a test modernization patch that updates `t/pack-refs-tests.sh` to use `test_path_is_missing` instead of negated file existence tests. While the technical change itself is correct and aligns with current test suite conventions, Junio Hamano pointed out a submission workflow issue. The patch incorrectly assumes an intermediate state (where `! test_path_is_file` was used) that doesn't exist in the main repository, rather than directly updating the original `! test -f` checks. 

Junio explained this violates Git's patch submission norms, where contributors should submit clean series representing the desired end state rather than documenting their local development missteps. He referenced documentation on proper submission practices while affirming the technical merit of using `test_path_is_missing`. The patch author needs to resubmit a single patch going directly from the original code to the modernized version. This serves as both a rejection of the current submission and a clear example of Git's expectations for patch series structure.