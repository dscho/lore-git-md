This is a test modernization patch that updates the pack-refs test script to use the preferred `test_path_is_missing` helper instead of negated `test_path_is_file` checks. The change is part of an ongoing effort to standardize test assertions across Git's test suite for better error reporting.

The patch makes mechanical replacements in `t/pack-refs-tests.sh`, converting seven instances where tests were checking for the absence of files using `! test_path_is_file` to the more direct `test_path_is_missing` form. All changes are in the pack-refs test file and maintain the exact same test semantics - they're just using the more idiomatic assertion style.

The commit message is straightforward and correctly identifies what's being changed, though it doesn't provide deeper context about why this particular test file was targeted or whether it's part of a larger series. The changes themselves are simple and clearly correct, following a well-established in the project for test modernization. The patch appears ready to merge as-is.