Thread identity: Patch series (4/7) fixing behavior of `format.commitListFormat` configuration in `git format-patch`. Originating author not specified in this email.

Core problem: The `format.commitListFormat` option previously treated empty values as equivalent to a default format string ("log:[%(count)/%(total)] %s"), which was inconsistent with Git's conventional handling of empty config values. This patch removes that special case to make empty values truly empty.

Current status: Patch version not specified (appears to be first version submitted). Changes remove special string initialization in `builtin/log.c` and delete a now-redundant test case in `t/t4014-format-patch.sh`.

Key technical details: 
- Modifies `builtin/log.c` to stop initializing empty `format.commitListFormat` with default string
- Removes test case from `t/t4014-format-patch.sh` that verified old behavior
- Follows Git's typical config handling pattern where empty means "no value"

Open questions: None raised in this patch. The change appears straightforward and aligns with established patterns.

Participant positions: No reviews or responses yet (initial submission).

Related work: Part of a 7-patch series, though series context/goal not provided in this email. Change appears self-contained regarding `format.commitListFormat` behavior.