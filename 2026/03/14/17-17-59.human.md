This is a technical discussion about submodule handling in the ongoing effort to improve `git checkout -m` with autostash behavior. The thread has progressed from an initial proposal for a new `--autostash` flag to a more comprehensive reworking of the `-m` option's conflict resolution.

Junio Hamano responds to Phillip Wood's concerns about submodule edge cases. Phillip had questioned whether the pre-check for conflicts is necessary if stashing could be triggered by `unpack_trees()` failure, and raised the issue that `git stash` doesn't support `--recurse-submodules` while `checkout -m` does. 

Junio clarifies his position on the design, expressing skepticism about the need for submodule-specific stash handling. He suggests that submodule checkouts could handle their own stashing independently, without requiring the parent `git stash` command to understand submodules. The exchange reveals a philosophical difference - Junio prefers to avoid expanding `git stash`'s responsibilities unless absolutely necessary, while Phillip sees value in more integrated submodule support.

This discussion represents the final technical hurdle before the patch series can be finalized. The core functionality appears settled, but the submodule question remains open. Junio's stance suggests the solution may involve leaving submodule handling as-is rather than extending stash capabilities, though this could mean maintaining some special-case code for submodules in the `-m` path.