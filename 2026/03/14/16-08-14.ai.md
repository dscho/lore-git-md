This is a follow-up bugfix from Jeff King addressing a memory leak that surfaced after Andrew Au's zombie process cleanup patch was queued. The leak is in `transport_color_config()` where string config values weren't being freed after use. Jeff proposes switching to `repo_config_get_string_tmp()` to avoid allocations, noting this leak existed since 2018 but only became detectable with the recent changes.

The patch is minimal (4-line change) and well-explained, with Jeff providing context about why the leak went undetected until now (it required specific compiler and test conditions to surface). While unrelated to the original zombie process fix, Jeff suggests it either as a separate fix or as an amendment to Andrew's queued patch since that's what exposed the issue.

Key technical points:
- Fixes config string leaks in transport color handling
- Uses `_tmp` variant to avoid allocations
- Leak predates current work but was hidden by stack reuse patterns
- Only surfaces under clang/reftable test conditions
- Safe to apply as it doesn't interact with the process cleanup logic

This is a real but low-impact issue that Jeff caught through rigorous testing. The fix is straightforward and unlikely to be controversial, though Junio may choose whether to apply it separately or combine it with the queued patch.