This is a review response from Junio C Hamano on Kristoffer Haugsbakk's patch adding `--format` support to `git name-rev`. The maintainer raises several technical questions about the implementation details:

1. **Usage string consistency**: Junio notes the documentation was updated but questions whether the `name_rev_usage[]` array needs a matching update to include the new `--format` option.

2. **Option interaction**: He probes the behavior when `--format` and `--no-name-only` are used together, suggesting the documentation's claim that `--format` implies `--name-only` may need clarification or implementation adjustment.

3. **Memory management**: Junio questions whether `free()` is sufficient versus `FREE_AND_NULL()` for the notes message cleanup, given the context is reused across multiple calls.

4. **Initialization patterns**: He suggests `init_revisions()` might be preferable to the static `REV_INFO_INIT` for proper repository setup, and wonders why the pretty-print context is initialized indirectly rather than directly assigned.

The review maintains Git's characteristic thoroughness while being constructive - pointing out potential issues without rejecting the approach outright. These are precisely the sort of implementation details the maintainer routinely catches, and addressing them will strengthen the patch before inclusion. The questions indicate the feature itself is conceptually sound but needs some polish in the details.