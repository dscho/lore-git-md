Bugfix for `git subtree` recursion depth limitation. Colin Stagner follows up on Arun Sharma's 2021 report about the shell script hitting recursion limits when processing large histories (like PostgreSQL's 52k-commit repo). He proposes a patch series that aims to fix the mutual recursion between `process_split_commit()` and `check_parents()` in `contrib/subtree/git-subtree.sh`.

This appears to be a fresh attempt at solving the long-standing issue, separate from the 2020 C conversion PR #493 that Johannes Schindelin had mentioned. The patch series is already at v2, suggesting some initial review has happened elsewhere. Colin is specifically asking Arun to test with the original PostgreSQL reproduction case, which would validate whether this solves the real-world problem.

The approach isn't described in the email, but since it's targeting the shell script rather than converting to C, it likely restructures the recursion to avoid hitting Bash's function nesting limit. This would be a more surgical fix than a full rewrite, potentially easier to review and merge if it works. The ball is now in Arun's court to verify whether this actually resolves the issue in practice.