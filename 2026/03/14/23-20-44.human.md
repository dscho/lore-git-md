This is a small bugfix patch improving error messages in the pretty-format code. The patch changes two error messages in `pretty.c` to be more specific when commands don't support the `%(count)` and `%(total)` format specifiers. Instead of the generic "this format specifier can't be used with this command", the new messages explicitly name the problematic specifier ("%(count) is not supported by this command" and similarly for %(total)).

The change is straightforward and clearly improves the user experience by making the error messages more actionable. The patch is part of a 7-patch series (this is patch 1/7), though without seeing the cover letter we don't know the broader context of the series. The change itself is small (4 lines modified) and focused, with no obvious issues in the implementation.