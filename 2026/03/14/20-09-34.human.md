This is a brief follow-up in Phillip Wood's series removing `the_repository` usage from worktree functions. Junio C Hamano pointed out that the header comment for `get_worktree_git_dir()` still incorrectly states the function accepts NULL to return the current worktree's git dir, even though the patch explicitly removed that behavior by making NULL a BUG() condition. Phillip acknowledges the oversight and will send an updated version.

The exchange is straightforward - just catching a documentation mismatch that needs to be aligned with the implementation change. This is the final polish on a series that has already been technically reviewed and approved, part of the ongoing effort to eliminate `the_repository` global variable usage. The patch itself was the last step in hardening the worktree API against implicit global state dependencies.