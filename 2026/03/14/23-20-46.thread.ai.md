Thread identity: Patch series part 3/7 modifying `git format-patch`'s cover letter generation, specifically renaming `--cover-letter-format` to `--commit-list-format`.

Core problem/goal: Align the CLI option name with its configuration variable (`format.commitListFormat`) while improving shell completion behavior for cover-letter-related options. The change clarifies that the formatting applies specifically to the commit list portion of cover letters.

Current status: Patch introduces the renamed `--commit-list-format` option which now implies `--cover-letter` unless explicitly disabled. Maintains backward compatibility through config variable name. Part of ongoing series (position 3/7) but stands independently.

Key technical details:
- Modified files: `builtin/log.c` (option parsing), documentation, tests
- New option: `--commit-list-format` replaces `--cover-letter-format`
- Behavior change: New option implies `--cover-letter` unless `--no-cover-letter` specified
- Config remains `format.commitListFormat` (unchanged from before)
- Shell completion: `--cover-<TAB>` now unambiguously expands to `--cover-letter`

Open questions/loose ends: None raised in this patch - appears self-contained. Future patches in series may build on this change.

Participant positions: Single-author patch (no review discussion yet). Tests and documentation updated to reflect changes.

Related work: Part of larger series (3/7) about `format-patch` cover letters, but no specific dependencies mentioned in this commit.