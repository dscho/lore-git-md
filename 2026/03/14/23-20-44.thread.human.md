This thread begins with a straightforward bugfix to improve error messages in Git's pretty-formatting code. The patch makes error messages more specific when users attempt to use the `%(count)` or `%(total)` placeholders with commands that don't support them, replacing generic warnings with explicit mentions of the problematic placeholders. The change is minimal (just two lines modified) and clearly explained in the commit message.

As the first in a 7-patch series, this sets the stage for subsequent improvements to the pretty-formatting system. While we don't yet have the cover letter describing the full scope of the series, this initial patch appears uncontroversial and focused on user experience, providing clearer feedback when format placeholders are misused. The changes are small enough that they're unlikely to generate significant discussion unless later patches in the series introduce more substantial modifications.