This is a test modernization patch, part of the ongoing effort to update Git's test suite to use more robust path-checking helpers. Zakariyah Ali contributes a single patch that updates `t/t2000-checkout-cache-clash.sh` to replace direct `test -f` and `! test -f` checks with the preferred `test_path_is_file` and `test_path_is_missing` helper functions.

The patch is straightforward, converting 5 instances of raw shell tests to the more descriptive helper functions. These helpers provide better error messages when tests fail and make the test intent clearer. The changes are mechanical and don't affect test behavior - they just modernize the implementation to match current test suite standards.

This appears to be a clean, focused contribution that aligns with the project's test modernization guidelines. The patch follows the established pattern for such conversions and touches only the intended test file. There's no cover letter or discussion thread provided, so this seems to be a standalone improvement rather than part of a larger series.