This is the second patch in a feature series introducing per-remote SSH configuration options. The patch prepares the ground for the main feature by plumbing the remote name through to `git_connect()`.

The change adds a `remote_name` parameter to `git_connect()` and updates all callers to pass NULL (for now) or the actual remote name. The transport layer is modified to pass the remote's name when establishing connections. The patch also adds remote name to the diagnostic output when `CONNECT_DIAG_URL` is set.

This is a mechanical change that doesn't yet implement the per-remote SSH configuration feature - that comes in the next patch. The author notes this is a non-breaking forward change since all existing callers pass NULL. The diff shows careful updates across multiple files (connect.c, connect.h, transport.c, and builtin commands) to maintain consistency.

The changes are straightforward plumbing work needed for the series' goal of supporting `remote.$name.sshIdentityFile` and `remote.$name.sshOpts`. The patch doesn't introduce new behavior yet, but sets up the infrastructure cleanly. The implementation appears solid, with all call sites properly updated and the remote name properly threaded through from the transport layer down to the connection handling code.