This is a test infrastructure discussion in the ongoing thread about enabling "set -e" earlier in the test suite. Jeff King (Peff) responds to Junio's proposed workaround for shell signal handling differences, confirming that Junio's more complete solution (`&& echo 0 || echo $?`) would work better than his own initial suggestion, though both produce the same result for this specific test case.

Jeff reports encountering additional test failures when running the series in CI, including one straightforward case where a `setfacl` command needs `|| true` to avoid failing under "set -e". He also mentions seeing failures when `test_done` runs with no tests executed (such as in skip_all cases), though he hasn't investigated those further.

The exchange continues the thread's theme of balancing strict error checking with shell compatibility, showing how even simple test patterns may need adjustment when "set -e" is enabled globally. Jeff's CI findings suggest there may be more such cases to address beyond the signal handling tests that have been the recent focus. The tone remains pragmatic, acknowledging the tradeoffs between correctness and maintainability in test infrastructure changes.