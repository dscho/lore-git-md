This is a documentation patch updating the man pages for `git format-patch` to accurately reflect the behavior of the `--commit-list-format` option and related configuration.

The patch clarifies two key points about commit list formatting in cover letters:
1. Documents that when neither `--commit-list-format` nor `format.commitListFormat` is set, the default is `shortlog` (previously only mentioned the config would be used if present)
2. Updates the description of `--cover-letter` to use the more general term "commit list" rather than "shortlog" since the format can now be customized

The changes are straightforward documentation updates that bring the man pages in line with the actual implementation. The patch appears to be part of an ongoing series (referencing mf/format-patch-commit-list-format) that introduced more flexible commit list formatting options. The commit message clearly explains what was outdated and how the documentation is being corrected.

The changes touch both the config documentation (format.adoc) and the command documentation (git-format-patch.adoc), with minor wording improvements to make the behavior more precise. This is a well-scoped documentation fix that helps users understand the available formatting options for patch series cover letters.