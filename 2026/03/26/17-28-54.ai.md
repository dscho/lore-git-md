This is a follow-up discussion in Michael J Gruber's const-correctness series addressing ISOC23 compatibility. Junio C Hamano suggests an alternative approach for handling the const-correctness issue in run-command.c's environment variable tracing.

Rather than casting the strchr() result directly, Junio proposes moving the const-stripping cast to the point where the string is assigned to the string_list's util field. This would better semantic alignment since the util field is fundamentally non-const in the string_list implementation. The change would still satisfy ISOC23 requirements while making the const-correctness workaround more localized to where the type mismatch actually occurs.

The discussion highlights the series' ongoing theme of balancing compiler compliance with code clarity - here debating whether to cast at the strchr() call site (as in the original patch) or defer the cast to where the non-const nature is truly required (Junio's suggestion). This refinement focuses specifically on the run-command.c change while leaving the http-push.c modification as proposed.

For future reference, this exchange demonstrates the maintainer's preference for const-correctness solutions that minimize the scope of type system violations, even in a series that's fundamentally about pragmatic compatibility fixes rather than architectural changes. The thread continues to show careful attention to where and how constness is relaxed in sensitive string-handling code.