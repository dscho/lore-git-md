[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber and follow-up series from Jeff King (Peff) addresses compiler warnings from ISOC23's stricter const pointer handling in string functions. The discussion has evolved from Gruber's initial mechanical fixes (using explicit casts across 19 files) to Peff's structural solution for revision parsing that eliminates const violations through allocation.

Gruber's series handled immediate ISOC23 compliance in refs backend, transport helpers, revision walking, builtins, and pseudo-merge subsystems via casts. Key discussion points included:
- Jeff King suggested replacing in-place string modifications with xmemdupz() allocations in revision parsing (builtin/rev-parse.c, revision.c)
- Junio Hamano noted the non-const function signature approach in rev-parse.c was safer than revision.c's internal casts
- Established pattern of using non-const aliases (via xstrdup_tolower()) for string modification before const storage (builtin/config.c)

Peff's 4-patch follow-up implements the allocation-based approach he previously suggested, with patches 1-3 now covering:
1. `handle_dotdot()` refactor in revision.c to avoid modifying const strings
2. `try_difference()` range parsing in builtin/rev-parse.c using allocated copies
3. Parent mark parsing ("^@", "^!", "^-") in revision.c with new allocation pattern:
   - Introduces three new variables to track allocated strings
   - Preserves combined mark parsing behavior (e.g. "foo^-^!")
   - Fixes incidental bug in string restoration
   - Adds proper cleanup in `out` section
   - Touches 25 lines with focused changes to parent mark logic

The series demonstrates a consistent approach of structural fixes over temporary workarounds, replacing in-place string modifications with xmemdupz() allocations and proper cleanup. The thread shows consensus that allocation is the preferred long-term solution over casting, with Peff's patches aligning with Junio's preference for minimizing type system violations. The evolution from Gruber's casts to Peff's allocations illustrates Git's incremental approach to const-correctness - addressing immediate compiler needs while working toward architecturally cleaner solutions. One patch remains in the series to complete the revision parsing const-correctness work.