[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber and follow-up series from Jeff King (Peff) addresses compiler warnings from ISOC23's stricter const pointer handling in string functions. The discussion has evolved from Gruber's initial mechanical fixes (using explicit casts across 19 files) to Peff's structural solution for revision parsing that eliminates const violations through allocation.

Gruber's series handled immediate ISOC23 compliance in refs backend, transport helpers, revision walking, builtins, and pseudo-merge subsystems via casts. Key discussion points included:
- Jeff King suggested replacing in-place string modifications with xmemdupz() allocations in revision parsing (builtin/rev-parse.c, revision.c)
- Junio Hamano noted the non-const function signature approach in rev-parse.c was safer than revision.c's internal casts
- Established pattern of using non-const aliases (via xstrdup_tolower()) for string modification before const storage (builtin/config.c)

Peff's 4-patch follow-up implements the allocation-based approach he previously suggested, with patch 1 now focusing specifically on range operator (".." and "...") handling in revision parsing:
- Refactors `handle_dotdot()` interface to avoid modifying const strings
- Replaces in-place NUL-termination with `xmemdupz()` allocation for modifiable copies
- Simplifies interface by passing parsed components (left/right sides, symmetric flag) explicitly
- Touches core revision.c (~20 lines changed) while preserving all behavior
- Lays groundwork for subsequent patches to apply similar fixes to other revision parsing cases

The thread shows consensus that allocation is the preferred long-term solution over casting. Peff's series aligns with Junio's preference for minimizing type system violations and appears likely to be accepted given Peff's expertise and the prior discussion trajectory. The evolution from Gruber's casts to Peff's allocations demonstrates Git's incremental approach to const-correctness - addressing immediate compiler needs while working toward architecturally cleaner solutions.