[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber and follow-up series from Jeff King (Peff) addresses compiler warnings from ISOC23's stricter const pointer handling in string functions. The discussion evolved from Gruber's initial mechanical fixes (19 files via casts) to Peff's structural solution using allocation patterns across revision parsing code.

Gruber's series handled immediate ISOC23 compliance in refs, transport helpers, revision walking, builtins, and pseudo-merge subsystems. Key discussion established:
- Jeff King advocated replacing in-place string modifications with xmemdupz() allocations
- Junio Hamano preferred non-const function signatures over internal casts
- Established xstrdup_tolower() pattern for string modification before const storage

Peff's 6-patch series implements the allocation approach:
1. `handle_dotdot()` refactor in revision.c to avoid const violations
2. `try_difference()` range parsing in builtin/rev-parse.c with allocated copies  
3. Parent mark parsing in revision.c with new allocation pattern (25 lines)
4. Final parent mark handling in builtin/rev-parse.c completing the transition:
   - Replaces `*dotdot = 0` with xmemdupz() allocation tracked via to_free
   - Renames `dotdot` to `mark` for consistency
   - Maintains all parent mark functionality ("^!", "^@", "^-")
   - Fixes string restoration bug in error paths
   - Touches 25 lines in try_parent_shorthands()

The series demonstrates Git's incremental approach to const-correctness - addressing immediate compiler needs (Gruber) while evolving toward architecturally cleaner solutions (Peff). The thread shows consensus that allocation patterns are preferred over casting, with Peff's implementation aligning with Junio's preference for minimizing type system violations. All patches follow a consistent approach of structural fixes: replacing in-place string modifications with proper allocation and cleanup. The work affects revision parsing internals across revision.c and builtin/rev-parse.c, establishing patterns likely to influence future const-correctness efforts.