[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber addresses compiler warnings from ISOC23's stricter const pointer handling in string functions like strchr(). The series makes mechanical adjustments across 19 files (refs backend, transport helpers, revision walking, builtins, pseudo-merge) using explicit casts rather than architectural changes to maintain compatibility. Key changes include:

Core revision parsing in `builtin/rev-parse.c` and `revision.c` where Git temporarily modifies argv strings for revision ranges ("..") and parent shorthands ("^@"). Jeff King (Peff) suggests replacing the in-place string modifications with xmemdupz() allocations as a more robust solution that would avoid const violations entirely, referencing prior work by Collin Funk on this issue. Peff indicates he has unpolished patches taking this approach, signaling potential follow-up work. Junio Hamano had previously noted the `rev-parse.c` approach (changing function signatures to take non-const pointers) is safer long-term than `revision.c`'s internal casts. Affected functions include `try_difference()`, `try_parent_shorthands()`, and `handle_revision_arg_1()`.

The pseudo-merge subsystem (`pseudo-merge.c`) now includes a cast for `bsearch()` return values to maintain compatibility with callers under ISOC23's stricter const propagation. Junio suggests the commit message should better document the ISOC23 context for this isolated change, though the adjustment is purely mechanical.

In `builtin/config.c`, Junio C Hamano concurs with Jeff King's suggestion to use a non-const alias for newly allocated strings (via `xstrdup_tolower()`) rather than casting `strchr()` results. This establishes the preferred pattern for cases needing temporary string modification before storage in const-marked structures: create local non-const variables during modification to maintain type safety without widespread casting. Peff had earlier noted this approach could apply to similar cases like `packet_reader`'s line handling.

In run-command.c's environment variable tracing, Junio proposes moving the const-stripping cast to the string_list's util field assignment point, better matching the string_list implementation's semantics where util is fundamentally non-const. This demonstrates the maintainer's preference for const-correctness solutions that minimize type system violations' scope.

D. Ben Knoble acknowledged the series but no substantive technical feedback has emerged beyond Junio and Peff's reviews. The thread shows consensus that while Gruber's casts address immediate ISOC23 compliance, the preferred long-term direction for revision parsing is allocation-based solutions (as Peff suggests) that eliminate const violations rather than working around them. The discussion continues the theme of balancing compiler compliance with code clarity through localized type system adjustments, with Peff indicating potential follow-up work to refactor argv handling in revision parsing. The latest exchange solidifies maintainer approval for the non-const alias pattern in `config.c` as a model for similar cases.