[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber addresses compiler warnings from ISOC23's stricter const pointer handling in string functions like strchr(). The series makes mechanical adjustments across 18 files (refs backend, transport helpers, revision walking, builtins) using explicit casts rather than architectural changes to maintain compatibility.

Patch 3/6 focuses on cases where Git's data structures (string_list's util member) interact with const data, particularly in environment variable handling (run-command.c) and URL parsing (http-push.c). The changes add targeted casts when calling strchr() on const strings to accommodate ISOC23's new const-preserving behavior, preserving existing functionality while satisfying compiler requirements. Key modifications involve strchr() calls in http-push.c's URL parsing and run-command.c's environment handling, demonstrating the series' pragmatic approach of minimal casts over interface redesign. No behavioral changes are introduced - this remains a const-correctness compliance effort triggered by ISOC23's stricter pointer qualifier propagation. The series has not yet received review feedback.