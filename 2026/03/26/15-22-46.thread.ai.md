[PATCH 0/6] const-correctness fixes for ISOC23 from Michael J Gruber addresses compiler warnings from ISOC23's stricter const pointer handling in string functions like strchr(). The series makes mechanical adjustments across 19 files (refs backend, transport helpers, revision walking, builtins, pseudo-merge) using explicit casts rather than architectural changes to maintain compatibility. Key changes include:

Core revision parsing in `builtin/rev-parse.c` and `revision.c` where Git temporarily modifies argv strings for revision ranges ("..") and parent shorthands ("^@"), with Junio Hamano noting the `rev-parse.c` approach (changing function signatures to take non-const pointers) is safer long-term than `revision.c`'s internal casts. Affected functions include `try_difference()`, `try_parent_shorthands()`, and `handle_revision_arg_1()`.

The pseudo-merge subsystem (`pseudo-merge.c`) now includes a cast for `bsearch()` return values to maintain compatibility with callers under ISOC23's stricter const propagation. Junio suggests the commit message should better document the ISOC23 context for this isolated change, though the adjustment is purely mechanical.

D. Ben Knoble acknowledged the series but no substantive technical feedback has emerged beyond Junio's reviews. The thread remains focused on compiler compliance through minimal adaptations - either interface adjustments or targeted casts - without modifying behavior. Junio's feedback shows preference for interface honesty over implementation hiding where string mutability is concerned, particularly in the revision parsing changes. The series now appears complete with all ISOC23-triggered const-correctness issues addressed across Git's codebase, including both high-traffic areas (revision walking) and newer subsystems (pseudo-merges).