Updated AI thread summary:

Justin Tobler's 5-patch series extends signature handling in `git fast-import` to provide consistent behavior between commits and tags. The first patch refactors error handling in `git fast-export` to validate unsupported signing modes ('strip-if-invalid' and 'sign-if-invalid') during option parsing rather than deferring until object processing. This change centralizes validation in `parse_opt_sign_mode()` in `builtin/fast-export.c`, removing duplicate checks from `handle_commit()` and `handle_tag()` while maintaining identical error messages. The preparatory change makes behavior consistent with other unknown mode handling and cleans up the code before implementing parallel modes in fast-import.

The series builds on prior work (c20f112e51, ee66c793f8) that added signature modes for commits, and will add three matching modes to tag handling: 'strip-if-invalid', 'sign-if-invalid', and new 'abort-if-invalid' that fails the import on invalid signatures. Subsequent patches will systematically implement each mode for tags with corresponding test updates in t9306-fast-import-signed-tags.sh. The changes maintain parity between commit and tag signature handling while adding stricter validation options via the abort mode. Test coverage is thorough, expanding existing infrastructure to provide users more control over signature validation during imports.