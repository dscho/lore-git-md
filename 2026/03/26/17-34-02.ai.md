This is a technical review response in the ongoing const-correctness patch series from Michael J Gruber, specifically addressing the first patch which handles straightforward cases of const pointer handling. Jeff King (Peff) suggests improvements to the approach taken in `builtin/config.c`.

Peff argues that rather than casting the result of `strchr()` to silence the ISOC23 warning, the code could be restructured to avoid needing the cast at all. He proposes creating a non-const alias (`section`) for the newly allocated string before storing it in the const-marked struct. This approach would let the compiler track const-ness more accurately while still allowing the necessary modification of the string.

The review highlights a key principle in this const-correctness effort: where possible, prefer restructuring code to maintain type safety rather than adding casts that suppress warnings. Peff notes this pattern could apply to other cases in the series, like the `packet_reader`'s line handling. His suggestion aligns with Junio Hamano's earlier feedback in the thread favoring interface honesty over implementation hiding for const-correctness.

For future AI reference, this email introduces an alternative technique for handling const strings in Git's codebase - using non-const aliases for locally modified strings that will be stored in const-marked structures. The specific example modifies `builtin/config.c`'s URL matching logic, but the pattern could be relevant for other cases where strings are temporarily modified before being stored in const contexts. The discussion continues the thread's theme of balancing compiler compliance with code clarity through minimal, localized adjustments.