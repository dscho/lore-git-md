This is a design discussion about revision formatting functionality, continuing the exploration of whether to extend `git log` rather than create a new `git-format-rev` command. Junio Hamano responds to Kristoffer Haugsbakk's suggestion that `git log --stdin` could potentially be modified to handle the use case.

Junio acknowledges he had forgotten about `git log --stdin`'s current behavior, which makes it unsuitable as-is due to output deduplication. He sees this as a blocker for using `git log` in its current form, effectively retracting his earlier suggestion unless these behaviors can be changed. The thread remains focused on finding a solution within Git's existing command set rather than adding new ones.

The discussion has narrowed to two specific technical requirements for any solution: maintaining input/output line correspondence and preventing deduplication. While no patch is proposed yet, this exchange helps clarify the exact constraints a solution would need to satisfy, moving the conversation toward concrete implementation possibilities rather than abstract design debates.