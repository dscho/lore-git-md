Michael J Gruber's series addressing ISOC23 compiler warnings continues with a const-correctness fix for the pseudo-merge subsystem. The latest patch modifies `find_pseudo_merge()` to explicitly cast the return value from `bsearch()`, maintaining compatibility with existing callers while satisfying ISOC23's stricter const propagation rules. 

Junio Hamano reviewed the change, noting it makes technical sense but suggesting the commit message could better explain the ISOC23 context for future readers. This follows the pattern established earlier in the series - making minimal mechanical changes (in this case just adding a cast) to adapt to the new compiler requirements without modifying behavior. 

The series already addressed similar const-correctness issues across 18 files, with Junio previously weighing in on the approach taken in the revision parsing code. There he expressed a mild preference for modifying function signatures rather than using internal casts when dealing with string mutations. This latest patch follows the cast-based approach, which remains technically valid for the immediate compliance goal. 

With this addition, the series now appears to have covered all ISOC23-triggered const-correctness warnings across Git's codebase, from core revision walking to the newer pseudo-merge functionality. The changes are focused on compiler compliance through minimal adaptations rather than deeper architectural modifications.