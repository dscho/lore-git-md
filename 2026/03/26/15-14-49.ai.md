This is a preparatory refactoring patch for the `git backfill` feature series, addressing a header dependency issue. The change exposes `DEFAULT_ABBREV` by adding `object-name.h` to `revision.h`'s includes, since the `REV_INFO_INIT` macro relies on it. This is a straightforward dependency cleanup that enables subsequent patches in the series to work with revision initialization without requiring explicit inclusion of `object-name.h`.

The patch is minimal and mechanical - just one include line added to `revision.h`. It's part of Derrick Stolee's ongoing work to enhance partial clone functionality, specifically preparing for the `git backfill` command's revision handling capabilities. The change follows Git's standard header organization practices and doesn't introduce any behavior changes, serving purely as groundwork for the feature implementation in later patches.

This appears to be a well-justified header adjustment that properly documents an existing implicit dependency in the codebase. The patch is cleanly separated from the more substantive changes in the series, following Git's convention of splitting preparatory refactors from feature implementation.