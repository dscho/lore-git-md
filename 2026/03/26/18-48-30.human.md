This is Junio C Hamano questioning the motivation for a new `git-format-rev` command in response to Kristoffer Haugsbakk's proposal. Junio suggests that revision formatting functionality might be better added as an option to an existing command like `git log` rather than creating a new builtin.

The exchange highlights a common tension in Git development between extending existing commands versus introducing new ones. Junio's question implies that the feature warrants its own command, and that the functionality might overlap with what `git log --pretty` already provides. This suggests the proposal will need strong justification to proceed as a separate command rather than an extension to existing tools.

Given Kristoffer's documentation focus, we can anticipate they'll respond with a detailed rationale for the separate command approach, possibly citing specific formatting needs that don't fit well with `git log`'s model. The thread is likely to explore whether the functionality is sufficiently distinct to merit a new command or if it would be better served by enhancing existing tools.