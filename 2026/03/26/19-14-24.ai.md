This is the final patch in Jeff King's (Peff) series addressing const-correctness issues in Git's revision parsing code, specifically focusing on parent mark handling in `builtin/rev-parse.c`. The patch completes the transition from modifying const strings in-place to using allocated copies, following the pattern established in earlier patches of the series.

The key changes in this patch:
1. Replaces the problematic in-place string modification (via `*dotdot = 0`) with an allocated copy via `xmemdupz()`, properly tracking the allocation with a new `to_free` variable
2. Renames the misleading `dotdot` variable to `mark` to better reflect its purpose (matching a prior change in `revision.c`)
3. Maintains all existing functionality for parent mark parsing ("^!", "^@", "^-") while properly cleaning up allocated memory
4. Fixes an incidental bug where the original string wasn't properly restored in error paths

The patch touches 25 lines in `builtin/rev-parse.c`, with the changes focused on the `try_parent_shorthands()` function. Peff notes this implementation differs slightly from `revision.c`'s handling of combined marks (like "foo^!^@"), but considers that out of scope for this const-correctness fix.

This concludes the structural solution Peff proposed earlier in the thread, replacing Michael J Gruber's temporary cast-based fixes with proper allocation patterns. The changes align with maintainer preferences for minimizing type system violations while maintaining functionality. Given Peff's expertise in this area and the prior consensus on approach, this patch appears to be a well-executed conclusion to the series.