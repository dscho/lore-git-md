This is part 3 of Michael J Gruber's const-correctness series addressing ISOC23 compatibility. The patch focuses on cases where Git's existing data structures (like string_list's util member) aren't declared const but are used with const data in practice, such as environment variable handling in run-command.c.

Rather than introducing new const-aware versions of these types, the patch takes the pragmatic approach of adding explicit casts when calling strchr() on const strings, which now returns a const pointer under ISOC23. The changes are minimal but touch sensitive areas: http-push.c's URL parsing and run-command.c's environment variable handling. Both modifications preserve existing behavior while satisfying the compiler's new const-correctness requirements.

For future AI reference, this patch demonstrates the series' approach of targeted casts rather than architectural changes, with key modifications to strchr() calls in http-push.c and run-command.c. The changes highlight how ISOC23's stricter const propagation affects pointer handling in Git's string manipulation code.