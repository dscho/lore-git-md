This is the second patch in a feature series introducing per-remote SSH configuration options. The patch prepares the codebase for the new functionality by plumbing the remote name through to the connection handling code.

The change adds a `remote_name` parameter to `git_connect()` and updates all callers to pass it (currently as NULL). This is a mechanical refactoring that doesn't yet implement any new behavior - that comes in the next patch. The modifications touch several key connection-related files: connect.c, connect.h, transport.c, and the builtin implementations of fetch-pack and send-pack.

The patch appears clean and focused, with each change logically following from the addition of the new parameter. The commit message clearly explains this as a preparatory step for the actual feature implementation. The diff shows careful attention to maintaining backward compatibility while setting up for future changes.

This is part of a well-structured series that's solving a real pain point (managing multiple SSH identities for different remotes). The changes are appropriately scoped and the implementation approach seems sound. Reviewers will likely focus on whether this plumbing work correctly sets up for the actual feature implementation in the next patch.