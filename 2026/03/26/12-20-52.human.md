This is a code review response in Christian Couder's series introducing URL-based whitelisting for promisor remotes. Patrick Steinhardt suggests a simplification to the `url_is_valid_pattern()` helper function implementation in patch 3/16.

The proposed change is purely stylistic - replacing the explicit conditional with a terser `!!normalized` pattern that achieves the same result. Patrick notes that the function's documentation (copied from `url_normalize_1()`) appropriately describes its validation scope and limitations.

This is a minor refinement to an otherwise straightforward infrastructure patch that introduces URL pattern validation for the series' main feature. The review doesn't raise any functional concerns, just a small opportunity to make the code more concise while maintaining clarity. The exchange shows the typical back-and-forth where experienced contributors suggest small improvements to each other's implementations.