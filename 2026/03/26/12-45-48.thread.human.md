This thread examines subtleties in `git stash` command syntax, particularly when the `push` subcommand is required versus optional. Quentin Bernet's patch initially corrected the documentation to show that options like `-m/--message` and `--include-untracked` can be used with both `git stash push` and plain `git stash`, despite the help text implying most options require `push`. 

D. Ben Knoble questioned whether the original documentation was actually incorrect, noting the help text's specific wording about `push` being required for certain options. Mirko Faina then demonstrated through testing that while pathspecs do require `push`, most options (contrary to the help text) work fine without it. In his latest response, Bernet stands by his changes, proposing to clarify his commit message to better explain that the current grammar omits valid option combinations. 

The discussion has revealed three key points: (1) a real inconsistency between command behavior and documentation, (2) subtle but important distinctions in Git's syntax notation, and (3) ambiguity about whether the original docs were wrong or just poorly worded. The thread remains unresolved, with participants carefully analyzing both the technical behavior and how best to document it. The changes affect both the man page (`git-stash.txt`) and command implementation (`stash.c`).