This thread discusses a documentation patch to clarify the command-line syntax for `git stash`. Quentin Bernet initially proposed changes to the man page and builtin code to show that flags like `-m/--message` can be used with `git stash push` (not just `git stash save`), adjusting the bracket placement in the syntax documentation. The patch appeared straightforward until D. Ben Knoble raised a question about whether the original bracketing was actually incorrect, noting that the help text indicates `push` is required when using certain options (except for `-p`). This suggests there may be a subtle distinction between optional and required components in the command's syntax that needs to be resolved. The discussion is still open, with the original author needing to address whether the documentation change accurately reflects the command's behavior or if further clarification is needed.