This thread documents a straightforward improvement to the `git stash` command syntax documentation. Quentin Bernet has identified and fixed an inaccuracy in how the `-m/--message` flag and other options are shown in both the man page and builtin code. The correction clarifies that these flags can be used with `git stash push`, not just the older `git stash save` form. The changes are minimal but precise, adjusting bracket placement and argument ordering in the documentation to accurately reflect the command's capabilities. This appears to be a clean documentation-only patch with no impact on actual behavior.