This is a feature patch proposing a new `git-format-rev` builtin, building on Kristoffer Haugsbakk's earlier work to add `--format` support to `git name-rev`. The patch appears to be in early stages, with the author indicating they are working on implementing the functionality as a separate command rather than extending `name-rev`.

The context shows this is a follow-up to Junio Hamano's "What's cooking" report which noted Kh's previous `name-rev --format` patches were in the queue. The new direction suggests moving the formatting capability into its own dedicated command, which would provide more flexibility than bolting it onto `name-rev`. 

The technical details are sparse in this initial email, but we can expect the new command to handle revision formatting similar to `git log --pretty`, while likely reusing some of the infrastructure from the `name-rev` implementation. This appears to be part of the ongoing effort to make Git's revision formatting more flexible and consistent across commands.

Given Kristoffer Haugsbakk's track record with documentation contributions, we can anticipate thorough man page updates to accompany the new command. The patch is still in development, so the implementation approach may evolve based on feedback about whether a separate command is preferable to extending `name-rev`.