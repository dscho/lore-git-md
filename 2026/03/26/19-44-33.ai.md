This is a continuation of the revision formatting discussion, with Junio Hamano responding to Kristoffer Haugsbakk's exploration of adapting `git log` for the use case. The thread has shifted from considering a new `git-format-rev` command to evaluating whether `git log` can be extended to handle revision formatting needs.

Junio acknowledges that `git log --stdin` in its current form isn't suitable due to its deduplication behavior and single-output limitation, calling it a "non-starter" for this use case. This confirms the technical constraints Kristoffer identified in his previous email - that any solution would need to modify `git log` to produce one line per input and avoid deduplication.

The exchange shows Junio maintaining his position that new commands should only be added when existing ones cannot reasonably be extended, while demonstrating openness to understanding the specific technical limitations. His "sorry about the noise" comment suggests he recognizes the conversation is productive despite initial skepticism.

The discussion remains focused on technical feasibility rather than design philosophy, with both participants working within the constraint of Git's existing command structure. The key open question is whether `git log` can be practically modified to meet these specific formatting needs while maintaining its core semantics, or if the use case is sufficiently distinct to warrant a new command.