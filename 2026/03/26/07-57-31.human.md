This is a bugfix discussion thread about `git fetch --prune-tags` behavior. Orgad Shaneh responds to Junio C Hamano's questions about whether the current implementation (which doesn't force-update tags) was an intentional design choice or an oversight.

Orgad's investigation reveals the feature history: `--prune-tags` was added in 2018, but tag clobbering protection was added later that same year, possibly overlooking the interaction with `--prune-tags`. He suggests that if the current behavior is deemed correct, the documentation should be updated instead, and proposes adding a `fetch.force` config option as an alternative solution.

The discussion highlights a test coverage gap - while the patch verifies that `--prune-tags` allows tag updates, it doesn't test the actual pruning behavior (removing tags that no longer exist remotely). Orgad notes that pruning tests live in `t/t5510-fetch.sh` and offers to add more comprehensive verification.

This exchange shows the patch is in a holding pattern while the historical context is clarified. The maintainer wants to understand whether this was an intentional divergence between docs and code before deciding whether to change the behavior or the documentation. The author appears responsive to feedback and willing to expand test coverage as needed.