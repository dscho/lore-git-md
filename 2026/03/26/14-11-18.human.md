This is a test improvement patch that fixes a subtle issue in the blame test suite. The change modifies t8003-blame-corner-cases.sh to properly capture git-blame's exit code by redirecting output to temporary files rather than piping it to grep. This ensures test failures from git crashes won't be silently ignored due to the pipe.

The patch is straightforward and mechanical - it updates 14 test cases to use the `>actual` pattern followed by a separate grep, rather than piping directly. The changes maintain all existing test assertions while making the tests more robust by properly checking git's exit status. The commit message clearly explains the motivation (avoiding unnoticed crashes) and the solution.

This appears to be a well-scoped, single-purpose improvement to test reliability. The changes are consistent and the existing test logic remains intact. The patch comes from Trieu Huynh, a new contributor participating in Google Summer of Code (as indicated by the GSoC prefix). The change follows Git's testing conventions and should be uncontroversial.