This is a review follow-up in the xdiff refactoring thread. SZEDER Gábor points out that several patches in Ezekiel Newren's series fail to build with `DEVELOPER=1` due to signed/unsigned comparison warnings. The issues occur in patches 1-4 where `long int` values are compared with `size_t` without proper type handling.

The final patch in the series (which converts these types to be more precise) resolves the warnings, but the intermediate states introduce compilation errors. This suggests the series needs to either reorder the patches or adjust the type handling in the earlier steps to maintain buildability throughout.

The review is constructive and specific, identifying exactly which comparisons trigger warnings and in which patches. This helps the author address the issues without needing to reproduce the build environment. The tone is professional and focused on the technical problem - no broader critique of the refactoring approach is offered, as the maintainer had already approved the overall direction.

For readers following the xdiff cleanup effort, this serves as a heads-up that the series may need another revision to handle these build warnings before merging. The core readability improvements remain uncontested, with only these technical build issues needing resolution.