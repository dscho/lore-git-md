[PATCH] revision parsing: make const-correctness violations explicit

This patch documents intentional const-correctness violations in revision parsing code by adding explicit casts where string arguments declared as `const char *` are modified. Changes focus on two areas:

1. `builtin/rev-parse.c`: Modifies `try_difference()` and `try_parent_shorthands()` to take `char *` parameters instead of `const char *`, with explicit casts when calling these functions with `argv` elements.

2. `revision.c`: Adds explicit casts when using `strstr()` results to modify strings (parent shorthands `^@`, `^!`, `^-`).

The patch acknowledges these as deliberate violations rather than attempting a full const-correctness fix. Changes are mechanical (8 lines total) and maintain existing behavior while making the string mutations explicit. This appears related to broader code hygiene efforts around string handling, though not tied to any named initiative. The small, focused nature suggests low risk of controversy.