Refactoring patch to improve const-correctness in path handling.

This is part 2 of a 6-patch series (no cover letter provided, so the overall goal isn't clear). The change makes `git_find_last_dir_sep()` return a `const char*` rather than `char*` since it operates on a const input and doesn't modify the string. The patch updates the two callers in `scalar.c` and `submodule.c` that were relying on the non-const return type to modify the string, adding explicit casts to document the intentional const-cast.

The change helps with ISOC23 compliance by properly marking functions that don't modify their inputs. The implementation is straightforward and the explicit casts make the intentional mutating operations clear. The patch touches low-level path handling code but doesn't change any behavior - it makes the const-correctness more explicit.