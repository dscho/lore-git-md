This is a code review response in Christian Couder's series adding URL-based whitelisting for promisor remotes. Patrick Steinhardt suggests a simplification to the `url_is_valid_pattern()` helper introduced in patch 3/16.

The proposed change would make the function more concise by combining the conditional free and return into a single expression using the `!!` operator to convert the pointer to a boolean. This is a minor stylistic improvement that doesn't affect functionality - the helper still checks if a string looks like a valid URL or URL (allowing '*' globs) by attempting normalization.

Patrick also notes that the function's documentation comment is appropriately copied from `url_normalize_1()`, which it wraps. The review is focused and constructive, offering a small optimization while confirming the documentation approach makes sense for this infrastructure piece.

This exchange shows the series progressing through review with attention to both correctness and code quality. Patrick's suggestion, if adopted, would slightly clean up the implementation without changing behavior, keeping the focus on the series' larger goal of secure promisor remote configuration.