This is the final patch in Derrick Stolee's v3 series enhancing `git backfill` for partial clones. The patch adds explicit testing for unknown argument handling, completing the series' validation of all major functionality.

The change ensures `git backfill` properly rejects invalid arguments in two cases: non-option arguments that don't match revision/pathspec syntax (triggering the revision machinery's "ambiguous argument" warning) and unknown `--` options (now caught by explicit checking in the command). The tests verify both error paths using `test_grep` to check the exact error messages.

This addresses feedback from Siddharth Shrimali who noticed the original silent ignoring of unknown arguments. The implementation maintains all previous functionality while adding these safety checks. With this patch, the series' test coverage now includes:
- Revision range and pathspec filtering
- Both prefix and wildcard pathspec optimization
- Argument validation
- Error case handling

The patch is straightforward and focused, matching the series' high standard of thorough testing. With Junio's ack still valid and all substantive issues addressed, this appears to be the final piece needed before integration.