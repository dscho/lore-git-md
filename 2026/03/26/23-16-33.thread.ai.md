Git thread summary (AI mode):

Junio Hamano's March 2026 "What's cooking" report tracks the state of topics across Git's integration branches (maint, master, next, seen). Key developments:

Object Database (ODB) abstraction work by Patrick Steinhardt progresses with object counting and generic name handling improvements merged to 'next'. Taylor Blau's MIDX improvements continue with part 3.2 (reachability bitmaps during compaction) now in 'next'. Test suite cleanup includes Junio's 'set -e' standardization and test_path_is_* modernization.

Experimental `git replay` gains new --ref option and revert mode. Build system changes led by Steinhardt include Meson/PCH optimizations. Adrian Ratiu extends configurable hooks with parallel execution support. Documentation updates and platform fixes (macOS/Homebrew) continue alongside the long-running `the_repository` removal effort.

Controversial topics like Rust preparation (via xdiff cleanup) and sideband sanitization remain in 'next' for extended testing. Junio provides clear status indicators for each topic's readiness (merge/reroll/review needed). The report serves as both status update and review request for in-flight changes across Git's development branches.