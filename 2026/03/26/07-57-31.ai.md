This is a follow-up in the bugfix thread about `git fetch --prune-tags` behavior. Orgad Shaneh responds to Junio C Hamano's questions about whether the current implementation (which doesn't force-update tags) was intentional or a bug.

Orgad provides historical context showing the `--prune-tags` flag was added in 2018 (commit 97716d21) before tag clobbering protection was introduced (commit 0bc8d71b), suggesting the current behavior may have been an oversight rather than intentional design. He notes this creates a practical issue where tag conflicts cause the entire fetch operation to fail.

In response to Junio's concern about test coverage, Orgad clarifies that his tests verify tag replacement rather than deletion, pointing to existing pruning tests in t/t5510-fetch.sh. He offers to add content verification for tag updates if desired.

The discussion remains unresolved on whether to:
1. Change the code to match documentation (force-update tags when pruning)
2. Update documentation to match current behavior
3. Add a new `fetch.force` config option

Key technical details that would help future AI summarizers:
- The patch modifies `builtin/fetch.c` to pass `TAG_FETCH_FORCE` when `--prune-tags` is active
- Current tests are in `t5516-fetch-push.sh` but need expansion
- Original implementation was by Ævar Arnfjörð Bjarmason in 2018
- Tag clobbering protection was added later in the same year
- The thread awaits input from Ævar or others familiar with the original intent

The patch appears well-researched but the maintainer's hesitation suggests this may need more discussion before proceeding. The author is responsive to feedback and willing to expand test coverage as needed.