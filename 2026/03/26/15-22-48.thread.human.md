This thread introduces a straightforward refactoring to improve type safety in Git's codebase. The patch changes `git_find_last_dir_sep()` in git-compat-util.h to return a `const char*` instead of `char*`, since this function (which wraps `strrchr()`) doesn't modify its input string. This makes the code more ISOC23-compliant and properly marks immutable returns as const.

The change requires explicit casts in scalar.c and submodule.c where the returned pointer is used in contexts that do modify the string (by temporarily writing a NUL terminator). The patch correctly identifies and updates all three affected locations. The commit message clearly explains the motivation and scope of the change, which appears to be a well-contained, low-risk improvement to type safety.