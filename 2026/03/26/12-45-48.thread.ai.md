Thread summary:

Documentation patch correcting the command-line syntax for `git stash` commands. Quentin Bernet fixes the man page and builtin code to show `-m/--message` and other flags can be used with `git stash push` (not just `git stash save`), adjusting bracket placement and moving the `[push]` optional argument. D. Ben Knoble questions whether the original bracketing was incorrect, noting the help text specifies `push` is required when using certain options (except `-p`), suggesting the original documentation may have been technically accurate. The discussion reveals a subtle distinction between optional and required command components in Git's syntax notation. The patch touches `Documentation/git-stash.txt` and `builtin/stash.c`. Open question remains about whether the original documentation was actually incorrect or just unclear.