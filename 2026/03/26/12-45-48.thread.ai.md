Thread summary:

Documentation patch correcting the command-line syntax for `git stash` commands. Quentin Bernet fixes the man page and builtin code to properly show that `-m/--message` and other flags can be used with `git stash push` (not just `git stash save`). The changes adjust bracket placement and move the `[push]` optional argument in the syntax documentation. This is a minimal but accurate documentation improvement with no behavioral changes to the actual stash functionality. The patch touches `Documentation/git-stash.txt` and `builtin/stash.c` to clarify the command syntax. No open questions or objections raised - appears ready for merging as a straightforward docs fix.