Thread identity: Documentation discussion about `git stash` command behavior versus documentation, focusing on when the `push` subcommand is required for various options and pathspecs.

Core problem: The SYNOPSIS in git-stash documentation inaccurately suggests `push` is mandatory for most options, when in reality non-option arguments (pathspecs) are the only elements that need explicit `push` (or `--` disambiguation). Options like `-m/--message` and `-u/--include-untracked` work with both `git stash` and `git stash push` forms.

Current status: Quentin Bernet (first-time contributor) has proposed revised grammar for the man page that better matches actual behavior, separating standalone `git stash` from `git stash push` cases while showing options work with both. Junio Hamano has confirmed the documentation/implementation discrepancy exists.

Key technical details: The behavior stems from `builtin/stash.c`'s argument parsing - pathspecs require special handling (`push` or `--`) while most options don't. Documentation files involved are `Documentation/git-stash.txt` and its AsciiDoc counterpart.

Open questions: Whether to best represent this in documentation - balancing grammatical precision against user clarity given EBNF notation limitations. Also whether to clarify the "non-option arguments" explanation since pathspecs are technically allowed with `--`.

Participant positions:
- Quentin Bernet (author): Driving documentation fixes with revised syntax proposals
- Junio Hamano: Confirmed implementation differs from docs, suggested commit message improvements
- D. Ben Knoble: Initially questioned if original bracketing was incorrect
- Mirko Faina: Demonstrated actual behavior differs from help text

Related work: This connects to broader documentation consistency efforts (Jean-Noël Avila's synopsis-style conversion) and affects command-line completion systems that rely on accurate syntax specifications.