This is a follow-up from Junio C Hamano regarding the `git replay --ref` patch series. The series had been queued for integration but is now encountering test failures when rebased on the updated base commit (ba5c0d03d3).

The issue manifests in test 46 of t3650-replay-basics.sh, which verifies that `--ref` rejects invalid refnames. The test expects the command to fail with a specific error message about invalid refnames, but instead gets a signal 6 (SIGABRT) crash from merge-ort. This occurs both when rebased and when applied cleanly to the old base, suggesting the problem isn't a rebase artifact but rather an interaction between the new validation code and the underlying merge machinery.

Junio's message indicates this needs investigation before the series can proceed to master. The crash suggests the validation logic in the `--ref` implementation isn't properly handling error cases before they reach merge-ort, or that merge-ort itself has changed in ways that affect this case. The test failure is clear and reproducible, making this a straightforward bug to diagnose.

For future summarizers: The key detail is the SIGABRT in merge-ort when testing invalid refnames with `--ref`. The test was previously passing, so this appears to be an interaction with changes in the base commit (sa/replay-revert at ba5c0d03d3). The validation logic in builtin/replay.c may need adjustment to catch errors earlier or handle merge-ort's new behavior.