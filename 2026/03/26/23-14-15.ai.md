This is a const-correctness follow-up in Jeff King's series addressing ISOC23 compiler warnings in revision parsing. The email continues the discussion with Junio Hamano about the `handle_dotdot()` interface in `revision.c`, which processes range operators like ".." and "...".

Jeff King acknowledges Junio's correction about a typo in the original patch (referring to "digit" instead of "dot"). The core of the email reinforces the necessity of the allocation-based approach they've settled on, where `xmemdupz()` creates copies of range operator strings rather than modifying them in-place. Jeff explains that while removing `const` from the interface would be more honest about the string modification, it would push the copying burden to callers. He also dismisses a more radical ptr/len approach as too invasive for the current problem.

The exchange shows the maintainer and contributor converging on an optimal solution - Jeff's allocation pattern has now received Junio's explicit approval after initial reservations. This patch series establishes important patterns for handling const strings in Git's codebase, particularly in the revision parsing logic. The changes affect core functionality (range operators in revision specs) while maintaining backward compatibility and proper memory management.