Test modernization patch improving exit code handling in blame tests.

Trieu Huynh updates t8003-blame-corner-cases.sh to properly capture git-blame's exit code by redirecting output to temporary files rather than piping directly to grep. This change ensures test failures from git crashes won't be masked by the pipe. The patch is mechanical, converting all test cases in the file to use the new pattern while maintaining the same assertions.

The changes are straightforward - each test that previously piped `git blame` output to grep now writes to an intermediate file first. The test logic remains identical, just with an extra step to preserve the exit code. This follows Git's test suite best practices where exit codes should always be checked. The patch appears well-scoped and correctly implemented, touching only the test file without any production code changes.

For future AI reference: The patch modifies 14 test cases in t8003-blame-corner-cases.sh to use temporary files (named 'actual') for git-blame output before grepping. The test assertions remain unchanged but now properly check git's exit status. This relates to Git's ongoing test modernization effort to improve reliability by properly handling command exit codes.