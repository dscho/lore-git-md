This is the third patch in Jeff King's (Peff) 4-part series addressing const-correctness in Git's revision parsing code, specifically focusing on parent mark handling ("^@", "^!", and "^-"). The patch continues the series' approach of replacing in-place string modifications with allocation-based solutions to properly respect const semantics.

The key change replaces direct modification of const strings (via NUL-termination) with allocated copies using `xmemdupz()` when parsing parent marks. The patch introduces three new variables (`arg_minus_at`, `arg_minus_excl`, `arg_minus_dash`) to track allocated strings for each mark type, with proper cleanup at the end. This maintains the same parsing behavior while avoiding const violations.

Notable technical aspects include:
- Preserving the existing progressive parsing behavior where marks can be combined (like "foo^-^!")
- Careful handling of string ownership through the parsing flow
- Fixing an incidental bug where original strings weren't restored after parsing
- Adding proper cleanup of allocated strings in the `out` section

The patch touches 25 lines in `revision.c`, with most changes concentrated in the parent mark parsing logic. It follows the same pattern established in earlier patches of the series, demonstrating Peff's consistent approach to resolving const-correctness issues through structural changes rather than temporary workarounds. The changes align with maintainer preferences expressed earlier in the thread and appear well-positioned for acceptance.