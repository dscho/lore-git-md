This is a small but important header fix in Derrick Stolee's `git backfill` series, which enhances partial clone functionality. The patch adds an explicit include of `object-name.h` to `revision.h` to resolve a hidden dependency - the `REV_INFO_INIT` macro uses `DEFAULT_ABBREV` from `object-name.h` but didn't declare this dependency. 

While this change appears simple, it's a necessary foundation for the rest of the series since it ensures proper header parsing behavior. The patch follows Git's standard practice of making header dependencies explicit rather than relying on transitive includes. The change is straightforward and uncontroversial, with no behavioral impact beyond fixing the include chain. As the first patch in the v3 series (which Junio has already acked), this sets up clean parsing behavior for the subsequent patches that implement the enhanced `git backfill` functionality.