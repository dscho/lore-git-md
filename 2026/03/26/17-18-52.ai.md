This is a const-correctness fix in the pseudo-merge subsystem, part of Michael J Gruber's series addressing ISOC23 compiler warnings. The patch modifies `find_pseudo_merge()` in `pseudo-merge.c` to explicitly cast the return value from `bsearch()` to non-const, maintaining compatibility with existing callers while satisfying ISOC23's stricter const propagation rules.

Junio points out that while the change makes sense in the context of the full series, the commit message could better explain the ISOC23 context for future readers who might encounter this isolated change. The patch itself is straightforward - just adding a cast - but the rationale stems from ISOC23 making `bsearch()` preserve const qualifiers from its input array, which affects how the function interacts with Git's existing pseudo-merge commit handling.

This is a mechanical adjustment rather than a functional change, keeping the same behavior while making the const/non-const conversion explicit. The patch touches the pseudo-merge commit lookup path but doesn't modify any actual logic - it's adapting to the new compiler requirements about pointer qualifiers. Junio's review suggests the change is correct but could use more explanation for posterity.