This is a documentation patch updating the behavior of `--commit-list-format` in `git format-patch`. It's the second iteration of changes that clarify how commit lists are formatted in cover letters.

The patch makes several documentation improvements based on Junio Hamano's feedback from the previous version. It now explicitly states that:
1. The default format is `shortlog` when neither the command-line option nor config is set
2. Any string containing '%' is treated as a custom format (without strict validation of placeholders)
3. Only the command-line `--commit-list-format` option (not the config variable alone) implies `--cover-letter`

The changes touch both the config documentation (format.adoc) and command documentation (git-format-patch.adoc), making the behavior more precise while maintaining clarity. The author has chosen slightly more verbose explanations than Junio suggested, aiming to help users better understand the rationale behind the format detection rules.

This is a well-scoped documentation update that brings the man pages in line with the actual implementation. The patch appears to be part of an ongoing series (mf/format-patch-commit-list-format) that introduced flexible commit list formatting options. The changes are straightforward and uncontroversial, addressing specific gaps identified in the previous documentation.