This is a discussion about a proposed `fetch.blobSizeLimit` config option for partial clones, continuing a thread where the feature's merits are being debated. Patrick Steinhardt acknowledges Alan Braithwaite's clarification that the patch only affects client-side behavior and would not impose new server requirements beyond existing `uploadpack.allowFilter` support. 

Patrick agrees with Alan's assessment of the limitations of `remote.*.partialCloneFilter` (it doesn't work for initial clones and requires additional config) but suggests the feature should be more flexible - allowing per-URL configuration like other Git settings, and supporting arbitrary filter specification rather than hardcoding blob size limits. He confirms blob size filters are widely supported by hosting providers today.

The exchange shows the technical concerns about the proposal have been addressed (bitmap compatibility, server requirements), leaving the discussion focused on design philosophy - whether this justifies a new config option versus using existing mechanisms or implementing a more general solution. Patrick's position appears to be that while the use case is valid, the implementation should be more flexible if adopted.