This is a discussion about the ordering of trailers (like Signed-off-by and Reviewed-by) when using the new `--trailer` option in `git rebase`. The patch series (now in its 7th iteration) adds trailer support to rebase by refactoring Git's trailer handling to be reusable across commands.

Phillip Wood, the current implementation adds trailers after signoffs in the rebase squashing logic, but questions whether trailers should come before signoffs instead. He suggests this might better match project conventions, using the example of wanting Reviewed-by to appear before Signed-off-by.

Kristoffer Haugsbakk responds by questioning whether this ordering should match existing behavior in `git commit` and `git tag`, noting that signoff options are now considered a historical mistake since they special-case one trailer type. He points out that trailer configuration is meant to handle ordering logic, though currently it manages same-key trailers and duplicates rather than cross-trailer relationships like signoff positioning.

The exchange highlights an implementation detail that needs resolution before finalizing the feature: whether to enforce a specific trailer ordering convention in rebase or defer to configurable trailer rules. This touches on the broader tension between Git's historical special-casing of certain trailers versus the more general trailer machinery being added here. The thread shows the patch series is nearing completion but still has minor behavioral questions to settle.