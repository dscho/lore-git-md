This is a preparatory patch in Patrick Steinhardt's performance optimization series targeting write contention in `git-upload-pack`. The patch introduces a new helper function `cast_size_t_to_ssize_t()` that safely converts between size_t` and `ssize_t` types with bounds checking to avoid platform-specific overflow issues.

The function follows the same pattern as the similar `cast_size_t_to_int()` already present in the file, adding overflow protection when converting unsigned size values to signed ssize_t. This is infrastructure work for the larger series - the commit message notes it will be used in the following commit (likely for buffer size handling in the upload-pack optimizations).

As a small, focused helper function addition with clear safety checks, this patch appears straightforward and uncontroversial. It follows existing patterns in the codebase and serves the clear purpose of enabling safe type conversions needed for the performance work. The implementation includes proper error handling via `die()` for overflow cases, matching Git's error reporting conventions.