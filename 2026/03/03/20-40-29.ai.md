This is the second patch in a two-part test modernization series for t3700 (git add tests). The patch converts remaining direct `grep` and `! grep` calls in t3700-add.sh to use the `test_grep` helper, following Junio's suggestion. This provides better debugging output when tests fail by automatically showing file contents on failure.

The changes are mechanical replacements that don't alter test logic - 18 lines converting `grep` to `test_grep` and 18 lines converting `! grep` to `test_grep !`. The patch touches various test cases including file mode checks, ignored file handling, and chmod behavior verification. This completes the test_grep conversion started in earlier versions, making the entire file consistent with modern test suite practices.

The patch follows the same pattern as the first in the series (which addressed exit code handling) - both are straightforward test infrastructure improvements that make failures more detectable without changing test behavior. With no outstanding feedback and clean application, this appears ready for merging as part of the ongoing test modernization effort.