This is a follow-up discussion in the performance fix thread for the hook subsystem in `git-receive-pack`. The conversation focuses on the broader cleanup of hook-related code beyond the immediate regression fix.

Adrian Ratiu confirms Patrick Steinhardt's question about eventually removing the legacy `find_hook()` function, stating this is indeed the goal. He explains that all hooks should migrate to the new hook.[ch] APIs, which provide more comprehensive functionality like `hook_exists()` that handles both config-defined and traditional hookdir hooks. The current patch series didn't address the remaining `find_hook()` usage for the "proc-receive" hook, but Adrian plans to convert that in a subsequent patch.

The discussion reveals the long-term direction for the hook subsystem - making `find_hook()` static (internal to hook.c) once all external callers have been converted to the new API. This aligns with the project's incremental refactoring approach, where performance-critical fixes (like the current sideband thread optimization) are addressed first, followed by broader API cleanups.

Patrick's question about the "proc-receive" hook being the last remaining user of `find_hook()` in receive-pack highlights an area for future work, which Adrian confirms he intends to tackle. The exchange shows consensus on the technical direction while deferring the remaining conversion work to a separate patch.