This is a design discussion about configurable partial clone filters, specifically addressing how to structure the configuration for URL-specific filter settings. Patrick Steinhardt responds to Alan Braithwaite's proposal for a hierarchical configuration approach with an alternative suggestion.

Patrick argues that the proposed `fetch.partialCloneFilter` configuration should instead be named `clone.<url>.defaultObjectFilter` and should only apply to initial clone operations, not subsequent fetches. His rationale is that converting a full clone to a partial one via fetch requires more careful consideration than the initial clone case.

The discussion has evolved from technical implementation details to configuration philosophy, with Patrick now focusing on the scope of when these filters should apply. This represents a narrowing of the proposed functionality compared to Alan's more comprehensive approach that would affect both clone and fetch operations.

The exchange shows the project's typical pattern of iterating on configuration design, with experienced contributors like Patrick weighing in on naming conventions and behavioral boundaries. The thread appears to converge toward a solution but still needs to reconcile these differing views on scope before the patch can proceed.