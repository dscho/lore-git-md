This is a follow-up discussion about configuring partial clone filters, now focused on the URL-based configuration approach. Junio Hamano responds to Patrick Steinhardt's suggestion of using `clone.<url>.defaultObjectFilter` for partial clone configurations.

Junio expresses agreement with the URL-based approach but raises an important clarification point about URL matching - whether the configuration should match exact repository URLs or use patterns to group similar repositories. This touches on Git's existing urlmatch infrastructure which supports pattern matching for configuration.

The email confirms the consensus that this configuration should only affect clone operations (not subsequent fetches), maintaining the safety boundary Patrick advocated for earlier in the thread. Junio's engagement here suggests the design is nearing completion, with only minor implementation details like URL matching granularity remaining to be settled.

This moves the thread closer to resolution, with the maintainer validating the core approach while identifying one remaining design consideration. The narrowed scope (clone-only) and URL-based configuration represent a compromise from Alan Braithwaite's original proposal but address the safety and configuration philosophy concerns raised during discussion.