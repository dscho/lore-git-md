This thread covers a two-patch series modernizing the test infrastructure in t3700-add.sh, which tests `git add` behavior. The author has iterated through three versions based on Junio's feedback, with the current version consisting of two focused mechanical improvements to test robustness.

The first patch addresses exit code handling in shell pipelines, replacing patterns like `git cmd | grep` with temporary files to properly capture git's exit code, and simplifies line counting tests by using direct grep checks. The second patch, now finalized, converts all remaining direct `grep` and `! grep` calls to use the `test_grep` helper, providing better debugging output when tests fail. Both changes are purely mechanical - they improve failure detection without altering the actual test logic.

The series is part of the ongoing test modernization effort to replace direct shell commands with more reliable test helpers. With both patches addressing Junio's feedback and no outstanding questions, the series appears ready for merging as straightforward improvements to the test suite's reliability. The changes are small (28 insertions and 15 deletions in t3700-add.sh) but follow established patterns for making test failures more detectable.