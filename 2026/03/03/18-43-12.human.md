This is a refactoring patch in Patrick Steinhardt's "git history split" series. It extracts core commit creation logic into a more flexible function to support the new split operation.

D. Ben Knoble asks about the "ext" suffix in the new function name `commit_tree_with_edited_message_ext()`, wondering if it stands for "extracted" or "external". The function generalizes the existing commit creation logic by allowing explicit specification of parent commits and tree objects, rather than deriving them from a single commit. This prepares for the split command which needs to create commits with different parent/tree combinations.

The change is mechanical - the original function becomes a wrapper that maintains existing behavior for `git history reword`, while the new extended function handles the more general case. This is part of the infrastructure work needed before introducing the actual split command in the next patch. Knoble's question suggests he's following along with the series but hasn't yet reviewed the implementation in depth.