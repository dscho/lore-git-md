Performance fix for hook subsystem in `git-receive-pack`, confirming the effectiveness of Adrian Ratiu's patch to restore early-exit optimization for sideband hooks. Patrick Steinhardt provides benchmark results showing the patch successfully reverts a performance regression, with `receive-pack` operations on 10,000 refs now running 2.7x faster than the unoptimized version. The fix uses the new `hook_exists()` API to check for hooks before spinning up async threads, avoiding unnecessary overhead when no hooks are configured.

Patrick notes the fix is "delightfully simple" and confirms through both local benchmarks and the project's Bencher infrastructure that performance has returned to pre-regression levels. He raises a tangential question about whether the older `find_hook()` function should eventually be removed, particularly noting its continued use for the "proc-receive" hook, but considers this a separate issue that could be addressed in future work.

The email serves as both a technical confirmation of the patch's effectiveness and a positive signal that this performance regression has been successfully addressed. With Junio C Hamano having already indicated he will queue the improved version of the patch, this thread appears to be reaching consensus on the solution. The benchmarks provide concrete evidence that the optimization matters in real-world scenarios with large numbers of refs.