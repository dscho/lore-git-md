This is a test modernization patch in a series improving t3700 (git add tests). The patch focuses on making test failures more detectable by avoiding suppressed exit codes in shell pipelines.

The key changes in this v3 patch:
- Replaces `git cmd | grep` patterns with explicit temporary files to ensure git's exit code isn't lost
- Simplifies line counting tests by replacing `wc -l` with direct `grep`/`! grep` checks
- Affects multiple test cases in t3700-add.sh that verify file modes, ignored errors, and chmod behavior

The changes are mechanical improvements to test robustness rather than functional changes to the tests themselves. This follows Junio's suggestion to split the original changes into focused patches. The patch is part of the ongoing test modernization effort to replace direct shell commands with more reliable test helpers.

The diff shows 28 insertions and 15 deletions across t/t3700-add.sh, with each change following the same pattern of making exit codes explicit. No behavioral changes to the actual tests - just improved failure detection. The patch appears ready for merging as a straightforward test infrastructure improvement.