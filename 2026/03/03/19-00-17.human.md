This is a follow-up to a bugfix patch series addressing submodule fetching behavior. The original series fixed a long-standing issue where submodule fetches incorrectly assumed the remote was always named "origin". The current email reveals two test infrastructure issues that slipped through earlier review:

1. `test_when_finished` calls incorrectly placed in subshells in t5526-fetch-submodules.sh, causing test failures in CI (this was already merged to the 'seen' branch)
2. Some test cases using `$pwd` instead of the correct `$(pwd)` syntax

The author, Nasser Grainawi, notes these issues weren't caught earlier because their local Ubuntu environment (using dash as /bin/sh) didn't surface them. They plan to send fixes for both problems. The core functionality of the patch remains sound - this is purely about test script correctness. The thread shows the typical Git development cycle where even well-reviewed patches sometimes reveal edge cases in CI that need follow-up fixes.