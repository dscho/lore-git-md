This is a documentation patch that adds information about Git's support for external commands in PATH. The thread shows Junio Hamano providing final review feedback on Omri Sarig's v3 patch to document how Git runs external `git-*` executables found in PATH as if they were built-in commands.

Junio approves the core content but suggests minor wording improvements: dropping redundant "runnable" phrasing and clarifying the interaction with aliases. He also proposes an additional change to cross-reference this feature from the "GIT COMMANDS" section in `git.adoc`, helping users discover both aliases and external commands more easily.

The patch itself is straightforward - it documents existing behavior that has been in Git since at least version 1.5.0 (implemented in `execv_dashed_external()`). The documentation explains the lookup order (built-ins first, then external commands in PATH, then aliases) and how parameters are passed through. This makes a useful but under-documented extensibility mechanism more discoverable.

The review discussion is constructive, with Junio providing Git's conventional style guidance for commit messages and documentation phrasing. The patch appears ready for merging after these final tweaks, representing a clear improvement to Git's user documentation without any technical changes to the code.