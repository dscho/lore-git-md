This is the final patch in Derrick Stolee's v4 series improving environment handling in `git for-each-repo`. The patch simplifies parameter passing by using `strvec_pushv()` instead of manually iterating through arguments, as suggested by Jeff King in earlier reviews.

The change is straightforward - it removes the now-unused `argc` parameter and replaces the argument-copying loop with a single `strvec_pushv()` call. This matches Git's common patterns for command argument handling and makes the code more concise while maintaining the same functionality.

The patch builds on the previous work in the series that established proper environment isolation and worktree support. With this final cleanup, the series appears ready for merging, having addressed all review feedback through four iterations. The changes are mechanical and unlikely to be controversial, focusing purely on code quality improvements rather than behavior changes.