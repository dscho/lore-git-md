This is a follow-up discussion about a test flakiness issue in the submodule remote handling patch series. The core functionality has already been reviewed and approved, but Ramsay Jones has identified an intermittent test failure in t5526-fetch-submodules.sh where the order of fetch output lines varies between runs.

The test expects a specific sequence of progress messages when fetching from multiple submodules with custom remote names, but the parallel nature of the fetches means the output order is not deterministic. Junio Hamano confirms this is not a real bug in the functionality - just an overly strict test assertion. The test failure occurs in case 44, which checks fetch behavior with custom remote names.

This is the final remaining issue before the series can be fully integrated. The discussion suggests relaxing the test to either sort the output or use more flexible matching rather than requiring an exact sequence. The core submodule remote handling changes themselves remain technically sound and have received positive reviews from Jacob Keller and Junio across multiple iterations.

The thread demonstrates how even after technical approval, real-world testing can uncover edge cases in test infrastructure that need addressing. The solution here will likely involve making the test more robust while preserving its ability to verify the underlying functionality works correctly.