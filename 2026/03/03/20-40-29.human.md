This is a test modernization patch, the second in a series of two that updates the `t3700-add.sh` test script. The patch converts remaining direct `grep` and `! grep` invocations to use the `test_grep` helper function, following Junio Hamano's suggestion. 

The changes are mechanical and focused on improving test robustness - `test_grep` provides better debugging output by automatically showing file contents when a check fails. The patch touches 18 test cases in t3700-add.sh, consistently replacing grep patterns with their `test_grep` equivalents while maintaining the same test logic. 

This continues the test modernization effort to replace direct shell commands with more reliable test helpers. The changes are straightforward with no behavioral impact on the actual tests themselves, just better failure reporting. The series appears ready for merging as a clean improvement to test infrastructure.