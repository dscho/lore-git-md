The existing thread summary and new email cover two entirely separate topics that should not be combined. The `for-each-repo` worktree fix series by Derrick Stolee is unrelated to brian m. carlson's SHA-1/SHA-256 interoperability work. These should remain as distinct thread summaries.

Here is the correct standalone summary for the SHA-1/SHA-256 interoperability thread:

brian m. carlson confirms their SHA-1/SHA-256 interoperability branch `bc/sha-256-interop-02` is ready for merging into Git's `next` integration branch. This 16-commit series introduces Rust infrastructure for maintaining mappings between objects stored with different hash algorithms (SHA-1 and SHA-256), representing a major step in the long-running effort to enable cross-hash repository operations. Key technical components include new Rust wrappers for hashfile operations, object ID handling, and algorithm abstraction, plus a binary object map format for tracking conversions between hash algorithms.

The work builds on carlson's foundational changes making Git's internals hash-agnostic, though they have previously stated full interoperability shouldn't block Git 3.0. Junio Hamano had already marked this for tentative inclusion in `next` in his "What's cooking" report, indicating technical consensus. The current email serves as final confirmation from the author that no further changes are needed before merging. Given the complexity of hash algorithm interoperability and the Rust dependencies being introduced, this would be one of the more significant changes queued for the upcoming release cycle. The series includes Rust bindings for hash context functions and infrastructure to safely convert between algorithms while maintaining the project's security guarantees.