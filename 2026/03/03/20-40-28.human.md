Test modernization patch improving failure detection in t3700 (git add tests). This is the first of two patches in a v3 series that has already incorporated Junio's feedback to focus the changes.

The patch addresses a subtle issue where test pipelines like `git cmd | grep` would mask git's exit code if it failed. The changes replace these with temporary files to properly catch git failures, and simplify line-counting checks by using `! grep` instead of `wc -l`. These are mechanical improvements to make test failures more detectable without changing test behavior.

The changes touch multiple test cases in t3700-add.sh, all following the same pattern of making test assertions more robust. The patch is part of the ongoing test modernization effort to replace direct shell commands with more reliable test helpers. With no outstanding objections from reviewers, this straightforward improvement appears ready for merging.