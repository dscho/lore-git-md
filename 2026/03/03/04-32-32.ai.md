This is a technical discussion about path formatting approaches for the `git repo-info` command, continuing the thread about adding path-related fields. Tian Yuchen advocates for using `ref-filter.c`'s format modifier parsing infrastructure rather than creating new ad-hoc syntax, arguing it provides clearer implementation and better extensibility.

The email makes several key points:
1. The `ref-filter.c` approach uses a clean registry-based system with dedicated parser functions for each format type, avoiding the "ancient technical debt" of `rev-parse`
2. Demonstrates how a `path` field could be added to the existing atom system with minimal code
3. Argues this provides better scalability for future additions like combined format specifiers (`%(path:commondir,relative)`)
4. Maintains that user-friendliness doesn't have to come at the cost of maintainability

The discussion remains technical but shows some convergence - while earlier proposals differed more substantially, Tian now frames this as choosing the right underlying mechanism rather than fundamentally different syntax. The thread continues weighing plumbing command constraints against output flexibility, with Junio Hamano's preference for simplicity still influencing the direction.