This is a documentation clarification regarding the deprecation of `git whatchanged`. Kristoffer Haugsbakk responds to Antoine Cousson's inquiry about the command's future, confirming its planned removal in Git 3.0 (though no release date is set) and providing equivalent `git log` invocations.

The response notes that `git whatchanged` and `git log` share the same underlying machinery, just with different defaults. It offers specific replacement commands: `git log --no-merges --raw` for identical output, and suggests `--stat` or `--name-only` as more human-readable alternatives to `--raw`. The reply links to Git's BreakingChanges documentation but doesn't address why the deprecation warning wasn't more visible before now.

This exchange highlights a common pattern in Git's evolution - consolidating functionality into fewer commands while maintaining backward compatibility through equivalent flags. The thread may prompt discussion about whether the deprecation timeline is appropriate or if `git log`'s defaults should better accommodate this use case.