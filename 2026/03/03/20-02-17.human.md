This is a bugfix patch in the submodule series that addresses a final cleanup issue in the test scripts. The thread has already established the core technical solution for fetching submodules from their correct remotes (not just assuming "origin"), and this email resolves the last remaining question about shell variable usage in the test scripts.

Junio Hamano confirms that using `$pwd` (a variable set earlier) is actually correct and makes the test cleanup simpler, contrary to Nasser Grainawi's initial concern. The fix involves moving `test_when_finished` cleanup handlers outside of subshells since they reference this pre-set variable. This is a minor test infrastructure tweak that doesn't affect the core submodule functionality.

The patch series is now fully resolved with all feedback incorporated, including Junio's earlier suggestions about parse_options() removal and test portability. Jacob Keller has reconfirmed the technical approach, and the only remaining action is moving these cleanup handlers - a straightforward change that maintains the existing behavior while making the tests more robust. The series is queued for integration with no outstanding issues.