This is a technical discussion in the ongoing thread about adding path-related fields to `git repo-info`. Jayatheerth K responds to Tian Yuchen's proposal to use `ref-filter.c` infrastructure for path formatting, noting that their own earlier suggestion of virtual fields (with format variants as separate fields) shares the same underlying approach despite different syntax.

The key points are:
- Both proposals aim to avoid global flags and support per-field formatting
- Jayatheerth shows how their virtual fields approach could parse format variants similarly to how `ref-filter` handles atom modifiers
- The discussion highlights the architectural similarity between the two approaches, with both separating field identity from formatting modifiers

This email doesn't introduce new functionality but rather clarifies the technical alignment between two competing proposals in the thread. The core debate remains whether to use `ref-filter`'s existing infrastructure or build a parallel system specifically for `repo-info`. Junio Hamano's preference for plumbing simplicity still looms over the discussion.