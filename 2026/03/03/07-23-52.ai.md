This is a technical discussion about path formatting approaches for the `git repo-info` command, specifically comparing different implementation strategies for handling path format variants (absolute vs relative).

Jayatheerth K responds to Tian Yuchen's proposal to use `ref-filter.c` infrastructure for path formatting by noting the structural similarity to Ayush Jha's earlier "virtual fields" approach. The key insight is that both proposals separate the path field identity (e.g. "path.toplevel") from its format modifier ("absolute"/"relative"), just with different syntactic representations.

The email provides concrete code examples showing how Ayush's approach could normalize variant keys like "path.absolute.toplevel" into base fields while setting format options, mirroring the atom parsing pattern Tian proposed from `ref-filter.c`. Jayatheerth emphasizes this isn't an argument against Tian's solution, but rather an observation that both designs share the same underlying architecture of separating field identity from presentation modifiers.

The discussion remains technical, focusing on implementation mechanics rather than reopening the broader design debate. Both approaches appear viable, with the choice likely coming down to which existing subsystem (`ref-filter.c` vs custom parsing) provides better long-term maintainability for this plumbing command's needs. Junio Hamano's earlier preference for simplicity in the plumbing context still looms over these implementation details.