This is a feature proposal for configurable partial clone filters, now evolving into a discussion of configuration design. The patch author Alan Braithwaite responds to Patrick Steinhardt's earlier feedback by proposing a refined approach using URL-specific configuration patterns.

The new design suggests a `fetch.partialCloneFilter` hierarchy that allows both global defaults and per-URL overrides, modeled after Git's existing `http.<url>.*` configuration pattern. Key aspects include support for arbitrary filter specifications (not just blob size limits), URL matching using the existing urlmatch infrastructure, and clear precedence rules between command-line flags, per-remote config, and these new settings.

The proposal addresses Patrick's concerns about flexibility while maintaining backward compatibility. It would affect both `git clone` and `git fetch` operations, with the configuration primarily relevant for initial clones since subsequent fetches would use the remote's established filter settings. The thread shows productive collaboration, with the author incorporating feedback while maintaining the original goal of making partial clone filters more configurable.