This is a test modernization patch, part 2 of a series (though part 1 wasn't provided in the context). The patch updates `t/t7412-submodule-absorbgitdirs.sh` to use Git's test helper functions instead of raw shell conditionals for path checks.

The changes replace direct shell tests (`test -f`, `test -d`, `! test -e`) with their more descriptive test helper equivalents (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`). These helpers provide better failure diagnostics than the silent pass/fail of shell primitives. The patch makes 11 such substitutions across various test cases that verify submodule directory and file states after running `git submodule absorbgitdirs`.

The modifications are mechanical and don't affect test behavior - they only improve failure reporting. The patch maintains all existing test assertions while making the test output more maintainer-friendly. The commit message clearly explains the motivation and scope of changes. This appears to be a straightforward improvement to test hygiene.