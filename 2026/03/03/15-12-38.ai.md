This is a documentation patch fixing AsciiDoc formatting in the alias configuration man page, part of the ongoing UTF-8 alias subsection implementation series that's currently in `next`. The patch adds missing list continuation markers (`+`) after code blocks in `Documentation/config/alias.adoc` to ensure proper rendering of the generated man page.

The change is straightforward - it inserts a `+` marker after the example showing how `git last` maps to `git cat-file commit HEAD`, maintaining proper list structure in the documentation. This addresses the last minor polish item identified in the thread, following Kristoffer Haugsbakk's earlier documentation review. The patch has already been signed off by the author (Jonatan Holmgren) and appears ready for inclusion.

This documentation fix completes all outstanding items for the UTF-8 alias subsection feature except for one remaining regression with dot-prefixed aliases. The series is otherwise functionally complete with memory leaks fixed, edge cases handled, and now all documentation properly formatted. The change touches only the alias.adoc file and doesn't affect runtime behavior.