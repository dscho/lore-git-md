This is a technical discussion about implementing path format variants in `git repo-info`, continuing the ongoing thread comparing three approaches. Tian Yuchen responds to Jayatheerth K's analysis of the virtual fields approach, noting that while the core idea of separating field identity from presentation is shared with Tian's own `ref-filter.c`-based proposal, the implementation details differ in ways that affect functionality.

Tian points out that Jayatheerth's code sketch for virtual fields would not support mixed-format outputs (like `path.absolute.toplevel,path.relative.gitdir` in a single command) because it uses a global state for path format. Supporting this would require per-field format tracking, essentially recreating the `struct used_atom` machinery from `ref-filter.c`. The exchange highlights how both proposals share the same architectural insight but differ in implementation complexity and subsystem integration.

The discussion remains technical and constructive, with both participants clearly understanding the tradeoffs. Tian maintains that leveraging `ref-filter.c` avoids reinventing existing infrastructure, while acknowledging Jayatheerth's point about the conceptual similarity between the approaches. The thread shows no signs of contention, just careful consideration of implementation details as the design converges.