This is a refactoring patch in Patrick Steinhardt's "git history split" series. The patch extracts a more flexible commit creation function from the existing `commit_tree_with_edited_message()` to support the split operation's need for explicit control over commit parents and trees.

The new `commit_tree_with_edited_message_ext()` function generalizes the commit creation logic by accepting explicit parent commits and tree objects rather than deriving them from a single commit. The original function becomes a wrapper that maintains the existing reword behavior. D. Ben Knoble asks about the "ext" suffix in the function name, suggesting it might stand for "extended" or "extracted" but noting the meaning isn't immediately clear from context.

The changes are mechanical and focused on preparing infrastructure for the split command in the next patch. The refactoring maintains all existing functionality while enabling the new use case. This follows the pattern seen throughout the series of carefully separating concerns and generalizing shared logic.