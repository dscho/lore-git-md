Feature patch series finalizing comprehensive cover letter formatting for `git format-patch`. This sixth and final iteration from Mirko Faina implements configurable cover letter generation through both command-line (`--cover-letter-format`) and configuration (`format.commitListFormat`) interfaces while maintaining backward compatibility with the traditional shortlog-based output.

The series introduces new `%(count)` and `%(total)` placeholders in `pretty.c` for patch numbering, refactors the shortlog generation into a dedicated function, and adds strict validation to limit placeholder usage to cover letters. The implementation cleanly separates concerns between the new `generate_commit_list_cover()` function and the existing shortlog machinery, with clear precedence rules between CLI and config options.

Documentation in the final patch provides thorough usage examples and clarifies the "log:" prefix requirement for custom formats. The test coverage in `t4014-format-patch.sh` exercises both the new formatting capabilities and backward compatibility. The range-diff shows only minor wording tweaks from v5, with all technical feedback addressed and Junio's sign-off now removed as the series is ready for merging.

This represents the culmination of five iterations that have refined the design based on feedback from Jeff King (Peff) and Junio Hamano, particularly around the numbering approach and configuration behavior matrix. The implementation is production-ready with no outstanding issues, offering users flexible cover letter formatting while preserving the existing shortlog output as the default.