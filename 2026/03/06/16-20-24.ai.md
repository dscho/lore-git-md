This is a technical discussion about the `git replay` revert functionality, specifically addressing two implementation details in the v4 patch series. The email resolves two open questions from earlier reviews:

1. **Commit ordering for reverts**: Confirms that reverts should process commits in chronological (creation) order (newest-first) rather than reverse chronological order, matching `sequencer.c`'s behavior for `git revert`. This reduces conflict likelihood. The implementation handles parent chaining correctly by using `last_commit` as the fallback base.

2. **Empty revert commits**: Decides to drop empty revert commits by default, aligning with `git cherry-pick`'s behavior rather than preserving them as interactive `git revert` does. This resolves an inconsistency in the initial implementation where empty reverts were kept for non-interactive operation.

The changes reflect Phillip Wood's review feedback and maintain consistency with existing Git behaviors. The discussion shows the patch author (Siddharth Asthana) incorporating this feedback into the upcoming v4 revision, indicating the series is nearing technical completion with these final polish items addressed. The thread context shows broader interface design questions (subcommands vs flags) remain open but are being deferred to avoid scope creep in this series.