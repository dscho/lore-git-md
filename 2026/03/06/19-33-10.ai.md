This is a follow-up discussion in the concluded thread about configurable partial clone filters via URL patterns. Junio C Hamano responds to brian m. carlson's late-stage concerns about the feature's potential impact on scripts expecting full clones.

Junio acknowledges brian's point about script reliability but expresses a personal preference for having this configuration directly in Git rather than limiting it to `scalar`. He notes that clone is a porcelain command where user experience improvements are expected, though he seems open to the scalar alternative. The key technical question Junio raises is whether the implementation provides a way to explicitly override the configured filter from the command line (which it does via `--filter`).

This exchange represents final discussion points after the feature's technical implementation had already been approved. Junio's response suggests he sees value in the URL-based configuration approach while recognizing the trade-offs brian raised about script expectations. The thread had already established that command-line filters take precedence over config, addressing Junio's specific concern about ensuring users can force full clones when needed.