This is a follow-up discussion about a bugfix for `git clone` that addresses a segfault when using `--revision` with servers that ignore ref filtering. The original patch from Adrian Friedli proposed a straightforward fix by adding a NULL check for `peer_ref` in `update_remote_refs()`.

Junio Hamano, the Git maintainer, raises some deeper questions about the underlying assumptions in the code. He suggests the issue might not just be a simple NULL check, but could indicate a more fundamental problem with how `guess_remote_head()` handles refs when servers don't respect the requested filtering. Junio wonders if other callers of `guess_remote_head()` might be affected, and whether the code should be more robust by properly handling cases where the first ref in the list might be invalid.

The discussion is still early - this is Junio's first response to the patch. While the immediate segfault fix is simple, Junio's questions suggest the solution might need to be more comprehensive to properly handle edge cases with misbehaving servers. The thread is likely to continue with either a defense of the simple fix or a proposal for a more thorough solution.