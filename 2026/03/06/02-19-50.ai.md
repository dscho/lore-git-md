This is a test infrastructure issue report from Ivan Ivanov about credential and HTTP-related test failures on Arch/Artix Linux systems. The email provides additional details about test t5564 ("clone can prompt for proxy password") failing with password prompts, showing the test output where it attempts to use a SOCKS proxy. 

The key new information is that the test failures now extend beyond the initial credential helper tests to include HTTP protocol tests (t5564 and others), all exhibiting similar symptoms of unexpected password prompts. Ivan confirms they are working around the issue by skipping affected cases via `GIT_SKIP_TESTS` but remains puzzled why the distro packager didn't encounter these problems. 

The thread has established that the root cause is test execution from `/dev/shm` (mounted with `noexec` flags), preventing helper script execution. This affects multiple test categories that rely on executable scripts - credentials, HTTP auth, and now proxy handling. The discussion is exploring whether these failures share a common underlying cause despite differing symptoms, and whether test infrastructure should better handle `noexec` environments. 

Technical details show the SOCKS proxy test succeeds when properly configured, suggesting the initial failure may be related to the execution environment rather than the test logic itself. The email continues the investigation into test portability across different system configurations.