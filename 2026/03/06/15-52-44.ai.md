This is a technical discussion about the `git replay` revert functionality, specifically addressing two implementation details in the ongoing patch series.

Phillip Wood and Siddharth Asthana are working through edge cases in the revert operation's behavior. The key points resolved in this exchange:

1. **Commit ordering for reverts**: They confirm reverts should process commits in chronological (creation) order rather than reverse chronological order to minimize conflicts. Siddharth notes this matches `sequencer.c`'s behavior for `git revert` and provides a straightforward implementation fix to properly chain commits when processing newest-first.

2. **Empty revert commits**: Phillip questions why empty reverts are preserved (unlike empty picks), arguing it's not useful. Siddharth explains this was for consistency with interactive `git revert` which stops on empty commits, but acknowledges the non-interactive `replay` could reasonably drop them. Phillip prefers dropping empty reverts to match cherry-pick behavior, and they agree erroring out would be inconsistent with other operations.

The discussion shows careful attention to edge case behavior and consistency with existing Git commands. Phillip's reviews continue to refine the implementation details while Siddharth demonstrates responsiveness to feedback. This represents the polishing phase of a feature that has already undergone significant design review from multiple core contributors.