This is a build system patch in Jeff King's series addressing memory leaks in mmap handling. The thread has already established that the core issue involves improper cleanup of pack index mmaps under sanitizers, with the solution being to force `NO_MMAP` (which converts mmaps to heap allocations) when building with LSan (LeakSanitizer). 

Ramsay Jones confirms that the meson build system change aligns with the Makefile behavior from earlier in the series, noting that platforms requiring `NO_MMAP` (like Cygwin and NonStop) are unlikely to be using meson builds. His comment about `NO_MMAP` not being in `GIT-BUILD-OPTIONS` hints at a potential follow-up consideration, but doesn't raise it as an issue needing resolution here.

The series is now complete with all five patches reviewed and ready for merging. This final meson patch ensures consistent leak detection behavior across both build systems when sanitizers are enabled, closing out the thread. No further discussion is expected.