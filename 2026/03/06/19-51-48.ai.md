Maintainer feedback on a clone bugfix. Junio C Hamano responds to Adrian Friedli's patch that addressed a segfault in `git clone --revision` when servers ignore ref_prefixes filtering.

Junio raises deeper questions about the underlying assumptions in the ref handling code. While acknowledging the immediate fix works, he probes whether the issue might indicate a more fundamental problem in `guess_remote_head()`'s contract with its callers. He notes there are four callers to this function and wonders if they all handle the case where a returned ref has no peer_ref. 

The maintainer suggests the solution might need to be more comprehensive - perhaps iterating through the `remote_head_points_at` list to find the first valid entry with a peer_ref, rather than just checking the first element. This implies the current fix might be treating a symptom rather than the root cause.

The technical discussion centers on the interaction between ref advertisement handling and the clone machinery's expectations. Junio's response indicates this may need more design consideration before settling on the minimal one-line fix, as the behavior could affect other code paths. The thread appears headed toward a more thorough analysis of how `guess_remote_head()` should work when servers don't fully respect the requested ref filtering.