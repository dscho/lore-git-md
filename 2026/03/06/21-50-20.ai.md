This is the final patch in a thread implementing configurable partial clone filters via URL patterns (`clone.<url>.defaultObjectFilter`). The patch addresses Junio C Hamano's request to ensure users can override the configured filter with `--no-filter` to force a full clone.

The implementation now explicitly checks for `--no-filter` and makes it override any URL-based filter configuration, providing a clear escape hatch. This addresses brian m. carlson's concern about scripts needing predictable behavior while maintaining the feature's usefulness for interactive users. The thread has reached consensus on the design, with Junio indicating he sees value in having this in core Git despite brian's suggestion to put it in `scalar`. The patch includes comprehensive test coverage and documentation updates, with all technical concerns from the review process now resolved. The feature is ready for integration, providing URL-based configuration for partial clone filters while maintaining clear boundaries by limiting to clone operations and preserving command-line override capabilities.