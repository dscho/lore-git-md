Thread identity: Question about Git's HTTP transport components - specifically the difference between `git-http-fetch` and `git-remote-http(s)`.

Core problem: Clara Engler seeks clarification on why Git maintains both `git-http-fetch` and `git-remote-http(s)` as separate components when both use libcurl for HTTP(S) communication. This is an architectural question about the design rationale behind having these distinct but related mechanisms in Git's object transfer system.

Current status: The thread is just starting with this initial question. No answers or technical explanations have been provided yet.

Key technical details: The question implies both components are HTTP-related and use libcurl, but serve different purposes in Git's architecture. The distinction between them isn't immediately clear from their names or high-level descriptions.

Open questions: The primary open question is why these two components exist as separate entities and what their respective roles are in Git's HTTP transport layer. This may lead to follow-up questions about historical context, performance characteristics, or protocol support differences.

Participant positions: Clara Engler has raised the initial question. No other participants have responded yet. Potential respondents might include HTTP transport experts or contributors familiar with Git's remote helper architecture.

Related work: This touches on Git's broader remote helper architecture and HTTP transport implementation, but no specific related patches or threads are mentioned yet. The answer may reference historical development of these components.