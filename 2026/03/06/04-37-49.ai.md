This is a historical note about mmap-related memory management issues, shared by Ramsay Jones in response to Jeff King's patch series fixing mmap leaks. Ramsay recalls a similar issue from 2012 when Cygwin builds used NO_MMAP, where a double-free bug in the mmap emulation layer caused intermittent failures. He includes a patch he wrote at the time that maintained a descriptor list to track mmap allocations and prevent double-frees, though he never merged it since Cygwin later dropped NO_MMAP.

The context shows this isn't a new issue - the current leak fixes touch on long-standing challenges with Git's platform-specific memory management. Ramsay's anecdote highlights how build configuration quirks (like NO_MMAP) can surface subtle resource management bugs that otherwise remain hidden. His included patch demonstrates an alternative approach to the same class of problems Jeff is now fixing, though the current solution takes a different direction by focusing on proper cleanup rather than tracking allocations.

This email doesn't require any action on the patch series (which is already reviewed and ready), but provides useful historical context about similar issues in the mmap compatibility layer, particularly for platform-specific builds. The thread shows how Git's cross-platform requirements can lead to subtle resource management challenges that span years and multiple contributors.