This is the final documentation patch in a series implementing configurable cover letter formatting for `git format-patch`. The patch adds comprehensive documentation for both the new `--cover-letter-format` CLI option and the related `format.commitListFormat` configuration variable.

The documentation explains:
1. The `format.commitListFormat` config option accepts the same values as `--cover-letter-format`, with "shortlog" as the default
2. The format string syntax requires a "log:" prefix for custom formats
3. Example usage showing how to specify alternative formats like `log: %s (%an)`
4. Integration with the existing configuration examples section

This completes the series which has gone through six iterations and received maintainer sign-off. The implementation cleanly modularizes the shortlog generation while adding flexible formatting capabilities, with strict validation of the new `%(count)` and `%(total)` placeholders. The documentation matches the final agreed design where custom formats must be prefixed with "log:" to distinguish them from the special "shortlog" case.

The patch follows Git's standard documentation style, adding clear explanations to both the config and command man pages while maintaining consistency with existing formatting options. As the final patch in a well-reviewed series, it's ready for merging with no outstanding issues.