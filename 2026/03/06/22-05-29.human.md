This is a technical discussion about the `NO_MMAP` compatibility layer in Git's build system, specifically around edge cases in memory management. The thread has progressed from an initial bug report about memory leaks during clone operations to a deeper examination of how the mmap emulation behaves when compiled with `NO_MMAP`.

Junio C Hamano is seeking clarification from Ramsay Jones about whether the current behavior of the emulation layer - which doesn't detect double `munmap()` calls and thus can trigger double-free bugs - is actually a problem in practice. Ramsay confirms that while the emulation doesn't match real `mmap()` behavior perfectly (which would return an error for duplicate unmaps), this only manifests if code incorrectly calls `munmap()` multiple times on the same region - something that would already be invalid usage.

The discussion is wrapping up final technical details before merging Jeff King's 5-patch series that fixes mmap-related memory leaks. The patches are technically sound and have consensus, with this exchange serving as last-mile verification of an edge case. The series addresses real issues in packfile handling while maintaining compatibility with platforms that can't use real mmap().

This email doesn't introduce new changes but rather confirms that the existing solution is correct, removing the last potential blocker for merging the fixes. The thread shows Git's careful approach to platform compatibility and memory management, even for edge cases.