This is a test infrastructure issue thread where Ivan Ivanov reports additional test failures on Arch/Artix Linux beyond the initial credential helper problems. The new failures involve HTTP proxy authentication (t5564) and show similar symptoms - password prompts appearing during test execution - though the root causes may differ from the earlier credential helper issues.

The thread has established that `/dev/shm` being mounted with `noexec` is preventing helper scripts from running, but Ivan notes the test failures manifest differently in the output. He's currently working around the issue by skipping affected suites via `GIT_SKIP_TESTS`, though he's puzzled why the distro packager didn't encounter these same problems. The discussion now includes whether the test infrastructure should better detect or handle `noexec` environments.

The specific t5564 failure shows Git prompting for a proxy password when it shouldn't, suggesting the askpass mechanism isn't working as expected in this environment. The test output indicates the SOCKS proxy test portion does work correctly, highlighting the inconsistent behavior. This adds to the growing list of affected test suites that now includes several HTTP protocol tests beyond the initial credential helper cases.