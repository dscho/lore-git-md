This is a technical refactoring patch in the ongoing `git replay` revert capability series. Siddharth Asthana responds to Toon Claes' feedback about the split handling of `commit_use_reference` between the new `sequencer_format_revert_header()` helper and its caller. The patch originally moved revert message formatting into a shared function but left some reference handling in the caller, creating an awkward split.

Asthana acknowledges the confusion and indicates he will adopt Phillip Wood's suggested approach for v4 - consolidating all message generation (including reference handling) into a single `sequencer_format_revert_message()` function with a boolean parameter for reference style. This addresses both the immediate code organization concern and Patrick Steinhardt's earlier suggestion about API boundaries.

The exchange shows the patch series maturing through review, with the author receptive to structural improvements even as the core functionality remains sound. The thread has established consensus on moving forward with Phillip's refactoring approach, which should clean up the internal API while maintaining the same behavior. This is preparatory work for the larger `git replay` revert feature that GitLab's Gitaly service needs.