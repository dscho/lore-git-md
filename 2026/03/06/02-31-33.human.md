This is a documentation discussion thread about `git stash` pathspec behavior. Mirko Faina responds to Aron Sigfridsson's original question about whether `git stash` pathspecs are interpreted relative to the current directory or repository root, noting that while the documentation doesn't specify this, testing shows paths are relative to the current directory.

Mirko observes this behavior matches `git status` and suggests it might be worth documenting, though notes most Git commands don't explicitly state their pathspec interpretation rules. The reply implies this may be considered obvious behavior that doesn't require documentation, as users can easily test the behavior themselves.

The exchange highlights a small but potentially confusing documentation gap in the `git stash` man page regarding pathspec resolution. No consensus has yet formed about whether this warrants a documentation update, as this is the first reply in the thread. The tone is matter-of-fact, treating this as a minor clarification rather than a significant behavioral issue.