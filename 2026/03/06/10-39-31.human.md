This is a dissenting opinion in the concluded thread about configurable partial clone filters via URL patterns. brian m. carlson raises concerns about the potential for unexpected behavior when scripts clone repositories without realizing a default filter is in effect.

The argument focuses on the principle of least surprise - suggesting that clone operations should remain predictable by default, with partial clones being an explicit opt-in choice. They propose that such configuration belongs in `scalar` rather than core Git, since scalar is specifically designed for large-repository workflows where these optimizations are expected.

This perspective contrasts with the thread's consensus that URL-based filter configuration provides a useful quality-of-life improvement. The concern about offline scenarios highlights a real-world edge case where the feature could cause problems, though the counterargument would be that partial clones are already an advanced feature requiring user awareness.

The email doesn't propose any technical changes to the implementation, but rather questions the fundamental premise of making partial clone behavior configurable in this way. It's notable as a dissenting voice in an otherwise settled discussion, representing a more conservative approach to Git's configuration surface area.