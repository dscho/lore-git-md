This is a follow-up discussion in Jeff King's mmap leak-fixing series, where Ramsay Jones provides historical context about similar mmap-related issues from Cygwin builds in 2012. The thread has already seen a complete 4-patch series from Jeff that fixes several memory leaks in pack index handling, all of which have been reviewed and are ready for merging.

Ramsay recalls an old Cygwin issue where `NO_MMAP` emulation caused double-free bugs that were difficult to reproduce, leading him to create (but never merge) a debug patch that tracked mmap blocks to prevent double-deletes. Jeff acknowledges this could indicate a lingering race condition in mmap handling, though it may have been fixed incidentally since 2012. He expresses distaste for Ramsay's tracking approach, preferring to fix the root cause rather than add validation layers.

The exchange highlights how platform-specific build configurations have historically exposed subtle resource management issues in Git's mmap handling. While the current leak fixes are complete, this tangent suggests there may be more mmap-related bugs lurking in edge cases, particularly around race conditions in packfile access. The discussion remains technical and good-natured, with Jeff's response maintaining focus on proper resource lifecycle management rather than defensive validation.