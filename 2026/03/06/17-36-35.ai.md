This is a follow-up in the thread about test failures on `/dev/shm` with `noexec` flags. Ivan Ivanov responds to Jeff King's suggestion that supporting `noexec` filesystems would add unnecessary complexity to the test infrastructure.

Ivan proposes an alternative approach - rather than copying the askpass helper to the test directory (which fails when that directory is on a `noexec` filesystem), the test could execute askpass from its original location. This would avoid the need for the more complex two-directory solution Jeff described.

Jeff had previously argued against making infrastructure changes to support `noexec` cases, advocating instead for simpler workarounds like using `GIT_SKIP_TESTS` or `TEST_OUTPUT_DIRECTORY=/tmp`. The core disagreement here is about whether the test infrastructure should be made more complex to handle edge cases like `noexec` mounts, or whether users should simply avoid those configurations when running tests.

The thread continues to explore the tradeoff between test portability and infrastructure complexity, with Ivan representing the "more portable" perspective and Jeff maintaining that the current simple workarounds are sufficient. The askpass helper execution location emerges as a specific technical point where these philosophies could be reconciled.