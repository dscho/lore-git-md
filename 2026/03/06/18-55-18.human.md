This is a technical clarification in the ongoing thread about mmap-related memory leaks in Git's pack index handling. Ramsay Jones confirms Junio C Hamano's understanding of the `NO_MMAP` emulation layer's double-free vulnerability, explaining that while the real `munmap()` would safely reject duplicate calls with `EINVAL`, the emulation layer incorrectly performs a second `free()` in this case. He clarifies that single-use calls work correctly, and the issue only manifests with duplicate cleanup attempts.

The exchange serves as final vetting before merging Jeff King's 5-patch series that fixes mmap-related leaks during clone and fsck operations. Ramsay's confirmation addresses Junio's last remaining question about edge case behavior in the compatibility layer, clearing the way for the series to proceed. The discussion remains focused and technical, with no new patches or objections raised.