Thread identity: Git GUI patch series adding automatic maintenance runs after commits, bringing feature parity with command-line `git commit` behavior.

Core problem/goal: When `maintenance.auto` is enabled, `git-gui` should automatically run maintenance tasks after commits, matching the CLI behavior. The GUI implementation intentionally differs by always detaching the maintenance process rather than honoring `maintenance.autoDetach` since blocking the UI would degrade user experience.

Current status: First patch in series adds 6 lines to `git-gui/lib/commit.tcl` inserting `git maintenance run --auto` between rerere and post-commit hook phases. No version history yet as this appears to be the initial submission.

Key technical details: Modifies commit sequence in GUI's Tcl code. Touches only `git-gui/lib/commit.tcl`. Behavior differs from CLI by always detaching maintenance process regardless of `autoDetach` setting.

Open questions: No test coverage included yet - may need verification of the detached maintenance behavior in GUI context. Series appears to have 3 additional patches (cover letter missing).

Participant positions: Only author participation so far (patch explains design difference from CLI behavior).

Related work: Builds on existing `git maintenance` infrastructure and command-line commit behavior. Part of ongoing GUI maintenance improvements.