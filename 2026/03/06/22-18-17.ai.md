This is a follow-up from the maintainer (Junio C Hamano) on the final patch implementing URL-based partial clone filters. The thread has already concluded with all technical issues resolved, and this email focuses on two minor documentation/test consistency points before merging.

Junio notes two small discrepancies in the final submission:
1. The commit message claims only URL-qualified configs are honored, but the documentation shows both bare and URL-qualified forms are supported. This appears to be an outdated statement from earlier iterations that wasn't updated in the final commit message.
2. The test script uses direct `git config` checks that could be modernized to use the preferred `test_config` helper for better error messages, following recent test infrastructure improvements.

These are minor polish items that don't affect the functionality - the implementation already correctly handles both config forms as shown in the docs, and the tests work as-is. The thread had previously established consensus on all substantive technical points, including URL matching behavior, command-line override precedence (`--filter` and `--no-filter`), and the clone-specific scope of the feature.

The email signals the patch is nearly ready for merging, with Junio performing final proofreading to catch these small inconsistencies. The author will likely send a trivial v5 to align the commit message with the actual behavior and modernize the test syntax, but no further design discussion is expected.