This is a follow-up discussion about memory management in the mmap emulation layer, specifically regarding the behavior of `git_munmap()` when built with `NO_MMAP`. Ramsay Jones clarifies his earlier comments about a historical bug where double-free could occur in the emulation layer when `munmap()` was called multiple times on the same region.

The key points are:
1. Ramsay explains that his earlier patch was purely illustrative - meant to demonstrate a historical issue (from Cygwin days) where invalid double `munmap()` calls would trigger double-free in the `NO_MMAP` emulation layer.
2. He notes that in normal usage, calling `munmap()` twice on the same region is invalid, and most callers don't check the return value anyway.
3. The discussion serves as background context for the broader mmap leak fixes, though Ramsay confirms this particular edge case is not an active concern needing fixing.

This email provides historical context about the mmap emulation layer's behavior but doesn't introduce any new technical changes - it's primarily clarifying Ramsay's earlier comments to ensure Junio understands they were about a past issue rather than a current problem. The thread remains technically resolved with Jeff King's patch series ready for merging.