This is a test failure report for the credentials subsystem. The reporter (Ivan Ivanov) is encountering an infinite credential prompt loop when running test t0300-credentials on Arch/Artix Linux, despite having all dependencies installed.

The test is failing at the very first credential check (0300.2) where it expects the helper to fill in username/password values "foo"/"bar" but instead gets empty strings. The subsequent test (0300.3) also fails similarly, not providing the expected Bearer token authentication details.

Key observations:
1. The test sets up several credential helper scripts in the local directory and modifies PATH to use them
2. The failure suggests either:
   - The test helper scripts aren't being executed as expected
   - Some system credential helper is intercepting the requests first
   - There's an environment-specific interaction preventing the test scripts from working

The infinite prompt loop is particularly concerning as it suggests the test harness isn't properly handling the case where no credential is provided. This appears to be a platform-specific issue affecting Arch/Artix Linux builds, though the root cause isn't immediately clear from the provided logs. The reporter notes they've installed optional dependencies like Apache HTTPD without resolving the issue.