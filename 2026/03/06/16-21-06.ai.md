This is a follow-up discussion in the mmap leak fix thread, where Jeff King (Peff) responds to historical context provided by Ramsay Jones about similar mmap-related issues from 2012. The exchange highlights how platform-specific build configurations have long exposed subtle resource management challenges in Git's mmap handling.

Ramsay Jones shared an unmerged patch from 2012 that took a different approach to handling mmap issues on Cygwin - tracking allocations to prevent double-frees rather than fixing the underlying double-free bugs. Jeff King notes this approach seems like a workaround rather than a proper fix, and suggests the current leak fixes address the root causes more directly.

The discussion reveals that mmap-related memory management has been a persistent challenge across different platforms and toolchains, with issues manifesting differently over time (leaks vs double-frees). Jeff King speculates that some historical bugs may have been unknowingly fixed in the intervening years, while others might still lurk in edge cases like racy reprepare_packed_git() calls.

This exchange doesn't introduce new technical changes to the patch series (which is already fully reviewed), but provides valuable context about the class of problems being addressed and the project's historical approach to mmap resource management. The tone is collegial, with Jeff King acknowledging Ramsay Jones' historical perspective while maintaining the project's preference for fixing root causes over workarounds.