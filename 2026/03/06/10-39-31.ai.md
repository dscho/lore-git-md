This is a final discussion in the thread about configurable partial clone filters via URL patterns. brian m. carlson raises concerns about the feature's potential to create unexpected behavior when scripts assume full clones but get partial ones due to URL-based configuration.

The email argues that default filters could lead to subtle failures in scripts when users are offline, since the partial clone behavior isn't explicitly requested. They suggest this type of configuration belongs in `scalar` rather than core Git, as `scalar` is specifically designed for managing large repository configurations.

This represents a dissenting view in an otherwise settled thread - the feature has already been through multiple iterations and received maintainer approval with all technical feedback addressed. The concern focuses on user experience rather than implementation details, suggesting the change might violate Git's principle of least surprise despite being technically sound.

Key points for future thread context:
- The objection is about user expectations and script reliability, not technical implementation
- Suggests `scalar` as a more appropriate place for this configuration
- Comes after the feature was considered complete by other participants
- Does not propose specific changes to the current implementation