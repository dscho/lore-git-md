This thread has clarified the handling of Git's `-U` diff option, which has historically accepted a bare `-U` (defaulting to 3 lines of context like `-p`) while requiring a number for `--unified=<n>`. The discussion established this as legacy behavior that must be preserved for backward compatibility, particularly for test 157 in t4013.

Tian Yuchen's documentation patch (now at v3) has been refined with Oswald Buddenhagen's wording, explicitly framing the bare `-U` behavior as "a historical accident" while maintaining `--unified=<n>` as the canonical form. Junio has approved this approach, indicating the core issue is resolved. Jean-Noël Avila raised a follow-up about whether to document `--unified` with optional parameter syntax despite the implementation requiring a number, but this appears to be a minor documentation style question that doesn't affect the settled behavior. The patch is now ready for merging with only potential stylistic refinements remaining for future consideration.