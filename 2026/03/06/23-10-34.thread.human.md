Here's the updated human-readable thread summary:

This thread addresses an inconsistency in how `git show -U` handles arguments - where a bare `-U` was silently accepted while malformed forms like `-Unan` properly errored. Junio Hamano identified this undocumented behavior, noting we must preserve the lenient parsing for backward compatibility (test 157 in t4013 depends on it) while properly documenting it as legacy behavior.

Tian Yuchen submitted documentation patches clarifying that bare `-U` defaults to 3 lines of context (matching `-p` behavior), which Oswald Buddenhagen then refined with clearer wording - describing it as "silently accepted as a synonym for `-p` due to a historical accident." The patches update both `diff-context-options.adoc` and `diff-options.adoc` to consistently frame this as legacy behavior while maintaining the existing functionality.

The solution preserves backward compatibility while properly documenting the behavior. D. Ben Knoble raised some remaining documentation formatting questions (like using `--unified[=<n>]` syntax), but these are considered non-blocking improvements. With Oswald's wording polish incorporated, the patch appears ready for merging, having satisfied Junio's original concern about undocumented behavior.