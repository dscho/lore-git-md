This thread addressed an inconsistency in how `git show -U` handles arguments, where bare `-U` was accepted while malformed forms like `-Unan` properly errored. Tian Yuchen submitted documentation patches clarifying that bare `-U` is legacy behavior (defaulting to 3 lines like `-p`) while `--unified=<n>` remains the canonical form. After wording improvements from Oswald Buddenhagen framing this as "a historical accident," Junio approved the changes as ready for merging.

Jean-Noël Avila has now raised a follow-up question about whether the long option (`--unified`) should be documented similarly with optional parameter notation, though the implementation actually requires the number. This is purely a documentation style consideration that doesn't affect the already-settled behavior. The core issue is resolved, with any remaining documentation refinements able to be handled separately. The thread shows consensus on preserving backward compatibility while properly documenting the current behavior.