Here's the updated human-readable thread summary:

This thread addressed an inconsistency in how `git show -U handles arguments - while malformed inputs like `-Unan` properly error out, a bare `-U` was silently accepted as equivalent to `-p` (showing 3 lines of context). Tian Yuchen submitted documentation patches to clarify this behavior, with the latest iteration incorporating wording improvements from Oswald Buddenhagen that frame bare `-U` as "silently accepted as a synonym for `-p` due to historical reasons."

Junio Hamano confirmed this behavior must be preserved for backward compatibility (notably test 157 in t4013 depends on it), but agreed the documentation should clearly mark it as legacy rather than intentional. The final patch updates both `diff-context-options.adoc` and `diff-options.adoc` to distinguish between the canonical `--unified=<n>` form and the legacy bare `-U` behavior.

With Oswald's natural phrasing improvements and no outstanding technical objections, Junio has indicated the patch is ready to proceed. D. Ben Knoble raised some remaining documentation syntax questions about formatting defaults and potential duplication between files, but these are considered non-blocking follow-up items. The core issue of properly documenting this legacy behavior while maintaining compatibility has been resolved.