This thread began with Junio C Hamano noticing an inconsistency in how `git show -U` handles arguments - the command was accepting `-U` without a number while rejecting malformed inputs like `-Unan`, contrary to what the documentation implied. After Tian Yuchen analyzed the backward compatibility implications of enforcing strict validation (which would break existing tests relying on the lenient behavior), the solution converged on updating the documentation to match the actual behavior rather than changing the implementation.

The current state is that the documentation has been updated to show `-U[<n>]` as accepting an optional number, defaulting to 3 lines of context (matching `-u` behavior). Now D. Ben Knoble has suggested two refinements to this documentation: first, applying the same optional syntax to the long form `--unified[=<n>]` to match Git's documentation conventions, and second, making the default value description more explicit about when it applies (both when the config is unset and when `<n>` is omitted). These are relatively minor documentation improvements that would bring this section more in line with Git's standard documentation patterns while maintaining the clarity achieved by Yuchen's initial fix.

The thread has remained focused and constructive throughout, with participants carefully considering both correctness and backward compatibility. The current discussion represents the final polishing of documentation to accurately reflect long-standing behavior while following project conventions.