This is a build system patch discussion in response to Jeff King's series fixing mmap-related memory leaks. Jacob Keller points out that the Makefile change to enable `NO_MMAP` for leak sanitizer builds should likely be mirrored in the meson.build configuration. He notes that meson currently only sets `NO_MMAP` for address sanitizer builds, not leak sanitizer ones, and questions whether this should be updated for consistency.

The thread context shows this is the final patch in a series that fixed several memory leaks in pack index handling that were only visible under certain sanitizer configurations. The discussion here is focused on ensuring the build system changes properly cover both Make and meson builds, maintaining consistent behavior across build systems. Jacob's observation suggests the meson configuration might need updating to match the Makefile's behavior, though he acknowledges the performance tradeoff is likely acceptable given the already-slow nature of sanitizer builds.