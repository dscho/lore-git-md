This is a follow-up discussion in the mmap leak fix series, where Junio Hamano is seeking clarification from Ramsay Jones about the double-free behavior in the `NO_MMAP` compatibility layer.

Junio questions whether the double-free issue Ramsay described could occur with correct single-use `munmap()` calls, or if it requires actual duplicate calls to `munmap()`. His confusion suggests he's trying to understand whether the problem stems from incorrect usage patterns or from flaws in the emulation layer itself.

The exchange comes after Jeff King's 5-patch series (already reviewed and ready for merging) that fixed memory leaks in pack index mmap handling. Ramsay had provided historical context about similar issues from Cygwin in 2012, noting that the `NO_MMAP` emulation was vulnerable to double-free bugs if memory regions were unmapped multiple times.

Junio's query appears to be ensuring the current fixes properly address the root cause rather than just symptoms. The thread has already reached consensus on the technical solution, so this is likely a maintainer's final verification before merging. The question doesn't indicate any problems with the patches themselves - just a desire to fully understand the historical context Ramsay raised.