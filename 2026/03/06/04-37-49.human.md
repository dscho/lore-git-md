Historical context on mmap leak detection. Ramsay Jones shares an anecdote from 2012 about encountering a similar double-free issue in Cygwin's NO_MMAP implementation, which he never fully diagnosed before switching to native mmap support. He includes a debug patch he wrote at the time that tracked mmap allocations to prevent double-frees, though he notes it was never merged and the branch is now been deleted.

This provides useful historical context for Jeff King's current mmap fixes, showing that similar issues have surfaced before under different conditions. The anecdote highlights how platform-specific quirks (like Cygwin's NO_MMAP) can expose subtle bugs that may resurface in other contexts (like ASan builds). Ramsay's patch demonstrates one approach to hardening the mmap emulation layer, though it's not directly relevant to the current fixes.