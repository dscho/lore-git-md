This is a refactoring patch that's part of a larger series to enhance cover letter formatting in `git format-patch`. The patch moves the shortlog-based summary generation code into its own dedicated function, `generate_shortlog_cover_letter()`, as preparation for upcoming patches that will introduce customizable formatting options.

The change is straightforward - it extracts the existing shortlog generation logic from `make_cover_letter()` into a new function while maintaining identical behavior. This modularization makes the code easier to work with when adding the new formatting features in subsequent patches. The patch has already gone through multiple iterations (v6) and appears ready for merging as part of the complete series.

The author (Mirko Faina) and reviewers (including Jeff King) have already vetted the approach, and the maintainer (Junio Hamano) has approved the overall architecture. This particular patch is a clean, mechanical refactoring that doesn't change functionality but improves the code structure in preparation for the new features.