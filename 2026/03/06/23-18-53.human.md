This is the final documentation patch in a feature series that adds configurable cover letter formatting to `git format-patch`. The patch adds documentation for both the new `--cover-letter-format` command-line option and the corresponding `format.commitListFormat` configuration variable.

Junio Hamano provides minor wording suggestions to improve the documentation clarity, specifically:
- The config variable should be called a "configuration variable" rather than just "config"
- The relationship between the config and command-line option should be phrased more naturally
- The command-line documentation should explicitly mention the config provides the default value

The series has gone through six iterations and is now in final form, having addressed all technical requirements and maintainer feedback. The implementation provides flexible formatting options while maintaining backward compatibility with the traditional shortlog-based output. The feature includes new `%(count)` and `%(total)` placeholders for patch numbering, with strict validation of their usage context.

This documentation patch completes the series by properly documenting the new functionality in both the config and command man pages, following the standard Git documentation style. With Junio's suggested wording improvements, the series appears ready for merging.