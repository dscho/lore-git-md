This is a cleanup patch in the now-obsolete `gui.GCWarning` configuration setting, part of a series modernizing git-gui's maintenance behavior. The patch removes the setting from Scalar's documentation and code since git-gui no longer uses it to warn about manual garbage collection, having switched to Git's built-in auto maintenance.

The changes touch Scalar's configuration handling (removing the setting from its recommended config), documentation (deleting the now-irrelevant entry), and test suite (updating tests to use `gc.auto` as a sentinel instead). The patch is straightforward and appears well-motivated, following through on the series' goal of removing git-gui's legacy GC infrastructure. No open questions remain beyond what was already noted in the thread about potential manual compression use cases.