This thread discusses modernizing git-gui's maintenance behavior by integrating with Git's built-in auto maintenance feature, removing git-gui's legacy manual garbage collection infrastructure. The series has progressed through several iterations, with the latest patch focusing on cleaning up Scalar's configuration to remove now-obsolete references to git-gui's `gui.GCWarning` setting.

The changes span multiple components: git-gui's TCL code has been updated to run maintenance automatically after commits (in detached mode suitable for GUI use), while removing the old loose objects warning dialog and manual "Compress Database" option. The latest patch completes this transition by removing related documentation and configuration from Scalar, updating tests to use `gc.auto` instead. 

Wolfgang Faust raised a question about whether manual compression might still have valid use cases, but this appears to be the only outstanding discussion point. The series otherwise represents a straightforward cleanup aligning git-gui with CLI behavior while making appropriate GUI-specific adaptations. The Scalar configuration changes follow logically from the main series and appear uncontroversial, with the author noting they should be CC'd to Scalar maintainers once the rest settles.