This is a refactoring patch in a series that adds configurable cover letter formatting to `git format-patch`. It's the second of five patches in the seventh iteration of the series, which has already gone through extensive review and is now in final form.

The patch moves the shortlog summary generation code from `make_cover_letter()` into a new dedicated function `generate_shortlog_cover_letter()`. This is a pure code reorganization with no behavior change - the same shortlog output is produced, just from a more modular structure. The refactoring prepares for subsequent patches that will introduce new formatting options for cover letters.

The implementation looks clean and focused, with all the shortlog-related initialization and output logic cleanly encapsulated in the new function. The patch maintains the existing behavior while making the code more maintainable and extensible. Given that this is part of a well-reviewed series that's now in its final iteration, it appears ready for merging.