Git thread summary:

Junio C Hamano identified an inconsistency in `git show -U` argument handling where `-U` without a number was accepted while `-Unan` properly errored out. The original implementation fix proposal was rejected after Tian Yuchen's analysis showed it would break backward compatibility with test 157 in t4013 that relies on `git diff -U` defaulting to 3 lines (matching `-u` behavior). The solution converged on a documentation update showing `-U[<n>]` as accepting an optional number.

D. Ben Knoble now suggests two documentation refinements: 1) showing the long form as `--unified[=<n>]` to match other Git documentation patterns, and 2) clarifying the default value applies both when config is unset and when `<n>` is omitted. He notes this pattern appears elsewhere in the docs, though some entries are more explicit about the conditions. The email also observes the option descriptions are duplicated between `diff-options.adoc` and `diff-context-options.adoc`, though this appears informational rather than a proposed change.

The thread has established:
- Implementation behavior (lenient `-U` parsing with 3-line default) is correct and must be preserved
- Documentation must accurately reflect this behavior
- Current solution is minimal doc update showing optional `[<n>]`
- Knoble's suggestions would further align with Git's doc conventions
- No implementation changes are needed or desired

Open questions:
- Whether to adopt Knoble's suggested doc syntax improvements
- Whether to address the doc duplication observation (though this seems lower priority)