Git thread summary:

Junio C Hamano identified and fixed an inconsistency in `git show -U` argument handling where `-U` without a number was incorrectly accepted while `-Unan` properly errored out. The patch modifies `diff_opt_unified()` to consistently validate the argument when present, moving error checking outside the conditional block. This affects the diff machinery's option parsing specifically for the `--unified` flag. Junio notes this technically correct behavior change might break existing tests in t4013 that rely on the current lenient handling, raising the question of whether the inconsistency should be preserved for backward compatibility despite being incorrect. The change is small and focused, touching only the unified diff option validation logic.