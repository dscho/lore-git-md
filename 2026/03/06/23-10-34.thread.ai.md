Git thread summary:

Junio C Hamano identified an inconsistency in `git show -U` argument handling where bare `-U` was accepted while malformed `-Unan` properly errored. The thread established that while the lenient `-U` parsing (defaulting to 3 lines like `-p`) must be preserved for backward compatibility (test 157 in t4013 depends on it), the documentation should frame this as legacy behavior rather than an intentional feature.

Tian Yuchen submitted a documentation patch updating `diff-context-options.adoc` and `diff-options.adoc` to explicitly document that `-U` without a number is accepted as legacy behavior, defaulting to 3 lines of context. The third iteration incorporates wording improvements from Oswald Buddenhagen, framing the backward compatibility as "a historical accident" and describing bare `-U` as "silently accepted as a synonym for `-p`". The documentation now clearly distinguishes between the legacy bare `-U` behavior and the canonical `--unified=<n>` form.

Key technical details:
- Default 3-line context matches `-p` behavior
- `--unified=<n>` remains the canonical long form
- Documentation explicitly marks bare `-U` as legacy with clear rationale
- Default applies both when `<n>` is omitted and when config is unset
- Maintains backward compatibility with existing test 157 in t4013
- Documents the configurable default (`diff.context`) falling back to 3 lines
- Uses consistent phrasing between the two documentation files

Open questions from D. Ben Knoble remain for potential follow-up:
- Whether to adopt doc syntax alignment (`--unified[=<n>]`) to match Git conventions
- Whether to further clarify the default value's dual application
- Documentation duplication between files (lower priority)

Participant positions:
- Junio: Core requirements satisfied by current patch
- Tian Yuchen: Implemented doc updates addressing main issue
- Oswald Buddenhagen: Provided final wording polish (now incorporated)
- D. Ben Knoble: Suggested doc syntax improvements remain open but not blocking

The patch appears ready for merging with Oswald's wording improvements, having addressed the core issue while preserving backward compatibility. The remaining documentation refinements could be handled in follow-up patches if desired. No new technical concerns have been raised in the latest iteration, which primarily confirms the wording improvements and prepares for final merge.