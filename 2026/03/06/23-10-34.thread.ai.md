Git thread summary:

Junio C Hamano identified an inconsistency in `git show -U` argument handling where bare `-U` was accepted while malformed `-Unan` properly errored. The thread established that while the lenient `-U` parsing (defaulting to 3 lines like `-p`) must be preserved for backward compatibility (test 157 in t4013 depends on it), the documentation should frame this as legacy behavior rather than an intentional feature.

Tian Yuchen submitted a documentation patch (v3) updating `diff-context-options.adoc` and `diff-options.adoc` to explicitly document that `-U` without a number is accepted as legacy behavior, defaulting to 3 lines of context. The final iteration incorporates Oswald Buddenhagen's wording improvements, framing the backward compatibility as "a historical accident" and describing bare `-U` as "silently accepted as a synonym for `-p`". The documentation now clearly distinguishes between the legacy bare `-U` behavior and the canonical `--unified=<n>` form.

Jean-Noël Avila raised a follow-up question about whether the long option (`--unified`) should also be documented with optional parameter notation (`[=<n>]`) despite the implementation requiring the number, which remains a documentation style consideration. D. Ben Knoble had previously noted potential syntax alignment opportunities in the documentation.

Key technical details:
- Default 3-line context matches `-p` behavior
- `--unified=<n>` remains the canonical long form (requires number)
- Documentation explicitly marks bare `-U` as legacy with clear rationale
- Default applies both when `<n>` is omitted and when config is unset
- Maintains backward compatibility with existing test 157 in t4013
- Documents the configurable default (`diff.context`) falling back to 3 lines
- Uses consistent phrasing between the two documentation files

Open questions:
- Whether to adopt doc syntax alignment (`--unified[=<n>]`) to match Git conventions (D. Ben Knoble)
- Whether to document `--unified` with optional parameter notation despite implementation requiring number (Jean-Noël Avila)
- Documentation duplication between files (lower priority)

Participant positions:
- Junio: Core requirements satisfied by current patch, accepts Oswald's wording as final
- Tian Yuchen: Confirmed v3 patch incorporates all requested changes
- Oswald Buddenhagen: Provided final wording polish now accepted by Junio
- D. Ben Knoble: Suggested doc syntax improvements remain open but not blocking
- Jean-Noël Avila: Raised new documentation style question about long option syntax

The patch is now finalized with Junio's approval of Oswald's wording improvements, having addressed the core issue while preserving backward compatibility. The remaining documentation refinements could be handled in follow-up patches if desired. No technical concerns remain outstanding, and the conclusion of discussion indicates readiness for merging.