This is a refactoring patch in the final iteration of a series that introduces configurable cover letter formatting for `git format-patch`. The patch moves the shortlog-based cover letter generation code into a dedicated function, `generate_shortlog_cover_letter()`, in `builtin/log.c`.

The change is purely structural - it extracts the existing shortlog generation logic from `make_cover_letter()` into its own function without altering behavior. This prepares for subsequent patches that will introduce customizable formatting options while maintaining backward compatibility with the traditional shortlog output.

Key technical details:
- The new function takes the same parameters as the extracted code block (shortlog struct, rev_info, commit list and count)
- All shortlog configuration (line wrapping, indentation, author grouping) remains unchanged
- The diff shows the mechanical movement of code with no functional changes
- This is patch 2/5 in the v6 series, which the thread context indicates is the final version ready for merging

The patch follows Git's convention of small, focused changes - it does one thing (code movement) to enable the larger feature while keeping the diff minimal and reviewable. The commit message clearly states this is preparatory work for the customizable formatting feature.