This is a follow-up in an ongoing thread about adding structured path information to `git repo info`. Lucas Seiki Oshiro responds to Pushkar Singh's RFC series proposing new `path.git-dir` and `path.common-dir` fields, cautioning that the foundational patch (likely referring to the initial `git repo info` implementation) hasn't been accepted yet and expressing skepticism about its immediate prospects.

The exchange suggests some misalignment in the thread's momentum - while previous discussion (summarized in the thread context) showed consensus around moving repository introspection from `git rev-parse` to `git repo info`, Oshiro's note indicates the base infrastructure may face more resistance than anticipated. This creates uncertainty about whether these incremental field additions can proceed independently or must wait for broader architectural approval.

The technical content itself remains sound (building on established path APIs like `repo_get_common_dir()`), but the procedural concern about patch dependency and sequencing now takes center stage. This may temporarily stall the series until the core `git repo info` functionality's status is clarified.