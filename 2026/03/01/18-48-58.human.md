This is a follow-up in an ongoing discussion about adding structured repository path information to `git repo info`. The email comes from Lucas Seiki Oshiro, responding to Pushkar Singh's proposal to add `path.git-dir` and `path.common-dir` fields.

The key point here is that Lucas is pushing back on the timing, noting that the foundational patch for this functionality hasn't been accepted yet ("it's too early to send this"). He expresses skepticism that the initial version will be accepted without changes ("I really don't think that it will be in v1"), suggesting the discussion may need to revisit some design aspects before moving forward with these field additions.

The thread has previously established consensus that `git repo info` should become the canonical place for this functionality (replacing `git rev-parse`), but Lucas's response indicates there may be unresolved questions about the implementation approach that need addressing first. The tone suggests this isn't a rejection of the concept, but rather a call to properly sequence the changes.