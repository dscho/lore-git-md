This is a test improvement patch that fixes a subtle issue in the performance test suite. The change addresses a problem in `t/perf/p5304-prune.sh` where piping `git count-objects` directly into `grep` could mask failures - if `count-objects` failed but `grep` succeeded, the test would incorrectly pass. The patch modifies the test to first write the output to a temporary file (`out`) before grepping it, ensuring the exit code of `git count-objects` is properly evaluated.

The change is small but meaningful - it makes the test more robust by properly detecting failures in the `count-objects` command. This is particularly important for performance tests which need to accurately report their preconditions. The patch follows Git's testing conventions by using the standard pattern of writing to a temporary file rather than relying on pipeline exit codes.

The commit message clearly explains the issue and solution, though it could potentially benefit from mentioning why this specific test case matters (it's part of the prune performance testing infrastructure). The change itself is straightforward and unlikely to be controversial - it's a clear improvement to test reliability.