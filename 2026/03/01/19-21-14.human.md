This is a documentation patch in Kristoffer Haugsbakk's series about `format.noprefix` configuration. It refines the AsciiDoc formatting in `diff-options.adoc` to make the documentation more translation-friendly, following feedback from Jean-Noël Avila.

The change restructures how the `--default-prefix` option's conditional text is presented, moving from a single paragraph with inline conditionals to two complete paragraphs (one for format-patch context, one for general diff context). This addresses translation workflow concerns where splitting single words across conditionals causes problems.

The v2 revision fixes a typo in the conditional tag (`ifndef` -> `endif`) and removes an unnecessary translator comment that Jean-Noël suggested wasn't needed. The patch is now properly aligned with the translation process while maintaining technical accuracy about how prefix overrides work in different contexts.

This is the final piece of a three-patch series that has already been reviewed and queued in the `next` branch, with all feedback incorporated. The changes are straightforward documentation improvements that don't affect runtime behavior.