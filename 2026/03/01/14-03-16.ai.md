This is part of an ongoing effort to move repository introspection functionality from `git rev-parse` to `git repo info`. The patch adds structured output for the common directory path via a new `path.common-dir` field, mirroring what `git rev-parse --common-dir` provides but in a more machine-readable format.

The implementation follows the established pattern in this series - it adds a new field to the `repo_info_field[]` array with a corresponding getter function (`get_path_common_dir()`). The function uses the existing `repo_get_common_dir()` API and respects the `--path-format` flag for consistent path representation. Like previous patches in the series, this avoids broader questions about path resolution semantics that are being discussed separately.

The change is mechanically straightforward - it adds 16 lines to `builtin/repo.c` to expose the common directory path through the structured output interface. The patch builds on Lucas Skeggs' earlier work on configurable path resolution while maintaining the narrow technical scope that Patrick Steinhardt has advocated for in this effort. Each field addition follows the same pattern of one focused change with tests (though the test changes aren't shown in this diff).

This continues the uncontroversial progression toward making `git repo` the canonical home for repository introspection, with `path.common-dir` joining previously added fields like `path.git-dir` and `path.toplevel`. The implementation uses established repository path APIs and follows existing behavior from `rev-parse`, suggesting it will progress smoothly through review like the rest of the series.