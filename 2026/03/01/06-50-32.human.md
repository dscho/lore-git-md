This is a discussion about path formatting options for the new `git repo-info` command, part of an ongoing series adding path-related fields. The thread has evolved from initial implementation to design refinement, with multiple proposals for handling path formats (absolute vs relative).

Jayatheerth K responds to Ayush Jha's suggestion of making path format part of the key itself (e.g. `path.absolute.toplevel` vs `path.relative.toplevel`). While acknowledging this could work for individual retrievals, Jayatheerth points out it wouldn't solve the `--all` case cleanly, where users might want all paths in a consistent format. They suggest that scripts needing both formats could simply invoke the command twice with different `--path-format` flags.

The discussion highlights the tension between maintaining consistency with `git rev-parse`'s behavior (which uses a global format flag) and designing an optimal solution for `repo-info`'s specific needs. The patch series appears well-considered, with shared path utilities and comprehensive tests, though the final approach to path formatting remains an open question.