This is a follow-up discussion in the ongoing thread about adding path-related fields to `git repo-info`. The conversation focuses on the design of path format options, specifically whether to use a global `--path-format` flag (current approach) versus per-field variants like `path.absolute.toplevel`.

Lucas Seiki Oshiro responds to Ayush Jha's suggestion of per-field naming by pointing out a key consideration: how this would interact with the `--all` flag that shows all available fields. The current global flag approach ensures consistent formatting across all path fields when using `--all`, while per-field variants would require either mixed formats in the output or multiple command invocations.

The exchange highlights the tradeoffs between these approaches, with Lucas maintaining the thread's established preference for the global flag solution that builds on shared `path.c` infrastructure. This keeps the design aligned with the project's GSoC 2026 roadmap while avoiding namespace complexity from per-field variants. The discussion remains focused on practical usage patterns rather than reopening the core design decisions settled earlier in the thread.