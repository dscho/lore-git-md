This is a follow-up in an ongoing discussion about adding structured repository path information to `git repo info`. The thread has been exploring whether to move certain path-related functionality from `git rev-parse` to the newer `git repo info` command.

Pushkar Singh acknowledges Lucas Oshiro's concern that the foundational `git repo info` infrastructure hasn't been accepted yet, and decides to pause further work on his patch series that would add `path.git-dir` and `path.common-dir` fields. This reflects a prudent approach to patch sequencing - Pushkar recognizes that building on unmerged infrastructure could be premature.

The exchange highlights a tension in the thread: while Patrick Steinhardt and others support the long-term vision of moving functionality from `rev-parse`, there's uncertainty about whether the base `git repo info` command will be accepted. Pushkar's response shows good contributor etiquette by choosing to wait rather than pushing forward with dependent patches.

This email doesn't introduce new technical content, but it does signal that development on this feature may stall until the core command's status is resolved. The implementation itself (which uses existing path APIs) remains uncontroversial, but the procedural question takes precedence for now.