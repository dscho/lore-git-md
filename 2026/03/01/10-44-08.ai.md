This is a feature patch series adding path-related fields to `git repo-info`, building on existing path formatting from `git rev-parse`. The discussion focuses on design decisions around path formatting approaches.

Phillip Wood raises two key considerations in response to Lucas Seiki Oshiro's series: First, he asks how this relates to an earlier effort (referencing a specific mailing list thread) that had proposed using "path.working-tree" rather than "path.toplevel" terminology. Second, he expresses support for the current approach of using a `--path-format` flag rather than having different defaults per path or separate keys for relative/absolute versions, noting the latter could become verbose.

The thread context shows this is part of an ongoing architectural discussion about how best to handle path formatting in `repo-info`, with multiple proposals on the table (format modifiers, per-field naming conventions) and practical considerations around `--all` output scenarios. Phillip's comments suggest the current implementation direction is reasonable but should consider prior naming discussions and maintain consistency with existing commands.

The technical foundation includes shared path handling utilities in `path.c` with standardized enums (`PATH_FORMAT_CANONICAL`/`PATH_FORMAT_RELATIVE`), comprehensive test coverage, and plans for additional path-related fields to be added during GSoC 2026. The discussion remains focused on finding the right balance between `rev-parse` compatibility and `repo-info`'s specific needs.