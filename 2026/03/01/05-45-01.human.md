This is a discussion about extending `git repo-info` with path-related fields, building on an existing 4-patch series that introduced shared path formatting infrastructure between `repo-info` and `rev-parse`. The thread shows active design refinement around how to handle path formatting in the command's output.

Ayush Jha proposes an alternative to the current `--path-format` flag approach, suggesting path format could be specified per-field (e.g. `path.relative.toplevel` vs `path.absolute.toplevel`) rather than as a global setting. This would allow mixing relative and absolute paths in a single invocation but may complicate the key namespace. The suggestion comes in response to Jayatheerth K's earlier endorsement of the flag-based approach for its simplicity and predictability.

The discussion reveals an ongoing tension between API flexibility and simplicity, with participants weighing the benefits of `rev-parse` compatibility against potential improvements. The patches themselves (which moved path formatting logic to `path.c` and added `path.toplevel` support) appear well-received as a foundation, with Jayatheerth noting they've already incorporated them into a GSoC proposal for further `repo-info` extensions.

This exchange shows the typical Git project pattern of incremental improvement - a working implementation followed by design refinement from multiple contributors. The path handling infrastructure seems likely to land, but the exact user-facing API may still evolve based on this feedback.