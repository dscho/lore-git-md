This is a refactoring patch in Adrian Ratiu's config-based hooks series (v4/12). It adds a new utility function `unsorted_string_list_remove()` to the string-list API, combining lookup and deletion operations into a single call.

The new function wraps the existing `unsorted_string_list_lookup()` and `unsorted_string_list_delete_item()` functions to provide a more convenient way to remove items by string value from an unsorted string list. The implementation follows the same pattern as other string-list helpers, with proper documentation in the header file.

This is a straightforward utility addition that will be used in subsequent patches of the series for managing hook configurations. The change is well-contained, with clear documentation and test coverage implied by its use in later patches. Patrick Steinhardt suggested this helper, indicating it fits the project's patterns for string-list operations.

The patch is part of a larger effort to implement config-based hooks, but this particular change is a mechanical refactoring that stands on its own merits. It follows the project's coding style and introduces no behavioral changes - just a new convenience API.