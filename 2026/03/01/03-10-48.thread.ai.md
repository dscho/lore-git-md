[GSoC proposal] git repo info improvements - Jayatheerth Kulkarni's proposal to enhance the `git repo info` command with category-based querying (replacing exact-match binary search), `rev-parse`-like path resolution, and sparse-checkout/submodule awareness, while removing `the_repository` dependencies (part of ongoing libification). Builds on Lucas Oshiro's GSoC 2025 work introducing `git repo`.

Karthik Nayak reviewed with suggestions for glob pattern matching extensions (e.g. `path*`, `*object*`) and noted technical challenges around `is_bare_repository()` call chains during libification. Recommended incorporating newer path resolution discussions from Mansi's proposal and expressed timeline concerns, particularly for Phase 1 path resolution and Phase 2 libification work.

Jayatheerth responds by pushing back on immediate glob pattern implementation (arguing current key set is small), while proposing `repo->worktree` checks as an alternative to `is_bare_repository()`. They've doubled allocated time for path resolution and libification in their updated proposal (3 weeks buffer for path/libification, 2 for querying), showing deeper investigation into `the_repository` removal challenges. Technical specifics remain focused on `strbuf_add_path` API usage, `get_layout_bare()` refactoring, path resolution targets (`path.git-dir`, `path.common-dir`), and sparse topology awareness. Open questions persist about glob pattern implementation timing and validation of the `repo->worktree` approach for bare repository detection.