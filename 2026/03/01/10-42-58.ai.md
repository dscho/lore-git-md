Refactoring patch as part of the `the_repository` removal effort. This first patch in a 2-part series moves the `editor_program` global variable from environment.c to become static within editor.c, where it is actually used. The change introduces a new `set_editor_program()` helper function to handle configuration of the editor path while maintaining the existing initialization timing and error reporting behavior.

The patch follows the established pattern for eliminating globals - relocating variables to the files where they're used and providing controlled access via helper functions. It touches editor.[ch] and environment.[ch], removing the global declaration while preserving all existing functionality. The change is mechanical and well-contained, with no apparent behavioral impact or UX regression.

This appears to be a straightforward, well-executed step in the ongoing project to remove global state from Git's codebase. The commit message clearly explains the motivation and implementation approach. As the first patch in a small series, it sets up for the subsequent change that will remove `the_repository` usage from sequence editor functionality.