This is a follow-up in the thread about `git log`'s `--decorate` behavior with format placeholders (`%d`). Junio C Hamano, the Git maintainer, is responding to Marc Branchaud's technical analysis of the current behavior and proposed changes.

Junio indicates he's now convinced by Marc's argument that `--decorate=no` should suppress all decorations including those from `%d` placeholders, given the documented connection between these features. He notes this makes sense because `--decorate` currently has no effect when the format doesn't contain `%d` or `%D`.

The maintainer agrees to proceed cautiously with this change - first letting it cook in the 'next' integration branch to catch any potential issues, then eventually releasing it to observe real-world impact. This reflects Git's conservative approach to behavior changes, even when they appear safe based on technical analysis.

The email confirms the technical consensus reached in the thread while outlining the practical next steps for implementation. Junio's approval means this change is now effectively approved to move forward through Git's standard integration process.