Thread identity: Performance test suite improvement fixing a subtle issue in `t/perf/p5304-prune.sh` where pipeline exit codes could mask failures.

Core problem: The test was piping `git count-objects` directly into `grep`, which could incorrectly pass if `count-objects` failed but `grep` succeeded. This is problematic for performance tests that need accurate precondition reporting.

Solution: Modified the test to write `git count-objects` output to temporary file `out` before grepping it, ensuring proper evaluation of the `count-objects` exit code.

Technical details: Changes the test script to use Git's standard pattern of writing to files rather than relying on pipeline exit codes. Touches only `t/perf/p5304-prune.sh`.

Status: Single patch, no version iterations. The change is straightforward and improves test reliability.

Key files: `t/perf/p5304-prune.sh` (performance test for prune operations)