This is a design discussion about path formatting in the `git repo-info` command, part of an ongoing effort to add path-related fields. The thread has progressed through initial implementation and positive reception, and now enters a phase of architectural refinement.

Ayush Jha proposes an alternative to the current `--path-format` flag approach, suggesting path format could be specified per-field through key naming conventions (e.g., `path.toplevel` vs `path.absolute.toplevel`). This would allow mixing relative and absolute paths in a single invocation, addressing a limitation of the global flag approach while potentially complicating the key namespace. The suggestion comes in response to Lucas Seiki Oshiro's patch series that introduced path handling by refactoring shared code from `git rev-parse` into `path.c`.

Technical context includes the recently added `PATH_FORMAT_CANONICAL` and `PATH_FORMAT_RELATIVE` enums, the `strbuf_add_path()` helper function, and test coverage in `t1900-repo-info.sh`. The discussion touches on core design tradeoffs between consistency with `rev-parse` (which uses global flags) and flexibility for `repo-info`'s use cases. Jayatheerth K had previously endorsed the flag-based approach for its predictability in scripting contexts.

This exchange represents active design iteration on infrastructure that multiple GSoC 2026 participants plan to build upon for additional path-related fields. The path handling code is proving generally useful but may need refinement in its API design before serving as a foundation for those extensions. Key open question is whether to maintain flag-based parity with `rev-parse` or adopt a more flexible but potentially more complex per-field approach.