This is the second patch in Tian Yuchen's v3 series to migrate encoding-related functionality from global variables to repository-specific storage, part of the ongoing `the_repository` removal effort. The patch focuses on updating pretty-printing functions to take a `struct repository*` parameter.

The changes propagate the repository context through the commit formatting chain, updating `pretty_print_commit()` and `pp_commit_easy()` to accept a repository parameter. The patch touches 14 files across various builtins (log, merge, stash, etc.) and core components (pretty, sequencer), consistently passing through the repository context where available while maintaining fallbacks to `the_repository` for unconverted callers.

The implementation follows established patterns seen in other `the_repository` removal work, with careful attention to maintaining behavior during the transition. The changes are mechanical but widespread, showing the ripple effect of removing global state. Test coverage appears maintained through the existing test suite, though the commit message doesn't explicitly mention new tests for the fallback paths.

This is a well-structured patch that clearly advances the series' goal while minimizing disruption to existing code. The changes are focused and follow the project's conventions for this type of refactoring.