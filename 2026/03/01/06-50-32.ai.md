This is a discussion about path formatting approaches in the `git repo-info` command, part of an ongoing thread about adding path-related fields. The email responds to Ayush Jha's proposal for per-field path format specification (e.g., `path.absolute.toplevel` vs `path.relative.toplevel`) by considering the tradeoffs between that approach and the current global `--path-format` flag.

Jayatheerth K points out that while per-field formatting would work for individual key retrieval, it doesn't solve the problem for `--all` output where mixed formats might be needed. They suggest that scripts needing both formats could simply invoke the command twice with different flags, and question whether the per-field approach is primarily a user-friendliness improvement rather than solving a technical limitation.

The discussion highlights the tension between maintaining consistency with `git rev-parse`'s global path format flag and designing an optimal solution for `repo-info`'s specific needs. This is part of a larger architectural discussion about how to best expose path information through the new command while keeping the implementation clean and maintainable. The thread shows active design refinement with multiple contributors (Jayatheerth K, Ayush Jha, Lucas Seiki Oshiro) working through the tradeoffs.