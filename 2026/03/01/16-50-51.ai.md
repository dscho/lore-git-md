This is a review comment on a feature patch series adding repository path introspection to `git repo info`. The thread has established consensus on moving this functionality from `git rev-parse` to `git repo` as a more structured alternative.

Jayatheerth Kulkarni raises two specific concerns about Pushkar Singh's patches adding `path.git-dir` and `path.common-dir` fields. First, they note the test file `t/t1900-repo-info.sh` hasn't been updated to verify path handling across different OS environments, particularly for the `--path-format=absolute` and `--path-format=relative` cases. Second, they point out the documentation (`Documentation/git-repo.adoc`) hasn't been updated to include these new fields.

The review focuses on practical testing gaps - specifically how NULL path returns would behave with `--all` and cross-platform path handling. This suggests the patches may need additional test coverage before merging, though the core approach (using existing `repo_get_git_dir()` and `repo_get_common_dir()` APIs) remains uncontroversial. The feedback maintains the thread's technical focus while identifying concrete missing pieces in the submission.