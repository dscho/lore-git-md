Here is the dense AI thread summary for future reference:

This thread introduces a new `fetch.blobSizeLimit` config option to enable partial clone behavior by default, proposed by Alan Braithwaite in v1 of the series. The change allows configuring a size threshold (e.g. "1m" or "100k") that automatically applies `--filter=blob:limit=<size>` during clone and fetch operations, making partial clones more discoverable than requiring explicit `--filter` flags. The precedence order is: command-line --filter > remote.partialclonefilter > fetch.blobSizeLimit.

Implementation touches `builtin/clone.c` and `builtin/fetch.c` to handle the new option, registering the remote as a promisor with the filter spec. Server-side filter support is required but silently ignored if unavailable. The patch includes thorough test coverage in `t/t5616-partial-clone.sh` verifying precedence rules and interaction with existing partial clone configurations. Documentation updates cover the new `fetch.blobSizeLimit` config key in config/fetch.adoc.

Technical details captured:
- New config provides default blob size limit for partial clones
- Maintains backward compatibility when server lacks filter support
- Clear precedence between command line, remote config, and global config
- Tests validate basic functionality and edge cases
- Changes limited to clone/fetch subsystems and partial clone tests
- No reviews or feedback yet on this initial version
- Potential open question: Should there be a way to override/disable the config on a per-command basis?
- Related to broader partial clone and promisor remote functionality