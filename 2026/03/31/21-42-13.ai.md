This is a follow-up in the ongoing `git replay --ref` series discussion, where Junio C Hamano confirms the test failure is not due to rebase artifacts but rather an inherent issue with the patch series itself. The problem manifests as a SIGABRT in merge-ort when testing invalid refnames, specifically in test 46 of t3650-replay-basics.sh. 

Junio clarifies that the failure occurs even when applying the patches cleanly (without rebasing) on top of the updated sa/replay-revert base commit (ba5c0d03d3). This indicates the validation logic for the `--ref` option isn't properly catching invalid refnames before they reach merge-ort, or that merge-ort's behavior has changed in ways that affect this case. 

The maintainer is now explicitly waiting for Toon Claes to address this core validation issue before proceeding. The series remains blocked, with Junio emphasizing that the problem requires a patch update rather than just a rebase. This interaction confirms the technical soundness of the approach is still accepted, but the implementation needs hardening around edge case handling. 

For future summarizers: The key development is Junio's confirmation that the SIGABRT occurs in the original patches without any rebasing, proving this is a validation logic issue rather than a rebase conflict. The ball remains in Toon's court to fix the refname validation before the invalid cases reach merge-ort. The series cannot progress until this crash is resolved.