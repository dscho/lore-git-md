This is a follow-up in the `git replay` series discussion, specifically about documentation style for the new `--ref` option. Toon Claes responds to Junio C Hamano's suggestion about using the modern documentation format (`--ref=<ref>::`) versus the traditional style. 

Toon acknowledges the inconsistency in documentation standards and expresses willingness to submit a cleanup patch, but agrees with Junio that it makes sense to wait until the feature stabilizes first. This exchange shows the series is still in active development, with both technical implementation (the SIGABRT issue noted in the thread summary) and documentation polish still being worked on.

For future summarizers: The key point here is the agreement to defer documentation standardization until after the core functionality is fully stable. The `--ref` option's syntax and surrounding documentation will eventually need alignment with modern Git documentation conventions, but that cleanup is intentionally being postponed. This doesn't affect the technical implementation or the ongoing investigation into the test failures.