This is a follow-up discussion on the `git replay --revert` feature series, now in its final v6 iteration. The thread focuses on edge cases in revision range validation, specifically whether the current conservative approach blocks some valid multi-range scenarios.

Toon Claes responds to Tian Yuchen's observation that while the implementation correctly prevents ambiguous commands like `git replay --revert main f1 f2`, it may be overly restrictive by also blocking valid cases like `git replay --revert main HEAD~5..HEAD~3 HEAD~1..HEAD`. Claes confirms this is indeed the current behavior and notes that `--advance` mode has the same limitation, suggesting this could be addressed in future work after the current series lands.

The exchange shows the feature has reached consensus on its core design, with only minor edge cases identified for possible future refinement. The discussion remains technical and constructive, focused on documenting known limitations of an otherwise solid implementation that is ready for merging. The conservative validation approach appears intentional as a safety measure for the initial implementation.