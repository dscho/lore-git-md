This is a small refactoring patch in Patrick Steinhardt's reftable portability series, specifically addressing macro usage consistency. The change replaces a Git-specific `UNUSED` macro with the reftable-specific `REFTABLE_UNUSED` in the fsck.c file, where it was accidentally introduced in an earlier commit. 

The patch is straightforward - a one-line change that swaps one unused parameter annotation for another. The commit message explains the context well: while ideally the Git-specific macro would be moved to prevent such leaks, this isn't currently feasible due to header dependencies. This is part of the larger effort to make the reftable code fully self-contained and portable for libgit2 integration.

Key technical detail: the change affects only internal reftable code (`reftable/fsck.c`) and maintains identical functionality - it's purely about code hygiene and subsystem boundary enforcement. The patch follows the series' pattern of small, focused changes that incrementally improve reftable's platform independence. No test changes are required since this doesn't modify behavior, just macro usage.