This is a final interface design clarification in the ODB abstraction effort, specifically about header file organization for stream-based write operations. Patrick Steinhardt confirms Justin Tobler's suggestion that there's no strong reason `struct odb_write_stream` couldn't be moved to "odb/streaming.h", though he doesn't propose making that change immediately.

The exchange represents the last piece of feedback being addressed in this patch series, which has already gone through multiple rounds of review. Patrick agrees with Justin's assessment that the streaming interface could logically live with other streaming-related functionality, though they're keeping the current organization for now. This follows their earlier decision to place the new `odb_write_stream_from_fd()` helper in "odb/streaming.h" rather than "odb/transaction.h".

The thread shows the careful attention to interface boundaries that has characterized the entire ODB abstraction effort. With this final organizational question resolved, the series appears ready for merging, having addressed all technical and documentation feedback while establishing clear foundations for future storage backend implementations.