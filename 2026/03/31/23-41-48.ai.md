This is patch 3/12 in Jeff King's series addressing C23 const-correctness warnings around `strchr()` usage. The patch modifies `transport-helper.c` to drop const from a variable that needs to be writable when processing push updates.

The change involves making the `key` variable non-const (while keeping `val` const) in the `push_update_ref_status` function. This is safe because `key` comes from a strbuf (which is inherently writable) and needs to be modified when finding spaces in the string. The patch follows the series' pattern of carefully choosing where to add or remove const to satisfy C23 requirements while maintaining correct behavior.

This is a straightforward mechanical change that aligns with the series' goal of fixing const-correctness warnings without introducing new issues. The commit message clearly explains the rationale and safety considerations. As with other patches in the series, it demonstrates Jeff King's characteristic attention to detail in handling subtle compatibility issues.