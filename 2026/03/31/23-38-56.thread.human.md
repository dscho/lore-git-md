Jeff King (Peff) has completed a 12-patch series addressing C23 compatibility warnings about const-correctness throughout Git's codebase. The series systematically fixes issues where `strchr()` and related string function usage needed adjustment across multiple subsystems including config, http, transport, pseudo-merges, and now the refs backend.

The final patch focuses on reflog parsing in the refs backend, where a `strbuf` buffer (which is writable) was incorrectly marked as const when being parsed. Peff's solution removes the const qualifier while maintaining type safety, extending the CONST_OUTPARAM() macro pattern he introduced earlier in the series. This required careful handling of `parse_oid_hex_algop()` since it isn't inline like previous cases.

Throughout the series, Peff has demonstrated his characteristic attention to detail, balancing C23 compliance with practical safety considerations. The changes are precise and mechanical where appropriate, while also uncovering architectural issues like untested code paths in the pseudo-merge bitmap handling. The series touches nearly a dozen subsystems but maintains a clear focus on const-correctness without introducing broader refactoring.

With this final patch addressing the refs backend, the comprehensive effort to make Git's codebase C23-compliant in its string handling is now complete. The changes will help ensure Git compiles cleanly with modern C standards while maintaining backward compatibility.