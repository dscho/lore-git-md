This is the first patch in Jeff King's 12-part series addressing C23 const-correctness warnings around `strchr()` usage. The patch focuses on the `convert.c` file, specifically the `ident_to_worktree()` function.

The change marks two pointer variables (`dollar` and `spc`) as `const` since they don't need to modify the data they point to. This silences compiler warnings that would appear with C23-compliant `strchr()` implementations while maintaining the same behavior. The patch is a straightforward, safe change that doesn't affect functionality - it just makes the constness contract more explicit.

This is typical of Peff's careful, precise style - addressing a potential future compatibility issue with minimal changes. The patch is well-contained and clearly motivated in the commit message, explaining both the technical change and why it's needed. As the first in the series, it sets the tone for the subsequent patches that will tackle similar const-correctness issues across the codebase.