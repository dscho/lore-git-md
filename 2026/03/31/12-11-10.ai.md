Feature addition to `git cat-file`'s batch mode, now ready for merging with all feedback addressed.

Siddharth Asthana's patch series implements dynamic mailmap toggling for `git cat-file --batch-command`, solving GitLab Gitaly's need to control name/email rewriting in long-running processes without restarting them. The final version consolidates the interface into a single `mailmap <bool>` subcommand that accepts standard Git boolean values (yes/no, true/false, on/off) and maintains mailmap data in memory once loaded.

The implementation introduces a `load_mailmap()` helper function shared between startup and runtime paths, uses `git_parse_maybe_bool()` for robust argument handling, and includes comprehensive tests covering boolean variants, invalid inputs, and interaction with `--[no-]mailmap` command-line options. Junio Hamano's final refinement separates the boolean parsing from mailmap loading for clearer logic while maintaining identical behavior.

This appears to be a well-executed solution to a real-world use case, with careful attention to review feedback and thorough test coverage. The changes are contained to `builtin/cat-file.c` and its documentation/tests, with no remaining technical issues. The thread shows consensus among reviewers (Junio Hamano, Patrick Steinhardt, Karthik Nayak) that the implementation is ready for merging.