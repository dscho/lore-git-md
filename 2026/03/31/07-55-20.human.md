This is a follow-up in the `git replay --ref` series, which is currently blocked by test failures. The series adds a `--ref` option to `git replay` for precise reference updates, but now encounters a SIGABRT crash in merge-ort when testing invalid refnames. 

Toon Claes responds to Junio Hamano's report that the test failures persist even when cleanly applying the patches (not just during rebase). The issue appears in test 46 of t3650-replay-basics.sh, where an invalid refname ("bad..ref") triggers an abort in merge-ort rather than the expected graceful error about invalid refnames. 

The series had previously been queued for integration but is now on hold pending investigation of this crash. The problem seems to stem from validation logic not catching errors before they reach merge-ort, or possibly changes in merge-ort's behavior in the updated base commit. Toon acknowledges the issue but doesn't yet propose a solution in this brief response. 

For readers tracking this feature, the key takeaway is that the `--ref` option implementation needs adjustment to properly handle invalid refname cases before they crash in merge-ort. The series remains technically sound but blocked on this edge case.