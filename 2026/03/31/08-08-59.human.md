This is a follow-up discussion on the `git replay --revert` feature, specifically addressing edge cases in revision range validation. Toon Claes responds to Tian Yuchen's observation about the current implementation being overly restrictive with certain valid multi-range cases like `HEAD~5..HEAD~3 HEAD~1..HEAD`.

Claes confirms the current behavior affects both `--revert` and `--advance` modes, suggesting this could be addressed in future work after the current patches land. The exchange shows the feature is essentially complete, with this being a minor limitation rather than a blocking issue. The discussion remains technical and constructive, with all parties recognizing the current conservative approach as a reasonable starting point that can be refined later.