This is a technical discussion about memory management in Taylor Blau's ongoing MIDX repacking series, specifically addressing how to handle string ownership when tracking kept hashes during MIDX file cleanup. Jeff King responds to Taylor's concerns about the `strset` API's limitations, confirming that while `strmap` (which `strset` builds on) can handle string duplication, it doesn't support clean ownership transfer of pre-allocated strings.

Jeff offers two cleaner alternatives to the problematic manual hashmap iteration Taylor proposed: either accepting the overhead of temporary string duplication (allocating, adding to set, then freeing) or using a reusable `strbuf` to minimize allocations. Both approaches avoid the "gross" violation of abstraction boundaries that would come from directly accessing the hashmap internals. The discussion highlights the careful tradeoffs being made in this low-level memory management code, with Jeff providing his characteristic thorough review of the implementation details.

This exchange continues the thread's pattern of rigorous correctness verification, with Jeff's suggestions helping steer the implementation toward more maintainable patterns. The conversation remains focused on technical merits while acknowledging the practical constraints of Git's existing APIs.