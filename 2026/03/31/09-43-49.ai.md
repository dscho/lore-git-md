Refactoring patch in an approved series. Phillip Wood provides a final review comment on Ezekiel Newren's xdiff cleanup series, noting the patch improves readability of the INVESTIGATE record handling in `xdl_cleanup_records()` but suggests consistent brace usage.

The patch converts nested ternary operations into explicit if/else blocks with BUG() checks for invalid states, making the control flow clearer while maintaining identical behavior. Phillip's only remaining suggestion is a style nit - that all conditional clauses should use braces for consistency, since one already does. This minor point doesn't block merging, as the series has already received maintainer approval from Junio C Hamano for inclusion in 'next'.

The changes touch the xdiff subsystem's record cleanup logic, specifically how it handles INVESTIGATE records (which can become either KEEP or DISCARD based on `xdl_clean_mmatch()` results). The explicit state handling and BUG() checks make the code more maintainable while preserving all existing behavior that passed tests in previous iterations. This represents the final polish on a series that has already addressed all substantive technical concerns through multiple review rounds.