This is a follow-up discussion about merging replay-related changes to Git's integration branches, specifically addressing Junio Hamano's request for clarification about the "main..topic" vs "topic" syntax issue in the `git replay` command.

Christian Couder provides additional context about a known limitation in the `tc/replay-down-to-root` patch series, which adds root commit support to `git replay`. The issue involves silent failure when using a simple "topic" reference instead of the required "main..topic" range syntax. Christian confirms this will be handled in a separate series, making the current implementation mergeable as-is.

The email also references Christian's more detailed comment elsewhere (linked in the message) that presumably explains the technical background of this edge case. This exchange demonstrates the project's typical workflow where known limitations are documented and deferred when they don't block immediate progress, with follow-up work planned to address them.

The discussion shows Christian acting as the topic maintainer for these replay changes, providing the necessary context to help Junio make merging decisions while ensuring technical debt is properly tracked for future resolution. This is part of the ongoing evolution of the experimental `git replay` command toward becoming a more robust history-rewriting tool.