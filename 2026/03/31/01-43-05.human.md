This is a final implementation refinement for the mailmap toggle feature in `git cat-file --batch-command`. Siddharth Asthana confirms they will use `git_parse_maybe_bool()` as suggested by Patrick Steinhardt to handle the mailmap subcommand's boolean argument parsing more robustly. This change will be included in the next version of the patch series.

The feature itself is already approved and ready for merging, with all prior review feedback addressed. This last tweak improves the implementation by using Git's standard boolean parsing helper, which understands various true/false variants (1/0, true/false, on/off) while maintaining case insensitivity. The change affects only the internal argument handling in `parse_cmd_mailmap()` and doesn't modify the user-visible behavior or the core functionality that GitLab's Gitaly service depends on.

With this confirmation, the author has now incorporated all outstanding suggestions from Junio Hamano, Patrick Steinhardt, and Karthik Nayak, making the series complete. The implementation provides dynamic mailmap toggling while maintaining performance through in-memory caching, solving the original use case of reusing long-running `cat-file` processes without restarting.