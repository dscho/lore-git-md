This is a review response in the ongoing ODB abstraction effort, specifically about making object database transaction initialization pluggable. Patrick Steinhardt acknowledges Justin Tobler's patch that switches from directly calling `odb_transaction_files_begin()` to using the new `begin_transaction()` callback in `struct odb_source`.

Patrick confirms this was an oversight in the original design and approves the change, noting with satisfaction that the code no longer needs to include "object-file.h". The patch is small (just 2 lines changed) but meaningful in the larger context of separating concerns in the ODB code. This is part of a 6-patch series that's making steady progress toward fully pluggable ODB transactions, with Patrick having already reviewed and approved the first patch in the series.