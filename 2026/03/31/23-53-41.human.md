This is the final patch in Jeff King's series addressing C23 const-correctness warnings, specifically targeting the refs backend. The patch fixes a warning in `show_one_reflog_ent()` where a writable strbuf buffer needs to modify a string (by writing a NUL terminator) but was being treated as const due to C23 `strchr()` behavior.

The solution involves making the source pointer non-const while maintaining type safety through the same `CONST_OUTPARAM` macro approach used earlier in the series. This requires renaming `parse_oid_hex_algop()` to an `_impl` version and wrapping it with a macro to handle the const/non-const conversion cleanly.

As with the rest of the series, this is a precise, mechanical fix that maintains correctness while adapting to stricter compiler requirements. The patch demonstrates the series' consistent approach of balancing C23 compliance with practical code needs, here applied to the refs subsystem. It concludes a thorough effort that touched multiple areas of the codebase with careful, localized solutions.