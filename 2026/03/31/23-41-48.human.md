This is patch 3/12 in Jeff King's series addressing C23 const-correctness warnings around `strchr()` usage. The patch modifies `transport-helper.c` to drop a `const` qualifier from the `key` variable, which comes from a strbuf and needs to be writable when passed to `strchr()`. 

The change is straightforward - it removes `const` from `key` while keeping it on `val`, since only `key` needs modification. This follows the pattern seen in earlier patches of the series where const is removed when necessary for compatibility with C23's stricter `strchr()` requirements. The commit message clearly explains the technical rationale and notes this is safe because the string originates from a writable strbuf.

As with the rest of the series, this is a precise, well-explained change from a trusted contributor addressing a real (if somewhat obscure) compatibility issue before it becomes problematic. The patch is small, focused, and unlikely to be controversial.