This is a follow-up discussion in the reftable portability series, specifically about the `REFTABLE_INLINE` macro introduced in the first patch. René Scharfe clarifies his earlier point about Git's handling of the `inline` keyword after Junio C Hamano notes that Git's build system does allow users to override `inline` via `config.mak`.

René confirms that setting `INLINE` to an empty string (as done for some legacy platforms) causes compilation problems with unused function warnings and duplicate symbol errors, suggesting this fallback path may not be as robust as intended. The exchange remains constructive, with both contributors focused on technical accuracy rather than debate. While this doesn't block the series (the patch was already approved), it ensures the commit message will precisely describe Git's actual behavior.

The discussion exemplifies Git's attention to detail even in infrastructure changes, particularly when establishing shared abstractions between Git and libgit2. The thread continues to show productive collaboration between Patrick Steinhardt (driving the reftable effort), Junio (providing architectural guidance), and René (offering precise technical corrections). This level of scrutiny suggests the series is being carefully vetted before integration.