This is a documentation patch in a series updating references to the deprecated `git config -l` option. Patch 2/3 focuses on `gitcvs-migration.adoc`, replacing the old syntax with the newer `git config list` subcommand while also improving the phrasing around the command reference.

The change is straightforward: it swaps `git config -l` for `list` and tightens up the surrounding text to avoid redundancy ("the man page" is dropped since the section syntax already implies it). The commit message explains the motivation clearly - both the deprecation update and the stylistic improvement to the cross-reference.

This continues the series that Kristoffer Haugsbakk started after noticing these outdated references while working on `git-var(1)`. The patch maintains the series' tight focus on documentation updates without introducing any behavior changes. The diff is clean and the rationale for the additional phrasing tweak is well-explained in the commit message.