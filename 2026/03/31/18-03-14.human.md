Part of the reftable portability series for libgit2 integration. This is Junio C Hamano's reply to the first patch in Patrick Steinhardt's 6-part series that abstracts platform dependencies from the reftable code to enable sharing between Git and libgit2.

Junio concurs with the approach of introducing a `REFTABLE_INLINE()` macro to handle compiler variations in inline function support, noting that Git's own `inline` macro could be used to define it within the project context. His brief reply indicates the change makes sense and aligns with Git's existing practices for cross-platform compatibility.

The exchange is straightforward - this is the maintainer acknowledging a well-justified technical change that moves the reftable library closer to being a standalone component. No concerns are raised, suggesting the patch (and likely the series) is on track for merging once the full review cycle completes. The thread shows the collaborative nature of the reftable effort between Git and libgit2 maintainers.