This is patch 8/12 in Jeff King's series addressing C23 compatibility warnings about const-correctness in `strchr()` usage. The patch introduces a clever macro solution to enforce type safety in `skip_prefix()`'s out-parameter handling.

The core issue is that `skip_prefix()` takes a const input but returns a non-const pointer into that string, which could allow writing to originally-const memory. Peff's solution uses macro trickery with a ternary expression that fails compilation if the out-parameter is less const than the input, while still allowing valid cases. The approach catches dangerous cases at compile time while avoiding explicit casts in most code.

The patch demonstrates Peff's characteristic attention to edge cases and compiler behavior. The solution is designed to work portably across compilers without relying on newer C11 features. While the macro implementation is somewhat dense, the commit message thoroughly explains the motivation and tradeoffs. This change enables safer fixes in subsequent patches while maintaining backward compatibility with existing call sites.

The patch touches `git-compat-util.h` to add the new macros, with no functional changes to `skip_prefix()` itself - just a wrapper that enforces stricter type checking. This is a common pattern in the series: mechanical changes to support const-correctness while preserving existing behavior where safe.