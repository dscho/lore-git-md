Feature refinement for `git cat-file`'s mailmap toggling.

Siddharth Asthana responds to Karthik Nayak's final review comments on the dynamic mailmap control patch for `git cat-file --batch-command`. The core functionality remains unchanged - the patch still adds a `mailmap` subcommand that accepts `yes`/`no` arguments to toggle name/email rewriting in long-running processes. 

Key changes in this iteration focus on documentation and testing improvements. Siddharth agrees to simplify the man page description by removing implementation details about caching and making the behavior description more concise. The documentation will now state that mailmap applies to all subsequent commands when enabled, rather than listing specific subcommands. 

On the technical side, Siddharth will convert the `mailmap_loaded` flag to a `bool` type and add new test cases verifying interaction between the runtime mailmap toggle and the existing `--[no-]mailmap` command-line options. These changes address Karthik's suggestions while maintaining the existing design where mailmap data stays cached in memory after first load, allowing efficient toggling without disk I/O.

The patch remains well-tested with existing cases covering basic toggling and buffer mode operation. With these final polish items addressed, the implementation appears ready for merging, having cleared multiple rounds of review including feedback from Junio Hamano and Patrick Steinhardt. The changes demonstrate GitLab Gitaly's practical need for dynamic mailmap control in cached processes while maintaining clean interface design.