This is the fifth patch in Patrick Steinhardt's six-part refactoring series improving flag handling in Git's object database (ODB) interface. The series is part of the ongoing ODB abstraction effort to enable pluggable backends.

This patch focuses on renaming the flags for `odb_has_object()` to follow the established conventions documented earlier in the series. The old `HAS_OBJECT_*` flag names are updated to `ODB_HAS_OBJECT_*` to be properly prefixed with the function name. The changes are mechanical but widespread, touching 22 files across various builtins and core functionality.

The patch maintains all existing behavior while making the code more consistent and type-safe. It continues the pattern seen in previous patches of this series, applying similar enum conversions to standardize flag handling across the ODB subsystem. The changes are well-contained and follow the same careful, systematic approach that characterizes Patrick's work on the ODB layer.

This is a straightforward code hygiene improvement that builds on the foundation laid by earlier patches in the series. While not user-visible, it contributes to the larger goal of making the ODB interfaces more maintainable and consistent as the abstraction work progresses.