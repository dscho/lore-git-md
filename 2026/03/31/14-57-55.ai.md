Feature proposal for a new `git whoami` command to display user identity. Giorgos Gasparis suggests adding a built-in command that would combine the functionality of `git config user.name` and `git config user.email` into a single output. The motivation comes from developers frequently needing to verify their active Git identity when switching between work and personal repositories.

The proposal notes that many users currently solve this with custom aliases, suggesting there's a clear need for this functionality. The suggested implementation would provide a cleaner, more official solution than individual workarounds. The email doesn't include a patch or discuss implementation details, focusing instead on establishing the use case and value proposition.

This appears to be a well-motivated quality-of-life improvement targeting a common workflow pain point. The simplicity of the concept and its alignment with Git's existing configuration model suggest it could be straightforward to implement, though the proposal would need to be followed by actual code to be evaluated properly. The lack of technical details in this initial email means reviewers would likely ask for a concrete implementation before forming strong opinions.