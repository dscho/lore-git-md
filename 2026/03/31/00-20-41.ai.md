This is a clarification request about the merging process for a series of related patches that modify `git format-patch`'s cover letter formatting capabilities. The email comes from Mirko Faina, who has contributed three interconnected patch series:

1. `mf/format-patch-cover-letter-format` (merged to 'next') - Introduced new `--cover-letter-format` option and `format.commitListFormat` config to customize how commits are listed in cover letters, adding `%(count)` and `%(total)` placeholders.

2. `mf/format-patch-commit-list-format` (also merged to 'next') - Renamed the option to `--commit-list-format`, added a "modern" preset, and improved CLI ergonomics. This builds on the first series.

3. `mf/format-patch-commit-list-format-doc` (pending) - Documentation updates and a fix removing unconditional 72-character line wrapping in commit lists.

Mirko is concerned that merging the intermediate series separately could introduce unwanted behavior (the 72-character wrapping) before the final fixes land. He suggests holding the merge of the second series until the documentation/fixes in the third series are ready to go together. This shows careful consideration of how the changes will land in practice, not just in theory. The thread context indicates these patches are part of Git's ongoing effort to modernize and make more flexible the cover letter generation in `format-patch`.