This is a follow-up discussion about a commit-graph bugfix patch that addresses a regression causing crashes with commit dates beyond year 2514. Patrick Steinhardt is checking in with Junio Hamano about the status of his fix, which was previously marked as "Waiting for review response" in Junio's "What's cooking" report.

The patch fixes an underflow issue when writing generation data for commits with very large timestamps (exceeding 34 bits). This is a regression that could cause Git to crash during commit-graph operations. Patrick specifically wants confirmation that the fix will be included in the next integration branch ('next') and make it into the upcoming release candidate (rc0) to prevent the regression from appearing in any tagged release.

The thread context shows this is part of ongoing work on the commit-graph subsystem, which has been a focus area for performance optimizations and stability fixes. The patch appears ready for merging based on prior discussion (referenced by message ID [1]), and Patrick is just seeking confirmation of the merge plan to ensure the regression doesn't slip into a release.