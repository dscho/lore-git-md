This is a discussion about a proposed `git whoami` feature to display user identity. Kristoffer Haugsbakk chimes in with additional context, noting they independently had the same thought while working with `git interpret-trailers`. They confirm Junio's earlier observation that `git var GIT_AUTHOR_IDENT` provides similar functionality but requires postprocessing step to remove the timestamp.

The thread continues to explore whether the proposed command offers enough value over existing solutions. Kristoffer raises a valid concern about Git's already large command set, implying the bar for new commands should be high. The discussion remains constructive but shows the proposal needs to clearly demonstrate advantages beyond what `git var` and simple aliases can provide.

The technical aspects seem straightforward - this would be a simple wrapper combining existing config lookups - but the debate centers on whether it's worth adding to core Git given the alternatives. The next step would likely be for the original proposer to address these concerns with concrete use cases where current solutions fall short.