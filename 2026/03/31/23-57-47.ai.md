This is the second patch in Patrick Steinhardt's six-part refactoring series improving flag handling in the object database (ODB) subsystem. The patch focuses on standardizing the type used for flags in `odb_for_each_object()` and related functions.

The change converts the flags parameter from an `unsigned` type to the more specific `enum odb_for_each_object_flags` across four files (odb.c, odb.h, packfile.c, packfile.h). This is a mechanical change that makes the interface more type-safe and self-documenting, following the conventions established in the first patch of the series which documented flag handling guidelines.

The patch touches the core object database interface but makes no functional changes - it's purely a code hygiene improvement. As with Patrick's ODB abstraction work, this change is carefully scoped and well-explained, though the commit message could benefit from mentioning that this is part of the larger series (which the diff context makes clear through the version numbering).

This continues the pattern seen in the series of making the ODB interface more consistent and better documented, which will help with the eventual goal of supporting pluggable backends. The changes appear straightforward and unlikely to be controversial, being a simple type conversion with no behavior impact.