This is a test modernization patch from Trieu Huynh, part of their GSoC project to improve Git's test suite. It continues the series' pattern of replacing direct command piping with temporary files and `test_grep` to properly capture Git command exit codes.

The patch modifies t6101-rev-parse-parents.sh, converting one test case that previously piped `git rev-parse` output directly to grep. The new version writes the output to a temporary file first, then uses `test_grep` to verify the content. This ensures any failure in `git rev-parse` would be caught rather than masked by the pipe.

Like the other patches in the series, this is a mechanical but important change that follows the established pattern seen in t8003 and other test files. The modification is straightforward and maintains the same test assertions while improving reliability. The series as a whole appears to be making consistent, focused improvements to test infrastructure without introducing new functionality.