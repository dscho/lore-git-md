This is a test modernization patch from Trieu Huynh, part of their GSoC project to improve Git's test suite. The patch continues the series' pattern of replacing direct command output piping with temporary files and `test_grep` to properly capture Git command exit codes.

The change modifies t6101-rev-parse-parents.sh, converting a `git rev-parse | grep` pipe into separate output redirection and `test_grep` steps. This ensures test failures in `git rev-parse` won't be masked by the pipe. The patch follows the same mechanical but important improvement pattern seen in previous patches in the series (t0100, t2206, t7004, etc.), maintaining test assertions while making them more reliable.

The modification is straightforward - just one test case in this file needed updating. Like the rest of the series, this change doesn't alter test behavior or Git functionality, only improves test robustness. The approach remains consistent with prior work and the established t8003 pattern referenced in the cover letter.