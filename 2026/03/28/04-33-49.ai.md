This is a review response discussing technical details of the `git replay --revert` implementation, focusing on three specific concerns:

1. **Extra headers handling**: Tian Yuchen questions whether reverts should inherit the original commit's extra headers (like GPG signatures) since reverts create new commits with current metadata. This touches on whether reverts should be treated as derivative works or fresh operations.

2. **Revision range validation**: The reviewer notes that while the current check prevents ambiguous multi-range reverts (like `main f1 f2`), it may also block valid use cases (like `HEAD~5..HEAD~3 HEAD~1..HEAD`). They suggest the `positive_refexprs > 1` condition might be too broad.

3. **Branch creation convenience**: A minor ergonomic suggestion to allow creating new branches directly via `git replay --revert new-branch HEAD~3..HEAD` rather than requiring pre-created branches.

The feedback is constructive and technical, coming from someone clearly familiar with the codebase. These points don't challenge the core design (which has already been extensively reviewed) but suggest refinements to edge case handling and usability. The tone is collaborative, aiming to "spark discussion" rather than demand changes.

This is the final patch in a well-vetted series, so these observations are likely too late to affect the current iteration, but they raise valid considerations for future improvements. The core functionality remains sound, as evidenced by the thorough review history and consensus from key maintainers.