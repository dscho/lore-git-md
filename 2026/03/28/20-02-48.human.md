Test modernization patch from Trieu Huynh (GSoC participant) updating t4140-apply-ita.sh to improve test reliability. This is patch 9/16 in a series addressing a common test suite issue where piping Git command output directly to other commands can mask failures by suppressing exit codes.

The change replaces a `git diff | grep` pipe with explicit output redirection to a temporary file followed by `test_grep`, matching the pattern established in earlier patches of this series. The modification ensures test failures won't be hidden if the `git diff` command itself fails. The patch follows the same mechanical but important improvement approach seen throughout the series, focusing specifically on the "apply with intent-to-add" test file.

Like previous patches, this maintains all existing test assertions while making the test more robust. The change is small and straightforward, continuing the series' consistent pattern of test infrastructure improvements without modifying Git's actual behavior. No reviews or feedback on this specific patch yet, but the overall approach has been clean and focused across the series.