This is a test modernization patch that improves error detection in the test suite. The change modifies t6101-rev-parse-parents.sh to avoid piping git-rev-parse output directly to grep, which would mask git's exit code if the command fails. Instead, it writes the output to a temporary file first, then checks the contents separately using test_grep.

The patch is small and focused, changing just one test case to follow the project's preferred pattern for capturing command output. This makes the test more robust by ensuring failures in git-rev-parse itself would be caught rather than silently ignored. The commit message clearly explains the motivation and the technical reason for the change (preventing unnoticed crashes).

The change aligns with Git's ongoing test modernization efforts to improve reliability and maintainability of the test suite. The use of test_grep rather than raw grep is also consistent with the project's test helper conventions. This appears to be a straightforward improvement with no obvious downsides or controversy.