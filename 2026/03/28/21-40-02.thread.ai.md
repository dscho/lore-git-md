Thread introducing TypeScript support for Git's built-in userdiff patterns, contributed by Dhruv Arora as a Google Summer of Code project. The implementation adds a TypeScript pattern to userdiff.c that handles common syntax including function declarations with export/async modifiers, class definitions, method definitions, arrow functions, and variable assignments with const/let/var. The second patch in the series makes three key improvements: fixes the pattern ordering in userdiff.c by moving the TypeScript entry to its correct alphabetical position between Scheme and TeX patterns, adds three new test files demonstrating pattern matching for class methods/exported default classes/exported async functions, and refines the pattern to properly handle the `default` keyword in exports while fixing variable declaration matching. The changes verify the pattern correctly identifies function/class names (marked as "RIGHT") that should be highlighted in diffs. The test coverage now includes more complex TypeScript syntax like async/await and class member declarations. The series is well-structured with focused commits and appropriate test coverage, appearing ready for merging as a self-contained GSoC project that fills a gap in Git's language support (TypeScript lacked dedicated patterns despite widespread use while JavaScript was already supported). Real-world TypeScript usage may reveal additional edge cases that could be addressed in follow-ups.