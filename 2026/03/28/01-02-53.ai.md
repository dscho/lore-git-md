This is a reply in the ongoing discussion about per-remote SSH configuration options, where Wesley Schwengle responds to brian m. carlson's arguments about existing solutions. The thread has established clear positions between those advocating for SSH host aliases and Git's existing mechanisms versus Wesley's push for Git-level configuration.

Wesley reiterates his workflow argument, emphasizing that SSH host aliases require machine-global configuration changes rather than Git-specific settings that could be version-controlled. He cites real-world use cases like myrepos and Perl metacpan where committed configurations would need URL rewriting, which he argues is less elegant than native Git configuration. His key point is that while brian's suggested solutions (url.insteadOf and core.sshCommand with -i) are technically possible, they require scripting where a declarative Git configuration would be cleaner.

The reply maintains Wesley's consistent position that Git-level SSH configuration would provide better portability and team workflow support, particularly for organizations using version-controlled repository management tools. However, it doesn't directly address the flexibility tradeoffs brian raised regarding core.sshCommand's ability to handle additional SSH options and dynamic scenarios. The discussion remains at an impasse between workflow ergonomics advocates and those prioritizing flexibility and minimal Git-specific SSH configuration.