This is a test modernization patch from Trieu Huynh, part of a 16-patch GSoC series improving test reliability by eliminating command piping that could mask exit codes. 

The patch updates t7004-tag.sh to replace two instances where `git cat-file` output was piped to `grep` with a more robust pattern: redirecting output to a temporary file (`actual`) and using `test_grep` to check the contents. This ensures test failures won't be hidden by pipe behavior. The changes are mechanical and follow the established pattern from t8003 that the series adopts.

The modifications are straightforward test assertions about tag messages in subdirectories, with no behavior changes to the actual Git commands being tested. The patch demonstrates the series' consistent approach - each change follows the same template of replacing pipes with temporary files and `test_grep`. This appears to be a clean, focused improvement to test reliability in the tag command tests.