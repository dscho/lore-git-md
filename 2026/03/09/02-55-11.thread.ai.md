Thread identity: Bugfix patch addressing const-correctness warning in bloom filter code when building with glibc-2.43, now with maintainer acknowledgment.

Core problem: The code declared a path parameter as const char* while modifying the string through a strrchr() pointer, triggering a compiler warning. The mismatch between const declaration and actual usage needed correction.

Current status: Single patch version sent and acknowledged by maintainer Junio C Hamano with "Right. Thanks" response, indicating likely merge without further discussion. Changes made: removed const qualifier from path parameter in get_or_compute_bloom_filter() and eliminated an unnecessary cast when assigning path to last_slash when no slash is found.

Key technical details: Touches bloom.c, specifically modifying the function signature of get_or_compute_bloom_filter() and one line of pointer manipulation in fill_bloom_key(). The fix makes the code's behaviorally correct mutability explicit at the type level without changing runtime behavior.

Participant positions: Maintainer Junio C Hamano has acknowledged the fix positively. No other discussion needed as this is a straightforward type-system cleanup aligning declarations with existing behavior. The patch remains uncontroversial and minimal in scope.

Related work: Similar const-correctness fixes are common in the codebase, particularly when compiler warnings expose mismatches between declared and actual usage patterns. This follows that established pattern of type-system hygiene improvements.