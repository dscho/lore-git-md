This is a test patch in Adrian Ratiu's ongoing hook configuration work, specifically verifying hook execution ordering behavior. It's part of a 10-patch series building on his previous accepted work currently in the 'next' branch.

The patch adds a new test case confirming that configured hooks (specified via Git config) execute before legacy hookdir hooks when multiple hooks are present for the same event. The test sets up two config-based hooks ("first" and "second") alongside a hookdir hook, then verifies both the listing order via `git hook list` and the actual execution order via `git hook run`. The test output shows config hooks running first, followed by the hookdir hook.

As a minor cleanup, the patch also removes a duplicate `git hook list -h` invocation from an existing usage test that was apparently noticed during development. The change is straightforward and well-contained, adding test coverage for documented behavior while cleaning up a small redundancy. Patrick Steinhardt suggested adding this test to verify the execution ordering guarantee.

The patch touches only the test file t/t1800-hook.sh, adding 28 lines for the new test while removing 1 redundant line. It follows the project's test style and includes clear expected/actual verification patterns. As part of an established series with prior review, this appears to be a straightforward addition of test coverage for existing functionality.