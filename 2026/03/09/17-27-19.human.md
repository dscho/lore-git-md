This is a documentation patch clarifying the behavior of `git diff -U` when used without a line count argument. The patch updates the man page to explicitly state that the number is optional for the short `-U` form, matching Git's longstanding behavior.

The thread context reveals that Junio Hamano initially identified an inconsistency where `-U` without a number was accepted while malformed inputs like `-Unan` were rejected. Tian Yuchen's analysis confirmed that enforcing strict numeric arguments would break existing tests (notably t4013.157) that rely on `-U` defaulting to 3 lines of context.

Rather than changing the implementation to match the previously inaccurate documentation, this patch takes the pragmatic approach of updating the docs to reflect reality. The change is minimal - just adding square brackets around `<n>` in the `-U` documentation to indicate optionality, while keeping the `--unified` long form's behavior unchanged (still requiring an explicit number).

This is a sensible resolution that preserves backward compatibility while fixing the documentation discrepancy. The patch is straightforward and uncontroversial, addressing a minor but real documentation bug that could confuse users reading the man pages.