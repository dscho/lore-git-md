This is a performance discussion thread about `git fetch` behavior on slow network mounts, specifically SMB/CIFS shares with asymmetric bandwidth. The email continues R. Diez's investigation into why fetches are slow, now focusing on parallelization opportunities and protocol implementation details.

R. Diez challenges brian m. carlson's characterization of Git's sequential file operations as "optimized", arguing that true optimization would involve parallel I/O - something modern tools like GNU make and even Git itself (for multi-remote/submodule cases) already implement. He suggests documenting this limitation more clearly to help users make informed decisions about their setup.

The discussion touches on alternative tools like rsync (which would check file metadata first) and explores how Git uses SSH - questioning whether it requires a Git binary on the remote host or falls back to basic SSH file operations. R. Diez notes his repository has no branches, making branch-specific fetch optimizations irrelevant in his case.

Key technical details for future reference: the thread confirms Git always uses the upload-pack protocol internally (explaining the observed behavior), explores SSH transport implementation questions, and maintains focus on SMB/CIFS performance characteristics. The exchange highlights a tension between Git's design assumptions (optimized for low-latency local filesystems) and real-world high-latency network scenarios.