This is a test modernization patch that updates `t0004-unwritable.sh` to use the preferred `test_path_exists` helper instead of direct `test -e` invocations. The change improves test failure diagnostics by providing clearer error messages when path existence checks fail.

The patch converts four instances where test prerequisites were checking for file existence using the older `test -e` syntax. Each case involves verifying that error output files exist after testing Git commands' behavior with unwritable repositories. The changes are straightforward mechanical replacements following the project's test modernization guidelines.

The commit message notes these changes were identified using a suggested grep pattern from the microproject ideas list, indicating this is likely a new contributor's first patch. The changes are small, focused, and correctly follow the project's conventions for test improvements.