Thread identity: Adrian Ratiu's ongoing work "[PATCH 0/10] hook: cleanups and small features" extending Git's hook configuration support, building on previous work currently in 'next'. Latest addition is a refactoring patch (v2 11/10) simplifying hook cleanup API.

Core problem or goal: Improve and extend Git's hook configuration subsystem with cleanups, small user-facing additions to `git hook list` (--show-scope and disabled hook display), robustness checks for invalid hook states, and now API simplifications. Continues Adrian's work to make hooks configurable via Git's configuration system.

Current status: Based on code already in 'next' branch from prior config-based hook work. Series includes refactoring, cleanup patches, two new features, defensive programming checks, and now API simplifications. Latest patch replaces custom hook cleanup with standard string_list_clear_func() pattern.

Key technical details:
- Touches hook.c, hook.h, string-list.[ch], builtin/hook.c, refs.c and related test files
- Adds --show-scope to `git hook list` to display config origin (system/global/local)
- Modifies `git hook list` to show disabled hooks
- Adds BUG() checks for invalid hook states (enabled without command, unknown type)
- Replaces hook_list_clear() with string_list_clear_func() and new hook_free() helper
- Stores data-free callback in hook struct rather than passing at cleanup time
- Renames function pointer types from `cb_data_free_fn`/`cb_data_alloc_fn` to `hook_data_free_fn`/`hook_data_alloc_fn` for clarity
- Relocates `unsorted_string_list_remove()` to string-list module
- Style cleanups: CALLOC_ARRAY usage, struct initialization, boolean logic, braces, includes, message capitalization
- Includes test coverage for new functionality
- Builds on existing config-based hook infrastructure

Open questions and loose ends: None mentioned in cover letter. Style changes, naming improvements, robustness checks, and now API simplifications address prior review feedback from Patrick Steinhardt and Junio Hamano.

Participant positions: Adrian is the author building on his prior accepted work. Patrick Steinhardt suggested both the string-list utility move, the function pointer rename, flagged style issues, proposed the robustness checks, and recommended the latest API simplification. Junio Hamano also provided style-related review comments on earlier versions.

Related work: Directly continues Adrian's previous hook configuration series that's currently in 'next'. Split off from a larger config series previously discussed.