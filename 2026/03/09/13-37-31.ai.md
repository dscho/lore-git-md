This is a bugfix patch that addresses a memory leak in `repo_init()`. The leak occurs when initializing a repository with v0 format but with v1 extensions enabled - an edge case that only happens in development environments. The patch adds a missing call to `clear_repository_format()` in the error path to properly clean up allocated memory.

The fix is straightforward and mechanical - just one line added to ensure proper cleanup. While the leak was unlikely to affect normal usage, it's patch properly closes the potential resource leak. This is the first patch in a larger series about parallel hook execution, but this particular change stands alone as a simple correctness fix that could be applied independently.

The commit message clearly explains both the problem and solution, and the change itself is minimal and focused. This appears to be a solid, uncontroversial fix that should be easy to review and merge.