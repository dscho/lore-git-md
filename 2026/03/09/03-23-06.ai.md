This is a small refactoring patch that addresses a compiler warning in the `remove_path()` function in `dir.c`. The warning occurs when building with glibc-2.43 due to discarding of a `const` qualifier when using `strrchr()` on the input path.

The patch introduces a separate `const char *last` variable to hold the result of `strrchr()` on the original input string, while keeping the mutable `slash` variable for the strdup'd copy. This makes the code's intent clearer and silences the compiler warning without changing behavior.

The change is straightforward and appears correct - it's a minor cleanup that improves type safety and code clarity. The patch follows Git's coding style and includes a clear commit message explaining both the problem and solution. Since it's a simple refactoring with no behavior change, it's unlikely to be controversial.