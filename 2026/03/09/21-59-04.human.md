This is a review comment from Junio Hamano on patch 9/10 of Adrian Ratiu's hook configuration series, focusing on the `--show-scope` output format for `git hook list`. The patch adds scope information (local, global, etc.) to hook listings when requested, but Junio questions whether the current implementation provides optimal machine readability when combined with the `-z` NUL-terminator option. He suggests the output format might better serve script consumers by separating the friendly name and scope into distinct fields rather than combining them in parentheses.

The comment indicates general approval of the patch's technical approach (noting it correctly uses the new `hook_config_cache_entry` structure introduced earlier) while raising a usability consideration for machine parsing. This is a minor refinement point rather than a blocking issue, consistent with Junio's earlier positive reception of the series. The discussion remains focused on implementation details rather than fundamental design at this stage.