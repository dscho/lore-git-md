This is a technical discussion about refactoring the object filter parsing code to avoid unnecessary strbuf usage, part of an ongoing cleanup effort.

Jeff King (Peff) reviews Deveshi Dwivedi's patch that replaces strbuf_split_str() with direct string handling in parse_combine_filter(). He suggests using strchrnul() for simpler pointer arithmetic and points out that the current behavior of handling trailing '+' delimiters may be correct per documentation, contrary to the original implementation. While approving the general direction, he notes the allocation pattern could potentially be optimized further but considers it not worth the complexity for this case.

The discussion builds on Junio Hamano's prior approval of the first patch in this series and continues the project-wide effort to reduce strbuf overhead where simpler string operations suffice. The thread shows consensus on the technical approach but surfaces a subtle documentation vs implementation discrepancy around filter syntax handling that may need resolution before finalizing the patch.