Test modernization patch updating t0004-unwritable.sh to use the preferred test_path_exists helper instead of direct test -e calls. This is part of the ongoing effort to modernize Git's test suite by replacing low-level shell test constructs with more robust test helper functions.

The patch changes four instances where test -e was used to check for the existence of output files in test prerequisites. The test_path_exists helper provides better diagnostic output when tests fail, making it easier to debug issues. The changes are mechanical and straightforward, simply replacing the test syntax while maintaining the same functionality.

This appears to be a clean, focused contribution that aligns with established test suite modernization patterns. The commit message clearly explains the motivation (better diagnostics) and how the changes were identified (via grep pattern matching). The patch touches only one test file and makes no functional changes to the tests themselves - it purely updates the test infrastructure usage.