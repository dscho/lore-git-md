Thread identity: Patch series (v3) to move `trust_executable_bit` from global to repository-scoped storage in `struct repo_settings`, part of the ongoing `the_repository` removal effort.

Core problem: Eliminating global state by making the `trust_executable_bit` setting repository-specific rather than process-wide, consistent with Git's architectural direction. This affects how Git handles executable bits when applying patches and computing diffs.

Current status: Third iteration (v3) of a three-part series now complete. The final patch removes direct handling of `core.filemode` from `environment.c` and moves it to `repo-settings.c` for lazy loading via `prepare_repo_settings()`. All references now use `the_repository->settings.trust_executable_bit` instead of the old global variable.

Key technical details: Changes involve:
- Adding `trust_executable_bit` to `struct repo_settings`
- Updating `apply.c` and `diff-lib.c` to use repository-specific settings
- Moving initialization to `repo_cfg_bool()` in `repo-settings.c`
- Removing global variable and direct config handling from `environment.c`
- Maintaining lazy-loading behavior through `prepare_repo_settings()`

Open questions: None remaining - the series appears ready for merging as part of the larger `the_repository` elimination effort.

Participant positions: The author has incorporated feedback from previous rounds (exact reviewers not named in this patch's changelog, but presumably including René Scharfe and other regular participants in `the_repository` removal effort).

Related work: Part of the multi-year `the_repository` removal project that touches virtually all subsystems to eliminate global state. This patch follows the established pattern of moving configuration from global to repository-scoped storage.