This is a documentation patch that follows up on Tian Yuchen's clarification of the `-U` option behavior in `git diff` and `git show`. The thread had previously established that while the documentation implied `-U` required a number, the implementation has always accepted `-U` without one (defaulting to 3 lines of context, matching `-u` behavior).

D. Ben Knoble suggests two potential documentation improvements: first, that the long form `--unified` could also be shown with optional syntax as `--unified[=<n>]` to match other Git documentation patterns, and second, that the default value description could be clarified to explicitly state that it applies both when the config is unset and when `<n>` is omitted. He notes this pattern appears elsewhere in the docs, though some entries are more explicit about the conditions.

The email also points out that these option descriptions are duplicated between two documentation files (`diff-options.adoc` and `diff-context-options.adoc`), though this appears to be an observation rather than a suggested change. The core of the message is about refining the documentation syntax to better match both existing conventions and the actual command behavior, building on Yuchen's initial patch that fixed the discrepancy between docs and implementation.