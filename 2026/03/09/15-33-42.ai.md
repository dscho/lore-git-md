This is a CI/build system patch in response to Patrick Steinhardt's series addressing macOS 15 iconv issues. Justin Tobler reviews the first patch, which simplifies the Meson build configuration for detecting BOM handling in iconv.

The patch removes redundant variables and version-dependent typedefs in the iconv-emits-BOM check, replacing them with a simpler void pointer cast approach. Justin confirms the change maintains the same functionality while making the code more straightforward. The review is positive, noting the simplification achieves the same behavior without the previous complexity.

This is part of a larger effort to ensure consistent iconv behavior across build systems (Makefile and Meson) on macOS 15, following up on a prior Makefile fix (d0cec08d70). The series demonstrates careful attention to cross-platform compatibility and build system maintenance, with this particular patch focusing on code clarity in the Meson configuration.