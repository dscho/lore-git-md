This thread addresses a straightforward bugfix for a const-correctness warning in Git's bloom filter code when building with glibc-2.43. The original patch identified that a path parameter was incorrectly marked as const while the code actually modified the string through a strrchr() pointer, triggering a compiler warning.

The fix removes the misleading const qualifier from the path parameter in get_or_compute_bloom_filter() and eliminates an unnecessary cast, making the code's mutability behavior explicit at the type level. The change is minimal - just two lines in bloom.c - and doesn't affect runtime behavior, only aligning the type system with the actual string manipulation.

Maintainer Junio C Hamano has already acknowledged the fix with a "Right. Thanks" response, indicating this uncontroversial type-system cleanup is likely to be merged without further discussion. The thread appears complete with this maintainer approval.