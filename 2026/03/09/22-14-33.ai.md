This is a discussion about submodule workflow improvements, specifically around branching operations across parent and submodule repositories.

D. Ben Knoble responds to Florian Schmidt's original proposal for adding `--also-parent` and failure tolerance to `git submodule foreach`. Knoble suggests that existing submodule configuration options (`submodule.recurse` and `submodule.propagateBranches`) might already address the branching workflow needs that motivated Florian's proposal, though he acknowledges this isn't a general-purpose solution.

The email doesn't introduce new technical details but rather points to existing mechanisms that could potentially solve the use case without new flags. This keeps the discussion alive while questioning whether the proposed changes are actually needed given current functionality. The thread context shows this is part of an ongoing evaluation of how best to handle cross-repository operations, with Jeff King's `for-each-repo` proposal currently leading in consensus.