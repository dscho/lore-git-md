This is the final patch in a const-correctness refactoring of the patch-id subsystem, specifically addressing an 8-year-old NEEDSWORK comment in `patch_id_neq()`. The patch replaces the old comment with clear documentation explaining the intentional const-cast compromise that preserves a performance optimization from 2016's b3dfeebb commit.

The key technical points are:
- The hashmap API requires const parameters for comparison functions
- The patch-id comparison needs to modify state (lazily computing patch IDs)
- The solution uses `container_of()` for an explicit, documented const-cast
- The optimization avoids unnecessary patch ID computations during rebase operations

The documentation now explicitly ties this design choice to the rebase optimization from b3dfeebb, providing future maintainers with clear context about why the const violation is justified. This represents a well-considered resolution to the tension between type safety and performance in this specific case.

The patch is ready for merging, having gone through multiple rounds of review with consensus established on both the technical approach and now the final documentation wording. It serves as a good example of how to document necessary compromises between competing code quality concerns.