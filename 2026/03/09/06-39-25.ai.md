This is a follow-up in a const-correctness refactoring thread about the patch-id subsystem. The email responds to Junio Hamano's review of v2, which documented a deliberate const-cast in `patch_id_neq()` as a trade-off between type safety and lazy evaluation optimizations.

Junio had requested more precise wording in the documentation comment, specifically asking to clarify that the lazy evaluation avoids unnecessary work during rebase operations (referencing commit b3dfeebb from 2016) rather than framing it as preventing a generic "performance regression". The author (Yuchen) acknowledges this feedback and agrees to incorporate the suggested phrasing in v3, which will reference the historical commit that introduced the lazy evaluation optimization.

The thread is nearing completion with only this minor documentation polish remaining. The technical approach - using `container_of()` for an explicit, justified const-cast to preserve the lazy evaluation optimization - has already been approved by Junio. This represents the final step in resolving an 8-year-old NEEDSWORK comment about const-correctness in the patch-id comparison machinery.

Key technical details for future reference: the conflict arises between hashmap API requirements (const parameters for comparison functions) and the need to modify objects in `commit_patch_id()`. The solution preserves the existing optimization where patch IDs are computed lazily during rebase operations to avoid unnecessary computations, while documenting the const-cast as a conscious design choice. The affected files are `builtin/log.c` and `patch-ids.[hc]`, with no structural changes to either the patch-id or hashmap subsystems.