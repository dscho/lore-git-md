This is a code cleanup patch in Adrian Ratiu's ongoing hook configuration series. The change replaces a custom `hook_list_clear()` function with the more standard `string_list_clear_func()` pattern, requiring some reworking of the hook cleanup logic. 

Eric Sunshine points out a minor style issue regarding the use of `UNUSED` in a header file, referencing a previous discussion with Junio Hamano about when this attribute should be used. The patch itself appears straightforward - it's part of the broader effort to standardize and clean up the hook subsystem's APIs while adding new configuration capabilities.

The series as a whole is nearing completion, with this being one of the final polish patches addressing reviewer feedback. The change doesn't introduce new functionality but makes the code more consistent with Git's common patterns for string list handling.