Test modernization patch updating path existence checks in t0004-unwritable.sh to use the test_path_exists helper instead of raw test -e commands. This is part of the ongoing effort to modernize Git's test suite by replacing direct shell test commands with more robust helper functions that provide better diagnostic output on failures.

The patch changes four instances where test -e was used in lazy prereq definitions to check for output files. Each replacement is straightforward, converting the old-style test to use test_path_exists with the same path argument (though with slightly different quoting style). The commit message notes this was found via a grep pattern suggested in the microproject ideas list, indicating it's part of a systematic cleanup rather than a one-off fix.

This is a mechanical change with no behavior difference - the test logic remains identical, just uses the preferred modern test helper. The patch is small, focused, and follows the established pattern for such test suite updates. It touches only one test file and makes no changes to actual test logic or assertions.