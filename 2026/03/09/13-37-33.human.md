This is part 3/9 of a feature series introducing parallel hook execution, building on Adrian Ratiu's earlier work on configurable hooks. The patch adds a global `hook.jobs` configuration option that sets the default number of hooks that can run simultaneously across all events.

The implementation parses the new config key during repository initialization and stores it in the hook configuration cache, though the value isn't yet used (that comes in later patches). The config defaults to 1 (serial execution) when unspecified, maintaining backward compatibility. Invalid values (non-positive integers) trigger warnings but are ignored.

This is straightforward plumbing work that follows the series' pattern of adding parallelization controls incrementally. The patch includes documentation updates and maintains the series' careful approach to backward compatibility. The implementation looks clean, with proper memory management and error handling for config parsing.

As part of a larger effort to make hooks more flexible and performant, this patch sets up infrastructure that will be built upon in subsequent commits to enable parallel execution where explicitly configured. The series continues to show thoughtful design with multiple opt-in control points for parallelization.