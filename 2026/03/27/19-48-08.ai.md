This is a documentation and behavior patch series for `git format-patch`'s commit list formatting. The cover letter introduces two changes building on the earlier `--commit-list-format` work.

The first patch updates documentation to properly document the default behavior when neither `--commit-list-format` nor `format.commitListFormat` is set (it falls back to `shortlog` format). This follows up on previous discussion where Junio Hamano suggested tightening the wording around format string parsing and cover letter implications.

The second patch changes the implementation to remove unconditional line wrapping from commit list entries, giving more control to custom formats. This appears to be a logical follow-up to the earlier flexibility added by the commit list format options.

The series is based on recent work that introduced the `--commit-list-format` option and is now addressing some documentation gaps and behavioral refinements. The changes are focused and well-scoped, touching both documentation and the core formatting logic in `builtin/log.c`. Given the careful iteration visible in the thread history and the straightforward nature of these follow-up improvements, this series looks ready to proceed unless new technical concerns emerge.