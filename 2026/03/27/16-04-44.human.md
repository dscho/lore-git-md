This is a post-merge discussion about the recently integrated `git format-patch --commit-list-format` feature, which improves commit list formatting in patch series. The thread focuses on fine-tuning the output formatting now that the core functionality is in place.

Junio Hamano reports two minor formatting quirks in the new feature: lack of indentation under headings and occasional spurious blank lines when using width specifiers. Mirko Faina suggests the blank line issue may stem from how `strbuf_add_wrapped_text()` interacts with the 72-character width guarantee in format strings, where prefix text (" n: ") could push lines over the wrapping threshold.

The discussion reveals a philosophical difference about wrapping behavior - Junio argues that end-user configurable output shouldn't force wrapping unconditionally, while noting that the default "modern" format could reasonably include wrapping. This represents typical post-merge refinement where real-world use surfaces edge cases, though the core implementation remains sound as confirmed by earlier reviews.

The exchange is technical but constructive, focusing on improving the feature rather than challenging its fundamentals. Mirko appears receptive to feedback, and we can expect either documentation clarifications or minor follow-up patches to address these observations. The feature's progression through 'next' suggests it's on track for inclusion in an upcoming release once these final polish items are resolved.