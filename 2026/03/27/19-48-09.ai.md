This is a documentation patch clarifying the behavior of `git format-patch`'s `--commit-list-format` option and related configuration. It's part of an ongoing series refining the commit list formatting feature.

The patch updates two documentation files to better explain the default behavior when neither `--commit-list-format` nor `format.commitListFormat` is set (it falls back to `shortlog` format). It also clarifies that only the command-line option (not the config alone) implies `--cover-letter`, addressing feedback from earlier in the thread.

The changes are focused and well-scoped, touching only documentation files (`format.adoc` and `git-format-patch.adoc`). The updates make the behavior more explicit while maintaining technical accuracy, particularly around the format string parsing rules and cover letter implications. This appears to be a straightforward documentation improvement building on prior consensus in the thread.