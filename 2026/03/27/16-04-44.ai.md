This is a post-merge discussion about the recently integrated `git format-patch --commit-list-format` feature, focusing on edge cases in the formatting behavior. Junio Hamano and Mirko Faina are examining two specific issues that emerged after the series was merged to 'next': lack of indentation under headings and unexpected blank lines when using width specifiers.

The conversation reveals that the blank line issue likely stems from an interaction between Git's `strbuf_add_wrapped_text()` function and the 72-character width guarantee in format strings, where prefix text (" n: ") could push lines over the wrapping threshold. Junio suggests that unconditional wrapping of user-configurable output may be problematic, proposing instead to use the `%w(w,i1,i2)` format specifier for controlled wrapping only when explicitly requested.

This exchange represents the typical post-merge validation phase where real-world usage surfaces subtle interactions not caught during review. The discussion remains narrowly focused on these formatting edge cases without challenging the overall design. Mirko appears receptive to feedback, having already committed to preparing documentation updates, and now likely considering whether additional code adjustments are needed for the width specifier behavior. The feature's core functionality remains sound, as confirmed by Phillip Wood's earlier validation.