This is a refactoring patch from Ezekiel Newren, part of a series improving the xdiff subsystem's code clarity. The patch simplifies `xdl_cleanup_records()` by removing an unnecessary local pointer variable (`recs`) and directly indexing the `xdfile.recs` array instead. 

The changes are straightforward: the first two loops now use direct array access rather than maintaining a moving pointer, while the last two loops (which didn't use the pointer anyway) have their declarations cleaned up. The diff shows 17 lines changed but is mostly whitespace adjustments - the actual logic remains identical, just expressed more clearly.

This is the first of six patches in v3 of the series, which has already received maintainer approval in principle. The changes are purely internal with no behavior impact, focused on making the code more maintainable. The patch follows Git's style well and appears ready for merging.