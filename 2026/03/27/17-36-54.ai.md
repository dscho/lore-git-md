This is a documentation patch finalizing the `git stash` syntax clarification thread. Quentin Bernet confirms he'll leave the suggested parser improvement (making `git stash -m foo <pathspec>` work as an implicit push) as future work, focusing instead on accurately documenting the current behavior.

The patch updates both the man page and built-in help text to show that:
1. The `push` subcommand is optional for most options (`-m`, `-u`, etc.)
2. Pathspecs still require either explicit `push` or `--` disambiguation
3. The syntax aligns with current implementation behavior

Junio Hamano has already approved this approach, noting the documentation need not complicate the synopsis with near-identical forms. The changes are minimal but precise - adjusting bracket placement in the syntax diagrams and ensuring consistency between the man page and command-line help. This appears ready for merging as it accurately reflects the code's current behavior while leaving room for potential future parser improvements.