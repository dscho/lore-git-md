This is a refactoring patch in the ongoing xdiff cleanup series, where Junio C Hamano reports test failures in t4071 and t8015 when applying patch 4/6, despite the first three patches passing. The patch itself aims to improve clarity in xdiff's record cleanup logic by introducing explicit per-file limit variables (`mlim1` and `mlim2`) and removing redundant conditions.

The key technical points are:
- The patch separates limit calculations for each file upfront rather than recalculating them in loops
- It eliminates a redundant `!need_min` check that was previously part of the INVESTIGATE condition
- The changes maintain the same logic flow but with clearer variable naming and initialization

The test failures indicate there may be a behavioral difference introduced by these changes, despite the patch's intention to be purely refactoring. This suggests either:
1) The test failures reveal an existing bug that the refactoring now exposes
2) There is an unintended behavioral change in the refactored code
3) The tests have assumptions about implementation details that the refactoring changed

For future reference, this interaction shows that even well-intentioned refactoring can surface subtle issues, and that Junio is carefully verifying the series against the test suite. The test failures will need to be addressed before this patch can proceed.