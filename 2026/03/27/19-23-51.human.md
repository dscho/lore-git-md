This is a refactoring patch in Ezekiel Newren's series improving xdiff's record cleanup logic. Patch 4/6 focuses on making the limit calculations in `xdl_cleanup_records()` clearer by:

1. Introducing separate `mlim1` and `mlim2` variables for each file's limits, initialized upfront rather than recalculated in loops
2. Removing a redundant `!need_min` check that became unnecessary after earlier changes
3. Moving the limit initialization outside the loops for better readability

The changes are purely internal - no behavior changes are claimed. The patch continues the series' trend of improving type safety and clarity in the xdiff code, building on earlier work that introduced more specific types and simplified logic flows. With maintainer approval already given for the type improvements in v2, this appears to be final polishing before merging.