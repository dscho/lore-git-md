Refactoring patch in the xdiff subsystem, finalizing a series focused on improving code clarity in the diff engine's record cleanup logic. This sixth patch in the v3 series simplifies how the `INVESTIGATE` action is handled in `xdl_cleanup_records()`, making the state transitions more explicit.

The change replaces a combined conditional check with separate steps that first resolve `INVESTIGATE` into either `KEEP` or `DISCARD` (based on `xdl_clean_mmatch()` results), then handles those terminal states distinctly. This makes the control flow more linear and adds a `BUG()` check for invalid states, improving debuggability. The patch maintains the same behavior while making the logic more maintainable.

This appears to be the final polishing patch in a well-received series that has already gained maintainer approval. The changes are purely internal to the diff engine with no user-visible impact, continuing the series' focus on type safety and readability improvements in xdiff's core algorithms.