This is a documentation patch finalizing the clarification of `git stash` syntax rules, following maintainer approval. Quentin Bernet has incorporated feedback from Junio Hamano and other reviewers to accurately document when the `push` subcommand is required versus optional.

The patch updates both the man page and built-in help text to show that most options (like `-m` for message or `-u` for untracked files) work with both `git stash` and `git stash push`, while pathspecs require either explicit `push` or `--` disambiguation. Junio has confirmed this approach sufficiently captures the current implementation, though he noted a potential future parser improvement to make `git stash -m foo <pathspec>` work as an implicit push.

The changes are now in their final form, with the documentation clearly separating the standalone `git stash` case from `git stash push` while showing options work with both. The thread has reached consensus, and the patch appears ready for merging as part of the ongoing documentation standardization effort. No further action is required from the author unless reviewers request minor tweaks.