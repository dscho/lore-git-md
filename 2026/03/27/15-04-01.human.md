This is a response in the ongoing discussion about per-remote SSH configuration options, where the author Wesley Schwengle pushes back against Johannes Sixt's suggestion that SSH host aliases are sufficient. Wesley argues that the SSH config approach is "hacky" and creates problems in two specific scenarios.

First, he notes that modifying hostnames in SSH config breaks copy-paste workflows - users can't simply copy a standard remote URL like `git@gitlab.com/...` without also adjusting their SSH config. Second, he points out this approach fails for shared repository configurations (like those managed by `myrepos`), where changing hostnames would require everyone to make local SSH config adjustments.

The response is brief but highlights concrete limitations of the SSH-only approach that the proposed Git-level solution would address. The "Why not both?" closing suggests Wesley sees value in having both mechanisms available, though he doesn't elaborate on why the Git solution is preferable when SSH config isn't viable. This keeps the discussion focused on real-world usability concerns rather than theoretical purity.