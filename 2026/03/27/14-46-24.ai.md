This is a review comment on patch 7/12 in the parallel hooks feature series, specifically addressing the documentation portion of the CLI `-j/--jobs` option. Patrick Steinhardt confirms that the documentation now correctly states how the `-j` flag overrides both the global `hook.jobs` config and per-hook parallel settings. 

The exchange is brief but significant as it confirms an important behavioral detail in the parallel execution system - that command-line job counts take precedence over all configuration settings. This matches the established pattern in other Git commands like `git fetch` where CLI flags override config values. The documentation patch being reviewed here makes this precedence explicit in the man page, which is particularly important given the safety implications of parallel hook execution.

This is part of the larger parallel hooks effort that has already received maintainer approval, with this particular documentation clarification being one of the final touches before the series is merged. The thread shows the careful attention being paid to documenting behavior clearly, especially around override precedence and safety constraints for non-parallelizable hooks.