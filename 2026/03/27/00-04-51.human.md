This is a documentation discussion thread about the syntax formatting for `git stash` commands. Mirko Faina joins the conversation responding to D. Ben Knoble's earlier question about whether the original `git stash` documentation was actually incorrect. Faina argues that options like `-m` should be allowed without explicitly specifying `push`, since they are options rather than non-option arguments, and provides concrete examples showing the current behavior matches this interpretation. The discussion continues to explore the subtle distinction between what the documentation says versus how the command behaves in practice.

The thread remains unresolved but has surfaced an important nuance - the current implementation appears more permissive than the documentation suggests when it comes to accepting options without the explicit `push` subcommand. This could indicate either a documentation bug or potentially unintended behavior in the command's implementation. The patch in question touches both the man page (`Documentation/git-stash.txt`) and the command's help text in `builtin/stash.c`.