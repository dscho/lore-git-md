This is a documentation clarification in the ongoing discussion about `git stash` command syntax. Junio Hamano weighs in on the thread, agreeing with Mirko Faina's observation that the current documentation incorrectly implies options like `-m/--message` require the `push` subcommand, when in fact they work fine without it. 

Junio notes the confusion stems from the SYNOPSIS section's misleading bracketing of `[push]` as optional, suggesting the patch would have been clearer if it explicitly called out that the current grammar omits valid combinations like `git stash --include-untracked`. The discussion reveals a disconnect between the actual command behavior (where many options work without `push`) and the documentation's stricter implied rules.

This email moves the conversation forward by establishing consensus that the documentation needs fixing, though the exact wording remains to be settled. The thread continues to explore whether to adjust the docs to match reality or consider changing the command behavior for consistency. Junio's intervention as maintainer lends weight to the interpretation that the current behavior is correct and the docs should be updated accordingly.