Refactoring patch in the xdiff subsystem, completing a series focused on improving code clarity in `xdl_cleanup_records()`. This final patch in the v3 series simplifies the INVESTIGATE action handling by making the state transitions more explicit.

The change replaces a compound conditional that combined KEEP and INVESTIGATE checks with separate, clearer logic paths. Each INVESTIGATE action now explicitly transitions to either KEEP or DISCARD based on the result of `xdl_clean_mmatch()`, with the state change happening immediately rather than being embedded in the conditional. The patch also adds BUG() checks to catch any illegal states, improving robustness.

This continues the series' theme of making the xdiff code more maintainable through better type safety and explicit state handling. The changes are purely internal - no behavior changes are claimed or expected. With this patch, the series' goals appear complete: the INVESTIGATE logic is now more straightforward to follow, with clear state transitions and additional safety checks.

Key technical details for future reference:
- Splits compound conditionals into explicit state transitions
- Adds BUG() checks for illegal states
- Maintains identical behavior to previous implementation
- Completes the type safety and clarity improvements from earlier patches
- Touches the same xdiff/xprepare.c file as rest of series

The patch appears ready for merging, representing the final polish on a series that has already received maintainer approval in previous iterations.