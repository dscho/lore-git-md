This is an update on the experimental `git replay` command, continuing the thread about merging two replay-related topics to the 'next' integration branch. Junio Hamano is responding to Christian Couder's feedback about the readiness of these changes.

The first topic (`tc/replay-down-to-root`) adds root commit support to `git replay`. Christian confirms the known syntax issue (where "topic" fails silently instead of requiring "main..topic" format) will be handled separately, making the current implementation mergeable. Junio notes he'll need to revisit the original discussion to fully understand this edge case.

The second topic (`sa/replay-revert`) introduces a `--revert` mode that builds on shared message formatting extracted from the sequencer. Christian reports that version 6 has addressed all outstanding issues, including those previously identified by Phillip Wood. Junio accepts this assessment with a brief acknowledgment.

These exchanges show the maintainer's typical workflow: relying on topic maintainers (Christian for replay) and domain experts (Phillip for sequencer-adjacent changes) to vet technical details, while keeping track of known limitations that may need follow-up. The revert functionality in particular represents meaningful progress for the experimental command, suggesting it's evolving toward becoming a more comprehensive history-rewriting tool.