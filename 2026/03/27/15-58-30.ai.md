This is a documentation clarification in the ongoing discussion about `git stash` command syntax. Junio Hamano weighs in on the thread, agreeing with Mirko Faina's observation that the current `git stash` behavior accepts option flags like `-m/--message` without requiring the `push` subcommand, contrary to what the help text implies. 

Junio notes that the SYNOPSIS section inaccurately suggests `push` becomes mandatory when any options are given, when in reality only non-option arguments (like pathspecs) require `push`. He suggests the original patch's commit message could have been clearer by explicitly calling out that the current grammar incorrectly omits valid option combinations like `git stash --include-untracked`.

The maintainer's intervention confirms the documentation does not match actual behavior, though he doesn't prescribe a specific fix. This leaves open whether to: (1) update the docs to reflect current behavior, (2) modify the command to enforce the documented rules, or (3) find middle-ground wording. The thread continues to examine this interface inconsistency that affects both user expectations and command-line completion systems. Key files remain `Documentation/git-stash.txt` and `builtin/stash.c`.