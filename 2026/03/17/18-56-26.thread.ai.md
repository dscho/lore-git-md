Git replay root commit handling patch series (v1 -> v2) - merged with doc patch submitted

Thread identity: Patch series enabling `git replay` to handle root commits in the `git replay` command, authored by Toon Claes. Technical change merged with documentation patch now submitted addressing ongoing discussions. Maintainer Junio C Hamano's documentation philosophy prevails in the current approach.

Core problem and solution: The series fixes `git replay`'s rejection of root commits by moving root commit logic into `mapped_commit()` with NULL pointer safety and improved "empty tree" labeling. The merged implementation addresses all technical concerns. Toon Claes now submits a documentation patch that:
1. Clarifies each ref in range is replayed separately down to root
2. Corrects empty commit handling description
3. Specifies single positive ref requirement in advance/revert modes
4. References existing "Specifying Ranges" docs rather than duplicating

Technical changes in v2 (merged):
- Root commit handling refactored into `mapped_commit()`
- NULL pointer check for `commit` parameter
- UX improvement showing "empty tree" for root commit ancestors
- Verified via updated t3650-replay-root-commits.sh test case

Current status: Implementation merged to `next`. Documentation patch submitted by Toon addresses Junio's insistence on documentation accompany features, though Toon questions whether some explanations duplicate existing docs. The patch adopts Junio's view that root commits don't need special error messages, treating them as general boundary cases. Christian Couder's error message enhancement proposal appears set aside in favor of clearer documentation.

Open questions resolved:
1. Documentation approach settled - follows Junio's preference for explicit behavior explanations without special warnings
2. Root commits confirmed as general boundary case without unique error messaging
3. Documentation updates submitted with the feature rather than deferred

Participant positions:
- Toon Claes: Implemented feature and now documentation, accepts Junio's documentation philosophy while questioning some redundancy
- Junio C Hamano: Documentation approach prevails, root commits treated as general boundary case
- Christian Couder: Error message proposal not adopted in current direction
- Ben Knoble: Previously proposed UX improvements (not part of current doc solution)

Related work: Exemplifies Git's documentation process where technical implementation precedes user-facing refinement. The thread shows Junio's influence in ensuring documentation accompanies features, with the submitted patch balancing completeness against redundancy by referencing existing range documentation. The discussion has clarified that boundary cases (including root commits) should be handled through general documentation rather than case-specific error messages, establishing a precedent for similar future features.