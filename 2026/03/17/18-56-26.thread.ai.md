Git replay root commit handling patch series (v1 -> v2) - maintainer ack with doc follow-up and UX discussion

Thread identity: Patch series enabling `git replay` to handle root commits in the `git replay` command, authored by Toon Claes. Technical change now approved by maintainer Junio C Hamano with documentation follow-up suggested by Christian Couder and additional UX considerations raised by Ben Knoble.

Core problem and solution: The series fixes `git replay`'s rejection of root commits by moving root commit logic into `mapped_commit()` with NULL pointer safety and improved "empty tree" labeling. Christian Couder identified a documentation gap regarding commit range specification, while Ben Knoble's follow-up suggests this may warrant more than just documentation - potentially an explicit option or interactive confirmation for root commit cases, based on his monorepo workflow experience.

Technical changes in v2:
- Root commit handling refactored into `mapped_commit()`
- NULL pointer check for `commit` parameter
- UX improvement showing "empty tree" for root commit ancestors
- Verified via updated t3650-replay-root-commits.sh test case

Current status: Maintainer-approved and queued for merging to `next`. Implementation addresses all technical concerns. Documentation clarification about range specification remains as follow-up work, now with additional UX considerations from Ben Knoble about making the command more user-friendly for monorepo workflows. Junio Hamano has weighed in on the documentation discussion, suggesting it should explain the semantic difference between `main..topic` and `topic` range specifications rather than just warning against one form. He notes that replaying from root commits can be legitimate in some cases, so documentation should clarify use cases rather than steer users away from valid workflows.

Open questions: Whether to address range specification confusion via documentation alone (Christian Couder) or with behavioral changes like confirmation prompts (Ben Knoble). The technical implementation would remain unchanged regardless. Christian Couder specifically suggests adding an explicit warning to the man page about accidental root commit replay when range syntax is omitted (e.g. `topic` vs `main..topic`), which Toon Claes agrees should be addressed but potentially as separate follow-up work. Junio's latest input suggests the documentation should take a more nuanced approach, explaining when each form is appropriate rather than warning against one. The new discussion confirms these UX refinements will be considered post-merge.

Participant positions:
- Toon Claes: Implemented and iterated based on feedback, acknowledges documentation need, considers UX improvements as follow-up work, corrected a reference to another series
- Junio C Hamano: Approved v2 for merging, now suggests nuanced documentation approach explaining valid use cases for both range specification forms
- Christian Couder: Suggested documentation clarification about range syntax, specifically warning about accidental root commit replay
- Ben Knoble: Proposed potential UX improvements based on monorepo experience, suggesting the issue may warrant more than just documentation

Related work: Connected to broader `git replay` development, particularly the parallel `--revert` series. Documentation and UX discussion reflects growing real-world usage of this plumbing command in advanced workflows, with attention shifting from pure plumbing to user-facing considerations as adoption increases. The current exchange shows this evolution continuing naturally as the feature matures, with maintainer input now shaping the documentation philosophy to match Git's typical approach of explaining behavior rather than prescribing workflows.