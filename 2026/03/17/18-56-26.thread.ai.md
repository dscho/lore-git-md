Git replay root commit handling patch series (v1 -> v2) - maintainer ack with doc follow-up

Thread identity: Patch series enabling `git replay` to handle root commits (commits with no parents), authored by Toon Claes. Technical change to the relatively new `git replay` command, now approved by maintainer Junio C Hamano for merging with a documentation follow-up suggested by Christian Couder.

Core problem and solution: The `git replay` command previously rejected root commits (initial commits with no parents). The series fixes this by moving root commit logic into `mapped_commit()` with NULL pointer safety and improved "empty tree" labeling for root commits in merge operations. Christian Couder's follow-up identifies a potential user confusion point regarding commit range specification that warrants documentation clarification.

Technical changes in v2:
- Root commit handling refactored into `mapped_commit()` per maintainer feedback
- Added NULL pointer check for `commit` parameter
- UX improvement showing "empty tree" for root commit ancestors
- Verified via updated t3650-replay-root-commits.sh test case
- Documentation note needed about range specification (Christian Couder)

Current status: Maintainer-approved and queued for merging. Junio indicates the change is ready to progress from `seen` to `next` integration branch, marking it as non-controversial and merge-ready. The implementation addresses all prior design concerns. Documentation clarification about range specification (not affecting code) remains as follow-up work.

Open questions: None blocking - all technical discussion resolved. Potential trivial conflicts with Siddharth's in-progress `--revert` functionality series remain noted but don't prevent merging. Documentation clarification about range specification identified as helpful addition.

Participant positions:
- Toon Claes: Implemented and iterated based on feedback
- Junio C Hamano: Approved v2 for merging, confirming design concerns addressed
- Christian Couder: Suggested documentation clarification about range specification to prevent user confusion

Related work: Connected to broader `git replay` development, particularly the parallel `--revert` series which may touch adjacent code paths. No substantive integration issues expected. Documentation suggestion anticipates future user-facing evolution of the command as noted by Elijah Newren.