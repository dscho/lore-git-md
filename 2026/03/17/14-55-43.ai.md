This is a follow-up discussion in the thread about changing `git rev-parse --parseopt` exit codes for help flags. Jeff King (Peff) responds to brian m. carlson's acknowledgment of the regression in `git instaweb -h` behavior, pointing out that the issue extends beyond just shell scripts using `--parseopt`.

Peff notes that many Git commands (like `git log -h`) currently exit with status 129 for help requests, suggesting the original behavior was more widespread than just the `--parseopt` case. He provides an untested patch that would make all help requests exit 0 by modifying `parse-options.c` directly, but cautions that this would be a more fundamental change in behavior rather than just fixing a bug.

The email raises important questions about Git's historical use of exit code 129 for help requests and whether this was an intentional design choice. Peff suggests researching the commit history or mailing list archives to understand the original rationale before proceeding with any changes.

This moves the discussion from being about a specific regression to considering a broader behavioral change across Git's option parsing infrastructure. The thread now faces a decision between maintaining historical consistency versus adopting what Peff considers a more correct behavior (exiting 0 for help requests). The solution will need to account for both the original `--parseopt` use case and the wider ecosystem of Git commands using the parse-options machinery.