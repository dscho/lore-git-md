Thread summary:

The thread begins with Tian Yuchen's patch to refactor `builtin/patch-id.c` as part of the ongoing `the_repository` removal effort. The change makes the hash algorithm dependency explicit by using `the_hash_algo` directly with `get_oid_hex_algop()` instead of the implicit `get_oid_hex()`. The author explains they considered making the code repository-aware by passing `repo->hash_algo` but encountered a test failure in repository-less scenarios, deciding the additional complexity wasn't justified for this case. The patch is a small but clear step toward the larger goal of eliminating implicit global state, properly signed-off and following project style. It surfaces the hash algorithm dependency without yet fully removing reliance on the global `the_hash_algo`. The change is focused and non-controversial, touching only one line in the patch-id implementation while maintaining existing behavior.