Test modernization patch that fixes a subtle issue in the t2203 test script. The problem being addressed is that piping `git status` output directly to `grep` would mask the exit code of the Git command if it failed. The patch changes the test to first capture the full status output in a temporary file, then filter it through grep, ensuring test failures are properly detected.

The changes are mechanical but important for test reliability - they modify four test blocks in t2203-add-intent.sh to use this more robust pattern. Each instance follows the same transformation: replacing direct piping with a two-step process of saving raw output then filtering. The patch maintains all existing test assertions while making them more accurate by properly propagating failures.

This appears to be a straightforward improvement with clear value. The changes are consistent and focused on test hygiene rather than modifying any core behavior. The commit message explains the motivation well and the implementation matches that description. No obvious issues or controversies here - just good test suite maintenance.