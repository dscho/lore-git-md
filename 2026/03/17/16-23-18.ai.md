This is a bugfix patch series that extends the recently merged improvements to `git apply`'s error reporting. The series builds on Jialong Wang's earlier work (already merged) that added input file identification to corrupt patch errors, now expanding that same context to header parsing, binary patch, and garbage patch errors.

The v4 series consolidates three follow-up patches into a single coherent effort, all targeting the same goal: making `git apply` error messages more informative by including the input source (filename or stdin) when reporting problems. The implementation touches `apply.c` extensively but remains focused on the error reporting improvements without changing core behavior. Test coverage is expanded across multiple test scripts to verify the new error formats.

This appears to be a well-executed continuation of the earlier work, maintaining the same clean approach and thorough test coverage. The changes are clearly motivated by real debugging needs when working with multiple patches. With the core concept already accepted in the previous merge and no outstanding objections raised, this follow-up series seems likely to proceed smoothly through review.

Key technical details for future reference:
- Modifies error reporting in `parse_git_diff_header()`, `parse_binary()`, and garbage patch detection
- Updates test expectations in `t4012-diff-binary.sh`, `t4100-apply-stat.sh`, `t4103-apply-binary.sh`, and `t4254-am-corrupt.sh`
- Maintains consistent format: "error: corrupt patch at <file>:<line>"
- Follows the same pattern of resetting line numbers between patch inputs
- No behavior changes beyond the enhanced error messages

As a GSoC contribution, this demonstrates good progress from the initial patch to a more comprehensive solution, showing the author's growing familiarity with the codebase and contribution process.