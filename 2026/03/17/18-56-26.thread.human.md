This thread centers on enabling `git replay` to handle root commits, with the technical implementation now approved by Junio Hamano for merging. The v2 patch series by Toon Claes refactored root commit handling into `mapped_commit()` with proper NULL checks and clearer "empty tree" labeling, verified by updated tests. 

While the code is ready, two documentation/UX discussions have emerged. Christian Couder identified a potential user confusion point: omitting range syntax (like using just `topic` instead of `main..topic`) could unintentionally trigger root commit replay. He suggests adding explicit warnings in the man page. Toon agrees this is worth documenting but proposes handling it separately from the current series. This builds on Ben Knoble's earlier monorepo workflow observations about making the command's behavior more intuitive - though the current focus is on documentation rather than interactive prompts. 

The thread shows `git replay` evolving from plumbing toward more user-facing functionality, with attention to both technical correctness and preventing common usage pitfalls. The core change is queued for `next`, while documentation improvements will follow to clarify range syntax expectations.