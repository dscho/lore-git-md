This is a test improvement that fixes a subtle issue in the test suite. The patch modifies `t/t2203-add-intent.sh` to properly capture the exit code from `git status` commands that are piped into `grep`. Currently, piping suppresses the exit status, which means test failures in `git status` could go unnoticed.

The change is straightforward - instead of piping directly to `grep`, the patch first writes the output to a temporary file (`actual.raw`), then filters it. This ensures any failure in `git status` will be caught by the test framework. The patch updates several test cases in the file to use this more robust pattern.

This appears to be a clean, focused improvement to test reliability. The changes are mechanical but important for catching potential regressions. The patch is part of a Google Summer of Code (GSoC) contribution, suggesting the author may be a newer contributor, but the change itself is well-formed and follows established test patterns.