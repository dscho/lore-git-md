This is a technical discussion about exit code behavior for help flags in Git's option parsing infrastructure, continuing the thread about `git rev-parse --parseopt`. Jeff King (Peff) provides historical research into why Git currently exits with status 129 for help requests (`-h`/`--help`), tracing it back to 2007 code where the same exit code was used for both error cases and explicit help requests.

Peff's investigation suggests the current behavior may have been an accidental inheritance rather than an intentional design choice - the 129 exit code originally applied to error cases showing help, and explicit help requests later reused the same code path without considering whether they should exit differently. He notes the distinction between these cases (requested help vs error-triggered help) may need clearer separation in the code.

The email moves the discussion from being about a specific regression in `git instaweb` to considering a more fundamental question about Git's option parsing behavior. Peff's research indicates the issue affects all commands using Git's parse-options machinery, not just shell scripts using `--parseopt`. This raises the stakes for any change, as modifying the behavior would affect many commands system-wide.

Key technical points include the 2007 origin of the 129 exit code, the conflation of error and help cases in the parse-options implementation, and the need to potentially separate these code paths if changing the behavior. The thread now faces architectural questions about whether to maintain historical behavior or standardize on exit 0 for help requests across all of Git's option parsing.