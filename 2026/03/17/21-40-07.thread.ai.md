Thread identity: Refactoring patch from René Scharfe replacing `prio_queue` LIFO usage with dedicated `commit_stack` structure.

Core problem or goal: Improve code clarity and type safety by using a purpose-built stack structure for LIFO operations on commits, rather than repurposing the more general `prio_queue` with NULL compare function.

Current status: Initial version of patch submitted, affecting three files - `builtin/name-rev.c`, `fetch-negotiator.c` (default implementation), and `skipping.c` negotiator.

Key technical details:
- Replaces `prio_queue` with `commit_stack` in all LIFO usage contexts
- Maintains identical LIFO behavior while providing type safety for commits
- Saves memory by eliminating `prio_queue`'s tie-breaking counter
- Changes are mechanical replacements of initialization, push/pop operations, and cleanup
- Touches negotiation code paths in `fetch-negotiator.c` and `skipping.c`
- Affects commit traversal in `builtin/name-rev.c`

Participant positions: René Scharfe as author proposing this focused refactoring. No reviews or responses yet in thread.

Related work: Part of ongoing code quality improvements around data structure usage in Git codebase. Follows similar cleanups of `prio_queue` usage patterns elsewhere.