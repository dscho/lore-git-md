This is a bugfix patch that improves error reporting in `git apply` by including the input file name when reporting corrupt patches. It's part of a v4 series extending earlier work that was already merged, showing the author's continued refinement of this feature.

The patch makes two key changes: it adds the input filename (or "<stdin>") to corrupt patch error messages, and resets line numbers between patch inputs when processing multiple files. The implementation touches `apply.c` to modify error reporting and updates several test files to verify the new behavior, including cases for file input, stdin, and multiple patches.

This appears to be a well-constructed follow-up to the author's previous work, maintaining the same careful approach with thorough test coverage. The changes are clearly motivated by real debugging needs when working with patch files, and build logically on the already-accepted concept of better error context. With the core approach established in earlier iterations and no apparent controversy in the thread, this seems likely to proceed smoothly toward merging.