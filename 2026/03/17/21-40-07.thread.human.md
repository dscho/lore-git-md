René Scharfe has proposed a focused refactoring to improve code clarity in three areas that use LIFO (stack) operations. The patch replaces generic `prio_queue` structures with a dedicated `commit_stack` when handling commits, making the stack behavior more explicit through the type system. This change affects commit traversal in `name-rev` and two negotiation implementations.

The mechanical conversion provides several benefits: clearer intent (using a stack rather than a repurposed priority queue), type safety (working specifically with commits), and slight memory savings by removing an unused priority counter. While the runtime behavior remains identical, the change follows René's characteristic pattern of small, precise improvements to code quality and maintainability. The patch appears uncontroversial and well-contained, typical of René's contributions.