This is a test improvement that fixes a subtle issue with test reliability. The patch modifies `t/t2203-add-intent.sh` to properly capture the exit code of `git status` commands when they're piped to `grep` in several test cases.

Currently, when `git status` is piped directly to `grep`, any failure from `git status` itself would be masked by the pipeline - the exit code would only reflect whether `grep` succeeded. The patch changes this by first writing the status output to a temporary file (`actual.raw`), then filtering that file with `grep`. This ensures test failures from `git status` will be properly detected.

The changes are mechanical but important for test correctness, touching 8 test blocks that check status output in various formats (regular, porcelain v1, and porcelain v2). Each follows the same pattern of splitting the pipeline into two steps. The patch appears well-scoped and correctly implements its stated purpose without introducing new issues.

This is the kind of small but meaningful test infrastructure improvement that helps maintain the reliability of Git's test suite. The change is clearly motivated and the implementation looks correct.