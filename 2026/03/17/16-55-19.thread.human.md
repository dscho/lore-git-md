This thread introduces a new feature to expose the Git directory path programmatically via `git repo info`. The initial patch from the author adds a `paths.git_dir` key that returns the same value as `git rev-parse --git-dir`, providing tools with a more direct way to access this information without shelling out to a separate command.

The implementation is clean and well-contained, adding the new field to the repository info reporting mechanism in `builtin/repo.c` while maintaining consistency with existing behavior. The patch includes both documentation and test coverage, following Git's standard practices. The change appears uncontroversial and addresses a clear use case for tooling integration. So far, there haven't been any replies or reviews on the thread.