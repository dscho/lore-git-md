This is a review comment from the maintainer (Junio C Hamano) on the `git replay` root commit handling patch. He generally approves the change ("OK") but raises a question about the design choice for handling `replayed_base` differently between root and non-root commits.

The key technical points Junio notes:
1. The patch correctly handles root commits by using the empty tree for `base_tree` when there are no parents
2. He questions why `replayed_base` is set directly to `onto` for root commits rather than going through `mapped_commit()`
3. Suggests this special case might better belong inside `mapped_commit()` itself for consistency
4. Acknowledges this is a minor point and that pushing the logic too far (like making `repo_get_commit_tree()` return empty tree for NULL) would be excessive

The review doesn't request changes but signals the maintainer's thought process about API design consistency. This is a typical Junio-style review - identifying subtle design questions while accepting the overall approach. The author may want to consider encapsulating the root commit logic more completely in a follow-up, but the patch appears acceptable as-is.