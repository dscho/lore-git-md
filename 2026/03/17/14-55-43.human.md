This is a discussion about exit code behavior for help flags in Git's option parsing machinery. Jeff King (Peff) responds to brian m. carlson's acknowledgment of a regression in `git instaweb -h behavior, where the test suite expects status 129 outside repositories but now gets 0.

Peff points out this isn't just about shell scripts using `rev-parse --parseopt` - even built-in commands like `git log -h` currently exit with 129. He suggests the change would need to touch `parse-options.c` more broadly to be consistent, but cautions this represents a deeper behavior change rather than just fixing `--parseopt`.

The email raises important questions about Git's historical use of exit code 129 for help requests and whether changing this would be a bug fix or a more significant behavior modification. Peff suggests researching why 129 was originally chosen before proceeding. The discussion highlights how seemingly simple changes to core infrastructure can have wide-ranging implications across Git's command interface.