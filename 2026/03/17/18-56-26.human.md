This is a feature patch for the experimental `git replay` command, which is still under development. Toon Claes adds support for replaying commits all the way down to the root commit, removing a previous restriction. The change involves modifying the commit-picking logic to handle root commits (those without parents) by using the empty tree as a base when needed.

The patch is straightforward - it removes a FIXME comment and error check while adding logic to properly handle root commits. Test coverage is updated to verify the new behavior works as expected. The author notes this might conflict with another in-progress series adding `--revert` support to replay, but considers the conflict resolution trivial.

The change looks well-contained and properly tested. Since `git replay` is still experimental, this is a reasonable incremental improvement to its functionality. The patch follows Git's coding style and includes both implementation and test updates.