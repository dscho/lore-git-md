This is a preparatory patch in Derrick Stolee's series enhancing the `git backfill` command's partial clone functionality. The change is a missing header dependency fix - adding `object-name.h` to `revision.h` because the `REV_INFO_INIT` macro relies on `DEFAULT_ABBREV` defined there. 

This is a straightforward infrastructure patch that doesn't change behavior, just ensures proper header inclusion for code that uses `REV_INFO_INIT`. The change is minimal (one line added) and clearly motivated by the macro's requirements. As the first patch in a series, it sets up clean header dependencies for subsequent changes that will work with revision walking.

The patch follows Git's standard practices for header organization and includes a clear commit message explaining the technical rationale. It's a small but necessary foundation for the more substantive changes that follow in the series.