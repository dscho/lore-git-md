The thread discusses improving argument validation for the `git backfill` command, which currently silently ignores invalid arguments. Siddharth Shrimali's patch series (now at v3) would make it reject unexpected arguments with an error, following the pattern used by `git bugreport`. The technical implementation has been refined through review feedback, particularly from Phillip Wood on test improvements.

However, Junio C Hamano raised a significant concern: Derrick Stolee is working on adding revision argument support to `backfill` (GitHub PR #2070), which would handle unknown arguments differently - treating them as potential revisions and producing Git's standard "unknown revision" error. Stolee has now confirmed this approach, noting his implementation will use Git's revision parsing machinery, making the current validation patch potentially obsolete.

The thread is now in a holding pattern. The straightforward validation approach in the current patch would conflict with the planned revision support. Three paths remain: Stolee's series could progress first (making this patch unnecessary), the current patch could merge as a temporary solution, or the two efforts could coordinate on a unified approach. For now, the decision awaits either progress on Stolee's work or further discussion about integration.