This is a bugfix for the `git backfill` command, which currently silently ignores unexpected arguments rather than erroring out like most Git commands do. The patch adds a standard check after `parse_options()` to validate that none remain, calling `usage_with_options()` if they do.

The change is straightforward - just 3 lines added to `builtin/backfill.c` to match the behavior seen in similar commands like `git gc` and `git repack`. The commit message clearly explains the current incorrect behavior and how this fixes it. 

This appears to be a well-scoped, uncontroversial fix that brings `git backfill` in line with Git's standard command-line argument handling patterns. The patch is small and focused, with a clear commit message explaining both the problem and solution. No tests are included, but the change is simple enough that the risk of regression seems low.