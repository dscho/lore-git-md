This is a technical discussion about memory handling in `strbuf_getwholeline()`, continuing the optimization thread around this core string buffer function.

René Scharfe responds to Jeff King's (Peff) assessment of a potential memory leak edge case in a way that highlights the tradeoffs between different implementations. While agreeing the issue hasn't caused problems in practice, René notes the current behavior violates the convention of rolling back state on error. He points out inconsistencies between the getdelim() and non-getdelim() implementations - the former discards partial reads while the latter preserves them, creating a behavioral mismatch.

The discussion has moved from performance optimization to deeper design questions about error handling consistency. René raises valid concerns about the philosophical approach: should the function discard partial reads (like getdelim() does) to maintain consistency across platforms, even though this means throwing away valid data? Or is it better to accept the inconsistency to preserve data integrity where possible?

This represents the nuanced final stage of a technical debate where the easy wins (performance improvements, obvious bug fixes) have been addressed, leaving more subjective questions about API design principles. The participants seem to be converging on leaving the function as-is given its proven stability, despite the theoretical imperfections.