Performance optimization discussion for strbuf_getwholeline().

Jeff King (Peff) responds to René Scharfe's suggestion about further simplifying the strbuf_getwholeline() implementation. The discussion centers on two potential micro-optimizations: removing the strbuf_reset() call and eliminating the early EOF check. Peff acknowledges René's analysis is correct - getdelim() must handle EOF conditions internally, making the upfront check redundant. 

This exchange represents the final refinement in a thread that has carefully balanced between performance gains (Peff's original 7% speedup demonstration) and code correctness (Junio's reallocation safety concerns). The current consensus appears to be that:
1. The buffer reuse optimization is worth keeping
2. Some safety checks can be safely removed without affecting correctness
3. The EOF path can be simplified at a small per-file performance cost

Peff notes his original EOF check was intended to simplify error handling but agrees the getdelim() contract makes it unnecessary. This thread exemplifies Git's meticulous approach to low-level optimizations, where multiple maintainers collaborate to validate both performance claims and implementation safety. The discussion appears ready to conclude with a cleaner version of the function that maintains the key optimizations while removing some now-redundant checks.