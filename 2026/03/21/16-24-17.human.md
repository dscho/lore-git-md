This is a refactoring patch that continues the strbuf.buf NULL check elimination effort, specifically focusing on `strbuf_getwholeline()`. Junio Hamano proposes a cleaner implementation that maintains the buffer reuse optimization while properly handling the strbuf invariants.

The patch removes the temporary NULL state that previously violated strbuf rules by introducing local variables to hold the buffer state during `getdelim()` processing. This approach:
- Preserves the performance optimization (buffer reuse) that Jeff King's benchmarks showed was valuable
- Eliminates the need for strbuf_reset() calls
- Properly handles memory cleanup in EOF cases
- Maintains the existing behavior while being more maintainable

The changes align with the ongoing Coccinelle effort to remove unnecessary NULL checks of strbuf.buf throughout the codebase. Junio notes this makes `strbuf_getwholeline()` no longer need to be excluded from those rules, as it no longer temporarily violates strbuf invariants.

This appears to be a well-considered refinement that addresses both the performance concerns raised earlier in the thread and the code hygiene goals of the broader strbuf cleanup effort. The implementation carefully preserves all edge case behavior while making the code more straightforward.