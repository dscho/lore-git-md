This is a bugfix for the `git backfill` command, which currently silently ignores unexpected arguments instead of erroring out like most Git commands do. The patch adds a simple check after `parse_options()` to call `usage_with_options()` if any arguments remain, providing consistent behavior with commands like `git gc` and `git repack`.

The change is straightforward - just 3 lines added to validate arguments in `builtin/backfill.c`. The commit message clearly explains the current incorrect behavior and how this patch fixes it to match Git's conventions. This appears to be a well-scoped, uncontroversial fix that improves user experience by failing clearly rather than silently ignoring mistakes.

The patch touches only the backfill command's argument parsing, with no wider implications. Given its small size and clear benefit, this seems like an easy candidate for merging once any potential review feedback is addressed.