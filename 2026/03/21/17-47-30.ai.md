Bugfix for `git backfill` argument validation.

Siddharth Shrimali sends v2 of his patch to make `git backfill` properly reject unexpected arguments, addressing Junio C Hamano's feedback from the thread. The command previously silently ignored extra arguments, which could confuse users. The new version adds explicit validation that matches Git's standard behavior for other commands.

The patch now includes both the requested error message (showing the invalid argument) and a test case verifying the behavior. The implementation follows the pattern from `git bugreport` that Junio suggested, with an `error()` call before showing the usage message. The test in t5620-backfill.sh confirms the command fails with the expected message when given an invalid argument.

This appears to fully address the maintainer's concerns from the previous round - it provides clear user-facing feedback while maintaining consistency with other Git commands. The test coverage ensures the behavior won't regress. The changes are minimal (5 lines of code plus 5 lines of test) and focused on the sort of argument validation that Git commands typically implement.