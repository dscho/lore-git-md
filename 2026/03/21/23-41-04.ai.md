Performance optimization and memory safety follow-up for strbuf_getwholeline().

René Scharfe responds to Jeff King's (Peff) assessment of the strbuf_getwholeline() memory leak discussion, noting that while the theoretical leak exists in edge cases, it hasn't manifested as a practical issue. He points out that strbuf_appendwholeline() - another single-line caller - already handles cleanup properly on error, demonstrating that some callers do account for this case.

The discussion has shifted from performance optimization to broader behavioral consistency questions. René identifies two key areas needing alignment between the getdelim() and non-getdelim() implementations: error handling should either consistently preserve partial reads (current non-getdelim behavior) or consistently discard them (getdelim behavior). He expresses concern about the current inconsistency but acknowledges the tradeoffs - discarding valid data feels wasteful, while maintaining different behaviors across platforms seems architecturally messy.

This exchange represents the nuanced final stage of a technical discussion where the easy wins (removing redundant feof checks) have been identified, leaving more philosophical questions about API design principles. René's response suggests the conversation may be reaching its natural conclusion, with any remaining changes likely to be minor behavioral tweaks rather than the significant performance improvements that initiated the thread.