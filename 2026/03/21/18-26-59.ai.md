Junio C Hamano raises important questions about Orgad Shaneh's patch to align `git fetch --prune-tags` behavior with its documentation. This is a bugfix discussion thread, with the maintainer probing the historical context and test coverage of the proposed change.

Junio questions whether the current behavior (where `--prune-tags` doesn't force-update changed tags) was actually a bug or a deliberate design choice, noting that Ævar Arnfjörð Bjarmason's original implementation might have intended the current behavior. He suggests the documentation might need updating instead of the code if the behavior was intentionally different.

The maintainer also points out a gap in the test coverage - while the patch adds verification that `--prune-tags` allows tag updates, it doesn't test the actual pruning behavior (removing tags that no longer exist remotely). This indicates the test suite should be expanded to cover both aspects of the documented `--prune-tags` behavior.

The discussion is now at a key juncture where historical context needs to be established before proceeding with the fix. Junio's intervention suggests this may not be as straightforward as initially presented, and the patch may need revision to either:
1. Better justify why the code should change to match docs (if behavior was unintentionally wrong)
2. Update documentation instead (if behavior was deliberately different)
3. Add more comprehensive test coverage for both aspects of tag pruning