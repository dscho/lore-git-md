Refactoring discussion about strbuf_getwholeline() optimization tradeoffs.

René Scharfe weighs in on the ongoing strbuf_getwholeline() discussion between Junio Hamano and Jeff King (Peff), focusing on potential micro-optimizations. He agrees with Peff's observation that the strbuf_reset() call could be eliminated since the function already adjusts sb->len appropriately after getdelim() completes. René also notes the EOF check at the start could be removed, as getdelim() must handle EOF conditions itself, though this trades a per-file optimization (early EOF return) for a per-line check.

This brief technical note reinforces Peff's position that some existing code in strbuf_getwholeline() may be unnecessary, while acknowledging the subtle performance tradeoffs involved. The discussion remains focused on the function's internals, particularly around buffer management and error handling paths. René's input suggests the function could be simplified slightly without fundamentally changing its buffer reuse behavior - the key performance optimization that Peff's benchmarks showed to be valuable.

The thread continues to explore the tension between code simplicity and performance in this core string-handling function, with multiple maintainers now contributing nuanced perspectives on specific implementation details. René's comments indicate potential for minor cleanups while preserving the buffer reuse optimization that Peff demonstrated has measurable impact.