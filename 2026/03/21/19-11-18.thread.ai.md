Here is the dense AI thread summary for this new thread:

Bug report for a segfault in `git-remote-http` when parsing refspecs outside a repository context. The crash occurs in `parse_refspec()` when it dereferences `the_hash_algo` while `the_repository->hash_algo` is NULL, triggered by having `remote.<name>.fetch` configured but no repository. Jo Liss provides detailed reproduction steps showing the crash happens with `git ls-remote -h <url>` outside a Git repository when global `remote.origin.fetch` is set. The issue affects both current master (7ff1e8dc1e16) and Git 2.51.0, reproduced on Ubuntu and macOS. This is a legitimate crash scenario in edge cases where remote-curl operations are performed outside a repository with certain configurations. The report includes complete environment details, backtrace, and clear reproduction steps. No proposed fix yet - this is the initial bug report.