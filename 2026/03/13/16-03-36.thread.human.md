Kristoffer Haugsbakk's patch series adds `--format` support to `git name-rev`, bringing its output formatting capabilities in line with `git log --format`. The v2 iteration incorporates feedback from Junio C Hamano and others, with careful attention to memory management and edge cases.

The implementation introduces a new `struct format_cb_data` to track formatting state and properly integrates with Git's pretty-print infrastructure. Key improvements since v1 include proper handling of `--no-name-only` interaction with `--format`, notes display support via `--notes`, and comprehensive test coverage for various format specifiers (`%s`, `%N`) and edge cases. The series begins with a preparatory coding style cleanup in `builtin/name-rev.c` to standardize brace usage.

The changes demonstrate responsiveness to prior technical concerns while maintaining backward compatibility. Though no Reviewed-by tags have appeared yet, the thorough test coverage and careful handling of memory management suggest the series is nearing readiness. This fills a useful gap for users needing to format individual commits that can't be easily processed through `git log` or `git show`.