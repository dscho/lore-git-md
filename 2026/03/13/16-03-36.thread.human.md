This thread centers on Kristoffer Haugsbakk's work to add `--format` option support to `git name-rev`, bringing its output formatting in line with `git log --format`. The series is now at version 2, with the first patch being a straightforward coding style cleanup that standardizes brace usage in `builtin/name-rev.c` to match Git's conventions. This preparatory change carries over from v1 and modifies two conditional blocks for consistency.

The main feature patch (not yet summarized here) introduces the new formatting capability while carefully addressing feedback from Junio Hamano and others on the initial version. Key improvements include proper handling of `--no-name-only` interactions, notes display support, and better memory management patterns. Test coverage in `t6120-describe.sh` has been expanded to verify edge cases around format specifiers and notes handling.

While the coding style patch is uncontroversial and likely to be accepted, the series as a whole remains under review. The changes demonstrate responsiveness to prior technical concerns, though no Reviewed-by tags have yet been added. The work maintains backward compatibility while bringing `name-rev`'s output formatting capabilities up to date with Git's established conventions.