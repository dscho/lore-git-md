Kristoffer Haugsbakk's series to introduce a new `git format-rev` command continues to progress carefully, with the latest patch (4/5) focusing on test coverage. The series has evolved from extending `git name-rev` to creating a new command that will support both revision-oriented and freeform text input modes. 

The current patch adds a regression test for the `--annotate-stdin --name-only` combination in `git name-rev`, which was recently refactored to use a new command abstraction that will be shared with the upcoming `format-rev` command. This test ensures the core formatting behavior remains correct before the new command is introduced in the final patch. The test follows existing patterns in t6120-describe.sh, comparing direct calls against stdin-processing variants.

The series shows thoughtful progression through preparatory refactoring, with each patch building toward the final goal while maintaining existing functionality. The experimental nature of the new command means there are no Reviewed-by tags yet, but the careful attention to test coverage and architectural separation suggests the approach is sound. The next patch will likely introduce the actual `format-rev` command implementation, building on this solid groundwork.