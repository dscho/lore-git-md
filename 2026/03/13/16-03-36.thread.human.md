Kristoffer Haugsbakk has proposed adding a `--format` option to `git name-rev`, bringing it in line with `git log --format` functionality. The two-patch series begins with a preparatory coding style cleanup in `builtin/name-rev.c`, standardizing brace usage in multi-arm conditionals to comply with Git's guidelines.

The main patch implements the formatting capability by leveraging `pretty_print_commit`, allowing `name-rev` to display commit information with customizable formatting while maintaining all existing behavior. The implementation handles various edge cases including note display and mixed input/output scenarios, with comprehensive test coverage in `t6120-describe.sh`. The author makes a compelling case for the feature by highlighting limitations of existing commands (`log`, `show`, `rev-list`, `cat-file`) when formatting individual commits on demand.

The changeset is well-contained, touching only `builtin/name-rev.c` while adding thorough tests. The implementation appears carefully considered, with the commit message documenting alternative approaches and noting minor output differences from `git log`. This series provides a useful tool for commit formatting in contexts where other commands fall short.