This is a cleanup patch in the v6 series expanding `git fast-import`'s signature handling capabilities. The patch removes an unused forward declaration of `sign_with_header()` from commit.h that was left behind when the function itself was removed in a previous commit (6206089cbd).

The change is straightforward - just deleting two lines that declared a now-nonexistent function. This is a minor housekeeping improvement that helps keep the codebase tidy by removing dead declarations. The patch is part of a larger effort to improve signature handling in fast-import, but this particular change is purely a cleanup with no functional impact.

The patch has been through multiple iterations (now at v6) and appears ready for merging, with all review feedback addressed in previous versions. It's a small but worthwhile cleanup that removes technical debt from the codebase.