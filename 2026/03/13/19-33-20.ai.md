This is a follow-up discussion in the ongoing thread about unifying conflict resolution in `git checkout` and `git switch` through autostash behavior. The original author Harald Nordgren responds to maintainer Junio Hamano's feedback about reframing the feature as an improvement to `checkout -m` rather than a new autostash option.

Nordgren expresses concern that the direction is diverged from his original need for a simple stash-and-checkout workflow, noting he personally never uses `checkout -m`. He suggests potentially keeping this as a separate autostash feature similar to what exists in `git pull --rebase`, rather than expanding the scope to overhaul `checkout -m`.

The email highlights a tension between:
1. The maintainer's vision of unifying and simplifying the existing `-m` behavior
2. The author's original use case for a straightforward autostash capability

This introduces a new consideration for the thread - whether to pursue both improvements separately (a unified `-m` overhaul plus a distinct autostash option) rather than combining them. Nordgren's perspective comes from practical workflow needs rather than the architectural simplification Junio favors.

The exchange suggests the thread may need to reconcile these viewpoints before finalizing the approach, with implications for both the implementation complexity and the user-facing feature set. The submodule handling concerns raised earlier by Phillip Wood remain an additional open question in this discussion.