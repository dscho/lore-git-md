Bugfix discussion clarifying promisor-remote behavior and edge cases in recursive lazy-fetch scenario.

Paul Tarjan responds to Patrick Steinhardt's questions about the promisor-remote recursive fetch issue, providing additional context about the production incident that revealed the bug. He clarifies that the "garbage collected" commits in his original report were actually prefetch refs pointing to promised-but-never-downloaded commits from ephemeral CI branches, which explains why the outer fetch received a thin pack but doesn't affect the core recursion issue.

The discussion reveals two additional subtle behaviors: first, how `fetch_objects()` persistently rewrites the blob filter configuration during recursive fetches (despite 23547c40's attempt to prevent this), and second, how manually unsetting the promisor remote configuration served as an effective mitigation. Paul notes these could warrant follow-up fixes but considers them out of scope for the current patch.

Technical details emerge about the interaction between prefetch maintenance tasks, promisor remotes, and filter persistence. The exchange demonstrates how production incidents can surface complex edge cases in Git's partial clone machinery, with the immediate fix (propagating `GIT_NO_LAZY_FETCH`) now well-validated but revealing adjacent areas that may need attention. The thread shows the patch is technically sound while highlighting related configuration management behaviors that could be improved separately.