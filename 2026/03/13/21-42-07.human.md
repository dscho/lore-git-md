This is a brief review response from Junio C Hamano on the final patch in Phillip Wood's series to remove `the_repository` dependencies from worktree functions. The patch itself converts a NULL worktree fallback to `the_repository` into a BUG() condition, completing the elimination of implicit global state from this API.

Junio points out one remaining issue - the function's documentation in `worktree.h` still claims the function accepts NULL to return the current worktree's git dir, which is no longer true after this change. This is a straightforward documentation update needed to match the new behavior. The comment should be revised to remove the NULL case description since the function will BUG() in that situation now.

The exchange shows the maintainer's attention to detail in catching this small but important documentation mismatch. Since the technical approach was already approved in earlier reviews, this appears to be the final tweak needed before the series can be merged as part of the ongoing `the_repository` removal effort.