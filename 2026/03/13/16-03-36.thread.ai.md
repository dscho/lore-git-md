Kristoffer Haugsbakk's patch series adds `--format` option support to `git name-rev`, modeled after `git log --format`. The v2 iteration incorporates feedback from Junio C Hamano and others, with key technical changes including:

1. Implementation now uses `struct format_cb_data` to track format state and `display_notes_opt` for notes handling, with `FREE_AND_NULL()` memory management. Properly handles `--no-name-only` interaction with `--format` and notes display via `--notes`.

2. Expanded test coverage in `t6120-describe.sh` verifies edge cases: format specifiers (`%s`, `%N`), notes references, and negation scenarios. Tests thoroughly validate stdin processing and format/notes interaction.

The series begins with a preparatory coding style patch (part 1/2) standardizing brace usage in `builtin/name-rev.c` (conditional blocks in `get_rev_name()` and `name_rev_line()`), carrying forward from v1 with minor formatting fixes. Core motivation remains enabling formatted output for commits that can't be easily processed through `git log` or `git show`, particularly with mixed/duplicate input.

Open items from v1 review appear addressed (memory management, notes display), though no Reviewed-by tags yet indicate ongoing review. Implementation maintains backward compatibility while adding formatting capability aligned with Git conventions. Changes demonstrate responsiveness to prior technical concerns, with careful attention to edge cases and memory safety.