Kristoffer Haugsbakk's patch series (v1) introduces `--format` option support to `git name-rev`, modeled after `git log --format`. The series consists of:

1. A preparatory coding style patch standardizing brace usage in `builtin/name-rev.c`, which Junio C Hamano reviewed positively while noting an unrelated spacing inconsistency. Kristoffer will remove the spacing changes in v2 while keeping the brace additions.

2. The main implementation adding format specifier support via `pretty_print_commit`, with comprehensive tests in `t6120-describe.sh`. The patch modifies `builtin/name-rev.c` to handle formatted output while maintaining backward compatibility.

Junio's detailed review prompted Kristoffer to agree to several improvements for v2: explicitly listing `--format` in usage, better `--no-name-only` interaction, using `FREE_AND_NULL()` for notes message handling, considering `init_revisions()`, and simplifying format context initialization. 

In the latest exchange, Kristoffer confirms the implementation already handles commits without notes (as tested) and explains the current strbuf pointer approach. While technically correct, he agrees with Junio that `FREE_AND_NULL()` would be cleaner for notes handling. The discussion continues to focus on implementation correctness in `builtin/name-rev.c`, particularly around memory management and notes display functionality, with thorough test coverage of various edge cases. No Reviewed-by tags yet indicate ongoing review process.