Kristoffer Haugsbakk's patch series (v1) introduces a `--format` option to `git name-rev` for pretty-printed commit information, similar to `git log --format`. The series consists of two patches:

1. A preparatory coding style fix (patch 1/2) standardizing brace usage in `builtin/name-rev.c` for which Junio C Hamano provided feedback about broader spacing inconsistencies while acknowledging the current change is correct.

2. The feature implementation (patch 2/2) adding format specifier support via `pretty_print_commit`, with comprehensive test coverage in `t6120-describe.sh`. The patch modifies `builtin/name-rev.c` to handle formatted output and note display while maintaining backward compatibility.

Junio's detailed technical review of patch 2/2 identifies several areas needing attention:
- Command-line help consistency regarding `--format` in `name_rev_usage[]`
- Potential interaction issues between `--format` and `--no-name-only` flags
- Memory safety consideration for using `FREE_AND_NULL()` instead of `free()` in notes handling
- Revision info setup potentially better handled via `init_revisions()`
- Code structure around format context initialization

The review maintains a constructive tone while flagging implementation details needing refinement. The core functionality appears sound but requires addressing these correctness and maintainability concerns before progressing. The series remains in active discussion with no Reviewed-by or Acked-by tags yet. Changes are contained to `builtin/name-rev.c` with thorough test coverage of format specifiers, stdin annotation, note display, and tree object formatting.