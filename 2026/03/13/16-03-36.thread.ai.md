Kristoffer Haugsbakk's patch series adds `--format` option support to `git name-rev`, modeled after `git log --format`. The v2 iteration incorporates feedback from Junio C Hamano and others, with key technical changes including:

1. Implementation uses `struct format_cb_data` to track format state and `display_notes_opt` for notes handling, with `FREE_AND_NULL()` memory management. Properly handles `--no-name-only` interaction with `--format` and notes display via `--notes`.

2. Expanded test coverage in `t6120-describe.sh` verifies edge cases: format specifiers (`%s`, `%N`), notes references, and negation scenarios. Tests thoroughly validate stdin processing and format/notes interaction.

D. Ben Knoble noted a documentation gap - while the implementation supports `--notes` with the new `%N` format specifier, this interaction isn't yet documented in the man page changes. This appears to be a minor oversight in an otherwise complete implementation that has addressed prior technical concerns around memory management and edge case handling.

The series begins with a preparatory coding style patch (part 1/2) standardizing brace usage in `builtin/name-rev.c` (conditional blocks in `get_rev_name()` and `name_rev_line()`). Core motivation remains enabling formatted output for commits that can't be easily processed through `git log` or `git show`, particularly with mixed/duplicate input.

Open items now include documenting the `--notes` interaction with `%N` format specifier. Implementation maintains backward compatibility while adding formatting capability aligned with Git conventions. Changes demonstrate responsiveness to prior technical concerns, with careful attention to edge cases and memory safety. No Reviewed-by tags yet indicate ongoing review may continue pending documentation updates.