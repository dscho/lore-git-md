Here is the updated AI thread summary:

Kristoffer Haugsbakk's series introducing the experimental `git format-rev` command continues with v3 patch 4/5, adding regression testing for the refactored stdin processing infrastructure. The series has evolved from extending `git name-rev` (v2) to creating a new command (v3) with shared formatting logic.

Key technical details:
- New `format-rev` command will support two input modes: `--stdin-mode=revs` (line-oriented) and `--stdin-mode=text` (freeform parsing)
- Current patch (4/5) adds dedicated test for `--annotate-stdin --name-only` combination in `git name-rev`
- Tests verify refactored stdin processing that will be shared with `format-rev`
- Follows existing test pattern in `t6120-describe.sh` with sorted output comparison
- Maintains all v2 functionality including notes support (`%N`) and tag peeling
- Previous patches:
  1. v3 patch 1: mechanical cleanup in `builtin/name-rev.c`
  2. Follow-up clang-format pass
  3. Core refactoring introducing command abstraction
- Marked EXPERIMENTAL to allow interface refinement

Current status shows:
- Documentation complete with examples and rationale
- Series progressing through testing phase before final command implementation
- No Reviewed-by tags yet due to experimental nature
- Core value remains: efficient revision formatting not easily done with existing commands

The thread demonstrates Git's iterative design - review feedback led to architectural refinement while preserving original use cases. The test-focused current patch ensures stability of shared infrastructure before introducing the new command in the final part of the series. The comprehensive test coverage approach follows Git's quality standards for new functionality.