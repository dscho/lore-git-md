Kristoffer Haugsbakk's patch series (v1) introduces `--format` option support to `git name-rev`, modeled after `git log --format`. The series consists of:

1. A preparatory coding style patch (1/2) standardizing brace usage in `builtin/name-rev.c`, which Junio C Hamano reviewed positively while noting an unrelated spacing inconsistency (`p+1` vs `p + 1`). Kristoffer acknowledges this was unintentional clang-format output and will remove the spacing changes in v2 while keeping the brace additions.

2. The main implementation (2/2) adding format specifier support via `pretty_print_commit`, with comprehensive tests in `t6120-describe.sh`. The patch modifies `builtin/name-rev.c` to handle formatted output while maintaining backward compatibility.

Junio's detailed review of patch 2/2 prompted Kristoffer's follow-up response agreeing to:
- Explicitly list `--format` in `name_rev_usage[]`
- Improve `--no-name-only` interaction with `--format`
- Use `FREE_AND_NULL()` for notes message handling
- Consider `init_revisions()` over `REV_INFO_INIT`
- Simplify format context initialization

The discussion focuses on implementation correctness in `builtin/name-rev.c`, with thorough test coverage of format specifiers, stdin annotation, note display, and tree object formatting. Kristoffer provides rationale for some current choices while appearing open to suggested improvements. No Reviewed-by or Acked-by tags yet indicate ongoing review. The series progresses smoothly with attention to both style consistency (braces, spacing) and technical implementation (format handling, memory management).