Kristoffer Haugsbakk's patch series adds `--format` option support to `git name-rev`, modeled after `git log --format`. The v2 iteration incorporates feedback from Junio C Hamano and others, with key changes including:

1. Implementation improvements: proper `--no-name-only` interaction with `--format`, notes display support via `--notes` option, and memory management using `FREE_AND_NULL()`. The code now uses a `struct format_cb_data` to track format state and `display_notes_opt` for notes handling.

2. Expanded test coverage verifying edge cases: combinations of format specifiers (`%s`, `%N`), notes references, and negation scenarios. The tests in `t6120-describe.sh` now thoroughly validate the interaction between `--format` and `--name-only`.

The series begins with a preparatory coding style patch (part 1/2) that standardizes brace usage in `builtin/name-rev.c` to align with Git's guidelines, modifying conditional blocks in `get_rev_name()` and `name_rev_line()`. This change carries forward from v1 with a minor formatting fix in the commit notes.

Technical details show careful attention to the v1 review feedback: the implementation now properly handles commits without notes and uses cleaner memory patterns. The series maintains backward compatibility while adding the new formatting capability. Open items from v1 review appear addressed, though no Reviewed-by tags yet indicate the review cycle continues. The changes demonstrate responsiveness to prior technical concerns about memory management and notes display functionality. Core functionality remains focused on bringing `name-rev`'s output formatting in line with Git's established conventions.