Here's a human-readable thread summary:

This thread reports a bug in `git describe --dirty` when run from outside a repository while using `--git-dir`. Thomas Braun found that the command incorrectly marks repositories as dirty in this scenario, though they're actually clean. The issue stems from how Git handles working tree setup when only given `--git-dir`, preventing proper dirty state detection. 

A workaround exists by also specifying `--work-tree`, suggesting the problem relates to commit 2ed5c8e174 which added working tree setup for `--dirty`. The bug affects repository discovery and working tree interaction in the describe command. Thomas is seeking guidance on where to start investigating a fix, noting this might be a good first-contributor issue. The core challenge appears to be ensuring proper working tree context is established when `--git-dir` is used alone.