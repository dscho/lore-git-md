This is a brief follow-up from Junio C Hamano on Phillip Wood's patch to remove `the_repository` usage from `get_worktree_git_dir()`. The patch itself had already been reviewed and approved, but Junio notes that the function's documentation in `worktree.h` still claims it accepts NULL to return the current worktree's git dir - a behavior the patch explicitly removes by making NULL a BUG() condition.

This is a straightforward documentation update needed to align the header comment with the implementation change. The patch itself was the final step in a series eliminating `the_repository` dependencies from worktree functions, and this follow-up ensures the API documentation accurately reflects the hardened interface that no longer falls back to global state. The change is uncontroversial and expected as part of the `the_repository` removal effort.