This is a test modernization patch that updates `t/pack-refs-tests.sh` to use Git's standard test helper functions like `test_path_is_file` and `test_path_exists` instead of direct shell `test -f` and `test -e` commands. The changes are mechanical replacements that don't modify the test logic - they make the tests more consistent with Git's test suite conventions and provide better error messages when tests fail.

The patch touches 14 lines across 10 different test cases in the pack-refs test file, converting:
- `test -f` to `test_path_is_file`
- `test -e` to `test_path_exists`
- `! test -f` to `! test_path_is_file`

This appears to be part of the ongoing test modernization effort in Git's test suite. The changes are straightforward and follow established patterns seen in other test files. The commit message could be more detailed about why this change is beneficial (better error messages, consistency with other tests), but the changes themselves look correct and uncontroversial.