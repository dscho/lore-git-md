This is a refactoring patch that completes the technical implementation for the `git replay --revert` feature by extracting revert message formatting logic into a reusable helper function. The patch moves the message generation code from `sequencer.c` into a new `sequencer_format_revert_message()` function that can be shared between the sequencer and the upcoming replay command.

The changes cleanly encapsulate the revert message formatting logic while maintaining all existing behavior. The helper function now handles both regular reverts and revert-of-revert cases, with explicit parameters for repository context and commit reference style rather than relying on global state. This addresses Phillip Wood's earlier feedback about consolidating the message generation logic.

Junio notes that the refactoring improves code organization by making the new function do slightly more than originally proposed, reducing the work needed by future callers. The patch represents the final technical piece needed for the revert functionality in `git replay`, with the implementation now ready for merging pending any final interface design decisions about subcommands versus flags.