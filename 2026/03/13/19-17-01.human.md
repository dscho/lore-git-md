This is the final implementation patch in a series adding configurable cover letter formatting to `git format-patch`. The patch series has gone through eight iterations and all design decisions are now resolved, with the maintainer-approved compromise on format string prefix handling (DWIM behavior matching `git log --pretty`).

The patch implements the actual alternate cover format functionality, building on the infrastructure established in earlier patches. Key technical points:
- Maintains the "log:" prefix requirement but allows dropping it when the string contains %-placeholders (following `git log --pretty` precedent)
- Adds simple %-presence checking (though notes this could be made more robust in future)
- Completes the production-ready implementation with all agreed functionality

The series is now in its final form and has been merged to the 'next' branch. The author notes one potential future improvement (more robust format string validation) but this doesn't block the current work. The patch demonstrates Git's careful interface design process, where technical debates are resolved through precedent and compromise while maintaining stability.