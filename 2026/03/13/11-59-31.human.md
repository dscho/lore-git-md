This is a code review response in the thread about the new `git maintenance list` command. Pablo Sabater suggests a refactoring to reduce duplication in the config file loading logic.

The review points out that the current implementation has nearly identical code paths for handling both the default global config and a specified config file. Pablo proposes consolidating this by first determining which config file to use (either the provided one or the global config), then using a single shared code block to load and query it.

The suggested change would make the code more maintainable by eliminating the duplicated configset initialization and query logic. Pablo notes this also simplifies the need to handle `global_config_file` cleanup separately, since both paths can use the same cleanup code.

This appears to be a constructive, focused review that identifies a real maintainability issue without changing the command's behavior. The suggested refactoring would make the code more DRY while preserving all existing functionality.