Bugfix follow-up addressing Windows test portability in the `git apply -p` validation patch. This fourth iteration from Mirko Faina inlines test patch data directly into the script rather than using external files, resolving CRLF-related test failures on Windows that were discovered after the main fix was merged.

The core fix (replacing `atoi()` with `strtol_i()` for proper `-p` argument validation) remains unchanged from the merged version. The new work addresses Jeff King's findings about Windows test failures by:

1. Moving the test patch content directly into the script rather than using an external file
2. Adding proper cleanup for all test cases
3. Maintaining the same test coverage for malformed inputs ("malformed", "2q", "-1")

This follows Jeff's recommended approach of inlining test data to avoid line-ending issues, similar to solutions used in other test directories. The patch shows good responsiveness to post-merge feedback while keeping the original fix intact. The underlying CRLF handling issue in `apply.c`'s path matching remains open but is now properly worked around in the tests.