Maintainer follow-up on the `git apply -p` bugfix. Junio C Hamano acknowledges the Windows test failure issue identified by Jeff King, where CRLF line endings in the test patch file caused parsing problems. The underlying issue stems from how Git checks out files with CRLF conversion on Windows, except for certain file types specified in `.gitattributes`.

Junio expresses frustration with Windows-related quirks ("Yuck... Joy of stumbling on ^W^Wworking with Windows. Sigh...") while agreeing with Jeff's proposed solution to inline the test patch data directly in the script. This approach would bypass the CRLF issue since test scripts are checked out with LF line endings.

The email confirms the thread's resolution path - addressing the immediate test failure through inlining workaround while leaving the broader CRLF parsing issue in `apply.c` for potential future consideration. This demonstrates Git's pragmatic approach to platform-specific issues, where known workarounds are often preferred over invasive fixes when they solve the immediate problem cleanly.