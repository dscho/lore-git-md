Refactoring patch to adjust comparison style in `git add` command.

This is a small code style change in `builtin/add.c`, modifying a comparison from `0 <= addremove_explicit` to the more conventional `addremove_explicit >= 0`. The patch touches only one line in the `cmd_add()` function, which handles the `--add`/`--remove` flags for the `git add` command. The change follows the common C coding practice of putting the variable on the left side of comparison operators.

The patch is straightforward and appears mechanically correct, though it introduces an extra space inside the parentheses that wasn't there before (`if ( addremove_explicit >= 0 )` vs original `if (0 <= addremove_explicit)`). The commit message is brief and to the point, though it doesn't explain why this particular change was made or whether it's part of a larger style standardization effort.

This appears to be a standalone change rather than part of a series. The author (aum2357) doesn't appear in the context document of notable contributors, suggesting this may be from a new or infrequent contributor. The change is simple enough that it's unlikely to be controversial, though reviewers might question whether the extra space in the parentheses was intentional.