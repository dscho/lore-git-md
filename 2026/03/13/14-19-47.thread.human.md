Phillip Wood has completed his series eliminating `the_repository` usage from the worktree subsystem, part of the broader effort to remove this global variable. The three-patch series follows a clear progression: first updating internal logic to use worktree-specific repositories, then simplifying interfaces, and finally hardening the API by removing NULL fallbacks to `the_repository`.

The key change in this final patch makes passing NULL to `get_worktree_git_dir()` trigger a BUG() rather than silently falling back to `the_repository`. Phillip verified all callers either provide specific worktrees or are already guaranteed non-NULL. He also corrected the header documentation that previously claimed NULL would return the current worktree's git dir - a behavior this series intentionally removes. Junio C Hamano had noted this documentation discrepancy in the first version, and Phillip has now addressed it.

Earlier patches in the series updated `is_current_worktree()` to use the worktree's own repository and simplified related functions. Tests in t2400 confirm the changes maintain correct behavior while making the code's dependencies more explicit. The work builds on Phillip's prior refactoring of worktree code and aligns with René Scharfe's ongoing project to eliminate `the_repository` across Git. With Junio's approval of the technical approach and Phillip's documentation fix, this thread is complete.