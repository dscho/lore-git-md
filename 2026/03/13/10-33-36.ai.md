This is a follow-up discussion in the build system improvements thread, specifically about the technical details of precompiled header (PCH) implementation. Phillip Wood asks whether the explanation about GCC's PCH behavior (which Patrick Steinhardt and SZEDER Gábor clarified in previous messages) should be added to the commit message.

The exchange confirms that while Git's header inclusion patterns could theoretically avoid using `-include` for PCHs, the safer approach is to force-include via compiler flag. This maintains compatibility with projects not originally designed for PCHs and preserves the ability to build without PCHs when needed. Phillip's question about documenting this rationale in the commit message suggests the discussion has reached sufficient clarity to be worth preserving in the project history.

The thread context shows this is part of a larger series introducing Meson PCH support (demonstrating 40% build speedups) and relocating build tools to a new `tools/` directory. The current email doesn't introduce new technical concerns but rather seeks to ensure proper documentation of decisions already made. This reflects the careful attention to detail typical of build system discussions on the list.