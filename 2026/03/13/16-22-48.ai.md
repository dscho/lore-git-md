This is a discussion about refactoring the experimental `git replay` command's interface, continuing the thread about moving from mutually-exclusive options to subcommands. Toon Claes responds to Justin Tobler's feedback on the RFC patch, providing more detailed rationale for the proposed changes.

The key clarification is about the asymmetry between `--onto` (which implies a rebase operation where the revision range's upper boundary determines the ref to update) versus `--advance`/`--revert` (where the option argument specifies the ref to update). Toon explains this difference through concrete examples showing how ref updates work in each case, highlighting why the current behavior can be confusing.

Regarding interface design concerns, Toon considers alternatives like making both `--onto` and `--ref` required (accepting some redundancy for plumbing commands) or combining `--advance` and `--revert` into a single option with a modifier. He also notes the experimental marker in the synopsis should be removed in a separate commit.

The discussion remains focused on design tradeoffs for this experimental command, with Toon showing willingness to iterate based on feedback. Key technical points include the ref update behavior differences between operation modes and the challenge of creating a consistent interface that maintains backward compatibility while improving usability. The thread shows active collaboration to refine the command's design before implementation polish.