This is a test modernization patch that updates `t/pack-refs-tests.sh` to use Git's standard test helper functions like `test_path_is_file` and `test_path_exists` instead of direct `test -f` and `test -e` invocations. 

The changes are mechanical and straightforward replacements of legacy test patterns with their modern equivalents. The patch touches 14 lines across various test cases that verify `git pack-refs` behavior, particularly around pruning and exclusion rules. All modifications preserve the original test logic while making the tests more consistent with Git's current test suite conventions.

The patch appears well-scoped and correctly implemented - it doesn't introduce any functional changes, just updates the test infrastructure. This is part of an ongoing community-wide modernization effort to standardize test patterns in Git's test suite. The author hasn't included any rationale in the commit message, but the motivation is clear from the changes themselves.