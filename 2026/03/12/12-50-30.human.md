Test modernization patch updating a helper function in the partial clone test script.

Siddharth Shrimali refactors the `delete_object` helper in t0410-partial-clone.sh to use more robust test infrastructure. The original version used manual path construction with sed, while the new version leverages `test_oid_to_path` (which handles hash algorithm independence) and adds explicit file existence checking with `test_path_is_file`. This makes the test both more maintainable and more reliable - it will now fail clearly if attempting to delete a non-existent object rather than silently succeeding.

The change is small and focused, touching only the test infrastructure without affecting test behavior. It follows Git's ongoing effort to modernize test scripts by replacing ad-hoc shell logic with purpose-built test helpers. The patch appears well-constructed with a clear commit message explaining both the what and why of the change.