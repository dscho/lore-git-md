This thread has evolved from Harald Nordgren's initial proposal for a `--autostash` flag into a comprehensive improvement of `git checkout -m`'s conflict resolution behavior. The current patch series (v5) successfully integrates autostash functionality while adding useful features like customizable conflict marker labels in `git stash apply`. Junio Hamano has approved the core approach, and the implementation now includes silent stash creation during checkout operations with thorough test coverage.

The final discussion point revolves around submodule handling. Phillip Wood initially raised concerns that the autostash approach might break submodule updates, since `git stash` doesn't support `--recurse-submodules`. However, after testing, Phillip found that the current `checkout -m --recurse-submodules` behavior already discards uncommitted submodule changes rather than merging them, making the new approach no worse. Junio maintains his position against expanding `git stash`'s scope for submodules unless absolutely necessary, suggesting submodules could handle their own stashing independently if needed.

With this clarification, the series appears ready for merging. The only remaining item is some final documentation polish, while submodule support is explicitly deferred per maintainer preference. This represents a significant UX improvement for branch switching with conflicts, with all major technical concerns now resolved through the collaborative review process.