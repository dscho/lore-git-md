This thread covers a feature series (now at v12) that enhances `git checkout -m` with autostash behavior during branch switching conflicts. The implementation includes new label customization options for `git stash apply` and silent autostash creation. The series has undergone extensive review from Phillip Wood, with Junio Hamano and Jeff King weighing in on key decisions like the `CHECKOUT_AUTOSTASH_HEAD` refname.

All major technical issues have been resolved, and the series has been merged into the `next` integration branch. The current discussion represents final polish - specifically about whether autostash messages should show just the source branch (as implemented) or both source and destination branches (Junio's preference). Junio suggests this UX refinement can be addressed separately after users have experience with the core functionality.

The series represents a significant quality-of-life improvement for branch switching workflows, particularly when dealing with uncommitted changes. It builds on existing autostash behavior from `git rebase`/`git merge` while adding new capabilities like conflict marker labeling. Phillip Wood's thorough review has shaped the implementation details, and the maintainer has approved the current state for merging while leaving the door open for message format improvements based on real-world usage.