This thread introduces a new feature adding `--autostash` support to `git checkout` and `git switch`, matching functionality already available in `git rebase` and `git merge`. The implementation allows automatically stashing local changes when switching branches, then reapplying them afterward, with both config (`checkout.autoStash`) and command-line (`--autostash`/`--no-autostash`) controls.

The patch is comprehensive, touching documentation, core checkout logic, and adding extensive test coverage (12 new test cases in t/t2061-switch-autostash.sh). It handles edge cases like dirty index states and conflicts while maintaining consistency with Git's existing autostash behavior. The implementation builds on the sequencer's autostash infrastructure, suggesting a well-considered approach that fits within Git's established patterns. This appears to be a polished contribution addressing a clear functionality gap in branch switching workflows.