This thread has reached the final polishing stage of a 12-iteration patch series that enhances `git checkout -m` with autostash behavior and adds customizable labels for conflict markers. The core functionality is settled: when switching branches with uncommitted changes, Git will now automatically stash them (using the established `CHECKOUT_AUTOSTASH_HEAD` refname) and reapply them after the checkout, with options to customize the conflict marker labels. The implementation handles both staged and unstaged changes while maintaining backward compatibility.

Junio Hamano and Phillip Wood have been closely reviewing the series, with Phillip providing key technical suggestions that Harald Nordgren (the author) has consistently implemented. The latest refinement simplifies the stash application code by consolidating functions rather than adding new wrappers - a minor but meaningful improvement to the internal API. The only remaining open question is about the wording of autostash messages (Junio prefers showing both source and destination branches for context), but this is considered non-blocking. The series is expected to merge to `next` soon after final commit message tweaks, representing a significant quality-of-life improvement for branch switching workflows.