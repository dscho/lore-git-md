This is a follow-up discussion in the ongoing `git fast-import` signature handling series, specifically about warning message behavior. The thread has settled on removing warning messages for both signature re-signing ("sign-if-invalid") and signature removal ("strip-if-invalid") modes, as they create unnecessary noise when users have explicitly requested these operations.

Justin Tobler confirms they will leave the current warning behavior unchanged in this patch series and address it in a separate follow-up patch after merging. This approach allows the current series to proceed while giving the community a dedicated opportunity to discuss the warning removal. The decision reflects consensus between Justin and Patrick Steinhardt that warnings are not useful when users have explicitly chosen to handle invalid signatures in a particular way.

The exchange shows the series is in its final polishing phase, with only minor documentation and this warning behavior question remaining before potential merge. The discussion maintains the project's careful approach to user-facing messaging, ensuring warnings serve a purpose rather than becoming noise.