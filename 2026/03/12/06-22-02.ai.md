This is a technical follow-up in Patrick Steinhardt's build system improvement series, specifically addressing Phillip Wood's questions about precompiled header (PCH) behavior with the reftable sources. The discussion clarifies that while the reftable code doesn't explicitly include git-compat-util.h, it already includes compat/posix.h and doesn't perform the same low-level symbol redefinitions that require special handling in the compat/ directory. Patrick confirms the current PCH approach (using -include) will work fine with reftable, noting they can apply the compat/ treatment later if any issues emerge.

Key technical details for future reference: the reftable backend's inclusion patterns are simpler than compat/ code, making it safe to implicitly include git-compat-util.h via PCH. The exchange also reinforces that meson's -include approach is necessary for general PCH usage, even though Git's header patterns might theoretically allow a simpler solution. This patch maintains compatibility across all build systems while enabling the demonstrated 40% compilation speedup from PCH.