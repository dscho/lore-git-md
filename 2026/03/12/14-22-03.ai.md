This is a follow-up discussion in the ongoing `git fast-import` signature handling series, specifically about warning messages when processing invalid signatures. The thread has settled on an API design using a consolidated `sign_buffer()` function with flags, and this email focuses on finalizing the warning behavior.

Patrick Steinhardt agrees with Justin Tobler's observation that warning messages for invalid signatures may create unnecessary noise in both the "sign-if-invalid" and "strip-if-invalid" modes. While Patrick doesn't insist on immediate changes, he suggests handling this consistently across both modes in a follow-up patch rather than making the change in the current series.

The discussion reveals that:
1. The warning messages were originally included for both signing and stripping invalid signatures
2. Both contributors now see these warnings as potentially excessive since users explicitly requested the operations
3. The solution would involve removing warnings from both modes, but this may be deferred to a separate patch

This email represents the final polishing phase of the series, where only minor behavioral and documentation tweaks remain before potential merging. The core technical work (signature handling infrastructure, test coverage, and API design) appears settled based on the thread context.