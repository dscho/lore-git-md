This is a refactoring patch that's part of a larger series to improve cover letter formatting in `git format-patch`. The patch cleanly extracts the shortlog generation code from `make_cover_letter()` into a new `generate_shortlog_cover_letter()` function. This is purely preparatory work - there's no behavior change, just moving code to make room for the new formatting features coming in later patches.

The patch is well-contained and clearly motivated by the upcoming changes. Junio C Hamano provides a minor style nit about the commit message wording ("As of now" is redundant per Git conventions) but otherwise approves the change. The implementation looks solid - it moves all the shortlog setup and output logic into the new function while maintaining the exact same behavior.

This is patch 1/4 in the v8 series, which has already gone through extensive review and is in its final form. The series as a whole adds configurable cover letter formatting options while maintaining backward compatibility with the existing shortlog output. This particular patch is the foundation that enables those later changes by cleanly separating concerns.