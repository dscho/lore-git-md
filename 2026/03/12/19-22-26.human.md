This is a cleanup patch in a series expanding `git fast-import`'s signature handling capabilities. It removes an unused forward declaration of `sign_with_header()` from commit.h that was left behind when the function itself was removed in a previous commit (6206089cbd).

The change is straightforward - just deleting two lines that are no longer needed. The patch is part of a larger effort to improve signature handling in fast-import, but this particular change is purely a code hygiene improvement with no functional impact. The series has already gone through several iterations (now at v5) and this cleanup was identified during that process.

The patch is well-contained and clearly explained in the commit message. It follows the common pattern of cleaning up leftover declarations after their corresponding functions are removed. Since it's part of an established series that's already been reviewed, this change is likely non-controversial and ready to be merged.