Thread identity: Bug report and discussion about command completion behavior in Git's shell completions, focusing on the porcelain/plumbing command distinction between bash and zsh implementations.

Core problem: Investigation of why bash and zsh show different command completion behavior despite apparently sharing the same completion script (`/usr/share/bash-completion/completions/git`). The maintainer has established that plumbing commands (like `symbolic-ref` and `bugreport`) should not appear in interactive interfaces, making bash's exclusion of them correct and zsh's inclusion potentially incorrect.

Current status: Junio C Hamano has authoritatively stated the bash behavior (excluding plumbing commands) is correct design. Andrey's observation reveals an apparent inconsistency where zsh completion shows these commands despite potentially using the same script, suggesting either shell-specific interpretation differences or different loading paths.

Key technical details: The shared completion script should filter commands based on Git's porcelain/plumbing classification. The divergence between shells could stem from:
1. Shell-specific interpretation of the same script
2. Different configuration/loading paths affecting final behavior
3. zsh potentially loading a different version of the completion script
The underlying principle remains that plumbing commands should not appear in interactive completion.

Open questions: 
- Why does zsh completion behave differently from bash when using what appears to be the same script?
- Is there a bug in how zsh processes the shared completion script?
- Should any commands (like `bugreport`) be reclassified as porcelain given their potential interactive use cases?

Participant positions: 
- Junio maintains the firm position that plumbing commands should not appear in completions, and any implementation showing them is likely wrong.
- Andrey has identified an inconsistency between shell behaviors that may indicate an implementation issue in how the shared script is processed.

Related work: The zsh completion implementation needs review to ensure proper adherence to Git's command categorization philosophy. This may prompt examination of:
- Shell-specific processing differences
- Potential need for separate completion scripts
- Consistency across all completion implementations
- Possible command reclassification discussions for edge cases like `bugreport`