Test infrastructure patch updating sed usage in Git's test suite. This is a follow-up to address a minor inconsistency identified in the previous discussion.

Junio Hamano updates the test suite's linter (`check-non-portable-shell.pl`) to explicitly allow `sed -E` (extended regular expressions) as a portable option, reflecting POSIX Issue 8 standards. The patch makes two changes: modifying the linter's pattern to recognize `-E` alongside `-n`, `-e`, and `-f`, and updating a test in `t6030-bisect-porcelain.sh` to use separate `-E -n` flags rather than the combined `-En` form.

The v2 revision fixes a typo in the commit message ("unwated" -> "unwanted") but more importantly corrects the linter's error message to list `-E` among the permitted options, addressing the mismatch Ramsay Jones pointed out in the thread. This completes the modernization effort by ensuring both the code and its error messages accurately reflect current POSIX standards and Git's actual usage patterns (which have included `-E` since 2022 without issues).

The changes are minimal and focused, touching only the linter script and one test file. The patch demonstrates Git's attention to detail in test infrastructure maintenance, where even small documentation mismatches in error messages are worth correcting. The update is uncontroversial and aligns with the project's standards-compliance goals.