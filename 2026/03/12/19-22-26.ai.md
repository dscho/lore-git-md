This is a cleanup patch in the v5 series expanding `git fast-import` signature handling capabilities. The patch removes an unused forward declaration of `sign_with_header()` from commit.h that was left behind when the function itself was removed in commit 6206089cbd ("commit: write commits for both hashes", 2023-10-01).

The change is straightforward and mechanical - it simply deletes two lines containing the obsolete function declaration. This is a minor but correct cleanup that improves code hygiene by removing dead code. The patch is well-formed with a clear commit message explaining the context and justification for the removal.

This is the first of three patches in the v5 series, serving as a simple preparatory step before the more substantial changes to signature handling infrastructure in the subsequent patches. The change is uncontroversial and unlikely to generate discussion, as it merely removes code that is definitively no longer used.