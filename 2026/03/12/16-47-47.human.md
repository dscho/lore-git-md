This is a follow-up in the thread about shell completion behavior differences between bash and zsh. Andrey points out that the bash completion script explicitly documents a `GIT_COMPLETION_SHOW_ALL_COMMANDS` variable that can be set to "1" to include plumbing commands in completions, which Junio had previously stated should not appear by default.

The exchange clarifies that while plumbing commands are intentionally hidden by default (per Git's design philosophy), there is a documented escape hatch for users who want to see them. This addresses the earlier confusion about whether the bash behavior was correct - it is, but with an opt-in mechanism for those who need it.

The thread now has two clear takeaways: 1) the default behavior (hiding plumbing commands) is working as designed, and 2) the mechanism to override this behavior is already in place via an environment variable. This likely resolves the core question about intended behavior, though the zsh implementation discrepancy noted earlier remains unaddressed in this particular message.