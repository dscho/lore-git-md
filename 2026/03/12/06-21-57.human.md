This is a build system infrastructure patch from Patrick Steinhardt, part of an 8-patch series introducing meson precompiled header (PCH) support while reorganizing build tools into a new `tools/` directory. The series is now complete with all technical aspects addressed.

The current patch (7/8) splits platform-specific sources into a separate 'compat' static library to isolate code that deliberately avoids `git-compat-util.h` (including reftable sources). This is preparation for the PCH implementation in the final patch, which demonstrates a 40% build speedup. 

Key discussion points clarified in this email:
- GCC's PCH behavior requires using `-include` to force inclusion before first token
- The PCH cannot be included from inside another header
- The approach maintains compatibility with existing builds while enabling the optimization

The series appears ready for merging, with only minor open questions about Coccinelle workflow impacts and potential future directory structure refinements. Patrick has addressed all technical feedback, and while Junio C Hamano has noted some workflow concerns, there are no blocking objections.