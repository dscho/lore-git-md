This is a feature inquiry about submodule behavior with worktrees. Xavier Morel describes their workflow using bare repositories with worktrees to avoid duplication, but hits a limitation when trying to use submodules - they can't point to an existing repository as a worktree base.

The core issue is that while submodules avoid some duplication via `--reference`, they still create independent repositories rather than leveraging a shared worktree setup. Xavier asks two questions: whether there's any current way to hack this behavior (they've tried manually tweaking `.git/modules` without success), and whether first-class worktree support for submodules might be added in the future.

The email doesn't propose any specific changes or patches - it's purely an information request about current capabilities and future directions. The scenario described suggests a real workflow pain point for users managing large repositories with submodules, but the technical feasibility of supporting this isn't addressed in the message itself.