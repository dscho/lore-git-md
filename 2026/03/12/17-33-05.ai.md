Test modernization follow-up from a GSoC participant. This is a small patch that continues the conversion of shell test commands to Git's test helper functions in t9200-git-cvsexportcommit.sh, specifically replacing `test -f` with `test_path_is_file` for better failure diagnostics.

The patch is version 4 of a series that has already seen significant discussion and partial merging. The maintainer (Junio C Hamano) previously approved the mechanical test helper updates in v3 but identified follow-up work needed around CVS setup verification. This version splits off one of those follow-ups - the remaining test helper conversion for file existence checking - as a separate patch.

The change itself is straightforward: a single replacement of `test -f` with `test_path_is_file` in a test that verifies a file exists after CVS operations. The test helper provides more detailed output on failure compared to the bare shell command. This continues the project's broader test modernization effort to improve debuggability and consistency across the test suite.

The patch demonstrates the project's typical workflow where initial mechanical changes lead to deeper structural improvements. The author (Pablo Sabater, a GSoC participant) is following maintainer guidance to separate concerns - first handling the simple test helper updates, then addressing the more nuanced test structure issues in subsequent patches. This incremental approach allows for focused review and merging while maintaining forward progress.