This is a discussion about finalizing warning behavior in the `git fast-import` signature handling series. The thread focuses on whether to keep warning messages when invalid signatures are either stripped or re-signed.

Patrick Steinhardt and Justin Tobler are converging on removing the warning messages entirely, as they agree the warnings create unnecessary noise when users have explicitly requested these operations. The discussion notes that if warnings are removed for the new "sign-if-invalid" mode, they should likely also be removed for the existing "strip-if-invalid" mode for consistency.

The exchange is part of the final polishing phase of a v4 series that builds on earlier merged work to expand `git fast-import`'s signature handling capabilities. The series has already settled other design questions around API consolidation and documentation wording. This particular thread shows the contributors working through one of the last remaining details before the series could be ready.

The patch appears well-considered, with both contributors demonstrating thoughtful attention to user experience and consistency. The discussion suggests the warning removal will likely be implemented in a follow-up commit to keep the current series focused.