Thread identity: Feature patch adding `--autostash` support to `git checkout` and `git switch`, bringing them in line with existing functionality in `git rebase` and `git merge`. Authored by [author name to be filled in from actual email].

Core problem/goal: Enable automatic stashing of local modifications when switching branches, with automatic reapplication after switch completes, matching behavior already available in other Git commands. Provides both config option (`checkout.autoStash`) and command-line flags (`--autostash`/`--no-autostash`) for control.

Current status: Initial version of patch submitted. Implements core functionality with thorough test coverage but has not yet received review feedback.

Key technical details:
- Modifies `builtin/checkout.c` to implement autostash logic
- Adds new config option in `Documentation/config/checkout.adoc`
- Updates man pages for both commands
- Comprehensive test coverage in new test file `t/t2061-switch-autostash.sh` (12 test cases)
- Updates shell completion
- Handles edge cases: dirty index, conflicting changes, clean worktrees
- Builds on existing sequencer autostash infrastructure

Open questions/loose ends: None yet - awaiting initial review feedback. Key review points likely to include:
- Interaction with other checkout/switch options
- Behavior in edge cases not covered by tests
- Documentation clarity

Participant positions: No reviews yet received. Author has demonstrated thorough preparation with comprehensive test coverage and documentation updates.

Related work: Builds on existing autostash implementation used by `git rebase` and `git merge`. Follows same pattern of config + command-line flag control.