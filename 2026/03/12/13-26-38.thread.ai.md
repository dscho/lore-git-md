Thread identity: Feature patch adding `--autostash` support to `git checkout` and `git switch`, bringing them in line with existing functionality in `git rebase` and `git merge`. Authored by [author name to be filled in from actual email].

Core problem/goal: Enable automatic stashing of local modifications when switching branches, with automatic reapplication after switch completes, matching behavior already available in other Git commands. Provides both config option (`checkout.autoStash`) and command-line flags (`--autostash`/`--no-autostash`) for control.

Current status: Initial version submitted but requires significant rework after maintainer feedback. Junio C Hamano suggests making autostash behavior conditional on actual conflicts rather than applying it unconditionally, and potentially integrating with/replacing the `-m` (merge) option.

Key technical details:
- Original implementation modified `builtin/checkout.c` with unconditional stashing
- Needs rework to add conflict detection before stashing
- May subsume `-m` option functionality
- Conflict detection logic should be reusable for `git rebase`
- Existing test coverage in `t/t2061-switch-autostash.sh` (12 test cases) will need updates
- Documentation in `Documentation/config/checkout.adoc` and man pages will require revision

Open questions/loose ends:
- Exact implementation strategy for conflict detection
- Whether/how to integrate with `-m` option
- Code structure for rebase reuse
- Edge case handling beyond initial test coverage

Participant positions:
- Maintainer (Junio C Hamano) requests major redesign for conditional stashing and potential `-m` integration
- Original author has not yet responded to review feedback
- No other contributors have weighed in yet

Related work: Builds on existing autostash implementation used by `git rebase` and `git merge`, but now with potential to improve those commands' conflict detection as well.