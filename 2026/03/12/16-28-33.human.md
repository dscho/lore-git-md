This is a refactoring patch that's part of a larger series to improve cover letter formatting in `git format-patch`. The patch cleanly extracts the shortlog generation code from `make_cover_letter()` into a new `generate_shortlog_cover_letter()` function. This is purely preparatory work - no behavior changes, just moving code to make room for the new formatting features coming in later patches.

The change is straightforward and well-contained, touching only `builtin/log.c`. Junio notes a minor documentation nit about the commit message wording ("As of now" is redundant per Git conventions), but otherwise approves the approach. This is the first of four patches in the v8 series, which has already gone through extensive review iterations and is now in its final form.

The series as a whole introduces configurable cover letter formatting, but this particular patch is just laying groundwork by modularizing the existing shortlog-based generation. It's a clean, mechanical change that makes the subsequent feature additions easier to implement and review.