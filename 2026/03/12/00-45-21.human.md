This is a test infrastructure patch from Junio C Hamano that updates Git's test suite to allow the use of `sed -E` (extended regular expressions) in test scripts. The change aligns with POSIX Issue 8 (2018) standards, which now document `-E` as a valid option, and follows real-world usage where `sed -E` has been working without issues since 2022.

The patch makes two key changes: it updates the `check-non-portable-shell.pl` linter to permit `-E` alongside existing allowed flags (`-n`, `-e`, `-f`), and it fixes a test in `t6030-bisect-porcelain.sh` that was using `-En` to use separate `-E -n` flags for better linting visibility. The range-diff shows a minor correction to the commit message (fixing "unwated" to "unwanted") and confirms the linter's error message was updated to include `-E` in the list of allowed options.

This is a straightforward modernization of test infrastructure that follows established standards and actual usage patterns. The change is small (two files modified) and uncontroversial, with Junio noting the `-E` option has been working fine in practice. The patch itself is well-explained with historical context about the original restriction and rationale for lifting it now.