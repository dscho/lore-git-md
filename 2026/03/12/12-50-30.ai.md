Test modernization patch updating a helper function in the partial clone test script.

This patch modernizes the `delete_object` helper in `t0410-partial-clone.sh` by replacing manual path construction with Git's built-in `test_oid_to_path` function. The change makes the test hash-independent (working with both SHA-1 and SHA-256 repositories) and adds explicit file existence checking via `test_path_is_file` before deletion, which provides better error messages if the object is missing.

The modification is straightforward and clearly motivated - it replaces fragile shell string manipulation with more robust test infrastructure functions. The patch follows Git's test modernization patterns and includes a clear commit message explaining the rationale. This appears to be a clean, uncontroversial improvement to test reliability.