This is a refactoring patch that's part of a larger series (v8 1/4) implementing configurable cover letter formatting for `git format-patch`. The patch cleanly extracts the shortlog-based cover letter generation code from `make_cover_letter()` into a new `generate_shortlog_cover_letter()` function. This is a pure code movement with no behavior changes, preparing for subsequent patches that will introduce customizable formatting options.

The refactoring maintains all existing functionality - the same shortlog formatting with author grouping, line wrapping, and indentation settings. The patch demonstrates good hygiene by keeping the function signature consistent with the original code's needs (taking the shortlog struct, rev_info, commit list, and count) and preserving all the initialization and output logic exactly as it was.

This is a straightforward preparatory change that's clearly part of a well-planned series. The commit message explains the motivation (enabling future format string support) without over-explaining the mechanical refactoring. The diff shows the clean separation of concerns while maintaining identical behavior.