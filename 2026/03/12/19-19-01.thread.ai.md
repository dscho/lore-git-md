Thread identity: Platform compatibility patch for GNU/Hurd build affecting the clar test framework.

Core problem or goal: Fix build/test behavior on GNU/Hurd systems which, like Windows with long path support, do not impose a PATH_MAX limitation. The change ensures consistent path length handling across platforms.

Current status: Single patch modifying `t/unit-tests/clar/clar.h` to add `defined(__GNU__)` alongside the existing Windows long path case, both using a 4096-byte path limit. No version history yet as this is the initial submission.

Key technical details: Changes path length handling in clar test framework by adding GNU/Hurd to share Windows' CLAR_WIN32_LONGPATHS behavior. Touches only the test infrastructure (not core Git functionality) with a one-line modification in the unit test header file.

Participant positions: Samuel Thibault submitted the patch identifying the platform-specific behavior. No other participants yet as this is the thread root.

Related work: Follows the pattern established for Windows long path support in the same file, suggesting prior consideration of similar issue on that platform. Part of broader test modernization effort including clar framework adoption.