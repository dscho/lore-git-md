Test modernization patch refining a partial clone test helper function.

Eric Sunshine reviews Siddharth Shrimali's patch that modernized the `delete_object` helper in `t0410-partial-clone.sh`. While he agrees with the core improvement of replacing manual path construction with `test_oid_to_path` (making the test hash-independent), he pushes back on the addition of `test_path_is_file` before the `rm` call. His argument is that `rm` already fails noisily for missing files, making the extra check redundant and potentially confusing to readers who might think there's some subtle reason for the check when there isn't.

This follows the thread's pattern of careful review even for small test improvements. Eric's feedback focuses on keeping the test code minimal and avoiding unnecessary operations, even when they don't hurt functionality. The discussion shows Git's culture of preferring the simplest solution that meets requirements, with reviewers willing to question additions that don't clearly add value. The patch now appears to need one more iteration to remove the redundant check while keeping the hash-independent path construction.