This is a follow-up discussion in the ongoing `git fast-import` signature handling series, specifically addressing warning message behavior in commit signature processing. The thread has settled on removing warning messages for both signature re-signing ("sign-if-invalid") and signature stripping ("strip-if-invalid") modes, as they create unnecessary noise when users have explicitly requested these operations.

Justin Tobler confirms they will leave the current implementation unchanged for this series and address the warning removal in a separate follow-up patch after merging. This approach allows the current series to proceed while giving the community a dedicated opportunity to discuss the warning message removal. The decision reflects consensus between Justin and Patrick Steinhardt that warning messages are not useful when users have explicitly opted into signature modification behaviors.

The discussion shows the series is in its final polishing phase, with only this minor behavioral consideration remaining before potential merge. The technical implementation itself is stable, having gone through multiple iterations and reviews. The follow-up warning removal will be a straightforward change touching `handle_signature_if_invalid()` and related test expectations, maintaining the separation of concerns established in the current architecture.