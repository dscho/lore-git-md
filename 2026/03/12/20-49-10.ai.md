This is a bugfix patch addressing zombie process cleanup in Git's network operations, now in its final form after review. The patch modifies `connect.c` and `transport-helper.c` to properly clean up child processes (like git-remote-https and ssh) when Git exits abnormally during network operations.

Junio C Hamano provides the final review, approving the technical approach while correcting two minor formatting issues: removing a blank line in the trailer block and excluding the Copilot AI attribution (which doesn't meet the project's standards for sign-off). The patch itself remains unchanged from v3 - it sets `clean_on_exit` and `wait_after_clean` flags on child processes spawned for proxy connections and remote helpers, ensuring proper cleanup via SIGTERM-then-wait even during abnormal exits.

The solution leverages Git's existing `run-command` infrastructure rather than custom atexit handlers, addressing Jeff King's earlier deadlock concerns. While the discussion established that running Git as PID 1 in containers is atypical (with tini being the preferred solution), this patch ensures Git properly cleans up its own children as a matter of correctness. The changes are narrowly scoped to network operations where abnormal exits are most likely to bypass normal cleanup paths.

With Junio's approval of the minor formatting fixes, this patch appears ready for merging. The technical consensus has been stable since v3, with the only changes being these final documentation and attribution cleanups. The minimal 6-line diff focuses precisely on the transport layer without expanding the scope to broader process management questions.