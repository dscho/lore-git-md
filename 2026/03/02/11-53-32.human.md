This is a discussion about the proposed `fetch.blobSizeLimit` config option, which would enable partial clone behavior by default. Patrick Steinhardt raises concerns about making blob size limits the default filter mechanism.

Patrick points out that server-side computation of blob sizes is expensive since it can't leverage optimizations like bitmaps. He suggests `--filter=blob:none` would be a more efficient default filter, though it would require more backfill fetches. He also questions whether a global config makes sense given that filter support varies by repository host.

The thread context shows this is a response to Alan Braithwaite's initial proposal, which aimed to make partial clones more discoverable by allowing users to set a default blob size threshold. Patrick's reply focuses on the performance implications and host-specific nature of filter support, suggesting the proposal may need reconsideration of both the filter type and scope.

This appears to be early-stage discussion about the tradeoffs involved in making partial clone behavior more accessible versus the server-side computational costs. The exchange highlights how Git's decentralized nature complicates default behaviors that depend on server capabilities.