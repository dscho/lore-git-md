This is a performance optimization discussion in the thread about duplicate option validation in Git's option parser. Jeff King (Peff) and René Scharfe are comparing benchmark results of different validation implementations across architectures.

The key points are:
- René reports that on an Apple M1, the strset implementation (from his patch) adds ~5ms (7% overhead) to help output generation compared to the baseline, while Jeff's qsort alternative performs worse (10% overhead)
- Jeff notes his i9 processor shows different relative performance characteristics, with the qsort approach being slightly faster in his tests
- Both agree the absolute differences are small enough that optimization isn't critical, especially since the validation only runs during help display (`-h`) rather than normal command execution
- The discussion confirms the strset implementation is a reasonable choice across architectures, with performance impact being architecture-dependent but minimal in practice

This email serves as final confirmation that the performance characteristics of the validation solution are acceptable, clearing the way for the patch series to proceed. The technical discussion shows careful cross-platform benchmarking but ultimately concludes the differences are negligible for the intended use case.