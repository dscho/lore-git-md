Thread identity: CI/build system patch addressing GitLab CI pipeline issue on Windows, authored by Patrick Steinhardt.

Core problem: The `GITLAB_FEATURES` environment variable in GitLab CI has grown too large (6.3KB), causing xargs(1) from Git for Windows to hit a POSIX argument length limit assertion failure. This breaks Windows CI pipelines.

Current status: Initial patch proposes unsetting `GITLAB_FEATURES` in `ci/lib.sh` when running under GitLab CI. The change is minimal (4 lines added) and focused specifically on the CI infrastructure issue.

Key technical details: Modifies `ci/lib.sh` to detect GitLab CI environment and unset the problematic variable. The fix targets Windows CI pipelines where xargs(1) from Git for Windows is affected by POSIX argument length limits.

Participant positions: Patrick Steinhardt (GitLab Git team lead) authored the patch with clear evidence of the environment size problem and its impact. No other participants yet as this is the first message in the thread.

Related work: This is a CI-specific fix that doesn't directly relate to other in-flight topics, though it may affect Windows testing infrastructure for other patches.