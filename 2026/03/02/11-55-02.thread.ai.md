Thread identity: CI/build system patch addressing GitLab CI pipeline issue on Windows, authored by Patrick Steinhardt, with Justin Tobler now participating.

Core problem: The `GITLAB_FEATURES` environment variable in GitLab CI (6.3KB) causes total environment size (17KB) to exceed POSIX argument length limits (12KB) when running xargs(1) from Git for Windows, triggering assertion failures that break Windows CI pipelines.

Current status: Initial patch proposes unsetting `GITLAB_FEATURES` in `ci/lib.sh` when running under GitLab CI (4-line change). Justin Tobler confirms the analysis but questions whether increasing the limit might be preferable to removing the variable.

Key technical details: Modifies `ci/lib.sh` to detect GitLab CI environment and unset the problematic variable. The fix specifically targets Windows CI pipelines where xargs(1) from Git for Windows hits POSIX argument length limits. Environment size measurements show 17KB total with the variable versus 10.7KB without it.

Open questions: Whether unsetting the variable (current approach) is better than attempting to increase system limits (Justin's suggestion). The variable's purpose and whether its removal could affect CI functionality remains to be discussed.

Participant positions: Patrick Steinhardt (GitLab Git team lead) authored the patch with environment size evidence. Justin Tobler (GitLab Git team) confirms the analysis but raises alternative solution of adjusting limits rather than removing the variable.

Related work: CI-specific fix that may affect Windows testing infrastructure for other patches, but no direct connection to other in-flight topics. The discussion remains narrowly focused on this Windows CI pipeline issue.