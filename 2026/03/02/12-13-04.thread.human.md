Here's a human-readable summary of the thread:

Patrick Steinhardt has proposed a new `git history split` command (v1 series, 8 patches) that allows splitting commits interactively while maintaining tree consistency. The implementation builds on extensive refactoring of Git's add-patch infrastructure to support in-memory index operations and introduces new commit creation logic. The command handles various edge cases (merge commits, empty splits) and includes 755 lines of tests covering scenarios like splitting tip/root/middle commits and branch updates.

D. Ben Knoble provided initial feedback, suggesting the command could silently handle cases where all/no hunks are selected (currently errors out) and questioning the naming of a new function (`commit_tree_with_edited_message_ext`), wondering whether "ext" stands for "extended" or "extracted". The rest of the implementation appears consistent with previous `git-history` commands like `reword`.

The series is structured with patches 1-7 refactoring core infrastructure (separating add-patch concerns, introducing new config handling, enabling in-memory index operations) before implementing the new command in patch 8. This continues Steinhardt's work on the experimental `git-history` command family, building on patterns established by earlier subcommands. The changes could enable future cleanups and benefit other commands needing similar functionality.