Here's the updated human-readable thread summary:

Patrick Steinhardt has proposed a new `git history split` command (v1, 8 patches) that would allow users to split commits while maintaining tree consistency. This builds on his ongoing work on the experimental `git-history` command family. The series begins with preparatory refactoring of the interactive add machinery to better separate concerns between `git add -p` and `git add -i`.

The first patch introduced a new `add-patch.h` header to isolate the core add-patch functionality. The second patch, just submitted, further refactors this by introducing a `struct interactive_options` to hold shared configuration, cleaning up the interface between components. These changes affect several commands that use interactive add functionality, including commit and stash.

The actual split functionality will come in later patches, which will build on this refactored infrastructure to implement commit splitting while automatically updating dependent branches (similar to how `git history reword` works). The series includes extensive tests (755 new lines) demonstrating the planned behavior. As this is the initial version from a core contributor driving the git-history effort, we can expect thorough review from experts in the add-patch and history subsystems.