This is the fourth patch in a 5-part series refactoring Git's oidmap cleanup API, specifically targeting the object database (odb) subsystem. The patch converts the replace_map cleanup in odb.c to use the new `oidmap_clear_with_free()` API introduced earlier in the series.

The change replaces a direct `oidmap_clear()` call with the new callback-based approach, adding a `free_replace_map_entry()` helper that properly frees each `struct replace_object` stored in the map. The implementation follows the established pattern from previous patches, using `container_of` to safely access entries. The diff is small (11 lines) and focused, maintaining the series' consistent approach to improving memory management clarity.

This continues the series' goal of making memory cleanup explicit and type-safe across Git's codebase. The patch appears well-crafted and builds logically on the foundation established by earlier patches. With the memory leak in patch 2 already addressed in v2, this change looks ready for merging as part of the complete series.