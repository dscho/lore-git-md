This is part 2 of Justin Tobler's 6-patch series adding repository statistics to `git repo`. The series has already been approved pending final polish, with this patch focusing on code quality improvements.

The patch introduces a new helper function `print_keyvalue()` to reduce code duplication in the machine-parsable output formatting for the `git-repo` command's "structure" subcommand. It replaces 16 repetitive `printf()` calls with cleaner calls to the new helper, while maintaining identical output format. The helper handles the key-value pair formatting with configurable delimiters, making the code more maintainable and easier to modify.

This is a straightforward refactoring that improves readability without changing functionality. The patch fits well with the series' overall goal of providing comprehensive repository statistics while keeping the code clean and maintainable. Since this is part of an already-approved series that's in its final polishing phase, this change represents a clear improvement that should be uncontroversial.