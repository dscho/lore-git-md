Junio C Hamano provides review feedback on Siddharth Shrimali's v2 patch that converts test patterns in t/t3700-add.sh to properly handle exit codes and use test_grep. This is part of Git's ongoing test modernization effort to improve reliability by fixing shell patterns that could mask failures.

The review identifies three main issues with the current patch: 1) The patch should be split into two logical changes (exit code handling first, then test_grep conversion), 2) There's an incorrect newline at EOF that suggests the patch was based on a botched earlier version rather than a clean tree, and 3) The Signed-off-by trailer has an extra blank line that violates formatting rules.

Junio suggests restructuring the series as two patches - first fixing pipeline exit code handling while cleaning up any previous mistakes, then converting to test_grep. He notes that while test_grep improvements are valuable, they should be treated as a separate logical change from the exit code fixes. The feedback maintains the project's high standards for test suite reliability while providing clear guidance for the next iteration.