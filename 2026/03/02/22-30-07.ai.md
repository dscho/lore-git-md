This is the third patch in a 5-part series refactoring Git's oidmap cleanup mechanism. The patch converts the list-objects-filter subsystem to use the new `oidmap_clear_with_free()` API introduced in the series' first patch, replacing the old boolean-flag based cleanup.

The change is straightforward: it adds a `free_seen_map_entry()` callback that uses `container_of` to safely free `seen_map_entry` structures, then updates `filter_trees_free()` to use the new API. Junio notes that while this particular conversion doesn't immediately improve clarity (since `seen_map_entry` currently holds no extra resources), it follows the same future-proof pattern as other conversions in the series and will pay dividends if the structure grows more complex later.

The patch maintains the series' high quality standards with careful use of `container_of` and thorough documentation. Junio's review suggests he sees this as a mechanical but worthwhile change that aligns with the series' overall direction. With the rev-list conversion already approved, this list-objects-filter change appears to follow the same proven pattern. The series now awaits resolution of Junio's earlier question about the `cmpfn` check in the foundational API change before proceeding further.