This is the third patch in Patrick Steinhardt's 8-part series introducing a new `git history split` subcommand. The patch continues refactoring the add-patch machinery to reduce dependencies between subsystems.

The change removes `add-patch.c`'s dependency on the `add-interactive` subsystem by directly embedding the interactive configuration (`struct interactive_config`) rather than inheriting it via `struct add_i_state`. This follows from the previous patch which split out the shared configuration between `git add -p` and `git add -i`.

The patch is a mechanical refactoring that:
1. Replaces `struct add_i_state` with direct fields for `struct repository*` and `struct interactive_config`
2. Updates all references to use the new direct fields instead of going through `s.s`
3. Changes initialization to use `interactive_config_init()` directly rather than `init_add_i_state()`

The diff shows 88-line file with 46 insertions and 42 deletions, indicating this is primarily a straightforward code movement with minimal functional changes. As part of the larger `git history` effort, this cleanup helps isolate the add-patch functionality for reuse in the new history editing commands.

Given Patrick's role as a core contributor and the careful separation of concerns in this refactoring, the patch appears well-considered and likely to be accepted after review. The changes are focused and don't appear to introduce new behavior, just cleaner internal structure.