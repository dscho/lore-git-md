This is the first patch in Justin Tobler's approved 6-patch series adding repository statistics to `git repo`. The series tracks various object store metrics like maximum object sizes and counts.

This particular patch refactors the object counting logic in `builtin/repo.c` to process statistics per-object rather than in bulk. The change moves the statistics updates inside the object iteration, switching from accumulating totals for all objects of a type to updating counts for each individual object. This prepares the codebase for the more granular statistics tracking that comes in later patches.

The implementation is straightforward - it replaces the batch processing with per-object switch statement while maintaining the same counting behavior. The diff shows 24 lines added and 29 removed, with the main change being structural rather than functional. This appears to be a clean refactoring that doesn't introduce new behavior, just sets up the infrastructure for the subsequent patches to build upon.

Given this is part of an already-approved series that's in final polishing stages, and the change is clearly motivated as preparation for later functionality, this looks like a solid incremental improvement to the code structure.