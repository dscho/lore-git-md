This is a follow-up in the config-based hooks series, where Adrian Ratiu is implementing the ability to specify hooks via Git configuration (hook.<name>.command, hook.<name>.event, etc.) rather than just filesystem hooks. The series is now at version 3.

Junio Hamano points out a workflow issue - the patch series is based on the 'next' branch rather than 'master', which creates a dependency that could block progress. He notes that the topic has already been in 'next' for several days and should now be moving forward incrementally. This is a maintainer-level concern about integration strategy rather than technical feedback on the hooks implementation itself.

The thread context shows this is part of a mature series (v3 of 12 patches) that has already addressed substantial review feedback from key contributors like Patrick Steinhardt. The core functionality appears solid, with features like config-based hooks, out-of-repo execution support, and disabled hook visibility already implemented. Junio's comment here is about the final steps to land the work rather than its technical merits.