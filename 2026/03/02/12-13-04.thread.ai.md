Updated AI summary:

Thread identity: New `git history split` subcommand patch series (v1, 8 patches) from Patrick Steinhardt, extending the experimental `git-history` command family.

Core problem/goal: Implement commit history editing via splitting commits while maintaining tree consistency. Key design choices include prompting for both resulting commit messages (not just first), avoiding hunk editing to prevent conflicts (second commit's tree matches original), and updating dependent branches like `git history reword`.

Technical details:
- Patch 1/8: Mechanical header refactoring separating add-patch concerns (struct add_p_opt, enum add_p_mode, run_add_p) into new add-patch.h while keeping add-interactive specifics in original file
- Patch 2/8: Introduces `struct interactive_options` for shared config between `git add -p` and `git add -i`, relocates color/config handling into new `interactive_config` with dedicated init/clear functions. Updates callers in builtin/add.c, builtin/commit.c, builtin/stash.c
- Adds capability to write in-memory indexes as trees (future patch)
- Implements split command in `builtin/history.c` (future patch)
- Touches subsystems: add-patch (refactored), cache-tree, history
- 755 lines of new tests demonstrating functionality (future patch)

Current status: First two patches of initial version submitted. Patch 1 is pure header refactoring. Patch 2 refactors interactive add machinery to reduce circular dependencies while maintaining functionality. No reviews or integration status yet.

Participant positions: Only Patrick Steinhardt so far as author. Given his role driving the git-history effort and architectural changes, this is likely to receive attention from reviewers familiar with those subsystems (potentially Phillip Wood for add-patch, Elijah Newren for history editing).

Related work: Part of ongoing `git-history` command family development, follows similar patterns to `git history reword` for branch updating. Builds on refactored add-patch and cache-tree infrastructure. The interactive add refactoring may enable future cleanups of the add-patch/add-interactive relationship.