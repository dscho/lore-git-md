This is a follow-up discussion in the ongoing thread about adding a `re-sign-if-invalid` mode to `git fast-import` for handling invalid commit signatures. The exchange focuses on technical implementation details raised in review feedback.

Justin Tobler responds to brian m. carlson's concerns about handling `extensions.compatObjectFormat` in the signature re-signing feature. He agrees to make the conservative choice of making the operation fail explicitly when encountering compatibility objects rather than attempting conversion, since the format is still experimental. This addresses brian m. carlson's strong recommendation to avoid silent corruption risks.

The discussion also touches on test coverage, with brian m. carlson suggesting additional tests similar to those in t7004 for embedded PGP messages in tags. Justin notes that while the current series only handles commit signatures, he plans to address tag signatures in a future follow-up series.

Key technical points being resolved:
- Explicit failure for `extensions.compatObjectFormat` cases rather than attempting conversion
- Future test coverage plans including tag signature handling
- Clear separation between current commit-focused work and planned tag support

The exchange shows the patch series moving forward with specific technical adjustments based on maintainer feedback, while deferring some related work (tag support) to keep the current changeset focused. The conservative approach to experimental features aligns with the project's handling of unstable interfaces.