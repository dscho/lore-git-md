This is a refactoring series converting Git's oidmap cleanup API from a boolean flag to an explicit callback-based approach. The cover letter introduces a 5-patch series that replaces `oidmap_clear(map, 1)` with `oidmap_clear_with_free()` across several subsystems, making memory management more explicit and type-safe.

The series follows a consistent pattern: each patch converts one subsystem to use the new API by introducing a dedicated free callback function for the specific structure type stored in the oidmap. The changes touch builtin/rev-list.c (handling missing_objects entries), list-objects-filter.c (seen_at_depth entries), odb.c (replace_map entries), and sequencer.c (string_entry structures). The first patch establishes the core API change in oidmap.[ch] and adds unit tests.

Version 2 addresses a memory leak found by Junio C Hamano in the rev-list conversion, adding proper cleanup of path strings in the missing_objects entries. The diffs remain focused and small (8-15 lines per patch), with clear commit messages explaining the motivation for each change. The series demonstrates good attention to detail in using the container_of pattern consistently to safely access embedded structures.

This appears to be a well-structured, incremental refactoring that improves code clarity and type safety while maintaining identical behavior. The changes align with Git's ongoing efforts to make resource management more explicit in internal APIs. The series looks ready for merging after the v2 fixes, assuming the memory leak correction properly addresses Junio's concern.