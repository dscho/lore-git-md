This is a follow-up discussion in the `git fast-import` feature series that adds a `re-sign-if-invalid` mode for handling invalid commit signatures during import. The thread has already established the core functionality and received substantial review feedback.

Justin Tobler responds to brian m. carlson's concerns about handling `extensions.compatObjectFormat` in the signature re-signing feature. He agrees to make the conservative change of making the operation fail explicitly when encountering compatibility mode, rather than attempting to handle it. This aligns with brian m. carlson's position that the experimental feature shouldn't be silently supported in its current form.

The exchange also reveals that tag signature handling will be addressed in a future series, with Justin planning to model the tests after t7004's embedded PGP message handling. The discussion shows the patch is progressing well through review, with the author responsive to technical concerns about edge cases and interoperability. The feature appears likely to land in a subsequent version after these final refinements.