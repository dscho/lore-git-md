This is the fourth patch in a 5-part series refactoring Git's oidmap cleanup API, specifically targeting the object database (odb) layer. The patch converts the replace_map cleanup in odb.c to use the new `oidmap_clear_with_free()` API introduced earlier in the series.

The change replaces a direct `oidmap_clear()` call with the new callback-based approach, adding a `free_replace_map_entry()` helper that properly frees each `struct replace_object` stored in the map. The implementation follows the established pattern from previous patches, using `container_of` to safely access entries while maintaining type safety. The 11-line diff is focused and straightforward, with 10 lines added (including the new helper function) and 1 line modified in the cleanup logic.

The patch maintains the existing behavior while making memory management more explicit and type-safe. It includes a conditional check for `replace_map_initialized` before attempting cleanup, preserving the original code's defensive programming. This continues the series' consistent approach to improving internal APIs for better resource management clarity.