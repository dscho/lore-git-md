This is a technical discussion about path format handling in the `git repo-info` command, part of an ongoing series to add path-related fields. The thread has previously established consensus on using a global `--path-format` flag rather than per-field format control.

Tian Yuchen proposes an alternative approach using format modifiers like `(%path:relative)` and `(%path:absolute)` that would be parsed in `ref-filter.c`. This would avoid global flags while maintaining readability. The suggestion comes in response to Junio Hamano's observation that `git rev-parse` allows per-option format changes but at the cost of complexity.

The proposal represents a middle ground between the current global flag approach and `rev-parse`'s fine-grained control. Implementation would involve adding path parsing to `ref-filter.c` and while Tian notes this isn't particularly challenging technically, it would represent a design shift from the previously agreed direction. The thread has already seen extensive discussion weighing interface simplicity against flexibility, with Junio favoring the simpler global flag solution despite its limitations.

This email keeps the technical discussion active while the series is being finalized, showing continued interest in exploring the best long-term API design for path formatting in Git commands. The core question remains how to balance user needs for format control against maintainability and interface clarity.