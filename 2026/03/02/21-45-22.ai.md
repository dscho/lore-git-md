This is patch 2/6 in Justin Tobler's series adding repository statistics to `git repo`. The patch focuses on code quality improvements in the output formatting logic.

The change introduces a new `print_keyvalue()` helper function to consolidate repeated printf patterns for key-value output in the machine-parsable formats of the `git-repo` "structure" subcommand. The function handles the key, delimiter, numeric value, and terminator character in a single call, replacing 16 separate printf statements with cleaner calls.

Key technical details:
- The helper maintains identical output format while reducing code duplication
- Uses `PRIuMAX` for consistent uintmax_t formatting
- Preserves the existing `fflush(stdout)` behavior
- All statistics fields (reference counts, object counts, inflated sizes, disk sizes) are converted to use the new function

This is a straightforward refactoring that improves maintainability without changing functionality. The patch follows the series' focus on careful code structure while implementing repository metrics collection. It addresses one of the code quality observations from earlier reviews in the thread.