This is part 2 of Tian Yuchen's series to migrate encoding-related functionality from global variables to repository-specific storage, as part of the ongoing `the_repository` removal effort. The patch focuses on updating pretty-printing functions to take a `struct repository*` parameter, allowing them to eventually access encoding settings from repository context rather than global state.

The changes are mechanical but widespread, touching 14 files across various builtins and helper functions. The patch updates `pretty_print_commit()` and `pp_commit_easy()` to take a repository parameter, then propagates this change through all callers. Where no repository context is readily available (particularly in builtin commands), the code falls back to `the_repository` as an interim measure.

This follows the established pattern for `the_repository` removal - plumbing the repository pointer through function calls while maintaining behavior via fallbacks. The changes appear straightforward and maintain existing functionality, with thorough updates to commit display paths in commands like `log`, `merge`, `stash`, and others. The patch builds on prior work in the series and integrates with Olamide Caleb Bello's `repo_config_values` infrastructure introduced in earlier versions.