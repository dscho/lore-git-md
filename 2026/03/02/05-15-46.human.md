This is part 4 of a 6-patch series introducing enhanced repository path reporting functionality. The series has evolved through multiple versions, with this iteration focusing purely on path reporting after removing metrics components from earlier versions.

The current patch adds a `--path-format` option to control whether paths are output as absolute or relative. This provides callers with explicit control over path formatting, aligning with existing `rev-parse` behavior patterns. The implementation centralizes path formatting logic in `repo_info_add_path()`, ensuring consistent handling across all path keys.

The patch is mechanically straightforward, adding an enum for the format options and wiring it through the existing infrastructure. It maintains the series' focus on providing a clean, structured interface for repository path information. The change appears well-contained and builds logically on the previous patches in the series.

This continues the series' progression toward a more flexible and script-friendly path reporting mechanism, with the remaining discussion likely focused on final naming and behavior details rather than fundamental design.