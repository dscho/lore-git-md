This is part 2 of Tian Yuchen's series to migrate encoding-related global variables to repository-specific storage, continuing the `the_repository` removal effort. The patch focuses on plumbing a `struct repository*` parameter through the pretty-printing APIs (`pretty_print_commit()` and `pp_commit_easy()`) to prepare for eliminating the `log_output_encoding` global.

The changes are mechanical but widespread, updating 14 files across builtins and core formatting logic. Each caller now passes either an explicit repository pointer or falls back to `the_repository` where context isn't readily available. The patch maintains existing behavior while enabling future patches to access encoding settings from repository-specific storage (using Olamide Caleb Bello's `repo_config_values` infrastructure introduced in the series).

Key technical points: The pretty-printing functions now take a `struct repository*` as their first parameter, with call sites updated in commands like `log`, `merge`, `stash`, and `show-branch`. The sequencer and range-diff machinery are also updated to pass through the repository context. This follows established pattern for global variable migration seen in other subsystems.

The patch appears well-structured, with changes focused purely on the plumbing needed for the encoding migration. It builds on prior feedback (Phillip Wood's suggestion to store encodings directly in `struct repository`) while incorporating newer infrastructure (`repo_config_values`). Test suite passes with sanitizers, suggesting careful attention to maintaining behavior during the transition.