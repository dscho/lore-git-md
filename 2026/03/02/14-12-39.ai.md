This is a follow-up in an ongoing thread about performance regressions in `receive-pack` and `fetch` operations, specifically focusing on a reftable backend slowdown. Adrian Ratiu clarifies that there appear to be two distinct issues at play, separating the earlier discussion about geometric maintenance from the reftable-specific regression.

The key point here is Adrian's realization that the reftable performance degradation (a 2.66x slowdown when handling 10k refs) bisected to commit fc148b146a ("receive-pack: convert update hooks to new API") may be unrelated to the earlier-discussed merge of the hooks configuration series (6a04cca28e). While the hooks configuration merge timing was initially suspected as a potential cause, Adrian now plans to investigate the reftable-specific regression separately.

This email marks a pivot in the thread's focus - from considering the hooks configuration series as a possible root cause to treating the reftable regression as an independent issue. The technical context remains the same: the regression manifests in the update hooks path when using the reftable backend, showing a significant system time increase (from 89ms to 376ms). Adrian's next step will be to reproduce and analyze this specific regression.