This is a brief discussion about path format handling in the ongoing `git repo-info` feature development. Junio C Hamano responds to brian m. carlson's observation about the flexibility of `git rev-parse`'s `--path-format` option compared to the current `git repo-info` implementation.

Junio acknowledges that while `rev-parse`'s ability to change path formats mid-command provides flexibility (allowing users to request different formats for different paths in a single invocation), this same capability has historically been a source of confusion for users. His comment suggests that the `repo-info` command's simpler, single-format approach may be preferable despite being less flexible, as it avoids the complexity that has caused issues in `rev-parse`.

The exchange highlights a design tradeoff between flexibility and usability that the project has encountered before. The thread context shows this is part of a larger discussion about how to best expose path-related information through the new `repo-info` command while maintaining clean, understandable behavior.