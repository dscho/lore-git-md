Thread identity: Performance fix for hook subsystem in `git-receive-pack`, addressing a regression in sideband hook handling introduced during the hook API conversion. Patch submitted by Adrian Ratiu, with problem identification by Patrick Steinhardt and root cause analysis by Jeff King.

Core problem: The recent hook API conversion removed an optimization where sideband hooks would exit early if no hook was found, causing unnecessary overhead from spinning up async threads that would do nothing. This affects `builtin/receive-pack.c` specifically.

Current status: Patch v1 submitted, adding back the early-exit check for sideband hooks. Small (12-line) focused change that has passed GitHub CI testing.

Key technical details: Modifies `builtin/receive-pack.c` to restore the optimization where sideband hooks check for existence before spinning up async threads. Only affects the receive-pack builtin's hook handling path.

Participant positions:
- Patrick Steinhardt identified the performance regression
- Jeff King root-caused the issue to the missing early-exit check
- Adrian Ratiu submitted the fix with their analysis

Related work: Part of ongoing hook subsystem improvements, following the recent hook API conversion work.