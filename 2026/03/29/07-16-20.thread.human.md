Here's a human-readable thread summary:

The thread discusses an RFC patch from Shabbir Bhojani proposing to add `--include-untracked` support to `git stash create`. This would bring the command in line with `git stash push`'s existing functionality, allowing users to create entries containing untracked files without modifying the working directory. The author found this particularly useful when development tools reacted poorly to working directory changes during normal stash operations.

The implementation appears solid, adding the new flag through the stash creation chain while maintaining existing behavior when the flag isn't set. The patch includes comprehensive tests and documentation updates. While marked as RFC, the change seems like a reasonable quality-of-life improvement for users who need to snapshot working states (including untracked files) without disrupting their editor/IDE workflow. No reviews or objections have been raised yet in the thread.