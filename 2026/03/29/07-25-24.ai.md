Feature enhancement for `git cat-file --batch-command` mailmap support.

Siddharth Asthana responds to Junio C Hamano's feedback on the mailmap toggling patch, agreeing to simplify the interface by consolidating the separate `use-mailmap` and `no-use-mailmap` commands into a single `mailmap` command that takes `yes`/`no` parameters. This change will reduce code duplication in the command dispatch table and make the API more intuitive. The author confirms that mailmap data remains cached in memory when disabled, avoiding reload cost when re-enabled, which addresses Junio's question about cache behavior.

The response indicates the patch will be revised as version 2 with these improvements, showing constructive engagement with maintainer feedback. The change affects `builtin/cat-file.c`'s command dispatch table structure while maintaining the same underlying mailmap caching behavior from `mailmap.c`. This appears to be moving toward a cleaner solution for GitLab's Gitaly service needs without introducing new technical concerns.