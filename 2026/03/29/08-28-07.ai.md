Feature addition to `git cat-file`'s batch mode, now with simplified mailmap toggling interface.

Siddharth Asthana follows up on his earlier proposal to add dynamic mailmap control to `git cat-file --batch-command`, incorporating Junio C Hamano's feedback to simplify the interface. The new version replaces the original `use-mailmap`/`no-use-mailmap` subcommands with a single `mailmap` command that takes `yes` or `no` arguments. This reduces code duplication in the command dispatch table while maintaining the same functionality - mailmap data is still cached when disabled to avoid reload costs.

The patch remains focused on GitLab's Gitaly service use case, where long-running `git cat-file` processes need to toggle mailmap processing without restarting. The implementation continues to touch the same areas: `builtin/cat-file.c` for the command dispatch logic, documentation updates, and tests in `t/t4203-mailmap.sh`. The test cases have been updated to use the new unified syntax while verifying the same behaviors around buffer mode and output size changes.

This revision appears to address all concerns from the initial review while keeping the core functionality intact. The changes are straightforward and well-contained, with no new technical concerns introduced. The thread demonstrates effective collaboration between contributor and maintainer to refine the interface while preserving the original motivation.