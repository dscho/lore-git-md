This is a refactoring patch in Taylor Blau's 16-part series on incremental MIDX/bitmap-based repacking. The patch extracts common MIDX command preparation logic into a new helper function `repack_prepare_midx_command()` that will be reused by future patches in the series.

The change factors out the shared setup steps for `git multi-pack-index` commands - setting `git_cmd`, pushing the subcommand and verb, and handling progress/bitmap flags - while leaving subcommand-specific arguments (like `--stdin-packs` for the `write` operation) to be added by the caller. This prepares for upcoming patches that will need to construct MIDX `compact` commands using the same basic setup.

The patch touches only `repack-midx.c`, moving existing code into the new helper while maintaining identical behavior. This is a clean, mechanical refactor with no functional changes, typical of the preparatory work seen throughout this series. The change demonstrates good attention to code organization as the incremental repacking functionality grows in complexity.