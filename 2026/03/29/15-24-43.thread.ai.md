Git backfill progress reporting thread summary:

Thread concerns Trieu Huynh's `git backfill` command improvements, adding `--[no-]progress` option following Git's standard progress reporting conventions. Current patch (v1) shows batch download progress by default on TTY. Maintainer Junio C Hamano and reviewer Derrick Stolee have provided substantive feedback converging on key issues.

Implementation details:
- Current patch tracks batches with "Downloading batches" status
- Progress shown by default on TTY (via `isatty(2)`)
- Adds progress fields to `backfill_context` struct
- Uses pre-increment operators in void contexts (stylistic issue)

Key feedback from reviewers:
1. Junio and Stolee agree batch counting may not be most useful - should track object count instead
2. Junio notes pre-increment operators violate Git conventions (should use post-increment/pre-decrement in void contexts)
3. Stolee's earlier concerns about verbose remote output and test coverage remain open
4. Need to verify compatibility with recent rev-list option changes

Required changes for next version:
- Switch from batch to object counting
- Adjust increment operators to follow project style
- Re-evaluate output verbosity
- Add TTY prerequisite to tests
- Consider testing object count metrics

Documentation will need updates based on final behavior decisions. Thread shows consensus on feature value but needs implementation refinements before merging. No new technical objections raised - focus remains on metric selection and code style improvements.