Git backfill progress reporting thread summary:

Thread concerns the `git backfill` command, which downloads missing objects from a repository. Current patch (v1) adds `--[no-]progress` option to show/hide progress meter during operation, following Git's standard progress reporting conventions. Implementation:

- Progress shown by default on TTY (via `isatty(2)` check), matching `git fetch` behavior
- Explicit `--progress`/`--no-progress` override available
- Tracks batches downloaded with "Downloading batches" status
- Adds progress fields to `backfill_context` struct with proper init/cleanup

Test coverage verifies:
- Progress appears with `--progress` on TTY
- Progress suppressed with `--no-progress` on TTY
- Default matches other commands (silent on non-TTY)

Patch appears well-scoped, following Git conventions for progress reporting. No reviews or discussion yet as this is the initial submission. Changes are limited to backfill command's user experience improvement, particularly useful for large repositories where operation may take noticeable time. Documentation updated via option help text.