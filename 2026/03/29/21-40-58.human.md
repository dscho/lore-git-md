This is a refactoring patch in Taylor Blau's incremental MIDX/bitmap repacking series. It introduces a new `strvec_init_alloc()` function that allows callers to pre-allocate space in a string vector when they know how many elements will be needed. The existing `strvec_init()` is reimplemented as a special case of this new function.

The change is straightforward - it provides a small optimization opportunity for cases where the number of elements is known in advance, avoiding reallocations as the vector grows. The implementation carefully handles edge cases (like zero allocation) and maintains the existing behavior where the vector always has space for a NULL terminator.

This appears to be a preparatory change for the larger repacking work, likely used in the MIDX file management code where the number of files to track is known ahead of time. The patch is small, well-contained, and has Junio's sign-off, suggesting it's already been reviewed. While minor on its own, it's part of a larger effort to improve Git's scalability for large repositories.