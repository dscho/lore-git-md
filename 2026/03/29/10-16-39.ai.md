This is part 1/5 of a refactoring series in the ongoing `the_repository` removal effort. Shreyansh Paliwal continues converting the refs subsystem to explicitly pass repository context rather than relying on global state.

The patch focuses on branch name handling helpers, adding explicit `struct repository*` parameters to functions like `copy_branchname()`, `check_branch_ref()`, `validate_branchname()`, and `validate_new_branchname()`. These functions previously implicitly used `the_repository` when calling `repo_interpret_branch_name()`. The changes propagate through builtin commands (branch, checkout, worktree) that use these helpers.

Key technical changes include updating function signatures across 9 affected files while maintaining existing behavior. The patch builds on prior work (jw/object-name-bitset-to-enum) that introduced branch interpretation enums. All callers are updated to pass `the_repository` where they don't already have repository context, keeping the changes mechanical and behavior-preserving.

This is a well-structured continuation of the series that follows established patterns for removing global state. The changes are straightforward and maintain existing functionality while making repository dependencies explicit. The patch appears ready for merging as part of the larger effort.