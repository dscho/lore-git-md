This thread introduces a series from new contributor Jiamu Sun adding subcommand autocorrection to Git's parse-options API, initially targeting commands like `git remote` and `git notes`. The first two patches establish the foundation: Patch 1 refactors subcommand handling in parse-options.c to simplify the code structure, while Patch 2 moves the existing command autocorrection logic from help.c into new autocorrect.{c,h} files with improved state management.

The refactoring in Patch 2 introduces a dedicated `autocorr` type with explicit modes (HINTONLY, NEVER, etc.) and separates alias resolution from autocorrection logic, though it still uses `the_repository` global. Both patches maintain existing behavior while preparing for the new subcommand autocorrection feature to follow. The series appears well-structured with clear commit messages explaining each preparatory step. No reviews have been posted yet as this is a new submission.