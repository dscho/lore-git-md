This is the first patch in a series adding subcommand autocorrection support to Git's parse-options API. It refactors the subcommand handling logic in parse-options.c to prepare for the autocorrection feature.

The patch extracts subcommand parsing from parse_options_step() into a new handle_subcommand() helper function. It also simplifies parse_subcommand() to return a basic success/failure status rather than an enum parse_opt_result. The changes make the code more maintainable by:
- Removing the switch over impossible parse_opt_result values
- Making the non-option path easier to follow
- Keeping subcommand handling logic together in one place

The implementation remains functionally equivalent but better organized for the autocorrection feature coming in later patches. The diff shows clean separation of concerns with no behavior changes - this is purely preparatory refactoring.

Key technical details:
- Moves logic to new handle_subcommand() function
- Simplifies parse_subcommand() return value
-1/0 instead of enum
- Maintains all existing error handling and usage messages
- Preserves PARSE_OPT_SUBCOMMAND_OPTIONAL behavior
- Files touched: parse-options.c
- No new tests yet (those come in patch 5/5)

This appears to be a well-crafted preparatory patch that follows Git's incremental change philosophy. The author has clearly separated the refactoring from the actual feature addition, making the series easier to review. The changes are focused and maintain all existing functionality while cleaning up the code structure.