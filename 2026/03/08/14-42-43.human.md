This is a follow-up discussion in the const-correctness refactoring thread for the patch-id subsystem. Tian Yuchen acknowledges Junio Hamano's critique of their initial approach, which identified a fundamental tension between the hashmap API's const requirements and the patch-id system's lazy initialization needs.

Yuchen now recognizes the dilemma: the hashmap API demands const parameters for comparison functions, but the patch-id system needs to modify objects during lazy initialization for performance reasons. They propose two possible paths forward - either accept the const-cast as a necessary compromise (documenting it clearly) or undertake a more invasive restructuring to pre-compute all patch IDs before hashmap insertion.

The discussion has shifted from a simple const-qualifier addition to confronting a design-level conflict between type safety and performance patterns. Yuchen appears to favor maintaining the current lazy initialization approach despite its const-cast requirement, seeing it as more practical than the alternative of pre-computing all patch IDs. They indicate they will send an updated patch soon, likely focusing on better documenting the tradeoff rather than attempting to fully resolve the architectural tension.

This exchange highlights how what began as a straightforward cleanup can uncover deeper design questions in Git's infrastructure. The thread remains active with Yuchen planning to submit a new version that addresses Junio's concerns about properly documenting the const-cast rather than pretending to achieve full const-correctness.