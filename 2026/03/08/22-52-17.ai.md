This is a technical discussion about `git fetch` performance over slow network connections, specifically addressing SMB/CIFS mounted repositories. The thread has evolved from diagnosing protocol-level issues to considering filesystem-specific optimizations.

brian m. carlson clarifies several key points about Git's behavior in this scenario. They explain that even for local filesystem fetches, Git still uses the upload-pack protocol internally for security and efficiency reasons, rather than doing direct file copies. This protocol usage explains the observed delays in reference resolution and tag peeling, even though the repository is accessed via a mounted share.

The response addresses R. Diez's questions about parallel I/O and branch-specific fetching. brian acknowledges that while parallel requests could theoretically help with high-latency network mounts, Git (like most Unix tools) is optimized for low-latency local filesystems. They suggest that fetching only the master branch with `--no-tags` would likely improve performance by reducing the amount of reference processing needed.

The email also confirms that with the standard configuration using `+refs/heads/*`, Git will fetch all branches and their associated tags by default. This explains why the original fetch operations were so slow - they were processing the entire reference namespace rather than just the needed branches.

Key technical details for future reference:
- Upload-pack protocol is always used, even for local filesystem fetches
- Default fetch configuration processes all branches and tags
- `--no-tags` can avoid tag-peeling overhead
- Branch-specific fetching (`git fetch origin master`) may help
- Parallel I/O not currently implemented for this case
- The discussion remains unresolved regarding whether filesystem-specific optimizations could bypass some protocol limitations
- brian maintains that network speed is the fundamental constraint
- R. Diez continues to explore optimization possibilities within their specific SMB/CIFS setup