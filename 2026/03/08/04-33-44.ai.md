This is a contributor housekeeping patch updating the `.mailmap` file. Tian Yuchen is adding a mapping from their old Gmail address (`a3205153416@gmail.com`) to their new custom domain address (`cat@malon.dev`). 

The change is straightforward - a single line addition to the `.mailmap` file which ensures contributions under the old email will be properly attributed to the new one. This is a common type of maintenance patch that helps keep contributor identities consistent in the Git history.

The patch follows the standard format for `.mailmap` entries and appears correctly formatted. No functional changes are introduced - this purely affects how commit authorship is displayed. The patch is ready to be applied as-is.