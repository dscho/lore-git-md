This is a follow-up discussion in the const-correctness refactoring thread for the patch-id subsystem, specifically addressing the tension between hashmap API requirements and lazy initialization needs.

Tian Yuchen acknowledges Junio Hamano's point about the fundamental conflict between the hashmap API's const-correctness guarantees and the patch-id subsystem's lazy initialization pattern. The core issue is that `patch_id_neq()` must modify objects (to compute patch IDs on demand) despite being called through a const interface. Yuchen now recognizes that their initial approach of adding const qualifiers without addressing this underlying design issue was insufficient.

The email presents two potential paths forward: either accept the const-cast as a necessary violation and document it clearly (the more pragmatic approach), or undertake a more invasive restructuring to pre-compute all patch IDs before hashmap insertion (which would avoid the const violation but potentially impact performance). Yuchen leans toward the first option, seeing it as more "reasonable" and "cost-effective" given the performance implications of pre-computation.

This discussion highlights a recurring pattern in Git's codebase where performance optimizations (like lazy evaluation) sometimes require compromising on strict type safety. The thread has evolved from a simple const-correctness fix to confronting a deeper architectural trade-off between API purity and practical performance considerations. Yuchen indicates they will send an updated patch soon, likely choosing between documenting the const-cast or modifying the NEEDSWORK comment to better capture the inherent tension.