René Scharfe identified and fixed a memory safety issue in the `git history reword` command where an uninitialized `struct rev_info` could be passed to `release_revisions()` if argument validation failed. His straightforward solution zero-initializes the struct at declaration to ensure safe cleanup in all code paths. The change is minimal and contained to `builtin/history.c`, typical of René's precise code hygiene contributions.

Patrick Steinhardt has reviewed and confirmed the correctness of the approach, signaling this is likely ready for merging. The patch doesn't affect functionality but prevents potential memory corruption when invalid arguments are provided. With no outstanding concerns raised, this small but important safety fix appears to be picked up in the next integration cycle.