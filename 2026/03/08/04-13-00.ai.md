This is a test modernization patch that concludes a series converting fragile test patterns in `t/t3310-notes-merge-manual-resolve.sh`. The patch replaces direct `git rev-parse` command substitutions with more robust test helpers and shell variables, following Eric Sunshine's review feedback across multiple iterations.

The changes improve debuggability by:
1. Using shell variables to store `rev-parse` output rather than inline command substitutions
2. Replacing multi-line `echo` chains with `test_write_lines`
3. Maintaining identical test coverage while providing better failure output

Eric Sunshine confirms this v4 iteration addresses all technical requirements and provides a Reviewed-by tag, indicating the series is now complete. The thread demonstrates Git's typical test improvement process - mechanical conversions to more robust helpers, guided by experienced reviewers, with the author (Francesco Paparatto) iterating responsively. No functional changes to the notes merge behavior are made; this is purely a test infrastructure improvement.

The patch exemplifies Git project priorities around test suite maintainability, showing how command substitutions can hide failures and how dedicated test helpers (`test_cmp`, `test_cmp_rev`, `test_write_lines`) provide better diagnostics. With all review feedback addressed and a reviewer sign-off, this appears ready for integration.