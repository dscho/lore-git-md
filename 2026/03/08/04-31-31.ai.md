This is a const-correctness refactoring patch targeting the patch-ids subsystem. Tian Yuchen addresses an 8-year-old NEEDSWORK comment in `patch_id_neq()` by properly propagating const qualifiers through the container_of macro usage and related function calls.

The patch makes several changes to ensure const correctness:
1. Changes the patch_id pointers to be const-qualified
2. Explicitly casts cmpfn_data to struct diff_options* rather than void*
3. Uses const struct patch_id in the container_of macros
4. Adds explicit casts to struct object_id* when passing patch_id fields to commit_patch_id()

The changes are mechanical and focused solely on type safety without altering behavior. The commit message clearly explains the changes and their motivation (resolving the longstanding NEEDSWORK comment). The diff shows careful attention to const propagation through the call chain.

This appears to be a straightforward, well-contained improvement to code hygiene in a low-level comparison function used by the patch-id machinery. The changes are unlikely to be controversial as they don't modify logic, just type annotations.