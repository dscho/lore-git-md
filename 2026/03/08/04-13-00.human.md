This is a test modernization patch in a series that has already gone through several iterations. The patch finalizes improvements to `t/t3310-notes-merge-manual-resolve.sh` by replacing fragile test patterns with more robust alternatives.

The changes focus on replacing direct `rev-parse` command substitutions with shell variables and test helpers like `test_cmp_rev` and `test_write_lines`. This makes test failures more debuggable by showing expected vs. actual output when comparisons fail. The patch also uses variables to store revision IDs rather than repeatedly calling `rev-parse`, which is both more efficient and clearer.

Eric Sunshine, who has been reviewing this series throughout its iterations, gives his final approval with a Reviewed-by tag, indicating the technical concerns have been addressed. The changes are purely about test infrastructure - they don't modify the actual notes merge behavior being tested, just how the tests verify it.

This appears to be the final version of a well-executed test modernization effort. The author (Francesco Paparatto) has been responsive to review feedback across multiple versions, and the changes follow Git's established patterns for making tests more robust. With Eric's approval, this seems ready for integration.