This is a bugfix patch for the `git history reword` command, part of the ongoing effort to improve memory safety in Git's codebase. 

René Scharfe identifies a potential crash scenario where `cmd_history_reword()` could attempt to free uninitialized memory if given invalid input. The patch simply initializes the `rev_info` struct to zero, preventing any undefined behavior during cleanup. This is a small but important fix that follows René's typical pattern of sending precise, focused patches that address subtle corner cases.

The change is minimal (a one-line diff) but meaningful, demonstrating the kind of careful attention to initialization and cleanup that helps prevent memory-related bugs. Given René's reputation for thoroughness and the clear explanation of the issue, this patch appears ready for merging with minimal discussion needed.