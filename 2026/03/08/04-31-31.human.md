A const-correctness fix for the patch-id comparison function. This small patch addresses an 8-year-old NEEDSWORK comment in `patch-ids.c` by properly handling const correctness in the `patch_id_neq()` function. The change involves adding const qualifiers to the patch_id struct pointers and using explicit casting when passing object IDs to `commit_patch_id()`.

The patch is straightforward and well-contained, touching only the one function. It follows the pattern of similar const-correctness fixes that have been made throughout the codebase. The commit message clearly explains the issue and solution, though it could perhaps benefit from mentioning why this particular case matters (likely to prevent accidental modification of what should be read-only data).

This appears to be a solid, uncontroversial cleanup patch that removes a longstanding TODO item. The change is mechanical in nature and unlikely to require much discussion before being picked up.