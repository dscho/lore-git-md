Const-correctness documentation patch for the patch-id subsystem. This is a follow-up to earlier discussion about resolving the tension between hashmap API requirements (which demand const parameters) and the lazy initialization pattern used in patch ID computation (which requires mutation).

Tian Yuchen's v2 patch addresses Junio Hamano's critique of the initial approach by explicitly documenting why the const-cast is necessary rather than trying to hide it. The patch removes an 8-year-old NEEDSWORK comment and replaces it with a detailed explanation of the design trade-off: maintaining the lazy evaluation optimization requires violating strict const-correctness, but pre-computing all patch IDs for true const-correctness would incur unacceptable performance costs.

The change is focused on `patch-ids.c` where the `patch_id_neq()` function lives. The new comment explains the intentional const-cast using `container_of()` and acknowledges this as a conscious choice between API purity and performance. This appears to be the pragmatic solution that emerged from the thread's discussion, favoring documentation of existing behavior over more invasive architectural changes.

The patch is small and well-contained, directly addressing the core issue identified in the thread. It represents a reasonable compromise position that preserves the existing performance characteristics while making the const-cast operation explicit and justified in the documentation. The approach seems likely to be acceptable given the discussion trajectory, as it provides clarity without requiring structural changes to the patch-id or hashmap subsystems.