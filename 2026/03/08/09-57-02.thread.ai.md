Thread identity: bugfix for `git history reword` command addressing memory safety issue in argument validation path.

Core problem: `cmd_history_reword()` could pass an uninitialized `struct rev_info` to `release_revisions()` if argument validation fails, leading to undefined behavior during cleanup.

Current status: patch v1 proposes zero-initializing the `rev_info` struct at declaration to ensure safe cleanup in all paths. Patrick Steinhardt has reviewed and confirmed the approach is correct.

Key technical details:
- File: `builtin/history.c`
- Function: `cmd_history_reword()`
- Change: adds `struct rev_info rev = {0};` initialization
- Impact: prevents potential memory corruption when argument validation fails
- Related command: `git history reword` argument processing
- Safety: ensures proper release_revisions() behavior in all code paths

Participant positions:
- René Scharfe identified and fixed the issue with characteristic precision
- Patrick Steinhardt reviewed and confirmed the correctness of the fix
- No objections or requests for changes raised

The patch remains minimal and focused, typical of René's code hygiene contributions. It doesn't affect functionality but makes error handling more robust. The change is contained to the history command implementation and appears ready for merging based on positive review feedback.