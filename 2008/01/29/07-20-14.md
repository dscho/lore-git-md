# Re: [RFH/PATCH] prefix_path(): disallow absolute paths

| Header | Value |
|--------|-------|
| **From** | Johannes Sixt <j.sixt@viscovery.net> |
| **To** | Junio C Hamano <gitster@pobox.com> |
| **Date** | 2008-01-29T08:20:14+01:00 |
| **Message-ID** | [479ED3AE.5000403@viscovery.net](https://lore.kernel.org/git/479ED3AE.5000403@viscovery.net) |

---

**Thread**: [thread](./../23/15-14-52.thread.md#:~:text=2008/01/29/07-20-14)

Junio C Hamano schrieb:
> ```diff
> +static int sanitary_path_copy(char *dst, const char *src)
>  {
> -	const char *orig = path;
> +	char *dst0 = dst;
> +
> +	if (*src == '/') {
> +		*dst++ = '/';
> +		while (*src == '/')
> +			src++;
> +	}
> ```
>


Advance notice: In this function, tests of the kind *src == '/' need to be
turned into is_dir_sep(*src) when we port to Windows.

> ```diff
> +		/* copy up to the next '/', and eat all '/' */
> +		while ((c = *src++) != '\0' && c != '/')
> +			*dst++ = c;
>  		if (c == '/') {
> -			path += 2;
> -			continue;
> -		}
> -		if (c != '.')
> +			*dst++ = c;
> ```
>


			*dst++ = '/';

will be needed on Windows to sanitize all is_dir_sep(c) to '/'.

> ```diff
> +			while (c == '/')
> +				c = *src++;
> +			src--;
> +		} else if (!c)
>  			break;
> ```
>

...
> ```diff
> +const char *prefix_path(const char *prefix, int len, const char *path)
> +{
> +	const char *orig = path;
> +	char *sanitized = xmalloc(len + strlen(path) + 1);
> +	if (*orig == '/')
> ```
>


	if (is_absolute_path(*orig))

> ```diff
> +		strcpy(sanitized, path);
> +	else {
> +		if (len)
> +			memcpy(sanitized, prefix, len);
> +		strcpy(sanitized + len, path);		
>  	}
> -	return path;
> +	if (sanitary_path_copy(sanitized, sanitized))
> +		goto error_out;
> +	if (*orig == '/') {
> ```
>


Ditto.

> ```diff
> +		const char *work_tree = get_git_work_tree();
> +		size_t len = strlen(work_tree);
> +		if (strncmp(sanitized, work_tree, len) ||
> +		    (sanitized[len] != '\0' && sanitized[len] != '/')) {
> +		error_out:
> +			error("'%s' is outside repository", orig);
> +			free(sanitized);
> +			return NULL;
> +		}
> +	}
> +	return sanitized;
>  }
> ```
>


I appreciate this new sanitary_copy_path() because I expect that we will
need at least one less #ifdef __MINGW32__/#endif compared to our current
Windows port.

-- Hannes


