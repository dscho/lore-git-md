# [PATCH 10/12] revision.c: handle tag->tagged == NULL

| Header | Value |
|--------|-------|
| **From** | Martin Koegler <mkoegler@auto.tuwien.ac.at> |
| **To** | Junio C Hamano <gitster@pobox.com> |
| **Date** | 2008-02-18T21:48:01+01:00 |
| **Message-ID** | [12033676841373-git-send-email-mkoegler@auto.tuwien.ac.at](https://lore.kernel.org/git/12033676841373-git-send-email-mkoegler@auto.tuwien.ac.at) |

---

**Thread**: [thread](./20-47-52.thread.md#:~:text=2008/02/18/20-48-01)

Signed-off-by: Martin Koegler <mkoegler@auto.tuwien.ac.at>
---
```
 revision.c |    4 ++++
 1 files changed, 4 insertions(+), 0 deletions(-)

```

```diff
diff --git a/revision.c b/revision.c
index 484e5e7..b1aebf8 100644
--- a/revision.c
+++ b/revision.c
@@ -177,6 +177,8 @@ static struct commit *handle_commit(struct rev_info *revs, struct object *object
 		struct tag *tag = (struct tag *) object;
 		if (revs->tag_objects && !(flags & UNINTERESTING))
 			add_pending_object(revs, object, tag->tag);
+		if (!tag->tagged)
+			die("bad tag");
 		object = parse_object(tag->tagged->sha1);
 		if (!object)
 			die("bad object %s", sha1_to_hex(tag->tagged->sha1));
@@ -689,6 +691,8 @@ static int add_parents_only(struct rev_info *revs, const char *arg, int flags)
 		it = get_reference(revs, arg, sha1, 0);
 		if (it->type != OBJ_TAG)
 			break;
+		if (!((struct tag*)it)->tagged)
+			return 0;
 		hashcpy(sha1, ((struct tag*)it)->tagged->sha1);
 	}
 	if (it->type != OBJ_COMMIT)
-- 
```

1.5.4.1.g96b77


