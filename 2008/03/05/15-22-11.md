# Re: [PATCH] Add compat/vsnprintf.c for systems that returns -1 on maxsize reached

| Header | Value |
|--------|-------|
| **From** | Johannes Sixt <j.sixt@viscovery.net> |
| **To** | Michal Rokos <michal.rokos@nextsoft.cz> |
| **Date** | 2008-03-05T16:22:11+01:00 |
| **Message-ID** | [47CEBAA3.5060109@viscovery.net](https://lore.kernel.org/git/47CEBAA3.5060109@viscovery.net) |

---

**Thread**: [thread](./../04/13-59-28.thread.md#:~:text=2008/03/05/15-22-11)

Michal Rokos schrieb:
> Thank you for your comments; is this better?


Better, but still not there. See below. The configure test looks fine, but
I can't test it.

Finally, please make this a proper patch with Signed-off-by for Junio to
pick up.

> ```diff
> +#undef vsnprintf
> +int git_vsnprintf(char *str, size_t maxsize, const char *format, va_list ap)
> +{
> +	char *s;
> +
> +	int ret = vsnprintf(str, maxsize, format, ap);
> +	if (ret != -1 )
> +		return ret;
> +
> +	s = NULL;
> +
> ```
>


What if maxsize == 0? Insert here:

```
	if (maxsize < 250)
		maxsize = 250;

```

> ```diff
> +	while (ret == -1) {
> +		maxsize *= 4;
> +		str = realloc(s, maxsize);
> +		if (! str) {
> +			free(s);
> +			return -1;
> +		}
> ```
>


ret == -1 at this time, so:

```
		if (!str)
			break;

```

Hm?

> ```diff
> +		s = str;
> +		ret = vsnprintf(str, maxsize, format, ap);
> +	}
> +	free(s);
> +	return ret;
> +}
> ```
>


-- Hannes



