# Re: [PATCH] merge-recursive: do not look at the index during recursive merge

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | git@vger.kernel.org |
| **Date** | 2018-01-09T10:25:36-08:00 |
| **Message-ID** | [xmqq373e7uu7.fsf@gitster.mtv.corp.google.com](https://lore.kernel.org/git/xmqq373e7uu7.fsf@gitster.mtv.corp.google.com) |

---

**Thread**: [thread](./../../../2017/12/20/11-43-10.thread.md#:~:text=2018/01/09/18-25-36)

Junio C Hamano <gitster@pobox.com> writes:

> ```diff
> + ...
> +	test_tick &&
> +	git commit --allow-empty -m "O2" &&
> +	O1=$(git rev-parse HEAD) &&
> +
> +	git reset --hard $O0 &&
> +	test_tick &&
> +	git commit --allow-empty -m "O2" &&
> +	O2=$(git rev-parse HEAD) &&
> ```
>


Does not affect the validity of the test at all, but the log message
of the $O1 should be made with -m "O1", not with -m "O2".  That fix
will be in the version I'll be queuing.

> ```diff
> +
> +	test_tick &&
> +	git merge -s ours $O1 &&
> +	B=$(git rev-parse HEAD) &&
> +
> +	git reset --hard $O1 &&
> +	test_tick &&
> +	git merge -s ours $O2 &&
> +	A=$(git rev-parse HEAD) &&
> +
> +	git merge $B
> +'
> +
>  test_done
> ```
>



