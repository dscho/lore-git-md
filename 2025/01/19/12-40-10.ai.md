This is a final review discussion for the ref-filter refactoring series that eliminates global state management.

Jeff King confirms he had prepared similar patches to René Scharfe's v2 series before seeing them, including addressing the memory leak in `u.base.name`. He agrees with René's approach of consolidating the cleanup into patch 3, though notes this means future developers would need to rediscover all the `_clear()` call sites if the functionality is ever needed again. Jeff also points out that `ls-remote` with `--format=%(is-base:foo)` would have leaked memory in the original implementation.

The exchange shows the series is now complete with all technical concerns addressed, including proper memory management. Jeff is satisfied with the implementation and suggests using a "Helped-by" trailer for attribution rather than stacking patches. This marks the final technical discussion before the series can be merged, confirming all edge cases (including previously unnoticed leaks) have been handled in the atom-based storage approach.