This is a response to a help request about removing unwanted commits from Git history. Reto provides a straightforward answer to Tim Tassonis's question about automated history rewriting, pointing to `git-filter-repo` as the recommended solution.

The response is brief but directly addresses the core need - an automated way to excise problematic commits (in this case, a large firmware blob) from repository history. The recommendation of `git-filter-repo` is appropriate as it's the modern replacement for older tools like `filter-branch` and is specifically designed for this type of history rewriting operation.

The answer assumes some familiarity with Git tooling but doesn't dive into implementation details, which would be the next logical step in the conversation. The response is technically correct and helpful for someone at Tim's apparent level of Git knowledge.