This is the final patch in René Scharfe's three-part series completing the ref-filter refactoring to eliminate global state management for ahead-behind and is-base calculations. The patch removes the last remaining global state (the `is_base_tips` string list) by moving commit references into the atom structs, mirroring the approach taken for ahead-behind functionality in earlier patches.

Key technical changes include:
- Adds `name` field to `base` struct alongside existing `commit` field to store both the committish string and parsed commit
- Updates `is_base_atom_parser()` to store parsed commits directly in atom structs rather than the global list
- Modifies `filter_is_base()` to iterate through atom structs instead of the global list
- Removes final cleanup of `is_base_tips` from `ref_array_clear()`
- Updates the iterative format check to consider is-base atoms alongside ahead-behind
- Maintains identical behavior while eliminating all global state management

The implementation now fully matches the approved hybrid architecture where commit references are stored directly in atom structs rather than being managed globally. This completes the series' goal of removing all shared state between formatting and sorting operations while preserving functionality. With all three parts now implementing the consistent pattern and all global state eliminated, the series appears ready for final review and merging.