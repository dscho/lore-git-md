Bugfix patch improving pack header parsing in `index-pack` and `unpack-objects`. This is the final patch in Jeff King's series addressing sparc64 alignment issues, now focusing on code quality improvements.

The change replaces a hardcoded 14-byte skip with `skip_prefix()` when handling the `--pack_header` option, making the code more maintainable. It also enhances error messages by including the full option name ("bad --pack_header") rather than just the argument value, which helps translators and debugging. The modification touches both `index-pack.c` and `unpack-objects.c` identically.

This is a small but thoughtful cleanup that follows through on the earlier architectural work to centralize pack header parsing. While not fixing any active bug, it improves code clarity and error reporting as part of the larger alignment-safety effort. The patch appears ready for inclusion, having evolved through review and now completing the series with these final polish touches.