This is the final patch in René Scharfe's three-part series eliminating global state management in Git's ref-filter subsystem. The patch completes the transition to storing commit references directly in atom structs by removing the last remaining global `is_base_tips` string list and moving all is-base functionality into the atom-based storage pattern.

The changes are focused and mechanical, following the same pattern established in earlier patches: commit references are now stored in the `base` field of `used_atom` structs, with corresponding updates to parsing and filtering logic. The patch removes the final cleanup of global state from `ref_array_clear()` and updates `filter_is_base()` to iterate through atoms rather than the now-removed global list.

This appears to be a clean conclusion to the series, with all technical goals achieved - the code now consistently uses atom-based storage for both ahead-behind and is-base functionality while maintaining identical behavior. The implementation matches the architectural direction previously approved by maintainers, and with this final piece in place, the series looks ready for merging.