New participant joins batch size discussion in backfill series.

Jean-Noël Avila provides a minor documentation formatting suggestion for patch 3/5 of the `git backfill` series, specifically about the `--batch-size` option description. The suggestion is to use backticks for consistent formatting of the option name in the documentation (`--batch-size=<n>` rather than plain text). This follows up on earlier discussions about naming consistency for this parameter (previously noted as needing updates from `--batch-size` to `--min-batch-size` in three documentation spots).

The email doesn't address the larger ongoing discussion about marking the command as experimental or the implementation details of the batch sizing functionality itself. It's a focused documentation nit that aligns with the project's style guidelines for man page formatting. The suggestion appears technically correct and non-controversial, though it doesn't resolve the open question about whether the parameter name should ultimately be `--batch-size` or `--min-batch-size` as previously discussed in the thread.