Bugfix and code quality improvement in the pack handling series. This final patch in Jeff King's series replaces manual string offset calculations with `skip_prefix()` in both `index-pack` and `unpack-objects` when processing the `--pack_header` option. The change eliminates a magic number (14) that was previously used to skip past the option prefix, making the code more maintainable. 

The functional behavior remains identical, but the error message is slightly modified to include the full `--pack_header` prefix for better context in translations. The patch touches the same two commands that were central to the original sparc64 SIGBUS crash report, though this particular change is more on code hygiene than the alignment fixes that dominated earlier patches in the series.

This appears to be a straightforward cleanup that follows naturally from the earlier architectural work to centralize pack header parsing. The change is small (6 lines modified in each file) and focused, completing the series' effort to make the pack handling code more robust and maintainable. Since this is the final patch in a well-reviewed series that has already addressed the core alignment issues, it seems likely to be accepted as a final polish.