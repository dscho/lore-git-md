This is a refactoring patch in Usman Akinyemi's series adding OS version reporting to Git's protocol. It extracts string sanitization logic from `git_user_agent_sanitized()` into a new `redact_non_printables()` helper function, preparing for reuse in upcoming patches for OS version string handling.

The change is straightforward - it moves the existing character sanitization code (which replaces non-printable characters and spaces with dots) into a dedicated function while maintaining identical behavior. The patch also improves memory handling by using `strbuf_detach()` rather than directly accessing the strbuf buffer.

This appears to be a clean, focused change that follows good refactoring practices. The commit message clearly explains the motivation (future reuse) and the implementation matches that goal. As part of the larger OS version capability series, this preparatory refactoring helps keep the subsequent patches cleaner by centralizing the string sanitization logic.