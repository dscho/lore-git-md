CI / build system patch aligning Meson's Perl version requirement with the main Makefile's 5.26.0 minimum. This is the first of two patches in Peter Oliver's v2 series, following up on review feedback from Patrick Steinhardt.

The patch makes a straightforward change to meson.build, updating the Perl version check from 5.8.1 to 5.26.0 to match the requirement established in commit 702d8c1f3b. The change is minimal (1 line modified) and maintains the same conditional logic where Perl is only required when explicitly enabled via build configuration. The commit message references the prior Makefile change as justification, though doesn't restate its rationale. The implementation appears technically sound and uncontroversial, being a direct alignment between build systems rather than introducing new requirements.

Key technical details for future reference: modifies meson.build line 755, maintains the perl_required conditional behavior, and follows the same version string format (">=5.26.0") as used elsewhere in the build system. The change is purely build-time with no runtime implications. The second patch in the series (not shown here) handles version detection robustness improvements mentioned in the thread summary.