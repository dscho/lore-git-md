This is a refactoring patch that's part of Usman Akinyemi's v3 series adding OS version capability to Git's protocol. The patch simplifies character sanitization in the user agent string handling by replacing manual ASCII range checks with the standard `isprint()` function, while explicitly preserving space character handling.

The change modifies `version.c` to use `isprint()` for printable character detection in `git_user_agent_sanitized()`, which will be used for OS version string sanitization in later patches. The implementation maintains the existing behavior of replacing non-printable characters (including spaces) with dots, but does so in a more maintainable way by leveraging the standard library function.

This is one of several preparatory changes in the series that address prior review feedback, specifically implementing the agreed-upon string sanitization approach using `isprint()`. The patch follows the thread's established direction of moving platform-specific code to the `compat/` system and standardizing on secure implementations. The change is straightforward and uncontroversial, with clear test coverage implied by the series' overall test infrastructure improvements.