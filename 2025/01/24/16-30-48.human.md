Build system patch aligning Meson's Perl version requirement with Git's main Makefile. This is the first of two patches in Peter Oliver's v2 series standardizing Perl version requirements across build systems.

The patch updates Meson's minimum Perl version from 5.8.1 to 5.26.0, matching the requirement established in the main Makefile by commit 702d8c1f3b last October. The change is straightforward - a one-line version bump in meson.build that ensures consistent Perl version checking between build systems.

This appears to be a well-justified change that closes a minor inconsistency between parallel build systems. The patch follows the established pattern from the Makefile change and includes a clear commit message linking back to the original version bump. The implementation is minimal and focused, touching only the version requirement line while preserving all other Perl-related build logic.

As noted in the thread summary, this version alignment has already gone through review iteration, with Peter incorporating feedback on documentation and implementation details. The patch seems ready for consideration, assuming no new issues emerge in final review.