This is the final patch in a refactoring series moving refspec-related functions from remote.c to refspec.c. The patch relocates `apply_refspecs()` and `apply_negative_refspecs()`, completing the centralization of refspec logic. 

The changes follow the established pattern as previous patches in the series: functions are moved verbatim with their declarations added to refspec.h and removed from remote.h. `apply_negative_refspecs()` filters refs matching negative refspecs from a linked list, and `apply_refspecs()` maps ref names using the refspec rules. Both functions maintain their existing behavior and continue to use helper functions like `omit_name_by_refspec()` and `query_refspecs()` that were moved in earlier patches.

The patch demonstrates the same careful approach seen in the series - symmetric header updates, preserved functionality, and no behavioral changes. With this final relocation, all major refspec operations now reside in refspec.c, achieving the series' goal of better code organization. The changes are straightforward and appear ready for inclusion, completing the logical grouping of refspec functionality that began in patches 1 and 2.