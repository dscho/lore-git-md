Refactoring patch in the promisor-remote/LOP series, extracting string sanitization logic. This is part of Christian Couder's ongoing work to improve large object handling in Git, specifically focusing on protocol safety and code quality improvements.

The patch extracts the string sanitization logic from `git_user_agent_sanitized()` into a new `redact_non_printables()` helper function. The new function handles trimming and replacing non-printable characters (including spaces) with dots, maintaining the same behavior but making it reusable. The change also switches to using `strbuf_detach()` for cleaner memory management when storing the sanitized string.

Key technical details:
- New static function `redact_non_printables()` in version.c
- Replaces manual ASCII range checks with `isprint()`
- Preserves special handling of space characters
- Changes memory management from direct buffer access to `strbuf_detach()`
- Part of broader string handling standardization in the LOP series

This appears to be a straightforward code organization improvement with no behavior changes, building on Usman Akinyemi's earlier string handling contributions. The patch follows good practices with a clear commit message explaining both the what and why of the change.