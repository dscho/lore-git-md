Refactoring patch moving refspec matching functions to their logical home.

This is the first of three patches in a series that reorganizes refspec-related code from `remote.c` into `refspec.c`. The patch relocates three functions that handle refspec pattern matching: `omit_name_by_refspec()`, `refspec_match()`, and `match_name_with_pattern()`. The changes are purely organizational - the functions are moved verbatim with no behavioral modifications.

The relocation makes architectural sense by grouping refspec logic together while keeping remote-specific code separate. The patch includes proper header file updates to expose the moved functions. Based on the thread context, this version has added comprehensive documentation that was requested in previous reviews.

The changes are low-risk since they only move existing code while maintaining all functionality. The patch appears well-crafted with clean diffs that show symmetric additions and deletions. This looks like a straightforward improvement to Git's code organization that should be uncontroversial to merge.