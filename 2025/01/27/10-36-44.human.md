Refactoring patch completing a series to centralize refspec logic. This final patch in the v2 series moves the remaining refspec application functions (`apply_refspecs()` and `apply_negative_refspecs()`) from remote.c to refspec.c, continuing the effort to consolidate all refspec-related code in one place. The changes are purely organizational - the functions are relocated verbatim with no behavioral modifications. 

The patch follows the same careful approach seen in previous parts of the series: header files are properly updated (adding declarations to refspec.h while removing them from remote.h), and all existing functionality including memory management patterns is preserved. The `apply_negative_refspecs()` function in particular handles the important task of filtering refs based on negative refspecs, while `apply_refspecs()` maps ref names according to the refspec rules.

With this patch, the series appears complete, having systematically moved all targeted refspec functions while maintaining exact behavior. The changes are low-risk and well-contained, making this a straightforward organizational improvement to the codebase.