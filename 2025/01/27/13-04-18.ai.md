This is part 10 of the reftable library refactoring series to remove Git dependencies. The patch removes the last remaining use of Git's `ARRAY_SIZE()` macro in the reftable reader code, replacing it with a direct calculation of array length using `sizeof(sections) / sizeof(*sections)`.

The change is straightforward and mechanical, affecting only a single loop in `reftable_reader_print_blocks()` that iterates through the `sections` array. This continues the series' pattern of systematically eliminating Git-specific macros from the reftable codebase to achieve full independence. The modification maintains identical functionality while reducing the dependency surface area.

This patch represents another incremental step in the larger effort to make reftable a standalone library. The change is small and focused, touching only one file with a simple substitution that preserves the existing behavior. It follows the established pattern seen in previous patches of replacing Git utilities with direct implementations or standard C constructs.