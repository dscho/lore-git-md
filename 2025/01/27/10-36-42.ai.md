Refactoring patch moving refspec matching functions to their logical home.

This is the first of three patches in a series that relocates refspec-related code from remote.c to refspec.c, part of ongoing efforts to better organize Git's codebase by logical components. The patch moves three key functions that handle refspec pattern matching: `omit_name_by_refspec()`, `refspec_match()`, and `match_name_with_pattern()`. 

The changes are purely organizational - the functions are copied verbatim to refspec.c while being removed from remote.c, with no behavioral modifications. The patch includes adding the moved functions' declarations to refspec.h and removing them from remote.h, maintaining proper header file organization. 

The commit message clearly explains the motivation - keeping refspec logic centralized in refspec.c while leaving remote-specific code in remote.c. The implementation appears careful and methodical, with a clean diff that shows symmetric additions and deletions. This follows the thread's established pattern of moving code while adding documentation (though the docstrings for these functions appear in later patches).

As noted in the thread summary, this v2 iteration has focused on improving documentation based on prior feedback, though those changes aren't visible in this particular patch. The changes are low-risk since they only relocate existing, well-understood code. The patch appears ready for consideration as part of the broader refactoring effort.