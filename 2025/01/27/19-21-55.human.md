Bugfix in the reftable refactoring series, converting a BUG() to proper error handling. This patch (6/19 in the series) changes `reftable_record_cmp()` to return an error code when comparing records of differing types, rather than aborting with a BUG(). This is part of the broader effort to make the reftable library more standalone by removing Git-specific error handling.

Justin Tobler notes a typo in the commit message ("refatble" instead of "reftable") but doesn't raise any substantive concerns about the change itself. The patch appears straightforward - it's converting what was previously a hard failure case into something the caller can handle gracefully, which aligns with the series' goal of making reftable more suitable as a standalone library.

This is one of the more mechanical changes in the series, focused on error handling rather than the deeper architectural questions (like the I/O utilities or parallel implementations) that have been raised elsewhere in the thread. The change seems uncontroversial and well-justified by the series' goals.