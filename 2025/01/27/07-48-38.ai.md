This is a technical discussion about Windows reftable backend compatibility, specifically addressing file unlinking behavior. Patrick Steinhardt responds to Johannes Sixt's alternative proposals for handling file-in-use errors on Windows systems.

The exchange clarifies a key misunderstanding about where POSIX unlink behavior is actually needed - Steinhardt explains it's Git's core that expects POSIX semantics, not the reftable library which can tolerate failed deletions. This distinction is crucial for the architectural debate about where to implement solutions.

Steinhardt expresses reservations about Sixt's proposed `CreateFileW` approach due to uncertainty about its behavior with files held by other processes and concerns about modifying the widely-used `unlink()` implementation. He seeks confirmation about whether their positions align regarding disabling POSIX emulation specifically for reftable cases.

The discussion touches on several technical specifics:
- Windows API behavior (`CreateFileW` with `FILE_FLAG_DELETE_ON_CLOSE`)
- The reftable library's tolerance for failed deletions
- Git's general expectations around `unlink()` behavior
- The architectural question of modifying platform layer vs. adapting reftable code

This email moves the conversation forward by clarifying requirements and constraints, though it doesn't propose new solutions. The thread remains in a state where multiple viable approaches exist but need further technical evaluation and consensus-building.