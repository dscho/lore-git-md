This is part 4 of the ongoing reftable refactoring series to remove Git-specific dependencies. The patch replaces the `COPY_ARRAY()` macro with direct `memcpy()` operations in `reftable/record.c`, specifically in the object record copying logic. 

The change is straightforward - it converts one instance of `COPY_ARRAY()` to an equivalent `memcpy()` call with the same safety check for zero-length arrays. The modification maintains identical behavior while eliminating a dependency on Git's array utility macros. This follows the established pattern seen in earlier patches of the series, where Git-specific helpers are systematically replaced with standard C/POSIX equivalents.

The patch touches a single file and makes a minimal change focused on the reftable record handling subsystem. Like previous patches in the series, it's a mechanical conversion that preserves all existing functionality while progressing toward the goal of making reftable fully independent of Git's core infrastructure. The change is well-contained and appears to be a safe transformation of array copying logic.