This is a follow-up question in a thread about type safety improvements in Git's commit parsing code, specifically addressing MSVC compiler warnings. The thread has been systematically converting integer types in commit.c to be more type-safe, particularly for string lengths and positions.

Patrick Steinhardt points out that in patch 4/4 (which updated `find_invalid_utf8()` to use `size_t` for the length parameter), the internal `offset` variable was not similarly updated from `int` to `size_t`. This appears to be an oversight in the original patch, as the variable is used to track positions within the same string buffer.

The observation is technically sound - if the function now takes a `size_t` length parameter, the offset tracking should logically use a compatible type to avoid potential type mismatches. This is consistent with the series' goal of eliminating MSVC warnings about integer truncation.

The comment is brief but raises a valid point that would likely be addressed in a v4 of the patch series. No other feedback or discussion is included in this email - it's a focused technical observation about maintaining type consistency within the function.