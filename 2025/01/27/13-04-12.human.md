This is part 4 of a 19-patch series refactoring the reftable library to remove Git-specific dependencies. The patch replaces the `COPY_ARRAY()` macro with direct `memcpy()` operations in the reftable record handling code.

The change is straightforward - it converts one instance of `COPY_ARRAY()` to an equivalent `memcpy()` call with proper bounds checking. This continues the series' pattern of removing Git-specific utilities in favor of standard C operations. The modification appears in the object record copying logic, which handles offset arrays when duplicating reftable records.

Like the previous patches in the series, this is a mechanical change that maintains the same behavior while reducing coupling to Git internals. The patch is small and focused, touching just 3 lines to make this isolated substitution. It follows the established pattern of the series by removing another Git-specific helper while preserving all functionality.