This is part 12 of a 19-patch series decoupling the reftable library from Git's core infrastructure. The patch continues the systematic replacement of Git-specific utilities with direct POSIX equivalents.

The change replaces `sleep_millisec()` with a Git wrapper function) with a direct call to `poll()` in reftable/stack.c's retry logic. The modification occurs in the lock acquisition retry path where the code implements exponential backoff with random jitter. The new implementation uses `poll(NULL, 0, delay)` which is functionally equivalent to the original `sleep_millisec()` call but removes another Git dependency.

This follows the series' established pattern of replacing Git helpers with their underlying POSIX implementations while maintaining identical behavior. The change is minimal (one line modified) and mechanical, similar to previous patches that replaced I/O operations, memory mapping, and random number generation. The patch maintains the same delay calculation and retry behavior, just using a more direct system call.

Key technical details for future reference:
- Modified file: reftable/stack.c
- Replaced function: `sleep_millisec()`
- New implementation: `poll(NULL, 0, delay)`
- Context: Used in lock acquisition retry logic with exponential backoff
- Behavior preserved: delay calculation and jitter remain unchanged
- Series position: Continues dependency removal pattern established in earlier patches