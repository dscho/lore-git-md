This is part 17/19 of the reftable decoupling series, focusing on platform compatibility. The patch splits POSIX-emulation functionality from Git-specific utilities in git-compat-util.h into a new compat/posix.h header, continuing the effort to make reftable independent of Git's core infrastructure.

Justin Tobler notes a typo ("depedning" -> "depending") in the commit message but raises no technical objections to the change. The patch itself is a straightforward refactoring that moves POSIX-related declarations (like `gettimeofday()` wrappers and `struct timeval`) to the new home while leaving Git-specific utilities in git-compat-util.h. This separation allows reftable to include only the POSIX compatibility layer without pulling in Git-specific dependencies.

The change exemplifies the series' architectural approach of methodically untangling dependencies - here addressing the platform compatibility layer after previous patches handled I/O utilities and error handling. While the high-level discussion about long-term maintenance of parallel implementations continues (as noted in the thread summary), this particular patch appears uncontroversial and well-contained.

Key technical details for future reference: the new compat/posix.h contains POSIX-emulating functionality previously mixed with Git utilities, while git-compat-util.h retains Git-specific helpers. The split enables reftable to use POSIX compatibility without inheriting Git dependencies, though it does create some code duplication that may need reconciliation if Git's POSIX emulation evolves.