This is a refactoring patch from Usman Akinyemi (with Christian Couder mentoring) that's part of the v4 promisor-remote/Large Object Promisor series. It replaces manual ASCII character range checks in the user agent sanitization code with the more readable `isprint()` function from `sane-ctype.h`.

The change makes the code's intent clearer while maintaining identical behavior - it still replaces non-printable characters and spaces with dots in the user agent string. The patch is small and focused, touching just one file with a simple logic change that's well-explained in the commit message.

As part of the larger series, this appears to be a preparatory cleanup that helps standardize character handling before introducing new promisor-remote functionality. The change itself is straightforward and unlikely to be controversial, though reviewers might want to verify the exact equivalence between the old and new checks.