This is a follow-up discussion in the cruft pack management series, specifically addressing the interaction between `--prune=now` and `--expire-to` options in `git gc`. ZheNing Hu responds to Jeff King's review of their earlier patch, agreeing that the current implementation correctly handles cases where both `--cruft` and `--expire-to` are used together, while leaving `--prune=now` behavior unchanged when `--expire-to` is used without `--cruft`.

The discussion reveals that `--expire-to` is currently only meaningful with `--cruft` in `git repack`, which justifies the double safeguard in the code. ZheNing also examines Taylor Blau's related patch and confirms it doesn't change this fundamental relationship. Both contributors now agree the fix might be better folded into the first patch of the series to avoid introducing a temporary buggy state.

The technical details remain sound, and the conversation primarily focuses on patch organization rather than functionality. This appears to be a minor refinement to an otherwise well-received feature addition, with no outstanding objections to the approach itself.