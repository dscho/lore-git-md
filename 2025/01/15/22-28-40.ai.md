This is a technical discussion about the ongoing standardization of Git's output behavior for help messages, specifically addressing edge cases in the implementation.

Jeff King (Peff) responds to Junio Hamano's observation about another class of help message handlers that bypass the parse-options infrastructure entirely. These cases use direct calls to `usage()` after manually checking for "-h" flags, and currently write to stderr via `vreportf()`. This represents a parallel track of conversions needed beyond the parse-options cases already being addressed.

The email highlights the complexity of fully standardizing help output behavior, as the problem spans multiple code paths with different implementations. The `vreportf()` dependency in particular will require refactoring to achieve consistent stdout output. This comes on top of existing challenges like the dual-purpose `PARSE_OPT_HELP` flag and exit code inconsistencies noted earlier in the thread.

Key technical details for future reference:
- Direct `usage()` callers represent a distinct conversion path from parse-options users
- `vreportf()` hardcodes stderr output, requiring refactoring for consistency
- These cases are detected by manual "-h" checks rather than parse-options infrastructure
- The discussion suggests a new helper `show_usage_and_exit_if_asked()` may be needed
- This expands the standardization effort's scope beyond initial parse-options focus

The exchange underscores that while test infrastructure is now in place (`GIT_TEST_HELP_MUST_BE_STDOUT`), the implementation work involves multiple technical challenges across different parts of Git's codebase. The thread continues to uncover new edge cases requiring attention in the standardization effort.