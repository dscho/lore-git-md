This is a final clarification in the zlib-ng performance optimization series, specifically addressing a tangential tooling discussion about patch generation format. Maintainer Junio Hamano confirms Konstantin Ryabitsev's observation that the `--full-index` flag is unnecessary when generating binary patches with `git format-patch`, since the `--binary` option already ensures full object names are included. 

The exchange resolves a minor side discussion that emerged during review about patch generation tooling (specifically the `b4` tool's behavior), but doesn't impact the technical content of the zlib-ng series itself. Hamano's confirmation reinforces that the series' technical approach is sound, with the remaining work being purely about tooling configuration (adjusting `b4`'s patch generation defaults) which Patrick Steinhardt has indicated he'll address separately.

This email marks the resolution of the last open discussion point in the thread, leaving the comprehensive zlib-ng integration series - with its ~25% performance improvements and thorough compatibility layer - effectively ready for merging pending only the final mechanical adjustments to the patch submission toolchain.