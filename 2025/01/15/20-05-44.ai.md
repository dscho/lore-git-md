This is a build system patch completing the Rust bindings infrastructure work by adding Makefile integration for the `libgit-rs` and `libgit-sys` crates. As the final patch in the series, it provides two key capabilities:

1. New `libgitrs` and `libgitrs-sys` targets (plus their test variants) that allow building and testing the Rust crates through the main Git Makefile rather than requiring direct Cargo invocation. This maintains consistency with Git's existing build workflow.

2. An `INCLUDE_LIBGIT_RS` build flag that automatically builds and tests the Rust components as part of `make all`. The implementation handles symbol visibility properly by adding `-fvisibility=hidden` to CFLAGS when the flag is set, addressing a technical requirement noted in earlier patches.

The changes touch three files: the main Makefile gains the new targets and build flag logic, the test Makefile adds corresponding test targets, and the `build.rs` script is updated to use the new flag rather than hardcoding visibility settings. The patch maintains the established pattern where the Rust components remain in `contrib/` without affecting core Git functionality.

This appears to be a well-integrated conclusion to the Rust bindings series, providing the expected build system integration points while preserving the optional nature of the Rust components. The implementation follows Git's existing patterns for optional components (similar to the fuzz testing infrastructure) and properly coordinates with the earlier symbol visibility work.