This is a technical discussion about the implementation details of standardizing Git's help output behavior, specifically focusing on the proposed `show_usage_help()` helper function.

Jeff King (Peff) responds to Junio Hamano's implementation sketch for the new helper function that will handle `-h` help output consistently to stdout. The key points are:

1. **API Design Concern**: Peff agrees with Junio's unease about the function's dual nature (sometimes exiting, sometimes being a no-op), suggesting a more descriptive name like `check_help_option()` might better convey its behavior.

2. **Exit Code Clarification**: Peff notes that the current implementation would exit with code 129 (via parse-options machinery) rather than 0 as shown in Junio's example, and that this matches the test expectations in `t0012-help.sh`.

The discussion represents the fine-tuning phase of the implementation, where the high-level approach (standardizing help output to stdout via parse-options changes) has been agreed upon, but specific API details are being worked out. This email focuses narrowly on the function signature and behavior rather than introducing new concepts, maintaining the thread's progression toward a concrete solution.