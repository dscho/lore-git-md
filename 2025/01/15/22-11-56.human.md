This is a technical follow-up in the ongoing standardization of Git's help output behavior. Junio Hamano notes an additional class of cases that will need updating beyond the `parse-options` API changes already discussed: direct calls to `usage()` that check for `-h` explicitly. These exist outside the `parse-options` framework and will require a new helper function (`show_usage_and_exit_if_asked()`) to maintain consistent stdout behavior.

The email highlights the complexity of fully standardizing help output, as the current implementation spans multiple code paths with different historical behaviors. While the main `parse-options` conversion is progressing with Jeff King's safety checks, this reveals another layer of technical debt that needs addressing for complete consistency.

The thread has largely settled the architectural approach (stdout output, temporary retention of exit code 129) and is now working through implementation details. This email shows the maintainer identifying one of the remaining edge cases that will need handling before the standardization effort can be considered complete.