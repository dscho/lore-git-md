Implementation milestone in the Git help output standardization effort, focusing on edge cases in the parse-options API. Junio Hamano identifies an additional class of callers that need updating beyond the recently introduced `show_usage_help_and_exit_if_asked()` helper - those using direct `usage()` calls protected by `argc == 2 && !strcmp(argv[1], "-h")` checks. This reveals a subtle distinction in the codebase between commands using parse-options infrastructure versus those manually handling help requests.

The email highlights a technical debt issue where the historical PARSE_OPT_HELP flag serves dual purposes - both for explicit help requests (where exit code 0 might be preferable) and ambiguous option errors (where non-zero is correct). Jeff King had previously noted this as a potential future bug vector if help handling diverges. Junio's observation about direct `usage()` callers suggests the standardization work will require auditing not just parse-options users but also any commands with manual help handling.

This moves the thread from the recently settled v2 API design phase into implementation details, specifically around edge cases that need special handling. The sigh at the end suggests this may represent more work than initially anticipated, though the core architectural decisions remain unchanged. The thread continues to track the mechanical work needed to fully implement the stdout standardization policy across all Git commands.