This thread has progressed from identifying an inconsistency in Git's help output behavior to developing a systematic solution with maintainer approval. The original report noted that different Git subcommands handle `-h` differently: some output help on stdout (exiting with 0), others on stderr, and `git reflog -h` even exits non-zero. After initial debate about Unix conventions, Junio Hamano settled the philosophical question by endorsing stdout standardization for help output.

Kristoffer Haugsbakk's comprehensive audit quantified the scope: 40 builtins use stderr versus 102 using stdout for help text. Jeff King (Peff) contributed technical analysis and test infrastructure, tracing the `git branch -h` behavior to a specific commit and proposing a solution through the parse-options API. The latest development comes from Junio, who has implemented a new helper function `show_usage_help()` that properly handles `-h` requests by outputting to stdout while maintaining stderr for errors. The design intentionally makes blind conversions difficult by requiring argc/argv checking at each callsite, ensuring developers properly review each case. The function exits with code 0 for help requests (matching most existing behavior) while keeping error cases at 129.

The thread now stands at an implementation milestone, with the parse-options API extension ready for review before being applied to the 40 affected builtins. The remaining work involves finalizing the helper function's name (Jeff King had proposed `check_help_option()` as more descriptive) and sequencing the conversions, with test coverage in place to prevent regressions. This represents a well-executed progression from bug report through design to implementation, with clear maintainer guidance and careful technical consideration of the API changes.