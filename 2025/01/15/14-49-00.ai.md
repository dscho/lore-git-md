[Security] Follow-up discussion on ANSI escape sequence sanitization implementation details.

Phillip Wood raises two technical concerns about the final implementation of the sideband control character sanitization. First, he questions whether passing newline characters to `maybe_colorize_sideband()` is correct behavior, suggesting this might be a bug in the current code. Second, he points out that the current DEL character escaping produces invalid UTF-8 output (turning 0x7f into 0xbf) and proposes using "^?" as a more portable representation for the DEL character.

For the test suite, Wood suggests improving the verification of color sequence blocking by using `test_cmp()` rather than just checking for absence of "RED" in the output. This would catch cases where the test script fails to produce the expected color sequences in the first place, rather than just verifying they were blocked. The email doesn't challenge the overall approach but focuses on specific implementation details that could be improved before finalizing the security fix. These appear to be straightforward technical corrections rather than fundamental objections to the security model. The thread context shows this is part of final polishing for a security fix that has already undergone significant review, with the core approach (three-state config with color-specific validation) already agreed upon.