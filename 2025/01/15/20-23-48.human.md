This is the final patch in Jean-Noël Avila's documentation standardization series for the git-commit man page. It completes the effort by updating two supporting files (`i18n.txt` and `signoff-option.txt`) to match the new AsciiDoc formatting conventions established in earlier patches.

The changes are mechanical and consistent with the series' approach: wrapping Git commands in backticks (like `` `git commit` ``) and standardizing option formatting (converting `--signoff` to `` `--signoff` ``). The patch also maintains the conditional inclusion system (the `ifdef::git-commit[]` blocks) while updating their contents to the new style.

At 12 lines total (6 additions, 6 deletions), this is a small but important capstone to the series, ensuring all related documentation follows the same presentation rules. The changes are purely about formatting - no technical content is altered. With this patch, the entire git-commit documentation now consistently uses the new AsciiDoc patterns for commands, options, and placeholders.

The series has progressed smoothly through its second iteration, with all previous patches already accepted. This final piece appears equally well-executed and should conclude the standardization effort for git-commit's documentation.