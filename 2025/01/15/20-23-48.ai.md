Documentation patch completing the git-commit manpage standardization series. This final patch updates two supporting files (`i18n.txt` and `signoff-option.txt`) to match the new AsciiDoc formatting conventions established in the previous patches. The changes are mechanical, applying the same patterns seen throughout the series: wrapping Git commands and options in backticks for consistency, while maintaining all technical content unchanged.

The `i18n.txt` modifications standardize the presentation of command references (`git commit`, `git log`, etc.) by converting single-quoted forms to backtick-wrapped format. The `signoff-option.txt` updates follow the same pattern for option flags (`-s`, `--signoff`), including the conditional `ifdef::git-commit[]` section that only appears in the git-commit context. Both files now align with the series' documentation guidelines while preserving their exact semantic meaning.

This concludes Jean-Noël Avila's 5-part standardization effort for git-commit documentation, which has systematically applied new formatting rules across the main man page, config documentation, and now these supporting files. The changes are purely presentational - no functional alterations or content rewrites - continuing the mechanical nature of the entire series. With all patches now submitted and no outstanding objections from reviewers, this appears ready for integration into the project's documentation standards.