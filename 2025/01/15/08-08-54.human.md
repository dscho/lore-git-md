This is a discussion about improving documentation for `git repack`'s `--expire-to` option. ZheNing Hu is responding to Jeff King's concerns about potential user confusion with the current behavior.

The thread reveals that `--expire-to` doesn't work as users might expect from its documentation. While the option suggests it takes a directory path, it actually expects a base filename pattern (similar to `pack-objects`), generating files with the given prefix rather than placing them in the specified directory. The current behavior creates files like `xxx.git/objects/pack-*.pack` when users might expect `xxx.git/objects/pack/pack-*.pack`.

ZheNing Hu confirms Jeff King's observation that the misleading documentation could cause problems, especially when users provide paths that don't end in "pack/". The discussion suggests the documentation should be clarified to better explain the actual behavior and requirements of the `--expire-to` parameter. This appears to be a straightforward documentation fix that would help prevent user confusion.