Bug report and implementation audit for Git help output behavior inconsistency, now with comprehensive data and maintainer-confirmed error case boundary. The thread began with Jonas Konrad's report of divergent behaviors in Git 2.48.1: `git -h` (stdout, exit 0) vs `git branch -h` (stderr) vs `git reflog -h` (stdout, non-zero). After initial debate between Jonas (stdout consistency) and Matěj Cepl (stderr as UNIX convention), maintainer Junio Hamano sided with stdout standardization, prompting implementation analysis.

Kristoffer Haugsbakk's expanded audit provides definitive numbers: 40 builtins output help to stderr versus 102 using stdout, confirming Junio's observation that stderr usage is the minority case. The comprehensive list includes core commands like `add`, `commit`, `fetch` (stdout) versus `branch`, `checkout`, `merge` (stderr). Junio has now explicitly validated Kristoffer's finding that `git-upload-pack` correctly uses stderr for error cases (no arguments), maintaining the critical distinction from `-h` help output. Some duplicate entries (like multiple `git log` instances) remain for verification.

Jeff King (Peff) has advanced technical discussion with:
1. Proposed test case (`t/t0012-help.sh`) verifying stdout help with empty stderr
2. Analysis tracing `git branch -h` stderr to commit 1dacfbcf13 bypassing parse-options
3. Concrete `branch.c` fix demonstrating safe parse_options() reordering
4. Systematic solution proposal: stdout variant of `usage_with_options()`

Technical considerations now include:
- Clear maintainer-confirmed boundary: `-h` help (stdout) vs error cases (stderr)
- `branch.c` fix shows repository setup can safely follow parse_options()
- 40 commands require modification to align with stdout standard
- Test infrastructure (`t0012-help.sh`) now available to prevent regression
- Audit data confirms stderr usage is the exception (28%) rather than rule

Key participant positions:
- Jonas Konrad: Original reporter, pro-stdout, contributed expanded audit
- Matěj Cepl: UNIX convention advocate (stderr)
- Junio Hamano: Maintainer, with technical audit backing stdout preference, now confirmed error case handling
- Kristoffer Haugsbakk: Provided definitive implementation data (40 vs 102 split) and error case verification
- Jeff King: Driving technical solution with code and test infrastructure

Open questions:
- Final adoption path for Peff's systematic approach vs command-by-command fixes
- Exit code normalization strategy
- Resolution of duplicate entries in audit data
- Handling of `git diff` family special cases
- Implementation sequencing based on audit findings (40 commands to modify)
- Potential future standardization for auxiliary tools (scalar, LFS, etc.)

The thread has progressed from philosophical debate through comprehensive audits to concrete implementation options, with maintainer-endorsed technical basis for change now including validated error case boundaries. Peff's contributions provide both specific fixes and architectural patterns, while Kristoffer's definitive audit data quantifies the scope. Next steps involve evaluating systematic solutions against the full dataset of 40 stderr-using commands, with test infrastructure now in place to validate any approach. The auxiliary tool findings suggest the standardization effort may eventually extend beyond core builtins.