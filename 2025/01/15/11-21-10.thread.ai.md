Thread summary:

Bug report and discussion about inconsistent help message behavior in Git commands, focusing on output streams (stdout vs stderr) and exit codes. Jonas Konrad initially reported the discrepancy in Git 2.48.1 between `git -h` (stdout, exit 0) and `git branch -h` (stderr), noting problems for piping help output. He has since expanded the report to show three distinct behaviors: `git -h` (stdout, 0), `git reflog -h` (stdout, non-zero), and `git branch -h` (stderr). Matěj Cepl maintains that writing help to stderr follows UNIX conventions where only "substantial material" should use stdout, advocating for `|&` as the proper capture method. Jonas counters that if stderr were Git's intended convention, many subcommands would need changing, and questions the logic differentiating "substantial" vs non-substantial help text. The core tension remains between two views: UNIX convention (stderr for help) versus consistency and usability (stdout for all help). Key open questions include: (1) Is there documented Git policy on help output streams? (2) Should behavior be standardized across commands? (3) If keeping stderr, should exit codes remain non-zero? (4) What criteria determine which help text is "substantial"? The thread shows no maintainer input yet to clarify Git's official stance or reference prior discussions on this design choice. The inconsistency between top-level `git -h` and subcommand help behaviors remains the central unresolved issue, now compounded by the discovery of the third variant (`git reflog -h` with stdout but non-zero exit). Technical specifics include the exact command variations showing divergent behavior and their output/exit code combinations. The discussion remains at an impasse between competing philosophies without technical rationale from Git's design principles.