Test infrastructure patch converting the decoration test to the Clar framework. This is part 2 of Seyi Kuforiji's 4-patch series modernizing unit tests under Patrick Steinhardt's mentorship, following up on v1 review feedback.

The patch converts t-example-decorate.c to u-example-decorate.c, restructuring the test to use Clar's conventions. Key changes include:
- Adding explicit initialize()/cleanup() hooks to ensure test isolation
- Splitting the test into discrete functions (add, readd, lookup, loop)
- Replacing custom assertions with Clar's cl_assert_* helpers
- Maintaining all original test coverage while improving reliability

The conversion follows the same pattern as the hashmap test in patch 1, with similar benefits: clearer test organization, better isolation between cases, and standardized assertions. The build system updates (Makefile and meson.build) are straightforward, removing the old test program and adding the new Clar suite.

This appears to be a well-executed conversion that addresses the review feedback from v1. The commit message clearly explains the motivation (removing test dependencies) and implementation approach. The patch is ready for integration as part of the broader Clar test framework adoption effort.