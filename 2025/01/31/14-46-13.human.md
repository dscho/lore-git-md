This is a test infrastructure patch in the atomic push exit code propagation series. Eric Sunshine provides a minor style suggestion for Patrick Steinhardt's patch that adds new porcelain output test cases for git-push.

The patch being reviewed adds two new test cases to t5548-push-porcelain.sh to exercise both non-atomic and atomic push scenarios with --porcelain output. Eric points out that the heredoc delimiters should use '\EOF' rather than bare 'EOF' for better practice, though notes this isn't significant enough to warrant a reroll of the patch series.

This is a small refinement to the comprehensive test modernization work that's been progressing through the series. The suggestion aligns with Git's test style guidelines while maintaining the test's functionality. The thread context shows this is part of a larger effort to properly propagate receive-pack errors during atomic pushes, with extensive test infrastructure improvements being made alongside the core protocol fixes.