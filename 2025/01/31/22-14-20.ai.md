Test infrastructure patch converting the strcmp-offset test to the Clar framework, completing Seyi Kuforiji's 4-patch series under Patrick Steinhardt's mentorship. This final conversion follows the established pattern of renaming the test file (t-strcmp-offset.c → u-strcmp-offset.c) and restructuring the test cases as discrete Clar test functions.

The patch introduces a new test case `test_strcmp_offset__empty()` to verify behavior with empty strings, while maintaining all existing test coverage through functions like `test_strcmp_offset__equal()` and `test_strcmp_offset__different()`. The original test-lib.h assertions are replaced with Clar's `cl_assert_equal_i()` equivalents. Build system files (Makefile and meson.build) are updated to reflect the file rename and framework switch.

This conversion follows the same high-quality pattern seen in the earlier patches of the series, with clear test isolation and improved function naming. The patch appears technically sound, having incorporated Patrick's feedback from earlier reviews. The only outstanding item is documenting the new empty-string test case in the commit message, as noted in the thread context. This completes the modernization of these core unit tests to the Clar framework while expanding test cases.