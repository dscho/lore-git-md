Test refactoring patch in the atomic push exit code propagation series. This is the second of eight patches in v4, continuing the preparatory test modernization work started in the first patch.

The patch refactors test setup code in t5548-push-porcelain.sh to extract reusable components. It splits the existing setup_upstream_and_workbench() function into two parts: create_upstream_template() handles the initial bare repository creation, while setup_upstream() manages repository cloning and HTTP protocol configuration. The original setup_upstream_and_workbench() now builds on these primitives.

Key technical changes include moving HTTP protocol configuration into the setup functions, adding BUG() assertions for parameter validation, and standardizing the repository setup process between local and HTTP test cases. The refactoring enables future test additions to reuse these setup routines while maintaining consistent behavior across protocols. No test behavior changes - this is purely structural improvement to support upcoming test additions in the series.

The changes touch only t/t5548-push-porcelain.sh, with 85-line diff showing the function extraction and consolidation of HTTP setup logic. The patch carries forward Jiang Xin's original work with Patrick Steinhardt now maintaining the series, as evidenced by the dual sign-off. This continues the pattern seen in patch 1/8 of modernizing test infrastructure before introducing behavioral changes.