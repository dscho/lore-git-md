This is a brief follow-up in the bugfix series introducing comprehensive ref validation checks, specifically addressing Junio's feedback on the final patch that integrates ref verification into `git fsck`.

The email is a simple acknowledgment from shejialuo agreeing with Junio's observation about unnecessary use of a `progress_num` variable in the `fsck_refs()` function. Junio had noted that passing the literal constant `1` directly to `start_progress()` would be cleaner, and shejialuo confirms this will be fixed in the next version.

This is a minor implementation detail in the larger series that adds ref validation through a new `git refs verify` child process. The thread context shows this is the final patch in an 8-part series that has undergone significant review and refinement, particularly around header validation and sortedness checking behaviors. The core technical decisions appear settled at this point, with only small polish items like this remaining.