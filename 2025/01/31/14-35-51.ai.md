Follow-up discussion in the packed-refs validation series regarding sortedness checking.

This email from shejialuo responds to Junio's feedback on patch 7/8 of the series, which deals with verifying sortedness of packed-refs files. The key points:

1. **Runtime behavior clarification**: shejialuo explains that the runtime actually handles both sorted and unsorted packed-refs files differently based on the header's "sorted" trait - sorting occurs during snapshot creation only when the trait is absent. This matches historical behavior seen in v1.5.0.

2. **Open question about strictness**: The author seeks clarification from Patrick Steinhardt about whether to always enforce sortedness, given Patrick's earlier warning about potential bugs from unsorted refs. This suggests some uncertainty about whether the current patch's approach (only checking when "sorted" trait exists) is sufficient.

3. **Implementation detail**: shejialuo acknowledges Junio's suggestion to use `size_t`/`ulong` for line numbers consistently across the validation code, agreeing to make this change.

The discussion continues to refine the balance between strict validation and backward compatibility, particularly around the handling of legacy packed-refs files without headers. The thread shows active engagement between the contributor and maintainers to align the validation behavior with both historical practice and modern safety requirements.