This is a test infrastructure patch in the atomic push exit code propagation series. Eric Sunshine provides a minor style suggestion for Patrick Steinhardt's patch that adds new porcelain output test cases for git-push.

The suggestion focuses on heredoc syntax in t5548-push-porcelain.sh, recommending the use of `\EOF` rather than bare `EOF` for better shell script hygiene. This matches modern Git test suite conventions where heredoc delimiters are typically escaped to prevent variable interpolation and other shell expansions. The change would apply to both the new test case shown (testing non-atomic push porcelain output) and another similar test case for atomic pushes that appears later in the patch.

Eric explicitly notes this isn't significant enough to warrant a reroll of the patch series, indicating it's a minor polish rather than a functional concern. The suggestion comes in the context of an already well-developed test infrastructure that was modernized earlier in the series, including protocol-agnostic setup functions and comprehensive coverage of push porcelain output scenarios.