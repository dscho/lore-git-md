This is a refactoring patch series that introduces generic wrappers for hash context operations in Git's codebase. Patrick Steinhardt proposes changing how hash algorithms interact with their contexts by storing the algorithm type directly in the context structure, eliminating the need to pass algorithm-specific function pointers for each operation.

The key change moves from algorithm-specific function calls (like `sha1_update_fn`) to generic wrappers (`git_hash_update`) that automatically use the correct algorithm based on the context. This simplifies calling code and reduces boilerplate, as shown in the example where initialization, updates, and finalization can now work through a single context structure.

The series builds on previous discussions about hash algorithm handling and touches a wide range of files (23 modified) across Git's codebase, including core functionality like object handling, diffing, and pack operations. The changes appear mechanical but widespread, suggesting careful review will be needed to ensure no subtle behavioral changes are introduced.

This looks like a well-structured effort to modernize Git's hash algorithm interface, with each commit logically building toward the final goal. The patch count and file list indicate this is a significant but focused refactoring that should make future work with hash algorithms more straightforward.