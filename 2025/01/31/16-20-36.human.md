This is a discussion about filetype validation in the packed-refs bugfix series, specifically addressing how to handle symbolic link attacks and HEAD verification.

Junio Hamano responds to shejialuo's approach to validating the packed-refs file, agreeing that using `open_nofollow()` is the right approach where available, with fallback to `lstat()` on platforms without it. The key point of contention is around HEAD validation - while shejialuo argues that corrupted HEAD files prevent repository recognition and thus don't need explicit checking, Junio insists that `git refs verify` should still report HEAD corruption to users diagnose repository issues.

The exchange reveals an important implementation detail: the validation will use `open_nofollow()` when possible (for better security against symlink attacks), falling back to `lstat()`+`open()` where necessary. Junio also suggests enhancing test coverage by creating test cases that combine HEAD corruption with packed-refs symlink attacks, though shejialuo maintains that HEAD validation happens at a different layer.

This discussion shows the series is nearing completion but still needs to resolve how to properly surface HEAD corruption errors to users through the new verification system. The technical approach to file validation appears settled, but the user-facing error reporting needs refinement.