This is a technical discussion about filetype validation in the packed-refs bugfix series, specifically addressing race conditions in symlink detection. The email is a response to Junio Hamano's questions about the security implications of using `open_nofollow()` versus `lstat()` for checking whether "packed-refs" is a regular file. Shejialuo explains that while both approaches have the same fundamental TOCTOU race condition limitations, `open_nofollow()` provides more consistent behavior by operating on a stable file descriptor once obtained. The discussion reveals that on platforms without O_NOFOLLOW support, `open_nofollow()` falls back to `lstat()` anyway, making the security benefits platform-dependent.

Key technical details include:
- The validation occurs in `packed-backend.c` using the existing `FSCK_MSG_BAD_REF_FILETYPE` message
- Test coverage in `t0602-reffiles-fsck.sh` verifies the symlink detection
- Junio suggests expanding tests to combine HEAD corruption with packed-refs tampering
- Shejialuo plans to clarify the commit title to explicitly mention "regular file checks"
- The discussion highlights the inherent race conditions in filesystem validation that no approach in this series can fully resolve

This exchange represents final polishing of patch 3/8 in the series, with Junio engaging on implementation tradeoffs while shejialuo maintains the current approach as the most robust available option given platform constraints. The thread shows consensus forming around the technical limitations while ensuring the checks remain as secure as practically possible.