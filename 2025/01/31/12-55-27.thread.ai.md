Thread summary:

This thread introduces a 4-part refactoring series to improve Git's hash algorithm infrastructure by converting the hash context from a union to a proper structure and adding generic wrapper functions. The series builds on prior discussions about hash algorithm infrastructure and is based on master (3b0d05c), incorporating tb/unsafe-hashtcleanup work.

The first patch converts `git_hash_ctx` from a typedef'd union into a structure containing a union, moving all hash-specific states (SHA1, SHA1-unsafe, SHA256) into a nested `state` field. This enables future extension to track the hash algorithm used. The change updates hash function implementations in `object-file.c` to access state through the new structure (23 lines added, 21 removed), maintaining identical behavior with an additional indirection layer.

The series will subsequently:
1. Remove the hash context typedef entirely
2. Introduce generic wrapper functions (git_hash_update, git_hash_final_oid) using the algorithm stored in the context
3. Update 23 files across the codebase to use the new wrappers (224 insertions, 201 deletions total)

The refactoring simplifies hash operations by eliminating separate algorithm tracking in calling code. While technically sound, reviewers should verify no performance regressions given hash operations' fundamental role. The changes touch multiple subsystems but focus on internal plumbing improvements rather than user-facing behavior. The first patch's mechanical nature makes it a low-risk starting point for the series.