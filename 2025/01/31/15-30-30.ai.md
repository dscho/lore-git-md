This is a preparatory refactoring patch in the `git clone --revision` series, focusing on tag handling internals. The change inverts the internal flag controlling tag cloning from `--no-tags` to `--tags` (default-enabled) to simplify the code and prepare for upcoming patches in the series. While functionally equivalent to the current behavior (since tags are cloned by default), this restructuring makes the code more consistent with the eventual `--revision` feature.

The patch updates both documentation and implementation: the man page now shows `--[no-]tags` as the official option pair (though `--tags` remains a no-op by default), while the code flips the internal boolean from `option_no_tags` to `option_tags` with default true. All related conditionals are updated to check the new flag. The change affects tag-related operations in `wanted_peer_refs()`, `update_remote_refs()`, transport refspec handling, and config writing.

This appears to be a straightforward mechanical change with no behavior modification, serving as groundwork for later patches in the series. The commit message references historical context about the original `--no-tags` addition and links to past discussion, though no tests are added since the visible behavior doesn't change. The patch maintains consistency with the series' broader goal of cleaning up `builtin/clone.c` internals.