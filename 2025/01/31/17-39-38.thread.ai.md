Updated AI summary:

CI infrastructure fix addressing edge case in GitLab CI whitespace/style check jobs' base commit determination. The thread concerns a bug where shell parameter expansion syntax (`${var-default}`) fails to properly handle null values of CI_MERGE_REQUEST_TARGET_BRANCH_SHA/CI_MERGE_REQUEST_DIFF_BASE_SHA variables in GitLab pipelines. Justin Tobler's patch changes this to `${var:-default}` syntax in two .gitlab-ci.yml locations (check-whitespace and check-style jobs) to correctly handle both unset and null cases. Toon Claes confirms the technical correctness of the fix, citing Bash manual documentation that explains the distinction between unset and null variable handling in parameter expansion. The change is minimal (adding two colons) but resolves a real-world edge case where variables could be set but null. Successful CI runs with the fix were demonstrated in the original patch submission. The thread shows consensus on the solution with no outstanding objections or follow-up work needed. The fix builds on prior related commits that introduced the current behavior and maintains compatibility with existing GitLab CI workflows.