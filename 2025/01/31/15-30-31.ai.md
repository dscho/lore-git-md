Refactoring patch in the `--revision` clone option series. This is the third patch in v4 of the 6-part series, focusing on simplifying the `wanted_peer_refs()` function in `builtin/clone.c`.

The changes streamline the ref mapping logic by:
1. Consolidating the single-branch and multi-branch paths to use the same refspec processing loop
2. Moving tag handling outside the branch-specific logic
3. Simplifying memory management with a single `to_free` pointer
4. Removing redundant warning about missing branches (now handled earlier)
5. Adding clearer comments about tag handling behavior

The refactoring maintains all existing functionality while reducing the function from 39 to 24 lines. This appears to be preparatory work for the upcoming `--revision` feature, making the ref mapping logic more maintainable. The changes are focused and well-contained, touching only this one function while keeping all tests passing.