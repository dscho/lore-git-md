This is a minor follow-up in the bugfix series introducing packed-refs validation checks, specifically addressing the final patch that integrates ref verification into `git fsck`.

The email responds to Junio C Hamano's review comment about unnecessary variable usage in the progress reporting code for the `fsck_refs()` function. shejialuo acknowledges the point and agrees to simplify the code by removing the redundant `progress_num` variable and using a literal constant `1` instead in the next version of the patch.

This is a small implementation refinement in the context of the larger series that:
1. Adds ref verification as a child process in `git fsck`
2. Maintains progress reporting capabilities
3. Follows the series' pattern of careful error checking and validation

The change doesn't affect the core functionality but improves code cleanliness as suggested by the maintainer. The thread context shows this is part of ongoing refinements to the comprehensive ref validation system, with previous discussions covering header validation, sortedness checks, and test infrastructure.