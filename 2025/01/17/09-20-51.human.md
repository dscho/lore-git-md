This is part 4 of a 5-patch series continuing the `the_repository` removal effort, focusing on pack-writing functionality. The patch converts `write_rev_file()` and its helper functions to take an explicit `hash_algo` parameter instead of using the global `the_hash_algo`.

The change follows the series' established pattern of bubbling up global variable usage to callers while maintaining compatibility. Most callers (like `index-pack`) simply pass `the_hash_algo` for now, while `midx-write.c` uses its available repository context instead. The patch includes mechanical updates to function signatures and call sites across `pack-write.c`, `midx-write.c`, and `index-pack.c`.

This is a straightforward refactoring with no behavior change - it's part of the incremental strategy to eventually eliminate global state. The commit message clearly explains the temporary nature of passing `the_hash_algo` from callers, acknowledging this as a stepping stone toward fuller conversion. The patch appears well-contained and follows the series' pattern of focused, documented changes.