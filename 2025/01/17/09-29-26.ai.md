This is a follow-up discussion on the first patch of the reftable hardening series, focusing on documentation preservation during refactoring. Patrick Steinhardt notes that while Karthik Nayak's patch correctly makes `ref_transaction_update_reflog()` static (removing its declaration from refs.h), the accompanying explanatory comment about the index field ordering behavior might still be valuable to keep. The comment currently describes how the index parameter can be used to control update ordering when multiple reflog updates are batched.

The exchange highlights a subtle but important aspect of API changes - preserving documentation even when implementation details become private. The index ordering behavior remains relevant to callers of the public `ref_transaction_update()` API that may internally use the now-private function. Patrick's comment suggests either moving this documentation to remain with the public API or finding another appropriate location to maintain visibility of this behavioral detail.

This is a minor but thoughtful refinement to the initial patch, showing attention to both code structure and documentation continuity in the refactoring process. The suggestion doesn't contradict the patch's core purpose of encapsulation but aims to preserve useful information for future developers working with the transaction API.