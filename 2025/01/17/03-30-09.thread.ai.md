Thread summary:

Bug report for sparc64-specific SIGBUS crash in `git pull` during object unpacking, occurring with clang-compiled Git (version 2.48.1.262) but not GCC. The crash triggers at `builtin/unpack-objects.c:653` when accessing `hdr->hdr_signature`, suggesting alignment issues with the buffer declaration during `htonl(PACK_SIGNATURE)` assignment. Reporter provides detailed system specs (sparc64, clang 19.1.4, glibc 2.40) and confirms an overalignment workaround prevents the crash, though the proper fix remains unclear. No bisect information or minimal reproduction case provided yet. Platform-specific nature suggests this may require either compiler-specific handling or buffer alignment adjustments in the unpack-objects code.