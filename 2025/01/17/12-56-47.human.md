Cleanup patch completing the sparc64 pack header alignment fix series. This final patch in Jeff King's 3-part series replaces magic number string offsets with `skip_prefix()` in both `index-pack` and `unpack-objects` when handling the `--pack_header` option. The change improves code clarity by eliminating manual string length calculations while maintaining identical functionality.

The patch modifies error messages to include the full `--pack_header` prefix, providing better context for translators. While the author jokingly acknowledges this might be "just churn" due to personal preference against manual string counting, the change aligns with Git's codebase standards and completes the architectural improvements started in earlier patches.

This follows PATCH 1's refactoring of shared header parsing logic and PATCH 2's alignment-safe buffer handling. The series collectively addresses a sparc64-specific crash while improving code maintainability across both affected commands.