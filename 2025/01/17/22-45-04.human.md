Junio Hamano notes an interesting observation about the sparc64 alignment issue in Jeff King's patch series. He points out that while both `input_buffer` and `buffer` in the original code are file-scope static variables (which typically get aligned in BSS), one happened to be misaligned while the other wasn't - suggesting the alignment was somewhat random. This reinforces why the fix using `put_be32()` is necessary, as it makes the code alignment-safe regardless of where buffers happen to land in memory. 

Hamano confirms that the approach in patch 2/3 is "absolutely the right thing to do," validating the technical direction of the fix. The comment doesn't raise any concerns or suggest changes, but provides useful context about why the bug manifested inconsistently even between similar code paths. This helps explain why the issue might have been harder to catch in testing.