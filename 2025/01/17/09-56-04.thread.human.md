This thread introduces a 3-patch series to add Meson build support to git-subtree, starting with a documentation build fix. The initial patch addresses a breakage caused by changes in how Git's main build system handles documentation version information. The author explains that commit a38edab7c8 moved from command-line version injection to generating versioned config files, which git-subtree's Makefile hadn't been updated to match. The fix updates the subtree Makefile to generate its own versioned config files using the same `GIT-VERSION-GEN` approach as the main build, while maintaining compatibility with both asciidoc and asciidoctor build paths.

The series appears to be a straightforward infrastructure improvement, with this first patch serving as a necessary prerequisite for the subsequent Meson support additions. The changes are mechanical and follow established patterns from Git's main build system. The remaining two patches in the series will introduce general contrib build support in Meson before specifically adding git-subtree support, continuing Git's ongoing adoption of Meson as an alternative build system.