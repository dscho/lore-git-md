This is a test infrastructure patch in a series refactoring Git's hash algorithm handling. It introduces a new `sha1-unsafe` test helper to exercise Git's unsafe SHA-1 operations, mirroring the existing `sha1` test helper but using the unsafe variants of the hash functions.

The patch modifies the generic hash test infrastructure to optionally use unsafe hash operations when requested, then exposes this through a new test helper command. The test script is updated to run all its existing test cases through both the safe and unsafe variants, verifying they produce identical results.

This appears to be a well-constructed test addition that will help validate the broader hash algorithm changes in the series. The implementation is straightforward - it builds on existing test infrastructure while cleanly separating the unsafe variant testing. The patch maintains test coverage parity between safe and unsafe operations, which will be important for the series' goal of unifying hash algorithm handling.