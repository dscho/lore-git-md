This is a test conversion patch in a series modernizing Git's test infrastructure by moving unit tests to the Clar framework. It converts the reftable tree test from the old test-lib style to the new Clar format, maintaining the same test coverage while using Clar's assertion macros.

The patch follows the established pattern seen in previous conversions in this series:
- Renames t-reftable-tree.c → u-reftable-tree.c
- Replaces test-lib assertions (check()/check_pointer_eq()) with Clar macros (cl_assert()/cl_assert_equal_p())
- Preserves both test scenarios from the original:
  - test_reftable_tree__tree_search: Verifies tree insertion and search operations
  - test_reftable_tree__infix_walk: Tests tree traversal functionality
- Updates build system files (Makefile and meson.build) to reference the new location

The conversion appears mechanical and correct, maintaining the same test logic while adapting to Clar's conventions. Like other patches in the series, it removes the old test file and adds the new Clar-format version. The commit message is brief but sufficient given this is part of an established conversion effort with detailed context in earlier cover letters.

One minor note is that while the patch follows the series' pattern well, it doesn't address the outstanding feedback about commit message wording and attribution that Patrick noted needs to be resolved before the series can be merged. This appears to be the last test conversion in the current series.