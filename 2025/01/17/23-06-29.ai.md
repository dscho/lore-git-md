This is Junio Hamano's response to Randall Becker's portability concerns about the OS version reporting feature, specifically addressing the NonStop platform's unconventional `uname` output format. The email represents a key moment in the thread where the fundamental design approach is being questioned.

Junio points out that if Becker's platform produces a usable version string from the standard `uname` command (albeit in a non-standard format), then the proposed `osversion.command` configuration option - which allows custom command execution to generate version strings - may be unnecessary. This challenges one of the series' core features that was meant to handle platform-specific version reporting.

The brief exchange highlights the tension between the current implementation's flexibility (allowing arbitrary command execution) versus simpler approaches that might work for most platforms. Junio's rhetorical question suggests he sees the command execution feature as over-engineering if standard `uname` output, even in unusual formats, would suffice for protocol purposes.

This response continues the thread's shift from implementation details to questioning fundamental design choices, particularly around security (command execution) and whether the feature's complexity is justified by real-world needs. The NonStop platform's output serves as a concrete example where the current design might be solving a non-problem.