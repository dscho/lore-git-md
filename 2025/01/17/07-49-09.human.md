This is a platform compatibility patch addressing compiler warnings about signed/unsigned comparisons in `connect.c`. 

Mike Hommey cleans up several `-Wsign-compare` warnings by changing loop variables and function parameters from `int` to `size_t` where they're compared with size-related values. The changes are straightforward type adjustments in functions dealing with server capabilities and remote ref processing. 

The patch removes a `DISABLE_SIGN_COMPARE_WARNINGS` define that was previously suppressing these warnings, suggesting the code is now clean enough to handle the stricter checking. Most changes are simple variable type switches, though one case required adjusting a function signature (`process_capabilities()`) which then rippled to its callers.

This appears to be a well-scoped, mechanical change that improves code hygiene without altering behavior. The patch follows Git's style and touches only the minimal necessary lines to fix the warnings.