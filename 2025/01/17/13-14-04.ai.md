Performance optimization discussion for `git bisect` in large repositories, continuing the thread on quadratic behavior in `do_find_bisection()`. Jeff King responds to Askar Safin's emphasis on bisection's importance by proposing practical workarounds while the algorithmic improvements are being developed.

The email suggests two immediate strategies for users facing slow bisection in massive histories like the kernel repository: (1) manually selecting approximate midpoints (e.g., major version tags like v4.0/v5.0) to reduce search space, and (2) using `--first-parent` to follow only the main development line, then refining the search around merge points. Both approaches trade some precision for significant speed gains, with the first-parent method completing in ~7 seconds versus the original 21 minutes.

King also prototypes a potential automated approximation approach using `rev-list --topo-order` with midpoint calculation, showing it finds a reasonably balanced split (526k vs 543k commits) in just 2 seconds. This demonstrates that while the core algorithm remains O(n²), practical solutions exist for users who can tolerate minor inaccuracies in early bisection steps.

The discussion continues to balance immediate usability improvements against deeper algorithmic changes, with King's suggestions providing stopgap measures that could inform future `git bisect` heuristics. The thread maintains focus on real-world kernel development needs while acknowledging the complexity of optimal bisection in non-linear histories.