This is the first patch in a 3-part series hardening the reftable migration path, specifically a refactoring change to mark `ref_transaction_update_reflog()` as static since it's only used within refs.c.

The patch removes the function declaration from refs.h and marks it static in refs.c, where it's the only consumer. This is a straightforward scope reduction that follows good encapsulation practices - the function was never meant to be part of the public refs API. The change was prompted by Junio Hamano's observation that the function didn't need external visibility.

The diff shows the function signature remains identical except for the static qualifier, and the documentation comment in refs.h is removed since it's no longer needed for an internal function. This is a clean-up change that doesn't affect behavior but improves code organization as part of the larger reftable migration improvements.

Key technical details for future reference:
- Function: `ref_transaction_update_reflog()` (now static)
- Files modified: refs.c (implementation), refs.h (removed declaration)
- No behavior changes - purely a visibility reduction
- Part of broader reftable migration hardening effort following corruption issues