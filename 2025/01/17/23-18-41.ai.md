This is a follow-up discussion in the OS version capability thread, specifically addressing the custom command execution feature's necessity. Randall Becker confirms that his NonStop platform's unconventional `uname -srvm` output ("NONSTOP_KERNEL L24 08 NSV-D") would be acceptable for the protocol's purposes. This directly responds to Junio Hamano's questioning of whether the `osversion.command` config variable (introduced in patch 6/6) is actually needed if standard `uname` output suffices even when non-standard.

The exchange suggests the custom command execution feature may indeed be solving a non-problem, as Becker indicates his platform's output would work fine without customization. This reinforces Junio's architectural objection to the runtime command execution approach and further undermines the justification for the configurable command feature. The discussion moves toward potentially simplifying the implementation by relying on standard `uname` output across platforms, even when that output doesn't follow conventional formatting.