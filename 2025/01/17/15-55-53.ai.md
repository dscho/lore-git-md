This is a follow-up in the sparc64 SIGBUS crash thread, confirming the bug and testing the proposed fix. Koakuma verifies that Jeff King's minimal reproduction case reliably triggers the SIGBUS crash on sparc64, and confirms that the proposed fix for `cmd_unpack_objects` resolves the write-side alignment issue. However, they note the crash still occurs in `unpack_all()` when reading the unaligned header, suggesting the read path needs similar alignment-safe treatment.

The email highlights an important gap in the current fix - while the write path was made alignment-safe using `put_be32()`, the read path still performs direct struct access that will fail on strict-alignment architectures like sparc64. This indicates the fix will need to be extended to cover both write and read operations consistently. The confirmation that sparc64 traps on all unaligned accesses (both reads and writes) reinforces the need for comprehensive alignment-safe handling throughout the pack header processing code.

Key technical details:
- Crash occurs in `unpack_all()` at line 583 when accessing `hdr->hdr_entries`
- Confirms sparc64 traps on all unaligned accesses (reads and writes)
- Validates the write-side fix using `put_be32()` works as intended
- Shows the read path needs equivalent treatment with alignment-safe reads

This email moves the thread forward by providing concrete verification of both the bug and partial fix, while identifying the remaining work needed to fully resolve the issue. The next step would be extending the alignment-safe approach to the read path in `unpack_all()`.