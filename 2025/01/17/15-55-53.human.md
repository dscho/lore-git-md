This is a follow-up in the sparc64 SIGBUS crash thread, confirming the bug and testing Jeff King's proposed fix. Koakuma reports that while the proposed change to `unpack-objects.c` does fix the immediate SIGBUS during the `--pack_header` write operation, the crash still occurs during subsequent reads in `unpack_all()`. 

The email confirms that sparc64 traps on all unaligned accesses (both reads and writes), meaning the same alignment-safe approach will need to be applied to the reading side as well. This suggests Jeff's patch series will need to be extended to cover both sides of the operation.

The exchange demonstrates good debugging collaboration - Jeff provided a minimal reproduction case, Koakuma confirmed it works and tested the partial fix, and identified where additional changes will be needed. The thread continues to show thorough technical understanding from both participants about the platform-specific alignment requirements.