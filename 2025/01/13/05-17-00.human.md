Follow-up discussion on ref-filter memory management fix.

Jeff King responds to René Scharfe's patch that fixed memory leaks in ref-filter's ahead-behind and is-base calculations by making the committish string lists static. While acknowledging the fix works, Jeff raises concerns about the long-term direction, suggesting an alternative approach where sorting operations would explicitly share a ref_format context struct rather than using static variables.

The discussion highlights two competing solutions: René's simpler approach that removes code by using statics versus Jeff's more explicit context-passing approach that might better support future lib-ification. Jeff provides a patch demonstrating his alternative but remains undecided, noting both approaches have merits - René's is simpler now while his might be more maintainable long-term.

This continues the thread about fixing ref-filter's memory management while touching on broader design questions about how to properly share state between formatting and sorting operations. The core issue remains ensuring consistent behavior across commands like branch, for-each-ref, and tag when handling these ref-filter atoms.