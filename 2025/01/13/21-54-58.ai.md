Security enhancement follow-up focusing on credential URL documentation. Brian m. carlson responds to Junio Hamano's query about the scope of `transfer.credentialsInUrl` warnings, confirming the current implementation only triggers for passwords in URLs (not usernames alone) by examining the code. The thread had previously established that both `.URL` and `.pushURL` should be checked consistently when warning about credentials in URLs, with consensus that the warning system should be comprehensive before changing the default from "allow" to "warn".

Carlson identifies a documentation gap - the term "credentials" in the current docs doesn't clearly specify whether it covers just passwords or usernames too. While the code correctly handles only passwords (which Carlson approves of), they propose clarifying the documentation to help users understand exactly what triggers warnings. This follows up on Derrick Stolee's earlier point about ensuring clear user guidance around the warning system. The email signals an upcoming documentation patch to address this precision issue, maintaining the security focus while improving user comprehension of the feature's boundaries.