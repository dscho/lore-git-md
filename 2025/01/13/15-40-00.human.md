This is a code review comment in Jeff King's ongoing combine-diff refactoring series (specifically patch 8/14). Patrick Steinhardt notes a subtle type change in the `path_appendnew()` function that wasn't mentioned in the commit message - the `len` parameter was changed from `int` to `size_t`. He also confirms the change makes sense and observes there's only one caller of this function, so no additional changes are needed.

The change itself (from the parent email) moves the path concatenation logic out of `path_appendnew()` and into its caller `emit_path()`, simplifying the helper function's interface. This is part of the series' broader effort to streamline the combine-diff path handling code. The review is positive overall, just noting a minor documentation oversight in the commit message.