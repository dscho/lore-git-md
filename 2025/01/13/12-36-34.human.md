Shell portability fix for blame tests. Patrick Steinhardt responds to Jan Palus's patch that fixes mksh compatibility in t8002-blame.sh tests. The original patch used a non-portable `printf "%0.s"` format that breaks on mksh, replacing it with the more widely supported `"%Ns"` format (already used elsewhere in the test suite). 

Steinhardt suggests minor improvements to the commit message, noting that Git doesn't typically use "Fixes" tags but rather references commits with `git log --format=reference`. He also proposes a more accurate subject line focusing on the portability aspect rather than the "simplification" angle. The change itself is minimal (2 lines) and maintains identical test expectations while fixing the mksh compatibility issue.

This appears to be a straightforward, uncontroversial improvement to test portability. The discussion is focused on commit message conventions rather than technical concerns, suggesting the patch is likely to be accepted with these minor wording tweaks.