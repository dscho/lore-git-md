This is a documentation patch in Jeff King's combine-diff refactoring series, specifically addressing a gap in struct field documentation that was noted in the thread. The patch adds clarifying comments to the `combine_diff_path.parent.path` field, explaining that it's only populated during combined diffs with `revs.combined_all_paths` set and when paths differ (e.g., for renames/copies).

Patrick Steinhardt's review is light - he observes this could have been included in the preceding commit but doesn't object to it being separate. The change itself is straightforward: a 6-line comment addition to `diff.h` documenting the conditional population of the parent path field. This maintains the series' pattern of improving code clarity through better documentation while refactoring the combine-diff internals.

Key technical details for future reference:
- Documents behavior of `combine_diff_path.parent.path` field
- Only set when `revs.combined_all_paths` is enabled AND paths differ
- Used for rename/copy cases
- NULL otherwise
- Field exists within flexible array member `parent[FLEX_ARRAY]`
- Follows earlier pattern of documenting struct fields in the series

The change is uncontroversial and aligns with the series' goals of making the combine-diff implementation more maintainable through explicit documentation. No behavior changes are introduced.