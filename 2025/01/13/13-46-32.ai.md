This is a build system patch aligning Meson configuration with the Makefile setup. The change moves the `config-list.h` target from `libgit_sources` to `builtin_sources` since it's only used by `builtin/help.c`. The patch is straightforward - a single line change in `meson.build` that matches the existing Makefile dependency structure. The commit message is minimal but correctly identifies the motivation (matching Makefile behavior) and impact (correcting where the config-list dependency belongs). This appears to be a mechanical cleanup with no functional changes, following the pattern established in the Makefile build system.