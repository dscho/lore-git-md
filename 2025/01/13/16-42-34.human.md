This is a reply in the ongoing discussion about hardening credential handling in Git URLs, specifically about changing the default behavior of `transfer.credentialsInUrl` from "allow" to "warn".

Junio Hamano (the maintainer) expresses general support for the security-minded proposal to warn by default when credentials appear in URLs, but raises two practical considerations. First, he notes the current implementation only checks `.URL` configurations while ignoring `.pushURL`, suggesting this gap should be addressed before changing defaults. Second, he echoes Derrick's earlier point that clear user guidance (via advice messages) would be needed to explain how to disable warnings if desired.

The email doesn't oppose the security change but flags two implementation details (.pushURL coverage and user messaging) that would need resolution first. This moves the discussion forward by identifying concrete next steps rather than debating the principle of warning by default. The tone suggests these are solvable issues rather than blockers.