Here's the human-readable thread summary:

This thread explores performance optimizations for `git bisect` in large repositories like the Linux kernel (with over 1 million commits). The core issue is a quadratic O(n²) slowdown in `do_find_bisection()` where each commit potentially traverses the entire graph. Jeff King (Peff) identified the main culprit - overly broad flag clearing across all commits after each distance calculation.

The discussion has progressed from problem identification to a working optimization. Jeff's patch modifies `clear_distance()` to only clear flags along the relevant commit path, reducing runtime by ~40% in tests (from 16m51s to 9m34s for the Linux kernel case). While this provides immediate relief, Jeff notes it doesn't fully solve the fundamental quadratic behavior.

In the latest email, Jeff re-examines potential algorithmic solutions. He retracts an earlier suggestion about using reverse-topological order traversal, explaining it would double-count commits with multiple parents. Instead, he points to Git's reachability bitmap computation as inspiration - though acknowledging this approach would be complex to implement. The thread now stands at a decision point: accept the current optimization as "good enough" for now, or invest in deeper architectural changes for a complete solution. Benchmarking shows the current improvements are meaningful, but there's clear potential for further gains if someone tackles the algorithmic challenge.