This thread explores ways to make `git bisect` more efficient in massive repositories like the Linux kernel, where the current O(n²) behavior can make bisection painfully slow. Jeff King (Peff) initially identified the performance bottleneck in `do_find_bisection()` and proposed a patch that reduces runtime by ~40% by limiting unnecessary flag clearing. He then shifted focus to practical workarounds kernel developers can use today, like manually selecting midpoints using version tags or using `--first-parent` traversal to approximate the bisection.

Junio Hamano has now entered the discussion with a proposal for a deeper algorithmic optimization. He suggests leveraging reachability information (potentially using bitmaps) to avoid redundant `count_distance()` computations when evaluating merge commits. The insight is that once we know how many commits a merge reaches, we can eliminate entire branches of the commit graph from consideration. This represents a shift from temporary workarounds to potential long-term improvements in the core algorithm.

The thread now has two parallel tracks: Jeff's immediate usability fixes that developers can apply now, and Junio's more fundamental optimization ideas that could yield even greater performance improvements down the line. Both approaches acknowledge the need to balance precision against performance in real-world use cases. While no new patches have emerged since Jeff's initial optimization, the discussion continues to explore how to make bisection practical for repositories with hundreds of thousands of commits.