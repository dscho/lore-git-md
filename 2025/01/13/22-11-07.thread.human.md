Here's the updated human-readable thread summary:

This thread investigates severe performance issues in `git bisect` when used on large repositories like the Linux kernel (over 1 million commits). Initial reports from Askar Safin and Ben Knoble showed bisect initialization taking 21 minutes with full CPU utilization, while simple operations like `git rev-list --count` complete the same range in seconds. Testing across Git versions confirmed this as a longstanding architectural limitation rather than a recent bug.

Jeff King's analysis has now pinpointed the core issue: the bisection algorithm in `do_find_bisection()` performs O(n²) operations by traversing the entire commit graph for each commit when calculating distances, compounded by inefficient flag clearing. While this explains the CPU-bound behavior observed earlier, King notes that even optimized flag management would still leave quadratic complexity. The discussion has progressed from identifying symptoms to understanding the fundamental algorithmic constraints, though no concrete solutions have been proposed yet beyond workaround suggestions like using smaller commit ranges.

The thread remains focused on this performance bottleneck, with participants available for further testing. While the analysis has deepened significantly with King's technical breakdown, the path forward for meaningful optimizations remains unclear given the inherent computational complexity of the current bisect implementation.