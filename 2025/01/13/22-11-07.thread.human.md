This thread explores performance optimizations for `git bisect` in large repositories like the Linux kernel, where the current implementation shows quadratic O(n²) behavior in `do_find_bisection()`. Jeff King identified the bottleneck in flag clearing operations and proposed a patch that reduces runtime by ~40% in tests (16m51s → 9m34s). The discussion has evolved to consider both immediate optimizations and deeper algorithmic changes, with Junio Hamano providing historical context about Git's original "stupid but sufficient" design philosophy.

Askar Safin, who initially reported the performance issues, recently emphasized why this matters: the Linux kernel's contributor guidelines explicitly require each patch to keep the kernel buildable and runnable precisely because `git bisect` is such a critical debugging tool. This reinforces that the performance improvements under discussion aren't just academic—they directly impact a workflow that shapes how thousands of developers contribute to the kernel.

The key technical question remains whether to settle for Jeff King's incremental optimization or pursue more fundamental changes leveraging modern Git features like reachability bitmaps. Junio's historical perspective suggests the community may now be open to architectural improvements that weren't feasible when bisection was first implemented. No new patches have emerged since Jeff's working optimization, leaving the next steps open between consolidating the current approach or prototyping more ambitious solutions.