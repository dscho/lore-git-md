This is a build system patch in Patrick Steinhardt's ongoing Meson transition series (now 5/9). It wires up proper version information propagation when creating distribution archives with `meson dist`.

The patch ensures the generated tarball includes accurate version information by modifying GIT-VERSION-GEN to handle Meson's build directory structure and adding a distribution script to populate the version file. The implementation addresses two technical nuances: handling the MESON_DIST_ROOT environment variable and stripping the "GIT_VERSION=" prefix from the template file.

The changes are minimal (18 lines total) but important for maintaining version consistency in distributed builds. This continues the series' pattern of methodically addressing each build system component with careful attention to edge cases. The commit message clearly explains the rationale and technical tradeoffs involved.