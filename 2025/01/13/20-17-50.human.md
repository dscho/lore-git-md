Build system patch in a series about documentation generation. This is part 6 of a 23-part series (the cover letter was not provided for context). The patch refactors how version and date information is embedded into Git's generated documentation, moving from command-line injection to generating configuration files via `GIT-VERSION-GEN`.

The change makes the build process more maintainable by centralizing version information handling. It adds generated files to .gitignore and updates the Makefile to produce `asciidoc.conf` and `asciidoctor-extensions.rb` from template files. The implementation follows Git's standard pattern for generated files with atomic updates and no-op detection.

Renato Botelho notes in the reply that `contrib/subtree/Makefile` depends on these files and suggests it should trigger their build automatically. This appears to be a straightforward technical improvement to the build system, though the series' broader goals aren't clear without the cover letter. The changes look clean and focused on the kind of build system plumbing that typically gets merged without controversy.