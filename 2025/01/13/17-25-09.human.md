This is a refactoring patch in the ongoing discussion about memory management in Git's ref-filter code. Junio C Hamano is responding to Jeff King's alternative approach to handling the shared state between ref formatting and sorting operations.

The email shows Junio agreeing with Jeff's direction toward better lib-ification by making the ref_format struct more explicit in the API. Jeff's patch modifies ref_sorting_options() to take a ref_format parameter, eliminating the need for static variables while maintaining the connection between formatting and sorting operations. The changes touch several commands (branch, for-each-ref, tag, ls-remote) and include adding the missing format struct to ls-remote.

Junio notes that while René Scharfe's simpler static variable approach deletes more code, Jeff's solution better aligns with long-term goals of moving state into context structures. The maintainer seems receptive to Jeff's approach, calling it "not too bad at all" despite the additional boilerplate required in callers.

This represents progress toward resolving the thread's core issue while also advancing Git's lib-ification efforts. The patch appears well-considered, touching all necessary callers and maintaining existing behavior while improving the API design.