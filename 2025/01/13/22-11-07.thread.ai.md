Here is the updated dense AI thread summary incorporating the new information:

Performance investigation of `git bisect old` operation on large repositories, specifically the Linux kernel repository (535,608 revisions between v6.13-rc7 and v3.0) where setup takes 21 minutes with single-core CPU saturation. Askar Safin reports the issue persists unchanged in Git 2.47.1, confirming the bottleneck remains in `git bisect--helper` during revision walking initialization. Repository state remains consistent across tests: 13M objects, 87k files at HEAD. The problem demonstrates fundamental performance limitations in single-threaded bisect setup for massive histories, with parallelization identified as the most promising solution path. 

Technical details confirmed across both reports: CPU-bound behavior in bisect--helper during setup phase, using newer `old/new` terminology (functionally equivalent to traditional good/bad), and Linux kernel repo as test case. The unchanged behavior across Git versions (2.39.5 → 2.47.1) indicates this is a longstanding architectural limitation rather than a regression. Reporter remains available for additional testing but considers current data conclusive regarding the single-threaded bottleneck. No alternative approaches or workarounds have yet been proposed in the thread. The core issue stands as a clear candidate for parallelization optimization in the bisect machinery, particularly for repositories with hundreds of thousands of commits.