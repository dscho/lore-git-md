Here is the dense AI thread summary capturing all key information from this initial report:

Performance issue with `git bisect old` on large repositories, specifically demonstrated on Linux kernel repository (535,608 revisions between v6.13-rc7 and v3.0) where operation takes 21 minutes with single-core CPU saturation. Reporter provides detailed reproduction case on Debian 12 with Git 2.39.5 (bare metal, tmpfs to eliminate disk I/O) showing `git bisect--helper` as the CPU-bound bottleneck in this single-threaded operation. Repository state: 13M objects, 87k files at HEAD. Suggests parallelization as potential solution. Uses `git bisect new/old` syntax rather than traditional good/bad. Reporter plans follow-up tests with newer Git versions but core issue appears to be lack of parallelism in revision walking during bisect setup phase.