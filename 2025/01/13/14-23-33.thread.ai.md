Updated AI summary:

Thread summary:

Bug report and resolution regarding inconsistent behavior of `git diff --merge-base` with annotated tags between Apple Git (2.39.5) on macOS ARM64 and standard Git on Windows. The original report documented a "fatal: --merge-base only works with commits" error on Apple Git when using annotated tags, while the same command worked on Windows. The reporter provided system details (Darwin 24.2.0, clang 16.0.0) and demonstrated a workaround using `git rev-list` to resolve tags to commits first.

Kristoffer Haugsbakk confirmed this was a known bug fixed in Git 2.43.0 by commit 4adceb5a299 ("diff: fix --merge-base with annotated tags"), which corrected the incorrect rejection of annotated tags when the error message was meant to only block non-commit-ish objects like trees. The reporter's Apple Git-154 version predates this fix, explaining the observed behavior discrepancy. The fix specifically addressed the diff machinery's handling of tag resolution, ensuring annotated tags (which are commit-ish) are properly accepted while maintaining rejection of tree objects (like `v2.48.0^{tree}`).

Key technical details include: the failing command syntax, error message text, version information (Apple Git-154 vs. fixed 2.43.0), the specific fix commit, and the workaround path. The resolution indicates this is not a platform-specific issue but rather a version-specific bug that has been resolved upstream, with upgrading to Git 2.43.0 or later being the recommended solution. The thread provides a complete case study of a cross-platform behavioral difference that was actually a version discrepancy, with the fix properly documented in the Git history.