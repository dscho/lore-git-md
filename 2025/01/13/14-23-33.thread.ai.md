Thread summary:

Bug report regarding inconsistent behavior of `git diff --merge-base` with annotated tags between Apple Git (2.39.5) on macOS ARM64 and standard Git on Windows. The command fails on Apple Git with "fatal: --merge-base only works with commits" when given a tag argument, while working as expected on Windows. The reporter provides detailed system information (Darwin 24.2.0, clang 16.0.0) and demonstrates a workaround using `git rev-list` to resolve tags to commits first. The discrepancy suggests a potential regression or build-specific issue in Apple's Git distribution, though the root cause remains unclear. Key technical details captured include the exact command syntax that fails, the specific error message, version information (Apple Git-154), and the successful workaround path. The cross-platform nature of the failure makes this noteworthy for investigation, particularly for users working across macOS and Windows environments. No responses or analysis from maintainers or other contributors yet.