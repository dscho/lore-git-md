Maintainer approval for credential URL security hardening. Junio Hamano concurs with Johannes Schindelin's analysis that while credential exposure risks differ between clone URLs (commonly auto-saved) and push URLs (explicitly configured), both should trigger the same credential warnings under the proposed `transfer.credentialsInUrl=warn` default. The thread had previously established technical consensus on password detection and warning mechanisms, with this exchange resolving the last open question about URL coverage parity. Hamano's "Amen to that" signals maintainer approval for uniform handling of `.URL` and `.pushURL`, clearing the path for final implementation. This aligns with the security initiative's goal of consistent credential hygiene while respecting that push URLs represent a more deliberate (and thus less risky) configuration choice by advanced users. The discussion confirms the feature's design will cover all URL storage locations without creating unnecessary friction for push operations.