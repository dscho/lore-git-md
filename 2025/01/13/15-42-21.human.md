Build system alignment patch with duplicate submission notice.

Patrick Steinhardt acknowledges that his earlier patch series (which included this change along with others) duplicates Alex Xu's standalone patch moving `config-list.h` from `libgit_sources` to `builtin_sources` in the Meson build configuration. Both changes correctly align the Meson build with Makefile behavior, since this generated file is only used by `builtin/help.c`. 

Steinhardt suggests preferring his series since it includes additional build system adjustments and also addresses a similar issue with "hook-list.h". The exchange serves primarily to coordinate which version should move forward, with no technical disagreement about the correctness of either implementation. This remains a straightforward build system tweak with no runtime impact.