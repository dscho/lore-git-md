This thread addresses a straightforward build system adjustment to properly scope the `config-list.h` dependency in Meson builds, moving it from `libgit_sources` to `builtin_sources` since it's only used by `builtin/help.c`. The change is technically sound but saw an unusual case of duplicate submission - Alex Xu sent it as a standalone patch while Patrick Steinhardt included it in a larger series the same day. Kristoffer Haugsbakk spotted the duplication, prompting Patrick to acknowledge it in a follow-up. Patrick prefers keeping his version since it's part of broader changes that also handle `hook-list.h` similarly. The thread has confirmed both patches are correct and now serves mainly to document the coordination between these parallel efforts. With no technical objections raised, this appears ready to proceed through either submission path.