This is a documentation patch in Jeff King's ongoing combine-diff refactoring series. Patrick Steinhardt notes that it adds a clarifying comment about when the `parent.path` field in `combine_diff_path` gets populated - specifically only during combined diffs with `revs.combined_all_paths` set, and only when the path differs from the post-image (like in renames or copies). 

The change is small and straightforward, just adding a comment to document existing behavior. Patrick observes it might have fit better in the previous commit, but doesn't object to it being a separate patch. This continues the series' pattern of methodically improving the code's clarity and documentation while maintaining behavior.