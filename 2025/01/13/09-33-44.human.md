This is the final version of a bugfix and usability improvement for the `help.autocorrect` configuration. The patch series has gone through several rounds of review and now addresses all technical concerns, with only minor documentation formatting changes in this v4.

The patch finalizes the behavior where `help.autocorrect=1` is now treated as "immediate" execution (a true boolean) rather than a 0.1 second delay, fixing user confusion. It properly handles boolean strings ("true"/"false") alongside numeric values and special keywords ("never", "prompt"), with false booleans mapping to AUTOCORRECT_NEVER. The implementation extracts the parsing logic into a new `parse_autocorrect()` function for clarity.

The documentation has been reorganized to group equivalent values together, making the behavior more intuitive. As Jeff King noted in previous reviews, there's a slight inconsistency between numeric 0 (show suggestion) and boolean false (hide completely), but this was deemed reasonable given user expectations.

All substantive technical issues from earlier rounds have been resolved, and this version just incorporates the final documentation tweak suggested by Jeff King to fold the "immediate" line. The thread has reached consensus and this appears ready for merging.