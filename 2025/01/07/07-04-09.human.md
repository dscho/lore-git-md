This is a follow-up documentation and cleanup patch series (3/3) that applies cosmetic improvements to the recently merged LSan race condition fixes. The series builds on the now-finalized technical solution for LeakSanitizer false positives in threaded test execution.

The patches make three small improvements to the leak detection code in `test-lib.sh` and `test-lib-functions.sh`:
1. Inverts the return value logic of `check_test_results_san_file_empty` to be more intuitive (true when files are empty)
2. Simplifies the LSan results checking code flow
3. Adds explanatory comments to document the LSan log checking behavior

These changes don't modify the functionality - they just improve code readability and maintainability based on Patrick Steinhardt's review feedback. The patches apply cleanly to the already-merged `jk/lsan-race-ignore-false-positive` branch (now in master), showing how the Git project continues to polish implementations even after technical consensus is reached.

The changes are straightforward and unlikely to be controversial, serving as a documentation capstone to the larger LSan thread race series. They demonstrate Git community's attention to code quality and long-term maintainability.