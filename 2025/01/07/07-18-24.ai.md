Test modernization patch updating grep calls to use test_grep in t7407-submodule-foreach.sh.

This is a straightforward test improvement that replaces raw `grep` calls with the Git test suite's `test_grep` helper in two test cases. The change provides better failure output when tests fail. The author notes that one of the original calls used `-sq` flags unnecessarily - `-q` suppresses output that would be useful in verbose mode, while `-s` suppresses errors that shouldn't occur since the file being grepped is generated by the test itself. 

The patch touches two test cases that verify how submodule foreach handles option-like arguments. Both tests check that arguments containing dashes are properly passed through the submodule commands. The first test verifies basic functionality while the second checks recursive behavior. The grep calls were being made against expected output files to double-check the results of Git commands that generate those files.

This appears to be a minor but thoughtful cleanup noticed while working on unrelated changes. The commit message clearly explains both the technical improvement (switching to test_grep) and the rationale for removing the questionable flags. The changes are limited to test infrastructure with no impact on production code.