Performance optimization series (final documentation polish). This is the third and final patch in a post-merge refinement series addressing LSan (LeakSanitizer) race conditions in threaded test execution.

Jeff King adds explanatory comments to clarify the purpose of an obscure `grep` pipeline in the LSan log checking code. The pipeline was introduced in an earlier commit to filter out false positives from the sanitizer's internal thread stack tracing, specifically ignoring leaks from `sanitizer::GetThreadStackTopAndBottom`. The new comments serve two purposes: they document that the `DEDUP_TOKEN` lines represent unique leaks we want to detect, while also noting the intentional suppression of sanitizer-internal false positives.

This patch follows the technical implementation work that was already merged (6 patches) and two earlier post-merge refinement patches that improved return value logic and process efficiency. The change is purely documentation-focused, adding context that would help future maintainers understand the rationale behind the filtering without having to dig through commit history. The implementation itself remains unchanged from the optimized version established in previous patches.

Key technical details preserved from the series include the DEDUP_TOKEN-based parsing architecture and the maintainer-approved tradeoff between diagnostic granularity and false-positive filtering. The comment references both the original implementation commit (b119a687d4) and the current context, providing breadcrumbs for deeper investigation while keeping the immediate code more self-documenting.

This concludes the post-merge refinement work, with all architectural decisions from the original series remaining intact while the codebase gains improved documentation and long-term maintainability. The patch exemplifies the project's attention to code clarity even after technical consensus has been reached on an implementation.