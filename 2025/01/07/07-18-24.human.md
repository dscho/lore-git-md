Test modernization patch improving test output in t7407 (submodule tests). Jeff King replaces direct `grep` calls with the more informative `test_grep` helper in two test cases, which will produce better failure output. He also removes the unnecessary `-sq` flags from one grep call, noting they don't provide value in this context (output is already handled by the test framework, and errors shouldn't occur on the test-generated file). The changes are straightforward and improve test maintainability, though the commit notes the test's unusual structure of grepping an expected output file is itself a bit odd (but preserved here as it serves a purpose). A minor but thoughtful cleanup from an experienced contributor.