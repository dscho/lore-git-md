This is a technical follow-up in the ongoing discussion about fixing the flaky t7422 submodule test related to SIGPIPE handling. Patrick Steinhardt responds to Jeff King's concerns about test fragility by proposing a more complex but targeted solution.

The patch discussion has reached a point where both contributors recognize the tradeoffs between different approaches. Patrick's latest iteration involves creating 2500 artificial submodules (~150kB output) using `git hash-object` and `git mktree`, with strategic sleeps around the `grep` call to ensure reliable SIGPIPE testing. This specifically targets the behavior fixed in commit 082caf527e (submodule status: propagate SIGPIPE).

While acknowledging the implementation complexity, Patrick argues this approach better preserves the submodule testing context compared to Jeff's preferred general-purpose `{ yes || true; }` pattern. The email reveals Patrick is still refining the exact setup (noting the submodules need to be recursive) but maintains the core concept is sound.

This exchange represents the technical depth of Git's testing infrastructure discussions, where contributors weigh test specificity against maintainability. The solution being debated would create one of the more elaborate test setups in the codebase, solely to verify proper SIGPIPE propagation in submodule operations - demonstrating the project's commitment to robust edge-case testing despite implementation costs.