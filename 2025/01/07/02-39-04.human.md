This is a technical discussion about fixing a flaky SIGPIPE test in the Git test suite, specifically in t7422 (submodule output testing). Jeff King responds to Patrick Steinhardt's earlier patch attempt, digging deeper into why the proposed solution using Perl to pre-fill the pipe buffer doesn't fully solve the race condition.

The core issue involves ensuring SIGPIPE is properly propagated in recursive submodule operations. Jeff demonstrates that even with the Perl pre-fill approach, there remains a race where the submodule process might complete its output before the grep process exits, causing the test to fail intermittently. He provides a concrete example using `sleep` to force the failure case.

Jeff explores several angles to make the test truly robust but concludes there may not be a portable way to reliably trigger SIGPIPE in the specific subprocess context the test wants to verify. The discussion shows the subtle complexities involved in testing signal handling across processes with buffered I/O, and suggests this might need to remain an acknowledged-but-unfixable flakiness unless someone finds a clever solution.

This email represents a deep technical dive into a tricky edge case that emerged during Patrick's CI modernization series, showing the thoroughness of Git's test suite and the attention to detail in its development process.