This is a bugfix discussion about GPG key handling in `git tag -s`. Jeff King (peff) responds to Carlos Maiolino's investigation of Git silently falling back to a different signing key when the configured smartcard-stored key is unavailable. 

The core issue is whether Git or GPG is responsible for the unexpected fallback behavior. Peff clarifies Git's role is limited to passing the configured `user.signingKey` to GPG without any fallback logic. He seeks concrete reproduction steps to determine if the problem lies in Git config handling or GPG's key selection. 

The discussion highlights a security-sensitive behavior gap: when hardware-backed keys are unavailable, users likely expect either failure or clear warnings rather than silent fallback to alternate keys. Peff acknowledges the current behavior isn't ideal but needs more data to pinpoint where the fix belongs - in Git's GPG invocation or GPG itself. 

This continues the thread's technical investigation into reliable signing behavior, with the next steps depending on whether Carlos can provide the requested command traces showing exactly when and how the key gets substituted.