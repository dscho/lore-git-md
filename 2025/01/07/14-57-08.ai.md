This is a review response in the ongoing ref consistency checking series, specifically addressing patch 2/10 which introduced `get_worktrees_without_reading_head()` to avoid premature termination during packed-refs verification. Karthik Nayak raises a fundamental design question about whether bypassing HEAD validation is the right approach, suggesting that an invalid repository state (where HEAD verification would fail) might indicate the repository is too broken to meaningfully run `git refs verify` at all.

The key technical concern revolves around whether exposing internal worktree functions to tolerate invalid repositories is justified, given that the primary goal is to report all ref validation errors rather than fail fast. The patch leverages commit 465a22b338's `get_worktrees_internal()` to skip HEAD reading, but the reviewer questions if this violates the principle that ref verification should operate on a minimally valid repository.

This discussion point touches on deeper questions about error recovery strategy in Git's verification tools - whether to fail fast on fundamental corruption or attempt comprehensive reporting even in degraded states. The thread context shows this is part of a larger effort to add systematic ref validation, with patch 2 specifically addressing an edge case where packed-refs parsing during worktree HEAD access could terminate the verification process prematurely.