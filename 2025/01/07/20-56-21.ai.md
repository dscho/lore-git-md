This is a response in the reftable entropy handling bugfix thread, where Junio C Hamano questions the approach of the solution. The email expresses strong skepticism about the need to accommodate platforms where `rand(3)` fails or aborts, suggesting instead that such platforms should fix their implementations rather than Git working around them.

Key technical points raised:
- The reftable retry delay calculation doesn't need cryptographic randomness, just predictable/repeatable values
- Using `git_rand()` with the new insecure flag introduces unnecessary dependency on Git infrastructure
- The change goes against reftable's trend to reduce dependencies on the rest of Git
- While accepting the first part of the series (general CSPRNG changes), Junio suggests pushing back on this specific application

The response maintains the security boundary discussion from earlier in the thread but introduces a new architectural concern about dependency direction in the reftable code. This represents a potential shift in the review discussion from purely technical implementation details to broader design philosophy questions about platform support requirements versus codebase purity.

The email doesn't propose an alternative solution but strongly implies that using plain `rand(3)` would be preferable for this non-cryptographic use case, if platforms can be relied upon to implement it correctly. This may prompt discussion about whether Git's compatibility requirements should include working around broken standard library implementations.