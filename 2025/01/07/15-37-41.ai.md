This is a reply in the reftable entropy handling bugfix thread, specifically addressing part 2 of Patrick Steinhardt's series that modifies reftable stack operations to use insecure random bytes when system entropy is exhausted.

Randall Becker confirms the patch addresses the observed test failures on NonStop systems where concurrent processes were exhausting entropy pools. He notes the solution appears correct and plans to retest on both affected platforms (NonStop and x86), though expecting different behavior on x86 due to hardware randomizer differences in OpenSSL.

The technical context remains unchanged from the thread summary: the patch modifies two reftable operations (retry delays during stack reloads and temporary filename generation) to use the new `CSPRNG_BYTES_INSECURE` flag introduced in part 1. The changes maintain the same mathematical behavior while allowing operation to continue when entropy is exhausted.

Key points from this reply:
- Provides real-world confirmation that the patch addresses the reported issue
- Indicates the problem may manifest differently across platforms (NonStop vs x86)
- Suggests broader test impact beyond the originally reported case
- No new technical concerns raised about the implementation

The reply serves as positive validation of the patch's effectiveness while highlighting the need for cross-platform verification. It doesn't introduce new technical discussion points but reinforces the practical importance of the fix.