Here is the dense AI thread summary:

Bug report from Keith Mendoza about a Git crash in `git restore --staged -p` during merge conflicts. When attempting interactive patch selection on an unmerged file after a merge conflict, Git hits an assertion failure in add-patch.c and aborts with a core dump. The crash occurs specifically when the diff starts with an "Unmerged path" line that the patch selection UI doesn't handle properly. Reproduction shows this affects Git 2.39.5 on Linux/WSL2. The bug manifests when combining `--staged` with `-p` on conflicted files, suggesting an edge case in the interaction between merge conflict handling and the interactive patch interface. The report includes clear reproduction steps, expected vs actual behavior, system information, and a core dump - indicating a legitimate bug affecting users trying to selectively stage portions of conflicted files. No responses or analysis from other contributors yet.