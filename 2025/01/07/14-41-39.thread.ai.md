Here is the updated dense AI thread summary:

Bug report from Keith Mendoza about a Git crash in `git restore --staged -p` during merge conflicts, now confirmed as fixed in newer versions. The original report described an assertion failure in add-patch.c when attempting interactive patch selection on unmerged files after a merge conflict, specifically when diffs contained "Unmerged path" lines that weren't properly handled. Reproduction showed the issue affected Git 2.39.5 on Linux/WSL2 when combining `--staged` with `-p` on conflicted files.

Kristoffer Haugsbakk identified this as a known issue fixed in v2.41.0 (commit 28d1122f9ca) that was introduced when the interactive patch interface became default in v2.37.0. The fix properly handles unmerged path markers in diff output. Technical details confirm the crash occurred in add-patch.c during conflict marker processing, with affected versions spanning v2.37.0 through v2.40.x. A clean reproduction demonstrates the fix works in v2.48.0-rc2, though the original reporter experienced the issue on an older version (v2.39.5). The thread establishes this was a real user-impacting bug causing core dumps during selective staging of conflicted files, with Jeff King's fix having been available since mid-2023 but not yet reaching all users via maintenance releases.