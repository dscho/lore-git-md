Here's a human-readable thread summary:

A user is asking about Git's status reporting capabilities, specifically whether it's possible to configure `git status` to show only ignored files while excluding untracked files from the output. They've experimented with various combinations of flags in the `--porcelain=v2` output format, including `--branch`, `--show-stash`, `--ignored=matching`, and different `--untracked-files` settings, but haven't found a working combination. Their attempts to use `--ignored=matching` with `--untracked-files=no` resulted in an error, suggesting this particular combination isn't supported. The example output they provided shows both untracked items (marked with "?") and ignored files (marked with "!"), which isn't what they want. This appears to be a genuine question about existing functionality rather than a bug report or feature proposal, touching on the interaction between different status reporting options in Git's porcelain output format.