Thread identity: User question about `git status` functionality for showing only ignored files without untracked files.

Core problem: The user wants to configure `git status` to display only explicitly ignored files (marked with "!" in porcelain output) while suppressing untracked files (marked with "?"). Current attempts using `--porcelain=v2` with various flag combinations either show both types or produce errors when trying to combine `--ignored=matching` with `--untracked-files=no`.

Technical details: The user has experimented with multiple flag combinations in `git status --porcelain=v2` including `--branch`, `--show-stash`, `--ignored=matching`, and `--untracked-files` with both "normal" and "no" values. The error occurs when attempting to combine `--ignored=matching` with `--untracked-files=no`, suggesting this combination may not be supported in current Git versions.

Current status: The thread begins with this user question; no responses or solutions have been provided yet. The core issue appears to be a limitation in how Git's status reporting options interact when trying to isolate ignored files from untracked files in porcelain output.

Open questions: Whether there exists a supported way to achieve the desired output format showing only ignored files, and if not, whether this represents a missing feature or intentional design choice in Git's status reporting.