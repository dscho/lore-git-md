Reftable entropy handling discussion reaches architectural impasse (v3) with clarified technical scope and persistent philosophical divide. Patrick Steinhardt's solution for OpenSSL CSPRNG failures now specifically contested on reftable subsystem boundaries after Junio Hamano's refined objections, despite technical validation from Randall Becker on NonStop systems.

Core changes and status:
- Part 1 (merged) modified Git's cryptographic random number generation with `CSPRNG_BYTES_INSECURE` flag in `csprng_bytes()`
- Part 2's reftable application narrowed to two non-cryptographic uses in reftable/stack.c:
  1. Retry delay calculation (randomness spreads out retries)
  2. Temporary filename generation (randomness prevents collisions)
- Junio maintains strong architectural objection to reftable depending on Git's CSPRNG layer despite accepting the infrastructure changes
- Clear distinction established between OpenSSL's PRNGD server issues (validated on NonStop) and general `rand()` functionality

Technical specifics:
- OpenSSL properly detects insecure state via `RAND_pseudo_bytes()`
- Reftable changes maintain mathematical behavior (`UINT32_MAX` for delays)
- Junio clarifies actual concern is about `git_rand()` usage, not `rand()` as previously discussed
- Two specific code locations in reftable/stack.c under architectural scrutiny

Key positions:
- Patrick Steinhardt: Maintains comprehensive solution addressing real platform failures through unified CSPRNG approach
- Randall Becker: Confirms PRNGD fragility on ia64 NonStop under load, validating technical need
- Junio Hamano: Firm on subsystem boundaries, proposes alternatives:
  - Standard library `rand()` for non-crypto needs
  - Constant values where randomness isn't critical
  - Platform responsibility for fixing broken implementations

Open questions sharpened:
- Architectural boundary: Is reftable's operational need sufficient to justify crossing CSPRNG dependency line?
- Implementation alternatives: Would `rand()` or constant values adequately serve reftable's non-cryptographic needs?
- Platform policy: Should Git work around PRNGD/server entropy issues or require platforms to fix implementations?
- Scope clarification: Confirmed discussion focuses specifically on `git_rand()` in reftable, not general `rand()` usage

The discussion has crystallized into a clear architectural decision point, with Junio drawing a bright line around reftable's dependencies while Patrick advocates for a comprehensive solution to observed platform failures. The path forward now requires either: 1) demonstrating why reftable's needs justify crossing the dependency boundary, 2) finding an alternative solution within Junio's constraints, or 3) revisiting the platform responsibility question. The technical validation remains undisputed, but the architectural fit continues to be the primary contention.