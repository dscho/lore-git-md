This is a test infrastructure patch addressing a flaky submodule SIGPIPE test, continuing the technical discussion between Patrick Steinhardt and Jeff King about reliable signal testing patterns.

Patrick proposes a new approach to stabilize the problematic t7422 test case that verifies SIGPIPE propagation in recursive submodule status checks. His solution generates 2500 artificial submodules to produce ~150kB of output, combined with strategic sleeps around the `grep` call that triggers the signal. The implementation uses `git hash-object` and `git mktree` for efficient setup, though Patrick acknowledges the complexity this adds to the test.

The patch represents a middle ground between Jeff's preferred `{ yes || true; }` pattern (which Patrick finds too generic) and the original fragile test. Key technical elements include the `.gitmodules` blob creation, tree construction with 2500 submodule entries, and careful timing control via pre/post-grep sleeps. While this provides specific coverage for commit 082caf527e's SIGPIPE fix, Patrick expresses reservations about whether the test's value justifies its complexity.

This email continues the thread's evolution from a specific test fix toward establishing patterns for reliable signal testing. The discussion now focuses on tradeoffs between test specificity, implementation complexity, and coverage guarantees, with Patrick leaning toward accepting this version despite its intricacies. The solution remains tightly coupled to the submodule context rather than adopting Jeff's more general signal-testing pattern.