This is a user question about Git's status reporting functionality, specifically regarding how to show only ignored files without untracked files.

The email asks whether there's a way to configure `git status` to display only explicitly ignored files, without showing untracked files. The user demonstrates their current approach using `--porcelain=v2` output with various flags, but notes they're still getting untracked files (marked with "?") in the output alongside the ignored files (marked with "!").

Key technical details include:
- Current command attempts using `--porcelain=v2` format
- Flags tried: `--branch`, `--show-stash`, `--ignored=matching`, and `--untracked-files` with both "normal" and "no" values
- The error received when trying to combine `--ignored=matching` with `--untracked-files=no`
- Example output showing both untracked files/dirs (`.gitignore`, `Configs/`, `Logs/`) and ignored files (`Configs/non-tracciarmi.txt`)

This appears to be a genuine question about existing functionality rather than a patch proposal. The user seems to have hit a limitation in Git's status reporting options where the combination of flags they want to use isn't supported. The question touches on the porcelain output format and the interaction between the `--ignored` and `--untracked-files` options.