This thread documents the ongoing conversion of Git's test infrastructure to use the `clar` unit test framework. The discussion began with a resolved debate about enforcing strict C function formatting in test declarations, where the project settled on maintaining conventional formatting standards. 

The current focus is a mechanical conversion of hash algorithm tests from the old t-*.c format to the new u-*.c format under `clar`. Seyi Kuforiji's latest patch (v2) shows a 79% similarity to the original tests, indicating a substantial but careful rewrite that preserves the test logic while adapting to the new framework. The changes include splitting monolithic test functions into focused cases (like `test_hash__empty_string`), replacing assertions with `clar`'s `cl_assert` macros, and updating build system references. 

The conversion follows established patterns from previous test migrations under Patrick Steinhardt's guidance. While the changes are largely mechanical, reviewers should verify that all original test coverage is maintained and that the new assertions properly capture the test conditions. With the formatting standards debate already settled and the current changes being routine maintenance, this thread appears to conclude once the technical review confirms the conversion's correctness.