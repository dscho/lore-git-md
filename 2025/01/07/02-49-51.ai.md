This is a technical follow-up in the CI modernization thread regarding SIGPIPE test reliability. Jeff King points out that Patrick Steinhardt's proposed fix for the flaky t7422 submodule test (using Perl to fill the pipe buffer) doesn't fully resolve the underlying timing issues. While the patch addressed one race condition (grep starting too slowly), Jeff notes the fundamental race with process exit timing remains, referencing his earlier v1 thread analysis.

The discussion continues refining the approach to reliably testing SIGPIPE propagation in submodules, with Jeff having previously proposed a more robust pattern using `{ yes || true; }` piped to `true` that avoids buffer size guessing and read-side timing dependencies. This exchange highlights the nuanced challenges in creating deterministic tests for asynchronous process behavior, particularly around pipe buffering and signal delivery timing. The thread has evolved from specific test fixes to establishing general best practices for SIGPIPE testing patterns.