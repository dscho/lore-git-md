This is a user question about `git status` behavior rather than a patch submission. The author is asking how to make `git status` show only explicitly ignored files (marked with "!" in porcelain v2 output) while suppressing untracked files (marked with "?"). 

The user has tried two approaches: one with `--untracked-files=normal` which shows both ignored and untracked files (undesired), and another with `--untracked-files=no` which fails with a fatal error about unsupported argument combination. The example shows they want to see just the "!" line for `Configs/non-tracciarmi.txt` without the "?" lines for other untracked items.

This appears to be a genuine limitation in Git's current command-line interface - there doesn't seem to be a supported way to show only ignored files while completely hiding untracked ones. The error suggests these options are intentionally mutually exclusive in the current implementation. The question may prompt discussion about whether this should be considered a bug or if there's an undocumented workaround.

The email is straightforward and shows the user has done reasonable experimentation before asking. It's the kind of question that might get a quick answer from experienced contributors familiar with status.c internals, or could surface a real gap in Git's porcelain options.