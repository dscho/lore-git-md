This is a technical follow-up in the ongoing discussion about fixing a flaky submodule test (t7422) related to SIGPIPE handling. Patrick Steinhardt responds to Jeff King's concerns about test fragility by proposing a more complex but targeted solution.

The patch would create 2,500 artificial submodules (~150kB of output) to reliably trigger the SIGPIPE condition that commit 082caf527e fixed, using `git hash-object` and `git mktree` for efficient setup. While acknowledging the implementation complexity, Patrick argues this maintains specific test coverage for the submodule SIGPIPE fix, preferring it over Jeff's suggested more generic `{ yes || true; }` approach.

The email shows Patrick recognizing a needed adjustment (making the submodule itself contain the recursive submodules) while standing by the core approach. This represents a middle ground in their technical debate - more complex than Jeff's general solution but more specific to the actual submodule behavior being tested. The discussion continues weighing test specificity against implementation complexity.