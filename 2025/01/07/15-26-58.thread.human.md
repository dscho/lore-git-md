Patrick Steinhardt's reftable entropy handling series has progressed with part 2, building on the infrastructure introduced in part 1. The thread began with a modification to Git's cryptographic random number generation to allow insecure fallback when system entropy is exhausted, specifically addressing OpenSSL CSPRNG failures. This foundational change added a `CSPRNG_BYTES_INSECURE` flag while maintaining strict security boundaries.

The second patch now applies this capability to the reftable stack, solving concrete test failures observed when multiple processes exhaust the entropy pool. It targets two specific use cases where cryptographic strength isn't required: retry delays during stack reloads and temporary filename generation. The changes are carefully scoped, with the commit message providing a reproduction scenario showing the original failures on NonStop systems using OpenSSL.

The series appears well-considered, with each part maintaining clear security boundaries and thorough documentation. Part 2's modifications to `reftable_stack_reload_maybe_reuse()` and `format_name()` demonstrate the intended use of the new infrastructure from part 1, solving real-world test failures while keeping the security implications minimal. No open questions have been raised by reviewers yet, though the thread remains open for feedback on the implementation details.