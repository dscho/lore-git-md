This is a follow-up discussion in the Windows reftable compatibility thread, questioning the approach of the proposed solution. Johannes Sixt (Hannes alias in email) raises two key points about Patrick Steinhardt's patch that modifies Windows' unlink behavior for reftables:

First, he asks why recent changes to Windows' atomic rename support (commit 391bceae) don't already address this issue, suggesting there might be overlap in the solutions. Second, he challenges the patch's strategy of handling the problem in the compatibility layer rather than fixing it directly in the reftable code where the Windows file-handling awareness exists.

The exchange highlights an architectural discussion about where platform-specific behavior should be handled - whether at the compatibility layer that abstracts OS differences or within subsystems that explicitly manage platform constraints. Johannes appears to favor keeping such fixes in the reftable layer where the Windows file-locking behavior is already understood and managed through mechanisms like `reftable_stack_clean()`.

This moves the thread from technical implementation details into a design philosophy discussion about responsibility boundaries between core Git and its platform compatibility layers.