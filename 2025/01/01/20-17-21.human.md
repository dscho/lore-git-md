Performance optimization patch in a series addressing LSan test reliability. This is patch 4/6 in Jeff King's series tackling LeakSanitizer race conditions in Git's test suite. The change simplifies leak detection logic in `test-lib.sh` by removing a now-unused leak counting function (`nr_san_dir_leaks_`) and consolidating it into the existing emptiness check.

The patch reflects an evolution in how Git's test infrastructure handles leak reports - where we previously tracked the number of leak reports, we now only care whether any exist at all. This cleanup prepares for further refactoring by eliminating redundant code paths. The author notes some potential confusion around the inverted grep logic (using `-qv` with a leading `!`) and floats an alternative naming scheme that might be clearer.

This appears to be a straightforward internal cleanup with no behavior change, part of the larger effort to make LSan results more reliable when running tests with threading. The change is small (13 lines removed, 8 added) and focused solely on test infrastructure.