This is a refactoring patch that improves the internal structure of the `set_head()` function in `builtin/fetch.c`. It's part of Bence Ferdinandy's ongoing work to fix HEAD symref handling in bare repository mirror operations.

The patch simplifies the function signature by passing the entire `remote` struct instead of individual parameters (`follow_remote_head` and `no_warn_branch`). This makes the code more maintainable by:
1. Centralizing remote property access through the struct
2. Eliminating the need to access `gtransport->remote` directly
3. Making the function's dependencies more explicit

The changes are mechanical - the behavior remains exactly the same, just with cleaner internal organization. This appears to be a preparatory step for further improvements to the mirror behavior fix that was previously discussed in the thread. The patch is small (15 lines changed) and focused, with no test changes needed since it's purely a refactoring.

Given that this comes from an established contributor addressing a known issue, and that the changes are straightforward and well-contained, this looks like a solid incremental step in the solution. The patch follows the style of the surrounding code and maintains all existing functionality while improving maintainability.