This is a platform compatibility patch (part 3/4 in a series) addressing MSVC compiler warnings in `apply.c`. The changes focus on fixing type conversion issues where 64-bit values were being truncated to 32-bit integers, and switching from `int` to `size_t` for variables storing `strlen()` results where negative values aren't possible.

The patch makes systematic changes across the apply machinery, updating function signatures and variable types to use more appropriate integer types. Key changes include converting `unsigned long` to `size_t` for length calculations, adjusting the return type of `parse_git_diff_header()`, and fixing hash algorithm hex length handling. The modifications appear focused and mechanical, with no behavioral changes intended.

This looks like a straightforward cleanup to improve Windows/MSVC compatibility. The changes are consistent with the stated goal of silencing compiler warnings while maintaining correctness. Since this is part of a series, readers may want to check the cover letter (if available) for broader context about the MSVC compatibility effort.