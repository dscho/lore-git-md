Platform compatibility patch addressing MSVC warnings in the interactive patch adding subsystem (`add-patch.c`). This first patch in Sören Krecker's v3 series focuses on type conversion issues between 64-bit and 32-bit integers, changing `unsigned long` to `size_t` for cross-platform consistency. The changes affect hunk header parsing and display logic, introducing a new `str_to_size_t` macro for safe string-to-size_t conversion with overflow checking. Key modifications include updating the `hunk_header` struct fields, `parse_range()` function, and various printf format strings to use `PRIuMAX` for portable size_t printing. The patch also updates the pluralization helper `Q_()` in `gettext.h` to use `size_t` for its count parameter. While mechanically straightforward, the changes touch core interactive patch handling paths, requiring careful review to ensure no behavioral changes in hunk number parsing or display. The patch maintains the series' focus on MSVC compatibility without altering functionality.