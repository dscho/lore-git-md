This is a bugfix discussion regarding fsck.skipList configuration validation.

The thread discusses how Git should handle invalid fsck.skipList configurations, particularly when the value is missing entirely (just "skipList" with no path specified). Junio Hamano notes that this is clearly a misconfiguration - Git expects a path but gets a valueless entry instead. The code already dies when it can't open a specified path, so there's no reason to tolerate this malformed case.

Toon Claes confirms agreement after testing the behavior and reviewing the code. This appears to be a straightforward cleanup to make configuration validation more consistent, with both participants in agreement about the approach. The change would make Git fail fast on obviously wrong configurations rather than potentially proceeding with undefined behavior.