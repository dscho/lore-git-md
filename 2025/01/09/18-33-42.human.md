This is a follow-up discussion on the build system race condition fix for parallel version generation. Martin Ågren acknowledges Johannes Schindelin's clear explanation of the race condition that occurs during parallel documentation builds (`make -j2 html man`), while noting the broader issue of recursive make's inherent problems.

Martin points out an additional consideration: the current `.gitignore` includes a pattern (`*+`) to catch temporary files with `+` suffixes that might linger after interrupted builds. He suggests an alternative approach of using `"$OUTPUT"."$$"+` as the temporary filename pattern, which would maintain the existing `.gitignore` coverage while still solving the Windows filename constraints. This would preserve the ability to catch stray temporary files while only slightly complicating the filename pattern.

The email doesn't propose changing the current patch but raises this as a potential refinement that could prevent any regression in temporary file handling. The discussion remains technical and constructive, focused on edge cases in the build system's file handling behavior.