Bugfix refinement for `git blame`'s OID formatting edge cases. This is a follow-up to Patrick Steinhardt's v2 patch addressing an out-of-bounds read when using large `--abbrev` values in `git blame`.

Johannes Schindelin points out a subtle but important correction needed in the test case from Steinhardt's v2 patch. The test was meant to verify proper handling of boundary commits (marked with ^ prefix) when truncating excessively large abbrev values, but accidentally tested the wrong invocation. Schindelin notes the test should use `HEAD..` rather than just `HEAD` to properly exercise the boundary commit case that originally exposed the formatting issue.

This continues the thread's careful attention to edge cases in OID output formatting, particularly around the interaction between prefix characters (^, *, ?) and length truncation. The discussion remains focused on ensuring robust test coverage while maintaining the security fix against buffer overreads. Schindelin's observation suggests the test case needs adjustment to properly verify the full scope of the fix, though the core solution (reverting to `printf` with length truncation) remains technically sound.

The exchange demonstrates the meticulous review process for security-sensitive formatting code, where even test case construction details matter for verifying edge case behavior. This follows earlier discussions about whether to allow `--abbrev` values exceeding `GIT_MAX_HEXSZ` at all, though that remains a separate design question not addressed in this immediate fix.