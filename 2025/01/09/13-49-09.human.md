This is a refinement in an ongoing discussion about `git blame`'s handling of object ID abbreviations. The thread has been focused on fixing an out-of-bounds read issue when using large `--abbrev` values, with careful attention to maintaining proper alignment for special commit markers (^, *, ? prefixes).

Johannes Schindelin responds to Shejialuo's observation about allowing `--abbrev` values exceeding `GIT_MAX_HEXSZ`, explaining the practical reason behind this design choice. When using range notation (HEAD..) with SHA-1 repos, Git prefixes boundary commits with ^ and truncates the last hex digit to maintain alignment - meaning the full 40-character hash needs to be available even when the output shows 39 characters. This subtle formatting requirement justifies supporting values up to the full hash length.

The discussion shows the patch series is nearing completion, having addressed both the security issue (buffer overread) and the formatting edge cases. The remaining conversation focuses on understanding the design rationale rather than proposing further code changes, suggesting the technical solution is sound. This level of attention to detail is characteristic of security-sensitive output formatting code in Git.