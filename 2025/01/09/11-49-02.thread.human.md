Here's a human-readable summary of the thread:

The thread began with Danila Manturov reporting a regression in `git fetch` behavior for shallow clones between versions 2.47.1 and 2.48.rc0, where tags pointing to the shallow boundary commit were no longer being fetched with `--depth=1`. The issue was demonstrated using a test repository with tags on both HEAD and the initial commit.

Bence Ferdinandy responded that this was already fixed by commit 6c915c3f85 ("fetch: do not ask for HEAD unnecessarily"), which addressed an issue where unnecessary HEAD requests in `ls-remote` were interfering with tag fetching logic. This fix was included in the final 2.48 release.

Danila has since followed up with additional CI testing that pinpoints commit 5f212684abb6 (the same commit originally suspected) as the exact point where the regression was introduced. The issue specifically manifests when fetching without an explicit refspec, suggesting the problem involves how default refspecs interact with shallow clone tag fetching.

While the regression has been fixed in 2.48, there remains some uncertainty about whether the fix fully covers the shallow clone edge case demonstrated in the original report. The thread shows good collaboration in identifying both the source of the regression and its resolution, though the specific shallow clone scenario may need additional verification.