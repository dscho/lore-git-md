Refactoring patch introducing a constructor for the `combine_diff_path` struct as part of the combine-diff memory management improvements series.

This second patch in the 14-part series introduces `combine_diff_path_new()`, a constructor function that centralizes initialization of the variable-sized `combine_diff_path` struct. The struct contains both a path string and a variable-length array of parent entries, making manual initialization error-prone. The new constructor handles the tricky pointer arithmetic for the path field (which sits after the parent array) and ensures all fields are properly zeroed, fixing some cases where parent arrays were left uninitialized.

Key technical changes include replacing manual allocations in `combine-diff.c` and `diff-lib.c` with calls to the new constructor, switching from `int` to `size_t` for length parameters, and keeping the existing `combine_diff_path_size()` macro public for now. The patch explicitly leaves `path_appendnew()`'s memory-reuse optimization untouched for future refactoring, noting its different allocation patterns.

The changes reduce code duplication and improve consistency, though no actual bugs were fixed beyond the initialization gaps. The diff shows a net reduction of 37 lines across three files while making the code more maintainable. This prepares for later patches that will rely on consistent struct initialization.