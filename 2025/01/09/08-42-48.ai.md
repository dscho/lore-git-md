This is a documentation patch in the ongoing combine-diff refactoring series. It adds clarifying comments about the `parent.path` field in the `combine_diff_path` struct, which was recently modified in patch 04 of the series.

The patch documents that the per-parent path field is only populated in specific cases: when performing a combined diff with `revs.combined_all_paths` enabled, and only when the path differs from the post-image (such as in rename or copy cases). Otherwise the field remains NULL. This clarifies behavior that was previously implicit in the code.

The change is straightforward - it adds a 6-line comment to `diff.h` explaining the field's usage. The comment helps maintainers understand the invariant established by the earlier patches in the series, particularly the strbuf-to-pointer conversion in patch 04. The documentation appears accurate based on the surrounding context of the refactoring work.

This is a minor but useful addition to the series, providing explicit documentation for behavior that was previously only evident by reading the implementation code. The patch follows the series' pattern of improving code clarity and maintainability through small, focused changes.