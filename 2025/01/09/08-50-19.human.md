This is patch 10/14 in a refactoring series improving Git's combine-diff implementation, focusing on memory management cleanup. Jeff King removes the public declaration of `combine_diff_path_size()` since all callers now use the preferred `combine_diff_path_new()` constructor instead. The change inlines the size calculation directly into the constructor and optimizes the math by reusing an intermediate multiplication result.

The patch continues the series' pattern of simplifying the API while maintaining functionality - here by eliminating an exposed helper function that's no longer needed. The diff shows the straightforward changes: removing the macro from diff.h and adjusting the allocation math in combine-diff.c. This follows logically from earlier patches that migrated all callers to use the constructor pattern consistently.

As with previous patches in the series, this appears to be a well-considered change that improves code hygiene without introducing new complexity. The commit message clearly explains the motivation and benefits of the change. Given Jeff King's active involvement in reviewing and now contributing to this series, the change carries additional credibility.