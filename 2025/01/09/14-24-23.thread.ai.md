Build system race condition fix for version generation during parallel builds, particularly affecting Windows. The thread originates from a patch modifying `GIT-VERSION-GEN` to use process-specific temporary filenames (`.$$` suffix) instead of static `+` suffix, addressing both parallel build collisions and Windows filename constraints. The issue emerged when `GIT-VERSION-GEN` was extended to generate version strings for documentation files (`asciidoctor-extensions.rb` and `asciidoc.conf`), creating race conditions during concurrent `make -j2 html man` invocations. The minimal 4-line fix preserves the current build architecture while solving the immediate problem, with acknowledgment that a more comprehensive build system redesign would be preferable but is deferred beyond the current release cycle.

Martin Ågren's follow-up notes an interaction with `.gitignore` patterns (`*+` currently catches temporary files) and suggests an alternative temporary filename format (`"$OUTPUT"."$$"+`) that would maintain `.gitignore` coverage while still providing process-specific uniqueness, though this would not address the Windows filename character limitation. The discussion remains focused on technical trade-offs between maintaining existing cleanup patterns versus broader platform compatibility, with no immediate changes proposed to the patch. Key technical details include the race condition mechanism, Windows filename constraints, `.gitignore` pattern implications, and the specific build scenarios affected (parallel documentation generation). No open objections remain; the thread continues to refine understanding of edge cases without challenging the core solution.