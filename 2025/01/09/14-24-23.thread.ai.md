Build system race condition fix for version generation during parallel builds, particularly affecting Windows. The thread originates from a patch modifying `GIT-VERSION-GEN` to use process-specific temporary filenames (`.$$` suffix) instead of static `+` suffix, addressing both parallel build collisions and Windows filename constraints. The issue emerged when `GIT-VERSION-GEN` was extended to generate version strings for documentation files (`asciidoctor-extensions.rb` and `asciidoc.conf`), creating race conditions during concurrent `make -j2 html man` invocations. The minimal 4-line fix preserves the current build architecture while solving the immediate problem, with acknowledgment that a more comprehensive build system redesign would be preferable but is deferred beyond the current release cycle. The patch is motivated by failed CI runs and targets an urgent blocker for Git for Windows v2.48.0-rc2. Key technical details include the race condition mechanism (concurrent temporary file operations interfering), Windows filename limitations (`+` character prohibition), and the specific build scenarios affected (parallel documentation generation). No open questions or objections are noted; the change appears uncontroversial and focused on a clear technical problem.