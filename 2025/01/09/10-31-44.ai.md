This is a typo correction in an ongoing bugfix thread about `git blame`'s object ID handling.

Kristoffer Haugsbakk submits a minor grammatical correction to Patrick Steinhardt's patch description, fixing "where as" to "whereas" in the explanation of why `fwrite` behaves differently from `printf` when handling NUL bytes in object ID output. The original patch addressed an out-of-bounds read issue when using very large `--abbrev` values by reverting from `fwrite` to `printf` while maintaining type safety.

This is purely an editorial suggestion that doesn't affect the technical content of the fix, which has already been well-explained in the thread. The correction helps maintain the project's documentation standards for commit messages and code comments. The thread context shows the underlying technical issue was already properly addressed with test coverage.