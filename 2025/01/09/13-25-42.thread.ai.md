Thread summary:

Documentation reorganization moving trailer configuration details from command-specific man pages to centralized config documentation. The patch relocates detailed documentation of `trailer.*` config options from `git-interpret-trailers.txt` to a new `config/trailer.txt` file while maintaining accessibility through include directives in both the config documentation and command-specific man pages. 

The change affects three files:
1. Creates new `config/trailer.txt` containing all trailer configuration details
2. Adds include reference in `config.txt` to surface the trailer docs
3. Replaces inline documentation in `git-interpret-trailers.txt` with includes pointing to the new file

Key configuration variables covered include trailer.separators (default ':'), trailer.where (placement options), trailer.ifexists (duplicate key handling), trailer.ifmissing (new key handling), and per-key overrides via trailer.<keyAlias>.* variants. The patch also notes the deprecation of trailer.<keyAlias>.command in favor of trailer.<keyAlias>.cmd.

This is a pure documentation reorganization with no content changes, following Git's standard practice of using include directives to avoid duplication while improving discoverability of configuration options that affect multiple commands (commit, tag, and interpret-trailers). The change appears well-executed and maintains all existing documentation while making it more centrally accessible.