This is a documentation patch that reorganizes how Git's trailer configuration options are presented. Julian Prein moves the detailed documentation of `trailer.*` config variables from `git-interpret-trailers.txt` into a new dedicated file `config/trailer.txt`, which is then included in both the interpret-trailers and git-config documentation.

The change makes sense as these configuration options affect not just `git interpret-trailers` but also other commands like `git commit` and `git tag`. The patch is cleanly implemented - it extracts all trailer-related config documentation verbatim into the new file and adds the appropriate include directives. The commit message clearly explains the change's motivation.

This appears to be a straightforward documentation improvement that better organizes the config documentation while maintaining all existing content. The patch follows Git's documentation conventions and doesn't introduce any functional changes - it just moves documentation to a more appropriate location.