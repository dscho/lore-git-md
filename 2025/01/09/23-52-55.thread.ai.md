Git grep EOF matching inconsistency: A bug report identifies that `git grep '^$'` incorrectly matches at end-of-file (reporting line number N+1 for an N-line file), while standard grep does not. The reporter provides clear steps to reproduce using a single-line test file, showing behavioral divergence between Git grep and GNU grep. The issue appears to stem from line counting or empty line matching logic at file boundaries. This affects compatibility with standard grep behavior and appears reproducible across Git versions. No patches or proposed fixes yet; the thread begins with this well-formed bug report including version details and minimal reproduction case.