This is a brief follow-up in the ongoing discussion about commit message formatting conventions, though the email itself primarily addresses a technical point about LSan test checking.

Jeff King responds to Junio Hamano's note about POSIX compliance of `grep -s` by acknowledging the existing usage in p4 tests (t7407 and t9831), but notes these are rarely run compared to core tests. He observes that the LSan tests in question also run on limited platforms, making the POSIX compliance concern less critical in practice.

The email doesn't advance the commit message style discussion from earlier in the thread, but maintains the casual tone characteristic of that exchange. The technical point about test coverage is relevant for understanding the context of the original patch (which modified LSan result checking in test-lib.sh), though the thread has largely shifted to style concerns.

Key technical detail: The `grep -s` flag (suppress errors about nonexistent files) is POSIX-compliant and already used in some tests, though in less-frequently-run test suites (p4-related) and for LSan-specific cases that don't run on all platforms.