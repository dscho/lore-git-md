This is a refactoring patch that's part of Jeff King's larger series improving combine-diff implementation. The patch focuses on memory management in `run_diff_files()` by delaying allocation of `combine_diff_path` structs until after error checking.

The key change moves the allocation after `check_removed()` to prevent potential memory leaks when that function returns an error. While the leak wasn't triggering in tests, the change makes the code more robust. The patch also serves as groundwork for future refactoring by making it easier to extract a constructor function, since all initialization data is available at the allocation point.

The diff shows the straightforward code reorganization - moving the allocation block down by about 10 lines while maintaining all the same initialization logic. Junio Hamano's reply indicates agreement with the approach. This is a small but meaningful step in the larger effort to improve combine-diff's memory handling and code structure.