This is a follow-up discussion about the `help.autocorrect` bugfix patch that has already received technical approval. Junio Hamano raises a design-level consideration about whether to treat the configuration as an extended boolean rather than the current string-based approach.

The core proposal is to allow boolean-like values (true/false/yes/no/on/off) to map to "immediate" and "never" respectively, while maintaining backward compatibility with the existing string values ("immediate", "never", "prompt") and numeric delays. This would make the configuration more intuitive while preserving all current functionality.

The discussion occurs after Johannes Schindelin has already approved the technical implementation in `help.c` that treats "1" as equivalent to "immediate". Junio acknowledges the current patch works but questions whether a more comprehensive solution would be better, noting the inconsistency where "no" doesn't currently work as users might expect.

This represents a higher-level design discussion about configuration parsing philosophy rather than a blocking concern about the current patch. The thread appears to be converging toward resolution, with this email opening a potential alternative direction that would require more substantial changes to the configuration handling logic.