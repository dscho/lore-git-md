This is patch 12/14 in a refactoring series improving Git's combine-diff implementation, focusing on code clarity through better variable naming. The patch changes the ambiguous parameter name "p" to "tail" in several tree-diff functions to more accurately reflect its role as a list tail pointer rather than a generic node pointer.

The key technical change is renaming the parameter throughout `ll_diff_tree_paths()` and `emit_path()` to clarify that this variable always represents the tail of the list being built, not the current node. The commit message explains how the previous name "p" caused confusion because it sometimes referred to newly allocated entries while other times referring to the list tail. The patch maintains all existing behavior while making the code's intent clearer.

The changes affect tree-diff.c's core diffing logic, modifying how paths are emitted and recursed into during tree comparisons. The patch updates 33 lines across the file, consistently renaming the parameter and adjusting related variable assignments in `emit_path()` to properly track both the new entry ("p") and list tail ("tail"). This continues the series' pattern of simplifying interfaces while preserving functionality.