This is a follow-up in the build system race condition thread, focusing on temporary filename patterns for parallel version generation. Junio C Hamano responds to Martin Ågren's suggestion about maintaining `.gitignore` compatibility while fixing the Windows filename issue. 

The email confirms that the current patch approach (using `.$$` suffix) is reasonable for a short-term fix, though alternatives like `~` would also work. Junio notes the previous failed attempt (referenced in the thread) failed due to inconsistency in applying changes across the codebase and not updating `.gitignore` patterns - issues the current patch properly addresses. 

Key technical points include: the importance of maintaining `.gitignore` patterns (currently matching `*+`), the trade-off between different temporary filename formats (`.$$` vs `~` vs `$$+`), and the distinction between a quick fix versus a more comprehensive build system redesign. The discussion remains focused on implementation details rather than fundamental objections, with consensus forming around the `.$$` approach as a pragmatic solution. 

The thread shows careful consideration of historical context (the failed 216.git attempt) and secondary effects (`.gitignore` patterns), while keeping the scope limited to the immediate parallel build issue. No new technical concerns are raised about the core solution.