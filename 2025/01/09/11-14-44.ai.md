Bugfix refinement for `git blame`'s OID output handling. Patrick Steinhardt responds to Johannes Schindelin's review of the earlier fix for an out-of-bounds read when using large `--abbrev` values. The discussion centers on two key issues: (1) where to place the length truncation check in the code to maintain proper formatting for boundary/unblamable/ignored commits (marked with ^, *, ? prefixes), and (2) correcting the test case to properly verify the truncation behavior.

Steinhardt proposes moving the `GIT_MAX_HEXSZ` length check after the prefix character handling rather than before, which would eliminate the need for the `+3` buffer adjustment while still preventing overreads. He also identifies that the test case should verify that an absurdly large abbrev value (9000) gets truncated to the hash algorithm's hex size (`$hexsz`), not that it passes through unchanged.

The changes affect `builtin/blame.c`'s OID emission logic and the verification in `t/t8002-blame.sh`. The subtlety primarily impacts SHA-256 mode due to size differences from SHA-1. The thread shows careful attention to both safety (preventing buffer overreads) and maintaining expected formatting behavior in edge cases.