Documentation patch reorganizing trailer configuration variables. This moves the detailed documentation of `trailer.*` config options from `git-interpret-trailers.txt` into a new `config/trailer.txt` file, while including it in both the config and interpret-trailers documentation. The change is purely organizational - no content is modified, only relocated.

The patch affects three documentation files: creates a new `config/trailer.txt` with all trailer configuration details, adds an include reference to it in `config.txt`, and replaces the inline documentation in `git-interpret-trailers.txt` with includes pointing to the new file. The trailer configuration affects multiple commands (`git-commit`, `git-tag`, and `git-interpret-trailers`) so this centralizes the documentation in the config section while maintaining accessibility from the command-specific docs.

Key configuration variables being documented include:
- trailer.separators (default ':')
- trailer.where (placement options: end/start/after/before)
- trailer.ifexists (handling of duplicate keys)
- trailer.ifmissing (handling of new keys)
- per-key overrides via trailer.<keyAlias>.* variants
- The deprecated trailer.<keyAlias>.command vs preferred trailer.<keyAlias>.cmd

The change appears well-executed - it's a straightforward documentation reorganization that improves discoverability of trailer config options while maintaining all existing content. The patch follows standard Git documentation practices by using include directives to avoid duplication.