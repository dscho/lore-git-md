Documentation patch updating the `git-restore` manpage to use the new documentation format. This is part of the ongoing effort to standardize Git's documentation style.

The patch makes several formatting changes to `Documentation/git-restore.txt`:
1. Replaces the `[verse]` block with `[synopsis]` for the command synopsis, which enables automatic formatting of placeholders and keywords
2. Consistently uses `_<placeholder>_` syntax for placeholders throughout
3. Wraps command options and keywords in backticks for proper rendering
4. Updates example text to use the new formatting style
5. Makes minor wording improvements (e.g., "remove" → "remove files")

The changes are purely presentational - no content or behavior is modified. The patch follows the established pattern seen in other documentation updates, applying the new style consistently across the entire manpage. The diff shows 54 insertions and 55 deletions, indicating this is primarily a reformatting with some minor wording tweaks rather than substantial content changes.

Key technical details for future reference:
- The synopsis block change affects how options like `--source=<tree>` are rendered
- Placeholder syntax changes affect references to `<pathspec>`, `<file>`, `<refA>`, etc.
- Backtick-wrapped elements include command options (`-s`, `--patch`), special values (`ours`, `theirs`), and file names (`hello.c`)
- The actual command behavior and all option semantics remain unchanged