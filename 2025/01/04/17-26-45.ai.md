This is a maintainer review comment in an ongoing thread about object name resolution with curly braces. Junio C Hamano is responding to a specific implementation detail in Elijah Newren's patch that fixes parsing of unpaired curly braces in object names.

The key technical point is about bounds checking in the parser - the current patch checks `*(cp+1)` before verifying `cp[0]` isn't the string terminator, which risks undefined behavior. Junio suggests an alternative formulation using `strchr()` that would be both safer and potentially more maintainable if additional special characters need to be handled in the future.

The comment reveals ongoing tension between:
1. The need for specific character handling in the parser (here for '@' and '^' before braces)
2. The maintainer's preference for more general solutions that don't bake in special cases
3. Implementation safety concerns around string bounds checking

This is part of the broader discussion about how Git should resolve ambiguous object names, particularly around the interaction between path specifications and describe-like patterns. The thread has evolved from specific parsing fixes to more fundamental questions about Git's approach to ambiguity resolution.