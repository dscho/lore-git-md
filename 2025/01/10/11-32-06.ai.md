This is a CI/build system cleanup patch in Patrick Steinhardt's ongoing CI series, now expanded to 10 parts. It removes obsolete Azure Pipelines infrastructure that was left behind when the project migrated to GitHub Actions in 2020.

The patch deletes two chunks of Azure-specific code: the main CI detection and configuration block in `ci/lib.sh` (21 lines removed) and test artifact handling in `ci/print-test-failures.sh` (5 lines removed). The removal is straightforward since Azure support was officially retired in commit 6081d3898f. The changes maintain all current GitHub Actions functionality while cleaning up dead code.

This final patch in the series completes the CI modernization effort by excising the last remnants of the old Azure infrastructure. It follows the thread's established pattern of careful, incremental cleanup while maintaining working CI functionality. The changes are minimal and low-risk, affecting only build system code that is no longer in use.