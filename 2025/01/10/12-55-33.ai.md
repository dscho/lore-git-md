Performance optimization patch continuing the zlib-ng integration series. This is patch 6/8, focusing on compatibility shims for `deflateSetHeader()`.

The patch moves the version-specific handling of `deflateSetHeader()` (introduced in zlib 1.2.2.1) from `archive-tar.c` into the new `compat/zlib-compat.h` abstraction layer. It provides a stub implementation for older zlib versions that simply returns Z_OK (success code) while ignoring its arguments. The change allows the direct use of `deflateSetHeader()` in the tar archive code without version checks, as the compatibility layer now handles the version differences transparently.

Key technical details:
- Removes `ZLIB_VERNUM` checks from `archive-tar.c`
- Adds stub implementation in `compat/zlib-compat.h` for zlib <1.2.2.1
- Maintains the same behavior (setting gzip OS header to 3 for Unix)
- Follows the series' pattern of centralizing zlib version compatibility

This continues the systematic preparation for zlib-ng integration by further abstracting version-specific zlib behavior. The change is straightforward and maintains backward compatibility while cleaning up the direct version checks in the tar archive code. The patch appears well-contained and follows the established pattern of the series.