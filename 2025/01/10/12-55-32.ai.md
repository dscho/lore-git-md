Performance optimization series introducing zlib-ng support (part 5/8). This patch moves the infrastructure work forward by centralizing the `deflateBound()` compatibility shim into the new `compat/zlib-compat.h` header.

The change relocates a zlib compatibility macro from `git-zlib.c` to the new abstraction layer, specifically handling cases where the system zlib is older than version 1.2.0 (which introduced `deflateBound()` natively). The patch is a straightforward code movement with no functional changes, maintaining the same calculation formula for the compressed output size upper bound.

This is part of the systematic preparation for zlib-ng integration, following earlier patches that established the compatibility layer and reorganized zlib-related headers. The change affects compression infrastructure but doesn't yet activate any zlib-ng functionality. The commit message clearly explains the version compatibility rationale and the move's purpose in the larger series context.

Key technical details for future reference:
- Moves `deflateBound` shim from `git-zlib.c` to `compat/zlib-compat.h`
- Keeps same calculation formula: `(s) + (((s) + 7) >> 3) + (((s) + 63) >> 6) + 11`
- Conditionally defined when `NO_DEFLATE_BOUND` is set or zlib version < 1.2.0
- Part of establishing consistent zlib API surface before zlib-ng integration
- Touches core compression path but maintains backward compatibility