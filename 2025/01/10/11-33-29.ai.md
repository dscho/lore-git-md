This is a code style review comment on the object-info series (v9 patch 1/8). Christian Couder provides feedback on the function comment style for the new `strtoul_ul()` helper function, which was added to handle safe string-to-unsigned-long conversion for the remote object-info feature.

The review points out two style issues: the comment should use C-style `/* */` delimiters rather than `//` line comments, and the wording should use imperative mood ("Convert" rather than "Converts") to match Git project conventions for function documentation. This is a minor but important consistency fix for the new utility function that's used in the series' response parsing logic.

The `strtoul_ul()` function itself is part of the infrastructure supporting the remote object-info capability, specifically for parsing object size responses from the server. The function provides robust error handling around `strtoul()` calls, which is important for safely processing server responses in the protocol implementation. This feedback comes as part of the final polish phase for the series, following earlier more substantive reviews of the core functionality.