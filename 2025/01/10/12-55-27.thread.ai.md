Thread summary:

Performance optimization patch series introducing zlib-ng support as an alternative zlib backend, authored by XXX (name not provided in input). The series refactors Git's zlib integration to allow runtime switching between standard zlib and zlib-ng, which provides optimized implementations using modern CPU features (SSE2, AVX2). Core changes include a new `compat/zlib.h` abstraction layer, removal of obsolete compatibility shims, meson build system updates for backend selection, and API shims for zlib-ng's prefixed symbols (`zng_*`). Initial benchmarks show 25% speedup in `git cat-file --batch --batch-all-objects` workloads, though this represents an extreme case. The implementation maintains compatibility with existing systems while enabling the performance optimization path. The changes touch core compression infrastructure across multiple files but appear focused, building on prior zlib refactoring work. The series addresses a real-world deployment constraint where distributions avoid globally replacing system zlib with zlib-ng, requiring this explicit integration path to access the optimized implementation. Performance gains could be particularly valuable for hosting providers and other heavy users of object access operations. No reviews or feedback from other contributors mentioned yet in this initial submission.