This thread discusses a performance optimization series that would add zlib-ng support as an alternative zlib backend in Git. The series, currently at 8 parts, systematically refactors Git's zlib integration to enable runtime switching between standard zlib and the optimized zlib-ng implementation (which can leverage modern CPU instructions like SSE2/AVX2.

The work so far has focused on infrastructure preparation: creating a new compatibility layer in `compat/zlib-compat.h`, reorganizing zlib-related headers to move them out of `git-compat-util.h`, and now in the latest patch (5/8), centralizing the `deflateBound()` compatibility shim into the new abstraction layer. This shim handles older zlib versions that lack native `deflateBound()` support, maintaining the same calculation formula for compressed output size estimation.

The changes are methodical and focused on the existing compression infrastructure, with no functional impact yet. The series appears carefully designed to maintain compatibility with both system zlib and reftable's independent zlib usage while laying groundwork for future performance optimizations. Initial benchmarks suggest potential 25% speedups in operations like `git cat-file --batch --batch-all-objects`.

No reviews or feedback from other contributors have appeared yet, but the systematic approach and clear commit messages suggest the author is being careful to minimize disruption while enabling this optimization path. The work remains in the preparatory stages, with the actual zlib-ng integration presumably coming in later patches of the series.