This thread introduces zlib-ng support as an alternative zlib backend, providing a ~25% speedup in object reading operations while maintaining compatibility with standard zlib. The 10-part series, now in its third iteration, has been thoroughly reviewed by Karthik Nayak and addresses all technical concerns. 

The implementation abstracts differences between zlib and zlib-ng through a new "compat/zlib-compat.h" header, handles symbol naming and constness variations, and provides build system integration via both Makefile and Meson. The latest patch completes the CI testing story by adding zlib-ng coverage through Alpine Linux, which was chosen because it uniquely provides the package while also running Git's test suite. 

With all platform compatibility issues resolved and CI coverage now comprehensive, the series appears ready for merging. The changes clean up obsolete compatibility shims while maintaining fallback to standard zlib, making this a low-risk performance win. The maintainer seems poised to pick up the series for integration branches.