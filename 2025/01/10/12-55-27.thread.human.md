This thread introduces a performance optimization series that enables Git to use zlib-ng as an alternative zlib backend, providing potential speedups through optimized SSE2/AVX2 implementations. The 8-part series, authored by XXX, systematically refactors Git's zlib integration to support runtime switching between standard zlib and zlib-ng while maintaining compatibility.

The work establishes a clean abstraction layer through several infrastructure changes: creating a new zlib compatibility header, reorganizing includes, adding compatibility shims for older zlib versions, and handling constness differences between APIs. The final patch adds build system support for zlib-ng, introducing a meson option to select the backend implementations and symbol remapping to work with zlib-ng's prefixed API.

Initial benchmarks show promising results, with about 25% faster performance in `git cat-file --batch --batch-all-objects` operations. The implementation is particularly considerate of distribution constraints, allowing zlib-ng to coexist with system zlib rather than requiring replacement. While the series appears technically sound, it hasn't yet attracted review feedback from other contributors. The changes remain focused on the zlib abstraction layer, keeping the door open for future optimizations while maintaining backward compatibility.