This is a follow-up to the git-instaweb Python server binding fix, where Junio C Hamano acknowledges and accepts Alecs King's patch that corrects the inverted IP address binding behavior. The patch flips the default binding from 127.0.0.1 to 0.0.0.0 and makes the -l flag properly bind to localhost only, matching documented behavior and security conventions.

Junio confirms the patch correctly addresses the long-standing inversion issue that has existed since the Python server support was introduced in 2019. He notes this is a well-spotted fix and indicates he will queue it for inclusion. The discussion also surfaces a related but separate issue about some backends ignoring the --local option entirely, though this is explicitly noted as being outside the scope of the current patch.

The technical details remain unchanged from the original patch - it's a minimal 2-line change in git-instaweb.sh that swaps the IP binding logic. The thread shows consensus that this is a clear bugfix that properly aligns the implementation with both documentation and security expectations for server binding behavior.