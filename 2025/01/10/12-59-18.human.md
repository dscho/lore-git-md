This is a technical follow-up in the git grep EOF matching bugfix thread. Andreas Schwab suggests an alternative approach to Jeff King's proposed fix, questioning why the matcher isn't using the REG_NOTEOL flag to prevent "^$" from matching at end-of-buffer. 

The comment builds on King's observation about regexec_buf() behavior, proposing what appears to be a more targeted solution than the boundary check currently being discussed. This introduces a new technical consideration about regex engine flags that wasn't part of the original patch discussion. 

The suggestion seems plausible but would need evaluation against the different regex implementations Git supports (PCRE, regcomp, etc.). It's unclear from this brief note whether REG_NOTEOL would fully address the edge cases King's patch handles, or if it might have other side effects. This appears to spark a new technical direction in the thread that maintainers will need to weigh against the existing proposed solution.