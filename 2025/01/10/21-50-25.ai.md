This is a technical discussion about the hashfile checkpoint API in brian m. carlson's hash algorithm refactoring series. Taylor Blau responds to Jeff King's suggestions about improving the checkpoint initialization and lifetime management.

Key points in this email:
- Taylor agrees with Jeff's proposal to store the hashfile pointer in the checkpoint struct, eliminating redundant parameters in `hashfile_checkpoint()` and `hashfile_truncate()`. He plans to implement this in two follow-up patches.
- Jeff raises a concern about potential resource leaks from OpenSSL's `EVP_MD_CTX_copy_ex()` in the clone operation, suggesting a need for a destructor function.
- Taylor acknowledges the leak concern but proposes handling it in a separate series, keeping the current focus on the checkpoint initialization changes.

The discussion continues the thread's focus on hardening the hashfile checkpoint mechanism while maintaining clean separation between safe and unsafe hash operations. This email specifically addresses implementation details around struct lifetime management and resource cleanup in the checkpoint API.