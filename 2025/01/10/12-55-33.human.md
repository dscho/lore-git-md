Performance optimization patch continuing the zlib-ng integration series. This is patch 6/8, moving the `deflateSetHeader()` compatibility handling into the new `compat/zlib-compat.h` abstraction layer.

The change removes version-specific `#ifdef` blocks from `archive-tar.c` and replaces them with a stub implementation in the compatibility layer when using older zlib versions (<1.2.2.1). The stub simply returns success without doing anything, maintaining the existing behavior for older zlib versions while allowing the code to be written without conditional compilation.

This follows the pattern established in earlier patches of centralizing zlib version compatibility logic in preparation for the zlib-ng backend. The change is straightforward and maintains backward compatibility while cleaning up the code. The patch appears well-contained, touching only two files with clear changes that align with the series' overall goal of abstracting zlib dependencies.