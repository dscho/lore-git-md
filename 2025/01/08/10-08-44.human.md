This is a follow-up discussion about the `--missing=print-type` feature for `git rev-list`. Christian Couder responds to Justin Tobler's patch with suggestions for code organization and potential future extensibility.

Christian agrees with the general approach of adding type information to missing object reports, though he raises the question of whether a more general `--missing-format` option might be preferable if additional metadata beyond object types might be useful in the future. He provides several concrete suggestions to improve the implementation:

1. Proposes extracting common code into helper functions (`add_missing_object_entry` and `print_missing_objects`) to reduce duplication between the `MA_PRINT` and `MA_PRINT_TYPE` cases
2. Notes that the helper functions could also clean up some edge cases in the output formatting
3. The feedback is constructive and focused on maintainability, suggesting the author consider these organizational improvements while keeping the core functionality

The discussion continues the thread's technical depth while remaining focused on implementation details rather than challenging the feature's fundamental design. Christian's suggestions appear aimed at making the code more maintainable if additional metadata reporting is added later.