This is a follow-up in the test modernization thread about converting hash algorithm tests from t-*.c to u-*.c format. The discussion has shifted from the implementation details to a specific stylistic question about function signatures in the test macros.

Junio Hamano responds to Patrick Steinhardt's agreement to use proper function bodies rather than macro tricks, but playfully suggests an alternative macro approach that might satisfy both syntax highlighting and the desired structure. The proposed macro would generate complete test functions with proper signatures while still allowing the test case definitions to remain compact. However, Junio acknowledges this may be over-engineering for just a few test cases.

Key technical details:
- The macro `T()` would expand test cases into full functions with the naming pattern `test_hash__<testname>`
- Each generated function would call `check_hash_data()` with the test inputs
- The macro includes an `extern` declaration to satisfy some compilers/editors
- Example test cases show the compact invocation style for empty string and single character hashes

This represents a minor stylistic refinement to the test conversion work, showing the attention to detail in maintaining both functionality and code quality standards. The tone suggests this is more of a thought experiment than a serious proposal, given the small number of test cases involved. The thread appears to be converging on simpler function definitions rather than clever macros.