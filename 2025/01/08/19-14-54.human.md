Final patch in a refactoring series to clean up hash algorithm handling. This 8th patch removes the now-unused `unsafe_` function variants from `struct git_hash_algo` after all callers have been converted to use the new `unsafe_hash_algo()` wrapper mechanism introduced earlier in the series.

The changes are straightforward deletions of the redundant function pointers from both the hash algorithm struct definition in `hash.h` and their implementations in `object-file.c`. This completes the transition from direct unsafe function calls to the indirect approach through algorithm pointers, which was the series' main goal.

The patch demonstrates good mechanical refactoring practice - first introducing and migrating to the new infrastructure (in patches 1-7), then removing the old code paths once they're no longer needed. This final cleanup step helps prevent future confusion by eliminating the possibility of mixing old and new calling patterns. The series as a whole appears well-structured and focused, with each patch building logically on the last.