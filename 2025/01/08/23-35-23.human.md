This is a documentation patch fixing a misplaced include directive in the Git config documentation. 

Adam Johnson noticed that commit 2a9dfdf260 ("difftool docs: de-duplicate configuration sections", 2022-09-07) accidentally left behind an include directive for the diff tools list in the wrong file. The directive was still in `diff.txt` while the related documentation had moved to `difftool.txt`. 

The patch simply relocates the `include::{build_dir}/mergetools-diff.txt[]` line from `Documentation/config/diff.txt` to its proper place in `Documentation/config/difftool.txt`, where it belongs with the other difftool configuration documentation. This ensures the generated list of diff tools appears in the correct section of the documentation.

The change is straightforward and clearly correct - it's just fixing an oversight from a previous documentation reorganization. The patch is minimal (just moving two lines between files) and properly signed off. This appears to be a simple but worthwhile documentation cleanup.