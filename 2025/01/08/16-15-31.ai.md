This is a discussion about the OS version capability patch in the ongoing series, focusing on configuration flexibility and character sanitization.

Junio Hamano raises concerns about the planned `osVersion.format` config option, arguing that allowing arbitrary user customization could lead to unnecessary variation in version strings that makes it harder for servers to identify similar systems. He suggests that some standardization (even if informal) would be more valuable than full flexibility, as the current approach risks enabling "random garbage" in version strings. This pushes back against the author's stated plan to implement placeholder-based customization.

The discussion also revisits the character sanitization behavior from earlier in the thread. Junio questions why carriage returns are being replaced with dots (via the `tr` command) while linefeeds are being stripped entirely, seeing no technical justification for treating these control characters differently. The author confirms the current behavior but doesn't address the underlying consistency concern.

This email doesn't introduce new code changes but highlights two philosophical tensions in the design: (1) the tradeoff between configurability and standardization in version strings, and (2) the sanitization function's handling of control characters. The thread context shows these discussions are happening alongside concrete implementation work to refactor uname handling and improve Windows test compatibility. Junio's comments suggest he may want to see the version string approach reconsidered before the format customization feature is implemented.