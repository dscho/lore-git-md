This is a follow-up in the ongoing discussion about extending `git rev-list --missing` to report more information about missing objects. Justin Tobler responds to Christian Couder's review of his initial patch that added a `--missing=print-type` option.

The key development here is a pivot toward a more extensible design. Instead of adding specific `print-type` functionality, Justin now proposes a generic `--missing-attr` option that could specify multiple attributes to print (like type and potentially path information later). This addresses Christian's concern about avoiding a proliferation of `print-*` options.

Justin also acknowledges several code organization suggestions from Christian, agreeing to factor out duplicate code into helper functions for adding missing object entries and printing them. The discussion remains focused on implementation details rather than fundamental design questions, suggesting the approach is converging toward consensus.

The patch appears well-considered, with Justin thoughtfully responding to feedback and proposing reasonable next steps. The change touches core rev-list functionality but maintains backward compatibility while allowing future extensibility. This continues the thread that has already seen productive input from multiple Git maintainers.