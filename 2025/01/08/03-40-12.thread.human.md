Here's a human-readable summary of the thread:

The thread discusses a proposed `--missing=print-type` option for `git rev-list` that would show both missing object IDs and their types (commit, tree, blob, tag). Justin Tobler's initial implementation uses an oidmap to track missing objects and adds the new output format. Christian Couder reviewed the patch, suggesting some code refactoring opportunities to reduce duplication while agreeing the functionality meets current needs.

Junio Hamano has now weighed in with more substantive feedback about the design. He points out that the object types are inferred from context rather than directly accessed, which should be clearly documented as a limitation. More significantly, he suggests the output format should be designed with future extensibility in mind - proposing a more general `?<oid> [key=value...]` syntax that could later accommodate additional metadata like paths, using C-style quoting for special characters. While not blocking the current feature, Junio encourages thinking ahead about how this might evolve into a richer missing object reporting mechanism.

The core functionality remains sound, but the discussion has shifted to consider both the documentation of type inference and the long-term design of the output format. The patch may need updates to address these concerns before being merged.