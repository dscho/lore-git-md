This is a review response discussing the implementation of the new `--missing=print-type` option for `git rev-list`. Christian Couder provides feedback on Justin Tobler's patch, suggesting potential refactoring opportunities to reduce code duplication while maintaining the same functionality.

The review focuses on two main areas where the code could be consolidated: (1) the addition of missing object entries, where Christian suggests extracting a helper function `add_missing_object_entry()` to handle both `MA_PRINT` and `MA_PRINT_TYPE` cases, and (2) the printing of missing objects, where a `print_missing_objects()` helper function could unify the output logic for both modes. 

Christian also raises a broader design question about whether a more flexible `--missing-format` option might be preferable if additional object information beyond type might be useful in the future, though he acknowledges the current `print-type` approach makes sense for the immediate need. The feedback maintains the patch's core functionality while proposing cleaner implementation patterns through helper functions to handle the shared logic between the two print modes.