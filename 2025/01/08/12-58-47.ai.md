This is a refactoring patch that's part of a series (1/4) about sanitizing user agent strings. The patch extracts string sanitization logic from `git_user_agent_sanitized()` into a new `redact_non_printables()` function for reuse in later patches. The function replaces non-printable characters with dots and trims whitespace.

The thread context reveals an issue where the commit message claims improvements (using `size_t` and moving variable declarations) that were already present in the codebase due to an unnoticed rebase. The author points this was an oversight during rebasing and promises to correct the commit message in the next version. The core refactoring itself (extracting the function and using `strbuf_detach()`) remains valid and uncontested.

Key technical details for future reference:
- The new `redact_non_printables()` function operates on a `strbuf`
- It replaces characters <=32 or >=127 with dots (ASCII control/non-printable chars)
- The patch changes memory management in `git_user_agent_sanitized()` to explicitly detach rather than implicitly use the strbuf buffer
- The function is currently static but appears intended for wider use in later patches
- The thread shows the author is responsive to review feedback about commit message accuracy