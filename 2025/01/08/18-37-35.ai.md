This is a refactoring patch in the ongoing series implementing client-side remote object-info support. It generalizes the packet-writing functionality from fetch-pack.c to make it reusable across different protocol commands.

The patch moves and renames `write_fetch_command_and_capabilities()` to `write_command_and_capabilities()` in connect.c, making it available for other protocol operations beyond just fetch. The function now takes the command name as a parameter rather than hardcoding "fetch". This change supports the series' goal of adding object-info capability by providing shared infrastructure for command packet construction.

Key technical details:
- Moves the function to connect.c with minimal logic changes, keeping all existing behavior (capability advertisement, session ID handling, server options)
- Handles the `advertise_sid` config by reading it in the function rather than relying on fetch-pack.c's static variable
- Maintains all existing error checking and protocol validation
- Updates all callers in fetch-pack.c to use the new generalized function
- Adds the new function declaration to connect.h for cross-file visibility

The change is well-justified as part of the series' architecture, with clear analysis of the `advertise_sid` handling implications. The patch follows the series' pattern of careful refactoring with attention to review feedback, as evidenced by the helped-by tags from Jonathan Tan and Christian Couder. No behavior changes are introduced - this is purely a code organization improvement to support future protocol commands.