This is a discussion about the reftable backend's correctness requirements around random number generation, specifically addressing a newly identified race condition. 

Junio Hamano responds to Patrick Steinhardt's explanation of how predictable temporary file suffixes could lead to data corruption if Git crashes mid-transaction. The scenario involves a sequence where a temporary table is written with a predictable suffix, but if Git dies before updating the reference list, a subsequent operation could collide with the same suffix, causing errors. While the reftable library can clean up stale tables, it doesn't do so on every write, making this a real correctness issue.

Junio expresses skepticism about whether it's reasonable to depend on cryptographic randomness for correctness in this case, drawing a parallel to how Git already relies on hash functions not colliding. The discussion highlights how what was initially thought to be a performance optimization (random suffixes) has turned out to be a correctness requirement due to this crash scenario.

This exchange shows the thread evolving from general RNG discussions to grappling with specific implications for reftable's crash safety. The core tension is between the need for reliable randomness (now understood as correctness requirement) and the practical limitations of OpenSSL's RNG on certain platforms.