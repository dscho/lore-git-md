Refactoring patch with a minor documentation issue. This is part 1 of a 4-patch series (though the cover letter wasn't provided in this context).

The patch extracts string sanitization logic from `git_user_agent_sanitized()` into a new `redact_non_printables()` helper function, with the goal of reusing this logic in future patches. The refactoring includes switching to `strbuf_detach()` for cleaner memory management.

Eric Sunshine points out that the commit message inaccurately claims to be introducing `size_t` and loop variable declarations that were actually already present in the code. The author acknowledges this was due to a rebase oversight and promises to fix the commit message in the next version.

The changes appear straightforward and the motivation for the refactoring (future reuse) seems reasonable, though the commit message inaccuracy suggests the author should be more careful when rebasing. The patch itself looks clean and maintains the existing behavior while improving code organization.