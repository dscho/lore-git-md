This is part 6 of the hash algorithm refactoring series, converting test helper code to use the new `unsafe_hash_algo()` infrastructure. The patch simplifies `t/helper/test-hash.c` by removing conditional logic that previously selected between safe and unsafe hash variants, instead using the algorithm pointer indirection introduced earlier in the series.

The key change moves the unsafe/safe decision point to the start of `cmd_hash_impl()`, where it sets the algorithm pointer once via `unsafe_hash_algo()` if needed. This eliminates repetitive conditionals throughout the function for each hash operation (init, update, final). The test helper continues to support both safe and unsafe modes through its command-line interface ('sha1' vs 'sha1-unsafe'), but internally uses the unified algorithm pointer approach.

This is a straightforward mechanical conversion that follows the test infrastructure pattern established in earlier patches. It demonstrates how the new infrastructure simplifies call sites by removing duplicate code paths while maintaining the same functionality. The change is small (17 lines modified) and focused, continuing the series' incremental approach to refactoring hash algorithm handling.