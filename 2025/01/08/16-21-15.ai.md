This is a technical discussion about random number generation in Git's reftable backend, now expanding to address broader system-wide RNG reliability issues. Patrick Steinhardt responds to Junio Hamano's suggestion to use simpler random number generation (system rand() or constants) in the reftable stack by identifying a previously overlooked correctness concern.

The key new technical detail is a race condition scenario where Git crashes mid-transaction: if two processes generate the same "random" suffix for temporary files (due to using predictable values), it could lead to write conflicts that aren't automatically cleaned up by the reftable library's stale file handling. This suggests the randomization serves a correctness purpose beyond just performance optimization.

Patrick also connects this to Randall Becker's reports of OpenSSL CSPRNG failures on ia64 systems, noting this is a wider problem affecting multiple parts of Git, not just reftable. He expresses skepticism about OpenSSL's reliability compared to other RNG backends, framing this as a platform-level issue rather than something Git should work around.

The discussion has evolved from reftable-specific optimizations to fundamental questions about Git's RNG strategy:
- Whether to maintain OpenSSL support despite its ia64 failures
- How to handle the identified reftable race condition
- The broader implications for all git_rand() callsites

Patrick's position remains that OpenSSL's CSPRNG is fundamentally unreliable, though he now acknowledges the reftable case has correctness dependencies on proper randomization that weren't initially apparent. The thread continues to balance platform-specific constraints (ia64, NonStop) against system reliability concerns.