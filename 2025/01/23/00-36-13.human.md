Bugfix for the newly merged path-walk API. Jeff King reports that Coverity scan flagged a redundant `parse_tree()` call in the path-walk code that was recently merged to 'next'. The function already calls `parse_tree_gently()` earlier and returns on failure, making the second parse unnecessary. This is a straightforward cleanup with no behavior change, addressing a minor code quality issue in the new path-walk infrastructure that enables batch object visiting by path or type. The fix is small (one line removed) and clearly correct, coming from an experienced contributor who regularly handles Coverity findings.