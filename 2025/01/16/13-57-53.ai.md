This is patch 9/10 in a bugfix series addressing packed-refs validation issues. Patrick Steinhardt is responding to shejialuo's patch that adds sorting validation for packed-refs entries.

The patch introduces a new `packed_fsck_ref_sorted()` function that verifies packed refs are properly sorted by comparing adjacent entries using `cmp_packed_refname()`. The implementation extracts refnames from the packed-refs records (skipping past the hash and space) and checks they appear in correct order.

Patrick approves the change, noting that unsorted packed-refs have historically caused subtle bugs that could silently return incorrect results. His brief but positive response ("Makes sense") suggests this validation is a worthwhile addition to the series' broader packed-refs hardening effort.

This patch continues the series' pattern of adding specific validation checks while Patrick has been pushing for a more backend-agnostic validation architecture in earlier reviews. However, he doesn't raise those architectural concerns here, likely because sorting validation is inherently packed-refs-specific and the implementation appears straightforward. The patch builds on the series' established validation infrastructure (using `fsck_options` and `fsck_packed_ref_entry` structures) without introducing new architectural elements that might conflict with Patrick's preferred direction.