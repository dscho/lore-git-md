This is a refactoring patch in a series cleaning up type usage in the reftable codebase. It changes the `len` field in `struct reftable_block` from `int` to `size_t`, since block lengths can never be negative. The change is straightforward and aligns with the series' goal of using more appropriate types throughout the reftable implementation.

The patch is part of a larger effort (10 patches total) to address compiler warnings and improve type safety as the reftable backend moves toward becoming a standalone library. Previous patches in the series have similarly converted various size-related variables to unsigned types where appropriate. This particular change affects the blocksource interface, which manages block storage for reftables.

The modification is small (just one line changed) but contributes to the overall consistency and correctness of the codebase. Like other patches in the series, it maintains compatibility with existing usage while removing the possibility of negative values that could never occur in practice. The change appears well-contained and low-risk, following the established pattern of the series.