Performance optimization patch in the zlib-ng integration series.

This is patch 6/10 in the third iteration of a series introducing zlib-ng support as an alternative zlib backend. The patch moves the `deflateSetHeader()` compatibility handling into the new centralized "compat/zlib-compat.h" infrastructure. 

The change removes version-specific `#ifdef` checks from archive-tar.c and instead provides stubs for older zlib versions in the compatibility header. This is part of the series' broader effort to abstract zlib implementations while maintaining compatibility. The patch keeps the same behavior but reorganizes the code to be more maintainable and consistent with the new zlib-ng support.

The change is straightforward - it moves existing functionality rather than adding new behavior. The commit message explains the motivation clearly: this prepares for subsequent zlib-ng integration by centralizing compatibility code. The patch has been reviewed as part of the larger series and appears ready for inclusion.