This is part of the ongoing effort to standardize help text output behavior across Git commands. Patch 3/6 introduces a new helper function `show_usage_if_asked()` that encapsulates the common pattern of checking for "-h" and showing usage text. 

The patch provides a cleaner way to the manual `if (argc == 2 && !strcmp(argv[1], "-h"))` checks that currently appear in many commands. Like the earlier `show_usage_with_options_if_asked()` helper, this new function ensures help text goes to stdout rather than stderr, while maintaining the existing exit code 129 for compatibility. The implementation uses a new `vfdreportf()` helper that can direct output to either stdout or stderr as needed.

Junio notes this is part of a multi-step process - first standardizing the output destination, with potential future work to reconsider the exit code (since showing help when explicitly requested arguably shouldn't be treated as an error). The patch includes thorough internal plumbing changes while maintaining the exact same external behavior for now.

This continues the series' careful approach of introducing infrastructure changes before converting individual commands in later patches. The implementation looks solid, building on established patterns from earlier in the series while handling the simpler case of non-options-style usage output.