This is a refactoring patch in a series addressing compiler warnings in the reftable codebase. It fixes potential integer overflow issues in the varint decoding logic while also cleaning up related code to eliminate -Wsign-compare warnings.

The patch rewrites `get_var_int()` to properly detect integer overflows during varint decoding, copying the more robust implementation from Git's own `decode_varint()`. It also refactors `put_var_int()` to match the simpler, battle-tested approach of `encode_varint()`, though this change is primarily for consistency and warning elimination rather than fixing a bug.

The changes include new unit tests to verify overflow detection, showing the author's attention to maintaining test coverage. This appears to be a well-considered change that improves both safety and code quality in the reftable implementation, which is being prepared to become a standalone library. The patch fits cleanly into the larger series addressing compiler warnings and code quality issues in the reftable code.