This is part of a test modernization series converting unit tests to the Clar framework, specifically focusing on the memory pool allocator tests. It's the second in a four-part series mentored by Patrick Steinhardt.

The patch converts the memory pool test from the old test-lib.h framework to Clar, splitting the single test function into two focused test cases (big_block and tiny_block) that verify allocation behavior with different block sizes. The test now uses Clar's assertion macros (cl_assert*) and follows the new u-*.c naming convention for unit tests.

The changes are straightforward - the test logic remains largely the same while adopting the new framework's conventions. Build system files (Makefile and meson.build) are updated to remove the old test and include the new one. The commit shows the author has incorporated Patrick's earlier feedback by renaming the test function from t_calloc_100 to the more descriptive test_many_pool_allocations.

This appears to be a clean conversion that maintains test coverage while modernizing the infrastructure. The series as a whole seems well-structured with clear mentoring and responsive revisions from the author.