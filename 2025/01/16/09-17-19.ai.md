This is part of the performance optimization series introducing zlib-ng support, specifically focusing on compatibility stubs for older zlib versions. The change moves the `deflateSetHeader()` compatibility handling from archive-tar.c to the central zlib-compat.h header, following the pattern established earlier in the series for other zlib compatibility functions.

The patch removes version-specific conditional compilation from archive-tar.c and instead provides a stub implementation in zlib-compat.h when building against zlib versions older than 1.2.2.1. The stub simply returns Z_OK (success) while ignoring its parameters, maintaining the existing behavior for older zlib versions. This change is part of the broader effort to centralize zlib compatibility code in preparation for zlib-ng integration.

Key technical details:
- Adds `struct gz_header_s` definition for older zlib versions
- Implements stub `deflateSetHeader()` that always succeeds
- Removes ZLIB_VERNUM checks from archive-tar.c
- Maintains identical behavior for older zlib versions
- Follows the series' pattern of centralizing compatibility code

The change is straightforward and maintains the series' goal of abstracting zlib version differences while cleaning up the codebase. It builds on prior work in the series that moved other compatibility code (like `deflateBound()`) to zlib-compat.h. The patch appears well-contained and properly integrated with the series' overall approach.