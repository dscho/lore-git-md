This is part 3/6 of a series standardizing help text output behavior across Git commands, converting `-h` output from stderr to stdout. The patch updates 13 builtin commands (am, branch, checkout--worker, checkout-index, commit-tree, commit, fsmonitor--daemon, gc, ls-files, merge, rebase, update-index) to use the new `show_usage_help_and_exit_if_asked()` helper introduced earlier in the series. 

The changeset is mechanical but thorough, replacing direct `usage_with_options()` calls with the new helper while maintaining the same exit code (129) for backward compatibility. The patch notes that t7600's "git merge -h" test could potentially be retired since t0012 now covers this behavior, though it keeps the test for now with adjusted output expectations. 

This continues the series' careful approach to consistent help output while preserving existing behavior during the transition period. The changes touch core commands but are low-risk since they only affect help text plumbing without altering functional behavior. The patch demonstrates the series' progression from infrastructure (earlier patches) to systematic command updates, with Junio Hamano handling the bulk of the mechanical conversions.