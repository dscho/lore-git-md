This thread began with D. Ben Knoble proposing to document that `git grep`'s `--line-number` and `--column` options can be negated with `--no-line-number` and `--no-column`, matching the existing `--no-color` pattern. The patch aimed to clarify that these command-line options override the corresponding `grep.lineNumber` and `grep.column` config settings.

Junio C Hamano responded, agreeing that the negated forms are useful but arguing against documenting each one separately. He pointed out that Git already has a standard convention for boolean options: using the `--[no-]option` syntax in documentation (as seen in commands like `git blame`) rather than listing negated forms individually. Junio noted this approach avoids redundancy, since the fact that command-line options override configs is a universal Git behavior that doesn't need repeating for each option. He suggested updating the patch to follow this established pattern instead. The discussion has shifted from documenting specific negated options to aligning with Git's broader documentation conventions for boolean flags.