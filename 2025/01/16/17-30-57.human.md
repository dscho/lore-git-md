This is the final patch in a series standardizing help text output behavior across Git commands, converting `-h` output from stderr to stdout. The patch converts the remaining "bog standard" usage() callers to use the new `show_usage_and_exit_if_asked()` helper.

Junio notes a correction needed in `builtin/fetch-pack.c` - the current patch adds the helper call inside the argument parsing loop, but it should instead be called before entering the loop to properly handle `-h` requests. This appears to be the only substantive change needed in this otherwise mechanical conversion patch.

The series has been carefully reviewed, with naming and implementation details settled in earlier rounds. This final patch completes the standardization effort by converting the remaining commands (21 files total) to use the new helper, ensuring consistent stdout behavior for help requests while maintaining existing error handling on stderr. The implementation appears solid, with the fetch-pack case being the only remaining tweak needed before finalizing.