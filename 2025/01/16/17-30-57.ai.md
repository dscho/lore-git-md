This is the final patch in a series standardizing help text output behavior across Git commands. The patch converts remaining builtin commands to use the new `show_usage_if_asked()` helper (note the name was simplified from earlier versions) to ensure `-h` help output goes to stdout rather than stderr.

The implementation handles a special case in `builtin/fetch-pack.c` where the original patch had an awkward placement of the help check inside a parameter parsing loop. Junio notes this should be moved before the loop for cleaner behavior, maintaining the same functionality but with better code organization. This demonstrates the careful attention being paid to edge cases in the conversion process.

The patch completes the mechanical conversion of all builtin commands, following earlier infrastructure work (patches 1-2) and conversions of other command categories (patches 3-5). The series has evolved through review feedback, particularly around naming (simplified from `show_usage_and_exit_if_asked`) and implementation details like the fd-based output handling.

While the core work of standardizing stdout behavior is now complete, the thread has noted potential follow-ups like changing the help exit code from 129 to 0, which would be handled separately to maintain focus on the current stdout standardization goal. The implementation includes comprehensive test coverage in t0012-help.sh to verify the new behavior.