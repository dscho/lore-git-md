Here is the updated dense AI thread summary incorporating the technical discussion about output flushing:

This series standardizes help text output behavior across Git commands, converting `git cmd -h` from stderr to stdout. Originated by Jonas Konrad's observation of inconsistent behavior between parse-options API (stdout) versus manual usage() implementations (stderr). The implementation introduces three key helpers - `show_usage_if_asked()` (patch 3), `show_usage_with_options_if_asked()` (patch 2), and `usage_with_options_internal()` (patch 1) - to centralize stdout behavior while maintaining exit code 129 for compatibility. These helpers use `vfdreportf()` for explicit flush control, which became a point of technical discussion between Jeff King (peff) and Junio Hamano regarding whether to use file descriptors (current) or FILE pointers (peff's suggestion) for handling output streams. Peff provided a patch demonstrating the FILE pointer approach would maintain the same functionality while being more explicit about buffering behavior, referencing commit 116d1fa6c6 that established this pattern.

The test infrastructure includes t0012-help.sh (patch 1/6) with `GIT_TEST_HELP_MUST_BE_STDOUT` to enforce stdout-only help output. Patch 4/6 converted usage_with_options() calls in 14 builtin commands to use show_usage_with_options_if_asked(), handling edge cases in merge.c and fsmonitor--daemon.c. Patch 5/6 addressed "oddball" cases in subcommands like credential and fetch-pack that didn't follow typical `-h` check conventions. The final patch (6/6) completes the standardization by converting the remaining 19 "bog standard" cases where commands directly check for `-h` and call `usage()`, including diff-files, fast-import, and rev-list.

Peff's review confirmed correct handling of edge cases where error messages remain on stderr, including scenarios where processes might write to stdout before processing "-h" flags. The series maintains exit code 129 for now, with Peff suggesting changing to 0 as future work. All built-in commands now exhibit uniform stdout behavior for help requests (-h), with comprehensive test coverage in t0012-help.sh verifying both output location and exit code behavior. The implementation is complete with all feedback addressed, including the core standardization goal and the technical discussion about output stream handling, while documenting exit code unification as follow-up work.