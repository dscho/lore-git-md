# Updated AI Thread Summary

git grep documentation update and CLI conventions clarification regarding negatable options and config/ENV precedence, with ongoing discussion about discoverability.

The thread originated with D. Ben Knoble's patch documenting `git grep`'s negatable `--line-number` and `--column` options as separate entries. Junio C Hamano suggested standardizing to Git's `--[no-]option` bracketed notation (consistent with commands like `git blame`) while noting command-line options inherently override configs. This discussion revealed broader documentation gaps about option precedence rules.

Junio implemented a comprehensive documentation patch adding a new section to `gitcli.txt` that:
1. Explicitly states command-line options override both config variables and environment variables
2. Provides concrete examples (`git tag -u` overriding `user.signingKey`, `git commit --author` overriding `GIT_AUTHOR_NAME` and `user.name`)
3. Documents the full precedence chain: command-line > environment > config
4. Places this near existing negated options guidance

brian m. carlson provided positive feedback, endorsing the documentation improvement. Eric Sunshine caught and fixed a typo in Junio's patch where "specifies" was incorrectly split as "specif es" in the `git tag -u` example. Junio has given final approval to merge the documentation patch.

In a follow-up, D. Ben Knoble:
- Discoverability concerns about centralized documentation in `git help cli` versus command-specific man pages
- Observation that all long options appear negatable (a new realization)
- Questions about potential impact on tooling (particularly Vim keybindings for manual searches)
- Seeks clarification on which Boolean options warrant standardized documentation

The thread demonstrates Git's documentation evolving to make implicit knowledge explicit while preserving backward compatibility. While the technical solution (documenting precedence rules and negatable option syntax) has maintainer approval, Knoble's follow-up raises important UX considerations about how users discover these conventions. The discussion remains constructive, focused on implementation details now that core principles are established. No behavior changes were proposed - this remains a documentation clarification addressing both immediate needs and general patterns.