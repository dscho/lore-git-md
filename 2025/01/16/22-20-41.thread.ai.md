# Updated AI Thread Summary

git grep documentation update and CLI conventions clarification regarding negatable options and config/ENV precedence, with subsequent typo fix.

The thread began with D. Ben Knoble's patch documenting `git grep`' negatable `--line-number` and `--column` options as separate entries. Junio C Hamano suggested standardizing to Git's `--[no-]option` bracketed notation, consistent with commands like `git blame`, while noting command-line options inherently override configs. This discussion uncovered broader documentation gaps about option precedence rules.

Junio then implemented a documentation patch adding a new section to `gitcli.txt` that:
1. Explicitly states command-line options override both config variables and environment variables
2. Provides concrete examples (`git tag -u` overriding `user.signingKey`, `git commit --author` overriding `GIT_AUTHOR_NAME` and `user.name`)
3. Documents the full precedence chain: command-line > environment > config
4. Places this near existing negated options guidance

brian m. carlson responded with positive feedback, endorsing the documentation improvement for its value to multiple audiences. Eric Sunshine subsequently caught and fixed a typo in Junio's patch where "specifies" was incorrectly split as "specif es" in the `git tag -u` example. This correction maintains the patch's technical accuracy while improving its polish.

The thread demonstrates Git's documentation evolving to make implicit knowledge explicit while preserving backward compatibility. The maintainer's sign-off and community response establish this as authoritative guidance on CLI conventions (`--[no-]option` notation) and precedence hierarchy (CLI > ENV > config), connecting specific cases (`git grep` options) to universal principles. No behavior changes were proposed - this remains a documentation clarification addressing both immediate needs and general patterns, now with improved accuracy.