This is patch 4/6 in a series standardizing help text output behavior across Git commands. The patch converts usage_with_options() calls in builtin commands to use the new show_usage_with_options_if_asked() helper, ensuring help text goes to stdout rather than stderr when explicitly requested with -h.

The changes touch 14 files, primarily replacing direct "-h" checks and usage_with_options() calls with the new helper function. Notable conversions include builtin/merge.c (where a special case test for corrupt index handling is preserved) and builtin/fsmonitor--daemon.c (which maintains its platform-specific error message). The patch demonstrates careful attention to edge cases while maintaining existing functionality - for example, the test in t7600-merge.sh is updated to expect stdout output but keeps its special verification of behavior with a corrupt index.

This continues the series' pattern of mechanical conversions while handling special cases appropriately. The implementation is straightforward and consistent with the infrastructure changes introduced in earlier patches. Review feedback from earlier versions has already been incorporated, including the helper function name refinement to show_usage_with_options_if_asked(). The patch maintains the series' focus on output stream behavior while leaving exit code unification (129 vs 0) as a documented follow-up task.