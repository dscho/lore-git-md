This is a technical refinement in the ongoing ref-filter memory management discussion, specifically addressing the `is_base` functionality. Jeff King builds on René Scharfe's earlier work while proposing further internal reorganization to store commit references directly in atom structs rather than the format's string list.

The key changes in this patch:
1. Moves `is_base` tip storage from `format->is_base_tips` to individual atom structs (`used_atom.u.base`), mirroring the earlier approach taken with ahead-behind calculations
2. Eliminates the `ref_format` parameter from `filter_is_base()`, making the data flow more explicit
3. Updates `can_do_iterative_format()` to check for `ATOM_ISBASE` atoms rather than the format's string list
4. Cleans up related string list handling in `ref_format_clear()`

The implementation maintains all existing functionality while continuing the thread's trend toward more contained state management. This appears to be a tactical step toward the hybrid approach discussed in the thread, where core calculation logic remains unchanged but data storage moves to atom structs. The changes touch `ref-filter.c` and `ref-filter.h`, with no behavioral changes expected for end users of `%(is-base:)` formatting.

This patch represents incremental progress in the architectural discussion, showing practical implementation of the atom-struct storage approach for both ahead-behind and is-base functionality. It demonstrates how the hybrid model can be applied consistently across related features in the ref-filter subsystem.