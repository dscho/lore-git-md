This is a follow-up discussion in the series standardizing help text output behavior across Git commands. The thread has been focused on converting `git cmd -h` output from stderr to stdout while maintaining compatibility.

Junio Hamano responds to Jeff King's suggestion about changing the exit code for help requests from 129 to 0. They agree this is an orthogonal issue that could be handled separately. Junio notes that while builtins would be easy to update, non-builtin commands would need individual attention, though these are now rare. 

The exchange shows the series is nearing completion, with the core behavior change (stdout output) now established as the expected standard. Any remaining deviations in non-builtin commands can be treated as bugs to fix later, rather than requiring further policy discussion. The exit code change is acknowledged as a possible future refinement but not blocking the current series.