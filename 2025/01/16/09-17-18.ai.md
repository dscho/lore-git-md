This is a refactoring patch in the zlib-ng optimization series, moving a compatibility shim for `deflateBound()` into the new zlib compatibility infrastructure.

The patch relocates the `deflateBound()` compatibility definition from git-zlib.c to compat/zlib-compat.h, where it logically belongs with other zlib compatibility shims. The function is needed for zlib versions older than 1.2.0, and this move prepares for future zlib-ng integration by centralizing all compatibility logic.

The change is straightforward - it removes the macro definition from git-zlib.c and adds it to the new header file with the same conditional compilation guards (checking either NO_DEFLATE_BOUND or ZLIB_VERNUM < 0x1200). The actual implementation remains identical, using the same formula to calculate the bound.

This continues the series' pattern of cleaning up zlib compatibility code while maintaining support for older zlib versions. The change is mechanical and low-risk, with no functional impact - it just reorganizes where the compatibility code lives. The patch fits naturally into the series' progression toward better zlib abstraction and zlib-ng support.