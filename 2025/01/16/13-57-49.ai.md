This is a review comment on patch 8/10 in the packed-refs validation series, focusing on object consistency checking. Patrick Steinhardt provides a brief but pointed response to the author's proposed approach for verifying object existence in packed refs.

The original patch proposed adding object validation by iterating through packed ref entries and using `parse_object()` to check each OID's existence, with a new `packed_fsck_ref_oid()` function to encapsulate this logic. Patrick's terse "This one, as well" suggests he sees similar architectural issues here as in his previous feedback on patch 7/10 - likely continuing his push for backend-agnostic validation design rather than packed-backend-specific checks.

The context shows Patrick has been consistently advocating for moving validation logic to a more generic level (via `git refs verify` infrastructure) rather than building packed-backend-specific validation state. His previous review of patch 7/10 suggested eliminating intermediate parsing state structures entirely, and this comment implies he sees the object validation approach in patch 8 as needing similar reconsideration within that broader architectural vision.

This continues the thread's pattern where Patrick identifies opportunities to simplify the implementation by pushing validation concerns up to more generic layers rather than adding packed-backend-specific infrastructure. The author will likely need to reconsider how object validation fits into the emerging backend-agnostic design being shaped through this review process.