This thread addresses the standardization effort to make `git cmd -h` output consistently go to stdout rather than stderr, initiated by Jonas Konrad's observation of the inconsistency between parse-options API and manual usage() implementations. The series introduces helper functions to centralize this behavior while maintaining compatibility with exit code 129.

Jeff King (Peff) has been closely reviewing the implementation, raising thoughtful points about function naming and implementation details. His latest feedback confirms the series correctly handles subtle edge cases, such as ensuring error messages about failed formatting still go to stderr even when the main output uses stdout. The patches follow a careful staging—infrastructure first, then command conversions—with test coverage in t0012-help.sh enforcing the new behavior.

Junio Hamano has been handling edge cases in the conversion, such as builtin/var.c's unique handling of "-h". Peff's most recent review of v3 indicates the technical work is largely complete, with only minor refinements remaining. He notes the infrastructure now makes it straightforward to consider changing the help exit code from 129 to 0, though this would need separate handling for any remaining non-builtin commands. The systematic approach has successfully addressed this long-standing inconsistency, with only final polishing left before the series can be merged.