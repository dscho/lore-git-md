Here's the updated human-readable thread summary:

This thread addresses a long-standing inconsistency in how Git commands output help text, with some using stderr (manual usage() calls) and others stdout (parse-options API). Jonas Konrad's series systematically converts all commands to use stdout for -h output while maintaining proper error handling. The implementation introduces new helper functions in parse-options.c and usage.c to centralize this behavior, carefully preserving exit codes and edge cases.

Jeff King (Peff) has been closely reviewing the implementation details, particularly around patch 4/6 which adds a vfdreportf() helper for flexible output handling. In the latest exchange, Peff confirms the patch correctly handles an edge case where message formatting failures still use stderr, while the main functionality properly routes help text to stdout. This shows the careful attention being paid to subtle correctness details even in the larger mechanical conversion.

The series follows a methodical approach: first adding infrastructure (patches 1-2), then converting commands in stages (3-6). The current focus is on "oddball" usage() callers, with Junio Hamano and Peff discussing special cases like builtin/var.c that don't explicitly check for -h. Test coverage in t0012-help.sh enforces the new stdout-only behavior while removing transitional stderr accommodations. When complete, all Git commands will show consistent behavior: -h output on stdout (exit 0) while errors use stderr (non-zero exit). Only minor naming and implementation details remain under discussion as the series nears completion.