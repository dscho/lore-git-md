This is part 4/6 of the series standardizing help text output behavior across Git commands. The patch introduces a new helper function `show_usage_and_exit_if_asked()` in usage.c to handle the common pattern where commands check for "-h" and call `usage()`. 

The helper centralizes the help text output behavior while maintaining the current exit code 129 for compatibility, though the commit message notes this exit code may be reconsidered later since help requests are successful operations. The implementation builds on existing error reporting infrastructure by adding a new `vfdreportf()` function that allows directing output to either stdout or stderr, though the helper currently uses stdout for help text.

This follows the series' pattern of first establishing infrastructure (patch 1's parse-options helper) then systematically converting commands. The change is mechanical but important for consistency, with test coverage already established in t0012-help.sh from earlier patches. The implementation shows careful attention to backward compatibility while moving toward the goal of consistent help text output behavior.