This is a continuation of the `the_repository` removal effort, specifically patch 4/5 in the series converting pack-writing functions to avoid global variables. The patch focuses on converting `write_rev_file()` and related functions to take a `hash_algo` parameter rather than using `the_hash_algo` global.

Junio Hamano provides review feedback noting that while the changes to `write_rev_file()` and other functions in the patch look correct, the commit message should explicitly mention all functions being converted (not just `write_rev_file()`). The changes propagate through callers in `midx-write.c` and other subsystems, following the series' established pattern of passing `the_hash_algo` from unconverted upper layers while removing global usage from lower layers.

The patch continues the incremental strategy seen throughout the series - converting specific functions to take hash algorithm parameters while temporarily allowing some callers to pass `the_hash_algo` until their layers can be fully converted. This maintains compatibility while making progress on the global variable removal. The review focuses on documentation completeness rather than technical concerns, suggesting the implementation approach is sound.