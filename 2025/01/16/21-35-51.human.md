This is part 4 of a 6-patch series standardizing help text output behavior across Git commands. The series converts `git cmd -h` output from stderr to stdout for consistency.

This patch focuses on converting builtin commands that use `usage_with_options()` to show help text. It replaces manual "-h" checks with the new `show_usage_with_options_if_asked()` helper introduced earlier in the series, ensuring help text goes to stdout while maintaining the existing exit code (129). The changes touch 14 files including major commands like `git merge`, `git commit`, and `git rebase`.

The patch preserves a special test case in t7600-merge.sh that verifies help works with a corrupt index file, though the same basic functionality is already covered by t0012-help.sh. The implementation appears thorough and mechanically sound, with each conversion following the same clear pattern.

This continues the series' careful approach of maintaining backward compatibility while moving toward more consistent behavior. The changes are straightforward mechanical conversions without functional changes beyond the intended stdout redirection.