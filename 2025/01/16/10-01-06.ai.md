Review feedback on the `git backfill` series (patch 4/5). This is part of the ongoing PATH WALK series introducing new path-walking functionality.

Patrick Steinhardt provides two specific pieces of feedback on the implementation details. First, he questions the asymmetry in pattern matching logic where one branch checks `== NOT_MATCHED` while another uses `!= MATCHED`, suggesting this might need clarification or consistency. Second, he notes that while the `stdin_pl` parameter name in the test helper could be more descriptive, he acknowledges this is less critical since it's test-only code.

The feedback focuses on code style and implementation details rather than fundamental design issues, continuing the pattern of thorough but non-blocking review seen in previous patches. No new concerns are raised about the core functionality or the `--sparse` option itself. The exchange maintains the collaborative tone of the thread, with Patrick providing specific observations while leaving implementation decisions to the author.

Key details mentioned include the pattern matching behavior in `path-walk.c` (using `path_matches_pattern_list`) and the test helper's new `stdin_pl` option. The feedback doesn't address any of the previously noted open questions about experimental status or object checking methods.