This is a reply in the ongoing `the_repository` removal effort, specifically discussing patch 4/5 in the series that converts pack-writing functions to avoid global variables. Patrick Steinhardt is responding to Karthik Nayak's patch that makes `write_rev_file()` take an explicit `hash_algo` parameter instead of using `the_hash_algo` global.

Patrick agrees with the approach taken in the patch, particularly noting that it makes sense to avoid reintroducing global variables in `midx-write.c` where they've already been eliminated. He confirms the patch's strategy of passing `the_hash_algo` from upper layers (where globals are still used) while using the repository context in `midx-write.c` (where globals have been removed) is sensible.

The reply reinforces the series' incremental approach to removing globals - accepting temporary usage in some layers while cleaning it up in others, with special attention to maintaining consistency in subsystems that have already been converted. This aligns with the thread's broader context of systematically eliminating `the_repository` and `the_hash_algo` dependencies across Git's pack-writing infrastructure.