This is a refactoring patch in the ongoing ref-filter memory management discussion, proposing a hybrid approach for handling is-base calculations. Jeff King builds on René Scharfe's earlier work to move the is-base state from a global string list into individual atom structs, similar to the earlier ahead-behind changes.

The key changes:
- Stores is-base commit references directly in the atom structs rather than a format-wide string list
- Removes the ref_format parameter from filter_is_base() and related functions
- Updates the can_do_iterative_format() check to consider is-base atoms
- Maintains all existing functionality while making the data flow more explicit

The patch appears well-considered, addressing both the immediate memory management issues and the longer-term API design goals discussed in the thread. It represents a pragmatic middle ground between global simplicity and explicit parameter passing, continuing the thread's trend toward better state containment. The changes are focused and the diff shows careful attention to maintaining all existing behavior while cleaning up the implementation.