This is a technical discussion about ref-filter memory management, continuing the thread on sharing state between formatting and sorting operations. Jeff King follows up on his previous proposal to store ahead-behind calculation data directly in atom structs rather than the format->bases string_list.

The key development here is King's implementation tweak that removes the ref_format parameter from filter_ahead_behind(), since the bases are now stored in used_atom.u.base.commit. This makes the global data access more explicit while maintaining the same functionality. The patch shows the necessary changes across builtin/branch.c, ref-filter.c and ref-filter.h to accommodate this.

King acknowledges this still involves "magic global data" but argues it's less problematic when contained within the existing atom structure. The change represents a middle ground between René Scharfe's preference for simplicity and King's longer-term goal of explicit parameter passing for lib-ification. The discussion continues to balance immediate code cleanliness against future API design considerations for the ref-filter subsystem.