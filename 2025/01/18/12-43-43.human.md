This is a technical discussion about hash algorithm safety in the ongoing hash algorithm refactoring series.

Jeff King follows up on an earlier discussion about how `hash_algo_by_ptr()` should handle unsafe hash algorithm variants. He proposes two alternative approaches to the current implementation: either returning `GIT_HASH_UNKNOWN` for unsafe variants (which would force callers to handle the case explicitly) or keeping the current loop-based approach that maintains pointer-to-ID conversion symmetry but risks undefined behavior with NULL inputs.

The email reveals an interesting edge case where passing NULL to the current implementation leads to undefined behavior (as shown by a test case in t1517), though this doesn't currently cause problems because the garbage algorithm value isn't actually used. Jeff suggests that returning `GIT_HASH_UNKNOWN` would be safer as it would catch such cases with a BUG() rather than silently proceeding with bad data.

This is a nuanced discussion about API safety in the hash algorithm infrastructure, showing careful attention to edge cases and potential future maintenance hazards. The conversation demonstrates the reviewers' thoroughness in ensuring the refactoring doesn't introduce subtle bugs, even when the current behavior appears to work by accident. The NULL pointer case, while not directly related to the unsafe variants discussion, highlights how the change might improve overall robustness.