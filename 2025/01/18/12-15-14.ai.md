This is a discussion about the checkpoint API implementation in the hash algorithm refactoring series. Jeff King responds to Taylor Blau's concerns about the placement of `hashfile_checkpoint_init()` calls in `bulk-checkin.c`.

The key technical issue revolves around the lifetime management of hashfile checkpoints when dealing with packfile size limits. Jeff identifies a potential bug where a checkpoint could reference an invalid packfile after truncation and restart, though analysis shows the current implementation is safe due to the control flow. He suggests moving the checkpoint initialization into the loop while keeping the variable declaration outside, matching Taylor's earlier suggestion.

Jeff acknowledges the complexity of the retry logic in `deflate_blob_to_pack()` and agrees with Taylor's assessment that more fundamental refactoring would be needed to properly clean up the checkpoint handling. The discussion reveals the checkpoint API's tight coupling with the pack-writing implementation, particularly around the `already_written()` check that uses checkpoint data after potential retries.

Both agree the current patch's minimal changes are the right approach, deferring larger structural cleanups. Jeff provides additional context about the control flow that could inform future refactoring, noting the loop can only execute twice and suggesting a more explicit retry structure might improve readability. The exchange demonstrates careful consideration of edge cases in the checkpoint API's integration with Git's pack-writing machinery.