This is the final part (3/3) of René Scharfe's ref-filter cleanup series, completing the migration to atom-struct storage for state management. The patch removes the now-obsolete `ref_format_clear()` and `ref_format_init()` functions after their memory management responsibilities were eliminated by the earlier parts of the series.

The changes are mechanical removals of these no-op functions and their calls from four command implementations (`branch`, `for-each-ref`, `tag`, `verify-tag`). The diff shows straightforward deletion of the initialization/cleanup calls and their implementations, with no behavior changes. This completes the architectural shift where state is now managed directly in atom structs rather than through the ref_format layer.

The removal is safe because parts 1 and 2 of the series already migrated all memory management responsibilities to the atom structs themselves. This final patch represents the last step in eliminating the old infrastructure after ensuring no remaining dependencies exist. The changes are consistent with the thread's technical direction and maintainer feedback, demonstrating the successful completion of the ref-filter state management redesign.