This is a follow-up discussion about a refactoring patch in Usman Akinyemi's OS version capability series. Christian Couder weighs in on the implementation details of the `redact_non_printables()` function that sanitizes version strings.

The discussion centers around whether to use Git's `isprint()` macro from `sane-ctype.h` instead of the current manual character range check. Christian notes that using `isprint()` would require special handling for spaces (ASCII 32) since the macro considers them printable, while the current implementation redacts them. He presents both approaches - the current version that treats spaces and non-printables uniformly by replacing them with dots, versus a hypothetical `isprint()` version that would need explicit space handling.

Christian suggests two possible approaches for incorporating this change: either as a separate preparatory patch at the start of the series, or as a follow-up after the main series settles. This reflects the ongoing refinement of the series' test infrastructure while the core functionality remains under debate.

The email doesn't propose a resolution but rather explores the technical tradeoffs between the approaches, noting that the choice may come down to stylistic preference since both would achieve the same sanitization behavior. This continues the thread's pattern of incremental improvements to the series' supporting code while the fundamental design questions remain open.