This is the final patch in a series addressing reftable corruption issues, specifically focusing on preventing invalid index updates after records are written. The patch enforces that `reftable_writer_set_limits()` can only be called before any records are added, preventing header/footer mismatches.

Patrick Steinhardt provides minor feedback on Karthik Nayak's implementation, suggesting two improvements: (1) clarifying that the change is preventative rather than fixing an existing bug (though not requiring a reroll), and (2) proposing to use the `next` field rather than `last_key` to detect when writing has begun, as it more directly tracks block writing state. He also requests adding a unit test to demonstrate the error case.

The technical discussion has largely converged, with this being the final refinement to an otherwise complete solution. The patch already includes comprehensive documentation updates and converts the function to return an error code when called at the wrong time. Patrick's suggestions aim to make the state detection more robust and the test coverage more explicit, but don't fundamentally change the approach.

Key technical details:
- Changes `reftable_writer_set_limits()` return type from void to int
- Adds state validation using writer's `last_key` (or potentially `next`)
- Documents the API contract requiring limits to be set first
- Affects reftable writer implementation and header/footer consistency
- Builds on previous patches addressing integer width and API visibility

The thread shows all major technical concerns have been addressed, with these being final polish items before merging. The series provides important safeguards for reftable migration, particularly for large repositories.