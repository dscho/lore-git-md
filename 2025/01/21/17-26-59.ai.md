Follow-up investigation into `git fetch` tag behavior regression in shallow clones, now examining transport protocol differences. Danila Manturov reports that while the previously discussed tag-fetching issue (where tags pointing to shallow boundary commits weren't fetched with `--depth=1`) appears fixed when using JSch (Java SSH library), it still fails with native SSH/HTTPS transports. This suggests the fix in 6c915c3f85 ("fetch: do not ask for HEAD unnecessarily") may have transport-specific behavior or that different protocol implementations handle shallow clone tag resolution differently. The observation builds on the thread's earlier findings where the regression was traced to commit5f212684abb6 and partially addressed, but now reveals additional complexity based on transport layer implementation. Bence Ferdinandy's earlier question about why the existing fix doesn't cover all cases remains relevant, with this new data point indicating protocol handlers may need separate consideration. The thread continues to investigate why tag fetching behavior varies between transport mechanisms for the same shallow clone scenario.