This is a follow-up discussion about the rejected `osversion.command` config feature in the OS version capability series, focusing on platform-specific implementation concerns.

Randall Becker responds to Junio Hamano's rejection of the configurable command approach, agreeing that NonStop should follow the Windows precedent of using platform-specific calls in the `compat/` layer rather than runtime command execution. While expressing personal reservations about the feature's utility (preferring direct `uname -a` usage for support purposes, and noting build-time OS version would be more useful than runtime), Becker commits to implementing a NonStop-specific solution via `compat/` once the series design is finalized. The exchange confirms the architectural direction of platform-specific compile-time solutions over runtime configuration, with Becker positioned to provide the NonStop implementation but still questioning the feature's fundamental value proposition. This maintains the thread's trajectory toward eliminating the security-risk command execution while continuing to debate the feature's core purpose.