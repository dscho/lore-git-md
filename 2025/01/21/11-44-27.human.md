This is the final patch in a 3-part series hardening the reftable backend against corruption during large repository migrations. The patch enforces that `reftable_writer_set_limits()` can only be called before any records are written, preventing header/footer mismatches in the reftable format.

Karthik Nayak responds to Patrick Steinhardt's review feedback on the implementation. The key discussion points are around the state tracking mechanism - Patrick suggests using the `next` field rather than `last_key` for detecting when records have been written, as it more directly tracks block writing state. Karthik considers this but notes it would still allow limit changes after some records are written, and proposes checking both fields for stricter validation.

The patch already includes comprehensive documentation of the API contract and test coverage for the error case. Karthik agrees to add an explicit unit test demonstrating the error condition, as suggested by Patrick. The changes follow through on the series' goal of preventing corruption during reftable conversion, particularly for repositories with large reflogs.

With all technical concerns addressed in previous rounds and only minor documentation/clarification tweaks remaining, this patch appears ready for merging as part of the completed series. The careful attention to edge cases and thorough test coverage make this a robust solution to the identified problem.