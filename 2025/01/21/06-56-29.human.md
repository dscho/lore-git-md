Final refinement to reftable writer safety checks

This email from Patrick Steinhardt provides the last review feedback on the third patch in the series hardening reftable's update index handling. The patch enforces that `reftable_writer_set_limits()` can only be called before any records are written, preventing header/footer mismatches.

Patrick suggests two minor improvements:
1. Clarifies the commit message wording to frame this as prevention rather than bug-fixing
2. Proposes using the `next` field rather than `last_key` to detect when writing has begun, as it's more directly tied to header writing

He also requests adding a unit test to demonstrate the error case, though notes this isn't worth a reroll on its own. The discussion shows the patch is otherwise technically sound and addresses the corruption prevention requirements identified earlier in the thread.

This concludes the review cycle for this series, with all three patches now ready for merging after incorporating these final tweaks. The changes provide robust protection against corruption while maintaining backward compatibility, particularly important for large repository migrations to reftable.