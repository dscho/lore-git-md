Build system refactoring in Patrick Steinhardt's Meson series (patch 10/13) that optimizes tool discovery. This patch removes redundant `sed` executable lookups by reusing a top-level variable declaration, following the pattern established for other core utilities in the build system.

The change modifies two files: it updates `Documentation/howto/meson.build` to reference the pre-declared `sed` variable instead of calling `find_program()` repeatedly, and removes `sed` from the list of tools being verified in a loop in `meson.build`. This builds on the build system improvements from earlier patches, particularly the tool path management changes in patch 8/13.

Key technical details:
- Eliminates multiple `find_program('sed')` calls in favor of single declaration
- Maintains same tool verification (just moves it earlier in build process)
- Keeps all existing functionality intact
- Follows same pattern used for other core utilities (`cat`, `grep`, etc.)

This appears to be a straightforward optimization that should have no functional impact but makes the build process slightly more efficient by avoiding redundant tool discovery. The change is consistent with the series' broader goal of simplifying and modernizing the build system infrastructure.