Bugfix for potential integer parsing overflow in `git apply`. Phillip Wood addresses an edge case where `strtoul()` could silently ignore numeric overflows when parsing hunk headers in patch files. The fix properly checks `errno` after the conversion and fails if an overflow occurred. While the error message ("corrupt patch") isn't particularly descriptive, the author notes this is acceptable since the case is extremely unlikely to occur in practice. The patch includes a test case that verifies the overflow detection. The commit message also references a related potential fix needed for `git add -p` but defers that to a separate discussion. This appears to be a straightforward and well-tested fix for a subtle parsing issue.