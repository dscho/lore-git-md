This is a review comment from Junio Hamano on patch 7/8 of the packed-refs validation series, focusing on the sortedness check implementation.

Junio points out that the sortedness verification should only be performed when the packed-refs file explicitly declares itself as sorted in its header. The current implementation accepts unsorted files (for backward compatibility with pre-v1.5.0 Git), so enforcing sortedness universally would be too strict. This aligns with earlier discussions about maintaining compatibility with historical behavior.

He also notes a minor technical detail about the line number storage in the new `fsck_packed_ref_entry` struct - suggesting `ulong` might be more appropriate than `int` for consistency, though acknowledging this isn't critical since even `int` provides sufficient range.

The comment continues the thread's pattern of careful refinement, with Junio ensuring the validation checks match Git's actual runtime behavior while maintaining backward compatibility. This patch is part of the final polishing before the series gets integrated, addressing one of the last remaining technical points about when sortedness verification should apply.