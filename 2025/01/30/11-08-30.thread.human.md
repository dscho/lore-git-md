Here's a human-readable thread summary:

This thread addresses a subtle bug in `git apply`'s patch parsing where large line numbers in hunk headers could silently overflow due to unchecked `strtoul()` calls. Phillip Wood's patch fixes this by properly initializing and checking `errno` to detect numeric overflows, though the error message remains a generic "corrupt patch" for now. The change is well-contained in `apply.c` with a new test case in `t4100-apply-stat.sh` that verifies overflow detection using absurdly large line numbers.

While the same issue exists in `git add -p`, that's being left for a future fix pending resolution of another related patch series. The current change is straightforward with good test coverage, though there's room to improve the error messaging in a follow-up. This appears to be a defensive fix for an edge case that wouldn't affect normal usage but properly handles malformed input. The patch looks ready to merge as-is, with the `git add -p` case acknowledged but intentionally deferred.