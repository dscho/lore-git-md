Resending notice for a repack bugfix patch. This is a follow-up to an RFC patch addressing promisor pack handling in `git repack`, where promisor objects were bypassing keep-pack protections. The original patch had line-wrapping issues that prevented clean application, as noted by brian m. carlson. The author (Tomáš Trnka) acknowledges the formatting problem, caused by KMail unexpectedly re-enabling line wrapping during resending, and promises a corrected version. 

The technical discussion about the patch's merits (which would make promisor pack handling respect `--keep-pack` and `.keep` files like regular repacks do) remains on hold until the formatting is fixed. The thread is still in RFC status, with open questions about test placement and awaiting input from repack/partial-clone experts. This email itself contains no technical content - it's an administrative note about the resend issue.