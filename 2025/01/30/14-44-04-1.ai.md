This is a build system patch (12/13) in Patrick Steinhardt's Meson refactoring series, focusing on consistent tool path resolution in documentation generation. The patch ensures all documentation-related `find_program()` calls use the custom `program_path` variable that was previously established for Windows compatibility, rather than relying on system PATH lookup. This affects several documentation tools: asciidoc, asciidoctor, xmlto, and xsltproc.

The change builds on the series' earlier Windows-specific path handling improvements (patches 8-9/13) and tool discovery optimizations (patch 10-13). It removes redundant `required: true` parameters since they're Meson's default, continuing the series' trend toward cleaner build configuration. The modification touches only `Documentation/meson.build`, updating program lookup calls while maintaining the same fallback behavior for asciidoc/asciidoctor auto-detection.

This patch completes the Windows toolchain reliability work by ensuring documentation tools follow the same path resolution pattern as other build tools in the system. It should be reviewed alongside the series' other path-handling changes, particularly for verification on Windows platforms where `program_path` contains Git for Windows tool locations. The change appears straightforward with no visible regressions, maintaining existing documentation backend selection logic while making the path resolution more consistent.