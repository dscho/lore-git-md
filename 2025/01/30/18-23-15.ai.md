This is a review response in the context of the packed-refs validation bugfix series, specifically addressing patch 3/8 which checks packed-refs filetype. Junio Hamano engages with shejialuo's implementation that uses `open_nofollow()` to detect symlink attacks on the packed-refs file.

The key technical discussion revolves around race condition handling between filetype checking and content validation. Junio questions whether the proposed `open_nofollow()` approach fundamentally solves more than a simple `lstat()` check would, noting that both methods remain vulnerable to post-validation file swaps. He observes the race window merely shifts rather than disappears, and points out the fallback to `lstat()` on platforms without `O_NOFOLLOW` weakens the portability argument.

The review approves the code's mechanical correctness - the new validation properly reports non-regular files via `FSCK_MSG_BAD_REF_FILETYPE` and includes appropriate test coverage in t0602-reffiles-fsck.sh. Junio suggests clarifying the commit title to explicitly mention "regular file" checks and proposes future test expansion to combine HEAD corruption with packed-refs tampering for more comprehensive validation scenarios.

This interaction continues the series' pattern of rigorous review around filesystem race conditions and validation completeness, with particular attention to the test strategy's evolution. The discussion surfaces important constraints about the limits of TOCTOU prevention in refs validation while maintaining the patch's forward progress through constructive critique of implementation details.