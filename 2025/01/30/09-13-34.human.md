Test infrastructure patch completing a series converting unit tests to the Clar framework. This final patch in the 4-part series converts the strcmp-offset test, following the same pattern as previous conversions of hashmap, decorate, and strbuf tests.

The patch rewrites the test cases for `strcmp_offset()` functionality into standalone Clar test functions, maintaining the same test coverage while changing only the framework. Key test cases preserved include: empty string comparison, equal strings, completely different strings, strings with a single character difference, and strings of different lengths. Each case verifies both the comparison result and the reported offset where strings diverge.

Build system files (Makefile and meson.build) are updated to remove the old test and add the new Clar version, following the established pattern from earlier patches in the series. The conversion appears mechanical and complete, with no functional changes to the actual test logic - only the assertion syntax changes from `check_int/check_uint` to Clar's `cl_assert_equal_i`.

This concludes the current batch of test conversions mentored by Patrick Steinhardt. The series appears ready for review, with all patches following a consistent approach to migrating tests while preserving their exact behavior. Reviewers will likely focus on verifying the test semantics remain identical, particularly for edge cases in string comparison behavior.