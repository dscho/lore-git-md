This is a follow-up in the ongoing discussion about runtime configuration paths and the `PREFIX` environment variable behavior. Randall S. reports successful testing of `GIT_CONFIG_SYSTEM` as a working solution for their use case of controlling system config file location at runtime, though they seek confirmation whether this is an officially supported mechanism.

Key technical details:
- `GIT_CONFIG_SYSTEM` successfully redirects config lookups when tested (`git config --system --list` reads from specified path)
- The test confirms this solves Randall's original permission restriction problem
- Open question remains about `GIT_CONFIG_SYSTEM`'s official status (documented vs coincidental behavior)
- Thread context shows this variable has test coverage in t1300-config.sh
- Junio previously clarified this as a viable runtime mechanism while maintaining the distinction from build-time `$(prefix)`

The email advances the thread by:
1. Providing empirical validation of `GIT_CONFIG_SYSTEM` functionality
2. Moving discussion from theoretical solutions to tested implementation
3. Raising the documentation question for this environment variable
4. Maintaining focus on runtime configuration needs while sidestepping the `PREFIX` debate

The thread appears to be converging on `GIT_CONFIG_SYSTEM` as the preferred solution, though documentation clarity remains an open item. Randall's testing confirms this meets their technical requirements if the variable's supported status is confirmed.