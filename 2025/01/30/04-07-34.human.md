Bugfix patch adding packed-refs header validation as part of the larger packed-refs fsck series. This is patch 4/8 in v2 of the series, which focuses specifically on validating the packed-refs file format (with object validation deferred to future work per Patrick Steinhardt's architectural direction).

The patch introduces three new validation checks for the packed-refs header: (1) detecting missing headers (warning only, for compatibility), (2) rejecting malformed headers that don't start with "# pack-refs with:" (hard error), and (3) warning about non-standard header content (even if syntactically valid). It implements these through new fsck messages and corresponding test cases.

The implementation reads the packed-refs file via strbuf_read and adds a helper function to parse lines while checking for proper newline termination. The changes align with existing validation patterns in create_snapshot() while adding more rigorous checks. Test coverage is thorough, verifying all three new validation scenarios with various malformed headers.

This appears to be a well-constructed patch that fits cleanly into the larger series. The commit message clearly explains the motivation and tradeoffs (particularly around backward compatibility), and the implementation follows established patterns in the codebase. The test cases are comprehensive and the new fsck messages are properly documented.