This is a follow-up discussion in Patrick Steinhardt's Meson build system refactoring series, specifically about patch 1/11 which deals with GIT_EXEC_PATH handling when runtime prefix is enabled. 

The exchange clarifies the issue where the Meson build was incorrectly always using absolute paths for GIT_EXEC_PATH when runtime prefix is enabled, unlike the Makefile which properly handles both cases. Justin Tobler confirms understanding of the problem - that this prevents proper relative path resolution and falls back to FALLBACK_RUNTIME_PREFIX. Patrick acknowledges Justin's correct interpretation of the issue.

Key technical points:
- The fix in patch 1 makes Meson match Makefile behavior for GIT_EXEC_PATH
- Runtime prefix mode requires relative path resolution from binary location
- Absolute paths break this relative resolution mechanism
- Justin caught and fixed a typo ("correcty" → "correctly") in the original commit message

This is a minor but important discussion confirming proper understanding of a build system behavior difference between Make and Meson, with Justin providing useful feedback that improved the patch's documentation. The thread shows good technical alignment between the contributor and reviewer on this specific build system detail.