This is the first patch in a 20-part series refactoring the reftable library to remove Git dependencies. It's a technical refactoring that replaces Git's `read_in_full()` with direct POSIX `read()` calls in the reftable stack implementation.

The change removes one of the final Git-specific I/O helpers from the reftable code. The patch carefully preserves the error handling semantics while simplifying the implementation - since reftable never uses non-blocking I/O, it can safely omit the `EWOULDBLOCK` handling that Git's version includes. The new implementation uses a loop to handle `EAGAIN`/`EINTR` retries explicitly.

This appears to be a straightforward, well-justified change that advances the series' goal of making reftable usable as a standalone library. The commit message clearly explains the rationale and what aspects of Git's implementation were intentionally not ported over. The diff shows careful attention to maintaining the same error conditions and edge case handling.