This is a build system refinement for the Rust bindings effort, specifically addressing style and readability feedback on the `libgit-sys` crate implementation from Phillip Wood's review of v7 2/4. The changes are minor but important for consistency:

Josh Steadmon responds to two specific review points about the Makefile implementation. First, he breaks up long source lists into variables for better maintainability (a suggestion Phillip made about the `partial_symbol_export.o` rule). Second, he fixes the multiline comment style in `public_symbol_export.c` to match Git's standard format (starting with `/*` on its own line).

These are the final polish touches on the `libgit-sys` crate's build infrastructure, which provides Rust FFI bindings to `libgit.a`. The changes show the careful attention to detail in this multi-patch series that's now nearing completion, with all substantive technical concerns already addressed in earlier rounds. The thread context indicates this work has maintainer approval and is ready for integration.