This is a minor build system refinement in the Rust bindings series. Junio C Hamano reviews Josh Steadmon's changes to the Makefile for building the `libgit-sys` Rust crate, suggesting a stylistic consistency improvement in how the `LIBGIT_PUB_OBJS` variable is initialized. The change follows the pattern used elsewhere in the Makefile where variables are first declared empty before being populated with `+=` operators. 

The patch is part of the final v8 series implementing Rust bindings for Git's C library, which has already received extensive review and is now in its polishing phase. Junio approves the changes overall, noting they are well-done aside from this minor stylistic suggestion. The series as a whole provides both direct C bindings (`libgit-sys`) and idiomatic Rust interfaces (`libgit`), with this particular patch focusing on build system integration details.