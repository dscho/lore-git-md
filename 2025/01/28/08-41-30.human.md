Header reorganization patch in the zlib-ng optimization series.

This is patch 4/10 in the performance-focused series introducing zlib-ng support. The change moves zlib-related includes from the broad `git-compat-util.h` header to the more specific `git-zlib.h`, reducing unnecessary inclusion of zlib functionality across the codebase. The patch carefully handles the reftable library as a special case, moving its zlib include to `reftable/system.h` to maintain the library's independence from Git's zlib wrappers.

The changeset updates 8 files with straightforward include adjustments, removing zlib from the compatibility header while adding it to files that actually need zlib functionality (archive handling, config parsing, checksum files, etc.). The reftable change demonstrates good attention to library boundaries, keeping the reftable codebase usable outside Git while still getting the zlib optimizations.

This is a clean, focused change that improves header hygiene as part of the larger zlib optimization work. The patch follows the series' established pattern of careful header management while maintaining compatibility, with all review feedback from earlier versions already incorporated.