This is a CI/build system patch in the zlib-ng optimization series, switching the Alpine Linux CI job from Make to Meson builds. The change simplifies CI configuration by letting Meson automatically detect build options rather than manually specifying them, while also preparing for easier zlib-ng integration. 

The patch updates both GitHub and GitLab CI configurations to use Meson for the Alpine Linux job, adds Meson as a dependency, and removes redundant GCC specification since it's already the default. A key addition is proper handling of the `GIT_TEST_UTF8_LOCALE` test variable through a new Meson build option, maintaining compatibility with existing test infrastructure.

This appears to be a straightforward modernization of the CI setup that aligns with the series' broader goal of improving build system flexibility. The changes are cleanly implemented with clear commit message explanations, and since this is part of a well-reviewed series with all technical issues already resolved, it seems ready for inclusion.