This is a final build system refinement in the Rust bindings series (v8 2/4). Junio C Hamano reviews Josh Steadmon's Makefile changes for the `libgit-sys` crate integration, approving the overall approach while suggesting a minor stylistic adjustment to match Git's Makefile conventions. The patch adds build infrastructure for the Rust wrapper, including:

1. Documentation of the new `INCLUDE_LIBGIT_RS` build flag
2. Addition of `LD` and `OBJCOPY` variables for Rust compatibility
3. Definition of `LIBGIT_PUB_OBJS` linking rules combining `libgit.a` with other required static libraries

Junio notes the Makefile changes are well-executed but suggests initializing `LIBGIT_PUB_OBJS` as empty before appending components, to match Git's standard Makefile pattern. This follows previous build system refinements in the series (v8 1/4's initialization improvements and v7 2/4's Makefile variable organization). The review confirms the technical approach while maintaining stylistic consistency with the project's build system conventions.