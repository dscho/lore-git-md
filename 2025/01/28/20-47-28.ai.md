This is a refinement patch in the Rust bindings series, specifically addressing config API exports in `libgit-sys`. The patch follows up on Phillip Wood's review feedback from v7, implementing two key improvements to the configset API bindings:

1. **Type safety enhancement**: The patch replaces direct casting between `config_set` and `libgit_config_set` with a proper wrapper struct, as suggested in v2 and reiterated by Phillip. This eliminates unsafe pointer casts and provides better type boundaries in the FFI layer.

2. **Style fixes**: Line wrapping for the config API functions has been adjusted to meet the 80-character limit, addressing Phillip's stylistic concern from the v7 review.

The changes maintain the core functionality of exporting configset operations (`alloc`, `free`, `add_file`, `get_int`) for use by higher-level Rust wrappers, while improving the implementation quality. This patch is part of the final polishing phase (v8) of the Rust bindings work, with all substantive technical issues now resolved and only minor style/build refinements remaining before integration.

The thread context shows this series has maintainer approval and is effectively complete, with this config API refinement being one of the last items before final submission. The changes demonstrate careful attention to FFI safety and code quality in the Rust bindings infrastructure.