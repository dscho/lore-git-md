Build system refinement for Rust bindings (v7 2/4 follow-up). Josh Steadmon addresses Phillip Wood's review feedback on the `libgit-sys` crate implementation from the Rust bindings series. The changes focus on style and readability improvements to the build system components:

1. **Makefile formatting**: Long source lists in the Makefile have been broken into variables for better readability, addressing Phillip's observation about line length. This affects the rules for building `partial_symbol_export.o` and related artifacts.

2. **Comment style fix**: The multiline comment in `public_symbol_export.c` has been corrected to use the Git project's preferred style (with `/*` on its own line), as Phillip noted in his review.

These are minor but important polish changes to the build infrastructure for the Rust FFI bindings, following through on the detailed review feedback from v7. The core functionality remains unchanged - this continues to provide the Rust wrapper crate (`libgit-sys`) that exposes Git's C library functions (starting with user agent string getters) through safe FFI bindings. The changes demonstrate responsiveness to code style concerns while maintaining all previously established technical properties of the symbol visibility management and build integration work.