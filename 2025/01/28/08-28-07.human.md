This is part of the ongoing reftable library decoupling effort, specifically addressing error handling in record comparisons. The patch modifies `reftable_record_cmp()` to return an error code when comparing records of different types, rather than aborting with a `BUG()`. This change makes the behavior more appropriate for library code where callers should handle errors gracefully.

The patch updates all callers of `reftable_record_cmp()` to check the new error return, including the priority queue implementation and merged iterator code. Test cases are also updated to verify the new error handling behavior. This continues the series' pattern of replacing Git-specific behaviors (like `BUG()`) with library-appropriate error returns.

The changes are thorough and well-tested, with corresponding updates to both implementation and test code. This appears to be a straightforward but important step in making the reftable library properly standalone. The patch fits cleanly into the larger series' goal of removing Git-specific dependencies while maintaining functionality.