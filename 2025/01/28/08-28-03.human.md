This is part of a larger refactoring effort to make the reftable library independent from Git's core infrastructure. The patch replaces Git's `write_in_full()` helper with a new reftable-specific implementation called `reftable_write_data()`, continuing the series' goal of removing Git dependencies.

The new implementation handles the same core functionality - writing all data with proper error handling for EAGAIN/EINTR cases - but does so using direct POSIX calls rather than Git's wrappers. The change is mechanical but important for the library's standalone viability, affecting several write operations in the reftable stack implementation.

This appears to be a straightforward, well-contained change that aligns with the series' architectural direction. The implementation follows the same pattern seen in previous patches for read operations, suggesting a consistent approach to the I/O refactoring. As with other patches in this series, the change maintains the same error semantics while removing Git-specific dependencies.