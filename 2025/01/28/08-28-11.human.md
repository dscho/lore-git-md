This is a refactoring patch in Patrick Steinhardt's ongoing series to decouple the reftable library from Git's core infrastructure. 

The change is straightforward: it removes one of the last remaining uses of the `ARRAY_SIZE()` macro in the reftable reader, replacing it with a direct calculation of the array length. This is part of the broader effort to eliminate dependencies on Git-specific helper functions and macros.

The patch is small and mechanical - just a single line change in `reftable/reader.c` that replaces the macro with equivalent manual array size calculation. The commit message explains the motivation clearly: to reduce dependence on Git's codebase. 

This appears to be a low-risk change since the behavior remains identical, just using a more portable way to calculate array size. It follows the pattern seen throughout this series of systematically replacing Git-specific constructs with standard C equivalents. The change is well-contained and doesn't appear to need any special review attention beyond verifying the mechanical correctness of the replacement.