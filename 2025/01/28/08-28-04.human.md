This is part of the ongoing reftable library decoupling effort, specifically addressing memory mapping functionality. The patch replaces Git's `xmmap()` with direct `mmap()` calls in the reftable blocksource code, continuing the series' work to remove Git-specific dependencies.

The change serves two purposes: it removes a hard dependency on Git's utility functions (allowing the reftable code to be used standalone), and it improves error handling by replacing Git's fatal `xmmap()` with proper error propagation when memory mapping fails. The implementation carefully maintains the same behavior while adding proper error checking, including handling the `MAP_FAILED` case and properly cleaning up resources.

This appears to be a straightforward and well-motivated change that fits cleanly into the larger series. The patch follows the established pattern seen in previous conversions, showing consistency with the overall architectural direction. The error handling is thorough, checking for mapping failures and properly propagating errors through the existing cleanup paths.

As with other patches in this series, the change is focused and clearly explained, with the commit message directly addressing both the technical change and its rationale within the broader context of making reftable independent from Git's core infrastructure.