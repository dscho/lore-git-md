This is the final patch in the Rust bindings series, implementing the higher-level `libgit` crate that provides idiomatic Rust wrappers around the FFI bindings from `libgit-sys`. The patch adds safe Rust interfaces for config value retrieval, including a `get_int()` method that properly handles error cases and type conversion.

Key technical details include:
- The `get_int()` method demonstrates the safe FFI pattern being established, converting C strings safely and using Rust's `Option` type to represent missing keys
- Build system integration is updated to include both `libgit-sys` and `libgit-rs` targets in the `all` rule when `INCLUDE_LIBGIT_RS` is set
- The implementation follows Rust best practices with proper error handling and type safety

The patch addresses Junio Hamano's feedback about build rule consolidation by simplifying the Makefile target definitions. The config API design has evolved through multiple revisions to achieve proper type safety, with this final version using Rust's type system to provide a more idiomatic interface compared to the raw C FFI.

This completes the technical implementation of the Rust bindings project, with all substantive review feedback addressed across the series. The work establishes a foundation for future Rust tooling while maintaining safety through careful FFI design, with the higher-level `libgit` crate providing the intended developer experience.