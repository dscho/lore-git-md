This is a technical clarification in the ongoing reftable decoupling series, specifically addressing questions about patch 1/19's conversion from Git's `read_in_full()` to direct POSIX I/O operations. Patrick Steinhardt responds to Justin Tobler's concerns about two aspects of the change:

First, regarding the `off_t` to `size_t` conversion, Patrick confirms it's safe because negative values are already checked and rejected earlier in the function. Second, he explains that the simpler EAGAIN/EINTR handling is sufficient because reftable doesn't use non-blocking I/O (O_NONBLOCK), so the more complex `xread()` behavior isn't needed.

The exchange demonstrates the careful attention being paid to maintaining correct behavior while removing Git-specific dependencies. Patrick plans to update the commit message to document these design decisions. This is a typical mid-series technical discussion where implementers validate edge cases in the refactoring approach.