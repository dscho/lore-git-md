This is a technical clarification in the ongoing discussion about integrating alternative grep tools with Git.

Matěj Cepl responds to Hongyi Zhao's suggestion of using a PATH-based workaround (linking `ug` as `git-ug`) to replace Git's grep functionality. Cepl explains this approach wouldn't work because it would require fundamentally rewriting Git's internal `git-grep` implementation to use an external program instead. The core issue remains that Git's grep is compiled into the binary rather than being dispatched as an external command.

The response suggests the architectural challenge is more substantial than a simple PATH trick could solve, though Cepl notes they're unsure if any existing projects have already implemented such a rewrite. This maintains the thread's focus on the technical feasibility questions raised earlier, without proposing new solutions.

The exchange highlights the discussion remains at an impasse regarding how to practically integrate alternative grep implementations while preserving Git's current behavior and option compatibility. No new technical approaches are introduced in this response, which primarily serves to correct a misunderstanding in the previous proposal.