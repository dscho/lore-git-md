This is a technical clarification in the ongoing discussion about integrating alternative grep tools with Git.

Matěj Cepl responds to Hongyi Zhao's suggestion of using a PATH-based workaround (linking `ug` as `git-ug`) to replace Git's grep functionality. Cepl clarifies that this approach would not work because Git's grep is an internal implementation in grep.c rather than an external command. The email notes that proper integration would require rewriting git-grep to use an external grep-like program, while acknowledging uncertainty about whether such a project already exists.

The response reinforces the key architectural limitation identified earlier in the thread: Git's grep functionality is compiled into the binary rather than being dispatched via external command. This maintains the thread's focus on the core technical challenge while gently correcting the proposed workaround. The email doesn't advance new solutions but keeps the discussion grounded in the actual implementation constraints.

The exchange highlights the gap between user expectations (wanting to plug in alternative tools) and Git's current architecture (with tightly integrated grep functionality). The thread remains at an impasse until either a concrete technical proposal emerges for modifying Git's internals or maintainers weigh in on whether such modifications would be considered.