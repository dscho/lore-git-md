This is a technical clarification in the ongoing discussion about integrating alternative grep tools with Git.

Matěj Cepl responds to Hongyi Zhao's initial proposal by correcting a misunderstanding about how `git grep` works. He points out that Git's grep functionality is implemented internally rather than shelling out to an external `grep` binary, which explains why attempts to use `ug` (from ugrep) directly via `git-ug` fail - there is no such Git subcommand. This clarification is important for the thread as it establishes that any integration would require more than just command substitution, likely needing changes to Git's internal grep implementation or a new subcommand dispatch mechanism.

The response doesn't address the broader feasibility questions raised in the original proposal, but provides key technical context that any solution would need to account for Git's current architecture. This moves the discussion forward by narrowing the technical scope - now participants know they're discussing modifying an internal implementation rather than just command-line tool substitution.

Key technical details reinforced: Git's grep is not a wrapper around system grep, and subcommands like `git-ug` don't automatically work just by having `ug` in PATH. The thread now needs to consider whether to propose (1) modifying Git's grep internals to support pluggable backends, (2) creating new subcommands with their own implementations, or (3) abandoning the approach if the changes would be too invasive.