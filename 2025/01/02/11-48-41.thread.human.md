This thread explores the technical feasibility of integrating alternative grep tools like ugrep as replacements for Git's built-in `git grep`. The discussion began with Hongyi Zhao proposing the idea, but Matěj Cepl quickly clarified that Git's grep is compiled into the binary rather than dispatched externally, ruling out simple PATH-based solutions like symlinking `ug` as `git-ug`.

Jonathan Nieder has now joined the conversation, shifting focus to understanding the concrete use cases that motivate wanting to replace `git grep`. He identifies three potential search contexts (historical commits, working copy, and index) and suggests that some efficiency needs might already be addressed by piping `git grep --name-only` to external tools. However, without clearer requirements from the original proposer, it's impossible to determine whether this requires Git changes, external tool modifications, or can be solved with existing features.

The technical constraints remain firm - Git's grep implementation is statically linked, so any integration would require architectural changes to support pluggable backends or new subcommand dispatch. The thread now awaits either more specific use case details from Hongyi Zhao or input from grep.c maintainers about whether such architectural changes would be considered worthwhile if compelling needs are demonstrated. So far, no maintainers have weighed in on the feasibility of modifying Git's grep architecture.