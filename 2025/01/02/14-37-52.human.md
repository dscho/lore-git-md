This is a follow-up in the ongoing discussion about meson test execution portability issues in t/Makefile. Junio Hamano points to a specific patch series that may be related to the problem where the `check-meson` target fails on shells lacking process substitution syntax (like dash). The issue centers around a non-portable `diff -u <(...)` command that requires bash, which multiple contributors have now independently confirmed. While a workaround exists (explicitly setting SHELL=/bin/bash), the thread is moving toward determining whether this should be documented as a bash requirement or if the test comparison should be rewritten to be more portable. Hamano references a specific patch series that might be the source of this issue, suggesting the discussion should continue there. This appears to be part of the broader meson build system modernization effort where such portability concerns need to be addressed.