Feature discussion follow-up about alternative grep tool integration.

Hongyi Zhao follows up on the technical clarification about Git's internal grep implementation, seeking confirmation about a potential workaround. The email suggests linking ugrep's `ug` as `git-ug` in PATH as a possible solution, though this appears to misunderstand Matěj Cepl's earlier point about Git's grep being an internal implementation rather than an external command dispatch.

This exchange highlights the architectural challenge - Git's grep functionality is implemented in grep.c rather than shelling out, making simple command substitution approaches ineffective. The thread remains at the technical feasibility exploration stage, with no clear path forward yet on how to integrate alternative grep tools while maintaining Git's current grep behavior and option compatibility. The discussion would benefit from input from Git maintainers about whether they would consider supporting pluggable grep backends or new subcommands as architectural directions.