Refactoring patch in a series centralizing refspec handling logic. This second patch moves three query-related functions (`query_refspecs`, `query_refspecs_multiple`, and `query_matches_negative_refspec`) from remote.c to refspec.c, continuing the effort to consolidate refspec operations in one place. The changes are purely organizational - 123 lines added to refspec.c and 122 removed from remote.c, with corresponding header adjustments. 

The moved functions handle core refspec query operations: checking if a ref matches negative refspecs (which exclude refs from operations), looking up single or multiple refspec matches, and handling both pattern and exact matches. The implementation remains unchanged, just relocated to be with other refspec logic. The patch follows the same mechanical code movement approach as the first in the series, keeping the focus on pure refspec operations while leaving remote-specific logic in remote.c.

This continues the architectural cleanup where refspec.c becomes the canonical home for refspec matching and query logic, while remote.c retains the remote repository interaction code. The changes appear straightforward with no behavioral modifications, just the movement of well-contained functions between files. The patch maintains all existing functionality while improving code organization.