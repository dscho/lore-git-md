Build system refinement for Rust bindings. This is part of the ongoing series adding Rust FFI bindings to Git's C library, specifically addressing build system integration points raised in review. Josh Steadmon responds to Junio Hamano's feedback about portability concerns in the Makefile changes needed for the Rust support.

The discussion focuses on several technical details: ensuring the build works without GNU binutils (though that's currently assumed), making the public symbol export compilation optional when Rust support is disabled, and fixing incorrect use of `-r` flag with `RM` for non-directory files. Steadmon notes these will be addressed in v7 revision by moving the build control variable earlier in the series.

The exchange shows the series is in final polishing stages, with the core approach already approved. The changes discussed here are build-system refinements rather than architectural concerns, suggesting the Rust binding work is nearing readiness for integration. The thread context indicates this is one of the last pieces before the series can be merged.