This is a CI/build system patch that simplifies Git's test infrastructure by merging two similar CI jobs as part of the broader reference deprecation effort. The patch eliminates the redundant "linux-gcc-default" job by folding its functionality into the existing "linux-gcc" job, since the differences between them (explicit GCC version, Python version handling, branch naming, and Ubuntu version) were either already covered by other jobs or no longer relevant (like Python 2 support being obsolete).

The changes touch CI configuration files across GitHub Actions (.github/workflows/main.yml) and GitLab CI (.gitlab-ci.yml), plus the shared ci/lib.sh script. The removal is straightforward - deleting job definitions and related conditional logic. This cleanup aligns with the thread's focus on infrastructure preparation for Git 3.0's breaking changes, though it's a general maintenance improvement rather than directly tied to the reference deprecation feature.

The patch appears well-justified and low-risk, removing test redundancy without reducing coverage. It follows the series' pattern of careful infrastructure preparation, though it stands alone in focusing on CI rather than the core deprecation mechanisms. The changes are mechanical and unlikely to be controversial, serving as housekeeping to simplify the test matrix before more substantive changes land.