Build system modernization patch in Patrick Steinhardt's Meson conversion series (v3 02/11). This change modifies the `GIT-VERSION-GEN` script to support Meson's build requirements by making two key adjustments: allowing version string generation without file I/O when needed, while maintaining backward compatibility with the Makefile-based build.

The patch introduces a new `--format` option that lets callers specify version string formatting directly rather than reading from a template file, and makes output to stdout optional. This is particularly useful for Meson integration where the build system needs to capture the version string directly rather than writing to a file. The changes preserve all existing substitution patterns (`@GIT_VERSION@`, `@GIT_MAJOR_VERSION@`, etc.) while adding flexibility.

The implementation carefully maintains the existing atomic-write behavior when writing to files (using temporary files and compare-and-swap) while cleanly handling the new stdout case. This appears to be a well-considered change that addresses a specific integration need without disrupting the current build system's operation. The patch follows the series' pattern of making targeted, backward-compatible adjustments to support Meson while keeping the existing build working.