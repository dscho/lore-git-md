Bugfix for reflog corruption in symref updates, now with maintainer feedback. This is the final discussion before merging the fix for a regression in Git 2.48.1 where symbolic reference updates (like HEAD) were creating corrupted reflog entries due to missing `old_oid` values.

Junio Hamano chimes in on Patrick Steinhardt's review, agreeing that the commit message could be clearer about the files-backend specificity of the bug and endorsing the suggestion to use `git symbolic-ref HEAD` in tests rather than hardcoding "main". He finds the current commit message structure confusing, with the fix being described twice in quick succession.

The patch has already been through several iterations with input from the original reporter (Nika Layzell), Jeff King (who provided test improvements), and now Patrick Steinhardt. All parties seem satisfied with the technical solution, which removes an overzealous optimization that broke symref reflogs while maintaining correct behavior for other cases.

This appears ready for merging, with only minor commit message polishing needed. The discussion highlights how subtle reflog handling can be, especially when optimizations interact with symbolic reference updates. The fix will restore proper reflog behavior for a fundamental Git feature.