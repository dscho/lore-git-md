Bugfix for reflog corruption in symref updates, continuing the thread about the regression introduced in Git 2.48.1. Patrick Steinhardt reviews Karthik Nayak's proposed fix for the files backend's reflog handling, which removes an early return in `lock_ref_for_update()` that was incorrectly skipping old_oid population for symref reflogs.

The review confirms the core fix is correct - removing the `REF_LOG_ONLY` early return allows proper old_oid recording for symref reflogs while maintaining other optimizations. Patrick notes the bug is specific to the files backend and suggests two minor improvements: mentioning the files backend specificity in the commit message, and using `git symbolic-ref HEAD` instead of overriding the test branch name in the test case.

The discussion shows the fix is technically sound, with Patrick verifying that `check_old_oid()` will still skip appropriately due to `REF_HAVE_OLD` being unset. This builds on the thread's earlier confirmation from both the original bug reporter and Jeff King that the fix works as expected. The review moves the patch closer to being ready for inclusion, with only minor documentation and test case polish needed.