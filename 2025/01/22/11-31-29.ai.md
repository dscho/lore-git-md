Build system patch introducing infrastructure for upcoming breaking changes in Git 3.0. This first patch in the v4 series establishes the `WITH_BREAKING_CHANGES` compile-time flag across all supported build systems (Makefile, Meson, CMake) and adds corresponding test prerequisites. The change implements a previously agreed requirement that breaking changes must be gated behind a build flag, though it doesn't yet activate the flag for existing deprecations.

The patch touches build configuration files across multiple systems, adding consistent handling of the new flag. Key modifications include Makefile additions for BASIC_CFLAGS, Meson build configuration with a new option, CMake string replacement, and a new test prerequisite `WITHOUT_BREAKING_CHANGES`. The changes maintain backward compatibility by default (flag disabled) while preparing the infrastructure for future deprecations like the legacy "branches/" and "remotes/" reference paths mentioned in the thread context.

This foundational change enables subsequent patches in the series to implement actual deprecation warnings and removals behind this flag. The implementation follows Git's standard build system patterns and includes appropriate test infrastructure, suggesting it will serve as a template for future breaking changes. The patch appears technically sound with comprehensive cross-build-system support, though its real utility will become apparent when later patches wire up specific deprecations.