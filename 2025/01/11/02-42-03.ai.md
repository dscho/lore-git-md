This is a technical discussion in the ongoing hash algorithm refactoring series, specifically addressing pointer safety concerns in patch 3. Jeff King responds to Taylor Blau's defense of the current approach to separating safe/unsafe hash operations via distinct `git_hash_algo` instances.

Key points in Jeff's response:
- Clarifies that his earlier `git_hash_algo_fns` struct proposal is actually complementary to the series' goals, not contradictory
- Explains the struct would still work with the existing `unsafe_hash_algo()` mechanism but provide stronger type safety
- Notes the hashfile would need to store both the algorithm pointer (for metadata like `raw_size`) and the function struct
- Acknowledges some remaining potential for confusion but suggests localized conventions could mitigate it
- Proposes limiting the changes to hashfile.c as a pragmatic middle ground

The discussion reveals two competing approaches to type safety:
1. Taylor's current implementation using separate algorithm instances
2. Jeff's alternative using nested function structs

Technical considerations include:
- Pointer comparison issues in `hash_algo_by_ptr()`
- Access to algorithm metadata vs. function pointers
- Code churn vs. safety tradeoffs
- The specific needs of `hashfile.c` as the primary unsafe operation user

This email advances the thread by:
- Resolving a misunderstanding about the relationship between the two approaches
- Identifying remaining technical constraints (need for dual storage in hashfile)
- Narrowing the scope of potential changes to balance safety and practicality

The core architectural debate continues, but with clearer alignment on how the proposals intersect with the series' goals.