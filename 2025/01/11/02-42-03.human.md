This is a technical discussion about the ongoing hash algorithm refactoring series, specifically addressing pointer safety concerns in patch 3.

Jeff King responds to Taylor Blau's defense of the current approach to separating safe and unsafe hash operations. While Taylor argues that having separate `git_hash_algo` instances prevents accidental mixing of safe/unsafe operations, Jeff suggests an alternative approach using a `git_hash_algo_fns` struct that would still maintain the series' goals while avoiding pointer confusion issues.

The key technical point is that Jeff's proposed approach would require storing both the algorithm pointer and the function struct in `hashfile`, since some operations (like accessing `raw_size`) still need the outer `algop`. He acknowledges this adds some complexity but suggests it could be managed by establishing conventions about when to use which access pattern.

This exchange represents ongoing refinement of the series' implementation details rather than fundamental disagreement about the approach. Both parties are working toward the same goal of preventing unsafe operation mixing, but differ on the best mechanism to achieve it. The discussion remains technical and collaborative, with Jeff offering concrete suggestions while acknowledging tradeoffs.