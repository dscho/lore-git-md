Test portability fix addressing printf formatting in Git's blame test suite, modifying t8002-blame.sh and t8003-blame-corner-cases.sh to replace non-portable `printf "%0.s"` sequences with equivalent `printf "%Ns"` syntax (N being 11 or hexsz) for padding spaces. The changes specifically target test cases verifying blame output formatting with blank boundary commits, testing `--abbrev` and `-b` flag behavior from commit e7fb2ca945.

Patrick Steinhardt initially reviewed Jan Palus's patch, confirming the technical approach matches portable usage in t5300 tests. Junio Hamano questioned whether the issue was general portability or specifically an mksh limitation, prompting discussion about commit message wording. Jan Palus's follow-up provides key technical clarification: mksh lacks a builtin printf and uses coreutils' implementation, which explicitly rejects "0" as a flag for string conversions. This confirms the issue stems from a deliberate coreutils restriction rather than mksh being "lacking". The distinction between `%0.s` (ambiguous flag/width) versus `%.0s` (clear precision) further justifies the `%${N}s` solution.

The technical solution (using %Ns instead of %0.s) has consensus and remains unchanged. Changes affect two test files while preserving test expectations, specifically helping mksh compatibility. Jan Palus acknowledges Junio's request for commit message refinement and indicates a v2 patch is forthcoming. The only remaining work is improving the commit message to accurately reflect that the portability issue stems from coreutils' printf implementation rather than being mksh-specific. No technical objections or code changes needed beyond this final documentation polish.

This is a straightforward portability improvement with minimal impact beyond test infrastructure, maintaining identical verification of blame's formatting behavior while fixing compatibility with shells that rely on coreutils' printf. The patch is in its final stage awaiting only commit message clarification before merging.