This is part 9 of Jeff King's combine-diff refactoring series, focusing on code simplification. The patch inlines the `path_appendnew()` helper function into its single caller `emit_path()`, replacing it with direct calls to the more fundamental `combine_diff_path_new()` constructor that was introduced earlier in the series.

The change eliminates 27 lines of redundant code while maintaining identical behavior. Junio Hamano approves the change with a "Very nice, indeed" comment, continuing his positive review of the series. The modification follows the series' pattern of removing intermediate helper layers and consolidating memory management logic into the core constructor functions.

Key technical points:
- Removes `path_appendnew()` wrapper function
- Replaces with direct `combine_diff_path_new()` call plus manual `next` pointer assignment
- Preserves all existing functionality including path copying and parent array initialization
- Maintains the series' focus on simplifying the diff path construction flow

This change builds on previous patches that established `combine_diff_path_new()` as the canonical way to create path objects, and represents another step in eliminating redundant helper functions from the combine-diff implementation. The series continues its methodical approach to refactoring this complex subsystem while maintaining behavioral correctness.