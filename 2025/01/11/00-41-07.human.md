This is a refactoring patch in Jeff King's ongoing combine-diff series, specifically addressing the `path_appendnew()` helper function. The change inlines this now-simplified function into its single caller `emit_path()`, replacing it with direct calls to the more fundamental `combine_diff_path_new()` constructor that was introduced earlier in the series.

Junio Hamano reviews the change with clear approval ("Very nice, indeed"), continuing his pattern of endorsing the series' systematic improvements. The patch eliminates 27 lines of redundant code while maintaining the same functionality, demonstrating the series' progression toward simpler, more direct code paths in the combine-diff machinery.

The change is part of a broader effort to clean up memory management and initialization complexity in the diff backend, with each patch building on previous simplifications. This particular step removes what has become an unnecessary abstraction layer now that the core allocation logic has been consolidated into `combine_diff_path_new()`.