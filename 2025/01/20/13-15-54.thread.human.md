Here's a human-readable summary of the thread so far:

Bob Ruiken reported an unexpected behavior where `git diff --name-only --ignore-space-at-eol` still shows files that only contain end-of-line whitespace changes, contrary to what one might expect when using the whitespace-ignoring flag. The issue was reproducible on Git 2.39.5 on macOS (arm64).

Jialuo has since confirmed the bug exists in Git's master branch as well and provided a minimal test case. The technical analysis points to `diff.c::diff_flush` as the root cause - when outputting just filenames (`DIFF_FORMAT_NAME`), the function exits early without applying the whitespace filtering rules. This happens because the name-only output works at the tree comparison level rather than examining file contents directly.

Two potential solutions have been suggested: either documenting this limitation more clearly, or making the options mutually exclusive. The thread has identified a genuine inconsistency in how Git's diff filtering options interact, where users might reasonably expect whitespace rules to apply uniformly across all output formats. No code fix has been proposed yet, as the responder noted unfamiliarity with the diff internals. The discussion remains open for further analysis or solutions from diff subsystem experts.