This is a housekeeping patch cleaning up obsolete file handling in the Git documentation system. It's the final patch in a 5-part series (though no cover letter context is provided). The change removes now-unneeded `.txt` file patterns from `.gitattributes` and `.gitignore` since the project has fully transitioned to using `.adoc` extensions for AsciiDoc files. The patch is straightforward - it deletes lines that previously handled `.txt` versions of documentation files like `howto-index`, `cmds-*`, `mergetools-*`, and `SubmittingPatches`. 

The changes are mechanical and appear correct - they simply remove patterns that are no longer needed after the AsciiDoc conversion. The patch maintains the existing handling of `.adoc` files while cleaning up the transitional `.txt` support. This appears to be a final cleanup step in a longer migration effort to modernize Git's documentation format.