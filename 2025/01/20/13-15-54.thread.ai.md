Updated AI thread summary:

Git diff --name-only --ignore-space-at-eol behavior analysis and bug confirmation. Thread originated by Bob Ruiken reporting unexpected interaction where files with only end-of-line whitespace changes still appear in `--name-only` output despite `--ignore-space-at-eol` flag. Jialuo confirms the issue exists in Git's master branch and provides minimal reproduction case. 

Technical root cause identified in `diff.c::diff_flush` where `DIFF_FORMAT_NAME` bit causes early exit before whitespace filtering is applied. Current implementation processes name-only output at tree comparison level rather than examining file contents, making it ignore whitespace options. 

Open questions include whether to: 1) document this as expected behavior (current limitation), 2) make the options mutually exclusive, or 3) modify the code to properly filter names based on whitespace rules. No patch proposed yet - requires deeper understanding of diff internals. Behavior affects users expecting consistent whitespace filtering across all diff output formats. Platform/version confirmation still needed beyond macOS (arm64) and master branch.