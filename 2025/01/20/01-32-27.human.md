# Rebase conflict explanation

Brian m. carlson provides a technical explanation of why Al Grant is seeing unexpected merge conflicts during rebase, specifically addressing why the conflicting lines don't appear in either branch as expected. The key insight is that during rebase, conflicts show differences between the current commit being rebased and the intermediate state that includes previously rebased commits, not just the original branches.

The response offers two concrete suggestions: First, setting `merge.conflictStyle` to `diff3` to see the merge base, which helps understand conflict origins. Second, it explains the rebase process through a commit graph example, showing how intermediate states during rebase can produce conflicts that don't directly match either original branch.

The conflict in question involves numpy signal processing parameters (window sizes 189 vs 10) in sample_processor.py, which Brian notes appears logically equivalent and suggests resolving in favor of the more readable version (with better whitespace). The explanation is technically precise while remaining accessible, focusing on the mechanics of rebase rather than the specific code change.