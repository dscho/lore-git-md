This is a build system patch adjusting Perl version requirements in the Meson build configuration. It's part of a 2-patch series (position 1/2), though the cover letter isn't provided here.

The patch changes the minimum required Perl version from 5.8.1 to 5.26.0 in meson.build. The commit message references commit 702d8c1 (a previous change that had lowered requirements to 5.8.1) as context, though it doesn't explicitly state why 5.26.0 is now needed. The change affects how Perl is detected during configuration - the build will now fail if Perl is required but a version below 5.26.0 is found.

The modification is straightforward, touching only the version check line in meson.build. The patch appears technically correct but lacks detailed motivation - we don't see what features require 5.26.0 or whether this aligns with other parts of Git's build system. The reference to 702d8c1 suggests this might be reverting or adjusting a previous decision about Perl version support.