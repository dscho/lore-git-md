This is a documentation housekeeping patch, the final part of a 5-patch series (though we haven't seen the cover letter). Brian Carlson removes now-obsolete references to `.txt` extensions in `.gitattributes` and `.gitignore` files, since the project has completed its migration from `.txt` to `.adoc` for AsciiDoc documentation files.

The changes are straightforward: deleting lines that previously handled `.txt` versions of documentation files like `howto-index`, `cmds-*`, `mergetools-*`, and `SubmittingPatches`. The patch appears to be a clean-up of vestigial configuration after an earlier transition to AsciiDoc formatting.

This is a small but tidy maintenance change that follows through on a completed transition. The patch is simple and focused, with no visible loose ends or complications. Readers who aren't working with the documentation build system can safely skip it, while those maintaining documentation infrastructure will appreciate the cleanup.