Here's a human-readable thread summary:

This thread documents the final cleanup step in Git's multi-year effort to convert all documentation from plain text to AsciiDoc format. The series removes now-obsolete handling of `.txt` files from Git's documentation system, as the project has fully transitioned to using `.adoc` extensions. The changes are mechanical but important - they clean up patterns in `.gitattributes` and `.gitignore` that previously handled transitional `.txt` versions of files like `howto-index` and `SubmittingPatches`. 

The patch appears straightforward and correct, representing the completion of a long-running documentation modernization effort. Since this is the final patch in the series and the changes are uncontroversial (merely removing patterns that are no longer needed), it will likely be merged without further discussion. The cleanup maintains all existing handling of `.adoc` files while removing vestigial support for the old text format.