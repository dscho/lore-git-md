This is a test infrastructure patch, part of an 8-part series (v10) implementing client-side remote object-info functionality. The patch refactors utility functions from `t1006-cat-file.sh` into a new shared library `lib-cat-file.sh` to improve code reuse and readability.

The change moves three string manipulation helpers (`echo_without_newline`, `echo_without_newline_nul`, and `strlen`) into the new library file. These utilities are used for testing `git cat-file` behavior, particularly around newline handling and string length calculations. The patch is straightforward - it extracts existing code without modifying behavior, just reorganizing it for better maintainability.

This appears to be a clean, well-motivated refactoring that follows the series' pattern of careful attention to code organization. The change will make these utilities available to other tests that may need similar functionality, particularly as the series adds new cat-file related tests. The patch maintains all existing test coverage while improving the code structure.