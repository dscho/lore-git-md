CI/build system patch switching the linux-musl job to use Meson. This is part of the larger zlib-ng optimization series, though it stands as a general CI improvement on its own. The change simplifies CI infrastructure by removing manual build option specifications and helps verify Meson's automatic detection of those options. It also paves the way for easier zlib-ng backend testing in future CI runs.

The patch handles platform compatibility by explicitly wiring up the `GIT_TEST_UTF8_LOCALE` variable through a new Meson build option, since Meson can't auto-detect this test requirement. Changes span CI configuration files (.github/workflows, .gitlab-ci), dependency installation, and Meson build files, with the linux-musl job renamed to linux-musl-meson for clarity.

The implementation looks clean and focused, with straightforward changes that maintain existing functionality while moving to the newer build system. As part of a well-tested series that's already addressed previous concerns, this appears ready for inclusion.