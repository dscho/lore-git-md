This is a refactoring patch in Patrick Steinhardt's ongoing Meson build system conversion series (v2, patch 2/11). It modifies the `GIT-VERSION-GEN` script to be more flexible for Meson integration.

The change allows `GIT-VERSION-GEN` to operate without requiring both input and output files, which is needed for Meson's version detection. The script now accepts a `--format` option as an alternative to an input file, and will write to stdout when no output file is specified. This maintains all existing functionality while adding the flexibility Meson requires.

The patch is a straightforward technical adjustment that follows the pattern of previous changes in this series - making existing Git infrastructure more modular to support the Meson build system. The implementation looks clean, with proper error checking preserved and the diff showing careful handling of both the new and old code paths. As with other patches in this series, it demonstrates attention to backwards compatibility while enabling the new build system.