This is a performance optimization patch series introducing zlib-ng support as an alternative zlib backend. The v2 series builds on previous work to refactor Git's zlib integration, now adding Makefile support and CI testing to complement the initial meson implementation.

The key changes in this version address the build system parity concern raised in previous discussion - Patrick Steinhardt has now added Makefile support for zlib-ng alongside the existing meson integration. The series also adds CI testing by adapting the linux-musl job to use meson and zlib-ng. These changes complete the implementation while maintaining all previously established compatibility guarantees.

The technical approach remains focused on providing a clean abstraction layer that allows runtime switching between standard zlib and zlib-ng (which offers optimized implementations using SSE2/AVX2). The implementation is distribution-friendly, working alongside rather than replacing system zlib, and benchmarks show ~25% speedup in operations like `git cat-file --batch-all-objects`.

The series now appears complete with both build systems supported and CI verification in place. The changes span build configuration, compatibility headers, and test infrastructure while keeping core code impact minimal. With the build system concern resolved, this positions the series well for maintainer consideration.