Feature request to extend Git's `-` shorthand (equivalent to `@{-1}`) to `git diff`, mirroring existing behavior in `git merge` and `git checkout`. Proposal suggests consistent shorthand support across commands but lacks implementation specifics. Would require modifying diff's argument parsing, documentation updates, and test coverage. Open questions include whether this shorthand should be universally applied to all commit-accepting commands. No reviewers or alternative approaches discussed.