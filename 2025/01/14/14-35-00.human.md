Build system fix for technical documentation generation in meson builds. This is the third iteration of a one-line change to properly declare dependencies for HTML documentation targets in `Documentation/technical/meson.build`. The patch ensures the build system knows these targets require `asciidoc.conf` to be available, fixing parallel build failures.

The change follows established patterns used elsewhere in the documentation build system. Reviewer Patrick Steinhardt approved the technical content in v2 but suggested a more accurate commit message title since Perl isn't actually involved. Author Sam James acknowledges this and will send a v3 with the updated message.

This appears to be the final tweak needed for this straightforward build system improvement. The thread shows good reviewer engagement and a responsive author, with the only remaining action being the commit message wording adjustment. The change is minimal but important for reliable parallel builds of Git's technical articles.