This is a refactoring patch in the zlib-ng optimization series, moving a compatibility shim for older zlib into a central location. The patch relocates the `deflateBound()` compatibility definition from `git-zlib.c` to `compat/zlib-compat.h`, where it logically belongs with other zlib compatibility code. This is part of the broader effort to abstract zlib usage in preparation for supporting zlib-ng as an alternative backend.

The change is straightforward - it moves the existing compatibility macro that handles cases where zlib is older than 1.2.0 (which didn't have `deflateBound()`). The patch maintains identical functionality while improving code organization, making it easier to modify the shim later when zlib-ng support is added.

As part of a well-structured series that's already addressed all major concerns, this patch appears to be a clean, logical step in the refactoring process. The commit message clearly explains the change and its purpose within the larger context of the zlib-ng integration work.