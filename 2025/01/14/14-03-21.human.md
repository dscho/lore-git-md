This is a build system correction following technical review. Sam James acknowledges Patrick Steinhardt's observation that their earlier meson build fix for documentation generation included an unnecessary dependency. The original patch added `documentation_deps` to both API index generation and HTML article targets, but Patrick correctly noted the API index script doesn't actually use `asciidoc.conf`. Sam confirms this upon rechecking and plans to resubmit the patch with the dependency removed from the API index target while keeping it for the HTML articles where it's genuinely needed. The exchange shows good technical diligence in refining build dependencies to only what's strictly necessary.