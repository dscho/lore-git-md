This thread discusses a proposal to extend Git's `-` shorthand (equivalent to `@{-1}`) to `git diff`, making it consistent with existing behavior in `git merge` and `git checkout`. The initial request suggested this change for better command consistency but did not provide implementation details or address potential challenges in `git diff`'s argument parsing.

A follow-up email added historical context, noting that a similar proposal for `git branch` was rejected in 2022, though the specific reasoning wasn't restated. This suggests the change may face resistance based on precedent, though it remains unclear whether `git diff` presents a different case to justify reconsideration. So far, the discussion lacks technical analysis of the implementation or broader consensus on whether the shorthand should be expanded to other commit-accepting commands. The thread remains open, with no clear direction yet on whether this proposal will move forward.