Build system fix for meson configuration, now acked and ready.

This is the final ack for the v3 patch fixing a parallel build race condition in Git's technical documentation generation. Sam James confirms the fix with a cleaned-up commit message, addressing Patrick Steinhardt's earlier review that caught an incorrect dependency in v2. The patch remains narrowly focused on adding the correct `documentation_deps` declaration in Documentation/technical/meson.build to ensure asciidoc.conf is available before building technical articles.

Key technical details remain unchanged from v3: the single-line addition of `depends: documentation_deps` in Documentation/technical/meson.build, with the dependency chain now correctly pointing to asciidoc.conf. The fix specifically targets technical documentation while leaving other properly-declared documentation targets unaffected).

With this ack from the build system maintainer and no further issues raised, the thread appears concluded with consensus that this minimal change properly resolves the parallel build issue. The patch is now in its final form and ready for inclusion in the meson build system.