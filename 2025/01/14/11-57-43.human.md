This is a cleanup patch in the zlib-ng optimization series, removing an unnecessary compatibility define. The patch drops the `z_const` macro from git-compat-util.h, which was originally added as part of zlib compatibility work but appears to serve no real purpose. The author notes that the macro doesn't actually change whether parameters are constant, and its original justification (carried over from removed compatibility code) was never documented.

The change is straightforward - just removing one line that defined `z_const` before including zlib.h. This is part of the series' broader effort to modernize and streamline Git's zlib integration while preparing for zlib-ng support. The patch maintains all existing functionality while removing what appears to be dead code.

As with the rest of this well-structured series, the commit message clearly explains the change and its rationale, though in this case the rationale is that there doesn't seem to be any good reason to keep the code. The change is low-risk and aligns with the series' goal of cleaning up zlib compatibility code.