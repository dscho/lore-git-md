Performance optimization completing the zlib-ng integration series with CI testing. This final patch adds test coverage for the new zlib-ng backend by modifying the Alpine Linux CI job to install zlib-ng-dev, which meson will automatically detect and use.

The patch explains the practical constraints around CI testing - Alpine was chosen as the only distribution that both packages zlib-ng and runs Git's test suite. The change is minimal, just adding the package to the dependency list, but provides important validation that the zlib-ng integration works in practice.

This completes the comprehensive zlib-ng support series which has shown ~25% speedups in object reading operations. With build system support, compatibility layers, and now CI verification all in place, the series appears ready for final review. The choice to test via Alpine's package rather than custom builds keeps the CI configuration simple while still exercising the new code path.