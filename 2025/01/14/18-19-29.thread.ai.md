[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags for `color` (default-allowed), `cursor`, and `erase` (default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates SGR (color), CUU/CUD (cursor), and ED/EL (erase) sequences against policy.

Technical implementation includes URL-specific configuration via `sideband_allow_control_characters_config()` integrated with Git's HTTP transport. Current implementation details:
- Bitmask flags: `ALLOW_ANSI_COLOR_SEQUENCES` (1<<0), `ALLOW_ANSI_CURSOR_MOVEMENTS` (1<<1), `ALLOW_ANSI_ERASE` (1<<2)
- `ALLOW_CONTROL_SEQUENCES_UNSET` (-1) state for configuration
- Backward-compatible config parsing with comma-separated values (`color,cursor,erase`)
- Phillip Wood's whitelist preserved (BEL, DEL handling)
- Comprehensive test coverage for sequence handling

Policy debate has intensified with Patrick Steinhardt and Johannes Schindelin advocating for restrictive defaults, now with Ondrej Pohorelsky's report that Fedora/CentOS/RHEL have successfully deployed restrictive defaults for six months without user issues. Junio C Hamano's latest response acknowledges the filtering mechanism's value but firmly rejects making it opt-out by default, framing this as a responsibility to users rather than technical feasibility. The philosophical divide is now clearly articulated:
- Security-first (Schindelin/Steinhardt/Pohorelsky): Restrictive defaults justified by terminal vulnerabilities, attack surface reduction, and production deployment evidence
- Compatibility-first (Hamano/Jeff King): Working setups must not be broken regardless of architectural merits

Security model characteristics remain unchanged:
- Default behavior permissive (allowing all sequences) per Hamano/Peff consensus
- `sideband.allowControlCharacters` provides granular restrictions
- Windows maintains stricter defaults due to platform risks
- Core implementation stable (bitmask flags, sequence validation)

Open items now focus on documentation and policy:
- Need to document both security philosophy positions and downstream experience
- Final configuration naming (`sideband.allowControlCharacters` still likely)
- Transport-layer integration stable (`sideband` scope preferred)
- Security vs compatibility debate continues with new empirical data favoring restrictions but Hamano maintaining compatibility stance
- Windows remains special case with stricter historical defaults
- Native formatting support as alternative path now has stronger case but not currently prioritized

The thread has reached technical implementation stability but remains at philosophical impasse regarding defaults, with Hamano's position as maintainer likely to prevail on the opt-in/opt-out question while the security arguments gain credibility from production evidence. Windows-specific concerns and downstream maintainer participation continue to shape the discussion.