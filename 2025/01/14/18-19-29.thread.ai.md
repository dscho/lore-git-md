[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags (ALLOW_ANSI_COLOR_SEQUENCES default-allowed, ALLOW_ANSI_CURSOR_MOVEMENTS/ERASE default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates sequences against policy.

New patch implements per-URL configuration via `sideband_apply_url_config()`, addressing Hamano's request for server-trust scoping. Adds URL-specific overrides using `http.<url>.*` matching logic while maintaining:
- Existing bitmask flag behavior
- Windows' stricter defaults
- Backward compatibility for comma-separated values
- Comprehensive test coverage

Security model debate continues with Schindelin presenting production evidence (Git for Windows 1+ year, Red Hat 6+ months) showing restrictive defaults work, while Hamano maintains opt-in compatibility stance. Current compromise:
- Permissive defaults preserved (Hamano/Peff position)
- Granular URL-specific restrictions available
- Windows remains special case
- Core sanitization stable

Open items:
- Documentation needed for both security philosophies
- Transport-layer integration complete
- Windows default divergence
- Native formatting alternative not prioritized

Implementation now technically complete with URL scoping satisfying Hamano's architectural concerns. Final resolution expected to document both positions while maintaining backward-compatible defaults, despite Schindelin's strengthened security case. Policy disagreement narrowed to default behavior with implementation consensus achieved.