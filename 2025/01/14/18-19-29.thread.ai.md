[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags (ALLOW_ANSI_COLOR_SEQUENCES default-allowed, ALLOW_ANSI_CURSOR_MOVEMENTS/ERASE default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates sequences against policy.

The series has evolved through multiple versions to introduce three configuration states for `sideband.allowControlCharacters` (v2 3/4): `default`/`color` (permits colors, line feeds and tabs as new default), `false` (blocks all control characters except basic formatting), and `true` (original allow-all behavior). The final patch adds granular bitmask flags (ALLOW_ANSI_CURSOR_MOVEMENTS, ALLOW_ANSI_ERASE, ALLOW_ALL_CONTROL_CHARACTERS) with comma-separated value parsing, while maintaining the restrictive default (colors only). Implementation avoids allocations by parsing CSV directly rather than using `string_list_split()`.

Per-URL configuration via `sideband_apply_url_config()` (using `http.<url>.*` matching) addresses Hamano's server-trust scoping request while maintaining Windows' stricter defaults. Technical review between Schindelin and Patrick Steinhardt resolved implementation details (intentional `int` buffer size, pending style fix for conditional braces, enum alignment improvement). Steinhardt and Schindelin agreed on keeping "true"/"false" support in comma-separated lists for consistency.

Security model debate shows consensus on implementation approach with Schindelin presenting production evidence (Git for Windows 1+ year, Red Hat 6+ months) supporting restrictive defaults, while Hamano/Peff maintain opt-in compatibility stance. Current compromise position:
- New permissive defaults for colors only
- Granular URL-specific restrictions available
- Windows remains special case
- Core sanitization stable

Open items resolved:
- Documentation needed for both security philosophies
- Transport-layer integration complete
- Windows default divergence documented
- Native formatting alternative not prioritized

Implementation is technically complete with URL scoping satisfying architectural concerns. Only minor style improvements (enum alignment, conditional braces) remain before finalization. The thread shows consensus on the implementation approach with policy disagreement narrowed to default behavior documentation.