[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides configurable sanitization via `sideband.allowControlCharacters` with evolving granularity: original boolean values (`true`/`false`), then string values (`default`/`color`) allowing ANSI color sequences (SGR format ESC[..m) by default, extended to support comma-separated values ("cursor,erase") permitting cursor movement (ESC[nA) and erase sequences (ESC[K), and now per-URL configuration via `sideband_apply_url_config()`.

Core sanitization in `strbuf_add_sanitized()` and sequence handlers validates against policy without allocations, with dedicated functions for each sequence type (`handle_ansi_color_sequence()`, cursor/erase handlers). The implementation maintains backward compatibility while expanding flexibility through URL-scoped settings that mirror `http.<url>.*` patterns, addressing Junio Hamano's suggestion for server-specific policies. Test coverage in `t/t5409-colorize-remote-messages.sh` verifies all sequence types and URL-specific behavior.

Security model combines layered protections: default allowance for colors only, explicit opt-in for other sequences via comma-separated config, per-URL restrictions through `sideband_apply_url_config()`, and Windows-specific hardening. Production validation includes Git for Windows (1+ year) and Red Hat (6+ months). Technical review by Patrick Steinhardt and Phillip Wood confirmed implementation soundness (buffer handling, enum design), with brian m. carlson endorsing the security/usability balance. Documentation covers expanded configuration options and URL scoping while maintaining warnings about server trust boundaries.

The series is now feature-complete with v5 adding URL-specific configuration as the final piece, resolving all architectural concerns about server trust scoping through the combination of URL matching and strict default policies. Implementation has no outstanding review items, with thorough test coverage and documentation for all supported sequence types and configuration modes.