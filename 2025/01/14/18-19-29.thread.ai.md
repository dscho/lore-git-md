[Security] ANSI escape sequence injection in sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides configurable sanitization via `sideband.allowControlCharacters` with per-URL configuration through `sideband_apply_url_config()`. Core sanitization in `strbuf_add_sanitized()` validates without allocations, replacing control characters with caret notation (e.g., `^[` for escape sequences) to prevent terminal state corruption and input buffer injection attacks.

The mature v5 series (rebased to v2.53.0 in v4) had production validation in Git for Windows (1+ year) and Red Hat (6+ months). Latest patches introduce nuanced control with URL-scoped configuration via `sideband.<url>.allowControlCharacters` mirroring Git's `http.<url>.*` pattern, default behavior allowing ANSI color sequences (`\033[31m` etc) via `ALLOW_ANSI_COLOR_SEQUENCES`, and new bitmask flags for cursor movements (`ALLOW_ANSI_CURSOR_MOVEMENTS`), erase sequences (`ALLOW_ANSI_ERASE`), and full passthrough (`ALLOW_ALL_CONTROL_CHARACTERS`).

Patch 3/7 specifically modifies default behavior to selectively allow ANSI color sequences while blocking other control characters, addressing brian m. carlson's concern about legitimate color usage in hooks. It introduces `handle_ansi_color_sequence()` to validate SGR (Select Graphic Rendition) sequences, maintaining caret notation replacement for blocked characters. The `sideband.allowControlCharacters` configuration now supports "default" (allow colors only), "false" (block all non-whitespace controls), and "true" (allow all controls) modes.

Junio Hamano proposes merging patches 1-6 to 'next' for extended testing, implementing the allowlist approach (filtering all non-color ANSI sequences) while deferring the Git 3.0 default behavior change (patch 7/7). This maintains current permissive behavior (allowing all control characters) for compatibility while establishing the technical foundation for future hardening. The implementation uses conditional compilation (`WITH_BREAKING_CHANGES`) to manage the transition path, with secure-by-default behavior (only ANSI colors) planned for Git 3.0.

Test coverage in `t/t5409-colorize-remote-messages.sh` verifies behaviors via `TURN_ON_SANITIZING` prerequisite, handling both current and future default modes. Production validation in Git for Windows and Red Hat confirms URL-scoped configuration stability. Documentation warns about unwanted ANSI sequences in both conditional blocks while keeping version-specific defaults separate. The series represents a compromise between immediate security needs and backward compatibility, with the transitional approach approved for 'next' testing.

The initial patch (v1) introduced basic sanitization via `strbuf_add_sanitized()` with a "block all control characters" approach, tested in `t5409-colorize-remote-messages.sh`. This conservative baseline was later expanded in the v5 series to include granular allowlists while maintaining the core sanitization mechanism. The production validation in Git for Windows and Red Hat environments began with this initial implementation before evolving to support the configurable allowlist model. Junio's preference for a blocklist approach ("filter known-bad sequences") remains unaddressed, but the allowlist implementation proceeds as the most mature path after extensive discussion.