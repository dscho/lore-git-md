[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags for `color` (default-allowed), `cursor`, and `erase` (default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates SGR (color), CUU/CUD (cursor), and ED/EL (erase) sequences against policy.

Technical implementation includes URL-specific configuration via `sideband_allow_control_characters_config()` integrated with Git's HTTP transport. Current implementation details:
- Bitmask flags: `ALLOW_ANSI_COLOR_SEQUENCES` (1<<0), `ALLOW_ANSI_CURSOR_MOVEMENTS` (1<<1), `ALLOW_ANSI_ERASE` (1<<2)
- `ALLOW_CONTROL_SEQUENCES_UNSET` (-1) state for configuration tracking
- Backward-compatible config parsing with comma-separated values (`color,cursor,erase`)
- Phillip Wood's whitelist preserved (BEL, DEL handling)
- Comprehensive test coverage for sequence handling

Policy debate has reached consensus with Junio C Hamano endorsing Jeff King's (Peff) proposal for permissive defaults with opt-in security controls. This resolves the tension between:
- Schindelin's original strict-defaults security stance
- Brian m. carlson's documented real-world breakage scenarios (Heroku push-to-deploy, Cargo builds)
- Peff's backward-compatibility arguments

Final security model characteristics:
- Default behavior remains permissive (allowing all sequences) to preserve compatibility
- New `sideband.allowControlCharacters` configuration provides granular opt-in restrictions
- Windows retains stricter defaults due to platform-specific security considerations
- Core implementation unchanged (bitmask flags, sequence validation)
- Configuration system supports future policy adjustments

Open items moving forward:
- Documentation updates to clarify default behavior and security tradeoffs
- Final naming of configuration option (`sideband.allowControlCharacters` likely)
- Transport-layer integration remains stable (`sideband` scope preferred over `transfer`)
- ISO/IEC 2022 encoding compatibility deferred as low-priority

The thread shows Git's security response pattern: technical implementation stabilized first (Schindelin's bitmask system), followed by policy refinement informed by real-world data (Peff, carlson), with maintainer (Hamano) making final call balancing security and compatibility. The flexible configuration design allows security-conscious users to lock down behavior while minimizing ecosystem disruption.