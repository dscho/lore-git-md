[Security] ANSI escape sequence injection vulnerability in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's patch series implements controlled sanitization via granular `sideband.allowControlCharacters` configuration, now supporting three primary sequence types through bitmask flags: `color` (default-allowed), `cursor`, and `erase` (both default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates sequences against configured policies, with special handling for SGR (color), CUU/CUD (cursor movement), and ED/EL (erase) sequences.

Technical implementation evolution:
- Core sanitization expanded with new `ALLOW_ANSI_CURSOR_MOVEMENTS` (1<<1) and `ALLOW_ANSI_ERASE` (1<<2) flags alongside existing `ALLOW_ANSI_COLOR_SEQUENCES` (1<<0)
- Configuration syntax enhanced to accept comma-separated values (`color,cursor,erase`) while maintaining backward compatibility with boolean/string values
- Phillip Wood's whitelist (BEL, DEL handling) preserved alongside new sequence-specific validation
- CSI decoder in test harness verifies exact sequence handling for all supported ANSI types
- Documentation details security implications per sequence type, noting cursor/erase remain disabled by default due to attack potential

Security implementation status:
- Default policy allows color sequences (SGR) while blocking cursor movement and erase
- Transport-specific protections maintained with clearer SSH boundary documentation
- Test coverage verifies:
  - Color sequence passthrough in pre-receive hooks
  - Default blocking of cursor/erase sequences
  - Config-driven allowance of specific sequence types
  - Backward compatibility with v2/v3 boolean configurations
- Windows implementation retains stricter defaults with same config surface

Open considerations resolved:
- Granular control addresses Brian Carlson's use cases while maintaining security
- Transport limitation documentation finalized
- Downstream severity classifications stabilized
- Architectural balance achieved through configurable per-sequence policies

The series concludes with a comprehensive solution allowing controlled ANSI sequence passthrough based on demonstrated needs, maintaining protection against injection attacks while supporting legitimate terminal formatting use cases. The final implementation offers explicit user control through `sideband.allowControlCharacters` with clear security guidance for each sequence type.