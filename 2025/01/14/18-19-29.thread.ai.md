[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags for `color` (default-allowed), `cursor`, and `erase` (default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates SGR (color), CUU/CUD (cursor), and ED/EL (erase) sequences against policy.

Technical implementation includes URL-specific configuration via `sideband_allow_control_characters_config()` integrated with Git's HTTP transport. Current implementation details:
- Bitmask flags: `ALLOW_ANSI_COLOR_SEQUENCES` (1<<0), `ALLOW_ANSI_CURSOR_MOVEMENTS` (1<<1), `ALLOW_ANSI_ERASE` (1<<2)
- `ALLOW_CONTROL_SEQUENCES_UNSET` (-1) state for configuration
- Backward-compatible config parsing with comma-separated values (`color,cursor,erase`)
- Phillip Wood's whitelist preserved (BEL, DEL handling)
- Comprehensive test coverage for sequence handling

Policy debate intensifies with Johannes Schindelin citing new production evidence from Git for Windows (1+ year) and Red Hat distributions (6+ months) showing restrictive defaults work in practice without user issues. He challenges Junio Hamano's opt-in/opt-out framing, noting the current proposal already represents a middle ground (allowing colors while blocking cursor/erase controls by default). Core philosophical divide remains:
- Security-first (Schindelin/Patrick Steinhardt/Ondrej Pohorelsky): Restrictive defaults justified by terminal vulnerabilities, attack surface reduction, and production deployment evidence
- Compatibility-first (Hamano/Jeff King): Working setups must not be broken regardless of architectural merits

Security model characteristics:
- Default behavior remains permissive (allowing all sequences) per Hamano/Peff consensus
- `sideband.allowControlCharacters` provides granular restrictions (likely final name)
- Windows maintains stricter historical behavior due to platform risks
- Implementation stable (bitmask flags, sequence validation)

Open items focus on documentation and policy resolution:
- Need to document both security philosophy positions and downstream experience
- Transport-layer integration stable (`sideband` scope preferred)
- Security vs compatibility debate continues with new empirical data favoring restrictions but Hamano maintaining compatibility stance
- Windows remains special case with different defaults
- Native formatting support as alternative path has stronger case but not prioritized

The thread shows technical implementation is complete but policy disagreement persists. Schindelin's latest response strengthens the security argument with production evidence while clarifying the proposal isn't fully restrictive (colors remain allowed by default). Hamano's maintainer position likely means compatibility-first approach will prevail despite growing security case. Documentation appears to be the next step before final resolution.