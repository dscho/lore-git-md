[Security] ANSI escape sequence injection in sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides configurable sanitization via `sideband.allowControlCharacters` with per-URL configuration through `sideband_apply_url_config()`. Core sanitization in `strbuf_add_sanitized()` validates without allocations, replacing control characters with caret notation (e.g., `^[` for escape sequences) to prevent terminal state corruption and input buffer injection attacks.

The mature v5 series (rebased to v2.53.0 in v4) had production validation in Git for Windows (1+ year) and Red Hat (6+ months). Latest patches introduce nuanced control with URL-scoped configuration via `sideband.<url>.allowControlCharacters` mirroring Git's `http.<url>.*` pattern, default behavior allowing ANSI color sequences (`\033[31m` etc) via `ALLOW_ANSI_COLOR_SEQUENCES`, and new bitmask flags for cursor movements (`ALLOW_ANSI_CURSOR_MOVEMENTS`), erase sequences (`ALLOW_ANSI_ERASE`), and full passthrough (`ALLOW_ALL_CONTROL_CHARACTERS`).

The implementation now delays secure-by-default sanitization until Git v3.0 via conditional compilation (`WITH_BREAKING_CHANGES`), maintaining backward compatibility (current default: `ALLOW_ALL_CONTROL_CHARACTERS`) while committing to future hardening (v3.0 default: `ALLOW_ANSI_COLOR_SEQUENCES`). This resolves Junio Hamano's concerns about changing defaults in minor releases while preserving the security roadmap. The problematic 'default' configuration value remains removed, simplifying to explicit choices ('color', 'cursor', 'erase', or 'all').

Test coverage in `t/t5409-colorize-remote-messages.sh` verifies behaviors via `TURN_ON_SANITIZING` prerequisite, now handling both current and future default modes. Production validation in Git for Windows and Red Hat confirms URL-scoped configuration stability across versions. Documentation has been updated to consistently warn about unwanted ANSI escape sequences in both conditional blocks (current and future behavior) while keeping version-specific default behavior descriptions separate, addressing translation needs without changing functionality. Junio's remaining concerns about the security model's allowlisting approach lack specific threat model alternatives, while the ISO/IEC 2022 encoding collision protection remains addressed through conditional compilation. The series proceeds to `next` with this transitional approach, representing a compromise between immediate security and backward compatibility after extensive discussion and validation.