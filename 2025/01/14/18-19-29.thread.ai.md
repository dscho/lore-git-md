[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides configurable sanitization via `sideband.allowControlCharacters` with evolving granularity: boolean values (`true`/`false`), string values (`default`/`color`) for ANSI color sequences (SGR format ESC[..m), extended to comma-separated values ("cursor,erase") for cursor movement (ESC[nA) and erase sequences (ESC[K), plus per-URL configuration via `sideband_apply_url_config()` implementing Junio Hamano's server-specific policy suggestion mirroring `http.<url>.*` patterns.

Core sanitization in `strbuf_add_sanitized()` and sequence handlers (`handle_ansi_color_sequence()`, cursor/erase handlers) validates against policy without allocations. Patrick Steinhardt reframes the discussion as a security fix rather than feature, arguing against prioritizing backward compatibility given the arbitrary code execution risk (e.g., title bar injection), citing precedent where Git changed behavior for security. The mature v5 series combines URL matching with strict defaults, having production validation in Git for Windows (1+ year) and Red Hat (6+ months). Technical reviews by Patrick Steinhardt and Phillip Wood confirm implementation soundness (buffer handling, enum design), with brian m. carlson endorsing the security/usability balance.

Test coverage in `t/t5409-colorize-remote-messages.sh` verifies all sequence types and URL-specific behavior, with v3 rebasing to newer Git releases and converting config docs to AsciiDoc format. Documentation covers expanded configuration options and URL scoping while maintaining warnings about server trust boundaries. The series is feature-complete with URL-specific configuration as the final architectural piece, showing maturity with thorough test coverage and no outstanding review items beyond Patrick's security-priority argument now central to the discussion.