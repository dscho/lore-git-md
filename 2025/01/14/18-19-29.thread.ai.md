[Security] ANSI escape sequence injection in Git's sideband channel (CVE-2024-32002, CVE-2024-52005/GHSA-7jjc-gg6m-3329). Johannes Schindelin's implementation provides granular control via `sideband.allowControlCharacters` configuration with bitmask flags for `color` (default-allowed), `cursor`, and `erase` (default-blocked). Core sanitization in `strbuf_add_sanitized()` and `handle_ansi_sequence()` validates SGR (color), CUU/CUD (cursor), and ED/EL (erase) sequences against policy.

Technical implementation includes URL-specific configuration via `sideband_allow_control_characters_config()` integrated with Git's HTTP transport. Current implementation details:
- Bitmask flags: `ALLOW_ANSI_COLOR_SEQUENCES` (1<<0), `ALLOW_ANSI_CURSOR_MOVEMENTS` (1<<1), `ALLOW_ANSI_ERASE` (1<<2)
- `ALLOW_CONTROL_SEQUENCES_UNSET` (-1) state for configuration
- Backward-compatible config parsing with comma-separated values (`color,cursor,erase`)
- Phillip Wood's whitelist preserved (BEL, DEL handling)
- Comprehensive test coverage for sequence handling

Policy debate has intensified with Patrick Steinhardt and Johannes Schindelin advocating for restrictive defaults, now supported by Ondrej Pohorelsky's report that Fedora/CentOS/RHEL have successfully deployed restrictive defaults for six months without user issues. This real-world evidence strengthens security arguments:
1. Terminal emulator vulnerabilities demonstrated via security advisories
2. Attack surface expansion for niche use cases is unjustified
3. Native formatting support would be safer than raw sequences
4. Security should not be opt-in (contra Junio C Hamano/Jeff King position)
5. Production deployment shows restrictive defaults are viable

Security model characteristics remain:
- Default behavior still permissive (allowing all sequences) per Hamano/Peff consensus
- `sideband.allowControlCharacters` provides granular restrictions
- Windows maintains stricter defaults due to platform risks
- Core implementation unchanged (bitmask flags, sequence validation)
- Downstream adoption demonstrates practical viability of restrictions

Open items expanded:
- Documentation needs to capture both security philosophy positions and downstream experience
- Final configuration naming (`sideband.allowControlCharacters` still likely)
- Transport-layer integration stable (`sideband` scope preferred)
- ISO/IEC 2022 encoding low-priority
- Native formatting support now has stronger case as alternative path
- Security vs compatibility debate continues with new empirical data favoring restrictions
- Downstream maintainers now active participants in policy discussion

The thread now reflects a clear philosophical divide between security-first (Schindelin/Steinhardt/Pohorelsky) and compatibility-first (Hamano/Peff) approaches, with Windows-specific concerns and now production deployment evidence adding complexity. While the solution is technically implemented, the policy discussion continues regarding whether rare use cases justify maintaining potentially risky functionality by default. Pohorelsky's report provides concrete evidence that restrictive defaults work in practice at scale, shifting the debate toward security considerations.