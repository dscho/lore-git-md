Junio Hamano clarifies the behavior of the `PREFIX` environment variable in response to a bug report about system config lookup. He notes that `PREFIX` is intended as a build-time configuration, not a runtime override, suggesting the reporter may be confusing it with the `RUNTIME_PREFIX` build option. The response implies the current behavior is working as designed, though Hamano expresses some uncertainty about the current state of `RUNTIME_PREFIX` functionality. This appears to resolve the reported discrepancy by explaining that the documentation likely refers to build-time behavior rather than runtime configuration. The exchange highlights a potential documentation clarification opportunity regarding when and how path prefixes are applied in Git.