This is a clarification response in an ongoing bug discussion about `PREFIX` environment variable behavior. Junio C Hamano corrects the initial bug report's assumption that `PREFIX` should affect runtime configuration paths, explaining that `PREFIX` is actually a build-time setting. He suggests the reporter may be looking for the `RUNTIME_PREFIX` build option instead, though notes uncertainty about its current maintenance status and whether it supports runtime path adjustments without moving the entire Git installation.

The response indicates the reported behavior is not actually a bug, but rather a misunderstanding of how Git's prefix system works. This moves the discussion from being a potential implementation flaw to being about documentation clarity or possible feature gaps in runtime configuration path control. The mention of `RUNTIME_PREFIX` being potentially unmaintained hints this might touch on a rarely-used or legacy feature area.