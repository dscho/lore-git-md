Junio C Hamano reinforces concerns about MSVC warning fixes in the `add-patch` subsystem. This email continues the thread discussing Sören Krecker's patch series that aims to address compiler warnings by converting types to `size_t` for cross-platform consistency.

Hamano explicitly agrees with Phillip Wood's earlier critique that using `size_t` for line numbers is architecturally questionable, especially since Git's diff/apply infrastructure consistently uses `unsigned long`. He notes that while the current `int` type for line numbers in `struct emit_callback` could potentially be upgraded to `long` or `unsigned long`, such a change would be outside the scope of this warning-fix series.

The email also highlights Phillip's technical point about proper `errno` handling in string-to-integer conversions, which the current `str_to_size_t` macro implementation lacks. Hamano endorses this as sound advice, suggesting the patch series needs refinement in this area.

This exchange shows the discussion has moved beyond implementation details to fundamental questions about whether the type conversion approach is appropriate at all. With multiple core maintainers now expressing reservations, the patch author will likely need to either provide stronger justification for the architectural divergence or reconsider alternative warning-suppression strategies. The thread remains at a decision point regarding how to handle these platform-specific warnings while maintaining consistency with Git's existing type usage patterns.