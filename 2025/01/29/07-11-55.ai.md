Build system refactoring in Patrick Steinhardt's Meson improvements series. This is the second patch in an 11-part series focused on cleaning up and optimizing Git's Meson build configuration.

The patch inlines the static 'git' library declaration directly into the `libgit` dependency definition, eliminating the intermediate variable `libgit_library`. The change simplifies the build configuration by making it clearer that the static library exists solely to serve the dependency, with no other intended uses. The technical change moves the `static_library()` call inside `declare_dependency()`, keeping all the same parameters (sources, compile flags, linked libraries, and include directories) but removing the separate variable assignment.

This follows the series' theme of simplifying Meson build logic, as previewed in the cover letter's mention of "inlining static 'git' library". The change is purely structural - no behavior or build outputs are altered, just the organization of the build definition. The patch-specific motivation cites avoiding confusion about the library's single-purpose nature as the rationale for the change.

Key technical points for future reference:
- Removes `libgit_library` variable
- Moves static library definition into `declare_dependency()`
- Maintains all existing link/compile parameters
- Touches only `meson.build`
- Part of broader Meson configuration cleanup series

The change appears straightforward and well-contained, with no obvious downsides or areas likely to provoke discussion beyond general review of the refactoring approach.