This is a clarification in an ongoing discussion about Git's handling of the `PREFIX` environment variable and system configuration paths. Junio Hamano corrects his previous statement, noting that he was specifically referring to the `$(prefix)` Makefile macro (all lowercase) rather than the `PREFIX` environment variable mentioned in the bug report.

The thread has revealed a discrepancy between Git's actual behavior (where `PREFIX` is build-time only) and documentation in progit2 that suggests it should work at runtime. The discussion has progressed to evaluating whether to implement runtime `PREFIX` behavior or declare the external documentation inaccurate, with `GIT_CONFIG_SYSTEM` emerging as a potential runtime alternative.

Junio's correction here is minor but important for technical accuracy in the discussion, as it distinguishes between the Makefile variable and environment variable naming conventions. The core issue remains unresolved - how to provide runtime control over system configuration paths, particularly for users with restricted system directory access.