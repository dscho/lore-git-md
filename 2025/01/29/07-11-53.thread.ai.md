Here is the updated dense thread summary incorporating the new email about common-main library simplification (v2 patch 4/13):

Patrick Steinhardt's Meson build system refactoring series (v2: 13 patches) progresses with structural improvements, now covering GIT_EXEC_PATH resolution, OpenSSL fallback, static library simplification, and common-main library restructuring. Key technical changes include:

1. GIT_EXEC_PATH resolution (patch 1/13) - precise Makefile behavior matching in runtime prefix mode using get_option('libexecdir') with proper fallback paths.

2. OpenSSL fallback correction (patch 2/13):
   - Fixed broken fallback to subproject wrapper when HTTPS backend is 'auto'
   - Modified dependency declaration for proper required/auto handling
   - Corrected backend reporting when no HTTPS backend selected
   - Critical for Windows builds lacking system OpenSSL
   - Maintained static library preference while enabling fallback path

3. Static library simplification (patch 3/13):
   - Inlined `libgit.a` definition directly into `declare_dependency()`
   - Eliminated intermediate `libgit_library` variable
   - Maintained identical source files/flags/linked libraries

4. Common-main library restructuring (new patch 4/13):
   - Removed `common_main_library` static library
   - Introduced `libgit_commonmain` combining common sources with `libgit.a`
   - Fixed git-remote-http aliases (git-remote-https) by linking only to `libgit.a`
   - Eliminated duplicate `main()` definitions in test helpers
   - Affected executables: git, git-shell, test helpers, HTTP programs
   - Build files modified: meson.build, oss-fuzz/meson.build, t/helper/meson.build, t/meson.build

5. Library architecture:
   - Consolidated curl usage into libgit_curl
   - Folded version.c into libgit.a while maintaining version-def.h
   - Streamlined executable dependencies

6. Windows-specific improvements:
   - Functional OpenSSL fallback (patch 2/13)
   - GitLab CI job limits to prevent linker OOM
   - Targeted PATH handling (Windows-specific cases)
   - Documentation tool resolution via `dirs: program_path`

Technical scope covers:
- Path resolution (runtime, exec, documentation)
- Library/dependency simplification (git, common-main, version, curl)
- Windows platform reliability (OpenSSL, linker, tool resolution)
- Build system robustness
- No behavior changes - only structural improvements

Reviewer engagement:
- Justin Tobler:
  - Confirmed GIT_EXEC_PATH understanding
  - Suggested documenting coreutils assumptions
  - Influenced PATH handling approach
- Community testing still needed for:
  - Windows-specific changes (especially OpenSSL fallback)
  - Build time impact
  - Edge case verification

Key v2 additions:
- OpenSSL fallback fixes (patch 2/13)
- Static library simplification (patch 3/13)
- Common-main restructuring (patch 4/13)
- Linker OOM prevention
- Further curl consolidation
- Test tool main() deduplication
- Precise GIT_EXEC_PATH resolution

Open questions:
- Final Windows platform validation (especially OpenSSL fallback)
- Interaction with concurrent build system changes
- Confirmation of no regressions in:
  - Documentation generation
  - Windows toolchain behavior
  - Cross-platform builds

The series demonstrates consistent progress in build system modernization, with patch 4/13 eliminating another layer of indirection while maintaining all functionality. The OpenSSL fix (patch 2/13) remains the most impactful change for platforms without system OpenSSL, while patches 3/13 and 4/13 represent incremental maintainability improvements in the broader effort.