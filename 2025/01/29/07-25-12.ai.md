This is a follow-up clarification in the now-completed reftable decoupling series. Patrick Steinhardt responds to Junio Hamano's approval of the build system changes in patch 15/20 ("compat: consistently resolve headers via project root"). 

The key point is that while Git still uses the `-Icompat/` approach for certain system header replacements (like regex and poll implementations), the series has eliminated this pattern for all other cases. This maintains compatibility where needed while simplifying the build system overall. The exchange confirms the final resolution of the build system changes after Junio's initial concerns were addressed in v2 of the series.

The thread is effectively closed with this being a minor clarification rather than introducing new discussion points. All technical decisions about the reftable separation have been settled, with the build system changes now properly handling both the general case (relative includes) and specific exceptions (system header replacements).