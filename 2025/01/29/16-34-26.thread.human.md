Here's a human-readable thread summary:

This thread reports a bug where Git fails to respect the `PREFIX` environment variable when looking up system configuration files, contrary to documented behavior. Randall S. Becker found that `git config --system` consistently uses the build-time configured path (like `/usr/local-ssl3.0/etc/gitconfig`) rather than honoring `${PREFIX}/etc/gitconfig` as specified in Git's documentation. The issue reproduces across different platforms (NSV and x86_64) and Git releases (2.47.1 and 2.45.1).

The report includes clear reproduction steps and demonstrates the discrepancy between actual behavior and the git-scm.com documentation. While the technical details are complete, the root cause in the code hasn't been identified yet. Given the cross-platform reproducibility and clear documentation mismatch, this appears to be a legitimate bug rather than a documentation error or platform-specific issue.