This thread began with Randall Becker reporting that Git wasn't respecting the `PREFIX` variable for runtime configuration locations, which turned out to be a misunderstanding - `PREFIX` is actually a build-time Makefile macro, not a runtime setting. Through collaborative investigation, the proper runtime mechanism was identified as the `GIT_CONFIG_SYSTEM` environment variable, which was already documented in Git's manpages and validated by the test suite.

Junio Hamano provided authoritative clarification throughout, confirming that the existing implementation meets all requirements and that the test coverage in t1300-config.sh intentionally verifies this behavior. The discussion also uncovered inaccuracies in the progit2/Git Internals documentation that had contributed to the initial confusion.

In the latest update, Randall confirms successful verification of `GIT_CONFIG_SYSTEM` functionality and raises the remaining question about whether to correct the external progit2 documentation. The technical matter is fully resolved within Git itself, with no code changes needed. The thread has effectively reached its conclusion, with only the optional follow-up of updating the external documentation reference remaining to be decided. This case serves as a good example of Git's configuration system working as designed, with clear separation between build-time and runtime mechanisms.