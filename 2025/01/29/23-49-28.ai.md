This is a follow-up in the ongoing discussion about runtime configuration paths and the `PREFIX` environment variable discrepancy. Randall S. continues investigating alternatives after Junio clarified that `PREFIX` is not actually implemented in Git's code despite being documented in progit2.

The email focuses on testing `GIT_CONFIG_SYSTEM` as a potential solution, noting its use in t1300-config.sh test cases. This suggests Randall is exploring whether existing mechanisms could satisfy the need for runtime configuration path control without requiring new `PREFIX` implementation. The discussion remains centered on resolving the gap between documented behavior (from progit2) and actual implementation, with `GIT_CONFIG_SYSTEM` emerging as a possible practical alternative that's already supported in the codebase. The thread continues to balance documentation accuracy concerns with technical solutions for the underlying use case of restricted system directory access.