Build system optimization: Final patch in Patrick Steinhardt's Meson cleanup series focuses on documentation tool detection.

This 11th and final patch in the series addresses documentation build tool detection on Windows by ensuring all `find_program()` calls in Documentation/meson.build use the custom program path. This path is particularly important on Windows where it points to Git for Windows' tool locations. The change makes the code more consistent and reliable across platforms while also removing redundant `required: true` arguments (the default behavior).

The patch modifies how the build system finds asciidoc/asciidoctor, xmlto, and xsltproc - tools critical for generating Git's documentation. While the changes are mechanical (adding `dirs: program_path` to each call), they complete the series' goal of making the Meson build more robust, especially for Windows users.

This concludes a well-structured series that systematically cleaned up and optimized Git's Meson build configuration. The patches have maintained careful attention to edge cases while simplifying the codebase (-23 lines in curl consolidation, -16 in version library removal, and now additional small wins in documentation handling). The series appears ready for review, with its platform-specific implications being the most likely area needing scrutiny.