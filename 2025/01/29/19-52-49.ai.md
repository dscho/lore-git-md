Architectural discussion on type conversion approach for MSVC warnings

Junio C Hamano reinforces Phillip Wood's concerns about using `size_t` for line numbers in the MSVC warning fixes, noting this diverges from Git's existing diff/apply infrastructure which uses `unsigned long`. The email highlights two key technical points:

1. The current patch still proposes using `size_t` despite previous feedback questioning this architectural choice. Junio suggests `long` or `unsigned long` would be more appropriate for line numbers, noting even `diff.c` uses `int` for similar purposes (with practical limits being acceptable).

2. Phillip provides implementation feedback on the `str_to_size_t` macro, noting it needs errno handling similar to Git's existing `strtoul()` patterns - specifically the need to clear errno before conversion attempts to properly detect overflow conditions.

The discussion has shifted from warning suppression mechanics to deeper design questions about type consistency across Git's codebase. Junio's intervention as maintainer carries significant weight in questioning the fundamental approach, while Phillip's technical critique provides concrete implementation concerns about error handling patterns.

The email also includes an incidental query about a potentially bogus Cc address, though this appears unrelated to the technical discussion. The core debate remains whether to proceed with the type conversion strategy or explore alternative warning-suppression approaches that maintain consistency with Git's existing type usage patterns.