This is a technical discussion about platform compatibility changes in the `add-patch.c` code, specifically addressing MSVC compiler warnings about type conversions. Phillip Wood pushes back on Sören Krecker's v3 patch that changes `unsigned long` to `size_t` for line number handling in the interactive patch interface.

Phillip questions the fundamental approach, noting that `git diff` and `git apply` use `unsigned long` for line numbers, and suggests the warnings could be silenced through other means. He also points out a technical issue with the proposed `str_to_size_t` macro implementation regarding proper `errno` handling in `strtoul()` calls.

The exchange highlights an ongoing architectural debate in this patch series - whether to change the fundamental type used for line numbers across Git's codebase (as Sören proposes) versus maintaining consistency with existing interfaces (as Phillip advocates). This follows earlier feedback from Patrick Steinhardt and Junio Hamano that also questioned the design approach.

The patch itself shows mechanical changes to use `size_t` and `PRIuMAX` formatting throughout `add-patch.c`, along with modifications to the pluralization helper in `gettext.h`. While technically sound, the discussion suggests the changes may be solving the wrong problem or introducing inconsistency with other parts of the codebase.