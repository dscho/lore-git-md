This is a follow-up in the discussion about runtime configuration paths in Git, specifically regarding the `PREFIX` environment variable. Randall S. Becker responds to Junio C Hamano's clarification that `PREFIX` is a build-time setting by pointing to documentation that appears to suggest runtime behavior. 

Randall explains their practical constraint: they lack permissions to create files in the system `/etc` location and need a way to test with an alternate config path at runtime. They propose adding a `GIT_CONFIG_SYSTEM` environment variable as a potential solution, asking for guidance on implementation.

The email highlights a disconnect between some users' expectations (based on documentation interpretation) and Git's actual architecture. The core issue has evolved from a perceived bug to either a documentation clarification need or a potential feature gap for runtime configuration path control. Randall's specific use case (testing without system permissions) provides concrete motivation for considering runtime path overrides.