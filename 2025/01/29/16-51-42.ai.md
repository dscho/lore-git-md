Platform compatibility discussion in response to Sören Krecker's v3 patch series, focusing on type conversion warnings in `add-patch.c`. Phillip Wood raises substantive technical concerns about the proposed changes, marking a continuation of the architectural debate around handling line numbers in Git's codebase.

Phillip questions the fundamental premise of converting from `unsigned long` to `size_t` for line numbers, noting that other Git commands (`git diff`, `git apply`) consistently use `unsigned long` for these values. He references his previous email suggesting alternative warning-suppression approaches rather than type changes. The technical critique focuses on two key aspects: the `str_to_size_t` macro implementation needs errno handling similar to other `strtoul()` uses in the codebase, and the architectural appropriateness of using `size_t` for line numbers remains unresolved from prior discussion.

The email highlights ongoing tension between two approaches: Sören's mechanical warning-fix strategy versus Phillip's preference for maintaining type consistency with the rest of Git's diff/apply infrastructure. Phillip's feedback suggests the next iteration should either justify the architectural divergence more thoroughly or consider alternative warning-suppression techniques that preserve the existing type system. The discussion remains technically focused but touches on broader design philosophy questions about cross-platform type handling in Git's core.