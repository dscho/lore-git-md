This thread began with an RFC patch from Tomáš Trnka addressing a bug in `git repack` where promisor pack objects were bypassing `--keep-pack` and `.keep` file checks, despite protections like `gc.bigPackThreshold`. The proposed fix modifies `repack_promisor_objects()` to properly honor these preservation mechanisms, with open questions about where to place test coverage (existing repack tests vs. partial clone tests). The technical change appears sound but awaits input from repack and object storage experts like Patrick Steinhardt or Jeff King.

Brian M. Carlson has since noted that the patch has line-wrapping issues preventing clean application, likely due to mail client formatting problems. While not reviewing the technical merits, Carlson pointed out the need to resend with proper formatting (suggesting `git send-email`) before substantive review can proceed. The core discussion about promisor pack handling remains pending until the author addresses these formatting concerns.