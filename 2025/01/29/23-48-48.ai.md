This is a clarification in an ongoing thread about the `PREFIX` environment variable behavior in Git's configuration system. Junio Hamano corrects his previous statement about `PREFIX` being a build-time concept, noting he was actually referring to the `$(prefix)` Makefile macro (all lowercase) rather than the environment variable. 

The thread has established that Git currently doesn't support runtime `PREFIX` environment variable usage for configuration paths, despite documentation in progit2 suggesting it should. The core issue remains the need for a runtime mechanism to control system configuration paths, with `GIT_CONFIG_SYSTEM` being a potential solution. 

Key technical points from this email:
- Clarification that the build-time concept is specifically the Makefile`$(prefix)` macro
- Implicit confirmation that the `PREFIX` environment variable discussion remains unresolved
- Maintains the thread's focus on the documentation/implementation discrepancy

The thread is at a decision point about whether to align Git's behavior with the progit2 documentation or declare that documentation inaccurate. The path forward likely involves either implementing runtime `PREFIX` support or updating documentation, plus potential coordination with progit2 maintainers.