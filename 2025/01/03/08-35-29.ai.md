This is a technical discussion about Meson build system usage patterns, specifically addressing target granularity and documentation generation compared to Makefile workflows.

Patrick Steinhardt responds to Toon Claes' question about building individual targets in Meson, explaining that while the current system requires configuring builds via `meson configure` and compiling everything with `meson compile`, there are ways to achieve more granular control. He demonstrates how to build specific components like the static library, executable, or individual man pages (e.g., `Documentation/git-add.1`) using Meson's target syntax.

The email includes a proposed patch (not yet implemented) that would add `docs` alias targets to Meson, similar to Makefile's `make docs` functionality. The patch shows how documentation targets could be aggregated using `alias_target()`, though Patrick notes this still requires docs to be configured during the initial setup. The changes would modify `Documentation/meson.build` to collect all documentation targets into a single `docs` alias.

This continues the thread's exploration of Meson adoption challenges, focusing on workflow differences from Makefiles. The discussion remains technical and constructive, with Patrick providing concrete examples and potential solutions while acknowledging current limitations in target granularity. The proposed changes would improve developer experience by making Meson's documentation build process more familiar to those accustomed to Makefiles.