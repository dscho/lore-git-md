Test modernization patch updating t7110-reset-merge.sh to use Git's test helper functions. This v2 submission incorporates feedback from Patrick Steinhardt on the initial version, including a corrected commit message (fixing "enanche" to "enhance") and a shorter subject line following Git conventions.

The patch converts six shell `test -f` and `! test -f` checks to use `test_path_is_file` and `test_path_is_missing` helpers in two test cases (`--merge` and `--keep` scenarios). The commit explains that while `! test -f` checks for non-file status and `test_path_is_missing` checks for path absence, the distinction doesn't matter in this context as the tests specifically want to verify path absence. The helpers provide more informative failure messages than the raw shell tests.

Changes are limited to test modernization with no behavioral impact - the test assertions remain semantically identical, just expressed through Git's preferred helper functions. The diff shows straightforward substitutions in the merge conflict resolution tests, verifying file2 and file3 states during reset operations. This continues Git's ongoing effort to improve test debuggability through specialized helpers.