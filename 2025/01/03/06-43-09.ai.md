This is the final patch in Patrick Steinhardt's series converting array indices from `int` to `size_t` for type safety, specifically addressing `repo_get_merge_bases_many()` in `commit-reach.c`. The patch follows the established pattern of the series by carefully converting signed integer indices to `size_t` while maintaining invariants at call sites. 

The changes focus on two non-trivial callers that perform arithmetic on the index values: `show_merge_base()` in `builtin/merge-base.c` (which requires `argc >= 2` to prevent underflow) and `check_merge_bases()` in the bisect code (which always has at least one good revision). Justin Tobler reviewed and approved the patch, noting only a minor typo ("ccheck") in the commit message that doesn't warrant a reroll.

This completes the series' coverage of `commit-reach.c` after previous patches addressed `builtin/log.c` and related infrastructure. The work systematically eliminates -Wsign-compare warnings through focused, well-justified changes that preserve semantics. With all technical concerns addressed and no outstanding objections, the series appears ready for inclusion as a clean set of type-safety improvements. The thread demonstrates the project's approach to warning elimination through incremental, carefully reviewed changes to specific subsystems.