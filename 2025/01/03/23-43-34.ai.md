Bugfix addressing edge cases in object name resolution with caret-based revision syntax (`^{...}`) while maintaining the core fix for unpaired curly braces in branch names. This is a follow-up to the initial patch that fixed parsing of branch names containing unpaired curly braces (like `foo{bar`).

Elijah Newren responds to Patrick Steinhardt's concerns about potential interactions with caret-based revision syntax (`<rev>^{<type>}` and `<rev>^{/<search text>}`). The key technical points addressed are:
1. Fixes an edge case where colons in patterns like `master^{/object-name:}:t/t1006-cat-file.sh` were incorrectly parsed as path separators
2. Maintains the original fix's behavior for unpaired braces in branch names while preserving proper colon handling in search patterns
3. Identifies and corrects a test setup issue where the FUNNYNAMES prerequisite was incorrectly assumed to be needed due to a missing commit rather than branch name limitations

The solution now properly handles both:
- The original case of unpaired braces in branch names (e.g., `foo{bar:file`)
- Caret-based revision searches containing colons (e.g., `master^{/object-name:}:file`)

Test coverage in t1006-cat-file.sh is being expanded to verify both scenarios, and the FUNNYNAMES prerequisite confusion has been resolved. The changes demonstrate careful attention to parsing edge cases while maintaining backward compatibility with existing revision syntax forms.