Bugfix addressing edge case in object name parsing with caret-based revision syntax. This is a follow-up to the ongoing thread about fixing object name resolution with unpaired curly braces in branch names.

Elijah Newren responds to Patrick Steinhardt's concerns about potential impacts on caret-based revision patterns (`<rev>^{<type>}`). He identifies and fixes a specific edge case where colons in patterns like `master^{/object-name:}:t/t1006-cat-file.sh` were being incorrectly parsed as path separators. The solution maintains proper colon handling in search patterns while preserving the core fix for unpaired braces.

The response also clarifies that the FUNNYNAMES test prerequisite was incorrectly applied - the test failures were actually due to missing HEAD references rather than Windows path issues. Newren commits to fixing the test setup by ensuring a commit exists before creating test branches.

This email moves the thread closer to resolution by:
1. Addressing the caret-syntax edge case
2. Correcting the test prerequisite misunderstanding
3. Maintaining all previously verified functionality

The patch appears technically sound with careful attention to backward compatibility and test coverage. The remaining action items from the thread (proper colon handling in caret searches and test verification) are being addressed systematically.