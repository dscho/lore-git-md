Refactoring patch in a series hardening Git's object storage against race conditions. This first patch renames variables in `check_collision()` within `object-file.c` to clarify which file is the source and which is the destination, replacing generic `_a`/`_b` suffixes with explicit `_source`/`_dest` names. The change is purely mechanical - 20 lines modified with no functional impact - preparing for a subsequent patch that will implement a retry mechanism for race conditions between collision checks and file operations. The patch follows cleanly from prior work in this area and shows careful attention to making the code's intent clearer before introducing behavioral changes.