Git diff-tree --cc regression with trace_printf and strbuf invariants: Wink Saville initially reported a segmentation fault when running `git diff-tree --cc` with `trace_printf` calls in `diff_tree_combined`. Jeff King diagnosed the crash as accessing uninitialized `path.buf` fields in `struct combine_diff_parent`'s parent array, proposing a fix that guards trace output by checking `rev->combine_all_paths` and `filename_changed(p->parent[i].status)`.

Wink confirms Jeff's fix resolves the immediate issue but raises deeper concerns about strbuf invariants in the combined diff machinery. While the trace-specific crash is addressed, the underlying `combine_diff_path` struct appears to violate strbuf's documented contract that `buf` should never be NULL when initialized. Observations indicate `find_paths_generic` and `find_paths_multitree` may properly initialize these strbufs, suggesting systemic issues in parent path management.

The discussion has expanded from a specific debugging crash to examining the combined diff implementation's adherence to strbuf guarantees, with questions about whether this constitutes a separate bug. Wink also inquires about Git's bug tracking practices beyond the mailing list. Key technical focus remains on `combine-diff.c` internals, particularly initialization and access patterns for parent path strbufs across different code paths, with the immediate crash resolved but broader design questions now open.