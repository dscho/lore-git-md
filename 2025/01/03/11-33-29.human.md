This is a documentation patch series fixing formatting issues in Git's built documentation. Martin Ågren addresses two minor rendering problems that appeared during the 2.48 development cycle, affecting how the documentation appears when built with different tools (AsciiDoc vs Asciidoctor).

The first patch corrects a heading line in `git.txt` that uses tildes for underlining, while the second ensures pathnames in `gitcli.txt` are properly typeset in monospace font. Both changes are straightforward one-line fixes to the documentation source files.

The patches appear well-scoped and clearly motivated, addressing specific formatting inconsistencies that would be visible in the rendered documentation. As documentation-only changes, they're unlikely to be controversial and should be easy to review. The series is complete with just these two small fixes.