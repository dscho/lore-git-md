This is a test fix addressing MinGW-specific CI failures, part of Patrick Steinhardt's larger CI modernization series. The patch solves an intermittent EBUSY error in t0060 that occurs when copying the Git executable during runtime prefix tests on Windows. While the root cause remains unclear (possibly a MinGW or Windows quirk), the solution is straightforward: factor out the common setup into a dedicated test case to avoid repeated file operations that trigger the error.

The change is small (6 lines added, 4 removed) and well-explained, with clear evidence of the CI failure it addresses. This appears to be a solid maintenance fix that improves test reliability without introducing complexity. As the first patch in a 10-part series, it sets a good precedent for the quality of the subsequent CI infrastructure changes.