This is a follow-up discussion in the ongoing bugfix thread about object name resolution with unpaired curly braces. Junio C Hamano responds to Patrick Steinhardt's technical questions about the patch's scope and test prerequisites.

The discussion focuses on two key technical points raised in Patrick's review: (1) whether the fix needs to account for similar brace syntax using '^' (like `<rev>^{<type>}`) rather than just '@{...}' patterns, and (2) whether the FUNNYNAMES test prerequisite is truly necessary since the tests only involve '{' and '@' characters (not the quotes/tabs/newlines that FUNNYNAMES typically guards against, especially on MinGW platforms).

Junio confirms interest in investigating the caret-based variants but doesn't provide new analysis yet. The exchange suggests these are the remaining open questions before finalizing the patch, with the core solution already being technically sound. The thread shows maintainer engagement with the review feedback while indicating these edge cases may need verification before merging.

For future reference in this thread: the key technical points to track are (1) verification of brace handling in caret-based revision syntax and (2) potential simplification of test prerequisites if FUNNYNAMES isn't actually needed for these specific cases. The discussion remains focused on implementation details rather than challenging the core fix.