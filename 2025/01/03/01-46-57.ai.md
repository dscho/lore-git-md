This is a refactoring patch in a series about type safety in the commit-reach code. It changes the `remove_redundant()` function family to use `size_t` for array indices instead of `int`, addressing -Wsign-compare warnings. The key technical change is splitting the function's return value into two parts: an error code return and a `size_t*` out-parameter for the deduplicated count.

The patch modifies several functions in commit-reach.c, consistently applying the same pattern: replacing `int` parameters with `size_t` and introducing a `dedup_cnt` out-parameter. The changes are mechanical but thorough, touching about 50 lines with a net +7 insertion count. The author notes this is part of a larger effort that will also update `get_merge_bases_many()` in a subsequent commit.

Justin Tobler's response indicates agreement with Patrick Steinhardt's approach, noting the thread context shows this is part of an ongoing cleanup to improve type safety. The change appears uncontroversial and well-contained, focusing specifically on the sign comparison warnings without attempting broader interface changes. The patch maintains the existing functionality while making the types more semantically correct for array indexing.