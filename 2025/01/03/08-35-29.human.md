This is a technical discussion about Meson build system usage patterns, continuing from earlier questions about documentation generation. Patrick Steinhardt responds to Toon Claes's inquiry about whether Meson supports building specific targets independently like Makefiles do.

Patrick explains that while Meson requires top-level configuration, it does allow building individual components through specific target syntax (demonstrating commands for static library, executable, and manpage builds). He proposes adding `alias_target()` functionality to create a `docs` target equivalent to Make's behavior, attaching a sample patch that would implement this by collecting all documentation targets under a single alias.

The exchange highlights a key workflow difference between Make and Meson - where Make allows per-directory builds, Meson maintains centralized control through `meson configure`/`compile` while still offering granular target selection. The proposed change would make documentation builds more convenient without altering Meson's fundamental architecture. The discussion remains focused on practical transition concerns as the project moves from Make to Meson.