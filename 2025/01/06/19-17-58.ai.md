This is a technical discussion about hardening hash function handling in `git fast-import`, building on the earlier segfault fix. Taylor Blau responds to Patrick Steinhardt's proposal for storing hash function state in `struct git_hash_ctx`, clarifying that the immediate issue in `stream_blob()` involves initializing with the wrong hash function rather than switching mid-stream. He reiterates his earlier suggestion to use `pack_file->algop->init_fn()` to align with the hashfile API's algorithm, which would prevent the mismatch causing the original crash. As additional protection, Taylor proposes adding a `hash_algo` field to `hashfile_checkpoint` to enforce algorithm consistency between hashfiles and their checkpoints. The exchange shows alignment on the need for robust hash function handling but diverges on implementation priorities - while Patrick focuses on long-term architectural changes, Taylor addresses the specific fast-import vulnerability with layered defenses. The thread continues refining solutions that balance immediate safety with future maintainability.