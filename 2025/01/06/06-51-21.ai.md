This is a technical response in an ongoing thread about branch detection in triangular workflows. Torsten Bögershausen suggests an alternative approach to Chris Packham's original problem of detecting the existence of remote '_incoming' branches in CI scripts.

The email proposes using `git fetch -p` followed by `git branch -r` as a simpler alternative to the problematic `git rev-parse --symbolic-full-name` approach. This would list all remote branches after pruning stale references, potentially providing cleaner output for script processing. The suggestion doesn't address the specific output format issues with `rev-parse` that Chris encountered, but offers a different method that might avoid those problems entirely.

Key technical details:
- The proposed commands would first update the remote tracking references (`fetch -p`)
- Then list them in a potentially more script-friendly format (`branch -r`)
- This approach would show all remote references rather than checking for a specific one
- No mention of how to specifically detect the '_incoming' pattern versus regular branches

This appears to be an initial suggestion rather than a complete solution, as it doesn't directly solve the triangular workflow detection problem (distinguishing between direct pushes and triangular workflows) that was central to Chris's original question. The response may spark further discussion about tradeoffs between different branch detection methods in scripts.