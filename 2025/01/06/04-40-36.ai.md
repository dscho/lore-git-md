This is a scripting question about remote branch detection, seeking advice on a cleaner way to check for branch existence in Git.

Chris Packham describes a triangular workflow where their CI system fetches from one branch (e.g. 'foo') but pushes to another ('foo_incoming'). They need to detect whether the '_incoming' variant exists to support repositories that don't use this workflow pattern. Their current approach uses `git rev-parse --symbolic-full-name` but encounters issues with stray '--' in the output when the branch exists.

The core problem is that `rev-parse` emits the requested ref followed by '--' when successful, which complicates parsing. This appears to be a legitimate edge case in Git's output handling that script authors need to work around. The email doesn't propose a specific solution but rather asks for better approaches from the community.

For future reference in this thread, key technical details include:
- The specific command causing issues: `git rev-parse --symbolic-full-name refs/remotes/origin/foo_incoming`
- The problematic output format when branch exists: "refs/remotes/origin/foo_incoming\n--"
- The workflow context: CI system that needs to distinguish between direct pushes and triangular workflows
- Current workaround involves redirecting stderr and handling the '--' in stdout

This seems like a reasonable question about Git's plumbing commands, likely to prompt suggestions about alternative approaches using `git show-ref`, `git for-each-ref`, or other ref-listing commands that might provide cleaner output for scripting purposes. The email is well-framed with clear context about the real-world use case.