This is a platform compatibility patch (part 2 of 4) addressing MSVC compiler warnings in date.c. The changes fix type mismatches where 64-bit values were being truncated to 32-bit integers, and switches variables holding strlen() results from int to size_t since those values can't be negative.

The patch makes straightforward changes to variable types in the approxidate_alpha() function, where several string length comparisons were previously using int instead of the more appropriate size_t. This is a clean, focused change that should eliminate compiler warnings without affecting behavior.

The commit message is brief but covers the essentials - it would have been helpful to mention whether these warnings were causing actual problems or were just cosmetic, but the technical changes themselves look correct. This appears to be part of a larger series (as indicated by "2/4"), though we haven't seen the cover letter explaining the series' broader context.