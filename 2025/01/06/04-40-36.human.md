This is a scripting question about remote branch detection. Chris Packham describes a triangular workflow where some repositories use a special `_incoming` branch naming convention for CI, while others don't. Their current approach using `git rev-parse --symbolic-full-name` works but has awkward output handling when the branch doesn't exist, leaving a trailing `--` in the variable when it does exist.

The email is essentially asking for advice on cleaner ways to test for remote branch existence in shell scripts. It's a practical problem with clear motivation (handling different workflow conventions across repositories), though the solution space is likely well-trodden ground for experienced Git scripters. The question is likely to draw responses suggesting alternatives like `git show-ref --verify` or `git ls-remote` checks.