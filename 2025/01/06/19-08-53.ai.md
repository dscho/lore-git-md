This is a platform compatibility patch addressing MSVC compiler warnings in date.c. It's part of a 4-patch series (position 2/4), though no cover letter context is provided to understand the series' broader goals.

The patch fixes type mismatch warnings that occur when 64-bit values are truncated to 32-bit integers on Windows builds. It also changes variable types from `int` to `size_t` for all cases where the variables hold results from `strlen()`, which is more appropriate since string lengths can't be negative. The changes are straightforward and mechanical, touching only type declarations in the `approxidate_alpha()` function where string length comparisons occur.

The modifications appear correct and well-scoped - they don't change any logic, just make the types more precise and compiler-friendly. The commit message is brief but adequately explains the technical motivation. Since this is a compiler warning fix rather than a behavior change, extensive testing justification isn't needed, though it would be verified by the normal CI pipeline including Windows builds.