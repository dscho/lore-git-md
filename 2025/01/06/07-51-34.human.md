CI housekeeping: This patch simplifies the CI test matrix by merging the redundant "linux-gcc-default" job into the standard "linux-gcc" job. The patch removes the special-case job from GitHub Actions, GitLab CI, and the shared CI library script.

The rationale is that the differences between these jobs no longer provide meaningful test coverage: the explicit GCC version testing is redundant with other jobs, the Python version distinction is obsolete since Python 2's EOL, and the default branch name testing is already covered elsewhere. The changes remove about a dozen lines of CI configuration while maintaining the same effective test coverage.

This appears to be a straightforward cleanup with no visible downsides, part of a larger series about removing deprecated functionality. The patch is cleanly implemented, removing all relevant configurations consistently across CI systems.