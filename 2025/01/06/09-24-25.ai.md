Refactoring patch in a series hardening Git's object file handling against race conditions.

This is the first of three patches in Patrick Steinhardt's v2 series addressing TOCTOU races in object collision detection. The patch performs a straightforward renaming of variables in `check_collision()` within `object-file.c` to clarify the roles of source and destination files. All changes are mechanical substitutions of `filename_a`/`filename_b` to `source`/`dest` and corresponding buffer/descriptor names, with no behavioral modifications.

The changes serve as preparatory work for subsequent patches that will implement different handling for source versus destination files. The patch has already been reviewed and approved in the thread discussion, with consensus that the renaming improves code clarity for the more substantive changes to follow. The diff shows 20 lines changed in a symmetric fashion, touching only the variable declarations and their usage throughout the function.

This patch stands alone as pure refactoring with no functional impact, making it a safe starting point for the series. The thread context indicates this change was uncontroversial and helped set up the more complex race condition fixes in later patches. The commit message clearly states the limited scope and purpose of the change.