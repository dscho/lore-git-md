Refactoring patch for OS version capability series. This is the first of four patches introducing a new protocol v2 capability to exchange OS version information between Git clients and servers.

The patch extracts string sanitization logic from `git_user_agent_sanitized()` into a new `redact_non_printables()` function, which will be reused in later patches for OS version reporting. The refactoring includes several cleanups: using `size_t` for loop iteration, moving variable declaration into the loop, and using `strbuf_detach()` properly.

The changes are straightforward and well-contained, focusing only on the version.c file. The commit message clearly explains the motivation (future reuse) and the specific improvements made. This appears to be solid preparatory work for the larger feature being introduced in the series.

The patch maintains existing behavior while making the code more reusable - exactly what you'd want from an initial refactoring in a series. The TODO comment about consecutive non-printable characters suggests there may be follow-up work needed, but that doesn't block the current changes.