Performance optimization series (4/6) - test infrastructure refinement for LSan leak detection. This email exchange between Patrick Steinhardt and Jeff King discusses implementation details of the leak-checking logic in `test-lib.sh`, specifically around the `check_test_results_san_file_empty_` function.

The discussion focuses on whether to use `find -exec` versus `xargs` for processing leak reports, with both agreeing the approaches are functionally equivalent. Jeff King suggests an alternative implementation that flips the function's return logic to avoid negation operator, though this appears to be more of a thought experiment than a requested change. The key technical points are:

1. The function filters false positives from LSan's thread stack traces (specifically "Unable to get registers from thread" and "sanitizer::GetThreadStackTopAndBottom")
2. Both developers confirm `find -exec` does properly propagate exit codes when using the `+` form
3. The current implementation using `xargs` is working as intended, with the discussion primarily about code readability around the negation operator

This is a minor refinement discussion within the larger performance series, showing the maintainers' attention to detail even after architectural decisions were settled. The exchange doesn't propose any functional changes to patch 4, but explores alternative implementations that might make the negation logic clearer.