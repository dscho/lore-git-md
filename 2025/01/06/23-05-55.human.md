Bugfix for credential-cache helper protocol compliance. This is the v3 patch in a series addressing incorrect capability handling in the credential-cache daemon. The patch fixes the credential helper to properly respect request capabilities when responding with authtype and credential fields.

The key change is modifying the condition for outputting authtype and credential fields to check `CREDENTIAL_OP_RESPONSE` rather than `CREDENTIAL_OP_HELPER`, ensuring these fields are only sent when explicitly requested. This aligns with the credential helper protocol requirements clarified in earlier discussion.

The patch also adds a new test case verifying that authtype and credential fields are only returned when the request includes the authtype capability. This addresses the test coverage gap identified in previous reviews. The implementation now correctly separates capability advertisement (which should always occur) from field output (which should be conditional).

The changes appear straightforward and well-motivated, with clear test coverage for the fixed behavior. This version incorporates the protocol understanding established in the thread and should resolve the original issue without introducing new problems.