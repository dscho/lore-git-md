Here's the updated human-readable thread summary:

This thread explores how to reliably detect triangular workflows in Git CI scripts, where CI systems fetch from one branch (e.g. 'foo') but push to another ('foo_incoming'). The discussion began with Chris Packham seeking a cleaner way to detect these '_incoming' branches than his current workaround using `git rev-parse`, which has output formatting quirks.

Several technical approaches emerged: Torsten Bögershausen suggested combining `git fetch -p` with `git branch -r` to list all remote branches, while Theodore Ts'o contributed a production-tested solution using `git ls-remote` that checks specific branches via exit codes. Each approach has tradeoffs between specificity, output cleanliness, and remote access requirements.

Junio C Hamano has now shifted the discussion to deeper architectural considerations, pointing out that branch existence checks alone can't reliably detect workflow patterns since new branches wouldn't have their '_incoming' counterparts initially. He proposes protocol-level solutions instead, such as server-advertised push targets or a dedicated 'refs/capabilities/incoming' marker. This suggests the conversation may evolve from immediate scripting solutions to potential Git protocol extensions for proper workflow signaling.

The thread currently leaves open questions about whether any branch-checking approach can be universally recommended, the performance implications of each method, and whether the original `rev-parse` edge case warrants behavior changes. With Hamano's intervention, the focus now includes broader design considerations about how Git could better support such workflows at the protocol level.