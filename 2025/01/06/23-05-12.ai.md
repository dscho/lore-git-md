This is a follow-up in the credential-cache capability handling bugfix thread. brian m. carlson confirms that M Hickford's proposed change to conditionally output the `authtype` capability was incorrect, as capabilities should always be advertised regardless of the request type. 

The key technical point here is that while credential field output should be conditional on `CREDENTIAL_OP_RESPONSE`, capability advertisement must always occur. brian notes that the current patch (without the incorrect capability change) still passes tests, suggesting the test coverage may need to be expanded to verify both aspects of protocol compliance - always-advertised capabilities and conditional field output.

brian indicates they may send follow-up test improvements, though this appears to be an optional enhancement rather than a required fix. The core protocol compliance issue has been resolved through the understanding that capability advertisement and field output have different requirements in the credential helper protocol. This email serves as confirmation that the v3 patch should revert the capability advertisement change while keeping the proper conditional field output logic.