This is patch 3/10 in a series adding comprehensive ref consistency checks, specifically focusing on verifying that the "packed-refs" file is a regular file. The patch builds on the thread's established pattern of adding systematic validation to Git's ref storage.

The change introduces a filesystem check using `lstat()` to ensure "packed-refs" is a regular file, not a symlink or special file. It reuses the existing `FSCK_MSG_BAD_REF_FILETYPE` message for reporting violations. The implementation handles cases where the file doesn't exist (not an error) and includes verbose mode output. A new test case verifies the check by creating a symlink and confirming the proper error is raised.

Key technical details:
- Adds validation in `packed_fsck()` function in `refs/packed-backend.c`
- Uses `S_ISREG()` macro to check file type
- Maintains existing behavior for missing files (not an error)
- Includes test in `t0602-reffiles-fsck.sh` with symlink scenario
- Follows series pattern of incremental validation checks before final fsck integration

The patch appears well-contained with clear motivation (preventing invalid packed-refs file types) and thorough test coverage. It fits logically in the series' progression of adding validation layers to ref storage.