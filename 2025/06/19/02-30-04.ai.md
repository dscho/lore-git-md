Bug report follow-up from Grant Birchmeier, confirming workaround effectiveness and questioning design rationale.

Grant responds to Jacob Keller and Phil Hord's suggestions about recovering from the `git checkout -b` during rebase situation. He confirms Jacob's workaround of using `git rebase --abort` and `git reflog` would work, though notes it would lose his partially completed rebase work (6 of 8 files already manually resolved). He proposes an alternative workaround - saving the rebased files to a temp directory before aborting, then restoring them - which would preserve his manual rebase work.

Grant also reacts to Phil's explanation that both branches will point to the rebased tip after completion, acknowledging this technical detail but strongly questioning whether the different behavior between `git checkout` and `git checkout -b` during rebase could be intentional or expected by users. His tone suggests this is likely a bug rather than a deliberate design choice, though he plans to test Phil's observation about the final branch state the next day.

The email advances the thread by validating the proposed workarounds while keeping focus on the core issue - the inconsistent behavior between checkout variants during rebase. No new technical analysis is added, but Grant's practical experience with the workarounds and skepticism about the behavior's intentionality help frame the issue's user impact. The thread remains in the bug-report phase awaiting deeper technical investigation.