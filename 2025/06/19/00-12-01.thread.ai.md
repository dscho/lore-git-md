Subject: `git checkout -b` succeeds during rebase when it should fail
Author: Grant Birchmeier
Thread type: Bug report
Status: Behavior now understood as potentially intentional feature, with Junio demonstrating branch-splitting use case

Core issue: Inconsistent behavior between `git checkout` (fails during rebase) and `git checkout -b` (succeeds) may serve legitimate workflow needs. Junio's response shows how this allows splitting mixed commits into separate topic branches during interactive rebase, challenging the initial bug report framing.

Technical details: The rebase state detection logic intentionally permits branch creation while blocking branch switching. Junio demonstrates a workflow where: (1) starting with mixed commits, (2) using interactive rebase to group them by topic, (3) creating new branches at strategic points (after finishing topic A but before starting topic B), and (4) continuing the rebase to complete separation. This produces clean topic branches A and B.

Workarounds: Jacob Keller's original suggestions (`git rebase --abort` + `git reflog`) remain valid for accidental branch creation cases. Grant's enhanced workaround (saving rebased files to temp directory) still applies when needing to recover from unintended branch creation while preserving rebase progress.

New considerations: K Jayatheerth's anecdote about emailing rebase artifacts suggests additional UX improvements around rebase safety could be valuable, though separate from the core branch creation behavior. Junio's example introduces the concept of branch-splitting as a deliberate workflow that depends on the current behavior.

Open questions: Should the behavior be documented as a feature? Are there ways to preserve the branch-splitting capability while preventing accidental misuse? Does the current implementation provide sufficient safeguards against unintended data loss? The thread has shifted from diagnosing a bug to evaluating design intent and potential documentation needs.

Participant positions: Jacob Keller provided recovery methods, Phil Hord noted technical observations about final branch state, Grant Birchmeier focused on user impact, K Jayatheerth contributed UX considerations, Junio Hamano demonstrated legitimate use case that may justify current behavior. Phillip Wood (sequencer expert) still not engaged.

Related work: Potential connections to sequencer internals and rebase documentation. The discussion now encompasses both the original safety concerns and newly revealed workflow capabilities. Future directions may include documenting the branch-splitting technique or adding warnings/confirmations for branch creation during rebase.