This is a technical response from Junio C Hamano about the `git checkout -b` during rebase behavior. He challenges the assumption that the current behavior is a bug, demonstrating through a practical example how allowing branch creation during rebase can be useful for splitting mixed commits into separate topic branches.

Junio shows a workflow where: (1) starting with mixed commits on master, (2) using an interactive rebase to group them by topic, (3) creating new branches at strategic points during the rebase (after finishing topic A but before starting topic B), and (4) continuing the rebase to complete the separation. This results in clean topic branches A and B, each containing only their respective commits.

The email suggests the current behavior may be intentional and useful, providing a counterpoint to Grant Birchmeier's original bug report. It introduces new technical context about rebase's branch-splitting capabilities that wasn't previously discussed in the thread. The example demonstrates how the behavior Grant reported could be repurposed as a feature for repository organization.

Key technical elements mentioned are the use of `git rebase -i` with manual todo list editing, strategic placement of break points, and the sequence of branch creation commands during the operation. This contrasts with the thread's previous focus on the behavior as an unintended inconsistency and potential data loss vector.

The response shifts the discussion from "is this a bug to fix" to "is this behavior we should preserve and document" - a significant new direction for the thread. It also implicitly questions whether Grant's proposed solution (making `checkout -b` fail) would break legitimate use cases like this branch-splitting workflow.