This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction during repacking (v5, 4/9). The patch moves the initialization and execution of a reachability traversal out of `read_packs_list_from_stdin()` and into its caller `read_stdin_packs()`, where it can be shared with other code paths.

The change prepares for future work that will use this traversal to find objects reachable from input packs but stored in unknown packs. The commit message explains this is groundwork for sharing the traversal logic with the `--unpacked` case, referencing `add_unreachable_loose_objects()` as a related function.

The diff shows a straightforward code movement - the `rev_info` setup and traversal execution is lifted from `read_packs_list_from_stdin()` to `read_stdin_packs()`, with the former now taking the `revs` struct as a parameter. This is part of the series' broader effort to optimize pack maintenance, with production validation showing 5-20% speed improvements.

As the thread context notes, this series is well-reviewed and production-validated, with Junio Hamano having already approved the key refactoring in patch 3/9. This particular patch appears to be a clean, focused preparation step for later functionality.