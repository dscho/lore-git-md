Feature implementation for the new `git repo-info` command (part 2/7 of the series). This patch adds the `--format` flag to control output format, with JSON as the default and currently only supported option. The implementation includes:

The patch establishes the basic output framework for the new command, with a JSON writer integration that currently produces empty output (to be populated by subsequent patches). It includes a test verifying the JSON output structure works at a basic level, with a Perl prerequisite check. The test infrastructure is set up in t1900-repo-info.sh.

This is a straightforward technical foundation for the output formatting system that will be built upon in later patches. The implementation looks clean and follows Git's existing patterns for format handling. The test coverage is minimal but appropriate for this scaffolding step - more substantive tests will come with the actual metadata output in future patches.

As part of the GSoC project to split repository metadata functionality from `git rev-parse`, this patch moves the series forward by implementing one of the core interface elements (format selection) while deferring the actual content population to later work. The changes integrate well with the command scaffolding added in patch 1/7.