This is a follow-up discussion about the performance optimization for `git fetch --prune`. Lidong Yan is questioning whether the sorting operation could be moved into `refs_warn_dangling_symref()` rather than `prune_refs()`, and whether `strset` could be used instead.

The email suggests moving the sorting operation deeper into the call stack, which would centralize the sorting logic but potentially obscure performance characteristics. The `strset` suggestion revisits an alternative implementation approach that was previously considered but rejected due to interface constraints.

This appears to be a technical refinement discussion rather than a challenge to the core approach. The thread context shows the optimization approach has already received positive feedback from multiple maintainers (Junio C Hamano, Jacob Keller, Jeff King), with the O(N*logN) solution being sufficient consensus. Jeff King had previously considered but dismissed the `strset` approach due to interface issues with `refs_warn_dangling_symrefs()` and the need to reuse the list for `refs_delete_refs()`.

Key technical points in this exchange:
- The current patch adds sorting in `prune_refs()`
- `prune_refs()` is only called once per fetch
- The suggestion would move sorting into `refs_warn_dangling_symref()`
- Reopens discussion of `strset` alternative despite previous interface constraints
- Builds on Jeff King's earlier technical validation of the approach

This seems like a minor optimization discussion that's unlikely to change the core approach, given the established consensus and the fact the series has already received positive feedback. The thread shows the main optimization patches are effectively done, with this being a post-review refinement suggestion.