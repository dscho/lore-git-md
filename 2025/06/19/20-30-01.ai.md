This is a discussion about improving Git's clang-format configuration and tooling, continuing the thread examining real-world formatting issues. Christian Couder responds to Junio Hamano's analysis of problematic clang-format suggestions, agreeing that many current outputs are false positives not worth enforcing. Key technical points raised: the tool produces inconsistent results around line wrapping in `builtin/fast-export.c`, particularly with signature handling code where it both incorrectly unwraps short lines and makes alignment worse between parallel operations. Christian suggests several configuration improvements: disabling unwrapping of already-short lines, making line wrapping decisions context-aware by considering surrounding line lengths, and potentially disabling comment wrapping entirely if the tool can't handle Git's preferred multi-line comment style. He also proposes workflow enhancements like a `--format-check` option for `git format-patch` and explores the idea of using AI-assisted formatting with Git's coding guidelines as input. The discussion highlights the tension between mechanical consistency and human judgment in code style, with both participants agreeing the current tool produces too many low-value suggestions that shouldn't block CI.