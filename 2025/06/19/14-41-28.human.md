This is a follow-up discussion in the performance optimization thread for `git fetch --prune`. Lidong Yan revisits their earlier suggestion about moving the sorting logic deeper into the call stack, but quickly realizes their proposed approach wouldn't work.

The email clarifies that while they initially thought moving `string_list_sort()` into `refs_warn_dangling_symref()` might be possible, they now recognize this wouldn't be safe. They also acknowledge that this realization means their alternative suggestion to use `strset` in that function isn't viable either.

This appears to be a minor course-correction in the discussion, with Lidong Yan walking back their earlier suggestion after realizing a technical constraint. The core optimization approach (using sorted string lists in `prune_refs()`) remains unchallenged, and the thread seems to be converging on the current implementation.