This is a follow-up discussion on a bugfix series about batched reference updates, specifically addressing D/F conflict handling in `git receive-pack`. The thread context shows this was the final patch in a series that has now been merged (v4), with Junio Hamano noting the transactional limitation as a known issue needing future work.

Karthik Nayak responds to Junio's review comments on the second patch, which split reference deletions into a separate transaction phase to handle D/F conflicts. The key technical points clarified are: (1) the pre-batched behavior already allowed partial success (deletes succeeding while creates fail), (2) the current solution avoids intra-transaction D/F conflicts while maintaining the same failure semantics as before, and (3) true transactional behavior would require conflict resolution within a single transaction - marked as future work via NEEDSWORK.

Junio had requested adding a reference to a similar `git fetch` fix and suggested a `test_expect_failure` case to document the transactional limitation. Karthik agrees to add the reference but seeks clarification on the test intent, noting the current behavior matches pre-batched semantics where individual updates could fail independently. The discussion confirms this is an acceptable interim solution while the deeper transactional issue remains for future work.

The exchange shows consensus on merging the current approach while explicitly tracking the known limitation. Key files remain `builtin/receive-pack.c` (transaction phases) and the test suites validating the behavior. This wraps up the immediate fixes from the series while leaving the transactional D/F conflict resolution as an open problem tied to broader reference transaction semantics.