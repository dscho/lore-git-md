This is a performance optimization discussion in the ongoing thread about speeding up `git fetch --prune` with many refs. Jeff King (peff@peff.net) responds to Phil Hord's patch that restructures the dangling ref detection to avoid O(N^2) behavior.

Jeff expresses general approval for the approach, noting that while further optimization to O(N) might be possible by relying on sorted ref_store iterators, the current O(N*logN) solution is likely sufficient. He confirms that ref outputs are indeed sorted, citing a recent ref-filter change as evidence, but suggests stopping at the current optimization level is reasonable since n-log-n is typically "good enough" in Git.

The discussion touches on potential alternative data structures (strset vs string_list) but concludes the current approach is fine since it matches the existing refs_warn_dangling_symrefs() interface and is already used similarly in remote.c. This email reinforces that the technical direction of the patch is sound, with Jeff's historical knowledge of the codebase providing useful context about sorting guarantees.

The thread appears to be converging toward acceptance of Phil's optimization approach, with Jeff's comments helping to validate some of the implementation choices while acknowledging the dramatic performance improvement (410s to 4s in the test case) makes this a clear win regardless of minor output ordering changes.