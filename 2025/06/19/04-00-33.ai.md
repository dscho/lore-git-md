This is a follow-up discussion in the performance optimization thread for `git fetch --prune`. Jeff King (peff) responds to Phil Hord's first patch with additional technical insights.

Peff confirms the approach is sound and provides important context about ref iteration guarantees. He notes that while an O(N) optimization might be possible by leveraging sorted ref_store iterators (citing for-each-ref's use of this property), the current O(N*logN) solution is likely "good enough" given other bottlenecks in the code. 

The discussion touches on potential alternative implementations using strset instead of string_list, but peff concludes the current approach is reasonable since it matches the existing refs_warn_dangling_symrefs() interface and supports reuse for refs_delete_refs(). This reinforces the patch's design decisions while leaving the door open for future optimizations.

Key technical details mentioned:
- Reference to commit 2e7c6d2f41 establishing ref iteration ordering guarantees
- Confirmation that for-each-ref relies on sorted ref iteration
- Discussion of strset vs string_list tradeoffs
- Note about refs_warn_dangling_symrefs() interface constraints

The email shows consensus building around the patch's approach, with peff's comments providing both validation of the technical direction and useful historical perspective about the codebase's evolution. No new concerns are raised about the output ordering changes that were previously discussed in the thread.