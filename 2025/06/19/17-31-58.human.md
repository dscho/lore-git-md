This is a response from Junio C Hamano in the ongoing discussion about `git checkout -b` behavior during rebase. The thread has been examining whether allowing branch creation/switching mid-rebase is a bug or intentional design.

Junio presents a counterpoint to the current discussion by demonstrating a legitimate use case where `git checkout -b` during rebase is actually helpful. He describes a workflow where one can use this behavior to cleanly separate two interleaved topics (A and B) into distinct branches during an interactive rebase. His experiment shows how creating new branches at strategic points in the rebase process can produce two clean topic branches without cross-contamination.

This challenges the initial bug report's assumption that the behavior is always undesirable. Junio's example suggests there may be valid reasons for the current implementation, though he doesn't directly address why the behavior differs between regular `checkout` and `checkout -b`. The response shifts the discussion from "is it a bug?" to "when is this behavior useful versus dangerous?" - potentially opening up design considerations about making the feature more intentional rather than removing it entirely.