This is the foundational patch for the new `git repo-info` command, introducing the basic scaffolding as part of a 7-patch series. The patch establishes the command's infrastructure by:

1. Creating the initial `builtin/repo-info.c` implementation with basic option parsing (using PARSE_OPT_KEEP_UNKNOWN_OPT to allow future field specifications)
2. Adding build system integration across Makefile, meson.build, and .gitignore
3. Registering the command in git.c and builtin.h with RUN_SETUP flag

The implementation currently provides only a skeleton - it accepts no specific options and produces no output, serving as the base for subsequent patches that will add JSON/plaintext output formats and metadata query capabilities. The patch follows Git's standard patterns for new subcommands, with clean separation between the command declaration and its eventual functionality.

Key technical details for future reference:
- Command invocation: `git repo-info` (registered in git.c)
- Initial parse-options behavior preserves unknown options for future field specification
- Build system hooks ensure the command compiles in both Make and meson environments
- Follow-up patches will expand the empty cmd_repo_info() implementation

This patch represents the uncontroversial boilerplate portion of the series, with all substantive functionality and design decisions deferred to later patches. The thread context shows this is part of a mentored GSoC project that has already received architectural review, with JSON output format approved and plaintext format recently settled on category-prefixed key-value pairs.