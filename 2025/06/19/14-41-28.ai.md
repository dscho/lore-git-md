This is a follow-up discussion in the performance optimization thread for `git fetch --prune`. Lidong Yan revisits their earlier suggestion about moving the sorting operation deeper into the call stack, specifically into `refs_warn_dangling_symref()`, but then retracts it upon realizing this wouldn't work. The email also re-raises the possibility of using a `strset` in that function, which had been previously considered but dismissed due to interface constraints with `refs_warn_dangling_symrefs()`.

The discussion continues the thread's exploration of implementation details for the O(N*logN) optimization, with Lidong now acknowledging the sorting must remain in `prune_refs()`. The `strset` alternative remains an open question, though previous technical analysis from Jeff King had noted interface compatibility issues. This email doesn't introduce new technical arguments but shows ongoing consideration of alternative approaches even after the core solution has gained consensus. The thread appears to be winding down with the current implementation likely to be accepted, though participants are still exploring edge cases and alternatives.