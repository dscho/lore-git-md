Refactoring patch in a performance optimization series for MIDX and cruft pack handling.

This is the second patch in Taylor Blau's 9-part series improving pack maintenance performance. It's a straightforward scope reduction for the `revs` variable in `add_object_entry_from_pack()`, moving its declaration closer to where it's actually used. The change is small (just 2 lines modified) but part of a larger effort to optimize pack-objects behavior, particularly around cruft pack handling in MIDX during repacking.

The patch follows good practice by limiting variable scope where possible, though the impact is minor in isolation. As part of the complete series (which has already received positive reviews and production validation), this contributes to the overall code quality improvements. The series as a whole shows significant performance gains (5-20% speedups) for repositories with many unreachable objects.