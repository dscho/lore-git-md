This is a follow-up discussion on patch 2/5 of the promisor-remote protocol enhancement, focusing on test coverage for the server-side field advertisement feature. Christian Couder suggests expanding the test cases to verify both comma-separated and space-separated field lists in the `promisor.sendFields` configuration, which the protocol supports but wasn't explicitly tested in the original patch.

The test being discussed verifies that the server properly advertises configured fields (partialCloneFilter and token) while ignoring unsupported fields (stuff). The test uses packet tracing to confirm the transmitted data matches expectations, including proper URL encoding and field ordering. Christian's suggestion would strengthen validation of the config parsing logic by ensuring both common delimiter formats work correctly.

This is a minor refinement to an otherwise complete test suite for the feature. The core functionality remains unchanged - the patch already handles both delimiter styles internally, but adding explicit tests would make that support more visible. Since this is the final version of the series and all major design points are settled, this would be a straightforward documentation improvement to incorporate before merging.