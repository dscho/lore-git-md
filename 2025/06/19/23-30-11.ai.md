Refactoring patch in a performance optimization series for MIDX and cruft pack handling.

This is the second patch in Taylor Blau's 9-part series optimizing pack maintenance, specifically focusing on scope reduction of a variable in pack-objects. The change moves the declaration of the 'revs' variable inside the conditional where it's actually used, limiting its scope to only the OBJ_COMMIT case within add_object_entry_from_pack().

The modification is straightforward - it relocates the 'struct rev_info *revs' declaration from the outer 'if (p)' block to the inner 'else if (type == OBJ_COMMIT)' block where it's actually needed. This follows good coding practices by minimizing variable scope. The change was previously noted in the thread as having a corrected range-diff, indicating it was reviewed for accuracy.

As part of the larger series that's now at v5 with all edge cases addressed, this patch represents one of the foundational cleanups that enables later optimizations. The series has received positive feedback from multiple maintainers and is production-validated at GitHub with measurable performance improvements. This particular change, while small, contributes to the overall code hygiene of the pack-objects machinery being modified.