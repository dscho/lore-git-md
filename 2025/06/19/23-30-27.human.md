This is a minor refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. 

The patch simply reorders two functions in `builtin/pack-objects.c` to prepare for future changes where `show_commit_pack_hint()` will need to call `show_object_pack_hint()`. Currently `show_commit_pack_hint()` is a no-op function, so this change has no functional impact - it's purely about code organization to make the next patch cleaner.

The change was suggested by Elijah Newren during review. This is a straightforward preparatory step in the larger series that has already received significant review and validation. The patch is small (just moving 6 lines of code) and low-risk, aligning with the series' overall readiness for merging.