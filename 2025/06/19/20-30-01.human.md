This is a discussion about improving Git's clang-format integration and tooling, continuing the thread examining real-world formatting issues. Christian Couder responds to Junio Hamano's analysis of problematic clang-format suggestions, agreeing that many current outputs are false positives that shouldn't trigger CI failures. Key points:

Christian and Karthik have discussed potentially disabling some dubious style checks in the GitLab "check-style" job. Christian suggests adding a `--format-check` option to `git format-patch` to run style checks before sending patches. He identifies several categories of problematic clang-format suggestions: unnecessary line unwrapping (making parallel operations look dissimilar), overly aggressive comment formatting, and inconsistent treatment of string concatenation.

The discussion highlights the tension between mechanical consistency and human judgment in code formatting. Christian proposes some heuristics that might help - like only wrapping lines longer than surrounding context, or disabling comment wrapping entirely if it can't be configured properly. He also muses about whether an AI tool with Git's CodingGuidelines as context might produce better results than the current mechanical approach.

The thread shows developers grappling with how to make automated formatting tools more useful while minimizing noise, with no clear solution yet but several concrete pain points identified from real code examples in `gpg-interface.c` and fast-import/export code.