Grant Birchmeier reported that `git checkout -b` succeeds during a rebase when a regular `git checkout` would fail, potentially leaving the original branch in an unrecoverable state. While workarounds exist (like `git rebase --abort` plus reflog recovery), they come with the cost of losing partially completed rebase work. The thread initially focused on whether this inconsistency is a bug that should be fixed.

Junio C Hamano has now weighed in with a different perspective, demonstrating through a concrete example how allowing branch creation during rebase can be a useful feature for splitting mixed commits into separate topic branches. His workflow shows how interactive rebase combined with strategic branch creation can help organize commits by topic. This suggests the behavior may be intentional rather than a bug, shifting the discussion from "should we fix this" to "should we document and preserve this capability."

The thread now has two main perspectives: Grant's concern about potential data loss and confusing behavior for users, versus Junio's demonstration of the behavior's utility in advanced workflows. Key open questions remain about whether the current implementation is the right balance between safety and flexibility, and whether additional safeguards (like confirmation prompts) might help prevent accidental misuse while preserving the advanced use cases Junio described. Phillip Wood, as the sequencer expert, would be a valuable voice to help assess the technical tradeoffs here.