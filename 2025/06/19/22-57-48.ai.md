Feature implementation for the `git repo-info` command series, adding an `--allow-empty` flag. This is patch 4/7 in the v2 series introducing repository metadata functionality.

The patch adds a new `--allow-empty` option that forces empty output when no fields are explicitly requested. The implementation includes:
- A new `allow_empty` boolean flag in the `repo_info` struct
- Corresponding `OPT_BOOL` parse-options entry
- Updated test cases in t1900-repo-info.sh to verify behavior for both JSON (2-line output) and plaintext (0-line output) formats

The change maintains the series' focus on careful interface design, with tests verifying the empty output behavior matches expectations for both output formats. The implementation is straightforward, building on the existing format infrastructure from earlier patches. The commit message is clear but could benefit from more detail about the use case for this flag (e.g. scripting scenarios where empty output is preferable to erroring out).

This continues the pattern seen in previous patches of implementing features in small, testable increments while maintaining the architectural separation between format handling and metadata collection. The change is logically independent from the core metadata functionality being developed in parallel, allowing it to be reviewed and potentially merged separately.