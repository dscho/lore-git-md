This is a v4 patch series in the ongoing `the_repository` removal effort, focusing on eliminating sparse-checkout-related global variables. The series builds on Junio Hamano's guidance from the v3 review to use direct access to `repo_settings` rather than getter/setter functions.

The series now includes three patches:
1. Moves `core_apply_sparse_checkout` to `repo_settings.sparse_checkout` with direct access
2. Similarly moves `core_sparse_checkout_cone` (new in this version)
3. Removes `sparse_expect_files_outside_of_patterns` by localizing it (also new)

The changes affect 15 files across various commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.), removing 73 lines while adding 46. The implementation follows the now-established pattern of initializing settings in `prepare_repo_settings()` and accessing them directly, as these are commonly-used configurations.

This appears to be a well-executed refactoring that completes the sparse-checkout portion of the globals removal effort. The patches are cleanly split and the commit messages clearly explain the changes. With Junio's architectural guidance now incorporated and all design questions resolved, this series looks ready for final review and merging.