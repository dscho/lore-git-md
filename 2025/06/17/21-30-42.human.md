This is a refactoring patch in a series improving submodule remote URL handling. It moves two helper functions (`starts_with_dot_slash` and `starts_with_dot_dot_slash`) from `submodule--helper.c` and `submodule-config.c` into `dir.h` as static inline functions. These functions check path prefixes in a cross-platform way, similar to existing `_native` variants in dir.h.

The change eliminates duplicate implementations and makes the helpers available earlier in the submodule--helper compilation. The author notes some naming considerations (keeping the simple names rather than adding `_platform`) and adds clear documentation comments explaining the functions' purpose.

This appears to be straightforward preparatory work for the larger submodule URL matching series, with no behavior changes. The patch is cleanly implemented with matching removals from both original locations and proper documentation added to the header.