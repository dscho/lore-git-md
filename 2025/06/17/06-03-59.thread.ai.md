Thread summary:

Feature patch series adding SHA-256 support to git-gui, the TCL-based Git GUI. The core change makes hash length detection dynamic (40 chars for SHA-1, 64 for SHA-256) rather than hardcoded to SHA-1. Key technical changes: git-gui.sh now detects object format via `git rev-parse --show-object-format` and sets global `hashlength` variable. Four files modified to handle hash IDs: blame view, repository selection, remote branch deletion interfaces updated with dynamic regex patterns via TCL's `string map`. Null SHA1 constant becomes dynamically-sized null hash. Maintains backward compatibility with SHA-1 repositories while adding SHA-256 support. This aligns with Git's multi-hash-algorithm work, though rationale for git-gui changes now (vs waiting for broader SHA-256 adoption) could be better explained in commit message. No reviews or discussion yet as this is the initial submission.