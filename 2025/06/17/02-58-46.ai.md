This is part 4/6 of a refactoring series improving submodule remote lookup logic, specifically focusing on the fallback remote name determination in detached HEAD state.

The patch introduces a new `repo_default_remote()` helper in remote.c that encapsulates the logic for determining a default remote, replacing the hardcoded "origin" fallback in `repo_get_default_remote()`. The new helper first checks the branch configuration (via `remotes_remote_for_branch()`) before falling back to checking for a single remote or "origin". This provides more robust handling for cases where users have renamed their default remote.

Key technical details:
- New `repo_default_remote()` function added to remote.h/remote.c
- Modified `repo_get_default_remote()` in submodule--helper.c now delegates to this helper
- Existing `branch_get()` refactored into `repo_branch_get()` to support repository parameterization
- New test case in t7406 verifies behavior with renamed remotes

The email includes a suggestion from Lidong Yan to consider inlining `repo_default_remote()` into `repo_get_default_remote()` since they're closely related, though the patch currently keeps them separate. The changes maintain backward compatibility while improving the fallback logic, with test coverage for the renamed remote scenario.

This continues the series' pattern of repository parameterization and logic consolidation, building toward the URL-based remote matching feature in part 6. The patch appears well-structured with clear commit message and test coverage.