Here's a human-readable thread summary:

This thread reports a bug in Git's rename detection when symlinks are involved. The issue occurs when a file is renamed and then replaced with a symlink pointing to its renamed version - Git fails to recognize the original rename relationship, instead showing the file as deleted and the new one as newly created. 

The reporter provides a clear reproduction case: first renaming a file (commit 5d51b10), then creating a symlink to the renamed file via an amended commit (377d9bd). The rename detection machinery loses track of the file's history across this symlink operation. While the impact is limited to workflows that mix file renames with symlinks, this appears to be an unintentional gap in Git's rename tracking capabilities.

No solutions have been proposed yet, but the minimal test case makes this a good candidate for investigation by someone familiar with Git's diff and rename detection internals. The behavior clearly differs from what users would expect when tracking file movements.