This is a memory leak fix and cleanup patch in the remote.c subsystem, serving as preparatory work for the larger submodule remote URL improvement series.

The patch addresses several memory management issues in the branch and remote struct cleanup paths. Key fixes include:
- Adding missing refspec_clear() calls for remote->push and remote->fetch
- Properly freeing branch->merge_name elements and array
- Fixing a NULL pointer access in branch->merge handling
- Moving xstrdup into add_merge() to clarify memory ownership
- Removing const qualifier from branch->merge_name to allow proper cleanup

The changes enable safe configuration reading for submodule repositories by preventing memory leaks and crashes during cleanup. While the immediate motivation is supporting the submodule remote URL work, these are general improvements to the remote.c infrastructure. The patch includes careful handling of the tricky memory ownership between branch->merge and branch->merge_name arrays, though the commit message notes there's room for further cleanup of this relationship. The changes are well-justified with clear explanations of both the problems and solutions, and the diff shows careful attention to memory management details.