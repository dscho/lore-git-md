Thread identity: Bug report about Git's rename detection failing when files are replaced with symlinks pointing to their renamed versions.

Core problem: Git's diff machinery fails to track file renames when there's an intervening symlink operation. Specifically, when a file is renamed and then replaced with a symlink pointing to its renamed version, Git shows the original file as deleted and the new file as newly created, rather than recognizing the rename relationship.

Current status: Issue reported with clear reproduction steps but no proposed solution yet. The reporter asks whether this behavior can be fixed.

Key technical details:
- Reproduction involves:
  1. Renaming `somefile` to `somefile-renamed` (commit 5d51b10)
  2. Creating symlink `somefile` pointing to `somefile-renamed` and committing via `--amend` (commit 377d9bd)
- Affects Git's diff machinery's rename detection
- Impacts workflows mixing symlinks and file operations while maintaining content continuity

Open questions: Whether and how to fix this edge case in rename detection. No technical discussion or proposed solutions yet.

Participant positions: Single reporter so far, no maintainer or reviewer responses yet.