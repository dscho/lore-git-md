Final patch in a series removing global variables for sparse-checkout configuration. This third patch localizes the `sparse_expect_files_outside_of_patterns` variable, which was only used in one function (`clear_skip_worktree_from_present_files()` in sparse-index.c). The change removes the global variable and moves its declaration into the function that uses it, while also cleaning up related config handling code.

The patch removes the variable from environment.c/h and deletes the now-unused sparse config parsing code from config.c. It also eliminates a `USE_THE_REPOSITORY_VARIABLE` workaround in sparse-index.c. The functionality remains the same - the config value is now read directly via `repo_config_get_bool()` when needed.

This completes Ayush Chandekar's series migrating sparse-checkout configuration from globals to repository-specific settings, following the established pattern of moving config into `repo_settings`. The changes are clean and focused, with no behavioral impact. The series appears ready for merging as part of the ongoing effort to remove `the_repository` global.