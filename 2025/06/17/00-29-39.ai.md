Test modernization: This patch updates t/t2400-worktree-add.sh to use Git's test helper functions (`test_path_is_file`, `test_path_is_dir`, `test_path_is_missing`) instead of direct shell `test -[efd]` commands. The change improves test diagnostics by providing more informative failure messages when path checks fail.

The patch makes straightforward mechanical replacements across 10 test assertions in the worktree test file. Each change replaces a basic shell test with the equivalent Git test helper, maintaining the same logical checks but with better error reporting. The changes cover various worktree scenarios including locked branches, orphan branches, rebase/bisect states, and path sanitization.

Key files touched:
- t/t2400-worktree-add.sh (all changes are in this test script)
Key test helpers used:
- test_path_is_file (checks file existence)
- test_path_is_dir (checks directory existence)
- test_path_is_missing (verifies path doesn't exist)

The patch appears well-scoped and correctly implemented, following Git's established pattern for test modernization. It's part of an ongoing effort to improve test diagnostics across the codebase. The changes are purely about test infrastructure - no actual worktree behavior is modified. The patch includes a co-authored-by line indicating collaborative work, and maintains all existing test logic while just upgrading the assertion mechanism.