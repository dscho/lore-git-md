This is a review comment on the final patch in Ayush Chandekar's series to remove sparse-checkout-related global variables as part of the `the_repository` removal effort. The patch localizes the `sparse_expect_files_outside_of_patterns` variable to its only usage site in `sparse-index.c`.

Junio Hamano points out a minor formatting issue - a trailing whitespace in the patch where `repo_config_get_bool()` is called. The change itself looks correct, moving the variable from global scope to local while maintaining the same functionality. This follows the established pattern seen in the rest of the series for eliminating single-use globals.

The patch appears ready to merge once this trivial whitespace issue is fixed. It completes the series' goal of removing sparse-checkout configuration from global variables, with the first two patches handling repository settings migration and this final one demonstrating the localization approach for a single-use variable. The series as a whole represents significant progress in the ongoing `the_repository` removal effort.