This is a build system correction for the submodule remote lookup series. Junio C Hamano points out that patch 4/6 fails `make hdr-check` because `remote.h` uses `struct repository` in the new `repo_default_remote()` declaration without a forward declaration. The fix adds `struct repository;` to the header's forward declarations section, matching the style already used for other opaque structs in the file.

The change is minimal (just one line added) and purely technical - it doesn't affect the series' functionality but ensures clean compilation. This follows the pattern seen elsewhere in `remote.h` where structs are forward-declared before use. The fix maintains the series' goal of repository parameterization while addressing a minor oversight in header dependencies.

Key technical details:
- Adds missing `struct repository` forward declaration in `remote.h`
- Preserves all existing functionality from patch 4/6
- Matches existing style for opaque structs in the same file
- Required for `make hdr-check` to pass despite not affecting runtime behavior

This appears to be a routine build system fix for an otherwise complete series that's waiting for post-freeze consideration. The change is uncontroversial and aligns with the project's header inclusion patterns.