This is a refactoring patch that simplifies the branch and remote struct cleanup logic while addressing memory management concerns raised in the thread. Jacob Keller proposes eliminating the dual `merge_name`/`merge` arrays in favor of a single `merge` array of refspec_items, adding a `set_merge` flag to track initialization state.

The key changes:
- Removes `merge_name` array entirely, storing merge sources directly in refspec_items
- Adds `set_merge` flag to track whether `set_merge()` has initialized the merge refspecs
- Consolidates cleanup into new `merge_clear()` helper
- Updates all callers to use `merge[i]->src` instead of `merge_name[i]`
- Simplifies NULL checks since merge array is now more consistently managed

The patch addresses Junio's observation about redundant NULL checks by making the initialization state more explicit. While the solution still involves some complexity around partially-initialized refspec_items, it eliminates the parallel array maintenance and reduces the cleanup code paths. This continues the thread's theme of memory safety improvements while also simplifying the data structure.

The changes touch core remote handling code (remote.c, remote.h) and several callers (branch.c, builtin/pull.c), requiring careful review of the refspec usage patterns. The approach appears consistent with the thread's ongoing work to improve memory management in remote handling while maintaining functionality.