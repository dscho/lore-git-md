Here's a human-readable summary of the thread:

The thread began with reports of Solaris build issues related to `sed` compatibility - a warning about missing newlines in version-def.h generation and a more serious error about Solaris' native `sed` not supporting the `-E` flag for extended regex. Junio Hamano quickly provided a patch addressing both: using basic regex instead of extended regex for the config-list.h issue and adding explicit newlines via `printf` for version-def.h. Multiple Solaris users (Brad Smith and Collin Funk) confirmed the fixes worked across Solaris 10, 11 and Linux, with Collin adding a Reviewed-by.

The discussion of the technical fixes led to a broader conversation about portability best practices. Eric Sunshine initially suggested `echo` might have been simpler than `printf` for the newline issue, but after Paul Smith pointed out `echo`'s own portability pitfalls, they agreed Junio's `printf` solution was actually more robust. This exchange served as a useful case study in shell scripting tradeoffs.

Jean-Noël Avila then raised the important question of how to prevent similar issues in the future, suggesting the need for better Solaris testing infrastructure. In the latest email, Eric Sunshine proposes a practical middle ground: extending the existing `check-non-portable-shell.sh` script to catch sed portability issues during development. While not a full Solaris CI solution, this would help flag problems like the `-E` flag early, maintaining the thread's theme of favoring portable solutions while minimizing developer overhead. The discussion shows how a specific build fix evolved into broader considerations about sustainable cross-platform compatibility.