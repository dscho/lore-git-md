This is a platform compatibility discussion regarding Solaris build issues with sed. The thread has already seen a proposed solution from Junio Hamano that addresses both the newline warning in version-def.h generation and the `-E` flag error in config-list.h generation by using basic regex instead of extended regex. 

Eli Schwartz weighs in on the discussion about using GNU sed (`gsed`) as a solution, noting that on their Solaris system, GNU sed is only available as an optional third-party package, not as a standard Solaris component. They express skepticism about requiring users to install GNU sed to build Git, suggesting this would be an unattractive solution compared to Junio's already-working portable approach.

The email reinforces that Junio's solution (already confirmed working by Brad Smith) is preferable path since it maintains compatibility without introducing new dependencies. The discussion appears to be winding down with consensus around the portable regex approach rather than requiring GNU sed. No new technical details are introduced - this is primarily a commentary on the social/ecosystem aspects of the proposed `gsed` workaround.