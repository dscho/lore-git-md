This is a platform compatibility discussion about FreeBSD support policies, specifically regarding build system changes for `memmem()` implementation.

Junio C Hamano responds to Carlo Arenas Belón's proposed modification to the FreeBSD configuration in `config.mak.uname`. The key change would actively set `NO_MEMMEM` for FreeBSD versions below 12, while maintaining the existing build infrastructure. Junio expresses support for this more defensive approach, noting they don't particularly care about supporting versions older than FreeBSD 12.

The email shows movement toward consensus on actively enforcing version requirements through build system changes rather than just documentation. Junio's response indicates this won't be rushed into the upcoming release, giving time for proper review. The technical implementation would use shell expression matching to detect FreeBSD versions below 12 and force use of Git's compatibility `memmem()` implementation for those cases.

This moves the thread forward by providing maintainer approval for the build-system enforcement approach while keeping the door open for final polishing of both code and commit message. The discussion remains focused on the practical implications of version support rather than reopening the broader policy questions already addressed earlier in the thread.