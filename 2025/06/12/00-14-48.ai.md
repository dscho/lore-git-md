This is a technical discussion in the byte-order handling refactoring series, specifically addressing performance considerations around the ntohl/ntohll macros. Brian m. carlson responds to Sebastian Andrzej Siewior's patch 4/6 in the series, which aimed to always redefine these macros for optimal performance.

The key technical point is a performance optimization discussion around x86 architecture. While Sebastian's patch suggested that using the 'bswap' opcode would outperform shift-based implementations, Brian counters that modern compilers (both GCC and clang) already optimize shift-based byte swapping to the same bswap instructions at -O2 optimization level, which Git uses. This is demonstrated via a Compiler Explorer link showing the equivalent generated assembly.

This email doesn't propose any code changes but provides important context about compiler behavior that could influence the series' approach to macro definitions. The observation suggests that the performance motivation for forcibly redefining these macros might be less critical than initially thought, since compilers already handle the optimization automatically. The discussion remains focused on x86 architecture, leaving open whether similar optimizations apply to other platforms addressed in the broader series.

The exchange represents a typical performance-oriented code review discussion where implementation assumptions are validated against actual compiler behavior. It doesn't challenge the series' core correctness improvements for big-endian systems, but could inform decisions about how aggressively to pursue certain optimizations in the byte-swapping infrastructure.