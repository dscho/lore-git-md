This is a platform compatibility follow-up discussing Solaris build issues, specifically focusing on shell script portability patterns.

Eric Sunshine points out that the recent fix for Solaris sed compatibility (which added explicit newlines via `printf "%s\n"`) could have been avoided by using simpler `echo` statements instead. He notes that the original issue was introduced in commit f6a2efdc9b, which used `printf "%s"` without newlines in several places when `echo` would have worked just as well and been more robust.

The observation serves as a post-resolution commentary on coding style rather than proposing any new changes, suggesting that `echo` is generally preferable to `printf` for simple string output cases since it automatically handles newlines correctly. This builds on the thread's earlier resolution of Solaris sed issues while pointing to a broader pattern of shell script portability considerations. The email doesn't propose any action but provides useful context about alternative approaches that could prevent similar issues in the future.