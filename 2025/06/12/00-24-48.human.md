This is a performance-oriented refactoring discussion about byte-order handling in Git's bswap.h. Brian m. carlson responds to Sebastian Andrzej Siewior's patch that added support for the `__BYTE_ORDER__` macro as a fallback for endianness detection.

The main contribution here is a suggestion to optionally support C23's `<stdbit.h>` when available (with `__STDC_VERSION__ >= 202311L`), which provides standardized endianness macros. This would help with compilers that support modern C standards but aren't GCC/clang-compatible. The suggestion is framed as optional since MSVC support is unknown.

The exchange shows technical depth about compiler compatibility tradeoffs, with Brian noting this would be useful for "more esoteric compilers" while acknowledging it's core work on making endian detection more portable. The discussion remains focused on improving the patch's completeness rather than challenging its approach.