Platform compatibility patch addressing Solaris sed issues, following up on Junio's proposed solution. Brad Smith confirms that Junio's patch successfully resolves both reported Solaris build problems: the version-def.h generation warning (fixed by adding a newline in printf) and the config-list.h error (fixed by converting from extended to basic regex syntax). The confirmation indicates the changes work as intended without introducing new issues. The thread appears resolved with this practical solution that avoids the GNU sed dependency question while maintaining functionality. Key technical details include the BRE pattern `/^`*[a-zA-Z].*\..*`*::$/` replacing the ERE version, and the added newline in GIT-VERSION-GEN's printf call. The changes affect build scripts but not runtime behavior, with output formats preserved. This concludes the Solaris sed compatibility discussion unless further platform-specific issues emerge.