Platform compatibility patch discussing FreeBSD build configuration. This is a follow-up in a thread about enabling `memmem()` usage on FreeBSD systems.

The email responds to Brad Smith's suggestion to enable system `memmem()` starting from FreeBSD 6.0. Carlo Arenas raises concerns that the FreeBSD 6.0 implementation was buggy and incompatible until FreeBSD 12, suggesting any change should target later versions if testing confirms the system version is indeed faster than Git's bundled implementation.

The thread also touches on removing support for FreeBSD 4.x, which Carlo notes is already effectively unsupported as it predates C99 and lacks required POSIX features like `statvfs`. The discussion appears to be converging toward updating the build configuration while being cautious about which FreeBSD versions to enable certain features for.

Key technical details include the `memmem()` function compatibility timeline (buggy until FreeBSD 12) and specific platform requirements (C99, POSIX `statvfs`). The thread shows developers carefully considering both backward compatibility and modern system capabilities when adjusting build configurations.