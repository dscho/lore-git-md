This is a patch in the byte-order handling refactoring series, specifically the revert of a problematic change to bswap.h that caused incorrect behavior on big-endian systems. Junio Hamano is applying Sebastian's patch while noting a minor typo fix (31/64 -> 32/64) in the commit message.

The patch cleanly reverts commit 6547d1c9, which had incorrectly enabled `__builtin_bswap` macros on all platforms regardless of endianness. This broke assumptions elsewhere in the code where ntohl/ntohll macros relied on bswap being little-endian only. The revert removes 13 lines of built-in bswap detection infrastructure (which will be properly reintroduced later in the series) and restores the x86-specific inline assembly implementation.

The change is straightforward and clearly motivated by test failures on s390x (a big-endian platform). This aligns with the thread's broader goal of fixing endianness handling while maintaining performance. Junio's role here is purely as maintainer - he confirms the technical content while catching a minor documentation typo during application.