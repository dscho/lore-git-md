This is a platform compatibility discussion follow-up regarding FreeBSD version support policies.

Carlo Arenas responds to Junio Hamano's suggestion about documenting FreeBSD version requirements by proposing an alternative approach to enforce version 12+ support at the build system level. His untested patch modification would actively set NO_MEMMEM for FreeBSD versions below 12, though he notes this wouldn't actually break builds since Git's fallback implementation would still work.

The email continues the ongoing tension between documentation-based policies versus build system enforcement. Arenas' position appears to favor stronger technical guarantees by making the version requirement explicit in the build configuration, while still maintaining backward compatibility through Git's fallback implementations. His "UnfortunatelyYes" comment suggests this is a pragmatic rather than ideal solution.

Key technical details include the specific version check syntax (`expr "$(uname_R)" : '\([1-9][0-9]*\)\.'`) and the observation that this change wouldn't impact buildability due to Git's existing fallback mechanisms. The discussion remains focused on FreeBSD 12+ as the established cutoff point from earlier thread consensus.

This reply maintains the thread's separation between security support timelines (13.4+), implementation correctness (12+ for memmem()` fixes), and the practical question of how strongly to enforce version requirements. The proposed change would make version checking more explicit while avoiding the "actively block builds" approach that remains an open question in the thread.