This thread began as a technical discussion about when to use FreeBSD's system `memmem()` implementation versus Git's bundled version, given historical bugs in FreeBSD's implementation until version 12. The conversation has broadened into a policy discussion about platform support requirements, with Junio C Hamano now weighing in.

The technical consensus is settling on FreeBSD 12+ as the safe threshold for using system `memmem()`, while older versions would use Git's implementation. Brian m. carlson made a strong case for explicitly dropping FreeBSD 4.x support entirely due to its lack of C99 and POSIX features, as well as security risks from running end-of-life systems. This position has gained traction in the thread.

Junio has shifted the focus toward formalizing these requirements in Documentation/technical/platform-support.adoc, which already mandates C99 and active security support (effectively excluding FreeBSD versions below 13.4). He raises a new policy question: whether Git should actively prevent builds on unsupported FreeBSD releases, balancing the need to protect users from broken builds against the flexibility some developers need to work with older systems. This remains an open question for the community to discuss.

The thread has evolved from a narrow technical decision about `memmem()` implementations to broader considerations of platform support policies, with security implications and documentation practices now central to the discussion. The path forward appears to involve both technical changes (version-specific `memmem()` selection) and policy updates (formalizing version requirements in platform-support.adoc).