This thread has been discussing FreeBSD support policies, particularly around the build system's handling of `memmem()` implementations across different FreeBSD versions. The core question has been whether to simply document that FreeBSD 12+ is required (with older versions unsupported) or to enforce this through build system changes.

Two main approaches emerged: one patch proposed removing special-case build settings for pre-FreeBSD 12, while Carlo Arenas counter-proposed actively setting `NO_MEMMEM` for versions below 12 to enforce the requirement. Junio Hamano has now weighed in, expressing preference for Carlo's more defensive approach that would actively prevent builds on unsupported versions by forcing use of the compatibility `memmem()` implementation.

While consensus is forming around this build-system enforcement approach, Junio indicates this won't be rushed into the upcoming release, allowing time for proper review. The discussion has moved from debating policy (where version 12+ is now accepted as the cutoff) to implementation details, with security considerations and practical maintenance concerns driving the preference for active enforcement over passive documentation. The thread now focuses on finalizing the technical implementation of version detection and fallback behavior.