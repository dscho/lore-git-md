Patch 3/6 in the bswap.h refactoring series, focusing on MSVC compatibility. This patch explicitly defines endianness constants for Microsoft's compiler, which lacks the standard `__BYTE_ORDER__` definition. The change is straightforward - since MSVC only targets little-endian architectures (x86 and arm64), the patch hardcodes `GIT_BYTE_ORDER` as little-endian.

Junio Hamano notes a minor typo ("an arm" -> "on arm") in the commit message but otherwise appears ready to queue the patch. The change is a logical extension of the series' goal to modernize endianness handling while maintaining platform compatibility. The diff shows a clean 5-line addition with one conditional tweak to avoid redefinition conflicts.

This continues the pattern seen in earlier patches: platform-specific handling with minimal intrusion, keeping the door open for future standardization via C23`<stdbit.h>`. The MSVC case is particularly simple since the compiler's target architectures are exclusively little-endian, making the hardcoding uncontroversial.