Platform compatibility discussion about shell script portability. This is a follow-up to the resolved Solaris sed compatibility thread, where Eric Sunshine acknowledges Paul Smith's point about `echo` vs `printf` portability concerns. 

Eric had previously suggested that using `echo` instead of `printf` would have avoided the newline issue in version-def.h generation, but Paul correctly pointed out that `echo` has its own portability pitfalls across Unix implementations. In this brief response, Eric concedes the point, agreeing that `printf` is indeed the safer choice when the output format isn't strictly controlled, despite being slightly more verbose. 

This exchange serves as a useful postscript to the main thread, reinforcing why Junio's `printf`-based solution was the right approach. The discussion doesn't introduce new changes but provides valuable context about shell scripting best practices that may inform future portability fixes. The tone is collegial, with Eric appreciating the correction from Paul, a known shell scripting authority.