This is a technical discussion about FreeBSD compatibility, specifically regarding the use of the system's `memmem()` function versus Git's compatibility implementation.

Carlo Arenas responds to Brian m. carlson's earlier message about FreeBSD version support, clarifying that his concern isn't about build configuration but about behavioral differences in `memmem()` implementations. He notes that while Git's code doesn't currently trigger the problematic edge cases (like zero-length needles that could cause some FreeBSD versions to return NULL), it would be safer to only use the system `memmem()` in versions where these issues are resolved.

The discussion reveals an interesting tension between performance considerations (potentially faster system implementations) and safety (known behavioral quirks in older versions). Carlo suggests that before switching to system `memmem()`, there should be verification that it actually provides performance benefits over Git's compatibility code.

This exchange is part of the broader thread about establishing FreeBSD support policies, where technical details about specific functions intersect with version support decisions. The conversation remains focused on concrete technical concerns rather than policy questions, with Carlo providing specific implementation knowledge about `memmem()` behavior across FreeBSD versions.