This is a refactoring patch in the final version of the stash import/export series. It extracts stash revision parsing logic into a reusable helper function, `parse_stash_revision()`, which handles three cases: default stash reference (`@{0}`), numeric stash references (`@{n}`), and direct commit references. The function centralizes error handling for missing stashes and adds a `quiet` parameter to suppress error messages when needed.

The change is part of the larger effort to support stash export/import functionality, where this parsing logic will be reused. The patch maintains existing behavior while making the code more modular - the new function preserves all special stash reference forms but moves them behind a cleaner interface. This follows the series' pattern of factoring out reusable components, as seen with earlier extractions like `write_commit_with_parents()`.

Key technical details include:
- The function takes a `strbuf` for output and supports quiet failure modes
- It handles the same three input forms as before (null, numeric, direct)
- Error messages remain unchanged from previous versions
- The calling code in `get_stash_info()` is simplified to use the new helper

This appears to be the final form of this refactoring, having gone through eight review iterations with all feedback addressed. The change is straightforward and unlikely to be controversial, serving as infrastructure for the larger feature.