This is a code review email in the `git repo-info` feature development thread, where Junio C Hamano provides detailed feedback on argument handling and function design.

Junio responds to Karthik Nayak's earlier review of the patch, agreeing with the suggestion about 80-column wrapping but expanding on the rationale. He suggests grouping related arguments together (like `argc`/`argv`) and keeping the interface extensible for future additions. The example shows how argument ordering affects maintainability when new parameters are added later.

The review raises an architectural question about why the `format` parameter is passed as a string rather than being pre-processed into an enum by the caller, similar to how `allow_empty` handles the `--allow-empty` flag. This suggests the current implementation may have inconsistent parsing strategies between different parameters.

Junio's feedback maintains the thread's constructive tone while pushing for more thoughtful API design. The exchange shows the project's attention to detail in interface design, even for internal functions. This level of scrutiny is typical for core Git development, where maintainability and forward-compatibility are prioritized.