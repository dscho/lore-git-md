This is a platform compatibility discussion about FreeBSD version support policies, specifically regarding the `memmem()` implementation.

Junio C Hamano responds to Carlo Arenas Belón's suggestion to actively set NO_MEMMEM for FreeBSD versions below 12, rather than just documenting the version requirement. The proposed change would make the build system enforce the version cutoff by explicitly disabling the system's `memmem()` on older FreeBSD versions, while still allowing the build to proceed using Git's compatibility implementation.

Junio indicates he's fine with this more defensive approach, though notes there's no urgency since this concerns older systems and won't affect the upcoming release. He asks Carlo to prepare a final version incorporating both the code change and updated commit message.

The exchange shows movement toward consensus on actively enforcing the FreeBSD 12+ requirement in the build system, rather than just documenting it. This represents a middle ground between completely removing old version support and passively documenting version requirements. The discussion remains focused on technical implementation details rather than reopening the broader policy questions about version support.