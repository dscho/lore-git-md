This is part 3 of Sebastian Andrzej Siewior's performance-oriented refactoring of Git's bswap.h, specifically addressing MSVC compiler support. The patch hardcodes little-endian byte order for MSVC since it lacks `__BYTE_ORDER__` definitions but only targets little-endian architectures (x86 and arm64). 

The change introduces three new MSVC-specific defines (`GIT_LITTLE_ENDIAN`, `GIT_BIG_ENDIAN`, `GIT_BYTE_ORDER`) and modifies the fallback logic to skip redefinition if `GIT_BYTE_ORDER` is already set. Junio Hamano notes a minor typo ("an arm" -> "on arm") but otherwise appears ready to queue the patch.

Technically, this maintains the series' pattern of platform-specific handling while simplifying endianness logic. The 6-line change (5 insertions, 1 deletion) in compat/bswap.h follows the MSVC case established in earlier discussion, where platform quirks are addressed individually rather than through complex detection logic. This patch enables subsequent optimizations in the series (patches 4-6) to work reliably across compilers.

The thread context shows this is proceeding smoothly after patch 1's typo correction, with Junio requesting only minor commit message refinements. The MSVC handling appears uncontroversial as the compiler's architecture support makes the endianness assumption safe. Future patches will build on this foundation to standardize byte-swapping operations.