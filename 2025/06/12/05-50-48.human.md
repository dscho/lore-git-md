This is a follow-up discussion in the Solaris build compatibility thread, focusing on code style rather than the original build issues. Eric Sunshine points out that the fix for the Solaris sed newline warning (which used `printf "%s\n"`) could have been avoided entirely if the original code had used `echo` instead of `printf "%s"`. He notes this pattern appears in multiple places introduced by commit f6a2efdc9b, suggesting `echo` would be more straightforward and less error-prone for these cases.

The thread's core technical issues (Solaris sed compatibility problems) were already resolved by Junio's earlier patch, so this email serves as a stylistic commentary on the solution's implementation. Eric's observation doesn't challenge the correctness of the fix but highlights an opportunity for more idiomatic shell scripting. The discussion remains constructive, maintaining the thread's consensus around portable solutions while gently suggesting cleaner code patterns.