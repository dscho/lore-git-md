This is a platform compatibility discussion about FreeBSD support in Git's build system, specifically regarding `memmem()` usage and version requirements.

Brian m. carlson weighs in on the ongoing discussion about which FreeBSD versions should use the system's `memmem()` implementation. They note that FreeBSD 11.4 (the last of the 11.x series) has been end-of-life since 2021, and even earlier versions (back to 11.0) had functional though slow implementations. The argument is that users running obsolete, unsupported FreeBSD versions should expect to need manual configuration tweaks.

The email strongly supports dropping FreeBSD 4.x support entirely, noting its lack of C99 compliance and missing POSIX features like `statvfs`, plus its incompatibility with modern AMD64 hardware. This aligns with Carlo Arenas' earlier observations about the system's limitations.

Key technical points include the timeline of FreeBSD version support (11.4 EOL in 2021, 10.4 in 2018) and the practical constraints of FreeBSD 4.x. The discussion appears to be moving toward a consensus that newer FreeBSD versions (likely 12+) could safely use system `memmem()`, while older versions would either use Git's implementation or require manual configuration.

The email doesn't introduce new technical arguments but reinforces the case for modernizing FreeBSD version requirements, particularly by highlighting the security implications of running EOL systems. This moves the thread closer to resolution by providing additional justification for dropping legacy support.