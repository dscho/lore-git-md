This is a feature patch in the `imap-send` series, adding command-line override capability for the IMAP folder destination. It introduces a new `--folder`/`-f` option to `git imap-send`, allowing users to specify the target folder at runtime rather than relying solely on the `imap.folder` config setting. The implementation includes documentation updates in both the config and command manpages, plus the core logic to override the configured folder when the option is provided. The patch maintains backward compatibility by falling back to the config value when no command-line folder is specified.

The change is straightforward and well-contained, touching only the expected files (documentation and imap-send.c). It follows the series' pattern of enhancing `imap-send` usability while maintaining existing behavior. The documentation clearly explains the precedence rules between config and command-line settings, and the code properly handles memory management when overriding the folder value.

This patch fits naturally into the broader series by completing the folder management improvements alongside the new `--list` functionality. It addresses a practical workflow need where users might regularly send to different folders without wanting to modify their config. The implementation is clean and appears ready for inclusion, with no obvious technical concerns.