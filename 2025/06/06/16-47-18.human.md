This is a discussion about test coverage for a memory leak fix in the revision walking code. Junio Hamano responds to Lidong Yan's proposed test case for verifying the fix in `git show --merge` behavior during merge conflicts.

Junio acknowledges that his earlier suggested test (which directly manipulated index files) was intentionally minimal just to trigger the leaky code path, while Lidong's new proposal creates a more realistic merge conflict scenario through actual branch divergence and a failed merge. The exchange shows the project's typical progression from minimal verification to more comprehensive testing.

The test case in question creates two branches ("hello" and "goodbye") that modify the same file differently, then attempts a merge that will fail, putting Git into a conflicted state where `git show --merge` can demonstrate the fixed behavior. This represents a more natural workflow than Junio's original direct file manipulation approach.

The discussion highlights Git's philosophy of preferring realistic test scenarios over artificial ones when possible, while still valuing minimal tests for quick verification of specific behaviors. The thread remains open-ended, with no final decision yet on which test approach will be adopted.