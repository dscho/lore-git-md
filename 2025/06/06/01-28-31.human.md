This is a follow-up bugfix in the pack-bitmap memory leak series, now being reviewed by Junio Hamano. The patch addresses a remaining issue where `roots_bitmap` was only being conditionally freed in `find_boundary_objects()` when `cascade_pseudo_merges_1()` failed, despite the bitmap being used as a mutable parameter that doesn't take ownership.

Junio notes some grammatical issues in the commit message that need fixing - specifically the explanation about `cascade_pseudo_merges_1()`'s behavior with `roots_bitmap` is unclear and contains incorrect verb conjugation. The technical content appears sound though, continuing the series' work to ensure proper cleanup of bitmap resources.

This appears to be the final polish on a well-reviewed series that has already been merged to master (commit 11d45a6e) with comprehensive test coverage in t5333. The changes maintain the established pattern of unconditional `bitmap_free()` calls while fixing an oversight in the original implementation. No new test cases are needed here since the existing ASAN-based tests already verify the leak is fixed.