Style and error message cleanup for `imap-send`. This patch is part of the larger v14 series improving IMAP functionality, focusing on polishing error messages and output formatting in the `imap-send` code. The changes standardize capitalization (making "IMAP" and "OK" consistently uppercase), ensure error strings start lowercase and end with periods, and improve some message clarity (like specifying "CRAM-MD5" in an error). While minor, these changes follow the project's style guidelines and improve user-facing output. The patch touches various parts of the IMAP implementation but doesn't change any functionality - it's the final polish before the series gets merged.