This is a discussion about the OAuth2.0 authentication implementation in `imap-send`, specifically focusing on the password handling logic for libcurl. The email continues the thread examining whether the current approach to setting `CURLOPT_PASSWORD` and `CURLOPT_LOGIN_OPTIONS` is optimal.

Junio Hamano questions why the code checks for non-OAuth methods rather than explicitly checking for PLAIN/LOGIN when deciding to set passwords. He suggests the logic appears backwards and proposes an alternative implementation that would explicitly list the password-using methods. This relates to the v13 fix that corrected the auth method comparison operator from `||` to `&&`.

Aditya Garg explains the conservative approach was taken to avoid potential regressions, since the original code unconditionally set these options even for methods like CRAM-MD5. He acknowledges the implementation could be clearer but prioritized stability given the lack of test servers for some auth methods. Junio emphasizes the importance of documenting this rationale thoroughly in commit messages to help future maintainers.

The discussion reveals subtle aspects of libcurl's authentication handling:
- `CURLOPT_PASSWORD` appears to be used for PLAIN/LOGIN methods
- `CURLOPT_LOGIN_OPTIONS` may be PLAIN-specific despite ambiguous docs
- OAuth methods use completely different APIs (`CURLOPT_XOAUTH2_BEARER`)

Junio suggests the current implementation, while functionally correct after the v13 fix, could be made more maintainable by either:
1. Explicitly listing supported password methods (PLAIN/LOGIN)
2. Adding warnings for unsupported methods
3. Better documenting the conservative regression-avoidance approach

This exchange highlights ongoing design considerations around authentication method handling in `imap-send`, even after the critical OAuth logic fix was implemented in v13. The discussion doesn't reveal new functional issues but focuses on code organization and documentation clarity for future maintenance.