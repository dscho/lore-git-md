This thread addresses a byte-order handling issue in `compat/bswap.h` where `ntohl`/`htonl` macros were being incorrectly redefined on big-endian systems after commit 6547d1c9cb introduced built-in bswap support. The patch reorganizes the macro definitions to ensure these conversions only get replaced with byte-swapping operations on little-endian systems where bswap functions are available, while maintaining them as no-ops on big-endian systems.

The change moves 21 lines of macro definitions and adds 16 lines with more precise conditional logic, maintaining the same behavior but fixing the architecture-specific handling. The author has verified successful builds and test suite runs on both s390x (big-endian) and x86-64 (little-endian) platforms, confirming the fix works across different CPU architectures. This appears to be a straightforward correctness fix with no outstanding questions or objections noted.