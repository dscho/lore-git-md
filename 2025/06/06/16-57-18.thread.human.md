This thread focuses on refining byte-order handling in `compat/bswap.h`, originally prompted by Sebastian Siewior's fix for big-endian system issues. The core problem involves macro redefinitions that broke s390x tests, with the solution being a reorganization of the macros to properly account for endianness. Junio Hamano's review highlighted three key points for clarification in the commit message and noted an asymmetry in how 32-bit and 64-bit macros are handled.

The latest exchange digs deeper into this asymmetry. Junio explains that while 64-bit macros (`ntohll`/`htonll`) are always redefined (either as swaps or identity operations), the 32-bit versions (`ntohl`/`htonl`) only get redefined when `bswap32` is available, with no fallback equivalent to `default_bswap64()`. Sebastian is now considering whether to adjust the 64-bit behavior to match the 32-bit pattern or simply document the current implementation. Junio emphasizes that either approach would be acceptable as long as the behavior is clearly documented, particularly regarding `bswap32`/`bswap64` being internal implementation details.

The discussion remains technical and productive, with both participants carefully analyzing the implications. While the immediate focus is on improving the current patch, the broader question of whether `compat/bswap.h` could eventually be removed in favor of system-provided functions remains open. The thread shows good engagement with review feedback and a methodical approach to platform-specific edge cases.