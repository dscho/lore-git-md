This is a technical discussion about byte-order macro handling in `compat/bswap.h`, continuing the refactoring effort to fix big-endian system builds. Junio Hamano clarifies an asymmetry in the current implementation: while 64-bit macros (`ntohll`/`htonll`) are always redefined (as identity on big-endian), the 32-bit versions (`ntohl`/`htonl`) only get redefined when `bswap32` is available. 

The exchange reveals Sebastian initially missed this distinction in the patch's behavior. Junio notes this is an intentional but perhaps surprising design choice due to the lack of a `default_bswap32()` equivalent to the 64-bit case. Both maintainers are now considering whether to document this behavior more clearly or potentially standardize the approach between 32-bit and 64-bit macros.

The discussion remains focused on implementation details rather than proposing concrete changes, with Junio expressing openness to either keeping the current asymmetry (with better documentation) or aligning the 32/64-bit handling. This appears to be a careful, low-stakes maintenance discussion between experienced contributors who are methodically considering edge cases in platform-specific byte-order handling.