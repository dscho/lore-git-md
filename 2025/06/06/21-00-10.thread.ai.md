Thread identity: RFC proposal for `rebase--`, a new tool to improve conflict resolution during non-interactive rebases involving merge commits, authored by an external contributor (GitHub username not specified in summary). Thread type is a technical proposal for a complementary tool to `git rebase`.

Core problem or goal: Address limitations in Git's standard rebase when handling merge commits by reusing information from original merges to automatically resolve conflicts where possible, particularly in large repositories like the Linux kernel. The tool aims to produce identical rebased versions without user intervention for simple cases.

Current status: Initial prototype implemented in Python using pygit2, demonstrated to successfully rebase some Linux kernel commits where standard rebase fails. Currently operates in-memory without working tree integration, handling only whole-blob resolutions (not partial merges). Performance shown at under 0.4s for example cases.

Key technical details: Works by analyzing original merge commits to reuse existing objects (trees/blobs) during conflict resolution. Implementation approach is complementary to rather than replacing `git rebase`, specifically targeting non-interactive rebases with merges. Uses pygit2 library for Git object manipulation.

Open questions and loose ends: Need to implement blob merging capabilities (currently only whole-blob resolution). Working tree integration not yet implemented. Community feedback requested on approach and potential integration path. Performance characteristics on more complex cases unknown.

Participant positions: Only author contribution so far (initial proposal and prototype). No reviews or feedback yet as this is the thread root.

Related work: Connects to long-standing discussions about improving rebase's merge handling. Implementation approach differs from core Git's rebase machinery by being an external Python tool rather than C implementation. Uses pygit2 (libgit2 bindings) rather than direct object manipulation.