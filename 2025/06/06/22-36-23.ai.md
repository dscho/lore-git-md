This is a technical discussion about byte-order macro handling in `compat/bswap.h`, continuing the refactoring effort to fix big-endian system issues. Junio Hamano clarifies the asymmetry in how 32-bit (`ntohl`/`htonl`) and 64-bit (`ntohll`/`htonll`) macros are handled - the 32-bit versions are only redefined when `bswap32` is available, while the 64-bit versions are always redefined (either as `bswap64` or identity). 

The email reveals an implementation detail that `default_bswap32()` doesn't exist as a fallback like `default_bswap64()` does, explaining the conditional behavior. Sebastian Siewior asks whether to proceed with just improving the commit message or also adjusting the 64-bit macro definitions to match the 32-bit pattern. Junio expresses no strong preference but emphasizes the need for clear documentation about `bswap32`/`bswap64` being internal implementation details.

The discussion remains focused on platform-specific byte-order handling mechanics, with both participants carefully analyzing the implications of macro definition patterns. The thread continues to explore whether to maintain the current asymmetry or make the behavior more consistent between 32-bit and 64-bit cases, while also considering the broader question of whether `compat/bswap.h` could be eliminated entirely in favor of system-provided functions.