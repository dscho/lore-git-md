This is a follow-up in the thread about refactoring byte-order handling in `compat/bswap.h`. Sebastian Siewior responds to Junio Hamano's review of the previous patch, addressing several points:

1. **Commit Message Improvements**: Sebastian acknowledges Junio's request for a more detailed commit message that clearly states the problem (broken s390x tests due to incorrect macro redefinitions on big-endian systems), the faulty assumption (bswap macros being little-endian-only), and the solution (reordering definitions after byte-order detection).

2. **Implementation Oddity Discussion**: The thread reveals an asymmetry in the current implementation - while 64-bit macros (`ntohll`/`htonll`) are unconditionally redefined as identity functions on big-endian systems, the 32-bit versions (`ntohl`/`htonl`) only get redefined when `bswap32` exists. Sebastian asks whether Junio wants this addressed in a follow-up patch.

3. **Potential Future Work**: Sebastian suggests the file might be removable since `ntohl()` appears to be properly handled by glibc's built-in `__bswap_32`, though notes `ntohll()` still needs manual handling as it's non-standard.

The email shows good engagement with reviewer feedback while raising new technical considerations about the implementation's symmetry and potential for further simplification. The discussion remains focused on the byte-order handling mechanics in `compat/bswap.h`, with both parties carefully analyzing the macro definition behavior across platforms.