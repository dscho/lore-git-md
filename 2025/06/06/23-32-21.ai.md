This is a technical discussion about submodule remote handling and the `the_repository` removal effort, with Junio Hamano weighing in on Jacob Keller's investigation.

The email focuses on the challenge of making deprecated functions in `remote.c` (`read_remotes_file` and `read_branches_file`) properly repository-aware before their planned removal in Git 3.0. Jacob had asked whether patches to modify these functions to take repository pointers would be acceptable, given they currently incorrectly assume `the_repository` despite being called from code that already has repository context.

Junio strongly endorses this direction, noting two key points: (1) fixing such code paths that lose repository context is generally welcomed even without specific use cases, and (2) Jacob's concrete submodule use case makes this change particularly valuable. Junio explicitly seeks Patrick Steinhardt's opinion, suggesting this may affect areas Patrick maintains.

The discussion continues to balance immediate submodule functionality needs (where `clone.defaultRemoteName` configuration currently breaks submodule operations) with broader architectural evolution (proper repository parameter passing and deprecated function cleanup). The technical focus has shifted from the original submodule issue to deeper questions about how to modernize remote handling code in a way that serves both current needs and future removal plans.