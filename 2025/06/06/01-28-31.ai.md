This is a follow-up bugfix in the pack-bitmap memory leak series, addressing a minor grammatical issue in the commit message. The patch itself is already merged (commit 11d45a6e) and corrects the email is just pointing out a wording nit in the original submission.

The technical content remains unchanged from the v5 series that fixed memory leaks in `find_boundary_objects()` by making `roots_bitmap` cleanup unconditional via `bitmap_free()`. The fix handles cases where `cascade_pseudo_merges_1()` succeeds but still requires cleanup, with comprehensive test validation in t5333 demonstrating leak detection while maintaining shell portability.

Junio Hamano notes the commit message has awkward phrasing around the ownership semantics of `roots_bitmap` in `cascade_pseudo_merges_1()`. The intended meaning is that since the function uses the bitmap as a mutable reference without taking ownership, the caller remains responsible for cleanup regardless of success/failure. This is purely a documentation clarification - the actual code change and its memory safety guarantees remain correct as merged.

The thread shows the patch series is now complete with all feedback addressed, including this final wording polish. The established pattern of ASAN-based leak detection in tests and the unconditional cleanup approach serve as a reference for similar cases in the pack-bitmap subsystem.