Thread identity: Bug report about Git branch deletion behavior on Windows, reported by Ondra Medek. The thread concerns a specific failure mode where `git push origin :branch1` successfully deletes the remote branch but then incorrectly reports "remote ref does not exist" error.

Core problem: When deleting remote refs (both branches and custom refs) from a particular local repository clone on Windows (Git 2.49.0), the operation succeeds but reports failure. The issue appears to be repository-specific as fresh clones work correctly, suggesting local repository corruption rather than a general Windows bug.

Current status: No resolution yet. The reporter has tried `git gc --aggressive --prune` without success. The only known workaround is to create a fresh clone.

Key technical details:
- Affects both branches and custom refs, suggesting ref handling machinery issue
- Occurs specifically on Windows with Git 2.49.0
- Problem persists after aggressive garbage collection
- Fresh clones work correctly, pointing to local state corruption

Open questions:
- Is this a known bug?
- Are there repair options beyond recloning?
- What specific repository state causes this behavior?
- Does this affect other Windows Git versions?

Participant positions:
- Ondra Medek reported the issue and provided reproduction details
- No other participants have responded yet

Related work:
- May be connected to general ref handling issues on Windows
- Could relate to repository corruption patterns seen in other contexts