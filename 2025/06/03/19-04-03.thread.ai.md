Since this is spam with no technical content, we should not incorporate it into the thread summary at all. The existing AI thread summary (which appears to be empty/nonexistent since this is the first message) should remain unchanged.

For AI mode, we'll maintain an empty summary since this message contains nothing relevant to Git development. The proper handling is to silently discard spam messages from the summary tracking.