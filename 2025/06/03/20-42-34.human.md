This is a review comment on the first patch of a two-part series adding GPG signing support to `git subtree`. The reviewer, D. Ben Knoble, points out that while the commit message mentions `-S/--gpg-sign` (which will be added in patch 2/2), that flag isn't actually implemented in this preparatory refactoring patch. 

The reviewer suggests clarifying the commit message to explicitly state that this change to use `--stuck-long` parsing is groundwork for future flags with optional parameters (like `-S`), rather than implying the flag already exists. The patch itself is a mechanical conversion to long-option-with-equals style parsing (`--option=value`), removing redundant `shift` operations and making the code more consistent with Git's standard argument handling.

This appears to be a constructive review from someone familiar with the codebase, catching a minor documentation mismatch in an otherwise straightforward preparatory change. The feedback is focused and actionable, suggesting a small wording improvement while clearly understanding the technical context of the series.