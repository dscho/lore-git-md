Bugfix and test refinement for pack-bitmap boundary traversal. This is a follow-up to the v4 series addressing memory leaks in bitmap cleanup, specifically responding to Junio Hamano's observation about test environment variable handling.

The email confirms Junio's concern that the test case wasn't properly exercising the `GIT_TEST_PACK_USE_BITMAP_BOUNDARY_TRAVERSAL` environment variable since it wasn't being exported to child processes. Lidong Yan acknowledges this oversight and proposes moving the environment variable directly in front of the `git rev-list` command that needs it, ensuring the boundary traversal path is properly tested.

This completes the final verification step for the series, which already included:
- Unconditional `bitmap_free()` calls to fix memory leaks
- Pseudo-merge configuration in tests (`bitmapPseudoMerge.test.*`)
- Commit counting verification between HEAD~1 and HEAD

The test infrastructure now correctly validates the boundary object traversal path in pack-bitmap.c's `find_boundary_objects()` function, with all shell portability concerns (previously raised by Eric Sunshine) and environment handling issues resolved. The series has received technical approval from Patrick Steinhardt and now satisfies Junio's verification requirements regarding test coverage.