Compiler warning fix targeting parse-options.c. This patch addresses a GCC `-Wmaybe-uninitialized` warning that appears at `-O3` optimization level, where the compiler warns that the `arg` variable in `do_get_value()` could be used uninitialized. The warning occurs through the `error()` macro expansion path.

The fix is straightforward - initializes `arg` to NULL where it's declared. This silences the warning while maintaining the same behavior, since the function already handles NULL values properly in its logic. The change is minimal (one line modified) and low-risk, touching only the internal option parsing machinery.

The patch appears well-justified - compiler warnings at high optimization levels often reveal subtle code paths that static analysis can't prove are safe, even when human analysis shows they are. Initializing the variable is a defensive programming practice that makes the code more robust against future changes while eliminating the warning.

For future thread context: this touches the option parsing subsystem's internal `do_get_value()` helper function, initializing a `const char *arg` parameter that gets passed to various option handlers. The warning appeared under GCC with `-O3` but not at lower optimization levels, suggesting it's a false positive that the patch works around.