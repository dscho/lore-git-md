This is a refactoring patch in Patrick Steinhardt's maintenance task execution series (v4 02/12). It cleans up redundant quiet flag handling in `git gc` by removing a local `quiet` variable in favor of using the `quiet` field from the `maintenance_run_opts` struct consistently throughout the command.

The change is straightforward - there were two variables tracking the quiet state (one local, one in the options struct) which could cause confusion. The patch standardizes on using the options struct version, updating all references to the local variable. This is a small but meaningful improvement to code clarity as part of the larger maintenance refactoring effort.

The diff shows mechanical changes across several locations where the quiet flag affects behavior - during repository packing, progress reporting, and commit graph writing. The patch maintains all existing functionality while removing the potential for inconsistency between the two quiet variables.

This appears to be a well-contained cleanup that fits naturally into the series' broader goals of improving maintenance task code quality and reducing confusing patterns. The change is small but contributes to the systematic refactoring approach seen throughout the series.