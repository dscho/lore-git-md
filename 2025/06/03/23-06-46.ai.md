Platform compatibility fix addressing a clang warning on Windows. This is the final patch in a 4-part series (no cover letter seen).

The patch modifies `refs/files-backend.c` to work around a clang compiler warning about unreachable code when building on Windows. The warning occurs in the symlink reference creation path where clang incorrectly determines the `continue` statement can never be reached. The fix wraps the condition in `NOT_CONSTANT()` to prevent the compiler's static analysis from making this determination.

The change is minimal (a single line modification) and appears to be a straightforward workaround for a compiler quirk rather than indicating any deeper logic issue. The patch maintains the existing behavior while silencing the warning that would otherwise break the build with `-Werror`. As the last in a series, it likely builds on prior Windows-specific fixes, though the exact relationship isn't clear without seeing the earlier patches.