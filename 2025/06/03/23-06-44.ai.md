Compiler warning fix for reftable/basics.c, addressing a use-after-free warning in the `parse_names()` function. This is patch 2/4 in a series (no cover letter seen yet).

The change fixes a GCC warning that triggers at `-O3` optimization level, where the compiler incorrectly suggests `names[i]` could be used after being freed in the error cleanup path of `parse_names()`. The warning appears due to inlining of `reftable_free()` through multiple layers of helper functions. The patch makes the cleanup code more defensive by first checking if `names` is non-NULL before attempting to free its elements.

The fix touches the reftable subsystem's basic utilities, specifically the memory management helpers. The warning appears to be a false positive since the code already properly guards against NULL values in `reftable_free()`, but the additional NULL check makes the control flow more explicit to the compiler. The change follows a similar pattern to the first patch in the series (which addressed a different warning in parse-options.c), suggesting this series may be systematically addressing compiler warnings at high optimization levels.

Key technical details:
- Modifies error handling in `parse_names()` in reftable/basics.c
- Adds NULL check before cleanup loop
- Maintains same behavior for valid cases
- Changes memory management code paths in reftable utilities
- Follows same pattern as previous patch (v1) in series

The patch appears straightforward and low-risk, though it would be helpful to see the series cover letter to understand the broader context of these warning fixes. The change is small (8 lines modified) and focused purely on silencing the compiler while maintaining existing functionality.