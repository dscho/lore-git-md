This is the final approval in a thread about test infrastructure path resolution. Junio Hamano merges Mark Mentovai's patch to normalize test working directories, closing the discussion about terminology.

The patch fixes test failures by ensuring tests run from symlink-resolved paths, particularly affecting macOS and Windows maintenance tests. The thread saw extensive debate over how to describe this change, with proposals like "absolute pathname" being rejected as inaccurate since the paths were absolute already - the key change was symlink resolution.

Hamano acknowledges the terminology challenges but approves the original "normalized working directory" commit subject as the least misleading option, despite its potential ambiguity. He notes alternatives like "symlink-resolved file path" would be more precise but too verbose. The implementation uses `cd -P` behavior to resolve paths in `t/test-lib.sh`, affecting both `$TRASH_DIRECTORY` and `$HOME`.

This concludes the thread with the technical solution merged and documentation using "physical path" terminology while keeping the original commit message for precision. The change is minimal (2 lines) but has broad impact across the test suite, particularly for platforms with different symlink handling behaviors.