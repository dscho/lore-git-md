This is part 6 of Patrick Steinhardt's 12-patch series refactoring Git's maintenance task execution to resolve race conditions. The series is in its fourth iteration and has seen extensive review.

The current patch extracts the task execution logic from `maintenance_run_tasks()` into a new `maybe_run_task()` helper function. This is a straightforward refactoring that:
- Moves the auto-condition check, task execution, and trace2 instrumentation into a reusable function
- Maintains identical behavior while making the code more modular
- Prepares for future changes where this logic will need to be called from multiple places when maintenance tasks are split between foreground and background phases

The change is mechanical and well-contained, touching only `builtin/gc.c`. It builds on previous patches in the series that established the task array structure and centralized task selection. The commit message clearly states this is groundwork for upcoming changes to split task execution phases.

Given the series' progression and the focused nature of this change, it appears to be a solid incremental step in the larger refactoring effort. The patch follows the established pattern of small, well-explained changes that have characterized this series so far.