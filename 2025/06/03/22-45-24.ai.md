Platform compatibility issue affecting NonStop builds during Git v2.50.0-rc1 testing. Randall S. Becker reports a build failure when the Makefile attempts to generate 'lib/tclIndex' on NonStop systems where TCL is unavailable. The error occurs in the test phase when running generate-tclindex.sh, which fails due to missing required arguments (BUILD_DIR, BUILD_OPTIONS, LIBFILE).

This appears to be a regression in the build system's handling of TCL dependencies, specifically around the git-gui components. The error suggests the build process isn't properly accounting for platforms where TCL isn't present, unlike previous versions that presumably handled this case more gracefully. The reporter provides detailed error output showing the script invocation with insufficient parameters.

The issue is specific to line 200 of the Makefile and involves the interaction between generate-tclindex.sh and GIT-GUI-BUILD-OPTIONS. This follows a pattern where platform-specific build issues often surface during RC testing, though NonStop represents a less common platform configuration. The reporter indicates this isn't just a missing dependency but a legitimate build system problem that needs addressing.

This will likely require maintainer attention to either make TCL properly optional or implement correct platform detection in the build system. The error suggests the build process may need adjustments to either skip TCL-related targets when TCL is unavailable or provide proper fallback behavior. The issue could potentially affect other non-TCL platforms as well.