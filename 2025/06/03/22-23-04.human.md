Refactoring and bugfix for MIDX pack name access in chained multi-pack-index scenarios. This patch introduces a new helper function `nth_midxed_pack_name()` to safely access pack names across MIDX layers, where global and local pack IDs may differ. The change fixes actual bugs in the pack-bitmap.c code that could lead to out-of-bounds array accesses in chained MIDX cases.

Taylor Blau responds to Junio Hamano's question about direct `pack_names[]` array access, explaining that while most external callers should use the new helper, there are a few cases in midx-write.c where direct access remains more convenient. The patch is part of an ongoing effort to make MIDX chain handling more robust, building on previous performance optimizations and correctness improvements in this thread.

The change is relatively small (15 lines net) but addresses a subtle correctness issue, particularly in bitmap handling code. It follows the pattern of careful API evolution seen throughout this thread, introducing proper encapsulation while recognizing practical constraints in the implementation. The discussion continues to balance safety improvements against code clarity and convenience.