This is a platform compatibility discussion about `-Wunreachable-code` warnings in the build system. The thread context shows this is a follow-up to earlier CI build failures when enabling this compiler warning.

Mike Hommey points out another instance where `-Wunreachable-code` triggers on Windows builds, similar to the `sigfillset()` case Jeff King previously identified. The issue occurs in `refs/files-backend.c` where `create_ref_symlink()` is defined as always returning -1 on Windows (via `NO_SYMLINK_HEAD`), making the `if` condition's body unreachable. This mirrors the earlier case where platform-specific implementations made error handling code appear unreachable to the compiler.

The discussion reveals a tension between maintaining portable error checking (as required by POSIX) and avoiding compiler warnings from platform-specific optimizations. Both cases show how `-Wunreachable-code` exposes platform divergence in what should be standard error handling paths. The thread suggests this warning may need platform-specific suppression or code restructuring to accommodate different implementations while maintaining correctness.