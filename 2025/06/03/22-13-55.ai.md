This is a documentation patch in a bugfix thread about bitmap memory leaks. The email is a review comment on patch 2/3 of the series, which rewrites comments in `test_bitmap_commits()` to clarify bitmap lookup behavior.

Taylor Blau provides minor editorial feedback on the comment wording, suggesting "bitmapped commits" or "set of commits which have bitmaps" would be clearer than "bitmap selected commits". He notes this isn't significant enough to warrant a reroll of the series, but leaves that decision to others.

The original patch from Lidong Yan corrected misleading comments that suggested the commit table could be skipped entirely, when in fact bitmap entries must still be read to obtain commit IDs for output. The new comments clarify that while the function bypasses the commit lookup table (if present), it forces eager loading of bitmap entries.

This is a purely textual change that serves as preparatory work for the test infrastructure improvements in the subsequent patch. The thread context shows this is part of a well-reviewed series addressing bitmap memory leaks, with all technical issues already resolved and the series likely ready for merging.