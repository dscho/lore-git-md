This is a platform compatibility discussion about BSD header handling in `compat/posix.h`. The thread has progressed from the initial technical fix to a more nuanced discussion about whether to retain the `_NETBSD_SOURCE` macro definition for backward compatibility, even though it's now redundant after removing `_XOPEN_SOURCE`.

Collin Funk weighs in with new information from Autoconf's documentation, suggesting the macro might still be useful for older systems or future standards adoption. He provides a concrete example of how POSIX.1-2024 features are still hidden behind `_GNU_SOURCE` in glibc, implying similar cases might exist for NetBSD extensions. This moves the discussion beyond just "harmless to keep" to a more proactive argument about potential future compatibility benefits.

The patch itself (which standardizes header handling across BSD variants to fix `getdelim()` warnings) has already been tested and reviewed, so this final point about the macro is the only remaining decision before integration. The ball is in the maintainer's court to decide whether to accept Brad Smith's original removal or follow Collin's suggestion to keep `_NETBSD_SOURCE` as a precaution.