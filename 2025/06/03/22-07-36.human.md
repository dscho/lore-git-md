This is a follow-up in the ongoing discussion about `-Wunreachable-code` warnings in the Git codebase, specifically regarding platform-specific code paths. 

Junio Hamano responds to Mike Hommey's report of another case where the warning appears - this time in `refs/files-backend.c` where `create_ref_symlink()` always returns -1 on Windows (via `NO_SYMLINK_HEAD` define). This mirrors an earlier issue Jeff King found with `sigfillset()` platform divergence.

Junio suggests applying the same solution used previously - the `NOT_CONSTANT()` trick - to suppress the warning for this case. The thread shows the project carefully balancing between portable error checking and platform-specific optimizations while maintaining clean compiler output. 

This is part of the broader effort to enable `-Wunreachable-code` warnings in development builds while handling platform-specific edge cases. The discussion remains technical and solution-focused, building on prior work to maintain consistency in warning suppression approaches.