This is a documentation refinement in Patrick Steinhardt's v3 maintenance refactoring series (patch 10/12). The email continues the thread discussing error handling standardization via the new `die(NULL)` pattern, specifically focusing on improving the commit message wording.

The discussion originated from Kristoffer Haugsbakk's suggestion to change "open code the code" to "open-code the code" in the commit message, which Karthik Nayak agreed improved readability. Patrick now proposes an alternative phrasing that avoids the "open-code" terminology entirely, instead emphasizing the practical benefits of the change (avoiding magic numbers and error-prone `exit(128)` calls).

The proposed rewording maintains the technical accuracy while improving clarity, showing the patch's motivation more directly. This is a minor but thoughtful refinement to documentation that has already been through several iterations of review. The change doesn't affect the implementation, which standardizes error handling across multiple builtins by allowing `die(NULL)` to exit cleanly after custom error reporting.

This email represents the final polish on a well-reviewed patch that systematically eliminates direct `exit(128)` calls in favor of Git's standard error handling mechanisms. The discussion demonstrates careful attention to both technical correctness and clear communication in commit messages.