This is a follow-up in the platform-specific `-Wunreachable-code` warnings thread, specifically addressing Windows build issues.

Mike Hommey confirms that Junio Hamano's suggested solution works for suppressing `-Wunreachable-code` warnings in the Windows-specific case of `create_ref_symlink()`. The issue stems from `NO_SYMLINK_HEAD` being defined on Windows, which makes the function always return -1, creating dead code that triggers compiler warnings. The fix applies the `NOT_CONSTANT()` pattern previously used for similar cases like `sigfillset()`, maintaining the code's portability while silencing the warning.

This appears to be a straightforward technical resolution to a known build system quirk, continuing Git's pattern of handling platform-specific compiler warnings pragmatically. The discussion remains focused and uncontroversial, with the maintainer's suggested approach proving effective.