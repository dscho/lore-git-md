Refactoring patch continuing the effort to move `core.sparsecheckout` configuration from global variable to `repo_settings` struct, addressing review feedback on naming and initialization safety.

Ayush Chandekar responds to Patrick Steinhardt's review of their initial patch, agreeing that the "apply" in `core_apply_sparse_checkout` may be unnecessary and proposing to rename it to `sparse_checkout` for clarity. The discussion highlights two key technical issues: whether the getter/setter functions (`repo_settings_get_apply_sparse_checkout` and `repo_settings_set_apply_sparse_checkout`) provide sufficient value to justify their existence, and the need to ensure proper initialization via `prepare_repo_settings()` before accessing the configuration.

Chandekar suggests that if initialization is handled within the accessors, this might strengthen the case for keeping them rather than exposing direct struct field access. The email doesn't yet propose concrete changes but shows the author considering the tradeoffs between direct field manipulation versus encapsulated access with proper safety guarantees. This continues the thread's focus on eliminating global state while maintaining thread safety and proper initialization order.