This is a v4 patch series that finalizes two changes related to BUG() message standardization and sequencer validation, following maintainer feedback on the v3 series. The first patch updates the BUG() message in sequencer.c's `update_squash_messages()` to be more explicit about expected command types (FIXUP/SQUASH) rather than just saying "unknown command", as suggested by Junio C Hamano. The change maintains the same validation logic using `is_fixup()` but improves the diagnostic clarity for developers.

The second patch completes the mechanical conversion of remaining `BUG(_("..."))` instances to `BUG("...")` across builtin/mktag.c, builtin/worktree.c, and pack-bitmap-write.c, enforcing the project-wide standard that BUG() messages (being developer-facing) should not be translated. This was the last remaining piece of the BUG() standardization effort discussed in the thread.

The range-diff shows minimal changes from v3 - just the improved BUG() message wording in the sequencer patch and some commit message cleanup. The series appears ready for merging, having addressed all previous feedback and maintaining consensus on both the specific sequencer fix and broader BUG() message policy. The changes are straightforward with no behavior impact beyond improved error messaging.