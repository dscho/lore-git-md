Bugfix addressing a memory leak in pack-bitmap.c's boundary traversal logic, now with updated test handling for environment variables.

This v4 iteration from Lidong Yan finalizes the fix for a memory leak in `find_boundary_objects()` where `roots_bitmap` wasn't being freed when `cascade_pseudo_merges_1()` succeeded. The core change remains the unconditional `bitmap_free(roots_bitmap)` call, but the test implementation has been revised in response to Junio's feedback about environment variable handling.

Key technical details in this version:
- Maintains the unconditional `bitmap_free(roots_bitmap)` cleanup
- Modifies the test in t5333 to properly propagate `GIT_TEST_PACK_USE_BITMAP_BOUNDARY_TRAVERSAL` by moving it into the command invocation rather than setting it separately
- Keeps the same test scenario (pseudo-merge config with commits A and B) to verify the leak is fixed
- Resolves the last open question from Junio about environment variable propagation

The series appears technically complete with all prior feedback addressed. The test now properly exercises the boundary traversal path while maintaining shell portability. This should be ready for final review and integration into the next cycle.