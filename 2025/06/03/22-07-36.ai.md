This is a follow-up in the ongoing discussion about `-Wunreachable-code` warnings, specifically addressing platform-specific code paths in the refs backend.

Junio Hamano responds to Mike Hommey's report of another instance where `-Wunreachable-code` triggers on Windows due to `NO_SYMLINK_HEAD` making `create_ref_symlink()` always return -1. The code in `refs/files-backend.c` shows a conditional that becomes unreachable on Windows platforms. 

Junio suggests applying the same `NOT_CONSTANT()` workaround previously used for the `sigfillset()` case, which involved wrapping platform-specific constants to prevent the compiler from optimizing away code paths. This continues the pattern of platform-specific warning suppression while maintaining portable error checking.

The exchange highlights the tension between clean cross-platform code and compiler warnings, with Windows-specific limitations (`NO_SYMLINK_HEAD`) creating dead code that triggers warnings when `-Wunreachable-code` is enabled. The discussion remains focused on practical solutions rather than architectural changes to the symlink handling.