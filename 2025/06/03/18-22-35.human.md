This is the final approval in a thread about test infrastructure path handling, specifically normalizing working directories in Git's test suite to handle symlinks consistently.

Junio Hamano gives the green light to merge Mark Mentovai's patch with its original "normalized working directory" commit subject, rejecting Torsten Bögershausen's suggestion to use "absolute pathname" instead. The discussion had explored various terminology options ("physical path", "canonical path") but ultimately settled on keeping the original wording as most accurate, since the key issue was symlink resolution rather than making paths absolute (which was already the case).

The patch itself is a 2-line change to `t/test-lib.sh` that normalizes `$TRASH_DIRECTORY` and `$HOME` paths to match `cd -P` behavior, fixing test failures on macOS when run from symlink-containing directories. While small, the change has broad impact across the test suite by ensuring consistent path handling.

This concludes a thorough discussion that balanced technical precision with documentation clarity, preserving detailed explanations in the commit message while using "physical path" terminology in the docs. The patch will now proceed through the normal integration process in the 'next' branch.