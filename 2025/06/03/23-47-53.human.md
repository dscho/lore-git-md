This is a bugfix patch addressing a compiler warning in the reftable subsystem. It's part 2 of a 4-part series tackling `-O3` optimization warnings, though no cover letter has been seen yet to explain the full scope.

The patch fixes a use-after-free warning from GCC in `parse_names()` where the compiler couldn't prove that `names` wasn't NULL before the cleanup loop. The change adds a NULL check around the cleanup code, making the control flow more explicit to the compiler while maintaining identical behavior. The warning only appears at high optimization levels (-O3) and the fix follows the same conservative pattern as the first patch in the series (which addressed parse-options.c).

The change is minimal (8 lines modified) and targets an internal utility function in the reftable code. It builds on existing NULL-handling patterns in the reftable subsystem's memory management helpers. The patch appears well-contained and unlikely to conflict with other work, though we haven't yet seen the full series to understand if there are more related fixes coming.