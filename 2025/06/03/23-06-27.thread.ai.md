Thread identity: Bug report and feature proposal regarding submodule behavior when `clone.defaultRemoteName` differs from "origin", reported by Jacob Keller, with Junio Hamano now participating.

Core problem: Submodule operations fail when `clone.defaultRemoteName` is configured to something other than "origin" due to `repo_get_default_remote` in `builtin/submodule--helper.c` hardcoding "origin". The discussion has expanded to consider both configuration flexibility and implementation challenges, particularly around using superproject URLs directly versus remote naming.

Current status: Jacob Keller has identified concrete obstacles in using superproject `.gitmodules` URLs directly, finding that `remote.c` functions like `read_remotes_file` and `read_branches_file` (deprecated but still used) hardcode `the_repository` despite claiming repository-agnostic behavior. This blocks progress on URL-based approaches. The solution space now includes both architectural changes (making remote handling properly submodule-aware) and immediate fixes (aligning with Git 3.0's planned removal of deprecated functions).

Key technical details: The issue centers on `repo_get_default_remote`'s hardcoded behavior and now includes deeper investigation of `remote.c` internals. Key findings: (1) deprecated config-reading functions incorrectly assume `the_repository`, (2) there's asymmetry between repository and remote_state structure navigation, and (3) any solution must work with both current code and Git 3.0's removal plans. The change would affect submodule initialization, update, and fetch workflows, requiring modifications to remote selection logic and potentially broader remote handling architecture.

Open questions: Should deprecated functions be modified to take repository pointers before removal? Should parameter passing change from remote_state to repository pointers for submodule awareness? How to balance immediate fixes with architectural evolution? The fundamental tension remains between configuration flexibility and operational reliability, now complicated by implementation constraints in the remote handling code.

Participant positions: Jacob Keller continues driving technical exploration, now reporting concrete obstacles in the URL-based solution path. Junio Hamano's earlier concerns about reliability now intersect with these implementation challenges. The discussion shows increasing technical depth as solutions must navigate both architectural evolution and immediate bug fixes.

Related work: Connects to broader submodule design questions and the `the_repository` removal effort. The discussion may influence both submodule remote handling and the ongoing modernization of remote.c. The deprecated function issues highlight transition challenges in Git's codebase evolution.