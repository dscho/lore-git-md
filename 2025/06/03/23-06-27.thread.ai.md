Thread identity: Bug report and feature proposal regarding submodule behavior when `clone.defaultRemoteName` differs from "origin", reported by Jacob Keller, with Junio Hamano and Patrick Steinhardt now involved in the discussion.

Core problem: Submodule operations fail when `clone.defaultRemoteName` is configured to something other than "origin" due to hardcoding in `builtin/submodule--helper.c`. The discussion has expanded to address both the immediate configuration issue and deeper architectural challenges in remote handling code, particularly around repository awareness in deprecated functions that will be removed in Git 3.0.

Current status: Jacob Keller has identified concrete obstacles in the URL-based solution path, finding that `remote.c` functions like `read_remotes_file` and `read_branches_file` incorrectly assume `the_repository` despite being called with repository context. Junio Hamano strongly endorses modifying these functions to properly take repository pointers before their removal, citing both general code quality benefits and Jacob's specific submodule use case. Junio has explicitly sought Patrick Steinhardt's input, recognizing this may affect areas Patrick maintains.

Key technical details: The issue involves multiple layers: (1) `repo_get_default_remote`'s hardcoded "origin" behavior, (2) deprecated config-reading functions in `remote.c` that incorrectly use `the_repository`, and (3) broader questions about parameter passing between repository and remote_state structures. Changes would affect submodule initialization, update, and fetch workflows. The discussion now focuses on making `read_remotes_file` and `read_branches_file` properly repository-aware as a prerequisite for both the submodule fix and Git 3.0's removal of deprecated functions.

Open questions: How to implement repository parameter passing in the deprecated functions while maintaining stability? What other code paths might need similar attention before Git 3.0? How will Patrick Steinhardt and other maintainers of affected subsystems weigh in on these changes? The solution must balance immediate submodule functionality needs against the broader architectural evolution of remote handling code.

Participant positions: Jacob Keller continues technical investigation with Junio Hamano's strong support for repository-aware modifications to deprecated functions. Junio has elevated the discussion by seeking Patrick Steinhardt's input, signaling this may require coordination across subsystems. The consensus is forming around addressing repository awareness in deprecated functions as both a general improvement and enabler for the submodule fix.

Related work: Deeply connected to the `the_repository` removal effort and Git 3.0's planned deprecations. The discussion may influence both submodule remote handling and broader modernization of `remote.c`. The technical findings highlight transition challenges in Git's ongoing codebase evolution, particularly around maintaining functionality while removing legacy code.