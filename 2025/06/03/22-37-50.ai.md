Platform compatibility fix for `-Wunreachable-code` warnings on Windows.

This email confirms that the `NOT_CONSTANT()` workaround pattern (previously used for `sigfillset()` issues) successfully suppresses the `-Wunreachable-code` warning in `refs/files-backend.c` where Windows' `NO_SYMLINK_HEAD` definition makes `create_ref_symlink()` always return -1. The solution wraps the condition in `if (NOT_CONSTANT(!create_ref_symlink(...)))` to prevent the compiler from detecting the unreachable path while maintaining the existing error handling behavior. This follows Junio Hamano's suggestion and demonstrates the established pattern for handling platform-specific dead code warnings without altering the underlying symlink handling logic. The thread has now identified a clean resolution for this Windows-specific build warning while preserving cross-platform code correctness.