This is a documentation patch in a bugfix thread about bitmap memory leaks. It's the second patch in a 3-part series (v5) that addresses misleading comments in `test_bitmap_commits()`.

The patch clarifies that while the function can bypass the commit lookup table (if one exists), it must still load bitmap entries to obtain commit IDs for output. Taylor Blau provides minor stylistic suggestions about the phrasing ("bitmapped commits" might read better than "bitmap selected commits") but considers these too small to warrant a reroll of the series.

The change is purely textual and serves as preparatory work for the test infrastructure improvements in the subsequent patch. It follows the first patch's memory leak fix and precedes the third patch's comprehensive test coverage additions. The thread shows technical consensus on the approach, with this being a straightforward documentation improvement.