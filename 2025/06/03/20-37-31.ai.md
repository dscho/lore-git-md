This is a follow-up discussion in the resolved notes template whitespace bugfix thread. D. Ben Knoble weighs in with additional context about editor behavior and expands on Junio Hamano's earlier architectural suggestion.

The email provides two key observations:
1. Confirms the practical motivation for the fix (editor whitespace visibility) from a user perspective, noting that some editors highlight trailing whitespace intentionally for cleanup purposes
2. Extends Junio's `pretty.c` architectural suggestion to also cover `git commit -v` templates, which exhibit similar indentation behavior

Technical context remains unchanged from the thread summary - the focused `notes.c` solution using `strbuf_stripspace()` has been accepted, while the broader `pretty.c` approach remains a documented alternative for future consideration. The discussion here doesn't introduce new technical elements but reinforces the user-facing rationale and potential future directions.

The thread remains effectively closed, with this email serving as additional commentary rather than reopening discussion. The core technical decisions about implementation approach and POSIX compliance of test patterns were already settled in prior exchanges.