This is a platform compatibility patch that concludes the `-Wunreachable-code` warning discussion. Mike Hommey confirms that the `NOT_CONSTANT()` workaround pattern suggested by Junio Hamano successfully suppresses the compiler warning for Windows-specific dead code in `refs/files-backend.c`. The issue stemmed from `create_ref_symlink()` always returning -1 on Windows (via `NO_SYMLINK_HEAD`), making the subsequent `continue` unreachable. 

The solution maintains the existing behavior while making the code warning-clean, following the same pattern previously used for `sigfillset()`. This demonstrates Git's established approach to handling platform-specific dead code warnings without compromising cross-platform correctness. The patch is part of a larger series addressing various warning fixes, with this particular solution now confirmed working. The thread shows how the project balances build system robustness with platform-specific considerations.