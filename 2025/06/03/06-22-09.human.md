This is a bugfix patch addressing a test environment setup issue in the pack-bitmap subsystem. The patch is part of a v4 series that fixes memory leaks in bitmap cleanup behavior, with all technical aspects already reviewed and approved.

Lidong Yan responds to Junio Hamano's observation that the test wasn't properly exercising the boundary traversal path because the `GIT_TEST_PACK_USE_BITMAP_BOUNDARY_TRAVERSAL` environment variable wasn't being exported to child processes. The fix moves the variable assignment directly in front of the `git rev-list` command that needs it, ensuring the test properly triggers the boundary traversal code path in `find_boundary_objects()`.

The patch demonstrates good responsiveness to review feedback, with the author quickly acknowledging and fixing the oversight. The change is minimal but important for test validity, maintaining the series' focus on thorough testing of the bitmap memory leak fixes. With this final tweak, the series appears ready for integration, having addressed all reviewer concerns including shell portability and now proper environment variable handling.