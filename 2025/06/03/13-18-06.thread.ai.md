Thread identity: Refactoring patch series (v4) moving sparse-checkout configuration (`core.sparsecheckout` and `core.sparsecheckoutcone`) from global variables to `repo_settings` struct, part of the `the_repository` removal effort. Authored by Ayush Chandekar.

Core problem or goal: Eliminate global variables `core_apply_sparse_checkout`, `core_sparse_checkout_cone`, and `sparse_expect_files_outside_of_patterns` by moving sparse-checkout configuration into `repo_settings` or localizing variables, improving modularity in the broader repository-globals removal effort.

Current status: Final version (v4) with three patches under review, with Junio Hamano identifying critical initialization timing issues across all patches that require resolution, plus a minor formatting issue in the final patch:
1. Migrates `core_apply_sparse_checkout` to direct access pattern (15 files) - needs initialization verification
2. Migrates `core_sparse_checkout_cone` to `repo_settings` (10 files) - requires audit of `prepare_repo_settings()` calls in `builtin/grep.c`, `builtin/mv.c`, `dir.c`, `sparse-index.c`
3. Localizes `sparse_expect_files_outside_of_patterns` variable to its only user function - has minor trailing whitespace issue flagged by Junio

Key technical details:
- All sparse-checkout settings now accessed via `repo->settings.sparse_checkout` and `repo->settings.sparse_checkout_cone`
- `sparse_expect_files_outside_of_patterns` made local to `clear_skip_worktree_from_present_files()`
- Config parsing centralized in repo-settings.c via `prepare_repo_settings()`
- `USE_THE_REPOSITORY_VARIABLE` workarounds removed from multiple files
- No behavioral changes to sparse-checkout functionality
- Critical issue: must ensure `prepare_repo_settings()` called before any repository settings access (highlighted in `builtin/clone.c`, `builtin/mv.c`, `builtin/grep.c`, `dir.c`, `sparse-index.c` paths)
- Follows established direct access pattern for `repo_settings`
- Patch 3 demonstrates clean pattern for eliminating single-use globals

Open questions and loose ends:
- Documentation updates may follow for new access patterns
- Series requires initialization sequence verification across all patches before merging
- Need to audit all access points for proper `prepare_repo_settings()` calls
- Architectural patterns now available for future migrations
- Initialization safety concerns now identified as cross-cutting issue affecting all patches
- Final patch has trivial trailing whitespace issue to fix

Participant positions:
- Ayush Chandekar: implemented all versions, incorporated Junio's guidance
- Junio Hamano: identified critical initialization timing issues in both patch 1 and 2 reviews, provided definitive architectural guidance in v3 review, flagged final whitespace issue in patch 3
- Patrick Steinhardt: earlier input on access patterns
- Ben Knoble: originally advocated direct access approach
- Christian Couder: provided submission advice in earlier iterations

Related work: Completes a significant piece of the `the_repository` globals removal effort for sparse-checkout configuration. Builds on prior configuration migrations and the broader ODB abstraction work. The established access patterns (initialize once in `prepare_repo_settings()` then access directly) serve as a model for future similar migrations, though this review highlights the importance of proper initialization sequencing across all call paths. Demonstrates both repository-settings migration (patches 1-2) and variable localization (patch 3) approaches to eliminating globals, with initialization safety emerging as the key cross-cutting concern. The series appears ready for merging after addressing the minor formatting issue in patch 3 and verifying initialization safety across all call paths.