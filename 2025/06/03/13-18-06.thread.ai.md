Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into `repo_settings` with proper initialization, improving modularity in the broader repository-globals removal effort.

Current status: Final version (v3) under final review, with Junio Hamano raising design questions about the getter/setter implementation pattern. Implementation currently includes `repo_settings_get_sparse_checkout()` and `repo_settings_set_sparse_checkout()` functions that handle `prepare_repo_settings()` internally. All 14 affected call sites updated across commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.). The `core_sparse_checkout_cone` variable remains global for future work.

Key technical details:
- Variable name: `sparse_checkout` in `repo_settings` struct
- Config parsing moved from config.c to repo-settings.c
- `USE_THE_REPOSITORY_VARIABLE` workaround removed from builtin/backfill.c
- Current implementation uses full `prepare_repo_settings()` in accessors
- Alternative approaches suggested: direct access after initialization or lighter getter
- No behavioral changes to sparse-checkout functionality
- 14 files updated across multiple commands

Open questions and loose ends:
- Junio's design question about accessor pattern vs direct access
- Documentation updates needed for new access pattern
- `core_sparse_checkout_cone` remains as future work
- Potential audit of initialization safety patterns for similar migrations

Participant positions:
- Ayush Chandekar: implemented current version with naming consistency improvements
- Junio Hamano: raised final design question about access pattern consistency
- Patrick Steinhardt: earlier input on encapsulated access pattern
- Ben Knoble: originally advocated direct access but discussion converged
- Christian Couder: provided patch submission advice in earlier iterations
- Other reviewers previously satisfied with implementation

Related work: Continues systematic elimination of `the_repository` globals following established `repo_settings` patterns. Builds on prior configuration migrations and the broader ODB abstraction work. Leaves `core_sparse_checkout_cone` for future refactoring. Junio's feedback connects to broader patterns in the repository settings initialization system.