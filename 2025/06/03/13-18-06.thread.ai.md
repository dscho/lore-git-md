Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the ongoing `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into the `repo_settings` struct with proper getter/setter functions, improving modularity and supporting the broader effort to remove repository globals.

Current status: First version of patch sent by Ayush Chandekar, affecting 14 files across multiple commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.). Patrick Steinhardt has reviewed with technical concerns about naming, accessor utility, and initialization safety.

Key technical details:
- New accessors: `repo_settings_get_apply_sparse_checkout()` and `repo_settings_set_apply_sparse_checkout()` (under review for naming and necessity)
- Setting stored in `repo_settings.core_apply_sparse_checkout` (potential rename to `sparse_checkout`)
- Config parsing moved from config.c to repo-settings.c
- All callers updated to pass repository parameter
- Potential initialization issue: accessors don't ensure `prepare_repo_settings()` is called
- Enables removal of global from environment.h

Open questions and loose ends:
- Should the setting be renamed to drop "apply" for clarity?
- Are getter/setter functions justified when they don't add functionality?
- How to ensure proper initialization of settings before access?
- Need to address potential race conditions from unparsed settings

Participant positions:
- Ayush Chandekar: initial patch author
- Patrick Steinhardt: raised concerns about naming, accessor design, and initialization safety
- No other participants yet; this follows established pattern of similar refactorings by René Scharfe and others

Related work: Part of the long-running project to eliminate `the_repository` global, following similar conversions of other configuration settings to repository-specific storage. Builds on prior work moving repo-specific settings into the `repo_settings` struct.