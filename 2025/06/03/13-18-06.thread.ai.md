Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into `repo_settings` with proper initialization, improving modularity in the broader repository-globals removal effort.

Current status: Third iteration with emerging agreement on naming (`sparse_checkout` over `core_apply_sparse_checkout`) and access patterns. Patrick Steinhardt's latest input introduces nuance to the getter/setter vs direct access debate, noting that getters/setters become more valuable if they encapsulate `prepare_repo_settings()` calls. The thread is now considering either keeping getter/setter pattern with initialization handling, or standardizing on direct access with explicit preparation calls.

Key technical details:
- Renaming to `sparse_checkout` considered safe pending `git log -S` verification
- Two competing access patterns under consideration:
  - Direct struct access (`repo_settings->sparse_checkout`) with explicit `prepare_repo_settings()`
  - Getter/setter functions that internally handle preparation
- Config parsing remains in repo-settings.c
- Affects 14 files across commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.)
- No behavioral changes to sparse-checkout functionality itself

Open questions and loose ends:
- Final decision needed on access pattern (direct vs encapsulated)
- Documentation updates to reflect chosen access pattern
- Need to ensure all callers follow whichever pattern is chosen
- Potential follow-up to centralize initialization if direct access proves problematic
- Historical `core_apply_sparse_checkout` naming origin still pending verification

Participant positions:
- Ayush Chandekar: author, implementing consensus direction
- Patrick Steinhardt: clarified value proposition of getter/setters when they handle initialization
- Ben Knoble: originally advocated direct access, now considering encapsulated pattern
- Still awaiting potential input from René Scharfe on initialization safety

Related work: Continues systematic elimination of `the_repository` globals following established `repo_settings` patterns. Builds on prior configuration migrations and the broader ODB abstraction work that established this architectural direction.