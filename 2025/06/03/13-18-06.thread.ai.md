Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the ongoing `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into the `repo_settings` struct with proper getter/setter functions, improving modularity and supporting the broader effort to remove repository globals.

Current status: First version of patch sent, affecting 14 files across multiple commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.). Removes `USE_THE_REPOSITORY_VARIABLE` macro from builtin/backfill.c as it's no longer needed.

Key technical details:
- New accessors: `repo_settings_get_apply_sparse_checkout()` and `repo_settings_set_apply_sparse_checkout()`
- Setting stored in `repo_settings.core_apply_sparse_checkout`
- Config parsing moved from config.c to repo-settings.c
- All callers updated to pass repository parameter
- Enables removal of global from environment.h

Participant positions: No reviews or responses yet in thread. This follows established pattern of similar refactorings by René Scharfe and others in the `the_repository` removal effort.

Related work: Part of the long-running project to eliminate `the_repository` global, following similar conversions of other configuration settings to repository-specific storage.