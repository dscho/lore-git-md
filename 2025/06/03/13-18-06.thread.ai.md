Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into `repo_settings` with proper initialization, improving modularity in the broader repository-globals removal effort.

Current status: Naming consensus reached (`sparse_checkout` without "core" or "apply" prefixes) after historical investigation found no rationale for the "apply" qualifier. The thread is now converging toward getter/setter pattern that encapsulates `prepare_repo_settings()` calls, as this would prevent manual initialization at each access site. Implementation pending final confirmation of this direction.

Key technical details:
- Final variable name: `sparse_checkout` (verified safe via `git log -S`)
- Likely access pattern: getter/setter functions that internally handle `prepare_repo_settings()`
- Config parsing remains in repo-settings.c
- Affects 14 files across commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.)
- No behavioral changes to sparse-checkout functionality itself
- Historical naming investigation complete (no found rationale for "apply")

Open questions and loose ends:
- Final confirmation needed on getter/setter approach
- Documentation updates to reflect chosen access pattern
- Need to ensure all callers migrate to new accessors
- Potential follow-up to audit initialization safety if direct access proves problematic elsewhere

Participant positions:
- Ayush Chandekar: author, confirmed naming choice and leaning toward getter/setters with initialization handling
- Patrick Steinhardt: earlier input validated value of getter/setters when they encapsulate preparation
- Ben Knoble: originally advocated direct access, now considering encapsulated pattern
- Still awaiting potential input from René Scharfe on initialization safety considerations

Related work: Continues systematic elimination of `the_repository` globals following established `repo_settings` patterns. Builds on prior configuration migrations and the broader ODB abstraction work that established this architectural direction.