Thread identity: Refactoring patch moving `core.sparsecheckout` configuration from global variable to `repo_settings` struct, part of the `the_repository` removal effort.

Core problem or goal: Eliminate the global variable `core_apply_sparse_checkout` by moving sparse-checkout configuration into `repo_settings` with proper initialization, improving modularity in the broader repository-globals removal effort.

Current status: Implementation completed with getter/setter functions `repo_settings_get_sparse_checkout()` and `repo_settings_set_sparse_checkout()` that handle `prepare_repo_settings()` internally. All 14 affected call sites across commands (`clone`, `mv`, `sparse-checkout`, `worktree` etc.) updated to use new accessors. The `core_sparse_checkout_cone` variable remains global for now (future work).

Key technical details:
- Final variable name: `sparse_checkout` (historical investigation complete)
- New access pattern: encapsulated getter/setter functions in repo-settings.[ch]
- Config parsing moved from config.c to repo-settings.c
- `USE_THE_REPOSITORY_VARIABLE` workaround removed from builtin/backfill.c
- No behavioral changes to sparse-checkout functionality
- 14 files updated across multiple commands

Open questions and loose ends:
- Documentation updates needed for new access pattern (follow-up expected)
- `core_sparse_checkout_cone` remains as future work
- Potential audit of initialization safety patterns for similar migrations

Participant positions:
- Ayush Chandekar: implemented getter/setter approach as discussed
- Patrick Steinhardt: earlier input validated encapsulated access pattern
- Ben Knoble: originally advocated direct access but discussion converged
- No new objections or concerns raised in this implementation

Related work: Continues systematic elimination of `the_repository` globals following established `repo_settings` patterns. Builds on prior configuration migrations and the broader ODB abstraction work. Leaves `core_sparse_checkout_cone` for future refactoring.