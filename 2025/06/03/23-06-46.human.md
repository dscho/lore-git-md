This is a bugfix patch addressing a compiler warning on Windows. The patch fixes an "unreachable code" warning from clang in the files refs backend. The issue occurs when checking whether to create a symlink for a ref update - the compiler was incorrectly determining that the `continue` statement could never be reached.

The fix is simple but clever: by wrapping the condition in `NOT_CONSTANT()`, it prevents the compiler from making assumptions about the code path. This maintains the same behavior while silencing the warning. The change is minimal (just one line modified) and focused on a specific platform issue.

The patch appears to be the final part of a 4-patch series (as indicated by [4/4] in the subject), though the context of the other patches isn't provided here. The fix looks straightforward and well-justified - it solves a real compiler warning without introducing any new complexity. Windows users and developers who build with clang will benefit from this change.