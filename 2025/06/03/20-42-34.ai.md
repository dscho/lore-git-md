This is a review comment on Patch 1/2 of the GPG signing support series for `git subtree`. The reviewer, D. Ben Knoble, points out that the commit message references `-S/--gpg-sign` which isn't actually implemented in this patch. They suggest clarifying the motivation by stating this is preparatory change for future flags with optional parameters, like the upcoming GPG signing support.

The technical content of the patch itself (not shown in the review comment but referenced in the thread summary) converts argument parsing in `git-subtree.sh` to use `--stuck-long` mode, which:
- Changes short options to long options with equals syntax (e.g., `--message=msg`)
- Removes redundant `shift` operations
- Maintains the same functionality while preparing for cleaner handling of optional parameters

The review doesn't raise any functional concerns about the patch itself, just suggests improving the commit message clarity regarding the motivation. This appears to be a minor documentation nit on an otherwise mechanical refactoring that's part of a larger feature addition.