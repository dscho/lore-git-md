Platform compatibility fix addressing clang warnings on Windows. This is part 3 of a 4-patch series.

The patch cleans up several instances in `compat/mingw.c` where comma operators were used in return statements, which triggers warnings when compiling with clang on Windows. The author replaces these with explicit statements setting `errno` before returning the error value, making the code more readable while maintaining the same behavior.

The changes affect error handling in Windows-specific implementations of functions like `mingw_open_append()`, timer thread management, and signal handling. While purely stylistic, the modifications eliminate compiler warnings without changing functionality, following the pattern seen in other parts of Git's codebase.

The patch appears straightforward and well-contained, with each change following the same simple transformation pattern. As the third in a series, it continues the theme of Windows compatibility improvements, though the commit message could better explain why these particular warnings are worth addressing.