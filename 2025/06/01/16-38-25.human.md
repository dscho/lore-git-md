This is a documentation refinement in the ongoing test infrastructure discussion about path resolution strategies. Junio Hamano responds to Torsten Bögershausen's latest terminology suggestion ("physical path") for describing the test suite's path normalization behavior.

Junio accepts Torsten's "physical path" wording as reasonable given its connection to the `-P` flag used in the implementation, though he notes Git's codebase more commonly uses "absolute path". He suggests the key is internal consistency rather than strict terminology, proposing that functions like `strbuf_realpath()` could document that terms like "physical", "absolute", and "real" are used interchangeably.

The email includes a suggested rewrite of Torsten's commit message explanation to clarify that while `$HOME` and `$TRASH_DIRECTORY` may have identical values, their physical path representations (as resolved by `pwd -P`) might differ. This addresses the root cause of the t7900 test failures where `strbuf_realpath()` resolves paths differently than test expectations.

The discussion appears to be in its final stages, with Junio's approval of the technical approach and Torsten's terminology suggestion moving the thread toward resolution. The remaining work seems to be minor wording polish in the commit message before finalizing the patch.