This thread addresses a platform compatibility issue affecting OpenBSD and NetBSD systems with 4GB or more of RAM. The patch modifies Git's memory detection logic in `builtin/gc.c` to properly handle these configurations by adding support for the `HW_PHYSMEM64` sysctl parameter while maintaining backward compatibility with existing detection methods (`HW_MEMSIZE` and `HW_PHYSMEM`).

The change is narrowly scoped, focusing only on the BSD-specific memory detection path that affects Git's auto-configuration of memory limits (particularly for garbage collection). The patch maintains all existing behavior for other platforms and smaller-memory BSD systems while fixing the specific issue for >=4GB configurations. The commit message clearly identifies both the problem and the affected platforms, suggesting this is a straightforward, technically sound fix for a known limitation in Git's BSD support.