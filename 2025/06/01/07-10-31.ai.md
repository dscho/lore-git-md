This is a feature patch in the `imap-send` series, adding command-line folder specification. The patch introduces a new `--folder`/`-f` option to override the configured IMAP folder at runtime, addressing a common workflow where users need to send to different folders than their default configuration. The implementation follows the series' pattern of enhancing usability while maintaining backward compatibility.

The change adds the option to both documentation and code, with the command-line value taking precedence over the config when both are present. The documentation updates clarify the precedence rules and provide example usage. The code change is straightforward - it adds the option parsing and applies the override before connecting to the server. This matches the established behavior in other Git commands where command-line options override config values.

The patch fits cleanly into the broader series by providing another user-facing improvement alongside the authentication and memory management work. It's a focused change that doesn't introduce new dependencies or require changes to the authentication backends. The implementation appears correct, with proper memory management (freeing the old folder string before replacing it) and clear documentation updates.