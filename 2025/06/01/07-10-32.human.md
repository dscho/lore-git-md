This is a documentation and user experience patch in the ongoing `imap-send` improvement series. It focuses on standardizing error messages and output formatting in the IMAP client code.

The patch fixes numerous spelling and grammar issues in user-facing messages from `imap-send`, including:
- Adding missing periods at sentence ends
- Capitalizing first words of sentences
- Making error messages more consistent (e.g., "cannot" -> "Cannot")
- Standardizing IMAP protocol response formatting (e.g., "ok" -> "OK")

These changes touch both the main IMAP implementation and test scripts, with 44 lines modified across `imap-send.c` and `t1517-outside-repo.sh`. While purely cosmetic, these improvements make the tool's output more professional and consistent with Git's style.

As part of the larger v10 series that adds OAuth2 support and fixes critical bugs, this patch represents final polish to the user experience. The changes are straightforward and unlikely to be controversial, having already passed review in previous iterations of the series.