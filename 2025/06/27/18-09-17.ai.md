This is a follow-up discussion in the performance optimization series for pathspec bloom filters, specifically addressing review feedback on patch 2/2.

Junio Hamano points out two remaining issues from his earlier review of the multi-pathspec bloom filter implementation. First, he notes that `forbid_bloom_filters()` currently only checks the first pathspec item's magic bits, when it should verify none of the items have non-literal magic. Lidong Yan confirms this oversight and commits to fixing it in v3, along with adding corresponding tests to ensure bloom filters aren't incorrectly used with magic pathspecs.

The second discussion point concerns a potential future optimization regarding wildcard pathspecs (using `.nowildcard_len` to still allow bloom filtering for literal prefix portions), which Junio suggests could be handled in a follow-up patch series. While Lidong expresses interest in implementing this, Junio recommends focusing first on finalizing the current changes.

The exchange shows the thread moving toward completion, with the main technical work validated and only these final correctness checks remaining before the series could be merged. The discussion maintains a clear separation between immediate required fixes (magic bit checking) and future optimization opportunities (wildcard handling).