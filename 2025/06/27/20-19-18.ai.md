This is a follow-up discussion in the bugfix thread for Git daemon's EINTR handling during socket operations. Junio C Hamano weighs in on the variable declaration style question raised by Phillip Wood in the v2 review.

The email focuses on code organization preferences, suggesting an alternative arrangement for the `incoming` and `retry` variable declarations in the service_loop() function. Junio proposes moving both declarations together before the accept() call, rather than having them split across different scopes as in the patch. This is presented as a potential compromise between readability and the author's stated need for goto label placement.

The discussion remains technical and constructive, with no new substantive changes proposed to the core EINTR retry logic (which remains at 3 attempts). The thread's main open question about check_dead_children() timing is not addressed here. The exchange continues the pattern of careful review for this small but subtle socket handling fix.