This is a follow-up discussion about line length configuration in Git's codebase, specifically regarding the `.editorconfig` settings. Justin Tobler provides additional technical details about how vim's behavior changes with `max_line_length` settings, clarifying that it overrides the editor's `textwidth` configuration rather than just affecting lines exceeding the limit. 

The email reveals an important implementation detail: while Karthik Nayak's previous explanation suggested the 120-character limit would only affect lines exceeding that length, Justin demonstrates that in vim the setting actually changes the default wrapping behavior for all text formatting operations. This contradicts the intended behavior of only preventing "unacceptably long" lines while maintaining the 80-column guideline for normal cases.

The exchange highlights a technical gap between the conceptual goal (allowing occasional exceptions up to 120 chars while keeping most lines at 80) and actual editor behavior (where the setting may force 120-char wrapping by default). This suggests the proposed solution might not achieve its goal without additional editor-specific configuration workarounds. The discussion remains focused on finding a solution that properly separates the concepts of "recommended line length" (80) from "absolute maximum" (120) in a way that works across different editors.