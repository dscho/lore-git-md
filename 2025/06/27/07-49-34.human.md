This is a test patch in the RFC series introducing the new `git refs list` subcommand. It adapts the existing `for-each-ref` test suite to verify the new command's behavior matches exactly, while adding a new test file specifically for `refs list`.

The patch makes `t6300-for-each-ref.sh` generic by using a `GIT_REFS_LIST_CMD` variable that defaults to `for-each-ref`, then creates `t1461-refs-list.sh` that runs the same tests with the command set to `refs list`. This ensures feature parity between the commands while minimizing code duplication. The implementation is careful to skip tests that don't apply to the new command (like the `-h` help test) while maintaining all core functionality tests.

This is part of a larger effort to consolidate ref-related commands under a unified `git refs` namespace, with this initial version providing a drop-in replacement for `for-each-ref`. The test approach demonstrates the command's backward compatibility while establishing infrastructure for future evolution. The patch appears well-structured with clear commit message and proper sign-offs from mentors.