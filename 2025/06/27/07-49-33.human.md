This is the first implementation patch in a 2-part series introducing a new `git refs list` subcommand as part of ref command consolidation. The patch establishes the new subcommand as a direct copy of `git for-each-ref`'s functionality, reusing its core filtering and formatting logic while moving it under the new `git refs` namespace.

The implementation adds the subcommand to `builtin/refs.c` with identical options to `for-each-ref` (pattern matching, sorting, formatting, etc.) and documents it thoroughly in `git-refs.adoc`. The author explicitly states this duplication is temporary, with future enhancements planned exclusively for the new command while maintaining backward compatibility.

The patch appears well-structured with comprehensive documentation and maintains all existing functionality. As part of a GSoC project, it has multiple mentors listed. The approach seems pragmatic - starting with feature parity before evolving the new command independently. The main question for reviewers will be whether this is the right long-term direction for ref command consolidation.