This thread addressed a memory leak in Git's `send-pack` command, where pushing from a repository cloned with `--reference` could leak memory when processing refs from alternate object stores. Jacob Keller originally reported the issue, which involved a 2160-byte allocation in the `extra_have` oid array during push operations.

The initial fix added proper cleanup for the `extra_have` array, mirroring existing patterns for similar resources. Junio Hamano then refined this by consolidating all resource cleanup (including refs, refspecs, and shallow arrays) under a single `cleanup` label to handle error paths consistently. Jacob has now acknowledged this refinement as sound, confirming that both fixes follow established patterns and properly address the issue.

While technically complete, the thread notes a remaining test coverage gap for alternate-store push scenarios in t5539. Jacob indicated he won't be implementing these tests due to time constraints. With no outstanding objections or follow-ups needed, this concludes the discussion on this memory leak fix.