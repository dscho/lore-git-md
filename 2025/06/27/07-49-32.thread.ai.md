RFC patch series proposing `git refs list` subcommand as first step in ref command consolidation effort. Authored by GSoC participant with mentoring from experienced contributors, this initial version exactly mirrors `git for-each-ref` functionality while establishing new `builtin/refs.c` infrastructure (205 lines added across two files). Implementation reuses `for-each-ref` ref-filter logic, supporting all existing features: pattern matching, sorting, formatting, and filters (`--points-at`, `--merged`, `--contains`). Introduces `--include-root-refs` flag for explicit HEAD/pseudoref handling.

Test infrastructure added in second patch creates `t/t1461-refs-list.sh` that runs existing `for-each-ref` test suite against new command via `GIT_REFS_LIST_CMD=refs list` parameterization. Maintains all test coverage while verifying compatibility. Documentation review from Jean-Noël Avila provides detailed feedback on man page formatting (SYNOPSIS spacing, bracket placement) and content clarity, suggesting improvements to option descriptions and standardizing imperative mood phrasing. Review confirms technical accuracy while refining presentation to match Git documentation conventions, particularly around `--include-root-refs` wording ("along with" vs "apart from" regular refs).

Open questions remain about long-term `for-each-ref` feature adoption path and interface divergence justification. Seeks additional feedback from script users about migration. Maintains strict backward compatibility while providing new namespace for evolution, with temporary code duplication acknowledged as transitional step. Long-term ref command unification vision still to be shaped by community discussion, with this patch establishing initial technical foundation including comprehensive test coverage and now-reviewed documentation.