RFC patch series introducing `git refs list` as a thin wrapper around `git for-each-ref`, now at v4 with all major architectural refinements addressed and in final review. Authored by GSoC participant Meet Soni with mentoring from Patrick Steinhardt, the series consolidates ref listing functionality under `git refs` while preserving behavior through shared core logic.

Implementation establishes architectural foundation with:
- New `for-each-ref.h` header containing shared `for_each_ref_core()` helper and `COMMON_USAGE_FOR_EACH_REF` macro
- Both `cmd_for_each_ref()` and new `cmd_refs_list()` as thin wrappers calling the shared helper
- Usage string synchronization via header-based sharing
- Follows Junio Hamano's preferred helper function pattern with usage strings passed as parameters

Key technical elements:
- Core ref listing logic in `for_each_ref_core()` with `strvec` argument construction
- `COMMON_USAGE_FOR_EACH_REF` macro consolidates shared option documentation
- Test infrastructure (t1461-refs-list.sh) with shared `for-each-ref-tests.sh` library
- Fixed pre-existing man page inconsistency for `for-each-ref`
- Resolved t0450 documentation test breakage
- Replaces hardcoded `git for-each-ref` calls with `$git_for_each_ref` variable in tests

Series structure in v4:
1. Documentation refactoring - extracts option documentation into shared `for-each-ref-options.adoc`
2. Core logic extraction to `for_each_ref_core()` helper
3. `refs list` command implementation
4. Test refactoring (shared infrastructure)
5. New tests for `refs list`

Latest discussion centers on pattern matching behavior inherited from `git for-each-ref`, with Junio Hamano responding to Phillip Wood's concerns about current semantics being confusing. Junio clarifies the hierarchical matching is intentional (e.g., `refs/heads/m*` matches `morning` but not `mid/night`), arguing this is useful for listing main branches without showing topic branches. He suggests maintaining current single-asterisk behavior (anchored at hierarchy boundaries) while proposing future enhancement with double-asterisk (`**`) for cross-hierarchy matching (e.g., `refs/heads/m**` to catch all 'm' branches at any level).

Remaining feedback falls into two categories:
1. Minor nits from Patrick Steinhardt:
   - Requires alphabetical sorting of includes in `builtin/for-each-ref.c`
   - Needs proper explanation for t0450 test change where `for-each-ref` entry was removed
   - Suggests separating documentation fix into its own commit

2. Behavioral decision now leaning toward maintaining current pattern matching semantics per Junio's input, with potential future extension for `**` syntax. This addresses Phillip Wood's concern while preserving existing `for-each-ref` compatibility.

All major technical concerns have been addressed across four versions. Final work needed: commit organization polish, documentation details, and formal resolution of pattern matching behavior (likely accepting current semantics with possible `**` enhancement). Key participants: Meet Soni (author), Patrick Steinhardt (mentor), Junio Hamano (maintainer), Eric Sunshine (documentation), Jean-Noël Avila (documentation standards), Phillip Wood (behavioral consistency). Architectural foundation remains solid with shared core implementation between commands.