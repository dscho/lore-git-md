RFC patch series proposing ref command consolidation via `git refs list` subcommand, with parallel documentation improvements. Authored by GSoC participant Meet Soni with mentoring from experienced contributors. Initial version (205 lines added) establishes new `builtin/refs.c` reusing ref-filter logic from `git for-each-ref` with all existing functionality (pattern matching, sorting, formatting, filters like `--points-at`, plus `--include-root-refs` for HEAD/pseudorefs). Test infrastructure runs existing `for-each-ref` suite against new command via `GIT_REFS_LIST_CMD=refs list`.

Maintainer Junio C Hamano questions justification for new command, advocating wrapper implementation (like `git branch`/`tag`) to achieve discoverability without code duplication. Meet Soni clarifies discoverability motivation through `git refs` hierarchy. Hamano expresses skepticism about adding commands that could diverge from existing functionality, citing `git checkout` split as precedent where consolidation wasn't optimal. Shejialuo later confirms agreement with wrapper approach, requiring redesign of current parallel code paths. Maintainer position solidifies around wrapper pattern preserving behavior while improving organization.

Documentation improvements (Jean-Noël Avila) proceed independently, with initial minimal docs expanded via follow-up patches standardizing option syntax in both commands. Hamano reviews `git-for-each-ref` man page improvements focusing on consistency (option syntax, `_<placeholder>_` convention), identifying remaining style issues. Jean-Noël Avila agrees to produce v2 addressing all style concerns, making documentation track uncontroversial.

Open questions center on whether consolidation under `git refs` justifies new command given maintainer's strong wrapper preference. Technical foundation via ref-filter reuse remains sound, though implementation must shift to wrapper pattern. Community needs to evaluate discoverability benefits against maintenance concerns and potential behavioral divergence risks. Documentation standardization continues separately while wrapper approach emerges as likely resolution path.