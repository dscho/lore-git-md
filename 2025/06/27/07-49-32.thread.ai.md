RFC patch series introducing `git refs list` as a thin wrapper around `git for-each-ref`, now at v4 with architectural refinements addressing all prior maintainer feedback. Authored by GSoC participant Meet Soni with mentoring from Patrick Steinhardt. The series consolidates ref listing functionality under `git refs` while preserving existing behavior through shared core logic.

Implementation establishes architectural foundation with:
- New `for-each-ref.h` header containing shared helper `for_each_ref_core()` and `COMMON_USAGE_FOR_EACH_REF` macro
- Both `cmd_for_each_ref()` and new `cmd_refs_list()` as thin wrappers calling the shared helper
- Usage string synchronization via header-based sharing
- Follows Junio Hamano's preferred helper function pattern with usage strings passed as parameters

Key technical elements:
- Core ref listing logic in `for_each_ref_core()` with `strvec` argument construction
- `COMMON_USAGE_FOR_EACH_REF` macro consolidates shared option documentation
- Test infrastructure (t1461-refs-list.sh) carried forward from v3
- Fixed pre-existing man page inconsistency for `for-each-ref`
- Resolved t0450 documentation test breakage
- New test refactoring in v4 4/5 moves core test logic from `t6300-for-each-ref.sh` to shared `for-each-ref-tests.sh`
- Replaces hardcoded `git for-each-ref` calls with `$git_for_each_ref` variable
- Makes original test file a simple wrapper that sets variable and sources library
- Follows `git-blame`/`git-annotate` test sharing pattern

Series structure in v4:
1. Documentation refactoring - extracts option documentation from `git-for-each-ref.adoc` into shared `for-each-ref-options.adoc` (79 lines covering pattern matching, sorting, formatting) with include directive maintaining output
2. Core logic extraction to `for_each_ref_core()` helper
3. `refs list` command implementation (new patch adding `cmd_refs_list()` wrapper)
4. Test refactoring (shared test infrastructure)
5. New tests for `refs list` (final patch adding minimal test driver that reuses shared infrastructure)

Documentation incorporates:
- Jean-Noël Avila's standardization work
- Eric Sunshine's hygiene suggestions
- Shared options via `refs-list-options.adoc` include
- Fixed man page inconsistencies

Resolved from prior versions:
- Eliminated usage string duplication via macro
- Solved usage string synchronization
- Established maintainable architecture with core helper
- Implemented Junio's preferred wrapper pattern
- Now includes shared test infrastructure
- Added comprehensive test coverage through final patch

Open considerations:
- Future refinements to ref listing interface
- Long-term vision for `git refs` command consolidation
- Performance impact of `%(objecttype)` (6.65x difference noted but deferred)

Key participants:
- Meet Soni (author, implemented architectural refinements)
- Patrick Steinhardt (mentor, detailed implementation guidance)
- Junio Hamano (maintainer feedback on architecture)
- Eric Sunshine (documentation review)
- Jean-Noël Avila (documentation standards)

The series has addressed all technical concerns through iterative refinement. The v4 restructuring demonstrates active engagement with maintainer feedback while preserving original goals. With the architectural foundation now solid (including `for_each_ref_core()`) and test infrastructure maintainer-approved, the series appears ready for final review. The new documentation patch establishes shared option infrastructure while maintaining output fidelity, supporting command consolidation. The test refactoring follows established patterns and enables testing both commands with the same logic. The final test patch completes the implementation by verifying behavioral parity through shared test infrastructure.