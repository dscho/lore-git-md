RFC patch series introducing `git refs list` as a thin wrapper around `git for-each-ref`, authored by GSoC participant Meet Soni with mentoring from Patrick Steinhardt and others. Current v2 redesign addresses maintainer Junio C Hamano's concerns by eliminating code duplication while preserving discoverability goals. Implementation forwards all arguments through `cmd_refs_list` in `builtin/refs.c` to `cmd_for_each_ref`, ensuring identical behavior.

Technical changes include significant documentation restructuring with shared options extracted to `refs-list-options.adoc` for consistency between commands, now incorporating Eric Sunshine's suggestion to remove explicit command listing comments in favor of `grep`-based discovery. New test infrastructure in `t/t1461-refs-list.sh` runs all existing `t6300-for-each-ref.sh` tests with `GIT_REFS_LIST_CMD=refs list`, modified to use `${GIT_REFS_LIST_CMD:-for-each-ref}` throughout. Skips help test for `refs list` due to differing usage strings. Preserves all existing functionality including `--include-root-refs` for HEAD/pseudorefs while establishing new subcommand position in `git refs` hierarchy.

Performance considerations from Patrick Steinhardt (6.65x speed difference when omitting `%(objecttype)`) remain deferred. Documentation standardization progresses with Jean-Noël Avila addressing shared option syntax and man page conventions (`_<placeholder>_`), now refined per Eric Sunshine's maintainability feedback. Maintainer skepticism about command proliferation appears addressed by wrapper pattern.

Open questions: whether future iterations should optimize defaults (reducing ODB lookups) while preserving `for-each-ref` behavior, and potential migration of additional ref-related commands under `git refs` hierarchy. Current approach establishes namespace infrastructure without breaking changes, positioning wrapper for controlled behavioral divergence where justified. Final maintainer evaluation pending. Testing infrastructure now fully exercises compatibility between commands through shared test suite. Eric Sunshine's documentation polish suggestion accepted as low-impact improvement to long-term maintainability.