New RFC patch series proposing `git refs list` subcommand as first step in ref command consolidation effort. Authored by GSoC participant with mentoring from experienced contributors, this initial version exactly mirrors `git for-each-ref` functionality while establishing new `builtin/refs.c` infrastructure and `Documentation/git-refs.adoc`. Implementation reuses `for-each-ref` ref-filter logic (205 lines added across two files), supporting all existing features: pattern matching, sorting, formatting, and filters (`--points-at`, `--merged`, `--contains`). Introduces `--include-root-refs` flag for explicit HEAD/pseudoref handling. 

Test infrastructure added in second patch creates `t/t1461-refs-list.sh` that runs existing `for-each-ref` test suite against new command by setting `GIT_REFS_LIST_CMD=refs list` and sourcing `t/t6300-for-each-ref.sh`. Modifies `t/t6300-for-each-ref.sh` to use `${GIT_REFS_LIST_CMD:-for-each-ref}` throughout, making it parameterizable to test either command, with one exception skipping `-h` help test for `refs list` since it shares implementation. Maintains all existing test coverage while verifying new command's compatibility.

Verified via test substitution running existing tests with new command. Maintains strict backward compatibility while providing new namespace for evolution. Temporary code duplication acknowledged as transitional step. Open questions remain about which `for-each-ref` features to carry forward long-term and what improvements might justify interface divergence. Seeks feedback particularly from script users about migration path. Long-term vision for ref command unification still to be shaped by community discussion, with this patch establishing initial technical foundation including comprehensive test coverage.