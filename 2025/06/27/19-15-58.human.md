This is a follow-up in the thread about customizing ref completion sorting in Git's completion script. D. Ben Knoble responds to Nelson Benítez León's initial patch that introduced a `completion.forRefSort` config option, suggesting they resubmit the patch via GitGitGadget for proper formatting. The original patch aimed to let users customize the sort order of ref completions (like in `git checkout <TAB>`) from the default alphabetical to options like recent committer date.

Knoble's email doesn't review the technical content but focuses on submission mechanics, noting the current patch format (attached rather than inline) makes it harder to review. This is procedural guidance rather than technical feedback, leaving the actual patch discussion still open. The underlying feature proposal - configurable ref completion sorting - remains unchanged from the initial description.