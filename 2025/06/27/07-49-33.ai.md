Feature implementation as part of ref command consolidation effort. This first patch in the series introduces the `git refs list` subcommand, which currently mirrors all functionality of `git for-each-ref` exactly. The implementation reuses the existing ref-filter logic while establishing new infrastructure in `builtin/refs.c` and comprehensive documentation in `Documentation/git-refs.adoc`.

The patch adds 205 lines across two files, with the command supporting all existing `for-each-ref` features including pattern matching, sorting, formatting, and various filtering options (`--points-at`, `--merged`, `--contains` etc). The implementation carefully maintains backward compatibility while setting up a new namespace for future evolution. A notable addition is the `--include-root-refs` flag that explicitly handles HEAD and pseudorefs.

The approach appears well-considered, with clear mentoring from experienced contributors. The temporary code duplication is explicitly acknowledged as an intentional stepping stone. Test coverage is implied via the mention of running existing tests with command substitution, though no new test files are shown in this diff. The patch lays groundwork for potential future divergence from `for-each-ref` while currently maintaining strict parity.