This is part of the SHA-256 transition series, specifically adding infrastructure to distinguish between intentional SHA-1 usage and legacy format requirements. The patch introduces a new constant `GIT_HASH_ORIGINAL` in hash.h to explicitly mark cases where SHA-1 is required due to older formats (like bundle v1/v2) that hardcode SHA-1 rather than allowing algorithm negotiation.

The change is purely documentary at this stage - the value still equals `GIT_HASH_SHA1` - but serves an important signaling purpose. It separates cases where SHA-1 is used by necessity (for compatibility with old formats) from cases where it might be used by choice. This distinction will become more important when SHA-256 becomes the default hash algorithm.

The patch is small and focused, with a clear commit message explaining the motivation. It builds on the thread's established context about preparing for SHA-256 as the default hash algorithm. The author anticipates this constant will be useful for documentation purposes, though its addition could be somewhat controversial since it doesn't change behavior yet. This is patch 2/10 in a series that appears to be methodically preparing the codebase for the SHA-256 transition.