This is a final polish patch in the long-running `imap-send` improvement series, addressing minor logging issues. The changes are purely cosmetic but follow through on the series' attention to detail by standardizing error message formatting:

1. Makes error messages consistently lowercase (except for proper nouns like "IMAP") and removes trailing periods where inconsistent
2. Ensures protocol names ("IMAP", "OK", "CRAM-MD5") appear in uppercase
3. Fixes an incorrect error message that showed the hostname rather than auth method when reporting unknown authentication mechanisms
4. Tweaks several other messages for grammar/clarity (e.g. "no messages found to send" vs original "no messages to send")

The changes touch 12 lines across `imap-send.c`, all in error/status message output. This appears to be the final patch in the v19 series, following through on earlier work to standardize the user experience after the major functionality additions (OAuth2 support, folder management, configuration fixes). The changes are straightforward and unlikely to be controversial, serving as quality-of-life improvements to match Git's typical error message style.