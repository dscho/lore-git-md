This thread documents the complete lifecycle of improvements to Git's handling of symbolic port names in `git-credential-netrc` and `git-send-email`. The discussion began with a v3 patch series that added basic support for resolving symbolic port names (like "smtps") via `getservbyname`, which was merged. The v4 series then followed up with polish changes like portable shebangs and test formatting.

The final patch in the series, now being discussed, adds robust error handling by introducing a new `port_num` helper function in perl/Git.pm that validates both numeric and symbolic ports. For `git-credential-netrc`, it properly separates port numbers from protocol values in credential matching, fixing cases where symbolic ports were incorrectly processed. For `git-send-email`, it adds SMTP port validation with clearer error messages. The changes maintain backward compatibility while adding warnings for invalid ports in netrc files and errors for invalid SMTP ports.

Throughout the thread, Maxim Cournoyer has implemented proper submission practices following Junio Hamano's guidance, with no technical objections raised. The series serves as a reference for both port handling implementation and Git's iterative improvement process, demonstrating how functionality can be enhanced while preserving compatibility. Test coverage has been comprehensive at each stage, with new tests added for the validation behavior. The changes affect multiple Perl scripts but follow established patterns from earlier review feedback.