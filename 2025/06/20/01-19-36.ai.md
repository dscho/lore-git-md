This is part of the SHA-256 transition series, specifically updating legacy format handling to use the new GIT_HASH_ORIGINAL constant instead of hardcoded SHA-1 references. The patch makes mechanical changes across several files (receive-pack, bundle, transport, etc.) to replace GIT_HASH_SHA1 with GIT_HASH_ORIGINAL in contexts dealing with legacy protocols and formats that were implicitly SHA-1 based.

Key technical details:
- Updates 9 files with straightforward substitutions of GIT_HASH_SHA1 -> GIT_HASH_ORIGINAL
- Affects protocol handling (connect.c, fetch-pack.c), bundle format (bundle.c), and repository setup (setup.c)
- Maintains identical runtime behavior since GIT_HASH_ORIGINAL currently equals GIT_HASH_SHA1
- Changes are purely documentary at this stage, but prepare for future SHA-256 default

The patch is consistent with the series' goal of clearly distinguishing between legacy SHA-1 requirements and intentional SHA-1 usage. The changes are mechanical and well-scoped, touching only the expected protocol and format handling code. This appears to be a straightforward implementation of the approach outlined in the cover letter, with no new technical challenges introduced.