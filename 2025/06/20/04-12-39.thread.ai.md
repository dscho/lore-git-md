Thread identity: Completed v3 patch series for git-credential-netrc handling symbolic port names via helper-side resolution, merged with shared validation logic, following Junio Hamano's implicit acceptance via submission guidance, with a post-merge style note about test indentation.

Core problem: Resolved debate over whether symbolic port names in netrc files should be handled by credential helpers (via getservbyname) or restricted to numeric ports per credential protocol documentation, with Maxim Cournoyer's v3 series implementing helper-side resolution as the chosen approach. Final merged version included a minor style inconsistency in test indentation noted post-merge.

Current status:
- Final v3 patch merged with:
  - Shared `port_num` function in perl/Git.pm for unified validation
  - Proper handling of symbolic ports as ports (not protocols)
  - Complete test coverage including test_expect_failure cases
  - Platform-compatible getservbyname implementation
  - Numeric port range validation
  - Updated credential helper tests for numeric matching
  - One test case with inconsistent indentation (spaces vs tabs)
- Submission format clarified for future series:
  - Cover letters must reply to previous iteration's cover letter
  - Individual patches must reply to their own cover letter

Key technical details:
1. Shared infrastructure:
   - `port_num` function handles both numeric and symbolic ports
   - Used by git-credential-netrc and git-send-email
   - Validates 16-bit numeric range
   - Implements getservbyname resolution
2. Fixed behaviors:
   - Correctly treats symbolic ports as ports (fixes Emacs authinfo.gpg issue)
   - Maintains backward compatibility
   - Provides clear error messages
3. Test improvements:
   - SMTP port validation in git-send-email
   - test_expect_failure for invalid ports
   - Updated credential helper matching tests
   - One test case with mixed indentation (noted post-merge)

Participant positions:
- Brian Carlson maintained numeric-only position based on protocol docs
- Maxim Cournoyer implemented helper-side resolution in v3
- Junio Hamano implicitly accepted helper-side approach via submission guidance
- Junio noted post-merge style inconsistency in test indentation

Related work:
- git-send-email's port handling
- RFC6335 service name standards
- Emacs auth-source behavior
- POSIX getservbyname
- git-credential protocol documentation

The thread concludes with the helper-side resolution approach being merged, resolving the long-standing debate. The implementation includes all requested technical improvements and test coverage while maintaining compatibility. Junio's post-merge note about test indentation serves as a reminder of Git's coding style conventions but doesn't affect the merged functionality. Future impact includes proper patch submission formatting guidance from Junio for subsequent series and attention to style consistency in tests. The credential protocol's numeric-only wording remains unchanged but the implementation now practically supports symbolic port resolution via helpers.