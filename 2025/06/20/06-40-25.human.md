Bugfix for a memory leak in `imap-send`'s CRAM-MD5 authentication. This is part of a larger series (v19 02/10) improving IMAP functionality, but stands alone as a straightforward fix.

The patch fails to free the `response` buffer when `socket_write()` fails during CRAM-MD5 authentication. The fix adds the missing `free()` call in the error path, preventing memory leaks when network writes fail. This is a small but important correction to the authentication flow.

The change is minimal (just 3 lines) and clearly correct - it matches the existing cleanup that happens on the success path. While simple, it plugs a real leak that could occur during error conditions. The patch follows the style of the surrounding code and includes a clear commit message explaining the fix.

This is one of several authentication-related fixes in the series, which overall brings substantial improvements to `imap-send`'s reliability and feature set. The patch appears ready to merge as-is.