This is a review comment on patch 2/7 in the `git repo-info` GSoC series, focusing on the `--format` flag implementation. Junio C Hamano provides several technical suggestions for improving the code structure and test design.

The review notes that the enum definition for output formats should use a trailing comma per Git's style guidelines, making future additions cleaner. It also observes that the test references `--allow-empty` (a flag from later patches) without proper context, suggesting this creates confusion in the patch series flow.

A key suggestion is to reconsider the command's output model - rather than defaulting to showing everything unless filtered, the review proposes an explicit `--all` flag that would list all available fields, while normal operation would only show requested fields. This would make the behavior more intuitive and easier to document.

The review maintains the thread's focus on implementation quality and documentation clarity, while also catching an issue with premature references to functionality (`--allow-empty`) that properly belongs to later patches. The feedback continues the pattern of architectural guidance from Junio while aligning with Karthik Nayak's earlier emphasis on clear design rationale and documentation.

Technical details worth noting for future reference:
- The enum `output_format` currently only has `FORMAT_JSON`
- The test suite uses Perl JSON parsing with `PERLJSON` prerequisite
- Current output shows empty JSON object (`{}`) by default
- The review suggests moving to an opt-in field selection model rather than opt-out