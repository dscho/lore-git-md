This is a maintainer follow-up in the git-credential-netrc bugfix thread. Junio Hamano confirms that Andreas Schwab's citation of RFC6335 provides the technical justification needed for the port name validation changes. The RFC establishes that service names (used as symbolic ports) may contain hyphens but not underscores, which resolves the earlier uncertainty about allowed characters.

The email serves as an implicit approval of the patch's direction, now that the validation rules can be properly documented with reference to RFC6335. Junio notes this was exactly the kind of validation rules he wanted included in the commit message, and that the original alnum-only approach was insufficient.

This moves the patch closer to merging, as it still needs the author to:
1. Update the regex to `^[[:alnum:]-]+$` per RFC6335
2. Document the RFC as the validation standard
3. Make the previously requested style fixes (commit message formatting, Signed-off-by, and removing the unrelated shebang change)

The thread shows consensus forming around the technical solution, with the remaining work being proper documentation and cleanup of the patch submission itself. No new technical questions remain open after this authoritative reference to RFC6335.