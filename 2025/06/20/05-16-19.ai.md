This is a response in the ongoing `imap-send` feature series adding OAuth2.0 authentication support. The email addresses two technical review points from Phillip Wood about the OAUTHBEARER implementation in patch 4/10.

The author clarifies that host/port fields in the OAUTHBEARER string are intentionally omitted as they are optional per RFC7628 section 3.1, despite being mentioned in the code comment. This resolves one of Phillip Wood's observations about potential RFC compliance issues.

The other review point about fragile string construction using `snprintf()` is acknowledged but not yet addressed in this response. The author had previously noted this could be improved with `strbuf` or `xstrfmt()`, which remains an open action item for the patch series.

The response also requests being added to the CC list for future discussion, indicating the author may not be closely following the mailing list thread. This suggests future iterations may need explicit notification to this contributor for review.

Key technical details carried forward:
- OAUTHBEARER implementation omits optional host/port fields per RFC
- String construction safety in `oauthbearer_base64()` remains to be addressed
- Patch context: part of adding OAuth2.0 support via OAUTHBEARER and XOAUTH2 protocols for both OpenSSL and libcurl backends

The exchange shows the patch is in final polishing stages, with only minor implementation refinements remaining before potential integration.