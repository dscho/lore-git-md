This is a test infrastructure patch in the SHA-256 default transition series, updating t5300-pack-object.sh to handle hash algorithm changes. It's part 9/10 of brian m. carlson's series preparing Git for SHA-256 as the default hash algorithm when built with WITH_BREAKING_CHANGES.

The patch makes two key changes to the pack-object tests: First, it replaces the SHA1 prerequisite with DEFAULT_HASH_ALGORITHM for tests that run outside a repository, since these operations should use the built-in default hash (which will soon be SHA-256). Second, it updates conditional tests that check hash algorithm behavior to use $GIT_TEST_BUILTIN_HASH instead of hardcoding 'sha1', making them work correctly regardless of the active hash algorithm.

The changes follow the pattern seen in earlier patches of the series, where tests are updated to be hash-algorithm-agnostic. This particular patch focuses on operations that work outside a Git repository, ensuring they properly respect the default hash algorithm rather than assuming SHA-1. The modifications are straightforward mechanical updates to test conditions and prerequisites, maintaining the same test coverage while preparing for the SHA-256 transition.

The patch appears well-contained and consistent with the series' goal of making tests work correctly with both SHA-1 and SHA-256. It builds on the infrastructure changes introduced earlier in the series (GIT_TEST_BUILTIN_HASH and DEFAULT_HASH_ALGORITHM) without introducing new concepts.