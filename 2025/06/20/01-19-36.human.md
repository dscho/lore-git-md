This is part of the SHA-256 transition series, specifically updating legacy format handling to use the new GIT_HASH_ORIGINAL constant instead of hardcoded SHA-1 references. The patch makes mechanical changes across several files (bundle handling, network protocols, repository setup) to consistently use GIT_HASH_ORIGINAL where older formats assumed SHA-1 by default.

The changes are straightforward replacements that don't alter behavior - GIT_HASH_ORIGINAL still equals SHA-1 at this stage. The purpose is documentation and future-proofing, making it clear these are legacy cases rather than intentional SHA-1 usage. The patch touches core areas like bundle v1/v2 support, fetch/push protocols, and repository initialization.

This continues the series' clean separation between "default hash" (which may become SHA-256) and "original hash" (always SHA-1 for compatibility). The changes appear well-scoped and maintain backward compatibility while preparing for the eventual SHA-256 default.