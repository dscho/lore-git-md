This is a final review comment on the SHA-256 default transition series, specifically addressing backward compatibility testing concerns. Junio C Hamano raises an important practical consideration about the patch series that makes SHA-256 the default when built with WITH_BREAKING_CHANGES.

The email focuses on ensuring comprehensive testing of backward compatibility with existing SHA-1 repositories, particularly edge cases where repositories lack explicit core.repositoryformatversion declarations. While the series already handles repository format initialization and hash algorithm selection through the new GIT_HASH_ORIGINAL constant, Junio emphasizes the need to verify that Git remains fully functional with legacy SHA-1 repositories even when built with SHA-256 as the default.

This surfaces an implicit testing gap - while the series includes test suite updates with GIT_TEST_BUILTIN_HASH and DEFAULT_HASH_ALGORITHM, it may not sufficiently cover interaction with version-less historical repositories. The comment doesn't propose specific test additions but rather serves as a reminder to the author and reviewers to validate this critical interoperability case before finalizing the changes.

The exchange occurs after technical approval of the implementation, suggesting this is a final verification point rather than a blocking concern. It ties back to the series' core goal of making SHA-256 the default while maintaining backward compatibility, highlighting that the testing focus should extend beyond just new repository creation to include continued operation with existing repositories regardless of their format version status.