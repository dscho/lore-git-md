This is a review response in the ongoing "repo-info" command series, specifically addressing patch 4/7 which adds an `--allow-empty` flag. Junio C Hamano reiterates his preference from earlier in the thread to flip the flag's polarity, suggesting `--all` to show all fields by default rather than requiring `--allow-empty` to show nothing when no fields are specified. The patch itself is small - just adding the flag option and updating two tests to use it works as expected in both JSON and plaintext output formats.

The review maintains Junio's consistent architectural focus, questioning whether the flag design aligns with Git's typical command patterns. This continues the thread's theme of interface design scrutiny, where earlier feedback had already raised questions about field selection models and output behavior. The implementation appears technically sound but remains conceptually contentious regarding default behaviors.