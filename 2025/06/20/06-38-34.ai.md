This is a follow-up patch series in the ongoing `git imap-send` error message improvements thread. Joerg Thalheim builds on Aditya Garg's earlier work by splitting the changes into two focused patches and incorporating Junio Hamano's feedback.

The first patch fixes confusing terminology in an error message, changing "store" to "folder" to match the actual configuration option name (`imap.folder`). The second patch enhances error messages by using Git's `error()` and `advise()` APIs to provide configuration hints when required IMAP settings are missing. The series has been rebased on the `ag/imap-send-resurrection` branch as requested in previous discussion.

Key changes include:
- Replacing direct `fprintf()` calls with Git's standard message APIs
- Splitting the original single-patch approach into logical units (terminology fix + message improvements)
- Maintaining focus on user-facing error messages while the underlying IMAP functionality issues are addressed separately

The patch set shows good attention to review feedback and demonstrates clear incremental improvement over v1. The changes are small (9 lines modified) but meaningful for users encountering configuration issues. The thread context suggests these changes are compatible with the parallel work on fixing core IMAP functionality.