This is a follow-up in the ongoing `imap-send` feature series, specifically addressing review feedback about string handling in the OAuth2.0 authentication implementation. The patch being referenced (v18 04/10) introduced OAuth2 support via OAUTHBEARER and XOAUTH2 protocols, with new credential encoding functions that originally used `snprintf()` for string construction.

The email confirms that v19 of the series has addressed Phillip Wood's concern about fragile string handling by replacing `snprintf()` with safer alternatives (`strbuf` or `xstrfmt()` where appropriate) in the credential encoding functions. This affects not just the OAuth2 patch but also related patches building authentication strings. The change improves robustness by eliminating potential buffer overflow risks from manual string construction.

The thread context shows this was one of the final technical issues being resolved in the comprehensive `imap-send` overhaul, which includes critical bugfixes, multiple authentication method implementations, and new folder management features. The v19 update appears to complete the technical refinements for this long-running series.