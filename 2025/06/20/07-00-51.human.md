This is part of the ongoing `imap-send` feature and bugfix series, specifically addressing OAuth2.0 authentication support. The email is a follow-up to review feedback about string handling in the credential encoding functions.

The patch in question (now at v19) replaces fragile `snprintf()` usage with safer `xstrfmt()` calls when constructing OAuth2.0 authentication strings. This change was prompted by Phillip Wood's review noting the original implementation's potential vulnerabilities when building protocol-specific authentication strings. The fix affects the `oauthbearer_base64()` and `xoauth2_base64()` functions that handle the encoded credentials for IMAP authentication.

This appears to be a straightforward improvement addressing a code quality concern in an otherwise well-developed feature. The change aligns with the series' broader goal of making `imap-send` more robust while adding modern authentication methods. Since this is a response to specific review feedback and the series is now at v19 with multiple rounds of review, the change likely represents a final polish before potential inclusion.