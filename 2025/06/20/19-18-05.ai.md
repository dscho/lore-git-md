This is the final patch in the SHA-256 default transition series, implementing the conditional definition of GIT_HASH_DEFAULT based on WITH_BREAKING_CHANGES. The patch makes SHA-256 the default hash algorithm when Git is built with breaking changes enabled (targeting Git 3.0), while maintaining SHA-1 as the default otherwise.

The technical implementation is now complete, with Junio C Hamano providing a final style correction regarding preprocessor indentation in hash.h. The author confirms they will incorporate this formatting change in a v2 submission. This follows extensive discussion and testing of the series' backward compatibility with legacy repositories, including version-less SHA-1 cases.

Key technical points from earlier in the thread remain relevant: the series introduces GIT_HASH_DEFAULT (now conditionally SHA-256) and GIT_HASH_ORIGINAL (SHA-1) constants, updates repository initialization logic, and maintains comprehensive test coverage. The only outstanding question is the long-term naming of the SHA-1 constant (GIT_HASH_ORIGINAL vs something more explicit like GIT_HASH_MUST_BE_SHA1), which remains a post-transition consideration.

The patch itself is minimal - just the conditional definition in hash.h and a related test-lib.sh adjustment - but represents the culmination of the series' core goal. With Junio's style feedback addressed, this appears ready for final inclusion pending any last-minute testing concerns.