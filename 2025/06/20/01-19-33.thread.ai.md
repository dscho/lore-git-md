New AI thread summary:

This is the first patch in a 10-part series preparing for potential future hash algorithm changes in Git. The patch introduces a new constant `GIT_HASH_DEFAULT` (currently set to `GIT_HASH_SHA1`) in hash.h, establishing centralized infrastructure for referencing the default hash algorithm. The change is to enable easier future transitions away from SHA-1 by requiring only a single constant update rather than numerous direct SHA-1 references across the codebase. This initial patch is purely preparatory with no behavior changes - it simply adds the constant definition. Subsequent patches in the series will presumably replace direct SHA-1 references with this constant. The change is motivated by long-term maintainability concerns around hash algorithm flexibility.