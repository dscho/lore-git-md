This is the second patch in Joerg Thalheim's v3 series improving `git imap-send` error messages, following up on Aditya Garg's initial work. The patch focuses on enhancing configuration error reporting with actionable advice.

The changes replace direct `fprintf(stderr)` calls with Git's proper `error()` and `advise()` APIs when reporting missing IMAP configuration. For both missing `imap.host` and `imap.folder` cases, the patch now provides specific `git config` command examples showing how to set these values, including sample values like `imaps://imap.example.com` for the host and `Drafts` for the folder.

The implementation follows the series' pattern of making error messages more user-friendly while maintaining proper internationalization support through `_()` wrappers. The multi-line advice is properly split with newlines and will display with "hint:" prefixes for each line due to using the `advise()` API. This addresses earlier feedback about message formatting and API usage consistency.

The changes are minimal (9 lines modified total) and focused, touching only the error reporting paths in `imap-send.c`. The patch builds on the terminology correction from patch 1/2 in this series (changing "store" to "folder" to match the actual config name) while maintaining the same style of improved error guidance.

This appears ready for merging as it completes the v3 series' goal of improving configuration error messages, with all prior review feedback incorporated. The remaining IMAP functionality issues noted earlier in the thread remain out of scope for this particular patch series.