This is part of the SHA-256 transition series, specifically introducing a new constant to clarify legacy hash algorithm cases. The patch adds `GIT_HASH_ORIGINAL` as a synonym for `GIT_HASH_SHA1`, but with explicit documentation that it represents cases where SHA-1 is implied by older formats (like bundle v1/v2) rather than being an arbitrary choice.

The change is small but meaningful - it helps distinguish between "we're using SHA-1 because we have to" versus "we're using SHA-1 because we chose to". This is part of the larger effort to make Git's hash algorithm handling more explicit as SHA-256 becomes the default. The patch appears straightforward and well-justified, with a clear commit message explaining the distinction.

While this is a simple addition, it's an important conceptual piece of the SHA-256 transition, helping future readers understand when SHA-1 usage is truly required versus optional. The change is minimal and unlikely to be controversial, though some might question whether the new constant is strictly necessary given it has the same value as `GIT_HASH_SHA1`.