This is a UX improvement patch for `git imap-send`, part of Joerg Thalheim's v2 series building on Aditya Garg's earlier error message work. The patch replaces terse error messages about missing IMAP configuration with more helpful guidance, showing users exactly how to set up `imap.host` and `imap.folder` with example commands.

The changes are straightforward: where the code previously just said "no IMAP host/folder specified", it now uses Git's `error()` and `advise()` APIs to show both the problem and the solution. For example, it suggests running `git config imap.host imaps://imap.example.com` when the host is missing. The advice can be disabled if users prefer minimal output.

This continues the thread's focus on improving `imap-send` usability while core IMAP functionality issues are addressed separately. The implementation looks clean - it's a small, focused change (9 lines total) that properly uses Git's error reporting infrastructure. The patch follows the project's style and responds well to earlier feedback about API usage. While the broader IMAP functionality questions noted in the thread remain open, this seems like a clear incremental improvement to merge.