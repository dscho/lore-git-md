This is a user experience improvement patch for the `imap-send` command, part of a larger series introducing OAuth2.0 support and folder management features (v19 09/10). The change enhances the progress output during message sending to include the destination folder name, making it clearer where messages are being delivered.

The patch modifies two functions (`append_msgs_to_imap` and `curl_append_msgs_to_imap`) to display messages like "Sending 1 message to Drafts folder..." instead of the more generic "sending 1 message". The implementation is straightforward - it adds the folder name from the server configuration to the progress output string.

This appears to be a well-contained quality-of-life improvement that builds on the series' broader folder management capabilities. The change is minimal (4 insertions, 2 deletions) and maintains consistency between the OpenSSL and libcurl backend implementations. As part of the mature v19 series, it likely represents a polished version of earlier iterations that have already undergone review.