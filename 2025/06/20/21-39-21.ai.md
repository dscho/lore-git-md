This is a review response in the ongoing "repo-info" command series, specifically addressing patch 4/7 which adds the `--allow-empty` flag. Junio C Hamano continues his architectural guidance, reiterating his preference from earlier in the thread to invert the flag's polarity - suggesting `--all` to show everything by default with an opt-out model, rather than the current `--allow-empty` opt-in approach. 

The technical changes in this patch are minimal, just the flag declaration and test updates, but the discussion touches on a core design question about the command's default behavior. Junio's position remains consistent with his earlier feedback about interface design (item 6 in the open questions list), though he doesn't introduce new technical concerns beyond what was already noted in the thread summary about the flag's justification and documentation gaps.

The review maintains the pattern of focusing on high-level design decisions rather than implementation details, with the mentor-mentee relationship evident in the signed-off-by chain. This exchange keeps unresolved the fundamental question of whether the empty/non-empty output behavior should be opt-in or opt-out, a decision that will likely require input from other contributors given Junio's clear preference stated here.