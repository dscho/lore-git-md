This is a response in the ongoing thread about improving `git imap-send` error messages. Junio C Hamano notes that there are conflicting reports about the state of `imap-send` functionality - while Jörg Thalheim's patch improves error messages for missing `imap.folder` and `imap.host` configurations, Junio mentions having heard that the entire `imap.folder` functionality is "completely broken". He points to an in-progress series (referenced by the merge commit `adbc0b5d`) that apparently fixes these deeper issues.

Junio suggests Jörg should rebase the error message improvements on top of these pending fixes rather than applying them to the current `master`. He also recommends two technical improvements: using the `advise()` API for the hint messages (which would make them consistent with Git's standard hint formatting) and separating the "what's imap store?" naming clarification from the error message improvements into distinct patches.

The response indicates this error message work may need to wait for or coordinate with more fundamental fixes to the IMAP functionality. The technical details mentioned include the specific merge command needed to incorporate the pending fixes (`git merge --no-ff -m 'Merge ag/imap-send-resurrection' adbc0b5d`) and the suggestion to modify the patch to use Git's standard hint display mechanism rather than direct `fprintf` calls.