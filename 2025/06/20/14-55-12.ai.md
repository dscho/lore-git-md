This is a technical discussion about repository format initialization in the SHA-256 transition series. Junio C Hamano is reviewing patch 5/10 from brian m. carlson's series, which changes how repository formats initialize their hash algorithm.

The patch modifies repository format initialization to use GIT_HASH_DEFAULT for new repositories while maintaining GIT_HASH_ORIGINAL (SHA-1) for reading legacy repositories. The key changes are in setup.c and setup.h, updating the REPOSITORY_FORMAT_INIT macro and read_repository_format() function.

Junio raises several technical questions about the implementation details:
1. Questions the need to set hash_algo to GIT_HASH_ORIGINAL before reading config
2. Examines the behavior when version is -1 (no core.repositoryformatversion set)
3. Notes potential confusion around how extensions are handled in version-less repositories
4. Points out that handle_extension_v0() may be affected by the clear_repository_format() call

The discussion reveals subtle interactions between repository version handling and hash algorithm selection, particularly around edge cases where repositories might not declare their format version but still try to use extensions. Junio seems generally satisfied with the approach after working through the logic, though he highlights some potentially confusing implementation choices that warrant careful review.

This email continues the thread's focus on precise technical details of the SHA-256 transition, specifically around backward compatibility mechanisms when reading repository formats. The conversation remains focused on implementation correctness rather than the broader design questions settled earlier in the thread.