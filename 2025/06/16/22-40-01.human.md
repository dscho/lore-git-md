This is a design discussion email in the ongoing `git repo-info` feature development thread. Junio C Hamano responds to Lucas Seiki Oshiro's implementation questions about the plaintext output format for the new command.

Junio clarifies his vision for the plaintext format, emphasizing strict parsing rules: one-item-per-line output (even for multi-line values), literal output for simple values, and `quote_c_style()` for values with special characters. He notes this approach balances simplicity for basic parsers while handling edge cases properly.

The email raises two new design considerations: whether to include keys in the plaintext output (suggesting `path.git-dir=/git/dir` format rather than just values), and whether to eventually support a long-running server mode similar to `git cat-file --batch-command`. These represent important interface decisions that will affect how scripts consume the command's output.

The discussion continues the thread's careful attention to output format design, with Junio providing architectural guidance while leaving room for the implementer to consider these new suggestions. The email doesn't propose concrete changes but rather steers the design philosophy, maintaining the thread's focus on creating a robust, script-friendly interface.