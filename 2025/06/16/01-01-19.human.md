This is a code style refinement in the ongoing memory leak fix for mailinfo.c's boundary handling. Junio C Hamano responds to Lidong Yan's suggestion to restructure the error handling in `handle_content_type()` to use a `goto out` pattern rather than direct cleanup before returning.

The proposed change would consolidate the boundary cleanup code at a single exit point, following a common Git codebase idiom. Junio agrees the approach looks good, though notes there are other potential cleanups (like changing the strbuf pointer type) that would be out of scope for this specific patch.

This comes after the thread has already resolved the core memory leak issue and attribution questions, leaving only stylistic improvements to consider. The goto pattern change would maintain the same memory safety guarantees while making the code's organization more consistent with Git's conventions. Junio's response suggests this version is likely to be accepted, though he hints at future opportunities for additional refactoring.