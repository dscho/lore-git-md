This is a documentation refinement in the ongoing merge/pull compact summary thread. Junio Hamano responds to Kristoffer Haugsbakk's suggestions about the `merge.stat` config documentation, agreeing that while `diffstat` could theoretically be an alias for `true`, it's better to introduce such tweaks later to avoid breaking existing users. 

Hamano also discusses the phrasing of default values in documentation, noting that while "if not specified, defaults to" isn't strictly necessary (since "defaults to" already implies that), he personally finds the longer form easier to read. He provides examples from existing Git docs showing both styles are already in use.

The exchange represents final polish on the already-accepted technical implementation of the compact summary feature, with no further action needed beyond potential future documentation tweaks. The core functionality remains unchanged - users can now trigger compact merge summaries via either `--compact-summary` flag or `merge.stat=compact` config.