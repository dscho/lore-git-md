This is a follow-up to a bugfix patch addressing a memory leak in `mailinfo.c`. The original patch (from Jinyao Guo) fixed a case where allocated memory for email boundary handling wasn't being freed when hitting the `MAX_BOUNDARIES` limit. The technical content remains unchanged - it's still the same straightforward 3-line fix adding proper cleanup in `handle_content_type()`.

The current email resolves a submission protocol issue raised by Kristoffer Haugsbakk regarding mismatched authorship between the From header and Signed-off-by line. Jinyao Guo explains this was due to using two Git identities and has now added both signatures. The patch itself remains technically sound - a clear fix for a specific memory leak case that was uncontested in the thread. The only discussion has been about proper attribution practices when submitting patches.