This is a documentation patch that clarifies the build process for Git's pre-built documentation. The patch adds explicit clone commands to the Documentation/Makefile, showing users how to properly fetch the git-htmldocs and git-manpages repositories needed for the quick-install target. The author notes that while these repositories exist under both the git organization and gitster user on GitHub, the gitster versions have the correct branch names (master) and repository naming that the build system expects.

The change is straightforward and addresses a potential point of confusion - the commit message explains that cloning from the wrong source (the git organization's htmldocs repo) would lead to build failures since it uses gh-pages instead of the expected master branch. The patch provides the correct clone commands that will work with the existing build system.

The patch appears to be a minor but useful quality-of-life improvement for developers working with Git's documentation build system. It's a small change (just 5 lines added) with clear motivation and no obvious issues. The author does note some uncertainty about the formatting of their commit notes, but this doesn't affect the actual patch content.