This is a documentation patch that adds clone instructions for pre-built Git docs repositories to the Documentation/Makefile. The patch clarifies that users should clone from the gitster GitHub account rather than the git organization, as the latter uses different branch names (gh-pages vs master) and repository naming conventions that can cause the quick-install-html target to fail.

The change adds explicit git clone commands showing how to properly set up the adjacent documentation repositories needed for the quick-install target. The author notes that the gitster repositories have the expected naming (git-htmldocs and git-manpages) and branch structure, while the potential confusion with the git/htmldocs repository which uses different conventions.

The patch is straightforward and appears to be correctly implemented, though the author expresses some uncertainty about the indentation style in the Notes section. This is a minor documentation improvement that helps users avoid a potential pitfall when setting up the documentation build environment. The change is contained to the Makefile comments and doesn't affect any actual build logic.