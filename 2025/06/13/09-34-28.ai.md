This is a follow-up discussion in the `git last-modified` feature series, focusing on implementation details and documentation refinements. Toon Claes responds to Patrick Steinhardt's review of the v2 patches, addressing several technical points while proposing some implementation improvements.

Key discussion points:
1. **EXPERIMENTAL marker**: Toon explains the rationale for marking the command as experimental (following `git-replay` precedent) but is open to removing it if consensus prefers. This relates to ongoing discussion about interface stability.

2. **Flag support clarification**: Confirms `-r` and `-t` flags are implemented despite missing tests, with plans to add test coverage.

3. **Code quality improvements**:
   - Unused `ret` variable removal in `cmd_last_modified()`
   - Converting `count` to a boolean in `add_from_revs()`
   - Proper cleanup using `diff_free()` instead of `clear_pathspec()`
   - Error handling improvement by making `last_modified_init()` return error codes

4. **Hashmap behavior**: Discussion about removing redundant commit tracking in `last_modified_entry` since entries are removed after first match.

5. **Documentation gaps**: Plans to add function-level documentation in `last-modified.h` and comments explaining the path filtering mechanism.

The exchange shows active refinement of the implementation, with Toon addressing most of Patrick's technical suggestions while maintaining the core behavior. This represents the polishing phase of the feature development, where architectural questions are settled and focus is on code quality and edge cases. The thread context shows this is part of a well-reviewed series with multiple contributors providing input on different aspects (performance, documentation, implementation quality).