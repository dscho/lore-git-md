Refactoring patch in a series improving submodule remote handling. This is part 6/7 of Glen Choo and Jacob Keller's ongoing work to make submodule remote lookup more robust, particularly when remotes are renamed.

The patch moves the `get_default_remote_submodule()` function in `builtin/submodule--helper.c` to appear after `resolve_relative_url()`. The commit message explains this is preparatory work for a future refactor where `get_default_remote_submodule()` will depend on `resolve_relative_url()`. 

This is a straightforward code reorganization with no behavior changes - the diff shows the function being moved down in the file with identical implementation. The change appears well-justified as it will make future changes easier to review by keeping dependent functions together.

This continues the series' trend of careful refactoring to support the broader goals of improving submodule remote handling and removing `the_repository` dependencies. The patch maintains the series' high quality with a clear commit message and no functional impact.