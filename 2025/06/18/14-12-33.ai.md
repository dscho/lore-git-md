Platform compatibility fix for `diff --no-index` regression on AIX/NonStop systems. This is a follow-up to the original patch that introduced a build failure on platforms without `d_type` in `struct dirent`.

Marc Branchaud submits a minor correction to Carlo Marcelo Arenas Belón's original patch, fixing two small typos in the commit message (changing "will fail" to "git fails" and "nake" to "naked"). The actual code change remains unchanged - it still replaces the direct `e->d_type` access with the portable `DTYPE(e)` macro wrapper in `diff-no-index.c`.

The thread context shows this is a straightforward portability issue that has already been reviewed and tested by Collin Funk on AIX 7.3. Marc's contribution is purely editorial, improving the commit message's clarity without altering the technical solution. The patch continues to address the regression from commit 09fb155f11 while maintaining consistency with Git's existing use of the `DTYPE()` macro for cross-platform dirent handling.

Key technical details remain the same as in the original patch: the change affects how directory entry types are checked during pathspec-limited diff operations, using the established abstraction rather than platform-specific struct members. No new functionality is added or removed - this is purely a build-fix for affected platforms.