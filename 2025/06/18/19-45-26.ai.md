This is a follow-up discussion in the Coccinelle build system improvements thread, specifically about the `dtype.cocci` patch that enforces use of the `DTYPE()` macro for portable `struct dirent` access.

Collin Funk responds to Junio Hamano's concern about potentially allowing binaries to silently misbehave on platforms without proper `d_type` fallback code. While agreeing this isn't ideal, Collin notes the practical challenge of verifying fallback implementations and points out that test failures (like in `git diff --no-index`) would at least some issues, though requiring manual investigation. 

The email also clarifies a side discussion about testing on AIX - Collin had initially thought libcurl was mandatory for testing but Junio corrected this. Collin confirms the AIX testing was quick regardless and shares results in a linked message. The core technical discussion remains focused on how to best handle platform portability concerns in the build system, with Junio advocating for more robust validation of fallback paths.