This is a follow-up to the Windows terminal handling bugfix, suggesting an additional refinement to the patch. Carlo Arenas Belón proposes modifying `save_term()` to explicitly zero `cmode_out` in non-DUPLEX mode, which would make the behavior more consistent with the existing fix in `restore_term()`. The change would ensure `cmode_out` is never left uninitialized, even in error paths.

The email raises an interesting question about whether the original motivation for this code (apparently related to vi behavior in Git for Windows) still exists, suggesting this might be an opportunity for further cleanup if that use case is no longer relevant. This shows good attention to both the immediate fix and potential technical debt in the Windows terminal handling code.

The proposed change is marked as untested, and the author is looking for confirmation from Junio Hamano and possibly Git for Windows maintainers about whether the additional cleanup would be appropriate. The thread context shows this is building on James Duley's original fix for an assertion failure during interactive operations like `git add -p`.