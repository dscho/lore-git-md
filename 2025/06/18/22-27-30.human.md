This is a response in the reftable performance investigation thread, confirming that the block size is indeed configurable via `reftable.blockSize`. Brian M. Carlson clarifies that while the default 4KB size can be increased (with JGit using 64KB as an example), there's a tradeoff - larger blocks improve write performance over network mounts but may degrade read performance for individual ref lookups. The response validates Haylin Moore's observation about configurable block sizes in the documentation while cautioning about the read/write tradeoffs. No specific recommendation is given beyond suggesting power-of-two sizes, leaving the optimal configuration as an environment-specific tuning exercise. The exchange moves the thread from investigation to actionable configuration advice.