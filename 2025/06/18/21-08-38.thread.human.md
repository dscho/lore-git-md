This RFC patch series from [author name] addresses a significant performance bottleneck in `git fetch --prune` when dealing with repositories containing many refs. The current implementation has O(N²) behavior that becomes painfully slow (410 seconds for 28k refs) due to inefficient dangling ref detection using nested string comparisons.

The proposed solution restructures the code to use an O(N*logN) approach matching `git remote prune`'s efficient algorithm. The first patch introduces the key optimization by collecting refs to prune in a string list upfront and moving warnings to after all prune operations. The second patch, which just received Junio's informal "Nice." comment, cleans up now-obsolete code by removing the unused `refs_warn_dangling_symref()` helper and related struct field.

While the optimization brings dramatic speedups (410s → 4s in testing), there remain open questions about whether to accept the changed warning message ordering (grouped rather than interleaved) for simplicity, or maintain exact output ordering for tooling compatibility. The performance gains may outweigh these concerns, and the thread appears to be moving toward acceptance with Junio's positive reaction to the cleanup patch. The series represents a meaningful improvement to a core command's scalability.