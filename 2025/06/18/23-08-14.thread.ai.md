Thread identity: Patch series improving submodule remote lookup logic (v4), authored by Glen Choo with contributions from Jacob Keller and Lidong Yan. Combines refactoring, bugfixes, and feature improvements to make submodule remote handling more robust, particularly when renamed remotes are involved. Now complete with 7 parts plus regression fix, with the new patch focusing on URL-based remote matching. Maintainer Junio C Hamano has acked v3 and will apply the series after addressing a regression.

Core problem: Submodule remote lookup was fragile with renamed remotes. The series transitions from name-based to URL-based lookup where possible, since parent projects know submodule URLs from .gitmodules/config. Part of broader `the_repository` removal effort. The final patch introduces URL-based matching while maintaining all fallback paths. A regression in part 1 (removal of `branch->merge_name`) was found in t5582 pull/fetch tests and has now been addressed by Lidong Yan.

Current status: v4 series complete and maintainer-acked (v3) pending regression fix. Part 7 adds `repo_remote_from_url()` for URL-based remote lookup. Previous parts: refactored remote.c to remove `the_repository`, improved memory management, moved path helpers, added repo_default_remote() for better fallback behavior, and relocated get_default_remote_submodule(). The regression in part 1 involved a segfault when `branch->merge_name` was removed despite some code paths still depending on its null-check behavior. Lidong Yan's fix updates `branch_has_merge_config()` to check `branch->set_merge` instead and uses `FREE_AND_NULL` in `merge_release()` to prevent double-free issues.

Key technical details:
- Part 7:
  - New `repo_remote_from_url()` iterates remotes to find URL matches
  - Modified `get_default_remote_submodule()` now:
    1. Gets submodule URL from config
    2. Tries URL-based lookup
    3. Falls back to existing name-based logic
  - Handles relative URLs via resolution
- Regression fix (Lidong Yan):
  - Updates `branch_has_merge_config()` to check `branch->set_merge` flag (introduced in v3) instead of null `branch->merge_name`
  - Uses `FREE_AND_NULL` in `merge_release()` for memory safety
  - Minimal changes (2 lines in remote.c) addressing critical t5582 segfault
  - Preserves expected null-check semantics while maintaining v3's simplification
  - Verified locally against failing t5582 tests
- Previous parts:
  - Added repo_default_remote() for robust fallback
  - Moved get_default_remote_submodule()
  - Removed `the_repository` from remote.c
  - Replaced branch->merge_name with set_merge flag (v3 change now properly handled)
  - Improved memory management
  - Moved path helpers to shared `dir.h`

Open questions: None remaining - regression fix completes the series. Maintainer can now apply the v4 series as-is.

Participant positions: Jacob Keller contributed parts 5-7 and earlier path-checking refactoring. Maintainer Junio C Hamano acked v3 with "Sounds nice. Will replace." but identified the regression in t5582. Lidong Yan analyzed and fixed the regression with a minimal change that preserves the series' goals while restoring test stability. The fix has been verified locally against t5582.

Related work: Completes submodule remote handling improvements within ongoing `the_repository` removal effort. Builds on previous submodule and remote refactoring. Regression case illustrates challenges in removing legacy fields during refactoring and importance of comprehensive test coverage.