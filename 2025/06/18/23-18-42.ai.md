This is a performance optimization discussion in the ongoing thread about improving `git fetch --prune` efficiency. Jacob Keller responds to Phil Hord's first patch in the series, which restructures the dangling ref detection to avoid O(N^2) behavior.

Keller confirms the performance improvement is significant enough to justify the change, noting that logarithmic scaling should be sufficient for practical purposes. He explicitly supports the patch's reordering of output messages (dangling ref warnings now appear after all prune operations rather than interleaved), which was a potential point of discussion in the original proposal.

The response clarifies one detail from the original submission - while the cover letter mentioned sub-second performance, the actual test case showed 4 seconds for pruning 28k refs from a 126k-ref repository (down from 410 seconds). Keller adds his Reviewed-by tag, indicating formal approval of the technical approach.

Key technical points confirmed in this exchange:
- The string_list sorting approach matches `git remote prune`'s implementation
- Output ordering change is deemed acceptable
- O(N*logN) performance is considered sufficient for now
- The patch maintains all functionality while dramatically reducing runtime

This moves the thread forward with explicit developer support for both the performance optimization and its behavioral side effects, increasing the likelihood of the series being accepted.