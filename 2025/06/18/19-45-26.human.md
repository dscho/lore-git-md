Follow-up discussion about Coccinelle portability rule for dirent.d_type access.

This email continues the discussion about the `dtype.cocci` patch that enforces use of the `DTYPE()` macro instead of direct `d_type` access in `struct dirent`. Collin Funk responds to Junio Hamano's concern about potentially allowing binaries that silently misbehave on platforms without proper fallback` code.

Collin acknowledges the concern but notes there's no obvious way to validate the fallback code at build time. He points out that test failures in `git diff --no-index` would reveal directory detection problems, though this requires manual investigation. The email also clarifies that libcurl isn't actually required for testing the diff changes, though Collin had already tested on AIX and shared results separately.

The exchange shows the patch is technically sound but leaves open the question of how to better handle platform fallback validation - a tradeoff between build-time safety checks and runtime detection. The discussion remains focused on improving cross-platform robustness while maintaining existing functionality.