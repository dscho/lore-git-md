This is a minor editorial correction to the ongoing portability fix for `diff --no-index` on platforms without `d_type` in `struct dirent`.

Kristoffer Haugsbakk submits a tiny but precise wording improvement to the commit message, changing "build in" to "build on" when describing affected platforms. This follows Marc Branchaud's earlier typo fixes in the same thread, maintaining the pattern of small documentation refinements to an otherwise settled technical solution.

The patch itself remains unchanged - it still replaces `e->d_type` with the portable `DTYPE(e)` macro in `diff-no-index.c`, a fix that Collin Funk has already verified works on AIX 7.3. This email continues the thread's trend of minor polish rather than substantive changes, suggesting the technical content is now considered complete and correct by contributors.

The thread demonstrates how Git's review process often involves multiple eyes catching small imperfections even after the core technical solution is agreed upon. With this correction, the commit message now has more precise language while maintaining all the key technical information about the regression and its fix.