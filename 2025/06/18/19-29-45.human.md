This is a follow-up discussion in the ongoing thread about Coccinelle build system improvements, specifically regarding the patch that enforces use of the `DTYPE()` macro for portable filesystem type checking.

Junio Hamano responds to Collin Funk's review of the `dtype.cocci` patch, which aims to catch direct accesses to the non-portable `d_type` member in `struct dirent`. While Collin had approved the patch with a Reviewed-by tag, noting it would help catch build failures on niche platforms, Junio expresses concern that the current approach still allows binaries to be built that might silently misbehave - suggesting he may want stricter validation of fallback code paths.

The email also touches on an unrelated `git diff --no-index` fix that Collin mentioned testing on AIX, with Junio clarifying that libcurl isn't needed for that particular test. The core discussion remains focused on improving platform portability through systematic Coccinelle rules, with Junio signaling potential follow-up work to strengthen the safety checks around filesystem type detection.