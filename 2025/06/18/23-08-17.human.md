This is a refactoring patch in Glen Choo's submodule remote lookup improvement series (v3, patch 3/7). Jacob Keller moves two path-checking helper functions (`starts_with_dot_slash` and `starts_with_dot_dot_slash`) from being duplicated in submodule--helper.c and submodule-config.c into dir.h as static inline functions. 

The change eliminates code duplication while maintaining identical functionality, using the existing `path_match_flags` infrastructure with `PATH_MATCH_XPLATFORM` (as opposed to the `_native` variants already in dir.h). The commit message notes some naming consideration - keeping the simpler names rather than adding a `_platform` suffix for consistency with the existing API.

This appears to be a straightforward preparatory change for later submodule refactoring work in the series, though the specific usage isn't detailed here. The patch is cleanly implemented with proper documentation comments and removes exactly the duplicated code it aims to consolidate.