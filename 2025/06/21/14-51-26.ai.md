This is a reply in the ongoing thread about Bulgarian (bg) localization updates for git-gui, specifically addressing the po-file format standardization proposal. Johannes Sixt responds to Alexander Shopov's suggestion about using `msgcat --no-location` to remove source location comments from po-files.

The reply expresses no objection to the `--no-location` proposal but raises a new concern about maintaining stable ordering in .pot/.po files. Sixt notes that even with location comments stripped (as demonstrated via `git show` flags), it's still difficult to identify the two actual translation changes in the patch due to line movement. The suggestion is that enforcing a consistent ordering of strings in the generated files would further improve patch readability by reducing noise from line number changes.

This introduces a new technical consideration beyond the original location-comment discussion - the potential benefit of stable string ordering in translation files. The comment implies this could be addressed either through the build infrastructure or the tools generating the .pot files. No specific implementation path is proposed, but the idea is framed as complementary to the original format standardization suggestion.

The email maintains a constructive tone focused on improving the translation workflow, while shifting focus slightly from file size/diff noise reduction to the related issue of string ordering stability. This suggests the thread may expand to consider both format and ordering improvements together.