This is a follow-up discussion in the ongoing thread about making `make coccicheck` fail when Coccinelle suggests code changes. The thread has already established consensus on the core idea, with minor implementation details being discussed.

Eric Sunshine weighs in on Carlo's suggestion to use `-eq` instead of `=` for numeric comparison in the Makefile test condition. He notes that Junio Hamano has previously expressed a preference for `=` in similar contexts, linking to a past discussion. This appears to be the final remaining point of discussion in what has otherwise been a straightforward and uncontroversial build system change.

The patch itself (already agreed upon in principle) will make the Coccinelle check more rigorous by failing when it finds potential fixes, rather than silently succeeding while generating patch files. This small but important change will help developers notice and address suggested code improvements. The current discussion is purely about shell syntax style in the implementation.