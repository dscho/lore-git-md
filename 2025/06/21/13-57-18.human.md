This is a technical follow-up in the ongoing discussion about symbolic port handling in git-credential-netrc. Maxim Cournoyer responds to Andreas Schwab's RFC6335 reference by proposing a more robust port validation approach using Scheme code as an illustrative example. The proposed solution would use `getservbyname()` to properly resolve both numeric and symbolic port names, returning the numeric equivalent when valid or false when invalid.

The email demonstrates how this would work in practice, showing test cases for numeric ports (465), valid symbolic ports ("smtps" resolving to 465), and unknown ports. This builds on earlier discussion about where port resolution should occur in the credential system, showing one concrete implementation approach that could be adapted for the netrc helper.

The exchange continues the thread's shift from a simple regex fix to deeper architectural questions about port handling consistency across Git's credential system. While the initial regex change was approved, this follow-up suggests a more comprehensive solution may be needed to properly support symbolic ports as documented in git-send-email. The discussion remains active with no clear resolution yet on whether port resolution belongs in helpers or calling code.