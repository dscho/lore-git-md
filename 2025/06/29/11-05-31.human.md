Documentation patch improving the `git-for-each-ref` man page formatting and consistency. Jean-Noël Avila follows up on earlier discussion about documentation standards in the `git refs list` proposal thread, applying similar improvements to the existing `for-each-ref` documentation.

The changeset standardizes option syntax formatting (adding spaces around pipe characters in alternatives, consistent use of `_<param>_` markup), tightens wording (removing redundant explanations), and fixes minor typos. Notable changes include making the `--format` option's parameter optional with `[=<format>]` syntax and clarifying the `--exclude` pattern matching rules.

This appears to be a straightforward documentation cleanup that aligns with the project's style guidelines, building on the earlier review of the new `git-refs.adoc` file. The patch doesn't introduce new content but improves readability and consistency of the existing documentation, which will benefit both the current command and any future `refs list` implementation.