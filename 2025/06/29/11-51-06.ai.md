Refactoring patch extending the parse-options type precision work to OPTION_NEGBIT, part 4/6 in the series. This follows the established pattern of adding type safety and bounds checking to option handling, now covering the negation bit flag case.

The patch modifies OPTION_NEGBIT to use the same precision-based approach introduced earlier in the series, with several key changes:
- Adds precision specification for the rebase.c's --no-stat flag (sizeof(options.flags))
- Updates OPTION_NEGBIT handling in parse-options.c to use get_int_value/set_int_value helpers
- Includes OPTION_NEGBIT in parse_options_check() validation
- Updates the OPTION_NEGBIT macro to automatically set precision based on target variable size

Technical details mirror previous patches in the series:
- Maintains backward compatibility while adding internal safety checks
- Uses signed_int_fits() for bounds verification
- Preserves existing API surface
- Follows the pattern of one option type per patch

The changes affect:
- rebase.c's flag handling (--no-stat specifically)
- Core parse-options machinery (parse-options.[ch])
- Type safety infrastructure (get_int_value/set_int_value helpers)

This continues the incremental approach seen in patches 1-3 (PARSE_OPT_CMDMODE, OPTION_SET_INT, OPTION_BIT), with OPTION_BITOP and OPTION_COUNTUP remaining for future patches. The implementation quality remains consistent with the series' focus on precise type handling without user-facing changes.