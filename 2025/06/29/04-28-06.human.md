This is a technical cleanup in the string-list series, specifically addressing sign comparison warnings (-Wsign-compare) in the binary search implementation. The patch modifies the `get_entry_index()` function to use `size_t` consistently instead of `int`, which requires changing the binary search algorithm to avoid the previous -1 initialization trick.

The key changes are:
1. Initializing the search bounds to 0/nr instead of -1/nr
2. Simplifying the loop condition to `left < right`
3. Adjusting the search logic to use `middle + 1` when moving left
4. Removing the DISABLE_SIGN_COMPARE_WARNINGS flag now that the code is clean

This is part of shejialuo's v3 series modernizing string-list tests and implementation, which has already incorporated extensive reviewer feedback. The changes maintain the same behavior while making the code more type-safe. The patch appears well-justified and cleanly implemented, continuing the series' trend of careful modernization work.