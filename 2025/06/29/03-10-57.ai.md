This is a reply in the ongoing `git apply --intent-to-add` bugfix thread. Lidong Yan responds to Raymond Pasco's cover letter, agreeing with the core issue but offering a slightly different technical perspective on the solution.

Yan confirms that the fundamental problem is that `git apply --intent-to-add` fails to properly read the index before updating it, only handling new files correctly. They specifically highlight that patch 2/5 in the series solves this core issue by making Git read the index file properly before updating intent-to-add cache entries.

The reply makes an important clarification about patch 3/5's behavior - it aligns `git apply --intent-to-add` with `git add --intent-to-add` by making both commands ignore intent-to-add operations for files that already exist in the index. This ensures consistent behavior between the two commands when dealing with existing index entries.

The email doesn't introduce new technical changes but provides additional context about the relationship between the patches and existing Git behavior. It reinforces that the series both fixes broken functionality (patch 2) and improves consistency with related commands (patch 3). The tone suggests agreement with the overall approach while adding this explanatory note about the behavioral alignment.