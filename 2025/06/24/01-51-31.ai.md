This is the final patch in the v3 series addressing symbolic port name handling in git-credential-netrc, implementing Junio Hamano's requested changes. The patch focuses on test adjustments and helper function improvements to properly handle symbolic ports as ports rather than protocols.

Key changes in this version:
1. Renames the validation function from `is_port` to `port_num` to better reflect its dual role of validation and conversion
2. Simplifies the getservbyname implementation by removing redundant checks
3. Updates tests to properly reflect the new handling of symbolic ports (like 'imaps') as ports rather than protocols
4. Maintains backward compatibility while fixing the credential helper's behavior

The implementation now shares validation logic between git-credential-netrc and git-send-email through the new `port_num` function in perl/Git.pm. This function handles both numeric and symbolic ports, validates the 16-bit numeric range, and implements getservbyname resolution when needed.

The test changes demonstrate the corrected behavior where symbolic ports are now properly captured as ports in the credential hash/array rather than being misclassified as protocols. This fixes real-world issues like Emacs authinfo.gpg compatibility while maintaining all existing functionality.

With these final adjustments, the v3 series provides a complete technical implementation of helper-side symbolic port resolution, addressing all feedback from previous rounds. The implementation is now robust, well-tested, and maintains backward compatibility while fixing the original issue. The only remaining question is Junio Hamano's pending decision on whether to accept this helper-side resolution approach despite the credential protocol documentation's current wording about numeric ports.