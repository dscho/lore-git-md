Thread identity: Junio Hamano's "What's cooking in git.git" status report thread tracking the progression of in-flight Git topics through integration branches (seen/next/master/maint), now including brian m. carlson's SHA-256 by default series.

Core problem/goal: Provide maintainer-curated visibility into the state of all active development efforts in the Git project, with two major ongoing efforts:
1. General pipeline tracking (Patrick Steinhardt's preload-index changes, batched reference updates, test improvements)
2. SHA-256 transition (brian m. carlson's series to make SHA-256 default in Git 3.0+)

Current status updates:
- Master branch includes: Patrick Steinhardt's preload-index changes, `git stash` submodule/pathspec fixes, `git send-email` threading improvements, merge display leak fixes, and Phillip Wood's subtree GPG signing support
- Next branch additions: Jeff King's `test-seq-format` helper, submodule remote lookup cleanup, IMAP send fixes, brian m. carlson's stash export/import, and ps/contrib-sweep
- Karthik Nayak's `kn/fetch-push-bulk-ref-update` cleared for re-inclusion in 'next' after fix-up branch addresses reference deletion handling
- brian m. carlson's SHA-256 by default series (original 10-commit submission) flagged as "Needs review" - v2 addressing Junio's comments expected soon, delayed slightly by concurrent `index-pack` SHA-256 interop work

Key technical details:
- `the_repository` removal progresses with preload-index changes (object storage layer)
- Batched reference updates implementation in `receive-pack.c`, `send-pack.c`, and `refs.c` now with fixes for reference deletions and error cases
- SHA-256 transition work modifies core infrastructure (`hash.h`, `setup.c`) and test handling (`t5300`, `t4042`, `t1007`)
- Introduces constants `GIT_HASH_ORIGINAL` and `GIT_HASH_DEFAULT` while maintaining legacy format compatibility
- Changes behavior when outside repository context to use default hash algorithm

Open questions/loose ends:
- Oswald Buddenhagen's rebase instruction format validation patch needs reroll with sign-off and message cleanup
- brian m. carlson's SHA-256 series requires v2 addressing Junio's feedback
- Previous report's noted topics still needing review responses/rerolls remain pending

Participant positions:
- Patrick Steinhardt: Continuing object storage refactoring
- brian m. carlson: Preparing v2 of SHA-256 by default series, notes delay due to `index-pack` interop work
- Karthik Nayak: Addressed feedback on batched reference updates
- Oswald Buddenhagen: Still needs to resubmit rebase instruction format validation fix
- Junio Hamano: Flagged SHA-256 series as needing review, awaiting v2

Related work:
- Builds on years of SHA-256 preparatory work
- Connects to ongoing `index-pack` SHA-256 interop effort
- Continues long-running efforts: `the_repository` removal, test modernization, batched reference updates
- Rebase instruction format validation relates to sequencer robustness work

The thread now tracks both the general integration pipeline and the major SHA-256 transition effort, with brian m. carlson indicating a v2 of the hash algorithm changes is imminent. Junio Hamano's review feedback will be incorporated in the update, while the concurrent `index-pack` work suggests broader SHA-256 integration is progressing in parallel. The batched reference updates have cleared their hurdles, while the rebase instruction format validation remains pending a corrected submission.