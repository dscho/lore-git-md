This is a performance discussion related to the test infrastructure changes adding a `-f` format option to `test_seq`. Eric Sunshine provides historical context about a similar change that was rejected due to performance concerns with shell-based sequence generation versus `awk`. He notes that while the current pure-shell implementation of `test_seq` is likely fine for small sequences, there might be cases (like generating 10,000 refs) where an `awk`-based approach would be faster. 

Eric thoughtfully considers a hybrid approach that could switch between shell and `awk` implementations based on sequence size, though he presents this as an idea for future consideration rather than a blocker for the current patch series. The message serves as a useful performance-oriented footnote to the already-merged changes, documenting potential optimization paths should performance become a concern in real test runs.