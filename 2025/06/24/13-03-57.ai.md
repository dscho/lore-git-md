This is a technical discussion about output formats and scope for the new `git repo-info` command, part of Lucas Seiki Oshiro's GSoC project.

Phillip Wood continues the discussion about output format concerns, advocating for NUL-terminated plaintext output and raising important questions about path encoding in JSON. He explains that the proposed NUL-terminated format (key+LF+value+NUL) would handle newlines in values safely, matching Git's existing conventions in commands like `git config --list -z`. 

The email highlights two key technical challenges: First, the JSON output format's incompatibility with non-UTF8 paths, where Phillip suggests base64 encoding as a more robust solution than Lucas's proposed UTF-8 validation approach. Second, Phillip argues for expanding the command's scope to include path resolution functionality currently in `git rev-parse --git-path`, warning that omitting this could perpetuate unsafe path construction practices.

The discussion reveals a tension between the command's focused design goals and practical scripting needs. Phillip makes a case for a unified interface for repository settings (combining aspects of `rev-parse`, `var`, and `config`), while Lucas expresses concern about scope creep. This exchange shows the command's design is still evolving, with important decisions pending about both low-level output formatting and high-level functionality boundaries.