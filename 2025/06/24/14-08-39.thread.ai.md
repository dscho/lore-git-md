Git daemon signal handling and child process tracking redesign (v4/RFC) has now fully resolved the SA_RESTART debate while advancing two distinct solution paths. The thread has reached consensus on eliminating SA_RESTART manipulation, with remaining work split between Phillip Wood's documentation solution and Carlo Marcelo Arenas Belón's pipe-based tracking approach.

Current state:
1. **Closed paths**:
   - SA_RESTART toggling approach abandoned
   - Build system variable debate resolved as unnecessary

2. **Active solutions**:
   - *Documentation-focused* (Phillip Wood):
     - Requires no code changes to signal handling
     - Will document platform-specific EINTR behaviors
     - Maintains current git-daemon's interrupt handling
     - Has Junio Hamano's explicit support

   - *Pipe-based tracking* (refined RFC):
     - Uses `signal_pipe` in `struct child_process`
     - Child notification via pipe monitoring with poll()
     - Implementation improvements:
       * Mandatory pipe creation (no silent failures)
       * Windows support now focused on:
         - `sigaction(SIGCHLD)` behavior (signal 17 vs standard signals)
         - Current Windows `signal()` doesn't set errno despite documentation
         - Testing shows signal number 20 makes Windows CRT work as documented
         - Debate between:
           - Phillip: Make Windows `sigaction()` POSIX-consistent (set errno=EINVAL)
           - Carlo: Initially match current Windows `signal()` behavior (no errno) to avoid regressions
       * Performance benchmarking pending

Key technical consensus:
- SA_RESTART code paths will be removed entirely
- Documentation must cover platform signal behavior differences
- Pipe approach requires resolution of Windows-specific issues:
  - Whether to maintain current Windows behavior or enforce POSIX consistency
  - SIGCHLD (signal 17) vs standard signal handling differences
  - Testing reveals signal number 20 behaves as documented

Participant positions:
- Phillip Wood: Driving both documentation standards and pipe implementation rigor, advocates immediate POSIX-consistent Windows behavior
- Carlo Marcelo Arenas Belón: Focused on pipe refinements, prefers transitional Windows backward compatibility
- Junio C Hamano: Firmly supports eliminating SA_RESTART dependence
- Johannes Schindelin: Expected to provide Windows-specific expertise to resolve signal handling debate

Open questions now focused on:
- Windows implementation details for pipe tracking:
  - Immediate decision on `sigaction(SIGCHLD)` behavior (POSIX vs backward compatibility)
  - Whether to standardize on signal number 20 for Windows
- Final decision between documentation-only and pipe approaches
- Performance impact of pipe monitoring
- Documentation scope and location

Implementation roadmap:
1. Immediate: Resolve Windows signal handling behavior (awaiting Johannes Schindelin input)
2. Concurrent: Platform behavior documentation (common to both solutions)
3. Near-term: Pipe prototype performance testing
4. Decision: Windows viability assessment for pipe approach

The latest discussion has narrowed to Windows-specific signal handling details, with Phillip advocating for immediate POSIX consistency while Carlo prefers transitional backward compatibility. Testing has revealed that signal number 20 behaves as documented on Windows, potentially offering a path forward. Johannes Schindelin's Windows expertise may be needed to resolve the technical debate before either solution can be finalized.