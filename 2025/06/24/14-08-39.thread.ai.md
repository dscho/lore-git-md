Here is the updated AI thread summary incorporating the new email:

Git daemon signal handling and child process tracking redesign (v4/RFC) continues to refine the pipe-based approach while maintaining consensus on eliminating SA_RESTART manipulation. The discussion has narrowed to Windows-specific implementation details, with Carlo Marcelo Arenas Belón and Phillip Wood debating the tradeoffs between POSIX consistency and backward compatibility.

Current state:
1. **Closed paths**:
   - SA_RESTART toggling approach abandoned for Unix platforms
   - Build system variable debate resolved as unnecessary

2. **Active solutions**:
   - *Documentation-focused* (Phillip Wood):
     - Requires no code changes to signal handling
     - Will document platform-specific EINTR behaviors
     - Maintains current git-daemon's interrupt handling
     - Has Junio Hamano's explicit support

   - *Pipe-based tracking* (refined RFC):
     - Uses `signal_pipe` in `struct child_process`
     - Child notification via pipe monitoring with poll()
     - Implementation improvements:
       * Mandatory pipe creation (no silent failures)
       * Windows-specific considerations:
         - Testing confirms signal number 20 behaves as documented
         - Debate between:
           - Phillip: Enforce POSIX consistency (set errno=EINVAL)
           - Carlo: Maintain current Windows behavior (no errno) for backward compatibility
         - Carlo argues SA_RESTART toggling still needed for OpenBSD zombie cleanup
       * Performance benchmarking pending

Key technical consensus:
- SA_RESTART code paths will be removed for Unix platforms
- Documentation must cover platform signal behavior differences
- Pipe approach requires resolution of Windows-specific issues:
  - Whether to maintain current Windows behavior or enforce POSIX consistency
  - Testing confirms signal number 20 behaves as documented

Participant positions:
- Phillip Wood: Advocates for POSIX-consistent behavior on Windows and simpler solutions
- Carlo Marcelo Arenas Belón: Defends transitional approach, maintains some SA_RESTART code needed for OpenBSD
- Junio C Hamano: Firmly supports eliminating SA_RESTART dependence
- Johannes Schindelin: Expected to provide Windows-specific expertise (not yet engaged in this sub-thread)

Open questions:
- Windows implementation details:
  - Final decision on `sigaction(SIGCHLD)` behavior (POSIX vs backward compatibility)
  - Whether to standardize on number 20 for Windows signals
  - Resolution of Carlo's OpenBSD zombie cleanup concerns
- Performance impact of pipe monitoring
- Documentation scope and location

Implementation roadmap:
1. Immediate: Resolve Windows signal handling behavior (Phillip vs Carlo debate)
2. Concurrent: Platform behavior documentation (common to both solutions)
3. Near-term: Pipe prototype performance testing
4. Decision: Windows viability assessment for pipe approach

The latest exchange reinforces the technical divide between Carlo's position (defending some SA_RESTART code for OpenBSD and Windows backward compatibility) and Phillip's push for POSIX consistency. While the pipe approach remains uncontested, the Windows-specific implementation requires resolution before either solution can be finalized. Johannes Schindelin's Windows expertise may still be needed to break the deadlock on signal handling behavior.