Git daemon signal handling and child process tracking redesign (v4/RFC) with three competing approaches under evaluation: SA_RESTART toggling (original series), documentation-only (Phillip Wood), and new pipe-based tracking (RFC). Core issue involves platform-specific signal handling behaviors (SA_RESTART, EINTR) and race conditions in child process cleanup, with Windows SIGCHLD inconsistencies adding complexity.

Current technical landscape:
1. Original SA_RESTART approach (Carlo Marcelo Arenas Belon):
   - set_sa_restart() helper in daemon.c
   - Platform-specific handling for poll() interruptions
   - Windows sigaction() behavior debates (errno=EINVAL vs matching signal())

2. Minimal documentation approach (Phillip Wood):
   - Remove SA_RESTART manipulation
   - Document platform behaviors
   - Rely on git-daemon's EINTR handling (contested by Carlo)

3. New pipe-based tracking RFC (now two patches landed):
   - Introduces `parent_ipc_in` field in `struct child_process`
   - Child closes pipe descriptor to notify parent of exit
   - Parent monitors pipes via poll() in signal-free design
   - Dynamic pollfd array management with O(n) child scanning
   - Windows currently excluded due to pipe differences
   - Removes SIGCHLD handler entirely

Key technical details under debate:
- Historical EINTR safety assumptions in git-daemon (disputed evidence)
- Windows signal handling quirks (SIGCHLD=17 vs MSVC's 20, errno setting)
- Performance impact of pipe-based tracking (fd array scaling, O(n) scans)
- Accidental pipe closure edge cases
- Platform support matrix (Solaris, AIX, QNX, NonStop, Windows exclusion)
- Implementation completeness of pipe approach (now two patches)

Participant positions:
- Phillip Wood: Advocates minimal solution, provided commit evidence for EINTR safety, prefers POSIX sigaction semantics on Windows
- Carlo Marcelo Arenas Belon: Implemented original SA_RESTART solution, now proposing pipe alternative, identified Windows inconsistencies, suggests involving Dscho
- Junio C Hamano: Initially endorsed Phillip's view, now observing technical debate

Open questions:
- Resolution of EINTR safety debate (historical analysis needed)
- Architectural choice between three approaches
- Windows signal handling standardization vs pipe adaptation
- Performance evaluation of pipe-based tracking (O(n) scanning bottleneck)
- Documentation requirements for platform behaviors
- Windows pipe implementation feasibility
- Scaling characteristics with many children (fd limits, scan costs)

Related work:
- Linux pidfd API (not used for portability)
- Historical run_command changes affecting EINTR assumptions
- Windows signal() vs sigaction() implementation history

The pipe-based approach now has two concrete implementation patches demonstrating a complete signal-free alternative, though Windows support remains unimplemented and performance characteristics need evaluation. The architectural debate continues between the three approaches, with the pipe implementation providing working code that could avoid signal handling complexities if the performance and Windows adaptation prove viable. The thread awaits comparative analysis of the approaches, particularly regarding the pipe method's scaling and Windows compatibility.