Bugfix continuation in the Git daemon signal handling series, specifically addressing the empty signal handler question raised by Junio. 

Carlo Marcelo Arenas Belon clarifies that removing the empty child_handler() is not viable, as doing so would break EINTR behavior in poll() on certain systems, even when attempting to force it via siginterrupt(). This response provides important technical justification for maintaining the empty handler despite its seemingly redundant appearance. The explanation reveals deeper platform-specific signal handling nuances where the mere presence of a handler (even empty) affects system call interruption behavior differently than no handler at all.

The email advances the thread by closing one open question (empty handler necessity) while demonstrating the complexity of cross-platform signal behavior that motivates the series' architectural choices. Carlo's response reinforces the technical correctness of using sigaction() while implicitly acknowledging the implementation aesthetics concerns raised earlier in the thread. The series remains technically sound but still needs resolution on code organization and naming issues before finalization.