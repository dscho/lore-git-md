This is a technical discussion about output format design for the new `repo-info` command, specifically addressing Phillip Wood's NUL-terminated output proposal.

Junio Hamano weighs in on the NUL-terminated output format discussion, agreeing with Phillip Wood's approach of using `<key><LF><value><NUL>` format. He notes that since the `repo-info` command controls the key generation, they can safely assume keys won't contain LFs, making the parsing unambiguous. Junio also draws a parallel to `git config --list -z`'s handling of component names, confirming that the same safety guarantees apply there regarding LF and NUL bytes in variable names.

The email reinforces the technical soundness of Phillip's proposal while implicitly endorsing its adoption for the command's plaintext output mode. This moves the thread closer to resolution on one of the key open questions about output format initialization and termination. The brief exchange suggests growing consensus around this aspect of the interface design, though it doesn't address the remaining open questions about JSON encoding or path resolution scope.