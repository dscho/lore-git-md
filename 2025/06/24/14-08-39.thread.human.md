The thread has progressed from debating the core signal handling approach to working through Windows-specific implementation details for the new design. After reaching consensus to eliminate SA_RESTART manipulation entirely, the discussion has narrowed to two parallel tracks: Phillip Wood's documentation-focused solution and Carlo Marcelo Arenas Belón's pipe-based tracking approach.

The latest email focuses on a technical debate between Phillip and Carlo about how Windows' compatibility layer should handle SIGCHLD signals. Carlo wants to maintain the current behavior where Windows' `signal()` returns -1 without setting errno, while Phillip argues for making Windows' `sigaction()` follow standard POSIX semantics by setting errno=EINVAL. This is a nuanced discussion about maintaining consistency versus avoiding potential regressions during the transition. Both are testing Windows' actual behavior, with Carlo suggesting they involve Johannes Schindelin (the Git for Windows maintainer) for additional expertise.

While this Windows-specific discussion continues, the broader thread has largely settled on eliminating SA_RESTART manipulation, with the remaining work focused on either documenting platform behaviors (Phillip's approach) or implementing the more robust pipe-based tracking (Carlo's approach). The Windows signal handling question is a secondary consideration that needs resolution regardless of which primary solution path is ultimately chosen.