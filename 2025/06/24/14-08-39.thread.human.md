This thread addresses signal handling fixes in the Git daemon, particularly for OpenBSD and AIX systems where improper signal handling was causing zombie processes and crashes. The series has evolved through three versions, with ongoing discussion between the original author and Junio about the best architectural approach - whether to modernize with sigaction() or take a more minimal approach using siginterrupt().

The latest version adds a NO_SIGINTERRUPT build flag to handle platform-specific poll() behavior while maintaining backward compatibility. Junio has been consistently pushing for simpler, more focused changes, questioning some of the broader modernization attempts. Meanwhile, Carlo Marcelo Arenas Belón contributed a fix adding the UNUSED attribute to an empty handler function.

In the most recent email, Junio provided a focused style review on Carlo's SA_RESTART fallback patch, pointing out two minor formatting issues that don't affect functionality but align better with Git's coding conventions. This continues Junio's pattern of close attention to code quality throughout the series, though the core architectural debate about signal handling approaches remains unresolved.

The technical issues are all addressed at this point - OpenBSD zombies are fixed, AIX crashes resolved, and cross-platform coverage is complete. What remains are some style tweaks and the higher-level decision about which architectural path to take for the signal handling implementation. Junio appears to favor the simpler siginterrupt() approach over the more comprehensive sigaction() modernization.