This thread addresses signal handling improvements in the Git daemon, specifically targeting zombie process cleanup on OpenBSD and crashes on AIX. The series modernizes the code by replacing legacy `signal()` calls with `sigaction()`, allowing proper flag specification and removing manual handler rearming requirements on SysV systems.

Junio C Hamano has reviewed the first patch, noting its benefits for broader platform compatibility and suggesting minor commit message corrections. The second patch, which focuses on `daemon.c`, received similar attention from Junio - he caught a typo and questioned whether the empty child handler remains necessary now that manual rearming is eliminated. Carlo Marcelo Arenas Belon contributed a fixup marking an unused parameter in the handler function for clarity.

The changes consolidate platform-specific signal behavior handling while maintaining backward compatibility. The series introduces new infrastructure in `compat/posix.h` and touches multiple build systems, but the core fixes for OpenBSD and AIX will come in later patches that implement explicit EINTR handling during poll() operations. While the immediate changes are straightforward, Junio's comments suggest we may see further simplifications to the handler implementation in subsequent revisions.