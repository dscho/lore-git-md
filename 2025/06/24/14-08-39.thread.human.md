This thread addresses a complex signal handling issue in the Git daemon, where different platforms exhibit varying behaviors around interrupted system calls during signal handling. The discussion has centered on two solutions: toggling the `SA_RESTART` flag (the current approach) versus implementing a self-pipe mechanism.

The core problem involves zombie process cleanup on OpenBSD and crashes on AIX, stemming from platform-specific behaviors with `SA_RESTART` in `poll()` operations and race conditions between signal handlers. Junio Hamano and Phillip Wood have validated that the self-pipe approach would solve both issues more robustly, while Carlo Marcelo Arenas Belón has been maintaining and improving the current `SA_RESTART` toggling implementation.

The latest email from Carlo provides important technical clarifications about the `USE_NON_POSIX_SIGNAL` build flag, explaining that it serves dual purposes: working around missing `SA_RESTART` support and handling Windows compatibility. The email confirms that while `sigaction()` is preferred when available, platform quirks - particularly on AIX and Windows - complicate the current implementation. This reinforces the advantages of the self-pipe approach, which would avoid these platform-specific complexities entirely.

The thread remains at a decision point between these two valid solutions, with the technical discussion increasingly favoring the self-pipe for its portability and robustness. Junio's prior validation of this approach carries significant weight, though the final decision will need to weigh implementation effort against long-term maintenance benefits. The latest email has moved the discussion forward by providing concrete details about platform-specific behaviors that any solution must address.