Here's a human-readable summary of the thread's current state:

The thread began with Carlo Marcelo Arenas Belón's patch series addressing git-daemon signal handling issues, particularly zombie process cleanup on OpenBSD and crashes on AIX. The initial approach toggled the SA_RESTART flag during poll() operations, but Phillip Wood successfully argued for a simpler solution that would document platform behaviors rather than manipulate signal handling flags. Junio Hamano supported this direction, though Carlo raised concerns about whether historical changes had made the code more fragile to interrupted system calls.

The discussion has taken a new turn with Carlo's RFC patch introducing a self-pipe mechanism - a more fundamental architectural solution to the race condition between SIGCHLD delivery and poll() interruption. This approach, which Phillip had previously suggested, uses a pipe that the signal handler writes to when children exit, ensuring the event loop wakes up promptly. While this elegantly solves the race condition, it comes with some performance overhead from the additional pipe I/O.

Key participants remain engaged: Phillip continues to advocate for minimal viable solution (now with detailed code analysis supporting his position), Carlo is driving the technical exploration of alternatives, and Junio is observing the debate while clearly preferring simpler approaches when viable. The self-pipe RFC represents a middle ground - more involved than documentation but less intrusive than the original SA_RESTART manipulation.

The thread now has three viable paths forward: (1) Phillip's minimal documentation approach, (2) Carlo's original SA_RESTART solution, or (3) this new self-pipe mechanism. The decision point centers on how much complexity is justified to handle what all agree is a rare race condition, with tradeoffs between code simplicity, platform coverage, and architectural cleanliness.