This is a review response in the ongoing Git daemon signal handling bugfix series, specifically addressing patch 2/3 which modernizes signal handling with `sigaction()`. Junio C Hamano questions the necessity of this change, suggesting that using `siginterrupt()` on supported platforms might be a simpler solution that achieves the same goal without introducing new complexity. He notes that the current `sigaction()` approach doesn't seem to provide clear benefits for the series' objectives.

The technical context involves platform-specific signal handling behaviors, particularly around `SA_RESTART` and `SIGCHLD` processing. The original change aimed to standardize on BSD semantics and eliminate the need for manual signal handler rearming. Junio's feedback implies the series might be overcomplicating the solution when a more minimal approach could work.

This exchange highlights an architectural decision point in the series - whether to pursue the `sigaction()` modernization path or adopt a more targeted `siginterrupt()` solution. The thread history shows this isn't the first time Junio has questioned design choices in this series, having previously raised concerns about empty handler functions and code organization. The resolution of this discussion will likely influence the final shape of the signal handling improvements.