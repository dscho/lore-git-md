This is a technical discussion in the ongoing signal handling bugfix series for the Git daemon. Chris Torek provides historical context about Unix signal handling evolution to explain why an empty signal handler is necessary in the current patch series.

The email traces the messy history of signal handling from early Unix (V6/V7) through BSD variants to POSIX standardization, showing how implementation quirks and backward compatibility requirements led to the current situation where an empty handler function is sometimes needed for correct behavior. Chris confirms Carlo's earlier assertion that removing the handler would break EINTR behavior on certain platforms, though notes this stems from historical baggage rather than clean design.

The discussion helps justify the series' current approach of using sigaction() with an empty handler, though the historical perspective also highlights why the interface remains awkward. This isn't proposing any code changes, but provides valuable context for understanding the implementation choices in the patch series. The email reinforces that the current solution, while not elegant, correctly handles the platform-specific quirks that have accumulated over decades of Unix signal handling evolution.