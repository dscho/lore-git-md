This is the first patch in a series addressing signal handling issues in the Git daemon, specifically focusing on build system changes and platform compatibility.

The patch moves platform-specific SA_RESTART definitions from compiler flags in config.mak.uname into a centralized location in compat/posix.h. It removes the custom COMPAT_CFLAGS entries for NonStop and QNX systems that previously defined SA_RESTART=0, instead providing a default definition in the header file when the system doesn't provide it. This consolidation makes the behavior more maintainable and visible, setting up infrastructure that will be used by subsequent patches in the series to properly handle signals across different operating systems.

The change is straightforward - it adds a fallback definition of SA_RESTART as 0 (disabled) when not provided by the system headers, which matches the existing behavior but in a more maintainable way. The patch removes the now-redundant compiler flag definitions for NonStop and QNX platforms. This is a preparatory change for the later patches that will modernize signal handling in daemon.c.

The patch appears well-scoped and correctly implemented, with a clear commit message explaining the change. It touches only the expected build system files and the new compat/posix.h location, without introducing any behavior changes at runtime. The change follows Git's established patterns for platform compatibility code.