This is a bugfix follow-up to the Git daemon signal handling series, specifically addressing a minor issue in patch 3/3. The series modernizes signal handling to fix zombie process cleanup on OpenBSD and crashes on AIX.

Carlo Marcelo Arenas Belón submits a small fixup for the child_handler() function signature in daemon.c, adding the UNUSED attribute to the signo parameter. This matches the function's implementation which doesn't use the parameter (as noted in the comment about it being an empty handler). The change is purely cosmetic/compiler-hint related and doesn't affect functionality.

The fixup aligns with the series' broader goal of cleaning up signal handling code. Since the handler is empty and only exists to interrupt system calls, marking the unused parameter makes the intent clearer and prevents compiler warnings. This follows the pattern established in the main series of modernizing and hardening the daemon's signal handling infrastructure.

The change is straightforward and uncontroversial, though it does highlight that the series could benefit from a final review pass to catch such minor issues before submission. The fixup doesn't introduce any new platform-specific considerations or build system impacts beyond what was already covered in the main series.