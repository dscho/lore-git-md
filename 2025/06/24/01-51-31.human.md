This is the final patch in a v3 series addressing symbolic port name handling in git-credential-netrc. The patch implements several requested changes from Junio Hamano's review of v2, including renaming the validation function to `port_num` and simplifying the getservbyname return handling. 

The key change here is fixing how the test suite handles symbolic ports (like 'imaps') which were previously incorrectly captured as protocol values but now properly join with the hostname as ports. The test adjustments reflect this corrected behavior. The shared `port_num` function now provides unified validation for both numeric and symbolic ports across git-credential-netrc and git-send-email.

The implementation appears solid, with careful attention to review feedback and proper use of Perl's getservbyname behavior. The patch maintains backward compatibility while fixing the symbolic port handling that was causing issues with tools like Emacs authinfo.gpg. With comprehensive test coverage and platform-compatible implementation, this completes the technical work for helper-side resolution of symbolic ports.

The series now awaits Junio Hamano's final decision on whether to accept this helper-side resolution approach despite the credential protocol documentation currently specifying numeric ports only. The implementation demonstrates this approach works reliably while preserving existing behavior.