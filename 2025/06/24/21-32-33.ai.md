This is a technical discussion in the ongoing Git daemon signal handling bugfix series, specifically about the choice between `sigaction()` and the deprecated `siginterrupt()` approach.

Junio responds to Carlo's defense of using `sigaction()` by raising two key points. First, he notes the irony of using `NO_SIGINTERRUPT` (a name derived from the deprecated interface) to conditionally compile `sigaction()` code. Second, while acknowledging Solaris's requirements, he pushes back against the proliferation of `#ifdef`s in the implementation, requesting better abstraction.

The exchange reveals an architectural tension between:
1. Modern POSIX compliance (Carlo's position, backed by deprecation evidence)
2. Code cleanliness and minimalism (Junio's preference)

Technical specifics include:
- Solaris's need for direct `sigaction()` calls
- The naming conflict around `NO_SIGINTERRUPT`
- Maintainability concerns about conditional compilation

This moves the discussion forward by:
1. Accepting the technical necessity of `sigaction()` on some platforms
2. Shifting focus to implementation aesthetics
3. Potentially opening design space for better abstraction of signal handling

The thread remains unresolved on how to balance these competing concerns while maintaining cross-platform functionality. Junio's latest suggests the technical solution may be acceptable if presented more cleanly.