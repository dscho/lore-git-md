This is a follow-up in the performance discussion about `git rev-list --changed-paths` with multiple pathspecs. Kai Koponen acknowledges Junio Hamano's explanation that the current behavior (disabling bloom filters for multiple paths) is by design, but suggests it might be room for optimization. The key technical point is that while the current implementation in `forbid_bloom_filters()` explicitly disables bloom optimizations for multiple paths, Kai speculates that checking each path's bloom filter sequentially could theoretically work, though recognizes this would require significant implementation changes. The email doesn't propose concrete changes but keeps open the possibility of future optimizations for this case, framing it as a feature request rather than a bug. The thread remains focused on understanding the technical constraints while leaving the door open for potential performance improvements in multi-path queries.