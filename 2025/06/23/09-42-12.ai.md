Windows terminal handling bugfix follow-up. Phillip Wood clarifies technical points in the ongoing discussion about `compat/terminal.c` fixes for DUPLEX mode handling. He confirms that `hconout` is properly invalidated in all `restore_term()` cases except when using `stty`, where it's already invalid. He also notes that `cmode_out` being zero is indeed invalid (as confirmed by `SetConsoleMode()` documentation), though considers checking handle validity directly less hacky than relying on this. 

Phillip strongly advocates for submitting the fix upstream rather than to Git-for-Windows, as the bug exists in core Git code. This aligns with Carlo's earlier suggestion to split the fixes (initialization upstream, handle validation downstream). The email reinforces the technical consensus around proper state management between `hconout` and `cmode_out` while moving the discussion toward finalizing the patch submission path. No new technical proposals are introduced, but the clarification helps solidify the approach for the first-time contributor James Duley.