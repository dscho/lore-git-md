This is a review response in the test infrastructure improvement thread about adding format string support to `test_seq`. Junio C Hamano approves Jeff King's patch adding the `-f` option to `test_seq`, agreeing with the design decisions while noting some limitations.

The review confirms several key points from the implementation: the use of `%d` instead of `%g` for integer sequences, the automatic newline behavior matching GNU seq, and the conscious choice not to support multiple format specifiers (which would require complex parsing and deviate from standard seq behavior). Junio notes the t0021 test case changes the payload format but maintains the same essential characteristics, and agrees the awk-to-test_seq conversion in t0612 improves readability.

The response highlights the tradeoffs in the implementation - particularly the remaining shell loop in t7422 that can't be converted due to the multiple-specifier limitation - but concludes the changes are worthwhile improvements that stay true to the original seq behavior while cleaning up test code. The review validates the patch as a thoughtful enhancement to the test infrastructure that follows Unix principles while addressing real pain points in test authoring.