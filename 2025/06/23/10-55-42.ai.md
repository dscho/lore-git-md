This is a test infrastructure cleanup patch, part 1 of a 2-patch series improving the `test_seq` helper and its usage. The patch simplifies a confusing `printf` construct in t7422-submodule-output.sh that was passing a counter variable as an argument without any corresponding format specifier. The author notes this was hard to read and could have been fixed by adding `%d` placeholders, but instead opts to replace it with two simpler `echo` statements that make the string interpolation more obvious.

The change is straightforward and improves code clarity while maintaining identical functionality. The patch touches only one test file, modifying a loop that generates submodule configuration entries. The commit message clearly explains the rationale and alternatives considered, showing this is a well-considered change to test infrastructure. This appears to be a low-risk cleanup that addresses a long-standing readability issue identified in prior discussion.