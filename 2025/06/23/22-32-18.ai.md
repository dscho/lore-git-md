Refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. This is patch 4/9, moving the `rev_info` initialization and reachability traversal code from `read_packs_list_from_stdin()` to its caller `read_stdin_packs()`. 

The change prepares for sharing this reachability traversal logic with the `--unpacked` case (handled by `add_unreachable_loose_objects()`), which will be used in a future patch to find objects reachable from input pack commits but packed elsewhere. The diff shows the mechanical movement of 35 lines while maintaining all existing functionality, including the revision walk setup flags (`no_kept_objects`, `keep_pack_cache_flags`, etc.) and traversal statistics reporting.

This continues the series' pattern of clean internal restructuring before introducing new features, with the immediate effect being that `read_packs_list_from_stdin()` now takes a `rev_info*` parameter instead of managing its own. The change is straightforward and maintains all existing behavior while enabling the planned future use cases.