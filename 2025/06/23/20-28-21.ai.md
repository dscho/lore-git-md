This is a design discussion email in the ongoing `repo-info` command series, specifically addressing the interface design for field selection. The email shows Lucas Seiki Oshiro reconsidering the default behavior of the new `git repo-info` command based on feedback from Phillip Wood and Junio C Hamano.

Lucas originally implemented an opt-in model where the command would show a subset of important fields by default, with `--all` showing everything and explicit field selection available. However, after Phillip Wood's review (linked in the email), Lucas is now leaning toward Junio's preferred opt-out model where the command would show nothing by default unless fields are explicitly requested or `--all` is used.

The key technical point is that Phillip argues this stricter approach is more appropriate for a plumbing command, as it forces callers to be explicit about what they want and avoids locking in default behaviors that might need to change later. This relates to the thread's open question #1 about interface design resolution.

The email doesn't propose any code changes but signals a potential design shift that would affect multiple patches in the series. It shows the author engaging with feedback and reconsidering initial design choices, though no final decision is made here. The discussion remains focused on the core tension between usability (providing helpful defaults) and plumbing command principles (explicit specification).