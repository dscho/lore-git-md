Performance discussion about optimizing multi-path bloom filter queries in commit-graph. Junio Hamano responds to Kai Koponen's suggestion that checking bloom filters sequentially per path might be feasible, noting the current implementation's design around single-path optimization. He identifies two key functions (`prepare_to_use_bloom_filter()` and `forbid_bloom_filters()`) that would need modification, observing that the existing `bloom_keys[]` array structure could theoretically accommodate multiple paths by adding more keys (currently using two keys per path - one for directory and one for full path). However, he cautions that his analysis is preliminary and invites deeper technical review from bloom filter experts, implying the change may involve more complexity than surface-level appearances suggest. The exchange keeps open the possibility of future optimization while maintaining awareness of the current implementation's intentional design constraints.