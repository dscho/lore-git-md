This is a performance optimization thread discussing the final details of output formatting for the `git fetch --prune` speedup.

Jacob Keller responds to Phil Hord's latest proposal about warning message formatting in the optimized `git fetch --prune` implementation. The core optimization (moving from O(N^2) to O(N*logN) performance) has already been technically validated and approved by multiple reviewers, with only minor output formatting details remaining.

The current discussion focuses on how to best present the "dangling symref" warnings that occur when pruning causes remote-tracking refs to become unreachable. Jeff King had suggested making the warning messages more explicit by including which deleted ref caused the dangling state (e.g., "origin/HEAD points to the now-deleted origin/main"). Phil implemented this suggestion but Jacob points out a minor wording inconsistency - the message says "will become dangling after deletion" when in fact the deletion has already occurred by that point in the output.

This represents the final polish on an otherwise settled optimization. The thread shows consensus around the technical approach and output ordering changes, with participants now fine-tuning the exact warning message wording. The performance improvement (from 410s to 4s in test cases) remains the primary motivation, with these message format changes being secondary usability improvements.

Key technical details for future reference:
- The warning message now explicitly references which deleted ref caused the dangling state
- Current phrasing is "origin/HEAD will become dangling after origin/master is deleted"
- Jacob notes this should probably say "has become dangling" since deletions happen before warnings
- File modified: builtin/fetch.c (prune_refs function)
- Related to refs_warn_dangling_symrefs() behavior
- Builds on the sorted string list approach from earlier in the series
- No changes to the core O(N*logN) algorithm

The discussion appears to be wrapping up, with only this minor wording clarification remaining before the series can be finalized. All major technical concerns have been addressed and approved by multiple reviewers including Jeff King and Junio Hamano.