Thread summary:

This two-patch series improves Git's test infrastructure by enhancing the `test_seq` helper function. The thread originated from a year-old discussion about test output readability. Patch 1 cleaned up t7422-submodule-output.sh by replacing a confusing `printf` construct with simpler `echo` commands. Patch 2 (now received) adds a `-f` format option to `test_seq` that mimics GNU `seq -f` behavior, allowing printf-style formatting of sequence numbers directly within the helper.

The implementation uses `%d` format specifiers (rather than `seq`'s `%g`) since `test_seq` only handles integers. It automatically appends newlines to the output but has some limitations: format strings can only contain one `%d` specifier, and one test case in t7422 still requires a shell loop for including the sequence number twice. The change eliminates verbose shell loops or awk invocations in 9 test files across multiple subsystems (reftable, submodules, merges), replacing them with simpler `test_seq -f` calls.

The series represents a well-considered quality-of-life improvement for the test suite, with clear commit messages referencing prior discussion. The changes are focused on test readability and maintainability, touching only test files with straightforward modifications that have no functional impact on the tests themselves. The implementation appears careful and complete, addressing the original goal of modernizing sequence generation in the test suite.