Thread summary:

This two-patch series enhances Git's test infrastructure by improving the `test_seq` helper function. The thread began with a discussion about test output readability, leading to Patch 1 cleaning up t7422-submodule-output.sh by replacing a confusing `printf` construct with simpler `echo` commands. Patch 2 adds a `-f` format option to `test_seq` that mimics GNU `seq -f` behavior for printf-style formatting of sequence numbers.

Junio C Hamano has reviewed and approved Jeff King's implementation, confirming key design choices: using `%d` instead of `%g` for integer sequences, maintaining automatic newline behavior matching GNU seq, and intentionally not supporting multiple format specifiers to avoid complex parsing. The changes affect 9 test files across multiple subsystems (reftable, submodules, merges), replacing verbose shell loops or awk invocations with simpler `test_seq -f` calls.

While the implementation has some limitations (single `%d` specifier support means one test case in t7422 still requires a shell loop), Junio agrees the tradeoffs are reasonable and the changes represent a thoughtful enhancement to Unix-style test infrastructure. The patch series improves test readability and maintainability without altering test behavior, addressing the original goal of modernizing sequence generation in the test suite. The implementation is now considered complete and approved for merging.