Test infrastructure cleanup patch in a two-part series. This first patch simplifies a test case in t7422-submodule-output.sh by replacing a confusing `printf` command with clearer `echo` statements. The original code passed a counter variable to `printf` without using it in the format string, creating potential confusion for readers. The new version splits the output into two lines with separate `echo` commands, making the test's intent more obvious while maintaining the same functionality.

This is a straightforward quality-of-life improvement for test readability, addressing a minor but legitimate readability issue. The change is small and focused, touching only one test file. The commit message clearly explains the rationale and alternatives considered. As the first part of a series that will introduce more substantial test infrastructure improvements (adding format options to `test_seq`), this patch serves as a simple warm-up with clear standalone value.