This is the first patch in a 9-part performance optimization series focused on MIDX and cruft pack interaction during repacking. The patch standardizes how pack-objects handles incompatible command-line options by converting explicit error checks into calls to the standard `die_for_incompatible_opt2()` function.

The change affects several option pairs in `builtin/pack-objects.c` that were previously checked with custom error messages, including `--exclude-promisor-objects` vs `--exclude-promisor-objects-best-effort`, `--keep-unreachable` vs `--unpack-unreachable`, and `--stdin-packs` vs `--filter`. The standardization ensures consistent error message formatting across Git when incompatible options are used together.

A minor test update in t5331-pack-objects-stdin.sh adjusts the expected error message format to match the new standardized output. The patch is a straightforward refactoring that doesn't change behavior beyond error message formatting, serving as groundwork for the more substantial changes that follow in the series.

This initial patch demonstrates careful attention to detail, with all option incompatibility checks properly converted and test coverage maintained. It follows the pattern established by commit a699367bb8 for handling incompatible options, showing the author's awareness of existing conventions in the codebase.