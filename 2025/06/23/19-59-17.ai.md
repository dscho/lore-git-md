This is a follow-up discussion on Patch 2 of the finalized v4 series enhancing promisor-remote protocol validation. Justin Tobler notes an improvement in the `fields_from_config()` function where empty entries in the field list are now properly filtered out when parsing the `promisor.sendFields` configuration. This handles cases where users might specify fields with mixed comma and space delimiters (like "partialCloneFilter, token") which previously could produce an empty middle entry and trigger warnings. 

While approving the change, Justin suggests adding a clarifying comment about this behavior since the silent filtering of empty entries might not be immediately obvious to future readers. The observation comes after Christian Couder's implementation already addressed all major review points from earlier rounds, with this being a minor documentation suggestion that doesn't require a re-roll. The series as a whole remains ready for merging with its comprehensive handling of promisor remote field advertisement and validation.