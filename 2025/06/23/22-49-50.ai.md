This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. It's patch 2/9 in the v6 series.

The patch narrows the scope of the `revs` variable in `add_object_entry_from_pack()` by moving its declaration inside the `OBJ_COMMIT` conditional block where it's actually used. This is a straightforward scope reduction that improves code organization without changing behavior. The variable was previously declared at the start of the `if (p)` block but was only needed for commit objects.

Junio Hamano notes the change makes sense since this is the only block that needs the variable. The change is minimal (just 2 lines modified) and aligns with good coding practices of limiting variable scope. This appears to be a clean, uncontroversial improvement that's already been confirmed as part of the larger series review.

The patch builds on the series' goal of optimizing pack maintenance by cleaning up the pack-objects code in preparation for later changes. While small, it contributes to the overall code quality improvements that enable the more significant performance optimizations in later patches.