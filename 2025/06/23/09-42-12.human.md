This is a technical follow-up in the Windows terminal handling bugfix thread. Phillip Wood clarifies two key points about the proposed fixes for the `compat/terminal.c` issue where uninitialized `cmode_out` values could cause assertion failures during interactive operations.

First, Phillip questions whether `hconout` handles are truly left invalid after `restore_term()`, noting they should already be invalid when using `stty`. Second, he confirms that zero is not a valid console mode value (it's a bitfield where zero means no flags set), though he considers checking handle validity directly to be cleaner than relying on this.

The email reinforces that this fix belongs in the main Git repository rather than Git-for-Windows, as it addresses core terminal handling code. Phillip's response helps solidify the technical approach while keeping the discussion focused on upstream integration. The thread continues to show good collaboration between the first-time contributor (James Duley) and experienced reviewers working through Windows-specific edge cases in terminal state management.