This is a follow-up discussion on patch 2/5 of the finalized promisor-remote protocol enhancement series. The patch notes a subtle improvement in how the server handles field lists in the `promisor.sendFields` configuration.

Justin Tobler points out that the current version now properly handles mixed comma-and-space separated field lists (like "partialCloneFilter, token") by filtering out empty entries that previously caused warning messages. While approving the change, he suggests adding a comment to clarify this behavior since the empty-entry filtering might not be immediately obvious to future readers.

The change is a minor refinement to the already-approved series, addressing a real-world configuration edge case. The thread shows the series is in its final form with only small documentation nits remaining before being merged.