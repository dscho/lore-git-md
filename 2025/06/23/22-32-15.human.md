This is part 3 of a 9-part performance optimization series focused on improving MIDX and cruft pack interactions during repacking. The series has already received significant review and is nearing final approval.

Patch 3/9 is a straightforward refactoring that extracts the `--stdin-packs` handling code from `cmd_pack_objects()` into a new `read_stdin_packs()` function. This prepares for future changes where this logic will grow more complex. The change is purely structural - no behavior is introduced or modified.

The patch follows the series' pattern of incremental cleanups before introducing new functionality. It has already been reviewed and approved by Junio Hamano. The commit message clearly explains the change and provides historical context (the original 2021 introduction of `--stdin-packs`).

This is a small but sensible piece of the larger effort to optimize pack maintenance operations. The series as a whole has shown 5-20% speed improvements in GitHub's production environment. With all known issues addressed and approvals from key maintainers, this patch appears ready for inclusion.