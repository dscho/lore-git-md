Performance optimization patch (part 2/9 of series) that refines variable scope in pack-objects.

This patch moves the declaration of the `revs` variable inside the OBJ_COMMIT conditional block where it's actually used in `add_object_entry_from_pack()`. The change reduces the variable's scope from the entire "if (p)" block to just the commit object case where it's needed for revision walking. This is a straightforward code hygiene improvement that doesn't affect functionality but makes the code slightly more maintainable by limiting variable visibility.

The change is part of Taylor Blau's larger series optimizing MIDX and cruft pack interactions during repacking. While this particular patch is minor, it contributes to the series' overall goal of cleaning up and optimizing pack-objects behavior. The thread context shows this patch was noted in earlier range-diffs and fits into the broader refactoring work that includes more significant changes like stdin-packs handling improvements and delta compression optimizations in later patches.