This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack handling. It's part 2 of 9 in the v6 submission.

The patch makes a small but precise scope reduction in `add_object_entry_from_pack()` by moving the `revs` variable declaration inside the `OBJ_COMMIT` conditional block where it's actually used. This is a clean, straightforward change that follows good coding practices by limiting variable scope to only where needed.

While minor, this change is part of a larger effort to optimize pack-objects behavior, particularly around cruft pack inclusion in MIDX during repacking. The series has already received positive feedback, with this particular patch being noted in earlier range-diff as a scope refinement.

The change is simple enough that it doesn't require new tests - the existing test suite should verify the behavior remains correct. This is the kind of small but thoughtful cleanup that helps maintain code quality in a complex area of Git's object packing machinery.