Performance optimization patch in the MIDX/cruft pack series, addressing lazy clone edge cases.

This is Junio Hamano's review of patch 8/9 in Taylor Blau's performance optimization series. The patch introduces a new `--stdin-packs=follow` option for pack-objects, with special handling for lazy clones. The key technical change modifies `show_object_pack_hint()` to skip blob objects that aren't present locally in follow mode, while maintaining the existing behavior for other object types.

Hamano identifies two main points: First, he confirms the lazy clone scenario justification for checking object presence before adding entries. Second, he questions an apparently superfluous blank line added in `show_commit_pack_hint()`. The review is generally positive, with Hamano having already approved the core approach in earlier patches.

The thread context shows this is part of a mature series (v6) that has received extensive review, with patches 4/9 and 5/9 already approved by Hamano. The current patch builds on earlier refactoring work to enable the follow mode feature while maintaining compatibility. The series as a whole has demonstrated 5-20% speed improvements in production at GitHub.