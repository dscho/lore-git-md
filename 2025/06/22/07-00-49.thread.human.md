This thread reports warnings encountered when cloning the official git.git repository from GitHub. The clone operation succeeds but produces numerous "badFilemode" warnings (affecting over 60 objects) and one "missingTaggerEntry" warning, suggesting potential file mode inconsistencies and a malformed tag object in the repository's history.

The reporter (kryzet) provides the raw warning output listing affected object hashes but does not analyze root causes or suggest fixes. The volume of warnings indicates this may be a systematic issue rather than isolated corruptions. While the clone completes successfully, the warnings raise questions about repository maintenance practices and whether these represent actual problems needing correction or benign historical artifacts that could be safely ignored.

Key open questions include whether these warnings indicate issues that should be addressed in the repository's object database, and whether similar warnings might affect other clones or operations. The specific object hashes listed could help diagnose the problematic commits/files if further investigation occurs.