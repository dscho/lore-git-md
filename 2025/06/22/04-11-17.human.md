This is a discussion about improving Git's clang-format workflow, specifically responding to Jeff King's rebase-based formatting proposal.

Junio Hamano weighs in on Jeff's suggestion to use `git rebase -x` with `git clang-format -p` as a potential workflow improvement. He sees value in the interactive `-p` option for new contributors who might benefit from the tool's suggestions, while still trusting experienced developers' judgment over automated formatting. Junio acknowledges the current `make style` target's limitations since it only checks uncommitted changes.

The key pain point both identify is dealing with rejected formatting suggestions - they agree the current `.clang-format` configuration still produces some undesirable output that shouldn't be blindly accepted. This continues the thread's central tension between automation benefits and preserving code quality through human judgment. Junio's response maintains his middle-ground stance from earlier in the thread: open to workflow improvements but cautious about fully automating style enforcement while the tool still makes questionable suggestions.

The exchange highlights practical implementation challenges rather than introducing new philosophical positions, focusing on how to integrate formatting checks in a way that provides value without becoming burdensome. Both contributors seem aligned that the tool needs further refinement before wider adoption.