This is a build system refinement in the ongoing `make coccicheck` improvement thread. Junio Hamano responds to the shell syntax debate around testing empty patch files, confirming his longstanding preference for `=` over `-eq` in numeric comparisons (as Eric Sunshine had referenced from a 12-year-old email). He suggests an alternative approach using `grep -q` to check for non-empty files, noting its potential efficiency advantage since it would short-circuit after finding the first non-empty file rather than processing all files to count bytes.

The email reinforces the maintainer's stylistic preference while offering a technically superior solution to the original `wc -c` approach. The `grep` method would handle the same core requirement (failing if any Coccinelle suggestions exist) while being more performant and arguably more readable. This continues the thread's pattern of refining implementation details while maintaining consensus on the core goal of making `make coccicheck` properly fail when changes are needed. The discussion remains focused on shell syntax optimizations rather than introducing new concerns about the underlying approach.