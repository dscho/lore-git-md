This is a user experience improvement patch for `imap-send`, part of the ongoing series adding OAuth2.0 support and folder management features. The change makes credential prompts include the port number (e.g., `outlook.office365.com:993`) alongside the hostname, matching the behavior of `git send-email`. 

The implementation is minimal - a one-line change in `imap-send.c` that formats the credential host field to include the port using `xstrfmt()`. The commit notes this can't produce an undefined state since the code always falls back to default ports (993 for IMAPS, 143 for IMAP) when none is specified. 

This appears to be the final polish item noted in the thread summary before the series can be merged. The change is straightforward and aligns with existing Git conventions, though reviewers may want to verify the credential helper ecosystem handles port-inclusive hostnames correctly. The thread context indicates this was the last feature needing review attention in the otherwise-approved series.