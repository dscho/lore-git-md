This is a user experience improvement patch in the ongoing `imap-send` feature/bugfix series. The change modifies the credential prompt to include port numbers when requesting passwords, making it consistent with `git send-email`'s behavior.

The patch changes a single line in `imap-send.c` to format the host string with the port number using `xstrfmt()`, replacing the previous simple `xstrdup()` of just the hostname. The credential prompt will now show entries like `imaps://user@host:993` instead of just `imaps://user@host`.

This is part of the broader `imap-send` improvements that have been through 18 iterations of review. The change is straightforward and aligns with existing Git conventions, though it's worth noting this affects the user-facing credential helper interface. The implementation is minimal and focused, just modifying the host string construction in `server_fill_credential()` while maintaining all other credential handling behavior.

The patch follows the series' pattern of careful memory management (using `xstrfmt`) and maintains consistency with the rest of Git's credential handling. This appears to be a final polish item in the larger series that's already addressed all major technical concerns through extensive review.