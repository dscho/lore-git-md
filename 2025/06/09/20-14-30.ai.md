This is a discussion about user experience details in the ongoing `imap-send` feature series, specifically around credential prompt formatting. The thread has narrowed to one remaining decision point: whether to always show port numbers in credential prompts (Aditya Garg's preference, matching `send-email` behavior) or only show non-default ports (Junio Hamano's preference to reduce noise).

Junio clarifies his position that showing default ports (993/143) adds no value and could confuse users, while acknowledging the alternative approach would also be acceptable if properly explained. He suggests either:
1. Implementing his proposed logic to only show non-default ports via a simple conditional comparing `srvc->port` against the protocol defaults
2. Keeping the always-show behavior but improving the commit message explanation to justify this choice

The exchange reveals this is largely a matter of taste rather than technical correctness, with Junio indicating he won't block the series over this difference. This appears to be the final open question before the substantial `imap-send` improvements (OAuth2 support, folder management, configuration fixes) can be integrated.

Key technical details referenced:
- The port display logic lives in `server_fill_credential()`
- Default ports are 993 (IMAPS) and 143 (IMAP)
- Current behavior uses `xstrfmt` to always show host:port
- Alternative would compare `srvc->port` against `srvc->use_ssl ? 993 : 143`

The discussion remains collegial, with both parties seeking clarity rather than contention. With all other technical issues in the series resolved, this UX polish question appears to be the last hurdle before merge.