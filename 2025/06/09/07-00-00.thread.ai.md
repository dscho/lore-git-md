Updated AI summary:

This thread discusses improving Git's tag creation error messages to distinguish between cases where a tag already exists pointing to the same commit versus a different commit. The original proposal suggested modifying `builtin/tag.c`'s `create_tag()` function to show the existing tag's commit hash when a duplicate tag is detected, helping users distinguish harmless duplicates from dangerous overwrites. 

Junio Hamano opposes this change, arguing the current generic "tag already exists" message is sufficient because:
1. The tag isn't modified when the error occurs, allowing manual investigation
2. Showing raw commit hashes wouldn't provide immediate clarity without cross-referencing
3. Users can use tools like `git range-diff` when needed to compare commits

The discussion now presents two clear perspectives on error message design:
- Pro: More context helps users quickly assess severity of tag conflicts
- Con: Minimal messages suffice since users can investigate further when needed

Key technical details remain unchanged: the modification would affect tag creation in `builtin/tag.c`, handling both lightweight and annotated tags. The thread now includes Junio's position that the current behavior is preferable, creating a design decision point about whether the proposed improvement justifies the added complexity. No resolution has been reached yet between these viewpoints.