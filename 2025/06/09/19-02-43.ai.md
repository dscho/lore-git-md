This is a follow-up discussion in the ongoing `imap-send` feature/bugfix series, specifically addressing patch 8/10 which deals with credential request formatting. The email focuses on whether and when to display port numbers when requesting credentials from the user.

Aditya Garg responds to Junio Hamano's feedback about port number visibility in credential prompts. The core disagreement is about when to show ports: Garg wants to always show them (matching `send-email` behavior), while Junio suggests only showing non-default ports to avoid noise. Garg points out that Junio's suggested implementation would hide even explicitly-specified default ports (like 993 for IMAPS), which they consider undesirable.

The technical discussion centers around the `server_fill_credential()` function where the host:port string is constructed. Junio proposed conditional logic to only append the port when it differs from defaults (993/143), while Garg's current implementation unconditionally shows all ports via `xstrfmt()`. Garg notes their original "FWIW" comment about default ports was potentially confusing and could be removed from the commit message.

This exchange represents the final polish phase of the series, where minor UX decisions are being debated after the core technical changes have been settled. The discussion remains focused on this specific edge case of port display behavior, with no new functionality or bugs being introduced. The thread context shows this is part of broader improvements to `imap-send` including OAuth support, folder management, and configuration fixes.