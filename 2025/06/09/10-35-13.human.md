This is a workflow clarification in an already-resolved thread about interactive rebase techniques. Aditya Garg confirms they'll try Phillip Wood's suggestion to use `--keep-base` with `git rebase --autosquash` when re-rolling patch series to maintain the same base commit. 

The thread had previously established the modern way to handle fixup commits (no longer needing `GIT_SEQUENCE_EDITOR=true` since Git 2.44.0), and this final exchange adds the nuance that `--keep-base` should be used when iterating on patch series to avoid unwanted rebasing onto updated upstream branches. 

This is a simple acknowledgment that closes out the discussion with no remaining questions - the original workflow question has been thoroughly answered with both the standard approach and this additional best practice for patch series maintenance.