This is a technical reply in the ongoing discussion about duplicate tag handling, specifically addressing the idempotency proposal. Andreas Schwab points out a key limitation in Hilco Wijbenga's suggestion to make tag creation idempotent when the target commit matches: the proposal doesn't account for cases where the existing tag differs in type (annotated vs lightweight). 

The email highlights that silently succeeding for matching commits could be problematic when the tag types differ, since converting between annotated and lightweight tags is not a no-op operation. This introduces a new technical consideration to the idempotency approach, showing that the behavior change might need to be more nuanced than initially proposed.

The reply keeps focus on `builtin/tag.c`'s behavior but adds the tag type distinction as a factor any solution must handle. This moves the discussion forward by identifying a concrete case where idempotent behavior could cause silent data changes rather than being truly harmless. The observation supports Junio Hamano's position that the current explicit failure might be preferable, while still leaving room for solutions that could handle both commit matching and tag type preservation.