This is a final polish of the memory leak fix in revision walking code, with Junio Hamano providing minor style corrections to the test case. The patch itself (fixing the `prune` array leak in `prepare_show_merge()`) remains unchanged from v2, but Junio notes two formatting issues in the test script: using spaces instead of tabs for indentation, and having a space between `>` and the filename in redirections. These are standard Git project style requirements that new contributors sometimes miss.

The test scenario itself - creating three branches with conflicting changes and verifying `git show --merge` behavior - has already been thoroughly discussed and approved in previous iterations. Junio's review confirms the technical approach is sound while ensuring the code meets Git's stylistic conventions. This appears to be the final step before the patch can be merged, addressing the last remaining nits after the substantive discussion about test methodology was resolved in earlier rounds.