Thread summary:

Bugfix for interactive rebase state cleanup when instruction format is invalid. The thread addresses an edge case where an invalid `rebase.instructionFormat` configuration could leave the repository in a half-initialized state during interactive rebase failure. The core issue occurs when `get_commit_format()` dies due to an invalid format string after `init_basic_state()` has already created rebase metadata files. The patch reorders operations in `do_interactive_rebase()` to validate the instruction format and generate the todo list before initializing the rebase state, preventing the inconsistent state. Key technical changes include moving `sequencer_make_script()` before `init_basic_state()`, restructuring control flow with early `goto cleanup` when script generation fails, and adding a test case in t3415 to verify the rebase-merge directory isn't created for invalid formats. The solution avoids deeper changes to `get_commit_format()`'s error handling while providing clear regression protection through the new test. The diff shows code motion that improves logical flow (dedenting the success path) while maintaining existing functionality.