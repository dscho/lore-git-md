Bugfix for interactive rebase state cleanup when instruction format is invalid, addressing edge cases where invalid `rebase.instructionFormat` configuration could leave the repository in a half-initialized state. Øystein Walle's v2 patch reorders operations in `do_interactive_rebase()` to validate formats via `sequencer_make_script()` before `init_basic_state()`, with test coverage in t3415-rebase-autosquash.sh confirming cleanup of .git/rebase-merge on failure. Junio C Hamano has acked the patch for 'next', accepting it as an incremental improvement while deferring Phillip Wood's broader error handling suggestions.

Phillip Wood notes the patch doesn't address the parallel issue when `--autostash` is used (which creates state earlier in `cmd_rebase()`), though this doesn't block merging of the current fix. The technical distinction is that solving the autostash case would require a different approach and would make this change redundant, but the current patch still improves the non-autostash path without complicating future work. Consensus remains to merge this as-is while leaving autostash handling for future work. The test in t3415 provides regression protection regardless of future implementation path. No further changes expected before merging.