Thread summary:

Bugfix for interactive rebase state cleanup when instruction format is invalid, addressing an edge case where an invalid `rebase.instructionFormat` configuration could leave the repository in a half-initialized state during interactive rebase failure. The core solution involves reordering operations in `do_interactive_rebase()` to validate the instruction format via `sequencer_make_script()` before initializing rebase state with `init_basic_state()`. Junio C Hamano reviewed Øystein Walle's original patch, approving the technical approach but noting two process items: missing sign-off and suggesting `git show --histogram` for clearer diff visualization. Phillip Wood proposed a more comprehensive solution modifying `get_commit_format()` to avoid direct `die()` calls, requiring changes in `revision.c`, `builtin/log.c`, and `sequencer.c`, plus documentation updates in `MyFirstObjectWalk.adoc`. Wood's approach would enable proper cleanup of stashed changes when format validation fails and noted the changes should be based on 'ps/maintenance-ref-lock' branch for `die(NULL)` support. The discussion expanded from Walle's localized fix to addressing root causes in format validation error handling, with Wood providing detailed implementation guidance while noting limited availability for follow-up. Junio has now followed up weeks later to check on the patch's status, indicating the technical solution remains sound but requires sign-off and potentially Wood's suggested error handling improvements. The test case in t3415 verifying rebase-merge directory behavior for invalid formats remains relevant for regression testing. Key open questions: whether to proceed with Walle's original focused fix or Wood's broader error handling changes, and completion of required sign-off and documentation updates. The repository inconsistency issue remains unaddressed in mainline while implementation approach is being finalized.