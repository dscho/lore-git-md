This is a small but useful UI improvement in the ongoing `imap-send` series, which is nearing completion after extensive review. The patch modifies the credential prompt to include the port number alongside the hostname when asking for passwords (e.g., showing `outlook.office365.com:993` instead of just the hostname). 

The change makes `imap-send` consistent with `git send-email`'s behavior and helps users verify they're connecting to the correct port, especially useful when troubleshooting or using non-standard ports. The implementation is straightforward - just a one-line change to format the host string with the port number. 

Since this is part of a larger series that has already received maintainer approval for its core changes, this appears to be a final polish item before merge. The patch is simple, well-explained, and aligns with existing Git conventions.