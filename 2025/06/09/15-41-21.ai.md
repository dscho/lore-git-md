Bugfix patch in the `imap-send` series addressing a memory leak in CRAM-MD5 authentication. This is part of the larger v17 series that introduces OAuth2.0 support and fixes various IMAP-related issues.

The patch specifically fixes a case where memory allocated for the CRAM-MD5 response would leak if the socket write operation failed. The response buffer is now properly freed in the error path before returning. The change is minimal (adding a `free(response)` call) but important for correctness.

This appears to be one of the final cleanups in the comprehensive `imap-send` overhaul series. The thread context shows this series has undergone extensive review, with the CRAM-MD5 authentication being one of the earlier-reviewed components. The patch follows the series' pattern of thorough memory management, matching similar fixes made elsewhere in the authentication code paths.

Key technical details: The fix touches the `auth_cram_md5()` function in `imap-send.c`, specifically the error path when `socket_write()` fails. The response buffer is allocated by the `cram()` helper function earlier in the same function. This aligns with the series' broader focus on proper resource handling in authentication methods.