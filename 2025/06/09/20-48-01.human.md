Final polish on a memory leak fix. This is a bugfix patch that addresses style nits in the test case for the recently approved memory leak fix in `prepare_show_merge()`. Junio Hamano points out two formatting issues in Lidong Yan's test script: using spaces instead of tabs for indentation, and having a space between `>` and the filename in redirections. 

The test itself remains substantively the same - it still creates a three-branch merge conflict scenario to exercise the `--merge` flag behavior that was previously leaking memory. These are purely stylistic corrections to bring the test in line with Git's coding guidelines. The thread had already reached consensus on both the technical solution and test approach, with Junio having previously praised the comprehensive test design. This final iteration just cleans up the presentation before the patch gets merged.