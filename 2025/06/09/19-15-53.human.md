This is a bugfix patch in the ongoing `imap-send` series, specifically addressing CRAM-MD5 authentication handling. Junio Hamano notes a minor copy-paste error in the error message where `srvc->host` is incorrectly used instead of `srvc->auth_method` when reporting unknown authentication methods. He explicitly marks this as a "leftoverbit" that doesn't need fixing in the current patch, but something to address later.

The patch itself (from v16) was about making CRAM-MD5 authentication fail gracefully when OpenSSL isn't available, but this email focuses on a separate small issue spotted in the code. The thread context shows this is part of a larger series that adds OAuth2 support and fixes various IMAP authentication issues, with most technical concerns already addressed in v17. This particular observation doesn't block the series' progress but serves as a quality note for future cleanup.