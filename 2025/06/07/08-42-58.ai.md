Platform compatibility patch updating Solaris build configuration, now with review feedback.

Jonathan Nieder provides a positive review of Brad Smith's patch that updates Solaris build flags in `config.mak.uname`, noting the changes are reasonable given Solaris 11's decade-old support for mkdtemp(), memmem(), and strcasestr(). He adds a Reviewed-by tag, indicating the patch appears ready for inclusion.

The review also raises a separate maintenance question about potentially consolidating or removing support for older Solaris versions (5.6-5.10) in future cleanup. This isn't a blocking concern for the current patch but suggests a possible follow-up direction. The thread context shows this is the first substantive response to Brad's initial submission, which cleanly separated version-specific requirements while removing unnecessary restrictions for modern Solaris 11+ systems.

Key technical points remain unchanged from the original patch: the changes affect only build-time configuration in `config.mak.uname`, with no runtime behavior impact. The version-specific logic now properly reflects Solaris 11's capabilities while maintaining backward compatibility. Jonathan's approval suggests this is a non-controversial change likely to proceed smoothly.