Performance optimization completing a series on byte-order handling in bswap.h. This final patch reintroduces compiler built-in byte-swapping functions (`__builtin_bswap32/64`) when available, addressing performance concerns on architectures like RISC-V and MIPS where shift-based implementations generate suboptimal code. The change is carefully guarded with `__has_builtin` checks for compatibility with older compilers, and maintains the series' big-endian safety by keeping the definitions unconditional. 

The patch follows the series' progression from correctness fixes to optimization, now leveraging compiler intrinsics where beneficial. It specifically handles cases where shift/mask operations would be inefficient, while falling back gracefully on compilers without these built-ins. The implementation mirrors the approach from commit 6547d1c9 (reverted in patch 1) but now fits within the refined endianness detection framework established by earlier patches. 

Key technical points:
- Adds runtime detection of `__builtin_bswap` availability via `__has_builtin`
- Places the built-in definitions in the little-endian section (where swapping is needed)
- Maintains the existing `default_bswap` implementations as fallback
- Preserves the series' architectural neutrality while optimizing for specific platforms

This concludes the series' transition from problematic macros to a robust, optimized byte-swapping infrastructure that handles both correctness (big-endian systems) and performance (inefficient architectures) concerns.