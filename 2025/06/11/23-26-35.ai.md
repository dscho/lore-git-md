This is a follow-up discussion in the finalized `--compact-summary` feature thread, where D. Ben Knoble raises a broader architectural question about diff output configuration. The thread context shows the feature is already implemented with both CLI (`--compact-summary`) and config (`merge.stat=compact`) support, with all test refinements completed.

Ben Knoble probes whether the solution should have been more extensible from the start, suggesting a generalized "diff options" configuration mechanism rather than adding one-off flags. This echoes his earlier feedback that led to the current config approach, though he's now questioning if even that went far enough. The maintainer Junio Hamano had previously explained the current design preserves backward compatibility while adding the new compact format option.

The exchange highlights a tension between incremental feature addition and more comprehensive architectural solutions, though the thread context indicates this line of discussion is unlikely to change the already-finalized implementation. The feature remains focused on merge/pull output formatting without expanding to broader diff customization.