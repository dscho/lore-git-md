This is a follow-up discussion about the `--compact-summary` feature for `git merge` and `git pull`, responding to design questions about configuration options.

Junio Hamano addresses D. Ben Knoble's suggestion for a more general "diff options" configuration by proposing a specific evolution of the existing `merge.stat` boolean config. The key proposal is to extend `merge.stat` to accept either a boolean (maintaining backward compatibility with the current `--stat` behavior) or the string 'compact-summary' to enable the new compact format. This would allow users to set their preferred diffstat format via configuration rather than requiring command-line flags.

The email demonstrates the practical difference between the explicit flag approach (`git merge --compact-summary`) versus the proposed config-based approach (`git merge` with `merge.stat=compact-summary`). Hamano's response suggests a middle ground that maintains backward compatibility while adding the new format option, rather than pursuing a more radical redesign of output configuration.

This moves the thread forward by providing a specific counter-proposal to Knoble's architectural question, focusing on incremental evolution of existing configuration rather than introducing a generalized output control system. The discussion remains focused on merge/pull output formatting, with no new technical implementation details introduced in this response.