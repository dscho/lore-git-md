Follow-up to the `--compact-summary` feature, this is a minor test improvement suggestion from Eric Sunshine regarding Junio Hamano's patch extending `merge.stat` configuration to support the new compact summary format.

Sunshine points out a potential issue with the test's heredoc syntax - suggesting the use of `\EOF` (with backslash) rather than plain `EOF` in the test expectation. This is a small but important consideration for test robustness, as the backslash prevents shell variable expansion and command substitution within the heredoc.

The test itself verifies that setting `merge.stat=compact` produces the expected compact summary output format during a merge, matching the behavior of the command-line `--compact-summary` option. The test uses sed to normalize the commit hashes in the output for stable comparison.

This appears to be a focused, quality-focused review comment on an already well-tested feature. The core functionality isn't being questioned - just a minor implementation detail in the test infrastructure. The maintainer will likely either accept this as a trivial improvement or explain why the current form is preferable.