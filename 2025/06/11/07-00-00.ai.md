This is a discussion continuation about `git fetch` behavior and network reliability, specifically around completing interrupted prefetch operations when offline.

M Hickford confirms Junio's suggested workaround (`git fetch . 'refs/prefetch/remotes/origin/*:refs/remotes/origin/*'`) successfully completes prefetches when offline. They then propose adding a dedicated fetch option (`--offline` or `--complete-prefetch`) to formalize this workflow for users with intermittent connectivity.

The email advances the thread by:
1. Validating the manual reference transfer method works
2. Suggesting a user-facing command option to streamline the process
3. Implicitly questioning whether the current workaround is sufficiently discoverable

This follows Junio's earlier position that core fetch should remain simple while acknowledging prefetch` as the preferred periodic mechanism. The proposal would represent a middle ground - keeping core fetch simple while adding minimal plumbing to support the prefetch workflow.

Key technical details:
- Confirms prefetch references can be manually promoted via local fetch
- Suggests new option names that would abstract the reference transfer
- References git-fetch documentation as context

The suggestion appears well-intentioned but may face resistance given Junio's stated preference to keep core fetch simple. The thread's unresolved questions about prefetch documentation and interruption handling remain relevant to evaluating this proposal.