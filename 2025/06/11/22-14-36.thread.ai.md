Thread summary:

Performance-oriented refactoring of byte-order handling in Git's bswap.h, addressing a regression on big-endian systems. Series from Sebastian Andrzej Siewior (6 patches) revisits byte-swapping infrastructure after ntohl() macros incorrectly forced byte swapping on big-endian platforms. Core changes: reverts problematic change (patch 1), then systematically improves handling via __BYTE_ORDER__ (patch 2), fixes MSVC endian detection (patch 3), makes ntohl/ntohll consistent (patch 4), removes redundant x86 optimizations (patch 5), and carefully reintroduces built-in bswap support (patch 6). Focuses on compat/bswap.h with 118 lines changed (net -30). Handles platform differences (glibc/musl/Windows) and leverages modern optimization capabilities of gcc/clang, though musl libc's function-call approach to ntohl() may still warrant keeping Git's macros. No performance metrics provided but aims to maintain efficiency while fixing big-endian correctness. Technical progression shows clear problem-to-solution path.