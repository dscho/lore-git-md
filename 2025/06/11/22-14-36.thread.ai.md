Performance-oriented refactoring of Git's bswap.h by Sebastian Andrzej Siewior (6 patches) addresses byte-order handling regressions on big-endian systems. Current status: patch 1 (fixing incorrect ntohl() behavior on big-endian) applied by Junio Hamano with typo correction (31/64 -> 32/64 in commit message). Patch 2 added `__BYTE_ORDER__` support (GCC/Clang/ICC) as fallback when libc's `BYTE_ORDER` unavailable (e.g. MinGW), introducing `GIT_*` constants. Patch 3 now addresses MSVC by hardcoding little-endian (safe given MSVC's x86/arm64 targets), adding MSVC-specific `GIT_*` defines and fallback logic (6-line change in compat/bswap.h). Junio noted minor commit message typo ("an arm" -> "on arm") but otherwise appears ready to queue.

Technical details: series modernizes endianness detection while maintaining existing fallbacks, with platform-specific handling (MSVC addressed separately in patch 3). Subsequent patches will standardize ntohl/ntohll (patch 4), remove redundant x86 optimizations (patch 5), and reintroduce built-in bswap support (patch 6). Performance analysis from Brian m. carlson shows modern compilers optimize shift-based swaps to bswap on x86. Future-looking discussions note C23's `<stdbit.h>` and POSIX.1-2024's `<endian.h>` (Collin Funk) as potential future directions but not blocking current work.

Series demonstrates careful platform handling: RISC-V/MIPS optimizations preserved via `__builtin_bswap` reintroduction (patch 6). Changes focused in compat/bswap.h (158 lines net -70) balance compatibility with optimization paths. Open items: none blocking after patch 3's typo fix. Remaining patches expected to proceed similarly, building on this foundational endian detection improvement. Junio's involvement suggests smooth path to integration barring new technical concerns.