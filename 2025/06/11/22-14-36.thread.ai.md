Performance-oriented refactoring of byte-order handling in Git's bswap.h, addressing a regression on big-endian systems. Series from Sebastian Andrzej Siewior (6 patches) revisits byte-swapping infrastructure after ntohl() macros incorrectly forced byte swapping on big-endian platforms. Core changes: reverts problematic change (patch 1), then systematically improves handling via __BYTE_ORDER__ (patch 2), fixes MSVC endian detection (patch 3), makes ntohl/ntohll consistent (patch 4), removes redundant x86 optimizations (patch 5), and carefully reintroduces built-in bswap support (patch 6).

Technical progression shows clear problem-to-solution path through the series:
1. Revert problematic change causing big-endian regressions
2. Modernize endianness detection using __BYTE_ORDER__
3. Fix Windows/MSVC-specific detection
4. Standardize ntohl/ntohll family
5. Remove redundant x86 optimizations
6. Reintroduce built-in bswap support safely (current patch)

Final patch reintroduces compiler built-in byte-swapping functions (`__builtin_bswap32/64`) when available, addressing performance concerns on architectures like RISC-V and MIPS where shift-based implementations generate suboptimal code. The change is carefully guarded with `__has_builtin` checks for compatibility with older compilers, and maintains the series' big-endian safety by keeping the definitions unconditional. Implementation mirrors the approach from commit 6547d1c9 (reverted in patch 1) but now fits within the refined endianness detection framework.

Key technical points:
- Runtime detection of `__builtin_bswap` availability via `__has_builtin`
- Built-in definitions placed in little-endian section (where swapping is needed)
- Maintains existing `default_bswap` implementations as fallback
- Preserves architectural neutrality while optimizing for specific platforms

Changes focused entirely on compat/bswap.h with cumulative 158 lines changed (net -70). Handles platform differences (glibc/musl/Windows) while maintaining optimization paths. No performance metrics provided but design clearly aims to preserve efficiency while fixing correctness issues, particularly for big-endian systems. Musl libc's function-call approach to ntohl() may still warrant keeping Git's macros as fallback. Series demonstrates careful balance between platform-specific needs and maintainable code, concluding the transition from problematic macros to robust, optimized byte-swapping infrastructure.