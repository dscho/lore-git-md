Thread summary:

This is a platform compatibility patch series (v2, 3/6) addressing MSVC compiler support, specifically targeting endianness definitions for Windows builds. The current patch focuses on byte order handling when compiling with Microsoft's Visual C++ compiler, which lacks standard endianness macros like `__BYTE_ORDER__`. Since all MSVC-supported architectures (x86 and arm64) are little-endian, the patch explicitly sets `GIT_BYTE_ORDER` to little-endian in `compat/bswap.h` by adding new preprocessor definitions before the existing fallback logic. The change introduces `GIT_LITTLE_ENDIAN`/`BIG_ENDIAN` constants (1234/4321) while preserving existing behavior for other platforms via `#elif !defined(GIT_BYTE_ORDER)`. This is part of a larger effort to improve Windows/MSVC compatibility, with the current patch being a straightforward but necessary change given MSVC's architectural constraints. The series appears to be progressing through normal review channels without significant controversy.