This thread documents a thorough refactoring of Git's byte-order handling in bswap.h, addressing both correctness and performance concerns. Sebastian Andrzej Siewior's six-patch series began by reverting a problematic change that caused regressions on big-endian systems, then systematically rebuilt the byte-swapping infrastructure with proper endianness detection. 

The series progressed through several key improvements: modernizing endianness detection using __BYTE_ORDER__, fixing Windows/MSVC-specific handling, standardizing the ntohl/ntohll family of macros, and removing redundant x86 optimizations that modern compilers now handle automatically. The final patch, just received, carefully reintroduces compiler built-in byte-swapping functions (__builtin_bswap32/64) where available, addressing performance concerns on architectures like RISC-V and MIPS while maintaining compatibility with older compilers through __has_builtin checks.

Throughout the series, the changes have focused on compat/bswap.h, with a net reduction of 70 lines of code while improving both correctness (particularly for big-endian systems) and performance (leveraging modern compiler optimizations). The implementation now provides a robust foundation for byte-order operations that handles platform differences and optimization paths cleanly. With this final optimization in place, the series appears complete and well-tested across various architectures and compilers.