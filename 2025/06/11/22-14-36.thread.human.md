This thread introduces a performance-oriented refactoring of Git's byte-order handling in bswap.h, addressing a regression on big-endian systems. Sebastian Andrzej Siewior's 6-patch series first reverts the problematic change that caused incorrect byte swapping on big-endian platforms, then systematically improves the infrastructure. The changes restore proper endianness detection, fix MSVC compatibility, standardize ntohl/ntohll behavior, remove redundant x86 optimizations, and carefully reintroduce built-in bswap support. The series touches 118 lines in compat/bswap.h with a net reduction of 30 lines, maintaining efficiency while fixing correctness issues across different libc implementations (glibc, musl) and platforms (Windows, big-endian systems). The author notes modern compilers now handle many optimizations automatically, though some platform-specific considerations remain.