This is a performance-oriented refactoring patch that removes x86-specific byte-swapping optimizations, relying instead on modern compilers to generate efficient code from the generic implementation.

The patch is part 5 of a 6-part series (no cover letter was provided for context). It removes hand-optimized x86 assembly implementations of `bswap32` and `bswap64` from `compat/bswap.h`, falling back to the default shift-based implementations. The author demonstrates through compiler testing that modern compilers (including MSVC, GCC, and Clang) now recognize the shift patterns and generate equally efficient machine code, with optimizations appearing even in relatively old compiler versions (GCC 4.5+, Clang 3.0+).

The change simplifies the codebase by removing 40 lines of platform-specific assembly while maintaining performance. The commit message provides thorough evidence that the optimization is no longer needed, testing multiple compiler versions and architectures. This appears to be a well-justified cleanup that reduces maintenance burden without sacrificing performance.