This is a follow-up in the ongoing discussion about improving branch repointing functionality, originally proposed as a `git branch --force` extension but now considering alternative approaches.

Andrea Stacchiotti responds to Junio Hamano's suggestion of using `update-ref --stdin` for batch branch operations. While acknowledging the plumbing approach, Andrea continues advocating for a more user-friendly porcelain solution with a simple `git branch --some-flag A B C X` syntax. The email doesn't propose specific flag names yet, but maintains the position that direct branch command syntax would be more intuitive than piping to update-ref.

The discussion remains at an architectural level, with no new code shown. The core tension is between plumbing-level efficiency (update-ref) versus porcelain-level usability (branch command). Andrea's stance suggests they see value in keeping this functionality at the user-facing command level despite the plumbing alternative.

This keeps both implementation strategies on the table while highlighting the user experience considerations that might favor the porcelain approach. The thread appears to be moving toward needing a clear decision about which layer this functionality should live in before implementation details can progress.