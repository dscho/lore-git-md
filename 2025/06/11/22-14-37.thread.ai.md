Thread identity: Bugfix patch series (1/6) reverting byte-swapping macro changes that caused test failures on s390x (big-endian) systems. Originating author not specified in this email.

Core problem: Commit 6547d1c9 modified `compat/bswap.h` to use compiler built-in byte-swap functions universally when available, breaking Git's assumption that `bswap32/64` macros are only defined on little-endian systems. This assumption underlies network-to-host byte order macros (`ntohl/ntohll`), causing test failures on big-endian platforms.

Current status: Reverting the change by removing compiler built-in detection logic entirely, falling back to previous implementation that only defines byte-swap macros for x86/x86_64 architectures when using GCC. Minimal change (1 line kept vs 13 removed) targeting specific regression.

Key technical details: Affects `compat/bswap.h` implementation. Original change introduced universal built-in detection; revert restores architecture-specific behavior. Impacts network-to-host byte order conversion macros that rely on `bswap` macros being little-endian-only.

Open questions: Why does the codebase maintain this assumption about `bswap` macros being little-endian-only? This architectural constraint isn't explained in the commit message. The patch is part of a 6-part series but no cover letter or context about the broader changes is provided here.

Participant positions: No reviews or responses yet as this appears to be the initial submission in the thread. The justification focuses on test failures observed on s390x systems.

Related work: Reverts commit 6547d1c9. Part of a larger series (1/6) though full context isn't provided in this email.