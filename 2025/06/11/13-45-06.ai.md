This is the final patch in a completed 5-patch series enhancing the promisor-remote protocol with configurable validation of remote attributes. The series has gone through multiple rounds of review with all major design points settled.

The current patch completes the series' consistency improvements by introducing string constants for the mandatory "name" and "url" fields in the promisor-remote protocol, matching the approach already taken for "partialCloneFilter" and "token" fields in earlier patches. This eliminates string literals scattered through the code in favor of centralized definitions, improving maintainability and reducing error-prone duplication.

The changes are mechanical replacements of hardcoded strings with references to the new constants `promisor_field_name` and `promisor_field_url`. The patch affects the same key functions modified earlier in the series for field handling: `promisor_remote_info()` for server-side field generation and `parse_one_advertised_remote()` for client-side parsing.

As the final patch in a thoroughly reviewed series that has addressed all previous feedback, this appears ready for merging with no outstanding issues. It maintains the series' focus on type safety and consistency while introducing no functional changes.