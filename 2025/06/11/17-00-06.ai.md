This is a follow-up feature patch extending the `merge.stat` configuration to support the new `--compact-summary` format, building on the initial implementation from the same thread. The patch proposes making `merge.stat` accept either a boolean (true/false) or the string "compact", with "compact" triggering the same behavior as the new `--compact-summary` command-line option. This addresses earlier architectural questions about configuration flexibility while maintaining backward compatibility.

Key technical changes include:
- Modifying the config parser in `builtin/merge.c` to handle the new string value using `git_parse_maybe_bool_text`
- Updating documentation in `merge.adoc` to describe the new "compact" option alongside existing boolean values
- Adding a test case in `t7600-merge.sh` verifying the config-driven compact summary behavior matches the command-line version

The implementation carefully preserves the existing MERGE_SHOW_DIFFSTAT behavior while adding MERGE_SHOW_COMPACTSUMMARY support through the same configuration pathway. The patch includes robust error cases - unknown string values fall back to the default diffstat behavior, described in code as "setting from the future".

This appears to be a well-considered evolution of the initial feature, providing configuration flexibility while avoiding the complexity of optional-value command-line flags. The changes are localized to merge-specific code paths and include thorough documentation and testing. The approach suggests the maintainer favors extending existing configuration mechanisms over introducing parallel options.