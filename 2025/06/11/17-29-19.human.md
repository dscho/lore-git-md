This is a final polish patch in the `--compact-summary` feature series, addressing a minor implementation detail in the test suite. Maintainer Junio Hamano agrees with Eric Sunshine's suggestion to use `\EOF` rather than `EOF` for heredoc delimiters in the test cases, since no variable interpolation is needed in the test output expectations. This change ensures consistency with Git's test suite conventions while maintaining the exact same verification of the compact summary output format. The patch represents the last remaining loose end before this feature can be considered complete, with all architectural decisions already settled in earlier iterations of the series.