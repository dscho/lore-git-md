Final patch in a series enhancing promisor-remote protocol validation. This is a small refactoring that introduces string constants for the mandatory "name" and "url" fields in the promisor-remote protocol, following the same pattern already used for the optional fields ("partialCloneFilter" and "token") in previous patches.

The change is straightforward - it replaces hardcoded string literals with named variables for better maintainability and consistency. The patch touches only the promisor-remote.c file, modifying how the name and URL fields are handled in both the output generation (when advertising remotes) and input parsing (when processing advertised remotes).

As the final patch in a well-reviewed series that has gone through multiple iterations (now at v4), this appears to be a clean-up change that completes the standardization of field handling in the promisor-remote protocol. The series as a whole has already addressed all reviewer feedback and appears ready for merging.