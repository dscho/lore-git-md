This is a bugfix patch reverting a change to byte-swapping macros that caused test failures on s390x (big-endian) systems. The patch reverts commit 6547d1c9 which had modified `compat/bswap.h` to use compiler built-in byte-swap functions when available, regardless of endianness. 

The core issue is that the Git codebase assumes the `bswap32/64` macros are only defined on little-endian systems, using this assumption to redefine network-to-host byte order macros (`ntohl/ntohll`). The original change broke this assumption by defining the macros universally when built-ins were available, causing test failures on big-endian platforms like s390x.

The revert removes the compiler built-in detection logic entirely, falling back to the previous implementation that only defines the byte-swap macros for x86/x86_64 architectures when using GCC. The change is minimal - just 1 line kept versus 13 removed - and clearly targets a specific regression. 

This appears to be part of a larger series (1/6) though no cover letter is provided here. The fix seems straightforward and well-justified by the test failures it addresses. The commit message clearly explains both the problematic change and its effects, though it doesn't discuss why the original assumption about `bswap` macros being little-endian-only exists in the codebase.