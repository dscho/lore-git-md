Thread summary:

Platform compatibility series adding byte order detection improvements, currently at patch 2/6. Focuses on `compat/bswap.h` to support `__BYTE_ORDER__` macro for GCC/Clang/ICC when traditional libc macros (`__BYTE_ORDER`/`BYTE_ORDER`) are unavailable (e.g. MinGW). Does not support MSVC (version 19.43 confirmed lacking this). Defines Git's internal `GIT_BYTE_ORDER`, `GIT_LITTLE_ENDIAN`, and `GIT_BIG_ENDIAN` constants using compiler intrinsics where available, maintaining existing hardcoded fallback. Changes limited to `compat/bswap.h` with clear motivation for platforms lacking libc byte order macros. No reviews or discussion noted yet for this patch in the series.