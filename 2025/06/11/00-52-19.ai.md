This is part 5/6 of a refactoring series improving submodule remote lookup logic. The patch moves the `get_default_remote_submodule()` function in `builtin/submodule--helper.c` to appear after `resolve_relative_url()`, preparing for future changes where it will depend on that function. The change is purely code reorganization with no behavioral impact - the function is moved verbatim from one location to another in the same file.

The commit explains this is preparatory work to make future refactoring easier to review, where `get_default_remote_submodule()` will need to use `resolve_relative_url()`. By moving it now, the actual functional changes can be reviewed separately from this mechanical relocation. The patch follows the series' pattern of incremental, behavior-preserving changes that build toward the larger goal of improving submodule remote handling. The diff shows the straightforward movement of 15 lines of code with no modifications beyond the new location.