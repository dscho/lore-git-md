CI/Windows Coverity Build Fix (part 2/2)

This second patch in the series adds build log output when Coverity analysis fails, complementing the PATH ordering fix from the first patch. The change modifies `.github/workflows/coverity.yml` to display the contents of `cov-int/build-log.txt` when `cov-build` fails, helping diagnose analysis problems that previously produced cryptic failures.

The technical change is straightforward error handling logic that pipes the build log to stdout before exiting with failure status. This addresses a specific pain point in debugging Coverity issues on Windows where the root cause wasn't visible in the CI output. The patch maintains the same cross-platform compatibility as the first in the series, with the log file being a standard Coverity artifact regardless of OS.

This concludes the two-part fix for Windows Coverity builds, with both patches having addressed Junio Hamano's earlier concerns about PATH handling and now adding better debugging capabilities. The changes are confined to GitHub Actions workflow configuration with no source code impact, and the thread has reached consensus on the solution. The series appears ready for merging with both technical and debugging improvements now in place.