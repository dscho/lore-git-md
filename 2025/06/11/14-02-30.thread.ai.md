CI/Windows Coverity Build Fix Summary:

This thread addresses Windows-specific Coverity build failures caused by DLL name conflicts between Coverity 2025.3 binaries and Git for Windows SDK. Johannes Schindelin's two-patch series modifies `.github/workflows/coverity.yml` to resolve PATH ordering issues and improve failure debugging. The first patch switches PATH modification from prepend to append based on Coverity recommendations, removing a problematic `cygpath -au` conversion since `RUNNER_TEMP` already provides Unix-style paths in GitHub Actions. The second patch adds build log output when Coverity analysis fails, displaying `cov-int/build-log.txt` contents to help diagnose previously cryptic failures.

Key technical details: The changes affect only `.github/workflows/coverity.yml`, with line 147 modified to use `$PATH:$RUNNER_TEMP/cov-analysis/bin` and additional error handling to pipe build logs to stdout. The solution specifically addresses Windows build issues where Coverity's DLLs interfered with Git for Windows' toolchain, causing `windows.h` resolution problems. All changes maintain cross-platform compatibility while solving Windows-specific problems, with no source code impact beyond CI configuration.

The discussion resolved Junio Hamano's implementation concerns about PATH handling and debugging approach. Both patches have reached consensus, with Junio indicating readiness to merge. The series provides a complete solution that: 1) fixes the DLL conflict through proper PATH ordering, 2) removes non-portable `cygpath` usage, and 3) adds crucial debugging output for future CI failures. The changes follow Coverity best practices while being properly confined to GitHub Actions workflow configuration, with clear documentation of the Windows-specific considerations.