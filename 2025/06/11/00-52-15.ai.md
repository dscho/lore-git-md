First patch in a refactoring series to improve submodule remote lookup logic. Moves duplicate implementations of `starts_with_dot_slash()` and `starts_with_dot_dot_slash()` helper functions from submodule--helper.c and submodule-config.c into dir.h as static inline functions. The helpers check path prefixes while respecting cross-platform path matching rules via PATH_MATCH_XPLATFORM flag.

The change is straightforward infrastructure work needed for later patches in the series. The commit message notes some consideration was given to naming (whether to append `_platform` suffix) but opts for the simpler names. The patch removes 24 lines of duplicate code while adding 23 lines to dir.h with proper documentation comments.

This appears to be a clean preparatory change - the implementation remains identical, just centralized. The patch touches core header dir.h and two submodule-related files, with no behavior changes. Follow-up patches will build on this shared helper functionality for URL-based submodule remote matching.