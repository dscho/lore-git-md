This thread tracks a 6-part series refactoring Git's submodule remote lookup logic, with discussion now focused on fixing newly discovered memory leaks before the series can proceed. The changes began with infrastructure cleanups - consolidating helper functions and removing `the_repository` dependencies - then progressed to improving remote name fallback logic for submodules in detached HEAD state. The key addition was URL-based remote matching via `repo_remote_from_url()`, which first tries matching a submodule's configured URL before falling back to existing heuristics.

While the technical revisions for repository parameterization and crash fixes are complete, Junio C Hamano identified memory leaks in the new URL matching logic when running under LeakSanitizer. The leaks (1049 bytes across 6 objects) occur during submodule updates with invalid commits, specifically in config parsing allocations that aren't properly freed during error conditions. This isn't just a test artifact - it affects real-world usage, making it a blocking issue. The author had planned to resend the series unchanged after the release freeze, but must now address these leaks first. The CI failure provides concrete reproduction details to help diagnose the issue.

The discussion has involved Lidong Yan (who suggested code organization improvements) and Junio (who flagged the memory leaks). With the release freeze now over, attention turns to fixing the leaks so the series can progress toward inclusion. The changes span multiple files (dir.h, remote.c, submodule--helper.c) and maintain backward compatibility while adding more robust remote lookup behavior for submodules.