This is a refactoring patch in the v4 series enhancing promisor-remote protocol functionality. The email documents Christian Couder's implementation of Junio Hamano's suggestion to replace a custom linked list with a more standard `string_list` structure for managing promisor remote information.

The key changes in this version are:
- Replaced the custom linked list implementation with a `string_list` where each item's string field holds the remote nickname and the util field points to a `promisor_info` struct
- Renamed `remote_nick_find()` to the more descriptive `promisor_info_find()`
- Maintains sorting of the list where beneficial

This addresses Junio's earlier feedback about using more familiar, battle-tested collection types rather than custom implementations. The change improves code maintainability and readability while keeping the same functionality. The implementation appears straightforward and aligns with the series' overall goal of improving the promisor-remote infrastructure.

The patch represents a minor but meaningful improvement to the series' internal data structures, following through on design feedback from earlier reviews. It doesn't introduce new functionality but makes the codebase more consistent with Git's established patterns.