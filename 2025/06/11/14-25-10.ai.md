CI/Windows Coverity Build Fix follow-up.

This is Junio Hamano's response to Johannes Schindelin's second patch in the series addressing Windows Coverity build issues. The patch adds build log output when Coverity analysis fails, which Junio accepts and plans to merge along with the first patch in the series. 

The change is straightforward - it modifies `.github/workflows/coverity.yml` to display the contents of `cov-int/build-log.txt` when the `cov-build` command fails. This provides immediate visibility into analysis failures rather than requiring manual log inspection. The implementation wraps the existing `cov-build` command in a conditional that prints the log before exiting on error.

Junio's response indicates no further technical concerns with this particular change, unlike his earlier feedback about PATH handling in patch 1/2. The thread appears to be reaching resolution, with both patches now accepted pending any final verification. The changes remain confined to CI configuration with no impact on Git's core functionality.