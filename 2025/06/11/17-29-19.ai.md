This is a final polish patch in the `--compact-summary` feature series, addressing a minor test implementation detail in the config support tests. The email captures Junio Hamano agreeing with Eric Sunshine's suggestion to use `\EOF` rather than `EOF` as the heredoc delimiter in the test cases, since no variable interpolation is needed in the expected output. This is a straightforward improvement to the test robustness, ensuring the heredoc contents are treated literally.

The change affects the test scripts added in the previous patches that verify the new `merge.stat=compact` config option produces identical output to the `--compact-summary` command-line flag. The heredoc syntax adjustment doesn't modify any runtime behavior - it's purely about making the test definitions more precise by preventing any potential shell expansion of the expected output patterns.

This concludes the review feedback loop on the test implementation details for the feature. With this change, the series appears ready for finalization, having addressed both the core functionality and all raised testing considerations. The interaction remains focused on the merge/pull output formatting without expanding scope to other diff output configurations.