This is the final patch in a bugfix series addressing signal handling issues in the Git daemon, specifically focusing on ensuring proper EINTR behavior during poll() operations. The patch introduces explicit control over SA_RESTART behavior around poll() calls in the daemon's service loop. It adds a new `set_sa_restart()` helper function that toggles the SA_RESTART flag on the SIGCHLD handler, ensuring poll() will properly interrupt when child processes terminate (needed for zombie reaping). The implementation maintains the existing signal handling architecture while adding this fine-grained control.

Key technical details include:
- New `set_sa_restart()` function that modifies SA_RESTART flag dynamically
- Wrapping poll() calls with SA_RESTART disable/enable pairs
- Maintains compatibility with both POSIX and non-POSIX platforms
- Preserves the existing child reaping logic while making it more reliable
- Follows the series' pattern of centralizing signal handling control

The patch builds on previous discussion that established:
- SA_RESTART must be controllable for correct EINTR behavior
- Empty signal handlers are necessary on some platforms
- sigaction() is the modern, POSIX-compliant approach
- Cross-platform support requires careful flag management

This appears to be the final implementation refinement in the series, addressing the core technical requirements while maintaining clean code organization. The changes are focused and well-contained to the daemon's service loop, with proper attention to platform compatibility and signal handling correctness.