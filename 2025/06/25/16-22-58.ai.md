This is a code organization review in the ongoing Git daemon signal handling bugfix series. Junio C Hamano provides specific implementation feedback on Carlo Marcelo Arenas Belón's patch that modernizes signal handling using sigaction().

The key suggestion is to reorganize the signal-related code in daemon.c to group all conditional compilation blocks together and move them earlier in the file. Junio proposes moving the signal handler setup code (set_signal_handler, rearm_signal_handler, set_sa_restart) into a single #ifndef USE_NON_POSIX_SIGNAL block near the top, keeping the primary code paths cleaner. He includes a diff showing this reorganization while noting the USE_NON_POSIX_SIGNAL naming still needs work.

The changes maintain all existing functionality while improving code structure:
- Child handler implementation remains unchanged
- Signal setup logic is identical but better grouped
- The service_loop() call to set_signal_handler is updated to pass child_handler
- All platform-specific behavior is preserved (POSIX vs non-POSIX paths)

Junio acknowledges this is a style/readability improvement rather than a functional change, noting the file already has IPV6-related conditional compilation that makes such cleanups valuable. The review focuses on maintainability as the technical approach (using sigaction() with proper SA_RESTART handling) has already been accepted in previous discussion.

This continues the thread's progression from technical justification to implementation refinement, with Junio now focusing on code organization details after earlier discussions settled the architectural approach. The patch remains part of the broader effort to fix platform-specific signal handling issues in the Git daemon, particularly around zombie process cleanup and crash prevention.