This is a follow-up discussion in the finalized v5 series enhancing promisor-remote protocol validation. The email addresses final review feedback from Justin Tobler on patch 1/5, which refactors internal data structures from `strvec` to `string_list` with explicit `promisor_info` structs.

Christian Couder confirms several changes made in response to Justin's v4 review:
1. Fixed a typo ("use use" -> "use")
2. Kept the variable name `config_info` despite suggestion for `promisor_info_list`, arguing the config context is important
3. Clarified the BUG message about NULL URLs to say "URL is NULL" instead of "invalid URL"
4. Explains the protocol design choice to avoid trailing semicolons in the promisor-remote advertisement format, drawing analogy to SP-separated items in protocol-v2

The discussion confirms these were the last open items for this patch, with all changes now incorporated into the final v5 series that is awaiting merge. The technical details focus on internal implementation choices (string handling, protocol formatting) rather than introducing new functionality, as this was a refactoring patch preparing for the protocol extensions implemented in later patches.