This is a code organization review in the ongoing Git daemon signal handling series. Junio C Hamano provides feedback on Carlo Marcelo Arenas Belón's patch that modernizes signal handling using `sigaction()`. 

Junio suggests moving all signal-related code together earlier in the file to reduce `#ifdef` noise in the main code paths, though he acknowledges the file already has some IPV6-related conditional compilation. He provides a concrete diff showing how to reorganize the code to group the signal handling functions (`set_signal_handler`, `rearm_signal_handler`, and `set_sa_restart`) together under the `USE_NON_POSIX_SIGNAL` conditional block. 

The review notes that the `USE_NON_POSIX_SIGNAL` naming still needs work before finalizing the series, but Junio hasn't yet proposed an alternative. The suggested reorganization would make the code more maintainable by keeping platform-specific signal handling logic in one place rather than scattered through the file. This continues the thread's progression from debating technical approaches to refining implementation details.