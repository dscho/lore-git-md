This is a platform compatibility patch that's part of a series addressing signal handling in the Git daemon. The patch introduces a new `USE_NON_POSIX_SIGNAL` build flag to better handle systems that lack proper POSIX signal support, particularly around the `SA_RESTART` flag.

The change centralizes the handling of `SA_RESTART` by moving platform-specific workarounds from individual header files into a unified approach in `compat/posix.h`. It affects several platforms including Windows, NonStop, QNX, and MINGW, which previously had custom workarounds. The patch also updates the build system (Makefile, configure.ac, meson.build) to properly detect and set this flag.

This appears to be a well-considered change that's part of a larger effort to clean up signal handling across platforms. The commit message is clear about the motivation, and the implementation looks thorough, touching all the necessary build system components. The patch follows the ongoing discussion in the thread about proper signal handling modernization.