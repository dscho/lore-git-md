Thread identity: Refactoring patch moving clang-format's 'RemoveBracesLLVM' style rule from CI-specific configuration to main project configuration in `.clang-format`.

Core problem/goal: Standardize the optional brace removal for simple single-statement control flow bodies (if/else/for/while) across all development, not just CI checks, following successful testing since July 2024 (commit 1b8f306612).

Current status: The rule has been experimentally enforced in CI since mid-2024 without issues. This patch makes it a permanent project-wide standard by adding `RemoveBracesLLVM: true` to `.clang-format` and removing the temporary CI workaround.

Key technical details:
- Modifies `.clang-format` to include the brace removal rule
- Simplifies `ci/run-style-check.sh` by removing 17 lines that generated a modified config
- Maintains identical formatting behavior but applies it universally
- References LLVM coding style as the inspiration

Implementation notes:
- The change is straightforward with no expected compatibility issues
- Follows from successful real-world testing in CI environment
- Reduces maintenance overhead by eliminating special-case CI handling

The patch appears uncontroversial as it codifies an already-tested formatting preference without changing the actual formatting rules, just their scope of application. No open questions or objections noted in the commit message.