This is a user question about Git's revision range functionality, specifically asking how to exclude tagged commits from `git log` output. The email comes from M Hickford who is trying to filter out tagged commits when viewing history between two versions (v2.3.0 to v2.4.0).

The user reports that using `--exclude=ref/tags/v*` with `git log` doesn't work as expected - it fails to exclude any commits. They've found a workaround using `--decorate` with grep to filter out lines containing "tag: v", but note this is "inelegant".

The core issue appears to be a misunderstanding of how `--exclude` works in Git - it's meant for excluding references from reachability checks, not for filtering commit display. The grep workaround works because `--decorate` adds tag information to the output that can be filtered textually, though this approach loses the rich formatting of normal `git log` output.

This seems like a genuine user question rather than a patch submission, so there's no code quality to assess. The question touches on revision selection and output formatting capabilities in Git's log command. Similar functionality might be achievable through more advanced revision syntax or plumbing commands, but the email doesn't propose any specific changes to Git itself.