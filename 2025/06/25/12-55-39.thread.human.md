This thread focuses on optimizing Git's pathspec bloom filters, addressing Kai Koponen's report that combined pathspec queries were slower than separate invocations. Lidong Yan's v4 patch series introduces a new `struct bloom_keyvec` abstraction to manage multiple bloom keys, replacing direct pointer usage in `rev_info`. The changes touch core bloom filter handling while maintaining the existing fast path for single pathspecs.

The technical work includes a new API for managing bloom keys (`bloom_keyvec_new/free`), updated filter checking logic (`bloom_filter_contains_vec`), and careful handling of pathspec magic bits. The series has undergone thorough review from Junio Hamano, Patrick Steinhardt, and Derrick Stolee, with all substantive feedback addressed. Stolee's latest review confirms the technical approach while suggesting minor presentation improvements like performance measurements and code organization.

The final patch enables bloom optimization for multiple literal pathspecs while preserving the existing behavior for non-literal cases. Yan has acknowledged Stolee's remaining suggestions and plans to address them in the next version. The series appears ready for merging, having successfully addressed the original performance issue while maintaining backward compatibility. The changes demonstrate measurable speed improvements, particularly for exact-case path matches, though case sensitivity optimizations remain as future work.