Thread identity: User question from M Hickford about excluding tagged commits from `git log` output between versions v2.3.0 and v2.4.0, with technical responses from Junio C Hamano and Jacob Keller.

Core problem: How to filter out tagged commits when viewing history with `git log`. Original attempt using `--exclude=ref/tags/v*` failed because `--exclude` affects reference selection for reachability calculations, not output filtering. Current workaround uses `--decorate` with grep to remove lines containing "tag: v", though this loses formatting.

Key technical details: Junio explained Git's revision range model as set operations between positive/negative commit sets based on reachability. The v2.3.0..v2.4.0 range specifies endpoints without using `--all` or `--glob`, making `--exclude` irrelevant in this case. Fundamental limitation: cannot arbitrarily exclude intermediate commits (like tags) while maintaining proper reachability relationships. `--exclude` operates on reference selection phase, not post-processing. Keller's follow-up maintains this technical framing while probing for underlying use case.

Current status: Junio has clarified this isn't a missing feature but a conceptual limitation of Git's history traversal model. The grep workaround remains the practical solution unless the underlying model changes. Keller's response suggests the thread may conclude unless Hickford provides additional context about their specific needs.

Participant positions: M Hickford presented the problem and initial workaround. Junio provided authoritative technical explanation of why the desired filtering isn't possible within current Git revision semantics. Keller's follow-up seeks to understand the motivation behind the request, possibly to explore whether alternative approaches could meet the actual need while working within Git's constraints.

Open questions: Whether Hickford has additional context about their specific use case that might reveal alternative solutions. The technical limitation itself is clearly explained and unlikely to change without model modifications.

Related work: General `git log` filtering capabilities and revision selection syntax. No specific related patches mentioned, but this clarifies behavior of existing features and may inform future discussions about revision walker improvements.