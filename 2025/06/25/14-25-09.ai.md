Platform compatibility patch in the final v4 series for git-credential-netrc, making the Perl shebang more portable for development use. This change follows the already-merged technical solution for symbolic port handling from v3, with v4 being purely about final polish.

The patch modifies the shebang line in git-credential-netrc.perl from a hardcoded `/usr/bin/perl` to use `#!/usr/bin/env perl`, allowing the script to find Perl via PATH when run uninstalled during development. This matches Git's common practice for Perl scripts while maintaining the installed version's PERL_PATH behavior. The change is minimal (single line modification) and focused on developer convenience rather than affecting the core functionality of the credential helper.

Key technical details carried forward from the merged v3 series remain relevant to this script, including the shared `port_num` validation infrastructure and getservbyname resolution for symbolic ports. The patch maintains all existing functionality while improving portability.

This submission continues the established pattern of proper versioned patch series submission, with this being patch 1/3 in the v4 series that primarily addresses whitespace fixes in tests (as noted in the thread context). The change is straightforward and unlikely to generate discussion, serving as a minor quality-of-life improvement for developers working on the credential helper.