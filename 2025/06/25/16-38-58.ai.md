Security vulnerability in Git's Bash completion (command injection via branch names with `>` character). Junio Hamano weighs in on the discussion, directly responding to Kristoffer Haugsbakk's earlier suggestion to modify `git-check-ref-format` to disallow `>` characters in refnames. Hamano firmly disagrees with this approach, characterizing the issue as purely a bug in the completion code's failure to properly escape shell metacharacters rather than a problem with Git's refname validation rules.

The maintainer's position reinforces Phillip Wood's earlier technical argument that shell safety should be handled at the interface layer (completion scripts) rather than the data model layer (refname validation). Hamano's brief but pointed response suggests the solution path is now clear: the fix should focus on proper quoting in the Bash completion script rather than changing Git's refname character allowances. This maintains backward compatibility with existing repositories that may contain branches with `>` characters while addressing the security vulnerability.

The email doesn't propose specific code changes but provides important maintainer guidance that effectively settles the architectural debate. The issue remains open for implementation, with the expected fix location being the Bash completion script's handling of refnames containing shell metacharacters. The thread has now established consensus that the problem is specifically in `git-completion.bash`'s failure to escape special characters during tab completion, not in Git's core refname validation logic.