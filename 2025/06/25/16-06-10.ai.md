Signal handling refactoring in Git daemon, part of the ongoing platform-specific signal handling improvements. Junio C Hamano reviews Carlo Marcelo Arenas Belón's patch that replaces direct `signal()` calls with a `sigaction()`-based approach for SIGCHLD handling in the daemon.

The patch introduces a new `set_signal_handler()` function that centralizes signal setup, using `sigaction()` where available (with SA_NOCLDSTOP and SA_RESTART flags) and falling back to `signal()` for platforms with USE_NON_POSIX_SIGNAL defined. The child_handler() is modified to only rearm signals on non-POSIX systems, addressing an AIX crash issue while maintaining SysV compatibility where needed.

Junio notes two potential improvements: a missing #endif in the conditional compilation block and suggests moving all signal-handling related code together earlier in the file to reduce #ifdef pollution. The review focuses on code organization rather than technical approach, indicating acceptance of the sigaction() modernization while pushing for cleaner abstraction boundaries.

This continues the thread's progression from platform-specific bugfixes to implementation refinement, with the core technical decisions around signal handling modernization now settled. The remaining discussion centers on code structure and maintainability, with Junio advocating for minimizing conditional compilation visibility in main code paths.