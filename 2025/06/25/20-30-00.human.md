This is a user question about Git's revision range syntax. Mirth Hickford asks how to exclude tagged commits when using `git log` with a revision range like `v2.3.0..v2.4.0`. They tried using `--exclude=ref/tags/v*` which didn't work, and mention an awkward workaround involving piping to `grep`.

The email appears to be from someone encountering a real workflow need - wanting to see changes between versions while filtering out tagged commits. The question suggests either a gap in Git's existing revision range syntax or possibly just unfamiliarity with the right incantation. The workaround using `grep` indicates they've put some thought into solving the problem themselves before asking.

This seems like a reasonable question that might prompt discussion about whether Git should support such filtering natively, or if there's already a better way to achieve this that the community can point out. The thread context isn't provided, so we don't know if this is a follow-up to previous discussion or a new inquiry.