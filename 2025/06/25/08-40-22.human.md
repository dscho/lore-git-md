This is a technical discussion about test script optimizations for the new `git repo-info` command's reference format tests.

Phillip Wood responds to Junio C Hamano's suggestions for improving the test scripts in the `repo-info` patch series. Junio had pointed out several shell scripting anti-patterns in the test code, including unnecessary use of `cat` and inefficient pipelines combining `grep`, `cut`, and multiple `sed` commands.

Phillip acknowledges Junio's improvements, noting they not only reduce process overhead but also fix a subtle bug where values containing spaces would be incorrectly truncated by the original `grep | cut` approach. The optimized version uses a single `sed` command with multiple expressions to both extract the value and convert boolean 0/1 to false/true strings.

This exchange continues the thread's focus on refining the test infrastructure for the new command, building on earlier discussions about output formats and test robustness. The changes are purely about test implementation quality and don't affect the command's functionality or interface.