This is a technical refinement in the ongoing Git daemon signal handling series, specifically addressing SA_RESTART behavior around poll() calls. The patch introduces a new `set_sa_restart()` helper function that dynamically toggles the SA_RESTART flag for SIGCHLD signal handling during the daemon's service loop. This ensures poll() will properly return EINTR when child processes terminate, allowing timely zombie process reaping.

The implementation provides both POSIX (sigaction-based) and fallback (signal-based) versions of the helper. Junio C Hamano notes this resolves his earlier confusion from reviewing patch 2/3 in the series. The change is narrowly focused on fixing a specific race condition where SA_RESTART could prevent poll() from being interrupted by SIGCHLD signals on some platforms.

Key technical details include:
- New `set_sa_restart()` function with platform-specific implementations
- Maintains compatibility with non-POSIX systems through conditional compilation
- Directly addresses the OpenBSD zombie process issue by ensuring poll() can be interrupted
- Follows the series' architectural approach of centralizing signal handling logic
- Builds on previous infrastructure like `set_signal_handler()`

The patch represents incremental progress in the broader effort to modernize signal handling while maintaining cross-platform compatibility. It specifically tackles one of the identified race conditions in the daemon's child process management. The implementation appears technically sound and well-integrated with the series' established patterns.