This is a code review comment from Junio C Hamano on the signal handling patch series. The focus here is on the autoconf build system changes to detect SA_RESTART support.

Junio is questioning an empty `#ifdef SA_RESTART` block in the autoconf test code, pointing out that it appears to serve no purpose. This comes as part of the ongoing refinement of the signal handling implementation, where the series has already moved from using the deprecated `siginterrupt()` to modern `sigaction()` with proper SA_RESTART handling.

The discussion has progressed from technical justification to implementation polish, with Junio now focusing on small but important details in the build system code. This follows the pattern seen throughout the thread where initial technical debates about signal handling approaches have given way to careful review of the final implementation details.

The empty preprocessor block appears to be a minor oversight in the autoconf test that checks for SA_RESTART support, and Junio's comment suggests it should either be removed or given a meaningful purpose. This level of scrutiny is typical for the final stages of patch review when the core technical approach has been accepted but the implementation details are being perfected.