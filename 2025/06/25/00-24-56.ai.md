This is a process clarification email in the ongoing discussion about patch submission formatting for the git-credential-netrc series. 

Junio Hamano responds to Maxim Cournoyer's question about whether proper patch threading requires two separate `git send-email` invocations. Junio directs Maxim to the `git-send-email` documentation regarding the `--in-reply-to`, `--thread`, and `--no-chain-reply-to` options, which can achieve the desired threading structure in a single command. 

The email includes a concrete example from the docs showing how versioned patch series should thread when using these options: the v2 cover letter replies to v1's cover letter, while individual v2 patches reply to their own v2 cover letter. This maintains the submission format requirements previously established in the thread while avoiding multiple send steps.

Key technical references:
- `git-send-email` threading behavior with `--in-reply-to`
- Interaction between `--thread` and `--no-chain-reply-to` options
- The illustrated threading pattern for versioned series

This exchange represents the final process-oriented follow-up to the now-merged technical work on symbolic port handling. The response provides authoritative guidance that should resolve Maxim's workflow question while reinforcing the project's patch submission conventions.