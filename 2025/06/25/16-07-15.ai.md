This is a build system refinement patch in the ongoing Git daemon signal handling series. Junio C Hamano is reviewing Carlo Marcelo Arenas Belon's changes to the SA_RESTART detection logic in the autoconf build system.

The key technical point under discussion is an empty `#ifdef SA_RESTART` block in the autoconf test - Junio questions why this empty conditional exists. The patch introduces `USE_NON_POSIX_SIGNAL` as a build flag to handle platforms lacking proper POSIX signal support, replacing the previous ad-hoc compiler approaches. The autoconf test checks for SA_RESTART availability and sets this flag accordingly.

This represents a refinement of the build system infrastructure needed for the signal handling changes. The empty `#ifdef` appears to be a stylistic choice in the autoconf test (perhaps to ensure the header defines SA_RESTART without actually using it), but Junio's query suggests it may need clarification or removal. The patch continues the series' trend of centralizing platform-specific signal behavior while improving the build system's handling of these cases.

The discussion has moved from architectural debates about signal handling approaches to these finer points of implementation, particularly around build system details and code organization. Junio's review focuses on ensuring clarity and correctness in these supporting mechanisms as the series nears completion.