This is the final patch in a 5-patch series enhancing the promisor-remote protocol with configurable validation of remote attributes. The series is now complete and ready for merging.

The patch completes the series' effort to centralize string constants by introducing named constants for the mandatory "name" and "url" fields in the promisor-remote protocol, following the same pattern established in previous patches for "partialCloneFilter" and "token". This change eliminates string literals that were previously repeated throughout the code, improving maintainability and consistency.

The implementation replaces four direct string comparisons and concatenations with references to the new `promisor_field_name` and `promisor_field_url` constants. The diff shows 10 lines added and 4 removed across promisor-remote.c, with changes focused on the protocol capability advertisement and parsing logic. A new comment documents that these constants represent all fields used in the protocol capability.

This patch represents the final polish on the series, which has already addressed all reviewer feedback and passed CI testing. The changes are straightforward and low-risk, maintaining the existing behavior while improving code quality. With this patch, the series achieves its goal of making the promisor-remote protocol more maintainable and extensible while adding configurable field validation.