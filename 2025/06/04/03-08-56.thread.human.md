This thread began with Lidong Yan reporting and fixing a memory leak in `prepare_show_merge()` in Git's revision walking code. The straightforward fix added a missing `free()` call, but Patrick Steinhardt raised a broader question: why didn't the existing CI tests catch this leak? This led to a productive discussion about test coverage gaps and development practices, with Lidong learning about running leak-detection tests (`make test SANITIZE=leak`).

Junio Hamano has now stepped in with a concrete proposal to address the test coverage gap. He suggests adding a new test case that exercises `git show --merge` with an artificially created unmerged state - exactly the scenario where the leak occurred. The test would create merge conflicts without an actual merge by manipulating `MERGE_HEAD` and the index directly, then verify `show --merge` handles this case correctly. This serves both as regression prevention and documentation of expected behavior.

The thread has evolved from a simple memory leak fix to a case study in improving test infrastructure. Lidong had previously volunteered to work on test coverage, and Junio's detailed test case provides clear guidance for that next step. The discussion remains anchored in the original revision.c issue while demonstrating how even small fixes can reveal opportunities to strengthen Git's testing practices.