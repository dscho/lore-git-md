This is a review ack for a 3-patch series extending `git cat-file`'s batch mode functionality. Junio C Hamano indicates the series looks ready to merge to 'next' barring any last-minute feedback from others.

The series (previously reviewed in v1) adds new capabilities for inspecting tree entries in batch mode, including:
- A new `%(objectmode)` format specifier to show file permissions
- Better handling of submodules (now reporting their OIDs rather than treating as empty commits)
- Expanded test coverage, including cases with spaces in paths (though with a known limitation around `%(rest)` that's marked as test_expect_failure)

The review notes the changes address prior feedback and show no obvious issues. The implementation appears solid, with careful attention to edge cases and thorough test coverage. The submodule handling improvements in particular provide more useful output for tooling integration.

This looks like a well-executed extension of `git cat-file`'s capabilities that's likely to merge soon unless other reviewers spot issues. The changes are focused and non-controversial, building on existing batch mode infrastructure to expose more tree entry information efficiently.