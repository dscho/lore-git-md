This is a maintainer ack for the `git cat-file` batch mode extension series. Junio C Hamano indicates he has reviewed the 3-patch series (v2) and found no issues, suggesting it should be merged to 'next' barring any further feedback from others.

The series enhances batch object inspection with:
1. Improved test coverage for non-OID specifications, including explicit handling of paths with spaces (with a known `%(rest)` limitation marked as test_expect_failure)
2. New `%(objectmode)` format specifier exposing tree entry permissions
3. Revised submodule handling that reports submodule OIDs in error messages rather than treating them as empty commits

The maintainer notes this version addresses prior feedback, with the submodule handling approach being notably different from the initial submission. The implementation appears solid with thorough test coverage, and the changes focus on exposing existing tree entry data through the batch interface rather than introducing new core functionality.

This marks the likely final approval before merging, pending any last-minute objections from other reviewers. The series provides tooling-friendly improvements for programmatic inspection of tree entries and submodules while maintaining backward compatibility.