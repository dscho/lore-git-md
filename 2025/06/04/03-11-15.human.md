This is a bugfix patch addressing a memory leak in the commit-graph code. The issue occurs when `read_one_commit()` fails during graph writing - the progress meter allocated by `start_delayed_progress()` isn't properly cleaned up before jumping to the error handling code. The fix is straightforward: adding a `stop_progress()` call before the error-handling `goto`. 

The patch is small (just one line added) and clearly targets a specific resource leak scenario. While the impact is minor (a small memory leak during error conditions), it's a clean fix that follows the pattern used elsewhere in the codebase. The commit message explains the problem and solution concisely. 

This appears to be a standalone fix rather than part of a larger series. The change is uncontroversial and likely to be accepted as it simply brings error handling in line with established practices.