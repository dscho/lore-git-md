This is a naming clarification request in Patrick Steinhardt's ongoing object database (ODB) refactoring series. Toon Claes questions whether the proposed `odb_files_source` name should instead be `odb_files_backend` to better reflect the implementation details.

The thread context shows this is part of a carefully considered naming scheme where:
- The main structure was renamed from `object_directory` to `odb_source` to accommodate future pluggable storage mechanisms
- `backend` terminology was previously discussed and rejected for being too implementation-specific
- The series has already standardized on `odb_` prefix for all related functions

Toon's question highlights the tension between conceptual naming (`source`) and implementation reality (`backend`), though Patrick's earlier reasoning suggests he wants to keep the higher-level abstraction. This appears to be a final naming polish on an otherwise settled design, with the series already receiving positive reviews and nearing completion. The exchange exemplifies Git's meticulous approach to API design where even late-stage patches undergo careful scrutiny of terminology.