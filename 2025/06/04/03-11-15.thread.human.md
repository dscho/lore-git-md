This thread addresses a resource leak in the commit-graph writing code, specifically when reading commits from stdin. The issue occurs when `read_one_commit()` fails during commit-graph generation, leaving a progress meter allocated without proper cleanup. 

The fix is straightforward - a single line addition ensures the progress meter is properly stopped before jumping to the cleanup section when `read_one_commit()` fails. The change targets `builtin/commit-graph.c` in the `graph_write()` function, following established patterns for progress meter cleanup seen elsewhere in the codebase. While small, this fixes a real issue that could lead to resource accumulation during repeated failed operations.

The patch appears well-targeted and correctly implements the described fix. Given the focused nature of the change and its clear correctness, this seems likely to be picked up by the maintainer without requiring further discussion.