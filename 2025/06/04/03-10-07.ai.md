This is a bugfix patch addressing memory leaks in two callers of `repo_logmsg_reencode()`. The patch adds missing calls to `repo_unuse_commit_buffer()` in `builtin/shortlog.c` and `builtin/replay.c` to properly free memory allocated by the encoding conversion.

The changes are straightforward - in `create_commit()` (replay.c), the buffer is freed at the exit point, while in `insert_records_from_trailers()` (shortlog.c), an early return path is fixed to also free the buffer. The patch and commit message are clean and focused, with a clear explanation of what was wrong and how it's being fixed.

The affected functions are part of the shortlog and replay functionality, with the leaks occurring when processing commit messages. The fix ensures proper cleanup of temporary buffers used for message encoding conversion. This appears to be a solid, uncontroversial fix for a real issue, though it would be good to confirm the leaks were actually observable in practice (the commit message doesn't mention any specific leak reports or measurements).