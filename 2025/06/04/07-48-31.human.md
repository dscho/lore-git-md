Memory leak follow-up question. Patrick Steinhardt acknowledges Lidong Yan's straightforward fix for the memory leak in `prepare_show_merge()` but raises an important secondary issue: why wasn't this caught by CI tests?

The original patch (already summarized in the thread context) fixed an obvious leak where the `prune` array wasn't freed after being passed to `parse_pathspec()`. Steinhardt's reply suggests the fix is correct but questions the test coverage, implying this code path might not be exercised in automated testing.

This raises a potentially more significant issue than the leak itself - if this revision walking code isn't being tested, there could be other undetected issues. The exchange remains technical and constructive, with Steinhardt seeking to understand the testing gap rather than criticizing the original patch.