This is a technical discussion about submodule configuration cleanup in patch 2/2 of K Jayatheerth's series. Junio Hamano is reviewing the implementation approach for eliminating redundant `submodule.<name>.active` entries when the submodule is already covered by an active pattern.

The key point of confusion centers on whether the patch implements Junio's suggested approach of using `is_submodule_active()` directly (which would check all activation conditions including patterns and URLs, not just explicit `.active` settings). Junio notes two contradictory statements from the author - first claiming to have implemented something similar to his suggestion, then saying it was done "in a bit different way" - and requests clarification on whether this is truly equivalent behavior.

This exchange represents the final technical review hurdle before the patch can be merged. The thread summary indicates the overall approach using `is_submodule_active()` is sound and maintains backward compatibility, but Junio needs confirmation that the implementation actually follows through on this design rather than introducing subtle behavioral differences. The resolution here will determine whether this patch can join patch 1/2 in being ready for merging.