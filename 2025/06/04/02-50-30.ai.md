This is a final refinement of a bugfix patch in the pack-bitmap.c memory leak series, addressing Taylor Blau's review comments on test implementation details. The core technical fix (unconditional `bitmap_free()` for `roots_bitmap`) remains unchanged and was previously validated through the thread's extensive review process.

Key adjustments in this revision:
- Clarifies that pseudo-merge satisfaction depends on bitmap subset relations rather than size comparisons, per Taylor's explanation of `ewah_bitmap_is_subset()`
- Agrees to remove unnecessary `stableThreshold` configuration in the test case
- Will switch from environment variable (`GIT_TEST_PACK_USE_BITMAP_BOUNDARY_TRAVERSAL`) to repository config (`pack.useBitmapBoundaryTraversal`) for enabling boundary traversal
- Will adopt Taylor's suggested test assertion style using direct numeric comparison rather than file-based expectation checking
- Confirms avoidance of single-quotes in test scripts as a general best practice

The patch maintains all previously resolved aspects including Junio's environment variable handling requirements and ASAN validation, now with improved test implementation quality. This represents the final polish before potential inclusion, with all substantive technical concerns addressed across the v4 review cycle.