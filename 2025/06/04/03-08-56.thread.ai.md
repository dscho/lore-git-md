Memory leak fix in revision walking code, originally reported by Lidong Yan. The patch fixes a straightforward memory leak in `prepare_show_merge()` in revision.c where an array `prune` is allocated but not freed after being passed to `parse_pathspec()`. The fix adds the missing `free(prune)` call.

Patrick Steinhardt raised a secondary issue about why CI tests didn't catch this leak, highlighting a test coverage gap. Lidong Yan has acknowledged the coverage gap and offered to add a test case while asking about local test execution practices as a new contributor. Patrick has now responded with detailed commands for running both regular tests and leak-detection tests using Make and Meson build systems: `make test SANITIZE=leak` and meson's `-Db_sanitize=leak` option. This provides concrete guidance for new contributors while reinforcing the importance of test coverage that could catch issues like the original leak.

The thread now contains both the resolved memory leak issue and ongoing discussion about test coverage improvements. Two open items remain: whether to add specific test coverage for this case (with Lidong volunteering), and whether to document these testing procedures more prominently for new contributors. The technical focus has expanded from revision.c internals to include Git's test infrastructure and development practices, illustrating how simple fixes can reveal both specific test gaps and broader process questions. The original memory leak in `prepare_show_merge()` remains the thread's anchor point, with the discussion now covering the surrounding ecosystem that could prevent similar issues.