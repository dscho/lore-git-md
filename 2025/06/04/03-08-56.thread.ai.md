Memory leak fix in revision walking code, originally reported by Lidong Yan, with evolving test coverage discussion. The core fix addressed a straightforward memory leak in `prepare_show_merge()` in revision.c where an array `prune` was allocated but not freed after being passed to `parse_pathspec()`. 

The thread has progressed through two test coverage proposals: Junio Hamano initially suggested a minimal test creating artificial unmerged index states via direct index manipulation with `update-index --index-info`. Lidong Yan now counters with a more realistic test scenario that creates actual merge conflicts through branch divergence and failed merges, while still exercising the same leak-prone code path. Both approaches would have caught the original leak but Lidong's version provides broader coverage of merge conflict handling.

Key technical details remain the memory management in revision.c's merge state handling, with the discussion now focused on test suite design patterns for merge scenarios. The test proposals differ in their setup methods (manual index manipulation vs. real branch/merge workflow) but share the goal of verifying `git show --merge` behavior in conflicted states.

Open items now include:
1. Choosing between Junio's minimal test and Lidong's more comprehensive version
2. Potential synthesis of both approaches to cover both artificial and real merge cases
3. Final review of whichever test approach is selected
4. The still-pending question of whether to document testing procedures more prominently for new contributors

Participants have expanded from the original trio (Lidong Yan, Patrick Steinhardt, Junio Hamano) with Lidong taking a more active role in shaping the testing solution. The thread exemplifies how simple fixes often reveal broader process questions, in this case evolving from a one-line leak fix to a discussion of optimal test design for merge conflict scenarios.