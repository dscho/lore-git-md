Memory leak fix in revision walking code, originally reported by Lidong Yan. The patch fixes a straightforward memory leak in `prepare_show_merge()` in revision.c where an array `prune` is allocated but not freed after being passed to `parse_pathspec()`. The fix adds the missing `free(prune)` call.

Patrick Steinhardt raised a secondary issue about why CI tests didn't catch this leak, highlighting a test coverage gap. Lidong Yan has acknowledged the coverage gap and confirmed understanding of Patrick's detailed instructions for running both regular tests (`make test`) and leak-detection tests (`make test SANITIZE=leak` or meson's `-Db_sanitize=leak` option). This exchange demonstrates the project's mentoring dynamics for new contributors.

Two open items remain: whether to add specific test coverage for this case (with Lidong volunteering), and whether to document these testing procedures more prominently for new contributors. The technical focus spans from the original revision.c memory leak to Git's test infrastructure and development practices. The thread maintains its anchor in the `prepare_show_merge()` fix while illustrating how simple fixes can reveal both specific test gaps and broader process questions. The next expected development is Lidong's follow-up on adding test coverage for the leak case.