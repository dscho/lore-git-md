Memory leak fix in revision.c's `prepare_show_merge()` with comprehensive test coverage, now in final polished form. The fix addresses Lidong Yan's reported leak where the `prune` array allocated during `parse_pathspec()` wasn't freed. The solution remains a simple `free(prune)` as established in v1.

Test methodology evolved through discussion between Junio Hamano (minimal test approach) and Lidong Yan (realistic conflict scenario), with Junio endorsing Lidong's three-branch conflict test as superior for exercising `git show --merge` behavior. The final test includes:
- Three-branch setup (base, hello, goodbye) with intentional conflicts
- Failed merge state creation
- Verification of revision walking behavior during conflicts
- Correct Git project style (tabs, redirection spacing) per Junio's final review

Technical consensus was reached in v2 with:
1. Agreement on both memory fix and test approach
2. Junio's explicit approval of comprehensive test design
3. No open implementation questions

Junio's final review addressed only minor style nits (indentation and redirection formatting), confirming the patch meets all project requirements. The thread demonstrates how simple fixes can surface important test methodology discussions, particularly for merge-related code paths. Lidong's test now serves as a reference for merge state testing in revision walking code. All participant positions are settled with the patch ready for merging.