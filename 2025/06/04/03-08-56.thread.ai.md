Memory leak fix in revision.c's `prepare_show_merge()` with comprehensive test coverage, now in final polished form. The fix addresses Lidong Yan's reported leak where the `prune` array allocated during `parse_pathspec()` wasn't freed, using a simple `free(prune)` solution established in v1. The test methodology evolved through discussion between Junio Hamano (minimal test approach) and Lidong Yan (realistic conflict scenario), with Junio ultimately endorsing Lidong's three-branch conflict test as superior for exercising `git show --merge` behavior.

The final v3 patch maintains the technical solution unchanged from v2 while addressing Junio's final style nits:
- Three-branch test setup (base, hello, goodbye) with intentional conflicts
- Failed merge state creation
- Revision walking behavior verification
- Corrected formatting (tabs, redirection spacing) per Junio's review

All technical consensus was reached in v2 with:
1. Agreement on both memory fix and test approach
2. Junio's explicit approval of comprehensive test design
3. No open implementation questions

The v3 changes are purely stylistic - fixing indentation to use tabs consistently and correcting redirection spacing (`>` to `>`) in test file creation - confirming the patch meets all project requirements. The thread demonstrates how simple fixes can surface important test methodology discussions, particularly for merge-related code paths. Lidong's test now serves as a reference for merge state testing in revision walking code. All participant positions are settled with the patch ready for merging, showing how even straightforward memory fixes can lead to valuable test infrastructure improvements.