Memory leak fix in revision walking code. The patch addresses a straightforward memory leak in `prepare_show_merge()` in revision.c where an array `prune` is allocated but not freed after being passed to `parse_pathspec()`. The fix adds the missing `free(prune)` call. The change is minimal (single line addition) and clearly correct, with the array being allocated and used only within this function's scope. While not performance-critical (as `prepare_show_merge()` isn't in hot paths), this fixes a genuine memory leak. Patch follows standard contribution practices with proper sign-off.