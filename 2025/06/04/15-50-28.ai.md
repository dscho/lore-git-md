This is a follow-up discussion in the compiler warnings series, specifically addressing patch 4/4 which deals with unreachable-code warnings in the files-backend refs code when `NO_SYMLINK_HEAD` is defined.

Junio C Hamano responds to Patrick Steinhardt's suggestion about modifying the `create_ref_symlink` macro directly rather than working around its usage. Patrick had proposed wrapping the macro definition itself in `NOT_CONSTANT(-1)` to suppress the warning at its source. Junio acknowledges this as a clever approach but notes it wouldn't work for the original case that necessitated the `NOT_CONSTANT` macro's creation.

The exchange shows consideration of two approaches to the warning:
1. The original patch's solution working around macro usage
2. Patrick's more direct approach modifying the macro definition itself

Key technical details:
- The warning occurs in Windows builds where `NO_SYMLINK_HEAD` is defined
- The `create_ref_symlink` macro's behavior differs between symlink/non-symlink cases
- `NOT_CONSTANT` was created for a different but related purpose
- Discussion focuses on whether to fix at macro definition or usage site

This moves the thread toward deciding between two technically valid solutions, with Junio's response suggesting Patrick's approach may be preferable for this specific case while noting it doesn't generalize to all `NOT_CONSTANT` use cases. The exchange demonstrates careful consideration of warning suppression strategies in the codebase.