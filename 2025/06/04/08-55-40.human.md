This is a follow-up discussion in the ongoing `the_repository` removal effort, specifically about naming choices in Patrick Steinhardt's object database (ODB) refactoring series. Toon Claes points out that the documentation comment in `object-store.h` still uses the old terminology of "backends" when referring to object storage, while the series has settled on calling them "sources" (as `odb_source`).

The thread context shows this naming was already discussed and agreed upon in previous rounds (v3), with consensus that `odb_source` is clearer than alternatives like `odb_alternate`. Toon's note is a small but important catch to ensure the documentation aligns with the new terminology being established throughout the codebase.

This is a minor documentation polish in an otherwise mechanical and well-reviewed series that's nearing completion. The core technical work of renaming `raw_object_store` to `object_database` and establishing the `odb_` prefix convention has already been validated through multiple review rounds.