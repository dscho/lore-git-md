This thread addresses compiler warnings issue in Git's curl integration code, triggered by curl 8.14's stricter type checking. The original three-part patch series systematically fixed type mismatches where `int` values were being passed to `curl_easy_setopt()` instead of the expected `long` type. The fixes covered integer constants, variables, and symbolic constants across multiple files (`http.c`, `http-push.c`, etc.), with consensus from reviewers including Jeff King and Collin Funk. Junio merged the series after validation on various platforms.

Johannes Schindelin then discovered additional warnings in macOS CI related to boolean flags (`CURLOPT_UPLOAD`, `CURLOPT_NOBODY`, `CURLOPT_HTTPGET`) that were missed in the initial review. His follow-up patch applies the same solution - adding `L` suffixes to boolean literals - to these remaining cases. Junio is queueing this supplemental fix, noting it completes the warning cleanup. The thread demonstrates strong technical agreement throughout, with the only discussion being about whether to reference prior work in the commit message. This appears to be the final piece in addressing all curl 8.14 typecheck warnings.