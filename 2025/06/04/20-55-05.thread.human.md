This thread addressed a series of compiler warnings that surfaced with curl 8.14's improved type checking, specifically around `curl_easy_setopt()` calls where Git was passing `int` values where `long` was expected. The fixes came in three parts: first for integer literals (adding `L` suffixes), then for integer variables (adding explicit casts in `imap-send.c`), and finally for curl's own symbolic constants (adding `(long)` casts). 

Collin Funk reviewed and confirmed the technical approach, noting these warnings were appearing in GitHub CI environments. Jeff King observed the oddity that curl doesn't define these constants as `long` given their usage, but pointed out that curl's own documentation shows similar casts in examples. With no objections raised and the changes verified as correct, Junio C Hamano indicated the series would be queued for merging. The fixes are minimal and behavior-preserving, resolving real-world build issues while properly aligning with curl's API requirements.