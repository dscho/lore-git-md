This thread addresses compiler warnings that surfaced in Git's curl integration code when using curl 8.14, which introduced stricter type checking. The series systematically fixes type mismatches where Git was passing `int` values to `curl_easy_setopt()` when the API expects `long` parameters.

The three-part series takes a methodical approach: the first patch handled literal integer constants by adding `L` suffixes, the second addressed integer variables with explicit casts in `imap-send.c`, and this final patch now covers curl's own symbolic constants (like SSL and proxy settings) with the same casting pattern. Changes are minimal and focused, touching several files including `http.c` and `imap-send.c` but maintaining all existing behavior.

All patches are now submitted (v1) with clear commit messages explaining the technical rationale. While curl's API design is somewhat inconsistent (defining constants as `int` despite requiring `long`), the series pragmatically follows the documented usage patterns. The changes appear straightforward and uncontroversial, with no open questions raised yet. The series should fully resolve the warnings triggered by curl 8.14's improved compiler checks.