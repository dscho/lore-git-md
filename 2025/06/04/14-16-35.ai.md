This is a feature patch series (v2) adding GPG signing support to `git subtree`. The series consists of two patches, building on previous review feedback from Junio C Hamano and others.

The cover letter explains that v2 makes a documentation adjustment to the first patch's commit message, removing mention of the not-yet-implemented `-S` flag since that was premature in v1. The first patch now presents the `--stuck-long` parsing change as general preparation for future optional parameters rather than specifically tied to GPG signing.

The technical implementation remains unchanged from v1:
- Patch 1 refactors argument parsing to use `--stuck-long` mode for cleaner handling of parameters
- Patch 2 adds `-S/--gpg-sign` support that passes through to underlying `git commit-tree` and `git merge` operations
- Includes comprehensive tests covering various signing scenarios and operations

The changes span the shell script implementation, documentation, and test files. The series addresses a functionality gap where `git subtree` operations weren't respecting commit signing configurations, which could break workflows requiring signed commits. The implementation appears thorough with 113 lines of new tests exercising both basic signing and edge cases.

The thread history shows the author has been responsive to review feedback, with Kristoffer Haugsbakk providing guidance on contribution workflow norms regarding documentation updates. The series is now at version 2 with all previous feedback addressed.