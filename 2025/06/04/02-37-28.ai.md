This is the final patch in a 3-part feature series adding pathspec support to `git diff --no-index`, now at v4 and approved for merging. The thread context shows the series is complete with all technical concerns resolved, and this email contains only minor feedback from Ben Knoble about documentation clarity.

The patch implements pathspec filtering for directory comparisons in `--no-index` mode, matching repository-based diff behavior. Key technical details include:
- Pathspecs only supported when both inputs are directories
- Uses `parse_pathspec()` with `PATHSPEC_NO_REPOSITORY` flag
- Implements directory traversal with `read_directory_contents()` using `match_leading_pathspec()`
- Adds extensive test coverage (75+ lines) for various pathspec scenarios
- Documents the new functionality in git-diff.adoc

Ben Knoble's feedback notes the commit message uses `git diff-tree` as its example rather than `git diff`, which could be slightly confusing, and suggests the pathspec explanation could be more concise since readers are likely familiar with pathspec concepts. He also notes a missing period in the example command. These are minor documentation nits that don't affect the technical implementation.

The patch builds on the foundation from patches 1-2 (prep work and `PATHSPEC_NO_REPOSITORY` flag) to complete the feature. With maintainer approval and no outstanding technical issues, this concludes the series.