This is a small bugfix targeting the bundle-uri debug output functionality. The patch changes a `printf()` call to `fprintf(fp, ...)` in the `print_bundle_list()` function to ensure debug output goes to the specified file handle rather than always printing to stdout.

The change is straightforward and corrects what appears to be an oversight in the original implementation - the function takes a FILE* parameter but wasn't consistently using it for all output. This affects debugging output for bundle heuristics, which are part of Git's bundle URI feature for downloading bundles from remote servers.

The fix is minimal (a single line change) and well-contained, though it's worth noting the commit message could be more specific about why this matters (potential for debug output to go to the wrong place when the function is trying to write to a specific file). The patch appears technically sound and aligns with the function's documented purpose.