Thread identity: Bugfix series addressing compiler warnings in Git's curl integration code, triggered by curl 8.14's improved warnings. Originating author not specified in this summary.

Core problem: Type mismatches when calling `curl_easy_setopt()` - passing `int` values where `long` is expected. The three-part series systematically addresses integer constants (1/3), integer variables (2/3), and symbolic constants (3/3) to fully resolve the warnings while preserving behavior.

Current status: Complete at v1 with all 3 patches reviewed and approved:
1. Integer constants - added `L` suffixes
2. Integer variables - added explicit `(long)` casts in `imap-send.c`
3. Symbolic constants - added `(long)` casts to curl's own constants
Junio C Hamano confirms the series will be queued for merging after Jeff King's final review of patch 3/3.

Key technical details:
- Files touched: `http.c`, `http-push.c`, `imap-send.c`, `remote-curl.c`
- Specific fixes cover `CURLOPT_HTTPGET`, `CURLOPT_SSL_VERIFYPEER`, `CURLOPT_POSTFIELDSIZE`, `CURLOPT_PORT`, SSL verification options, and constants like `CURLSSLOPT_NO_REVOKE`, `CURL_REDIR_POST_ALL`, `CURLPROXY_*`
- Warnings only appear with `-O2` due to curl 8.14's commit 79b4e56b3
- Each patch has clear technical rationale in commit messages

Open questions: None - all technical concerns resolved. Jeff King noted the oddity of curl not defining these constants as `long` given their purpose, but confirmed the documentation shows explicit casts in examples.

Participant positions:
- Collin Funk provided Reviewed-by for all patches, confirming the approach matches their analysis
- Jeff King reviewed patch 3/3 and agreed with the casting approach despite the API oddity
- Junio C Hamano confirmed the series will be merged, concluding the thread
- Reviews confirm the warnings affect real environments (Debian unstable, GitHub CI)

Related work: The changes align Git's usage with curl's documented API requirements while preserving behavior. The series follows a logical progression through different categories of fixes (literals, variables, symbolic constants) to comprehensively address the warnings introduced by curl 8.14.