Thread identity: Bugfix series addressing compiler warnings in Git's curl integration code, triggered by curl 8.14's improved warnings. Originating author not specified in this summary.

Core problem: Type mismatches when calling `curl_easy_setopt()` - passing `int` values where `long` is expected. This was always technically incorrect but only became visible as warnings with curl 8.14. The series is split into three logical parts: integer constants (1/3), integer variables (2/3), and symbolic constants (3/3).

Current status: Complete at v1 with all 3 patches submitted:
1. Integer constants - adds `L` suffixes to constants passed to curl
2. Integer variables - adds explicit `(long)` casts for variables in `imap-send.c`
3. Symbolic constants - adds `(long)` casts to curl's own symbolic constants
Changes are minimal and focused on fixing curl 8.14 warnings while preserving behavior.

Key technical details:
- Files touched: `http.c`, `http-push.c`, `imap-send.c`, `remote-curl.c`
- Specific fixes in patch 1: `CURLOPT_HTTPGET`, `CURLOPT_SSL_VERIFYPEER`, `CURLOPT_POSTFIELDSIZE`
- Patch 2 focuses on `imap-send.c`: casts `srvc->port` (for `CURLOPT_PORT`) and `srvc->ssl_verify` (for SSL verification options)
- Patch 3 handles curl's symbolic constants: `CURLSSLOPT_NO_REVOKE`, `CURL_REDIR_POST_ALL`, multiple `CURLPROXY_*` constants
- Warnings only appear with `-O2` optimization due to curl 8.14's commit 79b4e56b3
- Each patch has clear commit messages explaining the technical rationale

Open questions: None raised yet - appears to be straightforward fixes awaiting review/merge. The series is now complete and should resolve all curl 8.14 warning issues.

Participant positions: No reviews or feedback shown yet on any submission. The technical approach appears uncontroversial and the changes are minimal while being technically correct.

Related work: Debian unstable's curl 8.14 rollout exposed these warnings, though the underlying type mismatches existed previously. The changes align Git's usage with curl's documented API requirements while preserving all existing behavior. The series follows a logical progression through different categories of fixes (literals, variables, symbolic constants).