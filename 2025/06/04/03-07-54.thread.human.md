This thread reports and fixes a memory leak in `fetch-pack`'s error handling path. The `cmd_fetch_pack()` function would fail to free the `sought_to_free` array when `finish_connect()` encountered an error. The patch adds proper cleanup by introducing a `goto` label to share the existing memory freeing logic between successful and error paths. The change is minimal (5 lines modified) and focused specifically on fixing the leak without introducing new complexity. The fix appears correct and ready for merging.