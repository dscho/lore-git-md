This is a small bugfix for the bundle-uri debug output functionality. The patch changes a `printf()` call to `fprintf(fp, ...)` in the `print_bundle_list()` function to ensure debug output goes to the specified file handle rather than always to stdout. 

The change is straightforward and corrects what appears to be an oversight in the original implementation - the function takes a FILE* parameter but wasn't consistently using it for all output. The commit message is brief but accurately describes the minimal change. 

This is a low-impact change affecting only debug output, so it's unlikely to be controversial. The patch is cleanly applied against the current codebase and touches only the intended line. While trivial, it's a sensible improvement to make the debug output behavior more consistent.