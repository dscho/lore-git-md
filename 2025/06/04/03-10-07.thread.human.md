This thread addresses memory leaks in callers of `repo_logmsg_reencode()`, with Lidong Yan's initial patch adding cleanup for commit buffers in `builtin/shortlog.c` and `builtin/replay.c`. The replay.c change was straightforward, but Patrick Steinhardt suggested restructuring the shortlog.c fix to use a `goto out` pattern for more consistent resource management.

Lidong has now submitted a v2 patch incorporating Patrick's feedback. The revision improves the control flow in shortlog.c by ensuring cleanup happens even in early return cases, while confirming this approach is safe even with initialized-but-unused strbufs. The technical discussion has reached consensus, with no outstanding objections. Both the original leak fixes and the refined implementation approach now appear ready for merging.