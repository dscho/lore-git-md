This is a bugfix follow-up in a thread about a resource leak in commit-graph writing. Patrick Steinhardt is reviewing Lidong Yan's patch that fixes a progress meter leak when reading commits from stdin fails. The original patch correctly adds a missing `stop_progress()` call in the error path of `graph_write()`.

Patrick confirms the patch looks good, noting that the progress meter cleanup doesn't need a NULL check since `stop_progress_msg()` handles that internally. He also suggests (as a nit) that related memory leak fixes could be grouped together in future patch series to make reviewing easier.

The fix is small and focused, addressing a clear resource leak scenario in commit-graph operations. The review indicates the change follows established patterns in the codebase and appears correct. This seems like a straightforward improvement that should be uncontroversial to merge.