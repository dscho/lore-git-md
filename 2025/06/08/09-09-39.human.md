This is the second patch in a documentation standardization series converting Git's man pages to a new AsciiDoc format. It focuses on `rev-list-description.adoc`, updating the syntax for commit range notation.

The patch mechanically converts the documentation of commit range syntax (like `A..B` and `A...B`) to use backticks consistently around the notation examples. This matches the new documentation style being applied across Git's man pages. The changes are straightforward - replacing single-quoted ranges with backtick-wrapped versions while preserving all the explanatory text.

As part of the larger 9-patch series standardizing `git-log` documentation, this change follows the same pattern as the first patch but applies it to the commit range syntax section. The diff shows 3 lines changed, all simple syntax updates with no content modifications. The patch appears clean and follows the established conversion patterns from prior documentation updates.