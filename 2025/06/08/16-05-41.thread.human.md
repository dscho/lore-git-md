This thread introduces a new `--compact-summary` option for `git merge` and `git pull`, proposed by maintainer Junio Hamano. The feature enhances merge output by adding `(new)` and `(gone)` annotations to the standard diffstat, providing clearer visibility of file operations without cluttering the output. 

The implementation is thorough, adding the option to both commands' documentation while carefully integrating with existing diff machinery via a new `MERGE_SHOW_COMPACTSUMMARY` flag. Test cases verify the new markers appear correctly for created/deleted files. The change is backward-compatible and maintains Git's existing output formats while offering this more informative variant.

As the patch comes from the maintainer and follows Git's established patterns for output formatting, it's likely to be merged without significant discussion unless reviewers spot edge cases in the diffstat handling. The focused scope and clean implementation suggest this could graduate quickly to `next` and then `master`.