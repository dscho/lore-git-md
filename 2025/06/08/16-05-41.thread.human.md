This thread discusses Junio Hamano's proposal to add a `--compact-summary` option to `git merge` and `git pull`, which would enhance the diffstat output with `(new)`/`(gone)` annotations for added/removed files. The initial implementation is complete, but D. Ben Knoble raised concerns about whether this feature should be implemented as a standalone option or as part of a more general output configuration system.

In response, Hamano has proposed extending the existing `merge.stat` configuration to accept either a boolean (for backward compatibility with the current `--stat` behavior) or the string 'compact-summary' to enable the new format. This approach would allow users to configure their preferred diffstat format without requiring command-line flags, while avoiding a more disruptive redesign of the output system. The discussion remains focused on this specific enhancement, with no new technical implementation details introduced in this latest exchange. The key open question is whether this incremental configuration approach satisfies the concerns about output flexibility.