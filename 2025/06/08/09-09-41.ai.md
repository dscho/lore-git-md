Documentation patch converting line range format descriptions to new AsciiDoc standards, part of the ongoing man page standardization effort (4/9 in series).

This patch updates `line-range-format.adoc` to match the documentation style established in prior patches. The changes are mechanical but important for consistency across Git's documentation. Key changes include:
- Replacing angle-bracketed placeholders (`<start>`, `<end>`) with underscored versions (`_<start>_`, `_<end>_`)
- Wrapping regex patterns and offset syntax in backticks (`/<regex>/`, `+<offset>`)
- Maintaining all existing content while applying the new formatting rules

The patch follows the same high-quality pattern seen in previous documentation conversions from this author. It handles the special case of line range syntax used with `git log -L` options, ensuring the formatting changes don't alter the technical meaning while improving readability. The changes affect how placeholders and special syntax are rendered in the final man page output, but don't modify any actual command behavior.

This continues the systematic approach seen in patches 1-3, now covering the line range format documentation. The remaining patches in the series will presumably address other related documentation files (`pretty-formats.adoc`, `diff-options.adoc`, etc.) following this same pattern. No new technical content is introduced - this is purely a documentation presentation improvement.