This is a user experience improvement patch in the `imap-send` series, specifically enhancing progress output during message sending. The change modifies two functions (`append_msgs_to_imap` and `curl_append_msgs_to_imap`) to display the destination folder name alongside the message count, making the output more informative. The patch shows the destination folder (stored in `server->folder`) in the progress message, changing from "Sending N message(s)" to "Sending N message(s) to FOLDER...".

The change is straightforward and maintains consistency between the OpenSSL and libcurl backends. This appears to be a final polish patch in the larger `imap-send` feature series (position 9/10), following through on the thread's goal of improving IMAP functionality and user experience. The implementation is clean with minimal changes (4 insertions, 2 deletions) focused solely on the progress message formatting.