Refactoring patch moving `core.sparsecheckout` configuration into `repo_settings` as part of the `the_repository` removal effort.

This patch implements the getter/setter approach discussed in the thread, moving the `core.sparsecheckout` configuration from the global `core_apply_sparse_checkout` variable into the `repo_settings` struct. The change affects 15 files across various commands that interact with sparse-checkout functionality, including `clone`, `mv`, and `worktree`.

The implementation introduces `repo_settings_get_sparse_checkout()` and `repo_settings_set_sparse_checkout()` functions that handle the `prepare_repo_settings()` initialization internally, addressing the thread's consensus about encapsulation. The patch also removes the now-unnecessary `USE_THE_REPOSITORY_VARIABLE` definition from `builtin/backfill.c`.

This appears to be a well-executed mechanical conversion that maintains the existing sparse-checkout behavior while advancing the broader goal of eliminating repository globals. The patch follows established patterns seen in prior `repo_settings` migrations and includes updates to all relevant call sites. The changes are straightforward replacements of direct variable access with the new getter/setter functions, with no behavioral changes to the sparse-checkout functionality itself.

The patch represents a concrete step forward in the thread's discussion, implementing the technical direction that had been converging toward in previous messages. It should be ready for review pending any feedback on the final getter/setter approach.