Documentation patch completing the `git-log` man page conversion series. This final patch standardizes the `log.*` configuration options documentation in `config/log.adoc` to match the new AsciiDoc format style.

The changes follow the established patterns from previous patches in the series, applying consistent formatting to configuration options and their descriptions. Key changes include:
- All configuration keys now wrapped in backticks (e.g. `` `log.decorate` ``)
- Complex option values formatted as description lists rather than inline paragraphs
- Command references standardized with backticks (`` `git log` ``)
- Preserved all existing content while improving presentation

Notable technical details:
- Handles 13 different `log.*` configuration options
- Converts the `log.decorate` option's values (`short`, `full`, `auto`) into a properly formatted description block
- Maintains all existing functionality descriptions while improving readability
- Completes the 9-part series that standardized the entire `git-log` documentation ecosystem

This patch follows the same high-quality mechanical conversion approach seen throughout the series, demonstrating Jean-Noël Avila's consistent methodology for documentation standardization. With this final patch, the comprehensive update to `git-log` documentation is complete, bringing it in line with Git's modern documentation standards.