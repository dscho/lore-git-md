Bugfix for `git stash` branch name corruption in submodule contexts. This is the revised version of the patch addressing all review feedback from the previous round.

K Jayatheerth's updated patch fixes the issue where `git stash list` would incorrectly show a submodule's branch name instead of the superproject's branch in stash entries. The core solution remains using `xstrdup()` to preserve the superproject branch name through submodule operations, but now includes all requested improvements:

1. Added proper memory management with `free(branch_name_buf)` to prevent leaks
2. Fixed indentation in the control flow around branch name handling
3. Included a comprehensive test case verifying superproject branch name preservation
4. Maintained the existing "(no branch)" fallback behavior

The test creates a superproject with a submodule, switches branches in both, then verifies the stash entry correctly shows the superproject's branch name. The implementation now properly handles the branch name resolution through a dedicated buffer while cleaning up allocations.

This appears ready for inclusion, having addressed all technical concerns raised during review (memory management, code style, test coverage) while maintaining the original solution's effectiveness. The patch cleanly applies the established pattern of using heap allocation to preserve refnames across operations that might reuse static buffers.