This is the cover letter for version 8 of a 2-patch series addressing submodule configuration management issues. The series has evolved through technical discussion, with patch 1/2 (preventing .gitmodules overwrites) remaining unchanged from previous versions and already approved for merging. Patch 2/2 (skipping redundant active entries) has undergone significant logic changes in response to feedback, now implementing a more focused approach within `module_add()` rather than using the suggested `is_submodule_active()` helper.

The author notes the current implementation wraps the logic tightly around the core needs of submodule addition while minimizing changes to broader submodule infrastructure. Test coverage appears thorough, including both existing tests (t7413 test 9) and new ones, with CI passing except for an already-known osx-gcc compiler error. The implementation maintains backward compatibility while addressing the original goals of ensuring new submodules are active without creating redundant config.

Key technical points for future reference: The solution handles edge cases where `is_submodule_active()` would incorrectly default to active state when global config is absent. The changes affect `builtin/submodule--helper.c` with test additions in t7400 and t7413. The remaining open question is whether maintainers will accept this focused implementation approach or request alignment with the previously suggested `is_submodule_active()` helper method.