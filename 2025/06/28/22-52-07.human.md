This is a documentation patch that improves the wording for `git apply --intent-to-add` as part of a larger bugfix series. 

The patch clarifies the documentation by removing a double negative ("ignored unless...not specified") and making it clearer that `--intent-to-add` is incompatible with both `--index` and `--cached` modes. The new wording is more direct while maintaining all the same technical information about when the option is active.

This appears to be a straightforward documentation improvement that accompanies the substantive fixes to `--intent-to-add` behavior in the earlier patches of the series. The change is small but makes the documentation more readable without altering the meaning. Since it's part of a series that has already addressed the core technical issues with the feature, this patch serves as a final polish to the user-facing documentation.