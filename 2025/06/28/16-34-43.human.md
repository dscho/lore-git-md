This is a test modernization patch, part of a larger series adding diff context configuration support for interactive patch commands. 

The patch converts test scripts to use `test_grep` instead of plain `grep` in test assertions. This provides better debugging output when tests fail by showing both the expected and actual output. The changes are mechanical replacements across two test files (t/t3701-add-interactive.sh and t/t4055-diff-context.sh), updating 38 test assertions in total.

This is a straightforward infrastructure improvement that follows Git's testing best practices. The changes don't affect test behavior - they just make failures more debuggable. The patch is cleanly focused on this single purpose as the first step in a larger series, with no functional changes to the code being tested.

The thread context shows this is part of v3 of a series that has undergone significant review and refinement, with this being one of the final preparatory patches before adding the main functionality. The use of `test_grep` here will help debug any issues in the subsequent patches that implement the actual diff context configuration features.