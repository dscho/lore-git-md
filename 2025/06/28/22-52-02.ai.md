Cover letter for a bugfix series targeting `git apply --intent-to-add`. This is a multi-patch effort to correct long-standing incorrect behavior in the `--intent-to-add` (`-N`) flag for `git apply`, which has been broken since its introduction in Git 2.19. The current implementation creates an empty index except for new files in the patch, making it largely useless except on blank repositories.

The series starts with a safety check preventing use outside a Git directory, then fixes the core issue by properly reading the index in intent-to-add mode. It ensures only new files get intent-to-add markers, adds comprehensive tests for various interaction cases, and clarifies the documentation. The author references two previous discussion threads about this issue, showing this is a known problem that's been discussed before.

Key technical details: The fixes touch `apply.c`'s index handling logic, add a new test file `t4140-apply-ita.sh` with 31 lines of new test coverage, and update documentation in `git-apply.adoc`. The changes are focused and well-scoped, with each patch addressing a specific aspect of the problem. The test coverage appears thorough, checking interactions with various Git states. This looks like a well-motivated series addressing a real usability issue that's been broken for years.