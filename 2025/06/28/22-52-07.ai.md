Documentation patch completing a series fixing `git apply --intent-to-add` behavior. This final patch clarifies the documentation for the `--intent-to-add` option in response to the technical fixes made in earlier patches of the series.

The changes simplify the wording by removing a double negative ("not specified") and making the conditions more direct ("ignored if"). It also properly distinguishes between `--index` and `--cached` modes, which was a point of confusion in the original documentation. The update aligns with the implementation changes from patches 2-4 that fixed how `--intent-to-add` interacts with index operations.

Key technical details carried forward from the thread context:
- Maintains consistency with the fixed behavior where `--intent-to-add` only affects working tree operations
- Reflects the distinction between `--index` (which updates both index and working tree) and `--cached` (which only checks/updates index)
- Preserves the note about `--3way` potentially implying `--index` behavior

The patch appears straightforward and uncontroversial, completing the documentation side of the series that already addressed the core implementation issues. It follows the same pattern of small, focused changes seen in the rest of the series.