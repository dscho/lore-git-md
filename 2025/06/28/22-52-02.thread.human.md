This thread documents the effort to restore correct behavior in `git apply --intent-to-add` (the `-N` flag), which had been broken since Git 2.19. The series, now at v2 with 4 patches, fixes both index reading and writing behaviors while adding comprehensive test coverage and documentation updates.

Raymond Pasco's patches address the core issue where `--intent-to-add` failed to properly handle the index except in empty repositories. The technical solution involves carefully scoped conditions in `apply.c` to distinguish between normal index operations and ITA-specific cases. Junio Hamano reviewed the architectural approach and the series has addressed his concerns, particularly around error handling in non-repository contexts (now silently ignored).

The final patch, just arrived, updates the documentation in `git-apply.adoc` to clarify the interaction between `--intent-to-add` and other flags like `--index` and `--cached`. The wording changes align with the implementation's actual behavior and remove confusing double negatives. With this documentation update, the series appears complete and ready for Junio to consider merging. The fixes restore the intended functionality where `--intent-to-add` properly preserves the intent-to-add state when applying patches, particularly for new files.