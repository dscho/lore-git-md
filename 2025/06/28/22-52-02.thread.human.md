This thread addresses a bugfix series for `git apply --intent-to-add`, restoring functionality that had been broken since Git 2.19. The core issue was that the `- N` flag wasn't properly handling index operations, making the feature work only in empty repositories. Raymond Pasco's v3 series provides a comprehensive fix across four patches: correcting index reading and writing behaviors, expanding test coverage, and updating documentation.

The technical changes are well-considered, with patch 1 fixing index reading and patch 2 precisely specifying when intent-to-add entries should be written. Patch 3 adds thorough test coverage, including mixed modification scenarios, while patch 4 (the subject of the latest email) completes the series with clear documentation updates. The docs now properly explain that `--intent-to-add` is ignored when either `--index` or `--cached` are used, removing a confusing double negative in the process.

Junio Hamano's earlier architectural concerns have been fully addressed, and the series appears technically complete with consensus on implementation decisions. The only remaining open question is whether to backport the fixes, though this would require creating backport-specific tests since the original test infrastructure didn't exist in 2018. The series is now ready for Junio to pick up for integration.