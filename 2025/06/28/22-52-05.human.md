This is the third patch in a bugfix series addressing `git apply --intent-to-add` behavior. The patch ensures intent-to-add markers are only written for new files when in worktree-only mode, fixing a case where the index could be modified incorrectly.

The change modifies the condition in `create_file()` to properly distinguish between three cases: when we're checking/updating the index normally, when we're in worktree-only mode with intent-to-add for new files, and when we shouldn't touch the index at all. The commit message explains the logic clearly - we can't just rely on `update_index` because intent-to-add sets that flag even in worktree-only mode.

The one-line change in `apply.c` is simple but important, preventing unintended index modifications while preserving the intended intent-to-add functionality. This builds on the previous patches' fixes to index handling, continuing the series' goal of making `--intent-to-add` work as originally designed. The patch appears well-reasoned and maintains consistency with Git's existing apply behavior patterns.