This is a user question about `git log` behavior regarding bisect refs. Brendan McDonnell reports that `git log --all --graph --decorate` used to show bisect references (like `refs/bisect/bad`) in its output a few years ago, but no longer does. He's looking for a way to restore this behavior without losing other refs that are currently shown.

The current workaround of using `--decorate-refs=refs/*` includes bisect refs but excludes some other references that would normally appear. McDonnell is asking whether this change was intentional (and if so, why and where it's documented) or if it's a bug that should be reported.

This appears to be a genuine behavior change that affects users who rely on visualizing bisect state through `git log` output. The question is well-framed, showing the exact command in use and demonstrating an understanding of the current workaround's limitations. It's likely to prompt discussion about whether this was an intentional regression and what the best solution would be - either documenting the change and recommended alternatives, or restoring the old behavior if it was unintentional.