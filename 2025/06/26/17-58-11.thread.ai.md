Thread summary:

This thread begins with a user question about `git log` decoration behavior regarding bisect refs. The core issue is that `git log --all --graph --decorate` no longer shows bisect references (`refs/bisect/`) in its output, though it reportedly did so a few years ago. The user found that adding `--decorate-refs=refs/*` includes bisect refs but excludes other references that were previously shown. They seek clarification on whether this is an intentional change (and if documented) or a bug to report.

Key technical details:
- Command in question: `git log --all --graph --decorate --pretty=oneline --abbrev-commit`
- Bisect refs live under `refs/bisect/` namespace
- Workaround `--decorate-refs=refs/*` has unwanted side effects
- Behavior change appears to have occurred around 2 years ago

The question touches on Git's reference filtering behavior in log decoration, specifically around special ref namespaces. This relates to how `--decorate` handles ref filtering and default exclusions. The user wants to understand if the change was intentional (and where documented) or if it represents a bug that should restore the previous behavior where bisect refs were included by default. No replies or resolutions have been posted yet.