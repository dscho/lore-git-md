This is a technical discussion about Windows signal handling compatibility in the context of the ongoing git-daemon signal handling fixes.

Carlo Marcelo Arenas Belon and Phillip Wood are debating how Git's Windows compatibility layer should implement `sigaction()` for SIGCHLD. The core disagreement centers on whether the wrapper should precisely mimic Windows' `signal()` behavior (which doesn't set errno for SIGCHLD) or follow standard `sigaction()` semantics (which would set errno=EINVAL for unsupported signals).

Carlo initially implemented the wrapper to match Windows' `signal()` behavior to avoid regressions, but Phillip argues the wrapper should behave like proper `sigaction()`. Carlo's testing reveals the errno behavior depends on Windows' internal numbering of SIGCHLD, suggesting deeper platform-specific quirks may need addressing. The discussion remains unresolved, with Carlo suggesting Windows' signal emulation might need changes to properly support standard `sigaction()` behavior.

This is a nuanced platform-specific detail in the larger signal handling rework, where the developers are carefully weighing backward compatibility against proper POSIX semantics. The exchange shows both technical rigor (with actual testing of errno behavior) and caution about introducing subtle behavioral changes.