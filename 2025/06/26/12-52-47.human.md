This is a follow-up discussion in the Git daemon signal handling bugfix series, specifically about Windows compatibility for SIGCHLD handling. Phillip Wood questions why the patch returns -1 for SIGCHLD in the Windows compatibility layer when the stated goal is to match signal()'s behavior (which returns SIG_ERR). The patch in question adds Windows support for sigaction(SIGCHLD) as part of standardizing signal handling across platforms.

The discussion is quite focused - it's a single technical question about implementation details in the Windows compatibility code. The broader context (from the thread summary) shows this is part of a larger effort to fix platform-specific signal handling in the Git daemon, with Windows being one of several platforms needing special attention. The question appears to be a minor consistency check rather than a fundamental objection to the approach.