This is a brief suggestion in an ongoing thread about `git pull --prune` failing due to lock file conflicts. The thread appears to be discussing a bug where pruning remote references fails when lock files already exist. 

The current email from ryenus proposes a simple solution: prefixing each lock file with a unique sequence number to avoid conflicts. This would be a straightforward way to prevent the locking issues, though the suggestion doesn't include implementation details or address whether this might have any side effects (e.g., cleanup of stale lock files). 

Since this is a reply in an existing thread, the full context would require seeing prior messages to understand the exact nature of the lock file problem and whether other solutions have been proposed or rejected. The suggestion here is minimal and doesn't include code or detailed analysis, so its viability would depend on deeper technical discussion in the thread.