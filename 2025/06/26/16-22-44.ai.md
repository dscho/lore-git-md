This is a discussion about editor configuration settings in the context of the ongoing clang-format and code style enforcement series. Justin Tobler responds to Karthik Nayak's patch proposing a 120-character line length limit in .editorconfig, raising concerns about editor behavior.

The email highlights a practical workflow impact: Justin notes that vim's format operator uses .editorconfig's `max_line_length` to determine wrapping, and setting it to 120 would cause his editor to produce longer lines than he prefers. He suggests either keeping the limit at 80 characters (matching Git's documented guidelines) or leaving it unset entirely, since .editorconfig values are editor hints rather than CI-enforced rules.

This adds a new dimension to the thread's line length discussion, moving beyond the philosophical debate about hard vs soft limits to consider how the setting affects real editor behavior. The concern is specific to vim's integration with .editorconfig, suggesting the change might have unintended consequences for developers using certain editor configurations. The email doesn't propose an alternative solution but raises a flag about the practical implications of Karthik's proposed change.

Key technical details for future reference: the debate centers on `.editorconfig`'s `max_line_length` property (currently proposed as 120), its interaction with vim's format operator, and how this relates to Git's documented 80-character guideline. The thread's broader context includes parallel changes to `.clang-format` configuration and CI enforcement mechanisms.