This is a response in the thread about `git log` decoration behavior regarding bisect refs. Jeff King identifies the specific commit (92156291ca) that changed the default decoration filtering behavior, which explains why bisect refs are no longer shown by default. He points to a previous discussion thread that considered adding bisect refs to the default set in a more intentional way, though this wasn't implemented in the original change.

The response provides a clear workaround - setting `log.initialDecorationSet` to "all" in the Git config will restore the pre-2022 behavior where bisect refs were included. This suggests the current behavior is intentional (though perhaps not optimally documented) rather than a bug. The commit in question was part of a broader effort to make reference decoration filtering more configurable, with bisect refs being unintentionally excluded from the new default set.

Key technical details for future reference:
- The behavior change originated in commit 92156291ca ("log: add default decoration filter")
- Workaround: `git config log.initialDecorationSet all`
- Related discussion in linked sub-thread about potentially adding bisect refs to default set
- No immediate plans to change the default behavior, but historical context provided

The response moves the thread forward by identifying the root cause and providing a solution, while acknowledging the original change's context. This appears to be a case where a well-intentioned refactoring had unintended consequences for some use cases.