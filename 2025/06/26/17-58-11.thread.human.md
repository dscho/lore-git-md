This thread discusses a user's observation that `git log --all --graph --decorate` no longer shows bisect references in its output, when it reportedly did so in the past. The user has found that adding `--decorate-refs=refs/*` includes bisect refs but excludes other references that were previously visible. They're seeking clarification on whether this is an intentional change (and if documented) or a bug. The behavior change appears to have occurred around two years ago, affecting the display of refs under `refs/bisect/` namespace. The core question revolves around Git's reference filtering behavior in log decoration and whether the previous behavior should be restored.