This is a user question about `git log` behavior regarding bisect refs, falling into the categories of documentation and possibly bugfix if the behavior is unintended.

The email asks why `git log --all --graph --decorate` no longer shows bisect references in its output, when it reportedly did so a few years ago. The user has found that adding `--decorate-refs=refs/*` includes bisect refs but at the cost of excluding other references that were previously shown. They're seeking clarification on whether this is an intentional change (and if so, where documented) or a bug that should be reported.

Key technical details for future reference:
- The command in question is `git log --all --graph --decorate --pretty=oneline --abbrev-commit`
- The bisect refs live under `refs/bisect/` namespace
- The workaround `--decorate-refs=refs/*` has unwanted side effects
- The behavior change appears to have occurred around 2 years ago

The question touches on Git's reference filtering behavior in log decoration, specifically around special ref namespaces. This could relate to changes in how `--decorate` handles ref filtering or default ref exclusions. The user is essentially asking for the restoration of previous behavior where bisect refs were included by default in the decorated output.