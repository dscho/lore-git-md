Platform compatibility patch in the Git daemon signal handling series, specifically addressing Windows (mingw) support for SIGCHLD handling.

Phillip Wood questions why the patch returns -1 for SIGCHLD in mingw's sigaction() implementation rather than SIG_ERR, since the commit message states the goal is to match signal()'s behavior which returns SIG_ERR. This is a code review point on patch v3/4 in the series, which adds Windows support for the new SIGCHLD handler approach using sigaction().

The patch itself (from Carlo Marcelo Arenas Belón) adds SA_NOCLDSTOP definition to mingw-posix.h and modifies mingw.c's sigaction() to handle SIGCHLD by returning -1 while maintaining existing SIGALRM support. The change enables future use of sigaction() for SIGCHLD handlers on Windows, though Phillip notes the inconsistency between the stated intent (matching signal()'s SIG_ERR) and the implementation (-1 return).

This email doesn't introduce new code - it's a review comment on an implementation detail of the Windows compatibility portion of the broader signal handling solution. The thread context shows this is part of the finalized SA_RESTART toggling approach for the Git daemon's signal handling, with Windows support being one of the platform-specific considerations.