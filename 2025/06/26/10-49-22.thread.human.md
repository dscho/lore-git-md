Here's the updated human-readable thread summary:

This thread investigates a bug where interactive rebase (`git rebase -i`) fails in secondary worktrees with false "local changes would be overwritten" errors, while non-interactive rebase works fine. The issue affects Git 2.49.0 on Darwin/arm64 systems and appears specific to worktrees created via `git worktree add`.

Denilson Sá Maia originally reported the issue and has now provided more detailed reproduction steps showing it occurs when rebasing a secondary branch against main with unrelated changes - the error incorrectly lists files from the target branch as locally modified. Phillip Wood has been investigating potential technical causes, including line ending/filter attributes or missing index refreshes during the rebase process.

The new details confirm the problem is consistently triggered in interactive mode when the secondary branch has changes to one file while main has unrelated changes elsewhere. The worktrees are on the same local filesystem with minimal configuration, ruling out cloud sync or most hook interference. The core issue appears to be how interactive rebase handles target branch checkouts in secondary worktrees, misidentifying incoming changes as local modifications.

While the exact root cause remains unclear, the thread has narrowed the conditions to: (1) interactive rebase only, (2) secondary worktrees, (3) specific file change patterns between branches. Phillip Wood continues to investigate whether this stems from attribute handling, index refresh timing, or other rebase machinery issues. No workarounds or fixes have been proposed yet, but the more precise reproduction steps should aid diagnosis.