This is a technical discussion about signal handling in the Git daemon, specifically around the `USE_NON_POSIX_SIGNAL` build flag. Junio C Hamano points out that the current documentation for this flag is insufficient - it doesn't explain how users should decide between using ANSI C `signal()` vs POSIX `sigaction()` when both are available. 

The key issue is that this isn't just a preference choice - some platforms may have a broken `sigaction()` implementation that requires falling back to `signal()`. Junio suggests the documentation should clarify this distinction and hints this might require renaming the flag to better reflect its purpose.

This email doesn't introduce new technical solutions but rather focuses on improving the clarity and accuracy of the build system's documentation. It references prior discussion in the cover letter response about the flag naming, indicating this is part of an ongoing refinement of the SA_RESTART approach in the daemon signal handling series.

The comment highlights the careful attention to detail needed in cross-platform compatibility code, where seemingly simple build flags can have subtle platform-specific implications. While not advancing the core architectural decision between SA_RESTART and self-pipe approaches, it ensures the current implementation's documentation properly reflects the technical realities.