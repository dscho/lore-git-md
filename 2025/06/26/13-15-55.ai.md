This is a technical discussion about Windows signal handling consistency in the ongoing git-daemon signal handling series. Carlo Marcelo Arenas Belón responds to Phillip Wood's query about why the Windows `sigaction()` implementation returns `-1` rather than `SIG_ERR` for error cases. The exchange focuses on error reporting behavior alignment between `signal()` and `sigaction()` calls.

The discussion reveals that while POSIX requires `EINVAL` for certain error cases, Git's Windows compatibility layer currently doesn't set `errno` consistently. Carlo notes that both `SIG_ERR` (from `signal()`) and `-1` (from `sigaction()`) would be equivalent in practice since the return values are ignored in Git's codebase. He points out the technical irony that while `SIG_ERR` is a function pointer type, casting it to an integer would produce the same `-1` value being returned.

This email represents a micro-decision point within the larger architectural debate about signal handling approaches. While not introducing new patches, it clarifies implementation details that could affect both the current SA_RESTART toggling approach and Phillip Wood's proposed minimal solution. The discussion remains technical and collegial, focused on achieving consistent behavior regardless of which signal handling strategy is ultimately adopted.