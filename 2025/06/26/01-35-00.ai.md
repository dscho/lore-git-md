This is a technical clarification in the ongoing Git daemon signal handling discussion, specifically addressing build system detection of SA_RESTART support. Carlo Marcelo Arenas Belón acknowledges an issue with the current implementation's preprocessor check for SA_RESTART in compat/posix.h, where an empty `#ifdef` block was incorrectly assumed to serve as a valid build-time check for POSIX compliance. Junio C Hamano pointed out that this check doesn't actually affect compilation, with the compilation failure instead coming from `siginterrupt()` deprecation warnings.

The email confirms the need to correct this detection mechanism, which is part of the broader signal handling infrastructure being discussed for fixing zombie process cleanup on OpenBSD and crashes on AIX. This ties into the architectural decision between continuing with the SA_RESTART toggling approach (which requires proper platform detection) versus switching to a self-pipe implementation that would avoid these platform-specific issues entirely.

Key technical points:
- The current SA_RESTART detection method is flawed and needs correction
- This affects platforms lacking SA_RESTART (QNX, NonStop, Windows)
- The issue was revealed through Junio's analysis of the preprocessor behavior
- The fix will need to properly validate platform capabilities for the SA_RESTART approach to remain viable

This email doesn't advance the architectural decision but highlights another technical debt item in the SA_RESTART approach that would need addressing if that path is chosen. The self-pipe alternative, previously validated by Junio, would avoid these platform detection complexities entirely.