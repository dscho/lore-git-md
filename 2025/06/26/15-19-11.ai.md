Phillip Wood follows up on Windows signal handling implementation details in the ongoing git-daemon signal handling discussion. This email focuses specifically on the Windows compatibility layer's `sigaction()` wrapper behavior for `SIGCHLD`.

The key technical points:
- Clarifies that while the current implementation returns `-1` (vs `SIG_ERR` from `signal()`), the more important behavior is maintaining consistent error reporting
- Notes the Windows `signal()` implementation doesn't set `errno` for invalid signals despite documentation claiming it should
- Questions whether the wrapper should match `signal()` behavior or proper `sigaction()` semantics

This continues the thread's refinement of Windows-specific signal handling details, now focusing on error reporting consistency rather than the core SA_RESTART debate. The exchange shows alignment on setting `errno = EINVAL` for unsupported signals to match existing patterns, but leaves open whether the wrapper should precisely mimic Windows' `signal()` quirks or implement standard `sigaction()` behavior.

The discussion remains technical and collaborative, with both parties working toward a solution that maintains consistency while properly documenting platform limitations. This represents a minor but important clarification in the broader signal handling architecture discussion.