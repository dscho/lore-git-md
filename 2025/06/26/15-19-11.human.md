This is a technical discussion about Windows signal handling compatibility in the context of the ongoing git-daemon signal handling fixes.

Phillip Wood and Carlo Marcelo Arenas Belón are working through the details of how Windows should handle `sigaction(SIGCHLD)` in Git's compatibility layer. The discussion centers on whether the Windows implementation should match the behavior of `signal()` (which doesn't set errno) or follow POSIX conventions (which would set errno = EINVAL). 

Phillip argues for consistency with POSIX behavior, noting that while current callers ignore the return value, future code might need to check errno. Carlo initially implemented matching `signal()`'s behavior but agrees with Phillip's suggestion after verifying Windows behavior. The exchange shows careful attention to cross-platform consistency and future maintainability, though the technical details are quite Windows-specific.

This is part of the larger git-daemon signal handling series, where Windows compatibility is one of several platform-specific considerations being worked out. The discussion appears productive, with both contributors converging on a solution that prioritizes long-term code health over short-term matching of existing behavior.