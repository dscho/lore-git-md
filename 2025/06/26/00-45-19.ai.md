This is a technical clarification in the ongoing Git daemon signal handling discussion, specifically addressing the `USE_NON_POSIX_SIGNAL` build configuration. Junio C Hamano points out that the current documentation for this flag is insufficient - it mentions a "preference" between ANSI C `signal()` and POSIX `sigaction()` without explaining how users should make that choice. 

The key issue is that this isn't simply a matter of preference between equally valid options, but rather a workaround for platforms where `sigaction()` is available but behaves incorrectly in specific ways (particularly around `SA_RESTART` behavior). Junio suggests the flag's name and documentation should better reflect this reality, as systems with both functions may still need to use `signal()` due to platform-specific quirks rather than arbitrary preference.

This email doesn't introduce new technical solutions but reinforces the need for clearer documentation and naming around the signal handling infrastructure, particularly as the thread weighs the tradeoffs between the SA_RESTART toggling approach and the self-pipe alternative. The comment suggests revisiting earlier discussion in the cover letter about the flag's semantics, indicating this naming/documentation issue was previously raised but perhaps not fully addressed in the implementation.