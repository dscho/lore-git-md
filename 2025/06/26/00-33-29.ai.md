This is a technical clarification in the ongoing Git daemon signal handling discussion, specifically about build-time detection of SA_RESTART support. Junio C Hamano questions the purpose of an empty `#ifdef SA_RESTART` block in the autoconf test, noting that it shouldn't affect compilation since it contains no code. Carlo Marcelo Arenas Belón had previously indicated this was meant to detect platforms lacking SA_RESTART (QNX, NonStop, Windows), but Junio points out the compilation failure would come from `siginterrupt()` issues rather than the empty preprocessor check.

The email highlights an implementation detail in the current SA_RESTART approach, where the build system check may not be working as intended. Junio demonstrates with a concrete example showing the empty preprocessor directive doesn't influence compilation on a Debian system, where the failure comes from `siginterrupt()` deprecation warnings instead. This doesn't directly advance the architectural decision between SA_RESTART and self-pipe approaches, but tightens the technical understanding of the current implementation's platform detection mechanism. The exchange serves as quality control for the build system code, ensuring the SA_RESTART detection is properly validated before committing to that path.