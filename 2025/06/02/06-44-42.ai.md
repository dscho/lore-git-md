Test infrastructure patch in Patrick Steinhardt's Meson TAP parsing series (v4 02/10). This patch addresses TAP parser confusion by silencing repository initialization output in test scripts, moving `test_create_repo()` calls into proper test cases and converting them to direct `git init --quiet` calls where appropriate.

The changes focus on four test scripts (t1007, t4041, t4060, t7401) that were using the deprecated `test_create_repo()` helper outside test cases, which could emit initialization messages that break TAP output parsing. The patch wraps these in proper test cases except for t1007's `push_repo()` helper, which gets converted to use `git init --quiet` directly due to its repeated usage pattern.

Key technical details for future reference:
- Replaces `test_create_repo()` with `git init --quiet` in t1007's `push_repo()`
- Wraps submodule setup in explicit test cases in t4041, t4060, and t7401
- Maintains test behavior while ensuring TAP compliance
- Builds on commit f0d4d398e28 which deprecated `test_create_repo()`
- This is part of the series' broader effort to make test output properly parseable by Meson's TAP parser (requiring v1.8+)
- Touches test scripts dealing with hash-object, diff-submodule, and submodule-summary functionality

The patch appears well-scoped and follows the series' established pattern of TAP output cleanup, with appropriate attention to minimizing test output changes while maintaining correctness. It addresses a specific technical requirement (TAP parser compatibility) in a focused way while modernizing test code usage patterns.