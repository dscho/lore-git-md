This is a small but useful user experience improvement in the ongoing `imap-send` feature/bugfix series. The patch modifies the credential prompt to include the port number when requesting passwords, making it consistent with `git send-email`'s behavior.

The change is straightforward - it updates the credential helper's host field to include the port number in the format "host:port". The implementation handles both explicit ports and default ports (993 for IMAPS, 143 for IMAP), though the commit notes that the default case is always covered by the existing code paths.

This patch represents final polish in the v12 series, aligning with the thread's broader goal of improving `imap-send`'s usability and consistency. The change is minimal (just one line modified) but meaningful for users who work with multiple IMAP servers on different ports, as it provides clearer context about which credentials are being requested. The patch follows the series' pattern of matching `git send-email` behavior where appropriate.