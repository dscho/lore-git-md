Feature and bugfix series for `imap-send` now in v12, addressing remaining authentication verification issues while maintaining all previously introduced functionality (OAuth2.0 support, folder management, configuration fixes). This iteration focuses on final polish and graceful handling of authentication method availability.

Key changes in v12:
1. Properly verifies OpenSSL support before attempting PLAIN, CRAM-MD5, OAUTHBEARER and XOAUTH2 authentication methods, preventing crashes when methods are requested without required library support. Each method now has compile-time guards and clear error messages when unavailable.

2. Documentation formatting improvements including:
   - Consistent backtick usage for literals (e.g., `LOGIN` command)
   - Wrapped documentation to 75 columns
   - Clarified folder specification requirements (config vs command-line)

3. Code style fixes:
   - Added trailing comma to last enum CAPABILITY member
   - Standardized error message formatting
   - Removed redundant OpenSSL support checks in favor of compile-time definitions

The series maintains all v11 functionality while addressing Junio Hamano's remaining concerns about authentication method verification. The implementation spans configuration parsing, authentication handlers, and IMAP protocol handling in `imap-send.c`, with changes to both OpenSSL and libcurl backends. Documentation updates cover OAuth2.0 setup and folder management.

Technical details:
- New compile-time guards via `#ifdef NO_OPENSSL` blocks for each authentication method
- Error messages standardized to lowercase starts without periods
- Enum CAPABILITY` now properly formatted with trailing comma
- Documentation text consistently wrapped to 75 columns
- Backticks used for all literals in docs (commands, methods, fields)

This appears ready for final review, having addressed all substantive technical feedback from the v11 discussion. The changes are focused and non-controversial, primarily adding missing safety checks and documentation polish.