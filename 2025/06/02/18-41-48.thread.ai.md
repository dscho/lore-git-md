Thread identity: Portability fix for OpenBSD's sed implementation affecting config-list.h generation.

Core problem or goal: Fix build failure on OpenBSD where sed` doesn't support \n` escape sequences, which broke during documentation processing for completion options (introduced in e1b81f54da).

Current status: Single patch proposed to work around the issue by using a shell variable with actual newline instead of \n` escape in generate-configlist.sh`.

Key technical details: Changes limited to generate-configlist.sh`, adding $nl` variable with literal newline and modifying sed pattern. Maintains same output format while being portable to OpenBSD.

Open questions: Whether other BSD variants share this sed limitation (not addressed in current patch).

Participant positions: Single author submission with no reviews yet. The fix appears correct for the stated problem.

Related work: Build regression introduced by e1b81f54da (March 2025) which added backtick handling for completion options.