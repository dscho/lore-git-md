This is part 15/17 of Patrick Steinhardt's series to refactor Git's object database (ODB) subsystem, continuing the systematic conversion of ODB-related functions to remove `the_repository` global state. The patch renames `has_object()` to `odb_has_object()` to match the established `odb_` prefix convention for ODB functions.

The change follows the same mechanical pattern seen in previous patches: adding an explicit `object_database` parameter (here via `the_repository->objects`), renaming with `odb_` prefix, and updating all 30 call sites across builtins, protocols and test helpers. A compatibility wrapper maintains backward compatibility for in-flight topics. The diff shows the widespread use of this function for object existence checks in operations like fetch, push, fsck, and object streaming.

This continues the series' progression toward enabling pluggable ODB backends by eliminating another `the_repository` dependency while maintaining the same behavior through careful mechanical conversion. The patch is straightforward with no functional changes, following the established pattern that has already received positive reviews in earlier parts of the series.