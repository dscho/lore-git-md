This is a portability fix that takes the documentation restructuring approach discussed in the thread, moving away from the sed-based workaround. The patch modifies config.adoc files to list related configuration variables on separate lines rather than comma-separated, eliminating the need for sed line continuation that caused issues on OpenBSD and dash.

The changes split two pairs of related config variables in gitcvs.adoc and http.adoc (gitcvs.dbUser/dbPass and http.lowSpeedLimit/lowSpeedTime) onto separate lines. This allows removing the problematic sed command that inserted newlines between comma-separated variables in generate-configlist.sh. The approach follows the precedent set by pretty-options.adoc's formatting style.

This v3 patch represents a shift from the earlier sed-based solutions (v1-v2) to the documentation restructuring approach that Jacob Keller and others supported. The solution is cleaner than the sed workaround as it produces output without "$nl" markers while maintaining readability. The change is minimal, touching only the specific variable pairs that needed line continuation in the sed script.

The patch appears well-executed with a clear commit message explaining the OpenBSD build failure it addresses. The changes are focused and maintain documentation consistency while solving the portability issue. This seems ready for maintainer consideration as it resolves the thread's core issue through the now-preferred documentation approach.