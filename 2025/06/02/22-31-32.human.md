This is a portability fix that takes the documentation restructuring approach discussed in the thread, addressing build issues on OpenBSD and dash shell. Collin Funk's v3 patch modifies the config documentation to list related configuration variables on separate lines rather than comma-separated, eliminating the need for problematic sed newline handling in generate-configlist.sh.

The patch changes two documentation files (gitcvs.adoc and http.adoc) to split previously comma-separated config variable declarations, and removes the sed command that was causing portability issues. This follows Jacob Keller's preference for the precedent set by pretty-options.adoc, providing a cleaner solution than the earlier sed workaround that left "$nl" markers in the output.

The solution is straightforward and maintains consistency with Git's documentation style while solving the immediate build problem. The patch appears well-considered, having evolved through discussion of alternative approaches in the thread. It's a good example of choosing documentation improvements over build system workarounds when both options are viable.