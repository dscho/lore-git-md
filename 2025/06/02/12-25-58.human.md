This is the final patch in a test infrastructure series converting reftable tests to the Clar framework. The series has been working through converting all reftable test components from a custom test framework to Clar, following the pattern of other Git test modernizations.

The patch cleans up the conversion by removing the now-unused original test helper files (`lib-reftable-clar.{c,h}`) and renaming the Clar-specific versions back to the original `lib-reftable.{c,h}` names. It also updates the build system (Makefile, meson.build) to remove references to the old test framework and properly link the new test binaries. The changes are mechanical but thorough, touching all build configurations and test files that included the helpers.

This completes the reftable test migration effort that has been progressing through the series. The patch appears well-executed, with careful attention to build system details and a clear commit message explaining the renaming rationale. As the capstone to the series, it provides the final cleanup needed to fully adopt Clar for reftable testing.