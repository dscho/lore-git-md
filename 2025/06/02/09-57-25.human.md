This is a test cleanup patch in a series addressing batched reference updates. It's the second of three patches in the series, following a segfault fix in the files backend.

The patch makes a straightforward improvement to test scripts in `t/t5516-fetch-push.sh` by changing single quotes to double quotes in test descriptions that contain variables. This ensures variable expansion works correctly in the output when tests fail, making debugging easier. The change affects three test cases that deal with remote push ref mappings.

While small, this is a sensible cleanup that follows Git's test conventions. The changes are mechanical and clearly correct, though they don't directly relate to the batched ref updates being fixed in the rest of the patches. The patch maintains the existing test behavior while improving debuggability.