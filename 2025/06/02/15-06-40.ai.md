This is a follow-up discussion about the first patch in the bugfix series addressing batched reference updates. The conversation focuses on test coverage for an edge case in the files backend where a deletion request with an expected old OID fails when the reference doesn't exist.

Junio Hamano clarifies that Karthik Nayak's earlier comment about missing test coverage was likely intended as a rhetorical suggestion to add tests, not just an observation. The specific edge case involves batched reference deletions where the transaction expects a particular old OID but the reference already doesn't exist - a scenario not currently covered by the test suite.

The discussion occurs in the context of Patrick Steinhardt's v1 series that already includes test coverage in `t/t1400-update-ref.sh` for the segfault fix, but appears to be identifying another test gap. This relates to the files backend's handling of failed updates during batched transactions, which differs from the reftable backend's more robust approach.

Key technical detail mentioned is the specific failure mode: a deletion request with an expected old OID when the reference is already non-existent. The thread context shows this is part of broader production-proven fixes from GitLab's experience with batched reference updates.