This is a test infrastructure patch in Patrick Steinhardt's Meson test integration series (v4 05/10). It addresses TAP output compliance by redirecting shell trace output to stderr rather than stdout.

The patch fixes an issue where verbose test output (from flags like `-x` or `--verbose`) was being sent to stdout, which breaks TAP parser compatibility. The solution redirects all test stdout to stderr when in verbose mode, sacrificing the ability to distinguish between stdout and stderr streams in favor of maintaining valid TAP output format. The commit notes this tradeoff is acceptable as the distinction hasn't proven useful in practice.

Key technical changes:
- Modifies file descriptor handling in `t/test-lib.sh` to redirect fd3 (test stdout) to stderr instead of stdout in verbose mode
- Updates test assertions in `t/t0000-basic.sh` to verify the new output routing
- Introduces `check_sub_test_lib_test_err` to handle the split output streams
- Preserves existing behavior for non-verbose modes (output still goes to /dev/null)

The changes build on the series' goal of making test output reliably parsable by Meson's TAP parser while maintaining all existing test functionality. This appears to be a well-justified change that completes the technical review cycle, with all prior feedback addressed and no outstanding concerns noted in the thread.