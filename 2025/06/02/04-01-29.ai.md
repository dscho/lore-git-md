Feature/bugfix patch in the `imap-send` series, addressing PLAIN authentication implementation details. This email responds to Junio Hamano's review comments about error message consistency and authentication flow verification in the PLAIN authentication method implementation.

The discussion focuses on two key technical details in the PLAIN authentication code path. First, there's a clarification about the difference between CRAM-MD5 (OpenSSL-only) versus PLAIN/OAuth2 (both OpenSSL and libcurl) support, explaining why the error messages differ in their wording. The author acknowledges the feedback and considers removing the error messages entirely based on prior OAuth2.0 discussion.

Second, the email addresses the broader issue of local capability verification before attempting authentication, which was previously raised for other authentication methods. The patch shows the server-side capability check via `CAP(AUTH_PLAIN)` but doesn't yet implement the local support verification that Junio has requested for all authentication methods in the series.

The technical implementation includes the `plain_base64()` helper function, `auth_plain()` authentication handler, and server capability checking in `imap_open_store()`. The code follows the established pattern for error handling seen in other authentication methods, with proper memory management of the base64-encoded response buffer.

This is part of the comprehensive IMAP authentication overhaul in the v11 series, which has already addressed similar issues for OAuth2.0 and CRAM-MD5. The remaining polish items for PLAIN authentication appear to be message phrasing consistency and the broader authentication flow verification question that affects all methods in the series.