Feature patch series adding GPG signing to `git subtree` operations, now addressing option parsing implementation details.

Patrik Weiskircher responds to Junio Hamano's suggestion about handling the `-S/--gpg-sign` option parsing in the ongoing subtree signing feature series. The maintainer had proposed a hybrid approach combining stuck-form `-S[<key-id>]` with `--gpg-sign[=<key-id>]` syntax, but Weiskircher demonstrates through testing that `git rev-parse` transforms both forms into the same output (`-S 'abcd'`), reintroducing the original ambiguity problem. 

The email includes concrete test output showing that both `-Sabcd` and `--gpg-sign=abcd` get normalized to identical forms after `git rev-parse` processing. Weiskircher expresses concern that implementing manual parsing would create maintenance burden, suggesting the current implementation constraints may require revisiting the parsing strategy.

This technical exchange follows the maintainer's earlier guidance to reorganize the patch series and improve the commit message. The discussion now focuses specifically on the mechanics of option handling, a critical implementation detail for the signing functionality. The thread shows the author working through the practical implications of the maintainer's suggestions while maintaining Git's existing option parsing conventions.