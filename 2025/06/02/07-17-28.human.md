Refactoring patch in Patrick Steinhardt's maintenance task execution series (v3 2/12). This patch simplifies the quiet mode handling in `git gc` by removing a redundant local variable that duplicated functionality already provided by the `maintenance_run_opts` struct. 

The change eliminates confusion about which `quiet` flag to use by standardizing on the struct field. The patch updates all references to use `opts.quiet` instead of the local `quiet` variable, including progress reporting for repacking, pruning, and commit graph writing. 

This is a straightforward cleanup that improves code clarity without changing behavior, part of the larger effort to improve maintenance task execution safety. The change is small (6 lines removed, 5 added) and focused, with no apparent impact on functionality. The patch maintains consistency with the series' broader goals of reducing global state and improving code maintainability.