This is a test patch in a series extending `git cat-file`'s batch mode functionality. The patch updates test infrastructure to handle generic object specifiers (not just OIDs) and adds new test cases, including paths with spaces.

The changes focus on the test helper `run_tests` in t1006-cat-file.sh, which now accepts any valid object specifier (refs, paths with OIDs, etc.) rather than requiring full OIDs. The patch adds several new cases:
- Tests for path-based object references (`<OID>:<path>`)
- Explicit testing of paths containing spaces
- Proper handling of the `%(rest)` format specifier limitation with space-containing paths (marked as test_expect_failure)
- Updates to tree content tests to include a new test file with spaces in its name

The test changes are thorough and well-considered, with clear attention to edge cases like space handling and compatibility between hash algorithms. The patch maintains backward compatibility while expanding test coverage, and properly documents the known limitation of `%(rest)` with spaces via a FIXME comment.

This appears to be a solid foundation for the subsequent patches in the series that will expose more object information through the batch interface. The test changes are focused and maintainable, with clear separation between test infrastructure updates and new test cases.