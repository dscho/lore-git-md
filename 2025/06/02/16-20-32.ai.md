This is a documentation refinement in the ongoing signature handling discussion for `fast-export`/`fast-import`. The email continues the thread's focus on clearly communicating the experimental nature of the signature handling features.

Junio Hamano responds to Christian Couder's suggestions about further clarifying the temporary nature of the current defaults in both documentation and test cases. Hamano maintains the position that the experimental label sufficiently communicates potential future changes, and that explicitly noting default behavior changes would be redundant for users opting into experimental features. The response indicates the maintainer's view that the current documentation approach (marking the feature as experimental with format change warnings) adequately covers both the data stream format and default behavior uncertainties.

Key technical points referenced:
- The `--signed-commits` option's experimental status
- Current default behavior matching `strip` mode
- Test case `signed-commits default is same as strip` in t9350
- Previous environment variable escape hatch (`FAST_EXPORT_SIGNED_COMMITS_NOABORT`) that was removed

The exchange shows consensus on the experimental marking approach but reveals differing perspectives on how explicitly to document potential default behavior changes. This represents the documentation-focused phase of the thread where implementation details are stable but communication strategies are being refined. The discussion remains technical but has moved from policy debates to fine-tuning warning messages and test case comments.