Test modernization for `git cat-file` batch mode. This is the first patch in a 3-part series extending `git cat-file`'s batch processing capabilities. The patch updates the test suite to handle generic object specifiers (like refs or path-based lookups) rather than just raw OIDs, and adds new test cases that include paths with spaces. 

The changes modify the test helper `run_tests` to accept any valid object specifier, with proper quoting throughout. It also documents a known limitation where the `%(rest)` format specifier doesn't work with object names containing spaces, marking those cases as expected failures. The test setup now includes a file with spaces in its path to exercise this case.

This appears to be a well-constructed test update that lays groundwork for the series' subsequent patches. The changes are thorough, with careful attention to quoting and backward compatibility. The expected failure for space-containing paths is properly documented rather than worked around, suggesting this may be addressed in future work.