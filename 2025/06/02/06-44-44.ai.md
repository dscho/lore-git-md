This is a test infrastructure patch in Patrick Steinhardt's Meson test integration series (v4 4/10). It refactors Python version detection for git-p4 tests to use proper prerequisite handling, maintaining TAP output compliance.

The patch modifies t9835 and t9836 to replace direct Python version checks with `test_lazy_prereq` calls (P4_PYTHON2 and P4_PYTHON3 respectively). This ensures the test output remains TAP-compliant by moving the version check logic into prerequisite evaluation. The changes include:
- Moving PATH manipulation outside the conditional check
- Consolidating the Python binary check, script creation, and validation into single prerequisite blocks
- Using `test_grep` to verify git-p4 output rather than direct grep
- Maintaining identical behavior while improving output formatting

This follows the series' pattern of prerequisite cleanup seen in previous patches (t0050, t3600, etc.), though focused specifically on Python version handling for git-p4 tests. The changes are mechanical refactoring with no functional impact beyond TAP output formatting. The patch builds on the series' established approach to prerequisite handling while addressing a Python-specific edge case.