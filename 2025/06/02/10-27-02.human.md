This is part of the ongoing `the_repository` removal effort, specifically focusing on renaming the core object storage structure. Patrick Steinhardt proposes changing `raw_object_store` to `object_database` (ODB) to better reflect its purpose and make the code more self-documenting.

The patch argues that the current "raw_" prefix is misleading (there is no non-raw variant) and unwieldy in function names. The new name aligns with existing terminology in the codebase and allows for cleaner function naming like `odb_has_object()`. This change is part of a larger effort to make the object subsystem more self-contained and eventually support pluggable backends.

The mechanical changes touch 8 files, updating type names and function signatures consistently. The commit message provides clear rationale for the change and notes this is the first of 17 patches in the series. The changes appear straightforward and well-contained, with no behavior modifications - just renaming for clarity as part of the broader architectural cleanup.