This is a follow-up discussion in the batched reference updates bugfix series, specifically about test coverage for the first patch which fixes a segfault in the files backend during failed batched updates.

Karthik Nayak clarifies that the edge case being fixed (deletion requests with expected old OID for non-existent refs) wasn't previously covered in the test suite. Junio Hamano's earlier comment is interpreted as suggesting to add such test cases, which Karthik confirms has already been done in the patch. The test was added to `t/t1400-update-ref.sh` to verify the fix for the segfault when skipping already-failed updates.

This exchange confirms the test coverage aspect of patch 1/3 is now addressed, with no further action needed on this particular point. The discussion remains focused on implementation details rather than introducing new concerns about the fix itself.