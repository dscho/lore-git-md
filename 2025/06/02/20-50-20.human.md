This is part 1/2 of a documentation patch series updating the MyFirstObjectWalk tutorial to modern Git practices. The patch focuses on removing usage of the deprecated `the_repository` global variable in favor of passing a `struct repository*` parameter, following changes introduced in Git's codebase.

The changes update the tutorial's example code to:
- Add the `repo` parameter to `cmd_walken()` function signature
- Replace `git_config()` calls with `repo_config()`
- Fix `repo_init_revisions()` to use the passed `repo` parameter
- Update all related documentation text to match these code changes

The patch is well-motivated by recent Git changes (referencing commits 9b1cb5070f and 036876a106) and maintains the tutorial's pedagogical clarity while bringing it up to date. It's part of the ongoing effort to eliminate `the_repository` usage and has already incorporated feedback from v1 review, particularly around function parameter consistency and commit message formatting.

This appears to be a straightforward documentation update that properly modernizes the tutorial while preserving its educational value. The changes are focused and clearly explained in both the commit message and the tutorial text itself.