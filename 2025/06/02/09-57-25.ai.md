This is a test cleanup patch in a bugfix thread about batched reference updates. It's the second of three patches in the series.

The patch makes a straightforward change to test descriptions in `t/t5516-fetch-push.sh`, switching from single to double quotes for test names that contain shell variables (like `$name`). This ensures the variables will expand when the test runs. The change affects three test cases that verify how `remote.$name.push` refspecs are handled during push operations.

While this is a minor cleanup, it's part of preparing the test suite for the upcoming third patch in the series which will modify batched reference update behavior in `git receive-pack`. The test file being modified (`t5516`) is where the F/D conflict tests for batched updates will land, as noted in the thread context. The changes are mechanical and don't affect test behavior - they just make the test output more accurate when variables are used in test names.

Key technical detail for future reference: the modified tests verify refspec handling during push operations, specifically testing the interaction between explicit push refspecs and `remote.$name.push` configuration. The test names now properly reflect their variable usage with double quotes. This file will later contain tests for the batched reference update fixes mentioned in the thread context.