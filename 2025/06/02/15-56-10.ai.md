This is a follow-up discussion in the ongoing signature handling thread, where Christian Couder responds to Junio Hamano's patch marking the `--signed-commits` feature as experimental. The email confirms agreement with Junio's approach while raising minor documentation suggestions.

Christian concurs with Junio's decision to:
- Mark the signature handling as experimental in documentation
- Revert the default behavior to `strip` (matching pre-feature behavior)
- Remove the `FAST_EXPORT_SIGNED_COMMITS_NOABORT` environment variable

He suggests two potential documentation enhancements:
1. Explicitly noting that the default behavior may change in future versions
2. Adding similar future-proofing language to the test case comments

The technical changes in Junio's patch include:
- Updates to `RelNotes`, `fast-export`, and `fast-import` documentation
- Removal of environment variable handling code from `builtin/fast-export.c`
- Test simplification in `t9350-fast-export.sh` to verify default matches `strip`

This response maintains the thread's trajectory toward stabilizing the feature's experimental status while offering minor suggestions to improve future clarity. The discussion remains focused on documentation and test coverage rather than reopening the core design decisions settled in earlier exchanges.