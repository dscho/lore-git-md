This is a test infrastructure patch in Patrick Steinhardt's series to improve Meson test integration. It addresses TAP parser compatibility by silencing repository initialization output that could confuse test harnesses.

The patch refactors several test scripts to move `test_create_repo()` calls into proper test cases rather than running them at script scope. This ensures the "Initialized empty Git repository" messages don't appear outside test blocks, which is important for TAP compliance. One exception remains in t1007 where the repo creation is wrapped in helper functions, so it uses `git init --quiet` instead.

The changes are mechanical but thorough, updating four test files to use direct `git init` calls (as `test_create_repo()` is deprecated) while maintaining the same test behavior. This is part of a larger effort to make Git's test suite work properly with Meson's TAP parser, with all technical concerns already addressed in prior review rounds. The patch appears clean and focused, with clear commit message explanations of both the what and why of each change.