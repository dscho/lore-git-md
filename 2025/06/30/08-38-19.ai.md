This is a refactoring and CI/build system patch series that modifies Git's clang-format configuration to reduce false positives in code formatting. The series follows up on previous discussions about clang-format integration and line length handling.

The key changes in v2 are: (1) removing the controversial .editorconfig line length patch that was causing unwanted editor behavior, (2) ensuring meson specifically checks for git-clang-format rather than clang-format, and (3) keeping the core formatting changes that reduce false positives. The series now consists of three patches: removing ColumnLimit to stop clang-format from line-wrapping, making RemoveBracesLLVM permanent in .clang-format, and adding a meson target for git-clang-format.

The sample diffs show the practical effect - reduced formatting noise in files like builtin/diff.c where string concatenation is now handled more cleanly. The changes appear well-considered, with Karthik Nayak responding to feedback about editorconfig behavior and verifying the meson target works with git-clang-format. The series represents a pragmatic refinement of Git's formatting tools rather than a radical change, though the author notes future potential to make the style check a required CI job.

For future thread context: this v2 series drops the problematic editorconfig line length change while keeping other formatting improvements. Key technical points are ColumnLimit=0 in .clang-format, RemoveBracesLLVM becoming permanent, and the new meson style target using git-clang-format. The changes affect code formatting behavior but not runtime functionality, touching .clang-format, meson.build, and CI scripts. The discussion has moved past line length debates to focus on practical formatting quality improvements.