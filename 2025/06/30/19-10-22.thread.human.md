This thread documents updates to the git-config(1) man page following the merge of the ps/config-subcommands series. The 5-patch series updates documentation for new and modified command-line options, with each patch handling specific documentation tasks. The first three patches introduce new documentation, while the last two clean up existing text.

The latest patch focuses on documenting the `--[no-]show-names` options, which control whether config keys are shown alongside values when using `git config get`. The default is `--no-show-names`, except when `--url` is used without subsections. The change is straightforward, lifting the option description from source code comments and following established documentation conventions.

Other changes in the series include standardizing on `--value` terminology (replacing the deprecated `value-pattern`) and adding the missing `--url` option to the synopsis. The updates are mechanical and non-controversial, touching only git-config.adoc to match current implementation behavior. Reviewers should verify the new documentation matches the actual behavior, but no significant issues are expected.