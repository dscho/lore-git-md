This is a technical discussion about Git's credential helper behavior, continuing the thread about credential persistence after failed operations. Brian M. Carlson responds to Michal Suchánek's concerns by clarifying the HTTP status code semantics that govern when credentials should be invalidated.

The key point is that servers should return 403 (Forbidden) or 404 (Not Found) rather than 401 (Unauthorized) when credentials are valid but the operation itself fails, as this prevents Git from unnecessarily clearing the credentials. Brian cites RFC 9110 to show this is the correct HTTP protocol behavior, implying the server implementation may be at fault here rather than Git's design.

Brian acknowledges that while a config option to preserve credentials could theoretically be added, they express concerns this would create troubleshooting difficulties and require documentation updates to prevent confusion. The discussion remains at an impasse between the current security-conscious design and Michal's workflow needs, with no immediate path forward beyond server-side fixes or custom credential helper workarounds.