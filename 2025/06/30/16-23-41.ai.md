This is a test modernization patch in the ongoing series about diff context configuration for interactive patch operations. The patch converts `grep` calls to `test_grep` in test files t3701 and t4055, following Git's modern testing practices that provide better debugging output when tests fail.

Junio C Hamano provides minor wording suggestions for the commit message, recommending phrasing it as a "preparatory clean-up" rather than calling it a "prerequisite", since the change isn't strictly required for subsequent patches but is a worthwhile improvement. He also notes the correct handling of negation in the test conversion (using `test_grep !` rather than `! grep`).

The patch itself is straightforward mechanical conversion of 38 grep calls to test_grep across the two test files, with the example diff showing one such conversion in t3701. This change is part of the test infrastructure improvements that form the foundation for the rest of the series implementing diff context configuration support. Junio confirms the technical correctness of the change while offering his standard guidance on commit message style.