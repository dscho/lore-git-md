This is a technical clarification in the ongoing `git apply --intent-to-add` bugfix series. Raymond Pasco responds to Lidong Yan's observation about patch 3/5, explaining the deeper reasoning behind the index-handling behavior.

The discussion centers on when `git apply --intent-to-add` should modify the index. Pasco confirms Yan's point about consistency with `git add --intent-to-add` for existing files, but emphasizes that the primary motivation is maintaining correct mode separation: the intent-to-add flag should only affect index entries for new files, not existing ones. This prevents the command from incorrectly updating the index for both modified and new files in mixed patch scenarios.

The exchange shows alignment on the technical approach while providing additional context about the design constraints. This appears to be a refinement of understanding rather than a disagreement - both contributors agree the current patch series correctly implements the desired behavior, with Pasco adding rationale about maintaining proper separation between index updates for different file states.