This is a documentation patch (part 3/5) in a series updating the git-config(1) man page to cover recently added command-line options. The patch focuses on documenting the `--value=<pattern>` and `--no-value` options that were introduced in the ps/config-subcommands merge (fe3ccc7aab) but not previously documented.

The changes add clear documentation for these options in git-config.adoc, explaining that with `get`, `set`, and `unset` subcommands, they match against a pattern (treated as an extended regular expression unless `--fixed-value` is used). The patch follows the established convention for documenting such options, with `--no-value` presented as the way to unset the pattern.

Key technical details for future reference: the pattern matching applies to three subcommands (`get`, `set`, `unset`), the default regex behavior can be overridden with `--fixed-value`, and this replaces the older `value-pattern` positional argument. The documentation aligns with the actual implementation introduced in the referenced commits.

This is a straightforward documentation update that completes the coverage of the config subcommands functionality. The patch is well-formed with a clear commit message linking to the original implementation and follows the project's documentation conventions. It continues the mechanical but important work of keeping docs in sync with code changes from the ps/config-subcommands series.