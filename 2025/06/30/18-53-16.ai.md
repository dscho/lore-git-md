This is a technical discussion about patch 3/5 in the `git apply --intent-to-add` bugfix series, focusing on the index update logic. Junio Hamano is questioning whether the proposed change to `create_file()` in `apply.c` should verify both `state->ita_only` and `state->update_index` together, rather than just checking `ita_only` for new files.

The core issue revolves around ensuring intent-to-add markers are only written for genuinely new files while maintaining proper index handling semantics. Raymond Pasco's patch currently uses `ita_only && patch->is_new > 0` as the condition, but Junio suggests the logic should explicitly confirm `update_index` is set when checking `ita_only`, as the two flags are conceptually related.

This discussion builds on earlier exchanges about the fundamental design of intent-to-add behavior in `git apply`, with Junio (as the original `add -N` inventor) providing architectural perspective while reviewing the technical implementation. The exchange demonstrates careful attention to flag interactions in the apply state machine, particularly around the boundary conditions between index updates, checks, and intent-to-add operations.

Key technical points at stake:
- The relationship between `update_index`, `check_index`, and `ita_only` flags
- Whether the condition should maintain explicit coupling between `ita_only` and `update_index`
- How to properly handle the transition between checking and updating the index in intent-to-add mode

This email doesn't introduce new test cases or functionality changes, but rather scrutinizes the internal consistency of the proposed solution. The outcome will affect how Git determines when to write intent-to-add index entries during patch application.