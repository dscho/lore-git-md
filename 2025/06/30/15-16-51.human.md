This is a discussion about the pipe-based approach to child process tracking in git-daemon, part of the ongoing signal handling redesign.

Junio responds to Phillip Wood's concerns about the proposed pipe-per-child design, noting that the "self pipe trick" traditionally refers to a single pipe for the daemon-wide signal handling rather than per-child pipes. He expresses surprise at the direction of having a pipe per child process, which was not the expected implementation.

The exchange highlights a key design question in the pipe-based solution - whether to use a single daemon-wide pipe or individual pipes per child. Junio's comment suggests the per-child approach may be overcomplicating the solution compared to the traditional self-pipe pattern. This comes as Carlo continues refining the pipe implementation based on earlier feedback, with the discussion now focusing on implementation details rather than the high-level approach.

The thread shows the technical tradeoffs being weighed between robustness (handling all race conditions) and simplicity (minimizing overhead and complexity. Junio's intervention may steer the discussion toward reconsidering the single-pipe approach as a potentially simpler alternative to the current per-child pipe design.