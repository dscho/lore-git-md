This is a documentation patch in a 5-part series updating the git-config man page. It adds documentation for the `--[no-]show-names` options that were introduced in Git's code earlier but not yet documented in the manual.

The patch explains that these options control whether `git config get` shows both keys and values (with `--show-names`) or just values (the default `--no-show-names` behavior). It notes a special case where `--url` option changes the default behavior, though that detail is deferred to a later note in the series rather than being included in the main documentation.

The change is straightforward - adding 6 lines to document these options in git-config.adoc, following the pattern established by similar options. The commit message references both the original implementation commit and a mailing list discussion about documentation conventions, showing good attention to historical context. This appears to be a well-justified documentation update that fills a clear gap.