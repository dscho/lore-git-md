This is the final patch in a documentation series updating the git-config(1) man page. It adds the `--url` option to the synopsis for the `get` subcommand, completing the documentation updates started in earlier patches.

The change is straightforward - it simply adds `[--url=<url>]` to the `git config get` synopsis line in git-config.adoc. This matches the actual implementation introduced in commit 4e513890008 ("builtin/config: introduce 'get' subcommand", 2024-05-06) but was apparently overlooked in the initial documentation.

This patch follows the same pattern as the rest of the series - mechanical updates to ensure the documentation accurately reflects the current implementation. It completes the work of documenting all command-line options for the config subcommands, with particular attention to standardizing terminology around pattern matching (using `--value=<pattern>` consistently) and ensuring all options appear in the synopsis.

The change is small and non-controversial, aligning with the established pattern of the series. It references the original commit that introduced the functionality, making it easy to verify the documentation matches the code. With this patch, the documentation updates for the config subcommands appear complete.