This is a technical discussion about patch 2/5 in the `git apply --intent-to-add` bugfix series. The patch modifies `read_apply_cache()` to properly handle index reading in intent-to-add mode by checking both `state->check_index` and `state->update_index` conditions.

Junio Hamano, the original inventor of `add -N`, weighs in with two key points:
1. He questions whether `apply -N`'s current approach of writing only ITA entries to the index is conceptually correct, suggesting the design might need reconsideration
2. He agrees with the technical implementation change in this specific diff, acknowledging that reading the index is necessary when either checking or updating it

The patch itself is a minimal change to `apply.c` that expands when `read_apply_cache()` is called, fixing the core issue where intent-to-add mode wasn't properly reading the index. This builds on prior work by Johannes Altmanninger and addresses a bug report from Ryan Hodges.

The discussion reveals an underlying design question about whether intent-to-add should write partial index entries, which may need resolution before the series can proceed. Junio's comment suggests this patch may be technically correct but could be addressing a potentially flawed approach.