This is a documentation patch series updating the git-config(1) man page to reflect changes introduced by the ps/config-subcommands series that was merged earlier. The series consists of 5 patches that systematically update the documentation for new and modified command-line options.

The changes focus on three main additions: documenting the new `--show-names` option (and its negation), standardizing documentation around the `--value` option (replacing the deprecated `value-pattern` terminology), and adding `--url` to the synopsis where it was previously missing. The patches appear to be straightforward documentation updates that follow the established patterns in the man page.

The series touches only the git-config.adoc file, making focused changes to ensure the documentation matches the current implementation. Each patch handles a specific documentation task, with the first three introducing new documentation and the latter two cleaning up existing text to use the preferred terminology. The changes are mechanical in nature, replacing deprecated terms with their modern equivalents and adding missing option documentation.

For future reference in thread summaries, key elements include: the `--show-names` option (newly documented), the transition from `value-pattern` to `--value` terminology, and the addition of `--url` to the synopsis. The changes are non-controversial documentation updates that follow established patterns, though reviewers might want to verify the new documentation accurately reflects the implementation's behavior.