Bugfix discussion in the `git apply --intent-to-add` series. This is Junio Hamano (the original inventor of `add -N`) responding to patch 3/5 in Raymond Pasco's series fixing index handling for intent-to-add mode.

Junio questions whether the patch's logic for writing intent-to-add entries is sufficiently precise. The current patch checks `ita_only && patch->is_new > 0` to determine when to write index entries, but Junio suggests it should also verify `update_index` is set, since that's the overarching flag indicating index modifications are allowed. This maintains consistency with the original design where `update_index` controls all write operations to the index.

The exchange shows careful attention to the conceptual model - Junio is ensuring the fix doesn't just make the code work, but that it follows the intended abstraction where `update_index` remains the master switch for index modifications. This suggests the series may need a minor adjustment to make the control flow more explicit about the relationship between these flags.

The thread context shows this is part of a larger effort to properly restore `--intent-to-add` functionality that's been broken since Git 2.19, with patch 3 specifically focusing on ensuring intent-to-add markers are only written for truly new files. Junio's feedback indicates the technical approach is sound but could be more precise in its flag handling.