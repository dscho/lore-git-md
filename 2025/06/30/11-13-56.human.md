This is a follow-up in the thread about `git branch -a` output format changes. Kristoffer Haugsbakk suggests an alternative approach to Olaf Hering's script parsing issue, recommending `git for-each-ref` with specific formatting and exclusion of HEAD references as a more stable solution than parsing `git branch -a` output.

The suggestion acknowledges the compatibility break while providing a practical workaround that avoids the need for manual filtering of the new "remote/HEAD -> remote/branch" lines. The proposed solution uses Git plumbing commands rather than porcelain, which typically offer more stable output formats for scripting purposes.

This response doesn't address whether the behavior change was intentional or whether a configuration option might be added, but focuses on providing an immediate solution for affected scripts. The recommendation appears technically sound and aligns with Git's philosophy of using plumbing commands for scripting needs.