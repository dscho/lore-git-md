This is a documentation patch series updating `git-config`'s manual page to reflect recent changes from the `ps/config-subcommands` series that was merged earlier. The series consists of five small patches that:

1. Document the new `--show-names` option
2. Document the `--value` option
3. Consistently use `--value` instead of the older `value-pattern` terminology
4. Add `--url` to the synopsis section

The changes appear straightforward and well-scoped, simply bringing the documentation in line with functionality that already exists in the code. The commit messages clearly reference the original implementation series, making it easy to trace the motivation. This looks like routine documentation maintenance that should be uncontroversial to merge.