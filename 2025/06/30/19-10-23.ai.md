This is a documentation patch in a 5-part series updating git-config(1) man page following recent subcommand changes. It focuses specifically on documenting the `--[no-]show-names` options introduced in commit 4e513890008 but not previously documented in the manual.

The patch adds clear documentation for both `--show-names` and its negated form `--no-show-names`, explaining they control whether config keys are shown alongside values when using `git config get`. It notes the default is `--no-show-names` except when `--url` is used without subsections, though this detail is intentionally kept minimal to avoid duplicating deprecated mode information.

The change is mechanical and straightforward, lifting the option description from source code comments and following established documentation conventions. The patch touches only git-config.adoc, adding 6 lines in the options section. This appears to be a routine documentation catch-up for functionality that was merged but not fully documented, with no behavioral changes or controversy expected. The series note references commit d4770964d50 for historical context about the `--url` exception case.