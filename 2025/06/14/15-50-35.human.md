This is a follow-up discussion in the ongoing thread about memory management in Git's notes subsystem. Lidongyan responds to Junio Hamano's explanation about why memory cleanup is important even on `die()` paths, where the OS would reclaim memory anyway.

The email acknowledges Junio's point about leak checkers flagging such cases, but raises a practical concern: adding cleanup before every `die()` call would be messy compared to using `goto` patterns for centralized cleanup. This suggests the discussion is shifting from whether to clean up (settled as "yes") to how best to implement that cleanup.

The exchange highlights Git's attention to detail in memory management, where even theoretically harmless leaks are addressed to maintain clean tool output. Lidongyan's response shows engagement with the project's standards while questioning implementation ergonomics, though no specific code changes are proposed here. The thread remains focused on the notes subsystem but touches on patterns that could apply more broadly.