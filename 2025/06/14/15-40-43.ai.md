This is a continuation of the memory management discussion in Git's notes subsystem, specifically addressing error handling philosophy. Junio Hamano responds to Lidongyan's question about whether memory leaks on `die()` paths matter in practice, since the OS reclaims memory on process termination.

Junio acknowledges that while the OS does clean up memory, leak detection tools like sanitizers would still flag these cases. His position is that fixing such cases helps maintain clean output from leak checkers, making it easier to identify genuine memory issues. This reinforces the value of the original patch's approach while addressing the broader question about error handling discipline.

Key technical details remain the same: parse_reuse_arg()` in `builtin/notes.c`, the interaction with `repo_get_oid()` and `repo_read_object_file()`, and the memory management pattern of moving allocations after error-prone operations. The discussion has now expanded to include considerations about tooling affects design decisions around error paths.

This email doesn't introduce new code changes but provides important context about why the project might choose to maintain strict cleanup discipline even in fatal error cases. It suggests that while the original technical fix is sound, there's room for discussion about the underlying principles guiding such decisions.