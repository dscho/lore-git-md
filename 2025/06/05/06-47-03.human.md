This is part 13/17 of Patrick Steinhardt's series to refactor Git's object database (ODB) subsystem, continuing the effort to remove `the_repository` global state. The patch (12/17) just completed removing `the_repository` from odb.c internals.

This patch renames `oid_object_info()` to `odb_read_object_info()` and its `_extended()` variant to match the established `odb_` prefix convention for ODB-related functions. The changes are mechanical but widespread, touching 48 files with 213 insertions and 170 deletions. Compatibility wrappers are added to avoid breaking in-flight topics.

The patch follows the same pattern as previous conversions in the series, standardizing the API naming while maintaining functionality. It's a straightforward change that aligns with the series' goal of making ODB operations explicitly take an object database parameter rather than relying on the implicit `the_repository`. 

The changes are well-contained and consistent with the established direction of the larger refactoring effort. Reviewers have already approved similar mechanical conversions in earlier patches, so this should be uncontroversial. The patch includes thorough updates to call sites across the codebase, demonstrating careful attention to detail.