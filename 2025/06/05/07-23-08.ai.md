Memory leak clarification in `repo_logmsg_reencode()` discussion.

Jeff King provides additional technical context about the memory management implications of the `repo_logmsg_reencode()` leaks being fixed in this thread. He explains that commit buffers are normally stored in a commit-slab, so leaks aren't typically flagged unless there's actual re-encoding (which requires mismatched encodings). He demonstrates how to force re-encoding via a test hack (`GIT_TEST_ALWAYS_REENCODE`) that surfaces these leaks in `t3650-replay-basics.sh` and `t6429`, though notes this is too specialized for permanent test inclusion.

The email also confirms Patrick Steinhardt's earlier suggestion about using `goto out` patterns in `shortlog.c` was correctly implemented in v2, avoiding potential issues with uninitialized variables. This thread has now covered all technical aspects of the memory management fixes, with consensus on the current approach. The discussion has moved from problem identification to implementation refinement, with no outstanding technical questions remaining.