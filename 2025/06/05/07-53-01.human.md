This is a follow-up to the memory leak fixes in `repo_logmsg_reencode()` callers. Jeff King confirms that Lidong Yan's v2 patch correctly addresses the leak in `builtin/replay.c` using his earlier test method, though notes the `shortlog.c` case isn't test-covered in practice. 

The technical discussion appears settled, with the implementation now handling both cases properly through `repo_unuse_commit_buffer()` cleanup. Jeff indicates the shortlog fix doesn't need additional test coverage since the leak scenario (commits without header separators) is too edge-casey to warrant dedicated tests. 

The thread shows consensus on these being merge-ready fixes, with no outstanding questions about the approach. The patches demonstrate careful attention to Git's memory management patterns while avoiding test bloat for unlikely scenarios.