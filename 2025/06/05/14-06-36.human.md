This is part 2/10 of the reftable test modernization series, converting the reftable basics test to use the Clar framework. The patch replaces the old test-lib.h-based test file (t-reftable-basics.c) with a Clar-compatible version (u-reftable-basics.c) while maintaining all existing test coverage.

The conversion follows the established pattern from the series: test functions are renamed to the test_* convention, assertions are updated to use Clar's cl_assert macros, and the test now integrates with the shared lib-reftable-clar helper library. The tests cover core reftable functionality including binary search utilities, string array handling, buffer operations, and endian number encoding/decoding.

The patch appears well-executed, with careful attention to maintaining the exact same test cases and assertions as the original. Build system files (Makefile and meson.build) are updated to remove the old test and add the new Clar version. This is a straightforward mechanical conversion that advances the series' goal of standardizing all reftable tests on the Clar framework.