This is a design discussion about the OAuth2.0 authentication implementation in `imap-send`, specifically focusing on the libcurl password handling logic. Junio Hamano responds to Aditya Garg's explanation of the current implementation, probing deeper into the authentication method handling design.

The key technical points under discussion are the conditions under which `CURLOPT_PASSWORD` should be set for libcurl authentication. The current code avoids setting passwords for OAuth methods (XOAUTH2/OAUTHBEARER) but Junio questions whether this is the most maintainable approach long-term. He suggests two alternative implementations that would make the behavior more explicit: either using a BUG/die() for unsupported methods (if they should never reach this codepath) or adding a warning when falling back to plain password authentication.

Junio also suggests using `NOT_CONSTANT()` to handle the OpenSSL compiler warning issue more cleanly, showing his preference for maintainable code patterns even in conditional compilation scenarios. The discussion reveals that CRAM-MD5 authentication is handled separately (and only with OpenSSL), while this codepath specifically deals with libcurl's password-based and OAuth authentication methods.

This email doesn't introduce new functionality but rather seeks to refine the implementation approach before final merge. The conversation highlights the careful balance between correctness, maintainability, and compiler warnings in the authentication handling code. Junio's suggestions aim to make the control flow more explicit and future-proof, while Aditya has explained the technical reasons behind the current implementation choices.