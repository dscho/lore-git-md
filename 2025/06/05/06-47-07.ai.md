This is the final patch in Patrick Steinhardt's v5 series refactoring Git's object database subsystem to remove `the_repository` global state. The patch renames `read_object_with_reference()` to `odb_read_object_peeled()`, completing the mechanical conversion of ODB-related functions to use explicit `object_database` parameters and the `odb_` prefix convention.

The renamed function handles "peeling" objects (recursively resolving tags and commits until reaching the requested object type). The new name better describes this behavior while matching the established naming pattern. The change affects 8 files with 37 insertions and 45 deletions, updating callers in `builtin/` commands and core tree-walking code to use the repository's ODB via `the_repository->objects`.

Notable technical details:
- The function signature changes to take an `object_database *` instead of `repository *`
- All call sites are updated to pass `the_repository->objects` or similar
- Documentation references are updated name
- No compatibility wrapper is needed due to limited usage
- Follows same pattern as previous patches converting ODB functions

This concludes the series' mechanical conversions, having systematically updated all major ODB functions to use explicit parameters rather than relying on `the_repository` global. The work establishes clean foundations for future pluggable ODB backends while improving code organization through consistent naming and reduced global state.