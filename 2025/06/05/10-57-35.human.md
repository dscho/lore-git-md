This is a follow-up discussion in a completed thread about fixing curl compiler warnings. Johannes Schindelin reports seeing additional compiler warnings in Git's CI on macOS with curl 8.14, despite the already-approved patch series that addressed similar issues. He provides a patch converting more `int` parameters to `long` in `curl_easy_setopt()` calls across `http-push.c`, `http.c`, and `remote-curl.c`, noting these were found in the `osx-gcc` CI job.

The email shows good technical diligence - Schindelin includes the specific error messages, links to CI failures, and a complete patch. However, it's unclear why these cases weren't caught by the original series, which had comprehensive review. The tone is collegial, asking Jeff King to incorporate these additional fixes rather than proposing an alternative approach.

This appears to be a straightforward extension of the already-approved work, catching a few more instances of the same type mismatch issue. Given the thread's prior consensus and the clean nature of these additional changes, they seem likely to be accepted as a follow-up. The main question raised is why these cases weren't found earlier, suggesting they may represent platform-specific manifestations of the same underlying issue.