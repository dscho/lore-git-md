This is patch 15/17 in Patrick Steinhardt's series refactoring Git's object database (ODB) subsystem to remove `the_repository` global state. The patch renames `has_object()` to `odb_has_object()` to match the established `odb_` prefix convention used throughout the series. 

The change is mechanical, updating 30 call sites across various builtins and core functionality that check for object existence. A compatibility wrapper maintains backward compatibility for in-flight topics. The patch follows the same pattern as previous conversions in the series, with no behavior changes beyond the API renaming.

Key technical details:
- The new function takes an explicit `object_database` parameter instead of implicitly using `the_repository`
- Flags like `HAS_OBJECT_RECHECK_PACKED` and `HAS_OBJECT_FETCH_PROMISOR` remain unchanged
- The wrapper ensures existing code continues compiling during transition
- Touches object existence checks in critical paths like fetch, push, fsck, and pack operations

This continues the systematic conversion of ODB-related functions to use explicit repository parameters, building on the infrastructure established in earlier patches. The change is straightforward and follows the established pattern, with no outstanding review concerns noted in the thread.