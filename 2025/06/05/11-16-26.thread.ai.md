Thread summary: Discussion about `git fetch` optimization strategies and `git maintenance prefetch` behavior, originating from a question about time-based fetch skipping. The thread explores conditional fetch skipping, network interruption handling, and reference storage locations. Junio C Hamano maintains that core `git fetch` should remain simple, with `git maintenance prefetch` as the recommended solution for periodic fetching.

Current status: No patches proposed; discussion remains conceptual. Core Git's design excludes time-based fetch features, with `git maintenance prefetch` as the official periodic fetch mechanism. Junio has clarified prefetch implementation details showing references are stored under `refs/prefetch/` rather than standard remote-tracking locations, and demonstrated how to manually promote these references when offline.

Key technical details:
- FETCH_HEAD timestamp tracking works for single remotes
- `git maintenance prefetch` stores references under `refs/prefetch/remotes/origin/*`
- Standard fetch destination pattern can be reconstructed as `refs/prefetch/remotes/origin/*:refs/remotes/origin/*`
- Local fetch (`git fetch .`) can transfer between reference namespaces
- Core `git fetch` intentionally avoids automated/time-based features

Open questions:
- Whether prefetch interruption handling needs improvement
- If documentation should better explain network reliability scenarios
- Whether original time-skipping needs are fully covered by prefetch
- Potential for documenting workaround patterns for interrupted prefetches

Participant positions:
- Konstantin Ryabitsev: Proposed practical workarounds using existing mechanisms
- Junio C Hamano: Confirmed design exclusion of time-based features, suggested prefetch, and explained prefetch reference storage details
- M Hickford: Raised network reliability concerns with prefetch approach

Related work:
- `git maintenance` subsystem (prefetch subcommand)
- Existing fetch optimizations (`--no-tags`, shallow clones)
- Potential documentation updates about fetch strategies
- Network reliability patterns in distributed workflows