This is a follow-up confirmation in the memory leak fixes thread. Jeff King verifies that Lidong Yan's v2 patch correctly addresses the memory leaks in both `builtin/replay.c` and `builtin/shortlog.c` by adding proper `repo_unuse_commit_buffer()` cleanup. 

Key technical points confirmed:
- The replay.c fix is now verified to work using Jeff's earlier test hack (`GIT_TEST_ALWAYS_REENCODE`)
- The shortlog.c case remains untested in the suite, but Jeff agrees this is acceptable since it would require an artificial test case (commits without header separators)
- No further changes are needed to the implementation

The thread has reached completion with this confirmation, as all prior technical discussion about the fixes (including Patrick Steinhardt's suggested cleanup pattern) has been addressed satisfactorily. The patches appear merge-ready with no outstanding concerns.