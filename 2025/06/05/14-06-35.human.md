This is a test infrastructure patch, part of the ongoing reftable test modernization effort to migrate from custom test helpers to the Clar framework. It's the first of a 10-part series converting reftable tests.

The patch introduces new Clar-compatible helper functions in `lib-reftable-clar.{c,h}` that will be used by subsequent test conversions. The helpers include utilities for hash generation, buffer writing, and reftable record validation. The implementation appears thorough, with careful handling of reftable-specific details like block sizes and hash IDs.

The changeset updates both Makefile and meson.build to include the new test helpers, showing good cross-build system awareness. The helper functions maintain the same core functionality as the existing test-lib.h versions but adapt them to Clar's assertion style.

This foundational work looks solid and well-contained, setting up the necessary infrastructure for the actual test file conversions that follow in the series. The commit message clearly explains the purpose and how it fits into the larger migration effort.