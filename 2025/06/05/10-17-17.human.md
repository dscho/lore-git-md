This is a follow-up in the ongoing investigation of Windows+Meson CI test hangs related to the `--skip-connectivity-check` feature for `git receive-pack`. The thread has identified a reproducible hang in the test infrastructure when using `tee` pipes with `receive-pack` on Windows under Meson builds.

Johannes Schindelin confirms the issue is consistently reproducible in GitHub Actions (unlike local environments) and proposes an alternative solution to Patrick Steinhardt's wrapper script approach. The key points:

1. The hang occurs specifically in the 'receive-pack missing objects bypasses connectivity check' test when using `tee | git-receive-pack` pipe chains in Windows+Meson CI environments (both GitHub and GitLab).

2. Schindelin's proposed solution (referenced via a pull request) differs from Steinhardt's wrapper script approach, though the exact technical details aren't shown in this email. He suggests his alternative may better address the root cause rather than just making the issue less likely.

3. The core functionality of the `--skip-connectivity-check` feature remains unaffected - this is purely a test infrastructure issue specific to Windows+Meson pipe handling.

4. The discussion highlights the challenge of debugging Windows-specific process/pipe behaviors that don't reproduce locally but consistently fail in CI environments.

The email advances the thread by providing another data point about reproducibility and proposing an alternative solution, while the fundamental question about the exact root cause remains open. The exchange shows active collaboration between multiple contributors to resolve this CI-specific edge case.