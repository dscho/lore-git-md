This is a user experience improvement patch in the ongoing `imap-send` feature and bugfix series. It enhances the progress output during email sending to show the destination IMAP folder name, making the operation more transparent to users.

The patch modifies two functions (`append_msgs_to_imap` and `curl_append_msgs_to_imap`) to include the folder name in their progress messages. Where previously users would see just "Sending 1 message", they'll now see "Sending 1 message to Drafts folder..." (or whatever folder is configured). The change is simple but useful, following the series' theme of improving `imap-send`'s usability.

This appears to be a straightforward, well-contained change that completes the user-facing output standardization work mentioned in the thread summary. The patch follows the established patterns of the series and maintains consistency with other recent output formatting improvements. As the 9th of 10 patches in v13, it's one of the final polish items in this mature series that has already addressed all major technical concerns.