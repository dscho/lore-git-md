Bugfix follow-up to the recently merged curl typecheck warnings series. This patch addresses additional boolean flag cases (`CURLOPT_UPLOAD`, `CURLOPT_NOBODY`, `CURLOPT_HTTPGET`) that were missed in the initial review but now trigger warnings in macOS CI builds with curl 8.14.0. The changes follow the same pattern as the earlier fixes - adding `L` suffixes to boolean literals to match curl's expected `long` parameter types.

Junio C. Hamano notes this will be queued on top of the already-merged three-patch series, and suggests tweaking the commit message to better reference the prior work. The changes touch HTTP-related code in `http.c`, `http-push.c`, and `remote-curl.c`, consistently applying the same fix pattern that was previously reviewed and approved.

This appears to be a straightforward completion of the earlier work, catching the remaining cases of the same underlying issue. The patch is well-contained and follows the established solution approach, suggesting it should integrate smoothly with the already-accepted changes.