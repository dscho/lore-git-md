This is a small but useful user experience improvement in the ongoing `imap-send` feature and bugfix series. The patch modifies the credential prompt to include the port number when asking for passwords, making it clearer which server is being accessed. This matches the behavior of `git send-email` and helps users verify they're being prompted for the correct endpoint, especially when dealing with multiple IMAP servers.

The change is straightforward - it formats the host:port string when setting up the credential helper. The commit notes that the default ports (993 for IMAPS, 143 for IMAP) will always be used if none is specified, so there's no need to handle a "no port" case. This is a clean, focused change that improves consistency with other Git commands.

As part of the larger v13 series that fixes critical IMAP functionality and adds OAuth support, this patch represents one of the final polish items to standardize the user experience. The implementation looks correct and follows the established patterns seen elsewhere in the codebase.