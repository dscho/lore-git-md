This is a bugfix patch addressing memory leaks in two callers of `repo_logmsg_reencode()`. Lidong Yan's v2 follows up on Patrick Steinhardt's suggestion to improve the control flow in `shortlog.c` by using a `goto out` pattern for consistent resource management, while keeping the straightforward fix in `replay.c`.

The patch fixes cases where commit buffers allocated by `repo_logmsg_reencode()` weren't being properly freed. The shortlog change now ensures cleanup happens even when no commit message body is found, while the replay.c addition plugs a similar leak. The thread has reached consensus on this approach, with the v2 changes showing the author incorporated the suggested improvements.

This appears to be a straightforward and well-justified cleanup. The changes are small but important for proper memory management, and the discussion shows the implementation has been thoughtfully reviewed. The patch should be ready for merging.