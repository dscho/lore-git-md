This is a bugfix patch addressing memory leaks in callers of `repo_logmsg_reencode()`. The v2 version incorporates feedback from Patrick Steinhardt to improve the control flow in `builtin/shortlog.c`.

The patch adds missing `repo_unuse_commit_buffer()` calls in two locations where `repo_logmsg_reencode()` was being called without proper cleanup. The change in `builtin/replay.c` is straightforward, adding the cleanup in the existing `out` label section. For `builtin/shortlog.c`, the v2 revision restructures the early return case to use a `goto out` pattern, ensuring consistent resource cleanup even when no message body is found.

The thread context shows this revision addresses Patrick's suggestion to consolidate cleanup paths in shortlog.c. The technical discussion has reached consensus, with Lidong confirming the safety of this approach even with initialized-but-unused strbufs. The changes appear well-considered and ready for merging, with no outstanding concerns about the implementation.