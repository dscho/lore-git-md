This is a follow-up in the critical build system regression thread affecting NO_TCLTK builds. Randall Becker asks for clarification on the temporary workaround suggested by Johannes Sixt, specifically whether the `NO_TCLTK=NoThanks` setting can be passed as a command-line argument to `make` rather than modifying `config.mak`.

The thread has already identified the root cause (incorrect `$@` interpretation in `generate-tclindex.sh` that deletes source files when tclsh is missing) and proposed two permanent fixes (targeted rm or line removal). This email focuses narrowly on the practical workaround question, showing users trying to work with the broken build while awaiting the proper fix.

The question implies Randall is testing the rc1 release and needs an immediate solution that doesn't require persistent configuration changes. This suggests the workaround may be needed by others testing the release candidate, though the answer (whether make accepts this as a command-line variable) is basic makefile behavior rather than Git-specific knowledge. The exchange maintains the thread's focus on practical impact mitigation while the core fix is being finalized for the 2.50.0 release.