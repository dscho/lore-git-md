This is a documentation improvement suggestion for Patrick Steinhardt's maintenance task refactoring series (v4 04/12). Derrick Stolee proposes adding explicit documentation about the incompatibility between `--task` and `--schedule` options in `git maintenance run`, matching the code change that enforces this restriction.

The patch being discussed already makes these options mutually exclusive in the implementation by using `die_for_incompatible_opt2()`. Stolee's suggestion would document this behavior in the man page, providing clear guidance to users and matching the existing documentation pattern for other incompatible option pairs in Git commands.

The change would add two new paragraphs (marked with `++`) to the `git-maintenance.adoc` file, each option's section getting a note about its incompatibility with the other. This maintains consistency with how Git documents other mutually exclusive options.

This appears to be a straightforward documentation improvement that aligns with the code changes in the series. Since the series has already received positive reviews and is in its fourth iteration, this suggestion would likely be a welcome addition to improve user-facing documentation clarity. The change doesn't affect the technical implementation but helps prevent user confusion about the option interactions.