Bugfix patch in the `imap-send` series addressing a memory leak in CRAM-MD5 authentication. This is part of a larger v13 series that adds OAuth2.0 support and fixes various IMAP-related issues.

The patch specifically targets a memory leak in `auth_cram_md5()` where the `response` buffer would not be freed if `socket_write()` failed. The fix adds the missing `free(response)` call in the error path before returning. This completes the memory management for CRAM-MD5 authentication that was identified as needing attention in earlier versions of the series.

The change is small (3 lines added, 1 removed) but important for correctness, ensuring no memory is leaked when network operations fail during authentication. The fix follows the pattern established elsewhere in the codebase where resources are properly cleaned up in all paths. This was one of the final memory management issues being addressed in the comprehensive `imap-send` overhaul series.

The thread context shows this is part of a well-reviewed effort that has already addressed several critical issues in IMAP functionality, including configuration parsing bugs and authentication method implementations. This particular fix is straightforward and unlikely to be controversial, completing the CRAM-MD5 implementation's error handling.