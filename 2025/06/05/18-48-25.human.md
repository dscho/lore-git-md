This is a design discussion about OAuth2.0 implementation in the `imap-send` series, specifically addressing authentication method handling in the libcurl backend.

Junio Hamano responds to Aditya Garg's explanation about compiler warnings and authentication method handling in the OAuth2.0 implementation. The discussion focuses on two key points:

First, regarding compiler warnings when building with NO_OPENSSL, Junio suggests using NOT_CONSTANT() as a potential solution to avoid warnings while maintaining the conditional compilation behavior. This addresses a code organization concern raised earlier in the thread.

Second, Junio probes deeper into the authentication method handling logic, seeking clarification on how the code deals with methods other than XOAUTH2 and OAUTHBEARER. Based on Aditya's response that CURLOPT_PASSWORD is only for plain/login auth while OAuth uses CURLOPT_XOAUTH2_BEARER, Junio proposes two alternative implementations that would make the behavior more explicit: either using BUG()/die() for unsupported methods, or adding a warning when falling back to plain password auth.

The exchange shows Junio pushing for clearer code that either strictly enforces supported methods or properly documents fallback behavior, while Aditya maintains that the current implementation correctly handles the supported cases. This is part of the ongoing refinement of the OAuth2.0 support before final merge consideration.