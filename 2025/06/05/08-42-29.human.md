This is a documentation and style cleanup patch in the `imap-send` series. It fixes minor inconsistencies in error messages and log outputs throughout the IMAP client code. The changes standardize capitalization (making "IMAP" and "OK" consistently uppercase), ensure error messages start with lowercase, and add missing periods at sentence endings. While small, these tweaks improve the consistency of user-facing output in a feature that's seen significant recent development.

The patch is part of a larger v13 series that adds OAuth2.0 support and fixes critical bugs in `imap-send`. This particular change doesn't affect functionality but brings the codebase in line with Git's style conventions for error reporting. The changes are straightforward and uncontroversial, touching only message formatting without altering any logic.