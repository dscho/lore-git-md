Refactoring patch in Patrick Steinhardt's v5 series (13/17) to remove `the_repository` global state. This patch renames `oid_object_info()` to `odb_read_object_info()` and its `_extended` variant to align with modern coding guidelines and the ongoing ODB` (object database) subsystem refactoring.

The changes are mechanical but widespread, touching 48 files to update function calls and documentation. The patch introduces compatibility wrappers to maintain backward compatibility with in-flight topics. Key technical details:
- Renames core object info functions to use `odb_` prefix consistently
- Updates all callers to pass `repo->objects` instead of `repo`
- Maintains same behavior while removing implicit `the_repository` usage
- Affects object info lookups across various subsystems (packfile operations, rev-list, fsck, etc.)

This follows the established pattern from previous patches in the series, completing the mechanical conversion of object info APIs. The changes are straightforward given the prior groundwork, with no new functionality or behavior changes beyond the naming and parameter updates. The patch demonstrates careful attention to backward compatibility during this transitional phase.