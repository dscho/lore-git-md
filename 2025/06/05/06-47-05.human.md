This is part 15/17 of Patrick Steinhardt's series to refactor Git's object database (ODB) subsystem, continuing the effort to remove `the_repository` global state. The patch renames `has_object()` to `odb_has_object()` to match the established `odb_` prefix convention for ODB-related functions.

The change is mechanical but widespread, touching 30 files with 87 insertions and 74 deletions. A compatibility wrapper is provided to avoid breaking in-flight topics. The patch follows the same pattern as previous conversions in the series, updating callers to pass the repository's object store explicitly rather than relying on `the_repository`.

This continues the methodical, incremental approach seen throughout the series - each function conversion builds on the infrastructure established by earlier patches. With core infrastructure and naming questions already resolved in prior reviews, this change appears straightforward and uncontroversial. The series as a whole represents significant progress toward enabling pluggable ODB backends while reducing global state.