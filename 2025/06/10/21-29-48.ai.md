This is a question about Git plumbing commands and branch filtering techniques. The email presents three different approaches to list commits between two revisions that are directly pointed to by branch refs, while preserving commit order.

The author D. Ben Knoble explores three methods, each with tradeoffs: (1) using `join` with sorted rev-list and for-each-ref output (loses topological sequence), (2) post-processing `git name-rev` output to validate branch names (works but inelegant), and (3) combining `rev-list --simplify-by-decoration` with `name-rev` (most elegant but potentially unreliable per documentation warnings). The thread appears to be a new discussion rather than a continuation of prior conversation.

Key technical details include the use of `refs/heads/*` pattern matching, `--simplify-by-decoration` behavior concerns, and the note about tag handling being intentionally ignored for this use case. The question centers on whether there's a canonical Git way to achieve this filtered branch-pointed commit list while maintaining commit order, suggesting the author may be looking for either confirmation of their approaches or suggestions for better alternatives.