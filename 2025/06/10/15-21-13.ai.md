This is the first patch in a 5-part series introducing a new `git repo-info` command as a GSoC project, following the thread's established context about splitting repository metadata functionality from `git rev-parse`. The patch lays the basic infrastructure by:

1. Adding a new builtin/repo-info.c file containing a skeleton implementation with empty option parsing
2. Updating build system files (Makefile, meson.build) to include the new command
3. Registering the command in builtin.h and git.c with RUN_SETUP flag
4. Adding git-repo-info to .gitignore

The implementation is currently minimal - the command accepts no options and returns immediately with no output. The patch follows standard Git conventions for adding new commands, with clean diffs and proper file placements. As the first in a series, it focuses solely on the command scaffolding, leaving actual functionality for subsequent patches. The commit message is concise but could benefit from explicitly noting this is part of a larger series (though the [1/5] prefix makes this clear).