Bugfix addressing a memory leak in revision walking during merge conflicts. This is the final version of a patch that has already gone through technical review and consensus on both the fix (adding a missing `free()` in `prepare_show_merge()`) and its test coverage.

The patch now includes a comprehensive test case that creates a three-way merge conflict scenario (base/hello/goodbye branches) to exercise the `git show --merge` code path where the leak occurred. The test methodology was refined through discussion between Lidong Yan and Junio Hamano, with the final version incorporating Junio's style suggestions on indentation and redirection formatting.

This represents a straightforward fix for a clear memory leak, now well-validated by both the simple code change and the thorough test case. The thread shows good collaboration between the contributor and maintainers to arrive at a robust patch that serves as a bugfix and also establishes a reference test for merge-related revision walking behavior. With all feedback addressed in v3, this appears ready for inclusion.