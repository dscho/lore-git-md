This is a follow-up in the pathspec handling regression thread, where Piotr Siupa reinforces the case for keeping exclude patterns silent when they match nothing. The email appears to be a technical glitch where the sender accidentally included formatting issues, but the core message continues advocating for the behavior Junio Hamano previously endorsed.

Piotr's position aligns with practical scripting needs, where silent non-matches for exclude patterns (like `:!pattern`) are essential for workflows such as `git add -- \*.c ':!auto-generated.c'`. The discussion has moved beyond the original empty-repo edge case to establish a clear design principle: exclude patterns should behave differently from positive pathspecs by not erroring on non-matches, mirroring how `.gitignore` works.

While the email itself doesn't introduce new technical arguments, it serves to solidify the emerging consensus that changing this behavior would break existing scripts. The original regression in empty repositories remains to be fixed, but the philosophical debate appears settled in favor of maintaining current exclude pattern semantics. The thread shows how a bug report evolved into clarifying a subtle but important distinction in Git's path specification handling.