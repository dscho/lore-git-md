This is a bugfix follow-up in the interactive rebase state cleanup thread. Phillip Wood responds to Øystein Walle's patch that fixed an edge case where invalid `rebase.instructionFormat` configurations could leave the repository in a half-initialized state during interactive rebase failures.

Phillip agrees with the core solution (reordering operations to validate the instruction format first) but suggests a more thorough fix: modifying `get_commit_format()` to avoid calling `die()` and instead propagate errors properly. This would allow proper cleanup when the format is invalid, particularly important for restoring stashed changes when using `--autostash`. He notes this would require updates to several callers and documentation, and recommends basing the work on the 'ps/maintenance-ref-lock' branch.

The response is constructive but points out the current patch's test should be expanded to verify autostash behavior. Phillip indicates he'll be unavailable but expects others can help Øystein with any questions. The discussion remains focused on improving the robustness of error handling in interactive rebase scenarios.