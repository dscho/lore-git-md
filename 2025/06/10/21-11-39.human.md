This is a follow-up discussion in the concluded thread about removing trailing whitespace from Git notes editor templates. D. Ben Knoble responds to Kristoffer Haugsbakk's confirmation that visual cleanliness was the primary concern, while raising a new observation about `git commit -v` behavior.

The email provides an empirical counterpoint to Kristoffer's earlier claim that `git commit -v` doesn't introduce trailing whitespace - Knoble attaches an editor template showing whitespace on blank lines under the diff. This doesn't challenge the already-agreed notes fix (which uses `strbuf_stripspace()`), but extends the discussion's context about where similar whitespace issues might appear in Git's editor integrations.

The exchange serves as documentation of real-world behavior, though it doesn't reopen any technical questions about the solution. The thread remains concluded with consensus on the notes-specific fix while leaving the door open for future consideration of broader whitespace handling improvements.