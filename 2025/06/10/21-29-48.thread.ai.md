Thread identity: Question from D. Ben Knoble about listing commits between two revisions that are directly pointed to by branch refs while preserving commit order, focusing on Git plumbing commands and branch filtering techniques.

Core problem: Seeking a canonical Git way to list commits between two revisions where each commit in the output is directly pointed to by a branch ref (refs/heads/*), maintaining topological order. The use case explicitly ignores tags and focuses only on branch references.

Technical approaches explored:
1. Using `join` with sorted rev-list and for-each-ref output - loses topological sequence
2. Post-processing `git name-rev` output to validate branch names - functional but inelegant implementation
3. Combining `rev-list --simplify-by-decoration` with `name-rev` - most elegant solution but potentially unreliable due to documented warnings about --simplify-by-decoration behavior

Key technical considerations:
- Relies on refs/heads/* pattern matching for branch identification
- Expresses concerns about --simplify-by-decoration's reliability for this use case
- Explicitly excludes tag handling from scope
- Focuses on plumbing command solutions rather than porcelain

Current status: Initial question with three proposed approaches, no responses or resolutions yet. The thread appears to be a new discussion seeking feedback on the existence of better alternatives or confirmation of the presented methods.