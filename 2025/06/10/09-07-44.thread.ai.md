Thread identity: Enhancement to `git branch --force` allowing multiple branch moves in a single command, similar to `cp` semantics.

Core problem or goal: Extend `git branch --force` to accept multiple source branches followed by a single destination, enabling bulk branch moves that previously required multiple commands. The change maintains consistency with existing behavior while adding functionality analogous to Unix `cp`.

Current status: Initial patch version proposing the enhancement. No reviews or feedback recorded yet.

Key technical details:
- Modifies `builtin/branch.c` to parse and handle multiple source names before the target branch
- Updates branch creation logic to process each source branch with the same target
- Adds test coverage in `t/t3200-branch.sh` for the multi-branch case
- Follows `cp` command semantics where multiple sources imply the last argument is the destination
- Preserves existing behavior for single-branch moves and other operations

Open questions and loose ends:
- No review feedback yet on the chosen `cp`-like semantics versus alternative interpretations
- Potential need to document the new behavior in the man page more explicitly
- No discussion yet of edge cases (e.g. moving branches that are currently checked out)

Participant positions: Single-author patch with no replies yet. The commit message presents a clear rationale for the design choice but awaits community feedback.

Related work: Builds on existing `git branch --force` functionality without requiring changes to other branch operations like move/copy.