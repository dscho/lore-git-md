This is a final follow-up bugfix in the completed series addressing memory leaks in pack-bitmap.c's boundary traversal logic. The patch corrects a lingering edge case where `roots_bitmap` would leak when `cascade_pseudo_merges_1()` succeeds, complementing earlier fixes that handled failure paths. The change makes `bitmap_free(roots_bitmap)` unconditional in `find_boundary_objects()`.

The email thread reveals this was an oversight in the original implementation (commit 11d45a6e) where the author initially only considered cleanup on failure paths. While Patrick Steinhardt had fixed a parallel leak in `find_objects()` (commit 55e563a), this boundary traversal case slipped through due to insufficient test coverage in t5333. Junio Hamano provides minor grammar corrections to the commit message while questioning how the original logic could have intended to leak on success.

Key technical points:
- Removes conditional `bitmap_free()` check that only cleaned up on failure
- Maintains consistency with the established pattern from earlier fixes
- Leak only occurs when `roots_bitmap` contains pseudo-merges
- Test gap in t5333 meant this wasn't caught by ASAN checks earlier

The change is straightforward and uncontroversial, being the final polish on an already-merged series. It demonstrates the value of thorough code inspection even after initial fixes, as subtle resource management patterns can sometimes miss edge cases. The discussion also highlights how test coverage gaps (here in boundary traversal scenarios) can allow leaks to persist even when similar cases are fixed.