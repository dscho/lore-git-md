Performance optimization and refactoring patch for in-process trailer handling, part of the `--trailer` feature series for `git rebase`. This is the core implementation change that moves trailer appending from an external `git interpret-trailers` process into the main Git process.

The patch introduces `amend_strbuf_with_trailers()` as the new workhorse function that handles all trailer operations in memory, while keeping `amend_file_with_trailers()` as a thin file-based wrapper. The implementation carefully replicates all edge case behaviors of the external process, including:
- Handling of `trailer.ifexists` policies
- Proper placement relative to RFC-2822 separators (`---`)
- Correct positioning before commit message comment lines (from `--verbose`)
- Maintaining CLI-then-config trailer ordering

Key technical details include:
- New `first_comment_pos()` helper to locate `#` comment blocks
- Multi-stage processing pipeline (parse CLI/config, merge lists, format output)
- Memory management with `strbuf` and trailer list operations
- Preservation of all existing behavior while eliminating fork/exec overhead

The changes affect `trailer.c` and `trailer.h`, with comprehensive logic to handle the trailer block parsing, merging, and reinsertion. The implementation appears thorough, with careful attention to edge cases and memory management. This forms the foundation for the subsequent rebase integration patch in the series.