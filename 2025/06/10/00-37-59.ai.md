Bugfix for memory leak in revision walking during merge conflicts, now in its final form after test style adjustments. This v3 patch from Lidong Yan addresses the same core issue as previous versions - freeing the `prune` array allocated during `prepare_show_merge()` in revision.c - but now with corrected test formatting per Junio Hamano's review. The substantive changes remain unchanged from the v2 solution that received technical approval.

The test case continues to exercise `git show --merge` behavior with a three-branch conflict scenario (base/hello/goodbye branches) that creates a realistic merge failure state. Changes from v2 to v3 are purely stylistic:
- Fixed indentation to use tabs consistently
- Corrected redirection spacing (`>` to `>`) in test file creation
- Maintained the comprehensive test coverage that Junio endorsed

The thread shows all technical discussion is settled, and this version addresses the final nitpicks around test formatting. The patch appears ready for inclusion, demonstrating how even simple memory fixes can lead to valuable test infrastructure improvements - in this case establishing a reference test for merge state behavior in revision walking code.