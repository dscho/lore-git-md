This is a test cleanup patch in a series addressing `git config get --type=color` functionality. The patch modernizes test style in t1300-config.sh by moving test expectations into the test bodies rather than defining them outside. 

Patrick Steinhardt notes that most test expectations were being defined outside the test cases, which doesn't match Git's modern test style. The patch moves these inline with the tests themselves, making the test flow more self-contained. Two exceptions remain where expectations are reused across multiple tests - these are left unchanged for now.

The changes are mechanical but thorough, touching 290 lines across the test file. The patch maintains all existing test behavior while improving readability and maintainability. This appears to be a straightforward quality-of-life improvement for the test suite as part of a larger series fixing color handling in `git config`.