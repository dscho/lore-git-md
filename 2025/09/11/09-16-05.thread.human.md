Here's the human-readable thread summary:

This thread introduces a series optimizing how Git's documentation is built and tested in the Meson build system, addressing CI resource waste identified by Gábor Szeder. The three-part series adds a dedicated "docs" build target to compile only documentation (skipping unnecessary binary builds) and improves build system reporting.

The first two patches laid the groundwork: one created the new documentation-only build infrastructure, while the second improved Meson's summary output to show which documentation processor (Asciidoc/Asciidoctor) is being used. The just-arrived third patch completes the effort by modifying the CI script to actually use this new "docs" target during documentation testing, eliminating the previous waste of compiling full binaries when only documentation needed verification.

The changes are build-system focused with no impact on Git's functionality. The series appears well-structured and complete, though it hasn't yet received formal review. When adopted, this should make CI documentation testing more efficient while maintaining the same validation coverage. The changes particularly benefit contributors working on documentation who need to verify their changes frequently.