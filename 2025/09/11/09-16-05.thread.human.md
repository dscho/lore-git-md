This thread focuses on optimizing the documentation build process in Git's Meson configuration to reduce CI resource usage. The series, now at v1 with three patches, introduces documentation-only builds and improves build visibility. Key changes include adding a `docs` build alias, enhancing Meson's summary output, and updating CI scripts to use the new target.

Patrick Steinhardt and Karthik Nayak have been the main discussants, working through implementation details. They've settled on a dual-alias approach (`doc` and `docs`) to satisfy both Make and Meson conventions. A question arose about whether documentation should be included in standard builds by default, but Patrick clarified that the current `meson_options.txt` behavior - where docs are built only when explicitly enabled - is correct as-is.

The technical implementation is now complete, with only two remaining open questions: final approval of the dual-alias naming convention, and any adjustments needed to alias placement after `contrib/`. The series appears ready for maintainer consideration once these last points are resolved.