This is a review comment on a refactoring patch in Patrick Steinhardt's series to move commit graph handling into the ODB source backend. Taylor Blau notes that an explicit NULL check for the commit graph in blame.c can be removed since the subsequent call to get_bloom_filter_settings() already handles that case internally.

The change is straightforward and non-controversial - it's a small cleanup that removes redundant code while maintaining the same behavior. This patch is part of the larger architectural discussion about where commit graph handling should live (repository-wide vs source-specific), but this particular comment focuses just on a minor implementation detail that improves code clarity.

The review suggests the patch is well-reasoned and ready to proceed, though it doesn't engage with the broader design questions being debated in the thread. For readers following the larger discussion, this represents a small but clean step in Patrick's proposed direction.