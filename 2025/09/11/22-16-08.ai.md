This is a review of patch 2/6 in Patrick Steinhardt's series refactoring commit graph handling for the pluggable ODB effort. Taylor Blau confirms the correctness of removing a redundant commit graph existence check in `check_maybe_different_in_bloom_filter()`, agreeing with Patrick's rationale that the subsequent generation number check already covers this case.

The patch removes an explicit NULL check for `r->repo->objects->commit_graph` in the bloom filter path of the revision walker, since the code path already verifies the commit's presence in the graph via generation number lookup. Taylor validates Patrick's performance analysis that any overhead from keeping the check is negligible, as the commit graph data slab lookup would be constant-time when unpopulated.

This continues the mechanical transition of commit graph access patterns from repository-wide to source-specific handling, though the architectural debate about graph ownership (repository vs source) remains unresolved in the broader thread. The change touches revision.c's bloom filter logic but doesn't yet engage with the deeper design questions about where graph metadata should logically live.

Taylor's review indicates this particular cleanup is uncontroversial and likely to proceed regardless of the ongoing architectural discussion, as it simply removes redundant checking without changing behavior. The patch represents an incremental step in the larger refactoring effort while the core team works through the more fundamental questions about graph abstraction boundaries.