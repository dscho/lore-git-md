This is a technical discussion about the implementation details of the signature handling feature in `git fast-import`, specifically addressing concerns about the parsing and storage of multiple signatures.

The email continues the review of patch 2/2 in Christian Couder's series adding signature control to `git fast-import`. Junio Hamano clarifies Patrick Steinhardt's question about handling multiple signatures - while the comment suggests commits might have two signatures (one per hash algorithm), the actual parsing happens in a loop where the function can be called multiple times. Junio notes this maintains compatibility with existing behavior that silently accepts malformed input with duplicate signatures for the same algorithm.

Junio agrees with Patrick's observation that the "up to 2" comment is misplaced and confusing in its current location near the signature storage function rather than the calling loop. He also endorses Patrick's suggestion to change a `BUG()` call to `die()` for unknown hash algorithms, recognizing this as a forward-compatibility issue rather than a programming error that should trigger an assertion.

The discussion reveals subtle aspects of the signature parsing implementation that need refinement:
- The dual-call design for handling multiple signatures
- Comment placement and clarity
- Appropriate error handling for future hash algorithms
- Maintaining consistency with existing behavior for malformed input

This exchange shows the reviewers focusing on edge cases and future-proofing the implementation, with Junio synthesizing Patrick's technical feedback while providing additional context about the design decisions. The conversation remains focused on improving the quality of the current patch rather than reopening architectural questions.