This is a review response in the ongoing commit graph refactoring discussion. Taylor Blau is responding to Patrick Steinhardt's first patch in the series, which removes an explicit commit graph check from the blame code.

The patch removes a redundant NULL check for `sb->repo->objects->commit_graph` in `setup_blame_bloom_data()`, since the subsequent call to `get_bloom_filter_settings()` already handles this case by returning NULL when no commit graph exists. Taylor confirms this simplification is correct, noting the behavior is preserved while making the code slightly cleaner.

This is a minor but sensible cleanup that doesn't appear controversial. The change fits with the series' broader goal of moving commit graph handling into the ODB source abstraction layer, though this particular patch is more about removing redundant checks than advancing the architectural debate. Taylor's positive response suggests this piece of the refactoring is uncontroversial and likely to proceed.