CI/build system patch completing the documentation testing optimization series for Meson builds. This third patch modifies the CI script to use the new "docs" target introduced in patch 1, avoiding unnecessary full project compilation when testing documentation builds.

The change is straightforward - it updates two `meson compile` commands in `ci/test-documentation.sh` to specifically build only documentation targets (`docs`) rather than the entire project. This addresses the resource waste identified by SZEDER Gábor where CI was previously building all binaries during documentation testing. The patch maintains the existing test coverage while reducing compute cycles.

Key technical details:
- Modifies both Asciidoc and Asciidoctor build paths in the test script
- Preserves all existing documentation validation (via `check_docs`)
- Keeps the same build directory cleanup behavior
- Builds on the `doc_targets` infrastructure from patch 1

This completes the series' goal of optimizing documentation build testing in CI. The changes are minimal and focused, with no impact on actual Git functionality. The patch appears well-aligned with the series' purpose and maintains consistency with the earlier infrastructure changes.