This is a build system patch adding installation support for the libsecret credential helper. The patch introduces a simple `install` target to the Makefile for the contrib/credential/libsecret component, which handles credential storage using GNOME's libsecret library.

The new target follows standard Git Makefile conventions, installing the built binary to the gitexecdir location with appropriate permissions (755). This brings the libsecret helper in line with other credential helpers that already support installation via make.

The change is straightforward and appears correctly implemented, though the commit message could benefit from explaining why this functionality is being added now (e.g., was there a specific use case or user request prompting it?). The patch touches only the Makefile and doesn't affect runtime behavior.