This is a patch series addressing bugs in the `git config get --type=color` functionality, with accompanying test cleanups. The series focuses on three main issues:

1. Restoring the ability to parse color values without requiring a config key, fixing a regression in behavior. The implementation special-cases color retrieval to maintain backward compatibility with existing use cases.

2. Preventing the pager from being spawned when printing ANSI color escape sequences, which was causing usability issues when trying to view color output directly in the terminal.

3. Refactoring parts of t1300-config.sh to improve test organization and style, though the author notes more work remains to fully clean up this test file. The test changes move expectations into test bodies and apply style fixes rather than modifying test behavior.

The changes touch both the config command implementation (builtin/config.c) and its test suite, with a significant diffstat showing the reorganization of test cases. The series appears well-scoped, addressing specific pain points in color handling while making incremental test improvements. The commit messages clearly explain each change's purpose, and the cover letter provides good context for reviewers.