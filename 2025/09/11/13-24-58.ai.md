This is the final patch in a 5-part series fixing bugs in `git config get --type=color` functionality, specifically addressing unwanted pager behavior when printing ANSI color codes.

The patch modifies `builtin/config.c` to skip pager setup when the requested output type is COLOR, since color escape sequences are meant for direct terminal output rather than pager consumption. The change is straightforward - it wraps the existing `setup_auto_pager()` call in a conditional checking the output type.

A new test case in `t1300-config.sh` verifies this behavior by setting a dummy pager and confirming the color output isn't intercepted by it. The test uses the test-terminal infrastructure to simulate a TTY environment where pager behavior would normally trigger.

This completes the series' technical changes after earlier patches handled test cleanup, error handling improvements, and color parsing restoration. The implementation appears clean and well-tested, with the test case covering both the pager bypass and correct color output preservation. The commit message clearly explains why pager behavior is undesirable for color output, linking it to practical scripting use cases.

Key technical details for future reference:
- Modified file: `builtin/config.c` (pager setup conditional)
- Test file: `t1300-config.sh` (new TTY test case)
- Test marker: `TTY` (terminal-dependent test)
- Core change: skips pager when `display_opts.type == TYPE_COLOR`
- Related config: `core.pager` (used in test verification)
- Output verification: compares decoded color output against expected `<RED>`