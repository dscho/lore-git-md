This is a code review comment on the refactoring patch in Christian Couder's signature handling series. Patrick Steinhardt suggests two small improvements to the patch that moves signature mode parsing into shared gpg-interface code.

First, he proposes reversing the error handling logic in parse_opt() to make the flow more natural - checking for failure first rather than success. This is a readability suggestion that doesn't change functionality. Second, he notes a missing period in a comment in gpg-interface.h.

The suggestions are minor polish items on an otherwise straightforward refactoring that has already received positive feedback from Junio Hamano. The changes would slightly improve code clarity but don't affect the core functionality being moved from fast-export.c to the shared gpg-interface files. The thread context shows this is part of a larger effort to enable signature handling in fast-import that's currently under review.