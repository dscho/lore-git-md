Thread identity: Patch adding installation support for the libsecret credential helper in contrib/credential/libsecret/Makefile.

Core problem or goal: The patch addresses a missing installation mechanism for the libsecret credential helper, which previously required manual installation. The goal is to provide standard Makefile rules for installing this helper alongside other Git components.

Current status: Initial version implementing the installation support. The patch adds standard Makefile rules using $(DESTDIR) and $(gitexecdir) variables for flexible installation locations.

Key technical details: The Makefile changes create target directories with 755 permissions and install the binary with executable permissions (755). The implementation follows Git's typical installation patterns.

Participant positions: The patch is signed off by its author (name not provided in the summary). No other participants mentioned yet as this appears to be the first version of the patch.

Related work: This builds upon the existing libsecret credential helper implementation in contrib/credential/libsecret/. The change is consistent with how other Git components are installed via Makefile rules.