This is a refactoring patch in Patrick Steinhardt's ongoing series to move commit graph handling into the pluggable ODB infrastructure. Taylor Blau reviews the second patch, which removes a redundant commit graph existence check in the bloom filter path.

The patch eliminates an explicit check for the commit graph in `check_maybe_different_in_bloom_filter()`, since the subsequent generation number check would already fail if no graph exists. Taylor confirms this is a safe simplification, noting that the commit graph data slab lookup would be constant-time anyway when no graph is present.

This appears to be a straightforward cleanup with no behavior change, similar to the first patch in the series that Taylor also approved. The change fits into the broader architectural discussion about where commit graph handling should live, but this particular piece seems uncontroversial as it's purely about removing redundant checks rather than making architectural decisions.

The review suggests this patch is likely to proceed while the larger design questions about repository-wide versus source-specific graph handling continue to be debated elsewhere in the thread.