Test cleanup patch modernizing t1300-config.sh test style.

This is the first of five patches in a series fixing color handling in `git config get`. The patch focuses on test file modernization in t1300-config.sh, moving test expectations into the test bodies rather than defining them outside. This matches Git's current test style guidelines.

The changes are mechanical - each test's expected output is moved from a separate `cat >expect` block into the test body itself. The author notes two exceptions where expectations are reused across multiple tests, leaving those unchanged for now. The diff shows 129 insertions and 161 deletions, reflecting the movement of content rather than functional changes.

This appears to be straightforward test cleanup with no behavior impact. The changes are consistent with Git's test style evolution and should make the tests more self-contained. The patch-specific changes look clean, though reviewers may want to verify the two preserved exceptions are indeed cases where sharing expectations makes sense.

Key technical details:
- Affects only t1300-config.sh test file
- Moves `expect` file creation into test bodies using `cat >expect <<EOF` pattern
- Preserves two cases where expectations are shared between tests
- No test logic or behavior changes
- Part of larger series addressing color handling issues (patches 2-5)