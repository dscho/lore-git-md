Thread identity: Discussion about potential deprecation of `git whatchanged` command, with new `--i-still-use-this` flag requirement to track active usage patterns and identify unanticipated use cases needing special consideration.

Core problem or goal: Evaluate real-world usage of `git whatchanged` to inform deprecation decision, balancing backward compatibility with command-line simplification. The command is redundant with `git log` functionality but has historical significance. Current focus is on identifying edge cases that might complicate removal while providing clear migration paths.

Current status: Deprecation warning added requiring `--i-still-use-this` flag. Stephen Harding reported active usage via oh-my-zsh alias (`gwch` mapping to `git whatchanged -p --abbrev-commit --pretty=medium`). Junio Hamano confirmed the flag's purpose is informational rather than gatekeeping, and provided a complete migration path showing how to replace the alias with `git log --no-merges -p --abbrev-commit` (noting `--pretty=medium` is already `git log` default).

Key technical details: Command provides log output with diff, equivalent to `git log --raw` or `git log -p` with different default formatting. Shell alias usage demonstrates specific formatting preferences that can be replicated with `git log` plus `--no-merges` to match typical `whatchanged` behavior. Maintainer confirms `--pretty=medium` is redundant in migration path as it matches `git log` defaults.

Open questions and loose ends: Whether additional unanticipated use cases will surface before final removal. Need to document recommended migration paths in release notes and deprecation warnings. Shell alias users may need education about equivalent `git log` flags.

Participant positions: Stephen Harding confirmed active usage via shell alias. Kristoffer Haugsbakk reinforced technical redundancy while providing migration advice. Junio Hamano clarified deprecation strategy and validated specific migration path, emphasizing informational rather than blocking purpose of `--i-still-use-this` flag.

Related work: Part of broader effort to clean up legacy commands while respecting established workflows, similar to `git repo-config` -> `git config` transition. Documentation updates may be needed to guide users toward preferred alternatives.