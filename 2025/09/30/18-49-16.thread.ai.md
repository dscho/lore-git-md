Thread identity: Discussion about potential deprecation of `git whatchanged` command, with new `--i-still-use-this` flag requirement to track active usage.

Core problem or goal: Evaluate real-world usage patterns of `git whatchanged` to inform deprecation decision, balancing backward compatibility with command-line simplification. The command is redundant with `git log` functionality but has historical significance.

Current status: Deprecation warning added requiring `--i-still-use-this` flag to continue using `git whatchanged`. Stephen Harding reports active usage via oh-my-zsh alias (`gwch`) mapping to `git whatchanged -p --abbrev-commit --pretty=medium`. Kristoffer Haugsbakk confirms technical redundancy with `git log` and suggests migration path using equivalent `git log` flags, potentially adding `--no-merges` to match common usage patterns.

Key technical details: Command provides log output with diff, equivalent to `git log --raw` or `git log -p` with different default formatting. Shell alias usage demonstrates specific formatting preferences (`-p --abbrev-commit --pretty=medium`) that can be replicated with `git log`. The `--no-merges` flag is identified as potentially important for matching typical `whatchanged` behavior.

Open questions and loose ends: Whether sufficient users will report usage to justify maintaining the code path long-term. Need to handle shell alias migration paths if deprecation proceeds, including consideration of `--no-merges` flag for common use cases.

Participant positions: Stephen Harding confirms active usage via shell alias. Kristoffer Haugsbakk reinforces technical redundancy while providing constructive migration advice. Maintainers (Junio Hamano et al) seeking usage data before final removal.

Related work: General effort to clean up legacy commands and reduce maintenance burden, while respecting established workflows. Similar discussions have occurred around `git repo-config` -> `git config` transition and other deprecated commands.