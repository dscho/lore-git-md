Thread identity: Discussion about potential deprecation of `git whatchanged` command, with new `--i-still-use-this` flag requirement to track active usage.

Core problem or goal: Evaluate real-world usage patterns of `git whatchanged` to inform deprecation decision, balancing backward compatibility with command-line simplification. The command is redundant with `git log` functionality but has historical significance.

Current status: Deprecation warning added requiring `--i-still-use-this` flag to continue using `git whatchanged`. Stephen Harding reports active usage via oh-my-zsh alias (`gwch`) mapping to `git whatchanged -p --abbrev-commit --pretty=medium`.

Key technical details: Command provides log output with diff, equivalent to `git log --raw` or `git log -p` with different default formatting. Shell alias usage pattern demonstrates specific formatting preferences (`-p --abbrev-commit --pretty=medium`) that users may need to migrate.

Open questions and loose ends: Whether sufficient users will report usage to justify maintaining the code path long-term. Need to handle shell alias migration paths if deprecation proceeds.

Participant positions: Stephen Harding confirms active usage via shell alias, providing concrete example of real-world workflow dependency. Maintainers (Junio Hamano et al) seeking usage data before final removal.

Related work: General effort to clean up legacy commands and reduce maintenance burden, while respecting established workflows. Similar discussions have occurred around `git repo-config` -> `git config` transition and other deprecated commands.