Bugfix for `git reflog write` command's config handling, following up on Patrick Steinhardt's earlier diagnosis. This patch implements the straightforward fix to make the command respect local `user.name` and `user.email` configuration values, aligning its behavior with other Git commands.

The change adds a call to `repo_config(repo, git_ident_config, NULL)` in `builtin/reflog.c` to properly parse Git configuration before writing reflog entries. This addresses the core issue where the command was previously only checking environment variables and falling back to system defaults, creating inconsistency with Git's normal config hierarchy handling.

Michael Lohmann's patch includes new test coverage that explicitly verifies the config handling by unsetting environment variables and checking that locally configured user information appears in the reflog entries. The test demonstrates the fix by creating a new reflog entry with custom local config after clearing the environment variables that previously masked this issue.

The patch carries Patrick Steinhardt's co-authored-by line, indicating technical alignment with the earlier analysis. The implementation appears minimal and focused, directly addressing the identified gap while maintaining the command's existing functionality when environment variables are present. The test addition follows the pattern of existing tests in t1421-reflog-write.sh while specifically targeting the config lookup behavior.

This resolves the open question from the thread about basic config handling, though it doesn't yet address potential follow-up work around other configuration-based author/committer overrides. The change is well-contained and unlikely to be controversial given the clear bug status and prior discussion.