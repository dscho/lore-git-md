This is a user response regarding the potential removal of `git whatchanged`. 

Stephen Harding reports encountering the new requirement to use `--i-still-use-this` when running `git whatchanged`, which is being considered for deprecation. He notes that he actively uses the command through an oh-my-zsh alias (`gwch`) that maps to `git whatchanged -p --abbrev-commit --pretty=medium`. His email serves as the requested confirmation that he still uses this command in practice.

The message highlights a real-world usage pattern (shell aliases) that might not have been fully considered in the deprecation discussion. While the email is informal in tone, it provides concrete evidence of `git whatchanged` being actively used in a common development workflow (shell aliases with specific formatting preferences). This could be relevant data point for maintainers evaluating whether to proceed with deprecation or consider alternative approaches to handling legacy commands.