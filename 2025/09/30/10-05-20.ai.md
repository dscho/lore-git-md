Feature implementation for `git replay` with atomic ref updates.

Phillip Wood raises several technical concerns about Siddharth Asthana's patch to make atomic ref updates the default in `git replay`. The key points center around code organization and interface design:

1. Questions the premise that `git update-ref --stdin` isn't atomic, suggesting the current pipeline may already provide atomicity
2. Proposes consolidating the `--output-commands` and `--allow-partial` options into a single `--update-refs` enum argument for clearer semantics
3. Notes code duplication in ref update logic that could be extracted into helper functions
4. Points out overly long lines from deep indentation that hurt readability
5. Requests line wrapping for a long SYNOPSIS line in documentation

The feedback focuses on implementation polish rather than the core functionality, which the thread summary indicates has already been approved. Phillip doesn't challenge the atomic-by-default approach itself, but suggests improvements to make the code more maintainable and the interface more intuitive. The suggestions align with Git's coding standards around clear option naming and avoiding code duplication.

This email represents final polishing feedback on an otherwise-approved feature, with the main technical debate points (atomic default, performance, test coverage) having been settled earlier in the thread. The implementation appears merge-ready pending these style and organization tweaks.