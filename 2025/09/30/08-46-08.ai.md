Feature patch for Rust libgit implementation adding configuration parsing methods. This v4 submission consolidates the previously split series into a single commit implementing boolean, unsigned long, and pathname parsing in the ConfigSet API.

The patch wraps Git's C functions (`git_configset_get_bool()`, `git_configset_get_ulong()`, `git_configset_get_pathname()`) through FFI, maintaining consistency with existing `get_int()` and `get_string()` methods. Key changes from v3 include:
- Added missing symbol exports in `public_symbol_export.[ch]` for boolean handling
- Rebased onto current mainline (bb697214)
- Squashed into single commit as requested
- Removed the separate documentation fix that was handled in another thread

The implementation includes comprehensive tests covering all new methods with various edge cases - boolean parsing handles true/false/yes/no/1/0 values, ulong supports both 32-bit and 64-bit values, and pathname returns PathBuf objects. The changes touch config.rs, test data files, and FFI export plumbing.

This appears ready for review as it addresses previous feedback about symbol exports and commit organization. The technical approach remains consistent with the project's Rust FFI strategy, though reviewers may want to verify the platform-specific handling of `c_ulong` types.