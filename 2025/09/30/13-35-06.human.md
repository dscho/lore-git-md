This is a documentation and cleanup patch in the submodule gitdir path encoding series. Kristoffer Haugsbakk submits a minor correction to Adrian Ratiu's earlier patch that removes the now-obsolete `validate_submodule_git_dir()` function. The function was originally meant to prevent path conflicts in submodule gitdirs, but the new path encoding scheme makes it redundant.

The patch fixes a typo ("purpouse" -> "purpose") in the commit message while maintaining the original technical content. This is part of the larger effort to safely encode submodule paths using URL-style encoding for cross-platform compatibility, which has seen extensive review and iteration across multiple versions of the patch series.

The change is straightforward and uncontroversial, aligning with the series' goal of cleaning up legacy code made obsolete by the new path encoding approach. The thread context shows this is one of many refinements being made to the comprehensive submodule path handling rewrite.