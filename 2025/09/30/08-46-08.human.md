This is a feature patch for the `libgit-rs` Rust implementation, adding three new configuration parsing methods to the ConfigSet API. The patch follows up on previous discussion by addressing feedback and consolidating the changes into a single commit.

The patch adds boolean, unsigned long, and pathname parsing capabilities to match Git's C implementation, wrapping the corresponding C functions through FFI. Key changes in v4 include:
- Added the `get_bool()` method that was previously missing from the FFI exports
- Rebased on the correct base commit
- Squashed into a single commit as requested
- Removed the unrelated README.md documentation fix that was split off per maintainer feedback

The implementation appears thorough, with comprehensive test coverage for all new methods including edge cases. The patch maintains consistency with Git's existing configuration parsing behavior while providing Rust-native types (like `PathBuf` for paths). The changes are focused on the Rust FFI layer and don't modify core Git functionality.

This represents the main technical work from the original series, now properly separated from the documentation fix that was split out earlier in the thread. The patch seems ready for review, having addressed the specific technical feedback about missing symbol exports from previous rounds.