This is a maintainer response in the ongoing `git whatchanged` deprecation discussion. Junio Hamano clarifies the purpose of the `--i-still-use-this` flag requirement, explaining it's not about proving usage but identifying unanticipated use cases that might need special consideration. He provides a direct migration path for Stephen Harding's oh-my-zsh alias, showing how to replace `git whatchanged -p --abbrev-commit --pretty=medium` with an equivalent `git log` command that adds `--no-merges` to match typical `whatchanged` behavior. The response confirms that `--pretty=medium` is already the default for `git log`, making that flag redundant in the new alias. This moves the thread forward by both acknowledging the reported usage pattern and demonstrating how it can be cleanly transitioned to `git log`, while maintaining the deprecation warning's original intent of gathering information about edge cases rather than blocking removal outright. The technical details about flag equivalency and defaults help validate the migration path being proposed.