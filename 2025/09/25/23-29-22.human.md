This is a refactoring patch that's part of a larger series introducing a new `git repo stats` command. It's the first of seven patches in version 3 of the series.

The patch renames the `repo_info()` function to `cmd_repo_info()` to follow Git's convention of prefixing subcommand entry points with `cmd_`. This is a straightforward change that affects only the function name and its single call site in the `cmd_repo()` function. The change is mechanical and uncontroversial, serving as groundwork for the more substantial functionality that follows in later patches of the series.

The patch is well-formed with a clear commit message and minimal, focused changes. It's part of a well-reviewed series that has gone through multiple iterations, with this particular change being a standard cleanup that aligns with Git's codebase conventions.