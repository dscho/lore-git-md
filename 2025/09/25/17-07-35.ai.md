This is the second patch in a three-part series fixing an inconsistency in `git format-patch`'s handling of Git notes between the main commit message and range-diff output for single commits. The patch refactors the notes argument passing by adding a new `rdiff_log_arg` member to the `rev_info` struct.

The technical change moves the notes arguments from a local variable in `builtin/log.c` to a persistent struct member in `rev_info`, preparing for the fix in patch 3/3. The patch adds the new `rdiff_log_arg` strvec to `revision.h` and updates `builtin/log.c` to use it instead of the local `log_arg` variable. This includes proper initialization in `REV_INFO_INIT` and cleanup in `cmd_format_patch`.

The commit message has been significantly improved from v1 based on Junio's feedback, now better explaining the context and motivation. The patch maintains the same functionality while setting up the infrastructure needed to fix the single-commit case. The changes are cleanly contained to `builtin/log.c` and `revision.h`, with the actual behavior fix coming in the next patch.

This is a well-structured preparatory change that follows the established pattern of storing range-diff related information in `rev_info`. The commit message clearly explains both the technical change and its purpose in the larger series, making it easy to review. The patch appears ready for inclusion.