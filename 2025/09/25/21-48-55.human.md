This is a brief follow-up in the SHA-1/SHA-256 interoperability series, specifically addressing patch 6/9 which adds `--show-object-format=compat` to `git rev-parse`. 

Patrick Steinhardt noticed that a test in this patch was incorrectly marked with the `RUST` prerequisite when it didn't actually need Rust support. Brian m. carlson confirms this observation and states they'll move the prerequisite fix to part 2 of the series in the next version. 

This is a minor correction to the test infrastructure that doesn't affect the core functionality of the patch, which allows querying the compatibility hash algorithm of a repository. The exchange shows careful attention to test prerequisites in this complex series about hash algorithm interoperability.