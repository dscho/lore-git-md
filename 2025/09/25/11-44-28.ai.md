This is a documentation patch fixing markdown formatting in the PO (translation) README. It's the first patch in a 2-part series that primarily focuses on adding boolean config handling to the Rust libgit implementation, though this particular patch is unrelated to that main effort.

The patch removes unnecessary backslash escaping from underscores in language code examples (like `ll_CC` and `zh_CN`) in `po/README.md`. The change is purely cosmetic - the escaping wasn't needed in Markdown and made the examples harder to read. The fix is straightforward and correct, touching only 3 lines in the documentation.

While this is a valid improvement, it's worth noting this documentation fix is unrelated to the main series' goal of implementing boolean config handling in Rust. The thread summary suggests this might be better as a standalone patch, though the change is small and harmless enough that including it here isn't problematic. The patch includes proper sign-off and follows standard Git contribution format.