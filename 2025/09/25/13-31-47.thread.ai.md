Bug report about `git merge --no-ff` failing with "stash failed" error, originally reported by Nikolay Shustov. The error occurs during Git's internal stash handling during merges, with diagnostic attachments provided (standard bug report and trace2 logs). Key missing details remain the Git version, operating system, and specific reproduction steps. The working theory points to problems in Git's stash handling during merges manifesting in certain environments, as the same merge scenario works correctly when the repository is hosted on Linux, suggesting platform-specific factors like filesystem behavior or permissions.

Johannes Schindelin (Dscho) has now joined the investigation, requesting additional diagnostics by suggesting running `git stash create` separately to isolate whether the failure occurs in the stash operation itself or in how it's called during merge. This approach aims to get more detailed error messages beyond the current "fatal: stash failed" output. The thread remains in information-gathering phase with no proposed fixes yet, still lacking key details like exact Git version and reproduction steps that would enable deeper analysis. The platform-specific nature of the issue is increasingly evident but not yet precisely characterized.