This is patch 9/10 in Adrian Ratiu's hook subsystem refactoring series, converting the post-update hook to use the modern `hook.h` interface. The patch follows the established pattern from previous conversions in the series, replacing direct process management with the `run_hooks_opt` infrastructure.

The changes simplify `run_update_post_hook()` by:
- Removing manual hook process setup in favor of `run_hooks_opt`
- Replacing direct sideband handling with the `hook_output_to_sideband` callback
- Maintaining the same ref filtering logic while using the new interface
- Preserving the existing behavior for both sideband and non-sideband cases

Key technical points:
- The patch touches only `builtin/receive-pack.c`
- Uses the `consume_sideband` capability introduced earlier in the series
- Keeps the same ref filtering logic (skipping failed/nonexistent refs)
- Maintains the sideband protocol support through the callback
- Reduces code by ~60% (19 lines removed, 6 added)

This continues the series' pattern of methodically converting each hook type while validating the interface in different scenarios. The post-update hook conversion demonstrates the flexibility of the new system to handle another hook type with minimal changes while maintaining all existing functionality. The patch appears straightforward and well-contained, following the established patterns from previous conversions in the series.