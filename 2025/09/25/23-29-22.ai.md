This is a refactoring patch that's part of the larger `git repo stats` series (1/7). It renames the `repo_info()` function to `cmd_repo_info()` to follow Git's convention of prefixing subcommand entrypoints with `cmd_`. The change is purely mechanical, updating both the function definition and its reference in the subcommand options array.

The patch is straightforward with no behavior changes - it touches only 3 lines in builtin/repo.c, modifying the function signature and its registration as a subcommand handler. This appears to be foundational work for the subsequent patches in the series that will build out the new repository statistics functionality. The change aligns with Git's established patterns for command implementations, making the codebase more consistent.

As the first patch in the series, it sets up the naming convention that will presumably be used for other subcommands (like the upcoming `stats` command) while maintaining backward compatibility with the existing `info` subcommand. The patch is cleanly implemented with no obvious issues, though its real value will become apparent as the rest of the series builds on this foundation.