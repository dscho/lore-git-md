This is a brief follow-up in the SHA-1/SHA-256 interoperability series, specifically regarding patch 6/9 which introduced the `rev-parse --show-object-format=compat` plumbing command. The email addresses a minor test infrastructure issue raised by Patrick Steinhardt's review.

The key point is that the `RUST` test prerequisite was incorrectly applied to a test case in t1500-rev-parse.sh that doesn't actually require Rust functionality. Brian m. carlson acknowledges this and states the fix will be to move this prerequisite requirement up to part 2 of the series in the v2 revision. This appears to be a straightforward cleanup of test prerequisites with no deeper technical implications.

The exchange confirms that the core functionality of `--show-object-format=compat` (which shows the compatibility hash algorithm for repositories in compatibility mode) remains unchanged and uncontroversial. This is a minor implementation detail in the broader context of the series' work on hash algorithm interoperability.