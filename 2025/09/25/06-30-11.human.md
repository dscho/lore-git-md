This is the final patch in the Rust infrastructure series, focusing on CI modernization. It enables Rust compilation in the breaking-changes CI jobs to verify the build infrastructure works as intended. The changes are straightforward - adding `cargo` to dependency installations and setting Rust-related flags in the test environment.

The patch completes the series' CI modernization by ensuring the breaking-changes testing path (which validates Git's transition to Rust) actually builds and tests the Rust components. This was the last remaining piece after previous patches refactored the build system and added Rust support infrastructure.

The implementation is minimal and focused, just adding Rust tooling to the CI environments and enabling it for the breaking-changes test cases. This matches the series' phased approach where Rust support will be optional (2.52), then default (2.53), and finally mandatory (3.0). The patch demonstrates the CI system is now ready to validate Rust integration as the series progresses toward mainline.