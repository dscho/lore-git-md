This is a feature patch series for the libgit-rs Rust library, adding boolean configuration handling. The main patch introduces a `get_bool()` method to the ConfigSet module, providing a more convenient way to work with boolean config values compared to manually parsing string values. The implementation includes proper error handling and comes with unit tests covering edge cases.

The series also includes a minor documentation fix for the translation team's README (fixing escaped underscores in Markdown), though this appears unrelated to the main feature. The changes are focused and well-contained, touching only the Rust library code and test files. The commit message clearly explains the motivation and implementation approach, though it doesn't reference any specific use cases or pain points that led to this addition.

For Rust contributors working with Git configuration, this looks like a straightforward quality-of-life improvement that follows existing patterns in the codebase. The testing approach suggests the author has considered edge cases, though reviewers may want to verify the exact parsing rules match Git's C implementation.