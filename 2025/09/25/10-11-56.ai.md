This is a follow-up in the ongoing discussion about modifying `git rebase -i`'s fixup behavior with `-C` flag. Phillip Wood responds to Mathias Rav's acknowledgment of alternative solutions, reinforcing the emerging consensus that the default fixup behavior should remain unchanged while leaving room for new opt-in functionality.

Phillip explicitly supports adding a new option to `fixup` that would copy the author information, distinguishing this from changing the default behavior. He references Oswald Buddenhagen's historical thread link as evidence that this request has come up before, while maintaining the position that the core fixup semantics should stay as-is. The email positions itself as a bridge between Mathias' practical needs and Junio Hamano's design principles, suggesting a path forward through an optional flag rather than a behavioral change.

Key technical details carried forward:
- Current fixup behavior preserves original author by design
- Historical precedent supports keeping this default
- New `--use-author` flag remains a potential compromise solution
- Mathias' custom script (`x f` invoking shell script) serves as fallback
- Documentation updates still needed for any new flag

This moves the thread closer to resolution by:
1. Confirming rejection of the original behavioral change
2. Endorsing an alternative implementation path (new flag)
3. Acknowledging the validity of the use case while respecting design principles
4. Leaving the exact flag name/semantics open for further discussion

The discussion appears to be converging on maintaining current fixup semantics while exploring an opt-in mechanism for author preservation, with Phillip's position aligning closely with Junio's earlier guidance.