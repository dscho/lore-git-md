This is a follow-up discussion in the `git repo stats` series, specifically addressing implementation details of the table formatting in patch 3/6. Justin Tobler responds to Patrick Steinhardt's review feedback on the human-readable output format implementation.

Key technical discussion points:
- The `stats_table_add()` function currently takes a single format string, with Patrick noting varargs could be added for future flexibility though not currently needed. Justin considers adding varargs in the next version.
- Patrick identifies that `stats_table_print()` can mark its table parameter as `const` since it doesn't modify the table contents. Justin confirms this change will be made.
- Patrick questions the current implementation's use of a `strbuf` intermediary before printing to stdout. Justin explains this was a holdover from earlier code and plans to switch to direct `printf` output in the next version.

The exchange shows the series nearing completion, with these being relatively minor implementation refinements to the table formatting logic. The core functionality remains unchanged - this is purely about code organization and style improvements in the human-readable output path. The discussion indicates the feature is technically sound, with reviewers now focusing on polishing the implementation details to match Git's coding conventions.