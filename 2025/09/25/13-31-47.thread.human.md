This thread investigates a bug where `git merge --no-ff` fails with a "stash failed" error, originally reported by Nikolay Shustov. The error appears to be platform-specific, as the same merge scenario works correctly when the repository is hosted on Linux, suggesting filesystem behavior or permissions may be involved. Johannes Schindelin has now joined the investigation, requesting more diagnostic information by suggesting running `git stash create` separately to isolate whether the issue lies in the stash operation itself or in how it's called during merging. The thread still lacks key details like the Git version and exact reproduction steps, keeping the investigation in an early information-gathering phase. No fixes have been proposed yet, but the Linux vs. other environment comparison provides an important diagnostic clue.