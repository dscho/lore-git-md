This is a technical discussion email in the ongoing Rust infrastructure thread, raising questions about modern C language features as alternatives to Rust's safety guarantees.

Eric Wong questions whether newer C features could provide similar safety benefits to Rust without requiring a new language. He lists several modern/experimental C capabilities: checked arithmetic (`stdckdint.h`), buffer overflow protection (`__counted_by__`), resource management (`__cleanup__`), concurrency (userspace RCU), format string checking, and parser generation tools. He particularly highlights `__cleanup__` and `__counted_by__` as most impactful, while acknowledging his general dislike for verbose AOT languages and concerns about Rust's compilation speed on older hardware.

The email introduces new technical considerations to the Rust transition discussion, suggesting C's evolving capabilities might reduce the safety gap with Rust. It touches on compilation performance tradeoffs and potential impact on contributor accessibility. While not opposing the Rust effort directly, it provides a counterpoint by enumerating C's modern safety features that could be leveraged instead of adopting Rust. The concerns about build times and hardware requirements add a practical dimension to the technical debate.

Key technical points raised:
- Specific C23/clang/gcc features (`stdckdint.h`, `__counted_by__`, `__cleanup__`) as Rust alternatives
- Existing tooling (Valgrind, ASAN, TSAN) for C safety
- Performance concerns about Rust compilation speed
- Impact on contributor base due to hardware requirements

This represents a new thread direction questioning fundamental assumptions about Rust's necessity by pointing to C's evolving safety features, while the main Rust series continues progressing through build system and governance refinements. The email doesn't directly conflict with the technical implementation in the patches but raises broader language choice considerations.