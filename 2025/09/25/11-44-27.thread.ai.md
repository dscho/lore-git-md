Thread identity: Feature patch series (2 parts) adding boolean config handling to Rust libgit implementation (`contrib/libgit-rs`), authored by an unspecified contributor. The first patch contains an unrelated documentation fix for PO (translation) README markdown formatting.

Core problem: Implementing proper boolean configuration value parsing (`true/false`, `yes/no`, `on/off`) in the Rust libgit implementation. The current v1 approach implements parsing directly in Rust (`libgit-rs`), while Phillip Wood suggests this should instead wrap Git's native `git_configset_get_bool()` through the FFI layer (`libgit-sys`) first. The documentation patch fixes unnecessary backslash escaping of underscores in language code examples (`ll_CC`, `zh_CN`) in `po/README.md`.

Technical details:
- Current implementation adds 24 lines to `contrib/libgit-rs/src/config.rs`
- Implements `get_bool()` method in `ConfigSet` module with case-insensitive parsing
- Uses `CString`/`CStr` for FFI safety and proper memory management
- Includes test coverage via new test config file (`config3`)
- Documentation patch touches 3 lines in `po/README.md`
- Both patches properly signed-off

Current status: v1 series posted with two reviews received. Core boolean parsing logic appears correct but architectural approach questioned. Documentation change is correct but may be better as separate submission.

Key technical feedback:
- Chris Torek suggests improving panic message phrasing in `get_bool()` method (minor style issue)
- Phillip Wood raises architectural concern about reimplementing boolean parsing in Rust rather than wrapping Git's C implementation via FFI (significant design issue)
- Wood notes current Rust implementation would parse values like "00" or "100" differently from Git's C code
- Feedback emphasizes `libgit-rs` should primarily provide safe wrappers around existing C functionality

Open questions:
- Whether author will restructure implementation to use FFI-wrapped `git_configset_get_bool()`
- Whether to split documentation fix into separate series
- Edge cases in boolean parsing behavior between Rust and C implementations
- How to maintain FFI safety while moving implementation to `libgit-sys` layer

Implementation locations:
- Current: `contrib/libgit-rs/src/config.rs` (Rust implementation)
- Proposed: Should move core logic to `libgit-sys` FFI layer
- Documentation: `po/README.md`

Participant positions:
- Chris Torek provided constructive review on error message phrasing
- Phillip Wood raised architectural concern about proper layer separation
- Wood's feedback indicates the feature is welcome if implemented according to project guidelines

Related considerations: The documentation change creates a minor organizational question about patch series scope. The boolean config implementation now faces a significant architectural decision - whether to proceed with Rust-native parsing or restructure to wrap Git's C implementation. Wood's feedback suggests the latter approach would be more aligned with project design principles, particularly regarding consistent behavior with Git's core. The series will likely need a v2 that reorganizes the implementation across both `libgit-sys` and `libgit-rs` layers while maintaining the existing test coverage and FFI safety guarantees.