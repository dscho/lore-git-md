This is a discussion email in the ongoing Rust infrastructure thread, raising concerns about adopting Rust by pointing to modern C features that could address some of the same safety goals. Eric Wong questions whether newer C features (checked arithmetic, buffer overflow guards, auto-cleanup attributes) might make Rust adoption unnecessary, and expresses concerns about Rust's compilation speed being prohibitive for some contributors.

The email lists several specific C23 and compiler-specific features that provide safety improvements similar to Rust's guarantees: `stdckdint.h` for arithmetic checks, `__counted_by__` for buffer bounds, and `__cleanup__` for resource management. Wong also mentions existing tools like Valgrind and sanitizers as alternatives to Rust's compile-time safety.

A notable concern raised is that Rust's slow compilation could exclude contributors without high-end hardware, potentially biasing development toward corporate interests. The tone is skeptical of Rust's necessity given C's evolving safety features, though Wong acknowledges Git's existing C code quality issues. This adds a new dimension to the Rust adoption discussion by questioning whether the safety benefits justify the toolchain costs when C alternatives exist.