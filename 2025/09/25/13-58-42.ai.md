Review feedback on the `git repo stats` output formats (patch 5/6).

Patrick Steinhardt provides final review comments on the keyvalue and NUL-delimited output format implementations in the `git repo stats` series. The discussion focuses on documentation clarity and implementation details that were addressed in previous rounds.

Key points:
- Confirms the human-readable table format should explicitly state it's the default (Justin plans to clarify wording)
- Agrees to keep the `core.quotePath` note in keyvalue format documentation as future-proofing, despite current lack of quoting implementation
- Approves the refactored switch statement for format selection as cleaner than previous version
- Notes the unconventional newline delimiter in NUL format (matching `git repo info` behavior) but suggests any changes belong in a follow-up series

The exchange shows consensus on all substantive points, with only minor documentation wording left to improve. This represents the final review feedback for this patch in the series, which has now addressed all technical concerns through multiple iterations.