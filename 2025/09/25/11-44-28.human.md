Documentation patch fixing Markdown formatting in the PO (translation) README. This is the first patch in a 2-part series, though it's unrelated to the main series topic of adding boolean config handling to the Rust libgit implementation.

The patch removes unnecessary backslash escapes from underscores in language code examples (like `ll_CC` and `zh_CN`) in the translation documentation. The change is purely cosmetic - the escaped underscores weren't needed in Markdown formatting and made the examples harder to read. The fix is straightforward and clearly correct, touching only the documentation lines where these examples appear.

While this is a minor change, it's a sensible cleanup that makes the documentation more readable. The patch could arguably have been submitted separately from the Rust config work, but bundling it here is harmless. The change is well-contained (3 lines modified) and properly signed off.