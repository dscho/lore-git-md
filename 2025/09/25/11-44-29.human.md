Feature patch adding boolean config parsing to Git's Rust library implementation. This is the second patch in a two-part series (the first being an unrelated documentation fix).

The patch implements a `get_bool()` method for the Rust `ConfigSet` type, matching Git's standard boolean parsing behavior (accepting true/yes/on/1 and false/no/off/0). The implementation builds on existing string parsing infrastructure, adding case-insensitive comparison of the values. It includes basic test coverage with a new test config file.

The change is straightforward and appears well-contained, adding 24 lines to the Rust config module. The implementation looks correct at first glance, though reviewers will want to verify it precisely matches Git's C behavior. The patch is properly signed-off and includes tests, suggesting it's production-ready pending review.

This continues the effort to build out Git's experimental Rust library with core functionality. While not part of main Git yet, such incremental improvements help validate the Rust implementation approach. The patch seems uncontroversial but may prompt discussion about exact boolean parsing semantics.