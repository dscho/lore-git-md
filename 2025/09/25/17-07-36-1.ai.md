Bugfix for `git format-patch` notes display consistency in single-commit range-diffs. This is the final patch in a v2 series addressing an inconsistency where `--[no-]notes` options weren't properly propagated to range-diff output for single commits.

The patch completes the fix by utilizing the new `rdiff_log_arg` struct member (added in patch 2/3) to properly pass notes arguments from `log-tree.c` to the range-diff machinery. The change ensures that single-commit range-diffs respect the same notes display settings as the main commit message output, matching the behavior already working for cover letters and multi-commit cases.

Key technical details:
- Modifies `show_diff_of_diff` in `log-tree.c` to pass the `rdiff_log_arg` to range-diff options
- Adds a new test case in `t/t3206-range-diff.sh` specifically for single-commit scenarios
- Test verifies correct behavior with both custom notes` and suppression cases
-no-notes`
- Maintains existing test coverage for multi-commit and cover letter cases
- Follows modern test practices with `test_grep` and `test_when_finished`

The series appears technically sound with all prior feedback addressed, including improved variable naming and proper initialization. This final patch ties together the infrastructure changes from the first two patches to fully resolve the original inconsistency. The test coverage is thorough, verifying both the positive case (custom notes display) and negative case (notes suppression).