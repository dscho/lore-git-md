**Classification**: Clarification on revision syntax numbering

This email from Junio C Hamano serves as a final technical correction in the settled discussion about Git's numbering conventions. It specifically addresses a minor inaccuracy in merge parent notation that was raised earlier in the thread.

The exchange confirms that merge parent references should use caret notation (`HEAD^1`, `HEAD^2`) rather than the tilde notation (`HEAD~2`, `HEAD~3`) that was initially mentioned. Junio acknowledges Ben Knoble's correction, clarifying that `HEAD^1` refers to the first parent (equivalent to `HEAD~1`), while higher numbers indicate subsequent parents in merge commits.

This follows the thread's established conclusion that Git consistently uses 1-based indexing, with the caret notation being the proper way to reference merge parents generically, not just in octopus merge scenarios. The clarification doesn't introduce new information but provides precise terminology that aligns with Git's documentation and existing behavior.

The email reinforces that the thread's core conclusions remain unchanged: Git's numbering is intentionally 1-based for both date formatting and revision syntax, with no changes needed to the codebase. This final exchange serves to ensure the technical record is accurate for future reference.