Bug report about `git merge --no-ff` failing with "stash failed" error.

Nikolay Shustov reports encountering a fatal "stash failed" error when attempting to run `git merge --no-ff` on their branch. The error occurs during what should be a standard merge operation, suggesting something is going wrong with Git's internal handling of the merge process. The reporter has helpfully included three diagnostic attachments: a standard bug report, trace2 logs, and trace2 logs with events enabled - though the actual contents of these files aren't provided in the email.

This appears to be a bug report rather than a patch submission. The error message suggests the failure occurs during Git's attempt to stash changes as part of the merge process, which could indicate either a problem with the working directory state or a bug in Git's stash handling during merges. The inclusion of detailed trace logs suggests the reporter has taken steps to help diagnose the issue, though without seeing the actual log contents it's hard to assess the root cause. The report lacks details about the Git version, operating system, or reproduction steps beyond the basic command.