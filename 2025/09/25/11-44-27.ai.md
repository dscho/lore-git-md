This is a feature patch series (2 parts) adding boolean config handling to the Rust libgit implementation. The cover letter introduces a new `get_bool()` method for the `ConfigSet` module in libgit-rs, which provides proper boolean parsing and error handling for configuration values. The implementation includes unit tests covering edge cases.

The first patch appears to be an unrelated documentation fix for escaped underscores in the PO (translation) README. The second patch implements the core functionality, adding 24 lines to `config.rs` with the new method and test data. The changes are focused on the Rust implementation and don't affect Git's core C code.

For AI context: The series adds `get_bool()` to `ConfigSet` in `contrib/libgit-rs/src/config.rs`, handling boolean config values with proper parsing (true/false, yes/no, on/off etc.). Tests are added in the same file and use a new test config file (`config3`). The PO README fix suggests some internationalization context but is likely incidental. The Rust implementation appears straightforward with test coverage, though reviewers may want to verify the boolean parsing logic matches Git's C implementation behavior exactly.