This is a follow-up in the `diff-no-index` buffer overflow fix thread, addressing a memory leak issue identified during final review. Jacob Keller responds to Junio Hamano's discovery that the error path in `diff_no_index()` was leaking two `strbuf` structs (`ps_match1` and `ps_match2`) when SANITIZE=leak testing was enabled. The fix adds corresponding `strbuf_release()` calls at the function's cleanup label, maintaining the original safety improvements while ensuring proper resource management.

The email confirms the leak exists exactly as speculated and provides the minimal diff to resolve it. Jacob explicitly authorizes squashing this change into the existing patch, indicating no further revisions are anticipated unless other issues emerge. This represents the final polish on a series that already addressed both buffer overflow risks (through `strbuf` conversion) and now completes the memory safety picture with proper cleanup. The thread appears ready for merging with maintainer approval, having progressed from initial bug report through implementation to final validation.