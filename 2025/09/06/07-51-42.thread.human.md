This thread has evolved from an initial proposal for a `git refs optimize` subcommand into a comprehensive refactoring of Git's reference storage optimization architecture. The discussion, guided by Junio Hamano's architectural input, has produced a shared implementation that now powers both the traditional `git pack-refs` and the new `git refs optimize` command.

Meet Soni's latest patch (5/9 in their GSoC series) factors out the core packing logic into a shared library (`pack-refs.c`), making `git pack-refs` a thin wrapper around this new infrastructure. The refactoring carefully preserves all existing behavior while enabling code reuse with the upcoming subcommand. The changes are mechanically clean - mostly moving code between files with minimal logic modifications - and include proper documentation via the new `PACK_REFS_OPTS` macro.

The implementation now shows all major pieces in place: the refs API has been extended with optimization support, both files and reftable backends implement the required functionality, and the command infrastructure shares core logic between implementations. A comprehensive 430+ line test suite verifies behavioral consistency across both commands. With this refactoring complete, the path is clear for the remaining patches to introduce the new subcommand while maintaining the stability of the existing `pack-refs` functionality.