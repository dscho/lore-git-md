Bugfix report for a regression in tag fetching behavior. This is a thread root email reporting a specific issue with `git fetch --tags` in Git 2.51.0. The reporter, David Bohman, observes that while the command shows which tags it would update in a bare repository, the tags are not actually being added - a behavior that worked correctly in Git 2.50.1. The report is concise but provides the key details needed to investigate: the exact command showing the issue (`git fetch --tags`), the type of repository affected (bare), and the version where the regression appeared (2.51.0). The reporter has already done basic troubleshooting by verifying the behavior works correctly in the previous version. This appears to be a legitimate regression report that would warrant investigation by the fetch/tag maintenance team. The next steps would likely involve reproducing the issue and bisecting to find the specific commit that introduced the regression.