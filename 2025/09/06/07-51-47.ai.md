This is the final patch in a 5-part series introducing the `git refs optimize` subcommand as a modern replacement for `git pack-refs`. The patch adds a new test script `t/t1463-refs-optimize.sh` that verifies the new command behaves identically to the old one by reusing the shared test library created in patch 4.

The test script is intentionally minimal - it just sets `pack_refs='refs optimize'` and sources the shared test library `t/pack-refs-tests.sh`. This ensures the new subcommand passes all the same tests as `git pack-refs`, confirming it's a true drop-in replacement. The test checks that branch semantics remain unchanged after optimization.

The patch also updates `t/meson.build` to include the new test script. This completes the test coverage for the new subcommand while maintaining all existing test cases through the shared library approach. The implementation follows the established pattern of the series - careful refactoring to share code and tests between old and new interfaces.

With this patch, the series appears complete and well-tested, having received mentor reviews from Patrick Steinhardt and shejialuo. The test strategy is particularly thorough, reusing 430+ lines of existing test logic through the shared library while adding minimal new test code.