Bug report: Regression in `git fetch --tags` behavior in Git 2.51.0.

David Bohman reports that `git fetch --tags` in a bare repository shows tags being updated during the fetch operation, but the tags don't actually get added to the repository. This worked correctly in Git 2.50.1, making this a regression in the latest release.

The report is concise but provides the key details needed to investigate: the exact command that fails (`git fetch --tags`), the type of repository where it occurs (bare), the observed vs expected behavior (tags listed but not stored), and confirmation that this worked in the previous version. The regression nature makes this higher priority since it represents a broken workflow that previously worked.

No additional context is provided about the repository setup or tag types involved, so further investigation would be needed to reproduce and diagnose the issue. The report would benefit from including the output of `git --version` to confirm the exact 2.51.0 build being used, and ideally steps to reproduce with a test repository.