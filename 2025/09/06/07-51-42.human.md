This is a feature patch series introducing a new `git refs optimize` subcommand as part of the ongoing effort to consolidate ref-related commands under the `git refs` namespace. The new subcommand serves as a modern alias for the existing `git pack-refs` functionality.

The series (5 patches) factors out the core packing logic from `builtin/pack-refs.c` into a shared library (`pack-refs.c`), updates documentation to share common options, and adds the new subcommand. It includes thorough test coverage, with existing tests being refactored into a shared test script and new tests added specifically for the optimize subcommand.

The changes in v2 focus on improving the implementation by properly moving the core logic to a shared library (rather than just a helper) and updating documentation wording. The patch series appears well-structured with clean separation of concerns and comprehensive test coverage, suggesting it's a serious contribution to the refs command consolidation effort.