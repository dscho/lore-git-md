This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and eliminate global variables. It's the 18th of 49 patches in the `tb/incremental-midx-part-3` effort.

The patch addresses compiler warnings that appear under `DEVELOPER=1` mode by changing two variable types from `int` to `size_t` where they interact with size-related values (a hash algorithm's hex size and a string list count). While the current code is functionally fine, the type mismatches trigger signed/unsigned comparison warnings. The changes are straightforward and maintain the same behavior while making the types match their actual usage context.

This is a minor cleanup that prepares for moving the `existing_packs` API to a separate compilation unit, where the warnings would otherwise require suppressing with `DISABLE_SIGN_COMPARE_WARNINGS`. The changes touch two functions that were previously refactored in this series: `existing_packs_mark_for_deletion_1()` (which handles pack deletion marking) and `existing_packs_collect()` (which gathers pack information).

The patch follows the series' pattern of methodical, incremental improvements while maintaining existing functionality. It demonstrates attention to code hygiene details that become important when splitting code into modules, though it doesn't introduce any architectural changes itself. The commit message clearly explains the motivation and tradeoffs of making these type changes.