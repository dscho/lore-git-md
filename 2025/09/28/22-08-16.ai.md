This is the 17/49 patch in Taylor Blau's major refactoring series for `builtin/repack.c`, continuing the modularization effort while removing global state dependencies. The patch focuses on making the pack removal functionality more self-contained by passing the `packdir` parameter explicitly rather than relying on module-scoped state.

The change modifies two key functions - `remove_redundant_packs_1()` and `existing_packs_remove_redundant()` - to accept a `packdir` parameter, which specifies where packfiles are written and scanned for redundant packs. This prepares for moving these functions to their own compilation unit in future patches, following the series' pattern of encapsulating functionality.

The patch maintains all existing behavior while making the implicit dependency on the pack directory location explicit. This continues the series' architectural improvements by:
1. Removing another case of shared state between functions
2. Making the pack removal logic more self-contained
3. Following the established pattern of parameterizing functions for modularization

The changes are mechanical but important for the larger goal of splitting `builtin/repack.c` into focused modules. The patch updates all call sites (two direct calls plus their helper) to pass the packdir parameter, which we can see the function will eventually operate on both regular and cruft packs. This builds on patch 16's relocation of `remove_redundant_pack()` while maintaining the series' consistent approach to eliminating global state.