This is the 36th patch in Taylor Blau's 49-part series refactoring `builtin/repack.c` to support incremental MIDX-based repacking while removing global variables. The patch inlines the `remove_redundant_bitmaps()` function into `write_midx_included_packs()`, eliminating the need for callers to track which packs were included in the MIDX.

The change moves the bitmap cleanup logic directly into the MIDX writing function, where it logically belongs. This is a preparatory step for making the caller completely oblivious to the set of packs included in the MIDX in future patches. The diff shows the straightforward movement of the bitmap removal code into `write_midx_included_packs()`, with the function now handling both MIDX writing and associated cleanup.

This continues the series' pattern of methodical refactoring - first relocating code (as seen in patch 35), then integrating it more tightly (here in patch 36). The change maintains all existing functionality while improving encapsulation. The patch follows established patterns from earlier in the series, using the same error handling approach with `goto done` that appears elsewhere in the codebase.

Key technical details:
- The `remove_redundant_bitmaps()` call moves from `cmd_repack()` to `write_midx_included_packs()`
- Error handling is unified through the `done` label
- The change affects the interaction between MIDX writing and bitmap cleanup
- No functional changes - pure refactoring maintaining existing behavior
- Prepares for future patches that will further abstract pack tracking

The patch aligns with the series' dual goals of modularization and global variable removal, specifically targeting the MIDX-related functionality. It demonstrates the incremental, behavior-preserving approach that has characterized the entire refactoring effort.