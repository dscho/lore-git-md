This is a user experience note in the ongoing `git whatchanged` deprecation discussion. Neal Miller shares a personal migration path, confirming that their specific `git whatchanged` alias (`gwch`) using `-p --abbrev-commit --pretty=medium` can be directly replaced with `git log` using the same flags, without needing the `--raw` or `--no-merges` options discussed earlier in the thread. This provides a concrete data point showing that some existing `whatchanged` use cases don't require the full substitution pattern being debated, particularly when the `-p` (patch) output option is used.

The email reinforces the thread's core premise that `git log` can serve as a drop-in replacement, while demonstrating that the exact substitution pattern may be simpler than initially proposed for certain configurations. Neal's case shows that some existing aliases may only need to change the command name while keeping all other flags identical, though this appears specific to patch-style output rather than the default `whatchanged` behavior being discussed in the broader thread. The message serves as a real-world validation of the deprecation path's flexibility, though it doesn't engage with the ongoing debate about optimal default flags for the standard `whatchanged` case.