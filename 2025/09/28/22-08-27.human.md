Refactoring patch in the ongoing repack.c modularization series. This is part 20 of 49 in Taylor Blau's effort to modernize Git's repacking machinery while removing global variables.

The patch simplifies naming conventions around temporary pack file handling. It renames `struct generated_pack_data` to the more concise `struct generated_pack`, and updates related function names to follow the convention of prefixing with the struct name (e.g., `generated_pack_populate()` instead of `populate_pack_exts()`). The changes are purely mechanical - no behavior is altered.

This continues the series' pattern of improving code organization and naming while breaking down the monolithic repack.c file. The changes appear straightforward and well-contained, affecting only internal pack file tracking during repacks. The commit message clearly explains the motivation for the renaming (reducing redundancy and awkward function names).

As with previous patches in the series, this maintains the careful separation of concerns while progressing toward the larger goal of modular repacking infrastructure. The changes are limited to about 30 lines of straightforward renaming, suggesting this is one of the simpler patches in the comprehensive refactoring effort.