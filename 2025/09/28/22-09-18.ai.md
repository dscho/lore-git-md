Refactoring patch in the tb/incremental-midx-part-3 series, converting MIDX pack name storage from raw arrays to string_list. This is patch 33/49 in Taylor Blau's ongoing effort to modularize `builtin/repack.c` while eliminating global variables.

The change replaces manual array management of MIDX pack names with a `string_list`, simplifying the code by using standard string list operations rather than direct memory allocation and iteration. The key modifications are:
- Replaces `char** midx_pack_names` and `size_t midx_pack_names_nr` with `struct string_list midx_pack_names`
- Updates `midx_has_unknown_packs()` and `midx_included_packs()` to use string_list iteration
- Removes manual memory management in favor of string_list's built-in cleanup

This continues the series' pattern of replacing low-level data structures with Git's standard utility types while maintaining behavioral equivalence. The change reduces parameter passing (eliminating the separate count parameter) and leverages existing string_list functionality for sorting and memory management. The patch appears well-contained, touching only the MIDX pack name handling logic while leaving surrounding repack behavior unchanged.

The commit message clearly explains the motivation - this simplifies the code path introduced in an earlier commit (5ee86c273b) that tracks packs from the original MIDX during repack operations. The change aligns with the series' broader goals of code modernization and reducing manual memory management.