Refactoring patch in the `tb/incremental-midx-part-3` series, continuing the modularization of `builtin/repack.c` with specific focus on promisor object handling. This is patch 26/49 in the ongoing effort to modernize Git's repack machinery while eliminating global variables.

The patch relocates `repack_promisor_objects()` and its helper `write_oid()` from `builtin/repack.c` to a new `repack-promisor.c` module, following the established pattern of:
1. Parameterizing functions (done in previous patches)
2. Extracting them to dedicated modules
3. Maintaining existing functionality

Key technical aspects:
- The moved function handles repacking of promisor objects (lazy-fetch candidates)
- Creates new `.promisor` files alongside packed objects
- Uses `packtmp` directory parameter added in patch 25
- Preserves two `NEEDSWORK` comments about potential delta optimization and promisor file content handling
- Maintains all error handling and safety checks
- Follows series convention of declaring in `repack.h` while implementing in dedicated module

The extraction:
- Removes 95 lines from `builtin/repack.c`
- Adds 102 lines to new `repack-promisor.c`
- Updates build files (`Makefile`, `meson.build`) to include new module
- Keeps identical function signature in `repack.h`

This continues the series' systematic approach to breaking down the monolithic repack implementation while maintaining strict behavioral equivalence. The change is well-contained and follows established patterns from prior patches in the series.