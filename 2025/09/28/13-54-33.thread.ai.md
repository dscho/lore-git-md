Git thread summary (AI mode):

This thread discusses a two-patch series improving gitk's window geometry persistence across sessions, combining work from Johannes Sixt (Hannes) and Michael Rappazzo. The current iteration (v3) restores complete window geometry (position + size) for both the main window and Tags/Heads windows, addressing a limitation caused by an obsolete Cygwin Tcl/Tk 8.4.1 workaround (fixed since 2011).

Core problem: The series solves two related issues - 1) restoring main window geometry handling (reverting b9bee11526ec) and 2) adding Tags/Heads window persistence. The implementation ensures windows remember their size, position, and relative spatial relationships across sessions and monitor changes.

Technical details: Patch 1 reverts to direct geometry restoration from `geometry(main)`, replacing complex size-only code. Patch 2 implements Tags/Heads persistence by storing geometry as `geometry(showrefs)` and adds four Tcl procedures: `save_showrefs_geometry`, `restore_showrefs_geometry`, `setup_showrefs_geometry_tracking`, and `destroy_showrefs`. The solution uses delayed `Map` event binding to avoid race conditions and replaces direct window destruction with geometry-saving wrappers. Changes are confined to gitk's Tcl code (90 lines modified total) with no core Git impact.

Current status: The series is complete with no outstanding technical objections. Patch 1 (foundational revert) is signed-off by Johannes Sixt with credit to Mark Levedahl. Patch 2 adopts Hannes's pre-existing `manage_showrefs_geometry` approach after three iterations, making it more robust than the initial solution. The patches are interdependent for correct platform behavior.

Participant positions: Johannes Sixt (original window handling author and sign-off on revert), Michael Rappazzo (Tags/Heads persistence author), Mark Levedahl (credited for assistance). The series addresses a long-standing UX issue where users previously had to manually reposition windows each session.