This is patch 36/49 in Taylor Blau's series refactoring `builtin/repack.c` to modularize the code and prepare for incremental MIDX-based repacking. The patch inlines the `remove_redundant_bitmaps()` function into `write_midx_included_packs()`, eliminating the need for callers to track which packs were included in the MIDX.

The change is a refactoring step that moves bitmap cleanup logic closer to where the MIDX is written, making the series' eventual goal of fully encapsulating MIDX-related operations more straightforward. The diff shows the function being moved from the caller (`cmd_repack`) into `write_midx_included_packs()`, with careful handling of the error return path preserved.

This continues the series' pattern of methodical restructuring - first moving code (as seen in patch 35), then inlining it (here in patch 36). The commit message clearly explains the motivation: to reduce the burden on callers by keeping MIDX-related operations self-contained. The change maintains all existing behavior while making the codebase more maintainable for the planned MIDX improvements.