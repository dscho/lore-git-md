This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and remove global variables. Patch 27/49 standardizes the naming of functions that operate on `struct pack_geometry` by prefixing them all with "pack_geometry_". 

The changes are purely mechanical renames affecting 6 functions: `geometry_pack_weight` becomes `pack_geometry_weight`, `geometry_cmp` becomes `pack_geometry_cmp`, `init_pack_geometry` becomes `pack_geometry_init`, `split_pack_geometry` becomes `pack_geometry_split`, `get_preferred_pack` becomes `pack_geometry_preferred_pack`, `geometry_remove_redundant_packs` becomes `pack_geometry_remove_redundant`, and `free_pack_geometry` becomes `pack_geometry_release`. All call sites are updated to match.

This continues the series' pattern of improving code organization and naming consistency while maintaining identical behavior. The changes make it clearer which functions are part of the pack geometry subsystem and follow Git's typical naming conventions for operations on a struct. The patch reduces cognitive overhead by making the function-scope relationship more explicit through naming.

The patch touches only `builtin/repack.c`, modifying 52 lines (26 insertions, 26 deletions) with no functional changes. It builds on previous work that introduced the `struct pack_geometry` and its supporting functions, now giving them a more systematic naming scheme. This is part of the broader effort to prepare the repack machinery for incremental MIDX functionality while removing `the_repository` usage.