This is part 25 of Taylor Blau's major refactoring series to modularize `builtin/repack.c` and eliminate global variables. The patch continues the pattern of converting functions to explicit parameter passing, this time modifying `repack_promisor_objects()` to take the `packtmp` directory as a parameter rather than accessing it as a static variable.

The change is straightforward - it's one of many small steps in the series to prepare functions for eventual relocation into dedicated modules. The patch adds the parameter while maintaining all existing functionality for handling promisor packfiles. This follows the same approach seen in recent patches (like patch 23 which parameterized `packdir`/`packtmp` in `generated_pack_install()`).

As with previous patches in the series, this maintains all safety checks and atomic rename behavior while making the code more modular. The commit message indicates this is preparation for moving the function in a future commit, continuing the incremental extraction methodology that has characterized the series so far.