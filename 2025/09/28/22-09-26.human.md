This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and prepare for incremental MIDX-based repacking. It's patch 35 of 49 in the series.

The change is straightforward: it reorders two functions (`remove_redundant_bitmaps()` and `write_midx_included_packs()`) to avoid needing a forward declaration. The commit message explains this is preparation for inlining `remove_redundant_bitmaps()` into `write_midx_included_packs()` in the next patch.

The patch maintains identical behavior while cleaning up the code organization. It's a small but logical step in the larger refactoring effort to break down the monolithic repack.c file into more modular components. The diff shows the functions being moved verbatim with no logic changes - just their order in the file is swapped. This continues the series' pattern of methodical, behavior-preserving changes to improve code structure.