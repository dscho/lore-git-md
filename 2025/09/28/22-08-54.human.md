This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and remove global variables. It's the 27th of 49 patches in the tb/incremental-midx-part-3 effort.

The patch standardizes function names related to `struct pack_geometry` by adding a consistent "pack_geometry_" prefix to all functions that operate on this structure. This includes renaming functions like `geometry_cmp` to `pack_geometry_cmp` and `get_preferred_pack` to `pack_geometry_preferred_pack`. The changes are purely mechanical - no behavior is altered, just the naming convention is made more consistent.

This continues the series' pattern of improving code organization and naming while preparing for future MIDX (multi-pack index) functionality. The patch follows the established methodology of making focused, incremental changes that improve code structure without introducing new behavior. All 26 function renames in this patch follow the same straightforward pattern, making it a clean, low-risk change that moves the codebase toward better modularization.