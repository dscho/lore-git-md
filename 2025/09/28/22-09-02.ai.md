This is the 29th patch in Taylor Blau's 49-part series refactoring `builtin/repack.c` to remove global variables and prepare for incremental MIDX-based repacking. The patch continues the series' pattern of parameterizing functions to eliminate implicit dependencies.

The change passes the `packdir` variable directly to `pack_geometry_remove_redundant()` rather than having it access the global `packdir` variable. This follows the same approach as the previous patch (28/49) which parameterized `pack_kept_objects`. The modification is small (3 lines added, 2 removed) and maintains identical behavior while making the function's dependencies explicit.

Key technical details:
- Updates the function signature and its single call site
- Continues the series' methodology of:
  1. Identifying static/global dependencies
  2. Converting them to parameters
  3. Maintaining behavior
- Prepares the function for potential future relocation to a new module
- Follows the established pattern seen in earlier patches for `remove_redundant_pack()` and other geometry-related operations

The patch is part of the broader effort to:
- Modularize the repack codebase
- Remove `the_repository` and other globals
- Standardize function naming (note the `pack_geometry_` prefix)
- Prepare for incremental MIDX functionality

No new tests or behavior changes are introduced - this is purely a refactoring step in the incremental series. The change is consistent with the series' demonstrated quality and careful approach to maintaining behavior while improving code structure.