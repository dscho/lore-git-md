This is part of the ongoing `the_repository` and `the_hash_algo` removal effort within the repack refactoring series. The patch continues Taylor Blau's systematic elimination of global hash algorithm references in `builtin/repack.c`, now targeting the promisor object repacking functionality.

The change modifies a single line in `repack_promisor_objects()` to use the repository's `hash_algo` field instead of the global `the_hash_algo`. This maintains consistency with previous patches in the series that have been converting similar direct global variable accesses to use repository-specific values. The change is straightforward - replacing one hash algorithm reference with another while keeping the same validation logic for hex object ID lines.

This patch follows the established pattern seen in earlier parts of the series, where repository pointers are being threaded through call chains to provide context for operations that previously relied on globals. The modification affects promisor pack handling, which was already updated in patch 5/49 to accept a repository parameter. No behavior changes are introduced - this is purely about removing global state dependencies.

The change is small and focused, continuing the series' incremental approach to refactoring while maintaining existing functionality. It demonstrates the same careful attention to detail seen in previous patches, with the commit message clearly explaining both the what and why of the change. This patch appears to be a straightforward continuation of the established refactoring work.