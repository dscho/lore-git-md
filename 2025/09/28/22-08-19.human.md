This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and eliminate global variables. It's the 18th of 49 patches in the `tb/incremental-midx-part-3` effort.

The patch addresses two minor type mismatch warnings that appear when compiling with `DEVELOPER=1`. It changes the type declarations of two variables (`hexsz` and `i`) from `int` to `size_t` to match the types they're compared against, avoiding unnecessary casts. While the current code works fine, the author notes this cleanup will prevent warnings when moving these functions to a separate compilation unit later in the series.

This is a small but thoughtful change that maintains the series' pattern of careful, warning-free refactoring. The patch follows the established approach of making incremental improvements while preparing for larger architectural changes to come. It doesn't change any functionality, but keeps the codebase clean as the repack machinery continues to be modernized.