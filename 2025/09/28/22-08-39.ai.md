Refactoring patch in the `tb/incremental-midx-part-3` series, continuing the modularization of `builtin/repack.c` with explicit parameter passing.

This patch extends the pattern established in patch 22 by modifying `generated_pack_install()` to accept explicit `packdir` and `packtmp` parameters rather than accessing them as static variables. The change affects one call site where pack installation occurs after repacking. The author notes the potential for parameter confusion due to three consecutive `const char*` parameters but considers a dedicated struct too heavyweight for this change.

Key technical points:
- Follows the series' systematic elimination of static variables
- Maintains the same pack installation behavior while preparing for module relocation
- Continues the `generated_pack_` naming convention established in patch 20
- Builds on pack installation logic extracted in patch 21
- Preserves atomic rename safety and error handling

The patch is a straightforward continuation of the series' architectural work, with minimal risk of behavioral changes. The author's aside about parameter ordering hints at a potential future refactoring opportunity, though not pursued here to maintain incremental progress. This change fits cleanly into the series' broader goals of modularization and global variable elimination.