This is patch 31/49 in Taylor Blau's series refactoring `builtin/repack.c` to modularize the code and remove global variables, part of the larger effort to enable incremental MIDX-based repacking.

The patch extracts the reference snapshotting functionality from `builtin/repack.c` into a new `repack-midx.c` module. This code is responsible for taking a snapshot of repository references when writing a MIDX (multi-pack index), which helps avoid race conditions during bitmap generation by ensuring the MIDX contains all referenced commits. The snapshot is passed to `git multi-pack-index write` via the `--refs-snapshot` option.

The change follows the series' established pattern: identifying cohesive functionality (reference snapshotting), moving it to a new module with proper API exposure in `repack.h`, and maintaining identical behavior while removing 68 lines from the main repack file. The patch adds 77 lines to the new module and updates build files accordingly.

This continues the series' systematic approach to modularization while maintaining the existing behavior. The extracted functionality remains focused on MIDX-related operations, keeping the architectural separation clean as the repack codebase is restructured.