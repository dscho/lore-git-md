This is the 17/49 patch in Taylor Blau's major refactoring of `builtin/repack.c`, continuing the effort to modularize the repack machinery while removing global variables. The patch focuses on preparing the redundant pack removal functionality for eventual relocation to a separate module.

The change makes the `packdir` parameter explicit in the redundant pack removal functions, rather than relying on the static variable. This is a straightforward mechanical change that:
- Adds `packdir` parameter to `remove_redundant_packs_1()` and `existing_packs_remove_redundant()`
- Updates the single call site in `cmd_repack()` to pass the directory
- Maintains all existing behavior while making the dependencies clearer

This follows the series' established pattern of gradually making implicit dependencies explicit before moving code between modules. The patch is small (14 lines changed) and focused, with no behavior changes - just preparation for future modularization. It continues the high-quality, incremental approach seen throughout this refactoring series.