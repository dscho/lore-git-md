This is the 44th patch in Taylor Blau's 49-part series refactoring `builtin/repack.c` to modularize the code and remove global variables, as part of the incremental MIDX-based repacking effort (tb/incremental-midx-part-3).

The patch modifies `finish_pack_objects_cmd()` to take a `write_pack_opts` struct parameter instead of individual fields, continuing the series' pattern of encapsulating configuration. Key changes include:
- Replaces explicit `local` parameter with `write_pack_opts_is_local()` helper from previous patch
- Accesses `packtmp` through `opts->packtmp` instead of static variable
- Updates all three call sites to pass the new parameter
- Initializes a new `write_pack_opts` struct in `cmd_repack()` for the direct call case

The changes maintain identical behavior while further decoupling the function from its callers, preparing it for eventual relocation to the repack.h API. The patch follows the series' established pattern of small, focused extractions that incrementally improve modularity while preserving functionality. No new tests are added as this is purely a refactoring with no behavior change.