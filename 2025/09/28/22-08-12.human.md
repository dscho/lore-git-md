This is patch 16 in Taylor Blau's 49-part series refactoring `builtin/repack.c` into modular components while removing global variables. The series is part of preparing for incremental MIDX-based repacking.

The patch moves the `remove_redundant_pack()` function from `builtin/repack.c` to the new `repack.[ch]` module, renaming it to `repack_remove_redundant_pack()` to follow the subsystem's naming conventions. The function handles cleanup of redundant packfiles, including MIDX-related operations when present. This is a mechanical change that:
- Extracts the function to make it available to other repack-related code
- Maintains identical behavior while improving code organization
- Follows the series' pattern of moving functionality out of the monolithic builtin

The change is straightforward and well-contained, affecting only pack cleanup logic. It continues the series' systematic approach to modularization while maintaining all existing functionality. The patch appears to be a clean, incremental step in the larger refactoring effort.