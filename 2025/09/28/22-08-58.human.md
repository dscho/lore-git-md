This is part 28 of Taylor Blau's 49-part series refactoring `builtin/repack.c` to remove global variables and prepare for incremental MIDX-based repacking. The patch continues the modularization effort by passing the `pack_kept_objects` parameter directly to `pack_geometry_init()` rather than relying on the static variable.

The change is straightforward - it modifies the function signature and updates the single call site to pass the additional parameter. This is part of the broader effort to make the pack geometry functionality more self-contained and ready to be moved to its own compilation unit in a future patch.

The patch follows the series' established pattern of methodically parameterizing dependencies before extracting functionality into separate modules. It maintains the same behavior while making the code's data flow more explicit. This continues the high-quality, incremental approach seen throughout the series, with clear commit messages and careful attention to detail.