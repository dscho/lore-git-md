This is patch 19 in Taylor Blau's 49-part series refactoring `builtin/repack.c` to support incremental MIDX-based repacking while removing global variables. The patch moves the `existing_packs` API - which tracks pack states (kept, non-kept, cruft) during repacking - from the builtin to the new `repack.c` module.

The change follows prior work that isolated this functionality, making it self-contained enough to extract. The patch:
- Moves 173 lines from `builtin/repack.c` to `repack.c`
- Adds 35 lines of declarations to `repack.h`
- Makes several helper functions static since they're now module-internal
- Maintains all existing behavior while cleaning up the builtin's API surface

This continues the series' pattern of methodically modularizing repack functionality while eliminating globals. The change appears well-contained and mechanical, with no behavioral impact. The commit message clearly explains the motivation and scope. This is another solid step in the larger refactoring effort.