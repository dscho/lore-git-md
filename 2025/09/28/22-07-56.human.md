This is the 12th patch in a 49-part series refactoring `builtin/repack.c` to eliminate global variables and prepare for incremental MIDX-based repacking. The patch introduces a new `repack_config_ctx` struct to pass both cruft and non-cruft pack object arguments to the configuration callback.

The change is motivated by the upcoming removal of the `delta_base_offset` static variable, which will instead become part of the `pack_objects_args` struct. The new context struct allows the configuration callback to access both sets of pack arguments when processing config values.

This continues the series' pattern of methodically converting global state into explicitly passed parameters. The change is small and focused, maintaining the existing behavior while progressing the larger refactoring goals. The patch appears well-contained with a clear commit message explaining both the immediate change and its context within the broader series.