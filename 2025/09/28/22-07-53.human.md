This is part 11 of a 49-part series refactoring `builtin/repack.c` into modular components while removing `the_repository` global usage, preparing for future incremental MIDX functionality. The patch creates a new compilation unit by extracting `struct pack_objects_args` and its cleanup function from `builtin/repack.c` into new `repack.[ch]` files. This is the first step in breaking up the monolithic 1700+ line file into focused components.

The change is straightforward - it moves the struct definition and `pack_objects_args_release()` function verbatim into the new files while updating build systems (Makefile and meson.build) to include the new compilation unit. The struct contains configuration options for `git pack-objects` like window size, depth, thread count, and filter options.

This appears to be well-executed mechanical refactoring with no behavior changes. The commit message clearly lays out the long-term goal of modularizing repack functionality and positions this as the first of many steps. The patch follows the series' established pattern of small, focused changes that incrementally restructure the codebase while maintaining existing functionality.