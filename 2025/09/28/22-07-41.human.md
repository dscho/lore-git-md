Global variable removal in repack series. This is the eighth patch in Taylor Blau's 49-part series refactoring `builtin/repack.c` to eliminate global variables while modularizing the code for future MIDX-based repacking features.

The change is straightforward - it replaces a direct reference to `the_hash_algo` with `repo->hash_algo` in the promisor object repacking code. This continues the series' pattern of passing repository state explicitly rather than relying on globals. The patch affects a validation check that ensures pack-objects output contains full hex object IDs.

As with previous patches in the series, this maintains existing behavior while progressing the technical goal of removing global variable dependencies. The change is small and focused, following the established pattern of the larger refactoring effort. No new tests are added since this doesn't modify functionality, just internal implementation details.

This continues the steady march through the repack codebase to modernize access patterns in preparation for future incremental MIDX work. The series remains focused and mechanical at this stage, with each patch tackling a small, well-defined scope.