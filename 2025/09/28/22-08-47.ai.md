This is part 25 of Taylor Blau's 49-part series refactoring `builtin/repack.c` to remove global variables and prepare for incremental MIDX-based repacking. The patch continues the series' pattern of parameterizing functions to eliminate implicit dependencies on global state.

The change modifies `repack_promisor_objects()` to accept the `packtmp` directory path as an explicit parameter rather than relying on global access. This follows the same approach used in previous patches (notably patch 23 which parameterized `packdir`/`packtmp` in `generated_pack_install()`). The modification is minimal - just adding the parameter and updating the single call site.

The patch maintains all existing functionality while making the function's dependencies more explicit, which is a key goal of the overall series. This prepares for future relocation of `repack_promisor_objects()` to a dedicated module, as was done with other repack components in earlier patches. The change is consistent with the series' methodology of incremental extraction with minimal behavioral changes.

Technical details show this continues the pattern established in patches 22-24: identifying functions that will be moved to new modules, parameterizing their dependencies first, then relocating them in subsequent patches. The `packtmp` parameter being added here is the same temporary pack directory path used elsewhere in the repack machinery.