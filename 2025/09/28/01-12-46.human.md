This is a user experience note in the ongoing `git whatchanged` deprecation discussion. Neal Miller shares a personal migration path, confirming that their specific `git whatchanged` alias (`gwch` with `-p --abbrev-commit --pretty=medium` options) can be directly replaced with `git log` using the same flags. This serves as a real-world validation of the thread's core premise that `git log` can serve as a drop-in replacement, though Neal's case doesn't involve the `--raw`/`--no-merges` flags under discussion elsewhere in the thread.

The email reinforces that different users may need slightly different `git log` invocations depending on their original `git whatchanged` usage patterns. While not introducing new technical arguments, it provides a concrete example of the transition working as expected for at least one user's needs. The tone is positive and solution-oriented, focusing on practical migration rather than debating the deprecation itself.