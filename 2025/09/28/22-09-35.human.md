This is the 37th patch in Taylor Blau's 49-part series refactoring `builtin/repack.c` to support incremental MIDX-based repacking while removing global variables. The patch continues the series' pattern of encapsulating functionality into configuration structs.

The change replaces multiple individual parameters passed to `midx_included_packs()` with a single `repack_write_midx_opts` struct pointer. This reduces parameter passing and prepares for inlining this function into `write_midx_included_packs()` in a future patch. The struct gains a new `existing` field to track pack sets, keeping this detail hidden from callers.

The patch maintains the series' careful approach - it's a pure refactoring with no behavior changes, just improved encapsulation. This continues the progression toward making `cmd_repack()` unaware of MIDX implementation details. The change is small (13 lines modified) but fits into the larger strategy of methodically converting direct parameter passing to structured configuration.

As with previous patches in the series, this demonstrates consistent attention to code organization and global variable elimination while preserving functionality. The work remains focused on preparatory refactoring before introducing new MIDX features.