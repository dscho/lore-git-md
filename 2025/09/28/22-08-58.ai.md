This is part 28 of Taylor Blau's 49-part series refactoring `builtin/repack.c` to modularize the repack machinery while removing global variables, in preparation for incremental MIDX-based repacking.

The patch continues the systematic parameterization of functions by passing the `pack_kept_objects` variable directly to `pack_geometry_init()` rather than accessing it as a static variable. This change prepares for moving the pack geometry functionality to its own compilation unit, following the pattern established in earlier patches of the series.

The modification is straightforward - it adds the `pack_kept_objects` parameter to `pack_geometry_init()` and updates its single call site. The diff shows 4 lines added and 2 removed, with no behavior changes. This follows the series' methodology of incrementally parameterizing functions before relocating them to new modules.

The change builds on patch 27's standardization of pack geometry function names and continues the broader effort to eliminate implicit dependencies on global state. As with previous patches in the series, this maintains identical behavior while improving code organization. The patch appears well-contained and follows the established pattern of the refactoring series.