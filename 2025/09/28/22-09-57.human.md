This is part 41 of Taylor Blau's 49-part series refactoring `builtin/repack.c` to eliminate global variables and prepare for incremental MIDX-based repacking. The patch continues the systematic encapsulation pattern by applying the new `write_pack_opts` struct to the `write_cruft_pack()` function, following the same approach used for `write_filtered_pack()` in the previous patch.

The changes simplify the function signature by bundling five common pack-writing parameters into the `write_pack_opts` struct, reducing parameter passing and duplication. The patch maintains identical behavior while making the code more maintainable - a typical example of the series' consistent methodology where each step identifies cohesive functionality, parameterizes dependencies, and relocates logic with minimal changes.

This is a straightforward structural improvement that builds on the foundation laid in patch 40. The changes are mechanical but meaningful, reducing the function's parameter count from 6 to 3 while keeping all existing functionality. The series continues to demonstrate careful, incremental progress toward the larger goal of modularizing repack.c and eliminating global state.