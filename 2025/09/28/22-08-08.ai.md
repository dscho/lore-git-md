This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c` and remove global variables. It's the 15th patch of a 49-part series that combines `the_repository`/`the_hash_algo` elimination with repack machinery modernization.

The patch standardizes function naming conventions for operations on the `struct existing_packs` type, following documentation established in commit 541204aabe. It renames 12 functions to use consistent prefixes: `existing_packs_` for operations on the collection and `existing_pack_` for individual pack operations. Key renamed functions include pack marking/deletion utilities (`existing_pack_mark_for_deletion`, `existing_packs_mark_for_deletion`), retention checks (`existing_pack_is_retained`), and collection operations (`existing_packs_collect`, `existing_packs_remove_redundant`).

The changes are purely mechanical - no behavior is altered, just naming. This continues the series' pattern of methodical, incremental refactoring while maintaining exact functionality. The patch touches 66 lines across `builtin/repack.c`, updating both declarations and all call sites. It builds on prior work that moved pack handling logic out of the builtin and into dedicated modules, with this change further clarifying the code organization through consistent naming.