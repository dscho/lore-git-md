Continuing the refactoring of `builtin/repack.c` to eliminate `the_hash_algo` global usage. This ninth patch in the 49-part series modifies `finish_pack_objects_cmd()` to accept a `git_hash_algo` parameter rather than using the global directly, maintaining consistency with the series' approach to repository-specific hash algorithm handling.

The change affects multiple call sites that now pass the hash algorithm from their repository context. Key modifications include:
- Adding `algop` parameter to `finish_pack_objects_cmd()`
- Updating the object ID length check to use the passed algorithm
- Modifying all callers (`write_filtered_pack()`, `write_cruft_pack()`, and `cmd_repack()`) to pass their repository's hash algorithm

The patch follows the established pattern of the series, where each change maintains existing behavior while removing global state dependencies. The diff shows the mechanical nature of these conversions, with straightforward parameter passing through the call chain. This continues the preparatory work for both incremental MIDX functionality and broader `the_repository`/`the_hash_algo` removal efforts.