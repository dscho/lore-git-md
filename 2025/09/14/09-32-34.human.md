This is a refactoring patch that adjusts how Git runs alias subprocesses to better align with the documented API in `run-command.h`.

The change removes an explicit `strvec_push(&cmd.args, "git")` when executing aliases and instead sets `cmd.git_cmd = 1`, which is the preferred way to indicate that the command should be run as a Git subprocess. The commit message notes this follows the API guidance from `run-command.h`, though it doesn't explain what practical difference this makes.

The patch appears to be a straightforward cleanup with no behavior change intended. The notes section shows the author's investigation into similar code patterns elsewhere in the codebase, including a case in `upload-pack.c` where explicitly pushing "git" is intentional. The change is small (just two lines modified) and focused on a single file (`git.c`).

While the patch seems technically sound, the motivation could be clearer - it's not immediately obvious why this change is beneficial beyond API conformance. The author's research suggests they've considered edge cases, but the patch lacks test coverage to demonstrate no regressions in alias handling.