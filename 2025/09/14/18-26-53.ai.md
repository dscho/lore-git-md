Bugfix follow-up confirming resolution of `safe.directory` server-side configuration issue. Marc-Jano Knopp verifies that the problem was indeed server-side, as Carlo Arenas Belón suggested, and that running the Git command directly on the server via SSH works correctly. This confirms the earlier analysis that the security check triggering the "dubious ownership" error occurs during server-side repository access rather than client-side clone operations.

The key technical detail here is that the solution involves configuring `safe.directory` on the server (`my.server`) where Git executes the security check, not on the client machine. The reporter's successful test demonstrates that the repository access works when the Git process runs with proper permissions on the server, bypassing the client-side configuration attempts that previously failed.

This email effectively closes the thread by confirming the root cause and solution, though it doesn't address why the server-side Git process initially rejected the properly permissioned shared repository. The resolution path is now clear: server administrators need to configure `safe.directory` exceptions where the security checks actually occur during remote operations.