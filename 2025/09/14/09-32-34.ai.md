This is a refactoring patch targeting Git's alias execution mechanism. The change modifies how Git runs commands when they are invoked via aliases, aligning with the documented API in `run-command.h`.

The patch removes an explicit `strvec_push(&cmd.args, "git")` call and instead sets `cmd.git_cmd = 1`, which is the preferred way to indicate that the command should be treated as a Git subprocess. The commit message notes this follows the API guidance from `run-command.h`, though it doesn't explain why the current approach needs changing beyond that.

The change appears to be a straightforward cleanup rather than fixing any specific bug. The notes section indicates the author did some investigation into similar patterns elsewhere in the codebase, finding one case in `upload-pack.c` where explicitly pushing "git" is intentional (with corresponding `git_cmd` setting), and another historical reference in trace2 code that wasn't relevant.

The patch touches the core command dispatch logic in `git.c`, specifically the `run_argv` function that handles alias expansion. The modification is small and focused - just two lines changed, one removed and one added. There are no tests included with the patch, which might be worth noting since this affects fundamental command execution behavior. The change doesn't appear to affect user-facing behavior, just internal implementation details.