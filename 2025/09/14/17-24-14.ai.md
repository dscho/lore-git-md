This is a follow-up discussion in the command deprecation framework series, specifically addressing the alias shadowing implementation (patch 2/7). Kristoffer Haugsbakk responds to Jeff King's suggestions about improving recursive alias error reporting.

The key technical points under discussion are:
1. The current implementation logs intermediate alias expansions (e.g., "'cmd1' is aliased to 'cmd2'") during recursion detection, which Jeff proposed could be simplified by checking `new_argv[0]` earlier in `handle_alias()`
2. Jeff suggested potentially removing the special "recursive alias" check entirely in favor of the more general loop detection output with `==>` markers
3. Kristoffer found that the informational messages only appear in `-h` help cases and don't significantly impact normal usage, so decided to keep the current behavior in v5 with minor documentation adjustments

The exchange reveals that while the core functionality (recursion detection in `handle_alias()`) is maintainer-approved and working correctly, there are still minor polish discussions about error message formatting. Kristoffer opts for minimal changes in v5, treating the output format discussion as a footnote rather than a core issue.

This email doesn't introduce new technical changes but confirms the stability of the alias shadowing implementation while documenting the rationale for keeping the current error reporting approach. The thread remains focused on final polish before integration, with all major technical concerns already resolved.