This is a follow-up discussion in the thread about improving worktree safety checks and advice messages. Kristoffer Haugsbakk confirms that using `bin-wrappers/git` resolves the testing issue where advice messages weren't appearing when testing via Git aliases. 

The exchange clarifies an implementation detail: when testing changes to Git's advice system, developers need to use the locally-built `bin-wrappers/git` rather than system Git aliases, since aliases execute the system Git binary which won't have the new advice messages. This doesn't require any changes to the patch series but provides important context for testing methodology.

The thread remains focused on finalizing the polish items for the worktree safety check improvements, with this message resolving one of the testing-related questions that came up during review. The core functionality of the patch series (adding better advice messages for worktree conflicts) remains unchanged.