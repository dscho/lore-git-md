This is part of the command deprecation framework series, specifically focusing on alias handling improvements. The patch moves the alias loop detection logic from `run_argv()` into `handle_alias()`, which is a preparatory step for allowing deprecated builtins to be shadowed by aliases. The change includes renaming of variables for clarity (`cmd_list` becomes `expanded_aliases`) and ensures proper handling of alias loops that might involve deprecated commands.

The patch maintains the same functionality but reorganizes it to better support the upcoming alias shadowing feature. It's a clean refactoring that follows the series' architectural direction established in previous versions, with Jeff King's original patch as the basis. The change is well-motivated in the commit message, explaining both the technical need (to handle a specific edge case with deprecated command aliasing) and the conceptual improvement (moving the logic to where it belongs).

This is patch 2/8 in the v5 series, which has already received significant review and appears ready for integration. The change is focused and doesn't introduce new functionality - it's purely preparatory work for the alias shadowing feature that follows in later patches.