The discussion about the deprecated `git whatchanged` command continues, with participants exploring ways to balance user convenience with the project's deprecation policy. The thread was sparked by Bjoern Bastian's admission that they still use the command for its brevity (autocompleting at `git wh`), despite it being functionally equivalent to the non-deprecated `git log --raw`.

Kristoffer Haugsbakk has now proposed a practical solution: creating a custom alias (`wh = log --raw --no-merged`) that maintains the typing convenience while using the supported command. This suggestion introduces the `--no-merged` flag as an additional refinement that wasn't previously discussed, showing that the alias can not only match but potentially improve upon the original command's functionality.

The thread has evolved from discussing whether to keep deprecated commands to exploring concrete migration paths that preserve user workflow efficiency. The proposed alias represents a middle ground - acknowledging that command-line ergonomics matter to users while still moving toward the project's goal of simplifying the interface by removing deprecated commands. This approach could serve as a model for handling similar deprecations in the future.