This is a follow-up in the thread about synchronizing Git stashes across machines, where Brooke Kuhlmann reports back on implementing Phillip Wood's suggested solutions. While the technical discussion continues, the key development here is that Brooke has successfully gotten the stash synchronization to work, though with some remaining quirks.

Brooke confirms that Phillip's configuration suggestions (enabling reflogs via `core.logAllRefUpdates` and setting up a custom fetch refspec for stashes) do allow `--force-with-lease` to work, though it still shows a rejection error message despite successfully updating the stash. The reflog shows activity, though only recording HEAD resets rather than stash-specific operations.

The email highlights that while the technical solution works, there's room for improvement in both documentation (to guide users through this setup) and possibly in Git implementation (to make stash refs behave more intuitively with reflogs by default). Brooke notes that having these changes would make the workflow more "implicit" rather than requiring explicit configuration.

This appears to be moving toward a resolution, with the remaining discussion points being about polishing the user experience rather than fundamental technical blockers. The exchange demonstrates how even with the right configuration, edge cases in Git's ref handling can produce surprising behavior that might warrant either documentation updates or code changes to make the intended usage pattern more obvious.