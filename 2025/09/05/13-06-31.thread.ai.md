Here's the updated AI thread summary:

Bugfix and performance improvement for `git last-modified` command addressing a BUG() when processing criss-cross merges. The issue stems from `diff_tree_combined()` incorrectly forcing recursive mode since 2006, conflicting with `last-modified`'s path tracking. The v2 patch removes this override while maintaining backward compatibility for `diff-tree -c`, resulting in nearly 2x performance improvement for `git last-modified` on git.git.

Key technical details: The change modifies `diff.c`'s `diff_tree_combined()` function to stop unconditionally setting `diffopt.flags.recursive=1`, moving this to specific callers (`diff-tree -c` and submodule handling). Test coverage in `t/tXXXX-last-modified.sh` verifies criss-cross merge handling.

Maintainer Junio C Hamano provided submission guidance regarding proper base branch selection, clarifying that patches should not generally be based on 'next' unless specifically depending on its contents. Kristoffer Haugsbakk followed up to clarify Toon Claes's choice to base the v2 series on 'next', noting it was an incorrect application of the fixup patch guidance since the base topic (kh/doc-markup-fixes) was unrelated to these changes. This exchange reinforces project norms about proper base selection while the technical solution remains uncontested.

Status: The technical solution is sound with demonstrated performance benefits. Open item remains rebasing the patch series to comply with submission norms. Consensus exists on both the recursive flag fix approach and the need to follow standard workflow expectations, with the latter being the current focus of discussion. The thread shows contributors aligning with project practices while maintaining technical progress.