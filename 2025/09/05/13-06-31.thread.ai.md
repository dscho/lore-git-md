Here's the updated AI thread summary:

Bugfix and performance improvement for `git last-modified` command addressing a BUG() when processing criss-cross merges. The issue stems from `diff_tree_combined()` incorrectly forcing recursive mode since 2006, conflicting with `last-modified`'s path tracking. The v2 patch removes this override while maintaining backward compatibility for `diff-tree -c`, resulting in nearly 2x performance improvement for `git last-modified` on git.git.

Key technical details: The change modifies `diff.c`'s `diff_tree_combined()` function to stop unconditionally setting `diffopt.flags.recursive=1`, moving this to specific callers (`diff-tree -c` and submodule handling). Test coverage in `t/tXXXX-last-modified.sh` verifies criss-cross merge handling.

Patrick Steinhardt raises important concerns about potential side effects, particularly regarding other callers like `stdin_diff_commit()` in `git-am` and `git-log` that might implicitly rely on the old behavior. Toon Claes responds in the latest email, acknowledging the need for more comprehensive caller analysis while maintaining this is a bugfix rather than behavior change. The discussion reveals:
- Uncertainty about why criss-cross merges specifically trigger the BUG()
- Performance benefits are limited to non-recursive mode (default for `last-modified`)
- Philosophical difference on approach (Toon favors direct fix, Patrick suggests more conservative flag-based solution)

Junio C Hamano provided earlier submission guidance regarding proper base branch selection (master vs next), which Toon has corrected. Kristoffer Haugsbakk initially noted the base selection issue.

Status: The technical solution remains promising but requires:
1. Further investigation of all `diff_tree_combined()` callers (currently only `diff-tree` handled)
2. Clearer explanation of criss-cross merge behavior
3. Resolution of approach disagreement (direct fix vs new flag)

Open questions:
- Full impact on `stdin_diff_commit()` and other callers
- Whether performance benefits justify the change risk
- If documentation should explicitly note the changed behavior

The thread combines deep technical discussion of diff machinery with process considerations, highlighting challenges in modifying long-standing behaviors while maintaining backward compatibility. Toon plans to investigate additional call sites in the next version while maintaining the position that this is a corrective change.