This thread addresses a platform-specific build issue where Git's unit tests fail to link against `gitmkdtemp()` on HP-UX systems due to header declaration problems. The discussion initially identified two solutions: a Git-specific workaround (including `compat/posix.h` in clar's sandbox header) or modifying the upstream clar test framework to handle HP-UX alongside its existing platform-specific fallbacks for Windows and Solaris.

Patrick Steinhardt advocated for the framework-level solution as more maintainable, contingent on HP-UX providing `mktemp(3p)`. Michael Osipov has now confirmed this approach works in practice, identifying the correct preprocessor macro (`__hpux`, lowercase) and verifying the fallback path functions correctly on HP-UX. This confirmation moves the thread toward resolution, with the consensus favoring upstream clar modifications that would benefit the broader ecosystem rather than a Git-specific fix.

The remaining action is for Patrick to propagate this change to clar's main repository. This case illustrates how the project handles platform compatibility issues in imported test frameworks, preferring solutions that align with the framework's existing patterns when possible. The successful resolution also provides a template for similar future cases as Git expands its unit test coverage.