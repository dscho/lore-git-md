This is a discussion about build system version string formatting, continuing the debate about whether Git should change its snapshot version format to match `git describe`'s output more closely.

Jonas Rebmann responds to Brian M. Carlson's objections about SemVer compatibility, clarifying that they're only advocating for the lexical format (hyphen separators) rather than semantic versioning rules. They argue that `git describe` already follows this common format, making it reasonable for `git version` to do the same. Rebmann also questions whether the current format (with dots) is truly Git's official version format or just an implementation detail, and notes they've already submitted a patch to make b4 handle the current format.

The email provides two key new points: (1) a link suggesting modern RPM packaging guidelines don't require the dotted format, and (2) the observation that while tools can adapt to Git's current format, standardizing on `git describe`'s output would make integration easier across the ecosystem. The discussion remains unresolved, with fundamental questions about version format philosophy versus practical tool compatibility still open.