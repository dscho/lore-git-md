This is a follow-up discussion in the protocol v2 ACK deduplication thread. Patrick Steinhardt responds to Junio Hamano's concerns about edge cases in the original patch that aimed to reduce redundant ACKs in upload-pack.

The conversation explores deeper protocol semantics around how upload-pack handles commit parent processing when clients send commits out of order. Junio had questioned whether pre-marking parent commits (a historical optimization) provides meaningful benefit, especially against clients that don't follow chronological commit ordering. Patrick agrees the current patch should focus narrowly on its memory optimization goal rather than trying to optimize parent handling.

Patrick indicates he'll send a v2 that maintains the original behavior for parent processing while still achieving the memory reduction from deduplicating non-commit objects. He leaves open the possibility of revisiting parent handling optimizations separately. The exchange references historical context of upload-pack's probing behavior and the `fetch.negotiationAlgorithm=skipping` option.

This email doesn't introduce new code but shows consensus forming around a conservative approach to the protocol change - keeping existing parent commit semantics while still getting the memory benefits of deduplication. The discussion suggests the v2 patch will be technically sound while leaving room for future optimization work.