This is a follow-up email in the ongoing discussion about Patrick Steinhardt's `git-history` command proposal, now at version 3 with 31 patches. The exchange clarifies that the `reword` subcommand was actually implemented in v2, contrary to what the cover letter suggests. Kristoffer Haugsbakk points out this documentation discrepancy, and Patrick Steinhardt acknowledges the correction.

The thread context shows this is part of a major effort to introduce Jujutsu-inspired history editing commands (`drop`, `reorder`, `split`, `reword`) with management commands (`abort`/`continue`/`quit`). The implementation builds on extensive preparatory work including sequencer infrastructure changes, `the_repository` removal, hook execution improvements, and add-patch subsystem refactoring. The `reword` subcommand specifically handles commit message editing while preserving other metadata, with comprehensive test coverage for edge cases.

This brief exchange doesn't introduce new technical content but ensures accurate documentation of what's already implemented in the series. The correction is minor but important for reviewers tracking the feature set's evolution across versions. The thread remains active with discussions about implementation details and potential future subcommands.