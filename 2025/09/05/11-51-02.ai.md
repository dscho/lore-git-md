This is part of the ongoing Rust infrastructure series, specifically focusing on CI testing for the breaking changes build configuration. The patch repurposes the existing "pedantic" CI job (which had become redundant) into a new Fedora-based test that exercises the Meson build system with breaking changes enabled.

Key technical details:
- Removes the obsolete `pedantic` job from both GitHub and GitLab CI configurations
- Adds new `fedora-breaking-changes-meson` job that:
  - Uses Fedora latest as the base image
  - Installs Meson/Ninja build tools via dnf
  - Sets `WITH_BREAKING_CHANGES=YesPlease` and `-Dbreaking_changes=true`
  - Runs full build and test suite (unlike the old pedantic job which only built)
- Modifies the test execution logic to always run tests (removing the `run_tests` conditional)
- Prepares for future Rust testing by ensuring this job will work with both build systems

The change serves multiple purposes:
1. Provides additional distro coverage (Fedora) for breaking changes testing
2. Validates Meson build system behavior with breaking changes enabled
3. Creates a foundation for upcoming Rust integration testing
4. Removes obsolete CI configuration

This is a straightforward infrastructure change that builds on the series' previous work establishing dual build system support and the breaking changes framework. The patch appears well-constructed with appropriate test coverage and follows the established pattern from earlier CI modifications in the series.