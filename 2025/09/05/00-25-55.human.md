This is a clarification email in the thread about fixing a dangling pointer issue in Git's slab allocator API. The discussion has reached consensus on the technical approach, with maintainer approval already given for the v5 patch.

ノウラ | Flare provides additional explanation about the NULL pointer handling in `alloc_state_free_and_null()`, clarifying that the check should be on `*s` (the dereferenced pointer) rather than `s` (the double-pointer itself). This ensures the function remains idempotent (safe to call multiple times on the same pointer) while still catching programming errors where a NULL double-pointer is passed.

The email reinforces the design decision that emerged from earlier discussion between Jeff King and Junio C Hamano - that checking `*s` properly balances API safety (allowing NULL inputs) with catching real programming errors (rejecting NULL double-pointers). This serves as final documentation of the rationale without requiring any further changes to the already-approved patch.

The thread shows the careful consideration Git contributors give to API design, particularly around memory management and error handling. With this clarification, the discussion appears fully resolved and the patch ready for merging.