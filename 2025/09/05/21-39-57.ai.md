This is the final approval for a bugfix series addressing duplicate ACK behavior in upload-pack's protocol v2 implementation. Junio C Hamano confirms the technical approach of patch 2/2, which now applies the deduplication logic uniformly to all object types (not just commits) while still preserving the special parent-marking behavior for commits. 

The key change in this version was reordering operations in `do_got_oid()` to ensure parents of seen commits are always marked with THEY_HAVE before checking the flag, maintaining conservative behavior for commit processing. The implementation now consistently prevents duplicate ACKs for all object types in protocol v2 while keeping the memory optimization benefits (~4MB/s savings) and backward compatibility with protocol v0.

Junio notes this completes the series' goals and will queue the patches, marking resolution of the issue. The solution maintains all edge case handling from the modernized tests in patch 1/2 (already merged) while simplifying the deduplication logic to be more uniform across object types.