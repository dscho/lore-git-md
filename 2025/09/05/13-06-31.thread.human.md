Here's the updated human-readable thread summary:

This thread addresses a bug in `git last-modified` that triggers a BUG() when processing criss-cross merges, tracing back to an unconditional recursive flag setting in `diff_tree_combined()` since 2006. The proposed fix removes this override while maintaining backward compatibility, showing nearly 2x performance improvements for the affected command.

The discussion has evolved through several technical exchanges between Toon Claes (who proposed the fix) and Patrick Steinhardt (who raised concerns). While Patrick acknowledges the problematic nature of the original code, he maintains reservations about potential side effects in other callers like `stdin_diff_commit()` used by `git-am` and `git-log`. Toon argues this is a clear bugfix with minimal real-world impact, but both agree more comprehensive analysis of all call sites would be valuable.

Key outstanding questions include:
- The full impact on other code paths that might implicitly rely on the old behavior
- Whether the performance benefits justify the change risk
- If documentation should explicitly note the behavior change

Junio C Hamano provided early guidance on proper base branch selection, which has been addressed. The thread exemplifies the careful balancing act required when modifying long-standing behaviors in core Git machinery, where even clearly suboptimal implementations may have subtle dependencies. Toon plans to investigate additional call sites in the next version while maintaining the position that this is a corrective change rather than a behavior modification.