Here's the human-readable summary of the thread:

The thread addresses a bug in `git last-modified` where it crashes (via BUG()) when processing criss-cross merges. The root cause was traced to `diff_tree_combined()` incorrectly forcing recursive mode since 2006, which conflicts with `last-modified`'s path tracking. Toon Claes's v2 patch fixes this by removing the recursive override while maintaining compatibility for `diff-tree -c`, showing nearly 2x performance improvements on git.git.

After initial discussion about proper base branch selection (clarified by Junio) and workflow issues noted by Kristoffer Haugsbakk, Patrick Steinhardt has raised important technical concerns about potential side effects. He questions whether changing `diff_tree_combined()`'s default behavior might break other callers that implicitly relied on the recursive flag, suggesting either a more thorough audit of all callers or introducing a new flag for better backward compatibility.

The patch currently shows promising performance benefits and fixes the immediate issue, but needs to address these safety concerns before being ready for final review. The discussion has revealed deeper questions about the original 2006 behavior and why criss-cross merges specifically trigger this issue.