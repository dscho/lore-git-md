Here's a human-readable summary of the thread's current state:

The thread addresses a bug in `git last-modified` that triggers a BUG() when processing criss-cross merges, tracing the issue to an unconditional recursive flag setting in `diff_tree_combined()` that's been present since 2006. Toon Claes' patch removes this override while maintaining backward compatibility, showing nearly 2x performance improvements for the affected command.

Patrick Steinhardt raised important concerns about potential side effects in other parts of the code that might implicitly rely on the old behavior, particularly in `git-am` and `git-log`. In the latest response, Toon acknowledges these concerns but maintains this is fundamentally a bugfix rather than a behavior change. He agrees to investigate the other callers Patrick mentioned but pushes back against adding a new flag, viewing it as unnecessary technical debt.

The discussion has revealed some unresolved questions - particularly why criss-cross merges specifically trigger the issue, and whether other parts of Git's diff machinery might be affected. While the performance benefits are clear, the patch's path forward depends on either demonstrating no regressions or finding a way to address Patrick's compatibility concerns. Junio Hamano had earlier provided guidance about proper base selection, which has since been corrected, showing the thread combines both technical depth and attention to process details.