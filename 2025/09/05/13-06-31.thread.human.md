Here's a human-readable summary of the thread:

The thread addresses a bug in `git last-modified` where it would hit a BUG() when processing criss-cross merges, traced back to `diff_tree_combined()` incorrectly forcing recursive mode since 2006. The technical solution (removing this override while maintaining backward compatibility) has been uncontroversial, showing nearly 2x performance improvements for the command on git.git.

While the patch itself is technically sound, the discussion has shifted to proper submission practices. Maintainer Junio Hamano emphasized that patches should generally be based on 'master' rather than 'next', unless specifically depending on its contents. Toon Claes initially based the series on 'next' incorrectly, but has now acknowledged this and provided the proper base information (master at 2462961280 with their Bloom filter changes merged in).

The exchange highlights how the Git community values both technical correctness and adherence to workflow norms. The patch appears ready for merging once properly rebased, with consensus on both the solution and the importance of following submission guidelines.