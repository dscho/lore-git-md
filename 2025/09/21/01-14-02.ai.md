This is a technical clarification in the ongoing Rust integration thread, specifically addressing position-independent code (PIC/PIE) requirements when linking Rust with libgit.a.

Ramsay Jones confirms that Meson defaults to using `-fPIC` compiler flags (as noted in his earlier cover letter), and explains this can be overridden through Meson's configuration. He raises two new technical points: first, that Rust's apparent requirement for PIC/PIE when linking to libgit.a might be addressed by passing flags to `rustc` to disable its default PIC behavior; second, that GNU `nm` produces warnings when examining the built Rust library, suggesting potential symbol visibility issues.

The email moves the discussion forward by providing concrete build system details (Meson's default PIC behavior is configurable) while introducing new technical questions about Rust's linking requirements and symbol handling. This comes after previous consensus that Rust defaults to PIC behavior, with Junio Hamano's concern about whether Meson behavior is portable across platforms. Jones' observation about `nm` warnings hints at potential deeper compatibility issues that may need investigation as the Rust integration progresses.