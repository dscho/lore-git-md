This is a follow-up discussion about a refactoring patch (v3 05/10) in the xdiff preparation series that has already been merged to 'next'. Phillip Wood raises a question about the rationale behind removing local variables `rchg[12]` and `rindex[12]` in favor of direct `xdf[12]` dereferencing within loops, despite the patch's explanation about removing the `diffdata_t` struct aliases.

The patch in question removes the `diffdata_t` wrapper struct that previously aliased fields from `xdfile_t` (`nrec`->`nreff`, `ha`, `rindex`, `rchg`), simplifying the code by working directly with `xdfile_t`. While the commit message explains the struct removal, Phillip notes it doesn't justify the additional change to loop variable access patterns. This appears to be a post-merge clarification request about internal consistency of the refactoring approach, separate from the already-resolved macro naming discussion that was the primary open issue from this series.

Key technical details:
- Modified files: xdiff/xdiffi.c, xdiff/xdiffi.h
- Removed: `diffdata_t` struct and its field aliases
- Changed: `xdl_recs_cmp()` signature and call sites to use `xdfile_t*` directly
- Questioned change: Removal of local variables in favor of repeated `xdf->` dereferencing in loops

This appears to be a minor clarification request about implementation choices rather than a substantive issue, as the patch's core transformation (struct removal) was already reviewed and merged. The question doesn't challenge the correctness or functionality, but seeks to understand the design rationale behind the additional variable access changes.