Thread identity: Completed 3-patch bugfix series (v2) addressing curl_easy_setopt() type mismatch causing CI failures in Git for Windows ARM64 builds, triggered by libcurl update. Originating author: Johannes Schindelin (Dscho). All patches reviewed and approved for merging, now queued for 'next' branch.

Core problem: libcurl API expects `long` for CURLOPT_INFILESIZE but receives incorrect type, caught by clang in ARM64 builds. Series fixes immediate issue and improves type safety across HTTP and IMAP curl interactions by introducing consistent type handling.

Current status: v2 series merged to 'next' after incorporating maintainer feedback. Final version includes two documentation improvements: added `#include` for gettext support in `http.h` and clarified `curl_off_t` description in IMAP patch. All three patches (HTTP helper, IMAP fixes, HTTP push) received maintainer ack from Junio C Hamano with no outstanding issues.

Technical details:  
- Introduces `cast_size_t_to_curl_off_t()` in http.h as central helper (relocated from `xcurl_off_t()` in remote-curl.c)  
- Uses `maximum_signed_value_of_type()` for bounds checking  
- Updates error messages with platform-specific size limitations (PRIuMAX format)  
- Patch 1: HTTP POST field size handling in RPC operations (moves/renames helper function to http.h, improves error reporting)  
- Patch 2: IMAP message size handling in imap-send.c (single-line change replacing direct cast with new helper, confirmed safe from unsigned wraparound)  
- Patch 3: HTTP push infilesize handling in http-push.c (3-line change completing series)  
- Replaces CURLOPT_INFILESIZE with CURLOPT_INFILESIZE_LARGE throughout  
- v2 changes: Final documentation improvements (gettext include, curl_off_t wording)  

Participant positions:  
- Dscho (author): Provided complete v1 series addressing all identified type safety issues, incorporated documentation improvements in v2  
- Junio C Hamano: Acked all patches, confirmed series ready for queueing after reviewing final documentation tweaks  

Open questions: None - series merged to 'next' branch. Thread complete.

Related work: Build failure was caught by Git for Windows CI on ARM64, but fix applies to all platforms. Follows pattern of previous curl-related type safety improvements in Git. Completes the type-safety effort across HTTP, IMAP and HTTP push subsystems.