Thread identity: Bugfix series addressing curl_easy_setopt() type mismatch causing CI failures in Git for Windows ARM64 builds, triggered by libcurl update. Originating author: Johannes Schindelin (Dscho). 3-patch series currently at v1.

Core problem: libcurl API expects `long` for CURLOPT_INFILESIZE but receives incorrect type, caught by clang in ARM64 builds. Series fixes immediate issue and improves type safety across HTTP and IMAP curl interactions.

Current status: First version (v1) of 3-patch series. First patch introduces foundational type-casting helper function, second patch applies it to IMAP subsystem.

Technical details:  
- Introduces `cast_size_t_to_curl_off_t()` in http.h, replacing `xcurl_off_t()` from remote-curl.c  
- Uses `maximum_signed_value_of_type()` for bounds checking  
- Updates error message to be platform-specific about size limitations  
- Patch 1 affects HTTP POST field size handling in RPC operations  
- Patch 2 modifies IMAP message size handling in imap-send.c  
- Same helper function used consistently across both subsystems  
- Changes span http-push.c, imap-send.c, http.h, remote-curl.c  

Context: Author notes this follows Git tradition of improving surrounding code when fixing issues, expresses surprise CURLOPT_INFILESIZE_LARGE wasn't used originally as it predates Git. Commit messages provide good historical context about curl API choices. IMAP patch maintains same careful approach as HTTP changes.

Open questions: None yet - awaiting review feedback on initial version. Reviewers may want to verify IMAP code paths exercise same boundary conditions that motivated HTTP changes.

Participant positions: Only author (Dscho) has contributed so far.  

Related work: Build failure was caught by Git for Windows CI on ARM64, but fix applies to all platforms. Follows pattern of previous curl-related type safety improvements in Git.