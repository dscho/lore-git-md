Thread identity: Bugfix series addressing curl_easy_setopt() type mismatch causing CI failures in Git for Windows ARM64 builds, triggered by libcurl update. Originating author: Johannes Schindelin (Dscho). 3-patch series now complete at v1.

Core problem: libcurl API expects `long` for CURLOPT_INFILESIZE but receives incorrect type, caught by clang in ARM64 builds. Series fixes immediate issue and improves type safety across HTTP and IMAP curl interactions by introducing consistent type handling.

Current status: Complete v1 series with all 3 patches. Patch 1 introduces type-casting helper, patch 2 fixes IMAP subsystem, patch 3 fixes HTTP push. Changes use CURLOPT_INFILESIZE_LARGE where available, matching Git's supported curl versions.

Technical details:  
- Introduces `cast_size_t_to_curl_off_t()` in http.h as central helper  
- Uses `maximum_signed_value_of_type()` for bounds checking  
- Updates error messages with platform-specific size limitations  
- Patch 1 (foundational): HTTP POST field size handling in RPC operations  
- Patch 2: IMAP message size handling in imap-send.c  
- Patch 3: HTTP push infilesize handling in http-push.c  
- Replaces CURLOPT_INFILESIZE with CURLOPT_INFILESIZE_LARGE where appropriate  
- Changes span http-push.c, imap-send.c, http.h, remote-curl.c  

Context: Follows Git tradition of improving surrounding code when fixing issues. Commit messages provide historical context about curl API choices and explain why simple casts would be insufficient. Author notes surprise CURLOPT_INFILESIZE_LARGE wasn't used originally despite being available.

Open questions: None explicitly raised yet - series appears complete and consistent. Reviewers may want to verify all code paths exercise boundary conditions properly.

Participant positions: Only author (Dscho) has contributed so far. Series appears ready for review with all planned fixes included.

Related work: Build failure was caught by Git for Windows CI on ARM64, but fix applies to all platforms. Follows pattern of previous curl-related type safety improvements in Git. Completes the type-safety effort across HTTP and IMAP subsystems.