This is a follow-up discussion about the xdiff refactoring series, specifically addressing the namespace pollution concern around the NO/YES/MAYBE macros introduced in patch 10/10.

Phillip Wood confirms that using `bool` for the `rchg` field (as Ezekiel Newren proposed) would work fine with Git's current header organization, since `stdbool.h` is properly available through `compat/posix.h` inclusion. He notes this approach would be preferable as it would prevent the macros from leaking into other files' namespaces.

The email also serves as a technical clarification about Git's header organization history, pointing out that while `stdbool.h` inclusion has moved between files, it remains consistently available through the current `git-compat-util.h` -> `compat/posix.h` chain. This addresses one of Newren's concerns about Rust compatibility implications.

This appears to be a straightforward technical confirmation that helps resolve the last open question from the merged preparatory series, moving the discussion toward a solution that both maintains clean namespaces and supports future Rust integration. The tone is constructive and the technical details are accurate, building on the prior discussion between Newren and Junio Hamano about macro visibility.