This is a follow-up discussion about a refactoring patch from the xdiff preparatory cleanup series that was already merged to 'next'. Phillip Wood questions the rationale for removing the `xdl_get_rec()` helper function in patch 4/10 while patch 6/10 introduces a different getter function.

The key technical detail is that `xdl_get_rec()` was removed because it aliased fields of `xrecord_t`, making field usage harder to track. However, Phillip points out the apparent contradiction that patch 6 introduces another getter function, suggesting the removal rationale may not be consistently applied. The patch itself shows straightforward mechanical changes replacing `xdl_get_rec()` calls with direct `xdf->recs[ri]` access and field references.

This discussion represents a post-merge review point about internal consistency in the refactoring approach, though it doesn't affect the already-merged changes' functionality. The thread context shows this is part of the completed preparatory work (patches 1-10) for future Rust compatibility changes, with all major technical issues already resolved except for the NO/YES/MAYBE macro naming question.