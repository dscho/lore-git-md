This is a review response from Junio C Hamano regarding Lucas Seiki Oshiro's patch adding a `--all` flag to `git-repo-info`. The patch is part of ongoing work to improve repository metadata access.

Junio raises several thoughtful points about the implementation and documentation. He notes the commit message could be clearer about whether `--all` returns keys or values (suggesting "values for all keys" would be more precise). He also examines the interaction between `--all` and explicit key arguments, identifying four possible design approaches for handling this case and observing the current implementation silently ignores extra keys when `--all` is given.

The review suggests adding tests for edge cases like `--all` with both valid and invalid keys, and hints that while the current behavior is acceptable if documented, other approaches might be more intuitive or logical. Junio's tone is constructive throughout, focusing on making the interface clear and predictable rather than rejecting the approach outright.

This continues the thread about enhancing `git-repo-info`'s functionality while maintaining its experimental status. The discussion remains technical and productive, with Junio providing guidance rather than directives.