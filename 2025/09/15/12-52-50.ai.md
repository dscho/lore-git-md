Refactoring patch in a series fixing `git config get --type=color` issues, focusing on error handling improvements.

This patch modifies the `get_color()` function in builtin/config.c to return an error code instead of calling `die()` when encountering invalid color values. The change makes the function more flexible for future callers that need their own error handling paths, while maintaining the same behavior for the existing caller (which already handles other error cases). The implementation adds a return value and propagates the error from `color_parse()`, while still ensuring the parsed color (or default) gets printed to stdout in all cases.

Key technical details:
- Changes function signature from `void` to `int` return
- Replaces `die()` with `error()` and returns non-zero on parse failure
- Maintains existing stdout output behavior via `fputs()`
- Updates the ACTION_GET_COLOR case in cmd_config_actions() to handle the return value
- Part of a larger series addressing color handling in git config (patches 4/5 and 5/5 will build on this)

The change is well-contained and follows the series' pattern of incremental improvements to color handling. The commit message clearly explains the motivation (preparing for a future caller) and maintains backward compatibility with existing behavior. This appears to be a straightforward refactoring that enables future work while not introducing new functionality or behavior changes.