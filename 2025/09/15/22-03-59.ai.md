This is a follow-up discussion in the completed ODB transaction refactoring series, addressing an edge case in `git update-index` behavior. The email clarifies an earlier observation about option ordering quirks in `update-index`, specifically around `--force-remove` processing.

Justin Tobler corrects his previous examples, noting that `--force-remove` (not `--remove`) causes all subsequent file arguments to be processed as removals, creating an order-dependent behavior difference between `--remove foo --add bar` and `--add foo --remove bar`. He points out this appears unintentional but is actually relied upon by existing tests in `t1001-read-tree-m-2way.sh` and `t1002-read-tree-m-u-2way.sh`.

The discussion doesn't propose any code changes - it's a post-series clarification about an observed behavior that interacts with the transaction system but isn't directly related to the ODB refactoring work. The thread context shows this series successfully completed its goal of restructuring transaction handling for future pluggable backends, with all implementation and documentation work finished.