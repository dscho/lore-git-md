Refactoring patch in Patrick Steinhardt's packfile store series (v5 8/15). This continues the migration of packfile management from `struct object_database` to the new `struct packfile_store`.

The patch refactors `prepare_packed_git()` and related functions to operate directly on a packfile store rather than a repository. Key changes:
- Renames `prepare_packed_git()` to `packfile_store_prepare()` and updates its signature
- Splits out MRU preparation into new `packfile_store_prepare_mru()`
- Removes standalone `rearrange_packed_git()` helper by inlining its sort operation
- Updates all callers (7 locations) to use the new store-based functions
- Maintains existing initialization sequence and MIDX handling
- Preserves all functional behavior while changing the underlying implementation

The changes affect core packfile operations in `packfile.c`, touching functions like `get_packed_git()`, `get_all_packs()`, and `find_pack_entry()`. This is a mechanical refactor that advances the series' goal of separating packfile management from object storage while keeping all existing invariants. The patch follows the established pattern from earlier in the series and introduces no new concepts.