This is part 3/15 of Patrick Steinhardt's series refactoring packfile management into a new `struct packfile_store`. The patch moves the `packed_git_initialized` flag from `struct object_database` to the new `struct packfile_store` where it more logically belongs, now named simply `initialized`. As a minor cleanup, it also converts the field from a bitfield to a proper boolean type.

The change is straightforward and mechanical - updating the flag's location and type while maintaining the same behavior. This continues the series' goal of centralizing packfile-related state in the new store structure. The patch touches three files with simple changes to declarations and usage sites, all clearly focused on this single migration.

This appears to be a well-contained step in the larger refactoring effort, with no visible impact beyond the internal reorganization. The commit message clearly explains the what and why, and the changes match that description exactly. As with the rest of this mature series, the patch looks ready to go.