This is a follow-up discussion about a refactoring patch that changes how Git aliases are executed. The original patch modified `git.c` to use `cmd.git_cmd = 1` instead of explicitly pushing "git" to the command vector when running aliases, following `run-command.h` API guidelines.

Junio Hamano raises several important points in response. He questions whether the documentation in `run-command.h` needs updating to clarify when to use `.git_cmd` versus explicitly pushing "git", noting that the `upload-pack.c` case shows there are exceptions where both approaches are needed together. He suggests the API documentation should explain these special cases where the "git" prefix is still required despite setting `.git_cmd`.

The discussion highlights that this seemingly simple refactoring touches on subtle API usage patterns. While the change appears correct for the alias case, there may be broader implications for how Git commands spawn subprocesses that need to be documented. No code changes are proposed in this email - it's focused on ensuring the API guidelines properly capture the nuances revealed by the patch.

This exchange suggests the original patch may need follow-up documentation work to prevent similar confusion in the future, though the core change to alias execution still appears sound. The thread is now moving from implementation to API documentation concerns.