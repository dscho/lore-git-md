Bugfix for `git send-email` handling of Reply-To headers when using `--compose --reply-to` together, causing duplicate headers rejected by providers like Gmail. The issue stems from interaction between Reply-To insertion in the intro message and subsequent header copying. NeilBrown's patch modifies `git-send-email.perl` to detect/store the Reply-To header during pre-processing (case-insensitive `^Reply-To:` match), preventing duplicates by storing in `$reply_to` variable, similar to existing In-Reply-To/References handling.

Junio C Hamano confirms the problem description but requests commit message improvements: (1) fix author/Signed-off-by mismatch, (2) use more imperative language. The technical solution (3-line change in header parsing block) is sound - properly captures Reply-To into `$reply_to` during pre-process to avoid `@xh` array duplication. The change specifically targets compose+reply-to interaction without broader impacts. While functionally complete, the patch needs minor polishing per maintainer feedback before acceptance. No test changes included yet, which may need addressing given the header handling behavior modification.