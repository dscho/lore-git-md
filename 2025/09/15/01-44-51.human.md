This is a technical discussion about alias recursion detection in the command deprecation framework series. Jeff King responds to Kristoffer Haugsbakk's concerns about test failures when modifying the alias expansion code. Jeff demonstrates that the current implementation (v5) already shows the same behavior Kristoffer was trying to avoid - extra alias expansion messages appearing before the loop detection error - even without involving deprecated commands.

The exchange focuses on a subtle edge case in the alias recursion detection logic, where the code prints additional alias expansion messages before catching the loop. Jeff provides a test case showing this occurs with any alias loop, not just with deprecated commands. The discussion suggests this behavior is acceptable since it only appears with `-h` help output and provides useful context about the alias chain before the fatal error.

This appears to be a minor polish discussion on an already well-tested feature, with Jeff confirming the current behavior is reasonable. The core functionality of the deprecation system remains unchanged from the v4 implementation that received maintainer approval.