Maintainer feedback on final edge case in command deprecation series.

Junio Hamano notes that when testing the v5 patches with `WITH_BREAKING_CHANGES=YesPlease`, the looping alias test (t0014.4) fails due to changed behavior. The test expects verbose output showing the alias loop detection, but now gets a simpler "cannot handle as builtin" message instead. This appears to be a side effect of the final changes made to handle alias recursion messaging more cleanly.

The issue is specific to testing deprecated builtins (`whatchanged` and `pack-redundant`) with looping aliases. While the core functionality is working as intended (preventing infinite loops), the test needs updating to match the new output format. This is a minor test adjustment rather than a functional problem, and comes after all major technical issues in the series were resolved in v4 and v5.

The patch series remains in good shape overall, with this being the last known test case needing adjustment before final submission. The maintainer's note serves as a reminder to update the test expectations to match the simplified error messaging that was intentionally introduced.