This is a follow-up discussion in the ongoing RFC series for the new `git-history` command, specifically about the `split` subcommand's user interface. 

Patrick Steinhardt responds to D. Ben Knoble's observation that the `split` operation currently shows a list of modified files (via `commit.verbose=true`) rather than the full diff that Ben expected. Patrick acknowledges this was an unfamiliar feature to him and proposes keeping the current behavior for now, with plans to address the diff display in a future update after the initial patches land.

The exchange highlights a minor documentation/UX clarification needed around what information users should expect during split operations. This doesn't appear to be a technical bug but rather a difference in expectations about the command's output format. The discussion remains constructive, with Patrick open to improving the behavior later in the development process.

This email doesn't introduce new technical changes but helps clarify the current state of the `split` subcommand's UI as the RFC series continues to evolve. The thread shows active engagement from contributors in refining the command's behavior before it gets merged.