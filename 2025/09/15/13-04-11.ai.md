This is a follow-up discussion about the `split` subcommand implementation in Patrick Steinhardt's `git-history` RFC series. D. Ben Knoble clarifies their earlier feedback about the split command's UI, noting they were expecting to see a diff output rather than just the list of modified files when splitting commits. Patrick acknowledges this was an unfamiliar feature (commit.verbose mode showing diffs during commit message editing) and proposes keeping the current modified-files-only display for now, with potential enhancements to show diffs in future iterations after the initial patches land. Ben accepts this approach and apologizes for the earlier confusion around commit.verbose being a red herring in the discussion.

Key technical details for future reference:
- The `split` subcommand currently shows only modified files list in the editor template, not full diffs
- Git's existing `commit.verbose` mode can show diffs during commit message editing, but this isn't currently utilized
- Patrick plans to keep current behavior for initial landing, with diff display as a potential future enhancement
- The exchange reveals a UI expectation mismatch that may need documentation clarification
- This doesn't affect the core functionality but highlights a potential area for UX improvement

The discussion shows responsive handling of feedback while maintaining focus on getting the core functionality landed first. The conservative approach aligns with Patrick's overall strategy for the series of incremental improvements after initial acceptance.