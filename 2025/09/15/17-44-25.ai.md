This is a follow-up discussion in the `git add -p` hunk splitting behavior thread, focusing on the version-gating approach for the new behavior. Junio Hamano responds to Phillip Wood's patch that changes split hunks to be marked as "undecided" rather than inheriting the parent hunk's selection state.

The core technical point under debate is whether the change should be gated behind the `WITH_BREAKING_CHANGES` mechanism (as proposed) or implemented as a runtime-configurable option. Junio argues this doesn't fit the typical use case for version-gated changes, suggesting either:
1. Treating it as a straightforward bugfix (implying no gating needed), or
2. Making it configurable via a runtime option if both behaviors have merit

The thread context shows this is part of a resolved series where:
- The technical implementation is complete (V2 addresses both hunk state and synchronization)
- The maintainer has previously indicated workflow considerations should drive interface decisions
- Phillip has already incorporated feedback on the approach

Key technical details referenced:
- Current behavior: Split hunks inherit parent's selection state
- Proposed change: Split hunks become `UNDECIDED_HUNK` (with `WITH_BREAKING_CHANGES`)
- Implementation location: `add-patch.c` hunk handling logic

The discussion now centers on the appropriate mechanism for behavior change rather than the change itself, with Junio questioning whether version-gating aligns with Git's typical change management patterns for this type of interactive workflow modification.