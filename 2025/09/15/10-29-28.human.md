This is a follow-up discussion in the feature patch series adding signature handling to `git fast-import`. The thread has progressed through several rounds of review, with the second patch now in its second version (V2) having addressed all technical feedback.

The current email focuses on a specific design decision about how `fast-import` should handle malformed signatures in STRIP mode. Christian Couder initially implemented STRIP mode to skip signature parsing entirely for performance reasons, but Junio C Hamano suggests it would be more user-friendly to parse signatures even in STRIP mode, as users might rely on this mode to handle problematic signature data.

Christian acknowledges the point and indicates he will modify the implementation to maintain the same behavior as V1 (which did parse signatures) while avoiding the "code ergonomics regression" Junio had noted in the initial review. The key concern is that `parse_one_signature()` could die() on invalid signatures, which needs careful handling.

This discussion represents the final refinement of the patch series, which has already received positive reviews and addressed all other technical concerns. The only remaining question was this specific behavior in STRIP mode, which Christian now agrees to adjust per Junio's suggestion. The series appears ready for merging once this last change is implemented.