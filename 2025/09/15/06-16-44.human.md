This is a review response in the ongoing refactoring thread about simplifying object lookup code. Patrick Steinhardt is commenting on René Scharfe's patch that replaces `get_oid_with_context()` calls with `repo_get_oid_with_flags()` where the object context isn't needed.

Patrick agrees with the general approach, noting that the two functions are effectively equivalent when the context isn't used. He points out a minor inconsistency in the return types (`int` vs `enum get_oid_result`) that predates this change, but clarifies this doesn't affect correctness and doesn't need to be addressed here. The example diff shows one of the mechanical conversions that removes boilerplate context handling code.

The review is positive overall, with Patrick confirming the correctness of all the changes while making an observation about the API design that could be improved separately. This keeps the thread moving forward with what appears to be a straightforward, uncontroversial refactoring.