This is a response in the ongoing discussion about `git update-ref`'s handling of pseudorefs like `MERGE_HEAD`. Patrick Steinhardt acknowledges the contradiction between Git's advice to use `update-ref` for managing `MERGE_HEAD` and the current restriction that prevents this.

The response suggests a technical solution: special-casing pseudorefs in relevant code paths while being careful to maintain architectural boundaries. Patrick notes that pseudorefs aren't real refs and shouldn't be handled through normal ref transactions, but proposes that `git-update-ref` could be modified to support single pseudoref operations outside the transaction system.

This moves the discussion forward by proposing a concrete technical approach to resolve the inconsistency, while recognizing the need to maintain proper separation between pseudorefs and regular refs. The suggestion appears to balance the practical need for pseudoref manipulation with Git's internal architecture constraints.