This is a follow-up discussion in the `git-history` RFC thread, specifically about the `split` subcommand's UI behavior. Patrick Steinhardt responds to D. Ben Knoble's observation that the `split` operation shows modified files (via `commit.verbose=true`) rather than the full diff that Ben expected.

Key points:
- Ben confirms the current behavior shows modified files in the editor template, not a full diff
- Patrick acknowledges this is new information to him about Git's capabilities
- Proposes keeping current behavior for initial landing, with potential future enhancement to show diffs
- The exchange reveals a documentation/UX gap around user expectations for split operations

Technical context:
- The `split` subcommand currently preserves the original commit's tree and message in the second split commit
- Discussion continues the thread's theme of UI/UX refinement for the new history editing commands
- Patrick's response indicates openness to future improvements while prioritizing initial landing

This interaction doesn't introduce new technical changes but surfaces an important UX consideration for the command's design. The thread's broader architectural questions (sequencer integration, command structure) remain open while this specific UI clarification gets resolved.