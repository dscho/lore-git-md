Review feedback on the fast-import signature handling patch. Patrick Steinhardt suggests a minor improvement to make the switch statement more defensive by using a `default` case rather than explicitly checking for `SIGN_ABORT` after it was already handled. He notes this would catch any unhandled enum values. The rest of the patch implementation looks good to him, with no other changes needed.

This is part of the ongoing feature series adding signature handling control to `git fast-import` to match `git fast-export`'s capabilities. The feedback is focused on a small but important defensive programming improvement to the core signature processing logic. The review indicates the overall approach is sound and the implementation is nearly ready to proceed.