Refactoring patch relocating ODB transaction code from bulk-checkin to object-file.

This is patch 4/6 in Justin Tobler's series to simplify Git's object database transaction interface. The patch moves all transaction-related code from bulk-checkin.[ch] to object-file.[ch], deleting the bulk-checkin files entirely. This is part of a larger effort to prepare for pluggable ODB backends by making transaction handling more generic.

The changes are mechanical - the code is moved verbatim with no functional changes. The commit message notes this is just the relocation step, with function renaming coming in the next patch. The diff shows 393 lines removed from bulk-checkin.c and 394 added to object-file.c, with corresponding header file changes and updates to Makefiles/build systems.

This appears to be a well-executed code movement that follows the series' goal of consolidating transaction handling. The patch maintains all existing functionality while setting up for the API simplifications that follow. Reviewers should focus on verifying the relocation preserves all behavior and that no bulk-checkin internals were missed.