This is part 11/15 of Patrick Steinhardt's packfile store refactoring series, which introduces a new helper function to simplify packfile loading. The patch adds `packfile_store_load_pack()`, which combines the common pattern of checking if a packfile exists in the store and adding it if not. 

The new function handles the non-trivial logic of reconstructing packfile paths (converting .idx to .pack paths), checking the store's hashmap, and adding new packs. It replaces several manual implementations of this pattern in fast-import, index-pack, midx handling, and pack preparation code. The changes are mechanical - the behavior is maintained while reducing code duplication.

This continues the series' goal of centralizing packfile management in the new `struct packfile_store`. The patch touches core packfile operations but doesn't introduce new behavior, just cleaner organization. With the thread already at technical resolution, this appears to be one of the final steps in solidifying the packfile store abstraction.