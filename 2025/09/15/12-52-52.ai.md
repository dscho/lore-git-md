This is the final patch in a 6-part bugfix series addressing issues with `git config get --type=color` functionality. The patch specifically prevents the pager from being used when printing ANSI color escape sequences, which was causing nonsensical behavior where color codes would be piped to the pager rather than being displayed directly in the terminal.

The change modifies `builtin/config.c` to skip pager setup when the output type is COLOR, and adds a new test case in `t1300-config.sh` that verifies this behavior works correctly in a TTY environment. The test uses the test-terminal infrastructure to simulate a terminal and confirms that color output bypasses the pager even when one is configured.

This completes the series' goal of fixing three distinct issues with color value handling in `git config get` while also modernizing the test suite. The patch is straightforward and well-tested, with no outstanding review comments or concerns noted in the thread. The series as a clean progression from test cleanup to implementation changes, with this final patch addressing the last identified issue.