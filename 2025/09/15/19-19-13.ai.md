Maintainer follow-up on the command deprecation series. Junio Hamano notes a test failure in t0014 when running with WITH_BREAKING_CHANGES=YesPlease, where the looping alias test for deprecated builtins now produces different output than expected. The test expects verbose alias loop detection messages but instead gets a simpler "cannot handle as a builtin" error.

The specific case involves mutually recursive aliases between `whatchanged` and `pack-redundant`, both marked as deprecated in this series. The test failure shows the divergence between the detailed loop trace (expected) and the new builtin-handling error (actual). This appears to be a side effect of the v5 changes that fixed memory leaks and alias loop regressions.

Key technical points:
- The test failure occurs only when the breaking changes flag is enabled
- Affects the alias recursion detection path for deprecated commands
- Demonstrates interaction between the new deprecation system and existing alias handling
- Test case t0014.4 specifically checks this edge case

This appears to be the final integration issue to resolve before the series can proceed. The maintainer's note suggests the test expectations need updating to match the new behavior, though it's unclear whether this represents an intentional simplification or an unintended side effect of the fixes.