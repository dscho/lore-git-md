This is part 12/15 of Patrick Steinhardt's packfile store refactoring series, focusing on moving MIDX-related functionality to their proper subsystem. The patch relocates `get_multi_pack_index()` from packfile.c to midx.c, where it logically belongs as part of the multi-pack index subsystem.

The key change exposes `packfile_store_prepare()` (previously static) to allow the MIDX subsystem to properly initialize packfiles before accessing them. The function is moved along with its documentation to packfile.h. This prepares for future addition of callers outside the packfile system while maintaining the current behavior where `get_multi_pack_index()` ensures packfiles are prepared before returning the MIDX.

The diff shows clean mechanical movement of code between files with no functional changes - 17 lines added and 8 removed across midx.c, midx.h, packfile.c and packfile.h. The patch continues the series' pattern of establishing clear subsystem boundaries while maintaining all existing behavior, with the packfile store becoming the single source of truth for pack management. This change was pre-approved in earlier discussion rounds and represents an uncontested step in the larger ODB abstraction effort.