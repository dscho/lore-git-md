This is a review response in the ongoing refactoring thread about simplifying object lookup calls. Patrick Steinhardt is responding to René Scharfe's patch that replaces `get_oid_with_context()` with `repo_get_oid_with_flags()` where callers don't need the object context.

Patrick agrees with the general approach but notes two API design quirks that aren't addressed by this patch: (1) the inconsistency between `repo_get_oid_with_flags()` returning `int` while `get_oid_with_context()` returns `enum get_oid_result`, and (2) similar return type issues in related functions like `repo_get_oid_committish()`. He considers these pre-existing issues that don't affect correctness and shouldn't block the current changes.

The review confirms the technical correctness of the conversions while pointing out broader API design questions that could be addressed separately. The discussion remains focused on the object lookup abstraction layer, with Patrick's comments suggesting potential future cleanup directions beyond the current scope.