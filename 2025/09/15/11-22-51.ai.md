This is a feature patch in the Rust infrastructure series, adding build information reporting for Rust support status. As part 4/9 of the v5 series transitioning Git to mandatory Rust support, it exposes whether the current Git build has Rust enabled through `git version --build-options` and `git bugreport` output.

The patch adds a simple conditional block to `help.c` that reports "rust: enabled" or "rust: disabled" based on the `WITH_RUST` compile-time flag. The rationale is that as Git begins shipping with both C and Rust implementations of some subsystems, this information will help diagnose Rust-specific bugs during the transition period.

Technically, this touches the version reporting infrastructure in `help.c`, adding a new build option line following the same pattern as existing features like fsmonitor. The implementation is straightforward `#ifdef` logic matching the build system's `WITH_RUST` toggle introduced in earlier patches of this series. The change is minimal (6 lines added) and focused solely on reporting, not affecting any runtime behavior.

This patch builds on the foundational work from v1-v3 that established the build system infrastructure, now making that configuration visible to end users. It addresses a practical debugging need that emerges from the series' core premise of maintaining dual C/Rust implementations during transition. The implementation appears uncontroversial and well-contained, with no open questions or objections noted in the thread context.