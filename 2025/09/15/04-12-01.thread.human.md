This thread addresses a bug in `git send-email` where using both `--compose` and `--reply-to` options together causes duplicate Reply-To headers, which email providers like Gmail reject. NeilBrown proposed a fix that modifies `git-send-email.perl` to properly handle Reply-To headers during pre-processing, storing them in a variable to prevent duplication (similar to how In-Reply-To and References headers are handled).

Junio C Hamano confirmed the technical approach is sound but requested some polish: the commit message needs to use imperative language and have matching author/Signed-off-by lines. NeilBrown has acknowledged this feedback and plans to resend the patch with these improvements. The core 3-line code change - which solves the header duplication issue without broader impacts - remains unchanged. 

While functionally complete, the patch may still need test coverage for the modified header handling behavior. The maintainer has validated the solution's design, and the thread is now waiting for NeilBrown to resubmit with the requested documentation improvements before final acceptance.