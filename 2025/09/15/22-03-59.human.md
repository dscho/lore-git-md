This is a follow-up discussion in the completed ODB transaction refactoring series, focusing on an edge case in `git update-index` behavior. Justin Tobler is clarifying an earlier observation about option ordering quirks in `git update-index --force-remove` and `--add` operations.

The key points are:
1. There's an inconsistency in how `git update-index` processes `--force-remove` - once encountered, it affects all subsequent file arguments, leading to order-dependent behavior differences.
2. This behavior appears unintentional but is actually relied upon by existing test cases in t1001 and t1002.
3. The discussion stems from earlier work in the series that touched `update-index.c` as part of the ODB transaction changes.

This isn't proposing any code changes - it's just documenting an observed quirk that came up during the refactoring work. The behavior, while potentially surprising, is apparently part of Git's established interface. The email serves as a useful note for future maintainers who might encounter this edge case.