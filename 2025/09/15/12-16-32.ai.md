Bugfix proposal addressing the `update-ref` vs pseudorefs contradiction. Patrick Steinhardt responds to Johannes Sixt's report about the broken merge-abort workflow where `git update-ref` can no longer modify `MERGE_HEAD` despite being Git's own recommended method.

The email proposes a technical solution: special-casing pseudorefs in `git-update-ref` while carefully avoiding treating them as normal refs. Key constraints mentioned include keeping pseudoref updates outside normal ref transactions and limiting the scope to single-pseudoref operations. This would restore the documented functionality while maintaining architectural boundaries.

The proposal directly addresses the thread's core issue - the mismatch between Git's advice (using `update-ref` for pseudorefs) and current behavior (blocking such operations). It suggests a middle path between full pseudoref support and the current blanket prohibition, focusing specifically on the `MERGE_HEAD` use case that triggered the report.

Technical considerations include the pseudoref/normal-ref distinction and transaction handling, suggesting the solution would need careful implementation to avoid bleeding pseudoref logic into the main ref system. The email doesn't provide implementation details but establishes the conceptual approach for discussion.

This moves the thread forward by offering a concrete direction to resolve the contradiction, though it leaves open questions about exact implementation boundaries and whether other pseudorefs beyond `MERGE_HEAD` should be included. The proposal maintains the original goal of backend-agnostic pseudoref manipulation while acknowledging the architectural constraints introduced by 8e4f5c2dc26e.