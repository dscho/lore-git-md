This is part 4/15 of Patrick Steinhardt's refactoring series to centralize packfile management in `struct packfile_store`. The patch moves the packfile path-to-struct mapping from `struct object_database` to the new `struct packfile_store`, completing the relocation of packfile tracking infrastructure.

The change affects how Git tracks which packfiles are already loaded by moving the `pack_map` hashmap and its associated comparison function into the packfile store. The diff shows mechanical updates to all affected call sites, including MIDX handling and packfile installation. The comparison function `pack_map_entry_cmp()` is made static since it's now only used within packfile.c.

This continues the series' clean separation of concerns by making the packfile store the sole owner of packfile tracking state. The changes are straightforward and maintain existing behavior while progressing the larger ODB abstraction effort. With the thread summary indicating all technical concerns were resolved in earlier iterations, this patch appears to be a non-controversial step in the established refactoring direction.