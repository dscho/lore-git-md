This is a follow-up discussion about the `split` subcommand in Patrick Steinhardt's `git-history` RFC series. D. Ben Knoble clarifies that their earlier confusion about the command's output stemmed from expecting to see a diff (like with `commit.verbose=true`) rather than just the modified files list that's actually shown. Patrick acknowledges this is a valid UI expectation but proposes keeping the current simpler implementation for now, with plans to enhance it later after the initial patches land. Ben accepts this approach and apologizes for the earlier confusion.

The exchange highlights a minor but important UI consideration for the new `split` command - whether to show diffs by default like `git commit -v` does. While the current behavior is simpler to implement, there's recognition that showing diffs may be more intuitive for users familiar with Git's existing commit interface. This will likely be revisited after the core functionality is merged. The tone remains collaborative, with both parties working to clarify expectations and find practical solutions.