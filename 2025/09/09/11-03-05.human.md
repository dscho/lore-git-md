Refactoring patch in Patrick Steinhardt's packfile store series (v4 6/15). This moves the "kept packfile" cache from `struct object_database` to the new `struct packfile_store`, completing the relocation of all packfile-related state to its new logical home.

The change is mechanical but significant - it moves the last remaining packfile-specific state out of the object database structure. The kept packfile cache is used by `git-pack-objects` to handle cruft objects, and this patch maintains all existing functionality while improving code organization. The diff shows straightforward updates to the cache accessors to point to the new location.

This continues the series' pattern of clean, incremental refactoring steps toward enabling pluggable object database backends. The patch-specific changes are limited to 3 files (odb.h, packfile.c, packfile.h) with 23 lines added and 15 removed. The commit message clearly explains the motivation and scope, noting this completes the packfile state relocation effort.

As with previous patches in the series, this appears to be a well-executed change with no user-visible impact, maintaining all existing behavior while advancing the architectural goal. The series continues to show high quality and careful attention to detail.