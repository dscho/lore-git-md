Refactoring patch in the xdiff/Rust compatibility series. This is patch 3/17, removing unused fields from xdiff's internal data structures as part of the preparatory cleanups before introducing Rust-compatible types.

The patch eliminates three unused fields: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. The changes show these were initialized but never actually used in the diff algorithm's operation. The removal simplifies the code by:
- Removing hash-related fields and their initialization
- Simplifying `xdl_classify_record()` by removing hash insertion logic
- Cleaning up `xdl_free_ctx()` by removing the rhash deallocation
- Reducing struct definitions in xtypes.h

This appears to be a straightforward cleanup with no behavior change, continuing the series' pattern of incremental improvements to xdiff internals. The changes are mechanical and focused - removing dead code that was likely left over from earlier implementations or optimizations. The patch maintains the series' goal of preparing the codebase for Rust compatibility while keeping current functionality intact.