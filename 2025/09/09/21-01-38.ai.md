This is a technical clarification in the ongoing discussion about `git add` behavior with invalid pathspecs. Jeff King responds to Junio C Hamano's explanation of `--ignore-errors` semantics, agreeing that the option works as documented for permission-related failures but does not address the original issue of mixed valid/invalid pathspecs. 

The exchange reinforces the distinction between two error classes: pathspec validation failures (which always abort) versus file access errors (which `--ignore-errors` can skip). Jeff notes the original reporter's need would require something like a hypothetical `--ignore-missing` flag rather than the existing `--ignore-errors`. 

This moves the discussion forward by eliminating `--ignore-errors` as a solution path while maintaining clarity about Git's current behavior. The thread continues to explore whether the conservative "all-or-nothing" approach should remain or if new functionality should be added to handle partial pathspec matches. Key technical terms remain the PATHSPEC_PREFER_FULL flag and the die_if_path_beyond_symlink() check from earlier messages.