Interface refinement discussion for `git replay --update-refs` functionality.

Elijah Newren raises several pointed questions about the design choices in Siddharth Asthana's patch adding atomic reference updates to `git replay`. The core concerns focus on interface clarity and implementation details:

1. **Flag proliferation**: Questions why three update modes (`--update`, `--update-refs`, and `--batch`) are proposed when the commit message primarily motivated atomic transactions. The descriptions don't clearly differentiate `--update` from `--update-refs`, and `--batch` seems redundant with the batched nature of ref transactions.

2. **Implementation quirk**: Challenges an unexplained initialization change (`result.clean = 1`) that appears to alter the merge result handling contract without clear justification.

3. **Code paths**: Asks whether the `update_ref_direct()` path serves any practical purpose given the focus on atomic transactions, suggesting it might be removable.

This continues the thread's pattern of interface refinement discussions, now drilling into specific implementation choices after earlier rounds addressed broader design philosophy. Newren's questions highlight areas where the current patch may need clearer motivation or simplification before proceeding. The exchange remains constructive but shows the careful scrutiny applied to command-line interface design in Git.