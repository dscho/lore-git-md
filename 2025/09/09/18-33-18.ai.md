This is a retraction and clarification in the ongoing discussion about `git add`'s behavior with invalid pathspecs. Jon Forrest follows up on his earlier suggestion for a new command-line flag to control how `git add` handles invalid files, realizing that the functionality he proposed already exists via the `--ignore-errors` option. 

The email serves as a correction to his previous proposal, acknowledging that Git already provides the lenient behavior he was suggesting through an existing mechanism. This resolves the immediate question about whether new flag infrastructure would be needed, as the desired "ignore invalid files" mode is already available. 

The thread context reveals this closes one avenue of discussion (adding new flags) while leaving open the broader questions about whether the default behavior should remain strict or if `--ignore-errors` should be more prominently documented for this use case. The realization about existing functionality may shift the conversation toward documentation improvements rather than behavior changes.

Key technical details for future reference: the `--ignore-errors` flag provides the alternative behavior Jon was seeking, and this interaction confirms it works for the pathspec validation case being discussed. The thread's core technical considerations (PATHSPEC_PREFER_FULL, exit code semantics) remain relevant for understanding why the default behavior exists, but the immediate proposal for new flag-based control is now moot.