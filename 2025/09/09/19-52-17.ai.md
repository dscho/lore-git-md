This is a continuation of the discussion about making `--update-refs` the default behavior for `git replay`, part of the ongoing feature development for atomic reference updates.

Elijah Newren responds to Junio Hamano's concerns about changing default behavior after 18 months of usage, clarifying that `git replay` was marked as experimental in four prominent places (including the command's usage message). He emphasizes this makes it different from typical experimental commands that only have a single warning marker. The exchange reveals agreement that if the new behavior is clearly better for most use cases, changing the default is acceptable, though preserving backward compatibility via an option would still be valuable.

Key technical points from this exchange:
- The experimental status is shown in `builtin/replay.c`'s usage message (visible via `-h`)
- Discussion is converging toward making `--update-refs` behavior default while keeping the old output-as-commands behavior available via a new flag
- Both maintainers acknowledge the tension between interface improvement and real-world adoption constraints

This moves the thread closer to resolution on the major open question about default behavior, while maintaining focus on the server-side workflow benefits of atomic transactions. The exchange suggests the change may proceed if the documentation clearly communicates the experimental nature and transition path.