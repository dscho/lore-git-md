This is patch 7/17 in a refactoring series preparing xdiff internals for Rust compatibility. Elijah Newren reviews Ezekiel Newren's patch that removes redundant fields from the xdlclass_t struct by consolidating them into an embedded xrecord_t. The changes are straightforward - replacing three separate fields (ha, line, size) with a single xrecord_t struct containing those same fields.

The patch appears to be part of the series' broader goal of simplifying xdiff's internal structures to make them more Rust-friendly. While the technical changes look correct, Elijah notes the commit message lacks motivation - it would help reviewers understand whether this is primarily about reducing redundancy, simplifying future Rust integration, or some other goal.

This continues the pattern seen in earlier patches of removing intermediate wrapper structures (like patch 5's removal of diffdata_t) to make the code more direct. The changes are localized to xprepare.c and maintain the same behavior while reducing the number of fields to track. As with previous patches in this series, the focus is on internal cleanup rather than functional changes.