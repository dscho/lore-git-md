This is a performance optimization discussion in the ongoing thread about managing inode exhaustion on NFS servers hosting many small Git repositories.

Brian M. Carlson responds to Kousik Sanagavarapu's implementation report about aggressive GC settings, focusing on the performance implications. They note that `transfer.unpackLimit=1` (previously recommended) is widely used at major forges and shouldn't cause performance issues, as packed objects enable optimizations like bitmaps.

The key technical point is a warning about Kousik's current GC settings (`gc.auto=1` and `autopacklimit=1`), which would trigger a repack after every push. Brian notes this could be prohibitively expensive, sharing that their workplace uses a less aggressive schedule (repacking every ~40 pushes) as a practical reference point.

This email advances the thread by shifting focus from configuration options to their operational impact, particularly the tradeoff between inode conservation and repacking overhead. It introduces concrete performance data points (40 pushes between repacks) that may serve as a benchmark for future discussion. The exchange continues to center around the core constraints of high push volume and immutable NFS/ext4 infrastructure while moving toward practical implementation guidance.