Platform compatibility follow-up confirming HP-UX mkdtemp() workaround approach. Michael Osipov validates Patrick Steinhardt's proposed solution to extend clar's platform-specific fallbacks for mkdtemp() to include HP-UX systems, reporting successful testing with `__hpux` macro (note lowercase spelling difference from Patrick's initial guess). The confirmation shows the exact working patch modifying clar/sandbox.h to treat HP-UX like Solaris/Tandem systems, using mktemp()+mkdir() when `__hpux` is defined.

This moves the thread toward resolution by:
1. Confirming HP-UX does provide mktemp(3p) as required by the fallback path
2. Identifying the correct preprocessor macro (`__hpux`) for HP-UX detection
3. Demonstrating the solution works in practice on an HP-UX system

The email implicitly endorses Patrick's upstream-first approach by asking him to propagate the fix to clar's main repository, which would benefit other projects using the test framework. This aligns with the thread's earlier discussion about avoiding Git-specific workarounds when possible. The successful test case provides the missing verification needed to proceed with the framework-level solution rather than the earlier local compat/posix.h inclusion approach.