This is a documentation patch in the ongoing `git replay` atomic ref updates thread. Andrei Rybak points out a formatting issue in Siddharth Asthana's documentation patch for the new `--update-refs` and `--batch` options. The problem is that the new examples have been inserted between the existing examples and the explanatory paragraph that refers to them, breaking the logical flow.

The thread context shows this is part of a larger discussion about implementing atomic reference updates in `git replay`, with active debate about interface design and deployment concerns. While this particular email is a minor documentation nit, it comes during a phase where the project is carefully refining the user-facing documentation of these new features.

The patch being discussed adds examples showing three usage modes of `git replay` with the new ref update functionality: direct ref updates (`--update`), atomic transactions (`--update-refs`), and batched updates with partial failure tolerance (`--batch`). Rybak's observation helps ensure the documentation maintains clear connections between examples and their explanations.