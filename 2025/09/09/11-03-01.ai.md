This is patch 2/15 in Patrick Steinhardt's v4 series refactoring packfile management into `struct packfile_store` as part of the ODB abstraction effort. The patch moves the list of packfiles from `struct object_database`'s `packed_git` field into the new `packfile_store` structure as the `packs` field.

Key technical details:
- Removes `packed_git` from `struct object_database`
- Adds `packs` to `struct packfile_store`
- Moves packfile closing/freeing logic from `odb_clear()` to new `packfile_store_close()` and `packfile_store_free()` functions
- Updates all packfile iteration code to use `store->packs` instead of direct `odb->packed_git` access
- Maintains same behavior while changing internal structure
- Touches 4 files (odb.c, odb.h, packfile.c, packfile.h)

The changeset is mechanical and straightforward, following the architectural direction established in the series. It demonstrates the incremental approach by moving one subsystem at a time while maintaining all existing functionality. The patch appears ready for merging as part of the larger series, with no outstanding technical concerns noted in the thread.