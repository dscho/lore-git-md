This is a technical refinement to the command deprecation framework series, specifically addressing the `--list-cmds=deprecated` implementation. Patrick Steinhardt suggests several code improvements to patch 1/8 of the series.

The main suggestion is to combine the two separate loops in `list_builtins()` (one for include_option and one for exclude_option) into a single loop with conditional checks, reducing code duplication. He also notes some line-wrapping opportunities in the function's documentation and declaration.

The email also raises the broader question of whether `command-list.txt` should be updated to reflect deprecated commands, though this appears to be a follow-up consideration rather than a blocking issue for the current patch. The suggestion aligns with the series' goal of improving visibility of deprecated commands.

These are relatively minor code organization suggestions that don't affect the functionality already implemented in the series. The thread context shows this is part of a mature discussion that has largely reached consensus, with Patrick offering final polish to the implementation details.