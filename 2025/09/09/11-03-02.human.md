Refactoring patch in the packfile store series. This is patch 3/15 in Patrick Steinhardt's v4 series introducing `struct packfile_store` to centralize packfile management as part of Git's object database abstraction effort.

The patch moves the `packed_git_initialized` flag from `struct object_database` to the new `struct packfile_store`, where it more naturally belongs. As part of this move, it also converts the flag from a bitfield to a proper boolean type now that's allowed in Git's codebase. The change is purely internal - no behavior changes, just better organization.

The changes touch 3 files with straightforward mechanical updates to the flag's declaration, initialization, and usage sites. This continues the series' pattern of incrementally moving packfile-related functionality to the new store structure while maintaining all existing behavior. The patch appears cleanly implemented with appropriate updates to both header definitions and all usage sites.

This is one of the smaller steps in the larger refactoring effort, but helps keep related functionality properly colocated in the new structure. The series as a whole has already addressed major architectural questions in previous rounds, with only minor documentation nits remaining.