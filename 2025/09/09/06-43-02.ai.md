This is a technical refinement to the command deprecation framework series, specifically addressing the implementation of `--list-cmds=deprecated`. Patrick Steinhardt responds to Kristoffer Haugsbakk's patch with suggestions to improve the code structure and maintainability.

The key technical points in this response:
- Proposes adding a new category for deprecated commands in documentation to actively discourage their use
- Notes overly long lines in the function declaration and definition that should be wrapped
- Suggests combining the two filtering loops in `list_builtins()` into a single loop with both include/exclude checks, reducing code duplication
- Maintains the same logical behavior while simplifying the implementation

The changes would affect:
- `git.c` (command listing implementation)
- The `list_builtins()` function interface and implementation
- Potential follow-on documentation updates for deprecated commands

This is a minor but thoughtful improvement to the series' technical foundation, showing attention to code quality and maintainability. The suggestion doesn't change any user-facing behavior but would make the implementation cleaner. The thread context shows this is part of the now-complete technical foundation for the deprecation framework, with all major design points already settled.