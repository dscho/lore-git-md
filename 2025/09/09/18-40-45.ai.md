This is a follow-up in the thread about `git add`'s behavior with mixed valid/invalid pathspecs. Jeff King points out that while Jon Forrest had suggested a new flag to control this behavior, there's actually an existing `--ignore-missing` option that provides similar functionality - though with the limitation that it only works in `--dry-run` mode. 

The email highlights an important technical detail about the current implementation: while the desired lenient behavior exists, it's restricted to dry-run operations. This suggests the core functionality for partial success exists in the codebase but isn't fully exposed to users. Jeff doesn't speculate why this limitation exists, leaving that as an open question.

This response moves the discussion forward by identifying existing code that partially addresses the requested behavior, while also surfacing a new dimension to consider - why the ignore functionality is currently limited to dry-run mode. The thread's central tension remains between Git's conservative default behavior and the desire for more flexible path handling, but now with additional context about partial implementations in the codebase.