This is a response email in the ongoing thread about adding atomic reference updates to `git replay` via a new `--update-refs` option. The email clarifies a point of confusion from the original cover letter regarding the introduction of both `--update` and `--update-refs` flags in the patch series.

Siddharth Asthana acknowledges Christian Couder's correction that both flags are new (not just `--update-refs` as initially stated) and maintains the focus on server-side use cases like Gitaly that benefit from atomic transactions. The exchange shows the documentation-first review approach Christian is taking to validate the flag design before implementation review.

Key technical details carried forward from the thread context:
- Core functionality remains unchanged: atomic transactions by default using `ref_store_transaction_begin()`
- Still targets bare repositories
- Maintains backward compatibility when not using new options
- Documentation synchronization remains an active concern

The email doesn't introduce new technical content but helps resolve a documentation ambiguity about which flags are being introduced. This keeps the thread moving toward consensus on the flag naming and behavior that Christian had previously suggested simplifying to a single `--update-refs` with modes rather than multiple flags.