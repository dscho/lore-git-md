Platform compatibility follow-up confirming upstream clar framework fix for HP-UX test builds.

Patrick Steinhardt confirms he has merged the HP-UX compatibility fix into the clar test framework (pull request #120) and will send a patch to update Git's version of clar. The solution uses `__hpux` (lowercase, as Michael Osipov confirmed was correct) alongside existing platform-specific fallbacks for Solaris and Tandem systems, employing the same mktemp()+mkdir() pattern. This resolves the original linking failure while maintaining consistency with the framework's existing portability approach.

The email thread shows the issue moving from problem identification through solution validation to upstream implementation. With the framework change merged, the remaining work is updating Git's copy of clar, which Patrick indicates he'll handle. This represents a clean resolution to the HP-UX build issue without requiring Git-specific workarounds, following the pattern established for other platforms in the test framework.

Key technical points for future reference:
- Framework fix uses `defined(__hpux)` macro check
- Follows existing mktemp(3p) fallback path used for Solaris/Tandem
- No longer requires local Git modifications (compat/posix.h inclusion or Makefile changes)
- Maintains clar's portable tempdir creation approach

The solution demonstrates how platform-specific test framework adaptations can be handled upstream rather than in individual projects, providing a template for future portability issues as unit testing expands across platforms.