This is part 10/15 of Patrick Steinhardt's packfile store refactoring series, continuing the effort to separate packfile management from the object database abstraction. The patch refactors `install_packed_git()` to work directly with a packfile store rather than a repository, renaming it to `packfile_store_add_pack()` to better reflect its purpose.

The change updates all callers across 7 files (fast-import.c, index-pack.c, http.c, http.h, midx.c, packfile.c, packfile.h) to use the new interface. The core functionality remains the same - adding a packfile to the store so its objects become accessible - but now operates directly on the packfile store structure rather than going through the repository layer. The patch includes updates to documentation comments to reflect the new function name and purpose.

This continues the series' pattern of mechanical, focused changes that improve code organization while maintaining identical functionality. The change helps clarify the separation between packfile management and the object database abstraction, moving toward the goal of enabling pluggable backends. No new technical concerns are introduced, and the change appears consistent with the series' established approach.