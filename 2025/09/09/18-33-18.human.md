This is a retraction and clarification regarding the `git add` behavior discussion. Jon Forrest follows up on their earlier proposal for a new flag to control how `git add` handles invalid pathspecs, realizing that the requested functionality already exists via the `--ignore-errors` option. 

The email serves as a correction to the previous discussion thread where Jon had suggested adding a new option to make `git add` ignore invalid files while processing valid ones. Upon checking the documentation, they discovered this behavior is already available through the existing `--ignore-errors` flag, which causes `git add` to continue with valid files even when encountering errors.

This effectively resolves the earlier exploration of modifying `git add`'s behavior, as the desired lenient mode turns out to be an existing feature rather than a missing one. The thread can now be considered closed with no changes needed to Git's implementation. The exchange highlights how even experienced contributors can occasionally overlook existing functionality, and serves as a good reminder to consult documentation before proposing new features.