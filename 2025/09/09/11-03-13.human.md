Refactoring patch in Patrick Steinhardt's packfile store series (14/15). This continues the effort to better separate packfile management into its own subsystem by making `get_all_packs()` operate directly on a packfile store rather than a repository.

The change updates 19 files to use the new `packfile_store_get_packs()` interface instead of the repository-scoped `get_all_packs()`. The diff shows mechanical conversions across various commands like `git fsck`, `git gc`, and `git pack-objects` that need to iterate over packfiles. The patch maintains identical behavior while clarifying the packfile store's role as the proper owner of packfile management.

This is part of the incremental, non-disruptive refactoring approach seen throughout the series - each patch makes focused changes while keeping all existing functionality working. The change fits naturally with previous patches that moved packfile-related state into the packfile store. No user-visible changes or performance impacts are expected from this internal API cleanup.