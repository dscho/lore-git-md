This is a technical discussion about submodule gitdir path encoding, specifically addressing version compatibility concerns in the ongoing series.

Phillip Wood follows up on Adrian Ratiu's response about whether older Git versions need protection from accessing the new `.git/submodules/` directory structure. While Adrian initially expressed uncertainty, Phillip provides additional technical context about submodule access patterns that strengthens the case for version protection. 

The key technical point is that submodule gitdir paths aren't only resolved during initialization - they're also accessed directly in other operations, meaning older Git versions could encounter the new paths without going through the `.git` file resolution that would handle the translation. This makes the proposed `extensions.submoduleEncoding` config key more clearly necessary as a version gate.

The discussion remains focused on implementation details rather than design alternatives, with both contributors now agreeing the version marker is needed for safety. This email doesn't introduce new alternatives like Patrick Steinhardt's earlier suggestions about repository extensions or directory structures, but reinforces one specific technical requirement (version compatibility) that affects the current approach.

The thread context shows this is part of broader discussions about path length validation and cross-implementation concerns, but this particular exchange is narrowly focused on the version marker question that was raised in earlier reviews. The consensus forming here suggests this specific aspect may be settled in the next iteration of the patch series.