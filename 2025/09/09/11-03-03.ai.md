Refactoring patch in Patrick Steinhardt's packfile store series (v4 4/15), moving the packfile path-to-struct mapping from `struct object_database` to the new `struct packfile_store`. This continues the architectural work to centralize packfile management.

The key changes:
- Moves the `pack_map` hashmap from `struct object_database` to `struct packfile_store` (renamed to just `map`)
- Converts the `pack_map_entry_cmp()` helper to static since it's now only used in packfile.c
- Updates all callers to use the new location
- Maintains identical functionality while cleaning up the ODB interface

Technical details:
- The map tracks loaded packfiles by path to avoid duplicate loading
- Changes span 5 files with 25 lines added/removed
- Touches midx.c, odb.c, odb.h, packfile.c, packfile.h
- Follows the series' pattern of mechanical moves with no behavior changes
- Builds on patch 2/15's foundation and precedes patch 5/15's work

The change is straightforward and maintains the series' high quality, with all references to the new location properly updated. This continues the incremental progress toward the larger goal of ODB abstraction for pluggable backends.