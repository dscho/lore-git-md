Refactoring patch in Patrick Steinhardt's packfile store series (v4 14/15). This continues the effort to consolidate packfile management by refactoring `get_all_packs()` to operate directly on a packfile store rather than a repository. The change affects 19 files across the codebase, updating all callers to use the new `packfile_store_get_packs()` interface.

Key technical details:
- Renames `get_all_packs()` to `packfile_store_get_packs()`
- Updates function to take `struct packfile_store*` instead of `struct repository*`
- Maintains identical behavior while better encapsulating packfile access
- Touches callers in commands like `git gc`, `git fsck`, `git repack`, and test helpers
- Follows the series' pattern of moving packfile operations to the dedicated store subsystem

The patch is mechanical and maintains all existing functionality while improving code organization. It builds on previous patches that moved packfile-related fields to `struct packfile_store`. The change is part of the incremental approach to separating packfile management from the object database.

This continues the high-quality refactoring seen throughout the series, with careful attention to maintaining behavior while improving interfaces. The patch fits naturally with the architectural direction established by earlier patches.