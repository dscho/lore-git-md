This is part 10/15 of Patrick Steinhardt's ongoing refactoring series to better separate packfile management from the object database. The patch renames `install_packed_git()` to `packfile_store_add_pack()` and updates it to work directly with a packfile store rather than a repository object. This continues the series' goal of moving all packfile-related functionality into the new packfile store structure.

The change is mechanical but touches several callers across fast-import, index-pack, http, and midx code. The new function signature better reflects that this is specifically about packfile management rather than general repository operations. The patch maintains all existing behavior while improving the architectural separation.

This appears to be another clean, focused step in the series' progression toward enabling pluggable object database backends. The changes are straightforward and well-contained, with no new technical concerns raised in the thread so far. The series continues to demonstrate careful, incremental refactoring that maintains backward compatibility while improving the internal structure.