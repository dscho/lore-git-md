This is a technical clarification in the ongoing discussion about `git add`'s behavior with invalid pathspecs. Jeff King corrects Jon Forrest's assumption that `--ignore-errors` provides the desired lenient behavior for mixed valid/invalid pathspecs. Through concrete examples, he demonstrates that `--ignore-errors` doesn't actually change the fundamental behavior - Git still fails completely when encountering invalid pathspecs, though the status output confirms the valid file (`foo`) remains unstaged.

The email highlights a subtle but important distinction in Git's error handling mechanisms. While `--ignore-errors` might sound like it would ignore invalid pathspecs, it appears to serve a different purpose (likely related to other types of errors during staging). This reinforces the thread's core theme about Git's conservative design philosophy and the technical boundaries of its existing partial success implementations.

The exchange surfaces new questions about the documentation of `--ignore-errors` and whether its behavior (or name) might be misleading to users expecting more lenient pathspec handling. This builds on earlier discussion points about the `--ignore-missing` option's dry-run limitation, suggesting the codebase may need clearer distinctions between different types of "ignore" behaviors for pathspec validation versus other error conditions.