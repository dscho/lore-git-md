This is part 4/6 of a refactoring series moving ODB transaction code from bulk-checkin to object-file. The patch relocates all transaction-related code from bulk-checkin.[ch] to object-file.[ch], deleting the now-empty bulk-checkin files and updating all callers to use the new location.

The change simplifies the exposed interface by reducing it to just `begin_odb_transaction()` and `end_odb_transaction()`, with the implementation details now properly encapsulated within object-file.c. The diff shows significant code movement (386 lines added, 446 removed) as the bulk-checkin functionality is absorbed into object-file.c while maintaining all existing behavior.

This continues Patrick Steinhardt's work to prepare Git's object database for pluggable backends by making transaction handling more generic. The patch appears well-executed, with thorough updates to all call sites and build system files. The commit message clearly explains the motivation and scope of the change.