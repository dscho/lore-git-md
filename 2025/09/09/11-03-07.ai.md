This is part 8/15 of Patrick Steinhardt's packfile store refactoring series, continuing the effort to move packfile management into a dedicated `packfile_store` structure as part of the ODB abstraction work. The patch refactors the `prepare_packed_git()` function and related helpers to operate on a `packfile_store` rather than a `repository` object, clarifying their scope and dependencies.

Key technical changes:
- Renames `prepare_packed_git()` to `packfile_store_prepare()` and updates its signature
- Moves MRU list initialization into new `packfile_store_prepare_mru()`
- Removes standalone `rearrange_packed_git()` helper by inlining its sort operation
- Updates all callers (7 locations) to use the new interface
- Maintains identical behavior while improving encapsulation

The changes affect packfile initialization and access across multiple functions including `get_packed_git()`, `get_all_packs()`, and `find_pack_entry()`. The patch follows the series' pattern of mechanical, behavior-preserving refactoring that moves state management into the new `packfile_store` structure. No user-visible changes or performance impacts are introduced.

This continues the architectural goal of separating packfile concerns from the broader repository/ODB interfaces, with all substantive technical decisions having been settled in earlier patches. The changes are straightforward and maintain the series' high quality standards.