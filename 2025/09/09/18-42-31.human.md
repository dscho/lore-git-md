Follow-up clarification on `git add` behavior options.

Jeff King responds to Jon Forrest's realization that `--ignore-errors` exists for `git add`, pointing out that it doesn't actually solve the original issue of partial success with mixed valid/invalid pathspecs. Through a simple test case, he demonstrates that `--ignore-errors` still fails completely when given a non-existent file, despite the option's name suggesting it might ignore such errors.

This email serves as a technical correction to the previous assumption, showing that the existing `--ignore-errors` flag behaves differently than what was needed in the original use case. The demonstration is clear and concrete, using a minimal reproduction that shows the current behavior doesn't match the intuitive expectation that invalid paths would be skipped while valid ones are processed.

The exchange highlights a potential documentation issue where option names might mislead users about their actual behavior, though this isn't stated explicitly. The thread continues to explore the gap between what functionality exists and what users might expect when dealing with partial failures in pathspec handling.