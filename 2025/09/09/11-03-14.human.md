This is the final patch (15/15) in Patrick Steinhardt's series refactoring packfile access to use a dedicated packfile store rather than operating directly on the repository. The patch renames `get_packed_git_mru()` to `packfile_store_get_packs_mru()` and updates it to work directly with the packfile store, completing the architectural shift established in previous patches.

The change is mechanical - it touches only 3 files (packfile.c, packfile.h, and builtin/pack-objects.c) to update the function signature and its single caller. The functionality remains identical, just better encapsulated within the packfile store subsystem. The commit message clearly explains the scope of the function and why it's being refactored.

This patch follows the same high-quality pattern seen throughout the series: careful, incremental changes that improve code organization without altering behavior. With this change, the series appears complete - all packfile-related operations have been moved to operate on the packfile store rather than the repository directly. The series has received positive feedback and appears ready for merging.