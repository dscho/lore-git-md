Refactoring patch in Patrick Steinhardt's packfile store series (v4 11/15). This patch introduces a new helper function `packfile_store_load_pack()` that consolidates the common pattern of checking for and loading packfiles into the packfile store. The function handles the key reconstruction, packfile lookup, and store registration in one place.

The change replaces several similar but slightly different implementations across fast-import.c, index-pack.c, midx.c, and packfile.c with calls to the new helper. The refactoring maintains all existing behavior while reducing code duplication. The patch follows the series' pattern of clean, mechanical improvements to packfile management as part of the larger ODB abstraction effort.

This appears to be a straightforward quality-of-life improvement for packfile handling code. The changes are well-contained and the commit message clearly explains the motivation. The patch-specific changes look correct and consistent with the series' architectural direction. No new concerns are introduced beyond what was already discussed in earlier patches of the series.