This is a follow-up discussion on a refactoring patch in the string-list API series. Patrick Steinhardt responds to Shejialuo's patch that replaced a negative index encoding scheme with an explicit `exact_match` boolean parameter in the string-list API.

Patrick generally approves of the change, noting it looks cleaner than the previous implementation. He points out how this change relates to the preceding patch that made the parameter optional (by allowing NULL). The main suggestion is to consider using `bool *exact_match` instead of `int *exact_match` to better indicate the binary nature of the parameter, though he notes this would require updating the signature in the previous commit as well.

The discussion is constructive and technical, focusing on API clarity rather than any objections to the core change. It shows the careful attention to detail in this refactoring series aimed at improving type safety in the string-list API.