This is a discussion about the submodule gitdir path encoding series, specifically addressing version compatibility concerns. Phillip Wood responds to Adrian Ratiu's earlier message about whether older Git versions would be able to handle the new `.git/submodules/` directory structure.

Phillip agrees with Adrian's assessment that an `extensions.submoduleEncoding` config key is likely needed to prevent older Git versions from attempting to read submodules stored in the new path structure. He notes that if submodule access only happened during initialization (via the `.git` file in the working tree), older clients might work fine, but since submodule access happens in other contexts too, the version check would be safer.

This continues the thread's theme of ensuring backward compatibility while introducing the new path encoding scheme. The exchange shows the careful consideration being given to how this change might affect users who switch between Git versions. The discussion remains technical and constructive, with both participants focused on preventing potential issues rather than debating the approach itself.