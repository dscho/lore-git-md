This is a design discussion about the `--update-refs` interface for `git replay`, part of the ongoing atomic reference updates feature series.

Christian Couder provides detailed feedback on the proposed interface, suggesting clearer naming and documentation. He prefers distinct flags like `--atomic-update` and `--batch-update` over the current `--update`/`--update-refs` approach, noting the potential confusion with `git rebase --update-refs` which serves a different purpose. The discussion reveals that the current implementation's `--update` flag provides non-atomic behavior matching `git update-ref --stdin`, while `--update-refs` offers atomic transactions by default with `--batch` allowing partial updates.

Christian emphasizes the importance of thorough documentation to explain the differences between these modes and their relationship to piping to `git update-ref`. He suggests either consolidating to a single `--update-refs` with modes or using more descriptive flag names, while maintaining the ability to match the non-atomic `update-ref` behavior when needed.

This email continues the thread's focus on refining the user interface design, with Christian providing concrete suggestions to improve clarity and avoid confusion with similar rebase functionality. The discussion remains productive, focusing on finding the most intuitive interface for server-side workflows while maintaining backward compatibility.