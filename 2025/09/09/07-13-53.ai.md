This is a feature proposal discussion for adding atomic reference updates to `git replay`. Elijah Newren responds to Siddharth Asthana's patch series introducing `--update-refs` functionality, questioning the design choice of making atomic updates opt-in rather than default behavior.

Newren raises two key points about the interface design:
1. Questions why atomic updates aren't the default behavior, suggesting the experimental nature of `git replay` makes this a good time to change defaults
2. Notes ambiguity in the stated server-side benefits, suggesting the commit message could more clearly specify which aspects (atomicity, bare repo support, or ergonomics) are most value for server workflows

The response indicates general agreement with the technical approach (use of transactions, bare repo support, silent success output) but focuses on UX considerations. This continues the thread's ongoing discussion about interface design, now shifting from flag naming (covered in previous exchanges) to default behavior philosophy.

For future summarization, note this exchange introduces:
- The question of whether atomic updates should be default behavior
- Need for clearer motivation about server-side benefits
- Implicit assumption that experimental commands can change defaults more freely
- Continued focus on `git replay`'s server-side use cases (GitLab/Gitaly)