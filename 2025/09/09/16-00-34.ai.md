This is a follow-up discussion about Git's `git add` behavior with invalid pathspecs, continuing the thread examining whether partial success should be allowed. Jon Forrest responds to Jeff King's technical concerns about exit code signaling by proposing an optional command-line flag to control behavior - maintaining the current strict failure mode by default while allowing an opt-in "ignore invalid files" mode. The suggestion addresses the backward compatibility concerns raised earlier while providing a path forward for users who want more lenient behavior. No implementation is provided, positioning this as a conceptual proposal to gauge interest rather than a concrete patch. The email keeps the discussion alive but doesn't significantly advance the technical considerations beyond what was already covered in the thread's historical analysis. Key technical elements remain the PATHSPEC_PREFER_FULL flag and exit code semantics, with the new suggestion being the flag-based opt-in approach. The thread remains in exploratory phase with no clear consensus yet on whether to change behavior or maintain status quo.