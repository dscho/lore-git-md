This is a follow-up in the ongoing discussion about `git add`'s behavior with mixed valid/invalid pathspecs. Jeff King points out that while Jon Forrest had suggested a new flag to control this behavior, there's actually an existing `--ignore-missing` option that already provides similar functionality - though with the limitation that it only works in `--dry-run` mode.

The email serves as a helpful pointer to existing functionality rather than proposing new changes. It's notable that Jeff King doesn't dig into why `--ignore-missing` is restricted to dry-run mode, leaving that as an open question. This suggests the thread may be nearing resolution, with the main remaining question being whether to expand the existing option's capabilities versus documenting the current behavior more clearly.

The exchange shows the value of experienced contributors recalling existing features that address reported issues, even if those features have their own limitations. This moves the discussion forward from debating new solutions to examining whether existing ones can be adapted or better documented.