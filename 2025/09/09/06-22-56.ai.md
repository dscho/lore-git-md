This is a follow-up discussion on the string-list API refactoring series, specifically addressing patch 2/4 which introduced an explicit `exact_match` boolean parameter to replace the previous negative index encoding scheme. Patrick Steinhardt provides positive feedback on the cleaner implementation in `mailmap.c` that results from this change.

The email highlights two technical points: First, it notes that the patch builds on the previous change that made the `exact_match` parameter optional (by allowing NULL). Second, Patrick suggests a potential improvement - using `bool *exact_match` instead of `int *exact_match` to better indicate the parameter's binary nature, though he acknowledges this would require updating the signature in the preceding commit as well.

This is a minor refinement suggestion rather than a substantive critique, continuing the thread's pattern of constructive feedback on the type safety improvements. The core functionality and approach of the series remain uncontested, with this email focusing on a small API documentation detail that could make the interface slightly more self-documenting. The change would affect `string-list.h` and any callers passing the `exact_match` parameter, though the impact would be limited since the values are already treated as boolean in practice.