This is a follow-up discussion in the ongoing thread about making `git replay` update refs atomically by default. Elijah Newren responds to Junio Hamano's concerns about changing the default behavior of `git replay` after 18 months of usage, even though the command is marked experimental.

Newren emphasizes that `git replay` was marked as experimental more prominently than usual - not just in documentation but also in the command's usage message (visible via `git replay -h`). He provides the exact code snippet showing the "(EXPERIMENTAL!)" warning in the command's help text. This counters the concern that users might not be sufficiently warned about potential behavior changes.

The exchange reveals alignment between Newren and Hamano that if the new behavior (atomic ref updates) is clearly better for most use cases, changing the default would be acceptable. They agree that maintaining backward compatibility through an option to get the old behavior (outputting update commands without applying them) would still be valuable.

This moves the discussion forward by addressing the key concern about changing defaults for an "established" experimental command, while maintaining focus on the technical merits of the proposed change. The thread continues to balance interface design improvements against real-world adoption concerns.