This is a refactoring patch in the xdiff Rust compatibility series, specifically removing the redundant `diffdata_t` struct that was just aliasing fields from `xdfile_t`. The patch demonstrates the series' incremental cleanup strategy by simplifying the code structure while maintaining identical behavior.

The changes are straightforward: the patch removes `diffdata_t` and updates all callers to directly access the `xdfile_t` fields it was wrapping. The commit message clearly documents the field mappings being eliminated (`nrec`->`nreff`, `ha`->`ha`, `rindex`->`rindex`, `rchg`->`recharge`). The diff shows mechanical updates to `xdl_recs_cmp()` and `xdl_do_diff()` to use the underlying struct directly.

Elijah Newren's review comment notes that viewing with `--color-moved` helps verify the structural changes match the commit message's description, suggesting this is a clean, well-explained refactoring. The changes touch core diff algorithm code in `xdiff/xdiffi.c` and its header, removing indirection without altering behavior - exactly the kind of preparatory cleanup the series aims for before Rust integration work.

This continues the pattern seen in earlier patches (like patch 4's removal of `xdl_get_rec()`) of simplifying xdiff's internal APIs. The changes appear uncontroversial and well-contained, though reviewers will want to verify no subtle behavioral changes were introduced in the field access conversions. The patch maintains the series' focus on mechanical, verifiable transformations that pave the way for later Rust compatibility work.