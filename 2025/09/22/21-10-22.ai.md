Bugfix patch addressing inconsistency in Git notes display between main commit message and range-diff output in `git format-patch`.

This patch is part 1 of a 2-part series fixing an inconsistency where notes attached to commits appear differently in the main message versus the range-diff section when using `git format-patch` for single-commit series. The issue occurs because notes handling wasn't properly propagated to the range-diff subprocess in the single-commit case.

The change moves the `other_arg` handling from a local variable in `builtin/log.c` to a new `rdiff_other_arg` member in the `rev_info` struct, laying groundwork for the actual fix in part 2. This is a preparatory refactoring with no functional changes yet. The patch modifies `builtin/log.c` to use the new struct member and adds proper cleanup in the format-patch command.

Key technical details:
- Adds `rdiff_other_arg` to `rev_info` struct in `revision.h`
- Updates notes handling in `cmd_format_patch` to use the new member
- Maintains memory safety with proper cleanup via `strvec_clear`
- Part of a focused series addressing a specific notes display inconsistency
- Small change (7 lines net) with clear commit message explaining the context

The patch appears well-crafted with attention to memory management and follows the established pattern for similar range-diff handling. The next patch in the series will presumably complete the fix by using this new member in `log-tree.c`.