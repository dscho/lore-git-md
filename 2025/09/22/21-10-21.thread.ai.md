Thread identity: Two-part patch series addressing `git format-patch` inconsistency in Git notes display between main commit message and range-diff output for single-commit series.

Core problem: When using `git format-patch` with range-diff, notes attached to commits are handled consistently - in single-commit cases, `--no-notes` was being ignored and notes appeared differently in the main message versus the range-diff section due to notes arguments not being properly propagated to the range-diff subprocess.

Current status: Series at v1 with both parts under review. Part 1 was preparatory refactoring moving `other_arg` handling to new `rdiff_other_arg` member in `rev_info`. Part 2 completes the fix by using this member to properly propagate notes arguments to range-diff subprocess in single-commit cases. Junio C Hamano has reviewed both parts, identifying initialization, naming, and commit message concerns.

Key technical details:
- Added `rdiff_other_arg` strvec member to `rev_info` struct in `revision.h` (part 1)
- Current implementation lacks proper initialization in `REV_INFO_INIT`
- Member name `rdiff_other_arg` may be too generic according to review
- Modified `builtin/log.c` to use new struct member (part 1)
- Updated `show_diff_of_diff` in `log-tree.c` to pass `opt->rdiff_other_arg` (part 2)
- Added test case for single-commit scenario with `--notes=custom`
- Test verifies consistent notes display in both main message and range-diff
- Maintains existing behavior for multi-commit cases
- Uses `test_grep` for pattern matching in new test
- Follows proper cleanup patterns with `test_when_finished`

Open questions:
- Need to address initialization oversight in `REV_INFO_INIT`
- Should `rdiff_other_arg` be renamed to better reflect its specific purpose?
- Commit message needs clarification about `--no-notes` implications
- Review feedback suggests the member's role may need clearer definition

Participant positions:
- Junio C Hamano reviewed both parts, noting initialization, naming, and commit message concerns
- No Reviewed-by or Acked-by tags yet
- Review suggests v2 will be needed to address technical feedback
- Review indicates technical approach is sound but needs refinement

Related work: Touches same revision walking code that handles notes display, though not directly to other ongoing efforts. The change is focused on this specific format-patch behavior inconsistency.