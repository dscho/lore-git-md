Thread identity: Two-part patch series addressing `git format-patch` inconsistency in Git notes display between main commit message and range-diff output for single-commit series.

Core problem: When using `git format-patch` with range-diff, notes attached to commits appear differently in the main message versus the range-diff section due to notes handling not being properly propagated to the range-diff subprocess in single-commit cases.

Current status: Series now complete at v1 with both parts. Part 1 was preparatory refactoring moving `other_arg` handling to new `rdiff_other_arg` member in `rev_info`. Part 2 completes the fix by using this member to properly propagate notes arguments to range-diff subprocess in single-commit cases. Test coverage added in `t3206-range-diff.sh`.

Key technical details:
- Added `rdiff_other_arg` member to `rev_info` struct in `revision.h` (part 1)
- Modified `builtin/log.c` to use new struct member (part 1)
- Updated `show_diff_of_diff` in `log-tree.c` to pass `opt->rdiff_other_arg` (part 2)
- Added test case for single-commit scenario with `--notes=custom`
- Maintains existing behavior for multi-commit cases
- Uses `test_grep` for pattern matching in new test
- Follows proper cleanup patterns with `test_when_finished`
- Small net changes (7 lines in part 1, 3 lines in part 2 plus test additions)

Open questions: None raised yet. The series appears complete and well-tested for the described issue. May need verification from reviewers familiar with revision walking and notes handling.

Participant positions: Only the author has commented so far. No Reviewed-by or Acked-by tags yet. Both patches appear well-crafted with attention to memory management and test coverage.

Related work: Touches same revision walking code that handles notes display, though not directly to other ongoing efforts. The change is focused on this specific format-patch behavior inconsistency.