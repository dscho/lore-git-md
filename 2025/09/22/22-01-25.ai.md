Review feedback on the second patch in the series fixing `git format-patch` notes display inconsistency. Junio C Hamano points out that the current description understates the issue - it's not just about getting different notes in different sections, but also about `--no-notes` potentially being ignored in single-commit cases. The reviewer suggests clarifying the commit message to reflect this more serious implication.

The patch itself propagates notes arguments to the range-diff subprocess in single-commit cases by using the new `rdiff_other_arg` member added in part 1. It adds a test case specifically for the single-commit scenario with `--notes=custom`, verifying notes appear correctly in the main message and range-diff output. The reviewer finds the technical approach sensible but hasn't yet provided formal Reviewed-by/Acked-by tags.

This follows up on earlier feedback about part 1 regarding initialization and naming of the new struct member. The series remains at v1 while these technical concerns are being addressed. The review suggests the patch is moving in the right direction but will need a v2 to incorporate the suggested commit message clarification and possibly other feedback.