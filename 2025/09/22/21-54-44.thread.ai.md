Thread identity: Documentation patch addressing case-insensitive filesystem behavior in Git's locking mechanism, specifically in `lockfile.c`.

Core problem or goal: Explains how case-insensitive filesystems (e.g. macOS default) can trigger "existing lock" errors when dealing with mixed-case refs, as different case variants resolve to the same lock file path. The patch doesn't fix the underlying issue but improves error messaging.

Current status: Single patch adding 4 lines and removing 1 line in `lockfile.c` to enhance the lock acquisition error message. No version history yet as this is the initial submission.

Key technical details: Modifies the error message shown when Git fails to acquire a lock, specifically addressing the case where mixed-case ref names collide on case-insensitive filesystems during operations like `git fetch` or `git pull`. The new message explains the filesystem behavior and suggests ensuring remote refs use consistent case variants as a workaround.

Open questions and loose ends: The commit doesn't indicate how frequently this issue occurs in practice. No review feedback or discussion yet on whether this messaging change is sufficient or if deeper changes might be needed.

Participant positions: Only the original author's contribution so far - no reviewers or other participants have commented yet.

Related work: This touches on long-standing case-sensitivity challenges in Git, particularly on macOS and Windows filesystems, though it doesn't reference any specific prior discussions or patches about case handling in the refs/locking subsystems.