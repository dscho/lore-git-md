Bugfix follow-up addressing test case behavior for `git stash show --` in the strvec management series.

Jeff King responds to Junio Hamano's observation that `git stash show` and `git stash show --` produce different output formats (diffstat vs patch) even before the memory management fixes. This behavioral quirk was discovered during testing of the patch series that fixes strvec memory issues in `setup_revisions()`.

Key points in this response:
- Acknowledges the unexpected output difference between `git stash show` and `git stash show --`
- Confirms this is pre-existing behavior unrelated to the memory fixes
- Suggests the output difference might be a separate bug, but outside the scope of the current memory management fixes
- Discusses test case options: either keep the current test that checks for leaks only, or modify it to not compare outputs
- Notes potential backward compatibility concerns if fixing the behavior, as scripts may rely on the current output
- Proposes addressing the behavior inconsistency as a separate future change

The discussion highlights an incidental discovery about `git stash`'s argument parsing while validating the memory management fixes. The core strvec fixes remain uncontroversial, with this exchange focusing on test case refinement and potential follow-up work. Jeff King maintains the position that behavior changes should be handled separately from the memory safety improvements.