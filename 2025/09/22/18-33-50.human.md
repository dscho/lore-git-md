This is a technical follow-up in the ongoing Rust integration effort, specifically addressing type naming conventions between C and Rust code. Ezekiel Newren confirms Junio Hamano's earlier point that Rust can technically use type names like `u16` as struct fields, though it's still considered bad practice. 

Newren reports he has already refactored his xdiff cleanup code (part of his 18-patch series) to use standard C type names (`uint16_t` etc.) rather than Rust-style names. This aligns with the thread's recent resolution to maintain C-style type conventions at the language boundary, avoiding potential confusion despite Rust's flexibility.

The exchange shows the Rust integration work progressing with careful attention to cross-language compatibility, particularly where generated bindings (via cbindgen) are involved. Newren's quick implementation change demonstrates responsiveness to maintainer feedback on this subtle but important technical detail.