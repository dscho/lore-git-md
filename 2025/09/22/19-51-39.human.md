This is a refactoring patch in the ongoing xdiff internals cleanup series preparing for Rust compatibility. It's patch 10/12 in v4 of the series, following earlier structural simplifications that have already been merged to 'next'.

The change removes redundant local variable aliases (`rchg1` and `rchg2`) in `xdl_build_script()`, directly accessing the `rchg` fields through the `xe` parameter instead. This continues the pattern seen throughout the series of eliminating intermediate variables and simplifying direct structure access.

The patch is small (just 4 lines removed, 3 added) and focused, maintaining the same behavior while making the code more straightforward. It follows logically from previous changes in the series that have been removing similar aliases and redundant data structures. Performance impact should be neutral, as with other changes in this carefully measured series.

This appears to be a straightforward cleanup with no obvious downsides, continuing the series' trend of making the code more maintainable while preserving behavior. The change hasn't raised any concerns in the thread so far, though there remains one open question from Phillip Wood about performance impact of an earlier patch (9/10) that hasn't been addressed yet.