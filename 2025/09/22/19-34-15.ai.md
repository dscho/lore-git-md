This is a discussion thread about Rust integration impacts on SHA-1/SHA-256 interoperability, specifically addressing concerns raised about platforms that won't support Rust.

SZEDER Gábor follows up on Brian m. carlson's explanation about Rust's role in hash algorithm interoperability, seeking clarification about what functionality will be unavailable on Rust-less platforms. The key technical point is that while single-hash operation (either SHA-1 or SHA-256 alone) will remain possible without Rust, the interoperability features (conversion between hash algorithms via `extensions.compatObjectFormat`) will require Rust and will simply error out if attempted without it.

Brian's proposed workaround involves using a Linux VM for hash transitions, though this creates a "flag day" scenario where repositories become temporarily unusable on older Git versions during transition. SZEDER expresses skepticism about this being an acceptable limitation, despite Brian's assessment that it's not problematic."

The exchange reveals an ongoing tension between the Rust transition's technical requirements and practical impacts on users of niche platforms. SZEDER suggests documenting these limitations more prominently in both the commit message and BreakingChanges document, while Brian maintains the position that the tradeoffs are reasonable given the project's direction.

This discussion occurs against the backdrop of the larger Rust integration effort's phased timeline (optional in 2.52, default in 2.53, mandatory by 3.0) and the parallel LTS maintenance strategy for Rust-free releases. The specific concern about hash interoperability adds a concrete use case to evaluate the Rust requirement's real-world consequences, particularly for users needing to transition repositories between hash algorithms.