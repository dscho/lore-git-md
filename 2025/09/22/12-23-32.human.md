Bugfix follow-up to dangling symref handling. Toon Claes reports an edge case where Jeff King's recent patch to prevent dangling symref clobbering breaks their workflow at $DAYJOB. Their use case involves explicitly deleting dangling symrefs by passing null OIDs in an update operation, which the current patch now blocks.

The proposed follow-up patch modifies the check in both refs backends to allow null-to-null updates (deletions) of dangling symrefs while maintaining protection against accidental overwrites. It includes a new test case demonstrating the allowed deletion scenario. This appears to preserve the original patch's safety while accommodating a legitimate deletion use case.

The change is minimal (two condition additions) and comes with test coverage, suggesting it's a well-considered follow-up. Since the main series is already in 'next', this would likely need to be queued as an incremental fix if accepted. The author is specifically asking Jeff King for feedback on whether this exception makes sense.