Refactoring patch in the xdiff preparation series for Rust compatibility. This is patch 2/12 in v4 of the series, continuing the structural simplifications of xdiff's internals. The change eliminates redundant local variables in `xdl_prepare_ctx()` that were effectively duplicating the work of `xdl_free_ctx()`, instead initializing and freeing the `xdfile_t` struct members directly. 

The patch moves the `xdl_free_ctx()` function definition earlier in the file to make it available for use in `xdl_prepare_ctx()`, then replaces manual variable management with direct struct member access. Error handling now uses `xdl_free_ctx()` consistently. The changes are purely internal - no behavior or performance impact is expected, just cleaner code organization.

This continues the series' pattern of simplifying xdiff's memory management patterns in preparation for Rust integration. The change is straightforward and aligns with the overall goal of reducing duplicate cleanup paths. The patch appears well-contained, touching only xprepare.c, and maintains the series' careful approach of behavior-preserving refactoring.