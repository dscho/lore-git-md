Refactoring discussion about `git update-ref` symref handling semantics.

Jeff King responds to Junio Hamano's question about whether Toon Claes' proposed patch to allow null-to-null updates of dangling symrefs would also need to modify the `symref-delete` command path. Jeff explains that all ref operations converge on the same "check the old value" code path regardless of operation type.

The email then digresses into a broader critique of `update-ref --stdin`'s design, suggesting the command structure could have been more logically organized with separate old-oid/old-target specifications and potentially eliminating the `symref-delete` command entirely. Jeff notes this is largely theoretical now given the established API, but points out current limitations like the inability to atomically convert a symref to a regular ref.

Key technical points:
- Current implementation routes all ref operations through `check_old_oid()`
- `symref-delete` may be conceptually redundant with proper old-target support
- Historical design decisions prevent some atomic operations between ref types
- The discussion remains academic as the immediate patch doesn't require `symref-delete` changes

This continues the thread's evolution from specific bugfix to broader API design considerations, with Jeff providing historical context about why the current implementation has these limitations. The email doesn't directly advance the immediate patch discussion but provides useful background for understanding the constraints.