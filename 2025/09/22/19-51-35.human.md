This is a refactoring patch in Ezekiel Newren's ongoing series to simplify xdiff internals for Rust compatibility. It removes the `diffdata_t` struct which was serving as a redundant wrapper around fields in `xdfile_t`. The patch updates all callers to directly access the `xdfile_t` fields instead, eliminating unnecessary indirection.

The changes are mechanical and straightforward - each field in `diffdata_t` had a direct counterpart in `xdfile_t`, and the patch simply removes the middleman. The diff shows updates to `xdl_recs_cmp()` and `xdl_do_diff()` to work directly with `xdfile_t` rather than going through the `diffdata_t` wrapper.

This continues the series' pattern of simplifying xdiff's data structures while maintaining identical behavior. The change appears clean and well-contained, touching only the xdiff internals without affecting any higher-level Git code. As with previous patches in the series, this helps pave the way for future Rust integration by reducing unnecessary indirection and making the data flow more explicit.