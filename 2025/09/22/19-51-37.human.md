This is part of Ezekiel Newren's ongoing refactoring of xdiff internals to prepare for Rust compatibility, specifically patch 8/12 in the v4 series. The change eliminates redundant field duplication in the `xdlclass_t` struct by embedding an `xrecord_t` instead of copying its fields individually (`ha`, `line`, `size`). 

The patch is a mechanical transformation that replaces direct field access with indirection through the embedded `rec` struct (e.g., `rcrec->ha` becomes `rcrec->rec.ha`). The commit message explains this avoids future type synchronization issues when `xrecord_t`'s fields change, which is relevant for upcoming Rust integration work. 

This continues the series-wide pattern of simplifying xdiff's data structures while maintaining identical behavior. The change is straightforward and builds on prior cleanups, with Phillip Wood credited for assistance. No performance impact is noted, and the diff shows clean mechanical updates to `xprepare.c` with no surprises.