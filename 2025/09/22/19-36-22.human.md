This is a test refinement patch in the ongoing strvec memory management fix series. Junio Hamano proposes simplifying the test case for the `git stash show --` memory leak fix by removing an unnecessary output comparison. The original test was comparing output between `git stash show` and `git stash show --`, but Jeff King had noted this comparison was only valid for empty-diff stashes. 

The patch changes the test to focus solely on verifying the memory leak is fixed when using `--`, marking it with SANITIZE_LEAK to only run in leak-checking builds. This keeps the test's documentation value while removing the potentially misleading output comparison. The change aligns with the thread's consensus to address the output format inconsistency separately from the memory safety fixes.

This appears to be a straightforward improvement to the test suite that better isolates what's being tested (memory management) from unrelated behavior differences. The patch maintains the series' focus on fixing memory issues while cleaning up the test implementation.