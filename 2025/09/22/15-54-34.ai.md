Bugfix follow-up addressing dangling symref deletion edge case in `git update-ref`.

Junio C Hamano responds to Toon Claes' proposed follow-up patch that would allow null-to-null updates for dangling symrefs via `git update-ref --stdin`. While acknowledging the technical correctness of the change, Hamano raises an important architectural question: since `update-ref` already has a dedicated `symref-delete` command for removing symbolic references, should the `update` command's behavior be expanded in this way?

The email highlights a key design consideration - whether to maintain strict separation between reference update types (with `update` handling object IDs and `symref-delete` handling symbolic references) or to allow some overlap in functionality. Hamano's query suggests this might be more than just a technical edge case fix, potentially touching on the intended semantics of the refs API.

The test case shown demonstrates the proposed behavior: creating a dangling symref (`refs/heads/dangling` pointing to nonexistent `refs/heads/does-not-exist`), then successfully deleting it via a null-to-null update. The verification steps confirm both the symref and its (nonexistent) target are properly gone after the operation.

This follow-up maintains all existing protections from the original series while addressing a legitimate workflow case, but introduces questions about API design consistency that may need discussion before finalizing the change. The thread context shows this builds on Jeff King's earlier fixes for dangling symref handling in the refs backend.