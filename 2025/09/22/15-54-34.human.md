Junio Hamano responds to Toon Claes's follow-up patch about dangling symref handling in `git update-ref`. This is a technical discussion about the edge case of deleting dangling symrefs, building on Jeff King's earlier series that fixed related issues.

Junio notes that while Toon's patch allows null-to-null updates for dangling symrefs (a case the original series blocked), there's already a dedicated `symref-delete` command in `update-ref` that serves this purpose. He questions whether the patch should also modify that code path for consistency.

The exchange shows careful consideration of the interface design - whether to expand the `update` command's capabilities versus directing users to the more specialized `symref-delete`. This suggests the change might need more discussion about the preferred API approach before being finalized. The patch itself appears technically sound but may need adjustment based on the project's philosophy about command specialization.