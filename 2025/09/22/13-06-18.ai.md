This is the first patch in a completed 5-part series fixing bugs in `git config get --type=color` functionality. The series has gone through four iterations with all review feedback addressed and is now ready for inclusion.

The patch modernizes test formatting in t1300-config.sh by moving test expectations into the test bodies rather than defining them outside. It converts most tests to use `<<\EOF` for non-expanded heredocs and `<<-EOF` for indentation-sensitive ones, while leaving two exceptions that are reused across tests. The changes are purely stylistic - 291 lines changed with 128 insertions and 163 deletions, but no functional impact.

This cleanup was requested during review to align with modern Git test style. The patch is well-structured, with each test now clearly showing both its setup and expectations in one place. The author has left two cross-test exceptions intact rather than forcing unnecessary duplication, showing good judgment about when to break from the new pattern.

Key technical details:
- Modifies t1300-config.sh test file extensively
- Uses `<<\EOF` for non-variable-expansion cases
- Uses `<<-EOF` for indentation-sensitive cases
- Leaves two cross-test expectation files unchanged
- No functional changes to test behavior
- Part of larger color parsing fix series (patches 3-5)