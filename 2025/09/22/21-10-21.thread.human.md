This thread addresses a subtle inconsistency in `git format-patch` where Git notes attached to commits would appear differently in the main commit message versus the range-diff section when generating single-commit patches. The issue stemmed from notes-related arguments not being properly propagated to the range-diff subprocess in single-commit cases.

The author has sent a two-part series to fix this. The first patch was a preparatory refactoring that moved the `other_arg` handling from a local variable to a new `rdiff_other_arg` member in the `rev_info` struct. The second patch, which we've now seen, completes the fix by using this new struct member to properly pass notes arguments to the range-diff subprocess in single-commit scenarios.

The solution adds test coverage in `t3206-range-diff.sh` specifically for single-commit cases, verifying that custom notes appear consistently in both the commit message and range-diff output. The changes are small and focused - just 3 lines of code changes plus test additions - and maintain the existing behavior for multi-commit cases. The series cleanly separates the refactoring from the behavior fix, making the changes easy to review.

At this point, the series appears complete and well-constructed, with proper test coverage for the specific edge case it addresses. The patches demonstrate attention to memory management and follow recent testing conventions. No reviewers have commented yet, but the changes seem straightforward and unlikely to be controversial.