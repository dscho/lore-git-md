This is a bugfix patch series addressing an inconsistency in `git format-patch`'s handling of Git notes in range-diff output for single-commit series. The issue causes notes to be displayed differently between the main commit message and the range-diff section, with the latter defaulting to `git-log` behavior.

The series introduces a two-part solution: first refactoring to add a new `rdiff_other_arg` member to `rev_info` struct, then use this in `log-tree.c` to ensure consistent notes display. The changes touch revision handling internals and affect both the core functionality and test coverage.

Key technical points include modifications to `builtin/log.c`, `log-tree.c`, and `revision.h`, plus new test cases in `t3206-range-diff.sh`. The author notes having run leak checks using a development configuration with sanitizers enabled, suggesting attention to memory safety.

The patch appears focused and well-motivated, addressing a specific inconsistency in behavior. The test additions suggest the change is verifiable, though the exact nature of the inconsistency isn't fully detailed in the cover letter. The changeset is small (21 lines net) and concentrated in related areas of the codebase.