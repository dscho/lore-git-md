This is a follow-up in the SHA-1/SHA-256 interoperability series, specifically addressing tag signature validation rules from patch 7/9. Brian responds to Junio's review comments about line wrapping and tag signature header coexistence.

The key development is Brian's decision to enforce mutual exclusivity between `gpgsig` and `gpgsig-sha256` headers based on the repository's hash algorithm - `gpgsig` will only be allowed in non-SHA-1 repositories and `gpgsig-sha256` only in non-SHA-256 repositories. This resolves the open question about header coexistence rules while allowing for future algorithm additions. Brian notes a corner case where converted tags may contain only a signature header without a trailing signature body.

The response confirms Brian will address Junio's line wrapping request in v2. The discussion continues refining the tag signature validation logic, with Brian providing implementation details about how signature headers will be handled during object conversion between hash formats. This moves the thread closer to consensus on tag signature storage rules, one of the remaining open questions in the interoperability work.