This is a follow-up discussion about a bugfix patch for memory leaks in `git stash show` when using `--` arguments. The conversation reveals an unexpected behavior difference between `git stash show` (which shows a diffstat) and `git stash show --` (which shows a patch) that predates the memory fixes.

Jeff King acknowledges Junio Hamano's discovery that the differing output formats between these commands is an existing behavior, not caused by the memory leak fixes. While both agree this behavior is nonsensical, they decide to keep it out of scope for the current memory management fixes. The discussion focuses on adjusting the test case to properly account for this output difference while still verifying the leak is fixed.

Jeff suggests either reverting to a simpler leak-only check or keeping the current test with adjusted expectations. He also notes this behavior quirk might warrant a separate fix in the future to make `git stash show --` more consistent. The exchange shows careful consideration of backward compatibility versus fixing odd behaviors, with a preference to address the memory issues first before tackling behavioral changes.