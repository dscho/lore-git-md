This is a follow-up discussion about the limitations of `--shallow-exclude` with commit hashes, building on the earlier thread that distinguished between expected behavior with branch names versus the now-fixed bug with commit hashes.

Philipp Hahn presents a concrete use case where the inability to exclude by commit hash creates problems in GitLab CI pipelines. They explain that while branch name exclusion works for standard merge requests, it fails for GitLab's "Merged Result Pipelines" where the base commit (E in their diagram) is no longer referenced by any branch. The commit hash is available via CI variables, but the current protocol doesn't support excluding by hash.

The email highlights a real-world scenario where this limitation forces inefficient full clones of large repositories (like the Linux kernel) when only shallow clones would suffice for CI jobs. While the thread previously established that the hash exclusion behavior was intentionally not supported, this follow-up makes a case for why that design might warrant reconsideration for CI/CD workflows.

No solution is proposed in this email - it's primarily seeking input on whether the technical constraints that made hash exclusion "flawed" could be overcome, given the demonstrated need in automated pipeline environments. The discussion remains technical and solution-oriented, with clear diagrams and references to GitLab's specific variables and pipeline types.