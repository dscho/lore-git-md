This is a CI/build system patch that concludes a series optimizing documentation testing in the Meson build system. The change modifies the CI script to use the new `docs` target introduced earlier in the series, avoiding unnecessary compilation of the full Git project when only testing documentation.

The patch updates `ci/test-documentation.sh` to explicitly request documentation builds (`meson compile docs`) rather than full project builds. This follows from the earlier patches that added dedicated documentation build targets, addressing Gábor Szeder's report about wasted CI resources. The change maintains all existing documentation validation checks while reducing compute overhead.

Karthik Nayak's review notes the patch's cleanliness and appreciates that it keeps the CI configuration forge-agnostic. The implementation appears straightforward, simply replacing two `meson compile` commands with their `docs`-target equivalents while preserving the surrounding test infrastructure.

This patch represents the final technical piece of the series, with only some naming convention and default behavior questions remaining open for maintainer decision (as noted in the thread summary). The change is well-contained and clearly motivated by the CI efficiency improvement goal established earlier in the thread.