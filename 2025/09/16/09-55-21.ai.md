This is a CI/build system discussion about documentation build options in the Meson configuration, continuing the thread about optimizing documentation testing.

Patrick Steinhardt clarifies the existing `meson_options.txt` behavior in response to Karthik Nayak's question about default documentation builds. The key technical points are:

1. The `docs` option is already declared in `meson_options.txt` (as required by Meson) and accepts an array of choices (`man`, `html`, or both) to specify which documentation formats to build.

2. The documentation targets are automatically enabled whenever at least one documentation format is selected through this option - there is no separate default setting needed.

3. This maintains consistency with the current implementation where `meson compile docs` builds documentation only when documentation is enabled via the options.

The exchange confirms the technical implementation is correct as-is, addressing Karthik's question about whether additional configuration was needed in `meson_options.txt`. This leaves the remaining open questions from the thread (naming conventions and alias placement) as the only outstanding items before the series can be finalized.