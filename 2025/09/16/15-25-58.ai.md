This is a brief follow-up in the ongoing bundle-URI bugfix thread, specifically addressing the handling of malformed bundle advertisements. Toon Claes seeks clarification from Junio Hamano on the preferred error reporting approach for invalid bundle configurations. 

The email is very short - just a direct question about whether replacing a silent `return -1` with an explicit `error()` message would satisfy Junio's earlier expressed preference for making misconfigurations visible to users rather than silently ignoring them. This comes after Junio and Justin Tobler had both argued that client-side validation should be strict to help users identify and fix configuration problems.

The technical context remains the bundle-URI parsing code in connect.c and bundle.c, where the discussion has centered on how to balance robustness (continuing operation when possible) versus strict validation (helping users catch configuration errors). No new code changes are proposed here - just a request for guidance on implementation direction from the maintainer. The thread appears to be converging toward explicit error reporting rather than silent failure for configuration issues, while maintaining graceful handling of transmission errors.