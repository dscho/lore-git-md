Refactoring patch in the `stash.index` config series. This is the third of four patches implementing a configurable approach to index restoration in `git stash` operations.

The patch moves three config-related global variables (`show_stat`, `show_patch`, and `show_include_untracked`) to consolidate them with other globals at the top of builtin/stash.c. The change is purely organizational - no behavior is modified. The author notes this prepares for accessing a new config variable (presumably `stash.index`) in subsequent functions, requiring the variables to be declared earlier in the file.

The diff shows the straightforward relocation of these variables from near the bottom of the file to a new section near the top, grouped with other existing globals like `ref_stash` and `stash_index_path`. The change is marked as best viewed with `--color-moved` to highlight the pure relocation.

This appears to be a mechanical preparation step for the main functionality in the series, maintaining the incremental approach seen in previous patches. The change is small (4 lines moved) and the commit message clearly explains the motivation without introducing new concepts. The patch follows the series' pattern of making focused, testable changes while working toward the larger goal of configurable index restoration.