This is a follow-up discussion in the CI/build system optimization series for Meson documentation builds. The thread has already covered the core technical implementation of adding a `docs` alias target, with Patrick Steinhardt proposing a dual-alias solution (`doc` and `docs`) to accommodate both Make and Meson conventions.

Karthik Nayak raises a new technical question about whether the documentation build should be enabled by default in `meson_options.txt`. This suggests the current implementation may not automatically build docs during a standard `meson compile` unless explicitly requested via `meson compile docs`. The question implies this might be an unintentional behavior change from the Makefile system where documentation builds were automatic when dependencies were met.

The discussion continues to focus on implementation details rather than revisiting the naming convention debate, which appears settled with Patrick's dual-alias proposal. Karthik's query touches on the meson.build configuration rather than the alias mechanism itself, specifically whether documentation should be part of the default build targets.

Key technical points in this email:
- The patch adds documentation targets to a `doc_targets` list variable
- The `alias_target('docs', doc_targets)` is placed after contrib/ to catch contrib manpages
- Question about default behavior in meson_options.txt remains open
- No changes proposed to the actual documentation build logic or CI integration

This appears to be a minor refinement question in an otherwise complete series, with the core functionality (documentation-only builds and improved visibility) already implemented and tested. The thread awaits maintainer decision on both the alias naming and now this default-build question.