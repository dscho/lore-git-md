This is a refactoring patch in a series about improving Git's color handling code. It simplifies the logic in `diff_flush_patch_all_file_pairs()` where color-moved detection is performed.

The patch removes a redundant conditional check inside the `o->emitted_symbols` block, since `emitted_symbols` is only set when `color_moved` is true anyway. The change eliminates an unnecessary nesting level while maintaining the same behavior, as the code was already effectively guarded by the outer `emitted_symbols` check. 

The author notes this makes the code easier to work with for future modifications, though acknowledges the current structure could theoretically support other features using `emitted_symbols` (which would require refactoring if they were added). The change touches only `diff.c`, modifying the color-moved detection logic while preserving all existing functionality.

This is patch 4/13 in a series that standardizes Git's color handling. The series builds on previous color-related work and includes both bugfixes and code quality improvements. This particular patch appears to be a straightforward cleanup with no behavior change.