This is the final patch in a 6-part refactoring series that establishes a clean ODB transaction interface, moving from files-backend specifics to a more general object database abstraction. The patch introduces new public API functions `odb_transaction_{begin,commit}()` in odb.[ch], providing an object-source-agnostic way to manage transactions. It renames the existing files-backend implementation to `object_file_transaction_{begin,commit}()` to clarify its scope, and updates all callers to use the new odb interface.

The changes are mechanical but important for the series' goal of enabling pluggable backends. The patch completes the transition from "bulk-checkin" terminology to "transaction" that was started in earlier patches. No behavior changes are introduced - this is purely about API boundaries and naming consistency.

The patch affects 9 files with 46 insertions and 19 deletions, updating transaction handling in commands like `add`, `update-index`, and `unpack-objects`, as well as core functionality in cache-tree and read-cache. The new odb.h interface provides the public contract while keeping files-backend specifics properly encapsulated in object-file.[ch].

This concludes the technical implementation work of the series, with all pieces now in place for future pluggable backends to implement their own transaction handling while callers use the unified odb interface.