This is a technical discussion about the implementation approach for the `reword` subcommand in the `git-history` series. Phillip Wood continues to advocate for reusing the sequencer's existing reword capability by passing a todo-list with "reword $oid" entries, arguing this would provide equivalent functionality with less new code. He challenges Patrick Steinhardt's concern about being in "interactive-rebase mode" by pointing out that both cherry-pick and rebase already share the same todo-list processing logic in the sequencer.

Phillip suggests adding a new sequencer entry point that accepts a todo-list directly, which would allow `git-history` to leverage existing sequencer operations like rewording commits and updating refs with minimal new code. He acknowledges there are arguments for avoiding the sequencer entirely (to prevent unnecessary worktree updates during operations), but sees no good reason for the current hybrid approach that uses the sequencer for cherry-picking but implements other operations separately.

The email also notes an existing limitation where `git status` cannot distinguish between `git cherry-pick` and `git history` operations, suggesting this as a future consideration. This discussion represents an ongoing tension between code reuse and operational mode separation in the `git-history` design, with Phillip advocating strongly for leveraging more of the sequencer's existing capabilities.