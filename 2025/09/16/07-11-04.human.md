This is a follow-up discussion on a bugfix and performance improvement for the `git last-modified` command, specifically addressing the handling of recursive diff behavior in criss-cross merges.

Patrick Steinhardt raises several technical questions about Toon Claes' proposed solution to stop unconditionally setting the recursive flag in `diff_tree_combined()`. The key concerns are:

1. The explanation doesn't clarify why criss-cross merges specifically trigger the BUG() while normal merges don't.
2. Questions whether the performance improvement only applies to `--no-recursive` mode.
3. Points out that other callers like `stdin_diff_commit()`, `git-am`, and `git-log` might need similar adjustments.
4. Expresses concern about potential behavior changes for other callers that might not be caught by tests.
5. Suggests an alternative approach using a new flag to avoid unintended side effects.

The discussion shows careful technical scrutiny of the patch's implications beyond the immediate `last-modified` use case. Patrick doesn't outright reject the approach but highlights areas needing clarification or potential risks. The thread appears to be moving toward ensuring the change is safe and well-understood before proceeding.