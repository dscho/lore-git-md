This is a follow-up discussion in the CI/build system optimization series for Meson documentation builds. The exchange clarifies technical details about the implementation from patch 1/3.

Karthik Nayak questions whether the `docs` target needs explicit declaration in `meson_options.txt`, which Patrick Steinhardt confirms is already present with array options for `man` and `html` formats. The discussion reveals that:

1. The `docs` option automatically enables documentation targets when any format is selected
2. The default empty array `[]` means no documentation is built by default
3. No additional configuration is needed beyond the current implementation

This resolves Karthik's concern about the target visibility in Meson's configuration system. The exchange confirms the technical correctness of patch 1's approach to documentation target handling, particularly regarding:
- The existing `meson_options.txt` integration
- The automatic enabling mechanism when formats are selected
- The intentional placement of the alias after `contrib/` to catch all manpages

The discussion provides important context for the series' implementation details while not requiring any changes to the submitted patches. It serves as a technical clarification that reinforces the design decisions made in the initial implementation.