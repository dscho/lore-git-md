Test failure report involving SOCKS proxy functionality in t5564.

Brian Carlson reports a test failure in t5564 (HTTP/SOCKS proxy tests) on Debian unstable with libcurl 8.16.0. The test attempts to clone via a Unix socket SOCKS proxy but fails at the "clone via Unix socket" step (test 6). The test expects to see "SOCKS4 request granted" in the trace output but instead shows HTTP proxy authentication behavior.

The detailed curl trace reveals the connection is being treated as an HTTP proxy rather than SOCKS, with the socks4:// URL in http.proxy config. The trace shows:
1. Initial 407 Proxy Authentication Required response
2. Subsequent Basic auth handshake
3. Successful Git protocol exchange after auth

Key observations for future discussion:
- Test config uses `http.proxy=socks4://localhost$PWD/%2530.sock`
- Expected SOCKS4 behavior isn't occurring
- Actual behavior suggests HTTP proxy interpretation
- Test failure occurs in the clone operation, not the prerequisite check
- Environment is Debian unstable with libcurl 8.16.0

This appears to be either a libcurl behavior change or a test expectation mismatch, but the commit message doesn't propose a solution. The detailed trace output will help diagnose whether this is a libcurl regression, test bug, or configuration issue.