Thread identity: User experience discussion about Git's command suggestions for unstaging files in different repository states (unborn vs. initialized), comparing `git status` output recommendations.

Core problem: Original inconsistency in `git status` advice - suggests `git rm --cached` before first commit but `git restore --staged` afterward. Technical analysis confirmed this was necessary (no HEAD in unborn repos), but discussion has evolved to consider both semantic clarity of recommendations and potential behavioral unification.

Current status: D. Ben Knoble proposes modifying `git restore --staged` to work in unborn repositories by treating them as having an empty tree, which would unify the unstaging workflow across repository states. This goes beyond Junio's earlier suggestion to prefer `git reset` in initialized repos, instead aiming to make `restore --staged` work universally.

Technical details:
- Pre-commit (unborn repo) current behavior:
  - Only `git rm --cached` possible (no commit to restore from)
  - Implementation dates to 2009 `is_initial` check
- Post-commit current behavior:
  - Recommends `git restore --staged` (promoted over reset in 2019)
  - Junio questioned if `git reset` would be clearer
- New proposal:
  - Make `restore --staged` work in unborn repos by assuming empty tree
  - Would match behavior of other Git commands with unborn branches
  - Eliminates need for different commands in different states
- Implementation considerations:
  - `restore --staged` currently fails in unborn repos with "could not resolve HEAD"
  - Change would require modifying restore.c to handle unborn case
  - Would maintain technical correctness while improving UX

Key implications:
- Original behavioral difference was technically necessary but potentially confusing
- Proposal would provide unified mental model ("restore" always works)
- Maintains safety (both commands preserve worktree)
- Aligns with Git's handling of unborn branches elsewhere
- Would require code change rather than just documentation update

Open questions:
- Should `git status` recommendations be unified via code change (proposed) or documentation?
- Is empty-tree assumption semantically correct for `restore --staged`?
- Would this change introduce any edge cases?
- How does this interact with Junio's `reset` vs `restore` clarity question?
- Should the documentation be updated regardless of implementation change?

Participant positions:
- Original poster identified initial behavioral difference
- Junio C Hamano confirmed technical necessity but questioned `restore` vs `reset` clarity
- D. Ben Knoble now proposes implementation change to unify behavior under `restore`
- Thread shows movement from analysis to concrete proposal

Related work:
- 2009 `is_initial` check implementation
- 2019 promotion of `git restore` over `reset`
- Git's existing handling of unborn branches as empty trees
- Ongoing discussions about command recommendations and new user experience

The discussion has progressed from validating current behavior to considering both documentation improvements and implementation changes. Knoble's proposal to modify `restore --staged` represents a potential path forward that would address the original UX inconsistency at the implementation level while maintaining technical correctness. This shifts the conversation from choosing between existing behaviors to creating new unified behavior.