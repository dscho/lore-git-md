Thread identity: User experience discussion about Git's command suggestions in different repository states, specifically comparing `git status` output before versus after initial commit.

Core problem: Inconsistency in `git status` advice for unstaging files - suggests `git rm --cached` before first commit but `git restore --staged` afterward. Technical investigation confirms this is necessary rather than a UX inconsistency, as pre-commit there's no HEAD to restore from. New discussion emerges about whether `git reset` might be more conceptually appropriate than `restore --staged` post-commit.

Current status: Maintainer (Junio C Hamano) and technical analysis (D. Ben Knoble) confirm behavior is correct given technical constraints, but Junio now questions whether `git reset` would be more semantically clear than `restore --staged` for unstaging in initialized repositories. Documentation improvements remain possible.

Technical details:
- Before first commit (no HEAD):
  - Only `git rm --cached` possible (no commit to restore from)
  - Removes from index while preserving worktree file
  - Behavior dates to 2009 `is_initial` check
- After first commit (HEAD exists):
  - Current advice suggests `git restore --staged` (promoted over reset in 2019)
  - Junio questions if `git reset <file>` would be more conceptually clear
  - Both commands reset index to HEAD state (preferred over rm --cached)
  - `restore --staged` fails in unborn repo with "could not resolve HEAD"
- Implementation difference stems from fundamental repository state

Key implications:
- Difference reflects technical necessity not UX oversight
- Historical changes (2009, 2019) shaped current behavior
- New users may still find this confusing despite technical reasons
- Semantic debate emerges about best command to recommend for unstaging
- Related commands: `git reset` (HEAD-dependent) could theoretically be suggested but `restore` is preferred modern alternative

Open questions:
- Should documentation better explain this distinction?
- Would clarifying text in `git status` help users understand why suggestions differ?
- Is there value in making `restore --staged` more graceful in unborn repos?
- Should `git status` recommend `reset` instead of `restore --staged` for unstaging?
- What are the pedagogical implications of each recommendation?

Participant positions:
- Original poster identified the behavioral difference
- Junio C Hamano confirmed technical necessity of current behavior but now suggests `reset` may be clearer
- D. Ben Knoble provided historical and implementation analysis reinforcing technical constraints
- Thread now reopened to discussion of best command recommendation

Related work:
- 2009 commit introducing `is_initial` check
- 2019 promotion of `git restore` over `git reset`
- Ongoing discussions about improving user-facing command suggestions

The discussion has evolved from technical validation of current behavior to reconsidering the post-commit recommendation. While the pre-commit behavior remains technically constrained, the choice between `restore --staged` and `reset` for initialized repositories introduces new dimensions around command semantics and user education. The thread is no longer fully resolved, with potential implications for Git's standard advice.