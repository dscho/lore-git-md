This is a follow-up discussion in the bugfix thread about `git last-modified` and recursive diff behavior. Toon Claes responds to Patrick Steinhardt's concerns about potential side effects from changing `diff_tree_combined()`'s recursive flag handling.

Key technical points in this response:
- Toon acknowledges uncertainty about why criss-cross merges specifically trigger the BUG() in `last-modified`, suggesting it relates to how merge attribution works in `log_tree_diff()`'s code paths
- Confirms the performance improvement only applies to non-recursive mode (default for `last-modified`)
- Agrees to address Patrick's concern about other callers (`stdin_diff_commit()` in git-am/git-log) in next version
- Argues the change fixes an ancient bug rather than introducing new behavior, though lacks concrete data
- Rejects Patrick's suggestion of a new flag as adding technical debt

The discussion reveals:
1) The criss-cross merge behavior remains partially unexplained
2) Caller impact analysis is incomplete (only `diff-tree` handled so far)
3) Philosophical difference on whether to treat this as bugfix (Toon) or risky change (Patrick)

Status: The technical solution remains promising but requires more comprehensive caller analysis before proceeding. Toon plans to investigate additional call sites while maintaining the position that this is a corrective change rather than a behavior modification. The thread highlights the challenge of modifying long-standing behaviors in complex diff machinery.