Patrick Steinhardt raises important technical concerns about Toon Claes's patch to fix the `git last-modified` criss-cross merge issue. This is a bugfix targeting the combine-diff machinery's recursive flag handling.

The core concern is about potential unintended side effects from changing `diff_tree_combined()`'s behavior. While the patch fixes the immediate issue by removing the forced recursive flag and moving it to specific callers (like `diff-tree -c`), Patrick questions whether this might break other callers that implicitly relied on the old behavior. He specifically notes that `stdin_diff_commit()` and other callers in `git-am` and `git-log` might need similar treatment.

Patrick suggests a more conservative approach - introducing a new flag to control the recursive behavior rather than changing the default - though acknowledges this would make the code more complex. The discussion reveals deeper questions about why the original behavior was needed (dating back to 2006) and why criss-cross merges specifically trigger the issue when normal merges don't.

This follow-up highlights important considerations for the patch's approach, suggesting the solution may need to either:
1. More thoroughly audit all callers of `diff_tree_combined()`
2. Find a way to preserve backward compatibility more comprehensively
3. Document the behavior change more explicitly

The thread shows the technical solution is progressing but still needs to address these safety concerns before final acceptance.