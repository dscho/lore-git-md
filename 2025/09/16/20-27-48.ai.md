Refactoring patch exploring stronger type safety for color handling. This is a technical exploration (marked "DO NOT APPLY") that converts the `git_colorbool` enum into a struct wrapper, preventing implicit conversions between bools and enums that caused bugs earlier in the series. The change is invasive, touching 31 files with mechanical conversions of enum usage to struct access patterns. Key technical points:

- Introduces `git_colorbool` as a struct containing the original enum (now `git_colorbool_f`)
- Requires explicit field access (`.f`) for comparisons and initialization
- Maintains all existing color behavior while adding compile-time type safety
- Demonstrates the pattern's viability but notes ugliness in the implementation

The patch serves as a proof-of-concept for the type safety approach discussed earlier in the thread, though the author acknowledges the syntactic overhead may not be worth the benefits. Changes are mechanical but widespread, affecting color handling in diff, grep, config, and interactive add subsystems. The patch would need to be rebased on the `jk/add-i-color` branch to avoid conflicts with ongoing work.