This is a bugfix and performance improvement for the `git last-modified` command, addressing an issue with criss-cross merges. The patch discussion continues from v2, focusing on potential side effects and backward compatibility concerns raised by Patrick Steinhardt.

Toon Claes responds to Patrick's questions about why the issue specifically manifests with criss-cross merges, admitting some uncertainty about the exact mechanism but noting it relates to how merge commits are processed differently in the diff machinery. The conversation reveals the complexity of the underlying combine-diff behavior that's been in place since 2006.

The key technical debate centers on whether removing the recursive flag override in `diff_tree_combined()` could break other callers like `git-am` and `git-log`. Toon argues this is actually fixing a long-standing bug rather than introducing new behavior, though acknowledges they lack concrete data to prove no regressions would occur. Patrick had suggested a more conservative approach with a new flag, but Toon prefers the cleaner solution despite the risk.

The exchange shows thoughtful technical discussion about balancing correctness against backward compatibility, with both contributors demonstrating deep knowledge of Git's diff machinery. While the patch appears technically sound and provides measurable performance benefits (nearly 2x speedup for `git last-modified`), the thread suggests it may need more caller auditing or documentation before final acceptance.