This is a CI/build system patch that's part of an ongoing series to optimize documentation testing in the Meson build system. The patch modifies the CI script to use the new `docs` target introduced earlier in the series, avoiding unnecessary full project builds when only testing documentation.

The change is straightforward - it replaces generic `meson compile` commands with `meson compile docs` in the documentation testing script. This leverages the new documentation-specific build target from patch 1, which was designed to save CI resources by skipping binary compilation when only documentation validation is needed.

The email includes positive feedback from Karthik Nayak, who notes appreciation for the CI configuration remaining forge-agnostic (not tied to GitHub/GitLab specifics). The implementation appears clean and well-aligned with the series' goals of reducing unnecessary builds while maintaining all documentation validation checks.

This patch represents the final technical piece of the optimization series, with the remaining open questions being around naming conventions (doc vs docs) and default build behavior rather than technical implementation. The change is small (4 lines) but completes an important CI efficiency improvement.