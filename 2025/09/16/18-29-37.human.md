This is part 5/6 of a refactoring series to establish ODB transaction interfaces, moving code from bulk-checkin to object-file to enable pluggable backends. The patch focuses on renaming functions and types to better reflect their new location and purpose.

The changes systematically replace "bulk-checkin" terminology with "transaction" or "packfile_transaction" throughout object-file.c, completing the conceptual shift from the old bulk-checkin system to the new transaction model. Notable renames include `bulk_checkin_packfile` -> `transaction_packfile`, `prepare_to_stream` -> `prepare_packfile_transaction`, and `flush_bulk_checkin_packfile` -> `flush_packfile_transaction`.

The patch also eliminates the standalone `finish_tmp_packfile()` function by folding its logic directly into `flush_packfile_transaction()`, simplifying the control flow. All functionality remains identical - this is purely a naming and organization improvement.

This continues the series' work of cleaning up the codebase after relocating the bulk-checkin functionality in patch 4. The changes are mechanical but important for maintaining clarity as the transaction API becomes the standard interface. The patch appears well-executed, with thorough updates to all affected call sites and no behavioral changes.