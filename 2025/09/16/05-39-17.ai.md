This is a follow-up to the documentation patch series addressing a build system issue. Junio Hamano provides a fix for the AsciiDoc compatibility problem in the renamed `urls-remotes-upstreams.adoc` file, where the configuration example was causing build failures with AsciiDoc while working fine with Asciidoctor.

The fix replaces the code-fenced configuration block with tab-indented plain text, which maintains identical visual output while resolving the parsing ambiguity. This change satisfies both toolchains - Asciidoctor produces the same output as before, and AsciiDoc no longer fails to build. The example shows branch configuration for `main` tracking `origin/main`.

This email confirms the technical resolution of the post-merge build issue that was caught in CI testing. The fix preserves the documentation's content while addressing the toolchain compatibility concern that emerged after the patches were merged. The change is minimal and focused solely on the presentation format of the configuration example, without altering the substantive content about upstream branch relationships.

The thread context shows this was part of broader discussions about documentation terminology and toolchain requirements, but this particular message is narrowly focused on implementing the agreed-upon technical solution for the build system problem. The fix aligns with the series' goal of improving push/pull/fetch documentation while maintaining compatibility with Git's documentation tooling.