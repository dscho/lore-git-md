This is a CI/build system discussion about naming conventions for Meson documentation build targets, continuing the optimization series for documentation testing.

Junio Hamano confirms understanding of the existing Meson `-Ddocs=` option behavior, noting it's too late to change the established `get_option('docs')` interface from the original Meson integration. The discussion resolves Patrick Steinhardt's proposal for dual aliases (`doc` and `docs`) by deciding to maintain the current single-target approach rather than adding compatibility aliases.

Key technical points:
- The `-Ddocs=` option accepts formats like `man,html` or empty to disable docs
- This matches the original Meson implementation from commit 904339ed
- The thread concludes no need for Makefile-compatible `doc` alias alongside `docs`

The decision leaves the series functionally complete with all technical aspects resolved, focusing only on the minor naming convention question which is now been settled in favor of keeping the existing Meson-style `docs` target name. This maintains consistency with the build system's internal option naming while achieving the original goal of documentation build optimization.