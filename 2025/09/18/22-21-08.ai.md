This is a technical discussion about a protocol-level bug in `git fetch --negotiate-only` behavior, building on the earlier thread about `--dry-run --filter` config modifications. The email focuses on diagnosing unexpected behavior when fetching from unrelated repositories.

The discussion has identified a clear protocol bug where:
1. The client incorrectly makes an empty request after receiving a NAK when it should exit
2. The server's state machine in `upload-pack.c` incorrectly falls through to sending a packfile when it should handle the negotiate-only case differently
3. There's ambiguity in protocol interpretation around empty ACK/NAK responses

Key technical details:
- The bug manifests when fetching from an unrelated remote (like after `git checkout --orphan`)
- The problematic code paths are identified in `upload-pack.c` lines 1805-1813 and 1712-1713
- Protocol v2 allows for empty "ACK" responses with "ready", but current implementation sends NAK or packfile instead
- The client/server interaction breaks down in this edge case

Randall Becker raises a new consideration about how shallow clones (`--depth=1`) might interact with this behavior, though this isn't yet analyzed in depth. The core protocol issue appears fixable now that the problematic code paths are identified, though there may be debate about the ideal protocol semantics for this edge case.

The thread continues to evolve from its original dry-run/config focus into deeper protocol analysis, with this email providing concrete technical details about the negotiate-only bug's mechanics. The shallow clone question suggests there may be additional edge cases to consider in any fix.