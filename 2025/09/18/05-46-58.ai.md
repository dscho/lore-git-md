This is the reftable backend implementation for the ref optimization functionality, part of the ongoing series to introduce a new `git refs optimize` command. The patch adds the reftable-specific implementation of the `optimize` action by wiring up the existing compaction function (`reftable_be_pack_refs`) to the new backend API. Since reftable's optimization is conceptually similar to its packing operation, the implementation is straightforward - the new `reftable_be_optimize` function simply calls through to the existing packing logic.

Key technical details:
- Adds 7 lines to `refs/reftable-backend.c` implementing the required backend method
- Follows the established pattern from the files backend implementation
- Maintains consistency with the architectural direction set earlier in the thread
- Uses the same `pack_refs_opts` structure for compatibility
- Wires the new function into the `refs_be_reftable` virtual table

This completes the backend implementation work for reftable, following the files backend implementation from earlier in the series. The patch appears well-contained and properly builds on the existing infrastructure, with clear documentation of the relationship between optimization and packing in the reftable context. The implementation steps match the architectural plan outlined in previous discussion, suggesting this is a straightforward but important piece of the larger ref optimization feature.