This is a technical discussion about the behavior of `git fetch --dry-run --filter` and its interaction with promisor objects and repository configuration. The email continues the thread examining whether the current behavior (which modifies `.git/config` during dry runs) should be changed to use a tmp-objdir quarantine approach.

Jeff King acknowledges Junio Hamano's point that the current behavior provides performance benefits by caching healthy objects between dry-run and real fetches. While noting that in this specific case the objects are promisor cruft that violates some rules, Jeff agrees the behavior shouldn't be changed. He observes that while users could manually use `GIT_OBJECT_DIRECTORY` with a tmp-objdir, this still modify the repo config due to how promisor information is stored.

The discussion reveals an underlying architectural tension - promisor information is fundamentally a property of packfiles but is currently tracked in the main repository configuration. While this causes edge cases when playing with alternate object directories, Jeff suggests this isn't worth revisiting now. The thread continues to weigh the tradeoffs between strict dry-run semantics, performance optimizations, and the practical realities of Git's object storage design.