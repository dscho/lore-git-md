This is a refactoring patch in Karthik Nayak's reftable fsck validation series (v3, 6/8). The patch alphabetically reorders the `fsck_msg_type` enum in `fsck.h` to maintain consistent organization of fsck message types. The changes are purely structural with no functional impact, fixing minor ordering inconsistencies where some message types were out of sequence.

The diff shows three main sections being reordered: error messages (where gitattributes/gitmodules entries were previously scattered), warnings, and info-level messages. Notable moves include grouping all gitmodules-related messages together and ensuring symlink-related messages appear consecutively in the info section. The patch follows the series' pattern of code hygiene improvements while working with fsck infrastructure.

This change is straightforward and uncontroversial, though it touches a central header file used across Git's validation system. The alphabetical ordering makes the enum easier to navigate and maintain, particularly as new fsck messages are added. The patch fits naturally in the series' broader effort to improve fsck organization while maintaining the separation between Git's fsck and reftable internals.