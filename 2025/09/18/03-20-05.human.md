This is a platform compatibility follow-up regarding NonStop x86 builds. Randall Becker confirms that Jeff King's proposed solution to replace `uintptr_t` with `uintmax_t` in the clar test framework would work on NonStop system, though he notes `uintptr_t` is technically a standard extension while `uintmax_t` is fully standard.

The discussion reveals two viable paths forward: either adopt Jeff's type substitution approach (which mirrors Git's existing portable type handling) or add `-D__NSK_OPTIONAL_TYPES__` to the build flags for NonStop systems. Randall is currently testing the compiler flag approach but acknowledges the type substitution would be cleaner if incorporated into Git's platform-specific configuration.

This exchange shows good progress toward resolving the build issue, with both participants offering technically sound solutions. The next step would likely be deciding which approach to standardize on - the more portable type change or the platform-specific compiler flag. Randall's testing of the flag-based solution may provide useful data for that decision.