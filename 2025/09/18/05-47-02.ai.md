Feature implementation adding the new `git refs optimize` subcommand as part of the ref optimization effort. This is patch 7/9 in the series that builds on previous infrastructure changes to introduce the user-facing command.

The patch adds the `optimize` subcommand to `git-refs`, which provides identical functionality to `git pack-refs` as its modern replacement. The implementation reuses the `pack_refs_core()` helper function that was factored out from `git pack-refs` in earlier patches, ensuring consistent behavior between the two commands. The documentation includes the new subcommand in `git-refs.adoc`, leveraging the shared options file via AsciiDoc includes to maintain parity with `git-pack-refs` documentation.

Key technical details:
- Adds `cmd_refs_optimize()` in builtin/refs.c that calls `pack_refs_core()`
- Documents the subcommand in git-refs.adoc with identical options as pack-refs
- Uses the `PACK_REFS_OPTS` macro for consistent option handling
- Maintains the same exit-code behavior as `git pack-refs`

This completes the core implementation of the new subcommand while maintaining all existing functionality. The patch follows the established pattern of the series by building on the shared infrastructure (pack-refs.c library and documentation includes) created in previous patches. The implementation appears clean and well-integrated with the existing codebase.