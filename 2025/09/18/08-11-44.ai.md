This is a bugfix patch in the reftable fsck validation series, specifically addressing format validation in the reftable backend. The patch tightens parsing of the 'tables.list' file by enforcing that it must end with a trailing newline, which was previously not checked.

The change modifies `parse_names()` in reftable/basics.c to return a REFTABLE_FORMAT_ERROR when no trailing newline is found, while maintaining all existing functionality for properly formatted input. The function signature is updated to accept an error output parameter, allowing callers to distinguish between memory allocation failures and format errors. Corresponding unit tests are added to verify both the new validation and existing behavior.

The patch demonstrates careful attention to error handling, with distinct error codes for format violations versus memory allocation issues. It follows the series' pattern of moving validation logic into the reftable layer while maintaining clear error reporting boundaries. The implementation includes comprehensive test coverage, with new cases for the error condition and preservation of existing empty-string handling behavior.

This change was prompted by Junio Hamano's feedback in prior rounds, moving what was originally a fsck check into runtime validation. The patch touches core reftable parsing logic but has limited scope, focusing specifically on the newline validation requirement while keeping the surrounding name parsing behavior intact.