This is the reftable backend implementation for the new `optimize` API, part of the ongoing ref optimization effort. It's the third patch in a 9-part series that's building out a replacement for `git pack-refs`.

The patch adds the reftable-specific implementation of the `optimize` operation, which for this backend means compacting tables. The implementation is straightforward - it reuses the existing `reftable_be_pack_refs()` function through a new wrapper. The change is minimal (just 7 lines added) as it builds on prior work.

This follows the architectural pattern established earlier in the thread where each backend provides its own optimization implementation. The files backend implementation was completed in previous patches, and now this one handles the reftable case. The patch maintains consistency with the series' approach of gradual, systematic implementation while preserving existing behavior.

The implementation looks clean and focused, directly addressing its narrow scope within the larger effort. With this patch, both major ref storage backends now support the new optimization API, setting up for the planned introduction of the new `git refs optimize` command in later patches.