This is a feature implementation following Junio Hamano's architectural guidance for ref optimization, now with backend-agnostic API and implementations for both files and reftable backends. Meet Soni's v3 series completely reworks the earlier approach to align with the settled direction of making optimization a backend method.

The 9-patch series introduces a new `refs_optimize()` API in `refs.[ch]` that backends must implement, with concrete implementations in `files-backend.c` (packing) and `reftable-backend.c` (compaction). The existing `pack-refs` command is converted to use this new API, while the new `git refs optimize` subcommand shares the same core logic through a factored-out `pack_refs_core()` helper. Documentation is reorganized with shared options in `pack-refs-options.adoc`, and tests are refactored into a shareable `t/pack-refs-tests.sh` library.

Key technical elements:
- New `refs_optimize()` replaces `refs_pack_refs()` in the refs API
- Backend implementations must handle their own optimization strategies
- Shared option parsing via `PACK_REFS_OPTS` macro
- Test coverage includes 430+ lines of refactored tests plus new `t1463` for the subcommand
- Documentation uses AsciiDoc includes for shared option descriptions

The series appears thorough, with each commit focused and well-explained. Test coverage is extensive, building on the existing pack-refs tests while adding new verification for the reftable backend case. The documentation approach follows Junio's earlier feedback about clear commit messages and proper file organization.

This represents the implementation phase of the previously agreed architecture, with all major components now in place: API definition, backend implementations, command integration, and shared test/documentation infrastructure. The remaining work would be final review and potential UI polish, but the core technical foundation seems complete.