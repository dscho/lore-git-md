This is a refactoring patch in the xdiff preparation series aimed at Rust compatibility. It eliminates redundant fields in the `xdlclass_t` struct by replacing them with direct access to an embedded `xrecord_t` struct, which already contained the same data (`ha`, `line`, and `size` fields). The change simplifies the code by removing aliased fields that could lead to maintenance issues, particularly when making future Rust-related changes.

The patch is part of a well-structured series that has already gone through one round of review and incorporates performance measurements and "Best-viewed-with" trailers for easier code review. This particular change is purely internal and maintains identical diff output while reducing the code by 4 lines. The series as a whole is now in its second version and ready for merging to 'next', with the Rust-specific changes deferred to a follow-up series.