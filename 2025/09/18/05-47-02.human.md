Feature implementation adding the new `git refs optimize` subcommand. This is part 7/9 of Meet Soni's series to modernize ref handling by introducing a replacement for `git pack-refs`.

The patch adds the new subcommand which provides identical functionality to `git pack-refs` but under the more intuitive `git refs optimize` name. It reuses the shared `pack_refs_core()` helper function that was factored out in earlier patches, ensuring consistent behavior between the old and new commands. The documentation is updated to include the new subcommand, leveraging the shared options file created earlier in the series.

This appears to be a well-structured addition that completes the architectural vision established earlier in the thread. The implementation is clean, reusing existing components while providing a more discoverable command name. The patch maintains all existing functionality while paving the way for eventual deprecation of `git pack-refs`. With thorough documentation and integration of the shared options system, this looks ready for inclusion pending any final review feedback.