This is a test modernization patch from Patrick Steinhardt, part of a 5-patch series fixing bugs in `git config get --type=color` functionality. The patch focuses on cleaning up test formatting in t1300-config.sh.

The changes move test expectations from being written outside test bodies (using `cat > expect` before `test_expect_success`) to being written inside the test bodies. This matches Git's modern test style. The patch also normalizes heredoc usage, using `<<\EOF` for cases without variable expansion and `<<-EOF` for indentation-sensitive cases. Two exceptions are left unchanged as they're reused across multiple tests.

The diff shows extensive mechanical changes to the test file, with no functional changes to the tests themselves. This appears to be preparatory work for the subsequent patches in the series that will modify the config color handling behavior. The changes are straightforward and follow established patterns for test modernization.

Key technical details:
- Modifies t1300-config.sh test file
- Changes test expectation writing style from external files to inline heredocs
- Standardizes on `<<\EOF` and `<<-EOF` heredoc markers
- Leaves two test cases unchanged due to cross-test reuse
- Part of larger series addressing color handling in git config (patches 2-5)