This is the final implementation of the `git refs optimize` subcommand, completing the ref optimization functionality that has been under development in this thread. The patch adds the new subcommand to `builtin/refs.c`, sharing the core packing logic via `pack_refs_core()` that was extracted from `git pack-refs`. 

The implementation shows:
- New `REFS_OPTIMIZE_USAGE` macro that reuses the existing `PACK_REFS_OPTS` for consistent option handling
- `cmd_refs_optimize()` function that serves as a thin wrapper around `pack_refs_core()`
- Addition of the new subcommand to the main `cmd_refs()` usage array

Junio Hamano notes two minor style issues that don't require a resend:
1. A function declaration alignment issue in `cmd_refs_optimize()` that he'll fix locally
2. An observation about inconsistent usage string handling in `cmd_refs()` that should be cleaned up in a follow-up patch after this series lands

This patch represents the culmination of the architectural work described in the thread summary, providing users with a backend-agnostic way to optimize ref storage through either `git pack-refs` (now refactored) or the new `git refs optimize` command. Both commands share identical core logic, test coverage, and documentation while maintaining compatibility with existing workflows. The implementation follows all the key architectural decisions made earlier in the thread, including the shared infrastructure approach and standardized API patterns.