This is a platform compatibility follow-up regarding NonStop x86 builds. Randall Becker confirms Jeff King's proposed solution to replace `uintptr_t`/`PRIxPTR` with `uintmax_t`/`PRIuMAX` in the test framework's pointer comparison output would work on NonStop systems, noting `uintmax_t` is more standard than the extension `uintptr_t`. 

Becker is currently testing a workaround using `-D__NSK_OPTIONAL_TYPES__` in CFLAGS but suggests the more permanent fix would be to add this define in `config.uname.mak` for NonStop. The discussion confirms the technical viability of King's approach while leaving open the implementation path (direct code change vs build system modification).

Key technical details for future reference: The change affects clar test framework's assertion formatting in `t/unit-tests/clar/clar.c`, specifically pointer comparison output. The proposed type substitution maintains identical runtime behavior while improving portability. NonStop's unusual type system classification makes `uintptr_t` an optional extension rather than standard.

This moves the thread toward resolution, with Becker validating the core technical approach while suggesting an alternative integration point. The remaining question is likely whether to modify the code directly (King's approach) or handle via build system configuration (Becker's suggestion).