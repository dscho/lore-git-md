This is part of the reftable fsck validation series, specifically addressing update index sequencing in reftable stacks. Patch 4/8 adds validation to ensure tables loaded into a reftable stack have sequential update indices, raising a REFTABLE_FORMAT_ERROR if they don't.

The change enforces that each table's min_update_index must be exactly one greater than the previous table's max_update_index. This maintains the expected sequential ordering of updates in the stack. The implementation adds the check during stack reloading and includes a new unit test that verifies the validation by attempting to add non-sequential updates.

This is a focused validation improvement that follows the series' architectural approach of moving checks from fsck to runtime validation where appropriate. The patch appears well-contained, with clear test coverage demonstrating both the happy path and error case. It builds on prior feedback in the thread about where validation should live.