Here's a human-readable summary of the thread's current state:

The thread addresses a compatibility issue in Git's test framework where pointer comparison output fails on NonStop x86 systems due to `uintptr_t` being an optional extension. After initially proposing to use `uintmax_t` as a workaround, Patrick Steinhardt discovered this solution breaks on 32-bit systems where pointer sizes differ. The discussion has now shifted to using the simpler `%p` format specifier, despite it producing platform-dependent output.

Junio Hamano recently weighed in, suggesting this approach could work if tests verify only the presence/absence of output rather than exact formatting. This would make the platform-specific nature of `%p` acceptable for diagnostic purposes. While this represents emerging consensus, the solution still needs validation from NonStop maintainer Randall Becker. The alternative would be to drop the problematic test case entirely.

The thread highlights the challenges of cross-platform compatibility in test infrastructure, particularly around pointer handling. What began as a NonStop-specific issue has revealed deeper architectural considerations affecting 32-bit systems, demonstrating how platform constraints can surface late in technical discussions. The outcome will influence how the clar test framework reports pointer comparison failures across different architectures.