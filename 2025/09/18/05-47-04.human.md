Test patch completing the `git refs optimize` implementation series. This is the final patch in a 9-part series that introduces a new subcommand to optimize ref storage, building on the existing `git pack-refs` functionality. The patch adds a test driver for the new `git refs optimize` command that reuses the comprehensive test suite originally written for `git pack-refs`.

The test script `t1463-refs-optimize.sh` is intentionally minimal - it simply sets the command to test (`refs optimize`) and sources the shared test library created in earlier patches. This ensures the new command gets identical test coverage to the existing `pack-refs` command while avoiding code duplication. The tests verify that the new command maintains the same branch semantics as the original.

This follows through on the architectural approach established earlier in the thread, where both commands share core logic through a common `pack_refs_core()` function. The patch completes the test coverage portion of the implementation, leaving only potential documentation polish as remaining work. The implementation appears thorough and well-integrated with the existing test infrastructure.