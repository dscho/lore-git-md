Refactoring patch in a series improving `git config` color handling. This is patch 3/5 in Patrick Steinhardt's series addressing color-related bugs in `git config get --type=color`.

The patch modifies the `get_color()` function in builtin/config.c to return an error code rather than dying when encountering invalid color values. This makes the function more flexible for future callers that may want to handle errors differently. The change maintains existing behavior for the current caller (`ACTION_GET_COLOR`) while preparing for a subsequent patch that will introduce new error handling.

The change is straightforward - converting the `die()` call to return an error code and adjusting the function signature. This follows the series' pattern of improving error handling while maintaining backward compatibility. The patch has already been through multiple review iterations and appears ready for inclusion.