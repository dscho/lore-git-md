This is a refactoring patch in the ongoing ref optimization series, converting `git pack-refs` to use the new backend-agnostic `refs_optimize()` API. The change is minimal (just one line) but significant as it makes the command work with any reference backend that implements optimization, rather than being files-backend specific. The patch maintains all existing option handling while switching the underlying implementation to the new API.

The change replaces the backend-specific `refs_pack_refs()` call with the generic `refs_optimize()` in `builtin/pack-refs.c`. This is part of the systematic conversion outlined in the thread, where both files and reftable backends now implement the optimization API. The patch preserves the existing option parsing and ref inclusion logic, only changing the final dispatch mechanism.

Key technical details include:
- Maintains identical behavior for files backend users
- Enables future support for reftable and other backends
- Keeps existing `pack_refs_opts` structure for compatibility
- Follows the architectural direction established earlier in the thread

This patch represents the user-facing culmination of the backend work shown in the thread summary, connecting the new API to an actual Git command. The minimal diff suggests this is a straightforward mechanical conversion now that the underlying infrastructure is in place. Subsequent patches in the series will presumably introduce the new `git refs optimize` command while maintaining this compatibility path.