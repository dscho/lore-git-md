This is a refactoring patch in the ongoing ref optimization series, specifically part 5 of 9. It extracts the core logic from `builtin/pack-refs.c` into a new shared library (`pack-refs.[ch]`) to enable code reuse with the upcoming `git refs optimize` command.

The patch cleanly splits the functionality:
- The new `pack-refs.c` contains all the option parsing and packing logic in a `pack_refs_core()` function
- The original `builtin/pack-refs.c` becomes a thin wrapper that just calls this shared function
- A new header `pack-refs.h` defines the public interface and a common options string macro

This is a straightforward structural change that maintains all existing behavior while preparing for the new subcommand. The implementation looks solid, with proper attention to header inclusion and build system updates. The commit message clearly explains the motivation (enabling code sharing) and the technical approach taken.

This continues the series' pattern of careful, incremental changes to modernize the refs infrastructure while preserving backward compatibility. The patch appears ready for inclusion, with the only remaining work being the actual introduction of the new subcommand in later patches.