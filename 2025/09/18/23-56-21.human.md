Refactoring patch in the xdiff preparation series for Rust compatibility. This is patch 2/10 in v2 of the preparatory cleanups that are now ready for merging to 'next'.

The patch simplifies memory management in `xdl_prepare_ctx()` by eliminating redundant local variables and directly initializing/freeing the `xdfile_t` struct members. The key change is removing the hand-rolled cleanup code in favor of using the existing `xdl_free_ctx()` function, reducing duplicate cleanup logic. This maintains identical behavior while making the code more maintainable.

The changes are purely internal to xdiff's preparation phase and don't affect diff output. The patch fits into the larger series goal of cleaning up xdiff internals to make them more amenable to potential Rust integration, though this particular change stands on its own as a general improvement. The commit message clearly explains the motivation and the change is straightforward, touching only xdiff/xprepare.c.