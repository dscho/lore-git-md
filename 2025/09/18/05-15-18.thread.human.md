Here's a human-readable summary of the thread so far:

The discussion centers on the deprecation of `git whatchanged`, with an end user requesting the command be kept while contributors maintain it should be replaced by `git log`. The thread began with a user (Hamid Ghadyani) expressing a desire to retain `git whatchanged` without providing specific technical justification for keeping the deprecated command.

Kristoffer Haugsbakk responded by clearly stating the official position - that `git whatchanged` is redundant functionality that can be exactly replicated using `git log --raw --no-merges`. This follows Git's documented migration path for users who relied on the old command. The response treats the deprecation as settled rather than opening it for reconsideration.

So far, the exchange shows a pattern of users expressing attachment to the familiar command while contributors consistently point to the supported alternative. No maintainers have participated yet, and no new technical arguments for keeping `git whatchanged` have emerged. The thread currently reflects a straightforward case of user preference meeting project policy on removing duplicate functionality.