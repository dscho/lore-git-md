This is a discussion about semantic conflict handling during rebases, continuing the thread about Antonio Mennillo's `git-rebase-clean` tool. Brian m. carlson responds to the revised proposal that now preserves commit granularity through temporary squashing and reconstruction.

Brian acknowledges the practical challenges of enforcing atomic commits in team environments, especially with junior developers, but expresses skepticism about the tool's broader applicability. He notes several limitations: the approach only handles specific transformations (like symbol renames), can't address many semantic changes (like required header inclusions in C), and might create unrealistic user expectations about its capabilities.

The response highlights two key concerns for Git core integration: the tree-sitter dependency (currently controversial in Git's ecosystem) and Git's philosophy of remaining language-agnostic. Brian points out that many Git users work with non-code content where language-specific tools wouldn't help. While recognizing the tool's value for certain cases, he suggests it may be better suited as an external tool rather than a core rebase strategy.

The exchange shows the tension between practical workflow needs and Git's design principles, with Brian maintaining a conservative stance on core integration while acknowledging the real-world problems the tool attempts to solve. The discussion remains open-ended, with no clear resolution yet about whether this approach could evolve into an acceptable solution.