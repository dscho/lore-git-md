This is a follow-up in the thread about adding `-u` as shorthand for `--set-upstream` in `git pull`. Junio C Hamano is seeking clarification about the proposed workflow justification from Julien Jerphanion's previous email.

The maintainer questions whether the described conda-forge workflow actually involves frequently changing the upstream branch definition, which would be the primary use case for a shorthand option. He specifically asks if users are repeatedly redefining the upstream to point to different forks they pull from and push to, which would make the `-u` option particularly valuable. This gets at whether the workflow justification truly demonstrates a need for the shorthand or if it's describing a less common pattern.

The email doesn't introduce new technical content but focuses on understanding the claimed use case better. The core issue remains whether this specific workflow is frequent and painful enough to justify permanently allocating the short option `-u` in `git pull`'s namespace. The thread is currently stalled on this point of clarification about the actual workflow patterns in conda-forge feedstocks.