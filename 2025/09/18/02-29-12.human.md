This is a platform compatibility discussion about NonStop x86 builds, specifically addressing a test framework issue with `uintptr_t` usage. Jeff King responds to Randall Becker's report of build failures in the clang test suite by analyzing the problem and proposing a potential solution.

Jeff notes that while Git's core code handles `uintptr_t` portably through `compat/posix.h`, the clang test framework has its own implementation that doesn't include similar compatibility measures. He identifies two potential issues: the `uintptr_t` type itself and the `PRIxPTR` format specifier that may not be universally available.

The proposed solution suggests replacing the problematic `uintptr_t`/`PRIxPTR` combination with `uintmax_t`/`PRIuMAX`, which might provide more portable pointer formatting. This appears to be a focused, surgical change that could resolve the immediate build issue without requiring broader test infrastructure modifications.

The discussion remains technical and constructive, with Jeff building on Randall's initial report by offering a concrete patch suggestion. The proposed change looks reasonable for the stated problem, though its effectiveness on NonStop platforms would need verification from Randall or other NonStop maintainers. This appears to be a typical platform-specific compatibility discussion where Git maintainers collaborate with platform experts to find minimally invasive solutions.