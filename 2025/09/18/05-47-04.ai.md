This is the final test implementation patch in the ref optimization series, adding test coverage for the new `git refs optimize` subcommand. The patch creates `t/t1463-refs-optimize.sh` as a thin test driver that leverages the shared test library (`t/pack-refs-tests.sh`) created in the previous commit. By setting `pack_refs='refs optimize'` before sourcing the shared tests, it ensures the existing 430+ lines of test coverage for `git pack-refs` are also run against the new subcommand, verifying identical behavior.

The implementation follows the established pattern of the series - minimal new code that reuses existing infrastructure. The test confirms the new subcommand`git refs optimize` works as a drop-in replacement for `git pack-refs`, maintaining the same branch behavior. The test is added to the meson build system and uses the same test framework variables (`GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME` and `GIT_TEST_DEFAULT_REF_FORMAT`) as other ref-related tests.

This completes the test coverage portion of the ref optimization work, with all existing pack-refs tests now running against both commands. The shared test library approach demonstrates the architectural principle of the new subcommand being behaviorally equivalent to the old one, while allowing future tests to be written once and run against both commands. The patch maintains the series' high standard of thorough test coverage with minimal duplication.