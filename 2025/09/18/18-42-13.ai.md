This is a test methodology discussion in the final stages of the "master" to "main" default branch name transition series. The email addresses a specific test case in `t0001-init.sh` that verifies advice messages about unconfigured branch names.

Junio Hamano agrees with Phillip Wood's observation that a test case checking advice messages should not be gated behind the `!WITH_BREAKING_CHANGES` conditional, since these messages should persist even after the breaking change takes effect. The test in question uses colorized advice output to verify the hint message appears during repository initialization when no default branch is configured.

This represents a minor refinement to the test suite that was overlooked when simplifying other test cases. Both maintainers are in agreement about keeping the advice message verification, differing only on whether to use exact output comparison (`test_cmp`) or pattern matching (`test_grep`) in previous discussions. The change is straightforward - simply removing the conditional from the test case while maintaining its core verification of the advice system's behavior.

The discussion shows the maintainers carefully considering long-term behavior even for transitional code paths, ensuring the test suite properly validates both current and future states of the advice system around branch naming. This is the last remaining detail being polished in the series before finalization.