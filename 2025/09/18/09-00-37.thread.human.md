Phillip Wood has sent a two-patch series fixing an inconsistency in `git rebase --autosquash` behavior regarding the `commit.cleanup` configuration. The issue, originally reported by Simon Cheng, involved fixup commits not properly respecting the configured message formatting during interactive rebase operations.

The first patch addressed the core problem by making fixup commits properly handle the `commit.cleanup` setting, bringing their behavior in line with regular pick commands. This included enhanced test coverage showing the fix working with different cleanup configurations. The second patch, now received, completes the work by removing the now-obsolete VERBATIM_MSG flag from sequencer.c, which was rendered unnecessary by the first patch's changes.

The series builds on previous rebase/autosquash work (commits 92c87bdc40 and f7d42ceec52) and appears ready for merging, with no outstanding questions or objections raised. The changes are focused and well-tested, simplifying the codebase while maintaining consistent behavior.