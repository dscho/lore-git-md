This is a follow-up in the `git whatchanged` deprecation discussion thread. Kristoffer Haugsbakk responds to Hamid Ghadyani's request to keep the deprecated `git whatchanged` command by providing the equivalent `git log` invocation that reproduces its behavior (`git log --raw --no-merges`).

The email reinforces the official position that `git whatchanged` is redundant functionality that can be fully replaced by `git log` with appropriate flags. No new technical arguments are introduced - it simply restates the documented migration path for users who still rely on the deprecated command.

Key technical detail provided is the specific `git log` flags that match `git whatchanged`'s output format (showing raw diff output while excluding merge commits). The response maintains the deprecation stance without engaging with the request to reconsider, suggesting this is likely considered a settled matter by contributors.

The brief exchange shows user pushback against deprecation but no substantive technical reasons to keep the command, with contributors consistently pointing to `git log` as the supported alternative. No maintainers have weighed in yet in this thread segment.