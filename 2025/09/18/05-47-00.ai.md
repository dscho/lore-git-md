This is a refactoring patch in the ongoing ref optimization series, specifically focusing on code organization for the `git pack-refs` command. The patch factors out the core logic from `builtin/pack-refs.c` into a new shared library (`pack-refs.[ch]`) to enable reuse by the upcoming `git refs optimize` command.

The key changes are:
1. Moves all option parsing and packing logic into new `pack_refs_core()` function in `pack-refs.c`
2. Leaves `builtin/pack-refs.c` as a thin wrapper that just calls the shared function
3. Introduces `PACK_REFS_OPTS` macro in the new `pack-refs.h` to share option documentation
4. Updates build systems (Makefile and meson.build) to include the new library

The refactoring maintains all existing functionality while making the code reusable. The patch follows the architectural direction established earlier in the thread, where `git pack-refs` was converted to use the new `refs_optimize()` API. This change prepares for the next step of introducing the new `git refs optimize` command while keeping the option handling consistent between both commands.

The implementation appears clean and well-motivated, with proper header documentation and build system integration. The patch maintains all existing behavior while enabling future expansion, aligning with the series' goal of modernizing ref storage operations.