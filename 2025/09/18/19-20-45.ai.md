This is a technical discussion about improving commit existence checking methods in Git, focusing on the limitations of `git fetch --dry-run --filter` and exploring alternatives like `--negotiate-only`.

Jeff King (peff) responds to Kevin Puetz's investigation of using `--negotiate-only` for checking commit existence without downloads. He confirms the observed "expected 'acknowledgments', received 'packfile'" error is likely a client-side bug in the negotiation protocol, though server behavior may also be problematic. The error occurs when checking unrelated commits/remotes, making the output less clean than traditional fetch errors.

The email explores several technical approaches for commit checking:
1. The current `--dry-run --filter` method (modifies config)
2. `--negotiate-only` (has protocol quirks)
3. Potential use of the "object-info" protocol extension (server-dependent)
4. Partial clone's internal fetch mechanism (still modifies config)
5. Workarounds using tmp-objdir or fake repos (complex but avoids config changes)

Key technical details:
- `--negotiate-only` protocol flow shows client making an extra empty request triggering incorrect packfile response
- Partial clone's `fetch_objects()` uses similar flags to Kevin's original approach
- Workaround suggestions involve environment variables (`GIT_ALTERNATE_OBJECT_DIRECTORIES`, `GIT_OBJECT_DIRECTORY`)
- Server would need explicit support for cleaner solutions like "object-info"

The discussion remains unresolved but surfaces multiple implementation constraints and edge cases that would need addressing for any robust solution. The thread continues weighing tradeoffs between correctness, complexity, and user experience for this niche but important use case.