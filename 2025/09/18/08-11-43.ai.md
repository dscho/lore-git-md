This is a refactoring patch in Karthik Nayak's reftable fsck validation series (v3 2/8). It moves a consistency check message from the files-backend implementation to the generic refs layer, making it shared between both files and reftable backends.

The change is straightforward: the "Checking references consistency" message that was previously printed in `files_fsck_refs()` is now moved to the generic `refs_fsck()` function that serves as the entry point for all ref backends. This ensures both backends will show the same message when running fsck in verbose mode. The patch removes the message from `files-backend.c` and adds it to `refs.c` with the same conditional `o->verbose` check.

This appears to be a preparatory change for the larger reftable validation work, ensuring consistent messaging across backends. The implementation is cleanly done with a simple code movement and no behavior change beyond the message location. The patch follows the series' theme of properly separating concerns between generic refs infrastructure and backend-specific implementations.

For future thread context: this touches `refs.c` and `refs/files-backend.c`, introducing no new concepts but establishing a pattern where fsck-related messaging is handled at the generic layer. The change is uncontroversial and unlikely to generate discussion beyond perhaps minor style points about message placement.