This is a reply in the ongoing discussion about the deprecation of `git whatchanged`. Kristoffer Haugsbakk responds to a user request to keep the deprecated command by providing the equivalent `git log` invocation that reproduces its behavior.

The email is brief and to the point, offering a direct solution for users who want `git whatchanged` functionality. The suggested replacement (`git log --raw --no-merges`) matches the output format of the deprecated command while using Git's more modern and feature-rich logging tool.

This reply reinforces the project's position that `git whatchanged` is redundant and can be fully replaced by `git log` with appropriate flags. The response doesn't engage with any potential arguments for keeping the command, instead providing a practical migration path for users.

The exchange shows the project's typical approach to deprecation - providing clear alternatives rather than maintaining legacy interfaces. No new technical information is introduced beyond the specific flag combination for reproducing `git whatchanged` behavior.