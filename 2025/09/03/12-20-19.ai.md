This is a response in the ongoing `git-history` RFC discussion thread, specifically about the `split` subcommand implementation. Patrick Steinhardt is addressing feedback from D. Ben Knoble regarding the diff visibility and error handling in the `split` operation.

Key points in this response:
1. Confirms the `split` subcommand should show diffs between old and new trees, but acknowledges potential bugs in the current wt-status interface implementation that may prevent this from working correctly in all cases. Patrick requests a reproducible test case to investigate further.

2. Notes that the error message about using `-m` or `-F` options is currently incorrect since only `-m` is implemented, though `-F` support might be added later.

3. Acknowledges the usability concern about not being able to retry after an empty commit message error, but explains this is currently difficult due to in-memory index constraints. While recognizing this as a valid improvement area, Patrick indicates it's not being addressed in the current iteration.

The response shows Patrick engaging with the detailed interface review while maintaining the current design boundaries of the RFC. The discussion continues to refine the user experience aspects of the `split` subcommand, particularly around diff visibility and error recovery workflows.