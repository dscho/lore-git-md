This is a discussion about validation philosophy in the reftable fsck series, specifically regarding patch 4/5 which checks for trailing newlines in `tables.list`. Patrick Steinhardt responds to Junio Hamano's architectural feedback about whether certain validation checks should be runtime requirements rather than fsck-only checks.

Patrick agrees with Junio's position that runtime parsing should reject malformed `tables.list` files entirely, particularly for format violations like missing trailing newlines. He draws a distinction between this case and earlier name validation checks - format integrity is fundamental while naming conventions are more advisory. The exchange highlights an ongoing tension in the series between fsck validation (which can warn about issues while allowing operation) versus runtime validation (which would fail operations on malformed data).

Key technical points:
- Junio questioned why only some anomalies (like missing newlines) would be fsck-only while others cause runtime failures
- Patrick concurs that format integrity checks belong in runtime parsing
- The discussion may lead to moving patch 4/5's newline check from fsck to core reftable reading code
- This aligns with Junio's earlier feedback about patch 5/5's update index validation

The thread shows the series evolving from pure fsck checks toward potentially stricter runtime requirements, particularly for fundamental format violations. Patrick's response indicates receptiveness to this architectural shift while maintaining that some validation (like table naming) can remain fsck-only.