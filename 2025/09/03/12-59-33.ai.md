This appears to be a bugfix for a hardware monitoring driver (nct7802.c) that handles temperature-to-fan mapping. The patch corrects a macro definition that doesn't match the chip's documentation. The current implementation uses a modulo operation that produces incorrect shift values, while the proposed fix uses conditional logic to match the documented behavior from the chip's 2020 specification (Table 7.2.95).

The change is narrowly focused on the SMARTFI_EN_SHIFT macro, which appears to control how temperature sensors are mapped to fan control outputs. The proposed fix uses a more explicit conditional expression that directly encodes the documented shift values (0 for x=0, 5 for x=1, and 2 otherwise) rather than the current modulo-based calculation.

While the patch is small and specific, it's addressing what appears to be a clear discrepancy between the code and hardware documentation. The commit message could be more detailed about the practical impact of this mismatch (whether it causes incorrect fan behavior or just doesn't match the docs), but the technical change itself seems straightforward. This would likely be of interest to maintainers of hardware monitoring drivers and users of the NCT7802 chip.