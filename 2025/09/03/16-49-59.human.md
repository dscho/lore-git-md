This is a documentation patch in Julia Evans' ongoing series to improve `git-checkout` documentation, now in its third revision. The patch focuses specifically on clarifying the `-b` and `-B` options.

The changes streamline the documentation by moving most of the detailed behavior description to the `-b` section and making `-B` simply reference it, since `-B` users are likely already familiar with `-b`. The patch removes redundant explanations about `--track` (pointing users to the OPTIONS section instead) and makes the default start point (current commit) explicit. The transactional behavior description is simplified while maintaining accuracy.

This continues the series' trend of making the documentation more approachable while preserving technical correctness. The changes appear well-considered, building on previous rounds of review that confirmed all implementation details. As the third revision in a mature series that has already addressed all major technical concerns, this patch represents final polishing of the documentation wording and organization.