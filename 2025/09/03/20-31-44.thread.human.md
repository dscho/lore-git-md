Here's a human-readable thread summary:

D. Ben Knoble has reported puzzling performance differences between Git binaries on macOS. A Homebrew-installed Git (4.4MB) runs basic commands about 5x faster than a self-compiled version (3.5MB), with timings of ~22ms vs ~110ms in a monorepo. The slower binary was built with DEVELOPER=1 flag but lacks some Homebrew settings like Perl/Python paths and USE_LIBPCRE, though these don't appear to explain the dramatic slowdown.

The investigation has ruled out macOS quarantine attributes as a factor. The binary size discrepancy (4.4MB vs 3.5MB) suggests meaningful compilation differences, though the exact cause remains unclear. The email also notes an incidental observation about RelNotes documentation not being converted to HTML during installation. This appears to be a genuine performance regression case rather than a routine build configuration issue, with future discussion likely focusing on compiler flags, optimization levels, or platform-specific compilation behaviors that might explain the significant speed difference.