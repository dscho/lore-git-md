This is a technical discussion about cache-tree infrastructure changes in Patrick Steinhardt's `git-history` RFC series (v3). The patch in question allows writing arbitrary in-memory indexes as trees, not just the repository's main index.

Junio Hamano notes this change might help simplify `git commit -o <pathspec>` by avoiding temporary index file handling, but questions whether hook requirements would still necessitate disk writes. Patrick responds that the sequencer already writes temporary indexes to disk for similar operations, suggesting the infrastructure could be reusable, though he's unsure if it would materially improve the existing implementation.

The exchange shows active consideration of how these low-level changes might benefit other Git commands beyond their immediate use in the history editing feature. This is part of the series' preparatory work (patch 5/16 in v2, now superseded by v3) that refactors core components to support the new functionality. The discussion remains technical and collaborative, with both participants exploring the implications of the change.