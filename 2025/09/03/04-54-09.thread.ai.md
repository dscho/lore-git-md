Here is the updated AI thread summary incorporating the new email:

This thread concerns a two-part series fixing duplicate ACK behavior in upload-pack protocol v2. Patch 1/2 modernized test infrastructure in t5530-upload-pack-error.sh to current Git conventions, cleaning up test patterns while maintaining coverage of upload-pack failure modes. Patch 2/2 (current email) addresses the core issue where non-commit objects sent repeatedly in "have" lines were stored multiple times in upload-pack's `have_obj` array, causing redundant ACKs and potential memory growth (4MB/s in testing).

The technical solution moves the `THEY_HAVE` flag check outside commit-specific logic in `do_got_oid()`, applying deduplication to all object types while maintaining backward compatibility with protocol v0/v1. The changes are confined to upload-pack.c's object handling logic. New test cases verify protocol v0 still ACKs duplicates (preserving backward compatibility) while v2 now ACKs each non-commit object only once, building on the test infrastructure cleanup from patch 1/2.

The series targets a regression/oversight in protocol v2 without affecting v0/v1, based on Git 2.51 (c44beea485). Key technical details include the test modernization patterns in t5530, changes to `have_obj` array management in upload-pack.c, and the protocol version-specific ACK behavior verification. The solution eliminates unnecessary network traffic while maintaining compatibility.