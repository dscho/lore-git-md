This is a documentation patch in Julia Evans' final polishing series for `git-checkout.adoc`. The patch focuses on restructuring the "restoring files" section to better match the command's SYNOPSIS and improve clarity.

The key change splits the file restoration documentation into two distinct cases: one for operations that specify a `<tree-ish>` (which updates both index and working tree), and another for operations without `<tree-ish>` (which only updates the working tree from the index). This makes the behavior differences more explicit and aligns with the command's structure. The patch also removes an unnecessary `-f` flag mention in the `<tree-ish>` case where it wasn't relevant.

As part of the v3 series that has already resolved all major technical accuracy questions, this change is purely organizational - it doesn't modify any actual behavior descriptions. The restructuring should make it easier for users to understand the different file restoration modes at a glance. The patch maintains all previously confirmed implementation details while improving the documentation's logical flow.