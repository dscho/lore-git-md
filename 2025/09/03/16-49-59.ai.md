This is a documentation patch in Julia Evans' v3 series improving `git-checkout` documentation, specifically focusing on clarifying the `-b` and `-B` options. The patch streamlines the explanation by moving most details to the `-b` description and making `-B` reference it, reducing redundancy.

The changes simplify the documentation by:
1. Removing redundant details about `--track` that can be found in the OPTIONS section
2. Explicitly stating the default start point (current commit) for `-b`
3. Making `-B` tersely reference `-b` rather than repeating shared behavior
4. Clarifying the failure case when checking out would overwrite uncommitted changes
5. Moving the transactional behavior explanation to `-b` where it applies to both options

The patch maintains all previously confirmed technical accuracy from earlier review rounds while improving readability. The `-B` description is notably more concise, assuming familiarity with `-b` since `-B` users would likely know `-b` already. The changes align with the series' goal of making the manual page clearer while preserving precise technical details.