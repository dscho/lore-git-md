This is a follow-up in the ongoing discussion about retaining `git whatchanged`. Anthony Clay responds to Jeff King's proposed patch that would allow aliases to override deprecated commands like `whatchanged`.

The email confirms that Clay and colleagues find the alias-based solution acceptable, noting it would work well with their existing Git configuration that gets imported across environments. This addresses one of their earlier concerns about maintaining workflow continuity. The response indicates general agreement with the technical approach proposed by Jeff King, though it doesn't touch on the remaining issue of container environments without alias configurations.

Key technical points from the thread context remain relevant: the patch modifies `git.c` to create an exception in the alias override rules specifically for deprecated commands, with `whatchanged` being the initial (and currently sole) command marked this way. The exchange shows progress toward a compromise that maintains the deprecation path while easing transition for users like Clay who have specific workflow dependencies.

The thread now appears to be converging on this solution, with the main outstanding question being whether it sufficiently address all real-world constraints (particularly in containerized environments where users may not control Git configurations). The discussion remains focused on balancing backward compatibility with long-term maintenance goals.