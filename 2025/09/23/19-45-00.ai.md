Documentation patch (2/4) in Julia Evans' series improving `git-pull` clarity. This patch focuses on explicitly listing the four main integration strategies for remote branches, addressing specific user confusion points from research.

The changes restructure the DESCRIPTION section to first emphasize `git fetch` happens before integration, then clearly enumerate the four options: `--ff-only` (now explicitly called out as default), `--rebase`, `--no-rebase`, and `--squash`, with brief explanations of each. The patch also mentions the corresponding configuration options (`pull.rebase`, `pull.squash`, `pull.ff`). The diff shows +14/-11 lines, maintaining technical accuracy while making the behavior more discoverable.

Key motivations from user feedback include: some users still expect merge as default, confusion about `--no-rebase` meaning merge, and lack of visibility for `--ff-only`. The patch addresses these by presenting the options as a clear numbered list with explicit defaults. This continues the series' pattern of moving from implicit knowledge to explicit documentation while keeping the same technical content. The changes align with the overall series goal of making `git-pull` behavior more transparent to users who don't know the underlying fetch/merge/rebase mechanics.