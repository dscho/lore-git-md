This is a follow-up discussion in the ongoing thread about fixing `git format-patch`'s inconsistent notes display between commit messages and range-diff output. The email focuses on refining the technical implementation details from the first patch in the series.

Junio C Hamano and Kristoffer Haugsbakk are discussing two key improvements to the initial solution: proper initialization of the new `rdiff_other_arg` strvec member in the `REV_INFO_INIT` macro, and potential renaming of the member to something clearer like `log_arg` or `log_args`. The discussion confirms that the initialization fix has already been incorporated, while the naming change remains under consideration.

The exchange clarifies that the core goal is ensuring range-diff shows the same notes namespaces as the main commit message output when comparing patches, rather than potentially showing different sets of notes. This addresses the original inconsistency where `--notes` arguments weren't being properly propagated to the range-diff subprocess in single-commit cases.

Technical points under discussion include:
- The initialization fix for `rdiff_other_arg` in `REV_INFO_INIT`
- Potential renaming of the member to better reflect its purpose
- Confirmation that the technical approach is sound but needs these refinements
- The relationship between format-patch's notes output and range-diff's comparison behavior

This email moves the thread forward by resolving the initialization concern and opening the naming discussion, while maintaining focus on the series' goal of consistent notes handling. The changes discussed here would affect the `revision.h` header and related initialization patterns.