This thread discusses a bugfix patch from Mathias Rav addressing inconsistent behavior in `git rebase -i`'s `fixup -C` command. The original patch proposed making author metadata handling consistent with message handling by preserving the current commit's author info when `-C` is used, rather than taking it from the previous commit. Karthik Nayak reviewed the technical changes positively but raised minor questions about help text wording and whether date handling should be made explicit.

Phillip Wood has now entered the discussion with a different perspective on the command's intended use. While Mathias's approach assumes `fixup -C` is used when splitting commits (where keeping the later commit's metadata makes sense), Phillip describes using it primarily for amending existing commits while rewording messages - a workflow where preserving the original commit's information would be preferred. He notes the current command design assumes fixing ancestors rather than descendants, and suggests manual squashing might be better for Mathias's split-commit scenario.

The discussion now needs to reconcile these two distinct use patterns for `fixup -C` operations. The core technical changes in the patch appear sound, but the behavioral change may impact workflows Phillip describes. Mathias will need to address both Karthik's original questions about implementation details and Phillip's concerns about workflow compatibility.