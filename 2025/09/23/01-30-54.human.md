This is the final resolution in a refactoring series to modernize xdiff internals for Rust compatibility. The thread has been discussing whether the `changed` array (formerly `rchg`) should use `bool` or `unsigned char` type, given its role in the diff algorithm's three-state logic (NO/YES/MAYBE).

Ezekiel Newren confirms that while the array itself only stores binary values (0/1), the MAYBE state is properly handled through a separate `u8 current_state` variable in xprepare.c. Junio Hamano had initially suggested `unsigned char` for safety but accepts the `bool` approach after seeing the technical clarification. The exchange includes pseudocode illustrating the control flow.

This concludes the series' technical discussions, with all major questions resolved. The preparatory cleanups (patches 1-10) are already in 'next', and this final clarification clears the way for merging the structural simplifications (12 patches). The work systematically eliminates redundant data structures while maintaining identical behavior, positioning xdiff for future Rust integration.