Refactoring patch in a completed series about packfile store centralization. This is patch 3/15 in Patrick Steinhardt's v6 series that was already merged, making minor final adjustments to the new `struct packfile_store` architecture.

The patch relocates an initialization flag from `struct object_database` to the new `struct packfile_store`, where it more logically belongs as part of the ongoing effort to centralize packfile management. The change is mechanical - moving the `packed_git_initialized` bit (now converted to a proper boolean) and updating the few access sites in packfile.c.

Since this is part of an already-merged series, the change represents final polish rather than new work. The commit message clearly explains the move and the boolean conversion, and the diff shows straightforward updates to odb.h, packfile.c and packfile.h. No functional impact is expected from this change - it's purely an internal cleanup as part of the larger architectural shift.