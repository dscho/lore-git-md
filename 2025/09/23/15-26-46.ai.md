This is a response in the `git repo stats` series, specifically addressing review feedback on patch 3/4 which implements the keyvalue output format. Justin Tobler responds to Patrick Steinhardt's suggestions about error message deduplication, function naming, and format handling.

Key technical details addressed:
- Agrees to change error message to use a generic "format '%s' not supported" string rather than hardcoding the table format check
- Explains the generic `stats_print()` name is intentional as it will be reused for NUL-delimited output in the next patch (which will combine patches 3 and 4)
- Notes the `BUG()` assertion is correct as all valid formats are already handled by the switch cases
- Plans to merge patches 3 and 4 to simplify the format handling logic

The response shows Justin thoughtfully considering Patrick's feedback while maintaining the architectural approach of having a shared print function for both keyvalue and NUL formats. This continues the series' pattern of iterative refinement based on review feedback, with Justin accepting some suggestions (error message unification) while explaining the rationale for keeping other design choices (generic print function name). The thread remains focused on polishing implementation details as the series nears completion.