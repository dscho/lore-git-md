This is a follow-up in the thread about deprecation warnings in Git 2.51.0, specifically addressing the `git whatchanged` and date-filtered `git log` functionality.

The original reporter S0AndS0 acknowledges Kristoffer Haugsbakk's helpful response about replacing `git whatchanged` with `git log --raw --no-merges` (or alternative output formats like `--stat` and `--name-only`). They indicate they'll create an alias to help with the transition.

The email raises a new question about the other half of the original issue - the deprecation warning for date-based filtering (`--since`). S0AndS0 asks whether there are plans to remove this functionality and what the replacement would be if so. This remains an open question in the thread, as Kristoffer's initial response only addressed the `whatchanged` aspect.

The tone is appreciative of the help received so far while seeking clarification on the remaining concern about date filtering. The thread continues to focus on understanding Git's deprecation plans and migration paths for these legacy features.