This is a review comment on a refactoring patch in the string-list API series. Karthik Nayak provides feedback on shejialuo's v2 patch that replaces negative index encoding with a boolean `exact_match` parameter in `string_list_find_insert_index()`.

Karthik suggests improving the commit message by first explaining what the function does before describing the negative index behavior it replaces. He notes the current approach of encoding exact matches via negative indices is confusing to read and approves of the cleaner boolean parameter approach. The review also acknowledges the patch's solution for callers that don't need the match information by allowing NULL for `exact_match`.

The feedback is positive overall, with Karthik calling the changes "much cleaner" and appreciating how they make the code more readable. This appears to be a final polish on a patch that has already gone through significant review and iteration in the thread. The series as a whole is methodically improving type safety in the string-list API while maintaining behavior.