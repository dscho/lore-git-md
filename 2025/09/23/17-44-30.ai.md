Documentation patch (part 3/4) refining the `git-push` man page's explanation of upstream branches. This follows the v2 series that was fully merged, with v3 addressing final feedback points.

The patch adds a comprehensive "UPSTREAM BRANCHES" section to `urls-remotes.adoc`, consolidating scattered information about this key Git concept. It explains how upstream branches are used by default for operations like `git pull`, `git push`, and `git status`, and describes where the configuration is stored. The section also covers automatic upstream branch setup during common operations like cloning and branch checkout.

Key technical details include:
- Documents the upstream branch relationship with `pushRemote` configuration
- Explains the connection between upstream branches and `git status` output
- Shows the actual config file storage format for upstream info
- Lists multiple ways upstream branches get automatically configured
- Notes the terminology variation between "upstream" and "tracking"

The patch maintains all prior build system compatibility (tab-indented examples) while adding this new section. It represents a polished addition to the documentation that was already well-reviewed in previous versions, with the new content following the same clear, user-focused style as the rest of the series.