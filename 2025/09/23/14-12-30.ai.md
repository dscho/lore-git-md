This is the final resolution in the xdiff refactoring thread, confirming the type representation for the `changed` array (formerly `rchg`). Junio C Hamano clarifies his position on using `bool` for the array after Ezekiel Newren's analysis showed it only stores binary states (0/1), with the MAYBE (2) state handled separately through a `u8 current_state` variable in xprepare.c.

Junio explicitly states he has no objection to the `bool` type for `changed[]` as long as the MAYBE state comparison logic remains sound. He notes that comparing a boolean (which can only be 0 or 1) against the literal value 2 (MAYBE) is valid C behavior, addressing his earlier concern about type safety. This confirmation resolves the last open question in the series.

The exchange shows mutual agreement on the technical solution while maintaining the three-state logic through separate variables. Junio also reassures Ezekiel about the phrasing in previous emails, indicating the discussion remained productive throughout. With this resolution, the v4 series appears ready for merging with all structural simplifications and type modernizations complete.