This is a technical discussion about bitmap-based push optimization strategies, continuing the thread about object reuse during pushes.

Jeff King clarifies how Git's reachability bitmaps can still provide benefits even after operations like `git pull` that create new bitmap-less packs. He explains the bitmap format's progressive degradation behavior: when traversing commits, Git can use any existing bitmap it encounters in the history, even if newer commits aren't covered by bitmaps. For example, it can use a bitmap at commit B to cover the history back to the root when processing commit C, only needing to traverse C's tree. For divergent histories (like commit D), the system relies on having multiple bitmaps sprinkled throughout history to limit traversal depth.

The key insight is that the presence of any bitmaps enables some optimization, even if not all objects are covered. The critical factor is Git's decision to use bitmaps at all during push negotiation, not whether every object has perfect bitmap coverage. This suggests that while maintaining fresh bitmaps is important for maximum efficiency, even stale bitmaps can provide meaningful bandwidth savings.

This response builds on Sainan's earlier confirmation that `git repack -adb` solves the amended commit case but raises concerns about bitmap invalidation after pulls. Jeff's explanation implies that the solution space may involve both bitmap maintenance strategies and smarter negotiation heuristics that better leverage partial bitmap coverage. The discussion continues to explore the tradeoffs between bitmap maintenance overhead and push optimization benefits.