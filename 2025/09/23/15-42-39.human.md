This is a bugfix patch in the reftable validation series, specifically addressing trailing newline checks in 'tables.list' files. Karthik Nayak responds to Junio Hamano's feedback by implementing proper error handling for missing newlines, which was previously silently ignored.

The changes modify `parse_names()` to return explicit error codes through a new `err` parameter, with callers like `fd_read_lines()` updated to handle these errors. A new unit test verifies the behavior when input lacks a trailing newline, returning `REFTABLE_FORMAT_ERROR` in such cases. The implementation maintains the existing behavior of dropping empty strings while adding proper validation.

This patch represents a focused improvement to the reftable validation infrastructure, catching a specific class of format errors that could lead to corruption. The changes are cleanly implemented with appropriate test coverage, and Karthik demonstrates responsiveness to review feedback by quickly addressing a minor whitespace issue Junio noted. The patch fits well within the larger series' goal of robust reftable validation while maintaining the library's clean boundaries.