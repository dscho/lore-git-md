This is a clarification in the ongoing thread about Git 2.51.0 deprecation warnings. D. Ben Knoble responds to S0AndS0's question about potential removal of the `--since` date-filtering option in `git log`, which had been raised as an open question in the thread.

The key new information is that Knoble finds no evidence in Git's release notes that `--since` is actually slated for deprecation, contrary to S0AndS0's concern. This suggests the warnings S0AndS0 encountered may be limited to `git whatchanged` usage rather than affecting date-filtering functionality more broadly.

The email is brief but important for the thread as it helps narrow the scope of the deprecation discussion. It indicates that while `git whatchanged` replacement is needed (as previously established), date-based filtering with `--since` appears safe to continue using. This leaves the thread with one less open question, though it doesn't explain why S0AndS0 saw warnings about date filtering in the first place.

Technical context for future summaries: The `--since` and related date-filtering options (`--until`, `--after`, `--before`) remain core `git log` functionality according to release notes. The thread now needs either confirmation that date filtering warnings were a false alarm, or explanation of which specific date-related uses are actually deprecated.