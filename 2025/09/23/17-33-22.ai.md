This is a follow-up discussion in the dangling symrefs thread, where Jeff King provides additional guidance on proper deletion patterns for dangling symrefs in response to Toon Claes' acceptance of the original technical solution.

Jeff suggests using `symref-delete` with explicit target verification as the safest approach for Gitaly to handle dangling symref deletions, rather than the current null-to-null update pattern. He explains that `symref-delete FOO_HEAD refs/heads/foo` provides atomic verification that the symref points to the expected target before deletion, similar to how `old-oid` checks work for regular refs. This maintains consistency with Git's atomic ref update semantics while preventing accidental deletions of non-symrefs.

The email reinforces the thread's conclusion that the original patch series (which prevents accidental clobbering of dangling symrefs during fetch operations) doesn't need modification, and that callers like Gitaly should adapt their usage patterns to use the existing `symref-delete` interface properly. Jeff's response provides concrete implementation guidance for Gitaly's migration path away from their current unusual usage pattern.

Key technical details mentioned include:
- The distinction between unconditional `delete` and target-verified `symref-delete`
- The parallel between `old-target` verification for symrefs and `old-oid` for regular refs
- The expectation that callers should know the expected state before deletion
- The test fixture context where target information is known

This email effectively closes the remaining open question about implementation guidance for Gitaly, allowing the original patch series to proceed unchanged while providing a clear path forward for affected downstream users.