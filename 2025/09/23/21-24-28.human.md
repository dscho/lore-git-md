This is a refactoring patch in the final stages of Ezekiel Newren's xdiff modernization series, which aims to clean up the diff algorithm internals for better maintainability and potential Rust integration.

The patch renames the `rchg` field in `xdfile_t` to the more descriptive `changed` across all xdiff implementation files. This is a straightforward rename with no behavior changes, updating all access sites to use the new name. The change appears mechanical and well-executed, touching all relevant files (xdiffi.c, xhistogram.c, xpatience.c, xprepare.c, xtypes.h, and xutils.c) with consistent updates.

This is patch 11/13 in the v5 series, which has already seen its preparatory cleanups merged to 'next'. The series has systematically eliminated redundant structures and improved code organization while maintaining identical runtime behavior. Earlier patches removed field duplication and an allocator, yielding a measured ~5% speedup.

The patch follows the series' pattern of careful, incremental improvements with thorough updates to all affected code paths. As with previous patches, it maintains the original algorithm semantics while making the code more maintainable. No open questions or controversies remain in the thread, and this appears ready for merging as part of the completed series.