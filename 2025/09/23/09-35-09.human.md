This is a follow-up discussion in the completed packfile store refactoring series, specifically addressing architectural boundaries in the code.

Patrick Steinhardt responds to Justin Tobler's observation about `midx.c` directly accessing the `packfiles->map` field, which was previously documented as private to `packfile.c`. The discussion clarifies that while the current access pattern should be updated to respect architectural boundaries, the comment about `packfile.c` exclusivity will eventually become obsolete as the data structures move into the object source backend.

The exchange represents minor architectural cleanup in an otherwise settled series. Patrick acknowledges the point but suggests deferring significant changes since the architecture will evolve further. This is typical post-merge refinement discussion where implementation details are being polished after the major technical work is complete.