Documentation formatting fix for Asciidoctor rendering issue in config/extensions.adoc.

Jean-Noël Avila identifies that the problematic "+" character rendering in the HTML output is specific to Asciidoctor (not asciidoc.py) and proposes a solution by restructuring the definition list syntax. The key changes involve:

1. Removing unnecessary open blocks (`--`) that were previously used as workarounds
2. Using triple colons (`:::` instead of `::`) for definition items that contain complex content (bullet lists followed by paragraphs)
3. Properly nesting bullet lists within explicit `--` blocks when needed

The patch demonstrates this approach by modifying several sections in extensions.adoc, including `worktreeConfig`, `preciousObjects`, and others. The changes maintain all existing content while fixing the visual rendering issue. This appears to be a targeted solution that leverages proper Asciidoc syntax rather than working around limitations.

The email notes this is specifically an Asciidoctor compatibility issue and provides a reference to Asciidoctor documentation about open block nesting limitations. The solution appears well-researched and addresses the original formatting problem while keeping the documentation's semantic structure intact.