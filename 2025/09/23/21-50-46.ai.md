This is a discussion about the proposed `git refs get` subcommand, focusing on interface design and discoverability concerns. Junio C Hamano questions whether creating a new subcommand is the right approach, suggesting instead that extending `git show-ref` with new options might be preferable. He notes that "difficult to discover" isn't a strong argument since the new command would be equally hard to find initially, and points out that `show-ref` was originally created to serve this same purpose.

The key technical consideration raised is whether to add a `--no-deref` option to `show-ref` rather than introducing a separate command. This directly challenges the design decision to create a new subcommand family, suggesting the functionality might better belong with the existing reference-display tools rather than as part of a new plumbing interface. The email implies that interface fragmentation should be minimized where possible, and that established commands with similar purposes should be extended when their behavior needs modification.

This response continues the thread's exploration of whether strict reference reading should be its own command or an option to existing tools, with Junio leaning toward the latter approach. The discussion now has two clear positions: the original proposal's strict separation of concerns versus the maintainer's preference for consolidating related functionality under existing commands.