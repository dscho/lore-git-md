Feature patch (2/4 in series) implementing object counting for the new `git repo stats` command. Justin Tobler responds to Patrick Steinhardt's review feedback on the object counting functionality, accepting all substantive suggestions while maintaining the core architecture.

Key technical changes in this iteration:
- Renames `struct stats` to `repo_stats` and splits into `ref_stats`/`object_stats` substructs to avoid naming collisions
- Documents object counting in bullet-point format per review suggestion
- Clarifies output as "Reachable objects" rather than just "Objects"
- Adds defensive `BUG()` for unexpected object types in traversal
- Plans to convert local struct copies to pointers for minor optimization
- Considers adding progress meter for potentially slow object traversal

The patch continues the series' pattern of clean separation between data collection (using `walk_objects_by_path()` with callback) and presentation (table formatting). Object counting integrates with the existing reference counting via shared `rev_info` setup, maintaining the command's unified view of repository state.

Review feedback has shifted from architectural concerns to polish items like naming consistency, documentation structure, and user experience touches. Justin's responses indicate the series is progressing toward merge readiness, with this patch addressing all technical feedback while deferring some UX improvements (progress meter) to future iterations. The thread shows productive collaboration with Patrick Steinhardt's reviews focusing on production-quality details.