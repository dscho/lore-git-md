This is part of the completed packfile store refactoring series that centralizes packfile management in `struct packfile_store`. Patch 14/15 refactors `get_all_packs()` to work directly on the packfile store rather than a repository, clarifying its scope as part of the ODB abstraction effort.

The change is mechanical but widespread, updating 18 files to use the new `packfile_store_get_all_packs()` function instead of the repository-based `get_all_packs()`. Each caller now explicitly accesses the packfile store through the repository's object store. The patch maintains all existing behavior while making the packfile store abstraction boundaries clearer.

This follows the architectural pattern established earlier in the series where packfile operations are moved to `packfile_store_*` functions while ODB operations remain in `odb_*` functions. The changes are straightforward and consistent with the series' goal of separating packfile management from object database responsibilities.

Since this is part of a now-merged series that has already undergone extensive review, the patch appears to be a final clean-up of the remaining `get_all_packs()` call sites to complete the architectural transition.