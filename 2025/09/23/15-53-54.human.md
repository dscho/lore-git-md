This is a follow-up discussion about part 1 of a two-part series fixing inconsistent Git notes display in `git format-patch` when used with range-diff. The patch adds a `rdiff_other_arg` field to `rev_info` to properly propagate notes arguments to the range-diff subprocess.

Kristoffer Haugsbakk responds to Junio C Hamano's review feedback, clarifying the intended behavior where notes namespaces should match between the main commit message and range-diff output. The author acknowledges two key issues raised in review: the need to initialize the new strvec member in `REV_INFO_INIT` (which they've now addressed), and the potentially too-generic name `rdiff_other_arg`. 

The discussion explores possible better names like `log_arg` and notes that this field is also used to pass `--merges` arguments. The exchange reveals some uncertainty about how generic this argument-passing mechanism should be, with Junio questioning whether the range-diff machinery should understand the arguments it forwards. The author suggests the current implementation is safe since arguments are constant strings.

This appears to be a thoughtful response to technical review feedback, with the author making one concrete change (initialization) while still considering the naming question. The discussion helps clarify the patch's purpose and implementation choices, suggesting the series is progressing toward a v2 that will address the remaining concerns.