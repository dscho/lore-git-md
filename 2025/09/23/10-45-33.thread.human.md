Here's the updated human-readable summary:

The thread discusses a proposal to add a new `git refs get` plumbing command for reading references without DWIM expansion, providing more predictable behavior than `git rev-parse`. The initial proposal emphasizes strict refname requirements and shows symref targets directly rather than dereferencing them. Ben Knoble raised concerns about whether removing DWIM behavior might make scripting harder, suggesting a potential `--dwim` flag as a compromise.

Junio C Hamano has now weighed in, questioning whether creating a new subcommand is necessary. He suggests that extending `git show-ref` with a `--no-deref` option might be preferable, noting that discoverability concerns apply equally to new commands and that `show-ref` was designed for similar purposes. This introduces a key design debate: whether to keep the functionality as a separate command (emphasizing strict separation of concerns) or to extend existing tools (minimizing interface fragmentation).

The discussion now centers on these two approaches, with the original proposal's authors needing to address whether their use case justifies a new command or could be served by enhancing `show-ref`. Junio's involvement signals that this is becoming an important design decision for the project.