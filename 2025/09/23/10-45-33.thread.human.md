The thread continues to debate the introduction of a new `git refs get` subcommand as part of a broader effort to consolidate reference-related operations under a unified `git-refs` command family. Originally proposed during GSoC, the command aims to provide predictable ref reading without DWIM expansion, positioned as an alternative to `git rev-parse` and `git show-ref --verify`.

Junio Hamano remains the primary skeptic, now joined by Ben Knoble in questioning the architectural approach. Junio argues that creating new command families doesn't inherently improve discoverability - users would still need to learn all subcommands - and suggests enhancing existing tools like `git show-ref` would be more practical. He challenges the premise that consolidation under `git-refs` reduces cognitive load, pointing out that documentation improvements could achieve similar benefits without the overhead of new infrastructure.

Patrick Steinhardt defends the consolidation vision, maintaining that a unified `git-refs` entry point with tab completion and centralized documentation offers better long-term organization than the current fragmented state across `show-ref`, `symbolic-ref`, and `update-ref`. Ben Knoble adds nuance by questioning whether these mechanisms actually serve scripting users effectively, sharing personal experience about blurring porcelain/plumbing boundaries in real workflows.

The discussion has evolved from implementation details to a fundamental architectural debate about command organization in Git's plumbing layer. While the technical implementation of `git refs get` appears sound (with Patrick providing detailed code review), the core disagreement remains unresolved: whether this functionality belongs in a new command family or should be added to existing tools. Junio-Patrick exchange represents a classic tension in Git's evolution between incremental improvement of established interfaces versus more structural reorganization.