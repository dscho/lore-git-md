This is part of Patrick Steinhardt's ongoing refactoring series to centralize packfile management in `struct packfile_store`. Patch 6/15 moves the "kept pack" cache from `struct object_database` to its new home in `packfile_store`, completing the migration of all packfile-related state.

The change is mechanical but meaningful - it relocates the cache used by `git-pack-objects` to handle cruft objects, updating all access sites to use the new location. The patch also cleans up the now-redundant comment in `odb.h` since the `packfiles` pointer is the last remaining packfile-related field there.

This appears to be a well-executed piece of the larger architectural effort, with thorough updates to all affected code paths. The commit message clearly explains what's being moved and why, though it doesn't highlight any particular challenges in the migration. As with previous patches in the series, this maintains existing behavior while moving state to its more logical home.