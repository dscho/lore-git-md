Junio Hamano responds to Meet Soni's proposal for a new `git refs get` subcommand with skepticism about creating a new command rather than extending `git show-ref`. He questions the discoverability argument, noting that `refs get` would be even less discoverable initially than `show-ref`. Hamano suggests that extending `show-ref` with a `--no-deref` option might be a better approach, as `show-ref` was originally designed for this type of reference inspection.

This moves the discussion toward interface design philosophy - whether to create new single-purpose commands or extend existing ones with new options. Hamano's response indicates he leans toward the latter approach, though he phrases it as a question rather than a directive. The exchange highlights an important design decision point in the thread about command proliferation versus option complexity.