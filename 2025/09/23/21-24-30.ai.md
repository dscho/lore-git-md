Refactoring patch concluding the xdiff modernization series by changing the `changed` field type from `char` to `bool`. This is the final patch (13/13) in v5 of the series that has systematically cleaned up xdiff internals for Rust compatibility while maintaining identical behavior.

The change converts the `changed` field (previously named `rchg`) in `xdfile_t` from `char` to `bool` since it only ever held 0/1 values. This follows earlier conversions of similar fields (`dis1`/`dis2` became `matches1`/`matches2` with enum-like `NONE`/`SOME`/`TOO_MANY` values) and completes the type safety improvements. The patch updates all assignments across xdiff's diff algorithms (xdiffi, histogram, patience) to use `true`/`false` instead of `1`/`0`, and comments are updated to reflect the boolean nature. 

This maintains the series' pattern of semantic-preserving changes while improving code clarity - the boolean type makes it immediately obvious that `changed` represents a binary state, distinct from the `matches` arrays' three-state values. The change touches the same core xdiff components as prior patches (xprepare.c, xdiffi.c, xhistogram.c, xpatience.c, xtypes.h) and follows the established approach of mechanical, behavior-preserving transformations.

The series is now complete with all planned structural simplifications, naming improvements, and type safety enhancements accomplished. This final patch represents a straightforward cleanup that logically follows from the earlier work, with no outstanding questions or concerns noted in the thread.