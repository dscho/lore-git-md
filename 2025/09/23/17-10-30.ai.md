This is a follow-up in the ongoing discussion about `git rebase -i`'s `fixup -C` behavior regarding author metadata consistency. Ben Knoble chimes in with a brief but important technical note, pointing out that while they can't speak to the workflow motivations being debated, the patch as submitted would require corresponding updates to manual pages that weren't included in the changeset (which currently modifies `rebase-interactive.c`, `sequencer.c`, and test files).

The comment doesn't take a position on the substantive debate between Mathias Rav and Phillip Wood about whether `fixup -C` should preserve the current commit's author info (Mathias's position) or the original commit's (Phillip's workflow preference). Instead, it surfaces a straightforward documentation gap that would need addressing regardless of how the behavioral question is settled. This keeps the thread moving forward by identifying a concrete omission while leaving the larger design discussion for others to continue.