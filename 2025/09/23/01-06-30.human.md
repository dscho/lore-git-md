This is the final resolution in the xdiff refactoring series, where Junio C Hamano confirms the technical details about the `changed` array (formerly `rchg`) type conversion. The discussion clarifies that while the code deals with three states (NO/YES/MAYBE) in some places, the `changed` array itself only ever stores binary 0/1 values, making `bool` the correct type choice. 

Ezekiel Newren had previously explained this distinction, and Junio now accepts this after careful code review. The key insight is that the MAYBE state is handled through a temporary variable (`current_state` in Junio's example) rather than being written to the `changed` array. This maintains the array's purely boolean nature while allowing the algorithm to handle the three-state logic where needed.

The exchange demonstrates good technical due diligence - Junio initially suggested `unsigned char` for safety but defers to Ezekiel's deeper analysis of the actual usage patterns. This concludes the major decisions in the xdiff refactoring effort, which has systematically modernized the codebase while preserving behavior. The series is now ready for merging with only minor commit message clarifications remaining.