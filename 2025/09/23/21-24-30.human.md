Final type-safety refinement in xdiff modernization series. This is the concluding patch in Ezekiel Newren's multi-version effort to clean up xdiff internals for better Rust compatibility. The change converts the `changed` field in `xdfile_t` from `char` to `bool`, since it only ever holds 0/1 values. This follows earlier patches that introduced enum-like macros for other fields and renamed variables for clarity.

The patch is mechanical but meaningful - it makes the code's intent clearer by using proper boolean types rather than numeric values. All changes are straightforward replacements of `0`/`1` with `false`/`true` across xdiff's various algorithms (myers, histogram, patience). The diff shows the systematic nature of the change, touching multiple files but only for this single purpose.

This appears to be the final piece of the xdiff refactoring puzzle, following previous work that eliminated redundant structures, improved type safety, and modernized the codebase. The series has been through multiple rounds of review and testing, with no outstanding issues. The changes maintain identical behavior while making the code more maintainable and better positioned for potential future Rust integration.