This is a documentation formatting fix addressing an Asciidoctor rendering issue in config/extensions.adoc. Jean-Noël Avila responds to Kristoffer Haugsbakk's report about incorrect "+" character display in HTML output when bullet lists appear within definition items.

The patch proposes changing the syntax from single-colon (`::`) to triple-colon (`:::`) for definition items, which appears to resolve the rendering issue in Asciidoctor while maintaining the intended structure. The change affects multiple sections in the file, including `worktreeConfig`, `preciousObjects`, and other extension definitions.

Avila identifies this as an Asciidoctor-specific bug (not present in asciidoc.py) and notes that the original workaround using `--` blocks was unnecessary. The solution maintains all existing content while fixing the visual presentation. This is a focused, technical documentation fix that doesn't affect the actual meaning or functionality described in the docs.