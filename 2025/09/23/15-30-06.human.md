This is a review comment on patch 2/4 of the `git repo stats` series, which adds object counting functionality to the new repository statistics command. Karthik Nayak notes that the patch uses nested structs (a good practice he had suggested earlier) but points out that much of the diff size comes from restructuring that could have been done in the previous patch to keep this one focused on the object counting logic itself.

The comment is constructive and technical, suggesting a way to better organize the changes across patches in the series. It doesn't raise any blocking concerns about the functionality itself, which appears to be working well with the nested `ref_stats` and `object_stats` structure now in place. This is the kind of incremental refinement that typically happens in later review rounds of a well-constructed patch series.