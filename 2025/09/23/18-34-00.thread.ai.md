Thread summary:

Documentation formatting issue in config/extensions.adoc reported by Kristoffer Haugsbakk, where `worktreeConfig` section's HTML rendering incorrectly displayed "+" characters when bullet lists were followed by continuation paragraphs within definition items. Jean-Noël Avila diagnosed this as an Asciidoctor-specific rendering issue (not affecting asciidoc.py) and proposed a solution by restructuring the definition list syntax. The fix involves:

1. Removing unnecessary open blocks (`--`) previously used as workarounds
2. Using triple colons (`:::` instead of `::`) for definition items containing complex content
3. Properly nesting bullet lists within explicit `--` blocks when needed

The patch demonstrates this approach by modifying multiple sections including `worktreeConfig` and `preciousObjects`, maintaining all content while fixing the visual rendering. The solution leverages proper Asciidoc syntax and references Asciidoctor documentation about open block nesting. The changes address the original formatting problem while preserving semantic structure, with no impact on functionality or accuracy. This appears to be a complete resolution to the reported issue.