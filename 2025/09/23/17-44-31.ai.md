Documentation patch refining the "where to push" section of `git-push` man page, part 3 of 4-part v3 series. This follows up on earlier feedback to improve clarity in the documentation flow.

The patch makes two key changes to the "where to push" explanation in `git-push.adoc`: First, it rephrases the description from "where to push" to "which repository to push to", which more precisely captures what the section is explaining. Second, it restructures the logic flow to present the repository selection process in a positive "try X, then Y, then Z" sequence rather than the previous negative phrasing ("if X is not specified...").

The changes maintain all the technical accuracy from previous versions while improving pedagogical flow. The patch continues the series' pattern of incremental refinements to make the documentation more accessible, building on the already-merged v2 series that established the core improvements. This particular change addresses feedback about making the documentation more direct and action-oriented in its explanations.

The diff shows straightforward textual changes to the introductory section, modifying only the repository selection explanation while leaving the subsequent refspec discussion unchanged. The patch maintains consistency with the series' broader goal of clarifying push behavior and upstream relationships, including the comprehensive "UPSTREAM BRANCHES" section added earlier in the series.