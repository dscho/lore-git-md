Here is the updated dense AI thread summary combining both messages:

A new plumbing command `git refs get` is being introduced as part of the `git-refs` subcommand family, developed during GSoC with two mentors. The command provides predictable reference reading without DWIM expansion (unlike `git rev-parse`) and clearer discoverability than `git show-ref --verify`. Key characteristics: requires exact refnames, shows direct symref targets without dereferencing (e.g. "ref: refs/heads/main" for HEAD), returns raw OIDs for regular refs. Implementation adds `cmd_refs_get()` in `builtin/refs.c`, documents the subcommand in `Documentation/git-refs.adoc`, and includes comprehensive tests in new file `t/t1464-refs-get.sh`. Tests cover basic ref reading, symref handling (including dangling), error cases, and explicitly verify no DWIM support.

The design choice to show symref targets rather than dereference them is intentional and highlighted as a feature. Ben Knoble questions whether removing DWIM expansion is actually beneficial for scripting use cases, noting `rev-parse`'s DWIM behavior can simplify handling user input in scripts. He suggests having two separate commands might recreate interface fragmentation and proposes a potential `--dwim` flag as a future option to provide flexibility. This raises important questions about strict refname requirements versus DWIM convenience, potentially revisiting earlier design decisions. The thread now contains both the original design justification and new considerations about whether to maintain strict purity or add optional DWIM behavior through a flag.