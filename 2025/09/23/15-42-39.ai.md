This is a bugfix patch in the reftable fsck validation series, specifically addressing trailing newline validation in 'tables.list' files. The patch implements feedback from Junio Hamano by moving this validation from fsck to runtime checks while maintaining proper error reporting.

Key technical elements:
- Modifies `parse_names()` in reftable/basics.h to return errors via an `err` parameter rather than silently failing
- Updates callers like `fd_read_lines()` to handle the new error reporting
- Adds explicit unit tests for missing newline cases that should return `REFTABLE_FORMAT_ERROR`
- Maintains existing behavior of dropping empty strings while adding proper error checking
- Includes a minor whitespace fix (extra space after `==` in tests) noted by Junio

The changes demonstrate responsiveness to prior review feedback while maintaining the reftable library's error handling patterns. The validation now occurs at runtime rather than during fsck, aligning with the series' architectural goal of keeping format validation within the reftable layer. Test coverage is thorough, verifying both the error cases and that valid inputs continue to work as before.

This patch represents a focused improvement to the reftable stack's robustness, catching malformed 'tables.list' files early with proper error reporting while keeping the changes minimal and well-contained. The implementation appears clean and the test coverage is comprehensive for the specific issue being addressed.