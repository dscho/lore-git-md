This is part of the packfile store refactoring series, specifically patch 14/15 in v6. The patch refactors `get_all_packs()` to operate directly on a packfile store rather than a repository, completing the transition to the new packfile management architecture.

The change updates all callers of `get_all_packs()` to first access the repository's packfile store and then call the new `packfile_store_get_all_packs()` function. This affects 18 files across various parts of the codebase including builtin commands (cat-file, count-objects, fsck, gc, pack-objects), helper utilities, and core packfile operations. The patch maintains all existing behavior while making the packfile store dependency explicit.

Key technical details:
- Introduces new `packfile_store_get_all_packs()` function in packfile.h
- Updates all call sites to first get packfile store from repository->objects->packfiles
- Preserves all existing functionality including MIDX handling
- Follows the architectural pattern established earlier in the series
- Completes the transition from repository-centric to packfile-store-centric packfile access

This is a mechanical final step in the packfile store refactoring that makes the architectural boundaries clear. The change is straightforward with no behavior modifications, just interface updates to match the new design. All prior discussion about the architectural approach was settled earlier in the series, so this patch represents the final implementation step.