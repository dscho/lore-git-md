This thread began as a question about Git's numbering conventions, specifically whether date formatting in `git log` output should use 0-based indexing (as suggested in the original report). Multiple contributors, including Ben Knoble, Jeff King, and Nico Williams, quickly confirmed that Git follows standard 1-based indexing for dates, with strftime formats like `%Y-%m-%d` correctly representing calendar conventions. The discussion then expanded to cover merge parent numbering in revision syntax, where initial references to tilde notation (`HEAD~2`) were corrected by Ben Knoble to use caret notation (`HEAD^1`) for parent references.

Junio C Hamano, as maintainer, provided the definitive confirmation that Git consistently uses 1-based indexing across both domains, with stash numbering being the only exception. Patrick Steinhardt later reinforced this by clarifying that caret notation applies generically to parent references, not just octopus merges. The thread concluded with consensus that no code changes were needed, as the current behavior aligns with standards and existing configuration options (like `--date=format:`) already provide customization flexibility.

Junio's latest email serves as a final technical correction, acknowledging Ben Knoble's earlier point about proper caret notation usage. While adding no new substantive conclusions, it ensures the thread's record is precise for future reference. The exchange now stands as a comprehensive resource on Git's 1-based conventions for both date handling and revision syntax.