This is a refactoring patch in the final series modernizing xdiff internals for Rust compatibility. It's part of Ezekiel Newren's v5 series (patch 10/13) that concludes the xdiff cleanup work.

The patch removes temporary variable aliases (`rchg1` and `rchg2`) in `xdl_build_script()` and replaces them with direct struct member access to `xe->xdf1.rchg` and `xe->xdf2.rchg`. This is a straightforward cleanup that follows the pattern of earlier patches in the series, eliminating unnecessary indirection and making the code more direct and readable.

The change is mechanical and doesn't affect behavior - it's part of the broader effort to simplify xdiff's internal structures and memory management. The series has already seen significant restructuring in previous patches, with this being one of the final cleanups before completion.

This patch, like the rest of the series, maintains the original algorithm's behavior while making the code more maintainable. The v5 series has addressed all previous feedback and is now ready for merging.