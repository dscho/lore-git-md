Refactoring patch in the xdiff modernization series. This is part 3 of 13 in the v5 series that concludes the xdiff internals cleanup for Rust compatibility.

The patch removes three unused fields from xdiff data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. These were initialized but never actually used in the code. The changes are straightforward deletions with no behavior impact, simplifying the code and data structures. The removal affects xdiff's preparation logic, where the unused hash-related fields are cleaned up from both the struct definitions and the initialization/classification code paths.

This continues the series' theme of eliminating cruft and modernizing the xdiff implementation while maintaining identical behavior. The patch is well-contained and follows the established pattern of the series - each change is small, focused, and clearly motivated. With the preparatory cleanups already merged and this being part of the final refinements, the series appears ready for integration.