Refactoring patch in the xdiff modernization series. This is part of Ezekiel Newren's v5 series to clean up xdiff internals for better maintainability and potential Rust compatibility. The series has already seen its preparatory patches merged to 'next'.

This specific patch simplifies memory management in `xdl_prepare_ctx()` by eliminating redundant local variables that were shadowing the `xdfile_t` struct members. Instead of manually managing separate variables and then copying them into the struct, the code now initializes and frees the struct members directly. The cleanup also consolidates error handling by using the existing `xdl_free_ctx()` function rather than duplicating its logic.

The changes are mechanical and focused on code organization - no behavior changes are introduced. The diff shows a net reduction of 48 lines through removal of variable declarations and duplicate cleanup code. This continues the series' theme of making the xdiff codebase more straightforward while maintaining identical runtime behavior.

As with the rest of the v5 series, this appears well-considered and builds on prior reviewed work. The patch follows the established pattern of the modernization effort, and since the earlier patches are already in 'next', this refinement seems likely to proceed smoothly.