This is a refactoring patch in a completed series that transitions the string-list API to use proper unsigned types for indices, specifically changing `string_list_find_insert_index()`'s return type from `int` to `size_t`. The patch is part of a v2 series that has already addressed all technical feedback from reviewers.

Karthik Nayak provides a final review of this patch, confirming the correctness of the `size_t` conversion and particularly noting the careful handling of the loop condition in `mailmap.c` to avoid underflow issues. He points out that the change from `0 <= --i` to `i--` properly maintains the loop's exit condition when `i` reaches 0, which is important since `size_t` cannot be negative.

The review also includes a super-nit suggestion to add documentation for `string_list_find_insert_index()` while making these changes, though this is marked as optional polish rather than a blocking issue. Karthik explicitly states the patch looks good to him, indicating this is final approval-level feedback rather than requests for changes.

This patch completes the mechanical conversion of the string-list API to use `size_t` consistently, removing the need for sign-comparison warning suppression in `mailmap.c`. The changes are straightforward type updates with no behavior modifications, affecting callers in `add-interactive.c`, `mailmap.c`, and `refs.c`. The thread context shows this work has already gone through significant review in v1 and v2, with Patrick Steinhardt and Karthik Nayak both providing detailed feedback that has now been addressed.