This is part of Patrick Steinhardt's packfile store refactoring series (v6 10/15), continuing the architectural separation of packfile management from object database responsibilities. The patch renames `install_packed_git()` to `packfile_store_add_pack()` and updates its signature to operate directly on a `packfile_store` rather than a repository, clarifying its scope in the new architecture.

The changes touch 7 files with mechanical updates to call sites in fast-import, index-pack, http, and midx handling code. The core functionality remains identical - adding a packfile to the store's linked list and hashmap - but now operates through the packfile store abstraction. The patch includes a new header declaration for `packfile_store_add_pack()` while removing the old `install_packed_git()` declaration.

This is a straightforward refactoring that follows the series' established pattern of migrating packfile operations to the packfile store interface. The diff shows careful attention to maintaining all existing behavior while updating callers to use the new API. As with previous patches in the series, this change helps solidify the architectural boundary between packfile management and object database operations.