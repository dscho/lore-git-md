This is the final patch in a bugfix series addressing color handling in Git's interactive patch mode. It adds documentation for the `interactive.diffFilter` configuration option in the diff-highlight README, completing the series that fixed color-related regressions in several interactive commands.

The patch documents how to configure diff-highlight to work with interactive patch modes like `git add -p` and `git checkout -p`. While this feature existed in Git, it was previously only documented on the Git side. The change helps users who discover diff-highlight first understand they can also use it with interactive commands.

This is a straightforward documentation update that was suggested by Isaac Oscar Gariano during the thread discussion. It complements the technical fixes in the earlier patches by ensuring users can properly configure color highlighting across all Git interfaces. The patch maintains the same style as the existing README and adds the new section in a logical location.