Refactoring patch in the command deprecation series, focusing on code symmetry.

This is patch 2/8 in v3 of the series extending Git's command deprecation system with alias shadowing support. The change is purely structural, making two adjacent loops in `git.c` more symmetric in their conditional logic. The original had one loop with a positive condition and another with a negative continue, while the modified version uses consistent positive conditions.

The change affects the `list_builtins()` function which is central to the series' new `--list-cmds=deprecated` functionality. The refactor doesn't alter behavior but improves code readability by making the include/exclude option handling more parallel in structure. The commit message notes this is a preparatory change to avoid mixing style cleanups with functional changes in later patches.

This appears to be a straightforward quality-of-life improvement that maintains the series' focus on command listing infrastructure. The change is small (5 lines removed, 3 added) and well-contained to `git.c`, with no impact on tests or documentation. As part of the larger deprecation framework work, it helps keep the codebase clean while adding new functionality.