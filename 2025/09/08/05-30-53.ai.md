This is the final patch in an 8-part series extending the promisor-remote protocol to support configurable validation of remote attributes. The series is now at v8 with all technical implementation complete, pending only a final terminology decision.

The patch converts the remaining `strbuf_split_str()` call in `mark_remotes_as_accepted()` to use `string_list_split()` for consistency with earlier conversions in the series. The change simplifies the code by:
1. Eliminating the need for manual string buffer management
2. Removing an unnecessary `strbuf_strip_suffix()` call
3. Using the more lightweight `string_list` API which better matches the function's needs

The modification maintains all existing functionality while following the series' broader effort to modernize string handling in the promisor-remote code. The patch demonstrates good attention to detail by cleaning up a redundant operation while making the change.

This completes the technical implementation work for the series, with all CI tests passing and documentation finalized. The only remaining discussion point is the terminology choice between "field" vs "configuration variable" for the new config options. The patch itself is a straightforward mechanical conversion that follows established patterns from earlier in the series.