This thread has finalized the design and naming for atomic reference updates in `git replay`, transitioning the series from RFC to merge-ready state. The implementation makes atomic ref updates the default behavior while maintaining backward compatibility through configurable output modes (`--ref-action=update|print` and matching `replay.refAction` config).

Junio Hamano has now weighed in on the last open question - the configuration naming - expressing clear preference for `replay.refAction`/`--ref-action` (Option 1) over alternatives. This confirms Christian Couder's earlier position and resolves the final naming decision. The implementation uses the ref transaction API (`ref_store_transaction_begin()` etc.) with comprehensive test coverage, and has addressed all technical feedback from reviewers including Patrick Steinhardt's performance concerns and Phillip Wood's atomicity verification.

With all design questions settled and maintainer approval of the naming scheme, this series is now complete and expected to be merged soon. The changes deliver a well-tested atomic ref update feature with clear documentation of the new defaults and configuration options.