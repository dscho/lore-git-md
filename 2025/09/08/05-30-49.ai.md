Code hygiene patch in the promisor-remote protocol series, introducing string constants for the mandatory 'name' and 'url' fields to match the approach already taken for optional fields like 'partialCloneFilter' and 'token'. This continues the series' pattern of replacing string literals with named constants for better maintainability and consistency.

The patch adds `promisor_field_name` and `promisor_field_url` constants alongside the existing field definitions, then updates all references to these fields in the code. It introduces a new helper function `skip_field_name_prefix()` to cleanly handle parsing of these fields from protocol advertisements, replacing direct `skip_prefix()` calls. The helper follows the same pattern used elsewhere in the series for field validation.

This change is part of the broader effort to standardize string handling in the promisor-remote protocol implementation, building on previous patches that introduced `struct promisor_info` and field validation. The implementation maintains all existing functionality while making the code more robust against typos and easier to modify. Test coverage remains unchanged as this is a pure refactoring with no behavior modification.