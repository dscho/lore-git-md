This is a bugfix patch for the `git subtree` contrib command, addressing the v2.44.0 regression in squashed subtree merges under prefixes during split operations. The patch is now in its final v2 form with all technical feedback incorporated.

Phillip Wood provides a thorough review of Colin Stagner's v2 patch, confirming the core fix is sound while suggesting minor test improvements. The key technical change rewrites `should_ignore_subtree_split_commit()` to use a single `git show` with trailer parsing instead of multiple `git log` calls, maintaining the logic for ignoring commits from other subtrees while fixing the squashed merge handling.

Review suggestions focus on test script polish: using `git switch --orphan` instead of checkout/rm, `@{-1}` syntax for branch references, removing unnecessary semicolons, and using test helper functions. Phillip also notes the test could be made branch-name-agnostic by either setting `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME` or enhancing the test repo creation helper.

The patch appears ready for merging with these minor test improvements. All substantive technical concerns from earlier discussion have been addressed, including the trailer parsing approach and nested subtree handling. The expanded test coverage now explicitly verifies both squashed and regular merge scenarios for nested subtrees under split prefixes.