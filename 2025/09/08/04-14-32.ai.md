This is a response in the ongoing discussion about Git's HTTP protocol limitations. Junio Hamano weighs in against Jeff King's suggestion that the dumb HTTP protocol could potentially implement directory scraping like httpdirfs does to discover refs without requiring pre-generated `info/refs`. 

Hamano makes a strong architectural point that such scraping behavior would fundamentally change the nature of "dumb HTTP" protocol, implying it would cross a line into more complex protocol behavior. This reinforces the thread's emerging consensus that the current limitations are intentional design constraints rather than missing features. The response suggests maintainers are unlikely to entertain changes that would blur the protocol's simple, well-defined boundaries, even if it means keeping the current `.git` suffix requirement and `update-server-info` dependency.

The email is brief but significant as it represents the Git maintainer's position on potential protocol changes, effectively ruling out one possible direction for addressing the original issue while keeping the discussion focused on the protocol's current constraints rather than speculative enhancements.