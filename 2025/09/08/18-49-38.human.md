Performance optimization patch (part 3/4) in the xdiff series, focusing on reducing function call overhead for hash calculations. Alexander Monakov moves the hashing functions from `xdiff/xutils.{c,h}` to a new `xhash.h` header and makes `xdl_hash_record_verbatim` an inline function. This change builds on part 1's hash function split (which showed 8% speedup) by eliminating call overhead for the common case of verbatim hashing (no whitespace handling).

The patch is a straightforward code reorganization that creates a new header for hash-related functions while maintaining all existing behavior. The inline optimization targets short strings where call overhead would be most noticeable. As with previous parts, this focuses on xdiff's preparation phase, which processes input before diff calculation. The series remains technically sound, though this patch alone doesn't include new benchmarks - the cumulative impact will likely be clearer when the full series lands.