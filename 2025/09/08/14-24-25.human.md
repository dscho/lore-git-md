Follow-up discussion in the submodule gitdir path encoding series. Phillip Wood raises a question about backward compatibility in patch 2/9, which moves submodule gitdirs from `.git/modules/` to `.git/submodules/`.

Phillip suggests adding a configuration key like `extensions.submoduleEncoding` to prevent older Git versions from trying to read repositories with the new path structure, since they wouldn't be able to locate submodules under `.git/submodules/`. This is a forward-looking concern about version interoperability.

The patch itself is part of a larger effort to encode submodule names to avoid filesystem conflicts, particularly for case-insensitive systems and Windows reserved filenames. It maintains backward compatibility by keeping existing paths under `.git/modules/` while directing new submodules to `.git/submodules/`.

The discussion appears technical but constructive, focusing on ensuring the change doesn't break existing workflows while enabling the new encoding scheme. Phillip's question indicates careful consideration of the transition path for users. The series has already incorporated feedback from multiple reviewers and passed CI tests, suggesting it's in good shape overall.