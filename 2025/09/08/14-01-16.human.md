This is a test reorganization patch in the submodule gitdir path encoding series. Adrian Ratiu moves tests for nested gitdir handling from t7450-bad-git-dotfiles.sh to t7425-submodule-mixed-git-dir-paths.sh, as they no longer test pathological cases now that submodule paths are encoded to prevent conflicts.

The patch transfers three test cases that verify proper handling of nested submodule git directories, including parallel cloning and checkout scenarios. These tests now belong with other mixed-path verification tests since the encoding scheme makes the nesting non-problematic. The tests remain unchanged except for their new location, continuing to use the `verify_submodule_gitdir_path` helper to check encoded path correctness.

This is a straightforward cleanup that better organizes the test suite now that the path encoding work has changed the nature of these test cases from "bad behavior to detect" to "correct behavior to verify". The patch fits naturally into the larger series that's nearing completion with comprehensive test coverage.