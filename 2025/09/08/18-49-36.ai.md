Performance optimization patch refactoring xdiff's hash calculation. This is part 1 of a 4-part series (no cover letter seen yet). The change splits `xdl_hash_record()` into two variants - one handling whitespace flags (`xdl_hash_record_with_whitespace`) and one for verbatim hashing (`xdl_hash_record_verbatim`), then makes the original function a simple inline wrapper that selects between them. This allows the compiler to hoist the whitespace flag test out of the hot loop in `xdl_prepare_ctx()`.

The commit includes benchmark results showing an 8% speedup when running `git log --oneline --shortstat` over a large commit range (v2.0.0 to v2.5.0). The changes touch xdiff's utility functions in xutils.c and xutils.h, modifying the hash calculation logic while maintaining identical behavior. The patch appears well-measured and focused, with a clear performance justification and straightforward implementation. The series likely continues with related xdiff optimizations.