Refactoring patch in the command deprecation series. This is patch 2/8 in v3 of Kristoffer Haugsbakk's series extending Git's command deprecation system with alias shadowing support.

The patch makes a small but clear improvement to the code structure in `git.c`, rewriting one loop to match the style of its sibling loop. The change replaces a `continue`-based conditional with a more direct `if` check, making the two loops in `list_builtins()` more symmetric in their structure. This is purely a code style/readability change with no functional impact.

The commit message explains this is being done now to avoid needing a "while at it" justification for such small tweaks later in the series. The change is straightforward and uncontroversial, touching only 5 lines in git.c with a simple logic restructuring that maintains identical behavior.

This patch is part of the larger effort to improve Git's command deprecation framework, but stands alone as a clean-up commit that could be reviewed and applied independently of the series' main functionality changes.