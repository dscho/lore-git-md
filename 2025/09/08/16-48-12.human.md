This is a refactoring discussion about the string-list API changes. Junio C Hamano is questioning the value of making the `exact_match` parameter optional in `get_entry_index()`, as proposed in the original patch. He argues that the change adds conditional logic to the implementation without providing significant benefit to callers, since they could just pass a throwaway variable if they don't need the exact match information.

The discussion centers on whether the added complexity in the implementation (two new NULL checks) justifies the minor convenience for callers who don't need the exact match flag. This appears to be a style/design preference discussion rather than a technical issue, as both approaches would work correctly. The exchange suggests this small API change might need more justification to be accepted.