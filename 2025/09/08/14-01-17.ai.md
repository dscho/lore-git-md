This is a test patch in the submodule gitdir path encoding series, adding comprehensive test cases for the new encoding functionality. As patch 10/10 in the v2 series, it completes the test coverage for the submodule path encoding work.

The patch adds two new test scenarios to t7425-submodule-mixed-gitdir-paths.sh. First, it verifies proper encoding of various problematic submodule names including slashes, percent signs, and mixed case, checking both new-style encoded paths and legacy paths. Second, it tests the NAME_MAX validation by attempting to create a submodule with a name that would exceed filesystem name length limits after encoding.

The tests use the series' new `verify_submodule_gitdir_path` helper to check path encoding correctness. They cover both the happy path (proper encoding of special characters) and error cases (exceeding NAME_MAX). The test cases specifically verify the interaction between new encoded paths and legacy paths, ensuring backward compatibility maintained throughout the series.

This completes the test matrix for the submodule path encoding functionality, complementing the earlier tests for nested gitdirs and mixed path scenarios. The test cases are well-constructed, covering both expected encoding behavior and system limits, matching the comprehensive approach seen throughout this series.