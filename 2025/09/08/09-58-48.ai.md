This is a discussion about the removal of the `git-new-workdir` script from Git's `contrib/` directory, part of Patrick Steinhardt's systematic cleanup series. The thread context shows this removal was already implemented in Git 2.51, but Gabriel Scherer reports it breaking their workflow due to `git worktree`'s branch-checkout restriction.

Kristoffer Haugsbakk responds to Gabriel's concerns, noting that `git worktree add --force` can override the branch-checkout restriction, though they acknowledge the discoverability issues with this option. The discussion highlights a key difference between the removed script and `git worktree`: the former allowed multiple checkouts of the same branch while the latter enforces branch uniqueness by default.

Technical details include:
- The `git-new-workdir` script was removed in v3 10/11 of the series
- `git worktree` alternative exists but has different behavior
- `--force` flag can bypass branch-checkout restrictions
- UX concerns about `--force` being used for multiple purposes

This email adds to the open question about whether `git-new-workdir` should be reinstated due to legitimate workflow needs that aren't fully covered by `git worktree`. The discussion touches on implementation details (branch checking behavior) and user experience (discoverability of `--force`). The thread context shows this is one of two remaining open questions in the otherwise complete series.