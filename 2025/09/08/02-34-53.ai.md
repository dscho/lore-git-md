This is a thread closure message regarding a resolved network connectivity issue with Git clones.

The thread discussed recurring clone failures with HTTP/2 errors ("RPC failed; curl 92 HTTP/2 stream 5 was not closed cleanly") and fetch-pack disconnects when cloning large repositories from GitHub. Participants investigated potential causes including ISP issues, HTTP/2 protocol problems, and server configurations. The original reporter (Ellie) confirms the issue has been resolved on GitHub's side through server configuration changes.

While the immediate problem is resolved, Ellie notes the broader value of implementing resumable transfers in Git, suggesting this remains a desirable feature for unreliable network conditions. The discussion included various troubleshooting attempts like forcing HTTP/1.1 via `git -c http.version=HTTP/1.1 clone`, though these only produced slightly different error messages. Other participants suggested the root cause was likely firewall/proxy configurations rather than Git itself.

Key technical elements mentioned include:
- HTTP/2 vs HTTP/1.1 protocol behavior differences
- `fetch-pack` sideband packet handling
- The `http.version` config override
- GitHub-specific server behavior

The thread concludes with the issue marked as resolved server-side, though the underlying discussion about Git's network resilience remains relevant for future consideration.