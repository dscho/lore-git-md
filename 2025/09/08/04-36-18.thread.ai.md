This implementation-ready 3-patch series finalizes atomic reference updates for `git replay`, transitioning from RFC to merge-ready state. The design makes atomic ref updates the default behavior while maintaining backward compatibility through configurable output modes. Core implementation uses `ref_store_transaction_begin()` in `builtin/replay.c` with extensive test coverage in t3650/t3651.

Current patch series status:
- Patch 1: Refactoring option validation (preparatory patch)
- Patch 2: Core atomic update functionality (reviewed with minor style fixes requested by Junio Hamano)
- Patch 3: Configuration support (final patch with naming decision resolved by maintainer)

Key technical changes in v4:
- Renamed `--update-refs` to `--ref-action` for semantic clarity (Junio Hamano's suggestion)
- Aligned config name with CLI option as `replay.refAction` (Christian Couder's feedback)
- Unified config/CLI values to both use `update`/`print` (removing mental mapping needed in v3)
- Converted to type-safe enum implementation (Junio's suggestion)
- Fixed t0450 synopsis test that was failing in v3
- Improved `--advance` documentation by removing confusing cherry-pick analogy
- Final style polish on enum definition and error messages (Junio's review feedback)

Implementation details:
- Core atomic update mechanism via `ref_store_transaction_begin()`, `ref_transaction_update()`, `ref_transaction_commit()`
- New `handle_ref_update()` helper function supporting both update and print modes
- Enum-based type safety with `REF_ACTION_UPDATE`/`REF_ACTION_PRINT` (now with trailing comma per style guide)
- Test modifications to use `--ref-action=print` where command output expected
- New atomic behavior verification tests
- Batched transactions addressing performance concerns (Patrick Steinhardt)
- Atomicity matching `git update-ref --stdin` guarantees (Phillip Wood)
- Error messages aligned with Git style guide (Junio's feedback)

Final interface refinements:
- Naming consensus achieved and confirmed by maintainer: `--ref-action`/`replay.refAction` pairing
- Documentation improved for `--advance` option behavior
- Enum-based implementation replacing string comparisons
- Example commands streamlined in documentation
- Early config validation with helpful error messages
- Additional test cases covering:
  - Config-driven behavior
  - CLI override precedence
  - Invalid config handling
  - Atomic behavior verification
- Final style polish on enum definition and error messages

Implementation consensus achieved:
- Patrick Steinhardt's performance concerns addressed via batched transactions
- Phillip Wood confirmed atomicity matches `git update-ref --stdin`
- Karthik Nayak clarified technical details of existing atomicity
- Elijah Newren's interface simplification suggestions informed final design
- Kristoffer Haugsbakk's `--format` proposal explicitly acknowledged as separate
- Junio Hamano's extensibility concerns satisfied by enum design and naming preference
- Christian Couder confirmed naming preference for `--ref-action`/`replay.refAction`
- Junio Hamano's final style review feedback incorporated

Documentation highlights:
- Clear warnings about experimental status maintained
- Examples demonstrate atomic default and fallback flags
- Server-side workflow recommendations emphasized
- Manpage reorganized for clarity with new defaults
- Config section details behavior using `replay.refAction`
- Explicit note about intentional lack of hook/formatting support
- Improved explanation of `--advance` behavior

Final config implementation details:
- Added `replay.refAction` config option in Documentation/config/replay.adoc
- Implemented config parsing in builtin/replay.c with early validation
- Maintained enum-based type safety matching CLI option
- Comprehensive test coverage for config-driven behavior, CLI override precedence, and error handling
- Follows Git's standard config precedence model
- Documents default fallback to `update` mode when unset

The series has reached full technical and naming consensus with all design questions resolved. Junio Hamano's confirmation of the `--ref-action`/`replay.refAction` naming convention represents the final maintainer approval needed before merge. His additional style feedback on enum definition and error messages has been incorporated as the final polish before merging. The implementation preserves flexibility for future extension while delivering a minimal, well-tested atomic ref update feature as the initial stable version, now complete with configurable defaults and improved interface design. Comprehensive test coverage and documentation make this ready for final maintainer approval and merging.