Here is the updated dense AI thread summary for this thread:

This RFC patch series introduces atomic reference updates for `git replay` via new `--update-refs` functionality, targeting server workflows (particularly GitLab/Gitaly). The 2-patch series (implementation + docs) enables direct ref updates without piping to `git update-ref`.

Core design provides atomic transactions by default (all refs update or none) using `ref_store_transaction_begin()`, with optional partial-update modes. Implementation modifies `builtin/replay.c` with extensive test coverage in t3650/t3651. Key constraints: incompatible with existing replay flags, success cases produce no output (matching `git update-ref --stdin`).

Christian Couder provided initial documentation review, clarifying:
- Both `--update` and `--update-refs` are new flags
- GitLab's interest in atomic updates
- Need to validate flag design before implementation review

Patrick Steinhardt's code review raises implementation concerns:
1. Performance issues with individual ref updates vs batched transactions, especially for reftable backend
2. Proposal to simplify from three flags to single `--update-refs` with mode parameter
3. Code quality improvements needed:
   - Use standard `die_for_incompatible_opt*()` helpers
   - Remove redundant comments
   - Consistent error handling

Open design questions:
- Flag naming (`--update` vs `--update-refs` vs `--batch`)
- Comparison with `git rebase --update-refs`
- Atomicity semantics differences
- Failure handling behavior
- Documentation/implementation split

Current implementation details:
- Uses ref transaction API from `builtin/update-ref.c`
- Targets bare repositories
- ~600 lines of new test coverage
- Backward compatible without new options

The discussion has progressed from documentation review (Christian) to implementation concerns (Patrick), with significant design questions remaining around API shape and performance. Patrick's reftable-specific feedback suggests needed restructuring. Next steps likely involve reconciling flag design with performance requirements before resubmission.