This is a discussion about the removal of `git-new-workdir` in Patrick Steinhardt's `contrib/` cleanup series, specifically addressing workflow impacts and alternatives. The email focuses on Junio Hamano's response to Kristoffer Haugsbakk's suggestion about using detached HEAD mode as a workaround for the script's removal.

Junio confirms that detached HEAD mode is a reasonable alternative for inspecting historical states ("sightseeing") when the `git-new-workdir` script is removed, though this doesn't address all workflow needs (like actively working on multiple branches simultaneously). This continues the thread's exploration of the fundamental behavior differences between the old script (which allowed multiple same-branch checkouts) and modern worktree commands (which enforce branch uniqueness).

Key technical points mentioned include:
- Detached HEAD mode as a partial solution for inspection workflows
- The distinction between inspection ("sightseeing") and active development use cases
- Ongoing discussion about UX concerns with `git worktree add --force` and error message improvements

The exchange helps clarify the remaining open question about `git-new-workdir` removal impacts, particularly highlighting that while technical alternatives exist, they may not perfectly match all historical workflows. This follows Gabriel Scherer's earlier report about workflow disruption and Kristoffer's technical analysis of worktree behavior differences.