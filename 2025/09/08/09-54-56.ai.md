This is a design discussion about the implementation of atomic reference updates in `git replay`, part of the ongoing RFC patch series introducing `--update-refs` functionality.

Patrick Steinhardt provides detailed code review feedback on Siddharth Asthana's implementation, focusing on performance considerations and API design. The key points are:

1. **Performance concerns** about individual ref updates (via `update_ref_direct()`) versus batched transactions, particularly regarding reftable backend efficiency. Patrick suggests using batched updates with `REF_TRANSACTION_ALLOW_FAILURE` for partial updates.

2. **Flag simplification** proposal to reduce from three flags (`--update`, `--update-refs`, `--batch`) to just `--update-refs` with an optional mode parameter like `--update-refs=allow-failures`.

3. **Code quality notes** about:
   - Using existing `die_for_incompatible_opt*()` helpers for option validation
   - Removing redundant comments that merely restate code
   - Maintaining consistent error handling patterns

The discussion continues the thread's focus on atomic update semantics while introducing new implementation-specific concerns. Patrick's feedback suggests the current approach may need revision to:
- Better handle performance across ref backends
- Simplify the user-facing API
- Align with Git's existing ref transaction patterns

This moves the conversation from documentation review (Christian Couder's earlier focus) into implementation details, with Patrick bringing reftable-specific performance knowledge to bear. The feedback indicates the patch may need significant restructuring before proceeding.