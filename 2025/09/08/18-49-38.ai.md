Performance optimization series part 3/4 for xdiff's hash calculation. This patch moves the hash-related functions from xutils.{c,h} to a new xhash.h header, making xdl_hash_record_verbatim an inline function to eliminate call overhead for short strings. The change continues the series' focus on micro-optimizing xdiff's preparation phase, building on part 1's hash function splitting and part 2's branch prediction hints. The patch reorganizes code without changing behavior: xdl_hash_record_with_whitespace remains an external function (likely due to size/complexity), while the simpler xdl_hash_record_verbatim is inlined. The new xhash.h gets included by xinclude.h and xdiff-interface.c, maintaining existing dependencies. This appears to be a straightforward code organization improvement that may yield small performance gains from inlining, though the commit message doesn't provide specific benchmarks. The change is surgical (54 lines added, 22 removed) and maintains the series' pattern of focused, compatible optimizations.