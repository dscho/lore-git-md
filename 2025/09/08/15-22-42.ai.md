This is a discussion about the removal of the `git-new-workdir` script from Git's `contrib/` directory, specifically addressing workflow gap concerns raised by Gabriel Scherer. The email clarifies that while `git worktree add --force` can bypass some restrictions, it doesn't fully replace `git-new-workdir`'s functionality for Gabriel's workflow of temporarily switching branches across worktrees.

Key technical points:
- Gabriel confirms `--force` doesn't solve their core issue - they need to temporarily checkout already-checked-out branches within worktrees
- They note `git checkout -d` (detached HEAD) is a possible workaround but has UX downsides (bash prompt behavior, workflow disruption)
- Suggests improving error messages to recommend detached HEAD when branch checkout fails due to worktree constraints

The discussion reveals a deeper behavioral difference between the old script and modern worktrees: `git-new-workdir` allowed multiple same-branch checkouts while worktrees enforce branch uniqueness. This surfaces UX concerns about `--force` serving multiple purposes and discoverability of the detached HEAD workaround.

The email advances the thread by:
1. Providing concrete evidence that the removal impacts real workflows despite the `--force` option
2. Identifying a remaining gap in worktree functionality for temporary branch inspection
3. Proposing specific UX improvements to mitigate the transition

This keeps open the question of whether `git-new-workdir` should be retained given these workflow needs, or if the solution lies in improving worktree`/`checkout` error handling and documentation.