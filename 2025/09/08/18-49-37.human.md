Performance optimization patch in xdiff series. This is the second of four patches aimed at speeding up xdiff's hash calculation. The change adds a `XDL_LIKELY` macro (using GCC's `__builtin_expect` when available) to annotate the unlikely branch in `XDL_ALLOC_GROW`, which is used in a hot loop during diff preparation. The goal is to improve code layout for better performance by helping the compiler optimize for the common case where no reallocation is needed.

The patch modifies `xdiff/xmacros.h` to introduce the new macro and apply it to the allocation check. While the commit message doesn't include benchmark numbers for this specific change, it's part of a series that previously showed an 8% speedup for certain `git log` operations. The change is straightforward and maintains the existing behavior while potentially improving performance through better branch prediction.

This appears to be a low-risk optimization that follows the pattern of the series - making targeted improvements to xdiff's hot paths. The implementation looks clean and maintains compatibility with non-GCC compilers through a fallback definition.