This is a response in the WebDAV PROPFIND feature discussion thread. Junio C Hamano pushes back against the proposal to add WebDAV support to Git's dumb HTTP protocol, arguing that the current behavior is intentional design rather than a bug needing a workaround.

The response emphasizes that dumb HTTP is meant to work with minimal server requirements, and adding WebDAV support would undermine that simplicity. Hamano characterizes the manual `update-server-info` step not as a workaround but as the expected workflow for dumb HTTP repositories. The note contrasts dumb HTTP's inefficiency with smarter protocols like SSH and smart HTTP, suggesting WebDAV support would bloat the intentionally minimal dumb HTTP implementation.

Key technical context preserved: the discussion centers on whether to modify Git's HTTP transport to handle WebDAV PROPFIND responses, with Hamano taking the position that this would violate the dumb HTTP protocol's design constraints. The response references the same server configuration and `git ls-remote` failure scenario from the original proposal but interprets them differently - not as interoperability problems but as expected behavior given the protocol's limitations.

This appears to be a fundamental disagreement about dumb HTTP's design philosophy rather than a technical implementation debate, with the maintainer asserting that WebDAV support would compromise the protocol's intentionally minimal nature. The response doesn't engage with the prior discussion's technical details about PROPFIND implementation but instead questions the premise of the feature request.