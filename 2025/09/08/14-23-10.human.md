This is a bugfix in the submodule gitdir path encoding series, addressing a potential undefined behavior issue in the pointer arithmetic when validating encoded submodule paths.

Phillip Wood points out a subtle but important issue in the pointer arithmetic used when checking path invariants in the new `submodule_name_to_gitdir()` function. The current code creates a pointer `p` that could be invalid if `buf->len` is less than `encoded_len`, which could trigger undefined behavior. He suggests reordering the checks to validate the lengths first, similar to how the now-removed `validate_submodule_git_dir()` function handled it.

This is a small but important correctness fix in the larger submodule path encoding work. The issue was caught during review of patch 8/12 which removed the old validation function in favor of BUG assertions. The fix maintains the same safety checks but does so in a way that avoids potential undefined behavior.

The change is straightforward and uncontroversial - it's clearly a bug that needs fixing, and the proposed solution follows the pattern from the original validation code. This should be a quick fix to incorporate into the series before it gets merged.