This is the foundational refactoring patch in a 7-part series extending the promisor-remote protocol. It introduces `struct promisor_info` to replace the previous parallel `strvec` arrays for storing promisor remote information (name and URL). The change provides better type safety and scalability for upcoming protocol extensions that will add more fields beyond just name and URL.

Key technical changes:
- Defines new `struct promisor_info` with explicit `name` and `url` members
- Replaces `promisor_info_vecs()` with `promisor_config_info_list()` that populates a string_list of promisor_info structs
- Adds cleanup function `promisor_info_list_clear()`
- Updates all callers to use the new structure, including `promisor_remote_info()` and `should_accept_remote()`
- Maintains existing functionality while improving code organization

The patch is well-structured with clear commit message explaining the motivation (scalability for future fields) and implementation approach. It includes documentation comments for the new struct and functions. The diff shows thorough conversion of all affected code paths with appropriate memory management.

This appears to be a solid technical foundation for the series, addressing the core data structure needs before adding new protocol capabilities in subsequent patches. The changes align with Git's code style and maintain all existing behavior while enabling future extensions.