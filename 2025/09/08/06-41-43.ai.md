This is a technical follow-up in the Rust infrastructure series, specifically addressing Makefile implementation details. Patrick Steinhardt responds to SZEDER Gábor's review of the `$(RUST_LIB)` target in the Makefile, which was incorrectly using `FORCE` instead of proper dependency declarations.

The key technical point is about Makefile best practices - SZEDER Gábor correctly identified that the Rust library target was using `FORCE` as a shortcut rather than properly declaring its dependencies. Patrick acknowledges this and agrees to change the implementation to use proper dependency tracking, though he notes the rationale for the original approach (cargo's argument handling making dependency lists potentially stale).

This exchange represents progress on one of the outstanding technical issues from the thread summary (1) the `FORCE` misuse in Makefile targets). While the response doesn't yet show the corrected implementation, Patrick's acknowledgment indicates this specific concern will be addressed in a future revision. The interaction demonstrates the ongoing refinement of the Rust infrastructure's build system integration, with reviewers catching subtle technical issues and the author responding constructively.