This is a documentation patch in a series introducing atomic reference updates to `git replay`. Kristoffer Haugsbakk points out a potential test failure in the patch series' documentation split. The first patch adds new `--update` and `--update-refs` flags to the implementation, while this second patch documents them, which may cause the `t/t0450-txt-doc-vs-help.sh` test to fail since it verifies sync between command-line help and documentation.

The note serves as a heads-up about a mechanical issue that would be caught by CI - the documentation update needs to be in sync with the implementation changes. This is a minor but important technical point about patch series organization, suggesting the documentation changes might need to be folded into the implementation patch to keep everything atomic.

The thread context shows this is part of a larger discussion about the new atomic reference update functionality for `git replay`, where design decisions around flag naming (`--update` vs `--update-refs`) and implementation approach are still being worked out. This particular email focuses narrowly on the documentation/testing aspect rather than the broader design questions.