This is a technical refinement in the command deprecation framework series, specifically addressing how deprecated commands are identified. The patch (3/8 in the series) focuses on improving the implementation of `is_deprecated_command()` to use the new `DEPRECATED` flag rather than maintaining a separate hardcoded list.

Junio C Hamano confirms Jeff King's suggested approach to check the `DEPRECATED` flag via `get_builtin()`, which provides a cleaner and more maintainable solution. This change doesn't affect user-facing behavior but improves the internal consistency of the deprecation system. The implementation now properly aligns with the series' goal of moving from ad-hoc deprecation handling to a more systematic approach.

The exchange shows consensus between two key maintainers (Junio and Jeff King) on this technical detail, marking an important step toward finalizing the series. This refinement was the last outstanding issue before the deprecation framework could be considered ready for merging.