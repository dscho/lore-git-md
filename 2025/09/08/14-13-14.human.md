This is part of the ongoing Rust infrastructure series, specifically focusing on CI testing for breaking changes. The patch repurposes the now-redundant "pedantic" CI job (which just did basic Fedora builds) into a more useful test that verifies breaking changes work with Meson builds. 

The change plugs two gaps in testing: it adds Fedora to the distros tested with breaking changes, and ensures the Meson build system works properly with those changes. The patch also sets up infrastructure for future Rust testing across both Makefile and Meson builds. 

The implementation modifies CI configuration files to replace the pedantic job with the new breaking-changes test, updates dependency installation to handle Meson requirements, and ensures tests run properly in the new configurations. This is a straightforward technical change that strengthens the test coverage for the Rust transition work.

As part of the larger series, this helps validate the infrastructure before the planned Rust adoption milestones. The patch appears well-constructed with clear motivation and appropriate test coverage for its scope.