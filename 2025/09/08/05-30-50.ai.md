This is part 4 of a 7-patch series extending the promisor-remote protocol to support configurable validation of remote attributes during clone/fetch operations. The series is now at v8 with all technical implementation complete, pending final terminology decisions.

The patch refactors the parsing logic for advertised promisor-remote fields into a new `parse_one_advertised_remote()` function that populates a `struct promisor_info`. This prepares for parsing additional fields (filter and token) in future commits while making the code more maintainable. Key changes include:

1. Introduces `promisor_info_free()` helper to centralize memory management
2. Restructures parsing to use the new `parse_one_advertised_remote()` function
3. Updates `should_accept_remote()` to take the full `struct promisor_info` rather than individual fields
4. Maintains all existing validation behavior while cleaning up the control flow

The changes follow the series' pattern of improving code hygiene while extending functionality. The patch builds on the foundational `struct promisor_info` introduced earlier in the series and aligns with the broader goal of handling additional remote attributes. Test coverage remains complete and CI is confirmed green.

Technical details relevant for future thread summaries:
- New function: `parse_one_advertised_remote()`
- Modified function signatures: `should_accept_remote()`
- Memory management: `promisor_info_free()` helper
- Field handling: maintains case sensitivity rules and URL decoding
- Validation: preserves all existing checks for mandatory name/url fields
- Follow-up work: will use same pattern for filter/token fields