This is a technical response in the promisor-remote protocol extension series, specifically addressing feedback on patch 3/5 from v7. The email confirms implementation of Junio Hamano's requested changes in the upcoming v8 series, focusing on string handling improvements in the remote attribute parsing logic.

Christian Couder acknowledges and implements several key changes from Junio's review: replacing `string_list_split_in_place()` with `string_list_split()` to avoid modifying input buffers, removing unnecessary `strbuf_strip_suffix()` calls, and converting the parsing function to take a `const char*` instead of `strbuf*`. These changes address the core concern about destructive string operations while maintaining functionality.

The response also notes that v8 completes the broader modernization effort by converting all remaining `strbuf_split*()` calls in promisor-remote.c to `string_list_split*()` variants. This aligns with the series' goal of cleaning up string handling while implementing the new remote attribute validation system. The changes demonstrate careful attention to Junio's feedback while maintaining the protocol's case sensitivity rules and validation behavior.

This email serves as a bridge between the v7 implementation feedback and the forthcoming v8 series, confirming resolution of the technical concerns raised about string manipulation safety and API usage. The changes preserve the protocol's functionality while adopting more robust string handling patterns, a key requirement for this security-sensitive component.