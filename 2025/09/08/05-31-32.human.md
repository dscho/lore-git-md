This is a follow-up discussion in the promisor-remote protocol extension series, specifically addressing parsing improvements in patch 3/5 from v7. Christian Couder confirms that Junio Hamano's feedback about string handling has been addressed in v8 of the series.

The discussion focuses on technical implementation choices for parsing remote advertisement fields, where Junio had pointed out that the v7 implementation was modifying input buffers in place. Christian confirms the v8 changes now use non-destructive string splitting via `string_list_split()` instead of `string_list_split_in_place()`, which was Junio's main concern. 

The email also notes several other improvements made in v8 based on Junio's suggestions: removing unnecessary `strbuf_strip_suffix()` calls, changing function parameters to use `const char*` where appropriate, and completing the conversion from `strbuf_split*()` to `string_list_split*()` functions throughout the promisor-remote code. 

This appears to be a productive technical discussion where the maintainer's feedback has been promptly incorporated. The changes seem straightforward and improve code safety by avoiding input modification while maintaining the same functionality. The thread is nearing completion with these parsing improvements now properly implemented.