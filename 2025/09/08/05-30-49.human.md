This is part of a series extending the promisor-remote protocol to support configurable validation of remote attributes. Patch 3/7 in v8 series focuses on code hygiene by introducing string constants for the mandatory 'name' and 'url' fields in the promisor-remote protocol, matching the approach already taken for optional fields like 'partialCloneFilter' and 'token'.

The patch introduces two new string constants (`promisor_field_name` and `promisor_field_url`) to avoid repeating string literals throughout the code. It also adds a helper function `skip_field_name_prefix()` to clean up the parsing logic for these fields. The changes maintain all existing functionality while making the code more maintainable and consistent with the rest of the series.

This appears to be a straightforward refactoring that follows the pattern established in earlier patches. The implementation looks clean, with proper attention to string handling and protocol parsing. The patch builds on the series' foundation of improving the promisor-remote protocol's robustness while keeping the changes focused and well-contained.