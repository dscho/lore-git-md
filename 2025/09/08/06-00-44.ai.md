This is a documentation patch in an ongoing thread about adding atomic reference updates to `git replay`. Christian Couder provides feedback on Siddharth Asthana's documentation patch (2/2 in the series) for the new `--update-refs` and `--batch` options.

Key points raised in the feedback:
- Questions whether documentation should be integrated with the implementation patch rather than separate
- Asks for clarification on why the specific option names (`--update`, `--update-refs`, `--batch`) were chosen over alternatives
- Requests comparison with `--update-refs` in `git rebase`
- Notes redundancy in phrasing about output behavior
- Points out missing details about atomicity behavior with `--update` vs `--update-refs`
- Asks for clearer differentiation between `--update` and `--batch` modes
- Requests confirmation whether `--update` stops at first failure like `git update-ref --stdin`

The feedback focuses on improving clarity and completeness of the documentation, particularly around:
- Option naming rationale
- Behavior differences between update modes
- Failure handling semantics
- Atomicity guarantees
- Relationship to existing commands

The thread context shows this is part of a well-structured series adding atomic ref updates to `git replay`, with thorough test coverage. The documentation patch being reviewed aims to explain the new options' behavior but needs refinement based on this feedback.