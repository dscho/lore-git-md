Documentation patch cleaning up formatting in `git-fast-import` manpage. 

Kristoffer Haugsbakk submits a one-line documentation change converting a literal block to a regular paragraph in the `git-fast-import` manpage. The change affects the documentation for the `feature export-marks` option, which was added in commit 68061e34702. The patch removes the indentation from four lines of text that were previously formatted as a literal block, making them flow as normal paragraph text. 

The change appears to be about consistent formatting rather than content - the actual warning text about trusting the program generating the fast-import stream remains unchanged. The commit message suggests this might have been an unintentional use of literal block formatting rather than a deliberate stylistic choice. 

The patch is minimal and straightforward, touching only the documentation file with simple whitespace changes. No functional changes are made to the code or to the meaning of the documentation. The change aligns with Git's documentation style where security warnings are typically presented as regular paragraphs rather than literal blocks.