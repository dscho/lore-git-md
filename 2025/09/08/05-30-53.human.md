This is the final patch in an 8-part series extending the promisor-remote protocol to support configurable validation of remote attributes. The series is now at v8 with all technical implementation complete, pending only a terminology decision.

The patch focuses on code hygiene, replacing the remaining `strbuf_split_str()` call in `string_list_split()` within `mark_remotes_as_accepted()`. This change follows the series' broader effort to modernize string handling in the promisor-remote code. The patch also removes an unnecessary `strbuf_strip_suffix()` call that was made redundant by the new string handling approach.

The change is straightforward and maintains the same functionality while using the lighter-weight `string_list` API. This continues the series' pattern of improving code quality and consistency, with all previous patches having already addressed the core protocol extension functionality and received positive feedback.

With this patch, the technical implementation of the series appears complete, leaving only the open question about whether to use "field" or "configuration variable" terminology in the new config options. The patch maintains the series' high quality with clean, focused changes and good test coverage.