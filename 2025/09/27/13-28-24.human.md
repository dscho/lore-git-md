This is a user question about git worktree behavior, specifically why worktree directories aren't automatically ignored in the main repository.

Jakub T. Jankiewicz asks why newly created worktree directories (like `base/` in their example) show up as untracked files in the main repository's `git status`, rather than being automatically ignored. They note it seems counterintuitive to have to manually add these to `.gitignore`.

The email doesn't propose any changes or patches - it's purely a question about the current behavior's rationale. The author mentions they're a long-time Git user (since 2010) but only recently discovered worktrees, suggesting this might be a common point for users new to the worktree feature.

The question comes from someone using Git 2.51.0 on Fedora, indicating they're not running the very latest version but still a modern enough one that worktree support is well-established. The tone is curious rather than critical, seeking to understand the design decision rather than challenge it.