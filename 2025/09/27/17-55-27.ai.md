This is a follow-up question in an existing thread about git-worktree behavior, challenging Junio Hamano's explanation of why worktree directories appear as untracked in the main repository.

Michal Suchánek points out an apparent contradiction in the current behavior: while a worktree's own `.git` directory is not shown as untracked, sibling worktrees (which also contain `.git` files) do appear as untracked in the main worktree's status. He frames this as an inconsistency in the design rationale provided by Junio.

The email doesn't propose any specific changes but raises a technical question about the internal logic - if worktrees are treated like any other repository (as Junio stated), why is there different handling between a worktree's own `.git` and other worktrees' `.git` files? This could potentially reopen discussion about whether the current behavior is truly consistent or merits reconsideration.

Key technical detail highlighted: the asymmetry in how `.git` files are treated between the current worktree (hidden) and other worktree directories (shown as untracked). The thread context shows this follows Junio's authoritative explanation of the design, making this a direct challenge to that reasoning.