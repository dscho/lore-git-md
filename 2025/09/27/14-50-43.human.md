This is a refactoring patch that's part of the `git repo stats` series (patch 1/7). It's a simple rename of the `repo_info()` function to `cmd_repo_info()` in builtin/repo.c to follow Git's convention of prefixing subcommand entry points with `cmd_`. 

The change is straightforward - it updates both the function declaration and its reference in the subcommand options. The patch has already been merged in previous iterations of the series, indicating it was uncontroversial. 

This is a minor preparatory change that establishes naming consistency before the more substantial work of implementing the new repository statistics functionality in subsequent patches. The 3-line diff shows the mechanical nature of the change, which doesn't affect behavior but helps maintain code organization standards.