Documentation patch fixing markdown formatting in the PO README file. This is the first patch in a v3 series primarily focused on implementing configuration value parsing in Rust, but this standalone change addresses a minor issue in the translation documentation.

The patch removes unnecessary backslash escaping of underscores in language code examples (like `ll_CC` and `zh_CN`) in `po/README.md`. The underscores don't need escaping in Markdown, and removing the backslashes makes the examples cleaner and more readable. The change is straightforward and touches three lines total, all in the same file.

While this is part of a larger series about Rust FFI implementation for configuration parsing, this particular patch is completely independent - it's a simple documentation fix that could theoretically be applied separately. The change is uncontroversial and doesn't affect functionality, just improves documentation formatting. The patch follows standard Git contribution practices with a clear commit message and proper sign-off.

For the AI summarizer context: This patch modifies `po/README.md` to remove backslash escaping from language code examples containing underscores (changing `ll\_CC` to `ll_CC` and similar). The change appears in three locations in the file where language codes are documented. The patch is cleanly separated from the rest of the series' Rust FFI work and could be considered for independent application if maintainers prefer.