This is maintainer feedback on a feature patch series implementing boolean config handling in Rust (`contrib/libgit-rs`). Junio Hamano provides guidance on how to structure the v2 submission, pointing out several process issues that need addressing before the series can move forward.

The key points from the maintainer are:
1. The documentation fix (patch 1/3) appears unrelated to the Rust work and should likely be submitted separately.
2. The series should be rebased onto the current mainline commit bb697214.
3. More importantly, the implementation feedback from v1 (to use FFI-wrapped C functions rather than Rust-native parsing) should be incorporated directly into the main implementation patch via amendment, not as a separate follow-up fix. This maintains a cleaner history showing only the final correct approach.

Hamano emphasizes that during the review phase, authors should amend patches to incorporate feedback directly rather than adding "fix-up" commits, reserving incremental fixes only for changes that have already graduated to the 'next' branch. The current v2 series still shows the original Rust-native implementation in patch 2/3 with corrections only coming in the new patch 3/3.

This is typical maintainer guidance about Git project workflow expectations. The technical content of the series (wrapping `git_configset_get_bool()` via FFI) appears sound based on prior discussion, but the presentation needs adjustment to follow project norms before proceeding further.