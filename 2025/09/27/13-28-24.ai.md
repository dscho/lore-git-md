This is a user question about git-worktree behavior, specifically asking why worktree directories aren't automatically ignored by Git.

The email comes from a developer who recently discovered git-worktree and is surprised that when creating a new worktree (in this case named "base'), the worktree directory appears as an untracked file in the main repository. They provide a concrete example showing `base/` appearing in `git status` output, and question why this isn't handled automatically by Git.

The question touches on core Git behavior rather than proposing any changes - it's seeking to understand the design rationale. The user appears to be running Git 2.51.0 on Fedora and has been using Git since 2010 but only recently started using worktrees.

Key technical points that would be relevant for future discussion:
- The specific behavior demonstrated: creating a worktree with `git worktree add base` makes `base/` appear as untracked
- No mention of `.gitignore` patterns being tried or existing
- The user's expectation that worktree directories should be automatically ignored
- Version mentioned is Git 2.51.0 (released 2023-11-20)

This appears to be a genuine question from a user encountering what they see as surprising behavior, rather than a bug report or feature proposal. The thread may involve explanations about Git's design decisions regarding worktrees and untracked files, or suggestions for workarounds like adding patterns to `.gitignore`.