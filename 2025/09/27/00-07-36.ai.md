This is a v2 feature patch series (3 parts) implementing boolean config handling in the Rust libgit implementation (`contrib/libgit-rs`), now restructured to use FFI-wrapped C functions as directed by maintainer feedback. The series builds on previous discussion where Junio Hamano confirmed the implementation must use Git's native `git_configset_get_bool()` through the FFI layer rather than Rust-native parsing.

Key changes in v2:
- Adds a new patch (3/3) addressing review feedback, though the core architectural change to use FFI isn't yet visible in the range-diff
- Maintains the documentation fix from v1 (1/3) for PO README markdown formatting
- Still shows Rust-native implementation in patch 2/3, suggesting the FFI transition may be incomplete
- Introduces new test file `config4` alongside existing `config3` tests

Technical details:
- 47 total lines changed across 5 files
- Core changes still in `contrib/libgit-rs/src/config.rs` (27 lines)
- FFI layer touched in `contrib/libgit-sys/src/lib.rs` (6 lines)
- Two test config files now used (`config3` modified, `config4` new)

Open questions from v2 submission:
- Whether patch 3/3 fully addresses the architectural requirement to use FFI
- If the documentation fix (1/3) should remain bundled or be split out
- How the test coverage maps to the new FFI-based implementation

The series appears to be in transition between the v1 approach and the maintainer-directed FFI solution, with the critical architectural change not yet clearly reflected in the visible diffs. The new test file suggests expanded validation of boolean parsing behavior.