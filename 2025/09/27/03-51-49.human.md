This is a documentation patch fixing markdown formatting in the translation (l10n) documentation. It's the first patch in a v3 series primarily focused on implementing configuration parsing in Rust, but this particular change stands alone as a simple formatting fix.

The patch removes unnecessary backslash escapes from underscores in language code examples (like `zh_CN` and `ll_CC`) in `po/README.md`. The author correctly notes these don't need escaping in Markdown. The change is straightforward and uncontroversial - it doesn't affect functionality, just makes the documentation render correctly.

While part of a larger series, this documentation fix could reasonably be applied independently. The change is small (3 lines) and clearly correct, though as noted in the thread summary, there's some question about whether it should be submitted separately from the Rust implementation work.