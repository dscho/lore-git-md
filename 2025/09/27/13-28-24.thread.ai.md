Thread identity: user question about git-worktree behavior regarding untracked worktree directories.

Core problem: why worktree directories (created via `git worktree add`) appear as untracked files in the main repository's status, and whether they should be automatically ignored. Example shows `base/` appearing in `git status` after `git worktree add base` in Git 2.51.0.

Current status: question only, no proposed changes or patches. User expectation is that worktree directories should be automatically ignored by Git.

Key technical details: worktree creation shows directory as untracked, no mention of existing `.gitignore` patterns, behavior observed on Git 2.51.0.

Open questions: design rationale for current behavior, whether this should be considered for future improvement, possible workarounds like manual `.gitignore` entries.

Participant positions: original question from user (running Git 2.51.0 on Fedora, using worktrees recently despite Git experience since 2010). No replies yet.