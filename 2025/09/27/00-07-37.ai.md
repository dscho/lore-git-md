Documentation patch fixing markdown formatting in the PO README. This is the first patch in a v2 series that primarily focuses on implementing boolean config handling in Rust, but this particular patch stands alone as a straightforward documentation fix.

The change removes unnecessary backslash escaping of underscores in language code examples within `po/README.md`. The author notes that underscores in strings like "ll_CC" and "zh_CN" don't need escaping in Markdown syntax. The diff shows three instances where backslashes were removed from these language code examples.

This patch remains unchanged from v1 of the series, where it was already correctly implemented. The thread context indicates this documentation fix may be better submitted separately from the main Rust implementation work, but the author has chosen to keep it bundled with the series for now. The change is small (3-line diff) and clearly correct, affecting only documentation formatting without any functional impact.

Key technical details:
- File modified: `po/README.md`
- Changes: 3 lines removing backslashes before underscores in language code examples
- Examples affected: "ll_CC", "zh_CN", and a general locale example line
- Maintains consistency with Markdown formatting rules where underscores in code don't need escaping

The patch appears ready to apply as-is, though the thread context suggests it might be cleaner to submit separately from the Rust implementation work. The change is uncontroversial and aligns with standard Markdown practices.