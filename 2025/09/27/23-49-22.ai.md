This is a follow-up in the `git repo stats` series regarding the clang-format change that was originally included as patch 3/7. The email confirms the resolution of an earlier discussion about separating this formatting change from the main series.

Junio Hamano agrees with Justin Tobler's plan to split the clang-format modification into its own standalone topic, and offers to handle the administrative side by queuing both topics separately. This avoids requiring Justin to resend the entire repo stats series without the formatting change. The clang-format change modifies `SpaceBeforeParens` behavior specifically for control macros (preventing unwanted spaces between macros and their parentheses), which Karthik Nayak helped identify.

This exchange demonstrates Git's practice of keeping unrelated changes in separate patch series, even when they originate from the same contributor. The resolution means the repo stats series can progress independently while the clang-format change follows its own review path. The thread context shows this formatting change was already merged as part of an earlier version of the series but will now be properly separated in future submissions.