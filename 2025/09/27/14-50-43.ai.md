This is a refactoring patch that's already been accepted (merged) as part of the `git repo stats` series. It's the first of seven patches that introduce a new repository statistics reporting command.

The patch makes a simple but important naming convention adjustment in `builtin/repo.c`, renaming the `repo_info()` function to `cmd_repo_info()` to follow Git's standard practice of prefixing subcommand entry points with `cmd_`. The change is minimal (just 3 lines modified) but establishes consistency needed for the larger series.

The thread context shows this was uncontroversial and merged early in the review process. As noted in the thread summary, this change helps establish the naming pattern that will be used for the new `cmd_repo_stats()` function introduced later in the series. The patch doesn't affect functionality - it's purely a code organization improvement that makes the codebase more consistent with Git's established conventions for command implementations.