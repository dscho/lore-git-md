This is a clarification note from Junio Hamano regarding the `git repo stats` series, specifically addressing character width handling in the table formatting.

Junio points out that while Justin Tobler correctly switched from `strlen()` to `utf8_strwidth()` in v4 to handle multi-byte characters (as suggested by Eric Sunshine), there's an important distinction between two different i18n concerns: byte length vs display width. The change properly addresses display width calculation for table formatting, which needs to account for characters that occupy multiple columns (like CJK ideographs), not just multi-byte encoding. 

Junio notes this isn't worth a resend since cover letter updates don't affect the final commit history, but wants to prevent confusion about the nature of the i18n fix. The clarification reinforces that the implementation now correctly handles both:
1. Multi-byte character encoding (preventing mid-character truncation)
2. Variable display widths (ensuring proper table column alignment)

This follows naturally from the earlier discussion where Eric Sunshine identified the i18n issue in patch 4/7's table formatting, and Justin implemented the suggested fix using `utf8_strwidth()`. Junio's note serves as both a technical clarification and a quality control checkpoint, ensuring the mailing list audience understands the precise nature of the internationalization improvements in the series.