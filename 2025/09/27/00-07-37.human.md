Documentation patch fixing markdown formatting in the PO README. This is the first patch in a 3-part series about boolean config handling in Rust libgit, though this particular patch stands alone as a straightforward docs fix.

The change removes unnecessary backslash escapes from underscores in language code examples (like `zh_CN` and `ll_CC`) in the translation documentation. The author notes these underscores don't need escaping in Markdown. The patch is small (3-line change) and clearly correct - it just makes the documentation render properly while keeping the same content.

This patch was also present in v1 of the series and remains unchanged in v2. While it's technically part of the boolean config series, it's really an independent documentation improvement that could likely be submitted separately. The change is simple and uncontroversial, though reviewers might reasonably suggest splitting it out from the more complex Rust implementation changes in the rest of the series.