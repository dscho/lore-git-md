Maintainer feedback on Rust boolean config series. Junio Hamano provides guidance on patch organization and implementation approach for the v2 series implementing boolean config handling in `contrib/libgit-rs`.

Key points:
- Questions the bundling of the PO documentation fix (patch 1/3) with the Rust implementation work, suggesting it might be better as a separate submission
- Strongly recommends rewriting the incremental "fixup" patch (3/3) into the main implementation patch (2/3) to present a cleaner history, following Git project conventions where pre-merge iterations should show the ideal final state rather than recording review process
- Confirms the architectural direction to wrap Git's native `git_configset_get_bool()` via FFI rather than reimplementing in Rust
- Notes the series needs rebasing on current mainline (bb697214) rather than its current base (d7810781fc)

The feedback focuses on project workflow norms rather than technical implementation, as the technical approach (FFI wrapping) has already been established in earlier discussion. Maintainer expects the next iteration to:
1. Possibly split out the documentation fix
2. Squash the fixup into the main implementation
3. Rebase on current mainline
4. Present as either 1-patch (Rust changes only) or 2-patch (if keeping doc fix) series

This represents the final polish stage before potential merging, with all architectural questions resolved in previous rounds. The series appears technically sound but needs formatting adjustments to meet project standards.