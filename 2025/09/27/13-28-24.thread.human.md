This thread begins with a user question about git-worktree behavior. The user, who has been using Git since 2010 but only recently started using worktrees, is surprised that when they create a new worktree (named "base"), the worktree directory appears as an untracked file in their main repository. They specifically question why Git doesn't automatically ignore worktree directories, seeing this as unexpected behavior.

The question focuses on understanding Git's design rationale rather than proposing changes. The user is running Git 2.51.0 on Fedora and provides a concrete example showing `base/` appearing in `git status` output. They haven't mentioned trying any `.gitignore` patterns yet. This appears to be a case where Git's actual behavior differs from what a user might intuitively expect when first using worktrees.

The discussion will likely involve explanations about why worktree directories aren't automatically ignored by default, possibly touching on Git's general philosophy about being explicit rather than implicit, or technical constraints in how worktrees are implemented. Contributors may also suggest practical solutions like adding specific ignore patterns.