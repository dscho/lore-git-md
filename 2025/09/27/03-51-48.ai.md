Feature patch series expanding Rust libgit configuration handling, now at v3 with additional methods beyond the originally proposed boolean parsing. The series maintains its core FFI-wrapped implementation approach for `get_bool()` while adding `get_ulong()` and `get_pathname()` methods in the final patch.

The v3 changes show:
1. The boolean parsing implementation (patch 2) now correctly wraps `git_configset_get_bool()` through FFI as previously directed, with comprehensive test coverage for edge cases like "00" and "100".
2. A new patch 3 introduces unsigned long and pathname parsing methods following the same FFI pattern, adding `libgit_configset_get_ulong()` and `libgit_configset_get_pathname()` bindings.
3. The documentation fix for PO README (patch 1) remains unchanged from previous versions.

Key technical details:
- Core boolean implementation matches Git's native behavior through FFI calls
- New methods maintain consistent error handling and memory management patterns
- Test coverage expanded to include:
  - Large numeric values (testing 64-bit `ulong` handling)
  - Relative and absolute path parsing
  - Edge cases for all value types
- Bindings updated in `libgit-sys/src/lib.rs` for new C functions

The series appears technically sound but now exceeds its original scope. While the boolean implementation addresses all prior feedback, the new methods in patch 3 would benefit from explicit maintainer approval of this expanded scope. The patch organization remains clean with logical separation between the boolean implementation and new additions.