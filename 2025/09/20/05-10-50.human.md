This is a review comment on patch 3/6 in a series addressing strvec memory management issues in `setup_revisions()`. Eric Sunshine points out a grammatical issue in the commit message that makes the explanation hard to follow. The patch itself (not shown in full here) introduces a new `setup_revisions_from_strvec()` wrapper API to safely handle strvec-based calls to `setup_revisions()`.

The thread context shows this is part of a comprehensive fix for memory management problems that originally manifested as a double-free crash in `git stash show -p`. The series includes both immediate fixes and architectural improvements to prevent similar issues across Git's codebase. This particular patch is one of the architectural improvements, converting callers to a safer API while maintaining backward compatibility.

Eric's comment is narrowly focused on improving the commit message clarity, suggesting the first sentence needs reworking for better readability. This appears to be a minor polish on an otherwise well-received patch series that has already seen extensive review and validation. The thread indicates the technical approach has consensus, with Jeff King (the author) having implemented both the core solutions and now proposing post-merge refinements.