This is a bugfix and refactoring patch in the strvec memory management series. Jeff King is responding to Eric Sunshine's review of patch 3/6, which introduced a new `setup_revisions_from_strvec()` wrapper API to safely handle strvec-based calls to `setup_revisions()`.

The exchange clarifies a grammatical issue in the original commit message that made it difficult to understand which call-sites were being converted. Jeff King provides the corrected sentence, which now clearly states the patch converts call-sites that use the `free_removed_argv_elements` option.

This is a minor documentation fix in response to review feedback, maintaining the technical correctness of the patch while improving its clarity. The change doesn't affect the code itself - it just makes the commit message's explanation of the conversion scope clearer to readers. The patch remains part of the larger effort to fix strvec memory management issues in revision parsing.