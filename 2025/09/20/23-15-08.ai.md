This is a follow-up discussion about Git's trailer formatting behavior, specifically around newline handling in the `%(trailers)` format placeholder. The email is a technical analysis of why empty trailer lines appear in Junio Hamano's log output query, not directly related to the xdiff refactoring series that forms the main thread.

Jeff King explains that the 17,501 empty lines in Junio's output come from `%(trailers)` always appending a newline, which then combines with `--format`'s own newline to create blank lines between commits. He demonstrates this with a clearer example using `%h %s%n(trailers:only,keyonly)` and discusses the tradeoffs of changing this behavior, noting it could break existing formats that rely on the current implementation.

The email suggests two potential solutions: making the formatter smarter about when to add final newlines (only when the text doesn't already end with one), or introducing a new option/placeholder for different newline handling. However, Jeff cautions that changing the current behavior might break existing user formats.

This is a standalone observation about Git's pretty-format trailer handling, unrelated to the xdiff refactoring work that was the main topic of discussion in the thread. The email provides technical insight into an edge case of trailer formatting but doesn't propose any concrete changes to the codebase.