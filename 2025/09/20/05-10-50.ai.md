This is a review comment on patch 3/6 in the strvec memory management series, focusing on the new `setup_revisions_from_strvec()` wrapper API. Eric Sunshine points out a grammatical issue in the commit message that makes the explanation of the `free_removed_argv_elements` behavior unclear. The patch itself (authored by Jeff King) introduces a safer interface for strvec-based calls to `setup_revisions()`, ensuring proper memory management when arguments are consumed during revision parsing.

The thread context shows this is part of a comprehensive fix addressing both immediate memory safety issues (double-frees and leaks) and broader architectural improvements to strvec usage patterns. Patch 3 specifically converts callers to the new wrapper API while maintaining backward compatibility. Eric's feedback is narrowly focused on documentation clarity rather than technical concerns, suggesting the core implementation is sound but could benefit from clearer explanation of the memory management behavior changes.