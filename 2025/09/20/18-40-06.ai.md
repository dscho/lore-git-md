Gitk window persistence improvement (part 1/2 of series). This patch fixes the main window's position restoration on initialization, completing the geometry persistence that previously only handled window size. The change modifies the Tcl/Tk code in gitk to use an `after idle` callback when restoring window geometry, ensuring proper timing across different operating systems. The fix specifically addresses the main window's position not being restored between sessions, while leaving the window size handling (which was already working) unchanged. The patch is small (2-line addition) and focused, modifying the `makewindow` proc to trigger geometry restoration after the window is mapped. This appears to be a straightforward technical fix for a specific UI quirk, though the commit message could benefit from more detail about which OSes exhibit the timing issue being addressed. The change is part of a two-patch series aimed at making gitk window geometry fully persistent.