This is a follow-up in the ongoing `git whatchanged` deprecation thread, with Ben Knoble providing an alternative command suggestion. The email offers a simpler `git log` invocation for James Puleo's specific use case of finding which ref last contained a deleted file.

Ben suggests using `git log -- path/to/deleted/file` (or possibly `git rev-list`) as a straightforward replacement for the deprecated `git whatchanged` command. This directly addresses James's stated need to locate the last ref containing a deleted file, though it's worth noting this differs slightly from Kristoffer's earlier suggestion of `git log --name-status --diff-filter=D -- <path>` which explicitly filters for deletion events.

The response is brief and technical, focusing purely on the command-line solution without engaging in the broader deprecation discussion. It provides a more minimal alternative to the options previously discussed in the thread, suggesting either `git log` or `git rev-list` with just the path parameter. This continues the thread's pattern of documenting migration paths while the fundamental question of whether the deprecation creates unnecessary friction remains open.