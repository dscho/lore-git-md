Thread identity: bug report about gitk's macOS-specific window focusing behavior when `osascript` is unavailable in PATH, reported by Robert Scott against gitk version 2.50.1.

Core problem: gitk unconditionally calls `osascript` to focus/raise its UI window on macOS, failing completely if the command isn't available. This affects environments like Nix shells where `osascript` might be missing, despite being non-critical functionality.

Current status: bug reported, no fix yet implemented. The reporter suggests making the `osascript` call non-fatal to gracefully handle missing command cases while maintaining current behavior when available.

Key technical details: macOS-specific UI behavior in gitk, dependency on `osascript` command in PATH, failure mode when command is missing. The proposed solution would wrap the call in error handling to prevent complete failure.

Open questions: whether to implement the suggested fix, potential edge cases in different macOS environments, and whether similar changes might be needed for other platform-specific quality-of-life features.

Participant positions: Robert Scott reported the issue and suggested the solution direction. No other participants have commented yet.