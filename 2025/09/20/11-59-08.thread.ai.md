Thread identity: bug report about gitk's macOS-specific window focusing behavior when `osascript` is unavailable in PATH, reported by Robert Scott against gitk version 2.50.1, with follow-up from Eric Sunshine and maintainer action from Johannes Sixt.

Core problem: gitk unconditionally calls `osascript` to focus/raise its UI window on macOS, failing completely if the command isn't available. This affects environments like Nix shells where `osascript` might be missing, despite being non-critical functionality. The issue was previously identified in 2018 with a fix proposed but not merged.

Current status: Johannes Sixt has implemented the solution from Eric Sunshine's 2018 patch - making the `osascript` call non-fatal while maintaining current behavior when available. Eric has verified the implementation correctness and provided updated lore.kernel.org links for the commit message's historical references. The technical solution is complete and tested, with only minor documentation polish remaining.

Key technical details: macOS-specific UI behavior in gitk, dependency on `osascript` command in PATH, failure mode when command is missing. The implemented solution wraps the `exec osascript` call in Tcl error handling to prevent complete failure. The approach remains valid from 2018 and has been confirmed working with current gitk code.

Open questions: Final decision on whether to incorporate Eric's updated lore.kernel.org links in the commit message (either via Johannes updating or Eric resubmitting). No technical questions remain - this is purely documentation hygiene before merge.

Participant positions: Robert Scott reported the issue. Eric Sunshine contributed historical context and the original 2018 patch solution, now verified the implementation and provided documentation updates. Johannes Sixt as maintainer has implemented the solution and is shepherding it through finalization. All parties agree on the technical approach.

Related work: the 2018 patch referenced by Eric Sunshine provided the technical solution now implemented. The thread has progressed from problem report through implementation to final documentation polish, with merge imminent once the commit message references are finalized.