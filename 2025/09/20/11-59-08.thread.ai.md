Thread identity: bug report about gitk's macOS-specific window focusing behavior when `osascript` is unavailable in PATH, reported by Robert Scott against gitk version 2.50.1, with follow-up from Eric Sunshine referencing a 2018 patch.

Core problem: gitk unconditionally calls `osascript` to focus/raise its UI window on macOS, failing completely if the command isn't available. This affects environments like Nix shells where `osascript` might be missing, despite being non-critical functionality. The issue was previously identified in 2018 with a fix proposed but not merged.

Current status: Eric Sunshine has surfaced a 2018 patch that would implement the suggested solution - making the `osascript` call non-fatal while maintaining current behavior when available. The thread now awaits either maintainer action to apply the existing patch or verification/updates to ensure it works with current gitk code.

Key technical details: macOS-specific UI behavior in gitk, dependency on `osascript` command in PATH, failure mode when command is missing. The solution would wrap the call in error handling to prevent complete failure. The technical assessment remains unchanged - this is a quality-of-life improvement for non-critical functionality.

Open questions: whether the 2018 patch can be applied as-is or needs updates for current gitk codebase, and whether the current maintainer (CC'd in Eric's email) will pick this up. The historical context suggests this might have been blocked by maintainer inactivity rather than technical objections.

Participant positions: Robert Scott reported the issue and suggested the solution direction. Eric Sunshine has now contributed historical context by pointing to an existing unmerged patch that would solve the problem, effectively endorsing the technical approach while highlighting the maintainer workflow issue that previously prevented resolution.

Related work: the 2018 patch referenced by Eric Sunshine represents prior work addressing this exact issue, providing a potential ready-made solution if still applicable. The thread's progression now depends on maintainer engagement with this historical context.