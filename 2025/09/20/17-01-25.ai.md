This is a documentation refinement in the SHA-1/SHA-256 interoperability series, specifically addressing pack index format details. The email resolves a lingering question about checksum length in the pack index v3 format documentation, confirming the decision to use full 32-byte SHA-256 checksums rather than truncated 20-byte values. Junio Hamano and Brian Carlson agree this aligns with Git's existing SHA-256 usage patterns and avoids unnecessary complexity.

The discussion reveals historical context - the original design may have considered 20-byte truncated checksums, but the implementation naturally produces full 32-byte hashes through `finalize_hashfile()` with `CSUM_HASH_IN_STREAM`. Both parties recognize this as either a documentation typo or an early design choice that's now being corrected before the format sees production use.

Key technical points:
- Pack index v3 will store full SHA-256 checksums (32 bytes)
- This matches behavior of other SHA-256 file formats in Git
- The `CSUM_HASH_IN_STREAM` mechanism inherently produces full-length hashes
- Documentation is being updated to reflect current implementation reality

This email marks resolution of one of the open questions from earlier in the thread, specifically the pack index checksum length discrepancy. The exchange demonstrates the series' progression from design discussion to concrete documentation updates, with both technical and historical context informing the final decision.