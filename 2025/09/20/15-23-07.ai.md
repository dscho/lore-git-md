This is a documentation patch in the SHA-1/SHA-256 interoperability series, specifically addressing pack index format details in response to Junio C Hamano's review. The discussion focuses on whether pack index v3 should use 20-byte or 32-byte SHA-256 checksums in the trailer section. 

Brian m. carlson confirms the original intention was to use 20-byte truncated hashes for pack index checksums, but now argues for consistency with Git's existing SHA-256 usage (32-byte checksums) to avoid unnecessary complexity. This responds to Junio's observation that the documentation contained a typo ("20-byte SHA-256") and his question about historical design intent.

The exchange reveals an implementation detail: Git currently uses full 32-byte SHA-256 checksums in practice, making the documentation mismatch more than just a typo. The thread context shows this is part of broader work on object storage formats (pack v3 index and loose objects) in the interoperability series, with Junio emphasizing documentation accuracy throughout the review.

Key technical points include:
- Pack index v3 format changes (8-byte offsets replacing 4-byte)
- SHA-256 checksum length consistency (20 vs 32 bytes)
- Historical context about truncated hash considerations
- The relationship to other hash-algorithm interoperability work in the series

This email doesn't introduce new code changes - it's a design clarification in response to documentation review. The discussion may influence future documentation patches in the series regarding hash storage formats, particularly around when (if ever) truncated hashes should be specified versus full-length hashes. The tone suggests consensus is forming around using full 32-byte SHA-256 checksums throughout.