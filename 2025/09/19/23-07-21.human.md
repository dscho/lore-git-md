Bugfix and architectural improvement for revision parsing memory management. This is the final patch in a series addressing strvec memory handling issues in `setup_revisions()`, which was causing crashes in commands like `git stash show -p` with invalid options.

Jeff King proposes an additional refinement to the NULL termination behavior in `setup_revisions()`. While the main patch already ensures proper termination at the argc boundary, this follow-up suggestion would extend that to all entries beyond the revised argc when they need to be freed (as with strvec inputs). The change would modify how `--` arguments are handled, freeing and NULLing all consumed arguments rather than just the `--` itself.

The patch demonstrates careful consideration of edge cases, maintaining backward compatibility while closing potential maintenance hazards. It builds on the new `setup_revisions_from_strvec()` API introduced earlier in the series, which provides safer strvec usage patterns. The changes are focused on the revision parsing core but affect many commands that use it (stash, bisect, rebase, etc.).

This appears to be a well-considered refinement to an already thorough fix series. The discussion shows thoughtful analysis of tradeoffs between completeness and complexity, with the author acknowledging the current solution may be "good enough" without this extra change. The series as a whole represents significant progress in making Git's revision parsing more robust against memory management errors.