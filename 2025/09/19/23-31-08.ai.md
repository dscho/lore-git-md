This is a review comment on patch 7/9 in the SHA-1/SHA-256 interoperability series, focusing on GPG signature validation in tag objects. Junio Hamano provides two main pieces of feedback:

First, he requests line wrapping for a long conditional statement checking for "gpgsig" or "gpgsig-sha256" headers, showing the preferred formatting with proper indentation. This is a style nit on code that validates the presence and format of GPG signature headers in tags.

Second, he raises a substantive design consideration about how multiple signature headers should be handled. The current patch validates individual signature headers but doesn't address whether tags should be allowed to contain both "gpgsig" and "gpgsig-sha256" simultaneously. Junio suggests future-proofing the design by considering rules like:
- Uniqueness of gpgsig* headers (no duplicates)
- Exclusion of primary hash algorithm from headers
- Consistency between header signatures and inline body signatures

The comment doesn't reject the patch's technical approach but probes the design space for handling multiple hash-algorithm signatures in tags. This continues the series' pattern of careful review focused on forward-compatible design decisions for SHA-1/SHA-256 interoperability. The discussion touches core tag parsing logic in fsck.c but doesn't indicate any fundamental flaws in the implementation.