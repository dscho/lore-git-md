Refactoring patch in the xdiff preparation series for Rust compatibility. This is patch 4/10 in the v3 series of preparatory cleanups that are being merged to 'next'.

The patch removes the helper function `xdl_get_rec()` from xdiff/xemit.c, which was previously used to access fields of `xrecord_t` through an indirection. The function's two call sites now directly access the `xdf->recs[ri]` array and its `ptr` and `size` fields. This change makes the field usage more transparent and is part of the broader effort to simplify xdiff's internals for potential Rust integration.

The modification is straightforward - it eliminates an unnecessary abstraction layer while maintaining identical behavior. The diff shows 25 lines removed and 13 added, with the changes focused on making the record access patterns more direct. This continues the series' theme of removing indirections and making the code's data flow more explicit, though unlike some other patches in the series, this one doesn't have any noted performance impact.