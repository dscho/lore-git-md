Refactoring patch in the xdiff preparation series for Rust compatibility. This is patch 2/10 in the preparatory cleanup series that's now being merged to 'next'.

The patch simplifies memory management in `xdl_prepare_ctx()` by eliminating redundant local variables and directly initializing/freeing the `xdfile_t` struct fields. Instead of maintaining separate local variables that shadow the struct fields, the code now works with the struct directly and uses the existing `xdl_free_ctx()` function for cleanup. This removes duplicate cleanup code paths and makes the memory management more straightforward.

The change is purely internal - there's no behavior difference, just cleaner code organization. It's part of the larger effort to clean up xdiff internals in preparation for potential Rust integration, though this particular change isn't Rust-specific. The patch follows the series' pattern of incremental, behavior-preserving improvements to xdiff's code structure.