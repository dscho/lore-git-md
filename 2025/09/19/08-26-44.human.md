Documentation patch for the ref optimization series. This is part 6/9 of Meet Soni's GSoC project to introduce a new `git refs optimize` subcommand while maintaining compatibility with the existing `git pack-refs` command.

The patch factors out the common options documentation from `git-pack-refs.adoc` into a new shared file `pack-refs-options.adoc`, which will allow both commands to use the same option documentation. This is a clean refactoring with no functional changes - the rendered documentation remains identical for `git pack-refs`.

The change follows the architectural direction established earlier in the thread, where the new `refs optimize` command shares infrastructure with `pack-refs`. The documentation restructuring mirrors the code refactoring done in previous patches to share implementation between the commands.

This appears to be a straightforward and well-executed documentation change that maintains consistency with the larger refactoring effort. The patch has been reviewed by mentors Patrick Steinhardt and shejialuo.