Test patch completing the `git refs optimize` test infrastructure. This is the final patch in Meet Soni's series implementing the new subcommand as a modern replacement for `git pack-refs`. The patch adds `t/t1463-refs-optimize.sh` as a minimal driver script that reuses the comprehensive test suite from the shared `pack-refs-tests.sh` library by setting `pack_refs='refs optimize'` before sourcing it.

Key technical details:
- The test verifies identical behavior between `git refs optimize` and `git pack-refs` through shared test infrastructure
- Follows the same pattern used for testing `git-for-each-ref`/`git-refs list` command sharing
- Added to meson.build test suite configuration
- Maintains existing test patterns with GIT_TEST_* environment variables
- Confirms branch semantics remain unchanged after optimization

The implementation builds on the previous test refactoring work (patch 8/9) that moved all test logic to the shared library. This completes the test coverage for the new subcommand while ensuring it maintains compatibility with the existing `pack-refs` behavior. The approach demonstrates thorough validation of the new command through reuse of the comprehensive test suite originally developed for `git pack-refs`.