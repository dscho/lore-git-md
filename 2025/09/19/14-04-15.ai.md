This is a technical discussion about edge cases in synchronizing Git stashes across machines, specifically focusing on push rejection scenarios with `--force-with-lease`. Phillip Wood is trying to understand why Brooke Kuhlmann's multi-export workflow fails while his test script succeeds, despite both involving two exports before pushing.

The key technical point is that Brooke's workflow shows `--force-with-lease` failing on the second push with a "remote ref updated since checkout" error, while Phillip's script doesn't reproduce this. Phillip is confused because both approaches involve:
1. Initial stash creation and export
2. First push
3. Second stash creation and export
4. Second push attempt

Broke has found that only full `--force` pushes work reliably in this scenario, though this is suboptimal for safety. The reflog creation workaround Phillip suggested earlier doesn't actually solve the remote update problem - while it makes the error disappear, the remote stash isn't properly updated.

The discussion reveals two important technical details:
1. The failure only manifests in multi-export scenarios where the same ref is exported and pushed multiple times
2. There's still uncertainty about how `git stash import` would handle forced updates from remotes

This email shows Phillip carefully comparing the two workflows to identify why they behave differently, suggesting the issue might be more subtle than initially thought. The core challenge remains finding a safe synchronization method that doesn't require full force pushes, with both participants trying to understand the exact conditions under which `--force-with-lease` fails.