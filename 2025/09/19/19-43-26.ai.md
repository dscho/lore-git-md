This is a follow-up in the `git whatchanged` deprecation discussion thread. Kristoffer Haugsbakk responds to James Puleo's report about the command's removal, confirming the deprecation path and suggesting alternative invocations of `git log` to achieve similar functionality.

The email clarifies that `git whatchanged` is being removed because its functionality is fully covered by `git log` with better defaults for human-readable output. It specifically recommends `git log --raw --no-merges` as a direct replacement that maintains the file-change listing behavior James was using. The response notes that `git log` offers more flexible output options like `--stat` or `--name-only` for different levels of detail.

Key technical details include the specific `git log` flags that can replicate `whatchanged` behavior, with `--raw` providing the file-change metadata and `--no-merges` matching `whatchanged`'s default filtering. The email positions this as part of Git's ongoing interface simplification efforts, where redundant commands are being phased out in favor of more versatile alternatives.

The response doesn't address James's usability concerns about the deprecation warning friction, focusing instead on the technical migration path. It maintains the thread's technical character while acknowledging the original use case of tracking deleted files across refs. The mobile disclaimer suggests the response might lack exhaustive testing of the exact flag combinations.