Final patch in the SHA-1/SHA-256 interoperability test infrastructure updates. This adds a `COMPAT_HASH` test prerequisite to handle cases where tests need to behave differently when running with SHA-256 storage and SHA-1 compatibility mode. The patch reworks the existing `BROKEN_OBJECTS` prerequisite (which detects when tests can create deliberately invalid objects) to depend on `COMPAT_HASH`, since broken objects can't be created in compatibility mode where SHA-1 values are required.

The change is minimal but important for test correctness - it ensures tests that try to create invalid objects will automatically skip when running in compatibility mode, where such tests would be invalid. This completes the test infrastructure portion of the series, building on the earlier introduction of `test_repo_compat_hash_algo` and related variables. The implementation is straightforward, just checking for the presence of the compatibility hash variable to determine the prerequisite status.

This patch maintains the series' focus on preparatory work for the hash transition, specifically around test suite support. It follows logically from the previous test infrastructure additions and helps ensure tests will behave correctly when the actual hash transition implementation lands. The change is small and focused, touching only test prerequisites in `t/test-lib.sh`.