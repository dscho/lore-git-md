This is the final implementation series for the `git refs optimize` subcommand, which provides a backend-agnostic replacement for `git pack-refs`. The series represents the culmination of an architectural effort to create a unified reference optimization API across Git's storage backends.

The 9-patch v4 series completes all major implementation work, with only minor documentation and style polish remaining. Key technical achievements include:

1. A new `refs_optimize()` API in refs.[ch] that follows Git's standard patterns, with implementations for both files and reftable backends. The final version removes a NULL check to align with project conventions.

2. Refactoring of `git pack-refs` to use the new API while maintaining backward compatibility. The command now works generically across backends (triggering compaction for reftable).

3. Introduction of the new `git refs optimize` subcommand that shares core logic with `pack-refs` via a new pack-refs.c library. Both commands now use identical option handling through the PACK_REFS_OPTS macro.

4. Comprehensive test infrastructure with a 430-line shared test library (t/pack-refs-tests.sh) used by both commands, plus new t1463-refs-optimize.sh to verify the subcommand's behavior matches pack-refs.

5. Documentation restructuring with common options in pack-refs-options.adoc included by both git-pack-refs.adoc and git-refs.adoc.

The series shows careful attention to code sharing - between backends, between commands, and between tests. The implementation cleanly separates concerns while maintaining identical behavior between the old and new interfaces. The v4 changes focus on final polish: improved commit messages and removing the NULL check from the API dispatcher to match Git conventions.

This represents the complete realization of the architectural vision established earlier in the thread, with all major components now implemented and integrated. The remaining work consists of minor documentation clarifications and style fixes that Junio Hamano will handle locally before merging.