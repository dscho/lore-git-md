This is the implementation of the new `git refs optimize` subcommand, completing the ref optimization functionality that has been developed throughout this thread. The patch adds the subcommand as a modern replacement for `git pack-refs`, sharing the same core logic via the previously-factored `pack_refs_core()` function.

The implementation connects several architectural pieces established earlier in the series:
- Uses the shared `PACK_REFS_OPTS` macro for consistent option handling
- Leverages the common options file via AsciiDoc include directive
- Maintains identical behavior to `git pack-refs` through the shared core function
- Integrates with the refs API's optimization infrastructure

Key technical details:
- Adds `cmd_refs_optimize()` in builtin/refs.c that calls `pack_refs_core()`
- Documents the subcommand in git-refs.adoc, including the shared options
- Updates the command dispatch to recognize the new subcommand
- Preserves all existing functionality while providing a more modern interface

The patch appears well-integrated with the series' architectural goals, completing the transition from the original `pack-refs` implementation to the new shared infrastructure while maintaining backward compatibility. The documentation follows the established pattern of sharing content between related commands.