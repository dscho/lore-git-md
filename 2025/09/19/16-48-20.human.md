Bugfix discussion for `git stash show` double-free issue. Jeff King responds to Junio Hamano's proposed solutions for the memory management problem in `show_stash()`, where invalid arguments trigger a double-free during cleanup. 

King notes that Junio's first solution (NULLing unused slots after `setup_revisions()`) would still leak the "-p" argument string, suggesting the need for `free_removed_argv_elements` flag. He observes this is a general issue with argument rearrangement, not just this specific case, and is surprised the test suite doesn't catch it.

The discussion expands to consider broader implications - King points out similar problems likely exist in other code paths like `bisect_rev_setup()`. He suggests a more comprehensive solution: creating a `setup_revisions()` wrapper that would handle memory cleanup consistently by automatically enabling the free flag, adjusting the strvec count, and maintaining NULL termination. 

This moves the conversation from a local fix to considering systemic improvements to the revisions API's memory management contract. The technical depth suggests this may require careful implementation to avoid introducing new edge cases while solving the immediate crash.