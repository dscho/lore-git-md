This is a documentation patch that corrects inaccuracies in the hash function transition documentation regarding tag signatures. 

The patch updates the technical documentation to reflect how tag signatures actually work in practice after a missed change in Git 2.29. Originally, the docs described SHA-256 signatures always appearing in a header field, but the implementation ended up with signatures for the current algorithm always appearing in the tag body while the opposite algorithm's signature appears in a header.

The changes clarify that dual-signed tags will contain both an in-body signature (for the current hash algorithm) and a header signature (for the other algorithm). The documentation now accurately describes the three possible signing scenarios that exist in the actual implementation, rather than the originally planned behavior.

This appears to be a straightforward documentation fix that brings the docs in line with reality. It's part of a larger 9-patch series (this is patch 3/9) about the hash function transition, though the exact theme of the full series isn't clear from this patch alone. The change is well-explained and the diff shows careful attention to making the documentation both accurate and clear.