Bugfix follow-up for strvec memory management series. This is a minor fix to patch 5/6 in the series that addresses strvec memory safety issues in `setup_revisions()`. Jeff King notes he missed adding a `struct strvec` forward declaration in `shallow.h` when converting `get_shallow_commits_by_rev_list()` to use strvecs. The oversight was caught by the hdr-check test.

The change is straightforward - just adding the missing forward declaration to maintain header file correctness. This doesn't affect the functionality of the broader strvec conversion work, which has already been reviewed and tested. The series as a whole improves memory safety across Git's revision parsing code by properly handling strvec memory management.

This is the kind of minor polish that often surfaces after a series is mostly complete, showing the value of Git's comprehensive test suite. The fix maintains the series' goal of making strvec usage patterns more robust while keeping header dependencies clean.