This is a refactoring patch in the ongoing ref optimization series, specifically part 5/9 of Meet Soni's GSoC implementation. The patch factors out the core logic of `git pack-refs` into a shared library to enable code reuse with the new `git refs optimize` subcommand.

The key changes move the implementation from `builtin/pack-refs.c` into new files `pack-refs.c` and `pack-refs.h`, with the original command becoming a thin wrapper. The shared `pack_refs_core()` function handles option parsing and the actual packing operation, while maintaining all existing behavior. The header defines a `PACK_REFS_OPTS` macro for consistent option documentation between commands.

The refactoring appears thorough, preserving all functionality while making the code more modular. The patch follows the established pattern in this series of separating core logic from command implementations, and the changes are mechanically straightforward - mostly moving code between files with minimal logic changes. This prepares for the implementation of `git refs optimize` in subsequent patches while keeping the existing `git pack-refs` behavior identical.