Refactoring patch in the xdiff preparation series for Rust compatibility.

This is patch 9/10 in the v3 series of preparatory cleanups for xdiff's Rust integration, focusing on removing redundant variable aliasing in `xdl_build_script()`. The change eliminates temporary `rchg1` and `rchg2` pointers that were aliases for `xe->xdf1.rchg` and `xe->xdf2.rchg`, instead using direct struct member access. This follows the series' pattern of simplifying xdiff's internal data access patterns while maintaining identical behavior.

The patch touches only `xdiff/xdiffi.c`, modifying 7 lines with straightforward pointer access simplification. As with other patches in the series, it includes a "Best-viewed-with" trailer suggesting `--color-words` for review clarity. The change is purely structural with no performance impact, continuing the series' goal of making the code more straightforward before the Rust-specific changes in the follow-up series (patches 11-17).

This patch was previously reviewed in v2 and has no substantive changes in v3 beyond administrative resubmission. It builds on earlier standardization of rchg values (YES/NO/MAYBE macros introduced in this patch series) while maintaining the exact same control flow and error handling behavior. The removal of aliasing variables reduces potential confusion points when the code is later ported to Rust.