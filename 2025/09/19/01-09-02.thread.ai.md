SHA-1/SHA-256 interoperability series (9 patches) by brian m. carlson continues with part 7 focusing on GPG signature validation in tag objects and part 8 addressing pack index format documentation. The series' technical scope includes GPG signature validation for "gpgsig" and "gpgsig-sha256" headers in tags (fsck.c), `rev-parse --show-object-format=compat` plumbing, pack index v3 format changes (8-byte offsets, SHA-256 checksums), loose object documentation (`gitformat-loose.adoc`), and test infrastructure (`COMPAT_HASH` prerequisite, `GIT_TEST_DEFAULT_HASH` syntax).

Recent discussion has resolved key technical questions: pack index v3 will use full 32-byte SHA-256 checksums, and tag signature headers follow strict mutual exclusivity (`gpgsig` only in non-SHA-1 repositories, `gpgsig-sha256` only in non-SHA-256 repositories). Patrick Steinhardt has now questioned whether this header exclusivity was an intentional design choice or oversight, though he acknowledges Brian's previous explanation that it was deliberate absent a third algorithm. This represents a final vetting of the signature storage policy rather than a challenge to the settled approach.

Junio C Hamano's review has shaped implementation details:
- Requested line wrapping fixes for long conditionals in signature validation
- Clarified relative algorithm terminology ("current" vs "compatibility")
- Resolved pack index checksum length discrepancy
- Improved phrasing about signature field removal
- Emphasized precision in object storage documentation

Patrick contributed forward-looking suggestions about the pack index v3 format:
- Proposed making trailer hash algorithm-agnostic (not hardcoded to SHA-256)
- Suggested clarifying commit messages about updating unimplemented specs
- Fixed build system configuration for `gitformat-loose.adoc` documentation by adding entries to both Makefile and meson.build

Open technical considerations:
- Final documentation wording for signature field removal
- Clear conveyance of relative algorithm terms
- Handling signature headers without bodies in converted tags
- Patrick's algorithm-agnostic trailer hash suggestion
- Commit message clarifications

The thread shows steady progress with major questions resolved and terminology being refined. Patrick's latest contributions include both high-level design vetting (signature header policy) and low-level build system fixes (documentation configuration). The series approaches finalization with all architectural decisions settled and remaining work focused on precise wording, future-proofing considerations in the pack index format, and completing mechanical integration tasks like documentation build support.