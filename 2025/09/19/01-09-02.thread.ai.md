SHA-1/SHA-256 interoperability series (9 patches) by brian m. carlson continues with parts 3/9 (GPG signature validation), 5/9 (loose object documentation), 7-9 (pack index format), and now 8/9 (test infrastructure). The series' technical scope includes GPG signature validation for "gpgsig" and "gpgsig-sha256" headers in tags (fsck.c), `rev-parse --show-object-format=compat` plumbing, pack index v3 format changes (8-byte offsets, SHA-256 checksums), loose object documentation (`gitformat-loose.adoc`), and test infrastructure (`COMPAT_HASH` prerequisite, `GIT_TEST_DEFAULT_HASH` syntax, `BROKEN_OBJECTS` prerequisite).

Part 3/9 provides historical context for tag signature header design, documenting the strict mutual exclusivity between `gpgsig` and `gpgsig-sha256` headers from Git 2.29's SHA-256 signature support omission. Parts 6-9 focus on pack index v3 format and test infrastructure, with settled decisions on trailer checksums following the repository's primary hash algorithm. Part 5/9 adds `gitformat-loose(5)` documentation, incorporating Patrick Steinhardt's feedback by removing premature mapping file details, improving loose objects vs packfiles description, and explaining sharding directories while keeping the brief "loose" name matching existing conventions.

Part 8/9's test infrastructure now includes the `BROKEN_OBJECTS` prerequisite for testing malformed objects in compatibility mode. Brian confirms v2 changes addressing Patrick Steinhardt's review: adding a concrete test case demonstrating `BROKEN_OBJECTS` usage and fixing a grammatical error ("if" -> "whether") in the prerequisite description. This builds on the series' broader test infrastructure work with `COMPAT_HASH` and `GIT_TEST_DEFAULT_HASH`.

Implementation details confirmed:
- Pack index v3 trailer checksums use repository's primary algorithm
- 4-byte object count field suffices
- SHA-1 repositories in SHA-256 compatibility mode use SHA-1 for trailer checksums
- Loose object storage paths documented as `$GIT_DIR/objects/[0-9a-f][0-9a-f]/*` with `loose-object-idx` file
- `BROKEN_OBJECTS` prerequisite added for testing malformed objects in compatibility mode

Junio C Hamano's earlier reviews shaped signature validation conditionals, algorithm terminology, and pack index checksum handling. Patrick Steinhardt's cumulative reviews cover:
1. Loose object format documentation (Makefile/meson.build integration)
2. Test infrastructure changes in patch 8/9 (`test_detect_hash()`, `BROKEN_OBJECTS`)
3. Patch 9/9 squashing suggestion
4. Pack index v3 trailer checksum algorithm selection
5. Implementation status clarifications
6. Patch 6/9 test prerequisite cleanup
7. `BROKEN_OBJECTS` demonstration test case addition

Open items:
- Final documentation wording (naming, content accuracy, structure)
- Commit message clarifications
- Handling signature headers without bodies in converted tags
- `GIT_TEST_DEFAULT_HASH` syntax verification in tests
- V2 updates to pack index v3 documentation regarding trailer checksum algorithm selection
- Moving `RUST` prerequisite from patch 6/9 to part 2
- `BROKEN_OBJECTS` test case implementation in v2

The series shows steady progress with all architectural decisions settled. Patrick's reviews and Brian's responses demonstrate careful attention to documentation quality and test infrastructure, with test coverage expanding to include malformed object handling through the new `BROKEN_OBJECTS` prerequisite. The `rev-parse --show-object-format=compat` functionality remains uncontroversial with only minor test adjustments needed. Test infrastructure accounts for compatibility hash scenarios through `COMPAT_HASH` and now `BROKEN_OBJECTS`.