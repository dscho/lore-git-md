SHA-1/SHA-256 interoperability series (9 patches) by brian m. carlson continues with part 7 focusing on GPG signature validation in tag objects. The patch adds validation for "gpgsig" and "gpgsig-sha256" headers in tags, extending the series' work on hash algorithm interoperability. Junio C Hamano's review provides both style and design feedback: he requests proper line wrapping for a long conditional statement and raises substantive questions about handling multiple signature headers. The design considerations include whether tags should allow both "gpgsig" and "gpgsig-sha256" simultaneously, header uniqueness rules, and consistency between header signatures and inline body signatures.

The series continues its incremental approach with patch 6/9 having added a `--show-object-format=compat` option to `git rev-parse` to display the compatibility hash algorithm name when configured. Previous parts (1-5) established test infrastructure (`COMPAT_HASH` prerequisite, `GIT_TEST_DEFAULT_HASH="sha256:sha1"` syntax) and documented object storage formats in `gitformat-loose.adoc` and pack index v3 format (8-byte offsets/trailers, 32-byte SHA-256 checksums). Junio's ongoing documentation review has emphasized precision in describing object storage formats and hash computation details.

Key technical details span:
- `Documentation/gitformat-loose.adoc` (loose object structure)
- Pack format documentation (v3 index format changes for SHA-256)
- `rev-parse --show-object-format=compat` plumbing
- GPG signature header validation in fsck.c
- Build system integration (meson.build updates)

Open questions include:
- Rules for multiple signature headers in tags (gpgsig/gpgsig-sha256 coexistence)
- Future-proofing signature validation for hash algorithm interoperability
- Documentation precision around object storage formats

The series progresses as groundwork for SHA-1/SHA-256 interoperability with Junio's review continuing to focus on both technical correctness and forward-compatible design decisions. All feedback to date has addressed implementation details and documentation clarity rather than raising architectural objections.