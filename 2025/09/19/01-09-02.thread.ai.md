SHA-1/SHA-256 interoperability series (9 patches) by brian m. carlson continues foundational work for hash algorithm interoperability, now with a v2 cover letter clarifying the series' scope and changes from v1. The series spans documentation updates (pack format, loose object storage), test infrastructure (`COMPAT_HASH`, `BROKEN_OBJECTS` prerequisites), and GPG signature validation in tags.

Technical scope includes:
- Pack index v3 format documentation (8-byte offsets, checksum algorithm selection)
- Loose object storage documentation (`gitformat-loose.adoc`) with sharded directory structure
- `rev-parse --show-object-format=compat` plumbing command
- GPG signature validation for both `gpgsig` and `gpgsig-sha256` tag headers
- Test infrastructure for compatibility mode (`COMPAT_HASH`, `BROKEN_OBJECTS`)

V2 changes address Patrick Steinhardt's reviews:
- Squashed test changes
- Added `BROKEN_OBJECTS` demonstration test
- Clarified pack index v3 trailer checksum handling
- Improved documentation wording
- Removed premature loose object mapping file details
- Fixed grammatical errors
- Added concrete test cases

Implementation details confirmed:
- Pack index v3 trailer checksums use repository's primary algorithm
- SHA-1 repositories in SHA-256 compatibility mode use SHA-1 for trailer checksums
- Loose object storage paths documented as `$GIT_DIR/objects/[0-9a-f][0-9a-f]/*`
- `BROKEN_OBJECTS` prerequisite for testing malformed objects in compatibility mode

Review feedback incorporated from:
1. Junio C Hamano (signature validation conditionals, algorithm terminology)
2. Patrick Steinhardt (documentation structure, test infrastructure, pack index details)

Open items:
- Final documentation wording refinements
- Commit message clarifications
- Handling signature headers without bodies in converted tags
- `GIT_TEST_DEFAULT_HASH` syntax verification
- Pack index v3 documentation updates regarding checksum selection
- `RUST` prerequisite movement from patch 6/9 to part 2

The series shows steady progress with architectural decisions settled. Test coverage now includes malformed object handling through `BROKEN_OBJECTS`, building on existing `COMPAT_HASH` infrastructure. The cover letter positions this as preparatory work for future interoperability patches, with current focus on documentation and test infrastructure foundations. Patrick's reviews and Brian's responses demonstrate careful attention to both technical accuracy and documentation quality.