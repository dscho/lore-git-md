SHA-1/SHA-256 interoperability series (9 patches) by brian m. carlson continues with part 7 focusing on GPG signature validation in tag objects and part 8 addressing pack index format documentation. The series' technical scope includes GPG signature validation for "gpgsig" and "gpgsig-sha256" headers in tags (fsck.c), `rev-parse --show-object-format=compat` plumbing, pack index v3 format changes (8-byte offsets, SHA-256 checksums), loose object documentation (`gitformat-loose.adoc`), and test infrastructure (`COMPAT_HASH` prerequisite, `GIT_TEST_DEFAULT_HASH` syntax).

Recent discussion has resolved the pack index v3 checksum length question - it will use full 32-byte SHA-256 checksums rather than truncated 20-byte values, matching Git's existing SHA-256 usage patterns. This decision emerged from examining the implementation naturally produces full-length hashes through `finalize_hashfile()` with `CSUM_HASH_IN_STREAM`, and aligns with other SHA-256 file formats in Git. The documentation is being updated to reflect this.

Tag signature terminology was refined through discussion with Junio C Hamano, defining "current algorithm" (repository's primary hash algorithm from `extensions.objectFormat`) vs "compatibility algorithm" (SHA-1 when SHA-256 is primary), with the relationship flipping during object conversion between hash formats. Junio emphasized the need for documentation to clearly convey this relativity, though deferred to Brian's judgment on current wording adequacy. The discussion confirmed tags cannot simultaneously contain both `gpgsig` and `gpgsig-sha256` headers unless a third algorithm is introduced.

Junio's ongoing review has shaped both implementation and documentation:
- Requested line wrapping fixes for long conditionals in signature validation
- Raised design questions about multiple signature headers in tags
- Prompted resolution of the pack index checksum length discrepancy
- Suggested improved phrasing about signature field removal (changing "and" to "or")
- Consistently emphasized precision in object storage format documentation
- Requested clarification on relative algorithm terminology in tag signatures

Open questions and design considerations:
- Final rules for tag signature header coexistence (gpgsig/gpgsig-sha256)
- Future-proofing signature validation across hash algorithms
- Precise wording for signature field removal during verification
- Ensuring documentation clearly conveys the relativity of "current" vs "other" algorithm terms

The series continues progressing with Junio's review focusing on technical correctness and documentation accuracy. Recent discussion has shifted from implementation details toward consensus on storage format specifications and precise terminology, with Brian providing clarifications on relative algorithm naming in tag signatures. All feedback addresses refinement rather than architectural concerns, with Brian incorporating documentation improvements in the next revision. The thread shows steady progress toward finalizing the SHA-1/SHA-256 interoperability work, with one major open question (pack index checksum length) now resolved and terminology clarifications being incorporated.