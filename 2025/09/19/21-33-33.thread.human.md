Here's a human-readable summary of the thread so far:

The thread discusses improvements to how Git handles deprecated commands in the build system when `WITH_BREAKING_CHANGES` is enabled. The initial patch focuses on making the documentation-related exclusions consistent with how deprecated commands are already handled in the build process. Specifically, it proposes adding `git-whatchanged` and `git-pack-redundant` to the list of programs excluded from documentation when building with breaking changes enabled, matching their existing exclusion from the built binaries.

The change also connects the documentation linting process (via the `check-docs` target) to respect these exclusions. While the patch acknowledges there are still some unresolved dependency issues around `GIT-EXCLUDED-PROGRAMS`, it represents a step toward better handling of deprecated commands in the build system. The changes are focused on the Makefile and documentation build infrastructure, maintaining consistency with existing patterns for command deprecation.