Bugfix proposal addressing the `git stash show` double-free issue with two approaches. Junio Hamano presents both a targeted fix for the immediate problem in `builtin/stash.c` and a more comprehensive solution modifying `setup_revisions()` to prevent similar issues across Git commands.

The first approach (shown in the initial diff) is a minimal fix that NULLs unused slots in the strvec after `setup_revisions()` returns, specifically addressing the double-free in `show_stash()`. This is simple and backport-friendly but only fixes this one case.

The second approach modifies `setup_revisions()` itself to NULL unused slots, which would prevent this class of bugs in all commands using the revisions API. The patch includes both the core change in `revision.c` and a new test case in `t3903-stash.sh` to verify the fix. While more thorough, this solution requires careful review due to its wider impact.

The email continues the thread's technical discussion about whether to prefer localized fixes versus systemic solutions for API contract violations. Hamano notes the tradeoffs (performance vs safety) and invites review of the more complex solution, acknowledging his own history of off-by-one errors in similar changes.