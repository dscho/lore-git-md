This is a refactoring patch in the ongoing ref optimization series. Meet Soni factors out the core logic from `builtin/pack-refs.c` into a new shared library (`pack-refs.c`/`pack-refs.h`) to enable code sharing with the new `git refs optimize` subcommand.

The patch moves all the option parsing and packing logic into a new `pack_refs_core()` function while leaving `cmd_pack_refs()` as a thin wrapper. This maintains the existing `git pack-refs` behavior while making the functionality reusable. The changes include:

1. Creating new `pack-refs.c` and `pack-refs.h` files with the shared logic
2. Defining a `PACK_REFS_OPTS` macro for consistent option documentation
3. Updating build systems (Makefile and meson.build) to include the new files

The refactoring looks clean and well-motivated, with the commit message clearly explaining the need for sharing logic between commands. This appears to be a straightforward structural change that enables the next steps in the ref optimization work while preserving existing behavior.