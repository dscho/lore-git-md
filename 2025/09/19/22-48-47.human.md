This is part 3 of a 6-part series addressing strvec memory management issues in `setup_revisions()`. The patch introduces a new wrapper function `setup_revisions_from_strvec()` that provides safer handling of strvec arguments when setting up revision parsing.

Jeff King's patch creates a more robust interface for callers that construct their own argument vectors (rather than using OS-provided argc/argv). The wrapper automatically handles two tricky aspects: ensuring freed elements are properly tracked, and maintaining correct strvec state after revision parsing shuffles arguments around. The implementation carefully manages memory ownership during argument shifts while maintaining backward compatibility.

The change converts several existing callers (in bisect, stash, submodule, and remote code) to use the new helper. While no actual bugs were found in these callers, the new wrapper prevents potential future issues by enforcing proper memory management. The patch is part of a larger effort to make strvec usage in revision parsing more robust, following earlier fixes for memory leaks and double-free issues.

This appears to be a well-considered change that improves the codebase's safety without disrupting existing behavior. The commit message thoroughly explains the rationale and provides clear guidance for future callers. The series is progressing methodically from immediate fixes to broader architectural improvements.