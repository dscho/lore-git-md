This is a refactoring patch in the xdiff preparation series for Rust compatibility, removing unused fields from core xdiff data structures.

Patch 3/10 in the v3 series eliminates three fields that were initialized but never actually used in the code: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. The changeset shows these fields being removed from both the type definitions in xtypes.h and their initialization/cleanup code in xprepare.c. The most notable impact is simplifying the signature of `xdl_classify_record()` by removing the unused parameters.

The patch is straightforward - it removes dead code without changing any behavior, which is typical for this preparatory series. The commit message is brief but sufficient, and the diff is cleanly focused on its stated purpose. Since this is part of the already-approved preparatory series that's heading to 'next', it represents uncontroversial technical debt cleanup.