This is a documentation patch that corrects inaccuracies in the hash function transition documentation regarding tag signatures. It's part of a larger series (3/9) though no cover letter context is provided.

The patch updates Documentation/technical/hash-function-transition.adoc to reflect how tag signatures actually work in practice since Git 2.29. The documentation previously described an intended design where SHA-256 signatures would always be in headers, but the actual implementation ended up with signatures for the current algorithm always being in-body and the opposite algorithm in headers. The changes clarify that dual-signed tags contain both an in-body signature (for current algorithm) and a header signature (for the other algorithm), and updates the description of the signed payload calculation.

Key technical details for future reference:
- Documents the actual behavior of `gpgsig` and `gpgsig-sha256` fields in tag objects
- Clarifies signature placement (in-body vs header) depends on current hash algorithm
- Updates description of how signed payload is calculated by removing both signature fields
- Affects SHA-1/SHA-256 interoperability documentation
- Follows from an implementation quirk introduced in Git 2.29

The patch appears straightforward and well-motivated, fixing documentation to match reality. The changes are focused and the commit message clearly explains the discrepancy being corrected.