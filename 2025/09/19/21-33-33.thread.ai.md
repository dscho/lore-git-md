Thread identity: Patch series discussing build system and documentation changes for handling deprecated commands when `WITH_BREAKING_CHANGES` is enabled.

Core problem: The build system has infrastructure to exclude deprecated commands from documentation (via `EXCLUDED_PROGRAMS`) but it's not consistently used. The patch aims to better align documentation exclusions with the existing binary exclusions for deprecated commands.

Current status: Initial patch proposing to add `git-whatchanged` and `git-pack-redundant` to `EXCLUDED_PROGRAMS` when `WITH_BREAKING_CHANGES` is set, matching their existing exclusion from `BUILT_INS`/`BUILTIN_OBJS`. Also makes `check-docs` target depend on `Documentation/GIT-EXCLUDED-PROGRAMS` to ensure documentation linting respects exclusions.

Key technical details: Changes affect the build system (Makefile) and documentation generation. Specifically modifies handling of `EXCLUDED_PROGRAMS` and `check-docs` target dependencies. The patch acknowledges remaining issues with `GIT-EXCLUDED-PROGRAMS` dependencies but proposes this as an incremental improvement.

Open questions: The author notes the dependencies around `GIT-EXCLUDED-PROGRAMS` are still problematic, suggesting this doesn't fully resolve all build system issues in this area. No review feedback or discussion yet on the patch.

Participant positions: Only the original author (not yet identified in this thread) has contributed so far. No reviewers or maintainers have commented on the proposed changes.