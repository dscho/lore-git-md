This is a refactoring patch in the xdiff preparation series for Rust compatibility, specifically removing unused fields from xdiff's internal data structures.

The patch eliminates three fields that were initialized but never actually used in the code: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. The changes include:
1. Removing the fields from their struct definitions in `xtypes.h`
2. Simplifying `xdl_classify_record()` by removing the unused hash-related parameters
3. Eliminating allocation and cleanup code for the unused `rhash` array
4. Removing the calculation of `hbits` which was only used for the unused hash

The changes are purely internal cleanup with no behavior change, as these fields were dead code. The patch follows the series' pattern of simplifying xdiff's internals to make the codebase more maintainable and Rust-compatible. It's a straightforward removal of unused code, with the diff showing the fields being deleted from both struct definitions and all related initialization/usage sites. The "Best-viewed-with" trailer suggests using `--color-words` to easily see the removals in the diff.