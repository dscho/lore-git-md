This thread began with a bug report about memory management issues in Git's revision parsing code, specifically a double-free` crash in `git stash show -p` with invalid options. Jeff King led the effort to fix both the specific regression (introduced in commit 748bd094) and broader architectural concerns about strvec usage patterns. The solution involved six patches that progressed from immediate fixes to architectural improvements, introducing new APIs like `setup_revisions_from_strvec()` and ensuring proper NULL termination in `setup_revisions()`.

Now Junio Hamano has pointed out a subtle issue with the test case for the `git stash show` memory leak fix. The test assumed `git stash show` and `git stash show --` would produce identical output, which is only true when the stash contains no diff (as in the artificial test case). Normally, these commands produce different output formats (diffstat vs patch). Junio suggests either modifying the test to explicitly create an empty-diff stash or simply testing the `--` case without output comparison when running under leak detection. He also notes a broader inconsistency in `git stash show`'s argument parsing where `--` doesn't properly delimit pathspecs from revision args, though this behavioral issue is being deferred to avoid scope creep in the memory fix.

The core memory management improvements remain valid, with this discussion focusing on test refinement for the first patch. The series has already been merged, addressing both immediate bugs and architectural concerns across Git's revision parsing code. This latest exchange highlights how even well-tested fixes can reveal subtle edge cases, and how Git's maintainers carefully balance correctness with scope when addressing issues.