This thread documents a comprehensive fix for memory management issues in Git's `setup_revisions()` function, originally triggered by a double-free crash in `git stash show -p`. The discussion has progressed from initial bug report to merged solution, with Jeff King implementing core fixes and architectural improvements to prevent similar issues across Git's codebase.

The latest development is a test refinement from Junio Hamano, simplifying the test case for the `git stash show --` memory leak by removing an output comparison that incorrectly assumed identical behavior between `git stash show` and `git stash show --`. This change follows Jeff King's earlier note that the output format difference (diffstat vs patch) is pre-existing and unrelated to the memory fixes. The revised test now focuses solely on leak detection, marked with SANITIZE_LEAK to clarify its purpose.

The series has achieved consensus on keeping behavior changes separate from memory safety fixes. Jeff King has proposed a post-merge refinement to extend cleanup of arguments beyond the revised argc, which would make the solution more thorough while maintaining safety guarantees. The changes have been thoroughly tested across Git's command surface area, with the test suite now being refined to properly account for output format differences while verifying the memory fixes work as intended.