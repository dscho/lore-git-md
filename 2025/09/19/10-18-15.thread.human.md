The thread began with Lauri Niskanen reporting a double-free crash in `git stash show -p` when invalid options are provided, traced to a regression from commit 748bd094. Junio Hamano identified the root cause in `show_stash()`'s cleanup path where `strvec_clear()` encounters duplicate entries after `setup_revisions()` processes arguments without proper NULL-termination. Two solutions were proposed: a local fix in `show_stash()` or modifying `setup_revisions()` itself.

Jeff King has now expanded the discussion, noting that while Junio's patch would fix the immediate double-free, it doesn't address related memory leaks when arguments like "-p" are rearranged. He points out this reveals a broader issue with `setup_revisions()`'s strvec handling that affects other commands. King proposes a comprehensive wrapper solution that would properly manage strvec cleanup by enforcing invariants across three dimensions: the `free_removed_argv_elements` flag, strvec's `.nr` field, and array NULL-termination.

The discussion has evolved from fixing a specific crash to considering architectural improvements in argument parsing. While the original regression remains narrow in scope (only affecting invalid-option cases), the proposed systemic fix could prevent similar issues in commands like `git bisect`. The thread now awaits feedback on whether to proceed with the local patch as a quick backport candidate or invest in the more thorough solution.