This thread documents a comprehensive fix for memory management issues in Git's revision parsing code, specifically around the `strvec` API usage in `setup_revisions()`. The problems were originally discovered through a crash in `git stash show -p` with invalid options, but Jeff King's analysis revealed the issue affected multiple commands through shared revision parsing code.

The solution progressed through several stages: immediate fixes for the leak and double-free issues, followed by architectural improvements introducing a new `setup_revisions_from_strvec()` wrapper API for safer strvec usage. The series modified 30 files across core and builtin commands, with careful attention to backward compatibility. Testing confirmed the fixes under various sanitizers, with new test cases added for the specific crash scenarios.

After the main patches were merged, Jeff King proposed a post-merge refinement to make the cleanup more thorough while maintaining safety guarantees. In a recent follow-up, he clarified documentation around the `free_removed_argv_elements` behavior in response to Eric Sunshine's review, ensuring the commit message accurately reflects which call-sites were converted. This attention to detail in both implementation and documentation exemplifies Git's careful approach to memory management fixes. The thread shows how a specific bug report led to systematic improvements in a core API used across the codebase.