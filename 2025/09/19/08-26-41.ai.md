This is a backend implementation patch in the ongoing ref optimization series, specifically adding reftable backend support for the new `optimize` API.

The patch implements the `optimize` operation for the reftable backend by simply wrapping the existing `reftable_be_pack_refs()` function, since compaction (the reftable equivalent of packing) already provides the optimization behavior needed. The implementation is straightforward - it adds a new `reftable_be_optimize()` function that calls through to the packing function, and wires it up in the reftable backend's vtable.

This completes one of the key backend implementations needed for the new ref optimization infrastructure, following the same pattern as the files-backend implementation. The approach maintains consistency with the architectural decision to have both backends support the optimize operation, even though for reftable it's essentially an alias for compaction. The patch is clean and minimal, with clear documentation in the commit message about the relationship between optimization and compaction in the reftable context.

This appears to be a well-executed piece of the larger refactoring effort, implementing the agreed-upon design without introducing new complexity. The patch follows the established pattern from earlier in the series and helps complete the backend support needed for the new `git refs optimize` command.