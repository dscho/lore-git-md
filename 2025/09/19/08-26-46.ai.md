This is a test refactoring patch in the ongoing `git refs optimize` implementation series. It prepares the test infrastructure for the new command by making the existing `pack-refs` tests shareable between both commands.

The patch moves all test logic from `t0601-reffiles-pack-refs.sh` into a new `t/pack-refs-tests.sh` file, replacing hardcoded `pack-refs` calls with a `$pack_refs` variable. The original test file becomes a minimal driver that sets this variable and sources the shared test library. This follows the pattern used for sharing tests between other related commands like `git-for-each-ref` and `git-refs list`.

Key technical details:
- The new `pack-refs-tests.sh` contains 431 lines of test logic covering all aspects of ref packing behavior
- Tests verify core functionality like pruning, exclusion patterns, worktree refs, and edge cases with broken refs
- The test suite will be reused for `git refs optimize` by setting `pack_refs="refs optimize"`
- Maintains identical test coverage for both commands to ensure consistent behavior

The change is part of the final implementation phase of the ref optimization work, following the architectural refinements discussed earlier in the thread. It demonstrates the project's commitment to thorough testing while avoiding duplication between the old and new commands.