Implementation patch for reftable backend optimization. This is part of the ongoing ref optimization functionality series that introduces a new `git refs optimize` subcommand as a more accurate alternative to `git pack-refs`. 

The current patch focuses on implementing the reftable backend's support for the new optimization API. It's a straightforward change that wires up the existing `reftable_be_pack_refs()` function to the new `optimize` slot in the backend's virtual table. For reftable, optimization means compacting the tables, which is exactly what the existing pack operation already does.

This is patch 3/9 in the v4 series, following the architectural framework established earlier in the thread. The implementation is clean and minimal, leveraging existing functionality rather than introducing new code paths. The patch has been reviewed by mentors Patrick Steinhardt and shejialuo, suggesting it's technically sound.

The change completes the backend support needed for the new optimization API, working in parallel with the files-backend implementation that was discussed earlier in the thread. With both backends now supporting the optimize operation, the series can move forward with the user-facing command implementation.