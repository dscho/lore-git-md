This is a refactoring patch in the ongoing ref optimization series, converting `git pack-refs` to use the new backend-agnostic `refs_optimize()` API. The change is minimal but significant - just swapping out the `refs_pack_refs()` call for `refs_optimize()` in builtin/pack-refs.c.

The patch continues the thread's goal of establishing consistent terminology ("optimize" rather than "pack") that better describes the operation across both files and reftable backends. While the behavior remains identical, this change makes `git pack-refs` properly backend-agnostic, paving the way for the new `git refs optimize` command to share the same implementation.

The patch appears straightforward and well-motivated, with proper mentorship credits and sign-off. It's part of a larger, carefully architected series that has undergone significant review, so this incremental change seems low-risk while advancing the overall design.