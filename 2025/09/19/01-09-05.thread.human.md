Here's a human-readable summary of the thread so far:

This thread documents a correction to Git's hash function transition documentation regarding how tag signatures are actually implemented. The patch clarifies that since Git 2.29, tag signatures work differently than originally planned - the current hash algorithm's signature appears in the tag body while the other algorithm's signature goes in headers, which is the opposite of what was initially documented.

The change updates technical documentation to accurately reflect that dual-signed tags contain both an in-body signature (for the current hash algorithm) and a header signature (for the alternate algorithm). It also corrects the description of how the signed payload is calculated by removing both signature fields. This is a focused documentation fix that brings the docs in line with the actual behavior introduced in Git 2.29.