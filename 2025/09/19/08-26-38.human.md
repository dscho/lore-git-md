This is the final implementation series for the new `git refs optimize` subcommand, which provides a modern replacement for `git pack-refs` as part of the effort to consolidate ref-related commands under the `git refs` namespace.

The 9-patch series completes the architectural work discussed earlier in the thread, with v4 focusing on final polish. Key changes include:
1. Removing a NULL check from the `refs_optimize()` API to align with Git conventions
2. Improved commit messages explaining the "optimize" terminology
3. Full test coverage showing behavioral equivalence between the new subcommand and `pack-refs`

The implementation now includes:
- A generic `refs_optimize()` API that works across both files and reftable backends
- Shared core logic between `pack-refs` and the new subcommand
- Comprehensive documentation with common options factored out
- A 430-line test library reused by both commands

This appears to be a well-executed implementation that maintains backward compatibility while introducing the new subcommand. The test coverage is particularly thorough, and the shared infrastructure approach ensures consistent behavior. With these final documentation and commit message improvements, the series looks ready for merging.