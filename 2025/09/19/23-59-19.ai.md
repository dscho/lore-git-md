This is a technical clarification in the ongoing Rust integration thread regarding build system differences in PIE (Position Independent Executable) handling. Collin Funk provides key documentation explaining why Rust can link against Meson-built libgit.a without explicit PIE flags while failing with Make-built libgit.a.

The Rust compiler documentation indicates it defaults to position-independent code (equivalent to -fPIC/-fPIE) for most targets, which explains why Meson-built libgit.a works without explicit flags. The Make build system currently doesn't compile with position independence by default, causing the linking failure. This confirms the observed behavior difference between the two build systems.

The email doesn't propose a solution but provides the missing technical context that explains the discrepancy Junio Hamano and Ezekiel Newren were investigating. This information helps frame the ongoing discussion about whether and how to conditionally apply PIE flags in the Make build system to maintain compatibility with Rust linking requirements while preserving build system parity between Make and Meson.

The note is particularly relevant as it comes from Rust's official documentation, providing authoritative backing for the observed behavior. This moves the discussion from speculation about build system differences to a concrete understanding of Rust's default compilation model versus Git's current Make behavior. The thread's next steps will likely involve deciding whether to modify Make's behavior to match Meson's implicit PIE handling or maintain explicit flags conditionally.