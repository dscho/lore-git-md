This is a follow-up fix to patch 5/6 in the strvec memory management series. Jeff King addresses a minor oversight in the header file changes from the previous patch - the `shallow.h` file was converted to use `struct strvec` but lacked the corresponding forward declaration. 

The change is straightforward: it adds the missing `struct strvec` forward declaration to `shallow.h` to satisfy header dependency checks. This maintains consistency with the broader conversion of `setup_revisions()` callers to use the new strvec-based API introduced earlier in the series.

The fix is clearly necessary for compilation and aligns with the series' goal of standardizing on strvec usage for safer memory management in revision parsing. It doesn't change any behavior or require additional testing beyond what was already validated for the original patch. The author caught this quickly after submission, showing careful attention to build system requirements.