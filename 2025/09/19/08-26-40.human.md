This is a backend implementation patch for the ongoing ref optimization effort, specifically targeting the files backend. It provides the concrete implementation of the new `optimize` API for the traditional files-based reference storage.

The patch wires up the new `files_optimize()` function to call the existing `files_pack_refs()` function, since packing is how optimization is achieved in the files backend. This is a straightforward but important piece of the larger ref optimization architecture, making the new API functional for existing repositories.

The implementation is clean and minimal, just adding the new function and a simple wrapper that reuses existing packing logic. This follows the architectural direction established earlier in the thread to maintain compatibility while introducing the new terminology. The patch includes appropriate mentoring tags and maintains the project's standard practices.

As part of a larger 9-patch series, this represents a key step in making the new optimization functionality operational for the files backend, while keeping the implementation simple by leveraging existing well-tested code paths.