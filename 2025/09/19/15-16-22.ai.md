This is a v3 resubmission of the xdiff refactoring series, now confirmed to contain 10 preparatory cleanup patches (previously thought to be 9) before the Rust-specific changes. The series simplifies xdiff internals by removing unused fields and redundant structures while maintaining identical behavior.

The key changes from v2 are purely administrative - resending to ensure patch 9 appears on the mailing list and minor cover letter tweaks. No code changes were made since v2 version that Junio Hamano had already approved for merging to 'next'. The series remains focused on non-Rust-specific cleanups, with Rust type changes deferred to a separate follow-up series (patches 11-17).

The preparatory cleanups significantly streamline the xdiff data structures:
- Removes unused fields like `xrecord_t.next` and `xdfile_t.hbits`
- Eliminates redundant wrapper struct `diffdata_t`
- Replaces the `chastore_t` arena allocator with contiguous arrays (5% perf gain)
- Standardizes handling of the `rchg` change marker array
- Documents performance impacts (5% regression from hash simplification offset by other gains)

The changes touch nearly all xdiff implementation files (xprepare.c, xdiffi.c, xemit.c etc.) but maintain identical diff output. Patch 10 completes the series by treating `rchg` values as an enum (YES/NO/MAYBE) consistently across all algorithms. The cover letter includes helpful before/after struct comparisons showing the simplification.

This version confirms the final structure (10 preparatory patches) after Junio noted the earlier miscount. The Rust-specific type changes remain properly separated in the follow-up series, maintaining the clean division between behavior-preserving refactoring and type system modifications.