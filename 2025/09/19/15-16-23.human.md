Refactoring patch in the xdiff preparation series for Rust compatibility.

This is the first patch in v3 of a series preparing xdiff internals for potential Rust integration. It's a purely organizational change that moves `xdl_prepare_env()` later in xdiff/xprepare.c to eliminate 8 static forward declarations. The patch has no behavior impact - it just rearranges code to make the file structure more logical by removing the need for those declarations.

The change is straightforward and mechanical, following the series' goal of simplifying xdiff internals through incremental cleanups. As noted in the thread context, this is part of 10 preparatory patches that have already been approved for merging to 'next', with Rust-specific changes deferred to a follow-up series. The patch includes a "Best-viewed-with" trailer suggesting `--color-moved` to easily see the pure code movement.