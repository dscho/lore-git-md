This is patch 9/15 in Patrick Steinhardt's packfile store refactoring series, continuing the separation of packfile management from the object database. The patch splits `reprepare_packed_git()` into more focused functions, with `odb_reprepare()` now handling the broader object database maintenance (alternates, loose cache) while `packfile_store_reprepare()` focuses solely on packfile reloading.

The change moves the multi-purpose `reprepare_packed_git()` function's responsibilities into two new functions with clearer single purposes. `odb_reprepare()` becomes the new entry point for callers needing to refresh the object database state, while `packfile_store_reprepare()` handles just the packfile-specific reloading. This maintains the same behavior while better separating concerns.

The patch updates 13 call sites across various commands (gc, repack, fetch-pack, etc.) to use the new `odb_reprepare()` function. The changes are mechanical but widespread, touching core operations that need to refresh object availability. The diff shows careful attention to locking and proper function ordering in the new implementation.

This continues the series' pattern of methodical, behavior-preserving refactoring. The changes align with the architectural goal of separating packfile management from the broader object database concerns. No test changes are needed since the external behavior remains identical, though the improved separation of concerns may help future maintenance.