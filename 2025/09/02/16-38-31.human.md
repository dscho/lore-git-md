This is a UX discussion about improving the transition path from `git whatchanged` to `git log --raw`. Jorg responds to Junio's explanation that `--raw` is the compatible replacement for `whatchanged`, arguing that the option name is unintuitive for this purpose.

Jorg makes the case that `--raw` suggests outputting less refined data, when in fact it *adds* file change information to the normal log output. They propose renaming the option to `--whatchanged` as a more logical alternative that would make the deprecation of the standalone command more palatable. The tone is constructive but expresses some nostalgia for the old command.

The discussion remains focused on naming and discoverability issues rather than technical implementation. While Junio's previous response established the technical equivalence between the commands, this follow-up highlights the human factors in command-line UX that aren't yet resolved. No concrete action is proposed beyond the naming suggestion.