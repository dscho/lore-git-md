This is a technical follow-up in the `git subtree` bugfix thread, clarifying implementation details for the squashed subtree merge regression fix.

Phillip Wood corrects his earlier assessment about where the `should_ignore_subtree_split_commit()` checks should be placed. He now confirms these checks are correctly positioned to prune parent recursion in `check_parents()`, preventing unnecessary traversal into other subtrees. This addresses his previous question about why ancestor traversal was needed when commits are already processed in ancestor-first order.

The email reinforces the current consensus that the checks should remain in their current location rather than being moved into `check_parent()`. This maintains the important functionality of pruning subtree recursions while processing split operations. Phillip notes the regression test primarily validates behavior through the `$extracount` variable that tracks these recursions.

This exchange helps solidify the technical approach for the v2 patch, confirming that the core logic placement is sound despite initial questions about traversal order. The discussion remains focused on the precise mechanics of commit examination in `git-subtree.sh` without introducing new concerns or alternatives.