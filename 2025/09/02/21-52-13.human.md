This is a security fix for Gitweb, following up on Taylor Blau's initial patch that addressed command-line injection vulnerabilities. Eric Sunshine provides a minor typo correction (fixing "agains" to "against" in the commit message) while reviewing the security issue.

The vulnerability allows malicious users to inject arbitrary Git command-line options through crafted URLs, demonstrated by the ability to create arbitrary files via the `hpb` parameter. The fix leverages Git's `--end-of-options` feature (introduced in 2019) to safely delimit where options end and arguments begin when Gitweb spawns Git subprocesses.

The patch appears to be a straightforward and important security hardening, with the commit message clearly explaining both the vulnerability and solution. The typo fix is trivial but shows careful review attention to detail. This continues the thread's focus on systematically securing Gitweb's interface with Git commands against injection attacks.