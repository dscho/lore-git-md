This email is part of the ongoing discussion about Patrick Steinhardt's series refactoring packfile management into `struct packfile_store`. The current focus is on Patch 14/16 which removes `get_packed_git()` in favor of `get_all_packs()`.

Patrick clarifies a key misunderstanding about these functions: both load all packfiles in the repository, with the only difference being that `get_all_packs()` also prepares MIDX information for each MIDX'd packfile. He notes that since MIDX can only reference packfiles in the same object directory (which are already loaded), there's no actual difference in the returned packfile lists between the two functions.

This realization simplifies the transition - most callers won't need special handling for MIDX packs since both functions provide the same packfile coverage. Patrick acknowledges Taylor Blau's earlier concerns about specific callers (`cmd_grep()` and `want_object_in_pack_mtime()`) but suggests they may not need special treatment given this new understanding. He plans to update the commit message to reflect this clarification.

The discussion touches on technical details like:
- The relationship between MIDX and packfile loading
- The subtle behavioral differences between the two functions
- Caller impact analysis
- Commit message accuracy

This email advances the thread by resolving a key misunderstanding about the functions' behavior, potentially simplifying the patch by reducing the need for caller-specific adjustments. The clarification may also affect how reviewers assess the safety and completeness of the transition from `get_packed_git()` to `get_all_packs()`.