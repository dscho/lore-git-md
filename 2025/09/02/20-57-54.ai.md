This is a test fixup patch in the ongoing documentation verification series for t0450. Junio Hamano is following up on Patrick Steinhardt's proposed changes to the allowlist mechanism for builtins with missing documentation.

The patch makes two key technical improvements to the test logic:
1. Corrects the prereq setting logic by reversing the `grep` condition (now setting prereqs in the `else` branch when builtins *aren't* in the allowlist)
2. Adds the missing `adoc-missing` allowlist file containing 9 intentionally undocumented builtins like `checkout--worker` and `submodule--helper`

The test now properly verifies documentation presence/absence against the allowlist with separate assertions for each case, replacing the previous silent-skip behavior. The changes maintain compatibility with both Makefile and Meson builds while making the documentation verification more rigorous.

Junio is seeking confirmation from Toon Claes (the original patch author) on whether to squash these changes into the previous patch or keep them separate. The thread shows this series is functionally complete and ready for final review, with only minor procedural decisions remaining before integration.