This is patch 12/15 in Patrick Steinhardt's packfile store refactoring series, moving `get_multi_pack_index()` from packfile.c to midx.c as part of the ongoing architectural reorganization.

The patch relocates the MIDX-related function to its logical subsystem while exposing the previously static `packfile_store_prepare()` function it depends on. The change is behavior-preserving but improves code organization by:
- Moving `get_multi_pack_index()` implementation to midx.c
- Adding its declaration to midx.h
- Making `packfile_store_prepare()` public with proper documentation
- Removing the now-redundant declaration from packfile.h

The commit message explains this prepares for future callers outside the packfile system that will need to access MIDX data. The diff shows straightforward mechanical changes with no functional impact, continuing the series' pattern of careful subsystem boundary definition.

This patch follows the established refactoring approach seen in previous patches, maintaining all existing functionality while cleaning up architectural boundaries. It sets up for the remaining patches in the series that will further standardize MRU handling and complete the packfile store migration.