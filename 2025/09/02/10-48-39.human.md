Refactoring patch in Patrick Steinhardt's packfile store series (14/15). This continues the mechanical conversion of packfile-related functions to work with the new packfile store abstraction rather than directly with the repository object.

The patch renames `get_all_packs()` to `packfile_store_get_packs()` and updates all callers to first retrieve the packfile store from the repository before calling. The change is behavior-preserving - it just makes the packfile store dependency explicit rather than implicit through the_repository. 

This follows the pattern established earlier in the series where functions are migrated to work with the packfile store directly. The diff shows updates across 19 files, mostly straightforward mechanical changes to call the new function. The packfile store is now being used consistently throughout the codebase for packfile access.

The patch appears well-executed with thorough updates to all affected callers. It's part of the final stretch of this long-running refactoring series that has been carefully reviewed over multiple iterations. The changes are low-risk since they don't modify behavior, just the interface through which packfiles are accessed.