This is a documentation patch in the final stages of review for Julia Evans' series to improve `git-checkout`'s man page. The discussion has narrowed to fine-tuning terminology around branch operations, specifically avoiding the word "reset" which some users associate only with emergency recovery. 

Junio Hamano notes that `git reset` remains one of Git's core commands listed in `git -h`, though acknowledges the list could use updating. He agrees with Julia's discomfort with the "transactional equivalent" phrasing for describing branch switching behavior, encouraging a rewrite of that section. 

The thread shows consensus on documenting branch operations in terms of explicit pointer modifications rather than `reset` terminology, while maintaining technical accuracy about the underlying operations. This represents one of the last remaining wording refinements before the series can be merged, having already resolved all technical concerns about argument parsing, conflict behavior, and other implementation details.