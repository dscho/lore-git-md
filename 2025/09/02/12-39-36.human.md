This is a follow-up discussion about performance optimization in `git describe`, specifically regarding the `rs/describe-with-lazy-queue-and-khash` patch series.

Jeff King (peff@peff.net) responds to René Scharfe's request to keep the khash-based optimization for `git describe` in the 'seen' branch while further evaluating an alternative oidset implementation. Jeff expresses openness to René's original khash approach but agrees that if oidset can achieve similar performance with simpler code, that would be preferable. 

The exchange shows the two developers collaboratively evaluating tradeoffs between two technical approaches (khash vs oidset) for tracking queued commits in `git describe`'s depth computation. This appears to be a thoughtful, low-conflict technical discussion about optimizing a core Git command, with both parties showing willingness to find the best solution. The patch remains under active evaluation rather than being rushed to integration.