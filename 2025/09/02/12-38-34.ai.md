This is a security-focused follow-up in the ongoing khash vs commit-slab optimization discussion, specifically addressing hash function vulnerabilities in `git describe`'s depth computation.

Jeff King responds to René Scharfe's concerns about `oidhash()` being vulnerable to collision attacks by noting that the current `commit->index` approach has similar weaknesses - an attacker could manipulate ref loading order to force collisions. He agrees that extending `oidhash()` to use more bits from the OID (via XOR with a second word) would mitigate the issue while likely having minimal performance impact, since hash verification already dominates the cost.

The email includes a concrete patch proposal that modifies `oidhash()` to incorporate additional entropy from the OID. This builds on the thread's established security considerations while maintaining the performance characteristics that made khash attractive for sparse access patterns in `git describe`. The discussion remains pragmatic about real-world attack likelihoods while acknowledging the value of hardening against potential O(N^2) degradation scenarios.

Key technical points:
- Current `oidhash()` uses only 32 bits from the OID, making targeted collisions feasible
- Proposed fix XORs in an additional 32 bits from the OID for more entropy
- Performance impact expected to be negligible compared to hash verification costs
- Maintains thread's focus on security-conscious optimization tradeoffs
- Builds on prior benchmarking showing khash's 3% advantage in `describe.c`

The exchange demonstrates how security considerations raised earlier in the thread are being operationalized into concrete implementation changes, while preserving the performance benefits that motivated the original khash adoption.