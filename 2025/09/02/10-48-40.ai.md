This is the final patch (15/15) in Patrick Steinhardt's packfile store refactoring series, completing the interface consolidation by converting `get_packed_git_mru()` to work directly on the packfile store. The patch renames the function to `packfile_store_get_packs_mru()` and updates its signature to take a packfile store pointer rather than a repository.

The changes are mechanical and behavior-preserving, following the same pattern as previous patches in the series. The MRU (most-recently-used) list functionality remains identical, just accessed through the new store-based interface. The patch updates one caller in `pack-objects.c` to use the new function signature, and moves the function declaration from the general packfile.h interface to the packfile store section.

This completes the architectural transition of all major packfile access functions to the new store-based interface. The series has carefully maintained existing behavior while establishing cleaner subsystem boundaries. With this final conversion, the packfile store now provides a complete, self-contained interface for all packfile operations, removing the last direct access to repository-global packfile state.

The patch touches performance-sensitive areas (pack-objects) but makes no functional changes. No test updates are needed since the behavior remains identical. This concludes the technical refactoring work of the series, leaving only the open discussion about convenience wrappers (as noted in previous reviews) to be resolved.