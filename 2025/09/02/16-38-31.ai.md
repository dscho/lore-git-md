This is a UX discussion about improving the naming of `git log --raw` as a replacement for the deprecated `git whatchanged` command. The email continues the thread where a user (jorg@ml1.net) expresses dissatisfaction with the `--raw` option name, arguing it doesn't intuitively convey the "show changed files" functionality that `whatchanged` provided. They suggest renaming `--raw` to `--whatchanged` would make the transition more logical for users. 

Key technical points:
- The user clarifies their objection isn't to deprecating `whatchanged` itself, but to the non-intuitive naming of its replacement
- They analyze `--raw` as suggesting "less refined output" rather than the additional file-change information it actually provides
- The proposed `--whatchanged` name would directly map to the deprecated command's functionality

The discussion remains focused on naming semantics rather than technical implementation, with the user explicitly framing this as a UX/design concern rather than a feature request. No new alternatives beyond `--whatchanged` are proposed, and the email maintains a constructive tone while clearly stating the naming preference. This keeps the thread's focus narrow on whether the core team would consider such a rename to improve the deprecation transition path.