The thread discusses the deprecation of `git whatchanged` and its replacement with `git log --raw`, focusing on user experience concerns. The original poster raised issues with the `--raw` option name, arguing it doesn't clearly indicate the "show changed files" functionality that `whatchanged` provided. They suggest renaming `--raw` to `--whatchanged` to make the transition more intuitive.

Junio C Hamano, the Git maintainer, has acknowledged that while `--raw` is the technical equivalent of `whatchanged`, its name may not be the most user-friendly choice. He notes that other output formats like `--name-only` and `--compact-summary` are available for different levels of detail. The discussion remains focused on whether to implement `--whatchanged` as an alias for `--raw` to improve discoverability, with no resolution yet on this specific proposal.

The thread highlights a common challenge in software evolution: how to deprecate old functionality while providing clear, intuitive alternatives. The core technical capability exists, but the naming and documentation of the transition path remain open questions.