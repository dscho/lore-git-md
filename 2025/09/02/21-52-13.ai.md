This is a follow-up in the Gitweb security fix thread, addressing command-line option injection vulnerabilities. Eric Sunshine provides a minor typo correction to Taylor Blau's patch that adds `--end-of-options` protection throughout Gitweb's Git command invocations.

The patch being corrected systematically prevents malicious URL parameters from being interpreted as Git command options by inserting `--end-of-options` before any user-controlled input. The specific example shown demonstrates how a crafted `hpb` parameter could previously write arbitrary files via `--output` injection.

Eric's contribution is purely editorial - fixing a typo in the commit message ("agains" -> "against"). The technical content and security approach remain unchanged from Taylor's original patch, which already received positive indications in the thread. This suggests the fix is progressing toward acceptance with only minor polish needed.

Key technical elements remain:
- Uses Git's existing `--end-of-options` feature (from 51b4594b40)
- Protects all Gitweb operations that spawn Git subprocesses
- User-controlled parameters include `hpb`, `hash_base`, `file_name` and others
- Follows consistent safety pattern across Gitweb.pm

The thread shows consensus forming around this being a comprehensive fix for the demonstrated vulnerability, with Eric's typo fix being the only outstanding change needed before potential merging.