Thread identity: Discussion about deprecating `git whatchanged` and improving its replacement in `git log --raw`, initiated by a user (jorg@ml1.net) concerned about UX of the transition path.

Core problem: The standalone `git whatchanged` command is being deprecated in favor of `git log --raw`, but this transition presents discoverability and intuitiveness issues. While `--raw` is the functional equivalent, its name doesn't clearly communicate the "what changed" functionality. The user specifically objects to the `--raw` name rather than the deprecation itself, proposing `--whatchanged` as a more intuitive alias.

Current status: Junio C Hamano has confirmed `--raw` as the current recommended alternative while acknowledging its UX shortcomings. The deprecation timeline remains unspecified. Alternative output formats exist (`--name-only`, `-p`, `--compact-summary`), with Hamano personally preferring `--compact-summary`. The user's specific proposal to rename `--raw` to `--whatchanged` is now on the table.

Key technical details:
- `git log --raw` produces output compatible with `whatchanged`
- Other format options available for different detail levels
- Core functionality preserved across these options
- User analysis: `--raw` suggests "less refined output" rather than file-change information
- Proposed solution: add `--whatchanged` as an alias for `--raw`

Open questions:
- Whether to implement `--whatchanged` alias for better discoverability
- How to document/signpost the transition
- Whether deprecation should proceed without a more intuitive alternative name
- Whether the team considers the naming argument compelling

Participant positions:
- Original user (jorg@ml1.net) strongly advocates for `--whatchanged` alias based on UX grounds
- Junio C Hamano maintains neutral stance from initial response, awaiting further discussion
- No other participants have weighed in yet on the specific rename proposal

Related work: Connects to broader CLI UX and documentation efforts, though not tied to specific initiatives. The discussion may inform future deprecation transitions. The narrow focus on naming semantics distinguishes it from technical implementation debates.