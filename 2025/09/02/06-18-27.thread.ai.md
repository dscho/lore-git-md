Thread identity: Discussion about deprecating `git whatchanged` and improving its replacement in `git log --raw`.

Core problem: The standalone `git whatchanged` command is being deprecated in favor of `git log --raw`, but this transition path is not intuitive to users. The `--raw` option doesn't clearly communicate the "what changed" functionality that `git whatchanged` provided.

Current status: `git whatchanged` is in the process of being deprecated, with no clear timeline mentioned. The replacement (`git log --raw`) exists but may not be sufficiently discoverable or intuitive.

Key technical detail: Proposal to add a `--whatchanged` option to `git log` as a more logical and discoverable alternative if the standalone command is removed. This would maintain functionality while improving the transition path for users.

Open question: Whether to implement `--whatchanged` as an alias for `--raw` in `git log` to make the transition more user-friendly. No maintainer or reviewer positions stated yet.

Participant positions: Initial request comes from a user perspective focusing on UX considerations, with no other participants yet engaged in the thread. The tone is constructive but doesn't reference prior discussions about the deprecation.