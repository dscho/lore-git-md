This is a user-facing feature request regarding the deprecation of `git whatchanged`. The email comes from a user who finds the current transition path from `git whatchanged` to `git log --raw` unintuitive. They argue that "--raw" doesn't clearly communicate the "what changed" functionality to users, and suggests adding a `--whatchanged` option to `git log` as a more logical alternative if the standalone command is to be removed.

The email doesn't reference any specific prior discussion about deprecating `git whatchanged`, but implies this is an ongoing change (note the conditional "if it will be abandoned"). The core of the request is about improving the discoverability and intuitiveness of the replacement functionality, with the key technical detail being the proposed addition of a `--whatchanged` option to `git log`.

This touches on Git's command-line interface design principles and backward compatibility considerations. The request appears to come from a genuine user perspective rather than proposing implementation details, focusing on the user experience aspect of the transition away from the deprecated command. The tone is constructive though brief, and doesn't reference any specific mailing list discussions about the deprecation process.