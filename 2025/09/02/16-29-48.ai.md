This is a response in the ongoing discussion about deprecating `git whatchanged` and improving its replacement in `git log --raw`. Junio C Hamano clarifies the rationale behind recommending `--raw` as the current alternative to `whatchanged`, while acknowledging it may not be the most intuitive choice.

The email explains that `--raw` is suggested because it produces output compatible with `whatchanged`, though Hamano notes there are better options depending on the level of detail desired (`--name-only`, `-p`, or his personal preference `--compact-summary`). The response maintains a neutral stance, neither advocating for keeping `whatchanged` nor pushing for immediate deprecation, but rather explaining the current state of alternatives.

Key technical points include:
- Confirmation that `--raw` is the direct functional equivalent to `whatchanged`
- Mention of alternative output formats (`--name-only`, `-p`, `--compact-summary`)
- Implicit acknowledgment of the discoverability/UX concern raised in the original email

The response doesn't directly address the proposal to add a `--whatchanged` option to `git log`, but does validate the core concern about `--raw` not being an intuitive replacement name. The tone suggests openness to discussion while maintaining the current recommendation as technically correct, if not perfectly user-friendly.