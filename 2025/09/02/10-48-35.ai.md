This is patch 10/15 in Patrick Steinhardt's packfile store refactoring series, continuing the methodical separation of packfile management from the object database. The patch refactors `install_packed_git()` to work directly with the packfile store rather than a repository object, renaming it to `packfile_store_add_pack()` to better reflect its purpose.

The changes update 7 files with straightforward mechanical conversions of all call sites to use the new function signature. The core logic remains identical - adding a packfile to the store's linked list and hashmap - but now operates directly on the packfile store rather than going through the repository's object store. Documentation comments are updated to match the new function name.

This patch follows the established pattern seen in previous patches of the series: maintaining identical external behavior while improving internal structure. It builds on the foundation laid in patch 8 (which refactored `prepare_packed_git()`) and patch 9 (which split `reprepare_packed_git()`), completing another step in the series' goal of cleanly separating packfile management from the object database.

The changes are behavior-preserving with no test modifications needed. The patch continues the series' consistent approach of methodical, focused refactoring while maintaining all existing functionality. It sets up for the remaining work in patches 11-15, which will handle MRU list standardization, helper functions, and final cleanup.