This is a minor documentation formatting fix in the now-merged `git-checkout` documentation series. Kristoffer Haugsbakk points out that some variable references in the man page were incorrectly rendered with both backticks and underscores (like `` `_<branch>__` ``), which affects the final output formatting. The fix simply removes the redundant backticks while keeping the semantic underlines for variable names.

The change is purely presentational and doesn't alter any technical content - it just ensures the documentation renders correctly. Since this is a follow-up to an already-approved series, it's likely to be quickly accepted as a cleanup. The thread context indicates this documentation effort has already gone through multiple thorough review cycles, so this appears to be catching one last minor formatting issue that slipped through.