This is a documentation refinement for the already-approved stash.index feature series. Phillip Wood and D. Ben Knoble are finalizing the interaction between the new stash.index config and --autostash operations (used by merge/pull/rebase). 

The core functionality was already implemented and tested in previous patches - this is just ensuring the documentation properly reflects how the config affects autostash behavior. Phillip suggests adding this to the stash.index documentation but isn't sure if it needs to be called out in each command's --autostash documentation as well.

The exchange shows the kind of thoughtful polish that happens right before a series gets merged - catching subtle interactions that weren't initially considered but don't require code changes. The maintainer has already approved the series, so this is just dotting the i's on the documentation. 

The patch series as a whole solves a real pain point where users lose carefully staged changes when applying stashes, while maintaining backward compatibility through the config option. This final documentation tweak ensures users understand the full scope of the new behavior.