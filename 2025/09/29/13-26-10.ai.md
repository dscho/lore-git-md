Documentation patch (standalone markdown formatting fix) in the context of a larger Rust FFI implementation series.

Phillip Wood confirms Junio Hamano's earlier suggestion that this documentation fix for `po/README.md` should be split out from the Rust configuration parsing series. The patch removes unnecessary backslash escaping of underscores in language code examples (like changing `ll\_CC` to `ll_CC`), which was overly cautious markdown formatting. Phillip provides the exact rebase command (`git rebase --onto HEAD~3 HEAD~2`) to help the author extract this change from the series.

The change itself is straightforward and uncontroversial - it simply corrects markdown syntax in documentation examples while maintaining identical rendered output. As noted in the thread summary, this documentation fix could be applied independently of the Rust FFI work. The patch appears ready to merge as-is if submitted separately, with no outstanding technical issues or review feedback needing resolution. This represents a clean separation of concerns from the main series' focus on Rust FFI implementations for configuration parsing.