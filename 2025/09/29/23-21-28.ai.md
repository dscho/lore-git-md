This is a code review comment on patch 12/49 in Taylor Blau's major repack.c refactoring series. Patrick Steinhardt suggests using C99 designated initializers for the `config_ctx` struct initialization, similar to feedback he gave on an earlier patch in the series.

The patch moves repack configuration handling into a new `repack_config_ctx` struct that contains pointers to both regular and cruft pack-objects configurations. Patrick's suggestion is purely about coding style - replacing the `memset()` with a more modern initialization pattern. This is consistent with his previous review style and doesn't indicate any functional concerns with the change.

The thread context shows this is part of a larger effort to modularize repack.c while removing global variables, with Patrick providing constructive code review throughout. His feedback here is minor and non-blocking, focused on maintaining consistent style across the series. The core technical approach of passing both pack-objects configurations via a context struct remains uncontested.