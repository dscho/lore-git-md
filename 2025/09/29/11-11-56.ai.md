Bugfix targeting the new `git reflog write` command. This email reports that the command fails to honor local `user.name` and `user.email` configuration values, instead falling back to system defaults. The reporter provides a clear reproduction case showing the command works correctly when environment variables are set but not when relying on config values.

The issue appears in the reflog entry authorship information, where the command uses system values (Michael <michael@mycomp.local> in the example) rather than the configured values (C O Mitter <committer@example.com>). The reporter reasonably expects config values to take precedence over system defaults, similar to other Git commands.

This seems like an oversight in the new `reflog write` implementation rather than intentional behavior. The reporter notes they lack C expertise to investigate `git_committer_info` internals, but the problem appears straightforward - the command isn't properly consulting the config hierarchy for committer information before falling back to system defaults.

The email doesn't propose a specific fix but raises an important usability issue for the new feature. Since `reflog write` is meant to create entries that would match those written by other Git operations, this inconsistency could cause confusion in tools that parse reflog data. The report is well-formatted with clear reproduction steps and expected/actual output, making it easy to verify.