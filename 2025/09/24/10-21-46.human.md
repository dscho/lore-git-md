This is the final patch in a completed refactoring of xdiff internals for Rust compatibility. Phillip Wood provides positive feedback response to Ezekiel Newren's change converting the `changed` field from `char` to `bool` in the xdiff data structures.

The patch is a straightforward type safety improvement that makes the code's intent clearer - the field was already being used as a boolean (only storing 0/1 values), so the change to `bool` better documents that intent. Phillip notes this separation of concepts (from the nearby `matches1`/`matches2` arrays that still use numerical values) makes the logic more understandable compared to the previous version.

This concludes a multi-patch series that has systematically modernized xdiff's internals through structural simplifications, naming improvements, and type safety enhancements while maintaining identical behavior. The series is now complete and ready for merging.