This is a refactoring patch that's part of the `git repo stats` series (1/6). It renames the `repo_info()` function to `cmd_repo_info()` to follow Git's convention of prefixing subcommand entry points with `cmd_`. The change is purely mechanical - it updates the function declaration, definition, and the OPT_SUBCOMMAND registration point in `builtin/repo.c`.

The patch is straightforward with no behavior changes, just a naming convention alignment. It serves as preparatory work for the larger series introducing repository statistics functionality. The modification touches only 3 lines in a single file, all within the same function context. This appears to be a clean, uncontroversial change that establishes consistent naming before adding new functionality in subsequent patches.

Key technical details for future reference:
- Function renamed from `repo_info()` to `cmd_repo_info()`
- Location: `builtin/repo.c`
- Part of establishing consistent command entry point naming
- No functional impact - purely a code organization change
- Follows established Git convention for subcommand functions