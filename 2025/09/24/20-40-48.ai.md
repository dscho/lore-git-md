This is the final patch in a feature series adding configurable index restoration to `git stash`. The email is a reply to Phillip Wood confirming the impact on `--autostash` behavior and discussing documentation updates.

The core functionality is already implemented and tested in previous patches, with this final exchange addressing documentation polish. The author agrees with Phillip Wood's observation that the new `stash.index` config will also affect `--autostash` operations (merge/pull/rebase), which wasn't previously called out in the commit message. They discuss whether this interaction should be documented, suggesting the series may need one final documentation tweak before merging.

Key technical points from context:
- `stash.index` config now affects all stash restoration paths including `apply`, `pop`, and `--autostash`
- Default remains false (no index restoration) for backward compatibility
- `--no-index` flag provides opt-out when config is true
- Test coverage is comprehensive including isolated test cases
- Code organization improvements were made in preparatory refactoring

The thread shows all technical concerns have been addressed, with maintainer approval already given. This final discussion is purely about ensuring the documentation fully captures the behavior, particularly the less-obvious `--autostash` interaction. The author appears ready to make any final documentation updates needed before the series is merged.