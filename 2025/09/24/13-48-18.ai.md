This is a discussion about alternative approaches to handling author metadata in rebase operations, specifically in response to the proposed `fixup -C` behavior change.

Phillip Wood responds to Johannes Sixt's counter-use-case about misordered commits that need to act as fixups for later commits. Phillip acknowledges Johannes' point about conflict avoidance being a key factor in keeping commits ordered, which wasn't clear from the original patch. 

The email suggests two alternative solutions to the metadata control issue: either extending the `squash` command with metadata specification options, or adding a new `--use-author` flag to `fixup` rather than changing its default behavior. This positions itself between Junio Hamano's stance on preserving original authorship and Johannes' use case needing metadata control.

Key technical points:
- Recognizes conflict avoidance as motivation for original proposal
- Proposes `squash` extension as alternative path (aligning with Johannes)
- Suggests `--use-author` flag as compromise solution
- Implicitly accepts Junio's design principle while seeking solution for Johannes' use case

The discussion continues exploring ways to satisfy the misordered commit scenario without violating the maintainer's design principles for `fixup` operations. The alternatives maintain the original author preservation while providing escape hatches for special cases.