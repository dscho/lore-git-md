This is a final refinement in the xdiff internals refactoring series, specifically addressing a documentation suggestion from Phillip Wood. The patch removes temporary variable assignments (`rchg1` and `rchg2`) that were aliasing the `rchg` fields in `xdf1` and `xdf2` structs, instead accessing the fields directly. Phillip Wood notes that the commit message should explain the rationale for this change, though the technical content itself is straightforward - it's a layer of indirection that's no longer needed in the refactored code.

The change is part of the larger effort to modernize xdiff's internals for Rust compatibility, where this particular patch follows through on the earlier conversion of the `rchg` field to a boolean type (now accessed directly rather than through temporary variables). The diff shows the mechanical change in `xdiff/xdiffi.c` where the temporary variables are eliminated in favor of direct struct member access, with no functional impact. This appears to be a final polish on the series, with Phillip Wood's feedback being the last remaining item before the changes can be considered complete.