This is a minor test infrastructure refinement in the SHA-1/SHA-256 interoperability series. Patrick Steinhardt suggests squashing patch 9/9 (which adds a `COMPAT_HASH` test prerequisite) into the previous patch, as it's a small follow-up to the test changes introduced there. The patch itself is straightforward - it adds a way to mark tests that should be skipped when running with a non-default hash, and adjusts the `BROKEN_OBJECTS` prerequisite to account for this case. 

The suggestion comes as part of the final polishing phase of this well-reviewed series, where all major technical decisions have been settled. Patrick's review continues the thread's pattern of careful attention to detail, though this particular note is more about commit organization than substantive changes. The series appears to be nearing completion with only minor documentation and test infrastructure refinements remaining.