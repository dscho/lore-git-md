This is a response in the ongoing discussion about the new `git refs get` subcommand, specifically addressing Junio Hamano's suggestion to extend `git show-ref` instead of creating a new command.

Patrick Steinhardt defends the decision to create a new subcommand under `git-refs`, arguing that while it may initially be less discoverable than `show-ref`, it will become more natural over time as users familiarize themselves with the `git-refs` family of commands. He positions this as completing the reference-handling functionality within `git-refs`, which already contains other reference-related operations.

The response maintains the original design philosophy of keeping reference reading functionality grouped with other reference operations rather than in `show-ref`, but does not directly address Junio's specific technical suggestion of a `--no-deref` option. This keeps the discussion open about whether the functionality should live in a new command or be added to an existing one, with Patrick advocating for the former approach based on conceptual organization rather than immediate discoverability.

Key technical points remain unchanged from the original proposal: the command provides strict reference reading without DWIM behavior and handles symref targets directly. The thread now has clear positions from both sides regarding command organization, with Patrick's response reinforcing the architectural reasoning behind the new subcommand approach.