Documentation fix for the SHA-1/SHA-256 interoperability series. This is a follow-up to patch 5/9 in the series, which added documentation for loose object storage but missed adding the new `gitformat-loose.adoc` file to both the Makefile and meson build systems.

Patrick Steinhardt confirms Junio Hamano's meson.build fix is correct, but notes the same addition is needed in the Makefile. He provides a complete diff that adds the new documentation file to both build systems' lists of manpage sources. This is a straightforward build system alignment change that should be uncontroversial.

The change is small but important for ensuring the new documentation is properly built and installed. It's part of the broader SHA-1/SHA-256 interoperability work, though this particular patch is purely about build system maintenance rather than the hash algorithm changes themselves.