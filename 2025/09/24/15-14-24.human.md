This is the final patch in a refactoring series modernizing xdiff internals for Rust compatibility. The change converts the `xdfile_t.changed` field from `char` to `bool` since it only ever holds 0/1 values, making the code's intent clearer. 

The patch builds on earlier work that introduced enum-like macros (NONE/SOME/TOO_MANY) for similarity matching states, and now completes the type safety improvements by properly distinguishing between boolean flags (changed) and enumerated states (matches). Phillip Wood reviewed positively, noting the improved clarity over the v4 approach.

This concludes a multi-phase effort that has systematically eliminated redundant structures, improved type safety, and modernized xdiff's internals while maintaining identical runtime behavior. All feedback has been addressed, including documentation of the rationale for various changes (like removing temporary variable aliases) and explicit explanations of field purposes. The series appears ready for merging.