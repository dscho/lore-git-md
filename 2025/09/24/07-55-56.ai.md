This is a minor review comment on patch 6/9 of the SHA-1/SHA-256 interoperability series, focusing on test prerequisites.

Patrick Steinhardt questions whether the `RUST` prerequisite is needed for a test case in `t1500-rev-parse.sh` that checks the `--show-object-format=compat` functionality. The test was introduced by brian m. carlson as part of adding compatibility hash format support to `git rev-parse`. Patrick observes that the test content doesn't appear to have any Rust-specific dependencies that would justify the `RUST` marker.

This appears to be a potential oversight in test setup rather than a substantive issue with the feature implementation. The test exercises core Git functionality for reporting hash algorithms and doesn't involve Rust-specific code paths. The comment maintains the thread's collaborative tone while catching what seems to be an unnecessary test constraint.

The exchange shows Patrick's attention to detail in reviewing test infrastructure aspects of the series, even as the main focus remains on the SHA-1/SHA-256 interoperability changes. This follows the pattern of thorough but constructive review seen throughout the thread.