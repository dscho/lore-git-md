[PATCH] diff-no-index: fix buffer overflow with trailing slash paths

Thread tracks a memory safety and performance improvement for `git diff --no-index` directory comparison when pathspecs end with '/'. Originated from Johannes Schindelin's bug report identifying a buffer overflow in pathspec handling. Jacob Keller's solution replaced integer-based path matching with `strbuf` operations in `diff_no_index.c`, fixing both overflow and redundant string copying while adding test coverage in `t4053-diff-no-index.sh`.

Current state incorporates Junio Hamano's review feedback confirming a memory leak in error paths via SANITIZE=leak testing. The fix adds `strbuf_release()` calls for `ps_match1` and `ps_match2` at the `out:` label, maintaining the original improvements while ensuring proper resource cleanup. Changes remain focused on `diff-no-index.c` with no behavioral impact beyond fixing the leak.

Key technical details: `strbuf` structs replace raw integer offsets (`skip1`/`skip2`), `read_directory_contents()` modified to use pre-initialized buffers, `queue_diff()` maintains paths recursively. Test coverage verifies trailing slash handling without regressing existing functionality.

Resolution status: Maintainer indicates readiness to merge, either incorporating the leak fix directly or having author squash it. Credits Johannes Schindelin for initial report and Jacob Keller for comprehensive solution. Final changes satisfy both immediate safety requirements (overflow/leak fixes) and longer-term code quality goals (eliminating redundant string operations). No outstanding questions or requested changes remain.