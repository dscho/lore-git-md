This is a follow-up in the thread about month numbering in `git log` output, confirming the existing behavior is correct and suggesting alternatives for custom formatting.

Jeff King (peff@peff.net) responds to the original report about 1-based month indexing, clarifying that the current output matches standard date representation conventions. He notes that while locales might theoretically support alternative numbering schemes through strftime's `%c` option, this would be unconventional for date display. The response focuses on providing practical solutions for users who prefer different date formats, pointing to Git's existing `--date` formatting options that allow customization via strftime specifiers.

Key technical details include the mention of strftime's `%c` format specifier and the specific example command showing how to use `git log --date=format:` with custom strftime patterns (`%A` for weekday, `%Y-%m-%d` for ISO-like date, etc.). The email reinforces the thread's consensus that the current 1-based month display is correct while offering workarounds for formatting preferences. No code changes are proposed - the response serves as documentation of existing functionality and closes the discussion by addressing the underlying need (custom formatting) rather than the originally proposed solution (changing month numbering).