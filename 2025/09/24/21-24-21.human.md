Refactoring patch (1/6) in the `git repo stats` series.

This is a straightforward rename of the `repo_info()` function to `cmd_repo_info()` in `builtin/repo.c`, bringing it in line with Git's convention of prefixing subcommand entry points with `cmd_`. The change is mechanical - just updating the function declaration and its single call site in the subcommand options table.

As the first patch in a 6-part series introducing repository statistics functionality, this appears to be preparatory work to establish consistent naming before adding new commands. The change is small (3-line diff) and low-risk, with no behavior modifications. The commit message is brief but clear about the motivation being adherence to project conventions.

This patch has already gone through review cycles as part of the larger series, and the simple rename seems uncontroversial. It sets up the naming pattern that will presumably be followed by the new `repo stats` subcommand being introduced in later patches.