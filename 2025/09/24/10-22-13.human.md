This is a minor documentation suggestion in the final stages of the xdiff refactoring series. Phillip Wood notes that patch 10/13 in the v5 series, which removes temporary variable aliases for the `rchg` fields in `xdl_build_script()`, should include an explanation in the commit message about why this change is being made. The change itself is straightforward - it eliminates temporary variables that were aliasing the `rchg` fields (now renamed to `changed` in previous patches) and accesses the struct fields directly instead. 

This comes as the series is nearly complete, with all major structural simplifications already done and most feedback addressed. Phillip's comment appears to be a final polish request rather than a substantive concern, as the technical changes in this patch are already sound and consistent with the series' goals of removing unnecessary indirections and improving code clarity. The series as a whole has been through multiple rounds of review and is otherwise ready for merging.