This is a discussion about potential alternatives to modifying `git rebase -i`'s fixup behavior, following Junio Hamano's rejection of the original patch.

Phillip Wood responds to Johannes Sixt's explanation of a use case where the proposed fixup behavior would be useful - when reordering commits that were originally fixups for later commits would cause conflicts. Phillip acknowledges this as a valid scenario that wasn't clear from the original patch's description.

The discussion is now pivoting toward alternative solutions that wouldn't change fixup's core semantics. Johannes suggested extending the `squash` command with metadata control options, while Phillip proposes adding a `--use-author` option to `fixup` as another possible approach.

The exchange shows contributors working within the maintainer's design constraints (preserving fixup's original authorship behavior) while still trying to address real workflow needs. The thread remains focused on finding a solution that satisfies both the principle of fixup operations and the practical case of handling misordered commits with conflicts.