This is a discussion about the proposed `git refs get` plumbing command, specifically addressing whether it should support DWIM (Do What I Mean) behavior. Patrick Steinhardt responds to Ben Knoble's suggestion about potentially adding a `--dwim` flag to the new command in the future.

The exchange highlights a key tension in the design: Ben had questioned whether completely removing DWIM expansion is beneficial for scripting use cases, noting that `rev-parse`'s DWIM behavior can simplify handling user input in scripts. He raised concerns about interface fragmentation and suggested a `--dwim` flag as a potential compromise.

Patrick's brief response indicates openness to the idea of adding DWIM support via a flag in the future, though he doesn't commit to any specific implementation. This keeps the door open for potential evolution of the command's behavior while maintaining the initial strict, non-DWIM approach.

The discussion remains unresolved but shows the maintainers are considering how to balance strict reference reading (the original goal) with practical scripting needs that might benefit from some DWIM behavior. This positions the thread for further discussion about whether and how to implement such flexibility in the new command.