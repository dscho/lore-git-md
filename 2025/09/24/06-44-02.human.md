This is a technical follow-up email about two ongoing patch series related to Git's object database (ODB) internals, pointing out an ordering issue in the integration branches.

SZEDER Gábor notes that the merge of 'jt/odb-transaction' into Junio's 'jch' branch introduces a call to `odb_reprepare()` in `object-file.c`, but this function doesn't exist yet in that branch - it will only be added later when 'ps/packfile-store' is merged. This creates a build-time dependency between the two series that wasn't properly sequenced. The email also observes that while the merge caused a conflict in 'odb.h', it surprisingly didn't trigger one in 'object-file.c' where the problematic call appears.

The two series in question are:
1. 'jt/odb-transaction' (7 commits) which builds transaction support for bulk object creation
2. 'ps/packfile-store' (16 commits) which refactors packfile storage internals

This appears to be a routine integration coordination issue that maintainers need to resolve by adjusting the merge order or adding temporary scaffolding. The email serves as a heads-up about the technical dependency between these two substantial refactoring efforts touching core object storage code.