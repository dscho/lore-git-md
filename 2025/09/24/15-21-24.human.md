This is a follow-up discussion in the ongoing thread about `git rebase -i`'s fixup behavior with the `-C` flag. Mathias Rav responds to the debate around whether fixup operations should preserve the original author information or take it from the current commit.

The email provides context about Mathias's personal workflow - they previously used a custom shell script to achieve the "fixdown" behavior (where a later commit is fixed into an earlier one) before discovering the `-C` flag. They share their simple implementation (a 2-line shell script that combines `cherry-pick -n` with `commit --amend -nC`) and note they're willing to revert to this approach if the proposed change isn't accepted.

Key points from this message:
- Mathias acknowledges Junio's design concerns about preserving the meaning of "fixup"
- They reveal they weren't previously aware of the "amend!" autosquash feature
- The email provides a concrete workaround for users who need the alternative behavior
- Mathias seems accepting of the community direction while sharing their personal use case

This moves the discussion forward by showing a practical solution exists outside core Git, potentially reducing the urgency of changing the default behavior while still documenting the use case. The tone is constructive, focusing on sharing information rather than pushing for the original proposal.