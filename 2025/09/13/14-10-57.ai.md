This is a technical refinement in the command deprecation framework series, specifically addressing alias recursion detection. The patch moves command tracking logic from `run_argv()` into `handle_alias()` as suggested by Jeff King, preparing for the subsequent alias-shadowing of deprecated commands.

The change relocates the `cmd_list` bookkeeping to better handle recursion cases that will arise when deprecated commands can be shadowed by aliases. While functionally similar, it does alter error output slightly by showing one additional alias expansion line before detecting cycles. The commit message notes this isn't purely a refactor due to the changed output behavior.

Key technical points:
- Moves alias cycle detection from `run_argv()`'s `while(1)` loop into `handle_alias()`
- Prepares for upcoming deprecated command shadowing by ensuring recursion checks happen at alias handling time
- Changes error output format (shows one more expansion step before fatal error)
- Maintains all existing functionality while enabling future work
- Follows Jeff King's architectural suggestion from earlier in the thread

The patch appears well-justified and aligns with the maintainer-approved direction for the series. It addresses a specific technical requirement (proper recursion detection when deprecated commands are involved) while keeping the changes focused and minimal.