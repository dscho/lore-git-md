Here's the updated human-readable thread summary:

This thread investigates a `safe.directory` security feature failure in shared repository scenarios. The original reporter described a bare repository setup with group ownership (root:git, mode 2770/660) where the documented workarounds failed to prevent "dubious ownership" errors during clone operations across Git versions 2.39.5 and 2.51.0. Initial attempts to bypass the check via global config, CLI overrides, and wildcard patterns all proved ineffective.

Carlo Arenas Belón has since clarified that the error actually originates server-side during remote repository access, not from the local clone operation. This means the client-side `safe.directory` configurations were being applied in the wrong context - the security check failing is happening on the server Git process. The suggested workaround now shifts to configuring `safe.directory` on the server itself after logging in as the same user.

While this explains why previous attempts failed, the core question remains: why is the server-side Git rejecting what appears to be a properly configured shared repository? The discussion has narrowed the problem's location but hasn't yet uncovered why the security check triggers in this specific shared-access scenario. The next steps would likely involve examining the server's Git configuration and environment to understand what ownership conditions are being rejected.