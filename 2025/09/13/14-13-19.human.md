This is the second patch in a series extending `--ignore-other-worktrees` support from `git checkout` to `git rebase`. The patch adds the option to `git rebase`, allowing users to override Git's safety check that prevents rebasing a branch that's checked out in another worktree.

The implementation follows the same pattern as checkout's existing behavior: when the option is given, rebase will proceed even if the branch is in use elsewhere. The patch adds documentation for the new option, updates the rebase command to accept the flag, and modifies the branch-checking logic to respect it. A test case confirms the new behavior works as expected.

The changes are straightforward and well-contained, building on the infrastructure work done in the first patch of the series. The test coverage is minimal but sufficient to verify the basic functionality. This appears to be a clean extension of existing behavior that maintains backward compatibility while giving users more flexibility in their worktree workflows.

As noted in the thread context, this is part of a larger effort to improve migration for users accustomed to older 'workdir' script behavior. The patch series is from a new contributor who has clearly studied the existing code and followed project conventions in their implementation.