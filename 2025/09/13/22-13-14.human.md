This is a clarification in the ongoing thread about `safe.directory` failing in shared repository scenarios. Carlo Arenas Belón points out that the error message about dubious ownership is actually coming from the Git command running on the remote server (`my.server`), not the local clone operation. 

The key insight is that the security check is happening server-side during the clone operation, which explains why local `safe.directory` configurations aren't having any effect. Carlo suggests testing whether the same error occurs when running Git commands directly on the server via SSH, which would help isolate whether this is purely a server-side configuration issue.

This response shifts the focus of the problem from being about `safe.directory` not working to being about where exactly the ownership check is being enforced. It's a helpful clarification that could lead the original reporter to test the right configuration on the server rather than the client. The email is brief but provides important technical context that wasn't clear in the original report.