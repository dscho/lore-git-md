This is a technical refinement to the command deprecation framework series, specifically addressing alias handling architecture. Kristoffer Haugsbakk responds to Jeff King's suggestion by moving the command bookkeeping logic into `handle_alias()` as a preparatory change before implementing deprecated command shadowing.

The patch relocates the seen-command tracking from the main loop in `git.c` to `handle_alias()`, which changes the error output slightly (adding an extra alias expansion line in cycle detection cases). The change isn't purely a refactor since it affects output, but it creates better architectural positioning for the deprecated command handling that will follow.

The commit message clearly explains the motivation - this reorganization is necessary to properly handle recursion detection when deprecated commands are involved. The implementation has been validated against existing test cases, though the error message output now shows one additional line during alias cycle detection (a minor behavioral change deemed acceptable).

This appears to be a well-considered change that follows through on Jeff King's architectural suggestion from earlier in the thread. The patch maintains all existing functionality while setting up cleaner foundations for the deprecated command shadowing feature to come in subsequent patches.