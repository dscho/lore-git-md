This is a user experience improvement for gitk, Git's graphical repository browser. The patch makes the "Tags and Heads" popup window remember its size and position between sessions, saving users from having to reposition it each time they open it.

Currently, the window always opens at a default position. The change stores the window geometry (size and position relative to the main gitk window) in the config file under `geometry(showrefs)`. The position is stored as an offset from the main window, so it maintains the same relative placement even if the main window moves or if gitk is opened on a different monitor setup.

The implementation adds several new Tcl procedures to handle parsing window geometry, saving/restoring the position, and properly cleaning up when the window closes. The changes look thorough, with attention to edge cases like keeping the window on-screen and properly unbinding event handlers. The 73-line diff is focused entirely on this one feature with no unrelated changes.

This seems like a straightforward quality-of-life improvement for gitk users who frequently use the Tags and Heads view. The patch appears well-constructed with proper geometry calculations and screen boundary checks.