Bug report about `safe.directory` security feature not working as documented. The user reports that despite following Git's suggested workaround to add a repository path to `safe.directory` in their global config, they still encounter the "dubious ownership" error when cloning a shared repository. The issue occurs across Git versions 2.39.5 and 2.51.0.

Key technical details:
- Repository setup: Shared bare repo on remote server with group permissions (root:git, mode 2770/660)
- Error occurs during clone operation with the same message suggesting `safe.directory` config
- Attempted solutions (all failed):
  - Adding path to global `safe.directory`
  - Using `-c safe.directory` CLI option
  - Trying to set `safe.directory` to "*"

The report suggests this is a regression in behavior that breaks existing workflows, with the user requesting either a fix for the `safe.directory` mechanism or information about which version introduced this security check so they can downgrade. The tone indicates frustration with what they perceive as a breaking change introduced without proper opt-in mechanism.

This appears to be a genuine bug report about the `safe.directory` feature not functioning as documented, potentially affecting users with shared repository setups. The report provides clear reproduction steps and environment details, making it actionable for maintainers. The mention of the issue persisting across multiple Git versions suggests it's not a simple version-specific regression.