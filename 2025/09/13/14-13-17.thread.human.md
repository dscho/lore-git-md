Gabriel Scherer's patch series improving worktree safety checks is nearing completion, with the discussion now focused on final polish to the advice messages shown when operations fail due to branch conflicts. The series adds new warnings and options for both `git checkout`/`git switch` and `git rebase` when they encounter branches checked out in other worktrees.

In response to Kristoffer Haugsbakk's review, Gabriel has refined the advice message in patch 1/3 to better distinguish between using `--ignore-other-worktrees` (which bypasses the safety check) and `--detach` (which creates a detached HEAD). The new version makes the primary recommendation clearer while keeping the detach suggestion as a secondary hint. This addresses Kristoffer's concern about potential user confusion between the two options.

The core functionality remains solid, with tests covering both checkout and rebase scenarios. Gabriel is also working on Kristoffer's other suggestions about enum ordering and commit message formatting. With these messaging refinements nearly settled, the series appears ready for final acceptance once the remaining polish is applied. The changes demonstrate Git's attention to both technical correctness and clear user guidance when handling multi-worktree scenarios.