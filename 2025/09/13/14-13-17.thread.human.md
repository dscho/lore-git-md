This thread introduces a series from a new contributor improving how Git handles branch conflicts across multiple worktrees, particularly for users migrating from older 'workdir' scripts. The initial patch added an advice message when checkout/switch operations fail due to branch conflicts, suggesting `--ignore-other-worktrees` or `--detach` as workarounds. It introduced new advice infrastructure and refactored the underlying branch-checking functions.

The second patch extended this behavior to `git rebase`, adding documentation and test coverage while maintaining the same safety defaults - operations still fail by default unless explicitly overridden with `--ignore-other-worktrees`. The latest patch now completes the rebase integration by adding the same advice message seen in checkout, providing a consistent user experience across both commands. The implementation builds on the earlier refactoring while keeping changes minimal and focused.

The series has progressed cleanly, with each patch building on the last while maintaining backward compatibility. Remaining work includes potential future additions like a config option (`branch.ignoreOtherWorktrees`) and expanded test coverage, but the core functionality is now in place. The author has followed project conventions well for a new contributor, with changes spanning advice infrastructure, branch checking logic, command implementations, and documentation.