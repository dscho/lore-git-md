This is a follow-up discussion in the worktree/rebase patch series about test coverage and potential future work. Gabriel Scherer responds to Ben Knoble's review of their patchset that extends `--ignore-other-worktrees` to `git rebase`.

Gabriel confirms that checkout behavior is already tested in t/t2060-switch.sh (though it doesn't verify the new advice messages) and notes they manually verified the hint messages appear correctly. They point out that while `worktree add` also uses the same underlying check, it wasn't modified in this series since it already has `--force` for similar functionality. 

The email raises two potential future directions that could be pursued separately: adding advice messages to `worktree add` failures, and potentially introducing a `--ignore-other-worktrees` option to `worktree` commands for consistency (though noting this would be mostly redundant with existing `--force`). Gabriel leaves the decision about whether to include any `worktree.c` changes in the current series up to further discussion.

The exchange shows the patch series is in good technical shape, with the discussion now focusing on test completeness and whether to expand the scope of changes to related commands. The core functionality appears ready, with any additional work being optional follow-ups.