Bugfix investigation regarding mergetool variable passing. Johannes Sixt responds to Randall Becker's report about missing environment variables in custom mergetool scripts, clarifying a potential documentation misunderstanding.

The key clarification is that the documentation refers to shell variables, not environment variables, which explains why they don't appear in `env` output. Johannes points to the mergetools/ directory scripts as examples, noting they are shell code fragments (not executable scripts) that would receive these variables directly in their scope rather than through the environment.

This suggests the behavior may be working as intended, though the documentation could potentially be clearer about the distinction between shell variables and environment variables. The response doesn't address whether the variables are actually be set correctly in the shell context, just that they wouldn't appear in the environment as Becker expected.

Technical details:
- Variables in question: BASE, LOCAL, REMOTE, MERGED
- mergetool implementation uses shell variable passing, not environment variables
- Reference implementation examples in mergetools/ directory
- Scripts are non-executable shell fragments without shebang lines

This moves the discussion toward whether the current behavior matches documentation's intent, and whether the documentation should be clarified to prevent similar confusion. The platform-specific aspects Becker observed may be irrelevant if the fundamental issue is about variable passing mechanism rather than environment setup.