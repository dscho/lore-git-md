This is a clarification about mergetool variable behavior in response to a reported issue. Johannes Sixt points out that the documentation describes shell variables" rather than "environment variables" when discussing the mergetool invocation. He notes that the built-in mergetool scripts in Git's mergetools/ directory are shell code fragments that don't use environment variables, suggesting the variables are meant to be shell variables available during command evaluation rather than exported to the child process's environment.

The response implies this may be working as designed rather than a bug, though it doesn't directly address whether the documentation could be clearer about this distinction. The observation about the non-executable nature of the built-in mergetool scripts serves as supporting evidence for the intended behavior.

This moves the thread forward by clarifying the technical mechanism (shell variables vs environment variables) but leaves open whether the documentation should be updated to prevent similar confusion. The response comes from an experienced contributor familiar with Git's mergetool implementation.