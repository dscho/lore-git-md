Thread summary:

Bug report about `safe.directory` security feature failing in shared repository scenarios. Original reporter described a bare repository setup (root:git, mode 2770/660) where `safe.directory` workarounds failed during clone operations across Git versions 2.39.5 and 2.51.0. Carlo Arenas Belón clarified that the "dubious ownership" error originates server-side during remote repository access, not client-side during clone. This means `safe.directory` configuration must be set on the server (`my.server`) where the security check executes, not the client.

Technical context:
- Security check triggering for shared repositories with group ownership
- Original troubleshooting focused incorrectly on client-side configuration
- Server-side Git process rejects repository despite proper group permissions
- Workaround path now targets server configuration rather than client

Key details:
- Error occurs during remote repository access by server-side Git process
- Server runs as user `myuser` which should have access via group permissions
- `safe.directory` must be configured where Git executes (server in this case)

Open questions:
- Why does server-side Git reject properly permissioned shared repository?
- Are there server-specific constraints affecting `safe.directory` evaluation?
- Does this represent a regression in server-side security checks?

The thread has shifted focus to server-side behavior, with the new understanding that the security check occurs during remote operations rather than local clone. This explains why previous client-side configuration attempts failed but raises new questions about server-side Git's handling of shared repositories. The reporter's next steps would involve verifying server configuration and permissions rather than client settings.