This is a design discussion email in the ongoing `git-repo-info` interface thread. Patrick Steinhardt concedes to Junio C Hamano's argument about scripting safety, agreeing to keep the `--all` flag rather than making no-argument invocations default to showing all fields.

The email marks a resolution to the key open question in the thread - whether to use `--all` flag (Junio's preference) or default-all behavior (Patrick's earlier proposal). Patrick acknowledges Junio's point that silent behavior changes when no arguments are given could cause problems for scripting use cases, which is a core Git design consideration.

This moves the thread forward by settling the interface design question, allowing implementation work to proceed with the `--all` flag approach. The decision maintains consistency with Git's scripting-first philosophy while still providing discoverability through the explicit `--all` option. No new technical details are introduced here - it's purely a design agreement that clears the path for the next steps in the implementation.