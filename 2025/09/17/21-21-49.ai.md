This is a response in the ongoing bug report thread about `git fetch --dry-run --filter` modifying local config unexpectedly. Brian m. carlson acknowledges the core issue that `--dry-run` should not modify `.git/config` (adding promisor remote configuration) but provides important context about why these settings would be needed for a real fetch operation.

The response clarifies that marking the remote as a promisor is technically required for partial clone operations to prevent repository corruption, but agrees this violates the dry-run contract. They suggest `--dry-run` may not be the right tool for the original use case (checking commit existence without downloading) since it shouldn't contact the server at all, and propose documenting this limitation.

Key technical details noted: The discussion reveals deeper questions about dry-run semantics with partial clones, and suggests the reporter's approach (using fetch to check commit existence) may need protocol-level changes rather than command-line solutions. The Conan package manager use case is acknowledged but deemed poorly served by current Git CLI capabilities.

The response maintains the thread's focus on Windows platform behavior while expanding the discussion to include dry-run's fundamental constraints. No fix is proposed yet, but the analysis helps frame the tension between partial clone requirements and dry-run guarantees. The exchange suggests this may require either documentation updates or deeper changes to how dry-run interacts with promisor remotes.