This is a CI/build system patch in the ongoing Git Rust integration series. Patch 9/18 focuses on installing Rust toolchains in GitHub Actions workflows, handling platform-specific requirements across Windows, macOS, and Linux (including musl and 32-bit environments).

The patch introduces two new scripts (`install-rust-toolchain.sh` and `install-rustup.sh`) and modifies the main workflow file to handle Rust installation differently across platforms. Key platform-specific considerations include: Windows requiring Rust 1.78.0+ for bcrypt library resolution, musl needing 1.72.0+ for libc compatibility, and 32-bit Linux containers requiring explicit target triple specification. The changes maintain consistency with the series' goal of optional Rust support while ensuring CI can properly test Rust-enabled builds across all supported platforms.

The implementation shows careful attention to cross-platform details, with clear version requirements and fallback mechanisms. The patch builds on previous infrastructure work in the series while adding the concrete CI integration needed to validate Rust builds. The changes appear well-motivated and technically sound, addressing real compatibility issues that would otherwise block Rust adoption in CI testing.