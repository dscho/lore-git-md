This is part of the ongoing command deprecation framework series, specifically focusing on the alias handling implementation. The patch relocates the alias loop detection logic from the main command processing in `run_argv()` into `handle_alias()`, which is a preparatory step for allowing deprecated builtins to be shadowed by aliases.

The key changes move the `seen-alias` bookkeeping (renamed to `expanded_aliases` for clarity) into `handle_alias()`, where it conceptually belongs. This prevents a blind spot where alias loops could be crafted via deprecated builtin names. The implementation maintains all existing functionality while better organizing the code for the upcoming alias shadowing feature.

Technical details include:
- Modified file: `git.c`
- Renamed `cmd_list` to `expanded_aliases` for better clarity
- Moved string list operations and loop detection into `handle_alias()`
- Preserved all existing error messages and loop detection behavior
- Based on earlier work by Jeff King to fix output issues

The change is part of a well-considered series that has gone through multiple iterations (now at v6) with thorough review. This particular patch represents a clean code reorganization that improves the foundation for the alias shadowing feature while maintaining existing behavior. The commit message clearly explains the motivation and technical approach, linking to previous discussion threads.