This is the third patch in a 4-part refactoring series to improve type safety in the string-list API by eliminating sign comparison warnings. The patch changes the return type of `string_list_find_insert_index()` from `int` to `size_t` to match its internal helper function `get_entry_index()`.

The changes are mechanical but require careful handling in callers. The most notable fix is in `mailmap.c` where the loop condition needed adjustment to properly handle `size_t` underflow (changing `0 <= --i` to `i--`). The patch also removes the now-unneeded `DISABLE_SIGN_COMPARE_WARNINGS` from `mailmap.c`.

This continues the series' goal of making the string-list API more type-safe while maintaining identical behavior. The changes affect several callers including `add-interactive.c`, `mailmap.c`, and `refs.c`. The patch builds on previous work in the series that made the index parameter optional and introduced the `exact_match` boolean flag.

The patch appears well-constructed with appropriate test coverage, though the commit message could better explain why this change is valuable beyond just matching the helper function's return type. The thread shows this version incorporates all feedback from the initial version, with reviewer Patrick Steinhardt having approved the technical approach.