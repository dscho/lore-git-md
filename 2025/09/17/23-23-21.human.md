This is a follow-up in the thread about `git fetch --dry-run --filter` unexpectedly modifying local config. Kevin Puetz responds to brian m. carlson's technical explanation about why promisor remote configuration is needed for partial clones, but maintains that `--dry-run` should not make persistent changes.

The discussion has evolved to focus on finding better ways to check commit existence without full downloads, which was the original use case from the Conan package manager. Puetz suggests `git fetch --negotiate-only` as a more efficient alternative, though notes it produces an odd error message when checking unrelated repositories. 

The thread reveals a tension between dry-run semantics and partial clone requirements, with no clear resolution yet. Puetz's exploration of `--negotiate-only` may point toward a better solution for the Conan use case, though the unexpected packfile response suggests there may still be edge cases to handle. The conversation remains technical and constructive, with both parties working toward understanding the underlying behaviors.