This is a response in the ongoing discussion about `git fetch --dry-run --filter` modifying local config. Brian m. carlson agrees with the original reporter that `--dry-run` should not modify the repository configuration, but offers additional context about why the promisor remote configuration would be needed if data were actually being downloaded.

The response clarifies that `--dry-run` likely shouldn't contact the server at all, which may not match the reporter's use case of checking commit existence. Brian suggests the current approach may not be the most efficient way to verify commit existence on a remote, and hints that custom protocol handling might be needed for this specific use case from the Conan package manager.

The exchange highlights a tension between the dry-run contract (no changes) and the technical requirements of partial clone operations. While no solution is proposed yet, the response helps frame the discussion by distinguishing between the bug (unwanted config writes) and the underlying use case (commit existence checking).