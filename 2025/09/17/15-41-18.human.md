This is a follow-up discussion about a build system issue in the ongoing command deprecation series. Kristoffer Haugsbakk confirms Jeff King's diagnosis that the Makefile` still includes `git-whatchanged` in `BUILT_INS` even when built with `WITH_BREAKING_CHANGES=YesPlease`, which causes confusing behavior. The stale executable remains present but non-functional, preventing proper alias shadowing and producing misleading error messages.

The core problem is that while `whatchanged` was removed from Git's command table in the deprecation series, the build system wasn't properly updated to exclude it from being built. This creates a mismatch where the executable exists but can't be used as either a builtin or an alias. Kristoffer verifies that applying Jeff's proposed Makefile change (conditionally excluding `git-whatchanged` from `BUILT_INS`) resolves the issue.

The email includes a draft commit message that clearly explains the problem and solution. This appears to be the final piece needed to properly complete the deprecation series' build system integration. The discussion shows good technical understanding from both participants and moves the thread toward resolution of the last known issue in the series.