This is a build system patch in the ongoing Rust integration series. It introduces a new `build-helper` crate that will handle common boilerplate code needed in Rust build scripts (`build.rs` files). The patch is part of a larger 18-part series focused on Git's Rust infrastructure.

The changes are minimal scaffolding for the new crate - adding it to the top-level Cargo workspace, creating empty Cargo.toml and lib.rs files, and updating .gitignore. While the implementation is currently empty, the commit message indicates this will eventually contain shared build functionality to avoid duplication across Rust components.

As part of the broader Rust transition effort, this appears to be a preparatory step for standardizing build processes across multiple Rust components in Git. The patch follows established patterns seen elsewhere in the series for new crate creation. Given the context from the thread summary about comprehensive build system support being developed, this seems like a logical piece of infrastructure for the Rust components to come.