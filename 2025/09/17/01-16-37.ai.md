This is a build system patch in the ongoing Rust integration series. It adds the `build-helper` dependency and build script to the new `misc` Rust crate, following the established pattern used by other Rust components in the project.

The patch makes two small but important changes to integrate the misc crate with Git's Rust build infrastructure: it adds `build-helper` as a build dependency in `Cargo.toml` and creates a minimal `build.rs` script that initializes the build helper. This matches the approach used by other Rust crates in the project, ensuring consistent build behavior and header generation capabilities.

The changes are straightforward and mechanical - they don't add any new features to the misc crate itself, which remains empty, but properly wire it into the build system. This is part of the incremental infrastructure work needed before actual utility functions can be added to the misc crate. The patch follows the same pattern seen in earlier parts of the series for other Rust components, maintaining consistency across the codebase.

Key technical details for future reference: the build script uses `BuildHelper::new()` with environment variables and calls `build()`, matching other crates. The `build-dependencies` section in Cargo.toml adds `build-helper` from the local path. This is purely a build system change with no runtime impact.