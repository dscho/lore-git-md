Bugfix follow-up for `git send-email` Reply-To header handling. Eric Sunshine provides minor review feedback on NeilBrown's patch that fixes duplicate Reply-To headers when using both `--compose` and `--reply-to` options. The review focuses on two small typographical corrections in the commit message: changing "extract" to "extracted" and "though" to "through" in the explanation of header processing logic. 

The original patch addressed a real-world issue where gmail would reject messages with duplicate Reply-To headers, caused by `git-send-email.perl` both passing through the original header and generating a new one from the `$reply_to` variable. The fix modified the `pre_process_file` subroutine to properly parse and handle the Reply-To header. 

This follow-up email doesn't introduce new technical discussion but rather provides editorial improvements to the commit message's clarity. The changes are purely cosmetic and don't affect the technical correctness of the solution. The thread shows the patch is in its final polishing stage before potential acceptance.