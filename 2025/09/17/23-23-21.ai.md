This is a follow-up in the thread about `git fetch --dry-run --filter` modifying local config unexpectedly. Kevin Puetz responds to brian m. carlson's technical explanation about why promisor remote configuration is needed for partial clones, while maintaining that the dry-run behavior remains problematic.

The email introduces `git fetch --negotiate-only` as a potential alternative solution for Conan's use case of checking commit existence without full downloads. Puetz reports that it works for their needs but notes an odd behavior when querying unrelated remotes (fatal error about unexpected packfile instead of a clearer "not our ref" message). They speculate this might be a GitHub server implementation issue rather than a client bug.

Key technical details for future reference:
- The discussion now centers around `--negotiate-only` as an alternative to `--dry-run --filter` for commit existence checks
- Current behavior with unrelated remotes produces "fatal: expected 'acknowledgments', received 'packfile'"
- Conan's specific use case remains the driving motivation for finding efficient commit-checking methods
- Windows platform context from original report remains relevant but unaddressed in this follow-up

The thread has shifted from reporting a dry-run bug to exploring better ways to implement commit existence checks, with `--negotiate-only` emerging as a promising but imperfect solution. The original dry-run config modification issue remains unresolved but may become moot if better alternatives are found.