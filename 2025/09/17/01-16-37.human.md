This is a build system patch in the ongoing Rust integration series. It adds the `build-helper` dependency and build script to the new `misc` Rust crate, following the same pattern established by other Rust components in the project.

The patch is straightforward - it adds a standard build script (`build.rs`) that uses the shared `BuildHelper` infrastructure, and declares the `build-helper` crate in `Cargo.toml`. This matches how other Rust crates in the project are structured, ensuring consistent build behavior across components. The change is purely infrastructural - the `misc` crate remains empty of actual code at this point, but now has the proper build scaffolding in place for when utility functions are added later.

As part of the larger Rust integration effort, this patch demonstrates the project's systematic approach to adding Rust components, with each new crate following established patterns for build configuration. It's a small but necessary piece of the infrastructure work needed before actual functionality can be added to the `misc` crate.