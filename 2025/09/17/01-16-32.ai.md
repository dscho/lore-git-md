This is a build system patch in the ongoing Rust integration series. It introduces a new `build-helper` crate that will handle common boilerplate code needed in `build.rs` files across the project. The patch is part of a larger 18-part series (currently at v2) that's establishing Git's Rust infrastructure.

The changes are minimal at this stage - just the skeleton of a new crate with empty `lib.rs` and basic `Cargo.toml` configuration. The crate is added to the workspace members list in the root `Cargo.toml`. The commit message suggests this will eventually contain shared build functionality, though the current implementation is just scaffolding.

This appears to be preparatory work for future Rust build system components, following the series' established pattern of incremental infrastructure development. The empty implementation suggests this is being introduced early to allow parallel development of dependent crates that will use these helpers. The patch maintains consistency with the series' approach to Rust integration, though reviewers may want to see more concrete functionality before merging.