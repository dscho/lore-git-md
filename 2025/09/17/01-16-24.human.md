This is a build system patch that's part of the larger Rust integration effort. It merges the reftable library into libgit.a, eliminating the separate libreftable.a target. This change follows the same pattern used earlier in the series for xdiff components, consolidating Git's internal libraries into a single archive.

The patch removes all references to the standalone REFTABLE_LIB variable and moves the reftable object files directly into LIB_OBJS. This simplifies the build system by reducing special cases and follows the series' strategy of preparing for Rust integration by unifying library handling. The changes are mechanical but important groundwork - they ensure Rust components can link against a single libgit.a rather than needing to track multiple archives.

This appears to be a straightforward, well-executed change that aligns with the series' goals. The patch is cleanly focused on its stated purpose with no apparent issues. As part 4 of 18 in this series, it builds on earlier build system changes and sets up for later Rust integration work.