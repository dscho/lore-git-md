This is the final exchange in a feature patch series adding signature handling to `git fast-import`. The series has gone through multiple rounds of review, with all technical feedback now addressed.

Christian Couder responds to Junio Hamano's concerns about potential churn in the implementation approach. The core issue was around how signature stripping should work in the new `--signed-commits` option. After some back-and-forth, they've settled on an approach where STRIP mode parses signatures (like ABORT mode) while still removing them from output, maintaining good code structure.

Christian indicates he's sent a V3 that incorporates all feedback and hopefully improves on both previous versions. The tone suggests this represents the final iteration, with Junio having previously expressed satisfaction with the technical implementation. This appears to be closing discussion on the patch series, which is now likely ready for merging.

The patch series as a whole adds important functionality for repository filtering operations by bringing signature handling in `fast-import` to parity with `fast-export`. The implementation is thorough, with comprehensive test coverage and consideration for future extension to tag signatures.