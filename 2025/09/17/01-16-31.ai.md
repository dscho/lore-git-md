CI / build system patch in the Rust integration series, specifically addressing dependency auditing.

This patch adds Cargo.lock file uploads as build artifacts across multiple GitHub Actions workflows (Windows, Windows+Meson, Linux, and static analysis jobs). The change enables auditing of Rust build dependencies by preserving the exact versions of crates used during CI runs. Each workflow variant gets its own uniquely named artifact (e.g. cargo-lock-windows, cargo-lock-windows-meson) to distinguish between different build environments.

The implementation uses GitHub's standard actions/upload-artifact@v4 action to capture the rust/Cargo.lock file from each job. This matches the series' broader theme of robust CI infrastructure for the Rust transition, complementing earlier patches that established Rust toolchain installation and cross-platform support. The change is straightforward with no apparent technical concerns - it simply makes existing dependency information available for post-build inspection without modifying build behavior.

This appears to be a minor but useful housekeeping improvement in the Rust integration effort, providing better visibility into dependency trees across different build configurations. The patch follows the series' pattern of incremental CI enhancements while maintaining consistency with existing artifact upload patterns seen in other workflow steps (like failed test artifacts).