This thread addresses a bug in `git send-email` where using both `--compose` and `--reply-to` options would create duplicate Reply-To headers in the intro message, causing rejection by email providers like gmail. The issue stems from the script both passing through the original header and generating a new one from the `$reply_to` variable.

NeilBrown submitted a minimal fix modifying `git-send-email.perl` to properly parse Reply-To headers in the `pre_process_file` subroutine, ensuring only one header appears. The patch includes clear reproduction steps and targets the specific email provider behavior that revealed the issue.

Eric Sunshine has now reviewed the patch, suggesting two minor typographical improvements to the commit message ("extract" → "extracted" and "though" → "through") to enhance clarity. These are purely editorial changes that don't affect the technical solution. With this review feedback addressed, the patch appears ready for final acceptance, solving a real-world usability problem in email submission workflows.