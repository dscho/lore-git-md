This is a technical discussion about the scope of history rewriting in the proposed `git-history` command, specifically regarding how it handles descendant commits and branch updates when dropping commits.

SZEDER Gábor raises an important clarification request about the behavior of the `drop` subcommand in Patrick Steinhardt's `git-history` proposal. The question focuses on commit graph traversal and branch updates when dropping a commit, using a specific example with four branches (branch1-4) and commit B as the target for dropping. The concern is about the potentially "scary" implications of rewriting "all descendants" and repointing branches automatically.

The email references Patrick's earlier confirmation that `git-history` would indeed follow Jujutsu's behavior of rebasing descendants and repointing branches, including HEAD. SZEDER's example graph shows multiple branches with varying relationships to commit B, testing the boundaries of what "all descendants" means in practice. This touches on core functionality questions about how aggressively the command should modify history and update references, particularly when multiple branches are involved.

Key technical details include:
- The example commit graph structure with four branches intersecting at different points
- The specific question of which commits would be rewritten when B is dropped
- The related question of which branch tips would be updated
- The underlying concern about safety and predictability of wide-ranging history modifications

This discussion directly impacts the design of the `drop` subcommand's behavior and could influence decisions about:
- Default vs configurable scope of history rewriting
- Safety mechanisms to prevent unintended wide-scale modifications
- Documentation requirements for complex graph scenarios
- Potential need for dry-run or confirmation modes

The thread context shows this is part of ongoing design discussions about `git-history`, where similar questions have arisen about other subcommands' behavior and the command's overall philosophy regarding automatic vs explicit operations.