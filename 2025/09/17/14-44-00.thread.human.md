Here's the updated human-readable thread summary:

The thread began with Kevin Puetz reporting that `git fetch --dry-run --filter` unexpectedly modifies local `.git/config` by adding promisor remote configuration, violating the expected dry-run semantics. This was observed on Windows with Git 2.51.0 while using the Conan package manager to check commit existence without full downloads. brian m. carlson confirmed the behavior violates dry-run principles but explained these settings are technically necessary for partial clone operations to prevent repository corruption.

In the latest update, Puetz acknowledges the technical rationale but maintains the dry-run behavior remains problematic for their use case. They've discovered `git fetch --negotiate-only` as a potential alternative solution that better fits Conan's needs for checking commit existence without downloads. While this works for their primary use case, they note an odd error message ("fatal: expected 'acknowledgments', received 'packfile'") when querying unrelated remotes, which may indicate a GitHub-specific implementation issue rather than a client bug.

The discussion has now shifted from reporting a dry-run bug to exploring better ways to implement commit existence checks. The original config modification issue remains unresolved but may become less critical if `--negotiate-only` proves to be a viable alternative. The Windows-specific context from the original report hasn't been addressed in this latest exchange. The thread currently leaves open whether to treat this as a documentation gap (clarifying dry-run limitations) or pursue code changes to either modify promisor config handling or improve the `--negotiate-only` behavior.