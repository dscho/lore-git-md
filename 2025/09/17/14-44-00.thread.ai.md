Here is the updated dense AI thread summary incorporating the new response:

Thread identity: Bug report about `git fetch --dry-run --filter` modifying local config unexpectedly, now evolving into discussion of commit existence checking methods. Originating author Kevin Puetz, observed on Windows with Git 2.51.0. Core problem has expanded from dry-run semantics violation to include exploration of `--negotiate-only` as alternative solution.

Core problem: Original issue where `git fetch --dry-run --filter=tree:0` modifies `.git/config` remains unresolved, but focus has moved to finding better ways to check commit existence without downloads. brian m. carlson previously confirmed dry-run behavior violates contract but explained technical necessity for partial clones. Kevin Puetz now proposes `--negotiate-only` as alternative, though notes it has its own quirks with unrelated remotes.

Technical details: Two distinct but related behaviors under discussion:
1. Original dry-run issue: Partial clone operations require promisor remote configuration (`promisor = true` and `partialclonefilter`), causing dry-run to modify config
2. New `--negotiate-only` behavior: Works for commit existence checks but produces confusing "expected 'acknowledgments', received 'packfile'" error with unrelated remotes (possibly GitHub server-specific)

Current status: Original dry-run issue remains unresolved but may become less critical if `--negotiate-only` proves viable. No consensus yet whether to:
- Fix dry-run behavior for partial clones
- Document limitations
- Promote `--negotiate-only` as preferred solution
- Address `--negotiate-only` edge cases

Participant positions: Kevin Puetz driving discussion toward practical solutions for Conan's use case, now evaluating `--negotiate-only`. brian m. carlson previously established technical constraints around partial clone requirements. Windows platform aspect noted but not yet addressed.

Open questions:
- Should `--negotiate-only` be officially recommended for commit existence checks?
- Is the packfile error with unrelated remotes a client or server issue?
- Does Windows need special handling for either approach?
- Should dry-run behavior be modified or just documented?

Related work: Connects to broader partial clone and fetch protocol discussions. May require coordination with GitHub if server behavior needs modification. Original dry-run issue could inform future design of read-only operations.

Key technical details for future:
- `--negotiate-only` works for commit checks but has edge cases
- Original dry-run/config modification issue remains technically valid
- Windows context still pending investigation
- Two distinct solutions now in play with different tradeoffs