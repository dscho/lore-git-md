This is a refactoring patch in a series improving type safety in the string-list API. It changes the `exact_match` parameter from `int` to `bool` in several string-list functions, making the code more semantically clear since this parameter fundamentally represents a true/false condition.

The patch updates both the implementation and header files, converting all internal uses of `exact_match` to use boolean values (`true`/`false`) rather than integers (1/0). It also changes the return type of `string_list_has_string()` from `int` to `bool` for consistency.

This change is part of a larger effort to eliminate sign comparison warnings in the codebase. The patch series has already gone through one round of review, with this version incorporating feedback to use proper boolean types. The changes are mechanical and don't affect functionality, just improving type safety and code clarity.

The patch appears well-contained and focused, with clean diffs that show straightforward conversions. Since it's part of an already-reviewed series that addresses compiler warnings, it's likely to be uncontroversial.