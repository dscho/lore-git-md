This is a documentation patch that updates sparse index expansion advice to reference the new `git sparse-checkout clean` command. It's part of the ongoing sparse-checkout clean feature series (v3 6/7) that combines Victoria Dye's original concept with Derrick Stolee's implementation and refactoring work.

The patch modifies the advice message in `sparse-index.c` to suggest using `git sparse-checkout clean` instead of the generic `git clean` when sparse index expansion occurs due to leftover files in tracked-but-sparse directories. This change reflects the new specialized command's ability to safely clean these specific cases while preserving the sparse-checkout boundaries.

Key technical details:
- Updates the `SPARSE_INDEX_ADVICE` string constant to reference the new command
- Maintains all existing advice about checking sparse-checkout patterns
- Positioned as patch 6/7 in the series, following core functionality and safety features
- Builds on the thread's established context of combining clean functionality with `the_repository` removal

The change is straightforward and well-motivated, connecting the new feature to a common user pain point (sparse index expansion due to leftover files). It follows the series' pattern of integrating the clean command while maintaining safety through force/dry-run requirements.