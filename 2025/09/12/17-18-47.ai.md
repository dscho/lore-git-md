This is a response in the ongoing bundle-URI bugfix thread, where Junio C Hamano weighs in on Justin Tobler's suggestion about error handling for malformed bundle configurations. The discussion centers on whether Git should silently skip bundles missing required 'uri' fields (as proposed in Toon Claes's patch) or actively warn users about such misconfigurations.

Junio agrees with Justin's position that client-side misconfigurations should not be silently ignored, arguing users should be notified so they can correct the issue. This contrasts with the original patch's approach of silently skipping invalid bundles, which was more focused on preventing segfaults from malformed server responses. The exchange highlights an emerging distinction in the thread between handling server transmission errors (where graceful degradation may be appropriate) versus client configuration errors (where stricter validation might help users catch mistakes).

The technical context remains focused on bundle-URI parsing in connect.c and bundle.c, particularly around the 'creationToken' heuristic validation. While the core segfault fix is uncontroversial, this response shifts the discussion toward user-facing behavior and error reporting philosophy. No code changes are proposed in this email - it's purely a design discussion about the appropriate level of strictness for bundle configuration validation.