This is a documentation patch that updates the sparse index expansion advice message to reference the new `git sparse-checkout clean` command. It's part of a larger series (6/7) combining Victoria Dye's sparse-checkout clean feature with Derrick Stolee's `the_repository` refactoring work.

The change is straightforward - it modifies the advice text shown when Git needs to expand a sparse index, suggesting the new specialized `sparse-checkout clean` command instead of the generic `git clean`. This makes sense since the new command is specifically designed to handle the common case where sparse index expansion occurs due to leftover tracked files outside the sparse-checkout scope.

The patch is small and focused, just updating a single help message. It builds on the core functionality established earlier in the series while improving user guidance. Since this is part of a well-developed series that's already gone through multiple iterations, the change appears solid and uncontroversial.