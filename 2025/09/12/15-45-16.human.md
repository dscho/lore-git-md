This is a build system patch in the ongoing Rust infrastructure series, specifically addressing a minor oversight in the v4 revision. SZEDER Gábor points out that while the patch series adds `/Cargo.lock` back to `.gitignore`, it fails to update the `clean` target in the Makefile to remove this file during cleanup operations.

The observation is straightforward and correct - when build artifacts are ignored, they should typically also be cleaned. This is a small but important detail for maintaining a clean working directory. The issue doesn't affect functionality but would be noticed by developers working with the Rust build infrastructure.

This comes in the context of Patrick Steinhardt's larger effort to introduce Rust into Git's core, where build system details are being carefully worked out across multiple iterations. The current patch series (v4) has already seen significant review and refinement, with this being a minor polish point in the Makefile integration work (patch 3/9 in the series).