This is a follow-up discussion in the feature patch series adding signature handling to `git fast-import`. The thread context shows this is the second version of patch 2/2, which implements `--signed-commits` after patch 1/2's infrastructure refactoring was merged.

Junio C Hamano acknowledges Christian Couder's V2 changes that clarified the scope decision to implement commit signature handling first before tags. While understanding the rationale (validating the design with one option first), Junio expresses mild skepticism about this scoping choice. He notes that signed tags are historically more common than signed commits, suggesting tags might have been the more logical starting point if only implementing one. However, he accepts the current approach as understandable.

The email confirms all technical feedback on the implementation has been addressed in V2, leaving only this higher-level discussion about feature scope. Christian had previously explained in the updated commit message and cover letter that tag support (`--signed-tags`) is planned as follow-up work after validating the design with commits. The exchange shows the maintainer's perspective on feature prioritization while indicating acceptance of the contributor's phased approach.

Key technical details from the thread context remain unchanged: the implementation handles commit signatures with consistent parsing across modes (abort/verbatim/strip/warn variants), maintains streaming behavior, and has comprehensive test coverage in t9305. The infrastructure from patch 1/2 (enum sign_mode, parse_sign_mode()) supports future tag handling.