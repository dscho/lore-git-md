This is a follow-up discussion in the feature patch series adding signature handling to `git fast-import`. The exchange focuses on technical details around signature parsing behavior that was addressed in the V2 revision.

The discussion clarifies how multiple signatures are handled in the implementation. Christian Couder explains that while the code can process up to two signatures (one SHA-1 and one SHA-256), the `store_signature()` function properly handles duplicate signatures by warning and ignoring additional signatures of the same type. This matches the existing behavior in `git fast-export`.

Junio C Hamano had noted some confusion about the "up to 2" comment in the code, pointing out it made more sense when near the parsing loop. In response, Christian indicates this comment has been removed in V2 as it wasn't providing clear value. This change appears to be part of the broader cleanup and refinement of the implementation based on review feedback.

The technical discussion confirms the signature handling is working as intended, with proper error checking for malformed input cases. This email represents the final resolution of implementation details that were raised during review, showing all feedback has been addressed in the V2 submission. The exchange demonstrates the thorough review process and the author's responsiveness to technical concerns.