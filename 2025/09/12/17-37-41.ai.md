This is a discussion thread continuation about bugfix for git-clone's bundle-URI handling, specifically addressing how to handle invalid server responses. Justin Tobler responds to Toon Claes's patch that proposed downgrading invalid bundle-URI response errors to warnings.

Justin questions the approach of silently ignoring invalid bundle-URI data from servers, suggesting that server misconfigurations should be fixed at their source rather than worked around in the client. He argues the client should expect properly formatted responses and that silent failures could mask real configuration problems. This aligns with Junio Hamano's earlier position in the thread distinguishing between server transmission errors (where graceful degradation may be appropriate) and client-side misconfigurations (where stricter validation helps users).

The technical focus remains on bundle-URI parsing in connect.c, where the patch would change an error() to warning() for malformed response lines. The discussion has shifted from pure implementation to design philosophy about error handling strictness, with Justin reinforcing the maintainer's view that client-side expectations should not be silently relaxed. No code changes are proposed in this email - it's purely a design discussion pushing back on the patch's approach to error conditions.