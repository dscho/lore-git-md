This is a discussion response about bugfixes for git-clone's bundle-URI handling. Justin Tobler is replying to Toon Claes's patch that prevents segfaults when bundle advertisements contain a 'creationToken' but lack the required 'uri' field.

Justin agrees with hardening Git against malformed bundle-URI data, but questions whether silently skipping invalid configurations is the right approach. He suggests that while segfaults should definitely be prevented, the client might want to surface misconfiguration errors rather than silently continuing. At minimum, he proposes adding warnings when invalid bundle-URI data is encountered.

The exchange shows thoughtful consideration of how to balance robustness against invalid input with clear feedback to administrators. This builds on the thread's theme of making bundle-URI handling more resilient while maintaining useful diagnostics. The discussion hasn't yet reached consensus on whether to fail or continue when encountering these malformed advertisements.