This is a technical clarification in the ongoing `contrib/` cleanup thread, specifically addressing the `git-new-workdir` removal debate. Phillip Wood provides a key technical detail about `git worktree` behavior that was missing from earlier discussion: the existence of `--ignore-other-worktrees` as a safer alternative to `--force` for checking out the same branch in multiple worktrees. He explains the historical context (`--force` predates this option) and safety rationale (preventing accidental overwrites of uncommitted changes). This directly addresses Gabriel Scherer's workflow concern about branch switching between worktrees, though it doesn't resolve the remote configuration sharing limitation that remains a sticking point for Scherer's 73-remote use case. The email reinforces that while technical alternatives exist, they may still require workflow adjustments compared to `git-new-workdir`'s behavior. This contributes to the thread's central tension between policy-driven cleanup and real-world workflow impacts.