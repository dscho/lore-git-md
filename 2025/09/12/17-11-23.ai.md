This is a discussion response regarding a bugfix for bundle-URI handling in git-clone. The thread involves two independent patch series addressing different edge cases in bundle-URI parsing, both aiming to make the feature more resilient to malformed server responses.

Justin Tobler responds to Toon Claes's patch that prevents a segmentation fault when bundle advertisements contain a 'creationToken' heuristic but lack the required 'uri' field. While agreeing with the segfault prevention, Tobler questions whether silently ignoring the misconfiguration is the right approach. He suggests that client-side misconfigurations (as opposed to server errors) might warrant an error message or at least a warning, rather than silent continuation.

The response highlights a potential philosophical difference in error recovery strategies - whether to be permissive (continue with warnings) or strict (fail fast) when encountering malformed bundle-URI configurations. This touches on the broader design question of how Git should handle optional bundle-URI data during clone operations when that data is malformed but not strictly required for operation.

Key technical context from the thread includes: the fixes affect bundle-URI parsing in connect.c and bundle.c, with comprehensive test coverage in t5558. The discussion centers around whether error handling should differ between server-side transmission errors (where graceful degradation makes sense) versus client-side configuration errors (where failing fast might be preferable).