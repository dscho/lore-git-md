Thread identity: Question about mergetool environment variables (BASE, LOCAL, REMOTE, MERGED) not being passed to custom mergetool scripts as documented, raised by Randall Becker.

Core problem: The documented environment variables for mergetool invocation are not being set when using a custom mergetool shell script wrapper configured via `mergetool.<tool>.cmd`. Test case shows temporary files are created (proving merge is occurring) but variables are not present in the environment when the mergetool script runs.

Current status: Issue reported but not yet resolved. Observed on Cygwin (Git 2.45.1) and NonStop (Git 2.51.0) platforms with local repositories. Minimal test case uses a shell script that dumps environment during merge conflict resolution of a simple file modification conflict in file 'a'.

Key technical details:
- mergetool configuration: `mergetool.<tool>.cmd`
- Expected variables: BASE, LOCAL, REMOTE, MERGED
- Temp files exist in working directory during merge
- Issue occurs regardless of repository having upstreams

Open questions:
- Is this a documentation issue or actual bug?
- Platform-specific behavior or general mergetool invocation problem?
- How does the variable passing mechanism work in current implementation?

Participant positions:
- Randall Becker reported the issue with detailed reproduction steps

Related work: None mentioned yet, but likely connects to general mergetool implementation and platform integration code paths.