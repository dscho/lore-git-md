This is a response to review feedback on the second patch of a feature series adding signature handling to `git fast-import`. Christian Couder addresses technical suggestions from Patrick Steinhardt regarding the implementation of the `--signed-commits` option.

Key changes made in V2 based on the feedback:
- Replaced `BUG()` with `die()` for unknown hash algorithms to better handle future cases
- Removed an unnecessary comment about signature processing
- Marked all new error/warning messages for translation
- Used `usagef()` instead of `die()` for invalid mode errors
- Improved test script by removing redundant branch name setup
- Kept documentation minimal for now but notes plans to improve it when adding more options

The response confirms alignment with Patrick's suggestions while explaining some design choices (like deferring comprehensive documentation improvements). Christian maintains the current approach of handling signatures in commits only (not tags) for this initial implementation, though the infrastructure supports both.

Technical details carried forward from the thread context:
- Uses the shared `enum sign_mode` and `parse_sign_mode()` from patch 1/2
- Maintains the dual-call design for multiple signatures
- Preserves existing behavior for malformed input
- Includes comprehensive test coverage in t9305

The exchange shows productive review iteration with Christian promptly addressing all concrete technical feedback while providing rationale for deferred improvements. The patch appears to be converging toward merge readiness with these changes.