This is a discussion thread continuation regarding the removal of `git-new-workdir` from Git's contrib directory, part of Patrick Steinhardt's systematic cleanup series. Gabriel Scherer raises several key points about the removal's impact on their workflow and questions the rationale now that usage has been demonstrated.

The email highlights technical limitations of the proposed `git clone --shared` alternative, particularly that it doesn't share remote configurations (unlike both `git-new-workdir` and `git worktree`). Scherer notes they maintain 73 remotes in their workflow and values the branch state synchronization between workdirs, which `git worktree` enforces differently by preventing multiple checkouts of the same branch.

Scherer directly challenges the removal rationale, pointing out the script has no maintenance burden and questioning whether demonstrated usage should warrant its reinstatement. They reference the xkcd "workflow" comic to illustrate how `git worktree`'s constraints (preventing multiple same-branch checkouts) disrupt their development pattern of independent working directories sharing a single git directory.

The thread context shows this discussion follows Junio Hamano's confirmation that detached HEAD mode could serve inspection workflows, but Scherer's use case involves active development. The email raises the possibility of Scherer submitting a patch to revert the removal if Patrick Steinhardt isn't interested in doing so, marking a potential pivot point in the thread's trajectory. The fundamental technical conflict remains between `git-new-workdir`'s permissive multiple-checkout behavior and `git worktree`'s branch uniqueness enforcement.