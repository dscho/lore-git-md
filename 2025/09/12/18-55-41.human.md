This is a discussion about the proposed removal of `git-new-workdir` from Git's `contrib/` directory, part of Patrick Steinhardt's systematic cleanup series. Gabriel Scherer raises concerns about the removal, noting that `git-new-workdir` provides functionality not fully replicated by `git worktree` or `git clone --shared`, particularly regarding shared remotes and branch state synchronization across multiple working directories.

Scherer reports using the script actively with 73 remotes configured, where `git clone --shared` fails to share remotes as expected. He questions the removal rationale, pointing out that while the script hasn't been updated in years, it causes no maintenance burden and is clearly still in use. He asks whether the script could be reinstated to `contrib/`, offering to submit such a patch himself if needed.

The email highlights a key difference in behavior between `git-new-workdir` and `git worktree`: the former allows multiple checkouts of the same branch while the latter enforces uniqueness, which Scherer finds limiting for his workflow. This appears to be a genuine use case not fully addressed by the proposed alternatives (detached HEAD mode for inspection or `clone --shared` for active development).

The discussion reveals a tension between the cleanup effort's goal of removing unmaintained scripts and the reality of undiscovered active users. Scherer's message provides concrete evidence that at least one user depends on behavior that current alternatives don't perfectly replicate, challenging the assumption that the script is obsolete."