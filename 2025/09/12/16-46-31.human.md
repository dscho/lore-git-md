This is a bugfix patch series addressing edge cases in `git clone`'s bundle-URI handling. The changes make the clone operation more resilient when servers send malformed responses during the bundle-URI negotiation phase.

The series consists of two small but focused changes to the bundle-URI feature. First, it teaches Git to ignore bundle entries that lack a URI rather than failing. Second, it prevents clone from aborting when receiving invalid packet lines during bundle-URI negotiation. Both changes maintain the principle that bundle-URIs are optional - if the server provides bad data, Git should gracefully fall back to regular cloning rather than failing.

The patches come with thorough test coverage in t5558, adding 49 lines of new scenarios that verify the improved error handling. The changes are minimal (just 7 net lines of code) and focused specifically on the error cases described. The commit messages clearly explain the problematic scenarios and how the changes address them.

This appears to be a well-considered quality-of-life improvement for the bundle-URI feature, making it more robust in real-world conditions where servers might not perfectly implement the protocol. The changes don't introduce new functionality but rather harden existing behavior against edge cases.