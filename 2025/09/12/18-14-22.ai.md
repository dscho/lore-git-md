This is a discussion about the removal of `git-new-workdir` from Git's contrib directory, part of Patrick Steinhardt's systematic cleanup series. The email provides two alternative approaches for Gabriel Scherer to maintain similar functionality after the script is removed:

1. Suggests using `git clone --shared` as a replacement, noting it could be aliased to minimize workflow disruption. This maintains the shared object store behavior while using core Git functionality.

2. Proposes manually preserving the script by either:
   - Copying it to a personal $PATH location
   - Extracting its history into a separate repository

The message acknowledges the workflow impact while offering concrete technical alternatives that avoid the maintenance burden on Git project. It positions these as personal solutions rather than arguing for keeping the script in contrib, aligning with the series' goal of removing unmaintained components.

Key technical details for future reference:
- `git clone --shared` alternative preserves object sharing
- Suggests global alias configuration (`new-workdir`) for UX continuity
- Mentions symlinking behavior of original script
- Implicitly confirms script's removal is proceeding as planned

The email doesn't challenge the removal decision but provides practical migration paths, representing a constructive response to the workflow concerns raised earlier in the thread. It assumes familiarity with both the original script's behavior and the ongoing discussion about worktree alternatives.