Here's a human-readable summary of the thread:

Christian Couder has proposed a patch series to bring signature handling capabilities to `git fast-import` that match the existing functionality in `git fast-export`. The series consists of two patches - the first refactors signature mode parsing into shared gpg-interface code, while the second implements the new `--signed-commits=<mode>` option for fast-import.

The implementation supports several signature handling modes ("verbatim", "strip", "abort", and warning variants of each) and works with both SHA-1 and SHA-256 signatures. The tests are comprehensive, covering various signature formats and verification scenarios. The default behavior differs from fast-export by preserving signatures ("verbatim") rather than aborting, which makes more sense for import operations.

There are two open design questions remaining: whether to keep synonym modes that might cause confusion, and whether warning variants should be separate modes or controlled by a --verbose flag. The implementation itself appears solid with good test coverage, and the discussion seems to be converging on these final design points before the series can be merged. The patches build on Christian's previous work in this area and maintain consistency with existing Git behavior.