This thread discusses Christian Couder's patch series adding signature handling control to `git fast-import` through a new `--signed-commits=<mode>` option, matching existing functionality in `git fast-export`. The two-patch series refactors signature mode parsing into shared gpg-interface code and implements the new fast-import option with several handling modes ("verbatim", "strip", "warn" variants, and "abort").

The implementation has been thoroughly reviewed, with Patrick Steinhardt providing key feedback that led to improvements in the switch statement robustness and test coverage. Christian has now addressed all technical concerns, including Patrick's suggestion about error handling - agreeing to add a more informative BUG message while noting invalid modes are already caught earlier. The series includes comprehensive tests covering various signature formats, hash algorithms, and handling modes.

Two design questions remain open for discussion: whether to keep synonym modes that might cause ambiguity, and whether warning variants should be separate modes or controlled by verbosity flags. With all technical issues resolved and positive review feedback from Patrick, the series appears ready for merging pending resolution of these higher-level questions. The thread shows productive collaboration between Christian and Patrick, with the v2 iteration demonstrating significant improvements over the initial submission.