Here's a human-readable summary of the thread:

Christian Couder is proposing a patch series to add signature handling control to `git fast-import`, matching the existing functionality in `git fast-export`. The series consists of two patches - the first refactors signature mode parsing into shared gpg-interface code, while the second implements the new `--signed-commits` option for fast-import with modes like "verbatim", "strip", and "abort".

The implementation is thorough, with comprehensive test coverage for different signature formats (OpenPGP/X.509/SSH), hash algorithms (SHA-1/SHA-256), and handling modes. The main difference from fast-export is that fast-import defaults to "verbatim" mode rather than "abort". The v2 iteration improved error handling, warning messages, and test coverage based on initial feedback.

Two design questions remain open for discussion: whether to keep potentially confusing synonym modes ("ignore" for "verbatim"), and whether warning variants should be separate modes or controlled by a --verbose flag. The series appears well-tested and ready for final review, with the shared gpg-interface infrastructure enabling consistent signature handling across both commands.