This is a refactoring patch that's part of a series adding signature handling to `git fast-import`. The patch moves the `enum sign_mode` definition and parsing logic from `fast-export.c` to `gpg-interface.{c,h}` to make it reusable. This is groundwork for adding similar signature handling options to `fast-import` in the next patch.

The changes are straightforward: the enum and parsing function are extracted with minimal modifications (just fixing some indentation). The new `parse_sign_mode()` function in gpg-interface.c will be used by both fast-export and the upcoming fast-import changes. The patch maintains all existing behavior, including support for synonym modes like "ignore" for "verbatim" and "warn" for "warn-verbatim".

This appears to be a clean, well-motivated refactoring that follows Git's established patterns. The commit message clearly explains the purpose and the changes are appropriately scoped for the first patch in a series.