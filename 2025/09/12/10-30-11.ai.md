This is a test expansion patch in the `git sparse-checkout clean` series, focusing on merge conflict scenarios. It documents current behavior around SKIP_WORKTREE bit handling during merges before planned updates.

The patch significantly expands test coverage for merge conflicts in sparse checkouts, replacing a simpler test case with a more comprehensive scenario. It creates a merge conflict in a sparse-checkout context, then attempts to clean the working directory while the conflict exists. The test confirms that files involved in the merge (with SKIP_WORKTREE bits set) are not cleaned, even with `-f` force option, due to current implementation limitations.

Key technical details:
- New test setup creates proper merge conflict with left/right branches modifying same file
- Verifies `sparse-checkout clean -f` fails when index cannot be converted to sparse
- Documents that merge-resolved files retain SKIP_WORKTREE bits and thus aren't cleaned
- Includes post-merge `sparse-checkout reapply` verification
- Maintains existing test file structure in t1091-sparse-checkout-builtin.sh

This test explicitly documents known limitations in the series' current state, particularly around SKIP_WORKTREE bit management during merges. The thorough test case will help validate future fixes for this edge case while preventing regressions. The test follows the series' pattern of comprehensive verification while acknowledging deferred functionality.