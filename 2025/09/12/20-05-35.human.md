This is a technical clarification in the ongoing `contrib/` cleanup series, specifically regarding the `git-new-workdir` removal debate. Phillip Wood responds to Gabriel Scherer's concerns about workflow disruption when switching branches across multiple worktrees.

Phillip points out that `git checkout --ignore-other-worktrees` exists as a solution for checking out the same branch in multiple worktrees, contrasting it with the more dangerous `--force` option that could overwrite uncommitted changes. This provides a middle ground between Scherer's workflow needs and Git's safety mechanisms.

The email reinforces the technical alternatives available in core Git (without `git-new-workdir`) while acknowledging the valid workflow concerns raised earlier in the thread. It doesn't take a position on whether `git-new-workdir` should be kept, but does show that some of the stated limitations of `git worktree` have existing solutions.

This moves the discussion forward by clarifying that the branch-checking behavior Scherer found restrictive can actually be overridden safely, though it still requires changing one's workflow habits. The exchange continues to highlight the tension between Git's safety defaults and real-world development patterns.