Here is the updated AI thread summary combining both patch series:

This thread contains two related but independent bugfix series for git-clone's bundle-URI handling, both making the feature more resilient to malformed server responses. Patrick Steinhardt's original 2-patch series (7 lines total) addressed cases: (1) ignoring bundle advertisements lacking a URI field rather than failing, and (2) preventing abort on invalid packet lines in bundle-URI responses. A new patch from a different author fixes a segmentation fault when a bundle advertisement contains a 'creationToken' heuristic but lacks the required 'uri' field, adding an explicit check in `fetch_bundle_uri_internal()` (3 lines). Both series include substantial test coverage in t5558 (49+ lines) verifying the fixes. The new patch complements Patrick's work by addressing a different failure mode (segfault vs. graceful fallback) while maintaining the same goal of robust handling of malformed bundle-URI data. Key technical details: changes affect bundle-URI parsing in connect.c and bundle.c, with tests covering packet line parsing, URI field validation, and creationToken handling. No reviews or responses have been received yet for either series. The thread demonstrates two independent approaches to hardening the same subsystem against edge cases in optional bundle-URI negotiation.