This is part 15/17 of the xdiff refactoring series preparing for compatibility with Rust's type system. The patch changes the `nrec` field in `xdfile_t` from `long` to `usize`, affecting 5 xdiff implementation files with 17 lines changed.

The modification follows the series' pattern of aligning C types with Rust expectations, where `usize` is Rust's standard type for sizes and indices. The change requires explicit casts when comparing with `long` values elsewhere in the code, particularly in loop conditions and boundary checks. All modifications are mechanical conversions maintaining identical runtime behavior.

This continues the incremental type system preparation from earlier patches, focusing on the record array size tracking. The change affects core diff operations but, like previous patches in the series, maintains strict behavioral compatibility while making the code more amenable to Rust interop. The patch touches several algorithms including split measurement, grouping, and merge operations, but only at the type system level.

Key files modified include xdiffi.c (diff algorithm core), xemit.c (output generation), xmerge.c (merge operations), xprepare.c (input processing), and xtypes.h (type definitions). The changes demonstrate how Rust type expectations propagate through the diff implementation, requiring explicit casts at C/Rust boundaries while keeping the underlying algorithms unchanged.