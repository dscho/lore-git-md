Refactoring patch in the xdiff Rust compatibility series. This is patch 14/17 in Ezekiel Newren's effort to prepare xdiff internals for potential Rust integration by splitting the `xrecord_t.ha` field into two distinct fields: `line_hash` and `minimal_perfect_hash`.

The change clarifies the dual purpose of the original `ha` field, which was being used both as a line content hash and as an index into the minimal perfect hash table. The patch updates all references across xdiff's core algorithms (histogram, patience, and Myers diff implementations) to use the appropriate new field name. While this is largely a mechanical change, it improves code clarity by making the distinct uses of these hash values more explicit.

The modifications touch 5 files with 20 lines added and 19 removed, following the pattern of previous patches in this series. Like earlier patches, this maintains identical runtime behavior while making the codebase more amenable to Rust integration. The change appears well-contained and focused, with no obvious technical concerns in the implementation.