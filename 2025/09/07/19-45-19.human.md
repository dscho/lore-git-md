Refactoring patch in the xdiff Rust compatibility series (16/17). This continues the type system alignment work by converting the `nreff` field in `xdfile_t` from `long` to `usize`, matching Rust's expectations. The change affects the diff record counting logic in xdiff's preparation phase.

The patch simplifies the code by eliminating temporary `nreff` variables and directly updating the struct field during record processing. This follows the series' pattern of mechanical but careful type changes that maintain identical runtime behavior while making the code more Rust-compatible. The modification touches two files (xprepare.c and xtypes.h) with straightforward changes to variable declarations and loop logic.

This is another incremental step in the larger effort to prepare xdiff for potential Rust integration. Like previous patches in the 10-17 range, it focuses on type system alignment without introducing Rust code. The changes appear well-contained and maintain the existing diff algorithm behavior while removing another type mismatch that would complicate future Rust interop.