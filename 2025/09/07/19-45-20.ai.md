This is the final patch (17/17) in a refactoring series preparing xdiff internals for Rust compatibility. The patch completes the type system alignment by converting several fields in `xdfile_t` to Rust-compatible types: `dstart` and `dend` become `i32` (from `long`), `rchg` becomes `u8*` (from `char*`), and `rindex` becomes `usize*` (from `long*`). These changes affect only the type declarations in `xtypes.h`, with no runtime behavior modifications.

The patch follows the series' pattern of mechanical type conversions to match Rust's stricter type system expectations. The `rchg` field's change to `u8*` is particularly notable as it aligns with Rust's distinction between character and byte arrays. The `rindex` conversion to `usize*` continues the series' theme of using size-appropriate unsigned types for indexing operations.

This concludes the series' type system adjustments, having systematically converted all core xdiff data structures to types that will map cleanly to Rust equivalents. The changes maintain strict backwards compatibility while establishing foundations for potential future integration work. The series has now addressed all planned type conversions mentioned in earlier patches, including the `rchg` field that was flagged for potential renaming in patch 9.