Refactoring patch changing `string_list_find_insert_index` return type from `int` to `size_t` to match its underlying `get_entry_index` implementation. This is part of a 4-patch series (position 3/4) though no cover letter context is provided.

The change propagates through several callers, with the most notable adjustment being in `mailmap.c` where the loop condition needed rewriting to properly handle `size_t` underflow (changing from `0 <= --i` to `i-- > 0`). This also lets us remove the `DISABLE_SIGN_COMPARE_WARNINGS` macro from mailmap.c. Other callers in `add-interactive.c` and `refs.c` get simpler type updates.

The patch appears mechanically sound - it's a straightforward type alignment that removes some warning suppression while maintaining identical behavior. The mailmap.c loop fix shows attention to edge cases with unsigned types. No tests are modified since this is purely a type change with no functional impact, though the existing test suite should verify the behavior remains correct.