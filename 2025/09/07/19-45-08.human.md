Refactoring patch in the xdiff cleanup series for Rust compatibility. This is patch 5/17 in the series, continuing the pattern of removing unnecessary abstractions from xdiff's C implementation to prepare for potential Rust integration.

The patch eliminates the `diffdata_t` struct, which was serving as a wrapper around fields from `xdfile_t`. The commit message clearly shows the direct field mappings between the two structs, demonstrating this is purely a simplification with no behavior change. The diff shows straightforward mechanical replacements throughout `xdiffi.c` and removal of the struct definition from `xdiffi.h`.

This continues the series' theme of removing indirection layers - following patch 4's removal of `xdl_get_rec()`, we now see another helper structure being deleted. The changes are focused and well-contained, touching only xdiff internals while maintaining the same external behavior. The diff shows a net reduction of 33 lines, mostly from removing the struct definition and its initialization code.

As with previous patches in the series, this appears to be a non-controversial cleanup that makes the code more direct while maintaining compatibility. The changes are consistent with the overall goal of simplifying xdiff implementation details to make future Rust integration easier.