Refactoring patch for the string-list API to make `exact_match` parameter optional in `get_entry_index()`.

This is a small API improvement to `string-list.c` that makes the `exact_match` output parameter of `get_entry_index()` optional by allowing NULL to be passed. The change is straightforward - it simply adds NULL checks before dereferencing the pointer. The motivation is to reduce boilerplate in callers that only care about the index position and not whether an exact match was found.

The patch touches only the implementation of `get_entry_index()`, modifying it to conditionally set the `exact_match` flag only when a non-NULL pointer is provided. This maintains all existing behavior while giving callers more flexibility. The diff shows minimal changes (4 insertions, 2 deletions) with clear logic.

This appears to be a well-contained, low-risk change that follows Git's coding style. The commit message clearly explains the rationale and the implementation matches the described behavior. As the first in a 4-part series, it likely sets up for further string-list API improvements, though the cover letter wasn't provided in this context.