This is part 10/17 of the xdiff refactoring series for Rust compatibility, marking the transition from C cleanups to Rust-specific type definitions. The patch introduces a new header file `compat/rust_types.h` that defines Rust-style primitive types (u8, i64, f32 etc.) as aliases for standard C types, establishing a common vocabulary for Rust interop.

The commit message makes a detailed case for using Rust types in C code rather than the reverse (C types in Rust), citing benefits like cross-language consistency, clarity, and future-proofing against other language integrations. Key technical points include the 32-bit `rust_char` type (contrasting with C's 8-bit char), and explicit avoidance of Rust's `c_*` types to maintain precision. The header defines 12 type aliases covering integers, floats, and size types, with a note that bool needs no special handling.

This represents the first concrete Rust-facing change in the series, shifting from internal xdiff cleanups to interface preparation. The change is mechanical (type definitions only) but establishes patterns for future Rust interop work. The rationale suggests this approach will be applied more broadly beyond xdiff, though the immediate impact is limited to the new header's availability. No behavior changes or test modifications are needed here. The patch continues the series' pattern of uncontroversial preparatory work, though the Rust-specific direction may invite more discussion than previous purely internal refactors.