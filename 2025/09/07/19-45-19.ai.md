This is part 16/17 of the xdiff refactoring series for Rust compatibility, focusing on type system alignment. The patch converts the `nreff` field in `xdfile_t` from `long` to `usize`, continuing the pattern established in previous patches (10-15) to make core xdiff data types match Rust's expectations.

The changes affect two files: `xdiff/xprepare.c` and `xdiff/xtypes.h`. The modification removes temporary `nreff` variables in favor of directly using the struct field, which now has Rust's preferred unsigned size type. This affects record counting operations during diff cleanup, though the runtime behavior remains identical. The patch follows the series' established approach of mechanical type changes that touch core logic while maintaining strict compatibility.

Key technical details include:
- Removal of intermediate `nreff` variables in favor of direct field access
- Conversion of `xdfile_t.nreff` from `long` to `usize` in the type definition
- Updates to record counting loops in `xdl_cleanup_records()`
- Maintenance of identical diff output despite type system changes

This continues the series' incremental preparation for potential Rust integration by making another core xdiff structure's types align with Rust's expectations. The change is focused and mechanical like previous type conversions in the series, touching similar areas of the codebase (xprepare.c and xtypes.h) while avoiding behavioral changes. The patch demonstrates how Rust's type preferences propagate through the diff algorithm's implementation, in this case affecting how records are counted during diff cleanup operations.