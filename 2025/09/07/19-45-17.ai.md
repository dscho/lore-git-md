This is a refactoring patch in the xdiff Rust compatibility series, specifically splitting the `ha` field in `xrecord_t` into two distinct fields: `line_hash` and `minimal_perfect_hash`. The change affects 5 xdiff implementation files with 20 lines changed.

The patch continues the series' pattern of making xdiff internals more explicit and Rust-compatible by separating what was previously a single hash field (`ha`) serving dual purposes. The new `line_hash` field stores the initial hash of the line content, while `minimal_perfect_hash` holds the unique identifier used for line matching during diff operations. This separation makes the data flow clearer and better matches Rust's type system expectations.

Key technical changes include:
- Modifies the `xrecord_t` struct in xtypes.h to add the new fields
- Updates all hash comparisons in diff algorithms (xdiffi.c, xhistogram.c, xpatience.c) to use `minimal_perfect_hash` instead of `ha`
- Adjusts hash table operations in xprepare.c to use the appropriate hash at each stage
- Maintains the same diff behavior while making the hash usage more explicit

The change is mechanical but touches core diff logic, requiring updates to all major xdiff algorithms. Like previous patches in the series, this maintains identical runtime behavior while improving type clarity. The split helps prepare for future Rust integration by making the hash usage patterns more explicit and potentially easier to model in Rust's type system.

This patch follows the uncontroversial preparatory nature of the series so far, with no reviews or discussion yet. It builds on the type system foundations from earlier patches while continuing to avoid introducing actual Rust code. The change affects the same set of xdiff files that have been modified throughout the series, with xprepare.c and xdiffi.c seeing the most churn.