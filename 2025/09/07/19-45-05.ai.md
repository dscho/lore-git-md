Refactoring patch in the xdiff Rust compatibility series (2/17). This patch continues the preparatory cleanups by simplifying memory management in xdl_prepare_ctx().

The change eliminates intermediate local variables that were being used to temporarily hold allocations before assigning them to xdfile_t struct fields. Instead, it initializes and manages the xdfile_t fields directly throughout the function. This makes the control flow clearer by removing the redundant variable assignments at the end of the function.

Key technical changes:
- Removes 6 local variables (hbits, nrec, hsize, recs, rhash, ha, rchg, rindex)
- Moves xdl_free_ctx() definition earlier in the file to allow its use in xdl_prepare_ctx()
- Maintains identical functionality while reducing code by 17 lines
- Changes error handling to use xdl_free_ctx() consistently

This appears to be a straightforward cleanup that improves code clarity without changing behavior. The patch follows the series' pattern of mechanical but meaningful refactors to prepare the xdiff codebase for potential Rust integration. The change is well-contained to xprepare.c and doesn't introduce any new concepts or dependencies.

The patch builds on the foundation established in patch 1/17 and continues the series' focus on simplifying the xdiff internals before introducing Rust-compatible types in later patches. The direct field access pattern used here could make future type conversions more straightforward.