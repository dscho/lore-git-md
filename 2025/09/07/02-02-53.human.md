This is a user question about `git add` behavior, specifically regarding how it handles invalid pathspecs mixed with valid ones.

The user reports that when running `git add` with a mix of existing and non-existent files (e.g., `git add x bogus_file`), Git fails completely with an error about the invalid pathspec, and none of the valid files are added to the index. They're asking whether this behavior is intentional.

This appears to be a long-standing behavior in Git that may surprise some users, as other commands (like `rm`) typically process the valid arguments even when some are invalid. The email doesn't propose any changes - it's just seeking clarification about current behavior. The question seems well-formed and points to a potentially unintuitive edge case in Git's command-line interface design.

The behavior described matches Git's current implementation, where `add` fails fast on any invalid pathspec rather than processing the valid ones first. This could be worth discussing as a potential UX improvement, though the email stops short of proposing specific changes.