Refactoring patch in a series preparing xdiff for Rust compatibility. This is patch 3/17 in Ezekiel Newren's series, following initial cleanups in patches 1-2.

The patch removes three unused fields from xdiff's internal data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. The changes are straightforward - the fields are deleted from struct definitions in xtypes.h and all related initialization and cleanup code is removed from xprepare.c. The diff shows these were only ever initialized but never actually used.

This continues the series' theme of simplifying xdiff internals before potential Rust integration. The changes are mechanical and appear safe since the removed code had no observable effect. The patch maintains the same functionality while reducing memory usage slightly by eliminating unused hash table storage.

As with the previous patches, this appears to be a non-controversial cleanup that makes the codebase smaller and simpler without changing behavior. It fits well with the series' goal of removing cruft before introducing Rust-compatible types in later patches.