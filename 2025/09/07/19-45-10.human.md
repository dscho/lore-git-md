Refactoring patch in a series preparing xdiff for Rust compatibility. This is patch 7/17 in Ezekiel Newren's series to clean up xdiff internals before potential Rust integration.

The patch simplifies the `xdlclass_t` structure by removing redundant fields (`ha`, `line`, `size`) that duplicated data already stored in the contained `xrecord_t` structure. Instead, it directly uses the `rec` field's members. This continues the series' pattern of eliminating unnecessary indirections and duplicated data, reducing the code by 4 lines while maintaining identical behavior.

The changes are mechanical and focused on internal consistency - all modified code paths now access the data through the `rec` field rather than separate fields. This aligns with the series' goal of making the codebase more straightforward for potential Rust conversion by reducing redundant storage and simplifying type relationships.

Like previous patches in the series, this appears to be a non-controversial cleanup that doesn't affect external behavior. The changes are limited to xdiff's preparation code and maintain the same hash matching logic, just with a cleaner internal representation.