Here's a human-readable summary of the thread so far:

Jon Forrest reported unexpected behavior in `git add` when mixing valid and invalid pathspecs. The command currently fails entirely if any pathspec is invalid, refusing to stage even the valid files. For example, `git add x bogus_file` fails completely rather than staging "x" and only rejecting "bogus_file". This behavior occurs in Git 2.50 on Fedora.

The question is whether this all-or-nothing approach is intentional design or potentially a bug. The report notes this might surprise users who expect partial staging of valid paths. There's no indication yet whether this represents a change from historical behavior or if there's prior discussion about this design choice. The thread appears to be seeking clarification about the intended behavior rather than proposing specific changes at this stage.