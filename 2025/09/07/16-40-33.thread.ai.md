Updated AI summary:

This is part 2 of a 4-patch series refactoring the string-list API's index return values, specifically changing `string_list_find_insert_index()` to use a boolean `exact_match` output parameter instead of negative-index encoding for exact matches. The technical implementation (updating callers in `add-interactive.c`, `mailmap.c`, and `refs.c`) remains sound but requires stronger justification.

Key developments:
- Junio C Hamano maintains that the negative-index convention is well-established and requests clearer motivation, specifically:
  1. Demonstration of why full `size_t` range is needed (currently limited by `int` return)
  2. Explanation of how negative encoding further restricts usable range
  3. Then proposing the boolean parameter solution
- Author shejialuo acknowledges the convention's established status and agrees to restructure the argument as suggested
- No technical changes to the approach - the boolean parameter solution remains the proposed fix
- All callers already adapted in current patch version:
  - `add-interactive.c` and `mailmap.c` check the boolean directly
  - `refs.c` passes NULL when match info isn't needed

Open questions:
- Awaiting revised patch with restructured justification per Junio's request
- Need to clearly show practical limitations of current negative-index approach
- Must demonstrate why boolean parameter is the right solution after establishing range limitations

The thread remains focused on design rationale rather than implementation, with the author now working to better frame the change's motivation while maintaining the technical solution. The patch affects multiple subsystems but requires stronger justification before proceeding.