This is patch 3/17 in a refactoring series preparing xdiff internals for Rust compatibility. The patch removes three unused fields from xdiff data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. 

The changes are straightforward deletions of dead code that was being initialized but never actually used. The patch modifies both the type definitions in `xtypes.h` and their related cleanup/initialization code in `xprepare.c`. The `xdl_classify_record()` function signature is simplified by removing the now-unused hash parameters, and memory allocation for the unused hash structures is eliminated.

This continues the series' pattern of mechanical, non-controversial cleanups that maintain identical functionality while simplifying the codebase. The changes are focused on the xdiff implementation details and don't affect any external interfaces. The patch appears well-scoped and correctly implemented, with test coverage presumably coming from the existing xdiff test suite.

For future AI reference: This touches xdiff/xprepare.c (17 lines removed) and xdiff/xtypes.h (3 lines removed), specifically targeting the xrecord_t and xdfile_t structures. The removed fields were related to an unused hash implementation, with no behavioral impact. The changes are part of the preparatory work for Rust integration by reducing unnecessary C-specific implementation details.