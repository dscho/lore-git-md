Refactoring patch in the xdiff cleanup series for Rust compatibility. This is patch 7/17, continuing the pattern of simplifying xdiff's internal data structures by removing redundant fields.

The patch eliminates three fields from `xdlclass_t` (`ha`, `line`, and `size`) in favor of storing a complete `xrecord_t` structure. This change:
- Removes duplicate storage of hash values and line data that were already available in `xrecord_t`
- Simplifies the code by having one canonical source for these values
- Updates all affected functions to access the data through the `rec` field
- Maintains the same behavior while reducing the code size by 4 lines net

The changes affect `xdl_classify_record()` and related classifier logic in xprepare.c, with straightforward mechanical updates to access the fields through the embedded `xrecord_t`. This continues the series' trend of removing unnecessary indirections and duplications in xdiff data structures.

The patch appears well-contained and follows the established pattern of the series - each change is small, focused, and maintains existing behavior while making the code more straightforward. No tests needed modification, suggesting the changes are purely internal with no observable impact.