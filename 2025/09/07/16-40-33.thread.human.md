This thread introduces a refactoring of the string-list API's index return convention to improve type safety and clarity. The series (currently at part 2 of 4) changes how `string_list_find_insert_index()` signals exact matches - replacing the current negative-index encoding scheme with a cleaner boolean `exact_match` output parameter.

The patch updates all callers across the codebase (including in add-interactive, mailmap, and refs subsystems) to use the new interface. The changes are mechanical but meaningful - callers now check a direct boolean flag rather than decoding negative values, and the refs.c caller can pass NULL when it doesn't need match information. The commit message clearly explains the motivation around API clarity and type safety concerns with the current `int` vs `size_t` handling.

The changes appear well-scoped and maintain the same insertion index semantics while providing a more direct interface. The diff shows careful attention to all affected call sites with no obvious oversights in the conversion. This looks like a straightforward improvement to an internal API used by several Git components.