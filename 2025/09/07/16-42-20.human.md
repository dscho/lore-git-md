Refactoring patch for the string-list API, part 1 of a series addressing sign comparison warnings. This initial change makes the `exact_match` parameter in `get_entry_index()` optional by allowing NULL to be passed when callers only need the index position. The modification is straightforward - it simply adds NULL checks before dereferencing the pointer.

The patch appears well-contained and focused changes, touching only `string-list.c` with minimal diff (4 lines added, 2 removed). It's a sensible first step in the series, making the API more flexible before the subsequent changes that will modify the return type and calling conventions. The commit message clearly explains the motivation (reducing unnecessary variable declarations) and maintains backward compatibility.

This looks like a clean, incremental improvement to the string-list API that shouldn't introduce any behavior changes while making the interface more convenient to use. The change is small enough to be easily while still providing value to callers that don't need the exact match information.