This thread continues the series preparing Git's xdiff internals for potential Rust compatibility through incremental refactoring. The author is systematically restructuring xdiff's core data structures and algorithms to align with Rust's type system while maintaining identical runtime behavior. 

The latest patch (14/17) splits the dual-purpose `ha` hash field in the `xrecord_t` structure into two distinct fields: `line_hash` for the initial content hash and `minimal_perfect_hash` for the unique identifier used during diff matching. This change makes the data flow more explicit and better matches Rust's expectations, affecting five core xdiff files with about 20 lines changed. The modification touches all major diff algorithms (xdiffi.c, xhistogram.c, xpatience.c) but maintains the same diff behavior.

So far the series has progressed without review feedback, focusing on mechanical but careful changes to xdiff's internals. The work builds on prior patches that reorganized code, eliminated redundancies, and began aligning types with Rust's system (like changing size types in patch 13). While these changes are preparatory and don't yet introduce Rust code, they establish foundations that would make future integration smoother by making xdiff's data usage patterns more explicit and its types more compatible with Rust's stricter requirements. The series continues to maintain strict backwards compatibility while incrementally reshaping xdiff implementation details.