This thread presents a 17-patch series refactoring Git's xdiff internals to prepare for potential Rust integration. The author has systematically converted core data structures to use Rust-compatible types while maintaining identical runtime behavior. The changes focus on xdiff's diff algorithm implementation, touching multiple files but primarily xprepare.c and xtypes.h.

The series began with preparatory cleanups (patches 1-9) reorganizing code and eliminating redundant data storage. Patches 10-17 then methodically aligned type definitions with Rust's expectations - converting fields like `nreff` to `usize` and `rchg` to `u8*`. The final patch completes this by adjusting `dstart`, `dend`, `rchg`, and `rindex` field types in `xdfile_t`. Throughout, the author has maintained strict backwards compatibility while making the codebase more amenable to future Rust interop.

Notably, this preparatory work has proceeded without introducing any actual Rust code. The changes demonstrate how Rust's type system expectations would propagate through Git's core diff operations. While the series hasn't attracted reviews yet, it represents a significant step in the broader Rustification effort, showing one approach to incrementally preparing C code for potential Rust integration. The work leaves open questions about platform-specific behavior and long-term maintenance of the type conversions, but provides a concrete foundation for future discussion.