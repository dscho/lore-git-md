This is a refactoring patch that changes the return type of `string_list_find_insert_index()` from `int` to `size_t` to match its underlying helper function `get_entry_index()`. The change is part of a 4-patch series (this being patch 3/4).

The patch updates all callers to use `size_t` variables for storing the return value, with the most notable change being in `mailmap.c` where the loop condition needed adjustment to properly handle `size_t` underflow. The author also removes a `DISABLE_SIGN_COMPARE_WARNINGS` macro from `mailmap.c` that's no longer needed with the proper unsigned types.

The changes are straightforward and mechanical, touching several files including `add-interactive.c`, `mailmap.c`, `refs.c`, and the string-list implementation itself. The patch appears well-constructed with a clear commit message explaining the rationale and showing all necessary call site updates. This seems like a clean improvement that better matches the underlying implementation while eliminating some warning suppression.