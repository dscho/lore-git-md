Refactoring patch in the xdiff Rust compatibility series. This is patch 12/17 in a set preparing xdiff internals for potential Rust integration by making type changes that align with Rust's stricter type system.

The patch changes `xrecord_t.ptr` from a `char*` to a `u8*` (unsigned byte pointer) to better match Rust's expectations for byte buffers. This requires adding explicit casts to `const char*` in several places where the pointer is passed to functions expecting character data, particularly in string comparison and regex matching functions. The changes are mechanical and focused on maintaining existing behavior while making the types more precise.

The patch affects core diff operations across multiple xdiff files, including line comparison, merge conflict detection, and whitespace handling. While the diff appears large (21 lines changed across 7 files), the changes are straightforward type adjustments that don't modify the underlying the diff algorithm.

This continues the series' pattern of incremental, uncontroversial changes that build toward Rust compatibility while maintaining C functionality. The patch follows naturally from the Rust type definitions introduced in patch 10 and their propagation in patch 11.