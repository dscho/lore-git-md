This is a clarification about `git add`'s pathspec behavior in response to a bug report. K Jayatheerth confirms that the current behavior - where `git add` fails entirely if any pathspec is invalid, rather than staging the valid files - is intentional. They point to a comment in `builtin/add.c` that suggests this design was deliberately chosen, though they don't provide historical context about why.

The response is brief and doesn't address potential user expectations or whether this behavior might be reconsidered. It simply states the current implementation matches the intended design. The explanation relies on a code comment rather than documentation or design principles, which may leave some questions unanswered for those seeking a deeper rationale.