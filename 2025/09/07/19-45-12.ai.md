This is patch 9/17 in a refactoring series preparing xdiff internals for Rust compatibility. The patch introduces named constants (NO/YES/MAYBE) for the previously magic-numbered `rchg` array values (0/1/2) that track line change states in the diff algorithm. 

The modification touches several xdiff implementation files (xdiffi.c, xhistogram.c, xpatience.c, xprepare.c) to replace numeric literals with the new symbolic names. The author suggests `rchg` might benefit from being renamed to `changed` but does not implement that change here. 

The patch continues the series' pattern of mechanical, non-controversial cleanups that improve code clarity without altering behavior. It maintains strict backwards compatibility while making the code more self-documenting. The changes are straightforward replacements with no algorithmic impact, affecting only the internal representation of line change states. 

Key technical details:
- Adds `#define NO 0`, `YES 1`, `MAYBE 2` to xdiff.h
- Updates all checks of `rchg` array values to use symbolic names
- Modifies 5 files with 38 insertions and 35 deletions
- Preserves all existing logic, just with more readable conditionals
- Does not yet rename `rchg` field as suggested in commit message

This fits cleanly into the series' progression of removing implementation obscurities that could complicate future Rust integration. The patch follows the established approach of making isolated, verifiable changes that collectively simplify the codebase.