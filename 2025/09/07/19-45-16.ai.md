This is part 13 of the xdiff refactoring series for Rust compatibility, continuing the type system alignment work. The patch changes `xrecord_t.size` from `long` to `usize` to better match Rust's expectations for size types, following the pattern established in previous patches for pointer types.

The change affects string handling throughout xdiff, requiring explicit casts to `(long)` at string function boundaries (like `xdl_blankline()`, `xdl_recmatch()`) where the existing API expects `long` parameters. The modifications span 5 files (xdiffi.c, xemit.c, xmerge.c, xprepare.c, xtypes.h) with 19 lines added and 20 removed, primarily mechanical conversions at call sites.

Key technical points:
- Maintains the same runtime behavior while changing the type system representation
- Follows the established pattern of interface adaptation through casting
- Touches core diff operations, merge logic, and record classification
- Updates the fundamental `xrecord_t` structure definition
- Requires size casts at string function boundaries similar to the pointer casts from patch 12

This continues the series' approach of making minimally invasive changes to align with Rust's type system while preserving existing behavior. The patch demonstrates how Rust's stricter size type expectations propagate through the codebase, similar to the pointer type changes in the previous patch. The changes remain focused on xdiff internals without affecting external interfaces.