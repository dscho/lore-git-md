This thread introduces a focused refactoring to clean up sign comparison warnings in the string-list API, specifically around `string_list_find_insert_index()`. The current implementation uses negative return values to indicate exact matches, which triggers compiler warnings about signed/unsigned comparisons.

The series, authored by [author name would appear here], takes a methodical approach: first making the index parameter optional (patch 1), then introducing an explicit `exact_match` boolean to replace the negative return convention (patch 2), and finally changing the function's return type from `int` to `size_t` (patch 3). A fourth patch enables sign comparison warnings for the refs subsystem to catch any regressions. The changes touch several files but are concentrated in `string-list.[ch]`, with minor updates to callers in `add-interactive.c`, `mailmap.c`, and `refs.c`.

The patches are small, well-separated, and build logically on each other. The approach replaces an implicit convention with explicit parameters while maintaining backward compatibility. This appears to be a straightforward cleanup with no open questions or objections raised so far in the thread.