Refactoring patch in the xdiff Rust compatibility series (4/17). This patch removes the `xdl_get_rec()` helper function from xdiff/xemit.c, which was serving as an unnecessary indirection for accessing `xrecord_t` fields. The change directly accesses `xdf->recs[ri]` fields (`ptr` and `size`) in the four call sites instead.

Key technical details:
- Eliminates 25 lines while adding 13 (net -12)
- Affects three functions in xemit.c:
  - `xdl_emit_record()` (simplified control flow)
  - `match_func_rec()` (direct field access)
  - `is_empty_rec()` (rewritten to use direct field access with loop)
- Maintains identical behavior while removing an abstraction layer
- Part of the series' goal to simplify xdiff internals for future Rust compatibility

The change appears straightforward and mechanical, continuing the series' pattern of removing unnecessary indirections in xdiff code. The commit message clearly states the motivation (making field usage more transparent) which aligns with the series' broader goals. No tests needed modification since this is purely an internal refactoring with no behavior change.