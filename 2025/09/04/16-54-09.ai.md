This is a follow-up discussion about the combine-diff recursive flag change that fixes a bug in `git last-modified`. Junio Hamano raises concerns about backward compatibility for `git diff-tree -c` behavior, noting the patch changes output that has been stable since 2006. The key issue is whether the test should be updated to explicitly show `-r` flag usage while preserving the historical output format showing 'dir/sub' rather than 'dir'. 

The email highlights a tension between fixing the underlying technical issue (which improves performance and correctness for `last-modified`) and maintaining expected behavior for existing `diff-tree -c` users. Hamano suggests the patch should either: 1) retain backward compatibility by keeping recursive behavior for `diff-tree -c` while fixing `last-modified`, or 2) explicitly document the breaking change by showing `-r` in tests while keeping the detailed output format.

This discussion focuses on the user-visible impact of the change rather than the technical implementation. The thread context shows the original patch already addressed several technical issues (BUG() fixes, performance improvements, submodule handling), but this email surfaces an important UX consideration about preserving long-standing behavior versus making intentional breaking changes. The resolution will likely require either modifying the patch to handle the special case or updating documentation to reflect the new behavior.