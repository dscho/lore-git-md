Here's the updated dense AI thread summary incorporating the new discussion:

This thread addresses a bug in combine-diff machinery affecting both the new `git last-modified` command and traditional `git diff-tree -c` behavior. The core problem is `diff_tree_combined()` forcibly setting the recursive flag in diff options since 2006 without justification. The patch removes this behavior, changing combine-diff output to properly respect non-recursive requests, which: 1) Fixes a BUG() in `git last-modified` when processing merge commits, 2) Corrects diff output to show 'dir' instead of 'dir/sub' without -r, 3) Provides 2x performance improvement for `last-modified` on git.git, 4) Requires explicit recursive=1 in submodule.c which relied on the old behavior. 

Junio Hamano raised concerns about backward compatibility for `diff-tree -c` output that has been stable since 2006, creating tension between fixing the technical issue and preserving expected behavior. Two potential resolutions emerged: 1) Maintain backward compatibility for `diff-tree -c` while still fixing `last-modified`, or 2) Explicitly document the breaking change by showing `-r` in tests while keeping detailed output format. The patch currently updates test expectations for diff-tree output patterns and adds a new test case for `last-modified` with merged subdirectories. Performance measurements demonstrate substantial improvements, but the discussion highlights an unresolved UX consideration regarding long-standing behavior versus intentional breaking changes. The thread shows the original patch already addressed several technical issues (BUG fixes, performance, submodule handling), but this follow-up surfaces important questions about output stability that may require either special-case handling or clear documentation of changed behavior.