Refactoring patch removing redundant commit graph check in blame.c. This is the first of a 6-part series moving commit graph handling from object_database to odb_source.

The patch eliminates an explicit check for `sb->repo->objects->commit_graph` in `setup_blame_bloom_data()`, since the subsequent call to `get_bloom_filter_settings()` already handles the NULL commit graph case internally. The change is straightforward - just removing 3 lines that perform a redundant verification before calling the bloom filter setup function.

This appears to be a clean, mechanical change that removes unnecessary code without affecting behavior. The commit message clearly explains the check is redundant and why. The change aligns with the series' broader goal of decoupling commit graph handling from the object database structure. No tests needed as this is a pure refactoring of existing functionality.