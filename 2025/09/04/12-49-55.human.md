This is the first patch in a 6-part refactoring series moving commit graph handling from the object database level to individual object sources. The change is a straightforward cleanup in the blame subsystem, removing a redundant check for the existence of a commit graph. The check is unnecessary because `get_bloom_filter_settings()` (called immediately after) already handles the no-commit-graph case by returning NULL. The patch is small (3 lines removed) and focused, touching only blame.c. It appears to be a safe, low-risk change that simplifies the code without altering behavior. The commit message clearly explains the change and its justification. This looks like a solid first step in the larger series to refactor commit graph handling.