This is a technical follow-up in the Rust infrastructure thread, focusing on build system interoperability concerns. brian m. carlson responds to Patrick Steinhardt's varint.c replacement patch with a specific request about conditional compilation.

The email suggests adding a `RUST` preprocessor define when building with Rust enabled, to allow C code to conditionally include Rust-specific interoperability code paths. The example shows a case where a function would either call Rust code or fail with an error message when built without Rust support.

This builds on carlson's earlier concerns about build system fragmentation, now addressing a concrete technical need that would arise when mixing Rust and C code. The request implies that the current approach of silently falling back to C (as shown in the meson.build diff) may not provide enough control for developers writing hybrid Rust/C interfaces.

The suggestion appears technically sound and aligns with common practices for feature detection in C codebases. It would help address one of the practical challenges of incremental Rust adoption while maintaining backward compatibility. This seems like a reasonable refinement to the proposed build system changes, though it would need to be coordinated with the overall Rust integration strategy.