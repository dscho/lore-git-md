This is a follow-up in the `git-history` feature discussion, specifically about the `split` subcommand's diff visibility issue.

Patrick Steinhardt responds to D. Ben Knoble's report that the `git history split` command isn't showing diffs between the old and new trees during interactive editing. While Patrick confirms the diffs should be visible and that they work in his environment, he acknowledges the wt-status interfaces are tricky to work with. 

Ben provides a detailed reproduction case using Patrick's own branch, showing that even when manually editing hunks or adding new diffs, no status information appears in the commit message template. Patrick confirms he can't reproduce the issue with the same steps, noting the editor should only prompt for the first commit's message while the second commit retains the original content (just with a different parent).

The exchange highlights an environment-specific bug in the `split` subcommand's diff display functionality, with the wt-status interface being the likely culprit. The discussion remains unresolved, with Patrick unable to reproduce the issue locally despite Ben's clear reproduction steps. This suggests the problem may depend on specific system configurations or Git builds.