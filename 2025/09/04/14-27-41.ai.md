This is a refactoring patch that splits header declarations from the interactive add-patch functionality out of "add-interactive.h" into a new "add-patch.h" header. It's part of Patrick Steinhardt's larger RFC v3 series introducing the `git-history` command, though this particular patch focuses on cleaning up the add-patch code organization.

The patch moves all add-patch related declarations (struct add_p_opt, enum add_p_mode, and run_add_p() function) from add-interactive.h to the new add-patch.h. The original header now only contains add-i specific declarations and includes the new add-patch.h. This creates clearer separation between the two subsystems.

Key technical details:
- Creates new add-patch.h header with 26 lines of declarations
- Removes 20 lines from add-interactive.h while preserving its core functionality
- Maintains all existing functionality through proper includes
- Updates add-patch.c to include the new header
- Preserves the ADD_P_OPT_INIT macro and all enum values

This appears to be a straightforward code organization improvement that doesn't change any behavior. The patch is cleanly separated from the main git-history functionality being developed in the series, suggesting it could potentially be submitted independently. The change follows good header organization practices by grouping related declarations together and reducing header bloat.