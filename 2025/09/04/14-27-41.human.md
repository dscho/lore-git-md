Refactoring patch in the `git-history` series. This is part of the preparatory work for the new `git-history` command, specifically cleaning up header file organization between the interactive add and patch subsystems.

Patrick Steinhardt splits out the `add-patch` declarations from `add-interactive.h` into a new `add-patch.h` header. The change moves all `add-patch` related types and functions (`add_p_opt`, `add_p_mode`, `run_add_p`) to the new home while keeping the interactive add-specific declarations in the original header. The patch maintains all existing functionality while making the code organization clearer.

This appears to be a straightforward code organization improvement with no behavior changes. The patch is cleanly separated from the main `git-history` functionality work, suggesting it could potentially be submitted independently if desired. The change follows good header file separation practices by moving related declarations together while maintaining all necessary includes.