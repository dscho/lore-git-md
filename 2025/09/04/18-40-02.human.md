This is a feature patch series (v4) adding enhancements to `git repo info`. Junio C Hamano confirms the latest version looks good and will be queued for the 'next' branch. The series has already been signed off by mentors Karthik Nayak and Patrick Steinhardt in previous iterations.

The two patches introduce:
1. A `-z` flag as shorthand for `--format=nul` (with minor style fixes to callback formatting and operator spacing in v4)
2. An `objects.format` field exposing the repository's object format

The changes in v4 are purely stylistic - adjusting line breaks before braces and operator spacing - with no functional changes from the v3 implementation that had already received approval. The implementation uses standard Git patterns like `OPT_CALLBACK_F` for option parsing and reads the hash algorithm via `repo->hash_algo->name`.

This appears ready to merge, with all technical concerns addressed in previous rounds. The stylistic tweaks in v4 demonstrate the author's attention to detail in following Git's coding conventions. The patches maintain clean separation between the two independent features while sharing common infrastructure where appropriate.