This is a brief response in the ongoing discussion about the `git-history` command proposal, specifically regarding potential `squash` and `fixup` subcommands.

Patrick Steinhardt confirms interest in eventually adding a `squash` subcommand to the `git-history` feature, in response to D. Ben Knoble's suggestion that such functionality could integrate with vim-fugitive's cF/cS keybindings. The reply is brief and doesn't address the technical implementation details or timeline, but establishes agreement on the general direction for future development. This aligns with the thread's parallel discussion track about expanding the command's functionality beyond the currently proposed subcommands (`drop`, `reorder`, `split`, `reword`). The response doesn't mention the `fixup` subcommand specifically, though the context suggests it would likely be part of the same enhancement. The exchange keeps this feature suggestion alive as a future possibility while the main focus remains on resolving the current implementation issues with the `split` subcommand's diff visibility.