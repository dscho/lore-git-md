Thread summary:

Bug report and investigation regarding prepare-commit-msg hook behavior during `git rebase --continue`. The core issue involves environment variables and `-c` config options not being properly propagated to the prepare-commit-msg hook during rebase continuation, despite working in normal commit operations. The original report demonstrates the problem in Git 2.51.0+ with clear reproduction steps showing missing environment variable `$FOO` and config setting `hooks.foobar` during rebase.

Jeff King responds with a detailed test case that successfully demonstrates the expected behavior - both environment and config settings are properly passed to the hook during `rebase --continue` in his environment. His test includes the hook implementation (printing environment/config values), rebase scenario setup (simple branch divergence with content conflict), and command format for passing context during rebase. The discrepancy suggests the original issue may be environment-specific or related to testing methodology differences.

Current status shows conflicting observations between the original report and Jeff's reproduction attempt. The thread now needs either: 1) confirmation from the original reporter about whether Jeff's test case works in their environment, or 2) identification of environmental differences that could explain the behavior variance. The test case serves as both verification tool and potential workaround template. Key technical details remain the hook's access to environment variables and config settings during rebase continuation operations, with focus on the precise conditions under which propagation fails or succeeds.