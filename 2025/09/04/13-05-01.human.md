Feature proposal response in ongoing `git-history` discussion. Patrick Steinhardt responds to D. Ben Knoble's suggestion for adding `squash` and `fixup` subcommands to the proposed `git-history` command. 

This brief reply confirms interest in the feature suggestion while keeping the door open for future implementation. Steinhardt explicitly agrees with the value of a `squash` subcommand, though the response doesn't commit to a timeline or implementation details. The exchange occurs within the broader context of the `git-history` RFC series (now at v3 with 26 patches), which aims to provide Jujutsu-inspired history editing capabilities.

The thread currently has two parallel discussions: troubleshooting a `split` subcommand issue (where some users report diff visibility problems) and this feature expansion discussion. While Steinhardt hasn't yet commented on the `fixup` suggestion specifically, the positive response to `squash` suggests openness to expanding the command's capabilities beyond the initially proposed subcommands (`drop`, `reorder`, `split`, `reword`). The interaction shows community-driven feature development, with Knoble proposing editor-inspired workflows and Steinhardt as maintainer signaling alignment with the direction.