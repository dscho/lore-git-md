Refactoring patch improving type safety in the object-name subsystem.

This small change modifies the `extend_abbrev_len()` function in object-name.c to explicitly declare its second parameter as a `struct min_abbrev_data*` rather than using a `void*` that gets cast internally. The commit message notes that all but one caller already pass the correct type, so this change helps the compiler enforce type safety going forward.

The change is straightforward - removing the internal cast and updating the function signature. The diff shows the function is called in the context of object ID abbreviation handling, where it appears to be part of the logic for determining minimum abbreviation lengths. While the patch doesn't change behavior, it follows good practice by making the interface more type-safe and self-documenting.

This appears to be a clean, focused improvement to the codebase with no obvious downsides. The change is small and low-risk, affecting only internal implementation details rather than any user-facing behavior. The commit message clearly explains the motivation and impact.