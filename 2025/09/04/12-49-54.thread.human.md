The thread continues to explore Patrick Steinhardt's series refactoring commit graph handling for the pluggable ODB effort, now with input from Derrick Stolee that reinforces Junio Hamano's earlier architectural concerns. The series aims to move commit graph tracking from object_database to odb_source through six incremental patches, with the first five making mechanical improvements while the sixth attempts the core architectural shift.

Stolee, drawing on his deep expertise with the commit graph implementation, characterizes commit graphs as "structured caches" that logically span the entire repository rather than being tied to individual object stores. He notes that operations like commit parsing and Bloom filters work at the repository level regardless of which object store contains the data. While supporting the cleanups in patches 1-5, Stolee agrees with Hamano that patch 6's fundamental change may be misaligned with how commit graphs conceptually operate. He suggests a middle path - keeping graph state at the object_database level while using odb_source for I/O operations.

This leaves the series at an impasse on its core premise. The technical discussion has settled some implementation details (like optimizing NULL checks in Bloom filter handling), but the major architectural question remains unresolved: whether commit graphs should remain repository-wide (as Hamano and now Stolee argue) or become source-specific (as the series proposes). With two respected contributors now raising similar concerns about the design direction, the series likely needs significant reworking to reconcile the pluggable ODB goals with the commit graph's repository-spanning nature.