This is a follow-up in the `git-history` feature discussion, specifically about the `split` subcommand implementation. Patrick Steinhardt is responding to D. Ben Knoble's report of a diff visibility issue in the `split` operation.

The core issue is that while the `split` subcommand should show diffs between old and new trees during interactive editing, D. Ben Knoble reports seeing no diff output in the commit template, even when manually editing hunks or adding new diff lines. Patrick confirms this should work and notes it works correctly in his environment, despite acknowledging the wt-status interfaces are complex and error-prone.

Key technical details:
- The problematic code path involves `wt_status_collect_changes_trees()` and `wt_status_print()`
- The issue appears environment-specific - Patrick cannot reproduce using the same branch and commands
- The current implementation only prompts for the first commit's message, keeping the second commit's tree and message identical to the original (only changing its parent)

This exchange highlights an implementation challenge in the `split` subcommand that may need platform-specific debugging. The discussion remains unresolved, with Patrick unable to reproduce the issue locally despite acknowledging the wt-status interface's complexity. This could indicate either an environment-specific bug or a misunderstanding of the expected workflow.