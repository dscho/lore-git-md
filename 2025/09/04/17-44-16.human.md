Bugfix for Git's slab allocator API, finalizing the thread's solution to prevent dangling pointers. This v5 patch from ノウラ | Flare implements the agreed-upon approach after extensive review, now removing the NULL checks in `alloc_state_free_and_null()` per Junio Hamano's final guidance that API violations should fail visibly rather than being silently ignored.

The changes:
1. Renames `allocate_alloc_state()` to `alloc_state_alloc()` for consistency with Git's naming conventions
2. Replaces `clear_alloc_state()` with `alloc_state_free_and_null()` that combines clearing and freeing atomically
3. Updates all callers in object.c to use the new API
4. Removes redundant `FREE_AND_NULL()` calls at call sites

The patch follows Git's API design philosophy where programming errors should be caught rather than handled defensively. The thread shows strong consensus on this approach, with Jeff King and Torsten Bögershausen having reviewed the technical solution positively. This appears ready for merging as the finalizes all discussion points from the thread.