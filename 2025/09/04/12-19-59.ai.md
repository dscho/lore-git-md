This is a discussion about partial clone workflows and tooling, specifically addressing the relationship between proposed functionality and existing tools like `git backfill` and `scalar clone`.

Derrick Stolee clarifies that `git backfill` is designed for blobless clones (fetching missing blobs) but isn't optimized for treeless clones, noting that refilling trees would be particularly expensive. He also points out that blobless clones by default are already available through `scalar clone`, which serves as Git's testing ground for new clone optimizations before they might graduate to mainline Git commands.

The email provides important context about the project's approach to evolving clone behavior - using Scalar as a proving ground for features that may eventually become core functionality, while maintaining `git clone`'s backward compatibility. Stolee suggests `scalar clone` may already be the "big-clone" solution being discussed, though acknowledges discoverability concerns.

Key technical points for future reference:
- `git backfill` exists for blobless clone workflows but not treeless
- `scalar clone` implements blobless-by-default behavior today
- Scalar serves as an incubator for clone optimizations
- Distinction between Scalar's rapid evolution vs core Git's stability requirements

The discussion continues the thread's exploration of partial clone usability improvements while grounding proposals in existing tooling and the project's incremental development philosophy.