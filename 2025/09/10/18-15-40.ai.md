This is a bugfix patch in the submodule gitdir path encoding series, addressing compiler warnings about unused variables in the path encoding implementation.

SZEDER Gábor points out that patch 6/10 introduces several unused variables in `submodule_name_to_gitdir()` that trigger compiler warnings when building with `-Werror`. The variables `name_max`, `encoded_len`, and `base_len` are declared but never used in the function's current implementation. This appears to be leftover from earlier iterations of the path encoding logic.

The issue is straightforward - these variables were likely part of the initial path validation approach that was later refined (as seen in the thread's discussion of path length validation options), but weren't cleaned up from the declarations. The compiler catches this as both unused variables and unused-but-set variables, causing the build to fail with `-Werror` enabled.

This patch would need to be amended to either remove these unused declarations or properly incorporate them into the path validation logic. Given the thread context shows active discussion about path length validation approaches (with three options under consideration), the simpler fix would likely be to remove these declarations until the final validation approach is settled.

The patch doesn't affect the core functionality of the submodule path encoding work, but does need to be addressed to maintain clean builds. It's a minor but necessary cleanup in this larger series that's already undergone significant technical review regarding its path encoding and compatibility mechanisms.