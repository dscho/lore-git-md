This is a status update on the `whatchanged` deprecation series, now acknowledging a memory leak issue that needs to be addressed before merging. The series (kh/you-still-use-whatchanged-fix) consists of 7 patches that update deprecation messaging and add infrastructure for shadowing deprecated builtins with aliases. Key changes include improved user guidance for `whatchanged` users (suggesting `git log` instead) and internal additions like the `deprecated` command category in `git --list-cmds`.

The series had been marked as ready for merging to 'next', but Jeff King identified a memory leak that Kristoffer Haugsbakk now needs to fix. Junio C Hamano confirms he will hold the series until the leak is resolved. The changes touch core command dispatching in `git.c` and include new test coverage in t0014 for builtin shadowing behavior.

This represents both user-facing improvements (better deprecation warnings) and internal plumbing work (the new command_category infrastructure). The memory leak fix will be the critical next step before this can proceed through the integration branches. The series remains part of the active technical work being tracked in the "What's cooking" reports.