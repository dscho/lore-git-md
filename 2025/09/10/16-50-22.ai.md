Classification: Refactoring patch in a signature handling series.

Junio C Hamano provides review feedback on Christian Couder's first patch in the signature handling series. The patch moves `enum sign_mode` and related parsing logic from fast-export.c to gpg-interface.{c,h} to enable code sharing with fast-import.

Key observations from the review:
- The reviewer notes the improved readability of the refactored parse_opt_sign_mode() function in fast-export.c, which now delegates to the new parse_sign_mode() helper
- A minor question is raised about the "unset" codepath behavior in parse_opt_sign_mode(), though the reviewer explicitly states this is out of scope for the current refactoring
- The new parse_sign_mode() implementation in gpg-interface.c is described as "very much expected", indicating it matches the reviewer's expectations
- The reviewer positively notes the clearer visibility of the sign_mode enum definition in the header file

Technical details carried forward:
- The shared enum sign_mode now lives in gpg-interface.h with values: SIGN_ABORT, SIGN_WARN_VERBATIM, SIGN_VERBATIM, SIGN_WARN_STRIP, SIGN_STRIP
- parse_sign_mode() helper function provides consistent parsing of these modes across components
- The refactoring maintains identical behavior while enabling reuse by fast-import

The review concerns are minor and the overall feedback is positive, suggesting the refactoring is on track for acceptance. The reviewer does not raise any objections to the technical approach or implementation details. This positions the series well for proceeding to the feature implementation in patch 2/2.