This is a follow-up email in the ongoing discussion about the `git-history` command proposal, specifically addressing implementation details around commit message handling during history editing operations.

Phillip Wood provides a reference link to a previous discussion about copying commit message headers during rebase operations, which appears relevant to the current `git-history` design. The linked thread shows community pushback against automatically copying certain headers by default during rebase operations - a consideration that may inform how the `git-history` command should handle similar cases.

The email is a minor correction adding a missing reference to Wood's previous message in this thread, rather than introducing new technical content. It connects to the broader discussion about how `git-history` should handle commit metadata during operations like `reword` and `split`, where Phillip Wood has been advocating for careful consideration of existing Git conventions and community preferences.

The reference suggests Wood is continuing his pattern of grounding design suggestions in prior Git mailing list discussions and established behaviors, maintaining his consistent approach throughout this RFC review process. This small addition doesn't change the overall technical landscape but provides concrete context for one of the many design decisions under discussion.