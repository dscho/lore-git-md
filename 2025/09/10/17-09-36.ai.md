Junio C Hamano provides a detailed code review of patch 2/2 in Christian Couder's series adding signature handling to `git fast-import`. The review focuses on the implementation of signature processing in the new `import_signature()` function and its caller `parse_new_commit()`.

Key technical concerns raised:
1. The division of responsibility between `import_signature()` and its caller is questioned, particularly around data stream consumption. The original code always parsed signatures first, while the new version handles parsing differently per mode.
2. The review notes potential maintainability issues - each new case in the switch statement must now manually handle advancing the data stream (via `read_next_command()` and `parse_data()`), which wasn't required before.
3. Junio speculates the change might be to avoid parsing signatures in STRIP mode, but questions whether this optimization is worth the ergonomic cost.
4. A minor suggestion to improve warning message clarity by adding "verbatim" when warning about signature imports.

The review doesn't reject the patch but raises important architectural questions about error handling and future maintenance. This suggests the patch may need revision before merging, though the core functionality appears sound. The feedback continues the thread's technical discussion from patch 1/2, now focusing on implementation details rather than the shared infrastructure aspects.