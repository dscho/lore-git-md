This is a reply in the Rust infrastructure thread, specifically addressing the varint subsystem conversion patch (v4 5/9) that changes integer types to explicit widths. Junio C Hamano provides maintainer feedback on Patrick Steinhardt's changes to use `uint8_t` for length and `uint64_t` for values in the varint encoding/decoding functions.

The reply accepts the technical approach while raising two considerations: First, Hamano notes that while `uint8_t` for length is acceptable (since varints are limited to 16 bytes), it's not strictly necessary. Second, he anticipates future discussions about Rust's `u64` vs C's `uint64_t` when Ezekiel Newren's related work merges, expressing a preference for maintaining `uint64_t` consistency in the C portions of the codebase.

This continues the thread's focus on type safety and platform independence while highlighting upcoming integration points with parallel efforts. The feedback suggests the patch is technically sound but flags stylistic considerations for future cross-language consistency. The reply maintains the series' momentum while documenting a maintainer preference that may influence later decisions in the Rust transition.