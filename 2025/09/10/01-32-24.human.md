This is a continuation of the discussion about refactoring the string-list API to make the `exact_match` parameter optional in `get_entry_index()`. Junio C Hamano pushes back on Ben Knoble's defense of the original patch, questioning the practical scenarios where callers wouldn't need the exact match information. He argues that since callers always need to distinguish between finding an exact match versus an insertion position, the proposed change adds unnecessary complexity to the library code without clear benefit.

The exchange highlights a fundamental API design question - whether to optimize for caller convenience at the cost of slightly more complex implementation code. Junio maintains his position that the current explicit parameter requirement is preferable, as he can't envision realistic cases where callers would want just the index without caring about match status. The discussion remains at an impasse regarding this tradeoff between caller and callee complexity.