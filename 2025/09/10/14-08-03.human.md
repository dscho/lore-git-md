This is a follow-up email in the ongoing discussion about the proposed `git-history` command series. Phillip Wood provides a reference link to a previous discussion about commit header copying behavior during rebase operations, which was accidentally omitted from his earlier message.

The link points to a GitGitGadget pull request discussion where contributors debated whether rebase should copy certain commit headers by default. This context appears relevant to the current `git-history` design discussions around how commit metadata should be handled during history editing operations.

The email itself doesn't introduce new technical content beyond supplying the missing reference, but it connects the current discussion to prior art in Git's rebase implementation. This helps situate the `git-history` proposal within Git's existing history-rewriting behaviors and community norms.