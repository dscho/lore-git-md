Documentation patch in Julia Evans' git-checkout.adoc series (v4 6/7). This patch focuses on restructuring the file restoration section to improve clarity by separating the description of two distinct operations: restoring files from a tree-ish (which updates both index and worktree) versus restoring from the index alone (which only updates worktree).

Key changes:
- Splits the combined file restoration section into two distinct subsections matching the SYNOPSIS structure
- Removes `-f` from the tree-ish form where it doesn't apply (only needed for index->worktree operations)
- Clarifies that tree-ish form always updates both index and worktree
- Updates the patch mode description to reference both preceding forms
- Addresses user feedback about confusing negative phrasing in the original text

The patch maintains all technical accuracy while improving readability by:
1. Making the two file restoration modes visually distinct
2. Removing implementation details that don't affect user behavior
3. Aligning documentation structure to actual command behavior
4. Simplifying sentence structures as noted in the commit message

This represents the final refinement of the file restoration documentation after earlier patches established the technical consensus. The changes are purely organizational/editorial with no behavior changes to the underlying commands.