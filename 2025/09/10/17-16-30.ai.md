This is a refactoring patch that simplifies several callers of `get_oid_with_context()` by switching them to use `repo_get_oid_with_flags()` instead. The change affects multiple commands including `ls-tree`, `rev-parse`, `stash`, and the list-objects filter code.

The key technical change is that `repo_get_oid_with_flags()` provides a simpler interface for callers that only need to specify lookup flags (like `GET_OID_HASH_ANY` or `GET_OID_QUIETLY`) but don't need the additional context information that `get_oid_with_context()` provides via its `object_context` output parameter. The patch removes the need to declare and manage these unused context structures, and eliminates the corresponding `object_context_release()` calls.

The changes are mechanical but widespread, touching 5 files and removing 50 lines while adding 17. The patch appears well-scoped and focused - each change follows the same pattern of replacing the more complex call with the simpler one when the context isn't needed. The commit message clearly explains the motivation and the change is consistent across all affected call sites.

This appears to be a straightforward cleanup that reduces boilerplate without changing behavior. The patch is part of an ongoing effort to simplify object lookup code paths in line with Git's move toward repository-aware APIs.