This is a follow-up to the `kh/you-still-use-whatchanged-fix` series about deprecating the `whatchanged` command. The thread context shows this was a 7-patch series that updated deprecation messages and added infrastructure for shadowing deprecated builtins with aliases, which Junio had marked as ready for merging to 'next'.

Kristoffer Haugsbakk notes they need to address a memory leak that Jeff King (Peff) identified in the series. The original email doesn't include details about the leak itself, but this appears to be a quality-control follow-up before final merging. The series touches core command-dispatching code (`git.c`) and adds new test coverage, so any memory issues would need resolution before integration.

The thread shows active maintenance work on Git's deprecation mechanisms, with this series representing both user-facing improvements (better guidance for `whatchanged` users) and internal plumbing (the `deprecated` command category). The leak fix will likely be a small but important follow-up to ensure the changes don't introduce resource management problems.