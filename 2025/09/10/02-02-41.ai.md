This is a final discussion in the `git subtree` bugfix thread regarding shell script style choices. The technical aspects of the fix have already been resolved in v3, with only minor syntax preferences remaining.

The email clarifies two shell scripting points raised by Phillip Wood's review of v2. Colin explains his use of `${var:-}` syntax as a habit from scripts using `set -u`, though acknowledges it's unnecessary here since `have_mainline` is explicitly defined. Junio Hamano suggests the more precise `${var-}` form would be better for handling truly unset variables without special-casing empty strings.

The thread shows consensus on all substantive changes, with Colin confirming he'll adopt `git switch --orphan` in v3 as suggested. This marks the resolution of the last open questions before the patch can be merged, with only stylistic preferences (the `${var-}` vs `${var:-}` syntax) remaining as a minor point that doesn't affect functionality.

Key technical details referenced include the already-implemented v3 changes: rewritten `should_ignore_subtree_split_commit()` function, removal of non-POSIX `local`, new test helpers, and comprehensive test coverage for nested subtree scenarios. The discussion confirms the fix maintains compatibility with Git 2.44 while properly handling squashed merges under prefixes.