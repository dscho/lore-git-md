This is a follow-up email from Junio C Hamano regarding the second patch in Christian Couder's series adding signature handling to `git fast-import`. The email raises a specific question about scope consistency between the two patches.

Junio notes that while patch 1/2 refactored shared signature handling infrastructure (including both commit and tag modes) into gpg-interface, patch 2/2 only implements commit signature handling in fast-import. He questions whether there was meant to be a third patch adding tag signature support, as the infrastructure work in 1/2 would logically support both.

The email doesn't introduce new technical concerns about the existing implementation, but highlights a potential gap in the series' completeness. It suggests the reviewer expected symmetry between fast-export's signature handling (which covers both commits and tags) and the new fast-import functionality being added.

This appears to be a coordination check rather than a technical critique - Junio is verifying whether the scope limitation to commits in patch 2/2 was intentional or if additional patches were meant to follow. The question implies that supporting both commit and tag signatures would be the natural extension of the refactoring work done in 1/2.

Key details for future thread context:
- The current patch only implements `--signed-commits` in fast-import
- The refactored infrastructure in gpg-interface supports both commit and tag modes
- Review expects symmetry with fast-export's dual signature handling
- Open question whether tag support is coming in a follow-up patch