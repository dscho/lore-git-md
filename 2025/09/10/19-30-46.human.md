This is a bugfix follow-up in the submodule gitdir path encoding series, addressing a minor but important cleanup issue. Adrian Ratiu responds to SZEDER Gábor's report about unused variables (`name_max`, `encoded_len`, `base_len`) in the `submodule_name_to_gitdir()` function that were triggering compiler warnings with `-Werror`.

The author acknowledges the oversight - these variable declarations were left behind when their related logic was moved to a different commit in the series. Adrian confirms the fix will be included in v3 of the patch series, along with additional testing to ensure bisectability. This represents a straightforward cleanup of the implementation that doesn't affect the core functionality but maintains code quality standards.

The response shows good maintainer hygiene - promptly addressing reviewer feedback about compiler warnings, even when they don't impact runtime behavior. This keeps the series clean as it moves toward finalization. The fix aligns with the thread's overall progress toward a robust implementation of submodule path encoding.