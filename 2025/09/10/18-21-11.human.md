This is a follow-up email from Junio C Hamano regarding Christian Couder's feature patch series adding signature handling to `git fast-import`. The thread concerns the second patch which implements a `--signed-commits=<mode>` option mirroring fast-export's behavior.

Junio notes an apparent inconsistency - while the series' first patch refactored shared signature handling infrastructure (including tag-related code), the second patch only implements commit signature controls without addressing tags. He asks whether there's a missing third patch that would complete the feature by adding tag support, or if this was an intentional design choice to limit scope.

The question highlights an important architectural consideration for the series - whether the signature handling should be symmetrical between commits and tags from the start, or if tag support could reasonably follow later. This could affect how reviewers evaluate the current patch's completeness and future maintenance implications.

The email doesn't reject the approach but seeks clarification on the design intent, suggesting the thread may continue with either a justification for commit-only support or an acknowledgment that tag handling should be added.