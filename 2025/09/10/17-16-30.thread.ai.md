Thread identity: Refactoring patch simplifying callers of `get_oid_with_context()` by switching to `repo_get_oid_with_flags()` where context isn't needed. Authored by [author name to be filled in from actual email].

Core problem/goal: Reduce boilerplate in object lookup code by using simpler `repo_get_oid_with_flags()` API when callers don't need the additional context information provided by `get_oid_with_context()`. This continues Git's move toward repository-aware APIs.

Current status: Initial version of refactoring patch affecting multiple commands (`ls-tree`, `rev-parse`, `stash`, list-objects filter). Changes are mechanical and behavior-preserving.

Key technical details:
- Replaces `get_oid_with_context()` calls with `repo_get_oid_with_flags()` when `object_context` output parameter isn't used
- Removes unused `struct object_context` declarations and corresponding `object_context_release()` calls
- Affects 5 files: builtin/ls-tree.c, builtin/rev-parse.c, builtin/stash.c, list-objects-filter.c, object-name.c
- Net reduction of 33 lines (50 removed, 17 added)

Participant positions: [No reviews or responses yet in this initial submission]

Related work: Part of broader effort to simplify object lookup code paths and migrate to repository-aware APIs. Connects to `the_repository` removal project and ODB abstraction work.