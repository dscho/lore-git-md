This thread centers on Christian Couder's work to add signature handling controls to `git fast-import`, mirroring existing functionality in `git fast-export`. The first patch, which refactored shared infrastructure, has already been merged after positive reviews from Junio and Patrick Steinhardt. The second patch, now in its second version, implements the actual `--signed-commits` option with several modes (abort, strip, verbatim, and warning variants).

Christian has been responsive to feedback, incorporating all technical suggestions from the reviews. The V2 changes include moving signature handling logic inline for better code flow, maintaining consistent parsing behavior across modes, and improving warning messages. The implementation carefully preserves fast-import's streaming nature while adding signature awareness, with comprehensive test coverage in t9305. 

Junio and Patrick have both engaged closely with the technical details, particularly around error handling patterns and code organization. While the infrastructure could support tag signatures, the current scope remains limited to commits - a deliberate choice that reviewers have accepted. The patch appears ready for merging, with any broader considerations about future extensions (like tag support or re-signing capabilities) deferred for later work. This represents a focused, well-executed addition to Git's repository filtering toolchain.