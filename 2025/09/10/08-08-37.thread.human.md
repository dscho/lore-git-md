Christian Couder's patch series to add signature handling controls to `git fast-import` has progressed through thorough review and is now in its second version with all feedback addressed. The series builds on existing `git fast-export` capabilities, starting with infrastructure refactoring (already merged) followed by the feature implementation for commits (now in V2).

The latest discussion clarified some technical details around signature parsing behavior. While the code can technically handle two signatures (SHA-1 and SHA-256), Christian confirmed the implementation properly warns and ignores duplicate signatures of the same type, matching `fast-export`'s behavior. Junio had noted some confusion about a comment regarding this, which Christian removed in V2 as it wasn't adding clarity.

The series has received positive reviews from both Junio and Patrick Steinhardt, with all technical concerns addressed in V2. The implementation now features inlined signature handling, consistent parsing behavior, improved warning messages, and comprehensive test coverage. Christian has deliberately scoped the current work to commit signatures only, with tag handling planned as follow-up work. With all review feedback incorporated and no outstanding issues, the series appears ready for merging.