Christian Couder has proposed a two-patch series to add signature handling controls to `git fast-import`, matching the existing capabilities in `git fast-export`. The first patch refactors shared signature handling infrastructure by moving the `enum sign_mode` and parsing logic to gpg-interface.{c,h}, creating a parse_sign_mode() helper function for consistent behavior across commands.

Junio C Hamano has reviewed the infrastructure refactoring (patch 1/2) and found it sound, with only a minor question about an out-of-scope codepath. The implementation matches his expectations, particularly noting the improved readability of the refactored code and clearer visibility of the enum definition. This positive review suggests the series is on track for acceptance.

The second patch, which implements the actual `--signed-commits=<mode>` option in fast-import with comprehensive test coverage, has not yet been reviewed. The infrastructure work appears solid, positioning the feature implementation well for consideration. The series will enable more flexible repository filtering operations with signature preservation controls.