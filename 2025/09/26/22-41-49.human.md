Refactoring patch in the xdiff internals cleanup series. This is part of Ezekiel Newren's ongoing effort to modernize Git's diff algorithm implementation, with a focus on simplifying memory management and improving code structure.

The patch eliminates redundant local variables in `xdl_prepare_ctx()` that were essentially duplicating the work of the existing `xdl_free_ctx()` function. Instead of managing separate variables for hash tables, change markers, and record arrays, the code now initializes and frees these directly through the `xdfile_t` structure members. This makes the memory management more straightforward by having a single consistent way to allocate and free these resources.

The changes are purely structural - no behavior is altered. This continues the series' pattern of removing duplicate code paths while maintaining the exact same diff algorithm semantics. The patch follows naturally from previous work in the series that simplified xdiff's memory handling, and it helps set the stage for potential future Rust integration by reducing complexity in the C codebase.

This appears to be a well-justified cleanup that reviewers have already vetted in previous iterations of the series. The implementation looks clean and focused, with the changes limited to the intended scope of removing redundant variable management.