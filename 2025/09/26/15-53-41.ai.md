This is a refactoring patch in Adrian Ratiu's hook subsystem modernization series, specifically addressing the post-rewrite hook conversion in sequencer.c. The patch replaces custom child_process handling with the new `hook.h` interface, using a `pipe_from_strbuf` helper for stdin feeding.

Key technical elements include:
- The `pipe_from_strbuf` helper function that writes hook input via a strbuf
- Removal of redundant signal handling now managed by run-command.c
- A leftover variable rename (`child` to `notes_cp`) from prior work that slipped through

Adrian responds to Phillip Wood's review feedback by:
- Acknowledging the `pipe_from_strbuf` implementation can be simplified to a single write rather than recursive callbacks
- Explaining the parallel execution model and stdin feeding tradeoffs
- Noting the strbuf reset is unnecessary
- Committing to fix style issues and remove the accidental variable rename in v2

The discussion reveals important implementation details about:
- Parallel hook execution characteristics
- Stdin feeding performance considerations
- Edge cases around empty input handling
- The balance between batching and responsiveness in parallel contexts

This patch continues the series' pattern of substantial code reduction (62 -> 38 lines here) while maintaining functionality. The review exchange demonstrates the technical depth of the hook modernization effort, particularly around parallel execution semantics and performance implications.