Documentation formatting patch (part 2/4) with maintainer feedback on AsciiDoc syntax changes. Junio C Hamano responds to Ramsay Jones's RFC patch that standardizes the formatting of `remembering-renames.adoc`, asking for clarification about which specific AsciiDoc warnings the changes address.

The maintainer notes two key syntax changes in the patch: (1) removal of periods after outline numbers (e.g. "0." -> "0") which Junio finds initially awkward though acceptable if it's standard practice, and (2) demotion of subsection headers from `===` to `==` level which appears correct given the document structure. Junio also comments on the improved readability of bullet-point examples with added blank lines, though this wasn't flagged as a warning fix.

The exchange reveals the patch is part of an RFC series (position 2/4) but still lacks context about the overall series goal. Technical content remains unchanged - the document still explains Git's rename detection caching during rebase/cherry-pick, including interactions with directory rename detection and edge cases where caching breaks down. The changes appear mechanically sound but the maintainer seeks explicit rationale for the syntax choices to properly evaluate them.