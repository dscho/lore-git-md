Documentation patch fixing formatting in commit-graph.adoc. This is part 4 of a series (though no cover letter context is provided to understand the series' overall purpose).

The patch makes several formatting improvements to the commit-graph technical documentation:
- Converts top-level headings from underline-style (`====`) to atx-style (`=`)
- Adds missing blank lines around lists and between sections
- Standardizes code block delimiters to use `----` consistently
- Fixes some inconsistent heading levels (changing `##` to `===` where appropriate)
- Escapes curly braces in a section title (`{hash}` -> `\{hash\}`)

The changes are purely presentational - no technical content is altered. The patch appears well-scoped and correctly implements standard Git documentation conventions. The commit message is brief but adequate for a straightforward formatting cleanup.