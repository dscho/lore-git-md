This is a brief follow-up in the ongoing discussion about `git whatchanged` usage. Kristoffer Haugsbakk responds to Neal Miller's note about still using the command by suggesting a replacement using `git log` with specific options (`--no-merges --raw`). The email also mentions alternative output formats (`--stat` or `--name-only`) that might be more readable than `--raw` for some use cases. The message doesn't introduce new technical depth but provides a practical migration path for users still relying on the deprecated `git whatchanged` command. The thread context shows this is part of a broader discussion about modernizing Git command usage.