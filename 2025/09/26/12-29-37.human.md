Refactoring patch in Adrian Ratiu's hook subsystem modernization series, addressing Junio's documentation feedback.

This is the third patch in a 10-part series converting Git's hook infrastructure to use the new `run_hooks_opt` interface. It focuses on the 'post-rewrite' hook in sequencer.c, replacing custom `child_process` setup with the standardized API. The change removes redundant signal handling since `run_hooks_opt` now handles SIGPIPE internally.

Junio provided detailed feedback on the commit message wording, noting it incorrectly framed the change as "using hook.h" rather than the more accurate description of replacing custom process handling with `run_hooks_opt`. Adrian acknowledges the feedback and plans to reword the message in v2, including a clearer explanation of the signal handling benefits.

The patch continues the series' pattern of simplifying hook calls while maintaining functionality. Like other conversions in the series, this shows significant code reduction (though exact line counts aren't provided for this specific patch). The technical implementation appears sound, building on the established `run_hooks_opt` pattern seen in the series.

This exchange demonstrates the careful review process for interface changes, where even technically correct patches receive scrutiny for clear communication of what's changing and why. Adrian's responsiveness to documentation feedback suggests the series is progressing well through review.