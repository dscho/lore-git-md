Refactoring patch as part of a series introducing reftable consistency checks. This moves a verbose-mode status message from the files backend to the generic refs layer, allowing both files and reftable backends to share the same output behavior.

The patch relocates the "Checking references consistency" message from files-backend.c to refs.c, where it becomes part of the refs_fsck() wrapper function that delegates to backend-specific implementations. The change is straightforward - the message is now printed before calling the backend's fsck method, maintaining the same conditional on o->verbose.

This appears to be a clean refactoring with no behavior change for the filesystem backend, while preparing for consistency checks in the reftable backend (which will be introduced in later patches). The commit message clearly explains the change's purpose and the diff shows proper removal of the old code alongside the new placement.

Key technical details for future reference:
- The message is now in refs.c's refs_fsck() function
- Still conditional on fsck_options->verbose flag
- Uses fprintf_ln(stderr) for output
- Part of a larger reftable consistency check effort (v4 2/7)
- Touches refs.c and refs/files-backend.c
- No functional changes to existing behavior