Refactoring patch in the xdiff internals modernization series. This patch (v6 06/12) removes the now-redundant `diffdata_t` struct and updates all callers to directly use `xdfile_t` fields instead. The struct's fields were simple aliases (`nrec`->`nreff`, `ha`->`ha`, `rindex`->`rindex`, `rchg`->`rchg`) that added no value while complicating the code structure. The change affects `xdiff/xdiffi.c` and `xdiff/xdiffi.h`, removing 33 lines while adding 10 (net -23).

Key technical details: The patch modifies the `xdl_recs_cmp()` function signature and all call sites to accept `xdfile_t*` instead of `diffdata_t*`, eliminating the temporary `dd1`/`dd2` variables in `xdl_do_diff()`. The diff algorithm behavior remains identical - this is purely a code structure improvement. The change aligns with the series' broader goals of simplifying xdiff's internal structure by removing redundant indirections and obsolete constructs, while maintaining all existing functionality. This was likely one of the earlier planned cleanups in the series given how straightforward the field aliasing was.

The patch appears well-contained and low-risk, touching only internal xdiff interfaces. It follows the established pattern of this series by removing unnecessary abstractions that complicate the code without providing benefits. The commit message suggests this struct was kept for historical compatibility but is no longer needed. No performance impact is expected since the change just removes an indirection layer.