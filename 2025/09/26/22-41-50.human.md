This is part of Ezekiel Newren's ongoing xdiff refactoring series (v6 03/12), cleaning up unused fields in the diff algorithm's internal data structures. The patch removes three fields that were initialized but never actually used: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. 

The changes are straightforward deletions with no behavior impact - the fields appear to be legacy from earlier implementations that used chained hashing, but the current code doesn't reference them. The patch updates `xdl_classify_record()` to no longer maintain the unused hash chain, and removes related initialization code in `xdl_prepare_ctx()`.

This continues the series' theme of simplifying xdiff internals by removing dead code and redundant structures. The changes are mechanical and low-risk since they only touch unused code paths. The patch follows the series' pattern of maintaining identical diff behavior while improving the code structure.