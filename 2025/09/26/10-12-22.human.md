Bugfix for `git add -p` hunk splitting behavior. This is the final email in a thread where Phillip Wood and Junio Hamano have been discussing changes to how split hunks are handled in the interactive patch interface. The patch series has already been approved for merging.

The key change makes split hunks always start in an "undecided" state rather than inheriting their parent hunk's selection status. This requires users to explicitly select or reject each split hunk, which Junio Hamano agrees provides clearer interaction semantics. Phillip confirms one minor edge case about navigation between split hunks, but this doesn't affect the overall approach.

The technical implementation is complete with all design questions resolved - there's no version-gating or runtime configuration, just a straightforward behavior change. The patch includes test coverage verifying the new behavior. Junio has indicated he's happy to take the patches as-is, and Phillip is just confirming whether any commit message tweaks are needed before merging.

This represents the conclusion of the discussion, with the maintainer-approved solution ready to be merged into Git's codebase. The change improves UI clarity in `git add -p` by making hunk selection more explicit after splitting.