This is a final administrative check for Phillip Wood's `git add -p` hunk splitting behavior changes, now approved for merging. The email captures Junio Hamano's thoughts on potential future improvements to the interactive patch interface, while confirming the current patch series is ready to go in as-is.

The discussion focuses on two remaining UI limitations in `git add -p` that aren't addressed by the current changes but could be future work: (1) the inability to revisit already-decided hunks/files without restarting the session, and (2) lack of visual indication for a hunk's current selection state. Junio suggests these could be tackled separately as `#leftoverbits`.

Key technical context from the thread remains relevant: the series unconditionally marks split hunks as `UNDECIDED_HUNK` in `add-patch.c`, requiring explicit user selection. The implementation has been through multiple iterations and now has maintainer approval with no configuration options, version gates, or conditional logic.

Junio confirms the patches can be taken as-is, though he muses about whether the commit message could better explain how the change improves the second UI problem (hunk state visibility). The thread shows consensus that while there's room for further UI refinement, the current changes represent a clear improvement to hunk splitting semantics without introducing maintenance complexity.

This concludes the administrative review with no changes requested - the series is cleared for merging in its current form, with the noted UI limitations remaining as potential future work items.