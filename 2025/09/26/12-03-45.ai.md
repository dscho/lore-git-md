This is a follow-up discussion in Adrian Ratiu's hook subsystem refactoring series, specifically addressing feedback on patch 2/10 which introduces stdin feeding via callback. The patch adds a `feed_pipe_fn` callback mechanism as an alternative to `path_to_stdin` for more efficient streaming of large data to hooks.

Key technical points from the exchange:
- Junio notes the undocumented precedence of `path_to_stdin` over `feed_pipe` in the current implementation
- Adrian agrees to add a `BUG()` check for mutually exclusive stdin feed methods in v2
- The discussion confirms the pipe allocation behavior when `feed_pipe` is used (`.in = -1` signaling pipe creation)
- Documentation improvements are promised for the overly-wide comment and missing precedence explanation

This interaction continues the thread's pattern of Junio focusing on interface documentation quality, with Adrian promptly addressing the feedback. The technical implementation remains sound, with this exchange resolving one of the previously noted documentation gaps in the series. The change will appear in v2 with clearer contracts around stdin feeding methods.