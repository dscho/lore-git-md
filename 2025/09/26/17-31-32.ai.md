Documentation refinement discussion about `push.default=simple` behavior.

This email continues the discussion about how to accurately document the behavior of `push.default=simple` in the `git-push` man page. Julia Evans responds to Junio Hamano's clarification that `simple` mode is a constrained variant of `upstream` mode that enforces name matching rather than determining push targets. She notes an edge case where `simple` mode can push to a same-named branch without an explicitly configured upstream, contrary to her initial understanding.

The discussion reveals a subtle behavior in Git's implementation: while `simple` mode generally requires an upstream configuration, it can fall back to same-name pushing in certain conditions (as changed in commit ed2b18292b). Julia shares her experimental patch attempting to make `simple` mode consistently require upstream configuration, though she acknowledges uncertainty about whether the current behavior was intentional.

Junio's proposed documentation wording emphasizes that `simple` mode follows `upstream` behavior with added name-matching constraints, rolling the "safety measure" explanation into the main description. The thread shows careful attention to accurately documenting behavior that has been technically settled in the code, with Julia continuing to refine the pedagogical presentation post-merge. The key technical point is that `simple` mode's primary role is enforcing name matching constraints on push targets rather than determining those targets.