This is a refactoring patch in the xdiff modernization series, specifically addressing variable aliasing in xdiff/xemit.c. The patch is part of a larger effort to clean up xdiff internals for Rust compatibility and improved code structure.

The change eliminates local variables that were aliasing fields from xrecord_t structs, instead directly accessing the struct fields. This affects several functions in xemit.c:
- xdl_emit_record() now uses xrecord_t* directly rather than extracting ptr/size
- match_func_rec() similarly uses the xrecord_t directly
- is_empty_rec() is rewritten to use direct struct access with a more compact loop

The modifications maintain identical behavior while making the code more consistent with the rest of the xdiff codebase's patterns. This follows the series' broader goals of simplifying the code structure and removing unnecessary indirections.

Key technical details:
- Changes are mechanical and behavior-preserving
- Touches only xdiff/xemit.c
- Removes 16 lines while adding 13 (net -3)
- Part of systematic elimination of redundant variables and indirections
- Follows pattern established in earlier patches of the series
- No functional impact on diff algorithm or output

The patch appears well-contained and low-risk, continuing the series' pattern of straightforward structural improvements. It builds on previous work that established direct struct access as the preferred pattern in xdiff internals.