This is a follow-up in the ongoing discussion about replacing the deprecated `git whatchanged` command. Junio C Hamano responds to Kristoffer Haugsbakk's suggested replacement commands (`git log --no-merges --raw` and alternatives) with a minor but practical observation: the `--no-merges` option may not be worth including in the replacement command due to its verbosity, implying the simpler `git log --raw` might suffice for most users.

The email continues the thread's technical discussion about command-line ergonomics rather than introducing new arguments about the deprecation itself. It doesn't challenge the core premise that `git whatchanged` should be replaced with `git log` variants, but offers a subtle refinement to the suggested migration path by questioning whether one of the flags is truly necessary for typical use cases.

Key technical detail for future reference: the discussion now includes consideration of whether `--no-merges` should be part of the standard replacement recipe, with Junio suggesting it may not provide enough value to justify its inclusion in the basic substitution pattern. This builds on the thread's established alternatives of `--raw`, `--stat`, and `--name-only` as output format options.