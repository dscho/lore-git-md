This is a refactoring patch that's part of a larger series introducing consistency checks for the reftable backend (patch 2/7). The change moves a consistency check message from being files-backend specific to the generic refs layer, allowing both files and reftable backends to use it.

The patch is straightforward - it takes the "Checking references consistency" message that was previously printed only in the files backend's fsck implementation and moves it to the generic `refs_fsck()` function. This ensures the message will be shown regardless of which backend is being used, as long as verbose mode is enabled.

The implementation looks clean - it adds the message printing to `refs.c` while removing the duplicate from `files-backend.c`. The change is small (4 lines added, 2 removed) and well-contained. Since this is part of a larger series about reftable consistency checks, this change helps maintain consistency between the two backends' user-facing behavior.