This is a brief follow-up in the `git whatchanged` discussion thread. Kristoffer Haugsbakk responds to Neal Miller's comment about still using `git whatchanged` by suggesting a replacement command using `git log` with equivalent options (`--no-merges --raw`). The email also offers alternative output formatting suggestions (`--stat` or `--name-only`) that might be more readable than `--raw` for some use cases.

The message doesn't introduce new technical discussion points but provides a helpful command substitution for users who still rely on the deprecated `git whatchanged` command. The tone is friendly and practical, offering clear alternatives without judgment about the older command's usage.