Refactoring patch removing unused headers in refs subsystem. This is part 1 of a 7-part series (v4) introducing reftable consistency checks, though this particular patch is a simple cleanup.

The patch removes three unnecessary header includes from refs backend files:
- `string-list.h` from refs/debug.c
- `object-file.h` from refs/files-backend.c
- `lockfile.h` from refs/reftable-backend.c

The changes are straightforward - each removed header was included but not actually used in its respective file. The patch follows Git's typical style for such cleanups, with minimal explanation needed since the removals are self-evident. The diff shows no functional changes, just removal of unused dependencies.

This appears to be preparatory work for the larger reftable series, cleaning up the codebase before adding new functionality. The changes touch multiple refs backends (files, reftable, and debug infrastructure), suggesting the author is working across the refs subsystem. No tests are included since this is purely a code organization change with no behavioral impact.