This is part of the ongoing xdiff internals refactoring series (v6 10/12) that modernizes Git's diff algorithm implementation. The patch renames the `rchg` field in `xdfile_t` to `changed` to better reflect its purpose - it tracks which lines in a file have been modified. 

The change is mechanical but meaningful, touching multiple xdiff implementation files (`xdiffi.c`, `xhistogram.c`, `xpatience.c`, etc.) to update all references to the renamed field. The commit message notes this is preparatory work for a follow-up change that will convert the field's type from `char` to `bool` for additional clarity.

As part of Ezekiel Newren's comprehensive xdiff cleanup series (now in its 6th iteration), this patch continues the effort to improve code readability and type safety while maintaining identical diff behavior. The series has already addressed all prior review feedback and is now in its final form, with this patch representing one of the last naming refinements before completion.