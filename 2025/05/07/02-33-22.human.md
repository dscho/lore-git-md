Bugfix for type confusion in parse-options machinery.

This is the second patch in a series addressing issues in Git's option parsing code. It fixes a case where an integer was incorrectly used where a boolean was expected in the test helper for unknown option handling. The changes include:

1. Correcting the type of variables `a` and `b` from `bool` to `int` in the test helper to match the OPT_BOOL macro's expectations
2. Cleaning up formatting and whitespace in parse-options.[ch] and the test file
3. Adding a missing return statement in the test helper

The patch maintains the existing behavior while making the types correct and the code more consistent. It's a straightforward fix that aligns with the series' goal of cleaning up rough edges in the option parsing subsystem. The changes are limited to test code and internal implementation details, so they shouldn't affect users directly.