This is a continuation of Patrick Steinhardt's series refactoring the object database (ODB) code to remove `the_repository` dependencies, specifically addressing iterator functions in patch 9/17. The patch converts `for_each`-style functions to take an explicit `object_database*` parameter, introducing new callback types `odb_for_each_backend_fn` and `odb_for_each_alternate_ref_fn`.

Derrick Stolee raises a naming concern about the term "alternate ref" in the context of the ongoing ODB terminology restructuring. He notes this term is tied to existing config keys (`core.alternateRefsCommand` and `core.alternateRefsPrefix`) and questions whether it should be updated to align with the new backend/shard/slice terminology being established. While acknowledging the difficulty of changing this established term, Stolee suggests keeping "alternate" as a way to differentiate between a repository's primary ODB and borrowed ones.

This discussion highlights the tension between maintaining backward compatibility with existing configuration and achieving consistent terminology in the refactored codebase. The feedback continues the pattern seen throughout the series where mechanical conversions (like removing `the_repository`) proceed smoothly, while naming decisions require careful consideration of both technical architecture and historical context. The thread's focus remains on establishing clear distinctions between container (`object_database`), instance (still being designed in patch 2/17), and backend concepts.