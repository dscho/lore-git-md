This is a response in the ongoing discussion about adding trailer-related options to `git rebase` and `git am`. Phillip Wood weighs in on Li Chen's RFC proposing both a dedicated `--reviewby` flag and a more general `--trailer` option.

Phillip argues against adding the specialized `--reviewby` option, suggesting the general `--trailer` mechanism (proposed in patch 2) would be sufficient. He notes that `--signoff` exists largely for historical reasons and that Git shouldn't add project-specific flags when a flexible alternative exists. His position aligns with earlier architectural concerns raised by Junio Hamano about code duplication.

The email doesn't introduce new technical details but reinforces the thread's central debate: whether to prioritize convenience flags for common cases (`--reviewby`) versus maintaining a single, flexible interface (`--trailer`). Phillip plans to review the second patch (implementing `--trailer`) separately, suggesting the discussion will continue with more concrete implementation feedback.