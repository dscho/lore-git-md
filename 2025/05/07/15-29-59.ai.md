This is a follow-up in the ongoing discussion about fixing flaky tests in t6011-rev-list-with-bad-commit.sh. Junio C Hamano responds to Patrick Steinhardt's acknowledgment of the earlier patch that added `chmod +w` before packfile edits. The exchange includes some typo corrections from Patrick regarding Junio's original message (fixing "rewrite many use" to "rewrote many uses" and "new" to "knew").

The core technical point remains that the test needs write permissions on packfiles before modifying them, which the `chmod +w` addition properly addresses. While Patrick has accepted this solution, the deeper question about using `sed` versus Perl for binary file manipulation (raised in Junio's previous email) remains unaddressed in this response. The interaction shows consensus on the immediate permission fix while leaving the binary editing methodology question open for future discussion.

Key details carried forward: the test modifies packfiles to corrupt commit objects, originally using Perl but converted to `sed` in commit cdbdc6bf. The thread has established that `chmod +w` is the correct approach for permission handling, regardless of which editing tool is ultimately chosen. The typo corrections suggest careful review of the historical context being discussed.