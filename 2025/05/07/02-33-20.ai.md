This is a bugfix series addressing memory leaks and type safety issues in the parse-options machinery. The cover letter indicates three patches that: (1) fix an xstrdup leak in short option parsing, (2) correct a boolean/int misuse, and (3) add strvec_push_wrapper to prevent UBSan failures.

The series touches core option parsing code in parse-options.[ch] and adds a new test helper (test-free-unknown-options.c) to verify the fixes. The changes appear focused on edge cases in option parsing where memory management and type safety were previously problematic. The test additions in t0040-parse-options.sh suggest the author is adding coverage for these specific failure modes.

Key technical details: The patches introduce a strdup-like function parameter to parse_opt_ctx to control memory allocation, replace an incorrect int-for-bool usage, and add wrapper infrastructure to prevent undefined behavior in string vector operations. The changes are contained to the option parsing subsystem but modify its core interfaces (adding new parameters and helper functions).

This looks like a focused cleanup of some long-standing rough edges in the option parsing code. The series appears well-structured with each patch addressing a distinct issue and including test coverage. The memory leak fix in particular seems likely to be non-controversial, while the interface changes (adding new parameters) might warrant closer review for backward compatibility implications.