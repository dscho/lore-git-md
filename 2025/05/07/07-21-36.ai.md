Final patch series implementing the last two maintenance tasks needed to fully replace `git gc`. This v5 iteration focuses on code cleanup and memory leak fixes while maintaining the simplified rerere GC condition check from v4.

The series now:
1. Drops the `get_worktree_names()` helper in favor of direct directory operations
2. Fixes a memory leak in `worktree_prune_condition()`
3. Further simplifies the worktree pruning logic
4. Maintains all previous functionality including:
   - New `maintenance.worktree-prune.auto` and `maintenance.rerere-gc.auto` configs
   - Compatibility with existing `gc.worktreePruneExpire`
   - Preserved child process spawning
   - Comprehensive test coverage

Key technical details:
- Worktree pruning now uses direct `opendir()`/`readdir()` instead of the removed helper
- Fixed `wtpath` memory leak in early-exit path of worktree pruning
- Simplified rerere GC condition remains as existence check (any positive auto value triggers if rr-cache has entries)
- All preparatory refactoring patches remain (indentation fixes, global var removal, function extraction)
- Test coverage includes edge cases and config validation
- Documentation updates for new configs and commands

The series appears technically complete with all identified issues addressed, positioning this as the final version before potential merging. The changes since v4 are focused on implementation cleanliness rather than functional changes.