This is a brief technical discussion about the proposed `--reviewby` and `--trailer` options for `git rebase` and `git am`. Kristoffer Haugsbakk responds to Phillip Wood's suggestion that the general `--trailer` interface could be enhanced to use the committer identity when no value is specified, which would cover the `--reviewby` use case without needing a dedicated flag.

Kristoffer raises two concerns: first, that automatically using committer identity for trailers might be confusing when trailers are used for non-identity metadata, and second, questions why `git var GIT_COMMITTER_IDENT` includes timestamp information. The email doesn't take a strong position on the main design debate but adds implementation considerations to the discussion.

This continues the thread's central tension between specialized convenience flags versus a more general-purpose approach, with Phillip advocating for the latter and others raising practical concerns. The exchange remains technical and constructive, focusing on how to best implement the functionality regardless of which approach is chosen.