This is a follow-up discussion in the finalized v2 series enhancing promisor-remote protocol validation. Patrick Steinhardt suggests further structural improvements to the implementation now that the protocol fields are strictly limited to "name", "url", "partialCloneFilter", and "token".

The key proposal is to replace the generic string_list-based field storage in `struct promisor_info` with dedicated struct members for each known field type. This would eliminate the need for positional assumptions (like the BUG() check enforcing 'name' as the first field) and make the code more type-safe. The suggestion comes after the series has already addressed major protocol design concerns and standardized on a constrained set of fields.

Patrick notes this change would simplify operations like `remote_nick_find()` by removing index-based field access. The thread context shows this is a refinement suggestion rather than a blocking concern - the v2 series has already resolved core protocol issues around field handling, case sensitivity, and backward compatibility through the string_list approach.

This represents potential future cleanup rather than a required change for the current series, which has already incorporated Patrick's earlier feedback on field handling strategy. The discussion indicates the implementation could be made more maintainable while maintaining the same protocol behavior.