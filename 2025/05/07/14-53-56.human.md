This is the third patch in a 4-part series optimizing memory usage in the packed-refs backend, specifically focusing on enabling mmap for large packed-refs files during fsck operations. The patch extracts the munmap operation from `create_snapshot()` into a new `munmap_temporary_snapshot()` function, making the logic reusable for fsck operations while maintaining the same behavior.

The change is a straightforward refactoring that moves 20 lines of mmap cleanup code into a dedicated function, with callers now explicitly using the new function when `mmap_strategy == MMAP_TEMPORARY`. This follows the pattern established in patch 2 (which extracted buffer allocation) and prepares for patch 4's planned mmap-based fsck verification.

The implementation preserves all existing behavior - it still makes a copy of mmapped content when needed and handles cleanup consistently. The patch appears well-structured, with the extracted function maintaining the same safety checks and memory management as the original code. This continues the series' trend of clean, incremental improvements to the packed-refs backend's memory handling.