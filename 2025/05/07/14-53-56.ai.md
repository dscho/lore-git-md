This is the third patch in a 4-part series optimizing memory usage in the packed-refs backend, specifically focusing on enabling mmap for large packed-refs files during fsck operations. The patch extracts the munmap operation from `create_snapshot()` into a new `munmap_temporary_snapshot()` function, following the pattern established in patch 2's buffer allocation extraction. This refactoring prepares the codebase for the upcoming fsck optimizations while maintaining existing behavior.

The 36-line change (23 insertions, 13 deletions) moves the temporary mmap cleanup logic into a dedicated function that will be reusable by the fsck verification code in patch 4. The new function handles the buffer copying and cleanup when `MMAP_TEMPORARY` strategy is used, exactly matching the previous inline implementation. Key technical details include preservation of the buffer copying behavior for temporary mmaps and explicit checking of `mmap_strategy == MMAP_TEMPORARY` at the call site, as suggested in earlier reviews.

This continues the series' pattern of structural refactoring before introducing behavior changes, with all modifications contained within packed-backend.c. The patch follows through on suggestions from Jeff King and Patrick Steinhardt to enable cleaner implementation of the fsck optimizations while maintaining the code's organization and safety checks. The changes are straightforward and maintain all existing functionality, positioning the codebase for the final optimization patch that will introduce mmap-based fsck verification.