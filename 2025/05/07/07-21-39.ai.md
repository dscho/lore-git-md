This is a refactoring patch in Patrick Steinhardt's v5 series to decompose `git gc` functionality into standalone maintenance tasks. The patch extracts worktree pruning logic from `cmd_gc()` into a new `maintenance_task_worktree_prune()` function, preparing for its later use as a standalone maintenance task.

The change moves the worktree pruning command spawning into a dedicated function with the same pattern used for other maintenance tasks. The new function takes the existing `maintenance_run_opts` and `gc_config` parameters, maintaining consistency with the task interface. The actual behavior remains unchanged - it still runs `git worktree prune --expire` with the configured expiration time.

This is one of several preparatory refactorings in the series that isolate `git gc` components before making them available as independent maintenance tasks. The patch follows the established pattern seen in previous maintenance task decompositions, keeping the child process spawning approach while cleaning up the code organization.

The change is straightforward with no apparent behavioral impact, just moving code into a more modular structure. It builds on prior work in the series that removed helper functions in favor of direct directory operations and fixed memory leaks in the worktree pruning logic. This appears to be a final cleanup before introducing the new maintenance task in a subsequent patch.