This is a refactoring patch in Patrick Steinhardt's v4 series to decompose `git gc` functionality into standalone maintenance tasks. The patch moves worktree pruning code into a separate function as preparation for adding it as a new maintenance task.

The email captures a minor commit message style discussion with Christian Couder, who suggested reordering the message to first state the motivation (adding a new maintenance task) before describing the refactoring. Patrick initially preferred keeping the subject repetition for self-contained readability but accepts Christian's suggested phrasing as an improvement.

The technical content of patch 3/7 remains unchanged - it purely extracts the worktree pruning logic from `gc.c` into a new standalone function with no behavior modifications. This follows the pattern seen earlier in the series where functionality is first isolated before being exposed as configurable maintenance tasks. The change is straightforward and uncontroversial, with the discussion focusing only on commit message presentation.

This patch is one of three preparatory refactorings in the series (along with indentation fixes and global variable removal) that enable the subsequent introduction of the new `worktree-prune` maintenance task in patch 6/7. The series as a whole has reached technical completion with all reviewer feedback addressed, now in final polishing stages for merge.