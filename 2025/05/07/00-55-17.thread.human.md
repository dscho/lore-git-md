This thread covers Derrick Stolee's series optimizing interactive Git commands (`git add -p/-i`, `git checkout -p`, `git reset --patch`) for sparse-checkout users by making them sparse-index aware. The changes prevent unnecessary index expansion, yielding dramatic performance improvements - up to 96% faster operations in sparse checkouts.

The series has progressed through multiple versions with thorough review from Elijah Newren, who identified several test case corrections and implementation details. Key changes include unified handling of index expansion between `git add -p` and `git add -i`, plus new support for `git checkout -p` and `git reset -p`. Performance tests show 96% reduction for `git add -p`, 95.6% for `git reset --patch`, and 91.6% for `git checkout --patch`, with minimal impact on full index cases.

The latest patch completes the series by optimizing `git reset --patch`, moving the sparse index check earlier in the command flow. Elijah has now reviewed all components of v2 and confirmed they follow established sparse-index patterns. The only remaining issue is a version control conflict from Junio's premature merge of v1 to 'next' while v2 was under review. With all technical concerns addressed and substantial performance gains demonstrated, the series appears merge-ready pending resolution of this administrative issue.