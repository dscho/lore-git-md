Derrick Stolee's series optimizing sparse index support for interactive commands (`git add -p/-i`, `git checkout -p`, `git reset --patch`) has progressed well, with Junio Hamano having already merged v1 to the 'next' branch. Elijah Newren's latest review notes a minor commit message typo (duplicate "git add -p" where it should say "git add -i") and confirms the test coverage and implementation approach look correct, particularly the unified handling of index expansion between `git add -p` and `git add -i`. 

While Elijah hasn't yet reviewed patch 3 (for `git reset --patch`), he gives preliminary approval to the other changes. The main outstanding issue is the version control conflict created by Junio's early merge of v1, which will require either reverting v1 or rebasing v2. The series continues to demonstrate substantial performance improvements (96% faster `git add -p`, 95.6% faster `git reset --patch`, and 91.6% faster `git checkout --patch` for sparse checkouts) while maintaining rigorous test coverage. With Elijah's thorough reviews and Derrick's responsive updates, the technical content appears merge-ready aside from the versioning question.