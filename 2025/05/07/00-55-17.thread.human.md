This thread discusses Derrick Stolee's performance optimization series that makes `git add -p/-i` and `git apply` sparse-index aware, preventing unnecessary index expansion for users of sparse-checkout. The changes show dramatic performance improvements - a 96.7% runtime reduction (from 2.09s to 0.07s) for `git add -p` with sparse index.

Elijah Newren has provided thorough review feedback throughout the series, initially identifying needed test case corrections and ambiguous wording, and now giving final sign-off after Stolee incorporated all suggestions. The latest email addresses the last remaining items: clarifying commit message distinctions between current and future behavior, and fixing test assumptions about index resparsification (using `git reset --hard && git sparse-checkout reapply` instead of just `git reset`).

The implementation touches `builtin/apply.c` and `builtin/add.c` while maintaining comprehensive test coverage in both compatibility (`t1092-sparse-checkout-compatibility.sh`) and performance (`p2000-sparse-operations.sh`) test suites. Phillip Wood has noted potential future work to extend these optimizations to other patch-oriented commands like `git checkout --patch`, but that remains outside the current scope. With Junio Hamano's prior approval and Newren's final feedback now addressed, the series appears ready for merging.