Here's a human-readable summary of the thread:

The thread explores whether Git's merge commit visualization tools could be enhanced to support reviewing already-committed merges using meld's 3-pane view (LOCAL/BASE/REMOTE), similar to how it works during conflict resolution. Britton Kerin initiated the discussion, noting this would be particularly useful for two-parent merges, which are common in practice.

Johannes Sixt raised concerns about the conceptual mismatch - while two-parent merges could be special-cased, Git supports merges with arbitrary numbers of parents (octopus merges) which wouldn't fit meld's fixed 3-pane model. Britton acknowledged this limitation but argued the common case of two-parent merges justifies tooling support, mentioning they currently use a StackOverflow workaround that could be generalized.

The discussion considers whether this functionality should be added to git-difftool or implemented as a separate utility, with technical considerations around handling tree diffs and different diff3 renderers. While the idea shows promise for improving merge commit review, the conversation remains open-ended about implementation approaches and whether the benefits outweigh the complexity.