Performance optimization patch in the sparse-index integration series, specifically targeting `git add -p/-i` behavior. This second patch in a 3-part series focuses on preventing unnecessary sparse index expansions during interactive adds by moving the sparse-index awareness check earlier in `cmd_add()`. The changes reveal an interesting asymmetry: `-p` still expands the index for out-of-cone edits while `-i` does not, though this is considered an edge case since users typically work within their sparse checkout. The patch adds comprehensive test coverage in `t1092-sparse-checkout-compatibility.sh` verifying both commands' behavior with in-cone and out-of-cone modifications. Key changes include moving the `command_requires_full_index=0` setting before interactive mode handling in `builtin/add.c` and adding new test scenarios that validate sparse index preservation during interactive sessions. The implementation maintains existing functionality while optimizing the common case, though the `-p` behavior for out-of-cone files leaves room for future improvement. This continues the thread's focus on making interactive workflows sparse-index aware without compromising correctness.