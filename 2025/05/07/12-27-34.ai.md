This is a follow-up question in the ongoing promisor-remote protocol enhancement series, specifically addressing implementation details of the field handling mechanism.

Karthik Nayak raises a question about the design choice to maintain field order in the `struct promisor_info`'s `fields` string_list. The current implementation explicitly documents that fields should not be sorted, using `string_list_append()` rather than `string_list_insert()`, to preserve insertion order. The question probes whether this ordering requirement is fundamental to the protocol (where clients might depend on field sequence) or merely an implementation artifact of how the information is parsed.

This touches on a subtle design point in the series that has already seen significant discussion around field handling constraints. The thread context shows the protocol has been carefully limited to specific fields ("partialCloneFilter" and "token") with case-insensitive names and case-sensitive values, where unknown fields must be ignored for backward compatibility. The question suggests potential tension between the implementation's current ordering requirements and the protocol's stated field independence.

The inquiry doesn't propose changes but seeks clarification that could inform future implementation refinements, particularly as Patrick Steinhardt has already suggested structural improvements (replacing string_list with dedicated struct members) as future work. The answer could influence whether such future changes should enforce field order independence more strictly.