This is a response in the ongoing discussion about adding trailer-related options to `git rebase` and `git am`. Phillip Wood weighs in on Li Chen's RFC proposing both a dedicated `--reviewby` flag and a general `--trailer` option.

Phillip argues against adding the specialized `--reviewby` flag, suggesting the more general `--trailer` interface should suffice. He views `--signoff` (which `--reviewby` was modeled after) as a historical artifact that might not be added today, and cautions against proliferating project-specific flags. The response indicates he'll review the `--trailer` implementation separately.

The email advances the thread by clearly taking a position on one of the open questions from the RFC - whether to support both specialized and general trailer options. Phillip's stance favors the more flexible approach, though he hasn't yet commented on the implementation details or the apply/merge backend differences mentioned in earlier messages. This moves the discussion toward potentially simplifying the proposal by focusing solely on the `--trailer` functionality.