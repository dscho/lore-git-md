This is a clarification email in the ongoing discussion about command-line interface design for diff context overrides in interactive patch commands.

Leon Michalak responds to Junio C Hamano's concerns about the documentation wording for the new `-U<n>` and `--inter-hunk-context=<n>` options in the interactive patch workflow. The key clarification is that the "implies --interactive/--patch" phrasing was intended to mean these options would only work when explicitly used with `--interactive` or `--patch`, not that they would automatically trigger interactive mode.

The email suggests there may have been a misunderstanding about the intended behavior - Leon explains they meant for the options to either do nothing or error out when used without `-i/-p`, rather than silently enabling interactive mode. This aligns with the thread's emerging consensus that context overrides should require explicit interactive mode flags.

The message doesn't propose any code changes but seeks to clarify the design intent behind the documentation wording. This comes at a point in the thread where the command-line interface design is being carefully reconsidered to avoid ambiguity and preserve future extensibility. The clarification helps ensure all participants share the same understanding of the proposed behavior as the implementation evolves.