This is a discussion about interactive diff context controls in the `add -i` and `add -p` workflows, part of an ongoing patch series to improve context handling in Git's interactive commands.

Junio C Hamano responds to Phillip Wood's proposal about hunk redisplay in `add -p`, clarifying that the current discussion focuses on `add -i`'s ability to set diff context for subsequent patch commands, not the per-hunk context adjustment during `add -p`. The key technical limitation noted is that `add -p`'s current implementation doesn't retain the necessary state to redisplay hunks with modified context once editing begins.

The exchange reveals two distinct context control mechanisms under consideration: Phillip's proposed in-session hunk redisplay for `add -p` versus Junio's suggestion of command-line style context parameters during `add -i`'s "What now>" prompt. While Phillip's approach would provide more immediate context for decision-making, Junio points out architectural constraints that may make this challenging to implement.

This discussion represents the final piece of the three-patch series, where patches 1-2 have largely settled on config and command-line context controls, while patch 3's interactive UI design remains in flux. The technical depth of the exchange suggests both contributors are thinking carefully about the user experience implications of each approach.