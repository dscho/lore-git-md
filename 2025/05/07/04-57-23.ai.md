This is a test fix discussion in the ongoing thread about flaky test t6011-rev-list-with-bad-commit.sh. Patrick Steinhardt is responding to Junio Hamano's patch that added `chmod +w` before packfile edits in the test script.

The email contains two typo corrections to Junio's original message (fixing "rewrite many use" to "rewrote many uses" and "new" to "knew"), but the substantive content is Patrick's acknowledgment of Junio's fix. He confirms that adding the `chmod +w` before the `sed` operation makes sense, which helps resolve the filesystem permission aspect of the test flakiness.

The thread context shows this is part of a larger discussion about two distinct issues: (1) the now-resolved permission problems during test setup (handled by both `mv -f` and `chmod +w`), and (2) the still-open question about whether `sed` is the right tool for binary file manipulation in tests. Patrick's response doesn't address the binary editing concern, focusing only on the permission fix.

Key technical details remain unchanged from the thread summary: the test verifies `git rev-list` behavior with corrupted commits, and the current method of corruption (via `sed` editing packfiles) may need revisiting for portability, though the immediate permission issue is solved. The patch being acknowledged modifies the packfile editing sequence to explicitly make files writable before modification.