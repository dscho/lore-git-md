Bugfix in parse-options machinery addressing type safety issues.

This is the second patch in a series fixing memory leaks and type safety problems in Git's option parsing. It corrects a case where an integer was incorrectly used as a boolean in the test helper code (test-free-unknown-options.c), while also cleaning up formatting and style issues throughout the parse-options code. The changes include:

1. Fixing the test helper to properly declare boolean variables as `int` rather than `bool` to match the OPT_BOOL macro's expectations
2. Reformatting code for better readability (adding newlines, fixing indentation)
3. Adding a missing return statement in the test helper
4. Cleaning up whitespace in the test-tool registration

The patch touches parse-options.[ch] and the test infrastructure, maintaining the same focus as the rest of the series on making the option parsing code more robust. The changes are straightforward and appear to be uncontroversial improvements, though reviewers will want to verify the boolean/int changes don't introduce any subtle behavioral differences in edge cases. The test helper modifications ensure the fixes are properly exercised.