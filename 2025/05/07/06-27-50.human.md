This is a policy discussion about the future of Git's `contrib/` directory, sparked by Patrick Steinhardt's ongoing cleanup series. The immediate context is the proposed removal of `git-new-workdir`, but the email quickly pivots to broader questions about what `contrib/` should represent in modern Git.

Junio Hamano (the tool's original author) acknowledges the script is obsolete compared to `git worktree` but notes he still uses one old instance without issues. He raises two concerns: tests that depend on `contrib/` components (though Patrick clarifies these only verify compatibility, not core functionality), and whether such removals should follow a formal deprecation cycle.

Patrick argues for a more aggressive cleanup policy where `contrib/` serves as a true staging area - experimental features either graduate to core or get removed, with no guarantees in between. He contrasts this with the historical view of `contrib/` as a permanent home for community contributions. The email ends with Patrick offering to draft a new `contrib/README` to formalize this approach, recognizing it may need discussion.

The exchange shows the series has progressed beyond individual tool removals to addressing systemic questions about Git's maintenance philosophy. While the technical case for removing `git-new-workdir` is clear, the policy discussion it triggered could significantly impact how future contributions are handled. Patrick's proposal would make `contrib/` more like other projects' "experimental" directories, with clearer expectations about feature lifecycle.