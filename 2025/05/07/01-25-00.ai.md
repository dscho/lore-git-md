This is a follow-up question in the ongoing `the_repository` removal series, specifically about patch 11/17 which handles submodule backends. Derrick Stolee is asking Patrick Steinhardt about test coverage for the new `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` environment variable introduced in the patch.

The patch converts submodule backend registration to use an explicit `object_database*` parameter instead of relying on `the_repository`. The new test variable appears to be a debugging aid that would trigger a BUG() if submodule backends are registered, but there's no accompanying test that exercises this code path. Stolee is checking whether test coverage was planned for this feature.

This is a relatively minor point in the larger refactoring effort, but highlights the careful attention to detail in the review process. The question doesn't challenge the technical direction of the patch (which aligns with the series' goals), but focuses on ensuring proper test coverage for the new debugging facility. The thread context shows this is part of a well-established pattern where mechanical conversions proceed smoothly but reviewers like Stolee help catch smaller details that might need attention.