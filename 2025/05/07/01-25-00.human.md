This is a review comment on patch 11/17 of Patrick Steinhardt's series to remove `the_repository` dependencies from the object database code. The patch handles submodule backends used by `git grep --recursive`, converting them to use an explicit `object_database*` parameter rather than relying on the global.

Derrick Stolee notes that while the patch adds a test-only `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` environment variable (presumably for debugging), there doesn't appear to be an accompanying test case that exercises this code path. This is a minor oversight in an otherwise mechanical conversion - the core change looks correct but could benefit from test coverage to verify the submodule backend registration behavior.

The comment is brief but constructive, continuing Stolee's pattern of careful technical review throughout this series. Since this is part of a larger refactoring effort that's already well underway, the core approach has been vetted in prior patches, and this appears to be a straightforward application of the established pattern.