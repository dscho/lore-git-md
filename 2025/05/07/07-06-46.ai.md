This is a minor follow-up in the ongoing maintenance task decomposition series, specifically addressing commit message wording for patch 2/7 which removes unnecessary global variables in `builtin/gc.c`. 

The email contains Patrick Steinhardt's acknowledgment of Christian Couder's suggested improvements to the commit message grammar from the previous version. Christian had proposed two alternative phrasings to fix the missing "is" in the original subject line: either adding "it's" ("where it's trivial to do") or simplifying to ("where trivial to do"). Patrick concurs with the need for correction but doesn't specify which alternative he'll use.

This exchange represents final polish on a refactoring patch that was already technically complete and approved, with the only remaining discussion being about commit message presentation. The patch itself removes three global `strvec` variables used for assembling subprocess arguments in `git gc`, keeping only the `repack` variable which is used in multiple places. This cleanup was part of the larger effort to extract worktree pruning and rerere GC functionality from `git gc` into standalone maintenance tasks.

No new technical changes are proposed here - the thread has moved into the final polishing phase where participants are reviewing commit message wording and presentation details before the series is merged. The exchange demonstrates the careful attention to documentation quality even in straightforward refactoring patches.