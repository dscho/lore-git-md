Phillip Wood responds to Li Chen's RFC proposing a `--reviewby` option for `git rebase` and `git am`. He argues against adding a dedicated `--reviewby` flag, suggesting the more general `--trailer` interface (proposed in the same series) should suffice. Phillip views `--signoff` as a historical artifact that might not be added today, and cautions against adding project-specific convenience flags.

He proposes an enhancement to make `--trailer` more ergonomic by automatically using the committer's identity when no value is specified, which would address the convenience use case without requiring a dedicated flag. This continues the thread's trend toward favoring flexible, general solutions over specialized ones.

Phillip indicates he will review the `--trailer` implementation separately, keeping the technical discussion focused on the more broadly applicable part of the proposal. His response moves the conversation toward potentially simplifying the patch series by dropping the `--reviewby` functionality entirely.