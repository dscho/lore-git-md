This is a design discussion about interactive diff context controls in the ongoing patch series. Junio C Hamano responds to Phillip Wood's proposal for hunk redisplay in `add -p`, clarifying the technical constraints.

The exchange focuses on the distinction between `add -i` and `add -p` behaviors regarding context settings. Junio notes that Phillip's suggested hunk redisplay approach would be difficult to implement in `add -p` due to missing state tracking, while the original proposal to set context via `add -i` subcommands remains viable.

Key technical points:
- The discussion confirms `add -p` cannot currently redisplay hunks with modified context due to implementation limitations
- Junio reiterates that context changes would need to restart from `add -i` level
- The thread maintains focus on the series' goal of providing flexible context controls while working within Git's existing interactive patch infrastructure

This email advances the thread by narrowing the design options for patch 3/3, effectively ruling out the hunk-redisplay approach for `add -p` while keeping the `add -i` subcommand path open. The implementation constraints highlighted here will likely shape the final solution.