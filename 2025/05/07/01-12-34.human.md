Part of the ongoing `the_repository` removal effort. This is a follow-up to patch 7 in Patrick Steinhardt's 17-part series refactoring the object database code, where Derrick Stolee points out a minor mix-up in the commit message.

The patch itself correctly removes `the_repository` usage from `odb_mkstemp()` by making the object database parameter explicit, but accidentally reused the commit message from patch 6 (which dealt with `assert_oid_type()`). Stolee notes the discrepancy but confirms the actual code changes look good - updating the function signature and callers to pass the object database directly rather than accessing it through the global.

This continues the careful, incremental work to eliminate global state from the object database code. While the commit message error is a minor oversight, the technical content appears sound and aligns with the series' goals. The thread shows the high level of scrutiny these foundational changes are receiving, with Stolee catching even small inconsistencies while approving the substantive changes.