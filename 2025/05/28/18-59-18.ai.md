This is a platform compatibility report for the NonStop platform. The email reports a build failure in Git 2.50.0-rc0 related to the reftable code. The specific error occurs in `blocksource.c` line 105 where the compiler (C99) fails to parse the `REFTABLE_UNUSED` macro usage in the `file_release_data` function declaration.

The reporter, rsbecker@nexbridge.com, notes they are an experienced NonStop and UNIX developer but doesn't speculate on the root cause beyond showing the compiler error. The error suggests either a syntax issue with the macro expansion or a compiler-specific limitation in handling the attribute annotations. No proposed fix is included - this appears to be purely an initial bug report.

For future thread context: The issue appears specific to the NonStop platform's C99 compiler and involves the reftable subsystem's blocksource implementation. The error occurs during declaration of `file_release_data` where `REFTABLE_UNUSED` attributes are applied to function parameters. This may indicate either a macro definition problem or compiler incompatibility with the attribute syntax.