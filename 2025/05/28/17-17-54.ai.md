This is a user experience improvement patch for the `imap-send` series, specifically enhancing credential helper output. The change modifies the password prompt to include both host and port information (e.g., `outlook.office365.com:993`) rather than just the hostname, matching the behavior of `git send-email`.

The implementation is straightforward - a single line change in `imap-send.c` that formats the credential host string to include the port number using `xstrfmt`. The commit notes this aligns with existing behavior where default ports (993 for IMAPS, 143 for IMAP) are used when none is specified, making the display consistent in all cases.

This appears to be a final polish patch in the v7 series, following through on the user experience improvements mentioned in the thread summary. The change is small but meaningful for users who may need to distinguish between different port configurations. The implementation is clean and maintains the existing memory management patterns used elsewhere in the series.