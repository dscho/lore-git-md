This is a follow-up in the ongoing discussion about signature handling in `fast-export`/`fast-import`, specifically addressing process questions around changing defaults for experimental features.

Junio Hamano responds to Elijah Newren's question about why this change doesn't follow Git's typical deprecation timeline. He explains that the original behavior change - which required explicit flags or environment variables to handle signed commits - was a mistake that broke existing workflows, since the tools previously silently stripped signatures. Because this change only appeared in an unreleased RC version, Junio argues it's better to revert to the old behavior now rather than go through a full deprecation cycle.

The key point is that this isn't a normal deprecation case, but rather fixing an incorrect approach to introducing new functionality. The patch maintains backward compatibility by restoring the previous default behavior while still allowing explicit control over signature handling through the `--signed-commits` option. This appears to be a pragmatic solution that balances stability with the eventual goal of proper signature support.

The exchange highlights Git's careful approach to behavior changes, even for experimental features, and shows the maintainers being responsive when a proposed change turns out to be more disruptive than intended.