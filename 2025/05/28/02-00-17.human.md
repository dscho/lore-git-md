This is a bugfix patch in the ongoing MIDX (multi-pack-index) optimization and correctness series. Taylor Blau responds to Patrick Steinhardt's discovery of a potential array index underflow in the MIDX bitmap handling code.

The issue occurs in `open_midx_bitmap_1()` where the code could attempt to access a negative array index when looking up pack names. This was introduced in a previous patch that tried to fix a warning message but didn't account for all edge cases in the MIDX pack indexing logic.

Taylor acknowledges the problem and plans to send a corrected version. He also notes this highlights a broader issue with direct access to the `pack_names` array, suggesting it would be better to provide an accessor function like `nth_midxed_pack_name()` to encapsulate this logic safely.

This exchange continues the thread's pattern of rigorous safety analysis, with Patrick catching edge cases and Taylor quickly responding with fixes. The discussion also surfaces opportunities for further API improvements to prevent similar issues. The patch appears well-motivated and part of a coordinated effort to harden the MIDX codebase.