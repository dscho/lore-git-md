This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction during repacking. It's part 4 of 9 in the v4 series.

The patch moves the initialization and execution of a reachability traversal out of `read_packs_list_from_stdin()` and into its caller `read_stdin_packs()`. This prepares for future changes where the same traversal logic will be reused for the `--unpacked` case. The change is mechanical - the same code is just being relocated to a different function - and maintains all existing behavior.

The commit message explains this is groundwork for a future commit that will modify the traversal's purpose to find reachable from input pack commits but packed elsewhere. The diff shows the straightforward movement of the `rev_info` handling code, with no functional changes beyond the reorganization.

This continues the series' pattern of careful refactoring to enable upcoming optimizations while keeping each change focused and reviewable. The patch follows the established style of the series and appears ready for inclusion.