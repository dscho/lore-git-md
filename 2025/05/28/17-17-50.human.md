This is a bugfix patch in the `imap-send` series, addressing a memory leak in the CRAM-MD5 authentication path. The patch specifically fixes a case where `auth_cram_md5()` would fail to free the `response` buffer if the socket write operation didn't complete successfully.

The change is straightforward - it adds a `free(response)` call in the error path where the socket write fails. This completes the comprehensive memory management fixes for CRAM-MD5 that have been iterated on throughout the series (as noted in the thread summary, Ben Knoble had previously identified related memory issues).

While small, this patch is part of the larger effort to make `imap-send` more robust, particularly around authentication paths. The thread context shows this is the 4th patch in the 9-part v7, which represents the final polish of a series that has undergone extensive review. The change aligns with the series' goal of thorough memory management across all error paths in the IMAP authentication code.