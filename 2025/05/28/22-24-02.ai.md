Feature proposal discussion about `git commit -A` behavior, with historical context from Junio C Hamano.

Junio C Hamano responds to Nico Williams' concern about changing `git commit -a` behavior, clarifying that while `git add .` became safe for dirty workspaces by respecting `.gitignore`, changing `-a` would still be problematic. He notes this would break expectations for users relying on the current behavior of only committing known files (including those marked with `git add -N`). The response reinforces the historical technical constraints that shaped the original implementation while acknowledging modern `.gitignore` capabilities.

The email provides important context about edge cases involving partially staged files (`git add -N`) that would be affected by any behavior change. Junio's position suggests maintaining backward compatibility for `-a` while leaving room for discussion about adding `-A` as a new option. The technical distinction between "known files" (tracked or `-N` marked) versus "new files" becomes a key consideration for any implementation.

This moves the thread forward by addressing specific workflow concerns while keeping the door open for the `-A` proposal, provided it doesn't alter existing `-a` behavior. The discussion now has clearer technical boundaries around what changes would be considered safe versus breaking.