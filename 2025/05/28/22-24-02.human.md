This is a discussion about the proposed `git commit -A` feature, with historical context from Junio C Hamano about why `git commit -a` behaves as it does.

Junio C Hamano responds to Nico Williams' concern that changing `git commit -a` to stage new files would be disruptive. He clarifies that while `git add .` became safer with `.gitignore` support, changing `git commit -a`'s behavior would still be problematic backward compatibility breakage. The key insight is that `git commit -a` was designed to only handle files Git already knows about (including those marked with `git add -N`), a deliberate choice from Git's early days when ignore functionality didn't exist.

The email reinforces that the current `-a` behavior should remain unchanged, as many workflows depend on its predictability. This supports keeping the status quo for `-a` while leaving room for discussion about adding a new `-A` option that would behave differently. Junio's historical perspective lends weight to maintaining the conservative approach for existing commands while considering new functionality separately.

The thread continues to show division between those wanting more convenient staging behavior and those prioritizing backward compatibility and explicit control. Junio's input suggests any change would need to come through a new flag rather than modifying existing behavior, with careful consideration of edge cases around ignored files and partial staging.