# AI Thread Summary

Performance optimization in the promisor-remote subsystem, focusing on `promisor_remote_get_direct()`. The thread began with Han Young's patch removing redundant `is_promisor_object()` checks that caused slowdowns in large repositories, arguing callers already filter out local objects. Junio C Hamano raised concerns about this assumption, noting callers may simply pass unfound objects rather than explicitly verified promisor objects, making the behavior change potentially unsafe. Junio also questioned the error handling implications - the patch would make the function die on any fetch failure rather than only for promised objects. He suggested exploring optimization of `is_promisor_object()` itself through caching mechanisms similar to the reverse index, while maintaining the current verification semantics. The discussion remains open with key questions: whether the caller behavior assumption holds, if the error handling change is acceptable, and whether optimizing the verification check would be preferable to removing it. Reviewers Christian Couder and Jonathan Tan are CC'd for their input on these promisor-remote implementation details. The core performance issue (slow `is_promisor_object()` checks in large repos) is acknowledged, but the proposed solution's safety and completeness are now in question.