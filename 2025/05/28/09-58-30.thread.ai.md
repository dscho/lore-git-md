# AI Thread Summary

Performance optimization in the promisor-remote subsystem, focusing on `promisor_remote_get_direct()` and `is_promisor_object()` checks. The thread began with Han Young's patch removing redundant `is_promisor_object()` verifications that caused slowdowns in large repositories, assuming callers already filter out local objects. Junio C Hamano raised concerns about this assumption, noting callers may pass unfound objects rather than explicitly verified promisor objects, making the behavior change potentially unsafe. He also questioned the error handling implications - the patch would make the function die on any fetch failure rather than only for promised objects.

Han Young's response provides technical details about why `is_promisor_object()` is slow - it must parse every object in promisor packfiles (millions in large repos) and their references. They acknowledge Junio's safety concerns about callers potentially passing missing non-promisor objects and confirm the error handling behavior change. Test results show git-cat-file and git-diff fail in both cases but with different error messages. Han references Calvin Wan's prior analysis that caching promisor objects is difficult due to their dynamic nature (local objects can become promisor objects).

Key open questions remain: whether the performance optimization can be achieved while maintaining safety of promisor object verification, if the error handling change is acceptable, and whether optimizing the verification check (perhaps through caching mechanisms similar to the reverse index) would be preferable to removing it. Reviewers Christian Couder and Jonathan Tan are CC'd for their input on these promisor-remote implementation details. The core performance issue (slow `is_promisor_object()` checks in large repos) is acknowledged, but the proposed solution's safety and completeness are still under discussion with no clear resolution yet.