Here's the updated human-readable thread summary:

The thread began with Randall Becker reporting a build failure on NonStop platforms when compiling Git 2.50.0-rc0, specifically in the reftable subsystem where the `REFTABLE_UNUSED` macro's GNU-specific `__attribute__((__unused__))` syntax wasn't supported. Carlo Marcelo Arenas Belón proposed a fix making the attribute conditional on `__GNUC__`, which Junio C Hamano approved for queuing. Brian M. Carlson noted this simpler approach is now acceptable given older GCC versions are no longer relevant.

Patrick Steinhardt then raised an interesting question about why Git's similar `MAYBE_UNUSED` macro works on NonStop while `REFTABLE_UNUSED` fails. Jeff King has now clarified this discrepancy: the core Git compatibility layer in `git-compat-util.h` includes platform-specific handling that makes `__attribute__()` a no-op on certain platforms before `MAYBE_UNUSED` is defined, avoiding the need for explicit `#ifdef` checks. This explains the different behavior between the two macros while confirming the queued reftable fix remains appropriate.

The discussion has revealed an architectural nuance in how Git handles compiler attributes across subsystems, though no further changes to the immediate fix are needed. The thread shows how platform compatibility issues can uncover deeper questions about codebase consistency, even when the practical solution is straightforward.