This is a platform compatibility report about a build failure on NonStop systems. The email reports that Git 2.50.0-rc0 fails to compile on a NonStop platform due to a syntax error in `reftable/blocksource.c`, specifically around the `REFTABLE_UNUSED` macro usage in the `file_release_data` function. The error suggests the compiler (which claims C99 compliance) is rejecting the function parameter attributes.

The reporter, a long-time NonStop and UNIX developer, provides minimal context beyond the immediate build failure. There's no analysis of why this might be happening or whether it relates to NonStop-specific compiler behavior. The tone suggests genuine confusion about why this standard-looking code would fail.

This appears to be a straightforward platform-specific build issue that would need investigation by someone familiar with NonStop's C implementation. The reporter doesn't propose a solution, but the report could help maintainers catch platform-specific issues before the final 2.50.0.