This is a documentation patch that adds clarifying comments about packfile lookup ordering in the MIDX/multi-pack-index context. It's part of Patrick Steinhardt's v3 performance optimization work that builds on the ongoing MIDX improvements thread.

The patch explains the subtle race condition implications of the current packfile lookup order in `add_packed_git()`. While it might seem inefficient to check auxiliary files (like .keep) before verifying the packfile itself (which would allow early bailout), the current order is actually necessary for correctness. The comment documents that pack deletion removes the .pack file first, so checking auxiliary files first ensures we either get a consistent view of all files or fail cleanly when the pack is missing.

This patch doesn't change behavior but provides important context for future maintainers working on the packfile/MIDX code. It's a small but meaningful addition to the ongoing effort to improve MIDX-related code quality and documentation. The explanation ties into the thread's broader themes of correctness and race condition avoidance in packfile handling.

The patch is well-contained and clearly motivated, with the comment providing valuable insight into non-obvious design constraints. It's part of a 2-patch series (v3) that includes performance optimizations for packfile lookups, with this first patch establishing the safety rationale for the subsequent changes. The comment will help reviewers and future developers understand why certain ordering constraints must be preserved even when optimizing the code.