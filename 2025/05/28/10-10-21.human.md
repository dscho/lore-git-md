This is a documentation patch series that appears to be a follow-up to earlier work on email credential helpers and formatting. The thread context shows this builds on completed documentation improvements for email authentication.

The current series focuses on cleaning up and clarifying the `git-send-email` documentation. The key changes include:

1. Adding a Yahoo credential helper reference and linking Google's `sendgmail` tool as an alternative for Gmail users. This maintains consistency with the credential helper approach established in earlier work.

2. Standardizing formatting throughout the documentation, particularly fixing inconsistent quoting styles (switching between ``, "", and '') and removing some broken links. The patch also adds CPAN links for required Perl modules to help users with installation.

3. Removing credential helper links from `gitcredentials` documentation, though the exact motivation for this change isn't stated in the commit message.

4. Making the purpose of using app passwords for Gmail more explicit in the send-email docs, likely to help users understand why they need to generate these special passwords when two-factor authentication is enabled.

The changes appear well-scoped and maintain the documentation standards established in the earlier merged series. The patches follow the project's typical style for documentation improvements, with thorough commit messages explaining the changes. This looks like straightforward maintenance work that should be uncontroversial to merge.