This is a user experience improvement for `imap-send`, part of a larger series adding IMAP functionality. The patch makes credential prompts more informative by including the port number alongside the hostname when requesting passwords (e.g., showing "outlook.office365.com:993" instead of just the host).

The change aligns `imap-send`'s behavior with `git send-email` and ensures credential helpers have complete connection information. The implementation is straightforward - modifying one line to format the host:port string. Since the code always uses default ports (993/143) when none is specified, there's no need to handle missing ports as a special case.

This appears to be final polish in a well-reviewed series, addressing a small but noticeable inconsistency in credential prompts. The change is minimal (2-line diff) and follows established patterns from other parts of Git.