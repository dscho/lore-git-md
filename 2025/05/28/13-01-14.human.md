This is a feature patch series (2 parts) adding GPG signing support to `git subtree`. The author, Patrik Weiskircher, explains they need this functionality for managing dependencies with signed commits. 

The first patch adds `-S/--gpg-sign` options to `git subtree` commands, mirroring the signing functionality available in core Git commands. The second adds corresponding tests to verify the new feature. The changes touch documentation, the shell script implementation, and test files.

The implementation appears straightforward, following Git's existing patterns for commit signing. The test coverage looks thorough, with 113 lines of new tests checking various signing scenarios. As a first-time contributor, the author invites feedback on their submission approach.

This seems like a well-motivated addition that would benefit users who need signed commits in their subtree workflows. The patch series is self-contained and follows Git's conventions for similar signing functionality elsewhere in the codebase.