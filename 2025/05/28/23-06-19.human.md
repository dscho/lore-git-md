This is a clarification in the ongoing discussion about adding a `-A` option to `git commit`. Jon Forrest reiterates that his proposal is strictly about introducing a new `-A` flag that would stage both modified tracked files and new untracked files (matching `git add -A` behavior), not about changing the existing `-a` behavior. This comes in response to Junio C Hamano's concern about backward compatibility for users relying on `-a`'s current behavior (which only stages modifications to tracked files, including those marked with `git add -N`).

The exchange shows the discussion is now clearly focused on whether to add `-A` as a new option, with consensus that `-a` must remain unchanged. Jon's brief reply serves to prevent any misunderstanding that might have arisen from earlier messages, keeping the thread's technical boundaries well-defined. The proposal remains in the discussion phase, with implementation details around edge cases (like handling ignored files and `-N` marked files) still to be resolved.