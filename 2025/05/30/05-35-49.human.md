This is a follow-up question in the resolved thread about the NonStop platform build fix for the reftable subsystem's `REFTABLE_UNUSED` macro. Patrick Steinhardt notes that Git's main codebase already has a similar `MAYBE_UNUSED` macro in `git-compat-util.h` with the same GNU attribute syntax, yet only the reftable version caused compilation failures. He's asking why the inconsistency exists when both macros appear identical in their use of `__attribute__((__unused__))`.

The thread had previously concluded with Junio Hamano and Brian Carlson agreeing that Carlo Arenas Belón's simple conditional compilation fix was sufficient, given that older GCC versions (which required more complex version checks) are no longer relevant. This new question doesn't challenge the solution but seeks to understand why the problem manifested specifically in the reftable code while the main codebase's similar macro worked fine.

No technical details or new proposals are introduced - it's purely a clarification request about existing behavior. The answer would likely involve examining how/when these macros are included on different platforms, but that context isn't provided in this email. The thread remains effectively closed regarding the original fix, with this being an academic follow-up.