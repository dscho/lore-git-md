This is a documentation patch that's part of an ongoing series to improve Git's email-related documentation (now at version 6). The patch adds references to two credential helpers: one for Yahoo accounts and Google's `sendgmail` tool for Gmail authentication.

The changes include adding a link to Google's `sendgmail` tool as an alternative method for sending emails through Gmail, and listing a new `git-credential-yahoo` helper alongside the existing Gmail and Outlook credential helpers. The patch maintains the existing documentation structure while expanding the options available to users.

This appears to be a straightforward documentation improvement that builds on previous work in the series. The changes are focused and well-contained, with clear links to external resources. The patch follows the style established in earlier iterations of this documentation series.