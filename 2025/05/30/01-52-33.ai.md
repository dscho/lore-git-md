This is a bugfix patch series finalizing the sequencer memory leak fix, now also addressing BUG() message formatting consistency.

The v3 series from Lidong Yan completes the work to replace error handling with BUG() assertions in `update_squash_messages()` for invalid command types, resolving a memory leak during interactive rebase operations. The first patch now moves the command validation to function entry using `is_fixup()` as previously discussed, while maintaining the buffer cleanup. The second patch is a new addition that standardizes BUG() message formatting across the codebase by removing translation markers (_()) from BUG() strings, touching several files beyond just the sequencer fix.

The changes reflect full consensus from maintainers, with Junio C Hamano and Phillip Wood having previously approved the technical approach. The series now addresses two distinct but related concerns: 1) fixing the sequencer's error handling contract and memory management, and 2) establishing consistent BUG() message formatting project-wide. The range-diff shows the evolution from v2, with the command validation now occurring earlier in the function and the elimination of the else clause that previously handled unknown commands.

Key technical details: The sequencer.c changes enforce that `update_squash_messages()` only handles TODO_FIXUP or TODO_SQUASH commands via early BUG() assertion. The formatting changes in the second patch modify builtin/mktag.c, builtin/worktree.c, and pack-bitmap-write.c to remove translation markers from BUG() messages, establishing a new convention that these programmer-facing assertions should not be translated. The patch series appears merge-ready, having incorporated all feedback from the discussion thread while expanding its scope to address a related code style concern.