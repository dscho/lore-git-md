This is a follow-up in the GPG signing support for `git subtree` series, where the author Patrik Weiskircher responds to Junio Hamano's technical feedback. The thread has progressed from initial implementation to detailed discussion of parsing challenges.

The key new development is a technical dilemma around implementing the `-S` option for GPG signing in the bash-based `git-subtree.sh`. The author identifies three possible approaches to handle optional key IDs with `-S`, all with tradeoffs: using `--stuck-long` (which would be the first use in Git's codebase), making `-S` parameterless, or requiring a parameter. The author leans toward `--stuck-long` despite its novelty in the codebase, but seeks maintainer guidance before proceeding.

This email shows the author thoughtfully engaging with the maintainer's feedback and surfacing implementation challenges early. The discussion has moved from general code organization to specific parsing mechanics, indicating the patch is progressing toward a technically sound solution. The maintainer's previous constructive feedback suggests this is a serious effort likely to result in merged functionality, though the parsing approach needs resolution.