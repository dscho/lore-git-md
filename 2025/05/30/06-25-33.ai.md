This is a technical clarification in the ongoing discussion about NonStop platform compatibility with the reftable subsystem's `REFTABLE_UNUSED` macro. Jeff King explains why the `MAYBE_UNUSED` macro in `git-compat-util.h` works on NonStop while `REFTABLE_UNUSED` fails, despite their similar definitions. The key difference is that `git-compat-util.h` includes platform-specific handling that makes `__attribute__()` a no-op on certain platforms before the `MAYBE_UNUSED` macro is defined, eliminating the need for a separate `#ifdef` check. This explains the inconsistency Patrick Steinhardt observed between the two macros' behavior on NonStop.

The response doesn't propose any changes to the already-queued fix from Carlo Marcelo Arenas Belón, but provides important context about Git's attribute handling infrastructure. It suggests that while the immediate reftable fix is correct, there may be broader architectural considerations about how attribute macros are handled across different parts of the codebase. The explanation helps connect the reftable-specific issue to Git's existing compatibility layer patterns.