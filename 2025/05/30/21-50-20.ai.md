This is a test patch in the ongoing pack-bitmap memory leak fix series, where Eric Sunshine provides additional context for Junio Hamano's observation about shell syntax portability in the test case.

The thread has established that Lidong Yan's v2 series correctly addresses the memory leak in `find_boundary_objects()` by making `roots_bitmap` cleanup unconditional, with comprehensive test coverage in t5333. Junio had noted two minor issues - one about commit organization (whether to combine the fix and test commits) and another about a shell syntax linter warning in the test's environment variable export.

Eric Sunshine now clarifies the specific nature of the linter warning: the test uses `export VAR=value` syntax which isn't fully portable across shells. The recommended portable form is to split it into separate assignment and export statements (`VAR=value && export VAR`). This is a common shell scripting best practice that the Git test suite generally follows.

The email doesn't propose any changes to the patch itself - it's purely informational, providing the exact test-lint command that surfaces the issue and the canonical way to write such exports in Git's test suite. This helps maintain consistency with the project's established patterns for shell script portability. The discussion remains focused on polishing the test implementation details rather than revisiting the core bitmap leak fix, which has already received technical approval.