This thread continues the effort to remove global state from Git's codebase, specifically targeting the `gc_before_repack()` function in `builtin/gc.c`. The initial patch removed a static `done` flag and improved error handling by making the function return error codes rather than calling `die()` internally. 

Ben Knoble raised a question about the choice of exit code 127 (typically used for "command not found"), suggesting it might not be the most appropriate. Patrick Steinhardt has now responded, agreeing this was incorrect and noting the proper exit code should be 128 to match Git's conventions. He also floated an idea about modifying `die_builtin()` to handle NULL pointer cases silently, though this seems more exploratory than a firm proposal.

The core technical change appears settled - removing the global state and improving error handling - with this follow-up addressing a subtle but important correctness issue. The discussion remains focused and productive, typical of the careful attention to detail seen in the broader `the_repository` removal effort led by René Scharfe. While the exit code question seems likely to be resolved quickly, the suggestion about `die_builtin()` may lead to further refinements in how Git handles errors during this ongoing refactoring.