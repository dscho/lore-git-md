This is a follow-up discussion in the NonStop platform compatibility thread about the `REFTABLE_UNUSED` macro fix. Patrick Steinhardt confirms understanding of why the reftable library's attribute handling differs from core Git's after Jeff King's explanation about `git-compat-util.h`'s platform-specific preprocessing. 

The key point clarified is that core Git's `MAYBE_UNUSED` works on NonStop because `git-compat-util.h` includes platform-specific handling that makes `__attribute__()` a no-op before the macro, while the reftable code lacked this preprocessing. Patrick acknowledges this explains why Carlo Marcelo Arenas Belón's conditional compilation fix for `REFTABLE_UNUSED` is the right approach, despite the surface inconsistency with core Git's macro.

This email effectively closes the remaining open question in the thread, confirming the queued fix is correct and no further changes are needed. The discussion highlights an architectural consideration for future work - potential standardization of attribute macros across the codebase - but the immediate platform-specific issue is resolved.