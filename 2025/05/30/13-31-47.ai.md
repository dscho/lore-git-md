This is a refactoring patch in Patrick Steinhardt's ongoing Meson build system integration series, specifically addressing test infrastructure. The patch introduces a `test_kwargs` dictionary in the Meson build configuration to centralize common test parameters, starting with the `timeout` setting that was previously duplicated across multiple test definitions.

The change affects four build files, moving the hardcoded `timeout: 0` parameter into a shared dictionary that's then referenced via `kwargs: test_kwargs`. This prepares for additional common parameters to be added in future commits. The modification is mechanical and doesn't alter test behavior, only the way parameters are specified in the build configuration.

Key technical details include:
- New `test_kwargs` dictionary defined in root `meson.build` with initial `timeout: 0` entry
- Updated test definitions in `contrib/credential/netrc/meson.build`, `contrib/subtree/meson.build`, and `t/meson.build` to use the shared kwargs
- Maintains identical test execution behavior while reducing duplication
- Part of broader effort to standardize test configuration across the codebase

The patch follows the series' pattern of uncontroversial infrastructure improvements and appears ready for integration, with no outstanding technical questions raised in the thread context. It builds on previous work to make the test suite more maintainable as the Meson integration progresses.