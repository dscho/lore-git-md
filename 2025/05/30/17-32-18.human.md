Feature patch adding a command-line folder override option to `git imap-send`. This is part of Aditya Garg's v9 series improving IMAP functionality, which has already addressed several authentication and configuration issues.

The patch introduces a new `--folder` (or `-f`) option that lets users specify the destination folder at runtime, overriding any `imap.folder` config setting. This is useful for users who frequently need to send to different folders. The implementation is straightforward - it adds the option parsing and applies the override before connecting to the server.

Documentation is thorough, showing both the new option and clarifying precedence rules (command line overrides config). The change is small (9 lines of code) and well-contained, touching only the option parsing and folder selection logic. This appears to be a clean, focused addition that completes the series' goal of making `imap-send` more flexible while maintaining backward compatibility.

As part of the larger series, this patch follows established patterns and doesn't introduce any new technical concerns. The thread context shows the series is mature and well-reviewed, with this being one of the final user-facing improvements before potential merge.