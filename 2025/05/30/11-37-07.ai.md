This is a documentation patch series (v6) that continues the email-related documentation improvements, focusing on credential helpers and formatting in `git-send-email` docs. The series builds on previously merged work while addressing review feedback.

The 4-patch series:
1. Adds links to Yahoo credential helper and Google's `sendgmail` tool
2. Standardizes formatting in `git-send-email.adoc` (removing $ from placeholders, fixing punctuation)
3. Removes credential helper links from `gitcredentials.adoc` (consolidating them in send-email docs)
4. Clarifies Gmail authentication requirements (app passwords vs OAuth2)

Key technical changes include:
- Replacing `$<num>` with `_<num>_` for placeholders
- Using 'Git:' instead of backtick-wrapped version
- Switching terminology from "method" to "mechanism" for OAuth
- Adding CPAN links for required Perl modules
- Rewording Gmail auth section to be clearer about app password requirements

The author pushes back slightly on Junio's prior review feedback, arguing that backtick-wrapping mail headers ('To', 'From') hurts readability when overused. The changes show responsiveness to prior feedback while maintaining some stylistic preferences where the author believes it improves docs readability.

This appears to be nearing final form after multiple iterations, with the remaining discussion points around documentation formatting preferences rather than technical content. The changes are focused and well-explained, with clear commit messages tracking the evolution of each patch through the review cycle.