This is a test patch that complements the memory leak fix in the pack-bitmap subsystem, specifically targeting the boundary traversal case with pseudo-merges. It's the second patch in a v2 series that began with a fix for `find_boundary_objects()` in pack-bitmap.c.

The test creates a scenario where a pseudo-merge bitmap would previously have been leaked: it sets up a repository with two commits (A and B), configures pseudo-merge settings to trigger the boundary traversal path, and verifies the bitmap-based count operation works correctly. The test doesn't directly detect the memory leak (that would require instrumentation), but exercises the code path where the leak would occur without the fix from patch 1/2.

The test setup is thorough, configuring all necessary pseudo-merge parameters and using the boundary traversal feature flag. It follows the minimal case described in the commit message where a single commit (A) would be in the "haves" set during traversal. The test validates the bitmap count operation rather than memory behavior, serving as a regression test for the fixed code path.

This completes the series by providing test coverage for the memory leak scenario, addressing Patrick Steinhardt's earlier review feedback. The test is well-contained in its own repository setup and follows existing patterns in t5333 for pseudo-merge bitmap testing. The series now provides both the fix and verification for this edge case in bitmap boundary traversal.