This is a bugfix patch series finalizing the sequencer memory leak fix discussed in the thread. The v3 version had already reached consensus on the technical approach (using `BUG()` instead of error handling for invalid commands), and this iteration makes two final adjustments:

1. The first patch now moves the command validation to function entry using `is_fixup()`, implementing a suggestion from earlier discussion. This makes the function's contract clearer upfront while maintaining the same safety guarantees.

2. A new second patch removes translation markers (`_()`) from `BUG()` messages across several files, following Git project conventions that assert messages shouldn't be translated. This change affects not just the sequencer fix but also cleans up similar cases in `mktag`, `worktree`, and bitmap code.

The changes are mechanical and non-controversial - the thread shows all key reviewers (Phillip Wood, Junio Hamano) had already approved the approach. The patch series appears ready for merging, with the v3 changes addressing the last minor style points raised during review.