This is a platform compatibility fix for the reftable subsystem that has reached consensus and is being fast-tracked into the release.

The thread started with a report of build failures on the NonStop platform due to the reftable code using GNU-specific `__attribute__((__unused__))` syntax in its `REFTABLE_UNUSED` macro. Carlo Marcelo Arenas Belón proposed making this conditional on `__GNUC__`, which Junio Hamano approved for queuing. Patrick Steinhardt initially questioned why this differed from core Git's `MAYBE_UNUSED` handling, but after Jeff King's explanation about platform-specific preprocessing in `git-compat-util.h`, Steinhardt confirmed the solution is correct.

With all technical questions resolved and no remaining objections, Junio now indicates this will be fast-tracked into the codebase before the next release candidate. The fix is straightforward and maintains compatibility needed for NonStop while preserving the existing behavior on other platforms. This concludes the discussion with no further action needed.