This is a refactoring patch that's part of the ongoing `the_repository` removal effort, specifically addressing global state in `gc_before_repack()`. The patch follows up on Patrick Steinhardt's earlier change that removed a static flag from `gc_before_repack()` and made it return an error code instead of dying internally.

Ben Knoble's response acknowledges the improvement in making error handling more explicit by moving the exit from the helper function to the main `cmd_gc()`. However, he questions the choice of exit code 127, noting that while this is a common convention (typically indicating "command not found"), it appears without explanation in the patch context. The change also adds a guard to prevent duplicate calls when `opts.detach` is positive.

Key technical points for future reference:
- The patch modifies `builtin/gc.c` to remove static state flag `done`
- Changes `gc_before_repack()` to use error returns instead of `die()`
- Adds explicit exit(127) in `cmd_gc()` for error cases
- Uses `opts.detach` to prevent duplicate calls
- Maintains the same functionality for `pack_refs` and `reflog_expire` tasks

The discussion appears to be converging on the technical approach, with only minor questions remaining about the exit code choice. The patch appears well-contained and focused on its stated goal of removing global state.