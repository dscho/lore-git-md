This is a refactoring patch in Patrick Steinhardt's series addressing maintenance operation race conditions. It fixes a minor but confusing inconsistency in function pointer typedefs within `builtin/gc.c`.

The patch corrects the typedefs for `maintenance_task_fn` and `maintenance_auto_fn` to properly declare them as function pointers rather than function types. This eliminates the need for users of these types to manually add pointer syntax, making the code more intuitive and consistent with typical C function typedef patterns. The change is purely syntactic - no behavior is altered, and all existing callers continue to work as before since the underlying function signatures remain identical.

This appears to be a straightforward cleanup that improves code clarity in preparation for later patches in the series. The change is small (5-line diff) and focused, with no apparent downsides. It follows the series' pattern of tightening up the maintenance subsystem's internal APIs before introducing more substantial changes to handle race conditions.