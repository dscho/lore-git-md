This is a v2 patch series addressing memory leak issues in pack-bitmap.c's boundary traversal logic, consisting of one refactoring patch and one new test case. The series builds on previous discussion that identified a real memory leak scenario when `cascade_pseudo_merges_1()` fails.

The first patch removes a conditional `bitmap_free()` check in `find_boundary_objects()`, making the cleanup unconditional. The commit message now better explains the ownership semantics - `roots_bitmap` is only used as a mutable reference by called functions and should always be freed locally. This addresses the memory leak path identified in review.

The second patch adds test coverage in t5333 that specifically exercises the failure path where memory would have previously leaked. The test manipulates pseudo-merge data structures to trigger the error case, verifying the fix works as intended.

Key technical details:
- Modifies pack-bitmap.c to always free `roots_bitmap` after cascade operations
- Adds test in t5333-pseudo-merge-bitmaps.sh to verify leak is fixed
- Follows pattern seen in similar functions like `find_objects()`
- Maintains same bitmap ownership model through call chain

The series shows good responsiveness to review feedback, with the author incorporating the deeper analysis into the commit message and adding the requested test coverage. The changes are focused and well-contained to the specific leak scenario discussed in the thread.