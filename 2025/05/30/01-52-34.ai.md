This is a bugfix patch for the sequencer, part of a v3 series that has already gained maintainer approval. The patch converts an error() call to BUG() in `update_squash_messages()` when handling invalid command types, since the function should only receive TODO_SQUASH or TODO_FIXUP commands from its caller. The change includes moving the validation check to function entry using the existing `is_fixup()` helper and removing the translated error message in favor of an untranslated BUG() assertion, aligning with the project's new convention that programmer-facing assertions shouldn't be localized.

The technical changes are straightforward: the validation now happens immediately via `if (!is_fixup(command)) BUG(...)` rather than failing later, and the error message loses its translation marker. The patch maintains proper buffer cleanup through the existing `repo_unuse_commit_buffer()` call. This change is part of a broader standardization effort for BUG() messages across the codebase, though that aspect is handled in the series' second patch.

The patch appears well-constructed with a focused scope - it touches only the necessary validation logic while preserving all existing behavior. The commit message clearly explains why BUG() is more appropriate than error() for this case. Since this is part of an approved series addressing both a specific memory leak and broader coding conventions, it seems ready for merging without further discussion.