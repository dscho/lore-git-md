Security/performance patch discussion continuing the thread about PATH handling for Git subprocesses. Phillip Wood responds to Patrick Steinhardt's concerns about PATH vs GIT_EXEC_PATH modification, arguing that prepending the Git executable's path (like /usr/bin) would be more disruptive than the current GIT_EXEC_PATH approach. The key technical point is that GIT_EXEC_PATH only affects Git's internal program lookup, while modifying PATH to include the Git binary's directory would impact all program lookups in child processes. This maintains the thread's focus on ensuring version consistency for Git subprocesses while minimizing side effects on non-Git programs. The exchange highlights the nuanced tradeoffs between different approaches to solving the core problem of maintaining Git version consistency in subprocesses without unnecessarily polluting the PATH environment variable. Phillip's position favors the current GIT_EXEC_PATH method as the less intrusive option, though the broader thread continues to debate whether this should apply to all non-Git commands or just be editor-focused.