This is a small refactoring patch that fixes a compiler warning by making type signatures consistent. The change addresses a `-Wsign-compare` warning that occurs when comparing `graph_line->width` (previously `size_t`) with `git_graph->width` (which is `int`). 

The patch removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro that was previously suppressing the warning and changes the type of `graph_line->width` to `int` to match the type used in the `git_graph` struct. This is a straightforward change that aligns the types where they interact, following the existing pattern in the codebase.

The modification is minimal (1 line changed, 3 lines removed) and focused on the type consistency issue. The commit message clearly explains the change and its motivation, though it doesn't discuss any broader implications or potential edge cases from the type change (which seems reasonable given the scope). This appears to be a clean, well-motivated change that should be uncontroversial to merge.