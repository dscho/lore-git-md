Test modernization patch in a series addressing Git's exec-path security issues. This is the first of four patches that prepares the test environment in t7005-editor.sh to be more robust by properly cleaning up environment variables and config settings between tests.

The patch used direct unset commands at the file level, but reviewer Patrick Steinhardt suggests moving to `test_when_finished` calls within each test case for better isolation. The author questions whether this would interfere with testing the sequence of editor overrides, indicating this may need more discussion about test design tradeoffs.

The changes are straightforward - converting direct config modifications to use `test_config` and adding cleanup for environment variables. The second hunk converting core.editor setting was already approved. This appears to be solid test infrastructure work that will make the subsequent security-focused patches easier to verify.