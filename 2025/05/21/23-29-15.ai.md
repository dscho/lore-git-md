This is a refactoring patch that introduces a new pathspec helper function as part of the series adding pathspec support to `git diff --no-index`. The patch creates `match_leading_pathspec()`, a wrapper around the internal `match_pathspec_with_flags()` that exposes the `DO_MATCH_LEADING_PATHSPEC` behavior needed for directory traversal in the no-index case.

The new function cleanly encapsulates two related flags (`DO_MATCH_DIRECTORY` and `DO_MATCH_LEADING_PATHSPEC`) that need to be set together when matching directory paths, avoiding the awkwardness of exposing raw flags through the public API. The implementation is minimal (16 lines added across pathspec.h and dir.c) and well-contained, with clear documentation explaining its purpose and relationship to the existing matching infrastructure.

This change lays groundwork for the subsequent patch that will implement pathspec filtering in `git diff --no-index`, where directory traversal requires matching leading path components. The commit message provides good context for why this new function is preferable to alternative approaches like exposing more internal flags directly. The patch appears technically sound and focused on its specific role in the larger feature series.