This is a refactoring patch in the final v3 series enhancing promisor-remote protocol validation. The email continues the naming discussion from earlier in the thread, specifically addressing how to name a helper function that finds promisor_info instances.

Christian Couder responds to Junio Hamano's suggestion about function naming, agreeing to use either `promisor_info_find()` or `promisor_info_find_by_name()` in the next version. The discussion clarifies that promisor_info instances are accessed both by name (for lookup) and via iteration (for advertisement generation and field matching), making a simple linear search currently sufficient.

Junio had raised the possibility of using strmap for optimization, but Christian argues this isn't currently worthwhile given the expected small number of promisor remotes. This aligns with the thread's earlier conclusion that such optimizations would be non-blocking future work.

The exchange shows the final naming resolution for patch 1/5's key helper function, with Christian accepting Junio's guidance while providing context about the actual usage patterns that make the simpler implementation appropriate. This completes one of the last open items in the series before final review.