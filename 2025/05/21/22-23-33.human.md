This is a discussion about a proposed Windows-specific feature addition, now facing objections about platform appropriateness.

Junio C Hamano follows up on brian m. carlson's objection to a patch that would show Windows users a tip about `git update-git-for-windows`. The core issue is whether this belongs in upstream Git given that Windows builds come from multiple sources (Git for Windows, Cygwin, MINGW), and only Git for Windows distribution actually provides this update command.

Junio probes whether the objection is to the concept entirely or just the implementation's broad `_WIN32` check, suggesting there might be room for a more targeted solution. The thread has moved from technical implementation to a design question about where platform-specific help text should live - whether in upstream (with proper conditional checks) or exclusively in downstream patches.

The discussion remains unresolved but is now focused on whether a more precise implementation could satisfy concerns, rather than the original simple approach. Git for Windows maintainer input would likely help determine the path forward.