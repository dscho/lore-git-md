This is a follow-up review comment on patch 3/6 of a documentation series about git-notes options. Junio Hamano notes that while the patch's splitting of combined `--[no-]option` forms into separate entries is correct, it surfaces an existing documentation gap regarding the default `--stripspace` behavior in git-notes.

The review identifies that the current documentation doesn't clearly state whether stripping whitespace and comment lines is the default behavior, or how it interacts with notes sourced from existing notes versus new input. Junio recalls from memory that the default stripspace behavior depends on the input source (stripping for new input but preserving existing notes), but suggests this should be documented in a separate follow-up rather than holding up the current series.

This continues the thread's theme of making implicit behaviors explicit in documentation, though Junio is content to defer this particular clarification to future work. The comment maintains the constructive tone seen throughout the review thread, with Junio providing implementation background while leaving the actual documentation update for someone else to tackle.