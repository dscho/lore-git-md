This is a thread-closing acknowledgement from the maintainer for a two-patch bugfix series addressing git stash's option parsing behavior. Junio Hamano confirms he has queued both patches from Phillip Wood that fix and improve the handling of `-p/--patch` with pathspecs in `git stash`.

The series resolves two related issues: first restoring the ability to use `git stash -p <pathspec>` (which broke in 2020 due to incorrect argv handling), and second allowing more flexible option ordering like `git stash -m <message> -p <pathspec>`. The implementation carefully balances backward compatibility with new flexibility by using PARSE_OPT_STOP_AT_NON_OPTION when push is assumed, while still rejecting malformed commands.

The patches modify builtin/stash.c's option parsing logic and add comprehensive test coverage in t3903-stash.sh. The thread shows consensus on the split approach, with the maintainer now accepting the final version. This concludes the discussion that began with identifying the regression and exploring solutions, resulting in these two focused fixes that maintain the expected behavior while expanding valid syntax.