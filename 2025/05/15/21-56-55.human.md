Maintainer note fixing a typo in a commit-graph bugfix patch. This is a brief administrative follow-up in a thread about fixing a memory leak in `fill_oids_from_packs()` when pack index opening fails. 

Junio Hamano notes he will correct the minor typo in the subject line ("commit-grap" -> "commit-graph") when applying the patch. The patch itself has already been through three iterations with thorough review from Patrick Steinhardt, who confirmed the technical correctness of the memory leak fix. The thread shows complete consensus on both the problem and solution, with this being the only remaining cosmetic issue before merging.

The core fix properly handles resource cleanup in the error path of `fill_oids_from_packs()` by adding `close_pack()` and `free(p)` calls when `open_pack_index()` fails. This addresses a case where a `packed_git` struct would leak. With this final subject line correction, the patch is ready for inclusion in the codebase.