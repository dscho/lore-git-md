This is a maintainer's final approval for patch 3/11 in a series addressing CodeQL static analysis findings. The patch converts a heap-allocated `write_commit_graph_context` to stack allocation in `commit-graph.c`, improving memory safety.

Junio Hamano agrees with Jeff King's suggestion to use `memset()` instead of `calloc()` for initialization, noting this smaller change would be easier to merge with other development branches. The maintainer also confirms the `.oid` member and parameter renaming is correct.

This concludes the review for this particular change in the series, which has already received thorough technical review from Jeff King. The patch is now ready for integration along with the rest of the approved static analysis cleanup series.