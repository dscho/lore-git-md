This is a refactoring patch in the ongoing CodeQL static analysis series, specifically addressing redundant conditionals flagged by the tool. Jeff King (Peff) reviews Johannes Schindelin's changes to `help.c` and `transport-helper.c` that simplify `if/else if` chains into clearer `if/else` structures when the second condition is the exact inverse of the first.

The changes are straightforward readability improvements rather than functional fixes - converting patterns like `if (i <= 0) ... else if (i > 0)` to the more direct `if (i <= 0) ... else`. Jeff concurs with the approach, noting the simplified version avoids making readers wonder about missing edge cases. Both reviewers agree the changes are correct and improve code clarity without altering behavior.

This continues the thread theme of addressing CodeQL findings while maintaining careful attention to whether changes actually improve the code versus just satisfying the analyzer. The patch is uncontroversial and ready for inclusion, representing one of the simpler fixes in the broader static analysis cleanup effort.