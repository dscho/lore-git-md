Maintainer response in the rebase comment character discussion. Junio C Hamano weighs in on Elijah Newren's patch to prefix commit messages with '#' in rebase todo lists, marking them clearly as comments rather than editable directives.

The email provides maintainer-level validation of the technical approach, noting the change happens at the output layer without affecting internal commit matching logic for features like `--autosquash`. Junio explicitly states this shouldn't break core functionality, though acknowledges potential impact on custom todo-editors that might parse the output incorrectly - dismissing such cases as not worth worrying about if they're matching against commit messages instead of the provided object names.

Key technical points confirmed:
- Change is output-only, preserving internal processing
- Autosquash matching remains unaffected
- Hardcoded '#' approach deemed acceptable
- Custom editor breakage considered low-priority

The response includes example output showing the visual transformation from `pick $COMMIT2 two_split` to `pick $COMMIT2 # two_split`, which Junio finds clearly communicates the intended distinction. His "cute and clever" remark suggests approval of the UX improvement's elegance while the technical details reinforce Phillip Wood's earlier assessment of the patch's correctness.

This moves the thread closer to resolution, with the last major maintainer concern (about editor compatibility) being explicitly downplayed. The patch now appears to have consensus on both its technical implementation and UX rationale.