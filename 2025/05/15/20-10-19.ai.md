This is a discussion about Change-ID metadata and email workflows, continuing the thread about social adoption factors for commit metadata.

Nico Williams responds to Junio Hamano's analysis of why certain metadata gets accepted while other forms don't. The key technical point is a suggestion to add `git log` filtering options for trailers, allowing users to hide metadata they don't care about. This would address the visual clutter concern while preserving the information for those who need it.

The email reinforces the thread's emerging theme that metadata adoption depends on both utility and presentation. Nico observes that Change-IDs are rarely useful in typical `git log` inspection, suggesting their value is situational rather than universal. This supports Junio's earlier point about `Signed-off-by` being tolerated despite verbosity because of its consistent utility.

The exchange highlights two technical aspects worth noting for future reference: 1) the potential for `git log` to gain trailer-filtering capabilities, and 2) the distinction between always-useful metadata (like `Signed-off-by`) and situationally-useful metadata (like Change-IDs). The discussion remains focused on email-based workflows, with no new proposals for header-based storage in this message.