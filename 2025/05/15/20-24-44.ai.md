This is a follow-up in the `git diff --no-index` pathspec support thread, confirming implementation direction. Jacob Keller acknowledges Junio C Hamano's suggestion to use `--` separator in documentation examples for clarity when showing the new pathspec-after-targets syntax. The email indicates a v2 patch exists but requires refinement before final polishing, with some temporary implementation hacks still present.

Key technical details for future reference:
- The implementation will maintain strict argument ordering: two comparison targets (files/directories) followed by pathspecs
- Documentation will recommend (but not require) `--` separator to visually distinguish targets from pathspecs
- Current work-in-progress has some temporary hacks that need cleanup before final submission
- Behavior intentionally mirrors `git diff-tree -r`'s treeish-then-pathspec structure

The thread shows steady progress toward implementation, having moved past design discussion into concrete coding. Next expected steps are a cleaned-up version of the v2 patch with proper documentation and resolution of the remaining implementation hacks. No new technical questions or concerns are raised in this email - it primarily serves as an acknowledgment of the agreed-upon direction.