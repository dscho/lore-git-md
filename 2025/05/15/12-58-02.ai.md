This is a style refinement in the ongoing diff context configuration series. Junio C Hamano points out an unnecessary stylistic issue in the patch - semicolons after closing braces in the config validation code for `diff.context` and `diff.interHunkContext`. Phillip Wood acknowledges the correction, noting it was missed in review.

The change affects the `init_add_i_state()` function where config values are read and validated. While functionally correct, the extra semicolons violate C style conventions since they create empty statements after block closures. This is a minor cleanup in code that was otherwise approved, showing the final polishing phase of the series where small style issues are being caught.

The thread context shows this is one of the last remaining items before the series can be finalized, with the test organization discussion being the other outstanding point. Junio's sharp eye caught this detail that slipped past previous reviews, demonstrating the value of multiple reviewers even for seemingly complete patches.