Documentation update consolidating email provider credential helpers. This is part 1 of a 2-patch series that reorganizes how Git documents email configuration tools.

The patch adds two references to the git-send-email documentation:
1. Links Google's `sendgmail` tool as an alternative method for Gmail authentication
2. Adds the `git-credential-yahoo` helper to the existing list of email credential helpers

This continues the thread's evolution from documenting Yahoo-specific SMTP settings to focusing on credential helper organization. The changes are purely documentation - no functional changes to Git itself. The patch follows Junio Hamano's earlier approval of the technical approach while simplifying the structure by keeping all email helper references in git-send-email.adoc rather than splitting them across files.

The commit message is straightforward but doesn't explain why these specific changes are being made now. However, the thread context shows this is part of a deliberate reorganization responding to earlier feedback about documentation structure. The changes appear technically sound and maintain consistency with the documentation's existing style.