Defensive programming patch for `git stash`'s tree parsing logic. This is part 6 of a 14-patch series focused on adding error checks for static analyzer findings across Git's codebase.

The patch modifies `reset_tree()` in builtin/stash.c to handle NULL returns from `parse_tree_indirect()`. The change adds a NULL check for the `tree` variable before proceeding with `parse_tree()`, returning -1 if either operation fails. This matches the series' pattern of hardening error handling without changing observable behavior.

The change is small (1 line modified) but addresses a potential NULL dereference identified by CodeQL static analysis. The patch follows the series' conservative approach - maintaining existing functionality while adding safety checks for theoretical edge cases. Like other patches in the series, it targets a specific subsystem (stash operations) while being part of a broader code quality improvement effort spanning multiple areas of Git's codebase.

Key technical details for future reference:
- Function modified: `reset_tree()` in builtin/stash.c
- Checks added for NULL return from `parse_tree_indirect()`
- Maintains same error return value (-1) as existing `parse_tree()` failure case
- Part of systematic NULL-check additions across the codebase
- Follows pattern seen in earlier patches for commit parsing and object lookup