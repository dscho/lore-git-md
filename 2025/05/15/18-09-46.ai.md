This is a design discussion follow-up in the `git diff --no-index` pathspec support thread. Junio C Hamano confirms Jacob Keller's proposed approach of placing pathspec arguments after the two comparison targets, drawing a parallel to `git diff-tree -r`'s argument structure. 

The email solidifies the technical direction by endorsing the pathspec-after-targets syntax while suggesting optional use of `--` in documentation examples to visually separate comparison targets from pathspecs. This maintains consistency with repository-based diff operations that was a key concern in earlier discussion.

Key technical points reinforced:
- First two arguments remain the comparison targets (files/directories)
- Subsequent arguments are treated as pathspecs when comparing directories
- Documentation should use `--` as a visual separator even when not strictly required

The response indicates consensus is forming around this approach, with no new concerns raised about the parsing semantics. This moves the discussion closer to implementation, with the next expected step being a revised patch series from Jacob Keller demonstrating the pathspec solution. The suggestion about `--` usage hints at documentation considerations that will need addressing in such a series.