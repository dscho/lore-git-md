This is a follow-up in the `git diff --no-index` pathspec support thread, confirming technical direction. Jacob Keller acknowledges Junio C Hamano's suggestion to use `--` separator in documentation examples for clarity when specifying pathspecs after comparison targets. The email indicates a v2 implementation exists but still needs refinement before final polishing, with some current "hacks" that require feedback.

The exchange shows consensus on the core approach: first two arguments as comparison targets (files/directories), followed by optional pathspecs. This maintains consistency with `git diff-tree -r` behavior while solving the original need to filter directory comparisons outside repositories. The thread has clearly moved from design discussion to implementation phase, though some rough edges remain in the current code.