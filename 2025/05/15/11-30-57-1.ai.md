This is a technical discussion thread about batched reference updates in fetch operations, specifically addressing directory/file (D/F) conflict handling during pruning operations. Patrick Steinhardt follows up on Karthik Nayak's explanation of why pruning must remain an independent step rather than being batched with other reference updates.

The key technical point clarified is that while the ref-transaction system's `extras` parameter can prevent logical conflicts, the files backend's physical locking mechanism still requires independent handling of D/F cases. Karthik provided a concrete example from the test suite showing how simultaneous locking of `refs/remotes/origin/dir` (as a directory) and `refs/remotes/origin/dir/file` would fail. Patrick acknowledges this is a valid limitation of the current implementation and suggests adding a TODO comment to document the edge case.

The discussion reveals deeper architectural questions about whether conflict resolution should be handled more fundamentally in the ref-transaction layer, though both parties agree this would be scope for a future series. This email resolves Patrick's initial question about transaction handling while leaving open the broader design question of how to better integrate conflict resolution into the transaction system. The exchange demonstrates careful attention to edge cases in the files backend's locking behavior while maintaining focus on the immediate performance optimization goals of the series.