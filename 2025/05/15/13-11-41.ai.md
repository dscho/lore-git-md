This is a refactoring patch in the ongoing CodeQL static analysis series, specifically targeting the commit-graph writing code. The change converts a heap-allocated `write_commit_graph_context` struct into a stack variable in `commit-graph.c`, eliminating a malloc/free pair while maintaining identical functionality.

The patch makes two key changes: first, it declares the context struct directly on the stack with initialization, and second, it updates all subsequent references to use the struct directly rather than through a pointer. This addresses a CodeQL finding about potential memory safety issues by ensuring the context's lifetime is clearly scoped to the function. The change also helps static analysis understand that storing addresses of local variables (like `bloom_settings` and `topo_levels`) in the context is safe since they share the same lifetime.

This follows the pattern seen elsewhere in the series of making small, focused changes to improve code robustness without altering behavior. The modification touches a core data structure in commit-graph writing but does so in a straightforward way that maintains the existing control flow and error handling. Like other patches in the series, this represents defensive programming rather than fixing an active bug.