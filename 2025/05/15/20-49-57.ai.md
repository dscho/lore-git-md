This is a refactoring patch in the static analysis cleanup series, specifically addressing the commit.c simplification that was previously reviewed. Jeff King proposes a more descriptive solution than the initial numeric comparison approach, extracting the intent-to-add entry check into a dedicated `has_non_ita_entries()` function. The new implementation:

1. Moves the ITA entry detection logic into a clearly named helper function
2. Uses an early-return pattern when finding any non-ITA entry
3. Eliminates the temporary `ita_nr` counter that was only used for the numeric comparison
4. Maintains the same behavior while making the code's purpose more obvious

The change follows Junio Hamano's earlier endorsement of making the original intent clearer through explicit loop-based checking rather than numeric comparison. This represents the final refinement of the commit.c portion of the static analysis series, having now received approval from both Jeff King and Junio Hamano. The patch demonstrates the thread's theme of using static analysis findings as opportunities for genuine code quality improvements beyond just satisfying the tool's requirements.