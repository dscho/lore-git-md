This is a discussion continuation in the documentation/UX thread about marking commit descriptions as comments in rebase todo lists. Phillip Wood responds to Reto's concerns about readability impact from syntax highlighting, defending the original patch's approach.

Phillip makes several technical points in defense of the proposed change: Git already uses '#' in merge command lines and appends '# empty' to empty commits in the same context, so this would be consistent with existing behavior. He argues that editors should only treat lines starting with the comment character (default '#') as comments, not any line containing '#', and considers editors that do otherwise to be buggy. The response clarifies that the patch uses a hardcoded '#' rather than the configurable comment_line_str.

The exchange highlights the tension between preventing user errors (original motivation) and maintaining readability (Reto's concern). Phillip's position is that editor implementations shouldn't dictate Git's output format, and that proper editor behavior would preserve readability while still making the comment status clear. This moves the discussion toward whether Git should accommodate current editor behaviors or expect editors to handle Git's output format correctly.

Key technical details for future reference:
- Current behavior already uses '#' in merge command lines (between parents and subject)
- Empty commits get '# empty' appended
- The patch uses hardcoded '#' rather than comment_line_str
- Debate centers on whether editor syntax highlighting behavior is correct/expected

The thread remains unresolved between the error-prevention and readability goals, with Phillip's response shifting focus to whether editor behavior should influence Git's formatting choices.