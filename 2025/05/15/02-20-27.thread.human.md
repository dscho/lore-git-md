Here's a human-readable summary of the thread so far:

The thread discusses a proposed change to make it clearer that commit descriptions in interactive rebase todo lists are comments rather than editable directives. The original patch, whose author isn't named in the summaries, suggests prefixing these messages with '#' to visually distinguish them, similar to how merge directives are already marked. The change would modify several test files and the sequencer output format.

Reto has now responded with concerns about this approach. While he agrees with the goal of preventing user confusion, he worries that marking the messages as comments would make them less readable in editors that dim comment text. He relies heavily on these messages (rather than commit SHAs) when navigating during rebases. The discussion now needs to balance two competing needs: preventing mistaken edits versus maintaining clear visibility of the commit messages that serve as navigation aids.

No alternative solutions have been proposed yet, though Reto hints that adding explanatory text might be preferable to changing the formatting. The thread is at an early stage with these two perspectives now on the table.