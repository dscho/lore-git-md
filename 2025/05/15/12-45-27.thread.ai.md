[PATCH v2 2/14] object-name.c: add NULL check for commit reference

This patch modifies the `get_parent()` function in object-name.c to add a defensive NULL check for the return value of `lookup_commit_reference()` before calling `repo_parse_commit()`. The change addresses a potential NULL pointer dereference identified by CodeQL static analysis. The modification is minimal (one line change) and maintains the existing error handling flow, simply adding the NULL check to the conditional that already handles parse errors.

The patch comes from Johannes Schindelin (Dscho), a long-time contributor and Git for Windows maintainer. While the commit message doesn't specify whether this NULL case occurs in practice, the change represents a legitimate safety improvement. The patch is part of a larger 14-part series (position 2/14), though the broader context isn't evident from this individual change. Reviewers may want to understand real-world scenarios where `lookup_commit_reference()` could return NULL in this code path. The change follows standard Git conventions and is unlikely to be controversial given its defensive nature.