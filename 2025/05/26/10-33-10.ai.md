This is a refactoring patch in the signature verification improvement series, specifically touching gpg-interface internals. The change converts boolean flag definitions in gpg-interface.h from numeric constants to bitwise left shift operations, which is Git's preferred style for flag definitions. The patch also fixes minor indentation.

The modification is straightforward and mechanical, affecting three verification-related flags: `GPG_VERIFY_VERBOSE`, `GPG_VERIFY_RAW`, and `GPG_VERIFY_OMIT_STATUS`. The new form makes it more explicit that these are bit flags while maintaining identical runtime behavior. This aligns with the series' broader goal of cleaning up gpg-interface internals, though this particular change is purely stylistic rather than functional.

The patch appears well-contained and low-risk, with no test changes needed since it doesn't alter behavior. It follows established Git coding conventions and fits naturally with the series' technical debt reduction focus. The change is small enough that it's unlikely to be controversial, though reviewers may check that no code relies on the specific numeric values (which would be poor practice anyway).