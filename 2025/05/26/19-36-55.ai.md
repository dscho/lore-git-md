This is a brief discussion about email validation in `git-send-email`, spun off from the stash import/export series review. The thread context shows this is a side conversation inspired by a typo ("pwodd" instead of "pwood") that occurred earlier in the series.

Junio Hamano proposes adding optional validation to `git-send-email` to catch address typos (like missing @ symbols), particularly when external recipients with valid email formats are also included. brian m. carlson agrees this could be useful, suggesting it could be made optional via command-line flag for rare cases where a local address without @ is actually intended.

The discussion is separate from the main stash export/import work, but was prompted by the email address typo that occurred during that series' review. The core stash functionality (commit chains under `refs/stash-export/`, validation logic, and round-trip testing) remains the primary focus of the thread, with this being a tangential but potentially useful tooling improvement suggestion.