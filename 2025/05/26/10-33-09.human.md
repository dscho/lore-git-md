This is a refactoring patch in the ongoing signature verification improvements series. It's part of a v2 series that focuses on verification workflows before tackling fast-export/fast-import changes.

The patch simplifies fingerprint parsing in the gpg-interface code by reusing an already-computed string pointer rather than recalculating it. The change is purely internal - no behavior or output changes - and eliminates a redundant `strstr()` call when processing SSH key verification output. The commit message clearly explains the change and its motivation.

This appears to be a straightforward cleanup that improves code efficiency without introducing any risk. It's a small but meaningful optimization in the SSH signature verification path, which is part of the larger signature handling work being discussed in the thread. The patch follows the series' focus on verification infrastructure improvements before addressing the more complex export/import functionality.