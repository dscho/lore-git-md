This is a technical discussion about signature algorithm handling in Git's fast-export/import functionality, part of the ongoing signature verification improvements. Christian Couder responds to brian m. carlson's earlier critique about how signature types and hash algorithms are represented.

The core issue is whether to represent signatures as a single combined field (like "openpgp" or "x509") or separate fields for protocol and hash algorithm. brian m. carlson argues convincingly that the current combined approach can't properly represent cases like SSH signatures on SHA-256 commits, since the protocol (SSH) and hash algorithm (SHA-256) are orthogonal dimensions.

Christian acknowledges the limitation and mentions he's taken a different approach in v2 of the patches - one that involves actually checking signatures to extract both pieces of information. This suggests the implementation is moving toward the more robust separate-fields model, though the details aren't fully explained here.

The exchange shows thoughtful technical discussion about getting the data model right before finalizing the interface. While the thread has seen previous iterations (including some test failures and temporary reverts), this particular message focuses on the fundamental representation question that will affect how signatures are handled in Git's import/export pipeline going forward.