This is a test modernization and warning-fix patch in the completed string-list test conversion effort. 

The patch changes the binary search implementation in string-list.c to use unsigned types consistently, eliminating a sign comparison warning. The author modifies the `get_entry_index()` function to return a `size_t` instead of an `int` by adjusting the initial search bounds from -1 to 0, which maintains the same behavior while avoiding type mismatches. 

Patrick Steinhardt suggested improving the commit message to better explain why this change preserves the original behavior, which the author has agreed to do. This is part of a broader cleanup where all string-list tests have now been converted from shell to C unit tests, with only minor documentation and test isolation improvements remaining before the series can be merged. 

The change is straightforward and uncontroversial, building on historical validation from Jeff King about the safety of these modifications. With the commit message clarification, this patch appears ready for inclusion as part of the final series.