This is a follow-up discussion in the ongoing signature handling improvements thread, specifically addressing backward compatibility concerns in `git fast-import`'s signature algorithm parsing.

Christian Couder responds to Junio Hamano's concerns about changes to how `fast-import` handles signature algorithm names. The original patch modified the parsing logic to use a more generic `valid_signature_name()` check while still special-casing SHA-256, which raised questions about backward compatibility with existing fast-export streams that use "sha1" as the algorithm name.

The email indicates Christian is reconsidering the approach and has already prepared a v2 series that may address these concerns differently. This appears to be part of the broader effort to improve signature verification workflows while maintaining compatibility with existing repositories and workflows.

The discussion highlights the delicate balance between improving signature handling and preserving backward compatibility - a recurring theme in this thread. Christian's response suggests the original approach may be abandoned in favor of the v2 series' verification-first strategy, which has been progressing well with new features like the `--summary` flag for `git verify-commit`.