This is a refactoring patch in the ongoing signature verification improvements series. It's part of a v2 series that focuses on verification workflows before tackling fast-export/import signature handling.

The patch simplifies fingerprint parsing in the gpg-interface code by reusing an already-computed pointer rather than recomputing the same string search. The change is small but meaningful - it eliminates a redundant `strstr()` call when processing SSH key verification results. The original code would search for "key " twice when populating the fingerprint field, while the new version uses the result from the first search that was already stored in the `key` variable.

This appears to be a straightforward cleanup with no behavior change, part of the larger effort to refactor gpg-interface internals mentioned in the thread context. The patch maintains the same error handling and edge case behavior (when no key is found) while being slightly more efficient. As part of a larger verification-focused series, this small optimization contributes to the overall goal of cleaning up and improving signature handling code.

The change is minimal (just 1 line modified) and well-contained, touching only the SSH-specific parsing path in gpg-interface.c. It follows the pattern seen elsewhere in the thread of incremental improvements to the signature verification infrastructure. The commit message clearly explains both the current behavior and the improvement being made.