Part of the ongoing `the_repository` removal effort, this patch in Patrick Steinhardt's series continues decoupling object database operations from global state. The patch transforms `assert_oid_type()` into `odb_assert_oid_type()`, making it take an explicit `struct object_database*` parameter instead of implicitly using `the_repository`.

The changes are mechanical but important for the architectural goal:
1. Function renamed with `odb_` prefix to match new convention
2. Parameter added to pass object database explicitly
3. Internal lookup now uses `odb->repo` rather than `the_repository`
4. Call sites in `commit-tree.c` and `commit.c` updated to pass `the_repository->objects`

The patch maintains the series' pattern of methodically replacing implicit repository access with explicit parameter passing while preserving all existing behavior. This particular change affects object type checking during commit creation operations, ensuring they remain functional while becoming repository-agnostic. The diff shows straightforward updates to both implementation and header, plus two caller adjustments that follow the new pattern.