This is patch 13/17 in Patrick Steinhardt's series refactoring Git's object database (ODB) subsystem to remove dependencies on `the_repository` global. The patch focuses on renaming functions for consistency with modern coding guidelines.

The main change is renaming `oid_object_info()` to `odb_read_object_info()` and its extended variant to `odb_read_object_info_extended()`. This aligns with the series' goal of making ODB operations more self-contained by using the `odb_` prefix consistently. The patch includes compatibility wrappers to avoid breaking in-flight topics that still use the old names.

The changes are mechanical but widespread, touching 48 files with 213 insertions and 170 deletions. The patch maintains all existing functionality while improving naming consistency. It's a straightforward part of the larger architectural effort to decouple ODB operations from global state.

The patch appears well-executed with thorough updates across the codebase. It follows the pattern established in earlier patches of the series, where each step makes incremental progress toward the goal of removing `the_repository` dependencies while maintaining backward compatibility.