This thread covers maintenance updates for Git GUI, combining two independent improvements from Johannes Sixt and Oswald Buddenhagen. The first set of changes modernizes the translation workflow by removing the generated git-gui.pot file from version control (treating it as ephemeral) and updating the po/README with current repository information. The second change makes Git GUI respect the core.commentChar and core.commentString configurations when processing commit messages, bringing it in line with core Git behavior.

The technical changes are well-contained, touching only Git GUI's translation infrastructure (po/ directory) and commit message handling (lib/commit.tcl). The patch series is cleanly split between these two themes, with corresponding updates to the Makefile and git-gui.sh. Both improvements appear straightforward and follow established practices - the .pot file removal aligns with common translation workflow patterns, while the comment character handling ensures consistent behavior across Git interfaces.