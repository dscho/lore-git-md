The thread concerns a platform-specific fix for BSD systems where Git was reporting incorrect CPU counts when simultaneous multithreading (SMT) is disabled. Brad Smith's patch modifies the `online_cpus()` function in `thread-utils.c` to properly handle BSD's `HW_NCPUONLINE` sysctl parameter, ensuring Git reports active CPUs rather than physical cores. The change follows a fallback chain (`HW_AVAILCPU` -> `HW_NCPUONLINE` -> `HW_NCPU`) and leaves behavior unchanged on non-BSD platforms.

Patrick Steinhardt has already reviewed and approved the change, confirming it matches OpenBSD's documented behavior. The patch is localized to BSD systems and carries no risk for other platforms. With no outstanding technical concerns, this appears ready for merging - Brad's follow-up email serves as a gentle reminder that this straightforward compatibility fix is awaiting final inclusion.