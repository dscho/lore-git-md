This is a bugfix patch addressing a memory leak in the commit-graph code, now in its third iteration. The patch fixes a case where `fill_oids_from_packs()` would leak memory when `open_pack_index()` fails, by properly cleaning up the `packed_git` struct with `close_pack()` and `free()` calls.

The thread history shows the patch has already gone through technical review, with Patrick Steinhardt confirming the fix is correct while suggesting documentation improvements. Lidong Yan has addressed these in previous versions - this v3 now correctly uses the "commit-graph:" subsystem prefix in the subject line (fixing a typo from "commit-grap" in the original submission) and references the function name rather than line numbers in the commit message.

The change is minimal (just 2 lines added) and focused specifically on the error path, with no impact on normal operation. With all feedback addressed and consensus established, this appears ready for merging. The fix prevents memory leaks that would occur during commit-graph operations when pack index opening fails.