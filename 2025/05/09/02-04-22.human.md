Cleanup patch removing an unused reftable macro. This is version 2 of a patch that originally proposed fixing a theoretical memory leak in the REFTABLE_REALLOC_ARRAY macro. After discussion, the simpler solution of removing the entirely unused macro was agreed upon.

The patch now completely removes REFTABLE_REALLOC_ARRAY from reftable/basics.h, along with some minor formatting cleanup of the remaining REFTABLE_CALLOC_ARRAY macro. The change is safe since the macro wasn't actually used anywhere in the codebase. This represents a straightforward code hygiene improvement for the reftable backend.

The thread shows a good example of review improving a patch - the initial memory leak fix was replaced with a simpler and more complete solution of removing dead code entirely. The patch appears ready for inclusion.