This is the final patch in Patrick Steinhardt's systematic cleanup of Git's `contrib/` directory, specifically removing the obsolete `stats` subdirectory. Elijah Newren provides a minor typo correction ("nodoby" -> "nobody") in the commit message but otherwise appears to approve of the removal.

The patch eliminates three statistical analysis scripts from `contrib/stats/`:
1. `git-common-hash` (last modified 2012) - obsolete since core now handles hash prefix heuristics
2. `mailmap.pl` (2012) - broken due to Perl syntax changes
3. `packinfo.pl` (2008) - functional but unused, with better alternatives in core `git-verify-pack`

The removal follows the established pattern from the series: thorough justification for each deletion, clear documentation of obsolescence reasons, and no test impact since these were standalone utilities. With this patch, the technical removal work of the series is complete (27 components removed across 11 patches), totaling 308 lines deleted in this final removal.

The thread context shows consensus around these removals, with the remaining work being policy documentation in `contrib/README`. This patch serves as a concrete example for handling statistical/analysis tools in the cleanup policy, demonstrating patterns for dealing with core-replaced functionality, broken tools, and unused utilities.