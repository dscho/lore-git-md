This is a bug investigation follow-up in the `git pull --rebase` thread where commits are unexpectedly dropped after changing a remote URL. D. Ben Knoble provides technical analysis by bisecting the issue, finding the behavior has been present since at least version 2.0 (commit d717282532). The bisect log shows the problem persists through multiple major versions, though the root cause isn't identified yet. 

Key technical details: The reproduction script was modified for bisect testing (available on GitHub Gist), revealing the longstanding nature of the issue. The email also notes an unrelated build problem with Git master (linker issue with `_false_but_the_compiler_does_not_know_it`) and questions whether `GIT_DIR` environment variable behavior during `git bisect run` is documented/intentional, as it complicated script debugging. 

The core bug remains unresolved - where `git pull --rebase` drops commits (F-G in the test case) when a clone's remote is changed to point to a different upstream, while the same operation works correctly when the remote isn't changed. The bisect confirms this isn't a recent introduction but a long-standing behavior. The thread now has concrete version information showing the issue spans Git's entire 2.x series.