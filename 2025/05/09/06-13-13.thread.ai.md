Thread summary:

Platform compatibility fix for BSD CPU detection in `thread-utils.c`. The thread addresses incorrect CPU count reporting on OpenBSD/NetBSD systems with SMT disabled, where `HW_NCPU` returns physical cores rather than active ones. The patch modifies `online_cpus()` to first check `HW_AVAILCPU`, then the new `HW_NCPUONLINE` where available (BSD-specific), falling back to `HW_NCPU`. This maintains existing behavior on other platforms while fixing the BSD case. The change is localized to thread-utils subsystem and follows established patterns in the file. The commit message clearly explains the motivation and platform-specific impact. No open questions or objections raised; this appears to be a straightforward, low-risk improvement for affected BSD systems.