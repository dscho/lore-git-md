Thread summary:

Platform compatibility fix for BSD CPU detection in `thread-utils.c`, addressing incorrect CPU count reporting on OpenBSD/NetBSD systems with SMT disabled. The patch modifies `online_cpus()` to implement a fallback chain checking `HW_AVAILCPU`, then BSD-specific `HW_NCPUONLINE` (where available), finally falling back to `HW_NCPU`. This ensures accurate reporting of active CPUs rather than physical cores when SMT is disabled, while maintaining existing behavior on other platforms.

Patrick Steinhardt reviewed and approved the change, confirming it aligns with OpenBSD documentation and correctly implements the intended behavior. The review references OpenBSD man pages as authoritative source for sysctl parameter behavior. Brad Smith, the patch author, has followed up to check on merge status - the change remains ready for integration with no outstanding technical concerns.

Key technical details preserved: the function's three-tier fallback logic (`HW_AVAILCPU` -> `HW_NCPUONLINE` -> `HW_NCPU`), BSD-specific sysctl handling in thread-utils.c, and unchanged behavior for non-BSD platforms. The patch follows established patterns in the thread-utils subsystem and has clear commit message explaining platform-specific impact. This is a low-risk change that only affects BSD systems with SMT disabled, with no impact on other platforms or subsystems.