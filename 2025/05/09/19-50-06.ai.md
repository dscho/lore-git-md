This is a discussion about error handling in the object-name API, specifically regarding the `GET_OID_GENTLY` flag used in the stash import/export series. Brian Carlson responds to Junio Hamano's suggestion about making `get_oid()` consistently return errors rather than sometimes dying, noting that while the idea has merit, it would require a more extensive refactoring than this series can accommodate.

The exchange reveals that the current implementation's error handling is more complex than it appears, with multiple callers relying on the existing behavior where `get_oid()` may die rather than return an error. Carlson confirms this isn't the right series to tackle that larger refactoring, though he acknowledges the technical debt. The discussion remains focused on the immediate needs of the stash export/import feature while flagging a potential future cleanup direction for the object-name API.

Key technical points include the `GET_OID_GENTLY` flag's role in reflog handling for the stash export feature, and the observation that callers already need to handle errors from other parts of `get_oid()`. The thread suggests this API could benefit from richer error reporting (distinguishing between refs-not-found vs reflog-exhausted cases), but that work would require its own dedicated series. This email serves as a reality check about the scope of changes possible within the current feature development.