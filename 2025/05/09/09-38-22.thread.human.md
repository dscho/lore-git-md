Here's a human-readable summary of the thread so far:

A user has reported several pain points when trying to filter out bot-generated commits from repository history using `git log`. The core issue stems from the lack of a straightforward way to exclude commits by author, forcing users to resort to complex Perl regex workarounds instead of a simple `--exclude-author` option.

More significantly, the current filtering behavior breaks the history visualization when using `--graph`. The graph's simplification doesn't properly handle excluded commits, resulting in disconnected or misleading history lines. The user also notes that filtering by subdirectory causes branch and tag decorations to disappear, since the filtered output omits the commits those refs point to. They suggest parent rewriting could help maintain context by showing "fake" decorations (with appropriate visual indication) for excluded commits.

These issues particularly impact `--all` views and also affect gitk's display. The report highlights real usability problems in Git's filtering capabilities, especially around maintaining visual history context when excluding unwanted commits. The use case (filtering bot spam from a low-traffic repository) provides concrete motivation for improvements to how Git handles filtered history visualization.