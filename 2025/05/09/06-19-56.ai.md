This is a follow-up discussion in the parse-options memory leak thread, where Patrick Steinhardt raises concerns about the proposed solution's complexity and scope. The thread previously established a fix for xstrdup leaks in short option parsing via OPT_UNKNOWN macro and associated infrastructure.

Patrick's key objections are:
1. The current approach would require modifying all parse_options() callers to use OPT_UNKNOWN and manage strvec cleanup, which seems disproportionate for a bounded memory leak.
2. Questions whether the strdup_fn field has other future use cases beyond this specific scenario.
3. Suggests an alternative approach using parse_options_ctx with a clear function, though acknowledges this would still require widespread caller modifications.

The email indicates the leak may not justify the required changes, proposing to potentially abandon the fix rather than introduce complexity. This represents a significant shift from the thread's previous trajectory where technical implementation details were being refined - now questioning whether to proceed at all.

Technical context remains focused on:
- parse-options.c memory management
- OPT_UNKNOWN macro integration
- Caller-side cleanup requirements
- The PARSE_OPT_KEEP_UNKNOWN_OPT flag behavior

The discussion has moved from implementation refinement to fundamental viability assessment, with Patrick's position being that the cure may be worse than the disease unless others disagree. No new code changes are proposed in this email - it's purely evaluating the approach's merit.