Bugfix addressing a memory leak in commit-graph pack handling. This is a follow-up to Lidong Yan's original patch that fixes the same issue, with Patrick Steinhardt providing review feedback.

The core issue remains the same - a memory leak in `fill_oids_from_packs()` when `open_pack_index()` fails. Patrick confirms the technical correctness of the fix, which adds proper cleanup with `close_pack()` and `free(p)` in the error path. However, he suggests improving the commit message style by referencing function names rather than line numbers, following Git project conventions.

This appears to be a straightforward resource management fix with no outstanding technical concerns. The discussion now focuses on commit message formatting rather than the solution itself. The patch is small (2 lines) and well-contained, targeting a specific error case in commit-graph operations involving problematic packfiles.