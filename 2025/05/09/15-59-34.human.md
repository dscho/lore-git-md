This is a follow-up discussion in the completed series optimizing mmap usage for packed-refs fsck operations. Jeff King (peff) provides some final implementation clarifications to shejialuo's patch.

The discussion focuses on two small but important details in the final patch that enables mmap for large packed-refs files during fsck. First, Jeff notes the snapshot struct could be stack-allocated rather than heap-allocated since it's only used locally. Second, he questions why the code unmaps before using the content, which would defeat the memory optimization goal - this turns out to be a holdover from the existing code's handling of potential sorting operations.

Jeff confirms that while the Windows mmap strategy may be less critical now that packed-refs files are pre-sorted, it's not worth changing at this point. For fsck specifically, he notes the linear processing means we can maintain the mmap throughout verification without copying.

This wraps up the technical discussion on the series, with all major points now resolved. The thread has Junio's ack and is ready for integration, with these final clarifications about proper mmap usage patterns.