This is a follow-up discussion in the completed series optimizing memory usage in the packed-refs backend. The author (shejialuo) responds to Jeff King's suggestion about simplifying the mmap implementation in the final patch.

The exchange clarifies that the current implementation uses heap allocation for the snapshot struct when stack allocation would suffice, since the variable is local to the function. Jeff notes this was likely copied from existing code that needs heap allocation for returning snapshots to callers. 

The author acknowledges that directly using `xmmap()` would be simpler but prefers reusing existing logic for consistency. They also share broader architectural thoughts about unifying the fsck verification with other packed-refs operations, noting the current redundancy in parsing logic between different functions. While recognizing this as imperfect, they indicate it's a pragmatic solution for now with hopes to refactor toward more unified handling in the future.

This discussion doesn't introduce new changes to the patch series (which was already finalized) but provides useful context about the author's longer-term vision for the packed-refs code structure. The technical points about memory allocation strategies confirm the series has reached its optimization goals while maintaining safe mmap handling patterns.