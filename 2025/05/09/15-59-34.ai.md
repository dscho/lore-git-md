This is a follow-up discussion in the completed packed-refs memory optimization series, specifically addressing implementation details in patch 4/4 about mmap usage during fsck operations.

Jeff King (peff) provides final technical clarifications about the mmap strategy in response to shejialuo's questions. He notes that the snapshot struct allocation in the fsck function could be simplified to stack allocation since it's not returned to a caller. More importantly, he explains why the current mmap/unmap pattern makes sense for the general case (where sorting may be needed) but isn't necessary for fsck operations which process the file linearly.

The discussion confirms that while Windows' MMAP_TEMPORARY handling has historical quirks (originally useful for sorting operations), these don't apply to fsck. Jeff suggests the direct mmap/verify/unmap approach is correct for fsck specifically, while preserving the existing mmap strategy for other cases. This aligns with the thread's conclusion that the series is ready for integration with these implementation details resolved.

Key technical points:
- Stack allocation preferred for local snapshot struct in packed_fsck()
- Direct mmap usage without premature unmapping is correct for fsck
- Historical Windows mmap behavior (MMAP_TEMPORARY) remains for sorting cases
- No need to maintain mmap strategy infrastructure for fsck-only path
- Confirms optimization goal of avoiding in-memory copies during verification

This wraps up the final open questions about patch 4's implementation, with all reviewers (including Junio Hamano) having previously acked the series. The discussion provides useful context about mmap approaches but doesn't require further changes before integration.