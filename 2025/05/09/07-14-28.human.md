Bugfix addressing a memory leak in commit-graph pack handling. This is version 2 of a straightforward fix that adds proper cleanup when `open_pack_index()` fails in `fill_oids_from_packs()`. The patch now properly references the function name rather than line numbers in the commit message, addressing feedback from Patrick Steinhardt in the previous version.

The change is minimal - just two lines adding `close_pack()` and `free(p)` calls in the error path. This prevents memory leaks when dealing with problematic packfiles during commit-graph operations. The thread shows consensus on both the problem and solution, with no outstanding technical concerns. The patch appears ready for inclusion after this final documentation polish.