Refactoring patch in Patrick Steinhardt's object database (ODB) modernization series. This is patch 13/17, continuing the systematic removal of `the_repository` dependencies while maintaining functionality.

The patch renames `oid_object_info()` to `odb_read_object_info()` and its extended variant to `odb_read_object_info_extended()`, aligning with modern coding guidelines and the ODB-focused naming pattern established earlier in the series. The changes affect 48 files across the codebase, with mechanical updates to all call sites. Compatibility wrappers are added to avoid breaking in-flight topics.

Key technical changes:
- New function signatures in `odb.h` with explicit `object_database` parameters
- All internal ODB operations now use repository-specific parameters
- Maintains identical behavior for object info lookups
- Preserves test coverage as external interfaces were converted earlier

The patch follows the series' pattern of methodical, low-risk changes - each commit tackles a specific subsystem while keeping the build stable. This completes the ODB function renaming started in earlier patches, leaving only validation and documentation work remaining in the series.