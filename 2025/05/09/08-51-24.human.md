Build system refinement in the ongoing Meson configuration series. This is a follow-up to patch 3/5 which fixed system-wide gitconfig/gitattributes path handling in Meson.

Patrick Steinhardt responds to Ramsay Jones' implementation with two minor suggestions for the Meson build configuration: using the `/` operator instead of `+` for path concatenation (though noting it's not strictly necessary here), and adding empty newlines between option handling blocks for better readability. He also raises a question about whether to document the now-implicit default paths in meson_options.txt since they're no longer hardcoded in the file.

The changes are small polish items on top of the already validated technical fixes in this series. The thread context shows this patch has already been confirmed to handle path quoting safely, so these are purely about code organization and documentation clarity. The series as a whole remains focused on making Git's build system more robust across platforms while the broader discussion about Meson adoption continues.