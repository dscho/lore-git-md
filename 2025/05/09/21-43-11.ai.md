This is the final acknowledgement and integration of Patrick Steinhardt's object database refactoring series. Junio Hamano confirms he will apply the v2 patches as-is, noting:

1. The only substantive change in v2 was renaming `struct odb_backend` to `struct odb_alternate`, which Junio has no strong preference about despite the noisy diff. 

2. Patrick fixed some commit message and commit organization issues to maintain bisectability, which Junio approves.

3. The controversial rename of object-store.h to odb.h remains in place since Junio has already handled the fallout from the initial change.

The email marks the completion point for this major architectural change that eliminated `the_repository` usage from all object database operations. With this final review acknowledgement, the series is ready to be merged into Git's main branch, establishing the new `odb_*` API convention across the codebase. The changes affect core object handling while maintaining identical behavior, paving the way for future pluggable ODB backends.