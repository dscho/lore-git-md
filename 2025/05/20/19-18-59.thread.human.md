This thread introduces a straightforward refactoring patch from first-time contributor Lucas Eiji, addressing signed/unsigned comparison warnings in `add-interactive.c`. The change removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro that previously suppressed these warnings and instead adds explicit `long int` casts where needed. 

The modifications touch several functions in the interactive add machinery - including list operations and UI handling - but are purely mechanical in nature, maintaining all existing behavior while making the type comparisons explicit. The changes appear correct and well-scoped, following Git's coding style. While the commit message could elaborate slightly more on why these particular comparisons needed attention, the patch itself looks ready for inclusion as a clean warning cleanup.