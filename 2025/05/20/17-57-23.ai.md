Documentation patch continuing the series improving `--stdin` usage documentation in git-notes. This fifth patch focuses specifically on the copy operation's `--stdin` behavior.

The patch adds a clarification that `git notes copy --stdin` cannot be combined with object names given on the command line, unlike the `remove --stdin` operation which can. This distinction wasn't previously documented, though the behavior itself existed. The change follows the series' pattern of making implicit behavior explicit in the docs.

The addition is made as a new paragraph in the `copy` section of git-notes.adoc, using the same `++` syntax for paragraph breaks seen in earlier patches of the series. This maintains consistency with the documentation style established in previous patches.

This patch continues the author's thorough approach to documenting edge cases and limitations in git-notes' stdin handling. While a small change, it completes the coverage of stdin-related behaviors in git-notes commands, complementing the earlier work on stripspace and comment character documentation. The change is straightforward and unlikely to be controversial, following the same documentation patterns already established in the series.