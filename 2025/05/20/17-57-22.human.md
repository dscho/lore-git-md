This is the fourth patch in a documentation series improving `--stdin` usage and comment character references across several Git commands. The patch focuses on `git-notes`, clarifying how comment characters are handled in note messages.

The change updates the documentation for `--stripspace` to be more precise about comment character behavior. Instead of hardcoding `#` as the comment character, it now references the configurable `core.commentChar` setting and links to its documentation. The patch maintains the existing behavior description while making it clear the `#` is just the default that can be changed.

This continues the series' theme of making documentation more accurate and interconnected. The change is small but meaningful, helping users understand how comment character configuration affects note processing. Like the rest of the series, it's a focused improvement that follows established documentation conventions.