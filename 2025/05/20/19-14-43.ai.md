Documentation patch (4/6) in series improving `--stdin` and comment character docs. This patch updates git-notes documentation to reference `core.commentChar` configuration when describing comment stripping behavior, replacing a hardcoded `#` reference.

The change makes the default behavior (using `#` when no config is set) explicit while linking to the configuration option that can override it. Junio Hamano suggests the same phrasing concern raised in patch 1/6 applies here - the current "see core.commentChar" reference could be more integrated into the sentence structure. He proposes alternative wording that directly shows the configuration hierarchy (using the config if set, falling back to `#`).

This continues the series' pattern of making implicit behaviors explicit in documentation. The patch touches only git-notes.adoc, modifying the `--[no-]stripspace` option description. It maintains consistency with the series' approach of standardizing comment character terminology while avoiding redundant `#` character mentions. The open question from earlier in the thread remains unresolved here - whether to more directly incorporate the config precedence information rather than using a "see also" reference.