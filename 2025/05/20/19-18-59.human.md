This is a refactoring patch from a first-time contributor, addressing compiler warnings in the interactive add interface.

Lucas Eiji Uchiyama removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro from `add-interactive.c` and fixes the resulting signed/unsigned comparison warnings by explicitly casting `nr` values to `long int` where needed. This appears to be part of a Google Summer of Code 2025 microproject, though the specific motivation for targeting this particular warning suppression isn't explained in the commit message.

The changes are mechanical but widespread, touching comparison operations throughout the file. The patch looks straightforward and correctly applies the same fix pattern consistently. While it doesn't introduce new functionality, removing warning suppression macros is generally good hygiene, especially in code that's part of the interactive interface where stability is important.

As a first contribution, this shows good attention to detail in applying the same fix consistently across the file. The patch would benefit from a more detailed commit message explaining why these particular warnings were worth addressing now, but the technical changes themselves appear sound.