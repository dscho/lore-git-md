This is a follow-up in the git stash option parsing thread, addressing feedback on the proposed fix for "-p" pathpec handling. Phillip Wood acknowledges Junio Hamano's suggestion to split the regression fix from the feature enhancement, admitting the combined approach was "just being lazy". The email confirms the author will restructure the changes into two patches: one fixing the 8c3713cede7 regression where "git stash -p <pathspec>" broke, and another relaxing 9e140909f61's restrictions to allow combinations like "-m <message> -p <pathspec>". The technical approach remains using PARSE_OPT_STOP_AT_NON_OPTION when push is assumed, maintaining rejection of malformed commands like "stash <pathspec> -p" while enabling more flexible option ordering. The test cases shown (verifying both the fixed behavior and new relaxed parsing) will be retained across the split patches. This response indicates the thread is moving toward consensus, with the author accepting the maintainer's guidance on patch organization while preserving the technical solution and test coverage discussed earlier.