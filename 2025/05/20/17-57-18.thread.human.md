Here's a human-readable summary of the thread so far:

This thread covers a documentation patch series (currently at v1) that aims to clarify how Git handles comment characters and `--stdin` behavior across several commands. The author has identified gaps in the documentation, particularly around the relationship between `core.commentChar` and related configuration options like `commit.cleanup`.

The series began with improvements to `git-notes` documentation, showing how `--stdin` works with argument vectors and making the documentation consistent between copy and remove operations. It then expanded to address `git-stripspace`'s comment character handling and the connection between different configuration options.

The second patch, which just arrived, focuses specifically on `commit.cleanup` documentation. It replaces direct mentions of the `#` character with references to `core.commentChar` (while noting that `#` is the default), making the configuration relationship clearer. The changes follow a pattern of removing redundant character mentions while maintaining accuracy.

So far there haven't been any reviews or feedback on the series. The author has done thorough work cross-referencing related configuration and CC'ing relevant prior contributors (including Peff for `core.commentString` work and Teng Long for notes separator work). The changes are purely documentation improvements with no behavior changes - just clearer explanations of existing functionality.