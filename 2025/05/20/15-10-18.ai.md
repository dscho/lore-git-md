This is the second patch in a finalized 2-part series implementing a hidden `--skip-connectivity-check` option for `git receive-pack`, primarily for GitLab's Gitaly service. The patch adds the actual implementation after the test infrastructure was established in patch 1.

Justin Tobler responds to Patrick Steinhardt's review feedback with two key documentation and testing improvements. First, the option's documentation is clarified to explicitly mention it bypasses validation of both reference connectivity and object existence in the transitive closure. Second, the test case is strengthened to verify the repository remains intentionally unconnected after the bypass, ensuring the test setup's validity.

The implementation remains focused on GitLab's specific use case where Gitaly can provide stronger connectivity guarantees than Git's general-purpose checks. The hidden flag (OPT_HIDDEN_BOOL) prevents casual misuse while allowing GitLab to optimize its transaction flow. Test coverage uses the established `git send-pack --receive-pack="tee | git-receive-pack"` pattern to verify both successful bypass and proper error handling.

With these final refinements addressing documentation precision and test robustness, the series appears technically sound and ready for potential security review from connectivity experts. The changes maintain all previous safeguards including sideband message verification and explicit corruption warnings while better documenting the exact scope of bypassed checks.