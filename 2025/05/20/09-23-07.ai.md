Cover letter for a bugfix series addressing memory leaks in pack-bitmap.c. This is version 2 of the series, now expanded to include test coverage for the fixes.

The series builds on previous work to fix memory leaks during corrupted bitmap loading. The first patch (from Lidong Yan) reorders validation checks in `load_bitmap_entries_v1()` to prevent leaks when validation fails. The second patch (from Taylor Blau) addresses a related leak scenario by removing redundant cleanup code and relying on centralized `free_bitmap_index()` handling. The new third patch (also from Lidong) adds test cases that verify the fixes by deliberately corrupting bitmap files to trigger the error paths where leaks were possible.

Key technical details: The test cases in t5310-pack-bitmaps.sh create specific corruption patterns that fail at the right points in loading to exercise both validation failures and partial-load scenarios. The changeset is minimal (29 lines modified in pack-bitmap.c, 50 lines of new tests) and maintains the thread's focus on robust error handling in performance-critical bitmap loading paths.

The thread has shown consensus on the technical approach, with this version now addressing the testing challenge Jeff King had noted earlier. The test addition is notable as it provides LSan-verifiable coverage for these edge cases, though the corruption patterns required precise crafting to hit the right failure points. The series continues the theme of simplifying error handling while ensuring proper cleanup.