This is the final patch in a documentation series clarifying `--stdin` behavior in git-notes. The patch standardizes how the `--stdin` option is documented between the `copy` and `remove` subcommands, addressing an inconsistency where `remove`'s documentation was more complete.

Junio Hamano reviews the patch and suggests simplifying the option description to just indicate `--stdin` is only valid for `remove` and `copy`, deferring details to the subcommand sections. The author had included additional specifics about combining with command-line arguments, but Junio finds this potentially confusing on first read.

The patch itself:
- Adds `--stdin` documentation to the `remove` subcommand section (matching existing `copy` coverage)
- Updates the general `--stdin` option description to reference both subcommands
- Clarifies that `remove --stdin` can combine with command-line object names while `copy --stdin` cannot

This completes the series' goal of making stdin behavior explicit and consistent across git-notes documentation. Junio's approval ("OK") suggests the patch is ready to proceed, though he offers an optional wording refinement. The author can choose whether to incorporate this suggestion.