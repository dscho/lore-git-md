This is a performance optimization discussion in the ongoing thread about reducing syscalls for missing packfiles. Patrick Steinhardt responds to Jeff King's proposed midx cache modification that would add a "failed lookup" state (-1) to prevent repeated checks for missing packs.

Patrick agrees the patch is simpler than expected but raises concerns about potential race conditions if auxiliary files (.keep, .promisor) are checked in a different order than they're created/deleted. He notes the current order ensures we always see auxiliary data when the pack exists, and changing this could introduce races where we see the pack but miss its auxiliary files.

The discussion reveals subtle timing dependencies in packfile handling - auxiliary files must be created before their .idx and deleted after their .pack. While Jeff's midx cache solution addresses the immediate performance issue (repeated syscalls for missing packs), Patrick cautions that any optimization must maintain these ordering guarantees to avoid corruption.

The thread shows careful consideration of both performance and correctness tradeoffs, with Patrick providing valuable analysis of the file creation/deletion ordering constraints. The proposed midx cache change appears technically sound but requires ensuring it doesn't violate these underlying invariants.