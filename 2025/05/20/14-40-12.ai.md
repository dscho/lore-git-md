This is a test cleanup patch removing obsolete leak-check variables. The patch removes references to `GIT_TEST_PASSING_SANITIZE_LEAK` and `TEST_PASSES_SANITIZE_LEAK` from three test scripts, as these variables became meaningless after commit 1fc7ddf35b made leak checking unconditional. The changes affect two performance test scripts (`p5313-pack-objects.sh` and `p5314-name-hash.sh`) and one API test (`t6601-path-walk.sh`).

The patch is straightforward and appears correct - it simply deletes the now-unnecessary variable declarations. The commit message clearly explains the context and motivation, referencing the specific commit that made these variables obsolete. The changes are limited to test files and don't affect any actual test logic, just removing configuration that's no longer needed.

This appears to be a simple cleanup with no controversy expected. The patch follows the pattern of similar cleanups that would have been done when leak checking was made mandatory, and these appear to be cases that were missed in that initial cleanup. The changes are minimal and focused, touching only the specific lines that need removal.