This is a test cleanup patch removing obsolete leak-check variables. 

Karthik Nayak removes three instances where tests were still setting `GIT_TEST_PASSING_SANITIZE_LEAK` or `TEST_PASSES_SANITIZE_LEAK` variables, which became unnecessary after commit 1fc7ddf35b made leak checking always enabled. The changes affect two performance test scripts and one regular test script.

The patch is straightforward - it simply deletes the now-pointless variable assignments. The changes are mechanical and uncontroversial, cleaning up technical debt from when leak checking was optional. The commit message clearly explains the change and references the commit that made these variables obsolete.

This appears to be a well-justified cleanup with no behavioral impact, just removing vestigial configuration that could confuse future readers. The patch targets test scripts only and doesn't affect production code.