This is a performance optimization discussion about packfile handling, specifically around reducing syscalls for missing packs in the multi-pack-index (midx) cache. The email is part of an ongoing thread where Patrick Steinhardt responds to Jeff King's proposed implementation for caching failed pack lookups in midx->packs[].

Patrick agrees with Jeff King's approach of adding a third state (-1) to midx->packs[] to cache failed lookups, noting it's simpler than he initially feared. However, he raises concerns about potential race conditions if auxiliary files (.keep, .promisor, .mtimes) are handled out of order with the main packfile. The current implementation ensures these files are seen if the pack exists, and Patrick suggests this ordering should be documented as intentional rather than just an optimization.

The discussion reveals important constraints about packfile integrity checking - auxiliary files must be created before their .idx file and deleted after their .pack file. Patrick's analysis of `unlink_pack_path()` confirms the deletion order is correct, but he's less certain about creation ordering. This leads him to conclude his original patch reordering auxiliary file checks was incorrect, as it could introduce races where auxiliary data is missed.

Jeff King had also floated the idea of lazy-loading auxiliary files, but Patrick expresses skepticism about this approach due to the same race condition concerns. Both agree that while the optimization opportunities are real, the performance gains may not justify the complexity in most real-world scenarios with properly maintained repositories.

Key technical details:
- Midx cache now handles three states: NULL (unchecked), valid pointer (found), and -1 (failed lookup)
- Pack verification safety requires .idx checks before .pack checks
- Auxiliary file ordering must maintain creation/deletion invariants
- Original optimization target was pathological cases with stale midx references

The thread shows consensus forming around Jeff King's midx cache modification while maintaining all safety constraints. The discussion has clarified important invariants about packfile handling that will inform future optimizations in this area.