Junio Hamano raises key practical considerations about MPTCP implementation in Git, moving the discussion from architectural concerns to deployment strategy. This is a follow-up to Matthieu Baerts' technical clarifications about Linux's MPTCP requirements.

Junio questions whether unconditionally passing IPPROTO_MPTCP would truly be harmless, suggesting the kernel would handle this transparently if that were the case. He speculates there must be tradeoffs (like potential latency vs throughput) that make MPTCP sometimes undesirable, explaining why platform layers don't automatically enable it.

The email focuses on configuration granularity, asking important practical questions: Should MPTCP be enabled per-repository, per-host, or globally? Junio implies the current `core.mptcp` proposal may be too simplistic, as real-world usage might require more nuanced control based on network conditions and repository locations.

This moves the thread's focus from "can we implement MPTCP?" to "how should we expose MPTCP configuration?" - a sign the architectural concerns have been addressed to Junio's satisfaction. The email doesn't propose specific solutions but sets clear requirements for any implementation, emphasizing the need for flexible, user-controllable behavior rather than blanket enablement.