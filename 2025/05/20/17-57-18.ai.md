This is a documentation patch series (6 parts) focused on improving documentation around `--stdin` usage in git-notes and clarifying `core.commentChar` references. The series appears to be self-contained rather than part of a larger effort.

The cover letter explains the motivation came from wanting to fix `--stdin` documentation in git-notes, which led to discovering related documentation gaps. The author notes uncertainty about whether to document `core.commentChar` or its alias `core.commentString`, ultimately choosing the former since it's more commonly referenced.

The patches touch three documentation files, with changes that:
1. Documenting the default source for stripspace behavior
2. Adding cross-references between commit.cleanup and core.commentChar
3. Reorganizing git-notes options with negations
4. Noting comment character configuration in git-notes
5. Clarifying `--stdin` usage with argument vectors in git-notes copy
6. Making `--stdin` documentation consistent between copy/remove operations

The changes are focused and well-scoped, with each patch addressing a specific documentation gap. The author has identified relevant prior work (Peff's core.commentString addition and Teng Long's notes separator work) and CC'd those contributors appropriately. The series appears to be a straightforward documentation improvement with no obvious technical controversies.