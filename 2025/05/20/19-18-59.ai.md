This is a refactoring patch that removes the DISABLE_SIGN_COMPARE_WARNINGS macro and fixes signed/unsigned comparison warnings in add-interactive.c. The changes are mechanical, adding explicit casts to `long int` at various comparison points where `nr` (an unsigned value) is compared with signed variables.

The patch appears to be a first-time contribution from Lucas Eiji as part of a microproject, addressing compiler warnings that appear when building with DEVELOPER=1. The changes are straightforward and focused - each modification follows the same pattern of adding a cast to resolve the signed/unsigned comparison warnings that were previously suppressed by the DISABLE_SIGN_COMPARE_WARNINGS macro.

The changes affect several functions in add-interactive.c that deal with list operations and user interface handling for the interactive add functionality. Key functions modified include find_unique(), list(), list_and_choose(), get_untracked_files(), run_patch(), run_diff(), and run_add_i(). The patch maintains all existing functionality while making the type comparisons explicit.

This appears to be a clean, well-scoped change that follows the project's style. The commit message could be slightly more detailed about why these particular comparisons needed attention (beyond just removing the warning suppression), but the technical changes themselves look correct and focused.