[PATCH v3 0/7] git diff --no-index with pathspec filtering

Series adds pathspec support to `git diff --no-index` for comparing directories/files outside Git repos, completing the implementation in the final patch. Core problem remains enabling pathspec filtering while maintaining consistent behavior between repo and non-repo contexts. Key technical implementation includes PATHSPEC_NO_REPOSITORY flag, modified fixup_paths() and read_directory_contents() handling, with special processing for directory inputs and proper prefixing.

Current version incorporates refined NO_REPO pathspec handling and improved prefix_path() behavior from previous iterations. Recent technical discussion focuses on the PATHSPEC_NO_REPOSITORY flag implementation details:

1. Safety checks added to prevent invalid combinations:
   - BUG check for PATHSPEC_ATTR without index
   - Modified warning logic for paths outside directory trees in no-repo mode
   - BUG check preventing PATHSPEC_NO_REPOSITORY with repository-dependent features

2. Junio Hamano raises design question about magic_mask validation approach in parse_pathspec(), suggesting it may be preferable to pass PATHSPEC_ATTR and PATHSPEC_FROMTOP in magic_mask rather than enforce via BUG check. This touches on API contract design for repository vs non-repository boundary.

3. Implementation details under discussion:
   - Proper NULL istate handling for PATHSPEC_ATTR
   - Error message generation in no-repo contexts
   - Relationship between magic_mask and PATHSPEC_NO_REPOSITORY

Series maintains current limitations (intentional design choices):
- Fixed pathspec argument position
- Pathspec interpretation relative to each input path
- Negative pathspecs require full qualification
- Directory-vs-directory comparisons only for filtering

Previous design discussions remain relevant:
- Junio's review of match_pathspec_with_flags() exposure (approved with minor style notes)
- Ongoing discussion about prefix_path_gently() slash handling (leniency vs strict input validation)
- Security implications of "./", "//", "../" handling in user input

Test coverage remains at 110 lines covering edge cases (basic patterns, nested dirs, globs, negatives). The series appears technically sound but awaits resolution on the magic_mask validation approach before finalization. Junio's latest feedback suggests considering an alternative design for enforcing repository-dependent feature constraints in the no-repo case.