This is a bugfix patch series addressing memory leaks in pack-bitmap.c, building on previous fixes in the thread. The series now includes three patches: two fixes (one from Lidong Yan and one from Taylor Blau) for different memory leak scenarios during bitmap loading, plus a new test case to verify the fixes.

The first patch (carried over from v1) reorders validation checks in `load_bitmap_entries_v1()` to prevent leaks when validation fails. The second patch (new in v2) incorporates Taylor Blau's solution that eliminates redundant cleanup paths by relying on centralized `free_bitmap_index()` handling. The third patch (also new) adds test cases that deliberately corrupt bitmap files to trigger the error paths where leaks were possible.

The series shows good technical progression - starting with the initial leak fix, then incorporating a more comprehensive solution from another contributor, and finally attempting to add test coverage for these edge cases. The test addition is particularly notable as it addresses Jeff King's earlier concern about the difficulty of testing these scenarios. The changes are focused (only touching pack-bitmap.c and test scripts) and the commit messages clearly explain the problem and solution.

This appears ready for consideration, with the main open question being whether the new test cases are practical and maintainable for catching future regressions in this error-handling code.