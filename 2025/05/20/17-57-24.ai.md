This is the final patch (6/6) in a documentation series improving `--stdin` usage documentation in git-notes and related areas. The patch focuses on making the `--stdin` option documentation consistent between the `copy` and `remove` subcommands of `git-notes`.

The key changes are:
1. Adds `--stdin` mention to the `remove` subcommand description (matching how `copy` documents it)
2. Updates the shared `--stdin` option documentation to reference both subcommands
3. Clarifies that while `remove --stdin` can combine with command-line object names, `copy --stdin` cannot (a distinction that wasn't previously documented)

The patch completes the series' goal of making implicit behaviors explicit, particularly around stdin handling in git-notes operations. It follows the same documentation style established in earlier patches, using consistent terminology and cross-references. The change is focused and well-explained, with clear commit message references to the original implementations (2010 for copy, 2011 for remove).

This final patch touches only Documentation/git-notes.adoc, maintaining the series' tight focus on documentation improvements. It builds on the foundation laid by previous patches in standardizing option documentation format and cross-referencing related configuration.