This is a documentation patch that improves the `git-stripspace` man page as part of a 6-part series. The patch makes two small but useful improvements: it updates the formatting of the default comment character `#` to use backticks (matching modern documentation conventions) and adds a cross-reference to `core.commentChar` configuration.

The change helps readers understand where the default comment character comes from by pointing them to the relevant configuration option. The patch is clean and focused, with a clear commit message explaining both changes. As the first in a series, it sets up documentation improvements that will be expanded upon in subsequent patches, particularly around comment character handling and `--stdin` behavior in related commands.

Key technical details for future reference:
- Updates `git-stripspace.adoc` to reference `core.commentChar`
- Changes quote style for `#` character to backticks
- Part of broader effort to document comment character behavior consistently
- Follows Jean-Noël Avila's documentation formatting conventions
- Sets up context for later patches that will expand on comment character documentation