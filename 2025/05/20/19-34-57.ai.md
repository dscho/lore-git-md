This is the third patch in a series addressing exec-path security and compatibility issues, specifically focusing on making `prep_childenv()` behavior consistent across platforms.

The patch moves the `prep_childenv()` call outside the platform-specific `#ifdef` block in `run-command.c`, ensuring environment preparation happens uniformly on all platforms (including Windows). This is a preparatory change for the series' final patch which will modify the child environment to remove Git's exec-path from PATH for non-Git commands. The diff shows careful handling of the childenv memory management, moving the `free()` call to a common location after both platform-specific code paths.

Key technical details include:
- Moves `childenv` declaration outside platform blocks
- Calls `prep_childenv()` before platform-specific code
- Updates Windows path (`mingw_spawnvpe`) to use prepared `childenv`
- Consolidates `free(childenv)` after both code paths
- Maintains same environment preparation logic, just at different timing
- Touches core process spawning infrastructure in `run-command.c`

This appears to be a well-justified refactoring that enables the subsequent security fix while maintaining the exact same behavior at this stage. The change follows the series' pattern of cleaning up infrastructure before making functional changes. The patch maintains the existing memory management (just reorganized) and doesn't introduce new allocations or leaks. Windows compatibility appears preserved since Johannes Schindelen was involved in earlier discussion.