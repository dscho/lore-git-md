This is the second patch in a 6-part documentation series improving consistency around comment character handling in Git's documentation. The patch focuses on clarifying the relationship between `commit.cleanup` and `core.commentChar` in the config documentation.

The change updates the `commit.cleanup` documentation to explicitly reference `core.commentChar` as the source for determining comment characters, rather than hardcoding `#`. It also improves phrasing by removing redundant mentions of the `#` character and referring to it consistently as "the comment character" throughout. The changes are small but meaningful, making the documentation more precise and maintainable.

This follows the pattern established in the first patch of the series, continuing the effort to properly document how comment characters are configured and referenced across Git's documentation. The patch maintains a clean, focused scope - it only touches the `commit.adoc` file and makes straightforward wording improvements without introducing new concepts.

The change appears well-considered, with the commit message explaining the rationale for each modification. As with the rest of the series, this is a documentation-only change that doesn't affect functionality, just clarity and consistency.