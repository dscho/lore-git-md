This is a test patch in the ongoing series about detecting memory leaks during corrupt bitmap index loading. Jeff King responds to lidongyan's technical discussion about where to place the test infrastructure, suggesting an alternative approach using shell commands rather than C code.

Jeff proposes using `dd` to directly modify the bitmap file on disk, which would more accurately simulate real corruption scenarios while avoiding the need for custom C test code. His example shows how to corrupt a specific offset (calculated based on hash algorithm size) in the bitmap file and then verify Git's behavior. This approach would replace the current `test-tool bitmap load-corrupt` implementation.

Key technical points in Jeff's response:
- Suggests marking tests with `!SANITIZE_LEAK` until leaks are fixed, or sequencing the patches with leak fixes first
- Prefers shell-based corruption via `dd` over C code for more realistic testing
- Shows concrete example calculation for corruption offset (120 + hash size)
- Maintains the same test verification pattern (comparing rev-list counts)

The email continues the thread's technical discussion about test implementation while maintaining focus on the core goal of verifying memory leak fixes for corrupt bitmap handling. Jeff's proposal would simplify the test infrastructure by leveraging existing shell tools rather than adding new C test code.