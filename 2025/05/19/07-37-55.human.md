This is a follow-up in the test infrastructure discussion for corrupt bitmap loading. The author (lidongyan) agrees with Patrick Steinhardt's suggestion to integrate their corruption detection tests into v2 of the original memory leak fixes series rather than submitting them as a standalone patch. 

The tests were originally motivated by two prior discussions about memory leaks during corrupt bitmap loading. The current patch had implemented two specific corruption scenarios (invalid commit positions and XOR offsets) with test infrastructure that carefully handles mmap'd data. 

The key development here is the agreement to reorganize the work - this will make the connection between the test cases and the memory leak fixes they're designed to verify more obvious to reviewers. The author plans to update the series with revised commit messages that better cross-reference the related work.

This represents progress toward consensus on how to structure this testing work, though the actual implementation details of the tests themselves remain unchanged from previous discussion. The focus now shifts to the integration with the original series rather than debating the test approach.