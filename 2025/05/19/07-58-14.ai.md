Test patch series for bitmap corruption detection, now considering shell-based approach.

The author (lidongyan) responds to Jeff King's suggestion to use shell commands (`dd`) rather than C code for implementing bitmap corruption tests. This email confirms the technical direction for the test infrastructure while maintaining the original goal of detecting memory leaks during corrupt bitmap loading.

Key technical points from the exchange:
- The shell-based approach would use `dd` to corrupt bitmap files at specific offsets (120 + hash size) rather than C code
- This better simulates real on-disk corruption scenarios
- Maintains the same verification pattern comparing `rev-list` counts
-against expected values
- The test functionality remains focused on two corruption methods (commit positions and XOR offsets)
- Still planned for integration with the memory leak fixes series

The author agrees with Jeff King's proposed approach, indicating the next version will likely implement the shell-based corruption testing while keeping the core header manipulation in pack-bitmap.c due to hash-algorithm dependencies. The thread shows consensus forming around this hybrid approach - C code for precise header manipulation where needed, but shell for the actual corruption testing.