This is a technical discussion email in the ongoing thread about making `git reset --hard` preserve pre-reset state. The email responds to Junio Hamano's concerns about implementation approaches, specifically addressing two key points:

brian m. carlson agrees with Junio's concern that using the regular stash mechanism by default would be problematic for scripts, suggesting instead that the same stash machinery could be used but with a different ref to avoid interfering with the user's stash stack. This maintains the technical capability while addressing the script compatibility issue raised by Junio.

The email also clarifies the current reflog behavior - confirming that while `reset --hard` does record to the reflog when changing HEAD, it doesn't preserve the local modifications being discarded, which is the core functionality being requested in this feature. This distinction is important as it shows the gap between current behavior and the proposed feature, while also indicating that the reflog infrastructure might provide part of the solution.

The technical discussion here is narrowing in on implementation details, with both participants converging on the need to preserve state without disrupting existing workflows. The key open question remains whether to build this on top of modified stash machinery (with a separate ref) or to extend the reflog system to capture the discarded changes more comprehensively.