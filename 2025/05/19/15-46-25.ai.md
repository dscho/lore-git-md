Performance optimization for midx packfile handling, specifically addressing repeated failed lookups of missing packs. This is a follow-up to Jeff King's proposal for reducing syscalls in `add_packed_git()` by caching failed pack lookups in the multi-pack-index.

Junio Hamano expresses agreement with Jeff King's approach of adding a third state (-1) to the midx->packs[] array to cache failed lookup results, preventing repeated failed lookups of missing packs. The proposed patch modifies `midx.c` to:
1. Check for and handle the new "failed lookup" state (-1) in `prepare_midx_pack()`
2. Skip already-failed lookups by returning early
3. Properly maintain the new state during midx cleanup in `close_midx()`

The changes maintain the existing safety invariants around pack verification order while addressing the performance issue of repeated syscalls for missing packs. The implementation uses a type-punned pointer (casting -1 to void*) to represent the failed state, which is a common pattern in Git's codebase for similar purposes.

This moves the discussion forward from the theoretical proposal to concrete implementation, with Hamano's approval suggesting the approach is sound. The patch remains focused on the midx cache optimization while preserving the earlier agreement that .idx checks must still precede .pack checks for correctness. The changes are localized to midx.c but have implications for the packfile access performance characteristics discussed throughout the thread.