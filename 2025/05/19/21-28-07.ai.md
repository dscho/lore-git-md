Classification: Feature proposal discussion (safety enhancement for `git reset --hard`)

Junio C Hamano responds to the ongoing discussion about making `git reset --hard` safer by preserving pre-reset state. While expressing openness to the feature concept, he raises two key concerns about the implementation approach. First, he cautions against using the stash mechanism by default, as this would unexpectedly modify the stash stack for all users (including scripts) without their explicit request. Second, he questions whether `reset --hard` already records some information in the reflog, suggesting the team should first clarify what exactly needs preserving.

The email advances the technical discussion by focusing on implementation details rather than the feature's merit, which appears generally accepted. Hamano's concerns suggest the solution should avoid surprising side effects in automated contexts while still providing the safety net for interactive use. His question about existing reflog behavior indicates this might influence whether the feature needs to create new infrastructure or can build upon what's already recorded.

Key technical points for future reference: the distinction between stash-based preservation (which maintains a separate stack) versus direct reflog recording (which would be branch-specific), and the need to consider script usage patterns. The reflog message format question remains open - whether to standardize it or make it configurable. This positions the thread at a decision point between modifying existing mechanisms versus creating new ones.