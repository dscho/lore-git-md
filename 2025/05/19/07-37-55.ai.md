This is a follow-up in the test infrastructure thread for corrupt bitmap loading, specifically addressing how to organize the patches. The author (lidongyan) agrees with Patrick Steinhardt's suggestion to integrate this corruption detection test work into v2 of the original memory leak fixes series rather than submitting it as a standalone patch. This would help reviewers see the connection between the test infrastructure and the memory leak issues it aims to catch.

The email confirms the test's primary purpose is to detect memory leaks during corrupt bitmap index loading, referencing two prior discussions that motivated the work. The author accepts the technical compromise of keeping core corruption functionality in pack-bitmap.c while moving test cases to t/unit-tests/, and now also agrees to reorganize the patches for better reviewability by tying them directly to the memory leak fixes they support.

Key technical context remains unchanged from the thread summary: the test infrastructure handles two corruption scenarios (commit positions and XOR offsets) through a new `load-corrupt` subcommand in test-tool bitmap, with careful handling of mmap'd data. The integration approach suggests future commit messages will explicitly reference both the test infrastructure and the memory leak fixes it verifies.