This is the final patch in a v3 series enhancing the promisor-remote protocol with configurable validation of remote attributes during clone/fetch operations. The patch focuses on the client-side validation mechanism, allowing clients to check specific fields ("partialCloneFilter" and "token") against server-provided values.

The discussion has evolved from implementation details to broader questions about protocol flexibility. Patrick Steinhardt raises concerns about future use cases that might require more complex validation rules than the current binary matching provides. Christian Couder advocates for a conservative approach, preferring to wait for concrete use cases before adding complexity to the protocol.

The current implementation provides a straightforward mechanism where clients can verify that server-advertised field values match locally configured ones. This serves immediate needs for filter consistency and basic token verification while maintaining protocol simplicity. The series has already addressed several rounds of feedback, including structural improvements (replacing string_list with direct struct members) and documentation clarifications.

This appears to be a well-considered incremental improvement to the promisor-remote protocol, with careful attention to backward compatibility and security implications. The discussion about future extensibility is thoughtful but doesn't block the current work, which solves a clear immediate need. The patch is likely ready for final review and potential merging.