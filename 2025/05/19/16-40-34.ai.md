This is a status update from Derrick Stolee responding to Junio Hamano's "What's cooking" report, focusing on three patch series where Stolee is the author or maintainer. The email provides brief acknowledgments and context for each series' current state.

For the `ds/sparse-apply-add-p` series (4 commits), Stolee notes that version 2 has incorporated feedback from v1 and is now in good shape. This series makes `git apply` and `git add -i/-p` sparse-index aware, avoiding unnecessary index expansions. It was briefly removed from 'next' but is now being reconsidered for inclusion.

The `ds/scalar-no-maintenance` series (5 commits) adds `--no-maintenance` options to several `scalar` subcommands (`clone`, `register`, `reconfigure`), allowing repositories to be added without automatic maintenance. Stolee specifically thanks Junio for including the final patch in this series, which is progressing toward 'master'.

Regarding the `ds/path-walk-2` series (13 commits), Stolee acknowledges the v3 update but indicates he won't rush feedback on this version. This substantial series introduces path-based delta compression in `pack-objects` via a new `--path-walk` option and related infrastructure, with performance tests and shallow push support.

The email shows Stolee tracking the integration status of his ongoing work, particularly noting where feedback has been incorporated (`sparse-apply-add-p`), where additional patches were successfully included (`scalar-no-maintenance`), and where more review time is needed (`path-walk-2`). All three series represent significant performance-oriented improvements touching core commands (`apply`, `add`), the Scalar extension, and packfile generation.