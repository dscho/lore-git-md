Final patch in the promisor-remote protocol enhancement, focusing on code consistency by introducing string constants for the mandatory 'name' and 'url' fields. This follows the pattern established in previous patches for 'partialCloneFilter' and 'token' fields, completing the standardization of all protocol field names.

The changes are mechanical but important for maintainability:
- Adds `promisor_field_name` and `promisor_field_url` constants alongside existing field definitions
- Updates all string comparisons and format strings to use these constants
- Maintains identical protocol behavior while reducing hardcoded strings
- Includes a clarifying comment about the purpose of these constants

This patch concludes the v3 series by applying the same consistency improvements to the core name/url fields that were previously applied to the optional fields. The changes are straightforward with no expected impact on functionality, as they only affect internal string handling while preserving the protocol's external behavior. All major design decisions were settled in earlier patches, with this being a clean-up to match the established pattern.