This is a follow-up discussion on a refactoring patch in shejialuo's series to modernize string-list tests and implementation. The patch in question (v2 2/8) removes an unused `insert_at` parameter from the `add_entry()` function in string-list.c. Jeff King provides historical context explaining that this parameter was originally used by the now-removed `string_list_insert_at_index()` function, which was deleted in 2014 as it proved not particularly useful in practice. He notes that modern code would likely use `strmap` instead for similar functionality.

The discussion confirms the safety of removing this dead code while providing useful background about its origins. This appears to be a straightforward cleanup with no objections, just additional context from a Git veteran about why the parameter existed and why it's safe to remove. The patch seems ready to proceed as part of the larger series after this historical clarification.