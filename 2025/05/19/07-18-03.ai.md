This is a review comment on a patch in the string-list test modernization series. Patrick Steinhardt provides feedback on patch 4/8, which converts the binary search implementation in string-list.c to use unsigned types consistently to eliminate sign comparison warnings.

The patch changes `get_entry_index()` to return `size_t` instead of `int` and modifies the binary search algorithm to initialize `left` to 0 rather than -1. Patrick suggests the commit message should better explain why this change maintains equivalent behavior to the original implementation.

Key technical details:
- The change affects the binary search implementation in string-list.c
- Focuses on the `get_entry_index()` function's return type and initialization logic
- Part of the broader effort to eliminate -Wsign-compare warnings in the codebase
- Follows earlier patches that converted other parts of string-list to use `size_t`

This is a minor refinement request for documentation clarity in an otherwise complete patch. The thread context shows this series is nearing completion with only small test isolation improvements and optional inlining opportunities remaining. Patrick's feedback here is focused on making the reasoning behind the change more transparent to future readers.