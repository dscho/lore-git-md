Build system fix for Perl module installation path. This is part 2 of a 5-part series standardizing Git's build system configuration.

The patch corrects the YAML.pm Perl module's installation location when using Meson. Currently it gets placed in the wrong subdirectory (`perl5/Git/SVN`) instead of the proper `Memoize` subdirectory. The fix is straightforward - updating the `install_dir` path in the Meson build file to include the missing directory component.

This appears to be a simple but important correction to ensure consistent file placement between Make and Meson builds. The change aligns with the series' broader goal of maintaining path handling consistency across build systems. The fix is minimal (just one line changed) and well-contained, with no apparent side effects or complications.

As part of an already well-reviewed series that has addressed all major technical feedback, this patch seems ready for inclusion. It follows the pattern of other path corrections in the series while focusing specifically on the Perl module installation location.