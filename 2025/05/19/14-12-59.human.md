This is the final patch in a 5-part series enhancing the promisor-remote protocol with configurable validation of remote attributes. The series has already undergone extensive review and refinement through three iterations.

The patch completes a minor but consistent cleanup by introducing string constants for the mandatory "name" and "url" fields in the promisor-remote protocol, matching the approach already taken for the optional "partialCloneFilter" and "token" fields in previous patches. This eliminates string literals that were repeated in the code and maintains uniformity in how all protocol fields are handled.

The changes are straightforward - two new static string constants are defined and used in place of the literal strings in both the output generation and parsing code paths. The patch follows the same pattern established earlier in the series and doesn't introduce any new functionality or behavior changes.

As the concluding patch of a well-reviewed series that has addressed all previous feedback, this appears ready for final consideration. It provides a clean finishing touch to the larger effort of making promisor-remote field handling more maintainable and consistent.