This is a build system patch that aligns GIT_EXEC_PATH handling between Meson and Make builds. It's part 4/5 of a series standardizing path configuration across Git's build systems.

The patch ensures Meson builds handle GIT_EXEC_PATH consistently with Make builds in two key ways: First, it defaults to the same relative path 'libexec/git-core' when no custom libexecdir is specified. Second, it enforces the same restriction as Make - when runtime_prefix is enabled, GIT_EXEC_PATH must be relative (not absolute).

The change allows Meson users to customize the installation location via -Dlibexecdir, matching Make's gitexecdir option. The implementation carefully preserves backward compatibility while adding the same validation rules as Make. This continues the series' theme of ensuring consistent behavior between build systems.

The patch appears well-considered, with clear commit message explaining the motivation and behavior with concrete examples. It builds on prior review feedback and completes the GIT_EXEC_PATH alignment work started earlier in the series. With cross-platform testing already confirmed for the series, this looks like a straightforward improvement to the build system consistency.