This is a follow-up review comment on a patch in the string-list test modernization series. Patrick Steinhardt is providing feedback on patch 4/8, which converts the binary search implementation in string-list.c to use unsigned types consistently.

The patch changes the binary search algorithm's return type from `int` to `size_t` to eliminate a sign comparison warning. Patrick suggests the commit message could better explain why the modified algorithm (starting with `left=0` instead of `left=-1`) maintains the same behavior as before. This is a minor documentation improvement request for an otherwise technical change that's part of a larger, nearly complete series.

The thread shows this series has been thoroughly reviewed, with Patrick providing detailed feedback throughout. This particular patch appears to be in good shape otherwise, just needing a bit more explanation in the commit message to help future maintainers understand the equivalence between the old and new implementations.