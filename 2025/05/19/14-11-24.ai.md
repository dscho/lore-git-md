This is a technical discussion about field ordering and function organization in the v3 promisor-remote protocol patch series. Christian Couder responds to Karthik Nayak's review comments on the BNF grammar notation and implementation details from the previous version.

The key points addressed are:

1. Field ordering in the BNF grammar - Christian prefers keeping the simpler grammar notation with an explicit clarification that "name" and "url" fields must appear first, rather than complicating the grammar to enforce ordering. He notes the parsing is already relaxed to handle out-of-order fields.

2. Function naming and organization - Christian agrees to rename `valid_field` to `is_valid_field` for consistency but pushes back on inlining `is_allowed_field` and `fields_from_config`, explaining they have standalone value and are reused in later patches.

The discussion shows the series is in final polishing stages, with Christian accepting some naming improvements while defending his design choices around grammar simplicity and function organization. This email doesn't introduce new changes but clarifies the rationale behind existing design decisions in response to review feedback.

Technical details mentioned include:
- The BNF grammar in protocol-v2.adoc
- Field handling in promisor-remote.c
- Functions like `is_allowed_field`, `is_valid_field`, and `fields_from_config`
- The `promisor.sendFields` config key

The exchange demonstrates the careful attention to both protocol specification clarity and implementation cleanliness that characterizes this mature patch series.