This is a code review comment on the string-list test modernization series. Patrick Steinhardt points out that the `list` variable in the test function should be moved inside `t_string_list_split()` rather than being shared at the test function level. This would prevent potential test interference through shared state, following better test isolation practices. The comment applies not just to this patch but to subsequent commits in the series as well.

The review is focused on test implementation details rather than the core string-list functionality. Patrick identifies a specific improvement to the test structure that would make the tests more robust by eliminating shared state between test cases. This is a minor but important refinement to the test conversion work that has otherwise been progressing well through the series. The suggestion aligns with the series' goal of producing high-quality, maintainable test code.