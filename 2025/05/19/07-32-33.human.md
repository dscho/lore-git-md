This is a minor documentation refinement in the build system standardization series. Patrick Steinhardt points out a small duplication in the Meson build configuration - the default paths for system-wide gitconfig and gitattributes files are now specified both as comments and in the option descriptions. 

The change itself was made by Ramsay Jones in the third patch of the v3 series, which standardizes path handling for system configuration files. While the technical implementation is sound (removing hardcoded `/etc` paths in favor of more flexible configuration), Patrick notes this cosmetic issue where the default value information appears twice in the meson_options.txt file.

This is a trivial follow-up in an otherwise well-tested and reviewed series that's nearing completion. The thread context shows the broader work has already addressed all major technical concerns around path handling across build systems. Patrick's observation doesn't block the series but serves as a final polish item.