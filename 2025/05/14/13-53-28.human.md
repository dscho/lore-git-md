This is a bugfix patch addressing a memory leak in the sequencer code. The issue occurs in `todo_list_rearrange_squash()` when the function fails - several allocated memory blocks (`next`, `tail`, `subjects`, and `subject2item`) were not being freed in the error path. 

The patch fixes this by ensuring cleanup happens before returning in the error case, using a `goto cleanup` pattern that was already present for the success case. The change is straightforward and appears correct - it simply extends the existing cleanup logic to cover the failure path.

The impact is limited to cases where `todo_list_rearrange_squash()` fails, which happens when the script was already rearranged (as indicated by the error message). While not a common error case, fixing memory leaks is always worthwhile for long-running processes like the sequencer.

The patch is small (8 lines changed) and focused - it doesn't attempt any broader refactoring, just fixes the specific leak. The commit message clearly explains both the problem and solution. This looks like a solid, uncontroversial fix that should be easy to review and merge.