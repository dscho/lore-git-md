This is a follow-up discussion in the final stages of Patrick Steinhardt's object database refactoring series, which has already been approved for integration. The email clarifies a key architectural point about how the new ODB subsystem handles alternates.

Patrick confirms Toon Claes's understanding that the primary object directory is now treated as the first alternate in a linked list under the new `odb_alternate` naming scheme. This was an implicit design decision that wasn't fully documented in the original patches. Patrick agrees with Toon's suggestion to add this explanation to patch 02/17, which handles the struct renaming.

The exchange serves as a final documentation improvement before the series lands, ensuring the relationship between primary and secondary alternates is properly recorded. This doesn't affect the technical implementation or block the already-approved series, but provides important context for future developers working with the ODB interfaces. The discussion also reinforces that the `odb_alternate` naming convention (settled in earlier rounds) successfully captures the system's architecture.