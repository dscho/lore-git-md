This is a discussion response regarding the proposed `--exclude` option for `git diff --no-index`. Junio C Hamano engages with Jacob Keller's RFC patch that would add wildmatch-based file exclusion when comparing directories outside a repository.

The key point raised is whether introducing a new --exclude` option is the right approach, given that regular `git diff` uses pathspecs (including negative pathspecs with `:!`) for similar functionality. Hamano suggests that `--no-index` mode should ideally support the same pathspec mechanism as the regular diff for consistency, though acknowledges the technical challenges in implementing this due to `--no-index`'s more complex argument parsing requirements.

The response is supportive of the general need for exclusion capabilities in `--no-index` mode but questions the design direction, preferring pathspec alignment over a new option. This moves the discussion forward by framing the next steps around either extending pathspec support in `--no-index` or justifying why a separate --exclude` mechanism is necessary despite the inconsistency. The tone is constructive, treating the RFC as a starting point for deeper design work rather than rejecting the concept outright.