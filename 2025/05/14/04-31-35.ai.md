This is a follow-up discussion in the final stages of Patrick Steinhardt's object database refactoring series, specifically addressing documentation clarifications around the alternates architecture. The email confirms that the primary object directory is now conceptually treated as the first alternate in the linked list structure, which was a key point that needed explicit documentation. Patrick acknowledges Toon Claes's suggestion to add this architectural detail to patch 02/17 (the struct rename patch) and confirms the change has been made.

The exchange solidifies the naming convention (`odb_alternate`) and its relationship to backend implementations (`odb_packed_alternate`, `odb_loose_alternate`, etc.), while also documenting the important architectural nuance that the primary object directory is now modeled as the first alternate. This is purely a documentation refinement that doesn't affect the already-approved technical approach, but ensures the conceptual model is properly explained for future maintainers. The series remains ready for integration with these non-blocking documentation improvements addressed.