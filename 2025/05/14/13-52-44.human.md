This is a documentation patch that improves the help text for `scalar reconfigure`'s `--maintenance` option, following up on the recently finalized design for Scalar's maintenance configuration.

The patch clarifies the three possible values (`enable`, `disable`, `keep`) in both the command-line help (`-h`) and the scalar.adoc documentation. The changes make the interface more discoverable by showing the valid choices directly in the help text, following Git's conventional pattern seen in options like `--fixup` and `--diff-filter`.

This appears to be a final polish on the Scalar maintenance configuration series, addressing the last remaining documentation piece after the technical implementation was settled. The patch is small and focused, just updating help text to match the agreed-upon OPT_BOOL approach that maintains the same three behaviors through different default semantics. The author notes this could be sent as part of a v4 if needed, but it seems like a straightforward improvement that builds on the already-approved design.