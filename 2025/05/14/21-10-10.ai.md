This is a discussion response in the ongoing thread about adding `--exclude` pattern matching to `git diff --no-index`. Junio C Hamano engages with Jacob Keller's RFC patch, agreeing with the need for exclusion capabilities but questioning whether implementing a separate `--exclude` mechanism is the right approach compared to supporting full pathspec syntax.

Key technical points raised:
- Notes the inconsistency between `--no-index` mode (proposing new `--exclude`) and regular diff (using pathspecs with `:!` syntax)
- Suggests users would naturally expect feature parity between repository and non-repository diff modes
- Observes the command-line parsing for `--no-index` is already complex due to supporting multiple comparison modes (files vs directories)
- Implies pathspec support would be more powerful and consistent, though acknowledges implementation challenges

The response indicates this is still in the design discussion phase, with no clear resolution yet on whether to proceed with the current `--exclude` approach or invest in pathspec support. The thread context shows this builds on Jacob's initial RFC which lacked tests/docs and sought design feedback - Junio's reply provides that feedback while validating the core need for exclusion functionality.