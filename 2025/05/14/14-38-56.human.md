This is the final ack and merge decision for the oidmap refactoring series. Junio C Hamano confirms that all three patches in the series are now queued for merging, following Patrick Steinhardt's explicit approval of the changes.

The third patch, which embeds the `replace_map` oidmap directly in the `raw_object_store` struct rather than using a pointer, completes the series' goal of simplifying the oidmap implementation. As noted in the thread, this change eliminates unnecessary pointer indirection since the map is always allocated with its containing struct and never checked for NULL. The series has been thoroughly discussed, with no outstanding objections, it's now cleared for integration.

The changes affect several Git components including commit-graph handling, object store management, and the replace-object subsystem, improving code clarity and encapsulation throughout. The series represents a clean, well-reasoned refactoring that maintains existing behavior while making the implementation more straightforward.