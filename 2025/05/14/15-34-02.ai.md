This is a review response in the ongoing `--dry-run` feature series for `git merge-tree`. Kristoffer Haugsbakk provides final approval for Elijah Newren's v3 patches, noting they address all previous feedback. The interdiff shows the last documentation and code changes needed to complete the transition from the original `--mergeability-only` flag name to `--dry-run` per Junio Hamano's suggestion. All instances of the old flag name have been updated in both the man page and option validation logic in builtin/merge-tree.c. The changes are straightforward textual replacements with no functional impact, maintaining the same validation checks against incompatible options like `--messages` and `--stdin`. This marks the final polish needed before the series can be accepted, with all technical implementation and naming consensus already established in previous rounds.