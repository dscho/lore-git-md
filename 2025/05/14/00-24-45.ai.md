This is a cover letter for a feature patch series adding a `--dry-run` option to `git merge-tree`, now in its third iteration. The series builds on the previously summarized thread where the implementation was refined through review feedback.

The v3 changes are minimal documentation and code updates to fully convert from the original `--mergeability-only` flag name to `--dry-run` as suggested by Junio Hamano. The series remains focused on providing Git forges with an efficient way to check mergeability without computing full merge results, with two key optimizations: early exit on conflicts and reduced object writing.

The implementation spans merge-ort internals (adding a `mergeability_only` flag) and the user-facing `merge-tree` command (adding the `--dry-run` option with appropriate safety checks). Test coverage in t4301 verifies all behaviors. The changes touch merge-ort.c's blob/tree writing logic and early exit paths, plus documentation and option validation in builtin/merge-tree.c.

This appears ready for finalization with all prior feedback addressed, including:
- Complete terminology conversion to `--dry-run`
- Clear commit message explanations of edge cases
- Safety checks preventing incompatible option combinations
- Comprehensive test coverage

The series shows careful attention to detail in both implementation and documentation, with all review feedback incorporated across the three versions. The performance optimization for mergeability checking appears well-considered with clear benefits for hosting platforms.