This is a documentation patch that follows up on the completed Scalar maintenance configuration series. It improves the help text and documentation for the `--maintenance` option in `scalar reconfigure` to match the three possible states (enable/disable/keep) that were agreed upon in the thread.

The patch makes two key changes:
1. Updates the command-line help in scalar.adoc to show the valid values explicitly as `--maintenance=(enable|disable|keep)`
2. Modifies the option description in scalar.c to use the same format for the help text

This implements the documentation pattern that Junio Hamano suggested and approved earlier in the thread, bringing consistency with other Git commands that show valid choices in their help text (like `--fixup` and `--diff-filter`). The changes are purely presentational - the underlying behavior remains unchanged from the final OPT_BOOL implementation that maintains the "keep" default behavior.

The patch appears as a follow-up (numbered 5/4) to the completed series, suggesting it's a minor documentation polish that Derrick Stolee is submitting separately rather than resending the whole series. The changes are straightforward and align with the thread's resolution to improve the help text patterns while keeping the simplified OPT_BOOL interface.