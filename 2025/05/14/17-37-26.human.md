This is a bugfix patch addressing behavior in `git pull --rebase` related to fork-point handling. The patch adds explicit support for `--fork-point` and `--no-fork-point` options to `git pull --rebase`, and properly respects the `rebase.forkpoint` configuration setting. 

The author reports this fixes a prior bug report where `pull --rebase` wasn't behaving as expected due to always using merge-base's fork-point logic. The changes add new option handling in builtin/pull.c, including config parsing and command-line option processing, with clear fallback behavior when neither is specified (defaulting to fork-point enabled).

The implementation looks straightforward - it adds the new options alongside existing rebase-related options in pull.c, handles the config value, and properly clears the fork-point when disabled. The patch includes a debug message showing whether fork-point is being used, which could help diagnose future issues. 

This appears to be a well-scoped change addressing a specific pain point in rebase behavior, though the commit message could benefit from more detail about the original problematic behavior and how this fixes it. The patch seems ready for review, with no obvious technical issues.