This is the final patch (17/17) in Patrick Steinhardt's v3 series refactoring Git's object database (ODB) subsystem to remove `the_repository` global state. The patch renames `read_object_with_reference()` to `odb_read_object_peeled()` to better reflect its purpose (recursively peeling tags/commits to reach an object of specified type) and align with the series' naming conventions.

The change is mechanical, updating all callers (8 files) to use the new name and pass the ODB parameter explicitly rather than getting it from `the_repository`. The function's behavior remains unchanged - it still handles the object peeling logic for commits and tags while maintaining the same error conditions and return values. Documentation references are updated to match.

This completes the systematic conversion of ODB-related functions in the series, with all previous patches having established the pattern of mechanical conversions that preserve functionality while removing global state. The rename follows the established `odb_` prefix convention seen in earlier patches (like `odb_read_object()`, `odb_has_object()`) and improves clarity of what the function actually does.

No compatibility wrapper is provided since the function has relatively few callers (mostly in builtin commands like `cat-file`, `fast-import`, and `grep`). The patch maintains the series' focus on mechanical changes with no functional impact, bringing consistency to the ODB API while enabling future pluggable backend work.