The thread explores adding pathspec support to `git diff --no-index` for filtering directory comparisons outside repositories. Jacob Keller initially proposed an `--exclude` option but shifted to full pathspec support after Junio C Hamano pointed out the need for consistency with repository diff's `:!exclude` syntax.

The current consensus positions the first two arguments as comparison targets (files or directories) and treats subsequent arguments as pathspecs when comparing directories - mirroring `git diff-tree -r`'s structure. Junio recently confirmed this approach, suggesting documentation use `--` as a visual separator between targets and pathspecs, though it won't be required syntactically.

Key technical questions remain about handling edge cases in argument parsing and whether to support all pathspec features, but the design direction now appears settled. With Junio's latest endorsement, the discussion has moved from design debate toward implementation, awaiting Jacob's revised patch series demonstrating the pathspec solution with corresponding tests and documentation updates.