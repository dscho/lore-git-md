git-gui commit message formatting bugfix addressing reported issue (Gareth Fenn) where Tk text widget/`puts` interaction left two trailing newlines. Patch modifies `lib/commit.tcl` to replace three regex substitutions with single `string trim` operation handling both leading/trailing newlines while maintaining internal newline compression (multiple->two newlines). 

Technical context established that git-gui's direct use of `commit-tree` plumbing (vs porcelain `git commit`) requires explicit blank line handling. Historical note: previous attempt (b9a4386) to use `git stripspace` was reverted (c0698df) due to Tcl version/macOS compatibility issues. Current `string trim` approach validated by Johannes Sixt and Junio C Hamano as correct solution given current constraints, though Johannes notes `stripspace` could be reconsidered if version requirements change. 

Oswald Buddenhagen provides review confirming technical approach, adding `Reviewed-by` tag while suggesting minor comment wording improvements ("did not consider" vs original "forgot"). Notes semantic distinction that patch prevents trailing newline creation rather than stripping existing ones. Raises edge case consideration for completely empty commit messages as theoretical concern that could warrant additional comment clarification. Confirms solution's conciseness advantage (2 lines added vs 4 removed) and discusses `puts` command's implicit newline behavior alternatives. 

All participants (Gareth Fenn, Johannes Sixt, Junio C Hamano, Oswald Buddenhagen) agree on implementation. No outstanding technical concerns; historical context and edge case notes preserved for future reference. Solution remains localized to `lib/commit.tcl` with clear complexity reduction.