git-gui commit message formatting bugfix addressing reported issue (Gareth Fenn) where Tk text widget/`puts` interaction left two trailing newlines. Patch modifies `lib/commit.tcl` to replace three regex substitutions with single `string trim` operation handling both leading/trailing newlines while maintaining internal newline compression (multiple->two newlines). 

Technical context established that git-gui's direct use of `commit-tree` plumbing (vs porcelain `git commit`) requires explicit blank line handling. Historical note: previous attempt (b9a4386) to use `git stripspace` was reverted (c0698df) due to Tcl version/macOS compatibility issues. Current `string trim` approach validated by Johannes Sixt and Junio C Hamano as correct solution given current constraints, though Johannes notes `stripspace` could be reconsidered if version requirements change. 

Solution remains localized to `lib/commit.tcl` with clear complexity reduction (4 lines removed, 2 added). All participants (Gareth Fenn, Johannes Sixt, Junio C Hamano) agree on current implementation. No outstanding concerns; historical context preserved for future reference if compatibility requirements evolve.