git-gui commit message formatting bugfix addressing reported issue (Gareth Fenn) where Tk text widget/`puts` interaction left two trailing newlines. Patch modifies `lib/commit.tcl` to replace three regex substitutions with single `string trim` operation handling both leading/trailing newlines while maintaining internal newline compression. 

Junio C Hamano confirms Johannes Sixt's analysis that git-gui's direct use of `commit-tree` (vs porcelain `git commit`) requires careful blank line handling. Technical approach validated: replaces multiple operations (4 lines removed, 2 added) with simpler `string trim` while preserving behavior. Specifically processes `comment_string` variable before passing to `commit-tree`, maintaining existing internal newline compression (multiple->two newlines). No outstanding concerns; change remains localized to `lib/commit.tcl` with clear complexity reduction. All participants (Gareth Fenn, Johannes Sixt, Junio C Hamano) agree on solution.