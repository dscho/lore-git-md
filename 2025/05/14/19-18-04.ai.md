Refactoring patch from a first-time contributor addressing compiler warnings. This is part of the SoC 2025 microprojects initiative.

The patch removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro from `add-interactive.c`, which was presumably suppressing signed/unsigned comparison warnings. The change is motivated by wanting to clean up compiler warnings when building with `DEVELOPER=1` flags. The commit message indicates this is the contributor's first patch to Git, submitted as part of Google Summer of Code 2025 microprojects.

The change appears straightforward - removing a compiler warning suppression that may no longer be needed. However, the patch doesn't provide context about why these particular warnings were being suppressed originally or why it's now safe to remove the suppression. The `add-interactive.c` file handles the interactive add interface, so signed/unsigned comparisons could be significant for array indexing and similar operations.

As a first contribution, this follows the common pattern of addressing low-risk compiler warnings. The patch is small and focused, though it would benefit from more explanation about the original need for the suppression and why it can now be removed. The use of `DEVELOPER=1` suggests this may be primarily a development-time cleanup rather than affecting production behavior.