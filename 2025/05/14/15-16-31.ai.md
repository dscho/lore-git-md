Performance optimization follow-up question regarding sparse index integration.

Phillip Wood raises a question about the scope of sparse index optimizations beyond the current `git add -p/-i` and `git apply` series. While acknowledging these commands will benefit from the current changes, he asks about other patch-oriented commands like `git checkout --patch` and `git reset --patch` - specifically whether they need additional work to properly handle the sparse index.

This comes after Elijah Newren's thorough review and approval of the performance improvements (96.7% speedup) and test coverage in the original series. The thread context shows consensus that the current patches are merge-ready, having addressed edge cases through comprehensive testing in both compatibility (`t1092`) and performance (`p2000`) test suites.

The question expands the discussion's scope to consider whether similar sparse index optimizations should be applied to other interactive commands that use the patch machinery. This could potentially lead to future work, though the current series remains focused and validated for its specific commands. The technical foundation from this series (particularly the `git apply` changes) would likely be reusable for those other commands if needed.