This is a bugfix for the pack-bitmap subsystem, addressing memory leaks during bitmap loading failures. Junio C Hamano is responding to Taylor Blau's analysis of a second leak scenario in `load_bitmap()` where partially loaded bitmap entries would be orphaned when error handling destroys the bitmap hashmap prematurely.

Taylor's proposed solution eliminates the `failed` label cleanup in `load_bitmap()` entirely, replacing all error paths with direct `return -1` to rely on callers' `free_bitmap_index()` for uniform cleanup. This approach would fix both the original validation-order leak (from Lidong Yan's patch) and this newly identified partial-load leak, while simplifying the code by removing duplicate cleanup paths.

The patch removes about 10 lines of error handling code from `pack-bitmap.c`, showing how the current `failed` label was redundantly performing cleanup that callers would handle anyway through `free_bitmap_index()`. Junio notes the rare case where a fix actually removes code rather than adding it, suggesting this is likely to be well-received.

This continues the thread's technical focus on robust error handling in the performance-critical bitmap loading paths. The change maintains the same memory management patterns while making the cleanup behavior more consistent. With both fixes (Lidong's and Taylor's), the bitmap subsystem would properly handle errors during loading without leaking memory in either validation failures or partial-load scenarios.