This is a follow-up discussion in the final stages of Patrick Steinhardt's object database refactoring series, specifically addressing file organization considerations for the ODB subsystem. The email clarifies Patrick's intended structure for the ODB implementation files, confirming that while the core interface (`odb.h`) will remain at the root level (analogous to `refs.h`), backend implementations will live in an `odb/` subdirectory (starting with `odb/alternate.h` in upcoming patches). This mirrors the refs subsystem organization pattern where most users only interact with the root-level header while implementation details are contained in the subdirectory. The response confirms Toon Claes' suggestion about future backend organization while maintaining the root-level interface approach that was already established in the v2 series. This is purely an implementation detail discussion that doesn't affect the already-approved technical approach of the series.