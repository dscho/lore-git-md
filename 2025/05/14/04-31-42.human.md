This is a follow-up discussion in Patrick Steinhardt's object database refactoring series, specifically about file organization for the ODB subsystem. The thread has already reached consensus on the core changes, with this exchange focusing on future file structure considerations.

Patrick confirms his plan to eventually move backend implementations into an `odb/` subdirectory (similar to the `refs/` structure), while maintaining `odb.{c,h}` at the root level as the primary interface. He emphasizes that most developers should only need to interact with the root-level header, with the subdirectory containing lower-level implementation details.

This is a non-blocking discussion about future maintainability rather than a change to the current patch series. The clarification helps set expectations for where future ODB backend work will live while keeping the main interface accessible. The exchange shows good forward-thinking about subsystem organization as the ODB work progresses toward pluggable backends.