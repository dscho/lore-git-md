This is a continuation of the `git-blame-tree` discussion, focusing on UI design and command structure. Junio Hamano responds to Toon Claes' arguments about handling multiple file arguments, clarifying how a hypothetical integrated `git blame` could distinguish between traditional line-by-line blame and the new tree-wide mode.

Junio proposes a hybrid approach where the command would automatically switch modes based on input: directories or multiple files trigger tree mode, while a single file path maintains traditional blame behavior. He also suggests an explicit `--tree` option to force tree mode regardless of input. However, he expresses strong preference for a completely separate command, arguing that the tree operation is conceptually distinct from blame and deserves its own name.

The email highlights the ongoing tension between integration (who want to extend `git blame`) and separation advocates (who see this as a distinct operation). Junio's position leans toward separation but includes practical suggestions for how integration could work if that path is chosen. The discussion remains unresolved, with naming and conceptual alignment still open questions.