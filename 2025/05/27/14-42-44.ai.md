This is a review response from Junio C Hamano regarding patch 2/2 in K Jayatheerth's submodule configuration series. The patch aims to reduce config clutter by avoiding redundant `submodule.<name>.active` entries when the submodule path already matches existing `submodule.active` patterns.

Junio raises several important technical concerns about the implementation. First, he questions whether the behavior change (where submodules would no longer remain active if `submodule.active` patterns change) might be seen as a regression by existing users. Second, he points out that the documentation indicates `submodule.<name>.url` also activates a submodule, suggesting the patch should check for this case too if following the same logic. 

The review focuses on implementation approach, suggesting the new `submodule_active_matches_path()` helper may not be necessary and could create maintenance burden by duplicating logic from `is_submodule_active()`. Junio proposes a simpler alternative: just checking `is_submodule_active()` before writing the `.active` entry, which would naturally account for both pattern matches and existing URL entries without needing new helper functions.

This feedback indicates the patch needs revision to properly handle all activation cases and consider backward compatibility implications. The maintainer appears to prefer a solution that leverages existing `is_submodule_active()` logic rather than introducing new pattern-matching helpers. The discussion remains technical and constructive, focusing on finding the right balance between config cleanliness and maintaining expected behavior.