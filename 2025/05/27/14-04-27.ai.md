This is a refactoring patch that's the first in an 11-part series addressing race conditions in Git's maintenance operations. The patch modernizes the maintenance task array initialization in `builtin/gc.c` to use designated field initializers rather than positional ones.

The change converts the `tasks[]` array to explicitly name each struct field (`name`, `fn`, `auto_condition`, `enabled`) when initializing the maintenance task entries. This makes the code more maintainable by removing the implicit ordering dependency of positional initialization and making field assignments self-documenting. The diff shows this change being applied consistently across all maintenance tasks including "prefetch", "gc", "pack-refs", and others.

While this patch doesn't yet address the race conditions mentioned in the series' overall goal, it establishes cleaner code structure that will be built upon in subsequent patches. The change is clearly mechanical and low-risk, touching only initialization syntax without altering any runtime behavior. This foundational cleanup is typical for a series opener, making later functional changes easier to review by separating structural improvements from logic changes.

Key technical details for future reference: The patch modifies the `maintenance_task` struct initialization pattern in `builtin/gc.c`, standardizing on named fields that will be used throughout the series. All task definitions are updated including their names, function pointers, and condition checkers. The `TASK_GC` entry shows the only task with an `enabled` field set.