Refactoring patch in a series addressing maintenance task race conditions. This is patch 6/11 in Patrick Steinhardt's series fixing ref locking races during auto-maintenance operations.

The patch extracts the task execution logic from `maintenance_run_tasks()` into a new helper function `maybe_run_task()`. This prepares for the upcoming split between pre-detach and post-detach task phases by making the task execution logic reusable. The extracted function handles the same auto-condition checking, trace2 instrumentation, and error reporting that was previously inline in the loop.

Key technical details: The new `maybe_run_task()` takes a `const struct maintenance_task*` parameter along with the repository, run options, and config. It returns 0 for skipped tasks (when auto-condition fails) or the task's execution status. The function preserves all existing behavior including trace2 regions and error messages. The diff shows pure code motion with no functional changes - the loop in `maintenance_run_tasks()` now delegates to this helper.

This continues the series' pattern of preparatory refactoring before implementing the core race fixes. The extraction follows naturally from the previous patch's state management changes and will enable cleaner implementation of the split-phase execution model promised in the cover letter. The change is focused entirely on `builtin/gc.c` with no test updates needed for this internal restructuring.