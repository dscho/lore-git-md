This is a documentation patch in a series improving `--stdin` usage documentation in git-notes. The patch focuses on cleaning up redundant and misleading information about whitespace stripping and comment line handling in git-notes options.

The changes remove repetitive mentions of whitespace stripping behavior from the `-m`, `-F`, and `-C` option descriptions, since this is already covered by the general `--stripspace` documentation. It also eliminates incorrect statements about comment line handling that were present in the documentation. For the `-C/--reuse-message` option, the patch clarifies that it implies `--no-stripspace` by default to maintain verbatim copying behavior.

The patch follows the series' goal of making the documentation more accurate and focused by removing duplicate information and fixing inaccuracies. It builds on previous work in the series that established proper documentation of `--stripspace` behavior and comment character handling. The changes align with standard Git documentation practices, where whitespace handling is typically documented once in the general context rather than repeated for each option.

Key technical points:
- Removes redundant whitespace stripping descriptions from `-m`, `-F`, and `-C` options
- Eliminates misleading mentions of comment line handling
- Documents `-C/--reuse-message`'s implicit `--no-stripspace` behavior
- Maintains consistency with the series' documentation improvements
- Touches only Documentation/git-notes.adoc

This patch appears to be a straightforward cleanup that improves documentation accuracy without introducing new concepts or changes to the underlying functionality. It's part of a well-reviewed series that has addressed all previous concerns.