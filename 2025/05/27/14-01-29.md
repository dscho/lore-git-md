# Re: [PATCH v7 1/2] submodule: prevent overwriting .gitmodules entry on path reuse

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | K Jayatheerth <jayatheerthkulkarni2005@gmail.com> |
| **Date** | 2025-05-27T07:01:29-07:00 |
| **Message-ID** | [xmqqplfurw3q.fsf@gitster.g](https://lore.kernel.org/git/xmqqplfurw3q.fsf@gitster.g) |

---

**Thread**: [thread](./../24/07-36-26.thread.md#:~:text=2025/05/27/14-01-29)

K Jayatheerth <jayatheerthkulkarni2005@gmail.com> writes:

> ```diff
> @@ -3546,6 +3550,29 @@ static int module_add(int argc, const char **argv, const char *prefix,
>  	if(!add_data.sm_name)
>  		add_data.sm_name = add_data.sm_path;
>  
> +	existing = submodule_from_name(the_repository,
> +					null_oid(the_hash_algo),
> +					add_data.sm_name);
> +
> +	if (existing && strcmp(existing->path, add_data.sm_path)) {
> ```
>


Looks quite straight-forward to me.  Great.

> ```diff
> +		if (!force) {
> +			die(_("submodule name '%s' already used for path '%s'"),
> +			add_data.sm_name, existing->path);
> +		}
> +
> +		/* --force: build <name><n> until unique */
> +		for (i = 1; ; i++) {
> +			strbuf_reset(&buf);
> +			strbuf_addf(&buf, "%s%d", add_data.sm_name, i);
> +			if (!submodule_from_name(the_repository,
> +						null_oid(the_hash_algo),
> +						buf.buf)) {
> +				break;
> +			}
> +		}
> +
> +		add_data.sm_name = sm_name_to_free = strbuf_detach(&buf, NULL);
> +	}
> ```
>



