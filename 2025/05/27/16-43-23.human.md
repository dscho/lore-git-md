This is a minor clarification in Patrick Steinhardt's maintenance operations series. Ramsay Jones points out what appears to be a typo in the commit message for patch 4/11 - the text says it's "sensible to accept both options" when the actual patch makes them incompatible. The correction suggests it should say "is not sensible".

The patch itself (not shown in full here) marks `--task=` and `--schedule=` as mutually incompatible options in `git maintenance run`, similar to how `--auto` and `--schedule=` already work. This makes sense since they represent different ways to select tasks - explicit selection versus repository-configured strategy.

This is a straightforward documentation fix for an otherwise technical series about race conditions in maintenance operations. The thread context shows this was an early preparatory patch in an 11-part series that ultimately restructures maintenance task execution for safety. Ramsay's observation doesn't affect the technical content but helps clarify the commit message's intent.