This is the final patch in a documentation series, standardizing option formatting in git-notes documentation to use the "stuck form" (`--option=value`) consistently. The change makes `--ref` match the style recommended in gitcli(7) and already used for other options in the file.

The patch is very focused - it simply changes `--ref <ref>` to `--ref=<ref>` in git-notes.adoc. The author notes this was an oversight in earlier rounds of the series and considered making it a separate thread, but included it here since they were already editing nearby sections.

As the last patch in a well-reviewed series that has addressed all feedback, this appears ready for merging. It completes the documentation standardization effort by ensuring consistent option formatting throughout the git-notes documentation. The change is small but meaningful for maintaining documentation consistency.