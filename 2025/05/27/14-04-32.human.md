Refactoring patch in a series addressing maintenance task race conditions. This is patch 6/11 in Patrick Steinhardt's series to fix ref locking races during auto-maintenance operations. The patch extracts the task execution logic from `maintenance_run_tasks()` into a new `maybe_run_task()` helper function. This is a straightforward code reorganization that makes the control flow clearer by separating the task selection logic from the actual execution. The change is purely internal - no behavior modifications or new features are introduced.

The extracted function will be reused in upcoming patches that split maintenance tasks into "before detach" and "after detach" phases to properly sequence operations and prevent races. The patch follows the series' pattern of preparatory refactoring before implementing the core fixes, maintaining clean separation of concerns established in earlier patches.

The change is well-contained, touching only `builtin/gc.c` with no test updates needed. The diff shows careful attention to preserving all existing functionality including trace2 instrumentation and error handling. This continues the series' methodical approach to restructuring the maintenance code before tackling the race conditions themselves.