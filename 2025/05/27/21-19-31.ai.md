This is part 2 of a 9-part documentation series improving `--stdin` usage and comment character handling documentation. The patch focuses on clarifying the relationship between `commit.cleanup` and `core.commentChar` in the configuration documentation.

The change updates `Documentation/config/commit.adoc` to more precisely document how `commit.cleanup` interacts with comment characters. It replaces a direct reference to the `#` character with a parenthetical note about `core.commentChar` (defaulting to `#`), making it clear this is configurable while maintaining the default value. The patch also removes redundant mentions of `#` later in the paragraph, referring consistently to "the comment character" instead.

This aligns with the series' goal of standardizing documentation about comment character handling and configuration relationships. The change is small but contributes to the overall consistency and accuracy of the documentation, particularly around how configuration options interact. The patch follows the established pattern of using backticks for config variables and maintains the existing technical content while improving its presentation.

As part of the v3 series that has already addressed Junio Hamano's review feedback, this patch appears ready for merging. It cleanly documents the current behavior without making assumptions about future implementation changes, which was a key concern raised earlier in the thread.