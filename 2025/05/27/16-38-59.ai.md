This is a technical discussion about potential optimizations to Git's index handling, specifically considering alternative in-memory data structures. Jon Forrest follows up on Junio Hamano's explanation of the current sorted-array implementation by proposing a hash-based approach for the in-memory index representation.

Jon suggests replacing the current sorted array (`active_cache[]`) with a hash table keyed by pathnames, which would eliminate the need for sorting during lookups. He acknowledges the on-disk index could remain unordered in this scheme. The key open question he identifies is whether the overhead of hash table construction would outweigh the benefits of avoiding sorting operations, particularly comparing the cost of hash creation against the current approach of building `cache_entry` structs and sorting them.

.

This proposal represents a new direction in the thread's exploration of index optimizations, distinct from both the original two-file index idea and the split index functionality discussed earlier. While the earlier conversation focused on write amplification during `git add`, this suggestion targets lookup efficiency in the in-memory representation. Jon presents this as an exploratory thought rather than a fully formed proposal, explicitly noting the need for performance comparison data.

.

The technical context remains rooted in `read-cache.c`'s implementation details, with the `cache_entry` structure and `active_cache[]` array as central components. This suggestion doesn't directly address the write amplification concerns that initiated the thread, but rather introduces a separate optimization possibility for read operations. The proposal's viability would depend on detailed benchmarking of hash table operations versus binary search in Git's specific usage patterns.

.

This email moves the discussion into a new technical dimension while the original question about write optimization remains unresolved. It suggests the thread may be branching into two parallel optimization tracks: one focused on write efficiency (where split index functionality may suffice) and another exploring alternative in-memory data structures for read operations. Jon's characteristically cautious tone - presenting this as an open question rather than a recommendation - leaves room for others to weigh the tradeoffs or suggest implementation experiments.