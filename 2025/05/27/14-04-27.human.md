Refactoring patch in a series addressing maintenance task race conditions.

This is the first of an 11-part series that fixes race conditions in Git's maintenance operations around reference locking. The patch converts the array of maintenance tasks in `builtin/gc.c` to use designated field initializers rather than positional initialization. This purely structural change makes the code more maintainable by explicitly naming each struct field when initializing task definitions, rather than relying on their order in the struct.

The change affects all maintenance tasks (prefetch, loose-objects, gc, etc.), converting their initialization to use field names like `.name`, `.fn`, and `.auto_condition`. While this patch doesn't fix any bugs itself, it sets up the codebase for the subsequent patches that will restructure how maintenance tasks execute to prevent races. The diff is mechanical and straightforward, just reorganizing existing code with no behavior changes.

As the first step in a larger series, this patch serves as groundwork for the more substantive changes to come. The thread context indicates this series will ultimately address real-world race conditions in maintenance operations, particularly around reference locking during auto-maintenance. This initial patch appears well-contained and low-risk, following good practice by separating refactoring from behavior changes.