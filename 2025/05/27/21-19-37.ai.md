Documentation patch in a completed series addressing `--stdin` behavior consistency between git-notes subcommands. This is patch 8/9 in the v3 series that has already addressed all review feedback, including Junio Hamano's concerns about wording and implementation details.

The patch standardizes how `--stdin` is documented for both `copy` and `remove` subcommands in git-notes. It makes two key changes: (1) adds the `--stdin` mention to the `remove` subcommand's description (matching how `copy` already documents it), and (2) consolidates the option documentation to simply refer to the subcommands rather than duplicating details. The change follows Junio's suggested phrasing of "Only valid for" in the option documentation.

The diff shows the actual documentation updates, moving the detailed stdin behavior description to the `remove` subcommand section while making the `--stdin` option entry more concise and consistent. This completes the series' goal of equal treatment between subcommands while eliminating redundant information. The patch references the original 2010/2011 commits that added these features, maintaining historical context.

Technical details include:
- Updates Documentation/git-notes.adoc
- Documents that `remove --stdin` can combine stdin with command-line arguments (preserving existing behavior)
- Uses Junio's preferred "Only valid for" phrasing
- Maintains connection to earlier patches' standardization efforts
- Follows the series' pattern of removing redundancy while preserving accuracy

This appears to be the final documentation polish in a thoroughly reviewed series, with no outstanding issues noted. The change is small but contributes to the overall goal of consistent, accurate documentation of git-notes behavior.