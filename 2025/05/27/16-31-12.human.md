This is a reply in the ongoing discussion about adding a `-A` option to `git commit`. Nico Williams weighs in as a user who frequently works with dirty workspaces, expressing that they would find it annoying if `git commit -a` automatically staged new files like the proposed `-A` would. They note that while they wouldn't personally use a `-A` option, they wouldn't object to its addition.

The reply doesn't introduce new technical details but provides an important user perspective - some developers actively rely on `git commit -a` *not* staging new files as part of their workflow. This suggests the proposed change would need to be carefully considered to avoid disrupting existing workflows, even as it aims to help users coming from other VCS systems. The brief nature of the response leaves open the core questions about implementation details and whether the Git maintainers would accept such an addition.