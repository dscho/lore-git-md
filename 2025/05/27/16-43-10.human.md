This is a historical perspective on the `git commit -a` behavior discussion. Kristoffer Haugsbakk responds to Jon Forrest's proposal for a `-A` option by examining the original implementation context from 2005.

The email traces `git commit -a` back to its roots, showing it was originally implemented as a pipeline of `git-diff-files` (tracked changes only) feeding into `git-update-cache`. Haugsbakk suggests the original design likely excluded untracked files intentionally to avoid accidentally committing build artifacts, noting the original commit message didn't strongly endorse the feature.

While not taking a position on the `-A` proposal itself, Haugsbakk provides useful historical context that may inform the current discussion. The email implies the current behavior wasn't an oversight but a deliberate (if perhaps under-documented) design choice from Git's early days. The response is thoughtful but doesn't advance the technical discussion about implementing `-A` or its edge cases.