This is a discussion about documentation improvements for the promisor-remote protocol's field advertisement feature, part of an already-finalized v3 patch series. Patrick Steinhardt suggests restructuring the `promisor.sendFields` documentation to use a bulleted list format for better readability and future extensibility, rather than the current paragraph that would need reflowing when new fields are added. He also clarifies his earlier misunderstanding about field configuration - the current design correctly allows servers to send different values per remote while using a global field name configuration.

Christian Couder confirms the current implementation already supports per-remote values for fields like partialCloneFilter and token, and that making the configuration per-remote could be added later if needed. The discussion also touches on how clients might use different filters with the same promisor remote, though this behavior predates the current changes. The exchange shows the series is in good shape, with this being a minor documentation polish suggestion rather than a substantive issue needing code changes.