This is a clarification about `git branch --show-current` behavior in detached HEAD state, responding to a bug report. Kristoffer Haugsbakk confirms the current behavior (returning nothing in detached HEAD) is intentional and suggests an alternative command to achieve what the reporter wanted - identifying branches pointing to the current commit. The suggested `git for-each-ref` command with `--points-at=HEAD` would list all local branch names referencing the detached HEAD commit.

The email reinforces that the original behavior is working as designed, not a bug, while providing a workaround for the reporter's use case. This positions the thread as likely resolved unless the reporter has follow-up questions about the suggested alternative or wants to argue for changing the documented behavior. The response is technically correct but doesn't address whether the current behavior might be worth reconsidering for usability in CI contexts like Jenkins.