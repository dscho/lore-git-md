This is a documentation patch in a series improving `--stdin` and whitespace handling documentation for git-notes. It's part 6 of 9 in the v3 series, which has already addressed most review feedback and appears ready for merging.

The patch explicitly documents that `--stripspace` is the default behavior for git-notes commands (except when using `-C/--reuse-message`), addressing a gap in the current docs. It also notes a subtle interaction where option ordering affects behavior - specifically how `-m` can override `-C`'s implicit `--no-stripspace`. The commit message acknowledges this as a "known limitation" that might be fixed later, following Junio Hamano's suggestion from earlier review discussion.

The changes are well-justified with references to prior discussion threads and implementation history. The patch maintains the series' high standard of documentation accuracy while adding important details about default behaviors and edge cases. Since this is a later patch in a thoroughly vetted series, it likely only needs a final check before being merged.