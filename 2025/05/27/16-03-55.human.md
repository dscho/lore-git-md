This is a user experience question about `git commit -a` behavior and a feature proposal.

Jon Forrest points out that `git commit -a` doesn't add new files to the index, which is documented behavior but perhaps unexpected for some users. They propose adding a new `-A` flag that would behave like `-a` but also include new files in the commit.

The email doesn't provide any historical context about why `-a` was designed this way originally, nor does it offer any implementation details for the proposed `-A` flag. The suggestion appears to be more of a conversation starter than a fully-formed patch proposal.

This seems like the kind of change that would need discussion about backwards compatibility and whether the behavior difference between `-a` and `-A` would be intuitive enough for users. The proposal might benefit from concrete examples of workflows where the current behavior causes problems.