This is a documentation patch in a series improving `--stdin` and whitespace handling documentation in git-notes. It's part 5 of 9 in the third revision of the series, which has already addressed most review feedback.

The patch removes redundant explanations about whitespace stripping behavior from several git-notes options (`-m`, `-F`, `-C`), since this is already covered by the default `--stripspace` behavior documented elsewhere. It also eliminates misleading mentions of comment line handling that were incorrect or unnecessary. The changes make the documentation more concise while maintaining accuracy.

Notable changes include:
- Removing duplicate whitespace stripping explanations from `-m` and `-F` options
- Clarifying that `-C/--reuse-message` implies `--no-stripspace` by default
- Dropping incorrect claims about comment line handling that were present in the original text

The patch follows the series' goal of cleaning up and standardizing the documentation around whitespace handling in git-notes commands. It builds on previous work in the series that established clearer documentation patterns and removed inconsistencies. The changes appear well-justified and align with the overall quality of the series, which has already incorporated extensive review feedback.