The thread discusses a proposal to add a `-A` option to `git commit` that would stage both modified tracked files and new untracked files, similar to how `git add -A` differs from `git add -u`. The original `-a` behavior (only staging modified tracked files) has deep roots in Git's early days when `.gitignore` functionality didn't exist, as explained by Kristoffer Haugsbakk and Junio C Hamano.

Junio has now clarified important technical boundaries: while modern `.gitignore` makes some operations safer, changing `-a`'s behavior would still break workflows for users relying on its current behavior, particularly around partially staged files (those marked with `git add -N`). He suggests maintaining backward compatibility for `-a` while leaving room to consider adding `-A` as a new option. 

The discussion has shifted from initial UX arguments to a more nuanced consideration of historical constraints and modern needs. Key open questions remain about how a new `-A` option should handle ignored files and whether it should maintain exact parity with `git add -A`. The proposal appears to have niche rather than broad appeal, with Junio's latest input providing important guardrails for any potential implementation.