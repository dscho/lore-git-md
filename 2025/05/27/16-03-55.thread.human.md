Here's a human-readable summary of the thread so far:

Jon Forrest has proposed adding a new `-A` option to `git commit` that would automatically stage both modified tracked files and new untracked files when committing. This would complement the existing `-a` flag, which only stages changes to already-tracked files. The suggestion comes from observing that some users expect `git commit -a` to include new files, particularly those coming from other version control systems with different command semantics.

The proposal is backward-compatible since it introduces a new flag rather than changing existing behavior. While the email doesn't delve into implementation details or edge cases (like handling of ignored files), it presents a clear use case where the new option could help reduce confusion and make Git more intuitive for certain workflows. The suggestion appears well-motivated but awaits feedback from the community on whether this behavior would be desirable and how it should be implemented.