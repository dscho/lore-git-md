Jon Forrest proposed adding a `-A` option to `git commit` that would stage both modified tracked files and new untracked files, similar to how `git add -A` differs from `git add -u`. The initial discussion saw mixed reactions - Nico Williams strongly opposed changing the existing `-a` behavior but was neutral about adding `-A` as a new option.

Kristoffer Haugsbakk has now contributed historical context, tracing `git commit -a`'s current behavior back to its 2005 implementation. The original code only processed tracked files, likely to avoid accidentally staging unwanted files like build artifacts. This suggests the current behavior has deep technical roots rather than being an arbitrary choice.

The thread now has two main perspectives: Jon's UX-focused argument for making staging behavior more intuitive, and Kristoffer's historical analysis showing the current behavior was deliberately designed. Key technical questions remain unresolved, particularly around handling ignored files and edge cases. The discussion hasn't yet addressed whether the historical rationale still applies to modern Git usage patterns.