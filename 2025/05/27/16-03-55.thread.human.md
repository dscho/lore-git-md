Here's the updated human-readable thread summary:

The thread discusses a proposal from Jon Forrest to add a `-A` option to `git commit` that would stage both modified tracked files and new untracked files, mirroring the behavior of `git add -A`. This would complement the existing `-a` flag which only stages changes to tracked files. The discussion has established historical context showing this behavior dates back to Git's early days when `.gitignore` functionality was limited.

Junio C Hamano clarified important technical boundaries, particularly the need to preserve current `-a` behavior for backward compatibility, especially regarding `git add -N` marked files. Jon has since reiterated that his proposal would only add the new `-A` option without changing `-a`'s behavior. Nico Williams opposes changing `-a` but is neutral on adding `-A`. The discussion has now clearly established both the historical rationale and modern constraints, with the remaining open questions focusing on edge cases around ignored files and partially staged content. The proposal remains under consideration as a potentially useful but niche addition.