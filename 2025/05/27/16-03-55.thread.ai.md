Thread identity: Feature proposal from Jon Forrest about `git commit` behavior, suggesting adding a `-A` option to stage new files automatically alongside `-a`'s behavior, with expanded historical context from Kristoffer Haugsbakk and Junio C Hamano.

Core problem: `git commit -a` only stages modifications to tracked files, not new untracked files, which can surprise users expecting more comprehensive staging. The proposal suggests `git commit -A` would behave like `git add -A` vs `git add -u` - staging both modified tracked files and new untracked files. Historical analysis reveals this behavior dates to Git's 2005 implementation and was influenced by early lack of `.gitignore` functionality, with Junio C Hamano now clarifying additional constraints around `git add -N` marked files.

Current status: Discussion stage with mixed reception. Jon Forrest has clarified that the proposal strictly maintains existing `-a` behavior while adding new `-A` option, directly addressing Junio C Hamano's backward compatibility concerns regarding `git add -N` cases. The historical context is fully established with implementation (f0b32737ad5) and design rationale (cb95bf488b4) documented, and the discussion has narrowed to whether adding `-A` would provide sufficient value given the niche use case.

Key technical details: Would modify `builtin/commit.c` to add new flag handling parallel to `-a` but with `ALL` semantics. Original implementation used diff-files/update-cache pipeline that only processed tracked files (including `-N` marked), a design choice influenced by early Git's lack of `.gitignore` and need for conservative behavior. Modern implementation would need to: 1) maintain exact `-a` behavior for backward compatibility, 2) clearly separate `-A` as staging both modified tracked files and new files, 3) handle edge cases around partially staged (`-N`) and ignored files consistently with `git add -A`. Jon's latest clarification confirms the proposal would not alter any existing behavior.

Open questions: Whether the new `-A` option provides enough value to justify the addition, given that users can achieve similar results with `git add -A && git commit`. The interaction with ignored files and `git add -N` marked files is now understood but would need to be precisely implemented if the feature proceeds. Junio's position suggests the change may be acceptable if it remains purely additive with no risk to existing workflows.

Participant positions: Jon Forrest maintains the proposal as a strictly additive UX improvement. Nico Williams remains opposed to changing `-a` behavior but neutral on adding `-A`. Kristoffer Haugsbakk provided initial historical implementation context. Junio C Hamano has established the technical constraints and appears open to considering `-A` as a new option provided it doesn't affect existing behavior. The discussion has shifted from technical feasibility to evaluating the feature's utility.

Related work: Direct analogy to `git add`'s `-A` vs `-u` distinction. Historical discussion includes original 2005 implementation (f0b32737ad5), documentation (cb95bf488b4), and Junio's insight about technical constraints including `.gitignore` evolution and `git add -N` interactions. The thread has progressed from initial proposal through historical analysis to focused discussion of implementation boundaries.