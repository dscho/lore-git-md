This is a test infrastructure discussion about path resolution strategies in Git's test suite, specifically addressing how to handle symlink-containing directories in macOS and Windows maintenance tests (t7900-maintenance.sh).

Junio Hamano responds to Mark Mentovai's concerns about canonicalizing paths in test-lib.sh, defending the infrastructure-level approach. He argues that forcing canonical paths via `cd -P` provides consistency across developer environments, preventing mysterious failures based on checkout location. Junio distinguishes between:
1) Test script bugs (like the original $HOME normalization issue)
2) Git's path resolution behavior
3) Test infrastructure reliability needs

Key technical points in this response:
- Current test-lib.sh already attempts path consistency via `cd -P` but had a gap with HOME setting
- Proposes fixing this inconsistency in the infrastructure rather than individual tests
- Suggests dedicated tests for Git's path resolution if edge cases need verification
- Notes such edge case tests would need to be symlink-aware filesystem specific

The email advances the architectural debate between:
- Mark's preference for targeted fixes preserving edge case testing
- Junio's infrastructure approach prioritizing consistent test behavior
- Introduces the possibility of dedicated path resolution tests as a middle ground

This maintains the technical, collegial tone while clarifying Junio's position on why infrastructure changes are preferable to individual test fixes for this class of issues. The core disagreement about whether canonicalization might hide real bugs in Git's `strbuf_realpath` remains unresolved.