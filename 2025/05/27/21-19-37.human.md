This is a documentation patch in a 9-part series improving `--stdin` usage documentation in git-notes. The series is now at v3 and appears ready for merging after addressing all review feedback.

The current patch focuses on equalizing the documentation of `--stdin` behavior between the `copy` and `remove` subcommands of `git-notes`. It makes two key changes: adding `--stdin` documentation to the `copy` subcommand's description (matching how it's presented for `remove`), and consolidating the option documentation to reference both subcommands equally.

The patch follows through on Junio Hamano's earlier suggestion to use the phrasing "Only valid for" when describing the option's scope. This continues the series-wide effort to standardize documentation while accurately reflecting implementation details - in this case, that `copy --stdin` cannot accept additional command-line object names while `remove --stdin` can.

This appears to be a straightforward documentation improvement that completes the series' goal of comprehensive and consistent `--stdin` documentation. The change is small but meaningful, bringing the documentation closer to the actual implementation behavior while maintaining clarity.