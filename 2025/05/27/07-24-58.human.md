Bugfix discussion regarding `git branch --show-current` behavior in detached HEAD state. Kristoffer Haugsbakk clarifies that the empty output from `git branch --show-current` in detached HEAD mode is actually the documented behavior, not a bug. They suggest an alternative command (`git for-each-ref`) that can show which branches point to the current commit in detached HEAD state, which appears to be what the original reporter actually wanted.

This is a helpful clarification that distinguishes between the command's intended behavior and the reporter's underlying need. The suggested alternative provides a practical solution for Jenkins job contexts where knowing branch relationships in detached HEAD state is valuable. The exchange highlights a potential documentation opportunity to make this distinction clearer for users who expect branch information in all states.