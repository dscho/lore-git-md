This is a bugfix thread about file mode preservation in `git apply --reverse`. The discussion has now concluded with Junio Hamano's final review comments on the test implementation details.

Junio clarifies his earlier question about a seemingly no-op `git checkout` in the tests, understanding it was used during development to sync executable bits but isn't strictly necessary. He notes this could be misleading in the final test suite since it suggests filesystem executable bits matter when `core.filemode` is off, which they shouldn't. 

The conversation reveals deeper considerations about test design philosophy. Mark explains his rationale for focusing on reversing deletions of executable files (the originally broken case), while Junio suggests broader test cases would be valuable - covering both executable and non-executable files in various states. They agree on preferring smaller, more isolated tests over complex chained tests for better failure diagnosis.

Key technical points mentioned include:
- The special case of `core.filemode` operation on executable-capable filesystems
- The importance of testing both forward and reverse operations
- Various file states that should be tested (nonexistent, wrong mode, correct mode)
- The original bug's manifestation when reversing deletions of executable files

The thread shows consensus on changing the current test structure, with Junio acknowledging Mark's isolation approach while suggesting alternative designs. No further changes to the patch itself are requested - the core fix (adding `|| p->is_delete` to mode bit handling) remains correct and properly tested.