Thread identity: Bugfix patch for buffer underflow in xdiff's `xdl_build_script` function, affecting Git's diff machinery. First submission from Alex Guo.

Core problem: The `xdl_build_script` function in xdiff/xdiffi.c contains a buffer underflow vulnerability where array accesses at negative indices can occur when counters reach 0, due to unsafe `rchg1[i1-1]` and `rchg2[i2-1]` accesses without proper bounds checking.

Current status: Initial patch version submitted, proposing to add explicit `i1 > 0` and `i2 > 0` conditions around the problematic array accesses. No reviews or feedback at this stage.

Key technical details: Modifies xdiff/xdiffi.c with 4 lines added and 3 removed. Changes are minimal and focused on the bounds checking fix. The xdiff library is performance-critical code used throughout Git's diff machinery.

Open questions: Impact analysis needed - whether this could cause crashes or just incorrect behavior. The patch lacks test coverage demonstrating the fixed behavior. Minor code style inconsistency (spaces vs tabs) in added lines may need addressing. Performance impact of added bounds checks should be evaluated given xdiff's critical path role.

Participant positions: Alex Guo as first-time contributor submitting the fix. No reviewers have commented yet.