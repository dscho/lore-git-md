This is a correctness patch addressing potential issues in MIDX (multi-pack-index) handling during repack operations. Taylor Blau analyzes and fixes a subtle bug in `want_included_pack()` that could have caused problems with incremental MIDX chains, though the current code is actually safe due to an existing guard in `midx_repack()`.

The patch adds ASSERT() statements to document and enforce the invariant that these functions only operate on non-incremental MIDXs. While the analysis shows the current implementation is correct, the assertions make this safety guarantee explicit and help prevent future regressions. The change is minimal - just adding three assertions - but demonstrates careful consideration of MIDX edge cases.

This continues the thread's focus on MIDX correctness, following earlier performance optimizations that have already been queued. The patch maintains the high quality seen throughout this discussion, with thorough analysis in the commit message and targeted changes. It's a good example of defensive programming that will help maintain MIDX reliability as the feature evolves.