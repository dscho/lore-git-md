This is part 8 of a 9-part documentation series improving `--stdin` usage documentation in git-notes. The patch standardizes how the `--stdin` option is documented between the `copy` and `remove` subcommands.

The change makes the documentation more consistent by:
1. Adding a mention of `--stdin` to the `remove` subcommand's description (matching how `copy` already documents it)
2. Simplifying the standalone `--stdin` option documentation to just refer to the subcommands rather than repeating details

The patch follows up on feedback from v1 of the series, where it was suggested to avoid duplicating information. The change is straightforward and maintains the series' focus on precise, implementation-matched documentation. The author has clearly studied the historical implementation (referencing commits from 2010 and 2011) to ensure accuracy.

This continues the series' methodical approach to documentation cleanup, though this particular patch addresses a simpler aspect than some of the more complex option precedence rules covered earlier in the series. The change appears technically sound and well-motivated as part of the overall documentation standardization effort.