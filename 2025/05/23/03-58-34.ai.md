This is the final patch in a bugfix series for `imap-send`, addressing memory leaks in the CRAM-MD5 authentication path. The patch specifically fixes a case where the `response` buffer would leak if `socket_write()` failed during authentication. The fix is straightforward - adding a `free(response)` call in the error path before returning.

The change is minimal (just 3 lines added) but important for correctness, as it completes the memory management fixes for the CRAM-MD5 authentication method. This builds on earlier work in the series that addressed configuration parsing issues and OAuth2.0 support. The patch follows the same careful approach to memory management seen in previous patches, ensuring all allocated resources are properly freed in both success and error cases.

Technical details show this is the last piece of a comprehensive cleanup - the series has already addressed similar issues in other authentication methods and configuration handling. The error message remains unchanged from previous versions, maintaining consistency with the codebase's error reporting style. With this patch, the series' goal of robust memory management across all `imap-send` authentication paths appears complete.