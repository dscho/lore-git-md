Thread identity: Bugfix for potential divide-by-zero in parallel fetch operations, affecting `builtin/fetch.c`.

Core problem: The `max_children` parameter controlling parallel fetch operations could be zero, leading to division by zero in `pp_collect_finished`. This affects both regular fetches and submodule fetches.

Current status: Patch v1 proposed, changing validation from `max_children < 0` to `max_children <= 0` in two locations. Falls back to configured parallel jobs (`config.parallel` or `config.submodule_fetch_jobs`) when zero is detected.

Key technical details:
- Fixes validation in `builtin/fetch.c`
- Touches parallel fetch logic for both main fetch and submodule fetch paths
- Does not modify core parallel processing code, only the validation checks
- Open questions: None raised yet - appears to be straightforward bugfix
- Participant positions: No reviews or discussion yet
- Related work: Connects to general fetch machinery and submodule handling code