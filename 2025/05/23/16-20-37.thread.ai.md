Thread identity: Bugfix for potential divide-by-zero in parallel fetch operations, affecting `builtin/fetch.c` and broader parallel job handling.

Core problem: The `max_children` parameter controlling parallel operations could be zero from multiple sources (buggy `online_cpus()`, loose config/CLI parsing), leading to division by zero in `pp_collect_finished`. Initial patch addressed only fetch paths.

Current status: Patch v1 proposed local validation fix. Junio C Hamano suggests more comprehensive solution addressing root causes across all parallel operations, not just fetch. Two approaches proposed: 1) hardening `online_cpus()` to never return <=0, 2) adding system-wide safeguard (`max_children = 1`).

Key technical details:
- Original fix modified validation in `builtin/fetch.c` (max_children <= 0)
- Junio's analysis reveals deeper issues in parallel job count propagation
- Proposed changes to `thread-utils.c` would affect all parallel operations
- Platform-specific fixes shown for HP-UX, BSD sysctl paths
- Maintainer prefers solving at CPU detection level rather than local validation

Open questions:
- Whether to implement platform-specific `online_cpus()` hardening
- Relative merits of fixing at detection vs. operation entry points
- Impact on other parallel operations (repack, checkout, etc.)

Participant positions:
- Junio C Hamano: advocates for systemic solution, provides concrete code
- Original patch author: initial local fix (awaiting response to Junio's points)

Related work:
- Connects to general parallel job machinery
- Submodule fetch handling
- Platform-specific CPU detection code paths