This is a documentation patch in a series addressing git-notes behavior and documentation. Junio Hamano provides technical review feedback on the interaction between `--stripspace` and other options in git-notes, noting that while the current documentation accurately describes the implementation, that implementation may itself be buggy.

The email focuses on the behavior when multiple message sources (`-C`/`-m`) are combined, where the current implementation applies stripping to the concatenated result rather than to each message component individually. Junio suggests this is likely incorrect behavior that should be fixed, though the documentation accurately reflects the current (flawed) implementation.

Key technical points raised:
- Current implementation uses `note_data.stripspace` on concatenated messages rather than applying stripping per `note_msg`
- Suggested fix would process each message component with its appropriate stripping before concatenation
- Documentation correctly describes current behavior but should perhaps note it as a known issue
- References specific versions (v8 vs v11) of the original patch series being documented

This email doesn't propose changes to the documentation itself, but rather provides important context about the underlying implementation that may affect how the documentation should frame the behavior. The discussion suggests the current series may need follow-up work to either fix the implementation or explicitly document the behavior as suboptimal.