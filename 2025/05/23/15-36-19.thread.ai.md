Subject: git send-email threading behavior fix for edited first patch in series. Core problem: when using --in-reply-to and editing first patch, subsequent patches incorrectly thread to original target rather than first patch. Root cause: threading logic used $message_num == 1 check which fails after editing (increments $message_num). Fix: replace with $num_sent == 1 check and move $num_sent++ increment before threading check. Current status: patch proposed, not yet reviewed. Key technical detail: affects perl/git-send-email.pl threading logic, specifically when both --in-reply-to is set AND first patch is edited. Solution maintains backward compatibility when --in-reply-to isn't set. Open questions: whether this change might impact other edge cases in the email threading logic, potential performance implications of moving $num_sent++ increment, and whether similar counter logic exists elsewhere in script that might need review. Participant: patch author identified and analyzed the issue but no reviews yet.