This is a discussion about test behavior and platform-specific regex handling, continuing the thread about Meson test integration.

Ramsay Jones email examines why the unexpected pass` of test 868/1023 (t7815-grep-binary.sh) on macOS was only noticed with Meson builds, despite the same behavior existing in make/prove test runs for years. The test checks whether `git grep .fi a` fails as expected (since '.' shouldn't match NUL bytes), but passes on platforms with BSD-derived regex implementations (Cygwin and now confirmed for macOS).

Key points:
- Demonstrates that traditional make/prove test runners silently accept unexpected passes (exit code 0) while Meson's TAP parser now surfaces them
- Shows test output proving the macOS regex behavior matches Cygwin's (NUL byte matching with '.'), making the `!CYGWIN` prerequisite insufficient
- Questions whether test runners should fail on unexpected passes, not just failures
- References Patrick Steinhardt's earlier Meson TAP parsing changes that made this platform-specific behavior visible
- Notes this isn't a new issue - the behavior has existed for a decade but wasn't flagged by existing test infrastructure

The email doesn't propose code changes but highlights an important testing philosophy question about whether and how to handle unexpected passes, while confirming the macOS regex behavior matches the known Cygwin case. This discussion may influence future test infrastructure decisions beyond the immediate Meson integration work.