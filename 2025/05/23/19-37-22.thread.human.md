This thread began with Mark Mentovai fixing macOS test failures in t7900-maintenance.sh by changing `pwd` to `pwd -P` to properly handle symlinked directories, matching Git's internal path resolution. Eric Sunshine identified a complication on Windows where Git's shell overrides `pwd` with a custom function that ignores arguments, but testing confirmed the change was harmless there. 

The discussion explored several approaches: Mark's minimal fix, Eric's path sanitization ideas, and now Junio Hamano has proposed a more fundamental solution. Junio suggests modifying test-lib.sh to canonicalize paths early by using `cd -P` before setting `TRASH_DIRECTORY` and `HOME`. This 2-line change would resolve the root issue across all tests rather than requiring per-test workarounds, while being compatible with both Unix and Windows environments.

The thread has reached clear consensus that Mark's original fix is acceptable as an immediate solution, but Junio's proposal offers a cleaner architectural improvement for future test reliability. Eric Sunshine, who initially raised concerns about Windows behavior, now favors this approach over his earlier path sanitization suggestions. The discussion shows how a specific test fix evolved into consideration of broader test infrastructure improvements for cross-platform consistency.