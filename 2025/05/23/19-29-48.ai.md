Documentation patch continuing the series improving `--stdin` and comment character documentation. This specific patch focuses on clarifying the relationship between `commit.cleanup` and `core.commentChar` in the configuration documentation.

The patch makes several precise wording improvements to the `commit.cleanup` documentation in `Documentation/config/commit.adoc`:
1. Adds a parenthetical reference to `core.commentChar` as the source of the comment character
2. Changes "the comment character `#`" to "the comment character (`core.commentChar`, default `#`)" to properly indicate the configurable nature of this value
3. Removes the second explicit mention of `#` later in the paragraph, using "comment character" consistently instead
4. Maintains all existing technical content while making the relationship between these configuration options clearer

This change aligns with the series' broader goal of accurately documenting comment character handling and option interactions. The patch is narrowly focused on one configuration file, following up on earlier work to properly separate concerns between stripping behavior and comment handling. The diff shows careful attention to technical accuracy while improving readability.