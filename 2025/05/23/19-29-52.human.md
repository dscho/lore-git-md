This is part 6 of a 9-part documentation series clarifying `--stdin` behavior in git-notes and related comment character handling. The series builds on previous work to improve accuracy and consistency across Git's documentation.

This specific patch explicitly documents that `--stripspace` is the default behavior for git-notes operations, except when using `-C/--reuse-message`. It also notes an important caveat about option ordering - when multiple options are used (like `-C` followed by `-m`), the default behavior of the last-specified option takes precedence. The change follows up on feedback from Junio Hamano about properly documenting which variant (regular or negated) of an option is the default.

The patch adds clear documentation of the whitespace cleaning rules (previously established in the implementation) while addressing concerns about option ordering that were raised during the original implementation of `--stripspace`. The documentation now matches the actual behavior while helping users understand subtle interactions between options.

This continues the series' pattern of making the documentation more precise and technically accurate, particularly around edge cases and option interactions. The change is focused and well-justified, building on both implementation details and prior mailing list discussion.