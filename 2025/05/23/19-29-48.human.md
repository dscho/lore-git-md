This is a documentation patch in a 9-part series improving `--stdin` and comment character documentation. It specifically updates the `commit.cleanup` config documentation to properly reference `core.commentChar` as the source of the comment character.

The patch makes several small but important wording improvements to the `commit.cleanup` documentation. It now correctly shows that the comment character comes from `core.commentChar` (with `#` as the default) rather than implying `#` is hardcoded. The changes also remove redundant mentions of `#` and standardize on referring to the "comment character" generically.

This follows up on feedback from the v1 series, where the relationship between comment handling and configuration was identified as needing clarification. The change is straightforward and maintains consistency with the series' goal of accurately documenting behavior. The patch appears well-considered, with a clear commit message explaining each change.