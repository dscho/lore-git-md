**Bugfix for potential buffer underflow in xdiffi.c**

This patch fixes a buffer underflow vulnerability in `xdl_build_script()`, a core diff algorithm function in Git's xdiff subsystem. The issue occurs when the function's loop condition allows array indices to reach 0 and then attempts to access `rchg1[i1-1]` or `rchg2[i2-1]`, which would underflow the buffer.

The fix adds explicit bounds checking (`i1 > 0` and `i2 > 0`) around these array accesses. The change is straightforward and appears correct - it maintains the same logic while preventing invalid memory access. The patch touches only 7 lines in xdiff/xdiffi.c with minimal changes to the control flow.

While the commit message doesn't describe how this issue manifests in practice (whether it's a security concern or just theoretical), buffer underflows are always worth fixing. The change looks well-contained and low-risk, though it might be worth asking whether there are test cases that could exercise this edge case.