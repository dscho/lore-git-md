This is a follow-up discussion in the thread about `git show` unnecessarily accessing parent trees in partial clones. Bob Bell acknowledges Junio Hamano's technical explanation about when tree access is truly needed (like with `--diff-filter`) versus when it's avoidable (simple metadata display with `-s`). 

Bob clarifies his testing approach - while removing the promisor remote created an artificial error case, it effectively demonstrated the underlying inefficiency he wanted to highlight. His real-world concern is optimizing object retrieval at scale in his corporate environment to minimize both local overhead and server load. 

The exchange confirms alignment on the core issue - `git show -s` should be able to avoid tree access like `git log` does for metadata operations. While Bob will proceed with workarounds (using `git log` or `blob:none`), he leaves the door open for potential future optimizations to `git show`'s object access patterns. The tone is collaborative, with both parties recognizing the validity of the optimization opportunity despite the initial test's unorthodox methodology.