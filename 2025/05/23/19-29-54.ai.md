This is part 8 of a 9-part documentation series improving `--stdin` usage documentation in git-notes. The patch focuses on equalizing the documentation treatment of `--stdin` between the `copy` and `remove` subcommands.

The change addresses a historical inconsistency where `remove --stdin` was documented differently from `copy --stdin`. The patch makes two key adjustments: (1) it adds a mention of `--stdin` to the `remove` subcommand's description (matching how `copy` documents it), and (2) consolidates the option documentation to simply refer readers to the respective subcommands rather than duplicating details.

The diff shows the actual changes: adding a paragraph about `--stdin` behavior under the `remove` subcommand section, and simplifying the `--stdin` option documentation to be a cross-reference rather than containing implementation details. This follows feedback from v1 of the series to make the documentation more consistent and avoid redundancy.

This continues the series' pattern of methodically improving documentation accuracy and consistency, though it addresses a more straightforward aspect than some of the previous patches dealing with complex option precedence rules. The change is small but helps maintain equal treatment of similar functionality across subcommands.