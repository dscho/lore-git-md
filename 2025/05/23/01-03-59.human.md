This is a documentation patch that adds clarifying comments about packfile handling invariants, part of the performance optimization series for multi-pack-index scenarios.

Taylor Blau is responding to Patrick Steinhardt's patch that documents the critical ordering requirements for auxiliary packfile checks (`.keep`, `.promisor`, etc.) relative to packfile existence checks. The comment explains two key invariants: auxiliary files must exist before their `.idx` file during creation, and must be deleted after their `.pack` file during removal.

This patch is the first in a two-part series that has already received maintainer approval. The documentation establishes the safety constraints that the second patch's performance optimization (a negative lookup cache for MIDX entries) must respect. Taylor's response indicates agreement with the documentation addition, noting it will help future readers understand these subtle but important ordering requirements.

The thread context reveals this is part of a broader effort to reduce syscalls in packfile handling, with the series providing a 4x speedup for some operations by avoiding repeated failed lookups of missing packfiles. While this particular patch doesn't change behavior, it provides the necessary context for understanding the optimization in the following patch.