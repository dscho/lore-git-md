This is part 6 of a 9-part documentation series clarifying `--stdin` behavior and related Git features. The patch specifically documents that `--stripspace` is the default behavior for git-notes operations, except when using `-C/--reuse-message`.

The change adds explicit documentation about the default stripping behavior and its interaction with other options, addressing feedback from Junio Hamano about properly documenting which variant (regular or negated) is the default. The patch notes that option order matters - for example, `-C <object> -m<message>` will use `--stripspace` because `-m`'s default overrides `-C`'s implicit `--no-stripspace`.

The documentation now includes the complete whitespace cleaning rules in a clear bullet list (carried over from previous patches) and adds the new default behavior explanation. This continues the series' pattern of matching documentation to implementation behavior, removing ambiguity about when stripping occurs. The change references both the original implementation discussion and prior review feedback about option ordering considerations.

Key technical details:
- Documents `--stripspace` as default except for `-C/--reuse-message`
- Explains interaction between multiple options affecting stripping behavior
- Maintains the bullet list of specific whitespace cleaning behaviors
- References implementation history and review discussions
- Follows the series' pattern of precise documentation matching actual behavior

The patch appears well-considered, building on established documentation patterns from the series while addressing specific review feedback about documenting defaults. It helps complete the picture of stripping behavior that the series has been methodically documenting across multiple commands.