This is a follow-up discussion in the ongoing thread about `git show` unnecessarily accessing parent trees in partial clones. Bob Bell responds to Junio C Hamano's technical analysis, confirming agreement that `git show -s` should not need to access tree objects when only displaying commit metadata (like author email) without diff-related options.

Key technical points in this response:
- Acknowledges the difference in default behavior between `git log` (which works correctly) and `git show -s` (which unnecessarily loads trees)
- Explains the rationale behind using the "corrupted repository" test case - it was a deliberate choice to make the unnecessary tree access visibly fail rather than just inefficiently fetch
- Reveals the broader optimization context: this is part of a larger effort to minimize object retrieval when operating at scale in their corporate environment

The email also provides context about the reporter's situation:
- Working within corporate constraints (Outlook email limitations mentioned)
- Primarily interested in the behavior for large-scale operations with minimal server impact
- Willing to work around the issue (using `git log` or `blob:none` filters) but hopes for a proper fix

The discussion appears to be winding down, with the reporter indicating they'll monitor for any future changes rather than pushing for immediate action. The technical consensus from the thread remains that this represents a legitimate optimization opportunity that could align `git show`'s behavior with `git log` for metadata-only operations.