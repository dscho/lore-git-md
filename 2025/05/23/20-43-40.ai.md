Test fix discussion for macOS/Windows path resolution in t7900-maintenance.sh. Mark Mentovai responds to Eric Sunshine's concerns about Windows compatibility with the `pwd -P` fix for macOS test failures. The key technical points:

1. Confirms Windows CI testing shows the patch regresses nothing, since MinGW's `pwd` override ignores all arguments (including `-P`) and always uses `-W` mode
2. Acknowledges Eric's suggestion for path sanitization with placeholders but argues:
   - The test's core need is verifying path tails, not full path resolution
   - Current equality comparison is simpler than parsing path components
   - Extending the sanitization approach would require modifying multiple test cases

The response maintains that the original fix remains the most straightforward solution for macOS while being harmless on Windows, though it doesn't address the deeper test fragility issues around path comparisons that Eric raised. This keeps the discussion open for either adopting incremental fixes or pursuing more comprehensive test infrastructure changes.