Documentation patch clarifying `git-stripspace`'s comment character handling. This is the first patch in a 9-part series improving `--stdin` documentation across several Git commands.

The patch makes two small but meaningful changes to `git-stripspace.adoc`:
1. Explicitly mentions that the default comment character `#` can be configured via `core.commentChar`
2. Updates the formatting to quote the `#` character with backticks, following modern documentation conventions

The changes are straightforward and uncontroversial, building on previous documentation standardization work. The commit message references feedback from Junio Hamano suggesting a terser mention of the configuration variable. This appears to be a well-considered incremental improvement to the documentation.

As the first patch in a larger series, it sets the tone for the subsequent documentation updates by demonstrating the attention to detail and consistency that characterizes the whole effort. The inter-patch dependencies appear minimal, so this could potentially be reviewed and applied independently of the rest of the series.