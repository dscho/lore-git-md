This is a documentation patch series (9 parts) that refines the documentation around `--stdin` behavior in git-notes and clarifies `core.commentChar` references, building upon the previous 6-part series that was already reviewed and merged. The series makes several key improvements to the git-notes documentation: it properly separates the discussion of `--stripspace` behavior from comment character handling (which were incorrectly conflated in v1), documents the default `--stripspace` behavior more clearly, and equalizes the treatment of `--stdin` between copy/remove operations. The patches also clean up option formatting and remove misleading statements about comment line handling that don't actually reflect how git-notes processes input.

Notable technical changes include:
- Splits combined `--[no-]option` forms into separate entries for better searchability
- Documents that `--stripspace` is the default except for `-C/--reuse-message`
- Clarifies interaction between multiple options when they affect stripping behavior
- Removes redundant discussion of comment characters from options where it didn't belong
- Updates git-stripspace.adoc to reference `core.commentChar` more clearly
- Uses consistent "stuck" form (`--option=value`) throughout

The series addresses feedback from v1, particularly around properly separating the concerns of whitespace stripping versus comment handling. The author notes they discovered the original approach was misguided when they realized comment character handling isn't actually controlled by the `--stripspace` option. The interdiff shows substantial reorganization of the documentation to reflect this understanding.

This appears to be a well-considered follow-up to the earlier series, with careful attention to accurately documenting behavior rather than just shuffling text around. The changes are focused and the commit messages clearly explain both the what and why of each adjustment.