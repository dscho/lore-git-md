This is a review comment on the reftable memory leak bugfix patch.

René Scharfe points out that the REFTABLE_REALLOC_ARRAY macro being fixed in the patch appears to be unused in the codebase. This observation suggests the patch may be addressing a theoretical issue rather than an actual problem, since unused code cannot leak memory in practice. The comment is brief but raises an important consideration about whether the fix is needed at all.

The thread context shows this is the first review comment on the initial version of the patch, which aimed to fix a memory leak in the reftable utility macro. The original patch modified the macro to properly free memory on reallocation failure, but did not establish that the macro is actually in use. This review shifts the discussion from implementation details to whether the code should exist at all.

Key technical points from the review:
- The macro appears unused in the codebase
- Suggests removal rather than fixing as an alternative approach
- Does not dispute the technical correctness of the leak fix itself

This introduces a new dimension to the thread - the maintainability question of whether to fix or remove unused code. The original author will likely need to either demonstrate actual usage of the macro or consider removing it entirely. The review also implicitly raises questions about code auditing practices - whether changes should include evidence of the problem's existence through test cases or usage analysis.