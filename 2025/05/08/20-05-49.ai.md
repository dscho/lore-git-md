This is a refinement discussion about patch 3 in the completed 4-patch series optimizing memory usage in the packed-refs backend. The patch had extracted munmap operations into a dedicated function but subtly changed the condition for when cleanup occurs (from `mmap_strategy != MMAP_OK` to `mmap_strategy == MMAP_TEMPORARY`).

Jeff King notes that while the current implementation ensures `snapshot->mmapped` is never set with `MMAP_NONE`, this invariant isn't explicitly documented. He suggests either preserving the original condition or documenting the invariant. Junio Hamano agrees this change should be part of a separate justification rather than an extraction refactor.

The discussion focuses on the mmap state management in packed-backend.c, specifically around the `munmap_temporary_snapshot()` function introduced in patch 3. The original series had already received Junio's final ack, but this exchange identifies one last potential improvement point regarding code clarity around mmap state transitions.

Key technical details:
- The condition change affects when memory cleanup occurs during packed-refs operations
- Current behavior remains correct due to implicit invariants
- The discussion centers on whether to make these invariants explicit or preserve the original more defensive condition
- This is a polish point on an otherwise completed and approved series

The thread shows the final review pass catching a subtle maintainability consideration, though not one that affects functionality. The series remains ready for integration with this observation noted as an optional follow-up.