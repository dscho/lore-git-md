This thread addresses a memory leak in the commit-graph code when handling packfiles. The original report identified that `fill_oids_from_packs()` could leak a `packed_git` struct when `open_pack_index()` fails, as the error path wasn't properly cleaning up the allocated resources. Lidong Yan submitted a concise fix adding the necessary `close_pack()` and `free(p)` calls in the error case.

Patrick Steinhardt reviewed the patch and confirmed the technical correctness of the solution. He suggested improving the commit message by referencing the function name rather than specific line numbers, aligning with Git project conventions. The actual code change remains uncontroversial - it's a straightforward resource management improvement for a clear error case. With this review feedback incorporated, the patch appears ready for merging as it addresses the leak without introducing new issues or complexity.