This thread addresses a memory leak in the commit-graph code, specifically in the `fill_oids_from_packs()` function where a `packed_git` struct would leak if `open_pack_index()` failed. The initial patch provided a straightforward fix by adding proper cleanup calls in the error path.

Patrick Steinhardt reviewed the first version and confirmed the technical correctness of the solution while suggesting an improvement to the commit message - to reference the function name rather than line numbers, following Git project conventions. Lidong Yan, the patch author, has now sent a second version incorporating this documentation feedback while keeping the actual code change unchanged (a minimal 2-line addition for resource cleanup).

The discussion shows clear consensus on both the problem and solution, with the fix being uncontroversial. The only remaining item from the first review - the commit message style suggestion - has now been addressed in v2. The change is narrowly scoped to the error path when pack index opening fails, with no impact on normal operation. This appears ready for merging once the final version is picked up by the maintainer.