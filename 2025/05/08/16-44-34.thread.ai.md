Here is the updated dense AI thread summary incorporating the new and existing information:

This ongoing build system series improves configuration and installation path handling across Git's build systems (Make, Meson, Autoconf), with particular focus on preventing an Autoconf regression before next release. The current 5-part series addresses:

1. GITWEBDIR path quoting in meson.build
2. YAML.pm Perl module installation location in Meson (fixed to `<prefix>/share/perl5/Git/SVN/Memoize`)
3. System-wide gitconfig/gitattributes path handling in Meson (moved from hardcoded `/etc` to proper sysconfdir-based paths while maintaining backward compatibility)
4. Meson's GIT_EXEC_PATH handling alignment with Makefile behavior (relative 'libexec/git-core' default with proper libexecdir support and runtime_prefix validation)
5. Autoconf sysinfo detection upgrade from AC_CHECK_LIB to AC_COMPILE_IFELSE for reliable `sysinfo()` function checking on Solaris/Cygwin

Eli Schwartz has confirmed patch #5 successfully fixes sysinfo() detection on Solaris 11.3 by verifying both the function and its required struct fields (totalram and mem_unit), aligning autoconf with meson's behavior. This technical solution is now validated on a key target platform.

The discussion has expanded to question autoconf's ongoing maintenance value, referencing Patrick Steinhardt's earlier comment about meson potentially replacing autoconf after git-gui/gitk integration completes. Schwartz notes recent autoconf fixes (including this sysinfo patch and earlier cygwin work) appear motivated by completeness rather than active use, raising the policy question of whether to continue investing in autoconf when meson transition seems imminent.

Ramsay Jones' original series remains technically sound with all patches confirmed working (patch #2 had a minor 'seen' branch conflict now resolved, patch #5's Solaris behavior verified). The thread now contains parallel tracks: the immediate technical fixes (complete and validated) and a strategic discussion about build system future direction. All changes remain confined to build configuration files (meson.build, meson_options.txt, configure.ac) with no runtime behavior impact when functions are properly detected. The series has achieved its initial goal of consistent, reliable behavior across all supported platforms and build configurations.