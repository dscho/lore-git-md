Bugfix addressing a memory leak in the email header decoding logic. This patch fixes a case in `mailinfo.c` where a `strbuf` allocated during header decoding could leak if `convert_to_utf8()` fails. The author (Lidong Yan) identifies the specific location (line 539) where the cleanup was missing and proposes adding `strbuf_release()` and `free()` calls before the error path exit.

The change is straightforward - wrapping the error case in a conditional that properly cleans up the `dec` strbuf before jumping to the release label. The diff shows the addition of just 3 lines (with one line context change) to handle this cleanup. The patch appears correctly targeted at fixing a real resource leak, though it might benefit from including a test case demonstrating the fix. The commit message could be more detailed about the impact (e.g. whether this could be triggered by malformed input or is just a theoretical leak).