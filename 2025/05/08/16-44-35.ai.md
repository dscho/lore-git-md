Build system patch fixing path quoting in Meson configuration. This is part 2 of a 5-part series addressing build system issues across Git's build infrastructure (Make, Meson, Autoconf). The specific change here ensures the GITWEBDIR path is properly quoted in meson.build by switching from `.set()` to `.set_quoted()` for this configuration value. This matches how other filesystem paths are handled in the build system and prevents potential issues with paths containing spaces. The change is straightforward and mechanical, touching only one line in meson.build. The patch follows the series' pattern of fixing path handling inconsistencies across Git's build systems, though this particular change appears less critical than some of the other fixes in the series (like the sysinfo check modification in patch #5). The author has confirmed this merges cleanly with the 'next' branch despite parallel work on Meson/Perl path customization.