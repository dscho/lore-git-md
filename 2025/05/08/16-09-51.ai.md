This is a user-experience question about `--force-with-lease` behavior in detached HEAD mode. The email describes a workflow where the author helps clean up colleagues' branches by working in a detached HEAD state, then force-pushing the changes back. The push fails despite no actual remote changes, suggesting an interaction issue between `--force-with-lease`, `--force-if-includes`, and detached HEAD state.

Key technical details: The author's alias `pf` combines `--force` and `--force-with-lease`, with `push.useForceIfIncludes=true` globally enabled. The push fails with a "remote ref updated since checkout" error even when the remote hasn't changed. The workaround of explicitly specifying the expected remote SHA-1 works, but is verbose. The core question is whether this is expected behavior and if there's a more ergonomic solution than either disabling `--force-if-includes` or manually specifying the expected SHA.

The email raises two specific technical questions: 1) whether `--force-if-includes` reflog checking works in detached HEAD mode, and 2) if there's a shorter syntax than the explicit `--force-with-lease=branch:SHA` form. This appears to be either a documentation gap or potential edge case in the force-push safety checks when working with detached HEADs.