This is a follow-up review in the gitk SHA-256 support thread, pointing out remaining hardcoded SHA-1 length assumptions in the code. Johannes Sixt identifies five additional locations where the value "40" (SHA-1 hash length) appears without being converted to use the new `$hashlength` variable introduced in the patch series. The affected areas span various gitk procedures: `longid`, `shortids`, `setlink`, `mkpatch`, and `mktag`. 

The review suggests these are straightforward mechanical conversions similar to those already made in the patches, rather than fundamental design issues. This appears to be a continuation of the code quality improvements noted in the thread summary, specifically addressing the regex length handling that Johannes previously flagged in the first patch. The feedback implies the series needs one more pass to fully eliminate SHA-1 length assumptions before merging.

Key technical details for future reference:
- The hardcoded "40" values appear in regex patterns and UI-related procedures
- All should be converted to use the dynamic `$hashlength` (40/64) variable
- Affects tag/patch generation and display formatting logic
- Does not impact the core SHA-256 preference model debate from earlier in the thread