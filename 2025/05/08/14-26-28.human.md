This is a discussion about integrating the proposed `git-blame-tree` functionality into the existing `git blame` command rather than creating a new command. Junio Hamano responds to Marc Branchaud's questions about the user interface design for directory handling.

Key points:
- Junio clarifies that `git blame path/to/file` must continue showing line-by-line blame as it always has, not just the last commit that changed the file
- He agrees that `git log -1 path/to/file` already provides the "last commit" information, making a new way redundant
- On directory handling, he's open to either recursive (like `git ls-files`) or non-recursive (like `git ls-tree`) behavior, though he initially assumed recursive would be the choice
- The discussion continues the thread's central debate about whether to integrate this functionality into `git blame` or keep it as a separate command

The exchange shows the maintainer weighing in on specific UI questions that would need resolution if the integration path is chosen, while maintaining backward compatibility with existing `git blame` behavior. The tone suggests Junio is still considering the integration approach but wants to ensure any changes don't break existing workflows.