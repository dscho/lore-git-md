This is a bugfix patch addressing a memory leak in the commit-graph code. When `fill_oids_from_packs()` fails to open a pack index, it leaks memory allocated earlier by `add_packed_git()`. The patch adds proper cleanup by calling `close_pack()` and `free()` in the error path.

The change is straightforward - just two lines added to handle resource cleanup in the error case. The fix targets a specific edge case where pack index opening fails, which appears to be a clear oversight in the original error handling. The patch follows the standard pattern seen elsewhere in the codebase for similar resource cleanup scenarios.

While small, this is a legitimate memory leak fix that improves robustness. The commit message clearly identifies the problem location (commit-graph.c lines 1925-1930) and explains the solution concisely. The patch appears well-targeted and uncontroversial.