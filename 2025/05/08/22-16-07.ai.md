Bugfix follow-up addressing memory leak in email header decoding. Junio C Hamano confirms Lidong Yan's patch correctly fixes the strbuf leak in `mailinfo.c` when `convert_to_utf8()` fails, but suggests two improvements to the submission: (1) better commit message phrasing that references the function name rather than line number, and (2) a more maintainable long-term solution by changing the `decode_*_segment()` functions' calling convention to take a caller-allocated strbuf rather than returning a heap-allocated one. The current patch adds proper cleanup in the error path with `strbuf_release()` and `free()`, which Junio acknowledges as correct but notes would become simpler if the underlying functions were refactored. The discussion remains focused on this specific leak in the email parsing subsystem's header handling logic.