This is a reply in the ongoing discussion about integrating the new `git-blame-tree` functionality into the existing `git blame` command. D. Ben Knoble weighs in on Marc Branchaud's proposal to unify the two commands under `git blame`, suggesting that the tool should automatically handle directories differently from files.

The email introduces a third alternative approach: keeping `git-blame-tree` as plumbing while having `git blame <dir>` use it internally, similar to how `git diff` and `git diff-tree` relate. This would maintain the separate implementation while providing a unified user interface. Knoble notes that currently `git blame` fails when given a directory path, and suggests this could be turned into useful functionality.

The discussion continues to explore the tension between implementation complexity and user experience, with Knoble's suggestion offering a potential middle ground between Marc Branchaud's integration proposal and Toon Claes's preference for a separate command. The email also notes an untranslated error message as a minor technical detail that could be addressed.

This reply adds another concrete technical pathway to the ongoing debate about command structure, while maintaining focus on the user experience benefits of having `git blame` automatically handle both files and directories. The suggestion of a plumbing/public split mirrors existing Git command patterns, potentially offering a compromise solution.