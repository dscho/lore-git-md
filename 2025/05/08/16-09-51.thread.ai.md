Thread identity: User-experience question about `--force-with-lease` behavior in detached HEAD mode, raised by an unnamed contributor.

Core problem: When working in detached HEAD state to clean up colleagues' branches, force-pushing with `--force-with-lease` and `--force-if-includes` fails with "remote ref updated since checkout" even when the remote hasn't changed. The push succeeds only when explicitly specifying the expected remote SHA-1 (`--force-with-lease=branch:SHA`), which is verbose.

Key technical details:
- User's `pf` alias combines `--force` and `--force-with-lease`
- Global `push.useForceIfIncludes=true` is enabled
- Detached HEAD state appears to interfere with reflog checking
- Workaround exists but requires manual SHA specification

Open questions:
1. Does `--force-if-includes` reflog checking work correctly in detached HEAD mode?
2. Is there a more ergonomic syntax than `--force-with-lease=branch:SHA` for this case?
3. Is this expected behavior or an edge case in the safety checks?

Current status: No maintainer response yet. The thread may need input from someone familiar with the push/refspec implementation to clarify whether this is working as intended or requires documentation/behavior changes.