This is a final review comment on a refactoring patch in the packed-refs memory optimization series. Junio Hamano agrees with Jeff King's observation that patch 3's extraction of mmap cleanup logic subtly changed the condition from checking `mmap_strategy != MMAP_OK` to `mmap_strategy == MMAP_TEMPORARY`. While the current implementation ensures these are equivalent (since `snapshot->mmapped` is never set with `MMAP_NONE`), Junio concurs that such changes in refactoring patches should either preserve the original logic or be clearly justified.

The discussion confirms this series is otherwise ready for integration, with all prior feedback addressed. This final note serves as a reminder about maintaining careful equivalence during refactoring - a point that could be addressed with either a comment documenting the invariant or by keeping the original condition. The thread shows the maintainers' attention to detail even in preparatory changes that enable the series' memory optimization goals.