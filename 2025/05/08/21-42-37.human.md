This is a review comment on a reftable memory management bugfix patch.

René Scharfe points out that the `REFTABLE_REALLOC_ARRAY` macro being fixed in Lidong Yan's patch appears to be unused in the codebase. Rather than fixing the memory leak in the macro, Scharfe suggests simply removing it entirely if it's not actually used.

This raises an important consideration for the patch - if the macro isn't used, fixing it may be unnecessary work. The original patch identified a real memory leak issue, but if the affected code isn't actually part of any execution paths, the fix may not provide practical benefit. The thread now has two paths forward: either demonstrate where this macro is used (if the author knows of cases René missed), or accept the suggestion to remove the unused code instead of fixing it.

The exchange shows good technical review - catching that a proposed solution might be addressing a non-problem - while remaining constructive. This is the first substantive feedback on the patch series.