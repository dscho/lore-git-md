This is a follow-up discussion in the ongoing build system improvement series, focusing on the sysinfo() detection patch (#5) and broader questions about autoconf maintenance.

Eli Schwartz confirms that patch #5 correctly fixes the sysinfo() detection on Solaris 11.3, where the function exists but is incompatible with Linux's version. The patch aligns autoconf's behavior with meson's by using a compilation check that verifies both the sysinfo() function and its required struct fields (totalram and mem_unit).

The email then raises a meta-question about whether maintaining autoconf support remains worthwhile, referencing Patrick Steinhardt's earlier comment about meson potentially replacing autoconf once git-gui and gitk integration is complete. Schwartz notes that recent autoconf fixes (including this sysinfo patch and the earlier cygwin configuration) appear motivated by completeness rather than active use, questioning who still relies on autoconf builds if even Solaris users aren't using it.

The discussion remains technical but shifts from implementation details to project policy - whether to continue investing in autoconf when meson transition seems imminent. The thread shows consensus on the technical correctness of the sysinfo fix while opening a parallel conversation about build system strategy. Key files involved remain configure.ac and related build configuration, with no runtime impact beyond correct feature detection.