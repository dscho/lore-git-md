This is a platform compatibility discussion about error handling in Git's filesystem operations. Jeff King responds to Junio Hamano's concerns about atomicity guarantees in `open_nofollow()`, clarifying that the current thread's focus is actually on portable errno handling for symlink operations rather than atomicity.

King notes that the wrapper function's documentation already warns about race conditions in the fallback implementation, though he acknowledges this might need stronger emphasis. He distinguishes between two issues: (1) the immediate BSD errno discrepancy being fixed (where different BSDs return EFTYPE vs EMLINK for symlinks), and (2) the broader question of when to rely on specific errno values versus writing more defensive code.

The email suggests the errno consistency work (the current patch focus) is somewhat separate from atomicity concerns, though both relate to platform-independent filesystem operations. King expresses mild skepticism about using `open_nofollow()` in contexts where a subsequent `stat()` check is needed anyway, implying the current approach might be over-engineered for the packed-refs validation use case.

This continues the thread's progression from BSD-specific fixes to more fundamental questions about Git's platform abstraction layer design. The discussion remains technical but collegial, with participants carefully distinguishing between immediate fixes and longer-term design considerations.