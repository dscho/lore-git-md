CI/build system cleanup removing obsolete `prove` cache linking. This is the first patch in a 3-part series eliminating unused build infrastructure components.

The change removes code that created symlinks/junctions to a `.prove` cache directory in the Git test directory (`t/`). This optimization was originally implemented for Travis CI to persist test results between runs, but became obsolete when Travis support was dropped. The patch deletes the linking logic from both `ci/run-build-and-tests.sh` and `ci/run-test-slice.sh`, totaling 10 lines removed.

The removal is straightforward with no functional impact on current CI systems. The commit message clearly explains the rationale and connects it to the broader context of Travis CI removal. This appears to be a well-justified cleanup of technical debt in the CI scripts, aligning with the series' goal of removing unused build infrastructure.