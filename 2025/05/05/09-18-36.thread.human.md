This thread documents Leon Michalak's patch series adding command-line and interactive controls for diff context settings in Git's interactive patch commands (`add/checkout/commit/reset/restore/stash` with `--patch`). The work builds on existing config support for `diff.context` and `diff.interHunkContext`.

The series has gone through multiple rounds of review, with Eric Sunshine providing detailed feedback on test hygiene and UI organization, and Kristoffer Haugsbakk offering documentation guidance. The current version includes both command-line options (`-U<n>`/`--unified=<n>` and `--inter-hunk-context=<n>`) and an interactive "context" subcommand in `git add -i` and `git commit -i` (placed at position 9 to preserve existing command numbering).

In the latest exchange, Leon confirms addressing all of Eric's feedback and asks whether to extend the test cleanup to other tests in the same files, demonstrating attention to detail beyond the immediate patch requirements. The implementation now features proper test hygiene (with `test_grep` and `test_config`), clear documentation using "number of context lines" phrasing, and an extensible UI design.

With all technical feedback addressed and the contributor showing responsiveness to review comments, this appears ready for maintainer consideration. The series provides a polished solution for adjusting diff context both via command-line and interactively, following Git's collaborative development process from initial implementation through iterative refinement.