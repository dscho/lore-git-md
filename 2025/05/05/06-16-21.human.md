This is a v2 resend of a three-patch series improving `git send-email` functionality, combining technical fixes with documentation updates. The series tightens FQDN validation in the email-sending code while expanding OAuth configuration guidance.

The first patch now enforces stricter RFC1035 compliance for domain names in `git-send-email.perl`, fixing cases like "MacBook.." that previously passed validation but caused SMTP failures. The v2 incorporates minor formatting improvements to the commit message based on earlier feedback.

Second and third patches focus on documentation: one adds detailed OAuth2.0 examples for Gmail and Outlook to `git-send-email.adoc`, while the other references two new credential helpers in `gitcredentials.adoc`. The v2 makes small grammar fixes to these docs.

The changes appear well-considered, with the FQDN validation addressing a concrete interoperability issue and the documentation updates filling a practical need for modern authentication methods. The series maintains its original scope while polishing presentation based on initial review.