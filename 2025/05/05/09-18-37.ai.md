This is a feature patch that makes interactive patch commands respect the `diff.context` and `diff.interHunkContext` configuration settings. It's the first in a 3-part series.

The patch modifies `add-interactive.c` and `add-patch.c` to read these config values and pass them through to the underlying diff machinery when running interactive commands like `add -p`, `commit -p`, `checkout -p`, etc. The changes are straightforward - adding config reading in the initialization code and passing the values through to the diff command line when present.

The patch includes comprehensive tests in `t4055-diff-context.sh` that verify the config is respected across all the major interactive commands. The tests follow a consistent pattern of checking behavior with and without the config set, and include cleanup to unset the config afterwards. The test coverage appears thorough, checking all the main interactive commands that use the patch interface.

The commit message clearly states the motivation - that these are UI options and should be respected consistently in the patch interface. The implementation looks clean and focused, with no obvious technical issues. The patch appears ready for review, though reviewers may want to verify the test coverage matches all affected commands.