Feature addition to interactive commands, completing a series about diff context control.

This final patch in the series adds a new "context" subcommand to `git add -i` and related interactive commands (`commit --interactive` etc.), allowing users to dynamically adjust the number of context lines shown in diffs during the interactive session. The implementation:

1. Adds the "context" command to the existing menu (bumping "help" to position 9)
2. Implements the interactive prompt to set context lines (with input validation)
3. Updates documentation and help text
4. Includes tests verifying the context adjustment affects subsequent diff output

The change builds on the previous patches that added command-line overrides for diff context, now providing runtime control within interactive sessions. The implementation appears thorough, including all expected components (documentation, tests, input validation) and maintains consistency with existing interactive command patterns.

This completes the series' goal of giving users more control over diff context in interactive workflows, whether via config, command-line options, or now runtime adjustment. The patch seems well-constructed and addresses its narrow scope cleanly without obvious oversights.