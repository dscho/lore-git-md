Here is the updated dense AI thread summary incorporating the new UI design discussion:

This thread documents a patch series introducing command-line and interactive controls for diff context settings in interactive patch commands (add/checkout/commit/reset/restore/stash with --patch). The work builds on commit 8b91eef812 (referenced via relative description) which made builtins respect diff.context and diff.interHunkContext configs.

Patch 1/3 establishes test infrastructure for diff context behavior in interactive commands, using test_config for automatic cleanup. Phillip Wood's review confirms the core implementation approach while suggesting test improvements: using test_grep, avoiding global -c config flags, and potentially consolidating tests in t3701-add-interactive.sh.

Patch 2/3 added -U<n>/--unified=<n> and --inter-hunk-context=<n> command-line options to override config settings, defaulting to 3 lines for unified diff and 0 for inter-hunk context when unset. The implementation introduced add_p_opt struct to pass context settings through interactive machinery, validated options are used only with --patch, updated documentation for all affected commands, and added tests.

Patch 3/3 implements a "context" subcommand in `git add -i` and `git commit -i` for interactively changing context lines shown in subsequent diffs. Following Eric Sunshine's feedback, the subcommand is at position 9 in interactive menus to preserve muscle memory. The implementation includes context-setting prompts, standardized documentation phrasing, test_grep usage, and corrected help text.

Junio C Hamano and Eric Sunshine raised UI design considerations about menu layout and input methods, preferring colored first-letter shortcuts over numeric selection. Leon Michalak (first-time contributor) has responded to these concerns by:
- Proposing "config" as an alternative to "settings" for the subcommand name to avoid letter conflicts
- Indicating willingness to drop the interactive subcommand patch (3/3) if UI compromises prove too problematic
- Maintaining the command-line options from patches 1-2 as the primary solution

The technical implementation of context control through config inheritance and command-line overrides is settled. Current discussion focuses on final UI polish regarding:
- Optimal menu organization (balancing new features against established command positions)
- Input method conventions (letter-based vs numeric selection)
- Terminology clarity ("config" vs "settings")

Leon continues adapting to project norms, having switched from top-posting to inline replies and splitting test improvements into separate patches as requested by Christian Couder. The series now provides both command-line overrides and (pending UI resolution) interactive adjustment of diff context settings, with comprehensive test coverage following project best practices. All technical feedback has been addressed, with the remaining discussion focused on interface refinements before potential merging.