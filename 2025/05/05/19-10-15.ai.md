This is a maintenance discussion about Git's legacy code management, specifically addressing the status of `contrib/` directory contents versus core commands like `git svn`. Junio Hamano responds to Patrick Steinhardt's earlier suggestion about cleaning up `contrib/` by proposing three clear fates for its contents: promotion to core, spin-off as independent projects, or removal. He notes that `git svn` isn't actually in `contrib/` (a point previously clarified by Johannes Schindelin), highlighting the need to distinguish between core legacy commands and truly contributed scripts.

The email reinforces the thread's emerging consensus that Git needs systematic handling of legacy components, whether in core or `contrib/`. Hamano's terse response doesn't propose specific next steps but provides a clear conceptual framework (move up/move out/disappear) that could guide future decisions. The discussion remains focused on policy rather than the original Windows-specific `git svn` bugs, though those remain unresolved in parallel. This brief exchange helps solidify the maintenance philosophy while leaving room for concrete proposals to emerge later in the thread.