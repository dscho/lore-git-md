This is the final patch in Patrick Steinhardt's v4 series implementing the last two standalone maintenance tasks to fully replace `git gc` functionality. The patch introduces the "rerere-gc" task for garbage collecting the rerere cache, completing the decomposition effort.

The implementation follows the established pattern for maintenance tasks, with a new `maintenance_task_rerere_gc()` function that spawns `git rerere gc` as a child process for compatibility. The auto-execution condition checks for the existence of the rr-cache directory with at least one entry when `maintenance.rerere-gc.auto` is positive, matching the simplified design from v4 that replaced v3's more complex entry-count heuristic. The patch maintains integer config consistency with other maintenance tasks while addressing Junio Hamano's earlier concerns about heuristic value.

Key technical elements include:
- New `rerere_gc_condition()` function implementing the existence check
- `maintenance.rerere-gc.auto` config with same integer semantics as other tasks
- Comprehensive test coverage verifying auto-execution behavior with various config values
- Documentation updates in both config and command man pages
- Follows same child process pattern as other maintenance tasks

The patch appears technically sound with thorough test coverage, completing the series' goal of extracting all `git gc` functionality into granular maintenance tasks. The only remaining open question from the thread is Eric Sunshine's concern about the `get_worktree_names()` helper API design from earlier patches, which doesn't affect this rerere-specific implementation.