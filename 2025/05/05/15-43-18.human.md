Junio Hamano's email continues the discussion about platform-specific behavior in Git's filesystem operations, specifically around the use of `open_nofollow()` for symlink checking. This is part of the ongoing thread about BSD errno handling and packed-refs operations.

The email focuses on the trade-offs between atomicity guarantees and platform compatibility. While Jeff King had suggested using `lstat()` for symlink checking in `fsck` operations (where atomicity isn't critical), Junio raises broader questions about when and how Git should handle platform limitations for operations that may need atomicity guarantees elsewhere in the codebase.

Key points:
- Acknowledges that for `fsck`, atomicity isn't critical and `open_nofollow()` can be treated as a simple helper
- Questions how to handle cases where atomicity is important but platform support varies
- Compares this to other Git operations that have platform-specific fallbacks
- Raises concerns about whether `open_nofollow()` should be used at all for cases requiring true atomicity
- Reflects on Git's general approach to platform abstractions and compatibility layers

The email doesn't propose specific code changes but rather invites discussion about Git's philosophy around platform capabilities and atomic operations. It's a thoughtful continuation of the thread that may influence how Git handles similar cases in the future.