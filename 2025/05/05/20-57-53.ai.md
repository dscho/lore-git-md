This is part of the ongoing `git whatchanged` deprecation series, specifically addressing Meson build system integration for the command's removal when built with `WITH_BREAKING_CHANGES`. 

Patrick Steinhardt points out that the Meson build system needs corresponding updates to stop installing `git-whatchanged` documentation alongside other manpages. He proposes moving it to the `manpages_breaking_changes` section and updating the `lint-docs-meson` target to exclude it from the main documentation checks. The changes mirror the existing handling of `git-pack-redundant.adoc` in the breaking changes section.

Junio C Hamano acknowledges the suggestion with a brief response, indicating agreement with the approach. This follows their established pattern of maintaining parallel documentation handling between Makefile and Meson builds, and between the two deprecated commands (`whatchanged` and `pack-redundant`). The exchange shows the attention to build system details required even for deprecated commands, ensuring consistent behavior across different build configurations.

The patch maintains the series' careful approach to deprecation by keeping documentation available (just in a different section) rather than removing it entirely, while still allowing the command itself to be excluded at build time. This matches the incremental deprecation strategy seen throughout the thread, where warnings precede actual removal and documentation updates follow a clear timeline.