This is a feature patch in the ongoing Scalar maintenance control series, adding a `--no-maintenance` option to `scalar register`. The change allows users to skip automatic background maintenance setup when registering a repository, which is useful for automation scenarios where background tasks might interfere with foreground operations.

The patch modifies the `register_dir()` function to accept a maintenance parameter, defaulting to enabled (1) for backward compatibility. Documentation is updated to explain the new flag, and tests verify that the option correctly skips maintenance setup. The implementation preserves existing error handling while adjusting messages to reflect the new conditional logic.

This appears to be a well-constructed patch with clear motivation (automation use cases) and thorough testing. It's part of a larger discussion about how maintenance control should be architected in Scalar commands, but this specific change is straightforward and technically sound. The patch maintains backward compatibility while adding the new option, and includes both documentation and test coverage.