This is a follow-up discussion in the Windows CI build system thread, specifically about building in Release mode to avoid test hangs. Patrick Steinhardt responds to Johannes Schindelin's points about the broader implications of using Release mode builds in CI.

The exchange confirms consensus on the immediate CI configuration change while acknowledging two longer-term considerations: (1) external dependencies may still trigger asserts regardless of Git's own code changes, and (2) the separate (but related) goal of converting Git's asserts to BUG() calls for better error reporting. Johannes notes Release mode has additional benefits beyond just avoiding the immediate test hangs, though doesn't elaborate further since the current justification is sufficient.

Key technical points reinforced: The Release mode build setting affects not just Git's code but also its dependencies' behavior, making it a more robust solution than just fixing Git's own assert usage. The discussion distinguishes between the tactical CI fix (needed now) and strategic assert-to-BUG conversion (a separate future effort).

The thread shows alignment between both contributors on the approach, with Patrick acknowledging Johannes' point about external dependencies and both agreeing the Release mode change stands on its own merits regardless of future assert handling improvements. This email doesn't introduce new technical elements but solidifies the rationale for the CI configuration change.