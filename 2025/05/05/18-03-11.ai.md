This is a continuation of the platform compatibility discussion around errno handling in filesystem operations, specifically addressing the atomicity guarantees of `open_nofollow()` versus its errno portability concerns.

Jeff King responds to Junio C Hamano's question about warning developers against using `open_nofollow()` for atomicity requirements. He points out that the function's header comment already warns about race conditions in the fallback implementation, though acknowledges this might not be sufficiently visible. The discussion references commit 00611d8440 which introduced tradeoffs between atomicity guarantees and performance costs.

The core tension here is between two separate but related issues: (1) the BSD errno divergence being addressed in Collin Funk's patches (where different BSDs return EFTYPE/EMLINK vs ELOOP for symlink operations), and (2) the broader question of when atomic operations are truly needed versus when portable error handling suffices. King notes that while improving errno consistency in wrappers is worthwhile (the immediate patch focus), there's a deeper design question about how much calling code should rely on specific errno values versus handling them opaquely.

Key technical points raised:
- `open_nofollow()`'s existing documentation about race conditions in fallback paths
- The irony that the racy fallback code actually provides more consistent ELOOP returns
- The observation that some operations (like the packed-refs fsck case) end up needing stat() checks regardless

This email moves the thread from the specific BSD errno fixes toward the architectural implications for Git's platform abstraction layer, particularly around atomic operation requirements and error handling contracts. The discussion remains unresolved on whether stronger developer guidance is needed about `open_nofollow()`'s limitations.