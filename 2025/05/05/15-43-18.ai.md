This is a platform compatibility discussion about atomicity guarantees in filesystem operations, specifically regarding the use of `open_nofollow()` versus `lstat()` for symlink detection in Git's packed-refs handling.

The email from Junio C Hamano responds to Jeff King's suggestion to use `lstat()` instead of `open_nofollow()` for symlink checking in fsck operations, since atomicity isn't critical for this use case. While agreeing that fsck` doesn't need atomicity, Hamano raises broader questions about when and how Git should handle platform differences in atomic operation guarantees.

Key technical points discussed:
- The current implementation uses `open_nofollow()` (introduced in cfea2f2da8) for symlink detection
- Jeff King proposed switching to `lstat()` for fsck since atomicity isn't required there
- Hamano notes `open_nofollow()` might be used elsewhere where atomicity matters
- Parallels are drawn to other platform-specific behaviors like `finalize_object_file()`'s collision detection

The discussion highlights an ongoing tension in Git's platform support - whether to treat certain behaviors as quality-of-implementation issues (with fallbacks) versus hard requirements. Hamano questions whether `open_nofollow()`'s current implementation (which can't guarantee atomicity on all platforms) should be flagged to prevent misuse in contexts where atomicity is truly needed.

This continues the thread's evolution from BSD errno handling to deeper questions about platform abstraction design, with implications for future filesystem operations in Git. The discussion remains open-ended, with no clear resolution yet on how to handle atomicity requirements versus platform capabilities.