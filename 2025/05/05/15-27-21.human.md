This is a refactoring patch that's part of the ongoing Scalar`--no-maintenance` flag series. It prepares the codebase for adding maintenance control by modifying the `register_dir()` function to accept a `maintenance` parameter that determines whether to enable background maintenance.

The patch makes two key changes: First, it adds documentation clarifying how `toggle_maintenance()` works (enabling maintenance when passed a non-zero value, disabling when passed zero). Second, it updates `register_dir()` to only enable maintenance when explicitly requested, rather than always doing so. This change is propagated to all callers, which currently all pass `1` to maintain existing behavior.

The implementation looks clean and focused, with clear docstrings and straightforward parameter passing. This appears to be foundational work for the larger feature that will allow controlling maintenance behavior via command-line flags in subsequent patches. The patch maintains backward compatibility while setting up for the new functionality.