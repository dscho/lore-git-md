This is the final patch in a feature series adding configurable maintenance behavior to Scalar commands. It extends the `--[no-]maintenance` option to `scalar reconfigure`, completing the implementation across all relevant Scalar commands (register, clone, and now reconfigure).

The patch follows the established pattern from previous patches in the series:
- Adds the `--[no-]maintenance` flag to `scalar reconfigure` with consistent OPT_BOOL handling
- Updates documentation in scalar.adoc to explain the new option
- Extends test coverage in t9210-scalar.sh to verify both maintenance enable and skip paths
- Key technical details:
  - Default behavior remains unchanged (maintenance enabled)
  - Uses same `toggle_maintenance()` helper as other commands
  - Preserves existing `--all` functionality for batch operations
  - Tests verify both positive and negative cases via trace2 events
  - Maintains backward compatibility

The implementation appears solid with thorough test coverage, completing the series' goal of giving users control over maintenance behavior during repository setup and configuration. The architectural questions raised earlier in the thread about maintenance control placement remain open but don't block this technically complete implementation.