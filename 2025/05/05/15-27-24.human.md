Feature patch completing the `--no-maintenance` option rollout across Scalar commands. This final patch in the series adds the option to `scalar reconfigure`, bringing consistency with the `register` and `clone` commands already modified in earlier patches.

The change allows users to skip automatic maintenance setup when reconfiguring Scalar enlistments, particularly useful for bulk operations via `--all`. The implementation follows the established pattern: a new `OPT_BOOL` flag controls maintenance behavior, defaulting to enabled for backward compatibility. Documentation updates clearly explain the new option alongside existing `--all` behavior.

Test coverage verifies both the maintenance-enabling default path and the new skip path, using the same `test_subcommand` approach seen in previous patches. The implementation preserves the existing maintenance.lock conflict prevention and maintains consistent behavior with the rest of the series.

This patch appears well-integrated with the prior work, completing the feature's rollout while leaving the underlying architectural questions about maintenance control placement (centralized vs per-command) for future discussion. The changes are focused and follow the established pattern, suggesting they're likely to be uncontroversial.