This is a continuation of the discussion about legacy code management in Git, specifically clarifying the status of `git svn` versus `contrib/` scripts. Patrick Steinhardt acknowledges Johannes Schindelin's correction that `git svn` is actually a core command (along with other import tools like `git-archimport` and `git-cvsserver`) rather than part of the `contrib/` directory. 

The email reinforces Schindelin's classification of these import tools as "dust collectors" - transitional utilities from Git's early days when interoperability with other VCS systems was more critical. While Steinhardt had originally suggested cleaning up unmaintained scripts in `contrib/`, this exchange makes clear that the discussion about legacy code needs to consider both the `contrib/` directory and certain core commands that have fallen into disuse.

The thread now has two parallel but related tracks: (1) the unresolved technical issues with `git svn` on Windows that started the thread, and (2) this broader discussion about how to handle various types of legacy code in the Git project. This particular email doesn't introduce new technical details but serves to clarify and reinforce the categorization of these legacy components, which will be important context for any decisions about their future maintenance or deprecation.