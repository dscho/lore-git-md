This is a v3 revision of a three-patch series improving `git send-email`'s domain validation and documentation. The series maintains its original focus from v2 with minor formatting tweaks - the main change in v3 is shortening a URL link in the documentation.

The first patch tightens domain name validation in `git-send-email.perl` to properly enforce RFC1035 rules, fixing cases like "MacBook.." that previously passed validation but caused SMTP failures. The second patch expands the send-email documentation with detailed OAuth2.0 examples for major providers and better organization. The third patch adds references to two new credential helpers (`git-credential-outlook` and `git-credential-gmail`) in the credentials documentation.

The author notes they're actually using one of these helpers to send this very email. The v3 changes are minimal - just URL shortening and some earlier v2 formatting fixes - suggesting the technical content is stabilizing. The documentation additions look thorough, with practical examples addressing modern authentication needs where app-specific passwords are no longer supported. While the domain validation changes still lack automated tests, the series appears well-structured and ready for final review.