This is a performance discussion in the ongoing platform compatibility thread about BSD errno handling in packed-refs operations.

Patrick Steinhardt provides concrete data points about packed-refs file sizes from GitLab.com's experience, confirming that while most are under 50MB, outliers like gitlab-org/gitlab's 2GB packed-refs exist. This supports Jeff King's earlier suggestion to use mmap() instead of strbuf for reading packed-refs during fsck operations, particularly for large repositories in fork networks.

The email clarifies real-world scale by noting that 99% of GitLab.com's packed-refs files are under 50MB (mostly under 5MB), but acknowledges the existence of pathological cases like the 2GB example. This data helps quantify the performance tradeoffs between the current strbuf approach and a potential mmap() implementation.

The discussion remains focused on optimization rather than the original BSD errno issue, though it stems from code inspection during that work. Patrick explicitly endorses refactoring to use mmap(), aligning with Jeff King's earlier technical arguments about memory efficiency. The thread now has concrete size metrics to inform any future optimization work, though this remains a separate concern from the core BSD compatibility fixes.