This is a continuation of the discussion about legacy code management in Git, specifically addressing the status of `git svn` and other import commands. 

Patrick Steinhardt acknowledges Johannes Schindelin's clarification that `git svn` is actually a core command (not in `contrib/`) alongside other legacy import tools like `git-archimport` and `git-cvsserver`. Both agree these commands are effectively "dust collectors" from Git's early days when interoperability with other VCS systems was more critical. 

The exchange reinforces the distinction between the `contrib/` directory (which contains a mix of maintained and unmaintained scripts) and these core-but-legacy import commands. While the original `git svn` Windows issues remain unresolved, this sub-thread has evolved into a broader conversation about how Git should handle aging functionality, whether in `contrib/` or the main codebase. The tone is collegial, with both participants aligned on the need for some systematic cleanup, though no concrete proposals emerge in this particular message.