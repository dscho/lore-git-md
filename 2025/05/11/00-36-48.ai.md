Documentation patch completing the `--intent-to-add` fix series. This final patch clarifies the documentation for `git apply --intent-to-add` to better reflect the behavior established by the earlier technical fixes in the series.

The changes improve the man page wording by:
1. Removing a double negative ("ignored unless...not specified")
2. Making it clearer that `--intent-to-add` is mutually exclusive with both `--index` and `--cached` modes
3. Maintaining the note about `--index` being implied by other options like `--3way`

This aligns with the technical changes in the earlier patches that fixed index corruption issues by properly separating the intent-to-add workflow from index-modifying operations. The documentation now accurately reflects that `--intent-to-add` is strictly a worktree operation that cannot be combined with index-writing modes.

The patch completes the comprehensive solution to the long-standing `--intent-to-add` bug by ensuring both the implementation and documentation properly communicate the command's behavior and limitations.