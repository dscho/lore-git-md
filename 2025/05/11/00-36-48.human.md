This is a documentation patch that concludes Raymond Pasco's series fixing `git apply --intent-to-add` behavior. The patch clarifies the documentation for the `--intent-to-add` option in `git-apply`, removing a double negative and making the distinction between `--index` and `--cached` modes clearer.

The changes reword the description to state positively that `--intent-to-add` is ignored when `--index` or `--cached` are used, rather than saying it's "not ignored unless not specified." This matches the technical fixes in earlier patches that properly handle these flag interactions. The documentation now better reflects the actual behavior established by the series, where `--intent-to-add` is designed to worktree-only operation for new files.

This final patch appears straightforward and uncontroversial, completing the series with matching documentation updates. The changes are minimal but improve clarity, which is particularly important given the subtle interactions between these flags that the series addresses.