This is a refactoring patch that's part of a series optimizing memory usage in the packed-refs backend. It extracts the buffer allocation logic from `load_contents()` into a new helper function `allocate_snapshot_buffer()`, making the code more reusable while maintaining all existing behavior.

The change allows the same allocation logic to be shared between normal packed-refs loading and fsck operations, which need similar functionality but can't use `load_contents()` directly. The patch moves 54 lines of code with a net change of +10/-0 lines, showing it's primarily a reorganization rather than adding new logic.

The implementation preserves all the existing mmap strategy considerations (MMAP_NONE fallback, 32KB threshold for small files, error handling) while making the code more modular. This is the second patch in a v3 series that has already gone through extensive review, with suggestions from Jeff King and Patrick Steinhardt incorporated.

The patch appears well-crafted with a focused scope - it doesn't change behavior but sets up the infrastructure needed for the fsck optimizations in the following patch. The commit message clearly explains the motivation and the code changes are straightforward. This looks like a solid preparatory change that's ready for inclusion.