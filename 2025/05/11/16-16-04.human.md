Bugfix addressing memory leaks in the reftable writer. This v2 patch expands on the initial submission by fixing two distinct leak scenarios in `reftable/writer.c`:

1. The original issue where a `zeroed` buffer would leak during failed padded writes (when `w->writer` fails) now includes proper cleanup with `reftable_free(zeroed)`.

2. A new fix for a similar leak in `writer_index_hash()` where allocated `key` memory would be lost if `reftable_buf_add` fails, now freed before error return.

The changes are minimal (6 lines added, 2 removed) and maintain the existing error handling patterns. The patch appears well-scoped, addressing clear resource management issues in the reftable subsystem's low-level writing paths. No tests are included, but the fixes follow the established cleanup patterns seen elsewhere in the codebase. This version improves on v1 by catching an additional leak case while keeping the same straightforward approach.