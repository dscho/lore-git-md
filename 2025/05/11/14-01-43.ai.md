This is the first patch in a 3-part series optimizing memory usage in the packed-refs backend, specifically addressing fsck operations. The patch fixes fsck behavior for empty packed-refs files by modifying packed-backend.c to skip consistency checks when encountering zero-length files, matching the runtime code's behavior.

The change is straightforward - it checks the file size before attempting to read the packed-refs content and skips verification if empty. This aligns with how other code paths like `create_snapshot` handle empty files. The patch includes a test case that creates an empty packed-refs file and verifies fsck doesn't report errors.

Key technical details:
- Modifies `packed_fsck()` in refs/packed-backend.c (3 lines added)
- Adds test in t0602-reffiles-fsck.sh (13 lines) using direct file creation method
- Maintains all existing error handling for non-empty files
- Follows the pattern established by runtime code paths

This is a clean, focused change that addresses a specific edge case while maintaining the existing behavior for non-empty files. The test coverage is thorough and the implementation matches the established patterns in the codebase. As noted in the thread summary, this patch has already gone through multiple iterations with all technical clarifications resolved.