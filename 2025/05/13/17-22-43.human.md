Bugfix discussion regarding `git pull --rebase` behavior when changing remote URLs. Mr Bill confirms Phillip Wood's analysis that the issue stems from stale reflog entries affecting the fork-point calculation, showing how `git merge-base --fork-point` returns different results between clone_1 (correctly pointing to commit C) and clone_2 (incorrectly pointing to G due to old reflog data). 

Bill notes his expectation that `git fetch` would update the upstream HEAD information after a URL change, but confirms Phillip's suggested workaround of using `rebase --no-fork-point` works in his case. The email serves as both a confirmation of the technical explanation and a real-world validation of the workaround.

This continues the thread's investigation into a long-standing behavior where `pull --rebase` can unexpectedly drop commits after remote URLs are changed, with the core issue now well-understood but still open for potential future fixes or documentation improvements. The discussion remains focused and productive, with the original reporter actively engaged in verifying solutions.