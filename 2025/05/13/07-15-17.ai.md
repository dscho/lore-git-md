This is a minor correction to Elijah Newren's merge-tree `--dry-run` feature series. Kristoffer Haugsbakk points out that the patch still references the old `--mergeability-only` flag name in the documentation, despite the thread having already settled on `--dry-run` as the preferred name (per Junio Hamano's guidance). 

The observation is straightforward - the diff snippet shown still contains the deprecated flag name in the documentation section. This appears to be an oversight in the final patch submission, as the thread had clearly moved to using `--dry-run` instead. The issue is purely documentation-related and doesn't affect the actual implementation, which correctly uses the new flag name in the code.

This email serves as a simple proofreading note rather than introducing new technical discussion. The thread had already reached consensus on all substantive aspects of the feature, making this a minor cleanup item that the author will likely address with a documentation fix.