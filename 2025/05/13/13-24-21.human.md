This is a follow-up email in the `git merge-tree --dry-run` feature series, where Junio Hamano is reviewing the second patch. The patch adds the user-facing `--dry-run` flag to `git merge-tree` for efficient mergeability checking, primarily targeting Git forges that want to test merges without computing full results.

Junio notes that the patch generally looks good, but points out a minor inconsistency: the documentation still refers to the internal `--mergeability-only` name rather than the final `--dry-run` flag name that was agreed upon earlier in the thread. This appears to be an oversight that needs correction before finalizing the patch.

The email confirms the technical approach is sound and the implementation matches the design discussed earlier, with the flag providing early exit on conflicts and avoiding unnecessary object writes. The only remaining issue is this documentation alignment, which should be a straightforward fix. The thread has already reached consensus on all major aspects of the feature.