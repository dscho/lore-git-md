This is a follow-up email in the `--dry-run` feature series for `git merge-tree`, where Junio Hamano is reviewing the second patch. The email focuses on a documentation inconsistency in the patch - while the command-line interface was changed to use `--dry-run` based on earlier feedback, the documentation still refers to the old `--mergeability-only` name. 

Junio notes that the first three lines of the commit message are nearly identical to patch 1/2 (which is fine since they share context), but points out the lingering documentation discrepancy. The diff shows the problematic section where `--mergeability-only` appears in the docs while the surrounding text describes the `--dry-run` behavior. 

This appears to be a minor oversight in an otherwise complete and well-tested series. The thread context indicates the implementation is sound and the naming discussion was already resolved in favor of `--dry-run`. The only remaining task is updating this one documentation reference to match the final interface name. The review is constructive and confirms the technical approach while catching a small consistency issue that needs fixing before final submission.