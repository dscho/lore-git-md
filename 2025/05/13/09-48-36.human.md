This is a reply in the oidmap refactoring series, specifically discussing the second patch which adds a size function to the oidmap API.

Patrick Steinhardt responds to Jeff King's explanation about the difference between checking the hashmap's allocated table size versus the actual number of entries. While initially confused, Patrick agrees that for the specific use case in `lookup_replace_object()` (which only needs to know if the map is empty), either check would work equivalently since the table size will be non-zero if there are any entries.

The exchange highlights a subtle but important distinction in the hashmap implementation details that the patch helps abstract away through the new `oidmap_get_size()` wrapper. This is part of the series' broader goal of improving API encapsulation by preventing direct access to hashmap internals. The discussion shows careful attention to edge cases and implementation details while maintaining the patch's correctness.