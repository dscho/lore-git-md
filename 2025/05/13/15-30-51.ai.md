This is a follow-up to the `--dry-run` feature for `git merge-tree` series, addressing a documentation inconsistency. The thread has already established consensus on the technical implementation and naming (`--dry-run` rather than the original `--mergeability-only` proposal), with all edge cases tested and reviewed.

Elijah Newren acknowledges Junio Hamano's observation (and Kristoffer Haugsbakk's earlier catch) that the documentation still referenced the old `--mergeability-only` flag name in the final patch. The inconsistency appears in both the man page documentation and the option validation code. Newren confirms this was an oversight and plans to send a re-roll fixing all remaining instances of the old flag reference.

The series is otherwise complete with:
- Core merge-ort plumbing changes to support mergeability checking
- Thorough test coverage of all behaviors
- Clear documentation of edge cases (like rename conflicts still writing objects)
- Safety checks preventing incompatible option combinations

This follow-up addresses the final polish needed before the series can be finalized, ensuring complete consistency in using the `--dry-run` terminology throughout. The change is purely mechanical - updating documentation and error messages to match the already-agreed interface.