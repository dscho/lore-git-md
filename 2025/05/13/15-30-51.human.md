This is the final cleanup for the `--dry-run` feature in `git merge-tree`. Elijah Newren responds to Junio Hamano's review, acknowledging that some documentation and option validation code still referenced the old `--mergeability-only` flag name instead of the final `--dry-run` name that was agreed upon earlier in the thread.

The author confirms this was an oversight that Kristoffer Haugsbakk also caught, and promises to send a re-roll fixing all remaining instances of the old name. This appears to be the last remaining issue before the series can be finalized, as the core functionality and test coverage are already complete.

The patch series as a performance optimization for Git forges, allowing them to check mergeability without computing the full merge result. The implementation carefully handles edge cases like rename conflicts while providing significant speedups for the common case. With this final naming consistency fix, the series appears ready for inclusion.