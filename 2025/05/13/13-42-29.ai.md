Bugfix and refactoring completion for mailinfo header decoding. This final v4 patch from Lidong Yan addresses Junio C Hamano's last review comments on the email header decoding refactoring in `mailinfo.c`. The changes focus on two remaining technical points from the v3 review.

First, Yan explains the retained `strbuf_grow()` calls in `decode_q_segment()` and `decode_b_segment()`, noting they prevent potential multiple reallocations during `strbuf_addch()` operations, even though this means slightly over-allocating memory (since Q/B encoding always expands the output). Hamano had accepted this as a harmless tradeoff.

Second, the patch completes the architectural transition to caller-allocated strbufs throughout the header decoding subsystem, with all decoder functions now properly taking initialized strbufs as output parameters and returning error status. This concludes the multi-iteration refactoring that began as a simple memory leak fix.

The thread shows this patch series has now satisfied all review requirements, with Hamano indicating the code is ready for merging. The changes demonstrate careful attention to both memory safety and API cleanliness in Git's email header processing code.