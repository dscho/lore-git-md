This is a reply in the oidmap refactoring thread, specifically about the third patch that embeds the replace_map directly in raw_object_store rather than using a pointer. Patrick Steinhardt agrees with Jeff King's change from the previous email, confirming that eliminating the extra pointer indirection is sensible since the map is always allocated with its containing struct and never checked for NULL.

The reply serves as an ack for the entire series, with Patrick noting that all three cleanup patches (the rename to oidmap_clear(), addition of oidmap_get_size(), and this pointer removal) look good. This suggests the thread may be nearing completion with no outstanding objections to the changes. The technical context remains as established in the thread summary: this change affects commit-graph handling, object store management, and replace-object implementation by converting access patterns from `r->objects->replace_map` to `&r->objects->replace_map` while maintaining identical behavior.