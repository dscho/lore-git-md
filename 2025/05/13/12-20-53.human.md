This is a feature patch adding a new configuration option for commit signing. Chris Down proposes adding `commit.signoff` as a config option that mirrors the existing `--signoff` flag behavior, similar to how `format.signoff` works for `git format-patch`.

The patch provides a clean implementation with documentation updates and a new test case. The motivation is clear: this helps users who regularly need signoffs but don't use patch-based workflow, avoiding the need for shell aliases. The implementation follows existing patterns seen in Git's codebase.

The changes are straightforward:
1. Adds config option documentation alongside existing signoff documentation
2. Implements the config handling in `builtin/commit.c`
3. Includes a test verifying the config option works as expected

This appears to be a well-considered, focused addition that maintains consistency with existing Git configuration patterns. The patch is small, self-contained, and comes with proper documentation and signoff.