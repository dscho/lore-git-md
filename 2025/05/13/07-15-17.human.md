This is a minor correction to Elijah Newren's merge-tree `--dry-run` patch series. Kristoffer Haugsbakk points out that the quoted diff still shows the old `--mergeability-only` flag name that was changed to `--dry-run` based on Junio Hamano's naming guidance in the earlier discussion.

The thread had already reached consensus on using `--dry-run` as the final flag name, so this appears to be just a documentation oversight in the quoted portion of the patch. The actual implementation in the series correctly uses `--dry-run` as the interface.

This is a simple catch that doesn't affect the functionality or the overall design of the feature, which was already well-received and considered complete. The series provides an optimized mergeability check for Git forges by avoiding unnecessary object writes during dry-run operations.