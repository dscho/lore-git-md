This is a follow-up discussion about performance optimizations for `git add -p/-i` with sparse index support, part of Derrick Stolee's 3-part series integrating interactive commands with the sparse index.

The email addresses Elijah Newren's review feedback on PATCH 2/3, specifically:
1. Clarifying the commit message to better distinguish between current behavior (pre-patch) and future work directions regarding index expansion differences between `-p` and `-i` modes
2. Correcting test assumptions about index resparsification - acknowledging that `git reset` alone doesn't fully resparsify the index and agreeing to use `git reset --hard && git sparse-checkout reapply` instead

Key technical points maintained from the thread context:
- The patch modifies `builtin/add.c` to move sparse index integration earlier in `cmd_add()`
- Preserves the asymmetric behavior where `-p` expands for out-of-cone edits while `-i` does not
- Maintains test coverage in `t1092-sparse-checkout-compatibility.sh` with corrected resparsification logic

The exchange shows Stolee incorporating Newren's expertise on sparse index edge cases, particularly around test accuracy for index state transitions. This follows the last remaining open items from Newren's review, suggesting the series is nearing final approval after these test corrections. The discussion remains focused on the technical implementation details rather than introducing new scope.