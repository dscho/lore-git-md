Performance optimization extending sparse-index integration to `git reset --patch`.

This email from Derrick Stolee responds to Phillip Wood's inquiry about expanding sparse-index optimizations to other patch-oriented commands. Stolee provides performance test results showing that while `git checkout --patch` benefits automatically from the current series' `git apply` changes (showing a 92% speedup in sparse index mode), `git reset --patch` still shows suboptimal performance (only 42% speedup).

Key technical details include:
- New performance tests added for `git checkout --patch` and `git reset --patch` showing their behavior with both full and sparse index
- The 92% speedup for `git checkout --patch` comes "for free" from the existing `git apply` optimizations
- `git reset --patch` requires additional work due to similar issues as `cmd_add()`
- Stolee plans to add another patch to the series to address `git reset --patch`

The email confirms the broader applicability of the sparse-index optimizations while identifying one remaining command (`git reset --patch`) that needs specific attention. This follows the thread's established pattern of thorough performance measurement and incremental optimization.