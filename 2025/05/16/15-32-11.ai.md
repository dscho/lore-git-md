This is a follow-up in the sparse index performance optimization thread, where Elijah Newren provides review feedback on Derrick Stolee's v2 series. The series integrates sparse index support with interactive commands (`git add -p/-i`, `git checkout -p`, `git reset --patch`), building on earlier optimizations to `git apply`.

Key points in this email:
1. Notes that Junio Hamano already merged v1 of the series to 'next', creating a potential version control conflict that will require either reverting v1 or rebasing v2
2. Identifies a typo in the commit message where "git add -p" appears twice when it should say "git add -i"
3. Confirms the test improvements and command integrations in v2 look good, specifically:
   - Unified index expansion behavior between `git add -p` and `git add -i`
   - New support for `git checkout -p` and `git reset -p`
   - Necessary changes to `builtin/reset.c` mirroring the `git add` optimizations
4. Mentions he hasn't yet reviewed the new patch 3 (for `git reset --patch`) but provides preliminary approval for the other changes

The email maintains the thread's technical focus on sparse index integration patterns while addressing the practical version control issue introduced by the premature merge. Elijah's review continues his pattern of careful attention to test accuracy and commit message clarity that has characterized this thread.