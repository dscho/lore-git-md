This is a follow-up discussion about a bugfix in the bundle-uri subsystem regarding `sscanf()` usage, part of an already-reviewed 11-patch series addressing CodeQL findings.

Phillip Wood chimes in to support Jeff King's earlier suggestion to replace `sscanf()` with more robust alternatives like `strtoul()` or `repo_config_get_ulong()`. He notes the current patch only fixes one instance while another `sscanf()` call in the same file remains (though not flagged by CodeQL). Phillip raises platform-specific concerns about 64-bit value parsing on Windows and suggests `strtoumax()` might be needed. He also proposes updating Git's coding guidelines to discourage `sscanf()` usage, noting it's only used in one test helper outside this file.

The discussion builds on the already-approved patch, suggesting potential follow-up work rather than blocking the current series. Phillip's comments indicate this is a good-faith technical discussion about making the code more robust, though the immediate patch is considered ready for integration as-is. The thread shows maintainers and contributors collaborating to address both the immediate static analysis finding and broader code quality concerns.