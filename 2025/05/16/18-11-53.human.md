This is a documentation and test maintenance patch in the `--path-walk` delta compression series. It aligns the usage message in `builtin/pack-objects.c` with the documentation in `git-pack-objects.adoc`, then removes `pack-objects` from the test exception list of commands where help output doesn't match documentation. 

The changes are straightforward: the usage message now matches the documentation's formatting and includes all the same options (including the new `--path-walk` flag introduced in this series). The patch follows the project's pattern of keeping command-line help synchronized with documentation, as verified by the t0450 test script. This is a minor but important consistency improvement in the larger series that's already been thoroughly reviewed.