This is a follow-up performance optimization in the ongoing sparse index integration series. Derrick Stolee responds to Phillip Wood's question about extending sparse index optimizations to `git checkout --patch` and `git reset --patch` commands.

Stolee shares performance test results showing that `git checkout --patch` already benefits from the series' `git apply` changes, achieving a 92% speedup (1.37s to 0.11s) with sparse index. However, `git reset --patch` only gets a 42% improvement (1.59s to 0.92s), indicating it still needs work similar to what was done for `git add`.

The email confirms the current series' optimizations have broader impact than originally scoped, while identifying one remaining command (`git reset --patch`) that will need additional changes. This suggests the sparse index work may continue expanding to cover more interactive commands in future patches. The performance measurements provide concrete evidence of the benefits, with the sparse index optimizations showing dramatic speedups where fully implemented.