Here's a human-readable thread summary:

Jon Forrest has proposed a new two-file index structure to optimize `git add` operations by avoiding full index resorting. The current implementation requires re-sorting the entire index when adding files, which can be expensive. His design introduces an "index index" containing sorted filenames with pointers to unsorted entries in the main index file, along with marking deleted entries for later cleanup rather than immediate removal.

The proposal shows technical understanding of index internals but leaves several practical considerations unaddressed, including impact on concurrent operations and existing tools like fast-import. While the core concept appears sound, the email lacks implementation specifics or performance data that would help evaluate its real-world benefits versus the added complexity of maintaining two index files. The discussion is just beginning, with no reviews or responses yet from other contributors familiar with the index subsystem.