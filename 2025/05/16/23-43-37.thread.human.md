This thread explores potential optimizations to Git's index file structure, originally proposed by Jon Forrest as a "two-file index" to reduce write amplification during `git add` operations. K Jayatheerth provided a detailed response explaining that Git already keeps the index as a sorted in-memory array that writes pre-sorted to disk, differing from Jon's initial assumption that each `git add` required a full resort.

Jon has since refined his proposal, now suggesting keeping only a smaller metadata structure in memory while leaving the full index on disk. However, he's also expressing doubts about the need for such optimization, noting that Git's own repository contains only 4638 index entries and current performance seems adequate for most cases. The discussion has touched on technical nuances like variable-sized cache entries affecting binary search efficiency, comparisons to database systems, and how existing features like split index already address some performance concerns.

While the conversation remains exploratory without concrete benchmarks, it demonstrates thoughtful technical exchange - Jon is reconsidering his initial proposal in light of new information while still exploring alternative approaches. No maintainers have weighed in yet, and the thread may be winding down as the original optimization motivation appears less compelling upon closer examination of Git's actual behavior and typical workloads.