This is the final patch in a 3-part documentation series modernizing the MyFirstContribution tutorial, specifically focusing on updating configuration access patterns. The patch replaces global `git_config()` calls with repository-aware `repo_config()` variants throughout the tutorial's example code.

The changes demonstrate proper modern Git usage by:
1. Using `repo` parameter passed to built-ins rather than relying on `the_repository` global
2. Updating configuration lookup examples to show repository-specific access
3. Including necessary headers (`repository.h`) for the new API
4. Maintaining consistency with surrounding text explanations

This completes the technical modernization portion of the tutorial updates, following the earlier patches that removed obsolete references and explained the UNUSED macro. The changes are straightforward and align with Git's ongoing effort to move away from global state. The patch appears well-formed with clear commit message explanations and proper documentation updates.