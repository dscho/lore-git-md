This is patch 4/13 in Jeff King's series removing support for non-standard object types in Git. The patch simplifies `git cat-file`'s type display logic now that unknown type handling has been removed.

The change replaces the strbuf-based type name handling with direct use of the object_type enum, since we no longer need to handle arbitrary type names from `OBJECT_INFO_ALLOW_UNKNOWN_TYPE`. The patch removes the strbuf infrastructure and uses the standard `type_name()` function to convert the enum to a string. Junio Hamano notes this is a good step toward eventually removing the type_name strbuf support entirely.

The technical change is straightforward - it removes the strbuf handling code and replaces it with direct enum usage. This is part of the broader cleanup after removing unknown type support, and follows naturally from patch 2/13's change to make `--allow-unknown-type` a no-op. The patch appears clean and well-contained, with no behavioral change beyond the internal implementation simplification.

This continues the series' pattern of removing now-unnecessary infrastructure after the core functionality changes. The change is small (9 lines removed, 4 added) and focused, with no outstanding questions or concerns raised in the thread. It builds on the earlier patches' removal of unknown type support while preparing for later removal of the type_name strbuf infrastructure.