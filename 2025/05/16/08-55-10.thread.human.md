This thread explores performance optimizations for packfile handling, particularly around reducing syscalls when dealing with missing packs referenced by stale multi-pack-index (midx) entries. Patrick Steinhardt's original patch proposed reordering checks in `add_packed_git()` to avoid unnecessary `access()` calls for auxiliary files when the main packfile doesn't exist, showing a 75% syscall reduction in pathological cases.

Junio Hamano provided important historical context, noting the current verification sequence (.idx before .pack) was deliberately designed to prevent using incomplete packs. Jeff King (peff) has now weighed in, agreeing the core safety constraint remains valid but arguing the auxiliary file optimization is orthogonal. He proposes caching failed pack lookups in the midx to prevent repeated stale lookups, while maintaining the critical .idx-first verification order.

The discussion has revealed deeper architectural questions about packfile integrity checking in modern Git. While the immediate focus remains on syscall reduction in `add_packed_git()`, participants are considering broader implications for midx cache invalidation during repacking and potential lazy-loading optimizations. The thread balances performance gains against Git's longstanding safety constraints, with Jeff's proposal offering a path forward that addresses both concerns. Further discussion may explore whether the original pack verification assumptions still hold and what other optimizations might be possible without compromising packfile integrity.