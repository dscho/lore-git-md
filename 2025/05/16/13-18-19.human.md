This is a final review comment on the `--dry-run` feature for `git merge-tree`, which has already been approved for merging. Phillip Wood raises a naming concern about the `--dry-run` flag's behavior differing from other Git commands that use the same flag name.

The patch series, now in its final form, adds a `--dry-run` option that completely suppresses output and avoids writing objects when checking mergeability - behavior that differs from commands like `git push --dry-run` which still produce diagnostic messages. Phillip notes this feels more like `git diff --quiet` in spirit, though he explicitly states he's not trying to bikeshed the naming at this late stage.

The thread context shows this feature has already gone through multiple rounds of review, with Junio Hamano previously suggesting the `--dry-run` name over the original `--mergeability-only` proposal. The implementation is complete with thorough test coverage and documentation, and all reviewers have signed off on the current approach. Phillip's observation comes as a stylistic note rather than a blocking concern, likely intended to inform future similar feature designs rather than change this already-approved work.