Performance optimization discussion about batched reference updates in fetch operations. This is part of the ongoing series to improve performance by using batched reference updates instead of per-reference transactions.

Patrick Steinhardt raises several technical questions about the error handling design in the batched fetch implementation. He notes that while the patch now properly reports generic errors (rather than ignoring them), the current approach using `REF_TRANSACTION_ALLOW_FAILURE` makes it unclear exactly which errors are being accepted. He suggests an alternative design where error handling decisions could be made during transaction preparation rather than after commit, which might offer more flexibility and clarity.

The discussion touches on some subtle aspects of the reference transaction API, particularly around when and how errors should be handled. Patrick acknowledges this feedback might have been more timely when the mechanism was first introduced, but the current implementation has revealed these design questions more clearly. He also catches a minor typo in a TODO comment ("about" vs "above").

This email continues the thread's technical depth around error handling in the batched reference updates feature, showing careful review of edge cases and API design considerations. The conversation remains focused on improving the implementation rather than challenging the core performance benefits, which have already been well-demonstrated in earlier benchmarks.