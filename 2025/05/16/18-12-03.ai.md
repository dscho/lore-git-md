This is the final patch in the path-walk delta compression series, completing shallow clone compatibility by removing the artificial restriction between `--shallow` and `--path-walk` options in `git pack-objects`. The patch builds on the newly added `edge_aggressive` mode from previous patches to properly handle shallow operations.

The key change removes the warning that previously prevented using path-walk with shallow operations, now that the underlying path-walk API correctly handles shallow cases through the `edge_aggressive` flag. The patch includes a test enhancement in t5538-push-shallow.sh that verifies both the object counting behavior and progress output when pushing from a shallow clone with path-walk enabled, including trace2 event verification of the path-walk operation.

The implementation moves the `shallow` flag to file scope in pack-objects.c to make it accessible to the path-walk logic, while removing the redundant warning check that's no longer needed. Test coverage confirms the feature works as expected, with the new test case specifically checking that path-walk mode activates properly via the `pack.usePathWalk` config during shallow pushes.

This completes all planned functionality for the path-walk series, with all edge cases (including shallow operations) now properly supported. The patch follows the established pattern of the series by adding targeted test coverage while removing now-unnecessary restrictions in the code.