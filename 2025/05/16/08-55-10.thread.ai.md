Performance optimization for packfile handling focusing on syscall reduction in `add_packed_git()` (packfile.c) and midx cache improvements. Original patch by Patrick Steinhardt reordered checks to avoid unnecessary `access(3p)` calls for auxiliary files (.keep, .promisor, .cruft) when main packfile doesn't exist, showing 75% reduction in syscalls for pathological cases with stale multi-pack-index entries. Junio Hamano provided historical context about original ordering being deliberate for correctness - ensuring .idx files precede .pack checks to prevent using incomplete packs.

Jeff King (peff) proposed adding a third state (-1) to midx->packs[] to cache "failed lookup" results, preventing repeated failed lookups of missing packs. Junio Hamano agreed with this approach. Patrick Steinhardt now also agrees with Jeff's implementation but raises concerns about potential race conditions if auxiliary files (.keep, .promisor, .mtimes) are handled out of order with main packfile. Analysis of `unlink_pack_path()` confirms deletion order is correct but creation ordering remains uncertain, leading Patrick to conclude his original patch reordering auxiliary file checks was incorrect as it could introduce races where auxiliary data is missed.

Current implementation in `midx.c`:
1. Adds handling for new "failed lookup" state (-1) in `prepare_midx_pack()`
2. Skips already-failed lookups by returning early
3. Maintains state during cleanup in `close_midx()`
4. Uses type-punned pointer (casting -1 to void*) following Git patterns
5. Preserves critical ordering: .idx checks must precede .pack checks

Key technical constraints clarified:
- Auxiliary files must be created before their .idx file
- Auxiliary files must be deleted after their .pack file
- Current `unlink_pack_path()` implementation maintains correct deletion order
- Original optimization of reordering auxiliary file checks was unsafe

Open questions and considerations:
- Proper handling of midx cache invalidation during repacking
- Jeff King's lazy-loading auxiliary files proposal deemed too risky by Patrick
- Performance gains may not justify complexity for well-maintained repos
- Need to document intentional ordering of packfile integrity checks

The thread shows strong consensus around Jeff King's midx cache modification approach, with both Junio Hamano and Patrick Steinhardt now in agreement. Core issue of reducing syscalls for stale midx entries is being addressed through caching failed lookups while maintaining all safety constraints. Discussion has clarified important invariants about packfile handling that will inform future optimizations, with auxiliary file optimizations now understood to require more care than initially thought.