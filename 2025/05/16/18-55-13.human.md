This is a brief administrative note from Junio C Hamano regarding the sparse index integration series. The email confirms that the previously merged v1 of the series will be reverted to accommodate the newer v2 patches, which Elijah Newren has reviewed and approved (with one minor typo fix noted in the `git add -p/-i` commit message).

The context shows this is part of a performance optimization effort to make interactive commands (`add -p/-i`, `reset --patch`, etc.) work efficiently with sparse checkouts by avoiding unnecessary index expansion. The v2 series has already demonstrated significant speedups (96% for `git add -p`, 95.6% for `git reset --patch`) and received thorough review from Elijah Newren, with all technical concerns addressed. Junio's note indicates the version control logistics are being handled to properly transition from v1 to v2 in the 'next' branch.