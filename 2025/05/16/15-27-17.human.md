This is a follow-up discussion in the path-walk delta compression series, specifically addressing review feedback on patch 02/13 which introduces the `--path-walk` option for pack-objects. Derrick Stolee responds to Taylor Blau's technical review points from earlier in the thread.

The email resolves three specific code-level questions from the review:
1. Explains why `sub_list_size` remains `unsigned int` (matching the `find_deltas()` interface) rather than switching to `size_t`
2. Agrees to rename `is_oid_interesting()` to better reflect its actual behavior (checking for UNINTERESTING objects)
3. Concludes that setting `revs->tag_objects = 1` is unnecessary and will be removed

These are minor implementation refinements to an otherwise technically complete patch series that has already received Junio Hamano's approval for the 'next' branch. The changes don't affect the core functionality of path-based delta compression but clean up some interface details raised during review. The thread shows the final polishing stage of a well-reviewed feature before integration.