This is a documentation patch in an ongoing series modernizing the MyFirstContribution tutorial, specifically addressing config access patterns. The patch updates the `cmd_psuh` example to use `repo_config` and `repo_config_get_string_tmp` instead of their global counterparts, demonstrating repository-aware configuration access.

Emily Shaffer's review focuses on pedagogical presentation rather than technical correctness, suggesting the tutorial should show incremental code additions rather than complete function replacements. She notes the commit message could better clarify that `git_config()` isn't actually buggy here - it's just outdated due to `the_repository` usage. The review also touches on maintaining the sample code branch, suggesting either keeping it in git/git or on her personal fork.

Key technical details include the switch from `git_config()` to `repo_config()` and corresponding string lookup functions, with the tutorial now showing proper repository parameter passing. The changes affect both the core example and references to `git status` internals. Shaffer's feedback indicates the series is nearly ready, pending minor commit message tweaks and presentation adjustments.

This continues the thread's theme of modernizing contributor documentation while maintaining educational effectiveness. The review doesn't raise new technical concerns about the config access patterns themselves, focusing instead on how best to present them to new contributors. The discussion about sample code maintenance remains open but doesn't block the series' progress.