This is the final patch in K Jayatheerth's v2 series (3/3) modernizing the MyFirstContribution tutorial, focusing on updating configuration access patterns. The patch completes the technical modernization effort by replacing global `git_config` calls with repository-aware `repo_config` variants throughout the tutorial examples.

The changes demonstrate proper repository-aware configuration access in three key places: (1) basic config lookup for user.name, (2) status preparation, and (3) default config handling. Each instance shows the transition from global state (`the_repository`, `git_config`) to repository-specific patterns (`repo` parameter, `repo_config`). The patch also adds the "repository.h" include where needed and updates surrounding documentation text to reference the new API names.

This builds on the series' earlier work (v2 2/3) that introduced repository parameters and UNUSED macros. The changes are consistent with Git's ongoing `the_repository` removal effort and provide new contributors with examples of modern configuration handling patterns. The patch maintains the tutorial's pedagogical flow while updating the technical content, showing both the direct config access pattern and how it propagates through helper functions like `wt_status_prepare`.

With this patch, the v2 series now fully addresses its technical modernization goals, leaving only the pedagogical discussions from the parallel v1 series as outstanding items. The changes are cleanly scoped to Documentation/MyFirstContribution.adoc with no functional impact beyond the tutorial content.