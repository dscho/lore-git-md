Refactoring patch as part of the path-walk delta compression series.

This is the first patch in a 13-part series introducing path-based delta compression to Git's pack-objects and repack commands. It extracts the delta eligibility checking logic from `prepare_pack()` into a new `should_attempt_deltas()` helper function. The change is purely structural - no behavior is modified, just moved to make the code more maintainable and prepare for reuse in later patches.

The patch cleanly separates the delta eligibility conditions (object size, type validity, delta reuse status, etc.) into a single function with clear early returns. This will be useful in subsequent patches that implement the new path-walk delta selection strategy. The diff shows the straightforward extraction with no functional changes - just moving 30 lines of logic into a new function and replacing the original with a simple call.

As the first step in a well-reviewed series that's already been approved for merging, this patch serves as a foundation for the more substantial changes that follow. It demonstrates the careful, incremental approach taken throughout the series.