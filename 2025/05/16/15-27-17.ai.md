This is a technical follow-up in the path-walk delta compression series, addressing review feedback on patch 02/13. Derrick Stolee responds to Taylor Blau's questions about implementation details in the `add_objects_by_path()` function and `get_object_list_path_walk()` setup.

Key points addressed:
- Confirms keeping `sub_list_size` as `unsigned int` to match `find_deltas()` interface requirements, though notes this could be upgraded later
- Agrees to rename `is_oid_interesting()` to its logical inverse since no other callers exist
- Will remove unnecessary `revs->tag_objects = 1` assignment that was questioned

The changes are minor implementation refinements responding to Taylor Blau's review, with no major architectural impact on the path-walk feature. This continues the pattern of thorough review and incremental improvement seen throughout the series. The thread context shows this patch is part of a fully reviewed series that Junio Hamano has approved for 'next', with these being final polish items before integration.