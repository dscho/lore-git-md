This is a simple typo fix in the object-file.h header, part of Jeff King's larger series removing support for non-standard object types. The patch corrects a misspelled variable name (`comapt_oid_in` -> `compat_oid_in`) in the `write_object_file_flags` function declaration.

While minor, this fix is relevant as it touches the same header file being modified throughout the series. The typo appears to be long-standing but harmless, as the variable is only declared here and not used elsewhere in the codebase. This patch serves as a straightforward cleanup before the more substantial changes to object handling that follow in the series.