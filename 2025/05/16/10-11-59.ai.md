This is a follow-up discussion about a bugfix in the bundle-uri subsystem regarding `sscanf()` usage, part of the larger static analysis cleanup series that has already been approved.

The email continues the conversation about patch 10/11, which originally addressed undefined behavior in `bundle-uri.c` when parsing `fetch.bundlecreationtoken` by ensuring the `maxCreationToken` variable is always initialized. Phillip Wood agrees with Jeff King's earlier suggestion that `sscanf()` may not be the best tool here, noting that Windows compatibility for 64-bit values would require `strtoumax()`-based parsing. He points out another `sscanf()` usage in the same file that wasn't flagged by the analyzer (but should also be fixed) and raises the question of whether to ban `sscanf()` in Git's coding guidelines, noting it's only used in one test helper beyond this case.

This discussion occurs after the series has already been approved, suggesting these are post-approval refinements rather than blocking issues. The technical focus remains on proper numeric parsing and potential future hardening, with Phillip offering Windows-specific considerations and a broader policy suggestion about `sscanf()` usage. The thread context shows this builds on earlier consensus about the bundle-uri changes while exploring additional improvements that could be addressed separately.