Updated AI thread summary:

MPTCP support thread summary:

This thread discusses Multipath TCP (MPT CP) support for Git on Linux systems, initially proposing automatic use of MPTCP v1 when available with fallback to regular TCP for the legacy protocol (port 9418). The discussion has evolved from implementation details to questioning whether Git should be MPTCP-aware at all, given RFC 6897's transparency guarantees.

Key technical details include: MPTCP's dual IPv4/IPv6 support, the v1/v0 distinction (v1 having proper fallback mechanisms), current limitation to port 9418 connections, and compile-time `#ifdef` checks. The implementation modifies socket creation in `connect.c`. Muhammad Nuzaihan has proposed technical improvements including Linux-specific checks and error handling, while Brian Carlson and Phillip Wood raised compatibility concerns about stripped-down kernels, IPv6/IPv4 mismatches, and container environments. Phillip suggested a Makefile knob (USE_MPTCP) and emphasized documentation needs.

Junio Hamano's intervention shifts the discussion's focus by citing RFC 6897's design principle that MPTCP should work transparently for applications, questioning whether Git needs explicit MPTCP support when the protocol handles subflow management automatically. This introduces a fundamental architectural consideration beyond the prior implementation debate.

Open questions now include: (1) whether to proceed with any Git-side MPTCP awareness given Junio's transparency argument, (2) if proceeding, how to handle configuration (Makefile knob vs runtime), (3) cross-platform limitations (Linux-only), and (4) documentation of both technical implementation and philosophical rationale. The thread has moved from "how to implement" to "whether to implement," with Junio's RFC reference providing new technical grounding for skepticism while maintaining neutrality on MPTCP's underlying value. Muhammad's empirical validation (Wireshark proofs) and use case for large repos remains unaddressed in this new context.