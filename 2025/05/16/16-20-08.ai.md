Performance optimization for `git reset --patch` with sparse index integration. This is the third patch in Derrick Stolee's v2 series optimizing interactive commands (`git add -p/-i`, `git checkout -p`, `git reset --patch`) for sparse-checkout users by preventing unnecessary index expansion.

The patch moves the sparse index check earlier in `cmd_reset()` to avoid expanding the index before the `--patch` logic runs. The implementation follows the same pattern as the earlier `git add -p` optimization, setting `command_requires_full_index=0` before the patch mode handling. Test coverage in `t1092-sparse-checkout-compatibility.sh` verifies both the expected behavior (no expansion for in-cone edits, expansion for out-of-cone edits) and performance characteristics.

Elijah Newren's review confirms the patch looks good, completing his review of the v2 series. The changes align with the established sparse-index integration patterns and demonstrate substantial performance improvements (95.6% reduction for `git reset --patch` in earlier benchmarks). The test suite additions comprehensively cover both behavior and index expansion scenarios, including edge cases where out-of-cone edits require full index expansion.

This patch represents the final reviewed component of the series, which now appears merge-ready pending resolution of the version control conflict from Junio's premature merge of v1 to 'next'. The implementation maintains consistency with the broader sparse-index optimization effort while delivering measurable performance benefits for sparse-checkout users.