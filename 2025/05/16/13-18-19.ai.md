This is a follow-up discussion on the already-approved `--dry-run` feature for `git merge-tree`. Phillip Wood raises a naming concern about the final implementation, comparing it to other Git commands' `--dry-run` behaviors.

Phillip notes that while the patch's `--dry-run` suppresses all output (like `git diff --quiet`), most other Git commands using `--dry-run` (e.g. `git push`, `git send-email`) still produce diagnostic messages. He frames this as an observation rather than a blocking objection ("I don't want to bike-shed"), suggesting the current behavior might be inconsistent with user expectations set by other commands.

The technical implementation remains unchanged from the approved version - it still uses the `mergeability_only` internal flag, validates incompatible option combinations, and includes tests verifying empty output and no object writes. The thread context shows this was already through multiple review rounds with consensus on the interface, so this appears to be a late-stage stylistic observation rather than a technical issue.

Key points for future thread tracking:
- The `--dry-run` naming is now the settled interface (per Junio's earlier guidance)
- Current implementation suppresses all output (unlike some other Git commands)
- Phillip's observation doesn't block merging but documents a potential UX inconsistency
- No code changes proposed - this is purely about command behavior documentation