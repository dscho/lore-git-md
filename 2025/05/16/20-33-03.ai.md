This is a follow-up discussion about the proposed MPTCP support patch, focusing on compatibility concerns and configuration options. Brian raises several important technical questions about edge cases in the current implementation that automatically uses MPTCP when available.

The email identifies three main scenarios where the current approach might fail: 1) when binaries built on MPTCP-capable systems run on stripped-down kernels without MPTCP, 2) IPv6-only connections on systems with IPv4-only MPTCP support (like FreeBSD), and 3) potential parallels to Go 1.24's problematic MPTCP rollout. Brian suggests making MPTCP usage configurable rather than automatic, while still keeping it as the default.

The discussion reveals that this primarily affects the legacy unauthenticated Git protocol (port 9418), which sees little modern use. However, Brian notes that the remaining users of this protocol may be precisely those running unusual configurations where automatic MPTCP could cause problems. The email doesn't propose specific configuration mechanisms but clearly argues for adding some user control over MPTCP usage.

Key technical details mentioned include the distinction between IPPROTO_MPTCP v1 vs v0, the layer 4 networking behavior, and the reference to Go 1.24's implementation problems. The thread context shows this builds on an initial patch that implemented automatic MPTCP detection without configuration options.