This is a technical discussion about potential index file optimization. Jon Forrest proposes a two-file index structure to avoid full index resorting during `git add` operations, suggesting an "index index" containing sorted filenames with pointers to unsorted full entries in the main index. The proposal includes marking deleted entries rather than immediately removing them, with periodic cleanup.

Key technical points:
- Current behavior requires full index resorting when adding files
- Proposed "index index" would store (filename, offset, length) tuples sorted by name
- Main index would remain unsorted with new entries appended
- Deleted entries would be marked rather than removed, cleaned up later
- Acknowledges sorting overhead remains but with less data movement

The email shows familiarity with index internals but doesn't reference prior discussions about index optimizations. It raises valid concerns about sorting overhead but doesn't address how this would impact existing index operations like fast-import or how the two-file approach would handle concurrency. The proposal appears theoretically sound but lacks implementation details or performance measurements.