The thread explores adding Multipath TCP (MPT CP) support to Git, with Muhammad Nuzaihan driving the technical proposal and Junio Hamano raising fundamental questions about whether Git should be MPTCP-aware at all, given RFC 6897's transparency guarantees. The discussion has evolved from initial implementation details to a deeper architectural debate, now informed by production deployment evidence and input from Linux MPTCP maintainer Matthieu Baerts.

Baerts' latest email provides crucial technical clarification, explaining why Linux's MPTCP implementation requires explicit application support through socket creation with `IPPROTO_MPTCP`, contrary to the RFC's vision. This validates Muhammad's technical approach of modifying the socket() call rather than getaddrinfo() parameters, while also acknowledging workarounds like `mptcpize` exist but native support would be preferable. The email resolves Junio's RFC 6897 concern by showing the Linux implementation reality diverges from the standard's transparency principle.

Key technical considerations include the minimal code change needed (a single protocol parameter), Linux's socket API requirements, and the proposed `core.mptcp` configuration (defaulting to false for clients). The discussion now weighs philosophical protocol design principles against concrete user benefits like uninterrupted operations when switching networks, with Muhammad's WIP patch awaiting final testing to demonstrate the approach.