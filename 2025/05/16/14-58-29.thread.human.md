This thread covers improvements to `git stash -p/--patch` option parsing and pathspec handling, now merged to the 'next' branch in its third version. Phillip Wood developed the two-patch series, which was reviewed by Martin Ågren and Junio Hamano. The changes fixed a regression from 2020 where `git stash -p <pathspec>` broke due to incorrect argv handling, while also making option ordering more flexible.

The core functionality is working correctly, but Martin identified a test coverage gap where the current tests using `test_write_lines s y n` could pass even with incorrect pathspec processing. While manual testing confirms the implementation works as intended, both Phillip and Martin agree the tests should be strengthened to better verify pathspec edge cases. Phillip has committed to addressing this in the next release cycle.

The discussion has now shifted from implementation to verification, with all parties satisfied with the merged changes but recognizing an opportunity to improve test robustness. The patches have successfully fixed the regression while improving usability, with only minor test refinements remaining for future work.