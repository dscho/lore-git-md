Performance optimization follow-up in the sparse index integration series. Elijah Newren provides final review feedback on Derrick Stolee's v2 patch series that makes interactive commands (`git add -p/-i`, `git checkout -p`, `git reset --patch`) sparse-index aware. 

The key issue noted is that Junio Hamano already merged the v1 patches to 'next', so the v2 improvements will need to be handled either as a revert-and-reapply or as follow-up fixes. Elijah confirms the test improvements in v2 are better, particularly around index expansion behavior consistency between `git add -p` and `git add -i`.

He spots one remaining issue in the commit message where a second reference to `git add -p` should likely be `git add -i`. The new patch 3 adding `git reset --patch` support hasn't been reviewed yet, but the other changes look good. This continues the thread's pattern of thorough review leading to incremental improvements in the sparse index integration work.