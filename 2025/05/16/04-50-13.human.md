Final patch in the series removing non-standard object type support. This 13th patch completes the cleanup by removing the last remaining infrastructure for writing objects with unknown types, specifically the `write_object_file_literally()` function and its helpers. 

The patch eliminates 80 lines of code that had become redundant after previous patches removed all callers of these functions. The key removal is `write_object_file_literally()`, which was a parallel implementation of `write_object_file()` that had accumulated tricky duplicated logic over time. Jeff notes this makes the codebase less error-prone by removing the maintenance burden of keeping these implementations in sync.

The changes are mechanical and well-contained to object-file.c/h, with no behavior change for standard object types. This concludes the series' goal of removing support for non-standard object types that were never properly usable beyond test cases. The patch maintains all existing functionality for standard Git objects while significantly simplifying the codebase.

As the final patch in a carefully constructed series, this appears ready for merging with no outstanding technical issues. The series as a whole represents a significant code quality improvement by removing long-dormant experimental features and their associated complexity.