Git development status snapshot from Junio Hamano's "What's cooking" report, with updates to multiple ongoing topics:

Graduated to master:
- BSD compatibility: `open_nofollow()` now handles ELOOP correctly
- Build system: Meson supports custom Perl paths
- Documentation fixes and test corrections
- Performance: improved path globbing with special characters
- Maintenance: new `rerere-gc` and `worktree-prune` tasks in `git maintenance`
- Memory leak fixes across multiple subsystems

New topics introduced:
- CI: MSVC/Meson build improvements
- Performance: batched reference updates for `fetch`/`push`
- Refactoring: continued `the_repository` removal in `replay` command
- Defensive programming and memory leak fixes
- Documentation: JSON writer API updates
- Packfile: access pattern changes

Ongoing development updates:
- `merge-tree`: version 4 of patch series submitted by Elijah Newren, renaming `--dry-run` to `--quiet` per Phillip Wood's feedback to better match Git's CLI conventions. Changes affect `builtin/merge-tree.c` and `merge-ort.c`, introducing `mergeability_only` option in ORT backend triggered by `--quiet` flag. Functionality remains unchanged (checking merge conflicts without writing results) but with more idiomatic naming. Previously noted in "What's cooking" as ready for 'next' with original flag name.
- `send-email`: Aditya Garg reports that the `ag/doc-send-email` series (merged to 'next') needs partial reversion - the OAuth credential helper documentation for Outlook/Gmail has issues requiring revert, while other documentation improvements (general `send-email` updates and FQDN validation checks) are ready for 'master'. Technical details involve `valid_fqdn` checks and OAuth2.0 examples.
- `sparse-apply-add-p`: Derrick Stolee confirms version 2 of this 4-commit series (making `git apply` and `git add -i/-p` sparse-index aware) has incorporated feedback and is being reconsidered for 'next' after brief removal. Changes avoid unnecessary index expansions in these commands.
- `scalar-no-maintenance`: Stolee's 5-commit series adding `--no-maintenance` options to `scalar` subcommands (`clone`, `register`, `reconfigure`) is moving toward 'master', with Junio having included the final patch. Allows repositories to be added without automatic maintenance.
- `path-walk-2`: Stolee's substantial 13-commit series introducing path-based delta compression in `pack-objects` via new `--path-walk` option. Includes performance tests and shallow push support. Stolee indicates he won't rush feedback on this v3 update, suggesting more review time needed for this complex change.
- Stash: import/export functionality
- Sparse index: integration with `apply` and `add -p`
- Object store: API cleanup
- Performance: mmap optimization for packed-refs access
- Pack-objects: path-based delta compression
- Deprecation: plans for `git whatchanged`

Discarded topics noted due to inactivity or conflicts. The report captures current Git development from documentation to major refactoring, with Stolee's updates on his three performance-oriented series (`sparse-apply-add-p`, `scalar-no-maintenance`, `path-walk-2`) showing varying stages of readiness. The `send-email` documentation update shows improvements progressing while identifying one component needing correction, and `merge-tree` naming change reflects Git's emphasis on consistent CLI patterns.