This is a follow-up discussion about the recently merged `--quiet` flag for `git merge-tree`. Kristoffer Haugsbakk explores potential enhancements to the batch processing capabilities, specifically suggesting a `--format` option and questioning the current incompatibility between `--quiet` and `--stdin`.

The email presents a real-world use case where the author currently uses a complex shell pipeline with `--stdin` to identify conflicting refs, involving temporary files and multiple text processing steps. They propose that a `--format` option could simplify this workflow by directly outputting only the conflicting OIDs.

The core technical discussion revolves around the current limitation where `--quiet` cannot be used with `--stdin`, which the author suggests might be unnecessarily restrictive. They argue that since `--quiet` represents a new mode of operation, it could potentially be extended to support batch processing with modified output semantics, creating a "dry-run batch mode" that would be more efficient for scripting use cases.

Key points for future thread context:
- Current implementation prevents combining `--quiet` with `--stdin`, `--messages`, `--name-only`, and `-z` via `die_for_incompatible_opt2` checks
- Author's proposed `--format` syntax follows Git's existing format conventions
- The suggestion implies modifying the merge-ort plumbing to support partial conflict reporting in batch mode
- Use case demonstrates practical need for efficient conflict detection across multiple merges

This email opens discussion about potential extensions to the recently merged functionality, focusing on improving scripting use cases while maintaining the optimization benefits of the `--quiet` approach. The proposal would require careful interface design to avoid breaking existing batch mode expectations while adding new capabilities.