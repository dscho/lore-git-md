Thread identity: Test patch adding corruption detection for bitmap loading functionality, specifically targeting the bitmap table lookup feature.

Core problem/goal: Verify Git's handling of corrupt bitmap indexes by introducing controlled test infrastructure to simulate and detect corruption scenarios during bitmap loading.

Current status: Initial patch introducing test infrastructure with two implemented corruption scenarios (invalid commit positions and invalid XOR offsets). Patrick Steinhardt has raised concerns about code organization, suggesting the test logic should move to test-bitmap.c or become a unit test in t/unit-tests/ rather than residing in pack-bitmap.c. The core test approach (deliberate in-memory corruption) remains uncontested.

Key technical details:
- New `test_bitmap_load_corrupt()` function in pack-bitmap.c (current location under discussion)
- Two corruption methods implemented:
  * `do_corrupt_commit_pos()` - corrupts commit positions in bitmap
  * `do_corrupt_xor_offset()` - corrupts XOR offsets
- New `load-corrupt` subcommand for test-tool bitmap helper
- Corresponding test case in t5310-pack-bitmaps.sh
- Uses `bitmap_corrupt_then_load()` helper for corruption/loading sequence
- Specifically tests bitmap table lookup feature (requires it enabled)
- Handles mmap'd data carefully during corruption testing

Open questions: Should the test infrastructure remain in pack-bitmap.c or move to test-bitmap.c/t/unit-tests/? Patrick Steinhardt requests justification for current location or consideration of alternatives to maintain separation of test/production code.

Participant positions:
- Patrick Steinhardt: Prefers moving test logic to test-specific files (test-bitmap.c or unit tests) unless strong reason exists to keep in production code. Does not challenge test validity.