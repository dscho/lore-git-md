This thread addresses a compilation issue on older macOS systems (Xcode 14.x and earlier) where Git's `NOT_CONSTANT` wrapper interacts poorly with system headers, causing linker errors. The problem was first identified by Torsten Bögershausen, with Junio C Hamano investigating the underlying behavior. Koji Nakamaru diagnosed the issue as stemming from duplicate library inclusion triggering a linker bug and proposed two solutions - a Makefile change or code-level initialization. The chosen fix initializes a variable in `not-constant.c` to alter symbol resolution, which Torsten confirmed works where the Makefile approach failed.

In a follow-up, Koji provides important version specifics: the issue persists through Xcode 14.3.1 (the final 14.x release) but is resolved in Xcode 15, which includes a new linker. This information, drawn from Apple's release notes, satisfies Junio's request for version requirements clarification. The thread has now fully characterized the problem's scope (affecting Xcode 14.x and earlier) and documented the working solution, with no further action needed beyond potential documentation updates to note the version-specific workaround. The fix remains minimally invasive while properly handling the platform-specific compilation failure.