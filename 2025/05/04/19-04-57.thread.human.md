Here's a human-readable summary of the thread:

The thread addresses a platform-specific compilation issue on older macOS systems related to commit 82e79c63642c, which introduced `NOT_CONSTANT` with `sigfillset()`. The problem occurs because macOS's `signal.h` header has a peculiar implementation that makes Git's error-checking wrapper fail to compile.

Torsten Bögershausen has identified the issue and proposed three potential solutions: creating a macOS-specific version of the macro that skips error checking, reverting the original commit, or finding an alternative approach. He mentions having a draft patch for the revert option but is seeking community input on better solutions before proceeding.

This appears to be another case where Git's safety checks need platform-specific accommodations, particularly around signal handling functions. While the exact affected macOS versions aren't specified, the core issue stems from an incompatibility between Git's defensive programming and macOS's header implementation patterns. The discussion is currently focused on finding the least intrusive solution that maintains safety while working around the platform quirk.