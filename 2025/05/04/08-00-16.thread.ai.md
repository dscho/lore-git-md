Here's the updated AI thread summary:

Bug report about unnecessary rename detection warnings in `git show -s`. Junio Hamano identified that Git still checks and warns about rename detection being skipped due to hitting `diff.renameLimit` even with `-s` (suppress diff output), despite no rename detection being needed. Jeff King proposed a fix with a small patch (4 lines in log-tree.c) that skips diff computation entirely when both DIFF_FORMAT_NO_OUTPUT is set and rev_info.always_show_header is true (as in `git show`). The patch avoids both the rename detection and its warnings when they can't affect output. Jeff had previously hesitated to submit this due to concerns about --exit-code behavior and corner cases, but the current discussion suggests it's worth revisiting. Technical details remain unchanged: warning originates in diffcore_rename.c's renameLimit check, occurs even with suppressed diffs, demonstrated with repos having 6000+ file changes. The proposed fix cleanly addresses the optimization opportunity while preserving all other behavior. No further discussion or objections noted yet.