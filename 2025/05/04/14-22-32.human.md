This is a minor style suggestion on the first patch of the `git send-email` improvement series. Kristoffer Haugsbakk points out that while the current patch format works (with footnotes and sign-off lines mixed together), it would be better style to separate footnotes from the trailer block with a blank line. The comment doesn't critique the technical content of the patch itself, which had already been summarized as improving FQDN validation in `git-send-email.perl` to enforce RFC1035 compliance.

The observation is purely about patch formatting conventions rather than the functionality changes, and serves as a gentle suggestion for future patch submissions. Since this is a style nit on an already-submitted patch rather than a substantive change request, it's unlikely to affect the progression of the series unless the original author chooses to respin the patches with this formatting tweak.