This is a refactoring patch in the final iteration of the stash import/export feature series. It extracts stash revision parsing logic into a reusable function, `parse_stash_revision()`, which handles special stash reference formats like numeric indices (e.g., `stash@{1}`) and the default `stash@{0}` case. The change is cleanly separated from behavior modifications, maintaining identical functionality while making the code more modular for future use in the import/export commands.

The patch demonstrates careful attention to detail - it preserves error messages, adds proper string buffer initialization, and keeps the quiet flag behavior consistent. As part of a well-reviewed series that's nearing completion, this change represents final polish rather than introducing new concepts. The extracted function will likely be used by the upcoming stash import/export subcommands to handle their revision parsing needs uniformly.