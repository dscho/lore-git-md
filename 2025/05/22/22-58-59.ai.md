Bugfix follow-up discussing test coverage for `git apply --reverse` file mode handling. Junio Hamano reviews Mark Mentovai's test additions for the executable bit preservation fix, focusing on test structure and coverage.

The review notes that while the core fix (adding `|| p->is_delete` to mode bit swapping) is correct, some test changes could be improved. Key points: the `reset --hard` in one test may unintentionally reduce coverage of working tree vs index interactions, and suggests restructuring the test sequence to reuse pre-generated patch files rather than regenerating them with shifting HEAD references. The review also questions some no-op `checkout` operations in the test sequence.

Hamano proposes an alternative test organization where patches would be generated once at the start and then applied/reversed in sequence, which would make the test flow clearer and avoid HEAD reference shifting. The existing tests correctly verify the core functionality but could be made more maintainable with this approach.

The discussion remains focused on test quality rather than the underlying fix, which both parties agree is correct. This appears to be a polishing pass on test coverage for a bugfix that's otherwise ready to go.