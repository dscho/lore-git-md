This is a follow-up in the ongoing discussion about the `escapeRefName` function in git-cvsserver.perl, now focusing on historical context and confirming its unused status. Todd Zullinger provides git-log evidence showing the function was added in 2012 but never actually used, while its counterpart `unescapeRefName` was utilized. This corroborates Jeff King's earlier empirical test showing the function could be made to fail without affecting test outcomes.

The key new information is the commit history showing `escapeRefName` was introduced as infrastructure that was subsequently never completed - the 2012 commit message explicitly states "The functions are not used yet" and subsequent development only employed the unescaping half of the mechanism. This historical context strengthens the case for removing the function rather than maintaining it, as it appears to represent abandoned functionality rather than accidentally unused code.

The email doesn't propose any action but provides important context for the broader discussion about cvsserver maintenance. It suggests the original author's intent was to build refname escaping that was never completed, raising questions about whether the CVS protocol implementation in git-cvsserver has always had this sanitization gap. This historical perspective may inform whether the function should be removed (as dead code) or if the original escaping functionality should be properly implemented (to match the protocol requirements).