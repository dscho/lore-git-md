This is a test patch in the ongoing series fixing memory leaks in pack-bitmap.c's bitmap loading paths. Taylor Blau questions whether this third patch is still necessary given the context of the thread.

The test in question attempts to verify the leak fixes by corrupting a bitmap's XOR offset field and checking for proper cleanup. However, Taylor points out that the test passes even without the earlier fixes, since Git already has fallback behavior for corrupted bitmaps. The key issue being tested (memory leaks during corruption handling) is now covered by the test suite's universal leak checking enabled in 1fc7ddf35b.

Taylor suggests the test may be redundant and proposes an alternative approach - implementing a test helper that dumps EWAH bitmap offsets by object ID rather than manually parsing the bitmap file structure. He notes the current `skip_ewah_bitmap()` implementation is fragile and suggests storing bitmap positions during normal operation would make testing more robust.

This email represents a potential simplification of the series, suggesting the test patch might be dropped entirely since the core leak fixes are already validated by existing infrastructure. The discussion shows careful consideration of test design principles and the value of each test case in relation to the overall series goals.