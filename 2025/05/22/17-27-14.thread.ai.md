Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now at v19, rebased on master. The series introduces OAuth2.0 authentication support, comprehensive folder management, and critical configuration parsing fixes. Key elements:

1. Critical configuration parser regression (since Git 2.46.0) where fields were incorrectly NULLed out (6d1f198f34) - fixed with proper field-specific cleanup in `git_imap_config()`. The bug made `git-imap-send` unusable when processing unrelated configuration options (`imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`) which incorrectly NULLed out `cfg->folder`. The fix properly NULLs out the correct struct fields for each configuration option rather than incorrectly clearing `cfg->folder` in all cases. This standalone fix addresses an urgent regression that broke basic IMAP functionality.

2. OAuth2.0 support via OAUTHBEARER (RFC-standard) and XOAUTH2 (Google-proprietary) protocols, implemented for both OpenSSL and libcurl backends. Patch 4/10 adds `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` capability flags, auth method dispatch in `imap_open_store()`, and credential handling in `setup_curl()`. The implementation handles both backends, with libcurl using `CURLOPT_XOAUTH2_BEARER` for both mechanisms while maintaining backward compatibility. v19 addresses the final outstanding review feedback by replacing fragile `snprintf()` with `xstrfmt()` in `oauthbearer_base64()` and `xoauth2_base64()` functions for safer string construction. Host/port fields in the OAUTHBEARER string remain intentionally omitted per RFC7628 section 3.1.

3. RFC 4616-compliant PLAIN authentication now fully implemented for both OpenSSL and libcurl backends via new `plain_base64()` helper, implementing proper NUL-delimited "user\0pass" format before base64 encoding. v19 improves this by using `strbuf` in `plain_base64()` for safer string building and better memory management. Adds OpenSSL backend support with new `AUTH_PLAIN` capability flag.

4. CRAM-MD5 improvements including:
   - Complete error paths with additional leak fix when socket write fails
   - New `try_auth_method()` helper for centralized authentication failure handling
   - Runtime OpenSSL availability check (replacing compile-time `die()`)
   - Fixed error message to reference `srvc->auth_method` instead of `srvc->host`
   - Plugged memory leak in `auth_cram_md5()` where `response` buffer would not be freed if `socket_write()` failed

5. New `--list` option implementing RFC6154-standard IMAP LIST command for discovering available folders and their server-specific naming conventions. Handles both backends:
   - OpenSSL: Direct IMAP protocol handling via `list_imap_folders()` issuing `LIST "" "*"`
   - libcurl: Uses `curl_easy_perform` patterns in `curl_list_imap_folders()`
   Shows folder hierarchies and attributes (e.g., `\Drafts`, `\Trash` flags) while reusing existing connection/auth infrastructure.

6. New `--folder` command-line override with proper memory management (freeing previous value via `free(server.folder)` before `xstrdup` of new value) and documented precedence rules (CLI overrides config but config remains fallback).

7. User experience improvements including:
   - Destination folder display during send progress
   - Port numbers in credential requests (now showing non-default ports only)
   - Standardized error message formatting throughout `imap-send.c`

The v19 update resolves the final technical issue from Phillip Wood's review by improving string construction safety in the OAuth2.0 and PLAIN authentication implementations. The series has undergone thorough review from Jeff King, Eric Sunshine, Brian Carlson, Junio Hamano, Ben Knoble, and Phillip Wood, with all major technical issues now addressed. The implementation spans configuration parsing, authentication handlers, base64 utilities, IMAP protocol handling, and folder management. The series appears technically complete and ready for integration. The critical configuration parser regression fix (6d1f198f34) is now explicitly called out as an urgent standalone fix for the Git 2.46.0 regression.