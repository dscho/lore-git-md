Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now in v17, rebased on master to resolve merge conflict with commit 30325e2 (no functional changes from v16). The series introduces OAuth2.0 authentication support, comprehensive folder management via new `--list` and `--folder` options, and fixes critical configuration parsing bugs. Key resolved issues include:

1. Critical configuration parser regression (since Git 2.46.0) where fields were incorrectly NULLed out (6d1f198f34) - fixed with proper field-specific cleanup that restored functionality. The bug manifested when processing unrelated configuration options (`imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`) which incorrectly NULLed out `cfg->folder`. The minimal 4-line fix in `git_imap_config()` properly isolates field cleanup.

2. OAuth2.0 support via OAUTHBEARER (RFC-standard) and XOAUTH2 (Google-proprietary) protocols, implemented for both OpenSSL and libcurl backends with helper functions `oauthbearer_base64()` and `xoauth2_base64()`. Includes RFC5801/RFC7628 compliance for OAUTHBEARER and proper Google XOAUTH2 formatting. Patch 4/10 specifically adds `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` capability flags, auth method dispatch in `imap_open_store()`, and credential handling in `setup_curl()`. Documentation covers Gmail and Outlook usage, noting Outlook only supports XOAUTH2.

3. RFC 4616-compliant PLAIN authentication now fully implemented for both OpenSSL and libcurl backends via new `plain_base64()` helper, implementing proper NUL-delimited "user\0pass" format before base64 encoding. The latest patch adds OpenSSL backend support with new `AUTH_PLAIN` capability flag and `auth_plain()` handler, completing the previously curl-only implementation. Memory management includes proper cleanup paths and maintains backward compatibility when OpenSSL support is compiled out.

4. CRAM-MD5 memory fixes including complete error paths (now with additional leak fix when socket write fails) and new `try_auth_method()` helper for centralized authentication failure handling. The latest patch adds proper `free(response)` in `auth_cram_md5()` error path when `socket_write()` fails and moves OpenSSL availability check from hard `die()` to graceful failure path in `try_auth_method()`. When CRAM-MD5 is requested without OpenSSL support compiled in, users now get a clear error message rather than an abrupt crash.

5. New `--folder` command-line override with proper memory management and documented precedence rules (CLI overrides config but config remains fallback).

6. RFC6154-compliant `--list` option showing available IMAP folders via new `list_imap_folders()` and `curl_list_imap_folders()` functions.

7. User experience improvements: destination folder display during send progress, port numbers in credential requests (host:port format), and standardized error message formatting throughout `imap-send.c`.

Technical implementation spans configuration parsing, authentication handlers, base64 utilities, IMAP protocol handling, and folder management. The series has undergone thorough review from Jeff King, Eric Sunshine, Brian Carlson, Junio Hamano, and Ben Knoble. V16 reorganization placed authentication methods first followed by folder features, with UI polish last. Junio approved core changes while noting minor log message capitalization nits, which were fixed in v16.

Current status: Junio confirmed capitalization fixes and approved early patches (bugfixes and authentication). Only final review needed on last 3-4 feature patches (folder listing/destination display). V17 is a clean rebase with no functional changes, indicating series is in final stages with all substantive feedback addressed and only minor organizational considerations remaining before potential merging. The configuration parser regression fix (now more precisely documented) was particularly critical as it made `git-imap-send` unusable since Git 2.46.0, though the impact was mitigated by `git-send-email` being the more common alternative. The latest CRAM-MD5 memory leak fix and OpenSSL dependency handling completes the authentication error path coverage, while the new PLAIN authentication patch completes the OpenSSL backend support matrix.